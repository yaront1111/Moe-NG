# task-5611791337894212a277600a0768f1f9 — supply-chain failures report their own reason

## What landed (3 production lines, 206 test lines)
1. `scripts/release/supply-chain.mjs:198` — the `observeTools` falsy guard returned
   `FROZEN_INSTALL_FAILED`; now returns **`TOOLCHAIN_OBSERVATION_FAILED`**. The genuine
   frozen-install guard at `:211` is byte-identical.
2. `scripts/release/release-subject.mjs:29` — `TOOLCHAIN_OBSERVATION_FAILED` added beside
   its sibling `TOOLCHAIN_IDENTITY_MISMATCH`. Set is now 20 members, nothing else moved.
3. `scripts/release/supply-chain.mjs:184-188` — `cleanRoots` got a per-root `try/catch`
   with a stderr diagnostic. See `mem:gotcha-cleanup-in-finally-replaces-the-real-outcome`.

Both files are **net 0 lines** (256 / 224 by `grep -c ''`) — the cleanup comment is a
trailing comment on the signature and the try/catch is one line, deliberately, because
supply-chain.mjs was already 6 over the 250 target.

## Verification
`pnpm typecheck:release` EXIT=0; `node --test tests/integration/release-supply-chain.test.mjs`
**59/59 pass**, exit 0, run from PowerShell (`mem:msys-tar-drive-letter-breaks-integration-gate`).
`pnpm typecheck:packaging` EXIT=0. Two mutation drills, both red, both restored and
sha256-verified.

## Traps this task contains for the next agent
- **`code` is vacuous here.** `releaseRefusal` returns the SAME `code`
  (`RELEASE_SUPPLY_CHAIN_REFUSED`) for every refusal in this subsystem. `reason` is the
  discriminator, `refusedBy` is the layer. A test asserting `code` passes before and after
  any reason fix.
- The cleanup test's throw trigger is a directory planted at `${root}.tar` (EISDIR), not a
  held handle — a held handle does NOT throw here, see
  `mem:gotcha-held-fd-does-not-block-rmsync-on-windows`.
- My owned paths were swept into a **foreign whole-tree commit**, `7263d13
  feat(task-cbc42f3383444c05a20c6bf56129e8c8)`. `git status`/`git diff` are clean for them.
  Review with `git diff b03da5a..HEAD -- scripts/release/supply-chain.mjs
  scripts/release/release-subject.mjs tests/integration/release-supply-chain.test.mjs`.
  Committed bytes verified identical to gated bytes; drills were not captured.

## Open findings, deliberately NOT fixed (DoD forbade touching the closed set)
1. `releaseRefusal(reason)` takes a plain string and never validates it against
   `RELEASE_REFUSAL_REASONS`. Live proof: **`SOURCE_ARCHIVE_FAILED` is emitted at :61, :63
   and :208 but is not a member of the array.** The frozen set is documentation, not
   enforcement; a typo'd reason ships silently. This is the highest-value follow-up.
2. `:190` returns `OUTPUT_PATH_INVALID` for ANY `validInput` failure — a relative
   `repositoryRoot`, a non-string `platform`, or a wrong key set all get blamed on the
   output path. `RELEASE_INPUT_INVALID` exists in the vocabulary and is emitted nowhere.
   Same wrong-reason-code family as this task; a clean next slice.
3. `:240` `catch { EVIDENCE_WRITE_INTERRUPTED }` swallows ANY exception in the whole try
   body — a throwing `readSourceFile` reaches it (my sweep drives exactly that).
4. `RELEASE_INVENTORY_EMPTY` is returned when componentCount/templateCount merely *differ*
   from 5/3, not only when empty.
