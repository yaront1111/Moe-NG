# Handoff: Anti-blocking admission QA-reopen fix

All seven approved steps are complete and the owned admission subtree is clean.

## Fix
- `admitGraph` now computes the landed hard-edge counterfactual analysis once and routes every validated HARD-edge necessity witness through `evaluateNecessityClaim`.
- Only a witness claim and `contract.necessity.truthClass` that both equal `DAEMON_VERIFIED` can produce an admitted result.
- Missing, malformed, AGENT_REPORTED, OBSERVED, HUMAN_APPROVED, UNKNOWN, HELD, or claim/contract-mismatched evidence fails closed with one edge-bound `ADMISSION_HARD_DEPENDENCY_UNPROVEN`.
- `entryFor` now supplies explicit edge-bound DAEMON_VERIFIED witnesses; no fixture depends on omitted evidence.
- The seeded property now exercises `admitGraph`, not only the helper, across 96 non-vacuous truth permutations.

## Evidence
- TDD RED: focused pass suite exited 1 with 21 failures after explicit witnesses exposed the old permissive parser/default.
- GREEN focused suite: 2 files / 86 tests, twice identical.
- Fresh required command: `pnpm --filter @moe/scheduler test` -> exit 0, 26 files / 364 tests.
- Fresh scheduler typecheck -> exit 0.
- Fresh repository regression: `pnpm typecheck && pnpm test` -> exit 0, 111 files / 1575 passed / 1 skipped.
- Production files remain <=250 lines; forbidden nondeterminism/package-import scan has zero hits.
- Independent adversarial reviewer found no blocker.

## Commit attribution
Concurrent Moe auto-commit `baa8012` swept the completed changes to `admission-pass.ts`, `admission-pass.test.ts`, and `admission-fixtures.ts` under a foreign policy-task message. This was documented in #general and not rewritten/reset. Explicit owned commit `b27de86` contains only `admission-invariants.test.ts`. Effective bytes since original admission commit `f221eb1` are the four paths above.