# task-fc6581042426444a826981943f441908 handoff

## State
- Codec implementation is complete; steps 1-5 COMPLETED, step 6 IN_PROGRESS and task BLOCKED only on the exact daemon package gate.
- A foreign whole-tree commit `f4966b5` (task-6cbff010) swept all three owned paths. Do not amend/reset/recommit merely for attribution. `git diff 9d60091..HEAD -- apps/daemon/src/telemetry/provider-run-codec.*` is the QA review surface.
- Owned paths are clean at handoff.

## Shipped surface
- `provider-run-codec.ts` (248 physical lines), `.js` exact bridge, and 27-test suite.
- Wire: UTF-8 `${PROVIDER_RUN_RECORD_VERSION}\n`, u32be canonical-body length, canonical JSON full record with computed `recordDigest`, then u32be 64 and lowercase hex digest.
- Digest domain is canonical sorted-key record with `recordDigest` excluded; caller digest ignored.
- Guard A compares recomputed reduced-domain digest to the tail. Guard B MUST compare canonical BODY bytes only, not the sealed tail, otherwise the guard-A mutation becomes vacuous.
- Canonicalization refuses undefined, holes/array expandos, non-finite values, accessors, symbols, non-plain objects, proxies, cycles and oversize; null/absent stay distinct. It preserves own `__proto__` as data through a null-prototype projection. Return record is a detached recursively frozen JSON snapshot.
- Decode traps are contained and return UNKNOWN/BYTES_MALFORMED.
- Codes/layer come only from imported arrays; no local stable-code literal redefinitions.

## Evidence
- Official Node 24.16.0 downloaded to `/tmp/moe-node-24.16.0`, tarball verified against official SHASUMS256. Focused command under it: 1 file/27 tests passed, exit 0.
- Six mutation drills recorded in step 5: independent guards, digest domain, caller digest, layer, outcome all reddened named assertions and restored.
- Bridge expected/actual sha256 `7b6742148af72b13515f875e11ef61c2831ab964773b6ef2192a4b28cf01d089`.

## Remaining
- Step 6: run `pnpm --filter @moe/daemon typecheck` and `pnpm --filter @moe/daemon test` separately under Node 24.16.0; both must exit 0 and test must show nonzero counts. Current moving foreign red is recorded in Moe block reason (latest typecheck under orchestrator/agent-spawner.test.ts; latest test has authorization/goal/bridge/timeouts).
- Step 7: load adversarial skill, review, record foreign-commit attribution, Clause-1 consumer task `task-1a7ff170ee544a3a8a10962c25e2ca5b`, and sibling `task-ea27beb6e1954d0e9dba8ad49cc1641e`; complete task after fresh gate.
- Do not touch provider-run-contracts/refusals or apps/daemon index.
