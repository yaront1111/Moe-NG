# task-fc6581042426444a826981943f441908 handoff

## Status
- APPROVED by QA (qa-ce3ba361) on 2026-08-16 at HEAD `1c9e46c`. All 7 steps complete.
- QA review surface: `git diff 9d60091..HEAD -- apps/daemon/src/telemetry/provider-run-codec*`.
- Owned paths clean; on-disk bytes verified byte-identical to `git show HEAD:<path>` for all five files.
  Original codec bytes were swept into foreign whole-tree `f4966b5`; validation changes into `d447a34`;
  the outcome assertion is explicit-path task commit `7da2a1a`. Do not amend/reset/recommit for attribution.

## Shipped surface
- `provider-run-codec.ts` (246 lines), `provider-run-codec.validation.ts` (231), both exact LF bridges, 31 codec tests
  (63 across the three telemetry suites).
- Digest domain is canonical sorted-key JSON over the record with `recordDigest` excluded. The encoder ignores
  caller digest, computes and attaches its own, and seals a repeated tail digest.
- Decode guard A recomputes the reduced-domain digest against the TAIL frame; guard B compares canonical BODY bytes
  with input body bytes. Independent: A alone misses a re-ordered-key body, B alone misses a forged tail.
- Descriptor-safe validation runs before sealing/adoption. Missing required identity/fact fields ->
  RECORD_MALFORMED/REFUSED; nested invalid -> FIELD_INVALID/REFUSED; parsed-but-unreadable -> RECORD_UNREADABLE/UNKNOWN
  before either digest guard. All at imported layer `PROVIDER_RUN_CODEC`.
- Nullable observation/refusal properties may be absent; present-undefined refuses; null stays distinct data.
  Extra canonical data (including own `__proto__`) is encoded, never silently dropped.
- Returned records are detached, recursively frozen JSON snapshots.

## QA evidence (re-run independently, not taken from the worker's summary)
- `pnpm --filter @moe/daemon typecheck` -> **EXIT 0** (full package, not scoped).
- `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/telemetry` ->
  **EXIT 0, Test Files 3 passed, Tests 63 passed** (matches the recorded `task.verification` exactly).
- Full `pnpm --filter @moe/daemon test` -> EXIT 1, `Test Files 2 failed | 95 passed (97)`, `Tests 4 failed | 1990 passed (1994)`.
  Foreign red: `orchestrator/agent-spawner.test.ts` (3) and `work/foundation-attempt-windows.test.ts` (1).
  Path-attributed: neither file contains a single occurrence of `telemetry` or `provider-run` (grep count 0/0),
  and NOTHING outside the codec directory imports `provider-run-codec`. Delta ∩ owned paths = EMPTY.
- **Six QA-run mutation drills**, byte-restored each time (sha `57af635e…`, restored=YES, FINAL==ORIG):
  - D1 disable ONLY guard B -> exactly 1 red: `refuses a non-canonical encoding … guard B` ("codec accepted the value").
  - D2 disable ONLY guard A -> red: `refuses bytes whose carried digest does not match the record: guard A`.
  - D3 fold `recordDigest` into its own domain -> red: `excludes recordDigest from the bytes it digests` (+3).
  - D4 trust caller digest -> red: `overwrites a caller-supplied recordDigest …`.
  - D5 `LAYERS[1]`->`[2]` -> 22 red, on the LAYER assertion: `expected 'PROVIDER_RUN_LEDGER' to be 'PROVIDER_RUN_CODEC'`.
  - D6 UNKNOWN->REFUSED -> 11 red, on the OUTCOME assertion: `expected 'REFUSED' to be 'UNKNOWN'`.
- DoD 4 grep: zero bare code/layer literals in either production file; positive control — the same pattern hits 29x
  in the test file, so the grep is not vacuous. `CODE` maps `PROVIDER_RUN_LEDGER_CODES[0,1,2,6,7,8]`, index-selected
  but pinned by exact-string assertions, so a slice-1 reorder reddens.
- Bridges byte-exact `export * from "./<base>.ts";` + LF, compared with `od -c` against the sibling contracts bridge.
- `apps/daemon/src/index.ts` carries zero `provider-run` occurrences; index-surface.test.ts green (the `61` pin is stale,
  it now reads 85 after peer publications, and this task never touched it).
- `provider-run-contracts.ts` shows 40/32 vs 9d60091 — that is slice-1's OWN adversarial aggregate-id hardening,
  swept by foreign whole-tree `f4966b5`. Not this task's bytes; codec depends on none of the changed symbols.

## Composition
- Pure codec consumer: task-1a7ff170ee544a3a8a10962c25e2ca5b (durable provider-run ledger), verified to exist
  on the board (status BLOCKED). Clause-1 option (a) satisfied by naming it; the codec currently has zero importers.
- Sibling task-ea27beb6e1954d0e9dba8ad49cc1641e was human-archived; `@moe/runner.normalizeProviderUsage` owns normalization.
- The ledger MUST inherit the digest-domain decision (recordDigest excluded) rather than include it in its own input.

## Known soft drift (not defects, not blocking — for the ledger slice to consider)
- `provider-run-codec.validation.ts:28` re-declares `LAUNCH_KINDS` as literals; the producer union
  `ClaudeLaunchResult["kind"]` is type-only with no runtime array, so there is nothing to import. A fifth arm added
  upstream would make validation silently refuse valid records.
- Same shape at `:203-204`, where `"CONTRACT"`/`"MEASUREMENT"` are inline although `@moe/scheduler` exports
  `MEASUREMENT_ISSUE_LAYERS`. They are per-arm discriminators, so the array cannot be swapped in wholesale.
