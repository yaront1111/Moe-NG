# QA handoff — task-2d9696160e674f26a8d422c45829d80e

Approved on 2026-08-14 after reopen repair `adf8f36`.

Verified current committed production bytes and repaired rejection paths:
- `boundSnapshot` validates every leaf before `digestOf`, so BigInt/cycle/function/symbol/undefined/invalid-count inputs refuse as `EXPANSION_BINDING_REQUEST_MALFORMED` at `REQUEST`/`BRIDGE` instead of throwing or disappearing from canonical bytes.
- `activeHoldOf` replays the reducer creation command and then exact-compares the presented hold to the replayed state with own-data readers; forged top-level/nested/accessor/extra/list bytes refuse as `EXPANSION_BINDING_HOLD_STATE_MISMATCH` at `HOLD`/`BRIDGE`.
- Production bridge remains the integration producer; fairness opportunity is validated and never synthesized; current goal/epoch/hold/run authority is checked; core admitted facts partition explicit leaves from the one scheduler-only digest.
- Production source counts checked: expansion-binding.ts 291, expansion-binding-facts.ts 228, fairness-evidence.ts 210, scheduler index.ts 332; all below 400. New `.js` bridge is exact one-line LF. Design hash remains `1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191`.

Fresh QA gates:
- `pnpm --filter @moe/core test` — 28 files / 579 tests passed.
- `pnpm --filter @moe/scheduler test` — 42 files / 1219 tests passed.
- `pnpm exec vitest run tests/integration/expansion-protocol.test.ts` — 1 file / 148 tests passed.
- `pnpm typecheck` — exit 0.
- `pnpm test` — 248 files / 5386 passed / 1 skipped.
- `pnpm verify:foundation` — 30 files / 487 passed.
- `pnpm verify:store` — 39 files / 444 passed.

Independent mutation: neutralized the production `matchesTrusted(value, replayed.state)` gate. The named `parentRunRef` forgery test failed because the production bridge returned success. Restored the file byte-exact (`git hash-object` matched HEAD `1f767077e5862d6658c29c5778f3d63d5f1aa0e7`), reran the named test green, and confirmed no owned worktree diff.