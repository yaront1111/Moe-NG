# task-2159fa19 — Publish Codex provider runtime surface (commit bb97c0a)

Landed the curated Codex seam at the `@moe/runner` root: `surface/codex-surface.ts`
(190 lines) + LF bridge, wired into `index.ts` as the 5th `export *` surface.

## Numbers a later task will need

- Root runtime values **165 -> 195**. `index-surface.test.ts:218` literal is now `195`.
- Published: **30 values + 42 types**. Withheld: 9 internals + all 7 Codex `MAX_*`.
- `codex-surface.js` sha256 `391ead1eb6d40e071dc6e31d36afc443abab0a1bbadb682ee5bf044dd9fa1d95`.

## The finding that shaped the design — 26 collisions, in two opposite families

Both planning extractors under-counted. Re-derive form-completely; the single-line
`export type { X } from "..."` form is the one that gets missed.

- **FAMILY A (16)** — provider-neutral names `providers/codex/` REDECLARES and
  `claude-surface.ts` already roots (`ObservationClock`, `PlatformIdentity`,
  `ProviderRuntimeObservation`, `MoeEffectIdentity`, `OBSERVATION_TRUTH_CLASSES`,
  `buildProviderRuntimeObservation`, ...). Published under `Codex*` ALIASES.
- **FAMILY B (10, not 9)** — `recovery-inventory-surface.ts` already publishes
  these from the same modules: the 9-name `codex-capabilities` block **plus
  `ProbeCodexRuntimeInput`**. NOT republished; that file is not the seam's to own.

**Blanket rule, adopt it if you extend this seam:** every name published here must
carry a Codex token. ESM DROPS a name supplied by two star paths instead of
erroring, so a missed alias is invisible except as a MISSING key in the
`Object.keys` equality — and type collisions have no runtime signal at all.
That is why 3 non-obvious names are also aliased: `rendererEnvelopeIdentity`,
`renderAdvisorySkills`, `MIRRORED_SKILL_RENDERER_INPUT_VERSION` (+ the three
`MirroredSkill*` types). The prefix marks Codex-slice provenance; the underlying
literal is still `moe-skill-renderer-input/1`.

## Production facts I got wrong and had to measure

- `observationDigestInput` returns `Record<string, unknown>` — the digest INPUT,
  not a digest string.
- `CODEX_PROOF_METHODS` has **12** members (not 5). `CODEX_CAPABILITIES` 12,
  `CODEX_CAPABILITY_STATUSES` 2, `CODEX_CONTEXT_POLICIES` 2.
- A `MirroredSkillFile.sha256` must recompute over the decoded `contentBase64`,
  or the renderer refuses "skill file <path> digest differs".
- **`renderAdvisorySkills` does NOT check `rendererInputVersion`.** That gate is
  `renderCodexContext.validateSnapshot` alone. The two published entry points do
  not share a gate — pin WHICH layer refused, or either can answer for the other.
- Re-export chains inside `providers/codex` are partial: `codex-probe` re-exports
  capabilities, `codex-render` re-exports render-skills, `codex-stream` re-exports
  stream-anomalies, and **none re-exports observation**.

## Verification shape worth copying

`apps/daemon` is the only package with a real workspace dep edge on `@moe/runner`.
Compiled bare-specifier probe there, PAIRED: positive exits 0; negative importing
the 9 withheld names fails with 7x TS2305 + 2x TS2724, while a published control
name in the same file resolves cleanly (so a total resolution failure, which would
be TS2307, cannot be mistaken for withholding).

Do NOT pass files on tsc's command line — TS5112 refuses the tsconfig, and
`--ignoreConfig` then drops `types:["node"]` and `lib:ES2024` and buries you in
unrelated TS2591/TS2550. Give each probe a tsconfig that EXTENDS the repo base.

Bridge drill semantics: see `mem:gotcha-vitest-follows-an-existing-js-bridge-but-not-a-missing-one`.

## Open / handoff

- **Acceptance consumer named, not landed: `task-22cfca91c5134b24aaf3e5734444fb93`**
  (Portability shadow gate). Nothing in production imports this surface yet; task
  rail 2 forbade adding a consumer here.
- Three deliberate couplings, all documented in-test: the `195` literal moves with
  any peer root export; the surface-directory test pins exactly 5 files so a 6th
  surface module reddens it; the Family A assertion depends on claude-surface
  continuing to publish its three provider-neutral values.
