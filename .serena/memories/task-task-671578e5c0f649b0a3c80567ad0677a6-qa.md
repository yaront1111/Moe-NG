# QA verdict: APPROVED 2026-08-09 by qa-58b24ffb

Task: "Complete the J1 command path: graph activation and final acceptance" (M1 exit blocker).
Worker handoff: `mem:task-task-671578e5c0f649b0a3c80567ad0677a6-handoff` — accurate, no claim
failed re-verification.

## Gates re-run by QA, true exit codes (not `| tail` masked)

| leg | result |
|---|---|
| owned suites (`src/goals src/planning src/bootstrap`) | **5 files / 77 tests, exit 0** |
| full daemon suite | **464 tests passed, 0 failed**; 1 FILE fails to COLLECT |
| `pnpm --filter @moe/daemon typecheck` | exit 1 — foreign only |
| `pnpm -r --no-bail typecheck` | exit 1 — foreign only |

**Every red is path-attributed to untracked foreign in-flight work**, intersection with this
task's owned paths (`goals/**`, `planning/**`, `bootstrap-contracts.ts`) is EMPTY:
- 14:25 run: `src/review/review-lineage.test.ts` TS6133 ×2, `src/review/review-services.ts`
  TS2304 `EMPTY_REVIEW_LINEAGE` (J4 agent; `review-services.ts` mtime 14:26:36 — edited DURING
  the QA session).
- 14:30 run: review errors GONE, replaced by `src/recovery/continuation-service.test.ts`
  TS2307 / `Cannot find module './continuation-service.js'` (J3 agent mid-TDD, production
  module not written yet).

Both directories are `??` untracked and post-date the task's last commit (13:49:52).
Trick that isolates it cheaply: `tsc --noUnusedLocals false` flushes the *other* errors hiding
behind TS6133 so you see the full foreign set in one run.

## Mutation drills QA ran itself (worker's claims not taken on trust)

Each neutralised ONE operand with a constant; each restore verified by `git hash-object` against
the pre-drill `git rev-parse HEAD:<path>` blob sha, never by `git status`.

| drill | red arm | files/tests |
|---|---|---|
| `goal-services.ts:82` `zeroAuthorityWitness === null` -> `false` | "refuses an acceptance carrying no zero-authority proof" | 1/1 |
| `approval-activation.ts:45` `graphApprovalRef` -> `"approval-forged"` | "binds the activation to THIS approval rather than to a caller-named one" | 1/1 |
| `approval-activation.ts:61` `expectedVersion` -> `1` | "refuses a stale expected goal version" + "refuses a second approval" | 1/2 |
| **atomicity**: inserted a second `commitAccepted` in `decideApproval` before `activateInitialGraph` | **14 arms red incl. "surfaces the CORE's code when the activation half is refused, and commits nothing"** | 3/14 |

The atomicity drill is the load-bearing one — it proves DoD 4 is not vacuous. Note drill 1
reddens on the LAYER (`DAEMON_INGRESS` -> `CORE_REDUCER`), not on "it refused": the core would
still refuse the missing witness. A layer-blind assertion would have stayed green.

## DoD 1 proven at the real runtime boundary

`node --experimental-strip-types -e "import('@moe/daemon')"` run from `apps/daemon` (self-
reference via `exports: {".": "./src/index.ts"}`; it does NOT resolve from the repo root) ->
31 exports, `GOAL_HANDLERS ['goal.create','goal.close']`, `PLANNING_HANDLERS
['approval.decide','plan.propose']`, `BOOTSTRAP_COMMAND_KINDS` length 10, `runBootstrapCommand`
a function. Vitest resolution alone would not have proven this.

## Disclosed, judged NOT rejection-worthy

1. `febf6a7` touches 4 `bootstrap/` files beyond the declared owned paths. **Forced**:
   `COMMAND_PREREQUISITES` is `satisfies Readonly<Record<BootstrapCommandKind,...>>` so a kind
   append without an entry is a compile error, and DoD 6's hand-written pins live in
   `bootstrap-services.test.ts` + `bootstrap-durability.test.ts`. Additive only, nothing
   clobbered, all inside `apps/daemon`.
2. The planning production half sits in FOREIGN commit `878538b` (swept by task-fa96b81c's
   completion hook) — see `mem:moe-finished-task-may-have-no-commit`. Review with
   `git diff 878538b~1 HEAD -- apps/daemon/src`.
3. Witnesses are caller-supplied JSON whose `truthClass` the caller asserts. That is the
   codebase-wide witness model (`goal.create`'s `projectReadyWitness` is identical), the core
   owns `strongTruth`, and the DoD asks only for "the witnesses the core's validators demand".
   Not a hole introduced here.

## Scope warning to carry forward

This approval closes **J1's command path only**. `mem:project-m1-exit-gate-gap-2026-08-09`
stays accurate: J3 (launcher/restart composition) and J4 (review persistence) remain missing —
and as of this session both are actively mid-flight in the shared tree, which is exactly why the
repo-wide gates are red. **A green J1 is not a green Foundation canary.**
