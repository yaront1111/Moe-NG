# QA verdict — task-93b0314e09f248118e21f92699989468 (planning-run expansion submission)

**APPROVED on reopen 1**, 2026-08-09, by qa-5be1a8d6. Reject #1 verdict and the full drill matrix
for both rounds are in the task comments. The worker's own handoff memory
(`task-task-93b0314e09f248118e21f92699989468-handoff`) is accurate and was deliberately left
intact — read it for the production-side detail.

## What reject #1 was, and why the fix is tests-only

Reject #1 was NOT a production defect. `sameHold` in `planning-run-submission.ts` was correct and
**unasserted**: all three `MALFORMED_HOLDS` fixtures are schema-invalid, so
`validExpansionProposeCommand` refused first and the identity guard behind it was never reached.
Cutting `HOLD_IDENTITY_KEYS` from 10 keys to `["holdId"]`, or deleting both `workerHandoff`
comparisons, each left the suite fully green. See
`mem:gotcha-identity-match-guard-shadowed-by-schema-layer`.

The reopen-1 delta is therefore `git diff 14e9af9..HEAD -- packages/core/src/planning` = 3 files,
158 insertions / 4 deletions, **zero production bytes**. Correct response to that reject.

## Verified gates (re-run by QA, unnarrowed, foreground)

- `pnpm --filter @moe/core typecheck` exit 0.
- `pnpm --filter @moe/core test` = **23 files / 363 tests** (baseline 22/359). Re-run after every
  drill: still 23/363.
- All 7 owned paths `git show HEAD:<p> | cmp -` MATCH. Owned tree porcelain EMPTY.
- Lines: reducer 215, submission **250 (zero headroom)**, results 146, fixtures 230,
  reducer.test 228, expansion.test 240, expansion-cancel.test 106.

## Drill matrix run by QA on the final bytes

| # | mutation | result | reddened |
|---|---|---|---|
| D1 | `HOLD_IDENTITY_KEYS` -> `["holdId"]` | RED 1/363 | expansion.test identity sweep |
| D2 | delete both `workerHandoff` compares | RED 1/363 | same sweep |
| D3 | drop `\|\| state.facets.livePlannerEffect` from `cancel()` | RED 2/363 | expansion-cancel draining fence |
| D4 | add `"CANCELLED"` to finalize transitions | RED 3/363 | expansion-cancel competing schedule |
| D5 | drop `"APPROVED"` from `goal.cancel` transitions | RED 2/363 | expansion-cancel goal-authorized sweep |

Right-reason check on D1/D2: panic reads `expected true to be false` at `expectIllegal`
(`result.ok`) — the run ACCEPTED a deviating proposal. A fixture pre-check failure would read
`expected false to be true`. See `mem:gotcha-drill-red-direction-distinguishes-right-reason`.

3 of the 4 cancel-file tests bite. The 4th (`refuses expansion-only planning.cancel`) is
double-fenced by a hard `illegal` at `planning-run-reducer.ts:178` and cannot be mutated without a
TS break; it still pins exact code + layer per lifecycle.

## Why the identity sweep survives future edits

`IDENTITY_FIELDS` in `planning-run-expansion.test.ts` is HAND-WRITTEN and asserted `toEqual`
against `Object.keys(CHANGED_HOLDS).sort()` — a whole SET, not a count, and deliberately not
derived from the production `HOLD_IDENTITY_KEYS`. A generated sweep would shrink silently under D1
and could never redden (`mem:qa-generated-table-cannot-police-its-own-generator`). Each case also
asserts `validExpansionHoldBinding(deviation) === true` against the production predicate, which is
what keeps the schema layer from shadowing the guard again.

## Disclosed, not held against this task

Repo-wide `pnpm typecheck` fails only in `apps/control-room` (`document-dossier-state.test.ts(42,19)`
TS2352; `document-preview-data.ts` TS2724 x2 + TS2739). Zero intersection with `packages/core`.
Reopen-0 bytes landed inside foreign whole-tree commits `5172b3e`/`14e9af9`/`3eaaca1`; per the
project custom rail that is never a rejection reason — verified by base-ref diff + cmp instead.

## Hazard for the next owner of this file

`planning-run-submission.ts` is at **250/250** against the hard cap enforced by
`planning-source-size.test.ts` (sweeps every `.ts` in `src/planning`, no allowlist). Anyone
extending propose/finalize/cancel must SPLIT the file before adding a line.
