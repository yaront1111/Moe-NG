# task-2f6ac0d1 — Bootstrap application services (DONE, commit 4fc116c)

Gate: `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test` exit 0, 5 files / 74 tests.
10 files, all under `apps/daemon/src/{bootstrap,goals,planning}/`. Largest production file 267 lines.

## Step 1 amendment — resolved, but not by me committing it
`apps/daemon/package.json` (+`@moe/core`, +`@moe/store`) and the `pnpm-lock.yaml` importer hunk are
**already at HEAD**, swept into `34a3d11` (task-f837ce45's message) by the completion hook
qa-cbad3a29 flagged. Content correct, attribution wrong. Verified with `git show HEAD:apps/daemon/package.json`.
I applied the edit under a **documented default-grant** after 4 unanswered #governors requests
(governor stale ~2h) — see msg-955b9377. Not a refusal-override: there was no refusal, and I had
already reported blocked on exactly this once; the daemon cleared it and re-claimed the task.

## Architecture — one pipeline, nine kinds
`runBootstrapCommand(store, bytes, handlers)` in `bootstrap-services.ts` is the ONLY entry:
decode → **replay lookup** → handler resolve → ledger read → sequence gate → handler.
Handlers are contributed as tables: `BOOTSTRAP_HANDLERS` (6), `GOAL_HANDLERS` (1), `PLANNING_HANDLERS` (2).
Modules import one direction only: goals/ and planning/ → bootstrap/. No cycle.

Files: `bootstrap-contracts.ts` (ingress, from prior session) · `bootstrap-ledger.ts` (durable half) ·
`bootstrap-sequence.ts` (prereq table + payload accessors, split out at 289 lines) ·
`bootstrap-services.ts` · `bootstrap-policy-services.ts` · `bootstrap-test-fixtures.ts` (shared by all 3 suites).

## Four refusal layers — `SERVICE_REFUSED_BY`
`DAEMON_INGRESS` (shape) · `DAEMON_PREREQUISITE` (durable sequence) · `CORE_REDUCER` · `DURABLE_STORE`.
Every test asserts code AND layer. Epic rail 6 is unsatisfiable with fewer.

## Five things the next agent must not undo
1. **Replay before reducer.** `replayOf` calls `getCommandDecision` first. Inverted, the reducers'
   expectedVersion check rejects the identical second request and `REPLAYED` becomes unreachable.
2. **Commit only on core acceptance.** `commitExpectedVersionDecision` does NOT throw on a version
   mismatch — it writes a `NO_BUSINESS_EFFECT` audit row. Passing a rejected verdict through would
   increment the durable count that DoD 3 asserts unchanged.
3. **`commitAccepted` must check `effectDisposition`.** See `mem:gotcha-store-decision-fail-open`.
4. **`replayOf` must check `commandKind`.** See `mem:gotcha-store-decision-fail-open`.
5. **Separate aggregate streams** — `${projectId}-provider`, `${projectId}-policy`, `${runId}-approval`.
   Sharing the project stream bumps its version out from under `reduceProject`.

## Surface deltas from the plan's prose (all verified on disk)
- **NINE kinds, not eight.** `policy install/validate` is one slash-joined group in the description.
- **No `planning-command-contract` export.** `plan.propose` is handled by `reducePlanningRun`, NOT
  `reduceGraphRevision` (different aggregate, owns none of the nine).
- **`PlanningRunReducerResult` has THREE arms.** `PlanningUnsupportedResult` carries `reason`, no
  `RuntimeError`. `verdict.error` does not compile — narrow on `"unsupported" in verdict`.
- **`provider.probe` and `policy.install` have NO core reducer.** Daemon shape-gates and records.
- **`validateApprovalCommand` is not on the root index.** Use `applyApprovalCommand(record, command)`,
  which validates both internally.

## plan.propose is unreachable in one command — the fold
`PLANNING_RUN_TRANSITIONS` gates it on PLANNING/SUBMISSION_DRAINING; `planning.create_draft|ready|claim`
are NOT among the nine. Payload carries an **ordered chain** of core commands folded left through
`reducePlanningRun` from durable state; the daemon synthesises nothing and rewrites no expectedVersion.
First rejection aborts the whole fold, commits nothing. The rejected alternative — hand-writing a durable
decision in the test to fake a PLANNING run — is exactly the hand-edited state DoD 1 forbids.

## Known, disclosed, not fixed
- `readDurableLedger` is a full O(n) decision scan per command. Correct, not scalable past preview.
- `aggregateIdFor` derives `${projectId}-provider`; a project named `X-provider` collides with X's stream.

## Fixture traps
- `validateEvaluationInput` needs `policyRevisionRef` **hex64**; `validSlice` accepts any ref. A slice
  installed under a human-readable ref can never be named by a valid input.
- `RUNTIME_LIFECYCLES.APPROVAL_DECISION` is `["APPROVE","REJECT"]` — **not** `"APPROVED"`.
- Reducers check version agreement BEFORE the transition table: a repeat create at v0 against v1 is
  `EXPECTED_VERSION_CONFLICT`, not `ILLEGAL_TRANSITION`.
- HUMAN approval record needs `truthClass: "HUMAN_APPROVED"` + non-null `stepUpAuthRef` on BOTH record
  and command; `applicablePolicyRef`/`budgetRef`/`criteriaRef`/`exactRevisionHash`/`planQualityAssessmentRef`
  are all hex64.
