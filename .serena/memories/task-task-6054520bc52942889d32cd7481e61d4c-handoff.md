# Handoff: task-6054520b Publish the @moe/daemon command surface

Steps 1-3 by worker-064d1267 (2026-08-09), step 4 by worker-f2e587cb after the governor
unblock. Owned paths: `apps/daemon/src/index.ts` (222 lines) and
`apps/daemon/src/index-surface.test.ts` (265 lines, 46 tests).

## WHERE THE DIFF IS

No commit names this task. A concurrent completion hook swept the tree and both files
landed inside FOREIGN commit `c699422` (task-e17da1c9). Working tree == HEAD. Review with:

```
git diff 9e0f123 HEAD -- apps/daemon/src/index.ts apps/daemon/src/index-surface.test.ts
```

Same pattern as `mem:task-task-d99ca771b0c24e1a8075d6b171b3a1f5-handoff`; see
`mem:gotcha-completion-hook-commits-whole-tree`.

## What shipped

31 curated runtime exports on the `@moe/daemon` root (was 1), plus the full type closure
for bootstrap / service / work / event-stream / doctor / graph-preview. No `export *`, no
fixture module, no behaviour change — every added line in `index.ts` is an export
statement, an export-block member, or a comment.

## Why it was blocked, and what actually cleared it

Not the assertion, the RUNTIME. The curated `.js` re-exports eagerly load
`src/bootstrap/bootstrap-contracts.js`, which did not exist, so plain Node returned
ERR_MODULE_NOT_FOUND while vitest stayed 46/46 green. The architect RETRACTED task rail 5
("apps/daemon needs no .js bridge") as simply false: a package needs bridges the moment
its root re-exports local modules. Bridges were then delivered by task-f6440f26 (19 now
tracked under `apps/daemon/src`), and the stale
`expect(Object.keys(daemon)).toEqual(["evaluateGraphPreviewRequestBytes"])` was retired by
the same task.

**Do not verify this class of task with vitest.** Use the plain-Node root probe in
`mem:pattern-prove-a-published-package-root-with-plain-node`. Measured here:
root import OK, 31 exports, handler tables `object`, decoders/claim/doctor `function`,
zero fixture names leaked; and the published `evaluateGraphPreviewRequestBytes({})`
returns REQUEST_INVALID / GRAPH_PREVIEW_REQUEST_INVALID / advisoryOnly true / authority
NONE.

## Verification

`pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck`
-> exit 0. Daemon 16 files / 367 tests. All 15 projects typecheck Done.

Counts: baseline 13 files / 300 tests; `index-surface.test.ts` contributes exactly 46
(measured by running it alone), the rest of the growth is foreign.

The third leg oscillated red for ~5 minutes on apps/control-room only — three workers
mid-TDD (recovery/*.test.tsx TS2307, doctor-console TS6133, goals/supplied-actions
TS6133). `apps/daemon typecheck: Done` throughout. Waited for a genuine full-chain green,
confirmed twice. Repo-wide `pnpm typecheck` is a SHARED gate: an in-flight red in any app
blocks completion of tasks in every other package.

## Trap that nearly produced a wrong conclusion

The Node probe needs `cd apps/daemon`, and **shell cwd persists between tool calls**.
Every later repo-relative `git ... -- <path>` then matched nothing and printed empty
output — which reads exactly like "clean working tree". Always `cd` back to the repo root
before a pathspec git command.

## Follow-up for whoever owns apps/control-room recovery

`"LIVE"` is not a member of `FixtureConnectionState`; the union in
`apps/control-room/src/fixtures.ts` is `CONNECTED | LAGGING | DISCONNECTED | HISTORICAL`.
Three recovery test files hit that same TS2322.
