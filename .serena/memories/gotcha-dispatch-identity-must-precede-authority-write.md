A full dispatch/reservation identity can itself fail canonical encoding (for example, individually bounded nested template records whose combined encoding exceeds the durable payload limit). If identity encoding happens after activation, the service returns a refusal but leaves EffectActivationCommitted authority residue. Compute and validate the full identity, and compare any existing reservation digest, before invoking the activation command. Pin with a production-surface test that expects the exact FOUNDATION_ATTEMPT_RECORD_DRIFT@DAEMON_FOUNDATION_ATTEMPT refusal and zero activation/dispatch events.