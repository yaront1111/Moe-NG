# Decision ledger decomposition — implementation handoff

Task `task-16909c47419946259d3a6775b7504e41`, worker `worker-7da09526`.
Delivered as commit `9c87dc6`, 9 files, +681/-415. Architecture and invariants
live in `mem:decision-ledger-decomposition`; this note covers process facts.

## Plan deviations (both accepted under the <=250 rail, recorded in step notes)

The plan named 7 owned paths / 3 new modules. Shipped 9 paths / 4 modules:

1. Step 2's single `decision-ledger-canonical.ts` measured ~303 honest lines, so
   row persistence was split out into `decision-ledger-record.ts` (+ `.js`).
2. `DecisionPlan` / `planDecision` / `lockedDecisionProposal` were placed in
   canonical rather than transaction. Without that move the transaction module
   landed at exactly 250 with zero headroom; it is also the better seam
   (transaction then holds control flow only).

Final counts: ledger 126, canonical 236, record 183, replay 55, transaction 201,
four `.js` bridges at exactly 1 line each.

## Evidence trail (reproducible)

- Baseline and final gate both `18 files / 112 tests` passed; full repo sweep
  `62 files / 662 passed, 1 skipped`.
- Zero test edits. HEAD blob hashes recorded in the step-1 note for the five
  command-decision/ambiguity suites, helpers, and the race worker.
- SQL templates proven string-equal to HEAD; `reconcileHistoricalDecision` and
  the whole `DecisionLedgerCore`/`createDecisionLedgerCore` tail proven
  diff-clean; string-literal set diff proved no stable error text changed.

## Notes for QA

- The only intentional non-verbatim structural change is that
  `classifyDecisionFailure` RETURNS the error and the catch throws it, instead of
  throwing in place. Code, message, and `cause` are identical; the Error is just
  constructed one frame deeper. Nothing observes construction depth.
- Repo has no lint tooling (no eslint/biome config, no lint script), so
  `typecheck && test` is the entire gate.
- Foreign working-tree changes (`.codex/`, `.moe/`, `packages/core/`,
  `packages/skills/`, `pnpm-lock.yaml`) were left uncommitted and untouched;
  the index held only owned paths before the pathspec commit.
