# Control-room journey gate — DELIVERED, 3 of 20, deliberately

`task-667b1085b3e04915a88336c7424045a1`. Worker `worker-a2c7f85f`, 2026-08-10.
Evidence commit **`0b1d7a7`** (plus `b24d2fd`). Both gates exit 0.

```
pnpm test:e2e         -> 4 files / 59 tests passed     (Node lane; the task's
                                                        Verification field names this,
                                                        but it is NOT Playwright)
pnpm test:e2e:browser -> 10 passed (9.7s)              <- DoD 4 is satisfied by THIS
```

## What landed (`tests/e2e/control-room/**`, 3 files, 744 insertions)

- `journey-coverage.ts` (235) — all twenty spec §12 scenarios. 3 COVERED, 17 UNKNOWN.
- `journeys.spec.ts` (341) — real Chromium over the real built bundle.
- `journey-coverage.test.ts` (183) — node-lane ledger arithmetic + rot guards.

**COVERED:** `CR-J1-002` (graph canvas never mounted), `CR-A11Y-001` (five truth
classes distinct by glyph/label/border alone), `CR-CMD-001` (every rendered action
carries a supplied command id — subset, injective, count>0 asserted first).

Plus DoD 2/3 invariants asserted as invariants, not smuggled in as scenario coverage:
fact→chip per workspace, disconnected banner never coexisting with an **enabled**
action, keyboard provenance asserted as **focus movement and focus return**,
narrow-window action parity, UNKNOWN rendering as UNKNOWN.

## The finding that changed the scope

A governor cleared `cr.runs` / `cr.resources` / `cr.banner.circuitbreaker` as "now
exist". The files exist; **none is reachable in a browser.** Full anatomy in
`mem:gotcha-file-exists-but-is-composed-by-nothing`. A file-existence check passes for
all three, which is why the ledger also asserts reachability.

## Three causes, three different owners — do not merge them

1. `SURFACE_ABSENT` (3 scenarios) → **`task-779d6804` is ARCHIVED**. Never built. Must
   be revived or re-created; nothing on the board will move it.
   (`mem:gotcha-an-archived-dependency-can-never-satisfy-itself`)
2. `SURFACE_NOT_COMPOSED` (2) → components exist, nothing mounts them.
3. `NO_DAEMON_BACKED_BROWSER_LANE` (12) → **currently UNOWNED.** `task-318379ea`
   landed a real transport (DONE) and `main.tsx` has a `?live=1` path, but no lane
   *starts* a daemon and seeds a fixture. `task-3f503779` held that and was archived
   for duplicating this task. **This gap has no owner.**

## Latency stays UNKNOWN

`REFERENCE_MACHINE_UNDEFINED`. No reference machine is named in the repo or the pinned
spec, so a p95/p99 bar certifies nothing. **No absolute latency assertion was
written.** Open human decision, deferred here from `task-ab8c9489` via
`comment-5dd36b5b` — this is still the only place it is recorded.

## Ten mutation drills, all killed by the named assertion

Two worth remembering. **A drill can redden for the wrong reason:** setting both action
buttons to one literal id fired the *uniqueness* guard, not the membership guard I was
testing — redone with unique-but-unsupplied ids to hit line 184
(`mem:mutation-drill-red-on-wrong-assertion`). And drill 5c (no `cr.action.*` rendered
at all) is what proves the sweep is non-vacuous.

Adversarial review then found **two vacuity holes in my own tests**: `null?.trim()`
returning `undefined` makes `not.toBe("")` pass, and a `Map` keyed by truth class
silently keeps only one chip per class. Both fixed in `0b1d7a7`.

## For QA

`journey-coverage.ts` is in **neither** of my commits — foreign commit `a6f8402`
(task-05bf0e0f, Broker) whole-tree swept it mid-session. Not amended, not reclaimed.

```bash
git diff $(git merge-base main HEAD)..HEAD -- tests/e2e/control-room/   # 3 files, 744 ins
```
Without that path filter the same diff shows **193 foreign files** under
`apps/`+`packages/` from other agents on this shared branch. None of it is mine;
`apps/control-room/src` was mutated only inside drills and restored (checksum + `git
diff` + `git status`). See `mem:gotcha-foreign-whole-tree-commit-preempts-your-pathspec-commit`.

**This does NOT satisfy DoD 1 in full and never claimed to.**
