# Release supply-chain gate handoff (blocked at step 4)

- Worker: worker-b6bbb60c
- Task: task-9449ce65fae544ef9809691ec079f599
- Current status: BLOCKED awaiting plan-order amendment; steps 1-3 complete, step 4 in progress.
- Production paths:
  - scripts/release/release-subject.mjs: 224 lines, strict checkJs passes, real distribution build/admission/compatibility subject.
  - scripts/release/supply-chain.mjs: ~252 lines pending a small compaction, strict checkJs passes. Exact HEAD resolution, two git archives/frozen installs, signed subject, SBOM/audit/license evidence, typed OS/doctor UNKNOWN, atomic content-addressed publication, junction defense, signal cleanup.
  - tests/integration/release-supply-chain.test.mjs: 25+ focused tests plus current step-4 package-script tests.
- package.json now preserves committed test:fault/test:security bytes from task-b5e9bd and adds uncommitted @cyclonedx/cdxgen 12.8.2, release:evidence, typecheck:release, verify:release, and Node release test discovery.
- pnpm-lock.yaml regenerated with pnpm 11.0.8. It also preserves pre-existing foreign current-manifest deltas (coordination removed @moe/core; scheduler @moe/context). Do not reset/drop them.
- TDD:
  - step-3 focused suite green 25/25 and strict checkJs green.
  - --head CLI/config/package pins green.
  - tool identity drift sweep green after adding TOOLCHAIN_IDENTITY_MISMATCH.
  - positive actual package-script test currently RED by design/order conflict.
- Exact blocker: current HEAD 3ec53d7 lacks this task's uncommitted package/lock cdxgen bytes. Real pnpm release:evidence archives exact HEAD, then frozen install refuses RELEASE_SUPPLY_CHAIN_REFUSED/FROZEN_INSTALL_FAILED. Independent temp archive reproduction: ERR_PNPM_OUTDATED_LOCKFILE, @cyclonedx/cdxgen@12.8.2 present in working package/lock but absent at archived HEAD. Pre-freeze green would mean false provenance/test knob/non-HEAD tree/early commit.
- Moe evidence: comment-81c563fefdfa42099f366ac2088492d2; report_blocked requested amendment to move freeze before actual positive script run, or defer actual positive assertion to post-freeze step 7.
- Once amended:
  1. If actual test deferred: keep package config/static tests in step4; commit exact owned paths in step5; then run actual positive package test and pnpm verify:release post-freeze.
  2. If freeze moved earlier: commit explicit owned paths only, respecting staged/foreign state.
- Remaining production issue before actual run: generateSbom currently uses cdxgen `-o -`, but cdxgen 12.8.2 writes output files rather than JSON stdout. TDD actual test should expose this after freeze. Fix with an OS-temp output file, process.execPath + locked node_modules/@cyclonedx/cdxgen/bin/cdxgen.js, --no-install-deps/--fail-on-error, read raw bytes, and cleanup. Observe cdxgen version is now parsed from real --version output.
- No generated dist evidence is committed; no corpus was read or generated; no push/reset/stash/worktree.