# task-bcea7056 — Project configuration digest codec — QA APPROVED (qa-50f0d628, 2026-08-15)

Commit `268b2ce`, 6 files / 891 insertions, all under `packages/core`. Working tree clean for owned paths; committed bytes == gated bytes.

## What landed
`packages/core/src/configuration/project-configuration-manifest.ts` (237 lines) + LF `.js` bridge + 528-line focused test. Root publishes 6 runtime values + 6 types: `createProjectConfigurationManifest`, `encodeProjectConfigurationManifest`, `decodeProjectConfigurationManifestBytes`, `PROJECT_CONFIGURATION_CODEC_CODES/_LAYERS`, `PROJECT_CONFIGURATION_SETTINGS_DIGEST_DOMAIN` (= `moe-project-configuration-settings/1`).

Digest = SHA-256 over domain tag + NUL + canonical bytes of the SETTINGS value alone. `settingsDigest` lives on the manifest, so its exclusion is structural, not a filter. Canonicalization is re-derived inside `@moe/core` on purpose: contracts keeps its helpers off the package root and a deep relative import is TS6059. Object keys sorted, arrays NOT (the limit table is positional).

## Gate re-run by QA (exact DoD command, foreground)
`pnpm --filter @moe/contracts test && pnpm --filter @moe/core test && pnpm typecheck` -> EXIT=0. contracts 12 files/416 tests, core 29 files/730 tests, typecheck `Scope: 18 of 19 workspace projects`, all Done. The repo-wide red the worker disclosed at completion (apps/control-room, packages/runner untracked peer files) had cleared by QA time — attribution was correct.

## Mutation drills run BY QA (6 + 1), each surgical, sha256-restored to e737bd6a63f1e733
- drop `policy` from digest preimage -> exactly 7 `policy.*` sweep leaves + the domain-body test red (sweep is preimage-bound, not fixture-bound)
- empty domain tag -> `derives the digest from the production domain tag` red
- delete NONCANONICAL check -> that case red
- delete decode digest recompute -> `excludes settingsDigest ... yet refuses a forged one` red
- flatten `JSON_DUPLICATE_KEY` mapping -> DUPLICATE_KEY case red
- `Uint8Array.prototype.slice.call` instead of `new Uint8Array()` -> species-trap test red
- rename the `.js` bridge -> `index-surface.test.ts` strip-types child-process probe red (bridge is load-bearing)

## Notes for the next reader
- `index.ts` 283 -> 264 lines. Verified mechanically: 266 -> 278 export entries, **zero removed**, only the 12 new. Pure same-specifier reflow.
- Out-of-owned-path edit: `supersession/supersession-engine.test.ts` 69 -> 75. FORCED — that assertion counts the whole `@moe/core` namespace, so any publication reddens it. One number + one comment line. Not scope creep.
- `task.verification.command` recorded `pnpm --filter @moe/core typecheck` instead of the DoD's repo-wide `pnpm typecheck`. Narrower than the DoD; QA ran the exact command instead. Not a reject — outcome and counts matched — but a worker should record the DoD command verbatim.
- Consumer per DoD 6 / global clause 1: **task-5dfc98fc3e7f4035a8012bd9ba032de3**. This landing clears its measured-ABSENT leg (the three create/encode/decode symbols), which in turn is one of three blockers on task-b937811e.
