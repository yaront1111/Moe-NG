# task-0c89476b78044024b07c86c0c8986bd0 — Disaster restore proof (CORE-S14)

## Outcome (2026-08-16) — DELIVERED, commit `4e0201a`
The 2026-08-09 "blocked" handoff below is now **obsolete**. Every capability it
listed as absent is on disk. 8 plan steps completed, 5 files, +1102 lines.

Gate: `pnpm test:fault` **EXIT=0**, `Test Files 10 passed (10)` / `Tests 83 passed (83)`.
Scoped: `npx vitest run --config tests/fault/vitest.config.ts disaster-restore`
EXIT=0, `Test Files 3 passed (3)` / `Tests 22 passed (22)`.

## What landed
- `packages/store/src/index.ts` — two lines: re-export `RECOVERY_ANCHOR_FAULT_POINTS`
  and `RecoveryAnchorFaultPoint`. This was the ONLY export gap (a plan-time probe
  found 25 of 26 needed symbols already reachable).
- `tests/fault/disaster-restore/disaster-harness.ts` (260) — fixtures only.
- `.../anchor-boundary-crash.fault.ts` (228) — DoD 1.
- `.../restore-identity-epochs.fault.ts` (327) — DoD 2.
- `.../recovery-unknown-truth.fault.ts` (285) — DoD 3.

## Measured protocol facts (do not re-derive)
- Slots are `["ACTIVE","PENDING"]`. First install: current=ACTIVE, target=PENDING,
  so after the switch currentSlot=PENDING. Second install targets ACTIVE.
- Per-boundary outcome, all 8 confirmed by running them:
  PREPARE / INACTIVE_INSTALL / TRANSACTION / FILE_FSYNC / DIRECTORY_PERSISTENCE /
  VERIFICATION / SWITCH -> **old authority** (pointer never moved).
  INSTALLED_MARKER -> **switched-but-unmarked**: currentSlot moved, slot verifies,
  `state` is still PREPARED. That is a THIRD observable state, not a mixture —
  `settledInstall` (recovery-anchor.ts:119-126) closes it with marker-only.
  Do not write a two-arm assertion that forces INSTALLED here.
- Resume after a PREPARE crash refuses `RECOVERY_ANCHOR_COMMAND_MISMATCH` (the
  anchor still belongs to the FIRST command). All seven other points resume to
  INSTALLED.
- `inspectRecoveryAnchor` runs the production `verifySlot` internally and
  publishes `slotVerified`. `verifySlot`/`slotDirectory` are NOT barrel-exported;
  you do not need them and must not reimplement them.

## Reachability map (grep-verified at HEAD cf137c6)
Exported from `@moe/daemon`: createRecoveryIncarnationService,
createNodeRecoveryCryptoPort, anchorIncarnation, readAnchoredIncarnation,
readSuccessionChain, recordRecoveryReconciliation, readRecoveryReconciliation,
readRecoveryCompletionEvidence, runRecoveryCompleteCommand, plus
RECOVERY_SUCCESSION_ERROR_CODES/_LAYER, RECOVERY_COMPLETION_CODES/_LAYER/
_SCHEMA_VERSION, RECOVERY_COMPLETE_PAYLOAD_KEYS, BOOTSTRAP_HANDLERS,
runBootstrapCommand.
**NOT exported** (zero hits in apps/daemon/src/index.ts): `runRestoreQuiesce` /
restore-controller, `readInstalledRestore`, `ensureGenesisRecoveryBinding`,
`RECOVERY_PROOF_CLASSES`, `RECOVERY_INVENTORY_LAYER`,
`RECOVERY_COORDINATOR_UNKNOWN_CODE`, any `RecoveryCompletionAuthority` factory
or type. Consequence: **a genuinely QUIESCED project cannot be built from
outside the daemon.** DoD 3's "no transition out of QUIESCED" is therefore
asserted as no lifecycle transition + no `ProjectRecovered` event + unchanged
aggregate version + null command decision.

## QA pointers
- Base-ref diff: `git diff cf272f67c3e34fe1d8c4dd00675512c67f165c05..HEAD -- packages/store/src/index.ts tests/fault/disaster-restore/`
- The commit is by explicit pathspec; no foreign work was swept in.
- Five mutation drills were run and each reddened the INTENDED assertion; see the
  step-7 note on the task for the exact edited ranges and error text.
- Two files exceed the 250-line target (327, 285) but are under the 400 hard cap.
  Not split because the task owns exactly five files.
- Deliberate deviation, stated plainly: the stale-grant refusal from
  `runRecoveryCompleteCommand` is NOT a `RECOVERY_COMPLETION_CODES` member. It is
  `UNKNOWN_TRUTH` refusedBy `RECOVERY_INVENTORY` — recovery-completion-digest.ts
  documents that a foreign refusal keeps its producer's code and layer. The test
  asserts that rather than forcing a completion code production never returns.

## Foreign state observed while running (not mine, not touched)
At session start `pnpm test:fault` was RED: 3 cross-host cases died on
`Cannot find module '...tests/fault/cross-host/cross-host-evidence.mjs'` (never
committed), and `pnpm --filter @moe/store typecheck` had 22 errors in
`event-read-model-contract.test.ts` (task-69 residue). **Both were repaired by
their owners mid-session** (HEAD 1576a44 -> cf137c6). No baseline exception was
needed in the end.

---

## Superseded: 2026-08-09 blocked handoff
The task was blocked pending eight prerequisite tasks. Seven are DONE, one
(`task-0325dcf7`) is ARCHIVED with its adapters present on disk, and
`task-6f786c58` is BLOCKED on gate execution rather than on capability — its
deliverable (recovery-completion*.ts) is landed and barrel-exported. The old
unblock probe's nine capabilities were re-measured present before planning.
