# task-0c89476b78044024b07c86c0c8986bd0 handoff

## Outcome (2026-08-09)
The Disaster restore proof remains **BLOCKED**. No implementation plan was submitted because the production capability is absent; only witness shapes/reducer transitions exist. I performed the required prerequisite-production breakdown and called `moe.report_blocked` with the exact unblock trigger.

Audit base was HEAD `1ce0059de305e9e8f13f099daa1b9c5249b34071`.
Design SHA-256 was reverified as `1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191`.
Benchmark SHA-256 was reverified as `A62B90436CC0B911FB28526AF7B7E0F2D1370F6F93DB91C26077F6E2956A589C`.

## Measured production state
Exists:
- `RestoreQuiesceWitness`, `RecoveryCompletionWitness`, `validRestore`, `validRecovery`, and `reduceProject` QUIESCED/READY transitions.
- @moe/store WAL/full-sync transactions and `OUTCOME_UNKNOWN` recovery.
- runner artifact CAS descriptor write/fsync/rename/persist/verify.
- core session generation/replay and exact capability matching.
- runner ordinary-restart QUARANTINED/UNKNOWN classifications and scheduler resource quarantine.
- generic human-only R3 policy obligations.

Absent:
- complete cursor-bound BackupGeneration capture/verifier;
- two-slot RecoveryAnchor PREPARED/INSTALLED install protocol/current-slot selector;
- fresh 256-bit recovery incarnation and signing-key epoch mint;
- recovery-incarnation/key-epoch binding in sessions, credentials, grants, or daemon authentication;
- complete post-N configured-class inventory and orphan quarantine/claim embargo;
- exact recovery digest and durable R3 `recovery.complete` composition;
- root `test:fault` / `test:security` lanes.

## Created prerequisite tasks and sequence
1. `task-5606947a9d7d4f228dc63e6ce4dea69a` — Cursor-bound complete backup generation (**PLANNING**).
2. `task-684e6972c8c1418f8996ea3edd61f00c` — Fresh recovery incarnation and signing-key epoch (**PLANNING**). The paired title is intentional: nonce + key epoch are one atomic fencing identity.
3. `task-b6e3dd2af916490fb2bc4d375a530683` — Crash-safe two-slot recovery anchor installer (**BACKLOG**, depends 1+2).
4. `task-8a01c025b65246aca09e69d82e009587` — Recovery-incarnation command authentication fence (**BACKLOG**, depends 2+3).
5. `task-0325dcf7ee744123b40cf583230c7b6a` — Node-side recovery-window inventory adapters (**BACKLOG**, depends 1+2).
6. `task-cf7fb147bd1c47698cbd65c9535370aa` — Recovery inventory quarantine coordinator and claim/effect embargo (**BACKLOG**, depends 3+4+5).
7. `task-6f786c58cabf4f85be8ed4135e68a752` — R3 disaster-recovery completion composition (**BACKLOG**, depends 1+3+4+6).
8. `task-b5e9bd6444514d02a1e554420c0245b8` — Isolated hostile-test lane infrastructure (**BACKLOG**). Serialize after active package.json owners `task-739879d0d1ce4a7ea4d1430688cdc4dc` and `task-9449ce65fae544ef9809691ec079f599`.

Task comment `comment-01dac749064641d9850276e3ee2ba02c` records the complete dependency list and unblock probe. `moe.report_blocked` left the task BLOCKED.

## Unblock trigger
All eight tasks must be DONE. Before planning CORE-S14, grep/probe the production disk for:
- BackupGeneration capture + verifier;
- RecoveryAnchor PREPARED/INSTALLED installer, inactive slot persistence/verification, atomic selector;
- fresh incarnation/key epoch mint;
- daemon-current credential/grant fence returning `SESSION_REPLAYED` from identity;
- exact seven-class inventory/quarantine record plus production work/effect embargo;
- R3 digest recomputation and durable reducer/store call site;
- runnable `pnpm test:fault`;
- deconflicted ownership of `tests/fault/disaster-restore/**` relative to Security task `task-05ce9b8f982f448a9cdaa4142a653f32`.

Do not unblock for exports, witness shapes, mock ports without production consumers, or a fixture-owned restore controller. Missing capability remains UNKNOWN.
