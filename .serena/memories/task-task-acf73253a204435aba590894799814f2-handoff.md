# QA handoff — task-acf73253a204435aba590894799814f2

QA reviewed current committed bytes at HEAD `ca77c059af5f72ab5dbb17d787805c0275d0532f`; the task source is commit `7cb86d4` and no later task-path diff exists.

## Decision
APPROVE planned. The holistic total-result/cleanup redesign fixes the prior fulfilled-hostile-result defect.

## Fresh QA evidence
- `pnpm --filter @moe/runner typecheck; pnpm --filter @moe/runner test`: exit 0, 55 files / 1711 tests.
- Plain-Node root import and unsupported launch smoke: exit 0, `runner-root-launcher-smoke: PASS CLAUDE_LAUNCH_PLATFORM_UNSUPPORTED LAUNCHER`.
- `pnpm exec vitest run --root . packages/runner/src/platform/windows/windows-boundary.smoke.test.ts`: exit 0, 1 file / 4 tests; the real Windows process-tree smoke executed.
- Repo gates `pnpm typecheck; pnpm test; pnpm verify:foundation; pnpm verify:store`: exit 0. Counts: root 248 files / 5386 passed + 1 skipped; foundation 30/487; store 39/444.
- QA mutation: changed the runtime hostile-result refusal code in production from `CLAUDE_LAUNCH_RUNTIME_THROWN` to `CLAUDE_LAUNCH_DEPENDENCY_THROWN`. Named hostile-result matrix reddened at `claude-launcher.test.ts:733` with the exact code mismatch. Original bytes restored SHA-256 exactly and `git diff --exit-code` passed.
- Task paths clean; `git diff --check 7cb86d4^..HEAD` clean; source commit contains exactly the nine planned paths.
- Production physical lines: contract 146, port-results 250, boundary-results 212, lifecycle 249, launcher 167. No production file exceeds the rail.
- Authoritative design hash remains `1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191`.

## Audit findings
Every injected port is captured by descriptor-safe capability binding and every result is decoded within the same containment. After lock acquisition, the single lifecycle exit cancels unless completion is proven, awaits close and stream settlement, then awaits one lock release. Malformed boundary shapes that still expose cleanup become BROKEN and are cancelled/closed; unowned shapes return CLEANUP_UNKNOWN. Generated sweeps assert exact positive counts (24 hostile fulfilled cases, 14 thenable/rejected awaited cases, 6 option/capability reflection cases), exact reason code/layer, exact trace, zero pre-open execution where applicable, and lock cleanup. Root publication and downstream consumer/canary task IDs are recorded.
