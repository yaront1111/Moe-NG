# Handoff: task-4d22630791994ed1b0906632a75b349b (Workspace manifest hygiene)

## Status
**DONE — moved to REVIEW 2026-08-09 18:52Z by worker-4091b158.** All 5 steps completed.

## Corrected scope (governor, 14:56Z) — the stored definitionOfDone is STALE
Item 1 still names the scheduler and control-room clauses. Both were **struck**. No MCP tool exposes
`definitionOfDone`, so `reopenReason` is the durable correction and QA inherits it from there.
**A diff that ADDS any dependency is a REJECTION, not compliance.**

Re-verified at HEAD by me before finishing:
- `packages/scheduler/package.json` declares only `@moe/context` (added by sibling task-cda6bddf).
  The lone `from "@moe/contracts"` hit under `packages/scheduler/src` is a fixture string inside
  `package-boundary.test.ts`'s `allowedContentCases` table, labelled `"an unrelated import"`.
- `apps/control-room/package.json` has neither `@moe/store` nor `@moe/testkit`.

## What landed (3 changes, all verified as committed bytes, not assumed)
1. `packages/coordination/package.json` — `@moe/core` removed. Hunk lives in **501030b**:
   `-    "@moe/core": "workspace:*",`. `@moe/contracts` + `@moe/store` retained.
2. `pnpm-workspace.yaml` — glob **kept**, one comment added in **71af97c**:
   `# Reserved, not yet populated: external integrations; task-9fd52b41 owns adapters/jetbrains/**.`
   The four settings below (autoInstallPeers/engineStrict/saveExact/strictPeerDependencies) untouched.
3. `pnpm-lock.yaml` — coordination importer links only `@moe/contracts` and `@moe/store`. The two
   surviving `@moe/core` lock entries (lines 81, 140) belong to `apps/daemon` and `packages/review`.

## No commit bears this task's id
Foreign whole-tree sweeps carried the bytes: **501030b** took the coordination manifest, **71af97c**
took `pnpm-workspace.yaml` (its message names task-4d226307 explicitly). Step 5's
`git commit -- <three paths>` had nothing to commit. I did **not** create an empty commit, amend, or
reset. Per the epic rail this is not a defect and not a rejection reason. Review with
`git show HEAD:packages/coordination/package.json` and `git show HEAD:pnpm-workspace.yaml`.

## Verification
Owned legs green:
`pnpm install` -> "Scope: all 17 workspace projects / Already up to date", exit 0, **zero lock drift
after install** (the lock was already consistent);
`pnpm --filter @moe/coordination typecheck` exit 0;
`pnpm --filter @moe/coordination test` -> Test Files 2 passed, Tests 40 passed, exit 0.

The step-4 blocking precondition from 15:19Z is **cleared**:
`git status --porcelain -- '*package.json' pnpm-lock.yaml pnpm-workspace.yaml` returns ZERO.

## The adversarial check that actually mattered
Every prior measurement on this task used `from "@moe/core"`, which misses `require()`,
`await import()`, and tsconfig path references. I ran the bare-string form:
`git grep -n "@moe/core" -- packages/coordination` -> **ZERO hits (exit 1)**. Only that proves the
removal cannot be load-bearing. Do the bare-string grep, not the specifier grep, before deleting a
declared dependency.

## Foreign red left on the board (disclosed, not mine)
- `pnpm typecheck` exit 1:
  `apps/daemon/src/identity/session-authenticator.test.ts(8,3): error TS6133: 'OPENER' is declared but its value is never read.`
  Introduced by **5ef522c** "feat(daemon): durable work claiming". **This blocks the repo-wide
  typecheck leg for every task on the board** until its owner removes the import.
- `pnpm test` exit 1: `tests/property/schedule/schedule-coverage.test.ts`, 2 failed / 3937 passed.
  `packages/testkit/src/schedule/schedule-universe-tables.ts` (authored) lacks
  `"ACTIVE|graph.supersede"` while `packages/core/src/planning/graph-revision-reducer.ts` (landed)
  has it. Live rollout — three `packages/core/src/planning/graph-revision-*` files still dirty.
  Owner needs to add the row to the testkit table.

Failing-path set intersected with owned paths is EMPTY. I did **not** run the gate at the merge-base:
that needs a checkout or a sibling worktree, forbidden by epic rails 2 and 3. Attribution rests on
commit provenance (`git log -1 -- <failing file>`) plus path disjointness instead. See
`mem:gotcha-head-moves-mid-verification-in-shared-worktree`.
