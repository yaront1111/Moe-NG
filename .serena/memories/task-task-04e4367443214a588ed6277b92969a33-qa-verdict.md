# QA verdict: APPROVED — Durable coordination recipient registry (2026-08-10)

`task-04e4367443214a588ed6277b92969a33`, commit `2fb0243`, reviewed by qa-12dfe1c1.

## Gate re-run by QA (not trusted from the worker's summary)
- `pnpm --filter @moe/daemon typecheck` exit 0.
- `pnpm --filter @moe/daemon test` -> 55 files, 872 tests, all pass.
- `pnpm typecheck` from the REPO ROOT -> `Scope: 17 of 18 workspace projects`, **exit 0**.
  The worker disclosed a foreign red here (`packages/scheduler` TS2307 on untracked siblings);
  it no longer reproduces — commit `72d7fb5` landed the scheduler dep edge afterwards. See
  `mem:gotcha-worker-disclosed-foreign-red-can-be-stale-at-review`.
- Focused suite `src/coordination/recipient-registry.test.ts` -> 15 tests, 1.2s.

## Mutation drills QA ran itself (5 of the worker's 8, each verified applied before running)
| mutation | result |
|---|---|
| `readActiveSession` -> `readSessionAuthority` (drops the active filter) | 2 failed |
| records.ts:93 role check widened to `typeof role !== "string"` | 2 failed |
| registry.ts:229 drop `record.projectId !== projectId` | 1 failed |
| registry.ts:234 positive answer returned UNFROZEN | 1 failed |
| registry.ts:233 comment out the generation (rotation) check | 1 failed |

All restored by `git checkout --`; `git hash-object` on both production files equals
`git rev-parse HEAD:<path>`. Drill line numbers are valid only at blob `4c6d49d6` /
`0e1036b4`.

## Facts confirmed on disk
- Per-FILE cap: `recipient-registry.ts` 249, `-records.ts` 184 (`grep -c ''`). Test 678 — the
  cap is per production file, so that is compliant. Task-level LOC is never a bar.
- Commit `2fb0243` contains EXACTLY the 5 owned files, 1113 insertions. `index.ts`,
  `package.json` and `pnpm-lock.yaml` untouched.
- Both `.js` bridges are `export * from "./<name>.ts";` with a trailing LF (`od -c`).
- 5 files landed against 3 named owned paths; the split was authorised by plan step 5.
- Consumer `task-4afcb06422ed4adb89430b7ea9758d7f` recorded (6 occurrences in the task JSON).

## Why the tests were judged non-vacuous
Refusals assert `{code, layer, outcome}` by `toEqual`, never bare "refused". Every refusal path
re-reads event count AND every persisted decision digest, so "wrote nothing" is a byte claim.
The positive answer is asserted against the REAL consumer (`createCoordinationService(...).read`
with `resolveRecipient: registry.resolveRecipient`), not against a re-implemented copy of
`isKnownRecipient` — which is the correct answer to rail 6's "assert against the production
surface". Generated sweeps assert `length > 0` at 4 sites.
