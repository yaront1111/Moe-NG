# Handoff: durable project instruction contract

- Implemented and committed exactly the four owned paths in `82ebebc`: root `AGENTS.md`, root `CLAUDE.md`, `tests/instruction-contract/checker.ts`, and its test.
- Canonical file: 77 lines / 3216 UTF-8 bytes. Claude bridge: 20 lines / 763 bytes. Checker: 174 lines, read-only and fail closed.
- TDD evidence: initial run failed on absent checker; after implementation 22 synthetic tests passed with only the planned missing-root integrations red; after both docs landed focused verification passes 24/24.
- The checker enforces stable missing/marker/bridge/size/generated/live-state codes, CRLF-safe line counts, UTF-8 byte caps, frozen deterministic results, and allows policy prose mentioning `.moe/`.
- Adversarial review found no write/delete APIs, forbidden markers in durable files, scratch artifacts from this task, or foreign staged paths. Commit `82ebebc` contains exactly the four owned paths.
- BLOCKER: exact complete-task gate `pnpm typecheck && pnpm test` exits 1 only in foreign in-flight task `task-12e18265f7a84c7eacdf79a3bef2bdf3`, currently editing `packages/contracts/src/runtime/**` and `packages/contracts/src/index.ts` mid-TDD. Do not touch those paths. Rerun the exact gate when that task reports contracts green, then complete this task.
- See `mem:gotcha-shared-tree-repo-gate` and `mem:gotcha-moe-wrapper-autocommit`.