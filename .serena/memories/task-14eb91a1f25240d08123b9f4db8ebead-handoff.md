# Core supersession decision kernel handoff

- Task: `task-14eb91a1f25240d08123b9f4db8ebead`; consumer: `task-1df0622e87cf42beae2cd82280e9ff99` must compose root `SUPERSESSION_DISPOSITION_KINDS`, `SUPERSESSION_KERNEL_LAYER`, and `decideSupersession` into `graph.supersede`.
- Commit: `025dc4948127e7c7b753b71d3f5cf4b142637060` (`feat(core): add supersession kernel`), exactly the four owned paths. Owned disk blobs equal HEAD.
- Surface: `packages/core/src/supersession/supersession-engine.ts` (250 lines), exact LF `.js` bridge, 263-line production-root test, curated `packages/core/src/index.ts` (249 lines). Root runtime namespace is 61 values; matching values are exactly the three above.
- Behavior: frozen six-kind tuple ADD/CARRY/REQUALIFY/REEXECUTE/CHANGE/REMOVE; exact predecessor/successor +1 binding; composed production `evaluateCarryForward` for authority and inputBinding; frozen exact `REVISION_REBOUND` or `SUPERSESSION_CONSEQUENCE_CHANGED` at `SUPERSESSION_KERNEL`; domain-separated length-framed SHA-256 authority hash; normalized/frozen accepted decision.
- Mutation drills: +1 guard, stored authority binding, evaluator rejected input, evaluator valid:false verdict all caused the intended named production test to go red, then restored byte-exact. Engine SHA-256 after restoration: `A4F9A352EDE93C333E2F04EEBDE4F48B7321CD44D6E0DFCB68EC507825F7BEC3`.
- Verification before final exact gate: repo `pnpm typecheck` exit 0. Repo `pnpm test` has the same baseline foreign failure only: `packages/scheduler/src/package-boundary.test.ts > keeps scheduler registrars behind the package-root import boundary`, scanning `apps\\daemon\\src\\daemon-main.ts` with `unterminated regular expression source token`; 1 failed, 203 passed files / 1 failed, 3870 passed, 1 skipped tests. Owned-path delta intersection is empty.
- Shared-root serialization: task `task-fcad40b6d26243439cd19fd3e49c924d` committed its six paths in `a646660`, then deliberately BLOCKED to release clean `index.ts` and break the circular core-gate dependency. After this task completes, that task can be unblocked and rerun only its step-8 gates.