# task-04e4367443214a588ed6277b92969a33 — Durable coordination recipient registry (BLOCKED)

Status at 2026-08-09 19:24Z: still BLOCKED. Re-verified by worker-97d6df26 at HEAD `95f5e5e`
(block was originally filed at `c8e845e` — HEAD advanced, neither blocker moved with it).
**Zero bytes written.** Working tree clean on every path this task would touch.

## Blocker A — `@moe/coordination` is not resolvable from `apps/daemon`

Kills taskRail 2 and DoD 1, 2, 4. There is no workaround; all escape hatches are closed.

    git show HEAD:apps/daemon/package.json | grep '@moe/'
      contracts, core, mcp, review, runner, scheduler, store    # 7 deps, no coordination
    ls apps/daemon/node_modules/@moe/
      contracts core mcp review runner scheduler store          # no coordination symlink

Closed hatches (checked, do not re-litigate):
- no re-export of coordination outside `packages/coordination/src`
- `tsconfig.base.json` has **no `paths`** (NodeNext resolution)
- deep relative import is **TS6059** — daemon `rootDir: "src"` + composite
- local duplication is forbidden by taskRail 2

Fix requires `apps/daemon/package.json` + `pnpm-lock.yaml` + a global `pnpm install`.
None of those are owned by this task. See `mem:undeclared-workspace-dep-has-no-escape-hatch`.
The install rewrites shared lockfile state workspace-wide, so it needs a clean-manifest
window — a dirty foreign `package.json` gets baked into the lock.

## Blocker B — daemon typecheck RED at baseline, on foreign committed code

Fresh run at HEAD 95f5e5e:

    pnpm --filter @moe/daemon typecheck
    src/identity/session-authenticator.test.ts(8,3): error TS6133: 'OPENER' is declared but its value is never read.
    Exit status 1

- `grep -n 'OPENER'` on that file: **one hit, line 8 — the import itself**. Genuinely unused.
- `git log -1 -- <file>` = `5ef522c feat(daemon): durable work claiming`
- `git status --porcelain -- apps/daemon/src/identity/` = EMPTY

Committed foreign work, not a waitable in-flight writer. Step 8 of this task's plan says
verbatim: *"Foreign red inside @moe/daemon itself is a BLOCK, not a disclosure."*
Consistent with `mem:owned-package-gate-red-is-a-block-not-a-disclosure`.

**This blocks every task in `apps/daemon`, not just this one.** Any worker in that package
hits it at baseline. Fix is deleting `OPENER,` from the import — one line — but it is
outside this task's owned paths, so it needs an owner or an explicit authorization.

## For the next agent

1. Re-run **both** literal checks above before doing anything. Do not trust this note's
   reasoning — `mem:moe-block-conditions-go-stale-silently`. A producer landing never
   notifies its blocked consumers.
2. If A is cleared (coordination in daemon deps *and* the symlink present), confirm with
   `cd apps/daemon && node -e "await import('@moe/coordination')"` — the manifest edge and
   the installed symlink can disagree until `pnpm install` runs.
3. B must be green before you can reach exit 0 on any daemon gate leg, regardless of A.
