# task-5611791337894212a277600a0768f1f9 — QA APPROVED (qa-50f0d628, 2026-08-15)

## Verdict
APPROVED. All 5 DoD items verified on disk, both gates re-run fresh, both halves
independently mutation-drilled by QA (not trusting the worker's drills).

## What QA re-ran
- `pnpm typecheck:release` (PowerShell) EXIT=0.
- `node --test tests/integration/release-supply-chain.test.mjs` (PowerShell, per
  `mem:msys-tar-drive-letter-breaks-integration-gate`): tests 59, pass 59, fail 0,
  skipped 0, NODE_TEST_EXIT=0 — includes the real 17s `release:evidence` e2e.
- Diff reviewed by base-ref, not by commit: `git diff b03da5a..HEAD -- <3 owned paths>`
  = 2 / 6 / 206 lines. Working tree clean for all three, so HEAD bytes == gated bytes.

## QA's own mutation drills (both restored, sha256-verified, FINAL_RESTORE_OK=True)
- **Drill A** revert `:198` to `FROZEN_INSTALL_FAILED`: 2 failed. Both reddened on the
  DISCRIMINATOR — `actual 'FROZEN_INSTALL_FAILED' / expected 'TOOLCHAIN_OBSERVATION_FAILED'`,
  not on some incidental assertion.
- **Drill B** strip the `cleanRoots` try/catch: 3 failed, including
  "cleans up on the signal path without replacing the outcome". Two reddened with the raw
  `ERR_FS_EISDIR` escaping the finally (the defect reproducing verbatim); the signal one
  on `exits actual [] / expected [130]` — stop() threw before reaching process.exit.
- Filter `--test-name-pattern` selected 16 tests; non-vacuous because the new cases are
  exactly the ones that failed.

## Evidence that mattered beyond the green suite
- The SUBORDINATE-FACT requirement is not asserted by any test, so QA checked the runtime
  output directly: the GREEN run's stderr carries 5 × `release temporary cleanup failed:
  <root>: SystemError [ERR_FS_EISDIR] ...`. Reported, not silenced. If you review a
  "report it but don't return it" rail, grep the gate LOG — a test cannot see it.
- Commit `6482e5f` carries this task's ID but NOT its bytes (only foreign `packages/store`
  work + `.moe` board state). The real bytes rode in on `7263d13`, a foreign whole-tree
  commit. Per the project rail that is not a rejection reason either direction; verify by
  base-ref diff + clean status. See `mem:foreign-commit-can-revert-your-landed-deliverable`.

## Accepted deliberately, do NOT re-litigate
- supply-chain.mjs stayed at 256 lines (already 6 over the 250 target BEFORE this diff,
  well under the 400 split threshold). The fix is net 0 lines by design — trailing comment
  on the signature, try/catch on one line. Per-file cap not breached by this task.
- The catch wraps BOTH `rmSync` calls, so a root whose directory removal throws skips its
  own `${root}.tar`. That is the per-root granularity the plan specified; the loop still
  continues to the remaining roots (asserted, 2-root arm).
- `SOURCE_ARCHIVE_FAILED` appears in the refusal table but is NOT a member of
  `RELEASE_REFUSAL_REASONS`. Pre-existing, disclosed by the worker, out of DoD scope.

## Open follow-ups the worker disclosed (all real, all verified out of scope here)
1. `releaseRefusal(reason)` never validates against `RELEASE_REFUSAL_REASONS` — the frozen
   array is documentation, not enforcement. SOURCE_ARCHIVE_FAILED is live proof.
2. `:190` blames ANY malformed input on `OUTPUT_PATH_INVALID`; `RELEASE_INPUT_INVALID`
   exists and is emitted nowhere. Same wrong-reason family as this task.
3. `:240` broad catch turns any exception into `EVIDENCE_WRITE_INTERRUPTED`.
4. `RELEASE_INVENTORY_EMPTY` fires when counts merely DIFFER from 5/3.
