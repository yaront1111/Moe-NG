# Task task-e94b2055e281489ea9e97820919f6856 handoff — BLOCKED after host audit

Reported BLOCKED during step 2 with no code produced.

## Fresh facts
- Host: `process.platform=win32`, `process.arch=x64`.
- Audit HEAD: `7afa17d336f5598836de1dbc160879c9cbf3e52e`.
- `packages/runner/src/platform/` absent; therefore no shipped macOS effect boundary/capability exists.
- `tests/fault/macos/` absent.
- Root `package.json` has no `test:fault`; engine declaration is Node `>=24.16.0 <25`, pnpm `11.0.8`.
- `apps/daemon/src/recovery/doctor-commands.ts` reports its schema version but has no exact engine/runtime version surface.
- Shared production classifiers do exist: Claude PROVEN|UNKNOWN observation lattice, ClaudeProcessExit/reconcileClaudeRun, classifyCrash, admitResume/advanceRecoveryDrain, and observeScope. The Codex dependency is real and fully bridged.

## Why blocked
The new global Clause 2 requires a conformance/acceptance task to name an existing production capability and its shipping task. When absent, prerequisite production tasks must be created; mock-backed shape proof is forbidden. Synthetic caller-supplied macOS observations and an off-host UNSUPPORTED test could validate pure shapes but cannot satisfy DoD 1 or the claim that the macOS fault gate passes. A Windows-green suite quoted as macOS proof would fabricate evidence.

The identical Linux sibling `task-e87a735386f643fe92c0eeff09bc4275` is already BLOCKED on this exact ruling; see `mem:task-task-e87a735386f643fe92c0eeff09bc4275-handoff`.

## Needed
Route prerequisites:
A. macOS platform port adapters/shared contract as an implementation task.
B. Doctor exact runtime-version reporting plus packaging declaration.
C. Owned `test:fault` lane.
D. Real macOS CI/host execution for this conformance task.
Resolve any `tests/fault/**` ownership collision first.

Step 1 was completed with this evidence. Step 2 was not started; task and worker moved to BLOCKED via moe.report_blocked.