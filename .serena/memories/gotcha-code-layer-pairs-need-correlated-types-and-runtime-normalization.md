A TypeScript interface with independent `code: CodeUnion` and `layer: LayerUnion` fields still represents every invalid cross-product. For stable refusal vocabularies, derive a discriminated mapped union from an exhaustive `Record<Code, Layer>` so each code is paired with only its canonical layer. That type rail is insufficient at a trust boundary because casts/wire data bypass it: normalize supplied reasons through the production mapping and fail closed to a stable malformed/binding refusal when the supplied layer differs. Add a runtime test that injects a forged valid-code/wrong-layer pair through the public production resolver; an isolated helper/type test cannot prove the boundary.