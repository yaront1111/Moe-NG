# task-912567934d54419683d1518d60059409 — Report agent spawn admission truth

Status: submitted to REVIEW. Consumer of task-d4329e8d's start-admission contract.

## What shipped
- `agent-spawn-contract.ts`: `SpawnStartRefusal = Extract<AgentSpawnStartResult, {ok:false}>`;
  `SpawnReport` / `RunOnceReport` moved here from agent-wrapper.ts; `SpawnReport` gains
  `refusal: SpawnStartRefusal | null`.
- `agent-wrapper.ts`: `spawnAgent` is now the producer's `AgentSpawnStart`. `staff` is async and
  awaits ADMISSION only; three arms (refused / unclassified-throw / admitted). Pass-level
  reentrancy latch on `runOnce`.
- `agent-wrapper-main.ts`: `claudeSpawner` -> `claudeSpawnStarter`, `await wrapper.runOnce()`,
  operator line names the refusing layer.

## Decisions a reviewer will question
1. **`outcome` was NOT closed into a union.** Deliberate. It carries adapter result codes
   (`opened.code`, `claimed.code`) that are genuinely `string`, pinned by 3 existing assertions.
   Drill proved `"SPAWNED" | string` is an EQUIVALENT MUTANT — TS collapses it to `string`, so it
   can never redden anything. The closed fact lives in `refusal` instead, guarded by
   `@ts-expect-error` negative compile tests.
2. **The negative compile test pins BOTH the alias and `NonNullable<SpawnReport["refusal"]>`.**
   A drill showed an alias-only assertion stayed green while the report's field was widened —
   the guard had detached from the surface callers read. See
   `mem:guard-downstream-of-normalizing-copy-is-unreachable` for the same class.
3. **DoD-1 fixture needs `platform: "win32"` on the starter.** `agentSpawnInvocation` returns
   unquoted on every other platform, so without it the refusal is unreachable and the case is
   green while proving nothing. The unquotable `&` travels in via the wrapper's minted sessionId,
   which becomes the `--mcp-config` path.

## Traps hit (both now separate memories)
- `mem:gotcha-admission-lifetime-split-turns-a-held-promise-into-a-hang`
- `mem:gotcha-making-a-sync-pass-async-opens-a-reentrancy-window`

## Review instructions
Use `git diff abc3dcf..HEAD -- <four owned paths>` (contract +32/-0, main +16/-8, test +400/-36,
wrapper +57/-36). Do NOT use merge-base cf272f6 — it predates the producer task and over-counts
agent-spawn-contract.ts as +126/-0.
Foreign whole-tree commit **49625fa** (task-32eddfd3) swept all four owned files mid-run; only the
latch landed under my own commit e881141. Per `mem:moe-finished-task-may-have-no-commit`, judge by
base-ref diff, not by commit authorship.

## Gate state at handoff
typecheck EXIT 0 / zero errors. Owned scope EXIT 0, 8 files / 85 tests.
Full `pnpm --filter @moe/daemon test` EXIT 1 — 2 failures in
`src/activation/activation-ledger-reader.test.ts`, a peer's UNCOMMITTED mid-TDD file (test dirty,
its production module clean; grep for my symbols in it = 0). Baseline at step 1 was 98/98 green,
so the failing-path delta intersected with owned paths is EMPTY.
