# Report agent spawn admission truth — blocked handoff

On 2026-08-15 at HEAD `2e34a5f`, the required producer contract from `task-d4329e8d020f40308209397079c5004d` was absent. `agent-spawner.ts` exports only `claudeSpawner`, still typed `(SpawnRequest) => Promise<void>` and resolving on child exit/error/timeout. Searches for SpawnStart/SpawnAdmission/startAgent/exitPromise/admission returned no matches. The consumer rail forbids mocking or reimplementing the producer and forbids editing producer files, so the task was reported BLOCKED with needsFrom the producer. Producer is PLANNING with architect-09b5f32a.

There is also current foreign unstaged WIP in `apps/daemon/src/orchestrator/agent-wrapper.ts` and `.test.ts` adding HUMAN_ONLY_STEPS; preserve it and do not plan/edit until its owner commits or clears it. Unblock/replan only after the producer surface exists on disk and wrapper paths are clean. Then grep/read the exact exported result symbol and ensure runOnce/main await only admission, never the separate exit promise.