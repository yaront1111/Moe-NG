# task-d4329e8d — Expose agent spawn admission (plan approved)

Approved 6-step, 2-file plan. Owned paths:
- `apps/daemon/src/orchestrator/agent-spawner.ts`
- `apps/daemon/src/orchestrator/agent-spawner.test.ts`

## Chosen boundary
Export `claudeSpawnStarter`, `AgentSpawnStart`, and closed `AgentSpawnStartResult`:
- typed preflight refusal: `{ok:false, code:"SPAWN_ARGUMENT_UNQUOTABLE", layer:"agent-spawn-invocation"}`
- accepted: `{ok:true, exit:Promise<void>}`

Admission resolves only from ChildProcess `spawn`; the separate `exit` preserves the existing bounded exit/error/timeout lifetime and credential cleanup. `claudeSpawner` remains the legacy `Promise<void>` adapter over the starter. Named downstream consumer: task-912567934d54419683d1518d60059409.

## Load-bearing details
- Call `agentSpawnInvocation` exactly once/request, catch only `SpawnInvocationRefusal`, and keep it before credential write.
- Unknown/synchronous spawn failures remove the written config and reject unchanged; never mint a generic code.
- Attach spawn/error/exit listeners before stdin/timer. Pre-admission error rejects start; post-admission error settles lifetime without relabeling admission; error+exit must settle once.
- With `shell:true`, Node's spawn event proves the shell started, not inner Claude readiness.
- `exit` is bounded slot/credential settlement and may resolve after timeout without independent physical-death proof.
- Bridge `agent-spawner.js` already wildcard-exports; no root export or wrapper edit.

## Tests/drill
Nine-token literal Windows refusal sweep with exact positive count, exact result, zero spawn, empty captured config dir; held-open child proves distinct promises; raw pre-start/sync failures preserve error identity and cleanup; post-admission error cleanup; existing legacy suite retained. Mutation converts only typed refusal to accepted, named exact code/layer test must red, then SHA restore.

Final gate: `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test`, with nonzero Vitest counts.