# task-d4329e8d020f40308209397079c5004d handoff

Blocked at step 1 with zero task bytes written. Fresh claim showed `workStartedAt:null`, 0/6 steps, no prior handoff, but both owned files were already foreign-dirty:
- `agent-spawner.ts`: detached process-group kill / killGrace / taskkill / CONFIG_DIRS / fileURLToPath lifecycle rewrite.
- `agent-spawner.test.ts`: 164-line matching lifecycle suite, currently racing/type-red.
- out-of-scope `agent-spawn-invocation.ts` also changes the UNQUOTABLE set from 9 chars to include `!()`.

There is no `claudeSpawnStarter` or `AgentSpawnStart` surface yet, so none of the dirty WIP is this task's planned admission work. Do not reset/stash/delete or edit over it. Unblock only after those three files are clean/committed by their owner, or governance explicitly transfers/amends scope. Then restart step 1: write the 9-token/admission/held-child tests first.

Relevant decisions: `mem:decision-child-spawn-admission-versus-lifetime`, `mem:task-task-89071eb1ea0d4ccd8015f61d10cd89f6-handoff`.