# QA approval — Provider slot activation reducer

Approved current task bytes from commit `2a82819` at repository HEAD `ca77c05`. Later foreign work changed only scheduler index-surface tests; the full current tree remained green. Task commit contains the nine disclosed scheduler/daemon compatibility paths and no unrelated source.

Fresh QA verification:
- Exact recorded gate exited 0: scheduler typecheck + 42 files/1219 tests; daemon typecheck + 63 files/948 tests; all 18 workspace typechecks; root 248 files/5386 passed + 1 skipped.
- Plain Node bare `@moe/scheduler` smoke exercised RESERVED -> ACTIVE and printed `SMOKE_OK default ACTIVE attempt:smoke`.
- Independent production-root hostile probe generated four cases: symbol/accessor/proxy predecessors returned exact `AUTHORITY_MALFORMED_INPUT`, dimension mismatch returned exact `AUTHORITY_STALE_LEASE`, none exposed a value, and valid activation returned frozen ACTIVE.
- Independent mutation changed the RESERVED guard so ACTIVE could reactivate. Exactly the named replay test reddened with `active replay: expected true to be false`; file restored SHA-exact (`C2AAC872...`) and clean against HEAD.
- Production sizes: resource-model 214, lease-resource 232, work-claim 158, scheduler index 332; all below 400 and focused files below 250.
- Design hash remains `1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191`.

DoD mapped: reserve returns frozen dimension-bound RESERVED/null attempt; activate exact-parses predecessor/command and returns fresh frozen ACTIVE/exact attempt; malformed versus stale identities/states pin distinct exact codes and never produce ACTIVE; root exports the function/type closure; downstream consumer task `task-e33747f982e0452a9f9d784fd1cb914d` is recorded.