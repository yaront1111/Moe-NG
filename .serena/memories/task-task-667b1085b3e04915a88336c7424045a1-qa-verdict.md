# Control-room journey gate — QA REJECTED (reopen 1)

`task-667b1085b3e04915a88336c7424045a1`. QA `qa-f3560083`, 2026-08-10.
Reviewed at HEAD **`0b1d7a7`**. Worker `worker-a2c7f85f`. See `mem:task-task-667b1085b3e04915a88336c7424045a1-handoff` for the worker's side — it is accurate.

## Everything the worker claimed re-measured TRUE

Both gates re-run by me, foreground, tree clean over `tests/e2e/control-room/`
before AND after, HEAD unchanged across both:

```
pnpm test:e2e         -> 4 files / 59 tests passed, exit 0
pnpm test:e2e:browser -> 10 passed (9.6s), exit 0     <- DoD 4, CONFIRMED
```

Also confirmed independently: spec sha256 `c55af8a9…` matches the task rail; §12 has
exactly 20 rows and the ledger transcribes all 20 ids **in spec order**; there is no J6
in the spec (§6.1–6.5 only), so DoD 1's "J1-J6" is a description artifact, not an
omission; `runs-surface`/`resources-surface` have **zero** production importers;
files 235/183/341 all under 400; `test-results/` untracked via a local `.gitignore`.

## The one defect — a silent omission inside the honesty artifact

**DoD 2 enumerates SEVEN invariants**: truth, provenance, keyboard, narrow-window,
**loading**, degraded, latency. The gate asserts five, records latency as a typed
UNKNOWN, and is **silent on loading** — not in `journeys.spec.ts`, not in
`SCENARIO_MATRIX`, not in the completion evidence. Silence there is indistinguishable
from coverage, which is the exact failure this task exists to prevent.

Loading is real production with committed ids — `board-surface.tsx:101`
`cr.board.skeleton`, `goals-home.tsx:231` `cr.goals.loading`, `doctor-console.tsx:231`
`cr.health.loading`, spec §11.3 per surface — but unreachable: `main.tsx:40` ->
`ControlRoomScaffold` -> `kernel.tsx:162` -> `ControlRoomPreview`, which never sets
`loading`. So it is `SURFACE_NOT_COMPOSED`, **the same cause the worker correctly
diagnosed for `cr.runs`/`cr.resources`**. They found the class and left one member
unrecorded. Fix is one record beside `LATENCY_RECORD` plus a rot guard mirroring
`journey-coverage.test.ts:178`.

## What I explicitly did NOT reject on

3-of-20 with 17 typed UNKNOWNs — correctly recorded, caused and owned, and **not the
worker's to close**. Also not rejection reasons: `journey-coverage.ts` riding in
foreign commit `a6f8402`, and no commit bearing this task id.

## Escalated to governor, not to the worker

DoD 1/2/3 as written cannot be met at this commit; **1 of 4 fully met**.
`NO_DAEMON_BACKED_BROWSER_LANE` covers 12 of 17 and is **UNOWNED** — `task-3f503779`
was archived as a duplicate of THIS task, so it cannot discharge it either.
`task-779d6804` is ARCHIVED and owns 3 more.

## My seven drills (do not re-derive them)

All seven reddened at the assertion aimed at. Drills 1–4 on the ledger (list identity,
non-empty `missingInput`, latency status, absent-id scan). Drill 5 is the one worth
keeping: marking graph scenario `CR-S9-001` COVERED with `productionFiles` pointing at
a **real-but-unrelated** file (`kernel.tsx`) leaves the node lane **15/15 green** — the
on-disk check asserts the LISTED file exists, not that it carries the scenario's ids.
Only the ledger/spec binding test at `journeys.spec.ts:339` catches it. The pair holds,
but do not describe the on-disk check alone as what blocks narrowing.
Drill 6 dropped a `SUPPLIED_COMMANDS` entry -> RED at `:191` (also proves the sweep
enumerates real actions). Drill 7 pressed `q` not `p` -> RED at `:264`, proving the
keyboard test asserts outcome, not handler presence.
