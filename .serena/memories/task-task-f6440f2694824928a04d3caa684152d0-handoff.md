# task-f6440f26 — @moe/daemon .js bridges + stale root-export assertion — WORKER HANDOFF (done)

Executed by `worker-0b27a5cc` 2026-08-09, 10/10 steps. Replaces the architect handoff; the
architect's numbers were all confirmed and are folded in below.

## Result

- **19 bridges added** to `apps/daemon/src` (0 tracked `.js` before → 19 after).
- One `it()` retired in `graph-preview-request.test.ts` (`Object.keys(daemon)` equality, -4 lines).
- New `apps/daemon/src/runtime-entrypoint.test.ts`, 175 lines, 3 tests.
- Gate: `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test` → exit 0.

Baseline was **RED** and is now green — say so, or a summary is false:

```
BEFORE  Test Files 1 failed | 14 passed (15)   Tests 2 failed | 363 passed (365)
AFTER   Test Files 16 passed (16)              Tests 367 passed (367)
```

Arithmetic, fully attributed: 365 − 1 retired + 3 new = 367; 15 suites + 1 new file = 16. Both
baseline failures were this task's work — `:241` retired, `:262` (real Node child importing
`@moe/daemon`, was status 1) fixed by the bridges.

## THE COMMIT DID NOT GO AS PLANNED — read this before auditing authorship

`git add -- <21 explicit paths>` exited 0 and staged **nothing**. Cause: commit **`a6e46f6`**,
a 65-file whole-tree sweep by the `task-386fcb4c` worker (message
`feat(task-386fcb4c...): Add package-wide .js runtime bridges to @moe/core`) had already
captured all 21 of my paths. Not revertible under epic rail 3; reported, not reverted.

Verified after the fact instead of assumed: `a6e46f6` under `apps/daemon` contains **only** the
19 bridges + the 2 test files — **no `index.ts`, no `.ts` service module**. All 19 committed
blobs re-read with `git show HEAD:<path>`: exact one-line form naming their own sibling, zero
`0x0D`, `content_bad=0`. Worktree == HEAD. Full write-up:
`mem:gotcha-foreign-whole-tree-commit-preempts-your-pathspec-commit`.

## Classification — the one judgement call, and the plan's literal rule is wrong

27 non-test `.ts` − 8 test-tier = 19. The 8: `bootstrap/bootstrap-test-fixtures`,
`http/http-test-fixtures`, `http/event-stream-fixtures`, `work/work-race-{fixtures,drift-table,world,schedule,tampers}`.

The plan's stated rule ("every importer is a test or test-only module") **over-excludes two
production modules**: `http/http-adapter.ts` has zero non-test importers, and
`work/work-lifecycle.ts` reduces to test-tier transitively via `work-race-world.ts`. Both are
production and both are bridged. Runner's filename rule fails here too. The rule that works —
forward closure from `index.ts` + every module with its own `<name>.test.ts` sibling — is
implemented in `runtime-entrypoint.test.ts` and written up in
`mem:convention-js-bridge-tier-classification`.

## Evidence a reviewer should re-run rather than trust

- Plain-Node, repo root: `import("./apps/daemon/src/index.ts")` → `LOADED exports=31
  undefinedBindings=[]`, exit 0. Was `ERR_MODULE_NOT_FOUND` on `bootstrap/bootstrap-contracts.js`.
- Regression control: runner (66 exports) and scheduler (36) still load.
- Negative control: **all eight** unbridged test-tier paths → literal `ERR_MODULE_NOT_FOUND`.
- Forbidden-key drill (`index.ts` line 219, additive `execution: true`, external backup, restored
  and `cmp -s` verified, blob back to `2bf258ff…`): the neighbour test went RED at
  `graph-preview-request.test.ts:236` — still load-bearing.
- Four drills on the new test: delete a bridge → `ERR_MODULE_NOT_FOUND` + `missing` names it;
  **mis-target a bridge → only `wrongContent` reddens while the import probe stays GREEN** (why
  the content assertion must exist); add a test-tier bridge → negative control flips to
  `code: "NONE"` and `unexpected` names it.

## Left alone deliberately

`apps/daemon/src/index.ts` never edited (drill fully restored). The sealed-subpath
`ERR_PACKAGE_PATH_NOT_EXPORTED` case and the forbidden-key loop both untouched and passing.
`task-6054520b`'s work was already committed before this task started, so its BLOCKED conditions
are now cleared — the daemon root loads under plain Node and the stale assertion is gone.
