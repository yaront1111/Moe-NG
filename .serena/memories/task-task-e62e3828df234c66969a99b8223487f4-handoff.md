# task-e62e3828 (Current expansion safe-release evidence) — QA APPROVED -> DONE 2026-08-26

Reviewed by qa-69068c25. Durable QA record: `comment-ffd1a969f1824b79b5d6dcbf53572e01`.
Supersedes the worker's delivery handoff (kept below as still-true shape notes).

## Verdict and what actually settled it
APPROVE. Delivered bytes = `9c41ad94` (830 ins) + `0a653455` (10 ins), the ONLY two commits
touching the three owned paths. Worktree blob == `HEAD:...authority.ts` == `2ca8c697`.
Sizes 304 / 1 / 535 — production under the 400 per-FILE cap. The 250-line production target
and 300-line test target are MISSED and DISCLOSED; neither is a rejection reason
(task-level LOC never is, and the test file is not a production source).

## The gate, re-measured by QA — DO NOT INHERIT THE WORKER'S NUMBERS
- Focused owned gate, twice, fresh foreground:
  `cd apps/daemon && npx vitest run --root . --config package.json \
     src/work/expansion-release-authority.test.ts src/runtime-entrypoint.test.ts`
  -> `Test Files 2 passed (2)` / `Tests 34 passed (34)`, EXIT 0.
- `pnpm --filter @moe/daemon test`: **run 1 EXIT 1** (`3 failed | 275 passed`,
  `6 failed | 5842 passed`), **run 2 EXIT 0** (`278 passed (278)` / `5848 passed | 12 todo`).
  The run-1 reds were LOAD FLAKINESS in foreign, git-clean files — one was literally
  `Test timed out in 5000ms` at `foundation-dispatch-derivation.test.ts:446`, plus
  `foundation-attempt-windows.test.ts:1037`. Second run cleared all of them.
  Lesson re-confirmed: `mem:qa-rerun-the-same-suite-twice-to-attribute-a-flaky-red`.
- `pnpm --filter @moe/daemon typecheck`: EXIT 1 twice, EXECUTED (`$ tsc --project
  tsconfig.json` echo + diagnostics). Failing-path set at HEAD = **only**
  `src/budget/budget-current-projection.test.ts` 4x TS7006 (144-147), a ` M` peer file whose
  `ROOT_REPLAY_FORGERIES` block is absent from `git show HEAD:<file>`.
  Run 1 ALSO reported `src/planning/node-mission-producer.ts(43,10) TS6133` which did NOT
  reproduce on run 2 — a peer mid-edit in the shared tree. Re-run before attributing.
- Zero diagnostics/failures name an owned path => (HEAD-failing minus base-failing) ∩ owned
  paths is EMPTY. DoD 5's exit-0 clause was narrowed under this row's own human
  delivery-bias `reopenReason` + the standing path-attributed-baseline rail.

## Why the tests are not vacuous (the parts worth copying)
- 21 generated arms (9 local + 6 read-path binding + 6 upstream), each pinning exact CODE
  **and deciding LAYER** and status, with `release`/`workerHandoff` asserted undefined.
- **Every arm carries its own DIVERGENCE control**: `askFaulted(storePath, NO_FAULT, query())`
  must be BOUND on the same served world before the fault is applied. That is what makes each
  named guard the SOLE refusing mechanism — the fix for
  `mem:gotcha-redundant-guard-hidden-by-an-identical-downstream-fence`.
- Denominators asserted BEFORE the sweep: 9/9 local, `BINDING_CASES 6 + writer-only 2 == 8`,
  `ALL_CASES 21`. `RELEASE_HANDOFF_BINDING_CODES` is a SHARED writer/reader tuple — only 6 of
  8 can leave the read path, and the partition is asserted rather than the arms faked.
- The BOUND control traverses the REGISTERED `foundation.verification` handler through
  `handleAsyncCommandRequest`; no hand-seeded release row can produce BOUND anywhere.
- `terminalFacts` gates `evidence.releasable`, which IS `effectsTerminal && resourcesTerminal`
  at `release-terminal-evidence.ts:251` — check the upstream definition before calling a
  copied-but-ungated flag a DoD gap. It is genuinely gated.

## QA's own two drills (one degree of freedom off the worker's seven)
1. Deleted the key-NAME pin in `admitExpansionReleaseQuery` (the entire payload of
   `0a653455`). The `prototype accessor` hostile case then returned **BOUND** — two decoy own
   DATA keys with the real names served by prototype getters — and the arm redded at
   `expansion-release-authority.test.ts:153`. Arity alone is NOT enough; the pin is load-bearing.
2. Dropped only `stored.record["attemptRef"] !== admitted.attemptRef`. Exactly ONE arm red:
   `a release row naming a different attempt`, `expected undefined to be
   'EXPANSION_RELEASE_EVIDENCE_CONFLICT'`. Narrowest mutant, no neighbour collateral.
Both restored from a `$TEMP` backup; empty `git diff`, blob SHA equal to HEAD, backup deleted.

## Traps hit during THIS review
- `mem:gotcha-vitest-it-each-label-truncates-and-breaks-t-filter` — NEW. `-t` on an
  interpolated `$label` matched nothing and exited 0 with `29 skipped`, i.e. a false green drill.
- `mem:bash-tool-cd-persists-and-breaks-later-paths` — a trailing `cd apps/daemon` in the drill
  command made the next restore `cp` fail with `No such file or directory`. It errored loudly
  here, but the same slip with a `git diff` restore proof reads as CLEAN. Always `cd` absolute.

## Shape of the delivered authority (for consumers)
`readCurrentExpansionRelease(store, query)`, query exactly `{attemptRef, projectId}`, own plain
enumerable DATA props, NAMES pinned, admitted before any store read. Owners answer everything:
`readFoundationActivationByAttempt` -> `readAttemptRelease` -> `deriveReleaseTerminalEvidence`
-> `readCurrentSafeBoundaryObservation` -> `readReleaseHandoffBinding`, then a LAST re-read of
the release aggregate version (aggregate-scoped, not a global horizon). BOUND is deep-frozen and
`release.handoff` **IS** `workerHandoff` (one value, so core's `same()` compares the object with
itself). Nine local codes; upstream refusals keep the owner's code AND layer verbatim.
`receiptRef` is the verified `verificationId`, never `receiptSha256`.

Consumers: the atomic expansion-request task, then `task-c4171c1cfe854cb78dd233794b342025`;
downstream `task-738a12a816e8421a96edd84648565a38` already filed. `expansion-request-service.ts`
and `expansion-request-test-fixtures.ts` already name this module.
