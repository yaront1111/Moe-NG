# task-e62e3828df234c66969a99b8223487f4 handoff

Blocked again after a fresh verify-or-block audit at HEAD `c1ab42b70efbdbfdc0354f68527afa55e46b9033` (the HEAD move did not touch the audited release sources).

## Current production truth

Core `ExpansionReleaseEvidence` still cannot be constructed as BOUND from durable daemon facts.

- The sole non-test caller of `recordAttemptRelease` is `foundation-attempt-service.ts#noteRelease`; it passes `disposition:null`, `effectsTerminal:false`, `resourcesTerminal:false`, `safeBoundaryObserved:false`, and `handoff:null`. `releaseWork` validates handoff before release, so this path appends **zero** AttemptReleaseRecorded events.
- `observationRef`, `receiptRef`, `terminalEffectRefs`, and `terminalResourceRefs` have zero non-test daemon occurrences.
- Durable attempt-resource records intentionally do not decide terminality; `applyAttemptResourceReport` has no production caller.
- Even a direct/test-authored accepted release row copies activation `attemptState=RUNNING` and provider slot `ACTIVE`. Core requires both `RELEASED`.
- Pure scheduler `releaseProviderSlot` exists but has zero daemon callers and is not exported by `@moe/scheduler` root.
- The scheduler ReleaseHandoff persisted by the release row is not core's `ExpansionHandoffBinding {digest,ref}`.
- A refusal-only reader is fail-closed but cannot satisfy the BOUND capability or accepted production control; a test that directly supplies true flags/handoff would forge the authority the task forbids.

## Existing blockers

- `task-ded026d6587442eabc28ff2609d231aa` safe boundary/observationRef, blocked via `task-00e4827dd9374ec79625351d560c82c8`.
- `task-6d40078159e84d92b3293d7b366cf727` terminal effect/resource refs, blocked via `task-1879166d7f12474fad8038e2a9d2433a`.
- `task-af9454f4438242e5854208284e0086e2` receiptRef and durable handoff binding, blocked via `task-203a5ca7aada4375a98a95df8773e249`.

## Newly filed corrective chain

The previous board note treated task-4731's pure slot reducer as composition, but current disk has no consumer. Also scheduler `index.ts` is 430 lines and cannot be edited before splitting.

1. `task-9f51def13fb1484482bcb6d68ce3708d` — split oversized scheduler root execution surface, no namespace change.
2. `task-2db110998677498abb11f65142a6fe25` — publish existing `releaseProviderSlot` and its type on bare `@moe/scheduler`.
3. `task-79f56ad581794b3fb195aeb441b16725` — atomically persist release outcome as attempt state and the reducer-returned provider-slot RELEASED state in the single release row, with distinct slot-refusal layer and zero partial write.

Current task was reported BLOCKED on the three existing producer tasks plus task-79f56. Replan only after all four direct producers are DONE, then re-run the complete 17-field and handoff map before naming reader steps.