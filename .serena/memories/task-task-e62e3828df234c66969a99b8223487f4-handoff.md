# task-e62e3828 — Current expansion safe-release evidence (BLOCKED again, 4th time)

architect-77071474, 2026-08-17, HEAD a42ae2f. Epic M2 Graph Beta. CRITICAL.
Supersedes the 2026-08-13 and 2026-08-15 handoffs. **Their open question is now answered.**

## The question three architects left open
"Does the completed prerequisite (task-6cbff010) retain every named release fact?"
**No — and it never had them.** 6cbff010 landed attempt DISPATCH. The release facts have no
production producer at all. Confirmed independently at a42ae2f; the earlier fa39721 measurement
by architect-27b64950 still holds unchanged.

## The measurement, reproducible
`ExpansionReleaseEvidence` = packages/core/src/expansion/expansion-planning-hold.ts:27-36,
**17 fields**. Grep each field name across `apps/daemon/src --include=*.ts`, excluding `test`:

**ZERO production producers (15 of 17):** effectsTerminal, leaseState, observationRef,
providerSlotState, receiptRef, safeBoundaryObserved, terminalEffectRefs, terminalResourceRefs,
resourcesTerminal, strongestReason, terminalTarget, attemptState, leaseRef, providerSlotRef,
and `WORK_RELEASE_OR_PAUSE`. Several have 1-3 hits but **every one is inside a
`*-test-fixtures.ts`** — recovery-test-fixtures.ts:107-108,123,142 and
bootstrap-test-fixtures.ts:191-192,200.

**Reachable (2 of 17):** `attemptRef` (durable attempt record carries attemptAggregateId /
attemptId) and `truthClass` (the reader's own DAEMON_VERIFIED stamp, earned by reading durable
bytes).

## The shape of the gap — kernels yes, persistence no
The pure kernels that COMPUTE these facts all exist:
`packages/runner/src/supervisor/drain-table.ts` (`WORK_RELEASE_OR_PAUSE`, rank 20, terminalTarget
RELEASED), `drain-disposition.ts`, `packages/runner/src/recovery/safe-boundary.ts`,
`packages/scheduler/src/authority/lease-drain.ts`, `authority-kernel.ts`.
**Nothing in apps/daemon persists any of it.** `attemptRecordBody`
(work/foundation-attempt-contracts.ts:182-199) writes activationDigest, attemptAggregateId,
attemptId, effectId, grantId, inputManifest, nodeKey, observation, reasonCode, reasonLayer,
registration, resultManifest, sessionId, stdout/stderr sha, truthClass, wrapperIdentity — and
zero release/lease/slot/terminal/boundary/receipt/handoff facts.

This is the exact "the kernel exists, the durable edge does not" shape. Do not let a grep that
hits `packages/` convince you the daemon can read it.

## Four prerequisites, disjoint fact sets, all PLANNING
- **task-740924b7** — reason, disposition{resumable,strongestReason,terminalTarget},
  attemptState, leaseRef, leaseState, providerSlotRef, providerSlotState.
- **task-6d400781** — effectsTerminal, terminalEffectRefs, resourcesTerminal, terminalResourceRefs.
- **task-ded026d6** — safeBoundaryObserved, observationRef.
- **task-af9454f4** — receiptRef + the `{digest, ref}` worker handoff. **Created by this pass**;
  it was the only fact pair no task owned. Both 740924b7 and 6d400781 explicitly scope these out.

Together they cover 15/15. Nothing else is missing.

## Why the handoff is load-bearing, not cosmetic
core `safeRelease` (expansion-planning-hold.ts:209-215) admits a hold only when ELEVEN conjuncts
hold, the last being `same(value.handoff, command.workerHandoff)` — the durable release
evidence's handoff must deep-equal the create command's. With no durable producer the daemon can
only echo the caller, which is exactly the caller-supplied authority the epic forbids.
`receiptRef` is NOT inside `safeRelease` but IS a required key of the `release()` parser
(:147), so a release object lacking it fails to parse and the hold is unbuildable.

## For whoever unblocks this
Do NOT plan the reader until all four are DONE, and re-measure the 15 fields then —
**including these counts**. See `mem:gotcha-a-blocked-handoff-goes-stale-in-both-directions`:
this task has now been blocked four times, and each note's measurements aged.
Downstream: task-738a12a8 and task-c4171c1c unblock when the reader lands.
