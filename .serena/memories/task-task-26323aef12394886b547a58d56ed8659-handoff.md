# Bounded daemon graph-preview ingress — QA APPROVED

Implementation (worker) and QA verdict for `@moe/daemon`.

## Shipped
- `apps/daemon/**` + apps/daemon pnpm-lock importer only. Sole runtime export `evaluateGraphPreviewRequestBytes` from package root.
- Contract `moe-graph-preview-request/1`: bounded decode failures -> `INPUT_REJECTED` (decoder code/message preserved), envelope defects -> shared frozen `REQUEST_INVALID`, valid envelopes -> `REQUEST_EVALUATED` carrying the `previewGraphSnapshot` result unchanged.
- All envelopes frozen, `advisoryOnly: true`, `authority: "NONE"`. Internal subpaths sealed.
- `src/index.ts` = 111 lines; test = 287 lines. Deps exactly `@moe/contracts` + `@moe/scheduler`.

## QA evidence (re-run independently 2026-08-07)
- `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test` -> exit 0, 1 file / 28 tests passed.
- Repo-wide `pnpm typecheck` clean; `pnpm test` -> 54 files / 430 tests passed. No regressions.
- Runtime probes beyond the suite, all correct: `__proto__` extra key -> `REQUEST_INVALID` (no prototype pollution); 200-deep nesting -> `INPUT_REJECTED`/`JSON_DEPTH_LIMIT_EXCEEDED`; `SharedArrayBuffer`-backed view -> `JSON_INPUT_TYPE_INVALID`; Node `Buffer` accepted -> `ANALYZED`; `"options": null` -> `REQUEST_EVALUATED` + `OPTIONS_INVALID` (domain failure never mislabeled as envelope failure); every branch `Object.isFrozen` true; `import("@moe/daemon/src/index.ts")` -> `ERR_PACKAGE_PATH_NOT_EXPORTED`.

## Open follow-up (not a defect of this task)
Root `vitest.config.ts` includes only `packages/**` and `tests/**`, so the repo-wide `pnpm test` does NOT execute the 28 daemon tests — they run only via the package-scoped script. Any future task that owns the root Vitest config should add `apps/**/*.test.ts`, otherwise daemon regressions stay invisible to the repo-wide gate. See `mem:gotcha-vitest-app-root-config`.

## Git note
Owned bytes landed inside shared auto-commit `bc8e4f5` (titled for concurrent task task-52fe511b...). Do not rewrite or reset that commit. See `mem:gotcha-moe-wrapper-autocommit`.
