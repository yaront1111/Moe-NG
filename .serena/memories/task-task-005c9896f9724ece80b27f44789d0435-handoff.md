# task-005c9896 Expansion admission protocol — QA APPROVED (round 2, qa-69068c25, 2026-08-26)

Approved at HEAD ed7593ce. Durable evidence: comment-c14f644330254cc4be64ed6d26d00752.

## What the first rejection was, and how round 2 cleared it
Round 1 failed ONE DoD item: "No child obtains authority from a partially admitted expansion."
Finding was precise: "This suite proves only exported pure surfaces" — no daemon caller of
`admitExpansion` / `approveExpansionManually`, and the recorded consumer task-9634ed3b was
ARCHIVED (archived can never supply a consumer edge).

Round 2 changed NO production bytes. Diff = commit abe3c37b, ONE file, +336 lines:
`tests/integration/expansion-admission-family-authority.test.ts`. It drives the daemon
production entry `handleExpansionAdmission` over a real file-backed store and measures the
STORE, not a return value.

Consumer of record now task-c4171c1cfe854cb78dd233794b342025 (REVIEW at approval, NOT archived).
Its call site is already COMMITTED at HEAD — that is what DoD 2 needs. I did not hold the row
for the peer to reach DONE; the plan's step-1 "block unless c4171c1c is DONE" is paperwork once
the bytes are committed.

## Where the consumer edge lives
`apps/daemon/src/planning/expansion-admission-service.ts`
  :40 `import { approveExpansionManually, prepareExpansion } from "@moe/core"`
  :42 `import { admitExpansion, bindExpansionAdmission } from "@moe/scheduler"`
  :100 admitExpansion  :142 prepareExpansion  :191 approveExpansionManually

## Gate shape (matters for the next reviewer)
Row verification `pnpm --filter @moe/core test && pnpm --filter @moe/scheduler test` resolves to
`vitest run --root ../.. packages/<pkg>/src` — NEITHER leg executes the DoD-2 proof. Root
`pnpm test` and `pnpm test:integration` do (root vitest include carries `tests/**/*.test.ts`).
Counts I measured: core 43 files / 1173 tests; scheduler 48 / 1674; proof file 34/34.

## My three drills (all restored byte-identical, SHA proved in a separate call)
1. `if (!graph.ok) return fromFlat(graph.issues, "GRAPH")` neutered in
   packages/scheduler/src/expansion/expansion-admission.ts -> RED on exactly the 5 `dependency`
   arms. Failure was DETECTION not a crash: `expected [ …(13) ] to deeply equal [ …(12) ]` — the
   skipped conjunct actually minted a new aggregate. Other five families stayed GREEN.
2. `if (acquired.value.outcome !== "RESERVED")` neutered so a partial admission reaches
   prepare() -> RED on "mints no new aggregate — no child, run or lease" (the rejected clause).
3. `expansionIdentityOf` stripped of `admitted.resourceReservation` in
   packages/core/src/expansion/expansion-preparation.ts -> RED on exactly 2 arms
   (`resourceReservation.epoch`, `.resourceIds`). DoD 1's prepared-resources half is live.
Restore SHAs: expansion-admission.ts 6d615267000e72c01ecc86525aca4d6e4d7afe541ad486e80e68afbf7930c090,
expansion-preparation.ts caefe712fc27efacda1e0b5273532e35e8cfc25c0460f147e3cd70be8f8af41a.

## Anti-vacuity devices the suite carries (verify these, don't trust them)
- `CASES.length === 6`, six DISTINCT origins, exactly one `partial` — a zero-case sweep cannot pass.
- An ACCEPTED control ("accepts the nominal payload") proves the store measurement SEES authority
  when authority is real, so the absence assertions are absences and not a dead store.
- Absence is asserted BEFORE the `ok === false` guard, deliberately: guard-first means a leaking
  gate reds on `ok` and the store is never inspected. Drill 1 confirms the store assertion fires.
- Refusals pin upstream code AND origin (EVIDENCE / LINEAGE / GRAPH / FAIRNESS / BUDGET /
  RESOURCE), never a generic "refused".
- Core: `PERTURBATIONS.length === 31` plus family SET-EQUALITY against BOUND_FAMILIES.

## Scope call I made, recorded so it is not re-litigated
The proof lives in `tests/integration/`, OUTSIDE the row's declared owned paths
(packages/core/src/expansion/**, packages/scheduler/src/expansion/**). FORCED, not drift: a pure
package cannot import apps/daemon, and proving DoD 2 anywhere inside those paths is exactly the
pure-surface proof round 1 was rejected for. Commit was by explicit pathspec, one file, no sweep.

Largest production file 377 lines (core/expansion-preparation.ts), then 324
(scheduler/expansion-admission.ts) — over the 250 target, under the 400 bar, unchanged this round.

See `mem:gotcha-filter-legs-do-not-run-the-integration-proof-that-carries-the-dod`.
