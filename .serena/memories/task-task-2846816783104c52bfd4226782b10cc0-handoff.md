# task-2846816783104c52bfd4226782b10cc0 — QA APPROVED (qa-50f0d628, 2026-08-14)

Reviewed by base-ref diff `6ffb4fe..HEAD` over owned paths. Worker's code landed
across earlier commits + `a9dadd9`. `55028d1` is a FOREIGN whole-tree hook commit
carrying this task's id but only `.moe/**` + `packages/runner/.../claude-launch-selection.test.ts`
(task-d23a913f's bytes). Per epic rail, judged the bytes, not the commit label.
Working tree clean over owned paths, so HEAD bytes == gated bytes.

## Gates re-run by QA (all foreground, fresh)
- Task command verbatim: `pnpm --filter @moe/daemon typecheck && pnpm --filter
  @moe/daemon exec vitest run --root . --config package.json
  src/recovery/recovery-incarnation.test.ts src/identity/genesis-recovery-binding.test.ts`
  -> CHAIN_EXIT=0, 2 files / 110 tests.
- DoD-5 breadth: `src/recovery/ src/identity/ src/index-surface.test.ts` -> 24 files / 508 tests.
- Whole package `pnpm --filter @moe/daemon test` -> 69 files / 1492 tests, exit 0.
- Per-file lines (`grep -c ''`): context 174, codec 176, genesis 181, contract 218,
  incarnation 236, anchor 139, genesis-recovery-binding 129. All <= 250.

## Three mutation drills QA ran against PRODUCTION (Edit, sha256-verified restore)
1. Drop origin tag from `contextParts` -> "separates two origins whose raw context
   parts are byte-identical" RED on incarnationRef equality.
2. Drop `publicKeySpkiHex` from `deriveIncarnation`'s bindingDigest input ->
   "binds the published SPKI into the binding digest" RED.
3. Let `decodeBinding` strip `restoreCommandId` before the GENESIS exact-key check
   -> "refuses a genesis payload that counterfeits a restore fact" RED
   (`expected { …(12) } to be null`); the drifted-row sweep stayed green, so it
   reddened for the right reason.
   All three restored to the pre-drill sha256 (context acfbfdc4…, codec 7b190599…).

## Residual, disclosed not blocking
`anchorIncarnation` still accepts the UNION, but `readAnchoredIncarnation` now
skips GENESIS rows. Re-anchoring an IDENTICAL genesis binding therefore returns
`false` (expectedVersion:0 conflict) instead of the restore path's idempotent
`true`. Unreachable in production — all four call sites (recovery-key-provider
:120/:149, recovery-succession:351, restore-test-harness:218) pass
`RestoreIncarnationBinding` — and it fails CLOSED. Whoever adds a durable genesis
anchor (task-e19074f841f9450296799abfba9bfcaa /
task-69fdb23140ab472ebd34c3e4478c3ef2) needs a genesis-side reader first, or
crash-retry of a genesis anchor will read as a conflict.

## Scope line the next task must not misread
Installed payload is STILL `encoder.encode(binding.storeContextDigest)` (64 hex).
The discarded-proof defect SURVIVES by design; what changed is the proof is now
RETURNED and `encodeBinding` can carry it.
