# task-52ec1406 — Crash reconciliation services — QA verdict: APPROVED

Approved by `qa-cbad3a29`. Commit `cec2b65` on `moe/work-2026-08-08` (`5f1fdb5` is board
metadata only, no code). 9 files, 1809 insertions, all under `packages/runner/src/recovery/`
and `apps/daemon/src/recovery/`.

## What I re-ran rather than trusted

- `pnpm --filter @moe/runner test` → 25 files / 852 passed. `pnpm --filter @moe/daemon test`
  → 6 files / 98 passed. Both exit 0, run twice (before and after my drill).
- `npx tsc --project packages/runner/tsconfig.json --noEmit` and the daemon equivalent, both 0.
  The typechecks matter here: `RuntimeCommandKind` annotations are what stop a drifted
  `recovery.reconcile_external` / `work.resume` from becoming an instruction nobody can carry out.
- `wc -l` on the four production files: 230 / 229 / 213 / 171. Under the 250 target. Never used
  PowerShell `Measure-Object -Line` (`mem:powershell-measure-object-line-undercounts`).
- `git log -- apps/daemon/package.json pnpm-lock.yaml` — last touched by OTHER tasks. Step 1's
  ownership amendment was correctly NOT taken, because a sibling had already landed what was needed.

## My own mutation drill — 11 mutants, 11 killed, 0 survivors

I did not take the worker's 24-mutant drill on faith; I ran an independent 11 against the six
mechanisms the DoD turns on. Method that made it trustworthy: a Node script outside the repo
(`%TEMP%`, deleted after) that for each mutant asserts the anchor string matches EXACTLY ONCE,
writes the mutation, runs the focused suite, restores from the in-memory original, and re-reads to
confirm. Then `git hash-object` on all three files against pre-drill hashes, plus `git rev-parse
HEAD` before/after — because in this shared worktree another agent's hook can commit a drill edit
(`mem:mutation-drills-in-shared-worktree`). 3/3 hashes identical, HEAD unchanged, dirs clean.

Using the Edit/Write path with exact strings sidesteps the perl-delimiter traps entirely
(`mem:gotcha-perl-delimiter-choice-silently-voids-a-drill`,
`mem:gotcha-perl-brace-delimiter-fakes-a-surviving-mutant`).

The one that mattered most: replacing `claimed.epoch > held.epoch` with `true` in
`ownershipProven` reddens "refuses to adopt on an authority whose epoch never advanced". So DoD 3's
ownership-theft protection is TESTED, not merely written. Neutralising the token operand
independently reddens its own named test, so the two conjuncts are separately load-bearing.

Also killed: stale-observation refusal (2 red, including the one asserting the outcome falls
OUTSIDE `RECOVERY_OUTCOME_KINDS` — refused is not dropped), SUSPECT-unestablished (5 red across 2
files), quarantine resource gate (3 red), DoD 1's UNKNOWN arm rewritten as the optimistic
`!== "ACTIVE"` (2 red), drain downgrade, resume proven-boundary operand, doctor stale-authority,
doctor exact-key, doctor `confirmationRequired` (5 red).

## Why the DoD items are actually met

- **DoD 1** — `admitSuccessorOverlap` admits only on POSITIVE equality with `PROVEN_RELEASED`, so a
  sixth lattice member added later blocks by default. `ACTIVE` and `UNKNOWN` carry distinct codes
  and a test asserts the two codes differ, which is the assertion that survives a lazy collapse.
- **DoD 2** — five frozen kinds, no `RESUMED` arm to return. Set equality asserted in BOTH
  directions against a HAND-TRANSCRIBED list, so a dropped member cannot agree with itself
  (epic rail 6). 11 uniquely-labelled cases with the count asserted non-zero AND exact, plus
  per-kind counts, so a classifier collapsing onto one kind is visible.
- **DoD 3** — strictly higher epoch AND a different token. `presenceLooksLive` is parsed and then
  read by no decision path; I confirmed that by reading `crash-classification.ts`, not by claim.
  Extended grep (`child_process|spawn\(|\bkill\(|Date.now|Math.random|new Date\(|performance.now|
  setTimeout|hrtime`) over both owned dirs: zero hits, so timer-based theft is unwritable.
- **DoD 4** — both gates exit 0.

## The one judgment call, recorded so it is not re-litigated

`CrashClassification` has a SIXTH arm, `REFUSED`, outside `RECOVERY_OUTCOME_KINDS`. Reached only by
malformed input, unproven ownership, or a stale observation. I did NOT treat this as a DoD 2
violation: it is fail-closed, carries a stable code and layer, and cannot resume — epic rail 4 asks
exactly this of unverifiable evidence. The alternative (mapping an unproven-authority crash into
`ADOPTED`) is the bug the DoD exists to prevent. The sweep's set equality covers well-formed crash
situations, which is what "injected crash" means.

Related: `mem:convention-runner-recovery-composes-supervisor`,
`mem:task-task-52ec1406b1064892b5b281307f514007-handoff`.
