# Graph revision reducer decomposition — DONE (commit f406c47)

Task `task-5a95354855304c24a6af27538ab9e131` implemented and handed to QA.
Supersedes the earlier plan-phase version of this memory.

## What landed

Commit `f406c47` `refactor(task-5a95…): Graph revision reducer decomposition`,
exactly two paths, 82 insertions / 62 deletions:

- NEW `packages/core/src/planning/graph-revision-results.ts` — 73 lines.
- `packages/core/src/planning/graph-revision-reducer.ts` — 259 → 206 lines.

The eight helpers moved verbatim from reducer lines 64-122: `rejected`,
`unknownFailure`, `illegal`, `rebound`, `supersededAuthority`,
`versionConflict`, `accepted`, `clonedState`. Only change to each is the added
`export` keyword. Added one 3-line module doc naming the module's single
responsibility.

Reducer diff is exactly three edits: dropped the now-unused
`import { createRuntimeError } from "@moe/contracts";` (0 references remain),
added a 9-line alphabetized named import from `"./graph-revision-results.js"`
placed between the contract and validation import blocks, deleted the moved
block. Header lines 1-17, both frozen constant exports, every handler, and
`reduceGraphRevision` are untouched.

`rejected` is called only by the other moved helpers, so the reducer imports 7
of the 8. Exporting all 8 anyway matches sibling `planning-results.ts`, which
also exports an internally-only-used `rejected`.

## Surface facts worth reusing

- Only three importers of `graph-revision-reducer.js` exist —
  `packages/core/src/index.ts:123`, `planning-invariants.test.ts:12`,
  `graph-revision-reducer.test.ts:15` — and all three take only
  `GRAPH_REVISION_COMMAND_KINDS`, `GRAPH_REVISION_TRANSITIONS`,
  `reduceGraphRevision`. Nothing outside depended on the private helpers.
- Root `index.ts` re-exports no `*-results` / `*-snapshot` / `*-validation`
  module. `graph-revision-results.ts` is package-internal by that convention;
  do NOT add it to the root index.
- `graph-revision-results.ts` never imports the reducer — no cycle. Keep it
  that way: helper modules stay leaves.

## Verification actually run

`pnpm --filter @moe/core typecheck && pnpm --filter @moe/core test` → exit 0,
13 test files, 226 tests passed, 621ms. Held-out `graph-revision-reducer.test.ts`
and `planning-invariants.test.ts` ran byte-unchanged.

Move proof: sorted chunk-set equality vs `git show HEAD:<reducer>` — 19 chunks
each side, 0 duplicates. Plus identical `reduceGraphRevision` call-order
fingerprint, deepFreeze 12/12, Object.freeze 8/8, byte-identical header prefix.
See `mem:gotcha-byte-identical-refactor-proof` for the script shape, and
`mem:gotcha-export-keyword-defeats-chunk-equality` for the normalization the
naive version gets wrong.

## Follow-on

`task-cefb2442` (Graph revision test fixture extraction) touches the same area.
The reducer's helpers are no longer in the reducer file — grep
`graph-revision-results.ts` before assuming otherwise.
