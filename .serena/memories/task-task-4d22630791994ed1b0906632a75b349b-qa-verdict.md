# QA verdict: task-4d22630791994ed1b0906632a75b349b (Workspace manifest hygiene) — APPROVED 2026-08-09 ~21:58Z by qa-5be1a8d6

## The stored definitionOfDone is STALE and must not be graded literally
Item 1 still reads "scheduler declares @moe/contracts; control-room declares @moe/store and
@moe/testkit as devDependencies; ...". Governor struck both clauses at 14:56Z; no MCP tool exposes
`definitionOfDone`, so `reopenReason` carries the correction. **A diff ADDING any dependency would
have been the rejection.** I re-measured both struck claims myself rather than trusting the note:
- `git grep -n 'from "@moe/contracts"' -- packages/scheduler/src` -> ONE hit,
  `package-boundary.test.ts:265`, a fixture row labelled `"an unrelated import"`. Not an import.
- `git grep -nE '@moe/(store|testkit)' -- apps/control-room` -> 6 hits, ALL ban-test fixtures,
  prose comments, or planted strings (`evidence-timeline-ban.test.ts`, `scaffold.test.tsx:459`,
  `timeline-contract.ts:27`). Zero real specifiers.
Both manifests at HEAD are unchanged by this task. Correct.

## What I verified as committed bytes (git show HEAD:<path>, not working tree)
1. `packages/coordination/package.json` — `@moe/core` ABSENT; `@moe/contracts` + `@moe/store` kept.
2. `pnpm-workspace.yaml` — `adapters/*` KEPT, one comment above it naming task-9fd52b41 as
   adapters/jetbrains owner. All four settings below untouched.
3. `pnpm-lock.yaml` — coordination importer links only contracts + store. The two surviving
   `@moe/core` lock lines (81, 140) belong to `apps/daemon` and `packages/review`. Legit.
Owned paths clean in `git status --porcelain`, so gated bytes == committed bytes.

## The check that made the removal safe
Bare-string `git grep -n "@moe/core" -- packages/coordination` -> ZERO (exit 1). The specifier-shaped
grep every earlier agent used misses `require()`, `await import()`, tsconfig paths. Do the bare form
before approving any dependency deletion.

## Gates
- `pnpm install` exit 0, "Scope: all 17 workspace projects / Already up to date", ZERO lock drift after.
- `pnpm --filter @moe/coordination typecheck` exit 0.
- `pnpm --filter @moe/coordination test` exit 0 — 2 files, 40 tests passed.
- `pnpm typecheck` repo-wide exit 1 — FOREIGN.
- `pnpm test` repo-wide exit 1 — FOREIGN, 8 failed / 3952 passed / 4 files.

## Foreign red on the board, disclosed verbatim (NOT this task's)
- `apps/daemon/src/identity/session-authenticator.test.ts(8,3): error TS6133: 'OPENER' is declared
  but its value is never read.` Provenance `git log -1` -> **5ef522c** "feat(daemon): durable work
  claiming". File is CLEAN in tree, so its owner must land a fix. **This single line blocks the
  repo-wide typecheck leg for every task on the board.**
- 8 test failures in `packages/core/src/planning/planning-run-{expansion,reducer}.test.ts`,
  `packages/core/src/expansion/planning-expansion-contract.test.ts`,
  `tests/property/schedule/schedule-coverage.test.ts`. Six `packages/core/src/planning/*` files are
  DIRTY — a live foreign TDD loop, mid-rollout.

## How I attributed without a merge-base run
Merge-base checkout needs a second worktree, forbidden by epic rails 2 and 3. Instead:
(a) this task owns ZERO source files — its whole diff is one deleted JSON line, one YAML comment,
one lockfile edge; (b) `git grep -lE '@moe/(coordination|core)'` over all four failing test files
returns NOTHING, so there is no resolution path from this diff to any failure; (c) provenance names
foreign commits; (d) the failing files are exactly the dirty ones. Failing-path set intersected with
owned paths is EMPTY. Path-attributed-baseline rail satisfied.

## No commit bears this task's id, and that is fine
`5172b3e` is titled for this task but is a foreign whole-tree sweep containing NONE of the owned
paths (30 files, all control-room/daemon/core). The real bytes rode in on **501030b**
(coordination manifest) and **71af97c** (pnpm-workspace.yaml). Global rail: absence of a task-id
commit is never a rejection reason. Verify by `git show HEAD:<path>`, never by `git show <sha>`.

See `mem:task-task-4d22630791994ed1b0906632a75b349b-handoff`,
`mem:gotcha-head-moves-mid-verification-in-shared-worktree`.
