# QA verdict: Protected budget reservations — APPROVED

Task `task-318c0732094c417f96932b5c83e7388b` (SPIDR 17.03), reviewed 2026-08-08 by `qa-f4052923`.
Worker handoff: `mem:task-task-318c0732094c417f96932b5c83e7388b-handoff`.

## What I re-ran myself (not trusted from the summary)

- `pnpm --filter @moe/scheduler typecheck` -> exit 0.
- Task gate `... exec vitest run --root ../.. packages/scheduler/src/budget/budget-reservation.test.ts`
  -> **exit 0, 40/40 passed**.
- Full `pnpm --filter @moe/scheduler test` -> **30 files / 473 tests, exit 0** (no regression from
  the 17.04 sibling landing mid-flight).
- Sizes measured **at HEAD** per `mem:gotcha-shared-index-commit-capture`:
  `budget-reservation.ts` 249, `.test.ts` 250, `.js` 1 and the bridge is exactly
  `export * from "./budget-reservation.ts";` — same shape as the `budget-account.js` /
  `budget-contract.js` siblings.
- Greps: `@moe/core|@moe/contracts|scheduler/src|@moe/scheduler/` -> one hit, in a header COMMENT
  stating core is deliberately not imported. `Date.|Date(|Math.random|performance.` -> zero.

## Independent mutation red-checks (6/6 red), file restored byte-identical

I did not take the worker's eleven-mutation claim on faith. Out-of-tree backup first,
`trap restore EXIT`, replacement asserted to hit exactly once (a silently-unapplied mutation
would read as a green false negative):

| mutation | result |
| --- | --- |
| `.available ?? 0) < amount` -> `< 0` (all-or-none / boundary fence) | 3 failed |
| `[reservation.state !== "RESERVED", ALREADY_SETTLED]` -> `[false, ...]` (one-use marker) | 4 failed |
| `PROTECTED_ADMISSION_PURPOSES.some(...)` -> `false` (protected-purpose fence) | 4 failed |
| `command.neverStartedProofRef === null` -> `false` (never-started requirement) | 2 failed |
| `shiftView(view, totals, -1)` -> `+1` (refund direction) | 1 failed |
| `[reservation.version !== expectedVersion, STALE_VERSION]` -> `[false, ...]` (CAS fence) | 2 failed |

Restore verified by `git hash-object` equality (`b87bda64...`) plus an empty
`git status --porcelain -- <path>`. The harness pattern that works here: `cp` to `/tmp` BEFORE the
first mutation, `trap restore EXIT`, node `split`-count assertion instead of `sed`.

## DoD mapping

1. All-or-none: `reserveForAdmission` refuses the whole set on any short line; tests assert
   `refused.view` is the SAME reference plus a JSON snapshot equality, and `reservation === null`.
2. Boundary both directions (`[MS 90, AT 1]` succeeds to zero available; `[89,1]` and `[90,0]`
   refuse) + protected lines deep-equal after EXECUTION activation, and both relabel attacks
   (protected->EXECUTION, EXECUTION->protected) refuse with distinct codes.
3. One-use: second activate at the fresh version -> `ALREADY_SETTLED`; the stale competitor applied
   to the winner's successor -> `STALE_VERSION` with the view returned by reference.
4. Precedence: every row of the 15-case table supplies NO gate and still asserts its own code,
   which is what proves each beats the last-checked gate rule.
5. Sizes/bridge/gate verified above.

Final test `expects [...observed].sort()` to equal all 18 published codes — a real
case-list guard (`mem:pattern-guard-the-case-list-not-just-the-cases`), not decoration.

## Accepted, documented limitations (not defects)

At-most-one reservation per admission is the store's job (this module supplies the CAS fence and a
deterministic length-prefixed `reservationId` to insert on); `fail()` deep-freezes the caller's own
view object; gate rejection is LAST in precedence so an unauthorized caller with a shortfall learns
`INSUFFICIENT_AVAILABLE` first. All three are stated in the worker's handoff, none violate a DoD.

## QA-side inverse of `mem:gotcha-shared-index-commit-capture`

`git log` showed TWO commits carrying this task's ID. `05b0dbf` is the worker's explicit-pathspec
commit — exactly the 3 owned paths, 500 insertions. `633422f`, two minutes later, is the wrapper's
end-of-session auto-commit (`Completed via Moe worker session.`) and contains **zero of this task's
files** — only `.moe` state and other agents' in-flight control-room work.

**So: never audit a task by grepping `git log` for its task ID.** The sweep commit borrows the ID of
whatever session was active. Find the worker's own commit (real body, explicit pathspec) and read
that. The same boilerplate body appears on `0ece364` for task-5ee5b801, so it is systemic, not this
worker's rail violation.
