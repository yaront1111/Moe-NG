{
  "id": "task-620953132b1b4844afcc9bcf4ffeac5e",
  "epicId": "epic-bf111658f9694b558bdc5596bbf0f924",
  "title": "[b2548479-pre] Durable CutoverAttempt aggregate: a daemon writer for cutover.admit_activate_approval (ACTIVATE_APPROVED from an admitted GO_ACTIVATE binding) and its fold reader through reduceCutover",
  "description": "PREREQUISITE carved from task-b254847909ca4199a70a3a06173f1cd9 (cutover.activate handler) by architect-c1a63707 at HEAD 3ded5047. Its DoD-3 requires the handler to refuse \"when the attempt is not ACTIVATE_APPROVED\" and to move ACTIVATE_APPROVED -> ACTIVE in one transaction — but NO daemon code persists a CutoverAttempt state at all: `git grep -n 'reduceCutover\\|CutoverAttemptState\\|CutoverAttempt' HEAD -- apps/daemon/src ':!**/*.test.*'` is EMPTY (worker-a224437e's comment-7fd72f09 measured the same at f27ba49b). The core reducer exists and is barrel-reachable: packages/core/src/cutover/cutover-reducer.ts `reduceCutover` (:235) with the edge table in cutover-contract.ts (`cutover.admit_activate_approval` -> ACTIVATE_APPROVED :66; `cutover.activate` requires ACTIVATE_APPROVED :52 and yields ACTIVE :67). The admission exists: packages/benchmark/src/activation-binding.ts `admitActivationBinding` (:149) verifies the human GO_ACTIVATE decision through decideApprovalAuthority (:161/:167) and emits a frozen ActivationBinding with `generations` (:62-65).\n\nDELIVER: a daemon-owned writer `cutover.admit_activate_approval` (house handler shape, NOT registered in any command table — registration is task-b8272ee0's) that consumes ONLY an admitted binding (admitActivationBinding is the sole admission; its refusal passes through with its own code+layer), appends the reducer-legal event to a per-project cutover attempt aggregate (aggregate id derived server-side; expectedVersion fenced) so that folding the aggregate with reduceCutover yields ACTIVATE_APPROVED, and refuses with distinct stable codes when the fold is not in a state that admits the edge (reducer verdict verbatim) or when the binding's decision was already consumed (replay answers from the store, never a second event). Plus a fold reader `readCutoverAttemptState(store, { projectId })` returning the reducer's state or ABSENT/UNREADABLE with exact codes. No live-system action; fixtures + SQLite only. CONSUMER: task-b2548479 step 3/4 (its handler folds this aggregate; its accepted path needs ACTIVATE_APPROVED durable).",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, in a row comment with `git rev-parse HEAD` quoted, greps unpiped with exit codes plus a positive control: no daemon CutoverAttempt persistence exists (grep EXIT 1; EXIT 0 = adopt/verify/report duplicate); `reduceCutover` exported from @moe/core's barrel and `admitActivationBinding` from @moe/benchmark's; the @moe/benchmark edge in apps/daemon/package.json is present (worktree ` M` hunk or committed) — coordinate with task-b2548479 step 3(A): whichever row commits the manifest first owns the lock gate (NOTHING RE-RESOLVED), the other adopts; apps/daemon/src/cutover/ holds task-b2548479's UNTRACKED marker WIP — add beside, never stage.",
    "A fresh store folds to ABSENT through `readCutoverAttemptState`; after ONE `cutover.admit_activate_approval` with an ADMITTED binding the fold is ACTIVATE_APPROVED with exactly one event on the aggregate and a decision record; the admitted binding's decision identity is recorded on the event so a second admission of the same decision REPLAYS (zero new events, zero new decisions — counts asserted) and a different decision for the same attempt refuses with the reducer's own verdict code (the edge table refuses a second ACTIVATE_APPROVED).",
    "Fail-closed: an un-admitted or caller-shaped binding never reaches the writer (admission refusal forwarded with its code+layer; zero events); a binding whose generations tuple is caller-supplied is not a fact this row reads (the writer records the ADMITTED tuple only, and task-b2548479's handler compares it to the snapshot reader) — arm asserts the persisted tuple equals the admitted one byte for byte.",
    "Mutation drills with SHA256 restore proofs (new files untracked): skip the admission -> the caller-cannot-choose arm reds; write a second event on replay -> the replay-count arm reds; derive the aggregate id from the request -> the server-derived-id arm reds. Gates standalone/unpiped with counts: `pnpm --filter @moe/daemon typecheck` (quote `$ tsc`), from apps/daemon `pnpm exec vitest run --root . --config package.json src/cutover`, `pnpm --filter @moe/daemon test` path-attributed; every production source <= 250 lines with `.js` bridges; explicit-pathspec commit; completion comment names task-b2548479 (consumer) and task-b8272ee0 (registration) and states: 'ACTIVATE_APPROVED is now a durable, reducer-folded daemon fact minted only from an admitted GO_ACTIVATE binding'."
  ],
  "taskRails": [
    "admitActivationBinding is the ONLY admission; never re-validate or re-decide the human gate (rail 7A: the admission's decideApprovalAuthority is the fence); the reducer owns the edge table.",
    "Not registered here (task-b8272ee0 registers); no live-system action; fixtures + SQLite only.",
    "Preserve task-b2548479's untracked marker WIP and the manifest/lock discipline (nothing re-resolved); explicit-pathspec commits; <=250 lines per production source; TDD red-first through the writer."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK, then post ONE row comment (DoD-1). Re-measure at YOUR claim HEAD (`git rev-parse HEAD`, quoted) — the architect measured at b1c76b13 and the tree has live writers. Run each grep UNPIPED and quote its exit code: (a) `git grep -n 'reduceCutover\\|CutoverAttemptState\\|CutoverAttempt' HEAD -- apps/daemon/src ':!**/*.test.*'` — EXPECT EXIT 1 (empty). EXIT 0 means a peer landed this capability: STOP, read the owning row in the board and either adopt it or report a duplicate rather than re-implementing it. (b) POSITIVE CONTROL on the identical rev/pathspec form so an empty result cannot be a broken grep: `git grep -c 'export' HEAD -- apps/daemon/src/index.ts` — EXPECT EXIT 0 with a count. (c) `git grep -n 'reduceCutover' HEAD -- packages/core/src/index.ts` and `git grep -n 'admitActivationBinding' HEAD -- packages/benchmark/src/index.ts` — both EXIT 0. (d) `git status --porcelain apps/daemon/package.json pnpm-lock.yaml apps/daemon/src/cutover` — expect ` M apps/daemon/package.json`, a CLEAN lock, and the untracked peer trio cutover-activation-marker.{ts,js,test.ts}. MANIFEST/LOCK COORDINATION with task-b2548479 step 3(A): the `@moe/benchmark` line is an uncommitted worktree hunk and the lock ALREADY carries the apps/daemon importer entry (lock lines 126-128, committed). If the hunk is still uncommitted when you commit, THIS ROW OWNS IT — commit apps/daemon/package.json by explicit pathspec and run NO install, NOTHING RE-RESOLVED. If task-b2548479 has already committed it, ADOPT and do not touch the file. Verify the edge with a compiled probe, not a grep: write a temp module under apps/daemon/src/cutover importing `admitActivationBinding` from bare `@moe/benchmark` and `reduceCutover` + `type CutoverAttemptState` from bare `@moe/core`, run `pnpm --filter @moe/daemon typecheck` (quote the `$ tsc` echo and the exit code), and DELETE the probe in the SAME command. THE COMMENT MUST ALSO RESTATE THE DoD-2 CORRECTION VERBATIM, because QA grades against it: cutover-contract.ts:52 admits `cutover.admit_activate_approval` ONLY from IMPORT_VERIFIED and cutover-reducer.ts `create()` refuses INPUT_INVALID for any non-preview command on an absent aggregate, so 'fresh store + one admit = ACTIVATE_APPROVED' is unsatisfiable without breaking taskRail 1. The binding reading is: the fresh-store ABSENT clause stands literally; the accepted-path arm seeds the aggregate to IMPORT_VERIFIED through production `reduceCutover`; and 'exactly one event' means the aggregate grows by EXACTLY ONE across the writer call. Also record that no board row owns a daemon writer for the four earlier edges (preview/admit_quiesce/begin/complete/verify_import), so today only this writer's refusal paths are production-reachable — a recorded gap, not a new row. Note the ~725 foreign dirty paths and continue; they are never a stop condition.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/package.json"
      ],
      "modifiedFiles": [
        "apps/daemon/package.json"
      ],
      "note": "VERIFY-OR-BLOCK passed at HEAD a82f56c8: daemon persistence grep EXIT 1 with positive control EXIT 0; both producer barrels EXIT 0; bare-specifier compiled probe printed `$ tsc --project tsconfig.json` and exited 0, then was deleted. Row comment comment-4a9fd202aa3d43b1896e9baff0063424 records the DoD-2 correction, board gap, peer WIP, and manifest/lock adoption. apps/daemon/package.json is declared; no install or lock re-resolution performed.",
      "startedAt": "2026-08-29T19:17:35.791Z",
      "completedAt": "2026-08-29T19:19:36.362Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST, then green: `apps/daemon/src/cutover/cutover-attempt-contracts.ts` (+ its one-line `.js` bridge `export * from \"./cutover-attempt-contracts.ts\";`) and the first arms of `apps/daemon/src/cutover/cutover-attempt.test.ts`. This module holds ONLY vocabulary, identity derivation and the codec — no store call, no admission, no reduction. Deliver: `CUTOVER_ATTEMPT_LAYER = \"DAEMON_CUTOVER_ATTEMPT\"`; a frozen `CUTOVER_ATTEMPT_CODES` roster (every code must have a planned emitter — an unreachable code is a claim no test can pin) covering at minimum CUTOVER_ATTEMPT_STATE_ABSENT, CUTOVER_ATTEMPT_EVIDENCE_UNREADABLE, CUTOVER_ATTEMPT_SEQUENCE_INVALID, CUTOVER_ATTEMPT_EVENT_TYPE_UNEXPECTED, CUTOVER_ATTEMPT_VERSION_DESYNC, CUTOVER_ATTEMPT_EXPECTED_VERSION_CONFLICT, CUTOVER_ATTEMPT_STORE_UNAVAILABLE, CUTOVER_ATTEMPT_FIELD_INVALID, CUTOVER_ATTEMPT_REPLAY_DIVERGED; the constants `CUTOVER_ATTEMPT_EVENT_TYPE` and `CUTOVER_ATTEMPT_COMMAND_KIND`; a `CutoverAttemptStore` port naming ONLY `commitExpectedVersionDecision`, `readEvents` and `getCommandDecision` (mirror activation-ledger-contracts.ts:195-208 — a port that cannot name `commitWithApply` cannot reach for one); `deriveCutoverAttemptAggregateId(projectId)` — SERVER-DERIVED FROM THE PROJECT ALONE, with the bounded-namespace + sha256-overflow shape of deriveActivationAggregateId (activation-ledger-contracts.ts:269-274); `deriveCutoverDecisionId(binding)` returning a BARE 64-hex sha256 over the canonical admitted binding (decision, authority.grant.{gateId,workRef,principalId,principalKind,grantedAtEpochMs}, the four generations in ACTIVATION_GENERATION_KEYS order, sourceCommit) — NO PREFIX, because SAFE_REF caps refs at 64 chars; and `encodeCutoverAttemptEvent` / `decodeCutoverAttemptEvent` for a payload of `{ admitted: {generations, sourceCommit, principalId, grantedAtEpochMs} | null, command: CutoverCommand }` using canonical key order + TextEncoder for encode and `decodeBoundedJsonBytes` from @moe/contracts for decode (there is NO bounded-JSON encoder exported — index.ts:44 exports the decoder only). ARMS: derived aggregate id is byte-identical for two different bindings under the same projectId and differs across projectIds; `deriveCutoverDecisionId` output matches /^[0-9a-f]{64}$/ AND matches the reducer's own SAFE_REF /^[A-Za-z0-9._:/-]{1,64}$/, asserted so a future prefix cannot slip through; codec round-trips every field and a truncated/garbage payload decodes to a refusal carrying CUTOVER_ATTEMPT_EVIDENCE_UNREADABLE, not a partial record.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts",
        "apps/daemon/src/cutover/cutover-attempt-contracts.js",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts",
        "apps/daemon/src/cutover/cutover-attempt-contracts.js",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "note": "TDD red: focused Vitest exited 1 because cutover-attempt-contracts.js did not exist. Green: focused Vitest `Test Files 1 passed`, `Tests 5 passed`, exit 0; daemon typecheck printed `$ tsc --project tsconfig.json`, exit 0. Added the exact store port, frozen local code roster, server-only aggregate identity, bare 64-hex canonical decision identity, bounded canonical event codec, unreadable-evidence refusal, and exact one-line runtime bridge. Production source is 200 lines.",
      "startedAt": "2026-08-29T19:19:40.616Z",
      "completedAt": "2026-08-29T19:23:28.034Z"
    },
    {
      "stepId": "step-3",
      "description": "RED FIRST, then green: `apps/daemon/src/cutover/cutover-attempt-reader.ts` (+ one-line `.js` bridge) exporting `readCutoverAttemptState(store, { projectId })`. IT FOLDS, IT DOES NOT READ A SNAPSHOT: derive the aggregate id from projectId, `store.readEvents(aggregateId)`, decode each payload, and replay the stored COMMANDS through production `reduceCutover` starting from `undefined`, in aggregateSequence order. Result union: `{ status: \"PRESENT\", state, version, admitted }` where `state` is the reducer's own returned state and `admitted` is the head admit event's admitted record (null when the aggregate has not reached ACTIVATE_APPROVED); `{ status: \"ABSENT\", code: \"CUTOVER_ATTEMPT_STATE_ABSENT\", layer }` for zero events — this exact code is what task-b2548479's handler forwards, per its parked handoff; and `{ status: \"UNREADABLE\", code, layer, storeCode }` for everything else. Fail closed with DISTINCT codes: a thrown store error -> CUTOVER_ATTEMPT_EVIDENCE_UNREADABLE carrying `DurableStoreError.code` verbatim in `storeCode` (null for a non-store throw); a non-contiguous or non-1-based aggregateSequence -> CUTOVER_ATTEMPT_SEQUENCE_INVALID; an event whose eventType is not CUTOVER_ATTEMPT_EVENT_TYPE -> CUTOVER_ATTEMPT_EVENT_TYPE_UNEXPECTED; an undecodable payload -> CUTOVER_ATTEMPT_EVIDENCE_UNREADABLE; and a mid-replay reducer refusal -> UNREADABLE carrying THE REDUCER'S OWN error code and layer \"CUTOVER\" verbatim, never re-stamped with this module's layer. ARMS, each asserting the code AND the refusing layer: fresh store -> ABSENT/CUTOVER_ATTEMPT_STATE_ABSENT (this is DoD-2's literal fresh-store clause); a store whose readEvents throws DurableStoreError -> UNREADABLE with storeCode preserved; a hand-planted event carrying a command the edge table refuses from the folded state -> UNREADABLE with the reducer's ILLEGAL_TRANSITION and layer CUTOVER; a gap in aggregateSequence -> SEQUENCE_INVALID. Keep the module under 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cutover/cutover-attempt.test.ts",
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts"
      ],
      "newFiles": [
        "apps/daemon/src/cutover/cutover-attempt-reader.ts",
        "apps/daemon/src/cutover/cutover-attempt-reader.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-reader.ts",
        "apps/daemon/src/cutover/cutover-attempt-reader.js",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "note": "TDD red: focused Vitest exited 1 on missing cutover-attempt-reader.js. Green: focused Vitest `Test Files 1 passed`, `Tests 11 passed`, exit 0; daemon typecheck printed `$ tsc --project tsconfig.json`, exit 0. Reader derives the server aggregate, validates 1-based contiguous sequences and event type, bounded-decodes payloads, folds commands through production reduceCutover, preserves DurableStoreError.code and reducer code/layer, returns the exact ABSENT code, and carries only the admitted record on the approval edge. Reader is 112 lines; bridge is exact.",
      "startedAt": "2026-08-29T19:23:31.775Z",
      "completedAt": "2026-08-29T19:26:21.994Z"
    },
    {
      "stepId": "step-4",
      "description": "RED FIRST, then green — THE WRITER'S ACCEPTED PATH: `apps/daemon/src/cutover/cutover-attempt-commit.ts` (+ one-line `.js` bridge) exporting `admitCutoverActivateApproval(store, input)` where `input = { correlationId, decidedAt, projectId, record }` and `record` is the caller's UNADMITTED binding bytes. NOT REGISTERED ANYWHERE — do not touch any command table, roster or registry; task-b8272ee0 owns registration. Order of operations, each step's failure returning before the next runs: (1) `admitActivationBinding(input.record)` is the SOLE admission — never re-run decideApprovalAuthority, never re-check the gate; (2) fold via `readCutoverAttemptState`; (3) compute the store fence from the durable event count and refuse CUTOVER_ATTEMPT_VERSION_DESYNC when it disagrees with `fold.state?.version ?? 0` (risk 4 — the two counters coincide only by construction); (4) build the CutoverAdmitActivateApprovalCommand with `commandId = deriveCutoverDecisionId(admission.binding)`, `expectedVersion = folded version`, and witness EXACTLY `{ approvalRef: <that same 64-hex id>, truthClass: <the admitted class> }` — no extra key, or `validWitness` refuses; (5) `reduceCutover(foldedState, command)`; (6) on acceptance ONE `store.commitExpectedVersionDecision` with `key = { commandId, principalId: binding.authority.grant.principalId, projectId }`, `targetAggregateId = deriveCutoverAttemptAggregateId(projectId)`, `expectedVersion` = the fence, and `events: [ONE EventDraft]` whose payload is `encodeCutoverAttemptEvent({ admitted: <FROM admission.binding ONLY>, command })`. THE PERSISTED GENERATIONS TUPLE AND sourceCommit COME FROM `admission.binding`, NEVER FROM `input.record` — task-b2548479's handler compares that tuple to the snapshot reader, so a caller-chosen tuple would be laundered into authority here. ARMS: seed the aggregate to IMPORT_VERIFIED by driving production `reduceCutover` over cutover.preview -> admit_quiesce_approval -> begin_quiesce -> complete_quiesce -> verify_import and appending each reducer-minted payload through this row's production encoder and the same store seam (the seed uses the production reducer, it does not reimplement it); then ONE `admitCutoverActivateApproval` with an ADMITTED binding must yield ok, `readCutoverAttemptState(...).state.lifecycle === \"ACTIVATE_APPROVED\"`, the aggregate grown by EXACTLY ONE event (`after.length === before.length + 1`), that head event's `commandKind === \"cutover.admit_activate_approval\"`, `state.version === before.length + 1`, `state.activateApprovalRef === deriveCutoverDecisionId(binding)`, and EXACTLY ONE new CommandDecisionRecord for the derived key with `effectDisposition === \"EFFECTS_COMMITTED\"`. Use a real file-backed `SqliteEventStore.openForProject(mkdtempSync(...), projectId)`, per activation-ledger-commit.test.ts:14-20,57,65-66.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cutover/cutover-attempt.test.ts",
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts",
        "apps/daemon/src/cutover/cutover-attempt-reader.ts"
      ],
      "newFiles": [
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt-commit.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt-commit.js",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "note": "TDD red: focused Vitest exited 1 on missing cutover-attempt-commit.js. Green: real file-backed SqliteEventStore arm passed; focused Vitest `Test Files 1 passed`, `Tests 12 passed`, exit 0; daemon typecheck printed `$ tsc --project tsconfig.json`, exit 0. The test seeds five prior edges by running production reduceCutover and persisting this row's encoded commands, proves the nonzero version precondition, then asserts exactly +1 event, exactly +1 decision, ACTIVATE_APPROVED/version 6/derived approval ref, and EFFECTS_COMMITTED. Writer admits first, folds, fences fold/store versions, constructs the exact witness, persists admitted bytes only, and uses one decision-store call. Writer is 163 lines; bridge exact.",
      "startedAt": "2026-08-29T19:26:26.277Z",
      "completedAt": "2026-08-29T19:29:48.045Z"
    },
    {
      "stepId": "step-5",
      "description": "RED FIRST, then green — THE WRITER'S REFUSAL AND REPLAY PATHS (DoD-2 tail + DoD-3). Implement and pin, every arm asserting the CODE and WHICH LAYER refused, never merely that the call failed: (a) REPLAY — a SECOND call with the SAME admitted binding produces the same decision key, the store answers REPLAYED, and the writer answers by RE-READING the fold (mirror answerReplayed in activation-ledger-commit.ts) rather than echoing the caller; assert ZERO new events and ZERO new decisions as COUNTS taken before and after (`after.length === before.length` on both), plus an ok result whose state is still ACTIVATE_APPROVED; if the re-read fold's activateApprovalRef disagrees with the admitted decision id, refuse CUTOVER_ATTEMPT_REPLAY_DIVERGED. (b) DIFFERENT DECISION, SAME ATTEMPT — a second, DIFFERENT admitted binding for the same project folds to ACTIVATE_APPROVED, which CUTOVER_TRANSITIONS does not admit as a source for this kind, so the writer returns THE REDUCER'S OWN VERDICT VERBATIM (ILLEGAL_TRANSITION, layer \"CUTOVER\", with the reducer's `details.sourceState`), appends nothing and writes no decision — assert both counts unchanged AND the exact code+layer. (c) FAIL-CLOSED ADMISSION — for each of an absent record, a caller-shaped record, a GO_QUIESCE decision, a wrong workRef, and a generations-unbound record, the admission refusal is FORWARDED WITH ITS OWN CODE AND LAYER (ACTIVATION_BINDING_* at layer GA_ACTIVATION_BINDING, and a decideApprovalAuthority refusal verbatim at ITS layer), ZERO events and ZERO decisions are written, and — critically — the store is never called at all. (d) BYTE-FOR-BYTE ADMITTED TUPLE — feed a caller record whose `generations`/`sourceCommit` differ from what the admission normalises/validates and assert the PERSISTED payload's `admitted` record equals `admission.binding.generations` and `.sourceCommit` exactly, key by key, read back off the durable event, not off the return value. (e) STORE FAULTS — a thrown DurableStoreError maps onto CUTOVER_ATTEMPT_EXPECTED_VERSION_CONFLICT / CUTOVER_ATTEMPT_FIELD_INVALID / CUTOVER_ATTEMPT_STORE_UNAVAILABLE with `storeCode` preserved verbatim, and a RETURNED non-EFFECTS_COMMITTED decision (no throw) is treated as a refusal, not success. Keep cutover-attempt-commit.ts under 250 lines; split it before 400 if it grows.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt.test.ts",
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts",
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "note": "TDD red: 27-test focused run had 5 precise failures (replay blocked by reducer ordering, input/limit store codes misclassified, returned conflict lost provenance). Green: focused Vitest `Test Files 1 passed`, `Tests 27 passed`, exit 0; daemon typecheck printed `$ tsc --project tsconfig.json`, exit 0. Replay now verifies the durable prior decision/fence, reproduces the original request, obtains REPLAYED, re-reads the fold, and validates its approval ref; tests prove zero ledger growth and durable-object identity. Exact reducer ILLEGAL_TRANSITION/CUTOVER/sourceState is pinned for a different decision. Six admission refusals preserve their owning layers with zero store calls and zero writes. Persisted admitted generations/sourceCommit are read back and compared key-by-key. Thrown and returned store refusals preserve stable local and store codes. Production lines: contracts 203, commit 232, reader 112.",
      "startedAt": "2026-08-29T19:29:52.554Z",
      "completedAt": "2026-08-29T19:34:43.310Z"
    },
    {
      "stepId": "step-6",
      "description": "MUTATION DRILLS WITH SHA256 RESTORE PROOFS (DoD-4). Every module this row creates is UNTRACKED, so `git diff` is BLIND to it and an empty diff proves NOTHING — the restore proof MUST be SHA256 equality against an out-of-tree backup. Procedure per drill: (1) `git status --porcelain` the exact paths first and record that they are `??`; (2) copy each to %TEMP% with a filename suffixed by your worker id; (3) apply the mutation; (4) re-run the named arm and quote the RED output; (5) restore; (6) in a SEPARATE tool call from the restore, prove `Get-FileHash -Algorithm SHA256` equality between the backup and the restored file — a PowerShell copy can silently no-op and 'the command returned' is not evidence the bytes moved. DRILL 1 — delete the `admitActivationBinding` call and pass `input.record` straight through: the fail-closed arm 5(c) must RED (a refused caller record now writes an event). DRILL 2 — on the REPLAYED disposition, append a second event instead of re-reading the fold: the replay-count arm 5(a) must RED on `after.length === before.length`. DRILL 3 — change `deriveCutoverAttemptAggregateId` to fold a caller-supplied field (e.g. `record.sourceCommit`) into the id: the different-decision arm 5(b) must RED, because the second binding now lands on a FRESH aggregate and refuses INPUT_INVALID instead of the reducer's ILLEGAL_TRANSITION — the arm asserts the exact code, so a code swap alone is enough to fail it. A drill that stays GREEN is a FINDING, not a pass: strengthen the arm until it reds, then re-run and quote both states.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt-reader.ts",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts",
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt.test.ts"
      ],
      "note": "All exact mutated sources were `??` before drilling; restore used out-of-tree %TEMP% backups and SHA256 equality in calls separate from Copy-Item. D1 admission bypass: dedicated generation-unbound arm RED, expected ACTIVATION_BINDING_GENERATION_UNBOUND/GA_ACTIVATION_BINDING/ok:false but received ok:true. Restored commit SHA E6BF0403...007C == backup. D2 replay append: replay arm RED on counts, expected {decisions:1,events:6}, received {decisions:2,events:7}. Restored commit SHA E6BF0403...007C == backup. D3 caller-derived aggregate: initial incomplete mutant stayed green, corrected mutant moved reduction onto the caller-derived fresh aggregate; different-decision arm RED, expected ILLEGAL_TRANSITION but received INPUT_INVALID. Restored contracts SHA 17E4D061...A8436 and commit SHA E6BF0403...007C, each equal to backup. Post-restore focused suite: Test Files 1 passed, Tests 28 passed, exit 0. Daemon typecheck printed `$ tsc --project tsconfig.json`, exit 0. Test file gained the dedicated non-vacuous admission arm; contracts was touched temporarily because the prescribed aggregate-id drill targets its owned derivation function, then restored byte-identically.",
      "startedAt": "2026-08-29T19:34:49.028Z",
      "completedAt": "2026-08-29T19:39:50.891Z"
    },
    {
      "stepId": "step-7",
      "description": "ADVERSARIAL SELF-REVIEW, GATES, COMMIT, COMPLETION RECORD. First re-read your own diff as an attacker: can any caller-supplied byte reach the persisted `admitted` record or the aggregate id? does any path return ok without an EFFECTS_COMMITTED decision? does any refusal re-stamp another layer's code with ours? is every code in CUTOVER_ATTEMPT_CODES actually emitted by some arm? do two concurrent writers both win (the expectedVersion fence must make the loser refuse with a code, and that loser arm must assert the code, not just `!ok`)? Confirm every production source is <= 250 lines (`wc -l` per file, tests exempt) and that each `.js` bridge contains exactly `export * from \"./<name>.ts\";` and no transpiled output — a dirty bridge shadows its .ts and can green a gate against code that is not under test. GATES, standalone and unpiped, each exit code quoted separately (a `;`-chain reports only the last command and a pipe reports tail's): (1) `pnpm --filter @moe/daemon typecheck` — quote the `$ tsc --project tsconfig.json` echo as execution evidence, since exit 0 alone does not prove it ran; (2) from apps/daemon, `pnpm exec vitest run --root . --config package.json src/cutover` — quote the `Test Files N passed` AND `Tests N passed` count lines; a run with no count line executed NOTHING and is not a pass; (3) `pnpm --filter @moe/daemon test` — quote its count lines, and PATH-ATTRIBUTE any red: record repo-wide failures at the merge-base and at HEAD, subtract, intersect with this row's owned paths, and report any foreign red VERBATIM with its exact failing lines and file paths. Note that a bare `pnpm test` at the root does NOT cover apps/**, so it is not evidence for anything here. COMMIT by explicit pathspec naming ONLY: the three .ts modules, their three .js bridges, cutover-attempt.test.ts, and apps/daemon/package.json IF this row still owns the uncommitted `@moe/benchmark` hunk (step 1). Never `git add -A`; never stage the untracked peer trio cutover-activation-marker.{ts,js,test.ts} or any of the ~725 foreign dirty paths. Report every touched path in complete_step.modifiedFiles. COMPLETION COMMENT must name task-b254847909ca4199a70a3a06173f1cd9 (the consumer, whose handler folds this aggregate and forwards CUTOVER_ATTEMPT_STATE_ABSENT) and task-b8272ee020a940009a1 (registration), state verbatim: 'ACTIVATE_APPROVED is now a durable, reducer-folded daemon fact minted only from an admitted GO_ACTIVATE binding', and record the measured gap that no board row yet owns a daemon writer for the four earlier edges, so only this writer's refusal paths are production-reachable today.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/daemon/src/cutover/cutover-attempt-contracts.ts",
        "apps/daemon/src/cutover/cutover-attempt-commit.ts",
        "apps/daemon/src/cutover/cutover-attempt-reader.ts",
        "apps/daemon/src/cutover/cutover-attempt.test.ts",
        "apps/daemon/package.json"
      ],
      "startedAt": "2026-08-29T19:40:00.359Z"
    }
  ],
  "status": "PLANNING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor flip (human-approved retry): step-7 amendment request unanswered 61m+ — mention routing does not wake queue-driven architect wrappers. Needed: authorize apps/daemon/src/index.ts with one commit-module export (or record the gate disposition) per comment-6d1db240c9414bcb857826669f886e75. Steps 1-6 delivered, typecheck + 57 cutover tests green; amend step 7 only, then resubmit via submit_plan (auto-approves in SPEED).",
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "priority": "MEDIUM",
  "order": 126.9,
  "comments": [
    {
      "id": "comment-413d6609602944728bbb6fc4cec7e447",
      "author": "architect-c1a63707",
      "content": "ARCHITECT RE-SCOPE BEFORE PLANNING (architect-c1a63707, HEAD 3ded5047) — this row as filed is UNDER-SCOPED; measured one level deeper, it must be SPLIT BY EDGE before any plan.\n\nTHE MEASUREMENT: the core edge table (packages/core/src/cutover/cutover-contract.ts:45-58 CUTOVER_TRANSITIONS) admits `cutover.admit_activate_approval` ONLY from IMPORT_VERIFIED, and `reduceCutover` (cutover-reducer.ts:235-250) refuses `illegal(current.lifecycle, kind)` for any other source state and `create(input)` only for the creation command (`cutover.preview`, admits no prior state). So a durable ACTIVATE_APPROVED cannot be minted onto an empty aggregate: the daemon must first persist preview -> QUIESCE_APPROVED -> QUIESCING -> QUIESCED -> IMPORT_VERIFIED, each edge carrying its own witness type (CutoverSourceInventoryWitness :79, CutoverApprovalWitness :89, CutoverQuiesceProofWitness :94, CutoverImportVerificationWitness :100) — and NONE of those daemon writers exists (`git grep -n 'reduceCutover\\|CutoverAttempt' HEAD -- apps/daemon/src` EMPTY). Each witness has a production fact source that must be composed, never hand-seeded: the GO_QUIESCE/GO_ACTIVATE human bindings (packages/benchmark admission), the live quiesce evidence artifact (LIVE_QUIESCE_EVIDENCE_FILENAME, cutover-generation-snapshot.ts:67) which only task-e60b874b's parked live run writes, and the durable import generation (readDurableImportGeneration).\n\nREQUIRED PLAN SHAPE (SPIDR by edge, build order): (1) a daemon CutoverAttempt aggregate service skeleton — server-derived aggregate id, fold reader `readCutoverAttemptState`, ONE generic edge writer that takes a reducer command + its witness and commits the reducer-legal event with expectedVersion fencing and replay-by-decision — plus the `cutover.preview` creation edge from a durable source inventory witness; (2) the two approval-admission edges (admit_quiesce_approval from a GO_QUIESCE admission, admit_activate_approval from the GO_ACTIVATE admission) — the human-gated halves; (3) begin/complete_quiesce from the live evidence artifact (gated on task-e60b874b's live run — refuse-until-fact-exists); (4) verify_import from readDurableImportGeneration. Row (1)+(2) is the honest minimum for task-b2548479's precondition ONLY IF a test can reach IMPORT_VERIFIED through production writers — it cannot until (3)/(4) exist, so b2548479's accepted path stays red-by-design until the whole chain lands. This is cutover-epic (bf111658, MEDIUM) design work behind the PR #3 lanes; the next planner should file the split rows and keep this row as (1). Released back to PLANNING with a handoff; not parked, so a fresh planner can take it.",
      "timestamp": "2026-08-29T18:12:32.007Z"
    },
    {
      "id": "comment-872c7f3241c24f4e8a323dc5937e5752",
      "author": "architect-fa5d746b",
      "content": "ARCHITECT DoD-2 CORRECTION + PLAN-TIME MEASUREMENT (architect-fa5d746b, HEAD b1c76b13d7972a4af258f98984680bef6fe21c38). BINDING ON WORKER AND QA — grade DoD-2 against the reading below, not its literal wording.\n\nMEASURED FACT. packages/core/src/cutover/cutover-contract.ts:52 — CUTOVER_TRANSITIONS[\"cutover.admit_activate_approval\"] = Object.freeze([\"IMPORT_VERIFIED\"]). packages/core/src/cutover/cutover-reducer.ts create() (:202) — `if (command.kind !== \"cutover.preview\" || command.expectedVersion !== 0) return inputInvalid();`. Therefore a FRESH aggregate plus one `cutover.admit_activate_approval` refuses INPUT_INVALID and the fold stays ABSENT. DoD-2's \"after ONE cutover.admit_activate_approval with an ADMITTED binding the fold is ACTIVATE_APPROVED with exactly one event on the aggregate\" is UNSATISFIABLE as literally worded; satisfying it would require bypassing or reimplementing the edge table, which taskRail 1 forbids (\"the reducer owns the edge table\").\n\nBINDING READING.\n(a) \"A fresh store folds to ABSENT through readCutoverAttemptState\" — STANDS VERBATIM, asserted literally (code CUTOVER_ATTEMPT_STATE_ABSENT, the code task-b2548479's handler forwards per its parked handoff).\n(b) The accepted-path arm SEEDS the aggregate to IMPORT_VERIFIED by driving PRODUCTION reduceCutover over cutover.preview -> admit_quiesce_approval -> begin_quiesce -> complete_quiesce -> verify_import and appending each reducer-minted payload through this row's own production encoder and the same store seam. The seed USES the production reducer; it does not reimplement it.\n(c) \"exactly one event on the aggregate\" means THE AGGREGATE GROWS BY EXACTLY ONE across the writer call — asserted as after.length === before.length + 1 AND head.commandKind === \"cutover.admit_activate_approval\" AND state.version === before.length + 1 — never as a total of one.\n(d) Everything else in DoD-2 is met VERBATIM: exactly one CommandDecisionRecord with effectDisposition EFFECTS_COMMITTED; a second admission of the SAME decision REPLAYS with zero new events and zero new decisions, both asserted as before/after counts; a DIFFERENT decision for the same attempt refuses with the reducer's own verdict (ILLEGAL_TRANSITION at layer \"CUTOVER\"), because ACTIVATE_APPROVED is not a source in the edge table.\n\nOTHER MEASUREMENTS RECORDED AT PLAN TIME.\n1. Daemon gap OPEN: `git grep -n 'reduceCutover\\|CutoverAttemptState\\|CutoverAttempt' HEAD -- apps/daemon/src ':!**/*.test.*'` -> empty, EXIT 1. Positive control on the identical rev/pathspec form: `git grep -c 'export' HEAD -- apps/daemon/src/index.ts` -> `HEAD:apps/daemon/src/index.ts:42`, EXIT 0. Not a duplicate.\n2. Barrels: packages/core/src/index.ts:403 exports reduceCutover; packages/benchmark/src/index.ts:84-85 export admitActivationBinding. Both EXIT 0.\n3. Dependency edge PROVEN, all three legs: apps/daemon/package.json carries `\"@moe/benchmark\": \"workspace:*\"` as an UNCOMMITTED ` M` hunk; pnpm-lock.yaml is CLEAN and its apps/daemon importer already lists `'@moe/benchmark' -> link:../../packages/benchmark` (lock lines 126-128); a compiled probe under apps/daemon/src/cutover importing both bare specifiers passed `pnpm --filter @moe/daemon typecheck` (echo `$ tsc --project tsconfig.json`, EXIT 0) and was deleted in the same command. NOTHING RE-RESOLVED — no install is needed.\n4. apps/daemon/src/cutover/ state: cutover-generation-snapshot.{ts,js,test.ts} are TRACKED AND CLEAN at HEAD (task-dcf0ce92, landed). cutover-activation-marker.{ts,js,test.ts} are UNTRACKED peer WIP owned by task-b2548479 — never stage, revert or edit. This row's files all use the `cutover-attempt-*` prefix and collide with neither.\n5. SAFE_REF HAZARD: the reducer fences commandId and every witness ref at /^[A-Za-z0-9._:/-]{1,64}$/. A bare sha256 hex is exactly 64 and passes; ANY prefix fails and the command is refused INPUT_INVALID, which reads like a state refusal but is a shape refusal. The decision id is therefore BARE 64-hex.\n6. RECORDED GAP, NOT A NEW ROW (delivery bias): a board scan of every cutover-titled row (2bf8fa1a/dcf0ce92/f25077c5/b5315f42 DONE, b2548479 BACKLOG, b8272ee0 BLOCKED, c4321b6f BACKLOG) shows NOTHING owns a daemon writer for the four earlier edges. Until one exists, only this writer's refusal paths are production-reachable — the correct fail-closed behaviour for this slice.",
      "timestamp": "2026-08-29T19:17:15.190Z"
    },
    {
      "id": "comment-4a9fd202aa3d43b1896e9baff0063424",
      "author": "worker-76a8c4ac",
      "content": "VERIFY-OR-BLOCK at worker claim (worker-76a8c4ac)\n\nHEAD: `git rev-parse HEAD` => `a82f56c8b91c267fc910aa64c3ed8887f16aeb63`, EXIT 0.\n\nUnpiped measurements:\n- `git grep -n 'reduceCutover\\\\|CutoverAttemptState\\\\|CutoverAttempt' HEAD -- apps/daemon/src ':!**/*.test.*'` => no output, EXIT 1. No daemon CutoverAttempt persistence exists at this HEAD.\n- Positive control, identical revision/pathspec form: `git grep -c 'export' HEAD -- apps/daemon/src/index.ts` => `HEAD:apps/daemon/src/index.ts:42`, EXIT 0.\n- `git grep -n 'reduceCutover' HEAD -- packages/core/src/index.ts` => line 403 exports CUTOVER_COMMAND_KINDS, CUTOVER_TRANSITIONS, reduceCutover, EXIT 0.\n- `git grep -n 'admitActivationBinding' HEAD -- packages/benchmark/src/index.ts` => line 84 exports admitActivationBinding, EXIT 0.\n- `git status --porcelain -- apps/daemon/package.json pnpm-lock.yaml apps/daemon/src/cutover` => ` M apps/daemon/package.json` plus only the peer-owned `?? cutover-activation-marker.{js,test.ts,ts}`; pnpm-lock.yaml clean; EXIT 0.\n- Manifest diff is exactly one added dependency, `\"@moe/benchmark\": \"workspace:*\"`. The committed lock importer already contains specifier `workspace:*` / version `link:../../packages/benchmark`. This row adopts and declares apps/daemon/package.json, runs NO install, and re-resolves NOTHING. Worker coordination message msg-02aaefe30a6c449c948849ef151f50c8 posted; peer marker files remain untouched.\n- Compiled bare-specifier probe under apps/daemon/src/cutover imported `admitActivationBinding` from `@moe/benchmark` and `reduceCutover` plus `CutoverAttemptState` from `@moe/core`. `pnpm --filter @moe/daemon typecheck` printed `$ tsc --project tsconfig.json`, EXIT 0. Probe deleted in the same command; `PROBE_PRESENT_AFTER_DELETE=False`.\n\nDoD-2 correction (binding reading): cutover-contract.ts:52 admits `cutover.admit_activate_approval` ONLY from IMPORT_VERIFIED and cutover-reducer.ts `create()` refuses INPUT_INVALID for any non-preview command on an absent aggregate, so 'fresh store + one admit = ACTIVATE_APPROVED' is unsatisfiable without breaking taskRail 1. The binding reading is: the fresh-store ABSENT clause stands literally; the accepted-path arm seeds the aggregate to IMPORT_VERIFIED through production `reduceCutover`; and 'exactly one event' means the aggregate grows by EXACTLY ONE across the writer call.\n\nThe current epic sibling roster has no row owning daemon writers for the earlier preview/admit_quiesce/begin/complete/verify_import edges; only this approval writer's refusal paths are production-reachable today. This is a recorded gap, not a new row. The shared tree currently has 761 dirty entries; they are foreign state, preserved and not a stop condition.",
      "timestamp": "2026-08-29T19:19:29.928Z"
    },
    {
      "id": "comment-6d1db240c9414bcb857826669f886e75",
      "author": "worker-76a8c4ac",
      "content": "FINAL-GATE BLOCKER (worker-76a8c4ac; no pass fabricated)\n\nPost-adversarial owned gates are green:\n- `pnpm --filter @moe/daemon typecheck` -> `$ tsc --project tsconfig.json`, TYPECHECK_EXIT=0.\n- from apps/daemon, `pnpm exec vitest run --root . --config package.json src/cutover` -> `Test Files 3 passed (3)`, `Tests 57 passed (57)`, CUTOVER_SUITE_EXIT=0.\n- Production lines: contracts 203, reader 126, commit 246. All three bridges are exact LF one-liners. Adversarial change now forwards the reducer refusal object's own layer instead of locally re-stamping CUTOVER.\n\nRequired full daemon gate executed foreground and unpiped, EXIT 1: `Test Files 64 failed | 247 passed (311)`; `Tests 843 failed | 5527 passed | 58 skipped | 12 todo (6440)`. Foreign failures are disclosed verbatim: `witness fixture approval.decide refused: BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH` at `src/activation/admission-witness-fixtures.js:172`; EPERM cleanup at `src/http/foundation-registry-fixtures.ts:529`; and goal catalog shape drift at `src/daemon-command-registry.test.ts:736` (received extra `binding:null`).\n\nBut this row also owns a NEW path-attributed failure and cannot use the foreign-red rule to excuse it. A clean committed-HEAD archive ran the runtime bridge scanner alone and passed: `Test Files 1 passed (1)`, `Tests 1 passed | 4 skipped (5)`, EXIT 0. Current `src/runtime-entrypoint.test.ts:218` fails and names these three owned bridges under `unexpected`: `cutover\\cutover-attempt-commit.js`, `cutover\\cutover-attempt-contracts.js`, `cutover\\cutover-attempt-reader.js`. They are required by steps 2-4, but no runtime module imports them because taskRail 2 forbids registration here and task-b8272ee020a940009a11c6eb6355d578 owns that later edge. Therefore HEAD-minus-baseline intersect owned paths is NONEMPTY.\n\nSmallest safe repair is an architect amendment adding `apps/daemon/src/index.ts` to step 7 and authorizing one export from `./cutover/cutover-attempt-commit.js`; that is composition only (no command registration/live action), and the import closure classifies all three bridges as runtime-tier. Step 7 currently says commit ONLY the eight existing paths, so I did not take that unowned fan-out. Amendment request is routed as msg-6d4a669a3f054337884cd4bdffb1412c.",
      "timestamp": "2026-08-29T20:00:24.183Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T18:10:56.225Z",
  "updatedAt": "2026-08-29T21:09:27.515Z",
  "metrics": {
    "firstClaimAt": "2026-08-29T18:11:32.910Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 8,
    "executedStepCount": 6
  },
  "priorHandoffs": [
    {
      "whatIsDone": "Measured at HEAD 3ded5047: no daemon CutoverAttempt persistence; core CUTOVER_TRANSITIONS admits admit_activate_approval only from IMPORT_VERIFIED; reduceCutover creates only on cutover.preview. Witness fact sources identified (benchmark admissions, live quiesce evidence artifact via e60b874b, readDurableImportGeneration). Re-scope + required split recorded on the row comment. No plan submitted.",
      "whatRemains": "File the edge rows (skeleton+preview / approval admissions / quiesce from live evidence / verify_import) and plan this row as the skeleton + preview + generic edge writer + fold reader. Keep task-b2548479 parked until the chain reaches IMPORT_VERIFIED through production writers.",
      "pitfalls": "Never hand-seed attempt events to reach ACTIVATE_APPROVED; the reducer owns the edge table; admitActivationBinding is the only GO_ACTIVATE admission (rail 7A: no second decideApprovalAuthority). apps/daemon/src/cutover/ holds task-b2548479's untracked marker WIP and the committed snapshot reader; the @moe/benchmark manifest edge is an uncommitted hunk in apps/daemon/package.json (lock gate: NOTHING RE-RESOLVED).",
      "openQuestions": "Whether the source-inventory witness for cutover.preview has a durable production producer today (measure before filing the preview edge).",
      "releasedAt": "2026-08-29T18:12:41.265Z",
      "releasedBy": "architect-c1a63707",
      "reason": "Explicit architect handoff after measuring one level deeper: the row must be SPIDR-split by reducer edge (comment just posted) before a plan; releasing for a fresh planner rather than holding it."
    }
  ],
  "contextFetchedBy": [
    "architect-fa5d746b",
    "worker-76a8c4ac"
  ],
  "planSizeWarnings": [
    "Plan touches 8 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T19:16:46.282Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — let the writer create the aggregate directly at ACTIVATE_APPROVED so DoD-2's 'fresh store -> one command -> ACTIVATE_APPROVED' reads literally. That requires bypassing or reimplementing CUTOVER_TRANSITIONS, which taskRail 1 forbids outright ('the reducer owns the edge table') and which would make the daemon mint an ACTIVATE_APPROVED that never passed IMPORT_VERIFIED. There is exactly one legal design, so this is not a fork worth a human gate.\n\nREJECTED — store the derived CutoverAttemptState snapshot on the event and have the reader return it. The DoD demands a FOLD 'through reduceCutover'; a snapshot read is not a fold, and it lets a corrupt writer mint any lifecycle it likes. CHOSEN: the event payload carries the CutoverCommand that produced it (plus the admitted binding facts); the reader replays those commands through production `reduceCutover` from `undefined`, so the state is DERIVED on every read and no stored byte can assert a lifecycle the edge table would refuse.\n\nREJECTED — a production `cutover-attempt-fixtures.ts` to seed PREVIEWED..IMPORT_VERIFIED. It would add a fourth production module whose only consumer is a test. The seed instead lives IN the .test.ts and drives production `reduceCutover` + the production encoder from this row's contracts module, so nothing about the fold is reimplemented test-side.\n\nREJECTED — filing prerequisite rows for the four missing earlier-edge writers. DELIVERY BIAS: this row's writer is complete and production-reachable the moment those edges exist, and its refusal path for a not-IMPORT_VERIFIED fold is itself required behaviour. The gap is recorded as a comment on this row and in the completion comment, not as new rows.\n\nREJECTED — `commitExpectedVersionDecisionLegs`. One aggregate, one leg; the single-leg `commitExpectedVersionDecision` writes the CommandDecisionRecord DoD-2 counts.",
    "codebaseInsights": "MEASURED BY architect-fa5d746b AT HEAD b1c76b13d7972a4af258f98984680bef6fe21c38 (unpiped, exit codes quoted).\n\n(1) NO DAEMON CUTOVERATTEMPT PERSISTENCE. `git grep -n 'reduceCutover\\|CutoverAttemptState\\|CutoverAttempt' HEAD -- apps/daemon/src ':!**/*.test.*'` -> no output, EXIT 1. Positive control on the same rev/pathspec form: `git grep -c 'export' HEAD -- apps/daemon/src/index.ts` -> `HEAD:apps/daemon/src/index.ts:42`, EXIT 0. Gap CONFIRMED OPEN; this is not a duplicate.\n\n(2) BARRELS. `git grep -n ... HEAD -- packages/core/src/index.ts` -> :403 `export { CUTOVER_COMMAND_KINDS, CUTOVER_TRANSITIONS, reduceCutover } from \"./cutover/cutover-reducer.js\";` (EXIT 0). `packages/benchmark/src/index.ts` :84-85 exports `GA_ACTIVATION_WORK_REF, GO_ACTIVATE_GATE_ID, admitActivationBinding` from `./activation-binding.js` (EXIT 0).\n\n(3) DEPENDENCY EDGE (rail 7, all three legs). (a) apps/daemon/package.json declares `\"@moe/benchmark\": \"workspace:*\"` — present as an UNCOMMITTED ` M` worktree hunk. (b) pnpm-lock.yaml IS CLEAN (git status --porcelain empty) and its `apps/daemon:` importer already carries `'@moe/benchmark': specifier workspace:* / version link:../../packages/benchmark` at lock lines 126-128. (c) COMPILED PROBE: a temp module under apps/daemon/src/cutover importing `admitActivationBinding` from bare `@moe/benchmark` and `reduceCutover` + `type CutoverAttemptState` from bare `@moe/core`; `pnpm --filter @moe/daemon typecheck` echoed `$ tsc --project tsconfig.json` and EXITED 0; probe deleted in the SAME command; post-delete `git status --porcelain apps/daemon/src/cutover` showed only the peer marker trio. EDGE PROVEN. NOTHING RE-RESOLVED is available: no pnpm install is needed.\n\n(4) PEER STATE IN apps/daemon/src/cutover/. TRACKED AND CLEAN AT HEAD: cutover-generation-snapshot.{ts,js,test.ts} (landed by task-dcf0ce92; `readCutoverGenerationSnapshot` at :300). UNTRACKED PEER WIP (task-b2548479, DO NOT STAGE, DO NOT EDIT): cutover-activation-marker.{ts,js,test.ts}. All four new modules of this row use the `cutover-attempt-*` prefix, which collides with neither.\n\n(5) THE REDUCER'S ACTUAL EDGE TABLE — THIS CONTRADICTS DoD-2 AS LITERALLY WRITTEN; SEE risks. packages/core/src/cutover/cutover-contract.ts:52 `CUTOVER_TRANSITIONS[\"cutover.admit_activate_approval\"] = [\"IMPORT_VERIFIED\"]`; :67 target state ACTIVATE_APPROVED. cutover-reducer.ts `create()` (:202) refuses INPUT_INVALID for ANY command that is not `cutover.preview` with expectedVersion 0 when state is undefined. So a FRESH aggregate + one `cutover.admit_activate_approval` REFUSES INPUT_INVALID and the fold stays ABSENT; ACTIVATE_APPROVED is reachable only from IMPORT_VERIFIED.\n\n(6) REDUCER SURFACE DETAIL THE WRITER MUST OBEY. `reduceCutover(state|undefined, command)`; refuses ILLEGAL_TRANSITION when `input.expectedVersion !== current.version` OR the source state is not in the edge list; CUTOVER_STATE_INVALID for a structurally broken but lifecycle-readable state; INPUT_INVALID otherwise; every refusal is `{error, layer:\"CUTOVER\", ok:false}`. `SAFE_REF = /^[A-Za-z0-9._:/-]{1,64}$/` fences commandId, attemptId, sourceManifestRef and every witness ref — a bare 64-hex sha256 fits EXACTLY; any prefix pushes it past 64 and the command is refused INPUT_INVALID. Witness key set for this kind is EXACTLY {approvalRef, truthClass} and `validWitness` rejects any extra key. `apply()` copies `witness.approvalRef` into `state.activateApprovalRef`. Reducer `state.version` starts at 1 after preview and increments by one per accepted command, so it equals the store's aggregate version by construction.\n\n(7) ADMISSION SURFACE. packages/benchmark/src/activation-binding.ts:149 `admitActivationBinding(record: unknown): ActivationBindingAdmission`. Order is fixed: ABSENT -> SHAPE_INVALID -> DECISION_MISMATCH -> WORK_MISMATCH -> `decideApprovalAuthority` (its `ApprovalAuthorityRefusal` returned VERBATIM, its own code AND layer) -> GENERATION_UNBOUND. On ok it returns `{binding, ok:true}` with `binding.generations` (four 64-hex digests, keys at :57-61) and `binding.sourceCommit` (40-hex). Layer const `GA_ACTIVATION_BINDING`.\n\n(8) THE WRITER/READER PATTERN TO MIRROR (do not invent a new one). apps/daemon/src/activation/activation-ledger-commit.ts (239 lines) — one `store.commitExpectedVersionDecision*` call, `refuseThrown` mapping DurableStoreError codes onto a local roster while preserving `storeCode` verbatim, `refuseDecided` treating a non-EFFECTS_COMMITTED decision as a refusal (NOT success), and `answerReplayed` re-reading durable bytes through the SAME production reader rather than echoing the caller. apps/daemon/src/activation/activation-attempt-reader.ts (247 lines) — the port interface naming ONLY the methods used, ABSENT vs UNKNOWN split, `storeCode` carried. Codec convention: apps/daemon/src/activation/activation-ledger-codec.ts. Store shapes: packages/store/src/store-contracts.ts:36 EventDraft{eventId,eventType,payload}, :68 CommandDecisionKey{commandId,principalId,projectId",
    "risks": "RISK 1 — DoD-2 IS UNSATISFIABLE AS LITERALLY WORDED; THE ARCHITECT'S BINDING READING IS BELOW. Measured fact (5) above: `cutover.admit_activate_approval` admits ONLY the source state IMPORT_VERIFIED, and `create()` refuses INPUT_INVALID for a non-preview command on an absent aggregate. DoD-2's 'after ONE cutover.admit_activate_approval with an ADMITTED binding the fold is ACTIVATE_APPROVED with exactly one event on the aggregate' therefore cannot be met from a fresh store without breaking taskRail 1. BINDING READING, to be restated verbatim in the row comment of step 1 and graded against by QA: (a) 'A fresh store folds to ABSENT through readCutoverAttemptState' stands EXACTLY as written and is asserted literally; (b) the accepted-path arm SEEDS the aggregate to IMPORT_VERIFIED by driving production `reduceCutover` over the four prior commands and appending each reducer-minted payload through this row's own production encoder and the same store seam; (c) 'exactly one event' means the aggregate grows by EXACTLY ONE event across the writer call — asserted as `after.length === before.length + 1` AND `head.commandKind === \"cutover.admit_activate_approval\"` AND `fold.state.version === before.length + 1` — never as a total of one. Everything else in DoD-2 (one decision record, replay yields zero new events and zero new decisions with both counts asserted, a different decision refuses with the reducer's own verdict code) is met verbatim.\n\nRISK 2 — SAFE_REF SILENTLY EATS A PREFIXED COMMAND ID. `commandId` must match /^[A-Za-z0-9._:/-]{1,64}$/. A bare sha256 hex is exactly 64 chars and passes; ANY prefix ('cutover-approval:' + hex = 81) fails and `snapshotCommand` returns undefined, so `reduceCutover` refuses INPUT_INVALID and the writer looks like it is refusing on state when it is refusing on shape. Use the bare 64-hex digest for BOTH the decision commandId and the `approvalRef` witness, and pin it with an assertion that the derived id matches SAFE_REF.\n\nRISK 3 — EXCESS WITNESS KEY. `validWitness` requires the witness own-key set to be EXACTLY {approvalRef, truthClass}. Do not smuggle generations or sourceCommit into the witness; they ride on the event payload's separate `admitted` record, outside the CutoverCommand.\n\nRISK 4 — TWO VERSION NOTIONS THAT ONLY COINCIDE BY CONSTRUCTION. The reducer's `state.version` and the store's aggregate version are independent counters that happen to agree. Do not assume it: compute the store fence from the durable event count, compare it to `foldedState?.version ?? 0`, and refuse CUTOVER_ATTEMPT_VERSION_DESYNC (fail closed) when they differ, rather than writing under a fence the fold does not back.\n\nRISK 5 — A RETURNED CONFLICT IS NOT A THROWN ONE. `commitExpectedVersionDecision` returns a NO_BUSINESS_EFFECT decision (no throw, no event) on an expected-version conflict. Mirror `refuseDecided`: any disposition that is not EFFECTS_COMMITTED is a refusal. Treating 'it did not throw' as success is exactly how a second admission would be reported committed while nothing was written.\n\nRISK 6 — REPLAY IDENTITY DOES NOT COVER THE PAYLOAD. The store's replay identity hashes projectId/principalId/commandId/commandKind/targetAggregateId/expectedVersion/requestBytes and NOT the events array, so a REPLAYED disposition must be answered by re-reading the fold from the store (mirroring `answerReplayed`), never by echoing the caller's binding. If the re-read fold disagrees with the admitted decision identity, refuse CUTOVER_ATTEMPT_REPLAY_DIVERGED.\n\nRISK 7 — PEER WIP IN THE SAME DIRECTORY. cutover-activation-marker.{ts,js,test.ts} are UNTRACKED and belong to task-b2548479. Never stage, revert or edit them; the commit uses an explicit pathspec naming only this row's files. The 725 dirty peer paths elsewhere in the checkout are noted and ignored, never a stop condition.\n\nRISK 8 — DIRTY/STALE .js BRIDGES SHADOW THEIR .ts. Each new module needs a one-line `export * from \"./<name>.ts\";` bridge and nothing more. A bridge holding transpiled JS makes basename resolution serve stale bytes and can green a gate against code that is not the source under test.\n\nRISK 9 — THE ACCEPTED PATH IS NOT YET PRODUCTION-REACHABLE. Measured fact (11): no row owns a daemon writer for the four earlier edges, so today only the refusal paths can fire in production. That is the correct fail-closed behaviour for this slice and is recorded in the completion comment, not worked around.",
    "keyFiles": [
      "packages/core/src/cutover/cutover-contract.ts",
      "packages/core/src/cutover/cutover-reducer.ts",
      "packages/benchmark/src/activation-binding.ts",
      "apps/daemon/src/activation/activation-ledger-commit.ts",
      "apps/daemon/src/activation/activation-ledger-contracts.ts",
      "apps/daemon/src/activation/activation-attempt-reader.ts",
      "apps/daemon/src/activation/activation-ledger-codec.ts",
      "apps/daemon/src/activation/activation-ledger-commit.test.ts",
      "packages/store/src/store-contracts.ts",
      "apps/daemon/src/cutover/cutover-generation-snapshot.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-29T19:39:51.032Z"
  },
  "planApprovedAt": "2026-08-29T19:16:48.430Z",
  "workStartedAt": "2026-08-29T19:17:35.791Z",
  "declaredFiles": [
    "apps/daemon/package.json"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "blockedOnTaskIds": null
}