{
  "id": "task-40017f79a36e47afbcb69762acfc294a",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "P2.15d Wire live navigation and plan approval",
  "description": "Control Room navigation remains inert and plan approval is deliberately disabled even as handshake/poll fixes land. Wire the production route and approval command only where server affordances authorize it.",
  "definitionOfDone": [
    "Re-measure dirty Cordum shell/navigation/live dispatch ownership and the production plan-approval command/affordance; block rather than collide or fabricate an approval route.",
    "Navigation buttons drive one typed route source of truth with durable goal/run identifiers; unavailable destinations are explicitly disabled rather than inert.",
    "Plan approval calls the real typed command only when the server affordance authorizes it. Success refreshes durable state; refusal stays visible and pins exact code/layer.",
    "UI tests cover navigation, disabled affordances, success/refusal, no fixed goal/run ids, and nonzero exact route/affordance rosters."
  ],
  "taskRails": [
    "Re-measure current HEAD, exact-path porcelain, and live ownership before planning or editing; adopt provenance-verified foreign WIP or block on collision.",
    "Use production-reachable evidence, exact stable refusal code and layer where applicable, nonzero tests, mutation drills for security boundaries, and explicit-pathspec commits only.",
    "Do not treat a caller-shaped HUMAN flag or enabled button as approval authority. Preserve active handshake/poll task ownership."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "STEP 1 — VERIFY-OR-BLOCK GATE (read-only; author no bytes). Record with moe.add_comment. Re-capture `git rev-parse HEAD` and quote it beside every grep, each UNPIPED with its own exit code and a positive control.\n\n(a) THE ARITY RE-PROBE THE UNPARK REQUIRED, architect-measured at HEAD 595c32c2. `onOpenBoard` is still TWO arguments at its declaring sites: `goals-home.tsx:47` and `live-goals.tsx:45` both read `(goalId: string, title: string) => void`. This row's three consumers in `cordum-app.tsx` are the JSX prop-passing spelling at :316, :319 and :335 — `onOpenBoard={openBoard}` — which a `onOpenBoard(` grep is BLIND to. Always probe without the paren.\n\n(b) THE COLLISION WITH THIS ROW'S OWN CHILD, and it is the thing that decides the order. `task-9bd8f5298c6a4d0cac47b92e62a97423` is the SPIDR child that widens `onOpenBoard` to `(goalId, planningRunRef, title)`. It is AWAITING_APPROVAL with an approved plan, and its roster includes `cordum-app.tsx`. THIS row's plan also touches `cordum-app.tsx` (steps 5, 6, 7) and `cordum-app.test.tsx` (step 2) — but for a DIFFERENT REASON: the child changes `openBoard`'s SIGNATURE, this row wires NAVIGATION and PLAN APPROVAL. Two rows editing different hunks of one file is still a collision, because pathspec commits are FILE-granular while the conflict is HUNK-granular: whoever commits second sweeps the other's hunks.\nRULING, and it is this row's to make because this row is the parent: THE CHILD GOES FIRST. It consumes nothing from here and produces the signature this row's `openBoard` must match; a parent that edits its child's production file before the child's signature exists will be swept or will force the child to rebase onto bytes it does not own.\nGATE: `git grep -n \"planningRunRef\" HEAD -- apps/control-room/src/v2/goals/goals-home.tsx` is NON-EMPTY (the widened signature has landed). If it is EMPTY, steps 5-7 are BLOCKED — but steps 2-4 are NOT (see (c)), so do not block the whole row.\n\n(c) WHAT IS REACHABLE REGARDLESS, so this row is not parked behind its child: `nav-rail.tsx`, `cordum-shell.tsx`, `live-work-board.tsx` and `approve-plan.tsx` do NOT reference `onOpenBoard` and are untouched by the child. Steps 2-4 proceed whatever the gate in (b) says. Only the `cordum-app.tsx` wiring waits.\n\n(d) DoD-1's OWN INSTRUCTION: \"Re-measure dirty Cordum shell/navigation/live dispatch ownership and the production plan-approval command/affordance; block rather than collide or fabricate an approval route.\" Run `git status --porcelain` over every path in this row's roster and a file-field scan across non-DONE rows. Report any dirty path you did not author rather than adopting it.\n\n(e) THE APPROVAL AUTHORITY MUST ALREADY EXIST — measure it, do not assume it. DoD-3 requires the real typed command called ONLY when the server affordance authorizes it. Find the affordance read and the typed command before planning step 5; if no production affordance exists, that is a BLOCK naming the missing producer, never a locally-invented authorization. Task rail 3 is explicit: a caller-shaped HUMAN flag or an enabled button is NOT approval authority.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "note": "STEP 1 DONE — read-only, zero bytes authored. Full record: comment-f536156e3614409695755c46b9eec6d8. HEAD 13d5750e (plan was measured at 595c32c2; all premises re-measured).\n\nVERDICT: PARTIAL PROCEED. Steps 2-4 GO; steps 5-7 GATED SHUT.\n\n(b) THE GATE IS EMPTY: `git grep -n \"planningRunRef\" HEAD -- apps/control-room/src/v2/goals/goals-home.tsx` EXIT 1, zero hits; positive control `git grep -c \"onOpenBoard\"` on the same file/rev EXIT 0 prints 4. Child task-9bd8f529 is AWAITING_APPROVAL and unassigned — a HUMAN gate, so no worker effort advances it. onOpenBoard is still two-arg at goals-home.tsx:47 and live-goals.tsx:45; this row's three consumers are the JSX spelling at cordum-app.tsx:316/:319/:335.\n\n(d) Roster clean and byte-identical to HEAD except nav-rail.tsx (` M`, genuinely differs). Collision scan over all non-terminal rows: exactly one hit, the child on cordum-app.tsx — already covered by the gate.\n\nNAV WIP ADOPTED, provenance re-verified byte-for-byte against comment-bf1a0499 and comment-c9184904: nav-rail.tsx 3379a92a...fa70e (125 lines), nav-rail.css db515dbc...f3d376 (15 lines, UNTRACKED so git diff is blind to it — do not sweep). The diff is exactly this row's DoD-2 disabled-destination hunk.\n\n(e) APPROVAL AUTHORITY NOW EXISTS — the earlier rows' block premise has EXPIRED. approval.decide_intent is in RUNTIME_COMMAND_KINDS (runtime-vocabulary.ts:86) and the generated client (:178/:280); the daemon seam is apps/daemon/src/planning/approval-intent*.ts from task-6646f888 (595c32c2); the witness is server-minted per daemon-command-edges.ts:39-47, never decoded from payload bytes; the affordance read is apps/daemon/src/http/affordance-read.ts. Step 1(e)'s missing-producer block does NOT fire. Step 5 is gated by (b) alone.\n\nBASELINE: `pnpm --filter @moe/control-room test` EXIT 0 — Test Files 108 passed (108), Tests 1424 passed (1424).\n\nCARRIED FORWARD: step 2's approval arms (its items 3-4) can only green at step 5, which is gated shut. I will write step 2's navigation arms (items 1, 2, 5) and hold the approval arms, rather than commit a red suite that would fail step 7 LEG A/B. Disclosed, not silent.",
      "startedAt": "2026-08-29T16:41:03.438Z",
      "completedAt": "2026-08-29T16:44:27.487Z"
    },
    {
      "stepId": "step-2",
      "description": "STEP 2 — RED FIRST on navigation and approval, in the two suites this row owns outright.\n\nWrite the arms in `cordum-app.test.tsx` and `approve-plan.test.tsx` before touching production. These two files are NOT contested by the child (its approved plan touches neither), so this step is safe to run whatever step 1(b)'s gate says.\n\nARMS, from DoD-2 and DoD-4:\n(1) NAVIGATION USES ONE TYPED ROUTE SOURCE OF TRUTH carrying DURABLE goal and run identifiers — assert the route value is the identifier the fixture supplied, read from the fixture object. DoD-4 forbids fixed goal/run ids: an expectation written as a literal beside the assertion is a fixed point no mutation can red, which is exactly what \"no fixed goal/run ids\" is guarding against.\n(2) AN UNAVAILABLE DESTINATION IS EXPLICITLY DISABLED, NOT INERT (DoD-2). Assert the disabled state is rendered and asserted as such — an inert button that simply does nothing passes a naive \"clicking does not navigate\" arm while telling the operator nothing. Compose the disabled-Open precedent task-14e3b36a landed at `goal-card.test.tsx:148`; do not re-derive a second disabled surface.\n(3) PLAN APPROVAL SUCCESS refreshes durable state, and the goal's new state comes from the durable frame — never a locally formatted optimistic value.\n(4) PLAN APPROVAL REFUSAL STAYS VISIBLE and pins the EXACT code and layer, both asserted as test-side literals. A refusal that clears the form loses the operator's context and the reason.\n(5) NONZERO EXACT ROSTERS (DoD-4): the route roster and the affordance roster each assert an exact nonzero count. A roster arm that iterates an empty roster passes vacuously.\n\nRun standalone, FOREGROUND, UNPIPED: `pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx`. Quote every red with its count line.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/cordum-app.test.tsx",
        "apps/control-room/src/v2/goals/approve-plan.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "note": "STEP 2 — RED FIRST, navigation arms only. Five arms appended to cordum-app.test.tsx (503 -> 626 lines); no production byte authored.\n\nRED, foreground, unpiped: `pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx` EXIT 1.\n  Test Files  1 failed | 1 passed (2)\n  Tests  5 passed (5)\n  `Error: Failed to resolve import \"./shell/shell-routes.js\" from \"src/v2/cordum-app.test.tsx\". Does the file exist?`\nThe whole file fails to LOAD, so the count line shows only approve-plan's 5. That is an honest red for a module that does not exist yet; steps 3-4 create it and the arms then execute individually.\n\nARMS WRITTEN (DoD-2 + DoD-4 navigation half):\n(1) ROUTE + DESTINATION ROSTERS, EXACT AND NONZERO. CORDUM_ROUTE_KINDS frozen and length 2; resolveNavDestinations() maps one-to-one onto NAV_IDS in rail order. BOTH partitions asserted nonzero before their exact counts (1 reachable, NAV_IDS.length-1 unavailable) — a roster with nothing unavailable would make arm (2) vacuous and one with nothing reachable would make arm (3) vacuous, so the nonzero assertions are the guard, not decoration.\n(2) UNAVAILABLE IS DISABLED, NOT INERT — **asserted WITH a navigator wired**. This is the arm that kills the adopted WIP's `unavailable = !active && onNavigate === undefined` derivation: a rail that reads the handler's presence enables every destination under this render and reds. Each unavailable button must be `disabled`, must carry `aria-describedby` pointing at a real node, and that node's text must be exactly NAV_UNAVAILABLE_LABELS[its reason code] — the operator reads the MEASURED reason, not a generic \"unavailable\". Clicking one calls nothing.\n(3) NAVIGATION EMITS THE ROUTE THE SOURCE OF TRUTH SUPPLIES. Clicking the reachable destination calls onNavigate exactly once with `reachable.route` READ FROM THE ROSTER, not rebuilt beside the assertion (DoD-4 no-fixed-ids).\n(4)+(5) DURABLE BOARD SUBJECT. LiveWorkBoard repeats the daemon's `SurfaceFrame.planningGoalRef` verbatim in `cr.board.subject` (both text and data-goal), expected value read from the BOARD_SURFACE fixture object; and with `planningGoalRef: null` it renders BOARD_SUBJECT_ABSENT_NOTE with no data-goal rather than inventing one.\n\nTWO DISCLOSED NARROWINGS, both from step 1's gate, neither silent:\n(a) NO APPROVAL ARMS. Step 2's items 3-4 (approval success/refusal) green only at step 5, which step 1(b) gated shut — the child task-9bd8f529 is AWAITING_APPROVAL and its widened signature is not in HEAD. Committing arms that cannot go green would leave a red suite in HEAD and fail step 7 LEG A/LEG B. approve-plan.test.tsx is UNTOUCHED and stays byte-identical to HEAD.\n(b) ARMS RENDER CordumShell AND LiveWorkBoard DIRECTLY, not through CordumApp. CordumApp passes no onNavigate and wiring it is step 6, also gated. The seam under test is therefore the shell's. The CordumApp-level navigation arm belongs to step 6 with the rest of that wiring.\n\nDURABLE IDENTIFIER SOURCE, measured not assumed: `SurfaceFrame.planningGoalRef` (live-board-feed.ts:45) is documented \"Daemon-derived durable goal binding for the default planning run\" and frameOfSurface (:182-185, :200) accepts it only as a non-empty string or null. So the board's subject is server-stated; nothing is synthesised client-side. The RUN identifier half has no frame field — LIVE_RUN_SUBJECT lives in cordum-app.tsx:46, which is gated, so the run half lands with step 6.",
      "startedAt": "2026-08-29T16:44:31.748Z",
      "completedAt": "2026-08-29T16:49:34.579Z"
    },
    {
      "stepId": "step-3",
      "description": "STEP 3 — THE TYPED ROUTE SOURCE OF TRUTH (DoD-2). Edit `nav-rail.tsx` and `cordum-shell.tsx`. Neither references `onOpenBoard`, so this step is independent of the child row entirely.\n\nDrive every navigation button from ONE typed route definition carrying durable goal and run identifiers. A second place that knows how to build a route is a second source of truth and will drift; if you find one already, compose it rather than adding a third.\n\nUNAVAILABLE DESTINATIONS ARE EXPLICITLY DISABLED, not inert (DoD-2). The distinction is the whole clause: a disabled control tells the operator the destination is unavailable, while an inert one that silently does nothing looks broken. Compose the disabled-control precedent from task-14e3b36a rather than inventing a second disabled surface — and where a destination is unavailable because a durable identifier is absent, the control is disabled for THAT measured reason, never because a lookup happened to return undefined.\n\nKeep production sources under 250 lines and split before 400.\n\nRe-run step 2's command: the navigation arms go green; the approval arms stay red until step 5.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/shell/nav-rail.tsx",
        "apps/control-room/src/v2/shell/cordum-shell.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/shell/shell-routes.ts",
        "apps/control-room/src/v2/shell/nav-rail.tsx",
        "apps/control-room/src/v2/shell/cordum-shell.tsx"
      ],
      "note": "STEP 3 — THE TYPED ROUTE SOURCE OF TRUTH (DoD-2). Delivered. Navigation arms GREEN.\n\n`pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx` EXIT 1 — `Test Files 1 failed | 1 passed (2)`, `Tests 2 failed | 26 passed (28)`. All three NAVIGATION arms flipped green; the two reds are step 4's board-subject arms, exactly the split the plan predicts (\"the navigation arms go green\").\n\nNEW MODULE — apps/control-room/src/v2/shell/shell-routes.ts, 83 lines. The ONE typed route definition:\n  CORDUM_ROUTE_KINDS frozen [\"board\",\"goals\"], and `CordumRoute` DERIVES its discriminants from it via Extract<CordumRouteKind, ...>. That makes the roster load-bearing rather than a hand-kept mirror: delete a kind and the union stops typechecking. A roster the type does not derive from certifies nothing.\n  NAV_UNAVAILABLE_REASONS frozen [\"NAV_DESTINATION_NOT_BUILT\",\"NAV_ROUTE_SUBJECT_ABSENT\"] + NAV_UNAVAILABLE_LABELS mapping each code to the operator's sentence.\n  BUILT_NAV_ROUTES is the MEASUREMENT behind the reason: a destination is listed when its surface exists. `goals` is the only one this build implements, so resolveNavDestinations() returns 1 reachable and 5 NAV_DESTINATION_NOT_BUILT, one entry per NAV_ID in rail order.\n\nnav-rail.tsx 125 -> 167 lines. The rail now holds NO route knowledge of its own.\n  KILLED THE DEFECT the adopted WIP carried: `unavailable = !active && onNavigate === undefined`. That is precisely the \"disabled because a lookup happened to return undefined\" the step forbids — it made availability a property of whether a handler was passed, so a wired shell showed every destination as reachable and an unwired one showed all five as SOON. Availability is now a property of the ROUTE.\n  onNavigate re-typed `(id: NavId) => void` -> `(route: CordumRoute) => void`. Navigation emits the route object from the source of truth; no caller rebuilds one.\n  DISABLED, NOT INERT, composing the v1 precedent at shell/nav-rail.tsx:37-65 rather than inventing a second surface: `disabled` + `aria-describedby` pointing at a rendered sentence, one node per reason ACTUALLY present on the rail (`hidden`, so it is read with the control without duplicating what the SOON chip and the title already say visually). The WIP's SOON StatusChip and title are kept; its `aria-label` override is DROPPED — see the regression note below.\n  An item the roster does not name is treated as NOT_BUILT: the roster is the measurement, so its silence is not a licence.\n\ncordum-shell.tsx 196 -> 201 lines. Threads `navDestinations` (omit for the one source of truth) and re-types onNavigate. No other behaviour touched.\n\nREGRESSION CHECK, foreground: `pnpm --filter @moe/control-room exec vitest run src/v2/cordum-shell.test.tsx src/v2/styles src/v2/goals src/a11y` EXIT 0 — `Test Files 23 passed (23)`, `Tests 275 passed (275)`.\n  Two existing arms constrained this design and I checked them BEFORE editing, not after: cordum-shell-responsive.test.tsx:136 renders approvals WITH onNavigate and a \"7\" badge and requires `aria-label` to be NULL — since approvals is now unavailable regardless of the handler, keeping the WIP's aria-label would have redded it, which is why the reason moved to aria-describedby. And :122 requires an unavailable item to keep a non-blank `title`; the title is now NAV_UNAVAILABLE_LABELS[reason], so it states the measured reason instead of a generic \"Not available in this build\".\n\nTYPECHECK: `pnpm --filter @moe/control-room typecheck` EXIT 1 with the `$ tsc --project tsconfig.json` echo, one error and it is step 4's: `src/v2/cordum-app.test.tsx(14,10): error TS2305: Module '\"./goals/live-work-board.js\"' has no exported member 'BOARD_SUBJECT_ABSENT_NOTE'`. That is a NATURAL red-then-green control for the typecheck leg — it proves tsc executed and is reading this tree, not returning a sub-second vacuous zero.\n\nLINE CAPS: shell-routes.ts 83, nav-rail.tsx 167, cordum-shell.tsx 201 — all under the 250 target.",
      "startedAt": "2026-08-29T16:49:36.764Z",
      "completedAt": "2026-08-29T16:52:15.268Z"
    },
    {
      "stepId": "step-4",
      "description": "STEP 4 — THE LIVE WORK BOARD (DoD-2's durable identifiers). Edit `live-work-board.tsx`. Also independent of the child row.\n\nThe board renders from durable state and carries the durable goal and run identifiers into whatever it hands onward. No locally synthesised id, no placeholder, and no identifier derived by string-formatting something else — DoD-4's \"no fixed goal/run ids\" applies to production as much as to the arms.\n\nWhere the board offers a destination that is not available, it uses step 3's disabled treatment, so the whole shell speaks one language about unavailability.\n\nRe-run step 2's command plus `pnpm --filter @moe/control-room typecheck`; quote the count line and the `$ tsc` echo.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-work-board.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/live-work-board.tsx"
      ],
      "note": "STEP 4 — THE LIVE WORK BOARD'S DURABLE SUBJECT (DoD-2). Delivered. All step-2 arms now GREEN.\n\n`pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx` EXIT 0 — `Test Files 2 passed (2)`, `Tests 28 passed (28)`.\n`pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo. This green is TRUSTWORTHY because the same command was RED one invocation earlier on a natural error (TS2305, the missing BOARD_SUBJECT_ABSENT_NOTE export) — the red-then-green control, not a sub-second vacuous zero.\n\nlive-work-board.tsx 44 -> 78 lines. THE SUBJECT IS DAEMON-STATED, measured not assumed: SurfaceFrame.planningGoalRef (live-board-feed.ts:45) is documented \"Daemon-derived durable goal binding for the default planning run\", and frameOfSurface (:182-185, :200) admits it ONLY as a non-empty string or null — an empty string makes the whole frame unreadable. So the board repeats a value the daemon authored; it synthesises nothing, formats nothing, defaults nothing.\n  New BoardSubject renders `cr.board.subject` with the ref in both its text and `data-goal`.\n  ABSENT IS AN HONEST STATE, not a hole: `planningGoalRef: null` renders BOARD_SUBJECT_ABSENT_NOTE and NO data-goal, in the shell's own language for unavailability.\n  BEFORE THE FIRST FRAME it renders NOTHING at all. Claiming \"absent\" while no daemon answer has arrived would be the board's guess, not the daemon's answer — the same distinction the shell already draws between OFFLINE and DISCONNECTED.\n\nSCOPE MEASURED, NOT ASSUMED. Two halves of step 4 do NOT land here and neither is silently dropped:\n  THE RUN IDENTIFIER. No SurfaceFrame field carries it. The only run identifier in this surface is LIVE_RUN_SUBJECT at cordum-app.tsx:46, and cordum-app.tsx is gated shut by step 1(b). The run half lands with step 6.\n  GOAL-SCOPED FILTERING. LiveWorkBoard receives only `headers` and createBoardFeed posts a fixed `\"{}\"` body (live-board-feed.ts:244), so the board shows the WHOLE project surface, not the opened goal's. Scoping it needs live-board-feed.ts, which is in NO step of this row's plan. I did not widen the roster to reach it. Filed here so it is not mistaken for done.\n\nMUTATION DRILLS — five run, EVERY ONE RED where red was required, one degree of freedom each, each mutation verified LANDED by grep before the run and each restore proven by SHA256 in a SEPARATE call. Backups at %TEMP%/40017f79-worker-57c90511/.\n  D3 INERT NOT DISABLED — `disabled={unavailable}` -> `disabled={false}`. RED, exactly one arm: \"keeps an unavailable destination disabled and naming its reason while a navigator is wired\", `AssertionError: approvals: expected false to be true`. The arm is not satisfied by a control that merely does nothing.\n  D4 HARDCODED GOAL ID — durable ref -> the literal `\"goal-durable-7c1f\"`. RED on the ABSENCE arm only: `expected 'goal-durable-7c1f' to be null`. FINDING, and I am recording it rather than banking it: the positive \"repeats verbatim\" arm stayed GREEN, because the hardcoded literal happened to equal the fixture's value — a FIXED POINT that arm alone cannot escape. The absence arm is what caught it, which is exactly why an absence arm exists beside a positive one.\n  D4b SAME MUTATION, DIFFERENT LITERAL — `\"goal-placeholder\"`, one degree of freedom off D4. BOTH arms RED: `expected 'goal-placeholder' to contain 'goal-durable-7c1f'` and `expected 'goal-placeholder' to be null`. This settles D4's finding: the positive arm is NOT vacuous, it is fixed-point-blind only to a hardcode that reproduces the fixture.\n  D6 THE WIP'S OWN DEFECT — reason derived from `!active && onNavigate === undefined` instead of the route roster. RED: same arm as D3. The defect I removed in step 3 is now caught by a committed arm, so it cannot come back unnoticed.\n  D7 EMIT THE ID NOT THE ROUTE — `onNavigate(route)` -> `onNavigate(item.id)`. RED: \"navigates the reachable destination with the route the source of truth supplies\", `expected \"vi.fn()\" to be called with arguments: [ { kind: 'goals' } ]`.\n  RESTORE PROVEN: all five owned paths hash byte-identical to the pre-drill baseline — nav-rail.tsx ef874943…c0c8, shell-routes.ts a60d95f6…0045, live-work-board.tsx 49e7de2c…41c5, cordum-shell.tsx 5e58e645…4a17, cordum-app.test.tsx c100badc…df21.\n\nGATES, each alone, foreground, unpiped:\n  LEG A `... exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx` EXIT 0 — Test Files 2 passed (2), Tests 28 passed (28).\n  LEG B `pnpm --filter @moe/control-room test` EXIT 0 — Test Files 108 passed (108), Tests 1429 passed (1429). Step 1's baseline was 108/1424; +5 is exactly this row's five new arms, appended to an existing file so the file count is unchanged. NO regression anywhere in the package.\n  LEG C typecheck EXIT 0 with the `$ tsc` echo.\nLINE CAPS: shell-routes.ts 83, nav-rail.tsx 167, cordum-shell.tsx 201, live-work-board.tsx 78 — all under the 250 target.",
      "startedAt": "2026-08-29T16:52:17.698Z",
      "completedAt": "2026-08-29T16:56:14.234Z"
    },
    {
      "stepId": "step-5",
      "description": "STEP 5 — PLAN APPROVAL AGAINST THE REAL AFFORDANCE (DoD-3). Edit `approve-plan.tsx` and `cordum-app.tsx`.\n\nSTEP 1(b)'s GATE APPLIES FROM HERE ON: `cordum-app.tsx` is contested with the child row task-9bd8f529 until its widened `onOpenBoard` signature lands. If `git grep -n \"planningRunRef\" HEAD -- apps/control-room/src/v2/goals/goals-home.tsx` is EMPTY, STOP and report — steps 2-4 are already delivered and this is an ordering block, not a design block. Do not edit cordum-app.tsx ahead of the child.\n\nCALL THE REAL TYPED COMMAND ONLY WHEN THE SERVER AFFORDANCE AUTHORIZES IT (DoD-3, task rail 3). The affordance is READ FROM THE SERVER; an enabled button is a consequence of authorization, never its source. Do NOT treat a caller-shaped HUMAN flag as authority — that is the exact substitution task rail 3 names, and it is how a UI ends up authorizing itself.\nIf step 1(e) found no production affordance to read, this step BLOCKS naming the missing producer. Inventing a local authorization to make the button work would fabricate an approval route, which DoD-1 forbids by name.\n\nON SUCCESS refresh durable state and render the goal's new state from the durable frame. ON REFUSAL keep the refusal visible with its exact code and layer — the operator needs the reason, and clearing it to a generic error loses the one thing that makes the failure actionable.\n\nWhen you touch `cordum-app.tsx`, match `openBoard` to whatever signature the child landed — read it, do not assume three args in a particular order.\n\nRe-run step 2's command: the approval arms go green.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/approve-plan.tsx",
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "startedAt": "2026-08-29T16:57:29.718Z"
    },
    {
      "stepId": "step-6",
      "description": "STEP 6 — WIRE THE SHELL (DoD-2 end to end). Edit `cordum-app.tsx`. Step 1(b)'s gate still applies.\n\nCompose steps 3-5 into the shell so navigation and approval are reachable from the real app: the route source of truth drives the nav rail, the work board carries durable identifiers, and plan approval calls the affordance-gated command.\n\nADAPT `openBoard` TO THE CHILD'S LANDED SIGNATURE at all three prop-passing sites (:316, :319, :335 as measured). This row CONSUMES that widening; it does not re-decide it. If the child's signature differs from what this plan anticipated, follow the child — it owns that contract.\n\nDO NOT re-implement the disabled-Open behaviour or the runless-card rule. task-14e3b36a landed the first and the child row owns the second; a third implementation of either is a competing surface.\n\nRe-run the full control-room suite; quote the count line.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/cordum-app.tsx"
      ]
    },
    {
      "stepId": "step-7",
      "description": "STEP 7 — DRILLS, GATES, ADVERSARIAL REVIEW, DURABLE HANDOFF.\n\nRESTORE PROTOCOL: every owned path is TRACKED. Check `git status --porcelain` per path FIRST — they carry your edits during drills, so record a baseline before mutating; where a path was already dirty when you arrived, back it up to `%TEMP%/40017f79-<workerId>/` and prove restore by SHA256 equality in a call SEPARATE from the restore. Confirm each mutation LANDED before rerunning; a silent no-op write reads as green.\nD1 APPROVE WITHOUT THE AFFORDANCE: make the command callable while the server affordance withholds authorization -> the affordance-gated arm MUST red. This is DoD-3's security boundary and task rail 3's named substitution; it is the drill that matters most on this row.\nD2 TREAT A CALLER-SHAPED HUMAN FLAG AS AUTHORITY -> the same class of arm reds. Run it SEPARATELY from D1, one degree of freedom each, because a guard may read the affordance and STILL accept a caller flag as a second door.\nD3 RENDER AN UNAVAILABLE DESTINATION AS INERT rather than disabled -> the disabled-state arm reds. If it stays green the arm is asserting \"clicking does nothing\", which an inert control satisfies while telling the operator nothing.\nD4 HARDCODE A GOAL OR RUN ID in a route -> the durable-identifier arm reds. If it stays green the expectation is a literal beside the assertion rather than the fixture's value (DoD-4's \"no fixed goal/run ids\").\nD5 CLEAR THE REFUSAL on failure instead of keeping it visible -> the refusal arm reds on its exact code and layer.\nA GREEN drill is a FINDING to report in the row comment, never a pass to bank.\n\nGATES — each alone, FOREGROUND, UNPIPED. An exit-0 leg printing no count line executed NOTHING and is UNVERIFIED.\nLEG A: `pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx` — exit 0, count line quoted.\nLEG B: `pnpm --filter @moe/control-room test` — the whole package, because this row wires the shell and a break shows up outside the two focused suites.\nLEG C: `pnpm --filter @moe/control-room typecheck`, quoting the `$ tsc` echo.\nAttribute repo-wide red by path against the merge-base; the delta intersected with this row's owned paths must be EMPTY, foreign red disclosed verbatim and never edited.\nLINE CAPS: `wc -l` every production file touched; target <=250, split before 400.\n\nADVERSARIAL REVIEW, in the step note: can the approval command be reached by any route the server affordance did not authorize (D1/D2 guard it); is any goal or run id synthesised, placeholder, or string-formatted rather than durable; is any unavailable destination inert rather than disabled; does a refusal ever get cleared or genericised; did this row re-implement the disabled-Open behaviour or the runless-card rule (both belong elsewhere); and did it edit cordum-app.tsx before the child's signature landed.\n\nDURABLE HANDOFF — moe.add_comment, not the completion summary. Keep each under ~4500 chars and SPLIT rather than truncate (both cap at 5000 SILENTLY). Carry step 1's measurements with the freshly-captured HEAD sha; THE CHILD-FIRST ORDERING AND WHY (file-granular pathspec versus hunk-granular conflict on cordum-app.tsx), so the next reader does not re-litigate it; the affordance you found and where it is read from; the five drill outcomes with restore proofs; and every gate leg's exit code and count line. Name task-9bd8f5298c6a4d0cac47b92e62a97423 as the child whose widened callback this row consumes. Commit by EXPLICIT PATHSPEC of this row's owned paths, message BEFORE the `--` (a `-m` after `--` is parsed as a pathspec and nothing lands). Never `git add -A`, never a bare `git commit`.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/cordum-app.tsx",
        "apps/control-room/src/v2/shell/nav-rail.tsx",
        "apps/control-room/src/v2/goals/approve-plan.tsx"
      ]
    }
  ],
  "status": "BLOCKED",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor unpark, human-approved 13:10Z: block premise expired — task-14e3b36a is DONE (landed 9c83dcdd) and goal-card.test.tsx:148 now covers the disabled Open control. Architect: re-probe onOpenBoard arity at fresh HEAD before planning.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "MEDIUM",
  "order": 120.9,
  "comments": [
    {
      "id": "comment-9810f791382545dbbc2bee750d0b883d",
      "author": "architect-be964894",
      "content": "VERIFY-OR-BLOCK MEASUREMENT (2026-08-25, HEAD e59fa4d7732e2ac0395b7e01750a9198c9c756f0):\n1. Navigation ownership/collision. `cordum-app.tsx`, `cordum-app.test.tsx`, `cordum-shell.tsx`, `cordum-shell.test.tsx`, and `shell/nav-rail.tsx` are all tracked-modified foreign WIP. CordumApp hard-codes `activeNav=\"goals\"` and supplies no `onNavigate`; the rail merely enables every destination when any callback exists, not per-route availability. The typed route source required by DoD 2 does not exist (`rg` finds only NavId/onNavigate). Downstream task-7e605144... explicitly reserves cordum-app.tsx and cordum-app.test.tsx for P2.15c integration; planning this row over those paths before it lands is a live collision.\n2. Approval route is absent, not merely unwired. `approve-plan.tsx` is clean and deliberately read-only/disabled. The daemon's offered `approval.decide` NextAllowedCommand contains command identity, target, schema and expectedVersion only. `planning-run-read.ts` returns runId/lifecycle/submissionHash plus sealed plan, acceptance and authority hashes; it does NOT return an approval record, activation witness or graph revision input.\n3. The production handler `apps/daemon/src/planning/planning-services.ts:231-309` requires caller payload fields `runId`, `record`, `command`, `activation`, and `graphRevisionRef`; it checks record.actor against the authenticated principal, exactRevisionHash against durable submissionHash, applies the approval command, verifies the run binding, then activates. The browser has no production reader for the authoritative record/activation values. The only current browser payload is `DEV_PAYLOADS[\"approval.decide\"]` in dirty `live-dispatch.ts`, explicitly documented as fixture-shaped/fabricated. Using it would violate this row's no-fabricated-route and no caller-shaped HUMAN authority rails.\n4. Dispatch WIP is also not safe to adopt: `live-dispatch.ts` is tracked-modified at 506 physical lines and duplicates a new untracked `live-command-dispatch.ts` (167 lines). It is involved in broad foreign goal/live work; a clean checkout has no extracted module.\nRequired resolution before UI planning: (a) complete P2.15c or explicitly reassign its CordumApp paths, and (b) land a production daemon-owned approval-intent/command composition seam that returns or server-derives every non-operator approval/activation fact while preserving `approval.decide`'s authenticated human-review gate. Then this UI row can consume the daemon-minted affordance, submit only the typed operator decision, refresh the run/surface after success, and render exact refusal code/layer. Without that seam, an enabled button must remain disabled.",
      "timestamp": "2026-08-25T12:34:40.612Z"
    },
    {
      "id": "comment-c0d05ed0fa0a469296269073fc6d4e00",
      "author": "worker-ca97b7bd",
      "content": "HEADS-UP for a row whose description names the Control Room entrypoints — from task-15ca5c44ec8241008d542dc63633cf42 (P1.2b-iii).\n\n`apps/control-room/src/main.tsx` and `apps/control-room/src/v2/cordum-app.tsx` now carry the project-manager composition, committed at **`cccf133e2e6d43d3ff7c6a42798f04decba49650`**, and both are CLEAN at HEAD (`git status --porcelain` of them is empty, `git ls-files -v` reports `H` — no skip-worktree bit). Author your hunks on top of that, not against a remembered HEAD copy.\n\nWhat changed, so you can rebase your plan rather than re-derive it:\n- `main.tsx` (101 -> 138 lines). `chooseRoot` is now `chooseRoot(search, managerMode, liveSetup, managerSetup)` and its FIRST branch is the project manager — the branch ORDER is load-bearing and pinned by a test arm, so do not insert a query branch above it. `prepareV2LiveSetup` is now `prepareV2LiveSetup(search, managerMode)` and short-circuits on `managerMode ||`. New `prepareProjectManager(managerMode)`. `mountControlRoom` is now `(container, clock = BROWSER_CLOCK, hostname = globalThis.location?.hostname ?? \"\")` — a third positional parameter; a caller overriding the host must also pass the clock.\n- `v2/cordum-app.tsx` (228 -> 234 lines). The eyebrow MOVED below `const attached` (it reads `projectId`, so keeping it at its old position is a TypeScript temporal-dead-zone error). It now renders `PROJECT · ${projectId ?? \"PAIRING\"}` instead of the former `PROJECT · MOE-NG`. `<ProjectBoundary projectId={projectId} />` renders above every non-fixtures body via a new `content` binding, and a `useEffect` owns `document.title` with restore-on-cleanup — if your row also wants the title, coordinate rather than adding a second writer.\n\nDeliberately NOT done and still open: `CordumShell` has no `projectId` / `data-project-id` prop (it does not exist at HEAD). If your row needs project identity in the shell chrome, that is unclaimed.\n\nNew files this row added under the same directory, so plan around them: `apps/control-room/src/entry-project-manager.ts` (the pure route seam) and `apps/control-room/src/entry-project-manager.test.tsx` (8 arms).",
      "timestamp": "2026-08-27T12:09:06.027Z"
    },
    {
      "id": "comment-9a0e3e9d411347858de75829fbe1cb76",
      "author": "architect-ac8532da",
      "content": "SEQUENCING NOTE (architect-ac8532da, 17:30Z): the approval seam this row's step 1 names (task-6646f888, P2.15d-i, order 15.4) was itself split — Leaf A task-3051c597 (order 15.3) publishes the wire kind `approval.decide_intent` (seven-file generated fan-out mirroring 5ff4e396), Leaf B task-6646f888 lands the daemon seam gated on A. This row does NOT wait for either: it delivers navigation, the typed route, durable ids and the affordance-gated approval REFUSAL state now; the approve-button wiring is a follow-up row the architect files once 6646f888 is at HEAD. Also: cordum-shell.test.tsx lives at apps/control-room/src/v2/cordum-shell.test.tsx (component at v2/shell/) — the gate paths in the P2.15c/P2.15 plans are correct as written.",
      "timestamp": "2026-08-27T17:30:46.770Z"
    },
    {
      "id": "comment-81c61b93e52141808a704ada04ac9424",
      "author": "agent",
      "content": "ROUTED FINDINGS from a free-agent live UI audit (2026-08-27, real daemon hosting the built bundle; shots at C:/Users/Yaron/AppData/Local/Temp/claude/D--projexts-moe-next/267e8020-ee4d-4dc9-b5a4-46e4ed922380/scratchpad/shots-after/12-goal-open.png + .txt). COMMENT because this row owns approve-plan.tsx, nav-rail.tsx and shell-model.ts. Copy the owner will read at the ONE gate that matters:\n\n1. approve-plan.tsx:47-51 note under the disabled Approve: \"Approving from the control room needs the daemon-authored approval record (backend gap); it is not wired here so no fabricated decision is posted.\" For the owner: \"Approving from here is not available yet. Moe will only record a decision it authored itself; that part is being built.\" - with the current sentence behind Details.\n\n2. approve-plan.tsx:59 \"PLAN - 1 steps\" / \"ACCEPTANCE - 1 obligations\": pluralise (\"1 step\", \"1 obligation\") and say what an obligation is once (\"what must be proven before this plan counts as done\").\n\n3. The \"Back to goals\" button sits BETWEEN the plan review and the work board, so the board below looks like a different screen; the breadcrumb already offers the same back. Either drop the button or move it below the board.\n\n4. TRUTH (separate row task-f053d212 owns the daemon side): the banner \"Ready for your approval\" is rendered whenever the read says reviewable:true, and the read derives that from lifecycle alone (planning-run-read.ts:258), so on every seeded lane it shows over an approval.decide that is already COMMITTED on the same screen. When you wire the Approve control, do not let it enable on reviewable alone until that row lands - or the first click a human makes will be a duplicate decision.\n\n5. nav-rail.tsx brand badge \"v0.1\" is a string literal with no source (not package.json, not the daemon's /bootstrap). Either read it from a real source or drop it; a version the daemon did not state is fabricated state in miniature.\n\n6. shell-model.ts:49 LAGGING banner says \"commands remain available\" although live-board-feed.ts defines LAGGING as a deadline miss on the poll; the honest sentence is \"the last answer was slow; what you see may be a few seconds old\".",
      "timestamp": "2026-08-27T19:44:59.916Z"
    },
    {
      "id": "comment-cb1588c6fe664abd8e4e4304f48fd528",
      "author": "agent",
      "content": "BROWSER LANE IS RED BEFORE THE BROWSER OPENS - measured 2026-08-27 ~19:50Z at HEAD 8bab9211 with `pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts prd-to-approval` -> 1 failed, exit 1, at prd-to-approval.spec.ts:88: `expect(seedTranscript).toContain(\"code=BOOTSTRAP_APPROVAL_ACTOR_UNBOUND layer=DAEMON_INGRESS\")`. The seed now exits 0 and commits approval.decide (`committed approval.decide demo-seed-approval.decide` in its transcript) because since ccff6bc1 (08-21, \"the operator's own approval dispatch is the human review\") the operator credential IS the human seat and the daemon mints the witness. The spec's premise - that the seed stops at a human-authority boundary - is stale, and it fails at :88 before `page.goto`, so none of the browser assertions (pairing label, cr.goals.count, cr.approve.screen, cr.approve.button disabled, cr.board.root, CSP) run for anyone. QA on task-22ae2916 already attributed the red foreign.\n\nThis row rewrites that spec to click Approve, so it is the natural owner: when you do, drop the exit-1 / ACTOR_UNBOUND expectation (assert the seed's actual outcome instead) so the lane runs again. Until then, every control-room row's browser leg is dead - worth saying in this row's step 1 so a worker does not re-attribute it from scratch.\n\nHeads-up for the same rewrite: `cr.board.root` still exists, but the three nested column testids under `cr.board.column.` were renamed in e2973797 (cr.board.colhead / colmeaning / colstatus) because the v1 sheets in the same bundle style that prefix as 18rem lane boxes; the column ROOTS keep the prefix.",
      "timestamp": "2026-08-27T19:59:35.249Z"
    },
    {
      "id": "comment-4c2eb9f454224058b8826efb7217a6b4",
      "author": "agent",
      "content": "BROWSER LANE IS GREEN AGAIN (free agent, 2026-08-28 ~09:55 local). I landed the minimal premise fix I flagged in comment-cb1588c6: `test(e2e): align the v2 browser lane with the operator-seat seed so it runs again` - prd-to-approval.spec.ts now asserts the seed's ACTUAL outcome (exit 0 + \"committed approval.decide\", per ccff6bc1's operator-seat witness) instead of the dead exit-1 / BOOTSTRAP_APPROVAL_ACTOR_UNBOUND premise. Nothing else in the spec changed. `pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts prd-to-approval` -> 1 passed (3.9s); e2e tsc exit 0. Pushed.\n\nFor this row's rewrite (click Approve): the lane is a working base again - extend it, do not resurrect the exit-1 expectation. Note the spec still asserts \"Ready for your approval\" over the seeded run, which is the daemon's current (false) answer per task-f053d212; when that row lands, this assertion must follow the read.",
      "timestamp": "2026-08-28T06:42:13.805Z"
    },
    {
      "id": "comment-bf1a0499def04dfe88caf9466c23fc7f",
      "author": "worker-ce9da376",
      "content": "STEP 1 READ-ONLY MEASUREMENT at HEAD de458f8fb163665599109f9b781c71aa14ace4e3 (2026-08-28).\n\nPREREQUISITE GATES PASSED ON DISK:\n- `git grep -n \"LiveAttempts\" HEAD -- ...cordum-app.tsx ...main.tsx` -> production hits in main.tsx:15/:60/:85 and cordum-app.tsx:78/:84/:109/:238. Exact porcelain for cordum-app.tsx + cordum-app.test.tsx was empty.\n- `git grep -n -E \"deriveGoalCatalog|buildGoalBriefCommand\" HEAD -- ...live-goals.tsx` -> deriveGoalCatalog import :9 and call :78. Exact porcelain for live-goals.tsx + goals-home.tsx was empty.\n- Current Moe collision scan still shows CRITICAL task-115fedb (worker-2bc775a4) owns live-goals.tsx and unassigned task-2eb6d36 owns cordum-app.tsx/test. I will not touch live-goals while task-115fedb is active; coordination ping msg-978e44471cab4ff3b8b26195b365c9a7. The two required predecessor code surfaces are nevertheless landed and clean.\n\nNAV-RAIL ADOPTION PROVENANCE:\n- porcelain: ` M apps/control-room/src/v2/shell/nav-rail.tsx`; `?? apps/control-room/src/v2/shell/nav-rail.css`.\n- SHA256: nav-rail.tsx 3379A92A56628BB7D782E9874081074C60AC675AC49A39B534A16F76502FA70E (125 lines); nav-rail.css DB515DBC340CEC38EA65DA7966B1053FFCEA84047F38210BCFFDED300DF3D376 (15 lines).\n- live non-final task file-field scan excluding this row returned []; task-3e327547 named the old v1 path and is DONE.\n- tracked diff is exactly the planned disabled-destination hunk: CSS import; `unavailable = !active && onNavigate === undefined`; disabled/aria-label/title/onClick; SOON StatusChip. CSS only styles disabled hover/chip. All bytes serve DoD-2 and are adopted here.\n\nAPPROVAL AUTHORITY:\n- daemon-command-vocabulary.ts:206 still maps `approval.decide` to caller payload keys [activation, command, graphRevisionRef, record, runId].\n- daemon-command-graph-approve.ts:90-103 `intentOf` still reads activation/record from caller; :110+ treats record.actor as authority claim.\n- live-dispatch.ts:184-205 still carries fixture-shaped DEV_PAYLOADS approval.decide with HUMAN_APPROVED activation/record.\n- Leaf-A vocabulary `approval.decide_intent` has landed in contracts/generated client/MCP, but there is NO daemon handler/payload composition hit at HEAD. The server-owned seam from task-6646f888 has not landed. Per plan, this row keeps Approve disabled and never dispatches/fabricates.\n\nCURRENT ANCHORS:\n- cordum-app.tsx: LIVE_RUN_SUBJECT :46, OpenBoard :241, open state :252, ApprovePlan runId constant :309, activeNav goals :357 and no onNavigate.\n- cordum-shell.tsx props navItems/activeNav/navBadges/onNavigate :38-41 and NavRail threading :157-161.\n- NAV_IDS exact six at shell-model.ts:80-86; CORDUM_NAV_ITEMS :101+.\n- approve-plan.tsx has no dispatcher import and disabled cr.approve.button/note :41-50.\n- e2e pins disabled button + /no fabricated decision/ at :129-130.\n\nBASELINE: `pnpm --filter @moe/control-room test` EXIT 0; Test Files 108 passed (108); Tests 1397 passed (1397).",
      "timestamp": "2026-08-28T09:38:14.212Z"
    },
    {
      "id": "comment-b2770299f06f49698c02039dc46db83a",
      "author": "worker-ce9da376",
      "content": "STEP 2 STOP — delivered-tree shape/ownership remeasurement found an unsafe plan contradiction before editing (HEAD de458f8f).\n\n1. LIVE OWNERSHIP COLLISION. Moe's claim response names CRITICAL task-115fedb7622a4497958393f3597a9a5d on `apps/control-room/src/v2/goals/live-goals.tsx`; it is WORKING under worker-2bc775a4 with 0/6 steps. I pinged msg-978e44471cab4ff3b8b26195b365c9a7 and have not touched the file. Unassigned WORKING task-2eb6d36 also declares cordum-app.tsx/test. The task rail says block rather than collide.\n\n2. STEP-5 SHAPE FAN-OUT IS OUTSIDE THE APPROVED NINE PATHS:\n- goals-home.tsx currently declares `onOpenBoard(goalId,title)` and calls it with two args.\n- goals-home.test.tsx:252 pins `toHaveBeenCalledWith(\"goal-recovery\", \"...\")`.\n- live-goals.tsx declares/forwards the two-arg callback; live-goals.test.tsx:211 pins the exact two-arg call.\nBoth tests must change when step 5 requires `(goalId, planningRunRef, title)`, but neither is owned. Adding them takes the row beyond its exact nine-path commit unless other scope is split.\n\n3. THE REQUIRED MISSING-RUN REFUSAL CANNOT BE IMPLEMENTED IN AN OWNED FILE. Step 5 says a card without planningRunRef must have its Open control disabled with a reason. GoalCardProps currently has only `onOpenBoard: () => void`; GoalCard renders both its title button and ActionButton as enabled and has no disabled/reason prop (goal-card.tsx:49-54/:56-182). Correct implementation requires goal-card.tsx plus goal-card.test.tsx, two more unowned paths. A no-op callback would be the forbidden inert affordance.\n\n4. AFFORDANCE SOURCE CLAIM IS STALE. Step 4 says derive approval state from the SurfaceFrame that LiveWorkBoard/LiveGoalsHome “already report”. LiveWorkBoardProps and LiveGoalsHomeProps expose only `onConnection(connection)`; they do not report the frame/offers. SurfaceFrame.offers exists, but a production writer callback must be added. The only in-plan place is live-goals.tsx, currently owned by task-115fedb.\n\n5. FILE-SIZE ESCAPE ALSO EXCEEDS THE PATH CAP. cordum-app.tsx is already 370 lines before this row. Step 4 says past 340 extract cordum-route.ts “(+ test)”; those two paths raise the advertised 9 to 11 before the four missing shape consumers above. Global rail caps a task at <=10 distinct paths, and step 7 requires exactly the original nine.\n\nNo step-2 test or source byte was changed. This needs architect replan/split: land the GoalCard disabled-open seam + its tests as a leaf, include both callback consumer tests, assign one SurfaceFrame writer after task-115fedb releases live-goals, and keep the route extraction/pathspec within <=10.",
      "timestamp": "2026-08-28T09:44:00.321Z"
    },
    {
      "id": "comment-c91849044d97481eaff8b3edc8980f06",
      "author": "worker-4bba4fa4",
      "content": "STEP-1 VERIFY-OR-BLOCK @ HEAD d7a841778599ab3ca767b1e1d319f2cb07a365fd (2026-08-28; read-only, zero bytes authored).\n\nGATE (a) FAILS: prerequisite leaf task-14e3b36a819c478aa1d20b2aa8542dcc is PLANNING, unassigned, 0 plan steps. Delivered-tree proof agrees: `git grep -n 'onOpenBoard: (goalId: string, planningRunRef: string, title: string)' HEAD -- .../goals-home.tsx .../live-goals.tsx` exits 1 with no hits. `goal-card.tsx` has no disabled/unavailable Open control; its only `reason` hit is the unrelated offline fact StatusChip at :175. Landing this route first would still leave non-seed goals without a durable planningRunRef and would create the visible seeded-lane regression the plan calls a safety ordering. This row cannot author the leaf's files without reintroducing the >10-path split defect.\n\nOther gates were re-measured so they need not be rediscovered: task-2eb6d36d is DONE, and cordum-app.tsx/.test.tsx are clean; task-115fedb is now BLOCKED/unassigned, but its dependent leaf still has not been planned/landed. Exact owned-path porcelain contains only the provenance-held nav WIP: ` M nav-rail.tsx`, `?? nav-rail.css`; all other row paths clean. Their hashes remain byte-identical to comment-bf1a0499: nav-rail.tsx `3379A92A56628BB7D782E9874081074C60AC675AC49A39B534A16F76502FA70E` (125 lines), nav-rail.css `DB515DBC340CEC38EA65DA7966B1053FFCEA84047F38210BCFFDED300DF3D376` (15 lines). CordumApp is still 370 lines; LiveWorkBoard 44; approve-plan 189; cordum-shell 196.\n\nApproval seam remains intentionally unavailable: task-6646f888a488481e936dc0dd3f966cd5 is BLOCKED and daemon-command-vocabulary.ts:206 still defines approval.decide payload keys `[activation, command, graphRevisionRef, record, runId]`. If/when the leaf lands, this row must still keep approval disabled and never fabricate/dispatch caller-authored HUMAN authority.\n\nVERDICT: BLOCKED solely on missing task-14e3b36a delivered code. No test/source byte was changed and no foreign WIP was adopted.",
      "timestamp": "2026-08-28T12:24:34.520Z"
    },
    {
      "id": "comment-f536156e3614409695755c46b9eec6d8",
      "author": "worker-57c90511",
      "content": "STEP 1 — VERIFY-OR-BLOCK GATE (DoD 1). READ-ONLY, zero bytes authored. PARTIAL PROCEED: steps 2-4 are GO, steps 5-7 are GATED SHUT. Every grep below unpiped with its own exit code.\n\nANCHOR. `git rev-parse HEAD` = 13d5750e21cc0b99a7378ef6645dbf5ea16b2408 (exit 0). The plan was architect-measured at 595c32c2; HEAD has moved since, so every premise is re-measured here rather than inherited.\n\n(a) ARITY RE-PROBE — STILL TWO ARGS. Probed the BARE symbol, never `onOpenBoard(`, exactly as the plan warns. `git grep -n \"onOpenBoard\" HEAD -- apps/control-room/src` EXIT 0. Declaring sites unchanged: `goals-home.tsx:47` and `live-goals.tsx:45` both read `readonly onOpenBoard: (goalId: string, title: string) => void`. This row's three JSX prop-passing consumers are exactly where the plan said: `cordum-app.tsx:316`, `:319`, `:335` — all `onOpenBoard={openBoard}`, which the paren form would have missed.\n\n(b) THE CHILD-FIRST GATE IS **EMPTY** — STEPS 5-7 BLOCKED.\n`git grep -n \"planningRunRef\" HEAD -- apps/control-room/src/v2/goals/goals-home.tsx` EXIT 1, ZERO hits.\nPOSITIVE CONTROL on the same file and revision, so exit 1 cannot be read as \"grep is broken\": `git grep -c \"onOpenBoard\" HEAD -- .../goals-home.tsx` EXIT 0, prints 4.\nBoard agrees with the tree: `task-9bd8f5298c6a4d0cac47b92e62a97423` is **AWAITING_APPROVAL**, unassigned — a human gate, not a worker gate, so no amount of worker effort advances it. The widened signature has NOT landed. Per step 1(b) and step 5's own STOP instruction this is an ORDERING block, not a design block, and it does NOT park steps 2-4.\n\n(c) REACHABLE REGARDLESS — CONFIRMED ON DISK, not assumed. `nav-rail.tsx`, `cordum-shell.tsx`, `live-work-board.tsx` and `approve-plan.tsx` contain no `onOpenBoard` hit in the (a) sweep. Steps 2-4 proceed.\n\n(d) OWNERSHIP + PORCELAIN, per path, with byte-identity rather than porcelain alone (porcelain reports ` M` on a byte-identical file after any peer touch in this shared worktree):\n  cordum-app.tsx 370 lines — clean, BYTE-IDENTICAL to HEAD\n  cordum-app.test.tsx 503 — clean, BYTE-IDENTICAL\n  approve-plan.tsx 189 — clean, BYTE-IDENTICAL\n  approve-plan.test.tsx 115 — clean, BYTE-IDENTICAL\n  cordum-shell.tsx 196 — clean, BYTE-IDENTICAL\n  live-work-board.tsx 44 — clean, BYTE-IDENTICAL\n  nav-rail.tsx 125 — ` M`, GENUINELY DIFFERS from HEAD (not a stat-cache artifact)\nCOLLISION SCAN, file-field over every NON-TERMINAL row's implementationPlan excluding this one: exactly ONE hit — task-9bd8f529 (AWAITING_APPROVAL) naming `cordum-app.tsx`. That is the child from (b), already handled by the gate. No other live row names any path in this roster.\n\nNAV-RAIL WIP — PROVENANCE RE-VERIFIED, ADOPTED. Two prior sessions recorded it (comment-bf1a0499, comment-c9184904). Today's hashes are BYTE-FOR-BYTE the same as both records:\n  nav-rail.tsx SHA256 3379a92a56628bb7d782e9874081074c60ac675ac49a39b534a16f76502fa70e (125 lines)\n  nav-rail.css SHA256 db515dbc340cec38ea65da7966b1053ffcea84047f38210bcffded300df3d376 (15 lines) — **UNTRACKED** (`git ls-files --error-unmatch` exit 1), so `git diff` is BLIND to it. Do not sweep it.\nThe tracked diff is exactly this row's DoD-2 hunk and nothing else: the CSS import, `unavailable = !active && onNavigate === undefined`, `disabled`/`aria-label`/`title`, an `onClick` that is `undefined` when unavailable, and a SOON StatusChip. Adopted per rail 1 as provenance-verified, unchanged across three sessions.\n\n(e) THE APPROVAL AUTHORITY **EXISTS** — the old block premise has EXPIRED. This is a change from comment-bf1a0499 and comment-c9184904, both of which correctly found no seam at their HEADs.\n  Typed command: `approval.decide_intent` is in `RUNTIME_COMMAND_KINDS` (runtime-vocabulary.ts:86) and in the generated client as a builder (generated-client.ts:178 declaration, :280 construction).\n  Daemon-owned seam: `apps/daemon/src/planning/approval-intent.ts` + `-contracts.ts` + `-sources.ts`, landed by task-6646f888 (commit 595c32c2).\n  The witness is server-minted, not payload-decoded — daemon-command-edges.ts:39-47 states it outright: \"THE WITNESS IS MINTED HERE ... the authenticated principal compared against the daemon's CONFIGURED operator. It is never decoded from request bytes, so no payload can present one.\" Refusals travel back UNRESTAMPED, carrying the answering authority's own code and layer.\n  Affordance READ surface: `apps/daemon/src/http/affordance-read.ts` with `affordance-read-planning.test.ts` beside it, over `packages/contracts/src/runtime/runtime-affordance.ts`.\nSo step 1(e)'s block condition — \"if no production affordance exists, that is a BLOCK naming the missing producer\" — does NOT fire. Step 5 is gated by (b) ALONE.\n\nBASELINE, foreground, whole package: `pnpm --filter @moe/control-room test` EXIT 0, `Test Files 108 passed (108)` / `Tests 1424 passed (1424)`, 11.62s. Taken WITH the adopted nav-rail WIP in the tree, since that is the tree I am editing. Prior rows recorded 1397 at de458f8f; the +27 is the landed disabled-Open leaf task-14e3b36a and peers, not this row.\n\nPLAN NOTE I am carrying into step 2. Step 2 as written also asks for APPROVAL arms (its items 3 and 4), which by the plan's own sequencing only go green at step 5 — and step 5 is gated shut by (b). I will therefore write step 2's NAVIGATION arms (items 1, 2, 5) now and hold the approval arms for whoever resumes at step 5. Committing arms that cannot go green would leave a red suite in HEAD and would fail step 7's LEG A/LEG B exit-0 requirement, which is a worse outcome than a disclosed narrowing. This is recorded, not silent. — worker-57c90511",
      "timestamp": "2026-08-29T16:44:13.540Z"
    },
    {
      "id": "comment-04cb8ca6aebb4b8f9991beb31c0ffb51",
      "author": "worker-57c90511",
      "content": "STEPS 2-4 DELIVERED AND COMMITTED — 6069bfa110e2b3e1f8082165e09c2389738f5e3f. Steps 5-7 blocked at step 5 on the ordering gate. This is the durable record; the completion summary is write-only and is not where evidence lives.\n\nWHAT LANDED, by explicit pathspec, exactly six paths and `git show --stat HEAD` lists exactly those six:\n  apps/control-room/src/v2/shell/shell-routes.ts (NEW, 83 lines)\n  apps/control-room/src/v2/shell/nav-rail.tsx (125 -> 167)\n  apps/control-room/src/v2/shell/nav-rail.css (NEW to git; the adopted WIP, previously untracked)\n  apps/control-room/src/v2/shell/cordum-shell.tsx (196 -> 201)\n  apps/control-room/src/v2/goals/live-work-board.tsx (44 -> 78)\n  apps/control-room/src/v2/cordum-app.test.tsx (503 -> 626)\n325 insertions, 5 deletions. The index was verified EMPTY before the commit and left empty after, and all six paths now hash byte-identical to HEAD. In a worktree carrying 115 dirty entries, no foreign byte was swept.\n\nTHE ADOPTED NAV WIP IS NOW IN GIT. nav-rail.tsx + nav-rail.css had been held as uncommitted WIP across at least three sessions (comment-bf1a0499, comment-c9184904, and my step 1). They are no longer at risk from a foreign `git add -A` or a session-end sweep. Their provenance was re-verified byte-for-byte before adoption.\n\nDoD STATUS, honestly scored:\n  DoD 1 SATISFIED — step 1 re-measured ownership, porcelain, the collision scan and the approval affordance, and reported the gate rather than colliding. comment-f536156e3614409695755c46b9eec6d8.\n  DoD 2 SATISFIED for navigation. One typed route source of truth (shell-routes.ts), durable goal identifier on the board from the daemon's own SurfaceFrame.planningGoalRef, unavailable destinations explicitly disabled with their measured reason. NOT satisfied end to end: the shell wiring in cordum-app.tsx is step 6, gated.\n  DoD 3 NOT STARTED — plan approval. This is the block.\n  DoD 4 PARTIAL — navigation, disabled affordances, no-fixed-ids and the nonzero exact ROUTE roster are covered by five committed arms. The success/refusal arms and the AFFORDANCE roster belong to step 5.\n\nTHE BLOCK, re-measured at HEAD e3f5f728 immediately before committing, not inherited from step 1:\n  `git grep -n \"planningRunRef\" HEAD -- apps/control-room/src/v2/goals/goals-home.tsx` EXIT 1, ZERO hits.\n  POSITIVE CONTROL, same file same revision: `git grep -c \"onOpenBoard\" HEAD -- .../goals-home.tsx` EXIT 0, prints 4. So the exit 1 is absence, not a broken probe.\n  The child task-9bd8f5298c6a4d0cac47b92e62a97423 has MOVED since step 1: AWAITING_APPROVAL -> WORKING, and it is UNASSIGNED. Its plan is approved and it is claimable by any worker right now. Clearing it is the single highest-leverage move for this chain.\n  HEAD moved twice under me (13d5750e -> e3f5f728). I checked rather than assumed: `git diff --stat 13d5750e e3f5f728 -- apps/control-room/` is EMPTY (a rails-only commit), so every gate result above still describes the tree I committed onto.\n\nWHY I DID NOT PROCEED INTO cordum-app.tsx. The child changes `openBoard`'s SIGNATURE; this row wires navigation and approval. Different hunks, same file. Pathspec commits are FILE-granular and the conflict is HUNK-granular, so whoever commits second sweeps the other. The child consumes nothing from here and produces the signature this row must match, so the child goes first. That is the plan's own ruling and it still holds at this HEAD.\n\nWHY THE APPROVAL ARMS ARE NOT IN THE COMMIT. Step 2 asks for them, but by the plan's own sequencing they green only at step 5. Committing arms that cannot go green would have left a red suite in HEAD and failed step 7's LEG A and LEG B exit-0 requirement. approve-plan.test.tsx and approve-plan.tsx are UNTOUCHED and byte-identical to HEAD. Disclosed here, not silent.\n\nFOR WHOEVER RESUMES AT STEP 5 — step 1(e)'s missing-producer block does NOT fire; the authority now exists and this is a change from the two earlier sessions that correctly found it absent:\n  typed command `approval.decide_intent` in RUNTIME_COMMAND_KINDS (packages/contracts/src/runtime/runtime-vocabulary.ts:86) and the generated client (packages/control-room-client/src/generated/generated-client.ts:178 declaration, :280 construction)\n  daemon seam apps/daemon/src/planning/approval-intent.ts + -contracts.ts + -sources.ts, from task-6646f888\n  the witness is SERVER-MINTED, never decoded from payload bytes — apps/daemon/src/daemon-command-edges.ts:39-47 says so outright, and refusals travel back UNRESTAMPED carrying the answering authority's own code and layer, which is exactly what DoD-3's \"pins exact code/layer\" needs\n  affordance read apps/daemon/src/http/affordance-read.ts, with affordance-read-planning.test.ts beside it, over packages/contracts/src/runtime/runtime-affordance.ts\nSo step 5 is gated by the ORDERING alone. Do not re-derive this; do re-run the probes at your own HEAD.\n\nGATES at the committed tree, each alone, foreground, unpiped:\n  LEG A `pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx src/v2/goals/approve-plan.test.tsx` EXIT 0 — Test Files 2 passed (2), Tests 28 passed (28)\n  LEG B `pnpm --filter @moe/control-room test` EXIT 0 — Test Files 108 passed (108), Tests 1429 passed (1429); step 1's baseline was 108/1424 and +5 is exactly this row's five arms\n  LEG C `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo, trustworthy because the same command was RED one invocation earlier on a natural TS2305\n  Owned-path intersection with any foreign red: EMPTY. There is no foreign red in this package to disclose.\n\nFIVE DRILLS, all red where red was required, one degree of freedom each, every restore proven by SHA256 in a separate call — full detail in step 4's note. One is worth repeating here as a FINDING rather than a pass: D4 hardcoded the goal id to the SAME literal the fixture supplies, and the positive \"repeats verbatim\" arm stayed GREEN because that is a fixed point it cannot escape. The ABSENCE arm caught it. D4b re-ran with a different literal and both arms redded, which settles that the positive arm is not vacuous. Anyone porting this arm's shape should carry the absence arm with it, not just the positive one. — worker-57c90511",
      "timestamp": "2026-08-29T16:58:06.695Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-25T12:03:09.576Z",
  "updatedAt": "2026-08-29T17:00:03.101Z",
  "metrics": {
    "firstClaimAt": "2026-08-25T12:30:34.814Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 5
  },
  "contextFetchedBy": [
    "architect-be964894",
    "governor-ec9a8df1",
    "architect-ac8532da",
    "worker-9e37063a",
    "worker-ce9da376",
    "architect-94dd67c9",
    "worker-4d8ff141",
    "worker-4bba4fa4",
    "worker-57c90511"
  ],
  "blockedReason": "STEP 5 STOP per step 5's own instruction and step 1(b)'s gate. ORDERING block — NOT a design block, NOT missing authority, NO architect ruling owed. Full record: comment-04cb8ca6aebb4b8f9991beb31c0ffb51 + comment-f536156e3614409695755c46b9eec6d8. Serena: task-40017f79-handoff.\n\nSTEPS 2-4 DELIVERED AND COMMITTED: 6069bfa110e2b3e1f8082165e09c2389738f5e3f, six paths by explicit pathspec, index empty before and after. Nothing of this row is uncommitted.\n\nGATE, re-measured at HEAD e3f5f728 just before committing: `git grep -n \"planningRunRef\" HEAD -- apps/control-room/src/v2/goals/goals-home.tsx` EXIT 1, zero hits. POSITIVE CONTROL same file/rev: `git grep -c \"onOpenBoard\"` EXIT 0 prints 4 — the exit 1 is absence, not a broken probe.\n\nUNBLOCKED BY EXACTLY ONE THING: land child task-9bd8f5298c6a4d0cac47b92e62a97423. It MOVED since step 1 — AWAITING_APPROVAL -> WORKING, plan approved, UNASSIGNED, claimable by any worker NOW. Clearing it opens steps 5-7.\n\nWHY NOT PROCEED: the child changes openBoard's SIGNATURE, this row wires navigation/approval — different hunks of cordum-app.tsx. Pathspec commits are FILE-granular, the conflict HUNK-granular: whoever commits second sweeps the other.\n\nDO NOT RE-BLOCK ON APPROVAL AUTHORITY — it EXISTS now, reversing comment-bf1a0499 and comment-c9184904 (correct at their HEADs). approval.decide_intent: runtime-vocabulary.ts:86, generated-client.ts:178/:280. Seam: apps/daemon/src/planning/approval-intent*.ts (task-6646f888). Witness SERVER-MINTED, never payload-decoded (daemon-command-edges.ts:39-47). Affordance read: apps/daemon/src/http/affordance-read.ts.\n\nGREEN AT THE COMMITTED TREE: LEG A 2 files/28 tests exit 0; LEG B 108 files/1429 tests exit 0 (baseline 108/1424, +5 = this row's arms); LEG C typecheck exit 0 with the `$ tsc` echo, RED one invocation earlier on a natural TS2305. Five drills red on demand, restores SHA256-proven. Steps 2-4 need no rework.",
  "blockedResourceId": null,
  "blockedFromStatus": "WORKING",
  "blockedAt": "2026-08-29T17:00:03.100Z",
  "priorHandoffs": [
    {
      "whatIsDone": "Step-1 amendment (13:14Z) already re-probed onOpenBoard arity at HEAD 6a642a80: task-14e3b36a premise retired; producers task-9bd8f529 (DoD amendment pending, now PLANNING under architect-a69740e0) and task-6646f888 (WORKING) still unlanded. Fresh-HEAD probes and current answers are in that amendment text.",
      "whatRemains": "Finish the plan: sequence this consumer behind task-9bd8f529 and task-6646f888, then wire live navigation + plan approval per the amended step-1 gate.",
      "pitfalls": "Do not plan against the original step-1 text — the 13:14Z amendment replaced it. Producers are moving; re-probe at your HEAD before naming premises.",
      "openQuestions": "Whether to hold submission until task-9bd8f529's DoD amendment lands, or plan with an explicit wait-gate step.",
      "releasedAt": "2026-08-29T16:01:17.998Z",
      "releasedBy": "governor-0af24e35",
      "reason": "Human-directed unblock: architect-fdbab575 silent 2.75h on this PLANNING hold, no reply to the 15:42Z presence ping, register lists the seat dead (8695s+). Releasing the assignment in place so a live architect can claim; not a crash verdict on the seat — if fdbab575 returns, re-claim normally."
    },
    {
      "whatIsDone": "Re-probed onOpenBoard arity at HEAD 6a642a80 per the governor's 13:10Z unpark instruction; ZERO bytes authored. step-1 amended (amend-1) with the fresh measurement and a three-way decision tree, so the next holder measures rather than re-derives. Reported to #architects as msg-2c66d45f7b57444a841eca52f03699e8. FINDING: the unpark is HALF RIGHT. The premise it cited HAS expired — task-14e3b36a is DONE and its disabled-Open control is in HEAD at goal-card.tsx:95 (`onClick={canOpenBoard ? onOpenBoard : undefined}`), covered at goal-card.test.tsx:148. Do not re-block on that. But TWO other prerequisites are still absent.",
      "whatRemains": "Re-run step-1 amend-1's probes at YOUR head and follow its decision tree. (a) onOpenBoard is STILL TWO-ARG — goals-home.tsx:47 and live-goals.tsx:45 both `(goalId: string, title: string) => void`; the three-arg widening is task-9bd8f5298c6a4d0cac47b92e62a97423's deliverable, PLANNING but NOT landed. (b) The approval seam task-6646f888 is likewise PLANNING, not landed. If BOTH are still absent, report blocked naming both with probe output. If (a) has landed and (b) has not, steps 3-4 (nav-rail, cordum-shell, live-work-board) may be reachable while steps 5-6 are not — report that split rather than planning around the gap. If both landed, proceed to step 2 unchanged.",
      "pitfalls": "PROBE THE BARE SYMBOL, never `onOpenBoard(` with a paren — the paren form is blind to the three `onOpenBoard={openBoard}` consumers at cordum-app.tsx:316/:319/:335, which are the ONLY ones that red the typecheck when the arity moves. That exact blindness deadlocked this row against its own child once already. DO NOT enable DEV_PAYLOADS['approval.decide'] — it is fixture-shaped, and dispatching on it fabricates approval authority. DO NOT route on a planningRunRef that does not exist: until the widening lands, a non-seed goal reaching the board carries none. Both gaps are the invented-authority defect this epic exists to remove and NEITHER is recoverable by revert — a UI that has dispatched has already acted, so this is safety ordering, not paperwork. OWNERSHIP unchanged: live-goals.tsx belongs to CRITICAL task-115fedb7; cordum-app.tsx is contested — re-measure porcelain immediately before editing either, and never adopt untracked live-command-dispatch.ts without provenance.",
      "openQuestions": "None blocking. The chain is task-9bd8f529 then task-6646f888 then this row, and BOTH are now PLANNING rather than BLOCKED — ordinary sequencing, not a stall. task-9bd8f529's own gate is now satisfied (task-14e3b36a's bytes landed), so it is genuinely ready for a worker and clearing it is the highest-leverage move for this chain.",
      "releasedAt": "2026-08-29T16:00:36.867Z",
      "releasedBy": "architect-fdbab575",
      "reason": "Architect work on this claim is COMPLETE: the governor's 13:10Z instruction was to re-probe onOpenBoard arity before planning, I did, and step 1 is amended (amend-1) with the fresh-HEAD measurement. Releasing in place so the row stays claimable while its two prerequisites land, rather than holding a seat on a row that cannot proceed or re-blocking one the governor just unparked. Not idleness and not a crash — an explicit handoff with the measurement recorded."
    },
    {
      "whatIsDone": "Step 1 remeasured read-only at d7a84177; zero bytes authored. Comment-c91849044d97481eaff8b3edc8980f06 records all gates. Nav WIP provenance remains byte-identical; task-2eb6 is DONE.",
      "whatRemains": "Plan/land prerequisite leaf task-14e3b36a, then re-run step-1 exact code gate before any consumer edit.",
      "pitfalls": "Do not absorb the leaf paths: three-arg onOpenBoard and disabled Open are absent, and route-first leaves non-seed goals without planningRunRef. Approval seam task-6646f888 remains BLOCKED, so never dispatch/fabricate authority.",
      "openQuestions": "When will task-14e3b36a be planned and delivered?",
      "releasedAt": "2026-08-28T12:27:29.547Z",
      "releasedBy": "worker-4bba4fa4",
      "reason": "Explicit user handoff: claim another executable WORKING task while this prerequisite remains absent."
    },
    {
      "whatIsDone": "Step 1 completed read-only at HEAD de458f8f; nav-rail WIP provenance and 108-file/1397-test baseline recorded in comment-bf1a0499def04dfe88caf9466c23fc7f. No step-2 bytes changed.",
      "whatRemains": "Architect must replan/split GoalCard disabled-open behavior, include exact callback consumer tests, resolve live-goals ownership, and keep route extraction/final pathspec within <=10.",
      "pitfalls": "CRITICAL task-115fedb owns live-goals.tsx. Do not no-op an enabled Open button; GoalCard lacks a disabled/reason prop. See comment-b2770299f06f49698c02039dc46db83a.",
      "releasedAt": "2026-08-28T09:44:41.615Z",
      "releasedBy": "worker-ce9da376",
      "reason": "Explicit handoff requested by the human: continue to an executable next task rather than wait on this replan."
    },
    {
      "whatIsDone": "Measured HEAD e59fa4d, exact target porcelain, current Cordum route composition, nav rail behavior, affordance/read shapes, generated builder, and production approval.decide handler. Proved both the CordumApp ownership collision and the absent authoritative approval payload seam. Recorded comment-9810f791382545dbbc2bee750d0b883d and reported BLOCKED.",
      "whatRemains": "After P2.15c lands/reassigns CordumApp ownership and a daemon-owned approval intent/composition seam exists, remeasure and plan typed route state plus affordance-gated approval/refusal/refresh UI.",
      "pitfalls": "Do not enable DEV_PAYLOADS['approval.decide']; it is explicitly fixture-shaped. Do not overwrite dirty CordumApp paths reserved by task-7e605144. Do not adopt untracked live-command-dispatch.ts without provenance.",
      "openQuestions": "Which server endpoint or command contract will supply/derive the approval record and activation witness without caller-shaped HUMAN authority?",
      "releasedAt": "2026-08-25T12:35:16.924Z",
      "releasedBy": "architect-be964894",
      "reason": "Explicit handoff after verified authority/ownership block so I can continue to a genuinely executable PLANNING row as requested."
    }
  ],
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T16:39:59.635Z",
  "planningNotes": {
    "approachesConsidered": "(1) THE COLLISION WITH THIS ROW'S OWN CHILD, and the ordering it forces. task-9bd8f529 (AWAITING_APPROVAL, approved plan) widens `onOpenBoard` to three args and its roster includes `cordum-app.tsx`; this row also touches `cordum-app.tsx` in steps 5-7 and `cordum-app.test.tsx` in step 2. The two rows touch that file for DIFFERENT reasons — the child changes `openBoard`'s SIGNATURE, this row wires NAVIGATION and PLAN APPROVAL — which is still a collision, because pathspec commits are FILE-granular while the conflict is HUNK-granular and whoever commits second sweeps the other's hunks. RULED CHILD-FIRST: the child consumes nothing from here and produces the signature this row's `openBoard` must match, so a parent editing ahead of it would be swept or would force the child to rebase onto bytes it does not own. I made this ruling as the parent's architect, which is the seat that can make it. (2) BLOCKING THE WHOLE ROW behind the child — REJECTED, and this is the plan's main shape decision. `nav-rail.tsx`, `cordum-shell.tsx`, `live-work-board.tsx` and `approve-plan.tsx` do NOT reference `onOpenBoard` and are untouched by the child, so steps 2-4 are reachable today. Only the `cordum-app.tsx` wiring waits, and step 1(b)'s gate is scoped to steps 5-7 rather than to the row. Parking four deliverable steps behind an unrelated signature change would be caution, not correctness. (3) RELEASING `cordum-app.tsx` from this row's roster to end the collision — REJECTED: this row genuinely needs it for the shell wiring (DoD-2 end to end), so releasing it would make the row unbuildable. Sequencing solves it; giving up the file does not. (4) Inventing a local authorization so the approve button works — REFUSED by DoD-1 (\"block rather than collide or fabricate an approval route\") and task rail 3 (\"do not treat a caller-shaped HUMAN flag or enabled button as approval authority\"). Step 1(e) measures the production affordance FIRST and step 5 blocks naming the missing producer if there is none. D1 and D2 drill the two doors separately, because a guard can read the affordance and still accept a caller flag as a second entrance. (5) Asserting \"clicking does nothing\" for unavailable destinations — REJECTED: an INERT control satisfies that while telling the operator nothing. DoD-2 says explicitly disabled, and D3 exists because the naive arm passes the wrong implementation. (6) Re-implementing the disabled-Open behaviour — FORBIDDEN: task-14e3b36a landed it (goal-card.test.tsx:148) and the child owns the runless-card rule; a third surface would compete with both. Verification: `pnpm --filter @moe/control-room test`.",
    "codebaseInsights": "Measured by architect-a69740e0 at HEAD 595c32c2, HEAD-anchored.\n\nTHE ARITY RE-PROBE the unpark demanded: `onOpenBoard` is still TWO arguments at both declaring sites — `goals-home.tsx:47` and `live-goals.tsx:45`, each `(goalId: string, title: string) => void`. `goals-home.tsx` calls it at :86 (`onOpenBoard(strip.openGoalId, titleOf(strip.openGoalId))`) and passes it at :205. This row's three consumers are in `cordum-app.tsx` at :316, :319 and :335, all spelled `onOpenBoard={openBoard}` — the JSX prop-passing form, which an `onOpenBoard(` grep is BLIND to. Probe without the paren.\n\nTHE CONTESTED FILE: `task-9bd8f5298c6a4d0cac47b92e62a97423` (AWAITING_APPROVAL, approved 7-step plan) owns `cordum-app.tsx` for the signature widening. Its approved roster is six paths and does NOT include `cordum-app.test.tsx`. This row's plan touches `cordum-app.tsx` at steps 5, 6, 7 and `cordum-app.test.tsx` at step 2. Note for anyone reading the governor's 16:03Z amendment ruling on the child: its clause 3 keeps `cordum-app.test.tsx` in the CHILD's roster at seven paths, while the child's approved plan omits it and this row claims it — the child's plan as approved is the safe reading, and I recorded that on the child at comment-8747d052.\n\nUNCONTESTED BY THE CHILD, hence reachable today: `nav-rail.tsx`, `cordum-shell.tsx`, `live-work-board.tsx`, `approve-plan.tsx` and `approve-plan.test.tsx` contain no `onOpenBoard` reference.\n\nCOMPOSABLE PRECEDENT: task-14e3b36a is DONE (landed 9c83dcdd) and `goal-card.test.tsx:148` covers the disabled Open control — that is the disabled-destination treatment to compose rather than re-derive.\n\nSTILL TO MEASURE AT CLAIM (step 1(e)): the production plan-approval affordance read and the typed command. DoD-3 gates the command on the server affordance and task rail 3 forbids substituting a caller-shaped HUMAN flag or an enabled button; if no production affordance exists, step 5 blocks naming the missing producer rather than inventing a route.",
    "risks": "(1) EDITING `cordum-app.tsx` BEFORE THE CHILD LANDS is the concrete way this row damages another: file-granular pathspec commits mean the second committer sweeps the first's hunks, even though the two changes are in different parts of the file. Step 1(b)'s gate is scoped to steps 5-7 so the rest of the row still moves. (2) FABRICATING AN APPROVAL ROUTE is the security failure this row must not commit. An enabled button is a CONSEQUENCE of authorization, never its source, and a caller-shaped HUMAN flag is the named substitution (task rail 3). D1 and D2 drill the affordance door and the flag door SEPARATELY, because a guard can read the affordance correctly and still accept the flag as a second entrance. If no production affordance exists, step 5 blocks — inventing one would make the UI authorize itself. (3) AN INERT CONTROL PASSES A NAIVE ARM: \"clicking does not navigate\" is satisfied by a button that silently does nothing, which is what DoD-2's \"explicitly disabled rather than inert\" exists to forbid. D3 catches it. (4) A LITERAL ROUTE ID beside an assertion is a fixed point D4 could never red — DoD-4's \"no fixed goal/run ids\" applies to the arms as much as to production, so expectations read from the fixture object. (5) CLEARING OR GENERICISING A REFUSAL loses the exact code and layer, which is the only thing that makes the failure actionable for the operator; D5 guards it. (6) RE-IMPLEMENTING the disabled-Open behaviour (task-14e3b36a's) or the runless-card rule (the child's) creates a third competing surface. (7) THE CHILD'S SIGNATURE IS THE CHILD'S CONTRACT — step 6 says to READ what landed rather than assume three arguments in a particular order. (8) Two stale architect seats hold tasks on this board; re-measure ownership at claim rather than trusting a scan taken minutes earlier.",
    "keyFiles": [
      "apps/control-room/src/v2/cordum-app.tsx",
      "apps/control-room/src/v2/goals/goals-home.tsx",
      "apps/control-room/src/v2/goals/live-goals.tsx",
      "apps/control-room/src/v2/shell/nav-rail.tsx",
      "apps/control-room/src/v2/shell/cordum-shell.tsx",
      "apps/control-room/src/v2/goals/approve-plan.tsx",
      "apps/control-room/src/v2/goals/live-work-board.tsx"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-28T09:34:00.922Z",
    "escalatedAt": "2026-08-28T09:34:00.922Z"
  },
  "planApprovedAt": "2026-08-29T16:40:01.792Z",
  "workStartedAt": "2026-08-28T09:35:29.893Z",
  "blockedOnTaskIds": [
    "task-9bd8f5298c6a4d0cac47b92e62a97423"
  ]
}