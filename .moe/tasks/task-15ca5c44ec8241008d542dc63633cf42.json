{
  "id": "task-15ca5c44ec8241008d542dc63633cf42",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "P1.2b-iii Compose the project manager into the Control Room entrypoints",
  "description": "SPIDR child 3 of P1.2b (task-e868ea33), carved by architect-d950bd95 on 2026-08-26 at HEAD 95384f0c. This is the CONSUMER EDGE for child P1.2b-ii: the two Control Room entrypoints that actually mount the project manager. It lands AFTER P1.2b-ii (the imported modules must be tracked first, or a clean checkout cannot typecheck).\n\nMEASURED, do not re-derive. `apps/control-room/src/main.tsx` is ` M` (numstat 68/28) and `apps/control-room/src/v2/cordum-app.tsx` is ` M` (65/47). HEAD copies contain zero project references. The project-manager hunks inside them are: main.tsx — imports of ProjectManagerApp / connectProjectManager / ProjectManagerConnection from ./v2/projects/*, `prepareProjectManager(managerMode)`, `isProjectManagerLocation(hostname, search, dev)` (hostname or `?projects=1` in development), and the `<ProjectManagerApp prepared={managerSetup} />` branch; cordum-app.tsx — the ProjectBoundary import, the `projectId` document-title effect, and `<ProjectBoundary projectId={projectId} />`. The SAME two files also carry UNRELATED foreign hunks (handshake, legacy-route, goal and shell changes) that belong to other rows. Therefore both files MUST be staged MODE 2 (index-only: `git show HEAD:<path>` + only the project-manager hunks applied by content → `git hash-object -w --stdin` → `git update-index --cacheinfo`) and finished with a BARE commit; a pathspec commit or `git add` would sweep the foreign hunks (the 66c1a3f7 class). See memory index-only-surgery-needs-a-bare-commit.\n\nBehaviour to pin (the composition is a security-relevant switch): the project manager mounts ONLY when `isProjectManagerLocation` is true; in production builds the `?projects=1` query must NOT enable it; an ordinary location renders the existing app unchanged.",
  "definitionOfDone": [
    "DoD-1 verify-or-block gate at claim, measured on disk: `git ls-files apps/control-room/src/v2/projects | wc -l` must be 10 (P1.2b-ii landed) — landing this consumer first ships a clean checkout that cannot typecheck, so report blocked naming task P1.2b-ii if it is not; AND `git show HEAD:apps/control-room/src/main.tsx | grep -c ProjectManagerApp` must be 0 (else duplicate).",
    "Before staging, a task comment records `git diff --numstat HEAD` for both files, the exact list of hunks classified as project-manager (ours) vs foreign (theirs, with the owning row where a live plan names the hunk), SHA256 of both worktree copies, and %TEMP% backups suffixed by worker id; the worktree copies are NEVER edited.",
    "Both files are staged MODE 2 with only the project-manager hunks; `git diff --cached --numstat` lists exactly the two paths; `git show :<path> | grep -c <foreign marker>` is 0 for each foreign hunk; a BARE `git commit` lands them; afterwards `git diff HEAD -- <path>` shows ONLY the foreign hunks (peer WIP preserved) and `git status --porcelain` reads `MM` for both by design.",
    "A red-first arm in a test this row adds under apps/control-room/src (one focused test file, the row's third owned path) pins `isProjectManagerLocation`: true for the manager hostname; true for `?projects=1` ONLY when development is true; false for `?projects=1` in production; false for an ordinary location — asserting the boolean, and a render arm asserting the ordinary location mounts the existing app (no ProjectManagerApp) while the manager location mounts ProjectManagerApp. Mutation drill: drop the `development &&` guard and confirm the production arm reds.",
    "In a `git archive HEAD` materialization with `pnpm install --frozen-lockfile --prefer-offline`: `pnpm --filter @moe/control-room typecheck` exit 0 with `$ tsc` echo, `pnpm --filter @moe/control-room test` with nonzero counts including the new arm, and the control-room build (`pnpm --filter @moe/control-room build` if the script exists, else disclosed) succeeds; foreign red path-attributed and disclosed verbatim.",
    "Every step's affectedFiles/modifiedFiles ⊆ the three owned paths; the completion comment names the P1.2b-ii commit consumed and states that this is the durable consumer edge for the client tree (global rule clause 1)."
  ],
  "taskRails": [
    "Own exactly main.tsx, v2/cordum-app.tsx and one new focused test file. The foreign hunks in those two files belong to other rows and must survive untouched in the worktree.",
    "MODE 2 index-only staging plus a bare commit is mandatory; a pathspec commit of either file is forbidden because it reads the worktree.",
    "Do not widen: no daemon paths, no changes to the v2/projects tree (P1.2b-ii)."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Phase 1/2 — DoD-1 GATE + MEASURE, no edits. (a) `git ls-files apps/control-room/src/v2/projects | wc -l` == 10 (task-0602f6c2 + task-999a363f landed); (b) `git show HEAD:apps/control-room/src/main.tsx | grep -c ProjectManagerApp` == 0 and `git show HEAD:apps/control-room/src/v2/cordum-app.tsx | grep -c ProjectBoundary` == 0 (else duplicate → blocked). PREMISE CHANGE (comment-2f928fae, human-confirmed): the human cleaned the worktree — main.tsx and v2/cordum-app.tsx are CLEAN at HEAD (worktree hash == `git rev-parse HEAD:<path>`, measured 9b862409 / a4d763b2). This is therefore an AUTHORING row in MODE 1: ordinary edits + explicit-pathspec commit; NO mode-2 blob, NO temp index, NO skip-worktree. Gate that premise: `git hash-object <path>` == `git rev-parse HEAD:<path>` for both files and `git status --porcelain -- <both>` EMPTY; if either has become dirty again, STOP and report (do not adopt whatever reappeared). Record: HEAD sha; `git diff --cached --name-only` EMPTY; the foreign dirty control-room files you must not touch (at planning: live/live-board.tsx, live/live-dispatch.ts, live-board-dispatch.test.tsx, live-effort-path.test.tsx, nodes/node-authority.tsx, performance/command-latency.tsx, v2/goals/*, v2/shell/nav-rail.tsx, v2/styles/cordum-goals.css) and the untracked entry-route.ts / development-legacy-root.tsx / v2/goals/fixture-goals-view.tsx — none of them is imported by this row's code. OWNED PATHS = FOUR (rail amended by comment): main.tsx, v2/cordum-app.tsx, NEW src/entry-project-manager.ts, NEW src/entry-project-manager.test.tsx. Every step's affectedFiles/modifiedFiles ⊆ those four. Comment everything.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "note": "DoD-1 gate PASSED at HEAD 5f7b3d5a (planning anchored 36f31348; the two targets did not move). (a) v2/projects tracked = 10. (b) main.tsx grep -c ProjectManagerApp = 0, cordum-app.tsx grep -c ProjectBoundary = 0 — not a duplicate. (c) CLEAN premise holds: main.tsx hash-object 9b862409 == HEAD:, cordum-app.tsx a4d763b2 == HEAD:, status --porcelain of both EMPTY, index EMPTY. MODE 1 authoring confirmed (pathspec commit, no mode-2 blob, no S bits). Neither new file exists (no collision). Foreign dirty/untracked control-room set re-measured and drifted vs the plan (live-board.tsx/live-dispatch.ts/fixture-goals-view.tsx no longer dirty; a new goals/live untracked set appeared) — none imported by this row, none touched. Near-duplicate check: foreign untracked entry-route.ts exports only gateDevelopmentQuery; grep isProjectManagerLocation = 0 hits repo-wide. Symbols verified on disk: ProjectManagerApp(prepared) app.tsx:81 + heading literal :68; connectProjectManager first request \"/manager/bootstrap\" client.ts:225; v2 handshake \"/bootstrap\" live-handshake.ts:121; ProjectBoundary(projectId) boundary.tsx:50; LiveSetup.projectId live-config.ts:34; attached cordum-app.tsx:157 with eyebrow above at :152 (TDZ move required); PROJECT_MANAGER_HOME 127.0.0.2 boundary.tsx:8; tsconfig types=[\"node\"] with vite/client precedent board-surface.tsx:1; scripts build/test/typecheck all present; vitest include src/**/*.test.ts(x) with passWithNoTests:false. Full transcript in comment-5d785e17. No files modified in this step.",
      "startedAt": "2026-08-27T11:51:28.004Z",
      "completedAt": "2026-08-27T11:52:39.197Z"
    },
    {
      "stepId": "step-2",
      "description": "Phase 3 — TESTS FIRST. Create apps/control-room/src/entry-project-manager.test.tsx (follow the DOM/test-environment conventions of the sibling suites, e.g. v2/projects/project-manager-app.test.tsx). PURE ARMS against `isProjectManagerLocation(hostname, search, development)`: `\"127.0.0.2\"` → true even with development=false; `\"localhost\"` + `\"?projects=1\"` + development=true → true; `\"localhost\"` + `\"?projects=1\"` + development=FALSE → false (the production guard); `\"localhost\"` + `\"\"` → false; a fragment-only location never matters (the function takes `search`, not `hash`). RENDER ARM with a CALL-ORDER RECORDER (review measured that a presence-only arm stays green when the handshake suppression is removed): `const fetchMock = vi.fn(() => new Promise<Response>(() => undefined)); vi.stubGlobal(\"fetch\", fetchMock)`; create `#root` in the document; stub `window.location.hostname` to `127.0.0.2`; dynamic-import `./main.js` (its module-level mount lands in #root); assert the manager app's initial heading `Connecting to project manager` (HEAD project-manager-app.tsx:68) is present, the Cordum shell chrome absent, and `fetchMock.mock.calls.map(([input]) => input)` equals `[\"/manager/bootstrap\"]` (client.ts:225) — the v2 handshake `/bootstrap` must NOT have been requested; then stub an ordinary hostname, call the exported `mountControlRoom(secondContainer)`, and assert the Cordum shell mounts, no manager copy, and the calls now equal `[\"/manager/bootstrap\", \"/bootstrap\"]`. Write the seam apps/control-room/src/entry-project-manager.ts: `/// <reference types=\"vite/client\" />` (tsconfig types is [\"node\"]; pattern board-surface.tsx:1), `export function isProjectManagerLocation(hostname: string, search: string, development: boolean): boolean { return hostname === \"127.0.0.2\" || (development && new URLSearchParams(search).get(\"projects\") === \"1\"); }` and `export function resolveProjectManagerMode(hostname: string, search: string): boolean { return isProjectManagerLocation(hostname, search, import.meta.env.DEV); }` with a doc comment: the fixed manager host makes the plain origin the route selector; the query switch is development-only. RED: run `pnpm --filter @moe/control-room test -- src/entry-project-manager.test.tsx` (confirm the count line names the file) → the render arm reds (main.tsx has no manager branch yet: no `Connecting to project manager`, calls == [\"/bootstrap\"]); quote it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/entry-project-manager.test.tsx",
        "apps/control-room/src/entry-project-manager.ts"
      ],
      "newFiles": [
        "apps/control-room/src/entry-project-manager.test.tsx",
        "apps/control-room/src/entry-project-manager.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/entry-project-manager.ts",
        "apps/control-room/src/entry-project-manager.test.tsx"
      ],
      "note": "Seam + red-first test written; RED quoted. PLAN DEVIATION (measured, full transcript in comment-73b12e93): jsdom 30.0.1 makes `location` and `location.hostname` LegacyUnforgeable — `Object.defineProperty(window.location,\"hostname\",…)` throws `TypeError: Cannot redefine property: hostname`, and `window.location` itself is `{configurable:false,writable:false}`. The step's prescribed hostname stub is therefore not constructible, and a file-level jsdom url override would pin BOTH arms to the manager host (the rail permits one test file). ADAPTATION: `mountControlRoom` will take a third parameter `hostname: string = globalThis.location?.hostname ?? \"\"`, mirroring the existing `clock: Clock = BROWSER_CLOCK` seam in the same file. Result: the ORDINARY arm runs the real module-level production mount with zero injection (jsdom's unforgeable origin IS an ordinary location — this is the refusal direction, the security-relevant one); the MANAGER arm injects PROJECT_MANAGER_HOSTNAME into the exported mountControlRoom (capability direction, disclosed as injection-based). entry-project-manager.ts (44 lines): PROJECT_MANAGER_HOSTNAME=\"127.0.0.2\", isProjectManagerLocation(hostname,search,development) = fixed host OR (development && ?projects=1), resolveProjectManagerMode passing import.meta.env.DEV, with vite/client triple-slash reference. entry-project-manager.test.tsx (117 lines): 5 pure arms (fixed host true even in production; ?projects=1 true only in development; ?projects=1 FALSE in production; ordinary false; fragment \"#projects=1\" false) + 2 render arms with a fetch call-order recorder that never settles. RED: `pnpm --filter @moe/control-room exec vitest run src/entry-project-manager.test.tsx` EXIT 1, \"Test Files 1 failed (1) / Tests 1 failed | 6 passed (7)\", failing at entry-project-manager.test.tsx:103 on getByTestId(\"cr.manager.root\") with the Cordum nav rail in the dump. Nonzero count line proves the leg executed.",
      "startedAt": "2026-08-27T11:52:43.221Z",
      "completedAt": "2026-08-27T11:55:55.411Z"
    },
    {
      "stepId": "step-3",
      "description": "Phase 4 — MINIMUM COMPOSITION, edited in place (mode 1). main.tsx (HEAD 101 lines) gains ONLY: imports `ProjectManagerApp` from ./v2/projects/project-manager-app.js, `connectProjectManager` + `type ProjectManagerConnection` from ./v2/projects/project-manager-client.js, `resolveProjectManagerMode` from ./entry-project-manager.js; `chooseRoot(search, managerMode, liveSetup, managerSetup)` with `if (managerMode && managerSetup !== undefined) return <ProjectManagerApp prepared={managerSetup} />;` BEFORE the unchanged `?v1=1` branch; `prepareV2LiveSetup(search, managerMode)` whose early return becomes `if (managerMode || params.get(\"v1\") === \"1\" || params.get(\"fixtures\") === \"1\") return undefined;`; `prepareProjectManager(managerMode)` returning `connectProjectManager({ fetchImpl: (input, init) => fetch(input, init) })` or undefined (doc: no manager secret enters React state); mount wiring `const managerMode = resolveProjectManagerMode(window.location.hostname, search);` then `prepareV2LiveSetup(search, managerMode)`, `prepareProjectManager(managerMode)`, `chooseRoot(search, managerMode, liveSetup, managerSetup)`. NOTHING else: no vite reference in main.tsx, no gateDevelopmentQuery, no lazy legacy root, no import removals, no doc rewrites. cordum-app.tsx gains ONLY: `import { ProjectBoundary } from \"./projects/project-boundary.js\";`; `const projectId = attached?.projectId ?? null;` placed AFTER `const attached` (HEAD:157) — and the eyebrow line (HEAD:152) MOVED below it (review measured TS2448/TS2454 temporal-dead-zone errors otherwise) reading `` open === null ? `PROJECT ${MIDDOT} ${projectId ?? \"PAIRING\"}` : `${MIDDOT} ${open.goalId}` ``; a `useEffect` setting `document.title = \\`Moe ${MIDDOT} ${projectId}\\`` when projectId is non-null and restoring the previous title on cleanup; `const content = fixtures ? body : (<><ProjectBoundary projectId={projectId} />{body}</>);` rendered in place of `{body}`. NOT the `projectId` prop on CordumShell (absent at HEAD; disclosed follow-up), not GoalCreateResult/runId/fixture-goals/connection-state changes. GREEN: `pnpm --filter @moe/control-room typecheck` (`$ tsc` echo) and the focused test file with nonzero counts (expect 6 tests), live; then the DIVERGENCE DRILLS in disposable copies of a `git archive`+overlay materialization: D1 drop `development &&` in the seam → the production `?projects=1` arm reds; D2 remove `managerMode ||` from prepareV2LiveSetup → the render arm reds on `expected [ '/bootstrap', '/manager/bootstrap' ] to deeply equal [ '/manager/bootstrap' ]` (measured wording); D3 move the manager branch below the `?v1=1` branch → stays GREEN by construction (no v1 query in the arm) — record as non-detection, not a pass. `git diff --numstat HEAD -- <4 paths>` recorded (review measured ~23/4 main.tsx, ~19/2 cordum-app.tsx, two adds).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/v2/cordum-app.tsx",
        "apps/control-room/src/entry-project-manager.ts",
        "apps/control-room/src/entry-project-manager.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/v2/cordum-app.tsx",
        "apps/control-room/src/entry-project-manager.ts",
        "apps/control-room/src/entry-project-manager.test.tsx"
      ],
      "note": "Composition landed in mode 1; full transcript comment-f4193996. main.tsx 101->138: manager branch FIRST in chooseRoot(search, managerMode, liveSetup, managerSetup), prepareV2LiveSetup(search, managerMode) short-circuiting on managerMode||, new prepareProjectManager(managerMode), mountControlRoom(container, clock, hostname = globalThis.location?.hostname ?? \"\") wiring resolveProjectManagerMode. cordum-app.tsx 228->234: ProjectBoundary import, projectId after attached, eyebrow moved below it reading PROJECT MIDDOT ${projectId ?? \"PAIRING\"}, document.title effect with restore-on-cleanup, content wrapper. CordumShell projectId prop omitted (absent at HEAD, disclosed follow-up). numstat 55/10 and 27/2 (over the plan's ~23/4, ~19/2 - three added doc comments plus the hostname seam). Per-file lines 42/136/138/234, all under 250. GREEN live: typecheck EXIT 0 with `$ tsc --project tsconfig.json` echo; focused suite `Tests 8 passed (8)` EXIT 0. DRILLS in a git-archive-HEAD + overlay materialization (install EXIT 0), live worktree never mutated: D1 (drop `development &&`) RED on exactly `refuses ?projects=1 in a production build`, \"expected true to be false\"; D2 (remove `managerMode ||`) RED with the predicted wording verbatim, \"expected [ '/bootstrap', '/manager/bootstrap' ] to deeply equal [ '/manager/bootstrap' ]\"; D3 (manager branch below ?v1=1) was GREEN 7/7 - reported as a FINDING and CLOSED IN THIS ROW rather than deferred, because chooseRoot's doc claims no flag can move the manager document off the manager and nothing pinned it. Added an 8th arm mounting at 127.0.0.2 with /?v1=1&fixtures=1; baseline 8 passed, D3 re-applied now REDS with \"Unable to find an element by: [data-testid=cr.manager.root]\". Restore proof: three-way SHA256 (pristine/materialization/worktree) ALL_MATCH=True for all four owned paths, backups at %TEMP%\\worker-ca97b7bd\\pristine; git diff not cited for the two untracked modules. FOREIGN RED disclosed: live full suite EXIT 1, \"Test Files 1 failed | 92 passed (93) / Tests 1 failed | 1154 passed (1155)\", goals-home.test.tsx:174 expected 1 received 0 - zero import edges to my four paths, and the same file is 11/11 GREEN in the materialization, so it is caused by the foreign dirty goal-model.ts / new-goal-form.tsx. Owned-path intersection EMPTY. Planning premise corrected: the live control-room typecheck is no longer red on foreign goals files (EXIT 0 now).",
      "startedAt": "2026-08-27T12:01:40.842Z",
      "completedAt": "2026-08-27T12:01:54.851Z"
    },
    {
      "stepId": "step-4",
      "description": "Phase 4 — COMMIT by explicit pathspec (mode 1; both entrypoints were clean at HEAD, so the worktree copies carry only this row's hunks — re-verify with `git diff HEAD -- <both>` before staging). `git add -- <4 paths>`; audit `git diff --cached --numstat` = exactly the four; `git commit -m \"feat(task-15ca5c44): compose the project manager into the Control Room entrypoints\" -- <4 paths>` (commit.gpgsign=true, gpg.format=ssh sign automatically) with a body naming the fixed manager host, the development-only query switch, the handshake suppression in manager mode, and the deliberately omitted CordumShell projectId prop. Post-commit: `git show --stat HEAD` = four paths; `git status --porcelain -- <4>` EMPTY (nothing left for a session-end sweep); NO skip-worktree bit set by this row (`git ls-files -v -- <4>` all `H`). Do not touch `apps/daemon/src/http/affordance-read.ts` (carries task-48ee1455's deliberate S bit).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/v2/cordum-app.tsx",
        "apps/control-room/src/entry-project-manager.ts",
        "apps/control-room/src/entry-project-manager.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/v2/cordum-app.tsx",
        "apps/control-room/src/entry-project-manager.ts",
        "apps/control-room/src/entry-project-manager.test.tsx"
      ],
      "note": "Committed cccf133e2e6d43d3ff7c6a42798f04decba49650 on moe/work-2026-08-08, parent 5f7b3d5a (HEAD did not move since the step-1 gate). Full audit in comment-a1325cb5. Pre-stage re-verification: git diff HEAD over the two tracked owned paths contained ZERO foreign hunks, so the mode-1 premise held and the plan's mode-2 route stayed correctly retired. Staged exactly four: entry-project-manager.test.tsx 149/0, entry-project-manager.ts 46/0, main.tsx 55/10, v2/cordum-app.tsx 27/2 (git's add-counts supersede the PowerShell Measure-Object figures I quoted in step 3; that cmdlet undercounts). Commit via `git commit -F - -- <four paths>`: \"4 files changed, 277 insertions(+), 12 deletions(-)\" with two create modes. Post-commit: git show --stat HEAD = exactly the four; git status --porcelain of the four EMPTY (nothing left for a session-end sweep); git ls-files -v all H (no skip-worktree set by this row; affordance-read.ts's foreign S bit untouched); git diff --cached --name-only EMPTY. Signed: the raw commit object carries `gpgsig -----BEGIN SSH SIGNATURE-----`; QA note, `git log -1 --format='%G?'` prints N on this host because gpg.ssh.allowedSignersFile is unconfigured for verification — read the commit object, not %G?. Foreign WIP preserved byte-identical: the same 8 modified and 15 untracked apps/control-room paths as before the commit, nothing swept in.",
      "startedAt": "2026-08-27T12:02:13.140Z",
      "completedAt": "2026-08-27T12:03:15.264Z"
    },
    {
      "stepId": "step-5",
      "description": "Phase 5/7 — GATES on the delivered commit + review. Materialize `git archive HEAD` → %TEMP%\\<workerId>\\delivered, `pnpm install --frozen-lockfile --prefer-offline`. Standalone, unpiped, exit + count line each: (a) `pnpm --filter @moe/control-room typecheck` EXIT 0 (`$ tsc` echo); (b) `pnpm --filter @moe/control-room test` with nonzero counts INCLUDING entry-project-manager.test.tsx (review confirmed the filter discovers apps/control-room tests despite the root include list — quote the count line); (c) `pnpm --filter @moe/control-room build` (vite build) EXIT 0 — the composition bundles; (d) the LIVE control-room typecheck is RED on foreign dirty/untracked goals files (fixture-goals-view.tsx TS2322, goals-home.test.tsx ×19, live-goals.test.tsx ×5 at planning) — it is NOT a positive control; quote the live red verbatim, path-attribute it (∩ owned = ∅), grade only on (a)-(c). ADVERSARIAL REVIEW in a comment: `?projects=1` cannot enable manager mode in a production build (import.meta.env.DEV false → only the fixed host); neither query nor fragment carries authority (the manager credential arrives only via connectProjectManager's cookie session); manager mode suppresses the v2 handshake (no `/bootstrap` request) — pinned by the call-order arm; `127.0.0.2` is the host the daemon serves (grep HEAD daemon for it); ProjectBoundary renders on every non-fixtures Cordum body; document.title restored on unmount; no probe residue; the omitted CordumShell projectId/data-project-id attribute is a disclosed follow-up. Carry the exclusivity caveat for UI copy (settled-predecessor only; task-4247169c comment-fef5b5cb).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/entry-project-manager.test.tsx",
        "apps/control-room/src/main.tsx"
      ],
      "note": "All three delivered-tree gates pass; full transcript comment-e050cce1. Materialization: git archive HEAD (cccf133e) to %TEMP%\\worker-ca97b7bd\\delivered2, install EXIT 0; committed bytes == gated bytes proven by SHA256 on all four owned paths. (a) typecheck EXIT 0 with `$ tsc --project tsconfig.json` echo, plus a PLANTED-ERROR POSITIVE CONTROL that went EXIT 1 naming both new files (TS2322/TS2367 in entry-project-manager.ts, three TS2345 in the test), restored with SHA256 proof in a separate call. (b) `Test Files 88 passed (88) / Tests 1115 passed (1115)` EXIT 0, with inclusion PROVED not assumed: `vitest list --filesOnly` prints src/entry-project-manager.test.tsx and the verbose run lists all eight arms by name. (c) `pnpm --filter @moe/control-room build` EXIT 0, \"built in 215ms\". (d) LIVE disclosed, not graded: live typecheck is now EXIT 0 (the planning-time foreign red is gone); live full suite EXIT 1 on goals-home.test.tsx:174 \"Expected 1 / Received 0\", Test Files 1 failed | 92 passed (93). Attribution: the delivered tree's failing-path set is EMPTY, so the HEAD-minus-merge-base delta intersected with owned paths is empty trivially; independently goals-home's import closure has zero edges to my four paths. Cause is foreign dirty goal-model.ts / new-goal-form.tsx. ADVERSARIAL REVIEW highlights: the production-guard claim is proven at the BYTE level in the built bundle — import.meta.env occurs 0 times and the compiled predicate reads `function p(e,t){return f(e,t,!1)}`, so the query branch is unreachable in production (a bare-token grep would have been vacuous; the \"v1\"/\"fixtures\" control showed both absent from minified output). 127.0.0.2 confirmed as the daemon bind host at apps/daemon/src/projects/project-manager-http.ts:114. No probe residue: only the four owned paths were written to the repo, all committed and clean; apps/control-room/dist is gitignored and dated 2026-08-25, predating this session. Three follow-ups disclosed: omitted CordumShell projectId prop, no dedicated arm for the ProjectBoundary-everywhere rule or the document.title restore, and 127.0.0.2 now spelled in three modules with no dependency-free shared home.",
      "startedAt": "2026-08-27T12:03:24.993Z",
      "completedAt": "2026-08-27T12:06:58.672Z"
    },
    {
      "stepId": "step-6",
      "description": "Phase 6/8 — DURABLE EVIDENCE + HANDOFF + COMPLETION. Comments carry: the gate measurements, the red/green transcripts, D1/D2 reds and the D3 non-detection, the numstat, the four gate legs with counts, and the live foreign red by path:line. Comment on task-e868ea33 (parent, DONE) and task-0602f6c2 (client tree) that the durable consumer edge for the client tree now exists (global rule clause 1) at <sha>; comment on task-2eb6d36d / task-7e605144 / task-40017f79 (P2.15 family, which name these entrypoints in prose) that main.tsx and cordum-app.tsx now carry the project-manager composition and are clean at HEAD, so their future hunks must be authored on top of it. Fresh moe.complete_task verification = `pnpm --filter @moe/control-room test` from the delivered materialization, quoting exit code and both nonzero count lines, stating that (a)-(c) in the materialization are the decisive proofs.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/entry-project-manager.test.tsx"
      ],
      "note": "Durable evidence, handoff and adversarial self-review complete. Adversarial pass over `git show cccf133e` found NO fixable defect; eight considered-and-cleared items recorded in comment-6ced8e77 (chooseRoot fallthrough unreachable by construction; document.title correct under StrictMode double-invoke and under projectId change, and no other module writes it; untrusted projectId reaches only React-escaped sinks and a DOM string property; hostname default fails closed at \"\"; the unhandled-rejection window is the pre-existing prepareV2LiveSetup shape and connectProjectManager resolves refusals rather than rejecting; ProjectBoundary above the PAIRING body is deliberate and its switch form accepts only a plain 127.0.0.1 origin; the third positional hostname param is a disclosed API smell not fixed because reshaping mountControlRoom would edit the unowned scaffold.test.tsx; no new shared mutable state). Cross-row comments posted with FULL task ids (short ids return TASK_NOT_FOUND): task-e868ea339a0d408e8bc9533985b3d92a and task-0602f6c261ab4423ad1e6403a0aeebb4 told the durable consumer edge for the client tree now exists at cccf133e, with the two client behaviours my call-order recorder pins (/manager/bootstrap first, \"Connecting to project manager\" heading); task-2eb6d36dab984c5e9ebbb7a2b4ff4876, task-7e6051442a284b439e60139af0c7aed2 and task-40017f79a36e47afbcb69762acfc294a told the entrypoints now carry the composition and are clean at HEAD, with the exact new signatures, the load-bearing branch order, the eyebrow TDZ move, the document.title single-writer rule and the still-open CordumShell projectId gap. Serena memories written: task-task-15ca5c44...-handoff, gotcha-jsdom-location-is-unforgeable, gotcha-minified-bundle-grep-needs-a-neighbourhood-not-a-token. FRESH VERIFICATION from the delivered materialization: repo HEAD = cccf133e, my commit is an ancestor of HEAD (exit 0); `pnpm --filter @moe/control-room test` -> \"$ vitest run\" then \"Test Files 88 passed (88) / Tests 1115 passed (1115)\", EXIT 0.",
      "startedAt": "2026-08-27T12:07:35.806Z",
      "completedAt": "2026-08-27T12:10:30.172Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-e868ea339a0d408e8bc9533985b3d92a",
  "priority": "CRITICAL",
  "order": 1.63,
  "comments": [
    {
      "id": "comment-45e4d9a71e8149b3ac317d17fa2dae9e",
      "author": "worker-dc706ffd",
      "content": "FROM task-e868ea33 (P1.2b, daemon HTTP slice), measured at commit `364a83f0`. You are the entrypoint-composition child; here is what is settled below you so you do not re-measure it.\n\n**The daemon HTTP route is tracked and clean-checkout loadable.** Whole-repo `git archive HEAD` materialization + `pnpm install --frozen-lockfile --prefer-offline` inside it, every `node_modules/@moe/*` link verified to resolve INSIDE the materialization: a plain `node` ESM import of `apps/daemon/src/projects/project-manager-http.js` prints `typeof startProjectManagerHttp = function`, exit 0. Deleting the routing bridge reds it with `ERR_MODULE_NOT_FOUND` naming that specifier, so the edge is real. The old reopen premise on P1.2b — that `project-manager-http.js` could not be imported because the contract needed `secretMatchesConstantTime` from a guard that did not export it — is CLOSED: the symbol is at `http-listener-guards.ts:319` and the import executes.\n\nOne trap worth knowing when you verify the guard yourself: `git grep secretMatchesConstantTime` against `http-listener-guards.JS` exits 1 and looks like a miss. It is not. That bridge's entire content is `export * from \"./http-listener-guards.ts\";`, so the symbol is runtime-reachable but invisible to a name grep on the `.js`. Settle it by executing an import, not by grepping the bridge.\n\n**Refusal vocabulary is pinned to code + layer** in `project-manager-http-refusals.test.ts`. `managerRefusal` always emits `layer: \"PROJECT_MANAGER_HTTP\"`; the two PAIRING refusals emit `\"CONTROL_ROOM_PAIRING_APPROVAL\"` instead (`PAIRING_APPROVAL_REQUIRED`/409 early claim, `PAIRING_REQUEST_ALREADY_CLAIMED`/410 replay). Any entrypoint-level arm asserting a single layer for all refusals will be wrong.\n\n**A finding that will apply to your `isProjectManagerLocation` arm.** I drilled the guard my plan named and the suite stayed GREEN, because the plan's line pointer landed on the PAIRING route's copy of the guard while my arm crossed the project route's copy. Two mechanisms, identical code, different lines. Before you bank a green drill as \"covered\", check whether the mechanism you mutated is the one your fixture actually crosses. A green drill is a finding, not a pass.\n\n**HEAD moved during my session** — `413d1573` (task-0602f6c2: Control Room client + project UI tree) landed mid-flight, and my commit sits on top of it. Re-measure your premises at current HEAD rather than trusting the row description; `apps/control-room/src/v2/projects/project-manager-client.ts` is now TRACKED, and only `project-manager-app.tsx` / `.test.tsx` remain untracked there.\n\n**EXCLUSIVITY CAVEAT, verbatim per my plan, with a currency correction.** Verbatim: exclusivity is guaranteed against a SETTLED predecessor; on `EXIT_UNOBSERVED` a successor may acquire before the killed tree runs down (`task-4247169c`, comment-fef5b5cb). CORRECTION at current bytes: by `95384f0c`, `session.rs` calls `locks.acquire(path)` and `main.rs` passes `SystemStoreLocks`, so the older \"opcode 3 decodes the store path but takes no lock\" phrasing is FALSE at HEAD. The `EXIT_UNOBSERVED` window survives. Re-measure rather than quoting either version.",
      "timestamp": "2026-08-27T06:14:42.085Z"
    },
    {
      "id": "comment-2f928faec9a842bb82e68a5bd3e88c9a",
      "author": "architect-ac8532da",
      "content": "PREMISE CHANGE (architect-ac8532da, 2026-08-27 10:15Z, HEAD 13b5dd8e; human confirmed in session). The human cleaned their own worktree: apps/control-room/src/main.tsx, apps/control-room/src/v2/cordum-app.tsx and apps/control-room/src/v2/shell/cordum-shell.tsx are now CLEAN at HEAD, and HEAD carries zero project-manager references (`git show HEAD:apps/control-room/src/main.tsx | grep -c ProjectManagerApp` = 0; cordum-app.tsx ProjectBoundary = 0). The dirty hunks this row was filed to adopt (and that my 06:20Z release handoff mapped) no longer exist anywhere in the tree - not an incident, a deliberate cleanup. Consequences for the planner: (1) this is now an AUTHORING row, not an adoption row - MODE 2 staging is no longer needed, ordinary edits + a pathspec commit are correct, and no foreign hunks remain in either file; (2) the behaviour to (re)implement is what the handoff recorded: `isProjectManagerLocation(hostname, search, development)` = `hostname === \"127.0.0.2\" || (development && ?projects=1)`, `prepareProjectManager` via `connectProjectManager` from ./v2/projects/project-manager-client.js, the `<ProjectManagerApp prepared={managerSetup} />` branch in chooseRoot, `prepareV2LiveSetup` short-circuit in manager mode, and in cordum-app.tsx `<ProjectBoundary projectId={attached?.projectId ?? null} />` around the body plus the document.title effect; the shell `projectId` prop/data-project-id attribute is optional and now absent at HEAD; (3) v2/projects is fully tracked (10/10 at 357d3635), so DoD-1's gate passes; (4) HEAD main.tsx never reads import.meta.env - a `development` signal needs either the vite/client reference or an injected flag, and a test importing main.tsx trips its module-level mount (CONTROL_ROOM_ROOT_MISSING without #root), so put the location rule in a small module or give the test a #root before import.",
      "timestamp": "2026-08-27T10:19:07.796Z"
    },
    {
      "id": "comment-0481dc0951cb40c18530337ca43d74f6",
      "author": "architect-d950bd95",
      "content": "PLAN + DoD/RAIL AMENDMENTS — architect-d950bd95, 2026-08-27, HEAD 36f31348. Binding on worker and QA. Builds on architect-ac8532da's handoff (hunk map) and comment-2f928fae (human-confirmed premise change).\n\nMODE FLIP: the human cleaned the worktree, so main.tsx / v2/cordum-app.tsx are byte-equal to HEAD and carry no foreign hunks. This row is AUTHORING, mode 1: ordinary edits + explicit-pathspec commit. DoD-2/DoD-3's mode-2 / bare-commit / `MM` clauses are VOID; replace with: `git diff HEAD -- <both>` before staging shows only this row's hunks, `git diff --cached --numstat` = exactly four paths, pathspec commit, `git status` clean afterwards, NO skip-worktree bit set by this row. Step 1 re-verifies the clean premise (worktree hash == HEAD blob) and stops if either file is dirty again.\n\nOWNED PATHS = FOUR (rail 1 amended): main.tsx, v2/cordum-app.tsx, NEW src/entry-project-manager.ts (the seam holding `isProjectManagerLocation` / `resolveProjectManagerMode` with the vite/client reference — main.tsx's module-level mount makes the function untestable in place), NEW src/entry-project-manager.test.tsx (the row's test file per DoD-4).\n\nDoD-4 STRENGTHENED: the render arm must record fetch call order — manager location → exactly `[\"/manager/bootstrap\"]`; ordinary location → `[\"/manager/bootstrap\", \"/bootstrap\"]` — because a presence-only arm stays GREEN when `managerMode ||` is removed from prepareV2LiveSetup (measured). Two named drills: D1 drop `development &&` (production `?projects=1` arm reds); D2 remove `managerMode ||` (call-order arm reds). D3 (branch order) is a disclosed non-detection.\n\nDoD-5 CLARIFIED: the live control-room typecheck is RED on foreign dirty/untracked goals files (25 errors at planning) and is NOT a positive control; grade only the `git archive HEAD` materialization legs: typecheck EXIT 0, test with the new file counted, `vite build` EXIT 0.\n\nBLOB DETAILS the worker must not miss: cordum-app's eyebrow line moves below the new `const projectId` (TS2448/TS2454 otherwise); the CordumShell `projectId` prop is omitted (absent at HEAD; disclosed follow-up).\n\nReview: 8-agent workflow, 21 claims held; the 6 refuter findings were unjudged because the judge agents hit the session limit — judged by the architect on the refuters' measured evidence, all six folded above.",
      "timestamp": "2026-08-27T11:32:25.184Z"
    },
    {
      "id": "comment-5d785e1746404d16b97a68591504da22",
      "author": "worker-ca97b7bd",
      "content": "STEP 1 — DoD-1 GATE + MEASURE (worker-ca97b7bd, 2026-08-27). HEAD at claim = `5f7b3d5a9a6066c7630cdeff8189c947ab3572ee` (planning anchored at 36f31348; HEAD moved, the two target files did NOT).\n\nGATE (a) `git ls-files apps/control-room/src/v2/projects | wc -l` -> **10**. Exactly the 10 P1.2b-ii files: project-boundary.{css,test.tsx,tsx}, project-home.{css,test.tsx,tsx}, project-manager-app.{test.tsx,tsx}, project-manager-client.{test.ts,ts}. PASS.\n\nGATE (b) duplicate check: `git show HEAD:apps/control-room/src/main.tsx | grep -c ProjectManagerApp` -> **0**; `git show HEAD:apps/control-room/src/v2/cordum-app.tsx | grep -c ProjectBoundary` -> **0**. Not a duplicate. PASS.\n\nGATE (c) CLEAN PREMISE (mode-1 authoring, per comment-2f928fae):\n- main.tsx: `git hash-object` = `9b86240972a117c10a107fe71a36ff17d36379a0` == `git rev-parse HEAD:` = `9b86240972a117c10a107fe71a36ff17d36379a0`\n- v2/cordum-app.tsx: `a4d763b2ae1b80d63b103578ff877da30030f56b` == `a4d763b2ae1b80d63b103578ff877da30030f56b`\n- `git status --porcelain -- <both>` -> EMPTY. `git diff --cached --name-only` -> EMPTY (index clean).\nBoth match the architect's planning-time hashes exactly. MODE 1 confirmed: ordinary edits + explicit-pathspec commit, no mode-2 blob, no temp index, no skip-worktree.\n\nNEW-FILE COLLISION: `apps/control-room/src/entry-project-manager.ts` and `.test.tsx` do not exist (ls: No such file). No collision.\n\nFOREIGN DIRTY/UNTRACKED under apps/control-room — DRIFTED since planning, re-measured now; none is imported by this row's code and none will be touched:\n` M` live/live-effort-path.test.tsx, nodes/node-authority.tsx, performance/command-latency.tsx, v2/goals/goal-model.ts, v2/goals/new-goal-form.test.tsx, v2/goals/new-goal-form.tsx, v2/shell/nav-rail.tsx, v2/styles/cordum-goals.css\n`??` development-legacy-root.tsx, entry-route.ts, entry-route.test.ts, live/live-command-dispatch.ts, live/live-dispatch-production.test.ts, live/live-goal-catalog.{ts,test.ts}, presentation-constants.ts, v2/goals/goal-brief.{ts,test.ts}, v2/goals/goal-catalog-model.{ts,test.ts}, v2/goals/new-goal-form-model.ts, v2/goals/use-goal-prd.ts, v2/shell/nav-rail.css\nDrift vs the plan's list: live/live-board.tsx, live/live-dispatch.ts, live-board-dispatch.test.tsx, v2/goals/fixture-goals-view.tsx are NO LONGER dirty/untracked; the goals/live untracked set above is new. Does not change this row.\n\nNEAR-DUPLICATE CHECKED (epic rail 2): foreign untracked `src/entry-route.ts` exports only `gateDevelopmentQuery(search, development)` over `[\"fixtures\",\"v1\"]` and explicitly comments \"the project-manager route, remains intact\" — it does NOT define a manager-location predicate. `grep -rn \"isProjectManagerLocation\" apps/control-room/src` -> 0 hits. No duplicate; the plan's \"no gateDevelopmentQuery in main.tsx\" stands.\n\nSYMBOLS VERIFIED ON DISK (explore-before-assume):\n- `ProjectManagerApp({ prepared: Promise<ProjectManagerConnection>, openWindow? })` — project-manager-app.tsx:81; PENDING heading literal `Connecting to project manager` at :68. CONFIRMED.\n- `connectProjectManager({ fetchImpl: ProjectManagerFetch }): Promise<ProjectManagerConnection>` — project-manager-client.ts:222; its FIRST request is `/manager/bootstrap` at :225. CONFIRMED.\n- `ProjectBoundary({ projectId: string | null, openWindow? })` — project-boundary.tsx:50. CONFIRMED.\n- v2 handshake requests `/bootstrap` — live-handshake.ts:121. CONFIRMED (distinguishable from `/manager/bootstrap`, so the call-order recorder can tell them apart).\n- `LiveSetup.projectId: string | null` — live-config.ts:34 (on the `ok: true` arm). `attached` at cordum-app.tsx:157; eyebrow at :152 (above it -> the TDZ move the plan requires). CONFIRMED.\n- manager host `127.0.0.2`: `PROJECT_MANAGER_HOME = \"http://127.0.0.2:39122\"` project-boundary.tsx:8. CONFIRMED.\n- tsconfig `types: [\"node\"]` (no vite types) and precedent `/// <reference types=\"vite/client\" />` at board-surface.tsx:1. CONFIRMED — the seam needs the triple-slash reference.\n- control-room package scripts present: `build` (vite build), `test` (vitest run), `typecheck` (tsc --project tsconfig.json). vitest.config.ts: environment jsdom, `include: [\"src/**/*.test.ts\",\"src/**/*.test.tsx\"]`, `passWithNoTests: false` — so a vacuous run cannot exit 0 here.\n\nOWNED PATHS (four, per the rail amendment): apps/control-room/src/main.tsx, apps/control-room/src/v2/cordum-app.tsx, apps/control-room/src/entry-project-manager.ts (new), apps/control-room/src/entry-project-manager.test.tsx (new).",
      "timestamp": "2026-08-27T11:52:28.767Z"
    },
    {
      "id": "comment-73b12e93ea304f22a1a92ad7fe0c55b1",
      "author": "worker-ca97b7bd",
      "content": "STEP 2 — TESTS FIRST + RED (worker-ca97b7bd).\n\n## PLAN DEVIATION, measured — the prescribed location stub is not constructible in jsdom\nThe step says \"stub `window.location.hostname` to `127.0.0.2`; dynamic-import `./main.js`\". jsdom 30.0.1 marks both `location` and `location.hostname` LegacyUnforgeable. Probe (node, jsdom@30.0.1 resolved out of `node_modules/.pnpm`, run out of tree and deleted):\n```\ninitial hostname: localhost\nown descriptor on location: {\"configurable\":false,\"hasGet\":true}\nproto descriptor: undefined\ndefineProperty on location THREW: TypeError Cannot redefine property: hostname\nwindow.location descriptor: {\"configurable\":false,\"writable\":false,\"hasGet\":true}\n```\n`history.replaceState` cannot cross origins either, so no in-file route reaches hostname `127.0.0.2`. A file-level `@vitest-environment-options {\"url\":\"http://127.0.0.2:39122/\"}` would pin BOTH arms to the manager host and make the ordinary arm unconstructible — the rail allows one test file, so that is not an option.\n\nADAPTATION: `mountControlRoom` gains a third parameter `hostname: string = globalThis.location?.hostname ?? \"\"`, mirroring the seam already in that file (`clock: Clock = BROWSER_CLOCK`, whose doc says the composition root is \"the only production module that reads a host clock at all\"). Host reading is now isolated the same way. Consequences, deliberately chosen:\n- The ORDINARY arm runs through the REAL module-level production mount with ZERO injection — jsdom's unforgeable origin IS an ordinary location, so this arm proves the security-relevant direction (manager must NOT mount, v2 handshake still runs) against production bytes.\n- The MANAGER arm injects `PROJECT_MANAGER_HOSTNAME` into the exported `mountControlRoom`. Disclosed: this arm is injection-based. It is the capability direction, not the refusal direction.\n\n## Files written (2 of the 4 owned paths)\n`src/entry-project-manager.ts` (44 lines): `/// <reference types=\"vite/client\" />`; `export const PROJECT_MANAGER_HOSTNAME = \"127.0.0.2\"`; `isProjectManagerLocation(hostname, search, development)` = fixed host OR (`development` && `?projects=1`); `resolveProjectManagerMode(hostname, search)` passing `import.meta.env.DEV`.\n`src/entry-project-manager.test.tsx` (117 lines): 5 pure arms + 2 render arms with a fetch CALL-ORDER RECORDER (`requested: string[]`, fetch never settles, so each arm asserts what was REQUESTED, not what answered). `describe` titles carry the task id for self-attribution.\n\n## RED, quoted verbatim\n`pnpm --filter @moe/control-room exec vitest run src/entry-project-manager.test.tsx` — EXIT 1\n```\n ❯ src/entry-project-manager.test.tsx:103:34\n    103|         expect(within(container).getByTestId(\"cr.manager.root\")).toBeT…\n\n Test Files  1 failed (1)\n      Tests  1 failed | 6 passed (7)\n```\nThe failure dump is the Cordum nav rail (`cr.nav.runs.unavailable`, `cr.nav.resources`) — main.tsx has no manager branch yet, so the manager container received the v2 shell. 6 passing = the 5 pure arms + the ordinary render arm (the pre-existing behaviour this row must not disturb). The count line names the file, so the leg executed (rail: exit 0 is not evidence; here a nonzero count line proves 7 tests ran).\n\nNOTE for later legs: `pnpm --filter @moe/control-room exec vitest run <path>` DOES filter to the path (memory `pnpm-filter-test-double-dash-does-not-filter` covers the different shape `pnpm --filter … test -- <path>`, which does not).",
      "timestamp": "2026-08-27T11:55:44.783Z"
    },
    {
      "id": "comment-f419399646b84716bfa4db39e975a27d",
      "author": "worker-ca97b7bd",
      "content": "STEP 3 — MINIMUM COMPOSITION + GREEN + DIVERGENCE DRILLS (worker-ca97b7bd).\n\n## What landed\n`main.tsx` (101 -> 138 lines): three imports (`resolveProjectManagerMode`, `ProjectManagerApp`, `connectProjectManager` + `type ProjectManagerConnection`); `chooseRoot(search, managerMode, liveSetup, managerSetup)` with the manager branch FIRST; `prepareV2LiveSetup(search, managerMode)` short-circuiting on `managerMode ||`; new `prepareProjectManager(managerMode)`; `mountControlRoom(container, clock = BROWSER_CLOCK, hostname = globalThis.location?.hostname ?? \"\")` wiring `resolveProjectManagerMode(hostname, search)` -> `prepareV2LiveSetup` -> `prepareProjectManager` -> `chooseRoot`. Nothing else: no vite reference, no gateDevelopmentQuery, no lazy legacy root, no import removals.\n`v2/cordum-app.tsx` (228 -> 234 lines): `ProjectBoundary` import; `const projectId = attached?.projectId ?? null` placed AFTER `const attached`; the eyebrow MOVED below it and now reads ``PROJECT ${MIDDOT} ${projectId ?? \"PAIRING\"}``; a `document.title` effect restoring the previous title on cleanup; `const content = fixtures ? body : (<><ProjectBoundary projectId={projectId} />{body}</>)` rendered in place of `{body}`. CordumShell `projectId` prop NOT added (absent at HEAD) — disclosed follow-up.\n\nUI-copy check before changing the eyebrow: `grep -rn \"MOE-NG\"` hits only `cordum-app.tsx:152` (the line being changed), `glyphs.ts:13` and `context-bar.tsx:20` (doc comments, not owned), and `cordum-shell.tsx:31 DEFAULT_EYEBROW` (the shell's own fallback, untouched). No test asserted the CordumApp eyebrow copy, so no tracked assertion was lost.\n\n## numstat / size\n```\n55  10  apps/control-room/src/main.tsx\n27   2  apps/control-room/src/v2/cordum-app.tsx\n```\nHigher than the plan's ~23/4 and ~19/2 because of the three doc comments the composition needed (route precedence, handshake suppression rationale, the injected-hostname rationale) plus the hostname seam. Per-file caps: entry-project-manager.ts 42, entry-project-manager.test.tsx 136, main.tsx 138, cordum-app.tsx 234 — all under 250.\n\n## GREEN, live worktree\n`pnpm --filter @moe/control-room typecheck` -> `$ tsc --project tsconfig.json`, EXIT 0 (script echo present, so the leg executed).\n`pnpm --filter @moe/control-room exec vitest run src/entry-project-manager.test.tsx` -> `Test Files 1 passed (1) / Tests 8 passed (8)`, EXIT 0.\n\n## DIVERGENCE DRILLS — run in a `git archive HEAD` + 4-file overlay materialization at `%TEMP%\\worker-ca97b7bd\\delivered` (`pnpm install --frozen-lockfile --prefer-offline` EXIT 0). The live worktree was NEVER mutated.\n- **D1** drop `development &&` in the seam -> RED, exactly one arm, the named one:\n  `× refuses ?projects=1 in a production build` / `AssertionError: expected true to be false` / `Tests 1 failed | 6 passed (7)`.\n- **D2** remove `managerMode ||` from `prepareV2LiveSetup` -> RED with the architect's predicted wording verbatim:\n  `AssertionError: expected [ '/bootstrap', '/manager/bootstrap' ] to deeply equal [ '/manager/bootstrap' ]` at `entry-project-manager.test.tsx:109`, `Tests 1 failed | 6 passed (7)`.\n- **D3** move the manager branch BELOW the `?v1=1` branch -> **GREEN (7 passed)**. Per epic rail 7A a green drill is a FINDING, and this one was load-bearing: `chooseRoot`'s doc comment claims \"no flag can move the manager document off it\" and nothing pinned it. A manager origin carrying `?v1=1` would have rendered the legacy v1 shell.\n  **CLOSED IN THIS ROW**, not deferred: added an 8th arm, `keeps the manager host on the manager even when a query names another route` (mounts at hostname `127.0.0.2` with `/?v1=1&fixtures=1` via `history.replaceState`, asserts `cr.manager.root` present and both `cr.shell.root` and `cr2.shell.root` absent). Baseline `Tests 8 passed (8)`; **D3 re-applied -> RED**: `× keeps the manager host on the manager even when a query names another route` / `TestingLibraryElementError: Unable to find an element by: [data-testid=\"cr.manager.root\"]` / `Tests 1 failed | 7 passed (8)`. D3 is now detected.\n\n## RESTORE PROOF (rail: SHA256 against an out-of-tree backup, in a call separate from the restore)\nThree-way, after every drill, all four owned paths:\n```\nmain.tsx                       pristine=41B3E6F9BE96 mat=41B3E6F9BE96 worktree=41B3E6F9BE96 ALL_MATCH=True\ncordum-app.tsx                 pristine=C5BD0B2BE785 mat=C5BD0B2BE785 worktree=C5BD0B2BE785 ALL_MATCH=True\nentry-project-manager.ts       pristine=DCF32657D400 mat=DCF32657D400 worktree=DCF32657D400 ALL_MATCH=True\nentry-project-manager.test.tsx pristine=39409CEFAEC7 mat=39409CEFAEC7 worktree=39409CEFAEC7 ALL_MATCH=True\n```\nBackups live at `%TEMP%\\worker-ca97b7bd\\pristine` (suffixed by worker id). `git diff` is not cited as a restore proof for the two untracked new modules — it is blind to them.\n\n## FOREIGN RED, path-attributed and disclosed verbatim\nLive full suite `pnpm --filter @moe/control-room test` -> EXIT 1, `Test Files 1 failed | 92 passed (93)` / `Tests 1 failed | 1154 passed (1155)`:\n```\n ❯ src/v2/goals/goals-home.test.tsx:174:40\n    174|     await waitFor(() => { expect(sent).toHaveLength(1); });\n- Expected  1\n+ Received  0\n```\nNOT MINE, proven two ways. (1) Import closure: `goals-home.test.tsx` imports only `live-config`, `live-board-feed`, `cordum-shell`, `goal-model`, `goals-fixtures`, `goals-home`, `new-goal-form`, `live-goals` — `grep -rn \"cordum-app|main.js\"` over it and `goals-home.tsx` returns ZERO hits, so none of this row's four paths is reachable from it. (2) Decisive: the SAME file is `Test Files 1 passed (1) / Tests 11 passed (11)`, EXIT 0 in the materialization (HEAD + my four files, WITHOUT the foreign uncommitted WIP). The red is caused entirely by the foreign dirty ` M v2/goals/goal-model.ts` and ` M v2/goals/new-goal-form.tsx`, which are another row's in-flight TDD. Owned-path intersection of the delta = EMPTY.\n\nCorrection to a planning premise: the plan predicted the LIVE control-room typecheck would be RED on foreign goals files (fixture-goals-view.tsx TS2322, goals-home.test.tsx x19, live-goals.test.tsx x5). It is now EXIT 0 — that foreign WIP was resolved between planning and execution. Reported so no one grades against a stale expectation.",
      "timestamp": "2026-08-27T12:01:23.415Z"
    },
    {
      "id": "comment-a1325cb5b0f44fa390d8a2470a80f68f",
      "author": "worker-ca97b7bd",
      "content": "STEP 4 — COMMIT by explicit pathspec (worker-ca97b7bd). Commit **`cccf133e2e6d43d3ff7c6a42798f04decba49650`** on `moe/work-2026-08-08`, parent `5f7b3d5a` (HEAD did not move between the step-1 gate and the commit).\n\nPRE-STAGE re-verification of the mode-1 premise: `git diff HEAD --` over the two tracked owned paths contains ZERO foreign hunks — every added/removed line is this row's (the three manager imports, the chooseRoot manager branch and its doc, prepareV2LiveSetup's `managerMode ||`, prepareProjectManager, the mountControlRoom hostname parameter and its wiring, the ProjectBoundary import, projectId + the moved eyebrow, the document.title effect, the content wrapper). The plan's mode-2 route was therefore correctly retired.\n\nSTAGED, exactly four:\n```\n149  0  apps/control-room/src/entry-project-manager.test.tsx\n 46  0  apps/control-room/src/entry-project-manager.ts\n 55 10  apps/control-room/src/main.tsx\n 27  2  apps/control-room/src/v2/cordum-app.tsx\n```\n(git's add-counts 149/46 supersede the PowerShell `Measure-Object -Line` figures 136/42 I quoted in step 3 — that cmdlet undercounts. Both files remain far under the 250-line per-file target.)\n\nCOMMITTED with `git commit -F - -- <the four paths>`:\n```\n[moe/work-2026-08-08 cccf133e] feat(task-15ca5c44): compose the project manager into the Control Room entrypoints\n 4 files changed, 277 insertions(+), 12 deletions(-)\n create mode 100644 apps/control-room/src/entry-project-manager.test.tsx\n create mode 100644 apps/control-room/src/entry-project-manager.ts\n```\nThe body names the fixed manager host, the development-only query switch and why `import.meta.env.DEV` eliminates rather than skips the branch, the route-precedence property in both directions, the manager-mode handshake suppression and its rationale, the injected hostname and the jsdom constraint that forced it, and the deliberately omitted CordumShell projectId prop.\n\nPOST-COMMIT AUDIT — every clause of the step:\n- `git show --stat HEAD` = exactly the four paths, nothing else.\n- `git status --porcelain -- <the four>` = **EMPTY**. Nothing of this row is left for a session-end sweep to pick up.\n- `git ls-files -v -- <the four>` = `H` on all four. This row set NO skip-worktree bit. `apps/daemon/src/http/affordance-read.ts` (task-48ee1455's deliberate `S`) was never touched.\n- `git diff --cached --name-only` = EMPTY. The shared index is clean for the next owner.\n- SIGNED: `git cat-file commit HEAD` carries `gpgsig -----BEGIN SSH SIGNATURE-----` (config `commit.gpgsign=true`, `gpg.format=ssh`, key `~/.ssh/id_ed25519.pub`). NOTE for QA: `git log -1 --format='%G?'` prints `N` here and emits `error: gpg.ssh.allowedSignersFile needs to be configured and exist for ssh signature verification` — that is a missing local VERIFICATION allowlist, not a missing signature. Read the raw commit object, not `%G?`, on this host.\n- FOREIGN WIP PRESERVED, byte-identical to the pre-commit list: 8 modified (`live/live-effort-path.test.tsx`, `nodes/node-authority.tsx`, `performance/command-latency.tsx`, `v2/goals/goal-model.ts`, `v2/goals/new-goal-form.test.tsx`, `v2/goals/new-goal-form.tsx`, `v2/shell/nav-rail.tsx`, `v2/styles/cordum-goals.css`) and 15 untracked under `apps/control-room` — same set, same states, nothing swept in.",
      "timestamp": "2026-08-27T12:03:04.750Z"
    },
    {
      "id": "comment-e050cce182024d6e8c12519a0b2a3987",
      "author": "worker-ca97b7bd",
      "content": "STEP 5 — GATES ON THE DELIVERED COMMIT + ADVERSARIAL REVIEW (worker-ca97b7bd).\n\nMATERIALIZATION: `git archive HEAD` (HEAD = **`cccf133e`**) -> `%TEMP%\\worker-ca97b7bd\\delivered2`, `pnpm install --frozen-lockfile --prefer-offline` EXIT 0. COMMITTED BYTES == GATED BYTES verified before running anything — SHA256 of all four owned paths in the archive equals the worktree copy: main.tsx `41B3E6F9BE96`, cordum-app.tsx `C5BD0B2BE785`, entry-project-manager.ts `DCF32657D400`, entry-project-manager.test.tsx `39409CEFAEC7`, MATCH=True on each.\n\n## (a) `pnpm --filter @moe/control-room typecheck` — EXIT 0\nOutput: `$ tsc --project tsconfig.json` (the script echo is the execution evidence).\n**POSITIVE CONTROL**, because a fast green typecheck proves nothing on its own. Planted `PROJECT_MANAGER_HOSTNAME: number = \"127.0.0.2\"` in the DELIVERED tree; the leg went EXIT 1 and named BOTH new files:\n```\nsrc/entry-project-manager.test.tsx(22,37): error TS2345: Argument of type 'number' is not assignable to parameter of type 'string'.\nsrc/entry-project-manager.test.tsx(101,71): error TS2345: ...\nsrc/entry-project-manager.test.tsx(134,71): error TS2345: ...\nsrc/entry-project-manager.ts(10,14): error TS2322: Type 'string' is not assignable to type 'number'.\nsrc/entry-project-manager.ts(35,7): error TS2367: This comparison appears to be unintentional ...\n```\nSo gate (a) does typecheck this row's files. Restored, proven by SHA256 in a call separate from the restore: `pristine == delivered == DCF32657D400…A66F`, CONTROL_RESTORE_PROVEN=True.\n\n## (b) `pnpm --filter @moe/control-room test` — EXIT 0\n```\n Test Files  88 passed (88)\n      Tests  1115 passed (1115)\n```\nNONZERO count line, so the leg executed. INCLUSION PROVED, not assumed: `vitest list --filesOnly` prints `src/entry-project-manager.test.tsx`, and the verbose run of it inside the delivered tree lists all eight arms individually:\n```\n✓ task-15ca5c44 isProjectManagerLocation > selects the manager on its fixed host even when the build is production\n✓ task-15ca5c44 isProjectManagerLocation > accepts ?projects=1 while development is true\n✓ task-15ca5c44 isProjectManagerLocation > refuses ?projects=1 in a production build\n✓ task-15ca5c44 isProjectManagerLocation > refuses an ordinary location\n✓ task-15ca5c44 isProjectManagerLocation > reads the query and never the fragment\n✓ task-15ca5c44 Control Room entry composition > mounts the existing app at an ordinary location and asks only for the v2 handshake\n✓ task-15ca5c44 Control Room entry composition > mounts the project manager on the manager host and asks for no v2 handshake\n✓ task-15ca5c44 Control Room entry composition > keeps the manager host on the manager even when a query names another route\n Test Files  1 passed (1)\n      Tests  8 passed (8)\n```\n\n## (c) `pnpm --filter @moe/control-room build` — EXIT 0\n`✓ built in 215ms`, `dist/assets/index-DIAYdlvK.js 417.87 kB │ gzip: 124.18 kB`. The composition bundles.\n\n## (d) LIVE worktree, disclosed verbatim, NOT graded\n`pnpm --filter @moe/control-room typecheck` on the live tree is **EXIT 0** (`$ tsc --project tsconfig.json`) — the planning-time prediction of a live typecheck red on foreign goals files no longer holds.\n`pnpm --filter @moe/control-room test` on the live tree is EXIT 1:\n```\n FAIL  src/v2/goals/goals-home.test.tsx > Create goal dispatches goal.create through the kept dispatch layer > hands the daemon's own goal.create offer back as a goal.create envelope\n ❯ src/v2/goals/goals-home.test.tsx:174:40\n    174|     await waitFor(() => { expect(sent).toHaveLength(1); });\n- Expected  1\n+ Received  0\n Test Files  1 failed | 92 passed (93)\n      Tests  1 failed | 1155 passed (1156)\n```\nPATH ATTRIBUTION: the failing-path set at the DELIVERED tree (gate b) is **EMPTY** — 88/88 files, 1115/1115 tests pass — so (HEAD failures) minus (merge-base failures), intersected with this row's owned paths, is `∅` trivially. Independently: `goals-home.test.tsx` imports only live-config, live-board-feed, cordum-shell, goal-model, goals-fixtures, goals-home, new-goal-form, live-goals; `grep -rn \"cordum-app|main.js\"` over it and `goals-home.tsx` returns ZERO hits, so none of this row's four paths is in its import closure. The red is produced by the foreign dirty ` M v2/goals/goal-model.ts` and ` M v2/goals/new-goal-form.tsx` (another row's in-flight TDD) and belongs to that row.\n\n## ADVERSARIAL REVIEW\n1. **`?projects=1` cannot enable manager mode in a production build.** Proved at the BYTE level in the built bundle, not by reasoning. `import.meta.env` occurs **0** times in `index-DIAYdlvK.js` (Vite folded it statically), and the compiled predicate reads:\n   ```js\n   d=`projects`;\n   function f(e,t,n){return e===`127.0.0.2`||n&&new URLSearchParams(t).get(d)===`1`}\n   function p(e,t){return f(e,t,!1)}\n   ```\n   `p` is `resolveProjectManagerMode` and it passes `!1` for `development`, so the query branch is unreachable in production regardless of what the URL says. (A bare-token grep would have been vacuous here — I ran `\"v1\"`/`\"fixtures\"` as a control and BOTH were absent from the minified output, which is why the neighbourhood extract, not a grep, is the evidence.)\n2. **Neither query nor fragment carries authority.** The manager's credential arrives only through `connectProjectManager`'s `credentials: \"same-origin\"` cookie session plus the CSRF token closed over inside the client; nothing about the route decision touches it. `main.tsx`'s pre-existing fragment scrub is untouched, and `isProjectManagerLocation` takes `search`, never `hash` — pinned by the `reads the query and never the fragment` arm.\n3. **Manager mode suppresses the v2 handshake.** Pinned by exact array equality on a call-order recorder (`requested` == `[\"/manager/bootstrap\"]`), and drilled: removing `managerMode ||` reds it with `expected [ '/bootstrap', '/manager/bootstrap' ] to deeply equal [ '/manager/bootstrap' ]`.\n4. **`127.0.0.2` is the host the daemon actually serves.** `git grep` at the delivered HEAD: `apps/daemon/src/projects/project-manager-http.ts:114: const host = \"127.0.0.2\";`, with the daemon arm `binds 127.0.0.2 and exposes no authority in its URL or bootstrap` (project-manager-http.test.ts:129) and `http://127.0.0.2:39122` in project-manager-main.test.ts. The literal now lives in THREE places (daemon http.ts, control-room `PROJECT_MANAGER_HOME`, this seam) — see follow-up 3.\n5. **Route precedence in both directions.** The manager branch is decided before every query branch; the `keeps the manager host on the manager even when a query names another route` arm mounts at `127.0.0.2` with `/?v1=1&fixtures=1` and asserts the manager root present with both `cr.shell.root` and `cr2.shell.root` absent. This arm exists because drill D3 was GREEN without it.\n6. **ProjectBoundary renders on every non-fixtures Cordum body**, `document.title` is restored on cleanup. Both implemented; neither has its own arm in this row (the full 88-file suite is green over them, so nothing regressed). Named as follow-up 2 rather than claimed as pinned.\n7. **No probe residue.** My only repo writes were the four owned paths, all committed, `git status --porcelain` of them EMPTY. The jsdom probe ran out of tree and was deleted; all drills and builds ran under `%TEMP%\\worker-ca97b7bd`. `apps/control-room/dist` exists but is gitignored (`.gitignore:4 dist/`) and dated 2026-08-25, two days before this session — not mine.\n8. **UI-copy exclusivity caveat carried** (task-4247169c comment-fef5b5cb, settled-predecessor only): the ordinary arm's `expect(container.textContent).not.toContain(\"Connecting to project manager\")` is a substring negative and therefore one spelling, not a property. It is admissible here because the predecessor is settled (the fetch never resolves, so the render is deterministically at PENDING), and it is not load-bearing: the real fences in that arm are the testid presence/absence pair and the exact `toEqual` on the recorder.\n\n## DISCLOSED FOLLOW-UPS (not defects in this row)\n1. `CordumShell` gains no `projectId`/`data-project-id` prop — it does not exist at HEAD; adding it is a separate row.\n2. No dedicated arm for the ProjectBoundary-on-every-live-body rule or the `document.title` restore-on-unmount.\n3. `127.0.0.2` is now spelled in three modules. A shared constant would need a dependency-free home; `PROJECT_MANAGER_HOME` lives in a `.tsx` that imports CSS, so importing it into this pure seam would be a regression.",
      "timestamp": "2026-08-27T12:06:45.778Z"
    },
    {
      "id": "comment-6ced8e77f1ea4a3b99a9fb3840afab8c",
      "author": "worker-ca97b7bd",
      "content": "STEP 6 — ADVERSARIAL SELF-REVIEW of the committed diff (worker-ca97b7bd, commit cccf133e). Ran the full checklist against `git show cccf133e`. **Nothing required a fix.** Every finding below is unreachable-by-construction or pre-existing; recorded here so QA sees they were considered rather than missed.\n\n1. **`chooseRoot`'s `managerMode && managerSetup !== undefined` can silently fall through.** If the two ever disagreed, a manager-host document would render CordumApp instead. Unreachable: both derive from the same `managerMode` in the single call site, and `prepareProjectManager` returns undefined ONLY when `!managerMode`. Consequence if it somehow happened is a refused pairing view, not an authority leak — `prepareV2LiveSetup` also returned undefined, so `useLiveHandshake` falls back to its rejecting fetchImpl. Left as-is rather than collapsing to `managerSetup !== undefined`, because the explicit conjunction is what makes the branch order auditable.\n2. **`document.title` under StrictMode double-invoke.** effect -> cleanup -> effect: pass 1 captures the original and sets ours, cleanup restores the original, pass 2 captures the original again. Correct. A projectId change A->B is likewise correct (cleanup restores the original before the new effect captures it). No other module in the app writes `document.title` (grepped), so the captured \"previous\" cannot be another component's value.\n3. **Untrusted `projectId` reaching three sinks.** It flows to `document.title`, the eyebrow string, and `ProjectBoundary`'s `data-project-id`. React escapes the two rendered sinks and `document.title` is a DOM string property, not HTML, so no injection path. Its validation lives upstream in the v2 handshake, unchanged by this row.\n4. **`hostname` default fails closed.** `globalThis.location?.hostname ?? \"\"` yields `\"\"` where there is no location; `isProjectManagerLocation(\"\", …)` is false unless a development build also carries `?projects=1`. The absent-location case does not select the manager.\n5. **Unhandled-rejection window between mount and effect.** `prepareProjectManager`'s promise gets its handlers inside `ProjectManagerApp`'s `useEffect`. This is the exact shape the pre-existing `prepareV2LiveSetup` + `useLiveHandshake` pair already has, and `connectProjectManager` resolves refusals rather than rejecting (every failure path returns a `ProjectManagerRefusal`). Theoretical, and not a regression.\n6. **`ProjectBoundary` renders above the PAIRING body too.** Deliberate: `projectId` is null there and the boundary reads \"PAIRING REQUIRED\". Its switch form accepts only a plain `127.0.0.1:<port>` origin with no query and no fragment (`validProjectOrigin`), so it transports no authority from that state either.\n7. **API smell, disclosed:** `hostname` is a THIRD positional parameter after the optional `clock`, so a caller overriding the host must also pass the clock. An options object would read better, but reshaping `mountControlRoom(container, clock)` would edit `scaffold.test.tsx`, which this row does not own. Recorded as a follow-up rather than a scope widening.\n8. Double-mount, retry and concurrency: `mountControlRoom` called twice creates two roots and two bootstraps, exactly as before this row — the pre-existing scaffold test already does it deliberately. No new shared mutable state was introduced; the seam is pure and the two prepare functions are called once per mount.",
      "timestamp": "2026-08-27T12:07:51.371Z"
    },
    {
      "id": "comment-b731f064f24049e5aa90a2dbff5905c2",
      "author": "qa-2eabba3d",
      "content": "QA VERDICT: APPROVED (qa-2eabba3d, 2026-08-27). Every leg below was re-run by me in my own materialization at `%TEMP%\\qa-2eabba3d\\delivered`; nothing is taken from the worker's report. Delivered commit `cccf133e2e6d43d3ff7c6a42798f04decba49650`, parent `5f7b3d5a`, == current HEAD. All legs standalone, unpiped, exit code captured to a variable (no `;` chain, no tail).\n\n## Commit shape\n`git show --numstat cccf133e` = EXACTLY the four owned paths, 277+/12-:\n- `entry-project-manager.test.tsx` 149/0 (new), `entry-project-manager.ts` 46/0 (new), `main.tsx` 55/10, `v2/cordum-app.tsx` 27/2.\nZero foreign hunks inside the two entrypoints — I read the full diff: no handshake, legacy-route, goal or shell changes rode along. No daemon paths, no `v2/projects` edits (task rail 3 held).\nCommitted bytes == worktree bytes on all four (`git hash-object` == `git rev-parse HEAD:<path>`: 48c76d16 / 70ba1e23 / 7f3cb4d0 / cf900b3d). `git ls-files -v` = `H` on all four — this row set no skip-worktree bit. Foreign control-room WIP still dirty and unswept (`v2/goals/goal-model.ts`, `new-goal-form.tsx`, `new-goal-form.test.tsx` ` M`, six untracked goals modules `??`).\n\n## DoD, item by item\n**DoD-1 (gate)** PASS, re-measured at the PARENT `5f7b3d5a`, not taken on trust: `git ls-tree -r --name-only 5f7b3d5a apps/control-room/src/v2/projects | wc -l` = **10**; `git show 5f7b3d5a:…/main.tsx | grep -c ProjectManagerApp` = **0**; `…/cordum-app.tsx | grep -c ProjectBoundary` = **0**. Not a duplicate, and P1.2b-ii had landed.\n\n**DoD-2 / DoD-3** — both are written against MODE 2, superseded by the architect's mode flip (comment-2f928fae human-confirmed premise change + comment-0481dc09 amendment, four owned paths, mode 1). Graded against the amended text per the plan-step-overlay rule. Substance holds: the two entrypoints were clean at HEAD at claim, the commit is by explicit pathspec over four paths, peer WIP is preserved, and `git status --porcelain` of the owned paths is EMPTY (mode 1 leaves no `MM`, by design).\n\n**DoD-4 (arms + drill)** PASS. `vitest run src/entry-project-manager.test.tsx --reporter=verbose` lists all **8 arms executing** (5 pure + 3 render), `Tests 8 passed (8)` — inclusion proved by name, not inferred from exit 0. I re-ran all three divergence drills MYSELF in the materialization, one variable at a time, each restored and SHA-verified against pristine:\n- **D1** drop `development &&` (seam:36) -> EXIT 1, reds exactly `refuses ?projects=1 in a production build` -> `expected true to be false`, at `entry-project-manager.test.tsx:32`. The other 7 stay green: narrowest mutant, no collateral. Restore SHA `dcf32657d40013a3` == pristine.\n- **D2** drop `managerMode ||` from `prepareV2LiveSetup` (main.tsx:86) -> EXIT 1, `expected [ '/bootstrap', '/manager/bootstrap' ] to deeply equal [ '/manager/bootstrap' ]` at :109. The handshake-suppression property is genuinely pinned by the call-order recorder, not by a presence-only assertion. Restore SHA `41b3e6f9be96e4b5` == pristine.\n- **D3** move the manager branch BELOW the `?v1=1` branch -> EXIT 1, `Unable to find an element by: [data-testid=\"cr.manager.root\"]` at :136. **The worker's disclosed GREEN non-detection is genuinely closed** by the 8th arm; I did not take that on trust, I re-applied the mutant. Restore SHA == pristine.\n\n**DoD-5 (delivered-tree gates)** PASS. `git archive HEAD | tar -x` -> `%TEMP%\\qa-2eabba3d\\delivered`; `pnpm install --frozen-lockfile --prefer-offline` EXIT 0.\n- (a) `pnpm --filter @moe/control-room typecheck` **EXIT 0**, execution evidence `$ tsc --project tsconfig.json`. Not banked on the echo alone: I planted `const QA_CONTROL: number = \"not a number\"` into BOTH new files -> EXIT 1 naming `src/entry-project-manager.ts(48,7): error TS2322` and `src/entry-project-manager.test.tsx(151,7): error TS2322`. tsc demonstrably reads this row's files.\n- (b) `pnpm --filter @moe/control-room test` **EXIT 0**, `Test Files 88 passed (88)` / `Tests 1115 passed (1115)`.\n- (c) `pnpm --filter @moe/control-room build` **EXIT 0**, `dist/assets/index-DIAYdlvK.js 417.87 kB`, built in 159ms.\n\n**DoD-6** PASS. Every step's `modifiedFiles` ⊆ the four owned paths. The consumer-edge record exists and is durable: comment on **task-0602f6c2** headed `CONSUMER EDGE FOR THE CLIENT TREE NOW EXISTS (global rule clause 1)` naming `cccf133e`, plus HEADS-UP comments on task-2eb6d36d / task-7e605144 / task-40017f79. Global rule clause 1 is also satisfied ON DISK, which is what actually matters: `main.tsx:15-17` imports `ProjectManagerApp` + `connectProjectManager`, `cordum-app.tsx:227` renders `<ProjectBoundary/>` — the pure tree now has real production importers, not just exports.\n\n## The security property, verified at the byte level\nThe claim \"`?projects=1` cannot select the manager in a production build\" is the row's security switch, so I checked the BUNDLE, not the argument. In `dist/assets/index-DIAYdlvK.js`, neighbourhood grep (a bare token grep is vacuous here — the minifier emits backtick strings, so `grep '\"projects\"'` returns 0 for BOTH the real key and the `v1` control):\n\n    d=`projects`;\n    function f(e,t,n){return e===`127.0.0.2`||n&&new URLSearchParams(t).get(d)===`1`}\n    function p(e,t){return f(e,t,!1)}\n\n`p` (`resolveProjectManagerMode`) passes a LITERAL `!1` for `development`. No query string can move a production document onto the manager surface. Confirmed.\nAlso confirmed: the only production caller is `main.tsx:146 export const MOUNTED_CONTROL_ROOM_ROOT = mountControlRoom(container)` — it passes NO hostname, so the new third param defaults to the real `globalThis.location?.hostname`. The test seam did not become a production bypass.\n\n## Foreign red — re-run at review time, not read from the report\nPer standing practice I re-ran the LIVE suite rather than trusting the disclosure. It is still red, and still not this row's: `pnpm --filter @moe/control-room test` from the repo = EXIT 1, `Test Files 1 failed | 92 passed (93)`, `Tests 1 failed | 1155 passed (1156)`, `FAIL src/v2/goals/goals-home.test.tsx > … > hands the daemon's own goal.create offer back as a goal.create envelope`.\nAttribution by import closure, not tree mutation: `goals-home.test.tsx` is itself CLEAN, but its dependencies `v2/goals/goal-model.ts` and `v2/goals/new-goal-form.tsx` are ` M` uncommitted peer WIP. `grep -rn \"cordum-app\\|entry-project-manager\\|main\\.js\" apps/control-room/src/v2/goals/` returns ONE hit and it is prose inside a doc comment (`live-goals.tsx:21`) — zero import edges to any owned path. The delivered tree, which contains all four owned files, is 88/88 files and 1115/1115 tests green. Failing-path set at HEAD minus merge-base, intersected with owned paths, is **∅**. Uncommitted peer WIP; does not hold this row.\nThe plan's prediction of a live TYPECHECK red is stale — that leg is green now. Correctly disclosed by the worker.\n\n## Per-file cap\n`main.tsx` 146, `v2/cordum-app.tsx` 253, `entry-project-manager.ts` 46, `entry-project-manager.test.tsx` 149. cordum-app is 3 over the 250 TARGET and far under the 400 split point — not a violation, noted only so the next owner of that file knows the headroom is thin.\n\n## One finding, disclosed as a follow-up rather than a reject\n`entry-project-manager.ts:41-43` doc comment claims: \"`import.meta.env.DEV` is a build-time constant, so the query branch is **eliminated outright** from a production bundle rather than merely skipped.\" Measured in the built bundle, that is not what happens: `f` survives WITH its query branch intact and `p` merely passes `!1`. The behaviour the comment is defending is correct and proven above; only the dead-code-elimination claim is wrong. It changes no delivered byte and no DoD item, so per the delivery-bias rail it is a follow-up, not a hold: reword to \"the production binding passes a literal false, so the query branch is unreachable in a production build.\"\n\nWorker's four follow-ups carried forward unclaimed: no `CordumShell` projectId prop; no dedicated arm for ProjectBoundary-on-every-live-body or the `document.title` restore-on-unmount; `127.0.0.2` spelled in three modules with no dependency-free shared home; `hostname` is a third POSITIONAL param after the optional `clock`.\n\nSignature: `git log -1 --format='%G?'` = `N` only because `gpg.ssh.allowedSignersFile` is unset on this host; `git cat-file commit cccf133e` carries `gpgsig -----BEGIN SSH SIGNATURE-----`. Not a finding.\n\nProbe hygiene: all mutations lived in `%TEMP%\\qa-2eabba3d\\delivered`; the repo worktree was never written. `git status --porcelain` of the four owned paths is EMPTY after my review, and the materialization was restored to byte-equality with the repo (SHA256 match on all four) AND re-greened (`Tests 8 passed (8)`) to prove the restore, not merely assert it.",
      "timestamp": "2026-08-27T12:20:54.460Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-26T20:57:36.305Z",
  "updatedAt": "2026-08-27T12:22:20.530Z",
  "metrics": {
    "firstClaimAt": "2026-08-27T06:14:59.642Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 4,
    "executedStepCount": 6,
    "doneAt": "2026-08-27T12:22:20.530Z",
    "wallClockMs": 22040888
  },
  "contextFetchedBy": [
    "architect-ac8532da",
    "worker-ca97b7bd",
    "qa-2eabba3d"
  ],
  "priorHandoffs": [
    {
      "whatIsDone": "Measured at HEAD 61e3dc5b by architect-ac8532da 2026-08-27 ~06:20Z; no bytes touched, no plan submitted. (1) BUILD-ORDER PREMISE FALSE: `git ls-files apps/control-room/src/v2/projects` = 8, not 10 - project-manager-app.{tsx,test.tsx} are still `??` and were carved into task-999a363f (P1.2b-ii-b, PLANNING, architect-e0ebd2e5) because app.tsx needs the `scope` hunk in the dirty pairing-confirmation.tsx. main.tsx's dirty hunk imports ./v2/projects/project-manager-app.js, so this consumer cannot land first. (2) HUNK MAP main.tsx (68/28 dirty): PROJECT-MANAGER hunks = imports ProjectManagerApp / connectProjectManager / ProjectManagerConnection, chooseRoot params (managerMode, managerSetup) + `<ProjectManagerApp prepared={managerSetup} />` branch, prepareV2LiveSetup(search, managerMode) early-return, prepareProjectManager(), exported isProjectManagerLocation(hostname, search, development) = hostname === \"127.0.0.2\" || (development && ?projects=1), mount wiring. FOREIGN hunks interleaved in the SAME functions = `/// <reference types=\"vite/client\" />`, gateDevelopmentQuery from ./entry-route.js (UNTRACKED with entry-route.test.ts), DevelopmentLegacyRoot lazy import of ./development-legacy-root.js (UNTRACKED), removal of resolveLiveSetupFromBuild/resolveShellMode/ShellModeRoot imports, import.meta.env.DEV gating of ?v1/?fixtures, doc-comment rewrites. A MODE 2 blob must be HEAD + only the project-manager pieces; note isProjectManagerLocation's call site passes import.meta.env.DEV, so the blob needs the vite/client reference (HEAD main.tsx never reads import.meta.env) or another development signal. (3) HUNK MAP cordum-app.tsx (65/47 dirty): PROJECT hunks = ProjectBoundary import (project-boundary.tsx IS tracked at 413d1573), `const projectId = attached?.projectId ?? null` (LiveSetupOk.projectId exists at HEAD live-config.ts:34), the document.title effect, the `content` wrapper `<ProjectBoundary projectId={projectId} />{body}`, and `projectId={projectId}` on CordumShell - that last prop exists ONLY in the dirty cordum-shell.tsx (+6/-4: optional projectId prop + data-project-id attribute), so it is either a 4th owned path or dropped from the blob. FOREIGN hunks = GoalCreateResult (not at HEAD goal-model.ts), DevelopmentFixtureGoals lazy import of ./goals/fixture-goals-view.js (UNTRACKED), OpenBoard.runId / eyebrow / connection-state changes (P2.15 family names cordum-app.tsx in prose). (4) OWNERSHIP: no live row's file fields name main.tsx or cordum-app.tsx; prose mentions only (P1.2b, P1.2b-ii, P1.2b-ii-b, P2.15, P2.15c, P2.15d).",
      "whatRemains": "Wait for task-999a363f (and task-0602f6c2 DONE). Then plan: DoD-1 gate = v2/projects tracked count 10 AND `git show HEAD:apps/control-room/src/main.tsx | grep -c ProjectManagerApp` = 0; MODE 2 blobs for both files built from `git show HEAD:<path>` + only the project-manager pieces (never the worktree copy), audited both ways with `git diff --no-index` (HEAD->blob = project hunks only; blob->worktree = foreign hunks only), proven in a `git archive` materialization (copy the two blobs in, `pnpm --filter @moe/control-room typecheck`, the new test) BEFORE committing; temp-index bare commit (read-tree / update-index --cacheinfo / write-tree / commit-tree -p / update-ref with old-value guard) then `git reset -q -- <paths>` on the shared index; one new focused test file pinning isProjectManagerLocation (hostname true; ?projects=1 true only with development; false in production; ordinary false) plus a render arm; drill = drop `development &&`. Only one mode-2 row in flight at a time (task-48ee1455 is one).",
      "pitfalls": "LIVE control-room typecheck is RED at 06:20Z (live-board.tsx:63/132 and live-board-dispatch.test.tsx: TS2554 'Expected 2-3 arguments, but got 4' and TS2353 'planningGoalRef' does not exist in type 'DispatchInput') - attribution posted in #general; do not read a green as this row's. Both owned files carry three or more foreign feature sets; a pathspec commit or `git add` of either sweeps them (66c1a3f7 class). Sandbox hook blocks pwsh Remove-Item/rmdir on temp trees; use Bash `rm -rf`.",
      "openQuestions": "Whether the cordum-shell.tsx projectId hunk is this row's 4th path or dropped from the blob (dropping it keeps the row at three paths and loses only the data-project-id attribute). Whether the isProjectManagerLocation test should import main.tsx (module-level mount side effect: it throws CONTROL_ROOM_ROOT_MISSING without #root) or the function should move to a small module - the filer's rail allows only three owned paths, so the test needs a jsdom document with a #root element before importing main.tsx, or the rail must be widened by a comment.",
      "releasedAt": "2026-08-27T06:19:35.705Z",
      "releasedBy": "architect-ac8532da",
      "reason": "Explicit architect handoff per the build-order rail: the row's main.tsx hunk imports ./v2/projects/project-manager-app.js, which is still untracked and owned by task-999a363f (P1.2b-ii-b, being planned by architect-e0ebd2e5). Planning it now would strand a worker at DoD-1 (8 of 10 v2/projects paths tracked). Re-plan after task-999a363f lands; measurements below."
    }
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-27T11:32:13.890Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED: the filed adoption shape (mode-2 blobs from the dirty entrypoints + temp-index commit + two skip-worktree bits) — the human cleaned the worktree at ~10:13Z (comment-2f928fae, human-confirmed): main.tsx / cordum-app.tsx / cordum-shell.tsx are byte-equal to HEAD and the foreign hunks no longer exist, so review measured that the S-bit route would have hidden a worktree BEHIND HEAD and blocked the next owner's `git add`. This is now an AUTHORING row in mode 1. REJECTED: testing `isProjectManagerLocation` by importing main.tsx alone — its module-level mount throws without #root; the pure function moved to a small seam module (`entry-project-manager.ts`) so the guard has a side-effect-free arm and main.tsx needs no vite/client reference. REJECTED: the draft's presence-only render arm — review measured it GREEN when `managerMode ||` is removed from prepareV2LiveSetup; the arm now records fetch call order (`/manager/bootstrap` vs `/bootstrap`) and D2 drills that exact mutant. REJECTED: keeping the eyebrow at HEAD:152 — TS2448/TS2454 temporal-dead-zone on `projectId`; the line moves below `const attached`. REJECTED: the CordumShell `projectId`/data-project-id prop — absent at HEAD, loses no tracked assertion; disclosed follow-up. Review: 8-agent workflow, 21 held; 6 refuter findings were unjudged (judge agents hit the session limit) and were judged by the architect from the refuters' evidence — all six folded.",
    "codebaseInsights": "MEASURED 2026-08-27 at HEAD 36f31348. v2/projects tracked 10/10 (413d1573 + 357d3635); HEAD main.tsx (101 lines) and cordum-app.tsx have zero project-manager references and are CLEAN in the worktree (hashes 9b862409 / a4d763b2 == HEAD). Foreign dirty control-room files (live-board.tsx, live-dispatch.ts, goals/*, nav-rail.tsx, cordum-goals.css, tests) and untracked entry-route.ts / development-legacy-root.tsx / fixture-goals-view.tsx are not imported by this row's code. `attached` is defined at HEAD cordum-app.tsx:157 and `LiveSetupOk.projectId: string | null` at live-config.ts:34. tsconfig types = [\"node\"]; `/// <reference types=\"vite/client\" />` precedent at board-surface.tsx:1. The manager client bootstraps via `/manager/bootstrap` (client.ts:225); the v2 handshake requests `/bootstrap`; ProjectManagerApp's initial heading is `Connecting to project manager` (app.tsx:68). `pnpm --filter @moe/control-room test` discovers apps/control-room suites despite the root include list (review measured); live control-room typecheck is RED on foreign goals files (25 errors), so only the materialization gates count. Signing: commit.gpgsign=true, gpg.format=ssh — a normal pathspec commit signs itself.",
    "risks": "(1) If either entrypoint is dirty again at claim, stop — do not adopt reappeared bytes; report. (2) The live typecheck is red for foreign reasons; never cite a live green or red as this row's. (3) Keep `?projects=1` behind `import.meta.env.DEV` — the production guard is the security property (D1). (4) Manager mode must suppress the v2 handshake (D2) or the browser starts a pairing request it never uses. (5) Do not touch affordance-read.ts (S bit, task-48ee1455). (6) HEAD moves fast; re-read at execution. (7) Every declared list ⊆ the four owned paths.",
    "keyFiles": [
      "apps/control-room/src/main.tsx",
      "apps/control-room/src/v2/cordum-app.tsx",
      "apps/control-room/src/entry-project-manager.ts",
      "apps/control-room/src/entry-project-manager.test.tsx",
      "apps/control-room/src/v2/projects/project-manager-app.tsx",
      "apps/control-room/src/v2/projects/project-manager-client.ts",
      "apps/control-room/src/v2/projects/project-boundary.tsx",
      "apps/control-room/src/live/live-config.ts",
      "apps/control-room/src/board/board-surface.tsx"
    ]
  },
  "budget": {
    "wallClockMs": 10800000,
    "warnedAt": "2026-08-27T11:50:53.489Z",
    "escalatedAt": "2026-08-27T11:50:53.489Z"
  },
  "planApprovedAt": "2026-08-27T11:32:15.977Z",
  "workStartedAt": "2026-08-27T11:51:28.004Z",
  "reviewStartedAt": "2026-08-27T12:11:00.426Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": "$ vitest run\n\n RUN  v4.1.10 C:/Users/Yaron/AppData/Local/Temp/worker-ca97b7bd/delivered2/apps/control-room\n\n\n Test Files  88 passed (88)\n      Tests  1115 passed (1115)\n   Start at  15:09:15\n   Duration  9.00s (transform 22.42s, setup 0ms, import 41.84s, tests 49.43s, environment 97.76s)\n\nVERIFICATION_EXIT=0\n\n[run from %TEMP%\\worker-ca97b7bd\\delivered2, a `git archive HEAD` materialization of commit cccf133e with `pnpm install --frozen-lockfile --prefer-offline` EXIT 0. Repo HEAD = cccf133e and `git merge-base --is-ancestor cccf133e HEAD` exit 0 at run time. Inclusion of the new file proved, not assumed: `vitest list --filesOnly` prints src/entry-project-manager.test.tsx and its verbose run lists all 8 arms, `Tests 8 passed (8)`. The other two decisive legs on the same tree: `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo plus a planted-error positive control that went EXIT 1 naming both new files; `pnpm --filter @moe/control-room build` EXIT 0, `built in 215ms`.]",
    "reportedAt": "2026-08-27T12:11:00.426Z"
  },
  "filesModified": [
    "apps/control-room/src/main.tsx",
    "apps/control-room/src/v2/cordum-app.tsx",
    "apps/control-room/src/entry-project-manager.ts",
    "apps/control-room/src/entry-project-manager.test.tsx"
  ],
  "completedAt": "2026-08-27T12:22:20.530Z",
  "reviewCompletedAt": "2026-08-27T12:22:20.530Z",
  "reviewSummary": "APPROVED at cccf133e. FULL EVIDENCE: comment-b731f064. Every leg re-run by me in %TEMP%\\qa-2eabba3d\\delivered.\n\nSHAPE: numstat = exactly the 4 owned paths, 277+/12-. Whole diff read: zero foreign hunks in the entrypoints, no daemon/v2-projects edits. Committed==worktree bytes; ls-files -v all H; foreign goals WIP preserved dirty.\n\nDoD-1 re-measured at PARENT 5f7b3d5a: v2/projects=10, ProjectManagerApp=0, ProjectBoundary=0. DoD-2/3 graded against the mode-2 -> mode-1 amendment (comment-2f928fae, comment-0481dc09).\n\nDoD-4: verbose run lists all 8 arms EXECUTING, Tests 8 passed (8). I re-ran all 3 drills, one variable each, restored + SHA-verified: D1 drop `development &&` reds exactly \"refuses ?projects=1 in a production build\", other 7 green; D2 drop `managerMode ||` -> expected ['/bootstrap','/manager/bootstrap'] to deeply equal ['/manager/bootstrap']; D3 branch order -> Unable to find cr.manager.root. The disclosed GREEN non-detection is genuinely closed.\n\nDoD-5 (my materialization, install EXIT 0): typecheck EXIT 0 with `$ tsc` echo plus a planted-error control redding both new files; test EXIT 0, 88/88 files, 1115/1115 tests; build EXIT 0.\n\nSECURITY: proven in bundle bytes (token grep vacuous, minified strings backticked): p(e,t){return f(e,t,!1)} - literal false, so ?projects=1 cannot select the manager in production. Only production caller is main.tsx:146 mountControlRoom(container), no hostname arg: the test seam is not a bypass.\n\nFOREIGN RED re-run not read: live EXIT 1 on goals-home.test.tsx, deps ` M` peer WIP, zero import edges to owned paths, delivered tree 88/88 green -> owned delta empty.\n\nDoD-6: steps subset of the 4 paths; consumer edge on disk and on task-0602f6c2.\n\nFollow-up not a hold: entry-project-manager.ts:41 claims the query branch is \"eliminated outright\" in production; it is not - behaviour right, comment wrong."
}