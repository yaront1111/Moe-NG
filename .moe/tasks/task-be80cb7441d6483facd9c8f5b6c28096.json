{
  "id": "task-be80cb7441d6483facd9c8f5b6c28096",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[P2.15d-i-pre-budget-2] Budget commitment consumers: supersede compares commitment-to-commitment, expansion criteria proof, seeds re-issued, and the BUDGET_REF slot in readApprovalRecordFacts",
  "description": "CONSUMER ROW split (SPIDR) from task-61a2e8ad (ruling (A), comment-87ad84c1 on task-ba102165) so the producer row stays under the 10-file cap. After task-61a2e8ad lands `budgetCommitmentMaterial` / `budgetCommitmentDigest` / `verifyBudgetCommitment` (apps/daemon/src/budget/budget-commitment.ts) and the activation bind-back (BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH @ DAEMON_PREREQUISITE), `ApprovalDecisionRecord.budgetRef` MEANS a decide-time commitment, no longer the activation root digest. Ruling condition 2 (consumer re-measure) is discharged here.\n\nCONSUMERS MEASURED AT HEAD 24ac4ae9 (`git grep -n budgetRef HEAD -- apps packages ':!*test*'`):\n1. apps/daemon/src/planning/graph-supersede-approval-binding.ts:73 `if (record.budgetRef !== predecessor.boundHashes?.budgetHash)` — a DIGEST comparison that refuses every commitment-carrying successor approval; re-anchor to compare the successor record's budgetRef with the commitment recomputed from the predecessor's durable material through the SHARED builder (bind-back, no boundHashes shape change), keeping GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH's exact code/layer.\n2. packages/core/src/expansion/expansion-approval.ts:227 `record.budgetRef !== criteria.budgetRef` and expansion-preparation.ts:83/:140/:242 (criteria.budgetRef validHex64) — prove by arm which notion the approved criteria carry and re-anchor the writer of criteria.budgetRef to the commitment if it copies the record.\n3. apps/control-room/src/approvals/approval-detail-plan.tsx:59 — display only; prove unaffected (label may say \"Budget commitment\").\n4. Seeds: apps/daemon/src/orchestrator/demo-seed-payloads.ts:395 and apps/control-room/src/live/live-dispatch.ts:198 carry `budgetRef: hex64(\"bb\")` — after the bind-back these approvals refuse at activation; re-issue them from the production commitment (never a literal) so the demo lane's live journey activates.\n5. readApprovalRecordFacts (task-ba102165's approval-record-facts.ts): fill the BUDGET_REF slot with `budgetCommitmentDigest(budgetCommitmentMaterial(...))`, absence -> APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE with upstream — gated on ba102165 DONE.\n\nUNPARK: task-61a2e8ad DONE on disk AND `git grep -n 'export function verifyBudgetCommitment' HEAD -- apps/daemon/src/budget/` EXIT 0. Item 5 additionally gates on task-ba102165 DONE.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, in a row comment with `git rev-parse HEAD` quoted, greps unpiped with exit codes plus a positive control: task-61a2e8ad DONE on disk AND `git grep -n 'export function verifyBudgetCommitment' HEAD -- apps/daemon/src/budget/` EXIT 0; the four consumer sites above still present at HEAD (re-measure the line numbers); for item 5, task-ba102165 DONE and `readApprovalRecordFacts` present — otherwise deliver items 1-4 and record item 5 as gated. Porcelain clean on every owned path; live ownership scan.",
    "graph-supersede-approval-binding.ts compares the successor record's budgetRef with the commitment recomputed from the predecessor's durable material via the shared builder (never a second material list, never boundHashes shape change); a successor approval carrying the true commitment is admitted, a flipped-hex one refuses GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH with its existing code+layer; the existing supersede arms stay green.",
    "Expansion: an arm proves what notion `criteria.budgetRef` carries at the delivered tree and the writer of approved criteria copies the commitment; expansion-approval.ts:227 admits a commitment-carrying record and refuses a mismatched one with EXPANSION_APPROVAL_BUDGET_MISMATCH (exact code).",
    "Seeds re-issued from production: demo-seed-payloads.ts and live-dispatch.ts derive the approval's budgetRef from `budgetCommitmentDigest(budgetCommitmentMaterial(...))` over the seeded store (never `hex64(\"bb\")`); the demo lane's live journey activates through the bind-back; the control-room display label/proof updated.",
    "If ba102165 is DONE: readApprovalRecordFacts derives the BUDGET_REF slot from the commitment (absence -> APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE @ DAEMON_APPROVAL_INTENT with upstream) and its arm proves equality with the activation bind-back's expected value through production on both sides.",
    "Mutation drills (one field of the predecessor material -> supersede arm reds; revert a seed to the literal -> the demo activation arm reds) with SHA256 restore proofs; gates standalone/unpiped with counts (`pnpm --filter @moe/daemon typecheck`, focused daemon suites, `pnpm --filter @moe/control-room test` if the UI changed, core expansion suites via `pnpm test`), path-attributed; explicit-pathspec commit; completion comment names task-61a2e8ad and task-ba102165 and states 'every budgetRef consumer now reads the commitment notion; no digest comparison remains'."
  ],
  "taskRails": [
    "Compose budget-commitment.ts's builder everywhere; never re-list the material fields; never change the boundHashes persisted shape.",
    "Seeds derive from production, never literals; the demo lane must activate through the real bind-back.",
    "Keep every consumer's existing refusal code/layer; re-anchor comparisons, do not widen them.",
    "Sequenced after task-61a2e8ad (and task-ba102165 for the seam slot); explicit-pathspec commits; foreign WIP preserved; <=250 lines per production source."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK (DoD-1) — read-only, ONE moe.add_comment, `git rev-parse HEAD` quoted, greps unpiped with exit codes + a positive control. (a) `git grep -n 'export function verifyBudgetCommitment' HEAD -- apps/daemon/src/budget/` EXIT 0 (producer landed; also quote budgetCommitmentMaterial :151 and budgetCommitmentDigest :175). (b) Re-measure the four consumer sites: graph-supersede-approval-binding.ts:73 digest comparison still present; expansion-approval.ts:227 and expansion-preparation.ts:83/:140/:242; approval-detail-plan.tsx:59; demo-seed-payloads.ts:395 and live-dispatch.ts:198 still `hex64(\"bb\")` — any site already re-anchored means a peer landed part of this row: adopt/verify, never duplicate. (c) task-ba102165 DONE and `git grep -n 'export function readApprovalRecordFacts' HEAD -- apps/daemon/src/planning/` EXIT 0; ALSO measure whether task-3b61860f has landed its walk: `git grep -n 'firstMissingApprovalFact' HEAD -- apps/daemon/src/planning/approval-record-facts.ts` — this row depends on it (dependsOn declared); quote what you find. (d) Bind-back precedent quoted: approval-activation.ts:163-175's verifyBudgetCommitment call and how it constructs {approvedRun, goalRef, projectId} — that construction is what steps 2 and 4 mirror. (e) porcelain on every owned path; note foreign dirt and continue.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-supersede-approval-binding.ts"
      ],
      "note": "Verify-or-block PASSED at HEAD 3a9dc2cd0ea018c865b4eb89d1ce92a008faec2e. Full evidence in comment-a4b089cc. Read-only; no bytes authored.\n\nAll three prerequisite rows are DONE on disk (61a2e8ad, ba102165, 3b61860f) and the producer is at the exact lines the plan named: budget-commitment.ts :151 budgetCommitmentMaterial, :175 budgetCommitmentDigest, :193 verifyBudgetCommitment (EXIT 0). All four consumer sites are still un-anchored at HEAD, so no peer landed part of this row: supersede :73 is still the digest comparison, expansion-approval.ts:227 still compares record.budgetRef to criteria.budgetRef, approval-detail-plan.tsx:59 is display-only, and both seeds still carry hex64(\"bb\"). Item 5 is UNGATED and genuinely unfilled — approval-record-facts.ts declares budgetRef at :55 and guards it at :159 but has no producer; 3b61860f's firstMissingApprovalFact walk is landed (:136 call, :175 declaration). Bind-back precedent quoted verbatim from approval-activation.ts:168-175, with a second agreeing caller at graph-activation-service.ts:185. Positive control: `git grep -c budgetRef HEAD -- apps/daemon/src` EXIT 0 across 19 files.\n\nONE MEASURED COLLISION, raised rather than worked around. Porcelain over the owned paths is clean except ` M apps/control-room/src/live/live-dispatch.ts`, which task-4942111bdaac427985e4636b2bbd0cad (named in my claim receipt) is mid-SPLIT on: 71 insertions / 392 deletions, new imports from live-board-feed.js, live-command-dispatch.js and live-dispatch-payloads.js. The seed literal this row must re-issue has MOVED out of the file the plan names — `grep -c 'hex64(\"bb\")' apps/control-room/src/live/live-dispatch.ts` is 0 in the worktree and the line is now at the UNTRACKED apps/control-room/src/live/live-dispatch-payloads.ts:204. Editing live-dispatch.ts from its HEAD content would destroy the peer's refactor; editing their untracked file would be adopting WIP the rails forbid. The daemon-side seed at demo-seed-payloads.ts:395 is clean and unambiguously this row's.\n\nSteps 2 and 3 are unaffected and start now; step 4's live-dispatch clause goes to #general for a routing call while the dependency-free work proceeds.",
      "startedAt": "2026-08-29T23:06:38.569Z",
      "completedAt": "2026-08-29T23:08:19.849Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — SUPERSEDE COMMITMENT-TO-COMMITMENT (DoD-2). In apps/daemon/src/planning/graph-supersede-approval-binding.test.ts: (i) a successor approval whose budgetRef IS the commitment (derived through PRODUCTION budgetCommitmentDigest(budgetCommitmentMaterial(store, <query mirrored from approval-activation.ts:163-175>)) over the predecessor's durable state) is ADMITTED past the budget check (fails today — the :73 comparison expects boundHashes.budgetHash); (ii) a flipped-hex budgetRef refuses GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH with the EXISTING code+layer; (iii) every existing supersede arm stays green (revision/scope/criteria/quality/policy checks untouched). THEN GREEN in graph-supersede-approval-binding.ts: replace the :73 comparison's RIGHT-HAND SIDE with the commitment recomputed from the predecessor's durable material via the SHARED builder — compose budgetCommitmentMaterial + budgetCommitmentDigest, NEVER a second material list, NEVER a boundHashes shape change (the persisted record is read, not rewritten); a material refusal maps to the same GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH refusal path or a refusal carrying the builder's upstream — keep the existing refusal vocabulary, widen nothing. If GenesisApprovedRun is unreachable from durable state at this seam (risk 1), STOP and report rather than threading a caller value. File is 102 lines — stays well under 250.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
        "apps/daemon/src/planning/graph-supersede-approval-binding.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/budget/budget-commitment.ts",
        "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
        "apps/daemon/src/planning/graph-supersede-approval-binding.test.ts",
        "apps/daemon/src/planning/graph-supersede-test-fixtures.ts"
      ],
      "note": "SUPERSEDE NOW COMPARES COMMITMENT TO COMMITMENT (DoD-2). Risk 1 fired, was ruled, and is discharged; the fan-out was found, reported and granted rather than routed around.\n\nRED FIRST. New arm \"admits a successor approval whose budgetRef is the durable commitment\": before the production change it refused with `Error: GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE` — `Tests 1 failed | 14 passed (15)`.\n\nRISK 1, RESOLVED BY RULING NOT BY THREADING. `GenesisApprovedRun.runBinding` is unreachable at this seam — no runId in GraphSupersedeRequest, SupersedeFacts, GraphRevisionState or GraphActivationBinding — and the step forbids threading a caller value, so I stopped and reported (comment-e48ab573). governor-0134a004 ruled option (a) (comment-8bb494c9) and granted budget-commitment.ts for the narrow helper only. Delivered exactly that: `budgetCommitmentMaterialForActiveGraph(store, {goalRef, projectId})` composing `readBudgetBinding` directly, with the EXISTING assembly extracted into one private `materialFrom(bound, goalRef, projectId)` that BOTH entry points call — one material list, one digest, no second builder, and `budgetCommitmentMaterial` behaviourally unchanged. The helper's doc records WHY the strict reader is the only one that could answer here: the genesis fallback fires only for a project that has never activated a graph, the opposite of a supersede seam. budget-commitment.ts 239 lines.\n\nGREEN. graph-supersede-approval-binding.ts:73's right-hand side is the commitment recomputed from the predecessor's own durable material via the shared builder. Same code, same layer; an upstream is attached only when the builder itself refused, so the vocabulary is unchanged and nothing widened. boundHashes is READ, never rewritten — no shape change. File 118 lines.\n\nTHE UNNAMED CONSUMER, measured and granted. graph-supersede-test-fixtures.ts:323 minted `budgetRef: binding.budgetHash`, the retired root-digest notion, for every successor approval in the suite. With the re-anchor that reddened 24 arms across 7 files (`Test Files 7 failed (7)` / `Tests 24 failed | 107 passed (131)`): graph-supersede-service 10, graph-supersede-approval-binding 7, active-graph-slot 2, graph-supersede-authority 2, active-graph-slot-ordering 1, foundation-context-prelaunch 1, daemon-command-graph-edges 1. I stopped and reported (comment-fe04e8d4) rather than take the unowned path, explicitly rejecting the alternative of overriding budgetRef inside my own test file — that would have left six suites red and the shared fixture still minting a retired notion. governor-0134a004 granted the path limited to :323 (comment-3acb9620). The fixture now derives through the same shared builder via a small local helper. AFTER: `Test Files 7 passed (7)` / `Tests 131 passed (131)`.\n\nONE HONEST CORRECTION, worth recording because it is a trap. My arm's non-vacuity guard was `expect(commitment).not.toBe(bound.budgetRef)`. That was decisive BEFORE the fixture fix and became a FIXED POINT after it — the fixture now mints the commitment too, so the guard was comparing the arm against itself and it failed loudly. I replaced it with a comparison against the RETIRED notion: a local `retiredBudgetNotion(store)` reads the predecessor's persisted boundHashes.budgetHash through the same production replay the matcher used to use, and the arm asserts the commitment differs from it. That guard stays meaningful permanently, and if the two digests ever aliased — they are domain-separated precisely so they cannot — the arm would say so instead of silently passing.\n\nGATES. From apps/daemon `pnpm exec vitest run --root . --config package.json src/planning/graph-supersede-approval-binding.test.ts` EXIT 0, `Tests 15 passed (15)`. All seven fixture consumers EXIT 0, `Tests 131 passed (131)`. `pnpm --filter @moe/daemon typecheck` EXIT 1 with exactly ONE error, and it is FOREIGN: `src/http/http-listener-gate-1-bearer.test.ts(236,54): error TS2339: Property 'requestBytes' does not exist on type 'CommandDecisionRecord'`. That file is UNTRACKED (`??`) and does not exist at HEAD (`git cat-file -e HEAD:...` exit 128) — it is task-298eb95c's in-flight Gate-1 bearer work, the same row this worker is separately blocked on. Zero of the typecheck errors touch this row's paths (`grep -c \"planning/\\|budget/\"` over the log is 0). The daemon typecheck was EXIT 0 earlier in this same step, before that peer file appeared, which dates the red to the peer and not to this diff.",
      "startedAt": "2026-08-29T23:20:51.143Z",
      "completedAt": "2026-08-29T23:26:53.717Z"
    },
    {
      "stepId": "step-3",
      "description": "EXPANSION NOTION PROOF (DoD-3) — measure FIRST, edit only what the measurement demands. (1) Find the production writer of approved criteria's budgetRef (`git grep -n 'budgetRef' HEAD -- apps/daemon/src ':!*test*'` — planning-authority-persistence.ts is the suspect; quote the write site). (2) IF the writer copies record.budgetRef verbatim: expansion-approval.ts:227's record-vs-criteria equality is notion-agnostic and needs NO production change — prove it with an arm: an approval record + criteria pair BOTH carrying the same production-derived commitment digest passes :227; a mismatched pair refuses EXPANSION_APPROVAL_BUDGET_MISMATCH (exact code asserted). Put the arm in core's existing expansion-approval test file, driven through the production validator surface. (3) IF the writer mints its own digest (not a copy): re-anchor THAT writer to copy the record's commitment (one-line class of change, in the daemon writer file — declare it via moe.declare_files, it is inside this row's scope), and the arm proves the pair now agrees. (4) expansion-preparation.ts validHex64 gates (:83/:140/:242) admit any 64-hex — no change; state that in the step note. Record which branch (2) or (3) was taken and why, quoting the measured writer lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/core/src/expansion/expansion-approval.test.ts"
      ],
      "modifiedFiles": [
        "packages/core/src/expansion/expansion-approval.test.ts"
      ],
      "note": "EXPANSION NOTION PROOF (DoD-3). BRANCH (2) TAKEN — no production change — but the measurement is stronger than the step's phrasing, so I am recording what is actually true.\n\n(1) THE WRITER MEASUREMENT. `git grep -n 'budgetRef' HEAD -- apps/daemon/src ':!*test*'` EXIT 0 returns 9 hits and NONE of them writes `criteria.budgetRef`: budget-commitment.ts:4 (doc), demo-seed-payloads.ts:395 (the seed literal), approval-activation.ts:163/:175 (bind-back), approval-record-facts.ts:55/:159 (type + guard), graph-activation-service.ts:158/:188 (bind-back), graph-supersede-approval-binding.ts:73 (the comparison step 2 re-anchors). planning-authority-persistence.ts — the step's named suspect — does not appear at all. The same grep over packages/core/src ':!*test*' returns expansion-approval.ts:227, expansion-preparation.ts:83/:140/:242/:244, and the approval-record contract/validator rosters (approval-contract.ts:49, approval-validation.ts:25/:33). So the honest finding is neither branch (2)'s \"copies verbatim\" nor branch (3)'s \"mints its own digest\": NO production module mints `criteria.budgetRef` at all. It arrives inside the sealed criteria payload and expansion-preparation.ts only SHAPE-validates it — :242 `validHex64(value[\"budgetRef\"])`, :244 passes the caller's value through unchanged.\n\nCONSEQUENCE, which is branch (2)'s conclusion by a different route: expansion-approval.ts:227 `record.budgetRef !== criteria.budgetRef` is an equality between two opaque 64-hex values and is NOTION-AGNOSTIC. Once approvals carry commitments, both sides carry commitments and the comparison keeps working with no production change. Being agnostic is now load-bearing rather than incidental, which is why it gets an arm.\n\n(2) THE ARMS, added to the existing core test file (no new module), driven through the production `approveExpansionManually` surface: a new describe \"expansion manual approval budget notion (task-be80cb74)\" with (i) an ADMIT arm where the preparation's frozen criteria AND the approval both carry a commitment-shaped digest, asserting `result.ok` true and that the bound decided approval carries that same ref; (ii) a REFUSE arm flipping one nibble on the approval side only, asserting EXPANSION_APPROVAL_BUDGET_MISMATCH with layer BINDING and component EXPANSION_APPROVAL through the file's own expectApprovalRefusal helper (which additionally pins byte-identical inputs, the exact result key set and deep freezing).\n\nThe digest is deliberately NOT a repeated nibble like every other fixture in the file, so an implementation that special-cased uniform hex would not survive. Its provenance is deliberately not reproduced: core cannot import the daemon's builder, and re-deriving the domain-tagged digest inside a core test would assert against a reimplementation rather than the production surface, which epic rail 6 forbids by name. Provenance-independence IS the property under test, so this is the correct shape rather than a compromise.\n\n(3) NON-VACUITY DRILL, run now rather than deferred. Backup to %TEMP% suffixed with my worker id; pre-drill SHA256 6c658beea75f2594f712c2d946928b981b2976bce94072c360a25be0ac8c7b97 matching the backup. Deleting the :227 comparison (replacing it with an always-false guard) reds TWO arms: my new \"refuses a commitment that disagrees with the frozen criteria by one nibble\" AND the pre-existing table arm \"refuses when 'approval.budgetRef' does not match the stored identity\" — `Tests 2 failed | 92 passed (94)`. Restored, SHA256 back to 6c658bee in a separate call, porcelain over packages/core/src/expansion shows only my test file dirty, and the suite is green again.\n\n(4) expansion-preparation.ts's validHex64 gates at :83, :140 and :242 admit ANY 64-hex value, so a commitment satisfies them exactly as the old digest did. No change, as the step predicted.\n\nGATE: `pnpm exec vitest run packages/core/src/expansion/expansion-approval.test.ts` EXIT 0, `Test Files 1 passed (1)` / `Tests 94 passed (94)` — up from 92, the two arms added here.",
      "startedAt": "2026-08-29T23:10:21.827Z",
      "completedAt": "2026-08-29T23:12:48.076Z"
    },
    {
      "stepId": "step-4",
      "description": "SEEDS RE-ISSUED FROM PRODUCTION (DoD-4). RED FIRST: the demo activation arm — extend the existing demo/bootstrap journey suite (bootstrap-finalize-journey.test.ts or demo-seed-env.test.ts, whichever already boots the seeded store; name it in the note) with an arm that walks the demo lane's approval into activation and asserts it PASSES the bind-back (today it refuses BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH @ DAEMON_PREREQUISITE because the seed carries hex64(\"bb\") — quote that red). THEN GREEN: (1) apps/daemon/src/orchestrator/demo-seed-payloads.ts:395 — `budgetRef` becomes the PRODUCTION derivation budgetCommitmentDigest(budgetCommitmentMaterial(store, <mirrored query>)) over the seeded store; the seed pipeline must run the derivation AFTER the genesis binding rows exist (risk 3) — if approvalRecord(input) has no store in scope, thread the derived value in from the seed step that HAS the store, never a literal; (2) apps/control-room/src/live/live-dispatch.ts:198 — same notion for the live fixture lane: derive or thread the production commitment; the control-room lane's existing tests stay green under `pnpm --filter @moe/control-room test`; (3) approval-detail-plan.tsx:59 — label the row \"Budget commitment\" (display-only; prove no other behavior change by the existing control-room suite). Neither over-cap file (411/440 lines) may GROW materially — the derivation replaces the literal in place; the pre-existing over-cap state is recorded in the completion comment as the epic-final follow-up, not fixed here.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/control-room/src/live/live-dispatch.ts",
        "apps/control-room/src/approvals/approval-detail-plan.tsx",
        "apps/daemon/src/bootstrap/bootstrap-finalize-journey.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/daemon/src/orchestrator/demo-seed-plan.ts",
        "apps/daemon/src/orchestrator/demo-seed-main.ts",
        "apps/daemon/src/orchestrator/demo-seed-surface.ts",
        "apps/daemon/src/orchestrator/demo-seed-surface.js",
        "apps/daemon/src/orchestrator/demo-seed-payloads.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-plan.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-main.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-finalize-journey.test.ts",
        "apps/daemon/src/http/affordance-read-planning.test.ts",
        "tests/e2e/foundation/j3-crash-harness.ts",
        "apps/control-room/src/live/live-dispatch-payloads.ts",
        "apps/control-room/src/live/live-dispatch.ts",
        "apps/control-room/src/live/live-board.tsx",
        "apps/control-room/src/live/live-app.tsx",
        "apps/control-room/src/live/live-board-dispatch.test.tsx",
        "apps/control-room/src/live/live-board.test.tsx",
        "apps/control-room/src/approvals/approval-detail-plan.tsx"
      ],
      "note": "SEEDS RE-ISSUED FROM PRODUCTION (DoD-4). Both literal sites retired; the demo lane activates. Measured at HEAD 1f2893d7.\n\nRED FIRST, quoted. New arm in `demo-seed-payloads.test.ts` (named per the step: this file, not bootstrap-finalize-journey.test.ts — it is the only suite that already drives the SEED'S OWN planned commands through `runBootstrapCommand` over a real store; the journey file drives `bootstrapSequence`, a different builder). Driving the shipped plan's `approval.decide` through `sendReviewed`:\n  `expected { advisoryOnly: true, …(6) } to match object { ok: true }` -> `\"code\": \"BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH\", \"ok\": false, \"refusedBy\": \"DAEMON_PREREQUISITE\"`\nExactly the refusal the step predicted. The shipped demo lane could not activate.\n\nTHE SHAPE OF THE FIX, and why it is not a one-line substitution. `approvalRecord(input)` has NO store and the commitment covers material that is not durable until the plan's own finalize terminal has committed — so the value cannot be computed at plan-build time by anyone. Threaded, per the step's own instruction:\n1. `approvalRecord(input, budgetRef)` — `budgetRef` is now a REQUIRED PARAMETER. The type says only a caller holding durable state can answer, so no future edit re-introduces a spelled ref without deleting the parameter.\n2. `DemoSeedInput.budgetRef: string | null`. `null` is not a default — it is \"I hold no commitment\", and the plan then OMITS `approval.decide` rather than spelling a placeholder. It omits for a DIFFERENT reason than `stopBeforeApproval` (capability vs policy) and both are kept.\n3. `runDemoSeed` is now two-phase: prelude with `budgetRef: null`, then `fetchBudgetCommitment(wire, runId)` (task-80b6bf7c's seam) once the finalize has committed, then the approval planned with WHAT THE DAEMON DERIVED. The seed derives nothing — a client-side derivation would be the second material list rail 1 forbids.\n\nBROWSER SITE, same notion, different transport. `DEV_PAYLOADS[\"approval.decide\"].record` no longer carries `budgetRef` at all; `payloadFor` is synchronous and pure so it CANNOT fetch one. `dispatchAffordance` reads it via task-952564b3's landed `readBudgetCommitment` and merges it in, injected through `DispatchInput.readBudgetCommitment` because live-dispatch holds no authenticated header set. `live-app.tsx` builds the real reader from `setup.headers`; `live-board.tsx` only threads it. FAIL-CLOSED with no reader: `BUDGET_COMMITMENT_READER_ABSENT @ CONTROL_ROOM_LIVE_DISPATCH`, never a fabricated ref.\n\nI edited `live-dispatch-payloads.ts`, NOT `live-dispatch.ts`, for the literal — per @qa-cc876c7b comment-2e8d1cc1 and @qa-063c9dff comment-6278d21a, anchored on `git grep -n 'hex64(\"bb\")'`. `live-dispatch.ts` IS edited, but for the reader wiring, which is the opposite of a vacuous discharge.\n\nLITERAL SITES AT THE WORKING TREE: `git grep -n 'hex64(\"bb\")' -- apps packages tests` returns ZERO production hits. Remaining hits are prose in comments plus two deliberate TEST INPUTS in demo-seed-payloads.test.ts (`not.toBe(hex64(\"bb\"))` and the divergence arm's input).\n\nARMS (12 new).\nDaemon, over a real ephemeral store: (a) activates when the approval carries `deriveApprovalBudgetRef`'s ref; (b) the plan CARRIES that exact ref — asserting only \"activation succeeded\" would stay green if the builder substituted another value the fence also accepts; (c) DIVERGENCE: `hex64(\"bb\")` refuses `BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH` / `refusedBy: DAEMON_PREREQUISITE`. Every earlier command has committed in that arm, so the material IS readable and the ref is the only thing wrong — which is why this code is reachable rather than an earlier absence code.\nSeed transport, over the loopback stub (taught `/budget/commitment/read`): (d) the dispatched approval carries the STUB's arbitrary ref — a client that recomputed or kept the literal answers a different string; (e) the read happens only AFTER all `DEMO_SEED_KINDS.length - 1` earlier commands committed; (f) a refused read stops the seed with the daemon's own code and `layer=DAEMON_PREREQUISITE`, dispatching no approval.\nBrowser: (g) no `budgetRef` in the base payload or in `payloadFor`'s output; (h) the READ ref reaches the builder, asked for the OFFER'S run not the operator's card; (i) a refusal travels out as `APPROVAL_AUTHORITY_UNSEALED @ APPROVAL_RUN_BINDING`, unrestated, nothing sent; (j) no reader = BUILD_REFUSED, nothing sent; (k) every other kind is untouched and attempts no read.\nDisplay: `approval-detail-plan.tsx:63` label `\"Budget\"` -> `\"Budget commitment\"`, with the reason in the code.\n\nSCOPE, DISCLOSED RATHER THAN BURIED. The step named 4 files; this landed 18. The extra ones are not discoveries I chose to absorb — they are what \"thread the derived value in from the seed step that HAS the store\" requires: the builder (`demo-seed-plan.ts`), the driver (`demo-seed-main.ts`), five `DemoSeedInput` fixture sites the required field reaches (`demo-seed-plan.test.ts`, `demo-seed-payloads.test.ts`, `bootstrap",
      "startedAt": "2026-08-29T23:27:28.538Z",
      "completedAt": "2026-08-30T06:14:01.656Z"
    },
    {
      "stepId": "step-5",
      "description": "THE BUDGET_REF SLOT (DoD-5) — runs only AFTER task-3b61860f is DONE (dependsOn enforces the WORKING gate; its walk `firstMissingApprovalFact` and the derived slots are the landed surface this step plugs into — re-measure at claim, adapt to what actually landed rather than re-introducing a second walk). RED FIRST in apps/daemon/src/planning/approval-record-facts.test.ts: with a seeded store where the commitment is derivable, `readApprovalRecordFacts` derives `derived.budgetRef` equal — through production on BOTH sides — to what verifyBudgetCommitment at the activation bind-back expects (assert equality against a digest recomputed via the shared builder, never a literal); with the genesis binding absent the walk answers APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE @ DAEMON_APPROVAL_INTENT carrying the builder's refusal as `upstream` (code+layer verbatim). THEN GREEN in approval-record-facts.ts: `ApprovalRecordFactsDerived` gains `readonly budgetRef?: string`; deriveFacts composes budgetCommitmentMaterial + budgetCommitmentDigest (upstream carried on refusal, field ABSENT — never defaulted, per the module's own ABSENCE IS NOT A VALUE doc); the walk map marks BUDGET_REF established when derived.budgetRef is present. File was 227 lines BEFORE 3b61860f's edits — re-measure; if this edit pushes it past 250, split the policy-ref/budget derivation half into a sibling module with a one-line `.js` bridge BEFORE 400 and declare the new paths.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/planning/approval-budget-ref.ts",
        "apps/daemon/src/planning/approval-budget-ref.js"
      ],
      "note": "THE BUDGET_REF SLOT (DoD-5) — filled, drilled, and split as the step sanctioned.\n\nRED FIRST, both arms, in approval-record-facts.test.ts: `Tests 2 failed | 21 passed (23)`. Arm 1 failed because `derived.budgetRef` was undefined; arm 2 failed with `AssertionError: expected undefined to deeply equal { …(2) }` because no upstream was carried.\n\nWHAT THE ARMS ASSERT. (1) \"derives a budget ref the activation bind-back itself accepts\" — over the production-seeded `reviewableStore()` (driveThrough \"approval.decide\"), it takes the derived ref and asks the PRODUCTION `verifyBudgetCommitment` — the same fence that guards activation — whether it accepts it. That is the strong form the DoD asks for: not \"equals a digest this test recomputed\", which any shared bug would satisfy, but the seam and the fence agreeing through production on both sides. It then additionally pins the ref to `budgetCommitmentDigest(budgetCommitmentMaterial(...))` so the notion is the commitment specifically. (2) \"leaves the slot ABSENT and carries the upstream when nothing durable answers\" — over a bare store the key is not present at all (`\"budgetRef\" in result.derived` is false, so `{}` and a zero digest stay different answers) and `upstream` is exactly `{ code: \"BOOTSTRAP_PREREQUISITE_MISSING\", layer: \"DAEMON_PREREQUISITE\" }`, the ledger reader's own code and layer.\n\nWHY THE ARMS GRADE `derived` AND NOT THE OK PATH. The roster order is RISK_TIER, STEP_UP, POLICY_REF, BUDGET_REF (approval-intent.ts:68-77) and RISK_TIER still has no producer — task-f42d5165 owns it — so the walk always stops there and BUDGET_REF is unreachable as `missing`. `ApprovalRecordFactsIncomplete.derived` exists precisely so derivation is gradable behind a refusal, which is what these arms use. Filling the slot is still real: FACT_ESTABLISHED's BUDGET_REF predicate now answers true.\n\nGREEN. `deriveApprovalBudgetRef` composes the run's OWN durable identity: `readApprovalIntentSources(store, projectId, runId)` supplies the verified ApprovedRunBinding, graphRevisionRef and goalRef, so a genuine `GenesisApprovedRun` is built from durable state with nothing fabricated and nothing presented by a caller (the request vocabulary is {projectId, runId}). It then composes the SHARED builder — `budgetCommitmentMaterial` + `budgetCommitmentDigest` — never a second material list. Refusals forward the code and layer of whoever said no: the ledger reader, the run binding, or the material builder.\n\nONE UPSTREAM SLOT, filled in ROSTER ORDER: the policy ref is listed before the budget ref, so its refusal wins when it has one; the budget upstream fills in otherwise. That is not a downgrade — the policy path can refuse with no upstream at all (which is why arm 2 saw `undefined` before this change), and a precise builder code beats nothing. The rule is stated in the code, not left to inference.\n\nI also corrected the slot's own doc comment, which asserted the ref could NEVER be set here because it is minted at activation. That stopped being true with task-61a2e8ad: the commitment covers material durable at decide time. Leaving the old prose would have left the next reader believing the slot is unfillable.\n\nTHE SPLIT, as the step instructed. The edit took approval-record-facts.ts to 253 lines, past the 250 cap. `deriveApprovalBudgetRef` and its result type moved to a new sibling apps/daemon/src/planning/approval-budget-ref.ts (55 lines) with the exact one-line LF bridge approval-budget-ref.js — the same split task-3b61860f took for the third slot into approval-policy-ref.ts. approval-record-facts.ts is back to 213 lines. Both new paths declared via moe.declare_files. The daemon bridge scanner is green with the new pair, which is what proves the bridge is exact and the module is classified runtime-tier.\n\nDRILL — HARDCODED RETURN, the mutant an equality-only assertion would miss. Backup to %TEMP% suffixed with the worker id; pre-drill SHA256 30637ebd6d11dae9e606dde5fbad18b00f486938eab523a67bb5bfc235518e0d matching the backup. Replacing the digest with `\"0\".repeat(64)` reds arm 1 with `AssertionError: BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH: expected false to be true` — the activation bind-back's OWN code, which is the receipt that the seam is graded by the production fence and not by a recomputation. `Tests 1 failed | 22 passed (23)`. Restored; SHA256 back to 30637ebd in a separate call; porcelain over apps/daemon/src/planning shows exactly the two ` M` files and the two `??` new ones, nothing else.\n\nGATES: `pnpm --filter @moe/daemon typecheck` EXIT 0 with `$ tsc --project tsconfig.json`; from apps/daemon `pnpm exec vitest run --root . --config package.json src/planning/approval-record-facts.test.ts src/runtime-entrypoint.test.ts` EXIT 0, `Test Files 2 passed (2)` / `Tests 28 passed (28)`.\n\nNOTE FOR STEP 2: while this step ran, governor-0134a004 ruled option (a) on the risk-1 escalation (msg-ca6b7664, comment-8bb494c9) and declared apps/daemon/src/budget/budget-commitment.ts as owned for `budgetComm",
      "startedAt": "2026-08-29T23:14:51.194Z",
      "completedAt": "2026-08-29T23:19:08.714Z"
    },
    {
      "stepId": "step-6",
      "description": "MUTATION DRILLS (DoD-6), one degree of freedom, restores proven by SHA256 vs %TEMP%\\be80cb74-<workerId>\\ backups for any UNTRACKED path and `git diff --exit-code` for tracked ones, proof in a call SEPARATE from the restore. (D1) flip one field of the predecessor material feeding the supersede recomputation (e.g. goalRef) -> step-2 arm (i) reds (admitted becomes refused) while arm (iii) stays green — proves the comparison reads the DERIVED commitment, not a stored hash. (D2) revert demo-seed-payloads.ts:395 to `hex64(\"bb\")` -> the demo activation arm reds with BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH; restore. (D3) in the supersede re-anchor, swap budgetCommitmentDigest for a raw sha256 over the same material (drop the domain tag) -> arm (i) reds — proves the DOMAIN-TAGGED digest is load-bearing, not just the material. (D4, only if step 5 ran) delete the budgetRef derivation from deriveFacts -> the BUDGET_REF slot arm reds on UNAVAILABLE-with-upstream vs derived. A GREEN drill is a FINDING — report it, never bank it. Record D1-D4 with red lines and restore proofs in ONE row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/daemon/src/planning/approval-record-facts.ts"
      ],
      "note": "MUTATION DRILLS D1-D6 (DoD-6). Six drills, six reds, six SHA256-proven restores. Full evidence in comment-ab950ee8. ZERO bytes authored by this step — every mutation was restored and proven.\n\nONE FINDING, which is the substantive part. D1 AS THE STEP WRITES IT DOES NOT TEST WHAT ITS TITLE CLAIMS. Mutating the QUERY (`goalRef` passed to `budgetCommitmentMaterialForActiveGraph`) reds arm (i), but through the ABSENCE branch: `GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE (BUDGET_COMMITMENT_MATERIAL_UNAVAILABLE/DAEMON_PREREQUISITE)`. A goal that does not exist has no durable binding, so the reader refuses and `record.budgetRef !== budgetCommitmentDigest(...)` is NEVER EVALUATED. Banking that red would certify the absence branch while believing the comparison was covered. The tell is the blast radius: 9 arms red, including arms that never reach a comparison at all.\n\nRE-RUN NARROWED to the clause the arm's title names — read left intact, one field of the MATERIAL feeding the digest flipped: `GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE`, upstream parenthetical GONE. Same code and layer, different branch, and the step's named control (\"refuses a single-field 'budgetRef' mismatch without residue\") STAYED GREEN as required.\n\nD2 seed literal (`approvalRecord` ignoring its new required parameter): 3 arms red across 2 suites, including the transport arm where the stub's arbitrary ref means only CARRYING it can pass — `expected 'bb000000…' to be '9c9c9c9c…'`. D3 domain tag dropped for a raw sha256 over the same material: arm (i) red, proving the tag is load-bearing and not just the inputs. D4 BUDGET_REF slot emptied: exactly 1 arm, correct blast radius (the absence arm must not move, and did not).\n\nD5/D6 ARE MINE, added because step 4 built a seam the plan predates. D5 skip the browser merge: 3 arms red, including both fail-closed arms flipping to ANSWERED — without the merge the board would SEND an approval carrying no budgetRef. D6 restate the daemon's refusal instead of carrying it: 1 arm red, `expected 'BUDGET_COMMITMENT_READER_ABSENT @ CONTROL_ROOM_LIVE_DISPATCH' to be 'APPROVAL_AUTHORITY_UNSEALED @ APPROVAL_RUN_BINDING'`. D5 alone would not have caught D6's mutant: the seam can be present and still lie about which layer refused.\n\nRESTORE DISCIPLINE. `git diff` was deliberately NOT used for `live-dispatch.ts` or `demo-seed-payloads.ts`: both are legitimately dirty with this row's step-4 work, so \"empty diff\" is the wrong assertion and a non-empty one proves nothing about which bytes. SHA256 against `%TEMP%\\be80cb74-worker-e98a7edb\\` backups, in calls SEPARATE from the restores, is the only proof that survives that. All five paths MATCH their pre-drill hash; porcelain over them reads exactly the same two ` M` entries as before drilling — no third file appeared, none flipped state.\n\nPOST-DRILL GREEN: daemon `Test Files 6 passed (6)` / `Tests 110 passed (110)` exit 0; control-room `Test Files 3 passed (3)` / `Tests 93 passed (93)` exit 0.",
      "startedAt": "2026-08-30T06:14:16.205Z",
      "completedAt": "2026-08-30T06:18:04.428Z"
    },
    {
      "stepId": "step-7",
      "description": "GATES + SELF-REVIEW + COMMIT + COMPLETION RECORD. GATES, standalone, unpiped, exit codes from their own runs, counts quoted: (G1) `pnpm --filter @moe/daemon typecheck` (quote `$ tsc`); (G2) from apps/daemon, `pnpm exec vitest run --root . --config package.json src/planning/graph-supersede-approval-binding.test.ts src/planning/graph-supersede-refusals.test.ts src/planning/approval-record-facts.test.ts src/bootstrap/bootstrap-finalize-journey.test.ts` (the `--root . --config package.json` form is mandatory); (G3) core expansion suites via root `pnpm test` scoped by the runner's include (quote the expansion test counts); (G4) `pnpm --filter @moe/control-room test` (UI changed — label + live-dispatch); (G5) `pnpm --filter @moe/daemon test` path-attributed per epic rail, foreign red disclosed verbatim. SELF-REVIEW in the row comment: (i) does ANY consumer still compare budgetRef against an activation-root-digest notion? (`git grep -n budgetRef` across apps+packages, classify every hit); (ii) is any material field list re-declared outside budget-commitment.ts? (rail 1 — grep BUDGET_COMMITMENT_MATERIAL_KEYS consumers); (iii) do the seeds derive strictly AFTER the genesis rows exist; (iv) per-file `wc -l` — owned files under 250 except the three PRE-EXISTING over-cap files (expansion-approval 319, demo-seed-payloads 411, live-dispatch 440), which must not have grown materially and are named as the epic-final split follow-up. COMMIT by EXPLICIT PATHSPEC — exactly the owned paths touched (porcelain them first; never the ~725 foreign dirty paths). COMPLETION COMMENT names task-61a2e8ad (producer) and task-ba102165 + task-3b61860f (seam family), quotes G1-G5 and D1-D4, records which branch step 3 took, and states EXACTLY: 'every budgetRef consumer now reads the commitment notion; no digest comparison remains'.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
        "apps/daemon/src/planning/approval-record-facts.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/daemon/src/orchestrator/demo-seed-plan.ts",
        "apps/daemon/src/orchestrator/demo-seed-main.ts",
        "apps/daemon/src/orchestrator/demo-seed-surface.ts",
        "apps/daemon/src/orchestrator/demo-seed-surface.js",
        "apps/daemon/src/orchestrator/demo-seed-payloads.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-plan.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-main.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-finalize-journey.test.ts",
        "apps/daemon/src/http/affordance-read-planning.test.ts",
        "tests/e2e/foundation/j3-crash-harness.ts",
        "tests/integration/control-room/live-approval-activation.test.ts",
        "apps/control-room/src/live/live-dispatch-payloads.ts",
        "apps/control-room/src/live/live-dispatch.ts",
        "apps/control-room/src/live/live-board.tsx",
        "apps/control-room/src/live/live-app.tsx",
        "apps/control-room/src/live/live-board-dispatch.test.tsx",
        "apps/control-room/src/live/live-board.test.tsx",
        "apps/control-room/src/approvals/approval-detail-plan.tsx"
      ],
      "note": "GATES + SELF-REVIEW + COMPLETION RECORD. Full evidence in comment-7644706693f440c6 (self-review i-iv, G1-G5 verbatim, attribution) and comment-ab950ee8 (drills D1-D6). The required sentence is stated there verbatim: every budgetRef consumer now reads the commitment notion; no digest comparison remains.\n\nSELF-REVIEW RESULTS. (i) 37 production `budgetRef` hits, every one classified: 3 compare a COMMITMENT (supersede + the two activation bind-backs), ZERO compare an activation-root digest; 1 notion-agnostic (expansion-approval.ts:227); 5 shape-only; 6 produce; 2 display — including `approval-fixtures.ts:106` `\"budget/envelope-19\"`, a UI sample that is not 64-hex and is never dispatched, flagged so nobody mistakes it for a payload. (ii) `BUDGET_COMMITMENT_MATERIAL_KEYS` has exactly TWO lines repo-wide, both inside budget-commitment.ts — no material list is re-declared anywhere; all four consumers compose the shared builder. (iii) the seeds derive strictly after the genesis rows, proven by a COMMITTED arm asserting `commandsBefore === DEMO_SEED_KINDS.length - 1` on recorded wire traffic, not by a claim. (iv) per-file counts recorded; nothing near the 400 split point.\n\nI RE-MEASURED THE STEP'S OWN LINE NUMBERS AND THEY WERE STALE. The step names the over-cap files as \"411/440 lines\": demo-seed-payloads.ts is 302 at HEAD and live-dispatch.ts is 119 — the 440-line file it meant was split into live-dispatch-payloads.ts by 55dc0a43 before this row was claimed. Growth is doc-only: demo-seed-payloads 302->318, live-dispatch-payloads 390->397, live-app 251->259 (already over at HEAD). expansion-approval.ts untouched at 319.\n\nA REAL REGRESSION I INTRODUCED, FOUND BY G3 AND FIXED. Removing `budgetRef` from DEV_PAYLOADS broke `tests/integration/control-room/live-approval-activation.test.ts` with `approval.decide: INPUT_INVALID@CORE_REDUCER`. It is the only arm that drives the BOARD'S OWN payload through the daemon end to end, and no grep for `budgetRef` could have found it — the file never names the field, it spreads `payloadFor`'s output. Fixed by making the arm do what production does: derive via `deriveApprovalBudgetRef` and merge, with `not.toHaveProperty(\"budgetRef\")` pinning that the base is deliberately incomplete. The arm is now stronger than before: it proves the board's payload plus the daemon's own derivation activates through the bind-back. That is DoD 4's browser half, end to end, and it is why this step owns a 19th path.\n\nADVERSARIAL PASS CHANGED ONE THING IN PRODUCTION: `withBudgetCommitment` was re-reading `targetAggregateId` off the caller-supplied affordance instead of using the target `planningTargetOf` had already resolved — two reads of one hostile object are two chances to disagree, and the commitment must cover the run the record names. Threaded in. Three others considered and left, with reasons, in the completion comment.\n\nGATES: G2 exit 0 (4 files / 88 tests); G3 root exit 0 (394 files / 9679 tests, core expansion 3/152); G4 control-room typecheck exit 0 with its `$ tsc` echo and test exit 0 (109 files / 1486 tests). G1 daemon typecheck exit 1 on ONE FOREIGN error and G5 daemon test exit 1 on 19 FOREIGN failures — both attributed with measurements, not assertions: `git show HEAD:<file> | grep -c` reads 0 for the symbol that errors while the peer-dirty worktree copy reads 3, with a working positive control (`it(` = 14) proving the zero is a true negative rather than a broken pathspec; and `git status --porcelain -- apps/daemon/src` shows the other four failing suites are byte-identical to HEAD with `grep -c demo-seed` = 0 on each. The 19 is UNCHANGED from the count I took before writing a line of step 4.\n\nCOMMIT: 19 owned paths, porcelain'd first and confirmed as exactly 17 ` M` plus the 2 `??` new surface files; `git diff --cached --name-only` was EMPTY before any staging. Peer dirt (cordum-goals.css, pairing-approval-handshake.test.ts, four untracked control-room `.js` bridges) left untouched — not reverted, not stashed, not staged.",
      "startedAt": "2026-08-30T06:18:10.288Z",
      "completedAt": "2026-08-30T06:28:04.508Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor promote (standing authority, premise cleared): producer task-61a2e8ad (two-phase budget commitment, budgetCommitmentDigest + bind-back) is DONE — the commitment-to-commitment consumers this row lands are now plannable against landed HEAD.",
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 170,
  "comments": [
    {
      "id": "comment-a4b089cc04824c6ebbecdb3f635a137d",
      "author": "worker-39362c3a",
      "content": "STEP-1 VERIFY-OR-BLOCK GATE (DoD-1), worker-39362c3a. Read-only, no bytes authored. Every grep unpiped with its own exit code.\n\n`git rev-parse HEAD` -> `3a9dc2cd0ea018c865b4eb89d1ce92a008faec2e`, EXIT 0.\n\n(a) PRODUCER LANDED. task-61a2e8ad is DONE on disk. `git grep -n 'export function verifyBudgetCommitment' HEAD -- apps/daemon/src/budget/` EXIT 0 -> budget-commitment.ts:193. The two builder halves are at the exact lines the plan named: `budgetCommitmentMaterial` :151, `budgetCommitmentDigest` :175 (EXIT 0).\n\n(b) THE FOUR CONSUMER SITES, all still present and NONE re-anchored — no peer has landed part of this row.\n1. `apps/daemon/src/planning/graph-supersede-approval-binding.ts:73` — `if (record.budgetRef !== predecessor.boundHashes?.budgetHash) {`. Still the digest comparison.\n2. `packages/core/src/expansion/expansion-approval.ts:227` — `if (record.budgetRef !== criteria.budgetRef) return \"EXPANSION_APPROVAL_BUDGET_MISMATCH\";`. expansion-preparation.ts :83 (the criteria field type), :140 (CRITERIA_KEYS roster), :242 (`validHex64(value[\"budgetRef\"])`) all present.\n3. `apps/control-room/src/approvals/approval-detail-plan.tsx:59` — `[\"approval.budget\", \"Budget\", (record) => record.budgetRef]`. Display only, as described.\n4. Seeds: `apps/daemon/src/orchestrator/demo-seed-payloads.ts:395` `budgetRef: hex64(\"bb\")` and `apps/control-room/src/live/live-dispatch.ts:198` `budgetRef: hex64(\"bb\")` — both still literals AT HEAD. See the COLLISION note below for what the worktree says.\n\n(c) SEAM ROW AND ITS DEPENDENCY. task-ba102165 is DONE; `git grep -n 'export function readApprovalRecordFacts' HEAD -- apps/daemon/src/planning/` EXIT 0 -> approval-record-facts.ts:127. task-3b61860f is also DONE and its walk HAS landed: `firstMissingApprovalFact` is declared at :175 and called at :136. The BUDGET_REF slot exists as a guard only — :159 `APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE: (derived) => derived.budgetRef !== undefined` — with `budgetRef?: string | undefined` at :55 and NO producer anywhere in the file (`git grep -n budgetRef` over it returns exactly those two lines). So item 5 is ungated and genuinely unfilled: the slot is declared and guarded but nothing derives it.\n\n(d) BIND-BACK PRECEDENT, quoted so steps 2 and 4 mirror it rather than invent a query. approval-activation.ts:168-175:\n    const committed = verifyBudgetCommitment(store, {\n      approvedRun: {\n        runBinding: input.binding,\n        verifiedGraphRevisionRef: input.graphRevisionRef,\n      },\n      goalRef: input.goalId,\n      projectId: request.projectId,\n    }, input.approval.budgetRef);\n    if (!committed.ok) return refuse(request.kind, committed.code, committed.layer);\nA second production caller with the same construction is graph-activation-service.ts:185. Two independent call sites agreeing is the shape steps 2 and 4 compose.\n\nPOSITIVE CONTROL for the grep harness: `git grep -c \"budgetRef\" HEAD -- apps/daemon/src` EXIT 0 across 19 files, so the negatives above are true negatives and not a broken pattern or an empty pathspec.\n\n(e) PORCELAIN over every owned path: clean except one — ` M apps/control-room/src/live/live-dispatch.ts`.\n\nCOLLISION, MEASURED — this changes step 4 and I am flagging it before writing anything. The claim receipt named task-4942111bdaac427985e4636b2bbd0cad as holding apps/control-room/src/live/live-dispatch.ts, and that row has an uncommitted SPLIT of the file in flight: `git diff --stat` shows 71 insertions / 392 deletions, with new imports from ./live-board-feed.js, ./live-command-dispatch.js and ./live-dispatch-payloads.js. The consequence for this row is concrete: the seed literal this row must re-issue is NO LONGER in the file the plan names. `grep -c 'hex64(\"bb\")' apps/control-room/src/live/live-dispatch.ts` is 0 in the worktree; the line now lives at `apps/control-room/src/live/live-dispatch-payloads.ts:204`, which is UNTRACKED (`??`) and belongs to that peer. Editing live-dispatch.ts against its HEAD content would destroy the peer's refactor, and editing their untracked file would be adopting WIP this row's rails forbid. The daemon-side seed (demo-seed-payloads.ts:395) is clean and unambiguously this row's.\n\nPROCEEDING as follows, which keeps every dependency-free clause moving: steps 2 (supersede) and 3 (expansion) are untouched by the collision and start now; step 4's daemon seed and the control-room display label proceed; step 4's live-dispatch clause is raised in #general for a routing call rather than resolved unilaterally. Item 5 is ungated and will be delivered, coordinating on approval-record-facts.ts, which the claim receipt also flags against task-f42d5165b747436a9f9b354dbfa37e3d.",
      "timestamp": "2026-08-29T23:08:05.639Z"
    },
    {
      "id": "comment-86531f99315142068814deca7cfb156f",
      "author": "governor-0134a004",
      "content": "GOVERNOR ROUTING RULING (DoD-4 site re-anchor, option (b) of comment/msg-ce62bbdc): the control-room seed literal named as live-dispatch.ts:198 has MOVED into untracked live-dispatch-payloads.ts:204, owned by in-flight task-4942111b. Ruling: (1) task-4942111b's holder (worker-d9877466) applies the one-line derivation in their own file — replace the hex64(\"bb\") budgetRef with budgetCommitmentDigest(budgetCommitmentMaterial(...)) per the landed 61a2e8ad bind-back, disclosed in their completion comment. (2) THIS row's DoD-4 control-room site is re-anchored as \"delivered by task-4942111b in live-dispatch-payloads.ts\" — this row proves the daemon-side seed (demo-seed-payloads.ts:395), the supersede re-anchor, the expansion proof and the BUDGET_REF slot, and at completion VERIFIES (read-only grep) that no hex64(\"bb\") budgetRef remains in either file at HEAD, blocking on 4942111b only if that final verify fails. Fallback (c) — carve a follow-up row — applies only if 4942111b dies unlanded. QA: grade DoD-4's control-room half against this ruling, not the stale :198 anchor.",
      "timestamp": "2026-08-29T23:09:04.913Z"
    },
    {
      "id": "comment-e48ab5737d1c4c57a598d25ee7c1241e",
      "author": "worker-39362c3a",
      "content": "STEP-2 RISK 1 FIRED — measured, reported, not worked around. The step says: \"If GenesisApprovedRun is unreachable from durable state at this seam (risk 1), STOP and report rather than threading a caller value.\" It is unreachable. Here is the measurement and a concrete fix, because the fix is one line in a file this row does not own.\n\nWHAT THE BUILDER DEMANDS. `budgetCommitmentMaterial(store, input: BudgetCommitmentQuery)` where `BudgetCommitmentQuery = { approvedRun: GenesisApprovedRun, goalRef, projectId }` (budget-commitment.ts:73-77), and `GenesisApprovedRun = { runBinding: ApprovedRunBinding, verifiedGraphRevisionRef: string }` (budget-genesis-binding.ts:55-58). `ApprovedRunBinding = { authorityRef, bodiesDigest, envelopeDigest, runId }` (approval-run-binding.ts:50-55).\n\nWHAT THE SEAM HAS. `matchSupersedeApproval(store, request, facts, record)`. `GraphSupersedeRequest` (graph-supersede-contracts.ts:88-100) carries commandId, correlationId, decidedAt, expectedPredecessorRevisionRef, expectedPreparationVersion, generation, goalRef, principalId, projectId, successorGraphContentHash, successorRevisionRef — no runId. `SupersedeFacts` (graph-supersede-facts.ts:39-51) carries active, dispositions, fundingAggregateId, generation, goal, aggregate ids, preparationVersion, successorContent — no runId. The predecessor's durable state is `GraphRevisionState` (graph-revision-contract.ts:122-138): boundHashes, goalRef, graphContentHash, graphEpoch, lifecycle, planHash, revisionId, submissionRef, version — no runId; and `GraphActivationBinding` (:27-33) is budgetHash, expectedGoalVersion, graphHash, policyHash, qualityHash — no runId either. The only production producer of an ApprovedRunBinding is `verifyApprovedRunBinding({ graphRevisionRef, run, runId, store })`, and the one durable composer of it, `readApprovalIntentSources(store, projectId, runId)` (approval-intent-sources.ts:103), takes a runId as its first-class input. So `verifiedGraphRevisionRef` IS reachable — it is `facts.active.revisionId` — but `runBinding` is not, by any path.\n\nTHE PART THAT MAKES THIS CHEAP TO FIX. `runBinding` is never READ. `grep -n \"runBinding\\|approvedRun\" apps/daemon/src/budget/budget-genesis-binding.ts` returns exactly four lines: :47 a doc comment, :56 the type field, :80/:106 parameter declarations, and :93 — the only dereference — `graphRevisionRef: approvedRun.verifiedGraphRevisionRef`. That dereference sits in the clean-empty-project FALLBACK branch, which `readGenesisBudgetBinding` reaches only when the strict `readBudgetBinding` refuses with BUDGET_PROJECTION_GRAPH_UNAVAILABLE + the clean-empty upstream (:61-63, :86-87). At the supersede seam `facts.active` is an ActiveGraphAccepted, i.e. the project HAS an active graph, so the strict reader answers and the fallback is unreachable. The commitment at this seam is therefore fully determined by durable state — material is {amounts: the fixed GENESIS_AMOUNTS roster, binding: the strict durable read, goalRef, projectId} (:159-165) — and `approvedRun` contributes nothing.\n\nSO THE ONLY THING BLOCKING STEP 2 IS A TYPE OBLIGATION, and satisfying it would mean writing a fabricated authority-shaped literal ({authorityRef, bodiesDigest, envelopeDigest, runId}) into production code. That is what the step's risk-1 clause forbids, and it is the class of thing this epic's rails reject on sight even when inert.\n\nPROPOSED AMENDMENT, smallest version first:\n(A) Add a narrow entry point to apps/daemon/src/budget/budget-commitment.ts — e.g. `budgetCommitmentMaterialForActiveGraph(store, { goalRef, projectId })` that composes `readBudgetBinding` directly and reuses the SAME material assembly and the SAME `budgetCommitmentDigest`, so no material field is re-listed and no second builder appears. Step 2 then composes that. This needs budget-commitment.ts declared as an owned path for this row (one file, one small function).\n(B) Alternatively make `approvedRun` optional on BudgetCommitmentQuery, with the genesis fallback refusing BUDGET_COMMITMENT_MATERIAL_UNAVAILABLE when it is absent and the fallback branch is reached. Same file, same ownership question, and it makes the inertness explicit in the type instead of in a comment.\n(C) If neither is acceptable, step 2 needs re-planning against a seam that does carry a runId — but note that nothing in the supersede path has one, so I expect (A).\n\nI recommend (A). Meanwhile I am NOT blocking the row: step 3 is delivered, and steps 4 (daemon seed) and 5 (the BUDGET_REF slot) are independent of this question and proceed now.",
      "timestamp": "2026-08-29T23:13:33.050Z"
    },
    {
      "id": "comment-8bb494c9cb2442c389bd116b87ba8088",
      "author": "governor-0134a004",
      "content": "GOVERNOR RULING on the step-2 risk-1 stop (comment-e48ab573), architect veto window open: option (a) — add narrow `budgetCommitmentMaterialForActiveGraph(store, {goalRef, projectId})` to apps/daemon/src/budget/budget-commitment.ts, composing readBudgetBinding and REUSING the same material assembly + digest (no field re-listed, no second builder). Grounds: preserves ruling A's single-builder property exactly (composition, not duplication); the worker measured runBinding's only dereference (:93 fallback) unreachable at the supersede seam, so nothing is weakened; option (b)'s optional-param + new refusal code changes the builder's contract more. budget-commitment.ts is GRANTED as an owned path for this addition only (declare_files issued). @architect-1825759f may veto/refine this before the worker reaches step 2's edit — silence by then = ruling stands per standing authority. Fabricating an authority-shaped literal stays forbidden either way.",
      "timestamp": "2026-08-29T23:14:12.082Z"
    },
    {
      "id": "comment-fe04e8d40ee5433abe0782d3425db670",
      "author": "worker-39362c3a",
      "content": "STEP-2 UNNAMED CONSUMER — stopping and reporting per the rail, with the blast radius measured rather than estimated. One line fixes it; I am not taking the extra path unowned.\n\nRED FIRST, captured. New arm \"admits a successor approval whose budgetRef is the durable commitment\" in graph-supersede-approval-binding.test.ts: it builds the commitment through `budgetCommitmentMaterialForActiveGraph` + `budgetCommitmentDigest`, guards that the notions genuinely differ (`expect(commitment).not.toBe(bound.budgetRef)` — a fixed point here would make the whole arm vacuous), and drives the production edge. Before the production change it refused with `Error: GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE` — `Tests 1 failed | 14 passed (15)`. That is the predicted red, and the not-equal guard passing is the proof that the root digest and the commitment really are different values on this fixture.\n\nGREEN, delivered under the governor's (a) ruling. `budgetCommitmentMaterialForActiveGraph(store, {goalRef, projectId})` added to budget-commitment.ts, composing `readBudgetBinding` directly. It re-lists nothing: I extracted the existing assembly into one private `materialFrom(bound, goalRef, projectId)` that BOTH entry points now call, so there is exactly one material list and one digest, and `budgetCommitmentMaterial` is unchanged in behaviour. budget-commitment.ts is 239 lines. graph-supersede-approval-binding.ts:73's right-hand side is now that commitment; the refusal keeps its exact code and layer, gaining an upstream only when the builder itself refused; boundHashes is read, never rewritten; the file is 118 lines. `pnpm --filter @moe/daemon typecheck` EXIT 0 with the `$ tsc` echo. My new arm passes.\n\nTHE UNNAMED CONSUMER. `apps/daemon/src/planning/graph-supersede-test-fixtures.ts:323` builds every successor approval used across the suite as `budgetRef: binding.budgetHash` — the predecessor's persisted activation ROOT digest, i.e. exactly the notion this row retires. With the re-anchor in place that fixture now refuses at the budget check before any arm reaches what it is actually testing. Measured fan-out, running the seven consumers of that fixture: `Test Files 7 failed (7)` / `Tests 24 failed | 107 passed (131)` —\n  src/planning/graph-supersede-service.test.ts (19 tests | 10 failed)\n  src/planning/graph-supersede-approval-binding.test.ts (15 tests | 7 failed)\n  src/planning/active-graph-slot.test.ts (6 tests | 2 failed)\n  src/planning/graph-supersede-authority.test.ts (4 tests | 2 failed)\n  src/planning/active-graph-slot-ordering.test.ts (3 tests | 1 failed)\n  src/work/foundation-context-prelaunch.test.ts (20 tests | 1 failed)\n  src/daemon-command-graph-edges.test.ts (64 tests | 1 failed)\nEvery one of the 24 is the same cause: a fixture-built successor approval carrying the root digest where the matcher now expects the commitment.\n\nTHE FIX IS ONE LINE, at line 323 of the shared fixture: `budgetRef: binding.budgetHash` becomes the commitment through the same shared builder. Step 2's affectedFiles name only graph-supersede-approval-binding.ts and its test, so this is an unnamed consumer and the rail says stop and report rather than take the path. It is the same forced-mirror class already granted three times on this board (comment-ca325742, comment-05c9e76b, comment-42049106).\n\nWHY THE ALTERNATIVE IS WORSE, since it exists and is inside owned paths: I could override budgetRef locally in graph-supersede-approval-binding.test.ts, which I do own. That would leave the other SIX suites red and the shared fixture still minting a retired notion — hiding the breakage rather than fixing it, in six files nobody would then be looking at. The one-line fixture edit is both smaller and more honest.\n\nCROSS-ROW HAZARD, flagged rather than discovered later: task-4e013e45's DoD-3 requires `git diff --stat` to be EMPTY over `apps/daemon/src/**/*fixtures*.ts`. That row is committed (659267db) and its clause was measured against its own diff, but a QA re-running the clause at review time would see this fixture dirty and could read it as that row's. It is not — it is this row's, and this comment is the record.\n\nREQUEST: grant `apps/daemon/src/planning/graph-supersede-test-fixtures.ts` to this row, limited to line 323's budgetRef derivation. Until then step 2 stays in progress; the production change is correct and owned, and I am not committing a tree that reds 24 arms. Steps 3 and 5 are independently green and are being committed now by explicit pathspec so they cannot be swept.",
      "timestamp": "2026-08-29T23:24:06.666Z"
    },
    {
      "id": "comment-3acb9620f6204e73b7ba4ae928178f97",
      "author": "governor-0134a004",
      "content": "GOVERNOR SCOPE GRANT (forced-mirror class, 4th of its kind — precedents comment-ca325742 / comment-05c9e76b / comment-42049106): apps/daemon/src/planning/graph-supersede-test-fixtures.ts is added to this row's owned paths, LIMITED TO the :323 site where `budgetRef: binding.budgetHash` mints the retired notion — replace with the production-derived commitment per the delivered re-anchor (comment-fe04e8d4). Grounds: the DoD's commitment-to-commitment comparison mechanically forces the fixture that feeds the compared surface; a fixture still minting the old notion would make the new arm assert a contradiction. No other line in the fixture changes; disclose the one-line diff in the completion comment. declare_files issued.",
      "timestamp": "2026-08-29T23:24:34.207Z"
    },
    {
      "id": "comment-b7fc5caa5ed644aba8da14fde0fae444",
      "author": "worker-39362c3a",
      "content": "STEP-4 IS BLOCKED ON BOTH HALVES, for two different and independently measured reasons. Steps 1, 2, 3 and 5 are DELIVERED and COMMITTED (cd8a5c24, 1c27ea12).\n\nHALF A — THE DAEMON SEED. DoD-4 says demo-seed-payloads.ts:395 must derive `budgetRef` from `budgetCommitmentDigest(budgetCommitmentMaterial(store, ...))` \"over the seeded store\". There is no store. THE DEMO SEED IS A PURE HTTP CLIENT.\n- `approvalRecord(input: DemoSeedInput)` (demo-seed-payloads.ts:386) takes no store, and its only caller is demo-seed-plan.ts:210, which builds a STATIC command list before anything executes — so even a store-aware variant would be computing the commitment before the genesis binding rows it depends on exist. That is the step's own risk 3, and it is not avoidable by reordering inside the plan builder.\n- The step's fallback — \"thread the derived value in from the seed step that HAS the store\" — has no such step. `grep -rn \"@moe/store\\|SqliteEventStore\" apps/daemon/src/orchestrator/demo-seed-*.ts` returns hits in exactly ONE file, demo-seed-payloads.test.ts, a test. Every production module in the lane (demo-seed-main/http/plan/payloads/env/refusal) imports no store. Positive control: `@moe/store` IS imported elsewhere in the same directory (agent-session-fence.ts, agent-wrapper-main.ts), so the grep works and the negative is real.\n- The seed reaches the daemon over HTTP: demo-seed-http.ts `wireFor(config, fetchImpl)` posts to `${config.origin}${path}`, and demo-seed-main.ts's own header states the design — \"A pure CLIENT — it changes no daemon contract and invents no command.\"\n- Nor can it fetch the commitment: `grep -rn \"budgetCommitment\" apps/daemon/src/http/` returns NOTHING across 61 production files there, and of the seven files mentioning \"budget\" only goal-catalog-entry.ts and static-asset-host.ts are production. No route exposes it.\nSo the clause is not implementable as written without changing what the seed IS. The three shapes I can see, for an architect to choose:\n(A) Add a daemon read the seed calls after the finalize step and before approval.decide, returning the commitment for {projectId, goalRef}. Keeps the seed a client; costs one HTTP seam and its own row.\n(B) Have the approval.decide handler derive `budgetRef` server-side instead of accepting the caller's. Cleanest for seeds AND the live lane — but it makes the activation bind-back compare a server value against itself, which is exactly the vacuity ruling comment-87ad84c1 set the two-phase design up to avoid. I do not recommend it without that ruling being revisited.\n(C) Accept that the demo lane's approval cannot carry a valid commitment and drop the \"demo lane activates through the real bind-back\" clause. That retires the gate rather than satisfying it, so I flag it only for completeness.\nI recommend (A), as a prerequisite row this one dependsOn.\n\nHALF B — THE CONTROL-ROOM SEED, unchanged since my 23:08Z escalation and still unanswered. task-4942111b holds an uncommitted SPLIT of apps/control-room/src/live/live-dispatch.ts (71 insertions / 392 deletions) and the literal moved to the UNTRACKED apps/control-room/src/live/live-dispatch-payloads.ts:204. Re-measured just now: live-dispatch.ts still ` M`, the payloads module still `??`, `grep -c 'hex64(\"bb\")' apps/control-room/src/live/live-dispatch.ts` still 0. Both available moves remain forbidden — editing live-dispatch.ts from its HEAD content destroys the peer's refactor, editing their untracked file adopts their WIP.\n\nCONSEQUENCE. Steps 6 and 7 both depend on step 4: drill 2 is \"revert a seed to the literal -> the demo activation arm reds\", and there is no such arm to red. So the row cannot proceed past 4/7 on either half without a decision. Reporting blocked rather than inventing a seam or reverting a peer. Everything delivered is committed and recorded, so nothing is at risk of a sweep: cd8a5c24 (steps 3+5, 5 paths) and 1c27ea12 (step 2, 4 paths).",
      "timestamp": "2026-08-29T23:28:55.119Z"
    },
    {
      "id": "comment-fa76f879e6824e9daed658082e02bf21",
      "author": "governor-0134a004",
      "content": "GOVERNOR SCOPE GRANT (fold, per qa-063c9dff's security-lane measurement msg-45d26f1e): BUDGET_COMMITMENT_LAYER's boundary-roster entry is folded into THIS row, since budget-commitment.ts is this row's live surface. Owned paths gain tests/security/boundary-roster.security.ts and tests/security/durable-store-boundaries.security.ts, LIMITED TO adding the BUDGET_COMMITMENT_LAYER roster entry AND its cardinality pin bump (the suite reds two assertions per file — entry alone is a half-fix). The other 10 missing layers stay with the architect's backfill row — do NOT add them. Disclose the exact one-entry diffs in the completion comment.",
      "timestamp": "2026-08-29T23:44:23.303Z"
    },
    {
      "id": "comment-85037aff4d47400598ca6db145d5315b",
      "author": "worker-b5bb4e0f",
      "content": "STEP-4 REOPEN RE-MEASUREMENT — blocker persists at current HEAD 547c6c55b73734d864c6897fd03b067c62cad052; zero bytes authored.\n\nWHAT CHANGED: task-4942111bdaac427985e4636b2bbd0cad is DONE and its completion commit 55dc0a4332096f3b39ef1448073a7b4542123af4 is an ancestor of HEAD. The former untracked/WIP collision is gone: live-dispatch.ts and live-dispatch-payloads.ts are tracked and exact-path clean.\n\nWHAT DID NOT CHANGE: the literal the governor's comment-86531f99 required 494 to replace is still committed at apps/control-room/src/live/live-dispatch-payloads.ts:204 (`budgetRef: hex64(\"bb\")`); git blame assigns the line to 55dc0a43. This row's approved/declared scope still names live-dispatch.ts, not the moved payloads file, and the ruling made this row's action read-only verification. That verification fails.\n\nTHE DAEMON TOPOLOGY IS STILL IMPOSSIBLE AS WRITTEN. At HEAD, `git grep -n -E '@moe/store|SqliteEventStore' HEAD -- 'apps/daemon/src/orchestrator/demo-seed-*.ts' ':!*test*'` exits 1. buildDemoSeedPlan creates the full static list before execution and calls approvalRecord(input) while constructing it; demo-seed-main then POSTs that already-built list. `git grep -n 'budgetCommitment' HEAD -- apps/daemon/src/http ':!*test*'` exits 1. Therefore there is still no seed step with a store and no HTTP read/offer seam from which the pure client can obtain the commitment after genesis rows exist.\n\nCONTROL ROOM HAS THE SAME AUTHORITY GAP. Its package has no @moe/store/daemon dependency; live-dispatch-payloads.ts is a pure payload module, and production contains no budgetCommitmentMaterial/budgetCommitmentDigest call or surfaced commitment fact. Copying/relisting the digest would violate the single-builder rail and still would not read durable state.\n\nREQUESTED ARCHITECTURE ACTION: create/land a daemon-owned post-binding commitment read/offer seam (recommended shape from the prior block: HTTP read between finalize and approval.decide), then amend this row's step/ownership to consume it dynamically in the daemon and Control Room seed lanes, including live-dispatch-payloads.ts. Reopening 494 or merely granting that one file is insufficient without the production seam; deriving server-side inside approval.decide would make the bind-back compare a server value against itself and violate ruling A.\n\nUntil that production prerequisite exists, step 4 cannot be made working without scope expansion or fabricated authority. Steps 1,2,3,5 remain delivered in commits cd8a5c24 and 1c27ea12; step 4 stays IN_PROGRESS; steps 6-7 remain legitimately gated on its red arm.",
      "timestamp": "2026-08-29T23:50:56.027Z"
    },
    {
      "id": "comment-d640777bab564c7b9fdeb75c52d755f5",
      "author": "governor-0134a004",
      "content": "GOVERNOR DoD-COORDINATE AMENDMENT (amendment-by-comment per standing practice; the field is not tool-editable): DoD-4's control-room seed site is re-anchored from apps/control-room/src/live/live-dispatch.ts:198 to apps/control-room/src/live/live-dispatch-payloads.ts:204 — task-4942111b's landed split (commit 55dc0a43) moved the `budgetRef: hex64(\"bb\")` literal there; the old anchor no longer contains it (grep -c = 0). live-dispatch-payloads.ts is ADDED to this row's owned paths, LIMITED TO replacing that literal with the production-derived commitment (shared builder). The file is tracked and its owner row is DONE — no WIP conflict remains. The daemon-seed half of DoD-4 remains parked on the missing commitment-read prerequisite (comment-85037aff); this amendment clears only the control-room half. declare_files issued.",
      "timestamp": "2026-08-30T00:09:28.771Z"
    },
    {
      "id": "comment-7f67985b9a4243e4938813b5c0ccd307",
      "author": "qa-063c9dff",
      "content": "QA HEADS-UP FROM A DIFFERENT ROW — your DoD-4 line anchors are STALE as of 2026-08-30T00:48Z, and both of the numbers this row and the governor's routing ruling depend on have moved. Filing here rather than in chat so it reaches whoever re-enters, since chat hops are throttled and this is row-specific.\n\nWHAT MOVED. task-a888038d (T1-b producer, approved by me at commit `37f95c18`) split the demo seed and mirrored a classifying policy table into the control-room live payload. Both files this row must edit were carried in that commit, correctly declared and flagged `Moe-Contested` against this task id. The budgetRef literal did not change, but its line number did:\n- `apps/daemon/src/orchestrator/demo-seed-payloads.ts` — `budgetRef: hex64(\"bb\")` was :395, is now :286 (the file lost 125 lines to a new `demo-seed-policy.ts`).\n- `apps/control-room/src/live/live-dispatch-payloads.ts` — was :204, is now :218.\n\nWHY IT MATTERS TO YOU SPECIFICALLY. This row's DoD-4 names \":395\" and @governor-0134a004's option-(b) routing ruling to @worker-d9877466 names \":204\". Neither number resolves to the budgetRef any more. A re-entry session that opens either file at the cited line will land on unrelated code, and a `sed -n '395p'`-style window will read as if the clause were already satisfied or already broken depending on what is there now. Anchor on the literal, not the coordinate: `git grep -n 'hex64(\"bb\")' -- apps/daemon/src/orchestrator/demo-seed-payloads.ts apps/control-room/src/live/live-dispatch-payloads.ts` returns both sites in one command and cannot go stale the same way.\n\nWHAT DID NOT HAPPEN, since a contested-path commit on this board usually deserves suspicion. I checked before approving: `git grep 'hex64(\"bb\")'` returns hits at BOTH `37f95c18^` and `37f95c18` in both files, so nothing of this row's work was reverted — the fix simply had not landed yet at either revision, and the peer's commit moved the surrounding lines without touching the literal. Your steps 3 and 5 commits (`cd8a5c24`, `1c27ea12`) are also untouched by it.\n\nONE MORE THING FOR THE SAME EDIT. `demo-seed-payloads.ts` is now 302 lines (down from 411, which was over the 400 hard cap) and `bootstrap-policy-authority-reader.ts` is at 247 — three lines under the 250 target, with two rows editing it. If your budgetRef change grows either file, budget for a split rather than assuming there is room.\n\nNot claiming anything here and not asking for a reply; this row is BLOCKED and I hold no claim on it. Recorded so the measurement is on the row when it unblocks.",
      "timestamp": "2026-08-30T00:57:39.127Z"
    },
    {
      "id": "comment-a9d4c08e804a40bcbb6a013808b63a8d",
      "author": "worker-d9877466",
      "content": "PREMISE RE-MEASURED AT HEAD b8dd739a — IT STILL HOLDS. Block is REAL, not stale. Do not flip this row.\n\nThe stored blockedReason was measured at HEAD 547c6c55, which is now 9 commits behind. Standing practice is to distrust a block premise anchored on a dead HEAD, so I re-ran it clause by clause from a read-only seat. Every load-bearing claim survives.\n\nINTERVENING COMMITS (547c6c55..b8dd739a, all 9):\n```\nb8dd739a  Foundation seal -> durable project config (task-e4cd1eb6)\n89533c6c  compose Gate 1 resolver into a reachable HTTP read route\n368e02ab  register cutover.activate on its own command edge\n37f95c18  T1-b producer: run-scoped policy evaluation at finalize\n32db01aa  resolve Product Contract Gate 1 from authenticated durable human authority\nee4d4b50  CutoverAttempt reducer + daemon cutover.activate\n6f01aa08  retire caller-carried launchTemplate in foundation.dispatch\na62a1f71  activate cutover in one transaction with the v2 marker\n1d2e5dc4  derive the foundation.dispatch launch template server-side\n```\nTwo of these (32db01aa, 89533c6c) land an AUTHENTICATED DAEMON HTTP READ ROUTE, which is the shape this row's prerequisite asks for. That is the reason the premise deserved re-measuring — and it is also why the answer is not the one it looks like from the commit subjects. Those routes resolve PRODUCT CONTRACT GATE 1. They do not carry a budget commitment.\n\nCLAUSE 1 — \"the sole internal derivation requires the store.\" STILL TRUE.\n`apps/daemon/src/budget/budget-commitment.ts:31` imports `SqliteEventStore` from `@moe/store`, and the module's own header names itself the ONE CANONICAL BUILDER (ruling condition 1). Full production import closure of `budget-commitment.js`, zero test files:\n```\napps/daemon/src/bootstrap/bootstrap-test-fixtures.ts:8\napps/daemon/src/budget/budget-genesis-leg.ts:29,53\napps/daemon/src/planning/approval-activation.ts:19\napps/daemon/src/planning/approval-budget-ref.ts:4\napps/daemon/src/planning/graph-activation-service.ts:21\napps/daemon/src/planning/graph-supersede-approval-binding.ts:15\napps/daemon/src/planning/graph-supersede-test-fixtures.ts:25\n```\nEvery consumer is store-side: genesis leg, activation, supersede binding, fixtures. Not one is an HTTP handler.\n\nCLAUSE 2 — \"daemon HTTP/affordance surfaces expose no commitment.\" STILL TRUE, and this is the clause most likely to be misread.\n`grep -n budgetRef apps/daemon/src/http/affordance-read.ts` returns NOTHING. The only `budgetRef` hits under `apps/daemon/src/http/` are in `.test.ts` siblings — `affordance-read.test.ts:364` and `affordance-read-planning.test.ts:520` — and both obtain it by calling `fixtureBudgetCommitmentFor(store, ...)`. That helper is defined at `apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts:576` and TAKES THE STORE AS ITS FIRST ARGUMENT. A test proving the route's payload can hold a commitment is not the route producing one; the test builds the value store-side and hands it in. Anyone grepping `budgetRef` across `http/` and seeing four hits will conclude the surface exists. It does not.\n\nCLAUSE 3 — \"the landed control-room split still commits budgetRef: hex64(\\\"bb\\\"), which this row is not authorized to edit.\" STILL TRUE, verbatim:\n```\napps/control-room/src/live/live-dispatch-payloads.ts:218\n  approvalRef: \"approval-1\", approvedNodeScope: [DEMO_NODE_REF], budgetRef: hex64(\"bb\"),\napps/daemon/src/orchestrator/demo-seed-payloads.ts:286\n  budgetRef: hex64(\"bb\"),\n```\nTwo placeholder sites, not one. Note the partial-movement trap: `bootstrap-test-fixtures.ts:532` and `approval-activation.test.ts:349` both carry prose saying the placeholder \"stopped being a valid approval\" and \"became the DERIVED\" value. That migration reached the FIXTURE path only. The two production/demo payload sites above still ship the literal, so a reader who finds that prose first will wrongly conclude the placeholder is retired.\n\nCONCLUSION. Step 4 remains unsatisfiable from this row's affected files. The browser fixture client has no `SqliteEventStore`, the daemon exposes no read surface returning the commitment, and the single lawful builder is store-gated. The two escape hatches the reason already rejected are still the only alternatives and are still barred: server-side overwrite in `approval.decide` makes bind-back a self-comparison, and copying the algorithm violates the single-builder rail that `budget-commitment.ts` exists to enforce.\n\nTHE ACTUAL BOTTLENECK IS AN UNFILED ROW. `dependsOnUnmet: 0` and `blockedOnTaskIds: null` on this row mean the daemon will NEVER auto-unblock it — a manual block with satisfied deps stays blocked forever, and clearing it takes two writes by an authorized seat, not one. The reason opens \"REAL BLOCKER (no dependency row exists yet)\" and that is still literally true: I checked every non-DONE row on the board (7 total) and none covers the prerequisite. @architect the missing row is an AUTHENTICATED DAEMON READ/OFFER RETURNING THE SHARED-BUILDER COMMITMENT AFTER FINALIZATION, plus dynamic seed orchestration to fetch and thread it, plus an ownership amendment covering the moved control-room payload and a real-process/store journey arm.\n\nBLAST RADIUS — this single unfiled row parks the epic's whole tail:\n- task-be80cb74 (this row, BLOCKED, 4/7 steps done)\n- task-f42d5165 [T1-c reader] (WORKING, `dependsOnUnmet: 1` — this row is the 1)\n- task-c289b8fb PR #3 integration re-run (WORKING, `dependsOnUnmet: 2` — this row and f42d5165)\nBoth WORKING rows show `assignedWorkerId: null` / `hasWorker: false`, so they are unclaimable-but-unheld: `wait_for_task` on WORKING returns `hasNext:false` and a worker seat idles out rather than reporting why. That is the shape the board is in right now.\n\nMeasured read-only. Zero bytes touched. I hold no row and am not claiming this one.",
      "timestamp": "2026-08-30T04:07:00.102Z"
    },
    {
      "id": "comment-4170a3e254b04d8690cc81e31a0ca6c3",
      "author": "worker-d9877466",
      "content": "PREREQUISITE NAMED BY THIS ROW'S `blockedReason` IS NOW FILED: **task-80b6bf7cd73040e5bf15069d31af81a8** — \"[P2.15d-i-pre-budget-1 producer] Authenticated daemon read returning the shared-builder budget commitment for a finalized run, plus the seed-side fetch seam that threads it\". PLANNING, HIGH, order 169, same epic, `createdBy: WORKER`, filed 04:43:21Z by worker-d9877466.\n\nDo NOT re-file it. Content is this row's own `blockedReason` clause-for-clause plus the frozen fleet spec (row 1 of Serena `pending-architect-filings-2026-08-30`); it is not a re-derivation and it is not a probe row.\n\n**THIS ROW IS STILL BLOCKED AND FILING DID NOT CHANGE THAT.** Stating it explicitly because a filed producer is exactly what looks like a remedy and is not one. Measured on disk at 04:43Z:\n\n    task-be80cb74.dependsOn          = [\"task-3b61860f...\"]   -> DONE\n    task-be80cb74.blockedOnTaskIds   = null\n    task-be80cb74.status             = BLOCKED\n\nA satisfied `dependsOn` does not clear a manual block, and the auto-unblock path keys on `blockedOnTaskIds`, which is null. So no id reaching DONE — including task-80b6bf7c — will ever move this row. **A SECOND WRITE IS STILL OWED, and it is governor/human:** point `blockedOnTaskIds` at task-80b6bf7c (or clear the block once that row lands). `moe.set_task_dependencies` returned `[NOT_ALLOWED]` to a worker seat on execution, and QA confirmed it is gated from their side too — no seat currently on the fleet can take it.\n\nThis is the only row on the board carrying that shape. Checked the other BLOCKED row for the same defect: `task-e60b874b.blockedOnTaskIds = [c289b8fb, 2bf8fa1a]`, both resolve to real rows (2bf8fa1a DONE, c289b8fb WORKING), so it self-clears correctly. One row, not a pattern.\n\n**PREMISE OF THIS BLOCK: NOT RE-LITIGATED, AND IT SURVIVED FOUR INDEPENDENT RE-MEASURES** at HEAD b8dd739a (grep-at-sha on the cited symbol; production import closure of `budget-commitment.js`; diff-vs-symbol anchoring across all 9 commits since the stored anchor 547c6c55; per-commit UTC timestamps against `blockedAt`). Nothing since 547c6c55 creates the named prerequisite — 89533c6c is a *Gate 1* resolver read route carrying no budget commitment, and the edit to `live-dispatch-payloads.ts` in that range was task-a888038d rotating `POLICY_REF`, with `budgetRef: hex64(\"bb\")` still shipping. Do not clear this block as stale.\n\n**SCOPE BOUNDARY between the two rows, so nobody collides on the same lines.** task-80b6bf7c delivers the read route and a seed-side fetch seam. It deliberately does **not** touch the `budgetRef: hex64(\"bb\")` literals at `live-dispatch-payloads.ts:218` and `demo-seed-payloads.ts:286` — replacing those is *this* row's DoD 4 and belongs here. Two different files, both production; anchor on `git grep -n 'hex64(\"bb\")'`, never the coordinates, since both moved once already at 37f95c18.\n\nThe ownership amendment this row's `blockedReason` asks for (covering the moved control-room payload plus a real-process/store journey arm) is carried as DoD 7 of task-80b6bf7c.",
      "timestamp": "2026-08-30T04:43:52.486Z"
    },
    {
      "id": "comment-2e8d1cc16a8f48e9be00477c9851b49e",
      "author": "qa-cc876c7b",
      "content": "QA MEASUREMENT — DoD 4 NAMES A FILE THAT NO LONGER CARRIES THE VALUE. Filed here rather than only in chat so it survives to whoever re-enters this row. Measured at HEAD b8dd739a, 2026-08-30T04:46Z. I do not hold this row and have changed nothing on it.\n\nDoD 4 verbatim: \"Seeds re-issued from production: demo-seed-payloads.ts and live-dispatch.ts derive the approval's budgetRef from budgetCommitmentDigest(budgetCommitmentMaterial(...)) over the seeded store (never hex64(\\\"bb\\\"))...\"\n\nMEASURED, unpiped, exit codes quoted:\n  git grep -n 'budgetRef' HEAD -- '*live-dispatch.ts'        EXIT 1  (ZERO hits)\n  git grep -n 'hex64(\"bb\")' HEAD -- apps/control-room apps/daemon\n    apps/control-room/src/live/live-dispatch-payloads.ts:218\n    apps/daemon/src/orchestrator/demo-seed-payloads.ts:286\n\nSo DoD 4 is half-stale:\n- demo-seed-payloads.ts  CORRECT. Carries the literal at :286.\n- live-dispatch.ts       STALE. Carries no budgetRef at all. It is 119 lines and its :22 is\n                         `export { DEV_PAYLOADS, payloadFor } from \"./live-dispatch-payloads.js\";`\n                         — a RE-EXPORT BARREL over the module that actually holds the literal.\n                         The payload module was extracted out from under it by\n                         55dc0a43 \"feat(control-room): derive dispatch identities from the surface offers\".\n\nCONSEQUENCE: a worker obeying DoD 4 literally opens live-dispatch.ts, finds no budgetRef to re-derive,\nand either invents one or reports blocked again. The clause is currently satisfied VACUOUSLY — the file\ndoes contain \"never hex64(\\\"bb\\\")\" in the trivial sense that it contains nothing. Granting this row\nauthorization over live-dispatch.ts would grant authority over a file with nothing in it. The fix is the\nFILE NAME, not only the permission.\n\nAMENDMENT NEEDED (gated authority — set_task_dependencies / DoD amendment are [NOT_ALLOWED] to worker and\nQA seats; this is an architect/governor/human write):\n  DoD 4 file list -> `demo-seed-payloads.ts` and `live-dispatch-payloads.ts`\n  ownership grant -> apps/control-room/src/live/live-dispatch-payloads.ts\nAnchor future references on `git grep -n 'hex64(\"bb\")'`, NOT on :218 — that number already moved once\n(:204 -> :218 at 37f95c18) and :395 -> :286 for the daemon seed in the same commit.\n\nRELATED, so the next reader has the whole picture:\n- The producer this row's blockedReason names was FILED as task-80b6bf7c ([P2.15d-i-pre-budget-1 producer],\n  createdBy WORKER, 2026-08-30T04:43:21Z, PLANNING, held by worker-d9877466). It is NOT in this row's\n  dependsOn or blockedOnTaskIds — both fields are unchanged (dependsOn = [task-3b61860f, DONE];\n  blockedOnTaskIds = null). So task-80b6bf7c can reach DONE and THIS ROW STAYS BLOCKED FOREVER. Wiring it\n  in is a separate gated write.\n- The blockedReason's \"Required prerequisite\" is THREE conjuncts (daemon read/offer; dynamic seed\n  orchestration; ownership amendment + real-process/store journey arm). task-80b6bf7c delivers leg 1 and a\n  seam for leg 2. Leg 3 is not deliverable by any new row — it is an amendment to THIS row's own scope, and\n  the file-name defect above is the load-bearing part of it. Credit: the three-leg decomposition is\n  qa-063c9dff's; the file-name measurement is mine.\n- The block's substantive premise still HOLDS at HEAD: no production read route exposes the commitment\n  (`git grep -n budget HEAD -- apps/daemon/src/http` returns only test files plus incidental\n  budgetAccountRef / a comment), and budgetCommitmentMaterialForActiveGraph sits at\n  apps/daemon/src/budget/budget-commitment.ts:195 with its only production consumer inside the daemon.\n  Do NOT clear this block on the strength of the producer being filed.\n\nZero bytes touched. Zero commits.",
      "timestamp": "2026-08-30T04:46:55.452Z"
    },
    {
      "id": "comment-3c0fed91a6804e31bc4f2e005a273ddc",
      "author": "qa-cc876c7b",
      "content": "QA CORRECTION TO comment-2e8d1cc1 + A CYCLE. Measured at HEAD b8dd739a, 2026-08-30T05:01Z. I do not hold this row and have changed nothing on it.\n\nCORRECTION, MINE. comment-2e8d1cc1 said \"DoD 5 is already landed at HEAD\". THAT OVERSTATES IT and a worker\nwho reads it as \"skip DoD 5\" gets rejected. Correction raised by qa-063c9dff and verified by me at source.\nDoD 5 HAS TWO CONJUNCTS WITH DIFFERENT ANSWERS:\n\n(A) EQUALITY ARM -- FULLY CLOSED, in the strong form. approval-record-facts.test.ts:441-458\n    \"derives a budget ref the activation bind-back itself accepts\":\n      :452  verifyBudgetCommitment(store, approvedRunQuery(store), derivedRef)  -> expect ok true\n      :457  expect(derivedRef).toBe(budgetCommitmentDigest(material.material))\n    Its own comment at :448-450 refuses the weak form (\"Not 'equals a digest this test recomputed' --\n    that would pass for any shared bug\"). The production fence is asked to accept the seam's ref, so seam\n    and fence agree through production on both sides. NOBODY RE-DERIVES THIS.\n\n(B) CODE-EMISSION CLAUSE -- NOT CLOSED, AND UNCLOSABLE AT THIS HEAD. DoD 5 also requires\n    absence -> APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE @ DAEMON_APPROVAL_INTENT with upstream.\n    The absence arm at :460-471 asserts only:\n      :467  expect(\"budgetRef\" in result.derived).toBe(false)\n      :468  expect(result.upstream).toEqual({code:\"BOOTSTRAP_PREREQUISITE_MISSING\", layer:\"DAEMON_PREREQUISITE\"})\n    It NEVER asserts APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE. The suite states why at :213-215:\n      \"readApprovalRecordFacts cannot show STEP_UP -> BUDGET_REF today: `riskTier` has no durable producer\n       until the T1 chain's last row (task-f42d5165) lands, so the command always answers the roster's\n       FIRST code and every later row is unreachable behind it.\"\n    approval-record-facts.ts:195/:201 (slot) and approval-intent.ts:76 (code registered) establish the SLOT\n    and REGISTER the code. They do not establish the EMISSION, which is what the clause asks for.\n\nSO: DoD 5 is HALF-CLOSED. (A) done, (B) blocked. Do not skip the clause; do not redo (A).\n\nTHE CYCLE -- this is stronger than the \"inverted edge\" already filed as comment-76aa7742.\n  DoD 5(B) can only be demonstrated once slot 0 (RISK_TIER) is filled, so the first-miss walk reaches\n  slot 3 (BUDGET_REF).  Filling RISK_TIER is task-f42d5165's job.\n  task-f42d5165.dependsOn INCLUDES task-be80cb74.\n  => be80cb74 DoD 5(B)  <-- f42d5165  <-- be80cb74.\nA backwards edge is a latency problem. A CYCLE means one of the two rows cannot satisfy its DoD as written\nin ANY execution order until the edge is cut. Whoever holds the gated verb should treat that as the\ndecisive argument, not the roster-order reading alone. Three independent derivations now agree: roster\norder (comment-76aa7742, qa-063c9dff), f42d5165 DoD 3's disjunction (qa-063c9dff), and this clause.\n\nREMEDY, and it is the same single gated write already named:\n  set_task_dependencies(task-f42d5165, dependsOn MINUS task-be80cb74)\n  [NOT_ALLOWED] to worker and QA seats -- executed receipt, worker-39362c3a.\nAlternative that needs no gated verb: amend DoD 5 to name (B) as deferred to f42d5165, which is a scope\ndecision and not mine to make.\n\nUNCHANGED FROM comment-2e8d1cc1, both still hold at this HEAD:\n- DoD 4 names live-dispatch.ts, which carries ZERO budgetRef (EXIT 1); the literal is at\n  live-dispatch-payloads.ts:218 and demo-seed-payloads.ts:286. Amendment needed on the FILE NAME, not\n  only on the authorization.\n- This row is now wired: blockedOnTaskIds = [task-80b6bf7c], so it AUTO-UNBLOCKS when that lands. It will\n  arrive in WORKING carrying 4 of 7 steps done, DoD 5(A) closed, DoD 5(B) blocked, and DoD 4 unsatisfiable.\n  RE-MEASURE CLAUSE BY CLAUSE. Do not work the list top to bottom.\n\nCredit: the half-closed split and the :213 citation are qa-063c9dff's; I verified both at source and derived\nthe cycle. Zero bytes touched, zero commits.",
      "timestamp": "2026-08-30T05:01:36.965Z"
    },
    {
      "id": "comment-6278d21a6e12487dbe5610c5e2e342f4",
      "author": "qa-063c9dff",
      "content": "QA GRADING POSITION, filed before this row is claimed so it constrains me rather than surprising the claimer. Two clauses moved under this row while it sat blocked. Re-measure clause by clause against HEAD; do NOT work the list top to bottom. Measured at HEAD b8dd739a.\n\n=== DoD 4 — NAMES A FILE THAT HOLDS NOTHING (credit qa-cc876c7b) ===\nDoD 4 says \"demo-seed-payloads.ts and live-dispatch.ts derive the approval's budgetRef ... (never hex64(\"bb\"))\".\n\n  git grep -n 'budgetRef' HEAD -- '*live-dispatch.ts'        EXIT 1, ZERO hits\n  live-dispatch.ts  = 119 lines, :22 export { DEV_PAYLOADS, payloadFor } from \"./live-dispatch-payloads.js\"\n  git grep -n 'hex64(\"bb\")' HEAD -- apps/control-room apps/daemon\n    apps/control-room/src/live/live-dispatch-payloads.ts:218   <- the live site\n    apps/daemon/src/orchestrator/demo-seed-payloads.ts:286     <- the live site\n    (2 other hits are prose in comments: bootstrap-test-fixtures.ts:532, approval-activation.test.ts:349)\n\nlive-dispatch.ts is a re-export barrel; the payload module was extracted out from under it by 55dc0a43. Editing the barrel satisfies \"never hex64(\"bb\")\" VACUOUSLY. This row's blockedReason also says it \"is not authorized to edit\" live-dispatch-payloads.ts — the ownership fence names the same stale path.\n\nRULING: I will grade DoD 4's seed clause against the files that hold the literal —\napps/control-room/src/live/live-dispatch-payloads.ts and apps/daemon/src/orchestrator/demo-seed-payloads.ts.\nI will NOT reject this row for editing live-dispatch-payloads.ts. I WILL reject an edit to live-dispatch.ts as a vacuous discharge. Anchor on `git grep -n 'hex64(\"bb\")'`, never on :218 — that number already moved once (:204 -> :218 at 37f95c18). Scope is the two literal sites only, not the control-room split at large. All other DoD 4 clauses graded unchanged.\n\n=== DoD 5 — HALF ALREADY DISCHARGED, HALF DISCHARGEABLE BY DECOMPOSITION ===\n(A) EQUALITY CLAUSE: CLOSED, in the strong form. Do not re-derive it.\n    approval-record-facts.test.ts:440-457 asks the PRODUCTION activation fence\n    verifyBudgetCommitment to accept the seam-derived ref, then pins digest equality via the\n    shared builder. Its own comment refuses the weak recompute form.\n\n(B) EMISSION CLAUSE (\"absence -> APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE @ DAEMON_APPROVAL_INTENT\n    with upstream\"): NOT closed, but NOT blocked either. A \"cycle with task-f42d5165\" was raised\n    on this row and is FALSIFIED. The arm exists at HEAD:\n      approval-record-facts.test.ts:225-238  it.each arm 4:\n        { applicablePolicyRef, riskTier: \"R3\", stepUpAuthRef } -> APPROVAL_MISSING_FACT_CODES[3]\n        assertion: expect(firstMissingApprovalFact(derived)).toBe(expected)   EXACT code\n      approval-intent.ts:75   CODES[3] === \"APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE\"\n      approval-record-facts.ts:141-142  const missing = firstMissingApprovalFact(derived);\n                                        if (missing !== null) return incomplete(missing, ...)\n    riskTier is supplied as test input — no store, no durable producer, no f42d5165. And :141 is\n    why this is production and not a mock detour: the seam calls exactly that function. The matrix\n    is guarded at :241-251 (all four codes pinned in order + toHaveLength(4)), so arm 4 cannot\n    drift onto a different code.\n    What IS unreachable at HEAD: a SINGLE end-to-end arm through readApprovalRecordFacts proving\n    code AND layer AND upstream together — deriveFacts injects only serverDerived.stepUpAuthRef,\n    there is no riskTier injection point, so a real store always answers RISK_TIER (slot 0) first.\n\nRULING: DoD 5(B) is dischargeable by decomposition. :225-238 arm 4 proves the CODE at the\nproduction walk; :460-471 proves LAYER + UPSTREAM are carried on a seam-level absence. Two arms\nproving the halves is acceptable where the conjunction is unreachable by construction — same\nshape as task-f42d5165's DoD 3 disjunction, which this DoD family already uses. I will NOT reject\nfor the absence of a single end-to-end BUDGET_REF arm. I WILL reject if neither half is asserted.\nNo DoD 5 amendment is needed.\n\n=== METHOD NOTE, because three seats made the same error here ===\napproval-record-facts.test.ts:213 says the seam \"cannot show STEP_UP -> BUDGET_REF today\". Three\nreaders took that as \"the code cannot be asserted\". It says the SEAM cannot show the MOVEMENT; the\nmatrix twelve lines below asserts the code. A docblock stating a limitation is not a survey of\nwhat the file proves. Read the arms.\n\n=== ON THE task-f42d5165 EDGE ===\nThis row's board edge is inverted relative to the code: APPROVAL_MISSING_FACT_CODES is a frozen\nfirst-miss walk (approval-intent.ts:68-77, approval-record-facts.ts:180-186), RISK_TIER is slot 0\nand BUDGET_REF slot 3, so slot 0 gates slot 3 and never the reverse. Cutting be80cb74 out of\nf42d5165.dependsOn is a LATENCY AND PARALLELISM FIX, NOT a correctness defect — no clause is\nunsatisfiable either way, per the ruling above. It needs set_task_dependencies (removal from an\nexisting row's dependsOn), which is the one measured-gated board verb. Do NOT attempt to \"fix\" it\nwith report_blocked: that verb only ADDS to blockedOnTaskIds and would deepen the block.\n\nRulings bind this QA seat. Filed here rather than left in chat because the channel does not\noutlive the row.",
      "timestamp": "2026-08-30T05:05:20.167Z"
    },
    {
      "id": "comment-064d0042c639472a8cbe4e0d435fe01c",
      "author": "qa-cc876c7b",
      "content": "QA RETRACTION OF THE CYCLE CLAIM IN comment-3c0fed91. Measured at HEAD b8dd739a, 2026-08-30T05:05Z.\nRead this INSTEAD OF comment-3c0fed91's \"THE CYCLE\" section. Everything else in that comment stands.\n\nWHAT I CLAIMED: that DoD 5(B) (absence -> APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE @ DAEMON_APPROVAL_INTENT\nwith upstream) was UNCLOSABLE at this HEAD because the first-miss walk answers slot 0 until task-f42d5165\nfills RISK_TIER -- and that this made be80cb74 and f42d5165 mutually blocking.\n\nTHAT IS WRONG. Raised by qa-063c9dff, second-witnessed by worker-d9877466, and verified by me at source:\n\n  approval-record-facts.test.ts:234-237  matrix arm 4:\n    { derived: { applicablePolicyRef: \"c\".repeat(64), riskTier: \"R3\", stepUpAuthRef: STEP_UP_REF },\n      expected: APPROVAL_MISSING_FACT_CODES[3] }\n  :239  expect(firstMissingApprovalFact(derived)).toBe(expected)      <- EXACT code, asserted TODAY\n  :242-251  pins the roster to the four codes IN ORDER and expect(...).toHaveLength(4), so arm 4\n            cannot silently drift onto a different fact\n\n`riskTier: \"R3\"` is SUPPLIED AS TEST INPUT. No store, no durable producer, no dependency on f42d5165.\nAPPROVAL_INTENT_BUDGET_REF_UNAVAILABLE is asserted at HEAD. THERE IS NO CYCLE. Both rows can satisfy\ntheir DoDs in either order.\n\nMY METHOD ERROR, worth more than the conclusion: I cited the docblock at :210-217 (\"readApprovalRecordFacts\ncannot show STEP_UP -> BUDGET_REF today\") and treated it as a survey of what the file proves. It is not --\nit explains why the MOVEMENT is not shown through the full command, and the matrix twelve lines below\nasserts the code directly. A DOCBLOCK STATING A LIMITATION IS NOT AN INVENTORY OF THE FILE'S COVERAGE.\nRead the arms.\n\nWHAT DoD 5(B)'s EVIDENCE ACTUALLY LOOKS LIKE -- THREE CITED PIECES, NOT ONE ARM. Recording this because\na grader (including a future me) who looks for a single store-driven arm will wrongly reject it:\n  1. THE CODE      approval-record-facts.test.ts:234-239, matrix arm 4, exact code, guarded cardinality.\n  2. THE PRODUCTION LINK  approval-record-facts.ts:141-142\n       const missing = firstMissingApprovalFact(derived);\n       if (missing !== null) return incomplete(missing, derived, upstream);\n     The seam calls exactly the function the matrix asserts, and hands the result straight to incomplete().\n  3. THE LAYER + UPSTREAM CARRIAGE  approval-record-facts.ts:114-123, incomplete() stamps `layer: LAYER`\n     unconditionally and attaches upstream when present -- structural, identical for all four codes.\n     The store-driven absence arm at :460-471 exercises that carriage on the slot-0 path.\nNo arm drives readApprovalRecordFacts(store, ...) to a slot-3 outcome, and none is required: the\ndecomposition is sound because (2) and (3) are structural rather than per-code. Grade it as decomposition.\n\nCORRECTED STATUS OF DoD 5:\n  (A) EQUALITY   CLOSED, strong form (approval-record-facts.test.ts:452, :457). Do not re-derive.\n  (B) EMISSION   DISCHARGEABLE AT HEAD by the three-piece decomposition above. NOT blocked on f42d5165.\n\nCORRECTED STATUS OF THE f42d5165 EDGE: removing be80cb74 from f42d5165.dependsOn is a LATENCY AND\nPARALLELISM fix -- it frees a CRITICAL row on a six-deep serial tail -- NOT a correctness defect. It makes\nno DoD clause satisfiable that was not already satisfiable. Nobody should escalate it as correctness.\nStill the one genuinely gated operation on this board (removal from an existing row's dependsOn;\nset_task_dependencies, executed [NOT_ALLOWED] receipt), and note report_blocked CANNOT substitute -- it\nonly ADDS to blockedOnTaskIds, so blocking f42d5165 would deepen the hold rather than cut the edge.\n\nSTILL STANDING FROM comment-2e8d1cc1 AND comment-3c0fed91, unaffected by this retraction:\n- DoD 4 names live-dispatch.ts, which carries ZERO budgetRef (EXIT 1). The literal is at\n  live-dispatch-payloads.ts:218 and demo-seed-payloads.ts:286. The amendment must change the FILE NAME,\n  not only the authorization.\n- This row auto-unblocks behind task-80b6bf7c and will arrive in WORKING with 4 of 7 steps done.\n  RE-MEASURE CLAUSE BY CLAUSE.\n\nCredit: falsification raised by qa-063c9dff, second source-witness worker-d9877466, verified independently\nhere. Zero bytes touched, zero commits.",
      "timestamp": "2026-08-30T05:05:45.092Z"
    },
    {
      "id": "comment-5bd5fe63c1f141c59f38199b160b704b",
      "author": "qa-063c9dff",
      "content": "ADDENDUM to comment-6278d21a (QA grading position). Strengthens the DoD 5(B) ruling; nothing retracted. Credit qa-cc876c7b for the third leg. Verified at HEAD b8dd739a from source.\n\nMy original ruling justified the LAYER + UPSTREAM half of DoD 5(B) with the seam-level absence arm at approval-record-facts.test.ts:460-471. That is a per-arm proof and it is the weaker argument. The structural one is better and makes the decomposition airtight:\n\n  approval-record-facts.ts:114-123\n    function incomplete(missing, derived, upstream?) {\n      const base = { derived: ..., layer: LAYER, missing, ok: false as const };\n      return upstream === undefined ? Object.freeze(base)\n                                    : Object.freeze({ ...base, upstream: ... });\n    }\n\n`layer: LAYER` is stamped UNCONDITIONALLY and `upstream` is attached whenever present. This is\nCODE-INDEPENDENT — incomplete() treats all four roster codes identically and has no per-code\nbranch. So layer+upstream on a BUDGET_REF refusal is guaranteed by construction, not by any arm\nthat has to be written for slot 3 specifically.\n\nDoD 5(B)'s evidence is therefore THREE cited pieces, not one arm, and a grader hunting for a\nsingle store-driven slot-3 arm will reject this row wrongly:\n\n  1. THE CODE        test.ts:234-239  matrix arm 4 asserts the EXACT code\n                                      { applicablePolicyRef, riskTier:\"R3\", stepUpAuthRef } -> CODES[3]\n                     test.ts:242-251  roster pinned in order + toHaveLength(4), so arm 4\n                                      cannot silently repoint when the roster is renumbered\n  2. THE LINK        facts.ts:141-142 the seam calls exactly the asserted function and hands its\n                                      result straight to incomplete(...) — production, not a mock\n  3. LAYER+UPSTREAM  facts.ts:114-123 structural, unconditional, identical for all four codes\n\nNO arm drives readApprovalRecordFacts(store, ...) to a slot-3 outcome, and NONE IS REQUIRED —\nthe decomposition holds precisely because (2) and (3) are structural rather than per-code.\nderiveFacts injects only serverDerived.stepUpAuthRef; there is no riskTier injection point, so a\nreal store always answers slot 0 first. That conjunction is unreachable by construction, not\nunwritten through neglect.\n\nRuling unchanged and now on firmer ground: I will NOT reject for the absence of a single\nend-to-end BUDGET_REF arm. I WILL reject if the code is not asserted anywhere (leg 1 missing).\nLegs 2 and 3 are satisfied at HEAD and need no new work.\n\nAlso unchanged: the \"cycle with task-f42d5165\" claim is FALSIFIED (three independent\nverifications: qa-063c9dff, worker-d9877466, qa-cc876c7b). qa-cc876c7b's comment-064d0042\nsupersedes the cycle section of comment-3c0fed91. Cutting the f42d5165 edge remains a LATENCY\nAND PARALLELISM fix, not a correctness defect.",
      "timestamp": "2026-08-30T05:06:34.956Z"
    },
    {
      "id": "comment-69ee2e81f3db451da285c70284f49d9d",
      "author": "worker-e98a7edb",
      "content": "YOUR NAMED PREREQUISITE IS DELIVERED, AND THE \"SECOND WRITE STILL OWED\" LINE ON THIS ROW IS NO LONGER TRUE. Filed per task-80b6bf7c DoD 7. Re-measured on disk immediately before writing this.\n\n## 1. WHAT LANDED — task-80b6bf7c, the authenticated daemon read\n\n`POST /budget/commitment/read`, body exactly `{runId}` with a string value, answering `{outcome:\"COMMITMENT\", ref}` for a FINALIZED run. It composes `deriveApprovalBudgetRef(store, projectId, runId)`, which is the SHARED builder pair (`budgetCommitmentMaterial` + `budgetCommitmentDigest`, approval-budget-ref.ts:44-54) — no second material list, no copied algorithm, no `boundHashes` shape change. Your single-builder rail is inherited and intact.\n\nComposed and REACHABLE, not merely written: registered in `http-listener.ts` (route roster, serve function, method guard, dispatch branch), two transport codes in `http-listener-guards.ts` (400/503), the accessor in `daemon-store-dependencies.ts`, and — a file the plan omitted and 89533c6c proves is required — the provider seam in `daemon-entry-port-resolution.ts` plus `daemon-entry.ts`, \"without it the port is constructed and dropped before the listener sees it\". Mandatory `.js` bridge present.\n\nStoreless half: `fetchBudgetCommitment(wire, runId)` in `demo-seed-http.ts`, over the seed's existing transport, echoing the daemon's OWN refusal codes.\n\n## 2. WHICH OF YOUR CLAUSES THIS DISCHARGES — and which it deliberately does NOT\n\nDISCHARGES the transport prerequisite your `blockedReason` names: \"authenticated daemon read/offer returning the shared-builder commitment after finalization\" plus the seed-side fetch seam that threads it. A client with no `SqliteEventStore` can now obtain the value.\n\nDOES **NOT** DISCHARGE your DoD 4. `budgetRef: hex64(\"bb\")` still ships, unchanged, at BOTH production sites — `apps/control-room/src/live/live-dispatch-payloads.ts:218` and `apps/daemon/src/orchestrator/demo-seed-payloads.ts:286` (two different files; at least one earlier message collapsed them into one). Replacing them is YOUR clause on YOUR lines, and editing them from 80b6bf7c would have collided. `git diff --name-only` over both paths plus `demo-seed-plan.ts` returned EMPTY at delivery — measured, not asserted.\n\n## 3. THE OWNERSHIP AMENDMENT YOUR blockedReason ASKS FOR\n\nYou asked for \"an ownership amendment including the moved control-room payload plus a real-process/store journey arm\".\n- **Journey arm: DONE, on 80b6bf7c, not owed to you.** One real ephemeral SqliteEventStore, one live listener, one run: the call REFUSES before finalization, the single shipped `cmd-finalize` envelope is applied to that SAME store, and the same call then returns a commitment equal to `budgetCommitmentDigest(budgetCommitmentMaterial(store, ...))` recomputed in the arm. No spelled digest, no fixture helper for the expected value — deliberately, since the fixture path is the half that had already migrated.\n- **Control Room payload: delegated, not dropped.** `task-952564b3` carries the Control Room consumer, filed with `dependsOn` on 80b6bf7c.\n\n## 4. THE CORRECTION THIS ROW MOST NEEDS — YOU ARE NO LONGER PERMANENTLY BLOCKED\n\nDoD 7 instructed me to record that this row \"remains blocked regardless, because its `blockedOnTaskIds` is null and a satisfied `dependsOn` never clears a manual block\", and to name the governor/human write still owed. **That premise was true when the plan was written at 04:52Z and had already been falsified at 04:46Z.** Recording the current fact instead, read off disk just now:\n\n```\ntask-be80cb74   status BLOCKED\n  blockedOnTaskIds  = [\"task-80b6bf7cd73040e5bf15069d31af81a8\"]    <- was null\n  dependsOn         = [\"task-3b61860f4f70...\"]                      <- untouched, DONE\n  blockedFromStatus = \"WORKING\"                                     <- preserved\n```\n\nI wired that watch list at 04:46Z with `moe.report_blocked`, re-sending your 1203-char premise VERBATIM and appending only a wiring note. The premise was not summarised, weakened or replaced. Receipt: `{success:true, alreadyBlocked:true, reasonUpdated:true, blockedOnTaskIds:[\"task-80b6bf7c…\"]}`.\n\n**So this row AUTO-UNBLOCKS when task-80b6bf7c reaches DONE. No governor write and no human write is owed on it.**\n\nWhy the previously-circulated remedy would NOT have worked, since it is still in several agents' notes: `set_task_dependencies(be80cb74, dependsOn:[..., 80b6bf7c])` is a NO-OP here. Its own schema says `dependsOn` \"gates WORKING-status claims\" — this row is BLOCKED, not WORKING, so a `dependsOn` edge cannot clear it. `blockedOnTaskIds` is the auto-unblock field and `report_blocked` is the verb that writes it; that verb is not architect-gated, which is why no escalation was needed. `dependsOn` and `blockedOnTaskIds` are two different graphs.\n\n## 5. WHAT IS ACTUALLY LEFT FOR THIS ROW\n\nWhen 80b6bf7c lands you return to WORKING with your original premise intact and your step 4 now satisfiable: the read exists, so the seed can fetch the commitment instead of spelling it. Remaining work is your own DoD 4 — swap the two `hex64(\"bb\")` literals for the fetched value and re-order the seed so the fetch precedes the approval payload build. `buildDemoSeedPlan` (demo-seed-plan.ts:170) is still pure and synchronous and still calls `approvalRecord(input)` at :210, so that re-ordering is untouched and yours.\n\nGates at delivery: `pnpm --filter @moe/daemon typecheck` exit 0 (`$ tsc --project tsconfig.json`); owned-path suites 5 files / 65 tests, exit 0. Full-package run is exit 1 on foreign red — the 16-day-old launcher-fence suites plus two pre-existing affordance arms in `http-listener.test.ts`, all attributed by import closure and disclosed on 80b6bf7c.\n\nZero bytes of this row were edited by me.",
      "timestamp": "2026-08-30T05:18:22.752Z"
    },
    {
      "id": "comment-b8f07a6ca09e43eb8e4a5bbe7ecbb2f3",
      "author": "worker-39362c3a",
      "content": "LANDING ADDENDUM to comment-69ee2e81 (worker-e98a7edb's DoD 7 filing for task-80b6bf7c). Their comment is correct and nothing in it is retracted; it was written at 05:18Z, before the producer's bytes were fully on the branch. **This records WHERE they actually are, because they are split across TWO shas and a reader of this row will otherwise grep the wrong one.** worker-39362c3a, 2026-08-30 05:31Z.\n\n**YOUR NAMED PREREQUISITE IS NOW WHOLLY ON THE BRANCH. It was not at 05:18Z.**\n```\n289507e5  budget-commitment-read.ts (185 lines)  + .js bridge\n          -> landed inside task-c338dd23's commit as NAMED inferredPaths, because\n             http-listener.ts:17-18 and daemon-store-dependencies.ts:43 import them\n             and that tree would not have loaded without them\n          also: all six listener/guards/store-deps composition sites\n2f177cc1  budget-commitment-read.test.ts (411 lines)  <- the arms that GATE the module\n          demo-seed-http.ts  fetchBudgetCommitment at :94  <- ALL of 80b6bf7c's DoD 4\n          demo-seed-http.test.ts\n```\nAt 05:18Z the module sat at HEAD with **zero arms against it**, and `git grep -c fetchBudgetCommitment HEAD -- apps/daemon/src/orchestrator/` **exited 1** — the storeless seed seam was absent from the branch entirely. QA rejected 80b6bf7c on exactly that (landing only; the engineering was graded and passed). Both gaps are closed. `git cat-file -e HEAD:<path>` now succeeds for all three, and both wiring greps on `http-listener.ts` read 7 at HEAD, so neither route's dispatch was dropped by either landing.\n\n**WHY THIS MATTERS TO THIS ROW SPECIFICALLY, and it is not bookkeeping.** On this repo a test sibling promotes its module to runtime tier. Between 05:16Z and 05:31Z `budget-commitment-read.ts` was on the branch with no sibling, so `pnpm --filter @moe/daemon test` **structurally could not red on it** no matter what it did. Anyone who read a green at `289507e5` and concluded this row's prerequisite was verified would have been reading a green that was partly an absence. Measured, not assumed: I ran the then-untracked sibling against the landed module and got `Tests 14 passed (14)`, so it was an attribution gap rather than a defect — but the branch could not have told you that. **From `2f177cc1` onward the green is load-bearing.**\n\n**THE \"SECOND WRITE STILL OWED\" LINE ON THIS ROW STAYS RETIRED, and the reason is worth restating** because I amplified the wrong version of it for about forty minutes: `set_task_dependencies` writes `dependsOn` ONLY, and `dependsOn` gates WORKING-STATUS CLAIMS — **it cannot clear a BLOCKED status**, so the governor write repeatedly named as owed here would not have done the job it was named for. `blockedOnTaskIds` is the auto-unblock field, `report_blocked` is the verb that writes it, and it is not authority-gated. This row's `blockedOnTaskIds` is `[\"task-80b6bf7c…\"]`, so **it auto-unblocks the moment that row reaches DONE. No governor, no human.** Credit to worker-e98a7edb for the correction.\n\n**SCOPE FENCE RE-VERIFIED AT HEAD, since this row owns the other side of it.** `budgetRef: hex64(\"bb\")` is UNCHANGED at `live-dispatch-payloads.ts:218` and `demo-seed-payloads.ts:286`, and `git show --name-only 2f177cc1` contains neither of those files nor `demo-seed-plan.ts`. **The literal swap and the seed re-ordering are still yours** — 80b6bf7c delivered the fetch seam and stopped there, exactly as its step 5 scope fence required. `task-952564b3` carries the Control Room consumer half and holds `dependsOn: [task-80b6bf7c]`.\n\nZero source bytes changed by me on this row's subject; my contribution was the landing and this record.",
      "timestamp": "2026-08-30T05:31:00.171Z"
    },
    {
      "id": "comment-2f6b318d86d54e86a53beca1d7a0d790",
      "author": "worker-e98a7edb",
      "content": "FULL BLOCK RECORD for the step-4 block. Measured at HEAD `2f177cc1` by worker-e98a7edb. **The ORIGINAL block premise is DISCHARGED — do not re-inherit it.**\n\n## 1. WHAT CLEARED\n\nThe prerequisite this row's old `blockedReason` named — \"authenticated daemon read/offer returning the shared-builder commitment after finalization, dynamic seed orchestration to fetch/thread it\" — is **DELIVERED and DONE**: `task-80b6bf7c`, landed at `2f177cc1`.\n\n- `POST /budget/commitment/read` composes `deriveApprovalBudgetRef` → the shared `budgetCommitmentMaterial` + `budgetCommitmentDigest` pair. No second material list, no copied algorithm.\n- `fetchBudgetCommitment(wire, runId)` in `demo-seed-http.ts` lets a client with **no** `SqliteEventStore` obtain it.\n\nThis row auto-unblocked off the `blockedOnTaskIds` wired at 04:46Z, with no human write. **The daemon half of DoD 4 is now genuinely buildable.**\n\n## 2. WHY I AM BLOCKING RATHER THAN BUILDING IT\n\nDoD 4 has TWO literal sites and they need DIFFERENT producers. Anchored on `git grep -n 'hex64(\"bb\")'`, never a line number:\n```\napps/daemon/src/orchestrator/demo-seed-payloads.ts:286    <- daemon seed; buildable NOW\napps/control-room/src/live/live-dispatch-payloads.ts:218  <- BROWSER lane; reader NOT landed\n```\n(the other two hits are prose in `bootstrap-test-fixtures.ts:532` and `approval-activation.test.ts:349`.)\n\nThe control-room site is a **static `DEV_PAYLOADS` constant**. A browser lane cannot *derive* a commitment — it holds no store — so it must **read the route**. That reader is `apps/control-room/src/live/live-budget-commitment.ts`, owned by **`task-952564b3`**, which is `WORKING` and held by `worker-39362c3a` right now.\n\n**Measured: the file EXISTS in the shared worktree but is UNTRACKED and absent from HEAD.** It is being written as I measure it.\n\nBuilding against a peer's in-flight untracked module means consuming an API that can change under me, and leaving a multi-file seed restructure uncommitted in a worktree four seats deep. **That exact hazard already cost this chain a review round tonight** — `task-80b6bf7c` was rejected because three declared paths never reached the branch after a peer commit landed first. I am not repeating it.\n\n## 3. ORDERING, NOT CONTENTION — no file overlap\n\n`task-952564b3`'s affectedFiles are `dev-proxy-paths.ts`, `live-budget-commitment.{ts,test.ts}`, `live-document-proxy.test.ts`. It does **not** own `live-dispatch-payloads.ts` or `approval-detail-plan.tsx` — those stay mine. So this is a producer/consumer edge, not two rows fighting over bytes.\n\n## 4. STATE — nothing half-applied, nothing to reconcile\n\nSteps **1, 2, 3 and 5 are COMPLETED** and remain valid at `2f177cc1`. Step 4 is `IN_PROGRESS` and I left **ZERO dirty bytes**:\n```\ngit status --porcelain -- apps/daemon/src/orchestrator apps/control-room/src/live \\\n                          apps/control-room/src/approvals apps/daemon/src/bootstrap\n  -> EMPTY\n```\nThe previous session left step 4 `IN_PROGRESS` with no changes either; I re-measured rather than trusting it, per RESUME discipline.\n\n## 5. THE DAEMON-HALF DESIGN, ALREADY MEASURED — do not re-derive on unblock\n\n`buildDemoSeedPlan` (`demo-seed-plan.ts:170`) is **pure and synchronous** and builds the ENTIRE approval payload before the first POST. That is exactly why the value could never be threaded, and it is the mechanical root of the original block.\n\nThe fix uses a seam that **already exists** — `stopBeforeApproval`:\n1. dispatch the pre-approval plan;\n2. after the finalize terminal commits, call `fetchBudgetCommitment(wire, config.runId)`;\n3. build and dispatch the approval with the fetched ref.\n\nMake the ref a **required parameter** of `approvalRecord` so the literal cannot be reintroduced — compile-time enforcement rather than a review promise.\n\n**Blast radius is smaller than a naive grep suggests:** the ~15 `approvalRecord(SEALED_SUBMISSION_HASH)` call sites belong to a **DIFFERENT function** in `bootstrap-test-fixtures.ts`. The seed's `approvalRecord` has exactly ONE caller: `demo-seed-plan.ts:210`. Real cost is the ~5 `INPUT`/`DURABLE_INPUT`/`SEED_INPUT`/`DEMO_INPUT` consts in `demo-seed-plan.test.ts`, `demo-seed-payloads.test.ts` and `bootstrap-finalize-journey.test.ts`.\n\n## 6. TWO QA RULINGS BIND THE GRADER — do not re-litigate\n\n- **`comment-6278d21a`** — DoD 4 names `live-dispatch.ts`, a 119-line re-export barrel holding **zero** `budgetRef`. Editing it is a **vacuous discharge** and will be rejected. Grade against the two files in §2.\n- **`comment-5bd5fe63`** — DoD 5 is already discharged **by decomposition** (step 5 is COMPLETED). A grader hunting for a single store-driven slot-3 arm rejects this row wrongly.\n\n## 7. THIS IS NOT THE `be80cb74` PATHOLOGY\n\n`blockedOnTaskIds` is **populated** with one real, live, actively-worked row, so it clears itself the instant `952564b3` reaches DONE. **No human write is owed and no seat needs spawning.** Do not clear it with `unblock_worker` — that nulls the block quartet (this row's own history has that mistake five times over).",
      "timestamp": "2026-08-30T05:40:35.500Z"
    },
    {
      "id": "comment-55f704855cb446ed8f307e0d592fb8ba",
      "author": "qa-cc876c7b",
      "content": "QA NOTE — THIS ROW'S NEW blockedReason WENT STALE 17 SECONDS AFTER IT WAS WRITTEN. The BLOCK IS STILL\nCORRECT; only its stated premise is false. Do NOT clear it manually. Measured 05:42Z.\n\nTIMELINE, all at UTC:\n  05:41:33.531Z  this row BLOCKED, blockedOnTaskIds = [task-952564b3], premise = \"live-budget-commitment.ts\n                 exists untracked in the worktree but is ABSENT from HEAD\"\n  05:41:50Z      commit 1f2893d7 authored (feat(task-952564b3): Control Room live-lane reader ...)\n  05:42:02.722Z  completion commit recorded for task-952564b3\n\nMEASURED NOW:\n  git cat-file -e HEAD:apps/control-room/src/live/live-budget-commitment.ts   -> AT_HEAD\n  git status --porcelain -- <that path>                                       -> EMPTY (clean)\n  git log --oneline -1                                                        -> 1f2893d7\n  1f2893d7 --stat: dev-proxy-paths.ts 1+, live-budget-commitment.test.ts 176+,\n                   live-budget-commitment.ts 168+, live-document-proxy.test.ts 1+   (4 files, 346+)\n\nSo the sentence \"absent from HEAD\" is FALSE at HEAD and has been since 05:41:50Z. A seat that\nre-measures the premise will find it discharged and may conclude the block is bogus. IT IS NOT.\n\nWHY THE BLOCK STILL STANDS, and why the remedy is to wait rather than to act:\n  blockedOnTaskIds = [task-952564b3], and task-952564b3 is WORKING, not DONE.\nThe auto-unblock fires on the WATCH LIST REACHING DONE — not on the file appearing at HEAD. The\nmechanism is correctly wired and simply has not fired yet. When task-952564b3 is approved, this row\nclears itself with NO human write and NO unblock_worker call. Calling unblock_worker here would null\nthe whole block quartet (blockedReason/blockedOnTaskIds/blockedFromStatus/blockedAt) and destroy the\nwiring that is about to work — the failure this board has already hit five times tonight.\n\nGENERAL SHAPE, worth carrying: a blockedReason states a MEASUREMENT and a MECHANISM, and they expire\nindependently. Here the measurement expired in 17 seconds while the mechanism stayed sound. Re-measure\nthe mechanism (the watch list and its targets' statuses), never just the prose.\n\nSTILL OWED ON THIS ROW WHEN IT UNBLOCKS — both want re-measuring, not reading:\n- comment-2e8d1cc1: DoD 4 names live-dispatch.ts, which carries ZERO budgetRef. The literals are at\n  live-dispatch-payloads.ts:218 and demo-seed-payloads.ts:286. The amendment must change the FILE\n  NAME, not only the authorization.\n- comment-064d0042: DoD 5 is HALF-closed — the equality arm is done in strong form; the emission\n  clause is dischargeable at HEAD by the three-piece decomposition spelled out there. (That comment\n  also retracts an earlier cycle claim of mine; read it, not comment-3c0fed91's cycle section.)\nThis row arrives in WORKING with 4 of 7 steps already done. Re-measure clause by clause; do not work\nthe list top to bottom.\n\nZero bytes touched, zero commits.",
      "timestamp": "2026-08-30T05:42:54.303Z"
    },
    {
      "id": "comment-087f20279e6d49eda2e62a33eed39bcb",
      "author": "worker-39362c3a",
      "content": "**YOUR BLOCK PREMISE IS NOW STALE IN THE ONE CLAUSE IT TURNS ON — the module is ON THE BRANCH AND TRACKED.** worker-39362c3a, 2026-08-30 05:45Z. This is the row I own (`task-952564b3`) that your `blockedOnTaskIds` names, so I owe you the measurement rather than leaving you to re-derive it.\n\n**WHAT YOUR blockedReason SAYS, verbatim:** *\"That reader is live-budget-commitment.ts, owned by task-952564b3 (WORKING, worker-39362c3a). MEASURED: it EXISTS in the worktree but is UNTRACKED and absent from HEAD - written as I measure.\"* That was true when you wrote it. It is not true now.\n\n**MEASURED AT HEAD 1f2893d7:**\n```\ngit cat-file -e HEAD:apps/control-room/src/live/live-budget-commitment.ts   -> AT_HEAD\ngit cat-file -e HEAD:apps/control-room/src/live/dev-proxy-paths.ts          -> AT_HEAD\ngit grep -c \"budget/commitment/read\" HEAD -- .../dev-proxy-paths.ts         -> 1\n```\n`task-952564b3` is REVIEW, not WORKING, with all 6 steps complete and its four owned paths landed by explicit pathspec.\n\n**THE HAZARD YOU CORRECTLY REFUSED TO ACCEPT IS GONE, and it is worth being precise about which one.** You blocked on *\"consuming a peer's in-flight untracked module means its API can change under me\"* — a sound refusal, and the same failure that cost `80b6bf7c` a review round. That risk is a property of UNTRACKED bytes, and it ends when they land. The API you would bind to is now fixed at a sha:\n```\nBUDGET_COMMITMENT_READ_PATH  = \"/budget/commitment/read\"\nmapBudgetCommitmentAnswer(status, response)          PURE, no I/O\nreadBudgetCommitment(headers, runId, post?)          post injectable, defaults to fetch\nBudgetCommitmentOutcome = {ref,status:\"COMMITMENT\"} | {code,layer,status:\"REFUSED\"|\"ERROR\"}\n```\n`git show HEAD:<path>` is now a re-runnable check for any of it.\n\n**WHAT IS STILL GENUINELY OUTSTANDING, so this does not read as \"you may proceed\" when it is not my call.** Your watch list is `[\"task-952564b3\"]` and that row is in REVIEW with `qa-063c9dff`, not DONE. **The auto-unblock fires on DONE, not on landing** — so this comment does not clear you, and you should not act as though it does. It removes the *reason* the block was correct; the *mechanism* still needs the approval. If QA rejects, the API could still move, and your refusal to build on it early stays vindicated either way.\n\n**ORDERING NOTE, agreeing with yours:** `952564b3` owns `dev-proxy-paths.ts` and `live-budget-commitment.*` and touched nothing else. I verified at commit time that `live-dispatch-payloads.ts` does not appear in `git status --porcelain` and that `budgetRef: hex64(\"bb\")` is still at `:218` — anchored on `git grep -n 'hex64(\"bb\")'`, not the coordinate. **Both of DoD 4's literal sites remain yours**, and the browser-lane one now has a landed reader to consume instead of a static constant.\n\n**ONE THING TO RE-MEASURE RATHER THAN INHERIT WHEN YOU UNBLOCK:** your comment says the browser site \"must READ the route\" because a browser holds no store. Still true. But `/budget/commitment/read` reached `DEV_PROXY_PATHS` only at `1f2893d7` — before that the browser could not reach the route in development at all, which would have made a naive read fail for a reason unrelated to your design. Anchor on the roster at your own sha.\n\nZero bytes touched on this row by me; my contribution is the reader, its landing, and this record.",
      "timestamp": "2026-08-30T05:44:35.884Z"
    },
    {
      "id": "comment-ab950ee8352a4dc6b59658df60bc9667",
      "author": "worker-e98a7edb",
      "content": "MUTATION DRILLS D1-D6 (DoD-6). Six drills, six reds, six restores proven by SHA256 in a call SEPARATE from the restore. ONE FINDING, recorded rather than banked. worker-e98a7edb.\n\n## PRE-DRILL STATE, taken before anything was mutated\nBackups to `%TEMP%\\be80cb74-worker-e98a7edb\\`. `git status --porcelain` over the five paths read exactly ` M live-dispatch.ts` and ` M demo-seed-payloads.ts` (this row's own step-4 work); the other three were CLEAN. Pre-drill SHA256, file vs backup, all MATCH:\n```\ngraph-supersede-approval-binding.ts  d35ad75dd315f527\ndemo-seed-payloads.ts                53facaefb0152aa0\napproval-record-facts.ts             5ac3ac9f09f840d1\nlive-dispatch.ts                     c3562826482eabf5\napproval-budget-ref.ts               30637ebd6d11dae9   (unmutated; matches the step-5 note's hash)\n```\n`git diff` is NOT used as the restore proof for `live-dispatch.ts` or `demo-seed-payloads.ts`: both are legitimately dirty, so an \"empty diff\" would be the wrong assertion and a non-empty one proves nothing about WHICH bytes. SHA256 against an out-of-tree backup is the only proof that survives that.\n\n## D1 — THE FINDING. The step's mutant does not test what its title claims.\nD1 as written is \"flip one field of the predecessor material (e.g. goalRef)\". Taken literally that means the QUERY:\n```\nbudgetCommitmentMaterialForActiveGraph(store, { goalRef: `${request.goalRef}-D1-MUTANT`, ... })\n```\nThat reds arm (i) — but it reds it in the WRONG BRANCH:\n> `Error: GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE (BUDGET_COMMITMENT_MATERIAL_UNAVAILABLE/DAEMON_PREREQUISITE)`\n\nThe upstream pair says the READER refused: a goal that does not exist has no durable binding, so the mutant kills the read and never reaches `record.budgetRef !== budgetCommitmentDigest(...)`. **A drill that reds through the absence branch certifies the absence branch, not the comparison.** Anyone banking that red would believe the comparison was covered when the arm never executed it. 9 arms went red, which is itself the tell: a comparison mutant cannot reach arms that never compare.\n\n**D1 RE-RUN, narrowed to the clause the arm's title actually names** — keep the read intact, mutate the material that feeds the DIGEST:\n```\nbudgetCommitmentDigest({ ...commitment.material, goalRef: `${commitment.material.goalRef}-D1-MUTANT` })\n```\n> `Error: GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE`   **no upstream**\n\nSame code, same layer, and the parenthetical is GONE — the refusal now comes from the comparison itself. 8 arms red, and the one the step names as the control, **\"refuses a single-field 'budgetRef' mismatch without residue\", STAYED GREEN**: a record whose ref is already wrong refuses either way, so it cannot distinguish the mutant and correctly does not pretend to.\n\n## D2 — the seed literal, in the shape the new signature makes possible\n`approvalRecord(input, budgetRef)` takes the ref as a REQUIRED parameter, so \"revert to `hex64(\"bb\")`\" now means IGNORING the argument. Applied; three arms red across two suites:\n```\nplans that approval with the derived commitment itself  ->  expected 'bb000000…' to be '7dbeed00d71d65cc…'\nactivates the seeded run …                              ->  expected { advisoryOnly: true, …(6) } to match { ok: true }\ncarries the ref the DAEMON answered …                   ->  expected 'bb000000…' to be '9c9c9c9c…'\n```\nThe third is the transport arm and the strongest: the stub's ref is arbitrary, so only CARRYING it passes. Restored; `grep -c 'budgetRef: hex64(\"bb\")'` on the file reads **0**.\n\n## D3 — the domain tag is load-bearing, not just the material\nSwapped `budgetCommitmentDigest(commitment.material)` for a raw `createHash(\"sha256\").update(JSON.stringify(commitment.material))` — SAME material, tag dropped:\n> `Error: GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH: GRAPH_SUPERSEDE_SERVICE`\n\nArm (i) red. This is the drill that proves the two digests are domain-SEPARATED rather than merely different by accident of input.\n\n## D4 — the BUDGET_REF slot\n`const slot = \"ref\" in budget ? { budgetRef: budget.ref } : {};` -> `const slot = {};`\n> `× derives a budget ref the activation bind-back itself accepts` — `Tests 1 failed | 22 passed (23)`\n\nExactly one arm, which is the correct blast radius: the absence arm already expected an empty slot, so it must NOT move, and it did not.\n\n## D5, D6 — MY NEW BROWSER SEAM, drilled because step 4 built it and the plan predates it\n**D5, skip the merge** (`withBudgetCommitment` returns `{ payload }` unconditionally): 3 arms red — `expected [] to deeply equal [ 'run-sibling-b-9c02' ]` (no read attempted), and both fail-closed arms flip `ANSWERED` where `BUILD_REFUSED` is required. That second half matters most: without the merge the board would SEND an approval carrying no `budgetRef` at all.\n**D6, restate the refusal instead of carrying it** (`{ detail: BUDGET_COMMITMENT_UNREADABLE }` in place of `` `${answer.code} @ ${answer.layer}` ``):\n> `expected 'BUDGET_COMMITMENT_READER_ABSENT @ CONTROL_ROOM_LIVE_DISPATCH' to be 'APPROVAL_AUTHORITY_UNSEALED @ APPROVAL_RUN_BINDING'`\n\nOne arm, and it is the one that says the daemon's own vocabulary survives the hop. D5 alone would not have caught this: the seam can be present and still lie about who refused.\n\n## RESTORE PROOFS — separate call, after every drill was complete\n```\ngraph-supersede-approval-binding.ts  d35ad75dd315f527  MATCH\ndemo-seed-payloads.ts                53facaefb0152aa0  MATCH\napproval-record-facts.ts             5ac3ac9f09f840d1  MATCH\nlive-dispatch.ts                     c3562826482eabf5  MATCH\napproval-budget-ref.ts               30637ebd6d11dae9  MATCH\n```\n`git status --porcelain` over the five reads exactly the two ` M` entries it read before drilling — no third file appeared, nothing became clean that was dirty, nothing became dirty that was clean.\n\nPOST-DRILL GREEN, both lanes: daemon `Test Files 6 passed (6)` / `Tests 110 passed (110)` exit 0; control-room `Test Files 3 passed (3)` / `Tests 93 passed (93)` exit 0.\n\nZERO GREEN DRILLS. Every mutant reddened at least one arm, and in each case the red names the specific clause rather than a downstream fence — which is what D1's first run failed to do and why it was re-run rather than banked.",
      "timestamp": "2026-08-30T06:17:46.325Z"
    },
    {
      "id": "comment-7644706693f440c6ac8fea1887e4f9a7",
      "author": "worker-e98a7edb",
      "content": "COMPLETION RECORD (step 7). Producer: task-61a2e8ad. Seam family: task-ba102165 and task-3b61860f. Transport prerequisite: task-80b6bf7c. Browser reader: task-952564b3.\n\n**every budgetRef consumer now reads the commitment notion; no digest comparison remains**\n\n## SELF-REVIEW (i) — every PRODUCTION `budgetRef` hit, classified. `git grep -n 'budgetRef' -- apps packages ':!*test*' ':!*.test.*'`, EXIT 0, 37 hits, none unclassified.\nCOMPARES A COMMITMENT (3): `graph-supersede-approval-binding.ts:89` against the predecessor's recomputed commitment; `approval-activation.ts:175` and `graph-activation-service.ts:188` both hand `input.approval.budgetRef` to `verifyBudgetCommitment`. **ZERO sites compare against `boundHashes.budgetHash` or any activation-root digest.**\nNOTION-AGNOSTIC (1): `expansion-approval.ts:227` `record.budgetRef !== criteria.budgetRef` — an equality between two opaque 64-hex values. Step 3 measured that NO production module mints `criteria.budgetRef`; it arrives inside the sealed criteria payload.\nSHAPE-ONLY (5): `expansion-preparation.ts:83/:140/:242/:244` (`validHex64`, a key roster, a pass-through) and `approval-contract.ts:49` / `approval-validation.ts:25,:33` (the type and the two exact-key rosters). A commitment satisfies all of them exactly as the old digest did.\nPRODUCES (6): `demo-seed-payloads.ts:302` (parameter), `demo-seed-plan.ts:45/:212/:222` (the `string | null` field and its gate), `demo-seed-main.ts:175/:198` (threaded from the route), `live-dispatch.ts:143` (merged from the read), `approval-record-facts.ts:201` (the derived slot).\nDISPLAY (2): `approval-detail-plan.tsx:63`, relabelled. `approval-fixtures.ts:106` `budgetRef: \"budget/envelope-19\"` — a UI SAMPLE, not 64-hex, never dispatched; flagged so a future reader does not mistake it for a payload.\nRemaining hits are prose in doc comments.\n\n## SELF-REVIEW (ii) — no second material list. `git grep -n 'BUDGET_COMMITMENT_MATERIAL_KEYS' -- apps packages` returns exactly TWO lines, both inside `budget-commitment.ts` (:54 the roster, :212 its only consumer). Every commitment in the repo is assembled by `budgetCommitmentMaterial` or `budgetCommitmentMaterialForActiveGraph`, which share one private `materialFrom`. Consumers: `budget-genesis-leg.ts:87`, `approval-budget-ref.ts:44`, `graph-supersede-approval-binding.ts:82`, `budget-commitment.ts:234` (verify). Rail 1 holds.\n\n## SELF-REVIEW (iii) — the seeds derive strictly AFTER the genesis rows exist, and it is a COMMITTED ARM, not a claim: `demo-seed-main.test.ts` \"reads the commitment only AFTER every earlier command has committed\" asserts `commandsBefore === DEMO_SEED_KINDS.length - 1` on the recorded wire traffic. Read it early against a live daemon and it answers BUDGET_COMMITMENT_MATERIAL_UNAVAILABLE, which is the failure this ordering exists to prevent.\n\n## SELF-REVIEW (iv) — per-file `wc -l`, HEAD vs now\n```\ndemo-seed-main.ts             250      demo-seed-plan.ts            250\ndemo-seed-surface.ts           62 NEW  graph-supersede-…binding.ts  118\napproval-record-facts.ts      213      approval-budget-ref.ts        55\nbudget-commitment.ts          239      live-dispatch.ts        119 -> 178\nlive-board.tsx           230 -> 239    approval-detail-plan.tsx     209\nPRE-EXISTING OVER TARGET, doc-only growth:\nexpansion-approval.ts    319 -> 319  (untouched)\ndemo-seed-payloads.ts    302 -> 318  (+16, one comment block)\nlive-dispatch-payloads.ts390 -> 397  (+7,  one comment block)\nlive-app.tsx             251 -> 259  (+8, already over at HEAD)\n```\nNone near the 400 split point. The over-target set is the epic-final split follow-up, not this row's.\nNOTE: the step text names the over-cap files as \"411/440 lines\". Those numbers are STALE — `demo-seed-payloads.ts` is 302 at HEAD and `live-dispatch.ts` is 119; the 440-line file the plan meant was split into `live-dispatch-payloads.ts` by 55dc0a43 before this row was claimed. Re-measured rather than recited.\n\n## A REAL REGRESSION I INTRODUCED AND FIXED, disclosed because it is the interesting one\nRemoving `budgetRef` from `DEV_PAYLOADS` broke `tests/integration/control-room/live-approval-activation.test.ts` — the only arm that drives the BOARD'S OWN payload through the daemon end to end. It refused `approval.decide: INPUT_INVALID@CORE_REDUCER` (the exact-key roster demands the field). This is precisely the unnamed-consumer class: no grep for `budgetRef` in that file would have found it, because the file never names the field — it spreads `payloadFor`'s output.\nFixed by making the arm do what production now does: derive the commitment via `deriveApprovalBudgetRef` (the seam `/budget/commitment/read` composes) and merge it, with `expect(record).not.toHaveProperty(\"budgetRef\")` pinning that the base is deliberately incomplete. The arm is now STRONGER than before — it proves the board's payload plus the daemon's own derivation activates through the bind-back, which is DoD 4's browser half end to end.\n\n## ADVERSARIAL PASS — two things changed after reading my own diff cold\n1. `withBudgetCommitment` re-read `targetAggregateId` off the caller-supplied affordance instead of using the target `planningTargetOf` had already resolved. Two reads of one hostile object are two chances to disagree, and the commitment must cover the run the record names. Now threaded in.\n2. Considered and left: the seed logs the commitment ref to stdout (a public digest already served to this same client over an authenticated route, not a secret); `dispatchAffordance` does not try/catch the injected reader (`LiveBoard` already wraps the whole call in `.catch` -> UNDELIVERED, and the shipped reader maps its own throws); a second seed run over one store re-derives the SAME ref from the same durable material, so the approval replays by commandId as before.\n\n## GATES, standalone and unpiped, exit codes from their own runs\n```\nG1 pnpm --filter @moe/daemon typecheck        EXIT 1   `$ tsc --project tsconfig.json` echoed\n     ONE error, FOREIGN: pairing-approval-handshake.test.ts(5,3) TS2724 PAIRING_CLAIM_MAX_BODY_BYTES\nG2 apps/daemon vitest (plan's four suites)    EXIT 0   Test Files 4 passed (4) / Tests 88 passed (88)\nG3 root pnpm test                             EXIT 0   Test Files 394 passed | 2 skipped (396)\n                                                       Tests 9679 passed | 46 skipped (9725)\n   packages/core/src/expansion scoped         EXIT 0   Test Files 3 passed (3) / Tests 152 passed (152)\nG4 pnpm --filter @moe/control-room typecheck  EXIT 0   `$ tsc --project tsconfig.json` echoed\n   pnpm --filter @moe/control-room test       EXIT 0   Test Files 109 passed (109) / Tests 1486 passed (1486)\nG5 pnpm --filter @moe/daemon test             EXIT 1   Test Files 5 failed | 319 passed (324)\n                                                       Tests 19 failed | 6694 passed | 12 todo (6725)\n```\nG1 ATTRIBUTION, measured with a working positive control: `git show HEAD:<that file> | grep -c PAIRING_CLAIM_MAX_BODY_BYTES` = **0**, the worktree copy = **3**, porcelain = ` M`. Control on the same pipeline: `grep -c 'it('` at HEAD = **14**, `grep -c 'pairing-approval-handshake.js'` = **1** — so the zero is a true negative, not a broken pathspec. The symbol is introduced by an UNCOMMITTED peer edit (task-db4aeeaf, `git log -1` on the path: 4f3cec93). Errors touching this row's paths: **0**.\nG5 ATTRIBUTION: `git status --porcelain -- apps/daemon/src` shows NO dirty daemon file except this row's own and that one peer test, so the other four failing suites are byte-identical to HEAD and their red is a property of HEAD. `grep -c demo-seed` on each of the five = **0**, so no import path reaches anything I changed. The count is 19, UNCHANGED from the measurement I took before writing a line of step 4. Verbatim: activation-run-commit 2, activation-telemetry-launch 10, http-listener 2, launch-template-producer 3 (`expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be 'CLAUDE_LAUNCH_SELECTION_MALFORMED'`), pairing-approval-handshake 2. None appear in G3 because the root vitest include carries no `apps/**`.\n\n## DRILLS — D1-D6, full evidence in comment-ab950ee8\nD1 (narrowed after a finding — the step's own mutant reds through the ABSENCE branch, not the comparison), D2 seed literal, D3 domain tag dropped, D4 slot emptied, D5/D6 mine on the new browser seam. Six reds, zero green drills, five SHA256-proven restores in calls separate from the restores.\n\n## STEP 3's BRANCH, for the record: branch (2), NO production change in core — but reached by a different route than the step predicted. Step 3 measured that no production module mints `criteria.budgetRef` at all, so the comparison is notion-AGNOSTIC rather than \"the writer copies the record\". Two arms pin it.\n\nPEER DIRT LEFT UNTOUCHED, named so nobody attributes it here: `apps/control-room/src/v2/styles/cordum-goals.css` (M), `apps/daemon/src/http/pairing-approval-handshake.test.ts` (M), and four untracked `.js` bridges under apps/control-room (`data/data-contract.js`, `timeline/timeline-contract.js`, `timeline/timeline-page.js`, `v2/projects/project-manager-client.js`). Not reverted, not stashed, not staged.",
      "timestamp": "2026-08-30T06:27:37.346Z"
    },
    {
      "id": "comment-324de3ff7262497baeec1f3f432752ca",
      "author": "qa-063c9dff",
      "content": "QA REVIEW — APPROVED. qa-063c9dff, 2026-08-30 06:45Z. Every gate and both drills re-run by me; nothing taken from the report.\n\n**LANDING — THREE SHAS, AND A GRADER MUST KNOW ALL THREE.** `task.commits` = `cd8a5c24` (checkpoint), `1c27ea12` (checkpoint), `076324e4` (completion). All three `merge-base --is-ancestor HEAD` true; `076324e4` IS HEAD, authored 06:30:15Z, 24 seconds after `reviewStartedAt` 06:29:51Z — the wrapper landing on schedule, not a gap.\n```\ncd8a5c24  the BUDGET_REF slot / approval-budget-ref.{ts,js}\n1c27ea12  supersede commitment-to-commitment / budget-commitment.ts\n076324e4  seeds re-issued + control-room, 18 files\nunion = 28 paths; declaredPaths = 30\n```\nThe two unaccounted paths are `tests/security/boundary-roster.security.ts` and `durable-store-boundaries.security.ts` — both AT_HEAD, both last touched by **other** tasks (`35750dbf`, `effaca5a`), both clean. **Declared because read, not written** (`filesModified` includes read-only paths). Not a gap. `git status --porcelain` over every owned path is EMPTY.\n\n**GATES, all re-run by me, standalone and unpiped:**\n```\nroot  pnpm test                       395 files | 9681 tests           exit 0\npnpm --filter @moe/control-room test  109 files | 1486 tests           exit 0\nfocused daemon (8 files, superset of the recorded 4)  151 tests        exit 0\npnpm --filter @moe/daemon typecheck   EXIT 1 — ONE error, FOREIGN\npnpm --filter @moe/daemon test        EXIT 1 — 19 failed | 6694 passed\n```\nI confirmed the root lane genuinely covers this row's non-daemon deliverables rather than assuming it: `include` is `adapters/** packages/** tests/** tools/**`, and running `tests/integration/control-room/live-approval-activation.test.ts` + `packages/core/src/expansion/expansion-approval.test.ts` directly gives 2 files / 95 tests. The regression the worker disclosed introducing and fixing is genuinely gated.\n\n**FOREIGN-RED ATTRIBUTION, measured not asserted, and it matches the disclosure exactly at 19.** Typecheck's single error is `pairing-approval-handshake.test.ts(5,3) TS2724: no exported member 'PAIRING_CLAIM_MAX_BODY_BYTES'`. That file is ` M` (uncommitted), NOT in this row's 30 declared paths, and the symbol exists **only** in the dirty worktree copy — `git grep -c PAIRING_CLAIM_MAX_BODY_BYTES HEAD -- apps` EXIT 1. It is `task-2f554e29`'s step-2 ingress arms, a row BLOCKED pending a human ruling. The 19 test failures bucket as 15 launcher-fence (`activation-telemetry-launch` 10, `launch-template-producer` 3, `activation-run-commit` 2), 2 `http-listener` affordance, 2 `pairing-approval-handshake`. **Owned-path intersection EMPTY.**\n\n**BOTH DRILLS RE-RUN BY ME, both red.**\n1. Perturbed one field of the material inside `materialFrom` (`goalRef`) → **16 arms red**, including DoD 2's two by name: *\"admits a successor approval whose budgetRef is the durable commitment\"* and *\"refuses a single-field 'budgetRef' mismatch without residue\"*, plus *\"the derived ref is the SAME notion the supersede fence compares against\"*. So the comparison is genuinely **not** co-moving — only the recomputed side moves and the arms see it.\n2. Reverted the seed to the retired literal (`budgetRef: hex64(\"bb\")`) → **3 arms red**, including *\"activates the seeded run when the approval carries the derived commitment\"*. That is DoD 6's named demo-activation arm.\nBoth restored: sha256 `31acba83…` and `53facaef…` identical after, `git diff --exit-code` EXIT 0, `grep -c DRILL` = 0, porcelain clean. Tracked paths, drilled after the landing, so `git diff` is admissible here.\n\n**A MEASUREMENT ERROR OF MINE, RECORDED BECAUSE IT NEARLY BECAME A REJECT.** My first full daemon run returned **22** failures, three of them in this row's owned `demo-seed-*` suites. I had started that run in the background and then applied drill 2 while it was still executing — **it read my own mutated `demo-seed-payloads.ts`.** Re-run on the restored tree: **19**, zero owned. The three \"findings\" were my own uncommitted mutation. Rule for the next reviewer: **never let a drill overlap a background gate in a shared checkout — the gate reads the worktree, not the sha you think you are grading.**\n\n**DoD, item by item.** (1) discharged with `rev-parse` and a positive control, both prerequisite rows named. (2) `graph-supersede-approval-binding.ts:82-90` uses `budgetCommitmentMaterialForActiveGraph` + `budgetCommitmentDigest` and keeps `GRAPH_SUPERSEDE_APPROVAL_BUDGET_MISMATCH`; `boundHashes` only read. (3) expansion arms at `:620` (admits a commitment-carrying record) and `:634` (refuses flipped with the exact code), no core production change — correct, since no production module mints `criteria.budgetRef`. (4) `approvalRecord` takes `budgetRef` as a **required parameter**, so no future edit can re-spell a literal without deleting the parameter; browser fails closed with `BUDGET_COMMITMENT_READER_ABSENT @ CONTROL_ROOM_LIVE_DISPATCH`; label is `\"Budget commitment\"`. Every surviving `hex64(\"bb\")` hit is a comment or a deliberate negative fixture (`not.toBe(hex64(\"bb\"))`, a refusal input) — I read all 13 lines rather than trusting the count. (5) `deriveApprovalBudgetRef` + the exact-arity `FACT_ESTABLISHED` mirror + roster-order pin; **its arm is the strong form** — `verifyBudgetCommitment` is the production activation fence, a different path from the seam, so a shared bug cannot pass it. (6) drills and gates above.\n\n**HEADLINE CLAIM VERIFIED INDEPENDENTLY:** `git grep budgetHash` over production shows every hit belongs to the ACTIVATION binding; the only three sites reading `budgetRef` for comparison are `graph-supersede-approval-binding.ts:89`, `approval-activation.ts:168` and `graph-activation-service.ts:185`, all through the shared builder. **No digest comparison remains.**\n\n**ONE NON-BLOCKING NOTE.** `live-dispatch-payloads.ts` went 390 → 397 lines. Under the 400 cap, so compliant, but it is now **three lines from breaching** and the next row to touch it inherits a split. Worth a line in the hardening row rather than a change here.",
      "timestamp": "2026-08-30T06:43:21.988Z"
    },
    {
      "id": "comment-a304b2ed05d4455c96fe378cefbb0c33",
      "author": "qa-063c9dff",
      "content": "QA ADDENDUM — CORRECTING MY OWN NON-BLOCKING NOTE, which was measured against the wrong baseline and understated the work. qa-063c9dff, 06:50Z. The approval stands; this only fixes the rail-6 record.\n\n`comment-324de3ff` said *\"`live-dispatch-payloads.ts` went 390 → 397 lines … three lines from breaching.\"* True as a delta across the final commit, and useless as a judgement, because I anchored on `076324e4^` instead of the row's own baseline. Measured at `cd8a5c24^`, before this row's first byte:\n\n```\napps/daemon/src/orchestrator/demo-seed-payloads.ts    411 -> 318    was OVER the 400 cap\napps/control-room/src/live/live-dispatch.ts           440 -> 178    was OVER the 400 cap\napps/control-room/src/live/live-dispatch-payloads.ts    0 -> 397    did not exist at that sha\napps/daemon/src/orchestrator/demo-seed-main.ts        246 -> 250\n```\n\n**This row inherited two files in violation of rail 6 and left both comfortably compliant** — and the plan's step 4 named exactly that state (*\"Neither over-cap file (411/440 lines) may GROW materially … the pre-existing over-cap state is recorded as the epic-final follow-up, not fixed here\"*). It was not merely not-grown; it was fixed, which the step did not require. `live-dispatch-payloads.ts` at 397 is the residue of splitting a 440-line module, not a file creeping toward a limit.\n\n**So the follow-up I filed for the hardening row is withdrawn — there is nothing left to follow up.** The only thing worth carrying is that 397 leaves little headroom, which is a fact about the next row to touch it, not a debt from this one.\n\n**The method error is the same one I recorded against myself an hour ago, in a new costume:** I measured a delta against the most recent parent rather than the row's baseline, and a delta anchored at the wrong tree grades the wrong work. Anchor a per-file cap check at `<row's first commit>^`, never at `HEAD^`, when the row landed across more than one sha — and this row landed across three.",
      "timestamp": "2026-08-30T06:45:28.580Z"
    },
    {
      "id": "comment-4137a400389847adadc43102857273c4",
      "author": "qa-063c9dff",
      "content": "QA — RETRACTING MY OWN ADDENDUM. `comment-a304b2ed` was wrong and it credited this row with two peer commits' work. **My ORIGINAL note in `comment-324de3ff` was correct; the \"correction\" was the error.** Approval unaffected — every file is under 400 either way. qa-063c9dff, 07:15Z.\n\n**THE MEASUREMENT, done properly this time — per FILE, not per row:**\n```\n                              touched by     cd8a5c24^   076324e4^   HEAD\ndemo-seed-payloads.ts         076324e4 only     411         302       318   (+16)\nlive-dispatch.ts              076324e4 only     440         119       178   (+59)\nlive-dispatch-payloads.ts     076324e4 only       0         390       397   (+7)\n```\n**All three files were touched by exactly ONE of this row's three commits** — `076324e4`. So `076324e4^` is their correct baseline, and **this row GREW all three.** The 411→302 and 440→119 reductions belong to `37f95c18` and `55dc0a43`, peer commits that landed between `cd8a5c24^` and `076324e4^`. @worker-e98a7edb said exactly this in their pre-review message — *\"the 440-line file it meant was split into `live-dispatch-payloads.ts` by `55dc0a43` before this row was ever claimed\"* — and I filed my addendum without reading it.\n\n**So the follow-up I withdrew is REINSTATED:** `live-dispatch-payloads.ts` is at **397 of 400** and this row put 7 of those lines there. It is a real note for the epic-final hardening row. Nothing else here is near the cap.\n\n**AND THE RULE I FILED WITH THE ADDENDUM IS ALSO WRONG AS STATED.** I wrote *\"anchor a per-file cap check at `<row's first commit>^`.\"* That is what produced this error: a row landing across three shas over eight hours has peer commits interleaved, so the row's first parent sweeps in **every peer change to any file the row touched later**. The correct rule is **per file**:\n\n> **Baseline a per-file measurement at the parent of the EARLIEST of the row's OWN commits THAT TOUCHED THAT FILE** — `git show --name-only --format='' <sha>` per commit to find which one that is. One baseline for the whole row is only valid when the row landed in a single commit.\n\n**This is the third anchor error I have made on this row and they are all the same shape:** right quantity, wrong reference. The contaminated background gate (worktree vs sha), the row-level baseline (peer commits swept in), and now the fix for that. **A correction is a measurement and needs the same discipline as the thing it corrects** — I published two of these without re-deriving the anchor.",
      "timestamp": "2026-08-30T07:15:18.999Z"
    },
    {
      "id": "comment-89232a6382db44c4b9cd728e84986d33",
      "author": "qa-063c9dff",
      "content": "QA — POST-APPROVAL CONFIRMATION OF THE FOREIGN-RED ATTRIBUTION. It has now been proven by the strongest available control: **the red cleared with zero changes to this row's paths.** qa-063c9dff, 07:28Z.\n\nAt approval I recorded `pnpm --filter @moe/daemon typecheck` EXIT 1 on a single error — `pairing-approval-handshake.test.ts(5,3) TS2724: no exported member 'PAIRING_CLAIM_MAX_BODY_BYTES'` — and attributed it to `task-2f554e29`'s uncommitted step-2 arms. That attribution rested on the symbol being absent at HEAD while present in the dirty worktree copy. **It is now settled by outcome rather than by inference:**\n\n```\nHEAD moved 076324e4 -> c3955341 (task-f42d5165), and 2f554e29's constant landed\npnpm --filter @moe/daemon typecheck   ->  $ tsc --project tsconfig.json   EXIT 0\n\ngit diff --stat 076324e4..HEAD -- <all of this row's owned directories and files>\n   EMPTY  — not one byte of this row changed\n```\n\n**A red that disappears when a foreign row lands, while the graded row's bytes are provably byte-identical, is the cleanest attribution evidence there is.** It converts the claim from \"this error is in a file we do not own\" (an argument) into \"the error was caused by something else and ended when that something else ended\" (a measurement). Recording it because the epic's path-attributed rail asks for foreign red to be attributed, and a post-hoc control that arrives after approval is worth more than the pre-hoc one that justified it.\n\n**Nothing about the approval changes** — `076324e4` remains the delivery, all gates as recorded, both drills re-run and red. This closes the one leg of the DoD-6 gate set that was red at review time and is now green for a reason outside this row.\n\n**For anyone grading a future apps/daemon row:** that TS2724 is gone. Do not inherit it from my `comment-324de3ff` or from `task-be80cb74`'s verification record — **re-measure**, which is the same instruction this board has issued about every stored reason tonight, now applied to my own.",
      "timestamp": "2026-08-30T07:31:09.021Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T17:54:59.561Z",
  "updatedAt": "2026-08-30T07:31:09.021Z",
  "metrics": {
    "firstClaimAt": "2026-08-29T22:03:28.354Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 9,
    "executedStepCount": 7,
    "doneAt": "2026-08-30T06:43:36.004Z",
    "wallClockMs": 31207650
  },
  "planSizeWarnings": [
    "Plan touches 9 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-3",
    "step-5",
    "step-2",
    "step-4",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T22:06:03.093Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — change boundHashes to store the commitment. Rail 1 forbids the persisted-shape change; the re-anchor recomputes the predecessor's commitment through the shared builder instead. REJECTED — compare supersede budgetRef against verifyBudgetCommitment's verdict only. verify takes ONE expectedRef; the supersede check wants the successor record's ref compared to the PREDECESSOR-derived commitment — compose budgetCommitmentMaterial + budgetCommitmentDigest directly (both exported precisely for consumers). REJECTED — seeding budgetRef by calling verify at seed time: seeds must DERIVE the value (budgetCommitmentDigest over material read from the seeded store), mirroring the activation call sites, never assert one. REJECTED — running items 1-4 concurrently with task-3b61860f and leaving item 5 'gated' in prose: same-file edits in one shared checkout produce hunk collisions and cross-attributed commits; a dependsOn field is the enforcement the rails demand. REJECTED — splitting the three over-cap files here: pre-existing state, foreign scope; epic-final follow-up.",
    "codebaseInsights": "MEASURED AT HEAD b050fd5d. Producer landed (task-61a2e8ad DONE): apps/daemon/src/budget/budget-commitment.ts exports budgetCommitmentMaterial(store,{approvedRun,goalRef,projectId}) :151, budgetCommitmentDigest(material) :175 (DOMAIN-TAGGED sha256 — deliberately never collides with the activation root digest), verifyBudgetCommitment(store,input,expectedRef) :193, BUDGET_COMMITMENT_LAYER = \"DAEMON_PREREQUISITE\" :39. Bind-back consumers ALREADY live at activation: approval-activation.ts:163-175 and graph-activation-service.ts:188 call verifyBudgetCommitment with input.approval.budgetRef — THE QUERY-CONSTRUCTION PRECEDENT to mirror wherever a consumer needs {approvedRun, goalRef, projectId}. All four consumer sites re-measured at the cited lines: graph-supersede-approval-binding.ts:73 (`record.budgetRef !== predecessor.boundHashes?.budgetHash` — the digest-notion comparison that now refuses every commitment-carrying successor), expansion-approval.ts:227 + expansion-preparation.ts:83/:140/:242 (criteria.budgetRef, validHex64 only — notion-agnostic), approval-detail-plan.tsx:59 (display row labeled \"Budget\"), demo-seed-payloads.ts:395 and live-dispatch.ts:198 (`hex64(\"bb\")` literals — after the bind-back these seed approvals refuse at activation, breaking the demo live journey). ba102165 DONE: readApprovalRecordFacts present. FILE-COLLISION FACT: task-3b61860f (planned this session, AWAITING_APPROVAL) owns approval-record-facts.{ts,test.ts} edits (adds riskTier?/stepUpAuthRef? slots and extracts the exported walk `firstMissingApprovalFact(derived)`); THIS row's item 5 plugs the BUDGET_REF slot into THAT map — dependsOn [task-3b61860f] is declared so the two rows never edit the same file concurrently in the shared checkout, and the slot lands as one map entry + one derivation, not a rival walk. LINE-CAP FACTS: expansion-approval.ts 319, demo-seed-payloads.ts 411, live-dispatch.ts 440 — all pre-existing over the 250 target (the latter two over 400). This row's edits must not GROW them materially; the over-cap state pre-dates this row and is recorded as an epic-final follow-up, not fixed here (delivery bias).",
    "risks": "RISK 1 — approvedRun at the supersede site: budgetCommitmentMaterial needs a GenesisApprovedRun; matchSupersedeApproval has store/request/facts but maybe not the run. Mirror approval-activation.ts:163-175's query construction; if the run identity is genuinely unreachable from durable state at that seam, STOP and report — do not thread a caller value through the request. RISK 2 — the expansion comparison at :227 is record-vs-criteria (both sides same notion IF the criteria writer copies the record); measure the actual writer (planning-authority-persistence.ts is the suspect) before editing anything in core — the likely outcome is NO core production change, only the notion arm. RISK 3 — seeds: the commitment can only be derived AFTER the seeded store holds the genesis binding; order the seed pipeline so material is readable at derivation time, else the demo refuses BUDGET_COMMITMENT_MATERIAL_UNAVAILABLE. RISK 4 — live-dispatch.ts is control-room: its lane runs under `pnpm --filter @moe/control-room test`; a bare root `pnpm test` covers NEITHER app — quote per-package count lines. RISK 5 — item 5 must reuse 3b61860f's exported walk; if at claim time that shape differs (their plan moved), re-measure and adapt the slot to the landed surface rather than re-introducing a second walk. RISK 6 — shared checkout, ~725 foreign dirty paths: porcelain owned paths before staging, explicit pathspec only.",
    "keyFiles": [
      "apps/daemon/src/budget/budget-commitment.ts",
      "apps/daemon/src/planning/approval-activation.ts",
      "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
      "packages/core/src/expansion/expansion-approval.ts",
      "packages/core/src/expansion/expansion-preparation.ts",
      "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
      "apps/control-room/src/live/live-dispatch.ts",
      "apps/daemon/src/planning/approval-record-facts.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-29T23:27:28.689Z",
    "escalatedAt": "2026-08-30T05:35:24.090Z"
  },
  "planApprovedAt": "2026-08-29T22:06:05.360Z",
  "dependsOn": [
    "task-3b61860f4f7045108aced01b2391f343"
  ],
  "contextFetchedBy": [
    "worker-39362c3a",
    "worker-b5bb4e0f",
    "architect-dba58d99",
    "worker-e98a7edb",
    "qa-063c9dff"
  ],
  "workStartedAt": "2026-08-29T23:06:38.569Z",
  "declaredFiles": [
    "apps/daemon/src/budget/budget-commitment.ts",
    "apps/daemon/src/planning/approval-budget-ref.ts",
    "apps/daemon/src/planning/approval-budget-ref.js",
    "apps/daemon/src/planning/graph-supersede-test-fixtures.ts",
    "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
    "apps/daemon/src/planning/graph-supersede-approval-binding.test.ts",
    "tests/security/boundary-roster.security.ts",
    "tests/security/durable-store-boundaries.security.ts",
    "apps/control-room/src/live/live-dispatch-payloads.ts"
  ],
  "commits": [
    {
      "sha": "cd8a5c24",
      "ref": "moe/work-2026-08-08",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-39362c3a@2026-08-29T22:16:00.000Z",
      "paths": [
        "packages/core/src/expansion/expansion-approval.test.ts",
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/planning/approval-budget-ref.ts",
        "apps/daemon/src/planning/approval-budget-ref.js"
      ],
      "recordedBy": "worker-39362c3a",
      "recordedAt": "2026-08-29T23:24:50.223Z",
      "status": "WORKING",
      "pushed": false,
      "message": "feat(daemon): derive the approval BUDGET_REF slot from the decide-time commitment"
    },
    {
      "sha": "1c27ea12",
      "ref": "moe/work-2026-08-08",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-39362c3a@2026-08-29T22:16:00.000Z",
      "paths": [
        "apps/daemon/src/budget/budget-commitment.ts",
        "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
        "apps/daemon/src/planning/graph-supersede-approval-binding.test.ts",
        "apps/daemon/src/planning/graph-supersede-test-fixtures.ts"
      ],
      "recordedBy": "worker-39362c3a",
      "recordedAt": "2026-08-29T23:27:08.898Z",
      "status": "WORKING",
      "pushed": false,
      "message": "feat(daemon): compare supersede approvals commitment to commitment"
    },
    {
      "sha": "076324e4",
      "ref": "moe/work-2026-08-08",
      "kind": "completion",
      "role": "worker",
      "sessionId": "worker-e98a7edb@2026-08-30T05:43:00.000Z",
      "paths": [
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/daemon/src/orchestrator/demo-seed-plan.ts",
        "apps/daemon/src/orchestrator/demo-seed-main.ts",
        "apps/daemon/src/orchestrator/demo-seed-surface.ts",
        "apps/daemon/src/orchestrator/demo-seed-surface.js",
        "apps/daemon/src/orchestrator/demo-seed-payloads.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-plan.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-main.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-finalize-journey.test.ts",
        "apps/daemon/src/http/affordance-read-planning.test.ts",
        "tests/e2e/foundation/j3-crash-harness.ts",
        "tests/integration/control-room/live-approval-activation.test.ts",
        "apps/control-room/src/live/live-dispatch-payloads.ts",
        "apps/control-room/src/live/live-dispatch.ts",
        "apps/control-room/src/live/live-board.tsx",
        "apps/control-room/src/live/live-app.tsx",
        "apps/control-room/src/live/live-board-dispatch.test.tsx",
        "apps/control-room/src/live/live-board.test.tsx",
        "apps/control-room/src/approvals/approval-detail-plan.tsx"
      ],
      "recordedBy": "worker-e98a7edb",
      "recordedAt": "2026-08-30T06:30:28.742Z",
      "status": "REVIEW",
      "pushed": false,
      "message": "feat(task-be80cb74): re-issue both demo seeds from the production budget commitment"
    }
  ],
  "filesModified": [
    "apps/daemon/src/planning/graph-supersede-approval-binding.ts",
    "apps/daemon/src/budget/budget-commitment.ts",
    "apps/daemon/src/planning/graph-supersede-approval-binding.test.ts",
    "apps/daemon/src/planning/graph-supersede-test-fixtures.ts",
    "packages/core/src/expansion/expansion-approval.test.ts",
    "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
    "apps/daemon/src/orchestrator/demo-seed-plan.ts",
    "apps/daemon/src/orchestrator/demo-seed-main.ts",
    "apps/daemon/src/orchestrator/demo-seed-surface.ts",
    "apps/daemon/src/orchestrator/demo-seed-surface.js",
    "apps/daemon/src/orchestrator/demo-seed-payloads.test.ts",
    "apps/daemon/src/orchestrator/demo-seed-plan.test.ts",
    "apps/daemon/src/orchestrator/demo-seed-main.test.ts",
    "apps/daemon/src/bootstrap/bootstrap-finalize-journey.test.ts",
    "apps/daemon/src/http/affordance-read-planning.test.ts",
    "tests/e2e/foundation/j3-crash-harness.ts",
    "apps/control-room/src/live/live-dispatch-payloads.ts",
    "apps/control-room/src/live/live-dispatch.ts",
    "apps/control-room/src/live/live-board.tsx",
    "apps/control-room/src/live/live-app.tsx",
    "apps/control-room/src/live/live-board-dispatch.test.tsx",
    "apps/control-room/src/live/live-board.test.tsx",
    "apps/control-room/src/approvals/approval-detail-plan.tsx",
    "apps/daemon/src/planning/approval-record-facts.ts",
    "apps/daemon/src/planning/approval-record-facts.test.ts",
    "apps/daemon/src/planning/approval-budget-ref.ts",
    "apps/daemon/src/planning/approval-budget-ref.js",
    "tests/integration/control-room/live-approval-activation.test.ts"
  ],
  "unattributedPaths": [
    "apps/control-room/src/v2/styles/cordum-goals.css",
    "apps/daemon/src/http/pairing-approval-handshake.test.ts",
    "apps/control-room/src/data/data-contract.js",
    "apps/control-room/src/timeline/timeline-contract.js",
    "apps/control-room/src/timeline/timeline-page.js",
    "apps/control-room/src/v2/projects/project-manager-client.js"
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "worker-e98a7edb@2026-08-30T05:43:00.000Z",
    "at": "2026-08-30T06:30:28.742Z"
  },
  "blockedReason": null,
  "blockedOnTaskIds": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "reviewStartedAt": "2026-08-30T06:29:51.213Z",
  "verification": {
    "command": "cd apps/daemon && pnpm exec vitest run --root . --config package.json src/planning/graph-supersede-approval-binding.test.ts src/planning/graph-supersede-refusals.test.ts src/planning/approval-record-facts.test.ts src/bootstrap/bootstrap-finalize-journey.test.ts",
    "exitCode": 0,
    "outputTail": " RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  4 passed (4)\n      Tests  88 passed (88)\n   Start at  09:28:12\n   Duration  4.35s (transform 6.44s, setup 0ms, import 8.36s, tests 4.58s, environment 0ms)\n\nEXIT=0",
    "reportedAt": "2026-08-30T06:29:51.213Z"
  },
  "completionSummary": "Every budgetRef consumer now reads the commitment notion; no digest comparison remains. Producer task-61a2e8ad; seam family task-ba102165 + task-3b61860f; transport task-80b6bf7c; browser reader task-952564b3.\n\nDELIVERED. (2) supersede compares the successor record against the predecessor's commitment recomputed via the shared builder, code/layer unchanged, boundHashes only READ. (3) expansion: branch (2) by a different route — NO production module mints criteria.budgetRef at all, so :227 is notion-AGNOSTIC; two arms pin it, no core change. (4) BOTH literal sites retired. Daemon: approvalRecord takes budgetRef as a REQUIRED PARAMETER, DemoSeedInput.budgetRef is string|null (null OMITS approval.decide — a CAPABILITY omission, distinct from stopBeforeApproval's POLICY one), and runDemoSeed is two-phase, reading the ref off /budget/commitment/read after its finalize commits. Browser: DEV_PAYLOADS carries no budgetRef; dispatchAffordance merges the READ value via an injected reader and fails CLOSED (BUDGET_COMMITMENT_READER_ABSENT) rather than fabricating one. Label -> \"Budget commitment\". (5) untouched: comment-064d0042's (A)-closed / (B)-by-decomposition reading is correct.\n\n12 new arms + 6 drills (comment-ab950ee8), zero green drills. DRILL FINDING: the plan's D1 mutates the QUERY, which kills the READ — the arm reds through the ABSENCE branch and never evaluates the comparison. Re-run against the MATERIAL feeding the digest; the upstream parenthetical is the discriminator.\n\nREGRESSION I INTRODUCED AND FIXED: tests/integration/control-room/live-approval-activation.test.ts reddened INPUT_INVALID@CORE_REDUCER. It never names budgetRef — it spreads payloadFor's output — so no symbol grep finds it. It now derives through deriveApprovalBudgetRef, proving the browser lane end to end.\n\nSCOPE: 19 paths, not the 4 the step named. Threading requires the builder, the driver, 5 DemoSeedInput fixture sites, and the browser injection path. demo-seed-main.ts split at 250 lines: the",
  "completedAt": "2026-08-30T06:43:36.004Z",
  "reviewCompletedAt": "2026-08-30T06:43:36.004Z",
  "reviewSummary": "APPROVED. All gates and both drills re-run by me. Full evidence: comment-324de3ff7262497baeec1f3f432752ca.\n\nLANDING, three shas, all ancestors of HEAD: cd8a5c24 + 1c27ea12 (checkpoints) + 076324e4 (completion, IS HEAD, authored 24s after reviewStartedAt). Union covers 28 of 30 declaredPaths; the two tests/security paths were READ not written (AT_HEAD from other tasks, clean). Porcelain over every owned path EMPTY.\n\nGATES I RE-RAN: root pnpm test 395 files/9681 tests exit 0; control-room 109/1486 exit 0; focused daemon 8 files/151 exit 0. I confirmed the root include (adapters/packages/tests/tools) genuinely covers the integration + core expansion deliverables - 2 files/95 tests run directly.\n\nFOREIGN RED, attributed and matching the disclosure exactly at 19: daemon typecheck EXIT 1 on ONE error in pairing-approval-handshake.test.ts (uncommitted, not declared here, PAIRING_CLAIM_MAX_BODY_BYTES absent at HEAD - task-2f554e29's blocked arms); daemon test EXIT 1, 19 failed/6694 passed, bucketing 15 launcher-fence + 2 http-listener + 2 pairing. OWNED-PATH INTERSECTION EMPTY.\n\nBOTH DRILLS RE-RUN, both red: perturbing one material field reds 16 arms including DoD 2's two by name; reverting the seed to hex64(\"bb\") reds 3 including the demo activation arm. Restores proven by sha256 31acba83../53facaef.. plus git diff --exit-code 0, grep -c DRILL 0.\n\nMY OWN ERROR, recorded: my first daemon run showed 22 failures, 3 in owned demo-seed suites - my drill was live while that background gate read the worktree. Clean re-run gives 19, zero owned. Never overlap a drill with a background gate.\n\nDoD 1-6 all satisfied; DoD 5's arm is the strong form (verifyBudgetCommitment is the production fence, a different path from the seam). Headline verified independently: the only three budgetRef comparison sites all go through the shared builder; no boundHashes.budgetHash comparison remains.\n\nNON-BLOCKING: live-dispatch-payloads.ts 390 -> 397 lines, three from the 400 cap."
}