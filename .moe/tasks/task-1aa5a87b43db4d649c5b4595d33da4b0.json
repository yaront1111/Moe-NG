{
  "id": "task-1aa5a87b43db4d649c5b4595d33da4b0",
  "epicId": "epic-e0c54bddb3004bf29e98b2a43cea2dc2",
  "title": "Fence Foundation release until terminal resource truth",
  "description": "Safety-ordering prerequisite for task-a20e8ef668b54c3abbfce37a505252eb and task-48c79a29e1a4416b8db4c2916f5a5381. Measured at committed HEAD 16c6230ef6679b20da0f0c68b62da58214a0b945: task-918145a558574ac5b02da3a5f2655782 landed authenticated resource.reconcile plus a strict nonzero terminal reader, but Foundation still calls recordAttemptRelease immediately from noteRelease before reconciliation. Nonterminal evidence flows to scheduler DRAINING, and attempt-release-store commits only expectedVersion 0, so a later terminal fact cannot upgrade the poisoned first row. Deliver a narrow pre-release safety fence: before any core binding or scheduler release commit, strictly require the current attempt resource set to be nonzero, horizon-stable and wholly terminal. Otherwise defer/refuse with no release or binding movement. This row does not build the nine-key handoff or the eventual post-verification retry; task-48c79a29 owns that finalizer. Consumers: task-a20e8ef668b54c3abbfce37a505252eb and task-48c79a29e1a4416b8db4c2916f5a5381.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK FIRST: on the delivered tree, prove by production call-site/readback that resource.reconcile still writes through applyAttemptResourceReport, the strict reader requires a nonzero wholly terminal stable set, Foundation still attempts recordAttemptRelease before reconciliation, and the release store remains first-write expectedVersion 0. If any premise moved, report blocked with the exact changed call site; relaxing this gate can persist an un-upgradeable DRAINING row.",
    "recordAttemptRelease (or one immediately preceding server-owned seam that dominates every production caller) strictly re-reads current resource terminal evidence before deriving the core handoff binding or invoking scheduler release. Absent, zero, active, quarantined, unknown, unreadable or horizon-moved resources return one stable deferred/refused outcome carrying exact wrapper and source code/layer and write zero release/binding events.",
    "After the real resource.reconcile production path terminalizes a nonempty current set, the same release seam proceeds to its existing downstream handoff/terminal checks without caller-supplied terminal flags; this row neither fabricates the scheduler handoff nor converts an unrelated later refusal into DRAINING.",
    "Identical deferred attempts remain side-effect free, and concurrent reconciliation/release is horizon/version fenced so a stale terminal read cannot authorize release. Non-resumable cleanup remains distinct and is not weakened by the resumable-release fence.",
    "File-backed SqliteEventStore tests traverse the real Foundation/reconcile/release paths, assert exact code/layer and raw release/binding/resource counts, and mutation-drill early DRAINING, zero-set terminality, stale-horizon authorization and checking after the binding write; generated race/hostile cases assert positive counts.",
    "Fresh `pnpm --filter @moe/daemon typecheck` and `pnpm --filter @moe/daemon test` pass with nonzero execution evidence; commit explicit owned paths and record both downstream consumer task IDs."
  ],
  "taskRails": [
    "Safety constraint: no resumable release row or core handoff binding may be written before a current nonzero resource set is conclusively terminal.",
    "Do not accept caller terminal/state claims and do not turn UNKNOWN/zero/absent resources into terminal truth.",
    "This row owns the early-release fence only; task-48c79a29 owns post-verification retry/finalization and task-a20e8ef owns the scheduler handoff builder.",
    "Exact code/layer, TDD, <=250-line target per production source, explicit-path commits, preserve foreign work."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK GATE (DoD 1), re-measured at YOUR head — the filer's reads were at 16c6230; mine are at 7267e38 and ALL FOUR PREMISES HOLD, but the world moved and step 3 depends on how. Re-run and paste: (a) `grep -n \"applyAttemptResourceReport\" apps/daemon/src/work/resource-reconcile-command.ts` -> :149 inside `runResourceReconcileCommand` (:136), reached from `daemon-command-registry.ts:209`; (b) `release-terminal-evidence-contracts.ts` `provenTerminal` = `nonTerminal.length === 0 && terminal.length > 0` (nonzero AND wholly terminal), `release-terminal-evidence.ts` `splitResources` maps ATTEMPT_RESOURCE_RECORD_ABSENT -> EMPTY_SPLIT -> false and every other read failure -> RELEASE_TERMINAL_RESOURCE_UNKNOWN, and the horizon-moved arm fires at the END of `deriveReleaseTerminalEvidence`; (c) `foundation-attempt-service.ts:116-125` `noteRelease` calls `recordAttemptRelease` immediately, invoked at :197 (proven) and :217 (unproven), and DISCARDS its outcome (returns `settled`) — so a new refusal changes no dispatch answer, only what gets written; (d) `attempt-release-store.ts:210-215` commits at `expectedVersion: 0` with the event id copied from the grant. THE THING THAT MOVED, and it is NOT a premise failure: task-6d400781 landed, so `recordAttemptRelease` ALREADY derives terminality server-side — `deriveReleaseTerminal` at `attempt-release-disposition.ts:183`, before `deriveHandoffBinding` (:188) and `releaseWork` (:191), with retired caller keys refused at :157. The defect is therefore NOT a relayed flag; it is that a DERIVED `resourcesTerminal: false` still PROCEEDS to `releaseWork` -> DRAINING -> `commitRelease` at expectedVersion 0. The un-upgradeable row is real and this row's fence is the fix. If any of (a)-(d) has moved, report blocked with the exact changed call site. BLAST RADIUS, measured — paste it: `grep -n \"RESOURCES_PENDING\" apps/daemon/src/work/attempt-release-disposition.test.ts` returns exactly TWO sites (:818, :1062); every other DRAINING fixture uses EFFECTS_PENDING or UNOBSERVED_TERMINALITY, where `resourcesTerminal` is TRUE, so the fence leaves them untouched and DRAINING stays reachable by the effects and boundary arms. Baseline: `pnpm --filter @moe/daemon typecheck` then `pnpm --filter @moe/daemon test`, separate legs, separate exits, count lines verbatim.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/release-terminal-evidence.ts",
        "apps/daemon/src/work/attempt-release-store.ts"
      ],
      "note": "VERIFY-OR-BLOCK GATE PASSED at HEAD 7267e38f4c2f3ac41f7edc55a230f26b358027eb. All four premises hold; NOT blocked.\n\n(a) RECONCILE INGRESS WRITES THROUGH applyAttemptResourceReport.\n  apps/daemon/src/work/resource-reconcile-command.ts:35 imports it; :149 `const outcome = applyAttemptResourceReport(store, binding, report);` inside `export function runResourceReconcileCommand(` at :136.\n  Reached from apps/daemon/src/daemon-command-registry.ts:25 (import) and :209 `const outcome = runResourceReconcileCommand(store, {`.\n  PATH CORRECTION vs plan text: registry is apps/daemon/src/daemon-command-registry.ts, NOT src/work/ (plan said `work/daemon-command-registry.ts`). Line :209 matches exactly.\n\n(b) STRICT READER REQUIRES NONZERO + WHOLLY TERMINAL.\n  release-terminal-evidence-contracts.ts:87-88 `export const provenTerminal = (split) => split.nonTerminal.length === 0 && split.terminal.length > 0;` with the header \"an empty family is not proof, it is the absence of proof\".\n  release-terminal-evidence.ts:161 `const EMPTY_SPLIT = Object.freeze({nonTerminal: [], terminal: []})`; :165 `splitResources`; :170-172 ATTEMPT_RESOURCE_RECORD_ABSENT -> EMPTY_SPLIT, every other read failure -> `refuseReleaseTerminal(\"RELEASE_TERMINAL_RESOURCE_UNKNOWN\", ...)` (:172, :181).\n  Horizon-moved arm at the END of deriveReleaseTerminalEvidence: :238-240 `if (horizonMoved(store, horizon)) return refuseReleaseTerminal(\"RELEASE_TERMINAL_RESOURCE_UNKNOWN\", \"the event horizon moved while this answer was composed\")`.\n\n(c) FOUNDATION STILL RELEASES IMMEDIATELY.\n  foundation-attempt-service.ts:116-125 `function noteRelease(bound, record, settled)` calls `recordAttemptRelease(store, bound, record, {...})` and `return settled;` — outcome DISCARDED. Invoked at :197 (proven) and :217 (unproven). Import at :21.\n\n(d) RELEASE STORE IS FIRST-WRITE expectedVersion 0.\n  attempt-release-store.ts:206 `export function commitRelease(`; :210 comment \"expectedVersion 0: a second release on this aggregate cannot append.\"; :215 `expectedVersion: 0, key: {commandId: `${commandId}:RELEASED`, ...}`. Event id copied from the grant (disposition.ts:218 `${durable.grant.grantId}:RELEASED`).\n\nTHE DEFECT CONFIRMED (task-6d400781 landed, so this is NOT a caller-relay defect):\n  attempt-release-disposition.ts:154-156 refuse retired caller keys FIRST (boundary, terminal, handoff); :176 deriveSafeBoundary; :183 `const terminal = deriveReleaseTerminal(store, bound, durable);` — refusal carried, but a DERIVED `resourcesTerminal: false` PROCEEDS; :188 deriveHandoffBinding; :190 releaseWork -> DRAINING; :218 commitRelease at expectedVersion 0. Un-upgradeable DRAINING row is real. Plan said releaseWork at :191; actual :190 (one-line drift, same ordering). Retired-key refusals at :154-156, plan said :157-159 — same drift.\n\nBLAST RADIUS, measured — `grep -n \"RESOURCES_PENDING\" apps/daemon/src/work/attempt-release-disposition.test.ts`:\n  :230 the fixture definition `{effects: true, resources: false}`\n  :818 `const resourcesPending = activated(\"flags-resources\", \"OBSERVED\", RESOURCES_PENDING);` — READ, not assumed: this is the transposition-guard test \"hands the kernel a TWO-key record whose flags are not interchangeable\". It calls `deriveReleaseTerminal(...)` DIRECTLY (:822-825) and never calls recordAttemptRelease on that fixture. STAYS AS-IS, confirmed by reading.\n  :1062 `drainedByResources` = `rowFrom(drainedBy(\"resources\", \"OBSERVED\", RESOURCES_PENDING, [true, false]))` — the arm that must become a deferral. `drainedBy` (:1034-1052) asserts `written.ok && written.outcome === \"DRAINING\"` and `durableRowCount === 1`.\n  Every other DRAINING fixture is EFFECTS_PENDING or UNOBSERVED_TERMINALITY (= EFFECTS_PENDING, :239), both `resourcesTerminal: true`, so the fence leaves them untouched and DRAINING stays reachable.\n\nINSTRUMENTS CONFIRMED PRESENT in attempt-release-disposition.test.ts: `expectNoDurableRow` :353, `durableRowCount` :362 (rows OUT of store), `releaseDecisionCount` :370 (pages decisions by targetAggregateId), `activated` :274, `seedTerminality` :265 (THROWS if it seeds less than asked), `terminaliseResources` :255 (real applyAttemptResourceReport FAIL report).\n\nCODE-SEAM CONFIRMED: `ATTEMPT_RELEASE_CODES` (store :80-84) is closed AND pinned by an exact-membership sibling test at test :387-393 — a new member there would red it. `AttemptReleaseRefused[\"code\"]` (store :96-97) already unions four vocabularies -> that is the seam. `refuse()` (store :114) is typed to AttemptReleaseCode only, so the fence needs its own factory.\nStore API confirmed: `SqliteEventStore.getAggregateVersion(aggregateId: string): number` at packages/store/src/sqlite-event-store.ts:286. `deriveAttemptResourceAggregateId` exported at attempt-resource-authority-contracts.ts:150.\n`.js` bridge convention confirmed: all 8 siblings (attempt-release-*.js, release-terminal-*.js) exist and are git-tracked, each one line `export * from \"./x.ts\";`.\n\nBASELINE GATES AT 7267e38, BOTH LEGS RED BEFORE ANY EDIT OF MI",
      "startedAt": "2026-08-22T16:07:04.390Z",
      "completedAt": "2026-08-22T16:10:23.825Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD RED — all arms go in `attempt-release-disposition.test.ts` DELIBERATELY, not a new file: that suite already owns `activated()`, the named `Terminality` fixtures, `durableRowCount` (:362, counts rows OUT OF THE STORE), `releaseDecisionCount` (:370, pages decisions by target aggregate) and `expectNoDurableRow` (:353). Those are exactly the zero-rows-AND-zero-decisions instruments DoD 2 demands, and reimplementing them in a second file is the reimplemented-helper defect global rail 1 names. ARMS: (1) THE FENCE — `activated(..., \"OBSERVED\", RESOURCES_PENDING)` then `recordAttemptRelease` refuses with the fence's exact code under `DAEMON_ATTEMPT_RELEASE`, and `[durableRowCount, releaseDecisionCount]` is `[0, 0]` plus `expectNoDurableRow`; (2) THE SWEEP over every non-terminal shape DoD 2 lists — zero/absent set (no `terminaliseResources` at all), ACTIVE set (RESOURCES_PENDING), and a QUARANTINED member driven by a real `applyAttemptResourceReport` FAIL/UNKNOWN report — each asserting the SAME wrapper code and zero rows, with an explicit `expect(cases.length).toBe(3)` so a sweep that generated nothing cannot pass; (3) THE UNKNOWN PASSTHROUGH — an unreadable resource row makes `deriveReleaseTerminalEvidence` refuse RELEASE_TERMINAL_RESOURCE_UNKNOWN and that refusal must still arrive with the PRODUCER's code and layer (`carryTerminalRefusal`), NOT restamped as the fence's: DoD 2's 'one stable outcome' means one outcome SHAPE, never one code collapsing four layers; (4) THE LIFT (DoD 3) — same fixture, then terminalize through the REAL authenticated ingress `runResourceReconcileCommand(store, {payload:{activationAggregateId, ...report}, ...})` rather than the harness's direct `applyAttemptResourceReport`, and the SAME release call then proceeds to its existing downstream checks and writes exactly one row; (5) IDEMPOTENCE — the deferred call twice, asserting `[0, 0]` after both, so a deferral is side-effect free; (6) NEGATIVE CONTROL — EFFECTS_PENDING and UNOBSERVED_TERMINALITY still write their DRAINING rows unchanged, which is what proves the fence is resource-scoped and did not silently kill DRAINING. Update the two measured RESOURCES_PENDING sites: :1062 `drainedByResources` becomes a deferred-not-drained arm (its `drainedBy` helper asserts `written.outcome === \"DRAINING\"` and `durableRowCount === 1`; both change for this arm only), and :818's transposition guard stays as-is because it calls `deriveReleaseTerminal` directly and never releases — confirm that by reading it, do not assume. Run focused: RED.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/attempt-release-disposition.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/attempt-release-disposition.test.ts",
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-resource-fence.js"
      ],
      "note": "TDD RED, precise and for the right reason. All arms live in attempt-release-disposition.test.ts (no second file), reusing its own durableRowCount / releaseDecisionCount / expectNoDurableRow / activated / seedTerminality.\n\nFIRST RED was uninformative — `Cannot find module './attempt-release-resource-fence.js'`, `Test Files 1 failed (1) / Tests no tests`. A module-resolution red fails the whole FILE and shows no arm, so I authored the fence module's CONTRACT half early (code roster + predicate + refusal factory + .js bridge) with NO wiring in attempt-release-disposition.ts. Step 3 owns the wiring. That turned the red precise.\n\nFOCUSED RED (`pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/work/attempt-release-disposition.test.ts`) -> `Test Files 1 failed (1)` / `Tests 5 failed | 50 passed (55)`, every failure reading `Error: expected a refusal, received a recorded row` — i.e. the un-upgradeable DRAINING row this row exists to stop:\n  x DEFERS a release whose resource set the reducers can still move\n  x carries FIXED prose, so a refusal leaks no durable identifier\n  x DEFERS every non-terminal resource SHAPE under one code, writing nothing\n  x LIFTS once the REAL reconcile command terminalises the set, then releases\n  x leaves a deferred attempt side-effect free however often it is repeated\n\nTWO ARMS PASS ALREADY AND MUST KEEP PASSING (they are controls, not fence arms):\n  ok \"carries an UNREADABLE set under the PRODUCER's code and layer, not the fence's\" — pins RELEASE_TERMINAL_RESOURCE_UNKNOWN + RELEASE_TERMINAL_EVIDENCE layer against the producer's OWN answer, asserts `.code !== ATTEMPT_RELEASE_RESOURCES_UNPROVEN`, and [0,0] rows/decisions. This is DoD 2's \"one outcome SHAPE, not one code collapsing four layers\".\n  ok \"does NOT weaken the non-resumable cleanup path: the fence is resource-scoped\" — EFFECTS_PENDING still DRAINS with exactly 1 row.\n  ok \"publishes ONE fence code, outside the daemon's closed release vocabulary\" — roster is exactly [ATTEMPT_RELEASE_RESOURCES_UNPROVEN] and NOT in ATTEMPT_RELEASE_CODES (whose exact-membership sibling test at :387 stays green).\n\nTHE ABSENT-SET CASE WAS MEASURED, NOT ASSUMED — two failed attempts recorded because the second is now a durable fixture comment:\n  (1) `slot.rows: []` -> `Error: activation refused: WORK_PAYLOAD_MALFORMED`. The ingress will not commit an activation declaring zero resources, so that is NOT a production route.\n  (2) `slot.rows: [RESOURCE_ROW, {...RESOURCE_ROW}]` -> activation COMMITS, and `terminaliseResources` then refused with `ATTEMPT_RESOURCE_RECORD_ABSENT` — proving `admitBind` rejected the duplicate (ATTEMPT_RESOURCE_MEMBER_DUPLICATE, attempt-resource-authority.ts:96) while the activation stayed durable. That is exactly the fail-closed observable `activation-resource-binding.ts` documents (\"a bind refusal is not an activation refusal\"), so it is an honest production route to a zero set with NO planting.\n  Final shape: `activated(\"fence-absent\",\"OBSERVED\",RESOURCES_PENDING,[RESOURCE_ROW,{...RESOURCE_ROW}])`.\n\nSWEEP IS NON-VACUOUS THREE WAYS: `expect(cases.length).toBe(3)`; each case asserts the PRODUCER's own per-family answer `[true, false]` (effects terminal, so no other layer can be what refuses); and `expect(shapes).toEqual([\"ATTEMPT_RESOURCE_RECORD_ABSENT\", [\"ACTIVE\"], [\"QUARANTINED\"]])` read out of `readAttemptResources` — three genuinely different durable shapes, not three spellings of one. QUARANTINED is driven by a real `applyAttemptResourceReport` FAIL/UNKNOWN report; `adapterFail` (lease-resource.ts:165) quarantines the reported member itself, and `probeMember`'s `grantSuccessorCapacity` leg then moves it, so it reads MOVABLE = non-terminal.\n\nTWO MEASURED RESOURCES_PENDING SITES HANDLED AS PLANNED:\n  :818 transposition guard — READ, not assumed: it calls `deriveReleaseTerminal` DIRECTLY on both fixtures and never calls recordAttemptRelease. UNTOUCHED.\n  :1062 `drainedByResources` — DELETED and replaced by a block comment stating why (expectedVersion 0 means that DRAINING row could never be upgraded), pointing at the fence block. The sweep test went 3 arms -> 2 (`expect([effects.length, boundary.length]).toEqual([1,1])`, `expect(rows.length).toBe(2)`), and its two survivors are named as the fence's NEGATIVE CONTROL — both carry resourcesTerminal true, so a fence that killed DRAINING outright reds there.\n\nALSO CHANGED: `activationBytes(rows = [RESOURCE_ROW])` and `activated(..., rows = [RESOURCE_ROW])` parameterised for the absent case only; every existing call site keeps the default and is byte-identical in behaviour.\n\nFENCE MODULE (contract half, 89 lines, well under the 250 target): ATTEMPT_RELEASE_RESOURCE_FENCE_CODES (one member), AttemptReleaseResourceFenceCode, ATTEMPT_RELEASE_RESOURCES_UNPROVEN_MESSAGE (fixed prose, never interpolated), `resourcesUnproven(flags)` reading `resourcesTerminal` and nothing else, `refuseUnprovenResources()` hoisted under DAEMON_ATTEMPT_RELEASE. Header states the s",
      "startedAt": "2026-08-22T16:10:27.747Z",
      "completedAt": "2026-08-22T16:18:12.549Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN — new module `apps/daemon/src/work/attempt-release-resource-fence.ts` plus its `.js` bridge (`export * from \"./attempt-release-resource-fence.ts\";` — every sibling in this directory has one and it is tracked; a missing bridge is invisible to tsc and vitest). It is a SMALL module and must stay small: its value is the documented safety rule, the stable code, and the ordering it enforces — do NOT pad it, and do NOT re-derive terminality inside it. CONTENTS: (1) a one-member frozen code constant + exported type for the fence's own stable code (name it for the rule, e.g. ATTEMPT_RELEASE_RESOURCES_UNPROVEN); (2) a predicate over `ReleaseTerminalFlags` that reads `resourcesTerminal` and NOTHING else — `provenTerminal` already means nonzero-AND-wholly-terminal, and a second definition here is exactly the drift `release-terminal-evidence.ts`'s header forbids; (3) the refusal factory, refusing under the EXISTING `DAEMON_ATTEMPT_RELEASE` layer (this is genuinely this daemon's own precondition decision, the same class as `refuseTerminalClaim`) with FIXED PROSE as its message — never interpolated: a message carrying resource ids or counts leaks durable identifiers through the one field a refusal may carry; (4) the header states the safety rule: no release row and no handoff binding may be written while the attempt's current resource set is not conclusively terminal, because `commitRelease` pins expectedVersion 0 and a DRAINING row written now can never be upgraded. WIRING: in `attempt-release-disposition.ts`, immediately AFTER `deriveReleaseTerminal` (:183) and BEFORE `deriveHandoffBinding` (:188), return the fence refusal when the flags say resources are unproven. That placement is the deliverable — the existing derive call already IS the strict current read, taken inside this call after the durable activation, so DoD 2's 're-reads' is satisfied by composing over it; adding a SECOND read would double the horizon race and create a second definition. In `attempt-release-store.ts` widen `AttemptReleaseRefused[\"code\"]` (:96-97) with the new code type — the union already carries three foreign vocabularies, so this is the established seam; do NOT insert a member into the closed `ATTEMPT_RELEASE_CODES` array, whose header explicitly warns against daemon-only additions and whose sibling tests are coupled to its membership. Run step-2 arms GREEN plus the full daemon suite.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-store.ts"
      ],
      "newFiles": [
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-resource-fence.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-store.ts",
        "apps/daemon/src/work/attempt-release-resource-fence.ts"
      ],
      "note": "GREEN. Focused: `Test Files 1 passed (1)` / `Tests 55 passed (55)`, exit 0.\n\nWIRING, exactly where the plan named it — attempt-release-disposition.ts, AFTER `deriveReleaseTerminal` and BEFORE `deriveHandoffBinding`:\n    const terminal = deriveReleaseTerminal(store, bound, durable);\n    if (!terminal.ok) return terminal;\n    // THE RESOURCE FENCE, and its POSITION is the deliverable. [...]\n    if (resourcesUnproven(terminal.flags)) return refuseUnprovenResources();\n    const handoff = deriveHandoffBinding(store, bound, durable);\nNo SECOND evidence read was added: the existing derive call already IS the strict current read, taken inside this call after the durable activation, so DoD 2's \"re-reads\" is satisfied by composing over it — and a second call would double the horizon race and create the second definition release-terminal-evidence.ts's header forbids.\n\nCODE SEAM as planned. attempt-release-store.ts `AttemptReleaseRefused[\"code\"]` widened to `AttemptReleaseCode | AttemptReleaseResourceFenceCode | AuthorityErrorCode | ReleaseTerminalCode | SafeBoundaryRefusalCode`. `ATTEMPT_RELEASE_CODES` membership UNCHANGED — its exact-membership sibling test (test :387-393) is still green, which is the check that would have caught an insertion.\n\nCYCLE HAZARD HANDLED AND DOCUMENTED. The fence imports `DAEMON_ATTEMPT_RELEASE` from attempt-release-store.js as a VALUE; the store imports the fence's code as `import type`, which is ERASED, so no runtime cycle exists and the temporal-dead-zone crash that file's header warns about cannot occur. A comment at the import site says so.\n\nNOT RE-EXPORTED from attempt-release-disposition.ts, deliberately: the composer re-exports the STORE's names only — `deriveReleaseTerminal`, `deriveSafeBoundary` and `deriveHandoffBinding` are all imported from their own modules by consumers, and the fence follows that established shape.\n\nFENCE MODULE FINAL: 89 lines (target <=250). Predicate reads `resourcesTerminal` and nothing else. Refusal is a hoisted function with FIXED prose. Header states the safety rule, why UNKNOWN never reaches the fence (the producer refuses first, carried under ITS code and layer), and why the version guard in step 4 is aggregate-scoped rather than global (a global `readEventHorizon` re-check moves on ANY unrelated write and would livelock a busy daemon).\n\nFULL DAEMON SUITE: `Test Files 1 failed | 207 passed (208)` / `Tests 1 failed | 4576 passed | 2 todo (4579)` — exit 1. THE FAILURE IS FOREIGN AND PRE-EXISTING, disclosed verbatim:\n  FAIL src/work/foundation-attempt-windows.test.ts:888 `expect(outcome.ok).toBe(true)` (a real-process capture/dispatch arm).\n  It was ALSO red at my zero-edit baseline in step 1 (then 2 failures at :918 in the same file) BEFORE I touched anything.\n  ATTRIBUTION MEASURED, not asserted: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/work/foundation-attempt-windows.test.ts` in ISOLATION -> `Test Files 1 passed (1)` / `Tests 8 passed (8)`, EXIT 0. Green cold, red warm = a load/race condition in that file's real-process arm, not a logic break, and not reachable from this diff — `noteRelease` DISCARDS `recordAttemptRelease`'s outcome (foundation-attempt-service.ts:116-125), so no dispatch answer anywhere can change.\n  HEAD moved during this step: 7267e38 -> 2f3da9c (peer commit \"fix(daemon): source the settlement decision's audit stamp and principal from durable facts\"). That commit CLEARED the two baseline TS6133 typecheck errors — `pnpm --filter @moe/daemon typecheck` is now EXIT 0 with the tsc echo and no diagnostics. Baseline test count moved 4571 -> 4579 total (+8 = my new arms) and 4567 -> 4576 passed (+9: my 8 plus the peer's).",
      "startedAt": "2026-08-22T16:18:15.378Z",
      "completedAt": "2026-08-22T16:21:35.909Z"
    },
    {
      "stepId": "step-4",
      "description": "THE VERSION FENCE (DoD 4), and the instrument matters. `recordAttemptRelease` is FULLY SYNCHRONOUS — `grep -n \"await\\|async\" ` over :145-222 returns nothing — so between the evidence read and `commitRelease` no in-process interleaving is possible; the residual window belongs to a SECOND connection on the file-backed store, which is exactly what the tests and other agents are. IMPLEMENT: capture `store.getAggregateVersion(deriveAttemptResourceAggregateId(bound.aggregateId))` (exported from `attempt-resource-authority-contracts.ts:150`) beside the fence check, re-read it immediately before `commitRelease`, and refuse with the fence's code if it moved. DO NOT use a global `readEventHorizon()` re-check here: the global horizon moves on ANY unrelated write in the store, so a global re-check would refuse nearly every release on a busy daemon — a livelock this row must not create. The producer's OWN global-horizon arm stays where it is and is not touched. THE ARM: a store wrapper that DELEGATES to the real file-backed store and, on the first `getAggregateVersion` call for the resource aggregate, performs a REAL `runResourceReconcileCommand` through the production path before returning — a genuine production write interposed in the window, never a fabricated state or a stubbed version number. Assert the fence's code AND `[durableRowCount, releaseDecisionCount] === [0, 0]`. State in the module header why the fence is aggregate-scoped rather than global.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-disposition.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-disposition.test.ts"
      ],
      "note": "VERSION FENCE landed. Focused: `Test Files 1 passed (1)` / `Tests 57 passed (57)`, exit 0. `pnpm --filter @moe/daemon typecheck` exit 0 with the `$ tsc --project tsconfig.json` echo.\n\nIMPLEMENTATION, aggregate-scoped exactly as planned:\n  fence module adds `readAttemptResourceVersion(store, activationAggregateId): number | null` — `store.getAggregateVersion(deriveAttemptResourceAggregateId(...))` wrapped in try/catch, returning null on a throw. NULL IS NOT ZERO and the header says so: an absent aggregate answers 0, a store that threw answers nothing, and collapsing them would let an unreadable head authorise a release.\n  and `resourceVersionMoved(captured, current)` = `captured === null || current === null || captured !== current` — an unreadable head on EITHER side counts as moved.\n  attempt-release-disposition.ts: capture immediately after the fence check; re-read as the LAST statement before `commitRelease`, refusing with the fence's own code. NO global `readEventHorizon()` re-check — the comment at the capture site states why: the global horizon moves on ANY unrelated write and would refuse nearly every release on a busy daemon, which is a livelock. The producer's own global-horizon arm in release-terminal-evidence.ts is untouched.\n\nTHE ARM — a delegating Proxy over the REAL file-backed store, not a stub:\n  `interposing(store, onCaptured)` returns `new Proxy(store, {get})`. Non-function properties pass through; every method except `getAggregateVersion` is returned bound to the real store; `Reflect.get(base, key, base)` keeps the receiver on the real store so its private `#core` stays reachable. For `getAggregateVersion` the wrapper answers with the REAL pre-write version FIRST and only then lets the second writer in — that ordering is load-bearing, because firing the write before the answer would make capture and re-read agree and the arm would test nothing.\n  Assertions: the aggregate REALLY moved (`getAggregateVersion(RESOURCE_AGGREGATE)` toBe `before + 1`, read out of the store); `refusalOf(outcome)` = {ATTEMPT_RELEASE_RESOURCES_UNPROVEN, DAEMON_ATTEMPT_RELEASE}; `[durableRowCount, releaseDecisionCount]` = [0, 0]; `expectNoDurableRow`.\n  CONTROL ARM ADDED: the SAME proxy with a no-op interposition RELEASES (row outcome RELEASED, durableRowCount 1, version unchanged). Without it a proxy that broke every store call would produce the refusal above for the wrong reason and read as a pass.\n\nA MEASUREMENT THAT CHANGED THE INSTRUMENT, and it is asserted inside the committed suite rather than left in this note. The plan asked for a real `runResourceReconcileCommand` as the interposed write. MEASURED: once a set is PROVEN TERMINAL no reconcile-reachable reducer will move it — `adapterConfirm` (lease-resource.ts:124) requires PENDING_ACQUIRE, `adapterFail` (:152) requires PENDING_ACQUIRE or ACTIVE, and the GRANT variant is deliberately unreachable from that wire (resource-reconcile-command.ts:110-113). So no production command can append to that aggregate in this window. The arm therefore RUNS the real production command in the window anyway and ASSERTS its refusal — `reconciled.ok === false`, `code === \"ATTEMPT_RESOURCE_SET_REFUSED\"` — which keeps the frozen-set fact as a committed, re-runnable measurement instead of a transcript, and then lands the movement as a real durable append through `store.commitExpectedVersionDecision` at the measured head (the same instrument this suite already uses in `plantUndecodableTerminal` and `plantReleaseEvent` for arms no production writer can reach). Nothing is stubbed and no version number is fabricated. The guard still has to hold against a writer this row cannot enumerate — a second daemon, a replay, a later producer — which is exactly what fail-closed means here.\n\nLINE COUNTS after trimming my own comments (target <=250, hard cap 400):\n  attempt-release-resource-fence.ts 122 (new)\n  attempt-release-disposition.ts 251 (was 222 at HEAD)\n  attempt-release-store.ts 253 (was 250 at HEAD — the pre-existing file was already AT the target, so a 1-line import plus a 2-line cycle-hazard comment is the floor; splitting a coherent 253-line module would be strictly worse)",
      "startedAt": "2026-08-22T16:23:03.256Z",
      "completedAt": "2026-08-22T16:26:17.327Z"
    },
    {
      "stepId": "step-5",
      "description": "MUTATION DRILLS — one at a time, restore proven per path: `git diff -- <file>` empty for TRACKED files, and for the NEW untracked fence module a SHA256 comparison against an out-of-tree backup under %TEMP% suffixed by worker id, taken in a call SEPARATE from the restore (`git diff` is blind to `??` paths and would certify nothing). Run `git status --porcelain` on the exact mutated paths BEFORE drilling. DRILLS: (1) EARLY DRAINING — delete the fence call from `recordAttemptRelease` so a non-terminal set proceeds; the fence arm AND the zero-rows/zero-decisions assertions must red; (2) ZERO-SET TERMINALITY — make the predicate treat an empty enumerated set as terminal (invert the zero case); the absent-set sweep case must red, and if it does not, the sweep is not generating that case — fix the sweep, do not weaken the drill; (3) STALE-HORIZON AUTHORIZATION — remove the pre-commit version re-read; step 4's interposed-write arm must red; (4) CHECK-AFTER-BINDING — move the fence call to AFTER `deriveHandoffBinding`, then after `releaseWork`, then after `commitRelease`; the first two must red on ordering assertions and the third on the row count. A drill that stays GREEN is a finding: say so and strengthen the arm before proceeding. ADVERSARIAL CHECKLIST: producers untouched (`release-terminal-evidence*.ts`, `attempt-resource-authority.ts`, `resource-reconcile-command.ts`, `attempt-release-{boundary,terminal,handoff,record}.ts`); `ATTEMPT_RELEASE_CODES` membership unchanged; no caller-supplied terminal or state value read on ANY path; the retired-key refusals at :157-159 still fire FIRST and in order; DRAINING still reachable by the effects and boundary arms (the negative control); `noteRelease` still discards the outcome so no dispatch answer changed.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-disposition.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/attempt-release-disposition.test.ts",
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-disposition.ts"
      ],
      "note": "MUTATION DRILLS — five run one at a time, every restore proven by SHA256 against an out-of-tree backup under %TEMP%\\drill-worker-40cfc767, taken in a call SEPARATE from the restore. `git status --porcelain` BEFORE drilling gave ` M` for attempt-release-{disposition,store}.ts and the test, `??` for attempt-release-resource-fence.ts — so `git diff` certifies NOTHING for the new module AND nothing for the three already-dirty files, and SHA256 is the only valid proof for all four. Focused runs used `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/work/attempt-release-disposition.test.ts`.\n\nDRILL 1 EARLY DRAINING — fence call replaced by `if (false as boolean)`.\n  RED: `Tests 5 failed | 52 passed (57)`\n  x DEFERS a release whose resource set the reducers can still move\n  x carries FIXED prose, so a refusal leaks no durable identifier\n  x DEFERS every non-terminal resource SHAPE under one code, writing nothing\n  x LIFTS once the REAL reconcile command terminalises the set, then releases\n  x leaves a deferred attempt side-effect free however often it is repeated\n  RESTORE: SHA256 421A2A2C45A72052B7BFACDCC18CBCE7511B7916A413575F95F58E89F5DE7A4F == backup.\n\nDRILL 2 ZERO-SET TERMINALITY — the fence predicate reads ONE derived boolean, so the zero case lives in the PRODUCER: `provenTerminal` mutated to `split.nonTerminal.length === 0` (dropping `&& split.terminal.length > 0`), which makes an EMPTY set terminal.\n  RED, and it names the case: `AssertionError: absent: expected [ true, true ] to deeply equal [ true, false ]` — so the sweep really does generate the absent case.\n  RESTORE: SHA256 A3209640132652895B09D4B02609CB1DCFB22E215412DE7D97EC30721B8A5A67 == backup, AND `git diff --stat` + `git status --porcelain` both empty (tracked, previously unmodified producer — double proof).\n\nDRILL 3 STALE-VERSION AUTHORIZATION — pre-commit re-read removed (`resourceVersionMoved(resourceVersion, resourceVersion)`).\n  RED: x REFUSES when the resource aggregate moves between the read and the commit — `Error: expected a refusal, received a recorded row`. `Tests 1 failed | 1 passed` — the CONTROL arm (same proxy, nothing moves) stayed green, so the red is the guard and not the proxy.\n  RE-RUN after the step-5 refactor below: red again, same arm, same message.\n\nDRILL 4 CHECK-AFTER-BINDING, three positions:\n  4a fence moved AFTER `deriveHandoffBinding` -> RED on x answers under the FENCE's layer, not a LATER seam's, when both would refuse. `Tests 1 failed | 57 passed`.\n  4b fence moved AFTER `releaseWork` -> RED, same arm, `Tests 1 failed | 57 passed`.\n  4c fence moved AFTER `commitRelease` -> RED on FIVE arms with `AssertionError: expected [ 1, 1 ] to deeply equal [ +0, +0 ]` (and `absent: expected [ 1, 1 ]...`) — the row AND decision counts.\n  RESTORE after 4: SHA256 421A2A2C... == backup.\n\nA DRILL-DRIVEN FINDING, fixed before proceeding rather than reported after. 4a/4b would have stayed GREEN as first written. MEASURED WHY: on a deferred path `deriveHandoffBinding` writes NOTHING (`recordReleaseHandoffBinding` answers `written:false` when the journal is ABSENT, release-handoff-binding.ts:137, and these fixtures journal nothing), while `releaseWork` and `releaseProviderSlot` are pure kernels — so a fence moved past any of them returns the SAME code over the SAME zero rows and the move is invisible. NEW ARM ADDED to make ordering observable: \"answers under the FENCE's layer, not a LATER seam's, when both would refuse\" plants an event of the WRONG type on `deriveAttemptJournalAggregateId(record.activationDigest)`, which `currentEvent` rejects; the arm first asserts against the PRODUCTION surface that the later seam really would refuse (`deriveHandoffBinding` -> `[\"JOURNAL_RECORD_MALFORMED\", \"DAEMON_JOURNAL_APPEND\"]`) and then asserts the release comes back as the FENCE's deferral with [0,0]. That is DoD 3's other half — an unrelated later refusal must not stand in for the deferral, and must not become DRAINING.\n\nA SECOND CORRECTNESS FIX FOUND BY THE ADVERSARIAL CHECKLIST, not by a drill. `readAttemptResourceVersion` took `bound.aggregateId` — the CALLER's value. The production writer (`activation-resource-binding.ts`) targets `deriveAttemptResourceAggregateId(deriveActivationAggregateId(record.effectIntent.aggregateId, record.effectIntent.idempotencyKey))`, and the producer reads `bound.activationAggregateId` off the by-attempt reader. Watching an aggregate the writer never targets would make the guard a silent no-op that still read as coverage. The fence now derives from the COMMITTED ACTIVATION by the writer's own derivation, matching this module's stated rule that facts are re-read from the activation and the caller record is identity-agreement only. Header says so. Drill 3 re-run confirms the guard is still falsifiable after the change, and the race arm passing at all is the positive proof that the derived aggregate is the one the interposed write moves.\n\nADVERSARIAL CHECKLIST, all measured:\n  PRODUCE",
      "startedAt": "2026-08-22T16:26:23.676Z",
      "completedAt": "2026-08-22T16:34:25.840Z"
    },
    {
      "stepId": "step-6",
      "description": "GATE + COMMIT + RECORD. Separate legs, separate exits, counts read from the OUTPUT not the exit code: (1) `pnpm --filter @moe/daemon typecheck`; (2) `pnpm --filter @moe/daemon test` — nonzero `Test Files N passed` line quoted verbatim; (3) `pnpm typecheck` repo-wide as a PATH-ATTRIBUTED BASELINE (merge-base failures, HEAD failures, the delta intersected with owned paths must be EMPTY; disclose any foreign red verbatim). Commit by explicit pathspec — the fence module, its `.js` bridge, `attempt-release-disposition.ts`, `attempt-release-store.ts`, `attempt-release-disposition.test.ts` — with `git status --porcelain` checked before and `git show --stat` after. COMPLETION RECORD as a task COMMENT, never the completion summary (that field is discarded): (a) the step-1 gate reads pasted with their line numbers; (b) the fence's code and layer, and the statement that producer refusals still arrive under THEIR code and layer; (c) per-drill red test names, and the restore proof used for the untracked module; (d) count lines for every leg; (e) BOTH downstream consumer task ids — task-a20e8ef668b54c3abbfce37a505252eb and task-48c79a29e1a4416b8db4c2916f5a5381 — with the note that task-48c79a29 still owns the post-verification retry that turns a deferral into a release; (f) THE DISCLOSURE THIS ROW MUST NOT SILENTLY SWALLOW: the same expectedVersion-0 poisoning is still reachable through the EFFECTS family — the unproven dispatch path (`foundation-attempt-service.ts:217`) never calls `recordTerminalEffect`, so `effectsTerminal` is false there and a DRAINING row is still written at expectedVersion 0. Rail 1 and every DoD clause scope this row to RESOURCES, so do not widen; report the measurement so the governor can file it. verification.command: `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test`",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-store.ts",
        "apps/daemon/src/work/attempt-release-disposition.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/attempt-release-resource-fence.ts",
        "apps/daemon/src/work/attempt-release-resource-fence.js",
        "apps/daemon/src/work/attempt-release-disposition.ts",
        "apps/daemon/src/work/attempt-release-store.ts",
        "apps/daemon/src/work/attempt-release-disposition.test.ts"
      ],
      "note": "GATE + COMMIT + RECORD done. Full completion record lives in THREE task COMMENTS (the completion summary field is discarded and is not a durable surface):\n  comment-365670561ed04cfda8ec64791182410d — DoD-1 gate reads with line numbers, the fence's code/layer/message, the statement that producer refusals still arrive under THEIR code and layer, all three gate legs with count lines, line counts.\n  comment-f918b09f8c334911b788235baca76456 — per-drill red test names, restore proofs, and the two defects the drills and the adversarial checklist found and I fixed before finishing.\n  comment-6b22ae8c9a8444f69eca2771c4d1cacd — both downstream consumer ids, the EFFECTS-family disclosure, the residual version-guard window, and the method notes a grader needs.\n\nGATE LEGS, separate, exits read separately, counts read from OUTPUT (delivered tree, HEAD 939b20e):\n  1. `pnpm --filter @moe/daemon typecheck` -> EXIT 0, `$ tsc --project tsconfig.json` echo present, zero diagnostics.\n  2. `pnpm --filter @moe/daemon test` -> EXIT 0, \"Test Files  208 passed (208)\" / \"Tests  4580 passed | 2 todo (4582)\".\n  3. `pnpm typecheck` repo-wide -> EXIT 0, every workspace package prints \"Done\". PATH-ATTRIBUTED BASELINE: the failing-path set at HEAD is EMPTY, so the delta intersected with owned paths is trivially empty. The step-1 baseline reds (two TS6133 in foundation-attempt-windows.test.ts, plus one flaky real-process arm in the same foreign file, and later one TS6133 in a peer's uncommitted foundation-attempt-service.ts) were all FOREIGN and all cleared as their owners committed; HEAD moved 7267e38 -> 2f3da9c -> 939b20e during the task. Nothing of mine was ever red.\n\nCOMMIT bb6978b48ade9e354aeb5b641658c460c5558f83 on moe/work-2026-08-08, by EXPLICIT PATHSPEC only. `git status --porcelain` before staging confirmed exactly five owned paths under apps/daemon/src/work ( M disposition.ts, M store.ts, M disposition.test.ts, ?? resource-fence.ts, ?? resource-fence.js) and `git diff --cached --stat` before committing showed exactly those five and nothing else: 586 insertions, 18 deletions. `git show --stat` after confirms the same five. Owned paths are clean afterwards. No git add -A, no push, no merge, no reset, no stash. No debug, probe, scratch or generated file is in the commit — the two temp files used to assemble large test blocks were removed and `ls` confirms their absence.\n\nADVERSARIAL SELF-REVIEW, run over the diff before finishing. CONCURRENCY: two concurrent releases are still fenced by commitRelease's expectedVersion 0 (unchanged); the version guard narrows rather than eliminates the read-then-commit window, and the only complete fix — carrying the resource aggregate's expected version as a second decision leg — changes the release store's commit identity, which the plan rejected as out of scope. Disclosed in comment 3. INPUTS: every uncertainty fails CLOSED — `!flags.resourcesTerminal` refuses on a missing flag, `readAttemptResourceVersion` converts a throw to null rather than escaping (a crash is not a refusal), and `resourceVersionMoved` treats null on EITHER side, including both, as moved. SIDE EFFECTS: the fence returns before any write; the one path that can leave a durable artefact behind (an inert handoff binding on a late version move, only when the attempt has a journal) is measured, bounded and disclosed rather than half-fixed. EMBARRASSMENT TEST: the two things I would have been dinged for — a guard watching an aggregate the production writer never targets, and two ordering drills that could not fail — were both found here and fixed before this step closed.",
      "startedAt": "2026-08-22T16:34:29.382Z",
      "completedAt": "2026-08-22T16:40:09.538Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "PROMOTED FIRST OF THE THREE a20e8ef6 PREREQUISITES — it is a SAFETY WALL with a production-reachable defect behind it, not a producer gap. Filed by architect-c4355c55 at committed HEAD 16c6230 from task-a20e8ef6's DoD-0 verify-or-block (comment-77f6242e there); M1 release-handoff track; PR#3 review item (1).\n\nTHE DEFECT, measured on the row that filed this one: task-918145a5's reconciliation capability is REAL (daemon-command-registry → runResourceReconcileCommand → applyAttemptResourceReport; deriveReleaseTerminalEvidence requires a nonzero wholly-terminal stable set) but it is an INDEPENDENT command path — foundation-attempt-service.ts calls recordAttemptRelease IMMEDIATELY, with noteRelease invoked BEFORE reconciliation. A nonterminal resource set therefore reaches the scheduler as DRAINING; attempt-release-store.ts commits at expectedVersion 0; and the existing focused control proves a later terminal read CANNOT upgrade that first DRAINING row (1 file / 1 selected test, exit 0). So the first live release permanently strands the attempt in DRAINING. Bar 2: reachable the moment any release runs after reconciliation exists, which is now. Bar 1: a20e8ef6 and 48c79a29 cannot land safely without it.\n\nSCOPE AS FILED, which I am holding you to: a pre-release resource-terminal FENCE before any binding/release write — the release path refuses (stable code + layer, UNKNOWN never coerced to terminal) until the reconciled resource set is wholly terminal per the scheduler-owned reducer result; task-48c79a29 RETAINS the later retry/finalizer (do not absorb it); no caller may assert terminality or a released state (rail 1 of the parent). This row orders two things that already exist; it does not build a producer.\n\nINPUTS PRESENT AT 16c6230 (the filer's measurement, re-usable; re-run at YOUR head): reconciliation ingress landed (918145a5 DONE at bcd265a + 86da6cc); `deriveReleaseTerminalEvidence` and the strict readiness reader (release-terminal-evidence.ts, with the horizon-moved arm from 918145a5 step 4); the immediate release call at foundation-attempt-service.ts:278-289 / noteRelease; attempt-release-store.ts's single-write expectedVersion-0 commit. Verify-or-block step 1 pastes those reads.\n\nWHY FIRST: the other two prerequisites (d9842aae checkpoint producer for completedSteps/nextSafeAction; 4a318d03 artifact-roster digest) are PRODUCERS the nine-key builder consumes and can plan in parallel as architect seats free; this one is the ordering defect that makes every later release row unsafe, and it is the smallest. Promoted under governor standing authority and the human's \"do all M1\" ruling. No scope narrowed, no rail waived.",
  "createdBy": "WORKER",
  "parentTaskId": "task-48c79a29e1a4416b8db4c2916f5a5381",
  "priority": "CRITICAL",
  "order": 205.3,
  "comments": [
    {
      "id": "comment-365670561ed04cfda8ec64791182410d",
      "author": "worker-40cfc767",
      "content": "COMPLETION RECORD 1/3 — DoD-1 GATE READS, THE FENCE, AND THE GATE COUNTS. Commit bb6978b48ade9e354aeb5b641658c460c5558f83 (5 files, 586 insertions, 18 deletions), explicit pathspec, on moe/work-2026-08-08.\n\nDoD-1 VERIFY-OR-BLOCK, re-measured at HEAD 7267e38 (filer measured 16c6230). All four premises HOLD; not blocked.\n(a) resource-reconcile-command.ts:35 imports applyAttemptResourceReport; :149 calls it inside `runResourceReconcileCommand` (:136); reached from apps/daemon/src/daemon-command-registry.ts:25 + :209. PATH CORRECTION vs the plan: the registry is apps/daemon/src/daemon-command-registry.ts, NOT src/work/ — line :209 matches exactly.\n(b) release-terminal-evidence-contracts.ts:87-88 `provenTerminal = split.nonTerminal.length === 0 && split.terminal.length > 0`. release-terminal-evidence.ts:161 EMPTY_SPLIT; :165 splitResources; :170-172 ATTEMPT_RESOURCE_RECORD_ABSENT -> EMPTY_SPLIT, every other read failure -> RELEASE_TERMINAL_RESOURCE_UNKNOWN (:172, :181); horizon-moved arm at the END, :238-240.\n(c) foundation-attempt-service.ts:116-125 noteRelease calls recordAttemptRelease immediately and returns `settled` — the outcome is DISCARDED. Invoked at :197 (proven) and :217 (unproven). Still true at the delivered tree.\n(d) attempt-release-store.ts:206 commitRelease; :210 \"expectedVersion 0: a second release on this aggregate cannot append.\"; :215 `expectedVersion: 0`; event id copied from the grant (disposition.ts:218).\nTHE DEFECT (task-6d400781 landed, so NOT a caller-relay defect): disposition.ts :154-156 refuse retired keys, :176 deriveSafeBoundary, :183 deriveReleaseTerminal — a DERIVED `resourcesTerminal:false` then PROCEEDED to :188 deriveHandoffBinding, :190 releaseWork -> DRAINING, :218 commitRelease at expectedVersion 0.\n\nTHE FENCE. New module apps/daemon/src/work/attempt-release-resource-fence.ts (131 lines) + tracked one-line .js bridge.\n  CODE: ATTEMPT_RELEASE_RESOURCES_UNPROVEN. LAYER: DAEMON_ATTEMPT_RELEASE.\n  MESSAGE: fixed prose, never interpolated — \"the attempt's durable resource set is not proven terminal\". A test asserts the exact literal and that it contains neither the resource id, the activation aggregate id nor the attempt ref.\n  The code rides `AttemptReleaseRefused[\"code\"]` (store :96-97, already a union of four vocabularies). `ATTEMPT_RELEASE_CODES` membership is UNCHANGED — its exact-membership sibling test stays green, and a test asserts the fence code is NOT in that array.\n  PRODUCER REFUSALS STILL ARRIVE UNDER THEIR OWN CODE AND LAYER. An unreadable/unknown/horizon-moved resource answer refuses at `deriveReleaseTerminalEvidence` and is carried by `carryTerminalRefusal` BEFORE the fence is consulted. The arm pins that against the producer's own answer for the same store: code RELEASE_TERMINAL_RESOURCE_UNKNOWN, layer RELEASE_TERMINAL_EVIDENCE, message = the upstream code — and separately asserts the code is NOT the fence's. \"One stable outcome\" is one outcome SHAPE, never one code collapsing four layers.\n  PLACEMENT: between deriveReleaseTerminal and deriveHandoffBinding. No SECOND evidence read was added — the existing derive IS the strict current read, and a second call would double the horizon race and create the second definition release-terminal-evidence.ts forbids.\n  VERSION GUARD: the attempt's own resource-aggregate head is captured beside the check and re-read as the LAST statement before commitRelease; a move refuses under the same code. Aggregate-scoped, not global — a `readEventHorizon` re-check moves on ANY unrelated write and would livelock a busy daemon. The aggregate is derived from the COMMITTED ACTIVATION by the same derivation the production binder uses (activation-resource-binding.ts), never from the caller's `bound.aggregateId`.\n\nGATE LEGS, run separately at the delivered tree, HEAD 939b20e, counts read from OUTPUT not exit code:\n  1. `pnpm --filter @moe/daemon typecheck` -> EXIT 0, with the `$ tsc --project tsconfig.json` execution echo and zero diagnostics.\n  2. `pnpm --filter @moe/daemon test` -> EXIT 0, \"Test Files  208 passed (208)\" / \"Tests  4580 passed | 2 todo (4582)\".\n  3. `pnpm typecheck` REPO-WIDE -> EXIT 0, every workspace package \"Done\". The path-attributed delta is therefore trivially EMPTY: there are no failing paths at HEAD at all.\n  Focused subject file: \"Test Files  1 passed (1)\" / \"Tests  58 passed (58)\" (was 55 pre-existing; +12 new arms, -1 retired DRAINING arm folded into the sweep).\n\nLINE COUNTS (target <=250 per production source, hard cap 400): fence 131 (new); attempt-release-disposition.ts 251 (was 222); attempt-release-store.ts 253 (was ALREADY 250 at HEAD, so a 1-line type-only import plus a 2-line cycle-hazard comment is the floor). No scratch, probe or generated file is in the commit.",
      "timestamp": "2026-08-22T16:38:44.466Z"
    },
    {
      "id": "comment-f918b09f8c334911b788235baca76456",
      "author": "worker-40cfc767",
      "content": "COMPLETION RECORD 2/3 — MUTATION DRILLS, RESTORE PROOFS, AND THE TWO DEFECTS THE DRILLS FOUND.\n\nRESTORE PROOF METHOD. `git status --porcelain` on the exact mutated paths BEFORE drilling gave ` M` for attempt-release-{disposition,store}.ts and the test file and `??` for attempt-release-resource-fence.ts. `git diff` therefore certifies NOTHING here — nothing for the untracked module and nothing for the three already-dirty files — so every restore is proven by SHA256 against an out-of-tree backup under %TEMP%\\drill-worker-40cfc767, hashed in a call SEPARATE from the restore. One drill touched a clean tracked producer and got both proofs.\n\nDRILL 1 EARLY DRAINING — fence call replaced by `if (false as boolean)`. RED, `Tests 5 failed | 52 passed (57)`:\n  x DEFERS a release whose resource set the reducers can still move\n  x carries FIXED prose, so a refusal leaks no durable identifier\n  x DEFERS every non-terminal resource SHAPE under one code, writing nothing\n  x LIFTS once the REAL reconcile command terminalises the set, then releases\n  x leaves a deferred attempt side-effect free however often it is repeated\n  every failure reading \"expected a refusal, received a recorded row\".\n  RESTORE: SHA256 421A2A2C45A72052B7BFACDCC18CBCE7511B7916A413575F95F58E89F5DE7A4F == backup.\n\nDRILL 2 ZERO-SET TERMINALITY — the fence predicate reads ONE derived boolean, so the zero case lives in the producer: `provenTerminal` mutated to drop `&& split.terminal.length > 0`, making an EMPTY set terminal. RED and it NAMES the case: \"AssertionError: absent: expected [ true, true ] to deeply equal [ true, false ]\" — proof the sweep really generates the absent case.\n  RESTORE: SHA256 A3209640132652895B09D4B02609CB1DCFB22E215412DE7D97EC30721B8A5A67 == backup, AND `git diff --stat` + `git status --porcelain` both empty.\n\nDRILL 3 STALE-VERSION AUTHORIZATION — pre-commit re-read removed. RED on x REFUSES when the resource aggregate moves between the read and the commit (\"expected a refusal, received a recorded row\"), with `Tests 1 failed | 1 passed`: the CONTROL arm (same delegating proxy, nothing moves, releases normally) stayed green, so the red is the guard and not the proxy. Re-run after the step-5 refactor: red again, same arm.\n\nDRILL 4 CHECK-AFTER-BINDING, three positions:\n  4a after deriveHandoffBinding -> RED on x answers under the FENCE's layer, not a LATER seam's, when both would refuse.\n  4b after releaseWork -> RED, same arm.\n  4c after commitRelease -> RED on FIVE arms, \"expected [ 1, 1 ] to deeply equal [ +0, +0 ]\" (row AND decision counts).\n  RESTORE after 4: SHA256 421A2A2C... == backup. Final post-drill SHA256s re-hashed for all four paths and the committed bytes are those bytes.\n\nDEFECT 1, FOUND BY A DRILL THAT STAYED GREEN, fixed before proceeding. 4a/4b were green as first written. MEASURED WHY: on a deferred path `deriveHandoffBinding` writes NOTHING — `recordReleaseHandoffBinding` answers `written:false` when the journal is ABSENT (release-handoff-binding.ts:137) and these fixtures journal nothing — while `releaseWork` and `releaseProviderSlot` are pure kernels, so a fence moved past any of them returns the SAME code over the SAME zero rows. NEW ARM makes ordering observable: it plants an event of the wrong type on `deriveAttemptJournalAggregateId(record.activationDigest)`, asserts against the PRODUCTION surface that the later seam really would refuse (`deriveHandoffBinding` -> [\"JOURNAL_RECORD_MALFORMED\", \"DAEMON_JOURNAL_APPEND\"]), and then asserts the release comes back as the FENCE's deferral with [0,0]. That is DoD 3's other half: an unrelated later refusal must not stand in for the deferral and must not become DRAINING.\n\nDEFECT 2, FOUND BY THE ADVERSARIAL CHECKLIST rather than a drill. `readAttemptResourceVersion` originally took `bound.aggregateId` — a CALLER value. The production writer targets `deriveAttemptResourceAggregateId(deriveActivationAggregateId(record.effectIntent.aggregateId, record.effectIntent.idempotencyKey))`. Watching an aggregate the writer never targets would have made the guard a silent no-op that still read as coverage. Fixed: the fence now derives from the committed activation by the writer's own derivation, matching this module's rule that facts are re-read from the activation and the `record` argument is identity-agreement only.\n\nADVERSARIAL CHECKLIST, all measured at the delivered tree: producers untouched (`git status --porcelain` empty for release-terminal-evidence{,-contracts}.ts, attempt-resource-authority{,-contracts}.ts, resource-reconcile-command.ts, attempt-release-{boundary,terminal,handoff,record}.ts, foundation-attempt-service.ts); ATTEMPT_RELEASE_CODES membership unchanged; the disposition diff is purely ADDITIVE, no line removed; no caller-supplied terminal or state value read on any path; retired-key refusals still the first three statements (:164 boundary, :165 terminal, :166 handoff); DRAINING still reachable through the effects and boundary arms; noteRelease still discards the outcome, so no dispatch answer changed.",
      "timestamp": "2026-08-22T16:39:12.071Z"
    },
    {
      "id": "comment-6b22ae8c9a8444f69eca2771c4d1cacd",
      "author": "worker-40cfc767",
      "content": "COMPLETION RECORD 3/3 — CONSUMERS, AND TWO DISCLOSURES THIS ROW MUST NOT SILENTLY SWALLOW.\n\nDOWNSTREAM CONSUMERS, both recorded as required:\n  task-a20e8ef668b54c3abbfce37a505252eb — the scheduler nine-key ReleaseHandoff builder. This row does not fabricate that handoff; production still passes a null scheduler handoff and `AttemptReleaseRequest` keeps its four keys.\n  task-48c79a29e1a4416b8db4c2916f5a5381 — STILL OWNS the post-verification retry that turns a deferral into a release. This row deliberately does NOT build it. A deferred attempt today stays deferred until something re-drives `recordAttemptRelease` after `resource.reconcile` has terminalised the set; the LIFT arm proves that second call then releases, but nothing in production yet schedules it. That is the finalizer's job and the deferral is the safe state in the meantime (no row is better than an un-upgradeable DRAINING row).\n\nDISCLOSURE 1 — THE EFFECTS FAMILY IS STILL EXPOSED TO THE SAME expectedVersion-0 POISONING. MEASURED at the delivered tree, not inherited from the plan text: `recordTerminalEffect` is called at foundation-attempt-service.ts:163 ONLY, inside the proven path that precedes the `noteRelease` at :197. The `unproven` function (:211-221) reaches `noteRelease` at :217 with no `recordTerminalEffect` call anywhere on that path, so `effectsTerminal` derives FALSE there, `releaseWork` answers DRAINING, and `commitRelease` pins that row at expectedVersion 0 exactly as the resources case did. Task rail 1 and every DoD clause on this row scope it to RESOURCES, so I did NOT widen the fence to `releasable` — a fence over both families would also have killed the effects and boundary DRAINING arms that are this row's negative control. Reporting the measurement so a governor can file it as its own row.\n\nDISCLOSURE 2 — A RESIDUAL WINDOW IN THE VERSION GUARD, stated precisely. The fence itself (a non-terminal set) refuses BEFORE `deriveHandoffBinding`, so that path writes zero release AND zero binding events, asserted. The VERSION guard, however, sits immediately before `commitRelease`, which is after the binding derivation — so if a second connection moved the resource aggregate late, on an attempt that HAS a durable journal, a handoff binding row may already exist while no release row does. Three things bound it: (i) the binding is documented inert by its own module (\"a FACT about what the attempt handed off, not authority — only a consumer that also sees RELEASED may act on it\"), and that state is already reachable today through every late refusal on this path (kernel rejection, slot rejection, encode drift, COMMIT_UNAVAILABLE); (ii) MEASURED in step 4 and asserted inside the committed suite — once a set is proven terminal NO reconcile-reachable reducer will move it (`adapterConfirm` requires PENDING_ACQUIRE at lease-resource.ts:124, `adapterFail` requires PENDING_ACQUIRE or ACTIVE at :152, and the GRANT variant is unreachable from that wire), so this window is only reachable by a writer this row cannot enumerate; (iii) moving the binding derivation later was rejected because its own header requires it BEFORE the kernel. Closing it properly means making the release commit carry the resource aggregate's expected version as a second decision leg, which changes the release store's commit identity — precisely the approach the plan rejected as out of scope. Disclosed rather than half-fixed.\n\nMETHOD NOTE FOR QA, so an arm is not misread as vacuous. The version-race arm interposes through a Proxy that DELEGATES every call to the real file-backed store (`Reflect.get(base, key, base)` keeps the private fields reachable) and answers the first `getAggregateVersion` for the resource aggregate with the real PRE-write value before letting the second writer in — firing the write first would make capture and re-read agree and the arm would test nothing. In the window it RUNS the real `runResourceReconcileCommand` and asserts its refusal (`ok:false`, code ATTEMPT_RESOURCE_SET_REFUSED), which is how the frozen-set fact above lives in the committed suite rather than in a transcript, and then lands the movement as a real durable append at the measured head. Nothing stubs a version number. The paired control arm releases normally over the same proxy.\n\nONE FIXTURE WORTH READING BEFORE GRADING. The zero/absent resource case takes a production route with no planting: the ingress ADMITS a slot declaring the same resource twice, `admitBind` refuses it (ATTEMPT_RESOURCE_MEMBER_DUPLICATE, attempt-resource-authority.ts:96), and a bind refusal is not an activation refusal by design — so the activation is durable while the reader answers ATTEMPT_RESOURCE_RECORD_ABSENT. `slot.rows: []` was tried FIRST and is NOT a route: the ingress refuses it with WORK_PAYLOAD_MALFORMED. The sweep asserts `cases.length === 3`, the producer's own per-family answer per case, and `shapes === [\"ATTEMPT_RESOURCE_RECORD_ABSENT\", [\"ACTIVE\"], [\"QUARANTINED\"]]` read out of `readAttemptResources`, so the three cases are provably three different durable shapes rather than three spellings of one.",
      "timestamp": "2026-08-22T16:39:40.911Z"
    },
    {
      "id": "comment-7b62a1aa216e44ffa70ebaa1471d3071",
      "author": "qa-8546c5ba",
      "content": "QA VERDICT: APPROVED at commit bb6978b. Everything below was run by me, not read.\n\nDIFF: 5 files, +586/-18. Per-file at HEAD: attempt-release-resource-fence.ts 131, attempt-release-disposition.ts 251, attempt-release-store.ts 253, release-terminal-evidence.ts 255, test 1932. The three at 251-255 sit above the <=250 TARGET and far below the 400 split cap, and epic rail 5 bars task-level LOC as a reject reason.\n\nFOUR MUTATION DRILLS, each applied alone at bb6978b and restored byte-exact (disposition SHA 1B754145...4016, fence SHA 760ABF92...DA30, git diff empty after each):\n  D1  `if (resourcesUnproven(terminal.flags)) return refuseUnprovenResources();` -> `if (false)`\n      6 RED BY NAME: \"DEFERS a release whose resource set the reducers can still move\" / \"carries FIXED prose, so a refusal leaks no durable identifier\" / \"DEFERS every non-terminal resource SHAPE under one code, writing nothing\" / \"answers under the FENCE's layer, not a LATER seam's, when both would refuse\" / \"LIFTS once the REAL reconcile command terminalises the set, then releases\" / \"leaves a deferred attempt side-effect free however often it is repeated\".\n  D2  version guard disabled (`if (false && resourceVersionMoved(...))`)\n      1 RED BY NAME: \"REFUSES when the resource aggregate moves between the read and the commit\". Narrow, which is the point.\n  D3  MY OWN, not in your record - the guard's AGGREGATE DERIVATION. `readAttemptResourceVersion` pointed at `durable.effectIntent.aggregateId` instead of `deriveAttemptResourceAggregateId(deriveActivationAggregateId(...))`.\n      1 RED BY NAME: the same moved arm. Your header claims \"watching an aggregate the writer never targets would make this guard a silent no-op that still read as coverage\" - that claim is now drilled, not just asserted. This is the failure mode I most expected to find undefended and it is not.\n  D4  POSITION drill. Moved `if (resourcesUnproven(...))` from before `deriveHandoffBinding` to immediately before `commitRelease`.\n      1 RED BY NAME: \"answers under the FENCE's layer, not a LATER seam's, when both would refuse\". The position IS the deliverable and the position is pinned.\nBaseline before drills: 58 passed (58), EXIT 0. Focused re-run at HEAD d0779f4 after restore: 2 files / 79 passed, EXIT 0.\n\nDoD, item by item.\n  1 VERIFY-OR-BLOCK: premises re-measured on the delivered tree and recorded; I spot-confirmed the load-bearing ones - `commitRelease` still first-write expectedVersion 0, the reconcile ingress still writes through the real command.\n  2 The fence sits after `deriveReleaseTerminal` and BEFORE `deriveHandoffBinding` and before any kernel ask. All seven enumerated shapes (absent, zero, active, quarantined, unknown, unreadable, horizon-moved) refuse before any write; the UNREADABLE arm carries the PRODUCER's code and layer unrestamped rather than the fence's, which is the distinction that usually gets collapsed.\n  3 The LIFT arm drives the AUTHENTICATED PRODUCTION INGRESS `runResourceReconcileCommand`, not a direct authority call, then releases with no caller-supplied terminal flag. That is DoD 3 exactly.\n  4 Idempotent deferral asserted side-effect free (0 rows / 0 decisions, twice, identical answers); version fence proven by D2 and D3; \"does NOT weaken the non-resumable cleanup path\" arm present and green.\n  5 The SHAPE sweep carries its own positive control - `expect(cases.length).toBe(3)` AND a final `expect(shapes).toEqual([\"ATTEMPT_RESOURCE_RECORD_ABSENT\", [\"ACTIVE\"], [\"QUARANTINED\"]])` so a seeding slip cannot collapse three shapes into one repeated case and still pass. That is the anti-vacuous control rail 6 asks for, and most sweeps on this board do not have it.\n  6 Consumers recorded in the commit message: task-a20e8ef6 and task-48c79a29.\n\nGATES - and the package is RED right now for reasons that are provably not yours.\n  Your recorded leg: typecheck + test EXIT 0, 208 files / 4580 passed | 2 todo, at 16:42Z. I INDEPENDENTLY CORROBORATE that: my own COLD typecheck (buildinfo deleted) and full daemon run at 16:39Z, on a worktree that already contained these exact bytes untracked, were EXIT 0 at the same 208 / 4580. Your evidence was real when recorded.\n  At HEAD d0779f4 now: typecheck EXIT 1 with exactly ONE error path - `src/budget/budget-settlement-application.test.ts(593,19) TS2352` - which is worker-934d8b66's UNCOMMITTED in-flight fix for task-a91e9fe2 (the truncated-receipt arm I asked them for 20 minutes ago). Not in HEAD, not your path.\n  `pnpm --filter @moe/daemon test` EXIT 1: 14 files / 133 tests. Root cause read, not guessed: every failing arm dies at seeding with \"world seed refused at plan.propose: PLANNING_AUTHORITY_GRAPH_CONTENT_MISMATCH\", and `apps/daemon/src/planning/journey-authority-bodies.ts` is ` M` with an uncommitted 212-line rewrite (task-c96ef2d1's graphContentHash flip). The failing set - activation, bootstrap, goals, http, orchestrator, planning, work/foundation-context-selection - has ZERO overlap with this row's paths, and your own suites are green at that same HEAD.\n\nYOUR TWO DISCLOSURES, both checked rather than accepted.\n  D2 residual window: CONFIRMED and correctly bounded. `deriveHandoffBinding` does write (`recordReleaseHandoffBinding`), so a late version move can leave a binding row with no release row. Your bound (ii) is the load-bearing one and it verifies verbatim: `adapterConfirm` refuses unless PENDING_ACQUIRE (lease-resource.ts:124-126) and `adapterFail` refuses unless PENDING_ACQUIRE or ACTIVE (:152-154), so no reconcile-reachable reducer can move a set that is already proven terminal. DoD 2's enumerated list is satisfied before the binding; DoD 4 says a stale read may not authorise a RELEASE, and it does not. Disclosed rather than half-fixed was the right call.\n  D1 effects family: CONFIRMED as a real, production-reachable defect of the same class, correctly OUT of scope here (task rail 3 scopes this row to resources, and widening the fence to `releasable` would have killed the effects/boundary DRAINING arms that are this row's negative control). It needs a LIVE BOARD ROW, not a note on a row about to go DONE - a comment on a closed row has no future claimant. I am raising it in-channel now.",
      "timestamp": "2026-08-22T16:53:51.490Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-22T14:33:47.293Z",
  "updatedAt": "2026-08-22T16:55:31.046Z",
  "metrics": {
    "firstClaimAt": "2026-08-22T16:00:50.068Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 6,
    "doneAt": "2026-08-22T16:55:31.045Z",
    "wallClockMs": 3280977
  },
  "contextFetchedBy": [
    "architect-0975cd3a",
    "worker-40cfc767",
    "qa-8546c5ba"
  ],
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-22T16:06:37.100Z",
  "planningNotes": {
    "approachesConsidered": "(a) Change `commitRelease`'s expectedVersion so a DRAINING row can be upgraded later — REJECTED: it edits the release store's commit identity and replay semantics, which rail 3 scopes away from this row, and it would leave a nonterminal release row standing as durable truth. (b) Fence on `releasable` (both families) rather than resources — REJECTED as scope creep against task rail 1 and every DoD clause, all of which are resource-scoped; the effects residual is DISCLOSED in step 6 instead so the governor can file it. (c) Re-derive terminality inside the fence — FORBIDDEN: `release-terminal-evidence.ts`'s header names a second opinion over the same rows as the drift that makes a QUARANTINED member read as something else. The fence composes over the producer's single boolean. (d) A second `deriveReleaseTerminalEvidence` call to satisfy 're-reads' literally — REJECTED: it doubles the horizon race and creates the second definition (c) forbids; the existing call at :183 already IS the strict current read and already precedes the handoff derivation and the kernel. (e) A new test file for the fence arms — REJECTED: `attempt-release-disposition.test.ts` already owns `durableRowCount`, `releaseDecisionCount` and `expectNoDurableRow`, the exact zero-rows-AND-zero-decisions instruments DoD 2 needs; a second file would reimplement them, which global rail 1 names as a defect. (f) A global `readEventHorizon()` re-check before commit — REJECTED as a livelock hazard: any unrelated write moves the global horizon, so nearly every release on a busy daemon would refuse. The aggregate-scoped version fence is the correct instrument.",
    "codebaseInsights": "MEASURED AT HEAD 7267e38 (the filer measured 16c6230; re-measure at yours). All four DoD-1 premises HOLD: reconcile ingress `runResourceReconcileCommand`:136 -> `applyAttemptResourceReport`:149, reached from `daemon-command-registry.ts:209`; `provenTerminal` (release-terminal-evidence-contracts.ts) = nonzero AND wholly terminal, ABSENT set -> EMPTY_SPLIT -> false, horizon-moved arm present; `noteRelease` (foundation-attempt-service.ts:116-125) calls `recordAttemptRelease` immediately at :197 and :217; `attempt-release-store.ts:210-215` commits at expectedVersion 0 with the grant-derived event id.\n\nWHAT MOVED SINCE THE FILING, and it changes the diagnosis without changing the defect: task-6d400781 landed, so terminality is ALREADY server-derived — `deriveReleaseTerminal` at `attempt-release-disposition.ts:183`, with the retired caller keys `effectsTerminal`/`resourcesTerminal` refused outright at :157. So this is NOT a caller-relay defect. The defect is that a DERIVED false still PROCEEDS: :183 derives, :188 derives the handoff, :191 calls `releaseWork` which answers DRAINING, and :218 `commitRelease` pins the row at expectedVersion 0. The fence slots between :183 and :188.\n\nBLAST RADIUS, exactly two sites: `RESOURCES_PENDING` appears at test :818 (a `deriveReleaseTerminal` transposition guard that never releases — verify, do not assume) and :1062 (`drainedByResources`, the arm that must become a deferral). Every other DRAINING fixture is EFFECTS_PENDING or UNOBSERVED_TERMINALITY, both of which carry `resourcesTerminal: true`, so DRAINING stays reachable and the rest of the suite is untouched.\n\nORDERING IS SAFE: `recordAttemptRelease` is fully synchronous (no `await` in :145-222) and `releaseProviderSlot` only composes a value — the first and only write is `commitRelease`. So a fence returning before it leaves zero rows and zero decisions by construction.\n\nUNION SEAM: `AttemptReleaseRefused[\"code\"]` (attempt-release-store.ts:96-97) is already `AttemptReleaseCode | AuthorityErrorCode | ReleaseTerminalCode | SafeBoundaryRefusalCode` — foreign vocabularies are the established pattern, so the fence declares its own code and widens the union rather than inserting into the closed `ATTEMPT_RELEASE_CODES` array (whose header explicitly warns against daemon-only members).\n\nEVERY sibling `.ts` in `apps/daemon/src/work/` has a tracked one-line `.js` bridge (`export * from \"./x.ts\";`). The new module needs one.",
    "risks": "(1) The fence looks trivial — one boolean read — so QA will hunt for a vacuous assertion. The defence is in the TESTS: file-backed stores, real `runResourceReconcileCommand` for the lift arm, and zero-rows-AND-zero-decisions rather than 'it refused'. (2) The `drainedBy` helper at :1036 asserts DRAINING and `durableRowCount === 1` for all three arms; changing only the resources arm without touching the other two is the delicate edit. (3) The UNKNOWN passthrough is the rail-6 trap: an unreadable resource must still arrive under RELEASE_TERMINAL_RESOURCE_UNKNOWN and the producer's layer, NOT the fence's code — 'one stable outcome' in DoD 2 means one outcome SHAPE, not one code collapsing four layers. (4) The version-fence arm must interpose a REAL production write; a stubbed version number would test the stub. (5) The new module is untracked during drills, so `git diff` certifies nothing — SHA256 against a %TEMP% backup, in a separate call. (6) Shared worktree: pathspec commits only; `attempt-release-disposition.test.ts` is a busy surface, so keep the edits to the two measured sites plus appended arms.",
    "keyFiles": [
      "apps/daemon/src/work/attempt-release-disposition.ts",
      "apps/daemon/src/work/release-terminal-evidence.ts",
      "apps/daemon/src/work/release-terminal-evidence-contracts.ts",
      "apps/daemon/src/work/attempt-release-store.ts",
      "apps/daemon/src/work/attempt-release-disposition.test.ts"
    ]
  },
  "budget": {
    "wallClockMs": 5400000
  },
  "planApprovedAt": "2026-08-22T16:06:39.182Z",
  "workStartedAt": "2026-08-22T16:07:04.390Z",
  "reviewStartedAt": "2026-08-22T16:42:05.926Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\n$ vitest run --root . --config package.json src\n\n RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n\n Test Files  208 passed (208)\n      Tests  4580 passed | 2 todo (4582)\n   Start at  19:40:14\n   Duration  58.71s (transform 60.03s, setup 0ms, import 198.01s, tests 367.93s, environment 17ms)\n\nVERIFY_EXIT=0",
    "reportedAt": "2026-08-22T16:42:05.926Z"
  },
  "filesModified": [
    "apps/daemon/src/work/attempt-release-disposition.ts",
    "apps/daemon/src/work/release-terminal-evidence.ts",
    "apps/daemon/src/work/attempt-release-store.ts",
    "apps/daemon/src/work/attempt-release-disposition.test.ts",
    "apps/daemon/src/work/attempt-release-resource-fence.ts",
    "apps/daemon/src/work/attempt-release-resource-fence.js"
  ],
  "completedAt": "2026-08-22T16:55:31.045Z",
  "reviewCompletedAt": "2026-08-22T16:55:31.045Z",
  "reviewSummary": "APPROVED at bb6978b. 5 files, +586/-18; per-file 131/251/253/255 (target 250, cap 400). Full verdict: comment-7b62a1aa.\n\nFOUR DRILLS, each alone, restored byte-exact (SHA 1B754145...4016, 760ABF92...DA30):\nD1 fence check -> if(false): 6 RED BY NAME, incl. \"DEFERS a release whose resource set the reducers can still move\" and \"LIFTS once the REAL reconcile command terminalises the set\".\nD2 version guard disabled: RED \"REFUSES when the resource aggregate moves between the read and the commit\".\nD3 MINE, not in your record: readAttemptResourceVersion pointed at the wrong aggregate - same arm REDS, so the header's \"silent no-op\" claim is drilled, not asserted.\nD4 POSITION: fence moved to just before commitRelease - RED, the FENCE-layer arm.\nBaseline 58/58 EXIT 0; focused re-run at HEAD after restore 79/79 EXIT 0.\n\nDoD: 1 premises re-measured; 2 fence precedes deriveHandoffBinding and any kernel ask, all seven shapes write zero rows, UNREADABLE keeps the PRODUCER's code/layer; 3 LIFT arm drives the real runResourceReconcileCommand ingress; 4 idempotent deferral 0 rows/0 decisions twice; 5 sweep has cases.length===3 AND a distinct-shapes assertion; 6 consumers a20e8ef6 + 48c79a29 recorded.\n\nGATES: your 208/4580 EXIT 0 corroborated - my own COLD typecheck and full daemon run at 16:39Z, on a worktree holding these exact bytes, matched. Red NOW is foreign: typecheck's one error path is worker-934d8b66's uncommitted a91e9fe2 fix (budget-settlement-application.test.ts:593); 14 files/133 tests all die at \"world seed refused at plan.propose: PLANNING_AUTHORITY_GRAPH_CONTENT_MISMATCH\" from the uncommitted journey-authority-bodies.ts rewrite. Zero overlap with this row.\n\nDisclosures verified: deriveHandoffBinding does write, so the late-race binding window is real and bounded (adapterConfirm needs PENDING_ACQUIRE, lease-resource.ts:124). The effects-family exposure is real and needs its own live row."
}