{
  "id": "task-3697c02bf2ab41a896fcfacb125d9e46",
  "epicId": "epic-af14d73585e14c0da0264d48c37587c3",
  "title": "bug: a live release.decide outruns the browser's 15s transport abort, so the operator never sees the PR link",
  "description": "Filed from task-817d893fa1254a4d82d2888af1f87a47 step 4 (the Gate 3 LIVE drive). MEASURED, not inferred: real pull request https://github.com/yaront1111/Moe-NG/pull/32 was opened by the product on 2026-09-08T17:04:12Z and the browser session that ordered it never rendered the link.\n\nREPRO (tests/e2e/control-room/release-approval-live.spec.ts, MOE_LIVE_RELEASE_PR=1 against a real GitHub remote): arm + confirm the Release card. `release.decide` runs publishOnce, then `gh pr create`, then a `gh pr view` re-read (release-decide-service.ts:193-206, release-pr-port.ts:189-196) and takes tens of seconds against a real remote. The browser command transport aborts at 15s (packages/control-room-client/src/client-transport.ts:118 DEFAULT_REQUEST_TIMEOUT_MS, no requestTimeoutMs supplied by the release path). LiveGoalRelease's post-submit refresh() (live-goal-release.tsx:38) then fires into the still-busy daemon and the read aborts too (live-effect-read.ts:70, AbortSignal.timeout(15_000)), so useEffectRead settles on ERROR and renders cr.release.read-refusal - \"The release evidence could not be read right now.\" - which it never retries out of.\n\nNET EFFECT: the command IS delivered, the PR IS opened and the RELEASED receipt IS recorded, but the operator's card ends on an error note. Only a remount/reload surfaces the link. The Gate 3 promise \"the PR link renders after a RELEASED receipt\" holds only for a fast (faked) gh.\n\nNOT this row's to fix: task-817d893f proves Gate 3, it does not rebuild it (its task rail 1), and the timeout lives in the shared command transport, not in the release card. The live spec asserts the true behaviour (receipt polled off /release/read, then the goal re-opened) and carries this defect in a comment.\n\nSUGGESTED SHAPE (not a plan): either raise requestTimeoutMs for the long-running command kinds, or make release.decide return promptly and let the card poll the receipt - and give LiveGoalRelease a retry rather than a terminal ERROR state.",
  "definitionOfDone": [
    "A browser session that confirms a release against a slow (real) gh either keeps waiting for the daemon's answer or recovers on its own: the PR link appears in the SAME session without a manual reload. Asserted by a test that makes the decide take longer than the transport bound.",
    "The refusal path is unchanged: a genuinely refused release still renders its code VERBATIM with its layer, and a transport abort is still distinguishable from a daemon refusal by code."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "SETTLE THE MECHANISM — THE FILING'S KEY CLAIM IS CONTRADICTED BY THE SOURCE. NO EDITS. Do this before writing a line; the rest of the plan is written to survive either answer, but the record needs the truth.\n\nTHE DISCREPANCY. The description and the live spec's comment (release-approval-live.spec.ts:315) both say the card lands on `cr.release.read-refusal`, \"which `useEffectRead` never retries out of\". Read apps/control-room/src/v2/components/use-effect-read.ts and check that against the code:\n- `:19` installs `setInterval(refresh, pollMs)` with `pollMs = 5_000` — a 5-SECOND POLL, not a one-shot.\n- the failure path is `.catch(() => failure).then((value) => { setFrame({ read, value }); busy = false; ... })` (:12-16), so an aborted read STORES the failure value AND clears `busy`. Nothing latches.\n- the effect's deps are `[read, failure, pollMs]`; in live-goal-release.tsx `read` is a `useMemo` on `[setup, goalId]` and `failure` is a module constant, so the effect is not torn down between polls.\nOn the source, the hook RETRIES every 5s. Either the claim is wrong, or something else latches.\n\nSETTLE IT WITH A MEASUREMENT, not by re-reading. The cheapest discriminator is a component-level test, not the live lane: render `LiveGoalRelease` with a reader that fails the first N calls and then returns a PRESENT receipt, advance timers past 5_000, and see whether the card leaves `cr.release.read-refusal` on its own. `use-effect-read.test.tsx` already exists next to the hook — read it first for the fake-timer idiom this codebase uses.\n\nALSO CHECK THE OTHER LATCH CANDIDATE, because it produces the same operator-visible symptom by a different route: live-goal-release.tsx:44 computes `offered = releaseOffer(frame, goalId) !== null` from the BOARD frame, and :46-50 returns the error note only when `offered`, else `null`. After a RELEASED receipt the daemon stops offering `release.decide`, so the card can also DISAPPEAR rather than show the link. Record which of the two the operator actually saw.\n\nRECORD, verbatim, in the step note: whether the 5s poll fires; whether the card recovers on its own and after how long; and which state it ends in. If the poll DOES work, say so plainly — the filing's parenthetical is then wrong and the defect is narrower (a card that reports a FAILURE while the command is still in flight), which is still a real defect and still what DoD 1 asks you to fix. Do not soften the record to match the filing.",
      "status": "PENDING",
      "affectedFiles": []
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — NEW `apps/control-room/src/v2/goals/live-goal-release.test.tsx`, THE ARM DoD 1 NAMES (test files are exempt from the line cap).\n\nDoD 1's clause is literal: \"Asserted by a test that makes the decide take longer than the transport bound.\" Build that at the COMPONENT level with fake timers — do NOT reach for the browser lane or a real `gh`. The live drive already happened (PR #32) and re-running it opens another real pull request every time the suite runs.\n\nTHE ARM. Render `LiveGoalRelease` with:\n- a submit that behaves the way the real transport did: it does NOT resolve with the daemon's answer, it settles as a TRANSPORT ABORT after the bound (the shape `client-transport.ts` returns — `{ delivered: false, code, layer }`, the `TransportRefused` case whose own comment at :103-106 says `delivered` separates \"a daemon that refused\" from \"a daemon that never answered\");\n- a reader that answers ABSENT (or fails) while the decide is in flight and then answers PRESENT with a receipt carrying a `prUrl`, modelling the daemon finishing.\nThen assert the operator-visible promise: `cr.release.link` becomes visible IN THE SAME MOUNT, with no remount and no reload. Advance timers rather than sleeping.\n\nASSERT THE INTERMEDIATE STATE TOO, because it is the actual defect: while the decide is in flight the card must NOT be showing `cr.release.read-refusal`. A card that says \"The release evidence could not be read right now.\" about a command that is succeeding is telling the operator something false, and that sentence — not the eventual recovery — is what sent this row to the board.\n\nDO NOT ASSERT A REMOUNT. `release-approval-live.spec.ts:320-327` already covers \"re-open the goal and the link is there\"; re-proving it here would pass without the fix.\n\nRun the single file and quote the failing assertion. An arm that was never seen red is not red-first.",
      "status": "PENDING",
      "affectedFiles": [],
      "newFiles": [
        "apps/control-room/src/v2/goals/live-goal-release.test.tsx"
      ]
    },
    {
      "stepId": "step-3",
      "description": "THE FIX — A CLIENT-SIDE TIMEOUT IS NOT EVIDENCE THE COMMAND DID NOT HAPPEN.\n\nThat sentence is the whole row. The daemon received `release.decide`, opened PR #32 and recorded a RELEASED receipt; the browser stopped waiting and then told the operator the evidence \"could not be read right now\". The card reported a failure about work that was succeeding.\n\nIN apps/control-room/src/v2/goals/live-goal-release.tsx, hold the fact that a submit is OUTSTANDING and let it govern the ERROR branch at :46-50. While a submit has been sent and has not come back with a DAEMON answer, a failed read is PENDING, not failed: keep the card in a waiting state (or keep the last good evidence on screen) and let the existing poll deliver the receipt. The port wrapper at :34-43 already intercepts every submit — that is the seam; do not add a second decision path, and do not reshape `createReleasePort`.\n\nTHE STATE MUST CLEAR. Whatever marks the submit outstanding has to be released when the daemon answers, when a read finally succeeds, and on unmount — a card stuck in \"waiting\" forever is the same defect wearing a friendlier word. Say in a comment what clears it.\n\nIF, AND ONLY IF, STEP 1 MEASURED THAT THE 5s POLL DOES NOT FIRE, also fix apps/control-room/src/v2/components/use-effect-read.ts — and then say exactly what latched, because the source at :12-19 says it should not. Do not touch that hook on suspicion: it backs several cards, and a change there is a change to all of them.\n\nDO NOT RAISE `DEFAULT_REQUEST_TIMEOUT_MS` (client-transport.ts:118) AND DO NOT THREAD A LONGER `requestTimeoutMs` FOR THIS KIND. The description offers it as one shape; it is the wrong one here. That constant is the shared bound for EVERY command kind in the control room, a real `gh pr create` plus `gh pr view` has no bound the browser can know in advance, and a number picked to cover the observed run re-arms for the first slower remote — while the operator stares at a spinner with no evidence either way. Recovering is right; guessing a bigger number is not. If you conclude otherwise, stop and say so with the measurement rather than changing it.\n\nRe-run step 2's arm and confirm it goes GREEN.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goal-release.tsx"
      ]
    },
    {
      "stepId": "step-4",
      "description": "THE REFUSAL PATH IS UNCHANGED, AND THAT IS A SEPARATE ARM (DoD 2). Both clauses, in the same new test file.\n\n(a) A GENUINE DAEMON REFUSAL STILL RENDERS ITS CODE VERBATIM WITH ITS LAYER. Drive a submit that comes back DELIVERED with a refusal, and assert the rendered `CODE @ LAYER` text — `OutcomeNote` prints exactly that inside its Details block (outcome-note.tsx:22). Use the release vocabulary's real code, `RELEASE_EVIDENCE_INCOMPLETE`, not an invented one, and assert the LITERAL string. This is the arm that stops step 3's \"treat a failure as pending\" from swallowing a real refusal — which is the one way this fix could do harm, so write it as though that is what you are hunting.\n\n(b) A TRANSPORT ABORT IS STILL DISTINGUISHABLE FROM A DAEMON REFUSAL BY CODE. Assert the two paths reach DIFFERENT codes, and assert them by their literal values — not merely that both are non-empty, and not by asserting only that a note rendered. Project rail 1: a test that asserts only \"it refused\" is one added layer away from vacuous. The discriminator already exists in the type system — `TransportRefused` carries `delivered: false` and a transport-layer code, a daemon answer carries the engine's own code and layer — so assert the layer too, and pin WHICH layer answered.\n\nA note on framing worth putting in the file's header comment: the fix in step 3 does not make a transport abort mean \"success\". It makes it mean \"unknown, still waiting\", which is what it has always actually meant. The refusal vocabulary is untouched, and these two arms are the proof.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goal-release.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/live-goal-release.test.tsx"
      ]
    },
    {
      "stepId": "step-5",
      "description": "ONE CONCENTRATED CLOSE: DRILL, GATES, ADVERSARIAL PASS.\n\nTHE DRILLS — `git status --porcelain` the mutated paths FIRST. live-goal-release.tsx is TRACKED, so an empty `git diff -- <path>` is a valid restore proof, in a call SEPARATE from the restore. live-goal-release.test.tsx is NEW and UNTRACKED, and `git diff` is BLIND to it: back it up out of tree under %TEMP% suffixed with your worker id and prove the restore by SHA256 equality (global rail 13). Take the backup BEFORE planting any mutation.\nTwo mutations, each against the single test file:\n(i) Revert step 3's change — let a failed read during an outstanding submit render the refusal again — and confirm the DoD-1 arm goes RED naming the missing link or the wrongly-rendered `cr.release.read-refusal`. That is the row's subject; if it stays green the arm proves nothing.\n(ii) Make the outstanding-submit state swallow a DELIVERED daemon refusal too, and confirm arm 4(a) goes RED. That proves the guard is scoped to transport aborts and has not blindfolded the refusal path.\n\nGATES, each FOREGROUND and STANDALONE, exit code AND count line quoted verbatim:\n- `pnpm --filter @moe/control-room test` — the owned-package leg, and the one that actually covers these files.\n- `pnpm typecheck`.\n- `pnpm test` — disclose the root result. It is the ROOT lane only: its vitest include has NO `apps/**`, so it does not cover a single file this row touches and must never be quoted as evidence that they pass. Say that explicitly rather than letting a green root count imply coverage.\nRead the OUTPUT, not the exit code: a `--filter` leg prints \"No projects matched the filters\" and EXITS 0, and the discriminator is the `Test Files N passed` line.\nPath-attributed baseline (global rail 3): repo-wide failures at merge-base and at HEAD, complete only when the delta intersected with this row's owned paths is EMPTY, and disclose any foreign red verbatim with its path. The checkout is shared and currently dirty with peer work under apps/control-room/src/v2/ops/ — note it, never revert or commit it.\n\nADVERSARIAL PASS (load `adversarial-self-review`). Ask, in order: What if the operator confirms twice while a submit is outstanding — does the guard admit a second decide? What if the component unmounts mid-flight? What if the daemon never answers at all — does the card wait forever with no way out, and is that better or worse than the error note it replaced? And the honest one: after this fix, is there still any state in which the PR link exists in a durable receipt but the operator's session never shows it?",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goal-release.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/live-goal-release.test.tsx"
      ]
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 76,
  "comments": [
    {
      "id": "comment-fba98254b4cc4880896873ccb953f5e1",
      "author": "architect-5385678f",
      "content": "ARCHITECT architect-5385678f 2026-09-08 — PLANNED (5 steps / 2 files, 1 new). One correction to the filing that the worker must settle FIRST, and it changes what gets fixed.\n\nTHE \"NEVER RETRIES\" CLAIM IS CONTRADICTED BY THE SOURCE. Both the description and release-approval-live.spec.ts:315 say the card lands on `cr.release.read-refusal`, \"which `useEffectRead` never retries out of\". Read apps/control-room/src/v2/components/use-effect-read.ts:\n- `:19` installs `setInterval(refresh, pollMs)` with `pollMs = 5_000` — a five-second POLL, not a one-shot read.\n- `:12-16` the failure path is `.catch(() => failure).then((value) => { setFrame({ read, value }); busy = false; … })` — an aborted read STORES the failure value AND clears `busy`. Nothing latches.\n- deps are `[read, failure, pollMs]`; in live-goal-release.tsx `read` is a `useMemo` over `[setup, goalId]` (:32) and `failure` is a module constant (:23-25), so the effect is never torn down between polls.\nOn the source, the hook retries every 5s. Either the parenthetical is wrong, or something else latches. I did not run the live lane, so I am not overturning a field observation from a plan seat — step 1 settles it with a component-level test and fake timers, and is told to record the answer plainly even if it contradicts the filing.\n\nTHE OTHER LATCH CANDIDATE, same operator-visible symptom, different route: live-goal-release.tsx:44 computes `offered = releaseOffer(frame, goalId) !== null` from the BOARD frame, and :46-50 renders the error note ONLY when `offered`, else returns `null`. After a RELEASED receipt the daemon stops offering `release.decide` (parent row's DoD 3), so the card can also VANISH rather than show the link. Step 1 records which was actually seen.\n\nWHY THE PLAN IS STILL DECISIVE. Under BOTH readings the same defect is present and the same fix answers it: **the card reports a FAILURE about a command that is succeeding.** \"The release evidence could not be read right now\" is on screen while the daemon is opening the pull request. A client-side timeout is not evidence the command did not happen — client-transport.ts:103-106 says `delivered` separates \"a daemon that refused\" from \"a daemon that never answered\", and a 15s abort is the second, which means UNKNOWN, not failed. So the fix holds an outstanding-submit state in the port wrapper at :34-43 (a seam that already intercepts every submit) and lets a failed read read as PENDING while one is in flight; the existing poll then delivers the receipt in the same session.\n\nREJECTED — RAISING THE TIMEOUT, which the description offers first. Three reasons: (1) `DEFAULT_REQUEST_TIMEOUT_MS` (client-transport.ts:118) is the shared bound for EVERY command kind in the control room; (2) a real `gh pr create` plus `gh pr view` has no upper bound the browser can know in advance, so a number picked to cover the observed run re-arms for the first slower remote — the same \"bound calibrated against a moving quantity\" this board has hit twice this week (see task-0bd37766 and task-893389ae); (3) IT WOULD NOT EVEN REACH THE READ PATH. The two 15s bounds are in different transports: the command path is `ControlRoomTransport`, but the read is a bare `fetch` in `readEffect` with `AbortSignal.timeout(15_000)` HARD-CODED at live-effect-read.ts:70 and no option to supply. \"Pass requestTimeoutMs on the release path\" fixes half the problem by construction.\n\nREJECTED — making `release.decide` return promptly and polling. Better long-run architecture, but the decide's answer currently IS the receipt; making it async is a wire-semantics change with a new in-flight state and every consumer updated. That is several rows, and this is a bug fix on the operator's side.\n\nREJECTED — asserting it through the live browser lane. The live drive already opened a real PR (#32); re-running it opens another every suite run. DoD 1's clause \"a test that makes the decide take longer than the transport bound\" is satisfiable deterministically with fake timers at component level.\n\nTHE FIX'S OWN FAILURE MODE, which DoD 2 exists to catch and which step 5's second drill hunts: treating a failed read as \"pending\" is right for a transport abort and WRONG for a daemon that answered with a refusal. Step 4(a) asserts a delivered `RELEASE_EVIDENCE_INCOMPLETE` still renders VERBATIM with its layer; drill (ii) makes the guard swallow a delivered refusal and requires that arm to go RED. The framing to keep: this does not make a transport abort mean \"success\" — it makes it mean \"unknown, still waiting\", which is what it always actually meant.\n\nGATE NOTE: `pnpm test` does NOT cover this row at all — the root vitest include has no `apps/**`. `pnpm --filter @moe/control-room test` is the only leg exercising these files; quoting a green root count as evidence would be a false completion claim.",
      "timestamp": "2026-09-08T17:45:23.732Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-08T17:11:09.286Z",
  "updatedAt": "2026-09-08T17:45:23.732Z",
  "metrics": {
    "firstClaimAt": "2026-09-08T17:39:59.296Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 2
  },
  "stepsCompleted": [],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-08T17:44:59.661Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED, and this is the one the description suggests first — raise `DEFAULT_REQUEST_TIMEOUT_MS` or thread a longer `requestTimeoutMs` for `release.decide`. Three reasons, in order of weight: (1) that constant is the shared round-trip bound for EVERY command kind in the control room, so a change there is a change to all of them; (2) a real `gh pr create` followed by a `gh pr view` re-read has no upper bound the browser can know in advance, so any number chosen to cover the observed run re-arms for the first slower remote — the same \"bound calibrated against a moving quantity\" failure this board has already seen twice this week; (3) it does not even reach the read path, whose 15s is hard-coded at live-effect-read.ts:70 with no option to supply. Waiting longer also leaves the operator staring at a spinner with no evidence either way, where recovering gives them the link.\n\nREJECTED — make `release.decide` return promptly and let the card poll (the description's second shape). It is the better long-run architecture, but it is a daemon command-semantics change: the decide's answer currently IS the receipt, and making it async means a new in-flight state on the wire, a new read to poll, and every other consumer of that answer updated. That is a several-row change and this row is a bug fix on the operator's side.\n\nREJECTED — change `useEffectRead` first. It backs several cards, and the source says it already polls. Step 3 permits touching it ONLY if step 1 measures that the poll does not fire, and requires the worker to say what latched.\n\nREJECTED — assert the fix through the live browser lane. The live drive already opened a real PR (#32); re-running it on every suite run opens another. DoD 1's clause — \"a test that makes the decide take longer than the transport bound\" — is satisfiable with fake timers at the component level, which is faster, deterministic, and does not have side effects on github.com.",
    "codebaseInsights": "THE FILING'S CENTRAL MECHANISM CLAIM IS CONTRADICTED BY THE SOURCE, and the worker must settle it before fixing anything. Both the description and the live spec comment (tests/e2e/control-room/release-approval-live.spec.ts:315) say the card lands on `cr.release.read-refusal` \"which `useEffectRead` never retries out of\". Read apps/control-room/src/v2/components/use-effect-read.ts:\n- :19 `const timer = setInterval(refresh, pollMs)` with `pollMs = 5_000` — a five-second POLL, not a one-shot read.\n- :12-16 the failure path is `.catch(() => failure).then((value) => { setFrame({ read, value }); busy = false; ... })` — an aborted read stores the failure AND clears `busy`. Nothing latches.\n- deps are `[read, failure, pollMs]`; in live-goal-release.tsx `read` is a `useMemo` over `[setup, goalId]` (:32) and `failure` is a module constant (:23-25), so the effect is not torn down between polls.\nOn the source the hook retries every 5s. Either the claim is wrong, or a different thing latches.\n\nTHE OTHER LATCH CANDIDATE, which produces the same operator-visible symptom by a different route: live-goal-release.tsx:44 computes `offered = releaseOffer(frame, goalId) !== null` from the BOARD frame, and :46-50 renders the error note ONLY when `offered`, else returns null. After a RELEASED receipt the daemon stops offering `release.decide` (that is the parent row's DoD 3), so the card can also DISAPPEAR rather than show the link. Step 1 records which the operator actually saw.\n\nEITHER WAY THE ROW HAS A REAL DEFECT AND THE SAME FIX, which is why this plan is decisive despite the open question: the card reports a FAILURE about a command that is succeeding. `cr.release.read-refusal` says \"The release evidence could not be read right now\" while the daemon is opening the pull request. A client-side timeout is not evidence the command did not happen — the transport's own comment at client-transport.ts:103-106 says `delivered` separates \"a daemon that refused\" from \"a daemon that never answered\", and a 15s abort is the second, which means UNKNOWN, not failed.\n\nTHE TWO BOUNDS, both real and both 15s: packages/control-room-client/src/client-transport.ts:118 `DEFAULT_REQUEST_TIMEOUT_MS = 15_000` on the COMMAND path, and apps/control-room/src/live/live-effect-read.ts:70 `AbortSignal.timeout(15_000)` hard-coded on the READ path. They are different transports — the read does not go through `ControlRoomTransport` at all, it is a bare `fetch` in `readEffect`. Note that the read bound takes no option, so \"supply requestTimeoutMs for the release path\" would not even reach it.\n\nTHE SEAM FOR THE FIX ALREADY EXISTS. live-goal-release.tsx:34-43 wraps `createReleasePort(setup)` in a `useMemo` whose `submit` already intercepts every call to fire `refresh()`. That wrapper is where an outstanding-submit state belongs. `createReleasePort` (release-port.ts:25-33) is a frozen one-method port over `spendOffer` and does not need to change.\n\nTEST SURFACE: `apps/control-room/src/v2/goals/live-goal-release.test.tsx` does NOT exist and is this row's to create. `apps/control-room/src/v2/components/use-effect-read.test.tsx` DOES exist — read it for the fake-timer idiom before writing step 2.",
    "risks": "1. THE FIX'S OWN FAILURE MODE IS SWALLOWING A REAL REFUSAL. Treating a failed read as \"pending\" is right for a transport abort and WRONG for a daemon that answered with a refusal. DoD 2 exists for exactly this, and step 4(a) is written to hunt it: a delivered `RELEASE_EVIDENCE_INCOMPLETE` must still render VERBATIM with its layer. Drill (ii) in step 5 confirms the guard is scoped.\n\n2. A WAITING STATE THAT NEVER CLEARS IS THE SAME BUG IN A FRIENDLIER WORD. Whatever marks the submit outstanding must clear on the daemon's answer, on a successful read, and on unmount. Name what clears it in a comment.\n\n3. DOUBLE CONFIRM. The arm/confirm control is two clicks; if the operator confirms again while a submit is outstanding, does a second `release.decide` go out? The adversarial pass asks this explicitly.\n\n4. `pnpm test` DOES NOT COVER THIS ROW AT ALL — the root vitest include has no `apps/**`. `pnpm --filter @moe/control-room test` is the only leg that exercises these files. Quoting a green root count as evidence would be a false completion claim.\n\n5. THE NEW TEST FILE IS UNTRACKED, so `git diff` is blind to it during drills — back it up out of tree and prove restoration by SHA256 (global rail 13). This is where unrestored mutants ride into commits wearing a clean diff.\n\n6. SHARED CHECKOUT: peer work is currently dirty under apps/control-room/src/v2/ops/ (activity-screens.tsx and its test). Not this row's, not a stop condition, never to be reverted or committed.",
    "keyFiles": [
      "apps/control-room/src/v2/goals/live-goal-release.tsx",
      "apps/control-room/src/v2/components/use-effect-read.ts",
      "packages/control-room-client/src/client-transport.ts",
      "apps/control-room/src/live/live-effect-read.ts",
      "tests/e2e/control-room/release-approval-live.spec.ts"
    ]
  },
  "planApprovedAt": "2026-09-08T17:45:01.818Z"
}