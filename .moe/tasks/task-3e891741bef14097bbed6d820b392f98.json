{
  "id": "task-3e891741bef14097bbed6d820b392f98",
  "epicId": "epic-e0c54bddb3004bf29e98b2a43cea2dc2",
  "title": "Seal the Foundation context manifest record",
  "description": "Prerequisite for task-203a5ca7aada4375a98a95df8773e249; hard-depend on task-c7f2530b4a4445398a5e572f0568ea1d. @moe/context's manifest digest binds its seven render fields and exactBytes, but not project/session/node/attempt/graph/config/input provenance and it has no durable decoder. Define the daemon-owned canonical authority envelope and strict codec without changing the context package.",
  "definitionOfDone": [
    "One exact versioned FoundationContextManifestRecord binds projectId, sessionId, nodeKey, attemptRef, graphRevisionRef, graphContentHash, graphEpoch, configurationDigest, inputManifestDigest, the complete public ContextRenderManifest, and an outer recordDigest over every field; all ids/digests/integers/keys are bounded and deeply frozen.",
    "The encoder accepts only descriptor-safe exact-key plain data, requires manifest.version `context-manifest.v1`, byte values 0..255 and nonempty exactBytes, recomputes `digestContextManifest(manifest.binding)`, and derives rather than trusts recordDigest.",
    "The decoder uses bounded canonical bytes, distinguishes exact MALFORMED, VERSION_UNSUPPORTED, MANIFEST_DIGEST_MISMATCH, RECORD_DIGEST_MISMATCH and NONCANONICAL codes at a frozen FOUNDATION_CONTEXT_CODEC layer, re-encodes and byte-compares before returning authority.",
    "Focused tests pin the complete code/key/version roster, hostile accessors/proxies/unknown keys/oversize/truncation/negative-zero cases with a positive generated count, one accepted real renderContext control, and mutations hashing a reference or omitting an outer binding field that redden named assertions.",
    "Context and daemon package tests/typecheck execute separately with nonzero counts; completion records the ledger child of task-203a5ca7aada4375a98a95df8773e249 as consumer."
  ],
  "taskRails": [
    "Own only apps/daemon/src/work/foundation-context-manifest-codec.ts, its exact one-line LF .js bridge, and its focused test; production source <=250 lines.",
    "Use public @moe/context `digestContextManifest`; never copy its canonical digest, accept a caller digest/reference, or claim its inner digest binds outer authority fields.",
    "No store, launch, registry, graph reader, or provider behavior in this codec task; exact stable code/layer assertions and explicit owned-path commit."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "TDD the roster and the record shape. Create `apps/daemon/src/work/foundation-context-manifest-codec.test.ts` RED. Pin BY HAND, transcribed rather than derived from production, three things DoD 4 calls the \"complete roster\": the five decoder codes (MALFORMED, VERSION_UNSUPPORTED, MANIFEST_DIGEST_MISMATCH, RECORD_DIGEST_MISMATCH, NONCANONICAL), the single frozen layer `FOUNDATION_CONTEXT_CODEC`, and the exact outer key set — projectId, sessionId, nodeKey, attemptRef, graphRevisionRef, graphContentHash, graphEpoch, configurationDigest, inputManifestDigest, manifest, recordDigest. Assert the code list length equals 5 and every member is distinct; a derived roster would grow on both sides and stay green. Also pin the version literal `context-manifest.v1` — import `CONTEXT_MANIFEST_VERSION` from the `@moe/context` ROOT and assert the transcribed string equals it, so the constant and the expectation are checked against each other rather than one being copied from the other.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "note": "REOPEN pass (reopenCount 1). Step-1 landed already and QA did NOT fault it — re-verified on disk at HEAD fbbabda, not re-written.\n\nHand-transcribed roster present at test:44-68 (EXPECTED_CODES 5 entries, EXPECTED_KEYS 11 entries, EXPECTED_CONTEXT_MANIFEST_VERSION \"context-manifest.v1\"), asserted at test:167-204: sorted-equality against the production lists, toHaveLength(5)/(11), Set-size distinctness on both, Object.isFrozen on both, FOUNDATION_CONTEXT_CODEC literal, and EXPECTED_CONTEXT_MANIFEST_VERSION toBe(CONTEXT_MANIFEST_VERSION) imported from the @moe/context ROOT so the constant and the expectation check each other rather than one being copied.\n\nBaseline before touching anything: npx vitest run --root . --config package.json src/work/foundation-context-manifest-codec.test.ts -> EXIT=0, Test Files 1 passed, Tests 50 passed. Count line read, not just the exit code.",
      "startedAt": "2026-08-18T04:21:27.727Z",
      "completedAt": "2026-08-18T04:21:44.089Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD the encoder's admission rules, each as its own arm with its exact code and the `FOUNDATION_CONTEXT_CODEC` layer, and each asserting NOTHING is returned as authority. (a) `manifest.version` other than `context-manifest.v1` -> VERSION_UNSUPPORTED, kept separate from MALFORMED because a knowingly-declined real shape and a shape that is not what it claims lead to opposite operator actions. (b) An `exactBytes` entry outside 0..255, a non-integer, or a NEGATIVE ZERO -> MALFORMED. Negative zero is named explicitly in DoD 4 and is the subtle one: `JSON.stringify(-0)` is `\"0\"`, so a canonical round-trip silently normalises it and a naive equality check never sees the difference — detect it with `Object.is(value, -0)`, and apply the same check to `graphEpoch`. (c) Empty `exactBytes` -> MALFORMED; the render manifest never legitimately carries zero bytes. (d) A `manifest.digest` that does not equal a fresh `digestContextManifest(manifest.binding)` -> MANIFEST_DIGEST_MISMATCH. (e) A caller-supplied `recordDigest` that disagrees with the derived one -> RECORD_DIGEST_MISMATCH; rail 2 forbids accepting a caller digest at all, so the encoder DERIVES and the mismatch is how that is proven rather than asserted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "note": "REOPEN pass. Step-2 landed already and QA did NOT fault it — re-verified on disk, not re-written. Every arm here already asserts its EXACT code with toBe (never toContain), the FOUNDATION_CONTEXT_CODEC layer, and via refusalOf() (test:156-163) that neither `record` nor `bytes` rides along on a refusal.\n\n(a) test:214 version \"context-manifest.v2\" -> toBe VERSION_UNSUPPORTED, plus test:222 asserting it is NOT MALFORMED.\n(b) test:225-236 exactBytes 256 / -1 / 12.5 / \"7\" -> toBe MALFORMED; test:238 NEGATIVE ZERO exactBytes, which first proves the hazard is real (JSON.stringify(-0)===\"0\", -0===0 true, Object.is(-0,0) false) then asserts MALFORMED; test:252 the same for graphEpoch; test:260 the paired POSITIVE-zero ACCEPTANCE so an implementation that just rejects zero cannot pass both negative arms.\n(c) test:265 empty exactBytes -> MALFORMED.\n(d) test:273 forged manifest.digest -> toBe MANIFEST_DIGEST_MISMATCH.\n(e) test:281 caller recordDigest \"f\"*64 -> toBe RECORD_DIGEST_MISMATCH, paired with test:291 asserting the accepted path returns the DERIVED digest.\n\nThe QA defect is confined to the step-3 sweep (test:353-360), which is the only place a code assertion was written as toContain. Fixed there, not here.",
      "startedAt": "2026-08-18T04:21:46.201Z",
      "completedAt": "2026-08-18T04:21:56.054Z"
    },
    {
      "stepId": "step-3",
      "description": "TDD the hostile-input sweep and the accepted control. HOSTILE, with a POSITIVE GENERATED COUNT asserted so a sweep that silently produced zero cases cannot pass: accessor (getter) properties on the outer record and on `manifest.binding`, a revoked Proxy, `__proto__` and other unknown keys, a null-prototype object, oversize payloads past the bounded-decode limit, and truncated bytes. Each must refuse with its exact code and layer; none may let a value survive into the returned record. Reuse `exactKeys`, `isRecord` and `snapshotFoundationValue` from `./foundation-attempt-codec.js` — the descriptor-safe helpers already exist in this directory and a second implementation would drift from them. THE ACCEPTED CONTROL, which DoD 4 requires and which a refusal-only suite is blind without: build a REAL `AdmittedContextSelection`, run the REAL `selectContext` then the REAL `renderContext` from the `@moe/context` root, feed the resulting `ContextRenderManifest` into the encoder with plausible outer identities, and assert the encode succeeds and the decode returns a deeply frozen record whose every field round-trips. Do not hand-forge the manifest — the whole point is that a genuine renderContext output is admitted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "note": "THE QA DEFECT, FIXED HERE. Test-only change; production untouched (sha256 1fddeda6... unchanged, git status clean for the .ts).\n\nWHAT WAS WRONG. test:357 asserted `expect(FOUNDATION_CONTEXT_MANIFEST_CODES).toContain(result.code)` — \"it refused with one of my five codes\". Every guard already answers with one of the five, so that assertion was free and both of QA's drills survived 50/50.\n\nTHE FIX. HOSTILE_CASES is now a 3-tuple `[name, builder, FoundationContextManifestCode]` and the sweep asserts `expect(result.code).toBe(expected)`. Also added three structurally WELL-FORMED attacks so the expected-code column is genuinely non-constant rather than eleven copies of MALFORMED: a manifest claiming \"context-manifest.v0\" -> VERSION_UNSUPPORTED; a REAL binding from a second renderContext swapped in under the original digest -> MANIFEST_DIGEST_MISMATCH; an outer identity (projectId) swapped under its now-stale digest -> RECORD_DIGEST_MISMATCH. Count assertion 11 -> 14, plus a hand-written assertion that the distinct expected codes are exactly those four (NONCANONICAL is decoder-only and keeps its own arm) and that every one is a roster member.\n\nDRILLS, both of QA's, each restored by CONTENT to the snapshot hash:\n D1 codec.ts:182 MALFORMED -> NONCANONICAL: was 50/50 GREEN, now 6 FAILED / 47 passed. Reddened arms: revoked proxy as the record, an extra unknown key, a missing key, an array instead of a record, null instead of a record, a string instead of a record — exactly the exactKeys() path.\n D2 codec.ts:184 MALFORMED -> VERSION_UNSUPPORTED: was 50/50 GREEN, now 5 FAILED / 48 passed. Reddened arms: accessor on the outer record, accessor on manifest.binding, revoked proxy as the manifest, a null-prototype record, an oversize manifest past the bounded limit — every hostile class DoD 4 names, and the exact substitution test:222 forbids.\nThe two drills redden DISJOINT arm sets, which is the proof the sweep now discriminates per guard rather than per outcome.\n\nSuite after the fix: 53 passed (was 50), EXIT=0. Restored file hash 1fddeda6612d48d150ecf368bffd98117850fea61d048372bb26a88b22b66124, `git status --porcelain` on it prints nothing.\n\nAccepted control (unchanged, still green): the REAL selectContext -> renderContext chain feeds the encoder, and the decode returns a deeply frozen round-tripping record.",
      "startedAt": "2026-08-18T04:22:38.729Z",
      "completedAt": "2026-08-18T04:24:14.751Z"
    },
    {
      "stepId": "step-4",
      "description": "TDD the property that the outer digest binds EVERY outer field, which is the real deliverable and the one DoD 4's mutation clause targets. Write a per-field sweep over the ten non-digest outer fields (projectId, sessionId, nodeKey, attemptRef, graphRevisionRef, graphContentHash, graphEpoch, configurationDigest, inputManifestDigest, manifest): for each, mutate that field alone, re-derive, and assert `recordDigest` CHANGES. Assert the generated case count is exactly 10 and greater than zero. This is what makes \"an outer binding field was omitted from the digest\" a red test rather than an invisible defect — omitting `graphEpoch` from the hashed input leaves every other assertion green. Separately assert the inner and outer digests are independent: changing an outer identity must NOT change `manifest.digest`, and changing `manifest.binding` must change BOTH. Rail 2 is explicit that the inner digest does not bind outer authority fields, so this pair is the executable form of that rule.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "note": "REOPEN pass. Step-4 landed already and QA did NOT fault it — re-verified on disk (test:495-569), not re-written.\n\nPER-FIELD, NOT A LUMP. FIELD_VARIATIONS (test:504-515) carries one variation per non-digest outer field; test:529 mutates THAT FIELD ALONE, first asserting the variation is actually a change (`expect(varied[field]).not.toEqual(base[field])`) and then that the derived digest moves. The `manifest` arm varies through a genuinely different renderContext output (otherManifest()), not a null.\n\nTHE SWEEP CANNOT SILENTLY SHRINK. test:517 asserts length > 0, length === 10, AND that the swept field names equal `FOUNDATION_CONTEXT_RECORD_KEYS` minus recordDigest — tied to the PRODUCTION roster, so an eleventh outer field added later cannot be left unswept and unbound while every arm stays green.\n\nSTABILITY ARM (test:540): the same fields derive the same digest twice. Without it a deriver returning a random value would satisfy every \"the digest changed\" arm while binding nothing.\n\nINDEPENDENCE PAIR, rail 2 made executable: test:547 an outer identity change (projectId + graphEpoch) leaves manifest.digest identical while the OUTER digest moves; test:560 a manifest.binding change moves BOTH.\n\nSuite green at 53 after the step-3 fix; drill (ii) in step 7 is what proves this sweep is per-field rather than ten names on one assertion.",
      "startedAt": "2026-08-18T04:24:18.508Z",
      "completedAt": "2026-08-18T04:24:33.822Z"
    },
    {
      "stepId": "step-5",
      "description": "Implement `apps/daemon/src/work/foundation-context-manifest-codec.ts` plus its bridge `foundation-context-manifest-codec.js`. THE BRIDGE IS ONE LINE, `export * from \"./foundation-context-manifest-codec.ts\";`, and rail 1 says LF exactly — every sibling bridge in this directory is LF and a CRLF written by a Windows editor is a byte difference in a tracked file that no typecheck or vitest run will surface. Verify the committed bytes rather than trusting the editor. Exports: the frozen record version, the frozen five-code list, the `FOUNDATION_CONTEXT_CODEC` layer, the record type, `encodeFoundationContextManifestRecord` and `decodeFoundationContextManifestRecord`. Compose the existing helpers from `./foundation-attempt-codec.js` — `exactKeys`, `encodeFoundationPayload`, `decodeFoundationPayload`, `sameBytes`, `sha256Hex`, `isRecord` — rather than writing a second canonical layer. The decoder does the same byte discipline its siblings do: bounded decode, then RE-ENCODE and byte-compare against the input bytes before returning authority, answering NONCANONICAL when they differ (`readStoredFoundationAttempt` at foundation-attempt-store.ts:132-148 is the template, and it keeps ABSENT/AMBIGUOUS/DRIFT distinct for the same reason this codec keeps its five distinct). `digestContextManifest` comes from the `@moe/context` ROOT and is never reimplemented. Everything returned is deeply frozen. Target <=250 physical lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.ts",
        "apps/daemon/src/work/foundation-context-manifest-codec.js"
      ],
      "newFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.ts",
        "apps/daemon/src/work/foundation-context-manifest-codec.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.ts",
        "apps/daemon/src/work/foundation-context-manifest-codec.js"
      ],
      "note": "REOPEN pass. Production landed already and QA explicitly verified it green — NOT re-written, and the QA fix was test-only. Re-measured at HEAD fbbabda:\n\nBRIDGE BYTES, read not trusted: `od -c` shows `export * from \"./foundation-context-manifest-codec.ts\";\\n` — 56 bytes, terminated by a bare \\n, no \\r anywhere. Re-checked AFTER both mutation drills restored (the restore is `cp` from a byte snapshot, never git checkout, so no editor could reintroduce CRLF).\n\nPRODUCTION FILE: 250 physical lines, at the rail target. sha256 1fddeda6612d48d150ecf368bffd98117850fea61d048372bb26a88b22b66124; `git status --porcelain` on it prints nothing, i.e. byte-identical to HEAD.\n\nCOMPOSITION, not a second canonical layer: exactKeys / snapshotFoundationValue / encodeFoundationPayload / decodeFoundationPayload / sameBytes / isRecord all imported from ./foundation-attempt-codec.js. `digestContextManifest` and CONTEXT_MANIFEST_VERSION come from the @moe/context ROOT bare specifier and are never reimplemented; `git diff --numstat HEAD -- packages/context/` is empty, so rail 1/3 hold.\n\nDECODER BYTE DISCIPLINE (codec.ts:233-250): bounded decode, full re-admission through the encoder, then sameBytes() against the INPUT bytes before any authority is returned; a mismatch answers NONCANONICAL. Same shape as readStoredFoundationAttempt.\n\nONE DEVIATION FROM THE SIBLING TEMPLATE, deliberate and commented at codec.ts:210-222: `plainify()`. The bounded JSON parser builds objects with Object.create(null) (bounded-json-parser.ts:87) and the shared descriptor-safe copyValue refuses any non-array object whose prototype is not Object.prototype, so this codec could not otherwise re-admit its OWN output. plainify is a prototype adapter over bytes this module produced — it changes no key, value or ordering, and a null-prototype object arriving from a CALLER is still refused, which the \"a null-prototype record\" hostile arm pins (and drill D2 reddened).",
      "startedAt": "2026-08-18T04:24:36.231Z",
      "completedAt": "2026-08-18T04:24:53.871Z"
    },
    {
      "stepId": "step-6",
      "description": "Verification gate, four legs, exit codes captured SEPARATELY — DoD 5 says \"execute separately with nonzero counts\", and an `&&` chain that dies at a typecheck leg hides every later count line: `pnpm --filter @moe/context typecheck`, `pnpm --filter @moe/context test`, `pnpm --filter @moe/daemon typecheck`, `pnpm --filter @moe/daemon test`. Both names resolve (`@moe/context` is packages/context, `@moe/daemon` is apps/daemon, each declaring both scripts — verified at plan time). READ THE OUTPUT: `pnpm --filter <pkg> <script>` prints \"No projects matched the filters\" and EXITS 0 for an absent package, so a leg with no `Test Files N passed` line is UNVERIFIED rather than passing; record the count for both test legs. The context legs matter even though rail 3 forbids editing that package — this codec calls its public `digestContextManifest`, so a context change would surface here first. The daemon suite can be red at baseline from foreign in-flight slices: attribute by the merge-base-versus-HEAD failing-path delta intersected with the three owned paths, disclose foreign red verbatim with exact failing lines, and never fabricate green.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "note": "Four gate legs run SEPARATELY at HEAD fbbabda after the step-3 fix. Exit codes captured with ${PIPESTATUS[0]} so a tail pipe cannot report its own status, and the COUNT LINE read on both test legs — an exit-0 leg with no count line would be UNVERIFIED, not passing.\n\nLEG 1 `pnpm --filter @moe/context typecheck` -> `$ tsc --project tsconfig.json`, EXIT=0.\nLEG 2 `pnpm --filter @moe/context test` -> `$ vitest run --root ../.. packages/context/src`, EXIT=0, **Test Files 5 passed (5), Tests 24 passed (24)**.\nLEG 3 `pnpm --filter @moe/daemon typecheck` -> `$ tsc --project tsconfig.json`, EXIT=0.\nLEG 4 `pnpm --filter @moe/daemon test` -> `$ vitest run --root . --config package.json src`, EXIT=0, **Test Files 130 passed (130), Tests 2808 passed (2808)**, 13.41s.\n\nEvery leg printed its resolved script line, so none hit the \"No projects matched the filters\" exit-0 trap. Both package names resolve by NAME: @moe/context is packages/context, @moe/daemon is apps/daemon.\n\nFOCUSED: `npx vitest run --root . --config package.json src/work/foundation-context-manifest-codec.test.ts` -> EXIT=0, Test Files 1 passed, **Tests 53 passed** (was 50 before this pass; +3 is the non-MALFORMED hostile arms added in step-3).\n\nCOUNT MOVEMENT, attributed. QA measured 2801 at HEAD 39a1b2c. My diff adds exactly 3. 2808 - 2801 - 3 = 4 tests arrived from the two foreign commits landed since (219076d and fbbabda, task-6d319381 import-event store port). NO RED anywhere, so there is nothing to attribute by merge-base-vs-HEAD delta and nothing foreign to disclose verbatim — the (2)-minus-(1) intersection with my owned paths is empty because set (2) is empty.\n\nThe context legs are not ceremonial: this codec calls the public `digestContextManifest`, so any change under packages/context surfaces here first. `git diff --numstat HEAD -- packages/context/` is empty.",
      "startedAt": "2026-08-18T04:25:00.632Z",
      "completedAt": "2026-08-18T04:25:37.874Z"
    },
    {
      "stepId": "step-7",
      "description": "Mutation drills and adversarial review, then stop. FOUR drills, each restored before the next, each naming the test that reddens — the first two are named by DoD 4 directly: (i) hash a REFERENCE instead of canonical bytes when deriving `recordDigest` (for example digest the object identity or a `[object Object]` coercion) — the per-field sweep from step 4 must redden, because two different records would then collide; (ii) OMIT one outer field from the hashed input — that field's arm in the step-4 sweep must redden while the other nine stay green, which is exactly what proves the sweep is per-field rather than a single lump assertion; (iii) delete the re-encode byte-comparison so non-canonical bytes decode — the NONCANONICAL arm must redden; (iv) accept the caller's `recordDigest` instead of deriving it — the RECORD_DIGEST_MISMATCH arm must redden. A drill that reddens the WRONG test, or hangs, is not a pass; record the exact failing test names. Drills run in a SHARED worktree — snapshot each file's hash and restore by CONTENT, never `git checkout`, and re-verify the `.js` bridge is still LF after any restore. Then run `adversarial-self-review`: a manifest whose `exactBytes` is at the bounded-decode ceiling, a `graphEpoch` of 0 versus -0, duplicate keys in the encoded JSON, and the production file's physical line count against the 250 target.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.ts",
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-context-manifest-codec.test.ts",
        "apps/daemon/src/work/foundation-context-manifest-codec.ts",
        "apps/daemon/src/work/foundation-context-manifest-codec.js"
      ],
      "note": "SEVEN DRILLS, each restored by CONTENT (cp from a byte snapshot, never git checkout) and each verified back to sha256 1fddeda6612d48d150ecf368bffd98117850fea61d048372bb26a88b22b66124 before the next. Baseline 53/53.\n\n(i) HASH A REFERENCE — codec:146 `encodeFoundationPayload({ ref: String(bound) })`: 13 FAILED / 40 passed. All ten per-field arms + both independence arms + \"an outer identity swapped under its now-stale recordDigest\" (every record collides on \"[object Object]\", so the stale digest matches and the forgery is admitted).\n(ii) OMIT ONE OUTER FIELD — codec:145 skips graphEpoch: **1 FAILED / 52 passed**, and the one is \"moves the recordDigest when graphEpoch alone changes\". Exactly one arm, which is the proof the sweep is per-field rather than one lump assertion wearing ten names.\n(iii) DELETE THE RE-ENCODE BYTE COMPARE — codec:246 drops `!sameBytes(...)`: 1 FAILED, \"refuses non-canonical bytes that still parse to the same record\".\n(iv) ACCEPT THE CALLER DIGEST — codec:195 compares the derived digest to itself: 2 FAILED, \"refuses a caller recordDigest that disagrees with the derived one\" and the new stale-digest hostile arm.\n(D1) codec:182 MALFORMED->NONCANONICAL: 6 FAILED (exactKeys path). WAS 50/50 GREEN before this pass — QA's first surviving mutant, now killed.\n(D2) codec:184 MALFORMED->VERSION_UNSUPPORTED: 5 FAILED (both accessors, revoked-proxy-as-manifest, null-prototype record, oversize). WAS 50/50 GREEN — QA's second surviving mutant, now killed. D1 and D2 redden DISJOINT arm sets, so the sweep discriminates per guard, not per outcome.\n(v) EXTRA, mine — codec:154 VERSION_UNSUPPORTED->MALFORMED: 2 FAILED, the dedicated test:214 AND the new sweep arm \"a manifest claiming a version this codec never issued\". Proof the added non-MALFORMED arms are load-bearing rather than decorative.\n\nADVERSARIAL REVIEW, the four items the step names, measured not reasoned. Throwaway probe file, run once and deleted in the same command; `git status --untracked-files=all` over apps/daemon/src/work/ shows no untracked file.\n- exactBytes AT THE CEILING: n=511 and n=512 pass the shape guard (refused later by the probe's deliberately fake digest, i.e. MANIFEST_DIGEST_MISMATCH); n=513 -> FOUNDATION_CONTEXT_MALFORMED. MAX_ITEMS=512 in foundation-attempt-codec.ts:13 binds `binding.exactBytes`. A REAL rendered context is thousands of bytes, so this codec will refuse to seal one. OUTSIDE owned paths, needs its own task, and posted as a comment to the consumer.\n- DUPLICATE KEYS in the encoded JSON -> FOUNDATION_CONTEXT_MALFORMED. The bounded parser refuses first, so the operator-visible code is MALFORMED, not NONCANONICAL. Disclosed rather than assumed.\n- graphEpoch 0 vs -0: -0 refused MALFORMED (test:252), +0 ACCEPTED (test:260) — the pair is what stops \"rejects zero outright\" from passing.\n- PRODUCTION LINE COUNT: 250, at the target.\n\nConcurrency/IO/cleanup items from the checklist do not apply: this module is a pure codec with no file handles, sockets, subprocesses, shared mutable state or retry path. Everything returned is deeply frozen; nothing rides along on a refusal (asserted by refusalOf and by `not.toHaveProperty(\"record\")` on every sweep arm).\n\nINTEGRITY AFTER ALL DRILLS: bridge `od -c` still ends `\"; \\n` with no \\r, 56 bytes. Only the test file differs from HEAD. Focused suite 53/53.",
      "startedAt": "2026-08-18T04:28:27.913Z",
      "completedAt": "2026-08-18T04:29:21.405Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": "qa-a8258c2e",
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "REJECT - the hostile sweep does not assert the reason code, proven by two surviving mutants. Epic rail 6, and plan step 3 (\"Each must refuse with its exact code and layer\").\n\nEVERYTHING ELSE VERIFIED GREEN. Four gate legs re-run by me, separately, at HEAD 39a1b2c: @moe/context typecheck EXIT=0; @moe/context test EXIT=0, Test Files 5 passed, Tests 24 passed; @moe/daemon typecheck EXIT=0; @moe/daemon test EXIT=0, Test Files 130 passed, Tests 2801 passed (worker reported 2799; foreign in-flight tests). Focused: 1 file / 50 tests. Owned paths clean and byte-identical to HEAD; the .js bridge is exactly 56 bytes and LF, verified with od -c; production is 250 physical lines; packages/context untouched. No commit bears this task id - NOT a rejection reason (global rail 5); base-ref diff over owned paths is 250/0, 528/0, 1/0, all additions.\n\nTHE DEFECT. test:353-360 asserts toContain(result.code) over this codec's own five-code roster, i.e. \"it refused with one of my codes\". All 11 hostile cases in fact refuse with FOUNDATION_CONTEXT_MALFORMED, so the exact assertion was free. Drills, each restored byte-identical:\n D1  codec.ts:182 MALFORMED -> NONCANONICAL        => 50/50 PASSED (survives)\n D2  codec.ts:184 MALFORMED -> VERSION_UNSUPPORTED => 50/50 PASSED (survives)\n CTL codec.ts:121 delete !Object.is(value,-0)      => 2 FAILED, both negative-zero tests by name\nThe control proves the harness detects real regressions, so D1/D2 surviving is a hole in the assertions, not a broken drill. D2 is the sharp one: line 184 refuses both accessor cases, revoked-proxy-as-manifest, the null-prototype record and the oversize case - every hostile class DoD 4 names - while line 222 elsewhere asserts VERSION_UNSUPPORTED must never stand in for MALFORMED.\n\nDoD 5's consumer clause is also unrecorded on the board; see issues.\n\nNOT rejection reasons, do not \"fix\": the 512-item exactBytes ceiling (outside owned paths, correctly disclosed), task-level LOC, the missing task-id commit.",
  "createdBy": "WORKER",
  "parentTaskId": "task-203a5ca7aada4375a98a95df8773e249",
  "priority": "CRITICAL",
  "order": 80.03,
  "comments": [
    {
      "id": "comment-f36ac868a8864024ba2caf713a7625d5",
      "author": "worker-b8af1a87",
      "content": "CONSUMER OF RECORD (DoD 5, and QA issue 3 — this id was previously only in a Serena memory, never on the task record).\n\nConsumer: **task-22fa35a59f204777b4db56f7516ef57b** — \"Commit the Foundation context manifest before launch\", status WORKING. Verified on disk, not from memory: its description reads \"Prerequisite for task-203a5ca7aada4375a98a95df8773e249; hard-depend on task-3e891741bef14097bbed6d820b392f98\", so it is exactly the ledger child DoD 5 names and it hard-depends on this codec.\n\nSecond downstream reader: task-225d25f7e58a401b8b414dd69ef3ab60.\n\nDISCLOSURE THE CONSUMER NEEDS BEFORE IT WRITES ANY REAL RECORD — measured this pass with a throwaway probe (deleted in the same command, nothing left on disk):\n  CEILING n=511 -> shape ADMITTED (refused later, at the digest check, by my probe's fake digest)\n  CEILING n=512 -> shape ADMITTED (same)\n  CEILING n=513 -> FOUNDATION_CONTEXT_MALFORMED\n`snapshotFoundationValue`'s MAX_ITEMS = 512 (foundation-attempt-codec.ts:13) applies to `manifest.binding.exactBytes`. A REAL rendered agent context is thousands of bytes, so this codec WILL refuse to seal it with MALFORMED. That is outside this task's owned paths (the constant lives in the sibling codec) and needs its own task; task-22fa35a5 is the one that hits it first with real input. My fixture renders ~70 bytes, so the suite never nears the ceiling — the limit is real and undetected by a green suite.\n\nAlso measured: a DUPLICATE KEY in the encoded JSON is refused FOUNDATION_CONTEXT_MALFORMED (the bounded parser rejects it before the re-encode byte-compare could answer NONCANONICAL). Refused either way, but the code an operator sees is MALFORMED, not NONCANONICAL.",
      "timestamp": "2026-08-18T04:28:25.939Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-17T17:46:29.449Z",
  "updatedAt": "2026-08-18T04:30:36.781Z",
  "metrics": {
    "firstClaimAt": "2026-08-18T03:34:28.014Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 14,
    "rejectCount": 1,
    "reopenCount": 1
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [
    {
      "item": "Focused tests pin the complete code/key/version roster, hostile accessors/proxies/unknown keys/oversize/truncation/negative-zero cases with a positive generated count, one accepted real renderContext control, and mutations hashing a reference or omitting an outer binding field that redden named assertions.",
      "rejectedAt": "2026-08-18T04:19:56.733Z",
      "rejectedBy": "qa-a8258c2e"
    },
    {
      "item": "Context and daemon package tests/typecheck execute separately with nonzero counts; completion records the ledger child of task-203a5ca7aada4375a98a95df8773e249 as consumer.",
      "rejectedAt": "2026-08-18T04:19:56.733Z",
      "rejectedBy": "qa-a8258c2e"
    }
  ],
  "planSubmittedAt": "2026-08-18T03:36:25.876Z",
  "planningNotes": {
    "approachesConsidered": "COMPOSE THE EXISTING DAEMON CODEC HELPERS RATHER THAN WRITE A SECOND CANONICAL LAYER. `apps/daemon/src/work/foundation-attempt-codec.ts` (194 lines) already exports `exactKeys`, `snapshotFoundationValue`, `encodeFoundationPayload`, `decodeFoundationPayload`, `sameBytes`, `sha256Hex`, `isRecord` and `textOf` — the exact descriptor-safe and canonical-byte primitives this codec needs, in the same directory. A parallel implementation would drift from them, and the drift would live in durable bytes where it cannot be corrected after the fact.\n\nTHE OUTER DIGEST IS DERIVED, NEVER ACCEPTED. Considered letting the encoder validate a caller-supplied `recordDigest` and pass it through. Rejected: rail 2 forbids accepting a caller digest at all. Deriving it and refusing RECORD_DIGEST_MISMATCH on disagreement is strictly stronger, and it turns \"the caller cannot supply authority\" into a tested behaviour rather than a comment.\n\nFIVE DISTINCT CODES, NOT ONE \"BAD BYTES\" CODE. MALFORMED, VERSION_UNSUPPORTED, MANIFEST_DIGEST_MISMATCH, RECORD_DIGEST_MISMATCH and NONCANONICAL answer five different operator questions and must never stand in for each other. This mirrors the sibling `readStoredFoundationAttempt`, which keeps ABSENT / AMBIGUOUS / DRIFT separate for the same reason, and the `@moe/import` contract's own note that \"the four DECODE codes are deliberately distinct rather than one bad-file code\".\n\nTHE PER-FIELD DIGEST SWEEP IS THE CENTRAL TEST, not a nicety. DoD 4's mutation clause (\"omitting an outer binding field\") is only detectable if each of the ten outer fields is mutated INDIVIDUALLY and asserted to move the digest. A single \"the digest changes when the record changes\" assertion stays green while nine of ten fields are unbound.",
    "codebaseInsights": "MEASURED AT HEAD a42ae2f. The premise is confirmed exactly.\n\nTHE INNER DIGEST REALLY DOES BIND ONLY SEVEN FIELDS. `digestContextManifest(binding)` (packages/context/src/context-render.ts:57-62) is `canonicalSha256({version: CONTEXT_MANIFEST_VERSION, binding})`, and `ContextDigestBinding` (:24-32) has exactly seven members: optionalSelection, journalCountLimit, journalTextLimit, ordering, rendererVersion, exclusions, exactBytes. No projectId, sessionId, nodeKey, attemptRef, graphRevisionRef, graphContentHash, graphEpoch, configurationDigest or inputManifestDigest appears anywhere in it. So the task's claim is precise: an OUTER envelope with its own digest is genuinely required, and it is daemon-owned because @moe/context must not change (rail 1 and 3).\n\n`ContextRenderManifest` (:34-38) is `{version: \"context-manifest.v1\", binding, digest}`. `CONTEXT_MANIFEST_VERSION` is declared at context-contract.ts:2 and is exported from the package root. `renderContext` (:64-83) returns `{bytes, manifest}` deep-frozen, with `exactBytes` being the rendered byte array — so a genuine manifest always has nonempty exactBytes, which is why the empty case is malformed rather than a legitimate edge.\n\nTHE ROOT BARREL IS FIVE `export *` LINES (index.ts: context-contract, context-render, context-selection, dead-end-journal, release-handoff), so a grep for `digestContextManifest` in index.ts returns NOTHING while the symbol is genuinely exported. Probe, do not grep — I confirmed by compiled probe and by plain-Node import that `selectContext`, `renderContext` and `digestContextManifest` all resolve from the bare specifier.\n\nTHE DEPENDENCY EDGE IS ALREADY LANDED, so this task plans no manifest work: `apps/daemon/package.json` declares `@moe/context` and `pnpm-lock.yaml:107-110` carries the importer link. That was task-c7f2530b, which measured complete-on-arrival.\n\nHELPERS TO COMPOSE, all in the owned directory's sibling: foundation-attempt-codec.ts exports `exactKeys` (:47), `encodeFoundationPayload` (:150), `decodeFoundationPayload` (:161), `sha256Hex` (:169), `sameBytes` (:171), `isRecord` (:187), `snapshotFoundationValue` (:43).\n\nTHE BYTE-DISCIPLINE TEMPLATE is `readStoredFoundationAttempt` (foundation-attempt-store.ts:132-148): decode, RE-ENCODE, byte-compare against the stored payload, and keep the failure modes as separate codes. Mirror that shape rather than inventing a fourth idiom in this package.\n\nGATES RESOLVE BY NAME: `@moe/context` is packages/context and `@moe/daemon` is apps/daemon, each with `typecheck` and `test`.",
    "risks": "1. NEGATIVE ZERO IS THE SUBTLE ONE, and DoD 4 names it deliberately. `JSON.stringify(-0)` is `\"0\"`, so a canonical encode/decode round-trip silently normalises it and every byte comparison still passes. Detect it with `Object.is(value, -0)` on `graphEpoch` and on every `exactBytes` entry; a `value === 0` check cannot see it, and neither can a digest comparison.\n2. THE PER-FIELD SWEEP MUST BE PER FIELD. A lump assertion that \"the digest changes when the record changes\" passes while nine of the ten outer fields are unhashed. Assert the case count is exactly 10, and require drill (ii) to redden exactly one arm.\n3. THE INNER DIGEST DOES NOT BIND OUTER FIELDS, and a plausible-looking shortcut is to reuse `manifest.digest` as the record's identity. That would let two records with different projectId or graphEpoch share an identity. The independence pair in step 4 exists to make that a red test.\n4. THE `.js` BRIDGE MUST BE LF and rail 1 says so explicitly. It is invisible to typecheck and vitest, and a drill restore can silently reintroduce CRLF — re-verify the committed bytes after every restore.\n5. DO NOT TOUCH packages/context. Rail 1 and rail 3 both forbid it, and rail 2 forbids copying its canonical digest. If `digestContextManifest` appears to need a change, that is a different task.\n6. A refusal-only suite is blind to a wrong ACCEPTED value. The accepted control must run the REAL `selectContext` -> `renderContext` chain; a hand-forged manifest would prove only that the codec accepts what the test happens to build.\n7. Reuse the sibling descriptor-safe helpers rather than writing new ones — a second `exactKeys` that admits an accessor while the original refuses it is a divergence living in durable bytes.\n8. Mutation drills run in a shared worktree; snapshot hashes and restore by content, never `git checkout`.",
    "keyFiles": [
      "packages/context/src/context-render.ts",
      "packages/context/src/context-contract.ts",
      "packages/context/src/context-selection.ts",
      "apps/daemon/src/work/foundation-attempt-codec.ts",
      "apps/daemon/src/work/foundation-attempt-store.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000
  },
  "planApprovedAt": "2026-08-18T03:36:27.959Z",
  "contextFetchedBy": [
    "worker-b8af1a87",
    "qa-a8258c2e",
    "architect-f956ffe1"
  ],
  "workStartedAt": "2026-08-18T03:45:02.617Z",
  "reviewStartedAt": "2026-08-18T04:30:07.082Z",
  "verification": {
    "command": "Four legs run SEPARATELY, each exit code captured on its own (no && chain, no shared pipeline): pnpm --filter @moe/context typecheck ; pnpm --filter @moe/context test ; pnpm --filter @moe/daemon typecheck ; pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": "EXIT1=0 (context typecheck: $ tsc --project tsconfig.json)\nEXIT2=0 (context test: $ vitest run --root ../.. packages/context/src) -> Test Files 5 passed (5), Tests 24 passed (24)\nEXIT3=0 (daemon typecheck: $ tsc --project tsconfig.json)\nEXIT4=0 (daemon test: $ vitest run --root . --config package.json src) -> Test Files 130 passed (130), Tests 2808 passed (2808)\n\nEvery leg printed its resolved script line, so none hit the \"No projects matched the filters\" exit-0 trap. Count lines read, not just exit codes.\n\nFOCUSED: npx vitest run --root . --config package.json src/work/foundation-context-manifest-codec.test.ts -> EXIT=0, Test Files 1 passed, Tests 53 passed (was 50; +3 non-MALFORMED hostile arms).\n\nCOUNT ATTRIBUTION: QA measured 2801 at 39a1b2c. Mine adds exactly 3. The remaining +4 arrived with foreign commits 219076d and fbbabda (task-6d319381). NO RED anywhere, so the merge-base-vs-HEAD failing-path delta is empty and there is no foreign red to disclose verbatim.\n\nCONSUMER (DoD 5): task-22fa35a59f204777b4db56f7516ef57b, \"Commit the Foundation context manifest before launch\", status WORKING. Verified on disk: its description reads \"Prerequisite for task-203a5ca7aada4375a98a95df8773e249; hard-depend on task-3e891741bef14097bbed6d820b392f98\", i.e. the ledger child DoD 5 names. Also recorded as comment-f36ac868 on this task. Second reader: task-225d25f7e58a401b8b414dd69ef3ab60.",
    "reportedAt": "2026-08-18T04:30:07.082Z"
  },
  "filesModified": [
    "apps/daemon/src/work/foundation-context-manifest-codec.test.ts",
    "apps/daemon/src/work/foundation-context-manifest-codec.ts",
    "apps/daemon/src/work/foundation-context-manifest-codec.js"
  ],
  "reviewCompletedAt": "2026-08-18T04:19:56.733Z",
  "rejectionDetails": {
    "failedDodItems": [
      "Focused tests pin the complete code/key/version roster, hostile accessors/proxies/unknown keys/oversize/truncation/negative-zero cases with a positive generated count, one accepted real renderContext control, and mutations hashing a reference or omitting an outer binding field that redden named assertions.",
      "Context and daemon package tests/typecheck execute separately with nonzero counts; completion records the ledger child of task-203a5ca7aada4375a98a95df8773e249 as consumer."
    ],
    "issues": [
      {
        "type": "test_failure",
        "file": "apps/daemon/src/work/foundation-context-manifest-codec.test.ts",
        "line": 357,
        "description": "Line 357 asserts toContain(result.code) over the codec's own 5-code roster - not the specific stable code (epic rail 6; step 3: 'Each must refuse with its exact code and layer'). Drill: codec.ts:182 MALFORMED -> NONCANONICAL leaves 50/50 GREEN, hiding a wrong code for revoked-proxy-as-record, extra key, missing key, array, null and string. Fix: carry an expected code per case (third tuple element) and assert result.code toBe it. All 11 cases are MALFORMED today."
      },
      {
        "type": "test_failure",
        "file": "apps/daemon/src/work/foundation-context-manifest-codec.test.ts",
        "line": 353,
        "description": "Same sweep, sharper: drill codec.ts:184 MALFORMED -> VERSION_UNSUPPORTED also leaves 50/50 GREEN. Line 184 is the path that refuses BOTH accessor cases, revoked-proxy-as-manifest, the null-prototype record and the oversize case - every hostile class DoD 4 names. Yet line 222 asserts VERSION_UNSUPPORTED must never stand in for MALFORMED, so the sweep admits the exact substitution a sibling test forbids. After fixing, re-run both drills and record which arms redden."
      },
      {
        "type": "other",
        "file": "apps/daemon/src/work/foundation-context-manifest-codec.ts",
        "description": "DoD 5: 'completion records the ledger child of task-203a5ca7... as consumer.' The consumer is task-22fa35a59f204777b4db56f7516ef57b ('Commit the Foundation context manifest before launch', PLANNING, hard-depends on this task), but that id appears nowhere on the task record - get_context shows comments: [] and no occurrence in verification.outputTail or any step note. It lives only in a Serena memory. Fix: record the id on the task itself."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "REJECT - the hostile sweep does not assert the reason code, proven by two surviving mutants. Epic rail 6, and plan step 3 (\"Each must refuse with its exact code and layer\").\n\nEVERYTHING ELSE VERIFIED GREEN. Four gate legs re-run by me, separately, at HEAD 39a1b2c: @moe/context typecheck EXIT=0; @moe/context test EXIT=0, Test Files 5 passed, Tests 24 passed; @moe/daemon typecheck EXIT=0; @moe/daemon test EXIT=0, Test Files 130 passed, Tests 2801 passed (worker reported 2799; foreign in-flight tests). Focused: 1 file / 50 tests. Owned paths clean and byte-identical to HEAD; the .js bridge is exactly 56 bytes and LF, verified with od -c; production is 250 physical lines; packages/context untouched. No commit bears this task id - NOT a rejection reason (global rail 5); base-ref diff over owned paths is 250/0, 528/0, 1/0, all additions.\n\nTHE DEFECT. test:353-360 asserts toContain(result.code) over this codec's own five-code roster, i.e. \"it refused with one of my codes\". All 11 hostile cases in fact refuse with FOUNDATION_CONTEXT_MALFORMED, so the exact assertion was free. Drills, each restored byte-identical:\n D1  codec.ts:182 MALFORMED -> NONCANONICAL        => 50/50 PASSED (survives)\n D2  codec.ts:184 MALFORMED -> VERSION_UNSUPPORTED => 50/50 PASSED (survives)\n CTL codec.ts:121 delete !Object.is(value,-0)      => 2 FAILED, both negative-zero tests by name\nThe control proves the harness detects real regressions, so D1/D2 surviving is a hole in the assertions, not a broken drill. D2 is the sharp one: line 184 refuses both accessor cases, revoked-proxy-as-manifest, the null-prototype record and the oversize case - every hostile class DoD 4 names - while line 222 elsewhere asserts VERSION_UNSUPPORTED must never stand in for MALFORMED.\n\nDoD 5's consumer clause is also unrecorded on the board; see issues.\n\nNOT rejection reasons, do not \"fix\": the 512-item exactBytes ceiling (outside owned paths, correctly disclosed), task-level LOC, the missing task-id commit.",
      "rejectedAt": "2026-08-18T04:19:56.733Z",
      "reopenCount": 1,
      "failedDodItems": [
        "Focused tests pin the complete code/key/version roster, hostile accessors/proxies/unknown keys/oversize/truncation/negative-zero cases with a positive generated count, one accepted real renderContext control, and mutations hashing a reference or omitting an outer binding field that redden named assertions.",
        "Context and daemon package tests/typecheck execute separately with nonzero counts; completion records the ledger child of task-203a5ca7aada4375a98a95df8773e249 as consumer."
      ],
      "issues": [
        {
          "type": "test_failure",
          "file": "apps/daemon/src/work/foundation-context-manifest-codec.test.ts",
          "line": 357,
          "description": "Line 357 asserts toContain(result.code) over the codec's own 5-code roster - not the specific stable code (epic rail 6; step 3: 'Each must refuse with its exact code and layer'). Drill: codec.ts:182 MALFORMED -> NONCANONICAL leaves 50/50 GREEN, hiding a wrong code for revoked-proxy-as-record, extra key, missing key, array, null and string. Fix: carry an expected code per case (third tuple element) and assert result.code toBe it. All 11 cases are MALFORMED today."
        },
        {
          "type": "test_failure",
          "file": "apps/daemon/src/work/foundation-context-manifest-codec.test.ts",
          "line": 353,
          "description": "Same sweep, sharper: drill codec.ts:184 MALFORMED -> VERSION_UNSUPPORTED also leaves 50/50 GREEN. Line 184 is the path that refuses BOTH accessor cases, revoked-proxy-as-manifest, the null-prototype record and the oversize case - every hostile class DoD 4 names. Yet line 222 asserts VERSION_UNSUPPORTED must never stand in for MALFORMED, so the sweep admits the exact substitution a sibling test forbids. After fixing, re-run both drills and record which arms redden."
        },
        {
          "type": "other",
          "file": "apps/daemon/src/work/foundation-context-manifest-codec.ts",
          "description": "DoD 5: 'completion records the ledger child of task-203a5ca7... as consumer.' The consumer is task-22fa35a59f204777b4db56f7516ef57b ('Commit the Foundation context manifest before launch', PLANNING, hard-depends on this task), but that id appears nowhere on the task record - get_context shows comments: [] and no occurrence in verification.outputTail or any step note. It lives only in a Serena memory. Fix: record the id on the task itself."
        }
      ]
    }
  ]
}