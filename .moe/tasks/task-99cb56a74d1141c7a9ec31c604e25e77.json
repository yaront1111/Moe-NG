{
  "id": "task-99cb56a74d1141c7a9ec31c604e25e77",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Server-obtainable quoted runtime observation for Claude pin requests",
  "description": "MEASURED GAP (2026-08-16, at commit 66ae6ef, by grep + a real Windows run). createClaudeRuntimePinRequest (task-75ee4a84) mints fs/facts/clock from three plain data fields, but NOTHING in the shipped public surface can produce the `quotedObservation` it validates against, so no consumer can reach a PROVEN Claude launch.\n\nSymbol by symbol, all in packages/runner/src/surface/claude-surface.ts:85-92 (explicitly WITHHELD): `observeInstalledClaudeRuntime`, `probeClaudeRuntime`, `createNodeClaudeRuntimeFs`, `ClaudeRuntimeFactsPort`. Also internal: `capabilitySchemaDigestOf` (packages/runner/src/providers/claude/claude-capabilities.ts:273) and `canonicalDigest`. Public `buildProviderRuntimeObservation` accepts caller-claimed `reportedVersion` and `adapterCapabilitySchemaDigest`, and prepareClaudeRuntimePin compares exactly those two plus platformIdentity (claude-runtime-pin.ts factsDigest, lines 130-141).\n\nREPRODUCED on real Windows 10.0.26200 through apps/daemon/src/work/foundation-attempt-windows.test.ts against the genuinely installed claude.exe 2.1.233: a self-assembled quote whose closure hash and paths are all TRUE is still refused CLAUDE_RUNTIME_OBSERVATION_CHANGED@RUNTIME, because the capability schema digest and reported version can only come from an observation the runner made itself. A stand-in binary refuses CLAUDE_RUNTIME_OBSERVATION_INVALID@RUNTIME after the shipped broker really ran its --version probe.\n\nConsequence: task-6cbff010's dispatch service composes the real launcher correctly and persists honest SUSPECT/UNKNOWN advisories, but the PROVEN branch (grant consumed -> PREFLIGHT/STARTED registration -> result manifest) cannot be exercised end to end by any consumer, and MUST NOT be faked by reimplementing the digest authority in a test.\n\nNEEDED: a bare-root capability that RETURNS a quoted ProviderRuntimeObservation for the installed runtime without handing the caller an executablePath-steerable observer — e.g. a discovery step that resolves the installed Claude itself and answers {observation} (or a durable host-capability record the activation path can carry), preserving the asymmetry task-75ee4a84 deliberately created. Consumers: task-6cbff010 (acceptance), the Foundation ingress chain (task-a9fd91c3) and task-44d48.",
  "definitionOfDone": [
    "A bare-root @moe/runner capability answers a quoted ProviderRuntimeObservation for the installed Claude runtime without accepting a caller-named executablePath, filesystem, facts port or clock.",
    "createClaudeRuntimePinRequest accepts that observation and prepareClaudeRuntimePin reaches a PROVEN pin against the really installed binary on Windows, proven by a real launch, not by a rebuilt digest.",
    "Every refusal keeps the exact code and layer of the authority that answered; a quote no production observation minted stays refused with CLAUDE_RUNTIME_OBSERVATION_CHANGED.",
    "apps/daemon/src/work/foundation-attempt-windows.test.ts can be extended to a real PROVEN dispatch (grant consumed, PREFLIGHT then STARTED registration, result manifest persisted) with no test-owned capability."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "BASELINE AND CONFIRM THE GAP — it is real, precisely measured by whoever filed this, and it is the direct consequence of an asymmetry task-75ee4a84 created on purpose. Re-verify at current HEAD (measured at 81a9f30).\n`claude-surface.ts` states the asymmetry and its reason verbatim: \"WITHHELD, and the asymmetry is the whole deliverable: `createNodeClaudeRuntimeFs`, `RUNTIME_PIN_CHUNK_BYTES`, `observeInstalledClaudeRuntime`, `probeClaudeRuntime`, `ClaudeRuntimeFsPort`, `ClaudeRuntimeFactsPort` and `ClaudeRuntimeObservationRefused` all stay internal. Publishing any one of them would re-open the injectable seam this factory exists to close — `observeInstalledClaudeRuntime` most of all, because it takes an `executablePath` and a consumer holding it could point the host observer at a binary no quote ever committed to.\"\nThat reasoning is CORRECT and must survive this task. What it left missing is the other half: nothing public can produce the `quotedObservation` the factory validates against, so no consumer can reach a PROVEN launch.\nWHY A SELF-ASSEMBLED QUOTE CANNOT WORK, and the measurement that proves it is not a bug to route around: `factsDigest` (claude-runtime-pin.ts:130-141) compares exactly `reportedVersion`, `adapterCapabilitySchemaDigest` and `platformIdentity{os,arch,osVersion}` — with the comment \"Compares only what the runtime IS, so pinned paths may legitimately differ.\" The version and the capability digest can only come from an observation the runner made itself, which is why the filer's real-Windows run refused a quote whose closure hash and paths were all TRUE with `CLAUDE_RUNTIME_OBSERVATION_CHANGED@RUNTIME`. Reproduce that refusal yourself before building; it is the spec.\nNO EXISTING PUBLIC RESOLUTION: `claude-launch-selection.ts` is about launch flags, model and reasoning effort, is not published from the surface, and resolves no executable. So discovery is genuinely new.\nGATES, each separate with its own exit code — never `;`-chained, never piped into tail: `pnpm --filter @moe/runner typecheck` and `pnpm --filter @moe/runner test`, recording count lines and every failing file as your before-set.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/providers/claude/claude-runtime-pin.ts",
        "packages/runner/src/providers/claude/claude-host-runtime.ts"
      ],
      "note": "BASELINE + GAP REPRODUCED at HEAD c7013ad.\n\nGATES (each run alone, exit code captured via ${PIPESTATUS[0]}, never ;-chained):\n- `pnpm --filter @moe/runner typecheck` -> EXIT 0 (`tsc --project tsconfig.json`, no diagnostics).\n- `pnpm --filter @moe/runner test` -> EXIT 0, COUNT LINES: `Test Files 66 passed (66)` / `Tests 2216 passed | 1 skipped (2217)`. Nonzero executed count, so the leg really ran (globalRail 8/9 discriminator). Before-set of failing files: EMPTY.\n\nGAP REPRODUCED on this real Windows host (10.0.26200), throwaway probe under $TEMP, deleted immediately after (`rm -f`, confirmed) — nothing scratch inside the repo:\n- Built a quote through the PUBLIC `buildProviderRuntimeObservation` naming the genuinely installed `C:\\Users\\Yaron\\.local\\bin\\claude.exe` with its TRUE sha256 (measured with node:crypto over the real 320,400,544-byte file) and a TRUE platformIdentity.\n- `createClaudeRuntimePinRequest` ACCEPTED it (`HYDRATED_OK`).\n- `prepareClaudeRuntimePin` REFUSED: `CLAUDE_RUNTIME_OBSERVATION_CHANGED@RUNTIME`, message \"the runtime observed now is not the runtime the quote bound\".\nThat is the spec, exactly as filed. The closure hash and paths were all TRUE; what a caller cannot mint is `reportedVersion` and `adapterCapabilitySchemaDigest`, and `factsDigest` (claude-runtime-pin.ts:130-141) compares exactly those two plus platformIdentity{os,arch,osVersion}.\n\nWITHHELD LIST CONFIRMED at claude-surface.ts:84-92 verbatim, and `index-surface.test.ts` already pins 14 withheld names against the imported NAMESPACE (not file text) at its \"withholds every runtime capability the pin-request factory mints\" test.\n\nNO EXISTING PUBLIC RESOLUTION confirmed: `claude-launch-selection.ts` resolves no executable and is not on the surface; no `claude-runtime-discovery.*` exists (`ls` -> No such file or directory). Discovery is genuinely new.\n\nHOST CENSUS for step 3's resolution design (measured, not assumed): 51 PATH entries; exactly ONE `claude.exe` survives realpath de-duplication -> `C:\\Users\\Yaron\\.local\\bin\\claude.exe`. So the one-candidate arm is reachable on this host and DoD 2 is provable here. Broker present at `dist/windows-job-native/release/moe-windows-job-broker.exe` (162,816 bytes), so the real `--version` probe can run.\n\nDESIGN FACTS also measured: runner tsconfig `include` is `src/**/*.ts`, so test files ARE covered by the typecheck gate (a type-level assertion in the test file is enforceable). `WindowsProcessUnknown` is NOT currently on the root, while `ClaudeFailure` and `ClaudeRuntimePinFailure` are — so publishing that one type is what lets a consumer name all four refusal arms without restamping any of them.",
      "startedAt": "2026-08-16T14:51:08.631Z",
      "completedAt": "2026-08-16T14:56:55.227Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD RED — the discovery contract, whose whole job is to be UNSTEERABLE. Create the test against the not-yet-existing capability.\nTHE SHAPE: a bare-root function that takes NO caller-named executable path, filesystem, facts port or clock — ideally no arguments beyond bounded options that cannot select a binary — resolves the installed Claude runtime from the HOST itself, observes it through the withheld `observeInstalledClaudeRuntime`, and answers `{observation}` or an existing stable refusal. The asymmetry is preserved because the caller cannot say WHICH binary; the host answers that.\nPIN THE UNSTEERABILITY AS A TYPE-LEVEL FACT, not a runtime check: assert there is no parameter, overload, optional field or exported type through which a caller could supply an executable path, an fs, a facts port or a clock. A runtime rejection is one refactor from being relaxed; an absent parameter is not.\nRESOLUTION MUST REFUSE AMBIGUITY. If host resolution finds ZERO candidates, refuse. If it finds MORE THAN ONE, refuse — never pick the first. A caller who can influence the host's candidate set (PATH order, a shadowing install) must not be able to steer the observation by making one candidate win; refusing ambiguity is what removes that lever. Pin both with exact code and layer.\nREFUSALS PASS THROUGH UNCHANGED. `observeInstalledClaudeRuntime` answers with four arms — an observed record, a `ClaudeRuntimePinFailure`, a `ClaudeFailure<ClaudeObservationErrorCode>`, or a `WindowsProcessUnknown`. Each keeps its own code AND the layer of the authority that answered; do not restamp them into a discovery vocabulary, because four layers can answer here and restamping erases which did (DoD 3).\nAND THE NEGATIVE SURFACE: assert the root still does NOT publish `observeInstalledClaudeRuntime`, `probeClaudeRuntime`, `createNodeClaudeRuntimeFs`, `ClaudeRuntimeFsPort` or `ClaudeRuntimeFactsPort`. Assert against the imported NAMESPACE, not the file text — the barrel can re-export via `export *`, which a grep cannot see through.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts"
      ],
      "newFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts"
      ],
      "note": "TDD RED verified for the RIGHT reason, not a typo and not a pass:\n`npx vitest run packages/runner/src/providers/claude/claude-runtime-discovery.test.ts` -> EXIT 1, `Test Files 1 failed (1)`, `Error: Cannot find module './claude-runtime-discovery.js'`. The capability does not exist yet.\n\nTHE SHAPE PINNED: `discoverInstalledClaudeRuntime()` takes ZERO arguments and answers `{ok:true, observation, installedRoot}` or an existing stable refusal. `installedRoot` is returned because `createClaudeRuntimePinRequest` requires it and deriving it would make every consumer reimplement a dirname; it leaks nothing the observation's own closure path does not already carry.\n\nUNSTEERABILITY IS PINNED TWICE, deliberately, because either alone survives a plausible drill:\n1. TYPE-LEVEL — `type TakesNoArgument = Parameters<typeof discoverInstalledClaudeRuntime> extends readonly [] ? true : false` assigned to `true`. Measured: the runner tsconfig `include` is `src/**/*.ts`, so the TYPECHECK gate enforces this file. `[a?: string]` is not assignable to `[]`, so an optional parameter breaks compilation.\n2. RUNTIME ARITY — `discoverInstalledClaudeRuntime.length === 0`. A TS optional parameter still emits a declared parameter, so arity catches the plain `executablePath?: string` drill even if typecheck were skipped.\n3. RUNTIME BEHAVIOUR — \"answers the same runtime whether or not a caller passes a steering argument\" calls the function twice, once through a widening cast with a stand-in executable path, and asserts both answers are identical. This is the one that kills the `options = {}` DEFAULTED-parameter variant, which has arity 0 and would slip past (2).\n\nAMBIGUITY: zero candidates -> `CLAUDE_RUNTIME_PATH_MISSING@RUNTIME`; more than one -> `CLAUDE_RUNTIME_PATH_DUPLICATE@RUNTIME`. Both codes already exist in the frozen `CLAUDE_RUNTIME_PIN_ERROR_CODES`, so no closed enum is widened (that would couple sibling `::ALL` tests). A separate test proves DE-DUPLICATION: one binary reachable by two search entries must reach the OBSERVER, not the duplicate refusal.\n\nREFUSAL PASSTHROUGH measured, not assumed. Probe under $TEMP (deleted): a real `where.exe` copied to `claude.exe` and handed to `observeInstalledClaudeRuntime` answers exactly `{\"code\":\"CLAUDE_RUNTIME_OBSERVATION_INVALID\",\"layer\":\"RUNTIME\",\"truthClass\":\"UNKNOWN\",\"message\":\"installed runtime request probe did not exit cleanly, so no version was observed\"}` — i.e. the shipped broker really created the process. The test asserts that exact code AND layer, so restamping it into a discovery vocabulary reddens.\n\nTESTS DRIVE THE HOST, NEVER A PARAMETER: they rewrite `process.env.PATH` and `USERPROFILE` (saved and restored in `afterEach`). That is the only lever a caller has left once the parameter is gone, and refusing ambiguity is what makes it useless.\n\nEVERY ARM HAS A POSITIVE EXECUTED CASE OFF WINDOWS TOO: each test asserts the `CLAUDE_RUNTIME_PLATFORM_UNSUPPORTED` arm rather than skipping, so the file can never pass by generating zero cases.\n\nDoD 2 and the two negatives are in this same file: the PROVEN round trip, the self-assembled quote still refused `CLAUDE_RUNTIME_OBSERVATION_CHANGED@RUNTIME`, and the stand-in still refused `CLAUDE_RUNTIME_OBSERVATION_INVALID@RUNTIME`. The round trip asserts only the three fields `factsDigest` compares plus a DIFFERING pinned executable path — whole-object equality would redden on a legitimate difference and then get \"fixed\" by loosening the assertion that matters.",
      "startedAt": "2026-08-16T14:57:54.126Z",
      "completedAt": "2026-08-16T15:00:32.137Z"
    },
    {
      "stepId": "step-3",
      "description": "IMPLEMENT THE DISCOVERY CAPABILITY. Create `claude-runtime-discovery.ts` plus its one-line `.js` bridge (`export * from \"./claude-runtime-discovery.ts\";`, LF ending — the bridge guard byte-compares).\nHOST RESOLUTION, and keep it narrow: resolve candidates from the host's own configuration — the PATH entries and the platform's known install locations — canonicalising each and de-duplicating by real path before counting, so one binary reachable two ways is one candidate rather than an ambiguity refusal. Exactly one surviving candidate proceeds; zero or many refuse. Do NOT accept a hint, an override, or an environment variable a caller could set to choose — that would reintroduce the steerability the withheld comment exists to prevent, in a new costume.\nThen call the withheld `observeInstalledClaudeRuntime` with the resolved path and the installed root derived from it, and return its observation unchanged. This module is the ONLY thing that may hold that function's `executablePath` argument, and it hands it a path no caller chose.\nERROR MESSAGES MUST NOT ECHO the resolved path, argv, environment values or digests — task-32eddfd3 was QA-rejected once on exactly that rail, and a discovery refusal naming the paths it searched would leak the host layout to any caller who can trigger one.\nKeep the file under 250 physical lines by `grep -c ''` (PowerShell `Measure-Object -Line` undercounts). Do NOT modify `claude-host-runtime.ts`, `claude-runtime-pin.ts` or `claude-runtime-request.ts` — they belong to landed tasks; this is additive.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.ts",
        "packages/runner/src/providers/claude/claude-runtime-discovery.js"
      ],
      "newFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.ts",
        "packages/runner/src/providers/claude/claude-runtime-discovery.js"
      ],
      "modifiedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.ts",
        "packages/runner/src/providers/claude/claude-runtime-discovery.js"
      ],
      "note": "GREEN, and every case executed with a real duration — no silent zero-case pass.\n\n`npx vitest run packages/runner/src/providers/claude/claude-runtime-discovery.test.ts --reporter=verbose` -> EXIT 0, `Test Files 1 passed (1)` / `Tests 10 passed (10)`. Durations prove the expensive arms really ran rather than short-circuiting: PROVEN pin 3163ms, self-assembled-quote refusal 1643ms, steering probe 1311ms (two real `--version` launches), stand-in passthrough 54ms.\n`pnpm --filter @moe/runner typecheck` -> EXIT 0, so the type-level unsteerability assertion compiles at the shape it was written for.\n\nSIZE: `grep -c ''` -> claude-runtime-discovery.ts 200 lines (under the 250 target), bridge 1 line. Bridge bytes verified with `xxd`: `export * from \"./claude-runtime-discovery.ts\";\\n`, LF, byte-identical in shape to the sibling `claude-host-runtime.js`.\n\nRESOLUTION, narrow on purpose: the host's search path plus ONE known install location (`<home>\\.local\\bin`, which is where this runtime actually ships and where the installed binary really is on this host). No speculative directory list — every extra directory is another place a shadowing binary could turn a working host into an ambiguity refusal. NO hint, override or dedicated environment variable exists; the ONLY influence a caller retains is ADDING to the candidate set, and adding turns one candidate into two, which refuses. The lever moves the host to a fail-closed answer, never to a chosen one.\n\nTRUNCATION WOULD HAVE BEEN A HOLE, so it refuses instead. A search path longer than MAX_SEARCH_ENTRIES=512 returns CLAUDE_RUNTIME_PATH_INVALID rather than scanning a prefix — silently scanning a prefix hands the steering lever straight back: pad the search path, the real install falls off the end, and one shadowing candidate wins uncontested.\n\nDE-DUPLICATION BEFORE COUNTING: each candidate is `realpath`ed and folded (Windows path identity is case-insensitive) into a Map before the count, so one binary reachable by two search entries — or found both on the path and at the known location — is ONE candidate. Proven by its own test, which requires the OBSERVER to answer rather than the duplicate refusal.\n\n`stat` throws on a missing entry instead of answering falsy, so the isFile() guard lives inside the try.\n\nMESSAGE HYGIENE: no refusal message names a path, an argument, an environment value or a digest. Asserted by a test whose regex rejects a backslash, a drive letter, `claude.exe`, `AppData`, `Temp`, `PATH` and `USERPROFILE`.\n\nPASSTHROUGH: the observer's non-ok arms are returned as the SAME object, unchanged, keeping code and layer. `ClaudeFailure` declares no layer at all and none is invented for it.\n\nNOT TOUCHED, as the plan requires: claude-host-runtime.ts, claude-runtime-pin.ts and claude-runtime-request.ts are unmodified. This is purely additive.",
      "startedAt": "2026-08-16T15:00:32.784Z",
      "completedAt": "2026-08-16T15:02:28.811Z"
    },
    {
      "stepId": "step-4",
      "description": "PUBLISH IT, AND REWRITE THE ASYMMETRY COMMENT SO IT STAYS TRUE. Add the discovery function and the result/refusal types a consumer needs to `packages/runner/src/surface/claude-surface.ts`, and update `packages/runner/src/index-surface.test.ts`, which pins the root export list and its exact length — a bare addition fails until the fixture is updated, and that failure is the fixture working.\nTHE COMMENT IS PART OF THE DELIVERABLE. The existing WITHHELD paragraph explains that `observeInstalledClaudeRuntime` stays internal \"because it takes an `executablePath` and a consumer holding it could point the host observer at a binary no quote ever committed to.\" That reason still holds and must remain — but the paragraph now also has to say what IS published and why publishing it does not re-open the seam: the discovery capability names no binary, so a consumer can obtain an observation of THE installed runtime without being able to choose WHICH runtime gets observed. Write that distinction explicitly. A future reader who sees an observation-returning function on the root and no explanation will reasonably conclude the withholding was abandoned.\nKeep every existing withheld name withheld. The negative surface assertions from step 2 must still pass, and step 6's drill re-proves they read the namespace rather than the file text.\nMeasure `claude-surface.ts` after editing with `grep -c ''` — it was 212 lines before the pin-request task added to it, so confirm it is still under the 250 target and split the export block into a sibling surface module rather than compressing if it is not.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/surface/claude-discovery-surface.ts",
        "packages/runner/src/surface/claude-discovery-surface.js",
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "PUBLISHED, and the export-count fixture did its job before I touched it.\n\nTHE FIXTURE WORKED FIRST, recorded rather than skipped: with the bare addition and the fixture untouched, `npx vitest run packages/runner/src/index-surface.test.ts` -> EXIT 1, `AssertionError: expected [ …(230) ] to deeply equal [ …(229) ]`, `+ \"discoverInstalledClaudeRuntime\"`. That failure IS the fixture working; the count was then raised 229 -> 230 and the name transcribed by hand into the pin-request group, never derived from the namespace under test.\n\nA SECOND GUARD I DID NOT KNOW ABOUT ALSO FIRED, and it is the \"a roster can grow your axis mid-task\" shape: `leaves the ten Family B Codex types owned by exactly one surface module` enumerates `surface/*-surface.ts` off disk and pins `SURFACE_SOURCES.size` to 6. A new surface module makes 7 -> `AssertionError: expected 7 to be 6`. Roster updated to 7 with `claude-discovery-surface.ts` added to the sorted key list. Both Family A and Family B assertions still hold unchanged: the new module's STRIPPED source (its export statements) contains no Family A or Family B name, and `ProviderRuntimeObservation`, `RUNTIME_PINNING_METHODS` and their siblings stay owned by claude-surface.ts + codex-surface.ts exactly as before.\n\nSIZE FORCED A SPLIT, and I split rather than compressed, as the plan directs. Adding the seam took claude-surface.ts to 255 by `grep -c ''` — over the 250 target. Moving only my own additions still left it at 255, so I moved the runtime pin-REQUEST seam OUT WHOLE, paragraph and all three export statements, into the new sibling `claude-discovery-surface.ts`. Not churn for its own sake: the factory and the only published thing that can produce a quote it accepts are ONE seam, and the withheld list now lives beside the thing whose publication it has to justify. Final `grep -c ''`: claude-surface.ts 220, claude-discovery-surface.ts 76, claude-runtime-discovery.ts 200, index.ts 250 — every one at or under target.\n\nTHE BARREL LINE went into claude-surface.ts (`export * from \"./claude-discovery-surface.js\"`) rather than index.ts, because index.ts was already exactly at 250 and a line there would have pushed a second file over. The root namespace is identical either way, and the namespace-reading export test proves it rather than a grep, which is blind to `export *`.\n\nTHE COMMENT IS THE DELIVERABLE, now in two places that cannot drift apart silently. claude-surface.ts keeps a 6-line pointer saying the seam moved and why. claude-discovery-surface.ts carries the full reasoning: the withheld list verbatim and unchanged in letter and reason; why a self-assembled quote is refused CLAUDE_RUNTIME_OBSERVATION_CHANGED and must stay refused; and the EXACT distinction — what makes `observeInstalledClaudeRuntime` dangerous is its `executablePath` ARGUMENT, not that it returns an observation, and discovery takes no argument, so a consumer obtains an observation of THE installed runtime without choosing WHICH one is observed. A reader who finds an observation-returning function on this root will not conclude the withholding was abandoned.\n\n`WindowsProcessUnknown` is published as a TYPE ONLY, and the module says why it is not a withheld-list case: it is a plain refusal RECORD, and a consumer unable to name the fourth arm cannot branch on the layer that refused, which is exactly what DoD 3 requires to survive. It grants nothing that can observe, spawn or mint.\n\nTHE SURFACE TEST NOW PROVES UNSTEERABILITY THROUGH THE ROOT TOO, at compile time: `Parameters<typeof runner.discoverInstalledClaudeRuntime> extends readonly [] ? true : false` and `WindowsProcessUnknown extends DiscoverInstalledClaudeRuntimeResult ? true : false`, both assigned `true`, plus the runtime arity check. It calls the published function and feeds its observation straight into the published `createClaudeRuntimePinRequest` — obtained by CALLING, never by constructing a literal that would typecheck against a locally re-declared shape.\n\nGATES: `pnpm --filter @moe/runner typecheck` EXIT 0. `pnpm --filter @moe/runner test` EXIT 0, `Test Files 67 passed (67)` / `Tests 2228 passed | 1 skipped (2229)` — strictly above the step-1 baseline of 66/2216.",
      "startedAt": "2026-08-16T15:07:55.540Z",
      "completedAt": "2026-08-16T15:08:14.941Z"
    },
    {
      "stepId": "step-5",
      "description": "THE REAL-WINDOWS PROOF — DoD 2, and the only thing that distinguishes this from a shape exercise. On a real Windows host with the genuinely installed claude.exe, drive the whole round trip: discovery answers an observation, `createClaudeRuntimePinRequest` accepts it, and `prepareClaudeRuntimePin` reaches a PROVEN pin. Assert PROVEN, and assert it came from a REAL LAUNCH — the shipped broker actually ran its `--version` probe — not from a rebuilt digest. The filer proved the negative on this exact hardware (claude.exe 2.1.233, Windows 10.0.26200); this is the positive that closes it.\nASSERT THE ROUND TRIP BINDS, both directions: the quoted observation's `reportedVersion`, `adapterCapabilitySchemaDigest` and `platformIdentity` survive into the pin — those are exactly what `factsDigest` compares — while the pinned closure paths may legitimately differ, per that function's own comment. A test asserting whole-object equality would fail for the wrong reason and then get \"fixed\" by loosening the assertion that matters.\nAND KEEP THE NEGATIVE (DoD 3): a self-assembled quote that no production observation minted still refuses `CLAUDE_RUNTIME_OBSERVATION_CHANGED` at `RUNTIME`, and a stand-in binary still refuses `CLAUDE_RUNTIME_OBSERVATION_INVALID` at `RUNTIME` after the broker really probed it. Both must remain true AFTER this capability ships — the point is that discovery is the only route to a valid quote, not that quotes stopped being checked.\nON A NON-WINDOWS HOST assert the platform-unsupported refusal arm instead, with a positive executed-case count either way. A silently skipped proof generates zero cases and passes while proving nothing, which for this task would mean shipping the capability with its central claim untested.\nDoD 4 SCOPE NOTE: `apps/daemon/src/work/foundation-attempt-windows.test.ts` belongs to task-6cbff010's slice. This task's obligation is to make a real PROVEN dispatch REACHABLE with no test-owned capability, and to say so in the completion; extending that file is the consumer's call. If you do extend it, say why and treat it as an owned path — do not edit it incidentally.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts"
      ],
      "note": "DoD 2 CLOSED ON THIS REAL WINDOWS HOST. The filer proved the negative on this exact hardware; this is the positive that closes it.\n\n`npx vitest run packages/runner/src/providers/claude/claude-runtime-discovery.test.ts --reporter=verbose` -> EXIT 0, `Tests 10 passed (10)`, fresh AFTER publication. The durations are the evidence that the expensive arms really ran rather than short-circuiting:\n- `reaches a PROVEN pin against the really installed runtime through discovery alone` 2968ms\n- `still refuses a self-assembled quote no production observation minted, with CLAUDE_RUNTIME_OBSERVATION_CHANGED at RUNTIME` 1585ms\n- `answers the same runtime whether or not a caller passes a steering argument` 1235ms (two real `--version` launches)\n- `keeps the stand-in binary refused after the shipped broker really probed it` 57ms\n\nTHE ROUND TRIP: discovery answers an observation -> `createClaudeRuntimePinRequest` accepts it -> `prepareClaudeRuntimePin` returns `ok:true` with `observation.truthClass === \"PROVEN\"`. It is a REAL LAUNCH, not a rebuilt digest, and the test cannot have faked it: nothing in that file computes a capability schema digest or parses a version, and no published surface can mint either. The version is asserted against the provider's own version SHAPE and the schema digest against 64-hex, so the values are read, never reconstructed.\n\nTHE BINDING IS ASSERTED IN BOTH DIRECTIONS, and deliberately NOT by whole-object equality — that would redden on a legitimate difference and then get \"fixed\" by loosening the assertion that matters. What must survive is exactly what `factsDigest` compares: `reportedVersion`, `adapterCapabilitySchemaDigest` and `platformIdentity{os,arch,osVersion}`, asserted equal between the quote and the pin. What must DIFFER is the pinned path: `prepared.executablePath` is asserted NOT equal to the quoted closure's path, because the pin is a content-addressed copy and that function's own comment says pinned paths may legitimately differ.\n\nBOTH NEGATIVES STILL HOLD AFTER THE CAPABILITY SHIPPED — the point is that discovery is the ONLY route to a valid quote, not that quotes stopped being checked:\n- A quote built through the PUBLIC `buildProviderRuntimeObservation` with the real executable, its real sha256 and a real platform identity, but a made-up version and capability digest, is still refused `{code: CLAUDE_RUNTIME_OBSERVATION_CHANGED, layer: RUNTIME, truth: UNKNOWN}`.\n- A stand-in binary is still refused `CLAUDE_RUNTIME_OBSERVATION_INVALID` at `RUNTIME`, after the shipped broker really created the process (the message is \"probe did not exit cleanly\", reachable only post-spawn).\n\nNON-WINDOWS ARM: every test asserts the `CLAUDE_RUNTIME_PLATFORM_UNSUPPORTED` arm rather than skipping, so the file cannot pass by generating zero cases. A silently skipped proof would ship the capability with its central claim untested.\n\nDoD 4 — REACHABLE, MEASURED, AND NOT EXTENDED BY ME. `apps/daemon/src/work/foundation-attempt-windows.test.ts` belongs to task-6cbff010's slice and I did not touch it. That file's own comment states the gap this task closes verbatim: \"no case here reaches PROVEN: the public surface withholds `observeInstalledClaudeRuntime`, `probeClaudeRuntime` and `capabilitySchemaDigestOf`, so NO consumer can mint a quote production would accept... The gap is reported as a prerequisite instead.\" That prerequisite is now shipped, and I verified the edge rather than asserting it:\n- `apps/daemon/package.json` declares `\"@moe/runner\": \"workspace:*\"`.\n- `pnpm-lock.yaml` carries the `apps/daemon` importer entry `'@moe/runner': specifier workspace:* / version link:../../packages/runner`.\n- A COMPILED PROBE inside apps/daemon importing from the BARE specifier `@moe/runner` — `discoverInstalledClaudeRuntime`, `createClaudeRuntimePinRequest`, `DiscoverInstalledClaudeRuntimeResult`, `DiscoveredClaudeRuntime`, `WindowsProcessUnknown`, `ClaudeRuntimePinRequestResult` — and composing them into a pin request: `pnpm --filter @moe/daemon typecheck` EXIT 0. Probe DELETED in the same command and its absence confirmed by `ls` (No such file or directory) and by `git status --porcelain`.\n- Daemon typecheck baseline immediately before the probe was also EXIT 0, so the probe's green is attributable and not inherited from a red baseline.\n\nSo a real PROVEN dispatch is reachable by task-6cbff010 with NO test-owned capability: it calls `discoverInstalledClaudeRuntime()` from the root, hands the observation to `createClaudeRuntimePinRequest`, and the launcher applies `prepareClaudeRuntimePin` itself. Extending that file is the consumer's call, not mine.",
      "startedAt": "2026-08-16T15:07:51.589Z",
      "completedAt": "2026-08-16T15:09:33.049Z"
    },
    {
      "stepId": "step-6",
      "description": "MUTATION DRILLS, GATE, ADVERSARIAL REVIEW, COMMIT. Drills, each alone and reverted before the next: (1) add an optional `executablePath` to the discovery function and prefer it when present; a NAMED test must redden — this is the exact seam the withheld comment exists to keep closed, and the most plausible \"convenience\" regression. (2) Make ambiguous resolution pick the first candidate; the many-candidates case must redden — proving a caller who can shadow a binary on PATH cannot steer the observation. (3) Restamp one `observeInstalledClaudeRuntime` refusal arm into a discovery code; a NAMED test must redden on the lost layer. (4) Re-export `observeInstalledClaudeRuntime` from the barrel; the negative surface assertion must redden, proving it reads the namespace and not the file text. (5) Interpolate the resolved path into a refusal message; the message-hygiene assertion must redden. For EVERY drill record the exact replaced range you edited — a drill that applied nothing reads as green — confirm the red lands on the INTENDED assertion, restore with `git checkout -- <file>` (never by hand), and verify `git status --short` clean before the next.\nGATES, each separate with its own captured exit code: `pnpm --filter @moe/runner typecheck` exit 0; `pnpm --filter @moe/runner test` reading the COUNT LINE strictly above the step-1 baseline; `pnpm typecheck` repo-wide with the path-attributed baseline — HEAD failing paths minus merge-base failing paths, intersected with your owned paths, MUST be empty. Quote any foreign red verbatim without repairing it.\nADVERSARIAL REVIEW: (a) is there ANY parameter, overload, optional field or exported type through which a caller could name a binary, an fs, a facts port or a clock? (b) does resolution refuse zero and many rather than picking? (c) do all four observer refusal arms pass through with code AND layer intact? (d) does the surface comment now explain BOTH what stays withheld and why publishing discovery does not re-open the seam? (e) do the negative surface assertions still pass, read against the namespace? (f) does the real-Windows proof assert PROVEN from a real launch, with the two refusal negatives still holding? (g) does any refusal message echo a path, argv, environment value or digest? (h) is every production file under 250 lines by `grep -c ''`, the bridge one LF line? (i) does `git status --porcelain` show only owned paths with every drill reverted?\nCommit by explicit pathspec — never a bare `git commit`, never `git add -A`. Verify with `git show --stat`. In the completion: quote the count lines, name the test that reddened for each drill, state plainly that a PROVEN Claude launch is now reachable by a consumer with no test-owned capability, and record the consumers — task-6cbff010 (acceptance), task-a9fd91c3 (Foundation ingress) and task-44d4873e. Keep verification.command under 500 characters.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.ts",
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/providers/claude/claude-runtime-discovery.ts",
        "packages/runner/src/providers/claude/claude-runtime-discovery.js",
        "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts",
        "packages/runner/src/surface/claude-discovery-surface.ts",
        "packages/runner/src/surface/claude-discovery-surface.js",
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "FIVE DRILLS, EACH ALONE, EACH RED ON THE INTENDED ASSERTION, EACH RESTORED BYTE-IDENTICAL.\n\nThe deliverable files are UNTRACKED, so `git checkout -- <file>` has no HEAD blob and would have DESTROYED them. Restores were done from a sha256-verified snapshot outside the repo, and every restore confirmed with `diff` plus `sha256sum -c` against the pristine digests (claude-runtime-discovery.ts 345f9fe6..., claude-discovery-surface.ts bb48af64...).\n\nDRILL 1 — optional `executablePath` preferred when present. Replaced range: the signature line 160 (`(): Promise<...>` -> `(executablePathHint?: string): Promise<...>`) plus lines 170-171 (`const candidates = await resolveCandidates()` -> the hint-preferring ternary). RED on THREE intended assertions:\n- typecheck `src/providers/claude/claude-runtime-discovery.test.ts(123,9): error TS2322: Type 'true' is not assignable to type 'false'` (the type-level `TakesNoArgument`)\n- typecheck `src/index-surface.test.ts(893,9): error TS2322` (the same fact through the ROOT)\n- vitest `takes no parameter through which a caller could name a binary, an fs, a facts port or a clock` -> `expected 1 to be +0`\n- vitest `answers the same runtime whether or not a caller passes a steering argument` -> `expected { kind: 'REFUSED', …} to deeply equal { kind: 'OBSERVED', …}`\nThat third one is the load-bearing one: it is the only assertion that also kills the DEFAULTED-parameter variant `(opts = {})`, which has arity 0 and would slip past the `.length` check.\n\nDRILL 2 — ambiguous resolution picks the first candidate. Replaced range: deleted lines 179-184, the whole `if (resolved.length > 1)` refusal. RED on `refuses more than one resolved candidate rather than picking one, with CLAUDE_RUNTIME_PATH_DUPLICATE at RUNTIME`. Nine others stayed green, so the red is attributable to that guard alone.\n\nDRILL 3 — restamp the observer's refusal into a discovery code. Replaced range: lines 191-193 (`return observed;` and its two comment lines) -> `return discoveryRefusal(\"CLAUDE_RUNTIME_PATH_INVALID\", ...)`. RED on the NAMED `passes an observer refusal through with the exact code and layer of the authority that answered` -> `expected { …(2) } to deeply equal { …(2) }`, plus `counts one binary reachable by two search entries as one candidate` -> `expected 'CLAUDE_RUNTIME_PATH_INVALID' to be 'CLAUDE_RUNTIME_OBSERVATION_INVALID'` and `keeps the stand-in binary refused after the shipped broker really probed it`.\n\nDRILL 4 — re-export `observeInstalledClaudeRuntime` from the barrel (appended one line to claude-discovery-surface.ts, line 77). RED on `withholds every runtime capability the pin-request factory mints` -> `expected [ 'observeInstalledClaudeRuntime' ] to deeply equal []`, plus the export-count test at 231 vs 230. THIS IS THE PROOF THE ASSERTION READS THE NAMESPACE: the leak travelled index.ts -> claude-surface.ts -> claude-discovery-surface.ts through two `export *` hops, which a grep of the barrel text could never have seen.\n\nDRILL 5 — interpolate the resolved paths into a refusal message. Replaced range: line 182, the DUPLICATE message string -> a template naming `resolved.join(\", \")`. RED on `keeps every discovery refusal free of the paths, arguments and environment it searched` -> `expected 'more than one installed claude runtim…' not to match /[\\\\]|[A-Za-z]:|claude\\.exe|AppData|T…/u`.\n\nEvery drill's `diff` against the snapshot is quoted above, so none of them \"applied nothing and read as green\". After the last restore, `git diff --stat` on the two TRACKED files showed only my real changes and the untracked five were byte-identical to pristine.\n\nGATES, each separate with its own captured exit code:\n- `pnpm --filter @moe/runner typecheck` EXIT 0\n- `pnpm --filter @moe/runner test` EXIT 0, COUNT LINES `Test Files 67 passed (67)` / `Tests 2228 passed | 1 skipped (2229)` — strictly above the step-1 baseline of 66 / 2216\n- `pnpm typecheck` repo-wide EXIT 0, `grep -c \"error TS\"` = 0, run again AFTER the commit. The path-attributed delta (HEAD failing paths minus merge-base failing paths, intersected with owned paths) is EMPTY because there are no failing paths at all. NO foreign red to disclose at this HEAD; I am not quoting one that does not exist.\n\nADVERSARIAL REVIEW, all nine:\n(a) NO parameter, overload, optional field or exported type through which a caller could name a binary, an fs, a facts port or a clock. Zero-arity, pinned at type level and by drill 1.\n(b) Zero refuses CLAUDE_RUNTIME_PATH_MISSING, many refuses CLAUDE_RUNTIME_PATH_DUPLICATE, neither picks. A search path over 512 entries REFUSES rather than truncating — a silent prefix scan would let PATH padding push the real install off the end and leave a shadow uncontested.\n(c) All four observer arms pass through as the SAME object with code and layer intact. `ClaudeFailure` declares no layer and none is invented for it.\n(d) The comment explains BOTH halves, and lives beside the seam it justifies.\n(e) Negative surface assertions pass, read again",
      "startedAt": "2026-08-16T15:09:33.288Z",
      "completedAt": "2026-08-16T15:15:51.477Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": "qa-ce3ba361",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-6cbff01023b14b26a78fc5e3eb1dd8a9",
  "priority": "HIGH",
  "order": 110.9,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-16T11:51:54.479Z",
  "updatedAt": "2026-08-16T15:16:59.239Z",
  "metrics": {
    "firstClaimAt": "2026-08-16T13:14:36.418Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 6
  },
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-16T13:17:10.688Z",
  "planningNotes": {
    "approachesConsidered": "1) Publish `observeInstalledClaudeRuntime` from the root. REJECTED, and the surface comment already argues it better than I could: it takes an `executablePath`, so a consumer holding it \"could point the host observer at a binary no quote ever committed to\". Publishing it would close this gap by re-opening the one task-75ee4a84 exists to close.\n\n2) Let `createClaudeRuntimePinRequest` build its own quote internally. REJECTED: the factory validates the quote against a freshly observed one, so a self-built quote would make the comparison compare an observation against itself — the check would pass by construction and stop meaning anything.\n\n3) Publish `capabilitySchemaDigestOf` and `canonicalDigest` so a caller can assemble a valid quote. REJECTED, and it is the most dangerous option because it looks like closing an information gap: those two are exactly what make a quote unforgeable. A caller able to mint the capability digest can mint a quote for a runtime nobody observed — the digest authority reimplemented outside the runner.\n\n4) Accept an optional path hint, or an environment variable, so callers can help discovery in odd installs. REJECTED: that is the steerable argument in a new costume. Drill 1 targets it precisely because it is the most plausible \"convenience\" regression.\n\n5) Pick the first candidate when host resolution finds several. REJECTED: it hands back the steering lever to anyone who can shadow a binary on PATH. Refuse ambiguity.\n\n6) Restamp the observer's four refusal arms into one discovery code for a simpler consumer API. REJECTED: four authorities can answer, and DoD 3 requires each refusal to keep the code and layer of whichever did.\n\n7) Extend `foundation-attempt-windows.test.ts` as part of this task. NOT PLANNED as an obligation: that file belongs to task-6cbff010's slice. This task must make a real PROVEN dispatch REACHABLE with no test-owned capability and say so; extending that file is the consumer's call, and if the worker does it they must own the path deliberately rather than incidentally.",
    "codebaseInsights": "MEASURED AT HEAD 81a9f30. This gap is real, it was measured properly by whoever filed it (grep plus a real Windows run against claude.exe 2.1.233 on 10.0.26200), and it is the direct consequence of an asymmetry I specified in task-75ee4a84's plan. Worth saying plainly: that plan argued `observeInstalledClaudeRuntime` must stay internal because publishing it \"re-opens the exact authority hole the factory closes\". That reasoning was right and the surface comment now states it verbatim. What the plan did not do was provide the other half — a way to OBTAIN a quote — so the factory validates against something no consumer can produce.\n\nTHE MEASUREMENT THAT MAKES SELF-ASSEMBLY IMPOSSIBLE, and shows this is not a bug to route around: `factsDigest` (claude-runtime-pin.ts:130-141) compares exactly `reportedVersion`, `adapterCapabilitySchemaDigest` and `platformIdentity{os,arch,osVersion}`, with the comment \"Compares only what the runtime IS, so pinned paths may legitimately differ.\" The version and the capability digest can only come from an observation the runner made itself — `capabilitySchemaDigestOf` (claude-capabilities.ts:273) and `canonicalDigest` are both internal. Hence the filer's result: a quote whose closure hash and paths were ALL TRUE still refused `CLAUDE_RUNTIME_OBSERVATION_CHANGED@RUNTIME`.\n\nTHE WITHHELD LIST, confirmed at claude-surface.ts:84-92: `createNodeClaudeRuntimeFs`, `RUNTIME_PIN_CHUNK_BYTES`, `observeInstalledClaudeRuntime`, `probeClaudeRuntime`, `ClaudeRuntimeFsPort`, `ClaudeRuntimeFactsPort`, `ClaudeRuntimeObservationRefused`, and `prepareClaudeRuntimePin` (\"the launcher applies it itself\").\n\nTHE RESOLUTION THAT PRESERVES THE ASYMMETRY: a capability that DISCOVERS the installed runtime itself. The steerability the comment fears comes from the `executablePath` ARGUMENT, not from returning an observation. A function that names no binary — the host answers which one — hands the caller an observation without handing them the choice. That is the distinction the surface comment must be rewritten to carry, or a future reader will read the new export as the withholding having been abandoned.\n\nNO EXISTING PUBLIC RESOLUTION: `claude-launch-selection.ts` covers launch flags, model evidence and reasoning effort (`CLAUDE_LAUNCH_SELECTION_CODES`, `CLAUDE_MODEL_EVIDENCE_KINDS`, `CLAUDE_REASONING_EFFORTS`), is not published from the surface, and resolves no executable. Discovery is genuinely new.\n\nAMBIGUITY IS THE SUBTLE STEERING VECTOR. A caller who cannot name a path may still be able to influence which candidate the host finds — PATH ordering, a shadowing install. Refusing on more than one candidate removes that lever entirely, which is why zero-and-many must both refuse rather than the resolution picking a winner. Canonicalise and de-duplicate by real path first, so one binary reachable two ways is one candidate rather than a false ambiguity.\n\nHOST FACTS, measured earlier this session: `claude.exe` is at `C:\\Users\\Yaron\\.local\\bin\\claude.exe` and the release broker is built at `dist/windows-job-native/release/moe-windows-job-broker.exe` — note that path is relative to the WORKSPACE ROOT because the gate passes `--target-dir`; `native/target/release` does not exist and reads as a false \"not built\".",
    "risks": "1) THE STEERABLE ARGUMENT COMES BACK AS A CONVENIENCE. \"Just an optional path for unusual installs\" reintroduces exactly the seam the withheld comment protects. Drill 1 is the guard, and the unsteerability must be a TYPE-level fact — an absent parameter — rather than a runtime rejection one refactor from being relaxed.\n\n2) AMBIGUOUS RESOLUTION IS A QUIETER VERSION OF THE SAME HOLE. A caller who cannot name a binary may still shadow one on PATH. Zero and many both refuse; canonicalise and de-duplicate first so a single binary reachable two ways is not a false ambiguity.\n\n3) THE COMMENT WILL GO STALE AND MISLEAD. It currently says an observation-returning function stays internal. After this task the root publishes one. Without the rewritten distinction — discovery names no binary — the next reader concludes the withholding was abandoned and publishes the rest.\n\n4) THE PROOF MUST BE A REAL LAUNCH. DoD 2 says PROVEN \"proven by a real launch, not by a rebuilt digest\". A test that reconstructs the digest would pass while proving nothing, and it is the natural shortcut when the real broker is slow.\n\n5) THE TWO NEGATIVES MUST SURVIVE. A self-assembled quote must still refuse `CLAUDE_RUNTIME_OBSERVATION_CHANGED@RUNTIME` and a stand-in binary `CLAUDE_RUNTIME_OBSERVATION_INVALID@RUNTIME` AFTER this ships. The deliverable is that discovery is the only route to a valid quote — not that quotes stopped being checked.\n\n6) WHOLE-OBJECT EQUALITY WOULD FAIL FOR THE WRONG REASON. `factsDigest` deliberately ignores pinned paths, so asserting the whole observation survives unchanged reddens on a legitimate difference and then gets \"fixed\" by loosening the assertion that matters. Assert the three compared fields.\n\n7) MESSAGE HYGIENE. task-32eddfd3 was QA-rejected once for refusal messages echoing runtime paths; a discovery refusal naming the paths it searched leaks host layout to anyone who can trigger one.\n\n8) `export *` HIDES A LEAK FROM GREP. Negative surface assertions must read the imported namespace; drill 4 proves they do.\n\n9) THE EXPORT-COUNT FIXTURE WILL FAIL until updated, and that is it working — do not loosen the count.\n\n10) A SKIPPED PLATFORM PROOF generates zero cases and passes, which here would mean shipping the capability with its central claim untested. Assert the platform-unsupported arm off Windows, with a positive executed count either way.\n\n11) CONSUMERS: task-6cbff010 (acceptance), task-a9fd91c3 (Foundation ingress), task-44d4873e (verification receipt dispatch). All three are waiting on a reachable PROVEN launch.",
    "keyFiles": [
      "packages/runner/src/surface/claude-surface.ts",
      "packages/runner/src/providers/claude/claude-host-runtime.ts",
      "packages/runner/src/providers/claude/claude-runtime-pin.ts",
      "packages/runner/src/providers/claude/claude-runtime-request.ts",
      "packages/runner/src/providers/claude/claude-capabilities.ts",
      "packages/runner/src/index-surface.test.ts"
    ]
  },
  "budget": {
    "wallClockMs": 5400000,
    "warnedAt": "2026-08-16T14:50:18.381Z",
    "escalatedAt": "2026-08-16T14:50:18.381Z"
  },
  "planApprovedAt": "2026-08-16T13:17:12.756Z",
  "contextFetchedBy": [
    "worker-5678886b",
    "qa-ce3ba361"
  ],
  "workStartedAt": "2026-08-16T14:51:08.631Z",
  "reviewStartedAt": "2026-08-16T15:16:52.042Z",
  "verification": {
    "command": "pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\n$ vitest run --root ../.. packages/runner/src\n\n RUN  v4.1.10 D:/projexts/moe-next\n\n\n Test Files  67 passed (67)\n      Tests  2228 passed | 1 skipped (2229)\n   Start at  18:14:15\n   Duration  9.00s (transform 24.35s, setup 0ms, import 35.99s, tests 40.03s, environment 5ms)\n\nCHAIN_EXIT=0\n\n---\nAlso run separately, each with its own captured exit code:\n  pnpm --filter @moe/runner typecheck -> EXIT 0\n  pnpm --filter @moe/runner test      -> EXIT 0, Test Files 67 passed (67) / Tests 2228 passed | 1 skipped (2229)\n  pnpm typecheck (repo-wide)          -> EXIT 0, grep -c 'error TS' = 0, re-run AFTER the commit\nStep-1 baseline before this work: 66 files / 2216 tests. Count line is strictly above it.",
    "reportedAt": "2026-08-16T15:16:52.042Z"
  },
  "filesModified": [
    "packages/runner/src/surface/claude-surface.ts",
    "packages/runner/src/providers/claude/claude-runtime-pin.ts",
    "packages/runner/src/providers/claude/claude-host-runtime.ts",
    "packages/runner/src/providers/claude/claude-runtime-discovery.test.ts",
    "packages/runner/src/providers/claude/claude-runtime-discovery.ts",
    "packages/runner/src/providers/claude/claude-runtime-discovery.js",
    "packages/runner/src/surface/claude-discovery-surface.ts",
    "packages/runner/src/surface/claude-discovery-surface.js",
    "packages/runner/src/index-surface.test.ts"
  ]
}