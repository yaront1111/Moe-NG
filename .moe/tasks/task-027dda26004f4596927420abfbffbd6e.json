{
  "id": "task-027dda26004f4596927420abfbffbd6e",
  "epicId": "epic-e0c54bddb3004bf29e98b2a43cea2dc2",
  "title": "Node lander self-heals a dirty runtime-metadata tree instead of looping BASELINE_UNAVAILABLE",
  "description": "MEASURED LIVE (UnAI, 2026-09-17): goal-b2cc3b54's FIRST node.deliver looped ~5 min refusing REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE. Root cause: the node lander's baseline() (apps/daemon/src/orchestrator/repository-delivery-runtime.ts:128-141) calls git.observe(workspace) and returns null the moment ANY entry is dirty, logging e.g. `[lander] <node>: TRACKED_RUNTIME_METADATA_DIRTY (1 tracked runtime metadata path(s) changed: [\".moe-next/start.ps1\"] ...)`. node.deliver then refuses BASELINE_UNAVAILABLE and the wrapper auto-retries FOREVER (\"automatic retry after <ts>\") until a human checkpoints the tree. It only unblocked when the operator manually `git commit`ed the one dirty path.\n\nThe dirty path was `.moe-next/start.ps1`, edited earlier this session to add `--operator-stdin`. Two compounding facts:\n1. `.moe-next/start.ps1` is TRACKED, yet `.moe-next` is in .gitignore — a tracked file inside an ignored dir. The runtime-metadata git-exclude work (PR#46 era) never `git rm --cached`ed it, so any edit to it dirties the tree.\n2. The lander treats a dirty tracked runtime-metadata path as a hard baseline refusal, with no self-heal — so a routine operator edit to a runtime file wedges ALL node execution behind a human.\n\nOWNER DIRECTION (2026-09-17): \"instead of the dirty cycle, the node should commit what's needed or stash.\" i.e. the lander should checkpoint/stash the dirty RUNTIME-METADATA paths itself rather than park behind a human.\n\nDELIVERABLE OPTIONS (plan the leanest; likely BOTH — prevention + defense):\n- (a) DEFENSE: before refusing, the lander auto-checkpoints (commit) or stashes dirty paths that are on the RUNTIME-METADATA allowlist only, then re-observes and proceeds. Scope is the crux: it must NEVER auto-commit product code — only the known runtime-metadata set (.moe-next/ runtime files). A dirty PRODUCT path must still block (that dirt is the operator's real work).\n- (b) PREVENTION: untrack the runtime-metadata files that should not be in product history (`git rm --cached .moe-next/start.ps1` and siblings; keep them gitignored) so they never dirty the tree. Verify which .moe-next/* paths are tracked-but-ignored across a fresh project and the fixtures.\n\nRE-MEASURE BEFORE PLANNING: what git.observe/the lander classifies as \"tracked runtime metadata\" (the allowlist), where that set is defined, and whether option (b) alone removes the class of dirt or the lander can still see other tracked runtime paths dirty.",
  "definitionOfDone": [
    "A dirty TRACKED RUNTIME-METADATA path (e.g. an edited .moe-next/start.ps1) no longer wedges node.deliver: the lander either checkpoints/stashes it and records the baseline, or the path is untracked so it never dirties the tree. Verified against the exact live repro (edit start.ps1, claim a node, confirm BASELINE_RECORDED not BASELINE_UNAVAILABLE).",
    "Scope fence proven: a dirty PRODUCT-CODE path STILL refuses the baseline (BASELINE_UNAVAILABLE) and is NOT auto-committed. Test asserts both arms — runtime-metadata dirt self-heals, product dirt still blocks — with the exact reason code/layer (global rail: assert the code, not just outcome).",
    "If option (b): enumerate the .moe-next/* paths that are tracked while .moe-next is gitignored and untrack the runtime-metadata ones; confirm a fresh project bootstrap does not re-track them.",
    "No infinite-retry stall path remains for the runtime-metadata dirty class: the wrapper reaches a delivered/executing node or a DISTINCT actionable refusal, not an endless REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE loop."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "CONFIRM THE MEASURED STATE, then stop. Paste each command's output into the step note. (a) `git grep -n TRACKED_RUNTIME_METADATA_DIRTY` -- expect exactly the 11 lines across 5 files listed in planningNotes insight 6; if a SIXTH file appears, stop and moe.report_blocked rather than taking an unnamed path (global rail: a worker who finds an unnamed consumer mid-step must stop). (b) `sed -n '150,162p' apps/daemon/src/repository/git-landing-port.ts` -- confirm the metadata filter and the detail-only refusal. (c) `sed -n '128,141p' apps/daemon/src/orchestrator/repository-delivery-runtime.ts` -- confirm gate B returns null on `!observed.ok` OR `entries.length !== 0`. (d) In the LIVE project: `git -C D:/projexts/UnAI ls-files -- .moe .moe-next` and `grep -n moe D:/projexts/UnAI/.gitignore`. Expect the four tracked paths and the two narrow ignore lines from insight 3. THIS IS THE MEASUREMENT THAT RULES OPTION (b) OUT -- if it instead shows `.moe-next/` wholesale-ignored with accidental tracked leftovers, stop and report, because then the task's option (b) is live and this plan is wrong. Note the pre-existing foreign dirty paths in the shared moe-next checkout in your step note and never touch them.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "MEASURED AT HEAD bb56f065 (2026-09-18). All four measurements match planningNotes exactly; no sixth consumer file; option (b) confirmed RULED OUT.\n\n(a) `git grep -n TRACKED_RUNTIME_METADATA_DIRTY` -> exactly 11 SOURCE lines in 5 files (plus 4 hits inside `.moe/tasks/task-027dda26...json`, which is this task's OWN board record -- the description/plan/insights text, not a code consumer):\n  apps/daemon/src/repository/git-landing-port.ts:36,38,157            (producer)\n  apps/daemon/src/repository/git-landing-port.test.ts:46,58,89        (toMatchObject asserters)\n  apps/daemon/src/orchestrator/node-lander.ts:7,195                   (consumer, OUT OF SCOPE per step 5)\n  apps/daemon/src/orchestrator/node-lander-tracked-metadata.test.ts:59,65\n  apps/daemon/src/orchestrator/repository-delivery-runtime.test.ts:186\nNo SIXTH file. Every one is inside this plan's owned paths.\n\n(b) git-landing-port.ts:150-159 confirmed VERBATIM:\n    const all = parseStatus(status.stdout);\n    const metadata = all.filter((entry) => !entry.untracked && isMoeMetadata(entry.path));\n    if (metadata.length > 0) {\n      const paths = JSON.stringify(metadata.slice(0, 4).map((entry) => entry.path.slice(0, 120))).slice(0, 350);\n      return { ok: false, code: TRACKED_RUNTIME_METADATA_DIRTY,\n        detail: `${String(metadata.length)} tracked runtime metadata path(s) changed: ${paths}. Review these existing changes with git status --short before continuing.` };\n    }\nDetail-only refusal confirmed: paths are sliced to 4 entries / 120 chars each / 350 chars total -> UNPARSEABLE, so insight 5 holds and the additive `paths` field is required.\nClassifier at :76-78 confirmed a directory-NAME heuristic over `MOE_DIRECTORIES = new Set([\".moe\", \".moe-next\"])`, win32-lowercased per segment. NEW FINDING FOR STEP 3: `isMoeMetadata` is currently a MODULE-PRIVATE `function`, NOT exported (only `LANDER_IDENTITY` and `TRACKED_RUNTIME_METADATA_DIRTY` are). Step 3 mandates importing it rather than reimplementing, so step 3 must add the `export` keyword to the existing declaration -- one word, no logic change, no new symbol.\n\n(c) repository-delivery-runtime.ts:128-141 confirmed. Gate B is line 132: `if (!observed.ok || observed.observation.entries.length !== 0)`. The `entries.length !== 0` disjunct logs BASELINE_WORKSPACE_DIRTY and is ordinary PRODUCT dirt -- it MUST SURVIVE (DoD item 2). The `!observed.ok` disjunct is where gate A's refusal lands and is the ONLY branch step 5 touches. The existing log format is one line: `[lander] ${nodeRef}: ${observed.code} (${observed.detail}); Moe rechecks repository admission automatically.`\n\n(d) THE MEASUREMENT THAT RULES OPTION (b) OUT -- in the LIVE project D:/projexts/UnAI:\n  `git -C D:/projexts/UnAI ls-files -- .moe .moe-next` ->\n    .moe-next/README.md\n    .moe-next/node-specs/01-core-memory-identities.json\n    .moe-next/seed.ps1\n    .moe-next/start.ps1\n  `grep -n moe D:/projexts/UnAI/.gitignore` ->\n    2:.moe-next/store.sqlite*\n    3:.moe-next/*.local.*\nExactly the four tracked paths and the two NARROW ignore lines from insight 3. `.moe-next/` is NOT wholesale-ignored, so these four are NOT accidental tracked leftovers inside an ignored dir -- they are the operator's deliberately versioned project content (their node specs and launch scripts). The task description's premise (\"`.moe-next` is in .gitignore -- a tracked file inside an ignored dir\") is FALSIFIED by this measurement. `git rm --cached .moe-next/start.ps1` would strip the operator's versioned launcher from their history: data loss of intent, not a fix. DoD item 3 is conditional (\"If option (b)\") and is therefore discharged as NOT APPLICABLE, to be disclosed with this output in the completion record per step 7(D). Proceeding with option (a) only, as planned.\n\nFOREIGN DIRTY PATHS in the shared moe-next checkout: `git status --porcelain | grep -v '^.. \\.moe/'` returned NOTHING -- zero foreign PRODUCT dirt. The only dirty entries are daemon-owned board state under `.moe/` (26 modified task/role/channel files plus untracked `.moe/attempts/`, `.moe/resources/browser-e2e-gate.json`, `.moe/resources/full-suite-gate.json`), which the wrapper writes as I work. I will not touch, revert, stash or stage any of them.\n\nRead-only step: no files modified.",
      "startedAt": "2026-09-17T21:49:03.174Z",
      "completedAt": "2026-09-17T21:49:43.380Z"
    },
    {
      "stepId": "step-2",
      "description": "RED then GREEN -- make the dirty metadata paths machine-readable. In apps/daemon/src/repository/git-landing-port.test.ts first ADD a failing arm: the TRACKED_RUNTIME_METADATA_DIRTY refusal carries `paths` equal to the EXACT, FULL, sorted list of dirty tracked metadata paths -- assert the literal array for a small case, and for the existing 20-path fixture at line ~58 assert `paths.length === 20` so the truncation that limits `detail` to four entries is proven NOT to limit `paths`. Confirm RED. Then in apps/daemon/src/repository/git-landing-port.ts widen the refusal variant at :36 to carry `readonly paths?: readonly string[]` and populate it at :157 from the already-computed `metadata` array, sorted, UNTRUNCATED and UNSLICED. Leave the `detail` string byte-identical -- git-landing-port.test.ts:60 asserts `\"20 tracked runtime metadata path(s)\"` and node-lander-tracked-metadata.test.ts:59 asserts the code appears in a detail string; both must stay green untouched. The field is ADDITIVE so every existing toMatchObject still passes; confirm that by running the three consumer test files named in planningNotes insight 6. Keep this file at or below its current 257 lines -- it is already over the 250 target, so add the field and nothing else.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/git-landing-port.ts",
        "apps/daemon/src/repository/git-landing-port.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/repository/git-landing-port.ts",
        "apps/daemon/src/repository/git-landing-port.test.ts"
      ],
      "note": "RED then GREEN done. The dirty metadata path set is now machine-readable via an additive `paths` field.\n\nRED FIRST. Added three assertion sites to git-landing-port.test.ts BEFORE touching production:\n1. NEW test \"carries the full sorted dirty tracked metadata path set for a self-heal to act on\" -- a status fake feeds THREE records DELIBERATELY OUT OF ORDER (`\" M .moe-next/start.ps1\"`, `\"A  .moe/operator.ps1\"`, `\" D .moe-next/README.md\"`) and asserts the LITERAL array `[\".moe-next/README.md\", \".moe-next/start.ps1\", \".moe/operator.ps1\"]`. Out-of-order input is the point: a missing `.sort()` fails rather than being accidentally satisfied by git's own ordering. Also asserts `result.code` explicitly (`toBe`), not just `ok:false`.\n2. On the EXISTING 20-path fixture (now line ~65): `expect(result.paths).toHaveLength(20)`. That fixture's `detail` is capped at four entries / 120 chars / 350 chars, so this arm is what proves the truncation does NOT reach `paths`.\n3. On the EXISTING real-git `it.each` arm (6 cases: modified, deleted, staged addition, renamed out, renamed in, outside subtree): hoisted the already-computed expected path into `const dirtyMetadata` and added `expect(!observed.ok && observed.paths).toEqual([dirtyMetadata])` -- an EXACT single-element array against REAL git, not a fake. This is the arm that proves the self-heal will be handed exactly the metadata path and nothing else (no `src/tracked.ts`, no `operator-start.ps1`).\n\nRED OBSERVED, for the right reason: `Test Files 1 failed (1) / Tests 8 failed | 13 passed (21)`, every failure `AssertionError: expected undefined to deeply equal [ '.moe-next/tracked.ts' ]` (and siblings) at git-landing-port.test.ts:112 -- i.e. the field was absent, not a typo or an unrelated throw. 8 = 6 it.each arms + the new literal test + the 20-path fixture.\n\nGREEN, two edits, NET ZERO LINES:\n- git-landing-port.ts:36 -- widened the refusal variant with `readonly paths?: readonly string[]` plus an inline doc comment saying it is present on TRACKED_RUNTIME_METADATA_DIRTY only (NOT_A_REPOSITORY / GIT_FAILED share the variant and carry no paths). Optional and ADDITIVE, so every existing `toMatchObject` still matches.\n- git-landing-port.ts:156-158 -- renamed the existing local `paths` (the truncated JSON log fragment) to `shown` and populated the new field from the already-computed `metadata` array: `paths: metadata.map((entry) => entry.path).sort()`. UNTRUNCATED and UNSLICED -- no `.slice(0, 4)`, no `.slice(0, 120)`, no `.slice(0, 350)`; those three remain on `shown` alone.\n\n`detail` IS BYTE-IDENTICAL. Proven by the diff: the template literal's text is unchanged character for character and `${shown}` receives the identical expression the old `${paths}` did. I deliberately did NOT feed the sorted array into `detail` -- doing so would have changed which four paths the 20-path fixture shows (sorted first-four is file-0/file-1/file-10/file-11, git-order first-four is file-0/file-1/file-2/file-3). So git-landing-port.test.ts:60 (`\"20 tracked runtime metadata path(s)\"`) and node-lander-tracked-metadata.test.ts:59 (code inside a detail string) were left untouched and stay green.\n\nFILE SIZE HELD: git-landing-port.ts is still exactly 257 lines (`wc -l`), unchanged from before this step -- the field rode in on existing lines. No max-len lint rule exists in this repo (`grep -rn \"max-len|printWidth\" eslint.config.* .eslintrc* package.json` -> nothing); the type line is now the file's longest at ~330 chars, which is a style cost I accepted to honour the plan's explicit \"at or below its current 257 lines\".\n\nVERIFICATION (all three consumer test files from insight 6, one command, count line read not exit code):\n  pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/repository/git-landing-port.test.ts src/orchestrator/node-lander-tracked-metadata.test.ts src/orchestrator/repository-delivery-runtime.test.ts\n  -> `Test Files  3 passed (3)` / `Tests  41 passed (41)` -- a real nonzero executed count, not a \"No test files found\" exit-0.\n  pnpm --filter @moe/daemon typecheck -> clean (`tsc --project tsconfig.json`, no diagnostics).\n\nNOT DONE HERE (deferred to step 3, as planned): `isMoeMetadata` is still module-private. Step 3 adds the `export` keyword so the checkpoint fence imports the production classifier instead of reimplementing it.\n\nNo foreign paths touched. The `warning: in the working copy of 'keep.txt'/'remove.txt', LF will be replaced by CRLF` lines in the run output come from the suites' own temp scratch repositories, not this checkout.",
      "startedAt": "2026-09-17T21:49:48.305Z",
      "completedAt": "2026-09-17T21:52:33.568Z"
    },
    {
      "stepId": "step-3",
      "description": "The self-heal, as a NEW focused module so neither existing file grows past its cap. Create apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts exporting one function, roughly `checkpointRuntimeMetadata({git, workspace, nodeRef, paths, clock}): Promise<{outcome: string; detail: string; ok: boolean}>`. Behaviour: (1) refuse `RUNTIME_METADATA_CHECKPOINT_UNKNOWN_PATHS` if ANY supplied path is not classified as Moe runtime metadata -- this is the SCOPE FENCE and it must be enforced HERE, in production, not merely relied upon from the caller. Do NOT reimplement the classifier: export `isMoeMetadata` from git-landing-port.ts and import it, so the fence and the observer can never disagree (global rail: a property is asserted against the production surface, never a helper that reimplements it). (2) Call the EXISTING `git.commit(workspace, paths, message)` -- never write new git plumbing; it already stages by explicit nul-delimited pathspec under LANDER_IDENTITY, which is what keeps epic rail 3 (no `git add -A`, no stash, no reset) satisfied. Owner direction was \"commit what's needed or stash\": COMMIT is chosen and STASH is forbidden by epic rail 3, so a stash path must not be written. (3) Commit message names what happened and why in plain words -- that the node runtime checkpointed pre-existing runtime-metadata changes so execution could proceed, naming the node -- because this commit lands in the OPERATOR'S history and must be self-explaining and revertable. (4) On commit failure return `RUNTIME_METADATA_CHECKPOINT_FAILED` carrying git's stderr tail. (5) Re-observe; if metadata is STILL dirty return the DISTINCT `RUNTIME_METADATA_CHECKPOINT_INEFFECTIVE` rather than looping -- a silent re-loop here would just relocate the bug. On success return `RUNTIME_METADATA_CHECKPOINTED` carrying the count and the commit sha. Never log or embed a credential. Also create the one-line bridge apps/daemon/src/orchestrator/runtime-metadata-checkpoint.js containing exactly `export * from \"./runtime-metadata-checkpoint.ts\";` -- the orchestrator directory has 94 such bridges for 96 sources and an import will not resolve without it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/git-landing-port.ts"
      ],
      "newFiles": [
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts",
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts",
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.js",
        "apps/daemon/src/repository/git-landing-port.ts"
      ],
      "note": "Self-heal module created: apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts, 122 lines (well under the 250 target), plus its one-line `.js` bridge. Neither existing file grew.\n\nEXPORTED THE CLASSIFIER, DID NOT REIMPLEMENT IT. git-landing-port.ts:76 `function isMoeMetadata` -> `export function isMoeMetadata` -- one keyword, zero logic change, file still 257 lines. The checkpoint imports that exact symbol, so the fence and the `observe` call that produced the paths cannot disagree about what \"runtime metadata\" means (global rail: assert against the production surface, never a helper that reimplements it).\n\n`checkpointRuntimeMetadata({git, nodeRef, paths, workspace})` -> `{detail, ok, outcome}`. Four outcomes, each an exported `as const` string so callers and tests pin the code rather than a message:\n\n(1) SCOPE FENCE -- `RUNTIME_METADATA_CHECKPOINT_UNKNOWN_PATHS`, enforced in production before any git call. Refuses when `paths` is EMPTY or when ANY path fails `isConfinedMetadata`. That predicate is `isMoeMetadata(path)` AND no `..` segment AND not absolute. The two extra conditions are a deliberate trust-boundary guard, not speculation: `isMoeMetadata` is a per-segment directory-NAME test, so `.moe-next/../src/app.ts` CONTAINS a `.moe-next` segment and would otherwise classify as metadata while pointing at product code -- `git commit --only` would then commit `src/app.ts`. `observe`'s own output never contains such a path (git emits normalized root-relative paths), but the fence must hold for any input because it IS the fence. Absolute paths (`/x`, `C:/x`) are rejected for the same reason: they are not root-relative pathspecs. Backslash forms (`.moe-next\\..\\src\\app.ts`) already fail, since the whole string is one segment and is not in MOE_DIRECTORIES. The refusal detail names the count and the offending paths.\n\n(2) COMMIT BY REUSE, NO NEW GIT PLUMBING. Calls the existing `git.commit(workspace, paths, message)` (git-landing-port.ts:188-213), which stages via `git add --pathspec-from-file=- --pathspec-file-nul` and commits `--only` with the same nul-delimited pathspec under LANDER_IDENTITY (user Moe, gpgsign off). Explicit pathspec only -- no `git add -A`, no stash, no reset, no checkout: epic rail 3 satisfied by reuse. Per the plan I wrote NO stash path; the owner's \"commit what's needed or stash\" resolves to commit because rail 3 forbids stash.\n\n(3) COMMIT MESSAGE, plain words, first line `chore(moe): checkpoint runtime metadata before staffing <nodeRef>`. Body names the node, lists each checkpointed path one per line, states WHY (a node cannot record a landing baseline while tracked runtime metadata is dirty, so Moe checkpointed rather than waiting for a human), states the scope limit (only .moe/ and .moe-next/; product changes untouched) and says it is safe to revert. This lands in the OPERATOR'S history, so it has to explain itself without the reader knowing this task exists.\n\n(4) `RUNTIME_METADATA_CHECKPOINT_FAILED` on `!committed.ok`, carrying git's own `detail` -- which `commit` already builds with `tail()` (last 600 chars of stderr, `toWellFormed()`). No throw escapes for a git failure.\n\n(5) RE-OBSERVE, PROVEN NOT ASSUMED -> `RUNTIME_METADATA_CHECKPOINT_INEFFECTIVE`. Returned on ANY non-ok re-observe, with the detail distinguishing the two shapes: metadata still dirty (`TRACKED_RUNTIME_METADATA_DIRTY`) vs could-not-confirm (`GIT_FAILED`/`NOT_A_REPOSITORY`, code named inline). DELIBERATE DESIGN CHOICE, worth QA's attention: I did NOT report success on an unconfirmable re-observe even though the commit itself returned 0. Epic rail 4 says unverifiable evidence stays UNKNOWN and never gains authority, and claiming CHECKPOINTED there would send the caller into exactly the retry loop this module exists to end. One outcome covers both because both mean \"the checkpoint was not proven effective\"; the operator gets the distinction in the detail, which the log line carries.\n\nSUCCESS: `RUNTIME_METADATA_CHECKPOINTED` with count, the truncated path list, `committed.receipt.sha` and `committed.receipt.branch`.\n\nDEVIATIONS FROM THE PLAN'S SKETCHED SIGNATURE, both deliberate:\n- DROPPED `clock`. The plan wrote the shape as \"roughly\". Nothing in the module needs a time: git stamps the commit itself, and hand-stamping a timestamp into a durable record is a known anti-pattern here. An unused injected dependency would be dead flexibility.\n- `git` is typed `Pick<GitLandingPort, \"commit\" | \"observe\">` rather than the whole port, so the module's blast radius is visible in its own signature and a test fake needs two methods, not seven.\n\nNo credential is logged or embedded: the module only ever emits paths, counts, a sha, a branch name and git's own stderr tail.\n\nVERIFICATION: `pnpm --filter @moe/daemon typecheck` -> clean (`tsc --project tsconfig.json`, no diagnostics). `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/orchestrator/orchestrator-bridge-census.test.ts src/index-surface.test.ts",
      "startedAt": "2026-09-17T21:52:36.367Z",
      "completedAt": "2026-09-17T21:54:43.288Z"
    },
    {
      "stepId": "step-4",
      "description": "Unit-test the new module against a REAL git repository in a temp dir (the repository tests already work this way -- copy that fixture style rather than mocking git, because the whole defect lives in git's actual behaviour). In a NEW apps/daemon/src/orchestrator/runtime-metadata-checkpoint.test.ts assert each outcome by its EXACT code, never merely ok/not-ok: (1) a dirty tracked `.moe-next/start.ps1` yields RUNTIME_METADATA_CHECKPOINTED, HEAD MOVED, the working tree is clean for that path, and the commit's author is Moe (from LANDER_IDENTITY) rather than the operator -- so history shows plainly who wrote it; (2) passing a path outside the metadata classification yields RUNTIME_METADATA_CHECKPOINT_UNKNOWN_PATHS and HEAD IS UNCHANGED and `.git/index` is byte-identical -- this is the fence's own unit arm and it must prove NO EFFECT, not just a refusal; (3) a commit that cannot succeed yields RUNTIME_METADATA_CHECKPOINT_FAILED carrying git's own words. Mutation-verify arm (2), which is the load-bearing one: back up runtime-metadata-checkpoint.ts to %TEMP%/moe-task-027dda26-runtime-metadata-checkpoint-<workerId>.ts recording its SHA256 (task id in the path, per the drill rail), delete the fence check, confirm arm 2 goes RED, restore by copying the backup back, and prove the restore by SHA256 equality in a command SEPARATE from the restore. The file is new and therefore untracked, so `git diff` is blind to it and is NOT valid restore proof here. Record both hashes in the step note.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.test.ts"
      ],
      "note": "NEW apps/daemon/src/orchestrator/runtime-metadata-checkpoint.test.ts, 9 executed tests, ALL against a REAL git repository in a temp dir (no git mocking anywhere -- the fixture copies git-landing-port.test.ts's `scratchRepository` style, because the whole defect lives in git's actual behaviour). Every arm asserts the EXACT outcome code with `toBe`, never ok/not-ok.\n\nFIXTURE: real repo, `--initial-branch=trunk`, committed under the OPERATOR'S identity (`Operator <operator@example.test>`), holding one product file (`src/product.ts`) and TWO tracked runtime metadata files (`.moe-next/start.ps1`, `.moe-next/seed.ps1`).\n\nARM 1 -- success. Dirty `.moe-next/start.ps1` (the exact live repro edit, `--operator-stdin`) -> `RUNTIME_METADATA_CHECKPOINTED`, `ok === true`, HEAD MOVED (`not.toBe(head)`), the new sha appears in the detail, `git status --porcelain -- .moe-next/start.ps1` is EMPTY, and `git show --name-only --format= HEAD` is EXACTLY `.moe-next/start.ps1` -- so the commit carried nothing else with it. IDENTITY: `git log -1 --format=\"%an <%ae>\"` is EXACTLY `Moe <moe@moe.local>`, not the operator's, so history says plainly who wrote it. Subject pinned verbatim; body asserted to name the node. Plus a REVERTABILITY arm: `git revert --no-edit HEAD` under the operator's identity succeeds and leaves the path clean -- the plan's rationale for the message was \"revertable by an operator with one `git revert`\", so it is tested, not asserted.\n\nARM 2 -- THE FENCE, proving NO EFFECT. `src/product.ts` dirty and supplied -> `RUNTIME_METADATA_CHECKPOINT_UNKNOWN_PATHS`, detail names the path, HEAD UNCHANGED, `.git/index` BYTE-IDENTICAL (`readFileSync ... toEqual`), and the file is STILL ` M src/product.ts` afterwards. Nothing committed, nothing even staged.\n\nARM 2b -- the fence's hostile inputs, 5 generated cases (`it.each`), each asserting UNKNOWN_PATHS + HEAD unchanged + index byte-identical: a traversal that escapes while still containing a metadata segment (`.moe-next/../src/product.ts`), an absolute posix path, an absolute Windows path, a product path mixed in WITH a legitimate metadata path (the mixed case -- one bad path poisons the whole set, it is not silently filtered), and the empty set. Four of the five pair the bad path with a genuinely dirty `.moe-next/start.ps1`, so a fence that filtered instead of refusing would commit the good one and be caught by \"HEAD unchanged\".\n\nARM 3 -- `RUNTIME_METADATA_CHECKPOINT_INEFFECTIVE`. Both metadata files dirty, only one supplied: the commit SUCCEEDS but the class does not clear -> INEFFECTIVE, `ok === false`, detail contains \"still dirty\" AND names the surviving `.moe-next/seed.ps1`, and HEAD's commit still touches only the supplied path. This is the \"do not relocate the bug\" guarantee.\n\nARM 4 -- `RUNTIME_METADATA_CHECKPOINT_FAILED` with GIT'S OWN WORDS. `.moe-next/never-existed.ps1` passes the fence (it IS classified metadata and IS confined) but no such file exists, so REAL git refuses the pathspec: detail contains the path and the literal `did not match any files`. The module does not predict git's error -- it relays it.\n\nONE TEST-ONLY FIX during authoring: my `git` helper `.trim()`s output, so `\" M src/product.ts\"` arrives as `\"M src/product.ts\"`. Corrected the expectation to the trimmed form with a comment saying why; production untouched.\n\n=== MUTATION DRILL on ARM 2 (the load-bearing one) ===\nPRE-FLIGHT, before any mutation: `git status --porcelain -- apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts` -> `?? apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts`. UNTRACKED, so `git diff` is structurally blind to it (empty before, during and after) and is NOT valid restore proof here -- SHA256 against an out-of-tree backup is the only one.\n\nBACKUP (task id in the path per the drill rail, seat id as the suffix):\n  C:\\Users\\Yaron\\AppData\\Local\\Temp/moe-task-027dda26-runtime-metadata-checkpoint-worker-6433dce1.ts\nPRE-MUTATION SHA256 (live file and backup, measured together in the same call as the copy):\n  ec20a0cdc47cdb4b500819b31d4c20e9d0f77bf3035a51c003d6a9b572065fe0\n\nMUTATION -- deleted the fence check itself: line 79 `if (paths.length === 0 || foreign.length > 0) {` -> `if (paths.length === 0) {`. The empty-set guard survives so the mutant is minimal and targets exactly the fence. MUTANT SHA256: e9779b1f39ec36f624d8a6a002af20054f466e2e5afbf40cf476a84f04ae8f01 (proves the bytes actually changed on disk, not just that sed returned).\n\nRED OBSERVED: `Tests 5 failed | 4 passed (9)`. Arm 2 at :75 went `Expected \"RUNTIME_METADATA_CHECKPOINT_UNKNOWN_PATHS\" / Received \"RUNTIME_METADATA_CHECKPOINTED\"` -- i.e. WITHOUT the fence the module really does commit the operator's `src/product.ts`, which is precisely the damage DoD item 2 forbids. The four 2b arms went red too.\nNOTABLE, and why pinning the code (not the outcome) mattered: the `/etc/hosts` mutant arm returned `RUNTIME_METADATA_CHECKPOINT_FAILED` -- real git refused the pathspec, a SECOND refusal layer answerin",
      "startedAt": "2026-09-17T21:54:45.910Z",
      "completedAt": "2026-09-17T21:57:06.083Z"
    },
    {
      "stepId": "step-5",
      "description": "Wire the self-heal into gate A only. In apps/daemon/src/orchestrator/repository-delivery-runtime.ts baseline() (:128-141): when `observe` refuses with code TRACKED_RUNTIME_METADATA_DIRTY AND the refusal carries a non-empty `paths`, call checkpointRuntimeMetadata with those paths, log `[lander] <nodeRef>: <outcome> (<detail>)` in the existing one-line format, and on success RE-OBSERVE and fall through to the normal path so a BASELINE_RECORDED is written and the node spawns. On any non-success outcome log that DISTINCT code and return null as today -- the operator then sees an actionable line naming the repair instead of an opaque BASELINE_UNAVAILABLE repeating forever (DoD item 4). ATTEMPT THE CHECKPOINT AT MOST ONCE PER UNCHANGED DIRTY PATH-SET per node: the wrapper retries this call on a timer, and re-running a failing commit against the operator's repository every few seconds would turn one bug into a write storm. DO NOT TOUCH GATE B: `entries.length !== 0` must still return null and still log BASELINE_WORKSPACE_DIRTY, because that branch is ordinary PRODUCT dirt and auto-committing the operator's real work is exactly what DoD item 2 forbids. Keep this file under 250 lines (199 now) -- if the branch does not fit in a handful of lines, move more of it into the checkpoint module rather than growing this one. Do not change node-lander.ts:195: the lander's LANDING-time handling of the same code is a different moment with a different answer and is out of scope.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/orchestrator/repository-delivery-runtime.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/repository-delivery-runtime.ts"
      ],
      "note": "Self-heal wired into GATE A ONLY. repository-delivery-runtime.ts 199 -> 225 lines (under the 250 target). Two imports, one helper closure, four lines inside baseline(). node-lander.ts NOT TOUCHED (its :195 landing-time handling of the same code is out of scope, as the step says).\n\nTHE WIRING, baseline() at :147-160:\n      let observed = await git.observe(brief.workspace);            // was `const`\n      const dirtyMetadata = !observed.ok && observed.code === TRACKED_RUNTIME_METADATA_DIRTY ? observed.paths ?? [] : [];\n      if (dirtyMetadata.length > 0 && await checkpointMetadata(nodeRef, brief.workspace, dirtyMetadata)) {\n        observed = await git.observe(brief.workspace);              // RE-OBSERVE on success\n      }\nOn success it falls through to the UNCHANGED normal path, so `landerFor(...).baseline(nodeRef)` runs, BASELINE_RECORDED is logged and the node spawns. The guard requires BOTH the exact code AND a NON-EMPTY `paths` -- an `observe` fake that omits the new optional field yields `[]` and the branch is skipped entirely, i.e. old behaviour, fail-closed.\n\nGATE B IS UNTOUCHED, verbatim in the diff: `if (!observed.ok || observed.observation.entries.length !== 0)` and both its log branches are byte-identical. Ordinary PRODUCT dirt still returns null and still logs BASELINE_WORKSPACE_DIRTY. That is DoD item 2 and step 6 adds its asserting arm. The only reason `observed` became `let` is the re-observe; no other statement in the function changed.\n\nONCE PER UNCHANGED DIRTY PATH-SET, PER NODE (`metadataCheckpoints: Map<string, {attempt, outcome}>`, closed over by the runtime instance):\n- Key is the nodeRef; the value holds the dirty set joined on `\\0` plus the outcome of that attempt.\n- Same nodeRef + same set -> NO second `checkpointRuntimeMetadata` call, so no second commit attempt. The wrapper's timer cannot become a write storm against the operator's repository.\n- The set CHANGES -> the guard RE-ARMS automatically, because the key comparison is over the set itself, not a boolean \"tried once\". Sorted upstream by the port, so ordering is stable and a re-order cannot spuriously re-arm.\n\nDESIGN CHOICE WORTH QA'S ATTENTION (DoD item 4). A suppressed retry still LOGS, carrying the PRIOR DISTINCT outcome: `[lander] <nodeRef>: RUNTIME_METADATA_CHECKPOINT_FAILED (already attempted for this unchanged set of N runtime metadata path(s); not retried. Resolve or checkpoint them by hand.)`. Without this, pass 1 would print the actionable code and passes 2..N would print only the generic TRACKED_RUNTIME_METADATA_DIRTY line -- an operator joining late would see exactly the opaque repeating refusal this task exists to kill. So on EVERY pass the operator sees a distinct, actionable, named code, while the commit is attempted at most once. Not a silent re-loop, and not a write storm.\n\nLog format is the existing one-line `[lander] <nodeRef>: <OUTCOME> (<detail>)` shape, matching :158 and :139 exactly, so wrapper-pass-log parsing and reader habits are unchanged.\n\nVERIFICATION:\n- `pnpm --filter @moe/daemon typecheck` -> clean (`tsc --project tsconfig.json`, no diagnostics).\n- `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/orchestrator/repository-delivery-runtime.test.ts` -> `Tests 1 failed | 18 passed (19)`. EXACTLY ONE failure, and it is the EXPECTED, PLANNED inversion: repository-delivery-runtime.test.ts:183, `expected { ok: true, pid: 41328, ... } to match object { ok: false, ... }`. That is the live-repro test from insight 7 asserting the OLD behaviour (BASELINE_UNAVAILABLE, starts===0) while the fixed runtime now STAFFS THE NODE -- a real spawned pid where there was previously a refusal. That failure IS the fix, measured. Step 6 rewrites that test deliberately and adds the product-dirt and mixed-tree arms. No other test in the file moved.\n\nNo foreign paths touched.",
      "startedAt": "2026-09-17T21:57:08.784Z",
      "completedAt": "2026-09-17T21:58:55.014Z"
    },
    {
      "stepId": "step-6",
      "description": "Invert the live-repro test and add its opposing arm. In apps/daemon/src/orchestrator/repository-delivery-runtime.test.ts the existing case at :170-192, \"refuses tracked runtime metadata dirt before any seat or baseline write\", IS the live repro and currently asserts the behaviour this task removes -- BASELINE_UNAVAILABLE, starts===0, HEAD unchanged, `.git/index` byte-identical. REWRITE IT DELIBERATELY, renaming it to say it now CHECKPOINTS that dirt and proceeds, and assert: `result.ok` is true, `starts === 1`, `readLatestLandingBaseline` is NON-null, the log contains RUNTIME_METADATA_CHECKPOINTED and BASELINE_RECORDED, HEAD MOVED, and the new commit touches ONLY `.moe-next/start.ps1` (assert the committed path list, not just that a commit happened). State in a comment on that test WHY the old assertions were inverted -- they encoded \"Moe never writes the operator's history unasked\", and the owner's 2026-09-17 direction narrowed that to \"except for runtime metadata, which otherwise wedges every node behind a human\" -- so a future reader sees a recorded decision, not a weakened gate. Then ADD the opposing arm required by DoD item 2, modelled on the product-dirt case already at ~:160: with a dirty PRODUCT file (and no metadata dirt), assert the exact code REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE, `starts === 0`, the log contains BASELINE_WORKSPACE_DIRTY and NOT RUNTIME_METADATA_CHECKPOINTED, HEAD UNCHANGED and `.git/index` byte-identical -- the operator's real work must be provably untouched. Add a third arm for the MIXED tree (metadata dirt AND product dirt together): the metadata is checkpointed, the product dirt still refuses with BASELINE_UNAVAILABLE, and the product file is still dirty afterwards. The mixed case is the one a naive fix gets wrong, so it is not optional.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/orchestrator/repository-delivery-runtime.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/repository-delivery-runtime.test.ts"
      ],
      "note": "Live repro inverted deliberately, both opposing arms in place. `Test Files 1 passed (1)` / `Tests 20 passed (20)` (was 19; +1 net test).\n\nTHE INVERSION. :170-192 \"refuses tracked runtime metadata dirt before any seat or baseline write\" is now \"checkpoints tracked runtime metadata dirt and proceeds to staff the node\". The setup is UNCHANGED and hoisted into a shared `dirtyTrackedMetadata(f)` helper (commit `.moe-next/start.ps1` under the OPERATOR'S identity, then edit it) so both arms build the identical live scenario from one place. New assertions: `result.ok === true`, `starts === 1`, `readLatestLandingBaseline(...)` NOT null, the log contains RUNTIME_METADATA_CHECKPOINTED and BASELINE_RECORDED, HEAD MOVED, and -- the committed PATH LIST, not merely that a commit happened -- `git show --name-only --format= HEAD` is EXACTLY `.moe-next/start.ps1`. Added beyond the step: `git log -1 --format=\"%an <%ae>\"` is `Moe <moe@moe.local>`, so the inverted arm also pins that it is Moe's identity on the operator's history and not the operator's own.\n\nTHE RECORDED DECISION is an 8-line comment ABOVE the rewritten test, not a note buried in a step record. It states what the old assertions were (BASELINE_UNAVAILABLE, starts===0, HEAD unchanged, index byte-identical), what rule they encoded (\"Moe never writes the operator's history unasked\"), what was MEASURED on 2026-09-17 (one edited `.moe-next/start.ps1` -> ~5 minutes of BASELINE_UNAVAILABLE on the wrapper's retry timer, cleared only when a human committed the path by hand -> every node wedged behind an operator), the owner's narrowing of the rule to \"except for runtime metadata\", and that the two arms below hold the REST of the old rule in place. A future reader sees a decision with its measurement, not a gate someone quietly loosened.\n\nARM 2 -- PURE PRODUCT DIRT (DoD item 2). DELIBERATE DEVIATION FROM THE STEP TEXT, flagged for QA: the step said to ADD this arm \"modelled on the product-dirt case already at ~:160\". That case at :144-172 ALREADY asserts every clause the step lists -- exact code REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE, `starts === 0`, no baseline, BASELINE_WORKSPACE_DIRTY in the log, HEAD unchanged, `.git/index` byte-identical -- so a new test would have been a byte-for-byte duplicate of an existing one plus one assertion, at the cost of another ~15 s fixture spin-up. I instead added the ONE missing clause to it: `expect(f.logs.join(\"\\n\")).not.toContain(\"RUNTIME_METADATA_CHECKPOINTED\")`, with a comment saying the self-heal must not reach this path. Same coverage, no duplicate. If QA prefers a separately-titled arm, say so and I will split it.\n\nARM 3 -- THE MIXED TREE, a genuinely NEW test: \"refuses a dirty product path that sits beside checkpointed metadata, leaving it dirty\". Dirty tracked metadata AND dirty tracked product (`keep.txt`) together. Asserts REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE by exact code, `starts === 0`, no baseline, the log contains BOTH RUNTIME_METADATA_CHECKPOINTED AND BASELINE_WORKSPACE_DIRTY (so the metadata really was healed and the product really did still refuse -- the two-outcome interleaving is the point), the checkpoint commit touches ONLY `.moe-next/start.ps1`, `keep.txt` is STILL `M keep.txt` afterwards, and its CONTENT is byte-for-byte the operator's in-progress text. This is the case a naive fix gets wrong by sweeping the whole tree into the checkpoint.\n\nBetween arms 2 and 3 both product-dirt shapes are now covered: UNTRACKED product dirt (`operator-note.txt`, the pre-existing arm) and TRACKED product dirt (`keep.txt`, the mixed arm).\n\nNOT asserted in arm 1: HEAD unchanged / index byte-identical, obviously -- the whole point is that HEAD moves. Those two assertions live on in arms 2 and 3, which is where the old rule still applies.\n\nVERIFICATION: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/orchestrator/repository-delivery-runtime.test.ts` -> `Test Files 1 passed (1)` / `Tests 20 passed (20)`, a real nonzero executed count. Contrast with the step-5 measurement on the same file BEFORE this rewrite: `Tests 1 failed | 18 passed (19)` at :183 with `expected { ok: true, pid: 41328, ... } to match object { ok: false, ... }` -- so the inversion is not a test I bent to fit; it is the one pre-existing assertion the fix genuinely reversed, and it now reads the way the production behaviour actually is.",
      "startedAt": "2026-09-17T21:58:57.815Z",
      "completedAt": "2026-09-17T22:00:54.664Z"
    },
    {
      "stepId": "step-7",
      "description": "GATE AND ADVERSARIAL REVIEW. (A) Run, as ONE command, reading the OUTPUT and not the exit code: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/repository/git-landing-port.test.ts src/orchestrator/runtime-metadata-checkpoint.test.ts src/orchestrator/repository-delivery-runtime.test.ts src/orchestrator/node-lander-tracked-metadata.test.ts`. A leg with no `Test Files N passed` count line RAN NOTHING and is not a pass -- a bad filter prints \"No projects matched the filters\" and exits 0. Quote the count line. (B) Then the apps/** regression explicitly, because the root vitest include has NO apps/**, so `pnpm test` would prove nothing here: `pnpm --filter @moe/daemon test`, count line quoted. Attribute any red by the path-attributed baseline rule -- repo-wide failures at your merge-base, at HEAD, the delta intersected with this task's owned paths -- and report foreign red verbatim rather than hiding it or claiming a green you did not get. (C) ADVERSARIAL SELF-REVIEW on this diff: Can any path reach `git.commit` that is not runtime metadata -- including via symlink, a `..` segment, or a Windows case alias such as `.MOE-NEXT/`? Does the classifier the fence uses agree with the classifier `observe` uses, or did a copy creep in? What happens on a detached HEAD, on an unborn branch with no commits, mid-rebase, or with an index.lock held by another process -- coded refusal or raw throw? What happens when two nodes checkpoint the same workspace concurrently? Does the once-per-unchanged-path-set guard actually prevent repeated commit attempts under the wrapper's retry timer, and does it correctly RE-ARM when the dirty set changes? Is the resulting commit revertable by an operator with one `git revert`? Record each answer in the completion record. (D) Re-run the mutation drill from step 4 if you touched the fence after step 4, and disclose in the completion record that DoD item 3 (option (b), untracking) was measured NOT APPLICABLE, quoting the `git ls-files` and `.gitignore` output from step 1(d) as the reason -- do not leave a DoD item silently unaddressed.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts",
        "apps/daemon/src/orchestrator/repository-delivery-runtime.test.ts"
      ],
      "note": "GATE + ADVERSARIAL REVIEW done. Full gate table and the foreign red verbatim are in row comment comment-fcad91b958e7480489cbeda473117ecf; the detached-HEAD follow-up is comment-25f011b5d09241bbaf46310885263465.\n\n(A) `Test Files 4 passed (4)` / `Tests 52 passed (52)`. (B) `pnpm --filter @moe/daemon test` -> `Test Files 745 passed (745)` / `Tests 12311 passed | 21 skipped (12332)`, EXIT=0. Repo-wide `pnpm typecheck` exit 0. Foreign red in the root and security lanes (5 failures, 2 files) disclosed verbatim in the comment; path-attributed intersection with this row's owned paths is EMPTY and each exclusion is measured, not asserted.\n\n(C) ADVERSARIAL SELF-REVIEW -- every question answered, several by RUNNING a probe against the delivered module rather than reasoning about it. Probe ran out of tree under %TEMP% and was deleted; `git status --porcelain` afterwards shows exactly my 7 paths and nothing else, so no probe file entered the repo (epic rail 5).\n\nQ1. Can a non-metadata path reach `git.commit`?\n- `..` SEGMENT: NO. `.moe-next/../src/app.ts` contains a `.moe-next` segment and WOULD have passed a bare `isMoeMetadata`; `isConfinedMetadata` rejects any `..` segment. Unit-arm 2b case 1 asserts UNKNOWN_PATHS + HEAD unchanged + index byte-identical.\n- ABSOLUTE PATHS: NO. `/etc/hosts` and `C:/Windows/...` rejected. Both are 2b arms.\n- MIXED SET: NO. One bad path refuses the WHOLE set; the fence does not silently filter and commit the rest. 2b case 4 pairs a bad path with a genuinely dirty `.moe-next/start.ps1` and asserts HEAD unchanged, so a filtering implementation would be caught.\n- WINDOWS CASE ALIAS `.MOE-NEXT/`: ACCEPTED, and that is CORRECT, not a hole. `isMoeMetadata` lowercases each segment on win32, so `.MOE-NEXT` classifies as metadata -- and `observe` uses the SAME function, so it is also what reported the path dirty in the first place. On a case-insensitive filesystem `.MOE-NEXT/` IS the Moe metadata directory. git-landing-port.test.ts already has a win32-only arm for observe refusing that spelling.\n- BACKSLASH FORM: rejected. `.moe-next\\..\\src\\app.ts` splits to ONE segment which is not in MOE_DIRECTORIES.\n- SYMLINK: probed. A symlink inside the metadata dir commits as the entry `.moe-next/link`; git stores a symlink as its target STRING and does not follow it, so no product content is captured. A symlinked `.moe-next` itself is a single entry `.moe-next`, likewise not traversed.\n\nQ2. Fence classifier vs observe's classifier -- SAME SYMBOL, no copy. `grep -rn isMoeMetadata apps/daemon/src` returns exactly ONE definition (git-landing-port.ts:76), its two uses inside `observe` (:154 metadata filter, :160 the product filter), and ONE import in runtime-metadata-checkpoint.ts:1 used at :72. (Note for future greps: `git grep` MISSED the checkpoint's import because that file is still untracked -- plain `grep -rn` is required on a row that adds files.)\n\nQ3. Hostile git states -- CODED REFUSAL OR SUCCESS, NEVER A RAW THROW. All six probed:\n  unborn branch (zero commits) : CHECKPOINTED, branch=main -- commits the repo's first commit\n  detached HEAD               : CHECKPOINTED, branch reported \"HEAD\"  <-- the one real ceiling, see below\n  mid-rebase (conflicted)     : CHECKPOINTED (a rebase leaves HEAD detached, same class)\n  index.lock held             : CHECKPOINT_FAILED carrying `fatal: Unable to create ... index.lock`\n  symlink in metadata dir     : CHECKPOINTED, entry confined\n  NOT_A_REPOSITORY            : CHECKPOINT_FAILED via `commit`'s own `located.top === null` path\nZero raw throws across all arms -- the `catch`-free design holds because `commit`/`observe` already return results rather than throwing.\n\nQ4. TWO NODES CONCURRENTLY on one workspace -- probed with `Promise.all`. Result: n1 INEFFECTIVE / n2 FAILED, exactly ONE commit landed (`chore(moe): checkpoint runtime metadata before staffing n1`), the other path left dirty, no throw and no partial commit. git's own index.lock serialises them; the loser gets a coded refusal and the next wrapper pass re-arms on the now-smaller dirty set. No lost update. n1's INEFFECTIVE is the re-observe being conservative (n2's file was still dirty when n1 looked) -- which is the fail-closed answer, not a bug.\n\nQ5. THE ONCE-PER-UNCHANGED-SET GUARD -- was UNTESTED, which was a real coverage gap, so I ADDED a test rather than reasoning about it: \"attempts one checkpoint commit per unchanged dirty set and re-arms when that set changes\" (repository-delivery-runtime.test.ts). It spies the REAL port factory and faults only `commit`, counting attempts. Pass 1: BASELINE_UNAVAILABLE, log has RUNTIME_METADATA_CHECKPOINT_FAILED and git's stderr, commits === 1. Pass 2, same set: refused again and commits STILL === 1, log has \"already attempted for this unchanged set\" -- the wrapper's timer CANNOT become a write storm. Pass 3, after dirtying a second metadata file: commits === 2 -- the guard RE-ARMS and does not latch forever. Executed count `Tests 1 passed | 20 skipped (21)`, so ",
      "startedAt": "2026-09-17T22:00:59.315Z",
      "completedAt": "2026-09-17T22:15:50.445Z"
    }
  ],
  "planRevision": 15,
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "HUMAN",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 2,
  "comments": [
    {
      "id": "comment-25f011b5d09241bbaf46310885263465",
      "author": "worker-6433dce1",
      "content": "FOLLOW-UP ITEM for the epic-final hardening row (recorded as a comment per the DELIVERY BIAS rail, not filed as a new row).\n\nDETACHED-HEAD CEILING IN THE RUNTIME-METADATA CHECKPOINT. Measured 2026-09-18 with a throwaway probe against the delivered module (probe run out of tree under %TEMP%, deleted; no probe file entered the repo):\n\n  unborn-branch          : RUNTIME_METADATA_CHECKPOINTED ok=true  (commits the repo's first commit, branch=main)\n  detached-head          : RUNTIME_METADATA_CHECKPOINTED ok=true  branch reported as \"HEAD\"\n  mid-rebase-conflict    : RUNTIME_METADATA_CHECKPOINTED ok=true  (rebase leaves HEAD detached)\n  index-lock-held        : RUNTIME_METADATA_CHECKPOINT_FAILED     \"fatal: Unable to create ... index.lock\" (git's own words, coded, no throw)\n  symlink-inside-metadata: RUNTIME_METADATA_CHECKPOINTED ok=true  committed entry is `.moe-next/link` itself; git stores a symlink as its target string and does not follow it, so no product content escapes\n  concurrent (2 nodes)   : n1 INEFFECTIVE / n2 FAILED, ONE commit landed, other path left dirty, no throw, no partial commit\n\nTHE GAP: on a DETACHED HEAD (which includes an interrupted rebase) the checkpoint commit lands on the detached head, so `git rebase --abort` / a later `git checkout <branch>` would discard it together with the operator's metadata edit. Recoverable from the reflog for 90 days, but not from the branch.\n\nWHY IT IS NOT FIXED HERE: preventing the write needs a PRE-commit head read, and `GitLandingPort` (apps/daemon/src/repository/git-landing-port.ts:49-52) exposes only `commit` and `observe` — there is no head/branch read. A POST-commit check on `committed.receipt.branch === \"HEAD\"` is possible today but would not prevent the write, only stop the node proceeding, so it costs lines without closing the gap. Adding the port method is out of this row's scope and per-file cap (git-landing-port.ts is already 257 lines, over the 250 target).\n\nPRECEDENT WORTH NOTING for whoever picks this up: `GitPushResult` at git-landing-port.ts:59-61 ALREADY carries a `DETACHED_HEAD` refusal code, so the codebase already treats a detached head as refusal-worthy on the publish path. The natural fix is a `head(workspace)` read on the port plus a fifth outcome `RUNTIME_METADATA_CHECKPOINT_DETACHED_HEAD` refused BEFORE `git.commit`.\n\nNOT A REGRESSION THIS ROW INTRODUCES, for the record: a node was already staffable into a detached-HEAD workspace before this change whenever the tree was clean — baseline() never checked head state. This row's only new exposure is the metadata commit itself landing there. The ceiling is also recorded in the module's own header comment (runtime-metadata-checkpoint.ts:21-25) so a future reader sees it in the code, not only on the board.",
      "timestamp": "2026-09-17T22:14:32.580Z"
    },
    {
      "id": "comment-fcad91b958e7480489cbeda473117ecf",
      "author": "worker-6433dce1",
      "content": "GATE EVIDENCE with denominators, and the FOREIGN RED disclosed verbatim (kept out of completionSummary for its 2000-char cap).\n\nOWNED-PACKAGE LEGS -- ALL EXIT 0, all count lines quoted:\n(A) plan's named 4-file gate, ONE command:\n  pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/repository/git-landing-port.test.ts src/orchestrator/runtime-metadata-checkpoint.test.ts src/orchestrator/repository-delivery-runtime.test.ts src/orchestrator/node-lander-tracked-metadata.test.ts\n  -> `Test Files  4 passed (4)` / `Tests  52 passed (52)`\n(B) apps/** regression explicitly (the root vitest include has NO apps/**, so `pnpm test` proves nothing about this row):\n  pnpm --filter @moe/daemon test\n  -> `Test Files  745 passed (745)` / `Tests  12311 passed | 21 skipped (12332)`, EXIT=0, 313.55 s\n(C) pnpm --filter @moe/daemon typecheck -> exit 0; pnpm typecheck (repo-wide, every package) -> exit 0\n(D) census tripwires specifically: src/orchestrator/orchestrator-bridge-census.test.ts + src/index-surface.test.ts -> `Test Files 2 passed (2)` / `Tests 222 passed (222)` (bridge present; EXPECTED_EXPORTS still 156, i.e. the new module did NOT leak into the package barrel)\n\nFOREIGN RED, run as extra insurance beyond the plan's gate, NOT hidden and NOT claimed green:\n\n1) `pnpm test` (root lane) EXIT=1 -- `Test Files  2 failed | 464 passed | 1 skipped (467)` / `Tests  3 failed | 10695 passed | 52 skipped (10750)`\n   FAIL tests/instruction-contract/checker.test.ts > accepts the real root CLAUDE.md\n     AssertionError: expected [ { code: 'FILE_OVERSIZED', ... } ] to deeply equal []\n     detail: \"CLAUDE.md has 119 lines and 6570 bytes; limits are 40 and 2048.\"\n   FAIL tests/integration/release/release-version-surfaces.test.ts > discovers the exact nonempty current-version occurrence set in both directions\n     AssertionError: unclassified tracked current-version occurrence: expected [ ...(5) ] to deeply equal []\n     + \"apps/control-room/CLAUDE.md 8046 v0.1\"\n     + \"apps/daemon/src/cli/moe-cli-mcp.test.ts 2883 0.1.0\"\n     + \"tests/integration/CLAUDE.md 9293 v0.1\"\n     + \"tools/packaging/CLAUDE.md 6705 v0.1\"\n     + \"tools/packaging/CLAUDE.md 6790 v0.1\"\n   FAIL (same file) > goes red for an unclassified tracked current-version occurrence\n\n2) `pnpm test:security` EXIT=1 -- `Test Files  1 failed | 11 passed (12)` / `Tests  2 failed | 1002 passed (1004)`\n   FAIL integrity-boundaries.security.ts > CUTOVER_V2_AUTHORITY_LAYER AFTER / RACE\n     Error: refusal code mismatch: expected CUTOVER_V2_NOT_ACTIVE, observed CUTOVER_V2_STATUS_UNKNOWN\n     at hostile-harness.ts:262 via integrity-boundaries.security.ts:296 and :320\n\nATTRIBUTION (path-attributed baseline: failing paths at HEAD minus at merge-base, intersected with this row's owned paths). INTERSECTION IS EMPTY. Measured, not asserted:\n- Every implicated input file is BYTE-IDENTICAL to its committed HEAD blob: `git status --porcelain -- CLAUDE.md README.md apps/control-room/CLAUDE.md apps/daemon/src/cli/moe-cli-mcp.test.ts tests/integration/CLAUDE.md tools/packaging/CLAUDE.md` returned EMPTY. Their last commits are 1cb6a82a / 82ebebce / b51935a7, none of them mine.\n- The version scanner reads TRACKED files only (`release-version-surfaces.test.ts:289,300` spawn `git ls-files -z` and `git ls-files --deleted -z`), so my three NEW files are structurally invisible to it.\n- My four MODIFIED tracked files ARE in `git ls-files`, so the scanner does read them -- and `grep -nE '0\\.1\\.0|v0\\.1'` over all seven of my paths returns NO MATCHES (exit 1). They cannot contribute an occurrence.\n- `grep -rln 'git-landing-port|repository-delivery-runtime|runtime-metadata-checkpoint' tests/security/` returns NOTHING (exit 1): the security lane never references any path this row owns.\n- The cutover red has a named, dated cause that is not this row: the 2026-09-18 fault-as-absence sweep fixed `cutover-v2-authority` so an UNREADABLE marker store now refuses CUTOVER_V2_STATUS_UNKNOWN instead of CUTOVER_V2_NOT_ACTIVE. The hostile census still pins the old code. That is a census that must follow reality, owned by that work.\n- `tests/security/boundary-roster.security.ts` (the 90-row layer census that a new apps/daemon/src file or a bare `layer:` literal would red) PASSED: the new module declares no `layer:` literal and exports no `*_LAYER`.\n\nNone of these five failures is in a package this row owns, and none is reachable from its diff. Disclosed as required; no green claimed that was not measured.",
      "timestamp": "2026-09-17T22:14:58.619Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-17T20:39:33.812Z",
  "updatedAt": "2026-09-17T22:22:52.612Z",
  "metrics": {
    "firstClaimAt": "2026-09-17T20:52:06.923Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 7
  },
  "contextFetchedBy": [
    "architect-73d103f8",
    "worker-6433dce1"
  ],
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-17T20:56:12.361Z",
  "planningNotes": {
    "codebaseInsights": "RE-MEASURED AT HEAD 2026-09-17. The description's root-cause chain is CORRECT; its premise about WHY the file is tracked is WRONG, and that kills option (b).\n\n(1) THE TWO GATES. There are two, not one. GATE A = `git-landing-port.ts:154-159 observe()`: `const metadata = all.filter(e => !e.untracked && isMoeMetadata(e.path))`; if non-empty it returns `{ok:false, code:TRACKED_RUNTIME_METADATA_DIRTY, detail}` -- a HARD refusal, no baseline. GATE B = `repository-delivery-runtime.ts:131-137 baseline()`: returns null when `!observed.ok` OR `observed.observation.entries.length !== 0`. Gate B is what still refuses ordinary PRODUCT dirt (logged BASELINE_WORKSPACE_DIRTY), and it MUST SURVIVE -- DoD item 2 is exactly gate B. Null from baseline() -> `repository-delivery-coordinator.ts:125 deliveryRefusal(\"REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE\")` -> `wrapper-pass-log.ts:35` prints \"automatic retry after <ts>\" forever.\n\n(2) THE CLASSIFIER IS A DIRECTORY-NAME HEURISTIC. `git-landing-port.ts:70,76-78`: `MOE_DIRECTORIES = new Set([\".moe\", \".moe-next\"])` and `isMoeMetadata(path)` = any path SEGMENT is one of those (lowercased on win32). So every tracked file anywhere under `.moe-next/` is \"runtime metadata\", whatever it actually is.\n\n(3) OPTION (b) IS WRONG AND MUST NOT BE PLANNED. Measured in the live repo: `cd D:/projexts/UnAI && git ls-files -- .moe .moe-next` returns FOUR paths -- `.moe-next/README.md`, `.moe-next/node-specs/01-core-memory-identities.json`, `.moe-next/seed.ps1`, `.moe-next/start.ps1`. And `grep -n moe .gitignore` returns only `.moe-next/store.sqlite*` and `.moe-next/*.local.*`. So `.moe-next/` is NOT wholesale gitignored, and those four files are the OPERATOR'S OWN DELIBERATELY VERSIONED PROJECT CONTENT: their node specs and their launch scripts. They are not tracked-by-accident leftovers. `git rm --cached .moe-next/start.ps1` would strip the operator's versioned launcher out of their history going forward -- that is data loss of intent, not a fix. DoD item 3 is conditional (\"If option (b)\") and this measurement discharges it as NOT APPLICABLE. Corollary: `runtime-metadata-excludes.ts:80-86` only checks tracked-ness for `moe.config.json` and the store files (refusing RUNTIME_METADATA_TRACKED); it deliberately never checks the `.moe`/`.moe-next` DIRS, it only writes ignore patterns for them -- and ignore patterns do not affect tracked files. So there is no existing prevention surface to extend, either.\n\n(4) THE SELF-HEAL PRIMITIVE ALREADY EXISTS AND IS RAIL-COMPLIANT. `GitLandingPort.commit(workspace, paths, message)` (git-landing-port.ts:51, impl :188-213) stages with `git add --pathspec-from-file=- --pathspec-file-nul` and commits with `commit --only --pathspec-from-file=- --pathspec-file-nul` under `LANDER_IDENTITY` (:72, user Moe, gpgsign off). EXPLICIT PATHSPEC, never `add -A` -- so epic rail 3 is satisfied by reuse, with no new git surface to write. `repository-delivery-runtime.ts:84` already binds `const git = createGitLandingPort()`, so both `observe` and `commit` are in scope at the baseline() seam.\n\n(5) THE PATHS ARE NOT MACHINE-READABLE TODAY. The refusal carries only `detail: string` -- `git-landing-port.ts:157-158` formats at most FOUR paths, each sliced to 120 chars, whole string sliced to 350. A self-heal cannot parse that. The port must additionally return the path array. This is the one shape change in the task.\n\n(6) SHAPE-CONSUMER ENUMERATION for that change (rail: grep the SHAPE, not the symbol). `git grep -n TRACKED_RUNTIME_METADATA_DIRTY` over the whole tree returns exactly 11 lines in 5 files: git-landing-port.ts:36,38,157 (producer); git-landing-port.test.ts:46,58,89 (`toMatchObject({ok:false, code})`); node-lander.ts:7,195 (consumer); node-lander-tracked-metadata.test.ts:59,65; repository-delivery-runtime.test.ts:186. `git grep -n 'tracked runtime metadata path'` finds the ONE detail-string asserter: git-landing-port.test.ts:60 `expect(result.detail).toContain(\"20 tracked runtime metadata path(s)\")`. Every one of these is inside this plan's owned paths. Because the new field is ADDITIVE and the detail string is unchanged, no existing `toMatchObject` breaks.\n\n(7) THE LIVE REPRO IS ALREADY A TEST, AND IT ASSERTS THE OPPOSITE. `repository-delivery-runtime.test.ts:170-192`, \"refuses tracked runtime metadata dirt before any seat or baseline write\", builds the EXACT live scenario -- commits `.moe-next/start.ps1`, edits it, starts the runtime -- and asserts `REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE`, `starts === 0`, no baseline, HEAD unchanged, and `.git/index` BYTE-IDENTICAL. Inverting this test is the central act of this task and must be done deliberately, not incidentally.\n\nFILE SIZES: git-landing-port.ts 257 (over the 250 target, under 400 -- do not grow it much); repository-delivery-runtime.ts 199; node-lander.ts 284. The self-heal therefore goes in a NEW small module, not inline. The orchestrator directory carries 94 `.js` bridges for 96 `.ts` sources, so a new `.ts` there needs i"
  },
  "planApprovedAt": "2026-09-17T20:56:14.489Z",
  "workStartedAt": "2026-09-17T21:49:03.174Z",
  "reviewStartedAt": "2026-09-17T22:22:52.610Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  745 passed (745)\n      Tests  12311 passed | 21 skipped (12332)\n   Duration  300.34s (transform 26.46s, setup 0ms, import 338.14s, tests 792.86s, environment 35ms)\nEXIT=0\n(no FAIL lines)",
    "reportedAt": "2026-09-17T22:22:52.610Z",
    "source": "agent-reported"
  },
  "filesModified": [
    "apps/daemon/src/repository/git-landing-port.ts",
    "apps/daemon/src/repository/git-landing-port.test.ts",
    "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.ts",
    "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.js",
    "apps/daemon/src/orchestrator/runtime-metadata-checkpoint.test.ts",
    "apps/daemon/src/orchestrator/repository-delivery-runtime.ts",
    "apps/daemon/src/orchestrator/repository-delivery-runtime.test.ts"
  ],
  "completionSummary": "DELIVERED option (a) only; option (b) measured NOT APPLICABLE. Full gate table + foreign-red attribution in comment-fcad91b958e7480489cbeda473117ecf; detached-HEAD follow-up in comment-25f011b5d09241bbaf46310885263465.\n\nDoD 1 — a dirty tracked `.moe-next/start.ps1` no longer wedges node.deliver. `baseline()` (repository-delivery-runtime.ts:147-160) now checkpoints that dirt and records the baseline. The live repro was ALREADY a test asserting the opposite (repository-delivery-runtime.test.ts:170-192); it is rewritten and asserts ok=true, starts===1, baseline NON-null, RUNTIME_METADATA_CHECKPOINTED + BASELINE_RECORDED logged, HEAD moved, the commit touching EXACTLY `.moe-next/start.ps1`, author `Moe <moe@moe.local>`, and revertable by one real `git revert`. An 8-line comment records WHY the old assertions were inverted (owner direction 2026-09-17), so it reads as a decision, not a loosened gate.\n\nDoD 2 — scope fence proven BOTH arms, by exact code. Product dirt still refuses REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE at gate B (`entries.length !== 0`, byte-identical in the diff) and asserts NOT RUNTIME_METADATA_CHECKPOINTED, HEAD unchanged, `.git/index` byte-identical. NEW mixed-tree arm: metadata checkpointed AND product dirt still refuses AND the product file still dirty with its original bytes. The fence is enforced in PRODUCTION (new runtime-metadata-checkpoint.ts, 128 lines) via the IMPORTED `isMoeMetadata` — not a reimplementation — plus `..`-segment and absolute-path rejection, because `.moe-next/../src/app.ts` would otherwise classify as metadata. 9 unit arms against REAL git, each pinning its exact outcome code. MUTATION-VERIFIED twice: deleting the fence makes it commit `src/product.ts` (5 red). Restores proven by SHA256 (untracked file, so `git diff` is invalid proof): 74cd5ae8…828f91 before = after.\n\nDoD 3 — NOT APPLICABLE, measured not skipped. `git -C D:/projexts/UnAI ls-files -- .moe .moe-next` → 4 paths; `.gitignore` carries only `.moe-next/store.sqlite"
}