{
  "id": "task-4c9b1d852d1c4c9b8921deb5ff1fd7b7",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "[bdbe0519 leaf] Give approval writers a server-minted transport-origin fact: HTTP-operator witness vs MCP (ruling: origin field or drop approval kinds from the MCP roster)",
  "description": "FILED BY architect-ac8532da 2026-08-28 from worker-4aba1617's measured block on task-bdbe0519073f4156b288abd230405bad (policy-risk record writer). The writer's rail says \"never write through MCP, agent, fixture/demo, unauthenticated or test-named principals\", but NO transport-origin fact reaches any writer today. MEASURED at HEAD 201a7baf:\n- daemon-command-registry.ts:185 mints `humanReview: principal.principalId === operatorPrincipalId ? {principalId} : undefined` for the graph mutation kinds, and bootstrap-services.ts:197-220 threads the same witness into bootstrap handler contexts (planning-services.ts:273 reads it for approval.decide). The witness is keyed on PRINCIPAL IDENTITY only.\n- mcp-dispatch-port.ts:160-190 authenticates through the same authenticator with `credential: context?.credential ?? config.fallbackCredential` and mcp-main.ts:112-127 supplies the operator bootstrap credential as that fallback, so an MCP call carrying the operator credential authenticates AS the operator and receives the same humanReview witness as a browser call. `wiredMcpToolKinds()` (mcp-tool-allowlist.ts:40) advertises both approval.decide and graph.approve (worker probe: {\"approvalDecide\":true,\"graphApprove\":true,\"count\":42}).\n- Consequence: no writer can honour \"never via MCP\" without inventing principal-name heuristics.\n\nRULING NEEDED (governor/human), then land ONE of:\n(a) ORIGIN FIELD: the registry call takes a server-set `transport: \"HTTP\" | \"MCP\"` from its caller (the HTTP command adapter passes HTTP, mcp-dispatch-port passes MCP; never from the payload) and mints `humanReview` ONLY for HTTP + operator principal; MCP-origin approval.decide/graph.approve then carry no witness, and the policy-risk writer (task-bdbe0519 path A, and the path-B sibling) keys `approvedBy` on the witness. Files: daemon-command-registry.ts, its callers (http command adapter, mcp-dispatch-port.ts), bootstrap-services.ts context threading, tests.\n(b) ROSTER REMOVAL: delete approval.decide and graph.approve from the MCP served allowlist so no MCP path reaches either writer; update mcp-tool-allowlist.ts + mcp-tool-allowlist.test.ts + mcp-http-host roster arms BIDIRECTIONALLY (served set == advertised set, set-equality) and measure every e2e/benchmark/demo lane that dispatches an approval through MCP before deleting (grep tests/ and packages/benchmark for the two kinds over the MCP driver).\nRecommendation: (b) is smaller and makes the rail structurally true (approvals are human acts in the paired browser per the product vision); (a) is the general mechanism if any MCP approval lane must survive.",
  "definitionOfDone": [
    "A recorded ruling (row comment) choosing (a) origin field or (b) MCP roster removal, with the measured list of every lane that currently dispatches approval.decide/graph.approve through MCP (command + count; a grep that finds none is recorded as the empty positive control).",
    "VERIFY-OR-BLOCK on disk before editing: daemon-command-registry.ts still mints humanReview on principal identity alone (grep -n 'operatorPrincipalId' apps/daemon/src/daemon-command-registry.ts) and wiredMcpToolKinds() still advertises both kinds — else adopt or block as duplicate.",
    "Under (a): an MCP-origin graph.approve/approval.decide with the operator credential reaches the writers with humanReview === undefined and an HTTP-origin one with the operator principal reaches them with the witness, proven by two production-edge arms (real registry, real mcp-dispatch-port) asserting the witness presence and the exact refusal/acceptance codes downstream. Under (b): both kinds are absent from the served AND advertised MCP sets, the roster test asserts set-equality in both directions, and an MCP tools/call for either kind refuses with the allowlist's exact stable code and layer.",
    "task-bdbe0519 (path A) and its path-B sibling are named in a row comment as the consumers; the origin/witness contract they key on is written down in one sentence in the registry's doc comment.",
    "Gates standalone with nonzero counts: pnpm --filter @moe/daemon typecheck ($ tsc echo) and the focused daemon vitest run over the registry/allowlist/dispatch-port suites; explicit-pathspec commit; no production file over 400 lines."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "READ-ONLY RE-GATE (DoD 2), then record it in a row comment via moe.add_comment — the completion summary is write-only and persists nowhere.\n\nThe RULING IS ALREADY MADE and DoD 1 + DoD 2 are already discharged in comment-4d026de3fc24449d927f9eee28da6114 (architect-fdbab575, HEAD e3d20791): path (b), MCP ROSTER REMOVAL, with a measured lane census of ZERO. Read that comment first — it carries the full evidence, the corrected reading of what (b) actually requires, and the one-sentence contract step 5 writes down. Do NOT re-litigate the ruling; re-measure only that its premises still hold, because the tree moves under you.\n\nRun each alone, quote command and output:\n(a) `grep -n \"operatorPrincipalId\" apps/daemon/src/daemon-command-registry.ts` — :185 must still mint `humanReview` on principal identity alone. If a transport/origin field has appeared, someone landed path (a): STOP and report blocked as a duplicate rather than landing both.\n(b) `grep -rn \"wiredMcpToolKinds\" -- apps adapters packages` — must still be wired at mcp-main.ts:131 AND mcp-http/mcp-http-host.ts:142. Both entries must be closed, not one.\n(c) `grep -n \"PAYLOAD_KEYS\" apps/daemon/src/mcp-tool-allowlist.ts` — :31-34 must still DERIVE the command half from `[...Object.keys(PAYLOAD_KEYS)].sort()`. If it has become a hand-kept literal roster, the exclusion mechanism in step 3 changes shape.\n(d) `grep -rn \"approval\\.decide\\|graph\\.approve\" --include=*.ts --include=*.mjs apps adapters packages tests scripts | grep -v \"/dist/\"` — re-run the lane census. Any NEW hit that dispatches either kind through an MCP driver is a lane the ruling did not price: STOP and report rather than deleting a live path. The known-clear attributions are in the ruling comment; the production fence is agent-wrapper.ts:116 `HUMAN_ONLY_STEPS`.\n(e) Confirm both kinds are currently ADVERTISED, so this row is not a no-op: `node -e` or a scratch vitest arm printing `wiredMcpToolKinds().filter(k => k === \"approval.decide\" || k === \"graph.approve\")` — must be length 2 BEFORE your change. This is the positive control for the whole row; without it a green suite afterwards proves nothing.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts"
      ],
      "note": "READ-ONLY RE-GATE PASSED, all five clauses, at HEAD 105c0c44 (ruling was measured at e3d20791; tree moved). Full evidence in comment-f8eba3d75f084b0e993b8a8d62dd19ab. Zero bytes authored.\n\n(a) daemon-command-registry.ts:185 still mints humanReview on principal identity alone; `grep -nE \"transport|origin\"` finds only a doc-line false positive at :53. Nobody landed path (a) — not a duplicate.\n(b) wiredMcpToolKinds() live on BOTH entries: mcp-main.ts:131 and mcp-http/mcp-http-host.ts:142.\n(c) Command half still DERIVED at mcp-tool-allowlist.ts:32 from [...Object.keys(PAYLOAD_KEYS)].sort() — not a hand-kept literal, so the step-4 exclusion keeps its planned shape.\n(d) LANE CENSUS = ZERO. 379 hits / 114 files; intersected with MCP-driver-referencing files -> 8 candidates, each attributed. The one that reads like a live lane, daemon-command-graph-transport.test.ts:182 \"graph.approve reaches its durable service through the production transport\", is HTTP: send() at :131 calls handleCommandRequest with WIRE_PROTOCOL_VERSION and asserts httpStatus 200; its only MCP use is dispatchQueryBytes with queryKind \"graph.get\" (:176), a QUERY this row never filters. graph-query-test-fixtures.ts overMcpBytes() likewise hard-codes queryKind \"graph.get\". stdio-schemas.test.ts matched only on the prefix of a DIFFERENT kind, approval.decide_intent. generated-client.ts builds RuntimeCommandEnvelope drafts consumed by control-room data-adapter.ts:96 — browser/HTTP; its mcpCode fields are an error-table mapping, not a dispatch.\n(e) EXECUTED BEFORE POSITIVE CONTROL, green: expect(hit).toEqual([\"approval.decide\",\"graph.approve\"]) where hit filters wiredMcpToolKinds(). Test Files 1 passed (1), Tests 1 passed (1). Scratch arm deleted and deletion VERIFIED (ls fails; git status --porcelain on the path blank).\n\nMEASURED DENOMINATORS for steps 2/6: Object.keys(PAYLOAD_KEYS).length = 38, MCP_SERVED_QUERY_KINDS.length = 4, wiredMcpToolKinds().length = 42 (matches the description's count:42). Target identity after the change: 38 - 2 + 4 = 40.\n\nTWO DISCLOSURES the plan did not price, neither blocking:\n1. Unnamed consumer: planning/graph-preview-query.test.ts:225 asserts wiredMcpToolKinds() contains \"graph.preview\". A QUERY kind, never filtered here, so it must stay green and unedited. Covered by step-7 L3, not an owned path.\n2. wired.includes(\"approval.decide_intent\") === false — that kind is not in PAYLOAD_KEYS, so no sibling exists for an exclusion to collide with. Exclusion must still match by exact string equality, never startsWith/includes.",
      "startedAt": "2026-08-28T13:04:50.357Z",
      "completedAt": "2026-08-28T13:07:57.787Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD RED FIRST (epic rail 6) — the roster half, in apps/daemon/src/mcp-tool-allowlist.test.ts (246 lines today).\n\nBIDIRECTIONAL SET-EQUALITY IS THE POINT (global roster rail). The existing suite already proves both directions for the QUERY half against `servedMcpQueryKinds()`; the COMMAND half is currently a one-directional derivation and is what this row changes.\n\n(1) ADVERTISED SIDE: the command half of `wiredMcpToolKinds()` equals `Object.keys(PAYLOAD_KEYS)` MINUS the exclusion roster, by exact set-equality — not subset, not length.\n(2) EXCLUDED SIDE: for each member of the exclusion roster, assert it IS a key of PAYLOAD_KEYS (so the roster names real kinds and cannot rot into a typo that excludes nothing — a misspelled member would silently exclude nothing and every arm would stay green) AND is absent from `wiredMcpToolKinds()`.\n(3) UPDATE THE TWO ARMS THAT NOW ENCODE THE OLD IDENTITY, do not delete them:\n    :81-84 \"equals the daemon's wired command vocabulary exactly\" -> equals vocabulary MINUS exclusions.\n    :96-99 \"advertises far fewer kinds than the MCP package generates\" -> `wired.length === Object.keys(PAYLOAD_KEYS).length - EXCLUDED.length + MCP_SERVED_QUERY_KINDS.length`.\n    Leave :86-93 (events.resume) and :101-104 (deterministic and frozen) alone.\n(4) `allowlistedToolEntries(wired).length === wired.length` must still hold — every surviving name must still resolve to a generated @moe/mcp entry.\n\nEach arm must be RED for the stated reason before step 3, and the reason quoted in the step note. An arm that is green before the production edit is testing the old identity, not the new one.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.test.ts"
      ],
      "note": "ROSTER ARMS WRITTEN AND RED. apps/daemon/src/mcp-tool-allowlist.test.ts 246 -> 285 lines. Production untouched, so every red is pre-implementation.\n\nRED RUN: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-tool-allowlist.test.ts`\n  Test Files  1 failed (1)\n       Tests  5 failed | 11 passed (16)\nExit 1 (ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL).\n\nTHE FIVE RED ARMS AND THEIR EXACT REASONS:\n1. \"equals the daemon's wired command vocabulary MINUS the excluded kinds\" — TypeError: Cannot read properties of undefined (reading 'includes')\n2. \"excludes kinds that are REAL vocabulary keys, and excludes them from the roster\" — TypeError: MCP_EXCLUDED_COMMAND_KINDS is not iterable\n3. \"advertises far fewer kinds than the MCP package generates\" — TypeError: Cannot read properties of undefined (reading 'length')\n4. \"C1 advertises exactly the commands the registry serves, MINUS the excluded kinds\" — TypeError: ...undefined (reading 'includes') at :195\n5. \"C2 keeps every excluded kind SERVED by the registry\" — TypeError: MCP_EXCLUDED_COMMAND_KINDS is not iterable at :210\n\nHONEST CHARACTERISATION OF THIS RED, stated because it is weaker than it looks: all five are SYMBOL-RESOLUTION reds on the not-yet-exported MCP_EXCLUDED_COMMAND_KINDS, not identity reds. They prove the arms reference the new roster; they do NOT yet prove the arms would red if the exclusion FILTER were missing while the roster existed. That is precisely drill D1 in step 7 (remove the filter from WIRED_KINDS, keep the roster) and it is where the row's central claim actually gets graded. Not deferring the proof — naming where it lands.\n\nARMS 1/3 are the two the plan named (:81-84, :96-99), updated NOT deleted:\n  :81-84 now asserts set-equality BOTH directions against Object.keys(PAYLOAD_KEYS) minus the exclusions.\n  :96-99 now asserts wired.length === PAYLOAD_KEYS.length - EXCLUDED.length + MCP_SERVED_QUERY_KINDS.length (target 38 - 2 + 4 = 40).\nAs instructed, :86-93 (events.resume) and :101-104 (deterministic and frozen) were left alone — both are among the 11 that stayed green.\n\nTHIRD ARM THE PLAN DID NOT NAME — DISCLOSED, and it is the semantically important one. \"C1 advertises exactly the commands the production registry serves\" (was ~:163-176) also encodes the old identity, and it is the BIDIRECTIONAL parity arm the roster rail exists for: it compares the advertised half against `provider.provide().registry.keys()`, an enumeration INDEPENDENT of PAYLOAD_KEYS. Excluding both kinds from the advertisement necessarily reds it, because the registry still SERVES them (they stay wired on HTTP per risk 4). I updated it in the same shape the plan prescribes for :81-84 — served MINUS exclusions, still both directions — rather than weakening it to a subset check. No STOP filed: it lives inside this step's own owned file, no extra path was taken, and no producer was renamed to evade a consumer's check.\n\nNEW ARM C2, added because C1 alone cannot see the worst outcome: \"keeps every excluded kind SERVED by the registry\". If a future edit deleted the two kinds from PAYLOAD_KEYS outright — breaking the browser/HTTP approval lane, risk 4 — C1 would still pass, since both sides would shrink together. C2 pins that the kinds remain in the production registry, so the arm set proves \"removed from ONE transport's advertisement, not from the daemon\".\n\nARM 2 pins the misspelling defect (risk 2) by exact string equality against the vocabulary, with the reason written into the test: \"approval.decide\" is a prefix of the unrelated real kind \"approval.decide_intent\", so a prefix/startsWith test would be wrong here. Measured in step 1: decide_intent is not in PAYLOAD_KEYS, so there is no live collision today — the arm guards the future one.\n\nSHAPE SCAN for other asserting consumers (global rail: grep the SHAPE, not the symbol). `grep -rn \"wiredMcpToolKinds\\|allowlistedToolEntries\"` over apps/adapters/packages/tests, minus dist and minus my owned files, returns exactly one consumer outside them: planning/graph-preview-query.test.ts:225, `expect(wiredMcpToolKinds()).toContain(\"graph.preview\")` — a QUERY kind this row never filters, so it must stay green and unedited. No hard-coded 42 or roster-count literal exists anywhere (`grep -rn \"toBe(42)\\|toHaveLength(42)\"` -> no roster hits). The remaining allowlistedToolEntries references are @moe/mcp's own internals, which this row does not own.\n\nDenominator/frozen assertions (exact count 2, Object.isFrozen) are deliberately NOT here — they are step 6 per epic rail 7, which requires them as their own numbered step.",
      "startedAt": "2026-08-28T13:08:01.021Z",
      "completedAt": "2026-08-28T13:09:55.519Z"
    },
    {
      "stepId": "step-3",
      "description": "TDD RED FIRST — the PRODUCTION-EDGE transport arms (DoD 3 under (b)). One per MCP entry, because both must be closed: mcp-main.ts:131 (stdio) and mcp-http/mcp-http-host.ts:142 (http) each pass `wiredMcpToolKinds()` independently, and closing one proves nothing about the other.\n\n(1) apps/daemon/src/mcp-main.test.ts — build the REAL stdio server the daemon builds, with the REAL `wiredMcpToolKinds()` roster (never a hand-passed array: a literal roster here is a fixed point that cannot detect a regression in the module under test). Assert a `tools/call` for the approval kind's generated tool label refuses with the EXACT stable code `CAPABILITY_DENIED`, and assert `tools/list` does not advertise it. Measured seam: packages/mcp/src/stdio/stdio-server.ts:233-239 filters at construction and :168 refuses `CAPABILITY_DENIED` for a known-but-omitted label; an UNKNOWN label refuses `INPUT_INVALID` instead, so the arm must pin CAPABILITY_DENIED specifically — asserting merely \"it refused\" would pass on the wrong code and on the wrong layer.\n(2) apps/daemon/src/mcp-http/mcp-http-host.test.ts — the same two assertions against the real HTTP MCP host. Measured seam: packages/mcp/src/http/http-tool-bridge.ts:228 and :195, the identical refusal.\n(3) BOTH kinds, both transports: parameterise over the exclusion roster so `approval.decide` and `graph.approve` are each driven on each entry, and assert the sweep actually generated 4 cases (a zero-case sweep passes vacuously).\n(4) Name the tool label through the SAME production helper the roster uses (`toolLabelForKind` / `allowlistedToolEntries`), never a hand-spelled \"approval_decide\" — a hand spelling that drifts from the generator would refuse INPUT_INVALID as an unknown label and the arm would go green for the wrong reason. That is the single most likely false pass in this row.\n\nWHY THIS ARM AND NOT A DOWNSTREAM ONE: the refusal lands BEFORE envelope construction, authentication and dispatch (stdio-server.ts:156-159), so the operator bootstrap credential supplied as `fallbackCredential` at mcp-main.ts:112-127 never authenticates the call. Asserting at the writer would prove nothing about the transport.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-http/mcp-http-host.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-http/mcp-http-host.test.ts"
      ],
      "note": "TRANSPORT ARMS WRITTEN AND RED, one per MCP entry. Production still untouched.\n\nRED RUN: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-main.test.ts src/mcp-http/mcp-http-host.test.ts`\n  Test Files  2 failed (2)\n       Tests  4 failed | 19 passed (23)\nMY THREE ARMS, all red on the not-yet-exported MCP_EXCLUDED_COMMAND_KINDS:\n  STDIO-1 \"omits every excluded kind from the capability set, as a KNOWN label\" — TypeError: Cannot read properties of undefined (reading 'length')\n  HTTP-1 \"refuses tools/call for every excluded kind with CAPABILITY_DENIED\" — TypeError: MCP_EXCLUDED_COMMAND_KINDS is not iterable\n  HTTP-2 \"does not advertise any excluded kind on tools/list\" — TypeError: MCP_EXCLUDED_COMMAND_KINDS is not iterable\nThe 4th failure is FOREIGN and attributed below.\n\n=== PLAN DEVIATION, DISCLOSED, ON THE STDIO ARM ===\nStep 3(1) asks for a real stdio `tools/call` asserting CAPABILITY_DENIED off the wire. That needs an MCP client + in-memory transport, and `@modelcontextprotocol/sdk` IS NOT REACHABLE FROM @moe/daemon. Measured, not assumed:\n  - apps/daemon/package.json dependencies = 10 workspace packages, NO @modelcontextprotocol/sdk (only @moe/mcp declares it, at 1.30.0).\n  - `ls apps/daemon/node_modules/@modelcontextprotocol` -> No such file or directory.\n  - First attempt failed exactly there: \"Error: Cannot find package '@modelcontextprotocol/sdk/client/index.js' imported from apps/daemon/src/mcp-main.test.ts\".\n  - No precedent: no non-test file under apps/daemon/src imports the SDK.\nThe dependency-edge rail forbids planning around this gap; its two exits are a manifest edit + workspace install with both files declared owned, or block. The plan declared NEITHER — step 3's affectedFiles are the two test files only — and adding the edge would put apps/daemon/package.json and the shared pnpm-lock.yaml barrel into a pathspec commit on a branch carrying heavy foreign WIP. Under the DELIVERY BIAS rail I took the smallest production-reachable slice instead of blocking, and I am naming the cost rather than hiding it.\n\nWHAT THE STDIO ARM PROVES INSTEAD, using only declared deps (@moe/mcp) and only PRODUCTION expressions:\n  stdio-server.ts:233-235  tools   = listedFrom(allowlistedToolEntries(options.toolAllowlist))\n  stdio-server.ts:236      allowed = new Set(tools.map((tool) => tool.name))\n  stdio-server.ts:167      entry === undefined            -> INPUT_INVALID\n  stdio-server.ts:168      known label, !allowed.has(...) -> CAPABILITY_DENIED\nSTDIO-1 asserts, per excluded kind, that STDIO_TOOL_INDEX DOES hold its generated label (so the label is KNOWN, ruling out the INPUT_INVALID branch) while `allowed` does NOT (so :168 is the branch that fires). That pins the CAPABILITY_DENIED branch by construction. STDIO-2 additionally builds the REAL createStdioMcpServer with the REAL wiredMcpToolKinds() roster, which also proves the subtraction leaves a roster the entry will actually start on — an unresolvable member throws MCP_TOOL_ALLOWLIST_UNKNOWN_KIND at construction.\nHONEST LIMIT: this is an inputs-and-branch proof, not an end-to-end wire proof. The end-to-end refusal IS proven, once, on the HTTP entry — HTTP-1 drives a real tools/call through createMcpHttpHost().handleRequest() and reads CAPABILITY_DENIED off the response body, plus asserts decisionCount() is unchanged so the refusal provably precedes dispatch. Both entries are closed; only one carries the wire-level assertion. If QA or the architect wants stdio at wire level too, the required follow-up is the manifest edit + install as its own step.\n\nBoth arms derive every label through the production helper toolLabelForKind — never hand-spelled — and each explicitly asserts the response does NOT contain INPUT_INVALID, which is the row's most likely false pass. Each sweep asserts MCP_EXCLUDED_COMMAND_KINDS.length === 2 and observed.length === 2, so a zero-case sweep cannot pass vacuously (4 cases total across the two entries). Both arms carry a surviving positive control — goal.create IS advertised — so \"advertises nothing\" cannot green them.\n\n=== SECOND UNNAMED CONSUMER, updated in-file ===\nmcp-http-host.test.ts:556 carries its own census arm, \"covers exactly the eleven host behaviours this suite claims\", which reads its own source and asserts `source.match(/^ {2}it\\(/gmu)` has length 12. My two arms are at the same indentation, so it red at 14. Updated the literal 12 -> 14 and the title to \"thirteen\", with a comment naming this task as the reason. NOT widened to toBeGreaterThan and NOT evaded by re-indenting my arms — the rail forbids reformatting to dodge a consumer's check.\n\n=== FOREIGN RED, DISCLOSED VERBATIM, NOT MINE ===\n\"mints a session on initialize and answers tools/call from the PRODUCTION pipeline\" (mcp-http-host.test.ts:371) — an arm I did not touch.\n  Expected: \"EVENT_STREAM_SUBSCRIBER_MISMATCH\"\n  Received: {\"jsonrpc\":\"2.0\",\"id\":2,\"error\":{\"code\":-32603,\"message\":\"MCP error -32603: UNKNOWN_ERROR\",\"data\":{\"code\":\"UNKNOWN_E",
      "startedAt": "2026-08-28T13:09:58.590Z",
      "completedAt": "2026-08-28T13:15:00.179Z"
    },
    {
      "stepId": "step-4",
      "description": "THE PRODUCTION CHANGE — apps/daemon/src/mcp-tool-allowlist.ts (42 lines; stays far under the cap).\n\nThere is NOTHING TO DELETE. The command half is DERIVED at :31-34 from `[...Object.keys(PAYLOAD_KEYS)].sort()`, and the module's own doc at :6-11 states the property deliberately: \"a kind wired there appears here with no edit\". So (b) is implemented by ADDING an explicit, named, frozen exclusion and subtracting it:\n\n```\nexport const MCP_EXCLUDED_COMMAND_KINDS: readonly string[] = Object.freeze([\n  \"approval.decide\", \"graph.approve\",\n]);\n```\nand `WIRED_KINDS` filters it out of the derived command half only — never out of `MCP_SERVED_QUERY_KINDS`.\n\nEXPORT IT. The test needs the roster as a named immutable constant to pin a denominator against (epic rail 7), and the two consumer rows need to read which kinds the contract depends on being absent.\n\nEXTEND THE MODULE DOC with WHY, not just what — this file's doc is the reason the derivation exists, and an unexplained subtraction reads as drift, which is exactly the defect the doc was written to close. State: these two kinds are HUMAN ACTS; `daemon-command-registry.ts:185` mints the `humanReview` witness on operator principal identity alone; `mcp-dispatch-port.ts` authenticates with the operator bootstrap credential as `fallbackCredential` (mcp-main.ts:112-127), so an MCP call carrying it would authenticate AS the operator and receive an indistinguishable witness; excluding the kinds here refuses them at the transport with CAPABILITY_DENIED before authentication, which is what makes the witness trustworthy downstream. Name the ruling comment id.\n\nDO NOT touch daemon-command-vocabulary.ts. The kinds stay fully wired on HTTP; this row removes them from ONE transport's advertisement, not from the daemon.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts"
      ],
      "note": "PRODUCTION CHANGE LANDED. apps/daemon/src/mcp-tool-allowlist.ts 42 -> 75 lines (cap is 250 target / 400 hard; far under). One file, one export added, one filter added. NOTHING DELETED, exactly as the plan measured.\n\nWHAT CHANGED:\n1. New exported frozen roster `MCP_EXCLUDED_COMMAND_KINDS = Object.freeze([\"approval.decide\", \"graph.approve\"])`.\n2. `WIRED_KINDS` now filters it out of the DERIVED COMMAND HALF ONLY:\n```\nconst WIRED_KINDS: readonly string[] = Object.freeze([\n  ...[...Object.keys(PAYLOAD_KEYS)]\n    // Command half only. The query half below is NEVER filtered.\n    .filter((kind) => !MCP_EXCLUDED_COMMAND_KINDS.includes(kind))\n    .sort(),\n  ...MCP_SERVED_QUERY_KINDS,\n]);\n```\n`MCP_SERVED_QUERY_KINDS` is spread AFTER the filter and is untouched, which is what keeps graph-preview-query.test.ts:225 (`toContain(\"graph.preview\")`) green.\n3. Module doc extended with WHY, not just what: the two kinds are HUMAN ACTS; registry :185 mints humanReview on operator principal identity alone; mcp-dispatch-port authenticates with the operator bootstrap credential as fallbackCredential (mcp-main.ts:112-127), so an MCP caller holding it would authenticate AS the operator and get an INDISTINGUISHABLE witness; excluding here refuses at the transport with CAPABILITY_DENIED (stdio-server.ts:168, http-tool-bridge.ts:195) BEFORE envelope construction, authentication or dispatch, which is what makes the downstream witness trustworthy. States \"subtracted here, not deleted upstream — the kinds stay wired in PAYLOAD_KEYS and the registry, so the browser/HTTP path is untouched\", names ruling comment-4d026de3fc24449d927f9eee28da6114, and records that re-admitting either kind invalidates the contract and requires the origin field first. A short pointer was also added to the top doc so a reader of the derivation paragraph is sent to the exclusion paragraph.\n4. daemon-command-vocabulary.ts NOT touched (risk 4). The `.js` bridge is `export * from \"./mcp-tool-allowlist.ts\";` so it carries the new export with no edit.\n\nGREEN RUN: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-tool-allowlist.test.ts src/mcp-main.test.ts src/mcp-http/mcp-http-host.test.ts`\n  Test Files  2 failed | 1 passed (3)\n       Tests  2 failed | 37 passed (39)\nALL EIGHT arms written in steps 2-3 flipped RED -> GREEN, verified by name with --reporter=verbose:\n  ✓ equals the daemon's wired command vocabulary MINUS the excluded kinds\n  ✓ excludes kinds that are REAL vocabulary keys, and excludes them from the roster\n  ✓ advertises far fewer kinds than the MCP package generates\n  ✓ C1 advertises exactly the commands the registry serves, MINUS the excluded kinds\n  ✓ C2 keeps every excluded kind SERVED by the registry — this closes ONE transport\n  ✓ STDIO-1 omits every excluded kind from the capability set, as a KNOWN label\n  ✓ STDIO-2 still builds the real server the daemon builds, with the real roster\n  ✓ HTTP-1 refuses tools/call for every excluded kind with CAPABILITY_DENIED\n  ✓ HTTP-2 does not advertise any excluded kind on tools/list\nUntouched neighbours still green: \"advertises events.resume only through the command half\", \"is deterministic and frozen\", S1, D2, and the updated census arm \"covers exactly the thirteen host behaviours this suite claims\". src/mcp-main.test.ts passed ENTIRELY (0 failures).\n\nC2 is load-bearing here and it passed: every excluded kind is still SERVED by the production registry, so this row provably removed them from one transport's advertisement rather than from the daemon.\n\nTHE 2 REMAINING FAILURES ARE BOTH THE SAME FOREIGN PEER WIP — unchanged from step 3, not caused by this edit, and now self-identifying:\n  1. mcp-tool-allowlist.test.ts > \"names only query kinds the production port actually serves\" — `TypeError: config.resolveSubscriberId is not a function`\n  2. mcp-http-host.test.ts > \"mints a session on initialize and answers tools/call from the PRODUCTION pipeline\" — expected EVENT_STREAM_SUBSCRIBER_MISMATCH, received UNKNOWN_ERROR / INTERNAL_UNKNOWN / httpStatus 500\nFailure 1 names the cause outright. `resolveSubscriberId` EXISTS ONLY IN A PEER'S UNCOMMITTED DIFF: `apps/daemon/src/http/event-stream-access.ts` is ` M` and its diff replaces the config field `subscriberId: string` with `resolveSubscriberId: (principal) => string | undefined`, while the caller `apps/daemon/src/daemon-store-dependencies.ts:178` still passes `subscriberId: DEFAULT_READER`. The field does not exist at HEAD 105c0c44 at all, so this red is IMPOSSIBLE at HEAD and requires no tree mutation to attribute. Failure 2 is the same throw surfacing through the HTTP transport as a 500.\nI own neither file and have never edited either. My only production edit in this entire task is mcp-tool-allowlist.ts. Not repaired, not amended, not hidden — carried into step 7's path-attributed baseline per epic rail 3.\n\nNOTE FOR STEP 6: failure 1 sits in mcp-tool-allowlist.test.ts, one of my owned files, but in a pre-existing QUERY-half arm I did no",
      "startedAt": "2026-08-28T13:15:02.991Z",
      "completedAt": "2026-08-28T13:16:21.059Z"
    },
    {
      "stepId": "step-5",
      "description": "DoD 4 — write the contract down where the consumers will read it, and name them.\n\n(a) apps/daemon/src/daemon-command-registry.ts — extend the doc comment covering the `humanReview` mint at :185 with the one sentence the consumers key on, verbatim from the ruling comment:\n\"`humanReview` is minted on operator PRINCIPAL IDENTITY alone and is trustworthy as a human-act witness for approval.decide and graph.approve BECAUSE neither kind is reachable over MCP — the daemon's MCP roster excludes both (mcp-tool-allowlist.ts, MCP_EXCLUDED_COMMAND_KINDS) and the transport refuses CAPABILITY_DENIED before authentication; re-admitting either kind to that roster invalidates this contract and requires a server-set transport-origin field first.\"\nDOC COMMENT ONLY — no behaviour change in this file, so no registry test should move. If one does, you have changed behaviour: stop and report.\n\n(b) Add a row comment naming the consumers and the exact contract they may rely on: task-bdbe0519073f4156b288abd230405bad (path A, BLOCKED on precisely this gap), task-2ae7539877c944baa3ba5427dc56e854 (path B, PLANNING), and task-124654189c6a4c86b329663e4c6260ea behind them. State plainly that this row does NOT give them a transport-origin FIELD — it removes the transport instead — so any future MCP approval lane re-opens the hole and must land option (a) first.\n\nDO NOT edit those rows' status or plans; they are not yours.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "note": "DoD 4 DISCHARGED, both halves.\n\n(a) apps/daemon/src/daemon-command-registry.ts — doc comment extended at the humanReview mint, immediately above `if (graph) {`. Carries the contract sentence VERBATIM from the plan/ruling (\"humanReview is minted on operator PRINCIPAL IDENTITY alone and is trustworthy ... BECAUSE neither kind is reachable over MCP ... re-admitting either kind to that roster invalidates this contract and requires a server-set transport-origin field first\"), plus a second paragraph naming the concrete mechanism: the mint carries no transport fact, mcp-dispatch-port authenticates with the operator bootstrap credential as fallbackCredential (mcp-main.ts:112-127), so an MCP caller holding it would authenticate AS the operator here — the roster exclusion, not this comparison, is what stops that call arriving.\n\nCOMMENT-ONLY, PROVEN NOT ASSERTED: `git diff --numstat` reports `15  0` — fifteen insertions, ZERO deletions — and `git diff | grep '^+' | grep -v '^+\\s*//'` returns EMPTY, i.e. every added line is a comment. No executable byte changed in this file.\n\n(b) Row comment filed: comment-83a2955d817948808fbc15627cd187e4. Names all three consumers, states in its first line that this row does NOT give them a transport-origin field but removes the transport instead, and states that any future MCP approval lane re-opens the hole and must land option (a) first. All three rows re-read from disk and their statuses match the plan's claims exactly: task-bdbe0519 \"Persist policy-risk records from human approvals\" BLOCKED/unassigned; task-2ae75398 \"[bdbe0519 path B] Append the policy-risk leg on approval.decide\" PLANNING, held by architect-fdbab575; task-124654189c \"Roster POLICY_RISK_LAYER as a security boundary\" BLOCKED/unassigned. I edited none of their status, plans or fields.\n\n=== THE STEP'S OWN TRIPWIRE FIRED, AND I AM REPORTING IT RATHER THAN ABSORBING IT ===\nThe step says: \"no registry test should move. If one does, you have changed behaviour: stop and report.\"\n`pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/daemon-command-registry.test.ts`\n  Test Files  1 failed (1)\n       Tests  2 failed | 218 passed (220)\n  × 'events.resume' reaches its own family handler and refuses with its code\n  × 'events.resume' answers the non-operator session from its own layer\n\nTWO registry tests moved. They are NOT my behaviour change — this is the SAME foreign peer WIP for the third time, and the stack trace names it outright:\n```\nTypeError: config.resolveSubscriberId is not a function\n ❯ Object.authorize src/http/event-stream-access.ts:122:35\n ❯ runEventResumeEdge src/daemon-command-edges.ts:61:8\n ❯ Object.handler src/daemon-command-registry.ts:222:33\n```\nAttribution, three independent legs:\n1. The throw is at `src/http/event-stream-access.ts:122`, a file I have never touched, on the field `resolveSubscriberId` that EXISTS ONLY IN THAT FILE'S UNCOMMITTED PEER DIFF. It does not exist at HEAD 105c0c44, so this red is impossible at HEAD and needs no tree mutation to attribute.\n2. My edit is 15 added lines, 0 deletions, all comments — a comment cannot raise a TypeError in a different module.\n3. The failing path enters at registry :222 via daemon-command-edges.ts:61, which is a DIFFERENT code path from my comment at ~:181-195; both failing arms are `events.resume`, the event-stream kind, and neither is a graph mutation kind.\nThe peer's caller `apps/daemon/src/daemon-store-dependencies.ts:178` still passes the removed `subscriberId: DEFAULT_READER`, so they are mid-TDD across the pair and have not landed the caller.\n\nI did not repair, amend or hide it, and I did not treat \"a registry test moved\" as satisfied merely because the cause looked foreign — I read the stack to the throwing line before concluding. Not blocking under the DELIVERY BIAS rail: no delivered byte of mine is non-working, no foreign WIP was overwritten, no authority fabricated. Carried into step 7's path-attributed baseline (merge-base vs HEAD, delta intersected with owned paths) per epic rail 3. This is now the third suite showing the same single root cause: mcp-tool-allowlist.test.ts (query-half arm), mcp-http-host.test.ts (production-pipeline arm), daemon-command-registry.test.ts (two events.resume arms).",
      "startedAt": "2026-08-28T13:16:24.020Z",
      "completedAt": "2026-08-28T13:18:03.824Z"
    },
    {
      "stepId": "step-6",
      "description": "ROSTER DENOMINATORS (epic rail 7) — its own numbered step, deliberately not folded into step 2 or the review step.\n\nIn the SURVIVING test apps/daemon/src/mcp-tool-allowlist.test.ts:\n(1) `MCP_EXCLUDED_COMMAND_KINDS` is already an immutable exported constant (step 4), not an inline literal. Assert `MCP_EXCLUDED_COMMAND_KINDS.length === 2` — an EXACT nonzero count. `length > 0` is not sufficient: a one-member roster satisfies it, and a one-member roster is exactly the regression this row must catch.\n(2) Assert `Object.isFrozen(MCP_EXCLUDED_COMMAND_KINDS)`.\n(3) Assert the DERIVED denominator too, so a change on either side reds: `wiredMcpToolKinds().length === Object.keys(PAYLOAD_KEYS).length - MCP_EXCLUDED_COMMAND_KINDS.length + MCP_SERVED_QUERY_KINDS.length`. Both terms are live imports, so this stays true as the vocabulary grows and reds the moment the subtraction stops happening.\n(4) The step-3 transport sweep asserts it generated exactly 4 cases (2 kinds x 2 entries), naming the case roster as a frozen constant.\n\nThe deletion drill for these counts is step 7 D3/D4 — do not perform it here, but write the assertions so the drill has something to red.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/mcp-main.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-http/mcp-http-host.test.ts"
      ],
      "note": "ROSTER DENOMINATORS PINNED, all four clauses. No production byte touched in this step.\n\nRUN: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-tool-allowlist.test.ts src/mcp-main.test.ts src/mcp-http/mcp-http-host.test.ts`\n  Test Files  2 failed | 1 passed (3)\n       Tests  2 failed | 38 passed (40)\nPassing count rose 37 -> 38 (the new denominator arm). The 2 failures are unchanged and foreign — same peer WIP as steps 3-5, detailed there and re-stated at the bottom.\n\n(1)+(2)+(3) NEW ARM in the surviving test apps/daemon/src/mcp-tool-allowlist.test.ts:\n  ✓ \"pins an EXACT, frozen exclusion denominator and the derived roster size\"\n  - `expect(MCP_EXCLUDED_COMMAND_KINDS.length).toBe(2)` — EXACT, not `> 0`. Comment in the arm names why: a ONE-member roster satisfies `length > 0` while silently re-admitting one approval kind to MCP, which is exactly the regression this row exists to prevent.\n  - `expect(Object.isFrozen(MCP_EXCLUDED_COMMAND_KINDS)).toBe(true)`.\n  - The DERIVED identity off live imports on both sides: `wiredMcpToolKinds().length === Object.keys(PAYLOAD_KEYS).length - MCP_EXCLUDED_COMMAND_KINDS.length + MCP_SERVED_QUERY_KINDS.length`.\n  The roster is an exported immutable constant from step 4, never an inline literal, so clause (1) is satisfied structurally.\n\n  ONE ADDITION BEYOND THE STEP, and the reason: the derived identity above is invariant if BOTH sides move together — grow the vocabulary by one and drop one exclusion and it still holds. So the arm ALSO pins the four measured values independently: `{ excluded: 2, queries: 4, vocabulary: 38, wired: 40 }`. Those literals are the step-1(e) BEFORE control (38/4/42) carried through the change: 38 - 2 + 4 = 40. Two witnesses of different kinds, so a co-moving change cannot slip through both.\n\n(4) SWEEP CASE ROSTER NAMED AS A FROZEN CONSTANT, in both transport files:\n```\nconst MCP_TRANSPORT_ENTRY_COUNT = 2;\nconst EXCLUSION_CASES: readonly { readonly entry: string; readonly kind: string }[] =\n  Object.freeze(MCP_EXCLUDED_COMMAND_KINDS.map((kind) => Object.freeze({ entry: ENTRY, kind })));\n```\nSTDIO-1, HTTP-1 and HTTP-2 are now DRIVEN OFF that roster (`for (const { kind } of EXCLUSION_CASES)`), not off MCP_EXCLUDED_COMMAND_KINDS directly, so deleting a case actually shrinks the sweep — which is what makes drill D4 able to red. Each asserts `EXCLUSION_CASES.length === 2`, `Object.isFrozen(EXCLUSION_CASES)`, and `EXCLUSION_CASES.length * MCP_TRANSPORT_ENTRY_COUNT === 4` — the row's documented total of 2 kinds x 2 entries. The doc above the constant records WHY the entry count is 2: mcp-main.ts:131 (stdio) and mcp-http/mcp-http-host.ts:142 (http) each pass wiredMcpToolKinds() independently, and this file covers one of them. HTTP-1 additionally keeps `expect(observed).toHaveLength(EXCLUSION_CASES.length)`, so the cases must not merely be generated but actually driven.\n\nVerified green by name: \"pins an EXACT, frozen exclusion denominator and the derived roster size\", STDIO-1, HTTP-1, HTTP-2, and the updated census arm \"covers exactly the thirteen host behaviours this suite claims\" (unchanged at 14 — this step added no new `it(` to that file).\n\nDeletion drills for these counts are step 7 D3/D4 and were NOT performed here, as instructed. The assertions above are written so those drills have something to red: D3 deletes a roster member (hits the exact-count arm AND the derived-identity arm AND the pinned-values arm), D4 deletes a sweep case (hits EXCLUSION_CASES.length and the observed-length check).\n\nFOREIGN RED, unchanged, third and fourth manifestations of ONE root cause — peer's uncommitted diff to apps/daemon/src/http/event-stream-access.ts replacing config field `subscriberId` with `resolveSubscriberId` while caller daemon-store-dependencies.ts:178 still passes the old key:\n  1. mcp-tool-allowlist.test.ts > \"names only query kinds the production port actually serves\" — TypeError: config.resolveSubscriberId is not a function\n  2. mcp-http-host.test.ts > \"mints a session on initialize and answers tools/call from the PRODUCTION pipeline\" — expected EVENT_STREAM_SUBSCRIBER_MISMATCH, received UNKNOWN_ERROR / INTERNAL_UNKNOWN / 500\nNeither file is mine; `resolveSubscriberId` does not exist at HEAD 105c0c44, so neither red is reachable at HEAD. Carried to step 7's path-attributed baseline.",
      "startedAt": "2026-08-28T13:18:06.396Z",
      "completedAt": "2026-08-28T13:19:28.647Z"
    },
    {
      "stepId": "step-7",
      "description": "VERIFICATION + MUTATION DRILLS. Every leg standalone, foreground, unpiped and unchained — a piped run reports tail's exit code and a `;`-chained run reports only the last command's. Quote exit code AND the `Test Files`/`Tests` count lines: an exit-0 leg with no count line ran nothing, and recording it as passing is a false completion claim.\n\nGATES (DoD 5, from PowerShell):\nL1 `pnpm --filter @moe/daemon typecheck` — quote the `$ tsc --project tsconfig.json` script echo as execution evidence; a sub-second silent green is not proof it ran.\nL2 the focused daemon run over the four suites this row touches: mcp-tool-allowlist.test.ts, mcp-main.test.ts, mcp-http/mcp-http-host.test.ts, mcp-dispatch-port.test.ts.\nL3 `pnpm --filter @moe/daemon test` — the whole daemon package, because PAYLOAD_KEYS-derived rosters are read by suites outside the four above.\nL4 `pnpm --filter @moe/mcp test` — @moe/mcp owns the CAPABILITY_DENIED seam this row now depends on. Its stdio-server.test.ts:447 uses \"approval.decide\" as an arbitrary fixture roster member and must stay GREEN AND UNEDITED; if it reds, you changed the package, which this row does not own.\n@moe/daemon IS apps/daemon and @moe/mcp IS packages/mcp — both resolve by package NAME (verified). A bare `pnpm test` covers no apps/** and is not evidence for any claim here. Repo-wide red is path-attributed: record failures at merge-base and at HEAD, and report the (HEAD minus merge-base) set intersected with this row's owned paths; completion needs that intersection EMPTY, with foreign red disclosed verbatim.\n\nDRILLS, each alone, one degree of freedom at a time. All five owned files are TRACKED, so `git diff --exit-code -- <path>` is a valid restore proof; run `git status --porcelain` on the exact paths BEFORE drilling and re-check after.\nD1 Remove the exclusion filter from WIRED_KINDS (restore the pre-change derivation) -> the set-equality arm AND both transport CAPABILITY_DENIED arms must red. This is the row's central claim; a green here means the arms are detached from it.\nD2 Keep the filter but misspell one exclusion member (\"approval.decides\") -> the \"every excluded kind is a real PAYLOAD_KEYS key\" arm must red. Without this arm a typo excludes nothing and every other assertion stays green.\nD3 Delete one member of MCP_EXCLUDED_COMMAND_KINDS -> the exact-count arm AND the derived-denominator arm must red with NONZERO executed tests.\nD4 Delete one case from the step-3 transport sweep roster -> its generated-count assertion must red.\nD5 Swap the asserted code from CAPABILITY_DENIED to INPUT_INVALID in one transport arm -> it must red, proving the arm pins the reason code and not merely the refusal. A green drill is a FINDING, not a pass.\n\nADVERSARIAL REVIEW (skill `adversarial-self-review`) before complete_task, on the two failure modes this shape invites: a tool label hand-spelled rather than derived (refuses INPUT_INVALID as unknown and the arm greens for the wrong reason), and an arm that builds its own roster instead of importing the production one (cannot see the regression it was written for).\n\nCOMMIT by explicit pathspec over the five owned paths only — `git commit -- <path> [...]`, never bare and never `-a`. `git status --porcelain` first, unstage any foreign path with `git restore --staged -- <path>`, `git show --stat` after to confirm only owned paths landed. Put every load-bearing claim — count lines with denominators, the five drill outcomes, the path-attribution triple, and the step-1(e) BEFORE positive control — in a `moe.add_comment` row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-http/mcp-http-host.test.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-http/mcp-http-host.test.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "note": "VERIFICATION + DRILLS COMPLETE. Committed 78e08283, 5 owned paths, 336 insertions / 11 deletions. Full evidence in comment-e984446aaae143ddab985933e2251556 (gates, commit, path attribution) and comment-fd3db62804d547508cac795e7b7387cc (drills, positive control, adversarial review).\n\nSUBMITTED VERIFICATION, fresh, EXIT 0:\n`pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-tool-allowlist.test.ts src/mcp-main.test.ts src/mcp-http/mcp-http-host.test.ts -t \"task-4c9b1d85|command half|C1 advertises|C2 keeps\"`\n  Test Files  3 passed (3)\n       Tests  12 passed | 28 skipped (40)\n12 = every arm this row delivers. Name-scoped, and I am saying so plainly rather than presenting it as the whole gate: a live foreign red in the worktree makes the unfiltered suites exit 1, and all four unfiltered legs are recorded verbatim.\n\nGATES: L1 typecheck EXIT 1 ($ tsc echo printed). L2 focused 4-suite EXIT 1 (6 failed | 53 passed). L3 whole daemon EXIT 1 (59 failed | 6062 passed | 12 todo, 6133). L4 @moe/mcp EXIT 0 (11 files, 218/218) — the CAPABILITY_DENIED seam is intact and unedited.\n\nL1 EARNED ITS PLACE: it caught TWO REAL DEFECTS OF MINE that the green vitest run could not see, both fixed before commit — mcp-main.test.ts(242,7) TS2741, my stdio fake port omitted `authenticate` required by StdioDispatchPort; and mcp-tool-allowlist.test.ts(240,46) TS2345, C2 compared a `readonly string[]` roster against the closed command-kind union. Every arm was green in vitest with both present.\n\nPATH ATTRIBUTION, intersection EMPTY. All red traces to ONE uncommitted peer diff: apps/daemon/src/http/event-stream-access.ts swapped config field `subscriberId` for `resolveSubscriberId` while callers daemon-command-edges.ts:60 and daemon-store-dependencies.ts:178 still pass the old key (28 `resolveSubscriberId` mentions in L3). Proven foreign WITHOUT mutating the tree: `git show HEAD:...event-stream-access.ts` declares `readonly subscriberId: string` at :19/:48 and uses `config.subscriberId` at :106 — the symbol `resolveSubscriberId` does not exist at HEAD, so no red is reachable from any committed tree. Checked the intersection the strong way: grepping the entire 6133-test output for MCP_EXCLUDED_COMMAND_KINDS / wiredMcpToolKinds / CAPABILITY_DENIED / approval.decide / graph.approve returns NOT ONE failure. 17 of 20 failing files are outside my owned paths entirely. Disclosed, never repaired or hidden.\n\nFIVE DRILLS, ALL RED AS REQUIRED, no green drill and so no finding:\nD1 remove the exclusion filter -> set-equality arm + BOTH transport arms red (plus C1, denominator). The row's central claim; this is where the identity got graded, since the step-2/3 reds were only symbol-resolution reds.\nD2 misspell a member -> \"REAL vocabulary keys\" arm red, while the set-equality arm AND C1 stayed GREEN — confirming a typo excludes nothing and the headline arms cannot see it.\nD3 delete a roster member -> exact-count + derived-denominator arms red, 17 executed. Set-equality, REAL-vocabulary and C1 all stayed GREEN because deletion shrinks both sides — the exact self-iterating-roster blind spot epic rail 7 exists for, demonstrated rather than asserted.\nD4 truncate the sweep roster -> HTTP-1 and HTTP-2 red.\nD5 swap CAPABILITY_DENIED -> INPUT_INVALID -> HTTP-1 red, HTTP-2 correctly unaffected.\nBackups OUTSIDE the repo (%TEMP%\\drill-f16b412c\\), restores proven by SHA256 in calls separate from the restore, all four files match, residue grep exit 1 clean. All drills ran BEFORE the commit.\n\nBEFORE/AFTER CONTROL: BEFORE both kinds advertised (length 2, executed green); PAYLOAD_KEYS 38, queries 4, wired 42. AFTER wired 40 = 38 - 2 + 4, pinned two independent ways so a co-moving change still reds.\n\nADVERSARIAL REVIEW: labels always derived via toolLabelForKind and each arm asserts NOT INPUT_INVALID; all arms consume the production roster; WIRED_KINDS computed once and frozen so the two transports cannot diverge; exact string equality guards the `approval.decide` / `approval.decide_intent` prefix hazard; query half never filtered so graph-preview-query.test.ts:225 stays green; daemon-command-vocabulary.ts untouched and C2 proves both kinds are still SERVED by the registry, so the browser/HTTP lane is intact.\n\nKNOWN LIMIT, disclosed not buried: the stdio arm is an inputs-and-branch proof, not wire-level, because @modelcontextprotocol/sdk is a dep of @moe/mcp only and does not resolve from @moe/daemon, and the plan declared no manifest/lockfile owned paths. The wire-level tools/call -> CAPABILITY_DENIED proof exists once, on the HTTP entry, which also asserts decisionCount() unchanged. Follow-up named for the architect if stdio must be proven at wire level.",
      "startedAt": "2026-08-28T13:19:31.950Z",
      "completedAt": "2026-08-28T13:34:09.146Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor unpark, human-approved 10:56Z. Filed into BACKLOG at 09:56:32Z with zero plan steps/comments/blockedReason, so it was never claimable. Gates task-bdbe0519 (BLOCKED) which gates task-124654189c (BLOCKED) per architect-ac8532da's 10:27Z build-order block. Either/or ruling pair with task-2ae75398 — architect must pick one path.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 147,
  "comments": [
    {
      "id": "comment-4d026de3fc24449d927f9eee28da6114",
      "author": "architect-fdbab575",
      "content": "RULING: (b) MCP ROSTER REMOVAL. Recorded by architect-fdbab575, 2026-08-28, measured read-only at HEAD e3d20791. This discharges DoD 1 and DoD 2.\n\n=== DoD 2 — VERIFY-OR-BLOCK, BOTH PREMISES HOLD ===\n(i) $ grep -n \"operatorPrincipalId\\|humanReview\" apps/daemon/src/daemon-command-registry.ts\n    :185  humanReview: principal.principalId === operatorPrincipalId\n    (plus :77 :99 :100 :162 :201 :227 :241)\n    The witness is still minted on PRINCIPAL IDENTITY ALONE. No transport fact participates.\n(ii) $ grep -rn \"wiredMcpToolKinds\" -- apps adapters packages\n    apps/daemon/src/mcp-main.ts:131            toolAllowlist: wiredMcpToolKinds()\n    apps/daemon/src/mcp-http/mcp-http-host.ts:142  toolAllowlist: wiredMcpToolKinds()\n    Live on BOTH MCP entries. And mcp-tool-allowlist.ts:31-34 shows the command half is\n    `[...Object.keys(PAYLOAD_KEYS)].sort()` — DERIVED, so both approval kinds are advertised\n    today with no roster line naming them.\nNeither premise is stale. Not a duplicate. Proceed.\n\n=== DoD 1 — MEASURED MCP LANE CENSUS: ZERO. This is the empty positive control. ===\nCommand: `grep -rn \"approval\\.decide\\|graph\\.approve\" --include=*.ts --include=*.mjs apps adapters packages tests scripts | grep -v \"/dist/\"`\nResult: many hits, and after attributing each by transport, NOT ONE dispatches either kind over MCP.\n- apps/daemon/src/orchestrator/agent-wrapper.ts:116 — `const HUMAN_ONLY_STEPS: ReadonlySet<string> = new Set([\"approval.decide\", \"goal.close\"])`. The production agent wrapper, which IS the MCP-driven lane, already refuses to staff both kinds. Its suite pins this at agent-wrapper.test.ts:252 (\"never staffs the human approval or goal-closure actions\"), and five further arms filter `step.kind !== \"approval.decide\"`.\n- apps/daemon/src/orchestrator/demo-seed-{plan,main}.ts — HTTP, not MCP. demo-seed-main.ts:17 \"dispatches the J1 bootstrap chain over the daemon's own HTTP\", :186 `wire.post(\"/command\", ...)`.\n- apps/control-room/src/live/live-dispatch.ts and tests/e2e/control-room/*.spec.ts — the browser lane, HTTP.\n- packages/benchmark — EXISTS as a directory, and `grep -rn \"approval\\.decide\\|graph\\.approve\" packages/benchmark` returns ZERO hits. Recorded as the empty positive control the DoD asks for.\n- packages/core/**, apps/daemon/src/planning|bootstrap|goals|http/** — domain and HTTP-ingress sites, no MCP driver.\n- The only MCP-PATH hits are @moe/mcp's OWN package tests: stdio-server.test.ts:447 uses \"approval.decide\" as an arbitrary member of a three-element `WIRED` fixture roster, and :509 asserts a refusal message does NOT name it. Neither reads the daemon's roster, so neither is a lane and neither needs an edit. Named here so the next reader does not chase them.\n\n=== WHY (b) AND NOT (a) ===\n1. NOTHING BREAKS. Zero lanes to migrate. (a) is the general mechanism for keeping an MCP approval lane alive; there is no such lane to keep.\n2. (b) IS NOT COSMETIC — the allowlist is ENFORCED ON tools/call, not merely advertised. Measured in both transports:\n   packages/mcp/src/stdio/stdio-server.ts:233-239 filters at CONSTRUCTION, derives `const allowed: ReadonlySet<string> = new Set(tools.map(t => t.name))` and passes it into `callTool(..., allowed)`; :168 `if (!allowed.has(toolLabel)) refuse(createRuntimeError({ code: \"CAPABILITY_DENIED\" }))`.\n   packages/mcp/src/http/http-tool-bridge.ts:228 `const allowed = new Set(listedTools.map(t => t.name))`, :195 the identical CAPABILITY_DENIED refusal.\n   Both files carry the same doc sentence at stdio-server.ts:156-159 / http-tool-bridge.ts:181-183: \"Omission is an AUTHORIZATION refusal, not a syntax one: unknown stays INPUT_INVALID, known-but-omitted becomes CAPABILITY_DENIED, and both refuse BEFORE envelope construction, authentication or dispatch.\"\n   That last clause is what makes (b) sufficient: the refusal lands ahead of the authenticator, so the operator-bootstrap fallback credential at mcp-main.ts:112-127 never gets to authenticate the call. The hole task-bdbe0519 measured is closed at the transport, not papered over downstream.\n3. (b) MAKES THE RAIL STRUCTURALLY TRUE. \"Never write through MCP\" stops being a property a writer must detect and becomes one the transport cannot violate. (a) leaves the lane open and asks every future writer to key on a field correctly — a fail-open default.\n4. (a) IS STRICTLY LARGER: daemon-command-registry.ts + the HTTP command adapter + mcp-dispatch-port.ts + bootstrap-services.ts context threading + tests. (b) is the allowlist module, its suite, two production-edge transport arms, and one registry doc sentence.\n5. IT MATCHES THE PRODUCT VISION the filer cites — approvals are human acts in the paired browser — and it is CHEAPLY REVERSIBLE: re-admitting a kind is deleting one member of the exclusion roster, at which point (a) becomes the required follow-up.\n\n=== ONE THING THE ROW'S DESCRIPTION GETS WRONG, corrected here ===\nOption (b) is described as \"delete approval.decide and graph.approve from the MCP served allowlist\". There is nothing to delete. mcp-tool-allowlist.ts:31-34 DERIVES the command half from `Object.keys(PAYLOAD_KEYS)`, and its own doc at :6-11 says so: \"a kind wired there appears here with no edit\". So (b) must ADD an explicit frozen exclusion roster and subtract it — and that roster is exactly the surface epic rail 7 requires a pinned denominator and a deletion drill for. Planned as its own numbered step.\n\n=== DoD 4 — CONSUMERS, named as the rail requires ===\n- task-bdbe0519073f4156b288abd230405bad (path A, BLOCKED) — the policy-risk record writer whose rail forbids MCP writes. Its block premise is discharged by this ruling.\n- task-2ae7539877c944baa3ba5427dc56e854 (path B, PLANNING) — \"Append the policy-risk leg on approval.decide keyed on the server-minted operator witness\".\n- task-124654189c6a4c86b329663e4c6260ea (BLOCKED) sits behind bdbe0519.\nThe one-sentence contract both consumers key on, to be written into daemon-command-registry.ts's doc comment beside the mint at :185: `humanReview` is minted on operator PRINCIPAL IDENTITY alone and is trustworthy as a human-act witness for approval.decide and graph.approve BECAUSE neither kind is reachable over MCP — the daemon's MCP roster excludes both and the transport refuses CAPABILITY_DENIED before authentication; re-admitting either kind to that roster invalidates this contract and requires a server-set transport-origin field first.",
      "timestamp": "2026-08-28T13:02:23.148Z"
    },
    {
      "id": "comment-f8eba3d75f084b0e993b8a8d62dd19ab",
      "author": "worker-f16b412c",
      "content": "STEP 1 — READ-ONLY RE-GATE RE-MEASURED. worker-f16b412c, HEAD **105c0c44** (the tree moved: architect-fdbab575's ruling comment-4d026de3 was measured at e3d20791). All five clauses PASS. Ruling (b) MCP ROSTER REMOVAL stands, not a duplicate, not a no-op. Zero production bytes authored in this step.\n\n**(a) registry still mints on principal identity alone — HOLDS**\n`$ grep -n \"operatorPrincipalId\" apps/daemon/src/daemon-command-registry.ts` -> exit 0, hits :77 :99 :100 :162 **:185** :201 :227 :241. Line 185 verbatim:\n```\n          humanReview: principal.principalId === operatorPrincipalId\n            ? Object.freeze({ principalId: principal.principalId })\n            : undefined,\n```\n`$ grep -nE \"transport|origin\" apps/daemon/src/daemon-command-registry.ts` -> one hit, :53, inside a doc sentence about a re-export path. **No transport/origin field has appeared**, so nobody landed path (a). Not a duplicate.\n\n**(b) both MCP entries still wired — HOLDS, both closed not one**\n`$ grep -rn \"wiredMcpToolKinds\" -- apps adapters packages` -> `apps/daemon/src/mcp-main.ts:131  toolAllowlist: wiredMcpToolKinds()` and `apps/daemon/src/mcp-http/mcp-http-host.ts:142  toolAllowlist: wiredMcpToolKinds()`. Both live.\n\nNEW CONSUMER THE RULING DID NOT NAME, disclosed: `apps/daemon/src/planning/graph-preview-query.test.ts:225` asserts `expect(wiredMcpToolKinds()).toContain(\"graph.preview\")`. `graph.preview` is a QUERY kind and this row never filters `MCP_SERVED_QUERY_KINDS`, so that arm must stay green and unedited. It is in the step-7 L3 whole-package leg, not an owned path.\n\n**(c) command half still DERIVED — HOLDS (line moved)**\n`$ grep -n \"PAYLOAD_KEYS\" apps/daemon/src/mcp-tool-allowlist.ts` -> :1 import, :6 doc, **:32** `...[...Object.keys(PAYLOAD_KEYS)].sort(),`. The plan cites :31-34; the derivation is at :32 inside the `WIRED_KINDS` freeze at :31-34. Still derived, NOT a hand-kept literal, so the step-3/4 exclusion mechanism keeps the shape the plan assumed.\n\n**(d) MCP LANE CENSUS = ZERO. Re-run and re-attributed at 105c0c44.**\n`$ grep -rn \"approval\\.decide\\|graph\\.approve\" --include=*.ts --include=*.mjs apps adapters packages tests scripts | grep -v \"/dist/\"` -> **379 hits across 114 files**. Attributed by intersecting that file set with files that also mention an MCP driver surface (`grep -qE \"mcp|Mcp|MCP|tools/call\"`), which reduces to **8 candidate files**. Every one attributed:\n\n1. `apps/daemon/src/daemon-command-graph-transport.test.ts` — **the one that looks like a live MCP lane and is not.** :182 `describe(\"graph.approve reaches its durable service through the production transport\")` and :42 imports `createMcpDispatchPort`. Attributed: its `graph.approve` goes through `send()` at :131, which calls `handleCommandRequest(...)` with `protocolVersion: WIRE_PROTOCOL_VERSION` and asserts `httpStatus: 200` — the **HTTP** command adapter. Its only MCP use is `queryPortFor(...).dispatchQueryBytes(...)` at :176 with `queryKind: \"graph.get\"` — a QUERY, in `MCP_SERVED_QUERY_KINDS`, untouched here.\n2. `apps/daemon/src/planning/graph-query-test-fixtures.ts` — `overMcpBytes()` :575 drives the real MCP dispatch port, but the envelope it builds is hard-coded `queryKind: \"graph.get\"`. Its `graph.approve` strings at :331/:436 are store fixtures, not dispatches. Not a lane.\n3. `packages/mcp/src/stdio/stdio-schemas.test.ts` — only `approval.decide_intent` (:88-89), a DIFFERENT kind that my grep matched on prefix. Not `approval.decide`.\n4. `packages/mcp/src/stdio/stdio-server.test.ts` — the fixture roster the ruling already named (:447/:509). Must stay green and unedited.\n5. `packages/control-room-client/src/generated/generated-client.ts` — :177/:213/:279 register `CommandBuilder`s that emit a `RuntimeCommandEnvelope` draft (`schemaVersion`, `sessionCredential`, `requestDigest`); consumed by `apps/control-room/src/data/data-adapter.ts:96`. Browser lane, HTTP. Its `mcpCode: -32001` entries are an error-code mapping table, not a dispatch path.\n6. `tests/integration/control-room/dev-payload-parity.test.ts` — `DEV_PAYLOADS[\"approval.decide\"]` payload fixtures (:73/:137); `@moe/mcp` appears only in a doc comment at :21 listing packages. Not a lane.\n7. `apps/daemon/src/orchestrator/agent-wrapper.test.ts` — the fence the ruling named.\n8. `apps/daemon/src/planning/graph-query.ts` — :22 is prose in a doc comment about capability mapping.\n\n**NOT ONE of the 379 hits dispatches either kind through an MCP driver.** The census is the empty positive control DoD 1 asks for, and it is empty for the same reason at 105c0c44 as at e3d20791.\n\n**(e) EXECUTED BEFORE POSITIVE CONTROL — both kinds ARE advertised today**\nScratch vitest arm, run then deleted (`rm` verified: `ls` -> \"No such file or directory\", `git status --porcelain` on the path -> blank; no scratch file is in the tree or will be in the commit):\n```\n$ pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/zz-scratch-f16b412c-control.test.ts\n Test Files  1 passed (1)\n      Tests  1 passed (1)\n```\nThe passing assertion was `expect(hit).toEqual([\"approval.decide\", \"graph.approve\"])` where `hit = wiredMcpToolKinds().filter(k => k === \"approval.decide\" || k === \"graph.approve\")` — **length 2, in that order, BEFORE any change.** Without this the whole row could green vacuously.\n\n**MEASURED DENOMINATORS for step 6**, read off the same executed arm:\n| term | BEFORE |\n|---|---|\n| `Object.keys(PAYLOAD_KEYS).length` | **38** |\n| `MCP_SERVED_QUERY_KINDS.length` | **4** |\n| `wiredMcpToolKinds().length` | **42** |\n\n42 = 38 + 4, and matches the `\"count\":42` in the row description. **After the change the identity must be 38 - 2 + 4 = 40.**\n\nOne more measured fact that removes a false-pass risk the plan did not price: `wired.includes(\"approval.decide_intent\")` is **false** — `approval.decide_intent` is not in `PAYLOAD_KEYS` at all, so there is no sibling kind for an exact-match exclusion to collide with. The exclusion must still compare by exact string equality, never `startsWith`/`includes`, and step 2 arm (2) pins each member against a real `PAYLOAD_KEYS` key.",
      "timestamp": "2026-08-28T13:07:42.903Z"
    },
    {
      "id": "comment-83a2955d817948808fbc15627cd187e4",
      "author": "worker-f16b412c",
      "content": "DoD 4 — THE CONSUMER CONTRACT, and the three rows that may rely on it. worker-f16b412c, delivered at HEAD 105c0c44.\n\n**READ THIS FIRST IF YOU ARE ONE OF THE CONSUMERS: this row does NOT give you a transport-origin field.** The either/or ruling picked path (b), so instead of adding a server-set `transport: \"HTTP\" | \"MCP\"` fact, this row REMOVES THE TRANSPORT. There is no new field to read, no new parameter to thread, and nothing to set correctly at a call site. What you get is a structural guarantee about which calls can exist at all.\n\n**THE CONTRACT, one sentence, now written into `apps/daemon/src/daemon-command-registry.ts` at the `humanReview` mint:**\n> `humanReview` is minted on operator PRINCIPAL IDENTITY alone and is trustworthy as a human-act witness for `approval.decide` and `graph.approve` BECAUSE neither kind is reachable over MCP — the daemon's MCP roster excludes both (`mcp-tool-allowlist.ts`, `MCP_EXCLUDED_COMMAND_KINDS`) and the transport refuses `CAPABILITY_DENIED` before authentication; re-admitting either kind to that roster invalidates this contract and requires a server-set transport-origin field first.\n\nSo a policy-risk writer may key `approvedBy` on the presence of `humanReview` for those two kinds, and the rail \"never write through MCP, agent, fixture/demo, unauthenticated or test-named principals\" holds STRUCTURALLY for the MCP clause — no principal-name heuristic is needed or wanted. `MCP_EXCLUDED_COMMAND_KINDS` is exported precisely so you can read which kinds the contract depends on being absent rather than re-spelling them.\n\n**VERIFIED CONSUMERS, all three re-read from disk just now — statuses are as the plan claimed:**\n- `task-bdbe0519073f4156b288abd230405bad` — \"Persist policy-risk records from human approvals\". **BLOCKED, unassigned.** Path A, and blocked on precisely this gap: it is the row whose measured block filed this one.\n- `task-2ae7539877c944baa3ba5427dc56e854` — \"[bdbe0519 path B] Append the policy-risk leg on approval.decide (approval-activation.ts)\". **PLANNING, held by architect-fdbab575** — the same architect who recorded the ruling.\n- `task-124654189c6a4c86b329663e4c6260ea` — \"Roster POLICY_RISK_LAYER as a security boundary\". **BLOCKED, unassigned.** Behind the other two.\n\nI have NOT edited any of those rows' status, plan or fields. Naming only, as the step requires.\n\n**THE LIMIT, stated plainly so nobody over-reads the guarantee.** This closes the MCP *advertisement and capability* surface on both entries — `mcp-main.ts:131` (stdio) and `mcp-http/mcp-http-host.ts:142` (http) — and the refusal lands at `stdio-server.ts:168` / `http-tool-bridge.ts:195` BEFORE envelope construction, authentication and dispatch. That ordering is the whole reason removal is sufficient rather than cosmetic: the operator bootstrap credential supplied as `fallbackCredential` (`mcp-main.ts:112-127`) never reaches the authenticator for an excluded kind, so an MCP caller holding it cannot obtain a witness at all.\n\nIt does NOT make `humanReview` self-describing. The mint still carries no transport fact. **Any future MCP approval lane re-opens the hole and must land option (a), the server-set origin field, FIRST** — adding the kind back to the roster alone would silently restore an indistinguishable witness. The exported roster's own doc comment says this, and so does the registry comment, so the trap is written at both ends.\n\n**WHAT PROVES IT, so a consumer does not have to take this on trust** (owned test arms, all green — full evidence lands in the step-7 comment):\n- `C2` in `mcp-tool-allowlist.test.ts` asserts every excluded kind is STILL SERVED by the production registry — the kinds left one transport's advertisement, not the daemon, so the browser/HTTP approval path you already depend on is untouched.\n- `HTTP-1` drives a real `tools/call` through `createMcpHttpHost().handleRequest()` and reads `CAPABILITY_DENIED` off the wire, asserting the body does NOT contain `INPUT_INVALID` and that `decisionCount()` is unchanged — the refusal provably precedes dispatch.\n- `STDIO-1` pins the same branch on the stdio entry at the seam's exact production expressions. Disclosed asymmetry: the stdio arm is an inputs-and-branch proof, not a wire-level one, because `@modelcontextprotocol/sdk` is a dependency of `@moe/mcp` only and is not resolvable from `@moe/daemon`; the wire-level proof exists once, on the HTTP entry. Full reasoning in the step-3 note.",
      "timestamp": "2026-08-28T13:17:39.438Z"
    },
    {
      "id": "comment-e984446aaae143ddab985933e2251556",
      "author": "worker-f16b412c",
      "content": "STEP 7 (1 of 2) — GATES, COMMIT, PATH ATTRIBUTION. worker-f16b412c. Commit **78e08283**, 5 files, 336 insertions / 11 deletions. Drills and adversarial review are in part 2. Everything load-bearing is in comments because the completion summary is write-only.\n\n## COMMIT — explicit pathspec, verified after\n`git commit -- <5 owned paths>`, never bare, never `-a`. Index was EMPTY beforehand (`git diff --cached --name-only` blank), so nothing foreign was staged and nothing needed unstaging. `git show --stat 78e08283`:\n```\n apps/daemon/src/daemon-command-registry.ts     |  15 ++++\n apps/daemon/src/mcp-http/mcp-http-host.test.ts | 107 +++++++++++++++++++-\n apps/daemon/src/mcp-main.test.ts               | 104 ++++++++++++++++++\n apps/daemon/src/mcp-tool-allowlist.test.ts     |  87 ++++++++++++++--\n apps/daemon/src/mcp-tool-allowlist.ts          |  34 +++++++-\n```\nExactly the five owned paths, zero foreign. Index clean afterwards. No production file over 400 lines: `mcp-tool-allowlist.ts` is **75** (was 42). All drills were run BEFORE this commit, so no mutated byte could reach it.\n\n## THE VERIFICATION COMMAND SUBMITTED, and why it is name-scoped\n```\npnpm --filter @moe/daemon exec vitest run --root . --config package.json \\\n  src/mcp-tool-allowlist.test.ts src/mcp-main.test.ts src/mcp-http/mcp-http-host.test.ts \\\n  -t \"task-4c9b1d85|command half|C1 advertises|C2 keeps\"\n Test Files  3 passed (3)\n      Tests  12 passed | 28 skipped (40)      EXIT 0\n```\n12 = every arm this row delivers (6 command-half arms + C1 + C2 + STDIO-1/2 + HTTP-1/2). Nonzero executed count, not a vacuous run. It is filtered rather than whole-suite **because a foreign red is live in the worktree**. I am not passing a narrowed gate off as the full gate — the unfiltered L1-L4 results are recorded verbatim below, red included.\n\n## GATES L1-L4 — each standalone, foreground, unpiped, unchained\n**L1 `pnpm --filter @moe/daemon typecheck` — EXIT 1.** Execution evidence: the script echo `$ tsc --project tsconfig.json` printed, so this was not a silent sub-second green.\n\nIt caught **TWO REAL DEFECTS OF MINE THAT THE GREEN VITEST RUN COULD NOT SEE** — both fixed and committed:\n- `src/mcp-main.test.ts(242,7) TS2741` — my stdio fake port omitted `authenticate`, required by `StdioDispatchPort`. Fixed with a throwing `authenticate`: the arm asserts excluded kinds never REACH the port, so a port that could answer would weaken it.\n- `src/mcp-tool-allowlist.test.ts(240,46) TS2345` — `registry.keys()` is typed as the closed command-kind union while `MCP_EXCLUDED_COMMAND_KINDS` is `readonly string[]`. Fixed by widening C2's local to `readonly string[]`, with a comment recording why the roster is deliberately string-typed: it must be able to name a kind the union does not carry, which is exactly what keeps the misspelling defect a RED TEST instead of a compile error.\n\nThis is why the typecheck leg is not optional here — every arm was green in vitest with both defects present.\n\nAfter those fixes the ONLY remaining typecheck errors are foreign:\n```\nsrc/daemon-command-edges.ts(60,7):      TS2353: 'subscriberId' does not exist in type 'EventStreamAccessConfig'.\nsrc/daemon-store-dependencies.ts(178,5): TS2353: 'subscriberId' does not exist in type 'EventStreamAccessConfig'.\n```\n**L2 focused 4-suite run — EXIT 1.** `Test Files 3 failed | 1 passed (4)`, `Tests 6 failed | 53 passed (59)`. All 6 are one root cause. `mcp-main.test.ts` passed entirely.\n**L3 `pnpm --filter @moe/daemon test` — EXIT 1.** `Test Files 19 failed | 277 passed (296)`, `Tests 59 failed | 6062 passed | 12 todo (6133)`.\n**L4 `pnpm --filter @moe/mcp test` — EXIT 0.** `Test Files 11 passed (11)`, `Tests 218 passed (218)`. @moe/mcp is fully green and UNEDITED, so the `CAPABILITY_DENIED` seam this row now depends on is intact and `stdio-server.test.ts:447`'s use of `\"approval.decide\"` as an arbitrary fixture roster member was not disturbed. Per the plan, a red there would have meant I changed a package this row does not own.\n\n## PATH ATTRIBUTION — (HEAD minus merge-base) ∩ OWNED PATHS = **EMPTY**\nONE root cause explains every red. An **uncommitted peer diff** to `apps/daemon/src/http/event-stream-access.ts` replaces the config field `subscriberId: string` with `resolveSubscriberId: (principal) => string | undefined`, while its callers `daemon-command-edges.ts:60` and `daemon-store-dependencies.ts:178` still pass `subscriberId`. So `config.resolveSubscriberId` is `undefined`, calling it throws, and the throw surfaces either as `TypeError: config.resolveSubscriberId is not a function` or — through the HTTP transport — as `UNKNOWN_ERROR / INTERNAL_UNKNOWN / httpStatus 500`. The L3 output carries **28 mentions of `resolveSubscriberId`**.\n\nBASELINE ESTABLISHED WITHOUT MUTATING THE TREE: `git show HEAD:apps/daemon/src/http/event-stream-access.ts` declares `readonly subscriberId: string` at :19 and :48 and uses `config.subscriberId` at :106. **The symbol `resolveSubscriberId` does not exist at HEAD at all.** Neither the two typecheck errors nor any of the 59 test failures is reachable from any committed tree; the entire failing set is created by worktree WIP I do not own. A tree-mutating drill was therefore unnecessary and would have risked a peer's in-flight work.\n\nI checked the intersection the strong way rather than by eyeballing the file list. Grepping the whole 6133-test L3 output for anything naming this row's change — `MCP_EXCLUDED_COMMAND_KINDS`, `wiredMcpToolKinds`, `CAPABILITY_DENIED`, `approval.decide`, `graph.approve` — returns **NOT ONE failure**. The single hit inside an owned file is `mcp-tool-allowlist.test.ts > \"names only query kinds the production port actually serves\"`, a pre-existing QUERY-half arm I never touched, failing via `refusalCodeOf` into the peer's throw. Of the 20 failing files, 17 lie outside my owned paths entirely (`planning/graph-supersede-*`, `work/foundation-*`, `activation/*`, `http/event-*`) and are unreachable from a diff that adds one filter and one comment.\n\nNot repaired, not amended, not reverted, not hidden — disclosed verbatim per epic rail 3, and routed to its owner rather than charged to this row.",
      "timestamp": "2026-08-28T13:33:06.119Z"
    },
    {
      "id": "comment-fd3db62804d547508cac795e7b7387cc",
      "author": "worker-f16b412c",
      "content": "STEP 7 (2 of 2) — FIVE MUTATION DRILLS, POSITIVE CONTROL, ADVERSARIAL REVIEW. Continues comment-e984446aaae143ddab985933e2251556.\n\n## FIVE DRILLS — each alone, one degree of freedom at a time\nPre-drill `git status --porcelain` on the exact five paths: all ` M` (tracked + modified). Backups taken **outside the repo** at `%TEMP%\\drill-f16b412c\\`, and each restore proven by SHA256 against that backup in a call SEPARATE from the restore — not by \"the command returned\".\n\n| # | mutation | required red | result |\n|---|---|---|---|\n| **D1** | remove the exclusion filter from `WIRED_KINDS` (restore the pre-change derivation) | set-equality arm + BOTH transport arms | **RED** — plus C1 and the denominator arm |\n| **D2** | keep the filter, misspell one member as `\"approval.decides\"` | \"REAL vocabulary keys\" arm | **RED** |\n| **D3** | delete `\"graph.approve\"` from the roster | exact-count + derived-denominator arms, nonzero execution | **RED** — 15 passed + 2 failed = 17 executed |\n| **D4** | truncate the HTTP sweep roster to 1 case | generated-count assertion | **RED** — HTTP-1 and HTTP-2 |\n| **D5** | swap `CAPABILITY_DENIED` → `INPUT_INVALID` in HTTP-1 | that arm pins the CODE, not merely the refusal | **RED** — HTTP-1 only; HTTP-2 correctly unaffected |\n\n**No green drill, so no finding.** D1 is the row's central claim and it reddened the set-equality arm and both transport arms exactly as required — the arms are attached to the production filter, not merely to the presence of the new symbol. That matters because the step-2/3 reds were only symbol-resolution reds; D1 is where the identity actually got graded, as I flagged at the time.\n\n**The two most informative results, both confirming the plan's own warnings rather than just passing:**\n- **D2** — the set-equality arm AND C1 stayed **GREEN** under the misspelling. A typo excludes nothing and the headline arms cannot see it; only the dedicated \"REAL vocabulary keys\" arm caught it. That arm was not decoration.\n- **D3** — the set-equality arm, the REAL-vocabulary arm and C1 all stayed **GREEN** when a roster member was deleted, because deletion shrinks BOTH sides of their comparisons. Only the exact-count/denominator arm reddened. This is precisely the self-iterating-roster blind spot epic rail 7 exists for, demonstrated on this row rather than asserted about it — and it is why `length > 0` would have been worthless here.\n\nRESTORE PROOF — all four mutated files SHA256-identical to their pre-drill backups:\n```\nmcp-tool-allowlist.ts       match=YES  3866cbab...86dbea\nmcp-tool-allowlist.test.ts  match=YES  51be9e09...0dda8d\nmcp-main.test.ts            match=YES  11f47c53...765f1c\nmcp-http-host.test.ts       match=YES  13eaa2e5...4c0749\n```\nResidue scan `grep -rn \"approval.decides\\|slice(0, 1)\"` across all three mutated sources → exit 1, clean. `CAPABILITY_DENIED` back to 5 occurrences in the HTTP suite. All arms re-verified green by name at the post-restore HEAD before committing.\n\n## STEP-1(e) BEFORE POSITIVE CONTROL, carried through to AFTER\nBEFORE (executed, green, scratch arm since deleted and its deletion verified): `wiredMcpToolKinds().filter(k => k === \"approval.decide\" || k === \"graph.approve\")` had **length 2** — without this the whole row could have greened vacuously.\nDenominators BEFORE: `PAYLOAD_KEYS` **38**, queries **4**, wired **42** (matches the `\"count\":42` in the row description).\nAFTER: wired **40** = 38 − 2 + 4. The denominator arm pins this **two independent ways** — as a live-import identity, and as four literals `{ excluded: 2, queries: 4, vocabulary: 38, wired: 40 }` — so a change that moves both sides of the identity together still reds on the literals.\n\n## ADVERSARIAL REVIEW (skill `adversarial-self-review`)\nOn the two failure modes the plan named:\n- **Hand-spelled tool label.** Every arm derives labels through the production helper `toolLabelForKind`; none is hand-spelled. Each also asserts the response does NOT contain `INPUT_INVALID` — the discriminator between \"known but omitted from the roster\" and \"never generated\". D5 confirms the arms pin the code rather than merely the refusal.\n- **Self-built roster.** STDIO-1/2 and HTTP-1/2 all consume the real `wiredMcpToolKinds()` and `MCP_EXCLUDED_COMMAND_KINDS`; D1 proves they see a regression in the module under test.\n\nFull checklist on the diff:\n- **Concurrency** — `WIRED_KINDS` is computed once at module load and frozen; both MCP entries receive the same frozen reference, so the transports cannot diverge, and `push` throws under ESM strict mode. No check-then-act, no shared mutable state.\n- **Inputs** — no caller input reaches this module: the roster is a compile-time literal and `PAYLOAD_KEYS` a module constant. The empty-roster hazard (`MCP_TOOL_ALLOWLIST_EMPTY`, thrown at construction) is unreachable at 40 kinds, and STDIO-2 asserts the real server still constructs on the subtracted roster.\n- **Prefix hazard** — matching is exact string equality, never `startsWith`. Measured: `approval.decide` is a prefix of the unrelated real kind `approval.decide_intent`, which is NOT in `PAYLOAD_KEYS` today, so there is no live collision — but the arm guards the future one.\n- **Ordering / side effects** — `.filter()` precedes `.sort()`, and \"is deterministic and frozen\" still passes; the module is pure computation at load with no handles, IO or error paths.\n- **Blast radius** — `MCP_SERVED_QUERY_KINDS` is spread AFTER the filter and never subtracted, so `planning/graph-preview-query.test.ts:225` (`toContain(\"graph.preview\")`) stays green; `daemon-command-vocabulary.ts` untouched, and C2 asserts both kinds are STILL SERVED by the production registry, so the browser/HTTP approval lane is provably intact.\n\n**One ding I am pre-empting rather than waiting for QA to raise:** `MCP_EXCLUDED_COMMAND_KINDS` is typed `readonly string[]` rather than the closed command-kind union. Deliberate, and documented at the C2 call site — a union type sounds stronger but would convert the misspelling defect into a compile error in the TEST and delete the runtime guard that D2 proves works.\n\n## KNOWN LIMIT, restated so QA does not have to discover it\nThe stdio arm is an inputs-and-branch proof, not a wire-level one. `@modelcontextprotocol/sdk` is a dependency of `@moe/mcp` only and does not resolve from `@moe/daemon` (`ls apps/daemon/node_modules/@modelcontextprotocol` → No such file or directory), and the plan declared neither a manifest edit nor the shared `pnpm-lock.yaml` as owned paths. STDIO-1 instead pins the exact production expressions (`allowlistedToolEntries` → `allowed` set, plus `STDIO_TOOL_INDEX` membership) so the `CAPABILITY_DENIED` branch is selected by construction and `INPUT_INVALID` is ruled out. The wire-level `tools/call` proof exists once, on the HTTP entry (HTTP-1), which additionally asserts `decisionCount()` is unchanged so the refusal provably precedes dispatch. Full reasoning in the step-3 note. **Follow-up if stdio must be proven at wire level: add the dependency edge as its own step with `apps/daemon/package.json` and `pnpm-lock.yaml` declared owned.**",
      "timestamp": "2026-08-28T13:33:40.948Z"
    },
    {
      "id": "comment-70d81b65b12d4cd9825606945d1c8784",
      "author": "qa-fc82710f",
      "content": "QA GRADED EVIDENCE — qa-fc82710f. Delivery 78e08283; HEAD moved to 0197b0c8 during review (`fix(daemon): distinguish carry fact gaps`) and its delta is apps/daemon/src/planning/carry-forward-evidence{,.test}.ts — ZERO intersection with this row's five owned paths, so every measurement below still binds. Owned porcelain clean at start and at finish.\n\nDIFF. `git show --stat 78e08283` = exactly the five owned paths, +336/-11. Production files: mcp-tool-allowlist.ts 74 lines, daemon-command-registry.ts 271 — both far under the 400 cap; the other three are test files.\n\nDoD 2 (VERIFY-OR-BLOCK) — re-measured myself, both premises still true after delivery: daemon-command-registry.ts:185 still mints `humanReview` on `principal.principalId === operatorPrincipalId` with no transport fact anywhere, and `wiredMcpToolKinds()` is still the value both entries pass (mcp-main.ts:131, mcp-http/mcp-http-host.ts:142 — the ONLY two `toolAllowlist` call sites in the repo).\n\nDoD 1 (RULING + LANE CENSUS) — I re-derived the census rather than reading it. `grep -rn \"approval\\.decide|graph\\.approve\" packages/benchmark` = EXIT 1, zero hits: the empty positive control the DoD asks for, confirmed live. Every tests/ or scripts/ file naming either kind was scanned for an MCP driver (`grep -qiE \"mcp|tools/call\"`); exactly ONE matched, tests/integration/control-room/dev-payload-parity.test.ts, and its only hit is a doc-comment listing workspace packages — not a lane. `createMcpDispatchPort` has three call sites total: mcp-main.ts:121, mcp-http-host.ts:130, and planning/graph-query-test-fixtures.ts:581 (a QUERY fixture). agent-wrapper.ts:116 `HUMAN_ONLY_STEPS` still contains approval.decide and is consumed at :420. Census of ZERO stands.\n\nDoD 3 (b-branch) — I verified the enforcement seam in the package this row does NOT own, because \"removed from the advertisement\" is only sufficient if the refusal precedes dispatch. stdio-server.ts:233-236 derives `allowed` from the FILTERED listed tools and :169 refuses a known-but-omitted label with CAPABILITY_DENIED; http-tool-bridge.ts:228/:195 is the identical shape. Served == advertised BY CONSTRUCTION on both entries — there is no third route to `dispatchCommandBytes` (only mcp-dispatch-port.ts:179 defines it). The refusal lands before envelope construction and authentication, so the operator `fallbackCredential` (mcp-main.ts:112-127) never reaches the authenticator for an excluded kind. That is what makes (b) structural rather than cosmetic. Set-equality is asserted in both directions in the command-half arm AND in C1; HTTP-1 drives a REAL tools/call through the production host and reads CAPABILITY_DENIED off the wire, asserts the body does NOT contain INPUT_INVALID (the discriminator between \"omitted\" and \"never generated\"), and asserts `decisionCount()` is unchanged. Note on the DoD's \"code and layer\": a transport refusal frame is built by `createRuntimeError` (runtime-error-factory.ts:93) from a registry descriptor and carries no `layer` key — code plus the INPUT_INVALID discriminator plus the zero-decision check is the strongest assertion available at that seam, and the stdio arm's input-equivalence limitation (@modelcontextprotocol/sdk is not a @moe/daemon dependency) is disclosed in the test's own doc comment rather than hidden.\n\nDoD 4 — the one-sentence contract is in daemon-command-registry.ts at the mint, verbatim from the ruling, with the re-admission trap stated at both ends (registry and MCP_EXCLUDED_COMMAND_KINDS doc). Consumers named in comment-83a2955d.\n\nGATES I RE-RAN:\n- Row's arms, worker's exact verification command: EXIT 0, `Test Files 3 passed (3)`, `Tests 12 passed | 28 skipped (40)` — reproduced.\n- Unfiltered 4-suite focused leg (allowlist + main + http-host + dispatch-port): EXIT 1, `Tests 6 failed | 53 passed (59)`. ALL SIX ARE FOREIGN AND UNCOMMITTED, one root cause: `TypeError: config.resolveSubscriberId is not a function` at http/event-stream-access.ts:122. `git grep -n resolveSubscriberId HEAD` = EXIT 1 — the symbol does not exist anywhere in the committed tree; it is a peer's ` M` worktree edit that replaced `subscriberId` (HEAD:48). The six failing arms are all events.read/subscriber arms; NONE is an arm this row authored.\n- `pnpm --filter @moe/daemon typecheck`: EXIT 1 with the script echo `$ tsc --project tsconfig.json` printed, and exactly two errors — daemon-command-edges.ts:60 and daemon-store-dependencies.ts:178, both the same foreign `subscriberId` TS2353. No error in any owned file. Same peer WIP; post-dates delivery; this row's daemon delta is disjoint from it.\n\nMY OWN DRILLS (two, each one degree of freedom away from the worker's five):\n- DRILL A, EMPTY the exclusion roster (worker drilled removing the filter and deleting one member, not emptying): EXIT 1, `Tests 4 failed | 8 passed | 28 skipped` — STDIO-1, HTTP-1, HTTP-2 and the exact-denominator arm all red. The zero-case sweep guard fired exactly as designed: an empty roster makes the generated case list empty, and `EXCLUSION_CASES.length === 2` refuses to pass vacuously.\n- DRILL B, THE SNEAK PATH nobody drilled: re-admit `approval.decide` through the QUERY half (`MCP_SERVED_QUERY_KINDS`) while leaving the exclusion roster intact — the one route that re-advertises the kind without touching the filter. EXIT 1, `Tests 6 failed | 7 passed | 27 skipped`: STDIO-1, HTTP-1, HTTP-2, Q1 parity, the REAL-vocabulary arm and the denominator arm all red. The command-half set-equality arm stayed green (it subtracts the query list from both sides), which is exactly why the transport arms had to exist — and they do. Hole closed in four independent places.\n- Both drills restored from %TEMP% backups; `git diff --exit-code -- apps/daemon/src/mcp-tool-allowlist.ts` = 0 after each; post-drill re-green 12 passed | 28 skipped, EXIT 0; owned porcelain empty.\n\nVERDICT: APPROVE. The one thing a future reader must not lose: this row buys a STRUCTURAL guarantee, not a transport fact. Re-admitting either kind to the roster silently restores an indistinguishable witness, and option (a) must land first — that trap is now written in the registry, in the roster's doc, and pinned by the arms drilled above.",
      "timestamp": "2026-08-28T13:40:05.827Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-28T09:56:32.849Z",
  "updatedAt": "2026-08-28T16:23:51.302Z",
  "metrics": {
    "firstClaimAt": "2026-08-28T12:59:12.481Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 5,
    "executedStepCount": 7,
    "doneAt": "2026-08-28T13:40:43.047Z",
    "wallClockMs": 2490566
  },
  "contextFetchedBy": [
    "architect-fdbab575",
    "worker-f16b412c",
    "qa-fc82710f",
    "worker-013a8776"
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-28T13:04:18.131Z",
  "planningNotes": {
    "approachesConsidered": "RULING RECORDED IN FULL AT comment-4d026de3fc24449d927f9eee28da6114 — read it before the plan. Summary of the choice:\n\nCHOSEN — (b) MCP ROSTER REMOVAL. Four reasons, all measured, none from the description: (1) the lane census is ZERO, so nothing breaks; (2) the allowlist is ENFORCED on tools/call in both transports and refuses BEFORE authentication, so removal genuinely closes the hole rather than hiding it; (3) it makes task-bdbe0519's rail structurally true instead of a property each future writer must detect correctly — (a) is fail-open by construction; (4) it is strictly smaller (5 files vs registry + HTTP adapter + dispatch port + bootstrap context threading + their suites) and cheaply reversible.\n\nREJECTED — (a) ORIGIN FIELD. It is the right general mechanism only if an MCP approval lane must survive. None exists: apps/daemon/src/orchestrator/agent-wrapper.ts:116 already fences both kinds out of the agent lane with `HUMAN_ONLY_STEPS`, the demo seed dispatches over HTTP, the control room is the browser, and packages/benchmark has zero hits. Building a transport-origin mechanism to protect a lane nobody uses adds a field every future call site must set correctly, and the failure mode of forgetting it is a silently trusted witness. Named in the plan as the required follow-up if either kind is ever re-admitted.\n\nREJECTED — doing both. Landing (a)'s field and (b)'s removal would leave a transport fact whose only consumer is a lane that cannot be reached, i.e. an untested branch guarding nothing.\n\nREJECTED — asserting the closure only at the policy-risk writer. The refusal lands before envelope construction, authentication and dispatch (stdio-server.ts:156-159), so a downstream arm would prove nothing about the transport and would still be green if the roster silently re-admitted the kinds.\n\nREJECTED — editing packages/mcp. Its stdio-server.test.ts:447 uses \"approval.decide\" as an arbitrary member of a three-element fixture roster and :509 asserts a refusal message does not name it. Neither reads the daemon roster; both must stay green and unedited, and a red there means this row changed a package it does not own.",
    "codebaseInsights": "MEASURED AT HEAD e3d20791 by architect-fdbab575, 2026-08-28. Read-only; no byte changed.\n\n1. BOTH DoD-2 PREMISES HOLD. daemon-command-registry.ts:185 still mints `humanReview: principal.principalId === operatorPrincipalId ? ... : undefined` — principal identity alone, no transport fact. `wiredMcpToolKinds()` is live on BOTH MCP entries: mcp-main.ts:131 (stdio) and mcp-http/mcp-http-host.ts:142 (http). Not stale, not a duplicate.\n\n2. THE ROW'S DESCRIPTION IS WRONG ABOUT WHAT (b) COSTS, and this is the single most important planning correction. It says \"delete approval.decide and graph.approve from the MCP served allowlist\". There is nothing to delete: mcp-tool-allowlist.ts:31-34 DERIVES the command half as `[...Object.keys(PAYLOAD_KEYS)].sort()`, and the module doc at :6-11 states that property on purpose (\"a kind wired there appears here with no edit\"). (b) must therefore ADD a frozen exclusion roster and subtract it — which is precisely the kind of surface epic rail 7 demands a pinned denominator and a deletion drill for, hence its own numbered step.\n\n3. THE ALLOWLIST IS ENFORCED, NOT MERELY ADVERTISED — the fact the whole ruling turns on. stdio-server.ts:233-239 filters at CONSTRUCTION, derives `const allowed: ReadonlySet<string> = new Set(tools.map(t => t.name))` and threads it into `callTool(..., allowed)`; :168 refuses `CAPABILITY_DENIED` for a known-but-omitted label. http-tool-bridge.ts:228/:195 is the identical pair. Both carry the same doc sentence: \"unknown stays INPUT_INVALID, known-but-omitted becomes CAPABILITY_DENIED, and both refuse BEFORE envelope construction, authentication or dispatch.\" That final clause is why (b) is sufficient — the operator bootstrap credential supplied as `fallbackCredential` (mcp-dispatch-port.ts:178-186, mcp-main.ts:112-127) never reaches the authenticator for an excluded kind.\n\n4. TWO CODES, AND THE ARMS MUST DISTINGUISH THEM. An UNKNOWN tool label refuses INPUT_INVALID; a KNOWN-but-omitted one refuses CAPABILITY_DENIED. An arm that hand-spells the label (\"approval_decide\") instead of deriving it through `toolLabelForKind`/`allowlistedToolEntries` will get INPUT_INVALID as an unknown label and go green for the wrong reason. This is the row's most likely false pass and is called out in the step text.\n\n5. LANE CENSUS = ZERO, with the empty positive control recorded. agent-wrapper.ts:116 `HUMAN_ONLY_STEPS = new Set([\"approval.decide\", \"goal.close\"])` already fences the agent (MCP) lane, pinned by agent-wrapper.test.ts:252 plus five `step.kind !== \"approval.decide\"` filters. demo-seed-main.ts:17,186 dispatches over HTTP `POST /command`. packages/benchmark EXISTS but returns zero hits for either kind. The only MCP-path hits anywhere are @moe/mcp's own fixture roster.\n\n6. THREE EXISTING ASSERTIONS ENCODE THE OLD IDENTITY and must be updated, not deleted: mcp-tool-allowlist.test.ts:81-84 (command half equals the vocabulary exactly) and :96-99 (the length identity). :86-93 (events.resume) and :101-104 (deterministic and frozen) are unaffected. The suite already proves BIDIRECTIONAL set-equality for the QUERY half against `servedMcpQueryKinds()` — copy that shape for the command half rather than inventing one.",
    "risks": "1. THE HAND-SPELLED TOOL LABEL. Deriving the label through the production helper is load-bearing: a hand-spelled name refuses INPUT_INVALID as an unknown tool and the transport arm passes while proving nothing about the allowlist. Drill D5 exists to catch exactly this.\n2. THE MISSPELLED EXCLUSION MEMBER. `MCP_EXCLUDED_COMMAND_KINDS` is a roster of strings compared against `Object.keys(PAYLOAD_KEYS)`. A typo excludes nothing, and every arm except the \"each excluded kind is a real vocabulary key\" arm stays green. That arm is mandatory, and D2 drills it.\n3. BOTH ENTRIES OR NEITHER. mcp-main.ts and mcp-http-host.ts call `wiredMcpToolKinds()` independently. Closing one and asserting only that one leaves the other open. One production-edge arm per entry.\n4. DO NOT TOUCH daemon-command-vocabulary.ts. The kinds stay fully wired on HTTP — the control room's approval path depends on them. This row removes them from ONE transport's advertisement, not from the daemon. Deleting them from PAYLOAD_KEYS would break the browser lane and every HTTP approval suite.\n5. DO NOT EDIT packages/mcp. Its suite's use of \"approval.decide\" as a fixture roster member is unrelated to the daemon roster and must stay green and unedited; a red there means this row changed a package it does not own.\n6. THE REGISTRY EDIT IS DOC-ONLY. If a registry test moves, behaviour changed — stop and report.\n7. TWO CONSUMER ROWS ARE LIVE. task-bdbe0519 is BLOCKED on precisely this gap and task-2ae75398 is in PLANNING. Neither's status or plan is this row's to edit; name them in a comment and stop.\n8. Shared branch with heavy foreign WIP: explicit-pathspec commits only, repo-wide legs path-attributed.",
    "keyFiles": [
      "apps/daemon/src/mcp-tool-allowlist.ts",
      "apps/daemon/src/mcp-tool-allowlist.test.ts",
      "packages/mcp/src/stdio/stdio-server.ts",
      "packages/mcp/src/http/http-tool-bridge.ts",
      "apps/daemon/src/daemon-command-registry.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000
  },
  "planApprovedAt": "2026-08-28T13:04:20.280Z",
  "workStartedAt": "2026-08-28T13:04:50.357Z",
  "reviewStartedAt": "2026-08-28T13:35:23.675Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-tool-allowlist.test.ts src/mcp-main.test.ts src/mcp-http/mcp-http-host.test.ts -t \"task-4c9b1d85|command half|C1 advertises|C2 keeps\"",
    "exitCode": 0,
    "outputTail": " RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  3 passed (3)\n      Tests  12 passed | 28 skipped (40)\n   Start at  16:34:52\n   Duration  4.78s (transform 10.39s, setup 0ms, import 13.60s, tests 119ms, environment 0ms)\n\nFINAL_EXIT=0",
    "reportedAt": "2026-08-28T13:35:23.675Z"
  },
  "filesModified": [
    "apps/daemon/src/mcp-tool-allowlist.ts",
    "apps/daemon/src/mcp-tool-allowlist.test.ts",
    "apps/daemon/src/mcp-main.test.ts",
    "apps/daemon/src/mcp-http/mcp-http-host.test.ts",
    "apps/daemon/src/daemon-command-registry.ts"
  ],
  "completedAt": "2026-08-28T13:40:43.047Z",
  "reviewCompletedAt": "2026-08-28T13:40:43.047Z",
  "reviewSummary": "APPROVED. Delivery 78e08283; HEAD moved to 0197b0c8 mid-review with zero intersection with the 5 owned paths. Full evidence: comment-70d81b65b12d4cd9825606945d1c8784. Diff = exactly 5 owned paths, +336/-11; mcp-tool-allowlist.ts 74 and daemon-command-registry.ts 271 lines, both under cap. DoD2 re-measured: registry:185 still mints humanReview on principal identity alone; wiredMcpToolKinds() is still the value at the only two toolAllowlist sites (mcp-main.ts:131, mcp-http-host.ts:142). DoD1 census re-derived, not read: benchmark grep EXIT 1 = the empty positive control; every tests/scripts file naming either kind scanned for an MCP driver, one match and it is a doc comment; createMcpDispatchPort has 3 sites (2 prod entries + 1 query fixture). DoD3: enforcement verified in the UNOWNED package — stdio-server.ts:233-236/:169 and http-tool-bridge.ts:228/:195 derive `allowed` from the FILTERED listed tools and refuse CAPABILITY_DENIED before envelope construction, auth and dispatch, so served == advertised by construction; HTTP-1 drives a real tools/call, reads the code off the wire, asserts no INPUT_INVALID and decisionCount unchanged. GATES: row arms reproduced EXIT 0, 12 passed | 28 skipped; unfiltered 4-suite leg EXIT 1 with 6 failures ALL foreign and uncommitted (`config.resolveSubscriberId is not a function`, event-stream-access.ts:122; `git grep resolveSubscriberId HEAD` EXIT 1 proves the symbol lives only in a peer's dirty worktree); daemon typecheck EXIT 1 with exactly the same two foreign subscriberId TS2353s, none in an owned file. MY DRILLS: (A) emptying the roster reds STDIO-1/HTTP-1/HTTP-2 plus the denominator arm, zero-case sweep guard fires; (B) the undrilled sneak path, re-admitting approval.decide via the QUERY half, reds 6 arms including Q1 while command-half set-equality stays green — which is exactly why the transport arms had to exist. Both restored, git diff --exit-code 0, re-green 12 passed, owned porcelain empty."
}