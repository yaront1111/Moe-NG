{
  "id": "task-4dd4424c1c264f4490bcb7cad01e7585",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Production verifier authority provider for the agent wrapper",
  "description": "PREREQUISITE of task-97554aa4293e40eab56c0b642e18513a (Foundation self-host canary), discovered at the canary's step-1 resume-gate re-measurement on 2026-08-17.\n\nMEASURED GAP at committed HEAD a42ae2f1f9168d13b8b786404fc47b366cc8edd9 (git grep against HEAD, not the working tree):\n\n  git grep -n \"verificationAuthority\" HEAD -- apps/ packages/ | grep -v '\\.test\\.'\n    apps/daemon/src/orchestrator/agent-wrapper-main.ts:240:      verificationAuthority: () => null,\n    apps/daemon/src/orchestrator/node-verifier.ts:62:  readonly verificationAuthority: (\n    apps/daemon/src/orchestrator/node-verifier.ts:165:      const authority = config.verificationAuthority(nodeRef, brief);\n\n  git grep -n \"VerifierAuthorityFacts\" HEAD -- apps/ packages/ | grep -v '\\.test\\.'\n    -> only the type declaration (review/verifier-receipt-contracts.ts:15), its re-export\n       (review/verifier-receipt-ledger.ts:22) and two consuming positions. ZERO producers.\n\nTHE DEFECT. The only production host that constructs the node verifier -- apps/daemon/src/orchestrator/agent-wrapper-main.ts, the real `agent-wrapper-main.js` entry the canary must spawn -- passes `verificationAuthority: () => null`, with the shipped comment \"No production authority provider is shipped yet. Refuse a green test rather than manufacture calibration, package or policy facts in-process.\"\n\nnode-verifier.ts:165-172 turns that null into, for EVERY node:\n    reports.push({ detail: \"host verifier authority unavailable\", nodeRef,\n                   outcome: \"VERIFICATION_AUTHORITY_UNAVAILABLE\" });\n    continue;\n\nSo `recordVerifierReceipt` (node-verifier.ts:176) is unreachable and the `integration.accept_output` dispatch (node-verifier.ts:152, :198) never fires through the production host. The refusal is honest and correctly fails closed -- this task is NOT a request to weaken it. It is the missing producer.\n\nWHY IT BLOCKS THE CANARY. Canary DoD 3 requires one exclusive task to complete \"plan, execute, verify, independent review, accept ... with exact receipts\", and canary plan step 3 requires the journey to \"execute the real verifier, independent review and final acceptance\" through production surfaces. With no authority producer, the only reachable verify outcome is VERIFICATION_AUTHORITY_UNAVAILABLE. Global rail Clause 2 forbids the alternatives: no narrowed DoD, no mock-backed journey, no authority reimplemented inside the E2E test.\n\nObjective: ship the daemon-owned producer of `VerifierAuthorityFacts` and wire it into agent-wrapper-main, so a node with genuine durable calibration/package/policy facts verifies and earns a receipt, while a node WITHOUT them still refuses with the same stable outcome.\n\nDeliverable: a focused daemon module (suggested apps/daemon/src/review/verifier-authority-provider.ts plus its one-line .js bridge) that reads the facts from the durable store -- never from caller input, never minted in-process -- plus focused tests; and the agent-wrapper-main.ts:240 edit replacing `() => null` with that provider.\n\nNOT in scope: changing the VerifierAuthorityFacts shape, the receipt ledger, review-services acceptance, or anything under tests/e2e/foundation.\n\nRead verifier-receipt-contracts.ts:15 for the exact required fields BEFORE planning, and confirm each field has a durable source; if any field has no durable source on disk, that is a further prerequisite to file, not a field to fabricate.",
  "definitionOfDone": [
    "A production daemon module produces VerifierAuthorityFacts by reading the durable store; git grep shows it is the value passed at apps/daemon/src/orchestrator/agent-wrapper-main.ts, and `verificationAuthority: () => null` no longer appears anywhere in production.",
    "Every field of VerifierAuthorityFacts (per apps/daemon/src/review/verifier-receipt-contracts.ts:15) is sourced from a durable record; a test proves no field is defaulted, minted in-process, or read from caller input.",
    "A test drives the real createNodeVerifier with the real provider over a seeded store and asserts a receipt was recorded via recordVerifierReceipt and the integration.accept_output dispatch fired -- asserting the receipt identity, not merely ok:true.",
    "A test with the durable facts ABSENT asserts the literal outcome string VERIFICATION_AUTHORITY_UNAVAILABLE and that no receipt row was written; the fail-closed path is preserved, not weakened.",
    "A mutation drill on the provider's durable read makes the receipt-recording test go red for its receipt assertion (not for a crash), and production bytes are restored exactly.",
    "pnpm typecheck and pnpm --filter @moe/daemon test are exit 0 with a nonzero printed Test Files count line; owned production files are each <=250 physical lines."
  ],
  "taskRails": [
    "Fail closed with stable reason codes; an absent durable fact stays VERIFICATION_AUTHORITY_UNAVAILABLE and never gains authority by default.",
    "Do not weaken or delete the existing refusal branch at node-verifier.ts:165-172; add the producer, keep the guard.",
    "Use TDD. Keep each production source focused, target <=250 lines, split before 400.",
    "Stage and commit only explicit owned paths by pathspec; no git add -A, no worktree, no push, no merge."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Re-measure the authority matrix. THIS PLAN REPLACES AN EARLIER ONE that blocked here because two of three fields had no durable source; both gates have since landed, and the architect re-measured at HEAD d2151d4. Confirm each fact yourself with `git grep`, then record the matrix in the step note. (1) THE GAP IS STILL OPEN: `verificationAuthority: () => null` now sits at apps/daemon/src/orchestrator/agent-wrapper-main.ts:280 (it was :240 at a42ae2f -- the line MOVED, so grep for it, never trust the number). (2) POLICY -- durable, as originally measured: `installPolicy` (bootstrap/bootstrap-policy-services.ts) folds `{slices:{[sliceRef]:slice}}` behind a PolicyInstalled event. Note that apps/daemon/src/review/reviewer-calibration-record.ts already demonstrates the read shape -- `readDurableLedger(store, projectId)` then `stateOf(ledger, policyAggregateId(projectId))` then the `slices` object -- so check whether a reusable policy reader now exists before writing a third copy of that traversal. (3) CALIBRATION -- SOLVED by task-5edf037f: `readReviewerCalibration(store, projectId)` at review/reviewer-calibration-record.ts:138 returns `ReviewerCalibrationRead`, either `{ok:true, calibration}` or a refusal carrying `REVIEWER_CALIBRATION_NOT_INSTALLED` or `REVIEWER_CALIBRATION_UNREADABLE` (frozen list at :49). (4) PACKAGE ITEMS -- SOLVED by task-92031b0a: `ReviewRoundRecord.packageItems` is a `StoredPackageItems` discriminated union (`{status:\"PRESENT\", items}` or `{status:\"ABSENT\"}`), and `verifyStoredPackageItems(record)` at review/review-package-restore.ts:83 rebuilds through the kernel's `buildReviewPackage` and compares against the digest the HANDLER stored. If any of the four has changed, stop and re-plan rather than proceeding on this text.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "AUTHORITY MATRIX re-measured at HEAD d2151d49e672c0f28870b8207c72c9888a31de55 (git grep against HEAD AND working tree; identical).\n\n(1) GAP STILL OPEN. `git grep -n \"verificationAuthority\" HEAD -- apps/ packages/ | grep -v '\\.test\\.'`\n  agent-wrapper-main.ts:280:      verificationAuthority: () => null,\n  node-verifier.ts:62 (port decl), :165 (call site). Refusal branch :166-172 intact. `verifierStore` + `config.projectId` both in scope at wrapper :247/:275.\n\n(2) POLICY - durable, unchanged. installPolicy at bootstrap-policy-services.ts:42 folds `{slices:{...current,[sliceRef]:slice}}` behind PolicyInstalled; aggregate id `${projectId}-policy` (bootstrap-sequence.ts:51-53 via aggregateIdFor). NO reusable policy reader exists: `git grep -n SLICE_REF -- apps/ packages/` returns only REVIEWER_CALIBRATION_SLICE_REF; reviewer-calibration-record.ts:99-102 holds a PRIVATE policyAggregateId. So the provider writes the traversal for its own slice (second copy, not third).\n\n(3) CALIBRATION - SOLVED, unchanged. readReviewerCalibration at reviewer-calibration-record.ts:138 -> ReviewerCalibrationRead; refusal codes frozen at :49 (REVIEWER_CALIBRATION_NOT_INSTALLED / _UNREADABLE), layer DAEMON_PREREQUISITE.\n\n(4) PACKAGE ITEMS - SOLVED, unchanged. ReviewRoundRecord.packageItems: StoredPackageItems (review-round-items.ts:26-39); verifyStoredPackageItems at review-package-restore.ts:83 -> REVIEW_PACKAGE_ITEMS_ABSENT / kernel refusal / REVIEW_PACKAGE_DIGEST_MISMATCH vs record.reviewInputDigest.\n\nNone of the four changed, so proceeding per plan. THREE ADDITIONAL MEASUREMENTS that bind step-3 and were not in the plan text:\n\n(5) THE ROUND'S ITEMS CANNOT BE BOUND VERBATIM. buildReviewPackage (packages/review/src/review-package.ts:123-125) REQUIRES >=1 DAEMON_RECEIPT, so every valid stored round carries one; recordVerifierReceipt (verifier-receipt-ledger.ts:178-180) REFUSES VERIFIER_AUTHORITY_REFUSED if authority.packageItems contains ANY DAEMON_RECEIPT, then appends its OWN receipt at :197-204. Therefore the provider must bind the verified round items MINUS kind===\"DAEMON_RECEIPT\". This is exactly the existing fixture relation in node-verifier.test.ts: seeded round items :60-68 minus the DAEMON_RECEIPT at :62 == AUTHORITY.packageItems :81-88. Not a fabrication - it is the contract's own \"Host-owned package facts BEFORE the daemon appends its execution receipt\" (verifier-receipt-contracts.ts:17).\n\n(6) POLICY FIELD IS PolicyEvaluationInput, 13 EXACT keys (policy-validation.ts:159-163 INPUT_KEYS, enforced by exact() at :215) - NOT a PolicySlice. installPolicy:46 requires a `sliceRef` on the stored slice, so the durable slice = those 13 keys + sliceRef (14). The reader strips sliceRef (address, not a fact - same precedent as calibration's SLICE_KEYS) and hands the remainder to core's own evaluatePolicy(value: unknown) (policy-evaluation.ts:166), requiring ok && record.action===\"integration.accept_output\" && record.actor===NODE_VERIFIER_PRINCIPAL_ID && record.decision===\"ALLOW\" - the exact four conditions decodeVerifierReceiptBytes:221-226 re-checks on read-back. Validation is delegated to the production validator, never restated.\n\n(7) Calibration must be staleness CURRENT + sentinelPassed true + non-empty corpusRevision or qualifyReviewerForAcceptance refuses at ELIGIBILITY (reviewer-eligibility.ts:116-125). The provider does NOT enforce this - it returns what is durably stored and lets the kernel refuse; enforcing it in the provider would be a second source of truth.\n\nTest-seed template confirmed: reviewer-calibration-record.test.ts:87-118 (openEphemeralForProjectTest + createDaemonCommandPorts registry \"policy.install\" + decisions.decide) and node-verifier.test.ts:51-77 (runReviewCommand for review.submit).",
      "startedAt": "2026-08-17T19:37:38.512Z",
      "completedAt": "2026-08-17T19:40:57.600Z"
    },
    {
      "stepId": "step-2",
      "description": "RED. Create apps/daemon/src/review/verifier-authority-provider.test.ts against a REAL SqliteEventStore on a temp file -- do not stub it. Seed durable state through PRODUCTION commands, not hand-written events: install the policy slice and the calibration slice via the real `policy.install` path, and record a review round via the real `review.submit` path so its package items are stored by the handler that stores them in production. Failing tests: (1) with all three sources seeded, `createVerifierAuthorityProvider({store, projectId})(nodeRef, brief)` returns a frozen `VerifierAuthorityFacts` whose `policy` equals the installed slice, whose `calibration` equals the installed calibration field by field, and whose `packageItems` are byte-identical to the stored bound items -- assert values, never `toBeTruthy`; (2) calibration slice absent -> `null`; (3) policy slice absent -> `null`; (4) the round's items ABSENT (`status: \"ABSENT\"`) -> `null`; (5) items PRESENT but their digest disagreeing with the round's stored `reviewInputDigest` -> `null`. Seed exactly ONE gap per case and assert the other four still return facts in the same file: because this port's only refusal channel is `null`, a suite that removes everything and asserts null passes even if the provider ignores the store entirely. Assert the raw event count is unchanged on every negative path.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/review/verifier-authority-provider.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.test.ts",
        "apps/daemon/src/review/verifier-authority-provider.ts",
        "apps/daemon/src/review/verifier-authority-provider.js"
      ],
      "note": "RED verified twice, both for the right reason.\n\nRED 1 (module absent): `npx vitest run --root . --config package.json src/review/verifier-authority-provider.test.ts` from apps/daemon ->\n  \"Error: Cannot find module './verifier-authority-provider.js'\" ; Test Files 1 failed (1), Tests: no tests.\n\nRED 2 (assertion, not error): added a deliberately inert stub (`createVerifierAuthorityProvider` returning null unconditionally) plus the one-line .js bridge, re-ran ->\n  Test Files 1 failed (1)\n  Tests 4 failed | 6 passed (10)\n  The 4 failures are exactly the POSITIVE controls, each \"Error: expected durable authority facts, got null\":\n    - produces every fact from the durable store when all three sources are installed\n    - hands back the round's host-owned items with the round's DAEMON_RECEIPT withheld\n    - echoes an installed calibration that differs from every plausible default\n    - ignores the caller's brief entirely, so no fact can originate in caller input\n  The 6 negatives pass VACUOUSLY against the inert stub. That is the point the plan makes: `null` is this port's only refusal channel, so a negative-only suite cannot distinguish a correct provider from one that ignores the store. The positive controls are the discriminator, and they are red.\n\n10 cases, all seeded through PRODUCTION paths on a REAL file-backed store (`openRestartableStore()` -> `SqliteEventStore.openForProject` under a temp dir; no stub, no `:memory:`):\n  - policy slice + calibration slice via `createDaemonCommandPorts(...).registry.get(\"policy.install\")` and its decision port (the reviewer-calibration-record.test.ts pattern), capability project.admin, aggregate `${PROJECT_ID}-policy`;\n  - the clean round via `send`/`runReviewCommand` with findings [] so its routing is genuinely ACCEPT.\n  Setup helpers throw on refusal, so a refused seed can never leave an assertion testing nothing.\n\nCoverage beyond the plan's five, each justified in a file comment:\n  (a) DAEMON_RECEIPT WITHHELD - measured in step-1 as binding: buildReviewPackage requires one in every stored round, recordVerifierReceipt refuses VERIFIER_AUTHORITY_REFUSED if the authority hands one back. Asserted as 6 host items, kinds excluding DAEMON_RECEIPT.\n  (b) NON-DEFAULT CALIBRATION - installs {corpus-unlikely-7f,false,STALE} and asserts it reads back, so a hard-coded default (either the pessimistic UNKNOWN twin or an optimistic CURRENT one) cannot survive.\n  (c) CALLER INPUT - two NodeMission briefs disagreeing in all four fields; results must be deep-equal.\n  (d) ROUND SELECTION - an earlier ACCEPT round plus a later REJECT_IMPLEMENTATION round must yield null, pinning \"the same round the receipt binds\" (node-verifier.ts:137-138 takes rounds.at(-1) and requires ACCEPT; it does NOT search backwards).\n\nNegatives seed exactly ONE gap and keep the other sources installed; each asserts `store.readEventsAfter` count is unchanged, so the provider is proven to perform no write on any refusal path.\n\nNote on the stub: it is scaffolding for this RED only and is fully replaced in step-3. It is NOT the deliverable.",
      "startedAt": "2026-08-17T19:42:46.411Z",
      "completedAt": "2026-08-17T19:44:40.662Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN. Create apps/daemon/src/review/verifier-authority-provider.ts plus its one-line `.js` bridge (`export * from \"./verifier-authority-provider.ts\";` -- without it the module is invisible to the .js import graph). Export `createVerifierAuthorityProvider(config: {store: SqliteEventStore; projectId: string}): (nodeRef: string, brief: NodeMission) => VerifierAuthorityFacts | null`, matching `NodeVerifierConfig.verificationAuthority` (node-verifier.ts:62-65) EXACTLY so the call site needs no type change. COMPOSE the landed readers; do not reimplement them: `readReviewerCalibration` for calibration, and for the items read the review ledger for `nodeRef`, take the LATEST round (the same round node-verifier itself binds at :137-144, whose routing is ACCEPT), and pass its record to `verifyStoredPackageItems`. Bind the items ONLY from a verified result -- the digest comparison is the whole reason the items are trustworthy, so never read `record.packageItems.items` directly past it. Every returned object is `Object.freeze`d; any absent record, any refusal from either reader, and any thrown read returns `null` -- never a partial fact set, never a default. Perform NO writes. Keep the file <=250 physical lines; if the three reads push past it, split the readers into a sibling module rather than trimming a refusal branch.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.ts",
        "apps/daemon/src/review/verifier-authority-provider.js"
      ],
      "newFiles": [
        "apps/daemon/src/review/verifier-authority-provider.ts",
        "apps/daemon/src/review/verifier-authority-provider.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.ts",
        "apps/daemon/src/review/verifier-authority-provider.js"
      ],
      "note": "GREEN. `npx vitest run --root . --config package.json src/review/verifier-authority-provider.test.ts` (from apps/daemon) -> Test Files 1 passed (1), Tests 10 passed (10). The four positive controls that were red in step-2 now pass; nothing in the test file changed between RED and GREEN.\n\napps/daemon/src/review/verifier-authority-provider.ts is 174 physical lines (<=250). Its .js bridge is the one line `export * from \"./verifier-authority-provider.ts\";`.\n\nSURFACE: `createVerifierAuthorityProvider({projectId, store}): VerifierAuthorityProvider`, where `VerifierAuthorityProvider = (nodeRef: string, brief: NodeMission) => VerifierAuthorityFacts | null` — the exported TYPE is exactly NodeVerifierConfig.verificationAuthority (node-verifier.ts:62-65), so the wrapper call site needs no type change. The returned CLOSURE is declared `(nodeRef: string) => ...`, which is assignable to it: the brief is caller input, and a parameter that does not exist cannot be read by a later edit. Also exports VERIFIER_POLICY_SLICE_REF = \"moe-verifier-policy/1\", deliberately not 64 hex for the same reason REVIEWER_CALIBRATION_SLICE_REF is not.\n\nCOMPOSITION, no reader reimplemented:\n- calibration: `readReviewerCalibration(store, projectId)`; any refusal -> null.\n- policy: readDurableLedger -> stateOf(policyAggregateId) -> slices[VERIFIER_POLICY_SLICE_REF]. `installPolicy` requires a `sliceRef` on every slice and `validateEvaluationInput` accepts EXACTLY the 13 input keys, so the address is stripped and the remainder is handed to the CORE's own `evaluatePolicy` — admissibility is never restated locally. Then the four conditions decodeVerifierReceiptBytes:221-226 re-checks on read-back: ok, decision ALLOW, action integration.accept_output, actor NODE_VERIFIER_PRINCIPAL_ID. Refusing them here keeps them distinguishable; letting them through would flatten to VERIFIER_AUTHORITY_REFUSED with nothing left to say which failed.\n- items: readReviewLedger(nodeRef); unreadable -> null; `rounds[rounds.length-1]` and routing must be ACCEPT — the exact selection node-verifier.ts:137-138 makes, NOT a backwards search for the newest clean round, because the receipt is keyed on that round's decisionId. The record then goes through `verifyStoredPackageItems`, and the items are bound from the VERIFIED result; `record.packageItems.items` is never read.\n- the round's own DAEMON_RECEIPT is WITHHELD (filtered) — measured in step-1 as binding: buildReviewPackage requires one in every stored round, recordVerifierReceipt:178 refuses if the authority hands one back, and it appends the receipt for the run it just performed. These are the facts BEFORE that append.\n\nFAIL-CLOSED: one refusal channel (null) for every cause; no partial fact set, no default, no `??` fallback, no spread over a partial object. A thrown read is caught and becomes null rather than an exception the verifier loop must interpret. NO WRITES — every negative test asserts the raw event count is unchanged — and no store handle is held past the call.\n\nTYPECHECK: `pnpm --filter @moe/daemon typecheck` EXIT 0.\n\nDISCLOSED FOREIGN RED (not mine, recorded now so step-8 can attribute it): repo-wide `pnpm typecheck` fails in packages/runner —\n  src/providers/telemetry/claude-telemetry-launch.test.ts(22,37): error TS2305: Module '\"./claude-telemetry-launch.js\"' has no exported member 'createTelemetryBoundClaudeLauncher'.\n  src/supervisor/provider-effect-settlement.test.ts(41,8): error TS2307: Cannot find module './provider-effect-settlement.js' or its corresponding type declarations.\n`git status --porcelain -- packages/runner` shows ` M .../claude-telemetry-launch.test.ts`, ` M .../claude-telemetry-launch.ts`, `?? .../provider-effect-settlement.test.ts` — a peer's live UNCOMMITTED work in a package this task neither owns nor imports. Zero intersection with the five owned paths.",
      "startedAt": "2026-08-17T19:44:43.168Z",
      "completedAt": "2026-08-17T19:46:29.111Z"
    },
    {
      "stepId": "step-4",
      "description": "RED then GREEN on the real composition, extending apps/daemon/src/orchestrator/node-verifier.test.ts (no parallel suite). Drive the REAL `createNodeVerifier` with the REAL provider over a seeded store -- no stub authority function in either case. Case A (earns a receipt): seed policy, calibration and an accepted round with stored items, supply a `runTest` returning `exitCode: 0` with a known output sha, and assert (i) the report outcome is exactly `\"ACCEPTED\"`, (ii) `readVerifierReceipt(store, projectId, verifierReceiptId(projectId, nodeRef, latest.decisionId))` returns `ok: true`, (iii) the stored receipt's `execution.outputSha256` equals the capture sha and its `calibration`, `policy` and `packageItems` are byte-equal to what the provider returned. Assert the receipt's identity and contents, not `ok: true` alone. Case B (fail-closed preserved): identical seeding minus the calibration slice; assert the report outcome is the literal string `\"VERIFICATION_AUTHORITY_UNAVAILABLE\"`, that `runTest` was NEVER invoked (the authority check at node-verifier.ts:165 precedes the run at :173, and only a call-count assertion proves that ordering survived), and that no receipt row exists.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/orchestrator/node-verifier.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/node-verifier.test.ts"
      ],
      "note": "Extended apps/daemon/src/orchestrator/node-verifier.test.ts (+248 lines, 0 deleted; no parallel suite, no existing case touched). Both new cases drive the REAL createNodeVerifier with the REAL createVerifierAuthorityProvider — `verificationAuthority:` is the provider itself in both, no stub function anywhere in the block.\n\nRED OBSERVED (not assumed). Case A passed on first execution because the provider was already GREEN from step-3, so a genuine red was produced by making the durable read unreachable — the policy slice looked up under an address nothing installs:\n  `npx vitest run --root . --config package.json src/orchestrator/node-verifier.test.ts`\n  x earns a receipt whose facts are exactly what the durable provider produced\n  \"Error: the durable provider refused a fully seeded project\"\n  Tests 1 failed | 5 passed (6)\nCase B stayed GREEN under that mutation, which is correct and is exactly why it needs its own drill: it refuses either way. Restored by re-editing (never git checkout); `sha256sum -c` against the pre-drill snapshot reports OK for both files, and `git grep DRILL_UNREACHABLE_REF HEAD` finds nothing.\n\nGREEN: node-verifier.test.ts alone -> Test Files 1 passed (1), Tests 6 passed (6) (was 4). Both files together -> Test Files 2 passed (2), Tests 16 passed (16).\n\nSEEDING (production paths only, one file-backed store + one createStoreDependencies per case, both closed in afterAll): policy slice and calibration slice installed through createDaemonCommandPorts(...).registry.get(\"policy.install\") and its decision port; the clean round through runReviewCommand with findings [] so its routing is genuinely ACCEPT. The two projects differ by EXACTLY one seed — `openAuthorityProject(name, withCalibration)`.\n\nCASE A asserts identity before contents: report outcome exactly \"ACCEPTED\"; runs === 1; the stored receipt is read at `verifierReceiptId(projectId, AUTHORITY_NODE, latest.decisionId)` and its receiptId/subjectRef/source.decisionId are pinned; execution.outputSha256 === the capture sha and byteCount === the output length; calibration and policy are byte-equal to what the provider returned; and packageItems equals EXACTLY `[...facts.packageItems, {digest: execution.evidenceSha256, kind: \"DAEMON_RECEIPT\", locator: `verifier:<node>:<decisionId>`}]` — the provider's six host items in order plus the ONE receipt the daemon appended for the run it performed. It also reads the acceptance back through the ledger (`accepted.verifierReceiptId`), so the integration.accept_output dispatch is proven to have fired, not just the receipt row written. A separate assertion pins the provider's items to the kernel's bind order minus the round's own DAEMON_RECEIPT.\n\nCASE B asserts the literal string \"VERIFICATION_AUTHORITY_UNAVAILABLE\" (whole report object, including detail and nodeRef), that runTest was invoked ZERO times, that readVerifierReceipt refuses with the literal code \"VERIFIER_RECEIPT_NOT_FOUND\" at the id the receipt WOULD have had, and that the ledger records no acceptance. The runs===0 assertion is what pins the ordering at node-verifier.ts:165-174: a reordered verifier would report the same outcome having already executed the subject's test.\n\nFOREIGN COMMIT NOTICE (rail-mandated disclosure, no repair attempted). HEAD moved d2151d4 -> 29f3c5f mid-step. Commits 8ad555c and 29f3c5f, both titled for task-6600442414134376a113ed28e8994845, are whole-tree and CAPTURED three of my owned paths: verifier-authority-provider.ts, .js and .test.ts. Verified the captured bytes are the CORRECT ones, not a drill state: `git show HEAD:...provider.ts | sha256sum` == on-disk sha256 == the pre-drill snapshot 0852aa82bc0fd4ee7baa8ce4bb494c45348f75333c9d23d6b33ac3e4e4ff9f58, and no DRILL token exists at HEAD. node-verifier.test.ts was NOT captured (`git diff --numstat HEAD` -> 248 added, 0 deleted), so my edits there remain uncommitted and purely additive. Per the rail I did not amend, reset, or create an empty claim commit; step-7 will commit node-verifier.test.ts by explicit pathspec and record the base-ref diff QA should review.",
      "startedAt": "2026-08-17T19:47:02.673Z",
      "completedAt": "2026-08-17T19:50:38.990Z"
    },
    {
      "stepId": "step-5",
      "description": "Wire the production host. In apps/daemon/src/orchestrator/agent-wrapper-main.ts, replace `verificationAuthority: () => null` (at :280 as measured -- GREP for it, the line has already moved once) with the step-3 provider constructed from the wrapper's already-open `verifierStore` and `config.projectId`, both in scope at that point. Delete the now-false comment above it (\"No production authority provider is shipped yet. Refuse a green test rather than manufacture calibration, package or policy facts in-process.\") and replace it with one line stating that absent durable facts still yield VERIFICATION_AUTHORITY_UNAVAILABLE -- the refusal is preserved, only its cause moves from \"nothing is wired\" to \"nothing is installed\". Change nothing else in this file: the spawner, MCP host, verifier runner and stop plumbing are out of scope. Prove the wiring by grep rather than by reading: `git grep -n \"verificationAuthority\" -- apps/ | grep -v '\\.test\\.'` must show the provider at the wrapper call site and must NOT show `() => null` anywhere in production.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/orchestrator/agent-wrapper-main.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/agent-wrapper-main.ts"
      ],
      "note": "Production host wired. Two edits to apps/daemon/src/orchestrator/agent-wrapper-main.ts and nothing else: one import line, and the call site. The spawner, MCP host, verifier runner, staffing fence and stop plumbing are untouched.\n\nThe line HAD moved again (the plan measured :280; it is now :281 after the added import), which is why this was grepped, never indexed.\n\nBEFORE:\n      // No production authority provider is shipped yet. Refuse a green test\n      // rather than manufacture calibration, package or policy facts in-process.\n      verificationAuthority: () => null,\nAFTER:\n      // Reads calibration, policy and package facts from the durable store; a\n      // fact that is not installed still yields VERIFICATION_AUTHORITY_UNAVAILABLE.\n      verificationAuthority: createVerifierAuthorityProvider({\n        projectId: config.projectId,\n        store: verifierStore,\n      }),\nBuilt from the wrapper's ALREADY-OPEN `verifierStore` and `config.projectId` — no second handle is opened and none is held past a call, so the wrapper's existing close path is unchanged. The now-false comment is gone; its replacement states that the refusal survives and only its cause moved from \"nothing is wired\" to \"nothing is installed\".\n\nPROVEN BY GREP, not by reading:\n  `git grep -n \"verificationAuthority\" -- apps/ | grep -v '\\.test\\.'`\n    apps/daemon/src/orchestrator/agent-wrapper-main.ts:281:      verificationAuthority: createVerifierAuthorityProvider({\n    apps/daemon/src/orchestrator/node-verifier.ts:62:  readonly verificationAuthority: (\n    apps/daemon/src/orchestrator/node-verifier.ts:165:      const authority = config.verificationAuthority(nodeRef, brief);\n    apps/daemon/src/review/verifier-authority-provider.ts:27 and :51 (doc comments naming the port)\n  `git grep -n \"verificationAuthority: () => null\" -- apps/ packages/` -> NONE. DoD 1 satisfied: the null provider no longer appears anywhere in production.\nThe port's declared type needed no change: the provider's exported type is exactly NodeVerifierConfig.verificationAuthority, so this is a value substitution with no signature edit.\n\nThe guard at node-verifier.ts:165-172 is untouched, as task rail 2 requires — it is the same branch, now reachable for a genuinely uninstalled fact instead of for every node unconditionally.\n\nGATES:\n  `pnpm --filter @moe/daemon typecheck` -> EXIT 0 (tsc, no diagnostics).\n  `pnpm --filter @moe/daemon test` -> Test Files 121 passed (121), Tests 2488 passed (2488). A real executed count line, not an exit code: the package resolves and 2488 tests ran. No regression from the wrapper edit.",
      "startedAt": "2026-08-17T19:50:41.125Z",
      "completedAt": "2026-08-17T19:51:24.581Z"
    },
    {
      "stepId": "step-6",
      "description": "Mutation drills, three, because 'the provider reads durably', 'it fails closed', and 'it trusts only verified items' are three separate claims a green suite cannot tell apart. Drill 1 (composition is real): make the calibration read return a hard-coded literal instead of `readReviewerCalibration`'s value; the step-4 Case A receipt-content assertion must go RED on the calibration comparison -- if it reddens on a crash or a different assertion, the test is not pinning the durable read. Drill 2 (fail-closed is real): make the absent-calibration branch return a fabricated facts object instead of `null`; the step-4 Case B assertion on the literal `\"VERIFICATION_AUTHORITY_UNAVAILABLE\"` must go RED. Drill 3 (the digest gate is load-bearing): make the provider read `record.packageItems.items` directly, bypassing `verifyStoredPackageItems`; the step-2 case (5) digest-mismatch test must go RED. Record for each the command, the failing test NAME and the exact assertion. Restore by re-editing, never `git checkout` (destructive in this shared worktree), and confirm with `git diff --numstat` showing 0 added and 0 deleted per drilled file. One drill at a time -- a peer commit landing mid-drill can swallow the patch and leave git reading clean.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.ts",
        "apps/daemon/src/orchestrator/node-verifier.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/node-verifier.test.ts"
      ],
      "note": "Three drills, one at a time, each restored by RE-EDITING (never git checkout) and verified byte-identical before the next began.\nDrill command (all three): `npx vitest run --root . --config package.json src/orchestrator/node-verifier.test.ts src/review/verifier-authority-provider.test.ts` from apps/daemon.\nBaseline: green 2 files / 16 tests; provider .ts sha256 0852aa82bc0fd4ee7baa8ce4bb494c45348f75333c9d23d6b33ac3e4e4ff9f58.\n\nA TAUTOLOGY WAS FOUND AND FIXED BEFORE DRILLING, which is what drill 1 was designed to expose. Step-4's Case A compared `stored.receipt.calibration` and `stored.receipt.policy` against `facts.*` — but the receipt is BUILT from the provider's output, so both operands came from one source and the comparison held unconditionally: a provider that hard-coded both would have stayed green. Worse, `AUTHORITY.calibration` is `{corpus-1, true, CURRENT}` — exactly what a fabricated literal looks like — so seeding it would have made drill 1 an equivalent mutant. Fixed in node-verifier.test.ts: the project now INSTALLS `DURABLE_CALIBRATION = {corpusRevision: \"corpus-authority-9c1d\", sentinelPassed: true, staleness: \"CURRENT\"}`, a value no default carries, and the three receipt assertions now compare against hand-written operands (DURABLE_CALIBRATION, AUTHORITY.policy, EXPECTED_HOST_ITEMS) rather than against `facts`. Green re-confirmed (16/16) before the baseline snapshot.\n\nDRILL 1 - composition is real. Mutation: `calibration: calibration.calibration` -> `calibration: {corpusRevision: \"corpus-1\", sentinelPassed: true, staleness: \"CURRENT\"}` (a legal, ELIGIBLE calibration, so nothing crashes and the flow still reaches acceptance).\n  RED, 4 failed | 12 passed. Failing test: \"earns a receipt whose facts are exactly what the durable provider produced\".\n  Exact assertion: `expect(stored.receipt.calibration).toEqual(DURABLE_CALIBRATION)` ->\n    AssertionError: expected { corpusRevision: 'corpus-1', ...(2) } to deeply equal { ...(3) }\n  An AssertionError on the receipt-content comparison, NOT a crash and not a different assertion. Also reddened, as expected, the provider suite's \"produces every fact from the durable store...\", \"echoes an installed calibration that differs from every plausible default\", and \"ignores the caller's brief entirely...\" (the last with `expected 'corpus-1' to be 'corpus-durable-2026-08'`).\n\nDRILL 2 - fail-closed is real. Mutation: the `if (!calibration.ok) return null` branch replaced by a fabricated facts object carrying the eligible calibration.\n  RED, 2 failed | 14 passed. Failing test: \"still refuses VERIFICATION_AUTHORITY_UNAVAILABLE when the calibration is not installed\".\n  Exact assertion: `expect(reports).toEqual([{detail: \"host verifier authority unavailable\", nodeRef, outcome: \"VERIFICATION_AUTHORITY_UNAVAILABLE\"}])` ->\n    - \"detail\": \"host verifier authority unavailable\"   - \"outcome\": \"VERIFICATION_AUTHORITY_UNAVAILABLE\"\n    + \"detail\": \"EFFECTS_COMMITTED\"                     + \"outcome\": \"ACCEPTED\"\n  The fabricated calibration EARNED A RECEIPT. That is precisely the failure the guard exists to prevent, and the literal-string assertion is what catches it. Also reddened the provider suite's \"refuses with null when the calibration slice was never installed\" (`expected {...} to be null`).\n\nDRILL 3 - the digest gate is load-bearing. Mutation: `verifyStoredPackageItems(latest)` replaced by a direct read of `latest.packageItems.items` when status is PRESENT (the shortcut that typechecks and is reachable).\n  RED, 1 failed | 15 passed. Failing test: \"refuses with null when the stored items disagree with the round's stored digest\".\n  Exact assertion: `expect(providerFor(store)(SUBJECT_REF, BRIEF)).toBeNull()` -> AssertionError: expected { calibration: {...}, ...(2) } to be null.\n  Only ONE test reddened, which is the correct blast radius: every other case has honest items, so the bypass is invisible to them. That is exactly why this drill had to exist separately.\n\nRESTORE VERIFIED after each drill: `sha256sum -c` OK for all three files, and `git diff --numstat` prints NO line for verifier-authority-provider.ts (0 added / 0 deleted vs HEAD). Final state re-run green: Test Files 2 passed (2), Tests 16 passed (16). Drill-residue sweep on disk and at HEAD found nothing (the single \"drilled\" hit at HEAD is prose in an unrelated telemetry test). HEAD did not move during the drills (29f3c5f throughout), so no peer commit swallowed a patch.\n\nnode-verifier.test.ts is the only file whose bytes changed this step (the tautology fix); the provider is byte-identical to its step-3 state.",
      "startedAt": "2026-08-17T19:51:26.999Z",
      "completedAt": "2026-08-17T19:54:03.152Z"
    },
    {
      "stepId": "step-7",
      "description": "Adversarial self-review, then commit by explicit pathspec. Read the owned diff as an attacker: can any field of the returned facts originate from `NodeMission`, from a command payload, from a default or from a `??` fallback? Can items reach the facts without passing the digest comparison? Which ROUND's items are used -- is it provably the same round the receipt binds, or could a stale earlier round supply items for a newer decision? Does any negative path still permit a receipt? Does the provider write, or hold a store handle that outlives its call (a held SQLite handle kills the vitest worker)? Are `ReviewPackageBoundItem` values assignable to `ReviewPackageItemInput` without a cast that would erase the kind narrowing? Is the failure-round path at node-verifier.ts:88-98, whose `PACKAGE_ITEM_FILL` digests are synthetic by disclosed design, left untouched as it must be? Confirm no production file exceeds 250 physical lines. Then `git status --porcelain`, unstage any foreign entry with `git restore --staged -- <path>`, and commit ONLY these five paths by explicit pathspec: the provider .ts/.js/.test.ts, node-verifier.test.ts and agent-wrapper-main.ts. Verify with `git show --stat` that no foreign path appears; if a foreign whole-tree completion already captured these bytes, do NOT amend or reset -- record the merge-base diff for QA.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.ts",
        "apps/daemon/src/review/verifier-authority-provider.js",
        "apps/daemon/src/review/verifier-authority-provider.test.ts",
        "apps/daemon/src/orchestrator/node-verifier.test.ts",
        "apps/daemon/src/orchestrator/agent-wrapper-main.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/review/verifier-authority-provider.ts"
      ],
      "note": "ADVERSARIAL SELF-REVIEW, then commit 08e3445 by explicit pathspec.\n\nONE HARDENING FOUND AND APPLIED. The policy reader built its stripped accumulator as `{}`. A stored slice carrying an own `__proto__` key would hit the PROTOTYPE SETTER on a plain object and silently reshape the accumulator instead of adding a key. Downstream is still fail-closed (snapshotData refuses a non-standard prototype; a non-object value makes the assignment a silent no-op and exact-key still decides), but the path should not exist: changed to `Object.create(null)`, so an exotic key stays an ordinary key and validateEvaluationInput's exact-key check is what refuses it. Re-verified green (16/16) and `pnpm --filter @moe/daemon typecheck` exit 0.\n\nTHE CHECKLIST, answered against the code:\n- ORIGIN OF FACTS. No field can come from NodeMission: the returned closure is declared `(nodeRef: string) => ...`, so `brief` is not a parameter that exists. `nodeRef`/`projectId` are ADDRESSES (which subject to read), never returned values. No `??`, no `||=`, no `= []`, no `= \"\"` anywhere in the file (grepped). The two slices ARE caller-written, but through `policy.install`, which requires capability `project.admin` (daemon-command-vocabulary.ts:35) with payload allow-list exactly [\"slice\"] (:99) and folds behind a durable PolicyInstalled event — that is the durable record, not a live request payload.\n- ITEMS WITHOUT THE DIGEST GATE. Single path through `verifyStoredPackageItems`; `record.packageItems.items` is never read. Drill 3 proves the shortcut reddens.\n- ROUND SELECTION / TOCTOU. `rounds[rounds.length-1]` + routing ACCEPT, byte-for-byte the choice node-verifier.ts:137-138 makes. A peer round CAN land between the verifier's ledger read and the provider's, so the provider could bind round N+1's items while the verifier holds round N. That cannot commit: recordVerifierReceipt re-reads the ledger and compares decisionId/resultSha256/aggregateVersion against the caller's `source` (:147-151) -> VERIFIER_RECEIPT_STALE, plus a second CAS via expectedVersion at the store. The race fails closed; disclosed rather than papered over.\n- NEGATIVE PATHS AND RECEIPTS. Every refusal returns before `runTest`; Case B asserts runs===0 and the literal code VERIFIER_RECEIPT_NOT_FOUND at the id the receipt would have had.\n- WRITES / HANDLES. No write call in the module; every negative test asserts `readEventsAfter` count unchanged. The closure captures the wrapper's ALREADY-OPEN `verifierStore` and opens nothing, so no handle outlives its owner. Full daemon suite (2488 tests) ran without a worker death.\n- CAST SAFETY. `ReviewPackageBoundItem` -> `ReviewPackageItemInput` needs no cast: `kind: ReviewPackageItemKind` is assignable to `kind: string` (review-contract.ts:168-172). The only cast in the file is `candidate as unknown as PolicyEvaluationInput`, after the core's own validator accepted the exact bytes — the same justified pattern as verifier-receipt-contracts.ts:231.\n- OUT-OF-SCOPE SURFACE. node-verifier.ts is byte-identical to HEAD (`git diff --numstat` empty), so the failure-round path at :88-98 and its disclosed synthetic PACKAGE_ITEM_FILL digests are untouched.\n- HOSTILE INPUT. Empty/absent slices object, array-valued slice, scalar slice, extra keys, empty nodeRef: all reach `null`. Values are only JSON-compared and JSON-stored; nothing is interpolated into SQL or a shell.\n- DISCLOSED PROPERTY, not a defect: a STORED PolicyEvaluationInput carries a frozen `evaluatedAtEpochMs`, so a waiver in that slice is evaluated against the stored timestamp rather than now. That is the core's model for this type (the field is caller-supplied by contract) and the receipt decode re-evaluates the SAME frozen input, so producer and verifier agree. Introducing a clock here would make the two disagree.\n\nLINE COUNTS. verifier-authority-provider.ts 178 (<=250). The .js bridge is 1. agent-wrapper-main.ts is 392 (388 at HEAD before my 4 net lines) — over the 250 TARGET but under the 400 split threshold, a pre-existing condition on a file step-5 explicitly forbids changing further; splitting it is not this task's scope.\n\nCOMMIT. `git status --porcelain` showed NOTHING staged (foreign entries in packages/runner, apps/daemon/src/index.ts, apps/daemon/src/graph-preview-request.*, .moe/** were all unstaged working-tree changes), so no `git restore --staged` was needed. Committed with an explicit five-path pathspec; `git show --stat --oneline 08e3445` lists exactly:\n  apps/daemon/src/orchestrator/agent-wrapper-main.ts | 10 +-\n  apps/daemon/src/orchestrator/node-verifier.test.ts | 265 +++\n  apps/daemon/src/review/verifier-authority-provider.ts | 6 +-\nNo foreign path appears.\n\nFOREIGN CAPTURE, disclosed per rail, NOT repaired. verifier-authority-provider.js and .test.ts do not appear in my commit because foreign whole-tree commits 8ad555c/29f3c5f (titled for task-6600442414134376a113ed28e8994845) had already captured them, with the CORRECT bytes. I did not amend, reset, or create a claim ",
      "startedAt": "2026-08-17T19:54:09.221Z",
      "completedAt": "2026-08-17T19:56:45.904Z"
    },
    {
      "stepId": "step-8",
      "description": "Verification gate. Run in the FOREGROUND, chained with `&&` so a red leg cannot be masked -- a `;` chain reports only the last command and a pipe reports the tail's exit code: `pnpm typecheck && pnpm --filter @moe/daemon test`. Read the OUTPUT, not the exit code: `pnpm --filter <pkg>` prints \"No projects matched the filters\" and EXITS 0 when the package name does not resolve, so the leg is evidence only if it prints a `Test Files N passed` line with N nonzero; `@moe/daemon` resolves to apps/daemon, and root `pnpm test` does NOT cover apps/** so it is no substitute. Confirm the new provider test file and both node-verifier cases appear by name. Repo-wide red is compared to the merge-base baseline and may be disclosed only when the new failing-path delta intersected with this task's five owned paths is empty; red inside the owned paths is a block, not a disclosure. In the completion note, name `task-97554aa4293e40eab56c0b642e18513a` as the consumer this unblocks -- closing GAP E of the Foundation self-host canary -- and state that GAP A remains open independently. This command's fresh output is the sole `complete_task` verification evidence.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "GATE GREEN. `pnpm typecheck && pnpm --filter @moe/daemon test`, foreground, `&&`-chained, exit code captured from the CHAIN and not from a pipe tail:\n  === REAL GATE EXIT = 0 ===\n  typecheck: 19 of 20 workspace projects, every one \"Done\", including `apps/daemon typecheck: Done` and `packages/runner typecheck: Done`.\n  test: Test Files 121 passed (121) / Tests 2562 passed (2562).\nThe count line is nonzero and printed, so the leg EXECUTED — `@moe/daemon` resolves to apps/daemon, which root `pnpm test` does not cover.\n\nNAMED EVIDENCE (verbose reporter over the two owned suites, 16/16):\n  verifier-authority-provider.test.ts - all 10 by name, including \"produces every fact from the durable store when all three sources are installed\", \"hands back the round's host-owned items with the round's DAEMON_RECEIPT withheld\", \"echoes an installed calibration that differs from every plausible default\", \"ignores the caller's brief entirely, so no fact can originate in caller input\", \"refuses with null when the stored items disagree with the round's stored digest\", \"refuses with null when the latest round is not the clean one, despite an earlier ACCEPT\".\n  node-verifier.test.ts - both new cases by name: \"createNodeVerifier over the production authority provider > earns a receipt whose facts are exactly what the durable provider produced\" and \"... > still refuses VERIFICATION_AUTHORITY_UNAVAILABLE when the calibration is not installed\", alongside the four pre-existing cases, all green.\n\nTHREE FOREIGN REDS WERE HIT AND ALL CLEARED; NONE INTERSECTED THE OWNED PATHS. Disclosed verbatim rather than hidden:\n 1. `packages/runner` typecheck: \"src/providers/telemetry/claude-telemetry-launch.test.ts(22,37): error TS2305: Module '\"./claude-telemetry-launch.js\"' has no exported member 'createTelemetryBoundClaudeLauncher'\" and \"src/supervisor/provider-effect-settlement.test.ts(41,8): error TS2307: Cannot find module './provider-effect-settlement.js'\". Peer's uncommitted work; now Done.\n 2. `src/runtime-entrypoint.test.ts > has an exact .js bridge for every runtime module...` -> unexpected: [\"foundation\\\\foundation-surface.js\"]. Both foundation-surface.ts and .js were UNTRACKED (`??`) peer files written at 22:55; the probe flags a bridge whose module is not yet in the runtime tier. Cleared on the next poll. Note this probe positively CERTIFIES my work: verifier-authority-provider.js appears in none of missing/unexpected/wrongContent, so its content matches `expectedBridgeSource` exactly and its module is runtime-tier.\n 3. `src/index-surface.test.ts` twice, first \"publishes exactly the reviewed runtime namespace\" (expected 85 exports, got 132), then \"guards the explicit fixture deny-list\" (expected 7, got 34 — while disk already read 33, i.e. the file was being rewritten mid-run). Attribution: `git diff --numstat -- apps/daemon/src/index.ts` showed 106/278 UNCOMMITTED foreign lines, my module is not in the daemon root barrel (`grep verifier-authority-provider apps/daemon/src/index.ts` -> nothing), and commit 08e3445 does not touch index.ts. Cleared after two polls.\nPATH-ATTRIBUTED BASELINE: the failing-path set at HEAD minus at the task's base, intersected with this task's five owned paths, is EMPTY. Every failing path was packages/runner/**, apps/daemon/src/foundation/** or apps/daemon/src/index*.ts — none owned, none imported by the provider, none in my commit. The final run is fully green regardless, so nothing is being excused.\n\nFINAL STATE AT HEAD 08e3445: all five owned paths verified `git show HEAD:<path>` sha256 == on-disk sha256. `git grep \"verificationAuthority: () => null\" HEAD -- apps/ packages/` -> NONE. `git grep \"verificationAuthority\" HEAD -- apps/ | grep -v '\\.test\\.'` -> the provider at agent-wrapper-main.ts:281, the port declaration and call site in node-verifier.ts, and two doc-comment mentions.\n\nCONSUMER THIS UNBLOCKS: task-97554aa4293e40eab56c0b642e18513a, the Foundation self-host canary. This closes its GAP E — the canary's DoD 3 and plan step 3 require the journey to execute the real verifier and reach final acceptance through production surfaces, which was unreachable while the only production host passed `() => null` and every node returned VERIFICATION_AUTHORITY_UNAVAILABLE. With the provider wired, a node whose project has the calibration slice, the moe-verifier-policy/1 slice and a clean round with stored items now earns a receipt and fires integration.accept_output. GAP A of that canary remains OPEN and INDEPENDENT of this task; nothing here addresses it.",
      "startedAt": "2026-08-17T19:56:48.457Z",
      "completedAt": "2026-08-17T20:01:22.875Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor promotion under the human's active unstall directive: both measured gates cleared today — reviewer calibration durable writer (task-5edf037f) DONE and review package item persistence (task-92031b0a) DONE. The provider can now be built from durable sources; re-measure the 3-field authority matrix at current HEAD before planning (agent-wrapper-main.ts:240 was () => null at a42ae2f).",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 4.5,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-16T21:47:27.141Z",
  "updatedAt": "2026-08-17T20:07:56.971Z",
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-17T19:36:54.858Z",
  "metrics": {
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 5,
    "firstClaimAt": "2026-08-16T22:13:51.833Z",
    "executedStepCount": 8,
    "doneAt": "2026-08-17T20:07:56.971Z",
    "wallClockMs": 78845138
  },
  "planningNotes": {
    "approachesConsidered": "THIS PLAN REPLACES AN EARLIER ONE. The first attempt blocked correctly at its own step-1 gate: two of three VerifierAuthorityFacts fields had no durable writer, and the worker refused to mint six 64-hex digests from three scalars. That block was right, and it also caught an architect error -- my original notes claimed package items were derivable from `readReviewLedger`, when `submitRound` was discarding them. Both gaps are now closed by task-5edf037f (calibration) and task-92031b0a (package items), so the provider is buildable from real durable sources and this plan composes them. REJECTED NOW: (a) reimplementing either reader inside the provider -- both are landed public surfaces and a local copy would drift and then police the copy rather than production; (b) reading `record.packageItems.items` directly -- it is reachable and typechecks, but it skips `verifyStoredPackageItems`, which is the only thing making stored items trustworthy; drill 3 exists to catch exactly that shortcut; (c) widening the port to return a diagnostic refusal code instead of `null` -- `NodeVerifierConfig.verificationAuthority` is node-verifier's contract and the task rails put it out of scope, so the tests carry the discrimination instead by seeding one gap at a time; (d) defaulting any field -- see the risks; a legal-but-unproven calibration is the single most dangerous wrong answer here and the landed reader already refuses rather than synthesising one.",
    "codebaseInsights": "Re-measured at HEAD d2151d4 (the earlier plan measured a42ae2f; several lines have MOVED, so grep rather than trusting numbers). THE GAP IS OPEN: `verificationAuthority: () => null` at apps/daemon/src/orchestrator/agent-wrapper-main.ts:280 -- it was :240 at a42ae2f. The port is `NodeVerifierConfig.verificationAuthority` (node-verifier.ts:62-65), called at :165; on null it pushes outcome VERIFICATION_AUTHORITY_UNAVAILABLE and `continue`s at :166-172, so `recordVerifierReceipt` (:176) and the integration.accept_output dispatch (:152 replay, :198 fresh) stay unreachable. THE THREE SOURCES, all now real. POLICY: installPolicy folds {slices:{[sliceRef]:slice}} behind PolicyInstalled; reviewer-calibration-record.ts already shows the read traversal (readDurableLedger -> stateOf(ledger, policyAggregateId(projectId)) -> slices), so check for a reusable reader before writing a third copy. CALIBRATION: `readReviewerCalibration(store, projectId)` at review/reviewer-calibration-record.ts:138 returns ReviewerCalibrationRead -- {ok:true, calibration} or a refusal with REVIEWER_CALIBRATION_NOT_INSTALLED / REVIEWER_CALIBRATION_UNREADABLE (frozen at :49); slice ref is REVIEWER_CALIBRATION_SLICE_REF = \"moe-reviewer-calibration/1\" (:37); its header states every field originates in the stored slice with no default, no ?? fallback and no spread. PACKAGE ITEMS: ReviewRoundRecord.packageItems (review-read-model.ts:28) is StoredPackageItems = {status:\"PRESENT\", items: readonly ReviewPackageBoundItem[]} | {status:\"ABSENT\"} (review-round-items.ts:26-39); `verifyStoredPackageItems(record)` at review/review-package-restore.ts:83 refuses REVIEW_PACKAGE_ITEMS_ABSENT when absent, delegates kernel refusals from buildReviewPackage, and compares against `record.reviewInputDigest` -- its comment says \"Against the digest the HANDLER stored, never against a second recomputation of these items\", i.e. the tautology is already guarded. `restoreReviewPackage` (:109) is the ledger-reading variant. ReviewPackageBoundItem is {digest, kind: ReviewPackageItemKind, locator} and is assignable to ReviewPackageItemInput ({digest, kind: string, locator}) with no cast. A new .ts module needs its one-line .js bridge or it is invisible to the .js import graph.",
    "risks": "The single most dangerous wrong answer is a synthesised absent calibration: `{corpusRevision:\"\", sentinelPassed:false, staleness:\"UNKNOWN\"}` is a legal ReviewerCalibration that reviewer-eligibility maps to REVIEWER_CALIBRATION_UNPROVEN, so returning it LOOKS fail-closed and passes a careless review while asserting a calibration exists when none does. The landed reader already refuses instead; the provider must propagate that refusal as null and never rebuild the value. Second, the port's only refusal channel is `null`, so a negative suite that removes everything and asserts null passes even against a provider that ignores the store -- seed exactly one gap per case and keep passing controls in the same file. Third, `record.packageItems.items` is reachable and typechecks without the digest check; bypassing `verifyStoredPackageItems` yields items that are present but unattested, which is precisely the authority-without-evidence failure epic rail 4 forbids -- drill 3 pins it. Fourth, ROUND SELECTION: the provider must bind the same round the receipt binds (node-verifier takes the latest round whose routing is ACCEPT at :137-144); silently using an earlier round would attach stale evidence to a newer decision and every assertion would still pass. Fifth, ordering: the authority check precedes runTest, so Case B must assert runTest was never called or a reordering regression stays green. Sixth, line numbers have already moved once between plans -- grep, never trust them. Seventh, close every SqliteEventStore handle on every path; a held handle kills the vitest worker. Finally, leave node-verifier.ts:88-98 alone: its PACKAGE_ITEM_FILL digests are synthetic by disclosed design and out of scope.",
    "keyFiles": [
      "apps/daemon/src/orchestrator/agent-wrapper-main.ts",
      "apps/daemon/src/orchestrator/node-verifier.ts",
      "apps/daemon/src/review/reviewer-calibration-record.ts",
      "apps/daemon/src/review/review-package-restore.ts",
      "apps/daemon/src/review/review-round-items.ts",
      "apps/daemon/src/review/review-read-model.ts",
      "apps/daemon/src/review/verifier-receipt-contracts.ts"
    ]
  },
  "budget": {
    "wallClockMs": 7200000,
    "warnedAt": "2026-08-17T19:34:25.385Z",
    "escalatedAt": "2026-08-17T19:34:25.385Z"
  },
  "planApprovedAt": "2026-08-17T19:36:56.927Z",
  "contextFetchedBy": [
    "worker-a83aecdf",
    "qa-12cda609"
  ],
  "workStartedAt": "2026-08-16T22:14:05.318Z",
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorHandoffs": [
    {
      "whatIsDone": "Step 1 (authority matrix) fully measured at HEAD a42ae2f, no code written. Task premise re-confirmed open: agent-wrapper-main.ts:240 still passes `verificationAuthority: () => null` and VerifierAuthorityFacts has zero producers. Per-field durable-source matrix resolved: (1) policy HAS a durable source — installPolicy (bootstrap-policy-services.ts:42-61) folds {slices:{[sliceRef]:slice}} behind PolicyInstalled, readable via installedSlices/stateOf, and policy.install's payload allow-list is [\"slice\"] (daemon-command-registry.ts:113). (2) calibration has NO durable writer — ReviewerCalibration {corpusRevision,sentinelPassed,staleness} (review-contract.ts:128), sole producer on disk is review-test-fixtures.ts:249. (3) packageItems have NO durable source AND are not derivable — this overturns step-1 premise (b). Also measured the constraints any future provider must satisfy: buildReviewPackage needs >=1 CRITERION plus singleton GRAPH_HASH/PLAN_HASH/RUBRIC/SUBMITTED_BYTES/INTEGRATED_TREE (review-package.ts:119-134); recordVerifierReceipt refuses a provider-supplied DAEMON_RECEIPT (verifier-receipt-ledger.ts:178) because it appends its own; calibration must pass calibrationCodes (reviewer-eligibility.ts:116-125) i.e. non-empty corpusRevision, sentinelPassed true, staleness not STALE/UNKNOWN; and the policy input must evaluatePolicy to ALLOW for action integration.accept_output by actor daemon:node-verifier or decodeVerifierReceiptBytes rejects the receipt (verifier-receipt-contracts.ts:221-226). Full detail is in the task's blockedReason and in Serena memories `task-4dd4424c-handoff` and `gotcha-review-package-items-have-no-durable-source`.",
      "whatRemains": "Blocked pending an architect decision, already routed to architect-2cc07e26 (msg-bbaeae51 in #architects): file durable-source prerequisites for calibration and packageItems, chain task-4dd4424c behind them, no DoD narrowing. Then steps 2-8 run as planned. Two things the next claimer must NOT do: do not resume this task until those prerequisites land, and do not satisfy packageItems by minting digests.",
      "pitfalls": "The decisive trap: 'derive packageItems from durable review state' reads plausible and measures false. submitRound (review-services.ts:150-155) commits only {lineage,reviewInputDigest,round,routing} and DISCARDS the payload items, and they are not recoverable from the request either — CommandDecisionRecordBase (store-contracts.ts:86-106) exposes resultBytes and requestSha256 but has NO requestBytes field. So the durable record holds a hash of the package, never its contents. Producing six 64-hex content digests from three scalars (resultSha256/decisionId/aggregateVersion) is minting, not deriving, and would yield a receipt whose reviewInputDigest is unbound from anything a reviewer saw. Do not source any field from NodeMission — it is {instructions,test,title,workspace} (agent-wrapper.ts:47-52) and carries none of the three fields; it is also host-owned input, not durable authority. Do not reuse the PACKAGE_ITEM_FILL synthetic set at node-verifier.ts:93-97: it is disclosed-synthetic and explicitly out of scope. Do not weaken the refusal at node-verifier.ts:165-173 — note it correctly precedes runTest at :174, so a fail-closed test asserting 'runTest never invoked' is sound. Nothing was edited: git status --porcelain over apps/daemon/src/review/ and apps/daemon/src/orchestrator/ was empty at HEAD a42ae2f, and there is no commit for this task.",
      "openQuestions": "(A) Where should the round's package items durably live? Natural fix is for submitRound to persist the admitted items in its committed result (or a sibling record keyed by decisionId) with review-read-model.ts parseRound surfacing them — but review-services is outside this task's stated scope, so it needs its own task. Worth noting GRAPH_HASH/PLAN_HASH have durable analogues on the graph_revision aggregate (graphContentHash/planHash) while RUBRIC/SUBMITTED_BYTES/INTEGRATED_TREE/CRITERION have no durable producer at all — and the daemon wires no graph.* kinds, the same Graph Beta gap that blocked task-a9fd91c3. (B) Is a policy.install slice keyed by an explicit sliceRef the sanctioned durable home for calibration? It looks lawful — the allow-list admits an arbitrary slice object and review-contract.ts:127 calls calibration 'caller-supplied durable facts', so caller-supplied-then-durably-written is the contract's own shape — but that is a design call for an architect, not a worker. I did not implement (B) alone because the provider must return all three fields or null, so without (A) there is no provider.",
      "releasedAt": "2026-08-16T22:21:18.642Z",
      "releasedBy": "worker-a83aecdf",
      "reason": "Self-release of a BLOCKED task's assignment, per governor suggestion (msg-e8e6901f). Task stays BLOCKED awaiting the architect decision routed to architect-2cc07e26; this only frees the worker slot so I can take the next plan that lands. Not an abandonment and not an idle-time release — the block is filed with full measurement and needs an architect, not another worker."
    }
  ],
  "reviewStartedAt": "2026-08-17T20:03:38.083Z",
  "verification": {
    "command": "pnpm typecheck && pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": "packages/runner typecheck$ tsc --project tsconfig.json\npackages/runner typecheck: Done\napps/daemon typecheck$ tsc --project tsconfig.json\napps/daemon typecheck: Done\n\n RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  121 passed (121)\n      Tests  2570 passed (2570)\n   Start at  23:02:51\n   Duration  7.34s (transform 47.40s, setup 0ms, import 116.58s, tests 72.11s, environment 10ms)",
    "reportedAt": "2026-08-17T20:03:38.083Z"
  },
  "filesModified": [
    "apps/daemon/src/review/verifier-authority-provider.test.ts",
    "apps/daemon/src/review/verifier-authority-provider.ts",
    "apps/daemon/src/review/verifier-authority-provider.js",
    "apps/daemon/src/orchestrator/node-verifier.test.ts",
    "apps/daemon/src/orchestrator/agent-wrapper-main.ts"
  ],
  "completedAt": "2026-08-17T20:07:56.971Z",
  "reviewCompletedAt": "2026-08-17T20:07:56.971Z",
  "reviewSummary": "APPROVED. 6/6 DoD verified against bytes at HEAD.\n\nDoD1: grep at HEAD shows createVerifierAuthorityProvider({projectId, store: verifierStore}) at agent-wrapper-main.ts:281; `verificationAuthority: () => null` grep EXIT 1 (gone from production). DoD2: 3/3 fields durable — calibration<-readReviewerCalibration, policy<-installed moe-verifier-policy/1 slice validated by the core's evaluatePolicy, packageItems<-latest ACCEPT round via verifyStoredPackageItems only. Closure declared (nodeRef), no `brief` param; hostile-brief + \"differs from every plausible default\" tests pin non-fabrication. DoD3: node-verifier.test.ts drives REAL createNodeVerifier with the REAL provider, asserting receipt IDENTITY (receiptId, subjectRef, source.decisionId), execution.outputSha256/byteCount, calibration+policy+packageItems byte-equal to what was INSTALLED, plus ledger.accepted.verifierReceiptId proving accept_output committed. DoD4: literal VERIFICATION_AUTHORITY_UNAVAILABLE, runs===0 (ordering), VERIFIER_RECEIPT_NOT_FOUND, accepted undefined.\n\nDoD5 - my own drills. A (hard-coded calibration): RED at node-verifier.test.ts:452:40 on the receipt calibration assertion, AssertionError not crash. B (fabricated facts instead of null): RED at :486:23 on the outcome literal. Restored by re-edit; sha256 de5af6a2...a8f4dd matched baseline, git diff --numstat empty, suites green after.\n\nDoD6: pnpm typecheck EXIT 0 (apps/daemon Done); pnpm --filter @moe/daemon test EXIT 0 - Test Files 121 passed (121), Tests 2571 passed (2571). provider.ts 178 lines. agent-wrapper-main.ts 392 but was 388 pre-task (blob 88b7560): pre-existing, under 400, not task-caused.\n\nProvider files were first added by foreign sweep 29f3c5f, so 08e3445 carries only 3 files - verified by base-ref bytes per global rail, not commit membership. Canary note: nothing in production installs moe-verifier-policy/1 or moe-reviewer-calibration/1; both are operator policy.install commands."
}