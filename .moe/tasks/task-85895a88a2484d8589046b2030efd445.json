{
  "id": "task-85895a88a2484d8589046b2030efd445",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "The Needs-you incident card: the real error line, a rollback confirm that names the sha, and a dismiss that is not a resolve",
  "description": "SPIDR grandchild B of task-f008f29a38d54ae7a10e97cad1c4443d (architect-aa54e231, 2026-09-05). Owns the parent's DoD 2, 3 and 4 - the three properties the parent called out as consequences of this being an INCIDENT SURFACE. It carries the parent's dependsOn unchanged.\n\nTHIS ROW ADDS THE FIFTH NEEDS-YOU KIND, and that is deliberate: it is the UI change the daemon-side incident row was explicitly forbidden from making. Measured at HEAD (branch moe/work-2026-09-04): apps/control-room/src/v2/approvals/needs-you-model.ts:28 declares a CLOSED roster - NEEDS_YOU_KINDS = [\"PLAN_APPROVAL\", \"ESCALATION\", \"GATE_1\", \"READY_TO_CLOSE\"] - and its docblock states the governing principle: every item is \"derived only from things the daemon already states\", each rests on a daemon OFFER or read, and \"Every item routes to the goal\".\n\nAN INCIDENT BREAKS THAT LAST SENTENCE and you must handle it deliberately rather than by accident: a deployment outage routes to an ENVIRONMENT, not a goal. Decide where the card routes and say why in the record; do not leave `goalId` holding something that is not a goal, because every existing consumer of a Needs-you item assumes that field means what it says. The first four kinds also each rest on a daemon OFFER - keep that property: the Roll back control should be offered by the daemon, not synthesised in the browser, or the card will offer an action the command path can refuse.\n\nTHREE PROPERTIES, ALL OF THEM ABOUT AN OPERATOR UNDER TIME PRESSURE:\n1. THE LAST ERROR LINE VERBATIM. \"Unhealthy\" sends the operator to the logs this card was supposed to replace.\n2. THE CONFIRM NAMES THE TARGET SHA. Rolling back to the wrong sha during an incident is a second incident.\n3. DISMISS IS NOT RESOLVE. Dismissing while the environment is still DOWN must leave it visibly DOWN elsewhere and must let the card return at the next failure threshold. A dismiss that reads as recovery is how an outage gets forgotten.\n\nCONCURRENCY IS HIGH HERE. needs-you-model.ts (197 lines) and needs-you.tsx (207 lines) are being edited RIGHT NOW by task-a988ca1c, task-33ceae56 and task-84cd2e55, all WORKING. Expect a rebase, never delete a peer's roster entry, and re-measure the roster before editing it - it may already have grown.\n\nNOT IN SCOPE: the Deployments card and the backups list (grandchild A); the Playwright journey, the live drive and the docs (grandchild C).\n\nVERIFY: `pnpm --filter @moe/control-room test`.",
  "definitionOfDone": [
    "The incident card renders 'production unhealthy since <t>' with THE LAST ERROR LINE VERBATIM. Assert the exact error text reaches the rendered card, not that some error-ish string is present - a card showing only 'unhealthy' sends the operator to the logs it was meant to replace.",
    "THE ROLLBACK CONFIRM NAMES THE TARGET SHA at the moment of confirmation - assert the sha is in the confirm surface itself, not merely in the card behind it. Rolling back to the wrong sha during an incident is a second incident.",
    "DISMISS IS NOT RESOLVE, asserted in BOTH directions: after dismissing, the environment still reads DOWN on the Environments surface, AND the card returns at the next failure threshold rather than staying silent. One direction alone permits a dismiss that quietly ends monitoring.",
    "The fifth kind is added to NEEDS_YOU_KINDS without disturbing the four that exist - assert all five are present, and re-measure the roster before editing since three rows are extending this file concurrently and it may already have grown.",
    "The Roll back control is OFFERED BY THE DAEMON rather than synthesised in the browser, matching how the existing four kinds each rest on a daemon offer or read. Assert the control is absent when the daemon offers no rollback - a card that offers an action the command path will refuse is the dead-button hazard this board has already shipped once.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/control-room test`, `pnpm typecheck`."
  ],
  "taskRails": [
    "THE REAL ERROR LINE, VERBATIM. An incident surface that summarises the error has failed at the one job it has.",
    "THE CONFIRM NAMES THE SHA. Not the card, the confirm - that is the moment the operator commits.",
    "DISMISS MUST NOT READ AS RECOVERY, asserted in both directions: still DOWN elsewhere, and the card returns.",
    "THE CONTROL IS OFFERED BY THE DAEMON, never synthesised. The existing four kinds all rest on a daemon offer; a browser-minted control can offer what the command path refuses.",
    "AN INCIDENT DOES NOT ROUTE TO A GOAL. The model's docblock says every item routes to the goal; decide deliberately where this one routes and never put a non-goal id in a goalId field.",
    "REBASE, DO NOT DELETE A PEER'S ENTRY: task-a988ca1c, task-33ceae56 and task-84cd2e55 are all editing needs-you-model.ts and needs-you.tsx concurrently.",
    "ASCII-ONLY SOURCES, APOSTROPHE-FREE JSX TEXT, EXACT-KEY decoders, dev-proxy-paths pin for any new read."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "PREREQUISITE HARD STOP AND A FRESH ROSTER MEASUREMENT. Four dependencies were unlanded at plan time.\n\nVERIFY ON DISK:\n- `git grep -rn \"/deployments/health/read\" -- apps/daemon/src` — the read (task-509c6c1609174adb9228940bc002f5c3). Confirm it is reachable from the COMPOSED production listener, not only a test server.\n- `git grep -rn \"moe-health-probe\\|incident\" -- apps/daemon/src/monitoring` — the probe ring and incident lifecycle (task-6ec875f4 / task-cc05c473).\n- `git grep -rn \"deployment.rollback\" -- apps/daemon/src` — the rollback the card offers (task-95e68a8d chain).\n- `git grep -c \"cr\\.env\\|environments-screen\\|Environments\" -- apps/control-room/src/v2` — grandchild A's Environments surface (task-df972c274f2a43eda3a9f57d2780c6f9), which DoD 3 asserts against.\nIf any is absent, `moe.report_blocked` with `blockedOnTaskIds`. Do NOT mock the read, do NOT synthesise an offer, do NOT assert DoD 3 against an internal flag instead of A's surface.\n\nRE-MEASURE THE CONTENDED ROSTER — three rows (task-a988ca1c, task-33ceae56, task-84cd2e55) are editing these two files concurrently and none had landed at plan time:\n  `grep -n \"NEEDS_YOU_KINDS\\|KIND_ORDER\" apps/control-room/src/v2/approvals/needs-you-model.ts`\n  `grep -n \"KIND_EYEBROW\" apps/control-room/src/v2/approvals/needs-you.tsx`\nRecord what each contains NOW. REBASE onto peer entries; deleting one to clean your diff is the failure rail 6 names.\n\nAlso read needs-you-model.ts:186-187 and confirm the comparator still dereferences `left.goalId` — step 2 depends on it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you-model.ts",
        "apps/control-room/src/v2/approvals/needs-you.tsx"
      ],
      "note": "PREREQUISITES ALL LANDED; ROSTER RE-MEASURED; PLAN STEP 3 IS ALREADY BUILT BY A SIBLING.\n\nMEASURED AT HEAD ac5c9ecd (branch moe/work-2026-09-04), no amendments on any step (activeAmendmentId null on all seven).\n\n1. `/deployments/health/read` EXISTS and is production-composed: apps/daemon/src/http/deployments-health-read.ts:43 DEPLOYMENTS_HEALTH_READ_PATH, and apps/daemon/src/daemon-entry-deployments-health.test.ts:2 states it is reached THROUGH THE PRODUCTION LISTENER.\n2. Probe ring + incident lifecycle EXIST: apps/daemon/src/monitoring/health-probe-ring.ts, health-incident.ts (+ .js bridge), health-probe-contracts.ts.\n3. `deployment.rollback` is now OFFERED BY THE DAEMON: apps/daemon/src/http/affordance-rollback-offers.ts (landed by my own prerequisite row task-2f2ed034, commit 84cba8ef), imported at affordance-read.ts:36 and spent at :474. AT MOST ONE offer per project, targetAggregateId = projectId (rollback-command.ts:85 refuses anything else). Client builder exists: generated-client.ts:196/:321.\n4. Environments surface EXISTS: apps/control-room/src/v2/ops/environments-section.tsx renders `data-status={view.state}` on testid `cr.environments.card.<env>` plus `.state` words. That is DoD 3 direction 1's assertion target.\n\nROSTER IS EIGHT, NOT FOUR (plan's line numbers are stale; peers landed four more kinds):\n  needs-you-model.ts:52 NEEDS_YOU_KINDS = [\"PLAN_APPROVAL\",\"PLAN_REJECTED\",\"PREVIEW\",\"RELEASE\",\"DEPLOY\",\"ESCALATION\",\"GATE_1\",\"READY_TO_CLOSE\"]\n  needs-you-model.ts:112 KIND_ORDER: eight entries, PLAN_APPROVAL 0 .. READY_TO_CLOSE 7\n  needs-you.tsx:47 KIND_EYEBROW: eight entries\n  wc -l: needs-you-model.ts 249, needs-you.tsx 233 (plan said 198/211). Both near the 250 ceiling, so incident logic goes in NEW focused modules rather than growing these two.\nComparator still dereferences goalId: needs-you-model.ts:238 `|| left.goalId.localeCompare(right.goalId)`.\n\nPLAN STEP 3 IS ALREADY DELIVERED and must NOT be duplicated (global rail 6: re-implementing a landed surface is a silent duplicate). apps/control-room/src/live/live-deployments-health.ts exists with exact-key decoding, carries `lastError.line` through UNCHANGED, decodes `incident {id, openedAt}` and `rollbackTarget {imageDigest, sha, toReceiptRef}` as first-class members; the proxy pin is already at dev-proxy-paths.ts:33. Step 3 becomes a verification step, not a build step.\n\ngoalId DECISION (rail 5), recorded here and settled by PRECEDENT rather than invention: needs-you-escalation.ts:39 already writes `goalId: goal?.goalId ?? \"\"` for an item with no goal. So an empty string already means NO GOAL in this exact file family. The incident item will carry `goalId: \"\"` (no id at all - never an environment id) and carry the environment in a DISCRIMINATED `incident` member, which is the plan's first option. `planningRunRef: \"\"` too, which needs-you.tsx:170 already uses to suppress the open-the-goal button. The comparator keeps a total order and no consumer sees a non-goal id.\n\nFOREIGN DIRT NOTED, NOT TOUCHED (6+ paths belong to other sessions): .codex/*, .grok/config.toml, .moe/messages/*, .moe/roles/*, 11 .moe/tasks/*.json, apps/daemon/src/http/affordance-read.ts (M), and untracked Moe-NG_Deep_Review_2026-09-05.md, apps/daemon/src/preview/preview-goal-landing.test.ts, tests/e2e/control-room/*.\n\nPrior session (worker-76f4ef35, then worker-4d8571e7) made NO source edits; wip commit ac5c9ecd carries only the task json.",
      "startedAt": "2026-09-07T19:36:53.537Z",
      "completedAt": "2026-09-08T02:34:26.134Z"
    },
    {
      "stepId": "step-2",
      "description": "THE FIFTH KIND, AND THE goalId DECISION (DoD 4, rail 5). Add the incident kind to `NEEDS_YOU_KINDS` (needs-you-model.ts:28) and to the TWO exhaustive `Record<NeedsYouKind, ...>` maps it feeds — `KIND_ORDER` (:72) and `KIND_EYEBROW` (needs-you.tsx:41). Both stop compiling until they gain the key; that coupling is why this is a two-file change rather than one, and `needs-you.tsx` imports only the TYPE, so a symbol grep never names it.\n\nKIND_ORDER IS A PRIORITY, NOT A FORMALITY. An incident is an outage in progress; choose its position relative to PLAN_APPROVAL, ESCALATION, GATE_1 and READY_TO_CLOSE deliberately and say why in the note. Getting it wrong buries an outage under routine approvals.\n\nTHE goalId DECISION, which rail 5 requires be made deliberately and which is MECHANICAL, not just semantic: needs-you-model.ts:186-187 sorts with `... || left.goalId.localeCompare(right.goalId)`. `goalId` is required on every item and is DEREFERENCED there. An incident routes to an ENVIRONMENT. Do NOT put an environment id in `goalId` — every existing consumer assumes that field means what it says, and the comparator would be silently comparing two different kinds of identifier.\nChoose one and record it: a discriminated member carrying the environment, or an optional routing target with the comparator taught to handle its absence. Whichever you pick, assert the OTHER FOUR kinds still sort exactly as before — that is the regression this decision risks.\n\nASCII-only, apostrophe-free JSX text (the scheduler boundary scan reds on an apostrophe in JSX), and `wc -l` both files against the 250 ceiling — they were 198 and 211.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you-model.ts",
        "apps/control-room/src/v2/approvals/needs-you.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/approvals/needs-you-incident.ts",
        "apps/control-room/src/v2/approvals/needs-you-incident.test.ts",
        "apps/control-room/src/v2/approvals/needs-you-model.ts",
        "apps/control-room/src/v2/approvals/needs-you.tsx",
        "apps/control-room/src/v2/goals/plan-rejected-surfaces.test.tsx"
      ],
      "note": "THE NINTH KIND (not the fifth - peers grew the roster to eight before I arrived), AND THE goalId DECISION.\n\nROSTER, all nine, peer entries preserved verbatim:\n  NEEDS_YOU_KINDS = [\"INCIDENT\",\"PLAN_APPROVAL\",\"PLAN_REJECTED\",\"PREVIEW\",\"RELEASE\",\"DEPLOY\",\"ESCALATION\",\"GATE_1\",\"READY_TO_CLOSE\"]\n  KIND_ORDER: INCIDENT 0, then the previous eight shifted by one in their EXISTING relative order.\n  KIND_EYEBROW (needs-you.tsx): + INCIDENT: \"Incident\".\nNothing was deleted. I also corrected two docblock sentences the addition made false: \"Seven kinds of item\" (already stale at eight before me) is now \"Nine\", and \"Every item routes to the goal\" now names the one deliberate exception.\n\nWHY INCIDENT SORTS FIRST: alone among the kinds it is an outage ALREADY IN PROGRESS rather than a decision waiting its turn - users are losing the product while it sits in the list. Every other kind is work that can wait one position; burying an outage under routine plan approvals is how it gets read late.\n\nTHE goalId DECISION (rail 5), settled by PRECEDENT not invention. The incident item carries `goalId: \"\"` and `planningRunRef: \"\"` - NO id at all, and specifically never an environment id in a goal's field. needs-you-escalation.ts:39 already writes `goalId: goal?.goalId ?? \"\"` for an item whose goal is unknown, so the empty string already means \"no goal\" in this exact family. The environment travels in a DISCRIMINATED `incident` member (the plan's first option), where a consumer must ask for it by name. Consequences, all checked: the comparator at needs-you-model.ts (`left.goalId.localeCompare(right.goalId)`) keeps a total order because \"\" is still a string; needs-you.tsx already suppresses the open-the-goal button when `planningRunRef` is empty, so no card offers to open a goal that does not exist.\n\nSORT REGRESSION ASSERTED THROUGH THE PRODUCTION COMPARATOR, not a reimplementation: the same board derived twice, once with an open incident and once without, gives [\"INCIDENT\",\"PLAN_APPROVAL\",\"GATE_1\"] and [\"PLAN_APPROVAL\",\"GATE_1\"]. Roster arm asserts MEMBERSHIP of all nine, never a count (three peer rows are still adding kinds; a frozen count would red on their landing rather than on a defect).\n\nUNNAMED EXACT-ROSTER CONSUMER FOUND AND UPDATED - DISCLOSED IN FULL. apps/control-room/src/v2/goals/plan-rejected-surfaces.test.tsx:58 asserts `expect([...NEEDS_YOU_KINDS]).toEqual([...])` with the eight-kind array frozen inline. It is NOT in this plan's affectedFiles and no symbol grep of \"INCIDENT\" would have named it. It reddened on the first run:\n  \"Test Files 1 failed | 200 passed (201)\" / plan-rejected-surfaces.test.tsx:58:34\nI added \"INCIDENT\" in first position and changed NOTHING else in that file. This is the consumer being made CORRECT, not the producer being reformatted to evade a check - the arm still asserts an exact ordered roster, and its companion `new Set(NEEDS_YOU_KINDS).size` uniqueness assertion is untouched. Blocking the row over a one-line test-roster update would have stranded working bytes against DELIVERY BIAS; the extra path is declared here and in modifiedFiles.\n\nNEW MODULE, not growth in the contended files: apps/control-room/src/v2/approvals/needs-you-incident.ts (119 lines) holds `IncidentFacts`, `incidentKeyOf`, `incidentItems` and the rollback resolution, mirroring needs-you-escalation.ts (type-only `NeedsYouItem` import, so no runtime cycle).\n\nLINE COUNTS, disclosed against the 250 target: needs-you-model.ts 249 -> 268, needs-you.tsx 233 -> 234. Both are OVER the 250 target and far under the 400 hard cap. The plan expected 198/211; three peer rows landed four extra kinds into these files before I claimed, so a ninth kind cannot be added inside 250. I deliberately did NOT reclaim lines by moving `EscalationFacts`/`CloseFacts` out: needs-you-model.ts is being edited by three concurrent rows and a structural move maximises the rebase surface rail 6 warns about, for 14 lines.\n\nASCII-only; no apostrophes added to JSX text.\nGate at this step: `npx vitest run --root apps/control-room src/v2/approvals/needs-you-incident.test.ts src/v2/goals/plan-rejected-surfaces.test.tsx` -> \"Test Files 2 passed (2) / Tests 22 passed (22)\". Package typecheck (`tsc --noEmit`) exit 0.",
      "startedAt": "2026-09-08T02:35:12.829Z",
      "completedAt": "2026-09-08T02:39:06.812Z"
    },
    {
      "stepId": "step-3",
      "description": "THE READ CLIENT AND ITS PROXY PIN (rail 7). Create the live read module under apps/control-room/src/live/ for `/deployments/health/read`, following the neighbouring live read clients.\n\nEXACT-KEY DECODING: an unknown key and a missing key each REFUSE with a specific code, asserted with the code AND the layer that refused — never merely that decoding failed.\n\nCARRY THE LAST ERROR LINE THROUGH UNCHANGED. The decoder must not truncate, trim, summarise or re-case it. This is the one field the whole card exists to show, and a decoder that tidies it defeats the row as surely as a card that omits it.\n\nADD THE PROXY PIN to `DEV_PROXY_PATHS` in apps/control-room/src/live/dev-proxy-paths.ts (frozen list at :5), keeping the file's existing ordering. Note `/design/read` is NOT pinned there, so a pin is not automatic for every route — without it the dev lane cannot reach the read at all.\n\nDecode the OPEN INCIDENT and the PREVIOUS DEPLOY SHA as first-class members: step 4 renders the error line and step 5 needs the sha at the confirm.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/live/live-deployments-health-read.ts",
        "apps/control-room/src/live/dev-proxy-paths.ts"
      ],
      "newFiles": [
        "apps/control-room/src/live/live-deployments-health-read.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/live/live-deployments-health.test.ts"
      ],
      "note": "THE READ CLIENT AND ITS PROXY PIN ALREADY EXIST. This step became a VERIFICATION step, not a build step, because building it again would have been the silent-duplicate failure the global rails name (re-implementing a landed surface collides with its owner in the shared worktree).\n\nMEASURED ON DISK:\n- apps/control-room/src/live/live-deployments-health.ts (334 lines) is the read client the plan asked me to create. It POSTs exactly {environment} to `/deployments/health/read`, decodes exact-key at EVERY level via `exactDataRecord`, and refuses at its own layer.\n- THE PROXY PIN IS ALREADY THERE: dev-proxy-paths.ts pins \"/deployments/health/read\" between \"/deployments/read\" and \"/environments/read\", with a comment that also records the plan's own point - \"/design/read above is deliberately NOT pinned, so a pin is not automatic for every route\". live-deployments-health.test.ts:292 asserts the pin (\"pins the route in DEV_PROXY_PATHS, or the dev lane answers it with Vite\").\n- EXACT-KEY REFUSALS ALREADY ASSERT CODE AND LAYER, not merely that decoding failed: extra key, missing key, unknown key nested inside lastProbe, an invented `state`, a series missing its window, a frame answering about a DIFFERENT environment - each asserts {code: \"DEPLOYMENTS_HEALTH_RESPONSE_INVALID\", layer: \"CONTROL_ROOM_DEPLOYMENTS_HEALTH\"}. The three refusal ENVELOPES (route {code,layer}, listener {code,layer,outcome}, probe store {code,layer,ok:false}) each carry the refusing authority's own code and layer through.\n- THE OPEN INCIDENT AND THE ROLLBACK TARGET ARE ALREADY FIRST-CLASS: `incident {id, openedAt}` and `rollbackTarget {imageDigest, sha, toReceiptRef}`, the latter gated on hex64 `toReceiptRef` and docker's `sha256:<64 hex>` digest. NOTE FOR STEP 5, and it corrects the plan's wording: the sha the confirm must name is `rollbackTarget.sha` (receipt-bound, spendable), NOT `rollbackSha`, which the client's own docblock marks DISPLAY ONLY and \"positional over the raw ledger - can name the deploy running right now\".\n\nTHE ONE GAP I FOUND AND CLOSED. The decoder's by-value arm round-tripped only a CLEAN error line (\"Error: failed to solve: process did not complete successfully\"). Nothing asserted the line survives untidied, which is the exact defect DoD 1 exists to prevent. Added ONE arm to live-deployments-health.test.ts: \"carries a hostile last-error line through with NO trim, truncation or re-casing\", round-tripping a line with LEADING WHITESPACE, mixed case, a host:port, a parenthesised counter and a long tail, asserting `.line` equals the literal.\nPROOF THE ARM EXECUTES (not just that the file is exit 0): `npx vitest run --root apps/control-room src/live/live-deployments-health.test.ts -t \"hostile last-error\"` -> \"Test Files 1 passed (1) / Tests 1 passed | 52 skipped (53)\". Whole file: \"Tests 53 passed (53)\" (52 before).\n\nThe same property is asserted a second time at the MODEL seam by needs-you-incident.test.ts, which builds every fixture by putting a wire body through `mapDeploymentsHealthAnswer` rather than hand-shaping a view - so no arm in this row asserts against a shape the real wire never produces.\n\nNo new module and no second proxy pin were created.",
      "startedAt": "2026-09-08T02:39:10.852Z",
      "completedAt": "2026-09-08T02:40:06.000Z"
    },
    {
      "stepId": "step-4",
      "description": "THE CARD AND ITS ERROR LINE (DoD 1). Render the incident item in needs-you.tsx: 'production unhealthy since <t>' with THE LAST ERROR LINE VERBATIM.\n\nASSERT THE EXACT ERROR TEXT reaches the rendered card — seed a distinctive error string through the decoder and assert that exact string is in the output. DoD 1 is explicit that asserting 'some error-ish string is present' does not count, and the reason is concrete: a card showing only 'unhealthy' sends the operator to the logs this card was built to replace. Assert against the whole rendered container, not one node.\n\nDo not truncate the error line for layout. If it is long, wrap or scroll it — an ellipsis in the middle of a stack line is the same failure in a nicer font. If a bound is genuinely needed, say what it is and why in the note.\n\nRender the 'since <t>' from the incident's opened-at, not from the last probe timestamp: the operator wants how long this has been happening, not when it was last checked. Those diverge exactly when it matters.\n\nASCII-only and apostrophe-free JSX text.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you.tsx",
        "apps/control-room/src/v2/approvals/needs-you.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/approvals/incident-card.tsx",
        "apps/control-room/src/v2/approvals/incident-card.test.tsx",
        "apps/control-room/src/v2/approvals/incident-frames.fixture.ts",
        "apps/control-room/src/v2/approvals/needs-you-incident.test.ts",
        "apps/control-room/src/v2/approvals/needs-you.tsx"
      ],
      "note": "THE CARD AND ITS ERROR LINE.\n\nNEW FILE apps/control-room/src/v2/approvals/incident-card.tsx (126 lines) holds the incident body; needs-you.tsx gains only the import, the two handler props, a `decisionKeyOf` branch and one render block, because it is contended and was already at 234 lines. I wrote the WHOLE component in this step (rollback arm/confirm and dismiss included) so the contended file is edited ONCE rather than three times; steps 5 and 6 assert those properties and do their drills against it.\n\nTHE ERROR LINE IS RENDERED VERBATIM AND IS NEVER TRUNCATED. `{facts.lastError.line}` under `cr2-approve-mono` - the SAME class the Environments section renders this line under, so the two surfaces cannot drift on how it looks. No slice, no ellipsis, no summary anywhere in the component; grep it. It WRAPS rather than truncating, and there is NO length bound: an ellipsis in the middle of a stack line is the same failure in a nicer font. The code, layer and instant ride in a SEPARATE `.errormeta` node so nothing is interpolated into the operator's one real clue - and an arm asserts the error node does NOT contain the code, which is the way that would silently regress.\n\nSINCE COMES FROM THE INCIDENT'S OWN openedAt, never the probe. The headline (from the model) is `production unhealthy since 2026-09-08T01:14:52.000Z` and the card repeats `open since <openedAt>`. An arm asserts the whole rendered container does NOT contain the lastProbe instant 2026-09-08T02:44:00.000Z. That divergence is real and matters: a 30s probe makes `lastProbe.at` read \"just now\" for an outage that started an hour ago.\n\nASSERTED THROUGH THE WHOLE QUEUE, not the leaf: every arm renders `<NeedsYou>` and finds the card by `cr.needsyou.item.incident.production#7`. A leaf-only test would stay green the day the queue stopped rendering the leaf. Fixtures come from the new incident-frames.fixture.ts, which builds every health view by putting a WIRE BODY through the production decoder `mapDeploymentsHealthAnswer` and asserts it really decoded (an ERROR would have made every arm vacuous).\n\nDoD 1 ARMS (all against the rendered container, not a hand-picked node):\n- \"renders the environment, the since-instant and the error line VERBATIM\": `card.textContent` CONTAINS the exact literal \"  Error: connect ECONNREFUSED 10.4.19.7:8443 (attempt 3/3)\" - leading whitespace, address and case intact - and the error node EQUALS it.\n- \"does not truncate, ellipsise or re-case a long error line\": a 480+ char line with trailing spaces, asserted EQUAL and asserted not to contain \"...\".\n- \"names the refusing authority BESIDE the line, never interpolated into it\".\n- \"takes SINCE from the incident opened-at, not from the last probe\".\n- \"says so plainly when the daemon has recorded no error line\": a distinct `.noerror` node rather than a blank.\n\nAlso landed here and asserted in step 5/6 terms: `decisionKeyOf` returns the incident key `production#7` for an incident item, so a rollback refusal is rendered by the existing generic OutcomeNote under `cr.needsyou.result.production#7` at the refusing authority's own code and layer (arm: DEPLOY_ROLLBACK_IN_PROGRESS @ DAEMON_DEPLOY_ENGINE).\n\nI also corrected the needs-you.tsx docblock's now-false reading order (\"plans, then exhausted reviews, contracts, goals ready to close\") to name the incident first.\n\nASCII-only; no apostrophes in any JSX text (checked: every apostrophe in the file is inside a docblock comment, none in rendered strings).\n\nGATE: `npx vitest run --root apps/control-room src/v2/approvals/incident-card.test.tsx src/v2/approvals/needs-you-incident.test.ts` -> \"Test Files 2 passed (2) / Tests 24 passed (24)\". Package typecheck `tsc --noEmit` exit 0.\nLINE COUNTS: incident-card.tsx 126, needs-you.tsx 234 -> 257 (over the 250 target, under the 400 cap; disclosed for the same peer-growth reason as step 2).\nNOTE ON RTL: this package does NOT auto-clean between renders - the first run failed 10/11 arms with getMultipleElementsFoundError until I added `beforeAll(IS_REACT_ACT_ENVIRONMENT)` + `afterEach(cleanup)`, which is the convention needs-you.test.tsx already uses.",
      "startedAt": "2026-09-08T02:40:35.507Z",
      "completedAt": "2026-09-08T02:43:49.680Z"
    },
    {
      "stepId": "step-5",
      "description": "THE ROLL BACK CONTROL: OFFERED, AND THE CONFIRM NAMES THE SHA (DoD 2 and 5).\n\nOFFERED BY THE DAEMON, NEVER SYNTHESISED (DoD 5, rail 4). Use the existing `offerFor(surface, commandKind, target)` at needs-you-model.ts:77, exactly as the other four kinds do. It returns `undefined` when `surface.outcome !== \"SURFACE\"`.\nASSERT THE CONTROL IS ABSENT when the daemon offers no rollback — absent, not disabled. Query for the testid and find nothing. A card that offers an action the command path will refuse is the dead-button hazard this board has already shipped once.\n\nTHE CONFIRM NAMES THE TARGET SHA (DoD 2, rail 2), and the assertion is specifically about WHERE: assert the sha is in THE CONFIRM SURFACE ITSELF, not merely in the card behind it. The confirm is the moment the operator commits, and rolling back to the wrong sha during an incident is a second incident. Arm-then-confirm, following the PUBLISH card's shape.\n\nAssert the sha shown is the PREVIOUS deploy receipt's sha from the read — compare against that value, not against a pattern. A pattern passes for the wrong sha, and this is the one number the operator is trusting.\n\nAlso assert the confirm names the ENVIRONMENT. 'Roll back to <sha>' without saying which environment is the same class of error one level up.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you.tsx",
        "apps/control-room/src/v2/approvals/needs-you.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/approvals/rollback-port.ts",
        "apps/control-room/src/v2/approvals/rollback-port.test.ts",
        "apps/control-room/src/v2/approvals/use-incident-health.ts",
        "apps/control-room/src/v2/approvals/live-needs-you.tsx",
        "apps/control-room/src/v2/approvals/incident-card.test.tsx",
        "apps/control-room/src/v2/approvals/incident-frames.fixture.ts"
      ],
      "note": "THE ROLL BACK CONTROL: OFFERED, AND THE CONFIRM NAMES THE SHA.\n\nOFFERED BY THE DAEMON, NEVER SYNTHESISED. `rollbackFor` in needs-you-incident.ts reads the offer off the SurfaceFrame and returns null when `surface.outcome !== \"SURFACE\"` - the same fail-closed rule `offerFor` states. It matches by commandKind ALONE, and that is measured rather than sloppy: affordance-rollback-offers.ts mints AT MOST ONE offer per project at the PROJECT aggregate (rollback-command.ts:85 refuses any other target), so there is no per-environment offer to match. The per-environment authority is `rollbackTarget` on that environment's own health frame, and BOTH must hold.\n\nABSENT, NOT DISABLED - three arms, each querying the testid and finding NOTHING:\n- \"is ABSENT - not disabled - when the daemon offers no rollback\" (surface with no offer) - and it also asserts the CARD is still there, so the arm cannot pass by the whole card vanishing.\n- \"is ABSENT when this environment has no receipt to roll back to\" (rollbackTarget: null).\n- \"is ABSENT when the browser was handed no rollback dispatcher\".\n\nTHE CONFIRM NAMES THE SHA AND THE ENVIRONMENT, AT THE CONFIRM. Arm-then-confirm; before arming, `cr.needsyou.incident.production#7.confirm` DOES NOT EXIST (asserted). Once armed, the confirm surface - the nodes that exist only when armed - carries both: the `.confirm` line reads \"Roll back production to 0b9c1f2a3d4e5f60718293a4b5c6d7e8f9012345\" and the armed BUTTON's own label and aria-label carry the full sha and the environment too. Asserted against the fixture's `rollbackTarget.sha` VALUE, never a hex pattern.\nTHE SHA IS SHOWN IN FULL, not a 7-char prefix. On a deploy card a prefix is a nicety; here it is the one number the operator is trusting under time pressure, and two receipts can share a prefix.\nCORRECTION TO THE PLAN'S WORDING, measured: the sha shown is `rollbackTarget.sha` (resolved from the RECEIPT the daemon would spend), NOT the frame's `rollbackSha`, which live-deployments-health.ts marks DISPLAY ONLY and \"positional over the raw ledger - can name the deploy running right now\". Naming the positional one at a confirm is exactly the second incident rail 2 warns about.\nSecond press is what spends it: an arm proves press 1 calls nothing, cancel stands it down and clears the confirm surface, and only the second press dispatches - with the item whose `rollback.target.sha` is the fixture's.\n\nNEW: rollback-port.ts (45 lines) + rollback-port.test.ts (6 arms, all passing). It spends the offer VERBATIM (asserted by object identity, not equality) with EXACTLY the three keys `exactRequest` admits - {environment, restoreDatabase, toReceiptRef} - and correlation prefix `ui-rollback`.\nIT SENDS THE RECEIPT REF, NOT THE SHA, with its own arm: the handler gate is /^[0-9a-f]{64}$/u, so a port that sent the 40-char sha the operator was shown would refuse DEPLOY_ROLLBACK_REQUEST_INVALID at ingress every time, and only during an incident.\nrestoreDatabase IS PINNED FALSE, with an arm and a reason: restoring discards every write made during the outage, and an incident card is the worst possible place for that to be implicit. Rolling the IMAGE back is recoverable; rolling the DATA back is not.\nRefusal arms name CODE AND LAYER, never merely that it failed: DEPLOY_ROLLBACK_IN_PROGRESS @ DAEMON_DEPLOY_ENGINE (daemon), TRANSPORT_REQUEST_FAILED @ CONTROL_ROOM_TRANSPORT (transport), OFFER_KIND_UNBUILDABLE @ CONTROL_ROOM_DEPLOY_ROLLBACK (this port) - three DIFFERENT layers, so an arm cannot pass because a nearer layer answered first.\n\nTHE CONSUMER EDGE IS LANDED, not merely exported. live-needs-you.tsx now: polls `/deployments/health/read` per DEPLOYED environment via the new use-incident-health.ts hook, feeds `health` and `dismissedIncidents` into deriveNeedsYou, and passes `onRollback` (which spends through createRollbackPort) and `onDismissIncident` into NeedsYou. THE ENVIRONMENT SET IS READ OFF THE SAME `/deployments/read` ANSWERS the DEPLOY items already poll - not a name roster, and not a second enumeration that could drift from live-environments.tsx. Reason recorded in the hook: the health route answers for ANY name and an undeployed environment returns an empty ring the daemon states as DEGRADED, so a roster would paint a new project with environments that do not exist.\n\nGATE: `npx vitest run --root apps/control-room src/v2/approvals src/v2/goals/plan-rejected-surfaces.test.tsx` -> \"Test Files 16 passed (16) / Tests 130 passed (130)\". Package typecheck exit 0.\nLINE COUNTS: rollback-port.ts 45, use-incident-health.ts 67, live-needs-you.tsx 186 -> 230.",
      "startedAt": "2026-09-08T02:43:57.382Z",
      "completedAt": "2026-09-08T02:46:12.092Z"
    },
    {
      "stepId": "step-6",
      "description": "DISMISS IS NOT RESOLVE (DoD 3, rail 3) — the row's subtlest property, asserted in BOTH directions because either alone permits the bug.\n\nDIRECTION 1: after dismissing, the environment STILL READS DOWN on grandchild A's Environments surface. Assert against that surface, not against this card's own state. A dismiss that clears the card and leaves the operator believing the environment recovered is how an outage gets forgotten.\n\nDIRECTION 2: the card RETURNS at the next failure threshold rather than staying silent. That threshold is the probe row's lifecycle (task-cc05c473: one open incident per outage, closed on recovery, a new one at the next three consecutive failures) — assert against that lifecycle rather than inventing a re-raise rule here.\n\nONE DIRECTION ALONE PERMITS A DISMISS THAT QUIETLY ENDS MONITORING, which is why DoD 3 names both. Write them as two arms so a future edit cannot delete one and leave a green suite.\n\nMake the dismiss's scope explicit in the note: does it dismiss this incident, or mute the environment? They differ on the second outage, and the answer must match direction 2.\n\nMUTATE AND CONFIRM RED: make dismiss also clear the environment state, run direction 1, confirm RED; restore. Make dismiss suppress the next raise, run direction 2, confirm RED; restore. These files are TRACKED, so `git diff -- <path>` is a valid restore proof; `git status --porcelain` them first.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you-model.test.ts",
        "apps/control-room/src/v2/approvals/needs-you.test.tsx"
      ],
      "startedAt": "2026-09-08T02:46:20.900Z"
    },
    {
      "stepId": "step-7",
      "description": "ROSTER ARMS, GATES AND SELF-REVIEW (DoD 4 and 6).\n\nROSTER (DoD 4): assert ALL FIVE kinds are present — the four that existed plus the incident — so adding the fifth provably disturbed none. Do NOT assert a hard COUNT of roster members: three peer rows are adding entries concurrently and a frozen count would red on their landing rather than on a defect. Assert membership of each named kind instead.\nAssert the other four still SORT as before (step 2's goalId decision is what could break this).\n\nGATES — each standalone, each quoted with its `Test Files N passed` count line; an exit-0 leg with no count line RAN NOTHING:\n- `pnpm --filter @moe/control-room test` — the row's named verification command and its ONLY real evidence.\n- `pnpm typecheck`.\nState explicitly that root `pnpm test` covers NO apps/** file, so it is not evidence for anything this row owns.\n`wc -l` both edited files and quote them against the 250 ceiling (198 and 211 at plan time — a peer may have grown them).\n\nSELF-REVIEW:\n- Is there a non-goal id in ANY `goalId` field? Grep the new item construction. This is rail 5 and the comparator at :186-187 is what makes it bite.\n- Does the error line survive verbatim from decoder to DOM — no trim, no truncate, no case change? Round-trip a string with leading whitespace and punctuation.\n- Does the confirm still name the sha when the card is re-rendered mid-incident by a surface poll? A card re-keyed by a poll losing its armed state is the board-chain hazard in a new place.\n- Did a peer's roster entry survive your rebase? Re-read both files and confirm you deleted nothing.\n- Does any rendered string carry a URL with embedded credentials from a probe error (epic rail 3)? A health-check failure can echo a connection string.\n\nPATH-ATTRIBUTED BASELINE: merge-base failures, HEAD failures, subtract, intersect with owned paths; complete only when EMPTY. Disclose foreign red verbatim with paths.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you-model.test.ts",
        "apps/control-room/src/v2/approvals/needs-you-model.ts"
      ]
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": "worker-4d8571e7",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-f008f29a38d54ae7a10e97cad1c4443d",
  "dependsOn": [
    "task-e651725c92434127a7b8d72f0ac5246b",
    "task-cc05c47362dc41b0b2b416a70ad45b42",
    "task-95e68a8dd2464e7b8ad4b1f8c4d2b2e4",
    "task-509c6c1609174adb9228940bc002f5c3",
    "task-df972c274f2a43eda3a9f57d2780c6f9"
  ],
  "priority": "HIGH",
  "order": 123.2,
  "comments": [
    {
      "id": "comment-9b9fae6b03374aafbb8fc3d34cbc6798",
      "author": "worker-76f4ef35",
      "content": "STEP-1 AUDIT, NO SOURCE EDITS. All five declared prerequisites are landed:true in get_context with durable completion/verification records. The missing seam is different: daemon rollback OFFER, filed task-2f2ed034fbec4fe48b49a951951c9505. createAffordancePort (http/affordance-read.ts) iterates BOOTSTRAP_COMMAND_KINDS, which excludes rollback, then explicit providers; full HTTP subtree has zero deployment.rollback matches. Handler exists but is not an offer. rollback-command.ts:39-49 requires environment/toReceiptRef/restoreDatabase; :92 checks targetAggregateId===projectId. The health read carries previous.sha only (:276), not the selected receipt binding. UI must not invent an offer or repurpose sha as receipt reference.\n\nPLAN AMENDMENT NEEDED before resume (no activeAmendmentId/amendments were present):\n1. Roster now EIGHT: PLAN_APPROVAL, PLAN_REJECTED, PREVIEW, RELEASE, DEPLOY, ESCALATION, GATE_1, READY_TO_CLOSE. KIND_ORDER is 0..7 in that sequence; KIND_EYEBROW preserves all eight. Comparator still dereferences goalId at model:239-240. Choose a discriminated environment member and incident-first priority, never environment in goalId. Model 249 lines, card 233; focused incident model/card modules needed rather than monolith growth.\n2. Step 3 duplicates a delivered surface: live/live-deployments-health.ts already provides the exact-key client, open incident, lastError.line and rollbackSha; dev-proxy-paths.ts:33 already pinned. Reuse existing reader, do not create live-deployments-health-read.ts.\n3. Unnamed asserting consumer discovered: v2/goals/plan-rejected-surfaces.test.tsx:58-61 has an EXACT roster array. Global shape-consumer rail requires amendment adding it before widening roster.\n4. Plan omits production composition: v2/approvals/live-needs-you.tsx:127 calls deriveNeedsYou with no environment health; :134-177 handles close/escalate/preview only. It and tests plus rollback port/routing seam must be owned to make the card production-reachable, not merely fixture-renderable.\n5. Grandchild A Environments is v2/ops/environments-section.tsx through live-environments.tsx/LiveHealth. LastError source is DEPLOY_RECEIPT, not a probe exception (prerequisite completionSummary explicitly explains this).\n\nNo tests run: prerequisite audit halted before behavior edits; no green/completion claim. Foreign dirty paths preserved, no declarations or staging.",
      "timestamp": "2026-09-07T19:39:28.709Z"
    },
    {
      "id": "comment-107a5720e8044791ac80b2ad27358d61",
      "author": "worker-4d8571e7",
      "content": "SECOND SESSION ON THIS ROW - YIELDING TO THE LIVE ONE. Additive findings only; I made NO source edits, ran no gate, and touched no board status.\n\nTWIN DETECTION (the clock, not the notes - both sessions share seat worker-4d8571e7):\n- My `complete_step` on step-1 was refused `[INVALID_STATE] Step is in COMPLETED state`. Disk says step-1 completedAt 2026-09-08T02:34:26.134Z and step-2 startedAt 02:35:12.829Z, while my own call was made at ~02:35:2xZ. A call I had not yet made cannot have closed the step.\n- Independent proof: `git status --porcelain -- apps/control-room` shows `?? apps/control-room/src/v2/approvals/needs-you-incident.ts`, 6428 bytes, mtime 02:35:43Z - written the same second I read it. That is a session typing, not debris.\n- needs-you-model.ts has already moved under the twin: NEEDS_YOU_KINDS is now at :58 (was :52 when I measured at 02:2xZ) and the comparator at :255 (was :238).\nPer the yield protocol I am not re-editing files (last writer wins and would destroy green work) and not calling report_blocked (it would flip the row out of WORKING mid-step).\n\nI independently reached the SAME four decisions the twin recorded in its step-1 note - goalId \"\" by the needs-you-escalation.ts:39 precedent, the offer matched by commandKind alone because affordance-rollback-offers.ts mints AT MOST ONE project-scoped offer, step 3 already delivered by task-509c6c16, dismiss keyed to the incident id - so that note is corroborated by a second measurement, not single-sourced.\n\nTHE ONE THING THAT NOTE DOES NOT CARRY, and it is time-critical because the twin is inside step-2 (the roster edit) right now:\n\nAN UNNAMED EXACT-ROSTER CONSUMER OUTSIDE THE PLAN'S AFFECTED FILES (global rail 19 / the bidirectional-roster rail). apps/control-room/src/v2/goals/plan-rejected-surfaces.test.tsx:58-61 pins the roster as an ORDERED EXACT ARRAY, not a membership check:\n\n    expect([...NEEDS_YOU_KINDS]).toEqual([\n      \"PLAN_APPROVAL\", \"PLAN_REJECTED\", \"PREVIEW\", \"RELEASE\", \"DEPLOY\", \"ESCALATION\", \"GATE_1\",\n      \"READY_TO_CLOSE\",\n    ]);\n    expect(new Set(NEEDS_YOU_KINDS).size).toBe(NEEDS_YOU_KINDS.length);\n\nADDING ANY NINTH KIND REDS THIS TEST. It is in v2/goals/, imports the constant from ../approvals/needs-you-model.js, and appears in NO step of this plan's affected files - exactly the frozen-roster hazard step 7 warns against, already sitting in a peer test. `pnpm --filter @moe/control-room test` will catch it, but only after the roster edit is written.\n\nThe honest fix is to add the new kind to that literal in its declared order; its own docblock asks for \"every advertised kind is a distinct string in the declared order\", so updating it preserves the assertion rather than evading it. It must NOT be fixed by renaming or reformatting the producer. Whoever lands this should `moe.declare_files` that path, since no step lists it.\n\nFULL SWEEP FOR OTHER CONSUMERS A NINTH KIND COULD RED (`git grep -n \"NEEDS_YOU_KINDS|KIND_ORDER|KIND_EYEBROW\"` plus a sweep for hard counts and literal kind arrays over apps/control-room/src):\n- plan-rejected-surfaces.test.tsx:58 - THE ONLY hard red. Confirmed.\n- KIND_ORDER (needs-you-model.ts:128) and KIND_EYEBROW (needs-you.tsx:47) are exhaustive `Record<NeedsYouKind, ...>` maps - they stop COMPILING until they gain the key, so `pnpm typecheck` catches them; no test pins their contents.\n- countLabel pins are SAFE, all of them, but only conditionally: needs-you-model.test.ts:108/:134/:202/:348 and plan-rejected-surfaces.test.tsx:74 call deriveNeedsYou WITHOUT a health input, so an absent/undefined health member must yield zero incident items. If the new input member is ever made required, all five red at once.\n- live-needs-you.test.tsx:70 expects \"2 decisions need you\". This one is a REAL RISK for step 5's composition: if LiveNeedsYou starts a health read with no injected reader, that test can gain an item or leak a poll. Give the health reader the same `?? default` injection shape the other five readers use, and that test stays green.\n\nNo files created or modified by this session. Foreign dirt observed and left alone: 27 modified (.moe/**, .codex/**, .grok/config.toml, apps/daemon/src/http/affordance-read.ts) and 7 untracked, plus the twin's in-flight needs-you-incident.ts.",
      "timestamp": "2026-09-08T02:36:53.169Z"
    },
    {
      "id": "comment-2169bd68f6ce4537850f71bd2bce1dea",
      "author": "worker-4d8571e7",
      "content": "WORKER-4d8571e7 SESSION 3 (PID 31036, launched 02:38:31Z): YIELDING AGAIN TO A LIVE TWIN, PLUS ONE DEFECT THE TWIN IS ABOUT TO WALK INTO AT STEP 4/5.\n\nTWIN CONFIRMED LIVE, NOT AN ORPHAN. Two claude.exe worker seats hold the SAME workerId AND the same taskId: PID 65624 (prompt file moe-system-prompt-worker-60888.md, created 02:31:52Z) and me, PID 31036 (…-71748.md, created 02:38:31Z). Both greps answer `workerId=worker-4d8571e7` and `Claimed task id: task-85895a88…`, so this is the wrapper double-assign, not a stale process. Liveness is measured, not inferred: the task JSON moved AFTER my launch - step-2 completedAt 02:39:06.812Z, step-3 startedAt 02:39:10.852Z - and plan-rejected-surfaces.test.tsx mtime is 02:38:32Z, one second after my process was created. I made NO source edit, NO step-state call and NO status change. Last writer wins on a shared file and the twin is five steps of work ahead of me.\n\nTHE DEFECT: `decisionKeyOf` WAS NEVER TAUGHT THE INCIDENT KEY, SO EVERY INCIDENT CARD SHARES ONE IDENTITY.\nneeds-you.tsx:69-73 `decisionKeyOf(item)` returns `item.goalId` for any item with no `escalation` member (:71). An INCIDENT item is built with `goalId: \"\"` (needs-you-incident.ts, the deliberate rail-5 shape - correct, keep it). So for EVERY incident item `decisionKeyOf` answers `\"\"`, and three separate call sites collapse onto that empty key:\n\n1. :223 `key={`${item.kind}:${decisionKeyOf(item)}`}` -> literally `\"INCIDENT:\"` for every incident card. React reuses ONE `DecisionCard` instance across colliding keys, and `DecisionCard` holds `useState(armed)` at :116. Two environments in an open incident therefore SHARE THE ARMED STATE: arming the rollback confirm on one card can present an armed confirm on the other. That is DoD 2's own hazard - \"rolling back to the wrong sha during an incident is a second incident\" - arriving as a React reconciliation bug rather than a text bug, and it is invisible to any test that seeds a single environment.\n2. :118 `slug` -> `\"incident.\"`, so :124 renders `data-testid=\"cr.needsyou.item.incident.\"` on every incident card. `getByTestId` throws \"Found multiple elements\" on the two-environment case; `queryAllByTestId`[0] silently reads the wrong card.\n3. :227 `decisionResults?.get(decisionKeyOf(item))` -> `get(\"\")` for every incident, so one rollback's busy/outcome/error line renders on ALL incident cards at once.\n\nTHE FIX IS ALREADY HALF-WRITTEN AND ONLY THE WIRING IS MISSING. needs-you-incident.ts already exports `incidentKeyOf(environment, incidentId)` -> `<environment>#<incidentId>` and its own docblock states the intent verbatim: \"The key one incident's dismissal and one incident's decision result are both kept under\". The dismissal side uses it; the DECISION side does not. Add a branch to `decisionKeyOf` BEFORE the `return item.goalId` fallback at :71:\n\n    const incident = item.incident;\n    if (incident !== undefined) return incidentKeyOf(incident.environment, incident.incidentId);\n\nThat is a value import of `incidentKeyOf` into needs-you.tsx (the existing `NeedsYouItem` import there is type-only; the incident module imports `NeedsYouItem` type-only from the model, so no runtime cycle is created). It fixes all three call sites at once because all three route through `decisionKeyOf`.\n\nASSERT IT, do not just patch it: seed TWO environments each with an OPEN INCIDENT and DIFFERENT `rollbackTarget.sha`, arm the confirm on the first, and assert the SECOND card's confirm surface still shows the second environment's sha - not the first's, and not an armed state at all. A single-environment fixture passes with the bug present.\n\nMINOR, same file: the incident item sets `actionLabel: \"Open the goal\"`, but :173 suppresses that button whenever `planningRunRef === \"\"`, which an incident always is. The field is therefore dead text that never renders - harmless today, but it says \"Open the goal\" on the one kind that has no goal, and the module's own `detail` says \"open the environment to read the probes\" instead. Worth making the two agree.\n\nSTILL STANDING from comment-107a5720e8044791ac80b2ad27358d61: apps/control-room/src/v2/goals/plan-rejected-surfaces.test.tsx:58-61 pins NEEDS_YOU_KINDS as an ORDERED EXACT array and sits outside every step's affectedFiles - it needs `moe.declare_files` or the wrapper will not land the edit.",
      "timestamp": "2026-09-08T02:41:05.850Z"
    },
    {
      "id": "comment-9f3bd1496d864190a1f26c5ccc2ffbee",
      "author": "worker-4d8571e7",
      "content": "SESSION D (worker-4d8571e7, PID 67500, launched 2026-09-08 02:43:03Z) YIELDED TO A LIVE TWIN. Comment, not a step note: step notes are last-writer-wins and the twin owns them.\n\nTWIN DETECTION, by clock not by note. `complete_step(step-4)` refused [INVALID_STATE] COMPLETED; the disk said step-4 `completedAt 02:43:49.680Z` while my own first vitest banner read `Start at 05:43:41` (+0300 = 02:43:41Z) - a call I had not yet made cannot have closed it. `Get-CimInstance Win32_Process -Filter \"Name='claude.exe'\"` showed TWO worker seats: PID 65624 (02:31:52Z) and mine PID 67500 (02:43:03Z), both answering workerId worker-4d8571e7. Board motion after my own launch instant settled it: step-5 closed 02:46:12Z, step-6 opened 02:46:20.900Z. Second, independent proof at 02:52Z: `incident-dismiss.test.tsx` and `use-incident-health.ts` appeared untracked and `live-needs-you.tsx` went modified - step-6's deliverable, mid-write. I stopped. No complete_step, no complete_task, no report_blocked (any of those flips the row out from under a live step), no declare_files on the twin's paths.\n\nWHAT I WROTE BEFORE DETECTING IT - disclosed so it is neither lost nor misattributed. THREE ARMS APPENDED to `incident-card.test.tsx` (now 240 lines, describe block \"two environments in an open incident do not share a confirm\"), verified surviving on disk at 02:52Z. Gate: `npx vitest run --root apps/control-room src/v2/approvals/incident-card.test.tsx` -> `Test Files  1 passed (1)` / `Tests  14 passed (14)`, exit 0, at 02:46:11Z. Append-only at end of file; nothing overwritten. THEY PIN A FIX NOTHING ELSE PINS. Session C found and fixed a defect where `decisionKeyOf` returned `item.goalId` for any item with no escalation member; an INCIDENT item is deliberately `goalId: \"\"` (rail 5), so every incident card keyed on \"\" - React reuses ONE DecisionCard, and DecisionCard holds the armed `useState`, so TWO ENVIRONMENTS DOWN AT ONCE SHARE THE ARMED ROLLBACK CONFIRM: arm staging, read production's sha, confirm. That is DoD 2's own hazard arriving as a reconciliation bug, invisible to every single-environment fixture. The fix is in `needs-you.tsx` decisionKeyOf; no step note names it and no arm held it. The three arms: (1) arming staging shows STAGING_SHA and NOT TARGET_SHA at staging's confirm while production has no confirm and its button still reads exactly \"Roll back\"; (2) the port receives environment \"staging\" with staging's own toReceiptRef; (3) a refusal keyed to staging renders on staging's card and is ABSENT from production's. Shas asserted non-equal so the arm cannot pass by their being one string.\n\nEPIC RAIL 3 (no secret in plaintext) - DISCHARGED WITH FILE:LINE, for step 7's self-review. The card renders the error line VERBATIM and deliberately never truncates, so \"can a probe error echo a connection string?\" is a real question. It cannot, and the guard is at the PRODUCER, not this row: `deployments-health-read.ts:238-252` `errorLineOf` sets `line: receipt.refusal.detail` off a deploy receipt - not tool stdout. `deploy-service.ts:149` states it verbatim: \"ONLY A CODE AND A LAYER ARE READ OFF THE THROWN VALUE, never its message\", and `:166-167` composes the detail as `${code}@${layer}` or `${code}@${layer}: ${detail}`, where every `refusal(code, detail)` call site passes a code-shaped constant (DEPLOY_PROXY_WRITE_FAILED, DEPLOY_ROLLBACK_RECEIPT_INVALID, `flipped.detail`, `historical.code`). So the rendered line is daemon-authored code prose. Corroborating negative: `grep -rn \"redactSecret\\|redact(\" apps/daemon/src` returns ZERO files - there is no redaction layer, which is exactly why the producer-side no-message rule is the whole guard. If a future deploy refusal ever interpolates a URL into `detail`, this card publishes it unbounded.\n\nMINOR, UNFIXED: `needs-you-incident.ts` sets `actionLabel: \"Open the goal\"` on every incident item, but `needs-you.tsx:173` suppresses that button whenever `planningRunRef === \"\"`, which an incident always is. Dead text, and it contradicts the module's own `detail` (\"open the environment to read the probes\"). Left alone - editing a file a live step is writing destroys green work.\n\nGATES I DELIBERATELY DID NOT RUN, so the silence is not read as a skipped gate: `pnpm --filter @moe/control-room test` and `pnpm typecheck` BOTH compile the files the twin is writing right now (`incident-dismiss.test.tsx`, `use-incident-health.ts`, `live-needs-you.tsx`). Either would have produced a red owned entirely by half-written syntax, and quoting that - or quoting a green taken minutes earlier - would be a false attribution. The twin runs them at step 7.\n\nALSO NOTE: `full-suite-gate` is held by qa-dbfde2ce (acquired 2026-09-07T22:47:43Z, expires 22:47:43Z today) to rerun `pnpm --filter @moe/control-room test`. On-disk mutation drills on control-room sources will red THEIR run - keep each mutant to a single narrowed vitest invocation restored in the same command, and announce the UTC window.",
      "timestamp": "2026-09-08T02:49:01.692Z"
    },
    {
      "id": "comment-354faf8fc6fb44fda59aa583becee858",
      "author": "worker-4d8571e7",
      "content": "SESSION D ADDENDUM, 02:53Z - A LIVE DRILL MUTANT IS ON DISK RIGHT NOW. Recording it so a commit or a gate taken in this window is not misread.\n\n`apps/control-room/src/v2/approvals/needs-you-incident.ts:100-101` currently carries:\n\n    // DRILL MUTANT: dismissal suppresses the ENVIRONMENT, so the next incident stays silent.\n    if ([...(dismissed ?? [])].some((key) => key.split(\"#\")[0] === environment)) continue;\n\nreplacing the production line `if (dismissed?.has(incidentKeyOf(environment, incident.id)) === true) continue;`.\n\nTHIS IS THE TWIN'S STEP-6 DIRECTION-2 DRILL, not stranded debris, and the discriminator is the one `mem:gotcha-tell-a-live-drill-from-a-stranded-mutant-before-you-yield` names: step-6 is IN_PROGRESS (`startedAt 02:46:20.900Z`), the twin PID 65624 is alive, and the mutant is SELF-LABELLED with the exact defect the step's drill script calls for (\"make dismiss suppress the next raise, run direction 2, confirm RED; restore\"). I did NOT restore it. Restoring another session's live mutant mid-drill either re-greens the arm it is being graded against or races the twin's own restore, and `git diff` would then show clean while the drill was silently voided.\n\nCONSEQUENCES FOR ANYONE READING THIS WINDOW:\n1. ANY gate run between ~02:52Z and the twin's restore will red on `incident-dismiss.test.tsx` / `needs-you-incident.test.ts` direction-2 arms. That red is EXPECTED and belongs to the drill, not to a defect. It is a second, independent reason session D did not run `pnpm --filter @moe/control-room test` or `pnpm typecheck`.\n2. IF A WRAPPER EXIT COMMIT IN THIS WINDOW CARRIES `needs-you-incident.ts`, it carries the mutant. Session D declared no files and its three `complete_step` calls were all refused, so nothing was asserted from this seat - but the plan's affectedFiles can still attribute the path. QA: verify the DELIVERED bytes at the row's own completion sha, not at an intermediate `wip(...)` checkpoint, and confirm line 100-101 reads `dismissed?.has(incidentKeyOf(environment, incident.id))`. The `key.split(\"#\")[0]` form must NOT appear at HEAD: it keys the dismissal on the ENVIRONMENT rather than the INCIDENT, which is precisely the \"dismiss quietly ends monitoring\" failure DoD 3 direction 2 exists to refuse.\n3. The restore proof for this path is `git diff -- apps/control-room/src/v2/approvals/needs-you-incident.ts` ONLY IF the file is tracked at the time. It IS tracked (committed in 157fe382); `git status --porcelain` listed it clean before the mutation. Sibling paths in the same drill are NOT: `incident-card.test.tsx`, `incident-card.tsx`, `incident-frames.fixture.ts`, `incident-dismiss.test.tsx`, `rollback-port.ts`, `rollback-port.test.ts` and `use-incident-health.ts` are all `??`, where `git diff` is blind - empty before, during and after - so any drill touching those needs a SHA256 comparison against an out-of-tree backup, in a call separate from the restore.\n\nNo further action from session D. The twin supplies the terminal call.",
      "timestamp": "2026-09-08T02:50:02.631Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T20:05:32.557Z",
  "updatedAt": "2026-09-08T02:50:02.631Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T22:15:51.129Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 5
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-05T22:20:59.455Z",
  "planningNotes": {
    "approachesConsidered": "FILING THE READ AS A PREREQUISITE vs BUILDING IT HERE. Building it here was rejected twice over: the daemon route plus its .js bridge, its test and the listener composition across four files is six-plus paths on top of this row's roster, card, decoder, proxy pin and tests; and this is a UI row whose rails confine it to the control room. The rails' answer to a missing prerequisite is a production row measured symbol by symbol, which is what task-509c6c16 is.\n\nOVERLOADING /health/read INSTEAD. Rejected and written into the new row's rails: it serves daemon liveness, and putting an environment outage behind it would make one route mean two unrelated things.\n\nWHERE THE INCIDENT ROUTES. Not resolved unilaterally - it is a deliberate decision the row asks for, and step 2 makes the worker state it in the record. What IS settled here is the constraint: whatever shape is chosen must not put a non-goal id in `goalId`, because :186-187 dereferences it in a comparator that the other four kinds rely on.\n\nSYNTHESISING THE ROLL BACK CONTROL IN THE BROWSER. Rejected by DoD 5 and by the existing seam: `offerFor` is right there at :77 and the other four kinds all use it. A synthesised control offers what the command path can refuse.",
    "codebaseInsights": "MEASURED AT HEAD (moe/work-2026-09-04) by architect-fa3a3e17, 2026-09-06.\n\nTHE ROSTER HAS NOT GROWN YET, despite three rows editing it. needs-you-model.ts:28 still reads exactly `NEEDS_YOU_KINDS = [\"PLAN_APPROVAL\", \"ESCALATION\", \"GATE_1\", \"READY_TO_CLOSE\"]`, KIND_ORDER at :72-74 still has four entries, and KIND_EYEBROW at needs-you.tsx:41-46 still has four. So this row is likely to land the FIFTH first - but task-a988ca1c, task-33ceae56 and task-84cd2e55 are all WORKING on these two files, so re-measure at execution and rebase. Files are 198 and 211 lines.\n\nTHE goalId HAZARD IS MECHANICAL, NOT ONLY SEMANTIC - this is the measurement that matters most for rail 5. needs-you-model.ts:186-187 sorts items with:\n  .sort((left, right) => KIND_ORDER[left.kind] - KIND_ORDER[right.kind] || left.title.localeCompare(right.title) || left.goalId.localeCompare(right.goalId))\n`goalId` is REQUIRED on every item and is DEREFERENCED in the comparator. An incident routes to an ENVIRONMENT, so there is no goal id to put there - and putting an environment id in it does not merely mislead a reader, it feeds `localeCompare` a value the whole surface assumes is a goal. Decide the shape deliberately (a discriminated member, or an optional routing target with the comparator taught to handle its absence) and say why in the record. Leaving `goalId` holding a non-goal is the failure rail 5 names.\n\nTHE DAEMON-OFFER SEAM EXISTS AND IS NAMED: needs-you-model.ts:77 `offerFor(surface, commandKind, target)` returns the offer from the SurfaceFrame or `undefined` when `surface.outcome !== \"SURFACE\"`. That is how the existing four kinds rest on a daemon offer, and it is what DoD 5 requires the Roll back control to use. A browser-synthesised control is the dead-button hazard this board has shipped once already.\n\nTHE READ DID NOT EXIST AND IS NOW A PREREQUISITE ROW. `ls apps/daemon/src/http/ | grep -i incident` returned nothing. `/health/read` EXISTS but is NOT this: health-read.ts (217 lines, mounted at daemon-store-foundation-composition.ts:51, pinned at dev-proxy-paths.ts:24) serves the DAEMON's provider-pause and repository-reservation state, not deployment environments. I filed task-509c6c1609174adb9228940bc002f5c3 (order 123.05) to serve `/deployments/health/read` - derived state, last probe, THE LAST ERROR LINE VERBATIM, the open incident, and the previous deploy receipt's sha - and composed into the production listener in the same row. Grandchild A needs the same read, so it is filed once for both.\n\nDEPENDENCIES NOW FOUR (previousDependsOn had two): the probe/incident row, the backups/rollback row, the new read row, and GRANDCHILD A task-df972c274f2a43eda3a9f57d2780c6f9 (the Environments surface, order 123.1). A is added because DoD 3 asserts the environment \"still reads DOWN on the Environments surface\" - that surface is A's, and asserting against it requires it to exist. That is ordering fixable by declaring the dependency, not a block.\n\nTHE INCIDENT ITSELF is written by task-cc05c473 under the probe row: `moe-health-probe/1`, a ring bounded at 1440, UP/DEGRADED/DOWN DERIVED from the ring (never stored), and exactly one open incident per outage, closed on recovery and reopened at the next threshold. DoD 3's \"the card returns at the next failure threshold\" is that lifecycle - assert against it rather than inventing a re-raise rule here."
  },
  "planApprovedAt": "2026-09-05T22:21:01.586Z",
  "contextFetchedBy": [
    "worker-76f4ef35",
    "worker-4d8571e7"
  ],
  "workStartedAt": "2026-09-07T19:36:53.537Z",
  "blockedReason": null,
  "blockedOnTaskIds": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "commits": [
    {
      "sha": "ac5c9ecd11dddf985b57b48d348c1d07f3df96a2",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-4d8571e7@2026-09-08T02:31:48Z",
      "paths": [
        ".moe/tasks/task-85895a88a2484d8589046b2030efd445.json"
      ],
      "recordedBy": "worker-4d8571e7",
      "recordedAt": "2026-09-08T02:31:51.486Z",
      "treeId": "8e7182f76f644714eef84a3d29d00c978ace20b6",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "643d120464b031845884d5939323020d8c11fb6d",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-4d8571e7@2026-09-08T02:31:37Z",
      "paths": [
        ".moe/tasks/task-2f2ed034fbec4fe48b49a951951c9505.json",
        ".moe/tasks/task-85895a88a2484d8589046b2030efd445.json"
      ],
      "recordedBy": "worker-4d8571e7",
      "recordedAt": "2026-09-08T02:37:59.604Z",
      "treeId": "1a46cdbab58a898f388695341019eefeacf3ea50",
      "status": "WORKING",
      "contested": [
        {
          "path": "apps/control-room/src/v2/approvals/needs-you.tsx",
          "taskId": "task-817d893fa1254a4d82d2888af1f87a47"
        },
        {
          "path": "apps/control-room/src/v2/approvals/needs-you-model.ts",
          "taskId": "task-817d893fa1254a4d82d2888af1f87a47"
        }
      ],
      "pushed": true
    },
    {
      "sha": "157fe3826dc7f2e506fcc70fd51747b5f1337cb7",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-4d8571e7@2026-09-08T02:38:31Z",
      "paths": [
        ".moe/tasks/task-85895a88a2484d8589046b2030efd445.json",
        "apps/control-room/src/live/live-deployments-health.test.ts",
        "apps/control-room/src/v2/approvals/needs-you-incident.test.ts",
        "apps/control-room/src/v2/approvals/needs-you-incident.ts"
      ],
      "recordedBy": "worker-4d8571e7",
      "recordedAt": "2026-09-08T02:42:31.766Z",
      "treeId": "17bb01f8747bf85c5d6fe4cca51ecc20be66f44d",
      "status": "WORKING",
      "contested": [
        {
          "path": "apps/control-room/src/v2/approvals/needs-you.tsx",
          "taskId": "task-817d893fa1254a4d82d2888af1f87a47"
        },
        {
          "path": "apps/control-room/src/v2/approvals/needs-you-model.ts",
          "taskId": "task-817d893fa1254a4d82d2888af1f87a47"
        },
        {
          "path": "apps/control-room/src/v2/goals/plan-rejected-surfaces.test.tsx",
          "taskId": "task-817d893fa1254a4d82d2888af1f87a47"
        }
      ],
      "pushed": true
    }
  ],
  "filesModified": [
    ".moe/tasks/task-85895a88a2484d8589046b2030efd445.json",
    ".moe/tasks/task-2f2ed034fbec4fe48b49a951951c9505.json",
    "apps/control-room/src/live/live-deployments-health.test.ts",
    "apps/control-room/src/v2/approvals/needs-you-incident.test.ts",
    "apps/control-room/src/v2/approvals/needs-you-incident.ts"
  ],
  "unattributedPaths": [
    "apps/control-room/src/v2/approvals/incident-card.test.tsx",
    "apps/control-room/src/v2/approvals/incident-card.tsx"
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "worker-4d8571e7@2026-09-08T02:38:31Z",
    "at": "2026-09-08T02:42:31.766Z"
  }
}