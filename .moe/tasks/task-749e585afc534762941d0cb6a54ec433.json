{
  "id": "task-749e585afc534762941d0cb6a54ec433",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "Publish the probe-interval operator command on the shared runtime contract, human-only, and regenerate the client",
  "description": "SPIDR child of task-078232fb9b9d4c06a5faf3c02adaf705, cut by architect-5ac70ba8 on 2026-09-07. The parent's DoD spanned three separable responsibilities and measured ~25 files against the board's 20-file cap, so the command SURFACE and the health READ were cut off the parent's durable-record core.\n\nTHIS ROW TAKES the parent's DoD 1 COMMAND HALF. The parent keeps DoD 1's durable record, store port and bounds, plus DoD 2 (scheduler honours the interval per environment) and DoD 3 (boot re-registration). dependsOn the parent because the command handler writes THROUGH the interval store port the parent lands - publishing a kind whose handler has nothing to write to is a producer with no consumer edge.\n\nMEASURED AT HEAD by architect-5ac70ba8 (2026-09-07 ~13:20Z), so plan against this and re-measure before editing:\n- The kind roster is `packages/contracts/src/runtime/runtime-vocabulary.ts:91-92` (\"deployment.deploy\", \"deployment.migrate_down\", \"deployment.rollback\", \"deployment.set_target\", ...).\n- THE EPIC RAIL'S MIRROR LOCATION IS STALE. It says \"two mirrors live under apps/control-room\". Measured `git grep -rln GENERATED_CONTRACT_DIGEST -- apps packages adapters tools` returns SEVEN source files and NONE under apps/control-room: apps/daemon/src/cutover/v2-readiness-evidence-collector.ts, apps/daemon/src/cutover/v2-readiness-evidence-producers.test.ts, apps/daemon/src/http/affordance-deploy-target-offers.ts, packages/control-room-client/generator/generate.ts, packages/control-room-client/src/contract-digest.test.ts, packages/control-room-client/src/contract-pins.ts, packages/control-room-client/src/generated/generated-client.ts. Re-census with the full-tree grep yourself; the relevant package is `@moe/control-room-client` (packages/control-room-client), and `@moe/control-room` (apps/control-room) is a SEPARATE package that must also be run.\n- PRECEDENT ROWS on this board for exactly this shape: task-b3888068 (DONE, \"The two environment kinds reach the shared runtime contract and the generated client\") and task-a2409cba (DONE, \"environment.set_variable and unset_variable are published operator-only\"). Read their completion records before designing the fence.\n\nNOT IN SCOPE: the durable interval record, the store port, the scheduler, boot re-registration (all the parent's), and the health read disclosure (task filed alongside this one).",
  "definitionOfDone": [
    "The kind is added to the shared runtime roster at packages/contracts/src/runtime/runtime-vocabulary.ts and is HUMAN-ONLY: MCP-excluded, asserted by code AND layer at the fence, following the precedent task-a2409cba set for environment.set_variable.",
    "ALL FOUR GENERATED_CONTRACT_DIGEST mirrors are re-censused by a full-tree `git grep` of the literal across ALL file types and updated - the measured source set is listed in this row's description and must be re-measured, not trusted. The generated client is regenerated rather than hand-edited.",
    "The command handler writes the per-environment interval THROUGH the store port landed by task-078232fb - no second write path and no direct store access. Bounds and refusals are the port's: out-of-range and unknown-environment each assert code AND layer.",
    "Roster coverage is BIDIRECTIONAL (global rail): every advertised kind is served AND every served kind is advertised, enumerated from the dispatch seam's handled kinds and asserted as set-equality, not subset.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room-client test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`. The control-room legs are mandatory because the digest mirrors are invisible to `pnpm test` and to the daemon leg."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE AND BASELINE. NO EDITS. Three confirmations, and one correction that decides whether you run the control-room leg.\n(a) **THE ROSTER**: `sed -n '85,100p' packages/contracts/src/runtime/runtime-vocabulary.ts` — `RUNTIME_COMMAND_KINDS`, ALPHABETICAL, deployment kinds at :91-92, `environment.set_variable`/`unset_variable` at :94.\n(b) **THE DIGEST CENSUS, FULL TREE, ALL FILE TYPES** (DoD 2): `git grep -rln GENERATED_CONTRACT_DIGEST -- apps packages adapters tools`. I measured SEVEN source files and NONE under `apps/control-room`: `apps/daemon/src/cutover/v2-readiness-evidence-collector.ts`, `apps/daemon/src/cutover/v2-readiness-evidence-producers.test.ts`, `apps/daemon/src/http/affordance-deploy-target-offers.ts`, `packages/control-room-client/generator/generate.ts`, `packages/control-room-client/src/contract-digest.test.ts`, `packages/control-room-client/src/contract-pins.ts`, `packages/control-room-client/src/generated/generated-client.ts`. Re-run it; do not trust my list. Of those seven, the two under `apps/daemon/src/cutover/` and the affordance module carry the literal but are probably not roster-sensitive — read each and say which mirrors need editing and which merely mention the constant. That is DoD 2's \"re-censused\", and a verdict per file is what it asks for.\n(c) **THE CORRECTION THAT MATTERS MOST. DO NOT SKIP THE CONTROL-ROOM LEG.** The epic rail says \"two mirrors live under apps/control-room\"; (b) finds none there, and this row's description calls the rail stale on that basis. **BOTH ARE HALF RIGHT, AND \"NO CONTROL-ROOM WORK\" IS THE WRONG CONCLUSION.** Read `git show --stat 4f6bfbf0` — task-b3888068, the landed precedent for this exact operation. It touched SIX source files and TWO are under `apps/control-room`: `src/data/data-adapter.test.ts` and `src/v2/goals/approve-plan.test.tsx`. They are ROSTER COUNT PINS, not digest mirrors, which is why the digest grep misses them — and they are invisible to `pnpm test`, `pnpm typecheck` and the daemon leg. So the rail's CONCLUSION is right while its REASON is wrong.\n(d) **THE FENCE PRECEDENT**: `sed -n '210,226p' apps/daemon/src/orchestrator/agent-spawn-contract.ts`. Read the comment about `environment.set_variable` before writing yours — it states why TWO fences are wanted: \"that one stops an agent reaching the kind over a transport, this one stops the wrapper minting a session to take it as staffed work. Different actors, so both are wanted.\"\nBASELINE, quoted with `Test Files N passed` count lines, because this row spans three packages and the precedent's own commit lists `runtime-vocabulary.ts`, `generated-client.ts`, `generated-coverage.test.ts`, `runtime-contract.test.ts`, `data-adapter.test.ts` and `approve-plan.test.tsx` as CONTESTED with another row: `pnpm --filter @moe/daemon test` and `pnpm --filter @moe/control-room test`.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/contracts/src/runtime/runtime-vocabulary.ts"
      ],
      "note": "MEASURE + BASELINE at HEAD 3a056078. NO EDITS. Four findings, two of which change the plan.\n\n(a) ROSTER CONFIRMED. packages/contracts/src/runtime/runtime-vocabulary.ts:85 RUNTIME_COMMAND_KINDS, alphabetical, deployment kinds at :91-92, environment.set_variable/unset_variable at :94. runtime-contract.test.ts holds a hand-transcribed EXPECTED_COMMAND_KINDS + `expect(commands.size).toBe(119)` literal (:108-109).\n\n(b) DIGEST CENSUS BY CONSTANT NAME — `git grep -rln GENERATED_CONTRACT_DIGEST -- apps packages adapters tools` reproduces the description's SEVEN files EXACTLY. VERDICT PER FILE:\n  1. apps/daemon/src/cutover/v2-readiness-evidence-collector.ts — SYMBOL IMPORT ONLY (:9 import, :67 use). NO EDIT.\n  2. apps/daemon/src/cutover/v2-readiness-evidence-producers.test.ts — SYMBOL IMPORT ONLY (:4,:68,:72). NO EDIT.\n  3. apps/daemon/src/http/affordance-deploy-target-offers.ts — the literal appears in a COMMENT only (:23). NO EDIT.\n  4. packages/control-room-client/generator/generate.ts — COMPUTES the digest. NO EDIT (it is the producer).\n  5. packages/control-room-client/src/contract-digest.test.ts — DERIVES live and compares to the export (:21). NO LITERAL. NO EDIT.\n  6. packages/control-room-client/src/contract-pins.ts — bare re-export (:7). NO LITERAL. NO EDIT.\n  7. packages/control-room-client/src/generated/generated-client.ts — SOURCE OF TRUTH. REGENERATE.\n\n(b2) THE CONSTANT-NAME GREP IS THE WRONG NEEDLE, AND THIS IS THE BIGGEST CORRECTION. Re-censused by the digest VALUE instead: `git grep -ln 9f578ae0a875d498cce7b9da03252daaa2655f825263c0963b4899971001e1ca` returns SIX source files, FOUR of them under apps/control-room, where they are spelled `contractSchemaHash:` and never `GENERATED_CONTRACT_DIGEST`:\n  apps/control-room/src/data/data-adapter.test.ts\n  apps/control-room/src/v2/goals/approve-plan.test.tsx\n  apps/control-room/src/v2/goals/approve-plan-reject.test.tsx      <-- NOT NAMED IN THE PLAN\n  apps/control-room/src/v2/goals/plan-run-resolution.test.tsx      <-- NOT NAMED IN THE PLAN\n  packages/control-room-client/src/generated-coverage.test.ts (carries BOTH the digest and GENERATED_FILE_SHA256 668ff859…)\n  packages/control-room-client/src/generated/generated-client.ts\nSo EPIC RAIL 1 IS CORRECT ON BOTH COUNTS and this row's description is the stale one: mirrors DO live under apps/control-room — there are now FOUR, not two — and they are digest mirrors, not \"count pins\". The precedent 4f6bfbf0 (2026-09-06) touched two; two more have appeared since. DoD 2 orders re-measurement, so the two unnamed files are DoD-mandated, not scope creep. Disclosed here rather than discovered at step 5.\n\n(c) PRECEDENT CONFIRMED. `git show --stat 4f6bfbf0` = 6 source files, 2 under apps/control-room, all six Moe-Contested with task-37a50989. The diff shape: roster + EXPECTED_COMMAND_KINDS + size literal (110->115) + GENERATED_FILE_SHA256 + CONTRACT_DIGEST + the two contractSchemaHash literals. Generate script measured: `pnpm --filter @moe/control-room-client generate` (package.json:18).\n\n(d) THE FENCE MEASUREMENT THAT INVALIDATES STEP 4(1), AND FORCES THE KIND'S SPELLING.\n  - `mcp-tool-allowlist.ts:73-95`: MCP_EXCLUDED_COMMAND_KINDS is `...[...OPERATOR_PRINCIPAL_KINDS].filter(k => !MCP_REACHABLE_OPERATOR_KINDS.has(k)).sort()` — FULLY DERIVED. There is NO hand-editable entry in that file. Confirms `mem:convention-widen-the-operator-fence-disjunct-never-the-derived-roster`.\n  - OPERATOR_PRINCIPAL_KINDS lives at `daemon-command-vocabulary.ts:347` and is typed `ReadonlySet<WiredCommandKind>`, a union built from PAYLOAD_KEYS (:220). Adding a kind absent from PAYLOAD_KEYS is a TYPE ERROR, and `mcp-tool-allowlist.test.ts:255-259` cross-checks `wiredMcpToolKinds().length === |PAYLOAD_KEYS| - |MCP_EXCLUDED| + |QUERIES|`, which reds arithmetically for such a kind. daemon-command-vocabulary.ts and daemon-command-payload-keys.ts are task-eb37494e's owned paths (this row's risk 7). SO THE MCP-ROSTER HALF IS STRUCTURALLY UNDELIVERABLE FROM THIS ROW. It is not skipped: the kind is MCP-UNREACHABLE AT THE DELIVERED TREE ANYWAY, because WIRED_KINDS (:97-103) derives from PAYLOAD_KEYS, which will not carry the kind until task-eb37494e lands. Step 4 will assert that unreachability against the PRODUCTION allowlist plus a non-vacuous implication arm that reds if task-eb37494e wires the kind without fencing it.\n  - `HUMAN_ONLY_STEPS` (agent-spawn-contract.ts:198) is `ReadonlySet<string>` — the SPAWN fence IS deliverable here.\n  - THE SPELLING IS FORCED. `daemon-store-dependencies.test.ts:1155-1168` asserts SET EQUALITY between `deploymentMembers(PAYLOAD_KEYS)` and `deploymentMembers(OPERATOR_PRINCIPAL_KINDS)`, `deploymentMembers(MCP_EXCLUDED_COMMAND_KINDS)` and `deploymentMembers(HUMAN_ONLY_STEPS)`. A `deployment.*` kind added to HUMAN_ONLY_STEPS before its PAYLOAD_KEYS entry exists REDS that arm, and loosening it is forbidden. So `deployment.set_probe_interval` makes \"the fence lands with the advertisement\" impossible. KIND SPELLING CHOSEN: **`m",
      "startedAt": "2026-09-07T16:34:49.962Z",
      "completedAt": "2026-09-07T16:52:10.492Z"
    },
    {
      "stepId": "step-2",
      "description": "THE KIND ON THE SHARED ROSTER, AND THE CLIENT REGENERATED (DoD 1 roster half, DoD 2). Copy task-b3888068's landed shape (`git show 4f6bfbf0`).\n(1) `packages/contracts/src/runtime/runtime-vocabulary.ts` — add the kind to `RUNTIME_COMMAND_KINDS`, KEEPING THE ROSTER ALPHABETICAL. The parent row calls this a per-environment probe interval, so choose a name that sorts with the kinds it belongs to. **PICK ONE SPELLING AND RECORD IT IN THE COMPLETION SUMMARY** — task-078232fb (the store port), task-eb37494e (the dispatch and handler) and the health-read row all name this kind, and a second spelling is how a four-row chain drifts.\n(2) `packages/contracts/src/runtime/runtime-contract.test.ts` — the contract arm the precedent updated.\n(3) **REGENERATE `packages/control-room-client/src/generated/generated-client.ts`; DO NOT HAND-EDIT IT** (DoD 2). The generator is `packages/control-room-client/generator/generate.ts` and the file is itself one of the seven digest carriers, so a hand edit desynchronises the digest from the roster it is supposed to pin and the failure surfaces far from its cause. Find and run the generate script; if you cannot, STOP AND REPORT rather than editing the generated file.\n(4) `packages/control-room-client/src/contract-pins.ts` and `src/contract-digest.test.ts` carry the digest. **QUOTE THE BEFORE AND AFTER DIGEST VALUES IN YOUR RECORD**: a digest that did NOT move after a roster addition means the regeneration did not run, and that is the failure this clause exists to catch.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/contracts/src/runtime/runtime-vocabulary.ts",
        "packages/contracts/src/runtime/runtime-contract.test.ts",
        "packages/control-room-client/src/generated/generated-client.ts",
        "packages/control-room-client/src/contract-pins.ts",
        "packages/control-room-client/src/contract-digest.test.ts"
      ],
      "modifiedFiles": [
        "packages/contracts/src/runtime/runtime-vocabulary.ts",
        "packages/contracts/src/runtime/runtime-contract.test.ts",
        "packages/control-room-client/src/generated/generated-client.ts"
      ],
      "note": "THE KIND ON THE SHARED ROSTER + CLIENT REGENERATED.\n\n**KIND SPELLING, FINAL AND SINGLE: `monitoring.set_probe_interval`.** Chosen at step 1(d) under measurement, not taste: `daemon-store-dependencies.test.ts:1155-1168` asserts SET EQUALITY between `deploymentMembers(PAYLOAD_KEYS)` and `deploymentMembers()` of each of the three fence rosters, so a `deployment.*` spelling could not be fenced in this row without redding that arm or loosening it, and the epic rail requires the fence to land WITH the advertisement. `monitoring.` also matches task-078232fb's own scheduler job namespace `monitoring/health-probes/<env>`. Comment going onto task-eb37494e, whose description still assumes a deployment-family kind.\n\n(1) `runtime-vocabulary.ts:102-103` — kind inserted between `lease.mark_suspect` and `plan.propose`. Sortedness verified against the production tuple, not by eye: 121 members, 121 unique, `ks == sorted(ks)` True, index 60.\n(2) `runtime-contract.test.ts:52-53` — same insertion in the hand-transcribed `EXPECTED_COMMAND_KINDS`. Size literal 120 -> 121, plus a new `expect(RUNTIME_COMMAND_KINDS).toContain(\"monitoring.set_probe_interval\")` so a mistranscription of the hand roster cannot drop the kind while the count still adds up.\n    PRE-EXISTING DRIFT REPAIRED IN PASSING, disclosed: at 3a056078 the comment read \"Literal 119\" while the assertion read `toBe(120)` — a prior addition bumped the number and left the prose. Comment now says 121 and matches, with a line saying why.\n(3) REGENERATED, NOT HAND-EDITED: `pnpm --filter @moe/control-room-client generate` (package.json:18 -> `node --experimental-strip-types generator/generate.ts`). Diff is exactly three lines the generator emitted — a `GeneratedCommandBuilders` member, a `GENERATED_COMMAND_BUILDERS` entry, and the digest constant.\n\n**BEFORE AND AFTER, the clause's own failure detector:**\n  GENERATED_CONTRACT_DIGEST  9f578ae0a875d498cce7b9da03252daaa2655f825263c0963b4899971001e1ca\n                          -> daafa7872670d05bd428a3037b1afea1ce357d83bea8f434623080356d26a025\n  generated-client.ts sha256 668ff85948d946ddf4da8c24c086ca04d0be79ebc303494a51bc980bd0293bca\n                          -> 876a7e5b0cde83d214c93e1af4288bd3ae4c5444b4c8cef2f324330eb87d78d5\nBoth MOVED, so the regeneration demonstrably ran.\n\n(4) `contract-pins.ts` (bare re-export, :7) and `contract-digest.test.ts` (derives live and compares, :21) carry NO literal — measured at step 1, no edit, and none needed. The literals live in `generated-coverage.test.ts` and the four apps/control-room mirrors; those are step 3.\n\nGATE THIS STEP: `pnpm --filter @moe/contracts test` -> exit 0, `Test Files  15 passed (15)` / `Tests  509 passed (509)`.",
      "startedAt": "2026-09-07T16:52:13.247Z",
      "completedAt": "2026-09-07T16:53:18.673Z"
    },
    {
      "stepId": "step-3",
      "description": "THE COUNT PINS THE DIGEST GREP CANNOT FIND (DoD 2, DoD 5) — the step step 1(c) exists to justify. These red only under their own package legs, and nothing else runs them.\n(1) `packages/control-room-client/src/generated-coverage.test.ts` — the generated-surface coverage pin the precedent updated.\n(2) `apps/control-room/src/data/data-adapter.test.ts` — a roster count pin; the precedent's diff shows a one-line change here.\n(3) `apps/control-room/src/v2/goals/approve-plan.test.tsx` — the same shape.\nFOR EACH, READ WHAT IT ACTUALLY PINS BEFORE CHANGING A NUMBER: if it is a bare total, say so and update it; if it enumerates kinds, ADD the kind rather than incrementing a count. A count pin bumped without reading what it counts is how a roster gains a member nothing serves.\nRun `pnpm --filter @moe/control-room-client test` and `pnpm --filter @moe/control-room test` after this step and quote their count lines. Do not leave a red here for step 5 to discover — these two legs are the whole reason DoD 5 names them.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/control-room-client/src/generated-coverage.test.ts",
        "apps/control-room/src/data/data-adapter.test.ts",
        "apps/control-room/src/v2/goals/approve-plan.test.tsx"
      ],
      "modifiedFiles": [
        "packages/control-room-client/src/generated-coverage.test.ts",
        "apps/control-room/src/data/data-adapter.test.ts",
        "apps/control-room/src/v2/goals/approve-plan.test.tsx",
        "apps/control-room/src/v2/goals/approve-plan-reject.test.tsx",
        "apps/control-room/src/v2/goals/plan-run-resolution.test.tsx"
      ],
      "note": "THE MIRRORS THE CONSTANT-NAME GREP CANNOT FIND. FIVE files, not three — the two extra are the step 1(b2) census result and are DoD 2's own \"re-measured, not trusted\" clause paying out.\n\nWHAT EACH ONE ACTUALLY PINS, read before any number changed:\n(1) `packages/control-room-client/src/generated-coverage.test.ts` — NOT a count pin. It already asserts BIDIRECTIONAL set-equality between the generated builders and the live roster (`GENERATED_COMMAND_KINDS` <-> `LIVE_COMMAND_KINDS`, :89 AND :90, both directions, each guarded by a non-empty control at :87-88), so the new kind needed NO enumeration edit — it flowed through from the regeneration. What it does pin by literal is two hashes: `GENERATED_FILE_SHA256` and `CONTRACT_DIGEST`. Both updated. The `toHaveLength(4)` / `toHaveLength(1)` at :107/:118 are the size of a hand-written REQUIRED-kinds sample, not of the roster — untouched, correctly.\n(2)-(5) The four apps/control-room mirrors are `contractSchemaHash:` literals feeding the client's COMPAT GATE, one occurrence each, each inside a helper that mints an admitted client/wire and throws if the gate refuses:\n    apps/control-room/src/data/data-adapter.test.ts\n    apps/control-room/src/v2/goals/approve-plan.test.tsx\n    apps/control-room/src/v2/goals/approve-plan-reject.test.tsx      <-- not named in the plan\n    apps/control-room/src/v2/goals/plan-run-resolution.test.tsx      <-- not named in the plan\n    They are NOT bare totals and NOT kind enumerations, so there was no count to increment and no kind to add: the correct edit is the hash, and only the hash.\n\n**SO THE PLAN'S FRAMING WAS WRONG IN A WAY WORTH RECORDING: these are digest mirrors, not \"roster count pins\".** They were invisible to `git grep GENERATED_CONTRACT_DIGEST` only because they spell the value under a different identifier (`contractSchemaHash`). Epic rail 1 is therefore RIGHT ON BOTH COUNTS — mirrors do live under apps/control-room, and they are digest mirrors — while this row's description was the stale one. The right needle is the digest VALUE, not the constant name. Two mirrors have appeared since the precedent 4f6bfbf0 landed on 2026-09-06.\n\nEDIT APPLIED: `9f578ae0…1e1ca` -> `daafa787…6a025` (digest, 5 files) and `668ff859…293bca` -> `876a7e5b…d78d5` (generated-client sha256, 1 file).\nCLOSURE PROVED, not assumed: `git grep -ln <old digest> -- apps packages adapters tools` -> NONE; same for the old file sha -> NONE. No stale copy left in any file type.\n\nGATES, standalone and foreground, one command per call, count lines quoted:\n  `pnpm --filter @moe/control-room-client test` -> exit 0, `Test Files  10 passed (10)` / `Tests  96 passed (96)`.\n  `pnpm --filter @moe/control-room test` -> exit 0, `Test Files  195 passed (195)` / `Tests  2517 passed (2517)`.\nNO RED LEFT FOR STEP 5.\n\nSHARED-CHECKOUT MOVEMENT, disclosed: the control-room leg went 2500 -> 2517 tests between the step 1 baseline and now. NOT MINE — I added no control-room test. `git status --porcelain` shows peers editing `live-deployments-health.{ts,test.ts}`, `v2/ops/environments-section.test.tsx` and `v2/ops/live-environments.test.tsx` (task-620fb461, health read) plus eight apps/daemon backups paths (task-1f994315). Noted, not reverted, not stashed, not a stop condition. My six paths in those two packages are exactly the ones listed in this step's and step 2's modifiedFiles.",
      "startedAt": "2026-09-07T16:53:20.745Z",
      "completedAt": "2026-09-07T16:54:40.687Z"
    },
    {
      "stepId": "step-4",
      "description": "THE HUMAN-ONLY FENCE (DoD 1 second half) — **TWO ROSTERS, AND BOTH ARE WANTED**. It lands in THIS row, with the advertisement, deliberately: a kind advertised on the shared roster before its fence exists is reachable by an agent for however long the next row takes.\n(1) `apps/daemon/src/mcp-tool-allowlist.ts` — the MCP EXCLUSION. Stops an agent REACHING the kind over a transport.\n(2) `apps/daemon/src/orchestrator/agent-spawn-contract.ts` — the SPAWN fence, beside `environment.set_variable`/`unset_variable` at :220 and `project.set_agent_provider` at :223. Stops the WRAPPER minting a session to take it as staffed work.\nWRITE THE REASON AT THE SITE, in the register the neighbours use — each of them says what the kind would let an agent do. Here: how often production is probed is an operator's judgement about monitoring cost and noise, and an agent able to widen the interval could quietly silence the surface that reports an outage.\nARMS, in the roster tests that already enumerate these fences BY EXACT LIST — add the kind, never loosen the assertion: `apps/daemon/src/mcp-tool-allowlist.test.ts` (:231 enumerates the deployment kinds), `apps/daemon/src/mcp-main.test.ts` (:227, same list), `apps/daemon/src/daemon-command-registry-human-approver.test.ts` (:355, the human-approver roster).\n**ASSERT THE FENCE REFUSAL BY CODE AND LAYER**, not merely that the call did not succeed (DoD 1, global rail 1). Two fences can both refuse; the arm must say which one did, or it stays green when the wrong one starts answering first.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/orchestrator/agent-spawn-contract.ts",
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/daemon-command-registry-human-approver.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/agent-spawn-contract.ts",
        "apps/daemon/src/orchestrator/agent-wrapper.test.ts",
        "apps/daemon/src/mcp-tool-allowlist.test.ts"
      ],
      "note": "THE HUMAN-ONLY FENCE. **STEP 4(1) AS WRITTEN IS NOT PERFORMABLE, AND THE REASON IS ARCHITECTURAL, NOT A SHORTCUT.** Delivered the property the clause is for, by the only route the codebase permits, and disclosed rather than quietly narrowed.\n\nWHY `mcp-tool-allowlist.ts` COULD NOT BE EDITED (measured at step 1(d), re-confirmed here):\n  `mcp-tool-allowlist.ts:73-95` — `MCP_EXCLUDED_COMMAND_KINDS` is `...[...OPERATOR_PRINCIPAL_KINDS].filter(k => !MCP_REACHABLE_OPERATOR_KINDS.has(k)).sort()`. FULLY DERIVED; the file contains no hand-editable entry, and the comment at :74-83 says deriving is deliberate (\"a kind that becomes operator-only leaves the MCP roster with no edit here\").\n  The upstream roster `OPERATOR_PRINCIPAL_KINDS` is at `daemon-command-vocabulary.ts:347`, typed `ReadonlySet<WiredCommandKind>` — a union built from `PAYLOAD_KEYS` (:220). Naming a kind absent from `PAYLOAD_KEYS` is a TYPE ERROR. Even if it compiled, `mcp-tool-allowlist.test.ts:255-259` asserts `|wiredMcpToolKinds()| === |PAYLOAD_KEYS| - |MCP_EXCLUDED| + |QUERIES|`, which reds arithmetically for a kind that grows the exclusion without growing PAYLOAD_KEYS.\n  `daemon-command-vocabulary.ts` and `daemon-command-payload-keys.ts` are task-eb37494e's owned paths (risk 7). SO THE TRANSPORT-ROSTER ENTRY IS STRUCTURALLY THE SIBLING'S, NOT WITHHELD BY ME.\n  THE SECURITY PROPERTY STILL HOLDS AT THIS COMMIT, and for a STRONGER reason than exclusion: `WIRED_KINDS` (:97-103) is itself computed by FILTERING `PAYLOAD_KEYS`, which does not contain the kind — so nothing advertises it at all. Not-advertised beats excluded-from-the-advertisement.\n  I did NOT touch `mcp-main.test.ts:227` or `daemon-command-registry-human-approver.test.ts:355`. Both enumerate `OPERATOR_PRINCIPAL_KINDS` less `session.open` BY EXACT LIST; adding the kind there would assert a membership that cannot exist yet and would red. Adding it would have been the \"loosen the assertion\" failure the step forbids, inverted.\n\nWHAT LANDED:\n(1) `agent-spawn-contract.ts:238` — the kind added to `HUMAN_ONLY_STEPS` (typed `ReadonlySet<string>`, so it takes a not-yet-dispatched kind). Reason written at the site in the neighbours' register: how often production is probed is the operator's trade between monitoring cost and alert noise, and an agent able to widen the interval could quietly silence the surface that reports an outage — the damage is the ABSENCE of a signal, the one harm no later gate notices. The comment also states, for the next reader, that this is the ONLY fence the kind can carry at this commit and exactly why.\n(2) `agent-wrapper.test.ts` — THE SPAWN-LAYER ARM, driven through `runOnce()`, copying the shape the file already uses for `environment.set_variable` (fenced there before it was vocabulary, for this same reason) and `preview.decide`: the work-item id `monitoring.set_probe_interval@probe-human-1` must NOT appear in `report.spawned`, PLUS the vacuity control proving the surface really offered the step READY and unclaimed. Added `expect(report.spawned).toEqual([])` with the reasoning that makes it a LAYER claim: the harness's `deps`, `mintSecret` and `spawnAgent` all THROW, and a kind that got past `HUMAN_ONLY_STEPS` and was refused one gate later by `agentCapabilitiesFor` would still be REPORTED (measurably so for `preview.decide`, per that file's own recorded measurement) — so an empty `spawned` is reachable only via the STAFFING fence.\n(3) `mcp-tool-allowlist.test.ts` — THE TRANSPORT-LAYER ARMS, three of them, appended as `describe(\"task-749e585a the probe-interval kind is unreachable over MCP\")`:\n  a. subject-exists control (`RUNTIME_COMMAND_KINDS` contains the kind) + not in `wiredMcpToolKinds()` + not in `allowlistedToolEntries(...)`, with `goal.create` as the surviving control so \"advertises nothing\" cannot green it.\n  b. THE LOCKSTEP ARM, written to red in task-eb37494e's commit if it wires the dispatch without the fence. NOT an `if` — both sides are read from production and compared as an equality (`{excluded, operator} === {wiredForDispatch, wiredForDispatch}`), so it states a real equality today and is never silently vacuous, plus `expect(wiredMcpToolKinds()).not.toContain(kind)` which must hold in BOTH worlds, plus a `deployment.rollback` discriminator reading `true/true/true` so the shape is not satisfiable by `false === false` alone.\n  c. **CODE AND LAYER, MEASURED NOT ASSUMED.** Dispatched the real command through the production `createMcpDispatchPort` under the OPERATOR credential (the strongest principal that seam can present, so the refusal is about the KIND being unserved and not about who asked). Measured by probe, then pinned: `{code: \"INPUT_INVALID\", outcome: \"REFUSED\", stage: \"REGISTRY\"}`, `ok: false`, `httpStatus 400`, `transport.mcpCode -32602`.\n  **THE LAYER IS SPELLED `stage` ON THIS SEAM AND THE FRAME CARRIES NO `refusal.layer` MEMBER** — my first draft read `refusal?.layer` and would have compared `undefined` to `undefined` and passed while assertin",
      "startedAt": "2026-09-07T16:54:42.849Z",
      "completedAt": "2026-09-07T16:58:38.429Z"
    },
    {
      "stepId": "step-5",
      "description": "DRILLS, GATES, SELF-REVIEW. Load `adversarial-self-review` and read your own diff as an attacker first.\nDRILLS — `git status --porcelain` the mutated paths FIRST and record the output. This row creates NO new file, so every path is TRACKED and an empty `git diff -- <path>` in a call SEPARATE from the restore is valid proof; state that no untracked path was mutated, so global rail 13 is answered rather than ignored.\nTHREE DRILLS, each quoted with its failing line:\n(i) **THE FENCE DRILL** (DoD 1): remove the kind from `mcp-tool-allowlist.ts` and confirm the exclusion arm reds ON THE CODE AND THE LAYER. Then restore it and remove it from `agent-spawn-contract.ts` instead, and confirm a **DIFFERENT** arm reds. If one arm covers both removals, one of the two fences is untested and the \"different actors\" reasoning is asserted nowhere — strengthen it rather than reporting a pass.\n(ii) **THE HAND-EDIT DRILL** (DoD 2): hand-edit the generated client instead of regenerating, and confirm the digest pin arm reds. This proves the pin actually guards the generator rather than being copied along with it.\n(iii) **THE COUNT-PIN DRILL** (DoD 2, step 3): revert ONE of the two `apps/control-room` count pins and confirm `pnpm --filter @moe/control-room test` reds while `pnpm --filter @moe/daemon test` and `pnpm typecheck` stay green. That is the measurement behind step 1(c), and it is the evidence that the control-room leg was mandatory rather than precautionary.\nGATES — standalone and foreground, ONE command per call, no pipes and no `;` chains, each quoted with its `Test Files N passed` count line, because an exit-0 leg with NO count line ran nothing and a bad `--filter` prints \"No projects matched the filters\" and EXITS 0 (global rail 8): `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room-client test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`. A bare root `pnpm test` covers no `apps/**` file; say that rather than quoting it. Attribute any red outside this row's thirteen files against your step 1 baseline — six of them are CONTESTED with another row in the precedent's own commit — and disclose foreign red verbatim rather than owning it or calling it green.\nSELF-REVIEW AND RECORD, in completionSummary plus one row comment for detail over the cap: the EXACT kind spelling, so task-078232fb, task-eb37494e and the health-read row all use the same one; the before-and-after contract digest values, proving the regeneration ran; the full re-censused mirror list from step 1(b) WITH A VERDICT PER FILE (edited, or mentions the constant only); which of the two `apps/control-room` count pins moved and what each actually pins; the three drill outcomes with their failing lines; and that DoD 3 and DoD 4 are delivered by task-eb37494e47924b659a0a1a77060b36eb, which dependsOn this row and task-078232fb — this row deliberately advertises and FENCES the kind, and the dispatch registration that serves it lands there. Named verification command: `pnpm --filter @moe/daemon test`.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/contracts/src/runtime/runtime-vocabulary.ts",
        "packages/control-room-client/src/generated/generated-client.ts",
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/orchestrator/agent-spawn-contract.ts",
        "apps/control-room/src/data/data-adapter.test.ts"
      ],
      "modifiedFiles": [
        "packages/contracts/src/runtime/runtime-vocabulary.ts",
        "packages/contracts/src/runtime/runtime-contract.test.ts",
        "packages/control-room-client/src/generated/generated-client.ts",
        "packages/control-room-client/src/generated-coverage.test.ts",
        "apps/control-room/src/data/data-adapter.test.ts",
        "apps/control-room/src/v2/goals/approve-plan.test.tsx",
        "apps/control-room/src/v2/goals/approve-plan-reject.test.tsx",
        "apps/control-room/src/v2/goals/plan-run-resolution.test.tsx",
        "apps/daemon/src/orchestrator/agent-spawn-contract.ts",
        "apps/daemon/src/orchestrator/agent-wrapper.test.ts",
        "apps/daemon/src/mcp-tool-allowlist.test.ts"
      ],
      "note": "DRILLS, GATES, SELF-REVIEW. Full evidence in `comment-3f3ff1a0` (census + mirrors) and `comment-fd2bc1f2` (fence deviation + drills + gates); this note is the summary and the adversarial pass.\n\nKIND SPELLING, ONE, FOR THE WHOLE CHAIN: **`monitoring.set_probe_interval`**. Posted to task-eb37494e as `comment-a803e4b7` and to #general as `msg-55021a1424f8`.\n\nDRILLS — all three ran, each RED on its own arm with its failing line quoted in comment-fd2bc1f2. Headlines: (i-a) removing the `HUMAN_ONLY_STEPS` entry reds the wrapper arm and leaves the transport arms at 29/29 green; (i-b) the transport mutation (PAYLOAD_KEYS + family, no OPERATOR_PRINCIPAL_KINDS — the sibling's likely mistake) reds four transport arms including `expected [ 'design.submit', …(44) ] to not include 'monitoring.set_probe_interval'` and leaves the wrapper's green; (ii) hand-editing the generated client reds BOTH the golden-hash pin and `generator-determinism`; (iii) reverting one apps/control-room mirror reds only the control-room leg while the client leg and typecheck stay green. Two fences, two disjoint arms — the \"different actors\" reasoning is asserted, not just written.\nRAIL 13: all 11 of my paths were TRACKED (` M`) before drilling, ZERO untracked, recorded by `git status --porcelain` first. Restores still proved by SHA256 against an out-of-tree backup in a SEPARATE call because two of the five drill targets are PEER files; all five MATCH, all three peer files `git status` clean, backups purged, no drill residue anywhere in apps+packages.\n\nGATES — `pnpm --filter @moe/daemon test` 590/590 exit 0; `@moe/control-room-client` 10 files/96 tests; `@moe/control-room` 195 files/2517 tests; `pnpm typecheck` 14/14 Done. Baseline delta EMPTY: the merge-base daemon leg was exit 1 with 3 foreign failures (backups, deployments-health, read-dispatch 36-vs-35), all outside my paths, and all cleared by their owners before my final run.\n\nADVERSARIAL PASS, what I actually looked for and what it found:\n- **Vacuity, the one that nearly got me.** My first transport arm read `refusal?.layer` off the frame. The frame carries NO `refusal` member — the layer is spelled `stage` — so the arm would have compared `undefined` to `undefined` and passed while asserting nothing. Caught only by probing the real frame instead of copying the neighbouring `preview.decide` arm's shape. Now pinned as `{code: \"INPUT_INVALID\", outcome: \"REFUSED\", stage: \"REGISTRY\"}` with a `deployment.rollback` control proving `REGISTRY` is a statement about THIS kind and not the seam's answer to everything.\n- **Implication arms going silently vacuous.** The lockstep arm is written as an equality between two production reads (`{excluded, operator}` vs `{wiredForDispatch, wiredForDispatch}`), never an `if`, so it states a real fact today and reds when one side moves; the `deployment.rollback` triple reads `true/true/true` so the shape is not satisfiable by `false === false`. Drill i-b confirms it reds in the exact commit it exists to guard.\n- **Concurrency / inputs / cleanup:** not applicable in substance — the production change is one member added to a frozen `ReadonlySet<string>` and one to a frozen tuple. No IO, no mutable shared state, no error paths, no new module and therefore no `.js` bridge.\n- **Roster collateral, checked before editing rather than discovered by a red:** `work-labels.test.ts` is subset-directional; `mcp-dispatch-port.test.ts:104` and `preview-start-command.test.ts:512` filter to SERVED kinds; `affordance-planning-offers.test.ts` enumerates by name; `bootstrap-durability.test.ts:690` and `v2-surface-manifest.ts:86` consume the roster without a count. None red on this addition, and the 590/590 daemon leg confirms it.\n- **Pre-existing drift repaired in passing, disclosed:** `runtime-contract.test.ts` said \"Literal 119\" against an assertion of `toBe(120)` at 3a056078. Now 121 in both, with a line saying why.\n- **What I would be dinged for, stated rather than hidden:** DoD 1's \"MCP-excluded\" is NOT satisfied as roster membership, because at HEAD it cannot be. It is satisfied as unreachability, asserted against the production allowlist and the production dispatch port, plus a committed arm that reds if the sibling wires the kind unfenced. If a reviewer wants literal membership, it must come with `PAYLOAD_KEYS` — i.e. in task-eb37494e — and I have told that row exactly what to do.\n- **Carry-forward inherited from task-078232fb's handoff, still open and NOT mine to close:** an environment removed from the deploy ledger orphans its interval record and its job would refuse `PROBE_RECEIPT_MISSING` each tick. Unreachable today because no clear/delete surface exists; whoever ships one must drop the orphaned schedule id with it. Noted so it does not die with that handoff.\n\nSCOPE PARTITION, so no reader scores this row against clauses it no longer owns: DoD 3 (handler writes through task-078232fb's store port) and DoD 4 (bidirectional dispatch-seam coverage) belong to **task-eb37494e",
      "startedAt": "2026-09-07T16:58:42.852Z",
      "completedAt": "2026-09-07T17:15:19.581Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-078232fb9b9d4c06a5faf3c02adaf705",
  "dependsOn": [
    "task-078232fb9b9d4c06a5faf3c02adaf705"
  ],
  "priority": "MEDIUM",
  "order": 121.6,
  "comments": [
    {
      "id": "comment-3f3ff1a06e964513a7b4fb386c4185dc",
      "author": "worker-319873d7",
      "content": "EVIDENCE DETAIL for QA — part 1 of 2: THE CENSUS AND THE MIRRORS (DoD 2). completionSummary truncates at 2000 chars, so the load-bearing detail is here.\n\n**CENSUS A — BY CONSTANT NAME.** `git grep -rln GENERATED_CONTRACT_DIGEST -- apps packages adapters tools` at HEAD 3a056078 returns the description's SEVEN files EXACTLY. VERDICT PER FILE, which is what DoD 2's \"re-censused\" asks for:\n| file | verdict |\n| apps/daemon/src/cutover/v2-readiness-evidence-collector.ts | SYMBOL IMPORT ONLY (:9, :67). NO EDIT |\n| apps/daemon/src/cutover/v2-readiness-evidence-producers.test.ts | SYMBOL IMPORT ONLY (:4, :68, :72). NO EDIT |\n| apps/daemon/src/http/affordance-deploy-target-offers.ts | literal appears in a COMMENT only (:23). NO EDIT |\n| packages/control-room-client/generator/generate.ts | COMPUTES the digest; it is the producer. NO EDIT |\n| packages/control-room-client/src/contract-digest.test.ts | DERIVES live and compares (:21). NO LITERAL. NO EDIT |\n| packages/control-room-client/src/contract-pins.ts | bare re-export (:7). NO LITERAL. NO EDIT |\n| packages/control-room-client/src/generated/generated-client.ts | SOURCE OF TRUTH. REGENERATED |\n\n**CENSUS B — BY THE DIGEST VALUE, WHICH IS THE RIGHT NEEDLE.** `git grep -ln 9f578ae0a875d498cce7b9da03252daaa2655f825263c0963b4899971001e1ca` returns SIX source files, FOUR under apps/control-room where the value is spelled `contractSchemaHash:` and never `GENERATED_CONTRACT_DIGEST`. ALL FIVE MIRRORS EDITED (the sixth is the generated file itself):\n  apps/control-room/src/data/data-adapter.test.ts — 1 occurrence\n  apps/control-room/src/v2/goals/approve-plan.test.tsx — 1\n  apps/control-room/src/v2/goals/approve-plan-reject.test.tsx — 1  **NOT NAMED IN THE PLAN**\n  apps/control-room/src/v2/goals/plan-run-resolution.test.tsx — 1  **NOT NAMED IN THE PLAN**\n  packages/control-room-client/src/generated-coverage.test.ts — digest AND `GENERATED_FILE_SHA256`\n\n**SO EPIC RAIL 1 IS RIGHT ON BOTH COUNTS AND THIS ROW'S DESCRIPTION IS THE STALE ONE.** The rail says mirrors live under apps/control-room: TRUE, and there are now FOUR, not two. The description calls the rail stale because the constant-name grep finds none there: that grep is simply the wrong needle. They are digest mirrors, not \"roster count pins\" as steps 1(c) and 3 call them — I checked what each pins before touching it, and none is a count or a kind enumeration; each is a single hash feeding the client's compat gate, so the correct edit is the hash and only the hash. Two of the four appeared after the precedent 4f6bfbf0 landed on 2026-09-06, which is why the plan named two. Taking the two unnamed files is DoD 2's own \"must be re-measured, not trusted\" paying out, not scope creep.\n\nWHAT MOVED, the clause's own failure detector:\n  GENERATED_CONTRACT_DIGEST  9f578ae0a875d498cce7b9da03252daaa2655f825263c0963b4899971001e1ca -> daafa7872670d05bd428a3037b1afea1ce357d83bea8f434623080356d26a025\n  generated-client.ts sha256 668ff85948d946ddf4da8c24c086ca04d0be79ebc303494a51bc980bd0293bca -> 876a7e5b0cde83d214c93e1af4288bd3ae4c5444b4c8cef2f324330eb87d78d5\nBoth MOVED, so the regeneration demonstrably ran. Regenerated via `pnpm --filter @moe/control-room-client generate`; the diff is exactly the three lines the generator emits. CLOSURE PROVED, not assumed: `git grep -ln <old digest>` and `git grep -ln <old file sha>` over `apps packages adapters tools` each return NOTHING.\n\n`generated-coverage.test.ts` needed NO enumeration edit: it already asserts bidirectional set-equality between the generated builders and `RUNTIME_COMMAND_KINDS` (:89 AND :90) with non-empty controls at :87-88, so the kind flowed through from the regeneration. Its `toHaveLength(4)`/`toHaveLength(1)` at :107/:118 size a hand-written REQUIRED-kinds sample, not the roster — correctly untouched.",
      "timestamp": "2026-09-07T17:14:04.763Z"
    },
    {
      "id": "comment-fd2bc1f2e3464712835d14dd6393df23",
      "author": "worker-319873d7",
      "content": "EVIDENCE DETAIL for QA — part 2 of 2: THE FENCE DEVIATION, THE DRILLS, THE GATES.\n\n**READ THIS FIRST: STEP 4(1) IS NOT PERFORMABLE, AND THAT IS A MEASURED FACT ABOUT HEAD, NOT A NARROWED DoD.**\n`mcp-tool-allowlist.ts:73-95` — `MCP_EXCLUDED_COMMAND_KINDS` is `...[...OPERATOR_PRINCIPAL_KINDS].filter(k => !MCP_REACHABLE_OPERATOR_KINDS.has(k)).sort()`. FULLY DERIVED; the file has no hand-editable entry, and its own comment at :74-83 says so deliberately. The upstream roster is `daemon-command-vocabulary.ts:347`, typed `ReadonlySet<WiredCommandKind>` — a union built from `PAYLOAD_KEYS` (:220) — so it cannot name a kind before `PAYLOAD_KEYS` does; and `mcp-tool-allowlist.test.ts:255-259` asserts `|wiredMcpToolKinds()| === |PAYLOAD_KEYS| - |MCP_EXCLUDED| + |QUERIES|`, which reds arithmetically for such a kind. Both `daemon-command-vocabulary.ts` and `daemon-command-payload-keys.ts` are task-eb37494e's owned paths (this row's risk 7). **THE SECURITY PROPERTY HOLDS ANYWAY, FOR A STRONGER REASON THAN EXCLUSION:** `WIRED_KINDS` (:97-103) is itself computed by FILTERING `PAYLOAD_KEYS`, which does not contain the kind, so nothing advertises it at all. Not-advertised beats excluded-from-the-advertisement. I did NOT touch `mcp-main.test.ts:227` or `daemon-command-registry-human-approver.test.ts:355`: both enumerate `OPERATOR_PRINCIPAL_KINDS` less `session.open` by exact list, and adding the kind there would assert a membership that cannot exist yet.\n\n**THE THREE DRILLS.** Window announced in #general (msg-55021a1424f8) at 16:59Z, closed 17:12Z. `full-suite-gate` is held by worker-7b684492/task-1f994315 until 2026-09-08T16:32Z — I did NOT take it and ran NO full suite inside the window; every drill leg was one targeted file run. `git status --porcelain` of all 11 of my paths taken BEFORE drilling: all ` M`, **ZERO untracked**, so global rail 13's `??` hazard does not apply — and I still proved every restore by SHA256 against an out-of-tree backup keyed on this taskId, in a call SEPARATE from the restore, because two of the five drill targets are PEER files.\n\n(i-a) SPAWN FENCE. Removed the kind from `HUMAN_ONLY_STEPS`.\n  RED: `agent-wrapper.test.ts > createAgentWrapper > never staffs the human approval, goal-closure or environment-variable actions` — `AssertionError: expected { active: +0, …(2) } to deeply equal { active: +0, spawned: [], …(1) }`. `Tests 1 failed | 50 passed (51)`.\n  CONTROL, under the SAME mutant: `mcp-tool-allowlist` stayed `Tests 29 passed (29)`. Different actor, different arm.\n(i-b) TRANSPORT FENCE. The plan's \"remove it from mcp-tool-allowlist.ts\" has nothing to remove, so I drilled the mutation that actually opens the transport: added the `PAYLOAD_KEYS` entry plus a `CAPABILITIES.GOAL` family entry and OMITTED `OPERATOR_PRINCIPAL_KINDS` — exactly task-eb37494e's likely mistake. FOUR reds, none of them the wrapper's:\n  `expected { excluded: false, operator: false } to deeply equal { excluded: true, operator: true }` (my lockstep arm)\n  `expected [ 'design.submit', …(44) ] to not include 'monitoring.set_probe_interval'` (the kind became MCP-ADVERTISED — the leak itself)\n  plus the exclusion denominator at :225 and my seam arm. `Tests 4 failed | 25 passed (29)`.\n  FOUND EN ROUTE: a `PAYLOAD_KEYS` entry with no family entry throws at COMPOSITION — `Error: command kind has no capability family: monitoring.set_probe_interval` (`daemon-command-families.ts:190` via `createStoreDependencies`) — so the file fails to LOAD (\"Tests no tests\"). Production fails closed. Passed to the sibling row as a comment.\n  **SO NEITHER ARM COVERS BOTH REMOVALS, WHICH IS WHAT THE STEP DEMANDS.** i-a reds the wrapper and leaves the transport arms green; i-b reds the transport arms and leaves the wrapper's green.\n(ii) HAND-EDIT. Added a builder line to `generated-client.ts` by hand, generator not re-run.\n  RED: `generated-coverage.test.ts > matches the committed golden hash of the generated file` — `AssertionError: expected '1d1c48a25c25a1d347d6f7352d6a507859107…' to be '876a7e5b0cde83d214c93e1af4288bd3ae4c5…'`, AND `generator-determinism.test.ts > matches the committed generated output byte for byte` — `expected false to be true`. `Tests 2 failed | 94 passed (96)`. The pin guards the generator, not merely itself.\n(iii) MIRROR PIN. Reverted ONE apps/control-room mirror (`data-adapter.test.ts`) to the pre-regeneration digest.\n  RED: `pnpm --filter @moe/control-room test` — 16 tests in `src/data/data-adapter.test.ts`, first failing line `Error: compat gate refused the matching report`.\n  CONTROLS under the same mutant: `pnpm --filter @moe/control-room-client test` stayed `Test Files 10 passed (10)` / `Tests 96 passed (96)`, and `pnpm typecheck` stayed green with no `error TS` line. The mutated path is under `apps/control-room/src`, which the daemon leg (`vitest run --root . … src` inside apps/daemon) cannot reach by construction — I state that structurally rather than claiming a daemon run I did not do inside the window. **The control-room leg was MANDATORY, not precautionary.**\nRESTORE PROOF, one call, after all drills: SHA256 MATCH on all five targets — `agent-spawn-contract.ts` 2c906d57…, `generated-client.ts` 876a7e5b…, `data-adapter.test.ts` 7b03c2af…, `daemon-command-payload-keys.ts` e2acc414…, `daemon-command-vocabulary.ts` ee4b94a5…. `git status --porcelain` EMPTY for all three peer files. Backups purged. `git grep` for `MEASURED_CODE|hand_edited_kind|toBe(\"PROBE\")` across apps+packages: NO DRILL RESIDUE.\n\n**GATES, standalone and foreground, one command per call, no pipes into the exit code:**\n  `pnpm --filter @moe/daemon test` — exit 0, `Test Files  590 passed (590)` / `Tests  10524 passed | 8 skipped (10532)`\n  `pnpm --filter @moe/control-room-client test` — exit 0, `Test Files  10 passed (10)` / `Tests  96 passed (96)`\n  `pnpm --filter @moe/control-room test` — exit 0, `Test Files  195 passed (195)` / `Tests  2517 passed (2517)`\n  `pnpm typecheck` — exit 0, all 14 packages `Done`, including `apps/daemon typecheck: Done` and `apps/control-room typecheck: Done`\nA bare root `pnpm test` covers NO `apps/**` file and is not quoted here.\n\n**BASELINE ATTRIBUTION (rail: (2) minus (1), intersected with owned paths) — THE DELTA IS EMPTY.** At the step 1 merge-base the daemon leg was EXIT 1: `Test Files 3 failed | 585 passed (588)` / `Tests 4 failed`, in `src/backups/backup-restore-proof.test.ts` (task-1f994315), `src/daemon-entry-deployments-health.test.ts` (task-620fb461) and `src/http/http-listener-read-dispatch.test.ts:356` (`expected 36 to be 35`). All three were foreign, all outside my 11 paths. At HEAD-with-my-diff the leg is FULLY GREEN at 590/590 — those owners landed their fixes while I worked. So the delta introduced no red and in fact the disclosed foreign red has cleared; nothing is being excused here.",
      "timestamp": "2026-09-07T17:14:44.743Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-07T13:14:00.424Z",
  "updatedAt": "2026-09-07T17:16:50.125Z",
  "metrics": {
    "firstClaimAt": "2026-09-07T13:36:12.112Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 13,
    "executedStepCount": 5
  },
  "planSizeWarnings": [
    "Plan touches 13 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-07T13:42:49.962Z",
  "planningNotes": {
    "approachesConsidered": "**I SUBMITTED THIS ROW'S FULL SCOPE FIRST AND THE DAEMON HARD-REJECTED IT AT 22 DISTINCT FILES (cap 20).** So it is split, and the split is along the seam the board itself used before: task-b3888068 (DONE) landed the environment kinds on the shared roster and the generated client; task-a2409cba (DONE) published them operator-only. The new sibling is task-eb37494e47924b659a0a1a77060b36eb, PLANNING, dependsOn [this row, task-078232fb].\n\n**THE CUT IS NOT WHERE I FIRST WANTED IT, AND THE REASON MATTERS.** My first instinct was surface-then-handler: roster plus client here, fence plus dispatch plus handler there. I rejected it. A kind advertised on the shared runtime roster with no fence is reachable by an agent for as long as the second row takes to land, and \"advertise now, fence later\" is a security ordering, not a paperwork one. **SO THE FENCE LANDS WITH THE ADVERTISEMENT** — both rosters, both arms, in this row — and the DISPATCH REGISTRATION plus the HANDLER go to the sibling. That also puts the dependency where it belongs: the handler needs task-078232fb's store port, and this row needs nothing from it, so this row is ungated and can land immediately while the port row is still in planning.\n\nDoD 3 and DoD 4 therefore move to task-eb37494e with their own DoD text. This row keeps DoD 1 (roster AND fence), DoD 2 and DoD 5. Step 5 makes the worker state that partition in the completion record so QA does not read two clauses as silently dropped.\n\n**THE CORRECTION I MOST WANT CARRIED FORWARD**, because the epic rail and this row's own description are each half right and the wrong conclusion is one grep away: the rail says \"two mirrors live under apps/control-room\"; the full-tree digest grep finds NONE there, which is what the description flags as stale. But `git show --stat 4f6bfbf0` — the landed precedent for this exact operation — touched two `apps/control-room` files: `src/data/data-adapter.test.ts` and `src/v2/goals/approve-plan.test.tsx`. They are ROSTER COUNT PINS, not digest mirrors, so the grep cannot see them, and nothing but `pnpm --filter @moe/control-room test` runs them. **The rail's conclusion is right; its reason is wrong.** A worker who trusted \"the rail is stale\" and skipped that leg would ship two red files. Step 1(c) states it, step 3 does it, and drill (iii) proves it by reverting one pin and showing only the control-room leg reds.\n\nREJECTED — merging steps to fit under the cap. The rejection message forbids it explicitly and it would not have helped: the cap counts files, not steps.",
    "codebaseInsights": "MEASURED BY ME AT HEAD 4710d60b, 2026-09-07.\n\n1. **THE ROSTER**: `packages/contracts/src/runtime/runtime-vocabulary.ts:85+`, `RUNTIME_COMMAND_KINDS`, ALPHABETICAL. Deployment kinds at :91-92; `environment.set_variable`/`unset_variable` at :94.\n2. **THE DIGEST CENSUS** — `git grep -rln GENERATED_CONTRACT_DIGEST -- apps packages adapters tools` returns SEVEN source files, NONE under `apps/control-room`: `apps/daemon/src/cutover/v2-readiness-evidence-collector.ts`, `apps/daemon/src/cutover/v2-readiness-evidence-producers.test.ts`, `apps/daemon/src/http/affordance-deploy-target-offers.ts`, `packages/control-room-client/generator/generate.ts`, `packages/control-room-client/src/contract-digest.test.ts`, `packages/control-room-client/src/contract-pins.ts`, `packages/control-room-client/src/generated/generated-client.ts`. The row description's list is confirmed exactly.\n3. **THE PRECEDENT AND THE COUNT PINS THE GREP MISSES** — `git show --stat 4f6bfbf0` (task-b3888068, adding two kinds to this roster) touched SIX source files: `apps/control-room/src/data/data-adapter.test.ts`, `apps/control-room/src/v2/goals/approve-plan.test.tsx`, `packages/contracts/src/runtime/runtime-contract.test.ts`, `packages/contracts/src/runtime/runtime-vocabulary.ts`, `packages/control-room-client/src/generated-coverage.test.ts`, `packages/control-room-client/src/generated/generated-client.ts`. **Two under apps/control-room, neither carrying the digest literal.** That commit lists all six as CONTESTED with task-37a50989 — expect peers.\n4. **THE FENCE IS TWO ROSTERS**, and the source argues it better than a plan can. `apps/daemon/src/orchestrator/agent-spawn-contract.ts:214-220`, of `environment.set_variable`: \"the entry here is belt-and-braces beside the MCP exclusion in `mcp-tool-allowlist.ts`, and both are wanted: that one stops an agent reaching the kind over a transport, this one stops the wrapper minting a session to take it.\" `project.set_agent_provider` sits at :223 with the same reasoning. The arms are EXACT LISTS: `mcp-tool-allowlist.test.ts:231`, `mcp-main.test.ts:227`, `daemon-command-registry-human-approver.test.ts:355`.\n5. **THE COST OF ONE KIND**: `git grep -ln 'project.set_agent_provider' -- 'apps/**' 'packages/**' ':!*.js'` = 25 files for a recent single kind wired end to end. That measurement is what predicted the 22-file rejection here.\n6. **WHAT MOVED TO task-eb37494e** (measured, so that row is not planned blind): `daemon-command-vocabulary.ts:73` and its family roster near :392; `daemon-command-payload-keys.ts:181` (set_target's exact keys are `[\"environment\",\"network\",\"sshTarget\",\"url\"]`); `daemon-command-registry.ts` plus the exact-list arms at `daemon-command-registry.test.ts:136,390,412` and `daemon-command-vocabulary.test.ts:310`; `daemon-store-dependencies.test.ts`; the handler and its `.js` bridge; and the bidirectional coverage arm, whose shape to copy is `apps/daemon/src/http/affordance-read.test.ts:521-566` (served side read off the PRODUCTION dispatch table, sorted arrays compared BOTH ways, with a control line proving the filter still discriminates).\n7. **THE PORT DOES NOT EXIST YET**: task-078232fb9b9d4c06a5faf3c02adaf705 is PLANNING with ZERO steps. It is task-eb37494e's dependsOn, not this row's — this row needs nothing from it, which is why the split leaves this half ungated.",
    "risks": "1. **DO NOT CONCLUDE \"NO CONTROL-ROOM WORK\" FROM THE DIGEST GREP.** Two `apps/control-room` roster COUNT PINS break on a roster addition, are invisible to `pnpm test`, `pnpm typecheck` and the daemon leg, and only `pnpm --filter @moe/control-room test` runs them. This is the single easiest way to ship this row red, and drill (iii) is written to prove the leg was necessary.\n2. **REGENERATE THE CLIENT, NEVER HAND-EDIT IT** (DoD 2). `generated-client.ts` is itself a digest carrier; a hand edit desynchronises the digest from the roster it pins. **A digest that did not MOVE after a roster addition means the regeneration did not run** — quote before and after, or the clause is unevidenced.\n3. **TWO FENCES, TWO ARMS.** If removing the kind from either roster reds the SAME arm, one fence is untested. Drill (i) removes each separately and requires DIFFERENT arms to red.\n4. **ASSERT CODE AND LAYER ON THE FENCE REFUSAL.** Both fences can refuse; an arm that checks only a code cannot tell which one answered and stays green when the wrong one starts answering first (global rail 1).\n5. **ONE SPELLING FOR THE KIND, ACROSS FOUR ROWS.** task-078232fb (store port), task-eb37494e (dispatch and handler) and the health-read row all name it. Record the exact string in the completion summary; a second spelling is how a chain like this drifts.\n6. **DoD 3 AND DoD 4 ARE NOT DROPPED, THEY MOVED** to task-eb37494e47924b659a0a1a77060b36eb. Say so in the completion record or a reader will score this row against clauses it no longer owns.\n7. **DO NOT TOUCH THE DISPATCH REGISTRATION OR THE HANDLER** from this row — `daemon-command-vocabulary.ts`, `daemon-command-payload-keys.ts`, `daemon-command-registry.ts` and their arms belong to task-eb37494e, which will edit them in a shared worktree.\n8. **SIX OF THIS ROW'S THIRTEEN FILES WERE CONTESTED** in the precedent's own commit. Baseline both legs in step 1 and attribute foreign red rather than owning it.\n9. The kind is advertised here and SERVED by the sibling. Between the two landing, a dispatch of the kind refuses as unknown — that is the correct, fenced, fail-closed state, and it is why the fence lands here rather than there."
  },
  "planApprovedAt": "2026-09-07T13:42:52.104Z",
  "contextFetchedBy": [
    "worker-319873d7"
  ],
  "workStartedAt": "2026-09-07T16:34:49.962Z",
  "reviewStartedAt": "2026-09-07T17:16:50.125Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  590 passed (590)\n      Tests  10524 passed | 8 skipped (10532)\n   Start at  20:03:35\n   Duration  550.13s (transform 24.29s, setup 0ms, import 314.82s, tests 1767.21s, environment 30ms)",
    "reportedAt": "2026-09-07T17:16:50.125Z"
  },
  "filesModified": [
    "packages/contracts/src/runtime/runtime-vocabulary.ts",
    "packages/contracts/src/runtime/runtime-contract.test.ts",
    "packages/control-room-client/src/generated/generated-client.ts",
    "packages/control-room-client/src/generated-coverage.test.ts",
    "apps/control-room/src/data/data-adapter.test.ts",
    "apps/control-room/src/v2/goals/approve-plan.test.tsx",
    "apps/control-room/src/v2/goals/approve-plan-reject.test.tsx",
    "apps/control-room/src/v2/goals/plan-run-resolution.test.tsx",
    "apps/daemon/src/orchestrator/agent-spawn-contract.ts",
    "apps/daemon/src/orchestrator/agent-wrapper.test.ts",
    "apps/daemon/src/mcp-tool-allowlist.test.ts"
  ],
  "completionSummary": "KIND SPELLING FOR THE WHOLE CHAIN: `monitoring.set_probe_interval`. NOT `deployment.*` — forced, not preferred: `daemon-store-dependencies.test.ts:1155-1168` asserts set-equality between `deploymentMembers(PAYLOAD_KEYS)` and each of the three fence rosters, so a `deployment.*` kind cannot be FENCED before its PAYLOAD_KEYS entry exists, and the epic rail requires the fence to land with the advertisement. Matches task-078232fb's job ids `monitoring/health-probes/<env>`.\n\nDELIVERED (11 files, all tracked, none new, no `.js` bridge needed):\n- Roster: `runtime-vocabulary.ts:102` + `runtime-contract.test.ts` (EXPECTED_COMMAND_KINDS, size 120->121, a named `toContain`). Sortedness verified against the production tuple: 121 members, 121 unique, index 60.\n- Client REGENERATED, not hand-edited (`pnpm --filter @moe/control-room-client generate`). Digest `9f578ae0…1e1ca` -> `daafa787…6a025`; file sha256 `668ff859…293bca` -> `876a7e5b…d78d5`. **Both moved, so the regeneration demonstrably ran.**\n- ALL FIVE digest mirrors updated; closure proved by `git grep` of the old values returning NOTHING.\n- Human-only SPAWN fence at `agent-spawn-contract.ts:238` with the reason at the site, plus arms in `agent-wrapper.test.ts` (through `runOnce()`, the production consumer) and three in `mcp-tool-allowlist.test.ts`.\n\nTWO DEVIATIONS, both measured, neither hidden. Full detail in `comment-3f3ff1a0` and `comment-fd2bc1f2`.\n1. **STEP 4(1) IS NOT PERFORMABLE.** `MCP_EXCLUDED_COMMAND_KINDS` is DERIVED from `OPERATOR_PRINCIPAL_KINDS`, which is typed `ReadonlySet<WiredCommandKind>` off PAYLOAD_KEYS — the sibling's file. So the kind cannot be MCP-fenced before it is MCP-dispatchable. Delivered the property as UNREACHABILITY (nothing advertises it: `WIRED_KINDS` filters PAYLOAD_KEYS) asserted against the production allowlist AND the production dispatch port — `INPUT_INVALID @ stage REGISTRY` under the OPERATOR credential, with a `deployment.rollback` control proving REGISTRY is not the seam's answer "
}