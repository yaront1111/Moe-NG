{
  "id": "task-166d375f8ce9492aaf50587233545992",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "project.set_agent_provider is MCP-excluded and human-only, with bidirectional fence tests",
  "description": "SPIDR grandchild (fence) of task-8e793ae3c63f41189bcc05f6d7c15605 (architect-2e480458, 2026-09-05), INTERFACE axis, same cut as task-20b7d6bb for release.decide. dependsOn grandchild B (set after B is filed) plus task-5f883e4e (preview.decide fence, WORKING) and task-20b7d6bb (release.decide fence, BLOCKED) because they edit the SAME two roster files and the SAME MCP count pins.\n\nWHAT THIS ROW OWNS: MCP_EXCLUDED_COMMAND_KINDS, HUMAN_ONLY_STEPS, bidirectional set-equality, the MCP unreachable arm, the extra MCP count pins (mcp-tool-allowlist.test.ts; census mcp-main.test.ts / mcp-http-host.test.ts / daemon-command-registry-human-approver.test.ts at claim and name every pin this commit must move).\n\nNOT IN SCOPE: family table, PAYLOAD_KEYS, store, spawn, UI. The kind must already be SERVED by the registry (grandchild B) before this row adds it to the advertised exclusion list — otherwise set-equality reds on an advertised-but-unserved entry.\n\nVERIFY: `pnpm --filter @moe/daemon test`.",
  "definitionOfDone": [
    "`project.set_agent_provider` is a member of MCP_EXCLUDED_COMMAND_KINDS and of HUMAN_ONLY_STEPS. mcp-tool-allowlist.test.ts count pins are reconciled to the printed expected-vs-received in the same commit.",
    "A bidirectional set-equality test enumerates the served set from the dispatch seam and asserts set equality with the advertised roster in both directions. Verify by deleting the entry from each side in turn and confirming both go RED; quote both reds.",
    "A second arm proves the kind is unreachable over MCP: it hits the port's generic INPUT_INVALID refusal, matching the existing behavioural binding mcp-tool-allowlist.test.ts already holds for the other excluded kinds. Assert CODE AND LAYER.",
    "Mutation drill, quoted: (a) remove the kind from MCP_EXCLUDED_COMMAND_KINDS → the set-equality test goes RED; (b) remove it from HUMAN_ONLY_STEPS → the staffing arm goes RED. Restore proof per global rail 13 (untracked paths: SHA256 vs %TEMP%, not git diff).",
    "Gates quoted with `Test Files N passed`: `pnpm --filter @moe/daemon test`, `pnpm typecheck`."
  ],
  "taskRails": [
    "HUMAN_ONLY_STEPS is ONE Set at orchestrator/agent-spawn-contract.ts:195 (re-exported by agent-wrapper.ts:24). Do not look for a second.",
    "Bidirectional set-equality: enumerate the SERVED set from the dispatch seam (registry.keys() for commands, servedMcpQueryKinds() for queries) and assert SET EQUALITY with the advertised roster in BOTH directions. A test that iterates MCP_EXCLUDED_COMMAND_KINDS alone sees only one direction.",
    "Do not edit daemon-command-registry.ts or daemon-command-vocabulary.ts (grandchild B). Do not add a stub kind to make the fence green.",
    "No count literal is trustworthy. Reconcile mcp-tool-allowlist.test.ts length and derived arithmetic to printed expected-vs-received. Concurrent movers: task-5f883e4e (preview fence) and task-20b7d6bb (release fence).",
    "FEATURES ONLY."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE AND BASELINE. NO EDITS. (a) CONFIRM THE PREREQUISITE LANDED, at the tree not the board. This row's ordering constraint is REAL and mechanical, not paperwork: `mcp-tool-allowlist.test.ts` arm C2 (\"keeps every excluded kind SERVED by the registry\") asserts `registry.keys()` contains EVERY member of MCP_EXCLUDED_COMMAND_KINDS, and the \"excludes kinds that are REAL vocabulary keys\" arm asserts `Object.keys(PAYLOAD_KEYS).includes(kind)`. So adding `project.set_agent_provider` to the exclusion roster BEFORE task-c422b106 serves it reds two arms immediately. Prove it landed: `git grep -n 'set_agent_provider' apps/daemon/src/daemon-command-vocabulary.ts` must hit PAYLOAD_KEYS. If it does not, moe.report_blocked with blockedOnTaskIds ['task-c422b106af2945b1a1ad4ed49556bf66'] and stop. Read that row's completionSummary from get_context.epicSiblings for the exact kind string rather than retyping it from this description. (b) NOTE THE ASYMMETRY, because it is not obvious and it explains the two halves of this row: HUMAN_ONLY_STEPS is a bare `ReadonlySet<string>` whose only asserting arms (affordance-planning-offers.test.ts:360 and :427) iterate offered `approval.*` kinds, so it TOLERATES a kind the registry does not serve - `environment.set_variable` and `environment.unset_variable` are in it today while absent from daemon-command-vocabulary.ts (`grep -c` = 0). MCP_EXCLUDED_COMMAND_KINDS does NOT tolerate that. Same commit, different ordering requirements. (c) CENSUS THE COUNT PINS YOURSELF and write the list in the step note - the number to move is 6 -> 7 and it appears in FOUR test files: mcp-tool-allowlist.test.ts (:142 `toBe(6)`, and the four-key object literal `{excluded: 6, queries: 6, vocabulary: 46, wired: 46}`), daemon-command-registry-human-approver.test.ts (:318-324, a HAND-WRITTEN `expectedExclusions` roster plus TWO `toHaveLength(6)`), mcp-main.test.ts (:207 a second HAND-WRITTEN `EXPECTED_EXCLUDED_COMMAND_KINDS` roster, :228 `EXCLUSION_CASES.length).toBe(6)`, and :230 the DERIVED `EXCLUSION_CASES.length * MCP_TRANSPORT_ENTRY_COUNT).toBe(12)`), mcp-http/mcp-http-host.test.ts (:688 and :719, two `EXCLUSION_CASES.length).toBe(6)`). Run `git grep -n 'toBe(6)\\|toHaveLength(6)\\|toBe(12)' -- apps/daemon/src` and reconcile against that list; a pin the list misses is a stop-and-report, not an extra file. (d) BASELINE (global rail 3), standalone foreground, quoting each count line: `pnpm --filter @moe/daemon test`, `pnpm typecheck`. Foreign red here is pre-existing - quote it verbatim so step 4 can subtract it. (e) `git status --porcelain -- apps/daemon/src` and note peer dirt; never revert or stash it.",
      "status": "PENDING",
      "affectedFiles": []
    },
    {
      "stepId": "step-2",
      "description": "THE TWO ROSTER ENTRIES. Production files only, both small. (1) apps/daemon/src/mcp-tool-allowlist.ts (98 lines): add the kind to MCP_EXCLUDED_COMMAND_KINDS at :59-82. Every existing member carries a why-comment in the file's voice explaining WHY that kind must be unreachable over MCP; write one. The standing contract is stated in the file header and repeated per member: `daemon-command-registry.ts` mints the human-review witness on operator PRINCIPAL identity alone, and the MCP port authenticates with the operator bootstrap credential - so an MCP caller would arrive AS the operator and be indistinguishable from a browser one. For this kind the reason is its own: choosing which agent provider staffs a project decides WHICH VENDOR receives the operator's source and credentials, which is the operator's decision about their own project, not staffable work. Do NOT restate the generic paragraph - name what this kind specifically hands over. (2) apps/daemon/src/orchestrator/agent-spawn-contract.ts (229 lines): add the kind to HUMAN_ONLY_STEPS, whose literal runs :195-216 and currently holds EIGHT members. Follow the `environment.*` precedent at :210-216: those two carry a comment saying the entry is belt-and-braces BESIDE the MCP exclusion, and that both are wanted - one stops an agent reaching the kind over a transport, the other stops the wrapper minting a session to take it. The same is true here; say so in one sentence rather than repeating theirs. Do NOT edit daemon-command-registry.ts or daemon-command-vocabulary.ts - task-c422b106 owns those and a second row touching them is a collision in the shared checkout.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/orchestrator/agent-spawn-contract.ts"
      ]
    },
    {
      "stepId": "step-3",
      "description": "RECONCILE THE COUNT PINS. Four test files, no new test logic - and that is the point of this step, so read this before writing an assertion. DoD-2 AND DoD-3 DESCRIBE TESTS THAT ALREADY EXIST (rail 19, gap-claimed-present-actually-closed): mcp-tool-allowlist.test.ts already holds the bidirectional set-equality arms - C1 compares `[...provider.provide().registry.keys()]` (the dispatch seam) against the advertised roster with BOTH directions spelled out, C2 pins every excluded kind as still SERVED, Q1 does the query half - and mcp-main.test.ts STDIO-1 plus mcp-http-host.test.ts already sweep `MCP_EXCLUDED_COMMAND_KINDS` to prove each member is unreachable over its transport and hits the generic refusal. Those sweeps are `.map` over the roster, so ADDING THE MEMBER GENERATES ITS CASES AUTOMATICALLY. Your job is to move the pins that guard the sweep's size, not to write a parallel test. Writing a new arm where a sweep already covers the kind is duplicate coverage a reviewer will reject. (1) mcp-tool-allowlist.test.ts: `:142 expect(MCP_EXCLUDED_COMMAND_KINDS.length).toBe(6)` -> 7. Then the four-key object literal `{excluded: 6, queries: 6, vocabulary: 46, wired: 46}`: `excluded` -> 7 and `vocabulary` -> whatever PAYLOAD_KEYS now holds, but DO NOT assume `wired` moves - it is `vocabulary - excluded + queries`, and this row adds one to BOTH sides, so wired is UNCHANGED by this diff. Take every number from the PRINTED expected-vs-received, never from arithmetic you did in your head, because task-40e52fa8 / task-54b74b80 / task-c422b106 are all moving `vocabulary` concurrently. The prose comment above that literal is already stale (it narrates `45 - 5 + 5 = 45` while the literals say 46/6/6/46); update it to match what you leave behind or delete the arithmetic sentence rather than adding a third stale version. (2) daemon-command-registry-human-approver.test.ts:318-324: the HAND-WRITTEN `expectedExclusions` array gains the kind, and BOTH `toHaveLength(6)` become 7. That array is an independent transcription on purpose - do not derive it from the production roster, or the arm becomes a tautology. (3) mcp-main.test.ts: `EXPECTED_EXCLUDED_COMMAND_KINDS` at :207 gains the kind (a SECOND independent transcription, same reason); `:228 EXCLUSION_CASES.length).toBe(6)` -> 7; and `:230 expect(EXCLUSION_CASES.length * MCP_TRANSPORT_ENTRY_COUNT).toBe(12)` -> 14. THAT DERIVED ONE IS THE EASY MISS - it reads as an unrelated fact and is the only pin whose value is not the number you are changing. (4) mcp-http/mcp-http-host.test.ts:688 and :719, both `EXCLUSION_CASES.length).toBe(6)` -> 7. NOTE :623 pins `it(` count at 15 with the comment \"fourteen behaviours + this counter\" - it must NOT move, because you are adding no `it`. If you find yourself moving it, you added a test the sweep already covers: stop and reconsider.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/daemon-command-registry-human-approver.test.ts",
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-http/mcp-http-host.test.ts"
      ]
    },
    {
      "stepId": "step-4",
      "description": "DRILLS, GATES, SELF-REVIEW. (a) DRILLS, recorded in the step note. `git status --porcelain` the exact mutated paths FIRST. ALL SIX FILES IN THIS PLAN ARE TRACKED, so an empty `git diff -- <path>` is the correct restore proof for each, in a call SEPARATE from the restore. DoD-4's parenthetical \"(untracked paths: SHA256 vs %TEMP%, not git diff)\" does not apply to this row - state that explicitly with the `git status` output backing it, rather than performing a SHA256 ritual on tracked files, which proves nothing and reads as evidence. Global rail 13 fires on NEW modules; this row creates none. THREE DRILLS, restore and prove after each: (i) DoD-4(a) - remove the kind from MCP_EXCLUDED_COMMAND_KINDS only, leaving every count pin at 7. Multiple arms must red; quote the C1 set-equality failure specifically, showing the advertised-vs-expected difference, not merely a count mismatch. (ii) DoD-2's second direction, which is the arm most likely to be vacuous and the one the DoD asks you to prove BOTH ways - with the roster intact, make the kind unserved (the cheapest honest way is to filter it out of the `served` list inside the C1 arm's own fixture, NOT by editing daemon-command-vocabulary.ts, which this row does not own). C2 must red naming the kind as advertised-but-unserved. Quote it. (iii) DoD-4(b) - remove the kind from HUMAN_ONLY_STEPS only. WARNING, AND THIS IS THE ONE TO GET RIGHT: the two existing HUMAN_ONLY_STEPS arms (affordance-planning-offers.test.ts:360 and :427) iterate offered `approval.*` kinds and a fixed two-kind compiler list, so NEITHER SEES THIS KIND - drill (iii) may well stay GREEN with the entry deleted. If it does, say so plainly: it means HUMAN_ONLY_STEPS membership is UNGUARDED for this kind, and DoD-4(b) is unsatisfiable as written. Do NOT invent a passing drill to satisfy the sentence. The correct output is a short arm asserting the kind is in HUMAN_ONLY_STEPS (in mcp-tool-allowlist.test.ts, beside the exclusion pins, since this row already owns that file) plus a note that the DoD's \"staffing arm\" does not exist - then drill THAT arm. (b) GATES - standalone foreground, one command per call, no pipes and no `;` chains, quoting the `Test Files N passed` count line for each; an exit-0 leg with NO count line ran nothing: `pnpm --filter @moe/daemon test` and `pnpm typecheck`. Root `pnpm test` is NOT evidence here and must not be quoted as such - vitest.config.ts:32 includes no `apps/**`, so it runs zero tests against all six files. Attribute any red by failing paths at HEAD minus the step-1 baseline, intersected with this row's six owned paths; that intersection must be EMPTY. Disclose foreign red verbatim. Named complete_task verification command: `pnpm --filter @moe/daemon test`. (c) ADVERSARIAL PASS on this diff only: is the kind spelled IDENTICALLY in all four hand-written places and in the production roster (the allowlist test's misspelling arm catches a typo in the production roster, but a typo in one of the two independent transcriptions reds as a set difference - read the failure text, do not just re-run); did `wired` stay unchanged as predicted, and if it moved, WHY; did a concurrent row move `vocabulary` between your edit and your final gate (re-read the printed value immediately before completing); does mcp-http-host.test.ts:623's `it(` count still read 15; did you touch daemon-command-vocabulary.ts or daemon-command-registry.ts by reflex. (d) COMPLETION RECORD must state: that the bidirectional set-equality arms and the per-transport unreachable sweeps ALREADY EXISTED and this row moved their size pins rather than writing new ones - naming C1, C2, Q1, STDIO-1 and the http sweep - so a QA reading DoD-2 and DoD-3 does not look for new tests that should not exist; the full list of pins moved with their file:line; whether drill (iii) went red or green and what you did about it; and that the registry wiring is task-c422b106's, verified by grep at the delivered tree rather than re-implemented here.",
      "status": "PENDING",
      "affectedFiles": []
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-8e793ae3c63f41189bcc05f6d7c15605",
  "dependsOn": [
    "task-c422b106af2945b1a1ad4ed49556bf66",
    "task-5f883e4e12c34842b3cb6c2f218e4256",
    "task-20b7d6bba60f445faa87a51a09d4b125"
  ],
  "priority": "HIGH",
  "order": 71.25,
  "comments": [
    {
      "id": "comment-dfc8cae7ce0648199cab84dce23cb636",
      "author": "architect-b63fabf3",
      "content": "TWO DoD CLAUSES DESCRIBE TESTS THAT ALREADY EXIST, AND ONE MAY BE UNSATISFIABLE. architect-b63fabf3, 2026-09-05, measured at HEAD f3b94eaa. Plan approved: 4 steps, 6 files, no split.\n\n## DoD-2 and DoD-3 are already built (rail 19: gap claimed present, actually closed)\n\nDoD-2 asks for \"a bidirectional set-equality test [that] enumerates the served set from the dispatch seam\". It exists — `mcp-tool-allowlist.test.ts` arm **C1** compares `[...provider.provide().registry.keys()]` against the advertised roster with both directions spelled out, **C2** pins every excluded kind as still SERVED, **Q1** does the query half.\n\nDoD-3 asks for \"a second arm [proving] the kind is unreachable over MCP\". Also exists — `mcp-main.test.ts` STDIO-1 and `mcp-http-host.test.ts` both sweep `MCP_EXCLUDED_COMMAND_KINDS` per transport. **Those sweeps are `.map` over the roster, so adding the member generates its cases automatically.**\n\nSo this row's real work is the count pins, and the plan forbids writing a parallel arm. A worker reading the DoD literally would have duplicated five existing tests. QA: do not look for new tests here.\n\n## The ordering constraint is mechanical, not paperwork\n\nC2 iterates `MCP_EXCLUDED_COMMAND_KINDS` asserting each is in `registry.keys()`, and a sibling arm asserts each is a real `PAYLOAD_KEYS` key. Adding the kind to the exclusion roster before task-c422b106 serves it reds **both**. That is why the dependency exists, and step 1 makes the worker prove it landed by grep before touching anything.\n\n## An asymmetry that is documented nowhere, and it breaks DoD-4(b)\n\n`HUMAN_ONLY_STEPS` (agent-spawn-contract.ts:195-216, eight members) is a bare `ReadonlySet<string>`. Its only asserting arms — affordance-planning-offers.test.ts:360 and :427 — iterate offered `approval.*` kinds and a fixed two-kind compiler list. **Neither enumerates the set.** So it tolerates a kind the registry does not serve, and today it carries `environment.set_variable` / `environment.unset_variable` while `grep -c environment.set_variable daemon-command-vocabulary.ts` = **0**.\n\nConsequence: **DoD-4(b) — \"remove it from HUMAN_ONLY_STEPS → the staffing arm goes RED\" — will likely stay GREEN**, because no arm guards this kind's membership. There is no \"staffing arm\" for it to red.\n\nThe plan tells the worker to report that plainly, add a short membership arm in a file this row already owns, and drill *that* — rather than fake a red or quietly drop the clause. Flagging it here so it is not read as a worker failing the DoD.\n\n## The pin census is wider than the DoD implies: 6 → 7 in FOUR test files\n\n- `mcp-tool-allowlist.test.ts:142` `toBe(6)`, plus the witness object `{excluded: 6, queries: 6, vocabulary: 46, wired: 46}`\n- `daemon-command-registry-human-approver.test.ts:318-324` — a hand-written roster **plus two** `toHaveLength(6)`\n- `mcp-main.test.ts:207` — a *second* independent hand-written roster; `:228` `toBe(6)`; **`:230` `EXCLUSION_CASES.length * MCP_TRANSPORT_ENTRY_COUNT).toBe(12)` → 14**\n- `mcp-http-host.test.ts:688` and `:719`\n\nThat derived `12 → 14` is the likeliest omission in the row: it sits two lines from a pin that *is* the number being changed, and reads as an unrelated fact.\n\n**`wired` does not move.** It is `vocabulary − excluded + queries`; this row adds one to `excluded` while task-c422b106 adds one to `vocabulary`. Predicting it lets the worker notice if it moves anyway. The prose above that object is *already* stale — it narrates \"45 − 5 + 5 = 45\" against literals reading 46/6/6/46.\n\n## One correction to DoD-4's restore clause\n\nIt says \"(untracked paths: SHA256 vs %TEMP%, not git diff)\". All six files here are tracked and this row creates no new module, so `git diff` **is** the correct proof. Global rail 13 fires on new modules. The plan has the worker say so with `git status` output rather than perform a SHA256 ritual on tracked files, which proves nothing and reads as evidence.",
      "timestamp": "2026-09-05T16:53:44.412Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T14:53:41.137Z",
  "updatedAt": "2026-09-05T16:53:44.412Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T16:48:57.796Z",
    "plannedStepCount": 4,
    "plannedDistinctFileCount": 6
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-05T16:53:20.871Z",
  "planningNotes": {
    "approachesConsidered": "NO SPLIT. The row measures at SIX distinct files against the 10-file cap: two small production rosters plus four test files carrying the count pins. It is one coherent change - a kind joins two fences and every pin guarding those fences moves in the same commit - and splitting it would ship a branch where the roster and its denominators disagree.\n\nTHE ONE REAL DECISION WAS WHETHER THIS ROW WRITES TESTS OR MOVES PINS, and the answer is measured: DoD-2 and DoD-3 both describe tests THAT ALREADY EXIST (rail 19, gap-claimed-present-actually-closed). mcp-tool-allowlist.test.ts already holds C1 (served-from-the-dispatch-seam vs advertised, both directions spelled out), C2 (every excluded kind still SERVED) and Q1 (the query half); mcp-main.test.ts STDIO-1 and mcp-http-host.test.ts already sweep the roster for per-transport unreachability. Those sweeps are `.map` over MCP_EXCLUDED_COMMAND_KINDS, so adding the member generates its cases automatically. The plan therefore forbids writing a parallel arm and spends its effort on the pins instead. An architect who read the DoD literally would have had the worker duplicate five existing arms.\nREJECTED, deriving either hand-written exclusion roster from the production constant: daemon-command-registry-human-approver.test.ts:318 and mcp-main.test.ts:207 are INDEPENDENT transcriptions on purpose - deriving them makes the comparison a tautology.\nREJECTED, adding a new refusal code or a new `*_LAYER`: the existing generic INPUT_INVALID refusal is what every other excluded kind hits, and a new layer constant would demand a boundary-roster backfill for no gain.\nREJECTED, touching daemon-command-vocabulary.ts or daemon-command-registry.ts to make the fence green: task-c422b106 owns them, and rail 3 of this row forbids it. The dependency is mechanical - see codebaseInsights (C2).\nREJECTED, treating DoD-4's untracked-path restore clause as binding: all six files are tracked, so `git diff` IS the correct proof; performing a SHA256-vs-%TEMP% ritual on tracked files would be evidence-shaped noise.",
    "codebaseInsights": "MEASURED 2026-09-05 by architect-b63fabf3 at HEAD f3b94eaa on moe/work-2026-09-04.\n\n1. THE KIND DOES NOT EXIST YET: `git grep -n 'set_agent_provider' -- . ':!.moe'` = ZERO hits anywhere in the tree. Nothing to duplicate, and the prerequisite (task-c422b106, WORKING) genuinely has not landed.\n\n2. THE ORDERING CONSTRAINT IS MECHANICAL, not procedural. mcp-tool-allowlist.test.ts arm C2 iterates MCP_EXCLUDED_COMMAND_KINDS and asserts `[...provider.provide().registry.keys()].includes(kind)` for each; the \"excludes kinds that are REAL vocabulary keys\" arm asserts `Object.keys(PAYLOAD_KEYS).includes(kind)`. Adding the kind to the exclusion roster before the registry serves it reds BOTH. That is why dependsOn task-c422b106 is real.\n\n3. AN ASYMMETRY WORTH KNOWING, and it is not documented anywhere: HUMAN_ONLY_STEPS (agent-spawn-contract.ts:195-216, EIGHT members) is a bare `ReadonlySet<string>` and its only asserting arms - affordance-planning-offers.test.ts:360 (offered `approval.*` kinds must be in it) and :427 (two named compiler kinds must NOT be) - never enumerate the whole set. It therefore TOLERATES an unserved kind, and today it carries `environment.set_variable` / `environment.unset_variable` while `grep -c environment.set_variable daemon-command-vocabulary.ts` = 0. MCP_EXCLUDED_COMMAND_KINDS tolerates no such thing. CONSEQUENCE FOR THE DRILL: deleting this kind from HUMAN_ONLY_STEPS may leave the suite GREEN, because no arm guards its membership. The plan tells the worker to report that rather than fake a red.\n\n4. THE PIN CENSUS, complete, 6 -> 7 across FOUR test files (`git grep -n 'toBe(6)|toHaveLength(6)|toBe(12)' -- apps/daemon/src`):\n   - mcp-tool-allowlist.test.ts:142 `expect(MCP_EXCLUDED_COMMAND_KINDS.length).toBe(6)`; and the four-key witness object `{excluded: 6, queries: 6, vocabulary: 46, wired: 46}` just below it.\n   - daemon-command-registry-human-approver.test.ts:318-324 - a hand-written `expectedExclusions` array of six, plus `expect(expectedExclusions).toHaveLength(6)` AND `expect(MCP_EXCLUDED_COMMAND_KINDS).toHaveLength(6)`.\n   - mcp-main.test.ts:207 `EXPECTED_EXCLUDED_COMMAND_KINDS` (a second hand-written array of six); :228 `EXCLUSION_CASES.length).toBe(6)`; :230 `EXCLUSION_CASES.length * MCP_TRANSPORT_ENTRY_COUNT).toBe(12)` - DERIVED, and the easy miss because its value is not the number being changed.\n   - mcp-http/mcp-http-host.test.ts:688 and :719, both `EXCLUSION_CASES.length).toBe(6)`.\n\n5. `wired` DOES NOT MOVE. `wiredMcpToolKinds()` = PAYLOAD_KEYS minus MCP_EXCLUDED plus MCP_SERVED_QUERY_KINDS (mcp-tool-allowlist.ts:84-90). This row adds one to `excluded` while task-c422b106 adds one to `vocabulary`, so the difference is unchanged. Predicting it lets the worker notice if it moves anyway.\n\n6. THE WITNESS OBJECT'S PROSE IS ALREADY STALE: the comment above it narrates \"45 - 5 + 5 = 45\" while the literals read 46/6/6/46. Fix or delete the arithmetic sentence; do not add a third version.\n\n7. mcp-http-host.test.ts:623 pins `source.match(/^ {2}it\\(/gmu)` at 15 (\"fourteen behaviours + this counter\"). It moves ONLY if an `it` is added - which this row should not need, since the sweeps auto-generate. A worker who moves it has written a duplicate arm.\n\n8. FILE SIZES, all under the 400 cap and none near it after this diff: mcp-tool-allowlist.ts 98, agent-spawn-contract.ts 229, mcp-tool-allowlist.test.ts 347, mcp-main.test.ts 280, mcp-http-host.test.ts 728 (a test file, exempt from the production cap), daemon-command-registry-human-approver.test.ts 331.\n\n9. GATE REACH: vitest.config.ts:32 include = adapters/**, packages/**, tests/**, tools/** - no `apps/**`. Root `pnpm test` sees ZERO of these six files; `pnpm --filter @moe/daemon test` is the only real gate.",
    "risks": "FIRST - THE DERIVED PIN. `mcp-main.test.ts:230 expect(EXCLUSION_CASES.length * MCP_TRANSPORT_ENTRY_COUNT).toBe(12)` must become 14. It is the only pin whose literal is not the number being changed, it sits two lines from a pin that IS, and it reads as an unrelated fact. This is the single likeliest omission in the row.\n\nSECOND - DoD-4(b) MAY BE UNSATISFIABLE AS WRITTEN. No existing arm guards this kind's HUMAN_ONLY_STEPS membership (see codebaseInsights 3), so the drill can stay green with the entry deleted. The honest output is to say so, add a short membership arm in a file this row already owns, and drill that. Faking a red, or quietly dropping the clause, are both rejects.\n\nTHIRD - THREE CONCURRENT ROWS MOVE THE SAME LITERALS: task-5f883e4e (preview fence) and task-20b7d6bb (release fence) both add to MCP_EXCLUDED_COMMAND_KINDS, and task-c422b106 / task-40e52fa8 / task-54b74b80 all move `vocabulary`. Every number must come from the PRINTED expected-vs-received at the moment of the final gate, never from arithmetic. Re-read the witness object immediately before completing.\n\nFOURTH - TWO INDEPENDENT HAND-WRITTEN TRANSCRIPTIONS (human-approver:318, mcp-main:207) must each gain the member, spelled identically. They exist to be independent; deriving either from the production roster makes its comparison vacuous, and a typo in one reds as a set difference rather than a helpful message.\n\nFIFTH - DO NOT WRITE A PARALLEL TEST. The bidirectional set-equality and the per-transport unreachable sweeps already exist and auto-generate this kind's cases. New arms here are duplicate coverage; if mcp-http-host.test.ts:623's `it(` count needs moving, that is the signal you added one.\n\nSIXTH - DoD-4's restore-proof parenthetical points at untracked paths and SHA256 backups. All six files here are TRACKED and this row creates no new module, so `git diff` is the correct proof. Say that with `git status` output rather than performing the ritual.\n\nSEVENTH - the MCP port authenticates with the operator bootstrap credential (mcp-main.ts:112-127), which is WHY exclusion rather than a capability gate is the fence. Do not 'improve' this by adding a capability check instead.\n\nEIGHTH - shared checkout. Peer dirt under .moe/ and .grok/ is NEVER a stop condition: note it, never revert, stash or `git add -A`. Report every path you touch in complete_step.modifiedFiles.",
    "keyFiles": [
      "apps/daemon/src/mcp-tool-allowlist.ts",
      "apps/daemon/src/orchestrator/agent-spawn-contract.ts",
      "apps/daemon/src/mcp-tool-allowlist.test.ts",
      "apps/daemon/src/daemon-command-registry-human-approver.test.ts",
      "apps/daemon/src/mcp-main.test.ts",
      "apps/daemon/src/mcp-http/mcp-http-host.test.ts"
    ]
  },
  "planApprovedAt": "2026-09-05T16:53:23.039Z"
}