{
  "id": "task-dd4ffa0c40834c59892f957fbf61fb23",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Graph Beta: persist the current active graph projection",
  "description": "Graph Beta prerequisite for task-a9fd91c373c244c78df97945965a8038. Compose the landed @moe/core graph-revision lifecycle with SqliteEventStore so production code can answer “current graph snapshot for this project” from durable revision events. Measurement at HEAD a42ae2f1f9168d13b8b786404fc47b366cc8edd9: packages/core/src/planning/graph-revision-{contract,reducer}.ts exist; packages/store/src has zero GraphSnapshot/current-graph readers; apps/daemon only previews caller snapshots. This task owns the durable projection/read seam only. The daemon graph command/query transport is a separate consumer task. Stage: GRAPH BETA per the design’s incremental-delivery boundary. Not in scope: Foundation attempt ingress, launch templates, workspaces, fan-out dispatch, new runtime kinds, or edits to the authoritative design.",
  "definitionOfDone": [
    "A focused production projection folds canonical graph-revision events per project and returns exactly one current ACTIVE revision as an immutable scheduler-compatible GraphSnapshot plus revision identity and graphEpoch provenance.",
    "No active revision returns an exact stable UNKNOWN/not-found code and layer; corrupt, non-canonical, stale, or multiple-active histories fail closed under distinct stable code/layer mappings and yield no snapshot authority.",
    "Projection replay is deterministic and order/identity checked: duplicate delivery is idempotent, conflicting bytes or impossible lifecycle ordering refuse, and raw event counts do not change on reads.",
    "Focused tests use a real SqliteEventStore, assert exact refusal code and layer, prove generated hostile-case counts are nonzero, and include one accepted active-graph control read from production events rather than a helper reimplementation.",
    "The task names the daemon graph transport task as its real consumer and records that task id in completion evidence; pnpm --filter @moe/daemon test reports a nonzero executed-test count and pnpm --filter @moe/daemon typecheck exits 0 for owned paths."
  ],
  "taskRails": [
    "Use the existing @moe/core graph revision contracts/reducer and existing runtime graph vocabulary; do not mint command or query kinds.",
    "Keep persistence, projection, and daemon transport concerns separate; this task does not wire HTTP/MCP or Foundation attempt ingress.",
    "Fail closed with stable reason codes/layers and assert the reason code, layer, and nonzero generated-case count in every refusal sweep.",
    "Use TDD; target <=250 physical lines per production source and split before 400; preserve foreign shared-tree work and commit only explicit owned paths."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Verify both producer surfaces at committed HEAD before composing them; the reopen reason names them but statuses are not proof. (1) REPLAY (from task-ee27ed7c): `replayGraphRevisionEvents(history: unknown)` at packages/core/src/planning/graph-revision-replay.ts:211, root-exported from @moe/core (index.ts:109) with GRAPH_REVISION_REPLAY_CODES and the result types. Read its contract comments -- the whole input is snapshotted before any discriminator is read; a byte-identical redelivery is idempotently ignored; same version with different bytes refuses GRAPH_REVISION_REPLAY_IDENTITY_CONFLICT; a refusal returns code and layer alone, never partial state. This means the projection does NOT re-implement duplicate/ordering/identity rules -- the core replay already owns them, and this task's DoD 3 idempotence/refusal items are satisfied by DELEGATING and surfacing the core's own codes. (2) CONTENT (from task-e3d5fd05, absorbing 77e8cb44): `encodeGraphContent(content, policyOverride?)` and `decodeGraphContent(input)` on the @moe/scheduler root (index.ts:90-91), with GraphContent carrying `graphContentHash` over all seven design-197 fields, a separate `snapshotIdentity`, `content: GraphRevisionContent`, and `bytes`. The root comment says encodeGraphContent is the ONLY route to a hash -- the projection must never compute one. (3) Prove the workspace edges from inside apps/daemon with a temporary compiled probe importing `replayGraphRevisionEvents` from bare `@moe/core` and `decodeGraphContent` from bare `@moe/scheduler`, typecheck exit 0, delete the probe in the same command. Both packages are already declared in apps/daemon/package.json -- but prove it, don't read it. (4) Confirm nothing writes graph-revision events in production yet: tests must seed via `reduceGraphRevision`, never hand-written event literals.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "apps/daemon/src/planning/active-graph-projection.test.ts"
      ],
      "note": "STEP 1 (REOPEN #1) — QA'S DEFECT 1 REPRODUCED, NOT TAKEN ON TRUST. I wrote the failing test before touching production and got their exact measurement:\n```\nFAIL > distinguishes a create-less history from an empty project\nAssertionError: expected 'ACTIVE_GRAPH_ABSENT' to be 'GRAPH_REVISION_REPLAY_MISSING_CREATE'\nTests 1 failed | 12 passed (13)\n```\n**QA IS RIGHT ON BOTH DEFECTS AND I ACCEPT THEM IN FULL.** A create-less-but-activated history and a genuinely empty project return the identical refusal, so a corrupt history is indistinguishable from an absent one — exactly what DoD 2 forbids. My step-7 attacker pass answered \"Do two causes collapse into one code? NO\"; that answer was wrong, and it was wrong because I checked the CODE VOCABULARY rather than probing the behaviour. Distinct codes existing is not distinct codes firing.\n\nWHY MY SUITE MISSED IT, stated plainly because it is the more important lesson: my hostile sweep already contained a \"missing create\" case, and it passed. It asserted only `ok === false`, `layer === ACTIVE_GRAPH_PROJECTION_LAYER` and `typeof code === \"string\"`. QA's point that the layer assertion is VACUOUS is correct — `refuse()` hardcodes `layer` on every path, so that assertion cannot ever hold another value. Net, the only live assertion was \"it did not succeed\". In my step-4 note I called this \"one deliberate loosening\" and defended it as avoiding an implementation choice. That reasoning was wrong: the loosening is precisely what let a real defect sit inside a green sweep, which is the failure mode epic rail 6 exists to prevent. I am not repeating the argument — I am pinning the codes.\n\nTWO TESTS ADDED, and the pair is the point rather than either alone:\n- `distinguishes a create-less history from an empty project` — drives the REAL reducer, then filters `GraphRevisionCreated` out of the generated history (asserting the filter actually removed exactly one event, so it cannot silently become a no-op), commits the remainder, stores the body, and requires `GRAPH_REVISION_REPLAY_MISSING_CREATE` with `sourceLayer: \"CORE_GRAPH_REVISION_REPLAY\"`, plus an explicit `not.toBe(\"ACTIVE_GRAPH_ABSENT\")`.\n- `answers ABSENT for a genuinely empty project, distinctly` — the control that gives the first test its meaning. Without it, a projection that answered MISSING_CREATE for everything would pass.\n\nSTATE RE-MEASURED AT HEAD 873cde6 (task start was 01820a8; four commits since):\n- All six of my paths are still at HEAD and intact.\n- `npx tsc --project apps/daemon/tsconfig.json --noEmit` -> **EXIT 0, zero diagnostics**. QA recorded a TS6133 in `src/work/foundation-context-manifest-codec.ts` and correctly declined to attribute it to me. For the record that file is MINE, from task-3e891741, and the diagnostic was transient — it existed only while that file was mid-write during QA's review window. It is clean now and that task gated at DTC=0. Nothing to fix.\n- QA's \"VERIFIED GOOD, do not rework\" list is banked: the 130/2799 daemon run, both per-file line counts, the two load-bearing drills, real-store seeding, and the missing-commit attribution to 9b9e44e. I am not re-doing any of it.\n\nFIX DIRECTION ACCEPTED as QA specified: enumerate by a means a create-less history cannot escape (`readEventsAfter` plus the `graph-revision:<projectId>:` prefix), then pin code AND sourceLayer per hostile-sweep case. Steps 2-3 need no change — the body record is untouched by this reject.",
      "startedAt": "2026-08-18T04:05:01.629Z",
      "completedAt": "2026-08-18T04:05:42.339Z"
    },
    {
      "stepId": "step-2",
      "description": "RED on the content-body record. Create apps/daemon/src/planning/graph-body-record.test.ts over a REAL SqliteEventStore on a temp file. The body stored is the CANONICAL CONTENT BYTES the scheduler codec produced -- never a hand-serialised snapshot, because encodeGraphContent is the only lawful route to a graphContentHash and this store must not mint a second serialisation. Failing tests: (1) `putGraphBody(store, projectId, graphContent)` -- where graphContent is an `encodeGraphContent` SUCCESS value -- durably appends one record keyed by its `graphContentHash` and returns that hash; (2) `readGraphBody(store, projectId, graphContentHash)` returns bytes that `decodeGraphContent` accepts AND whose re-derived hash equals the requested key -- run the real decoder in the assertion, because the decoder's digest check is the integrity authority, not a local comparison; (3) stored bytes that fail decodeGraphContent (corrupt them in the store between put and read) refuse with the DECODER'S own code surfaced plus this module's layer -- assert the literal code; (4) an absent hash refuses with a DISTINCT stable not-found code (absence is not corruption); (5) putting the same content twice is idempotent -- one record, same hash, raw event count unchanged on the second put; (6) reads never change the raw event count. Assert code AND layer literals on every refusal.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-body-record.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/planning/graph-body-record.test.ts"
      ],
      "note": "STEP 2 (REOPEN #1) — NO CHANGE REQUIRED, and nothing rewritten. The plan steps reset to PENDING on reopen, but QA's reject names two defects and BOTH live in the projection, not in the body record. QA's own \"VERIFIED GOOD, do not rework\" list covers this suite, so re-deriving it would burn a cycle and risk perturbing bytes they already graded.\n\nRE-VERIFIED RATHER THAN ASSUMED:\n- `npx vitest run src/planning/graph-body-record.test.ts` -> `Test Files 1 passed (1) | Tests 9 passed (9)`.\n- `git diff --numstat HEAD -- graph-body-record.{ts,js,test.ts}` -> **0 lines**, i.e. all three are byte-identical to what is committed and what QA reviewed.\n\nThe nine cases stand as recorded in the first pass: put appends exactly one content-addressed record; read re-validates through the REAL `decodeGraphContent`; a corrupt row surfaces `GRAPH_CONTENT_DIGEST_MISMATCH` with `sourceLayer: GRAPH_CONTENT_IDENTITY`; an absent hash gives the distinct `GRAPH_BODY_ABSENT`; a misfiled row gives `GRAPH_BODY_IDENTITY_MISMATCH`; a hand-serialised body is refused `GRAPH_BODY_NOT_ENCODED` with nothing appended; put is idempotent; reads never append on either path; the code vocabulary is frozen and asserted exhaustively.\n\nWorth noting against QA's defect 2: this suite does NOT have the vacuous-assertion problem they found in the projection sweep. Every refusal case here pins an exact literal code and, where a verdict is passed through, the `sourceLayer` that produced it. The defect was specific to the projection's hostile-ordering sweep, which is where I am fixing it.",
      "startedAt": "2026-08-18T04:05:53.732Z",
      "completedAt": "2026-08-18T04:06:04.423Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN the body record. Create apps/daemon/src/planning/graph-body-record.ts plus its one-line `.js` bridge (`export * from \"./graph-body-record.ts\";` -- required or the module is invisible to the .js import graph). It accepts only an encodeGraphContent success value on the write side (refuse anything else -- accepting raw fields here would recreate the mint-your-own-hash seam the scheduler deliberately withholds), stores the canonical bytes keyed by graphContentHash, and on the read side runs decodeGraphContent over the stored bytes before returning anything, surfacing the decoder's refusal codes under this module's layer. No local hashing, no local canonicalisation, no second serialisation. Keep the file <=250 physical lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-body-record.ts",
        "apps/daemon/src/planning/graph-body-record.js"
      ],
      "newFiles": [
        "apps/daemon/src/planning/graph-body-record.ts",
        "apps/daemon/src/planning/graph-body-record.js"
      ],
      "note": "STEP 3 (REOPEN #1) — NO CHANGE REQUIRED. `graph-body-record.ts` (191 lines) and its LF `.js` bridge are byte-identical to the committed, QA-reviewed bytes (`git diff --numstat HEAD` -> 0 lines) and its suite is green at 9/9.\n\nQA's reject touches neither file. Both defects are in `active-graph-projection.ts` (enumeration) and `active-graph-projection.test.ts` (the vacuous hostile sweep), and QA's \"VERIFIED GOOD, do not rework\" list explicitly banks the per-file line counts and the drill that proves this module's post-decode identity check is load-bearing.\n\nRecording one thing rather than re-deriving the module: the body record's read path is what the projection's `ACTIVE_GRAPH_BODY_UNAVAILABLE` refusal wraps, and that wrapping already carries `sourceCode` (e.g. `GRAPH_BODY_ABSENT`) alongside `sourceLayer: GRAPH_BODY_RECORD`. That is the shape QA is asking me to extend to the hostile sweep in step 4 — pin the underlying code, not just the fact of refusal. The pattern was already right here; it was the sweep that had detached from it.",
      "startedAt": "2026-08-18T04:06:07.116Z",
      "completedAt": "2026-08-18T04:06:16.165Z"
    },
    {
      "stepId": "step-4",
      "description": "RED on the current-active fold. Create apps/daemon/src/planning/active-graph-projection.test.ts over a real store, seeding CANONICAL revision events by driving `reduceGraphRevision` from bare @moe/core and appending its emitted events -- NEVER hand-written event objects, or the projection is tested against shapes production would never write. Failing tests for `readCurrentActiveGraph(store, projectId)`: (1) one ACTIVE revision returns its revisionId, graphEpoch, planHash, graphContentHash, the decoded GraphRevisionContent from step 3's body read, AND the separately-named structural snapshotIdentity -- assert graphContentHash !== snapshotIdentity on the accepted control, pinning dec-64b2391c at the consumer; (2) zero ACTIVE revisions -> stable not-found code, NO snapshot authority; (3) TWO ACTIVE revisions across two aggregates for one project -> DISTINCT stable code -- split-brain refuses, never picks a winner; (4) SUPERSEDED predecessor plus ACTIVE successor -> only the successor, epoch exactly predecessor+1; (5) a corrupted per-revision history (reordered or conflicting bytes) surfaces the CORE REPLAY'S own refusal code (e.g. GRAPH_REVISION_REPLAY_IDENTITY_CONFLICT) with this module's layer attached -- assert WHICH layer refused, since core and projection can both answer; (6) ACTIVE revision whose graphContentHash has no stored body, or whose body read refuses -> its own distinct code, no snapshot; (7) two consecutive reads are byte-stable and change no counts. Assert the hostile-ordering sweep's generated case count is nonzero before asserting its cases.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/active-graph-projection.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/planning/active-graph-projection.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/active-graph-projection.test.ts"
      ],
      "note": "STEP 4 (REOPEN #1) — QA'S DEFECT 2 FIXED: THE SWEEP NOW PINS CODE AND REFUSING LAYER PER CASE, and it immediately catches defect 1.\n```\nFAIL > refuses every hostile ordering with its OWN code and refusing layer\nAssertionError: missing create code: expected 'ACTIVE_GRAPH_ABSENT' to be 'GRAPH_REVISION_REPLAY_MISSING_CREATE'\nTests 2 failed | 11 passed (13)\n```\nThat is the proof the rewrite worked: the SAME sweep, over the SAME three cases, previously passed. The only thing that changed is what it asserts.\n\nWHAT WAS WRONG, accepted without argument. The old sweep asserted `ok === false`, `layer === ACTIVE_GRAPH_PROJECTION_LAYER`, and `typeof code === \"string\"`. QA's finding that the layer assertion is VACUOUS is exactly right — `refuse()` hardcodes `layer` on every return path, so it can never hold another value and the assertion can never fail. Strip it and `typeof code === \"string\"` (true of every string) and the sweep reduced to \"it did not succeed\". Epic rail 6's own wording — \"a test asserting only refused/threw is one added layer away from vacuous\" — describes this precisely, and here it was not even one layer away: the defect was already inside.\n\nTHE CASE TABLE NOW CARRIES FOUR COLUMNS: name, history, expected code, expected `sourceLayer`. Each iteration asserts the exact code and the exact refusing layer, with per-assertion messages so a failure names which case broke. I kept `layer` asserted too, but it is now the LEAST informative of the three rather than the only one.\n\nTHREE GUARDS ON THE TABLE ITSELF, because a case table can rot in ways a loop cannot see:\n- `length > 0` and `=== 3` (the nonzero-count requirement QA told me to keep).\n- All case names distinct — a duplicated name silently halves a sweep while the length still reads right.\n- **Every expected code asserted `not.toBe(\"ACTIVE_GRAPH_ABSENT\")` before the loop runs.** This is the structural guard against the exact regression QA found: if someone later \"fixes\" a failing case by editing the expected code to the absent code, the table rejects it. The collapse cannot be re-introduced by weakening an expectation.\n\nI also fixed a latent bug in the harness while I was here: the temp-directory name was `hostile-${cases.findIndex(...)}`, which re-scans the table by name on every iteration and would collide for two cases sharing a name. It is now derived from the case's own code and history length.\n\nEXPECTED CODES TAKEN FROM QA'S MEASUREMENT, not from my reading: \"missing create\" and \"reversed\" -> `GRAPH_REVISION_REPLAY_MISSING_CREATE`, \"activation before submission\" -> `GRAPH_REVISION_REPLAY_VERSION_BREAK`, all three with `sourceLayer: CORE_GRAPH_REVISION_REPLAY`. The loop currently throws on the first case, so cases 2 and 3 are not yet confirmed against a running projection — I will verify all three in step 5 once the enumeration fix lands, and I will report it honestly if either differs from QA's prediction rather than editing the table to match whatever comes out.\n\nAlso standing from step 1: the dedicated `distinguishes a create-less history from an empty project` test and its `answers ABSENT for a genuinely empty project` control. The pair is deliberate — the sweep proves the corrupt case gets its own code, the control proves the absent case still gets ABSENT, and neither is meaningful without the other.",
      "startedAt": "2026-08-18T04:06:19.309Z",
      "completedAt": "2026-08-18T04:07:04.747Z"
    },
    {
      "stepId": "step-5",
      "description": "GREEN the projection. Create apps/daemon/src/planning/active-graph-projection.ts plus its `.js` bridge. Per revision aggregate: read events, hand the raw history to `replayGraphRevisionEvents`, and take the returned GraphRevisionState -- do NOT reimplement lifecycle, duplicate-delivery, ordering or identity rules locally; the core replay owns them and its refusal codes pass through with this module's layer attached. Select the single ACTIVE revision across the project's aggregates; more than one is the split-brain refusal, zero is not-found. Compose the step-3 body read for the ACTIVE revision's graphContentHash and return one frozen result: {content, snapshot fields, revisionId, graphEpoch, planHash, graphContentHash, snapshotIdentity, provenance naming the aggregate the epoch was bound from}. graphEpoch is a BOUND REFERENCE to the goal-owned epoch (contract's own comment) -- report it, never compute it. Unresolvable state stays UNKNOWN with an exact code and layer and gains no authority. Keep the file <=250 lines; split the aggregate-enumeration from the compose if it runs long.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/active-graph-projection.ts",
        "apps/daemon/src/planning/active-graph-projection.js"
      ],
      "newFiles": [
        "apps/daemon/src/planning/active-graph-projection.ts",
        "apps/daemon/src/planning/active-graph-projection.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/active-graph-projection.ts"
      ],
      "note": "STEP 5 (REOPEN #1) — GREEN. `Test Files 4 passed (4) | Tests 61 passed (61)` (was 59; +2 for the create-less case and its empty-project control). Production is **219 lines**, still under 250.\n\nTHE FIX, exactly the direction QA specified. `enumerateAggregateIds` no longer keys discovery on the `GraphRevisionCreated` event. It now pages `store.readEventsAfter(cursor, ENUMERATION_PAGE)` over ALL stored events and keeps every `aggregateId` starting with `graphRevisionAggregateId(projectId, \"\")` — the `graph-revision:<projectId>:` prefix. A create-less aggregate can no longer escape enumeration by lacking the one event discovery used to depend on, so it reaches `replayGraphRevisionEvents` and the core answers with its own `GRAPH_REVISION_REPLAY_MISSING_CREATE` under `sourceLayer: CORE_GRAPH_REVISION_REPLAY`.\n\nThe prefix is built from the module's OWN `graphRevisionAggregateId` deriver rather than a hand-written string. If the keying scheme ever changes, enumeration follows it automatically instead of silently matching nothing — a hardcoded `\"graph-revision:\"` would have been a second statement of the same fact, free to drift.\n\n`enumerateAggregateIds` now takes `projectId`, which it needed all along: the old signature could not scope discovery to a project at all, and only got away with it because the store is opened project-bound. Passing it explicitly makes the scoping visible rather than incidental.\n\nQA'S PREDICTED CODES ALL CONFIRMED AGAINST THE RUNNING PROJECTION. In step 4 I flagged that the sweep threw on its first case, so cases 2 and 3 were still unverified, and said I would report honestly if either differed. Both matched: with the fix in place the full three-case sweep passes as written — \"missing create\" and \"reversed\" -> `GRAPH_REVISION_REPLAY_MISSING_CREATE`, \"activation before submission\" -> `GRAPH_REVISION_REPLAY_VERSION_BREAK`, all three `sourceLayer: CORE_GRAPH_REVISION_REPLAY`. No expectation was edited to match an observed value; the table was written from QA's measurement before the fix existed.\n\nTHE THREE SITUATIONS NOW ANSWER DISTINCTLY, which is the whole point of the reject:\n- empty project -> `ACTIVE_GRAPH_ABSENT`, `sourceLayer: null`\n- DRAFT-only project -> `ACTIVE_GRAPH_ABSENT`, `sourceLayer: null` (correct — a real history that simply never reached ACTIVE is an absence, not a corruption)\n- create-less-but-activated -> `GRAPH_REVISION_REPLAY_MISSING_CREATE`, `sourceLayer: CORE_GRAPH_REVISION_REPLAY`\nDoD 2's \"corrupt histories fail closed under DISTINCT stable code/layer mappings\" now holds by measurement rather than by the vocabulary merely containing distinct codes — which was my original error.\n\nDELEGATION IS UNCHANGED AND STILL THE DESIGN: the projection gained no lifecycle knowledge from this fix. It does not now know what a create event means; it simply stopped using one event kind as a proxy for \"this aggregate exists\" and lets the core replay be the only thing with an opinion about history validity.\n\nNothing else touched — `graph-body-record.*` remain byte-identical to the reviewed bytes, and everything on QA's \"VERIFIED GOOD, do not rework\" list is untouched.",
      "startedAt": "2026-08-18T04:07:07.944Z",
      "completedAt": "2026-08-18T04:07:52.898Z"
    },
    {
      "stepId": "step-6",
      "description": "Mutation drills, three. Drill 1 (the ACTIVE selector is real): widen the projection's lifecycle filter to also accept APPROVED; the split-brain or not-found test must go RED on its code assertion. Drill 2 (the decode gate is load-bearing): make the body read return stored bytes without running decodeGraphContent; the corrupt-body test from step 2 must go RED. Drill 3 (delegation is real, not decorative): make the projection swallow the core replay's refusal and fold whatever partial state it can; the step-4 case (5) test must go RED on the core code assertion -- if it stays green, the test is not actually driving a corrupted history through the production path and must be strengthened. A drill that reddens by crashing or on a different assertion has not proven the binding. Restore by re-editing, never `git checkout` (destructive in the shared worktree), confirm `git diff --numstat` shows 0 added 0 deleted per drilled file, one drill at a time -- a peer commit landing mid-drill can swallow the patch and leave git reading clean.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/active-graph-projection.ts",
        "apps/daemon/src/planning/graph-body-record.ts"
      ],
      "note": "STEP 6 (REOPEN #1) — QA'S TWO REQUIRED RE-DRILLS PLUS THE THREE ORIGINALS. All five killed their intended test on a CODE assertion. Final state: both production files byte-identical to their pre-drill baselines, `Test Files 4 passed (4) | Tests 61 passed (61)`.\n\nDRILL A (QA-required): make `collectActive` swallow the replay refusal and skip the aggregate. **THREE tests red, ALL on code assertions:**\n```\n> surfaces the CORE REPLAY's own code ... expected 'ACTIVE_GRAPH_ABSENT' to be 'GRAPH_REVISION_REPLAY_IDENTITY_CONFLICT'\n> refuses every hostile ordering ...     missing create code: expected 'ACTIVE_GRAPH_ABSENT' to be 'GRAPH_REVISION_REPLAY_MISSING_CREATE'\n> distinguishes a create-less history ... expected 'ACTIVE_GRAPH_ABSENT' to be 'GRAPH_REVISION_REPLAY_MISSING_CREATE'\n```\nThis is the exact check QA specified — \"the sweep must go RED on a CODE assertion, not on `ok===false` alone; if it stays green the sweep is still detached from its subject.\" Under the OLD sweep this same drill left it green, because swallowing a refusal still produces `ok === false`. The sweep is now attached to its subject.\n\nDRILL B (QA-required): revert enumeration to create-event-only. **2 failed | 11 passed**, failing `distinguishes a create-less history from an empty project` on `expected 'ACTIVE_GRAPH_ABSENT' to be 'GRAPH_REVISION_REPLAY_MISSING_CREATE'` and the sweep's matching case. So the enumeration fix is load-bearing and the new test genuinely detects its absence — it is not passing for some incidental reason.\n\nTHE THREE ORIGINAL DRILLS RE-RUN, to prove the fix regressed nothing QA had already banked:\n- Widen the lifecycle filter to accept APPROVED -> 1 failed, exactly `treats an APPROVED revision as NOT active, even with its body stored`.\n- Body read trusts the row and skips `decodeGraphContent` -> 1 failed, exactly `surfaces the DECODER's own code under this module's layer for a corrupt row`.\n- (Delegation is covered by drill A above, which is the stronger form of the original drill 3.)\n\nRESTORE VERIFICATION: `sha256` of both production files matches the pre-drill baselines exactly — `c3722bc8...` for the projection, `322306187...` for the body record. One drill at a time; every restore by re-editing content, never `git checkout`.\n\n=== A THIRD FOREIGN WHOLE-TREE COMMIT LANDED DURING THIS STEP, AND I CHECKED WHAT IT CAPTURED ===\n`git diff --numstat HEAD -- apps/daemon/src/planning/` came back EMPTY mid-drill, which is the tell that a foreign commit had swallowed my working tree. HEAD is now **39a1b2c `feat(task-d288909b...)`**. The dangerous version of this is a hook committing a MUTATED file, leaving a mutant in history while git reads clean, so I audited every commit that has ever touched the file:\n```\n39a1b2c  drilledEnum=0  drilledAPPROVED=0  drilledSwallow=0   <- current HEAD, the FIXED bytes\n9b9e44e  drilledEnum=1  drilledAPPROVED=0  drilledSwallow=0   <- the pre-fix version QA reviewed\n```\n`drilledEnum=1` on 9b9e44e is the ORIGINAL create-event-only enumeration, i.e. the defect QA rejected — not a drill artifact. **No drilled state was ever committed.** HEAD holds `readEventsAfter` (the fix) and `lifecycle === \"ACTIVE\"` (unwidened), and all three files' HEAD blobs hash-match my working tree. The capture happened after my restores, which I confirmed rather than assumed.\n\nThat makes three foreign whole-tree captures in this session (9b9e44e, 76e7396, 39a1b2c). Consequence for QA: as before, no commit bears this task's id, and the correct review instrument is the base-ref diff over owned paths.",
      "startedAt": "2026-08-18T04:07:56.862Z",
      "completedAt": "2026-08-18T04:09:37.146Z"
    },
    {
      "stepId": "step-7",
      "description": "Adversarial review, consumer edge, commit, gate. ATTACKER PASS: can a caller-supplied snapshot or raw content reach the durable body without passing through encodeGraphContent's success value? Can two ACTIVE revisions ever yield a snapshot instead of refusing? Can the projection return content whose re-derived hash was never checked? Does any read path append? Is any store handle left open (a held SQLite handle kills the vitest worker)? Does any refusal collapse two distinct causes into one code? Is snapshotIdentity ever equated with graphContentHash anywhere, including in a test helper? CONSUMER EDGE per the global composition rail: name the consumer tasks in the completion note -- task-c4b807784 (authenticated current-graph query) reads this projection, and task-eacea969/task-32c1ba45 consume it for transitions; all are BLOCKED, so record the ids and state plainly that no live call site exists yet. COMMIT by explicit pathspec, only the six owned paths, after `git status --porcelain` and unstaging any foreign entry; verify with `git show --stat`; if a foreign whole-tree completion already captured these bytes, do NOT amend or reset -- record the merge-base diff for QA. GATE, foreground, `&&`-chained: `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck`. Read the OUTPUT -- a filtered leg is evidence only with a nonzero `Test Files N passed` line; `@moe/daemon` is apps/daemon and root `pnpm test` does not cover apps/**. Confirm both new test files appear by name and the hostile sweep printed a nonzero count.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-body-record.ts",
        "apps/daemon/src/planning/graph-body-record.js",
        "apps/daemon/src/planning/graph-body-record.test.ts",
        "apps/daemon/src/planning/active-graph-projection.ts",
        "apps/daemon/src/planning/active-graph-projection.js",
        "apps/daemon/src/planning/active-graph-projection.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/active-graph-projection.ts",
        "apps/daemon/src/planning/active-graph-projection.test.ts"
      ],
      "note": "STEP 7 (REOPEN #1) — GATE GREEN, ATTACKER PASS RE-RUN AGAINST THE DEFECT THAT GOT THROUGH.\n\nGATE: `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck` -> **GATE_EXIT=0**, `Test Files 130 passed (130) | Tests 2801 passed (2801)`. Up exactly +2 from the 2799 QA measured — the create-less case and its empty-project control. Both my suites still present; the planning slice is 4 files / 61 tests.\n\nATTACKER PASS, re-asked with the question that failed last time. My first-pass answer to \"does any refusal collapse two distinct causes into one code?\" was NO, and it was wrong — I had checked that the code VOCABULARY contained distinct codes rather than probing which code each situation actually produces. Existing-but-never-fired is indistinguishable from absent to any caller. This time the answer is grounded in the suite's own assertions: **eight distinct codes are now pinned** across the projection suite — `ACTIVE_GRAPH_ABSENT`, `ACTIVE_GRAPH_SPLIT_BRAIN`, `ACTIVE_GRAPH_BODY_UNAVAILABLE`, `GRAPH_REVISION_REPLAY_MISSING_CREATE`, `GRAPH_REVISION_REPLAY_VERSION_BREAK`, `GRAPH_REVISION_REPLAY_IDENTITY_CONFLICT`, plus `GRAPH_BODY_ABSENT`/`GRAPH_BODY_RECORD` on the wrapped-source path — each asserted with its `sourceLayer`.\nRe-checked and unchanged by the fix: `grep -c \"\\.commit(\"` in the projection is **0**, so no read path appends; `grep -cE \"sha256|createHash|canonical\"` is **0**, so the fix introduced no local hashing; the fix touched enumeration only and gave the projection no lifecycle knowledge.\n\nTHE FIX DELTA QA SHOULD REVIEW — `git diff --numstat 9b9e44e..HEAD -- apps/daemon/src/planning/`, i.e. rejected version -> now:\n```\n70  12  active-graph-projection.test.ts\n17  11  active-graph-projection.ts\n```\nProduction is 17 added / 11 removed: the `REVISION_CREATED_EVENT` constant deleted, `enumerateAggregateIds` switched from `readEventsByTypeAfter(\"GraphRevisionCreated\", ...)` to `readEventsAfter(...)` filtered by the `graph-revision:<projectId>:` prefix (derived from the module's own `graphRevisionAggregateId`, not a hand-written string), and the one call site updated to pass `projectId`. `graph-body-record.*` do not appear in the delta at all — untouched, exactly as QA's \"do not rework\" list requires.\n\nCOMMIT: nothing to stage. `git status --porcelain -- apps/daemon/src/planning/` is EMPTY because **a THIRD foreign whole-tree commit — 39a1b2c `feat(task-d288909b...)` — captured the fix**, after 9b9e44e captured the original. Per epic rail 3 I did not amend or reset. I verified what was captured rather than assuming: HEAD holds `readEventsAfter` (the fix) and `lifecycle === \"ACTIVE\"` (unwidened), all three files' HEAD blobs hash-match my working tree, and an audit of every commit that ever touched the file shows **no drilled state was ever committed** (`drilledEnum=0 drilledAPPROVED=0 drilledSwallow=0` at HEAD; the `drilledEnum=1` at 9b9e44e is the original defect QA rejected, not a drill artifact).\n\nCONSUMER EDGE unchanged and still route (a), ids re-verified on disk: task-c4b807784f434b05b34ea62207ea34f8 (authenticated current-graph query), task-eacea969d636420b8f5c47304039e056 (atomic active-graph transition service), task-32c1ba453469448d87bf0925b3590657 (durable supersession preparation) — all BLOCKED, so no live production call site exists yet.\n\nWHAT I'D WANT A REVIEWER TO CHECK HARDEST, since my own attacker pass missed this class once: that the create-less test and the sweep's \"missing create\" case are not passing for an incidental reason. Drill B is the evidence — reverting enumeration to create-event-only reddens both on their code assertions, so they detect the specific defect rather than merely coexisting with the fix.",
      "startedAt": "2026-08-18T04:09:40.053Z",
      "completedAt": "2026-08-18T04:10:39.965Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "REJECT on DoD 2 and DoD 4. Both MEASURED with probes over a real SqliteEventStore, not inferred from reading.\n\nDEFECT 1 (DoD 2, production). A corrupt/truncated history is INDISTINGUISHABLE from an empty project. Probe, three situations, one identical answer:\n  EMPTY_STORE :: code=ACTIVE_GRAPH_ABSENT layer=ACTIVE_GRAPH_PROJECTION src=null srcCode=null\n  DRAFT_ONLY  :: same\n  NO_CREATE   :: same\nNO_CREATE holds the submitted+activated events with GraphRevisionCreated absent. enumerateAggregateIds keys discovery on that one event, so the aggregate never reaches replayGraphRevisionEvents and its dedicated GRAPH_REVISION_REPLAY_MISSING_CREATE never fires. DoD 2 requires corrupt histories to map to DISTINCT code/layer. Your step-7 attacker pass answered \"Do two causes collapse into one code? NO\" - measurement contradicts it. Your own sweep lists \"missing create\" as a hostile case, so this is in scope.\n\nDEFECT 2 (DoD 4 + epic rail 6, tests). The hostile sweep asserts nothing but \"it did not succeed\" - which is why defect 1 stayed invisible. See issues.\n\nVERIFIED GOOD, do not rework:\n- pnpm --filter @moe/daemon test -> Test Files 130 passed (130) | Tests 2799 passed (2799), exit 0. Your two suites by name: 2 files / 20 tests.\n- Per-file 191 and 213 lines, under 250.\n- Drill (delete post-decode identity check) reddens the misfiled-body test. Drill (split-brain >1 -> >2) reddens line 360, the exact SPLIT_BRAIN code assertion. Both load-bearing; files restored byte-identical (sha256 + empty numstat).\n- Real store, real reduceGraphRevision seeding, zero local hashing, delegation pinned by the IDENTITY_CONFLICT test.\n- Missing commit NOT counted against you: 9b9e44e swallowed your six paths; worktree bytes match HEAD.\n- apps/daemon typecheck RED (TS6133 in src/work/foundation-context-manifest-codec.ts) - UNTRACKED peer file, not attributed to you, not yours to fix.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 114,
  "comments": [
    {
      "id": "comment-fbecba49e83f4441aea8655698ddcbb6",
      "author": "governor-7610ed5a",
      "content": "Governor durable copy of the full block measurement (the current 147-char blockedReason is a stub written after an unblock_worker ownership release; the original detail existed only in chat, which this project has proven destructible):\n\narchitect-f956ffe1, 22:16Z, HEAD a42ae2f — \"Fresh symbol audit overturns this task's premise. GraphRevisionEvent contains lifecycle/hash facts but no GraphSnapshot/content bytes; @moe/scheduler has no canonical snapshot codec/content-hash producer; @moe/core has reduceGraphRevision(command) but no event apply/replay/hydration surface. A daemon projection would otherwise invent both content identity and a parallel lifecycle reducer, violating the task rail and stale-dependency global rail.\"\n\nPrerequisites filed and PLANNING: task-77e8cb44 (canonical graph snapshot content identity — claimed by architect-f956ffe1 at 22:17Z) and task-ee27ed7c (replay graph revision events into state — unclaimed). Re-promote this task only after both land; re-measure the three absent surfaces above at the then-HEAD first.",
      "timestamp": "2026-08-16T22:17:31.471Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-16T21:45:45.041Z",
  "updatedAt": "2026-08-18T04:24:34.472Z",
  "metrics": {
    "firstClaimAt": "2026-08-16T22:11:17.151Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 14,
    "rejectCount": 1,
    "reopenCount": 1,
    "doneAt": "2026-08-18T04:24:34.472Z",
    "wallClockMs": 108797321
  },
  "contextFetchedBy": [
    "worker-f144c751",
    "qa-12cda609"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [
    {
      "item": "No active revision returns an exact stable UNKNOWN/not-found code and layer; corrupt, non-canonical, stale, or multiple-active histories fail closed under distinct stable code/layer mappings and yield no snapshot authority.",
      "rejectedAt": "2026-08-18T04:04:07.843Z",
      "rejectedBy": "qa-12cda609"
    },
    {
      "item": "Focused tests use a real SqliteEventStore, assert exact refusal code and layer, prove generated hostile-case counts are nonzero, and include one accepted active-graph control read from production events rather than a helper reimplementation.",
      "rejectedAt": "2026-08-18T04:04:07.843Z",
      "rejectedBy": "qa-12cda609"
    }
  ],
  "planSubmittedAt": "2026-08-18T03:06:24.734Z",
  "planningNotes": {
    "approachesConsidered": "I drafted a plan for this task once before (ownership refused the submission -- it belonged to architect-f956ffe1 then), and this replan is BETTER than that draft in three specific ways because the producers have since landed. (1) The old draft had the body record canonicalise and hash the snapshot itself; that is now forbidden and unnecessary -- encodeGraphContent is the only lawful route to a graphContentHash (the scheduler root comment says exactly this and withholds the raw mechanics), so the body record stores the codec's canonical BYTES and the read side re-validates through decodeGraphContent, whose digest check is the integrity authority. (2) The old draft had the projection fold lifecycle events with locally-enforced ordering/duplicate rules; task-ee27ed7c landed replayGraphRevisionEvents with snapshotted input, idempotent redelivery, identity-conflict refusal and no-partial-state -- so the projection DELEGATES and surfaces core codes rather than reimplementing rules, which also kills a whole class of drift. (3) The old draft could not name the structural/content identity split; now the result carries BOTH graphContentHash and snapshotIdentity and the accepted control asserts they differ, pinning dec-64b2391c at the consumer. REJECTED: (a) storing decoded content JSON instead of canonical bytes -- a second serialisation invites a second identity; (b) accepting raw content fields at putGraphBody -- recreates the mint-your-own-hash seam; (c) a single combined module -- body record and lifecycle fold have separate refusal vocabularies and would breach the per-file line target; (d) waiting for a durable graph-revision event writer -- none exists in production yet, and this task's contract is the read/persist seam; tests seed through reduceGraphRevision, the same way the replay surface's own suite does.",
    "codebaseInsights": "Measured at HEAD 8222b9e. REPLAY: packages/core/src/planning/graph-revision-replay.ts:211 replayGraphRevisionEvents(history: unknown) -> GraphRevisionReplayResult; root-exported at packages/core/src/index.ts:109 with GRAPH_REVISION_REPLAY_CODES and result types; admissible() enforces exact event keys per kind; byte-identical redelivery ignored; same-version-different-bytes -> GRAPH_REVISION_REPLAY_IDENTITY_CONFLICT; missing create -> GRAPH_REVISION_REPLAY_MISSING_CREATE; refusals carry code+layer, never partial state. CONTENT: packages/scheduler root exports encodeGraphContent(content, policyOverride?) and decodeGraphContent(input) (index.ts:90-91); GraphContent = {schemaVersion, graphContentHash (all seven design-197 fields, lowercase hex64 -- the spelling @moe/core's revision gate requires), snapshotIdentity (structure-only, documented never equal), content: GraphRevisionContent, bytes}; decodeGraphContent refuses GRAPH_CONTENT_NOT_BYTES / TOO_LARGE / digest mismatch etc. from the frozen GRAPH_CONTENT_ISSUE_CODES over three frozen layers. GraphRevisionState (core contract :122) carries boundHashes/goalRef/graphContentHash/graphEpoch/lifecycle/planHash/revisionId/submissionRef/version; graphEpoch is a bound reference to the goal-owned epoch -- report, never compute. NOTHING writes graph-revision events in production yet (grep: zero non-test writers), so tests seed via reduceGraphRevision. apps/daemon/src/planning/ already exists (approval-activation, approval-gate, planning-services) -- the new modules sit beside them. apps/daemon/package.json already declares @moe/core, @moe/scheduler, @moe/store; step 1 still proves the edges with a compiled bare-specifier probe because a manifest line is not a resolution proof. A new .ts module needs its one-line .js bridge.",
    "risks": "The load-bearing risk is identity theatre: a projection that returns content whose hash was never re-checked is authority-shaped without being authoritative. The design here routes ALL integrity through decodeGraphContent -- drill 2 proves the gate is load-bearing, and the accepted control asserts graphContentHash !== snapshotIdentity so the two-hash confusion (the exact failure dec-64b2391c was raised to prevent, still easy because a structural value once wore the content name) cannot re-enter through this consumer. Second, split-brain: two ACTIVE revisions must refuse, never disambiguate by epoch or recency -- picking a winner grants authority during precisely the condition the projection exists to detect. Third, silent local lifecycle rules: if the projection reimplements ordering/duplicate handling instead of delegating to replayGraphRevisionEvents, the two drift and the tests police the copy; drill 3 forces the delegation to be real. Fourth, hand-written event literals in tests let the projection pass against shapes production never writes -- seed through reduceGraphRevision only. Fifth, distinct causes need distinct codes: no-active, multiple-active, replay-refused, body-absent, body-corrupt are five failures; collapsing any two makes a later guard change invisible. Sixth, close every store handle on every path including refusals; a held SQLite handle kills the vitest worker. Seventh, consumers (c4b80778, eacea969, 32c1ba45) are all BLOCKED -- name them as the consumer edge honestly rather than implying a live call site. Finally, the shared worktree rules: restore drills by re-editing, commit by explicit pathspec only.",
    "keyFiles": [
      "packages/core/src/planning/graph-revision-replay.ts",
      "packages/core/src/planning/graph-revision-reducer.ts",
      "packages/scheduler/src/graph-content.ts",
      "packages/scheduler/src/graph-content-issues.ts",
      "packages/store/src/sqlite-event-store.ts",
      "apps/daemon/src/planning/planning-services.ts",
      "apps/daemon/package.json"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-18T03:21:25.303Z",
    "escalatedAt": "2026-08-18T03:21:25.303Z"
  },
  "planApprovedAt": "2026-08-18T03:06:26.815Z",
  "workStartedAt": "2026-08-18T03:21:34.494Z",
  "reviewStartedAt": "2026-08-18T04:11:13.229Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\n$ vitest run --root . --config package.json src\n Test Files  130 passed (130)\n      Tests  2801 passed (2801)\n   Duration  13.26s\nGATE_EXIT=0\n\nRun fresh at HEAD 39a1b2c after both QA-required re-drills were restored and byte-identity re-verified. 2801 is +2 on the 2799 QA measured: the create-less case and its empty-project control. Planning slice alone: 4 files / 61 tests (was 59).",
    "reportedAt": "2026-08-18T04:11:13.229Z"
  },
  "filesModified": [
    "apps/daemon/src/planning/active-graph-projection.test.ts",
    "apps/daemon/src/planning/graph-body-record.test.ts",
    "apps/daemon/src/planning/graph-body-record.ts",
    "apps/daemon/src/planning/graph-body-record.js",
    "apps/daemon/src/planning/active-graph-projection.ts"
  ],
  "reviewCompletedAt": "2026-08-18T04:24:34.472Z",
  "rejectionDetails": {
    "failedDodItems": [
      "No active revision returns an exact stable UNKNOWN/not-found code and layer; corrupt, non-canonical, stale, or multiple-active histories fail closed under distinct stable code/layer mappings and yield no snapshot authority.",
      "Focused tests use a real SqliteEventStore, assert exact refusal code and layer, prove generated hostile-case counts are nonzero, and include one accepted active-graph control read from production events rather than a helper reimplementation."
    ],
    "issues": [
      {
        "type": "missing_feature",
        "file": "apps/daemon/src/planning/active-graph-projection.ts",
        "line": 119,
        "description": "enumerateAggregateIds discovers aggregates ONLY by the GraphRevisionCreated event, so an aggregate whose create event is absent is never replayed. Probe over a real store: empty project, DRAFT-only project, and a create-less-but-ACTIVATED history all return the SAME refusal - code=ACTIVE_GRAPH_ABSENT layer=ACTIVE_GRAPH_PROJECTION sourceLayer=null sourceCode=null. DoD 2 requires a corrupt history to map to a DISTINCT code/layer, not to the answer a clean empty project gives."
      },
      {
        "type": "missing_feature",
        "file": "apps/daemon/src/planning/active-graph-projection.ts",
        "line": 123,
        "description": "Fix direction: enumerate by a means a create-less history cannot escape. store.readEventsAfter(cursor, limit) exists and lets you keep every aggregateId matching the graph-revision:<projectId>: prefix; enumerating on all graph-revision event kinds also works. The create-less aggregate then reaches replayGraphRevisionEvents and surfaces GRAPH_REVISION_REPLAY_MISSING_CREATE with sourceLayer=CORE_GRAPH_REVISION_REPLAY. Add a test asserting it differs from ACTIVE_GRAPH_ABSENT."
      },
      {
        "type": "test_failure",
        "file": "apps/daemon/src/planning/active-graph-projection.test.ts",
        "line": 475,
        "description": "The hostile sweep asserts only read.ok===false, read.layer===ACTIVE_GRAPH_PROJECTION_LAYER and typeof read.code==='string'. The layer assertion is VACUOUS: refuse() at active-graph-projection.ts:100 hardcodes layer on every refusal, so it can never hold another value. Net, the only live assertion is 'it did not succeed' - exactly what epic rail 6 and DoD 4 ('assert exact refusal code and layer') forbid, and two layers can refuse here."
      },
      {
        "type": "test_failure",
        "file": "apps/daemon/src/planning/active-graph-projection.test.ts",
        "line": 458,
        "description": "Codes the sweep leaves unpinned - three codes, two layers: 'missing create' -> ACTIVE_GRAPH_ABSENT / sourceLayer=null; 'reversed' -> GRAPH_REVISION_REPLAY_MISSING_CREATE / sourceLayer=CORE_GRAPH_REVISION_REPLAY; 'activation before submission' -> GRAPH_REVISION_REPLAY_VERSION_BREAK / sourceLayer=CORE_GRAPH_REVISION_REPLAY. Carry expected code AND sourceLayer per case-table entry and assert both. Keep the nonzero-case-count assertion."
      },
      {
        "type": "other",
        "file": "apps/daemon/src/planning/active-graph-projection.ts",
        "line": 162,
        "description": "Re-drill before completing. Drill A: make collectActive swallow the replay refusal and skip the aggregate - the sweep must go RED on a CODE assertion, not on ok===false alone; if it stays green the sweep is still detached from its subject. Drill B: after the enumeration fix, revert enumeration to create-event-only - the new create-less test must go RED on its code assertion. A drill that reddens by crashing or on another assertion proves nothing."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "REJECT on DoD 2 and DoD 4. Both MEASURED with probes over a real SqliteEventStore, not inferred from reading.\n\nDEFECT 1 (DoD 2, production). A corrupt/truncated history is INDISTINGUISHABLE from an empty project. Probe, three situations, one identical answer:\n  EMPTY_STORE :: code=ACTIVE_GRAPH_ABSENT layer=ACTIVE_GRAPH_PROJECTION src=null srcCode=null\n  DRAFT_ONLY  :: same\n  NO_CREATE   :: same\nNO_CREATE holds the submitted+activated events with GraphRevisionCreated absent. enumerateAggregateIds keys discovery on that one event, so the aggregate never reaches replayGraphRevisionEvents and its dedicated GRAPH_REVISION_REPLAY_MISSING_CREATE never fires. DoD 2 requires corrupt histories to map to DISTINCT code/layer. Your step-7 attacker pass answered \"Do two causes collapse into one code? NO\" - measurement contradicts it. Your own sweep lists \"missing create\" as a hostile case, so this is in scope.\n\nDEFECT 2 (DoD 4 + epic rail 6, tests). The hostile sweep asserts nothing but \"it did not succeed\" - which is why defect 1 stayed invisible. See issues.\n\nVERIFIED GOOD, do not rework:\n- pnpm --filter @moe/daemon test -> Test Files 130 passed (130) | Tests 2799 passed (2799), exit 0. Your two suites by name: 2 files / 20 tests.\n- Per-file 191 and 213 lines, under 250.\n- Drill (delete post-decode identity check) reddens the misfiled-body test. Drill (split-brain >1 -> >2) reddens line 360, the exact SPLIT_BRAIN code assertion. Both load-bearing; files restored byte-identical (sha256 + empty numstat).\n- Real store, real reduceGraphRevision seeding, zero local hashing, delegation pinned by the IDENTITY_CONFLICT test.\n- Missing commit NOT counted against you: 9b9e44e swallowed your six paths; worktree bytes match HEAD.\n- apps/daemon typecheck RED (TS6133 in src/work/foundation-context-manifest-codec.ts) - UNTRACKED peer file, not attributed to you, not yours to fix.",
      "rejectedAt": "2026-08-18T04:04:07.843Z",
      "reopenCount": 1,
      "failedDodItems": [
        "No active revision returns an exact stable UNKNOWN/not-found code and layer; corrupt, non-canonical, stale, or multiple-active histories fail closed under distinct stable code/layer mappings and yield no snapshot authority.",
        "Focused tests use a real SqliteEventStore, assert exact refusal code and layer, prove generated hostile-case counts are nonzero, and include one accepted active-graph control read from production events rather than a helper reimplementation."
      ],
      "issues": [
        {
          "type": "missing_feature",
          "file": "apps/daemon/src/planning/active-graph-projection.ts",
          "line": 119,
          "description": "enumerateAggregateIds discovers aggregates ONLY by the GraphRevisionCreated event, so an aggregate whose create event is absent is never replayed. Probe over a real store: empty project, DRAFT-only project, and a create-less-but-ACTIVATED history all return the SAME refusal - code=ACTIVE_GRAPH_ABSENT layer=ACTIVE_GRAPH_PROJECTION sourceLayer=null sourceCode=null. DoD 2 requires a corrupt history to map to a DISTINCT code/layer, not to the answer a clean empty project gives."
        },
        {
          "type": "missing_feature",
          "file": "apps/daemon/src/planning/active-graph-projection.ts",
          "line": 123,
          "description": "Fix direction: enumerate by a means a create-less history cannot escape. store.readEventsAfter(cursor, limit) exists and lets you keep every aggregateId matching the graph-revision:<projectId>: prefix; enumerating on all graph-revision event kinds also works. The create-less aggregate then reaches replayGraphRevisionEvents and surfaces GRAPH_REVISION_REPLAY_MISSING_CREATE with sourceLayer=CORE_GRAPH_REVISION_REPLAY. Add a test asserting it differs from ACTIVE_GRAPH_ABSENT."
        },
        {
          "type": "test_failure",
          "file": "apps/daemon/src/planning/active-graph-projection.test.ts",
          "line": 475,
          "description": "The hostile sweep asserts only read.ok===false, read.layer===ACTIVE_GRAPH_PROJECTION_LAYER and typeof read.code==='string'. The layer assertion is VACUOUS: refuse() at active-graph-projection.ts:100 hardcodes layer on every refusal, so it can never hold another value. Net, the only live assertion is 'it did not succeed' - exactly what epic rail 6 and DoD 4 ('assert exact refusal code and layer') forbid, and two layers can refuse here."
        },
        {
          "type": "test_failure",
          "file": "apps/daemon/src/planning/active-graph-projection.test.ts",
          "line": 458,
          "description": "Codes the sweep leaves unpinned - three codes, two layers: 'missing create' -> ACTIVE_GRAPH_ABSENT / sourceLayer=null; 'reversed' -> GRAPH_REVISION_REPLAY_MISSING_CREATE / sourceLayer=CORE_GRAPH_REVISION_REPLAY; 'activation before submission' -> GRAPH_REVISION_REPLAY_VERSION_BREAK / sourceLayer=CORE_GRAPH_REVISION_REPLAY. Carry expected code AND sourceLayer per case-table entry and assert both. Keep the nonzero-case-count assertion."
        },
        {
          "type": "other",
          "file": "apps/daemon/src/planning/active-graph-projection.ts",
          "line": 162,
          "description": "Re-drill before completing. Drill A: make collectActive swallow the replay refusal and skip the aggregate - the sweep must go RED on a CODE assertion, not on ok===false alone; if it stays green the sweep is still detached from its subject. Drill B: after the enumeration fix, revert enumeration to create-event-only - the new create-less test must go RED on its code assertion. A drill that reddens by crashing or on another assertion proves nothing."
        }
      ]
    }
  ],
  "completedAt": "2026-08-18T04:24:34.472Z",
  "reviewSummary": "APPROVED at HEAD d60a48f. Both prior defects re-MEASURED fixed, not read.\n\nDEFECT 1 (DoD 2) FIXED: enumerateAggregateIds pages store.readEventsAfter, keeping every aggregateId with the graph-revision:<projectId>: prefix. Create-less history now answers GRAPH_REVISION_REPLAY_MISSING_CREATE / sourceLayer=CORE_GRAPH_REVISION_REPLAY, distinct from empty (ACTIVE_GRAPH_ABSENT/null). Pagination audited: nextCursor strictly advances, zero-select returns hasMore=false, oversized record throws not truncates - no loop, no silent stop. Prefix cannot leak across projects: store is project-bound (assertCachedProjectBinding).\n\nDEFECT 2 (DoD 4) FIXED: sweep carries expected code AND sourceLayer per case; asserts length>0, ===3, distinct names, none may be ACTIVE_GRAPH_ABSENT.\n\nMY 3 DRILLS, all killed on the intended assertion:\nA create-event-only enumeration -> 2 RED on CODE (\"missing create code: expected ACTIVE_GRAPH_ABSENT to be GRAPH_REVISION_REPLAY_MISSING_CREATE\").\nB collectActive swallows replay refusal -> 3 RED on CODE (GREEN under the old vacuous sweep).\nC refuse(code,null) -> 3 RED on sourceLayer (\"expected null to be CORE_GRAPH_REVISION_REPLAY\") - live, not decorative.\n\nGATES re-run by me, foreground: daemon typecheck exit 0 (prior TS6133 gone); daemon test Test Files 130 passed (130) | Tests 2801 passed (2801) exit 0; planning slice 4 files/61 tests exit 0; pnpm typecheck exit 0. Files 219 and 191 lines. graph-body-record.* byte-identical to previously-verified bytes. Consumers named, on disk, all BLOCKED (c4b80778/eacea969/32c1ba45).\n\nDISCLOSED not counted: test:security 96-vs-93; unrostered = this task's 2 layer constants + peer IMPORT_SHADOW_READ_LAYER. Already red at 94-vs-93; bumping the roster mid-peer-flight entangles counts. Needs follow-up roster task.\n\nQA REPAIR: foreign whole-tree commit fbbabda captured my drill A mid-run. Restored reviewed bytes in d60a48f (explicit pathspec, 1 file/1 line, no amend/reset). All six owned paths: HEAD == reviewed bytes."
}