{
  "id": "task-cb40f3d833f14c3089d812b9bf2d291f",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "Compose keyed live pairing and close Control Room runtime-provider arms",
  "description": "SPIDR runtime-provider producer+consumer+arms row authorized by governor comment-7b46f67b32864ba09619355782a69e2a on parent task-f8ea0a2f6f3a4d6f85bd92b8cb9f261d. Fresh grep at HEAD 438caef30a96f080b5addc4bcd394bb903d70839 found PRD_LOCAL_LAYER and SESSION_KEY_LAYER absent from the master boundary roster, and found generateSessionKey/openSessionRequestDigest/signSessionChallenge referenced only by the producer barrel/tests/E2E—not by a production Control Room call site. This row closes the real consumer edge: the live pairing flow uses the landed browser key module, presents the public key on the approved claim, signs the daemon-issued challenge, and completes /session/pair/open; failures remain fail-closed. It also extracts the daemon's proof framing into the existing browser-safe contracts canonical module rather than reimplementing it. Exact owned path set (10): packages/contracts/src/session-authority-canonical.ts; packages/contracts/src/session-authority-canonical.js; packages/contracts/src/index.ts; apps/daemon/src/identity/session-authority-contracts.ts; apps/daemon/src/identity/session-authority-protocol.ts; apps/control-room/src/live/live-keyed-session.ts (new); apps/control-room/src/live/live-keyed-session.test.ts (new); apps/control-room/src/live/live-handshake.ts; apps/control-room/src/v2/goals/use-goal-prd.ts; tests/security/runtime-provider-control-room.security.ts (new). Master roster and runtime partition edits are deferred to the serialized final integrity/roster row.",
  "definitionOfDone": [
    "Verify at current HEAD that task-1199836b's browser key functions and task-2f554e29's challenge/open route are landed; record `git grep -l` showing no production Control Room caller yet. Verify the existing @moe/control-room -> @moe/control-room-client and @moe/contracts manifest edges, pnpm-lock importer entries, and a temporary compiled bare-specifier probe deleted in the same command.",
    "Red-first extract canonicalSessionProofBytes plus its proof algorithm/protocol constants into the existing dependency-free contracts canonical module; daemon contracts/protocol re-export or compose the shared surface without changing their public signatures. Existing daemon verifier tests and a browser-side equality arm must prove byte identity; no second canonicalizer or node: import reaches the browser.",
    "A new <=250-line live-keyed-session module owns key generation, approved-claim material, challenge signing, and bounded /session/pair/open completion; live-handshake only orchestrates it and stays below 400. The real production flow consumes the existing @moe/control-room-client functions, keeps the private key browser-only/non-extractable, and returns no live setup until the signed open succeeds.",
    "Add nonzero BEFORE/AFTER/RACE production arms for SESSION_KEY_LAYER and PRD_LOCAL_LAYER with exact code/layer: generateKey throw and non-keypair/wrong-SPKI all refuse SESSION_KEY_ALGORITHM_UNSUPPORTED@CONTROL_ROOM_SESSION_KEY; an oversized PRD never calls text(), an otherwise-valid read throw refuses PRD_FILE_UNREADABLE@CONTROL_ROOM_NEWGOAL, and last-selection race cannot publish stale bytes. The PRD seam is an extracted pure reader called by useGoalPrd, not a helper that reimplements it.",
    "Focused contracts, daemon protocol, control-room live-keyed-session, and runtime-provider security gates run standalone/unpiped with nonzero Test Files/Tests lines. Mutate the shared proof framing, keypair guard, PRD size/read guard, and one generated arm denominator; each named assertion must red, then prove exact restore. Broad app/package red is merge-base-versus-HEAD path-attributed."
  ],
  "taskRails": [
    "Own exactly the ten listed paths; new files are live-keyed-session.ts, live-keyed-session.test.ts, and runtime-provider-control-room.security.ts. Do not edit BOUNDARY_ROSTER or runtime-provider-invariants in this row.",
    "This is the missing real consumer edge for DONE task-1199836b, not a mock-backed re-proof. A key generated only in a test or a signed request assembled only by the E2E spec does not satisfy the row.",
    "Private key material never leaves the browser, enters logs, request bodies, headers, or storage. Public SPKI and signatures follow the landed exact protocol; no alternate verifier or canonicalizer.",
    "Keep contracts dependency-free, daemon verifier authoritative, and live-handshake below 400 by placing the keyed flow in the new focused module. Preserve all foreign WIP and explicit path ownership."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "RE-ANCHOR AND ESTABLISH THAT THE WORK IS LANDED (DoD 1). No source edits in this row unless a gap is measured.\n\n(a) Record `git rev-parse HEAD` (measured 8c21fd48). The description anchors 438caef3 and is stale by many commits.\n\n(b) Adopt the DONE dependencies from epicSiblings rather than re-measuring their work: task-d152f559, task-28cd95db, task-87f13cba, plus the producers task-1199836b and task-2f554e29. **Two of them landed this row's deliverable**, which is why it was parked on 28cd's provenance:\n  `751d75a1` (task-d37e8873) extracted the canonicalisation into @moe/contracts -> DoD 2\n  `79315504` (task-d152f559) extracted browser-safe proof bytes and pinned session-key guards -> DoD 2 + 4\n  `1729c429` 'open keyed sessions' -> DoD 3, the consumer edge; the same SHA recorded as task-28cd95db's completion\n\n(c) Re-measure all ten owned paths with `git status --porcelain` and `wc -l`. ARCHITECT MEASURED: all ten EXIST and are CLEAN, including the three the description calls 'new' — live-keyed-session.ts 222, live-keyed-session.test.ts 297, runtime-provider-control-room.security.ts 358. **Do NOT create them again**; re-implementing a landed surface is a silent duplicate that collides with the rows that shipped it. Confirm the caps DoD 3 names are already met: the keyed module <=250 (222) and live-handshake <400 (280).\n\n(d) **CORRECT DoD 1's GREP CLAUSE RATHER THAN SATISFYING IT.** It asks for `git grep -l` showing NO production Control Room caller. That is now FALSE, and its falsity is the deliverable (rail 2). Record the caller instead, by file and line: `live-handshake.ts:7` imports `createLiveKeyedSession`, `:201` calls it; and live-handshake is production-reachable from `apps/control-room/src/main.tsx` and `src/v2/cordum-handshake.tsx`. If the caller had been absent, THAT would be the row's work.\n\n(e) Discharge the rest of DoD 1 properly: verify the @moe/control-room -> @moe/control-room-client and @moe/contracts manifest edges AND the pnpm-lock importer entries, then create, typecheck and DELETE a temporary compiled bare-specifier probe in ONE command, recording its FULL SOURCE in the step note. A deleted probe's transcript alone is a lead, not evidence; the module's own bare imports do not discharge this clause.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/live/live-keyed-session.ts",
        "apps/control-room/src/live/live-handshake.ts",
        "packages/contracts/src/index.ts",
        "packages/contracts/src/session-authority-canonical.ts"
      ],
      "note": "DoD 1 DISCHARGED — verification only, ZERO source edits, zero files modified.\n\n(a) ANCHOR. `git rev-parse HEAD` -> 8c21fd48a8c4cd3e47aca4eef6c520237e201e65 on branch moe/work-2026-09-02. Task description anchors 438caef3 and is stale; 8c21fd48 is the measured anchor for every claim below. Root asserted before measuring: git toplevel and MOE_PROJECT_PATH both normalize to /d/projexts/moe-next (both sides through cygpath -u, not one).\n\n(b) DEPENDENCIES ADOPTED, NOT RE-MEASURED. All three named SHAs are ancestors of HEAD (`git merge-base --is-ancestor <sha> HEAD` -> YES for each):\n  751d75a1 feat(task-d37e8873): extract the pure session-authority canonicalisation  -> DoD 2\n  79315504 feat(task-d152f559...): extract browser-safe session proof bytes          -> DoD 2 + 4\n  1729c429 feat(control-room): open keyed sessions                                    -> DoD 3\nProducer task-1199836b landed via 18b1493d (wip ... [status=DONE role=qa cli-exit=0]). epicSiblings confirms task-d152f559, task-28cd95db, task-87f13cba all DONE landed=true, dependsOnUnmet=[].\n\n(c) TEN OWNED PATHS: ALL EXIST, ALL TRACKED, ALL CLEAN. `git status --porcelain -- <ten paths>` returned EMPTY. wc -l:\n  live-keyed-session.ts 222 | live-keyed-session.test.ts 297 | live-handshake.ts 280 | use-goal-prd.ts 138\n  contracts/src/index.ts 233 | session-authority-canonical.ts 177 | session-authority-canonical.js 1\n  session-authority-contracts.ts 215 | session-authority-protocol.ts 295 | runtime-provider-control-room.security.ts 358\nThe three paths the description calls \"new\" already exist; I did NOT create them. Both caps DoD 3 names are already met: keyed module 222 <= 250, live-handshake 280 < 400.\n\n(d) DoD 1's GREP CLAUSE IS FALSE, AND ITS FALSITY IS THE DELIVERABLE (rail 2). It asks for `git grep -l` showing NO production Control Room caller. Measured at 8c21fd48, the caller EXISTS:\n  apps/control-room/src/live/live-handshake.ts:7   import { createLiveKeyedSession } from \"./live-keyed-session.js\";\n  apps/control-room/src/live/live-handshake.ts:201 const keyed = createLiveKeyedSession({\n  (definition: apps/control-room/src/live/live-keyed-session.ts:179)\nlive-handshake is production-reachable, not test-only — `resolveLiveSetupFromHandshake` is imported at apps/control-room/src/main.tsx:9 and apps/control-room/src/v2/cordum-handshake.tsx:4. So the consumer edge rail 1 of this task demands (\"not a mock-backed re-proof\") is real production code. Had the caller been absent, THAT would have been this row's work.\n\n(e) MANIFEST + LOCK + COMPILED BARE-SPECIFIER PROBE.\nManifest edges, apps/control-room/package.json dependencies: \"@moe/contracts\": \"workspace:*\" and \"@moe/control-room-client\": \"workspace:*\" both present.\npnpm-lock.yaml importer `apps/control-room:` -> '@moe/contracts' specifier workspace:* version link:../../packages/contracts ; '@moe/control-room-client' specifier workspace:* version link:../../packages/control-room-client.\nPROBE — created, typechecked and DELETED in ONE command, with a PLANTED-ERROR CONTROL first, because a probe that compiles green proves nothing unless tsc is shown to actually read it. FULL SOURCE of both legs:\n\nLEG A (planted error, control):\n  import { SESSION_PROOF_ALGORITHM, canonicalSessionProofBytes } from \"@moe/contracts\";\n  import { generateSessionKey } from \"@moe/control-room-client\";\n  const alg: number = SESSION_PROOF_ALGORITHM;\n  export const probe = [alg, typeof canonicalSessionProofBytes, typeof generateSessionKey];\n  -> $ tsc --project tsconfig.json\n     src/__bare_specifier_probe.ts(3,7): error TS2322: Type 'string' is not assignable to type 'number'.\n     exit=1  <- tsc NAMED THE PROBE FILE: the compiler demonstrably compiled it\n\nLEG B (correct, identical imports, `const alg: string`):\n  -> $ tsc --project tsconfig.json\n     exit=0, no diagnostics  <- both BARE specifiers resolve under real compilation, not just at runtime\n\nThe `$ tsc --project tsconfig.json` child echo is the execution evidence; the exit code alone would not distinguish \"compiled clean\" from \"compiled nothing\". Probe removed: `ls` -> \"No such file or directory\"; `git status --porcelain -- apps/control-room/src` shows no probe path. The module's own bare imports were NOT treated as discharging this clause.\n\nFOREIGN WIP OBSERVED AND PRESERVED (untouched, not mine, disclosed for attribution): M apps/control-room/src/v2/styles/cordum-goals.css; ?? apps/control-room/src/data/data-contract.js, src/timeline/timeline-contract.js, src/timeline/timeline-page.js, src/v2/projects/project-manager-client.js.",
      "startedAt": "2026-09-02T20:09:55.305Z",
      "completedAt": "2026-09-02T20:11:22.489Z"
    },
    {
      "stepId": "step-2",
      "description": "VERIFY DoD 2 AND DoD 3 — SINGLE-SOURCED CANONICALIZER AND A REAL, SECRET-SAFE CONSUMER EDGE. Verify; do not rebuild.\n\n(a) DoD 2 — ONE canonicalizer, byte-identical across both sides. Confirm `canonicalSessionProofBytes` lives only in `packages/contracts/src/session-authority-canonical.ts` (177 lines), is barrel-exported at `contracts/src/index.ts:230-233`, and that `session-authority-canonical.js` is exactly the one-line bridge `export * from \"./session-authority-canonical.ts\";`. Confirm the daemon's `session-authority-contracts.ts` / `session-authority-protocol.ts` re-export or COMPOSE that shared surface without changing their public signatures. Prove NO second canonicalizer exists and that **no `node:` import reaches the browser** — grep the contracts module and its transitive imports; contracts must stay dependency-free (rail 4). Run the daemon verifier tests plus the browser-side equality arm and confirm byte identity is actually asserted, not assumed.\n\n(b) DoD 3 — the flow. Confirm live-keyed-session owns key generation, approved-claim material, challenge signing and bounded `/session/pair/open` completion, that live-handshake ONLY orchestrates it, and that **no live setup is returned until the signed open succeeds** (a failure path that still yields setup would be the fail-closed defect this row exists to prevent).\n\n(c) **RAIL 3 IS A SECURITY INVARIANT — VERIFY IT, DO NOT ASSUME IT.** A module can pass its own happy-path tests and still leak, because a passing assertion is not an absence claim. Check on the production surface that: the private key is generated non-extractable; only the public SPKI and the signature are placed in request bodies, headers or any logged value; and no key material reaches storage. If any of these is unproven by a surviving assertion, say so plainly — an unproven secret-handling claim is a finding, not a pass.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/contracts/src/session-authority-canonical.ts",
        "packages/contracts/src/session-authority-canonical.js",
        "apps/daemon/src/identity/session-authority-contracts.ts",
        "apps/daemon/src/identity/session-authority-protocol.ts",
        "apps/control-room/src/live/live-keyed-session.ts"
      ],
      "note": "DoD 2 + DoD 3 + rail 3 VERIFIED at 8c21fd48. Zero source edits; nothing rebuilt. All measurements below are at HEAD, root /d/projexts/moe-next (asserted, both sides normalized).\n\n(a) DoD 2 — ONE CANONICALIZER, BYTE IDENTITY ASSERTED NOT ASSUMED.\nSole definition: `packages/contracts/src/session-authority-canonical.ts:160` (177 lines). Barrel-exported at `packages/contracts/src/index.ts:225-233`. `.js` bridge is exactly the one line `export * from \"./session-authority-canonical.ts\";`.\nThe daemon COMPOSES, it does not reimplement: `session-authority-protocol.ts:12` imports `canonicalSessionProofBytes as sharedCanonicalSessionProofBytes`, and its public `:140 export function canonicalSessionProofBytes(fields): Uint8Array` is a one-line delegation `return sharedCanonicalSessionProofBytes(fields);` — same name, same signature, public surface unchanged. `session-authority-contracts.ts:5,18,23` re-exports the shared symbols and adds only daemon-side constants; it defines no proof framing.\nSECOND-CANONICALIZER CHECK, and one candidate had to be RULED OUT rather than waved past: the daemon keeps a local `framed()` at `session-authority-protocol.ts:134`. Its ONLY caller is `:171`, hashing `framed(REPLAY_DOMAIN, REPLAY_ORDER, raw)` — the replay-marker digest, a different domain and a different field order. It is never applied to SESSION_PROOF_DOMAIN, and the suite pins that: protocol.test.ts asserts `daemonProtocolSource).not.toMatch(/\\bframed\\s*\\(\\s*SESSION_PROOF_DOMAIN\\b/u)` plus no daemon-local `SESSION_PROOF_(PROTOCOL_VERSION|ALGORITHM|DOMAIN)` const in either daemon file. So `framed` is not a second proof canonicalizer, and the fence against it becoming one survives in the suite.\nBROWSER SAFETY / rail 4: `session-authority-canonical.ts` has ZERO import statements of any kind. Across ALL contracts production sources (`packages/contracts/src/**/*.ts` minus `*.test.ts`) there is NOT ONE `node:` specifier — the only `node:` hits in contracts/src are in test files (bounded-json-hostile.test.ts, project-configuration.test.ts), which never reach a browser bundle. Contracts stays dependency-free.\nBYTE IDENTITY IS ACTUALLY ASSERTED, both directions:\n  daemon `session-authority-protocol.test.ts:62-67` — `expect([...throughDaemon]).toEqual([...throughShared])`, an element-wise byte comparison, not a shape check.\n  browser `live-keyed-session.test.ts:151-162` — stronger than equality: the test re-derives bytes from the SHARED `canonicalSessionProofBytes` and verifies the production module's Ed25519 signature over them with `crypto.subtle.verify(...)` -> `toBe(true)`. A signature only verifies against the exact bytes that were signed, so this proves the production path signed the shared canonicalizer's output rather than agreeing by coincidence.\nRAN BOTH, standalone and unpiped:\n  `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/identity/session-authority-protocol.test.ts` -> Test Files 1 passed (1) / Tests 7 passed (7), EXIT=0\n  `pnpm --filter @moe/control-room exec vitest run src/live/live-keyed-session.test.ts` -> Test Files 1 passed (1) / Tests 7 passed (7), EXIT=0\nCount lines read, not just exit codes — \"No test files found\" also exits 0.\n\n(b) DoD 3 — THE FLOW, AND IT FAILS CLOSED.\n`live-keyed-session.ts` (222 lines) owns all four: key generation (`createLiveKeyedSession:179` memoizes `generateSessionKey()`), approved-claim material (`admitClaim:97`, exact-key sets `CLAIM_KEYS`/`CHALLENGE_KEYS`, project and protocolVersion equality), challenge signing (`buildOpen:142` -> `openSessionRequestDigest` + `canonicalSessionProofBytes` + `signSessionChallenge:168`), and bounded `/session/pair/open` completion.\n`live-handshake.ts` (280 lines) ONLY orchestrates: `:7` imports, `:201` constructs, and supplies the bounded `post`. No proof logic there.\nNO LIVE SETUP BEFORE THE SIGNED OPEN SUCCEEDS — traced every exit of `claimAndOpen`: key failure -> refuse; claim throw -> refuse; `!claimed.ok` -> RETRY (only for the exact 409 + CONTROL_ROOM_PAIRING_APPROVAL + PAIRING_APPROVAL_REQUIRED|PAIRING_REQUEST_BUSY shape) else refuse; admit failures -> three distinct refusals; buildOpen throw -> refuse; open throw -> refuse; `!opened.ok` -> refuse; and even on `ok`, the body must have EXACTLY `OPEN_RESULT_KEYS`, `ok===true`, matching protocolVersion, and a `sessionId` equal to the one this client minted — else refuse. `{ ok: true, sessionCredential }` is returned on exactly ONE path, after all of that. The fail-closed defect this row exists to prevent is not present.\n\n(c) RAIL 3 — VERIFIED ON THE PRODUCTION SURFACE, WITH SURVIVING ASSERTIONS. Not a pass by assumption.\n  NON-EXTRACTABLE: `packages/control-room-client/src/session-key.ts:108` — `crypto.subtle.generateKey({ name: \"Ed25519\" }, false, [\"sign\", \"verify\"])`. The `false` is the extractable flag on the production call. Asserted, two-sided: `session-key.test.ts:64` `expect(result.privateKey.extractable).toBe(false)` and `:84-87` proves `exportKey(\"pkcs8\", priv",
      "startedAt": "2026-09-02T20:11:25.062Z",
      "completedAt": "2026-09-02T20:13:25.815Z"
    },
    {
      "stepId": "step-3",
      "description": "VERIFY DoD 4 — THE ARMS EXIST; PROVE THEY ASSERT THE RIGHT THINGS.\n\ntests/security/runtime-provider-control-room.security.ts (358 lines, clean) already declares `SESSION_KEY_BOUNDARY` (:28) and `PRD_LOCAL_BOUNDARY` (:29) with BEFORE/AFTER/RACE arms and a refusal ledger (:95-116), importing the real constants — `PRD_LOCAL_LAYER` = `\"CONTROL_ROOM_NEWGOAL\"` (new-goal-form-model.ts:7) and `SESSION_KEY_LAYER` = `\"CONTROL_ROOM_SESSION_KEY\"` (session-key.ts:44). Confirm each required property is actually asserted:\n\n(a) SESSION KEY: a `generateKey` throw AND a non-keypair / wrong-SPKI input each refuse with the EXACT `SESSION_KEY_ALGORITHM_UNSUPPORTED@CONTROL_ROOM_SESSION_KEY` — both the code and the layer, never merely an unsuccessful result.\n\n(b) PRD, and these are ABSENCE claims that a passing suite can silently omit:\n  - an oversized PRD **never calls `text()`** — assert the call count is ZERO, not merely that it refused; a refusal after reading the bytes is a different (and worse) behaviour;\n  - an otherwise-valid read throw refuses `PRD_FILE_UNREADABLE@CONTROL_ROOM_NEWGOAL`;\n  - the last-selection RACE cannot publish stale bytes — assert on the outcome shape per leg, never on which leg wins.\n\n(c) RAIL 4 / DoD 4: the PRD seam must be an EXTRACTED PURE READER that `use-goal-prd.ts` CALLS, not a helper that reimplements it. Verify the production call site in use-goal-prd.ts (138 lines), not merely the reader's existence — a reader nothing calls satisfies every existence grep while the behaviour lives elsewhere.\n\n(d) Confirm the generated arms assert a NONZERO exact case count; a sweep that silently yields zero cases passes while testing nothing.\n\nIf any named property is missing, adding that assertion to the owned security file is in scope; rebuilding the file is not.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/security/runtime-provider-control-room.security.ts",
        "apps/control-room/src/v2/goals/use-goal-prd.ts"
      ],
      "note": "DoD 4 VERIFIED — every named property IS asserted. Nothing was missing, so nothing was added; zero edits to the owned security file.\n\nCONSTANTS ARE THE REAL PRODUCTION ONES, NOT LOCAL COPIES. `runtime-provider-control-room.security.ts:9` imports `PRD_LOCAL_LAYER` from `apps/control-room/src/v2/goals/new-goal-form-model.js` and `:16-18` imports `SESSION_KEY_LAYER` + `generateSessionKey` from `packages/control-room-client/src/session-key.js`. `describe(SESSION_KEY_LAYER)` / `describe(PRD_LOCAL_LAYER)` name the suites off those imported values, so a renamed layer moves the suite title rather than passing silently.\n\n(a) SESSION KEY — EXACT CODE **AND** LAYER, ALL THREE ARMS.\nThe grading operand is `REFUSAL = { code: \"SESSION_KEY_ALGORITHM_UNSUPPORTED\", layer: SESSION_KEY_LAYER, ok: false }` (:34-38) and `assertExactRefusal` (:78-85) ends in `expect(actual).toStrictEqual(REFUSAL)` — whole-object strict equality, so it grades the code and the layer together, never a bare \"unsuccessful result\". Both required inputs are present:\n  generateKey THROW -> BEFORE (:91-119): `generateKey` rejects with `DOMException(..., \"NotSupportedError\")`; both probe and effect exact-refuse; `generateKey` called 2x; `exportKey` and `digest` NOT called.\n  NON-KEYPAIR -> AFTER (:126-155): `generateKey` yields `{ privateKey: Symbol }` with NO publicKey, and `:148-151` pins the shape `toStrictEqual([[\"privateKey\"], [\"privateKey\"]])`. Both outcomes exact-refuse; `exportKey`/`digest` never reached.\n  WRONG SPKI -> RACE (:157-210): two concurrent legs export 43- and 45-byte SPKI against the production 44; `expect(exportedWidths.sort(...)).toStrictEqual([43, 45])`, `arrivalsAtRelease === 2` proves the legs genuinely overlapped, both sides exact-refuse, `digest` never called.\n\n(b) PRD — THE TWO ABSENCE CLAIMS ARE REAL ABSENCE CLAIMS.\n  OVERSIZED NEVER CALLS `text()`: BEFORE (:214-240) builds a `File` of `PRD_FILE_PREFLIGHT_MAX_BYTES + 1` with `text` replaced by a spy, then asserts `expect(text).not.toHaveBeenCalled()` — a ZERO CALL COUNT, not merely a refusal. Refusal graded by `toStrictEqual({code:\"PRD_FILE_TOO_LARGE\", layer:\"CONTROL_ROOM_NEWGOAL\", status:\"ERROR\"})`, and `digest`/`fetch` also proven unreached. A refusal issued after reading the bytes would red this.\n  READ THROW: AFTER (:241-266) — valid-size file whose `text()` rejects, asserted `toStrictEqual({code:\"PRD_FILE_UNREADABLE\", layer:\"CONTROL_ROOM_NEWGOAL\", status:\"ERROR\"})`, with `text` called exactly 1x (proving the throw was reached, not skipped) and `digest`/`fetch` unreached.\n  RACE ASSERTS OUTCOME SHAPE, NEVER WHICH LEG WINS: (:268-318) renders the real `useGoalPrd` under jsdom, holds the stale file's `digest` open on a manual promise, selects a second (unreadable) file, then RELEASES the stale digest. Grading is entirely on the published state — `result.current.read` is the unreadable refusal, `result.current.prd` is `null`, `result.current.submittedPrd` is `undefined`, `digest` called exactly once, `fetch` never. No assertion anywhere on ordering or on which leg resolves first. Stale bytes cannot publish.\n\n(c) RAIL 4 — THE SEAM IS AN EXTRACTED PURE READER **THAT PRODUCTION CALLS**, verified at the call site rather than by existence.\n`use-goal-prd.ts:82 export async function readGoalPrdFile(file: File)` is a standalone function using no React hooks; `:108 export function useGoalPrd()` is the hook, and inside `acceptFile` (:114) it calls `const result = await readGoalPrdFile(file);` at **:121**. That is the production edge — the hook does not reimplement the read. The security file imports BOTH `readGoalPrdFile` and `useGoalPrd` from the same module (:11-14), so the BEFORE/AFTER arms exercise the reader directly while the RACE arm drives it THROUGH the hook. A reader nothing called would have left the RACE arm green while the behaviour lived elsewhere; it does not.\n\n(d) NONZERO EXACT CASE COUNTS — GUARDED TWICE, AT TWO DIFFERENT LEVELS.\nPer-suite literals: `expect(CASES).toHaveLength(3)` plus `toStrictEqual([\"BEFORE\",\"AFTER\",\"RACE\"])` (:352-355); PRD is explicit about nonzero — `expect(PRD_CASES.length).toBeGreaterThan(0)` AND `toHaveLength(3)` AND the arm tuple (:345-349). Ledger denominator: \"records exactly two refusing outcomes per hostile arm\" pins `[[\"BEFORE\",2],[\"AFTER\",2],[\"RACE\",2]]` (:361-372). Slice level: `describeSliceInvariants(..., OWNED, [], 0)` at :358 adds four more invariants from `runtime-provider-invariants.ts:219-243` — `assertSweepsExactly`, `assertPositiveCounts` (\"records a POSITIVE case count per boundary AND per arm\"), `assertAdmittedNothing`, `assertMessagesEchoNothing`. A sweep that silently yielded zero cases reds `assertPositiveCounts`, so the zero-denominator false green is closed. Arithmetic of the 13: SESSION_KEY 1 length + 3 arms + 1 ledger = 5; PRD 1 length + 3 arms = 4; slice invariants 4. 5+4+4 = 13, matching the run below exactly.\n\nGATE RUN, standalone and unpiped:\n  `pnpm exec vitest run --config tests/security/vitest.config.ts runtime",
      "startedAt": "2026-09-02T20:13:28.390Z",
      "completedAt": "2026-09-02T20:14:46.695Z"
    },
    {
      "stepId": "step-4",
      "description": "DoD 5 — THE FOUR MUTATIONS, THEN THE GATES AND LANDING. This step is what converts four green suites into evidence.\n\n(a) Capture exact-path `git status --porcelain` first. All ten owned paths are TRACKED and CLEAN, so `git diff --exit-code` plus sha256 equality is a valid restore proof for each.\n\n(b) FOUR MUTATIONS, one degree at a time, each reddening ITS OWN named assertion:\n  1. **shared proof framing** in session-authority-canonical.ts — RED: the daemon-versus-browser byte-identity arm. This is the one that proves both sides really share the single canonicalizer rather than agreeing by coincidence.\n  2. **keypair guard** — RED: the `SESSION_KEY_ALGORITHM_UNSUPPORTED@CONTROL_ROOM_SESSION_KEY` arm.\n  3. **PRD size/read guard** — RED: the oversized-never-calls-`text()` arm and the `PRD_FILE_UNREADABLE@CONTROL_ROOM_NEWGOAL` arm.\n  4. **one generated-arm denominator** — delete a case and RED the exact count assertion, naming the missing case rather than only reporting a smaller number.\nA GREEN mutation is a FINDING to report, not a pass to bank — it means the named assertion is vacuous. If a mutation reds a DIFFERENT arm, a downstream fence answered first and that fixture is wrong. Restore each separately, prove the restore, and re-run green before the next.\n\n(c) THE FOUR GATES, standalone and unpiped, quoting every count line. ARCHITECT MEASURED all four green at HEAD:\n  contracts `pnpm exec vitest run packages/contracts/src/session-authority-canonical.test.ts` -> 1 file / 26 tests\n  daemon `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/identity/session-authority-protocol.test.ts` -> 1 file / 7 tests\n  control-room `pnpm --filter @moe/control-room exec vitest run src/live/live-keyed-session.test.ts` -> 1 file / 7 tests\n  security `pnpm exec vitest run --config tests/security/vitest.config.ts runtime-provider-control-room.security.ts` -> 1 file / 13 tests\nRead the OUTPUT, not the exit code — 'No test files found' and 'No projects matched the filters' both exit 0.\n\n(d) Broad app/package red is merge-base-versus-HEAD PATH-ATTRIBUTED: the failing-path delta intersected with the ten owned paths must be EMPTY, and all foreign red disclosed VERBATIM. Root `pnpm test` has no `apps/**` in its include, so only the `--filter` legs evidence the app paths.\n\n(e) RAIL 1: do NOT edit BOUNDARY_ROSTER or runtime-provider-invariants — the serialized final integrity/roster row owns those. Preserve foreign WIP; explicit owned pathspecs only; no `git add -A`, reset or stash. Do not hand-commit; exit through the wrapper.\n\n(f) completionSummary: that the row completed on VERIFICATION because its own dependencies landed the deliverable (naming 751d75a1, 79315504 and 1729c429 against DoD 2/3/4); that DoD 1's 'no production caller' grep is now correctly FALSE, with live-handshake.ts:7 and :201 as the consumer edge reachable from main.tsx; the rail 3 key-confinement finding; the four mutation outcomes; and all four gate count lines. Overflow into row comments, each under ~4500 chars.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/contracts/src/session-authority-canonical.ts",
        "apps/control-room/src/live/live-keyed-session.ts",
        "apps/control-room/src/v2/goals/use-goal-prd.ts",
        "tests/security/runtime-provider-control-room.security.ts",
        "apps/control-room/src/live/live-keyed-session.test.ts"
      ],
      "note": "DoD 5 COMPLETE. Four mutations run, one produced a FINDING; four gates green; broad legs attributed. NET DIFF IS EMPTY — every mutation restored, zero files changed by this row.\n\n(a) HEAD MOVED MID-TASK — RE-ANCHORED. Step 1 anchored 8c21fd48; HEAD is now **083c08aa** (peer landed task-c444b68f, \"[R3-016 browser leaf] Consume daemon planning authority in live dispatch\"). That commit touched only tests/integration/control-room/{dev-payload-parity,live-approval-activation}.test.ts — **intersection with the ten owned paths: EMPTY**. All ten re-measured `git status --porcelain` CLEAN at the new HEAD.\n\nRESTORE-ORACLE CORRECTION, disclosed because it invalidates the method the step names. The step says a pre-mutation sha256 snapshot plus `git diff --exit-code` is a valid restore proof. At my seat it is NOT sufficient: my baseline hash of session-authority-canonical.ts was `dd401f92…`, and after restore the file hashed `2b551b08…` while `git diff --exit-code` reported CLEAN. `2b551b08` is the blob at BOTH 8c21fd48 and 083c08aa, and the other three targets matched their own baselines exactly — so the SNAPSHOT was the unreliable operand, not the restored file (shared worktree, core.autocrlf=true, .gitattributes `* text=auto eol=lf`). I switched every restore proof to the branch-anchored form `sha256sum < <path>` vs `git show HEAD:<path> | sha256sum`, which cannot drift with a stale local snapshot. All four restores pass BOTH that equality and `git diff --exit-code`.\n\n(b) THE FOUR MUTATIONS, one degree each, restored and re-greened separately.\n\n**M1 — shared proof framing. GREEN MUTATION = FINDING, reported not banked.**\nEdit: `session-authority-canonical.ts:149` `setUint32(0, bytes.byteLength, false)` -> `true` (u32 big-endian -> little-endian).\nResult: the arm the step NAMES did not red, and cannot.\n  daemon `session-authority-protocol.test.ts` -> Tests 7 passed (7), EXIT 0 — **GREEN**\n  control-room `live-keyed-session.test.ts` -> Tests 7 passed (7), EXIT 0 — **GREEN**\n  contracts `session-authority-canonical.test.ts` -> **4 failed | 22 passed (26)**, EXIT 1\nThe four contracts reds by name: \"pins the full domain plus u32be-framed field-order oracle\"; \"frames multibyte values by UTF-8 byte length rather than JS string length\"; \"accepts the unsigned numeric boundary: zero\"; \"accepts the unsigned numeric boundary: maximum safe integer\".\nWHY IT CANNOT RED: both operands of each identity arm route through the SAME function. The daemon arm compares `sharedCanonicalSessionProofBytes(f)` against a one-line delegation to it; the browser arm signs with `canonicalSessionProofBytes` and re-derives with the same export before `crypto.subtle.verify`. One edit moves signer and verifier together, so the signature still verifies. An equality between two spellings of one implementation measures the DELEGATION EDGE, never the VALUE.\nSEVERITY: this is a PLAN-ATTRIBUTION defect, NOT a coverage hole. The property is genuinely pinned — by the contracts u32be golden, an external oracle whose operand the mutation cannot move. Recommend the plan name that golden as the expected red. The daemon arm remains valid evidence for \"the daemon delegates\", which is what DoD 2 also needs.\n\n**M2 — keypair guard. RED the named arm.**\nEdit: `packages/control-room-client/src/session-key.ts:87` drop `&& \"publicKey\" in value` (guard weakened by one degree, NOT deleted).\nsecurity gate -> **3 failed | 10 passed (13)**, EXIT 1. Named arm first: `CONTROL_ROOM_SESSION_KEY > AFTER - two private-only results cannot borrow valid downstream crypto`. The two other reds are downstream fences that NAME the same arm rather than masking it — the ledger denominator, and `admits nothing and proves nothing` with `expected [ 'SESSION_KEY_LAYER#AFTER' ] to deeply equal []`. BEFORE and RACE stayed green: correct divergence, only the non-keypair input moved.\nNote session-key.ts is NOT one of the ten owned paths; drilled and restored byte-identically to HEAD.\n\n**M3a — PRD size guard weakened (`> MAX` -> `> MAX * 2`). Reds the right arm, but a FENCE SHADOWS the named assertion.**\nsecurity gate -> 2 failed | 11 passed (13). The PRD BEFORE arm reds — but at `runtime-provider-control-room.security.ts:233`, the `ledger.refused(...)` call, with `HostileHarnessMisuseError: refusal carries no string code/reasonCode`. That is BEFORE `expect(text).not.toHaveBeenCalled()` at :236, so the named zero-call-count claim never executed. A coarse mutation being caught by a cheaper fence is not a defect, but it leaves the named absence claim UNGRADED — so I built the divergence fixture rather than banking it.\n\n**M3a-bis — DIVERGENCE FIXTURE that isolates the absence claim.** Move the size check to AFTER `await file.text()`, keeping the refusal code and layer identical. The ledger fence now passes, and exactly ONE test reds:\n  `CONTROL_ROOM_NEWGOAL > BEFORE - oversize preflight refuses before text or digest`\n  `AssertionError: expected \"vi.fn()\" to not be called at all, but actually been called 1",
      "startedAt": "2026-09-02T20:14:49.130Z",
      "completedAt": "2026-09-02T20:22:56.096Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Still required; blocked rather than backlogged per human cleanup policy: runtime-provider consumer pending 28cd completion provenance. Unblock after 28cd lands.",
  "createdBy": "WORKER",
  "parentTaskId": "task-f8ea0a2f6f3a4d6f85bd92b8cb9f261d",
  "dependsOn": [
    "task-d152f559bc2e4c9785f343ede125dcd8",
    "task-28cd95db8bb6444b861eac4384c7fdc2",
    "task-87f13cbaa8ee43208810abead90d5744"
  ],
  "priority": "CRITICAL",
  "order": 188.4,
  "comments": [
    {
      "id": "comment-62aa06055c374bc9888c06b6d8af9a6c",
      "author": "architect-21d05545",
      "content": "PLAN-TIME REACHABILITY / SCOPE BLOCK (delivered-tree anchor HEAD b3a7551dfaaf9473a2bbabfb9cef93dda9b451ad). All seven existing owned paths were clean; the three declared new files are absent. Physical lines: contracts canonical 68, contracts index 228, daemon contracts 221, daemon protocol 305, live-handshake 307, use-goal-prd 108; no existing production source crosses 400. Both prerequisite rows are DONE/merged. `git grep` confirms generateSessionKey/openSessionRequestDigest/signSessionChallenge have no production Control Room caller; only producer barrel/tests/E2E use them. `apps/control-room/package.json` declares both @moe/control-room-client and @moe/contracts, pnpm-lock importer lines 74-79 link both workspaces, and a temporary `apps/control-room/src` compiled probe imported both bare specifiers under `pnpm --filter @moe/control-room typecheck` exit 0, then was deleted by the same Node command.\n\nThe exact ten-path roster is insufficient for the required behavior and cannot be planned without violating rails 1/8. Changing resolveLiveSetupFromHandshake from bearer claim -> setup into key-bearing claim -> challenge -> signed `/session/pair/open` necessarily changes exact fetch/response expectations in at least THREE asserting consumers omitted from ownership: `apps/control-room/src/live/live-handshake.test.ts` (healthy path explicitly expects exactly bootstrap/request/claim and a claim body with no challenge), `apps/control-room/src/v2/cordum-app.test.tsx` (real handshake fixtures return the old seven-key claim and no open route), and `tests/integration/control-room/pairing-claim-parity.test.ts` (drives daemon bearer/keyed claim bodies through the current client but serves no open route). Accepting those old keyed responses as a bearer fallback would contradict DoD 3's “no live setup until signed open succeeds” and fail open.\n\nA second omitted production path is `packages/control-room-client/src/session-key.ts`: it owns `isKeyPair`, the 44-byte SPKI guard, and `SESSION_KEY_ALGORITHM_UNSUPPORTED@CONTROL_ROOM_SESSION_KEY`. DoD 4 and 5 require non-keypair/wrong-SPKI arms and a production mutation of that keypair guard; mutating or changing this unowned path is forbidden. `git grep` verified those guards are not present in any owned module.\n\nAdding the four mandatory reachable paths to the ten listed paths yields 14 distinct files, beyond the daemon's <=10 plan cap, so this is a SPIDR split signal rather than an amendment. Suggested production slices: (A) shared browser-safe proof framing/constants with contracts+daemon compatibility; (B) real keyed live pairing with session-key.ts, live-keyed-session, live-handshake, and all three existing caller tests; (C) PRD pure-reader plus runtime-provider hostile table after A/B, retaining exact code/layer and denominator drills. The `moe-epic-breakdown` skill required before filing split rows is not installed/exposed in this session (only moe-planning/moe-qa-loop exist), so I did not invent child rows without it.",
      "timestamp": "2026-08-30T22:19:25.023Z"
    },
    {
      "id": "comment-bfb2ead85631448db049a5ea7a5fbe9f",
      "author": "governor-0134a004",
      "content": "GOVERNOR RULING — SPIDR split AUTHORIZED per comment-62aa0605's own decomposition. Three children (architect files them; governor will set deps on the posted ids):\n1) PROOF FRAMING row — the keypair-guard arms/drill seam, owning packages/control-room-client/src/session-key.ts and its direct proof files;\n2) KEYED LIVE FLOW + CALLERS row — the handshake change plus the three asserting consumers it reaches (live-handshake.test.ts, cordum-app.test.tsx, pairing-claim-parity.test.ts);\n3) PRD/RUNTIME HOSTILE COVERAGE row — the remaining runtime-provider arms.\nThis parent re-scopes to whatever remains within its ten paths after the children exist, or becomes the thin serialized closure if nothing remains — your measurement decides, state it when filing.\n\nBINDING ON EVERY CHILD: no bearer fallback that fails open — the old path is removed or refuses closed (FENCE lineage, ffa05408 ruling); producer behavior and its hostile arms stay together (same rail-compliance basis as the f8ea0a2f reslice, comment-7b46f67b).\n\nSKILL ABSENCE IS NOT A BLOCKER: the moe-epic-breakdown skill is guidance; your comment already contains the measured decomposition. File the children with create_task directly, carrying the same exact-path rosters and one-seam-per-row discipline you would have gotten from the skill. Row returns to PLANNING. Authority: operator standing delegation.",
      "timestamp": "2026-08-30T22:20:00.795Z"
    },
    {
      "id": "comment-1b0aac3fe68d492192a73641cd9882d3",
      "author": "architect-21d05545",
      "content": "SPIDR CHILDREN FILED under governor ruling comment-bfb2ead85631448db049a5ea7a5fbe9f (delivered-tree measurement HEAD d0882c7a433156f9f644b172c989e48eefe42c75).\n\nA — task-d152f559bc2e4c9785f343ede125dcd8, order 188.31, PLANNING, depends on DONE 2f554e29 + 1199836b. Exact 10 paths: contracts canonical ts/js/index + canonical test; daemon session-authority contracts/protocol + protocol test; control-room-client session-key + test; NEW runtime-provider-control-room.security.ts. It owns shared browser-safe proof framing/constants AND the production SESSION_KEY keypair/SPKI guards with BEFORE/AFTER/RACE arms, keeping producer and hostile proof together.\n\nB — task-28cd95db8bb6444b861eac4384c7fdc2, order 188.32, PLANNING, depends on A + DONE 2f554e29 + 1199836b. Exact 7 paths: NEW live-keyed-session.ts/test; live-handshake.ts/test; scaffold.test.tsx; cordum-app.test.tsx; pairing-claim-parity.test.ts. This closes the real Control Room consumer edge through keyed claim -> exact challenge -> signed /session/pair/open -> setup, with no missing-challenge/bearer fallback. Correction to my earlier three-consumer list: shape grep found scaffold.test.tsx also hard-pins the bearer claim body/count and must be owned; it is included.\n\nC — task-87f13cbaa8ee43208810abead90d5744, order 188.33, PLANNING, depends on A. Exact 3 paths: use-goal-prd.ts; new-goal-form.test.tsx; the A-landed runtime-provider-control-room.security.ts. It serially extends A's receipt/denominator with PRD_LOCAL_LAYER BEFORE/AFTER/RACE arms and the production reader/generation seam; A and C cannot collide because C depends on A.\n\nRESIDUAL PARENT MEASUREMENT: every one of the original ten paths is now owned by A, B, or C; no implementation byte remains honestly assignable to this parent. Per the ruling, re-scope this row to the thin serialized closure (or archive it if preferred), set its dependsOn to A+B+C, and do not retain the stale exact-ten implementation rail. The closure can confirm the three durable completions and combined focused counts/consumer grep without inventing a fourth implementation seam. Full runtime-provider roster/partition serialization remains in the already-designated epic-final integrity row; children run focused receipts and disclose that intentional full-lane staging red.",
      "timestamp": "2026-08-30T22:27:47.281Z"
    },
    {
      "id": "comment-70a36aa0b2fc43648cb6e4b0c2cb8efc",
      "author": "governor-0134a004",
      "content": "GOVERNOR RE-SCOPE AMENDMENT (completes ruling comment-bfb2ead8; where this contradicts the row description/rails, THIS COMMENT WINS). Children filed per comment-1b0aac3f: A task-d152f559 (proof + SESSION_KEY), B task-28cd95db (keyed live flow + all 4 asserting consumers, dep A), C task-87f13cba (PRD reader + serialized runtime sheet, dep A). dependsOn set to [A, B, C] (the two ids it replaced are both DONE — no gate lost).\n\nTHIS PARENT IS NOW THE THIN SERIALIZED CLOSURE: after A+B+C land, it runs the composed runtime-provider closure gate (the axis's completeness/roster arms as mapped in comment-1b0aac3f) and records the sha-anchored green. The stale exact-ten implementation rail is STRUCK — this row implements nothing (zero residual implementation, measured); a plan for it names verification legs only. Judged in intersection form against its remaining owned proof paths. The no-fail-open bearer rule stays binding here and on every child. Not archived — the closure keeps the axis's single green anchor.",
      "timestamp": "2026-08-30T22:28:49.543Z"
    },
    {
      "id": "comment-fa949742c954400583a4f0380de96764",
      "author": "qa-368df350",
      "content": "QA VERIFICATION RECORD — qa-368df350, anchored HEAD 083c08aa. Root D:/projexts/moe-next, daemon 9876, `git rev-parse --show-toplevel` checked against $env:MOE_PROJECT_PATH before any measurement.\n\nSCOPE FIRST, because it determines the whole verdict. governor-0134a004's re-scope amendment (2026-08-30T22:28:49, \"where this contradicts the row description/rails, THIS COMMENT WINS\") makes this parent the THIN SERIALIZED CLOSURE: \"this row implements nothing (zero residual implementation, measured); a plan for it names verification legs only\", the exact-ten implementation rail STRUCK, judged in intersection form against its remaining owned proof paths. So \"COMPLETED ON VERIFICATION — zero source edits, empty net diff\" is the authorized shape, not a shortfall.\n\nNO COMPLETION COMMIT, AND THAT IS CORRECT HERE. task.commits is empty and lastCommitOutcome null, which I verified rather than waived: all TEN declared paths are ON HEAD by `git cat-file -e HEAD:<p>` (exit 0 each, not merely porcelain-empty) AND CLEAN by per-path porcelain. Nothing to land. Caps measured: live-keyed-session.ts 222 (<=250), live-handshake.ts 280 (<400).\n\nDEPENDENCIES — all three DONE on disk (A task-d152f559, B task-28cd95db, C task-87f13cba; B was approved by qa-ee158f8f during my review). Their commits are ancestors of HEAD by `git merge-base --is-ancestor`: 751d75a1 ANCESTOR, 79315504 ANCESTOR, 1729c429 ANCESTOR.\n\nDoD 1 — the grep clause IS false at HEAD, worker's correction CONFIRMED at my seat. The clause asks to \"record `git grep -l` showing no production Control Room caller yet\"; the caller exists:\n  apps/control-room/src/live/live-handshake.ts:7 imports createLiveKeyedSession, :201 calls it\n  live-handshake reachable from apps/control-room/src/main.tsx:9 and apps/control-room/src/v2/cordum-handshake.tsx:4\nA real production consumer edge, so the clause is unsatisfiable as written and correcting it is the right deliverable, not a dodge. Manifest half verified independently: apps/control-room/package.json declares @moe/control-room-client and @moe/contracts at workspace:*, and pnpm-lock importer lines 74-79 link both workspaces (`version: link:../../packages/control-room-client`).\n\nDoD 4 seam clause verified: readGoalPrdFile is an exported pure reader at use-goal-prd.ts:82 and useGoalPrd CALLS it at :121 — extracted, not a reimplementing helper.\n\nDoD 5 — VERIFICATION FIELD IS NARROWED; I RAN ALL FOUR LEGS MYSELF. task.verification records only the runtime-provider leg. Run standalone and unpiped by me at 083c08aa:\n  contracts session-authority-canonical.test.ts   1 file / 26 tests  exit 0\n  daemon session-authority-protocol.test.ts       1 file /  7 tests  exit 0\n  control-room live-keyed-session.test.ts         1 file /  7 tests  exit 0\n  runtime-provider-control-room.security.ts       1 file / 13 tests  exit 0  (reproduces the recorded outputTail exactly)\nRUN-SHAPE NOTE for whoever repeats this: the root vitest include is [\"adapters/**\",\"packages/**\",\"tests/**\",\"tools/**\"] with NO apps/**, so `pnpm exec vitest run <contracts-test> <daemon-test>` reports \"Test Files 1 passed\" and EXIT 0 while silently never running the daemon file. `pnpm --filter @moe/daemon exec vitest run <path>` also answers \"No test files found\" because apps/daemon has no config and inherits the root one. The daemon leg needs the package's own runner shape: `vitest run --root . --config package.json`. I hit both false shapes before getting a real measurement.\n\nDoD 5 MUTATION CLAUSE — four drills, all RED on their named assertions, each with sha256 backup and `sha256sum -c` restore:\n  A shared proof framing (session-authority-canonical.ts:164, domain + \"X\"): RED on \"pins the full domain plus u32be-framed field-order oracle\" (expected [88,0,0,0] to strictly equal [0,0,0,1]) and on \"frames multibyte values by UTF-8 byte length rather than JS string length\".\n  B keypair guard (control-room-client/session-key.ts:89 isKeyPair -> && true): RED on \"AFTER - two private-only results cannot borrow valid downstream crypto\", plus the arm denominator and the completeness invariant (`expected ['SESSION_KEY_LAYER#AFTER'] to deeply equal []`).\n  C PRD size guard (use-goal-prd.ts:83 -> if (false)): RED on \"BEFORE - oversize preflight refuses before text or digest\" and the no-admission invariant.\n  D generated arm denominator (dropped one ledger.refused on the SESSION_KEY BEFORE arm): RED on \"records exactly two refusing outcomes per hostile arm\" — `expected [['BEFORE',1],…] to strictly equal [['BEFORE',2],…]`.\nRestore proof: sha256 OK on all four targets and porcelain over all four EMPTY; all four focused legs re-run green afterwards.\n\nFINDING I AM DISCLOSING RATHER THAN REJECTING — the byte-identity claim rests on ONE arm, not three. Drill A corrupted the shared canonicalizer, and only the CONTRACTS oracle reddened. Both consumer arms stayed GREEN:\n  apps/control-room/src/live/live-keyed-session.test.ts  7/7 PASS under drill A\n  apps/daemon/src/identity/session-authority-protocol.test.ts  7/7 PASS under drill A\nI ruled out the artifact explanation before calling it: @moe/contracts has `\"exports\": {\".\": \"./src/index.ts\"}` with no dist and no build step, so the source mutation genuinely reaches both consumers, and session-authority-protocol.ts:140 is a pure pass-through (`return sharedCanonicalSessionProofBytes(fields)`). So this is a coverage observation, not a resolution failure. The mechanism is the mirror: live-keyed-session.test.ts:151 computes its expected `signable` by calling `canonicalSessionProofBytes` — the same module production signs with — so one edit moves both sides and the arm cannot detect a framing change by construction. DoD 2's \"existing daemon verifier tests and a browser-side equality arm must prove byte identity\" is therefore satisfied only by the contracts oracle. NOT charged to this row: DoD 2 is dependency row A's (task-d152f559) already-approved clause, this parent implements nothing by governor ruling, and the property IS covered — just not where the DoD says it is. Same shape as the :791/:795 mirror graded on task-59eaddfb an hour ago. Flagging for the architect: the fix is a fixed-oracle byte literal on at least one consumer side.\n\nBROAD BASELINE — with zero source edits there is no diff to path-attribute. Measured at this same HEAD 083c08aa within the last half hour: full @moe/control-room 113 files / 1562 tests passed exit 0; root `pnpm typecheck` 19 projects with every `$ tsc --project tsconfig.json` echo, exit 0; root vitest 3 failed / 10254 passed / 426 files, all three foreign and quoted verbatim on task-c444b68f (store barrel `expected 'undefined' to be 'function'` beside the standing untracked packages/contracts+store index.js bridges; two release-version-surfaces arms). Full security lane `pnpm test:security`: 12 files / 903 tests passed, exit 0 — the composed closure gate the governor named, and the `runtime-provider-control-room.security.ts` typecheck red worker-9179846c disclosed as transient does not reproduce.",
      "timestamp": "2026-09-02T20:30:07.956Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-30T20:56:45.473Z",
  "updatedAt": "2026-09-02T20:31:36.037Z",
  "metrics": {
    "firstClaimAt": "2026-08-30T22:15:05.297Z",
    "plannedStepCount": 4,
    "plannedDistinctFileCount": 10,
    "executedStepCount": 4,
    "doneAt": "2026-09-02T20:31:36.037Z",
    "wallClockMs": 252990740
  },
  "contextFetchedBy": [
    "architect-21d05545",
    "worker-76a8c4ac",
    "architect-b2b16103",
    "architect-5e59a71e",
    "architect-8b0f89f2",
    "architect-1f97776b",
    "governor-c11c29e2",
    "architect-dbf9e869",
    "worker-f06d587f",
    "qa-368df350"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "blockedOnTaskIds": null,
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-02T20:08:50.075Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — building the three 'new' files. All three exist, are tracked and clean, and their gates are green: live-keyed-session.ts (222), live-keyed-session.test.ts (297), runtime-provider-control-room.security.ts (358). Creating them again is the silent-duplicate failure the re-measure rail names, and would collide with the dependency rows that landed them.\nREJECTED — extracting canonicalSessionProofBytes into contracts. Commit 751d75a1 already did it, the barrel exports it at index.ts:230-233, and both the browser module and the daemon consume that single module. A second extraction would be the second canonicalizer DoD 2 forbids.\nREJECTED — wiring live-handshake to the keyed session. Already wired at live-handshake.ts:7 and :201, and reachable from main.tsx and cordum-handshake.tsx.\nREJECTED — recording DoD 1's `git grep -l` as showing no production caller. It would be false today, and its falsity IS the deliverable. Record the caller instead, with file and line.\nREJECTED — taking green gates as sufficient. Four green suites prove the code runs; they do not prove the arms are non-vacuous or that private key material stays browser-only. DoD 5's mutations and rail 3's secret-handling are what convert green into evidence, so the plan keeps both.\nCHOSEN — verify each DoD clause against measured HEAD, prove the consumer edge and the key-confinement invariant, run the four named mutations to show the arms are load-bearing, and complete on verification.",
    "codebaseInsights": "MEASURED 2026-09-02 at HEAD 8c21fd48. The description anchors 438caef3 — stale by many commits. **THIS ROW'S WORK IS ALREADY LANDED, BY ITS OWN DEPENDENCIES.** Plan to VERIFY, not to build; re-implementing a landed surface is a silent duplicate.\n\n### ALL THREE DEPENDENCIES ARE DONE — AND TWO OF THEM LANDED THIS ROW'S DELIVERABLE\ntask-d152f559 DONE, task-28cd95db DONE (5 commits), task-87f13cba DONE. The producers the row consumes are also DONE: task-1199836b (8 commits, browser key functions) and task-2f554e29 (challenge/open route).\n`git log` on the owned paths shows exactly who landed what:\n  `751d75a1` feat(task-d37e8873): extract the pure session-authority canonicalisation into @moe/contracts, composed by the daemon  -> **DoD 2**\n  `79315504` feat(task-d152f559): extract browser-safe session proof bytes and pin session-key guards  -> **DoD 2 + DoD 4** (a dependency of this row)\n  `1729c429` feat(control-room): open keyed sessions  -> **DoD 3**, the consumer edge (this is the same SHA recorded as task-28cd95db's completion)\nThat is why the row was parked 'pending 28cd completion provenance': 28cd was carrying this row's consumer edge.\n\n### ALL TEN OWNED PATHS EXIST AND ARE CLEAN — INCLUDING THE THREE THE ROW CALLS 'new'\n```\nsession-authority-canonical.ts  177  clean     live-keyed-session.ts        222  clean  (\"new\", <=250 ✓)\nsession-authority-canonical.js    1  clean     live-keyed-session.test.ts   297  clean  (\"new\")\ncontracts/src/index.ts          233  clean     live-handshake.ts            280  clean  (<400 ✓)\nsession-authority-contracts.ts  215  clean     use-goal-prd.ts              138  clean\nsession-authority-protocol.ts   295  clean     runtime-provider-control-room.security.ts 358 clean (\"new\")\n```\nNothing is dirty or untracked. Every line cap the DoD names is already satisfied.\n\n### THE CONSUMER EDGE IS REAL AND PRODUCTION-REACHABLE — THE ROW'S WHOLE POINT (rail 2)\nDoD 1 asks for `git grep -l` proving NO production Control Room caller. **That is now FALSE**, and its falsity is the deliverable:\n  `apps/control-room/src/live/live-handshake.ts:7` — `import { createLiveKeyedSession } from \"./live-keyed-session.js\";`\n  `apps/control-room/src/live/live-handshake.ts:201` — `const keyed = createLiveKeyedSession({ … })`\n  `live-keyed-session.ts:5,10` imports from the BARE specifiers `@moe/contracts` and `@moe/control-room-client`, calling the landed producer functions.\nAnd live-handshake is reached from PRODUCTION, not just tests: `apps/control-room/src/main.tsx` and `apps/control-room/src/v2/cordum-handshake.tsx` (also live-config.ts). So this is not the mock-backed re-proof rail 2 forbids — the key module is called by the real flow.\n\n### THE SHARED CANONICALIZER IS SINGLE-SOURCED (DoD 2)\n`canonicalSessionProofBytes` lives in `packages/contracts/src/session-authority-canonical.ts` (177 lines), is barrel-exported at `contracts/src/index.ts:230-233`, and its `.js` bridge is exactly one line: `export * from \"./session-authority-canonical.ts\";`. Both sides consume that one module — the browser via live-keyed-session.ts:4, the daemon via coordination-adapter.test.ts:26 and recipient-registry.test.ts:18. No second canonicalizer.\n\n### THE FOUR DoD-5 GATES ARE ALL GREEN — I RAN EVERY ONE\n  contracts   `pnpm exec vitest run packages/contracts/src/session-authority-canonical.test.ts`  -> **1 file / 26 tests**\n  daemon      `--filter @moe/daemon … src/identity/session-authority-protocol.test.ts`          -> **1 file / 7 tests**\n  control-room `--filter @moe/control-room exec vitest run src/live/live-keyed-session.test.ts`  -> **1 file / 7 tests**\n  security    `pnpm exec vitest run --config tests/security/vitest.config.ts runtime-provider-control-room.security.ts` -> **1 file / 13 tests**\n\n### THE ARMS EXIST FOR BOTH CONSTANTS (DoD 4)\ntests/security/runtime-provider-control-room.security.ts declares `SESSION_KEY_BOUNDARY` (:28) and `PRD_LOCAL_BOUNDARY` (:29) with BEFORE/AFTER/RACE arms and a refusal ledger (:95-116). It imports the real constants: `PRD_LOCAL_LAYER` from apps/control-room/src/v2/goals/new-goal-form-model.js:7 (`\"CONTROL_ROOM_NEWGOAL\"`) and `SESSION_KEY_LAYER` from packages/control-room-client/src/session-key.ts:44 (`\"CONTROL_ROOM_SESSION_KEY\"`).",
    "risks": "THE BIGGEST RISK IS REBUILDING LANDED WORK. The description was written at HEAD 438caef3 and calls three files 'new'; all three now exist, clean and green. A worker taking the description at face value will recreate them and collide with the dependency rows that shipped them. Every step below re-measures before acting.\nGREEN GATES ARE NOT EVIDENCE OF NON-VACUITY. 26 + 7 + 7 + 13 passing tests prove the modules run. DoD 5 still requires four mutations — shared proof framing, keypair guard, PRD size/read guard, and one generated-arm denominator — each reddening its NAMED assertion. A mutation that comes back green is a FINDING to report, not a pass to bank; that is the only thing standing between 'these suites exist' and 'these suites test something'.\nRAIL 3 IS A SECURITY INVARIANT AND MUST BE VERIFIED, NOT ASSUMED: private key material never leaves the browser, and never enters logs, request bodies, headers or storage. Check what live-keyed-session actually puts on the wire — public SPKI and signature only — and that the key is non-extractable. A landed module passing its own tests can still leak, because a test asserting a happy path does not assert an absence.\nDoD 4's PRD CLAUSE HAS TWO NEGATIVE PROPERTIES that a passing suite may not cover: an oversized PRD must NEVER call `text()` (assert the call count is zero, not merely that it refused), and the last-selection race must not publish stale bytes. Both are absence claims and need an explicit observation.\nRAIL 4 / DoD 4: the PRD seam must be an extracted pure reader CALLED BY useGoalPrd, never a helper that reimplements it. Verify the production call, not just the presence of a reader.\nDoD 1's PROBE CLAUSE is not discharged by the module importing a bare specifier: it asks for the manifest edges, the pnpm-lock importer entries, and a temporary compiled bare-specifier probe deleted in the same command, with its source recorded. A deleted probe's transcript alone is a lead, not evidence.\nIF A CLAUSE IS UNSATISFIABLE BECAUSE THE LANDED IMPLEMENTATION DIFFERS from what this row planned, report it and route back rather than editing landed sources to fit a stale DoD.\nRAIL 1: do not edit BOUNDARY_ROSTER or runtime-provider-invariants — the serialized final integrity/roster row owns those. SHARED CHECKOUT: preserve foreign WIP, explicit owned pathspecs only.",
    "keyFiles": [
      "apps/control-room/src/live/live-keyed-session.ts",
      "apps/control-room/src/live/live-handshake.ts",
      "packages/contracts/src/session-authority-canonical.ts",
      "tests/security/runtime-provider-control-room.security.ts",
      "apps/control-room/src/v2/goals/use-goal-prd.ts"
    ]
  },
  "budget": {
    "wallClockMs": 3600000
  },
  "planApprovedAt": "2026-09-02T20:08:52.180Z",
  "planSizeWarnings": [
    "Plan touches 10 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "workStartedAt": "2026-09-02T20:09:55.305Z",
  "reviewStartedAt": "2026-09-02T20:23:34.683Z",
  "verification": {
    "command": "pnpm exec vitest run --config tests/security/vitest.config.ts runtime-provider-control-room.security.ts",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next/tests/security\n\n Test Files  1 passed (1)\n      Tests  13 passed (13)\n   Start at  23:23:02\n   Duration  1.04s (transform 173ms, setup 0ms, import 225ms, tests 697ms, environment 0ms)\nEXIT=0",
    "reportedAt": "2026-09-02T20:23:34.683Z"
  },
  "filesModified": [
    "apps/control-room/src/live/live-keyed-session.ts",
    "apps/control-room/src/live/live-handshake.ts",
    "packages/contracts/src/index.ts",
    "packages/contracts/src/session-authority-canonical.ts",
    "packages/contracts/src/session-authority-canonical.js",
    "apps/daemon/src/identity/session-authority-contracts.ts",
    "apps/daemon/src/identity/session-authority-protocol.ts",
    "tests/security/runtime-provider-control-room.security.ts",
    "apps/control-room/src/v2/goals/use-goal-prd.ts",
    "apps/control-room/src/live/live-keyed-session.test.ts"
  ],
  "completionSummary": "COMPLETED ON VERIFICATION — zero source edits, empty net diff. Every DoD was already satisfied by this row's own dependencies; the work was proving it and correcting one false DoD clause.\n\nDEPENDENCIES LANDED THE DELIVERABLE (all ancestors of HEAD): 751d75a1 (task-d37e8873) canonicalisation into @moe/contracts -> DoD 2; 79315504 (task-d152f559) browser-safe proof bytes + session-key guards -> DoD 2+4; 1729c429 \"open keyed sessions\" -> DoD 3, the same SHA recorded as task-28cd95db's completion, which is exactly why this row was parked on 28cd provenance.\n\nHEAD MOVED MID-TASK: anchored 8c21fd48, finished at 083c08aa (peer task-c444b68f). That commit touched only tests/integration/control-room/{dev-payload-parity,live-approval-activation}.test.ts — intersection with the ten owned paths EMPTY. All ten exist, tracked, CLEAN; caps met (live-keyed-session 222<=250, live-handshake 280<400).\n\nDoD 1's GREP CLAUSE IS FALSE AND THAT IS THE DELIVERABLE. It asks for `git grep -l` showing NO production caller. The caller exists: live-handshake.ts:7 imports createLiveKeyedSession, :201 calls it, and live-handshake is production-reachable from main.tsx:9 and v2/cordum-handshake.tsx:4. Real consumer edge, not a mock re-proof. DoD 1's probe clause discharged with a PLANTED-ERROR CONTROL: leg A compiled with a deliberate TS2322 so tsc NAMED the probe file, leg B identical bare imports exit 0; probe deleted in the same command.\n\nDoD 2: sole canonicalizer at session-authority-canonical.ts:160, barrel :225-233, .js bridge one line. Daemon COMPOSES (protocol.ts:140 delegates to the shared import, signature unchanged). Ruled out the local `framed()` at protocol.ts:134 — its only caller is the REPLAY digest at :171, and the suite pins that it never touches SESSION_PROOF_DOMAIN. Zero node: specifiers in any contracts production source.\n\nDoD 3 FAILS CLOSED: traced every exit of claimAndOpen; {ok:true, sessionCredential} is returned on exactly one path, after the signed open succeeds AND the re",
  "completedAt": "2026-09-02T20:31:36.037Z",
  "reviewCompletedAt": "2026-09-02T20:31:36.037Z",
  "reviewSummary": "APPROVED at HEAD 083c08aa. Full record: comment-fa949742.\n\nSCOPE: governor-0134a004's re-scope (22:28:49, \"THIS COMMENT WINS\") makes this the thin serialized closure - \"implements nothing\", exact-ten rail STRUCK. Zero source edits is authorized.\n\nNO-COMPLETION-COMMIT CORRECT, verified not waived: all 10 declared paths ON HEAD by `git cat-file -e HEAD:<p>` AND clean per-path. Nothing to land. Caps 222<=250, 280<400. Deps A/B/C DONE; 751d75a1/79315504/1729c429 all ancestors.\n\nDoD 1 grep clause IS false at HEAD, correction confirmed: live-handshake.ts:7 imports, :201 calls createLiveKeyedSession; reachable from main.tsx:9 and v2/cordum-handshake.tsx:4. Manifest + lock importer 74-79 verified. DoD 4: readGoalPrdFile:82 called by useGoalPrd:121.\n\nDoD 5 - verification recorded ONE leg; I ran all four standalone: contracts 26, daemon protocol 7, live-keyed-session 7, security 13. Run-shape trap: root include has no apps/**, so handing root vitest the daemon test says \"1 passed\" EXIT 0 having never run it; needs `--root . --config package.json`.\n\nFour drills all RED on named assertions, sha256 restore each: A framing -> \"u32be-framed field-order oracle\"; B keypair guard -> \"AFTER - private-only results cannot borrow downstream crypto\"; C PRD size -> \"BEFORE - oversize preflight refuses before text or digest\"; D denominator -> [BEFORE,1] vs [BEFORE,2]. Porcelain empty after restore; legs re-run green.\n\nDISCLOSED not charged: under drill A only the CONTRACTS oracle reddened - both consumer arms stayed 7/7 GREEN. Not an artifact: @moe/contracts exports ./src/index.ts (no dist), protocol.ts:140 is a pass-through. live-keyed-session.test.ts:151 builds expected bytes from the same canonicalizer, so one edit moves both sides. Byte identity rests on the contracts oracle alone; row A's clause, flagged to the architect.\n\nBaselines same HEAD: security lane 12 files/903 exit 0; control-room 113/1562; root typecheck 19 projects exit 0; root vitest 3 foreign."
}