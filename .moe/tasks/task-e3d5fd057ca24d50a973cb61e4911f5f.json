{
  "id": "task-e3d5fd057ca24d50a973cb61e4911f5f",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Canonical GraphRevisionContent contract, codec and content-hash producer",
  "description": "NEW HEAD OF THE GRAPH BETA CHAIN. Filed 2026-08-17 by architect-2cc07e26 after the human resolved decision dec-64b2391cb80f4c0cbb6e88353017cc65 in favour of OPTION A (full GraphRevisionContent authority). Option B, the non-authoritative structural projection, was REJECTED because it cannot drive Foundation execution and would leave the canary's GAP A open while appearing to progress.\n\nTHE AUTHORITY, from the pinned design line 197, verbatim:\n  `GraphRevisionContent`: immutable canonical node/edge set, repository base tree,\n  parent revision, completion node, policy revision, decomposition budget,\n  graph hash, and author.\n\nMEASURED STATE at committed HEAD a42ae2f (git grep against HEAD, not the working tree):\n- `git grep -n \"GraphRevisionContent\" HEAD -- packages/ apps/ | grep -v '\\.test\\.'` returns ZERO. The type does not exist in code at all.\n- @moe/core owns the field that consumes it: `GraphRevisionState.graphContentHash` (packages/core/src/planning/graph-revision-contract.ts:125), validated as hex64 by `validHex64(command.graphContentHash)` (graph-revision-reducer.ts:85) and compared against approval/activation witnesses at :131 and :162. Core treats it as an OPAQUE hex64 string and never computes it.\n- @moe/scheduler HAS landed a canonical codec -- packages/scheduler/src/graph-content.ts and graph-content-format.ts, both committed with .js bridges and exported from the root (index.ts:86, :93) -- BUT its own header states \"its `graphIdentity` is the only thing hashed\", and `graphIdentity` is structural (nodes and edges) only.\n\nTHE DEFECT THIS CREATES. The scheduler currently exports a symbol named `graphContentHash` that computes a STRUCTURAL identity covering one of the seven fields design 197 names. Under decision A that name is load-bearing and wrong: a downstream consumer binding a durable revision to it, or any check that greps for the name, passes while carrying an identity that omits the repository base tree, parent revision, completion node, policy revision, decomposition budget and author. This is precisely the \"never equate the two hashes\" failure the decision was raised to prevent, and it currently sits in a public package root.\n\nObjective: land the canonical GraphRevisionContent contract, its strict codec, and the domain-separated content-hash producer, so a durable graph revision can prove the stored content is the one its approved hash names.\n\nDeliverable: a focused @moe/core production module (core already owns graphContentHash's consumer, so the producer belongs beside it) plus its one-line .js bridge, root export, and focused tests. It COMPOSES the scheduler's canonical snapshot bytes as ONE field -- do not reimplement structural canonicalisation, and do not re-validate the graph; `validateGraphSnapshot` remains the single structural authority.\n\nRELATED RE-SCOPE, not owned by this task but must be coordinated: task-77e8cb4439b148f386d2bbf244c79c16 is BLOCKED with 4 of 6 steps landed. Its work is sound and must be PRESERVED, but its exported name must stop claiming content authority -- rename to a structural name (snapshotIdentity / graphStructureHash) keeping the domain separation. Its retraction message is msg-75057af0. Whoever plans this task should confirm on disk whether that rename has happened, because if it has not, two symbols named for content authority will coexist.\n\nNOT in scope: persistence of the content (that is task-dd4ffa0c), graph lifecycle, daemon transport or ingress, Foundation attempt dispatch, new runtime command or query kinds, or edits to the authoritative design.",
  "definitionOfDone": [
    "A public @moe/core GraphRevisionContent contract declares all seven design-197 fields (canonical node/edge set, repository base tree, parent revision, completion node, policy revision, decomposition budget, author) with a bounded shape per field; a test asserts the field set exactly, so a later addition or omission cannot pass silently.",
    "A public producer returns a domain-separated lowercase SHA-256 graphContentHash over ALL seven fields; a test proves changing EACH field individually changes the hash -- assert one case per field and assert the case count equals the field count, so a field silently excluded from the hash is caught.",
    "The producer composes the scheduler's canonical snapshot bytes for the node/edge field and does not reimplement structural canonicalisation or re-validate the graph; a test proves two structurally equivalent node/edge orderings yield the same content hash when all other fields are equal.",
    "The content hash is NEVER equal to the scheduler's structural identity for the same graph: a test asserts the two values differ for a snapshot with non-empty other fields, and asserts the domain separation tag is present, pinning decision dec-64b2391c.",
    "A strict decoder rejects malformed, non-canonical, oversized, extra-keyed, hostile-descriptor/proxy and digest-mismatched bytes under stable core code/layer pairs and never returns content authority; every refusal asserts the exact code and layer and the generated hostile-case count is asserted nonzero.",
    "A mutation drill on the production hash path (drop one field from the hashed input) makes a NAMED per-field test go red for its hash-difference assertion, not for a crash, and production bytes are restored exactly.",
    "The @moe/core root exports the symbols with LF .js bridge and plain-Node loadability coverage; completion names task-dd4ffa0c40834c59892f957fbf61fb23 as the real consumer, and pnpm typecheck && pnpm test report nonzero Test Files counts with each production file <=250 physical lines."
  ],
  "taskRails": [
    "Decision dec-64b2391c is RESOLVED as option A: full content authority. Never equate graphContentHash with the scheduler's structural graphIdentity, and never widen the structural identity to stand in for content.",
    "Compose @moe/scheduler's canonical snapshot bytes through its public root as ONE field; do not introduce a second structural graph validator and do not include policy overrides in content identity except via the design-named policy revision field.",
    "Keep the codec deterministic and identical across Windows, macOS and Linux; never depend on filesystem path normalisation, locale ordering, or object key insertion order.",
    "Fail closed with stable reason codes and layers, assert nonzero generated case counts, use TDD, target <=250 lines per production file and split before 400, and commit only explicit owned paths by pathspec."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "READ THIS FIRST: the task description says the producer belongs in @moe/core. THAT IS WRONG and the plan overrides it. Confirm on committed HEAD before writing anything. `git show HEAD:packages/core/package.json` shows @moe/core depends on @moe/contracts ONLY; `git show HEAD:packages/scheduler/package.json` shows @moe/scheduler depends on @moe/context, @moe/contracts AND @moe/core. The dependency runs scheduler -> core. Putting a producer in core that composes the scheduler's canonical snapshot bytes would require core -> scheduler and be CIRCULAR. So this work lands in @moe/scheduler, which already owns the validator, the canonical codec, the withheld format mechanics and the GRAPH_CONTENT issue/layer vocabulary, and which can import core types freely. Record that correction in the step note. Then confirm the rest: `git grep \"GraphRevisionContent\" HEAD -- packages/ apps/` (minus tests) returns ZERO; @moe/core's `GraphRevisionState.graphContentHash` (packages/core/src/planning/graph-revision-contract.ts:125) is an opaque hex64 gated by `validHex64` (graph-revision-reducer.ts:85) and compared at :131 and :162, so core never computes it and this producer must satisfy that exact hex64 spelling.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "CONSUMER EDGE, RECORDED FIRST (global rail clause 1, and the QA reject's gap 1): the real consumer of this surface is task-dd4ffa0c40834c59892f957fbf61fb23 (persistence of GraphRevisionContent). Also blocked on this surface and unblocked by it: task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8, task-c4b80778. Mirrored into moe.add_comment and the Serena handoff memory so no 5000-char note cap can eat it.\n\nREOPEN CONTEXT (reopenCount 1): QA rejected on the completion RECORD only and stated the CODE is done and verified by its own re-run plus two independent mutation drills. This session therefore RE-VERIFIES the landed bytes from disk/git rather than reimplementing, and spends its effort on the three record gaps.\n\nSTEP-1 RE-MEASURED AT COMMITTED HEAD (now afe9940; the deliverable commit is 991dd14, one behind):\n- `git show HEAD:packages/core/package.json` -> dependencies = {\"@moe/contracts\": \"workspace:*\"} ONLY.\n- `git show HEAD:packages/scheduler/package.json` -> dependencies = {\"@moe/context\", \"@moe/contracts\", \"@moe/core\"}.\n  The edge runs scheduler -> core. A core-hosted producer composing scheduler canonical snapshot bytes needs core -> scheduler and is CIRCULAR; this repo has no tsconfig \"paths\" and no project references, so a deep relative import fails TS6059. The plan's override of its own task description (\"producer belongs in @moe/core\") stands, re-confirmed, and the work correctly lives in @moe/scheduler.\n- `git grep -n \"GraphRevisionContent\" HEAD -- packages/ apps/ | grep -v '\\.test\\.'` no longer returns ZERO (it did at a42ae2f, before this task): 12 hits, all in packages/scheduler/src/graph-content-fields.ts (type + GRAPH_REVISION_CONTENT_KEYS + interface at :66), graph-content.ts (:59 re-export, :88 `readonly content: GraphRevisionContent`), and index.ts:99 (root publication). That is this task's own landed work.\n- Core still only STORES and COMPARES: `graphContentHash` at packages/core/src/planning/graph-revision-contract.ts:78, :125, :147; gated hex64 by validHex64 in graph-revision-reducer.ts:85; compared against witnesses at :131/:162. Zero computation in core. The producer emits the lowercase 64-char hex spelling that gate requires.\n- Worktree clean for owned paths: `git status --porcelain -- packages/scheduler/ tests/security/` printed nothing.",
      "startedAt": "2026-08-17T18:37:44.692Z",
      "completedAt": "2026-08-17T18:38:12.190Z"
    },
    {
      "stepId": "step-2",
      "description": "Confirm the defect this task exists to fix, and size the edit. Read packages/scheduler/src/graph-content.ts (208 lines) and graph-content-format.ts (184 lines) at HEAD. Facts to verify: (a) `GraphContent` is `{schemaVersion, graphContentHash, snapshot, bytes}` -- snapshot ONLY, none of the other design-197 fields; (b) its `graphContentHash` field comment says \"the spelling @moe/core's revision gate requires\", so it was built to be handed straight to core's gate -- under decision dec-64b2391c option A that binding is exactly the defect, because a structure-only identity would satisfy core's hex64 check; (c) graph-content-format.ts:110-111 hashes `identity` -- the ValidatedGraph.graphIdentity -- and nothing else; (d) `SCHEMA_TAG = \"MOE-GRAPH-CONTENT/1\"` (:28) and `HASH_DOMAIN = \"MOE-GRAPH-CONTENT-HASH/1\"` (:33) already exist and are deliberately separate, so domain separation is a pattern to EXTEND, not invent. Both files are near the 250-line target, so plan the split now: put the new per-field canonicalisation in a NEW sibling module rather than growing either file past cap. Note the deliberate design you must preserve: the root export comment (index.ts:71-77) says `canonicalGraphJson`/`graphContentHash` are WITHHELD from the public surface because a consumer holding them could mint canonical bytes for a snapshot the kernel never accepted, and `encodeGraphContent` is the only route to a hash. Keep that property.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/graph-content.ts",
        "packages/scheduler/src/graph-content-format.ts",
        "packages/scheduler/src/index.ts"
      ],
      "note": "CONSUMER EDGE (repeated at the front of every note so the 5000-char cap cannot eat it): real consumer = task-dd4ffa0c40834c59892f957fbf61fb23. Blocked dependents unblocked by this surface: task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8, task-c4b80778.\n\nSTEP-2 RE-MEASURED AT COMMITTED HEAD afe9940. The defect this task existed to fix is CLOSED on disk, and the fix is the landed 991dd14 bytes; nothing was rewritten this session.\n\n(a) `GraphContent` is no longer `{schemaVersion, graphContentHash, snapshot, bytes}`. At graph-content.ts:80-90 it is `{schemaVersion, graphContentHash, snapshotIdentity, content: GraphRevisionContent, bytes}` — the seven-field record, not a snapshot alone.\n(b) The `graphContentHash` doc comment still says \"the spelling @moe/core's revision gate requires\" (graph-content.ts:78), which is correct and now HONEST: the value it names covers all seven design-197 fields rather than structure alone.\n(c) The structure-only value did not vanish — it is published beside the content hash under a name that cannot be reached by asking for content: `GraphContent.snapshotIdentity` (graph-content.ts:87), documented at :81-86 as \"never equal to it (dec-64b2391c)\". That is the rename half of this lane's problem, landed.\n(d) Domain separation was EXTENDED, not invented: SCHEMA_TAG = \"MOE-GRAPH-CONTENT/2\" (graph-content-format.ts:36), SNAPSHOT_IDENTITY_DOMAIN = \"MOE-GRAPH-SNAPSHOT-IDENTITY/1\" (:44, consumed at :146 with length framing `${DOMAIN}\\n${identity.length}:`), and GRAPH_CONTENT_SCHEMA_VERSION = 2 (graph-content.ts:66). The content-hash domain moved with the digest construction into graph-content-fields.ts, which is exactly the split the step called for.\n\nFILE SIZES AT HEAD, all under the 250 target (epic rail 5 is PER FILE): graph-content.ts 229, graph-content-fields.ts 234, graph-content-format.ts 220, graph-content-issues.ts 77. The split was needed: neither original file could absorb the fold.\n\nWITHHELD-MECHANICS PROPERTY PRESERVED and strengthened — index.ts:71-83 now states that both graph-content-format.js and graph-content-fields.js stay withheld \"because they take an already-validated graph and a consumer holding any of them could mint bytes or a hash the kernel never accepted. `encodeGraphContent` is the only route to a `graphContentHash`.\" The root publishes only GRAPH_CONTENT_ISSUE_CODES, GRAPH_CONTENT_LAYERS, GRAPH_CONTENT_SCHEMA_VERSION, GRAPH_REVISION_CONTENT_KEYS, MAX_GRAPH_CONTENT_BYTES, decodeGraphContent, encodeGraphContent plus the types.\n\nAll four production modules carry a one-line LF `.js` bridge, confirmed by `git show HEAD:<f> | cat -A` -> `export * from \"./<f>.ts\";$` with a single trailing `$` and no `^M`.",
      "startedAt": "2026-08-17T18:38:19.842Z",
      "completedAt": "2026-08-17T18:38:42.170Z"
    },
    {
      "stepId": "step-3",
      "description": "RED on the contract shape, in the existing packages/scheduler/src/graph-content.test.ts. Define GraphRevisionContent as the SEVEN design-197 fields and write the failing shape tests first. The fields: canonical node/edge set (the existing validated snapshot), repository base tree, parent revision, completion node, policy revision, decomposition budget, author. NOTE while designing: `completionNodeKey` already lives INSIDE GraphSnapshot (packages/scheduler/src/graph-model.ts), so decide explicitly whether it is carried once via the snapshot or restated as its own field, and write the decision in the test file's header -- restating it in two places invites them to disagree. Tests: (1) a well-formed content value encodes and its field set is EXACTLY the declared set, asserted against a frozen expected key list so a later addition or omission fails rather than passing; (2) `parentRevision` is legitimately absent for an INITIAL revision -- assert an explicit absent marker encodes deterministically and is NOT confused with an empty string; (3) each field's bound is enforced (over-long refs, non-hex digests, a negative or non-integer decomposition budget, an empty author) and each refuses with an exact code and layer from the existing frozen GRAPH_CONTENT_ISSUE_CODES / GRAPH_CONTENT_LAYERS vocabulary -- extend those frozen lists rather than inventing a parallel one.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/graph-content.test.ts"
      ],
      "note": "CONSUMER EDGE, FIRST: real consumer = task-dd4ffa0c40834c59892f957fbf61fb23. Unblocked dependents: task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8, task-c4b80778.\n\nSTEP-3 (contract shape) RE-VERIFIED FROM COMMITTED BYTES at HEAD afe9940; the tests landed in 991dd14 and were not rewritten this session.\n\nTHE SEVEN FIELDS, one frozen list that is BOTH the declared set and the serialization order (graph-content-fields.ts:45-53), alphabetical \"because it is the only order that needs no second definition to reproduce\": author, completionNode, decompositionBudget, parentRevision, policyRevision, repositoryBaseTree, snapshot. GRAPH_REVISION_CONTENT_KEYS is Object.freeze'd and published at the root (index.ts:88). The interface at :66-74 types parentRevision as `string | null` with the comment \"`null` for an initial revision — an explicit absence, not an empty ref\".\n\nTHE completionNode OVERLAP WAS DECIDED, NOT DODGED. `completionNodeKey` also lives inside GraphSnapshot, so the field is stated twice ON PURPOSE and reconciled in exactly one place: graph-content-fields.ts:60-65 records that completionNode is \"the author's DECLARATION of the completion node and is reconciled against the graph the kernel accepted, never trusted over it\", and a disagreement refuses as GRAPH_CONTENT_COMPLETION_DRIFT at the identity layer. Two tests pin it: \"refuses a completion node the graph does not agree with\" (:948) and \"refuses drifted completion bytes on the way back in\" (:955) — the second proves the reconciliation runs on decode, not only on encode.\n\nSHAPE TESTS ON DISK (packages/scheduler/src/graph-content.test.ts, 1157 lines):\n- :784 \"declares exactly the seven fields, frozen and canonically ordered\" — asserts against the frozen expected key list, so a later addition OR omission fails rather than passing silently (DoD 1).\n- :794 \"accepts a well-formed content value and answers with exactly those keys\"; :807 \"round-trips every field through the decoder unchanged\".\n- :814 \"carries an absent parentRevision as an explicit marker, deterministically\" and :834 \"never confuses an absent parentRevision with an empty string\" — the two halves of the initial-revision case.\n- :871 \"refuses every out-of-bound field as GRAPH_CONTENT_FIELD_INVALID\" plus :889 \"names the offending field in the refusal message\" and :896 \"accepts a hex64 repository base tree as well as hex40\" (HEX_TREE at graph-content-fields.ts:57 admits both git object formats and prefers neither).\n- :964 \"refuses a content record whose snapshot is judged by the validator, not here\" — validateGraphSnapshot stays the SINGLE structural authority; codec-layer refusals never restamp a validator code.\n\nVOCABULARY WAS EXTENDED, NOT DUPLICATED: the frozen lists gained GRAPH_CONTENT_FIELD_INVALID (codec layer) and GRAPH_CONTENT_COMPLETION_DRIFT (identity layer); no new layer was invented. :715 asserts the vocabularies are closed/frozen/sorted, :729 asserts every declared codec code is emitted from a REAL path, :752 asserts every declared layer is used — so a code declared but unreachable fails.",
      "startedAt": "2026-08-17T18:38:47.509Z",
      "completedAt": "2026-08-17T18:39:19.818Z"
    },
    {
      "stepId": "step-4",
      "description": "RED on the hash, which is the heart of the task. Still in graph-content.test.ts. (1) THE PER-FIELD SWEEP required by DoD 2: for EACH of the seven fields, hold the other six constant, change that one, and assert the graphContentHash changes. Assert the generated case count EQUALS the field count -- a sweep that silently skipped a field is exactly how a field gets omitted from the hash while every test stays green, and a bare count-free loop cannot catch it. (2) ORDER INDEPENDENCE: two structurally equivalent node/edge orderings with all other fields equal produce the SAME hash, inherited from validateGraphSnapshot's canonical ordering rather than re-implemented. (3) THE DECISION GUARD, pinning dec-64b2391c: for a content value whose non-snapshot fields are non-empty, assert `graphContentHash !== ` the scheduler's structural identity for the same snapshot, and assert the hash domain tag is present and distinct from the wire schema tag. This is the test that makes the two-hash confusion impossible to reintroduce. (4) DETERMINISM: the same logical content yields byte-identical canonical bytes across two independent encodes, and object key insertion order does not affect the result.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/graph-content.test.ts"
      ],
      "note": "CONSUMER EDGE, FIRST: real consumer = task-dd4ffa0c40834c59892f957fbf61fb23. Unblocked dependents: task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8, task-c4b80778.\n\nSTEP-4 (the hash) RE-VERIFIED FROM COMMITTED BYTES, graph-content.test.ts at HEAD afe9940.\n\n(1) PER-FIELD SWEEP, DoD 2 — \"changes when any one field changes, once per declared field\" (:1009). The sweep is driven by FIELD_VARIATIONS, a list keyed BY FIELD NAME, and its coverage is asserted against the PRODUCTION key list rather than a hand-written count:\n    expect(FIELD_VARIATIONS).toHaveLength(GRAPH_REVISION_CONTENT_KEYS.length);\n    expect(FIELD_VARIATIONS.map(([field]) => field)).toEqual([...GRAPH_REVISION_CONTENT_KEYS]);\nThat is the stronger form of the DoD's \"assert the case count equals the field count\": a count alone still passes if a case is generated for the wrong field, and a bare loop passes if it generates nothing at all. Inside the loop each case asserts both the hash AND the canonical bytes differ from base, and a `seen` Map rejects any two fields hashing alike; the loop closes with `expect(seen.size).toBe(GRAPH_REVISION_CONTENT_KEYS.length + 1)`.\n\nHONEST DISCLOSURE, carried in the test body itself (:1057-1060 region comment): the completionNode case CO-VARIES with the snapshot, because the kernel BINDS the two — a completionNode that disagrees with the graph is refused GRAPH_CONTENT_COMPLETION_DRIFT, so no honest fixture can move that field alone. Its case is therefore a presence check; the field's real authority is pinned by the two drift-refusal tests (:948, :955). This is an equivalent-mutant situation disclosed in the source, not a hole hidden by wording. See mem:gotcha-a-field-the-kernel-pins-cannot-have-an-honest-per-field-hash-case.\n\n(2) ORDER INDEPENDENCE — \"inherits the validator's canonical ordering instead of re-deriving it\" (:1043) sweeps the full permutation cross-product of nodes x edges and asserts one hash; \"encodes every node/edge ordering to byte-identical output\" (:273) does the same at the byte level. No structural canonicalisation is reimplemented: validateGraphSnapshot supplies the order.\n\n(3) LENGTH FRAMING — \"is length-framed, so a value cannot bleed across a field boundary\" (:1029) builds two contents whose CONCATENATED field text is provably identical (`rev-ab`+`polcd` vs `rev-abpol`+`cd`, asserted equal in the test before use) and asserts the hashes differ. Without framing that pair collides, which is a forgery, not a bug.\n\n(4) THE DECISION GUARD, dec-64b2391c — describe \"content authority is never the structural identity\" (:1085): \"answers with both hashes and they differ\" (:1086), \"holds the structural identity constant while content authority moves\" (:1099), \"moves the structural identity when the structure moves\" (:1112), \"separates the wire tag from both digest domains\" (:1125), \"keeps the structural identity out of the bytes it does not authorize\" (:1137), and \"is a digest over the structural identity, never the identity string\" (:1148). The three domains are distinct constants: SCHEMA_TAG \"MOE-GRAPH-CONTENT/2\" (format.ts:36), GRAPH_CONTENT_HASH_DOMAIN \"MOE-GRAPH-CONTENT-HASH/2\" (fields.ts:38, consumed at :193), SNAPSHOT_IDENTITY_DOMAIN \"MOE-GRAPH-SNAPSHOT-IDENTITY/1\" (format.ts:44, consumed at :146).\n\n(5) DETERMINISM — \"is deterministic and blind to key insertion order\" (:1058), plus \":227 serializes one fixed-key envelope with no whitespace\" and \":361 re-encodes to the same bytes it accepted\".",
      "startedAt": "2026-08-17T18:39:23.035Z",
      "completedAt": "2026-08-17T18:39:46.886Z"
    },
    {
      "stepId": "step-5",
      "description": "GREEN. Create the new sibling module packages/scheduler/src/graph-content-fields.ts plus its one-line `.js` bridge (`export * from \"./graph-content-fields.ts\";` -- without it the module is invisible to the .js import graph) holding the per-field bounded validation and canonical encoding for the six non-snapshot fields. Then extend graph-content-format.ts to fold ALL seven fields into the digest -- today :110-111 hashes only `identity` -- keeping the existing length-framed, domain-separated construction so no field can be smuggled across a boundary into another. Extend graph-content.ts's `GraphContent` to carry the full content, keeping `graphContentHash` spelled as the lowercase 64-char hex @moe/core's gate requires. BUMP THE SCHEMA: `GRAPH_CONTENT_SCHEMA_VERSION` 1 -> 2, `SCHEMA_TAG` and `HASH_DOMAIN` to their /2 spellings. The wire shape is changing and the existing frozen issue code `GRAPH_CONTENT_UNSUPPORTED_SCHEMA` already exists, so version-1 bytes must decode to that clean refusal rather than being silently reinterpreted -- add a test for it. Keep every file at or under 250 physical lines; if format.ts cannot absorb the fold, move the digest construction into the new module rather than trimming a guard.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/graph-content-fields.ts",
        "packages/scheduler/src/graph-content-fields.js",
        "packages/scheduler/src/graph-content-format.ts",
        "packages/scheduler/src/graph-content.ts"
      ],
      "newFiles": [
        "packages/scheduler/src/graph-content-fields.ts",
        "packages/scheduler/src/graph-content-fields.js"
      ],
      "note": "CONSUMER EDGE, FIRST: real consumer = task-dd4ffa0c40834c59892f957fbf61fb23. Unblocked dependents: task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8, task-c4b80778.\n\nSTEP-5 (GREEN) RE-VERIFIED ON DISK at HEAD afe9940. All four production modules exist, are committed, carry LF one-line `.js` bridges, and are under the 250 cap: graph-content.ts 229, graph-content-fields.ts 234, graph-content-format.ts 220, graph-content-issues.ts 77.\n\nTHE DIGEST FOLDS ALL SEVEN FIELDS (packages/scheduler/src/graph-content-fields.ts:157-198), read verbatim this session:\n  preimageParts() returns one (name, typeTag, payload) triple per declared field, in GRAPH_REVISION_CONTENT_KEYS order: author \"s\", completionNode \"s\", decompositionBudget \"i\" (String()), parentRevision \"z\"+\"\" when null ELSE \"s\"+ref, policyRevision \"s\", repositoryBaseTree \"s\", snapshot \"g\" + canonicalSnapshotJson(graph).\n  graphContentDigest() seeds sha256 with `${GRAPH_CONTENT_HASH_DOMAIN}\\n` then, for every part, `frame(name)+frame(tag)+frame(payload)` where frame(t) = `${t.length}:${t}`. Domain first, every field length-framed, so nothing can shift across a boundary — the property the :1029 framing test pins.\n  The TYPE TAG is what keeps an absent parentRevision distinct from any present ref (\"z\" vs \"s\"), not a magic empty string — which the codec does not admit as a ref anyway.\n  The snapshot field is COMPOSED, not re-derived: `canonicalSnapshotJson(graph)` over the already-validated graph, so canonical ordering is inherited from validateGraphSnapshot. No second structural validator exists.\n\nSCHEMA BUMP LANDED, all three constants at their new spellings: GRAPH_CONTENT_SCHEMA_VERSION = 2 (graph-content.ts:66), SCHEMA_TAG = \"MOE-GRAPH-CONTENT/2\" (graph-content-format.ts:36), GRAPH_CONTENT_HASH_DOMAIN = \"MOE-GRAPH-CONTENT-HASH/2\" (graph-content-fields.ts:38). SNAPSHOT_IDENTITY_DOMAIN stayed at /1 deliberately — the structural identity's preimage did not change. Version-1 bytes refuse cleanly rather than being reinterpreted: test \"refuses an unsupported schema tag, version-1 bytes included\" (graph-content.test.ts:468) asserts GRAPH_CONTENT_UNSUPPORTED_SCHEMA, the pre-existing frozen code.\n\ncanonicalContentJson (:205-218) emits the same seven keys in the same declared order with no whitespace and `null` — not `\"\"` — for an absent parent, and projectContent (:221-233) assembles the frozen answer in that order so Object.keys is stable.\n\nformat.ts could NOT absorb the fold, so the digest construction moved into the new sibling module exactly as the step directed; no guard was trimmed to make room.\n\nFRESH GATE EVIDENCE for this step's claim (full log kept out of the repo, at /d/tmp/gate-e3d5fd05.log):\n  LEG1 `pnpm typecheck` -> TYPECHECK_EXIT=0 (repo-wide).\n  LEG2 `pnpm --filter @moe/scheduler test` -> SCHEDULER_EXIT=0, `Test Files 45 passed (45)` / `Tests 1411 passed (1411)`. NONZERO count line present, so the filter resolved and the suite really executed — not the \"No projects matched the filters\" exit-0 vacuum.",
      "startedAt": "2026-08-17T18:40:47.036Z",
      "completedAt": "2026-08-17T18:41:11.517Z"
    },
    {
      "stepId": "step-6",
      "description": "Publish the surface and separate the two identities by NAME, which is the rename half of this lane's problem. Update packages/scheduler/src/index.ts to export the GraphRevisionContent type and the extended codec. THE STRUCTURAL IDENTITY MUST STOP WEARING THE CONTENT NAME: whatever the root currently publishes that resolves to a structure-only value must be named for what it is (snapshotIdentity / graphStructureHash), so a consumer cannot reach a structural value by asking for content. Preserve the withheld-mechanics property from index.ts:71-77 -- `encodeGraphContent` stays the only route to a hash, and the raw canonicalisation helpers stay unexported. Extend the existing root-export/bridge guard tests so the new symbols are covered, and verify plain-Node loadability of the new `.js` bridge from a consumer package. COORDINATE, do not collide: task-77e8cb44 is BLOCKED with 4 of 6 steps landed and committed and owns overlapping files. Before editing, `git log -1 -- packages/scheduler/src/graph-content*.ts` and re-read those files; if its rename has already landed, adopt the name it chose instead of inventing a second one, and say so in the step note.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/index.ts",
        "packages/scheduler/src/graph-content.ts"
      ],
      "note": "CONSUMER EDGE, FIRST: real consumer = task-dd4ffa0c40834c59892f957fbf61fb23. Unblocked dependents: task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8, task-c4b80778.\n\nSTEP-6 (publish + rename) RE-VERIFIED at HEAD afe9940.\n\nROOT PUBLICATION (packages/scheduler/src/index.ts:84-100): value exports GRAPH_CONTENT_ISSUE_CODES, GRAPH_CONTENT_LAYERS, GRAPH_CONTENT_SCHEMA_VERSION, GRAPH_REVISION_CONTENT_KEYS, MAX_GRAPH_CONTENT_BYTES, decodeGraphContent, encodeGraphContent; type exports GraphContent, GraphContentIssue, GraphContentIssueCode, GraphContentLayer, GraphContentResult, GraphRevisionContent.\n\nTHE RENAME LANDED, and the two identities are separated BY NAME: the structure-only value is published only as `GraphContent.snapshotIdentity` (graph-content.ts:87). Nothing named for content resolves to a structural value, so the mistake dec-64b2391c exists to prevent is not reachable by autocomplete. index.ts:71-83 states this in the surface doc: \"NAMES ARE LOAD-BEARING (dec-64b2391c): the STRUCTURE-ONLY value is `GraphContent.snapshotIdentity`, never a symbol reachable by asking for content\".\n\nWITHHELD MECHANICS PRESERVED AND WIDENED: graph-content-format.js AND the new graph-content-fields.js both stay unexported from the root, \"because they take an already-validated graph and a consumer holding any of them could mint bytes or a hash the kernel never accepted. `encodeGraphContent` is the only route to a `graphContentHash`.\" The new module strictly increased the withheld set; it did not open a seam.\n\nCOORDINATION WITH task-77e8cb44: re-checked `git log -1 -- packages/scheduler/src/graph-content*.ts` this session — no competing rename has landed; the names above are the ones that exist, so task-77e8cb44 must ADOPT snapshotIdentity rather than invent a second. Its landed work was preserved, never reset.\n\nPLAIN-NODE LOADABILITY, run FRESH this session from inside packages/scheduler (bare specifier resolves upward from the file, so a probe outside the package cannot test the edge). Probe encoded a two-node HARD chain and decoded it back:\n  BRIDGE_LOADED keys=7 schemaVersion=2 hashLen=64 hexLower=true separate=true contentKeys=[\"author\",\"completionNode\",\"decompositionBudget\",\"parentRevision\",\"policyRevision\",\"repositoryBaseTree\",\"snapshot\"] roundTrip=true\n  PROBE_EXIT=0\nThat is the bare `@moe/scheduler` specifier through the .js bridge graph under plain Node with no bundler and no vitest: seven keys in declared order, lowercase hex64 (the exact spelling core's validHex64 gate requires), content hash != snapshotIdentity, byte round trip stable.\nNEGATIVE CONTROL, obtained by accident and worth recording: my first probe used a wrong node shape and production refused `GRAPH_MALFORMED_NODE` / `GRAPH_VALIDATION` with an issues array — the probe cannot pass by loading a stub, and the validator (not the codec) is what judges structure.\nSCRATCH REMOVED IN THE SAME COMMAND: `rm -f probe-e3d5.mjs`, then `git status --porcelain -- packages/scheduler/` printed NOTHING. No probe file is in the tree or any commit (epic rail 5).\n\nDurable coverage, not just this probe: scheduler-entrypoint-smoke-worker.mjs + scheduler-runtime-entrypoint.test.ts and index-surface.test.ts are committed and ran inside LEG2's 1411 green tests.",
      "startedAt": "2026-08-17T18:41:22.980Z",
      "completedAt": "2026-08-17T18:42:14.943Z"
    },
    {
      "stepId": "step-7",
      "description": "Mutation drills, three, one per claim a green suite cannot otherwise distinguish. Drill 1 (every field is really hashed) -- the DoD-6 drill: remove ONE field from the hashed input in the digest construction; the step-4 per-field case for that field must go RED on its hash-difference assertion, not on a crash. Repeat for a second, different field so the drill proves the sweep rather than one lucky case. Drill 2 (the two hashes stay separate): make the content hash fall back to the structural identity; the step-4 decision guard must go RED. This is the drill that protects dec-64b2391c. Drill 3 (the schema bump is load-bearing): make the decoder accept version-1 bytes; the unsupported-schema test must go RED. Record for each drill the command, the failing test NAME and the exact assertion -- a drill that reddens by crashing, or on a different assertion, has not proven the binding. Restore exact bytes by re-editing, never `git checkout` (destructive in this shared worktree), and confirm with `git diff --numstat` showing 0 added and 0 deleted per drilled file. One drill at a time: a peer commit landing mid-drill can swallow the patch and leave git reading clean.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "packages/scheduler/src/graph-content-format.ts",
        "packages/scheduler/src/graph-content-fields.ts"
      ],
      "startedAt": "2026-08-17T18:42:25.926Z"
    },
    {
      "stepId": "step-8",
      "description": "Adversarial review, then commit and gate. ATTACKER PASS: can any consumer obtain a graphContentHash without going through the encoder, reopening the withheld-mechanics seam? Can two different logical contents collide -- are all seven fields length-framed so a value cannot bleed across a boundary? Can a hostile descriptor, getter or revoked proxy on the input object be read twice with different answers (snapshot hostile input before inspecting it)? Is the encoding stable across Windows, macOS and Linux, free of filesystem path normalisation and locale-sensitive ordering? Does an absent parentRevision encode distinctly from an empty string? Does any refusal reuse a code that means something else? Confirm every production file is <=250 physical lines and that no second structural validator was introduced. Name `task-dd4ffa0c40834c59892f957fbf61fb23` as the real consumer in the completion note, and state that task-77e8cb44, task-32c1ba45, task-eacea969, task-931f99e8 and task-c4b80778 are all blocked on this surface. COMMIT: `git status --porcelain`, unstage foreign entries with `git restore --staged -- <path>`, commit ONLY the owned paths by explicit pathspec, verify with `git show --stat`. GATE, foreground and `&&`-chained so no red leg is masked: `pnpm typecheck && pnpm test && pnpm --filter @moe/scheduler test`, plus a plain-Node import of the new bridge. Read the OUTPUT: a filtered leg is evidence only if it prints a `Test Files N passed` line with N nonzero, since `pnpm --filter` on an unresolved name prints \"No projects matched the filters\" and exits 0.",
      "status": "PENDING",
      "affectedFiles": [
        "packages/scheduler/src/graph-content.ts",
        "packages/scheduler/src/graph-content-format.ts",
        "packages/scheduler/src/graph-content-fields.ts",
        "packages/scheduler/src/graph-content-fields.js",
        "packages/scheduler/src/graph-content.test.ts",
        "packages/scheduler/src/index.ts"
      ]
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": "worker-b8af1a87",
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "The CODE is done and verified. Rejecting on the completion RECORD only — no production change is needed for the main gap.\n\nVERIFIED GREEN BY RE-RUN: `pnpm typecheck && pnpm --filter @moe/scheduler test` reproduced exactly — `Test Files 45 passed (45)` / `Tests 1411 passed (1411)`, GATE_EXIT=0. Files 229/234/220/77 all <=250. Both new `.js` bridges are one-line LF (`cat -A`). Root publishes GRAPH_REVISION_CONTENT_KEYS + GraphRevisionContent. `validateGraphSnapshot` remains the only structural authority (2 production call sites, both graph-content.ts). The sweep asserts against the PRODUCTION key list, not a hand-count. completionNode co-variation is disclosed in the test header — honest equivalent mutant, not a hole.\n\nMY OWN DRILLS (DoD 6), both restored sha256-identical, `git status --porcelain -- packages/scheduler/` empty after: (A) dropping `[\"author\",\"s\",fields.author]` from preimageParts reddens the NAMED test `changes when any one field changes, once per declared field` with `AssertionError: author: expected '42e85fb5…' not to be '42e85fb5…'` — the hash-difference assertion, not a crash. (B) `hash = snapshotIdentityHash(graph)` reddens `answers with both hashes and they differ`. dec-64b2391c is genuinely pinned.\n\nTHREE RECORD GAPS, all in DoD 7 / global rails:\n1. The completion never names task-dd4ffa0c. `dd4ffa0c` occurs in the task JSON only in `description`, `definitionOfDone`, and step-8's INSTRUCTION — in ZERO notes. Global rail clause 1 requires the consumer task id be RECORDED before completion.\n2. DoD 7 names `pnpm test`; the recorded verification is the narrower `pnpm --filter @moe/scheduler test`. Root `pnpm test` is exit 1 at HEAD and was never recorded or attributed.\n3. `pnpm test:security` is red on 3 tests naming GRAPH_CONTENT_LAYERS — the roster entry this task added — and nothing durable discloses it.\n\nDo NOT delete the roster entry to quiet (3); that hides a real boundary.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 113.05,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-17T17:03:06.265Z",
  "updatedAt": "2026-08-17T18:42:25.926Z",
  "metrics": {
    "firstClaimAt": "2026-08-17T17:43:57.144Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 14,
    "rejectCount": 1,
    "reopenCount": 1
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [
    {
      "item": "The @moe/core root exports the symbols with LF .js bridge and plain-Node loadability coverage; completion names task-dd4ffa0c40834c59892f957fbf61fb23 as the real consumer, and pnpm typecheck && pnpm test report nonzero Test Files counts with each production file <=250 physical lines.",
      "rejectedAt": "2026-08-17T18:36:35.007Z",
      "rejectedBy": "qa-12cda609"
    }
  ],
  "planSubmittedAt": "2026-08-17T17:46:12.572Z",
  "planningNotes": {
    "approachesConsidered": "THE PLAN OVERRIDES ITS OWN TASK DESCRIPTION ON ONE POINT. I filed this task saying the producer belongs in @moe/core, because core owns the consuming field. Measuring the manifests refutes it: @moe/core depends on @moe/contracts ONLY, while @moe/scheduler depends on @moe/context, @moe/contracts AND @moe/core. A core-hosted producer composing scheduler snapshot bytes needs core -> scheduler and is circular. So the work lands in @moe/scheduler, which already owns validateGraphSnapshot, the canonical codec, the withheld format mechanics and the GRAPH_CONTENT issue/layer vocabulary, and which imports core freely. REJECTED ALTERNATIVES: (a) a new package for the contract -- an entire workspace package, manifest, lock importer and bridge set for one contract that has exactly one natural home; (b) adding @moe/scheduler to @moe/core's dependencies -- circular, and there is no escape hatch in this repo (no tsconfig paths, no project references; a deep relative import fails TS6059); (c) building it in apps/daemon -- it would stop being a shared contract and every package needing content identity would deep-import a daemon module; (d) extending the existing GraphContent in place without a schema bump -- the wire shape changes, and silently reinterpreting version-1 bytes under version-2 rules is how a structure-only hash survives as if it were content; the frozen code GRAPH_CONTENT_UNSUPPORTED_SCHEMA already exists precisely for this. CHOSE: extend the scheduler's existing content surface to all seven design-197 fields, bump schema/tag/domain to /2, add a sibling module for per-field canonicalisation so neither existing file crosses the line cap, and rename the structural identity off the content name.",
    "codebaseInsights": "Measured at committed HEAD a42ae2f with `git show`/`git grep` against HEAD; the working tree is foreign-dirty. MANIFESTS: @moe/core deps = {@moe/contracts}; @moe/scheduler deps = {@moe/context, @moe/contracts, @moe/core}. Dependency runs scheduler -> core, so core cannot host this. CORE SIDE: GraphRevisionState.graphContentHash at packages/core/src/planning/graph-revision-contract.ts:125; gated by validHex64 at graph-revision-reducer.ts:85; compared against approval/activation witnesses at :131 and :162. Core stores and compares, never computes. `git grep \"GraphRevisionContent\" HEAD -- packages/ apps/` (minus tests) returns ZERO. SCHEDULER SIDE, all committed: graph-content.ts is 208 lines, graph-content-format.ts is 184, graph-content.test.ts is 643. GraphContent is `{schemaVersion, graphContentHash, snapshot, bytes}` -- snapshot only -- and its hash field carries the comment \"the spelling @moe/core's revision gate requires\", i.e. it was built to hand a structure-only identity to core's gate, which under option A is the defect. graph-content-format.ts:28 SCHEMA_TAG = \"MOE-GRAPH-CONTENT/1\", :33 HASH_DOMAIN = \"MOE-GRAPH-CONTENT-HASH/1\" (deliberately separate: a digest domain is not a wire tag), and :110-111 folds ONLY `identity` into the digest. Frozen vocabularies to EXTEND rather than duplicate: GRAPH_CONTENT_LAYERS = [GRAPH_CONTENT_CODEC, GRAPH_CONTENT_IDENTITY, GRAPH_VALIDATION]; GRAPH_CONTENT_ISSUE_CODES includes DIGEST_MISMATCH, MALFORMED, NONCANONICAL, NOT_BYTES, TOO_LARGE, UNREADABLE, UNSUPPORTED_SCHEMA. Structural failures pass through with their own GraphIssueCode under the GRAPH_VALIDATION layer and are never restamped. index.ts:71-77 documents a deliberate withholding: canonicalGraphJson/graphContentHash are unexported so no consumer can mint canonical bytes for a snapshot the kernel never accepted; encodeGraphContent is the only route to a hash. GraphSnapshot already contains completionNodeKey, which is also one of the seven design-197 fields -- resolve that overlap explicitly. task-77e8cb44 owns these same files with 4/6 steps landed and is BLOCKED pending a rename; check `git log -1 -- packages/scheduler/src/graph-content*.ts` before editing.",
    "risks": "The load-bearing risk is a field that is declared but never hashed: the contract would look complete, every shape test would pass, and revisions would bind to an identity missing (say) the author or the decomposition budget. Only a per-field sweep whose CASE COUNT is asserted equal to the field count catches it -- a loop without the count assertion passes while silently skipping a field, and drill 1 exists to prove the sweep is real. Second risk is the two-hash confusion this whole decision exists to prevent: the scheduler currently publishes a structure-only value under the content name, so the easiest possible mistake is to keep returning it; the step-4 decision guard and drill 2 pin it, and step 6 renames it so the mistake is not reachable by autocomplete. Third, the schema bump: without it, version-1 bytes decode under version-2 rules and a structure-only hash survives disguised as content. Fourth, length-framing every field -- without it two different contents can collide by shifting bytes across a field boundary, which is a forgery, not a bug. Fifth, hostile input must be snapshotted before inspection; a getter or revoked proxy read twice can answer differently between validation and hashing. Sixth, determinism across Windows/macOS/Linux: no path normalisation, no locale ordering, no reliance on object key insertion order. Seventh, an absent parentRevision (initial revision) must encode distinctly from an empty string. Finally, FILE COLLISION: task-77e8cb44 has landed committed work in exactly these files and is blocked pending a rename -- re-read them and adopt its chosen name if present rather than inventing a competing one, and never reset its work.",
    "keyFiles": [
      "packages/scheduler/src/graph-content.ts",
      "packages/scheduler/src/graph-content-format.ts",
      "packages/scheduler/src/graph-content.test.ts",
      "packages/scheduler/src/index.ts",
      "packages/scheduler/src/graph-model.ts",
      "packages/core/src/planning/graph-revision-contract.ts",
      "packages/core/package.json",
      "packages/scheduler/package.json"
    ]
  },
  "budget": {
    "wallClockMs": 7200000
  },
  "planApprovedAt": "2026-08-17T17:46:14.655Z",
  "contextFetchedBy": [
    "worker-b8af1a87",
    "qa-12cda609"
  ],
  "workStartedAt": "2026-08-17T17:46:36.937Z",
  "reviewStartedAt": "2026-08-17T18:28:06.153Z",
  "verification": {
    "command": "pnpm typecheck && pnpm --filter @moe/scheduler test",
    "exitCode": 0,
    "outputTail": "apps/daemon typecheck: Done\n\n Test Files  45 passed (45)\n      Tests  1411 passed (1411)\n   Start at  21:26:15\n   Duration  1.24s (transform 9.77s, setup 0ms, import 13.41s, tests 1.81s, environment 3ms)\n\nGATE_EXIT=0",
    "reportedAt": "2026-08-17T18:28:06.153Z"
  },
  "filesModified": [
    "packages/scheduler/src/graph-content.ts",
    "packages/scheduler/src/graph-content-format.ts",
    "packages/scheduler/src/index.ts",
    "packages/scheduler/src/graph-content.test.ts",
    "packages/scheduler/src/graph-content-fields.ts",
    "packages/scheduler/src/graph-content-fields.js",
    "packages/scheduler/src/graph-content-issues.ts",
    "packages/scheduler/src/graph-content-issues.js",
    "packages/scheduler/src/index-surface.test.ts",
    "packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs",
    "packages/scheduler/src/scheduler-runtime-entrypoint.test.ts",
    "tests/security/boundary-roster.security.ts"
  ],
  "reviewCompletedAt": "2026-08-17T18:36:35.007Z",
  "rejectionDetails": {
    "failedDodItems": [
      "The @moe/core root exports the symbols with LF .js bridge and plain-Node loadability coverage; completion names task-dd4ffa0c40834c59892f957fbf61fb23 as the real consumer, and pnpm typecheck && pnpm test report nonzero Test Files counts with each production file <=250 physical lines."
    ],
    "issues": [
      {
        "type": "missing_feature",
        "file": ".moe/tasks/task-e3d5fd057ca24d50a973cb61e4911f5f.json",
        "description": "Consumer edge not recorded. `dd4ffa0c` appears ONLY in description, definitionOfDone and step-8's instruction - ZERO completion notes. Cause: step-8's note is EXACTLY 5000 chars, the hard cap, truncated mid-sentence at 'No projects matched t'. Fix: record it where the cap cannot eat it (moe.add_comment, or the FRONT of a note): task-dd4ffa0c40834c59892f957fbf61fb23 is the real consumer. Also list blocked dependents 77e8cb44, 32c1ba45, eacea969, 931f99e8, c4b80778."
      },
      {
        "type": "test_failure",
        "file": "packages/scheduler/src/graph-content.ts",
        "description": "DoD 7 names `pnpm test`; recorded verification is the narrower `pnpm --filter @moe/scheduler test`. I ran root `pnpm test`: exit 1, `Test Files 2 failed | 289 passed (291)` / `Tests 3 failed | 6902 passed | 2 skipped (6907)`. Failing: tests/integration/release-archive-cleanup.test.ts (2) and packages/store/src/recovery-anchor.test.ts (1). Neither references scheduler nor is in your diff - foreign. Fix: run it, record the count line and the path-attributed baseline."
      },
      {
        "type": "regression",
        "file": "tests/security/boundary-roster.security.ts",
        "line": 199,
        "description": "`pnpm test:security` is RED and undisclosed: 3 failures, all in tests/security/integrity-boundaries.security.ts, all naming the roster entry you added - the integrity-entry count, the roster-minus-covered check, and 'generates a positive, three-armed case count for GRAPH_CONTENT_LAYERS'. tsc dies first on a foreign TS6133, so run `npx vitest run --config tests/security/vitest.config.ts` directly. Fix: disclose verbatim + file the follow-up for the 3 arms. Keep the entry."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "The CODE is done and verified. Rejecting on the completion RECORD only — no production change is needed for the main gap.\n\nVERIFIED GREEN BY RE-RUN: `pnpm typecheck && pnpm --filter @moe/scheduler test` reproduced exactly — `Test Files 45 passed (45)` / `Tests 1411 passed (1411)`, GATE_EXIT=0. Files 229/234/220/77 all <=250. Both new `.js` bridges are one-line LF (`cat -A`). Root publishes GRAPH_REVISION_CONTENT_KEYS + GraphRevisionContent. `validateGraphSnapshot` remains the only structural authority (2 production call sites, both graph-content.ts). The sweep asserts against the PRODUCTION key list, not a hand-count. completionNode co-variation is disclosed in the test header — honest equivalent mutant, not a hole.\n\nMY OWN DRILLS (DoD 6), both restored sha256-identical, `git status --porcelain -- packages/scheduler/` empty after: (A) dropping `[\"author\",\"s\",fields.author]` from preimageParts reddens the NAMED test `changes when any one field changes, once per declared field` with `AssertionError: author: expected '42e85fb5…' not to be '42e85fb5…'` — the hash-difference assertion, not a crash. (B) `hash = snapshotIdentityHash(graph)` reddens `answers with both hashes and they differ`. dec-64b2391c is genuinely pinned.\n\nTHREE RECORD GAPS, all in DoD 7 / global rails:\n1. The completion never names task-dd4ffa0c. `dd4ffa0c` occurs in the task JSON only in `description`, `definitionOfDone`, and step-8's INSTRUCTION — in ZERO notes. Global rail clause 1 requires the consumer task id be RECORDED before completion.\n2. DoD 7 names `pnpm test`; the recorded verification is the narrower `pnpm --filter @moe/scheduler test`. Root `pnpm test` is exit 1 at HEAD and was never recorded or attributed.\n3. `pnpm test:security` is red on 3 tests naming GRAPH_CONTENT_LAYERS — the roster entry this task added — and nothing durable discloses it.\n\nDo NOT delete the roster entry to quiet (3); that hides a real boundary.",
      "rejectedAt": "2026-08-17T18:36:35.007Z",
      "reopenCount": 1,
      "failedDodItems": [
        "The @moe/core root exports the symbols with LF .js bridge and plain-Node loadability coverage; completion names task-dd4ffa0c40834c59892f957fbf61fb23 as the real consumer, and pnpm typecheck && pnpm test report nonzero Test Files counts with each production file <=250 physical lines."
      ],
      "issues": [
        {
          "type": "missing_feature",
          "file": ".moe/tasks/task-e3d5fd057ca24d50a973cb61e4911f5f.json",
          "description": "Consumer edge not recorded. `dd4ffa0c` appears ONLY in description, definitionOfDone and step-8's instruction - ZERO completion notes. Cause: step-8's note is EXACTLY 5000 chars, the hard cap, truncated mid-sentence at 'No projects matched t'. Fix: record it where the cap cannot eat it (moe.add_comment, or the FRONT of a note): task-dd4ffa0c40834c59892f957fbf61fb23 is the real consumer. Also list blocked dependents 77e8cb44, 32c1ba45, eacea969, 931f99e8, c4b80778."
        },
        {
          "type": "test_failure",
          "file": "packages/scheduler/src/graph-content.ts",
          "description": "DoD 7 names `pnpm test`; recorded verification is the narrower `pnpm --filter @moe/scheduler test`. I ran root `pnpm test`: exit 1, `Test Files 2 failed | 289 passed (291)` / `Tests 3 failed | 6902 passed | 2 skipped (6907)`. Failing: tests/integration/release-archive-cleanup.test.ts (2) and packages/store/src/recovery-anchor.test.ts (1). Neither references scheduler nor is in your diff - foreign. Fix: run it, record the count line and the path-attributed baseline."
        },
        {
          "type": "regression",
          "file": "tests/security/boundary-roster.security.ts",
          "line": 199,
          "description": "`pnpm test:security` is RED and undisclosed: 3 failures, all in tests/security/integrity-boundaries.security.ts, all naming the roster entry you added - the integrity-entry count, the roster-minus-covered check, and 'generates a positive, three-armed case count for GRAPH_CONTENT_LAYERS'. tsc dies first on a foreign TS6133, so run `npx vitest run --config tests/security/vitest.config.ts` directly. Fix: disclose verbatim + file the follow-up for the 3 arms. Keep the entry."
        }
      ]
    }
  ]
}