{
  "id": "task-ac9d5e2d43334525bd990b1362652ce4",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "Prove the browser-only fresh start: journey, copy, docs",
  "description": "SPIDR child D of task-bb844c288fb346c0856778c738b9f073 - the hardening child. dependsOn child C (task-0a5d7212a7f644d5891df4e0e1545787). Filed by architect-d1711c47 2026-09-04, measured at main 68eb1c5d.\n\nWHAT THIS ROW OWNS: the proof that the capability children A-C shipped actually works from a browser against a real daemon, plus every sentence in the repo that currently says it does not.\n\nMEASURED AT 68eb1c5d - the exact doc anchors (the parent DoD's wording does not match the tree; use THESE):\n- apps/control-room/src/v2/goals/live-goal-create.ts:91-93 - the sentence to delete is split across lines and ends `+ \" seed); the browser cannot drive the pre-activation chain.\";`. The parent DoD's grep (`grep -n \"cannot drive the pre-activation chain\" <file>` = 0 matches) is satisfiable as written.\n- README.md:88-91 - the 'What this is not' sentence reads \"a fresh project cannot be started from the browser alone (goal creation waits on the project activation chain, which the seed script or the wrapper drives)\". Also re-date the \"Measured on 2026-09-03\" stamp on line 88.\n- docs/agent-stack-runbook.md:47-52 - the paragraph is `**New Goal** remains disabled until the project ledger contains legitimate durable repository, provider, distribution, backup, credential, and store receipts. The current fresh-project browser flow has no production writer for that complete receipt set...`. NOTE: the parent DoD calls this the \"New Goal remains disabled\" paragraph, but the literal string in the file is bolded markdown - `grep -n \"New Goal\" docs/agent-stack-runbook.md` finds it at :47. Do not chase the unbolded string; it does not exist.\n- The e2e lane is REAL and already drives a daemon: tests/e2e/control-room/ with playwright.config.ts, `pnpm test:e2e:browser` = `tsc -p tests/e2e/control-room/tsconfig.json && playwright test -c ...` (package.json:22). daemon-ports.ts exports `withDaemonBackedControlRoom` and lane options `seed: \"NONE\"` / `approval: \"HUMAN\"` - board-chain.spec.ts:1-33 documents the idiom and is the model to copy.\n- IMPORTANT: board-chain.spec.ts drives the LEGACY dev board (`cr.liveboard.*` testids, DEV_PAYLOADS). The new journey must drive the V2 surface - the Activate card child C shipped - or it proves the old path, not the product.\n- tests/e2e/control-room/tsconfig.json CANNOT reach into apps/ (TS6059), so expected strings are HAND-TRANSCRIBED from the modules that produce them; deriving them from those modules would make the assertions self-referential. Follow board-chain.spec.ts:35-43's stated reasoning.\n\nNOT IN SCOPE: changing production behaviour. If the journey finds a defect, that is a finding to report against child A/B/C - not a fix to smuggle in here.",
  "definitionOfDone": [
    "A Playwright journey under tests/e2e/control-room/ drives, against a REAL daemon with `seed: \"NONE\"` (no fixtures, empty store): browser-only register -> Activate project -> New Goal enabled -> goal.create_with_source ACCEPTED. `pnpm test:e2e:browser` quoted with its count line. The journey drives the V2 Activate card, NOT the legacy `cr.liveboard.*` dev board.",
    "`grep -n \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts` returns 0 matches at the delivered tree, and the replacement copy names the Activate card; live-goal-create's own test is updated so the new copy is asserted, not merely absent.",
    "README.md:88-91 'What this is not' fresh-project sentence rewritten to the measured truth (and the 'Measured on' date stamp updated), and docs/agent-stack-runbook.md:47-52 'New Goal remains disabled' paragraph rewritten - both in the SAME commit as the journey.",
    "A live drive on the lane is recorded as a row comment: store path, HEAD sha, transcript and screenshots, per row rail 4. Keep each comment under 4500 chars (fields truncate silently at 5000).",
    "`pnpm seed` still green with its count line quoted, and the wrapper's bootstrap path untouched - `git diff --stat` over the wrapper path shows no change.",
    "Whole-parent regression, gates all quoted with count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, `pnpm test` (root lane), `pnpm test:e2e:browser`. Repo-wide legs are evaluated as a path-attributed baseline (global rail 3): record merge-base failures, HEAD failures, and disclose any foreign red verbatim.",
    "Adversarial pass over the whole parent feature: what happens on a second concurrent Activate click, on a non-git project directory, on a project whose store is read-only, and when the daemon restarts mid-chain. Findings recorded in the completion summary."
  ],
  "taskRails": [
    "PROVE IT LIVE: an offline e2e over a REAL daemon is necessary but NOT sufficient - the completion must also record a live drive (transcript, store path, sha) in a row comment. Never restart the wrapper to unstick a seat (claim TTL 30 min).",
    "NO PRODUCTION BEHAVIOUR CHANGES. A defect found by the journey is reported against child A/B/C, not patched here - a hardening row that edits the thing it certifies proves nothing.",
    "The journey drives the V2 Activate card. A journey that clicks the legacy `cr.liveboard.*` dev board re-proves the pre-existing fake-witness path and does NOT satisfy this row."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "GATE AND RE-MEASURE. This row proves what children A-C2 shipped, so start only when all four are DONE: A task-741dad13e23d441a8385d0f14df5b5c1, B task-4b9c394debe94b41a80e2b46c6d55ab8, C task-0a5d7212a7f644d5891df4e0e1545787 (daemon read route), C2 task-3506e04abcef4fc3a69299ca238ae1b3 (the Activate card). NOTE: this row's `dependsOn` field names only C — C2 was split out of C at plan time and I could not add it (`moe.set_task_dependencies` refuses this seat: `[NOT_ALLOWED] architect or governor role required`). C2 is the row that ships the card this journey clicks, so verify it yourself via get_context.epicSiblings or `git log main --grep 'Moe-Task: task-3506e04a'`; if it is not DONE, report_blocked with blockedOnTaskIds naming it. Then re-measure the three doc anchors, because this row's DoD quotes two of them wrongly and the plan corrects both: `grep -n \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts` (expect 1 match at :93), `grep -n \"New Goal\" docs/agent-stack-runbook.md` (expect :47, BOLDED as `**New Goal** remains disabled` — the unbolded string the DoD quotes returns 0 matches repo-wide), and README.md:88-91 including its 'Measured on 2026-09-03' stamp.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goal-create.ts",
        "docs/agent-stack-runbook.md",
        "README.md"
      ],
      "note": "RE-VERIFICATION (governor comment-21fa144c5a: walk steps as re-verification, not re-implementation). Branch moe/work-2026-09-04 HEAD 7fd23222.\n\nDEPENDENCY GATE — all five landed on this branch, resolved by `git log --oneline --grep \"Moe-Task: task-<id>\" -1`:\n  A  task-741dad13 -> 19e5a5bb [status=DONE role=qa]\n  B  task-4b9c394d -> 74142850 [status=DONE role=qa]\n  C  task-0a5d7212 -> b9a6b8a5 [status=DONE role=qa]\n  C2 task-3506e04a -> 818174f7 [status=DONE role=qa]\n  D342 task-d342a2b1 (the un-parker) -> d103e849 [status=DONE role=qa]; its feat commit a1e12d22 is the one that carries this row's spec file.\nThe step-1 worry that dependsOn omits C2 stays moot: C2 is DONE.\n\nDOC ANCHORS re-measured at this HEAD — all three already rewritten by the earlier session's steps 4/5, and all are COMMITTED, not dirty (`git status --porcelain` over the five deliverable paths returns EMPTY):\n  ANCHOR 1 / DoD 2: `grep -c \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts` -> 0. The replacement at :96-98 names the card: `\" commits. Next step: use the Activate project card on this screen; it drives the whole chain from the browser.\"`, prerequisite half still from labelForMissing.\n  ANCHOR 2 / runbook: `grep -c \"no production writer\" docs/agent-stack-runbook.md` -> 0. :47 still carries the TRUE `**New Goal** remains disabled ...` sentence (kept per plan step 5); :49-56 now say the Activate card is the writer and the daemon mints each receipt from a MEASURED fact.\n  ANCHOR 3 / README: `grep -c \"cannot be started from the browser alone\" README.md` -> 0; :29 now reads `- **Fresh start, from the browser alone**: the Activate project card reads the...`; the 'Measured on' stamp at :123 reads 2026-09-05.\n  live-goal-create.test.ts asserts the NEW sentence at :115, :202, :205 (not absence-only).\n\ntest.fixme: ALREADY GONE. `grep -rn \"test.fixme\" tests/e2e/control-room/` -> 0 matches; the only `d342a2b1` hits in the spec are the :99-107 comment explaining the un-park. task-d342a2b1's commit a1e12d22 removed it, exactly as its own note claimed.\n\nREMAINING REAL WORK, per comment-364556ac: run the journey on the real seed:\"NONE\" lane and re-prove DoD 1/4/7. Nothing in the tree modified by this step.",
      "startedAt": "2026-09-06T00:50:36.451Z",
      "completedAt": "2026-09-06T00:51:14.870Z"
    },
    {
      "stepId": "step-2",
      "description": "STUDY THE LANE, then decide the journey's shape. Read `tests/e2e/control-room/board-chain.spec.ts:1-45` for the idiom and its two stated reasons: (a) `seed: \"NONE\"` leaves the store EMPTY so every commit is authored by a click on a production control — the only arrangement that can see a board-driven move break; (b) this directory's tsconfig CANNOT reach into apps/ (TS6059), so expected strings are HAND-TRANSCRIBED from the modules that produce them, because deriving them would make the assertions self-referential. Follow both. THE CRITICAL DIFFERENCE FROM board-chain.spec.ts: it navigates to `${lane.baseUrl}?v1=1` (:424), and that `?v1=1` param is what serves the LEGACY dev board with its fake DEV_PAYLOADS witness. This journey must navigate to `lane.baseUrl` with NO `v1` param so the V2 app is served — a journey that clicks `cr.liveboard.*` re-proves the pre-existing fake-witness path and certifies nothing. Read `pairing-operator-channel.spec.ts` for how a v2 lane reaches a paired session, and `daemon-ports.ts:376-390` for `DaemonLaneOptions` (`approval`, `liveCredentials`, `seed`, the operator channel).",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/board-chain.spec.ts",
        "tests/e2e/control-room/daemon-ports.ts"
      ],
      "note": "RE-VERIFICATION at HEAD 7fd23222. Every lane fact this step had to establish still holds, and the delivered spec at tests/e2e/control-room/activation-fresh-project.spec.ts obeys all of them.\n\nTHE `?v1=1` TRAP, still real: `grep -n 'v1=1'` -> board-chain.spec.ts:436 `await page.goto(`${lane.baseUrl}?v1=1`)`; apps/control-room/src/main.tsx:99 comments the exact `v1=1` route request that mounts the legacy shell. The delivered journey navigates BARE `lane.baseUrl` (spec :123) so V2 Cordum is served — row rail 3 satisfied, no `cr.liveboard.*` selector anywhere in the spec.\n\nDaemonLaneOptions (daemon-ports.ts:376-397) unchanged: {approval?, fixedDemoGoal?, liveCredentials, operatorChannel?, seed?}. The spec opens `{approval:\"HUMAN\", liveCredentials:\"ATTACHED\", operatorChannel:true, seed:\"NONE\"}`. `seedPid` is `number | null` (:249) and daemon-ports.ts:410-412 states NULL means \"no seed child was ever spawned\" — which is why the spec's first assertion (`expect(lane.seedPid).toBeNull()`) is the lane's own unseeded witness rather than a restatement of the option.\n\n`operatorChannel: true` is load-bearing and is NOT in the plan's option list: without it `pairingOperatorChannelAvailable` stays false, the tab can never pair, and the Activate card never mounts. The spec's `pairBrowser` helper reads the label from `output[aria-label=\"Pairing confirmation label\"]` and types it back on the daemon's own channel via `lane.approvePairing` — the operator's real ritual, nothing approved on their behalf.\n\nHAND-TRANSCRIPTION rule: the spec transcribes the accepted copy `Goal created: <title>` and the refusal fragment rather than importing them from apps/. Its one apps/ import is `ACTIVATION_CHAIN_KINDS` from activation-port.js — a ROSTER, not an expected string, and importing it is what makes the per-kind loop fail closed when a kind is added. (The old TS6059 blanket claim is stale for this lane; see mem:e2e-control-room-lane-can-import-apps-daemon-ts6059-comment-is-stale. The typecheck leg in step 6 is the gate on that.)\n\nDOM RECORDER seated before the first Activate click (spec :177-190), MutationObserver over `[data-testid^=\"cr.activate.step.\"]`, for the board-chain.spec.ts:26-33 reason. No tree modifications in this step.",
      "startedAt": "2026-09-06T00:51:20.659Z",
      "completedAt": "2026-09-06T00:51:42.200Z"
    },
    {
      "stepId": "step-3",
      "description": "THE JOURNEY. Create the spec under `tests/e2e/control-room/`. One lane: `withDaemonBackedControlRoom({ approval: \"HUMAN\", liveCredentials: \"ATTACHED\", seed: \"NONE\", ... })`, asserting `lane.seedPid` is null so the lane proves it ran unseeded. Then, in the browser and ONLY in the browser: navigate to `lane.baseUrl` (no `?v1=1`), reach a paired session, land on the Goals screen, assert New Goal is BLOCKED with the daemon's stated reason, find `cr.activate.root`, assert the receipt rows render, click `cr.activate.button` once, wait for the four chain steps to record, then assert New Goal is now ENABLED and drive `goal.create_with_source` to an ACCEPTED answer. Assert the receipts are MEASURED, not merely present: the repository row must carry the lane's actual HEAD sha (the lane knows it; transcribe rather than derive), and no row may read as a `<projectId>-*` literal. Seat a DOM recorder before the first click — board-chain.spec.ts:26-33 documents why: a card's dispatch report lives only until the next surface poll re-keys the card, so a watcher sampling on its own schedule can watch a dispatch succeed and see nothing.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "tests/e2e/control-room/activation-fresh-project.spec.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/activation-fresh-project.spec.ts"
      ],
      "note": "THE JOURNEY IS DELIVERED AND NOW RUNS GREEN ON A REAL seed:\"NONE\" LANE. The `test.fixme` this row was parked behind is GONE (removed by task-d342a2b1's a1e12d22, which also swept the spec into git); `grep -rn \"test.fixme\" tests/e2e/control-room/` -> 0 matches.\n\nTARGETED RUN, count line quoted (graded on the count line, not the exit code — global rail 8):\n  npx playwright test -c tests/e2e/control-room/playwright.config.ts activation-fresh-project.spec.ts\n  `ok 1 [chromium] > tests\\e2e\\control-room\\activation-fresh-project.spec.ts:98:1 > a fresh project goes from empty store to a created goal, in the browser alone (9.1s)`\n  `1 passed (10.1s)` — exit 0. Re-run inside the full lane: `ok  1 ... (9.7s)`.\n\nNON-VACUITY: TWO MUTATION DRILLS, both on TRACKED paths that were CLEAN before mutating (`git status --porcelain` empty on both), both restored and the restore PROVEN in a call separate from the restore.\n\nDRILL A — is the live copy assertion real? Reinstated the old sentence in live-goal-create.ts (`\" seed); the browser cannot drive the pre-activation chain.\"`). RED, at the intended line:\n  `Error: the copy no longer sends the operator to a terminal ... spec.ts:174`\n  `Received string: \"goal.create_with_source is blocked until Activate the project commits. Next step: run the wrapper (or the demo seed); the browser cannot drive the pre-activation chain.\"`\n  That received string is the strongest single artifact on this row: it is the REAL daemon's refusal for `goal.create_with_source`, rendered by the REAL V2 surface, read out of the browser. It proves the lane drove a live daemon end to end and that DoD 2's copy is asserted LIVE, not merely absent from source.\n  RESTORE PROOF: `git checkout -- <path>`, then separately `git status --porcelain` = EMPTY, `git diff --stat` = EMPTY, `grep -c \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts` = 0.\n\nDRILL B — is \"receipts are MEASURED\" real? In apps/daemon/src/bootstrap/activation-receipts-measure.ts:78 replaced the repository receipt's `head.value` detail with the literal `\"placeholder\"`. RED:\n  `locator resolved to <span class=\"cr2-needs-detail\" data-testid=\"cr.activate.reason.repository\">placeholder</span> - unexpected value \"placeholder\"` at spec.ts:138.\n  So the HEAD-sha assertion really reads the daemon's measured receipt through the DOM; a stubbed receipt cannot pass it.\n  RESTORE PROOF: separate call — `git status --porcelain` EMPTY, `git diff --stat` EMPTY, `sed`/grep shows :78 back to `measuredReceipt(\"repository\", ..., head.value)`.\n\nNO PRODUCTION BEHAVIOUR CHANGED (row rail 2): both drills were mutate-measure-restore, and the delivered tree is byte-identical to the pre-drill tree.\n\nTYPECHECK LEG for this lane: `npx tsc -p tests/e2e/control-room/tsconfig.json` exit 0 — so the spec's one apps/ import (`ACTIVATION_CHAIN_KINDS`) compiles; the plan's blanket TS6059 claim is stale for this directory.\n\nDoD 1 SATISFIED, with one MEASURED DEVIATION already disclosed in the spec at :152-163: the shipped `cr.goals.new` button is NOT gated on the goal.create offer (`createDisabledReason` derives from the connection descriptor only), so the \"BLOCKED then ENABLED\" pairing is asserted against `cr.goals.newgoal.report` — the daemon's own refusal words before Activate, `Goal created: <title>` after — with nothing between the two identical drafts but one click on `cr.activate.button`.",
      "startedAt": "2026-09-06T00:51:48.472Z",
      "completedAt": "2026-09-06T01:05:41.075Z"
    },
    {
      "stepId": "step-4",
      "description": "REPLACE THE COPY THAT CONTRADICTS THE PRODUCT. In `apps/control-room/src/v2/goals/live-goal-create.ts:88-93`, the BLOCKED branch of `goalCreateRefusal` currently ends `\" seed); the browser cannot drive the pre-activation chain.\"`. Replace the whole next-step sentence so it names the Activate card as the action the operator can take here, keeping the function's existing discipline: the prerequisite phrasing still comes from `labelForMissing`, never a paraphrase, and no sentence may carry a credential, csrf token or header. Update `live-goal-create.test.ts` to ASSERT THE NEW SENTENCE, not merely the absence of the old one — an absence-only assertion passes if someone deletes the branch entirely. `grep -n \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts` must return 0 matches at the delivered tree (DoD 2).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goal-create.ts",
        "apps/control-room/src/v2/goals/live-goal-create.test.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/live-goal-create.ts",
        "apps/control-room/src/v2/goals/live-goal-create.test.ts"
      ],
      "note": "RE-VERIFIED AND DRILLED. Delivered at HEAD, committed (working tree clean on both paths).\n\nDoD 2 GATE, verbatim: `grep -n \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts` -> 0 MATCHES.\n\nTHE REPLACEMENT (live-goal-create.ts:97-99), in the BLOCKED branch of `goalCreateRefusal`:\n  `${kind} is blocked until ${step.missing.map(labelForMissing).join(\" and \")}` + \" commits. Next step: use the Activate project card on this screen; it drives the whole chain from the browser.\"\nIt NAMES THE CARD, the prerequisite half still comes from `labelForMissing` (never a paraphrase), and it carries no credential, csrf token or header.\n\nTHE ABSENCE-ONLY HAZARD IS CLOSED, and I proved it rather than asserting it. live-goal-create.test.ts pins the new sentence at :115, :202, :205 via a `BLOCKED_ON_ACTIVATE` constant compared with `toBe` (exact equality, not `toContain`).\nDRILL C: deleted the whole next-step sentence from production (the exact mutation an absence-only test would miss — the plan names it as risk 5) and ran the unit file alone:\n  `cd apps/control-room && npx vitest run src/v2/goals/live-goal-create.test.ts`\n  RED, 4 arms: `Tests  4 failed | 9 passed (13)`, `Test Files  1 failed (1)`, exit 1.\n  `AssertionError: expected 'goal.create is blocked until Activate...' to be '...'`\n  `Expected: \"goal.create is blocked until Activate the project commits. Next step: use the Activate project card on this screen; it drives the whole chain from the browser.\"`\n  `Received: \"goal.create is blocked until Activate the project commits.\"` at live-goal-create.test.ts:294.\n  So the test asserts the NEW SENTENCE, and deleting the branch's guidance is caught by four arms, not zero.\n  RESTORE PROVEN IN A SEPARATE CALL from the restore: `git status --porcelain` EMPTY, `git diff --stat` EMPTY, and :97-99 read back byte-identical to the delivered text. (Tracked path, so `git diff` is a valid restore proof here; the untracked-path SHA256 rule does not apply.)\n\nPACKAGE GATE, count lines quoted (graded on the count line, not the exit code):\n  `pnpm --filter @moe/control-room test` -> `Test Files  164 passed (164)` / `Tests  2034 passed (2034)`, exit 0, 27.46s. Nonzero executed count, so the leg is not vacuous.\n\nLIVE CONFIRMATION of this same copy is in step 3's drill A: the real daemon's refusal, read out of the real browser, was `goal.create_with_source is blocked until Activate the project commits. Next step: ...` — the sentence reaches the operator on the shipped surface, not just in a unit assertion.",
      "startedAt": "2026-09-06T01:05:44.201Z",
      "completedAt": "2026-09-06T01:06:56.138Z"
    },
    {
      "stepId": "step-5",
      "description": "REWRITE THE DOCS TO THE MEASURED TRUTH, in the SAME commit as the journey. README.md:88-91 — the 'What this is not' list currently opens 'a fresh project cannot be started from the browser alone (goal creation waits on the project activation chain, which the seed script or the wrapper drives)'. Rewrite that clause to what now ships, and re-date the 'Measured on 2026-09-03' stamp on line 88. Leave every OTHER claim in that paragraph alone — the one-node fence, the missing preview/release gates, the Codex seat and the trusted-workspace verifier are all still true and are other rows' work. docs/agent-stack-runbook.md:47-52 — the paragraph says '**New Goal** remains disabled until the project ledger contains legitimate durable repository, provider, distribution, backup, credential, and store receipts. The current fresh-project browser flow has no production writer for that complete receipt set, so the project activation and goal cards remain blocked...'. The first sentence stays TRUE and should stay; it is the 'no production writer' sentence that is now false. Say what the Activate card does and that the daemon mints the witness from measured facts. Keep the surrounding manager-flow prose intact.",
      "status": "COMPLETED",
      "affectedFiles": [
        "README.md",
        "docs/agent-stack-runbook.md"
      ],
      "modifiedFiles": [
        "README.md",
        "docs/agent-stack-runbook.md"
      ],
      "note": "RE-VERIFIED at the delivered tree. Both docs are COMMITTED (commit 6d759782, `git show 6d759782 -- README.md docs/agent-stack-runbook.md`) and both paths are CLEAN in the working tree.\n\nREADME.md — the 'What this is not' list. The false clause is GONE: `grep -c \"cannot be started from the browser alone\" README.md` -> 0. The delivered hunk removed exactly `a fresh project cannot be started from the browser alone (goal creation waits on the project activation chain, which the seed script or the wrapper drives);` and re-dated the stamp `Measured on 2026-09-03` -> `Measured on 2026-09-05` (:123). Every OTHER claim in that paragraph is untouched, as the plan required — the multi-node/NOTHING_TO_COMMIT paragraph, the preview/release gates, the Codex seat and the trusted-workspace verifier all still read as before, so the README claims no capability this epic has not shipped. The positive claim was added as its OWN bullet at :29-35 rather than by widening a neighbouring one.\n\ndocs/agent-stack-runbook.md:46-56 — the FIRST sentence was kept verbatim, exactly as the plan directed: `**New Goal** remains disabled until the project ledger contains legitimate durable repository, provider, distribution, backup, credential, and store receipts.` (`grep -n \"New Goal\"` -> :47; it is bolded and starts mid-line, which is why the DoD's unbolded quote finds 0 matches repo-wide — the plan's correction, re-confirmed).\nThe FALSE sentence is gone: `grep -c \"no production writer\" docs/agent-stack-runbook.md` -> 0. Its replacement names the writer (`the **Activate project** card ... reads POST /activation/read and drives project.register, project.bind_repository, provider.probe and project.activate from a single button`), says the daemon mints each receipt from a MEASURED fact with the repository row carrying the real HEAD sha, and PRESERVES the fail-closed half — an unmeasurable receipt renders `UNKNOWN` with its code and layer, so the cards still block with their exact missing-authority reasons. The trailing `Development fixtures cannot clear those gates in a production build.` sentence and the surrounding manager-flow prose are intact.\n\nSAME-COMMIT clause of DoD 3: the docs landed in 6d759782 under this row's trailer. The spec and the copy change were swept into a1e12d22 by task-d342a2b1's wrapper (the known partial-sweep behaviour of the shared checkout, mem:peer-wrapper-partial-sweep-breaks-branch-head) rather than into 6d759782. DISCLOSED, not hidden: all five deliverable paths are on the branch and reachable from HEAD, but they are spread over two commits, not one. Nothing was lost and nothing is dirty.\n\nAlso checked, per QA comment-c2e8e9b8 (a drill mutant had once been swept into history on this exact path): `head -1 docs/agent-stack-runbook.md` reads `# Agent stack runbook` — the `#c` mutant is not present at the delivered tree.",
      "startedAt": "2026-09-06T01:06:58.276Z",
      "completedAt": "2026-09-06T01:07:25.584Z"
    },
    {
      "stepId": "step-6",
      "description": "GATES, LIVE DRIVE, ADVERSARIAL PASS. Run and QUOTE COUNT LINES for all of: `pnpm test:e2e:browser` (the new journey), `pnpm --filter @moe/daemon test` (demo-seed-*.test.ts must be green — parent DoD 4), `pnpm --filter @moe/control-room test`, `pnpm typecheck`, `pnpm test` (root lane — label it 'root lane only, apps/** not covered'). Grade every leg on its `Test Files N passed` line, never its exit code (global rail 8). Repo-wide legs are a path-attributed baseline (global rail 3): record merge-base failures, HEAD failures, and the delta intersected with this feature's owned paths; disclose any foreign red verbatim with its exact failing lines and never fabricate green. Confirm the wrapper's bootstrap path is untouched (`git diff --stat` vs merge-base over the wrapper paths). THEN THE LIVE DRIVE (row rail 4): drive a real fresh project in a real browser and record in a row comment the store path, the HEAD sha, the transcript and the screenshots — keep each comment under 4500 chars, fields truncate silently at 5000. ADVERSARIAL PASS over the whole parent feature: a second concurrent Activate click; a project directory that is not a git repo; a read-only store; a daemon restart mid-chain; a backups directory that already exists. Findings go in the completion summary. FINDINGS ARE REPORTED, NOT PATCHED: a defect in A/B/C/C2 is a comment on that row, because a hardening row that edits what it certifies proves nothing.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "tests/e2e/control-room/activation-fresh-project.spec.ts"
      ],
      "note": "GATES, LIVE DRIVE, ADVERSARIAL PASS — all three delivered. Full detail in row comments comment-21619c70 (live drive) and comment-d9b053bd (gates + foreign-red disclosure); the adversarial findings are filed on the owning row as comment-9cc1ff43 on task-741dad13.\n\nGATES, count lines only (never exit codes): tsc e2e lane exit 0; `pnpm test:e2e:browser` `21 passed | 6 failed` with THIS row's spec `ok 1 ... (9.7s)`; `pnpm --filter @moe/control-room test` `Test Files 164 passed (164)` / `Tests 2034 passed (2034)`; `pnpm typecheck` exit 0 all workspaces Done; demo-seed targeted `Test Files 5 passed (5)` / `Tests 69 passed (69)` (parent DoD 4, nonzero executed count). RED and foreign: `pnpm --filter @moe/daemon test` `12 failed | 493 passed (505)` / `19 failed | 9218 passed`; `pnpm test` ROOT LANE ONLY, apps/** not covered, `5 failed | 426 passed | 1 skipped (432)` / `58 failed | 10296 passed`. Every failing path is attributed in comment-d9b053bd to dirty/untracked peer WIP or to this host's 5000ms spawn-latency cliff; (2)-minus-(1) intersected with this row's owned paths is EMPTY, because this session's only byte change is one arm in a `*.spec.ts` the root lane cannot even see.\n\nONE CODE CHANGE THIS SESSION, and it is a strengthening of THIS ROW'S OWN TEST, found by the adversarial pass over my own diff. `StepRow` sets `ok = done || step.outcome.ok` where `done` is ALREADY_COMMITTED (activation-screen.tsx:96-97), so `data-ok=\"true\"` renders for a chain that committed NOTHING. The per-step arm therefore could not tell \"accepted on this click\" from \"already done\". Added `await expect(step).toContainText(\"accepted\")` alongside the attribute assertion, with the reasoning in-comment.\nDRILLED, because an unproven strengthening is decoration: backed the spec up out of tree (SHA256 d13802af0c26b573cd79e64ca30f93b3b0cc781c33a1261511c817ad90d07feb), flipped the expected word to `\"already done\"`, and the journey went RED at spec.ts:211 — so the card really renders \"accepted\" and the new arm discriminates. Restored from the backup, and proved the restore in a SEPARATE call by SHA256 equality (both sides d13802af…) plus `grep -n` showing :211 back to `.toContainText(\"accepted\")`.\nGreen after the strengthening: `1 passed (6.7s)`, and again `1 passed (8.9s)` on the traced live-drive run.\n\nLIVE DRIVE (row rail 4, DoD 4): real chromium + real daemon + real vite, `seed: \"NONE\"`. HEAD f4e1495ba63b0d29ac4fb5f5cf316bbcd506161a. Store `C:\\Users\\Yaron\\AppData\\Local\\Temp\\moe-e2e-daemon-Wg8YgA\\store.sqlite` (200704 bytes), isolated from 25 leftover scratch dirs by a before/after delta watcher rather than a glob. Trace archived out of tree at `%TEMP%\\ac9d5e2d-livedrive-trace-w2739fee7.zip`, sha256 979e030aaa7586ec707a378316b41388a60ea44991c882bebf21809916f4f53b, 135 entries of which 83 are screenshot frames.\n\nADVERSARIAL PASS, all five scenarios, findings REPORTED not patched (row rail 2): second concurrent Activate click -> fail-closed, but the losing caller sees `backup destination already exists`, which reads as a filesystem accident rather than \"another activation is running\" (the one finding, filed on child A); non-git directory -> `fatal: not a git repository` becomes an UNKNOWN repository receipt and activate refuses; read-only store -> still measures (`read-only, <n> bytes`), while a read-only project root refuses at the backup `mkdir` with the OS error text; daemon restart mid-chain -> next `readSurface()` throws, chain records ACTIVATION_SURFACE_UNREADABLE and stops, and a re-click resumes because committed steps return ALREADY_COMMITTED; pre-existing backups directory -> `pruneBackups` keeps 5, never removes the copy just written, and swallows list/remove failures as best-effort.\n\nWRAPPER BOOTSTRAP UNTOUCHED (DoD 5): no delivered path is under `apps/daemon/src/orchestrator/` or `scripts/`.",
      "startedAt": "2026-09-06T01:07:29.725Z",
      "completedAt": "2026-09-06T01:34:38.924Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "Governor correction: release_task auto-routed this row to REVIEW because all 6 steps show COMPLETED, but nobody has done the resume work yet — the journey spec still carries the test.fixme naming task-d342a2b1, and DoD 1/4/7 have not been re-proven since d342a2b1 landed (a1e12d2269d3). Back to WORKING so a worker resumes per comment-364556ac51fc4e77877754b04270f805: remove the fixme, run the journey on the real seed:NONE lane, quote the count line, then complete_task. Steps are done; only that re-verification remains.",
  "createdBy": "WORKER",
  "parentTaskId": "task-bb844c288fb346c0856778c738b9f073",
  "dependsOn": [
    "task-3506e04abcef4fc3a69299ca238ae1b3"
  ],
  "priority": "HIGH",
  "order": 4,
  "comments": [
    {
      "id": "comment-c2e8e9b869934a679f52842ee09b012b",
      "author": "qa-3fd61a21",
      "content": "DRILL MUTANT STRANDED AND SWEPT INTO HISTORY — restore proof void, now repaired by QA.\n\nWHAT HAPPENED. `docs/agent-stack-runbook.md` line 1 was left mutated as `#c Agent stack runbook` (main and the pre-commit tree both read `# Agent stack runbook`). This row is the contesting owner of that path per the completion trailer of 86657cac (`Moe-Contested: docs/agent-stack-runbook.md (task-ac9d5e2d43334525bd990b1362652ce4)`), and this row is WORKING with NO assignedWorkerId — nobody was left to restore it.\n\nCONSEQUENCE. task-7945f38d owns that runbook path too (it adds `### Reclaim after a restart`) and its wrapper completion commit 86657cac committed the whole file by pathspec, so the mutant became a real hunk in git history:\n```\n-# Agent stack runbook\n+#c Agent stack runbook\n```\nAfter that commit the worktree read CLEAN, so `git status`/`git diff` on the path could never have surfaced it again — the exact blind spot the drill-restore rail names for untracked paths, reached here through a landed commit on a TRACKED path. Any restore proof on this sheet that cited an empty `git diff -- docs/agent-stack-runbook.md` certifies nothing from 86657cac onward.\n\nWHAT I DID. Restored line 1 to `# Agent stack runbook` in the working tree during QA of task-7945f38d (`git diff --stat` = `1 file changed, 1 insertion(+), 1 deletion(-)`, the heading only; the peer's `### Reclaim after a restart` section and everything else untouched). It lands with this QA session's exit commit under task-7945f38d.\n\nACTION FOR THIS ROW. Do not re-mutate without a fresh out-of-tree SHA256 backup, and re-run whatever drill arm depended on that mutation — its earlier red was graded against a mutant that then rode into a commit, so the grading anchor and the delivered tree disagreed. Re-derive the restore proof by SHA256 or by `git diff` against a named SHA, not against the dirty tree.",
      "timestamp": "2026-09-04T21:56:00.422Z"
    },
    {
      "id": "comment-02defeb7ada145e8a10d2b6c309190cf",
      "author": "worker-75411639",
      "content": "STEPS 1-2 MEASUREMENT RECORD, kept so the resumed session does not redo it. Measured at branch moe/work-2026-09-04 HEAD 818174f7 (merge-base main 68eb1c5d).\n\n=== A. DEPENDENCY GATE: PASSED, all four children DONE and landed on this branch ===\nA  task-741dad13 DONE -> 19e5a5bb wip[status=DONE], 47ba19aa feat\nB  task-4b9c394d DONE -> 74142850 wip[status=DONE], c0a154a2 fix\nC  task-0a5d7212 DONE -> b9a6b8a5 wip[status=DONE]\nC2 task-3506e04a DONE -> 818174f7 wip[status=DONE], 13e1282c feat\nThe step-1 worry that `dependsOn` omits C2 is moot: C2 is DONE, so no block was needed on it.\n\n=== B. THE THREE DOC ANCHORS, RE-MEASURED ===\nANCHOR 1 (DoD accurate): `grep -n \"cannot drive the pre-activation chain\" apps/control-room/src/v2/goals/live-goal-create.ts`\n  -> `93:      + \" seed); the browser cannot drive the pre-activation chain.\";`   1 match, at :93.\nANCHOR 2 (DoD MIS-QUOTES it; plan's correction is right):\n  `grep -rn \"New Goal remains disabled\" --include=*.md .` -> 0 MATCHES REPO-WIDE.\n  `grep -n \"New Goal\" docs/agent-stack-runbook.md` -> `47:activation. **New Goal** remains disabled until the project ledger contains`\n  Bolded, and the sentence starts mid-line. Edit by content, never by that line number.\nANCHOR 3 (line numbers DRIFTED off the DoD's 88-91; README grew since 68eb1c5d):\n  `grep -n \"What this is not\" README.md`            -> :107\n  `grep -n \"Measured on\" README.md`                 -> :109  (NOT :88)\n  `grep -n \"fresh project cannot be started\" README.md` -> :110\n  Text at :109-112 is verbatim as the plan describes it, including the 'Measured on 2026-09-03' stamp.\n\n=== C. THE LANE, CONFIRMED ===\n- `?v1=1` trap is REAL: board-chain.spec.ts:436 navigates `${lane.baseUrl}?v1=1`; main.tsx:101 `params.get(\"v1\")===\"1\"` mounts DevelopmentLegacyRoot. Bare `lane.baseUrl` serves V2 Cordum.\n- DaemonLaneOptions (daemon-ports.ts:376-397) = {approval?, fixedDemoGoal?, liveCredentials, operatorChannel?, seed?}; `seed:\"NONE\"` leaves `seedPid` null (:404-408). `liveCredentials:\"ATTACHED\"` bakes VITE_MOE_LIVE_CREDENTIAL/CSRF (serverEnv, :167-178) so the V2 app is attached with NO pairing handshake.\n- installReportRecorder (board-chain.spec.ts:187-213) = page.addInitScript + MutationObserver over `[data-testid^=\"cr.liveboard.report.\"]`; the V2 analogue keys on `cr.activate.*`.\n- V2 testids the journey needs ALL exist: `cr.activate.{root,count,receipts,receipt.<member>,reason.<member>,code.<member>,signing,button,steps,step.<kind>,nowire,refusal}` (activation-screen.tsx); `cr.goals.new`, `cr.goals.newgoal.{form,title,outcome,criteria,prd.input,prd.status,report}` (goals-home.tsx:193-215, new-goal-form.tsx).\n- LiveActivate is mounted on Goals at cordum-app.tsx:369-371, pollMs=PAUSE_POLL_MS.\n- NOTE for whoever writes the journey: `cr.goals.new` is disabled ONLY by `createDisabledReason`, which cordum-app.tsx:336-340 derives from the CONNECTION descriptor, not from the goal.create step being BLOCKED. So pre-activation the button is ENABLED; the daemon's stated blocked reason surfaces at `cr.goals.newgoal.report` after submitting the form (live-goals.tsx -> createGoalDispatcher -> `goalCreateRefusal`). The DoD's \"New Goal is BLOCKED / then ENABLED\" must be read against that report, not against the button's disabled attribute. Also: `goal.create_with_source` is chosen ONLY when a PRD file is attached (live-goal-create.ts:203-206), so the journey must drive `cr.goals.newgoal.prd.input` via setInputFiles.\n- The repository receipt's rendered `reason` IS the HEAD sha: activation-receipts-measure.ts:78 `measuredReceipt(\"repository\", `repository/${top}@${head}`, head.value)` -> activation-read.ts receiptRow maps `detail` to `reason`, rendered at `cr.activate.reason.repository`.\n\n=== D. THE BLOCKER ===\n`/activation/read` is never wired into the production daemon; it answers HTTP 503 LISTENER_ACTIVATION_UNAVAILABLE on every real boot. Full evidence + the probe's complete source are in the comment I left on task-0a5d7212. Prerequisite row filed: task-316bb83b94a14e78957d282cb41566ba (PLANNING, order 3.5, parent bb844c28).\n\nConsequence for THIS row: ActivateScreen (activation-screen.tsx:113-124) early-returns on `outcome.status !== \"ACTIVATION\"` and never mounts `cr.activate.button`, so DoD 1 (journey), 4 (live drive) and 7 (adversarial pass over Activate) are unreachable, and DoD 2/3 would have me write copy and README/runbook prose asserting a browser-only fresh start that demonstrably does not work. Row rail 2 forbids fixing it here. Nothing in the tree was modified by steps 1-2.",
      "timestamp": "2026-09-05T03:30:06.971Z"
    },
    {
      "id": "comment-0d3db1fb4edd4df590a1c6e01b3bb06c",
      "author": "architect-fa3a3e17",
      "content": "OVERLAP NOTICE from task-80322112a48d44c69c6c55f846f8d43f (architect-fa3a3e17, 2026-09-05). Not a request for action on this row — a heads-up so two rows do not edit the same files, and a record that this row's blocked measurement is now load-bearing for a second epic.\n\nWHAT I FOUND WHILE PLANNING task-80322112 (\"New product from PRD: one form in the browser reaches a bound repository and a goal\", epic-3f9b9556, SPIDR child 3 of task-5ef1a0a9). Its DoD asks for a browser-only Playwright journey with no seed and no script, plus a README/VISION copy rewrite. Both are already yours:\n- `tests/e2e/control-room/activation-fresh-project.spec.ts` — yours, untracked at HEAD, `seed: \"NONE\"`, asserts `lane.seedPid` is null, drives the v2 card, parked with one `test.fixme` naming task-d342a2b1.\n- The README \"What this is not\" fresh-project sentence and the runbook paragraph — already rewritten by your steps 4 and 5, which is why `git grep -n \"cannot be started from the browser alone\"` now returns ZERO hits at HEAD. My row's DoD 5 wrote that grep as its gate; it is already satisfied and therefore vacuous as written. I have recorded that on my row rather than letting it retire the question unanswered.\n\nCONSEQUENCE FOR MY ROW, already acted on: I set task-80322112's dependsOn to include BOTH task-d342a2b1 (the activation-chain policy fix your block names) and THIS row. My journey needs a project activated before `goal.create_with_source` is offered, so it walks into the same `ACTIVATION_POLICY_UNMEASURED @ DAEMON_ACTIVATION_RECEIPTS` wall you measured. Planning a journey that cannot pass, or mocking past it, would be exactly the false completion you refused to file.\n\nWHAT I AM NOT DOING: not touching your spec, not re-editing the README sentences you rewrote, not re-filing your block. My plan writes a SEPARATE spec for the new-product-from-PRD journey and reuses your harness pattern (`withDaemonBackedControlRoom`, `daemon-ports.ts`, `seed: \"NONE\"`, the `lane.seedPid === null` assertion) rather than inventing a second one.\n\nTHANK YOU FOR THE HEADER on that spec — three things in it saved me from planning the wrong journey, and I have carried them into my plan: (1) `main.tsx:101` serves the legacy shell only for `?v1=1`, so the bare `lane.baseUrl` is the product and a journey clicking `cr.liveboard.*` re-proves the old dev path; (2) a card's dispatch report lives only until the next surface poll re-keys the card (board-chain.spec.ts:26-33), so the recorder must be seated before the first click; (3) receipts asserted as MEASURED — the repository row carrying the lane's own HEAD sha read from git, and no row reading as a `<projectId>-*` placeholder.",
      "timestamp": "2026-09-05T18:27:08.090Z"
    },
    {
      "id": "comment-364556ac51fc4e77877754b04270f805",
      "author": "governor-afbde490",
      "content": "GOVERNOR RESUME NOTE (23:11Z). This row auto-unblocked because its only blocker, task-d342a2b1 (activation chain installs the policy), is DONE at completion commit a1e12d2269d3. All 6 steps are already COMPLETED, so the runtime will let you complete_task immediately — DO NOT. The park reason was that DoD 1, 4 and 7 were unreachable while project.activate refused ACTIVATION_POLICY_UNMEASURED on the seed:\"NONE\" lane, and the journey was parked with ONE `test.fixme` naming task-d342a2b1 (step 3). Before completing: (1) remove that `test.fixme` and run the journey spec against the real daemon on the seed:\"NONE\" lane (recipe: mem:control-room-live-lane-recipe-2026-09-05); quote the count line, not the exit code. (2) Re-check DoD 1, 4, 7 explicitly and record the transcript path + sha in a comment. (3) If activate now refuses with a DIFFERENT code, report_blocked with that code — never re-park on the old reason. (4) Targeted runs only: the control-room package suite needs a GATE START in #general (one full-suite gate on the host at a time). If everything is green, complete_task with a summary that names the fixme removal and the live run.",
      "timestamp": "2026-09-05T23:11:36.512Z"
    },
    {
      "id": "comment-21fa144c5a25421d962736114e530cc6",
      "author": "governor-afbde490",
      "content": "GOVERNOR NOTE 00:40Z — READ WITH comment-364556ac. The six steps show PENDING again and reopenCount is 1 because I moved this row REVIEW→WORKING at 00:36Z (release_task had auto-routed it to REVIEW on the 6/6 count; the runtime treats the flip back as a reopen). Nothing was rejected by QA and no bytes were lost: every step's deliverable is already in the shared checkout and in the row's checkpoints (latest 74ec649f5cb8; one ref-contention rescue at refs/moe/rescue/task-ac9d5e2d…/20260905T232440Z). So: walk the steps as RE-VERIFICATION, not re-implementation — start_step, confirm the artifact exists and matches the step, complete_step with the evidence. The only real work is what comment-364556ac says: remove the test.fixme naming task-d342a2b1, run the journey on the real seed:\"NONE\" lane, quote the count line, re-check DoD 1/4/7. QA: a rejection on this row would be reopen #2 and flips it to PLANNING — reject only for a real defect, with the exact failing clause.",
      "timestamp": "2026-09-06T00:37:41.459Z"
    },
    {
      "id": "comment-21619c70f42c4005962dfef6e9b15e83",
      "author": "worker-2739fee7",
      "content": "LIVE DRIVE RECORD (row rail 4 / DoD 4) — worker-2739fee7, 2026-09-06.\n\nWHAT WAS DRIVEN. A real chromium against a REAL daemon process and a REAL vite dev server, on a store no script ever seeded (`seed: \"NONE\"`, `lane.seedPid` asserted null before the page is touched). Browser-only: navigate -> pair on the daemon's own operator channel -> submit a goal draft and read the daemon's refusal -> click `cr.activate.button` ONCE -> five chain steps accepted -> resubmit the SAME draft -> `Goal created: Browser-only fresh start`.\n\nHEAD SHA at the drive: `f4e1495ba63b0d29ac4fb5f5cf316bbcd506161a` (branch moe/work-2026-09-04; the branch head moves under peers, so this is the sha the drive actually ran at, taken in the same shell invocation as the run).\n\nSTORE PATH, this run's own, isolated from the 25 leftover scratch dirs by a before/after delta watcher rather than a glob:\n  `C:\\Users\\Yaron\\AppData\\Local\\Temp\\moe-e2e-daemon-Wg8YgA\\store.sqlite`  (200704 bytes)\n  observed at 01:32:11Z and 01:32:14Z during the run; projectId `e2e-proj-wg8yga`. The lane deletes its scratch at teardown, which is why the path is recorded live rather than after.\n\nTRANSCRIPT (the run's own count line — graded on the count line, not the exit code):\n  `npx playwright test -c tests/e2e/control-room/playwright.config.ts activation-fresh-project.spec.ts --trace on`\n  `ok 1 [chromium] > tests\\e2e\\control-room\\activation-fresh-project.spec.ts:98:1 > a fresh project goes from empty store to a created goal, in the browser alone (7.9s)`\n  `1 passed (8.9s)` — exit 0.\n\nSCREENSHOTS. Playwright trace captured with `--trace on` and archived OUT OF TREE so it survives a `test-results` clean:\n  `C:\\Users\\Yaron\\AppData\\Local\\Temp\\ac9d5e2d-livedrive-trace-w2739fee7.zip`\n  sha256 `979e030aaa7586ec707a378316b41388a60ea44991c882bebf21809916f4f53b`\n  135 entries, of which **83 are screenshot frames** (`resources/page@3a8e43f4…-<ms>.jpeg`), one per action — the pairing card, the pre-activation refusal, the receipt rows, the Activate click, each chain step and the accepted create. Open with `npx playwright show-trace <path>`.\n\nWHY THIS IS NOT A HERMETIC FAKE. The strongest single artifact is the drill in step 3: reinstating the old sentence in production made the browser read back\n  `\"goal.create_with_source is blocked until Activate the project commits. Next step: run the wrapper (or the demo seed); the browser cannot drive the pre-activation chain.\"`\nThat string is the REAL daemon's refusal for a REAL command kind, rendered by the shipped V2 surface. A mocked lane cannot produce it. A second drill blanked the daemon's measured HEAD-sha receipt to the literal `placeholder` and the browser rendered `<span data-testid=\"cr.activate.reason.repository\">placeholder</span>`, failing the arm — so the \"receipts are MEASURED\" assertion really reads the daemon's measurement through the DOM.\n\nNAVIGATION: bare `lane.baseUrl`, no `?v1=1` (row rail 3). The spec contains no `cr.liveboard.*` selector, so this certifies the V2 product surface and not the legacy dev board's DEV_PAYLOADS witness.",
      "timestamp": "2026-09-06T01:33:21.154Z"
    },
    {
      "id": "comment-d9b053bd521e4a86bd9629c633f851ed",
      "author": "worker-2739fee7",
      "content": "GATE RESULTS AND FOREIGN-RED DISCLOSURE (DoD 6, global rail 3 path-attributed baseline) — worker-2739fee7, 2026-09-06. Every leg graded on its count line, never its exit code.\n\nGREEN, OWNED:\n1. `npx tsc -p tests/e2e/control-room/tsconfig.json` — exit 0 (the lane's typecheck leg of `pnpm test:e2e:browser`).\n2. `pnpm test:e2e:browser` (playwright leg, full lane) — `21 passed`, `6 failed`, 9.6m. THIS ROW'S SPEC: `ok 1 ... a fresh project goes from empty store to a created goal, in the browser alone (9.7s)`.\n3. `pnpm --filter @moe/control-room test` — `Test Files 164 passed (164)`, `Tests 2034 passed (2034)`, exit 0.\n4. `pnpm typecheck` — exit 0, every workspace `Done` (apps/control-room, apps/daemon, packages/{benchmark,scheduler,review,runner}).\n5. demo-seed (parent DoD 4), targeted with a nonzero executed count: `cd apps/daemon && npx vitest run --root . --config package.json --testTimeout=30000 src/orchestrator/demo-seed-{env,http,main,payloads,plan}.test.ts` -> `Test Files 5 passed (5)`, `Tests 69 passed (69)`, exit 0.\n\nRED, ALL FOREIGN — disclosed verbatim, none fabricated green:\n6. `pnpm --filter @moe/daemon test` — `Test Files 12 failed | 493 passed (505)`, `Tests 19 failed | 9218 passed | 2 skipped (9239)`, 791s.\n   ATTRIBUTION: every one of the 12 files is under apps/daemon, which this row owns NO path in, and the shared checkout is mid-flight with peer WIP on exactly those modules. `git status --porcelain apps/daemon` shows 19 MODIFIED + 6 UNTRACKED peer paths, including the precise subjects that fail:\n   - `bootstrap-services.test.ts` \"covers exactly the twelve command kinds this surface owns\" and `j1-command-path.test.ts` \"publishes exactly the twelve owned kinds\" — roster counts, with `daemon-command-registry.ts`, `daemon-command-families.ts` and `daemon-command-async-entries.ts` all dirty (task-54b74b8041c64bc2868435bcbdac6dd6, release.decide wiring, itself BLOCKED mid-edit).\n   - `mcp-http-host.test.ts` / `mcp-main.test.ts` exclusion-roster arms — both test files dirty.\n   - `controlled-profile-generator.test.ts` (4 arms, .env.example names) — generator AND test dirty, plus untracked `environment/environment-required-variables.ts` (task-fd2d438f).\n   - `goal-services.test.ts` / `goal-live-evidence.test.ts` / `j1-command-path.test.ts` goal.close arms — commit 4b6d2bc2 \"close goals on scoped criterion evidence\".\n   - `runtime-entrypoint.test.ts` \".js bridge for every runtime module\" — 6 untracked peer modules without bridges.\n7. `pnpm test` (ROOT LANE ONLY — apps/** IS NOT COVERED by the root vitest include) — `Test Files 5 failed | 426 passed | 1 skipped (432)`, `Tests 58 failed | 10296 passed | 44 skipped (10398)`, 437s. The 5:\n   - `tools/packaging/pack-source.test.ts` \"materializes a selected commit without its tracked runtime state\" and `tools/packaging/pack-windows-main.test.ts` \"refuses tracked or untracked live packer drift from the labeled commit\" — both `Test timed out in 5000ms`; the second one's SUBJECT is untracked drift, and this checkout has 20+ foreign dirty/untracked paths.\n   - `packages/benchmark/src/freeze-manifest-admission.test.ts` — `Test timed out in 5000ms`.\n   - `packages/runner/src/verification/criterion-check-executor.test.ts` — `expected {…} to match object { containment: 'PROVEN' }`.\n   - `tests/e2e/foundation/multi-node-graph.e2e.test.ts` — `expected { runRef: 'multi-verify-all' } to match object { status: 'COMPLETED' }`.\n   Three of the five are bare 5000ms timeouts, the signature of this host's spawn-latency cliff.\n\n(2)-MINUS-(1) INTERSECTED WITH OWNED PATHS = EMPTY. This session's only byte change is one arm inside `tests/e2e/control-room/activation-fresh-project.spec.ts`, which the root vitest lane cannot even see (root includes `tests/**/*.test.ts`; the browser lane is `*.spec.ts` — disjoint by naming, per playwright.config.ts). No failing path is one this row owns.\n\nWRAPPER BOOTSTRAP UNTOUCHED (DoD 5): this row's delivered path set is README.md, docs/agent-stack-runbook.md, tests/e2e/control-room/activation-fresh-project.spec.ts, apps/control-room/src/v2/goals/live-goal-create.{ts,test.ts}. None is under `apps/daemon/src/orchestrator/` or `scripts/`. The large diff those paths show against merge-base 68eb1c5d is entirely peers' (seat-exit-classifier, seat-output-tail, up.ps1, ...), not this row's.",
      "timestamp": "2026-09-06T01:33:47.226Z"
    },
    {
      "id": "comment-a0881913e3414859a056170af8a6f98f",
      "author": "qa-f2906c26",
      "content": "QA REVIEW EVIDENCE (approval; reviewSummary is capped at 2000 so the detail lives here, global rail 14). Reviewed at completion commit d0c69621 = HEAD of moe/work-2026-09-04; kind=completion, Moe-Task trailer correct, `git branch --contains d0c69621` = the branch. I verified delivered bytes, not the summary.\n\nGATES I RE-RAN MYSELF (count lines, never exit codes):\n1. `npx tsc -p tests/e2e/control-room/tsconfig.json` -> exit 0.\n2. `npx playwright test -c tests/e2e/control-room/playwright.config.ts activation-fresh-project.spec.ts` -> `1 passed (7.9s)`.\n3. `pnpm --filter @moe/control-room test` -> `Test Files 167 passed (167)`, `Tests 2057 passed (2057)`, exit 0. Higher than the worker's 164/2034 because peers landed tests since; the package that owns live-goal-create.{ts,test.ts} is fully green at review time.\n4. FULL browser lane -> `22 passed | 5 failed (7.5m)`, this row's spec `ok 1`. BETTER than the disclosed 21|6.\n\nMY OWN NON-VACUITY DRILL, independent of the worker's three. Mutated the daemon's measured HEAD sha at apps/daemon/src/bootstrap/activation-receipts-measure.ts:78 to \"qaDrillPlaceholder\". The journey went RED at spec.ts:138 and the DOM read `<span data-testid=\"cr.activate.reason.repository\">qaDrillPlaceholder</span>` — so the \"receipts are MEASURED\" arm really reads the daemon's measurement through the browser. Restored with `git checkout --`; restore proven in a SEPARATE call by blob-hash equality, `git hash-object` == `git rev-parse HEAD:<path>` == fbf13abcbb6cd914ed7ba5124c4457d2b311bcf4, and 0 occurrences of the marker.\n\nTHE 5 E2E REDS, ATTRIBUTED PER FAILING FILE (not per directory), all foreign:\n- journeys.spec.ts:65 CR-J1-002 — `getByTestId('cr.nav.resources')` toBeDisabled() failed. cordum-app.tsx is DIRTY with peer WIP; task-bc4617d6's own summary describes rewiring that panel.\n- journeys.spec.ts:86 CR-A11Y-001 — toHaveLength on truth classes; styles/cordum-shell-layout.test.tsx DIRTY.\n- prd-persistence-boundary.spec.ts:127 — `Test timeout of 180000ms exceeded`. Owner = dfe9329c, task-503e927b, already on worker-afd89b78's disclosed e2e list. It runs a FULL `vite build` of the dirty apps/control-room inside the 180s budget; error-context.md shows the shell fully mounted, so the budget died in build/boot, not in an assertion. Fails in ISOLATION too, so it is not my concurrent gate either.\n- prd-to-approval.spec.ts:130 — peer CASING drift: `Expected pattern: /\\d+ GOAL/u`, `Received string: \"1 goal\"`.\n- provider-pause.spec.ts:181 — \"the wrapper must staff node.deliver@...\"; wrapper staffing, no control-room path.\nNone asserts this row's changed surface: journeys.spec.ts, prd-to-approval.spec.ts and provider-pause.spec.ts have ZERO references to `cr.activate`, the Activate copy or `newgoal`. prd-persistence-boundary touches `cr.goals.newgoal.report` only structurally (not.toHaveText(\"\"), textContent reads) and never pins the BLOCKED sentence. Failing-path set INTERSECT this row's owned paths = EMPTY (global rail 3).\n\nTHREE JUDGEMENT CALLS, recorded so they are not relitigated:\n(a) DoD 3's \"in the SAME commit as the journey\" is NOT literally met. The bytes span 6d759782 (docs, this row), e4855265 (the live-goal-create copy, swept by peer task-9ade7f88's wrapper), a1e12d22 (the spec, swept by task-d342a2b1's wrapper) and d0c69621. The worker disclosed the spread but misattributed the copy to a1e12d22; it is e4855265 (`git show e4855265^:...` still has the old sentence, HEAD has the new one). Wrapper-sweep artifact under the shared-checkout commit rail, all four ancestors of HEAD, DoD 2's grep = 0 at the delivered tree. Not a defect.\n(b) DoD 5's \"`pnpm seed` with its count line\" is unsatisfiable as written: `seed` = `node apps/daemon/src/orchestrator/demo-seed-main.ts`, a seed RUN with no count line. Substance proven by demo-seed `Test Files 5 passed (5)` / `Tests 69 passed (69)`, and the delivered path set touches nothing under apps/daemon/src/orchestrator/ or scripts/, so the wrapper bootstrap cannot have moved.\n(c) DoD 7's \"recorded in the completion summary\" — completionSummary is EXACTLY 2000 chars, truncated mid-DoD-6. The findings are in comment-9cc1ff43 on the OWNING row task-741dad13, which is what row rail 2 requires and what global rail 14 designates as the overflow.\n\nALSO CHECKED: the disclosed DoD-1 deviation is TRUE — cordum-app.tsx:374-379 `createDisabledReason` derives ONLY from `describeConnection(connection)`, never from the goal.create offer, so asserting a disabled New Goal would assert a product that does not exist; the substitute arm (the daemon really refuses on submit, in this row's rewritten words) proves more. `goal.create_with_source` really is the kind driven (live-goal-create.ts:248-250 keys on draft.prd, and the spec attaches a real file and pins the browser-computed sha256). A prior QA's stranded `#c Agent stack runbook` drill mutant is repaired at HEAD. Dependencies A/B/C/C2 all DONE and merged. live-goal-create.ts is 275 lines — over the 250 target, under the 400 split threshold, and the growth is peer task-9ade7f88's, not this row's ~3 lines.\n\nOPEN FINDING, filed and not chargeable here: two concurrent activations fail closed but the loser reads `backup destination already exists`, which looks like a stale-file accident rather than \"another activation is in flight\"; the browser's inFlight useRef guard is per-tab only. It sits with task-741dad13 (comment-9cc1ff43).",
      "timestamp": "2026-09-06T01:56:50.602Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-04T09:37:11.087Z",
  "updatedAt": "2026-09-06T01:57:57.395Z",
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-04T09:54:23.604Z",
  "metrics": {
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 7,
    "firstClaimAt": "2026-09-05T03:22:24.779Z",
    "executedStepCount": 12,
    "doneAt": "2026-09-06T01:57:38.856Z",
    "wallClockMs": 81314077
  },
  "planningNotes": {
    "approachesConsidered": "Rejected extending board-chain.spec.ts: it navigates to `${lane.baseUrl}?v1=1`, and that param serves the LEGACY dev board whose DEV_PAYLOADS witness is exactly the fake this feature removes. Extending it would prove the old path. A new spec on the V2 surface is the only journey that certifies the product.\n\nRejected deriving the expected strings from apps/ modules: tests/e2e/control-room/tsconfig.json cannot reach apps/ (TS6059), and board-chain.spec.ts:35-43 states the deeper reason — assertions derived from the module that produces them pass whatever the board put on screen. Hand-transcribe, as that spec does.\n\nRejected asserting only that the receipts render: the whole point is that they are MEASURED, so the journey asserts the repository row carries the lane's actual HEAD sha and that no row reads as a `<projectId>-*` literal. Rendering six rows would pass just as well against the old fake witness.\n\nOn the docs, rejected rewriting the whole README paragraph: only the fresh-project clause is now false. The one-node fence, the absent preview/release gates, the Codex seat and the trusted-workspace verifier are still true and belong to other rows of this epic — rewriting them here would make the README claim capability that has not shipped.",
    "codebaseInsights": "MEASURED AT main 68eb1c5d.\n\nTHE DoD MIS-QUOTES TWO ANCHORS; the plan corrects both.\n- `grep -rn \"New Goal remains disabled\" --include=*.md .` -> 0 MATCHES. The real line is docs/agent-stack-runbook.md:47 and it is BOLDED: `**New Goal** remains disabled until the project ledger contains legitimate durable repository, provider, distribution, backup, credential, and store receipts.` The FALSE sentence is the next one: 'The current fresh-project browser flow has no production writer for that complete receipt set, so the project activation and goal cards remain blocked with their exact missing-authority reasons.'\n- README.md:88-91: 'Measured on 2026-09-03, these are still missing or manual: a fresh project cannot be started from the browser alone (goal creation waits on the project activation chain, which the seed script or the wrapper drives); ...'. The date stamp on :88 also needs re-dating.\n- live-goal-create.ts:88-93 IS accurate as the DoD states it: the BLOCKED branch of `goalCreateRefusal` ends `+ \" seed); the browser cannot drive the pre-activation chain.\";` at :93, and the DoD's grep is satisfiable verbatim.\n\nTHE LANE IS REAL AND ALREADY DRIVES A DAEMON.\n- package.json:22 `test:e2e:browser` = `tsc -p tests/e2e/control-room/tsconfig.json && playwright test -c tests/e2e/control-room/playwright.config.ts`.\n- daemon-ports.ts:376+ `DaemonLaneOptions` = `approval?` (omitted = SPEED), `fixedDemoGoal?`, `liveCredentials`, `seed`, plus an operator-channel option; `withDaemonBackedControlRoom(options, fn)` yields a `DaemonLane` with `baseUrl` and `seedPid` (NULL when `seed: \"NONE\"`).\n- daemon-ports.ts:14-18 states why an unseeded lane is the only one that can see a board-driven move break.\n\nTHE `?v1=1` PARAM IS THE WHOLE GAME. board-chain.spec.ts:424 does `page.goto(`${lane.baseUrl}?v1=1`)` and then asserts `cr.liveboard` — that is the LEGACY board with DEV_PAYLOADS. Navigating without the param serves V2. A journey that forgets this passes while proving the wrong surface.\n\nDOM RECORDER, not sampling: board-chain.spec.ts:26-33 — live-board keys reports by [kind, aggregateId, version], so the commit that answers a click is also what destroys the answer's rendering. Seat a recorder before the first click.\n\nEXISTING V2-REACHING SPECS to copy pairing from: journeys.spec.ts, prd-to-approval.spec.ts, prd-persistence-boundary.spec.ts, pairing-operator-channel.spec.ts.",
    "risks": "1. THE `?v1=1` TRAP. Copying board-chain.spec.ts's navigation verbatim serves the legacy board, and the journey will pass while proving the fake-witness path this whole feature removes. This is the single most likely way to ship a green row that certifies nothing.\n\n2. THIS ROW'S dependsOn IS INCOMPLETE. It names C (task-0a5d7212) but not C2 (task-3506e04a), which was split out of C at plan time and is the row that actually ships the card this journey clicks. `moe.set_task_dependencies` refuses this architect seat, so step 1 verifies C2 by hand. A worker who trusts the field alone can start against a board with no Activate card.\n\n3. FAIL-CLOSED NEEDS A REAL PROVIDER CLI. Child B refuses activation when any receipt is unmeasurable, and the provider receipt probes `claude --version`. If the e2e host has no provider CLI on PATH, the journey fails at the Activate step for an environmental reason that looks exactly like a code defect. Check the host before debugging the card.\n\n4. THE DOCS PARAGRAPHS CARRY OTHER ROWS' TRUTHS. README:88-101 and runbook:45-52 each mix this feature's claim with claims about the one-node fence, preview/release gates, the Codex seat, the verifier and the manager flow. Rewriting more than the fresh-project clause would make the repo claim capability that has not shipped — a worse defect than the stale sentence.\n\n5. AN ABSENCE-ONLY COPY TEST IS VACUOUS. Asserting the old sentence is gone passes if someone deletes the whole BLOCKED branch. Assert the NEW sentence.\n\n6. THE LIVE DRIVE IS NOT OPTIONAL and is not satisfied by the e2e run (row rail 4). Never restart the wrapper to unstick a seat — the claim TTL is 30 minutes.",
    "keyFiles": [
      "tests/e2e/control-room/board-chain.spec.ts",
      "tests/e2e/control-room/daemon-ports.ts",
      "tests/e2e/control-room/pairing-operator-channel.spec.ts",
      "apps/control-room/src/v2/goals/live-goal-create.ts",
      "README.md",
      "docs/agent-stack-runbook.md",
      "package.json"
    ]
  },
  "planApprovedAt": "2026-09-04T09:54:25.707Z",
  "contextFetchedBy": [
    "worker-2739fee7",
    "qa-f2906c26"
  ],
  "workStartedAt": "2026-09-05T03:22:38.352Z",
  "blockedReason": null,
  "blockedOnTaskIds": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "commits": [
    {
      "sha": "36e501b88cd882de912c7261fbe0c2d8e27c1c8f",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-75411639@2026-09-05T03:22:25Z",
      "paths": [
        ".moe/tasks/task-0a5d7212a7f644d5891df4e0e1545787.json",
        ".moe/tasks/task-0b26dc9e8582428ea1af8032d9277550.json",
        ".moe/tasks/task-316bb83b94a14e78957d282cb41566ba.json",
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-75411639",
      "recordedAt": "2026-09-05T03:32:23.539Z",
      "treeId": "563f1ab1c0f6cc1a1d1c402ea90966e3a3363d13",
      "status": "BLOCKED",
      "pushed": true
    },
    {
      "sha": "6d759782e0475fd9b43aa5aa5ef63b805fae1d68",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:12:50Z",
      "paths": [
        ".moe/tasks/task-40e52fa8a85a4921b97bee933b00403f.json",
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json",
        ".moe/tasks/task-c851961b968942f880950acc16dc575a.json",
        "docs/agent-stack-runbook.md",
        "README.md"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:19:14.911Z",
      "treeId": "841ee6bffc1ddc018045a64d4a6e86b18349c594",
      "status": "WORKING",
      "contested": [
        {
          "path": "docs/agent-stack-runbook.md",
          "taskId": "task-6cbcf5ff05084f3ea2a2e3ea89c3fb54"
        },
        {
          "path": "README.md",
          "taskId": "task-161b7e9d566b4c47b429490a54a6cca1"
        }
      ],
      "pushed": true
    },
    {
      "sha": "9aa86c9f45dd24329d3f8ccee550418dff08ea75",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:19:54Z",
      "paths": [
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:20:22.846Z",
      "treeId": "22689b5030c492ef54b175270bc2a1cbd2243b8c",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "61b914f9ee22889a7c419837246d4a542efbdeb5",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:20:55Z",
      "paths": [
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:21:30.207Z",
      "treeId": "17e4d152532f2d2ee005e01b3fb500c2c2fee9b6",
      "status": "WORKING",
      "pushed": false
    },
    {
      "sha": "c719aec8dfa8fdab544294432c6ff6da8731a5dd",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:22:08Z",
      "paths": [
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:22:58.932Z",
      "treeId": "6cbb42baeb284b046340377052054a8b94e2173b",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "81172b946dc34f38f7e584132826e214788e5594",
      "ref": "refs/moe/rescue/task-ac9d5e2d43334525bd990b1362652ce4/20260905T232440Z",
      "kind": "rescue",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:23:37Z",
      "paths": [
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:24:45.120Z",
      "status": "WORKING",
      "pushed": false,
      "message": "MOE_RESCUE_REF task=task-ac9d5e2d43334525bd990b1362652ce4 ref=refs/moe/rescue/task-ac9d5e2d43334525bd990b1362652ce4/20260905T232440Z sha=81172b946dc34f38f7e584132826e214788e5594 reason=ref-contention"
    },
    {
      "sha": "9363dba86a00383cb1e1840cc2f28f196d266d19",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:25:20Z",
      "paths": [
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:25:34.465Z",
      "treeId": "f517c9783c01e3669dd247f735c4b6ffbbbc1dec",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "74ec649f5cb8cfc94d7b8caa2f712466933c6e3a",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-05T23:25:20Z",
      "paths": [
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-05T23:26:09.237Z",
      "treeId": "03c179e5ed5a49d327623ffdcc7cf2f6e0ae4a0d",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "d0c696213591820e0da327b4e21c336c4da8ca5f",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "completion",
      "role": "worker",
      "sessionId": "worker-2739fee7@2026-09-06T00:49:07Z",
      "paths": [
        ".moe/tasks/task-741dad13e23d441a8385d0f14df5b5c1.json",
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json",
        "tests/e2e/control-room/activation-fresh-project.spec.ts"
      ],
      "recordedBy": "worker-2739fee7",
      "recordedAt": "2026-09-06T01:36:31.405Z",
      "treeId": "b8b5750e7d7399f771eda2f28d0ddb6abe30f532",
      "status": "REVIEW",
      "pushed": true
    },
    {
      "sha": "b16bc4558647e8759bf179757a22d811a1ed1a5d",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "qa",
      "sessionId": "qa-f2906c26@2026-09-06T01:36:30Z",
      "paths": [
        ".moe/tasks/task-3780109018bb49b2bf3a00b18952d7ff.json",
        ".moe/tasks/task-8e793ae3c63f41189bcc05f6d7c15605.json",
        ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json",
        ".moe/tasks/task-bb0a71316bd343d18453224973563ed0.json",
        ".moe/tasks/task-bb844c288fb346c0856778c738b9f073.json",
        ".moe/tasks/task-c0e00f0a55ad43bfa5f6b323779834cd.json",
        ".moe/tasks/task-db63dacc7cbe49cab9826e9b49c8669f.json",
        "docs/agent-stack-runbook.md",
        "README.md"
      ],
      "recordedBy": "qa-f2906c26",
      "recordedAt": "2026-09-06T01:57:57.394Z",
      "treeId": "e766c4d0c6eddca483e121e278bb29fe48e2bd11",
      "status": "DONE",
      "contested": [
        {
          "path": "docs/agent-stack-runbook.md",
          "taskId": "task-6cbcf5ff05084f3ea2a2e3ea89c3fb54"
        },
        {
          "path": "README.md",
          "taskId": "task-161b7e9d566b4c47b429490a54a6cca1"
        }
      ],
      "pushed": true
    }
  ],
  "filesModified": [
    "apps/control-room/src/v2/goals/live-goal-create.ts",
    "docs/agent-stack-runbook.md",
    "README.md",
    "tests/e2e/control-room/board-chain.spec.ts",
    "tests/e2e/control-room/daemon-ports.ts",
    "tests/e2e/control-room/activation-fresh-project.spec.ts",
    "apps/control-room/src/v2/goals/live-goal-create.test.ts",
    ".moe/tasks/task-741dad13e23d441a8385d0f14df5b5c1.json",
    ".moe/tasks/task-ac9d5e2d43334525bd990b1362652ce4.json",
    ".moe/tasks/task-3780109018bb49b2bf3a00b18952d7ff.json",
    ".moe/tasks/task-8e793ae3c63f41189bcc05f6d7c15605.json",
    ".moe/tasks/task-bb0a71316bd343d18453224973563ed0.json",
    ".moe/tasks/task-bb844c288fb346c0856778c738b9f073.json",
    ".moe/tasks/task-c0e00f0a55ad43bfa5f6b323779834cd.json",
    ".moe/tasks/task-db63dacc7cbe49cab9826e9b49c8669f.json"
  ],
  "unattributedPaths": [
    "apps/daemon/src/daemon-command-design.test.ts"
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "qa-f2906c26@2026-09-06T01:36:30Z",
    "at": "2026-09-06T01:57:57.394Z"
  },
  "priorHandoffs": [
    {
      "whatIsDone": "All 6 steps COMPLETED before the 34-hour park; the park reason (project.activate refusing ACTIVATION_POLICY_UNMEASURED on the seed:NONE lane) is gone since task-d342a2b1 landed at a1e12d2269d3 and this row auto-unblocked 23:11Z. worker-2739fee7 claimed 23:12Z and was reading context when the limit killed the CLI. Checkpoints up to 74ec649f5cb8; rescue ref refs/moe/rescue/task-ac9d5e2d43334525bd990b1362652ce4/20260905T232440Z parks the bytes of one failed landing (ref contention) and self-heals at the next session.",
      "whatRemains": "Governor resume note comment-364556ac51fc4e77877754b04270f805 on the row: remove the test.fixme naming task-d342a2b1 in the journey spec (step 3), re-run the journey against the real daemon on the seed:NONE lane, quote the count line, re-check DoD 1, 4 and 7, then complete_task. Its approval auto-unblocks task-bb844c28.",
      "pitfalls": "Do not complete_task cold just because 6/6 steps show COMPLETED. If activate now refuses with a different code, report_blocked with that code, never the old reason.",
      "releasedAt": "2026-09-06T00:35:09.087Z",
      "releasedBy": "governor-afbde490",
      "reason": "Governor crash-recovery release: worker-2739fee7's CLI exited 1 five times in a row from 23:19Z (account usage limit, fleet-wide, now reset); wrapper paused auto-resume and asked for release_task. Releasing so the wrapper relaunches and resumes via handoff."
    }
  ],
  "reviewStartedAt": "2026-09-06T01:36:17.952Z",
  "verification": {
    "command": "npx tsc -p tests/e2e/control-room/tsconfig.json && npx playwright test -c tests/e2e/control-room/playwright.config.ts activation-fresh-project.spec.ts",
    "exitCode": 0,
    "outputTail": "TSC_EXIT=0\n\nRunning 1 test using 1 worker\n\n  ok 1 [chromium] > tests\\e2e\\control-room\\activation-fresh-project.spec.ts:98:1 > a fresh project goes from empty store to a created goal, in the browser alone (7.7s)\n\n  1 passed (8.6s)\nPW_EXIT=0",
    "reportedAt": "2026-09-06T01:36:17.952Z"
  },
  "completionSummary": "THE BROWSER-ONLY FRESH START IS PROVEN, and every sentence in the repo that denied it is gone. Resumed after task-d342a2b1 (d103e849/a1e12d22) closed the ACTIVATION_POLICY_UNMEASURED block; its commit had ALREADY removed this journey's `test.fixme` (`grep -rn \"test.fixme\" tests/e2e/control-room/` -> 0).\n\nDoD 1. `tests/e2e/control-room/activation-fresh-project.spec.ts` drives a REAL daemon + REAL vite + REAL chromium on `seed: \"NONE\"`, asserting `lane.seedPid` is null first. Bare `lane.baseUrl`, no `?v1=1`, no `cr.liveboard.*` selector (row rail 3). Pair -> submit draft -> daemon REFUSES -> one click on `cr.activate.button` -> five chain steps -> SAME draft -> `Goal created: Browser-only fresh start`. `1 passed (8.6s)`, tsc exit 0.\n\nDoD 2. `grep -c \"cannot drive the pre-activation chain\" .../live-goal-create.ts` = 0. Replacement names the Activate card; prerequisite half still from `labelForMissing`. The test pins the NEW sentence with `toBe`.\n\nDoD 3. README \"What this is not\" fresh-project clause removed, stamp re-dated to 2026-09-05, positive claim added as its own bullet; runbook's \"no production writer\" sentence replaced while KEEPING the true \"**New Goal** remains disabled...\" sentence and the fail-closed half. Delivered across two commits, not one: 6d759782 carries the docs, a1e12d22 carries the spec + copy (the peer wrapper swept them). Disclosed, not hidden.\n\nDoD 4. Live drive: HEAD f4e1495b, store `%TEMP%\\moe-e2e-daemon-Wg8YgA\\store.sqlite` (200704 bytes, isolated by a before/after delta watcher — the lane deletes its scratch), trace `%TEMP%\\ac9d5e2d-livedrive-trace-w2739fee7.zip` sha256 979e030a…, 83 screenshot frames. comment-21619c70.\n\nDoD 5. demo-seed targeted: `Test Files 5 passed (5)` / `Tests 69 passed (69)`. No delivered path under `apps/daemon/src/orchestrator/` or `scripts/`.\n\nDoD 6. control-room `164 files / 2034 tests` green; `pnpm typecheck` exit 0; e2e lane `21 passed | 6 failed`; daemon `12 failed | 493 passed`; root lane (apps/** NOT covered)",
  "touchedFiles": [
    "tests/e2e/control-room/activation-fresh-project.spec.ts",
    "README.md",
    "apps/control-room/src/v2/goals/live-goal-create.test.ts",
    "apps/control-room/src/v2/goals/live-goal-create.ts",
    "docs/agent-stack-runbook.md"
  ],
  "completedAt": "2026-09-06T01:57:38.856Z",
  "reviewCompletedAt": "2026-09-06T01:57:38.856Z",
  "reviewSummary": "APPROVED at completion commit d0c69621 (HEAD; kind=completion, trailer + branch --contains verified). FULL EVIDENCE: comment-a0881913.\n\nRE-RAN MYSELF: e2e tsc exit 0; activation-fresh-project.spec.ts -> `1 passed (7.9s)`; `pnpm --filter @moe/control-room test` -> `Test Files 167 passed (167)` / `Tests 2057 passed (2057)` exit 0; FULL browser lane -> `22 passed | 5 failed (7.5m)`, this row's spec `ok 1` (better than the disclosed 21|6).\n\nMY OWN DRILL: mutated the daemon's measured HEAD sha at activation-receipts-measure.ts:78 -> journey RED at spec.ts:138 with the mutant in the DOM. Restored, proven byte-identical in a SEPARATE call by blob-hash equality. Journey is bound to a real daemon, not hermetic.\n\nDoD 1 MET: bare lane.baseUrl (no ?v1=1), zero cr.liveboard.*, seed:\"NONE\" with seedPid asserted null, no test.fixme, real PRD attachment so the kind is goal.create_with_source, refuse-then-accept on the SAME draft. Its disclosed deviation is TRUE: cordum-app.tsx:374-379 derives createDisabledReason ONLY from the connection descriptor.\nDoD 2 MET: grep 0 at HEAD; test pins the NEW sentence with toBe across 4 arms; no other source asserts the old copy.\nDoD 3 substance MET, not literally one commit: bytes span 6d759782/e4855265/a1e12d22/d0c69621, a wrapper sweep the worker disclosed (it misattributed the copy to a1e12d22; it is e4855265). All ancestors of HEAD.\nDoD 4 MET: comment-21619c70 - store path+bytes, sha, count line, trace sha256, 83 frames.\nDoD 5 MET: `pnpm seed` is a seed RUN with no count line, so that clause is unsatisfiable as written; substance proven by demo-seed 5/69, no delivered path under orchestrator/ or scripts/.\nDoD 6 MET: all 5 e2e reds foreign, attributed per FAILING FILE; none asserts this row's surface; failing-paths INTERSECT owned-paths = EMPTY.\nDoD 7 MET: four scenarios plus a fifth in comment-9cc1ff43 on owning row task-741dad13; absent from completionSummary only because that field is at its 2000-char cap."
}