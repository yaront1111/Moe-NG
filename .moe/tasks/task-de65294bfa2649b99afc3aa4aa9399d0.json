{
  "id": "task-de65294bfa2649b99afc3aa4aa9399d0",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "/runs/read serves the declared migration identifiers and the browser decoder preserves them",
  "description": "SPIDR child of task-122b55fd2c284174876bd1d5573bdcc5, filed by architect-fa3a3e17 on 2026-09-06. That row's own title names two subjects - \"declare migration identifiers through durable authority\" AND \"Runs reads\" - and its description says \"Scope is authoritative declaration and production read contract\". Measured, those are far apart: the authority half touches the scheduler's frozen key roster, its canonical serialization, its digest binding and every compile/seal/replay consumer (10+ daemon files reference `nodeAuthorityHash`/`NodeDefinition` before the read contract is reached). The two together are well past a workable plan. THE PARENT KEEPS THE AUTHORITY (its DoD 1, 2, 3); this row takes the READ CONTRACT (its DoD 4).\n\ndependsOn the parent, because a read cannot serve a member the authority has not yet declared.\n\nTHE GAP, MEASURED AT HEAD by architect-fa3a3e17:\n- `apps/daemon/src/http/runs-read-contract.ts:76-94` carries no migration identifiers.\n- `apps/control-room/src/live/live-runs.ts:58-72` likewise none.\nSo the declaration the parent lands is invisible to the browser until this row serves it.\n\nWHAT THIS ROW OWNS: the production `/runs/read` contract serving the declared identifiers for the CORRECT EXECUTION IDENTITY, and the browser's exact-key decoder preserving them.\n\nFOUR PROPERTIES THE PARENT'S DoD 4 NAMES, all of them read-side and none of them obvious:\n1. THE CORRECT EXECUTION IDENTITY. A declaration belongs to one node's execution, and the read must not attribute it to another.\n2. NO SHARED-NODE-KEY CROSS-GOAL BLEED. Node keys are shared across goals; a read keyed on nodeKey alone will serve one goal's declaration on another goal's run. This is the defect most likely to pass a single-goal test.\n3. TWO DECLARED IDENTIFIERS BOTH SURVIVE - a read that keeps only the first passes every single-identifier fixture.\n4. MALFORMED WIRE FAILS CLOSED, with a stable code and layer.\nAND: NO RECEIPT OWNERSHIP INFERRED BY MATCHING FILENAME OR SHA (parent rail 1). If the read cannot establish ownership from durable identity, it serves nothing rather than guessing.\n\nNOT IN SCOPE: the declaration itself, the key roster, canonical serialization, the digest binding, schema versioning and compatibility - all the parent's. No UI wording or rendering: task-78eedd8ebc2b43c581ac21ea28648a9a owns that and renders via board-lanes.tsx (goal-nodes.tsx is only a wrapper).</description>",
  "definitionOfDone": [
    "Production `/runs/read` serves the declared migration identifiers for the CORRECT EXECUTION IDENTITY, asserted against a declaration written through the parent's real authority path - not a hand-built frame and not a fixture that mints the member directly.",
    "NO SHARED-NODE-KEY CROSS-GOAL BLEED: two goals whose runs share a nodeKey, only one of which declares identifiers, are asserted separately - the declaring goal serves them and the other serves none. A read keyed on nodeKey alone passes a single-goal test and fails this one, which is why it is its own clause.",
    "TWO DECLARED IDENTIFIERS BOTH SURVIVE the read and the browser decoder, asserted by value and by count. A read or decoder that keeps only the first passes every single-identifier fixture.",
    "THE BROWSER DECODER IS EXACT-KEY AND PRESERVES THE MEMBER: `apps/control-room/src/live/live-runs.ts` decodes it, an unknown key and a missing key each refuse with the specific code AND the layer that refused, and a MALFORMED wire fails closed rather than dropping the member silently.",
    "A RUN WITH NO DECLARATION IS DISTINCT FROM ONE DECLARING NONE - carry the parent's UNKNOWN-vs-explicit-empty distinction through the wire and the decoder rather than collapsing both to an empty array. Assert both.",
    "NO OWNERSHIP INFERRED FROM A FILENAME OR A SHA (parent rail 1). If durable identity does not establish which execution a declaration belongs to, serve nothing and refuse - assert that path rather than falling back to a match.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`. Root `pnpm test` covers no apps/** file, so say that rather than quoting a bare root count line."
  ],
  "taskRails": [
    "READ CONTRACT ONLY. The declaration, the key roster, canonical serialization, the digest binding and schema compatibility are the parent's (task-122b55fd). Touch none of packages/scheduler.",
    "NO OWNERSHIP BY FILENAME OR SHA MATCH. Establish it from durable identity or serve nothing.",
    "NO CROSS-GOAL BLEED. Node keys are shared across goals; key the read on the execution identity, not on nodeKey alone.",
    "NO UI. Wording and rendering belong to task-78eedd8e, which renders via board-lanes.tsx.",
    "EXACT-KEY DECODING with a stable code AND layer; a malformed wire fails closed rather than dropping the member."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE THE TWO PREREQUISITES BEFORE ANY EDIT. This row's dependsOn names task-122b55fd (authority) and task-75bff0c9 (the author that carries identifiers from planning source into the compiled node); at plan time BOTH were unmet. dependsOn gates the WORKING claim, so if you are reading this the runtime already believes they are DONE — verify it on disk anyway, because DoD 1 stands or falls on it.\n\nRun and record:\n- `git grep -rn \"declaredMigrations\" -- apps packages` — at plan time this returned hits ONLY under `packages/scheduler/src/node-authority/` and ZERO under `apps/`. You need at least one hit in the daemon's compile path (`apps/daemon/src/planning/v2-compiler/**`), which is what proves task-75bff0c9 landed and that a compiled node can actually carry the member.\n- `grep -n \"declaredMigrations\" packages/scheduler/src/node-authority/node-authority-contract.ts` — expect `:193 readonly declaredMigrations?: readonly string[];`. That exact spelling is the contract; do not rename it.\n- Confirm the read path still reaches it: `grep -n \"nodeAuthority\" packages/scheduler/src/graph-content-fields.ts` (expect `:82 readonly nodeAuthority: NodeAuthoritySection`) and `sed -n '78,105p' apps/daemon/src/http/runs-read.ts` (the `SealedNode` interface and `sealedNodesOf`, which is where the member is currently dropped).\n\nIF `declaredMigrations` HAS NO `apps/` HIT: there is no real authority path to assert against. `moe.report_blocked` with blockedOnTaskIds [\"task-122b55fd2c284174876bd1d5573bdcc5\", \"task-75bff0c9efc24daaad81ac81a373cd90\"], quoting the grep. Do NOT hand-build an authority body, do NOT mint the member directly in a fixture, and do NOT narrow DoD 1 — all three are explicitly forbidden.\n\nAlso `git status --porcelain` the paths below and note any peer dirt without touching it. No files change in this step.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "MEASURED AT HEAD (branch moe/work-2026-09-04), no files changed.\n\nPREREQUISITES BOTH LANDED — no block owed.\n- `git grep -rn declaredMigrations -- apps` => 20 hits (plan-time expectation was ZERO). Compile path present: `apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts:19` (`DECLARED_MIGRATIONS_KEY`) and `:141` (conditional spread into the authority draft). `-- packages` => 86 hits.\n- `grep -n declaredMigrations packages/scheduler/src/node-authority/node-authority-contract.ts` => `:193 readonly declaredMigrations?: readonly string[];` plus `:100` inside `NODE_DEFINITION_KEYS`. Exact spelling confirmed; not renamed.\n- Read path reachable: `packages/scheduler/src/graph-content-fields.ts:82 readonly nodeAuthority: NodeAuthoritySection`. `apps/daemon/src/http/runs-read.ts` — `SealedNode` at :78-85 (six fields, drops the rest), `sealedNodesOf` :87-105 iterates `graph.content.nodeAuthority.definitions` INSIDE the per-graph loop and derives `nodeRef` at :100 via `compiledExecutionRef(projectId, graph, definition.nodeKey)`.\n\nPLAN-TIME LINE ANCHORS DRIFTED (peer growth), corrected for later steps:\n- `runs-read-contract.ts` is 158 lines; `RunNodeView` is at :75-92, not :76-100.\n- `runs-read.ts` is 308 lines; emitted row is :220-256, not :221-255.\n- `live-runs.ts` is 383 lines (unchanged, 383/400).\n\nTWO PLAN CLAIMS MEASURED FALSE, both reduce scope:\n1. `apps/daemon/src/orchestrator/compiled-node-identity.test.ts` does NOT construct a `RunNodeView` literal. Its two `sharedKey` occurrences (:133, :163) are `toMatchObject({ sharedKey: ... })` assertions, which stay green under an added required member. No backfill owed there; I will re-confirm with `pnpm typecheck` rather than assume.\n2. The daemon fixture that runs-read.test.ts uses (`compiledPlanAuthority` -> `createCompiledPolicyAuthorityBody`) CANNOT carry a declaration: `apps/daemon/src/planning/compiled-policy-authority-body.ts:98-113` is the writer row's deliberate VERDICT that this daemon-synthesized draft legitimately declares NONE (absent, not `[]`), because `[]` would mint a schema-3 body and move `graphContentHash` for every shipped journey. So DoD 1's \"real authority path\" fixture must come from elsewhere.\n\nROUTE CHOSEN FOR DoD 1 (recorded now so step 4 does not re-derive it): `apps/daemon/src/planning/graph-query-test-fixtures.ts` already composes graphs from the PRODUCTION producers — `nodeDefinitionFor` (:196-234) builds a DRAFT and hands it to `createNodeDefinition` (@moe/scheduler, `node-authority-codec.ts:134`), `authoritySectionFor` (:249-265) then calls `deriveNodeAuthoritySet`. Putting `declaredMigrations` in that DRAFT is exactly what production does (`scheduler-node-planning-authority.ts:141` spreads it into `draft`), and the member on the resulting definition is produced by the codec's own `project()`, never assigned by the fixture — so it is a declaration WRITTEN THROUGH the parent's real authority path, not a hand-built frame and not a fixture minting the member directly.\n\nConfirmed the codec mints by CONTENT, which is what makes the arm meaningful: `node-authority-codec.ts:124-127` — `drafted.draft.declaredMigrations === undefined ? NODE_AUTHORITY_UNDECLARED_SCHEMA_VERSION (2) : NODE_AUTHORITY_SCHEMA_VERSION (3)`.\n\nWhy a splice is NOT available as a shortcut: `compiledExecutionRef` calls `encodeGraphContent(graph.content)`, which RE-DERIVES the authority set and refuses `GRAPH_CONTENT_AUTHORITY_DISAGREEMENT` on a stated set that is not the derived one. A definition spliced into `definitions` without re-deriving `authorities` makes `nodeRef` THROW.\n\nCROSS-GOAL (DoD 2) MECHANICS MEASURED: goal identity is `goal-${commandId}` (`affordance-read.ts:297`, `bootstrap-test-fixtures.ts:56`), so a second bound goal is a second `goal.create` + `goal.create_with_source` under a different command id. `catalogBoundGoals` (`document-coverage-goals.ts:36-62`) lists every GoalCreated event with a binding, sorted by goalId — so two bound goals both produce rows. The existing shared-key arm (runs-read.test.ts:321-341) only ever binds ONE goal and reads the other graph for the `shared` set, which is precisely why it cannot see the bleed this row must exclude.\n\nPEER DIRT: 52 dirty paths in the shared checkout. NONE of my six planned paths is among them (`git status --porcelain` over exactly those paths returned empty). Not touched, not a stop condition.",
      "startedAt": "2026-09-07T17:15:39.132Z",
      "completedAt": "2026-09-07T17:19:43.575Z"
    },
    {
      "stepId": "step-2",
      "description": "DAEMON CONTRACT: add the member to `RunNodeView` in `apps/daemon/src/http/runs-read-contract.ts` (the interface at :76-100, alongside the existing `criterionIds` / `dependsOn` / `nodeRef` members).\n\nSHAPE, and it is the load-bearing decision of this row:\n\n  /** Authored order, never sorted or deduped. `null` means the node's authority\n   * body declares nothing at all (UNKNOWN); `[]` means the author declared none. */\n  readonly declaredMigrations: readonly string[] | null;\n\nREQUIRED, not optional, and ALWAYS EMITTED. The browser's `exactDataRecord` (live-runs.ts:125-145) rejects any object whose own-key count differs from its roster, so a row that omitted the key when the declaration is UNKNOWN would fail to decode ENTIRELY — the whole Runs read would go dark, not just this member. Carrying `null` on the wire is what lets DoD 5's UNKNOWN-vs-explicit-empty distinction survive a strictly-exact decoder.\n\nMirror the member into the local `SealedNode` interface at `runs-read.ts:78-85` in the same edit so the two shapes stay adjacent and reviewable.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/http/runs-read-contract.ts",
        "apps/daemon/src/http/runs-read.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/http/runs-read-contract.ts",
        "apps/daemon/src/http/runs-read.ts"
      ],
      "note": "Member added to both shapes, REQUIRED and nullable, in alphabetical position beside `criterionIds`.\n\n- `apps/daemon/src/http/runs-read-contract.ts` `RunNodeView`: `readonly declaredMigrations: readonly string[] | null;` with the doc comment the step names verbatim (authored order, `null` = UNKNOWN, `[]` = declared none).\n- `apps/daemon/src/http/runs-read.ts` local `SealedNode`: same shape, one-line comment.\n\nNot optional, and always emitted, for the reason the plan measured: `live-runs.ts:125-145 exactDataRecord` fails when `keys.length !== expectedKeys.length`, so omitting the key on UNKNOWN would fail EVERY node row, not just this member.\n\nNo population yet, so both files are intentionally red at this point (`sealedNodesOf` does not supply the member and the emitted row does not carry it). Step 3 closes both. Contract file now 160 lines, runs-read.ts 310 — both far under cap.",
      "startedAt": "2026-09-07T17:19:46.110Z",
      "completedAt": "2026-09-07T17:20:02.470Z"
    },
    {
      "stepId": "step-3",
      "description": "DAEMON READ: populate the member in `sealedNodesOf` (`apps/daemon/src/http/runs-read.ts:87-105`) and carry it onto the emitted row (:221-255).\n\nSource it from the `definition` the loop is ALREADY holding:\n`declaredMigrations: definition.declaredMigrations === undefined ? null : Object.freeze([...definition.declaredMigrations])`\n\nWHY THIS EXACT SHAPE, three reasons, all of them DoD clauses:\n- `undefined -> null` is the UNKNOWN-vs-declared-none distinction (DoD 5). Do not use `?? []` — that collapses both and the parent's contract says `[]` is defaulted nowhere.\n- The loop at :92 iterates `graph.content.nodeAuthority.definitions` INSIDE the per-graph body, and :100 already derives `nodeRef` there via `compiledExecutionRef(projectId, graph, definition.nodeKey)`. Reading the member off that same `definition` gives the CORRECT EXECUTION IDENTITY for free (DoD 1) and makes cross-goal bleed structurally impossible (DoD 2). **Do not build a nodeKey-keyed map** — that is the exact defect DoD 2 names, and it passes every single-goal test.\n- Copy-and-freeze rather than aliasing the authority array, so a read cannot mutate durable content.\n\nPRESERVE AUTHORED ORDER: no `.sort()`, no `Set`, no dedup. The parent made order content and bound it into the digest.\n\nOwnership comes from `nodeRef` alone. Never infer it from a filename or a SHA (task rail 2 / DoD 6): if the definition is not in this graph's `definitions`, the node serves nothing.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/http/runs-read.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/http/runs-read.ts"
      ],
      "note": "Populated and carried, exactly as the step specifies.\n\n`sealedNodesOf` (now runs-read.ts:96-102):\n```\ndeclaredMigrations: definition.declaredMigrations === undefined\n  ? null : Object.freeze([...definition.declaredMigrations]),\n```\nEmitted row (`goalView`'s node map): `declaredMigrations: node.declaredMigrations,` beside `criterionIds`.\n\n- `undefined -> null` keeps DoD 5's UNKNOWN-vs-declared-none distinction. No `?? []`.\n- Sourced off the SAME per-graph `definition` that the loop already turns into `nodeRef` via `compiledExecutionRef(projectId, graph, definition.nodeKey)`. NO nodeKey-keyed map was built anywhere, so cross-goal bleed is structurally impossible rather than merely tested against.\n- Copy-and-freeze, not an alias, so the read cannot hand out (or mutate) durable authority content.\n- No `.sort()`, no `Set`, no dedup, no default: authored order is content and is bound into the parent's digest.\n- Ownership comes from `nodeRef`; a definition not in this graph's `definitions` is never reached, so nothing is inferred from a filename or a sha.\n\nType reachability confirmed on disk rather than assumed: `graph-content-fields.ts:65-68 NodeAuthoritySection { definitions: readonly NodeDefinition[] }` and `node-authority-contract.ts:193 readonly declaredMigrations?: readonly string[]`.\n\nVERIFIED: `npx tsc --project tsconfig.json --noEmit` in apps/daemon => EXIT 0. That also settles step 1's second measurement — `compiled-node-identity.test.ts` needs no backfill (its `sharedKey` sites are `toMatchObject`, not node literals), and the daemon tsconfig covers `src` including tests. runs-read.ts now 315 lines, under cap.",
      "startedAt": "2026-09-07T17:20:04.700Z",
      "completedAt": "2026-09-07T17:20:47.812Z"
    },
    {
      "stepId": "step-4",
      "description": "DAEMON TESTS in `apps/daemon/src/http/runs-read.test.ts`, driven through the real authority path measured in step 1 — a compiled graph whose node authority genuinely carries the declaration, never a hand-built body. Four arms, one per property:\n\n(a) CORRECT EXECUTION IDENTITY (DoD 1): a goal whose node declares two identifiers serves them on that node's `nodeRef` row.\n(b) NO SHARED-NODE-KEY CROSS-GOAL BLEED (DoD 2) — the arm that matters most: build TWO goals whose runs share the SAME `nodeKey`, where only one declares. Assert separately that the declaring goal's row serves the identifiers AND that the other goal's row serves `null`. An implementation keyed on `nodeKey` alone passes (a) and fails this; that is why it is its own arm.\n(c) TWO IDENTIFIERS BOTH SURVIVE, IN AUTHORED ORDER (DoD 3): assert `toEqual([\"<first>\", \"<second>\"])` — by value and in order, not `toHaveLength(2)`, which a reversing or deduping implementation would pass.\n(d) UNKNOWN vs EXPLICIT-EMPTY (DoD 5): a node whose authority declares nothing serves `null`; a node that declared none serves `[]`. Assert both in the same test so the two cannot be collapsed.\n\nAlso backfill the one node literal in `apps/daemon/src/orchestrator/compiled-node-identity.test.ts` (it states `sharedKey:`, so it constructs a full node and will not typecheck without the new member).\n\nDRILL EVERY ARM: mutate the production surface and confirm red — `?? []` instead of `null` must red (d); keying the projection on `nodeKey` must red (b); a `.sort()` must red (c). Restore and prove restoration with `git diff` (these paths are tracked).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/http/runs-read.test.ts",
        "apps/daemon/src/orchestrator/compiled-node-identity.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/http/runs-read.test.ts",
        "apps/daemon/src/planning/graph-query-test-fixtures.ts"
      ],
      "note": "FOUR ARMS ADDED, all driven through the parent's REAL authority path. Targeted leg: `Test Files 1 passed (1)` / `Tests 24 passed (24)` EXIT 0 (was 20 before this step).\n\nWHY THE FIXTURE ROUTE CHANGED FROM THE PLAN'S ASSUMPTION. `compiledPlanAuthority` — the builder runs-read.test.ts already uses — CANNOT carry a declaration: `compiled-policy-authority-body.ts:98-113` is the writer row's deliberate ruling that this daemon-synthesized draft declares NONE (absent, not `[]`), because `[]` would mint a schema-3 body and move `graphContentHash` for every shipped journey. I did not touch that file. Instead I reached the SAME production seam through `apps/daemon/src/planning/graph-query-test-fixtures.ts`, whose `nodeDefinitionFor` builds a DRAFT and hands it to `createNodeDefinition` (@moe/scheduler). The identifiers ride the draft exactly as production's `scheduler-node-planning-authority.ts:141` spreads them; the member on the resulting definition is the codec's own `project()` output, never assigned by the fixture. That is a declaration WRITTEN THROUGH the authority path, not a hand-built frame and not a fixture minting the member.\n\nFIXTURE CHANGES (all additive, defaults preserve existing behaviour byte-for-byte):\n- new exported type `MigrationDeclarations = ReadonlyMap<string, readonly string[]>`\n- `nodeDefinitionFor(..., declared?)` — CONDITIONAL SPREAD into the draft, never an assigned `undefined`: an own key holding `undefined` is a STATED member to the draft reader and would mint schema 3 for a node that declared nothing.\n- `authoritySectionFor(snapshot, declared?)` passes `declared?.get(nodeKey)`.\n- new `revisionContentFor(author, snapshot, declared?)` returning the unencoded `GraphRevisionContent`; `encoded()` now calls it (same values, `completionNode` read off `snapshot.completionNodeKey`, which IS \"dev-c\").\nA splice was NOT available as a shortcut: `compiledExecutionRef` calls `encodeGraphContent`, which re-derives the authority set and refuses `GRAPH_CONTENT_AUTHORITY_DISAGREEMENT`, so a definition inserted without re-deriving `authorities` makes `nodeRef` throw.\n\nTHE ARMS:\n(a) DoD 1/3 — declares [\"migration-b\",\"migration-a\"], asserted `toEqual` BY VALUE AND IN ORDER (never `toHaveLength`), plus `nodeRef === compiledExecutionRef(PROJECT_ID, graph, \"dev-a\")` so the declaration is pinned to the EXECUTION identity, and `not.toBe` on the durable array so the read is proven to hand out a copy.\n(b) DoD 2 — TWO BOUND GOALS. Second goal bound through the production `goal.create_with_source` under command id \"2\" (`goal-identity.ts:27` derives `goal-${commandId}`), so both goals produce rows in one `readRuns({})`. Both graphs carry dev-a/dev-b/dev-c; only goal-1 declares. Asserts goal-1's dev-a serves the identifiers AND goal-2's dev-a serves `null`, plus that goal-2 really surfaced all three keys so the null is not vacuous.\n(c) DoD 5 — one graph, three nodes: dev-a absent from the map (UNKNOWN), dev-b explicit `[]`, dev-c one identifier. `toEqual([null, [], [\"migration-c\"]])` in ONE assertion so they cannot be collapsed, plus `not.toEqual` between the two, plus the layer below: `schemaVersion` 2 vs 3 on those same definitions, which is the codec's own content-minted distinction.\n(d) DoD 6 — a declaration for a nodeKey the graph's authority does not define is served NOWHERE. Ownership is the per-graph definition list; nothing is matched by name, filename or sha.\n\nDRILLS — PROCESS-LOCAL, ZERO BYTES WRITTEN TO THE CHECKOUT. `full-suite-gate` is held by worker-4d8571e7 (task-620fb461) since 17:12:31Z, so an on-disk mutant would have corrupted their full legs. Method announced in #general (msg-556d81e1) before drilling: out-of-tree vitest config in %TEMP% whose plugin rewrites the module in memory, throwing on a missing anchor and throwing in buildEnd if never applied, per-mode cacheDir, package flags (--maxWorkers=4 --testTimeout=30000) restated.\n\n- POSITIVE CONTROL (harness armed, unmutated): `Test Files 1 passed (1)` / `Tests 24 passed (24)` EXIT 0. So a drill red cannot be the harness's fault.\n- D1 `?? []` instead of `null`: `3 failed | 21 passed (24)` EXIT 1 — `expected [ [], [], [] ] to deeply equal [ null, null, null ]`. Reds the UNKNOWN-vs-NONE arm.\n- D2 key the projection on nodeKey across graphs: `1 failed | 23 passed (24)` EXIT 1 — `expected [ 'migration-b', 'migration-a' ] to be null` at the cross-goal assertion. It reds THAT ARM AND ONLY THAT ARM, which is the whole point of DoD 2 being its own clause.\n- D3 `.sort()`: `2 failed | 22 passed (24)` EXIT 1 — `expected [ 'migration-a', 'migration-b' ] to deeply equal [ 'migration-b', 'migration-a' ]`. Order is genuinely pinned.\n\nRESTORE PROOF, stronger than mutate-and-restore because nothing was ever mutated on disk: `git diff --stat -- apps/daemon/src/http/runs-read.ts` => `1 file changed, 9 insertions(+)`, ZERO deletions (every drill above requires a deletion), and the full `git diff` shows only my two comments and three added lines. `grep` for the three",
      "startedAt": "2026-09-07T17:20:49.921Z",
      "completedAt": "2026-09-07T17:25:36.869Z"
    },
    {
      "stepId": "step-5",
      "description": "BROWSER DECODER in `apps/control-room/src/live/live-runs.ts`. Three small edits, ~6 lines total — this file is 383 physical lines against the 400 hard cap, so keep it tight and do NOT add a paragraph comment.\n\n1. `RunNodeView` (:58-72): add `readonly declaredMigrations: readonly string[] | null;` — same shape as the daemon contract.\n2. The exact-key roster in `nodeOf` (:237-240): add `\"declaredMigrations\"` to the array, in alphabetical position beside `\"criterionIds\"`.\n3. The decode in `nodeOf`: branch EXPLICITLY, and in this order —\n\n     const declaredMigrations = record.declaredMigrations === null\n       ? null : stringList(record.declaredMigrations);\n     if (declaredMigrations === undefined) return null;   // shape guard\n\n   then require it alongside the existing `if (criterionIds === null || dependsOn === null || review === null) return null;` guard, adapted so that `null` stays a LEGAL value for this member while a malformed one refuses.\n\nWHY THE EXPLICIT null BRANCH (DoD 4's fail-closed clause, and the likeliest silent defect in this row): `stringList` (:176) returns `null` for \"not an array\", and the wire's UNKNOWN is ALSO `null`. Funnelling both through one helper makes a malformed value — a number, a nested array, a string — indistinguishable from UNKNOWN, and the member is silently dropped instead of refusing. Branch on the literal `null` first, then demand a real string array.\n\nA missing or unknown key already refuses through `exactDataRecord`'s length-and-membership check with `RUNS_RESPONSE_INVALID` @ `CONTROL_ROOM_LIVE_RUNS` (:7-8, :122); do not add a second code.\n\nCONTINGENCY: if the edit would push the file past 400 lines, extract `nodeOf` into a sibling module and declare that new path — do not shorten an existing comment to squeak under.\n\nAdd no `@moe/scheduler` import: this file is a standalone wire decoder (measured: control-room imports the package nowhere) and must stay one.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/live/live-runs.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/live/live-runs.ts"
      ],
      "note": "Decoder is exact-key and fail-closed. `apps/control-room/src/live/live-runs.ts` is now 390 physical lines (was 383) against the 400 cap — no extraction needed, no comment shortened, no `@moe/scheduler` import added.\n\nFOUR EDITS (11 insertions, 4 deletions; the deletions are two re-wrapped lines, not removed logic):\n1. `RunNodeView`: `readonly declaredMigrations: readonly string[] | null;` with a ONE-line doc comment, alphabetical beside `criterionIds`.\n2. `nodeOf`'s exact-key roster: `\"declaredMigrations\"` added in alphabetical position; the array re-wrapped to stay inside the line budget. 13 keys now, matching what the daemon always emits.\n3. The decode, branching on the literal `null` FIRST:\n```\nconst declared = record.declaredMigrations;\nconst declaredMigrations = declared === null ? null : stringList(declared);\n```\n4. The guard, adapted so `null` stays a LEGAL value while a malformed one refuses:\n```\nif (criterionIds === null || (declared !== null && declaredMigrations === null)\n  || dependsOn === null || review === null) return null;\n```\n\nWHY THE GUARD IS WRITTEN THAT WAY rather than the step's `=== undefined` sketch. `stringList` (:176) returns `readonly string[] | null` — it can never produce `undefined`, so a `declaredMigrations === undefined` test would be dead code and every malformed value would decode as UNKNOWN. The load-bearing clause is `declared !== null && declaredMigrations === null`: it reads the RAW wire value, so \"the wire said null\" (UNKNOWN, legal) and \"stringList could not read it\" (malformed, refuse) stay distinguishable. Same intent as the step, correct against the helper's actual signature. Drilled in step 6, where the arms that pin it live.\n\nNo second refusal code minted: a missing or unknown key still refuses through `exactDataRecord`'s length-and-membership check, and `nodeOf` returning null propagates to `invalidResponse()` => `RUNS_RESPONSE_INVALID` @ `CONTROL_ROOM_LIVE_RUNS`.\n\nTYPECHECK RUN, and it found exactly the blast radius the plan predicted — `npx tsc --project tsconfig.json --noEmit` in apps/control-room EXIT 1 with 12 errors across the 10 named fixture files (live-needs-you.test.tsx x2, needs-you-model.test.ts, preview-rejection-invariants.test.tsx, replan-successor-port.test.ts, board-columns.test.ts, board-screen.test.tsx, goal-nodes.test.tsx, goal-publish.test.tsx, v2/runs/live-runs.test.tsx, v2/runs/runs-screen.test.tsx). That red is EXPECTED and is closed by step 6's backfill; it is my own diff's, disclosed here rather than hidden. Note `src/live/live-runs.test.ts` is NOT among them — it builds nodes as wire JSON, not typed literals, so its arms are additive.",
      "startedAt": "2026-09-07T17:25:40.694Z",
      "completedAt": "2026-09-07T17:27:04.594Z"
    },
    {
      "stepId": "step-6",
      "description": "BROWSER TESTS in `apps/control-room/src/live/live-runs.test.ts`, plus the fixture backfill the required member forces.\n\nARMS (DoD 3, 4, 5):\n(a) Two identifiers survive the decoder by value AND in authored order: `toEqual([\"<first>\", \"<second>\"])`.\n(b) `declaredMigrations: null` on the wire decodes to `null` — and assert THE WHOLE NODE still decodes, not merely the member. This is the guard against the omitted-key trap: if UNKNOWN were ever encoded by omission, every node row would fail `exactDataRecord` and the Runs read would go dark.\n(c) `[]` decodes to `[]`, distinct from (b). Assert both in one test so they cannot be collapsed.\n(d) MALFORMED FAILS CLOSED with the specific code AND layer: `declaredMigrations: 7`, `[\"a\", 3]`, and `\"a\"` each refuse `RUNS_RESPONSE_INVALID` @ `CONTROL_ROOM_LIVE_RUNS` — never a decoded node with the member dropped.\n(e) BIDIRECTIONAL ROSTER (global rail): the new key is accepted AND an extra unknown key on the node object still refuses. A one-directional arm goes vacuous the moment the roster changes again.\n\nFIXTURE BACKFILL — one line each, adding `declaredMigrations: null` to the single base node literal in each file (all sampled and confirmed to be one base object with a `...overrides` / `...extra` spread): live-needs-you.test.tsx, needs-you-model.test.ts, preview-rejection-invariants.test.tsx, replan-successor-port.test.ts, board-columns.test.ts, board-screen.test.tsx, goal-nodes.test.tsx, goal-publish.test.tsx, v2/runs/live-runs.test.tsx, v2/runs/runs-screen.test.tsx (this last one has 4 occurrences).\n\nThese are fixture edits only. Add no rendered string and change no wording — board-lanes.tsx rendering belongs to task-78eedd8e (task rail 4). If a file needs more than its node literal touched, STOP and report rather than widening.\n\nDrill (d) and (b) by mutating the decoder and confirming red.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/live/live-runs.test.ts",
        "apps/control-room/src/v2/approvals/live-needs-you.test.tsx",
        "apps/control-room/src/v2/approvals/needs-you-model.test.ts",
        "apps/control-room/src/v2/approvals/preview-rejection-invariants.test.tsx",
        "apps/control-room/src/v2/approvals/replan-successor-port.test.ts",
        "apps/control-room/src/v2/board/board-columns.test.ts",
        "apps/control-room/src/v2/board/board-screen.test.tsx",
        "apps/control-room/src/v2/goals/goal-nodes.test.tsx",
        "apps/control-room/src/v2/goals/goal-publish.test.tsx",
        "apps/control-room/src/v2/runs/live-runs.test.tsx",
        "apps/control-room/src/v2/runs/runs-screen.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/live/live-runs.test.ts",
        "apps/control-room/src/v2/approvals/live-needs-you.test.tsx",
        "apps/control-room/src/v2/approvals/needs-you-model.test.ts",
        "apps/control-room/src/v2/approvals/preview-rejection-invariants.test.tsx",
        "apps/control-room/src/v2/approvals/replan-successor-port.test.ts",
        "apps/control-room/src/v2/board/board-columns.test.ts",
        "apps/control-room/src/v2/board/board-screen.test.tsx",
        "apps/control-room/src/v2/goals/goal-nodes.test.tsx",
        "apps/control-room/src/v2/goals/goal-publish.test.tsx",
        "apps/control-room/src/v2/runs/live-runs.test.tsx",
        "apps/control-room/src/v2/runs/runs-screen.test.tsx"
      ],
      "note": "Decoder arms added and the ten fixtures backfilled. `src/live/live-runs.test.ts` => `Test Files 1 passed (1)` / `Tests 56 passed (56)` EXIT 0 (was 45). `npx tsc --project tsconfig.json --noEmit` in apps/control-room => EXIT 0, closing the 12 errors step 5 disclosed.\n\nFOUR ARMS in `apps/control-room/src/live/live-runs.test.ts` (new describe, plus one line on the base `NODE` wire literal so it carries the key production always emits):\n(a) DoD 3 — two identifiers `toEqual([\"migration-b\",\"migration-a\"])` BY VALUE AND IN ORDER, plus an explicit length 2. A decoder that kept only the first, or sorted, fails.\n(b) DoD 5 — UNKNOWN vs declared-none in ONE test: `[null, []]`, and then the WHOLE NODE via `toStrictEqual({...NODE, declaredMigrations: null})` and `...: []`. That whole-node assertion is the omitted-key guard: if UNKNOWN were ever encoded by omission, every node row would fail `exactDataRecord` and the Runs read would go dark, not just this field.\n(c) DoD 4 — EIGHT malformed wire values (`7`, `[\"a\",3]`, `\"a\"`, `[[]]`, `{}`, `[null]`, `true`, `[\"a\",undefined]`) each `toStrictEqual({code:\"RUNS_RESPONSE_INVALID\", layer:\"CONTROL_ROOM_LIVE_RUNS\", status:\"ERROR\"})` — the CODE AND THE LAYER, never a decoded node with the member dropped.\n(d) BIDIRECTIONAL ROSTER (global rail) — FORWARD: the new key is accepted and the decoded node's own-key SET equals the wire's (`Object.keys(...).sort()` both sides, on top of `toStrictEqual`). REVERSE: an extra unknown key still refuses, AND an ABSENT key refuses. Both directions, so the arm cannot go vacuous when the roster next changes.\n\nFIXTURE BACKFILL — exactly one line per file, `declaredMigrations: null` in alphabetical position, 11 sites across 10 files (live-needs-you.test.tsx has 2 base literals; the plan predicted 4 in runs-screen.test.tsx but MEASURED it is 1 base literal at :15 with `...extra` spreads). No rendered string added, no wording changed, no `.tsx` render assertion touched — board-lanes.tsx rendering stays task-78eedd8e's.\n\nDRILLS — PROCESS-LOCAL AGAIN, ZERO BYTES WRITTEN. `full-suite-gate` still held by worker-4d8571e7.\n- POSITIVE CONTROL (harness armed, unmutated): `Test Files 1 passed (1)` / `Tests 56 passed (56)` EXIT 0.\n- D4 drop the fail-closed clause `(declared !== null && declaredMigrations === null)`: `8 failed | 48 passed (56)` — ALL EIGHT malformed cases red, `expected { goals: [...] } to strictly equal { code: 'RUNS_RESPONSE_INVALID', ... }`. This is the exact silent defect the plan named, and it is now pinned.\n- D5 drop `\"declaredMigrations\"` from the exact-key roster: `6 failed` — the three new arms plus THREE PRE-EXISTING arms (`maps a full RUNS frame with every node fact intact`, tested-tree, landing). So the roster entry is load-bearing for the whole read, not just for this member.\n- D6 decode the member but omit it from the returned node: `7 failed` — `expected undefined to deeply equal [ 'migration-b', 'migration-a' ]` and `expected [ undefined, undefined ] to deeply equal [ null, [] ]`. A silently-dropping decoder cannot pass.\n\nRESTORE PROOF: nothing was ever mutated on disk. `git diff --stat -- apps/control-room/src/live/live-runs.ts` => `1 file changed, 11 insertions(+), 4 deletions(-)` (my step-5 edit, unchanged), and all three drill anchors are still present verbatim at :239, :253 and :277.",
      "startedAt": "2026-09-07T17:27:06.718Z",
      "completedAt": "2026-09-07T17:29:41.000Z"
    },
    {
      "stepId": "step-7",
      "description": "GATES (DoD 7), ADVERSARIAL REVIEW (Skill `adversarial-self-review`), AND PATH DECLARATION.\n\nAcquire `full-suite-gate` before the full legs (one full suite at a time on this host; announce GATE START/END with the UTC window) and release it on EVERY exit path including failure. Run each leg standalone and foreground — never piped, never `;`-chained, because a pipe reports tail's status and a chain reports only the last command's:\n1. `pnpm --filter @moe/daemon test` — quote `Test Files N passed (N)`.\n2. `pnpm --filter @moe/control-room test` — quote its own count line. This leg is MANDATORY: the root vitest include has no `apps/**`, so nothing else in this plan covers the decoder or the ten fixture files.\n3. `pnpm typecheck` — EXIT 0, with a planted-error control in a NEW file to prove the leg is not short-circuiting; delete the planted file and show the clean re-run. This leg is what catches a missed fixture backfill.\nDo NOT quote a bare root `pnpm test` count line as evidence for anything in this row — say plainly that the root lane covers no `apps/**` file.\n\nCheck the per-file line cap on the two production files you grew: `wc -l apps/control-room/src/live/live-runs.ts apps/daemon/src/http/runs-read.ts`. live-runs.ts was 383/400 at plan time.\n\nAdversarial pass, aimed at this row's specific holes: if two goals share a nodeKey AND both declare, does each row still serve its own? Does a node whose authority body is schema-2 (predating the declaration) serve `null` rather than throwing? Can a malformed `declaredMigrations` ever produce a decoded node with the member quietly missing? Does anything sort, dedup or default the list on either side? Did any arm assert only a count where order is content?\n\nDeclare EXACTLY the files changed via complete_step.modifiedFiles / moe.declare_files. Touch no `packages/scheduler` file (task rail 1) and no foreign dirty path.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/live/live-runs.ts",
        "apps/daemon/src/http/runs-read.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/http/runs-read-contract.ts",
        "apps/daemon/src/http/runs-read.ts",
        "apps/daemon/src/http/runs-read.test.ts",
        "apps/daemon/src/planning/graph-query-test-fixtures.ts",
        "apps/control-room/src/live/live-runs.ts",
        "apps/control-room/src/live/live-runs.test.ts",
        "apps/control-room/src/v2/approvals/live-needs-you.test.tsx",
        "apps/control-room/src/v2/approvals/needs-you-model.test.ts",
        "apps/control-room/src/v2/approvals/preview-rejection-invariants.test.tsx",
        "apps/control-room/src/v2/approvals/replan-successor-port.test.ts",
        "apps/control-room/src/v2/board/board-columns.test.ts",
        "apps/control-room/src/v2/board/board-screen.test.tsx",
        "apps/control-room/src/v2/goals/goal-nodes.test.tsx",
        "apps/control-room/src/v2/goals/goal-publish.test.tsx",
        "apps/control-room/src/v2/runs/live-runs.test.tsx",
        "apps/control-room/src/v2/runs/runs-screen.test.tsx"
      ],
      "note": "GATES RUN, ADVERSARIAL PASS DONE, PATHS DECLARED. Full evidence in comment-407bc50f (gates + red attribution) and comment-7ebce840 (drill table + adversarial pass); summarised here so nothing load-bearing lives only in a comment.\n\nGATE WINDOW 2026-09-07T17:39:10Z-18:18Z under `full-suite-gate`, announced (msg-137425f8) and RELEASED on the way out (msg-7162f862). Each leg standalone and foreground, never piped for its status, never `;`-chained.\n1. `pnpm --filter @moe/control-room test` EXIT 0 — `Test Files 197 passed (197)` / `Tests 2569 passed (2569)`.\n2. `pnpm typecheck` EXIT 0, 6 packages Done, with a planted-error control that produced `error TS2322` and exit 1 before deletion, then the clean re-run.\n3. `pnpm --filter @moe/daemon test` RED — `Test Files 10 failed | 580 passed (590)`. FOREIGN and disclosed, never fabricated green.\n\nI quote NO root `pnpm test` count line: the root vitest include carries no `apps/**` path, so the root lane covers not one file in this row.\n\nTHE DAEMON RED IS NOT MINE, and I measured it in the direction that could have convicted me rather than the one that flatters me: I reverted MY OWN four daemon files to their HEAD bytes in memory, left the peer working tree untouched, and re-ran the ten failing files — `5 failed | 5 passed (10)`, 7 tests still failing. The failures survive the removal of my diff. Driver: in-flight PEER work adding a command kind (62 -> 63) across `daemon-command-registry.ts`, `-vocabulary.ts`, `-payload-keys.ts`, `-families.ts` plus untracked `monitoring/probe-interval-command*.ts` (task-749e585a). No failing file is an owned path and none references `runs-read` or `graph-query-test-fixtures`. Owned legs at the delivered tree are all EXIT 0: runs-read.test.ts 25/25, apps/daemon/src/planning 57 files / 1265 tests, live-runs.test.ts 56/56. So (HEAD failures minus baseline) intersected with owned paths is EMPTY.\n\nLINE CAP: live-runs.ts 390/400, runs-read.ts 317, runs-read-contract.ts 161. Nothing extracted, no comment shortened to squeak under.\n\nADVERSARIAL PASS added ONE ARM rather than only recording a thought: \"gives each goal its OWN declaration when both goals declare on the same node key\", with two different declarations so serving either goal the other's value reds. Drill D2 confirms both cross-goal arms red under a nodeKey-keyed projection. The other four questions answered by measurement: a schema-2 body serves `null` and never throws (and is the DOMINANT case — every pre-existing graph in this suite is schema 2); a malformed value can never yield a decoded node with the member missing (D4 and D6 both red); nothing sorts, dedups or defaults on either side (`git diff | grep` matches only a doc comment); no arm asserts a count where order is content.\n\nSEVEN DRILLS, ALL PROCESS-LOCAL, ZERO BYTES WRITTEN to the shared checkout — the gate was held by a peer for most of this row, and an on-disk mutant would have corrupted their legs. Two positive controls (24/24 and 56/56) precede the mutants, the harness throws on a missing anchor and on never applying, and restore proof is structural: `git diff --stat` shows runs-read.ts `9 insertions(+)` with ZERO deletions, live-runs.ts `11 insertions(+), 4 deletions(-)` (two re-wrapped lines), while every drill requires a deletion.\n\n16 PATHS DECLARED, exactly what changed: 4 daemon (contract, read, its test, the graph-query fixture I widened additively) and 12 control-room (the decoder, its test, and ten one-line fixture backfills). No `packages/scheduler` file (rail 1). No foreign dirty path reverted, stashed, committed or otherwise touched; the peer dirt noted in step 1 grew during the row and was left entirely alone.",
      "startedAt": "2026-09-07T17:29:43.557Z",
      "completedAt": "2026-09-07T18:19:56.671Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "ARCHITECT",
  "parentTaskId": "task-122b55fd2c284174876bd1d5573bdcc5",
  "dependsOn": [
    "task-122b55fd2c284174876bd1d5573bdcc5",
    "task-75bff0c9efc24daaad81ac81a373cd90"
  ],
  "priority": "HIGH",
  "order": 112.74,
  "comments": [
    {
      "id": "comment-43feb2b6b8074567ba035a54518df249",
      "author": "architect-d6d49adc",
      "content": "SCOPE BOUNDARY from task-8f1f15bc's planning (architect-d6d49adc, 2026-09-07) — no action required unless you disagree.\n\ntask-8f1f15bc (\"Activity and deployment reads expose bounded migration receipt facts\") has DoD 1 reading \"activity and deployment/Runs read boundaries\". I planned it WITHOUT touching `apps/daemon/src/http/runs-read.ts` or `apps/control-room/src/live/live-runs.ts`, because THIS row (and its parent task-122b55fd DoD 4) owns both files. Two rows editing the same two files for two different migration members in a shared worktree is a collision, not two gaps.\n\nTHE TWO SUBJECTS ARE GENUINELY DIFFERENT, which is why this is a boundary and not a duplicate:\n- YOURS: DECLARED migration identifiers — what a node says it will run, from the authority path.\n- 8f1f15bc's: migration RECEIPT observations — what an execution actually did (outcome APPLIED/REFUSED/REVERTED, applied/reverted identifiers, nullable backupRef, refusal code/layer/failing basename).\n\n8f1f15bc takes only the ACTIVITY boundary (via `VERDICT_KINDS`, no shape change) and the DEPLOYMENT boundary (`goal-deployment-read.ts` + `live-deployments.ts`). Presentation stays with task-78eedd8e per that row's own DoD 5.\n\nIF /runs/read should ALSO carry receipt observations, the rail says extend the row that owns the capability rather than duplicate it — so that is an amendment here, not a new row and not a path in 8f1f15bc. Flagging it as a decision for you or a governor, not asserting it is needed.\n\nONE MEASURED FACT THAT MAY HELP YOUR PLAN, since your DoD 6 forbids inferring ownership from a filename or SHA: there IS an authoritative durable join between a deploy and its migration. `deploy-command.ts:267` passes `{ environment, requestId: decisionId, sha }`, so the DEPLOY receipt's `decisionId` IS the migration receipt's `requestId`, and `migrationReceiptId(projectId, requestId)` resolves it deterministically via `readMigrationReceipt`. That is a durable key lookup, not a match — usable if you need to relate a run to an executed migration without violating DoD 6. Note it does NOT give you a goalRef or nodeRef: the migration receipt's eleven keys (migration-receipt.ts:22-36) contain neither, so it remains a project/environment observation.",
      "timestamp": "2026-09-07T12:56:13.272Z"
    },
    {
      "id": "comment-407bc50fec484f1e8365e68b101fe9fb",
      "author": "worker-7b684492",
      "content": "EVIDENCE 1/2 — GATES (DoD 7), with the daemon leg's foreign red attributed. Gate window 2026-09-07T17:39:10Z-18:18Z under `full-suite-gate` (announced msg-137425f8, released msg-7162f862).\n\nLEG 2 — `pnpm --filter @moe/control-room test` EXIT 0\n  Test Files  197 passed (197)\n  Tests  2569 passed (2569)\nMANDATORY leg, and the only one that covers the decoder and the eleven fixture sites: the root vitest include carries no `apps/**` path, so a bare root `pnpm test` count line is evidence for NOTHING in this row and I quote none.\n\nLEG 3 — `pnpm typecheck` EXIT 0, 6 packages Done (benchmark, review, scheduler, control-room, runner, daemon).\nPLANTED-ERROR CONTROL, to prove the leg is not short-circuiting: I created `apps/daemon/src/http/zz-planted-typecheck-control-de65294b.ts` with `export const plantedControl: number = \"this is not a number\";`. Result:\n  apps/daemon typecheck: src/http/zz-planted-typecheck-control-de65294b.ts(2,14): error TS2322: Type 'string' is not assignable to type 'number'.\n  apps/daemon typecheck: Failed\n  [ELIFECYCLE] Command failed with exit code 1.  => EXIT 1\nThat was the ONLY error reported, which incidentally shows the rest of the tree (peer dirt included) typechecks clean. File deleted (`ls` confirms \"No such file or directory\") and the clean re-run is the EXIT 0 above. This leg is what would have caught a missed fixture backfill; before the backfill it reported 12 errors across the 10 named files, after it, zero.\n\nLEG 1 — `pnpm --filter @moe/daemon test` RED, DISCLOSED VERBATIM:\n  Test Files  10 failed | 580 passed (590)\nFailing files: daemon-command-registry-human-approver.test.ts, daemon-command-registry.test.ts, daemon-command-vocabulary.test.ts, daemon-store-dependencies.test.ts, daemon-v2-command-registry.test.ts, mcp-main.test.ts, mcp-tool-allowlist.test.ts, mcp-http/mcp-http-host.test.ts, preview/preview-start-command.test.ts, work/foundation-attempt-windows.test.ts.\nExemplar lines:\n  AssertionError: expected [ 'approval.decide', ...(25) ] to have a length of 25 but got 26\n  AssertionError: expected 63 to be 62 // Object.is equality\n  FAIL src/mcp-tool-allowlist.test.ts > task-749e585a the probe-interval kind is unreachable over MCP\n\nATTRIBUTION, run in the direction that can exonerate me rather than the one that flatters me. I reverted MY OWN four daemon files (`http/runs-read.ts`, `http/runs-read-contract.ts`, `http/runs-read.test.ts`, `planning/graph-query-test-fixtures.ts`) to their HEAD bytes IN MEMORY via an out-of-tree vitest plugin, left the peer working tree completely untouched, and re-ran those same ten files:\n  Test Files  5 failed | 5 passed (10)\n  Tests  7 failed | 741 passed (748)\nThe failures persist without my diff, so my diff does not cause them. (The harness throws in `buildEnd` if no owned module was imported; it did not throw, so the revert genuinely applied.) The count differs from the full run only because the shared tree kept moving under me — the peer added dirty files between my runs.\n\nTHE DRIVER, measured: `git status --porcelain -- apps/daemon/src` shows in-flight PEER work on `daemon-command-registry.ts`, `daemon-command-vocabulary.ts`, `daemon-command-payload-keys.ts`, `daemon-command-families.ts` plus untracked `monitoring/probe-interval-command.ts` / `-contracts.ts` — task-749e585a's new command kind, which moves every roster from 62 to 63 while the roster tests are mid-update. Every failing file is one of those or a consumer of them; NONE is an owned path, and `grep` finds no reference to `runs-read` or `graph-query-test-fixtures` in any of them.\n\nOWNED-PATH LEGS, all EXIT 0 at the delivered tree:\n- `apps/daemon/src/http/runs-read.test.ts` => Test Files 1 passed (1) / Tests 25 passed (25)\n- `apps/daemon/src/planning` (the fixture I widened, 57 files) => Test Files 57 passed (57) / Tests 1265 passed (1265)\n- `apps/control-room/src/live/live-runs.test.ts` => Test Files 1 passed (1) / Tests 56 passed (56)\nSo (HEAD failures minus baseline failures) intersected with this row's owned paths is EMPTY.\n\nPER-FILE LINE CAP: live-runs.ts 390/400 (was 383), runs-read.ts 317, runs-read-contract.ts 161. No production file over 400; no file extracted, no comment shortened to squeak under.",
      "timestamp": "2026-09-07T18:19:00.490Z"
    },
    {
      "id": "comment-7ebce840a1c24d8aae3990b2e041dd31",
      "author": "worker-7b684492",
      "content": "EVIDENCE 2/2 — ADVERSARIAL PASS and the full drill table. Every drill in this row ran PROCESS-LOCAL (out-of-tree vitest config in %TEMP%, plugin rewrites the module in memory, throws on a missing anchor AND in buildEnd if never applied, per-mode cacheDir, package flags `--maxWorkers=4 --testTimeout=30000` restated by hand). ZERO bytes were written to the checkout, because `full-suite-gate` was held by a peer for most of this row. Method announced in #general (msg-556d81e1) before the first drill. Restore proof is therefore structural, not ceremonial: `git diff --stat` on the two production files shows runs-read.ts `9 insertions(+)` with ZERO deletions and live-runs.ts `11 insertions(+), 4 deletions(-)` (two re-wrapped lines), while every drill above requires a deletion; all five anchors are still present verbatim.\n\nDRILL TABLE — positive control first, so a red can never be the harness's fault.\n| # | mutation | result |\n|---|---|---|\n| control (daemon) | harness armed, unmutated | 24 passed (24), EXIT 0 |\n| D1 | `?? []` instead of `null` in `sealedNodesOf` | 3 failed \\| 21 passed — `expected [ [], [], [] ] to deeply equal [ null, null, null ]` |\n| D2 | key the projection on `nodeKey` across graphs | 2 failed \\| 23 passed — BOTH cross-goal arms, `expected [ 'migration-b', 'migration-a' ] to be null` and `expected [ 'migration-one' ] to deeply equal [ 'migration-two', 'migration-three' ]` |\n| D3 | `.sort()` the projected list | 2 failed \\| 22 passed — `expected [ 'migration-a', 'migration-b' ] to deeply equal [ 'migration-b', 'migration-a' ]` |\n| control (browser) | harness armed, unmutated | 56 passed (56), EXIT 0 |\n| D4 | drop the fail-closed clause `(declared !== null && declaredMigrations === null)` | 8 failed \\| 48 passed — ALL EIGHT malformed cases |\n| D5 | drop `\"declaredMigrations\"` from the exact-key roster | 6 failed — the 3 new arms PLUS 3 pre-existing ones (full-frame, tested-tree, landing) |\n| D6 | decode the member but omit it from the returned node | 7 failed — `expected undefined to deeply equal [ 'migration-b', 'migration-a' ]` |\nD2 is the one that matters most: it reds ONLY the cross-goal arms, which is exactly why DoD 2 is its own clause.\n\nADVERSARIAL QUESTIONS, each answered with a measurement rather than a reading.\n1. TWO GOALS SHARE A nodeKey AND BOTH DECLARE — does each row still serve its own? Now an arm of its own, with two DIFFERENT declarations so serving either goal the other's value reds. D2 confirms it reds.\n2. DOES A SCHEMA-2 BODY (predating the declaration) SERVE `null` RATHER THAN THROWING? Yes, and it is the dominant case, not a corner: every graph in the pre-existing runs-read suite comes from `compiledPlanAuthority`, whose bodies are all schema 2, and all 25 arms pass. The UNKNOWN-vs-NONE arm asserts it directly — `versionOf(\"dev-a\") === 2` on the very definition whose row serves `null`, against `versionOf(\"dev-b\") === 3` for the explicit `[]`.\n3. CAN A MALFORMED VALUE EVER PRODUCE A DECODED NODE WITH THE MEMBER QUIETLY MISSING? No: D4 and D6 both red. Eight malformed shapes are pinned to `RUNS_RESPONSE_INVALID` @ `CONTROL_ROOM_LIVE_RUNS` — the code AND the layer.\n4. DOES ANYTHING SORT, DEDUP OR DEFAULT ON EITHER SIDE? `git diff | grep -E \"\\.sort\\(|new Set|\\?\\?|\\.filter\\(\"` over the three production files matches only a doc COMMENT (\"never sorted or deduped\"). No production line.\n5. DID ANY ARM ASSERT ONLY A COUNT WHERE ORDER IS CONTENT? No — every identifier assertion is `toEqual([...])`; the count assertions are additions to a value assertion, never a substitute.\n\nCHECKLIST ITEMS WORTH RECORDING, none of them blocking:\n- CONCURRENCY: `sealedNodesOf` is a pure per-request projection over freshly read graphs, with no shared mutable state and no check-then-act. The member is copy-and-frozen (`Object.freeze([...])`), asserted with `not.toBe` against the durable array, so a caller cannot mutate authority content or observe another request's copy.\n- ENORMOUS INPUT: the daemon side is bounded upstream at `NODE_AUTHORITY_LIMITS.maxMigrationEntries = 64`, enforced in `node-authority-fields.ts:103`, so an unbounded list cannot reach the read. The browser decoder imposes no bound of its own — but neither do the sibling `criterionIds` / `dependsOn` on the same object, so this is the existing same-origin trust posture and not a class this row introduces. Noted, not changed.\n- SPARSE ARRAY, the one genuine sharp edge I found and then measured away: `Array.prototype.every` SKIPS HOLES, so `stringList([,\"a\"])` would answer true. It is unreachable from the wire because `mapRunsAnswer` only ever sees `JSON.parse` output and `JSON.stringify([,\"a\"])` is `[null,\"a\"]` — which `stringList` refuses, and which is one of the eight malformed cases I assert. `stringList` is a shared helper (criterionIds, dependsOn); changing it is another row's call.\n- PROTOTYPE POLLUTION: `exactDataRecord` rejects any non-`Object.prototype` prototype and builds an `Object.create(null)` snapshot; `{}` and `[[]]` are among the refused cases.\n- TASK RAILS: no `packages/scheduler` file touched (rail 1); ownership comes from `nodeRef` alone, never a filename or sha (rail 2); no nodeKey-keyed map exists anywhere (rail 3); no rendered string or wording added — board-lanes.tsx stays task-78eedd8e's (rail 4); exact-key decoding with a stable code AND layer (rail 5).",
      "timestamp": "2026-09-07T18:19:30.905Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-06T20:05:36.694Z",
  "updatedAt": "2026-09-07T18:21:34.630Z",
  "contextFetchedBy": [
    "worker-f72a1438",
    "architect-5ac70ba8",
    "worker-7b684492"
  ],
  "metrics": {
    "firstClaimAt": "2026-09-07T12:49:38.146Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 16,
    "executedStepCount": 7
  },
  "planSizeWarnings": [
    "Plan touches 16 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-07T12:54:58.752Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — making the browser member OPTIONAL (`declaredMigrations?: readonly string[] | null`) to avoid touching 10 fixture files. It reads as the cheap option and is wrong twice: `exactDataRecord` is strictly exact, so the key is ALWAYS on the wire, and an optional TS member would let a fixture omit what production always sends — the fixtures would drift from the wire, which is exactly the class of hole DoD 4 exists to close. A required `readonly string[] | null` makes UNKNOWN structural and costs one line per fixture.\n\nREJECTED — omitting the key from the wire when the declaration is UNKNOWN. Every node row would then fail `exactDataRecord` and decode to null: the whole Runs read would go dark, not just the new member. This is the single most likely implementation mistake in the row; the wire always carries the key.\n\nREJECTED — a `Map` from `nodeKey` to declared identifiers built once across graphs. That is precisely the cross-goal bleed DoD 2 names, and it would pass a single-goal test. The member is projected from the per-graph `definition` inside the existing loop, so no lookup table is created at all.\n\nREJECTED — splitting the control-room fixture backfill into its own row. The type change and its fixtures cannot land apart: the moment `RunNodeView` gains a required member, `pnpm --filter @moe/control-room typecheck` is red until all 10 are updated. Not separable, and 16 files is inside the cap.\n\nREJECTED — blocking this row. Ordering is already expressed as a field (dependsOn names both prerequisites, both unmet), the runtime withholds it from WORKING claims on its own, and the global rail says never to block on ordering a declared dependency already fixes. Planning now is what unblocks the fleet the moment the writer lands.\n\nREJECTED — extracting `nodeOf` from live-runs.ts pre-emptively to buy line-cap headroom. 383 + ~6 stays under 400, and the extraction would add a file and a fresh export surface for no behavioural gain. Named as a contingency in step 5 instead.",
    "codebaseInsights": "MEASURED by architect-5ac70ba8 2026-09-07 12:50-13:05Z at HEAD on branch moe/work-2026-09-04.\n\n1. THE GAP IS REAL, both sites confirmed. `git grep -rn declaredMigrations -- packages apps` returns hits ONLY under `packages/scheduler/src/node-authority/` (contract :193 `readonly declaredMigrations?: readonly string[];`, codec, compose, and the codec test). **ZERO hits anywhere under `apps/`.** `runs-read-contract.ts:76-100` RunNodeView and `live-runs.ts:58-72` RunNodeView both carry no migration member, exactly as the description states.\n\n2. THE MEMBER IS REACHABLE ON THE READ PATH ONCE POPULATED. `packages/scheduler/src/graph-content-fields.ts:82` -> `readonly nodeAuthority: NodeAuthoritySection`, whose `definitions` are `NodeDefinition`s. `runs-read.ts:92` iterates `graph.content.nodeAuthority.definitions` and `sealedNodesOf` (:87-105) projects each into a local `SealedNode` (:78-85) that lists six fields and DROPS everything else — that projection is where the member is lost. The dependency edge is already live: apps/daemon imports `@moe/scheduler` (e.g. `planning/v2-compiler/authority-contracts.ts:2 import type { NodeDefinition, NodePlanningSourceContent } from \"@moe/scheduler\"`), and the barrel re-exports node-authority via `packages/scheduler/src/index.ts:262`.\n\n3. THE EXECUTION IDENTITY ALREADY EXISTS AND IS ALREADY PER-GOAL. `runs-read.ts:100` computes `nodeRef: compiledExecutionRef(projectId, graph, definition.nodeKey)` INSIDE the per-graph loop, and `RunNodeView` already carries both `nodeKey` (\"local graph/display name\", contract :85) and `nodeRef` (\"Opaque execution identity\", contract :87). So DoD 2's no-cross-goal-bleed property is satisfied by sourcing the member from the per-graph `definition` and carrying it on the `nodeRef`-keyed row — NOT by inventing a new key. The defect to guard against is a lookup map keyed on `nodeKey`; `runs-read.ts:228` already shows the shape of that hazard (`shared.has(node.nodeKey)`).\n\n4. THE BROWSER DECODER IS STRICTLY EXACT — THIS IS THE LOAD-BEARING FINDING. `live-runs.ts:125-145 exactDataRecord` requires `keys.length !== expectedKeys.length` to fail AND every own key to be in the roster. **There is no optional-key path.** Consequence, and it decides the wire design: the daemon must ALWAYS emit the key. `null` = UNKNOWN (the node's authority body predates the declaration), an array = the author declared, `[]` = declared none. That is how DoD 5's UNKNOWN-vs-explicit-empty distinction survives a strictly-exact decoder; an omitted key would make every node row fail to decode. The node roster to edit is the array at `live-runs.ts:237-240`; `stringList` (:176) already returns `readonly string[] | null` and is the right helper, but note it returns null for BOTH \"not an array\" and never for \"absent\", so the null-vs-array branch must be written explicitly rather than leaning on it.\n\n5. CONSUMER BLAST RADIUS, measured by counting `sharedKey:` occurrences per file (a RunNodeView literal always states it). CONTROL-ROOM, 10 consumer files beyond live-runs.ts itself: live-runs.test.ts, v2/approvals/live-needs-you.test.tsx, v2/approvals/needs-you-model.test.ts, v2/approvals/preview-rejection-invariants.test.tsx, v2/approvals/replan-successor-port.test.ts, v2/board/board-columns.test.ts, v2/board/board-screen.test.tsx, v2/goals/goal-nodes.test.tsx, v2/goals/goal-publish.test.tsx, v2/runs/live-runs.test.tsx, v2/runs/runs-screen.test.tsx. DAEMON: runs-read-contract.ts, runs-read.ts, orchestrator/compiled-node-identity.test.ts. Sampled three (board-columns.test.ts:11-16, runs-screen.test.tsx:14-17, goal-nodes.test.tsx:16-20) and every one is a SINGLE base fixture with a `...extra` / `...overrides` spread, so each consumer is a ONE-LINE edit, not a rewrite. 16 distinct files total, inside this project's `taskSizing.maxDistinctFiles: 20`.\n\n6. FILE-SIZE WATCH. `live-runs.ts` is 383 physical lines against the 400 hard cap (250 target). The planned addition is ~6 lines. Keep it to that; do not add a paragraph comment there.\n\n7. CONTROL-ROOM HAS NO `@moe/scheduler` IMPORT (`git grep -rn \"@moe/scheduler\" -- apps/control-room` -> nothing). It is a standalone wire decoder and must stay one — no new package edge is owed or wanted.\n\n8. PREREQUISITES ARE NOT LANDED AND THIS ROW IS CORRECTLY GATED. dependsOn = [task-122b55fd (parent authority), task-75bff0c9 (the author/writer)]; `dependsOnUnmet` names BOTH. task-122b55fd is WORKING with `pnpm --filter @moe/scheduler test` exit 0 at 11:33Z and its authority half is on disk (see 1), but `landed:false`. task-75bff0c9 is PLANNING and is the row that carries the identifiers from authored planning source INTO the compiled node — measured absent (`declaredMigrations` has zero `apps/` hits, so the v2-compiler does not carry it today). Until it lands, `nodeAuthority.definitions[i].declaredMigrations` is `undefined` on every real graph, and DoD 1 (\"a declaration written through the parent's real authority path — not a hand-built frame and not a fixtu",
    "risks": "1. THE ROW CANNOT START UNTIL BOTH PREREQUISITES LAND. If step 1's measurement finds `declaredMigrations` still absent under `apps/`, there is no real authority path to assert against and DoD 1 is unsatisfiable — `moe.report_blocked` with blockedOnTaskIds [task-122b55fd2c284174876bd1d5573bdcc5, task-75bff0c9efc24daaad81ac81a373cd90], never a hand-built authority body and never a fixture that sets the member directly.\n2. THE OMITTED-KEY TRAP (see approaches). If UNKNOWN is encoded by omitting the key, every node row silently fails to decode and the Runs screen empties. The decoder test must include a row with `declaredMigrations: null` and assert the WHOLE node still decodes, not just the member.\n3. `stringList` returns null for a non-array, and the wire's UNKNOWN is also null. Do not funnel both through one helper — branch on `record.declaredMigrations === null` FIRST, then require `stringList` to succeed, so a malformed value (a number, a nested array, `undefined`) refuses instead of being mistaken for UNKNOWN. That is DoD 4's fail-closed clause and the likeliest silent defect in the row.\n4. ORDER AND DUPLICATES ARE CONTENT (parent's ruling). Neither the read nor the decoder may sort, dedup or default. A test asserting only `toHaveLength(2)` would pass a reversing implementation — assert by value AND in order (`toEqual([\"migration-a\", \"migration-b\"])`), which is what DoD 3's \"by value and by count\" means.\n5. BIDIRECTIONAL ROSTER RAIL. The node key roster at live-runs.ts:237-240 is a served-capability roster: assert both that the new key is accepted AND that an unknown extra key still refuses, or the arm is one-directional and goes vacuous.\n6. `live-runs.ts` is 383/400 lines. Keep the addition to ~6 lines; if it would cross 400, extract `nodeOf` into a sibling module and declare the new path rather than shortening a comment to squeak under.\n7. NO UI. board-lanes.tsx and goal-nodes.tsx rendering belong to task-78eedd8e. Backfilling a fixture literal in a `.test.tsx` file is NOT rendering work and is in scope; adding a rendered string is not.\n8. TOUCH NO `packages/scheduler` FILE (task rail 1). Everything this row needs is already exported.\n9. Shared checkout: `git status --porcelain` before editing and never revert, stash or commit a foreign path.",
    "keyFiles": [
      "apps/daemon/src/http/runs-read-contract.ts",
      "apps/daemon/src/http/runs-read.ts",
      "apps/daemon/src/http/runs-read.test.ts",
      "apps/control-room/src/live/live-runs.ts",
      "apps/control-room/src/live/live-runs.test.ts",
      "packages/scheduler/src/node-authority/node-authority-contract.ts",
      "packages/scheduler/src/graph-content-fields.ts",
      "apps/daemon/src/orchestrator/compiled-node-source.ts"
    ]
  },
  "planApprovedAt": "2026-09-07T12:55:01.073Z",
  "workStartedAt": "2026-09-07T17:15:39.132Z",
  "reviewStartedAt": "2026-09-07T18:21:34.630Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": " Test Files  197 passed (197)\n      Tests  2569 passed (2569)\n   Start at  21:20:04\n   Duration  20.09s (transform 60.81s, setup 0ms, import 122.54s, tests 129.66s, environment 220.18s)",
    "reportedAt": "2026-09-07T18:21:34.630Z"
  },
  "filesModified": [
    "apps/daemon/src/http/runs-read-contract.ts",
    "apps/daemon/src/http/runs-read.ts",
    "apps/daemon/src/http/runs-read.test.ts",
    "apps/daemon/src/planning/graph-query-test-fixtures.ts",
    "apps/control-room/src/live/live-runs.ts",
    "apps/control-room/src/live/live-runs.test.ts",
    "apps/control-room/src/v2/approvals/live-needs-you.test.tsx",
    "apps/control-room/src/v2/approvals/needs-you-model.test.ts",
    "apps/control-room/src/v2/approvals/preview-rejection-invariants.test.tsx",
    "apps/control-room/src/v2/approvals/replan-successor-port.test.ts",
    "apps/control-room/src/v2/board/board-columns.test.ts",
    "apps/control-room/src/v2/board/board-screen.test.tsx",
    "apps/control-room/src/v2/goals/goal-nodes.test.tsx",
    "apps/control-room/src/v2/goals/goal-publish.test.tsx",
    "apps/control-room/src/v2/runs/live-runs.test.tsx",
    "apps/control-room/src/v2/runs/runs-screen.test.tsx"
  ],
  "completionSummary": "/runs/read serves `declaredMigrations` and the browser decoder preserves it. 16 paths: 4 daemon, 12 control-room. Full evidence in comment-407bc50f (gates + red attribution) and comment-7ebce840 (7-drill table + adversarial pass).\n\nCONTRACT: `declaredMigrations: readonly string[] | null`, REQUIRED and ALWAYS EMITTED on both sides. `null`=UNKNOWN, `[]`=declared none, array=authored order. Sourced in `sealedNodesOf` off the SAME per-graph `definition` the loop already turns into `nodeRef`, copy-and-frozen; NO nodeKey-keyed map exists anywhere, so cross-goal bleed is structurally impossible, not merely tested against.\n\nDoD 1 — driven through the parent's REAL authority path. `compiledPlanAuthority` cannot carry a declaration (compiled-policy-authority-body.ts:98-113 deliberately declares none), so I reached `createNodeDefinition` via `graph-query-test-fixtures.ts`, extended additively with an optional per-node declaration spread into the DRAFT — the same seam production's scheduler-node-planning-authority.ts:141 uses. The member is the codec's `project()` output, never assigned by a fixture. A splice is impossible: `encodeGraphContent` re-derives and refuses GRAPH_CONTENT_AUTHORITY_DISAGREEMENT.\nDoD 2 — TWO BOUND GOALS (`goal-${commandId}`), same nodeKey. One arm: declarer serves, other serves null. Second arm added by the adversarial pass: BOTH declare, different values, each serves its own. Drill D2 (nodeKey-keyed projection) reds both and NOTHING else.\nDoD 3 — `toEqual([\"migration-b\",\"migration-a\"])` by value and in order on both sides; drill D3 (`.sort()`) reds.\nDoD 4 — 8 malformed wire values each refuse `RUNS_RESPONSE_INVALID` @ `CONTROL_ROOM_LIVE_RUNS`. The plan's `=== undefined` guard would have been DEAD CODE (`stringList` never returns undefined); shipped guard tests the RAW wire value: `(declared !== null && declaredMigrations === null)`. Drill D4 reds all 8.\nDoD 5 — UNKNOWN vs explicit-empty asserted together, `[null, [], [\"migration-c\"]]`, plus schemaVersi"
}