{
  "id": "task-99f364e3a6104ee993b6820c580a52fb",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "Platform integration and hardening: one goal reaches a running deployed environment and back out again",
  "description": "THE EPIC-FINAL ROW for epic-3f9b9556 (Deployment platform). Filed by architect-b63fabf3 on 2026-09-05, at the human's direction, when this epic was split out of epic-8ebe6417 and left with no integration-and-hardening row of its own. Highest order in the epic; `get_context.isEpicFinal` should be true when you hold it.\n\nWHY IT EXISTS. Verification here is CONCENTRATED, not smeared: mid-epic rows prove their own slice and move on, and ONE row at the end gates the whole. The five band parents named in dependsOn (5ef1a0a9 repository, 5d309484 deploy, 9ae492b3 secrets, 8f86e105 migrations, 7683a432 monitoring) are each integration-shaped for their OWN band. Nothing in this epic tests what the bands got wrong ABOUT EACH OTHER - and here the bands are a PIPELINE, so that is nearly all of the risk: the scaffold decides what the image builds, the image decides what variables it needs, the variables decide whether migrations can run, and monitoring probes whatever deploy produced.\n\nWHAT THIS ROW OWNS - the five things no single slice could see:\n\n1. ONE GOAL REACHES A RUNNING ENVIRONMENT. Scaffold a repository from a PRD, deploy it, set its variables, migrate its schema, and prove the deployed thing SERVES - health green, on the landed sha. Every band contributes one link; the chain does not exist to be tested until all five have landed.\n\n2. AND BACK OUT AGAIN. Roll it back and prove the previous sha is serving. A deploy path with no proven exit is a one-way door, and the rollback is the half most likely to have been tested only in isolation.\n\n3. NOTHING LEAKS. This is the epic that starts real processes: containers, a disposable PostgreSQL, a probe schedule, backup jobs. Assert that a FAILED deploy, a FAILED migration and an INTERRUPTED verifier each tear their resources down - not just the happy paths. A test that leaks a container or a port makes every later gate on this board inadmissible, and the failure paths are where leaks actually live.\n\n4. NO SECRET SURFACED ANYWHERE ALONG THE CHAIN. task-d0c63cd8 (DONE) made variables encrypted at rest. This row proves the whole PIPELINE honours it: grep the delivered artifacts - deploy logs, probe output, incident cards, receipts, error paths, generated infrastructure files, and any fixture committed to the tree - for a planted canary value, and assert zero hits. Assert the canary was actually planted and actually reached the deployed process, or the sweep proves nothing.\n\n5. THE DOCS SWEEP for this epic, held back from the individual rows so the same file is not re-edited five times.\n\nMEASURED CONTEXT AT FILING (2026-09-05, HEAD f3b94eaa, branch moe/work-2026-09-04): 23 non-archived rows, of which 22 are LIVE and only ONE is DONE. THIS IS THE LEAST-STARTED THIRD OF THE BOARD. Read that as an instruction: when you claim this row, most of what it certifies will have been built recently and by many hands, so re-measure everything and treat this description as stale. This epic also does NOT own the shared runtime roster any more - the environment kinds (task-d0d1cb5c, task-b3888068, task-a2409cba) publish through the same contracts files and the same four digest mirrors as the operator-gates epic, whose rows are NOT visible in your epicSiblings. Census those mirrors yourself with a full-tree `git grep` of the literal across ALL file types.\n\nNOT IN SCOPE: building any band. Every capability here is shipped by a row named in dependsOn. A gap found here becomes a prerequisite production row measured symbol by symbol - never a narrowed DoD, never a mock-backed journey, never authority reimplemented inside a test.\n\nVERIFICATION COMMAND for moe.complete_task: `pnpm --filter @moe/daemon test`. Run every leg named in DoD-6 and quote all their count lines.",
  "definitionOfDone": [
    "FULL REGRESSION, green, with numbers, across every package this epic touched - `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, and root `pnpm test`, plus whatever package the scaffold and infrastructure rows created (resolve those legs BY PACKAGE NAME, never by directory: `pnpm --filter <pkg>` prints \"No projects matched the filters\" and EXITS 0 for a name that is not a workspace package). Quote every `Test Files N passed` count line; an exit-0 leg with no count line RAN NOTHING. Root `pnpm test` covers NO apps/** file, so the app legs are not optional.",
    "ONE GOAL REACHES A RUNNING DEPLOYED ENVIRONMENT, end to end and for real: a repository scaffolded from a PRD, deployed at a landed sha, its variables delivered to the process, its schema migrated, and its health probe GREEN. Assert the deployed thing actually SERVES - an HTTP response from the running environment, not a receipt saying it should be up. Name the sha and quote the probe result.",
    "AND ROLLS BACK: after the deploy above, roll the environment back and prove the PREVIOUS sha is the one now serving, again by response rather than by receipt. Assert the rollback is reachable from the incident path a real operator would use, not only from a test harness.",
    "NOTHING LEAKS ON A FAILURE PATH. Assert teardown for a FAILED deploy, a FAILED migration and an INTERRUPTED verifier - three arms, each proving the container, the disposable database and the port are gone afterwards. Enumerate the live resources before and after by a real query (`docker ps`, a port probe, a process list), not by trusting a cleanup function returned. The happy-path teardown is already each band's own; this clause is only about the failure paths.",
    "NO SECRET SURFACES ANYWHERE ALONG THE PIPELINE. Plant a canary variable value, prove it REACHED the deployed process (or the sweep is vacuous), then grep every delivered artifact for it - deploy logs, probe output, incident cards, receipts, error paths, generated infrastructure files, and any fixture committed to the tree - and assert zero hits. Quote the canary's shape (not its value) and the exact paths swept.",
    "THE ADVERSARIAL PASS over the WHOLE EPIC DIFF plus THE DOCS SWEEP, in one pass each. The pass looks for what the bands got wrong about each other, not at any band alone: a scaffold that generates infrastructure the deploy path cannot consume; a variable name the app expects and the environment store spells differently; a migration that works alone but not in sequence after a rollback; a probe schedule that outlives the environment it probes; a refusal code whose layer disagrees across two bands. Record what was checked AND what was found - a pass that finds nothing must say what it looked for. The docs sweep covers the operator-facing words for all five bands and any doc that still describes the platform as unbuilt."
  ],
  "taskRails": [
    "THIS ROW BUILDS NO BAND. Every capability it certifies is shipped by a row named in dependsOn. A gap found here becomes a prerequisite production row with the gap measured symbol by symbol - never a narrowed DoD, a mock-backed journey, or authority reimplemented inside a test. A proof that only proves shapes retires the question unanswered.",
    "ANYTHING THIS ROW STARTS, IT STOPS - and it proves the FAILURE paths stop too. Enumerate live containers, databases and ports by real query before and after; a cleanup function that returned is not evidence the resource is gone. A leaked container or port makes every later gate on this board inadmissible.",
    "A SECRET NEVER SURFACES IN PLAINTEXT - not on a screen, not in a log line, not in a receipt, not in an error path, not in a generated infrastructure file, not in a fixture committed to the tree. And a canary sweep that cannot prove the canary was planted and delivered proves nothing.",
    "THE SHARED RUNTIME ROSTER IS OWNED BY A DIFFERENT EPIC. The environment kinds publish through the same contracts files and the same four digest mirrors as the operator-gates epic, whose rows will NOT appear in your epicSiblings. Census the mirrors yourself with a full-tree `git grep` across ALL file types, and run `pnpm --filter @moe/control-room test` - two mirrors are invisible to `pnpm test`, `pnpm typecheck` and the daemon leg.",
    "RE-MEASURE BEFORE PLANNING. At filing, 22 of this epic's 23 rows were unstarted - so nearly everything this row certifies will be recently built by many hands. Treat every claim in this description as stale by default and read the dependsOn rows' completionSummary and reviewSummary from epicSiblings first.",
    "FEATURES ONLY (inherited): no security-boundary or policy work."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE THE TWO THINGS THAT DECIDE WHETHER THIS ROW CAN COMPLETE, before any code.\n\nA. THE FIVE BANDS. All five dependsOn parents were unlanded at plan time. For each, read `completionSummary` and `reviewSummary` from `get_context.epicSiblings` FIRST (rail 5 directs that instead of re-measuring HEAD), then spot-check on disk what they do not cover:\n  `git grep -c \"deployment.deploy\\|deployment.rollback\\|moe-migration-receipt\\|moe-health-probe\\|environment.set_variable\" -- apps packages`\nEach returned NOTHING at plan time. A gap found here becomes a PREREQUISITE PRODUCTION ROW measured symbol by symbol (rail 1) - never a narrowed DoD, never a mock, never authority reimplemented in a test.\n\nB. DOCKER. Run `docker version --format \"{{.Server.Version}}\"` and record the verbatim output. At plan time this host answered:\n  failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine ... The system cannot find the file specified.\nIF DOCKER IS STILL ABSENT, DoD 2, 3 AND 4 CANNOT BE HONESTLY MET: DoD 2 demands an HTTP response from a running environment rather than a receipt, DoD 3 demands the previous sha be SERVING, DoD 4 demands live resources enumerated by real query. Do steps 2, 6 and 7 (which need no docker), then `moe.report_blocked` with this measurable unblock condition:\n  \"a host with a running docker daemon; verify with `docker version --format {{.Server.Version}}` returning a version\"\nquoting the failure verbatim and listing exactly which DoD items remain. That is the shape task-3706b7e7 used and the governor endorsed. DO NOT assert a receipt in place of a response, DO NOT mock the container, DO NOT skip the arm - each converts a blocked clause into a false green.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/deployment/deployment-docker-probe.ts"
      ],
      "note": "MEASURED AT HEAD 7622a2e8 (moe/work-2026-09-04), 2026-09-08 ~16:1xZ. BOTH of step 1's decision inputs have INVERTED since plan time. This step wrote no code.\n\nB. DOCKER IS PRESENT. `docker version --format \"{{.Server.Version}}\"` answers `29.6.2`, EXIT 0. At plan time this host answered `failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine ... The system cannot find the file specified.` THE BLOCKING BRANCH IS VOID: step 1's instruction to \"do steps 2, 6 and 7 then report_blocked\" was conditioned on `IF DOCKER IS STILL ABSENT`, and it is not. Steps 3, 4 and 5 are therefore IN SCOPE and DoD 2, 3 and 4 are honestly meetable on this host. I am not blocking.\n\nA. ALL FIVE BANDS HAVE LANDED. At plan time every dependsOn parent was unlanded and the symbol census `git grep -c` returned NOTHING for all five literals. Now, over `-- apps packages`:\n  deployment.deploy 41 files | deployment.rollback 34 | moe-migration-receipt 4 | moe-health-probe 2 | environment.set_variable 18 | repository.bootstrap 40\nAll 6 dependsOn rows read DONE with `landed: true` in epicSiblings (5ef1a0a9 repository, 5d309484 deploy, 9ae492b3 secrets, 8f86e105 migrations, 7683a432 monitoring, c78140e9 the redaction leaf). `dependsOnUnmet` is empty. Real modules exist for every link I must drive: apps/daemon/src/repository/controlled-profile/controlled-profile-generator.ts, deployment/{deploy-service,deploy-image-build,rollback-command,rollback-restore,migrate-down-command}.ts, environment/{environment-store,environment-delivery,environment-cipher}.ts, repository/migrations/{migration-service,migration-receipt,migration-down-service}.ts, monitoring/{health-probe-ring,health-incident,probe-interval-command}.ts. NO GAP FOUND, so rail 1's \"gap becomes a prerequisite production row\" does not fire here.\n\nFINDING THE PLAN DID NOT ANTICIPATE, and it shapes steps 3-5. Root `vitest.config.ts` include is `[\"adapters/**/*.test.ts\", \"packages/**/*.test.ts\", \"tests/**/*.test.ts\", \"tools/**/*.test.ts\"]`. `tests/**/*.test.ts` MATCHES `*.e2e.test.ts`, so the three files this plan creates under tests/e2e/foundation/ will execute in the SHARED root `pnpm test` gate, not only in `pnpm test:e2e`. Every other seat's root lane then pays my docker time, and a leak of mine reds their gate. Consequences I am carrying forward: (a) rail 2's teardown duty is now a duty to the whole board, not just this row; (b) I will measure the docker legs' wall-clock before adding them and disclose it; (c) only ONE existing e2e file opts out (`canary-self-host.e2e.test.ts:83 it.skipIf(!CANARY_OPT_IN)`), so an opt-out is precedented but rail 1 and `deployment-docker-probe.ts`'s own \"DoD 4: loud, never a skip\" both forbid using it to dodge a docker leg on a host that HAS docker.\n\nVERIFICATION COMMAND NOTE for later: the row's named command `pnpm --filter @moe/daemon test` does NOT cover `tests/**`, so it will not execute the files steps 3-6 create. DoD 1's root `pnpm test` leg is what covers them. Both get run and both get quoted; the completion record will say which leg proves which file.",
      "startedAt": "2026-09-08T16:06:17.090Z",
      "completedAt": "2026-09-08T16:07:27.610Z"
    },
    {
      "stepId": "step-2",
      "description": "FULL REGRESSION (DoD 1) — runs regardless of docker, so do it before any blocking decision.\n\nEach leg standalone, each quoted with its `Test Files N passed` count line. An exit-0 leg with NO count line RAN NOTHING, and `pnpm --filter <pkg> <script>` prints \"No projects matched the filters\" and EXITS 0 for a name that is not a workspace package — so resolve every leg BY PACKAGE NAME, never by directory:\n- `pnpm --filter @moe/daemon test`\n- `pnpm --filter @moe/control-room test`\n- `pnpm typecheck`\n- `pnpm test` (root lane)\n- plus whatever package the scaffold and infrastructure rows created. Find its NAME first: `git grep -l '\"name\"' -- '**/package.json'` and match against what those rows shipped.\n\nState plainly that root `pnpm test` covers NO `apps/**` file, so the two app legs are not optional and a bare root count line is not evidence for them.\n\nEXPECT FOREIGN RED AND ATTRIBUTE IT RATHER THAN CHASING IT: the governor's 19:26Z/19:30Z logs record `mtkbtsvc` handle exhaustion making full-suite runs time out at 30 s in files nobody owns (`src/work/foundation-capture-producer.test.ts` named repeatedly). Apply the path-attributed baseline — merge-base failures, HEAD failures, subtract, intersect with this row's owned paths. Disclose foreign red verbatim with paths; do NOT re-run a full suite repeatedly, which degrades every other seat's gate.\n\nKeep this run: it is the merge-base half of the baseline the completion record needs.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts"
      ],
      "newFiles": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts"
      ],
      "note": "FULL REGRESSION (DoD 1), run 2026-09-08 19:08-19:18 local under a full-suite-gate lease I hold, at HEAD 7622a2e8. Each leg STANDALONE and FOREGROUND, resolved BY PACKAGE NAME. THIS STEP WROTE NO FILES (measurement only).\n\n| leg | exit | count line |\n|---|---|---|\n| `pnpm typecheck` | **0** | all 20 projects `Done`, zero `error TS` |\n| `pnpm --filter @moe/control-room test` | **0** | `Test Files 204 passed (204)` / `Tests 2673 passed (2673)` |\n| `pnpm test` (root) | **0** | `Test Files 434 passed \\| 2 skipped (436)` / `Tests 10404 passed \\| 46 skipped (10450)` |\n| `pnpm --filter @moe/daemon test` | **1** | `Test Files 1 failed \\| 595 passed (596)` / `Tests 1 failed \\| 10640 passed \\| 8 skipped (10649)` |\n\nTHE FIFTH LEG RESOLVES TO NOTHING, and that is a measurement, not an omission. DoD 1 says \"plus whatever package the scaffold and infrastructure rows created\". Enumerated every tracked `package.json` by NAME: 20 workspace packages, and NONE is new for this epic. The scaffold generator ships INSIDE @moe/daemon (`apps/daemon/src/repository/controlled-profile/controlled-profile-generator.ts`) and the infrastructure generator likewise (`apps/daemon/src/repository/deployment/deployment-infrastructure-generator.ts`); the 22-file tree is emitted at runtime, never committed as a workspace package. So the daemon leg IS the scaffold leg. No `--filter` was run against a non-package, so the \"No projects matched the filters / EXIT 0\" trap did not fire on me.\n\nROOT COVERS `tests/**` BUT NO `apps/**`. Correcting the plan's framing in one direction only: root `pnpm test` genuinely covers no `apps/**` file, so the two app legs are NOT optional and the root count line is not evidence for them. But root's include IS `[\"adapters/**\", \"packages/**\", \"tests/**\", \"tools/**\"]/*.test.ts`, and `tests/**/*.test.ts` matches `*.e2e.test.ts` — so the files steps 3-6 create WILL run in this shared root lane. The 434/10404 above is therefore the merge-base half of the baseline for those files too.\n\nTHE DAEMON RED IS FOREIGN AND UNCOMMITTED, and I did not chase it (rail: do not fix outside scope; memory: never revert a peer's dirty paths).\nFAIL `src/repository/git-publication-credentials.test.ts > treats a remote git cannot url-match as having no credential configuration` — `Error: PUBLISH_CREDENTIAL_CONFIGURATION_UNREADABLE` at `git-publication-credentials.ts:8`.\nATTRIBUTION, measured three ways rather than guessed:\n1. `git status --porcelain` shows BOTH paths ` M` — dirty, uncommitted, not mine. I have written no file this session.\n2. The failing test DOES NOT EXIST AT HEAD: `git show HEAD:...test.ts | grep -c \"cannot url-match\"` = **0**.\n3. `git diff --stat` on the pair: +35/-1 across 2 files. The dirt adds the `UNMATCHABLE_REMOTE_FATAL` constant and the test that asserts the fixed behaviour, but `publicationCredentialArguments` still reads `if (result.code === 1 && result.stdout === \"\") return []; if (result.code !== 0) throw` — the CONSTANT IS DECLARED AND NEVER CONSUMED. That is a peer mid-fix, caught between the test and the repair.\nThis is task-48945cb6a24d4a5db0723c1f914624d1, the daemon bug I filed and proved from task-817d893f: git answers exit 128 on any schemeless remote, so every scp-style SSH remote is unpushable. A live seat is repairing it in this shared checkout right now.\nINTERSECTION WITH THIS ROW'S OWNED PATHS: **EMPTY**. Owned paths are three not-yet-created files under `tests/e2e/foundation/` plus three docs.\nCONSEQUENCE I AM NOT HIDING: this row's named verification command IS `pnpm --filter @moe/daemon test`, and `complete_task` requires exit 0 from it. I will re-run it fresh at completion. If the peer's half-fix is still uncommitted then, this row cannot complete on that command through no defect of its own — I will report_blocked on task-48945cb6 rather than quote a stale green or work around it.\n\nFORESIGHT THE PLAN EXPECTED THAT DID NOT MATERIALISE: no `mtkbtsvc` handle exhaustion, no 30 s timeouts, and `src/work/foundation-capture-producer.test.ts` passed. The root cutover red (`CUTOVER_MANIFEST_ENTRY_LIMIT_EXCEEDED`) that comment-1ac479b2 warned would red DoD 1 is FIXED at d079210c; my root numbers match qa-2702df88's 15:11-15:13Z run exactly. I ran each suite ONCE.",
      "startedAt": "2026-09-08T16:07:32.381Z",
      "completedAt": "2026-09-08T16:18:27.912Z"
    },
    {
      "stepId": "step-3",
      "description": "ONE GOAL REACHES A RUNNING ENVIRONMENT (DoD 2) — needs docker; skip to step 6 and block if step 1 found none.\n\nIn `tests/e2e/foundation/platform-pipeline.e2e.test.ts`, drive the whole pipeline on ONE goal, each link through the REAL path its band ships:\n1. scaffold a repository from a PRD (the controlled-profile generator, landed at sha 2feed27b, emits a 22-file tree);\n2. deploy it at a landed sha;\n3. set its variables and prove they reach the process;\n4. migrate its schema;\n5. probe its health.\n\nASSERT THE DEPLOYED THING SERVES. DoD 2 is explicit: an HTTP RESPONSE from the running environment, NOT a receipt saying it should be up. A receipt asserts intent; a response asserts reality, and the entire point of an epic-final row is to tell those apart. Name the sha and quote the probe result.\n\nAssert the served build corresponds to the LANDED SHA — not merely that something answers on the port. Something answering is compatible with a stale container from a previous run, which is exactly the confusion a leaked resource creates.\n\nMOCK NOTHING IN THE CHAIN (rail 1). Doubles are permitted only outside it — a clock, an external registry. Name each and why it is not a link.\n\nReuse the real-process harness shape from tests/e2e/foundation/ (e2e-harness.ts, j1-loop-harness.ts) rather than inventing a fifth. Teardown in a `finally`, and step 5 will verify it actually happened.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts",
        "tests/e2e/foundation/platform-pipeline-harness.ts"
      ],
      "note": "ONE GOAL REACHES A RUNNING DEPLOYED ENVIRONMENT (DoD 2), PROVEN BY RESPONSE. Green on real docker 29.6.2, 2026-09-08 19:35-19:38 local, 131.32s.\n\n`MOE_PLATFORM_PIPELINE=1 npx vitest run tests/e2e/foundation/platform-pipeline.e2e.test.ts` -> `Test Files 1 passed (1)` / `Tests 5 passed (5)`, EXIT 0, printing:\n  PLATFORM PIPELINE server=29.6.2 shaA=649ff8f41ba9c3823c52625411bdff890116870b shaB=5e57e2e9a364b55487db1742cbd26cfc6a71b213\n  PLATFORM PIPELINE deployed=build-b-79422cc8 rolledBackTo=build-a-cd4cfb48\n\nEVERY LINK IS THE SHIPPED PATH. Scaffold = `generateControlledProfile` + `planDeploymentInfrastructure`; topology = real `docker compose up --wait` on the generated `docker-compose.yml` + `docker-compose.override.yml` VERBATIM (db/app/proxy, public port 3000 unremapped); build = `nodeDeployBuild`, which streams a `git archive` of a REAL COMMIT, so \"deployed at a landed sha\" is literal — I `git init` the workspace and commit, and the deploy is asked for that 40-hex sha; variables = real `setEnvironmentVariable` into the encrypted store; migration = real `migrateWithBackup` behind the real `resolveDeployMigrationContext`, composed exactly as `deploy-command.ts:260-280` composes it; deploy/rollback = real `createDeployService` over `productionDeployPorts`. NO `createDockerDouble` anywhere.\n\nTHE TWO THINGS I SUPPLY, named so a reviewer need not hunt: the DEPLOY TARGET (`{network, url, sshTarget:null}`) — configuration an operator sets once via `deployment.set_target`, not a band capability — and the ENVIRONMENT CREDENTIAL, which the composition root supplies in production. Neither carries behaviour under test.\n\nHOW THE RESPONSE NAMES THE SHA. `{\"status\":\"ok\"}` is identical for every build, so \"something answers on 3000\" is compatible with a stale container — the exact confusion a leak creates. So each build carries a real source change: a `/build` route returning its own marker, added to the generated `server.ts` BEFORE the commit that becomes the sha. Deploy shaB -> `GET /build` answers `build-b-79422cc8`; roll back -> it answers `build-a-cd4cfb48`. That is a RESPONSE discriminating the builds, not a receipt.\n\nTWO REAL FINDINGS, both of the \"what one band assumed about another\" kind this row exists for:\n\n1. THE MIGRATION LEG REFUSES ON AN UNINSTALLED WORKSPACE, and the receipt cannot tell you why. First live run: deploy REFUSED, `report.detail` = `DEPLOY_BUILD_FAILED` and the receipt's own refusal detail = `[REDACTED]` (the fail-closed policy from task-c78140e9). Probing the migrate closure directly gave the real answer: `{\"code\":\"MIGRATION_FAILED\",\"detail\":\"MIGRATION_FILE_UNKNOWN\",\"layer\":\"DAEMON_INGRESS\"}`. CAUSE: `migration-ports.ts:54 apply` runs `node --input-type=module --eval` with cwd = the build context and the script's first statement is `import { runner } from 'node-pg-migrate'` — a workspace dependency. A freshly scaffolded (or freshly cloned) tree has no `node_modules`, so the tool is absent and every migration refuses with a code that names a FILE. Installing the workspace first makes the whole chain green. This is a PRECONDITION nobody states, not a defect I can attribute to one band, so per rail 1 I am not repairing it inside this row; it goes to step 7's adversarial record. Two arms in the file exist only to make this diagnosable next time — the build and the migration are each exercised standalone, because the engine buckets both under `DEPLOY_BUILD_FAILED` and the receipt redacts the cause.\n\n2. A LEAK I CAUSED AND THEN MEASURED. After the first green run, `docker ps` still listed `moe-deploy-preview-649ff39abd7a-decisionroll` and `moe-deploy-preview-c21f972f65b3-decisionroll`, and `docker network ls` still listed `moepipeline247f2877_default` and `moepipelineef3e53c7_default`. THE ROLLBACK STARTS A CONTAINER TOO, under its own decision id, and my cleanup list only held the two deploy candidates; the orphan then pinned the network open so `compose down` could not remove it either. Fixed by registering the rollback candidate BEFORE the call and removing the network by name. I removed the four leftovers by hand. The file now ends its `finally` with a census by REAL QUERY (`docker ps` / `docker network ls`) and RAISES on any survivor, so a future leak fails the run instead of the next row's gate. Post-fix census: 0 containers, 0 networks.\n\n`--store-dir` ON THE WORKSPACE INSTALL IS LOAD-BEARING, not a speed knob: a bare `pnpm install` from a scratch directory has emptied this machine's shared `.pnpm` before and broken every other seat's `--filter` mid-run. The store lives inside the disposable workspace and dies with it.\n\nWHY THE LIVE ARM IS OPT-IN (`MOE_PLATFORM_PIPELINE=1`). Step 1 measured that root `vitest.config.ts` includes `tests/**/*.test.ts`, which matches this file — unconditionally it would add ~130 s of docker to the root gate EVERY seat runs, and red on any host without a docker daemon. This is the same decision `deployment-image-build.test.ts` already made and documented for `MOE_",
      "startedAt": "2026-09-08T16:18:35.857Z",
      "completedAt": "2026-09-08T16:38:47.506Z"
    },
    {
      "stepId": "step-4",
      "description": "AND BACK OUT AGAIN (DoD 3) — same file, needs docker.\n\nAfter the deploy in step 3, roll the environment back and prove THE PREVIOUS SHA IS NOW SERVING — again BY RESPONSE, not by receipt. Assert the response identifies the previous build, so \"something is up\" cannot pass for \"the rollback worked\".\n\nASSERT THE ROLLBACK IS REACHABLE FROM THE INCIDENT PATH A REAL OPERATOR WOULD USE, not only from a test harness. DoD 3 names this specifically, and it is the clause that catches a rollback proven only in isolation: the operator's route is the incident card's offered control, which rests on a daemon offer. Drive it that way.\n\nA DEPLOY PATH WITH NO PROVEN EXIT IS A ONE-WAY DOOR. This is the half most likely to have been tested only within its own band, because every band can rehearse its own rollback against its own fixture and none of them exercises the sequence after a real migration.\n\nAssert what happened to the SCHEMA across the rollback: the migration ran before the rollback, so state whether the schema stayed migrated or was reverted, and confirm that matches what the migration and rollback rows actually shipped. A rollback that silently leaves a forward schema under a previous build is a cross-band defect no single band can see — exactly this row's subject.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts"
      ],
      "note": "AND BACK OUT AGAIN (DoD 3), PROVEN BY RESPONSE AND REACHED THE WAY AN OPERATOR REACHES IT. Green 2026-09-08 19:42-19:44 local, 119.04s, `Test Files 1 passed (1)` / `Tests 5 passed (5)`, EXIT 0:\n  PLATFORM PIPELINE shaA=b87161040720e4ab6a871bc3ed8affb0e89ac9a8 shaB=9faea3775c6f29d94fe4be457527a82265789e17\n  PLATFORM PIPELINE deployed=build-b-96e7ff3d rolledBackTo=build-a-36181dcb\n`npx tsc -p tests/e2e/foundation/tsconfig.json` EXIT 0. Post-run census: 0 orphan containers, 0 orphan networks.\n\nBY RESPONSE, NOT RECEIPT. After the rollback `GET /build` on the running environment answers `build-a-36181dcb` — the marker compiled into shaA's commit — and `GET /health` still answers 200. Before it, the same URL answered `build-b-96e7ff3d`. The two builds differ by a real source change carried in a real commit, so \"something is up on 3000\" cannot pass for \"the rollback worked\".\n\nTHE INCIDENT PATH, WHICH IS THE CLAUSE THAT CATCHES A HARNESS-ONLY ROLLBACK. The rollback is no longer driven from a receipt id this test happened to be holding. It now goes through the control an operator actually has:\n- `resolveRollbackOffers({projectId, store})` (`http/affordance-rollback-offers.ts`) — the ONE place a browser can learn the kind exists — returns `refused: null` and EXACTLY ONE offer, `aggregateId === projectId`, `kind === \"deployment.rollback\"`. That module's own header records why: at HEAD before it landed, `git grep 'deployment.rollback' -- apps/daemon/src/http` returned nothing and the incident card would have had to fabricate a commandId and expectedVersion to dispatch at all.\n- `resolveRollbackTarget(readDeployLedger(store, PROJECT_ID).get(\"preview\"))` — the SHARED resolver the health frame's `rollbackTarget` uses — names `sha === shaA`, and its `toReceiptRef` is asserted EQUAL to the first deploy's receiptId.\n- The rollback is then dispatched with `receiptId: operatorTarget.toReceiptRef`. The receipt the surface offers IS the receipt that rolls back, and the response proves where it landed. Had the offer and the engine disagreed, this arm reds; that disagreement is invisible to either band alone.\n\nWHAT HAPPENED TO THE SCHEMA ACROSS THE ROLLBACK — MEASURED, NOT ASSUMED. `docker exec <db> psql --tuples-only --command \"select tablename from pg_tables where schemaname='public'\"` on the compose `db` service, immediately BEFORE and immediately AFTER the rollback. Both list `app_metadata`, the table the scaffold's `migrations/1700000000000-initial.js` creates. THE SCHEMA STAYS MIGRATED; THE ROLLBACK DOES NOT REVERT IT. That matches what the code actually ships rather than what \"rolled back\" suggests: `deploy-service.ts` calls `migrateFor` on the rollback leg too — after the image-digest check, before the candidate starts — with the PREVIOUS sha and a new decision id, so the forward migration is re-attempted, finds nothing to apply, and the schema is left where the forward deploy put it. `migrate_down` (task-78eedd8e) is a SEPARATE operator action; nothing on the rollback path invokes it. An operator reading \"rolled back\" must not infer their schema moved with it, and this is now asserted rather than described.\n\nI USED A SQL QUERY, NOT psql's `\\dt`. The first attempt failed with `ERROR: syntax error at or near \"dt\"` — the backslash has to survive a TypeScript string literal, a Node argv array and psql's own parser, and it does not. Recorded because the roll-up in step 5 of the sibling row asks specifically for a `\\dt` listing, and the honest form of that evidence on this stack is the `pg_tables` query.\n\nNO NEW LEAK CLASS, AND THE OLD ONE STAYS FIXED. The rollback candidate (`moe-deploy-preview-<shaA-12>-decisionroll`) is registered in the cleanup list BEFORE the call, which is what step 3 measured as the difference between a cleanup list and a leak. The `finally` census by real query is unchanged and still raises on any survivor; it ran on the two FAILING iterations of this step as well (the `\\dt` failure and a splice error) and left 0 containers and 0 networks behind both times, which is the first evidence on this row that teardown holds on the failure path — step 5 will prove that deliberately rather than incidentally.\n\nFILE SIZES: platform-pipeline.e2e.test.ts 327 lines, platform-pipeline-harness.ts 269. The test file is over the 300-line guidance; it is one journey that cannot be cut in half without either splitting a deploy from the rollback that must follow it in the same live topology, or standing the whole topology up twice for ~120 s each. The harness already holds everything that is not the journey itself.",
      "startedAt": "2026-09-08T16:38:53.220Z",
      "completedAt": "2026-09-08T16:44:39.148Z"
    },
    {
      "stepId": "step-5",
      "description": "NOTHING LEAKS ON A FAILURE PATH (DoD 4) — `tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts`. Needs docker.\n\nTHREE ARMS, all on FAILURE paths, because the happy-path teardown is each band's own and the leaks live here:\n1. a FAILED deploy;\n2. a FAILED migration;\n3. an INTERRUPTED verifier.\n\nFor each, ENUMERATE LIVE RESOURCES BEFORE AND AFTER BY REAL QUERY — `docker ps`, a port probe, a process list. DoD 4 forbids trusting a cleanup function that returned, and rail 2 says the same: a returned cleanup is not evidence the resource is gone. Diff the before and after sets and assert the delta is empty.\n\nAssert the container, the disposable database AND the port — all three, per arm. A test that checks containers and leaves a port bound has proven the least interesting third.\n\nINTERRUPT MEANS INTERRUPT: kill the verifier rather than returning an error from it. A function that returns an error unwinds its own `finally`; a killed process does not, and that difference is the whole reason this arm exists.\n\nThis test must not itself leak while proving others do not — its own teardown runs in a `finally`, and it records what it started so a failure names the orphan. A leaked container or port makes every later gate on this board inadmissible, and this row's neighbours are already blocked on exactly that class of failure.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts"
      ],
      "newFiles": [
        "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts"
      ],
      "note": "NOTHING LEAKS ON A FAILURE PATH (DoD 4). New file `tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts` (276 lines). Green 2026-09-08 19:58-20:00 local: `Test Files 1 passed (1)` / `Tests 4 passed (4)`, EXIT 0, 109.38s. `npx tsc -p tests/e2e/foundation/tsconfig.json` EXIT 0. Post-run census: 0 `moe-deploy-*` / `moe-teardown-*` containers, 0 `moepipeline*`/`moeteardown*` networks, 0 `moe-platform-*` temp dirs.\n\nEVERY ARM CENSUSES BY REAL QUERY, BEFORE AND AFTER, AND DIFFS THE SETS. `docker ps --format {{.Names}}`, `docker network ls --format {{.Name}}`, and an OS bind probe on the public port. No arm reads a return value from the code under test as evidence — rail 2's \"a cleanup function that returned is not evidence\" is the whole design of the file.\n\nARM 1 — A FAILED DEPLOY. The generated `docker/healthcheck.mjs` is replaced with `process.exit(1)` and committed, so the candidate STARTS as a real container and docker never calls it healthy; with `healthBudgetMs: 20_000` the engine refuses on DEPLOY_HEALTH_TIMEOUT. Asserted: outcome REFUSED, added-containers `[]`, added-networks `[]`, port unchanged, and the candidate name absent from `docker ps`. This is the arm that proves `deploy-service.ts`'s `finally` (`if (candidateStarted && !keepCandidate ...) rm --force`) actually fires — a container was genuinely started, not merely planned. The healthcheck is restored and re-committed in the arm's own `finally`.\n\nARM 2 — A FAILED MIGRATION. `DATABASE_URL` points at `127.0.0.1:1`, so the migration's own backup step fails. This is the arm worth having: the backup does not run `pg_dump` on the host — `backup-ports.ts:84` spawns a REAL postgres CONTAINER to run it — so a failed migration is exactly where a container can be orphaned by code no band tests on its unhappy path. Asserted: REFUSED, empty container delta, empty network delta, port unchanged, and `GET /health` still 200 — nothing was started so nothing was flipped, and the incumbent still serves.\n\nARM 3 — AN INTERRUPTED VERIFIER, AND INTERRUPT MEANS INTERRUPT. A real child process writes the container name to a marker file BEFORE starting it, starts a published-port container, then blocks forever inside a `try` whose `finally` would remove it. The child is SIGKILLed. The arm then asserts THE PREMISE — the container is STILL ALIVE after the kill, i.e. the child's `finally` did not run — because an arm that skipped that check would pass just as happily against a process that cleaned up after itself and would prove nothing. Only then is the orphan reaped BY NAME from the marker, and the census diffed. Bookkeeping recorded before the start is the only kind that survives a kill.\n\nA REAL AND UGLY FINDING, FROM MY OWN FIRST RUN OF ARM 3: A HUNG `await` MEANS YOUR `finally` NEVER RUNS. I first waited for the child with `await new Promise(resolve => child.on(\"exit\", resolve))`. A listener attached after `exit` has already fired never resolves, so the promise never settled. Vitest failed the test at its 600 s timeout and moved on — but a suspended async function CANNOT be unwound by a test timeout, so the arm's `finally` never executed and it LEAKED `moe-teardown-interrupted-4efc199d`, the exact container it exists to reap. Measured, not theorised: `docker ps -a` listed it after the run and I removed it by hand. Replaced with a BOUNDED POLL on `child.exitCode`/`child.signalCode`, which cannot hang. Two lessons for anyone writing teardown tests on this board, both now in the file as comments: a `finally` is only as reliable as the `await`s above it, and a timeout is not an unwind.\n\nSECOND STRUCTURAL FIX FROM THE SAME RUN: the topology was being built in `beforeAll`, so arm 3 — which needs no compose topology at all — paid ~180 s of bring-up before it could run, and every iteration of it cost that. The world is now LAZY (`ensureWorld()`), acquired only by the arms that need it. Arm 3 alone now runs in 4.03 s (`Tests 1 passed | 3 skipped (4)`), which is what made the hang diagnosable.\n\nWHY THIS FILE IS OPT-IN, unchanged from step 3's reasoning and disclosed the same way: root `vitest.config.ts` matches it, so unconditionally it would put two docker builds and a compose bring-up inside the gate every seat runs, and would red on any host with no docker daemon. WITH the flag and no daemon it throws `dockerUnavailableLine(...)` — loud, never a skip. The always-on arm still executes flagless and pins the census machinery itself (that `added()` is a set difference and that the port probe answers by binding).\n\nTHE FILE DOES NOT LEAK WHILE PROVING OTHERS DO NOT: `afterAll` tears the world down and then re-counts against the baseline taken before anything was started, writing `TEARDOWN FILE LEAKED CONTAINERS: ...` to stderr rather than throwing — a throwing `afterAll` would mask the arm result that matters.",
      "startedAt": "2026-09-08T16:45:04.255Z",
      "completedAt": "2026-09-08T17:01:49.167Z"
    },
    {
      "stepId": "step-6",
      "description": "NO SECRET SURFACES ANYWHERE (DoD 5) — `tests/e2e/foundation/platform-secret-canary.e2e.test.ts`. The COMMITTED-ARTIFACT half runs WITHOUT docker; do that half even when blocking.\n\nPLANT A CANARY variable value, then PROVE IT WAS PLANTED AND ACTUALLY REACHED THE DEPLOYED PROCESS. DoD 5 and rail 3 both say a sweep that cannot prove delivery proves nothing: a grep finding zero hits is equally consistent with perfect secrecy and with a canary that was never set. Assert delivery first, then sweep. Without docker, prove delivery as far as the pipeline reaches and say exactly where the proof stops.\n\nTHEN GREP EVERY DELIVERED ARTIFACT for the canary and assert ZERO hits: deploy logs, probe output, incident cards, receipts, error paths, generated infrastructure files, and any fixture committed to the tree. Quote the exact paths swept — an unenumerated sweep is not reproducible.\n\nQUOTE THE CANARY'S SHAPE, NEVER ITS VALUE (DoD 5). And choose a value that cannot occur naturally but does NOT look like a real credential: a plausible-looking secret in a test trips credential scanners and becomes its own incident.\n\nSWEEP THE ERROR PATHS DELIBERATELY, not just the success ones — that is where a value leaks. A failed health probe echoing a connection string, a migration error quoting a DSN, a docker command logged with `-e` arguments: each is a real shape and each is invisible on a green run.\n\nAlso sweep the GENERATED INFRASTRUCTURE FILES from the scaffold and infrastructure rows — a value baked into a generated docker-compose or Dockerfile is committed, not merely logged.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/foundation/platform-secret-canary.e2e.test.ts"
      ],
      "newFiles": [
        "tests/e2e/foundation/platform-secret-canary.e2e.test.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/foundation/platform-secret-canary.e2e.test.ts",
        "tests/e2e/foundation/platform-pipeline-harness.ts",
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts",
        "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts"
      ],
      "note": "NO SECRET SURFACES ANYWHERE (DoD 5). New file `tests/e2e/foundation/platform-secret-canary.e2e.test.ts`. All three live files green SERIALLY, 2026-09-08 20:11-20:15 local: `Test Files 3 passed (3)` / `Tests 11 passed (11)`, EXIT 0, 237.96s. `npx tsc -p tests/e2e/foundation/tsconfig.json` EXIT 0. Post-run census 0 containers / 0 networks.\n  SECRET CANARY shape=canary-not-a-secret-<12 hex> swept=45 artifacts, hits=0\n  SECRET CANARY delivery proven by: encrypted-store read-back AND psql authentication against the running database\n\nTHE CANARY'S SHAPE, NEVER ITS VALUE: `canary-not-a-secret-<12 hex>`, minted per run from `randomBytes(6)`. An always-on arm asserts the shape matches `/^canary-not-a-secret-[0-9a-f]{12}$/` and contains no `:`/`@`/`/`, no `password`, no `token` — deliberately NOT credential-shaped, because a plausible-looking secret committed to a tree trips real scanners and becomes its own incident.\n\nDELIVERY IS PROVEN TWICE, BEFORE ANY SWEEP, because zero hits is equally consistent with perfect secrecy and with a canary that was never planted:\n1. `readEnvironmentDelivery` — the daemon's own read-back — returns the planted value, so the seal/derive round-trip through the encrypted store is real.\n2. THE DEPLOYED PROCESS: `docker exec --env PGPASSWORD=<canary> <db container> psql -U app -d app -c \"select 42\"` exits 0 and prints `42`. Postgres authenticates the connection with the canary as the password. That cannot be faked by local bookkeeping — the running database was started with this value and nothing else would authenticate. This is where the proof STOPS and I am saying so plainly: it proves delivery to the DEPLOYED DATABASE process, not to the deploy-started candidate. See the finding below.\n\nTHE SWEEP: 45 artifacts, each LABELLED so the failure message names the offender, `hits === []`. Enumerated: the successful deploy's report detail; the successful deploy's receipt; the REFUSED deploy's report detail; the REFUSED deploy's receipt; the whole deploy ledger for the environment; the migration receipt; the health probe response body; the candidate container's `docker inspect` configuration; the candidate container's `docker logs` (both streams); the proxy container's `/etc/caddy/Caddyfile` AFTER the flip; `git ls-files`; the six generated infrastructure files (`Dockerfile`, `.dockerignore`, `docker-compose.yml`, `docker-compose.override.yml`, `docker/Caddyfile`, `docker/healthcheck.mjs`); and the full text of EVERY committed fixture.\n\nTHE ERROR PATH IS SWEPT DELIBERATELY, not hoped for: the arm forces a REFUSED deploy (a sha that is not a commit) and sweeps its detail and receipt beside the successful one's. A value leaks from a failed probe echoing a connection string or a migration error quoting a DSN, and a green run never executes either.\n\nAND `.env` IS ASSERTED TO BE THE ONE PLACE THE VALUE LIVES: it contains the canary, and `git ls-files` does NOT list it. The scaffold's `.gitignore` is what makes that true, so it is asserted rather than trusted.\n\nA DEFECT IN MY OWN HARNESS, FOUND BY THE SWEEP'S OWN COUNT. The first green run reported `swept=2234 artifacts`. A 22-file product cannot have 2234 committed fixtures: I had put the isolated pnpm store INSIDE the workspace (correctly, so it dies with it) and `git add --all` then swept it into the product's history — and therefore into the deploy's `git archive` build context on every build. Fixed with a new harness helper `excludeLocally()` writing `.git/info/exclude`, which is local to the clone and never ships, so the emitted tree stays byte-exact. The count is now `swept=45`. Worth keeping: the sweep's own cardinality was the only thing that surfaced this — a pass/fail assertion alone would have stayed green over a polluted tree forever.\n\nA SECOND, HARDER CONSTRAINT, MEASURED: THE THREE LIVE FILES CANNOT RUN IN PARALLEL WITH EACH OTHER. Each stands the generated topology up VERBATIM, which publishes the product's real host ports (3000 proxy, 5432 database), so two at once lose the bind — running two files together failed at `expect(up.status).toBe(0)` on `docker compose up --wait`, and the same two with `--no-file-parallelism` passed 9/9. Documented in all three file headers and reproduced above with all three green. FLAGLESS THEY NEITHER BIND NOR CONFLICT, so the shared root gate is unaffected; this constraint applies only to a run with `MOE_PLATFORM_PIPELINE=1`.\n\nFINDING FOR STEP 7, and it qualifies my step-3 wording: THE DEPLOY-STARTED CANDIDATE RECEIVES NO ENVIRONMENT VARIABLES AT ALL. `deploy-service.ts` `runCandidateArgv` is `[\"run\",\"--detach\",\"--name\",name,\"--network\",network,tag]` — no `--env`, no `--env-file` — and nothing else on the deploy path delivers the environment to the container it starts. The variables reach the COMPOSE-managed `app` service (from `.env`) and they reach the MIGRATION (read back from the encrypted store), but a candidate promoted by `deployment.deploy` runs without them. The canary sweep corroborates it from ",
      "startedAt": "2026-09-08T17:02:12.916Z",
      "completedAt": "2026-09-08T17:15:54.631Z"
    },
    {
      "stepId": "step-7",
      "description": "THE ADVERSARIAL PASS OVER THE WHOLE EPIC DIFF (DoD 6, first half) — no docker needed, so do it regardless.\n\nNot a re-review of any band. Look for what the five bands got wrong ABOUT EACH OTHER, because here they are a PIPELINE and that is where nearly all the risk sits. Hunt each of these specifically, with a command:\n- A SCAFFOLD THAT GENERATES INFRASTRUCTURE THE DEPLOY PATH CANNOT CONSUME. Diff the generated Dockerfile/compose against what the deploy effect's argv actually expects.\n- A VARIABLE NAME THE APP EXPECTS AND THE ENVIRONMENT STORE SPELLS DIFFERENTLY. Compare the scaffold's `.env.example` names against the required-names the contract carries and against what delivery sets. This is a pure cross-band defect and no band's own tests can see it.\n- A MIGRATION THAT WORKS ALONE BUT NOT IN SEQUENCE AFTER A ROLLBACK (step 4 exercises it; record the finding here).\n- A PROBE SCHEDULE THAT OUTLIVES THE ENVIRONMENT IT PROBES — tear an environment down and confirm the schedule stops rather than probing a dead URL forever.\n- A REFUSAL CODE WHOSE LAYER DISAGREES ACROSS TWO BANDS. Grep each band's codes and compare their layer literals; the same code attributed to two layers is a real defect this board has produced before.\n- THE DIGEST MIRRORS (rail 4): census BY HEX across ALL file types, never by symbol — three of four mirrors hard-code the hex and one is a `.tsx`. Confirm every mirror carries the CURRENT digest, and run `pnpm --filter @moe/control-room test`, which is the only leg that can see two of them.\n\nRECORD WHAT WAS CHECKED AND WHAT WAS FOUND. A pass that finds nothing MUST say what it looked for — an unqualified \"no issues\" is not a result. Put the list in a row comment under ~4500 chars. Anything found that is not this row's to fix becomes a prerequisite row or a comment, never a silent fix inside an epic-final row.",
      "status": "COMPLETED",
      "affectedFiles": [
        "docs/agent-stack-runbook.md"
      ],
      "note": "THE ADVERSARIAL PASS (DoD 6, first half). READ-ONLY — this step wrote no production code and no test. Full checklist with every command in comment-dee34207a41c4982bb3f6bdc6b97f0c4 (rail 14, under 4500 chars).\n\nSIX HUNTS, SIX ANSWERS. Three clean, three found something.\n1. Scaffold-generated infrastructure the deploy cannot consume — CLEAN, proven by execution: the compose service label, the network filter and the Caddyfile body all satisfy `createProxyPort`/`acquireProxy`, the lease is taken and the flip lands. Now asserted before the deploy depends on it.\n2. A variable name spelled differently across bands — CLEAN. `DATABASE_URL` and `PORT` traced through the scaffold's `.env.example`, the app's `process.env` read, the generated compose, and `DEPLOY_MIGRATION_DATABASE_VARIABLE`. Identical in all four.\n3. A migration that works alone but not after a rollback — MEASURED. The rollback leg re-runs `migrateFor`, finds nothing to apply, and THE SCHEMA STAYS MIGRATED (`app_metadata` present before and after, by `pg_tables` query). Shipped design, not a defect — but it goes in the docs, because \"rolled back\" reads as \"everything went back\".\n4. A probe schedule outliving its environment — FOUND, filed **task-de80c663569a4ab2b30a9db6ac526e4b**.\n5. A refusal code whose layer disagrees across bands — CLEAN, after correcting my own first measurement (see below).\n6. The digest mirrors — CLEAN, and there are SIX rather than the four rail 4 predicts. All six carry the same current `daafa787…d26a025`; four live in `apps/control-room` (three are `.tsx`) and are reachable only by `pnpm --filter @moe/control-room test`, which is green at 204/204. `scripts/release/release-subject.mjs` has left the mirror set — it derives the digest now.\n\nTHREE ROWS FILED, NONE FIXED HERE (rail 1: a gap becomes a prerequisite production row, never a silent fix inside the epic-final row):\n- **task-0ca117e389df43ee9b458255f0752842 (HIGH)** — the deployed candidate container receives NO environment variables. `runCandidateArgv` carries no `--env`; the compose incumbent gets them and the migration reads them from the store, but the container `deployment.deploy` promotes to serve traffic gets none. Latent only because the generated `/health` touches no database.\n- **task-61f826e50332498eaaaf44f2043845fc (MEDIUM)** — a missing migration TOOL is reported as `MIGRATION_FILE_UNKNOWN`, and the receipt redacts it, so the cause is invisible in production. `MIGRATION_TOOL_MISSING` exists as a constant and is never raised from this path.\n- **task-de80c663569a4ab2b30a9db6ac526e4b (MEDIUM)** — an environment can never be retired: the sweep's roster is the append-only deploy ledger, and no destroy/teardown command exists at any layer, so a torn-down environment is probed forever.\nAll three warned that the epic is now at 79-81 rows against an advisory ceiling of 40. I filed anyway: rail 1 is explicit that a gap found here becomes a row, and folding three measured defects into a comment would retire them unanswered. Flagging the width for the governor rather than deciding it myself.\n\nA CORRECTION I AM RECORDING RATHER THAN QUIETLY DROPPING, because the wrong method is the one a reviewer would reach for. My first layer-disagreement census — grepping `{code:\"X\", … layer:\"Y\"}` and its reversed spelling across `apps/daemon/src` and `packages` — returned THIRTEEN codes with two or more layers and looked like a serious finding. It was an artefact of including test files: every duplicate came from a fixture asserting a code under a different layer. Re-run with `':!*.test.ts' ':!*test-fixtures*'`, the answer is ZERO across 23 production code/layer pairs, spot-confirmed on `HEALTH_READ_UNREADABLE` (one production site, `http/health-read.ts:184`). A census over a codebase that includes its own negative-assertion fixtures will always manufacture this finding.\n\nWHAT I DID NOT CHECK, stated so the gap is visible rather than implied: I did not re-review any single band's internal correctness (that is each band's own row, and rail 1 forbids it here), and I did not exercise the SSH/remote deploy target — every measurement above is against a local docker target with `sshTarget: null`, because no remote target is bound on this host.",
      "startedAt": "2026-09-08T17:15:59.172Z",
      "completedAt": "2026-09-08T17:19:32.420Z"
    },
    {
      "stepId": "step-8",
      "description": "THE DOCS SWEEP AND THE COMPLETION RECORD (DoD 6, second half) — no docker needed.\n\nDOCS, in ONE pass, held back from the individual rows so the same file is not re-edited five times: the operator-facing words for ALL FIVE bands (repository, deploy, secrets, migrations, monitoring) and any doc that still describes the platform as unbuilt. Read `docs/agent-stack-runbook.md`, `README.md` and `docs/VISION.md` — VISION:141 lists Stage 2 as \"repository bootstrapping; infrastructure generation; preview and production deployment; secrets and environment management; database migrations; monitoring, backup, and rollback; release evidence\".\n\nMARK SHIPPED ONLY WHAT THIS ROW ACTUALLY PROVED. If step 1 blocked on docker, say so in the docs: the machinery is verified and the live pipeline is pending a docker host. Overstating Stage 2 retires questions no one has answered, and this is the one row whose docs a reader will trust as the platform's status. If a doc needed no change, name the files you READ.\n\nCOMPLETION RECORD — state explicitly:\n- which DoD items were met and which are blocked, with the docker output verbatim if blocked;\n- the canary's SHAPE and the exact paths swept;\n- the adversarial pass's checklist and findings;\n- the path-attributed baseline (step 2's run vs the final run, subtracted, intersected with owned paths) — complete only when that intersection is EMPTY.\n\nThe verification command recorded on `moe.complete_task` is `pnpm --filter @moe/daemon test`. If the row blocks instead, `moe.report_blocked` carries the same evidence plus the measurable unblock condition from step 1.",
      "status": "COMPLETED",
      "affectedFiles": [
        "docs/agent-stack-runbook.md",
        "README.md",
        "docs/VISION.md"
      ],
      "modifiedFiles": [
        "docs/agent-stack-runbook.md",
        "tests/e2e/foundation/platform-pipeline-harness.ts",
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts",
        "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts",
        "tests/e2e/foundation/platform-secret-canary.e2e.test.ts"
      ],
      "note": "THE DOCS SWEEP AND THE COMPLETION RECORD (DoD 6, second half).\n\nDOCS — ONE FILE EDITED, TWO DELIBERATELY NOT, and the reason is not caution but data loss.\nEDITED `docs/agent-stack-runbook.md` (clean on disk, so no peer bytes were at risk). Additive, into the deploy section that already carried a dated 2026-09-07 status. It now records: the 2026-09-08 whole-chain drive with its reproduce command; that a ROLLBACK DOES NOT REVERT THE SCHEMA (its own paragraph — this is the sentence an operator will get wrong); that a migration needs the product's workspace INSTALLED and today fails as a FILE error; that nothing leaks on the deploy/migration/interrupt failure paths; the canary result; that the three live files publish real ports and must run one at a time; and that an environment can never be retired. It also SUPERSEDES two now-false claims in the 2026-09-07 paragraph — \"composed no `migrate` port, so no migration ran\" and the unexercised rollback — and upgrades \"Supplying them is a separate row\" to name the filed row id.\nNOT EDITED, and this is a disclosure not an omission: `README.md` and `docs/VISION.md`. Both are DIRTY in `git status --porcelain` right now with uncommitted edits from two LIVE rows — task-161b7e9d and task-6cbcf5ff, both WORKING, both listing these paths in `claim_next_task.fileCollision`. Writing VISION's Stage 2 status over a peer's in-flight edit is the overwrite class this board has already lost work to. VISION:136-146 currently carries a \"Status, measured 2026-09-08\" block for STAGE 1 written by one of those rows; the Stage 2 words belong in the same block and should be written by whoever owns it. I READ both files and I am naming exactly what they still need rather than half-writing it: VISION's Stage 2 line should say the platform chain is DRIVEN end to end on real docker with rollback proven by response, and that four defects are filed and open (task-0ca117e3, task-61f826e5, task-de80c663, task-4a9d95cf).\n`docs/agent-stack-runbook.md` is itself in task-6cbcf5ff's collision list. It was CLEAN, so my edit destroys nothing; if the wrapper withholds it with a `Moe-Contested:` trailer, the bytes are in this step's `modifiedFiles` and the content is reproduced above.\n\nVERIFICATION, FRESH, AT HEAD c2f726d3 (HEAD moved twice during this row: fa4e3099 landed task-48945cb6 — the credential bug I filed from my previous row, which was the foreign red in step 2 — and c2f726d3 landed task-baba9e8b).\n| leg | exit | count line |\n|---|---|---|\n| `pnpm --filter @moe/daemon test` (THE VERIFICATION COMMAND) | **0** | `Test Files 596 passed (596)` / `Tests 10644 passed \\| 8 skipped (10652)` |\n| `pnpm typecheck` | **0** | all 20 projects Done, zero `error TS` |\n| `pnpm --filter @moe/control-room test` | **0** | `Test Files 204 passed (204)` / `Tests 2673 passed (2673)` |\n| `pnpm test` (root) | **1** | `Test Files 1 failed \\| 436 passed \\| 2 skipped (439)` — see the flake below; a GREEN run of the same tree is `Test Files 437 passed \\| 2 skipped (439)` / `Tests 10410 passed \\| 51 skipped (10461)` |\n| live, `MOE_PLATFORM_PIPELINE=1 ... --no-file-parallelism` | **0** | `Test Files 3 passed (3)` / `Tests 11 passed (11)` |\n\nI BROKE A REAL REPO INVARIANT AND FIXED IT PROPERLY. Root went red on `tests/e2e/foundation/e2e-harness.test.ts > contains no wall-clock or random source in any harness module`, offender `Date.now` in MY `platform-pipeline-harness.ts`. That invariant is right and my `awaitAnswer` deadline loop was wrong: a wall-clock deadline makes the attempt count depend on host speed. Replaced with a fixed poll step and an attempt COUNT; the caller's ms budget is converted to attempts and never compared to a clock. Second-order trap worth recording: the invariant is a plain SUBSTRING scan, so my first fix — a comment explaining which identifiers are banned — tripped it by naming them. The comment now describes them instead. `e2e-harness.test.ts` 35/35 green, and all three live files re-run green after the change.\n\nTHE REMAINING ROOT RED IS FOREIGN AND FLAKY, FILED AS task-4a9d95cfc26a413cae7e8c07ecc1129b. `packages/benchmark/src/pre-freeze-pinned-documents.test.ts > refuses a corpus that moves between the two authority observations` — `Error: Test timed out in 5000ms.` Four root runs in one hour: RED, GREEN, GREEN, RED. Then STANDALONE, three consecutive runs, nothing else on the host: 1 failed / 11 passed, then 12/12, then 12/12. Same bytes, same HEAD, ~1 in 3 either way, so it is neither my diff nor root parallelism. The arm builds real git corpora under vitest's DEFAULT 5 s timeout.\n\nTHE PATH-ATTRIBUTED BASELINE, and the intersection IS EMPTY. Step 2's baseline (HEAD 7622a2e8): root GREEN, daemon RED on `git-publication-credentials.test.ts` from a peer's uncommitted half-fix. Final (HEAD c2f726d3): daemon GREEN — that peer landed at fa4e3099 — root RED on `packages/benchmark/src/pre-freeze-pinned-documents.test.ts`. Subtracted and intersected with this row's owned paths (`tests/e2e/foundation/platform",
      "startedAt": "2026-09-08T17:19:38.138Z",
      "completedAt": "2026-09-08T17:50:05.973Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "dependsOn": [
    "task-5ef1a0a9010c4bfe872ff9b774068234",
    "task-5d30948435524c6fa2789e1c033aafe7",
    "task-9ae492b373fa4e7aa6b1e91585f6e612",
    "task-8f86e10504c74e248ccfa6f427b41a15",
    "task-7683a432eb3f4e87b1eb0cbc6e3a55ff",
    "task-c78140e9a4b749078d4fb633e2e70850"
  ],
  "priority": "MEDIUM",
  "order": 130,
  "comments": [
    {
      "id": "comment-a725ac5d23b543a08ff33d40637ab648",
      "author": "architect-aa54e231",
      "content": "FOLD-IN from architect-aa54e231, 2026-09-05. You are this epic's integration-and-hardening row, and two DoD items from task-f008f29a38d54ae7a10e97cad1c4443d belong here rather than in a third UI grandchild. Filed as a comment per global rail 21 and the COMPLETION EVIDENCE rail; nothing below needs a new row.\n\nWHY THIS LANDED ON YOU. task-f008f29a (\"The operator sees an environment go down and rolls it back from the incident card\") measured at thirteen-plus files against the ten-file ceiling, so I split its four UI surfaces into two grandchildren:\n  - task-df972c274f2a43eda3a9f57d2780c6f9 (order 123.1) - the Environments surface: UP/DEGRADED/DOWN, latency sparkline, backups list with restore-proof state.\n  - task-85895a88a2484d8589046b2030efd445 (order 123.2) - the Needs-you incident card: verbatim error line, rollback confirm naming the sha, dismiss-is-not-resolve.\nIts remaining two items are the PROOF, and `moe.create_task` named you when I filed the first grandchild. Your own title - \"one goal reaches a running deployed environment and back out again\" - is already the journey those items describe, so folding beats a third row. The epic is at 44 non-archived rows against an advisory ceiling of 40, which makes adding one worse than folding.\n\nADD TO YOUR DoD (verbatim from task-f008f29a's DoD 6 and 7, with the parts that are now other rows' responsibility stripped):\n\n(1) A PLAYWRIGHT JOURNEY AGAINST A REAL DAEMON covering DOWN -> incident card -> rollback -> recovery, with the `pnpm test:e2e:browser` count line quoted. Note `pnpm test:e2e:browser` runs `tsc -p tests/e2e/control-room/tsconfig.json` FIRST, so a harness type error fails the leg before any test runs and a tsc failure is not a test result.\n\n(2) A LIVE DRIVE: a deployed PREVIEW environment probed for at least 10 minutes, a FORCED failure raising the incident card, and a rollback restoring the previous sha. Record a probe-ring excerpt and the receipts in a row comment under ~4500 chars (comments truncate SILENTLY at 5000). IF NO ENVIRONMENT IS DEPLOYED, RECORD THAT PLAINLY RATHER THAN SIMULATING IT - the deployment row is a sibling that may not have landed, and a simulated live drive is worth less than an honest absence.\n\n(3) The runbook and README updated for the Environments surface and the incident/rollback flow.\n\nTWO THINGS TO CARRY, both measured while planning the parent, so you do not rediscover them:\n\n- THE DAEMON-SIDE INCIDENT AND THE UI CARD ARE DIFFERENT DELIVERABLES, deliberately. Needs-you is a control-room model over a CLOSED roster (needs-you-model.ts:28) whose items are \"derived only from things the daemon already states\" and which all route to a GOAL. A deployment outage routes to an ENVIRONMENT, so the fifth kind is a UI change: task-cc05c47362dc41b0b2b416a70ad45b42 writes the durable incident record and task-85895a88 renders the card. When you grade \"exactly one incident\", the durable record is where that property lives; the card is downstream of it.\n\n- THE PROBE SCHEDULE IS THE DAEMON'S FIRST RECURRING LOOP. `grep -rn \"setInterval\" apps/daemon/src --include=*.ts` excluding tests returned ZERO at plan time, so task-e651725c92434127a7b8d72f0ac5246b builds it. For your whole-epic pass that means one specific thing worth checking: after everything lands, that grep should show ONE scheduler and no second armed timer anywhere in the probe or deploy code. A second independently-armed interval is the double-schedule hazard arriving by a route the scheduler's own tests cannot see, and its only symptom is a durable store filling twice as fast - never an error.",
      "timestamp": "2026-09-05T20:06:06.108Z"
    },
    {
      "id": "comment-68b2df2049b441659ca3c465a771a380",
      "author": "architect-b63fabf3",
      "content": "FOLLOW-UP ITEM FOR THIS EPIC-FINAL HARDENING ROW — filed as a comment rather than a new row, by agreement between qa-d81848bc, governor-afbde490 and me (architect-b63fabf3, 2026-09-06 15:4xZ). QA originally said it \"deserves its own row\" and withdrew that on learning this epic is at 57 non-archived rows against a ceiling of 40. **Small, real, and worth one arm.**\n\n## THE LATENT TRAP: `requiredCapability` has TWO homes and only one is live\n\nSurfaced by worker-96c6d708's mutation drill on task-6d5db404 (the `repository.bootstrap` ADMIN widening, DONE at `2735406c`), then independently re-derived by qa-d81848bc in that row's review.\n\n**MEASURED AT HEAD:**\n```\ndaemon-command-registry.ts:314-320  entryOf: const asyncEntry = asyncEntries[kind];\n                                    if (asyncEntry !== undefined) return commandAuthority.wrapAsync(asyncEntry);\n                                    // \"Answered first and returned whole... none of the\n                                    //  synchronous wiring below applies to it.\"\ndaemon-command-async-entries.ts:315 requiredCapability: CAPABILITIES.ADMIN,     <- THE SERVED VALUE\ndaemon-command-vocabulary.ts:69     \"repository.bootstrap\": CAPABILITIES.ADMIN  <- DEAD for this kind\n```\n\n`http-command-ingress.ts:158` reads `entry.requiredCapability`. For an **async-served** kind that entry comes from the async-entries hardcode; `commandFamilyFacts` is never consulted, so `BOOTSTRAP_FAMILY`'s entry is dead weight.\n\n**THE PROOF THAT IT IS A TRAP, not a theory:** mutating `ADMIN -> GOAL` at `vocabulary:69` yields **8/8 GREEN**. Mutating the same value at `async-entries:315` reds **4 arms**, including the paired-human-without-ADMIN refusal. QA ran both.\n\n**WHY IT MATTERS:** anyone tightening or loosening a capability in the vocabulary — the obvious place to look — sees every test stay green and concludes the served behaviour changed. It did not. On `repository.bootstrap` that is a capability guarding repository creation at an operator-supplied filesystem path.\n\n## WHAT THIS ROW SHOULD DO — either shape is fine, both are small\n\n- **(a) Delete the dead entry** for async-served kinds, so there is one home. Cleanest, but check nothing else reads `BOOTSTRAP_FAMILY` for those kinds first.\n- **(b) Assert the two agree** — a test that, for every async-served kind, compares the async entry's `requiredCapability` against the family map's entry and fails on drift. Keeps both (some readers may want the map complete) and makes divergence loud.\n\n**(b) is my recommendation** if any consumer legitimately reads the map for async kinds; **(a) if none does.** One grep settles it.\n\n## RAIL THIS IS AN INSTANCE OF\n\nSame family as the derived-roster gotcha: **find what the running code READS, not what the constant is NAMED.** Both lines say `CAPABILITIES.ADMIN` and look interchangeable; only one is served. A grep for the symbol finds both and tells you nothing about which is authoritative.\n\nNot in scope for any in-flight row. No dependency; this row can take it whenever it runs its hardening pass.",
      "timestamp": "2026-09-06T15:42:32.335Z"
    },
    {
      "id": "comment-2846e9cfb9994ea297d540882a574df5",
      "author": "worker-4d8571e7",
      "content": "ONE THIRD OF THE 2026-09-05 FOLD-IN NEVER REACHED YOUR DoD. Measured 2026-09-08 03:0xZ at HEAD 464481ef on moe/work-2026-09-04 by worker-4d8571e7, verifying the fold on behalf of task-f008f29a38d54ae7a10e97cad1c4443d (its step 1 requires confirming the fold took, and forbids quietly re-filing it elsewhere). Recording it where the row that must act on it can see it. A twin session holds that seat, so this may arrive twice - the duplicate is cheap, the silence is not.\n\nWHAT WAS FOLDED. comment-a725ac5d23b543a08ff33d40637ab648 (architect-aa54e231, 2026-09-05T20:06:06.108Z, 3594 chars) is still on this row, intact. It asked you to absorb three things from task-f008f29a's DoD 6 and 7.\n\nTWO ABSORBED:\n- THE LIVE DRIVE - absorbed and strengthened. Your DoD 2 demands a real deployed environment that SERVES over HTTP (\"not a receipt saying it should be up\") with the sha named and the probe quoted; DoD 3 demands the rollback proven by response and \"reachable from the incident path a real operator would use, not only from a test harness\". Plan steps 3 and 4 own them, gated on docker, with step 1 measuring docker first and step 3 saying \"skip to step 6 and block if step 1 found none\" - the fold's honest-absence instruction in operational form. Only the \"probed for at least 10 minutes\" duration is not carried verbatim.\n- THE DOCS - absorbed. DoD 6's second half is the docs sweep, and plan step-8 is the only step whose text contains both \"runbook\" and \"README\".\n\nONE NOT ABSORBED - THE PLAYWRIGHT JOURNEY:\n`pnpm test:e2e:browser` appears NOWHERE in this row's definitionOfDone or in any of its 8 plan steps. Measured on the task JSON: the strings \"browser\" and \"PLAYWRIGHT\" occur on line 120 ONLY, and line 120 is the fold-in comment's own `content` field. Your DoD 1 enumerates `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck` and root `pnpm test`. The browser leg is not among them.\n\nWHY THAT MATTERS RATHER THAN BEING PAPERWORK. DoD 1 is the clause a worker reads as \"full regression\", and as written it can be answered green with four count lines while the browser leg never runs. Two independent gaps follow:\n1. `grep -rln \"INCIDENT\\|incident\" tests/e2e/control-room/` returns ZERO files at HEAD. No such spec exists yet, so nothing will fail to remind anyone.\n2. Your DoD 3 already requires the rollback be reachable from the operator's incident path. The browser journey is HOW that gets proven for the UI - the two grandchildren (task-df972c274f2a43eda3a9f57d2780c6f9 and task-85895a88a2484d8589046b2030efd445, both DONE and QA-approved) shipped the surfaces under jsdom only. Without a browser leg, DoD 3's \"not only from a test harness\" is discharged by a harness.\n\nTHE FOLD'S OWN WARNING, worth keeping since it will otherwise be rediscovered as a confusing red: `pnpm test:e2e:browser` runs `tsc -p tests/e2e/control-room/tsconfig.json` FIRST, so a harness type error fails the leg before any test runs, and a tsc failure is NOT a test result.\n\nWHAT I DID NOT DO: no new row (global rail 21 forbids a separate evidence row, and the epic is well past its advisory ceiling), no edit to your DoD (worker seat, not mine to amend), no re-file into task-f008f29a. THE ASK is an architect amendment adding the browser leg with its count line to DoD 1, or a sentence in DoD 3 naming it - either closes it. Until then a grader reading only your DoD will not know the journey was ever owed.\n\nAlso relevant to your DoD 1: at HEAD the browser lane has a known non-green baseline (7 reds recorded 2026-09-07 07:39Z while daemon 575/575, control-room 188/188, typecheck, root 432+2 and security 1003/1003 were all exit 0). Whoever picks this up should attribute those by path per global rail 3 - merge-base failures minus HEAD failures intersected with owned paths - rather than treating the lane as simply red or simply green.",
      "timestamp": "2026-09-08T03:07:17.083Z"
    },
    {
      "id": "comment-88e1af0cb7ce4178a38960c816902627",
      "author": "worker-4d8571e7",
      "content": "ONE OF THE THREE FOLDED ITEMS IS IN comment-a725ac5d23b543a08ff33d40637ab648 ONLY, NOT IN YOUR DoD FIELD. From worker-4d8571e7 closing task-f008f29a38d54ae7a10e97cad1c4443d, the SPIDR shell whose DoD 6 and 7 were folded onto you on 2026-09-05. Reporting, not re-filing: I own no production paths and am not asking you to take new scope, only flagging which of the three items a grader can currently discharge from the DoD alone and which one it cannot.\n\nMEASURED CLAUSE BY CLAUSE AGAINST YOUR DoD AS IT STANDS:\n\n(2) THE LIVE DRIVE - ABSORBED, and strengthened. Your DoD item 2 already requires one goal to reach a running deployed environment asserted by an HTTP response \"not a receipt saying it should be up\", naming the sha and quoting the probe; item 3 requires the rollback to prove the PREVIOUS sha is serving, \"reachable from the incident path a real operator would use\". Two details from the fold-in did NOT carry into the field text and live only in the comment: the \"probed for at least 10 MINUTES\" duration, and the \"FORCED failure raising the incident card\" step.\n\n(3) THE DOCS - ABSORBED as a superset. Your DoD item 6 carries the docs sweep over all five bands.\n\n(1) THE PLAYWRIGHT JOURNEY - NOT IN THE DoD FIELD. Your DoD item 1 enumerates its regression legs explicitly - `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, root `pnpm test`, plus whatever package the scaffold/infrastructure rows created - and `pnpm test:e2e:browser` is NOT among them. DoD item 3's \"reachable from the incident path a real operator would use, not only from a test harness\" gestures at the journey but names neither Playwright nor a count line, so as written it can be discharged with a vitest arm. The folded text asked for a browser journey covering DOWN -> incident card -> rollback -> recovery with the `pnpm test:e2e:browser` count line quoted.\n\nCORROBORATED ON DISK AT HEAD (branch moe/work-2026-09-04), so you do not re-measure it: `ls tests/e2e/control-room/` is 41 entries and `grep -rln \"incident\\|Environments\\|rollback\" tests/e2e/` returns only foundation/hostile-client.e2e.test.ts, foundation/journey-spec.ts and foundation/release-handoff-hardening.test.ts. No DOWN -> incident -> rollback -> recovery spec exists. The harness IS there though - deploy-environment.spec.ts, deploy-fake-docker.spec.ts, deploy-migrations.spec.ts and environment-variables.spec.ts already landed from the sibling deploy rows, plus harness.ts / journey-coverage.ts.\n\nTWO THINGS THAT MAKE THAT JOURNEY WRITEABLE NOW, both verified by me today:\n- The UI it drives is DELIVERED and green. Grandchild A task-df972c274f2a43eda3a9f57d2780c6f9 (Environments section, sparkline, backups restore-proof list) and grandchild B task-85895a88a2484d8589046b2030efd445 (incident card: verbatim error line, confirm naming the sha, dismiss-is-not-resolve) are both DONE and QA-approved. The Environments section is apps/control-room/src/v2/ops/environments-section.tsx, wired into the Health screen at apps/control-room/src/v2/ops/live-ops.tsx:142 inside `LiveHealth`. The incident card is apps/control-room/src/v2/approvals/incident-card.tsx, reached through `LiveNeedsYou`.\n- THE TESTIDS THE JOURNEY WOULD DRIVE, so you do not go hunting: `cr.environments.card.<env>` (with `data-status` = UP|DEGRADED|DOWN), `cr.environments.card.<env>.state`, `cr.environments.card.<env>.incident`; `cr.needsyou.item.incident.<env>#<incidentId>`, `.error`, `.errormeta`, `.noerror`, `.rollback`, `.rollback.cancel`, `.confirm`, `.dismiss`, `.dismissnote`. The rollback is ARM-THEN-CONFIRM: the first press only opens `.confirm`, the second spends the offer.\n\nALSO WORTH CARRYING: nothing is deployed in this project's store. `find .moe -iname \"*deploy*\" -o -iname \"*environ*\" -o -iname \"*probe*\"` returns zero entries today, so the live drive's honest-absence branch is live unless a deploy row changes that before you grade it.\n\nGATES I RAN TODAY at that HEAD, if useful as your baseline: `pnpm --filter @moe/control-room test` -> Test Files 204 passed (204) / Tests 2667 passed (2667) EXIT 0; `pnpm --filter @moe/daemon test` -> Test Files 595 passed (595) / Tests 10595 passed | 8 skipped (10603) EXIT 0; `pnpm typecheck` -> every project Done, EXIT 0. I did NOT run `pnpm test:e2e:browser` - my plan forbade quoting a count line for evidence this row does not own.",
      "timestamp": "2026-09-08T03:12:35.428Z"
    },
    {
      "id": "comment-b982f0794a4d45b89037ea816ddcabee",
      "author": "qa-eaa78526",
      "content": "A NONDETERMINISTIC SECRET-LEAK ARM IN THE CONTROL-ROOM LEG WILL RED YOUR DoD 1 AT RANDOM. Measured 2026-09-08 03:16-03:30Z by qa-eaa78526 while reviewing task-f008f29a38d54ae7a10e97cad1c4443d, on branch moe/work-2026-09-04 with the failing path CLEAN in `git status --porcelain` (mtime 2026-09-07 07:17:50, a full day older than the session). Filing here rather than opening a row: your DoD 1 names `pnpm --filter @moe/control-room test` as a regression leg, and whoever runs it has maybe a 1-in-3 chance of an unexplained red that has nothing to do with their diff.\n\nTHE FILE: `apps/control-room/src/v2/ops/environment-variables-screen.test.tsx`, describe block `NO VALUE IS EVER RENDERED OR RETAINED`. Added by `09f0df39` under task-ba83b202265d40d1885d3091f009b0a2 (\"The Environments screen shows what is set without ever showing a value\"), which is DONE and unassigned. Not owned by task-f008f29a or either of its grandchildren.\n\nTHE EVIDENCE THAT IT IS FLAKE AND NOT A DEFECT - a DIFFERENT ARM fails each time, on byte-identical bytes:\n- Full leg `pnpm --filter @moe/control-room test` at 03:16Z: `Test Files 1 failed | 203 passed (204)` / `Tests 1 failed | 2666 passed (2667)`, exit 1. Failing arm: **(b) holds no sentinel after an UNDELIVERED submit either**.\n  `AssertionError: expected '[\"div\",\"div\",\"cr2-card\",\"cr.env-vars....' not to contain 'SENTINEL-not-a-secret-0000-1111-2222'`\n- Narrowed re-runs `npx vitest run src/v2/ops/environment-variables-screen.test.tsx`, three consecutive:\n  run 1 -> `Tests 1 failed | 35 passed (36)`, failing arm **(a) does not carry the sentinel anywhere after a SUCCESSFUL submit**\n  run 2 -> `Tests 36 passed (36)`\n  run 3 -> `Tests 36 passed (36)`\nNarrowing is safe on this package: `apps/control-room` `scripts.test` is a bare `vitest run` with no flags to drop (unlike @moe/daemon, whose script carries `--testTimeout=30000`).\n\nWHERE THE NONDETERMINISM IS. The quoted value is a JSON array of React FIBER props, so it is `fiberProps(container)` that trips, not `domText(container)`. Both arms settle an async write - (a) a resolved dispatch, (b) a `Promise.reject(new Error(\"offline\"))` one - and then walk React's internal fiber tree for the sentinel. Whether a superseded fiber alternate still holds the submitted value is a scheduling race with React's commit/cleanup, so the arm measures GC timing as much as it measures retention. The 2026-09-07 07:39Z baseline recorded control-room at 188/188 exit 0 and the worker of task-f008f29a measured 204/204 exit 0 at 03:14:18Z - two minutes before my red on the same tree - so this has been quietly passing by luck, not absent.\n\nWHY IT IS WORTH ONE ARM'S WORK RATHER THAN A RETRY WRAPPER. Epic rail 3 says a secret never surfaces in plaintext anywhere, including a fiber. The property is right and worth asserting; a flaky assertion of it is worse than a solid one, because the next agent to see this red on an unrelated diff will most likely re-run until green and learn to distrust the whole leg. Awaiting React's commit before the walk (or asserting against a settled tree rather than a live container) would make it deterministic without weakening the property. Do NOT \"fix\" it by deleting the `fiberProps` half - that is the half that catches retention the DOM cannot see.\n\nATTRIBUTION FOR task-f008f29a, so this is not read as its red: that row shipped ZERO production bytes (its three recorded commits carry only `.moe/tasks/*.json`), so per global rail 3 the HEAD-minus-merge-base failing-path delta intersected with its owned paths is trivially EMPTY. I approved it on that basis and disclosed this red verbatim in its review summary.",
      "timestamp": "2026-09-08T03:23:49.332Z"
    },
    {
      "id": "comment-1ac479b2e8c248d58895c8002a1880d8",
      "author": "worker-4d8571e7",
      "content": "FOUR ITEMS FOR THIS EPIC-FINAL ROW, from worker-4d8571e7 closing task-7683a432 (the monitoring/backup/rollback roll-up, which is one of your dependsOn). Measured 2026-09-08 03:29-04:5xZ at HEAD 9bfd4142 on moe/work-2026-09-04. Reporting, not re-filing: this row owns no production paths and wrote none. Items 1 and 2 are follow-ups (global rail 21); items 3 and 4 will red YOUR DoD unless you plan for them.\n\n**1. YOUR DoD 1 (root `pnpm test` green) IS RED AT THIS TREE, AND NOT BECAUSE OF ANY DIFF.** `pnpm test` -> `Test Files 1 failed | 432 passed | 2 skipped (435)` / `Tests 1 failed | 10387 passed | 46 skipped (10434)`, exit 1. The single red, verbatim: `FAIL tests/migration/cutover/cutover-drill.test.ts > TASK-MF ARM E: captureCutoverManifest succeeds against the REAL repo root` — `{\"ok\":false,\"layer\":\"cutover-manifest\",\"code\":\"CUTOVER_MANIFEST_ENTRY_LIMIT_EXCEEDED\",\"path\":\".moe/tasks/task-eca1a82ffa844c679d25a60ad8bd165e.json\",\"detail\":\"entry count exceeds 10000\"}`.\nCAUSE, MEASURED: ARM E walks the real repo root under `MAX_WALK_ENTRIES` 10000 with `.git`/`dist`/`node_modules`/`target` excluded. That walk is **10538** entries here — `find . \\( -name node_modules -o -name .git -o -name dist -o -name target \\) -prune -o -print | wc -l` — against the **8257** that `tests/migration/cutover/cutover-manifest.ts:116` records for 2026-09-03. THE BOARD IS THE GROWTH: `.moe/tasks/` holds **895** JSON files and the refusal names one of them. This is a REPO-SIZE THRESHOLD CROSSING, not a code defect: it reds for every row at this tree, it is nobody's diff, and it cannot be attributed away by the rail-3 delta because no diff introduced it. Root `pnpm test` was exit 0 as recently as 2026-09-07 07:39Z (`432 passed | 2 skipped`), so the crossing happened in the last day. YOU CANNOT SATISFY \"root lane green\" WITHOUT SOMEONE MOVING `MAX_WALK_ENTRIES` OR EXCLUDING `.moe` FROM THE WALK — that is a production decision in a file this row does not own, so plan it or route it before you gate on it.\n\n**2. NO INCIDENT HISTORY IS EVER SERVED — the flapping-environment gap.** The store keeps every outage: `health-incident.ts:16-19` puts a PARTIAL UNIQUE INDEX on `(project_id, environment) WHERE closed_at IS NULL` so only one card is open at a time, `:76-80` closes it on the first SUCCESS, `:87-89` inserts a NEW row on the next three-failure tail, and rows are never deleted. NOTHING READS THEM: `deployments-health-read.ts:268-271` narrows the whole list with `incidents.findLast(i => i.closedAt === null)` and puts that single value on the view; the client's EXACT-KEY roster (`live-deployments-health.ts:279`) has no history member to decode. An operator watching an environment that has failed and recovered five times today sees exactly what a first-time failure shows — the difference between \"roll back\" and \"this is not a deploy problem\". One read member plus one UI line; the durable data is already there.\n\n**3. THE INCIDENTS TABLE IS UNBOUNDED WHILE THE PROBE RING IS CAPPED.** `HEALTH_PROBE_RING_LIMIT = 1440` is enforced per (project, environment) on EVERY write (`health-probe-ring.ts:102-104`). `health_incidents` has NO cap, NO retention and its read has NO `LIMIT`, and `daemon-store-foundation-composition.ts:699` calls `ring.incidents(environment)` on EVERY `/deployments/health` read — which the control room polls. Each row is fully re-validated on the way out (exact 6-key probe roster x3 per row, `health-incident.ts:22-37`) to produce ONE field. A long-lived flapping environment pays a growing cost on the exact surface an operator refreshes during an outage.\n\n**4. TWO CHEAP ARMS NOBODY OWNS.** (a) ROLLBACK-TO-A-ROLLBACK is right by construction but untested: a rollback APPENDS a new `moe-deploy-receipt/1` carrying the target's sha and digest (`rollback-command.ts:103`, validated :106-111), so the ledger reads [A, B, A'] and `resolveRollbackTarget`'s `ran[ran.length - 2]` (`rollback-target.ts:38-45`) offers **B** — roll-forward works, nobody is stranded. rollback-target.test.ts has seven arms and NOT ONE feeds a post-rollback history. (b) A ROLLBACK NEVER MIGRATES, and that should be pinned as well as documented: the migrate port is composed request-scoped INSIDE the deploy handler (`deploy-command.ts:232-260`), `productionDeployPorts` (:104-126) has no `migrate` member, and the rollback passes `options.ports ?? productionDeployPorts(...)`, so `migrateFor` returns null at `deploy-service.ts:156`. I chased the opposite hypothesis first — that the redeploy's migrate step re-applies the migration the restore just removed, silently undoing it inside one command — and MEASURED it away; the live arm's ports double omitting `migrate` is faithful to production, not papering over it. Worth a line in the runbook (your DoD 6 docs sweep): rollback replaces the image and, only when asked, restores the current deploy's pre-migration dump; without `restoreDatabase` it deliberately leaves the schema ahead of the code, because `deployment.migrate_down` owns that half.\n\nFOR YOUR DoD 6 DOCS SWEEP: `README.md:155` still says \"generation, monitoring, backup and rollback) are NOT claimed here\" — stale since this epic's monitoring/backup/rollback rows landed. I did not fix it from task-7683a432: both `README.md` and `docs/agent-stack-runbook.md` are in the PLAN affectedFiles of five live rows including yours and task-78eedd8e (REVIEW), so my edit would have been withheld as `Moe-Contested` and delivered nothing.",
      "timestamp": "2026-09-08T03:55:27.408Z"
    },
    {
      "id": "comment-a3256dc4bb9f411694b39e15f5fb2bb1",
      "author": "architect-f362f7a6",
      "content": "Redaction leaf task-c78140e9a4b749078d4fb633e2e70850 is now a structural prerequisite (existing dependency list preserved). User confirmed fail-closed [REDACTED] policy before deploy receipt serialization; UI remains a verbatim reader. Your final docs sweep should retire stale raw-stderr claims in deploy-receipt-contracts.ts (DEPLOY_BUILD_FAILED comment), deploy-ports.ts (migration diagnostic commentary), deploy-service.ts/deploy-command.ts migration-detail commentary, and http/deployments-health-read.ts:17 commentary; receipt/read schema itself is unchanged. Do not alter parser authority or copy redaction to readers. Leaf owns new deploy writes only and does not rewrite append-only historical receipts. Assess historical exposure and other existing receipt fields via the whole-platform hardening pass; no assertion that legacy stores were cleansed or that unrelated credential surfaces were fixed is permissible. Current numeric order134 makes the new bug isEpicFinal:true, but this existing integration row(order130) is the actual final consumer by its dependencies; please have governor correct order metadata rather than expanding the bug into a duplicate whole-epic hardening task.",
      "timestamp": "2026-09-08T11:36:42.378Z"
    },
    {
      "id": "comment-dee34207a41c4982bb3f6bdc6b97f0c4",
      "author": "worker-bcaa6d5e",
      "content": "THE ADVERSARIAL PASS OVER THE WHOLE EPIC DIFF (DoD 6, first half). worker-bcaa6d5e, 2026-09-08, HEAD 7622a2e8. SIX HUNTS, each with the command that answered it. WHAT WAS LOOKED FOR IS RECORDED WHETHER OR NOT IT FOUND ANYTHING — an unqualified \"no issues\" is not a result.\n\n1. A SCAFFOLD THAT GENERATES INFRASTRUCTURE THE DEPLOY PATH CANNOT CONSUME — CLEAN, and proven by execution rather than by reading. The deploy engine finds the proxy by `docker ps --filter label=com.docker.compose.service=proxy --filter network=<net>` (`createProxyPort`), and admits its Caddyfile only if, after stripping comments and blank lines, the body equals `deploymentInfrastructureFiles(\"\",[])[\"docker/Caddyfile\"]` with the upstream normalised to `app:3000` (`acquireProxy`, `DEPLOY_PROXY_CONFIG_UNSUPPORTED`). The generated compose brings `proxy` up with exactly that label and exactly that Caddyfile, the lease is acquired, the flip lands and the response changes. `platform-pipeline.e2e.test.ts` now asserts the discovery non-empty BEFORE the deploy depends on it, so a scaffold that stopped emitting a `proxy` service — or a deploy bound to another network — reds a named assertion instead of a redacted `DEPLOY_BUILD_FAILED`.\n\n2. A VARIABLE NAME THE APP EXPECTS AND THE STORE SPELLS DIFFERENTLY — CLEAN. Traced `DATABASE_URL` through all four spellings: scaffold `.env.example` (`controlled-profile-root-templates.ts:122`), the app's own read (`controlled-profile-package-templates.ts:179 process.env.DATABASE_URL`), the generated compose (`deployment-infrastructure-templates.ts:143 DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL in .env}`), and the deploy's migration resolver (`deploy-migration-context.ts:39 DEPLOY_MIGRATION_DATABASE_VARIABLE = \"DATABASE_URL\"`). Identical. `PORT` likewise (`:181` vs `:142`). `POSTGRES_USER/PASSWORD/DB` match the scaffold's own db service. No mismatch.\n\n3. A MIGRATION THAT WORKS ALONE BUT NOT IN SEQUENCE AFTER A ROLLBACK — MEASURED, AND THE ANSWER IS A PROPERTY OPERATORS MUST KNOW. `deploy-service.ts` runs `migrateFor` ON THE ROLLBACK LEG TOO — after the digest check, before the candidate starts — with the PREVIOUS sha and a new decision id. So the forward migration is re-attempted, finds nothing to apply, and THE SCHEMA STAYS MIGRATED: `select tablename from pg_tables where schemaname='public'` lists `app_metadata` both immediately before and immediately after the rollback. A rollback does NOT revert the schema; `migrate_down` (task-78eedd8e) is a separate operator action and nothing on the rollback path invokes it. Asserted in step 4, not merely described. NOT filed as a defect: it is the shipped design, and the ordering comment in `deploy-service.ts` argues for it. It IS going into the docs, because \"rolled back\" reads as \"everything went back\".\n\n4. A PROBE SCHEDULE THAT OUTLIVES THE ENVIRONMENT IT PROBES — **FOUND. FILED task-de80c663569a4ab2b30a9db6ac526e4b.** `createHealthProbeJob` takes its roster from `readDeployLedger(...).keys()`, and the deploy ledger is APPEND-ONLY, so an environment enters the sweep on its first deploy and can never leave. Worse than the shape the plan predicted: `git grep -l \"deployment.destroy\\|environment.destroy\\|deployment.teardown\\|ENVIRONMENT_DESTROY\" -- apps packages` returns NOTHING — the platform has no retire/destroy/teardown for an environment at any layer, so there is no operator action that could ever remove one. The per-environment job already reads an `active()` predicate per tick, so the mechanism exists at that granularity; the sweep's roster and the missing command are the gap.\n\n5. A REFUSAL CODE WHOSE LAYER DISAGREES ACROSS TWO BANDS — CLEAN, after correcting my own first measurement. A naive census over `{code:\"X\",...layer:\"Y\"}` and its reversed spelling returned THIRTEEN codes with two or more layers (`HEALTH_READ_UNREADABLE`, `EXPECTED_VERSION_CONFLICT`, `INPUT_INVALID`, …). Re-run with `':!*.test.ts' ':!*test-fixtures*'` it returns **NONE** across 23 production code/layer pairs: every duplicate came from test fixtures asserting a code under a different layer, not from two production sites disagreeing. Spot-confirmed on `HEALTH_READ_UNREADABLE`, which has exactly one production site (`http/health-read.ts:184`). Recording the wrong first answer deliberately — the naive grep is the one a reviewer would reach for, and it lies.\n\n6. THE DIGEST MIRRORS (rail 4), CENSUSED BY HEX ACROSS ALL FILE TYPES — CLEAN, and there are SIX, not four. Current digest `daafa787…d26a025`, read from `generated-client.ts`. `git grep -n <hex> -- packages apps scripts tools tests adapters` returns exactly six sites, all carrying the SAME value: `packages/control-room-client/src/generated/generated-client.ts` (source of truth), `packages/control-room-client/src/generated-coverage.test.ts`, `apps/control-room/src/data/data-adapter.test.ts`, and three `.tsx` files — `v2/goals/approve-plan.test.tsx`, `approve-plan-reject.test.tsx`, `plan-run-resolution.test.tsx`. FOUR of the six live in `apps/control-room` and are invisible to root `pnpm test`; `pnpm --filter @moe/control-room test` is the only leg that executes them, and it is GREEN (`Test Files 204 passed (204)` / `Tests 2673 passed (2673)`). A grep for any OTHER hex in a `CONTRACT_DIGEST|contractDigest|contractSchemaHash` context returns only this value, so no stale mirror survives. `scripts/release/release-subject.mjs` has LEFT the mirror set — it now calls `deriveContractDigest()` (`:7`, `:59`), which is task-a9c6d4a5 landed.\n\nALSO FOUND WHILE DRIVING THE CHAIN, both filed:\n- **task-0ca117e389df43ee9b458255f0752842 (HIGH)** — a deployed candidate container receives NO environment variables. `runCandidateArgv` is `[\"run\",\"--detach\",\"--name\",name,\"--network\",network,tag]` with no `--env`. The incumbent gets them from compose and the migration reads them from the store; the container `deployment.deploy` promotes to serve traffic gets none. Invisible today only because the generated `/health` is a pure function that touches no database.\n- **task-61f826e50332498eaaaf44f2043845fc (MEDIUM)** — a missing migration TOOL is reported as a missing migration FILE (`MIGRATION_FILE_UNKNOWN`), and the deploy receipt redacts the detail, so in production the cause is invisible. `MIGRATION_TOOL_MISSING` already exists as a constant and is never raised from this path.\n\nNONE OF THE THREE WAS FIXED HERE (task rail 1). Each is measured symbol by symbol in its own row.",
      "timestamp": "2026-09-08T17:19:07.974Z"
    },
    {
      "id": "comment-b82b0e027c864694b2ee2622f980b7d0",
      "author": "governor-12ae245c",
      "content": "GOVERNOR NOTE FOR STEP 8 (the docs sweep) — one claim that must not reach README.md or docs/VISION.md, plus a confirmation about your own scope. Posted on the row because my #workers message returned `routingTargets: []` (channel hop cap), so do not rely on having seen it there.\n\n**1. YOUR DoD IS UNCHANGED. Finish as planned.** The owner gave a coding-first direction at ~17:47Z and a dedicated preview row (task-047b1d8b) was deleted under it. I put the scope question to the owner and the ruling is: **it applies to that deleted row ONLY.** Your DoD 2 (an HTTP response from a running deployed environment, not a receipt) and DoD 3 (rollback proven by response, reachable from the incident card's real operator route) stand exactly as you planned them. Nobody will re-grade you against a moved bar. Do not pre-emptively soften a live-drive clause or drop a docker leg on the strength of that direction.\n\n**2. THE CLAIM TO KEEP OUT OF THE DOCS.** `docs/VISION.md:141` lists Stage 2 as \"repository bootstrapping; infrastructure generation; PREVIEW AND PRODUCTION DEPLOYMENT; secrets and environment management; database migrations; monitoring, backup, and rollback; release evidence\". A migration has **never** been applied to a standing preview environment on this host. `<project>/.moe-next/backups/pre-migration/preview/` has never existed anywhere here — measured by worker-812079a7, re-derived by qa-2702df88, verified again by me.\n\nThe row that would have proved it was DELETED as a priority call, and architect-9556982c recorded the reason on parent task-8f86e105 in the words that matter: **\"scope cancelled, NOT capability verified.\"** The deletion is not evidence the capability works, and nothing in your row's evidence closes it. task-8f86e105's DoD 5 was approved on DELIVERED SCOPE with the unmet half deliberately left visible; a doc sentence that flattens \"approved\" into \"shipped\" undoes the entire reason that clause was kept open.\n\nYour step note already says to mark shipped only what this row actually proved — apply it here specifically: if a sentence covers the whole Stage-2 list at once, SPLIT IT rather than let the preview clause ride in on the bands you did prove. Claim what your own gate output supports and nothing past it.\n\n**3. `docs/agent-stack-runbook.md` is dirty with a peer's work** and already carries the `gh` prerequisite section (heading at :110) and the RELEASE_PR_FAILED explanation. worker-812079a7's step 5 was explicitly told not to re-edit it; the same applies to you. Above all, do not regenerate that file from a HEAD copy — it would destroy uncommitted peer work in the shared tree, which has torn HEAD on this board before.",
      "timestamp": "2026-09-08T17:52:50.325Z"
    },
    {
      "id": "comment-16ce3bf9d5ef4f13b2119ad5335c884d",
      "author": "qa-2702df88",
      "content": "QA APPROVAL EVIDENCE. Approving. All six DoD items verified; the one thing I could NOT do myself is stated plainly below rather than buried.\n\nWHAT I RE-RAN MYSELF today, foreground, on this branch:\n- `pnpm --filter @moe/daemon test` EXIT 0 — 596 files / 10644 passed | 8 skipped. Identical to this row's recorded verification.\n- `pnpm test` (root) EXIT 0 at 15:11Z — 434 passed | 2 skipped (436). This row's own run reads 437 | 2 (439); the +3 is exactly its three new e2e files entering the root include (`tests/**/*.test.ts`), not a regression. worker-d0fc1403 independently ran root post-fix at 18:06Z: EXIT 0, 438 passed | 1 skipped, 10423 | 49.\n- `pnpm --filter @moe/control-room test` EXIT 0 — 204 / 2673. `pnpm typecheck` EXIT 0, 19 `$ tsc` echoes.\n\nWHAT I COULD NOT RE-RUN, AND WHY — the honest limit of this approval. The live arm (`MOE_PLATFORM_PIPELINE=1`) is the only thing that proves DoD 2, 3, 4 and 5 by execution, and I did not run it. Two blockers, in order:\n1. full-suite-gate was held continuously from 18:01Z (worker-d0fc1403's store row, then worker-9dcd2d4f's security baseline through ~18:40Z). This run stands up a real `docker compose` topology and binds ports 3000 and 5432 — starting it under a peer's suite would have corrupted both measurements, and queueing on the resource would have pinned this one-shot QA seat.\n2. More decisively: as of 18:29Z the shared tree carries an IN-FLIGHT REFACTOR OF THE DEPLOYMENT ENGINE ITSELF — `deploy-command.ts`, `deploy-ports.ts`, `deploy-service.ts` and `rollback-command.ts` modified, plus untracked `deploy-candidate-environment.ts` with no `.js` bridge (that missing bridge currently reds `apps/control-room/src/live/live-runs.test.ts:293` with ERR_MODULE_NOT_FOUND). A live pipeline run now would exercise a half-finished engine: a red would be unattributable to this row, and a green would be measuring code this row was never verified against. Neither is evidence. This row's live evidence was taken at HEAD 7622a2e8/c2f726d3, BEFORE that dirt existed.\n\nSo I verified the live clauses by ASSERTION SHAPE plus corroboration, and I am saying so rather than implying I watched them pass:\n\nDoD 2/3 — proven BY RESPONSE, not by receipt, which is what the clause demands. Each build carries its own `/build` marker committed into its own commit, so `GET /build` names which sha is serving: `expect(await servedBuild()).toBe(buildA)` before, `toBe(buildB)` after deploy, `toBe(buildA)` again after rollback, with `expect((await request(HEALTH_PATH)).status).toBe(200)` alongside. The rollback goes through the OPERATOR's route — `resolveRollbackOffers` off `/affordances/read`, asserting exactly one offer of kind `deployment.rollback`, then `resolveRollbackTarget` whose `toReceiptRef` is asserted equal to the first receipt's id — not from test-local variables. `withBuildRoute` refuses to mark a server that no longer carries the route it anchors on, so the marker cannot silently stop being injected.\n\nDoD 4 — three failure arms, each censusing containers, networks and the port by real query before and after, with `added(before, after)` asserted empty. The census helper is itself tested (`added([\"a\",\"b\"],[\"b\",\"c\"])` equals `[\"c\"]`), so a census that stopped seeing anything cannot pass. The interrupt arm asserts its OWN PREMISE — that the orphan is still alive after the kill and the child's `finally` did NOT run — which is the anti-vacuity control the DoD asked for. Corroboration I did not have to take on trust: worker-d0fc1403 observed `docker ps -a` EMPTY at 18:06Z from an unrelated row, after this row's stack had been up at 16:46-16:52Z. That is an independent seat confirming the teardown.\n\nDoD 5 — the canary is proven DELIVERED before anything is swept: read back through the encrypted store, and then postgres AUTHENTICATES a connection with it (`docker exec --env PGPASSWORD=<canary> ... select 42`). That is the half that makes a zero-hit sweep mean something. The sweep helper is proven able to FAIL (a text carrying the canary yields a hit; a clean text yields none), and the canary's shape `canary-not-a-secret-<12 hex>` is asserted not to resemble a credential.\n\nDoD 6 — the adversarial pass (comment-dee34207) is six named hunts each with the command that answered it, and it records what was looked for whether or not it found anything. One real finding, filed rather than absorbed: task-de80c663, the health probe roster comes from the APPEND-ONLY deploy ledger and `git grep` finds no destroy/teardown/retire at any layer, so an environment can never leave the sweep. Hunt 3 is the valuable one — a rollback re-runs the forward migration and does NOT revert the schema, measured by `pg_tables` before and after, and it is now in the runbook in the plainest words in the file.\n\nDoD 1 — the \"resolve legs BY PACKAGE NAME\" trap is handled: they enumerated the workspace packages and there is no scaffold/infrastructure package; both generators ship inside @moe/daemon, so the daemon leg IS that leg. I re-derived the package list myself and agree.\n\nDELIBERATE NON-FINDINGS, so this reads as a pass with reasons: the harness fabricates no doubles (`createDockerDouble` exists and is explicitly not used); the three live files are flagless by default, so they add discovered tests to the root lane but neither bind a port nor build an image — they are NOT the cause of the child-process timeouts worker-d0fc1403 chased and then correctly falsified; and `apps/daemon/src/repository/deployment/deployment-docker-probe.ts` appears in this row's declaredPaths but is CLEAN and last touched by e219aa43, so nothing of this row's is in it.\n\nHAND-LANDED as c5533f68 (tree 24365c3e): four untracked e2e files plus the runbook's single hunk, 1257 insertions. Untracked files are invisible to `git diff` and are dropped silently at exit — this row had no completion commit and `fa4e3099` had already skipped `platform-pipeline-harness.ts` once as MOE_ATTRIBUTION_UNRESOLVED. The peer-owned deployment refactor and `live-goal-release.test.tsx` were excluded and remain dirty for their owners. Not pushed; rides the next worker push.\n\nSIZE: 1257 added lines is far past the usual QA bar, and I am not rejecting on it. This row builds no band — its entire deliverable IS proof code, the epic rails put per-file caps on production files rather than an aggregate on test/fixture modules, and every new file is under 340 lines.",
      "timestamp": "2026-09-08T18:36:58.129Z"
    },
    {
      "id": "comment-6fc8edaea6274226aba146dadce8f287",
      "author": "worker-bcaa6d5e",
      "content": "POST-APPROVAL LEDGER CORRECTION (worker-bcaa6d5e, 2026-09-08, row already DONE — comment is the only writable surface left).\n\nTWO THINGS THIS ROW'S `completionSummary` CANNOT CARRY, because it is SEVERED AT THE 2000-CHAR CAP. Measured: `len(completionSummary) == 2000` exactly, ending mid-word at \"...proxy Caddyfile after the flip, `gi\". DoD 5's swept-artifact list is cut off partway, and DoD 6 (the adversarial pass) and DoD 7 NEVER APPEAR IN IT AT ALL. Anyone reading the summary alone will read a severed gate table as a skipped gate. The full evidence is in comment-dee34207 and the 8 step notes; the summary is not the record.\n\n1. THE PREVIEW/BACKUP CLAUSE IS **UNPROVEN-BY-DECISION**, NOT SATISFIED. This row stood a compose topology up and tore it down INSIDE the drill. That is NOT a standing PREVIEW environment. `<project>/.moe-next/backups/pre-migration/` does not exist anywhere on this host — re-measured 2026-09-08 21:0xZ: `ls` -> `No such file or directory`. No backup file path with a sha256 matching a receipt `backupRef` is quoted anywhere in this row's record. The row that would have proved it, task-047b1d8b, was filed and then DELETED on the owner's REPL direction (\"delete the preview tasks we just want to finish coding\"), recorded by architect-9556982c as \"scope cancelled, NOT capability verified\". Do not re-file it. Do not close it as satisfied either. It is cancelled scope, and the capability remains unverified.\n\nRelated and equally unproven: the row used `select tablename from pg_tables where schemaname='public'`, NOT psql `\\dt`. The literal `\\dt` was tried and failed — `ERROR: syntax error at or near \"dt\"` — the backslash does not survive TypeScript string literal -> Node argv -> psql.\n\n2. TEARDOWN HOLDS ON THE FAILURE PATH, AND THAT WAS PROVEN TWICE BY ACCIDENT. DoD 4 was graded on assertion shape (census before/after, `added()` empty). Stronger than that: the `finally` census by real query ran on the TWO FAILING iterations of that step as well (the `\\dt` failure and a splice error) and left 0 containers and 0 networks behind both times. A cleanup that holds when the body throws is the actual clause; the green path alone never tests it.\n\n3. CONTAINER CENSUS, INDEPENDENTLY DOUBLE-CONFIRMED. All four drill containers (`moeteardownf514c6f1-{proxy,app,db}-1`, `moe-teardown-interrupted-4efc199d`) are gone: `docker ps -aq` empty, `docker info` -> `containers=0 running=0`, taken on my seat and re-taken on qa-2702df88's. Two NON-container leftovers survive with a DIFFERENT prefix and predate this row: network `moeliveydmuzh_default`, volume `moelive36lkok_db-data`. Neither seat deleted them — a `db-data` volume with no row behind it is somebody's evidence, and deleting it is the one irreversible move available here.\n\n4. THE GATE NUMBERS IN `verification` ARE A STALE VINTAGE, NOT A REGRESSION. Recorded `pnpm --filter @moe/daemon test` EXIT 0 `596 passed (596)` / `10644 passed | 8 skipped`. Two later rows have since landed; qa-2702df88 measures the daemon lane now at `598 files / 10712 passed | 9 skipped`. The 9th skip is the deliberately filtered LIVE_PROVIDER arm from task-f9dc8291. Any epic-final gate table should quote the CURRENT shape and cite this row's numbers as the vintage they are.",
      "timestamp": "2026-09-08T21:11:23.166Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T16:47:01.388Z",
  "updatedAt": "2026-09-08T21:11:23.166Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T22:21:02.102Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 8,
    "doneAt": "2026-09-08T18:37:40.920Z",
    "wallClockMs": 245798818
  },
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-05T22:25:13.304Z",
  "planningNotes": {
    "approachesConsidered": "PLANNING AROUND THE MISSING DOCKER DAEMON. Rejected, and this is the row's central judgement. The available shortcuts are all forbidden: asserting a receipt instead of an HTTP response narrows DoD 2's explicit wording; mocking the container fabricates the authority the row exists to certify; and skipping the arm silently converts a blocked clause into a green one. DELIVERY BIAS permits a hard block exactly when proceeding would fabricate authority, and this is that case. The plan therefore does every docker-independent clause, and blocks on the rest with a measurable unblock condition - the same shape task-3706b7e7 used and the governor endorsed.\n\nWHAT IS STILL PROVABLE WITHOUT DOCKER, and it is not nothing: the full regression legs (DoD 1), the canary sweep over COMMITTED artifacts and GENERATED INFRASTRUCTURE FILES (part of DoD 5), the cross-band adversarial pass over the whole epic diff (DoD 6), and the docs sweep (DoD 6). Those are planned to run regardless, so a docker host later has only the live clauses left.\n\nSPLITTING THE ROW. Rejected. Its whole value is that ONE row sees what five bands got wrong about each other; splitting it by clause would recreate the smeared verification the epic-final pattern exists to prevent. Six files, eight steps.\n\nWRITING A FIFTH HARNESS for the journey. Rejected - tests/e2e/foundation already has four real-process harnesses; reuse their store-construction and teardown shape.",
    "codebaseInsights": "MEASURED AT HEAD (moe/work-2026-09-04) by architect-fa3a3e17, 2026-09-06. Rail 5 says treat the description as stale; it is, in two ways that change what this row can do.\n\n1. THIS HOST HAS NO DOCKER DAEMON, AND DoD 2, 3 AND 4 CANNOT BE HONESTLY MET WITHOUT ONE. Measured directly:\n  `docker version --format '{{.Server.Version}}'` -> `failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine; check if the path is correct and if the daemon is running: open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`\nDoD 2 requires an HTTP RESPONSE from a running environment (\"not a receipt saying it should be up\"), DoD 3 requires the previous sha to be SERVING after a rollback, and DoD 4 requires enumerating live resources by REAL QUERY (`docker ps`, a port probe). None of those is reachable without a docker daemon. This is not a code gap and not something a prerequisite row can close - it is a host capability.\nTHE PRECEDENT IS ALREADY ON THIS BOARD AND IT IS THE RIGHT ONE: task-3706b7e7 delivered 8/8 steps and BLOCKED rather than fabricate its docker-dependent clause, with its arm refusing loudly (`DEPLOY_DOCKER_UNAVAILABLE`) instead of skipping. The governor's 19:30Z log records that as the model for how to file such a block. Step 1 makes the same call explicit, with a measurable unblock condition.\n\n2. THE EPIC IS 47 ROWS, NOT 23. `grep -l epic-3f9b9556 .moe/tasks/*.json` by status: 32 WORKING, 7 DONE, 3 REVIEW, 3 BLOCKED, 2 PLANNING. The description's \"22 of 23 unstarted, the least-started third of the board\" is stale in the direction its own rail 5 predicted - most of what this row certifies is being built right now by many hands, and ALL FIVE band parents in dependsOn were still unlanded at plan time.\n\n3. THE PIPELINE IS A CHAIN OF ROWS THAT MOSTLY DO NOT EXIST YET. Spot-measured: `git grep -c \"deployment.deploy|deployment.rollback|moe-migration-receipt|moe-health-probe\" -- apps packages` returns nothing for each. The scaffold generator DID land (task-4a0f7064, sha 2feed27b, 22-file emitted tree, child build green in 27.43s) and `apps/daemon/src/repository/deployment/` exists from task-3706b7e7 (deployment-docker-probe.ts, infrastructure generator and templates). Everything between them - deploy effect, environment delivery, migrations, probe, rollback - is in flight.\n\n4. THE DIGEST MIRRORS MOVED AGAIN TODAY and rail 4 is right that this epic no longer owns them. Census BY HEX, never by symbol: three of the four mirrors hard-code the hex and never name `GENERATED_CONTRACT_DIGEST`, and one is a `.tsx` a `*.ts`-only grep cannot see. Between two readings hours apart the digest went ed5f6b87... -> f07b77e4... and `runtime-contract.test.ts`'s count went `toBe(104)` at :105 to `toBe(110)` at :108. The four mirror FILES held: generated-client.ts:907, generated-coverage.test.ts:36, apps/control-room/src/data/data-adapter.test.ts:18, apps/control-room/src/v2/goals/approve-plan.test.tsx:255.\n\n5. HOST GATE CONDITIONS THAT WILL AFFECT THE REGRESSION LEG: the governor's 19:26Z and 19:30Z logs record `mtkbtsvc` handle exhaustion (1.1M handles and climbing) making full-suite runs time out at 30 s in files nobody owns - `src/work/foundation-capture-producer.test.ts` named repeatedly - and warn against re-running full suites, which degrades every other seat's gate.\n\nHARNESS PRECEDENT for the journey: tests/e2e/foundation/ carries the real-process harnesses (e2e-harness.ts, j1-loop-harness.ts, j3-crash-harness.ts, j4-replan-harness.ts) and runs under `pnpm test:e2e`. Read them before inventing a fifth shape."
  },
  "planApprovedAt": "2026-09-05T22:25:15.484Z",
  "contextFetchedBy": [
    "worker-bcaa6d5e",
    "qa-2702df88"
  ],
  "workStartedAt": "2026-09-08T16:06:17.090Z",
  "reviewStartedAt": "2026-09-08T18:02:58.724Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  596 passed (596)\n      Tests  10644 passed | 8 skipped (10652)\n   Start at  20:50:26\n   Duration  697.02s (transform 26.08s, setup 0ms, import 381.30s, tests 2266.61s, environment 29ms)\n\nDAEMON_EXIT=0",
    "reportedAt": "2026-09-08T18:02:58.724Z"
  },
  "filesModified": [
    "apps/daemon/src/repository/deployment/deployment-docker-probe.ts",
    "tests/e2e/foundation/platform-pipeline.e2e.test.ts",
    "tests/e2e/foundation/platform-pipeline-harness.ts",
    "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts",
    "tests/e2e/foundation/platform-secret-canary.e2e.test.ts",
    "docs/agent-stack-runbook.md"
  ],
  "completionSummary": "DOCKER IS PRESENT ON THIS HOST (29.6.2) — the plan's report_blocked branch is VOID, and the whole chain ran for real. All 6 DoD items MET; none blocked. Detail: comment-dee34207 (adversarial pass) + the 8 step notes.\n\nDoD 1 at HEAD c2f726d3: `pnpm --filter @moe/daemon test` EXIT 0 `596 passed (596)`/`10644 passed | 8 skipped`; `pnpm typecheck` EXIT 0; `pnpm --filter @moe/control-room test` EXIT 0 `204 passed (204)`/`2673 passed`; root `pnpm test` GREEN run `437 passed | 2 skipped (439)`/`10410 passed | 51 skipped`. NO new workspace package exists for the scaffold/infrastructure rows — enumerated all 20 by NAME; both generators ship inside @moe/daemon, so the daemon leg IS that leg.\n\nDoD 2/3 BY RESPONSE, not receipt: scaffold, real `docker compose` topology, archive build from a REAL commit, variables through the encrypted store, real `migrateWithBackup`, real `createDeployService`. No double anywhere. Each build carries a `/build` marker in its own commit, so `GET /build` names which sha serves: after deploy it answers build-B, after rollback build-A. Rollback dispatched through the OPERATOR's route — `resolveRollbackOffers` (`/affordances/read`) + `resolveRollbackTarget` — asserted equal to the first receipt. A ROLLBACK DOES NOT REVERT THE SCHEMA (`pg_tables` before/after: `app_metadata` present both times); that is shipped behaviour and is now in the runbook.\n\nDoD 4: three FAILURE arms (failed deploy, failed migration, SIGKILLed process), each censusing containers/networks/port by real query before and after, deltas empty. The interrupt arm asserts its own PREMISE (orphan still alive after the kill) or it would prove nothing.\n\nDoD 5: canary shape `canary-not-a-secret-<12 hex>`. DELIVERY PROVEN TWICE before sweeping — store read-back, and postgres AUTHENTICATING a connection with it. Then 45 labelled artifacts swept, hits=0: both receipts, both report details, the ledger, migration receipt, probe body, candidate `inspect`+`logs`, proxy Caddyfile after the flip, `gi",
  "commits": [
    {
      "sha": "c5533f68b10fa519aeed38dc3255827bf08d643d",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "completion",
      "role": "qa",
      "sessionId": "qa-2702df88@2026-09-08T14:24:00Z",
      "paths": [
        "tests/e2e/foundation/platform-pipeline.e2e.test.ts",
        "tests/e2e/foundation/platform-pipeline-harness.ts",
        "tests/e2e/foundation/platform-secret-canary.e2e.test.ts",
        "tests/e2e/foundation/platform-teardown-failure-paths.e2e.test.ts",
        "docs/agent-stack-runbook.md"
      ],
      "recordedBy": "qa-2702df88",
      "recordedAt": "2026-09-08T18:36:19.686Z",
      "treeId": "24365c3ea42c0341e9503cbae93a9d974daed364",
      "status": "REVIEW",
      "pushed": false,
      "message": "QA hand-landing of four untracked e2e files plus the runbook edit. Peer-owned in-flight apps/daemon/src/deployment refactor excluded."
    }
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "qa-2702df88@2026-09-08T14:24:00Z",
    "at": "2026-09-08T18:36:19.686Z"
  },
  "completedAt": "2026-09-08T18:37:40.920Z",
  "reviewCompletedAt": "2026-09-08T18:37:40.920Z",
  "reviewSummary": "APPROVED, with one limit stated plainly. Full record: comment-16ce3bf9.\n\nRE-RUN BY ME: daemon EXIT 0, 596 files / 10644 passed | 8 skipped (identical to the recorded verification); root EXIT 0, 434 | 2 at 15:11Z — this row reads 437 | 2 and the +3 is its own new e2e files entering the root include, not a regression; control-room 204 / 2673 EXIT 0; typecheck EXIT 0, 19 `$ tsc` echoes.\n\nI DID NOT RUN THE LIVE ARM (`MOE_PLATFORM_PIPELINE=1`), rather than imply otherwise. full-suite-gate was peer-held from 18:01Z, and decisively: since 18:29Z the tree carries an in-flight refactor of the deployment engine (deploy-command/ports/service and rollback-command modified, untracked deploy-candidate-environment.ts with no .js bridge, currently redding live-runs.test.ts:293). A live run now exercises a half-finished engine — a red unattributable, a green measuring code this row never verified.\n\nLive clauses verified by assertion shape plus corroboration. DoD 2/3 assert the SERVED build by HTTP `/build` marker before deploy, after deploy and after rollback, rollback dispatched through the operator's `/affordances/read` offer with `resolveRollbackTarget` matched to the first receipt. DoD 4's three failure arms census containers/networks/port by real query, the census helper is itself tested, and the interrupt arm asserts its own premise (orphan alive, `finally` did not run); worker-d0fc1403 independently saw `docker ps -a` EMPTY at 18:06Z. DoD 5 proves the canary DELIVERED — postgres authenticates with it — before sweeping, and shows the sweep able to fail. DoD 6 is six named hunts with commands, one finding filed as task-de80c663, plus rollback-does-not-revert-schema measured on `pg_tables` and written into the runbook.\n\nHAND-LANDED c5533f68: four untracked e2e files plus the runbook hunk. No completion commit existed and fa4e3099 had already skipped the harness once. Peer refactor excluded. Not pushed."
}