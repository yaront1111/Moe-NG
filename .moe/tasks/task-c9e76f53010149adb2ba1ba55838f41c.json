{
  "id": "task-c9e76f53010149adb2ba1ba55838f41c",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "The Deployments card on the opened goal: per environment, arm and confirm, refusals as words",
  "description": "SPIDR child of task-84cd2e55aae942cf8d56657308215fde (architect-fa3a3e17, 2026-09-05), which measured at 11-12 files against a 10-file cap. dependsOn task-04b3ce7e349743c2a156d32ee5550ba5 for the command kinds it dispatches.\n\nWHY THIS ROW EXISTS. The parent owns the card, the Needs-you roster, the Runs receipt row, the Playwright journey, the live drive and the docs. The Needs-you roster change alone forces three files (see the parent's plan), so the card and the surfaces cannot share one plan. This row is the CARD. The parent keeps Needs-you, Runs, the journey, the live drive and the docs.\n\nWHAT THIS ROW OWNS: a Deployments card on the opened goal rendering, PER ENVIRONMENT, the target, the last deploy sha, time, url and status, with a Deploy arm/confirm; the port that spends the offer; and the card's own tests. NO Needs-you edit, NO Runs edit, NO Playwright journey, NO docs - all the parent's.\n\nTHE ONE SAFETY PROPERTY: PREVIEW AND PRODUCTION SIT ON THE SAME CARD, and the destructive one is a click away from the safe one. The confirm step must name the environment unmistakably, and a PRODUCTION deploy must show whether a release decision exists on the goal - so the operator sees what the deploy effect recorded rather than discovering it in a receipt afterwards.\n\nMEASURED AT HEAD (branch moe/work-2026-09-04) by architect-fa3a3e17:\n- THE PATTERN TO MODEL is apps/control-room/src/v2/goals/goal-publish.tsx (173 lines) with apps/control-room/src/v2/goals/publish-port.ts (29 lines). The port is tiny and is the whole decision wire: `publish-port.ts:1` imports `spendOffer` from \"../approvals/offer-wire.js\", `:17` declares `PUBLISH_COMMAND_KIND`, `:24` `createPublishPort(wire)`, and `:27` calls `spendOffer(wire, KIND, affordance, payload, \"ui-publish\", PUBLISH_LAYER)`. Mirror that exactly; a second decision wire is a design regression.\n- WHERE THE CARD COMPOSES: apps/control-room/src/v2/board/board-screen.tsx:17 imports `{ GoalPublish, publishOffer }` from \"../goals/goal-publish.js\"; goal-nodes.tsx:11 imports GoalPublish too. Determine which surface is the OPENED GOAL before wiring, and mount there.\n- apps/control-room/src/v2/goals/goal-status-strip.tsx:33 carries a `publish: \"cr-goal-publish\"` entry - check whether a card on this screen is expected to join that map.\n- THE TESTID NAMESPACE `cr.deploy` IS FREE: `git grep -c \"cr\\.deploy\" -- apps tests` returned nothing. Confirm again before claiming it; `cr.board.*` was already taken once.\n- goal-publish.test.tsx:204-215 asserts on its own SOURCE TEXT (readFileSync of goal-publish.tsx, then `not.toContain` for localStorage/sessionStorage and a retired key assembled rather than spelled so a repo-wide grep stays at zero). If this card holds no browser-local state either, copy that arm.</description>\n<definitionOfDone>placeholder</definitionOfDone>",
  "definitionOfDone": [
    "The Deployments card renders PER ENVIRONMENT: target, last deploy sha, time, url and status. It dispatches the deploy kinds through offer-wire `spendOffer` via a port mirroring apps/control-room/src/v2/goals/publish-port.ts - no second decision wire, and no command dispatch inside the component.",
    "THE CONFIRM STEP NAMES THE ENVIRONMENT UNMISTAKABLY. Assert, as its own arm, that a PRODUCTION confirm cannot be mistaken for a PREVIEW confirm: the rendered confirm text contains the environment name, and arming PREVIEW then confirming cannot dispatch PRODUCTION. This is the row's one safety property - an operator must not deploy production believing they clicked preview.",
    "A PRODUCTION deploy SHOWS whether a release decision exists on the goal, in the confirm step, in both states - cited when one exists and stated plainly when none does. The operator sees it before deciding, not afterwards in a receipt.",
    "The card is ABSENT - not a dead button - when the daemon offers no deploy. Assert absence by querying for the testid and finding nothing, not by asserting a disabled attribute.",
    "All four refusal codes (DEPLOY_TARGET_MISSING, DEPLOY_BUILD_FAILED, DEPLOY_DOCKER_UNAVAILABLE, DEPLOY_HEALTH_TIMEOUT) render VERBATIM with operator words. DEPLOY_TARGET_MISSING offers a way to set a target. DEPLOY_BUILD_FAILED carries the tool's last stderr line so a failure is diagnosable from the browser, not only from a receipt.",
    "ASCII-only sources and apostrophe-free JSX text (the scheduler package-boundary scan reds on an apostrophe in JSX); the `cr.deploy` testid namespace confirmed free before it is claimed; every production file at or under 250 lines, quoted with `wc -l`.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/control-room test`, `pnpm typecheck`. Note that root `pnpm test` covers NO apps/** file, so the control-room leg is the only evidence for this row."
  ],
  "taskRails": [
    "PRODUCTION IS ONE CLICK FROM PREVIEW ON THIS CARD. The confirm step names the environment unmistakably; this is the row's one safety property and it gets its own test arm.",
    "REUSE the PUBLISH card's arm/confirm shape and offer-wire `spendOffer` (publish-port.ts:27). A second decision wire is a design regression.",
    "THIS ROW PROVES NOTHING END TO END: no Playwright journey, no live drive, no docs - those are the parent task-84cd2e55aae942cf8d56657308215fde. No Needs-you edit and no Runs edit either; both are the parent's, and NEEDS_YOU_KINDS is being extended by two other rows concurrently.",
    "THIS ROW DOES NOT REBUILD: an infrastructure defect is task-3706b7e7's, a deploy-effect defect is task-04b3ce7e's chain. Comment there and report_blocked.",
    "ASCII-only sources and apostrophe-free JSX text; check the testid namespace is free before claiming it; keep every production file under 250 lines."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE, AND READ THE ROW'S OWN CORRECTION FIRST. NO EDITS. (a) THE DESCRIPTION TAIL IS CORRUPT AND THE ROW SAYS SO: architect-fa3a3e17's comment on this row records that a stray `</description><definitionOfDone>placeholder</definitionOfDone>` fragment leaked into the description text. IGNORE IT. The authoritative DoD is the seven-item `definitionOfDone` FIELD on the task record, beginning \"The Deployments card renders PER ENVIRONMENT\". Everything in the description ABOVE the fragment is intact and intended. Do not guess at what \"placeholder\" meant and do not treat it as unspecified scope. (b) CONFIRM THE PREREQUISITE. task-04b3ce7e publishes the deploy command kinds this card dispatches. Prove it at the tree: `git grep -n 'deployment\\.deploy' apps/daemon/src/daemon-command-vocabulary.ts` must hit. If it does not, moe.report_blocked with blockedOnTaskIds ['task-04b3ce7e349743c2a156d32ee5550ba5'] and stop - a card that spends an offer no registry serves renders a button that cannot work. Read that row's completionSummary from epicSiblings for the exact kind names and payload keys rather than retyping them from this description. (c) RE-CONFIRM THE TESTID NAMESPACE, which the row asks for by name because `cr.board.*` was taken once already: `git grep -c 'cr\\.deploy' -- apps tests`. I re-measured at plan time and it returned NOTHING - the namespace is free. Re-check anyway; a peer may have claimed it since. (d) DECIDE WHERE THE CARD MOUNTS, and settle it by reading rather than assuming. `board-screen.tsx:17` (237 lines) and `goal-nodes.tsx:11` (97 lines) BOTH import `{ GoalPublish, publishOffer }`. Determine which is the OPENED GOAL surface and mount there; say which you chose and how you can tell in the step note. Also check `goal-status-strip.tsx:33`, which carries a `publish: \"cr-goal-publish\"` entry - decide whether a Deployments card is expected to join that map, and state the decision either way rather than leaving it unconsidered. (e) BASELINE (global rail 3), standalone foreground, quoting each count line: `pnpm --filter @moe/control-room test`, `pnpm typecheck`. (f) `git status --porcelain -- apps/control-room/src/v2/` and note peer dirt; several goals/ files were dirty with another session's work earlier today, so read before editing and never revert.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "MEASURED AT CLAIM, NO EDITS. Both QA findings reproduce exactly, and one new measurement decides DoD 5.\n\n(1) DoD 1 CONFIRMED. `git grep -n createDeployPort -- apps tests` returns ONE line: deploy-port.ts:51, the definition. Zero callers in production and in test. Every test arm hand-builds a DeployPort literal (goal-deployments.test.tsx:68, :308, :338), so no arm reaches `spendOffer`. QA's swap argument holds: at deploy-port.ts:63 `affordance`/`payload` are both Readonly<Record<string, unknown>> and `correlationPrefix`/`layer` are both string, so either pair can be transposed and still compile.\n\n(2) DoD 5 CONFIRMED. `setTarget` appears only as its own declaration (deploy-port.ts:28, :43, :53, :56) and three test stubs (:69, :309, :339). No production caller.\n\n(3) NEW MEASUREMENT THAT DECIDES DoD 5, and QA could not have had it: `git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests returns **ZERO**. The daemon's affordance surface offers NO deployment kind at all -- not `deployment.set_target`, and not even `deployment.deploy`. `spendOffer` spends an OFFER, so a real `port.setTarget` dispatch is not merely unwired, it is IMPOSSIBLE today: there is no set_target affordance on any frame to spend. QA's first alternative (\"wire a real control through port.setTarget with an arm\") cannot be executed honestly; its second (\"name the row that owns it and DELETE the dead wire\") is the only truthful one. Owners named: the daemon write side is task-358b6ec8 (REVIEW, \"deployment.set_target writes the durable target\"); the browser affordance would belong to the containerized-deployment parent task-5d309484 (WORKING). No control-room row owns a set-target form.\n\n(4) DISCLOSURE CONFIRMED. board-screen.tsx BoardScreen accepts and renders `deploying` (:50, :107-110), but LiveBoard destructures at :205 without it and does not pass it at :234-246, while it does pass `publishing` at :241. The card has no path from the shipped app. board-screen.tsx IS in declaredPaths, so the passthrough is mine to add; cordum-app.tsx is not and I will not touch it.\n\n(5) SIZES, all under the 250 cap: deploy-port.ts 65, goal-deployments.tsx 246, board-screen.tsx 248. The `cr.deploy` testid namespace is used only by this row's two files (50 hits in the test, 6 in the component).",
      "startedAt": "2026-09-06T13:21:41.111Z",
      "completedAt": "2026-09-06T13:22:05.165Z"
    },
    {
      "stepId": "step-2",
      "description": "THE PORT. NEW FILE `apps/control-room/src/v2/goals/deploy-port.ts`, mirroring `publish-port.ts` - which is 29 lines and is the whole decision wire. Copy its SHAPE exactly: import `spendOffer` from \"../approvals/offer-wire.js\", declare the command kind and a `DEPLOY_LAYER` const, expose `createDeployPort(wire)` returning a frozen object whose `submit` calls `spendOffer(wire, KIND, affordance, payload, \"ui-deploy\", DEPLOY_LAYER)`. A SECOND DECISION WIRE IS A DESIGN REGRESSION (rail 2) - no fetch here, no dispatch inside any component. THE PAYLOAD IS WHERE THIS DIFFERS FROM PUBLISH AND IT MATTERS: publish carries `{goalId, remoteUrl}` for ONE target; deploy carries an ENVIRONMENT, and the environment is the safety-critical field. Take it as an explicit parameter, never defaulted and never inferred from component state - `submit(affordance, goalId, environment)`. Read publish-port.ts's docblock before writing yours: it explains why `remoteUrl` is ALWAYS SENT even when null (a missing key arrives as `undefined` and the daemon reads that as malformed, not as a default). Check task-04b3ce7e's delivered payload roster for the same trap and send every key the roster admits. Write the docblock in that file's voice, stating what the environment means and that nothing is deployed here - the daemon's effect runner performs the deploy and the receipt says what became of it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.ts"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/deploy-port.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.ts"
      ],
      "note": "THE PORT, with the DEAD WIRE REMOVED (QA's DoD 5 finding, second alternative taken).\n\nDELETED: `SET_TARGET_COMMAND_KIND`, `DeployTargetInput`, `DeployPort.setTarget` and its `createDeployPort` implementation. The port is now `submit` alone, spending the deploy offer through `spendOffer(wire, DEPLOY_COMMAND_KIND, affordance, { environment, sha }, \"ui-deploy\", DEPLOY_LAYER)`.\n\nWHY THE DELETE AND NOT THE WIRING, stated so a re-reader does not restore it: `spendOffer` spends an OFFER, and `git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests returns ZERO at this tree. The daemon's affordance surface emits no deployment kind at all, so there is no `deployment.set_target` affordance anywhere for a control to spend. Wiring a control would have meant either spending the DEPLOY offer for a set_target command -- a different decision on the wrong affordance -- or inventing an offer the daemon never made. Both are worse than the static note QA rejected. An exported kind wired to nothing is rail 2's \"second decision wire in waiting\", so it is gone rather than parked.\n\nOWNERS NAMED, as QA's alternative requires: the `deployment.set_target` command belongs to task-358b6ec8 (REVIEW) on the daemon side; a browser affordance for it belongs to the containerized-deployment parent task-5d309484 (WORKING), which is where the surface that offers it lands. No control-room row owns a set-target form today.\n\nThe docblock now carries that reasoning inline, including the grep that establishes it, so the next reader sees why the method is absent rather than assuming it was forgotten.\n\nPAYLOAD RE-VERIFIED against the daemon, not retyped from the plan: daemon-command-payload-keys.ts:173 is `\"deployment.deploy\": [\"environment\", \"sha\"]`, exactly what `submit` sends. deploy-port.ts is now 56 lines, under the 250 cap.",
      "startedAt": "2026-09-06T13:22:10.714Z",
      "completedAt": "2026-09-06T13:22:45.355Z"
    },
    {
      "stepId": "step-3",
      "description": "THE CARD, AND THE ONE BUG THAT COPYING THE MODEL PRODUCES. NEW FILE `apps/control-room/src/v2/goals/goal-deployments.tsx` (goal-publish.tsx is 173 lines; stay at or under 250, rail 5). Mirror its structure: an offer-discovery export beside the component (`publishOffer` at :36-41 finds the offer by `commandKind` AND `targetAggregateId`), `useState` for armed/busy/answer, an `ActionButton` arm/confirm, a cancel button while armed, and an `aria-live` answer note. READ THIS BEFORE WRITING THE ARM. goal-publish.tsx:147 is `onClick={() => { if (!armed) { setArmed(true); return; } decide(); }}` with a BOOLEAN `armed`. That is safe for PUBLISH because there is exactly ONE target. THIS CARD HAS TWO ENVIRONMENTS ON THE SAME SURFACE, so a boolean `armed` shared across both rows is the row's safety property inverted: arm PREVIEW, then click PRODUCTION, and `armed` is already true - it dispatches PRODUCTION IMMEDIATELY, with no confirm, from one click on a button the operator has never confirmed. That is DoD-2's failure, and copying the model faithfully is exactly how you get it. ARM STATE MUST BE KEYED BY ENVIRONMENT: hold `armed: string | null` (the environment name), and on a click where `armed !== thisEnvironment` RE-ARM to this environment rather than confirming. Only a click where `armed === thisEnvironment` dispatches. THE CONFIRM LABEL NAMES THE ENVIRONMENT (DoD-2): publish's is `Confirm: push to ${remote}`; yours must carry the environment name itself, so the rendered text of a production confirm cannot read like a preview one. THE PRODUCTION RELEASE-DECISION LINE (DoD-3) IS PART OF THE CONFIRM, NOT A FOOTNOTE: when the environment is production, the confirm step states whether a release decision exists on the goal - cited when one does, stated plainly when none does. Both wordings are asserted in step 4, so write them as two explicit strings rather than a conditional fragment that renders empty. THE CARD IS ABSENT WHEN THERE IS NO OFFER (DoD-4): return null, never a disabled button - the test queries for the testid and expects nothing. NO BROWSER-LOCAL STATE: no localStorage, no sessionStorage (step 4 pins this by source text). ASCII-ONLY AND NO APOSTROPHE IN JSX TEXT (rail 5) - the scheduler package-boundary scan reds on an apostrophe, so write \"the operator has not decided\" and never \"operator's decision\"; check the diff for smart quotes and dashes pasted from a description.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.tsx"
      ],
      "note": "THE CARD, ONE EDIT ONLY. QA said the card is good work and its safety property is genuinely proved, and told me not to restructure it. I did not: the environment-keyed `armed: string | null`, the arm/confirm shape, the release line, the absence-when-no-offer return and the refusal wording are all byte-unchanged.\n\nTHE ONE CHANGE is the DEPLOY_TARGET_MISSING note (was :187-191). It said \"Bind a target for this environment, then deploy again.\" -- which QA correctly read as a prerequisite named with no means, and worse, it implies the operator can act and then retry here. With the set_target wire deleted in step 2 there is no means to offer, so the honest fix is to say so rather than dress a note as an affordance. It now names the environment, names the command that binds it (`deployment.set_target`), and states plainly that this screen cannot bind one yet and it must be bound outside the browser.\n\nThat wording follows the file's own convention: `releaseLine` (:109-113) already writes the ABSENCE as an explicit sentence rather than a blank, on the reasoning that a blank reads as \"not applicable\" instead of \"nobody has done this\". A note promising a retry that cannot succeed is the same failure one layer out.\n\nRAIL 5 HELD: ASCII only, no apostrophe in JSX text (the string is assembled with template + concatenation, no contractions, no smart quotes or dashes). The testid `cr.deploy.<environment>.settarget` is unchanged, so the DoD-4-style absence assertion for non-TARGET_MISSING rows still reads the same key.\n\ngoal-deployments.tsx is now 252 lines against the 250 target -- OVER BY TWO, entirely from the five-line explanatory comment I added above the note. I will settle this in step 5 rather than leave it: the comment is the part that can shrink, and I would rather cut prose than cut the operator sentence that discharges the finding. Flagging it here so it is not discovered as a surprise at the gate.",
      "startedAt": "2026-09-06T13:22:55.636Z",
      "completedAt": "2026-09-06T13:23:16.392Z"
    },
    {
      "stepId": "step-4",
      "description": "THE TESTS, SAFETY ARM FIRST. NEW FILE `apps/control-room/src/v2/goals/goal-deployments.test.tsx`, modelled on goal-publish.test.tsx. (1) DoD-2, THE SAFETY ARM AND THE ROW'S MOST VALUABLE ARTIFACT, in two parts: (a) the rendered confirm text for PRODUCTION contains the environment name and is distinguishable from PREVIEW's; (b) ARM PREVIEW, THEN CLICK PRODUCTION, and assert NO dispatch occurred - the production button must move to armed, not fire. Assert on what the port RECEIVED (spy the port and check its calls), not on the button's label: a label can read correctly while the dispatch carries the other environment. This arm is what a boolean `armed` fails, and it must be written so that it does fail against one. (2) DoD-3: a production confirm with a release decision present CITES it; with none it states so plainly. Both wordings, both asserted - a conditional that renders empty passes an \"is present\" check while telling the operator nothing. (3) DoD-4: with no deploy offer on the frame, `screen.queryByTestId(\"cr.deploy.root\")` is NULL - absence by query, never a disabled attribute (goal-publish.test.tsx:200 is the exact precedent, `queryByTestId(...).toBeNull()`). (4) DoD-5: all four refusal codes render VERBATIM with operator words - DEPLOY_TARGET_MISSING additionally offers a way to set a target, and DEPLOY_BUILD_FAILED carries the tool's last stderr line, asserted with a distinctive planted string so a generic message cannot pass. (5) DoD-1: the card renders target, sha, time, url and status PER ENVIRONMENT - assert with two environments present that each row carries its own values and they are not crossed. (6) THE SOURCE-TEXT ARM, copied from goal-publish.test.tsx:204-215: readFileSync the component and assert `not.toContain` for localStorage and sessionStorage. Note the idiom in that file - a retired key is ASSEMBLED (`[\"moe\",\"publish\",\"remoteUrl\"].join(\".\")`) rather than spelled, so a repo-wide grep for it stays at zero; if you pin any string that way, use the same trick and say why in a comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "note": "THE ARM QA ASKED FOR, PLUS TWO MORE, AND ALL THREE PROVEN TO BITE BY DRILL.\n\nNEW: `wireWith(response)` (mirrors goal-publish.test.tsx:61-72) and a describe block \"the card dispatches through the REAL deploy port (DoD 1)\" that renders `createDeployPort(wire)` rather than a hand-built literal. Three arms:\n(1) spends the offer as `deployment.deploy` with payload EXACTLY `{environment, sha}` -- `toEqual`, not `toMatchObject`, because the daemon decoder is exact-arity (payload-keys.ts:173) so an EXTRA key is malformed too and toMatchObject would pass one. Also asserts `affordance` is the OFFER by identity, and `correlationId` matches /^ui-deploy-[0-9a-f]{16}$/.\n(2) an unreadable daemon answer surfaces layer `CONTROL_ROOM_DEPLOY` -- the one path where spendOffer returns the LAYER argument it was given, pinning the fourth positional argument from the other side.\n(3) a wire whose command roster lacks the kind refuses OFFER_KIND_UNBUILDABLE and sends nothing.\n\nDRILLS -- I did not trust the arms, I broke the production file three times and watched each go red. Backups to %TEMP% suffixed by worker id BEFORE mutating; restores from the BACKUP, never from git (the tree carries other rows' uncommitted work); byte-identity proven by SHA256 in a call separate from the restore.\n\nDRILL 1, transpose `affordance` and `payload` at deploy-port.ts (QA's exact hypothetical, both Readonly<Record<string, unknown>>): RED. `AssertionError: expected { ...(6) } to deeply equal { environment: 'production', ...(1) } - Expected + \"expectedVersion\": 3`. 1 failed | 22 passed.\nDRILL 2, transpose `correlationPrefix` and `layer` (both string, QA's other hypothetical): RED, TWO arms. `expected 'CONTROL_ROOM_DEPLOY-feb0d6f82c59a701' to match /^ui-deploy-[0-9a-f]{16}$/u` and `expected '...' to contain 'CONTROL_ROOM_DEPLOY'`. 2 failed | 21 passed.\nDRILL 3, revert the DoD-5 note to the wording QA rejected: RED. `expected 'Bind a target for this environment, t...' to contain 'preview'`. 1 failed | 22 passed.\n\nQA's premise was that either swap \"compiles clean and keeps 2135 tests green\". Both now fail, and each fails on the arm written for it rather than incidentally.\n\nRESTORE PROOFS. deploy-port.ts SHA256 02CEDB9A0FC8693A3F5F4525C40FA4BC6ABC8A7264F8298E7E29FEA7F046C4BD before drill 1, after drill 1, and after drill 2 -- identical all three times. goal-deployments.tsx SHA256 5A68156B3AAE294192E4529E0BF774D3A7C453EA19591703668B3BC9AB65235B before and after drill 3.\n\nALSO IN THIS FILE: the three `setTarget:` stubs (:69, :309, :339) are gone with the member they stubbed, and the DEPLOY_TARGET_MISSING arm now asserts the environment name, the command that binds a target, the honest limit (\"cannot bind one yet\") AND `not.toContain(\"then deploy again\")` -- a negative arm, so the rejected sentence cannot come back silently.\n\nGREEN AFTER ALL RESTORES: `pnpm --filter @moe/control-room exec vitest run --maxWorkers=1 src/v2/goals/goal-deployments.test.tsx` -- Test Files 1 passed (1), Tests 23 passed (23), EXIT 0 (was 20 tests before this step).",
      "startedAt": "2026-09-06T13:23:27.754Z",
      "completedAt": "2026-09-06T13:26:43.225Z"
    },
    {
      "stepId": "step-5",
      "description": "MOUNT IT, DRILL IT, GATE IT. (a) MOUNT the card on the opened-goal surface chosen in step 1(d), following how `GoalPublish` is composed there (board-screen.tsx:17 or goal-nodes.tsx:11), and pass it the offer discovered by the export from step 3. Keep the host file's growth to the composition itself - board-screen.tsx is 237 lines against a 250 target, so it has ~13 lines of headroom and nothing more; if the mount needs more than that, the wiring belongs in the card, not the host. (b) DRILLS. `git status --porcelain` the exact mutated paths FIRST. THE THREE FILES THIS ROW CREATES ARE NEW AND UNTRACKED (`??`), so `git diff` is BLIND to them - back each up out of tree (%TEMP%, suffixed by worker id) and prove restoration by SHA256 equality in a call SEPARATE from the restore. The host file you mount into IS tracked and takes an empty `git diff`. Say which proof applies to which path. TWO DRILLS: (i) THE SAFETY DRILL, and the reason this row exists - replace the environment-keyed arm state with a BOOLEAN `armed` (the shape goal-publish.tsx uses) and confirm the DoD-2 cross-environment arm goes RED. Quote it. If it stays green, that arm is not testing the property and must be fixed before you continue: a safety arm that never failed is not evidence. (ii) Make the production release-decision line render empty when no decision exists and confirm the DoD-3 \"states plainly\" arm reds - an absent line and a line saying \"no release decision\" are different facts to an operator. (c) GATES - standalone foreground, one command per call, no pipes and no `;` chains, quoting each `Test Files N passed` count line: `pnpm --filter @moe/control-room test` (the ONLY leg that sees any file in this row - vitest.config.ts:32 has no `apps/**`, so root `pnpm test` runs zero tests here and must not be quoted as evidence), and `pnpm typecheck`. `wc -l` every production file and quote it (DoD-6). Attribute red by failing paths at HEAD minus the step-1 baseline, intersected with this row's owned paths; that intersection must be EMPTY, and be careful separating peer red in goals/ from your own. (d) ADVERSARIAL PASS on this diff only: arm production, then arm preview, then click production - what dispatches; a deploy offer present for preview but NOT production (does the card render one row, or a production row with no working button); a refusal arriving while the other environment is armed - does it clear the wrong row's state; an environment name arriving from the daemon that is neither preview nor production; a double-click on a confirmed button (two dispatches?); an apostrophe or non-ASCII character anywhere in the diff. (e) COMPLETION RECORD must state: that the arm state is KEYED BY ENVIRONMENT and why a boolean is unsafe here, with drill (i)'s red quoted; which surface you mounted on and how you determined it was the opened goal; the `goal-status-strip.tsx:33` decision either way; the `wc -l` of every production file; and that no Needs-you, Runs, Playwright, docs or daemon file was touched - `git status --porcelain` proves it.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "apps/control-room/src/v2/board/board-screen.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx",
        "apps/control-room/src/v2/goals/deploy-port.ts"
      ],
      "note": "MOUNT CLOSED, DRILLED, GATED.\n\n(a) THE MOUNT -- QA's disclosed-not-charged finding is fixed in the owned file. BoardScreen already accepted and rendered `deploying` (:107-110), but LiveBoard -- what cordum-app.tsx:293 actually renders -- neither accepted nor forwarded it, so the card had NO path from the shipped app: a seam, not a mount, exactly as QA said. Added to board-screen.tsx: `deploying` on LiveBoardProps, the destructure, and `deploying={deploying}` on the BoardScreen call. Forwarded VERBATIM, unlike `publishing` which LiveBoard joins with its own remote read -- there is no board-owned read for deployments. cordum-app.tsx NOT touched: outside declaredPaths and carrying another row's uncommitted work.\n\n(b) DRILLS -- six in total across this step and step 4, every one caught, every one restored from an out-of-tree %TEMP% backup suffixed by worker id and proven byte-identical by SHA256 in a call SEPARATE from the restore. Both files here are TRACKED, but I used backups anyway rather than `git diff`/`git checkout`: this tree carries several rows' uncommitted work, and a bare `git diff` is not even a valid proof when a peer wrapper sweep has staged the path.\n\nDRILL (i), THE SAFETY DRILL the step names -- replaced the environment-keyed `armed: string | null` with the shared BOOLEAN `armed` that goal-publish.tsx uses (all five sites: state, isArmed, the arm branch, the dispatch reset, the cancel). FOUR arms went RED, and the decisive one is verbatim:\n  `ARMING PREVIEW THEN CLICKING PRODUCTION DISPATCHES NOTHING - it arms production instead`\n  `AssertionError: expected [ { environment: 'production', ...(1) } ] to deeply equal []`\nThat is the row's whole reason to exist, reproduced: with a boolean, arming preview and then clicking production DISPATCHES PRODUCTION off a button the operator never confirmed. Also red: the DoD-2 label arm, the DoD-3 release-row arm, and the production-then-preview ordering arm. 4 failed | 20 passed.\n\nDRILL (ii) -- made `releaseLine` return \"\" when no decision exists. RED: `expected '' to contain 'No release decision on this goal'`. 2 failed | 22 passed. An absent line and a line saying \"nobody has decided\" are different facts to an operator, and the arm knows it.\n\n(c) GATES, standalone foreground, after every restore:\n  `pnpm --filter @moe/control-room test` -- **Test Files 172 passed (172), Tests 2139 passed (2139)**, EXIT 0 (2135 at QA's re-run; +4 is my three DoD-1 arms plus the adversarial in-flight arm).\n  `pnpm typecheck` -- EXIT 0, every project Done with its `$ tsc` echo.\nRoot `pnpm test` deliberately NOT quoted: vitest.config.ts:32 has no `apps/**`, so it sees no file in this row.\n\nwc -l (DoD 6): goal-deployments.tsx **250**, board-screen.tsx **250**, deploy-port.ts **56**. All at or under the cap. Both 250s were over mid-step (253 and 252) from my own comments; I cut prose rather than the operator sentence or the mount, and said so in step 3 instead of letting the gate find it. Non-ASCII scan `grep -n '[^ -~]'` on all three production files: ZERO hits, so no apostrophe or smart quote for the scheduler package-boundary scan.\n\n(d) ADVERSARIAL PASS, and it found a real gap I then closed. Arm production/preview/production: covered, no dispatch. Environment neither preview nor production: covered. Double-click on a confirmed button: covered. THE GAP: nothing proved a deploy IN FLIGHT blocks arming the OTHER environment -- only that it blocks re-clicking its own. A card that disables just the busy row leaves the operator one click from a production deploy while a preview deploy is still running. Added that arm, then drilled it (`deployable` changed to `busy !== view.environment`): RED, `expected false to be true`. Restored, SHA256 identical. On a deploy offer present for preview but not production: not reachable -- `deployOffer` matches one offer per GOAL by targetAggregateId `deploy:${goalId}`, not per environment, so the rows come from the `environments` prop and cannot diverge that way. On a refusal landing while the other row is armed: cannot happen, because every button is disabled while any dispatch is in flight -- which is now the arm above.\n\n(e) FOR THE RECORD: the arm state is KEYED BY ENVIRONMENT and a boolean is unsafe here because two environments share one surface, proven by drill (i) above. Mounted on board-screen.tsx, which is the opened-goal surface: it is what cordum-app.tsx renders as LiveBoard and it already hosts GoalPublish the same way. goal-status-strip.tsx:33 -- decided NOT to add a `deploy` entry: that map keys the goal's own lifecycle standing, and a deployment is an act on the product a goal produced rather than a stage of the goal; adding it would put a per-environment fact in a single-valued strip. `git status --porcelain` shows this row touched exactly its four declared paths. The other dirty control-room paths (cordum-app.tsx, cordum-app.test.tsx, live-bootstrap-receipt.ts, v2/products/) are task-80322112's and are NOT in this commit. No Needs",
      "startedAt": "2026-09-06T13:26:48.268Z",
      "completedAt": "2026-09-06T13:33:04.971Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "REJECTED on DoD 1 and DoD 5. FULL EVIDENCE IS IN comment-915f456288b54abcb360e38d5a4aa49a - read it first; this field caps at 2000 chars.\n\nThe card is good work and its safety property is genuinely proved. Do NOT restructure it: keep the environment-keyed `armed: string | null` and the arm/confirm shape verbatim.\n\nI RE-RAN BOTH GATES FOREGROUND AND REPRODUCED YOUR NUMBERS: control-room `Test Files 172 passed (172)`, `Tests 2135 passed (2135)`; `pnpm typecheck` all Done with the `$ tsc` echo. DoD 2, 3, 4, 6, 7 MET (measurements in the comment). Size is not a finding.\n\nDoD 1 FAILS. `git grep -n 'createDeployPort' -- apps tests` returns ONE line: the definition at deploy-port.ts:51. Nothing constructs it, in production or in test - every arm hand-builds a DeployPort literal (test:68, :308, :338), so no test reaches `spendOffer`. Your code IS correct (payload keys checked against daemon-command-payload-keys.ts:173), but that rests on my reading, not an arm. spendOffer's affordance/payload are both Record<string,unknown> and correlationPrefix/layer both string, so swapping either pair at deploy-port.ts:63 compiles clean and keeps 2135 tests green. The model you were told to mirror DOES prove this: goal-publish.test.tsx:104 renders the real `createPublishPort(wire)` over a fake wire. FIX: one arm doing the same, asserting the wire got kind `deployment.deploy` and payload exactly `{environment, sha}`.\n\nDoD 5 FAILS. \"offers a way to set a target\" is static text at goal-deployments.tsx:187-191 - a prerequisite named with no means. You built `setTarget` and `SET_TARGET_COMMAND_KIND` and never called them. Either wire a real control through `port.setTarget` with an arm, or name the row that owns it and DELETE the dead wire.\n\nDISCLOSED, NOT CHARGED: LiveBoard (board-screen.tsx:204) never forwards `deploying`, so the card cannot render in the shipped app. Add the passthrough there OR comment on parent task-84cd2e55. Do not edit cordum-app.tsx.",
  "createdBy": "ARCHITECT",
  "parentTaskId": "task-84cd2e55aae942cf8d56657308215fde",
  "dependsOn": [
    "task-04b3ce7e349743c2a156d32ee5550ba5"
  ],
  "priority": "HIGH",
  "order": 92.5,
  "comments": [
    {
      "id": "comment-3844487ee74f413190d807d37baa47d8",
      "author": "architect-fa3a3e17",
      "content": "DESCRIPTION TAIL IS CORRUPT - IGNORE THE LAST LINE (architect-fa3a3e17, 2026-09-05, filed immediately after creating this row).\n\nMy create_task call leaked a stray XML fragment into the description. It ends with:\n\n  </description>\n  <definitionOfDone>placeholder</definitionOfDone>\n\nThat is NOT a Definition of Done and it is not scope. IGNORE IT ENTIRELY. There is no field named \"placeholder\" and nothing in this row is unspecified.\n\nTHE AUTHORITATIVE DoD IS THE `definitionOfDone` FIELD on this row, which was written correctly - 7 items, beginning \"The Deployments card renders PER ENVIRONMENT...\" and ending with the gates item. Read it from the task record, not from the description text. Everything in the description ABOVE that stray fragment - the split rationale, the measured `publish-port.ts` seam, the compose sites, the free `cr.deploy` namespace, the source-text assertion pattern at goal-publish.test.tsx:204-215 - is intact and intended.\n\nThere is no tool to edit a task description after creation, which is why this correction is a comment rather than a fix in place. If the corruption makes the row hard to read at claim time, ask a governor to re-file it; do not guess at what the fragment meant.",
      "timestamp": "2026-09-05T19:01:16.815Z"
    },
    {
      "id": "comment-09f979d2ab28428999ac6a65d8210cc1",
      "author": "architect-b63fabf3",
      "content": "THE MODEL THIS ROW IS TOLD TO COPY IS UNSAFE WHEN COPIED. architect-b63fabf3, 2026-09-05, HEAD f3b94eaa. Plan approved: 5 steps, 3 new files plus one composition line.\n\nRail 2 says reuse the publish card's arm/confirm shape, and that is right — but following it faithfully produces exactly the bug DoD-2 exists to prevent.\n\n## The failure, concretely\n\n`goal-publish.tsx:147`:\n\n```js\nonClick={(): void => { if (!armed) { setArmed(true); return; } decide(); }}\n```\n\n`armed` is a **boolean**. That is safe for publish, which has exactly **one** target. This card has **two environments on one surface**, so a shared boolean means:\n\n> arm PREVIEW → click PRODUCTION → `armed` is already `true` → **production dispatches immediately**, from a single click on a button the operator never confirmed.\n\nThat is DoD-2's failure reached by doing what the row asks. The fix is small: hold `armed: string | null` keyed by environment, and a click where `armed !== thisEnvironment` **re-arms** rather than confirming. Drill (i) reproduces the boolean and requires the safety arm to go red against it — a safety arm that has never failed is not evidence.\n\nRejected a per-environment boolean pair: it lets two rows be armed at once, which is a second confusing state on a card whose entire point is that the operator knows which environment they are about to touch. One `armed: string | null` makes \"exactly one environment is armed\" true by construction.\n\n## Two ways the safety arm could pass while the bug ships\n\n- **Asserting the button label.** A label can read \"Confirm: deploy to production\" while the dispatch carries preview. The arm must assert on **what the port received** — which is also why the plan makes `environment` an explicit port parameter rather than read from component state at dispatch time, so the confirmed value and the dispatched value cannot diverge.\n- **DoD-3's two wordings are two facts.** A conditional that renders *empty* when no release decision exists passes an \"is it present\" check while telling the operator nothing. Both strings explicit, both asserted; drill (ii) proves the empty case reds.\n\n## Carried over from the model, worth keeping\n\n`publish-port.ts`'s docblock records that `remoteUrl` is **always sent even as null**, because a missing key arrives as `undefined` and the daemon reads that as malformed rather than as a default. Check task-04b3ce7e's delivered payload roster for the same behaviour before assuming an omitted key is safe.\n\n`publishOffer` matches on **both** `commandKind` and `targetAggregateId` — matching on kind alone would pick up another goal's offer.\n\n## Measurements\n\n- `cr.deploy` namespace: **free** at plan time (`git grep -c` returned nothing). Re-check anyway; the row asks because `cr.board.*` was taken once.\n- **board-screen.tsx is 237 lines against a 250 target** — about 13 lines of headroom. The mount is a composition line; if it needs more than that, the wiring belongs in the card.\n- Two candidate mount sites both import `GoalPublish` (board-screen.tsx:17, goal-nodes.tsx:11). The plan makes the worker decide which is the opened goal and **say how they can tell**, rather than guessing.\n\nNoted your correction comment about the corrupt description tail — step 1 opens with it so a worker does not read `placeholder` as unspecified scope.",
      "timestamp": "2026-09-05T19:09:11.452Z"
    },
    {
      "id": "comment-915f456288b54abcb360e38d5a4aa49a",
      "author": "qa-4f771bb8",
      "content": "QA REVIEW IN FULL (qa-4f771bb8, 2026-09-06, HEAD cf1d4538). The qa_reject reason field caps at 2000 chars; this comment is the evidence and the rejection points at it.\n\nWHAT I RE-RAN MYSELF, both foreground, both reproducing the worker's numbers:\n- `pnpm --filter @moe/control-room test` -> `$ vitest run`, `Test Files  172 passed (172)`, `Tests  2135 passed (2135)`, 24.14s. Matches the verification record exactly.\n- `pnpm typecheck` -> every package `Done`, including `apps/control-room typecheck$ tsc --project tsconfig.json` and `apps/daemon`. The `$ tsc` script echo is present, so the compiler demonstrably ran rather than a cached green.\nDoD 7 MET.\n\nDoD 6 MET, measured not taken on trust. All four files ASCII-clean at cf1d4538 under `LC_ALL=C grep -n '[^ -~\\t]'`. `wc -l` at HEAD: goal-deployments.tsx 246, deploy-port.ts 65, board-screen.tsx 248 - every production file at or under 250. `git grep -ln 'cr\\.deploy' -- apps tests` hits only the row's own two files, so the namespace was free and is now claimed. No apostrophe in any rendered JSX string (MIDDOT comes from glyphs.js, which is why the source stays ASCII).\n\nDIFF: 4 files, exactly declaredPaths, 709 insertions / 1 deletion across 4a0cbf02, 67481f02, cf1d4538. Size is NOT a finding - this board has no per-task net-LOC bar and every production file is under cap; 386 of those lines are the test module.\n\nDoD 2, 3, 4 MET AND WELL BUILT. The environment-keyed `armed: string | null` is the right design and the architect's warning in comment-09f979d2 was heeded rather than pattern-matched. The arm at goal-deployments.test.tsx:374 - arm production, arm preview, click production, expect `calls` empty - is the one I would have asked for. Asserting on what the PORT RECEIVED instead of on a button label is exactly right, and the in-flight disabled arm at :335 closes a real double-dispatch. DoD 5's four codes, the unknown-code verbatim passthrough and the planted stderr line all hold.\n\n=== ISSUE 1, DoD 1: THE SPENDOFFER WIRE HAS NEVER RUN ===\n`git grep -n 'createDeployPort' -- apps tests` returns ONE line: the definition at deploy-port.ts:51. Nothing constructs it. Every test builds a `DeployPort` object literal by hand (goal-deployments.test.tsx:68, :308, :338), so no arm ever reaches `spendOffer`. DoD 1's central clause - \"dispatches the deploy kinds through offer-wire `spendOffer`\" - therefore has zero test evidence.\n\nNot a style point. `spendOffer` is `(wire, kind, affordance, payload, correlationPrefix, layer)` (offer-wire.ts:62-69). `kind` is typed `keyof ControlRoomClientSurface[\"commands\"]`, so typecheck does protect that one argument. But `affordance` and `payload` are BOTH `Readonly<Record<string, unknown>>`, and `correlationPrefix` and `layer` are BOTH `string`. Swap either pair at deploy-port.ts:63 and the file compiles clean and all 2135 tests stay green - while a production deploy dispatches the offer envelope as its payload. `payload` is a plain `Record<string, unknown>`, so nothing checks the keys are the exact-arity pair the daemon demands either.\n\nI VERIFIED BY HAND THAT THE CODE IS CURRENTLY CORRECT, so this is not a bug report: `daemon-command-payload-keys.ts:173` reads `\"deployment.deploy\": [\"environment\", \"sha\"]` and `:172` reads `\"deployment.set_target\": [\"environment\", \"network\", \"sshTarget\", \"url\"]`, and the port sends exactly those. The finding is that correctness on a production-deploy path rests on MY READING rather than on an arm - and the model this row was told to mirror does not rest it there. `goal-publish.test.tsx:104` renders `<GoalPublish port={createPublishPort(wire)} .../>`, the REAL factory over a fake wire, so publish's dispatch shape is proved. This row's is not.\n\nFIX, SMALL AND THE ONLY STRUCTURAL ONE I AM ASKING FOR: one arm mirroring goal-publish.test.tsx:104 - render `<GoalDeployments port={createDeployPort(wire)} .../>` against a fake `OfferWire`, arm and confirm production, and assert what the WIRE received: command kind `deployment.deploy`, payload exactly `{ environment: \"production\", sha: SHA }` with no extra and no missing key, and that affordance and payload did not trade places.\n\n=== ISSUE 2, DoD 5: \"OFFERS A WAY TO SET A TARGET\" IS A SENTENCE, NOT A WAY ===\nDoD 5 requires DEPLOY_TARGET_MISSING to \"offer a way to set a target\". goal-deployments.tsx:187-191 renders the static text \"Bind a target for this environment, then deploy again.\" - a prerequisite named, with no means to satisfy it. The test at goal-deployments.test.tsx:215-224 asserts only that the sentence is present, so it cannot tell a working affordance from a note.\n\nThe tell that the worker read the DoD the same way I do: `DeployPort.setTarget` and `SET_TARGET_COMMAND_KIND` were BUILT (deploy-port.ts:28, :43, :53) with the correct exact-arity payload, and then never called. `git grep -n 'setTarget' -- apps tests` finds the declaration, the implementation, and three test stubs. No caller.\n\nEITHER RESOLUTION IS ACCEPTABLE - pick one and say which:\n(a) Render a real control on the DEPLOY_TARGET_MISSING row that spends the `deployment.set_target` offer from the same frame through `port.setTarget`, with an arm on what the wire received. Watch the cap: goal-deployments.tsx is at 246 with 4 lines of headroom, so this may need to live in deploy-port.ts or a small sibling.\n(b) If the set-target affordance belongs to another row, say so in a comment NAMING that row, and DELETE `setTarget` and `SET_TARGET_COMMAND_KIND` rather than shipping an unreachable second wire. An exported command kind with no caller is what rail 2 is protecting against.\n\n=== ISSUE 3, DISCLOSED, NOT CHARGED: THE MOUNT IS INERT ===\n`cordum-app.tsx:293` renders `<LiveBoard>`, and `LiveBoard` (board-screen.tsx:204-247) neither accepts nor forwards `deploying` - it forwards `publishing` at :241 and stops. So `BoardScreenProps.deploying` has no path from the shipped app and the card renders NEVER in production today. The completion summary calls board-screen.tsx 237 -> 248 a mount; it is a seam, not yet a mount.\n\nI accept that the environments READ is the parent's - the card's own docblock at goal-deployments.tsx:37-38 says so - so I am NOT asking for data. But `LiveBoard` sits in a file this row owns and already edited, and the passthrough is two lines. DO NOT TOUCH cordum-app.tsx: outside declaredPaths and dirty from another row right now. So either add the `deploying` passthrough to `LiveBoard` in board-screen.tsx, or comment on parent task-84cd2e55aae942cf8d56657308215fde that it owns the `LiveBoard` prop as well as the read. Silence is the only wrong answer.\n\n=== ON RE-DELIVERY ===\nNothing already green needs re-proving beyond re-running the two DoD 7 gates. DO NOT RESTRUCTURE THE CARD - the arm/confirm design and the environment-keyed state are correct and I want them kept verbatim. A qa_reject resets every plan step to pending; that is the tool's behaviour, not a judgement on steps 1-5.\n\nCOMMIT GATE, for the record: `task.commits` is empty in get_context and null on disk, but I verified HEAD myself rather than relying on the field. Three commits carry this row - 4a0cbf02, 67481f02, cf1d4538 - and `git branch --contains cf1d4538` returns moe/work-2026-09-04. NO-COMPLETION-COMMIT is NOT part of this rejection.\n\nRAILS CHECKED AND NOT VIOLATED: no Needs-you edit, no Runs edit, no Playwright, no docs, no live drive (rail 3). No rebuild of deploy infrastructure (rail 4). This row adds no new command kind - `deployment.deploy` and `deployment.set_target` already exist in the daemon - so the epic's GENERATED_CONTRACT_DIGEST re-census rail does not fire. `CONTROL_ROOM_DEPLOY` is a new layer literal but control-room has no enumerating layer roster to backfill (`CONTROL_ROOM_PUBLISH` appears in 3 files, none of them a roster), so that is clean too.",
      "timestamp": "2026-09-06T12:48:32.750Z"
    },
    {
      "id": "comment-c1348fb6dff940ac8edc5c11af77dfe4",
      "author": "qa-4f771bb8",
      "content": "QA APPROVAL EVIDENCE IN FULL (qa-4f771bb8, round 2, HEAD a4d1b795). The qa_approve summary caps at 2000 chars and is not readable back from the board, so this comment is the durable record.\n\nGATES RE-RUN BY ME, FOREGROUND, NOT TAKEN FROM THE ROW:\n- `pnpm --filter @moe/control-room test` -> `$ vitest run`, `Test Files  172 passed (172)`, `Tests  2139 passed (2139)`, 32.74s. Round 1 was 2135; +4 is exactly the new arms.\n- `pnpm typecheck` -> every package `Done` with the `$ tsc --project tsconfig.json` echo, including apps/control-room and apps/daemon.\n- Targeted: `vitest run src/v2/goals/goal-deployments.test.tsx` -> `Test Files  1 passed (1)`, `Tests  24 passed (24)`, 2.92s.\n\nDoD 1 CLOSED, BETTER THAN I ASKED FOR. Three arms now render the REAL `createDeployPort` over a fake OfferWire (`wireWith`, mirroring goal-publish.test.tsx:61-72). All four transposable positional arguments are pinned INDEPENDENTLY:\n- payload by `toEqual({environment, sha})` — `toEqual` not `toMatchObject`, correctly reasoned from the daemon's exact-arity roster at daemon-command-payload-keys.ts:173, so an EXTRA key fails as well as a missing one;\n- `affordance` by IDENTITY, `toBe(OFFER)` — this is what catches the affordance/payload swap, and an identity check cannot pass a swapped object;\n- `correlationId` against `/^ui-deploy-[0-9a-f]{16}$/u` — catches the correlationPrefix/layer swap;\n- the LAYER from the other side, via the OFFER_ANSWER_UNREADABLE path surfacing CONTROL_ROOM_DEPLOY.\nPlus an arm proving an unbuildable kind sends nothing, and an arm proving the FIRST click arms without reaching the wire.\n\nI DID NOT RE-RUN THE TRANSPOSITION DRILLS, deliberately, and say so rather than implying I did: a leased convoy suite was running on this shared worktree, and mutating a source file mid-run is the exact hazard I ruled against for other rows today. The worker quoted both reds — `expected { ...(6) } to deeply equal { environment: 'production', ...(1) }` and `expected 'CONTROL_ROOM_DEPLOY-feb0d6f82c59a701' to match /^ui-deploy-.../` — and the second string is precisely what a prefix/layer swap produces, which is a credible red-first receipt. The mutant is restored: deploy-port.ts:54 reads `spendOffer(wire, DEPLOY_COMMAND_KIND, affordance, { environment, sha }, \"ui-deploy\", DEPLOY_LAYER)`.\n\nDoD 5 CLOSED VIA ALTERNATIVE (b), AND I RE-VERIFIED THE MEASUREMENT MYSELF AT CURRENT HEAD — after task-358b6ec8's `6cf7f15c` landed, not before: `git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests returns ZERO, and no offer/affordance/surface emission of either deployment kind exists anywhere in apps/daemon/src. So the daemon offers no deployment affordance on any frame, `port.setTarget` had nothing to spend, and a control here could only ever have spent the DEPLOY offer for a DIFFERENT command. Deleting the wire was the right of my two alternatives. `setTarget`, `SET_TARGET_COMMAND_KIND` and `DeployTargetInput` are gone from apps/control-room by grep; the owners are named in the docblock (task-358b6ec8 daemon side, task-5d309484 for a future browser affordance). The note now names the environment, names `deployment.set_target`, and states the honest limit, and the arm carries a NEGATIVE — `expect(note).not.toContain(\"then deploy again\")` — so the rejected wording cannot return silently. That negative is what makes this a fix rather than a rewording.\n\nDISCLOSED MOUNT CLOSED: `LiveBoard` (board-screen.tsx:204) now accepts and forwards `deploying`, so the card finally has a path from the shipped app. `cordum-app.tsx` was NOT touched as instructed — its 10/0 diff is another row's live-new-product work and contains no deploy reference (grepped).\n\nDoD 6 RE-MEASURED AT HEAD: all four files ASCII-clean under `LC_ALL=C grep -n '[^ -~\\t]'`. `grep -c ''`: deploy-port.ts 56, goal-deployments.tsx 250, board-screen.tsx 250 — AT the cap, not over, and worth flagging to whoever plans the next change here: both production files now have ZERO headroom under the 250 rule. Test file 542, not a production source. `cr.deploy` still hits only this row's files.\n\nDoD 2, 3, 4 unchanged and still proved. The environment-keyed `armed: string | null` and the arm/confirm shape are byte-identical, as I asked. One new safety arm I did not ask for and am glad to see: a deploy IN FLIGHT blocks ARMING the other environment, not merely re-clicking its own — that closes the shape where an operator is one click from production while a preview deploy they cannot see the result of is still running.\n\nWHY I HAND-LANDED THIS ROW. `task.commits` was empty ~5 minutes past `reviewStartedAt` 13:33:25Z, with no rescue ref for this task and no PUSH-BLOCKED / CHECKPOINT-UNPUSHED / MOE_COMMIT_REFUSED banner in any channel. Landing was not discretionary: `deploy-port.ts` and `goal-deployments.test.tsx` are MUTUALLY DEPENDENT this round — the test's `DeployPort` literals no longer carry `setTarget`, which the old interface required — so a partial peer sweep landing one without the other leaves HEAD non-compiling. That is the identical failure that cost this board an hour today at `ec970f50`. Verified on merits FIRST, landed SECOND, per the ratified rule.\n\nTHE LANDING, MEASURED AFTER THE FACT: `a4d1b795` carries FOUR files and no others (`git show --name-only`), exactly the row's declared set; `git diff HEAD` over all four is empty; `git branch --contains a4d1b795` = `moe/work-2026-09-04`; tree `313be452`. Recorded through `moe.record_commit` (outcome committed, kind completion, commitCount 1). NOT pushed from this seat — whoever pushes next carries it. One defect in my own record to disclose: my `record_commit` call returned `completion landing recorded with no paths` because the `paths` argument did not bind; `task.filesModified` was already correct and unaffected, but the commit record itself carries no path list, and `record_commit` dedupes by sha so it cannot be amended. The paths are the four named above.\n\nSize is not a finding: this board has no per-task net-LOC bar, and every production file is at or under 250.",
      "timestamp": "2026-09-06T13:40:24.017Z"
    },
    {
      "id": "comment-7044f01c8a204b62bf984002c592b925",
      "author": "worker-adefb11e",
      "content": "ACCURACY CORRECTION for the reviewer, filed as a comment rather than as an edit because this row is in REVIEW and the change is documentation-only.\n\n`apps/control-room/src/v2/goals/deploy-port.ts` explains why `DeployPort.setTarget` was deleted rather than wired, and names owners:\n\n> \"The command itself belongs to task-358b6ec8 on the daemon side; a browser affordance for it belongs to the containerized-deployment parent task-5d309484, which is where the surface that offers it will land.\"\n\nThe daemon half is right. **The second half is now known more precisely, and the pointer should read `task-9aea412b`.** Governor ruling at 14:07Z (msg in #general): `task-9aea412b` — \"The affordance surface offers `deployment.set_target`, so the binding control has something to spend\" — is the row that produces the OFFER, and `task-4d77ba04` — \"The operator binds a deployment target from the browser, dispatching `deployment.set_target` through offer-wire\" — is the row that builds the browser control and is dependency-gated on it. `task-5d309484` is their epic parent, so it is not wrong, just one level too coarse.\n\nNothing about the DELETE changes: the reasoning that no `deployment.set_target` affordance exists on any frame (`git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests = ZERO) is what the governor's own gating confirms — `task-4d77ba04`'s step 1 says the control cannot be built without that offer, which is the same measurement I made.\n\nTwo options for whoever grades this, both fine by me: (a) approve as-is and let `task-9aea412b` or `task-4d77ba04` tighten the pointer when it edits the file anyway, or (b) reject on this one line and I will change `task-5d309484` to `task-9aea412b` in a one-line edit. I would not hold the row for it — a pointer to the parent epic still leads a reader to the right place, and the substantive claim (no offer exists, so the wire was dead) is unaffected.",
      "timestamp": "2026-09-06T14:07:32.586Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T19:01:03.071Z",
  "updatedAt": "2026-09-06T19:08:22.004Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T19:06:20.557Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 10,
    "rejectCount": 1,
    "reopenCount": 1,
    "doneAt": "2026-09-06T13:41:10.220Z",
    "wallClockMs": 66889663
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [
    {
      "item": "DoD 1: dispatches the deploy kinds through offer-wire spendOffer via a port mirroring publish-port.ts - createDeployPort has zero callers in production and in test, so no arm reaches spendOffer",
      "rejectedAt": "2026-09-06T12:51:01.117Z",
      "rejectedBy": "qa-4f771bb8"
    },
    {
      "item": "DoD 5: DEPLOY_TARGET_MISSING offers a way to set a target - delivered as static text with no affordance; DeployPort.setTarget exists with no caller",
      "rejectedAt": "2026-09-06T12:51:01.117Z",
      "rejectedBy": "qa-4f771bb8"
    }
  ],
  "planSubmittedAt": "2026-09-05T19:08:54.241Z",
  "planningNotes": {
    "approachesConsidered": "NO SPLIT: three new files plus one composition line in a host, all in apps/control-room. The row was already carved out of task-84cd2e55 for exactly this reason and its rails fence the rest (Needs-you, Runs, Playwright, docs, live drive) to the parent.\n\nTHE ONE DESIGN FINDING, and it is the difference between this row shipping its safety property or inverting it: THE MODEL THIS ROW IS TOLD TO COPY IS UNSAFE WHEN COPIED. `goal-publish.tsx:147` implements arm/confirm as `onClick={() => { if (!armed) { setArmed(true); return; } decide(); }}` over a BOOLEAN `armed`. That is correct for publish, which has exactly ONE target. This card has TWO environments on one surface, and a shared boolean means: arm PREVIEW, click PRODUCTION, `armed` is already true, and production dispatches IMMEDIATELY from a single click on a button the operator never confirmed. That is precisely DoD-2's failure, reached by following the row's own instruction faithfully. The plan therefore requires arm state KEYED BY ENVIRONMENT (`armed: string | null`), with a click on a different environment RE-ARMING rather than confirming - and drill (i) reproduces the boolean to prove the safety arm actually fails against it.\nREJECTED - a separate armed boolean per environment: works, but scales wrong and lets two rows be armed at once, which is a second confusing state on a card whose whole point is that the operator knows which environment they are about to touch. One `armed: string | null` makes \"exactly one environment is armed\" true by construction.\nREJECTED - a modal confirm dialog: not the established shape on this board (publish, approve and activate all use inline arm/confirm), and rail 2 says reuse the publish shape. The fix is the state key, not a new interaction.\nREJECTED - inferring the environment from component state at dispatch time: the port takes it as an explicit parameter so the value dispatched and the value confirmed cannot diverge. That is also why step 4's safety arm asserts on what the PORT RECEIVED rather than on the button label - a label can read correctly while the dispatch carries the other environment.\nREJECTED - putting the release-decision read in this row: DoD-3 asks the card to SHOW it; the decision is read through the deploy chain's port, and Gate 3 itself is task-2a345b2e. This row renders what it is handed.",
    "codebaseInsights": "MEASURED 2026-09-05 by architect-b63fabf3 at HEAD f3b94eaa on moe/work-2026-09-04.\n\n1. THE ROW'S OWN DESCRIPTION IS PARTLY CORRUPT and its author filed a comment saying so: a stray `</description><definitionOfDone>placeholder</definitionOfDone>` fragment leaked into the text. The seven-item `definitionOfDone` FIELD is authoritative. Step 1 says this first so a worker does not treat \"placeholder\" as unspecified scope.\n\n2. THE SEAM IS TINY AND EXACT. `publish-port.ts` is 29 lines: `spendOffer` imported from ../approvals/offer-wire.js:1, `PUBLISH_COMMAND_KIND`:17, `createPublishPort(wire)`:24, and the one call at :27 - `spendOffer(wire, KIND, affordance, payload, \"ui-publish\", PUBLISH_LAYER)`. Its docblock carries a trap worth copying: `remoteUrl` is ALWAYS SENT even as null, because a MISSING key arrives as `undefined` and the daemon reads that as malformed rather than as a default.\n\n3. THE UNSAFE-WHEN-COPIED ARM: goal-publish.tsx:147 `onClick={(): void => { if (!armed) { setArmed(true); return; } decide(); }}`, label at :151 `armed ? \\`Confirm: push to ${...}\\``, cancel at :154-156 `{armed && !busy ? <ActionButton onClick={() => setArmed(false)} testId=\"cr.publish.cancel\">`. State at :74-78 (`typed`, `changing`, `armed`, `busy`, `answer`). Boolean `armed` is safe for one target and unsafe for two.\n\n4. OFFER DISCOVERY IS AN EXPORT BESIDE THE COMPONENT: `publishOffer(frame, goalId)` at :36-41 matches on BOTH `commandKind` and `targetAggregateId === \\`publish:${goalId}\\`` - copy that two-field match; matching on kind alone would pick up another goal's offer.\n\n5. THE ABSENCE PRECEDENT DoD-4 ASKS FOR: goal-publish.test.tsx:200 `expect(screen.queryByTestId(\"cr.publish.button\")).toBeNull()`.\n\n6. THE SOURCE-TEXT ARM: goal-publish.test.tsx:204-215 readFileSync's the component and asserts `not.toContain` for localStorage/sessionStorage, with the retired key ASSEMBLED (`[\"moe\",\"publish\",\"remoteUrl\"].join(\".\")`) so a repo-wide grep for it stays at zero. Copy the idiom AND the reason.\n\n7. THE NAMESPACE IS FREE: `git grep -c 'cr\\.deploy' -- apps tests` returned nothing at plan time. The row asks for a re-check because `cr.board.*` was taken once.\n\n8. MOUNT SITES, both real and both importing GoalPublish: board-screen.tsx:17 (237 lines - only ~13 of headroom against the 250 target) and goal-nodes.tsx:11 (97 lines). goal-status-strip.tsx:33 carries `publish: \"cr-goal-publish\"`. The plan makes the worker decide and state which, rather than guessing.\n\n9. GATE REACH: vitest.config.ts:32 include = adapters/**, packages/**, tests/**, tools/** - no `apps/**`. `pnpm --filter @moe/control-room test` is the only leg that sees any file in this row.",
    "risks": "FIRST - COPYING THE MODEL FAITHFULLY PRODUCES THE EXACT BUG THIS ROW EXISTS TO PREVENT. A boolean `armed` shared across two environment rows means arming PREVIEW and clicking PRODUCTION dispatches production with no confirm. Key the arm state by environment. Drill (i) reproduces the boolean and requires the safety arm to red against it - a safety arm that has never failed is not evidence.\n\nSECOND - THE SAFETY ARM CAN BE WRITTEN VACUOUSLY. Asserting the button LABEL reads \"production\" passes while the dispatch carries preview. Assert on what the PORT RECEIVED. That is also why the environment is an explicit port parameter rather than read from component state at dispatch time.\n\nTHIRD - DoD-3's TWO WORDINGS ARE TWO FACTS. A conditional that renders EMPTY when no release decision exists passes an \"is it present\" check while telling the operator nothing. Write both strings explicitly and assert both; drill (ii) proves the empty case reds.\n\nFOURTH - THE PAYLOAD KEY TRAP. publish-port.ts documents that a missing key arrives as `undefined` and reads as MALFORMED rather than as a default, which is why `remoteUrl` is always sent. Check task-04b3ce7e's delivered payload roster for the same behaviour and send every key it admits.\n\nFIFTH - THE HOST FILE HAS ALMOST NO HEADROOM. board-screen.tsx is 237 lines against a 250 target: the mount is a composition line, not a place to put wiring. If it needs more, the logic belongs in the card.\n\nSIXTH - RAIL 5 IS TWO TRAPS. An apostrophe in JSX text reds the scheduler package-boundary scan, and so does a non-ASCII character. Check the diff for smart quotes and dashes pasted from a description or a task record.\n\nSEVENTH - PEER DIRT IN goals/. Several files under apps/control-room/src/v2/goals/ were modified by another session earlier today. Read before editing, never revert or regenerate from a HEAD copy, and when a gate reds check whether the failing arm names your change before attributing it.\n\nEIGHTH - THIS ROW PROVES NOTHING END TO END (rail 3). No Playwright, no live drive, no docs, no Needs-you edit, no Runs edit - and NEEDS_YOU_KINDS is being extended by two other rows concurrently, so touching it would collide. A defect in the infrastructure or the deploy effect is filed against task-3706b7e7 or task-04b3ce7e, not fixed here.\n\nNINTH - global rail 13 fires for the three new files (untracked, `git diff` blind); the mount host is tracked. Do not apply one restore rule to both.\n\nTENTH - shared checkout. Peer dirt under .moe/ and .grok/ is NEVER a stop condition: note it, never revert, stash or `git add -A`. Report every path you touch in complete_step.modifiedFiles, including the three new files.",
    "keyFiles": [
      "apps/control-room/src/v2/goals/goal-publish.tsx",
      "apps/control-room/src/v2/goals/publish-port.ts",
      "apps/control-room/src/v2/goals/goal-publish.test.tsx",
      "apps/control-room/src/v2/board/board-screen.tsx",
      "apps/control-room/src/v2/goals/goal-nodes.tsx",
      "apps/control-room/src/v2/approvals/offer-wire.ts"
    ]
  },
  "planApprovedAt": "2026-09-05T19:08:56.445Z",
  "contextFetchedBy": [
    "worker-adefb11e",
    "qa-4f771bb8",
    "worker-f72a1438"
  ],
  "workStartedAt": "2026-09-06T12:30:12.855Z",
  "reviewStartedAt": "2026-09-06T13:33:25.367Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": "$ vitest run\n\n RUN  v4.1.10 D:/projexts/moe-next/apps/control-room\n\n Test Files  172 passed (172)\n      Tests  2139 passed (2139)\n   Start at  16:31:45\n   Duration  42.47s (transform 302.71s, setup 0ms, import 490.74s, tests 108.49s, environment 103.74s)\n\nEXIT=0",
    "reportedAt": "2026-09-06T13:33:25.367Z"
  },
  "filesModified": [
    "apps/control-room/src/v2/goals/deploy-port.ts",
    "apps/control-room/src/v2/goals/goal-deployments.tsx",
    "apps/control-room/src/v2/goals/goal-deployments.test.tsx",
    "apps/control-room/src/v2/board/board-screen.tsx"
  ],
  "completionSummary": "Both rejected DoD items fixed, and the disclosed mount closed. The card itself is unchanged apart from one note: QA said the safety property is genuinely proved and not to restructure it, so the environment-keyed `armed: string | null` and the arm/confirm shape are byte-identical.\n\nDoD 1 -- `createDeployPort` had zero callers, so no arm reached `spendOffer`. Added `wireWith()` (mirroring goal-publish.test.tsx:61-72) and three arms rendering the REAL port over a fake OfferWire: payload EXACTLY `{environment, sha}` by `toEqual` (not toMatchObject -- the daemon decoder is exact-arity per payload-keys.ts:173, so an extra key is malformed too); `affordance` is the offer BY IDENTITY; `correlationId` matches /^ui-deploy-[0-9a-f]{16}$/; an unreadable answer surfaces layer CONTROL_ROOM_DEPLOY; an unbuildable kind sends nothing. QA's premise was that transposing either argument pair \"compiles clean and keeps 2135 tests green\" -- I drilled both and both now RED: `expected { ...(6) } to deeply equal { environment: 'production', ...(1) }` and `expected 'CONTROL_ROOM_DEPLOY-feb0d6f82c59a701' to match /^ui-deploy-.../`.\n\nDoD 5 -- took QA's SECOND alternative, and one measurement forced it: `git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests returns ZERO. The daemon offers NO deployment affordance on any frame, so `port.setTarget` had nothing to spend and wiring a control would have meant spending the DEPLOY offer for a different command. Deleted `setTarget`, `SET_TARGET_COMMAND_KIND`, `DeployTargetInput` and three test stubs. Owners named in the docblock: task-358b6ec8 (daemon command), task-5d309484 (a future browser affordance). The note now names the environment, names `deployment.set_target`, and says plainly this screen cannot bind one -- with a NEGATIVE arm on \"then deploy again\" so the rejected sentence cannot return silently.\n\nDISCLOSED MOUNT -- fixed in board-screen.tsx: LiveBoard now accepts and forwards `deploying`. It was a seam, not a mount. cordum-ap",
  "reviewCompletedAt": "2026-09-06T13:41:10.220Z",
  "rejectionDetails": {
    "failedDodItems": [
      "DoD 1: dispatches the deploy kinds through offer-wire spendOffer via a port mirroring publish-port.ts - createDeployPort has zero callers in production and in test, so no arm reaches spendOffer",
      "DoD 5: DEPLOY_TARGET_MISSING offers a way to set a target - delivered as static text with no affordance; DeployPort.setTarget exists with no caller"
    ],
    "issues": [
      {
        "type": "test_failure",
        "file": "apps/control-room/src/v2/goals/goal-deployments.test.tsx",
        "line": 68,
        "description": "No arm exercises the real port: tests hand-build DeployPort literals (:68, :308, :338), so spendOffer is never called. affordance/payload are both Record<string,unknown> and correlationPrefix/layer both string, so swapping either pair at deploy-port.ts:63 compiles clean and keeps 2135 tests green. FIX: one arm mirroring goal-publish.test.tsx:104 - render with createDeployPort(wire) over a fake OfferWire, assert the wire got kind deployment.deploy and payload exactly {environment, sha}."
      },
      {
        "type": "missing_feature",
        "file": "apps/control-room/src/v2/goals/goal-deployments.tsx",
        "line": 187,
        "description": "DoD 5 asks DEPLOY_TARGET_MISSING to offer a WAY to set a target. Only static text 'Bind a target for this environment, then deploy again.' is rendered, and the test at goal-deployments.test.tsx:215 asserts only that the sentence exists - it cannot tell an affordance from a note. Either spend the deployment.set_target offer through port.setTarget with an arm on what the wire received, or name the row that owns it and delete the dead wire."
      },
      {
        "type": "other",
        "file": "apps/control-room/src/v2/goals/deploy-port.ts",
        "line": 43,
        "description": "DeployPort.setTarget and SET_TARGET_COMMAND_KIND have no caller: git grep -n setTarget -- apps tests finds the declaration, the implementation and three test stubs only. An exported command kind wired to nothing is a second decision wire in waiting, which is rail 2's concern. Resolve together with the DoD 5 issue."
      },
      {
        "type": "other",
        "file": "apps/control-room/src/v2/board/board-screen.tsx",
        "line": 204,
        "description": "DISCLOSED, not charged. cordum-app.tsx:293 renders LiveBoard, which neither accepts nor forwards `deploying` (it forwards `publishing` at :241). So BoardScreenProps.deploying has no path from the shipped app and the card renders never in production - the completion summary calls this a mount, but it is a seam. Add the two-line passthrough in this owned file, or comment on parent task-84cd2e55. Do not edit cordum-app.tsx: outside declaredPaths and dirty from another row."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "REJECTED on DoD 1 and DoD 5. FULL EVIDENCE IS IN comment-915f456288b54abcb360e38d5a4aa49a - read it first; this field caps at 2000 chars.\n\nThe card is good work and its safety property is genuinely proved. Do NOT restructure it: keep the environment-keyed `armed: string | null` and the arm/confirm shape verbatim.\n\nI RE-RAN BOTH GATES FOREGROUND AND REPRODUCED YOUR NUMBERS: control-room `Test Files 172 passed (172)`, `Tests 2135 passed (2135)`; `pnpm typecheck` all Done with the `$ tsc` echo. DoD 2, 3, 4, 6, 7 MET (measurements in the comment). Size is not a finding.\n\nDoD 1 FAILS. `git grep -n 'createDeployPort' -- apps tests` returns ONE line: the definition at deploy-port.ts:51. Nothing constructs it, in production or in test - every arm hand-builds a DeployPort literal (test:68, :308, :338), so no test reaches `spendOffer`. Your code IS correct (payload keys checked against daemon-command-payload-keys.ts:173), but that rests on my reading, not an arm. spendOffer's affordance/payload are both Record<string,unknown> and correlationPrefix/layer both string, so swapping either pair at deploy-port.ts:63 compiles clean and keeps 2135 tests green. The model you were told to mirror DOES prove this: goal-publish.test.tsx:104 renders the real `createPublishPort(wire)` over a fake wire. FIX: one arm doing the same, asserting the wire got kind `deployment.deploy` and payload exactly `{environment, sha}`.\n\nDoD 5 FAILS. \"offers a way to set a target\" is static text at goal-deployments.tsx:187-191 - a prerequisite named with no means. You built `setTarget` and `SET_TARGET_COMMAND_KIND` and never called them. Either wire a real control through `port.setTarget` with an arm, or name the row that owns it and DELETE the dead wire.\n\nDISCLOSED, NOT CHARGED: LiveBoard (board-screen.tsx:204) never forwards `deploying`, so the card cannot render in the shipped app. Add the passthrough there OR comment on parent task-84cd2e55. Do not edit cordum-app.tsx.",
      "rejectedAt": "2026-09-06T12:51:01.117Z",
      "reopenCount": 1,
      "failedDodItems": [
        "DoD 1: dispatches the deploy kinds through offer-wire spendOffer via a port mirroring publish-port.ts - createDeployPort has zero callers in production and in test, so no arm reaches spendOffer",
        "DoD 5: DEPLOY_TARGET_MISSING offers a way to set a target - delivered as static text with no affordance; DeployPort.setTarget exists with no caller"
      ],
      "issues": [
        {
          "type": "test_failure",
          "file": "apps/control-room/src/v2/goals/goal-deployments.test.tsx",
          "line": 68,
          "description": "No arm exercises the real port: tests hand-build DeployPort literals (:68, :308, :338), so spendOffer is never called. affordance/payload are both Record<string,unknown> and correlationPrefix/layer both string, so swapping either pair at deploy-port.ts:63 compiles clean and keeps 2135 tests green. FIX: one arm mirroring goal-publish.test.tsx:104 - render with createDeployPort(wire) over a fake OfferWire, assert the wire got kind deployment.deploy and payload exactly {environment, sha}."
        },
        {
          "type": "missing_feature",
          "file": "apps/control-room/src/v2/goals/goal-deployments.tsx",
          "line": 187,
          "description": "DoD 5 asks DEPLOY_TARGET_MISSING to offer a WAY to set a target. Only static text 'Bind a target for this environment, then deploy again.' is rendered, and the test at goal-deployments.test.tsx:215 asserts only that the sentence exists - it cannot tell an affordance from a note. Either spend the deployment.set_target offer through port.setTarget with an arm on what the wire received, or name the row that owns it and delete the dead wire."
        },
        {
          "type": "other",
          "file": "apps/control-room/src/v2/goals/deploy-port.ts",
          "line": 43,
          "description": "DeployPort.setTarget and SET_TARGET_COMMAND_KIND have no caller: git grep -n setTarget -- apps tests finds the declaration, the implementation and three test stubs only. An exported command kind wired to nothing is a second decision wire in waiting, which is rail 2's concern. Resolve together with the DoD 5 issue."
        },
        {
          "type": "other",
          "file": "apps/control-room/src/v2/board/board-screen.tsx",
          "line": 204,
          "description": "DISCLOSED, not charged. cordum-app.tsx:293 renders LiveBoard, which neither accepts nor forwards `deploying` (it forwards `publishing` at :241). So BoardScreenProps.deploying has no path from the shipped app and the card renders never in production - the completion summary calls this a mount, but it is a seam. Add the two-line passthrough in this owned file, or comment on parent task-84cd2e55. Do not edit cordum-app.tsx: outside declaredPaths and dirty from another row."
        }
      ]
    }
  ],
  "commits": [
    {
      "sha": "a4d1b7951c113ea13cc260d1a7bb22bd492173cb",
      "ref": "moe/work-2026-09-04",
      "kind": "completion",
      "role": "qa",
      "sessionId": "qa-4f771bb8@2026-09-06T12:30:36.000Z",
      "paths": [],
      "recordedBy": "qa-4f771bb8",
      "recordedAt": "2026-09-06T13:39:20.686Z",
      "treeId": "313be4526e80183fb00a21465c5db8e27ed1262e",
      "status": "REVIEW",
      "pushed": false,
      "message": "fix(task-c9e76f53010149adb2ba1ba55838f41c): the Deployments card proves its decision wire (retry after qa_reject #1) - QA hand-landing under the ratified rule: verified on merits first, landed second. The delivering session set REVIEW at 13:33:25Z and did not land a completion commit within 5 minutes; deploy-port.ts and goal-deployments.test.tsx are mutually dependent (the test's DeployPort literals no longer carry setTarget, which the old interface required), so a partial sweep would have left HEAD non-compiling.</message>\n<parameter name=\"paths\">[\"apps/control-room/src/v2/goals/deploy-port.ts\", \"apps/control-room/src/v2/goals/goal-deployments.tsx\", \"apps/control-room/src/v2/goals/goal-deployments.test.tsx\", \"apps/control-room/src/v2/board/board-screen.tsx\"]"
    }
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "qa-4f771bb8@2026-09-06T12:30:36.000Z",
    "at": "2026-09-06T13:39:20.686Z"
  },
  "completedAt": "2026-09-06T13:41:10.220Z",
  "reviewSummary": "APPROVED at HEAD a4d1b795. FULL EVIDENCE: comment-c1348fb6dff940ac8edc5c11af77dfe4 (this field caps at 2000 chars).\n\nGATES I RE-RAN FOREGROUND: control-room `Test Files 172 passed (172)`, `Tests 2139 passed (2139)` (round 1 was 2135; +4 = the new arms); `pnpm typecheck` all Done with the `$ tsc` echo; targeted goal-deployments.test.tsx 1 passed / 24 passed.\n\nDoD 1 CLOSED, better than I asked. Three arms render the REAL createDeployPort over a fake OfferWire. All four transposable arguments pinned independently: payload by toEqual (not toMatchObject - the decoder is exact-arity at payload-keys.ts:173); affordance by IDENTITY toBe(OFFER), which defeats the affordance/payload swap; correlationId by /^ui-deploy-[0-9a-f]{16}$/; layer via OFFER_ANSWER_UNREADABLE. Mutant restored at deploy-port.ts:54. I did NOT re-run the transposition drills and say so plainly: a leased convoy was running on this shared worktree, and mutating a source mid-run is the hazard I ruled against for other rows today.\n\nDoD 5 CLOSED via my alternative (b), premise re-verified at CURRENT HEAD after 6cf7f15c: `git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests = ZERO. No deployment kind is offered on any surface, so setTarget had no offer to spend; deleting beat wiring. The note names environment, command and honest limit, with a NEGATIVE arm on \"then deploy again\".\n\nMOUNT CLOSED: LiveBoard forwards `deploying`. cordum-app.tsx untouched. DoD 6: 4 files ASCII-clean; wc -l 56 / 250 / 250 - at the cap, zero headroom left.\n\nI HAND-LANDED a4d1b795: commits empty 5 min past reviewStartedAt, no rescue ref, no PUSH-BLOCKED. Not discretionary - deploy-port.ts and its test are mutually dependent this round, so a partial sweep leaves HEAD non-compiling (the ec970f50 shape). Verified first, landed second. 4 files and no others, `git diff HEAD` empty, branch contains it, recorded via record_commit. Not pushed from this seat."
}