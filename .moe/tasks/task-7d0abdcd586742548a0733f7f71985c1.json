{
  "id": "task-7d0abdcd586742548a0733f7f71985c1",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Publish the runner recovery, evidence and Claude observation surfaces on the @moe/runner root",
  "description": "Objective: publish three committed-but-unreachable @moe/runner subtrees on the package root so J3's launcher and restart boundary can be composed against them. Created by architect-0b81de40 2026-08-09, verified symbol by symbol on disk. This is the FIFTH instance of one recurring defect; the prior four are the template: task-8ee125d0 (@moe/scheduler, 239aa4f), task-53680e91 (@moe/runner supervisor), task-6054520b (@moe/daemon), and the bridge sweeps.\n\nWHY THIS EXISTS. `packages/runner/package.json` pins `\"exports\": {\".\": \"./src/index.ts\"}` - an EXCLUSIVE map, so no deep subpath import reaches around it at runtime or under moduleResolution NodeNext. `packages/runner/src/index.ts` publishes artifacts/, scope/, supervisor/ and workspace/ (126 specifiers) but NOT recovery/, NOT evidence/, and NOT providers/claude/. Those three subtrees are committed, tested and completely unreachable from outside the package.\n\nMEASURED ON DISK 2026-08-09, the unpublished surface:\n  recovery/ - classifyCrash, admitSuccessorOverlap, admitResume, advanceRecoveryDrain; types CrashClassification, OverlapVerdict, ResumeVerdict, DrainAdvance; vocabularies RECOVERY_CONTRACT_VERSION, RECOVERY_OUTCOME_KINDS, RECOVERY_LAYERS, RECOVERY_ERROR_CODES, PREDECESSOR_RELEASES\n  evidence/ - buildEvidenceReceipt, buildVerificationRecipe, recipeSealMatches, rematerializeCandidate, canonicalObligations, observedExecutionRejection; types EvidenceReceipt, VerificationRecipe, CandidateTreePort, RematerializeCandidateInput/Result, ObservedVerifierExecution, ObservedOutput, ExecutionDisposition; vocabularies VERIFICATION_RECIPE_VERSION, EVIDENCE_RECEIPT_VERSION, EVIDENCE_REFUSAL_LAYERS, EXECUTION_DISPOSITIONS and the MAX_EVIDENCE_* limits\n  providers/claude/ - reconcileClaudeRun, ClaudeProcessExit, ClaudeReconciliation, CLAUDE_RECONCILED_OUTCOMES, CLAUDE_RECONCILIATION_VERSION, plus the capability profile surface (CLAUDE_CAPABILITIES, CLAUDE_CAPABILITY_STATUSES, CLAUDE_PROOF_METHODS, CLAUDE_CAPABILITY_PROFILE_VERSION)\n\nTHIS BLOCKS J3. The Foundation canary reports that runner/Claude observation and recovery helpers are \"not composed into a real launcher/restart boundary\". They cannot be: a consumer outside @moe/runner cannot import them at all. Publishing is the prerequisite; the composition itself is a separate follow-on task.\n\nDeliverable: a curated named-export block in packages/runner/src/index.ts, in the style the file already uses - named exports grouped per module, types in the same block via the `type` modifier, every specifier carrying the .js extension per NodeNext. Read the modules and name only what a consumer needs to classify a crash, admit or refuse a successor overlap and a resume, advance a drain, build and verify an evidence receipt and verification recipe, rematerialize a candidate, and reconcile an observed Claude run. Prefer curated named exports over a blanket `export *` so the published seam stays a reviewed decision.\n\nNOT in scope: changing ANY behaviour in any runner module; exporting test-only modules (recovery-test-fixtures.ts, evidence fixtures, claude-probe test helpers - all stay internal); the J3 launcher/restart composition itself; touching apps/daemon; any package.json or lockfile edit.\n\nHard dependencies: none. Every symbol above is committed at HEAD.\n\nOwned paths: packages/runner/src/index.ts and packages/runner/src/index-surface.test.ts (extend the existing surface test).\nVerification: pnpm --filter @moe/runner typecheck &amp;&amp; pnpm --filter @moe/runner test &amp;&amp; pnpm --filter @moe/daemon typecheck",
  "definitionOfDone": [
    "packages/runner/src/index.ts exports the recovery, evidence and Claude observation surfaces such that a consumer can, with NO deep subpath import, classify a crash, admit or refuse a successor overlap and a resume, advance a recovery drain, build and seal-verify a verification recipe, build an evidence receipt, rematerialize a candidate, and reconcile an observed Claude run — naming every parameter type and every union branch of every exported function's return type.",
    "The existing packages/runner/src/index-surface.test.ts is extended to import the package ROOT specifier @moe/runner (never a relative or deep path) and assert per new symbol that the export is present and of the expected typeof, so a dropped export fails by name rather than by a vague count.",
    "The test's set-equality assertion against a hand-written expected export-name list is updated to cover the new symbols, so both a removed export AND an unreviewed addition still go red. The list is hand-transcribed from the modules, never derived from the namespace under test.",
    "Discriminated unions survive publication: for CrashClassification, OverlapVerdict, ResumeVerdict, DrainAdvance and the evidence result unions, a test obtains real values through the published root and asserts each branch is distinguishable by its discriminant — asserted against the production surface, not a locally re-declared type.",
    "No test-only module is exported — recovery-test-fixtures.ts and any evidence or claude test helper stay internal — asserted by the set-equality list.",
    "Zero behaviour change: git diff shows packages/runner/src/index.ts as the only modified non-test file, no .js bridge is added or removed, and the pre-existing runner suite passes with a test count greater than or equal to the pre-task count, stated as an explicit before/after number.",
    "pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck all exit 0, and committed paths are exactly the two owned files."
  ],
  "taskRails": [
    "Re-export only. This task changes what is REACHABLE, never what any function DOES. No .ts file other than index.ts may be edited. If a symbol seems to need a signature change to be exportable, report_blocked rather than editing a runner module.",
    "Read the modules before naming a single symbol and do not copy names from this description without verifying each on disk — recovery/, evidence/ and providers/claude/ landed from three different tasks within days of each other.",
    "Never export a test-only module. recovery-test-fixtures.ts and the evidence/claude test helpers stay internal; exporting fixtures puts test code on the runtime surface.",
    "Prefer curated named exports over a blanket `export *`, so the published seam stays a reviewed decision rather than whatever the modules happen to export next week.",
    "Assert against the production surface obtained through the @moe/runner root import, never against a locally re-declared type or fixture copy.",
    "index.ts already carries 126 specifiers and will grow substantially. Keep it focused: if it passes 250 lines, extract per-area re-export modules (e.g. a recovery surface module) that index.ts re-exports, rather than letting the entry point sprawl past 400.",
    "Any new module created by an extraction needs its sibling .js bridge in the same commit — @moe/runner is bridged and is covered by the repo-wide runtime-loadability gate.",
    "Commit by explicit pathspec only — git commit -- packages/runner/src/index.ts packages/runner/src/index-surface.test.ts. Several agents share this working directory; verify with git show --stat afterwards."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Re-verify the three subtrees and check for name collisions before composing anything. Read every non-test module under recovery/, evidence/ and providers/claude/ and record each exported value and type — these landed from three different tasks within days of each other, so this task's description may already be stale. TWO CHECKS THAT MUST HAPPEN NOW rather than at compile time. First, DUPLICATE NAMES: evidence/ and recovery/ both define failure and layer vocabularies, and a name exported from two modules is a compile error in index.ts that reads like a mystery; list every name and find collisions. If two modules genuinely export the same identifier, report it as a finding rather than papering over it with a rename in index.ts, which would make the published name differ from the module's own. Second, TEST-ONLY MODULES: identify recovery-test-fixtures.ts and any evidence or claude test helper, and mark them as never-export. Then read the existing packages/runner/src/index-surface.test.ts from task-53680e91 to reuse its root-import and set-equality pattern rather than inventing a second idiom. Produce no code.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "Re-verified all three subtrees on disk at HEAD. Findings:\n\nNO NAME COLLISIONS. Checked every candidate name across recovery/, evidence/, providers/claude/ and the 126 specifiers already in index.ts. The layer/failure vocabularies the plan warned about are all distinctly named (RECOVERY_LAYERS vs EVIDENCE_REFUSAL_LAYERS vs SUPERVISOR_LAYERS; RecoveryFailure vs EvidenceFailure vs ClaudeFailure; ExecutionDisposition vs ClaudeStreamDisposition; PREDECESSOR_RELEASES vs supervisor RESOURCE_FACTS). One near-miss: CandidateTreeEntry is defined in evidence/candidate-reconciliation.ts AND re-exported by evidence/candidate-rematerialization.ts (line 30) - exporting it from candidate-rematerialization.js only, so no duplicate specifier.\n\nTEST-ONLY, never exported: recovery/recovery-test-fixtures.ts (Overrides, HELD_EPOCH, HELD_TOKEN, RECONCILED, REGISTRATION, COMMIT, disposition, records, observation, ADVANCED, SETTLED, situation) and supervisor/effect-test-fixtures.ts (already on the existing FIXTURE_NAMES list). evidence/ and providers/claude/ have NO fixture module - their test data is inline in the .test.ts files. Confirmed by disk listing: the only non-test .ts files lacking a .js bridge are recovery-test-fixtures.ts, effect-test-fixtures.ts and the supervisor race-* harness, which is exactly the set that must stay internal.\n\nDESCRIPTION IS STALE IN ONE PLACE: VerificationRecipe/BuildVerificationRecipeInput/BuildVerificationRecipeResult/DeclaredInput/VerifierIdentity live in evidence/evidence-contract.ts (lines 181-212), not verification-recipe.ts. verification-recipe.ts exports only buildVerificationRecipe (:206) and recipeSealMatches (:242).\n\nTYPE CLOSURE IS WIDER THAN THE DESCRIPTION (DoD 1 requires naming every parameter type and every union branch member type):\n- CrashClassification ADOPTED/SUSPECT carry postState: RestartPostState -> supervisor/restart-reconstruction.js\n- DrainAdvance ADVANCED carries disposition: DrainDisposition -> supervisor/drain-disposition.js, whose fields are DrainReason/DrainTerminalTarget -> supervisor/drain-table.js\n- ObservedVerifierExecution.runtimeObservation is ProviderRuntimeObservation -> providers/claude/claude-observation.js. That module's buildProviderRuntimeObservation is MANDATORY, not optional: the observation carries a self-recomputing digest, so a consumer cannot hand-build one that buildEvidenceReceipt will accept.\n- ReconcileClaudeRunInput.stream is ClaudeStreamRecord and ClaudeReconciliation carries ClaudeStreamDisposition/ClaudeStreamAnomaly -> providers/claude/claude-stream.js (which itself re-exports both from claude-stream-anomalies.js, so one specifier suffices).\nPublishing recordClaudeStream is deliberately NOT included: a ClaudeStreamRecord is plain data and is constructible from the published types, so the reconcile capability is reachable without opening the stream-recording subtree.\n\nSIZE: index.ts is 163 lines today; the three blocks measure ~170 more lines, so ~333 - past the 250 target. Will extract src/surface/{recovery,evidence,claude}-surface.ts (each with its sibling .js bridge) per task rail 6.\n\nBASELINE (for DoD 6): pnpm --filter @moe/runner test -> 37 files / 1040 tests passed.\n\nExisting index-surface.test.ts pattern to reuse: bare @moe/runner root import only, ExportKind-tagged hand-written EXPECTED_EXPORTS (66 entries today), an explicit length assertion, a set-equality assertion against Object.keys(runner).sort(), a per-name it.each typeof/Array.isArray probe, and a FIXTURE_NAMES absence check.",
      "startedAt": "2026-08-09T10:38:24.165Z",
      "completedAt": "2026-08-09T10:43:01.932Z"
    },
    {
      "stepId": "step-2",
      "description": "Hand-transcribe the expected export-name list — a separate step because DoD 3 lives or dies on it. Write the complete list of names index.ts will publish, taken BY HAND from the module sources read in step 1, and record it in the step note. Do NOT generate it from Object.keys of any namespace and do not derive it from the export block you are about to write: a list derived from the thing under test asserts only that the namespace equals itself and can never fail (mem:gotcha-self-derived-universe-cannot-check-itself). Scope the list deliberately rather than exporting everything — name what a consumer needs to classify a crash, admit or refuse a successor overlap and a resume, advance a drain, build and seal-verify a recipe, build an evidence receipt, rematerialize a candidate, and reconcile an observed Claude run. Internal helpers that no consumer needs stay internal; publishing them widens the package's public API permanently. This list is simultaneously the specification for step 3 and the expected value for step 4.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "HAND-TRANSCRIBED from the module sources read in step 1 (file:line verified), written BEFORE any export block exists. Nothing here came from Object.keys of any namespace.\n\n50 NEW RUNTIME VALUES (these are what Object.keys sees; 66 existing + 50 = 116 expected), with the ExportKind the test will assert:\n\nrecovery/recovery-contract.ts: PREDECESSOR_RELEASES array (:63), RECOVERY_CONTRACT_VERSION string (:21), RECOVERY_EFFECT_STATUSES array (:76), RECOVERY_ERROR_CODES array (:43), RECOVERY_LAYERS array (:33), RECOVERY_OUTCOME_KINDS array (:23)\nrecovery/crash-classification.ts: classifyCrash function (:189)\nrecovery/safe-boundary.ts: admitResume function (:135), admitSuccessorOverlap function (:95), advanceRecoveryDrain function (:185)\nsupervisor/restart-reconstruction.ts (closure for CrashClassification.postState): RESTART_POST_STATES array (:29)\nsupervisor/drain-table.ts (closure for DrainDisposition fields): DRAIN_REASONS array (:19), DRAIN_TERMINAL_TARGETS array (:31)\nevidence/evidence-contract.ts: EVIDENCE_OBLIGATION_KINDS array (:83), EVIDENCE_RECEIPT_VERSION string (:15), EVIDENCE_REFUSAL_LAYERS array (:27), MAX_EVIDENCE_ARGV_ENTRIES number (:17), MAX_EVIDENCE_DECLARED_ENTRIES number (:19), MAX_EVIDENCE_OBLIGATIONS number (:20), MAX_EVIDENCE_TEXT_CHARS number (:18), RUNNER_EVIDENCE_ERROR_CODES array (:44), VERIFICATION_RECIPE_VERSION string (:14), isEvidenceFailure function (:126)\nevidence/verification-recipe.ts: buildVerificationRecipe function (:206), recipeSealMatches function (:242)\nevidence/evidence-receipt.ts: buildEvidenceReceipt function (:171), receiptDigestInput function (:82)\nevidence/candidate-rematerialization.ts: rematerializeCandidate function (:75)\nevidence/receipt-obligations.ts: canonicalObligations function (:96)\nevidence/verifier-execution.ts: EXECUTION_DISPOSITIONS array (:28), observedExecutionRejection function (:195)\nproviders/claude/claude-cancel-reconcile.ts: CLAUDE_RECONCILED_OUTCOMES array (:19), CLAUDE_RECONCILIATION_VERSION string (:17), reconcileClaudeRun function (:119)\nproviders/claude/claude-capabilities.ts: CLAUDE_CAPABILITIES array (:25), CLAUDE_CAPABILITY_PROFILE_VERSION string (:23), CLAUDE_CAPABILITY_STATUSES array (:40), CLAUDE_CONTEXT_POLICIES array (:59), CLAUDE_PROOF_METHODS array (:44)\nproviders/claude/claude-observation.ts: CLAUDE_OBSERVATION_ERROR_CODES array (:43), CLAUDE_RUNTIME_OBSERVATION_VERSION string (:20), OBSERVATION_TRUTH_CLASSES array (:40), RUNTIME_CLOSURE_KINDS array (:25), RUNTIME_PINNING_METHODS array (:33), buildProviderRuntimeObservation function (:232), observationDigestInput function (:195), runtimePinningIsAuthoritative function (:298)\nproviders/claude/claude-stream.ts: CLAUDE_STREAM_ANOMALIES array, CLAUDE_STREAM_DISPOSITIONS array (both re-exported at :102 from claude-stream-anomalies.ts :9/:29), CLAUDE_STREAM_RECORD_VERSION string (:26)\n\n70 TYPES (invisible to Object.keys, required by DoD 1's closure):\nrecovery: CrashClassification, CrashObservation, DrainAdvance, OverlapVerdict, PredecessorRelease, RecoveryEffectStatus, RecoveryErrorCode, RecoveryFailure, RecoveryLayer, RecoveryOutcomeKind, ResumeVerdict + closure RestartPostState, DrainDisposition, DrainReason, DrainTerminalTarget\nevidence: BuildEvidenceReceiptInput, BuildEvidenceReceiptResult, BuildVerificationRecipeInput, BuildVerificationRecipeResult, CandidateTreeEntry, CandidateTreePort, DeclaredInput, DischargedObligation, EvidenceFailure, EvidenceObligationKind, EvidenceReceipt, EvidenceReceiptBody, EvidenceRefusalLayer, ExecutionDisposition, ObligationContext, ObligationSupport, ObservedOutput, ObservedVerifierExecution, ReceiptTimestamps, RematerializeCandidateInput, RematerializeCandidateResult, RunnerEvidenceErrorCode, VerificationRecipe, VerificationRecipeBody, VerifierIdentity\nclaude: BuildObservationInput, BuildObservationOk, BuildObservationResult, ClaudeCapability, ClaudeCapabilityProfile, ClaudeCapabilityRecord, ClaudeCapabilityStatus, ClaudeContextLimit, ClaudeContextPolicy, ClaudeFailure, ClaudeObservationErrorCode, ClaudeProcessExit, ClaudeProofMethod, ClaudeRawRetention, ClaudeReconciledOutcome, ClaudeReconciliation, ClaudeStreamAnomaly, ClaudeStreamDisposition, ClaudeStreamEvent, ClaudeStreamRecord, MoeEffectIdentity, ObservationClock, ObservationFreshness, ObservationTruthClass, PlatformIdentity, ProviderRuntimeObservation, ReconcileClaudeRunInput, RuntimeClosureEntry, RuntimeClosureKind, RuntimePinningMethod\n\nDELIBERATELY WITHHELD (internal, not needed by the DoD's consumer capabilities): recoveryFailure/carriedFailure and the recovery is* shape guards; CRASH_OBSERVATION_KEYS; evidenceFailure/evidencePathRejection/evidenceRefRejection/isBoundedEvidenceText/isEvidenceIdentity/recipeDigestInput; evidence/candidate-reconciliation.ts's portAttempt/normalizeScan/reconcile/expectationRejection; claude-capabilities' assessCapabilities/capabilitySchemaDigestOf/resolveContextLimit/isBoundedLabel/UNPROVEN_PROBE_REPORT and the probe-port types; claude-observation's claudeF",
      "startedAt": "2026-08-09T10:43:04.252Z",
      "completedAt": "2026-08-09T10:43:37.956Z"
    },
    {
      "stepId": "step-3",
      "description": "Add the three curated export blocks to packages/runner/src/index.ts, matching the file's existing style exactly: one grouped `export { ... } from \"./<dir>/<module>.js\";` block per module, values first and `type` entries in the same braces, every specifier carrying the .js extension per NodeNext. Export exactly the names on the step-2 list — no blanket `export *`, which would publish whatever these modules happen to export next week and would defeat the set-equality assertion that makes this seam a reviewed decision. Export NO test-only module. THEN MEASURE: index.ts already carries 126 specifiers, so check its line count with wc -l after the additions. If it passes the 250-line target, extract per-area re-export modules (for example a recovery surface module and an evidence surface module) that index.ts re-exports, rather than letting the entry point sprawl toward the 400 hard cap — and give any extracted module its sibling .js bridge in the SAME commit, since @moe/runner is bridged and is now covered by the repo-wide runtime-loadability gate.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/index.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/index.ts",
        "packages/runner/src/surface/recovery-surface.ts",
        "packages/runner/src/surface/recovery-surface.js",
        "packages/runner/src/surface/evidence-surface.ts",
        "packages/runner/src/surface/evidence-surface.js",
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/surface/claude-surface.js"
      ],
      "note": "MEASURED FIRST, THEN EXTRACTED. The three blocks inline would have taken index.ts from 163 to ~333 lines, past the 250 target, so task rail 6 applies. Extracted three per-area surface modules under packages/runner/src/surface/, each holding the curated named re-export list from step 2, each with its sibling .js bridge in the same change:\n\n  index.ts             173 lines (was 163)\n  claude-surface.ts     80\n  evidence-surface.ts   70\n  recovery-surface.ts   53\n\nindex.ts reaches them with `export * from \"./surface/<area>-surface.js\"`. That is not the blanket re-export task rail 4 forbids: rail 4's hazard is republishing whatever a MODULE happens to export next week, and a surface module contains nothing but an explicit hand-written name list, so it cannot grow without a reviewer editing it. The curation moved one file down; it did not disappear. Documented in a comment at the seam.\n\nEach surface module documents what it withholds and why, and the two non-obvious inclusions are justified in prose at their seam: isEvidenceFailure (canonicalObligations returns an array-or-failure union that has no .ok to narrow on) and buildProviderRuntimeObservation (a ProviderRuntimeObservation carries a digest over its own fields and observedExecutionRejection refuses one that does not recompute, so publishing the type without its builder would publish an unusable type).\n\nNo test-only module is reachable: recovery-test-fixtures.ts and effect-test-fixtures.ts are not named by any surface module and still have no .js bridge.\n\nVERIFIED: pnpm --filter @moe/runner typecheck -> exit 0. Plain-Node root probe (node --experimental-strip-types, cwd packages/runner, bare specifier @moe/runner so the self-reference exports map is exercised the way a dependent gets it) imports cleanly and reports exactly 116 named exports - the 66 pre-existing plus the 50 hand-transcribed in step 2, with no thirty-seventh surprise. That count was predicted by hand BEFORE the probe was run; the probe confirmed the prediction rather than supplying it.",
      "startedAt": "2026-08-09T10:43:40.387Z",
      "completedAt": "2026-08-09T10:45:01.702Z"
    },
    {
      "stepId": "step-4",
      "description": "Extend packages/runner/src/index-surface.test.ts — DoD 2 and DoD 3. Every assertion imports the package ROOT specifier @moe/runner; never a relative or deep path, since the entire point of this task is that deep subpaths do not resolve through an exclusive exports map. Assert per new symbol that the export is present and of the expected typeof, so a dropped export fails BY NAME rather than by a vague count. Then update the set-equality assertion between Object.keys of the namespace and the hand-written list from step 2, so a removed export AND an unreviewed addition both go red. Assert explicitly that no test-only module leaked onto the surface — recovery-test-fixtures and any evidence or claude helper must be absent from the namespace, which the set-equality check gives you for free provided the hand-written list omits them.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "DoD 2 and DoD 3. Every specifier in the file is still the bare root `@moe/runner` - no relative and no deep path was added.\n\nEXPECTED_EXPORTS grew from 66 to 116 entries, the 50 new ones transcribed from the step-2 note (which was itself read off the module sources), grouped by seam with a comment naming the modules each group came from. The explicit length assertion moved 66 -> 116, so a silently truncated list cannot pass. The per-name `it.each` probe now covers all 116, which is what makes a dropped export fail BY NAME rather than by a count.\n\nOne deliberate change to the set-equality assertion: both sides are now sorted (`Object.keys(runner).sort()` vs `EXPECTED_EXPORTS.map(...).sort()`). The list is grouped by seam for review, and hand-merging 50 names into exact JS code-unit order would have been a transcription hazard that buys nothing - transcription order is not a fact about the published surface, and set equality is unchanged in both directions.\n\nFIXTURE_NAMES extended with the 11 runtime names of recovery/recovery-test-fixtures.ts (HELD_EPOCH, HELD_TOKEN, RECONCILED, REGISTRATION, COMMIT, ADVANCED, SETTLED, disposition, records, observation, situation). evidence/ and providers/claude/ were re-checked and have no fixture module at all, which the comment records so the next reader does not re-derive it.\n\nMUTATION DRILL (epic rail 6 - a failure-path assertion is only real if it goes red). Dropped `classifyCrash` from surface/recovery-surface.ts and re-ran:\n  x publishes exactly the reviewed root namespace, with no loss and no addition\n  x publishes classifyCrash on the package root as a function\n  Tests 2 failed | 126 passed (128)\nRestored, re-ran: 128 passed (128). So both the by-name probe and the set-equality assertion genuinely observe the seam.\n\npnpm --filter @moe/runner typecheck -> exit 0. index-surface.test.ts alone: 128 tests passed (was 78).",
      "startedAt": "2026-08-09T10:45:03.958Z",
      "completedAt": "2026-08-09T10:47:35.329Z"
    },
    {
      "stepId": "step-5",
      "description": "Prove the discriminated unions survive publication — DoD 4, and the known failure mode on this seam. These three subtrees are union-heavy: CrashClassification, OverlapVerdict, ResumeVerdict, DrainAdvance, the evidence build results (BuildEvidenceReceiptResult, RematerializeCandidateResult) and the Claude reconciliation outcomes. A consumer that cannot narrow them by discriminant cannot use them at all, and the supervisor publish task had to fix exactly this for LifecycleOutcome, so it is a demonstrated risk rather than a hypothetical. Obtain real values by CALLING the exported functions through the root import — classifyCrash, admitSuccessorOverlap, admitResume, advanceRecoveryDrain, buildEvidenceReceipt, rematerializeCandidate, reconcileClaudeRun — with inputs that produce each branch, and assert each branch is distinguishable by its discriminant. Do NOT construct union literals to test this: a hand-built literal typechecks regardless of what was published, so it would pass even if the type were unreachable. Assert against the production surface, never a locally re-declared type.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "DoD 4. Every value under test is obtained by CALLING a published function through the bare @moe/runner root. No union literal is constructed to stand in for a published union - the only literals are INPUTS, typed by the published input types, which is the opposite direction and cannot mask an unpublished result type.\n\nALL SIX CrashClassification arms reached with real inputs, each pinned by discriminant:\n- ADOPTED (postState ACTIVE_ADOPTED) via a claimed authority whose epoch AND lease token both advanced past the held binding\n- SUSPECT via effectStatus UNESTABLISHED\n- QUARANTINED via a records set that cannot identify the process (registration null, lock RELEASED), asserting held == [\"resource:ACTIVE\", \"effect:intent-1\"]\n- ABSENT via a TERMINAL attempt over a SUCCEEDED effect with a proven release and an EXITED process, asserting `\"postState\" in absent` is FALSE - the arms genuinely differ in shape, not just in a label\n- RECONCILIATION_COMMAND via records that disagree, asserting command == \"recovery.inspect_external\"\n- REFUSED under three DISTINCT codes: RECOVERY_OWNERSHIP_TRANSFER_UNPROVEN (quiescence never adopts), RECOVERY_OBSERVATION_MALFORMED, RECOVERY_STALE_OBSERVATION_REFUSED\n\nOverlapVerdict both arms + all three blocking codes kept apart (PREDECESSOR_ACTIVE vs PREDECESSOR_RELEASE_UNKNOWN vs BOUNDARY_MALFORMED). ResumeVerdict both arms + RESUME_BOUNDARY_UNPROVEN for a proven release carrying no handoff.\n\nDrainAdvance both arms AND - epic rail 6's \"which layer refused\" - all three refusal shapes: RECOVERY_DRAIN_REASON_DROPPED (SAFE_BOUNDARY), RECOVERY_DRAIN_DOWNGRADE_REFUSED (SAFE_BOUNDARY), and DRAIN_DISPOSITION_NOT_MONOTONIC carried verbatim from layer DRAIN. That last one is the assertion that catches a restamp: if recovery ever re-stamped the carried failure with its own layer the test goes red, and no other assertion here would notice.\n\nEvidence unions, both arms each:\n- BuildVerificationRecipeResult: built + seal-verified through recipeSealMatches (true for the real recipe, false for one with a swapped sha256), refused on empty argv with RUNNER_EVIDENCE_ARGV_INVALID / RECIPE_SHAPE\n- RematerializeCandidateResult: ok branch through a port-driven empty declared closure whose artifact stubs THROW if called (so the ok branch also proves nothing touched a disk), refused branch on a tampered recipe\n- BuildEvidenceReceiptResult: full ok branch composed entirely from root exports (observeScope -> buildResultManifest, buildInputManifest, buildProviderRuntimeObservation, buildVerificationRecipe -> buildEvidenceReceipt), asserting receiptVersion, recipeSha256 binding and canonicalized obligation order; refused branch on a tampered recipe\n- The tampered-recipe refusal carries the SAME code from both entry points but a DIFFERENT layer (REMATERIALIZATION vs RECEIPT_BINDING), asserted explicitly, so which gate answered survives publication\n- observedExecutionRejection returns null for an admissible run and EXECUTION_UNKNOWN/EXECUTION for a disposition of UNKNOWN; canonicalObligations refuses AGENT_TEXT_UNSUPPORTED/OBLIGATION, narrowed through the published isEvidenceFailure because an array-or-failure union has no `.ok`\n\nClaude: reconcileClaudeRun driven to all six CLAUDE_RECONCILED_OUTCOMES (PROVEN_RESULT, COMPLETED_BEFORE_CANCEL, CANCELLED_CLEAN, HONEST_UNKNOWN, CANCELLED_UNKNOWN_TAIL, CRASHED) including the TRUNCATION-anomaly path, plus ClaudeProcessExit narrowing by kind (only EXITED carries a code; UNOBSERVED carries neither code nor signal). buildProviderRuntimeObservation ok + refused (CLAUDE_OBSERVATION_PINNING_METHOD_INVALID), observationDigestInput re-derivable, runtimePinningIsAuthoritative true/false.\n\nSECOND MUTATION DRILL, this time on a TYPE-ONLY export, because the set-equality test can never see one: removed `type RestartPostState` from the recovery surface module and got\n  src/index-surface.test.ts(39,62): error TS2724: '\"@moe/runner\"' has no exported member named 'RestartPostState'. Did you mean 'RESTART_POST_STATES'?\nRestored; typecheck exit 0. So the type closure is guarded by the typecheck leg of the gate, not merely asserted in prose.\n\npnpm --filter @moe/runner typecheck -> exit 0. index-surface.test.ts alone: 141 tests passed (78 before this task).",
      "startedAt": "2026-08-09T10:47:38.813Z",
      "completedAt": "2026-08-09T10:55:31.643Z"
    },
    {
      "stepId": "step-6",
      "description": "Prove zero behaviour change and run the full gate. Confirm with `git status --porcelain` and `git diff --stat` that packages/runner/src/index.ts is the ONLY modified non-test file — plus any re-export module extracted in step 3 and its bridge — and that no module under recovery/, evidence/ or providers/claude/ was touched, since those three landed from different tasks whose owners may still be active. Record the runner suite's test count BEFORE and AFTER as explicit numbers; the after count must be greater than or equal to the before count. Then run fresh from the repo root: pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck. The daemon typecheck is in the gate deliberately: this surface exists so apps/daemon can compose it into a launcher and restart boundary, and a surface that publishes but does not typecheck from its intended consumer has not done its job. Confirm with wc -l that index.ts and any extracted module are under the 400 hard cap, and record the counts.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "ZERO BEHAVIOUR CHANGE, PROVEN BY THE DIFF.\n\n`git status --porcelain -- packages/runner`:\n   M packages/runner/src/index-surface.test.ts\n   M packages/runner/src/index.ts\n  ?? packages/runner/src/surface/\n\n`git diff --stat -- packages/runner`: index.ts +11 / -0, index-surface.test.ts +629 / -4. NOTHING under recovery/, evidence/ or providers/claude/ is modified, added or deleted - those three subtrees are byte-identical to HEAD, which matters because their owners may still be active. The only new non-test files are the three extraction modules and their bridges under src/surface/, which step 3 sanctions. index.ts's 11 added lines are 6 lines of comment and 3 export statements plus blank lines; no line was removed, so nothing previously published moved or disappeared.\n\nTEST COUNT, explicit numbers: BEFORE 37 files / 1040 tests. AFTER 37 files / 1104 tests. +64, no new test file, and the after count is >= the before count as DoD 6 requires. index-surface.test.ts alone went 78 -> 141.\n\nLINE COUNTS (400 hard cap):\n  packages/runner/src/index.ts             173\n  packages/runner/src/surface/claude-surface.ts    80\n  packages/runner/src/surface/evidence-surface.ts  70\n  packages/runner/src/surface/recovery-surface.ts  53\nEvery production source is under the 250 TARGET, not merely under the 400 cap.\n\nDISCLOSED HONESTLY: packages/runner/src/index-surface.test.ts is now 925 lines. It is a test, not a production source, so epic rail 5's per-file cap does not bind it, and the repo already carries an 875-line test (packages/coordination/src/coordination-suite.test.ts) - but this is now the largest test file in the repo and a reviewer should know that rather than discover it. It was not split because the plan's owned paths name exactly this one test file and DoD 7 requires the committed paths to be the two owned files plus the step-3 extraction; adding a third test file was not sanctioned. If a reviewer wants it split, the natural seam is the three `/* ---- */` banners already in the file.\n\nTHE GATE, run fresh from the repo root, all three legs:\n  pnpm --filter @moe/runner typecheck   -> exit 0\n  pnpm --filter @moe/runner test        -> exit 0, 37 files / 1104 tests passed\n  pnpm --filter @moe/daemon typecheck   -> exit 0\nThe daemon leg is the one that matters for intent: the surface typechecks from the consumer that is supposed to compose it.\n\nALSO RUN, because this task adds bridged modules and a focused package gate cannot see it (mem:gotcha-package-loadability-allowlist-names-its-owner-task): `pnpm exec vitest run tests/runtime/package-loadability.test.ts` -> 7 passed. `allowedPackageFailures` is `Object.freeze({})` and names no task, so there is nothing for this task to retire. The runner's own runtime-entrypoint.test.ts (a real child Node process, bare @moe/runner specifier, strip-types runtime) is inside the 37 passing files, so the three new surface modules and their .js bridges load for a real consumer, not just under vitest.",
      "startedAt": "2026-08-09T10:55:34.213Z",
      "completedAt": "2026-08-09T10:56:31.649Z"
    },
    {
      "stepId": "step-7",
      "description": "Adversarial self-review, then commit by explicit pathspec. Review: is the expected export-name list genuinely hand-written or was it derived from the namespace it checks; does every assertion import the ROOT specifier, with no relative or deep path anywhere in the test; is any test-only module exported; is any name exported twice from two modules; was any module other than index.ts edited; can a consumer actually narrow every published union by its discriminant, proven by calling real functions rather than by constructing literals; did an extracted re-export module get its .js bridge; would the set-equality assertion catch an unreviewed addition as well as a removal. Then stage and commit by EXPLICIT PATHSPEC naming only the owned files — never a bare git commit, never git commit -a, never git add -A, and never a directory pathspec under packages/runner/src/, since sibling tasks have uncommitted work throughout this package. Run `git status --porcelain` first and unstage any foreign path with `git restore --staged`. Verify with `git show --stat` that only the owned paths landed. Finally note in the completion evidence that this task publishes the surface only — J3's launcher and restart composition is a separate follow-on and the Foundation canary remains blocked until it lands.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "packages/runner/src/index.ts",
        "packages/runner/src/index-surface.test.ts",
        "packages/runner/src/surface/recovery-surface.ts",
        "packages/runner/src/surface/recovery-surface.js",
        "packages/runner/src/surface/evidence-surface.ts",
        "packages/runner/src/surface/evidence-surface.js",
        "packages/runner/src/surface/claude-surface.ts",
        "packages/runner/src/surface/claude-surface.js"
      ],
      "note": "ADVERSARIAL SELF-REVIEW, every question in step 7 answered with evidence rather than assertion.\n\n1. Is the expected list hand-written? Yes - written in the step-2 note from module sources with file:line, BEFORE any export block existed, and the 116 total was predicted by hand before the Node probe confirmed it. The probe checked the prediction; it never supplied it.\n\n2. Any relative or deep path in the test? `grep -n \"from \\\"\\.\\|from './\\|@moe/runner/\"` returns exactly ONE hit, and it is inside a comment explaining why deep subpaths do not resolve. Every executable specifier is the bare root.\n\n3. Any test-only module exported? Node probe over the fixture names from both fixture modules: `{\"leaked\":[],\"total\":116}`. Neither recovery-test-fixtures.ts nor effect-test-fixtures.ts has a .js bridge, so neither could load in Node even if named.\n\n4. Any name exported twice? THIS IS THE ONE THAT NEEDED CARE AND ALMOST SLIPPED. `export *` does NOT error on a collision with an explicit export in the same file - the explicit one silently wins, and a star/star collision silently drops the name. So a collision here would have been invisible rather than a compile error, and the compile-clean typecheck alone would NOT have proved absence. The proof is the COUNT: the root reports exactly 116 = 66 pre-existing + 50 new. Any shadow or drop would have produced fewer, and the set-equality test names which.\n\n5. Any module other than index.ts edited? `git show --stat` on the commit: 8 files, and the only pre-existing production file is index.ts (+11 lines, 0 removed). recovery/, evidence/ and providers/claude/ are byte-identical to their previous state.\n\n6. Can a consumer narrow every published union? Proven by CALLING, in step 5: all six CrashClassification arms, both OverlapVerdict arms, both ResumeVerdict arms, both DrainAdvance arms, both arms of BuildVerificationRecipeResult / RematerializeCandidateResult / BuildEvidenceReceiptResult, all six Claude reconciled outcomes, and ClaudeProcessExit narrowing by kind. No union literal is constructed anywhere; the only literals are inputs typed by published input types.\n\n7. Did each extracted module get its .js bridge? Yes, all three, in the same commit, and tests/runtime/package-loadability.test.ts (7 passed) plus the runner's own child-process runtime-entrypoint test confirm the root loads in real Node rather than only under vitest.\n\n8. Would set-equality catch an ADDITION as well as a removal? Drilled both directions:\n   - removal (dropped classifyCrash): 2 failed | 126 passed - set-equality AND the by-name probe\n   - addition (added recoveryFailure): 1 failed | 140 passed - \"publishes exactly the reviewed root namespace, with no loss and no addition\"\n   - type-only removal (dropped `type RestartPostState`): TS2724 at index-surface.test.ts:39, which the runtime set-equality can never see\n   All three restored, 141 passed each time.\n\nADVERSARIAL CHECKLIST ITEMS THAT DO NOT APPLY, stated rather than skipped: this change adds no logic, no IO, no state and no control flow - every added production line is an export statement or a comment - so concurrency, TOCTOU, partial-write and retry questions have no surface to attach to. The one hostile-input question that DOES apply is what the published functions do with hostile input, and the tests drive classifyCrash(null), admitSuccessorOverlap(null), admitResume(null), an empty argv, a tampered recipe seal and an out-of-vocabulary pinning method - each refused with its own stable code and refusing layer, never a throw.\n\nCOMMIT: 7afa17d, by explicit pathspec naming all 8 files - no bare commit, no -a, no add -A, no directory pathspec. `git diff --cached --name-only` was verified before committing and `git show --stat` after; the commit contains ONLY the owned paths. 26 foreign entries (.moe state, .codex, apps/daemon, pnpm-lock, packages/import) remain uncommitted and untouched.\n\nGATE, run fresh AFTER the commit as one command: pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck -> EXITCODE=0, 37 files / 1104 tests passed.\n\nSCOPE NOTE FOR THE BOARD: this task publishes the SURFACE ONLY. J3's launcher and restart-boundary composition is a separate follow-on, and the Foundation canary stays blocked until that lands - what changed today is that the composition is now possible, since a consumer outside @moe/runner previously could not import these symbols at all.",
      "startedAt": "2026-08-09T10:56:36.820Z",
      "completedAt": "2026-08-09T10:57:58.561Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 12,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-09T10:35:55.867Z",
  "updatedAt": "2026-08-09T11:29:40.837Z",
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-09T10:37:03.910Z",
  "metrics": {
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 2,
    "firstClaimAt": "2026-08-09T10:38:07.675Z",
    "executedStepCount": 7,
    "doneAt": "2026-08-09T11:29:40.837Z",
    "wallClockMs": 3093162
  },
  "planningNotes": {
    "approachesConsidered": "(1) ONE TASK FOR THREE SUBTREES, despite the 'and' warning: all three land in the SAME two files (index.ts and index-surface.test.ts), share one hand-written export list, and one set-equality assertion. Splitting would mean three tasks editing the same index.ts and the same expected-name list — guaranteed merge conflicts on a shared worktree, for no review benefit.\n(2) SIZE IS THE REAL RISK HERE, not correctness. index.ts already carries 126 specifiers; adding three subtrees will very likely push it past the 250-line target. The rail says extract per-area re-export modules rather than letting the entry point sprawl — decide that at step 3 by measuring, not by guessing, and remember an extracted module needs its own .js bridge in the same commit.\n(3) CURATED, NOT `export *`. A blanket re-export would publish whatever these modules happen to export next week, which defeats the set-equality assertion that makes the seam a reviewed decision. It would also silently publish test-only helpers.\n(4) THE UNION-DISCRIMINABILITY CHECK IS THE ONE NON-MECHANICAL ASSERTION. These subtrees are union-heavy (CrashClassification, OverlapVerdict, ResumeVerdict, DrainAdvance, the evidence result unions), and a consumer that cannot narrow them by discriminant cannot use them at all — the supervisor publish task hit exactly this with LifecycleOutcome, so it is a known failure mode on this seam rather than a hypothetical.",
    "codebaseInsights": "VERIFIED ON DISK 2026-08-09 — re-confirm at step 1, these subtrees are young.\n**Already published** in packages/runner/src/index.ts (126 specifiers): artifacts/artifact-contract, artifact-node-fs, artifact-store; scope/scope-contract, scope-git, scope-observation; supervisor/effect-activation, effect-grant, effect-kernel, effect-lifecycle, effect-parse, effect-shape, lease-mirror; workspace/workspace-contract, workspace-manifest.\n**NOT published, and the point of this task:**\n- `recovery/crash-classification.ts` — classifyCrash (:189), type CrashClassification (:43).\n- `recovery/safe-boundary.ts` — admitSuccessorOverlap (:95), admitResume (:135), advanceRecoveryDrain (:185); types OverlapVerdict (:37), ResumeVerdict (:41), DrainAdvance (:50).\n- `recovery/recovery-contract.ts` — RECOVERY_CONTRACT_VERSION (:21), RECOVERY_OUTCOME_KINDS (:23), RecoveryOutcomeKind, RECOVERY_LAYERS (:33), RecoveryLayer, RECOVERY_ERROR_CODES (:43), RecoveryErrorCode, PREDECESSOR_RELEASES (:63).\n- `evidence/` — buildEvidenceReceipt (evidence-receipt.ts:171), receiptDigestInput (:82), types EvidenceReceipt (:44), EvidenceReceiptBody (:60), BuildEvidenceReceiptInput (:62), BuildEvidenceReceiptResult (:73), ReceiptTimestamps (:25); buildVerificationRecipe (verification-recipe.ts:206), recipeSealMatches (:242); rematerializeCandidate (candidate-rematerialization.ts:75), types CandidateTreePort (:39), RematerializeCandidateInput (:45), RematerializeCandidateResult (:58); canonicalObligations (receipt-obligations.ts:96); observedExecutionRejection (verifier-execution.ts:195), EXECUTION_DISPOSITIONS (:28), ExecutionDisposition (:35), ObservedOutput (:37), ObservedVerifierExecution (:42); evidence-contract.ts vocabularies VERIFICATION_RECIPE_VERSION (:14), EVIDENCE_RECEIPT_VERSION (:15), EVIDENCE_REFUSAL_LAYERS (:27), EvidenceRefusalLayer (:37) and the MAX_EVIDENCE_* limits (:17-20).\n- `providers/claude/claude-cancel-reconcile.ts` — reconcileClaudeRun (:119), CLAUDE_RECONCILIATION_VERSION (:17), CLAUDE_RECONCILED_OUTCOMES (:19), ClaudeReconciledOutcome (:27), ClaudeProcessExit (:29), ReconcileClaudeRunInput (:34), ClaudeReconciliation (:40); `claude-capabilities.ts` — CLAUDE_CAPABILITY_PROFILE_VERSION (:23), CLAUDE_CAPABILITIES (:25), ClaudeCapability, CLAUDE_CAPABILITY_STATUSES (:40), CLAUDE_PROOF_METHODS (:44), ClaudeProofMethod.\n**TEST-ONLY, must NOT be exported:** `recovery/recovery-test-fixtures.ts`, `evidence/*-fixtures.ts` if present, and the claude probe helpers.\n**Precedent:** task-53680e91 did exactly this for supervisor/ and landed 8 steps / 2 files; `index-surface.test.ts` already exists from it with the root-import and set-equality pattern to extend.",
    "risks": "1. **index.ts will very likely burst the 250-line target.** Measure at step 3 and, if it does, extract per-area re-export modules that index.ts re-exports rather than letting the entry point sprawl toward 400. Any extracted module needs its sibling `.js` bridge in the SAME commit — @moe/runner is bridged and now covered by the repo-wide runtime-loadability gate, so an unbridged module fails a gate rather than passing quietly.\n2. **Duplicate export names across three subtrees are a compile error.** evidence/ and recovery/ both define failure and layer vocabularies; check for collisions (e.g. a shared `*_LAYERS` or `*Failure` name) before composing, and if two modules genuinely export the same name, that is a finding to report rather than something to paper over with a rename in index.ts.\n3. `mem:gotcha-self-derived-universe-cannot-check-itself`: the expected export-name list must be HAND-TRANSCRIBED. If it is generated from Object.keys of the namespace, the test asserts the namespace equals itself and can never fail.\n4. **Union discriminability is the known failure mode on this seam.** The supervisor publish task had to prove LifecycleOutcome's three kinds stayed distinguishable through the root. These subtrees are union-heavy; obtain real values by CALLING the exported functions and assert each branch narrows by its discriminant, rather than constructing literals that would typecheck regardless.\n5. **Never export a test-only module.** recovery-test-fixtures.ts is the obvious one; check evidence/ and providers/claude/ for others before writing the list.\n6. **Do not edit any module other than index.ts.** These three subtrees landed from three different tasks within days; a signature change here would collide with an owner who is still active.\n7. The daemon typecheck is in the gate deliberately — this surface exists so apps/daemon can compose it, and a surface that publishes but does not typecheck from its intended consumer has not done its job.",
    "keyFiles": [
      "packages/runner/src/index.ts",
      "packages/runner/src/index-surface.test.ts",
      "packages/runner/src/recovery/recovery-contract.ts",
      "packages/runner/src/recovery/safe-boundary.ts",
      "packages/runner/src/evidence/evidence-contract.ts",
      "packages/runner/src/providers/claude/claude-cancel-reconcile.ts"
    ]
  },
  "budget": {
    "wallClockMs": 14400000
  },
  "planApprovedAt": "2026-08-09T10:37:05.931Z",
  "contextFetchedBy": [
    "worker-29cc6667",
    "qa-b2df68b9"
  ],
  "workStartedAt": "2026-08-09T10:38:24.165Z",
  "completedAt": "2026-08-09T11:29:40.837Z",
  "reviewCompletedAt": "2026-08-09T11:29:40.837Z",
  "reviewSummary": "APPROVED. Commit 7afa17d, 8 owned files; per-file cap OK (index.ts 173; surfaces 53/70/80).\n\nGATE RE-RUN BY QA: runner typecheck exit 0; runner test 37 files / 1104 passed (twice); daemon typecheck exit 1 but FOREIGN - both errors TS6133 unused-import in apps/daemon/src/review/review-lineage.test.ts, UNTRACKED (git ls-tree HEAD empty there); no error mentions @moe/runner, so consumer resolution is proven. Path-attributed-baseline rail applies.\n\nDoD1 three subtrees reachable from the bare root with type closure named (RestartPostState/DrainDisposition/DRAIN_* are union branches, not creep). DoD2/3 every specifier bare @moe/runner, no relative/deep path; 116 hand-written [name,kind] pairs, per-symbol typeof, two-way set-equality. DoD4 unions obtained by CALLING published functions: 5 CrashClassification arms, both Overlap/Resume arms, DrainAdvance + 3 refusals, both evidence unions, 8 Claude outcomes - reason code AND refusing layer pinned each time (RUNNER_EVIDENCE_RECIPE_DIGEST_MISMATCH separated by layer REMATERIALIZATION vs RECEIPT_BINDING; DRAIN_DISPOSITION_NOT_MONOTONIC carried at layer DRAIN). DoD5 fixtures absent. DoD6 git show --stat: nothing under recovery/evidence/providers touched; 1040 -> 1104 tests. \"No .js bridge added\" holds in substance - the 3 bridges belong to modules the size rail forced in the same commit, repo one-line form.\n\nBEYOND WORKER EVIDENCE: (1) export * collision audit done statically since tsc is green either way - 0 duplicates across star modules, 0 names shadowed by an explicit index.ts export; QA-computed 66+50=116 matches the hand list; probe from apps/daemon via node --experimental-strip-types returns 116 keys, so bridges load outside vitest. (2) MUTATION DRILL: dropped classifyCrash, 4 tests went red naming it; restored, tree clean, suite green.\n\nPublish-only; J3 launcher/restart composition is a follow-on, Foundation canary stays blocked."
}