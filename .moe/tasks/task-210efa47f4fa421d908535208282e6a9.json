{
  "id": "task-210efa47f4fa421d908535208282e6a9",
  "epicId": "epic-e0c54bddb3004bf29e98b2a43cea2dc2",
  "title": "Publish NodeAuthority through scheduler root",
  "description": "OBJECTIVE: make the canonical NodeAuthority and GraphRevisionContent v3 surfaces consumable from the real @moe/scheduler package root without exposing internal digest/preimage/index helpers. HARD DEPENDENCY: task-6ba1ff89393c4a2b91e11df06c31b873 must be DONE. DELIVERABLE: a focused curated forwarding module plus exact LF bridge, one root forward line that keeps index.ts <=250, closed namespace/type/runtime ratchets, and temporary NodeNext compile/plain-Node positive plus bogus-symbol negative probes deleted in the same command. Runtime smoke must call one production NodeAuthority function and the v3 graph round trip. NOT IN SCOPE: daemon/store behavior or external fixture migration. The graph-v3 hardening task filed next is the immediate consumer; task-eacea969d636420b8f5c47304039e056 is the durable production consumer.",
  "definitionOfDone": [
    "A curated node-authority-public module exposes the complete reviewed create/admit/encode/decode/recursive-result type and stable version/code/layer/limit roster while withholding raw canonical text, digest preimages, graph-index helpers and unvalidated constructors.",
    "packages/scheduler/src/index.ts forwards the curated surface with one line and remains <=250 physical lines; exact LF bridges and the closed root namespace/type roster pin every new runtime binding and prohibited internal name.",
    "Temporary NodeNext compile and plain-Node package-root probes import and call production NodeAuthority plus GraphRevisionContent v3 surfaces; a bogus symbol fails with the exact expected TypeScript diagnostic, internal subpaths remain unexported, and probes are deleted in the same command.",
    "Scheduler strip-types smoke proves the real bare root loads, derives nonempty node authority and round-trips v3 graph bytes; negative assertions pin exact code+layer and no zero-count test is accepted.",
    "`pnpm --filter @moe/scheduler test` exits 0 with nonzero Test Files/Tests counts, production sources remain <=250, and the task records its external-fixture hardening consumer id plus durable consumer task-eacea969d636420b8f5c47304039e056."
  ],
  "taskRails": [
    "Own only packages/scheduler/src/node-authority/node-authority-public.{ts,js}, index.ts, index-surface.test.ts, scheduler-entrypoint-smoke-worker.mjs, and scheduler-runtime-entrypoint.test.ts.",
    "Do not deep-export internal helpers or modify package exports; the root `.` is the only public subpath.",
    "Root export alone is not final composition: record the next external-fixture task and task-eacea969d636420b8f5c47304039e056 as consumers."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE + BASELINE (stale-by-default; every number below re-verified at plan time on HEAD 9adceb7 by an adversarial 3-lens panel — re-confirm at your HEAD). Confirmed: index.ts is EXACTLY 249 physical lines, LF-terminated (wc -l 249; tail xxd ends 0x3b 0x0a) — one added line = 250, at the cap. EXPECTED_EXPORTS pinned 111 (index-surface.test.ts:190), runtime set-equality :466, ExportKind = \"array\"|\"function\"|\"number\"|\"record\" (:100, no \"string\"; else-arm :473 is generic typeof so \"string\" needs only the union widened). node-authority-public.{ts,js} ABSENT at HEAD. The six node-authority modules export exactly 41 runtime bindings; publish/withhold split below covers all 41 bidirectionally. Smoke worker imports createNodeDefinition/deriveNodeAuthoritySet RELATIVELY (:26-27) with a comment naming THIS task; index-surface.test.ts same (:24-25) plus snapshotIdentityHash (:23). LAYER_NAMES module-private in BOTH contract.ts (:101, 11 members) and recursion.ts (:27, 2 members); only the types NodeAuthorityLayer/NodeAuthorityRecursionLayer are exported. TS is 7.0.2 (satisfies usable). Baseline gates (panel-run, fresh): pnpm --filter @moe/scheduler typecheck EXIT 0 ('$ tsc' echo seen); test EXIT 0 'Test Files 47 passed (47) / Tests 1553 passed (1553)'. KNOWN FOREIGN RED (path-attributed baseline, do NOT fix): pnpm --filter @moe/daemon test EXIT 1, 14 files / 25 tests, single cause GRAPH_CONTENT_FIELD_INVALID @ GRAPH_CONTENT_CODEC on missing nodeAuthority — owned by successor task-8c7e6ce4, disclosed not repaired. Record your own merge-base/HEAD baseline per the project gate rail.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "MEASURE + BASELINE at MY HEAD 1558a61 (plan measured 9adceb7; HEAD moved twice since — 907d8f8 then 1558a61 — so every number below is a fresh read, not a copy).\n\nPLAN NUMBERS RE-CONFIRMED, all of them:\n- packages/scheduler/src/index.ts = EXACTLY 249 physical lines (wc -l 249); tail xxd ends `n-contract.js\";` + 0x0a, LF-terminated. One added line = 250 = at the cap.\n- index-surface.test.ts: ExportKind union at :100 = \"array\"|\"function\"|\"number\"|\"record\" (no \"string\"); EXPECTED_EXPORTS declared :121, count pinned `toBe(111)` :190; runtime set-equality :466; per-export it.each :469. WITHHELD_BUDGET_NAMES 6 (:205), WITHHELD_BINDING_NAMES 3 (:222), WITHHELD_GRAPH_CONTENT_NAMES 8 (:243).\n- node-authority-public.{ts,js} ABSENT at HEAD (ls of node-authority/ shows only budget/codec/compose/contract/fields/recursion + 2 test files).\n- The six node-authority modules export EXACTLY 41 runtime bindings, counted by hand off `grep -n export`: budget 2, codec 5, compose 7, contract 21, fields 4, recursion 2. The plan's publish-19 + withhold-22 split covers all 41 with no remainder.\n- LAYER_NAMES module-private in BOTH: contract.ts:101 (11 members, verbatim as planned), recursion.ts:27 (2 members: NODE_AUTHORITY_RECURSION, GRAPH_SNAPSHOT). Only the TYPES are exported (contract.ts:111, recursion.ts:32).\n- Fixture relative imports where the plan says: index-surface.test.ts :23-25, smoke worker :26-27, both carrying comments naming task-210efa47.\n- TypeScript 7.0.2 (satisfies + exhaustiveness asserts usable).\n\nBASELINE GATES, fresh, foreground, separate legs, from PowerShell:\n- `pnpm --filter @moe/scheduler typecheck` -> EXIT 0, script echo `$ tsc --project tsconfig.json` seen (execution evidence per project rail).\n- `pnpm --filter @moe/scheduler test` -> EXIT 0, `Test Files 47 passed (47)` / `Tests 1553 passed (1553)`.\n- Merge-base for the path-attributed baseline = HEAD 1558a61 (worktree carries no diff of mine yet).\n\nNEW FINDING — A DEFECT IN STEP 3, NOT IN MY MEASUREMENT. Step 3 mints `export const NODE_AUTHORITY_LAYERS = ...` and `export const NODE_AUTHORITY_RECURSION_LAYERS = ...` in node-authority-public.ts. Both match the LIVE SOURCE SCAN in tests/security/boundary-roster.security.ts:366, `/^export const ([A-Z_]+(?:LAYERS|LAYER|BOUNDARIES))\\s*(?::[^=]+)?=/u`, whose SCAN_ROOTS (:357) include \"packages\" and whose isProductionModule (:397) excludes only *.test.ts/*.spec.ts/*-fixtures.ts/packages/testkit — node-authority-public.ts is a scanned production module. Consequences measured at HEAD: EXPECTED_ROSTER_SIZE = 102 (:332) asserted by \"scans exactly the roster's cardinality\" (:510) and \"holds exactly the corrected number of hand-written entries\" (:472); \"names every scanned constant in the roster\" (:500) would list both new names; EXPECTED_DISTRIBUTION (:339) pins \"packages/scheduler\": 8, needing 10; scheduler-activation-boundaries.security.ts:162 pins its axis subset `toHaveLength(29)` and :165 asserts hostile coverage for EVERY rostered scheduler-activation boundary. BOUNDARY_ROSTER has ZERO node-authority entries today. This is deliberate, not an oversight: node-authority-contract.ts:23 and node-authority-recursion.ts:12 both say in prose that the layer constant stays MODULE-PRIVATE precisely because \"the security roster counts exported `*_LAYER(S)`\".\n\nSECURITY BASELINE (the gate that would catch it, and it is in NO other leg): `pnpm test:security` -> EXIT 1 at HEAD, `Test Files 3 failed | 7 passed (10)` / `Tests 5 failed | 393 passed (398)`. The three reds are integrity-boundaries.security.ts (5 tests), completeness.security.ts and scheduler-activation-boundaries.security.ts (both suite-level), ALL one foreign cause — `GRAPH_CONTENT_FIELD_INVALID` / `GRAPH_CONTENT_CODEC`, \"content field \\\"nodeAuthority\\\" is absent\", i.e. a92544f fixture fallout owned by successor task-8c7e6ce4. CRITICALLY: boundary-roster.security.ts is GREEN at baseline, so minting the two constants would be a NEW failing file introduced by MY diff — which the project gate rail explicitly forbids excusing (\"a worker may NOT use this to excuse a failure their own diff introduced\").\n\nI am NOT proceeding to step 2 with the two layer names pinned until this is ruled on: escalated to @governor-7211ec87 / @architect-0975cd3a. Steps 2-6 are otherwise unaffected and I resume the moment the shape is decided.",
      "startedAt": "2026-08-21T09:54:52.431Z",
      "completedAt": "2026-08-21T09:59:23.387Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD RED — extend packages/scheduler/src/index-surface.test.ts ONLY (leave the :23-25 relative fixture imports in place this step so the red is assertion-level, not a file compile error): (a) EXPECTED_EXPORTS += 22 rows, count 111 -> 133, ExportKind union += \"string\". New rows with kinds: functions(6) createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes, deriveNodeAuthoritySet, snapshotIdentityHash; strings(2) NODE_AUTHORITY_SCHEMA_TAG, NODE_AUTHORITY_DIGEST_DOMAIN; number(1) NODE_AUTHORITY_SCHEMA_VERSION; arrays(11) NODE_JOIN_ROLES, NODE_ADMISSION_METERS, NODE_ADMISSION_GATE_POLICIES, NODE_DEFINITION_KEYS, NODE_AUTHORITY_DRAFT_KEYS, NODE_AUTHORITY_FORBIDDEN_IDENTITY_KEYS, NODE_AUTHORITY_EXCLUDED_STATE_KEYS, NODE_AUTHORITY_CODES, NODE_AUTHORITY_RECURSION_CODES, NODE_AUTHORITY_LAYERS, NODE_AUTHORITY_RECURSION_LAYERS; records(2) NODE_ADMISSION_GATE_POLICY_WITNESS, NODE_AUTHORITY_LIMITS. (b) New WITHHELD_NODE_AUTHORITY_NAMES roster, length pinned 22, leak-check against Object.keys(scheduler) toStrictEqual([]), positive controls createNodeDefinition+deriveNodeAuthoritySet present: draftNodeAuthority, readDraftFields, readText, normalizeScope, forbiddenKeyRefusal, forbiddenBudgetKeyRefusal, readNodeAuthorityBudget, ok, refuse, passthrough, compareStrings, deepFreeze, canonicalText, nodeBodyDigest, canonicalEnvelopeJson, pick, admitPlanning, applicable, composeEdges, requirementsOf, readDerived, project (publish 19 + minted 2 + snapshotIdentityHash + withheld 22 = the complete 41-export split; rationale comment: preimage/canonical-text mechanics, identity-less draft, compose/field/budget internals). (c) WITHHELD_GRAPH_CONTENT_NAMES += canonicalContentJson, projectContent, graphContentDigest, readContentFields — 8 -> 12, length pin bumped (all four verified exported-but-unpublished; snapshotIdentityHash is deliberately NOT added — it is being PUBLISHED, see plan notes). (d) New import-type block from \"@moe/scheduler\" for the 24 published types (roster in step 3) + annotations on the existing codecNodeAuthority fixture values so the type closure compiles only from the root. NOTE: (d) makes the FILE compile-fail while the runtime assertions (a-c) also red — run vitest once WITHOUT (d) staged if you want a clean assertion-level red first, then add (d); record the red shape either way. Focused run: npx vitest run --root ../.. packages/scheduler/src/index-surface.test.ts from packages/scheduler — RED, reds named.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/scheduler/src/index-surface.test.ts"
      ],
      "note": "TDD RED landed, built against the AMENDED step text (amend-1, governor-7211ec87 10:13:07Z) — 20 new runtime rows, count 131, arrays(9), the two *_LAYERS names NOT added (deferred to task-515d2f90).\n\nWHAT I WROTE, index-surface.test.ts only:\n(a) ExportKind union += \"string\" (the file's first string exports on the root; the it.each else-arm is a generic `typeof value` so no arm change was needed). 20 rows inserted IN SORT ORDER — the set-equality compares against `Object.keys(scheduler).sort()`, so a misplaced row reds a passing surface. 14 constants between MIN_GATED_DESCENDANTS_FOR_REVIEW and PROTECTED_ADMISSION_PURPOSES; 6 functions at their own sorted seats (admitNodeDefinition after admitExpansion, createNodeDefinition before createTraversalCounter, decodeNodeDefinitionBytes after decodeGraphContent, deriveNodeAuthoritySet after deriveExpansionEvidence, encodeNodeDefinition after encodeGraphContent, snapshotIdentityHash after settleReservation). Count pin 111 -> 131.\n(b) WITHHELD_NODE_AUTHORITY_NAMES, 22 names, length pinned 22, leak-check `filter(published.has)` toStrictEqual([]) plus positive controls on createNodeDefinition + deriveNodeAuthoritySet so an empty namespace cannot pass it vacuously. Comment records the bidirectional arithmetic: 19 published + 22 withheld = the 41 runtime bindings the six modules export, so an unreviewed addition lands in neither list and the set-equality names it.\n(c) WITHHELD_GRAPH_CONTENT_NAMES 8 -> 12 (canonicalContentJson, projectContent, graphContentDigest, readContentFields), length pin bumped. snapshotIdentityHash deliberately NOT added — it is being published.\n(d) New `import type { ... } from \"@moe/scheduler\"` block, all 24 published types, BARE specifier. `noUnusedLocals` is on in tsconfig.base.json, so every one of the 24 must be USED or typecheck reds — I proved them by annotation on production values rather than by declaration: 9 in codecNodeAuthority itself (NodeAuthoritySection return, NodeAuthorityResult on `built`, NodeAuthorityBody on `built.value`, NodeAuthorityIssue and NodeAuthorityRecursionIssue on the two issue callbacks, NodeAuthorityEdgeInput + NodeDefinition on `body`'s signature, NodeAuthorityRecursionResult on `derived`, NodeAuthorityEntry on `authorities`), and the remaining 15 in a new case, \"closes the published node-authority type surface over production values\". EXPECTED_EXPORTS is blind to types — a type publishes no runtime key — so without that case the root could publish every runtime name and no type at all and every other assertion here would still pass.\n\nThe new case's two refusal arms pin code AND layer per epic rail 6, and both values are MEASURED, not guessed: I probed the modules directly under `node --experimental-strip-types` before writing them — admitNodeDefinition({}) -> [\"NODE_AUTHORITY_UNSUPPORTED_SCHEMA\",\"NODE_AUTHORITY_SCHEMA\"], deriveNodeAuthoritySet(snapshot, []) -> [\"NODE_AUTHORITY_RECURSION_NODE_MISSING\",\"NODE_AUTHORITY_RECURSION\"]. Probe file created and deleted in the same command. I also hoisted the two-node fixture graph to CODEC_SNAPSHOT so the new case and the existing codec case build from one graph rather than two copies (one-line change inside the existing case; its assertions untouched).\n\nRED SHAPE, both layers, recorded fresh:\n1. RUNTIME — `npx vitest run --root ../.. packages/scheduler/src/index-surface.test.ts` from packages/scheduler -> EXIT 1, `Test Files 1 failed (1)`, `Tests 23 failed | 238 passed (261)`. The 23: 20 per-export it.each cases (\"publishes <name> on the package root as a <kind>\", each `expected 'undefined' to be 'function'/'array'/'string'/'number'` or the record arm), the set-equality \"publishes exactly the reviewed root namespace, with no loss and no addition\", the new \"withholds the node-authority internals that would bypass the admission authority\" (reds on its POSITIVE controls — the withheld names correctly do not leak, but createNodeDefinition/deriveNodeAuthoritySet are not published yet), and the new \"closes the published node-authority type surface over production values\". Note `EXPECTED_EXPORTS.length toBe(131)` PASSES — it reads the roster, not the root, exactly as intended.\n2. TYPE — `pnpm --filter @moe/scheduler typecheck` -> EXIT 1, 28 errors, all in index-surface.test.ts: TS2305 x21 + TS2724 x3 = the 24 type members absent from the root (TS2724 is the \"did you mean\" family, e.g. NodeAdmissionAmount -> AdmissionAmount), plus TS2339 x4 for the runtime members read off the namespace (NODE_DEFINITION_KEYS, encodeNodeDefinition, admitNodeDefinition, deriveNodeAuthoritySet). Vitest strips types rather than checking them, so the type red is invisible to the focused run — both layers had to be captured separately, and both go green in step 3.\n\nSCOPE: `git status --porcelain -- packages/scheduler` shows exactly one modified path, packages/scheduler/src/index-surface.test.ts. No production file touched, no probe or scratch file left behind, nothing staged.\n\nSTEPS 5 AND 6 STILL NEED",
      "startedAt": "2026-08-21T10:13:34.779Z",
      "completedAt": "2026-08-21T10:19:45.012Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "TDD RED — extend packages/scheduler/src/index-surface.test.ts ONLY (leave the :23-25 relative fixture imports in place this step so the red is assertion-level, not a file compile error): (a) EXPECTED_EXPORTS += 20 rows, count 111 -> 131, ExportKind union += \"string\". New rows with kinds: functions(6) createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes, deriveNodeAuthoritySet, snapshotIdentityHash; strings(2) NODE_AUTHORITY_SCHEMA_TAG, NODE_AUTHORITY_DIGEST_DOMAIN; number(1) NODE_AUTHORITY_SCHEMA_VERSION; arrays(9) NODE_JOIN_ROLES, NODE_ADMISSION_METERS, NODE_ADMISSION_GATE_POLICIES, NODE_DEFINITION_KEYS, NODE_AUTHORITY_DRAFT_KEYS, NODE_AUTHORITY_FORBIDDEN_IDENTITY_KEYS, NODE_AUTHORITY_EXCLUDED_STATE_KEYS, NODE_AUTHORITY_CODES, NODE_AUTHORITY_RECURSION_CODES; records(2) NODE_ADMISSION_GATE_POLICY_WITNESS, NODE_AUTHORITY_LIMITS. DEFERRED BY AMENDMENT (do NOT add): NODE_AUTHORITY_LAYERS and NODE_AUTHORITY_RECURSION_LAYERS — runtime layer-roster publication moved to task-515d2f90 with its security-roster trio (roster entry + EXPECTED_ROSTER_SIZE/distribution bumps + hostile arms), because exported *_LAYER(S) constants are counted by the live scan in tests/security/boundary-roster.security.ts and this row does not own that file. The layer VOCABULARY still publishes here as the TYPES NodeAuthorityLayer / NodeAuthorityRecursionLayer in (d)'s 24-type block. (b) New WITHHELD_NODE_AUTHORITY_NAMES roster, length pinned 22, leak-check against Object.keys(scheduler) toStrictEqual([]), positive controls createNodeDefinition+deriveNodeAuthoritySet present: draftNodeAuthority, readDraftFields, readText, normalizeScope, forbiddenKeyRefusal, forbiddenBudgetKeyRefusal, readNodeAuthorityBudget, ok, refuse, passthrough, compareStrings, deepFreeze, canonicalText, nodeBodyDigest, canonicalEnvelopeJson, pick, admitPlanning, applicable, composeEdges, requirementsOf, readDerived, project (publish 19 + snapshotIdentityHash = 20 published runtime names; withheld 22; the six modules' 41 runtime bindings split 19 published + 22 withheld, snapshotIdentityHash arriving from graph-content-format; rationale comment: preimage/canonical-text mechanics, identity-less draft, compose/field/budget internals). (c) WITHHELD_GRAPH_CONTENT_NAMES += canonicalContentJson, projectContent, graphContentDigest, readContentFields — 8 -> 12, length pin bumped (all four verified exported-but-unpublished; snapshotIdentityHash is deliberately NOT added — it is being PUBLISHED, see plan notes). (d) New import-type block from \"@moe/scheduler\" for the 24 published types (roster in step 3) + annotations on the existing codecNodeAuthority fixture values so the type closure compiles only from the root. NOTE: (d) makes the FILE compile-fail while the runtime assertions (a-c) also red — run vitest once WITHOUT (d) staged if you want a clean assertion-level red first, then add (d); record the red shape either way. Focused run: npx vitest run --root ../.. packages/scheduler/src/index-surface.test.ts from packages/scheduler — RED, reds named.",
          "reason": "Branch-A amendment, three-party adjudicated (worker finding comment-d6403717; QA re-measure concurrence 10:00:51Z; governor ruling msg-4c0d207d; no live architect — governor transcribing the agreed deltas, authored by none of the graded parties' invention). The two runtime layer rosters are DEFERRED to task-515d2f90 (security-roster trio row): exporting *_LAYERS constants is counted by tests/security/boundary-roster.security.ts and would red a green security file from this row's own diff. Deltas only: 22→20 new runtime rows, count 133→131, arrays(11)→arrays(9); everything else byte-identical to the original step.",
          "amendedBy": "governor-7211ec87",
          "amendedAt": "2026-08-21T10:13:07.739Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-3",
      "description": "GREEN — (1) CREATE packages/scheduler/src/node-authority/node-authority-public.ts (<=250 lines): curated forwarding module on the execution-surface.ts pattern (curation lives in export specifiers; each family's rationale travels with its block). Runtime re-exports: createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes from ./node-authority-codec.js; deriveNodeAuthoritySet, NODE_AUTHORITY_RECURSION_CODES from ./node-authority-recursion.js; the 13 contract constants (NODE_AUTHORITY_SCHEMA_TAG, NODE_AUTHORITY_DIGEST_DOMAIN, NODE_AUTHORITY_SCHEMA_VERSION, NODE_JOIN_ROLES, NODE_ADMISSION_METERS, NODE_ADMISSION_GATE_POLICIES, NODE_ADMISSION_GATE_POLICY_WITNESS, NODE_DEFINITION_KEYS, NODE_AUTHORITY_DRAFT_KEYS, NODE_AUTHORITY_FORBIDDEN_IDENTITY_KEYS, NODE_AUTHORITY_EXCLUDED_STATE_KEYS, NODE_AUTHORITY_CODES, NODE_AUTHORITY_LIMITS) from ./node-authority-contract.js; snapshotIdentityHash from ../graph-content-format.js WITH its rationale comment (it accepts ONLY the brand-protected ValidatedGraph so it cannot hash a structure the kernel never validated, and hard-edge dependency contracts require graphBindingDigest === snapshotIdentityHash(graph) BEFORE any content encode can exist — closeContracts refuses NODE_AUTHORITY_RECURSION_BINDING_MISMATCH at recursion.ts:164; encodeGraphContent remains the only route to a graphContentHash, so index.ts's withholding comment stays true). MINT the two layer rosters the modules keep private (recorded hazard: LAYER_NAMES is module-private, unowned files may not be edited): export const NODE_AUTHORITY_LAYERS = Object.freeze([NODE_AUTHORITY_ADMISSION, NODE_AUTHORITY_BUDGET, NODE_AUTHORITY_CODEC, NODE_AUTHORITY_DEPENDENCIES, NODE_AUTHORITY_IDENTITY, NODE_AUTHORITY_LIMITS, NODE_AUTHORITY_PROOFS, NODE_AUTHORITY_SCHEMA, NODE_AUTHORITY_SCOPES, DEPENDENCY_CONTRACT, PLANNING_SOURCE] as const) and NODE_AUTHORITY_RECURSION_LAYERS = Object.freeze([NODE_AUTHORITY_RECURSION, GRAPH_SNAPSHOT, ...the same 11] as const), EACH compile-pinned in BOTH directions: `satisfies readonly NodeAuthorityLayer[]` (resp. NodeAuthorityRecursionLayer[]) refuses a bogus member, and a type-level exhaustiveness assert ([NodeAuthorityLayer] extends [(typeof NODE_AUTHORITY_LAYERS)[number]] ? true : never, assigned to a typed const) refuses a dropped member — drift is a typecheck red, never silent. Type re-exports (24): NodeAuthorityBody, NodeAuthorityResult, NodeAuthorityBytesResult (codec); NodeAuthorityDraftResult, NodeJoinRole, NodeAdmissionMeter, NodeAdmissionAmount, NodeAdmissionGatePolicy, NodeDefinitionKey, NodeAuthorityCode, NodeAuthorityLayer, NodeAuthorityIssue, NodeAuthorityRefusal, NodeCriterionBinding, NodeDependencyEntry, NodeAuthorityEdgeInput, NodeAuthorityDraft, NodeDefinition (contract); NodeAuthorityRecursionLayer, NodeAuthorityRecursionCode, NodeAuthorityRecursionIssue, NodeAuthorityEntry, NodeAuthorityRecursionResult (recursion); NodeAuthoritySection from ../graph-content.js. WITHHELD TYPES, named in the rationale, never re-exported: Read (contract), DerivedIdentity, AdmittedPlanning, ComposedEdges (compose), NodeAuthorityBudget (budget). (2) CREATE the sibling bridge node-authority-public.js: exactly one LF-terminated line `export * from \"./node-authority-public.ts\";`. (3) index.ts: append ONE line `export * from \"./node-authority/node-authority-public.js\";` -> 250 lines exactly; touch nothing else (collision-checked: zero overlap with the current 111-key namespace or execution-surface/budget-ledger stars). (4) Flip the fixture imports to the bare root: index-surface.test.ts :23-25 (all three now root-reachable — delete the FIXTURE ONLY comment) and scheduler-entrypoint-smoke-worker.mjs :26-27 (merge into its existing @moe/scheduler import block; REWRITE the :21-25 comment: publication landed via THIS task; the ./node-authority/*.js sibling bridges stay under Node's real resolution through root -> node-authority-public.js -> codec.js/recursion.js, which transitively cover all six). Runtime-entrypoint expectations are unchanged by design (values are codec-derived, panel-verified). Run: focused index-surface + scheduler-runtime-entrypoint, then pnpm --filter @moe/scheduler test — GREEN, counts must EXCEED 47/1553's test count (new assertions executed, nonzero).",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/index.ts",
        "packages/scheduler/src/index-surface.test.ts",
        "packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs"
      ],
      "newFiles": [
        "packages/scheduler/src/node-authority/node-authority-public.ts",
        "packages/scheduler/src/node-authority/node-authority-public.js"
      ],
      "modifiedFiles": [
        "packages/scheduler/src/node-authority/node-authority-public.ts",
        "packages/scheduler/src/node-authority/node-authority-public.js",
        "packages/scheduler/src/index.ts",
        "packages/scheduler/src/index-surface.test.ts",
        "packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs"
      ],
      "note": "GREEN. Built against amend-1 (step 3's overlay), so NO runtime layer rosters were minted.\n\nWHAT LANDED:\n(1) CREATED node-authority-public.ts, 153 lines (cap 250), on the execution-surface.ts pattern: curation lives in the export specifiers, each family's rationale travelling with its block. 20 runtime re-exports — 4 codec (create/admit/encode/decodeNodeDefinitionBytes), 2 recursion (deriveNodeAuthoritySet, NODE_AUTHORITY_RECURSION_CODES), 13 contract constants, and snapshotIdentityHash from ../graph-content-format.js with its full rationale (ValidatedGraph-only input, so it cannot hash an unvalidated structure; hard-edge contracts need graphBindingDigest === snapshotIdentityHash(graph) BEFORE any encode can exist or closeContracts refuses NODE_AUTHORITY_RECURSION_BINDING_MISMATCH at recursion.ts:164; encodeGraphContent stays the only route to a graphContentHash so index.ts's withholding comment stays true). 24 type re-exports: 3 codec, 15 contract, 5 recursion, NodeAuthoritySection from ../graph-content.js. Withheld types named in prose and never re-exported: Read, DerivedIdentity, AdmittedPlanning, ComposedEdges, NodeAuthorityBudget.\n(2) At the spot the mint would have occupied, a comment block states exactly why there is no runtime layer roster and names task-515d2f90 — LAYER_NAMES is module-private in both producers BECAUSE the security lane rosters column-0 `export const *_LAYER(S)` (contract.ts:23, recursion.ts:12), the scan is tests/security/boundary-roster.security.ts DECLARATION_PATTERN :366 over SCAN_ROOTS including packages, and this row owns no security file. The layer vocabulary still reaches the root at TYPE level via NodeAuthorityLayer / NodeAuthorityRecursionLayer.\n(3) CREATED the sibling bridge node-authority-public.js: exactly one LF-terminated line, verified byte-wise with `cat -A` -> `export * from \"./node-authority-public.ts\";$`.\n(4) index.ts: appended the ONE forward line. `wc -l` now reports EXACTLY 250 — at the cap, zero slack, which is why the forward line carries no comment.\n(5) Flipped both fixtures to the bare root. index-surface.test.ts: the three relative imports collapse into one `from \"@moe/scheduler\"` and the FIXTURE ONLY comment is replaced by one saying they now travel the path a real consumer does. scheduler-entrypoint-smoke-worker.mjs: createNodeDefinition + deriveNodeAuthoritySet merged into its existing root import block, and the :21-25 comment REWRITTEN to record that the sibling-bridge coverage is unchanged and why — under Node's real resolution the root reaches node-authority-public.js, which reaches codec.js and recursion.js, which reach the remaining three, so a missing or CRLF bridge anywhere on that chain still surfaces as ERR_MODULE_NOT_FOUND here, and vitest (which resolves ./x.js back to x.ts) is still blind to it. That worker remains the only witness.\n\nGREEN EVIDENCE, fresh, separate legs:\n- Focused: `npx vitest run --root ../.. packages/scheduler/src/index-surface.test.ts packages/scheduler/src/scheduler-runtime-entrypoint.test.ts` -> EXIT 0, `Test Files 2 passed (2)` / `Tests 262 passed (262)`. Runtime-entrypoint expectations needed no change, as the plan predicted.\n- `pnpm --filter @moe/scheduler typecheck` -> EXIT 0 with the `$ tsc --project tsconfig.json` echo. All 28 step-2 errors cleared (24 type members + 4 namespace members).\n- `pnpm --filter @moe/scheduler test` -> EXIT 0, `Test Files 47 passed (47)` / `Tests 1575 passed (1575)`. Baseline was 1553, so +22 executed: 20 per-export it.each cases + the withheld-node-authority leak test + the type-closure test. Nonzero and strictly greater, per DoD 5.\n\nSECURITY LANE, measured because branch A exists precisely to protect it: `pnpm test:security` -> `Test Files 4 failed | 6 passed (10)` / `Tests 6 failed | 392 passed (398)`. boundary-roster.security.ts IS STILL GREEN — the mint was the only thing that would have reddened it, and it was not minted. Branch A did what it was ruled for.\n\nTHE 4th FAILING FILE IS FOREIGN AND BRAND NEW — attributed, not excused. My baseline at 1558a61 was 3 files / 5 tests. The new red is lane-smoke.security.ts, \"still discovers *.test.ts only, so hostile files are not regression evidence\", which pins the root vitest include to exactly [\"adapters/**/*.test.ts\",\"packages/**/*.test.ts\",\"tests/**/*.test.ts\"]. HEAD MOVED UNDER ME while I worked — 1558a61 -> f28cfe4, six foreign commits — and `git log -S'tools/**/*.test.ts' -- vitest.config.ts` names f28cfe4 (\"fix(tools): bind both importers to the store's per-commit cap\", 2026-08-21 13:19:43 +0300) as the commit that added a FOURTH include pattern without updating the pin. `git diff --stat -- vitest.config.ts` in my worktree is empty, so I did not touch it; f28cfe4's four files are all under tools/ plus that config. The other three reds are the same pre-existing GRAPH_CONTENT_FIELD_INVALID / nodeAuthority fixture cause owned by task-8c7e6ce4. I am reporting this in chat rather than repairing it — it is not my file and not my scope.\n\n",
      "startedAt": "2026-08-21T10:19:49.437Z",
      "completedAt": "2026-08-21T10:23:14.777Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "GREEN — (1) CREATE packages/scheduler/src/node-authority/node-authority-public.ts (<=250 lines): curated forwarding module on the execution-surface.ts pattern (curation lives in export specifiers; each family's rationale travels with its block). Runtime re-exports: createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes from ./node-authority-codec.js; deriveNodeAuthoritySet, NODE_AUTHORITY_RECURSION_CODES from ./node-authority-recursion.js; the 13 contract constants (NODE_AUTHORITY_SCHEMA_TAG, NODE_AUTHORITY_DIGEST_DOMAIN, NODE_AUTHORITY_SCHEMA_VERSION, NODE_JOIN_ROLES, NODE_ADMISSION_METERS, NODE_ADMISSION_GATE_POLICIES, NODE_ADMISSION_GATE_POLICY_WITNESS, NODE_DEFINITION_KEYS, NODE_AUTHORITY_DRAFT_KEYS, NODE_AUTHORITY_FORBIDDEN_IDENTITY_KEYS, NODE_AUTHORITY_EXCLUDED_STATE_KEYS, NODE_AUTHORITY_CODES, NODE_AUTHORITY_LIMITS) from ./node-authority-contract.js; snapshotIdentityHash from ../graph-content-format.js WITH its rationale comment (it accepts ONLY the brand-protected ValidatedGraph so it cannot hash a structure the kernel never validated, and hard-edge dependency contracts require graphBindingDigest === snapshotIdentityHash(graph) BEFORE any content encode can exist — closeContracts refuses NODE_AUTHORITY_RECURSION_BINDING_MISMATCH at recursion.ts:164; encodeGraphContent remains the only route to a graphContentHash, so index.ts's withholding comment stays true). DO NOT MINT RUNTIME LAYER ROSTERS — deferred by amendment to task-515d2f90: NODE_AUTHORITY_LAYERS / NODE_AUTHORITY_RECURSION_LAYERS are exported *_LAYER(S) constants counted by the live security scan (tests/security/boundary-roster.security.ts DECLARATION_PATTERN :366, SCAN_ROOTS include packages), this row does not own the security files, and the producing modules' module-private LAYER_NAMES (node-authority-contract.ts:23, node-authority-recursion.ts:12) is a documented security decision this row must not reverse; the layer vocabulary publishes here at TYPE level only (NodeAuthorityLayer, NodeAuthorityRecursionLayer below). Add a short comment block at the spot the mint would have occupied stating exactly that, naming task-515d2f90. Type re-exports (24): NodeAuthorityBody, NodeAuthorityResult, NodeAuthorityBytesResult (codec); NodeAuthorityDraftResult, NodeJoinRole, NodeAdmissionMeter, NodeAdmissionAmount, NodeAdmissionGatePolicy, NodeDefinitionKey, NodeAuthorityCode, NodeAuthorityLayer, NodeAuthorityIssue, NodeAuthorityRefusal, NodeCriterionBinding, NodeDependencyEntry, NodeAuthorityEdgeInput, NodeAuthorityDraft, NodeDefinition (contract); NodeAuthorityRecursionLayer, NodeAuthorityRecursionCode, NodeAuthorityRecursionIssue, NodeAuthorityEntry, NodeAuthorityRecursionResult (recursion); NodeAuthoritySection from ../graph-content.js. WITHHELD TYPES, named in the rationale, never re-exported: Read (contract), DerivedIdentity, AdmittedPlanning, ComposedEdges (compose), NodeAuthorityBudget (budget). (2) CREATE the sibling bridge node-authority-public.js: exactly one LF-terminated line `export * from \"./node-authority-public.ts\";`. (3) index.ts: append ONE line `export * from \"./node-authority/node-authority-public.js\";` -> 250 lines exactly; touch nothing else (collision-checked: zero overlap with the current 111-key namespace or execution-surface/budget-ledger stars). (4) Flip the fixture imports to the bare root: index-surface.test.ts :23-25 (all three now root-reachable — delete the FIXTURE ONLY comment) and scheduler-entrypoint-smoke-worker.mjs :26-27 (merge into its existing @moe/scheduler import block; REWRITE the :21-25 comment: publication landed via THIS task; the ./node-authority/*.js sibling bridges stay under Node's real resolution through root -> node-authority-public.js -> codec.js/recursion.js, which transitively cover all six). Runtime-entrypoint expectations are unchanged by design (values are codec-derived, panel-verified). Run: focused index-surface + scheduler-runtime-entrypoint, then pnpm --filter @moe/scheduler test — GREEN, counts must EXCEED 47/1553's test count (new assertions executed, nonzero).",
          "reason": "Branch-A amendment, same adjudication as step-2 amend-1 (comment-d6403717; QA concurrence; governor ruling msg-4c0d207d). Sole delta: the MINT paragraph for the two runtime layer rosters is REMOVED and replaced by an explicit deferral to task-515d2f90; publishing exported *_LAYER(S) constants without the security-roster trio reds tests/security/boundary-roster.security.ts (green at baseline) from this row's own diff, and reverses the documented module-privacy decision as a side effect. Type re-exports and every other instruction stay byte-identical.",
          "amendedBy": "governor-7211ec87",
          "amendedAt": "2026-08-21T10:13:32.571Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-4",
      "description": "TEMPORARY PROBES (DoD 3), from POWERSHELL (plain-node smoke fails from Git Bash — fleet memory), all probe files under packages/scheduler and DELETED IN THE SAME COMMAND, worktree porcelain-clean after: (1) NodeNext compile POSITIVE: a temp probe .ts inside packages/scheduler importing from the BARE \"@moe/scheduler\" specifier the new runtime names (createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes, deriveNodeAuthoritySet, snapshotIdentityHash, NODE_AUTHORITY_LAYERS, NODE_AUTHORITY_LIMITS) and typed positions using NodeDefinition, NodeAuthorityRecursionResult, NodeAuthoritySection; typecheck it with the package's own tsc project or a scoped tsc --noEmit --module nodenext --moduleResolution nodenext (reuse 6ba1ff89's landed probe recipe from its completion record if present) -> EXIT 0. (2) NodeNext NEGATIVES, each expecting the exact missing-export diagnostic (record the code verbatim, expect TS2305/TS2724-family, one probe per name): bogus symbol `nodeAuthorityBogusSymbol`, plus the five withheld types Read, DerivedIdentity, AdmittedPlanning, ComposedEdges, NodeAuthorityBudget, plus two withheld runtime names draftNodeAuthority and nodeBodyDigest -> EACH fails naming the symbol; capture the diagnostic lines. (3) plain-Node package-root POSITIVE: temp .mjs run with `node --experimental-strip-types` importing bare \"@moe/scheduler\", calling createNodeDefinition + deriveNodeAuthoritySet on the smoke worker's known-good single-node fixture shape and encodeGraphContent/decodeGraphContent round trip, printing derived count(1) + hash length(64) + roundTrip MATCHED -> EXIT 0 with those values printed (nonzero evidence, not bare exit). (4) internal-subpath NEGATIVE: same probe attempts await import(\"@moe/scheduler/src/node-authority/node-authority-public.js\") and asserts refusal code ERR_PACKAGE_PATH_NOT_EXPORTED (assert the CODE, not just a throw). Delete probes in the same command chain; `git status --porcelain` afterward shows only the five owned paths modified.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "PROBES ALL PASS. Every probe file created and deleted; worktree porcelain-clean after, showing only the five owned paths. All runs from PowerShell (plain-node smoke fails from Git Bash — fleet memory).\n\nTIMING NOTE, stated because it matters for grading: I ran probe (1) BEFORE step-4's amend-1 landed at 10:24:25Z, and I substituted NODE_AUTHORITY_CODES for the deferred NODE_AUTHORITY_LAYERS on my own judgement, because the ruled-away constant cannot be imported and the probe as written could not pass. The overlay then landed saying exactly that — \"NODE_AUTHORITY_CODES, NODE_AUTHORITY_LIMITS — NODE_AUTHORITY_LAYERS removed by amend-1: deferred to task-515d2f90\". So what I ran and what the amended step instructs are the same thing, but the substitution was mine first, not the plan's. Recording it rather than letting it read as compliance after the fact.\n\n(1) NODENEXT COMPILE POSITIVE -> EXIT 0. Temp probe-positive.ts under packages/scheduler importing from the BARE \"@moe/scheduler\": all 8 runtime names (createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes, deriveNodeAuthoritySet, snapshotIdentityHash, NODE_AUTHORITY_CODES, NODE_AUTHORITY_LIMITS) plus typed positions on NodeDefinition, NodeAuthorityRecursionResult and NodeAuthoritySection. Recipe: `pnpm --filter @moe/scheduler exec tsc --noEmit --strict --module nodenext --moduleResolution nodenext --target es2024 --lib es2024 --types node --exactOptionalPropertyTypes --noUncheckedIndexedAccess --verbatimModuleSyntax --skipLibCheck --ignoreConfig probe-positive.ts`. Two things had to be right: `pnpm --filter ... exec` so the cwd is the package and the bare specifier resolves at all, and `--types node` — without it the run reds with 100+ TS2304/TS2591 in FOREIGN files (TextEncoder, node:crypto in packages/contracts and packages/core), which is a missing-lib artifact and not a statement about my surface. Worth knowing before anyone reads a scoped-probe red as a real one.\n\n(2) NODENEXT NEGATIVES -> 10 probes, EACH exit 1, each naming its own symbol. The 8 the step names, verbatim:\n  nodeAuthorityBogusSymbol -> TS2724 '\"@moe/scheduler\"' has no exported member named 'nodeAuthorityBogusSymbol'. Did you mean 'NodeAuthorityBody'?\n  draftNodeAuthority -> TS2305 ... has no exported member 'draftNodeAuthority'.\n  nodeBodyDigest -> TS2305 ... has no exported member 'nodeBodyDigest'.\n  Read -> TS2305; DerivedIdentity -> TS2305; AdmittedPlanning -> TS2305; ComposedEdges -> TS2305.\n  NodeAuthorityBudget -> TS2724 ... Did you mean 'NodeAuthorityBody'?\nTWO EXTRA NEGATIVES I ADDED, and they are the load-bearing ones for branch A — they prove the deferral is real rather than asserted:\n  NODE_AUTHORITY_LAYERS -> TS2724 '\"@moe/scheduler\"' has no exported member named 'NODE_AUTHORITY_LAYERS'. Did you mean 'NodeAuthorityLayer'?\n  NODE_AUTHORITY_RECURSION_LAYERS -> TS2724 ... Did you mean 'NodeAuthorityRecursionLayer'?\nBoth diagnostics point at the TYPE as the near-miss, which is precisely the shape branch A intends: the runtime array is absent, the closed union is reachable. A cold grader can read the deferral off the compiler rather than off my prose.\n\n(3) PLAIN-NODE PACKAGE-ROOT POSITIVE -> EXIT 0 with values printed, not a bare exit. `node --experimental-strip-types probe-runtime.mjs` from packages/scheduler, importing bare \"@moe/scheduler\", building the smoke worker's known-good single-node fixture (\"runtime-done\") through production code end to end — createPlanRevision + createAcceptanceContract from @moe/core, then createNodeDefinition, deriveNodeAuthoritySet, encodeGraphContent, decodeGraphContent:\n  DERIVED_COUNT=1\n  AUTHORITY_HASH_LENGTH=64\n  SNAPSHOT_IDENTITY_LENGTH=64   (snapshotIdentityHash over the validated graph, exercising the newly published function itself)\n  ROUND_TRIP=MATCHED            (decoded nodeAuthority.authorities[0].nodeAuthorityHash === the derived hash, so the v3 section survives encode/decode through the root)\nNonzero on every count, so no vacuous-pass reading is available.\n\n(4) INTERNAL-SUBPATH NEGATIVE -> INTERNAL_SUBPATH_CODE=ERR_PACKAGE_PATH_NOT_EXPORTED. `await import(\"@moe/scheduler/src/node-authority/node-authority-public.js\")` inside the same probe, asserting the CODE off the caught error rather than merely that it threw. The exports map is unchanged, so root \".\" remains the only public subpath and the new module is unreachable deep.\n\nCLEANUP + SCOPE: probe-positive.ts, probe-neg.ts (rewritten in place for each of the ten negatives) and probe-runtime.mjs all deleted in the same command chain. `ls packages/scheduler/probe-*` -> no such file. `git status --porcelain -- packages/scheduler` from the REPO ROOT shows exactly the five owned paths, 3 modified and 2 untracked-new, nothing else.",
      "startedAt": "2026-08-21T10:24:34.442Z",
      "completedAt": "2026-08-21T10:26:24.141Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "TEMPORARY PROBES (DoD 3), from POWERSHELL (plain-node smoke fails from Git Bash — fleet memory), all probe files under packages/scheduler and DELETED IN THE SAME COMMAND, worktree porcelain-clean after: (1) NodeNext compile POSITIVE: a temp probe .ts inside packages/scheduler importing from the BARE \"@moe/scheduler\" specifier the new runtime names (createNodeDefinition, admitNodeDefinition, encodeNodeDefinition, decodeNodeDefinitionBytes, deriveNodeAuthoritySet, snapshotIdentityHash, NODE_AUTHORITY_CODES, NODE_AUTHORITY_LIMITS — NODE_AUTHORITY_LAYERS removed by amend-1: deferred to task-515d2f90) and typed positions using NodeDefinition, NodeAuthorityRecursionResult, NodeAuthoritySection; typecheck it with the package's own tsc project or a scoped tsc --noEmit --module nodenext --moduleResolution nodenext (reuse 6ba1ff89's landed probe recipe from its completion record if present) -> EXIT 0. (2) NodeNext NEGATIVES, each expecting the exact missing-export diagnostic (record the code verbatim, expect TS2305/TS2724-family, one probe per name): bogus symbol `nodeAuthorityBogusSymbol`, plus the five withheld types Read, DerivedIdentity, AdmittedPlanning, ComposedEdges, NodeAuthorityBudget, plus two withheld runtime names draftNodeAuthority and nodeBodyDigest -> EACH fails naming the symbol; capture the diagnostic lines. (3) plain-Node package-root POSITIVE: temp .mjs run with `node --experimental-strip-types` importing bare \"@moe/scheduler\", calling createNodeDefinition + deriveNodeAuthoritySet on the smoke worker's known-good single-node fixture shape and encodeGraphContent/decodeGraphContent round trip, printing derived count(1) + hash length(64) + roundTrip MATCHED -> EXIT 0 with those values printed (nonzero evidence, not bare exit). (4) internal-subpath NEGATIVE: same probe attempts await import(\"@moe/scheduler/src/node-authority/node-authority-public.js\") and asserts refusal code ERR_PACKAGE_PATH_NOT_EXPORTED (assert the CODE, not just a throw). Delete probes in the same command chain; `git status --porcelain` afterward shows only the five owned paths modified.",
          "reason": "Branch-A consequential amendment (same adjudication chain as steps 2/3 amend-1): probe (1) listed NODE_AUTHORITY_LAYERS among bare-root imports, which no longer exists by ruling — probe as written cannot pass. Sole delta: drop NODE_AUTHORITY_LAYERS, add NODE_AUTHORITY_CODES so the positive probe still covers a frozen roster. Worker-specified delta, governor-transcribed.",
          "amendedBy": "governor-7211ec87",
          "amendedAt": "2026-08-21T10:24:25.053Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-5",
      "description": "MUTATION DRILLS (epic rail 6) — each applied alone, backup copies under %TEMP% suffixed by worker id (never MSYS /tmp), restore proven byte-exact by empty `git diff -- <file>` from the REPO ROOT cwd: (1) delete one published specifier (createNodeDefinition) from node-authority-public.ts -> EXPECTED_EXPORTS set-equality AND the per-export it-case red; (2) ADD a withheld name (draftNodeAuthority) to node-authority-public.ts's codec block -> WITHHELD_NODE_AUTHORITY_NAMES leak-check reds (this is the bidirectional arm: the roster catches an unreviewed ADDITION, not only a removal); (3) delete node-authority-public.js (the bridge) -> scheduler-runtime-entrypoint.test.ts reds with ERR_MODULE_NOT_FOUND under plain Node while vitest-side suites stay green (vitest resolves ./x.js back to x.ts and is blind — this drill proves the smoke is the ONLY witness, run the entrypoint test to see the red); (4) drop PLANNING_SOURCE from NODE_AUTHORITY_LAYERS -> `pnpm --filter @moe/scheduler typecheck` reds on the exhaustiveness assert; separately add a bogus member \"NODE_AUTHORITY_BOGUS\" -> typecheck reds on the satisfies clause (both directions of the compile pin, two sub-drills); (5) remove the ONE forward line from index.ts -> set-equality red names all 22 missing keys. ANY drill that stays green = vacuous assertion: strengthen the assertion first and say so in the completion record. After all restores: full `pnpm --filter @moe/scheduler test` green again. ADVERSARIAL CHECKLIST before gating: zero edits outside the five owned paths (`git status --porcelain` audited — producers node-authority-{contract,codec,compose,fields,budget,recursion}.ts, graph-content*.ts, execution-surface.ts, package.json ALL untouched); package.json exports map untouched (root '.' remains the only subpath); no probe/debug/scratch file remains; index.ts still exactly 250 lines; node-authority-public.ts <=250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "MUTATION DRILLS — all four KILLED. None stayed green, so no assertion needed strengthening. Each applied alone, backups under %TEMP% suffixed by worker id (never MSYS /tmp).\n\nA RESTORE-PROOF PROBLEM THE STEP TEXT COULD NOT HAVE KNOWN, handled explicitly. The step says to prove each restore byte-exact with an empty `git diff -- <file>`. That works ONLY for index.ts. node-authority-public.ts and node-authority-public.js are UNTRACKED (created this task, never committed), and `git diff` is blind to untracked paths — it returns empty whether the file is pristine, mutated, or deleted, so it would have \"proved\" a restore that never happened. I recorded SHA-256 of all three files before drilling and proved each restore by hash equality instead:\n  node-authority-public.ts  A687E6D3C45526CB651DFBD06CFC549B1E01437A7AFD60DD4D9C3B787E6C1C3B — matched after drill 1 AND after drill 2\n  node-authority-public.js  BB07A22E3ADFE20A777175BC2AC7CBB08CDD5F24C4FE2312F543EC46F2652010 — matched after drill 3\n  index.ts                  EAB4735EE0664FD87D05F8CBE6B454F52E3B2DE2481D8A835B05DC01B61735EF — matched after drill 5\nFor index.ts, which IS tracked, I ran the git check too and it agrees: `git diff --numstat -- packages/scheduler/src/index.ts` -> `1  0`, i.e. exactly my one added forward line versus HEAD and nothing else.\n\nDRILL 1 — delete the published specifier `createNodeDefinition,` from node-authority-public.ts. RED, EXIT 1, `Tests 5 failed | 256 passed (261)`. Named reds: \"publishes createNodeDefinition on the package root as a function\" (the per-export it-case), \"publishes exactly the reviewed root namespace, with no loss and no addition\" (the set-equality), plus the withheld-roster case (its positive control), the type-closure case and the codec-through-root case, all of which consume the missing function. Both arms the step demanded fired.\n\nDRILL 2 — ADD the withheld name `draftNodeAuthority,` into the codec export block. RED, EXIT 1, `Tests 2 failed | 259 passed (261)`. Named reds: \"withholds the node-authority internals that would bypass the admission authority\" and the set-equality. This is the bidirectional arm and it works: the roster catches an unreviewed ADDITION, not only a removal. Note the two drills produce DIFFERENT red sets (5 vs 2) — a roster that only iterated itself would have produced the same red twice, which is the vacuity this pair rules out.\n\nDRILL 3 — delete the sibling bridge node-authority-public.js. This is the one that matters most and it behaved exactly as predicted:\n  - scheduler-runtime-entrypoint.test.ts -> RED, EXIT 1, `Tests 1 failed (1)`, \"loads and executes the scheduler entrypoint in Node's strip-types runtime\", with the error verbatim: `Error: Cannot find module 'D:\\projexts\\moe-next\\packages\\scheduler\\src\\node-authority\\node-authority-public.js' imported from D:\\projexts\\moe-next\\packages\\scheduler\\src\\index.ts` — the ERR_MODULE_NOT_FOUND class, raised under Node's REAL resolution.\n  - index-surface.test.ts in the same broken state -> GREEN, EXIT 0, `Tests 261 passed (261)`.\nSo the vitest-side suite is genuinely blind to a missing bridge (it resolves ./x.js back to x.ts), and the plain-Node entrypoint smoke is the ONLY witness. Anyone who \"verifies\" this publication with vitest alone verifies nothing about the bridge.\n\nDRILL 4 — REMOVED BY AMEND-1, and correctly so: it drilled the `satisfies` clause and the exhaustiveness assert on the two runtime layer rosters, which branch A defers to task-515d2f90. There is no such clause on this row's surface, so the drill has no subject here. Not skipped for convenience — it has moved to the row that will own the constants.\n\nDRILL 5 — remove the ONE forward line from index.ts. RED, EXIT 1, `Tests 24 failed | 237 passed (261)`: all 20 per-export cases plus the set-equality, the withheld-roster case, the type-closure case and the codec-through-root case. The set-equality names the missing keys individually in its diff, so the failure text points at the publication, not at a bare count mismatch.\n\nPOST-RESTORE, everything green again from a fresh run: `pnpm --filter @moe/scheduler test` -> EXIT 0, `Tests 1575 passed (1575)`; `pnpm --filter @moe/scheduler typecheck` -> EXIT 0.\n\nADVERSARIAL CHECKLIST, all confirmed after the drills:\n  - `git status --porcelain -- packages/scheduler` -> exactly the five owned paths (3 modified, 2 untracked-new). Nothing else.\n  - The six producing modules (contract/codec/compose/fields/budget/recursion), graph-content.ts, graph-content-format.ts, graph-content-fields.ts and execution-surface.ts are ALL untouched — checked by explicit pathspec, not by eye.\n  - package.json untouched, so the exports map still has root \".\" as the only subpath (and step 4's probe proved the deep subpath refuses with ERR_PACKAGE_PATH_NOT_EXPORTED).\n  - No probe, debug or scratch file remains; the three %TEMP% backups were deleted at the end of the last drill and their absence verified.\n  - `wc -l`: index.ts exactly 250, node-authority-pub",
      "startedAt": "2026-08-21T10:26:35.309Z",
      "completedAt": "2026-08-21T10:28:32.816Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "MUTATION DRILLS (epic rail 6) — each applied alone, backup copies under %TEMP% suffixed by worker id (never MSYS /tmp), restore proven byte-exact by empty `git diff -- <file>` from the REPO ROOT cwd: (1) delete one published specifier (createNodeDefinition) from node-authority-public.ts -> EXPECTED_EXPORTS set-equality AND the per-export it-case red; (2) ADD a withheld name (draftNodeAuthority) to node-authority-public.ts's codec block -> WITHHELD_NODE_AUTHORITY_NAMES leak-check reds (this is the bidirectional arm: the roster catches an unreviewed ADDITION, not only a removal); (3) delete node-authority-public.js (the bridge) -> scheduler-runtime-entrypoint.test.ts reds with ERR_MODULE_NOT_FOUND under plain Node while vitest-side suites stay green (vitest resolves ./x.js back to x.ts and is blind — this drill proves the smoke is the ONLY witness, run the entrypoint test to see the red); (4) REMOVED BY AMEND-1 — it drilled the compile pins on the two runtime layer rosters, which are deferred to task-515d2f90 and absent from this row's surface; the drill has no subject here and lands with that row instead; (5) remove the ONE forward line from index.ts -> set-equality red names all 20 missing keys. ANY drill that stays green = vacuous assertion: strengthen the assertion first and say so in the completion record. After all restores: full `pnpm --filter @moe/scheduler test` green again. ADVERSARIAL CHECKLIST before gating: zero edits outside the five owned paths (`git status --porcelain` audited — producers node-authority-{contract,codec,compose,fields,budget,recursion}.ts, graph-content*.ts, execution-surface.ts, package.json ALL untouched); package.json exports map untouched (root '.' remains the only subpath); no probe/debug/scratch file remains; index.ts still exactly 250 lines; node-authority-public.ts <=250 lines.",
          "reason": "Branch-A consequential amendment (same adjudication chain): sub-drill (4) drilled the satisfies clause + exhaustiveness assert on the two runtime layer rosters, which are deferred to task-515d2f90 and do not exist — the drill has no subject. Sole delta: (4) removed; drills (1),(2),(3),(5) unchanged. Worker-specified delta, governor-transcribed.",
          "amendedBy": "governor-7211ec87",
          "amendedAt": "2026-08-21T10:24:38.395Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-6",
      "description": "GATE + COMMIT + RECORD. Separate legs, separate exit codes captured (never chained with ; or |): (1) `pnpm --filter @moe/scheduler typecheck` -> EXIT 0 with the '$ tsc --project tsconfig.json' script echo (the execution evidence); (2) `pnpm --filter @moe/scheduler test` -> EXIT 0, 'Test Files N passed' / 'Tests M passed' with M > 1553 recorded verbatim; (3) `pnpm typecheck` repo-wide -> record exit + any foreign failures path-attributed; (4) `pnpm --filter @moe/daemon test` -> EXPECTED EXIT 1 at baseline (14 files / 25 tests, GRAPH_CONTENT_FIELD_INVALID): verify the (HEAD-after minus merge-base) failing-path delta INTERSECTED with this task's owned paths is EMPTY and disclose the foreign red verbatim per the project gate rail — it belongs to successor task-8c7e6ce4. COMMIT strictly by explicit pathspec of the five owned paths: git add then `git commit -- packages/scheduler/src/node-authority/node-authority-public.ts packages/scheduler/src/node-authority/node-authority-public.js packages/scheduler/src/index.ts packages/scheduler/src/index-surface.test.ts packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs` (scheduler-runtime-entrypoint.test.ts only if actually modified — expected untouched); porcelain-audit staged set BEFORE committing (foreign staged paths -> git restore --staged, leave for their owners); `git show --stat` afterward proves only owned paths. COMPLETION RECORD: (a) roster deltas — root runtime namespace 111 -> 133 (19 forwarded + snapshotIdentityHash + 2 minted layer rosters), withheld rosters: node-authority 22 (new), graph-content 8 -> 12; (b) the snapshotIdentityHash publication rationale (ValidatedGraph-only input; hard-edge graphBindingDigest chicken-and-egg; daemon builders graph-query-test-fixtures + budget-ledger-fixtures carry HARD edges and need it — panel-verified); (c) per-drill red names incl. both compile-pin directions; (d) probe outputs incl. the exact TS diagnostic codes and ERR_PACKAGE_PATH_NOT_EXPORTED; (e) CONSUMERS RECORDED (task rail 3 + project consumer-edge rail): immediate external-fixture consumer task-8c7e6ce4d94a48968e58d09e6a528dff (BLOCKED, 'Harden external GraphRevisionContent v3 fixtures' — unblocks on this landing), durable consumer task-eacea969d636420b8f5c47304039e056 (BLOCKED, 'Graph Beta: atomic active-graph transition service'); (f) note to governor: chain link 5 landed — 8c7e6ce4 (link 6) is now claimable, its landing clears the daemon suite red and unparks 95597a17/bb923a7b. verification.command: `pnpm --filter @moe/scheduler typecheck && pnpm --filter @moe/scheduler test` (read the count lines, not the exit code).",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "packages/scheduler/src/node-authority/node-authority-public.ts",
        "packages/scheduler/src/node-authority/node-authority-public.js",
        "packages/scheduler/src/index.ts",
        "packages/scheduler/src/index-surface.test.ts",
        "packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs"
      ],
      "note": "GATE + COMMIT + RECORD complete. Committed at 190c206, five owned paths by explicit pathspec, +325/-42.\n\nGATE LEGS, run separately, never chained, each exit code captured on its own, at HEAD f28cfe4:\n(1) `pnpm --filter @moe/scheduler typecheck` -> EXIT 0 with the `$ tsc --project tsconfig.json` script echo (the execution evidence, not the exit code).\n(2) `pnpm --filter @moe/scheduler test` -> EXIT 0, `Test Files 47 passed (47)` / `Tests 1575 passed (1575)`. M = 1575 > 1553, so the new assertions genuinely executed; a vacuous or zero-count run is ruled out by the count line, not by the exit code.\n(3) `pnpm typecheck` repo-wide -> EXIT 0, TS_ERROR_COUNT=0. Worth flagging as GOOD news for the chain: bb923a7b's blockedReason recorded a repo-wide TS2345 at apps/daemon/src/planning/graph-body-record.ts(179,56) on the widened NodeAuthorityRecursionLayer union. That error is GONE at this HEAD — a peer fixed it since. Nothing repo-wide is red now.\n(4) `pnpm --filter @moe/daemon test` -> EXIT 1 as expected, `Test Files 14 failed | 168 passed (182)` / `Tests 25 failed | 3862 passed (3887)`. PATH-ATTRIBUTED per the project gate rail: the failing count is IDENTICAL to the recorded pre-existing baseline (14 files / 25 tests), so my diff added nothing; every failing file is under apps/daemon/ (graph-body-record.test.ts 8, foundation-registry.test.ts 4, foundation-registry-hostile.test.ts 7, foundation-dispatch-derivation.test.ts 5, foundation-attempt-service.test.ts 1, and the rest of the 14); ZERO failures name any packages/scheduler path (grep count 0). So (HEAD-after minus merge-base) intersected with my owned paths is EMPTY. Cause disclosed verbatim, not hidden and not fixed: `GRAPH_CONTENT_FIELD_INVALID` @ `GRAPH_CONTENT_CODEC`, \"content field \\\"nodeAuthority\\\" is absent, out of bounds, or not a data property\" — a92544f's fixture fallout, owned by successor task-8c7e6ce4. Per qa-6c108bfc's 09:55:21Z correction and governor's 09:54:15Z note, this row owns no daemon file, so the daemon leg is FOREIGN here and baseline attribution is the correct treatment — I did not hold myself to a raw daemon exit 0 and did not touch apps/daemon.\n\nALSO DISCLOSED, foreign and not mine: `pnpm test:security` is EXIT 1 with 4 failed files. Three are the same nodeAuthority fixture cause. The fourth, lane-smoke.security.ts, was GREEN at my 1558a61 baseline and was broken mid-task by foreign commit f28cfe4, which added a fourth `tools/**/*.test.ts` pattern to the root vitest include while lane-smoke.security.ts:197 pins that list to exactly three. Reported in chat at 10:23:47Z. boundary-roster.security.ts — the file branch A exists to protect — is GREEN.\n\nCOMMIT DISCIPLINE: staged set audited BEFORE committing — `git diff --cached --name-only` returned exactly the five owned paths, count 5, no foreign path staged, so nothing needed `git restore --staged`. This mattered: the worktree also carries a peer's uncommitted apps/daemon/src/recovery/recovery-key-provider.node.test.ts and foreign .moe-tmp*.txt scratch, none of which entered my commit. `git show --stat 190c206` proves the five: index-surface.test.ts +193/-42ish, index.ts +1, node-authority-public.js +1 (create), node-authority-public.ts +153 (create), scheduler-entrypoint-smoke-worker.mjs +19/-. scheduler-runtime-entrypoint.test.ts was NOT modified, as the plan expected. After the commit, `git status --porcelain -- packages/scheduler` is CLEAN, so the committed bytes are the gated bytes.\n\nADVERSARIAL SELF-REVIEW (skill run before completing; findings, all disclosed rather than silently judged benign):\n- STAR-EXPORT SHADOWING, latent not present. index.ts's forward line is the LAST statement and uses `export *`. If a future name in the public module collided with an existing explicit root export, the explicit one would win silently and `Object.keys` would be unchanged, so the set-equality could stay green while a consumer got the wrong binding. Today this is impossible-by-measurement, not by construction: a live `node --experimental-strip-types` probe of the bare root returned COLLISIONS=[] against all candidate names and ROOT_KEYS=111 before the change. Worth knowing for whoever adds the deferred rosters in 515d2f90.\n- ONE TYPE IS WITNESSED BY A HAND-BUILT VALUE. In the new type-closure case, 23 of the 24 types are annotated onto values production built. `NodeAuthorityDraftResult` is the exception — its only production producer is `draftNodeAuthority`, which is deliberately WITHHELD, so there is no published call that returns one. I construct `{ draft, ok: true }` by hand. That is a genuine weak spot in an otherwise production-fed proof and I would rather name it than have QA find it.\n- NON-NULL ASSERTIONS in the new test (`section.definitions[0]!`) are forced by `noUncheckedIndexedAccess`. If the fixture ever produced zero definitions the case would throw a TypeError rather than fail an assertion. The fixture throws earlier on a refusal, and drills 1 and 5 both showed the case failing",
      "startedAt": "2026-08-21T10:28:36.629Z",
      "completedAt": "2026-08-21T10:32:04.039Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "GATE + COMMIT + RECORD. Separate legs, separate exit codes captured (never chained with ; or |): (1) `pnpm --filter @moe/scheduler typecheck` -> EXIT 0 with the '$ tsc --project tsconfig.json' script echo (the execution evidence); (2) `pnpm --filter @moe/scheduler test` -> EXIT 0, 'Test Files N passed' / 'Tests M passed' with M > 1553 recorded verbatim; (3) `pnpm typecheck` repo-wide -> record exit + any foreign failures path-attributed; (4) `pnpm --filter @moe/daemon test` -> EXPECTED EXIT 1 at baseline (14 files / 25 tests, GRAPH_CONTENT_FIELD_INVALID): verify the (HEAD-after minus merge-base) failing-path delta INTERSECTED with this task's owned paths is EMPTY and disclose the foreign red verbatim per the project gate rail — it belongs to successor task-8c7e6ce4. COMMIT strictly by explicit pathspec of the five owned paths: git add then `git commit -- packages/scheduler/src/node-authority/node-authority-public.ts packages/scheduler/src/node-authority/node-authority-public.js packages/scheduler/src/index.ts packages/scheduler/src/index-surface.test.ts packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs` (scheduler-runtime-entrypoint.test.ts only if actually modified — expected untouched); porcelain-audit staged set BEFORE committing (foreign staged paths -> git restore --staged, leave for their owners); `git show --stat` afterward proves only owned paths. COMPLETION RECORD: (a) roster deltas — root runtime namespace 111 -> 131 (19 forwarded + snapshotIdentityHash = 20 new runtime names; runtime layer rosters NOT minted, deferred by amend-1 to task-515d2f90), withheld rosters: node-authority 22 (new), graph-content 8 -> 12; (b) the snapshotIdentityHash publication rationale (ValidatedGraph-only input; hard-edge graphBindingDigest chicken-and-egg; daemon builders graph-query-test-fixtures + budget-ledger-fixtures carry HARD edges and need it — panel-verified); (c) per-drill red names (drill (4) removed by amend-1 — subject deferred with the rosters); (d) probe outputs incl. the exact TS diagnostic codes and ERR_PACKAGE_PATH_NOT_EXPORTED; (e) CONSUMERS RECORDED (task rail 3 + project consumer-edge rail): immediate external-fixture consumer task-8c7e6ce4d94a48968e58d09e6a528dff (BLOCKED, 'Harden external GraphRevisionContent v3 fixtures' — unblocks on this landing), durable consumer task-eacea969d636420b8f5c47304039e056 (BLOCKED, 'Graph Beta: atomic active-graph transition service'), deferred-roster consumer task-515d2f90938044e79c911a957e6f78b0 (PLANNING, 'Publish NodeAuthority runtime layer rosters with their security-roster trio' — hard dep: THIS row DONE); (f) note to governor: chain link 5 landed — 8c7e6ce4 (link 6) is now claimable, its landing clears the daemon suite red and unparks 95597a17/bb923a7b. verification.command: `pnpm --filter @moe/scheduler typecheck && pnpm --filter @moe/scheduler test` (read the count lines, not the exit code).",
          "reason": "Branch-A consequential amendment (same adjudication chain): completion-record clause (a) still said 111→133 with \"2 minted layer rosters\". Deltas: 111→131 (19 forwarded + snapshotIdentityHash = 20 new), no minted rosters; consumers list gains task-515d2f90 as the deferred-roster consumer. Everything else byte-identical. Worker-specified delta, governor-transcribed.",
          "amendedBy": "governor-7211ec87",
          "amendedAt": "2026-08-21T10:24:57.940Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor unblock (governor-7211ec87, 2026-08-21 00:05Z): the blockedReason's single clause \"UNBLOCK WHEN task-6ba1ff89 is DONE\" is discharged — 6ba1ff89 QA-approved DONE at 00:03:22Z (commit a92544f). Planner must honor the reason's re-probe instruction: measure the landed v3/node-authority exported names and the scheduler index-surface ratchet numbers at HEAD before planning; root-publication hazards (ratchet exact export count, module-private layer constants, graphContentHash naming ambiguity) recorded in the preserved reason. Chain: this is link 5; endpoint 8c7e6ce4 (daemon/security fixture migration) rides behind it and is the row that clears the daemon suite red at HEAD, which also unblocks bb923a7b.",
  "createdBy": "WORKER",
  "parentTaskId": "task-329f02f5b79a4600a39dacefdf502517",
  "priority": "CRITICAL",
  "order": 88.215,
  "comments": [
    {
      "id": "comment-6272bda7002a4f308f37c7b7a5f67da9",
      "author": "architect-44ece6ad",
      "content": "Immediate real bare-root consumer filed: task-8c7e6ce4d94a48968e58d09e6a528dff migrates daemon/security fixtures through production @moe/scheduler. Durable business consumer remains task-eacea969d636420b8f5c47304039e056.",
      "timestamp": "2026-08-18T18:12:08.399Z"
    },
    {
      "id": "comment-a6dde63e36504cf9b402a46efa28bbd6",
      "author": "architect-6c720ab6",
      "content": "PARKED -> v0.2 by human scope-freeze decision 2026-08-18 (epic-fbe4fc137d914a588fae286d4e5f0492 'v0.1 Ship - Windows supervised MVP'). v0.1 = Windows + Claude + linear execution + local single-node; Graph Beta / GA evidence / portability deferred. NOT deleted: re-promote via BACKLOG->PLANNING when the freeze lifts, and RE-MEASURE this preserved reason first - it goes stale. Preserved blockedReason at park time (HEAD 1aa7cdc): Hard dependency task-6ba1ff89393c4a2b91e11df06c31b873 is BLOCKED and not DONE.",
      "timestamp": "2026-08-18T19:14:54.114Z"
    },
    {
      "id": "comment-bd26eb80f90442bf85c2231f7c1ad443",
      "author": "worker-934d8b66",
      "content": "UPSTREAM CLEARED + THIS ROW IS NOW THE FLEET'S CRITICAL PATH — worker-934d8b66, measured at HEAD 907d8f8, 2026-08-21T00:39Z.\n\n1. PRECONDITION SATISFIED. task-6ba1ff89 (\"Bind NodeAuthority into GraphRevisionContent v3\") is DONE, landed at a92544f (`feat(task-6ba1ff89): bind NodeAuthority into GraphRevisionContent v3`, 8 files by explicit pathspec, +1458/-314). Verified independently by worker-40cfc767: `git cat-file -t a92544f` -> commit, `git branch --contains a92544f` -> `* moe/work-2026-08-08`. Contract live at HEAD, grepped at the SHA not the worktree: `packages/scheduler/src/graph-content-fields.ts:82` and `:95` declare `readonly nodeAuthority: NodeAuthoritySection` on both content shapes, plus the field roster entry at `:42`. Landed file set: graph-content-fields.ts, graph-content-format.ts, graph-content-issues.ts, graph-content.ts, graph-content.test.ts, index-surface.test.ts, scheduler-entrypoint-smoke-worker.mjs, scheduler-runtime-entrypoint.test.ts. Completion verification was `pnpm --filter @moe/scheduler test` -> `Test Files 47 passed (47)` / `Tests 1553 passed (1553)`, exit 0.\n\n2. WHAT THIS ROW'S PLANNER NEEDS FROM 6ba1ff89 (recorded verbatim in that row's completion record as the DoD-5 consumer handoff): the v3 export names; the new schema/hash domain literals; MAX_GRAPH_CONTENT_BYTES's new value together with its arithmetic; and the explicit note that the meter/policy enums and the v3 surface publication itself remain THIS row's scope, not 6ba1ff89's.\n\n3. WHY IT IS URGENT, NOT MERELY READY. a92544f made `nodeAuthority` a REQUIRED encode input — no legacy arm, no default, by design — and deliberately scoped daemon fixture migration OUT. `git grep -c nodeAuthority -- apps/daemon/src` returns NOTHING: zero occurrences in the whole daemon source tree. So the daemon test leg is red at HEAD. Fresh focused run from apps/daemon: `npx vitest run --root . --config package.json src/planning/graph-body-record.test.ts src/planning/active-graph-projection.test.ts` -> `Test Files 2 failed (2)` / `Tests 8 failed | 1 passed (9)`, exit 1, every failure `fixture failed to encode: [{\"code\":\"GRAPH_CONTENT_FIELD_INVALID\",\"layer\":\"GRAPH_CONTENT_CODEC\",\"message\":\"content field \\\"nodeAuthority\\\" is absent, out of bounds, or not a data property\"}]`. Full-leg number measured earlier tonight: 14 failed files / 25 failed tests.\n\n4. THE CHAIN THIS ROW GATES. task-8c7e6ce4 (\"Harden external GraphRevisionContent v3 fixtures\") is the migration row; its blockedReason names exactly ONE unblock condition — \"UNBLOCK WHEN task-210efa47 is DONE\". Landing this row therefore unblocks 8c7e6ce4, which migrates the daemon fixtures, which clears the daemon red, which is the written exit-0 DoD wall currently blocking BOTH task-95597a17 (all 7 steps done, committed at 907d8f8) and task-bb923a7b (work landed at ed11ff9). Per the standing rule that a written DoD exit-code clause outranks a rail-3 attribution waiver, neither can be released without the fixtures actually landing.\n\n5. BOARD CONTEXT: 0 WORKING, 0 REVIEW, 2 PLANNING, 27 BLOCKED. Three workers and a QA are idle or blocked behind this row.\n\nCAVEAT for whoever plans it: 8c7e6ce4's blockedReason was written at HEAD e8d1a0a on 2026-08-19 and describes the whole 329f02f5 chain as unfinished. That text is STALE in the unblock direction — 6ba1ff89 has since landed. Re-measure the chain at current HEAD rather than trusting either that reason or these numbers; HEAD moves.",
      "timestamp": "2026-08-21T00:40:18.429Z"
    },
    {
      "id": "comment-6d98817cc17042f99e14e5a6d6cc549c",
      "author": "qa-6c108bfc",
      "content": "QA (qa-6c108bfc) — CONTEXT FOR WHOEVER PLANS THIS ROW. Not a scope change; downstream impact only. Chat is an unreliable carrier here (my sends at 03:00:50Z and 03:05:17Z both returned routingTargets omitting governor-7211ec87 despite naming it), so this is the durable copy.\n\nTHIS ROW IS THE SINGLE LEVER UNDER A FLEET STALL. Board measured 2026-08-21T03:2xZ: 0 WORKING, 0 REVIEW, 2 PLANNING (this row + task-d84305ec, both unassigned, both 0 plan steps), 2 BACKLOG, ~27 BLOCKED. No worker and no QA seat can claim anything. PLANNING is architect-only.\n\nWHAT DEPENDS ON IT, in order: 210efa47 (publish the surface) -> task-8c7e6ce4 \"Harden external GraphRevisionContent v3 fixtures\" (BLOCKED; owns the daemon fixture migration) -> `pnpm --filter @moe/daemon test` goes exit 0 -> task-95597a17 and task-bb923a7b both unpark. Those two rows are COMMITTED and correctly blocked, not stalled work: each has a written DoD clause reading verbatim \"pnpm --filter @moe/daemon test exits 0\", and a written DoD exit-code clause is not waivable by a chat ruling or by rail 3. governor-7211ec87 offered such a waiver on bb923a7b at 23:44Z and formally withdrew it at 00:05:11Z. I will not approve either row until the command exits 0.\n\nTHE RED THIS ROW UNBLOCKS, verified by me at HEAD 907d8f8, structurally, without running the suite:\n- `git grep -c nodeAuthority HEAD -- apps/daemon` -> zero matching files.\n- `packages/scheduler/src/graph-content-fields.ts`: required on both content shapes (:82, :95), in the roster (:42), read (:174), hard-refused (:178 `return { ok: false, field: \"nodeAuthority\" }`).\nProducer side never sets it, consumer side refuses its absence => `GRAPH_CONTENT_FIELD_INVALID` @ `GRAPH_CONTENT_CODEC` is guaranteed, not a stale log. Reported shape: 14 failed files / 25 failed tests, 21 occurrences of the one message. Cause is committed a92544f (task-6ba1ff89), which scoped daemon fixture migration OUT by design.\n\nWHY THE OBVIOUS SHORTCUT IS UNWORKABLE — do NOT file a \"repair the daemon fixtures\" row instead of this one. Two workers independently measured that apps/daemon cannot reach the code needed: `packages/scheduler/package.json` exports is exclusive (`{ \".\": \"./src/index.ts\" }`, no subpath), and `createNodeDefinition` / `deriveNodeAuthoritySet` / `snapshotIdentityHash` are absent from `src/index.ts` and from both star-re-export hops (index.ts:109 -> execution-surface.ts:218 -> budget-ledger-surface.ts), verified with a positive control on the enumeration. A fixture-repair row could only be satisfied by hand-rolling a counterfeit authority section duplicating production derivation — the exact divergence v3 exists to kill. Obvious edit site, unreachable dependency. That is what THIS row's title fixes.\n\nPLANNING NOTE FOR THE NEXT LINK: task-8c7e6ce4's description says \"No daemon/store production change\" while its own DoD requires a typecheck gate. Check that row can pass its own gate before it is dispatched, or it inherits this stall.",
      "timestamp": "2026-08-21T03:26:14.615Z"
    },
    {
      "id": "comment-9b503c1c8c8c422f823acccecc9504b7",
      "author": "qa-6c108bfc",
      "content": "QA ROUTING NOTE for whoever plans this row (qa-6c108bfc, 2026-08-21). Not a scope change, not a rail — a measured statement of what this row is currently holding up.\n\nMEASURED at HEAD 907d8f8, foreground, run twice: `pnpm --filter @moe/daemon test` -> `Test Files 14 failed | 168 passed (182)`, `Tests 25 failed | 3853 passed (3878)`, EXIT=1. I traced every one of the 14 files and found a SINGLE root cause: `GRAPH_CONTENT_FIELD_INVALID` / `GRAPH_CONTENT_CODEC`, \"content field \\\"nodeAuthority\\\" is absent, out of bounds, or not a data property\". Two of the 14 present as an unrelated-looking Windows `EPERM` temp-cleanup failure in `src/http/foundation-registry*.test.ts`; that is downstream handle-leak fallout from the same throw (`seamHarness` opens its provider at `foundation-registry-fixtures.ts:352` and `seedFoundationStore` throws at :367 before `close` is bound), not a second defect. Details on task-bb923a7b, comment-8cbba41e.\n\nWHY THIS ROW: the helpers those fixtures need — `createNodeDefinition`, `deriveNodeAuthoritySet`, `snapshotIdentityHash` — are not reachable from the `@moe/scheduler` root. `packages/scheduler/package.json` declares an EXCLUSIVE exports map (`\"exports\": { \".\": \"./src/index.ts\" }`, no subpath), so `@moe/scheduler/node-authority/*` is unimportable from apps/daemon, and none of the three appear in `src/index.ts` or through either star re-export. Publishing them is THIS row; migrating the external fixtures is task-8c7e6ce4 (BLOCKED). The daemon reds cannot be fixed ad hoc ahead of that pair, and attempts to do so collide with the 329f02f5 chain.\n\nBOARD STATE at time of writing: 0 REVIEW, 0 WORKING, 25 BLOCKED, 2 PLANNING (this row and task-d84305ec), both unassigned with planStepCount 0. Both worker seats are parked on wait_for_task with nothing claimable — task-bb923a7b (worker-40cfc767) and task-95597a17 (worker-934d8b66) are both BLOCKED behind gates that only this chain can clear. This row is CRITICAL priority and is, as far as I can measure, the whole board's critical path right now.\n\nNo action requested of QA and none taken: I did not move any row and I am not touching the fixtures.",
      "timestamp": "2026-08-21T03:51:18.254Z"
    },
    {
      "id": "comment-d640371771fa4242b7976560e36cf681",
      "author": "worker-40cfc767",
      "content": "DECISION RECORD — BRANCH A ruled for the step-3 minted-constants defect. Recorded by worker-40cfc767 at 2026-08-21T10:10Z because the deciding agents are chat-only and architect-0975cd3a went stale before applying it. I am NOT the author of this decision; I am its scribe. Authors and receipts below.\n\nTHE DEFECT (found in step 1, MEASURE, at HEAD 1558a61; full evidence in the step-1 note). Step 3 mints `export const NODE_AUTHORITY_LAYERS = ...` and `export const NODE_AUTHORITY_RECURSION_LAYERS = ...` in the new owned production module. Both match the live source scan at tests/security/boundary-roster.security.ts:366 — /^export const ([A-Z_]+(?:LAYERS|LAYER|BOUNDARIES))\\s*(?::[^=]+)?=/u — whose SCAN_ROOTS (:357) include \"packages\" and whose isProductionModule (:397) excludes only *.test.ts, *.spec.ts, *-fixtures.ts and packages/testkit. Reds: EXPECTED_ROSTER_SIZE 102 (:332) via :472 and :510; \"names every scanned constant in the roster\" (:500); EXPECTED_DISTRIBUTION \"packages/scheduler\": 8 (:344) needing 10; scheduler-activation-boundaries.security.ts:162 axis pin toHaveLength(29) plus its :165 \"covers every scheduler-activation boundary the roster declares\" arm. The privacy being reversed is DELIBERATE and documented verbatim in the producing modules (node-authority-contract.ts:23, node-authority-recursion.ts:12: the layer constant stays MODULE-PRIVATE because the security roster counts exported *_LAYER(S)). BASELINE: `pnpm test:security` at HEAD 1558a61 -> EXIT 1, Test Files 3 failed | 7 passed (10), Tests 5 failed | 393 passed (398), all three reds one FOREIGN cause (GRAPH_CONTENT_FIELD_INVALID / GRAPH_CONTENT_CODEC, \"content field \\\"nodeAuthority\\\" is absent\", task-8c7e6ce4's scope) — but boundary-roster.security.ts is GREEN at baseline, so the mint would be a SELF-INTRODUCED red, which the project path-attributed rail explicitly does not excuse. `pnpm test:security` is in no other gate, so DoD 5's scheduler leg would have stayed green while the lane went red.\n\nRULING — governor-7211ec87, chat msg-... 2026-08-21T10:00:26.999Z, verbatim: \"governor position: BRANCH A, and your refusal to pick it yourself was correct (A narrows a DoD, B widens a rail — architect lever either way).\" Requested amendment, three parts: (1) amend DoD 1 to scope the stable roster to version/code/limit + the TYPE re-exports NodeAuthorityLayer/NodeAuthorityRecursionLayer, explicitly deferring the RUNTIME layer-roster publication; (2) amend steps 2/3 to publish 20 runtime names (EXPECTED_EXPORTS 111 -> 131) dropping NODE_AUTHORITY_LAYERS/NODE_AUTHORITY_RECURSION_LAYERS; (3) FILE a deferred row owning tests/security/boundary-roster.security.ts + the scheduler-activation slice, landing roster entries (EXPECTED_ROSTER_SIZE 102->104, scheduler distribution 8->10, axis 29->31) + hostile arms together, sequenced free of 8c7e6ce4. Governor also directed: \"HOLD at 1/6 until the amendment overlay exists — do not pre-build step 2 names against unamended text.\"\n\nCONCURRENCE — qa-6c108bfc, 2026-08-21T10:00:51.835Z, after re-measuring every number independently rather than taking my report on trust: \"the defect is CONFIRMED, every number ... step 3 as written reverses a written security decision as a side effect, and it reddens boundary-roster.security.ts, which is GREEN at baseline. That is a self-introduced red ... path-attributed subtraction does not cover it, and I would reject on it. Holding at 1/6 instead of pinning names in step 2 was the right call.\" QA also RULED OUT the naming dodge (renaming the constants to evade the LAYER/LAYERS suffix).\n\nCURRENT BLOCKER FOR THE OVERLAY: `moe.amend_plan_step` is an architect/governor lever. architect-0975cd3a is `isAlive: false`, lastActivityAt 2026-08-21T09:51:22Z — it exited immediately after submitting this plan and never saw the defect. governor-7211ec87 last active 10:02:06Z, also stale. I will not amend my own plan or narrow my own DoD: a worker rewriting the requirement it is graded against is exactly the pattern the board's rails forbid, and a chat ruling is not a DoD amendment (qa-6c108bfc, this morning).\n\nSTATE: step 1 COMPLETED, steps 2-6 PENDING, nothing edited in the worktree, no probe or scratch file left behind (the one root-namespace probe was deleted in the same command; `git status` clean of my paths). Whoever holds the architect lever next: apply the three-part amendment above and this row runs to completion immediately — every other plan number was re-confirmed at HEAD 1558a61, and a live `node --experimental-strip-types` probe of the bare @moe/scheduler root returned 111 keys with ZERO collisions against all 22 candidate names and no withheld name already leaking.",
      "timestamp": "2026-08-21T10:10:27.215Z"
    },
    {
      "id": "comment-0a3dc278f9434ecfa6f96df24da7bdca",
      "author": "governor-7211ec87",
      "content": "GOVERNOR — BRANCH-A AMENDMENT APPLIED + DoD-1 GRADING INTERPRETATION (governor-7211ec87, 2026-08-21 10:13Z). Amendments amend-1 on step-2 and amend-1 on step-3 are LIVE (read step.amendments[activeAmendmentId], not the base description). Deltas, three-party adjudicated (worker finding comment-d6403717; QA independent re-measure 10:00:51Z; governor ruling msg-4c0d207d; transcribed by governor because no architect was alive — the content is the parties' verified consensus, not governor-authored plan design): 20 new runtime names not 22, EXPECTED_EXPORTS 111→131, arrays(9), the runtime layer-roster mint removed; deferred row FILED as task-515d2f90 (owns node-authority-public.ts layer-roster exports + tests/security/boundary-roster.security.ts + scheduler-activation slice, the full trio; hard dependency: THIS row DONE).\n\nDoD-1 GRADING INTERPRETATION, recorded for whoever grades this row — the DoD text itself is unamended and unamendable by chat: DoD 1's \"stable version/code/layer/limit roster\" is satisfied under branch A by the published runtime constants for version (NODE_AUTHORITY_SCHEMA_VERSION), codes (NODE_AUTHORITY_CODES, NODE_AUTHORITY_RECURSION_CODES) and limits (NODE_AUTHORITY_LIMITS) PLUS the layer vocabulary published at TYPE level (NodeAuthorityLayer, NodeAuthorityRecursionLayer — closed unions, compile-checkable from the root). The RUNTIME layer arrays are deliberately absent from this row's surface; their publication carries the security-roster trio obligation and is owned by task-515d2f90. Grading this row's DoD 1 as unmet for lacking the runtime arrays would demand exactly the change the security lane forbids this row to make. qa-6c108bfc: please ack this interpretation ON THIS ROW so a cold future QA session inherits it from the record, not from channel memory.",
      "timestamp": "2026-08-21T10:13:49.199Z"
    },
    {
      "id": "comment-cafea9c1821546c0962687408ecd353f",
      "author": "qa-6c108bfc",
      "content": "QA GRADING RULING ON DoD 1 UNDER BRANCH A — durable ack of comment-0a3dc278, recorded on the row so a cold grader is bound by it.\n\nDoD 1 reads verbatim: \"A curated node-authority-public module exposes the complete reviewed create/admit/encode/decode/recursive-result type and stable version/code/layer/limit roster while withholding raw canonical text, digest preimages, graph-index helpers and unvalidated constructors.\"\n\nRULING: under branch A, DoD 1 IS MET when the \"layer\" element of the roster is published as the TYPES `NodeAuthorityLayer` and `NodeAuthorityRecursionLayer` in the 24-type block, with the version/code/limit elements published as runtime constants. I will NOT reject this row for the absence of runtime `NODE_AUTHORITY_LAYERS` / `NODE_AUTHORITY_RECURSION_LAYERS` arrays.\n\nReasoning, on the record:\n1. DoD 1 says \"roster\", not \"runtime array\". A closed union type is a stable, compile-checkable layer vocabulary reachable from the package root — it satisfies the stated obligation. The clause never names a runtime binding.\n2. Publishing the runtime arrays is affirmatively FORBIDDEN to this row, not merely optional. `tests/security/boundary-roster.security.ts` scans production modules under [\"apps\",\"packages\",\"adapters\"] with /^export const ([A-Z_]+(?:LAYERS|LAYER|BOUNDARIES))\\s*(?::[^=]+)?=/u; a new packages/scheduler production module exporting those two names reds EXPECTED_ROSTER_SIZE (102), EXPECTED_DISTRIBUTION[\"packages/scheduler\"] (8), the scan-minus-roster set-equality arm, and scheduler-activation ROSTER_AXIS toHaveLength(29). That file is GREEN at baseline, so the red would be introduced by this row's own diff — which the path-attribution rail explicitly refuses to excuse.\n3. The privacy being reversed is documented and deliberate: node-authority-contract.ts and node-authority-recursion.ts each state the layer constant stays module-private BECAUSE the security roster counts exported *_LAYER(S). A forwarding row must not reverse a written security decision as a side effect.\n4. I verified all of (2) and (3) on disk myself at 10:00Z; I am not relying on the worker's or governor's measurement.\n\nBOUNDARY OF THIS RULING — it is not a general waiver:\n- It covers ONLY the two node-authority runtime layer rosters, deferred to task-515d2f90. Every other element of DoD 1 is graded as written: the version/code/limit runtime constants must be present and reachable from the package root, and the withholding half (raw canonical text, digest preimages, graph-index helpers, unvalidated constructors) is graded strictly with no allowance.\n- Renaming the constants to evade the roster scan's LAYER/LAYERS/BOUNDARIES suffix, or placing them in a path the isProductionModule predicate skips, is scan evasion and is an automatic reject regardless of this ruling.\n- If task-515d2f90 is not filed and reachable when I grade, the deferral has no home and I reconsider. Governor reports it filed (PLANNING, HIGH, hard dep 210efa47 DONE); I will confirm at grading time.\n\nALSO FLAGGING FOR THE PLAN, because it affects grading and is not yet fixed: the branch-A overlay is HALF-APPLIED. On disk right now step-2 and step-3 carry activeAmendmentId \"amend-1\"; steps 4, 5 and 6 carry none. Step 5's sub-drill (4) still reads \"drop PLANNING_SOURCE from NODE_AUTHORITY_LAYERS -> typecheck reds on the exhaustiveness assert ... add a bogus member -> reds on the satisfies clause\" — with the rosters deferred that drill has no subject and cannot be run as written. Step 6's completion record clause (a) still reads \"111 -> 133 (19 forwarded + snapshotIdentityHash + 2 minted layer rosters)\", contradicting amend-1's 131. The worker is correct to flag both. I will grade step 5 as satisfied by the other four drills and step 6 against 111 -> 131 / 20 forwarded names, but the overlay should be written so a cold grader does not have to rely on this comment.",
      "timestamp": "2026-08-21T10:14:40.088Z"
    },
    {
      "id": "comment-7a07ac297fb54679b9cd7b8c82e89968",
      "author": "qa-6c108bfc",
      "content": "QA FOREIGN-RED ATTRIBUTION, recorded so a cold grader does not charge these to this row. I measured all of it myself at HEAD f28cfe4; this is not a transcription of the worker's report.\n\nTWO foreign causes are live in `pnpm test:security`, neither introduced by task-210efa47:\n\n1. GRAPH_CONTENT_FIELD_INVALID / GRAPH_CONTENT_CODEC, content field \"nodeAuthority\" absent — integrity-boundaries.security.ts (5 tests), completeness.security.ts and scheduler-activation-boundaries.security.ts (suite-level). Pre-existing a92544f fixture fallout, owned by successor task-8c7e6ce4. Present at this row's baseline.\n\n2. NEW since this row started, and NOT this row's diff: commit f28cfe4 \"fix(tools): bind both importers to the store's per-commit cap\" added a fourth pattern `tools/**/*.test.ts` to the root vitest.config.ts include list. tests/security/lane-smoke.security.ts pins that list to exactly three entries with `toStrictEqual([\"adapters/**/*.test.ts\",\"packages/**/*.test.ts\",\"tests/**/*.test.ts\"])`, so the case \"still discovers *.test.ts only, so hostile files are not regression evidence\" now reds.\n\nVerified independently:\n- `vitest.config.ts:6` now reads include: [\"adapters/**/*.test.ts\", \"packages/**/*.test.ts\", \"tests/**/*.test.ts\", \"tools/**/*.test.ts\"].\n- `git log -S'tools/**/*.test.ts' -- vitest.config.ts` names f28cfe4 and nothing else.\n- `git show --stat f28cfe4` = tools/import/import-commit.ts, tools/import/import-shadow.test.ts, tools/import/import-shadow.ts, vitest.config.ts. No packages/scheduler path, no tests/security path.\n- This row owns five packages/scheduler paths; vitest.config.ts and tests/security/** are foreign to it.\n\nGRADING CONSEQUENCE: cause 2 does not block this row and must not be repaired by it — repairing it would mean editing an unowned config plus an unowned security file, i.e. the same overreach branch A was chosen to avoid. The fix belongs to whoever owns the f28cfe4 tools row: either add the fourth pattern to the lane-smoke pin or revert the include change. The pin should be updated deliberately, not loosened, since its whole purpose is that the root lane discovers *.test.ts only.\n\nboundary-roster.security.ts is GREEN at HEAD f28cfe4 — branch A held, and the mint would have been the only thing to red it.\n\nThis row's owned gate remains `pnpm --filter @moe/scheduler test` RAW exit 0 with nonzero, above-baseline counts. Worker reports EXIT 0, Test Files 47 passed (47), Tests 1575 passed (1575) vs baseline 1553; I will re-run it cold myself at REVIEW rather than accept that figure.",
      "timestamp": "2026-08-21T10:24:29.306Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-18T18:10:44.389Z",
  "updatedAt": "2026-08-21T10:37:06.009Z",
  "blockedFromStatus": null,
  "blockedAt": null,
  "blockedReason": null,
  "blockedResourceId": null,
  "metrics": {
    "firstClaimAt": "2026-08-19T22:52:52.557Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 5,
    "executedStepCount": 6,
    "doneAt": "2026-08-21T10:37:06.009Z",
    "wallClockMs": 128653452
  },
  "priorHandoffs": [
    {
      "whatIsDone": "Re-measured at e8d1a0a: predecessor 6ba1ff89 BLOCKED (links 1-4 unfinished). Re-blocked, link 5 of 6, with root-publication hazards recorded (index-surface ratchet, security roster, graphContentHash naming ambiguity).",
      "whatRemains": "Wait for 6ba1ff89 DONE; probe landed exports + ratchet numbers; plan the root publication.",
      "pitfalls": "Layer constants module-private; ratchet bumped exactly; do not deepen the structural-vs-content naming ambiguity.",
      "openQuestions": "None.",
      "releasedAt": "2026-08-19T22:53:18.086Z",
      "releasedBy": "architect-bdaffeaf",
      "reason": "Architect releasing the assignment only; BLOCKED state and reason stand."
    }
  ],
  "contextFetchedBy": [
    "architect-0975cd3a",
    "worker-934d8b66",
    "worker-40cfc767",
    "qa-6c108bfc"
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-21T09:51:22.078Z",
  "planningNotes": {
    "approachesConsidered": "(a) Deep subpath export for node-authority — forbidden by task rail 2 (root '.' is the only public subpath) and would bypass the curation review. (b) Inline the ~46 new export specifiers in index.ts — impossible: index.ts is at 249/250 lines; the execution-surface forwarding precedent exists exactly for this. (c) Re-export runtime LAYER_NAMES from the modules — impossible without editing unowned files (module-private by design); resolved by minting compile-pinned rosters in the owned public module: satisfies-clause refuses a bogus member, exhaustiveness-assert refuses a dropped one, so the hand-carried roster cannot silently detach (TS 7.0.2). (d) Withholding snapshotIdentityHash (initial draft) — REVERSED by measurement: daemon fixture builders graph-query-test-fixtures.ts (:78-81, TWO HARD edges) and budget-ledger-fixtures.ts (:62-63, ONE HARD edge) cannot author dependency contracts without graphBindingDigest === snapshotIdentityHash(graph) (closeContracts refuses NODE_AUTHORITY_RECURSION_BINDING_MISMATCH, recursion.ts:164), and the only other structural-identity route (GraphContent.snapshotIdentity) exists only AFTER a successful encode, which for hard-edge graphs requires the digest first — chicken-and-egg. Publishing is safe: the function accepts ONLY the brand-protected ValidatedGraph (graph-model.ts:47,:96), so no unvalidated structure can be hashed; encodeGraphContent stays the only route to graphContentHash, so index.ts's withholding comment remains true. (e) Flipping fixture imports in the RED step — rejected: an unresolved root import compile-fails the whole test file and hides the assertion-level red; flip happens in GREEN.",
    "codebaseInsights": "Measured at HEAD 9adceb7 (commands + outputs recorded in an adversarial 3-agent verification panel this session; every claim CONFIRMED except one roster gap, folded in): six node-authority modules export exactly 41 runtime bindings; publish 19 + withhold 22 covers all bidirectionally; four exported TYPES (DerivedIdentity, AdmittedPlanning, ComposedEdges, NodeAuthorityBudget) plus Read<T> are deliberately withheld and pinned by negative NodeNext probes since a runtime leak-check cannot see types. TWO distinct NodeAuthorityEntry types exist (recursion.ts:49 and graph-content-fields.ts:54, structurally identical); the public module exports recursion's (the deriveNodeAuthoritySet result type) and NodeAuthoritySection from graph-content.js — no root collision (index.ts publishes neither today; live root namespace enumerated at 111 keys with zero NODE_*/roster-name overlap incl. through the execution-surface and budget-ledger-surface stars). ExportKind lacks \"string\" — first string exports on the root; else-arm typeof check already generic. Sibling .js bridges: all six exist, single-line LF; new public.js bridge joins them; post-flip the root chain (index.ts -> public.js -> codec.js/recursion.js) transitively traverses all six, preserving the smoke's ERR_MODULE_NOT_FOUND bridge coverage. Layer roster members verbatim: contract (11) NODE_AUTHORITY_ADMISSION/BUDGET/CODEC/DEPENDENCIES/IDENTITY/LIMITS/PROOFS/SCHEMA/SCOPES + DEPENDENCY_CONTRACT + PLANNING_SOURCE; recursion adds NODE_AUTHORITY_RECURSION + GRAPH_SNAPSHOT (union = 13). Baseline gates fresh: scheduler typecheck+test EXIT 0, 47 files/1553 tests.",
    "risks": "(1) index.ts lands at EXACTLY 250 lines — zero slack; any extra comment line breaks DoD 2; the forward line carries no comment (curation rationale lives in node-authority-public.ts, per the execution-surface precedent). (2) The daemon suite is RED at HEAD by design (14 files/25 tests, GRAPH_CONTENT_FIELD_INVALID — successor task-8c7e6ce4's scope): disclose path-attributed, never fix, never let it block completion per the project gate rail. (3) Shared worktree: http/foundation fixtures and .moe files churn constantly — porcelain-audit before commit, explicit pathspec only. (4) Drill 3 (bridge deletion) reds ONLY under the plain-Node entrypoint test — running just vitest-side suites would call the drill vacuously green; run scheduler-runtime-entrypoint.test.ts specifically. (5) The minted layer rosters are the one hand-carried surface — the two-direction compile pin is load-bearing; QA should drill both directions (step 5 sub-drills 4a/4b). (6) Windows: plain-node probes from PowerShell only; drill backups in %TEMP% not /tmp; drill-restore diff from repo root cwd.",
    "keyFiles": [
      "packages/scheduler/src/index.ts",
      "packages/scheduler/src/index-surface.test.ts",
      "packages/scheduler/src/node-authority/node-authority-contract.ts",
      "packages/scheduler/src/node-authority/node-authority-recursion.ts",
      "packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs",
      "packages/scheduler/src/graph-content-format.ts",
      "packages/scheduler/src/execution-surface.ts"
    ]
  },
  "budget": {
    "wallClockMs": 5400000,
    "warnedAt": "2026-08-21T09:54:12.507Z",
    "escalatedAt": "2026-08-21T09:54:12.507Z"
  },
  "planApprovedAt": "2026-08-21T09:51:24.183Z",
  "workStartedAt": "2026-08-21T09:54:52.431Z",
  "reviewStartedAt": "2026-08-21T10:33:30.753Z",
  "verification": {
    "command": "pnpm --filter @moe/scheduler typecheck && pnpm --filter @moe/scheduler test",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\n\n RUN  v4.1.10 D:/projexts/moe-next\n\n Test Files  47 passed (47)\n      Tests  1575 passed (1575)\n   Start at  13:32:16\n   Duration  1.55s (transform 12.04s, setup 0ms, import 16.35s, tests 2.57s, environment 3ms)\n\nCOMMAND_EXIT=0",
    "reportedAt": "2026-08-21T10:33:30.753Z"
  },
  "filesModified": [
    "packages/scheduler/src/index-surface.test.ts",
    "packages/scheduler/src/node-authority/node-authority-public.ts",
    "packages/scheduler/src/node-authority/node-authority-public.js",
    "packages/scheduler/src/index.ts",
    "packages/scheduler/src/scheduler-entrypoint-smoke-worker.mjs"
  ],
  "completedAt": "2026-08-21T10:37:06.009Z",
  "reviewCompletedAt": "2026-08-21T10:37:06.009Z",
  "reviewSummary": "APPROVED. All gates re-run by me at HEAD 190c206, not taken from the worker's report.\n\nGATES (separate runs, raw exit codes): scheduler typecheck EXIT 0 with the `$ tsc --project tsconfig.json` echo; scheduler test EXIT 0, Test Files 47 (47) / Tests 1575 (1575) vs 1553 baseline, so new assertions executed and counts nonzero; repo-wide pnpm typecheck EXIT 0.\n\nFOREIGN, attributed not charged: daemon test EXIT 1 at 14 files / 25 tests — IDENTICAL to baseline, single cause GRAPH_CONTENT_FIELD_INVALID/GRAPH_CONTENT_CODEC \"nodeAuthority absent\" (8c7e6ce4's), and grepping the failure output for packages/scheduler returns 0. Delta-minus-baseline intersected with owned paths is EMPTY. test:security EXIT 1, 4 files / 6 tests: 3 the same fixture cause, 1 lane-smoke broken by foreign f28cfe4 (git log -S'tools/**/*.test.ts' -- vitest.config.ts names f28cfe4 alone). boundary-roster.security.ts GREEN — branch A held. Recorded on the row as comment-7a07ac29.\n\nDoD: (1) node-authority-public.ts publishes 20 runtime names + 24 types and withholds canonicalText/nodeBodyDigest/canonicalEnvelopeJson/draftNodeAuthority — read in the committed blob; layer vocabulary published as TYPES per my ruling comment-cafea9c18, runtime rosters deferred to filed row 515d2f90. (2) index.ts EXACTLY 250 lines (cap) with one `export *` line; sibling bridge one LF-terminated line (cat -A shows no CR); public.ts 153. (3) NodeNext positive EXIT 0; 10 negatives each EXIT 1 naming its own symbol (TS2724/TS2305); probes deleted, tree clean. (4) Smoke imports the BARE root: authorityHashLength 64, authorityNodeKeys [\"runtime-done\"], contentRoundTrip true, refusals pinned exact code:layer, internalSubpath ERR_PACKAGE_PATH_NOT_EXPORTED. (5) Consumers 8c7e6ce4 / eacea969 / 515d2f90 recorded in the commit message and step-6 record.\n\nRAILS: five owned paths only, +325/-42 (283 net, under the bar); package.json exports untouched. Index-surface leak checks carry positive controls, so they are not vacuous."
}