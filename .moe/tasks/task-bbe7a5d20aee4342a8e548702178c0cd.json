{
  "id": "task-bbe7a5d20aee4342a8e548702178c0cd",
  "epicId": "epic-af14d73585e14c0da0264d48c37587c3",
  "title": "Offer repository.publish_resolve for an UNKNOWN publish, and give the Publish card its resolve control",
  "description": "CHILD of task-21be6e9055984413a651ce6ee331e2c8 (the operator exit for an UNKNOWN publish), filed by architect-73d103f8 on 2026-09-18. Delivers the operator-facing half of that row's DoD 2. task-21be6e90 is the integration parent and declares dependsOn on this row.\n\nWHY A SEPARATE ROW. The Publish card acts through AFFORDANCE OFFERS, not a bespoke port: `release.decide` needed its own offer-mint row (task-21209552) for exactly this reason, because the ladder's offer union in apps/daemon/src/http/affordance-planning-offers.ts is a CLOSED type — adding a kind is a type edit plus a mint site, and a mint without the widening will not compile. Follow that precedent exactly: a LAZY FACT on the offer input supplied by the composition root, never a store walk from inside the ladder, resolved only for a goal that could actually hold an UNKNOWN publish (a per-poll walk over every goal is the 09-05 affordances hot-path incident repeating).\n\nTHE TARGET KEY IS A SAFETY PROPERTY. `readDurableLedger` keeps the last committed result per targetAggregateId, so a decision targeted at the bare goal id OVERWRITES the goal's durable record and drops it from `durableGoals` — the hazard affordance-planning-offers.ts already documents for preview and release. Target the resolve at the publish decision's own aggregate, never the goal id, and prove the goal survives a committed resolve.\n\nTHE CARD. apps/control-room/src/v2/goals/goal-publish.tsx gates Publish on `publish?.outcome !== \\\"UNKNOWN\\\"` (:91). For an UNKNOWN publish it shows the observation (delivered by the sibling observation row) and a resolve control offering NOT_TRANSMITTED and ABANDON, submitted through the offer exactly as the other cards submit theirs. The operator, not the daemon, asserts that the remote does not hold the sha, so the control must sit beside the observation it is deciding on. It is an OPERATOR affordance: `repository.publish_resolve` is human-only (row 3) and must never become staffable agent work.\n\nORDER. Depends on the service child (so the control never ships pointing at row 3's refusing stub) and on the observation child (so the card has something to show beside the control, and because both edit goal-publish.tsx).",
  "definitionOfDone": [
    "The affordance read offers `repository.publish_resolve` for a goal whose latest publish is UNKNOWN, asserted BY VALUE on commandKind and targetAggregateId against the PRODUCTION surface read; the target is NOT the bare goal id, and an arm commits a resolve and asserts the goal is STILL present in the durable goal set with its state unchanged.",
    "The offered-kind SET is asserted in both directions (set-equality, never `not.toContain`): offered for an UNKNOWN publish, absent for PENDING, PUSHED and REFUSED ones; and the kind is never staffable work.",
    "The fact the ladder reads is a lazy fact supplied by the composition root, resolved only for a goal with a publish, following the release precedent.",
    "The Publish card renders the resolve control only for an UNKNOWN publish, beside the observation, and submits NOT_TRANSMITTED or ABANDON through the offer; control-room arms assert the rendered control and the submitted payload by value, and that a refusal renders its code.",
    "Gates quoted with `Test Files N passed` lines: `pnpm --filter @moe/daemon test` for src/http/affordance*, and `pnpm --filter @moe/control-room test` (apps/control-room is in no other gate); `pnpm typecheck`."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "CONFIRM THE PREREQUISITES AND RE-MEASURE, then stop. (a) Confirm task-a47babf8 and task-52682237 are DONE: the resolve service is registered in place of row 3's stub, and `RunGoalPublish` carries `observation` and `decisionId`. (b) Re-confirm the offer union at affordance-planning-offers.ts:128-131, the lazy facts at :45-88, and the publish mint at :241. (c) Confirm `resolvePlanningOffers` fills compiler steps only from COMPILER kinds, so an operator kind offered through `staffAll` never becomes agent work. (d) Confirm goal-publish.tsx:37-40 still finds its offer by commandKind plus a hand-spelled target, since the resolve matcher follows it.",
      "status": "PENDING",
      "affectedFiles": []
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — the offer's shape and set, in apps/daemon/src/http/affordance-planning-offers.test.ts, following the release card's arms. Assert BY VALUE, as `commandKind@target` sets with SET-EQUALITY in both directions and never `not.toContain` (which passes against a differently-named offer): (1) a goal whose latest publish is UNKNOWN is offered `repository.publish_resolve@publish-resolve:<decisionId>` among its offers; (2) the same goal with a PENDING, a PUSHED and a REFUSED publish is offered NO resolve — one arm each; (3) the resolve's target is NOT the bare goal id and NOT `publish:<goalId>`; (4) the kind never becomes a compiler step (staffable agent work). Assert the lazy fact is called ONLY for goals that could hold a publish, by counting its calls across a surface of several goals. Confirm RED.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/http/affordance-planning-offers.test.ts"
      ]
    },
    {
      "stepId": "step-3",
      "description": "GREEN — mint it. In apps/daemon/src/http/affordance-planning-offers.ts: widen the closed union at :128-131 with `\"repository.publish_resolve\"`; add a lazy fact to `PlanningOfferInput` (e.g. `unknownPublishDecision: (goalId: string) => string | null`) documented like its siblings; and mint `offer(input, \"repository.publish_resolve\", `publish-resolve:${decisionId}`)` at the publish rung, reading the fact only where a publish is possible. Write at the site why the target is neither the goal id nor `publish:<goalId>` — `readDurableLedger` keeps the last result per target, so either would overwrite a durable record — and name the control-room matcher that spells the same target. In apps/daemon/src/http/affordance-read.ts supply the fact from the composition root: the goal's UNKNOWN publish decisionId via the publish ledger, or null. Confirm step 2's arms are GREEN.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/http/affordance-planning-offers.ts",
        "apps/daemon/src/http/affordance-read.ts"
      ]
    },
    {
      "stepId": "step-4",
      "description": "THE SURVIVAL ARM — prove the target is a safety property, not tidiness. In apps/daemon/src/http/affordance-read-planning.test.ts, following the release card's survival arm there (the suite header at :1350 states the hazard): commit a real `repository.publish_resolve` through the command edge, then assert BY VALUE that (a) the GOAL is still present in the durable goal set with its state unchanged, and (b) the PUBLISH decision's durable record is unchanged — the publish ledger still reads its request. Read both through `stateOf(readDurableLedger(...))`, before and after. MUTATION-VERIFY: back up affordance-planning-offers.ts to %TEMP%/moe-task-bbe7a5d2-affordance-planning-offers-<workerId>.ts recording its SHA256 (task id in the path, because seat ids are reused and a seat-only backup can hold the wrong vintage), change the minted target to `publish:<goalId>`, and confirm the survival arm goes RED — quote the line. Restore by copying the backup back, never `git checkout --` on this shared tree, and prove the restore by SHA256 equality in a call SEPARATE from the restore.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/http/affordance-read-planning.test.ts"
      ]
    },
    {
      "stepId": "step-5",
      "description": "The card's control. In apps/control-room/src/v2/goals/goal-publish.tsx add a resolve-offer matcher beside `publishOffer` (:37-40): the row whose commandKind is `repository.publish_resolve` and whose target is `publish-resolve:<decisionId>`, using the decisionId from `RunGoalPublish`, with a comment naming the daemon mint site that spells the same target. For an UNKNOWN publish with a resolve offer, render the two choices — NOT_TRANSMITTED ('the remote does not have this commit; allow a new publish') and ABANDON ('give up on this publish') — beside the observation task-52682237 shows, and submit `{ decisionId, resolution }` through the offer exactly as the other cards submit theirs. Render the command's refusal with its code and detail. In apps/control-room/src/v2/goals/goal-publish.test.tsx add arms by value: the control renders only for an UNKNOWN publish that has a resolve offer; each choice submits the exact payload; a refusal renders its code; and no control renders for PENDING, PUSHED or REFUSED.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-publish.tsx",
        "apps/control-room/src/v2/goals/goal-publish.test.tsx"
      ]
    },
    {
      "stepId": "step-6",
      "description": "GATES AND ADVERSARIAL REVIEW. Read each leg's OUTPUT, not its exit code. (A) `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/http/affordance-planning-offers.test.ts src/http/affordance-read-planning.test.ts src/http/affordance-read.test.ts`, count quoted. (B) `pnpm --filter @moe/control-room test`, count quoted — apps/control-room is in no other gate. (C) The full `pnpm --filter @moe/daemon test` and `pnpm typecheck`, both quoted; attribute any red by the path-attributed baseline rule, re-running a failing file ALONE first and capturing stderr. (D) ADVERSARIAL REVIEW, in the completion record: Can the resolve ever be offered to an agent, over MCP or as staffable work? Can it be offered for a publish that is not UNKNOWN? Does the lazy fact run for goals that cannot publish? Can the daemon's target and the card's matcher drift apart, and would any test notice? Does a committed resolve disturb the goal or the publish decision? (E) Record as a follow-up comment on this row, not a new row: command-side target validation, a refusal for a dispatch whose target is not `publish-resolve:<decisionId>`, which needs a new code in row 2's closed map.",
      "status": "PENDING",
      "affectedFiles": []
    }
  ],
  "planRevision": 1,
  "status": "WORKING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 1022.4,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-18T19:14:42.516Z",
  "updatedAt": "2026-09-18T19:42:22.075Z",
  "dependsOn": [
    "task-a47babf8291943efb08258a0c1f6cc78",
    "task-52682237c158403b83fcf54b3b0bb2bd"
  ],
  "metrics": {
    "firstClaimAt": "2026-09-18T19:40:12.093Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 6
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-18T19:42:19.802Z",
  "planningNotes": {
    "codebaseInsights": "Filed by me today as a CHILD of task-21be6e90, delivering the operator-facing half of its DoD 2. dependsOn [task-a47babf8 (the service, so the control never ships pointing at row 3's refusing stub), task-52682237 (the observation, which this control sits beside, and which also edits goal-publish.tsx)]. Follows task-21209552's release.decide offer precedent exactly. Measured at HEAD 2026-09-18.\n\n(1) THE CLOSED OFFER UNION. apps/daemon/src/http/affordance-planning-offers.ts:128-131 types the mintable kinds as `CompilerOfferKind | \"approval.decide\" | \"approval.decide_intent\" | \"goal.cancel\" | \"goal.close\" | \"plan.propose\" | \"preview.decide\" | \"release.decide\" | \"repository.publish\"`. Adding the kind is a type edit PLUS a mint site: a mint without the widening does not compile, and a widening without a mint offers nothing.\n\n(2) THE LAZY-FACT PATTERN. `PlanningOfferInput` (:45) carries per-goal facts supplied by the composition root: `closeReadiness` (:52), `landedCommit` (:75), `previewReceipt` (:88). The resolve needs one more — for a goal, the decisionId of its UNKNOWN publish, or null — supplied by apps/daemon/src/http/affordance-read.ts and resolved ONLY for a goal that could hold a publish. A per-poll walk over every goal would repeat the 09-05 affordances hot-path incident.\n\n(3) THE TARGET, AND THE SECOND HAZARD IT AVOIDS. `readDurableLedger` keeps the last committed result per targetAggregateId. Targeting the bare goal id overwrites the goal's durable record — the documented hazard. Targeting the PUBLISH decision's own aggregate (`publish:<goalId>`, minted at :241) would be the same hazard again: the resolve would overwrite the publish decision's durable result that the publish ledger reads. So the resolve targets a DISTINCT aggregate, `publish-resolve:<decisionId>`, keyed on the decision being resolved.\n\n(4) HOW THE CARD FINDS ITS OFFER. apps/control-room/src/v2/goals/goal-publish.tsx:37-40 `publishOffer(frame, goalId)` matches `row[\"commandKind\"] === \"repository.publish\" && row[\"targetAggregateId\"] === `publish:${goalId}``. The control-room cannot import daemon code, so spelling the target on both sides of the package boundary is the ESTABLISHED pattern here. The resolve matcher uses the decisionId the card already has from `RunGoalPublish.decisionId`. Each spelling carries a comment naming the other, so a future change edits both.\n\n(5) OFFERED, NEVER STAFFED. The ladder's `staffAll` makes offers staffable, but `resolvePlanningOffers` fills compiler steps only from COMPILER kinds. An operator kind that is in neither COMPILER_OFFER_KINDS nor COMPILER_SCHEMA_KINDS is therefore offered to the operator but never becomes agent work — exactly how release.decide stays an operator affordance. `repository.publish_resolve` is also human-only and MCP-excluded (row 3)."
  },
  "planApprovedAt": "2026-09-18T19:42:22.075Z"
}