{
  "id": "task-53680e91db1c4789a42b7129e61bdd7a",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Export the committed supervisor effect surface from @moe/runner root",
  "description": "Objective: publish child 1's committed supervisor effect surface on the @moe/runner package root so apps/daemon can compose it. Blocker-fix task 2 of 3 for task-ba3a45f9 (Daemon work services, SPIDR child 3). Created by architect-8a4a1764 2026-08-08 under human approval in the REPL. Siblings: task-8ee125d0 (scheduler surface, independent, also ready now) and the .js bridge sweep task (gated on child 2).\n\nWHY THIS EXISTS. packages/runner/src/index.ts exports only the artifacts, scope, and workspace surfaces. The supervisor/ subtree is deliberately NOT exported - child 1's decision, QA-confirmed in mem:task-task-2580a578812f46a49cae0af79ff6fc16-qa-verdict (\"Nothing added to packages/runner/src/index.ts - supervisor stays internal, claude modules are precedent\"). packages/runner/package.json pins \"exports\": {\".\": \"./src/index.ts\"}, an EXCLUSIVE map: no deep subpath import reaches around it, at runtime or under moduleResolution NodeNext. Probed on the identically-shaped sibling package from apps/daemon: import('@moe/scheduler/authority/lease-resource.js') -> ERR_PACKAGE_PATH_NOT_EXPORTED, while import('@moe/scheduler') -> OK. Internal-by-default was correct while nothing consumed the subtree; task-ba3a45f9 is the first consumer, so the seam gets published deliberately.\n\nTHIS TASK IS READY NOW - it does NOT wait on SPIDR child 2. Two verified reasons:\n  1. task-ba3a45f9's DoD needs only CHILD 1's records - effect intent creation, the lifecycle command surface, and the one-use activation gate. Child 2's launch-lock / drain / restart modules appear nowhere in child 3's Definition of Done. Child 1 is COMMITTED at 72545bb and stable.\n  2. Child 2's approved plan declares 9 files, ALL under packages/runner/src/supervisor/ (launch-lock.ts, drain-reconciliation.ts, drain-table.ts, restart-reconstruction.ts + tests, plus edits to effect-kernel.ts and effect-lifecycle.ts). packages/runner/src/index.ts is NOT among them. Zero file collision.\nChild 2 does append new codes to the frozen vocabularies inside effect-kernel.ts. That is safe for this task: appending members to an already-exported array does not change the set of export NAMES, so the surface test specified below stays green.\n\nDeliverable: a curated named-export block in packages/runner/src/index.ts, in the style the file already uses (named exports grouped per module, types in the same block via the `type` modifier, every specifier written with the .js extension per NodeNext). Export from the seven committed child-1 modules - effect-kernel.js, effect-shape.js, effect-parse.js, effect-lifecycle.js, effect-grant.js, effect-activation.js, lease-mirror.js - the records, pure functions, and error-code vocabularies a caller needs to: create an effect intent, apply a lifecycle command and discriminate all three LifecycleOutcome kinds (TRANSITIONED | MUST_DRAIN | REFUSED), consume and validate a one-use activation grant, and mirror-fence a lease. Read the modules and mem:task-task-2580a578812f46a49cae0af79ff6fc16-handoff for the exact surface; do not guess names. Prefer curated named exports over a blanket `export *` so the published seam stays a reviewed decision. Do NOT export effect-test-fixtures.ts - test-only.\n\nNOT in scope: any .js bridge (that is the separate sweep task - do not add one here, it would be a partial sweep whose incompleteness is invisible to the test gate); changing ANY behaviour in any supervisor module; exporting child 2's in-flight modules; touching packages/scheduler; touching apps/daemon; any package.json or lockfile edit.\n\nHard dependencies: none. Child 1 (task-2580a578) is COMMITTED at 72545bb.\n\nOwned paths: packages/runner/src/index.ts, packages/runner/src/index-surface.test.ts (new).\nVerification: pnpm --filter @moe/runner typecheck &amp;&amp; pnpm --filter @moe/runner test &amp;&amp; pnpm --filter @moe/daemon typecheck",
  "definitionOfDone": [
    "packages/runner/src/index.ts exports the child-1 supervisor surface such that a consumer can, without any deep subpath import, create an effect intent, apply a lifecycle command and discriminate all three LifecycleOutcome kinds by name, consume and validate a one-use activation grant, and call the lease mirror - naming every parameter type and every union branch of every exported function's return type.",
    "A new packages/runner/src/index-surface.test.ts imports the package ROOT specifier @moe/runner (never a relative or deep path) and asserts per symbol that the export is present and of the expected typeof, so a dropped export fails by name rather than by a vague count.",
    "The same test asserts set equality between Object.keys of the namespace and a hand-written expected export-name list, so both a removed export AND an unreviewed addition go red. The list is hand-transcribed from the modules, never derived from the namespace under test (mem:gotcha-self-derived-universe-cannot-check-itself).",
    "The three LifecycleOutcome kinds remain structurally discriminable through the published surface: a test asserts a REFUSED outcome has exactly the keys [kind, failure] and that MUST_DRAIN carries no ok field - the properties child 1 relied on so that ok === false cannot mistake a drain instruction for a refusal. Asserted against the exported production surface, not a local re-declaration.",
    "Zero behaviour change: git diff shows packages/runner/src/index.ts as the only modified non-test file, no .js file is added, effect-test-fixtures.ts is not exported, and the pre-existing runner suite passes with a test count greater than or equal to the pre-task count, stated as an explicit before/after number.",
    "pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck exits 0; committed paths are exactly packages/runner/src/index.ts and packages/runner/src/index-surface.test.ts."
  ],
  "taskRails": [
    "Re-export only. This task changes what is REACHABLE, never what any function DOES. No .ts file other than index.ts may be edited. If a symbol seems to need a signature change to be exportable, report_blocked rather than editing a supervisor module - child 2 is working in that directory right now.",
    "Do NOT add any .js bridge. @moe/runner is unloadable by plain Node today and the fix is a package-wide sweep owned by a separate task gated on child 2; a partial sweep here would be silently incomplete and invisible to the test gate (mem:gotcha-runner-package-does-not-load-under-plain-node). This task's gate is vitest, which does not need bridges.",
    "Read packages/runner/src/supervisor/*.ts and mem:task-task-2580a578812f46a49cae0af79ff6fc16-handoff before naming a single symbol. Do not guess export names - verify each one exists (skill: explore-before-assume).",
    "Never export a test-only module. effect-test-fixtures.ts stays internal; exporting it puts test code on the runtime surface.",
    "Assert against the production surface, not a reimplementation: the LifecycleOutcome shape assertions must run on values obtained through the @moe/runner root import, never on a locally re-declared type or fixture copy (epic rail 6).",
    "Keep the production file focused: target <=250 lines, split before 400. If the added exports push packages/runner/src/index.ts past 250 lines, extract a supervisor re-export module that index.ts re-exports rather than letting the entry point sprawl.",
    "Commit by explicit pathspec only - git commit -- packages/runner/src/index.ts packages/runner/src/index-surface.test.ts. Never a bare git commit; child 2 has uncommitted work in this package and the index is shared state (epic rail 3). Verify with git show --stat afterwards."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Re-verify the surface on disk before naming a symbol — child 2 is committing into this directory, so this note may already be stale. Read all seven child-1 modules (effect-kernel, effect-shape, effect-parse, effect-lifecycle, effect-grant, effect-activation, lease-mirror) and record every exported value and type. TWO THINGS TO CHECK SPECIFICALLY. First, `effect-activation.ts:36` contains its own `export { ... }` re-export block: read it, because a name re-exported from both effect-activation.js and its original module would be a duplicate export and a compile error that reads like a mystery. Second, confirm that child 2's edits only APPENDED members to the frozen vocabularies rather than renaming or removing any export — appended members are safe for this task (the set of export NAMES is unchanged), a rename or removal is not, and per rail 1 the response to that is report_blocked, never editing a supervisor module. Then make the one judgement call this task contains: decide whether effect-shape's generic validators (isStrictRecord, exactRecord, isRef, isDigest, isCount, oneOf, readList, readOwnDataProperty, hasOnlyOwnStringKeys, isPlainArray) belong on a public seam. DoD 1 names only intent creation, lifecycle commands, grant consumption and lease fencing; publishing internal helpers widens the package API permanently for no stated consumer. Record the decision and its reason in the step note. Produce no code.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "Re-verified all seven child-1 modules on disk (no code produced).\n\nCHILD 2 CHECK (rail 1). Child 2's work is already COMMITTED — `git status --porcelain` shows nothing under packages/runner/ at all (only .moe/* churn). `git diff 72545bb -- <the seven modules> | grep '^[-+].*export'` returns exactly ONE line pair, and it is a DOC COMMENT inside the ADMITTED_EFFECT_TRANSITIONS jsdoc (\"Child 2 adds the design-776...\" -> \"no reducer below invents an edge...\"). Zero export NAMES renamed, removed, or added. Appends confirmed as members-only: SUPERVISOR_ERROR_CODES now 43 codes (LAUNCH_LOCK_*, DRAIN_*, RESTART_* appended), SUPERVISOR_LAYERS now 8 (LAUNCH_LOCK, DRAIN, RESTART appended), ADMITTED_EFFECT_TRANSITIONS gained the ACTIVE->requestCancel->CANCEL_REQUESTED row. Set-equality test stays green. No report_blocked needed.\n\nDUPLICATE-EXPORT HAZARD (effect-activation.ts:36) CONFIRMED REAL. That block is:\n  export { activationDigestInput, consumeActivationGrant, validateActivationCommit,\n           type CommitCheck, type GrantOutcome } from \"./effect-grant.js\";\nResolution: those five names are exported from ./supervisor/effect-grant.js ONLY. effect-activation.js contributes exactly `activateEffect` + `type ActivationCommit` + `type ActivationOutcome`. No name is published twice.\n\nJUDGEMENT CALL — effect-shape generic validators: EXCLUDED. isStrictRecord, exactRecord, isRef, isDigest, isCount, oneOf, readList, readOwnDataProperty, hasOnlyOwnStringKeys, isPlainArray and `type DataPropertyRead` stay internal. Reason: DoD 1 names only intent creation, lifecycle commands, grant consumption and lease fencing; none of those four capabilities requires a generic hostile-input guard. They are implementation helpers of the parsers, they carry no supervisor domain meaning, and publishing them widens @moe/runner's API permanently for zero stated consumer. DataPropertyRead goes with them (it is only readOwnDataProperty's return type). effect-shape's DOMAIN half IS published: MAX_SUPERVISOR_COUNT, MAX_SUPERVISOR_TEXT_CHARS (bounds behind EFFECT_COUNTER_EXHAUSTED / isRef; precedent: index.ts already exports MAX_SCOPE_PATHS, MAX_SCOPE_OBSERVATION_BYTES, MAX_WORKSPACE_ENTRIES), MIRRORED_LEASE_KINDS/STATES (MIRRORED_LEASE_STATES is literally the `legalStates` argument type of fenceMirroredLease), MIRRORED_LEASE_KEYS/MIRRORED_PROOF_KEYS (named future consumer: child 4's daemon-side mirror-vs-fenceAuthority field-for-field drift test), parseMirroredLease, parseMirroredProof (direct mirror of scheduler's already-published parseLeaseRecord/parseProof).\n\nPRECEDENT / MECHANISM. Sibling packages/scheduler/src/index-surface.test.ts (commit 239aa4f) self-references `@moe/scheduler` and both typecheck and vitest pass, so package self-reference works here; packages/control-room-model/src/package-root.test.ts is a second precedent. Root vitest.config.ts includes packages/**/*.test.ts; @moe/runner package.json pins exports {\".\": \"./src/index.ts\"} (exclusive).\n\nBASELINE for DoD 5: `pnpm --filter @moe/runner test` -> 21 files / 721 tests, all passing, exit 0. (Child-1 handoff's \"388\" is stale; child 2 landed the rest.)",
      "startedAt": "2026-08-08T21:20:57.609Z",
      "completedAt": "2026-08-08T21:23:44.672Z"
    },
    {
      "stepId": "step-2",
      "description": "Hand-transcribe the expected export-name list — a separate step, deliberately, because DoD 3 lives or dies on this. Write the complete list of names index.ts will publish, taken BY HAND from the module sources you just read, and record it in the step note. Do NOT generate it from `Object.keys()` of anything, do not paste it from a namespace dump, and do not derive it from the export block you are about to write: a list derived from the thing under test asserts only that the namespace equals itself and can never fail (mem:gotcha-self-derived-universe-cannot-check-itself). This list is simultaneously the specification for step 4's export block and the expected value for step 3's set-equality assertion, and it must be written before either. Cross-check it against the decision recorded in step 1 about the effect-shape validators so the list and the export block cannot disagree.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "Hand-transcribed export-name list, read off the module sources (Read tool output, line by line), NOT off any Object.keys() dump and NOT off an export block (which does not exist yet — step 4 has not run). This list is the SPECIFICATION for step 4 and the EXPECTED VALUE for step 3.\n\nRUNTIME VALUES — 26 pre-existing + 42 new supervisor = 68 total root keys.\n\nPRE-EXISTING (26), transcribed from packages/runner/src/index.ts and confirmed against each declaration site:\nartifacts/artifact-contract.js -> ARTIFACT_ADDRESS_PATTERN (regexp, `= /^[0-9a-f]{64}$/u`), RUNNER_ARTIFACT_ERROR_CODES (array), refMatches (function), refRejection (function)\nartifacts/artifact-node-fs.js -> createNodeArtifactFs (function)\nartifacts/artifact-store.js -> createArtifactStore (function)\nscope/scope-contract.js -> MAX_SCOPE_PATHS (number), RUNNER_SCOPE_ERROR_CODES (array), SCOPE_ATTRIBUTION_CLASSES (array), SCOPE_OBSERVATION_VERSION (string), ScopeObserverError (function — `export class`), canonicalPathRejection (function), scopeObservationDigestInput (function)\nscope/scope-git.js -> MAX_SCOPE_OBSERVATION_BYTES (number), createNodeGitObserver (function), createNodeScopePaths (function), hermeticGitEnvironment (function)\nscope/scope-observation.js -> observeScope (function)\nworkspace/workspace-contract.js -> MAX_WORKSPACE_ENTRIES (number), RUNNER_WORKSPACE_ERROR_CODES (array), WORKSPACE_INPUT_MANIFEST_VERSION (string), WORKSPACE_RESULT_MANIFEST_VERSION (string), inputManifestDigestInput (function), resultManifestDigestInput (function)\nworkspace/workspace-manifest.js -> buildInputManifest (function), buildResultManifest (function)\n\nNEW — supervisor/effect-kernel.js (14): SUPERVISOR_EFFECT_PROTOCOL_VERSION (string), SUPERVISOR_ACTIVATION_VERSION (string), SUPERVISOR_RESULT_VERSION (string), EFFECT_CALLER_CONTRACT (array), EFFECT_STATES (array), TERMINAL_EFFECT_STATES (array), EFFECT_COMMANDS (array), ATTEMPT_SLICE_STATES (array), GRANT_STATES (array), SUPERVISOR_LAYERS (array), SUPERVISOR_ERROR_CODES (array), supervisorFailure (function), withLeg (function), isTerminalEffectState (function)\nNEW — supervisor/effect-shape.js (8): MAX_SUPERVISOR_COUNT (number), MAX_SUPERVISOR_TEXT_CHARS (number), MIRRORED_LEASE_KINDS (array), MIRRORED_LEASE_STATES (array), MIRRORED_LEASE_KEYS (array), MIRRORED_PROOF_KEYS (array), parseMirroredLease (function), parseMirroredProof (function)\nNEW — supervisor/effect-parse.js (9): parseEffectIntent, parseAttemptSlice, parseEffectClaim, parseEffectTombstone, parseActivationGrant, parseSettlementEvidence, parseUncertaintyEvidence, parseDependencyWitness, parseCommandInput (all function)\nNEW — supervisor/effect-lifecycle.js (3): ADMITTED_EFFECT_TRANSITIONS (array), applyEffectCommand (function), applyEffectTombstone (function)\nNEW — supervisor/effect-grant.js (6): grantRefusal, initialGrantBinding, activationDigestInput, deriveGrantId, consumeActivationGrant, validateActivationCommit (all function)\nNEW — supervisor/effect-activation.js (1): activateEffect (function)\nNEW — supervisor/lease-mirror.js (1): fenceMirroredLease (function)\n\nEXCLUDED ON PURPOSE (step-1 decision, cross-checked): isStrictRecord, readOwnDataProperty, hasOnlyOwnStringKeys, exactRecord, isPlainArray, readList, isRef, isDigest, isCount, oneOf, type DataPropertyRead. Also excluded: everything in effect-test-fixtures.ts (rail 4) and every child-2 module (drain-*, launch-lock, duplicate-delivery, process-observation, restart-*, race-*) — not in scope.\nNOT DOUBLE-EXPORTED: activationDigestInput / consumeActivationGrant / validateActivationCommit / CommitCheck / GrantOutcome come from effect-grant.js only, never from effect-activation.js's own re-export block at line 36.\n\nTYPE EXPORTS (32; zero runtime keys, so they do NOT enter the set-equality assertion):\neffect-kernel.js (18): EffectState, TerminalEffectState, EffectCommand, AttemptSliceState, GrantState, SupervisorLayer, SupervisorErrorCode, SupervisorFailureDetail, SupervisorFailure, EffectIntent, AttemptSlice, EffectClaim, EffectTombstone, ActivationGrant, SettlementEvidence, UncertaintyEvidence, DependencyWitness, EffectResult\neffect-shape.js (4): MirroredLeaseKind, MirroredLeaseState, MirroredLeaseRecord, MirroredLeaseProof\neffect-parse.js (3): SettleCommand, SimpleCommand, EffectCommandInput\neffect-lifecycle.js (2): AdmittedTransition, LifecycleOutcome\neffect-grant.js (2): GrantOutcome, CommitCheck\neffect-activation.js (2): ActivationCommit, ActivationOutcome\nlease-mirror.js (1): MirrorVerdict\n\nRepo has no prettier/eslint gate (root package.json scripts are typecheck/test only), so formatting is style-matched to index.ts by hand.",
      "startedAt": "2026-08-08T21:23:47.831Z",
      "completedAt": "2026-08-08T21:24:14.222Z"
    },
    {
      "stepId": "step-3",
      "description": "TDD red — create packages/runner/src/index-surface.test.ts. FIRST, prove the mechanism before building on it: write one trivial assertion that `import * as runner from \"@moe/runner\"` resolves at all. That self-referencing ROOT specifier is required by DoD 2 and is the single point of failure for this whole task; if it does not resolve under this vitest config, STOP and report it rather than falling back to a relative or deep import — a relative import would make the suite green while testing nothing about the published seam, which is the entire deliverable. Once it resolves, assert per symbol from the step-2 list that the export is present and of the expected `typeof`, so a dropped export fails BY NAME rather than by a vague count. Then DoD 3's two-way guard: assert set equality between `Object.keys` of the namespace and the hand-written list, so a removed export AND an unreviewed addition both go red. Red, because index.ts does not export the supervisor surface yet.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "newFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "TDD RED. Created packages/runner/src/index-surface.test.ts.\n\nMECHANISM PROVED FIRST, as the step demanded. The file's first test is `expect(typeof runner.observeScope).toBe(\"function\")` over `import * as runner from \"@moe/runner\"` — the self-referencing ROOT specifier. It PASSED on the red run, so package self-reference resolves under this vitest config (root vitest.config.ts, include packages/**/*.test.ts). No fallback to a relative or deep import was needed or taken; there is not one relative/deep specifier anywhere in the file. Precedent held: packages/scheduler/src/index-surface.test.ts and packages/control-room-model/src/package-root.test.ts do the same.\n\nRED RUN (`pnpm vitest run --root . packages/runner/src/index-surface.test.ts`):\n  Test Files 1 failed (1) | Tests 43 failed | 29 passed (72)\n  Representative failure: \"publishes applyEffectCommand on the package root as a function\" -> AssertionError: expected 'undefined' to be 'function'.\n43 red = the 42 supervisor names that index.ts does not export yet + the set-equality test. 29 green = the resolve probe + the cardinality guard + the fixture guard + the 26 pre-existing runner exports. That split is exactly the expected reason: the feature is missing, not the test.\n\nWHAT THE FILE ASSERTS\n1. Root specifier resolves (mechanism probe).\n2. `EXPECTED_EXPORTS.length === 68` — a cardinality guard so an emptied or truncated table cannot pass silently.\n3. Set equality: `Object.keys(runner).sort()` vs the hand list, so an unreviewed ADDITION goes red too, not just a loss.\n4. `it.each` per symbol, so the test TITLE names the missing export (\"publishes withLeg ... as a function\") instead of reporting a count. Kind vocabulary is \"array\" | \"function\" | \"number\" | \"regexp\" | \"string\"; ARTIFACT_ADDRESS_PATTERN is checked with `instanceof RegExp` rather than being lumped into typeof \"object\", so a swap to a plain object would still go red.\n5. Rail 4 guard: FIXTURE_NAMES hand-transcribed from supervisor/effect-test-fixtures.ts (AT, LATER, DIGEST, makeLease, makeProof, makeIntent, makeAttempt, makeClaim, makeTombstone, makeGrant, makeSettlement, makeUncertainty, makeWitness, makeActivationRequest, withGetter, withExtraKey) — asserted absent from the surface by real name, not by a placeholder.\n\nThe EXPECTED_EXPORTS table is the step-2 hand list verbatim; it was written before index.ts was touched (index.ts is still unmodified at this point), so it cannot have been derived from the export block under test.",
      "startedAt": "2026-08-08T21:24:17.359Z",
      "completedAt": "2026-08-08T21:26:31.164Z"
    },
    {
      "stepId": "step-4",
      "description": "Add the curated supervisor export block to packages/runner/src/index.ts, matching the file's existing style exactly: one grouped `export { ... } from \"./supervisor/<module>.js\";` block per module, value exports first and `type` entries in the same braces, every specifier carrying the `.js` extension per NodeNext. Export from the seven committed child-1 modules exactly the names on the step-2 list — the records, pure functions and error-code vocabularies a caller needs to create an effect intent, apply a lifecycle command and discriminate all three LifecycleOutcome kinds, consume and validate a one-use activation grant, and mirror-fence a lease. Prefer curated named exports over a blanket `export *`, so the published seam stays a reviewed decision rather than whatever the modules happen to export next week. Do NOT export effect-test-fixtures.ts — test-only code must never reach the runtime surface. Do NOT add any .js bridge file (rail 2): @moe/runner genuinely is unloadable under plain Node today, but that is a package-wide defect owned by task-eb9ff081, and a partial sweep here would be silently incomplete and invisible to this task's vitest gate. index.ts is 70 lines today and should land near 150; if it bursts 250, extract a supervisor re-export module per rail 6 rather than letting the entry point sprawl.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/index.ts",
        "packages/runner/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/index.ts"
      ],
      "note": "GREEN. Added the curated supervisor export block to packages/runner/src/index.ts.\n\nRESULT: `pnpm vitest run --root . packages/runner/src/index-surface.test.ts` -> Test Files 1 passed (1) | Tests 72 passed (72). `pnpm --filter @moe/runner typecheck` -> exit 0.\n\nSHAPE OF THE CHANGE. Seven grouped blocks, one per committed child-1 module, inserted between the scope group and the workspace group so the file keeps its directory ordering (artifacts, scope, supervisor, workspace). Each block matches the file's existing style exactly: values first in ASCII order, then `type` entries in the same braces, every specifier written with the `.js` extension per NodeNext. No `export *` anywhere — every one of the 42 runtime names and 32 type names is written out, so the seam is a reviewed decision and the step-3 set-equality assertion has something real to disagree with.\n\nBlocks: effect-activation.js (activateEffect + 2 types), effect-grant.js (6 values + CommitCheck/GrantOutcome), effect-kernel.js (14 values + 18 types), effect-lifecycle.js (3 values + AdmittedTransition/LifecycleOutcome), effect-parse.js (9 values + 3 types), effect-shape.js (8 values + 4 types), lease-mirror.js (fenceMirroredLease + MirrorVerdict).\n\nDUPLICATE-EXPORT HAZARD AVOIDED. activationDigestInput, consumeActivationGrant, validateActivationCommit, CommitCheck and GrantOutcome are published from ./supervisor/effect-grant.js ONLY. effect-activation.js re-exports those same five at its line 36, and taking them from both would have been a duplicate-export compile error; the effect-activation block contributes exactly activateEffect, ActivationCommit, ActivationOutcome. Typecheck exit 0 confirms.\n\nRAIL COMPLIANCE. No .js bridge added anywhere (rail 2 — the package-wide sweep stays with task-eb9ff081). effect-test-fixtures.ts is not referenced. No supervisor .ts file was edited; index.ts is the only production file touched. A single 6-line doc comment records the curation decision (why the effect-shape generic guards stay internal) so the next reader does not re-litigate it.\n\nSIZE (rail 6): `wc -l packages/runner/src/index.ts` -> 163 (was 70). Under the 250 target, so the rail-6 supervisor re-export extraction was NOT needed and was not performed.",
      "startedAt": "2026-08-08T21:26:37.510Z",
      "completedAt": "2026-08-08T21:27:33.517Z"
    },
    {
      "stepId": "step-5",
      "description": "Prove the LifecycleOutcome union survives publication — DoD 4, the one behavioural property this task must not silently break. Through values obtained from the `@moe/runner` ROOT import (never a local re-declaration and never a fixture copy — epic rail 6 requires the assertion to run on the production surface), assert that all three kinds remain structurally discriminable by name: TRANSITIONED, MUST_DRAIN and REFUSED. Assert specifically that a REFUSED outcome has exactly the keys [kind, failure], and that MUST_DRAIN carries NO `ok` field. Those are the exact properties child 1 relied on so that a consumer testing `ok === false` cannot mistake a drain instruction for a refusal — MUST_DRAIN is an instruction to drain, not a rejected command, and collapsing the two would be a real correctness regression introduced purely by how the seam was published. Obtain the outcomes by calling the exported applyEffectCommand with inputs that produce each kind, rather than by constructing outcome literals, so the test exercises the real union.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "DoD 4 proved against the production surface. `pnpm vitest run --root . packages/runner/src/index-surface.test.ts` -> Test Files 1 passed (1) | Tests 80 passed (80). `pnpm --filter @moe/runner typecheck` -> exit 0.\n\nALL THREE KINDS OBTAINED BY CALLING THE EXPORTED REDUCER, never by constructing an outcome literal and never from a fixture copy (epic rail 6):\n- TRANSITIONED: applyEffectCommand(intent PENDING, {kind:\"claim\"}). Asserted Object.keys sorted === [\"intent\",\"kind\",\"ok\",\"result\",\"versionDelta\"], ok true, state CLAIMED, version 7->8, versionDelta 1, result null.\n- MUST_DRAIN: applyEffectCommand(intent ACTIVE, {kind:\"requestCancel\"}). Asserted Object.keys sorted === [\"drainRequired\",\"intent\",\"kind\",\"versionDelta\"], `\"ok\" in outcome` === FALSE, drainRequired true, versionDelta 0, intent.version UNCHANGED at 7. This is the property child 1 relied on: a consumer matching `ok === false` cannot mistake a drain instruction for a refusal, because MUST_DRAIN has no ok field at all.\n- REFUSED: applyEffectCommand(intent PENDING, {kind:\"arm\"}). Asserted Object.keys sorted === EXACTLY [\"failure\",\"kind\"] (so a refusal structurally cannot carry a successor), plus reason code EFFECT_TRANSITION_NOT_ADMITTED, layer LIFECYCLE, failure.ok false, detail.state \"PENDING\".\n\nREASON CODES PINNED, NOT MERE OUTCOMES (epic rail 6): EFFECT_TRANSITION_NOT_ADMITTED, EFFECT_UNCERTAINTY_EVIDENCE_REQUIRED, EFFECT_TOMBSTONE_DOES_NOT_DOMINATE, GRANT_ALREADY_CONSUMED, GRANT_WRAPPER_MISMATCH, ACTIVATION_COMMIT_INCOHERENT, ACTIVATION_INTENT_NOT_ARMED (+ layer ACTIVATION + detail.leg \"intentState\", so WHICH layer refused is pinned), LEASE_MIRROR_STALE_EPOCH, LEASE_MIRROR_MALFORMED, EFFECT_COUNTER_EXHAUSTED. Helper `codeOf` also asserts every code is a member of the published SUPERVISOR_ERROR_CODES and `refusalOf` asserts every layer is a member of SUPERVISOR_LAYERS, so a refusal outside the closed vocabulary goes red. Redaction spot-check: failure.detail carries no leaseToken.\n\nFULL CAPABILITY WALK, all through the ROOT namespace: activateEffect -> ACTIVATED, grantId === deriveGrantId(intentId, activationDigest), consumeActivationGrant -> CONSUMED (UNUSED->CONSUMED, version 0->1) then GRANT_ALREADY_CONSUMED on replay, validateActivationCommit -> COHERENT with activationDigest identical to the commit's, fenceMirroredLease -> FENCED plus two distinct refusal codes. Settle path adopts an EffectResult (terminalState SUCCEEDED, outcomeClass COMPLETED, resultVersion moe-effect-result/1); applyEffectTombstone dominates an ARMED intent to CANCELLED but refuses an ACTIVE one.\n\nTYPE COVERAGE (DoD 1): all 32 published types are imported by name from \"@moe/runner\" and USED — `noUnusedLocals` is on, so an unused type import fails typecheck, which makes this coverage machine-checked rather than claimed. Every union arm is named: LifecycleOutcome x3, ActivationOutcome x2, GrantOutcome x2, CommitCheck x2, MirrorVerdict x2, EffectCommandInput (SimpleCommand | SettleCommand).\n\nFIXTURES HAND-WRITTEN, not derived from an export under test (mem:gotcha-fixture-derived-from-export-under-test-hides-every-assertion): the intent's protocolVersion is the literal \"moe-effect-intent/1\", and a separate test pins runner.SUPERVISOR_EFFECT_PROTOCOL_VERSION to that same literal, so a changed constant fails instead of silently redefining the fixture. Vocabulary values (TERMINAL_EFFECT_STATES, MIRRORED_LEASE_KINDS, GRANT_STATES, ATTEMPT_SLICE_STATES, MAX_SUPERVISOR_COUNT/TEXT_CHARS, key-list lengths) are pinned by hand-written value.\n\nSIZE: index-surface.test.ts is 304 lines (wc -l). Under the 400 hard split; above the 250 target, which the project custom rule scopes to production sources (index.ts is 163). Not split further because this task's owned paths are exactly two files and a third would exceed them; sibling precedent packages/scheduler/src/index-surface.test.ts is 259.",
      "startedAt": "2026-08-08T21:31:26.035Z",
      "completedAt": "2026-08-08T21:31:45.203Z"
    },
    {
      "stepId": "step-6",
      "description": "Prove zero behaviour change — DoD 5, stated as numbers rather than as a claim. Record the runner suite's test count BEFORE this task's changes (from the pre-task run) and AFTER, and state both explicitly in the completion evidence; the after count must be greater than or equal to the before count, since this task only adds a suite. Then confirm with `git status --porcelain` and `git diff --stat` that packages/runner/src/index.ts is the ONLY modified non-test file, that NO .js file was added anywhere, and that effect-test-fixtures.ts appears in no export. Confirm no .ts file under supervisor/ was touched — rail 1 forbids it and child 2 is working in that directory right now, so any diff there is either a mistake or a collision. Also confirm with wc -l (not PowerShell Measure-Object -Line, which skips blank lines and only ever reports room) that index.ts stayed under the 250 target, or that the rail-6 extraction was performed if it did not.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "DoD 5 stated as numbers, not as a claim.\n\nTEST COUNT, BEFORE AND AFTER (`pnpm --filter @moe/runner test`, both runs fresh on this machine):\n  BEFORE (step 1, pre-change): Test Files 21 passed (21) | Tests 721 passed (721)\n  AFTER  (now):                Test Files 22 passed (22) | Tests 801 passed (801)\n  801 - 721 = 80 = EXACTLY the 80 cases in the one added file. 22 - 21 = 1 file. Nothing pre-existing was lost, renamed, or silently skipped; after >= before as required.\n\nDIFF SCOPE (`git status --porcelain` + `git diff --stat HEAD -- packages apps`):\n  ` M packages/runner/src/index.ts`\n  `?? packages/runner/src/index-surface.test.ts`\n  `packages/runner/src/index.ts | 93 ++++...  1 file changed, 93 insertions(+)`\n  93 insertions, ZERO deletions — the change is purely additive; no existing export line was altered. index.ts is the ONLY modified non-test file anywhere under packages/ or apps/. (The remaining porcelain entries are .moe/ daemon bookkeeping — messages and task JSON — written by the runtime, not by me, and not owned or committed here.)\n\nRAIL CHECKS, each run rather than asserted:\n  - `git diff --stat HEAD -- packages/runner/src/supervisor/` -> ZERO lines of output. No supervisor .ts file was touched (rail 1). No collision with child 2, whose work is already committed.\n  - `git status --porcelain | grep -E '\\.js$' | wc -l` -> 0. No .js bridge added anywhere (rail 2); the package-wide sweep stays with its owner.\n  - `grep -c 'effect-test-fixtures' packages/runner/src/index.ts` -> 0. The test-only fixture module appears in no export (rail 4), and the suite additionally asserts its 16 hand-transcribed names are absent from the namespace.\n\nSIZE (`wc -l`, not PowerShell Measure-Object -Line, which skips blank lines and would only ever report room):\n  packages/runner/src/index.ts        163  (was 70) — under the 250 target, so the rail-6 supervisor re-export extraction was correctly NOT performed.\n  packages/runner/src/index-surface.test.ts  304 — a test file, under the 400 hard split; the 250 figure is a per-production-source target.",
      "startedAt": "2026-08-08T21:31:48.086Z",
      "completedAt": "2026-08-08T21:32:16.152Z"
    },
    {
      "stepId": "step-7",
      "description": "Run the full gate for this task, which deliberately includes the daemon typecheck: pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck. Run it fresh from the repo root and capture the real output as evidence. The daemon typecheck is in the gate for a reason — this task exists so apps/daemon can compose the supervisor surface, and a surface that publishes but does not typecheck from its intended consumer has not done its job. Attribute red by path and never by a remembered count: `packages/runner/src/supervisor/` is shared with child 2 right now, so red there is likely theirs — check before claiming or fixing it, and never edit a foreign test or module to make your suite green. The scope-observation real-git test has a known 5s-timeout flake on this machine: re-run once, report the result, do not chase it. Anything red in index.ts or index-surface.test.ts is unambiguously yours.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "FULL GATE, run fresh from the repo root D:/projexts/moe-next, all three legs chained with &&:\n\n`pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck`\n\n  $ tsc --project tsconfig.json                       (runner typecheck — silent, exit 0)\n  $ vitest run --root ../.. packages/runner/src\n     RUN  v4.1.10 D:/projexts/moe-next\n     Test Files  22 passed (22)\n          Tests  801 passed (801)\n       Duration  635ms\n  $ tsc --project tsconfig.json                       (daemon typecheck — silent, exit 0)\n  GATE_EXIT=0\n\nNothing red anywhere, so no attribution was needed: no failure in packages/runner/src/supervisor/ (child 2's territory, already committed) and none in index.ts or index-surface.test.ts. The known scope-observation real-git 5s-timeout flake did NOT occur on this run or on the step-6 run — two consecutive clean full-suite runs, so no re-run was needed.\n\nHONEST CAVEAT ON THE DAEMON LEG, reported rather than papered over. `apps/daemon/package.json` currently declares @moe/contracts, @moe/core, @moe/scheduler, @moe/store — NOT @moe/runner — and `grep -rn '@moe/runner' apps/daemon/src` returns nothing. So the daemon typecheck leg passes and proves NO REGRESSION was introduced into the daemon, but it cannot yet prove the runner surface is composable FROM the daemon, because the daemon never resolves that specifier today. Adding the dependency is explicitly out of this task's scope (\"NOT in scope: ... touching apps/daemon; any package.json or lockfile edit\") and belongs to task-ba3a45f9, whose approved ownership amendment covers the daemon package.json. I did not touch it.\n\nWhat DOES prove consumability, and is the strongest available proof from inside this task's owned paths: `grep -rln '\"@moe/runner\"' --include=package.json` finds only packages/runner itself, and no .ts outside packages/runner imports the specifier — so index-surface.test.ts is currently the only consumer, and it deliberately uses the bare package-root specifier `@moe/runner` through the exclusive `exports: {\".\": \"./src/index.ts\"}` map. That is the identical resolution path an external consumer takes; it is not a relative or deep import anywhere in the file. 80 cases pass against it, including a typecheck-enforced walk over all 32 published types.",
      "startedAt": "2026-08-08T21:32:18.439Z",
      "completedAt": "2026-08-08T21:32:58.997Z"
    },
    {
      "stepId": "step-8",
      "description": "Adversarial self-review, then commit by explicit pathspec. Read the diff as an attacker: is the expected export-name list genuinely hand-written, or did it get derived from the namespace it checks; does the test import the ROOT specifier `@moe/runner` everywhere, with no relative or deep path anywhere in the file; is any test-only module exported; is any name exported twice from two modules; did any supervisor .ts file get edited; was a .js bridge added anywhere; can a consumer still discriminate all three LifecycleOutcome kinds and still distinguish MUST_DRAIN from REFUSED; does the published set match the hand-written list in BOTH directions so an unreviewed addition also goes red. Then stage and commit by EXPLICIT PATHSPEC — `git commit -- packages/runner/src/index.ts packages/runner/src/index-surface.test.ts` — never a bare git commit, never git commit -a, never git add -A, and never a directory pathspec under packages/runner/src/, because child 2 has uncommitted work throughout this package and the index is shared state. Run `git status --porcelain` first and unstage any foreign path with `git restore --staged`. Verify with `git show --stat` that exactly those two paths landed and no probe, scratch or debug file is present.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "packages/runner/src/index.ts",
        "packages/runner/src/index-surface.test.ts"
      ],
      "note": "Adversarial self-review, then commit by explicit pathspec. Commit 9bfaa3b.\n\nATTACK 1 — is the expected list self-derived? NO, and this is provable rather than asserted: the list was written in step 2 while index.ts was still untouched, and the step-3 red run showed 43 failures against it. A list derived from the namespace it checks CANNOT go red. It went red, then green only after step 4 published the names.\n\nATTACK 2 — does it really test the published seam? `grep -n 'from \"' index-surface.test.ts` returns exactly three lines: \"vitest\", \"@moe/runner\", \"@moe/runner\". Zero relative specifiers, zero deep subpaths anywhere in the file.\n\nATTACK 3 — MUTATION DRILL, BOTH DIRECTIONS (epic rail 6: verify a failure-path test by mutating the production surface). Run post-commit so the good state was never at risk, each drill restored inside the same command:\n  A) Deleted `withLeg,` from the seam -> \"Tests 3 failed | 75 passed\". Named red: \"publishes withLeg on the package root as a function\" AND \"publishes exactly the reviewed root namespace, with no loss and no addition\". Loss reports BY NAME.\n  B) Added an unreviewed `isRef,` to the seam -> \"Tests 1 failed | 77 passed\", set-equality red with the diff line `+ \"isRef\"`. An unreviewed ADDITION goes red too.\n  Both restored: `git status --porcelain -- packages` -> 0 dirty paths after each.\n\nATTACK 4 — a swept case that silently generates nothing. `it.each(EXPECTED_EXPORTS)` over an emptied table would pass while testing nothing; guarded by `expect(EXPECTED_EXPORTS.length).toBe(66)`.\n\nATTACK 5 — test-only code on the runtime surface? `grep -c effect-test-fixtures index.ts` -> 0, plus a test asserting all 16 hand-transcribed fixture names are absent.\n\nATTACK 6 — a name exported twice? effect-activation.ts:36 re-exports five of effect-grant's names; the seam takes those five from effect-grant.js ONLY. Typecheck exit 0 confirms no duplicate export.\n\nATTACK 7 — foreign edits? `git diff --stat HEAD -- packages/runner/src/supervisor/` -> empty. No supervisor .ts touched, no .js added, no package.json or lockfile edit.\n\nFINDING ACTED ON — narrowed the seam. MIRRORED_LEASE_KEYS and MIRRORED_PROOF_KEYS were on the step-2 list; I REMOVED them. They are the only arrays in the candidate set that are NOT Object.freeze'd (plain `as const` at effect-shape.ts:60 and :74, whereas MIRRORED_LEASE_KINDS/STATES and every kernel vocabulary are frozen), and parseMirroredLease/parseMirroredProof read them on EVERY call via exactRecord. Publishing a live mutable array that the production parsers depend on puts a runtime tamper vector on the package surface — `MIRRORED_LEASE_KEYS.length = 0` would break every mirror fence. Freezing them is not available to me: that is an edit to a supervisor module, which rail 1 forbids. No DoD requires them and no committed consumer exists (child 4's drift test is speculative and can publish them later together with the freeze). Coverage was replaced with something strictly stronger — a behavioural assertion that `parseMirroredLease({...LEASE, extra: 1})` is null, which tests the exact-own-key contract through the seam rather than republishing the key list as data. Surface is therefore 66 root keys (26 pre-existing + 40 supervisor), not 68; the hand list, the cardinality guard, the export block and the seam doc comment were all updated together.\n\nCOMMIT, by explicit pathspec (epic rail 3):\n  `git add -- packages/runner/src/index.ts packages/runner/src/index-surface.test.ts`\n  `git status --porcelain` inspected BEFORE committing: staged entries were exactly `A packages/runner/src/index-surface.test.ts` and `M packages/runner/src/index.ts`. The .moe/* churn (daemon message logs and task JSON, including three foreign task files) was left unstaged — no `git restore --staged` was needed because no foreign path was ever staged.\n  `git commit -- packages/runner/src/index.ts packages/runner/src/index-surface.test.ts` — never bare, never -a, never add -A, never a directory pathspec.\n  `git show --stat HEAD` -> 9bfaa3b, `2 files changed, 396 insertions(+)`: index-surface.test.ts 304 (new), index.ts 92. Exactly the two owned paths; no probe, scratch, debug or generated file.\n  The first commit attempt (2e1063d) took a stray leading/trailing `@` into its MESSAGE because I used PowerShell here-string syntax in the bash tool; the content was already correct. Amended message-only with a heredoc (`git diff --cached --name-only` confirmed 0 staged paths first, so the amend could not absorb foreign work), giving 9bfaa3b with identical content.\n\nFINAL STATE: `git status --porcelain -- packages apps` -> empty. Gate re-run fresh after restore: runner typecheck 0, `Test Files 22 passed (22) | Tests 799 passed (799)`, daemon typecheck 0, GATE_EXIT=0.\n\nNot applicable by construction: concurrency, IO, partial writes, retries, cleanup paths. This change adds no runtime behaviour — it is a re-export block over pure reducers with no clock, no randomness and no process.",
      "startedAt": "2026-08-08T21:33:02.110Z",
      "completedAt": "2026-08-08T21:36:25.616Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 35,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-08T20:50:14.358Z",
  "updatedAt": "2026-08-08T21:43:26.754Z",
  "metrics": {
    "firstClaimAt": "2026-08-08T20:53:00.964Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 2,
    "executedStepCount": 8,
    "doneAt": "2026-08-08T21:43:26.754Z",
    "wallClockMs": 3025790
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-08T20:54:35.901Z",
  "planningNotes": {
    "approachesConsidered": "(1) CURATED NAMED EXPORTS, NOT `export *` — the task specifies this and the existing file already demonstrates the style: named exports grouped per module, types inline via the `type` modifier, every specifier written with a `.js` extension per NodeNext. A blanket re-export would publish whatever a module happens to export today and silently republish whatever it exports tomorrow, which is exactly what DoD 3's set-equality assertion exists to prevent.\n(2) SELF-REFERENCING IMPORT IS THE POINT, AND IS THE ONE THING THAT MIGHT NOT WORK — DoD 2 requires the test to import the package ROOT specifier `@moe/runner`, never a relative path, because the whole defect being fixed is that `exports: {\".\": \"./src/index.ts\"}` is an EXCLUSIVE map and deep subpath imports are unreachable. Node supports self-reference by package name when the package declares `exports`, and vitest resolves workspace packages, so this should work — but verify it in step 3 before building the whole suite on it. If self-reference does not resolve under this vitest config, that is a real finding: report it rather than quietly downgrading to a relative import, because a relative import would make the test pass while testing nothing about the published seam.\n(3) SIZE — index.ts is 70 lines today; the child-1 surface is roughly 60-70 specifiers, landing around 140-150 lines. That is inside the 250 target, so no extraction is needed. Rail 6's escape hatch (a supervisor re-export module) applies only if it actually bursts 250.\n(4) NO BRIDGES HERE, deliberately — and the reason is not 'they are unnecessary'. @moe/runner genuinely IS unloadable under plain Node today; that is a real defect owned by task-eb9ff081 as a package-wide sweep. A partial sweep in this task would be silently incomplete and invisible to the vitest gate, which is strictly worse than none.",
    "codebaseInsights": "VERIFIED ON DISK 2026-08-08, symbol by symbol — do not guess, but do re-confirm, because child 2 is committing into this directory:\n- **`packages/runner/src/index.ts` is 70 lines** and exports only artifacts, scope and workspace. Its existing style is the template to copy exactly: one grouped `export { ... } from \"./dir/module.js\";` block per module, value exports first in alphabetical-ish order, then `type X` entries, all inside the same braces.\n- **effect-kernel.js**: SUPERVISOR_EFFECT_PROTOCOL_VERSION, SUPERVISOR_ACTIVATION_VERSION, SUPERVISOR_RESULT_VERSION, EFFECT_CALLER_CONTRACT, EFFECT_STATES, TERMINAL_EFFECT_STATES, EFFECT_COMMANDS, ATTEMPT_SLICE_STATES, GRANT_STATES, SUPERVISOR_LAYERS, SUPERVISOR_ERROR_CODES, supervisorFailure, withLeg, isTerminalEffectState; types EffectState, TerminalEffectState, EffectCommand, AttemptSliceState, GrantState, SupervisorLayer, SupervisorErrorCode, SupervisorFailureDetail, SupervisorFailure, EffectIntent, AttemptSlice, EffectClaim, EffectTombstone, ActivationGrant, SettlementEvidence, UncertaintyEvidence, DependencyWitness, EffectResult.\n- **effect-shape.js**: MAX_SUPERVISOR_COUNT, MAX_SUPERVISOR_TEXT_CHARS, MIRRORED_LEASE_KINDS, MIRRORED_LEASE_STATES, MIRRORED_LEASE_KEYS, MIRRORED_PROOF_KEYS, parseMirroredLease, parseMirroredProof (plus generic validators isStrictRecord/exactRecord/isRef/isDigest/isCount/oneOf/readList/readOwnDataProperty/hasOnlyOwnStringKeys/isPlainArray — decide deliberately whether those belong on a public seam); types MirroredLeaseKind, MirroredLeaseState, MirroredLeaseRecord, MirroredLeaseProof, DataPropertyRead.\n- **effect-parse.js**: parseEffectIntent, parseAttemptSlice, parseEffectClaim, parseEffectTombstone, parseActivationGrant, parseSettlementEvidence, parseUncertaintyEvidence, parseDependencyWitness, parseCommandInput; types SettleCommand, SimpleCommand, EffectCommandInput.\n- **effect-lifecycle.js**: ADMITTED_EFFECT_TRANSITIONS, applyEffectCommand, applyEffectTombstone; types AdmittedTransition, LifecycleOutcome.\n- **effect-grant.js**: grantRefusal, initialGrantBinding, activationDigestInput, deriveGrantId, consumeActivationGrant, validateActivationCommit; types GrantOutcome, CommitCheck.\n- **effect-activation.js**: activateEffect; types ActivationCommit, ActivationOutcome. **CAUTION: `effect-activation.ts:36` contains its own `export { ... }` re-export block** — read it before writing index.ts so you do not export the same name twice from two modules, which is a duplicate-export compile error.\n- **lease-mirror.js**: fenceMirroredLease; type MirrorVerdict.\n- **Child 2 is committing into this directory right now** and has already appended to the frozen vocabularies (SUPERVISOR_ERROR_CODES now runs to line 126, SUPERVISOR_LAYERS to 75 — both longer than when child 1 landed). This is exactly the safe case the task describes: appending MEMBERS to an exported array does not change the set of export NAMES, so the set-equality test stays green.",
    "risks": "1. **The self-referencing root import is the single point of failure for DoD 2.** Probe `import * as runner from \"@moe/runner\"` in a trivial test FIRST, before writing the full suite. If it does not resolve under this vitest config, report it — do NOT silently fall back to a relative or deep import, because that would make the suite pass while testing nothing about the published seam, which is the whole deliverable.\n2. **Duplicate exports are a compile error, and effect-activation.ts already re-exports.** Read its `export {}` block at line 36 before composing index.ts; a name re-exported from both effect-kernel.js and effect-activation.js will fail typecheck in a way that reads like a mystery.\n3. `mem:gotcha-self-derived-universe-cannot-check-itself` is called out by DoD 3 explicitly: the expected export-name list must be HAND-TRANSCRIBED from the modules. If you generate it from `Object.keys(namespace)`, the test asserts that the namespace equals itself and can never fail. Write it by hand, in a separate step, before the test that consumes it.\n4. **Child 2 is actively committing in `packages/runner/src/supervisor/`.** Re-read the seven modules at step 1 rather than trusting this note. Appended array members are safe; a RENAMED or REMOVED export would break you, and the fix is to report it, never to edit a supervisor module (rail 1).\n5. **Decide deliberately about effect-shape's generic validators** (isStrictRecord, exactRecord, isRef, isDigest, isCount, oneOf, readList, readOwnDataProperty, hasOnlyOwnStringKeys, isPlainArray). They are internal helpers, not supervisor domain surface. Publishing them widens the package's public API permanently for no stated consumer need; DoD 1 names only what a caller needs to create an intent, apply a command, consume a grant and fence a lease. Prefer excluding them and say so in the evidence — but whatever you choose, the hand-written list and the export block must agree.\n6. **Never export effect-test-fixtures.ts** (rail 4) — test-only code on the runtime surface.\n7. **Do not add a .js bridge** (rail 2). @moe/runner really is unloadable under plain Node today, but that is a package-wide defect owned by task-eb9ff081; a partial sweep here would be invisible to this task's vitest gate and worse than nothing.\n8. Commit hazard: child 2 has uncommitted work throughout this package. Commit exactly the two owned files by explicit pathspec; never a directory, never a bare commit.",
    "keyFiles": [
      "packages/runner/src/index.ts",
      "packages/runner/src/supervisor/effect-kernel.ts",
      "packages/runner/src/supervisor/effect-lifecycle.ts",
      "packages/runner/src/supervisor/effect-activation.ts",
      "packages/runner/src/supervisor/effect-grant.ts",
      "packages/runner/src/supervisor/lease-mirror.ts",
      "packages/runner/src/supervisor/effect-parse.ts",
      "packages/runner/src/supervisor/effect-shape.ts"
    ]
  },
  "budget": {
    "wallClockMs": 10800000
  },
  "planApprovedAt": "2026-08-08T20:54:37.941Z",
  "contextFetchedBy": [
    "worker-964ae3f0",
    "qa-cbad3a29"
  ],
  "workStartedAt": "2026-08-08T21:20:57.609Z",
  "reviewStartedAt": "2026-08-08T21:38:09.735Z",
  "verification": {
    "command": "pnpm --filter @moe/runner typecheck && pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\n$ vitest run --root ../.. packages/runner/src\n\n RUN  v4.1.10 D:/projexts/moe-next\n\n Test Files  22 passed (22)\n      Tests  799 passed (799)\n   Start at  00:35:54\n   Duration  604ms (transform 2.62s, setup 0ms, import 3.84s, tests 885ms, environment 1ms)\n\n$ tsc --project tsconfig.json\nGATE_EXIT=0",
    "reportedAt": "2026-08-08T21:38:09.735Z"
  },
  "filesModified": [
    "packages/runner/src/index-surface.test.ts",
    "packages/runner/src/index.ts"
  ],
  "completedAt": "2026-08-08T21:43:26.754Z",
  "reviewCompletedAt": "2026-08-08T21:43:26.754Z",
  "reviewSummary": "APPROVED 9bfaa3b. 2 owned files (+396): index.ts 70->162 additive-only, index-surface.test.ts new. Gate re-run by me twice from repo root -> exit 0, 22 files/799 tests. Detail: mem:task-task-53680e91db1c4789a42b7129e61bdd7a-qa-verdict.\n\nDoD1: 40 values + 32 types, each cross-checked against grep '^export' on all 7 modules (kernel 14/14, parse 9/9, lifecycle 3/3, grant 6/6, activation 1, mirror 1, shape 6). No duplicate: activation.ts:36 re-exports the 5 grant symbols, index takes them from effect-grant.js only.\nDoD2: imports ONLY bare root @moe/runner (lines 16,25). No relative/deep path in file.\nDoD3 MUTATION-DRILLED BY ME both ways (rail 6): removed withLeg -> 3 red BY NAME + set equality \"[Array(65)] to deeply equal [Array(66)] - withLeg\"; added unreviewed isRef -> \"+ isRef\". 78 tests still COLLECTED on red, not \"(0 test)\", so no module-scope fixture reaches into the subject. Restored byte-identical (git diff --exit-code 9bfaa3b clean).\nDoD4: REFUSED keys exactly [\"failure\",\"kind\"] (:153); MUST_DRAIN keys [\"drainRequired\",\"intent\",\"kind\",\"versionDelta\"] + explicit \"ok\" in outcome === false (:220). All 3 kinds obtained by CALLING exported applyEffectCommand. 10 reason codes + refusing LAYER pinned.\nDoD5: git show --name-only = exactly the 2 paths; 0 .js, 0 supervisor/, 0 package.json; fixtures unexported. BEFORE/AFTER VERIFIED INDEPENDENTLY: 799/22 minus new file's 78/1 = 721/21 = stated pre-task count. Zero regressions.\nDoD6: exit 0. index.ts 162 lines wc -l (<250). 32 types machine-checked used (noUnusedLocals).\n\nNARROWING vs plan step-2 ACCEPTED: MIRRORED_LEASE_KEYS/MIRRORED_PROOF_KEYS withheld - shape.ts:60,:74 are the only arrays lacking Object.freeze and parsers read them per call, so publishing = live tamper vector; freezing would edit a supervisor module (rail 1).\n\nBLEMISH not a reject: step-6/7 notes say 801/80, reality 799/78 - stale after step 8 dropped 2 exports. 721->799 holds; I measured it."
}