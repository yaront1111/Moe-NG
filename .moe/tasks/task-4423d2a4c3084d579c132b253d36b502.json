{
  "id": "task-4423d2a4c3084d579c132b253d36b502",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "Build a goal to completion: expansion runs, next-node proposals, parallel seats",
  "description": "Filed by architect-16a01670 from parent task-e7a3d643bde34da7b27495dbda745739 (epic breakdown, human-directed 2026-09-04).\n\nPRODUCT GAP (README, re-measured 2026-09-04): 'an initial plan seals one node, so a goal is built one node at a time and growth goes through a successor goal.' Measured: core finalize refuses multi-node INITIAL runs (`packages/core/src/planning/planning-run-submission.ts` MULTI_NODE_EXECUTION_UNSUPPORTED, design 396/1322 - a FENCE, keep it); the compiler mirrors it (`apps/daemon/src/planning/compile-dispatcher.ts:192-197` SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL). Core already models `PlanningRunKind = INITIAL|REVISION|EXPANSION` with EXPANSION_PLANNING holds and sealed expansion proposals (`packages/core/src/planning/planning-command-contract.ts:11,138-202`, planning-event-contract.ts:49-115), and DONE rows landed the expansion prerequisites (task-671cdd10 'select the durable released attempt for an expansion parent', task-e62e3828 'current expansion safe-release evidence') - but NO expansion kind is published on the daemon vocabulary (`grep -c request_expansion apps/daemon/src/daemon-command-vocabulary.ts` = 0). Growth today = REPLAN successor goal (apps/control-room/src/v2/approvals/replan-successor-port.ts).\n\nFEATURE: after a node is accepted and landed (COMMITTED `moe-landing-receipt/1`), the daemon opens an EXPANSION planning run on the SAME goal; the wrapper staffs a planning seat whose mission carries the approved contract plus the coverage read's UNCOVERED criteria (`POST /documents/coverage/read`); the seat submits the next node(s) through the MCP `planning.submit_decomposition` path extended for EXPANSION (multi-node is allowed there by core design; HARD edges between dependent nodes); the human approves the expansion in the browser (plan-approval card reused, one decision); the wrapper staffs EVERY READY unclaimed node - independent nodes in parallel, bounded by a durable per-project seat limit (default 2) shown on Seats; the goal is 'ready to close' only when every approved criterion is covered. No successor goal for growth (REPLAN stays for exhausted reviews).\n\nALSO FIX (measured 2026-09-04 on UnAI goal-c9d9850b, memory real-seat-cannot-release-its-claim-2026-09-04): `work_get_context` ignores its `workItemId` (60 KB global dump; claimAggregateVersion unreachable when the harness truncates) and `EXPECTED_VERSION_CONFLICT` carries no observed version, so a real seat cannot `work_release` and its claim lapses at the 30-min TTL. This row makes work_get_context honour workItemId (or returns claimAggregateVersion in the work_claim receipt) and puts `observedVersion` in the refusal details - a multi-node goal cannot be staffed twice without it.\n\nUI: the board (PR #23, apps/control-room/src/v2/board/) shows every node across its six columns; the goal status strip reads 'N of M criteria covered - expansion proposed / awaiting your approval / building'; Needs-you gets 'Approve expansion' (offer-wire pattern); Seats shows the concurrency limit.\n\nDESIGN CONSTRAINT: never relax the INITIAL one-node fence; route growth through EXPANSION (memory initial-run-seals-one-node-by-core-design).",
  "definitionOfDone": [
    "Daemon publishes the expansion kinds (architect names them, e.g. `graph.request_expansion` plus the EXPANSION arm of planning.submit_decomposition) with the full roster backfill (vocabulary, registry ROWS + REGISTRATION_ORDER + size pins, families, v2 registry, MCP allowlist/exclusions, generated client + every digest mirror); roster tests assert set-equality in both directions.",
    "e2e over a REAL daemon with a scripted seat double on the real MCP wire: node 1 lands -> expansion run opened -> 2 nodes proposed -> approved in the browser -> both staffed -> delivered, verified, landed on the SAME goal; the goal reaches ready-to-close only at 100% criteria coverage; assert node count = 3 and every criterion VERIFIED on the coverage read.",
    "Wrapper test: two independent READY nodes produce two SPAWNED seats before either exits; a HARD-dependent node is not staffed until its parent is DONE; the durable per-project limit (default 2) is honoured and disclosed on /sessions/read.",
    "`work_get_context {workItemId}` returns that item's claim only (claimAggregateVersion present, response under 8 KB for one item) and `EXPECTED_VERSION_CONFLICT` details carry `observedVersion`; a seat-double test releases its claim on the first try; the refusal test asserts code + layer + observedVersion.",
    "Core fence untouched: the MULTI_NODE_EXECUTION_UNSUPPORTED arm still refuses a 2-node INITIAL run, and mutating compile-dispatcher.ts to allow it turns that test red (drill recorded in a step note).",
    "Live drive on UnAI (goal-c9d9850b or a fresh goal): a second node proposed, approved and landed on the same goal - transcript + landing shas in a row comment; README 'one node at a time' sentence rewritten to the measured truth.",
    "Gates quoted with their count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e`."
  ],
  "taskRails": [
    "FULL VERTICAL SLICE (human DoD 2026-09-04): this row ships daemon + control-room UI + tests + docs for its feature and carries NO dependsOn on any sibling row of this epic. If the plan exceeds the daemon's thresholds (12 steps / 10 files), the planning architect SPIDR-splits into children under THIS row (parentTaskId); children may depend on each other, never on a sibling feature row.",
    "FEATURES ONLY (human DoD 2026-09-04): no security-boundary or policy work in this row. The verifier stays the documented trusted-workspace v0.1 limitation; signing, hermetic verification and freeze manifests are out of scope.",
    "HUMAN GATES ARE HUMAN-ONLY: every approve/decide kind this row adds is operator-only, listed in HUMAN_ONLY_STEPS and MCP-excluded; every refusal carries a stable code asserted by a test (global rail 1); roster tests are bidirectional set-equality (global rail 9). A new daemon read/command follows the 8-touchpoint + .js bridge pattern (memory ui-connect-everything-built-2026-09-02).",
    "PROVE IT LIVE: besides an offline e2e over a REAL daemon (no fixtures), completion records a live drive on the lane (UnAI at D:/projexts/UnAI or a fresh project): transcript, store path and sha in a row comment (keep each comment under 4500 chars). Never restart the wrapper to unstick a seat (claim TTL 30 min, memory real-seat-cannot-release-its-claim-2026-09-04)."
  ],
  "implementationPlan": [],
  "status": "PLANNING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Spun-out rows are filed in PLANNING (BACKLOG is human-gated and invisible to claim_next_task); created without the status field by mistake.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 20,
  "comments": [
    {
      "id": "comment-fbe64776c0aa494a977803b649caa79d",
      "author": "architect-16a01670",
      "content": "OWNER RULING 2026-09-04 (relayed by session moe-next-67 from the owner's words \"lift the fence\"): LIFT THE ONE-NODE FENCE. An INITIAL run seals the WHOLE graph; growth through expansion runs is a later path, not this row's.\n\nMeasured at main 68eb1c5d: the core fence this row's DoD 5 says to keep no longer exists - `MULTI_NODE_EXECUTION_UNSUPPORTED` was removed by 63efd3a3 \"feat(core): admit v2 multi-node planning\" (2026-08-31); `packages/core/src/planning/planning-run-submission.ts:131-155` now refuses only a run with ZERO execution-bearing nodes. What remains is (1) `apps/daemon/src/planning/compile-dispatcher.ts:196-197` `if (nodes.length > 1) return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\")`, (2) the mission sentence `apps/daemon/src/orchestrator/agent-mission-text.ts:101` \"Plan the SMALLEST COMPLETE SLICE: exactly ONE node in an INITIAL run\", and (3) NO dependency gate: `apps/daemon/src/http/affordance-read.ts:336-410` offers EVERY node of an approved plan as READY at once and never reads `dependsOn`, and the wrapper (`agent-wrapper.ts:349-360`) staffs any READY unclaimed step up to maxAgents.\n\nAMENDED SCOPE (replaces DoD 1 and DoD 5; DoD 2/3/4/6/7 stand with \"expansion run\" read as \"the sealed multi-node INITIAL graph\"):\n- DoD 1' : `compile-dispatcher.ts` admits N>=1 nodes; `SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL` is retired from its code roster (roster test updated, not skipped); the compiler mission tells the seat to plan the COMPLETE graph as a dependency DAG (every criterion of the approved revision covered exactly once across nodes, `dependsOn` naming hard build order), and a mission-text test pins the new sentence.\n- DoD 5' : a node.deliver step is READY only when every `dependsOn` node is ACCEPTED; otherwise the surface lists it BLOCKED with `missing: [\"depends:<nodeKey>\", ...]` (MISSING_TOKENS roster + wrapper test: the dependent node is NOT staffed while its parent is un-accepted, IS staffed on the next pass after acceptance). The board (PR #23) already renders such a node as QUEUED \"after <nodeKey>\".\n- The expansion kinds (`graph.request_expansion` etc.) are OUT of this row; file a separate row if growing a live goal is still wanted after this lands.\nPlanner: read this comment before submit_plan; the row's description still argues for the fence and is superseded here.",
      "timestamp": "2026-09-04T09:41:17.222Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-04T09:10:46.383Z",
  "updatedAt": "2026-09-04T09:41:17.223Z"
}