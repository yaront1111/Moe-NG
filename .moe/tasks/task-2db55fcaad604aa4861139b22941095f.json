{
  "id": "task-2db55fcaad604aa4861139b22941095f",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "Deploy composes backup-before-migrate before activating the candidate",
  "description": "Producer child of task-78eedd8ebc2b43c581ac21ea28648a9a, governor-directed re-slice 2026-09-06 19:10Z. Parent remains browser proof; this child closes the production forward call. At local0737f06c AND owner origin4253eba9, git grep migrateWithBackup outside tests finds only migration-service.ts:78. Engine0a56 already shipped ordering/receipt/restore; reuse, do not rebuild. Deploy service/command exist; owner origin4ac0dbda changes build/release authority and must be reconciled before implementation, never plan against old deploy wiring. Environment delivery is shipped (readEnvironmentDelivery/resolveEnvironmentLaunch); reuse explicit project/environment resolution, never launchDelivery's refusal-to-undefined convenience for required DB context. One host-context resolver composed here may be reused by migrate-down sibling, which depends on this row. No remote target or connection string from browser. Parent consumer is task78; all gates need nonzero counts.",
  "definitionOfDone": [
    "A real production deployment.deploy dispatch reaches existing migrateWithBackup for the admitted project/environment and exact deploy workspace/SHA, before candidate activation or traffic flip. Test through default daemon composition with injectable bounded host effects, not an isolated call to engine.",
    "Missing/unreadable database or mismatched target/workspace refuses before any migration effect, with stable code and actual layer. Resolve named environment values at the host boundary; never silently select production for preview, accept URL/workspace in command payload, or persist/log a secret.",
    "Production-composition ordering arm awaits dump completion before apply; MIGRATION_BACKUP_FAILED leaves schema unchanged and prevents candidate activation/traffic flip. MIGRATION_FAILED names failing file, persists REFUSED migration receipt and prevents deployed-success claim; assert actual effect counters/schema, code+layer, not just return.",
    "Replay/concurrency tests prove no duplicate batch or unsafe interleaving; use existing migration project lock and receipt identity, retain backup after post-dump failure, handle receipt-write/cleanup failures explicitly. No automatic schema rollback or restore beyond authority already supplied.",
    "Default-composition test and fresh scoped daemon tests/typecheck execute with nonzero counts; full daemon/root/app gates disclosed per path and lease rules. New runtime modules have physical .js bridges. Parent78 and migrate-down composition child consume the shipped edge; no new feature sibling dependency or second migration engine."
  ],
  "taskRails": [
    "Reuse landed engine and owner-origin deploy authority; remeasure after reconciliation. Do not duplicate release/build authority.",
    "Connection values remain process/host-only; no value, hash, prefix, URL, SQL or tool-output leak in evidence.",
    "Pre-migration backups use existing .moe-next/backups/pre-migration/<env> and shared lock; no second backup location."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE AND BASELINE. NO EDITS. The architect re-measured every anchor at HEAD d5b191be this session and recorded the results in planningNotes.codebaseInsights — read that first and do not re-derive it. YOUR JOB HERE IS THE PART THAT GOES STALE: (a) `git rev-parse --short HEAD`. If it is NOT d5b191be, re-run `git merge-base --is-ancestor 4ac0dbda HEAD` (must be YES) and RE-READ the deploy sequence in deploy-service.ts, because every line number in steps 4 and 5 was measured at d5b191be and moves when the file moves. (b) Re-confirm the gap is still open before building it: `git grep -n \"migrateWithBackup\" -- apps packages` minus `*.test.*` must still return only migration-service.ts:78 (the definition) and migrate-down-test-fixtures.ts (a FIXTURE — test tier despite the filename; do not mistake it for a landed edge). (c) READ THE ENGINE end to end (migration-service.ts, 98 lines) so you reuse rather than rebuild: `MigrationInput` :10-19, replay by `requestId` with `MIGRATION_RECEIPT_CONFLICT`, the project-wide lock via `MIGRATION_LOCK_LEAF` spanning the receipt write, throwing `MIGRATION_IN_PROGRESS` when held and `MIGRATION_BACKUP_FAILED` when the lock dir cannot be created. (d) CAPTURE THE GATE BASELINE at the merge-base BEFORE your diff — `pnpm --filter @moe/daemon test` and `pnpm typecheck` — so step 7 can attribute red by HEAD-minus-baseline intersected with this row's owned paths. Note the ~26 foreign dirty paths in this shared checkout; never revert, stash or `git add -A` them.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-service.ts"
      ],
      "note": "MEASURED AND BASELINED. NO EDITS — zero files created or modified this step. Full detail in comment-21e27f4a.\n\n(a) HEAD = 25827b94, NOT the plan's d5b191be, so the staleness check fired: `git merge-base --is-ancestor 4ac0dbda HEAD` = YES (built on reconciled deploy wiring), and I re-read deploy-service.ts. THE ANCHORS DID NOT MOVE: image inspect :263, digest guard :265-266, `candidateStarted = true` :268, `startCandidate` :269. Steps 4/5 line numbers stand.\n\n(b) GAP STILL OPEN. `git grep -n migrateWithBackup -- apps packages` minus *.test.* = migration-service.ts:78 (definition) + migrate-down-test-fixtures.ts:19,43,219 (FIXTURE, test tier). `grep -n migrat` in deploy-ports.ts / deploy-service.ts / deploy-command.ts = NOTHING. deploy-migration-context.* absent. Nothing landed this edge underneath the row.\n\n(c) ENGINE READ (98 lines). ONE FACT THE PLAN DOES NOT STATE, load-bearing for steps 4 and 6: `migrateWithBackup` does NOT throw on a failed apply — it RETURNS a receipt with outcome REFUSED / refusal MIGRATION_FAILED (:68-69), and also returns a REFUSED receipt when the backup directory cannot be made (:88-89). It THROWS only for MIGRATION_RECEIPT_CONFLICT (:84), MIGRATION_IN_PROGRESS or MIGRATION_BACKUP_FAILED at the lock (:93), and MIGRATION_RECEIPT_WRITE_FAILED (:96). The call site must branch on `receipt.outcome` AND catch; handling one alone leaks the other as an unhandled deploy crash.\n\n(d) BASELINE AT 25827b94 BEFORE ANY DIFF, under a full-suite-gate lease acquired 11:07:08Z and RELEASED at 11:13Z (granted on to task-5d309484 — not held across implementation):\n  `pnpm --filter @moe/daemon test`  EXIT 0   Test Files  577 passed (577) | Tests 10357 passed | 7 skipped (10364)  294.24s\n  `pnpm typecheck`                  EXIT 0   19 projects, each with its own `$ tsc` echo\nCLEAN BASELINE: zero foreign red to carry into step 7's HEAD-minus-baseline attribution.\n\nGOVERNOR COMMENT-561ab473 RE-MEASURED — BOTH FLAGS REAL, BOTH RESOLVED WITHOUT A BLOCK:\n1. REFUSAL ROSTER: confirmed — DEPLOY_REFUSAL_CODES (deploy-receipt-contracts.ts:41) is exactly four codes and decodeRefusal (:150) rejects any code outside them AND any layer != DAEMON_DEPLOY_ENGINE, while every MIGRATION_* code is layer DAEMON_INGRESS. Widening would drag in NINE non-plan files (control-room live-runs.ts:84 hard-copies the four literals, plus goal-deployments, runs-read, the e2e fake, the runbook) — a plan-time split signal I will not take. RESOLVED by the file's own three-times-established convention: DEPLOY_BUILD_FAILED is already the coarse bucket with the precise cause in the DETAIL for causes that are not builds (:246 a proxy-lease failure, :256, :266 where neither detail string is a refusal code). DoD 3's required code+layer are asserted where they are actually true — on the MIGRATION receipt the engine already persists — plus \"deploy outcome is not DEPLOYED\". No cast, no restamp, no roster edit.\n2. CREDENTIAL SEAM: confirmed and it is a working-bytes blocker. `grep -n credential apps/daemon/src/daemon-command-async-entries.ts` = ZERO HITS, and DeployCommandOptions (:66-86) has no credential member, so `options.environmentCredential` (registry :127, consumed only at :512 for the environment edge) never reaches the async deploy entry — a resolver-only change would make every real production migration refuse ENV_STORE_KEY_UNAVAILABLE and ship DoD 1 non-working. TWO FILES ADDED TO SCOPE, DISCLOSED: apps/daemon/src/daemon-command-registry.ts (the :295 createAsyncCommandEntries call site, where that same options object is ALREADY in scope) and apps/daemon/src/daemon-command-async-entries.ts (AsyncCommandEntryOptions :113 + forward at :232). Both are optional-field forwarding that breaks no caller; 8 planned + 2 = 10 distinct files, under the 20 cap. No shape, roster or command-kind change, so epic rail 1's digest-mirror re-census does not fire.\n\nShared checkout holds ~26 foreign dirty paths from other sessions: noted, never reverted, stashed or `git add -A`'d.",
      "startedAt": "2026-09-07T11:04:23.056Z",
      "completedAt": "2026-09-07T11:13:18.398Z"
    },
    {
      "stepId": "step-2",
      "description": "THE HOST-CONTEXT RESOLVER — NEW `apps/daemon/src/deployment/deploy-migration-context.ts` plus its one-line `.js` bridge (DoD 5), with a NAME-MATCHED `deploy-migration-context.test.ts` in step 3 (without the matching name, runtime-entrypoint.test.ts classes the module TEST tier and reports the bridge as `unexpected`). It is a MODULE rather than an inline block because the migrate-down sibling that depends on this row reuses it. IT RESOLVES `MigrationInput`'s host half for an ADMITTED (project, environment, workspace, sha): projectRoot, workspace, databaseUrl. USE `readEnvironmentDelivery(config, environment)` (environment-delivery.ts:104), passing the deploy's ALREADY-ADMITTED environment string. DO NOT USE `resolveEnvironmentLaunch` — it derives the environment from `PURPOSE_ENVIRONMENTS` (:69-71), measured at HEAD as exactly `{PREVIEW: \"preview\", VERIFIER: \"verify\"}` with NO deploy member, so a production deploy would resolve some other environment's variables — DoD 2's \"silently select production for preview\" hazard from the opposite direction. IGNORE THAT MAP'S HEADER COMMENT, which says the map grows when task-04b3ce7e lands: it landed and the map did not grow. DO NOT USE `launchDelivery` — its own header (:170-176) says a refusal becomes `undefined`, so the child proceeds WITHOUT variables, which for a required DATABASE_URL means migrating with no database context. INSPECT THE REFUSAL AND FORWARD IT UNCHANGED: `readEnvironmentDelivery` already carries the code AND the layer that answered (SCOPE for an unknown environment, KEY for an underivable seal); restamping would report the wrong layer, and DoD 2 requires the actual one. A MISMATCHED TARGET OR WORKSPACE, OR AN ABSENT DATABASE VALUE, REFUSES HERE — before any migration effect. THE VALUE NEVER LEAVES THE PROCESS (task rail 2, epic rail 3): no `databaseUrl` in a return summary, a log line, a refusal detail or an error message, and nothing that could be serialised. Keep the module under 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-migration-context.js"
      ],
      "newFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-migration-context.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-migration-context.js"
      ],
      "note": "THE HOST-CONTEXT RESOLVER LANDED. `apps/daemon/src/deployment/deploy-migration-context.ts` (159 lines, under the 250 target) + its one-line `.js` bridge `export * from \"./deploy-migration-context.ts\";`, matching the sibling pattern in `deploy-ports.js` exactly. The step-3 test file will be `deploy-migration-context.test.ts` — the name matches the module, so `runtime-entrypoint.test.ts` classes the module RUNTIME tier and the bridge as expected rather than `unexpected`.\n\nEXPORTED SURFACE: `resolveDeployMigrationContext(config, request)` -> `DeployMigrationContextResult`, plus `DEPLOY_MIGRATION_DATABASE_VARIABLE`, `DEPLOY_MIGRATION_CONTEXT_DETAILS`, and the four types. The migrate-down sibling composes THIS function; it is deliberately not a private helper.\n\nRESOLVER CHOICE, as the plan directs and for the reasons re-measured in step 1: `readEnvironmentDelivery(config, environment)` with the deploy's ALREADY-ADMITTED environment string. `resolveEnvironmentLaunch` is NOT used — `PURPOSE_ENVIRONMENTS` is `{PREVIEW: \"preview\", VERIFIER: \"verify\"}` with no deploy member, so a production deploy would resolve another environment's variables; the map's header comment claiming it grows when task-04b3ce7e lands is stale (that row is DONE, the map did not grow) and both facts are recorded in the module's header so the next reader does not re-walk it. `launchDelivery` is NOT used — its refusal-to-`undefined` would present an unreadable store as \"no database configured\".\n\nREFUSALS FORWARDED UNCHANGED. `if (!delivered.ok) return delivered;` — the ENV_* code and the layer that actually answered (SCOPE for an unknown environment, KEY for an underivable seal) pass through untouched. No restamping: DoD 2 requires the ACTUAL layer, and this module's own two workspace codes are the only ones it mints.\n\nITS OWN CODES, all refusing BEFORE any migration effect: DEPLOY_MIGRATION_WORKSPACE_UNCONFIGURED (no host-scoped workspace at all), DEPLOY_MIGRATION_WORKSPACE_MISMATCH (a request-named workspace that disagrees with the host's — CHECKED, never adopted), DEPLOY_MIGRATION_DATABASE_UNSET (no DATABASE_URL, or an EMPTY one: `migrateWithBackup` would hand \"\" straight to `dump`, so empty is as absent as missing). Each carries `layer: DEPLOY_ENGINE_STAMP` = \"DAEMON_DEPLOY_ENGINE\", reusing the existing exported stamp rather than minting a new one.\n\nNO NEW `*_LAYER` CONSTANT, DELIBERATELY. `tests/security/boundary-roster.security.ts` treats any column-zero const whose NAME ends in LAYER/LAYERS/BOUNDARIES — even a module-private one — as a public security boundary owing a roster row in a file this row does not own. Reusing `DEPLOY_ENGINE_STAMP` keeps the security lane untouched. Recorded in the module's own comment so the migrate-down sibling does not trip it.\n\nSECRET CONFINEMENT (task rail 2, epic rail 3): `databaseUrl` appears on the `ok` branch only, because the engine requires it. Every refusal is minted by a single private `refuse(code)` that looks its detail up from a FROZEN table keyed by code — there is no call site that can interpolate a value into a message, which is the same discipline `environmentRefusal` keeps and the reason it keeps it. No log line, no error, no thrown message anywhere in the module. Step 3's arm (e) proves it with a positive control.\n\nVERIFICATION, with a POSITIVE CONTROL because a silent exit-0 typecheck proves nothing:\n  `pnpm --filter @moe/daemon exec tsc --noEmit -p tsconfig.json`  EXIT 0 (clean)\n  PLANTED CONTROL, appended to the NEW module: `const PLANTED_CONTROL: number = \"not a number\";`\n  -> EXIT 1, `src/deployment/deploy-migration-context.ts(161,7): error TS2322: Type 'string' is not assignable to type 'number'.`\n  So the typecheck demonstrably READS this new file; the clean run is a real green, not a vacuous one.\nRESTORE PROOF (global rail 13 — both new paths are UNTRACKED `??`, so `git diff` is blind to them and certifies nothing). Backed up to %TEMP%/dmc-ca28be90.bak before mutating; restored; proved in a SEPARATE call by SHA256 equality:\n  281ec5bf0aa640ea28eaa2cf97aceb528525f1c5fc7e52c069dbcaaae623e717  %TEMP%/dmc-ca28be90.bak\n  281ec5bf0aa640ea28eaa2cf97aceb528525f1c5fc7e52c069dbcaaae623e717  apps/daemon/src/deployment/deploy-migration-context.ts\n  `grep -c PLANTED_CONTROL` -> 0. `git status --porcelain` on both paths -> `??` for each, as expected for new files.\n\nNO new lock leaf, NO second replay check, NO second backup location: this module resolves inputs only. `migrateWithBackup` keeps the project-wide lock, the receipt identity and the replay (task rail 1).",
      "startedAt": "2026-09-07T11:13:22.397Z",
      "completedAt": "2026-09-07T11:16:08.853Z"
    },
    {
      "stepId": "step-3",
      "description": "THE RESOLVER'S ARMS — NEW `apps/daemon/src/deployment/deploy-migration-context.test.ts` (the name MUST match step 2's module exactly). FIVE ARMS: (a) a readable environment resolves the full `MigrationInput` host half for the ADMITTED environment; (b) THE WRONG-ENVIRONMENT ARM, DoD 2's core — variables bound for `preview` are NOT returned when resolving `production`, and vice versa; assert the isolation directly rather than trusting the call; (c) an unreadable or absent store refuses with the code AND the layer `readEnvironmentDelivery` returned, forwarded unchanged — assert BOTH (global rail 1), and assert it refuses BEFORE any migration effect; (d) a mismatched target or workspace refuses with its own stable code and layer; (e) NO SECRET LEAKS — assert that no returned value, refusal, or serialised form contains the database URL, WITH A POSITIVE CONTROL: feed a credential-shaped value and prove the assertion would have caught it. Without that control the arm passes vacuously because nothing credential-shaped was ever present — the exact failure mode epic rail 3 keeps producing on this board. Each arm asserts the specific reason code, never merely that it refused.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.test.ts"
      ],
      "note": "FIVE ARMS LANDED in NEW `apps/daemon/src/deployment/deploy-migration-context.test.ts` — the name matches step 2's module exactly, so `runtime-entrypoint.test.ts` classes the module RUNTIME tier and its `.js` bridge as expected.\n\nFRESH SCOPED RUN, NONZERO COUNT (global rail 8 — the count line is the discriminator, not the exit code):\n  `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/deploy-migration-context.test.ts`\n  EXIT 0   Test Files  1 passed (1)   Tests  5 passed (5)   848ms\n\n(a) FULL HOST HALF, not a subset. Asserted with `toEqual` on the whole `MigrationInput` — `{databaseUrl, environment, projectId, projectRoot, requestId, sha, workspace}` — because a resolver that silently dropped `projectRoot` would still \"work\" and then have `migrationBackupDirectory` create a backup tree somewhere nobody named.\n\n(b) THE WRONG-ENVIRONMENT ARM, DoD 2's core, asserted as ISOLATION IN BOTH DIRECTIONS. `production` and `preview` are seeded with DIFFERENT database URLs in ONE store, then resolved separately: each result must carry its own environment's value AND provably `not.toBe` the other's. A resolver that ignored the requested name — which is exactly what `resolveEnvironmentLaunch` would do, since `PURPOSE_ENVIRONMENTS` has no deploy member — fails this arm in whichever direction it defaulted.\n\n(c) REFUSALS FORWARDED WITH THE ENVIRONMENT SLICE'S OWN CODE AND LAYER, BOTH ASSERTED (global rail 1): `ENV_ENVIRONMENT_UNKNOWN` @ `SCOPE` for an unnamed environment, `ENV_STORE_KEY_UNAVAILABLE` @ `KEY` for an unreadable credential. Neither carries this module's stamp, which is the point — restamping would report a layer that did not refuse.\n\n\"BEFORE ANY MIGRATION EFFECT\" IS AN ASSERTION HERE, NOT A HOPE. Arms (c) and (d) run through a `migrateAfterResolving` helper that drives the REAL `migrateWithBackup` with counting `dump`/`apply` doubles and only when the resolution succeeded, then asserts `{applies: 0, dumps: 0}` on every refusal path. A resolver that refused but still let the caller reach `dump` shows `dumps: 1` and reds. The engine is the real one; only the two host effects are doubles, matching `migrate-down-test-fixtures.ts:43`'s stated discipline.\n\n(d) FOUR REFUSALS, each with its own stable code AND `layer: \"DAEMON_DEPLOY_ENGINE\"`: DEPLOY_MIGRATION_WORKSPACE_MISMATCH (a request-named workspace disagreeing with the host's), DEPLOY_MIGRATION_WORKSPACE_UNCONFIGURED (no host workspace at all), DEPLOY_MIGRATION_DATABASE_UNSET for an ABSENT database variable, and the same code for an EMPTY one — empty is asserted separately because `migrateWithBackup` would otherwise hand `\"\"` straight to `dump`, which is an effect with no database behind it.\n\n(e) NO SECRET LEAKS, WITH THE POSITIVE CONTROL THE PLAN AND EPIC RAIL 3 DEMAND. All four reachable refusals are minted WHILE a credential-shaped value (`postgres://app:prod-s3cr3t@db.internal:5432/app`) is really in the store, and each is asserted to contain neither the URL nor the password fragment under `JSON.stringify`. THE CONTROL: the SAME `contains` predicate is then fed a refusal whose detail was rewritten to `could not open <URL>`, and a bare `{detail: \"prod-s3cr3t\"}` — both must answer TRUE. Without that, four `toBe(false)` assertions are indistinguishable from a predicate that always answers false, which is the vacuity mode this board keeps producing. A third assertion proves the ok branch DOES carry the value, so \"nowhere in the refusals\" cannot be satisfied by a resolver that never resolved it.\n\nFIXTURES REUSED, NOT REBUILT: `environment-test-fixtures.ts`'s `openMemoryStore`, `credentialSource`, `unreadableCredentialSource`, `cleanUp`, `PROJECT_ID`, `CREDENTIAL`, and the production `setEnvironmentVariable` for seeding (asserted `{ok: true}` so a silently refused seed cannot make later assertions vacuous). No authority is reimplemented in the test.\n\nStep 7 drills (ii) and (iii) will mutate the PRODUCTION surface to prove arms (b) and (c) can actually see the two mistakes this module exists to prevent; not done here, per the plan's step ordering.",
      "startedAt": "2026-09-07T11:16:12.020Z",
      "completedAt": "2026-09-07T11:17:49.252Z"
    },
    {
      "stepId": "step-4",
      "description": "THE PORT AND THE ORDERED CALL SITE (DoD 1, 3). (1) `apps/daemon/src/deployment/deploy-ports.ts`: add a migration port to `DeployPorts` (:51-59, measured at HEAD as exactly `{build, docker, releaseDecision, ssh, target, transfer}`), following the shape of the existing members — an injectable bounded host effect, so a test can drive it without a database. (2) `apps/daemon/src/deployment/deploy-service.ts` (310 lines, under the cap — add the call and its refusal branch only): call the migration AFTER the image digest is verified (`image inspect`, :263) and BEFORE `candidateStarted = true` / `startCandidate` (:268-269). NOTE THESE TWO ANCHORS MOVED from the previous plan (264 and 270-271); if step 1 found HEAD past d5b191be, re-read them. THAT PLACEMENT IS THE SUBJECT OF THE ROW AND IS DELIBERATE ON BOTH SIDES — write it as a comment: migrating before the build could move the schema and then fail to produce an image, leaving the schema ahead of the code; migrating after `startCandidate` boots the candidate against the OLD schema. It mirrors the file's own principle at :229-232, that an effect with no durable trace is worse than a refusal. ON MIGRATION REFUSAL: return through the existing `refuse(...)` path so the deploy receipt records it, and DO NOT start the candidate or flip the proxy — `MIGRATION_FAILED` must never produce a DEPLOYED outcome (DoD 3). DO NOT re-implement replay, locking or receipt identity: `migrateWithBackup` owns all three (task rail 1), and a second lock leaf would defeat the shared lock the migrate-down revert depends on.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-ports.ts",
        "apps/daemon/src/deployment/deploy-service.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-ports.ts",
        "apps/daemon/src/deployment/deploy-service.ts"
      ],
      "note": "THE PORT AND THE ORDERED CALL SITE ARE IN.\n\n(1) `deploy-ports.ts` (351 -> 392 lines, under the 400 cap): `DeployMigrationPort = (environment, sha, decisionId) => Promise<DeployMigrationResult>`, with `DeployMigrationApplied {applied, ok:true}` and `DeployMigrationRefused {code, detail, layer, ok:false}`. Positional args mirror the existing `ReleaseDecisionPort = (environment, sha) => ...`. `decisionId` is carried because it IS the migration's replay identity: a replayed deploy must replay its receipt rather than start a second batch, and `migrateWithBackup` owns that replay by `requestId`. The refusal shape carries the MIGRATION's own code and layer verbatim — never restamped, because the deploy engine is not what refused.\n\nTHE PORT IS OPTIONAL IN THE TYPE, AND THAT WAS FORCED, NOT PREFERRED. A REQUIRED member would break every existing `DeployPorts` literal: `git grep -ln \"transfer:\"` names 13 construction sites, 11 of them test files plus `tests/e2e/control-room/fake-docker-dependencies.ts`, NONE in this row's affected paths. Widening a required member across them is the exact-key-roster rail's split signal. Optional in the type, MANDATORY in the production composition, and drill (iv) in step 7 proves the production default is the real migration BY OBSERVATION rather than by the type — which is precisely the contingency step 5 anticipates. The absent case is documented on the member itself as \"this composition did not ask for a migration\" (the rollback path and the docker-shaped arms), explicitly not a licence for production to omit it.\n\n(2) `deploy-service.ts` (310 -> 356 lines, under the cap): the call sits AFTER the digest guard (:263-266 `image inspect` + the `sha256:` regex) and BEFORE `candidateStarted = true` / `startCandidate`. The comment states why BOTH neighbours are wrong: before the build, a migration could move the schema and then fail to produce an image, leaving the schema ahead of the code with no image to roll back to; after `startCandidate`, the candidate boots against the OLD schema. Same principle as the file's own :229-232 — an effect with no durable trace is worse than a refusal.\n\nON REFUSAL: `return refuse(target, DEPLOY_BUILD_FAILED, migrated)` where `migrated` is `CODE@LAYER` or `CODE@LAYER: <failing file>`. Nothing is started and nothing is flipped — `candidateStarted` is still false, so the `finally` has no candidate to stop. The DEPLOY receipt therefore records a refusal (durable trace kept, unlike the receipt-less `report(...)` path at :233-235, which is only safe because it refuses before ANY effect and this point is after the build). Per step 1's measurement this carries the migration code in the DETAIL rather than as a deploy refusal CODE: `DEPLOY_REFUSAL_CODES` admits exactly four and `DeployRefusal.layer` is the literal `DAEMON_DEPLOY_ENGINE`, so a MIGRATION_* code cannot be one without a cast — and this file already uses DEPLOY_BUILD_FAILED as the coarse bucket with the precise cause in the detail three times (:246 a proxy lease, :256, :266). No cast, no restamp, no roster edit, no control-room twin touched.\n\nA NEW `migrateFor` HELPER, AND IT CATCHES — this is the fact step 1 found that the plan does not state. `migrateWithBackup` RETURNS a REFUSED receipt for a failed apply but THROWS for MIGRATION_IN_PROGRESS, MIGRATION_RECEIPT_CONFLICT and MIGRATION_RECEIPT_WRITE_FAILED, which are ordinary concurrency outcomes rather than bugs. Handling only one shape would leak the other as an exception escaping mid-sequence, past the durable trace. IT READS ONLY `code` AND `layer` OFF THE THROWN VALUE, NEVER ITS MESSAGE: the detail built here reaches the durable deploy receipt, and relaying free text from an arbitrary error is exactly how a connection value gets there (epic rail 3). An unrecognised throw becomes the fixed literal `MIGRATION_UNAVAILABLE`.\n\nNO second lock leaf, NO second replay check, NO second backup location (task rail 1).\n\nVERIFICATION, AND A FOREIGN RED THAT IS DISCLOSED, NOT HIDDEN:\n  `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/`\n  Test Files  6 failed | 8 passed (14)   Tests  89 passed (89)   EXIT 1\nALL SIX FAILURES ARE `(0 test)` MODULE-LOAD FAILURES, NOT ASSERTIONS — 89 tests ran, 89 passed, ZERO failed. The verbatim error:\n  Error: journey node definition refused: NODE_AUTHORITY_MALFORMED@NODE_AUTHORITY_ADMISSION\n   at readDraftFields ../../packages/scheduler/src/node-authority/node-authority-fields.ts:209:41\n   at draftNodeAuthority/createWithPlanning/createNodeDefinition node-authority-codec.ts:91,110,121\n   at nodeDefinitionOf src/planning/journey-authority-bodies.ts:126 -> bootstrap-test-fixtures.ts:144\nAffected: deploy-journey, deploy-release-binding, deploy-target-command, migrate-down-journey, migrate-down-real, migrate-down-admission.\nATTRIBUTION, MEASURED NOT ASSERTED. `git status --porcelain -- packages/scheduler/src/node-authority/` shows FOUR foreign dirty files from another live session: node-authorit",
      "startedAt": "2026-09-07T11:18:01.387Z",
      "completedAt": "2026-09-07T11:21:39.310Z"
    },
    {
      "stepId": "step-5",
      "description": "COMPOSE THE PRODUCTION EDGE (DoD 1, 5) in `apps/daemon/src/deployment/deploy-command.ts` (252 lines). Wire the real migration port — step 2's resolver feeding `migrateWithBackup` — into the production `DeployPorts` so a REAL `deployment.deploy` dispatch reaches it through the DEFAULT daemon composition, not only through an injected test object. READ HOW THIS FILE ALREADY COMPOSES: `productionDeployPorts()` (:93-115) holds the host effects, and the handler at :221 does `options.ports ?? { ...productionDeployPorts(store, projectId), releaseDecision: <real reader> }` — a spread whose members are then overridden with request-scoped readers. Your migration port needs the admitted goal/environment/workspace/sha, so it belongs in that same request-scoped position, not inside `productionDeployPorts()` which has no access to them. THE PRODUCTION DEFAULT MUST BE THE REAL MIGRATION: a composition that silently falls back to a no-op when no port is supplied would make every production deploy skip its migration while reporting success — the same class of defect as a fake docker default. If the port is optional in the type, drill (iv) in step 7 must prove by observation that the default is the real one. DECLARE THE `.js` BRIDGE for any new runtime module reached from here (DoD 5). Do not duplicate release or build authority (task rail 1): 4ac0dbda already reconciled it and this row composes rather than re-decides.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-command.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-command.ts",
        "apps/daemon/src/daemon-command-async-entries.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "note": "THE PRODUCTION EDGE IS COMPOSED, AND THE SEAM THE GOVERNOR FLAGGED IS THREADED.\n\nWHERE THE PORT WENT, and why not in `productionDeployPorts()`: the migration needs the ADMITTED environment, sha and decisionId, and that factory has access to none of them. It went in the REQUEST-SCOPED position — inside the `options.ports ?? { ...productionDeployPorts(store, projectId), ... }` spread, beside `releaseDecision`, exactly the pattern the file already uses for readers that must close over the admitted request.\n\nTHERE IS NO NO-OP FALLBACK. When no `ports` option is supplied — which is what every production `deployment.deploy` does — `migrate` is always the real `resolveDeployMigrationContext` feeding the real `migrateWithBackup`. The port is optional in the TYPE only (step 4's note explains why a required member was not available: 13 construction sites, 11 of them non-plan test files). Drill (iv) in step 7 proves the default is the real migration by OBSERVATION.\n\nTHE WORKSPACE IS THE BUILD CONTEXT, NOT A PAYLOAD KEY. `options.buildContext` feeds BOTH `workspace` and `projectRoot`, matching `migrate-down-test-fixtures.ts:220`'s own `projectRoot: root, workspace: root`. That is the same host-scoped directory the image is built from, so the migrations that run are the ones belonging to this sha; a caller-supplied path would let any operator-authenticated request migrate a directory nobody named, and an unconfigured daemon already refuses under DEPLOY_BUILD_CONTEXT_UNCONFIGURED. DoD 2's \"never accept URL/workspace in command payload\" holds: neither value is in PAYLOAD_KEYS.\n\n`decisionId` BECOMES THE MIGRATION'S `requestId`, so a REPLAYED deploy replays its migration receipt instead of starting a second batch. Replay, the project-wide lock and receipt identity all stay `migrateWithBackup`'s — nothing re-implemented, no second lock leaf (task rail 1).\n\nTHE ENGINE'S TWO RETURN SHAPES ARE BOTH MAPPED: APPLIED -> `{applied, ok:true}` carrying the engine's own batch list so an arm can assert WHAT ran; REFUSED -> `{code, detail, layer, ok:false}` taken from `receipt.refusal`, whose `detail` IS the engine's failing FILE for MIGRATION_FAILED — which is what DoD 3 requires the deploy to name, and is a file path, never a connection value. Throws (MIGRATION_IN_PROGRESS, MIGRATION_RECEIPT_CONFLICT, MIGRATION_RECEIPT_WRITE_FAILED) are caught by step 4's `migrateFor`, which reads only `code`/`layer` off the thrown value.\n\nTHE CREDENTIAL SEAM — TWO FILES BEYOND THE PLAN, DISCLOSED IN STEP 1 AND NOW WRITTEN. This is the governor's comment-561ab473 second flag, and without it a resolver-only change ships DoD 1 non-working: every real migration would refuse ENV_STORE_KEY_UNAVAILABLE@KEY because the seal is underivable.\n  `deploy-command.ts`            DeployCommandOptions gains `environmentCredential?: EnvironmentCredentialSource`; the port defaults it `?? (() => null)`, matching `daemon-command-registry.ts:512`'s own unwired-daemon default.\n  `daemon-command-async-entries.ts`  AsyncCommandEntryOptions gains the same member and forwards it into `createDeployCommandHandler` with the file's exactOptionalPropertyTypes spread idiom.\n  `daemon-command-registry.ts`   the `createAsyncCommandEntries` call at :295 forwards `options.environmentCredential` — THE SAME object already consumed at :512 for the environment edge, so this is one credential reader threaded further, not a second one.\nNEVER `process.env`: the governor named that explicitly, and this module's own rule for the build context is that host-scoped configuration is read ONCE at the composition root and passed down raw. All three are OPTIONAL additions that break no existing caller. No shape change, no roster change, no new command kind — epic rail 1's four GENERATED_CONTRACT_DIGEST mirrors and the control-room leg are untouched.\n\nDoD 5's `.js` BRIDGE: the only new runtime module is `deploy-migration-context.ts`, whose bridge landed in step 2. No other new module is reached from here.\n\nVERIFICATION — FRESH SCOPED RUN OVER EVERY FILE THIS ROW HAS CHANGED, NONZERO COUNT:\n  `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/deploy-service.test.ts src/deployment/deploy-migration-context.test.ts src/daemon-command-async-entries.test.ts src/daemon-command-registry.test.ts src/deployment/deploy-build-authority.test.ts src/deployment/deploy-rollback.test.ts`\n  EXIT 0   Test Files  6 passed (6)   Tests  472 passed (472)\n  `pnpm --filter @moe/daemon exec tsc --noEmit -p tsconfig.json` — the ONLY errors are the two foreign `packages/scheduler/src/node-authority/node-authority-codec.ts` TS2554s from the peer's uncommitted edit measured in step 4; `grep \"error TS\" | grep -v node-authority` returns NOTHING, so zero errors are attributable to this row.\n\nFiles this step: apps/daemon/src/deployment/deploy-command.ts, apps/daemon/src/daemon-command-async-entries.ts, apps/daemon/src/daemon-command-registry.ts. Running total for the row: 10 distinct paths, under se",
      "startedAt": "2026-09-07T11:21:52.636Z",
      "completedAt": "2026-09-07T11:23:31.446Z"
    },
    {
      "stepId": "step-6",
      "description": "THE ORDERING, REFUSAL AND CONCURRENCY ARMS (DoD 1, 3, 4) in `deploy-service.test.ts` and `deploy-journey.test.ts`. Drive the REAL command edge — deploy-journey.test.ts already resolves the async handler and injects `ports:` at :110-115, and migrate-down-test-fixtures.ts:43 records the discipline: \"The only doubles are the two host ports.\" ARMS: (a) DoD 1 — a real dispatch reaches the migration for the admitted project/environment with the exact workspace and SHA, asserted by OBSERVING the port called with those values, through the DEFAULT composition (no `ports` option), the way deploy-release-binding.test.ts:43-46 already proves a production composition. (b) DoD 3 ORDERING — the dump COMPLETES before the apply BEGINS; assert the observed sequence, not merely that both happened. (c) `MIGRATION_BACKUP_FAILED` leaves the schema UNCHANGED and prevents BOTH candidate activation and the traffic flip — assert the effect counters and the schema, plus code AND layer, never just the return value. (d) `MIGRATION_FAILED` names the failing file, persists a REFUSED migration receipt, and the DEPLOY receipt does NOT read DEPLOYED. (e) DoD 4 REPLAY/CONCURRENCY — a repeated dispatch produces NO duplicate batch (the engine's `requestId` replay), and two interleaved attempts do not both apply; use the EXISTING project lock and receipt identity, and assert `MIGRATION_IN_PROGRESS` with its layer rather than a generic failure. (f) A post-dump failure RETAINS the backup, and receipt-write/cleanup failures are handled explicitly — no automatic schema rollback or restore beyond the authority already supplied. Every refusal arm names its specific code and the layer that answered (global rail 1).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-service.test.ts",
        "apps/daemon/src/deployment/deploy-journey.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-service.test.ts",
        "apps/daemon/src/deployment/deploy-journey.test.ts",
        "apps/daemon/src/deployment/deploy-command.ts"
      ],
      "note": "SIX ARMS LANDED, and one of them found a REAL REGRESSION IN MY OWN STEP-5 COMPOSITION, which is now fixed.\n\nVERIFICATION — WHOLE DEPLOYMENT FOLDER, FRESH, NONZERO COUNT:\n  `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/`\n  EXIT 0   Test Files  14 passed (14)   Tests  173 passed | 1 skipped (174)\n  `pnpm --filter @moe/daemon exec tsc --noEmit -p tsconfig.json`  EXIT 0, `grep \"error TS\"` EMPTY — the foreign node-authority red disclosed in steps 4/5 has since cleared as that peer landed their work.\n  Per-file: deploy-service.test.ts 46 passed (42 pre-existing + 4 new), deploy-journey.test.ts 21 passed (20 pre-existing + 1 new).\n\nTHE REGRESSION, DISCLOSED IN FULL BECAUSE IT WAS MINE. Running the whole folder — not just my two files — reds `deploy-release-binding.test.ts` FIVE times with `DEPLOY_BUILD_FAILED: ENV_STORE_KEY_UNAVAILABLE@KEY`. That file drives the DEFAULT composition with no `ports` and no environment credential, so step 5's unconditional `migrate` member ran for real and refused. This was not a test problem: it meant EVERY deploy on a daemon that never configured an environment store would start refusing. FIX: the migration port is now composed ONLY when `options.environmentCredential !== undefined` — a WIRING fact, not a per-request escape hatch. A daemon with no environment store has no database to migrate; composing a port there could only ever refuse. The real composition root always supplies it (`daemon-store-foundation-composition.ts:277` `environmentCredential: () => config.credential`), so a production daemon always migrates; only an explicitly unwired composition does not, which is the case `daemon-command-environment.test.ts:702` already names in its own words. Recorded as a comment on the member itself. FOLLOW-UP for the epic-final row rather than a new row (delivery-bias rail): a wired-daemon assertion at the composition root would make \"the credential is present in production\" a test rather than a read of :277.\n\n(a) DoD 1 — THE DEFAULT COMPOSITION, NO `ports` ANYWHERE, in `deploy-journey.test.ts`. This is the arm the row is actually about: `deploy-command.ts` composes `options.ports ?? {...}`, so an injected `ports` REPLACES the request-scoped migration entirely — an arm that supplied `ports` could prove the engine's ordering perfectly while production shipped no migration at all. The arm seeds the environment store through the REAL `setEnvironmentVariable` with the real daemon credential, binds a durable target through the REAL `deployment.set_target` command, dispatches `deployment.deploy` through the registered async handler, and then OBSERVES: the migration receipt read back from the store is `{applied, environment: \"production\", outcome: \"APPLIED\", refusal: null, sha: SHA}` keyed by the DEPLOY's own commandId (so the replay identity really is the deploy's); `apply` received the host build context as its workspace — not a payload value and not a default; and `dump` received the value that was SEEDED, which nothing in the composition was ever told, so it can only have arrived by a genuine `readEnvironmentDelivery` for the ADMITTED environment. That last assertion is the one a `resolveEnvironmentLaunch` composition could never satisfy for `production`.\nOFFLINE PRESERVED BY MOCKING THE HOST EFFECT BOUNDARY ONLY: `nodeDockerRunner`, `nodeSshRunner`, `nodeImageTransfer`, `nodeDeployBuild` and `nodeMigrationPorts`, each via `importOriginal` so `createDockerDouble` and the argv builders stay real. `productionDeployPorts`, the resolver, the engine, the lock, both receipts and the whole deploy sequence are the shipped code. Same line `migrate-down-test-fixtures.ts:43` draws, same shape `deploy-release-binding.test.ts` uses to drive a production composition without a docker daemon.\n\n(b) ORDERING, ASSERTED AS A SEQUENCE. A journal records `dump:start, dump:end, apply:start, apply:end` and is compared with `toEqual` — the dump COMPLETES before the apply BEGINS, not merely that both happened. Then `docker.calls` indices prove the whole migration precedes the container start: `image inspect` index >= 0 and `run` index strictly greater.\n\n(c) MIGRATION_BACKUP_FAILED. Triggered for real by writing a FILE where `.moe-next` must be a directory — `migrationBackupDirectory` lstats every path component. Nothing mocked. Asserts the deploy refusal's code AND layer (`DEPLOY_BUILD_FAILED` @ `DAEMON_DEPLOY_ENGINE`, detail `MIGRATION_BACKUP_FAILED@DAEMON_INGRESS: backup failed`), that the SCHEMA is untouched and NEITHER host effect ran, and the ABSENCE of both `run` (candidate activation) and `exec ... tee` (the proxy write) — which is what \"prevents activation and traffic flip\" means as an assertion.\n\n(d) MIGRATION_FAILED + (f) BACKUP RETAINED, one test because the halves pull against each other. `apply` throws a real `MigrationExecutionError`; asserts the deploy detail is `MIGRATION_FAILED@DAEMON_INGRESS: 1725660000007_add_orders.sql`, the deploy receipt does NOT r",
      "startedAt": "2026-09-07T11:23:35.645Z",
      "completedAt": "2026-09-07T11:34:16.736Z"
    },
    {
      "stepId": "step-7",
      "description": "DRILLS AND GATES (DoD 5). DRILLS — run `git status --porcelain` on the mutated paths FIRST and say which proof applies to which: the edited files are TRACKED (empty `git diff -- <path>` is valid proof, in a call SEPARATE from the restore); step 2's and step 3's THREE NEW files are UNTRACKED and `git diff` is BLIND to them (empty before, during and after), so back each up out of tree (%TEMP%, suffixed with your worker id) and prove restoration by SHA256 equality — global rail 13, and this row is exactly the new-module case that rail fires on. FOUR DRILLS: (i) THE ORDERING DRILL, the row's subject — move the migration call to AFTER `startCandidate` and confirm arm (c) reds on the candidate having been activated despite a backup failure; quote the failing line. (ii) Swap `readEnvironmentDelivery` for `resolveEnvironmentLaunch` and confirm the wrong-environment arm (3b) reds — this is the mistake the plan exists to prevent, and the drill proves the arm can see it. (iii) Coerce the refusal to `undefined` the way `launchDelivery` does and confirm arm (3c) reds rather than the migration proceeding with no database context. (iv) Remove the production migration port from the composition and confirm the DoD-1 default-composition arm reds — proving the default is the real migration, not a silent no-op. GATES — standalone and foreground, ONE command per call, no pipes and no `;` chains, each quoted with its `Test Files N passed` count line; an exit-0 leg with NO count line RAN NOTHING (global rail 8): `pnpm --filter @moe/daemon test` and `pnpm typecheck`, plus fresh SCOPED runs of the new and edited files with NONZERO counts. Root `pnpm test` covers no `apps/**` file — say so rather than quoting it. Attribute any red by HEAD minus step 1's baseline, intersected with this row's owned paths, and disclose foreign red verbatim.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-service.ts",
        "apps/daemon/src/deployment/deploy-migration-context.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-service.ts",
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-command.ts"
      ],
      "note": "FOUR DRILLS RUN, ALL FOUR RED WHERE THEY SHOULD; GATES RUN; BOTH REPO-WIDE LEGS CARRY FOREIGN RED THAT ATTRIBUTES AWAY FROM THIS ROW BY A MEASURED CONTROL. Full drill transcript with verbatim failing lines is in comment-7beb57f1; summary here.\n\nDRILLS (tier check first: 7 TRACKED `M`, 3 UNTRACKED `??`; `git diff` is blind to the latter three so SHA256 against %TEMP%/drill-ca28be90-* is their only valid proof; every restore proved in a call SEPARATE from the restore):\n(i) Migration moved AFTER `startCandidate` -> arm (c) `expected [ [ 'run', '--detach', …(5) ] ] to deeply equal []` at deploy-service.test.ts:784, arm (d) same at :817. The candidate was ACTIVATED despite a backup failure. Restored, sha f1a99274…, order re-verified by `grep -n` (migrateFor :303, candidateStarted :314).\n(ii) `readEnvironmentDelivery` -> `resolveEnvironmentLaunch`. MY FIRST ATTEMPT WAS A BAD DRILL AND I REDID IT rather than bank it: the shim read `launched.variables` but the ok shape is `{delivered, environment, ok, purpose}`, so the arm reddened on my own adapter's TypeError instead of on the hazard. Re-applied against `.delivered`, arm (b) then failed with the real thing: `Expected: \"postgres://app:prod-s3cr3t@db.internal:5432/app\"` / `Received: \"postgres://app:preview-s3cr3t@db.preview:5432/app\"` — a production deploy resolving PREVIEW's database, DoD 2's hazard. Restored, sha 281ec5bf….\n(iii) Refusal coerced to `undefined` the `launchDelivery` way -> arm (c) `- \"code\": \"ENV_ENVIRONMENT_UNKNOWN\", - \"layer\": \"SCOPE\"` vs `+ \"code\": \"DEPLOY_MIGRATION_DATABASE_UNSET\", + \"layer\": \"DAEMON_DEPLOY_ENGINE\"`. The store's real refusal swallowed and re-reported from the wrong layer. Only an arm asserting BOTH code and layer can see this. Restored, sha 281ec5bf…, `grep -c \"DRILL:\"` 0.\n(iv) `migrate` member deleted from the default composition -> arm (a) `expected null to match object { …(5) }`: `readMigrationReceipt` answers NULL, no migration ran, the deploy still reported success. That is the silent-no-op defect, and it proves the production default is the REAL migration. Restored, sha 6313ebf4….\n\nGATES, standalone and foreground, one command per call, no pipes into the assertion and no `;` chains:\n1. `pnpm --filter @moe/daemon test` (THE PLAN'S NAMED VERIFICATION) — EXIT 1\n   `Test Files  4 failed | 574 passed (578)` / `Tests  6 failed | 10362 passed | 7 skipped (10375)` / 380.31s\n2. `pnpm typecheck` — EXIT 1, one error, verbatim:\n   `apps/control-room/src/v2/ops/agent-provider-toggle.test.tsx(33,15): error TS2352: Conversion of type 'NextAllowedCommand | undefined' to type 'Readonly<Record<string, unknown>>' may be a mistake…`\n   That path is FOREIGN dirty and this row owns ZERO control-room paths. `pnpm --filter @moe/daemon exec tsc --noEmit -p tsconfig.json` is EXIT 0 with `grep \"error TS\"` EMPTY.\n3. SCOPED, EVERY SUITE COVERING THIS ROW'S OWNED PATHS — EXIT 0\n   `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/ src/daemon-command-async-entries.test.ts src/daemon-command-registry.test.ts`\n   `Test Files  16 passed (16)` / `Tests  590 passed | 1 skipped (591)`\nROOT `pnpm test` NOT QUOTED, DELIBERATELY: the root vitest include carries no `apps/**` and every path this row owns is under apps/daemon, so a root count line would be evidence for nothing (global rail 18).\n\nATTRIBUTION OF LEG 1, MEASURED NOT ASSERTED. Step 1's baseline for the identical command, taken under a lease at 11:07Z before any byte of mine, was `Test Files 577 passed (577)` EXIT 0 — a CLEAN baseline, so HEAD-minus-baseline is the whole of this red. The 6 failing tests live in 5 files, NONE of them a path this row touches:\n  src/planning/v2-compiler/compiler.test.ts > binds exact nine-field node admission requests and a frozen bijective profile roster\n  src/planning/compiled-authority-bodies.test.ts > pins canonical bytes, digests, ordering, freezing, and its exact public surface\n  src/planning/compiled-authority-bodies.test.ts > loads the unchanged public bridge in plain Node and reproduces the canonical digests\n  src/planning/approval-activation.test.ts > COLLAPSE PIN: an approval onto an existing root commits ONE primary leg, unchanged\n  src/planning/approval-activation.test.ts > COLLAPSE PIN: the GENESIS approval still rides two legs, decision unchanged\n  src/daemon-foundation-command.test.ts > lets every advertised key past the payload-shape check when sent alone\nTHE CONTROL, and it is the decisive evidence: I backed up all ten of my paths, `git checkout --`'d my seven TRACKED ones and MOVED my three UNTRACKED ones out of the tree, leaving foreign dirt untouched — never reverted, stashed or `git add -A`'d — and re-ran those five files. `Test Files 4 failed | 1 passed (5)` / `Tests 6 failed | 181 passed (187)`: THE SAME SIX FAILURES BY NAME, with zero bytes of mine in the tree. Restored all ten immediately and proved each byte-identical by SHA256 in a separate call (deploy-service f1a99274, deploy-ports 7ea2d19c, deploy-comma",
      "startedAt": "2026-09-07T11:34:22.633Z",
      "completedAt": "2026-09-07T11:55:51.737Z"
    },
    {
      "stepId": "step-8",
      "description": "ADVERSARIAL PASS AND COMPLETION RECORD. Load `adversarial-self-review` and read your diff as an attacker. ANSWER IN WRITING: can the database URL reach ANY durable or observable surface — grep your diff for the value flowing into a receipt, a log, a refusal detail, an error message or a test assertion, and state where it is confined (task rail 2, epic rail 3); can a deploy for `preview` ever resolve `production`'s variables, or the reverse (the `PURPOSE_ENVIRONMENTS` hazard); does a migration refusal EVER leave a candidate running or the proxy flipped; can two concurrent deploys of the same project both apply a batch, and does the answer depend on the EXISTING lock rather than a new one; does a receipt-write failure leave the lock held; is the backup retained after a post-dump failure; and would a production deploy with NO migration port configured silently skip the migration and report success. THE COMPLETION RECORD MUST STATE: that 4ac0dbda was confirmed an ancestor of HEAD, so this was built on reconciled deploy wiring; the exact placement of the migration call relative to `startCandidate` and why both neighbours are wrong; WHICH resolver was used and why the other two were rejected, naming `PURPOSE_ENVIRONMENTS`' members and `launchDelivery`'s refusal-to-undefined; that no second engine, lock leaf or replay check was added; the four drill outcomes with their failing lines and restore proofs; and the scoped runs with their nonzero counts. NAME THE HOST-CONTEXT RESOLVER MODULE EXPLICITLY so the migrate-down sibling that depends on this row composes it rather than writing a second one. Put detail exceeding the 2000-char completionSummary cap in a row comment (keep each under ~4500). Report every path in complete_step.modifiedFiles.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-command.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-migration-context.js",
        "apps/daemon/src/deployment/deploy-migration-context.test.ts",
        "apps/daemon/src/deployment/deploy-ports.ts",
        "apps/daemon/src/deployment/deploy-service.ts",
        "apps/daemon/src/deployment/deploy-command.ts",
        "apps/daemon/src/deployment/deploy-service.test.ts",
        "apps/daemon/src/deployment/deploy-journey.test.ts",
        "apps/daemon/src/daemon-command-async-entries.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "note": "ADVERSARIAL PASS DONE (skill loaded, whole production diff read as an attacker). Full answers to all seven questions plus three residual risks are in comment-c7f337e8; the drill transcript is in comment-7beb57f1; the two governor-flag resolutions are in comment-21e27f4a. Headlines:\n\nCORRECTED MY OWN STEP-6 NOTE. I wrote that the receipt decoder applies `migrationFilename` to both `applied[]` and a MIGRATION_FAILED detail. It does NOT: migration-receipt.ts:63 applies it to `applied[]` only, and `refusal.detail` is validated by `text()` alone (non-empty, <=4096). The real secret guarantee sits earlier, in `MigrationExecutionError`'s constructor (migration-ports.ts:14), which coerces anything failing the filename grammar to `MIGRATION_FILE_UNKNOWN`. That distinction is load-bearing: `ports.apply` is caller-supplied and in production is the child-process runner, so a raw Error quoting the connection string would be a live leak — except migration-service.ts:69 reads `error.file` ONLY when the throw IS a MigrationExecutionError and takes the literal otherwise. Airtight, for a different reason than I first recorded.\n\nTHE ONE RESIDUAL I WOULD BE ASKED ABOUT IN A POSTMORTEM, stated first rather than buried: a daemon with NO `environmentCredential` composes no migration port and deploys without migrating. That gate is deliberate and was added in step 6 after my own regression — without it, `deploy-release-binding.test.ts` reddened five times with `DEPLOY_BUILD_FAILED: ENV_STORE_KEY_UNAVAILABLE@KEY`, i.e. every deploy on a daemon with no environment store would have begun refusing. The single production composition root supplies the credential unconditionally (`daemon-store-foundation-composition.ts:277` `environmentCredential: () => config.credential`; `git grep environmentCredential -- apps/daemon/src` minus tests returns that root and nothing else), and that line's own comment gives the same reasoning. FOLLOW-UP for the epic-final hardening row, not a new row (delivery-bias rail): an arm at the composition root would turn \"production is wired\" from a READ into a test.\n\nSECOND RESIDUAL, disclosed so DoD 2 is not over-claimed: `DEPLOY_MIGRATION_WORKSPACE_MISMATCH` is UNREACHABLE from the production deploy path — the composed port never passes `request.workspace`, so on that edge there is nothing to mismatch. The guard exists because the module is shared with the migrate-down sibling, which knows the workspace its batch was archived from; arm (d) exercises it directly.\n\nTHIRD: a receipt-write failure removes the lock in the engine's `finally`, but if `rmdirSync` itself fails the lock stays held and MIGRATION_RECEIPT_WRITE_FAILED is thrown — pre-existing engine behaviour, fail-closed, not introduced or repaired here.\n\nTHE FOUR OTHER ANSWERS, all NO with an arm and a drill behind each: no cross-environment resolution (arm b + drill ii, which produced `Expected \"…prod-s3cr3t@db.internal…\" / Received \"…preview-s3cr3t@db.preview…\"`); no candidate left running or proxy flipped on a migration refusal (arm c + drill i, `expected [ [ 'run', '--detach', …(5) ] ] to deeply equal []`); no two concurrent deploys both applying, and the answer depends on the ENGINE's existing lock leaf rather than a new one (arm e, MIGRATION_IN_PROGRESS@DAEMON_INGRESS); backup retained after a post-dump failure, asserted by `existsSync` on the file and not just the receipt field (arm d).\n\nNAMING THE MODULE FOR THE SIBLING, as the step requires: the migrate-down composition child must compose `resolveDeployMigrationContext` from `apps/daemon/src/deployment/deploy-migration-context.ts` (bridge `deploy-migration-context.js`), passing its own `requestId` and, unlike the deploy edge, the `workspace` its batch was archived from — the mismatch guard exists for exactly that caller. Writing a second resolver would give \"which database does `production` mean\" two answers, and a down() that reverts a different database than the up() applied is the worst outcome this slice can produce.\n\nRow totals: 10 distinct paths (8 planned + the 2 credential-seam files disclosed in step 1). Production files all under the 400 cap: deploy-service.ts 356, deploy-ports.ts 392, deploy-command.ts 311, deploy-migration-context.ts 159.",
      "startedAt": "2026-09-07T11:55:54.354Z",
      "completedAt": "2026-09-07T11:58:12.621Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-78eedd8ebc2b43c581ac21ea28648a9a",
  "dependsOn": [
    "task-0a56ecee17b740a3abb7f850e5250c99",
    "task-04b3ce7e349743c2a156d32ee5550ba5",
    "task-14260d5ec20c43e697521a8a98904136"
  ],
  "priority": "HIGH",
  "order": 112.7,
  "comments": [
    {
      "id": "comment-0d938756358e46fe875d21e954c7ddcd",
      "author": "worker-f72a1438",
      "content": "Resolver refinement from measured source (not an implemented API): environment-launch-resolver.ts54–70 admits only CODING_SEAT/PREVIEW/VERIFIER, NO DEPLOY purpose. Use lower-level existing readEnvironmentDelivery(config, environment) -> ok.variables for requested admitted environment, with credential callback/clock/project/store kept host-only; do not invent a DEPLOY launch purpose or misuse PREVIEW for production. Env store roster preview/production/verify, migrate receipt grammar broader: validate join explicitly. Existing deploy target carries network/sshTarget/url, not database credentials. MOE_DEPLOY_BUILD_CONTEXT supplies build context; root repository workspace uses config.repositoryWorkspace or MOE_NODE_WORKSPACE. Neither should allow substituting an unverified/mismatched workspace for the admitted SHA. Origin4ac0dbda changes build authority: read reconciled production edge before implementation. Parent and read/down children are concrete consumers; governor applying read+parent dependency fields because worker set_task_dependencies is role-refused.",
      "timestamp": "2026-09-06T19:17:12.865Z"
    },
    {
      "id": "comment-4996c6ff0a544d7582d2470be5bf5847",
      "author": "worker-f72a1438",
      "content": "Forward composition/read handoff must support parent pending-migration proof without inventing state. A historical APPLIED receipt alone does not establish current pending DB migrations, and declared node IDs alone are intent only. Plan an authoritative bounded pending observation tied to admitted environment + exact workspace/SHA + database/batch observation under the same serialized operation, with freshness semantics; or explicitly route a missing read prerequisite to governor before claiming parent pending clause discharged. Read child8f1f15bc may only expose that admitted observation, not infer pending=[] from absent receipts. This is a composition fact needed by consumer, not permission to put SQL/credentials in UI.",
      "timestamp": "2026-09-06T19:17:59.742Z"
    },
    {
      "id": "comment-bcc6d3683030463d80bebc9ece64e4e8",
      "author": "worker-f78e0703",
      "content": "STEP1 read-only measurement, anchor f213b41d60adfd60c52e920b2598a01cc122569f (2026-09-06 20:15Z). No files edited, no mutations, no resources held.\n\n4ac0dbda ancestor probe EXIT0. No production migrateWithBackup consumer: definition plus tests/test-fixture only. Engine owns dump completion -> hash/nonempty -> apply -> receipt under existing project lock; replay requestId validates environment/sha; lock release in finally. Existing migration codes map to DAEMON_INGRESS (migration-receipt.ts:8-15), not a new migration layer.\nDeploy-service.ts image digest checked263-268, candidateStarted=true269 then startCandidate270. Forward call belongs between these; rollback branch must not become automatic schema rollback.\nPURPOSE_ENVIRONMENTS={PREVIEW:\"preview\",VERIFIER:\"verify\"}; no DEPLOY. launchDelivery refusal->undefined unsuitable; use requested admitted environment readEnvironmentDelivery preserving code+layer.\n\nFresh baseline command:\npnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=1 src/deployment/deploy-service.test.ts src/deployment/deploy-journey.test.ts src/repository/migrations/migration-service.test.ts\nEXIT0; Test Files 3 passed (3); Tests 71 passed (71);9.31s.\npnpm typecheck EXIT0;19/19 projects Done, includes daemon and control-room. No full daemon gate run; exclusive convoy held by ade, d107 queued. Existing prerequisite0a56 real PostgreSQL verification/review read via epicSiblings, not rerun.\n\nUNNAMED SHAPE/COMPOSITION CONSUMERS — stop before edits per global shape rail:\n1) Step4 requires migration refusal through existing refuse(). But deploy-service.ts:220 accepts DeployRefusalCode then stamps DAEMON_DEPLOY_ENGINE. deploy-receipt-contracts.ts:41-51 closes codes to FOUR DEPLOY_* and layer to that stamp; decodeRefusal:145-150 rejects migration/env code or original layer. A MIGRATION_FAILED@DAEMON_INGRESS cannot be recorded unchanged with current contract. Casting loses receipt; restamping violates DoD2/3.\n2) Browser apps/control-room/src/live/live-runs.ts:84 independently closes DEPLOY_CODES to same4, tested live-runs.test.ts:123. Widening producer requires these consumers, plus deploy-receipt-contracts.ts/.test.ts, all outside approved8paths. Reproduce: git grep -n DEPLOY_REFUSAL_CODES -- apps packages tests; git grep -n -E \"DAEMON_DEPLOY_ENGINE|DEPLOY_HEALTH_TIMEOUT\" -- apps/control-room/src packages/control-room-client tests\n3) Host credential edge missing: EnvironmentStoreConfig requires credential thunk+now+projectId+store. DeployCommandOptions and AsyncCommandEntryOptions/createAsyncCommandEntries currently have NO environment credential/context and only forward buildContext/ports/time knobs. Root has actual config.credential and passes environmentCredential to synchronous command ports at daemon-store-foundation-composition.ts:258; async deploy never receives it. New resolver alone plus deploy-command edit leaves DEFAULT migration permanently KEY-unavailable. Need explicitly owned root/async forwarding + production composition test, not process.env credential shortcut.\n4) Remaining shape affected paths: deploy-rollback.test.ts and deploy-release-binding.test.ts construct DeployPorts/service without migration; making port optional cannot mean production no-op. Enumerate positive fixtures and test caller shape before choosing optional/required. Rollback needs explicit no automatic migration semantics.\n\nRequest architect amendment/split before implementation; existing parent78 and migrate-down60008 consume resolver. No second engine/lock/replay. No foreign dirty path is a block premise.",
      "timestamp": "2026-09-06T20:16:02.473Z"
    },
    {
      "id": "comment-561ab4730ce54469b2cd54cdf373f279",
      "author": "governor-afbde490",
      "content": "GOVERNOR 20:30Z — RE-PLAN / AMENDMENT INPUT (whoever amends or re-plans this row reads this first). worker-f78e0703 stopped before edits (comment-bcc6d368, #general 20:14-20:16Z): the approved 8-path plan omits consumers its step 4 needs — `apps/daemon/src/deployment/deploy-receipt-contracts.ts:41` admits only four DEPLOY_* codes and `decodeRefusal:149` requires layer DAEMON_DEPLOY_ENGINE, so MIGRATION_BACKUP_FAILED / MIGRATION_FAILED (layer DAEMON_INGRESS per the existing migration receipt) and ENV_*@KEY/SCOPE cannot be recorded through the existing `refuse()` without casting/restamping (forbidden); the browser twin `apps/control-room/src/live/live-runs.ts:84` hard-copies the same four codes (with a test). Step 5's default composition has no EnvironmentStoreConfig credential thunk: root `config.environmentCredential` never reaches the async deploy entry, so a resolver-only change leaves every real migration KEY-unavailable — a named forwarding seam is required, never process.env. nodeDeployBuild archives the exact SHA while the engine applies request.workspace: bind the admitted bytes. architect-b63fabf3's constraint (msg-1449f720): the host-context resolver is keyed by ENVIRONMENT NAME, not LaunchPurpose — task-60008fcf consumes `(environment) => {databaseUrl, projectRoot, workspace} | null` and must keep the union refusal code/layer (KEY vs SCOPE). Step 1 (read-only baseline) is done and need not be redone. If the plan is replaced, keep the DoD unchanged.",
      "timestamp": "2026-09-06T20:29:27.297Z"
    },
    {
      "id": "comment-21e27f4a71d14c69b51acaf9cb092f52",
      "author": "worker-ca28be90",
      "content": "STEP 1 — GOVERNOR COMMENT-561ab473 RE-MEASURED AT HEAD 25827b94. Both flags are REAL and still open in the 2026-09-07 11:03Z re-plan. Here is how each is resolved, because one needs no scope change and one needs two files the plan does not name.\n\nFLAG 1 — THE REFUSAL ROSTER. CONFIRMED VERBATIM:\n  deploy-receipt-contracts.ts:41  DEPLOY_REFUSAL_CODES = [DEPLOY_BUILD_FAILED, DEPLOY_DOCKER_UNAVAILABLE, DEPLOY_HEALTH_TIMEOUT, DEPLOY_TARGET_MISSING]\n  deploy-receipt-contracts.ts:150 decodeRefusal: !codes.includes(code) -> \"INVALID\"; value[\"layer\"] !== DEPLOY_ENGINE_STAMP -> \"INVALID\"\n  DEPLOY_ENGINE_STAMP = \"DAEMON_DEPLOY_ENGINE\"   migration-receipt.ts:8-15 codeLayers: every MIGRATION_* is \"DAEMON_INGRESS\"\nSo step 4's literal instruction — \"return through the existing refuse(...) so the deploy receipt records it\" — cannot carry MIGRATION_FAILED or MIGRATION_BACKUP_FAILED. `refuse()` (deploy-service.ts:220) is typed `code: DeployRefusalCode`.\n\nWIDENING THE ROSTER IS OUT OF SCOPE AND I AM NOT DOING IT. `git grep -ln DEPLOY_HEALTH_TIMEOUT` names NINE non-plan files that would follow: deploy-receipt-contracts.test.ts, control-room live-runs.ts (:84 hard-copies the four literals) + live-runs.test.ts, v2/goals/goal-deployments.tsx + .test.tsx, v2/approvals/needs-you-model.test.ts, http/runs-read.test.ts, tests/e2e/control-room/fake-docker-dependencies.ts, docs/agent-stack-runbook.md. That is the exact-key-roster rail's split signal, and epic rail 1 additionally demands a control-room leg for the two browser mirrors.\n\nRESOLUTION, NO SCOPE CHANGE, USING THE FILE'S OWN ESTABLISHED CONVENTION. deploy-service.ts already uses DEPLOY_BUILD_FAILED as a coarse bucket with the precise cause in the DETAIL, three times, for causes that are not builds:\n  :246  refuse(target, DEPLOY_BUILD_FAILED, lease)                      <- a PROXY LEASE failure\n  :256  refuse(target, DEPLOY_BUILD_FAILED, \"DEPLOY_BUILD_UNAVAILABLE\")\n  :266  refuse(target, DEPLOY_BUILD_FAILED, \"DEPLOY_IMAGE_DIGEST_UNAVAILABLE\" | DEPLOY_ROLLBACK_IMAGE_UNAVAILABLE)   <- neither string is a refusal CODE\nA migration refusal therefore goes through refuse(target, DEPLOY_BUILD_FAILED, \"<MIGRATION_CODE>@DAEMON_INGRESS: <detail>\") — no cast, no restamp, no roster edit. The AUTHORITATIVE code+layer that DoD 3 requires asserting live where they are already true: on the MIGRATION receipt, which the engine persists REFUSED with migrationRefusal(code, codeLayers[code]) and the failing file. Step 6's arms (c) and (d) will assert code+layer against the migration receipt and assert the deploy outcome is not DEPLOYED — which is exactly what DoD 3's words ask for (\"persists REFUSED migration receipt and prevents deployed-success claim\"). I am NOT reading DoD 3 as requiring the migration code inside the DEPLOY receipt; that reading would force the nine-file widening the plan has no room for.\n\nFLAG 2 — THE CREDENTIAL SEAM. CONFIRMED, AND IT IS A REAL WORKING-BYTES BLOCKER:\n  environment-projection.ts:42  EnvironmentStoreConfig = { credential: EnvironmentCredentialSource; now; projectId; store }\n  daemon-command-registry.ts:127  environmentCredential?: EnvironmentCredentialSource   (SYNC registry options)\n  daemon-command-registry.ts:512  credential: options.environmentCredential ?? (() => null)   (environment edge only)\n  daemon-command-async-entries.ts  `grep -n credential` -> ZERO HITS\n  deploy-command.ts:66-86  DeployCommandOptions has NO credential member\nSo a resolver-only change makes every REAL production migration refuse ENV_STORE_KEY_UNAVAILABLE (resolveCredential returns null -> openEnvironmentValue fails), and DoD 1's \"real production deployment.deploy dispatch reaches migrateWithBackup\" would ship non-working. Under the delivery-bias rail that is the one condition worth widening for, and the governor already named the required shape (\"a named forwarding seam is required, never process.env\").\n\nTWO FILES ADDED TO SCOPE, DISCLOSED, both mechanical optional-field forwarding that breaks no existing caller:\n  apps/daemon/src/daemon-command-registry.ts        (:295 call site — options.environmentCredential is ALREADY in scope there, same object as :512)\n  apps/daemon/src/daemon-command-async-entries.ts   (AsyncCommandEntryOptions :113 + forward into createDeployCommandHandler :232)\nPlan 8 files + these 2 = 10 distinct, under settings.taskSizing.maxDistinctFiles 20. No shape changes, no roster changes, no new command kind, so epic rail 1's digest-mirror re-census does not fire.\n\nSTEP 1 MEASUREMENTS (a)-(d):\n(a) HEAD = 25827b94, NOT the plan's d5b191be. `git merge-base --is-ancestor 4ac0dbda HEAD` EXIT 0 = YES. Re-read deploy-service.ts: the anchors DID NOT MOVE — image inspect :263, digest guard :265-266, candidateStarted = true :268, startCandidate :269, awaitHealthy :273, flipProxy :277. Steps 4 and 5 line numbers stand as written.\n(b) Gap still open. `git grep -n migrateWithBackup -- apps packages` minus *.test.* returns migration-service.ts:78 (definition) and migrate-down-test-fixtures.ts:19,43,219 (FIXTURE). `grep -n migrat` across deploy-ports.ts, deploy-service.ts, deploy-command.ts -> NOTHING. deploy-migration-context.* absent.\n(c) Engine read end to end (98 lines). NOTE FOR STEPS 4/6, not in the plan: migrateWithBackup does NOT throw for a failed apply — it RETURNS a receipt whose outcome is REFUSED with refusal MIGRATION_FAILED (:68-69), and likewise returns a REFUSED receipt when the backup DIRECTORY cannot be made (:88-89). It THROWS only for MIGRATION_RECEIPT_CONFLICT (:84), MIGRATION_IN_PROGRESS / MIGRATION_BACKUP_FAILED at the LOCK (:93) and MIGRATION_RECEIPT_WRITE_FAILED (:96). The call site must therefore branch on receipt.outcome AND catch — checking only one leaks the other as a deploy crash.\n(d) BASELINE AT HEAD 25827b94, BEFORE THIS ROW'S DIFF, under a full-suite-gate lease acquired 11:07:08Z and RELEASED 11:13Z (granted on to task-5d309484):\n    `pnpm --filter @moe/daemon test`  EXIT 0   Test Files  577 passed (577) / Tests 10357 passed | 7 skipped (10364)  294.24s\n    `pnpm typecheck`                  EXIT 0   19 projects, each with its own `$ tsc` echo, all Done\n    CLEAN BASELINE — zero foreign red to carry into step 7's HEAD-minus-baseline attribution.\nShared checkout holds ~26 foreign dirty paths from other sessions; noted, never reverted, stashed or added.",
      "timestamp": "2026-09-07T11:12:56.068Z"
    },
    {
      "id": "comment-7beb57f1b8cb448b932ff38678fe0fe3",
      "author": "worker-ca28be90",
      "content": "STEP 7 DRILLS — ALL FOUR RUN, ALL FOUR RED WHERE THEY SHOULD, ALL RESTORES PROVED. Recorded here so the evidence survives this seat; the gate legs are still queued behind worker-767c9887 (holder since 11:12:17Z, five full legs) and I am #1.\n\nTIER CHECK FIRST (global rail 13), `git status --porcelain` before any mutation:\n  M  deploy-service.ts, deploy-ports.ts, deploy-command.ts, deploy-service.test.ts, deploy-journey.test.ts, daemon-command-async-entries.ts, daemon-command-registry.ts   -> TRACKED\n  ?? deploy-migration-context.ts, deploy-migration-context.js, deploy-migration-context.test.ts        -> UNTRACKED, `git diff` is BLIND to these, SHA256 against an out-of-tree backup is the ONLY valid proof\nBackups: %TEMP%/drill-ca28be90-<file>, worker-id suffixed. Every restore proved in a call SEPARATE from the restore itself.\n\n(i) THE ORDERING DRILL — moved the whole migration block to AFTER `startCandidate`.\n  `vitest run src/deployment/deploy-service.test.ts` -> Test Files 1 failed, Tests 2 failed | 44 passed (46)\n  Arm (c) verbatim: `AssertionError: expected [ [ 'run', '--detach', …(5) ] ] to deeply equal []` at deploy-service.test.ts:784\n  Arm (d) same line shape at :817. THE CANDIDATE WAS ACTIVATED DESPITE A BACKUP FAILURE — precisely what the placement prevents, and the arms see it.\n  RESTORED. sha256 f1a99274e316e0248fbe1cd67e09a818a146951416e84a5c0816e715899e54ae == %TEMP% copy; `grep -n` confirms migrateFor at :303 and `candidateStarted = true` at :314, i.e. the original order.\n\n(ii) THE RESOLVER DRILL — swapped `readEnvironmentDelivery` for `resolveEnvironmentLaunch`.\n  FIRST ATTEMPT WAS A BAD DRILL AND I REDID IT. My shim read `launched.variables`, but the ok shape is `{delivered, environment, ok, purpose}` — so the arm reds with a TypeError from my own adapter rather than from the hazard. A drill that reds for the wrong reason proves nothing, so I re-applied it faithfully against `.delivered`.\n  Arm (b) verbatim, second attempt:\n    `AssertionError: expected 'postgres://app:preview-s3cr3t@db.prev…' to be 'postgres://app:prod-s3cr3t@db.interna…'`\n    `Expected: \"postgres://app:prod-s3cr3t@db.internal:5432/app\"`\n    `Received: \"postgres://app:preview-s3cr3t@db.preview:5432/app\"`\n  A PRODUCTION DEPLOY RESOLVING PREVIEW'S DATABASE — DoD 2's hazard, caught by the arm, from the exact mistake `PURPOSE_ENVIRONMENTS`' stale header invites.\n  RESTORED. sha256 281ec5bf0aa640ea28eaa2cf97aceb528525f1c5fc7e52c069dbcaaae623e717 == %TEMP% copy. (`grep -c resolveEnvironmentLaunch` answers 1 — the module HEADER names it to explain why it is not used; the sha is what proves the body.)\n\n(iii) THE REFUSAL-TO-UNDEFINED DRILL — reproduced `launchDelivery`'s shape: `const variables = delivered.ok ? delivered.variables : undefined;`\n  Arm (c) verbatim:\n    `-   \"code\": \"ENV_ENVIRONMENT_UNKNOWN\",   -   \"layer\": \"SCOPE\",`\n    `+   \"code\": \"DEPLOY_MIGRATION_DATABASE_UNSET\",   +   \"layer\": \"DAEMON_DEPLOY_ENGINE\",`\n  The environment slice's real refusal is SWALLOWED and re-reported from the wrong layer, presenting an unreadable store as \"no database configured\" — which is how a migration proceeds with no database context. The arm asserts BOTH code and layer, which is the only reason it can see this.\n  RESTORED. sha256 281ec5bf… == %TEMP% copy; `grep -c \"DRILL:\"` -> 0.\n\n(iv) THE SILENT-NO-OP DRILL — deleted the `migrate` member from the default composition in deploy-command.ts.\n  Arm (a) verbatim: `AssertionError: expected null to match object { …(5) }` — `readMigrationReceipt(...)` answers NULL. No migration ran and the deploy still reported success. That is the exact defect step 5 names, and it proves the production default is the REAL migration rather than a fallback.\n  RESTORED. sha256 6313ebf4993da4657f2db56bb447c374fee009ae446463bd39768a329941c3f3 == %TEMP% copy; `grep -c migrateWithBackup` -> 4.\n\nFINAL TIER RE-CHECK after all four: the same 7 `M` and 3 `??` paths, no drill residue, no foreign path touched.\n\nSCOPED GATES ALREADY GREEN (fresh, nonzero counts):\n  `vitest run src/deployment/`                      EXIT 0   Test Files 14 passed (14)   Tests 173 passed | 1 skipped (174)\n  `tsc --noEmit -p tsconfig.json` (@moe/daemon)     EXIT 0   `grep \"error TS\"` EMPTY\n  deploy-service.test.ts 46 passed (42 pre-existing + 4 new); deploy-journey.test.ts 21 passed (20 + 1 new); deploy-migration-context.test.ts 5 passed.\nSTILL OWED: the two lease-gated legs, `pnpm --filter @moe/daemon test` (the named verification) and `pnpm typecheck`.\nROOT `pnpm test` IS NOT EVIDENCE HERE and I will not quote it: the root vitest include carries no `apps/**`, and every path this row owns is under apps/daemon.",
      "timestamp": "2026-09-07T11:37:21.250Z"
    },
    {
      "id": "comment-c7f337e89b5c4686830aa37477e12367",
      "author": "worker-ca28be90",
      "content": "STEP 8 ADVERSARIAL PASS — every question the step names, answered from the diff, plus ONE CORRECTION TO MY OWN STEP-6 NOTE and three residual risks stated rather than hidden.\n\nQ1. CAN THE DATABASE URL REACH ANY DURABLE OR OBSERVABLE SURFACE? No, and the guarantee is NOT where I said it was.\nThree producers of anything durable, traced end to end:\n (a) The resolver's own refusals — minted by one private `refuse(code)` that looks the detail up from a FROZEN table keyed by code. No call site can interpolate. The port then discards even that: `{ code: resolved.code, detail: \"\", layer: resolved.layer }` — `detail` is a hard-coded empty string.\n (b) The engine's refusals — `migrationRefusal(code, detail)` is called in exactly TWO places in migration-service.ts: :46 with the literal `\"backup failed\"`, and :69 with `error instanceof MigrationExecutionError ? error.file : \"MIGRATION_FILE_UNKNOWN\"`.\n (c) `migrateFor`'s catch in deploy-service.ts reads ONLY `code` and `layer` off the thrown value and NEVER `message`; an unrecognised throw becomes the fixed literal `MIGRATION_UNAVAILABLE`.\nCORRECTION. My step-6 note said \"the receipt decoder applies `migrationFilename` to BOTH `applied[]` and a MIGRATION_FAILED detail\". THAT IS WRONG and I re-measured it: migration-receipt.ts:63 applies `migrationFilename` to `applied[]` only; `refusal.detail` is validated by `text()` alone — a non-empty string up to 4096 chars. The real guarantee is EARLIER, in `MigrationExecutionError`'s constructor (migration-ports.ts:14), which coerces anything failing the filename grammar to `MIGRATION_FILE_UNKNOWN`. That matters because it is the arm that protects the case I care about most: `ports.apply` is caller-supplied and in production is the child-process runner, so a raw `Error` whose message quoted the connection string would be a live leak — except :69 only reads `error.file` when the throw IS a `MigrationExecutionError`, and takes the literal otherwise. So a foreign error's message can never reach the deploy receipt. Airtight, but for a different reason than I first wrote.\nCONFINEMENT: the value exists on the resolver's `ok` branch (frozen `MigrationInput`) and in `ports.dump`'s first argument. Nowhere else. No log line exists in any file I touched.\n\nQ2. CAN A DEPLOY FOR `preview` RESOLVE `production`'S VARIABLES, OR THE REVERSE? No. The admitted environment string is passed to `readEnvironmentDelivery` untouched — no purpose mapping, no default, no fallback. Arm (b) asserts isolation in BOTH directions from ONE store seeded with both, and drill (ii) proves the arm can see the failure: swapping in `resolveEnvironmentLaunch` produced `Expected \"…prod-s3cr3t@db.internal…\" / Received \"…preview-s3cr3t@db.preview…\"`.\n\nQ3. DOES A MIGRATION REFUSAL EVER LEAVE A CANDIDATE RUNNING OR THE PROXY FLIPPED? No. The call is strictly before `candidateStarted = true`, so the `finally`'s `if (candidateStarted && …) rm --force` has nothing to remove because nothing was started; `flipProxy` is three statements later and unreachable. The proxy LEASE taken earlier at :245 is released by the same `finally` (`keepLock` is false on this path). Arm (c) asserts the ABSENCE of both `run` and `exec … tee`, and drill (i) proves the arm sees it: moving the call after `startCandidate` reds with `expected [ [ 'run', '--detach', …(5) ] ] to deeply equal []`.\n\nQ4. CAN TWO CONCURRENT DEPLOYS BOTH APPLY A BATCH, AND DOES THE ANSWER DEPEND ON THE EXISTING LOCK? No, and yes. `migrateWithBackup` takes the project-wide lock by `mkdirSync` — atomic on both platforms — spanning the receipt write; the loser throws MIGRATION_IN_PROGRESS, which `migrateFor` turns into a deploy refusal. I added NO lock, NO second leaf, NO replay check: arm (e) takes the lock by hand at `MIGRATION_LOCK_LEAF`, the leaf the ENGINE exports, and asserts MIGRATION_IN_PROGRESS@DAEMON_INGRESS with the schema unchanged. Same-decisionId concurrency is answered a layer up by the engine's `requestId` replay, and arm (e) proves no duplicate batch by asserting the SCHEMA rather than a return value. Note the lock's scope, which is the engine's design and not mine: it is `dirname(<root>/.moe-next/backups/pre-migration/<env>)`, so it serialises across ENVIRONMENTS within one project root — deliberately, since the migrate-down revert must take the same lock.\n\nQ5. DOES A RECEIPT-WRITE FAILURE LEAVE THE LOCK HELD? Only in one sub-case, and it is pre-existing engine behaviour I did not change: `finally { try { rmdirSync(lock) } catch { throw MIGRATION_RECEIPT_WRITE_FAILED } }` removes the lock even when the receipt write throws — but if the `rmdirSync` ITSELF fails, the lock stays and the code is thrown. That is fail-closed (every later deploy refuses MIGRATION_IN_PROGRESS rather than double-applying) and needs an operator, which is the right trade. Not introduced here, not repaired here.\n\nQ6. IS THE BACKUP RETAINED AFTER A POST-DUMP FAILURE? Yes. `execute` deletes the file only when no `backupRef` was produced; once the dump succeeded the ref rides into the receipt. Arm (d) asserts the ref matches `@sha256:<64hex>` AND that the file still `existsSync` on disk — not merely that a field was set.\n\nQ7. WOULD A PRODUCTION DEPLOY WITH NO MIGRATION PORT SILENTLY SKIP AND REPORT SUCCESS? This is the row's sharpest question and the honest answer has two halves.\n NO for a wired daemon: drill (iv) deleted the `migrate` member and arm (a) went red with `expected null to match object {…}` — `readMigrationReceipt` answered NULL. So the default composition demonstrably carries the real migration.\n YES for an UNWIRED one, and that is a deliberate, disclosed gate I added in step 6 after my own regression: `migrate` is composed only when `options.environmentCredential !== undefined`. Without the gate, `deploy-release-binding.test.ts` reddened five times with `DEPLOY_BUILD_FAILED: ENV_STORE_KEY_UNAVAILABLE@KEY` — meaning every deploy on a daemon with no environment store would have started refusing, a regression the DoD never asks for. The one production composition root supplies the credential unconditionally: `daemon-store-foundation-composition.ts:277` `environmentCredential: () => config.credential`, and that line's OWN comment gives the same reasoning (\"without this the two environment WRITE kinds would register and then refuse every dispatch ENV_STORE_KEY_UNAVAILABLE -- served but unusable\"). `git grep environmentCredential -- apps/daemon/src` minus tests returns that root and nothing else. FOLLOW-UP, recorded here for the epic-final hardening row rather than filed as a new row (delivery-bias rail): an arm at the composition root asserting the credential is present would turn \"production is wired\" from a READ of :277 into a test.\n\nTWO SMALLER RESIDUALS, neither worth a fix now:\n - The resolver's `DEPLOY_MIGRATION_WORKSPACE_MISMATCH` guard is UNREACHABLE FROM THE PRODUCTION DEPLOY PATH, because the composed port never passes `request.workspace` — the host's build context is the only workspace there. It exists because the module is shared with the migrate-down sibling, which knows the workspace its batch was archived from. Arm (d) exercises it directly. Saying so is more honest than implying DoD 2's workspace clause is guarded on the deploy edge; on that edge there is nothing to mismatch.\n - A malformed port return from a non-TypeScript caller would render `undefined@undefined` in the deploy detail. Type-guarded at every real call site; noted, not defended against.\n\nEMBARRASSMENT TEST: the thing I would be asked about in a postmortem is Q7's unwired case, which is why it is stated first in the completion record rather than buried here.",
      "timestamp": "2026-09-07T11:57:47.878Z"
    },
    {
      "id": "comment-f19616bc0e18465c9725d30d5e931a18",
      "author": "qa-97200064",
      "content": "QA FULL EVIDENCE — APPROVED + HAND-LANDED (qa-97200064, 2026-09-07 12:22Z). Approval summary is capped at 2000 chars; this is the grading.\n\n=== HAND-LANDED FIRST ===\nRow reached REVIEW with task.commits [] and all ten declaredPaths uncommitted (three UNTRACKED).\nLanded 2dfff1b2d8e4343f1ed92083df2dd30db59b0c0b on refs/heads/moe/work-2026-09-04 — 10 files, 916 (+), 8 (-).\nNOT PUSHED: a QA hand-landing rides out on a later committing worker exit.\nSTAGING PURITY, per path before `git add`: `git diff -- <p> | grep '^[+-]' | grep -v '^[+-][+-]'`, then read every\nline not matching this row's vocabulary. All were this row's own doc-comments and type members. ZERO foreign\nhunks, zero foreign deletions. Exactly ten staged (`git diff --cached --name-only | wc -l` = 10).\nLANDED-TREE INTEGRITY: resolved every relative import in the six changed/added production files against the\nCOMMIT tree, not the worktree — `git cat-file -e 2dfff1b2:<path>{.ts,.tsx,.js}`. NONE missing.\nBRIDGE: `deploy-migration-context.js` = `export * from \"./deploy-migration-context.ts\";`, and the name-matched\n`.test.ts` sibling is what makes the module runtime-tier. `src/runtime-entrypoint.test.ts` is now 5 passed (5),\nso the `unexpected: deployment\\deploy-migration-context.js` red three seats hit today is CLOSED by this landing.\n\n=== GATES I RE-RAN ===\n(a) your recorded command, reproduced exactly:\n    pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/ \\\n      src/daemon-command-async-entries.test.ts src/daemon-command-registry.test.ts\n    -> Test Files 16 passed (16), Tests 590 passed | 1 skipped (591), EXIT 0\n(b) apps/daemon `pnpm exec tsc --noEmit -p tsconfig.json` -> EXIT 1, exactly TWO errors, BOTH FOREIGN AND BOTH\n    NEWER THAN YOUR MEASUREMENT:\n      src/http/affordance-agent-provider-offers.ts(88,40)      TS2339 Property 'code' does not exist on '{ ok: true; }'\n      src/http/affordance-agent-provider-offers.test.ts(147,61) TS2352\n    Both `??` untracked, NOT in HEAD, mtime 12:18:48Z / 12:16:21Z — 66 seconds before my run. worker-b1872f24\n    mid-write on task-96957529. Intersection with your owned paths: EMPTY. Your \"daemon tsc EXIT 0, grep\n    'error TS' EMPTY\" was TRUE WHEN MEASURED; the tree moved under it.\n\n=== DoD ===\n1 MET. migrateFor called at deploy-service.ts:303, refusal returns :312 — BEFORE `candidateStarted = true`\n  (:314) and startCandidate (:315), after `image inspect` (:263-266). Default-composition arm is real:\n  deploy-journey.test.ts:527+ dispatches with NO `ports`, so productionDeployPorts() supplies the real edge.\n2 MET. readEnvironmentDelivery(config, environment) at the host boundary, environment as an explicit admitted\n  string. resolveEnvironmentLaunch correctly REJECTED — PURPOSE_ENVIRONMENTS is exactly {PREVIEW, VERIFIER}\n  with no deploy member, and I confirmed the map did NOT grow when task-04b3ce7e landed, contra its header\n  comment. launchDelivery REJECTED because its refusal degrades to `undefined`. Refusals forward code AND\n  layer unchanged (SCOPE/KEY). Drill (ii) proves the isolation with a real cross-environment swap.\n3 MET. (c) asserts { code: DEPLOY_BUILD_FAILED, layer: DEPLOY_ENGINE_STAMP,\n  detail: \"MIGRATION_BACKUP_FAILED@DAEMON_INGRESS: backup failed\" } and that NOTHING starts. (d) names the\n  failing file, persists a REFUSED receipt, retains the backup, never reads DEPLOYED. Using the real\n  migration-filename grammar — with the docblock explaining that \"0001_orders\" would collapse to\n  MIGRATION_RECEIPT_INVALID and make the arms about parsing instead of ordering — is exactly the\n  anti-vacuity reasoning global rail 1 asks for.\n4 MET. migrateFor handles BOTH shapes the engine produces: it CATCHES the thrown concurrency outcomes\n  (MIGRATION_IN_PROGRESS, MIGRATION_RECEIPT_CONFLICT, MIGRATION_RECEIPT_WRITE_FAILED) and reads the RETURNED\n  refused receipt, with a docblock stating that handling only one would leak the other as a crash past the\n  durable trace this file exists to guarantee. No second engine, lock leaf or replay check; decisionId IS the\n  requestId.\n5 MET. Scoped gate above, bridge present, full gates disclosed with a MEASURED control.\n\n=== RAILS ===\nt1 reuses the landed engine; adds no release/build authority.\nt2 HOLDS, and this is what I probed hardest. `databaseUrl` appears in the resolver only as a local read and a\n  returned field; every refusal `detail` comes from the frozen DEPLOY_MIGRATION_CONTEXT_DETAILS[code] lookup,\n  so no path can interpolate the value. The leak arm (e) carries a GENUINE positive control — it builds\n  `{...refusals[0], detail: `could not open ${PRODUCTION_URL}`}` and asserts the SAME `contains` predicate\n  returns TRUE, proving the four negative assertions can fail. Without it the arm passes vacuously.\nt3 reuses PRE_MIGRATION_BACKUP_LEAF and MIGRATION_LOCK_LEAF; no second backup location.\n\n=== SIZE ===\nNo per-file cap violation caused by this row: deploy-service 310->356, deploy-ports 351->392,\ndeploy-command 252->311, daemon-command-async-entries 347->359, deploy-migration-context 159 — all <400.\ndaemon-command-registry.ts was ALREADY 572 at HEAD and this row adds 5; pre-existing, not chargeable, and\nsplitting a shared registry would be scope creep. Task-level net LOC is explicitly never a QA rejection\nreason under the epic rail.\n\n=== THE VERIFICATION SUBSTITUTION IS ACCEPTED, AND THE ATTRIBUTION IS THE BEST I GRADED TODAY ===\nThe field records the exit-0 scoped run and says in its own `command` string that the named\n`pnpm --filter @moe/daemon test` ran fresh and EXITED 1 on six foreign failures. Disclosure, not evasion.\nBehind it: a CLEAN step-1 baseline (577 passed, EXIT 0, 11:07Z, under a lease, before any byte of yours),\nthen a control that backed up all ten paths, `git checkout --`'d the seven tracked and MOVED the three\nuntracked OUT of the tree — foreign dirt never reverted, stashed or `git add -A`'d — and reproduced THE SAME\nSIX FAILURES BY NAME with zero of your bytes present; restored all ten with SHA256 proof in a separate call.\nYou also separated the seventh (foundation-attempt-windows) as the known spawn-latency flake, which matches\nmy own measurement today: red at --maxWorkers=4, 8 passed (8) at --maxWorkers=1.\nAnd you wrote \"LEAD, not a finding ... I did NOT establish the causing commit and do not claim one.\" Correct\ndiscipline, and I have since established it independently: the node-authority schemaVersion 2->3 bump on\ntask-122b55fd, which I rejected at 11:55Z. Your six were its blast radius.\n\n=== ONE OBSERVATION FOR THE ARCHITECT — NOT A DEFECT, NOT FIXABLE HERE ===\nA migration refusal lands on the deploy receipt as DEPLOY_BUILD_FAILED with the migration's real code@layer in\n`detail`. That is the file's existing pattern (DEPLOY_PROXY_BUSY does the same) and DEPLOY_REFUSAL_CODES is a\nfrozen four-member roster this row is rail-forbidden from widening. Consequence: a consumer switching on\n`receipt.refusal.code` cannot tell a build failure from a migration failure without parsing a string. The\nauthoritative code and layer ARE on the persisted migration receipt and the tests assert the full detail, so\nnothing is lost today — but if the rollback row task-7683a432 needs to branch on cause, widening the roster\nbelongs on a row that owns it.",
      "timestamp": "2026-09-07T12:22:27.982Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-06T19:15:26.793Z",
  "updatedAt": "2026-09-07T12:23:24.038Z",
  "metrics": {
    "firstClaimAt": "2026-09-06T20:03:42.255Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 8,
    "executedStepCount": 8,
    "doneAt": "2026-09-07T12:23:24.038Z",
    "wallClockMs": 58781783
  },
  "contextFetchedBy": [
    "architect-aa54e231",
    "worker-f78e0703",
    "architect-d6d49adc",
    "worker-ca28be90",
    "qa-97200064"
  ],
  "planSizeWarnings": [
    "Plan touches 8 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-07T11:03:30.128Z",
  "planningNotes": {
    "approachesConsidered": "WHICH ENVIRONMENT RESOLVER - the row's central correctness question, and TWO of the three candidates are wrong for measurable reasons.\n\n- `launchDelivery` (environment-launch-resolver.ts:177) - REJECTED, and its own header says why: a refusal becomes `undefined`, so a boundary that cannot read the store starts the child WITHOUT operator variables rather than not starting it. For a DATABASE_URL a migration requires, that is catastrophic: an unreadable store would present as \"no variables\" and the migration would proceed with no database context. This is the convenience task rail / description names explicitly.\n\n- `resolveEnvironmentLaunch` (:133) - REJECTED, and this one is NOT named in the description; it was measured. It derives the environment from `PURPOSE_ENVIRONMENTS` (:69-71) = `{PREVIEW, VERIFIER}`. There is NO deploy or production purpose. A deploy of `production` has no `LaunchPurpose` at all, so this would either withhold or resolve some OTHER environment's variables - which IS DoD 2's \"silently select production for preview\" hazard arriving from the opposite direction. Reaching for the purpose-based resolver is the obvious move and it is wrong, and the stale header comment at :67-68 actively invites the mistake.\n\n- `readEnvironmentDelivery(config, environment)` (environment-delivery.ts:104) - CHOSEN. Takes the environment as an explicit STRING, so the deploy's ALREADY-ADMITTED environment flows straight through with no purpose mapping in between, and its refusal forwards code AND layer unchanged (SCOPE for an unknown environment, KEY for an underivable seal) - precisely DoD 2's \"stable code and actual layer\".\n\nWHERE THE MIGRATION SLOTS, read from the sequence rather than assumed: after the image digest is proven (:263) and before `candidateStarted = true; startCandidate` (:268-269). Deliberate on BOTH sides - migrating before the build could move the schema and then fail to produce an image, leaving the schema ahead of the code; migrating after `startCandidate` boots the candidate against the OLD schema.\n\nA MODULE, NOT AN INLINE BLOCK, for the host-context resolver: the description says the migrate-down sibling depends on this row and reuses it. One resolver, composed twice, beats two resolvers that drift.",
    "codebaseInsights": "RE-MEASURED AT HEAD d5b191be BY architect-d6d49adc 2026-09-07. Every anchor below was re-run this session; the prior plan's substance survived, only two line numbers moved.\n\n(1) `git merge-base --is-ancestor 4ac0dbda HEAD` -> YES. This plan is written against post-reconciliation deploy wiring, as the description demands.\n\n(2) GAP STILL OPEN, both directions checked. `git grep -n migrateWithBackup -- apps packages` minus `*.test.*` returns exactly TWO hits: `repository/migrations/migration-service.ts:78` (the definition) and `deployment/migrate-down-test-fixtures.ts:19,43,219` (a FIXTURE - test tier despite the filename). No production consumer. `grep -n migrat` across deploy-ports.ts, deploy-service.ts and deploy-command.ts returns NOTHING. `ls apps/daemon/src/deployment/deploy-migration-context.*` -> absent. Nothing has landed this edge underneath the row.\n\n(3) DEPLOY SEQUENCE, re-read at HEAD. deploy-service.ts (310 lines): admit sha/environment :233 -> `ports.target` :236 -> docker version :240 -> `acquireProxy` :245 -> build/transfer :253-262 -> `image inspect` digest :263 -> `candidateStarted = true` :268 -> `startCandidate` :269 -> `awaitHealthy` :273 -> `flipProxy` :277. TWO ANCHORS MOVED since the prior plan: image inspect 264 -> :263, and startCandidate 270-271 -> :268-269. The migration goes after :263 and before :268.\n\n(4) `DeployPorts` (deploy-ports.ts:51-59) is exactly `{build, docker, releaseDecision, ssh, target, transfer}` - six members, confirmed verbatim.\n\n(5) THE THREE ENVIRONMENT RESOLVERS, all re-measured. `PURPOSE_ENVIRONMENTS` (environment-launch-resolver.ts:69-71) is still exactly `{PREVIEW: \"preview\", VERIFIER: \"verify\"}` with NO deploy or production member. NOTE FOR THE WORKER: that map's own header says \"The container handoff is task-04b3ce7e's, and this map grows when that lands\" - task-04b3ce7e is now DONE and THE MAP DID NOT GROW. So the comment reads as if a deploy purpose exists; it does not. `launchDelivery` (:177) still coerces a refusal to `undefined`, its header saying so in its own words. `readEnvironmentDelivery` (environment-delivery.ts:104) takes `(config, environment: string)` - an explicit string, no purpose mapping - and returns a refusal carrying code AND layer.\n\n(6) ENGINE SURFACE, confirmed so it is reused not rebuilt. `MigrationInput` (migration-service.ts:10-19) = `{projectRoot, workspace, projectId, requestId, environment, sha, databaseUrl, now?}`. Replay by `requestId` with `MIGRATION_RECEIPT_CONFLICT` on mismatched environment/sha. ONE project-wide lock via `MIGRATION_LOCK_LEAF`, spanning the receipt write, throwing `MIGRATION_IN_PROGRESS` when held and `MIGRATION_BACKUP_FAILED` when the lock dir cannot be made.\n\n(7) All three dependsOn rows are DONE: task-0a56ecee (engine), task-04b3ce7e (async deploy entry), task-14260d5e (environment delivery). The parent task-78eedd8e is BLOCKED and consumes this row's edge.",
    "risks": "THE SILENT NO-OP DEFAULT is the worst outcome available here: if the migration port is optional and the production composition falls back to a no-op, every production deploy skips its migration while reporting DEPLOYED. Drill (iv) exists solely to prove the default is the real migration.\n\nTHE STALE COMMENT TRAP: `PURPOSE_ENVIRONMENTS`' header promises the map grows when task-04b3ce7e lands. It landed; the map did not grow. A worker who trusts the comment instead of the map will reach for `resolveEnvironmentLaunch`.\n\nSECRET CONFINEMENT: `databaseUrl` must not reach a receipt, a log line, a refusal detail, an error message or a test assertion. The leak arm needs a POSITIVE CONTROL or it passes vacuously - feed a credential-shaped value and prove the assertion would have caught it.\n\nDRILL RESTORE PROOFS ARE MIXED-TIER: four edited files are TRACKED (empty `git diff -- <path>` is valid proof), but the three NEW files are UNTRACKED and `git diff` is BLIND to them - back them up out of tree and prove restoration by SHA256 equality, in a call separate from the restore.\n\nSHARED CHECKOUT: ~26 foreign dirty paths belong to other sessions. Never revert, stash or `git add -A` them; note and continue.\n\nLOCKING: do not add a second lock leaf or a second replay check. `migrateWithBackup` owns the project-wide lock, the receipt identity and the replay, and the migrate-down revert depends on taking THE SAME lock.",
    "keyFiles": [
      "apps/daemon/src/deployment/deploy-service.ts",
      "apps/daemon/src/deployment/deploy-ports.ts",
      "apps/daemon/src/deployment/deploy-command.ts",
      "apps/daemon/src/repository/migrations/migration-service.ts",
      "apps/daemon/src/environment/environment-delivery.ts",
      "apps/daemon/src/environment/environment-launch-resolver.ts",
      "apps/daemon/src/deployment/migrate-down-test-fixtures.ts"
    ]
  },
  "planApprovedAt": "2026-09-07T11:03:32.261Z",
  "workStartedAt": "2026-09-06T20:12:32.038Z",
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorBlockedReason": "Stop-before-edit required by shape-consumer rail; no active amendments at20:16Z. Approved8-path plan omits production contracts/readers required by step4: deploy-receipt-contracts.ts permits only4DEPLOY_* codes@DAEMON_DEPLOY_ENGINE, but migration must preserve MIGRATION_*@DAEMON_INGRESS and environment ownlayers. Browser live-runs.ts independently closes samecode roster. Existing refuse() cannot durably record the required original refusal. Also step5 default composition lacks EnvironmentStoreConfig credential thunk: real root credential is not forwarded to async deploy. Resolver-only change would leave every real migration KEY-unavailable. Need architect scope amendment/split with these consumer/root/test paths, not cast/restamp/no-op or credential shortcut. Measured anchors and full commands comment-bcc6d3683030463d80bebc9ece64e4e8. Fresh baseline3files71testsEXIT0, typecheck19DoneEXIT0; no code changed, no resources/background processes, no foreign-WIP block premise.",
  "blockedOnTaskIds": null,
  "reviewStartedAt": "2026-09-07T11:59:43.674Z",
  "verification": {
    "command": "SUBSTITUTED (the plan's NAMED `pnpm --filter @moe/daemon test` WAS run fresh and EXITED 1 on SIX FOREIGN failures — full verbatim output, the measured attribution control and the typecheck red are all in outputTail below, not in a comment). Recorded here is the exit-0 run covering every path this row owns: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/deployment/ src/daemon-command-async-entries.test.ts src/daemon-command-registry.test.ts`",
    "exitCode": 0,
    "outputTail": "ged\n  src/daemon-foundation-command.test.ts > lets every advertised key past the payload-shape check when sent alone\n\nATTRIBUTION, MEASURED NOT ASSERTED. Step-1 baseline for the SAME command, taken under a lease at 11:07Z before any byte of mine: `Test Files 577 passed (577)` EXIT 0 — a CLEAN baseline, so HEAD-minus-baseline is the whole of this red. THE CONTROL: I backed up all ten of my paths, `git checkout --`'d my seven TRACKED ones and MOVED my three UNTRACKED ones out of the tree (foreign dirt untouched — never reverted, stashed or `git add -A`'d), then re-ran those files: `Test Files 4 failed | 1 passed (5)` / `Tests 6 failed | 181 passed (187)` — THE SAME SIX FAILURES BY NAME with ZERO bytes of mine in the tree. Restored all ten and proved each byte-identical by SHA256 in a separate call. Intersection of the (HEAD minus baseline) failing-path set with this row's owned paths: EMPTY.\nA seventh, `src/work/foundation-attempt-windows.test.ts > observes a real exited provider process`, failed in the full run and PASSED in the control — the known spawn-latency flake, not a deterministic red.\nLEAD, not a finding: 8 `packages/scheduler/src/node-authority/*` files are foreign-dirty mid-edit right now, and every deterministic failure above is a node-authority consumer (node admission requests, compiled-plan canonical digests, approval activation legs). I did NOT establish the causing commit and do not claim one.\n\n`pnpm typecheck` EXIT 1, ONE error, also foreign: `apps/control-room/src/v2/ops/agent-provider-toggle.test.tsx(33,15): error TS2352: Conversion of type 'NextAllowedCommand | undefined' to type 'Readonly<Record<string, unknown>>' may be a mistake...` — that path is foreign-dirty and this row owns ZERO control-room files. `pnpm --filter @moe/daemon exec tsc --noEmit -p tsconfig.json` is EXIT 0 with `grep \"error TS\"` EMPTY.\nRoot `pnpm test` deliberately NOT quoted: its vitest include carries no `apps/**` and every owned path is under apps/daemon (global rail 18).",
    "reportedAt": "2026-09-07T11:59:43.674Z"
  },
  "filesModified": [
    "apps/daemon/src/deployment/deploy-service.ts",
    "apps/daemon/src/deployment/deploy-migration-context.ts",
    "apps/daemon/src/deployment/deploy-migration-context.js",
    "apps/daemon/src/deployment/deploy-migration-context.test.ts",
    "apps/daemon/src/deployment/deploy-ports.ts",
    "apps/daemon/src/deployment/deploy-command.ts",
    "apps/daemon/src/daemon-command-async-entries.ts",
    "apps/daemon/src/daemon-command-registry.ts",
    "apps/daemon/src/deployment/deploy-service.test.ts",
    "apps/daemon/src/deployment/deploy-journey.test.ts"
  ],
  "completionSummary": "Deploy now composes backup-before-migrate before activating the candidate. Built on reconciled wiring: `git merge-base --is-ancestor 4ac0dbda HEAD` = YES.\n\nPLACEMENT, the row's subject: the migration runs AFTER the image digest is proven (`image inspect` :263-266) and BEFORE `candidateStarted = true` / `startCandidate` (:268-269). Both neighbours are wrong — earlier could move the schema and then fail to produce an image, leaving the schema ahead of the code with no image to roll back to; later boots the candidate against the OLD schema.\n\nRESOLVER: `readEnvironmentDelivery(config, environment)`, taking the deploy's already-admitted environment as an explicit STRING. `resolveEnvironmentLaunch` REJECTED — `PURPOSE_ENVIRONMENTS` is exactly `{PREVIEW:\"preview\", VERIFIER:\"verify\"}` with NO deploy member, so a production deploy would resolve another environment's variables (its header claims the map grows when task-04b3ce7e lands; that row is DONE and the map did NOT grow). `launchDelivery` REJECTED — its refusal becomes `undefined`, so an unreadable store would present as \"no database configured\". Environment refusals are forwarded UNCHANGED with their own code AND layer (SCOPE/KEY).\n\nNO second engine, lock leaf or replay check: `migrateWithBackup` keeps the project-wide lock, the receipt identity and the `requestId` replay; the deploy's `decisionId` IS that requestId.\n\nFOUR DRILLS, all red where they should (detail in comment-7beb57f1): (i) call moved after `startCandidate` -> `expected [ [ 'run', '--detach', ...(5) ] ] to deeply equal []`; (ii) resolver swapped -> `Expected \"...prod-s3cr3t@db.internal...\" / Received \"...preview-s3cr3t@db.preview...\"`; (iii) refusal coerced to undefined -> `ENV_ENVIRONMENT_UNKNOWN@SCOPE` replaced by `DEPLOY_MIGRATION_DATABASE_UNSET@DAEMON_DEPLOY_ENGINE`; (iv) migrate member deleted -> `expected null to match object` (no migration ran). Restores proved by SHA256 for the three UNTRACKED paths, SHA256 plus tracked diff for the rest, each i",
  "commits": [
    {
      "sha": "2dfff1b2d8e4343f1ed92083df2dd30db59b0c0b",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "completion",
      "role": "qa",
      "sessionId": "qa-97200064@2026-09-07T12:20:00Z",
      "paths": [
        "apps/daemon/src/deployment/deploy-service.ts",
        "apps/daemon/src/deployment/deploy-migration-context.ts",
        "apps/daemon/src/deployment/deploy-migration-context.js",
        "apps/daemon/src/deployment/deploy-migration-context.test.ts",
        "apps/daemon/src/deployment/deploy-ports.ts",
        "apps/daemon/src/deployment/deploy-command.ts",
        "apps/daemon/src/daemon-command-async-entries.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/deployment/deploy-service.test.ts",
        "apps/daemon/src/deployment/deploy-journey.test.ts"
      ],
      "recordedBy": "qa-97200064",
      "recordedAt": "2026-09-07T12:20:35.119Z",
      "status": "REVIEW",
      "pushed": false,
      "recoveredBy": "qa-97200064",
      "message": "feat(task-2db55fcaad604aa4861139b22941095f): Deploy composes backup-before-migrate before activating the candidate — QA hand-landing. Row reached REVIEW with task.commits empty and all ten declared paths uncommitted (three untracked). Each path measured to carry only this row's hunks before staging; 10 files changed, 916 insertions(+), 8 deletions(-). NOT PUSHED — a QA hand-landing rides out on a later committing worker exit."
    }
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "qa-97200064@2026-09-07T12:20:00Z",
    "at": "2026-09-07T12:20:35.119Z"
  },
  "completedAt": "2026-09-07T12:23:24.038Z",
  "reviewCompletedAt": "2026-09-07T12:23:24.038Z",
  "reviewSummary": "APPROVED. FULL EVIDENCE: comment-f19616bc.\n\nHAND-LANDED FIRST: row hit REVIEW with task.commits [] and all ten declaredPaths uncommitted (three untracked). Landed 2dfff1b2d8e4343f1ed92083df2dd30db59b0c0b on refs/heads/moe/work-2026-09-04, 10 files, 916(+)/8(-), recorded via record_commit. NOT PUSHED (QA landings ride a later worker exit). Per-path purity measured before staging: every non-vocabulary diff line was this row's own doc-comment or type member; ZERO foreign hunks; exactly 10 staged. Every relative import resolves at `git cat-file -e 2dfff1b2:<path>`.\n\nGATES I RE-RAN: (a) the recorded command, reproduced exactly — Test Files 16 passed (16), Tests 590 passed | 1 skipped (591), EXIT 0. (b) apps/daemon tsc EXIT 1, exactly TWO errors, both in src/http/affordance-agent-provider-offers.{ts,test.ts} — untracked, NOT in HEAD, mtime 66 s before my run (worker-b1872f24 mid-write on task-96957529). Intersection with owned paths EMPTY; the worker's \"tsc EXIT 0\" was true when measured.\n\nDoD 1-5 MET. Migration at deploy-service.ts:303, refusal returns :312 BEFORE candidateStarted (:314) and startCandidate (:315), after image inspect. Default-composition arm real: deploy-journey.test.ts:527+ dispatches with NO ports. readEnvironmentDelivery at the host boundary; resolveEnvironmentLaunch correctly rejected — PURPOSE_ENVIRONMENTS has no deploy member. migrateFor handles BOTH engine shapes, thrown and returned. Bridge present; runtime-entrypoint.test.ts now 5 passed (5), closing today's `unexpected` red.\n\nRAIL t2 HOLDS, probed hardest: refusal details come from a frozen lookup so nothing can interpolate the connection value, and the leak arm carries a GENUINE positive control proving the negative assertions can fail.\n\nThe verification substitution is disclosed, not evasive; its control — clean 577-passed baseline, then revert-and-move-out reproducing the same six failures with zero of the worker's bytes — is the best attribution I graded today."
}