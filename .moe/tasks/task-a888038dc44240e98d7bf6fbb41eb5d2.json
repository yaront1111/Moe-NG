{
  "id": "task-a888038dc44240e98d7bf6fbb41eb5d2",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[T1-b producer] Run-scoped policy evaluation at finalize: derive node-property facts from the sealed graph revision, evaluate through core, persist PolicyEvaluated with runId and computedTier; unclassifiable refuses",
  "description": "Second of the T1 chain (ruling comment-c86c35cb on task-ba102165): the PRODUCER that gives a run a durable pre-approval tier. Sequenced after [T1-a policy vocabulary]. UNPARK: T1-a DONE on disk AND `git grep -n 'export function deriveNodePropertyFacts' HEAD -- packages/core/src` EXIT 0.\n\nSTRUCTURAL CHOICE (the ruling left it to the architect): DAEMON-OWNED EVALUATION AT FINALIZE, not a caller-driven policy.validate. Reason, measured at HEAD 5b3d7313: `policy.validate` (bootstrap-policy-services.ts:160-175) takes `graphNodeRevisionRefs`, `scope`, `action`, `callerRiskHint`, `requiredFactIds` from the CALLER payload and resolves facts via resolvePolicyFact (activation-written record → circular); a run-scoped evaluation must instead take the run's node content from DURABLE state the daemon already holds at finalize — planning-authority-finalize.ts (`commitFinalizedSubmission`, PLANNING_SUBMISSION_FINALIZED_EVENT_TYPE :49) seals the submission whose graph revision carries the nodes; the same window readApprovalIntentSources later reads (approval-intent-sources.ts:35 approvedNodeScope, :40 graphRevisionRef).\n\nDELIVER: at finalize (or as a leg of the same decision), the daemon (1) reads the sealed graph revision's node content, (2) derives node-property facts through `deriveNodePropertyFacts` against the installed policy chain's riskClassifications, (3) runs the EXISTING core `evaluatePolicy` with those facts (action/scope/graphNodeRevisionRefs are server facts from the run, callerRiskHint null, evaluatorVersion the daemon's), and (4) persists a `PolicyEvaluated` record carrying `runId`, `graphNodeRevisionRefs` (the run's, server-derived) and the `riskAssessment` (computedTier non-null when classified) — extending the PolicyEvaluated payload/exact rosters (the writer at bootstrap-policy-services.ts:200-225 and the strict reader bootstrap-policy-authority-reader.ts:176-215 pin the shape; enumerate every pin). (5) UNCLASSIFIABLE REFUSES: when the assessment's computedTier is null the finalize decision REFUSES with an exact stable code and layer (fail-closed; ruling condition 3) and writes no PolicyEvaluated for the run. No default tier, no caller hint. Consumer: [T1-c reader] and, through it, approval.decide_intent's RISK_TIER slot.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, in a row comment with `git rev-parse HEAD` quoted, greps unpiped with exit codes plus a positive control: [T1-a] DONE on disk and `deriveNodePropertyFacts` + `riskClassifications` present in packages/core/src (EXIT 0); the finalize seam still seals at planning-authority-finalize.ts (`commitFinalizedSubmission`, PLANNING_SUBMISSION_FINALIZED_EVENT_TYPE); no run-scoped evaluation exists yet (`git grep -n 'runId' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-services.ts apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` EXIT 1); every exact-key pin of the PolicyEvaluated payload (writer :200-225, reader :176-215, fixtures, tests) enumerated and owned or split.",
    "A finalized run with classifiable nodes yields exactly ONE durable PolicyEvaluated carrying `runId` = the run, `graphNodeRevisionRefs` derived from the sealed revision (never the payload), and `riskAssessment.computedTier` equal to the maxTier of the classified node-property facts — proven through the production finalize command on a real store and read back through the strict reader; the tier equals what `evaluatePolicy` returns for the same facts computed independently in the arm.",
    "UNCLASSIFIABLE REFUSES: a run whose node has a property the policy chain does not classify makes finalize REFUSE with an exact stable code and layer (both asserted), writes NO PolicyEvaluated for that run, and leaves the submission un-finalized; divergence: the same run with the property classified finalizes — only the classification presence flips it.",
    "No caller influence: the finalize payload cannot carry facts, tiers, callerRiskHint, scope or graphNodeRevisionRefs for the evaluation (an arm presents each and asserts refusal at the exact-record ingress with code+layer); `policy.validate`'s existing caller-driven path is untouched and its suites pass unedited.",
    "Mutation drills with SHA256 restore proofs: read node refs from the payload instead of the sealed revision -> the server-derived arm reds; default computedTier to R0 on null -> the unclassifiable arm reds; drop runId from the record -> the reader-side linkage arm reds. Gates standalone/unpiped with counts: `pnpm --filter @moe/daemon typecheck`, the finalize/policy/authority-reader suites from apps/daemon, `pnpm --filter @moe/daemon test` path-attributed; every production source <= 250 lines (split before 400, `.js` bridges); explicit-pathspec commit; completion comment names [T1-c reader] as consumer and states: 'every finalized run now carries a policy-declared, daemon-computed risk tier or is refused as unclassifiable'."
  ],
  "taskRails": [
    "Server facts only: node content from the sealed revision, action/scope/refs from the run, callerRiskHint null; never from the finalize payload.",
    "Compose the existing evaluator and the existing PolicyEvaluated writer/reader shapes; extend rosters explicitly (fan-out enumerated), never a second event type or a parallel ledger.",
    "Unclassifiable refuses with exact code+layer and writes nothing; no default tier anywhere (ruling condition 3).",
    "Sequenced after [T1-a]; before [T1-c]. Explicit-pathspec commits; foreign WIP preserved; <=250 lines per production source; TDD red-first through the production finalize command."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK (DoD-1) — read-only, ONE moe.add_comment, `git rev-parse HEAD` quoted, every grep UNPIPED with its own exit code plus a positive control (a piped grep reports the tail's exit — the architect hit exactly this at plan time). (a) T1-a landed: `git grep -n 'export function deriveNodePropertyFacts' HEAD -- packages/core/src` EXIT 0 AND `git grep -n 'riskClassifications' HEAD -- packages/core/src` EXIT 0; quote the landed signatures and barrel exports — if the landed contract differs from this plan's naming, ADAPT and record the delta; if the contract itself differs (facts not derivable from node content + installed chain), report blocked naming the exact gap. (b) T1-a2 (task-4e013e45) DONE — the daemon exact-key readers admit the riskClassifications slice field; quote its completionSummary from epicSiblings instead of re-measuring its internals. (c) Finalize seam unchanged: commitFinalizedSubmission at planning-authority-finalize.ts:205, event type :49. (d) Gap still open: `git grep -n 'runId' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-services.ts apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` EXIT 1 (EXIT 0 = someone landed run linkage: adopt/verify/report duplicate). (e) FAN-OUT ENUMERATION (rail E7(B) + the shape-grep rail): grep the SHAPE, not just the symbol — the writer's payload key list (bootstrap-policy-services.ts:207-216), `\"PolicyEvaluated\"` across apps packages tests, the reader's exactObject rosters, fixture literals, and any hard-coded key counts. List EVERY pin with file:line in the comment. If the enumeration exceeds this plan's roster, that is a SPLIT signal — report it rather than absorbing. (f) porcelain on every owned path; note the ~725 foreign dirty paths and continue.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/planning-authority-finalize.ts"
      ],
      "note": "VERIFY-OR-BLOCK done, full record in comment-7bcc3be8. HEAD 547c6c55. Read-only step, zero files changed. Headlines: (1) T1-a landed under a DIFFERENT name and package — `deriveNodePropertyFactIds` in @moe/scheduler (node-policy-facts.ts:64), not `deriveNodePropertyFacts` in core (that grep EXIT 1); contract intact, adapting. (2) It returns fact IDs only, by design — this row mints truthClass DAEMON_VERIFIED and lets core's assessRisk supply the tier from riskClassifications. (3) Gap open: runId grep EXIT 1 on both bootstrap policy files (positive control projectId EXIT 0). (4) The pre-existing policy-risk-* family is a different capability (HUMAN_APPROVED tier from approval.riskTier) — not a duplicate. (5) FAN-OUT: all 22 `\"PolicyEvaluated\"` sites address `${projectId}-policy`; three PRODUCTION readers fail closed on any row the strict reader refuses, so the run-scoped row must land on its own run-addressed aggregate — that keeps all four exact-8-key pins green unedited and keeps the fan-out inside this plan's roster (no split signal). (6) Node source is `state.sealedHashes.graphContentHash` -> readGraphBody -> nodeAuthority.definitions; `state.graphRevisionRef` is a caller label and must NOT be used. (7) Porcelain empty on all owned paths; 99 foreign dirty paths noted and untouched.",
      "startedAt": "2026-08-29T23:44:31.517Z",
      "completedAt": "2026-08-29T23:49:06.861Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — THE RUN EVALUATION MODULE. NEW `apps/daemon/src/planning/run-policy-evaluation.ts` + one-line exact-LF bridge `run-policy-evaluation.js` + `run-policy-evaluation.test.ts`. Export `evaluateRunPolicy(store, { projectId, runId, sealedRevision })` (exact input shape adapted to what finalize actually holds at the seal point — step 1 records it) which: (1) derives node-property facts from the SEALED revision's node content through core's `deriveNodePropertyFacts` against the INSTALLED policy chain's riskClassifications (read from durable state, the same chain the existing evaluator uses); (2) constructs the core `evaluatePolicy` input from SERVER FACTS ONLY — action/scope/graphNodeRevisionRefs from the run's sealed content, callerRiskHint null, evaluatorVersion the daemon's POLICY_EVALUATOR_VERSION constant; (3) returns `{ ok: true, assessment }` where assessment carries computedTier (non-null, = maxTier of classified facts per the T1-a table) and the evaluation record material, OR `{ ok: false, code: <ONE new stable code, e.g. RUN_POLICY_UNCLASSIFIABLE>, layer: <the module's own layer literal, no *_LAYER column-zero export>, facts: <which property was unclassifiable> }` when ANY node property lacks a classification — computedTier null NEVER becomes a tier and NEVER defaults. ARMS: classifiable fixture -> tier equals an INDEPENDENT arm-side evaluatePolicy run over the same facts (production evaluator both sides, different composition path); unclassifiable property -> exact code AND layer; determinism (same sealed content twice -> same assessment); caller-shaped fields (facts/tier/hint in the module input) are unrepresentable — the input type simply has no such fields, assert via a compile-time-erased runtime guard arm if the module validates unknown input. Module <= 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/run-policy-evaluation.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/planning/run-policy-evaluation.ts",
        "apps/daemon/src/planning/run-policy-evaluation.js",
        "apps/daemon/src/planning/run-policy-evaluation.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/run-policy-evaluation.ts",
        "apps/daemon/src/planning/run-policy-evaluation.js",
        "apps/daemon/src/planning/run-policy-evaluation.test.ts",
        "apps/daemon/src/planning/run-policy-record.ts",
        "apps/daemon/src/planning/run-policy-record.js",
        "apps/daemon/src/bootstrap/bootstrap-policy-services.ts",
        "apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts"
      ],
      "note": "RUN EVALUATION MODULE GREEN: `Test Files 1 passed (1)` / `Tests 8 passed (8)`, standalone unpiped from apps/daemon.\n\nSPLIT INTO TWO PRODUCTION FILES (the single module hit 269 lines, over the 250 rail): `run-policy-evaluation.ts` 208 lines (derivation + evaluation) and `run-policy-record.ts` 122 lines (address, payload shape, refusal vocabulary) — the same division policy-risk-record.ts / policy-risk-leg.ts already uses. Both carry exact-LF one-line `.js` bridges (runtime tier); the `.test.ts` gets none.\n\nAPI adapted to the landed T1-a surface: `evaluateRunPolicy(store, ledger, {decidedAt, graphContentHash, principalId, projectId, runId})`. NO facts/tier/callerRiskHint/scope/graphNodeRevisionRefs member exists on the input type, so caller influence is unrepresentable rather than filtered.\n\nTWO STRUCTURAL DECISIONS, both forced by measurement:\n(1) THE RUN ROW GETS ITS OWN AGGREGATE `policy-run-evaluation:<runId>`, same `PolicyEvaluated` event type. Step 1 measured that all 22 PolicyEvaluated sites address `${projectId}-policy` and that three PRODUCTION readers scan it newest-first and fail closed on any row the strict reader refuses. A widened row there would sit at the head of all three scans.\n(2) THE CHAIN IS THE NEWEST INSTALLED CORE EVALUATION SLICE (`derivePolicySliceDigest(...).ok` and digest === install address, re-derived). This is coverage-free on purpose so CORE owns the unclassifiable verdict; a coverage-based selector would pre-empt core's own RISK_TIER_UNCLASSIFIABLE and become a shadow fence.\n\nFAN-OUT MEASURED EMPIRICALLY, not guessed. Baseline `pnpm exec vitest run` from apps/daemon at HEAD 547c6c55: `Test Files 5 failed | 311 passed (316)`, `Tests 18 failed | 6554 passed`. Route A (add riskClassifications to the shared POLICY_SLICE, changing POLICY_REF): `94 failed | 222 passed`, `1486 failed` — rejected, restored, `git diff --exit-code` EXIT 0. Route B (make the journey node's policySliceHash a real digest): strictly larger, moves SEALED_GRAPH_CONTENT_HASH/SEALED_SUBMISSION_HASH. Route D, ADOPTED: append a SECOND classifying `policy.install` to bootstrapSequence() leaving POLICY_REF byte-identical: `8 failed | 308 passed`, `23 failed` = exactly 3 new files (bootstrap-durability, bootstrap-replay-bytes, approval-activation — a count, an index sweep, two decision-digest KATs). Full detail going into a row comment.\n\nFacts derived by the production path for the journey graph (probed, then probe deleted): node.capability:capability-implement, node.read_scope:services/api/src, node.resource:resource-a, node.write_scope:services/api/src/node. These are node-key-independent, so ONE classification table covers all three journeys (daemon fixtures, demo seed, control-room).\n\nARMS (8): production-evaluator-vs-production-evaluator tier agreement (no literal expectation); server-derived refs/scope/DAEMON_VERIFIED/tier-null assertions; UNCLASSIFIABLE code+layer with a table that classifies something else (an EMPTY table digests to the already-installed slice's address and would be refused one layer earlier — that arm would have been vacuous); one-degree-of-freedom DIVERGENCE (same run, same store shape, only the table's fact ids differ); MAX-not-first-not-last with a descending table; POLICY_ABSENT; GRAPH_UNAVAILABLE; determinism by byte equality. Non-vacuity guard asserts the derived fact-id set is nonempty.\n\nAlso: `installedSlices` promoted to an export in bootstrap-policy-services.ts (one reading of the installed set, no duplicate navigator); bootstrap-test-fixtures.ts carries the route-D classifying slice + install envelope.",
      "startedAt": "2026-08-29T23:49:10.930Z",
      "completedAt": "2026-08-30T00:12:48.423Z"
    },
    {
      "stepId": "step-3",
      "description": "RED FIRST — FINALIZE COMPOSES THE EVALUATION ATOMICALLY (DoD-2 + DoD-3). Arms in the existing finalize suite (name it in the note; the suite driving commitFinalizedSubmission through the production command): (i) a finalized run with classifiable nodes yields EXACTLY ONE durable PolicyEvaluated carrying runId = the run, graphNodeRevisionRefs derived from the SEALED revision (never the payload), and riskAssessment.computedTier = the expected maxTier — read back through the STRICT reader (step 4's widened surface), counts asserted (one event, not per-node); (ii) UNCLASSIFIABLE REFUSES: a run whose node carries an unclassified property makes the finalize command REFUSE with the module's exact code+layer, writes NO PolicyEvaluated AND leaves the submission UN-FINALIZED (assert both ledgers' event counts unchanged — the seal must not land without the evaluation; single-decision atomicity); (iii) DIVERGENCE (DoD-3): the SAME run with that property classified in the installed chain finalizes green — only the classification presence flips the outcome. THEN GREEN: planning-authority-finalize.ts composes `evaluateRunPolicy` as a leg of the SAME decision that seals (the existing multi-leg/single-decision idiom; never a second command); the file is at 227 lines — the composition is a handful of lines, and if it crosses 250, move existing lines OUT into a sibling (fix-by-subtraction), never past the cap.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/planning-authority-finalize.ts",
        "apps/daemon/src/planning/run-policy-evaluation.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/planning-authority-finalize.ts",
        "apps/daemon/src/planning/planning-authority-finalize.test.ts",
        "apps/daemon/src/planning/run-policy-leg.ts",
        "apps/daemon/src/planning/run-policy-leg.js",
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
        "apps/daemon/src/bootstrap/bootstrap-journey-fixtures.ts",
        "apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts",
        "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-replay-bytes.test.ts",
        "apps/daemon/src/planning/approval-activation.test.ts",
        "apps/daemon/src/planning/expansion-release-selector-test-fixtures.ts",
        "apps/daemon/src/http/affordance-read-planning.test.ts",
        "apps/daemon/src/http/affordance-read.test.ts",
        "apps/daemon/src/http/http-listener-planning-run.test.ts",
        "apps/daemon/src/work/foundation-context-selection.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/control-room/src/live/live-dispatch-payloads.ts"
      ],
      "note": "FINALIZE COMPOSES THE EVALUATION ATOMICALLY. Suite `src/planning/planning-authority-finalize.test.ts`: `Test Files 1 passed (1)` / `Tests 36 passed (36)` (was 31 — five new arms).\n\nCOMPOSITION: `commitFinalizedSubmission` builds BOTH legs before any write and appends them to the SAME `commitAcceptedLegs` array. A refusal from the risk leg returns `refuse(request.kind, risk.code, risk.layer)` before the plan is composed, so the seal cannot land without the evaluation. planning-authority-finalize.ts is 239 lines (<=250); the leg builder lives in a NEW 78-line sibling `run-policy-leg.ts` because inlining would have pushed the seam past the cap.\n\nNEW LAYER `DAEMON_RUN_POLICY` added to SERVICE_REFUSED_BY, with the same closed-TYPE compile-time agreement the neighbouring literals use.\n\nARMS ADDED, all through the PRODUCTION `planning.finalize_submission` command on a real store:\n(i) the run row carries runId = the run, graphNodeRevisionRefs = [the SEALED content hash], riskTier R2, read back through the STRICT reader, event count asserted at exactly 1 (one per run, not one per node). The refs assertion is load-bearing because `finalizeCommand()` never states a node revision ref at all.\n(ii) UNCLASSIFIABLE REFUSES: exact code RUN_POLICY_UNCLASSIFIABLE AND layer DAEMON_RUN_POLICY; `residueOf(store)` (authority version, decision count, run version) strictly unchanged; zero events on the run-policy aggregate; zero PlanningSubmissionFinalized on the run.\n(iii) DIVERGENCE on one degree of freedom: identical worlds, differing only by one extra policy.install whose table names an UNRELATED fact id. Deliberately not an empty table — an empty table digests to the already-installed empty slice's address and policy.install refuses it one layer earlier, which would have made the arm grade the install ingress instead of the evaluator.\n(iv) CALLER INFLUENCE: six smuggled members (facts / riskAssessment / callerRiskHint / scope / graphNodeRevisionRefs / policyRevisionRef). CORRECTION TO THE PLAN'S EXPECTATION: the finalize terminal has NO exact-key roster — it has a forbidden-key list and the core reducer ignores unknown members — so \"refusal at the exact-record ingress\" is unsatisfiable for those six. The arm asserts the stronger observable instead: the DURABLE ROW is byte-identical to a clean run's for every one. Recorded, not quietly narrowed.\n(v) the one member the ingress DOES name, `sealedHashes`, asserted with code+layer (PLANNING_FINALIZE_BODIES_SUPPLIED @ DAEMON_INGRESS), so (iv) cannot read as \"nothing here refuses anything\".\n\nFAN-OUT ABSORBED (measured, not guessed). Seven worlds needed the classifying slice: bootstrapSequence (one extra install), expansion-release-selector-test-fixtures, foundation-context-selection, and the http worlds in affordance-read-planning (x3), affordance-read, http-listener-planning-run. `legacyProposedStore()` now also CARRIES the graph-body row it always implicitly had — the authority-ABSENT arms would otherwise be grading a graph-absent world. PRODUCTION surfaces: the demo seed's validatablePolicySlice() and the control room's live dispatch table both gained the same four-entry table (identical bytes, same digest fff1cc91…). Control-room baseline re-measured AT HEAD before and after: `67 failed | 41 passed (108)` both times — my change adds zero; restore proven by SHA256 equality in a call separate from the restore.\n\nTwo pin rotations, both recorded in-file with pre-rotation values: approval-activation.test.ts's four COLLAPSE digests (I first wrote that only the decision digests moved; re-measured — all four move, and the comment now says so and points at the three STRUCTURAL assertions that actually carry the collapse claim) and the control room's POLICY_REF literal.\n\nbootstrap-replay-bytes.test.ts was hardening, not just a fix: its conflict cases were keyed by spelled INDEX, so inserting one command silently renamed the plan.propose case to goal.create while the suite stayed green on the wrong kind. Now looked up by kind, with the case's own kind asserted.\n\n`pnpm --filter @moe/control-room typecheck` EXIT 0. `pnpm --filter @moe/daemon typecheck` EXIT 1 on ONE foreign path: `src/daemon-command-registry.test.ts(1271,7) TS2353 'cutoverActivation'` — a ` M` file I never touched; `git show HEAD:<path> | grep -c cutoverActivation` = 0 vs 1 in the worktree, so the symbol exists only in a peer's uncommitted edit.",
      "startedAt": "2026-08-30T00:30:50.985Z",
      "completedAt": "2026-08-30T00:31:14.290Z"
    },
    {
      "stepId": "step-4",
      "description": "RED FIRST — THE WIDENED PolicyEvaluated SHAPE (writer + strict reader + every pin from step 1(e)). Writer: bootstrap-policy-services.ts stays caller-driven and UNTOUCHED on its :160-175 path (DoD-4: policy.validate suites pass unedited); the RUN-scoped record is written by the finalize leg with the widened payload — runId, graphNodeRevisionRefs (server-derived), riskAssessment — alongside the existing keys, through the same event type. If the cleanest seam is a shared payload-shaping helper used by both writers, put it in the NEW module, not in the at-cap services file. Strict reader: bootstrap-policy-authority-reader.ts:176-215's exact checks EXTEND to admit and verify the new keys on run-scoped rows (replay-verified like every existing field — the riskAssessment must be re-derivable/consistent, not merely present) while LEGACY caller-driven rows (no runId) still read exactly as today — an arm pins BOTH row shapes through the reader. Reader is at 220 lines — same subtraction rule. Update every pin enumerated in step 1(e): fixtures, exact-key tests, any hard-coded key roster — each named in complete_step.modifiedFiles and declared via moe.declare_files (the fan-out was enumerated at step 1; if reality exceeds the roster, STOP and report the split signal, do not absorb).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-policy-services.ts",
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
        "apps/daemon/src/planning/run-policy-record.ts",
        "apps/daemon/src/planning/run-policy-evaluation.ts",
        "apps/daemon/src/planning/run-policy-evaluation.test.ts"
      ],
      "note": "THE WIDENED SHAPE, WRITER + STRICT READER. `src/planning/run-policy-evaluation.test.ts`: `Test Files 1 passed (1)` / `Tests 14 passed (14)` (was 8 — six new reader arms).\n\nWRITER: `bootstrap-policy-services.ts`'s caller-driven path is byte-untouched apart from one `export` keyword on `installedSlices` (so the evaluator reads the installed set through ONE reader instead of a second navigator). The run-scoped row is written by the finalize leg via `buildRunPolicyRow` in the NEW `run-policy-record.ts`, not by the at-cap services file. The eight caller-driven keys are spelled identically; the three extras are `graphNodeRevisionRefs`, `riskAssessment`, `runId`.\n\nREADER: `bootstrap-policy-authority-reader.ts` is 247 lines (<=250). `exactObject(payload, ROW_KEYS) ?? exactObject(payload, RUN_ROW_KEYS)`, where `RUN_ROW_KEYS = [...ROW_KEYS, ...RUN_POLICY_ROW_EXTRA_KEYS]` and the extra roster is IMPORTED from the writer's module rather than re-spelled — the two cannot drift. Because `exactObject` compares `Reflect.ownKeys().length`, the pair means exactly eight OR exactly eleven; a caller-driven row that grew a runId is still refused, and so is a run row missing one. The run half is REPLAY-VERIFIED, not merely admitted: `runScopedLinkage` compares the row's `graphNodeRevisionRefs` and `riskAssessment` against the values the reader's OWN independent `evaluatePolicy` replay produced, and refuses `POLICY_AUTHORITY_RUN_LINKAGE_MISMATCH` on disagreement. It also refuses a run row whose computedTier is null — a run-scoped row exists to carry a tier, and a tier-less one would read downstream as \"no risk\". `PolicyEvaluationAuthority` gains `runId: string | null` and `riskTier: PolicyRiskTier | null`, both null on caller-driven rows, which is the surface [T1-c reader] consumes.\n\nARMS (6): both shapes' exact key rosters asserted by sorted set-equality; a caller-driven row minted by the PRODUCTION `policy.validate` handler still reads with runId null and riskTier null; a run row reads with runId = the run and riskTier = the evaluator's computedTier; and four one-tamper refusal arms each asserting code AND layer — dropped runId -> ROW_UNREADABLE (ten keys match neither roster), foreign refs -> RUN_LINKAGE_MISMATCH, a tier the evidence does not derive -> RUN_LINKAGE_MISMATCH, an emptied run id -> RUN_LINKAGE_MISMATCH.\n\nThe arms live in `run-policy-evaluation.test.ts` DELIBERATELY, not in `bootstrap-services.test.ts`: leaving that suite unedited is itself the DoD-4 evidence, and the legacy operand is still a real production-written row rather than a hand-built lookalike.\n\nDoD-4 PROOF: `git diff --stat` EMPTY and `git status --porcelain` EMPTY on bootstrap-services.test.ts, policy-fact-adoption.test.ts, admission-gate-resolver.test.ts and policy-allowance-fixtures.ts; those three suites run `Test Files 3 passed (3)` / `Tests 109 passed (109)` UNEDITED. The step-1 fan-out enumeration therefore held exactly: all four exact-8-key pins (writer :207-216, reader ROW_KEYS, admission-gate-resolver.test.ts:501-503 with its literal digest KAT, bootstrap-services.test.ts:558-569) are green with zero edits, because the run row lives on its own run-addressed aggregate and never enters the `${projectId}-policy` scan the three production readers walk.",
      "startedAt": "2026-08-30T00:31:29.552Z",
      "completedAt": "2026-08-30T00:32:28.470Z"
    },
    {
      "stepId": "step-5",
      "description": "MUTATION DRILLS (DoD-5), one degree of freedom, backups to %TEMP%\\a888038d-<workerId>\\; SHA256 restore proofs for UNTRACKED paths (the new module trio — git diff is blind to them) in a call SEPARATE from the restore; `git diff --exit-code` acceptable only for tracked paths. (D1) in the finalize leg, read graphNodeRevisionRefs from the command payload instead of the sealed revision -> step-3 arm (i)'s server-derived assertion reds. (D2) in evaluateRunPolicy, default computedTier (e.g. R0) when a property is unclassified -> the unclassifiable arm (ii) reds AND the divergence arm (iii) can no longer distinguish — quote both. (D3) drop runId from the widened payload -> the reader-side linkage arm reds (run-scoped row no longer readable as run-scoped). (D4) make the evaluation a SECOND decision instead of a leg (commit seal first, evaluate after) -> arm (ii)'s both-ledgers-unchanged assertion reds on the refusal path (seal landed without evaluation). A GREEN drill is a FINDING — report, never bank. Record D1-D4 with red lines and restore proofs in ONE row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/run-policy-evaluation.ts",
        "apps/daemon/src/planning/planning-authority-finalize.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/run-policy-leg.ts",
        "apps/daemon/src/planning/planning-authority-finalize.ts"
      ],
      "note": "D1-D4 recorded in full in comment-ee612b18 with red lines and SHA256 restore proofs. Summary: D1 (leg reads the caller-stated graphRevisionRef) RED 14/36; D1b (the plan's literal payload-read mutant) DOES NOT COMPILE — TS2339 'request' does not exist on RunPolicyLegInput; D2 (default computedTier to R0) RED 4/50 at BOTH layers including the divergence arm; D3 (drop runId) RED 8, reader linkage arms named; D4 (seal without evaluation) RED 2/36 on the both-ledgers-unchanged assertions.\n\nTHE ONE PRODUCTION CHANGE THIS STEP MADE, and why. D1b exposed that at this seam the sealed hash and the caller-stated hash are PROVABLY EQUAL — the finalize reducer derives `sealedHashes` from the command's own `revision` — so no fixture can make them differ and a value-level test can never police a payload read. Per epic rail 7(A) I changed what the seam PRODUCES rather than deleting a redundant call: `buildRunPolicyLeg` was narrowed from `HandlerContext` (which carries `request.payload`) to an explicit `RunPolicyLegInput` with no payload member. The mutation is now unrepresentable, and D1 remains the value-level control proving the surviving read is load-bearing. run-policy-leg.ts 96 lines, planning-authority-finalize.ts 244 lines, both under the 250 rail.\n\nThree of the four drill targets are UNTRACKED (porcelain taken BEFORE drilling), so every restore is proven by SHA256 against an out-of-tree backup in a call SEPARATE from the restore — `git diff` would have certified nothing on them. All four RESTORED-IDENTICAL; suites back to `Test Files 2 passed (2)` / `Tests 50 passed (50)`.",
      "startedAt": "2026-08-30T00:35:23.226Z",
      "completedAt": "2026-08-30T00:35:34.027Z"
    },
    {
      "stepId": "step-6",
      "description": "GATES + SELF-REVIEW + COMMIT + COMPLETION. GATES, standalone/unpiped, exit codes from their own runs, counts quoted: (G1) `pnpm --filter @moe/daemon typecheck` (quote `$ tsc`); (G2) from apps/daemon `pnpm exec vitest run --root . --config package.json src/planning/run-policy-evaluation.test.ts src/planning/planning-authority-finalize.test.ts src/bootstrap/bootstrap-policy-services.test.ts src/bootstrap/bootstrap-policy-authority-reader.test.ts` (adapt names to the real suites recorded in step 1; the `--root . --config package.json` form is mandatory); (G3) DoD-4's untouched-path proof: the policy.validate suites pass UNEDITED — quote their counts and `git diff --stat` EMPTY on their test files; (G4) `pnpm --filter @moe/daemon test` path-attributed per epic rail, foreign red disclosed verbatim (the known baseline set: BOOTSTRAP_BUDGET_COMMITMENT_MISMATCH seed red until task-be80cb74 lands, EPERM at foundation-registry-fixtures, goal-catalog drift). SELF-REVIEW: (i) can ANY caller byte reach the facts, tier, scope, refs or hint? (walk every input of the finalize command to the evaluation); (ii) is there any path where the seal lands without the evaluation or vice versa? (iii) do legacy PolicyEvaluated rows still read exactly? (iv) `wc -l` every touched production file <= 250, the new module included. COMMIT by explicit pathspec of exactly the owned paths (porcelain first; never the foreign dirt). COMPLETION COMMENT names [T1-c reader] task-f42d5165b747436a9f9b354dbfa37e3d as consumer and task-cb0d65ff/task-4e013e45 as the landed prerequisites, quotes G1-G4 and D1-D4, and states EXACTLY: 'every finalized run now carries a policy-declared, daemon-computed risk tier or is refused as unclassifiable'.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/run-policy-evaluation.ts",
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/run-policy-evaluation.ts",
        "apps/daemon/src/planning/run-policy-evaluation.js",
        "apps/daemon/src/planning/run-policy-evaluation.test.ts",
        "apps/daemon/src/planning/run-policy-record.ts",
        "apps/daemon/src/planning/run-policy-record.js",
        "apps/daemon/src/planning/run-policy-leg.ts",
        "apps/daemon/src/planning/run-policy-leg.js",
        "apps/daemon/src/planning/planning-authority-finalize.ts",
        "apps/daemon/src/planning/planning-authority-finalize.test.ts",
        "apps/daemon/src/planning/approval-activation.test.ts",
        "apps/daemon/src/planning/expansion-release-selector-test-fixtures.ts",
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
        "apps/daemon/src/bootstrap/bootstrap-policy-services.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
        "apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts",
        "apps/daemon/src/bootstrap/bootstrap-journey-fixtures.ts",
        "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-replay-bytes.test.ts",
        "apps/daemon/src/http/affordance-read-planning.test.ts",
        "apps/daemon/src/http/affordance-read.test.ts",
        "apps/daemon/src/http/http-listener-planning-run.test.ts",
        "apps/daemon/src/work/foundation-context-selection.test.ts",
        "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
        "apps/daemon/src/orchestrator/demo-seed-policy.ts",
        "apps/daemon/src/orchestrator/demo-seed-policy.js",
        "apps/control-room/src/live/live-dispatch-payloads.ts"
      ],
      "note": "GATES + SELF-REVIEW complete; full record in comment-c0f88cc6. G1 daemon typecheck EXIT 0 (`$ tsc` echoed), G1b control-room typecheck EXIT 0, G2 owned suites EXIT 0 `Test Files 5 passed (5)` / `Tests 140 passed (140)`, G2b owned scope `1 failed | 95 passed (96)` with the single failure being the byte-untouched baseline-red `http-listener.test.ts`, G3 DoD-4 untouched-path proof (diff --stat and porcelain both EMPTY on seven policy.validate paths; `Test Files 4 passed (4)` / `Tests 115 passed (115)` unedited), G4 full package EXIT 1 with owned-path intersection EMPTY against the HEAD-547c6c55 baseline.\n\nFOREIGN RED DISCLOSED VERBATIM: `mcp-http-host.test.ts` and `mcp-main.test.ts` both `expected 3 to be 2` on EXCLUSION_CASES.length, caused by a peer's ` M apps/daemon/src/mcp-tool-allowlist.{ts,test.ts}` widening the roster 2->3; `foundation-attempt-windows.test.ts` red only under full-suite contention (256732ms real-process arm) and `Test Files 1 passed (1)` / `Tests 8 passed (8)` standalone; `http-listener`, `activation-run-commit`, `activation-telemetry-launch`, `launch-template-producer` all carried forward from the merge-base baseline unchanged.\n\nSELF-REVIEW found and fixed two things rather than just noting them. (a) `demo-seed-payloads.ts` was 411 lines at HEAD — already past the 400 split threshold before this row touched it — so instead of growing it to 428 I split the three policy bodies into a new `demo-seed-policy.ts` (156 lines) with a re-export, leaving the file at 302: 109 lines SMALLER than it arrived. (b) Two implicit invariants are now documented in code: `selectInstalledSlice`'s dependence on insertion order (sound because `installPolicy` refuses reinstalling a core slice at its own address) and `runScopedLinkage`'s serialized riskAssessment comparison being key-order sensitive (both sides originate in core's one `assessRisk` literal; fail-closed is correct for a risk tier).\n\nEvery production file touched is under the 400 split threshold; all new modules are 96-247 lines. 26 owned paths declared via moe.declare_files, verified against porcelain, no foreign path included.",
      "startedAt": "2026-08-30T00:35:36.844Z",
      "completedAt": "2026-08-30T00:46:11.452Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": "qa-063c9dff",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Human directive (session owner): drain the backlog into planned state. T1-b producer is CRITICAL and its dependsOn (cb0d65ff, 4e013e45) gates only WORKING claims — planning proceeds now so the row is executable the moment the deps land.",
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 172,
  "comments": [
    {
      "id": "comment-7bcc3be88e684e9d8a46de6381102ffb",
      "author": "worker-e98a7edb",
      "content": "STEP 1 VERIFY-OR-BLOCK. `git rev-parse HEAD` = `547c6c55b73734d864c6897fd03b067c62cad052`. Every grep below run UNPIPED, own exit code. Controls: POSITIVE `git grep -n 'export function evaluatePolicy' HEAD -- packages/core/src` EXIT 0 (policy-evaluation.ts:176); NEGATIVE `git grep -n 'zzzNoSuchSymbolQqq' HEAD -- packages/core/src` EXIT 1.\n\n(a) T1-a LANDED, BUT NOT AT THE PLAN'S NAME OR PACKAGE — ADAPTING, not blocking.\n- `git grep -n 'export function deriveNodePropertyFacts' HEAD -- packages/core/src` **EXIT 1**.\n- `git grep -n 'riskClassifications' HEAD -- packages/core/src` EXIT 0 (policy-contract.ts:117 `PolicySlice.riskClassifications?`, :101 `PolicyRiskClassification{factId,tier}`; policy-composition.ts:156 folds it; policy-validation.ts:149 `CLASSIFIED_SLICE_KEYS`).\n- task-cb0d65ff status DONE on disk, 8 commits. Its completionSummary: \"**@moe/scheduler** derives the closed fact-id set from the admitted sealed NodeDefinition; only T1-b may stamp DAEMON_VERIFIED and persist a run tier\".\n- LANDED SIGNATURE: `packages/scheduler/src/node-authority/node-policy-facts.ts:64 export function deriveNodePropertyFactIds(value: unknown): NodePropertyFactIdsResult` -> `{factIds: readonly string[], ok:true} | NodeAuthorityRefusal`. Roster :32 `NODE_PROPERTY_FACT_KINDS = [\"node.capability\",\"node.read_scope\",\"node.resource\",\"node.write_scope\"]`. Barrel: `node-authority-public.ts:201-208` re-exports both, and `index.ts` forwards that module wholesale.\n- DELTA I AM ADOPTING: it returns fact **IDs**, never `PolicyFactInput`s — by design (its header :15-18 \"IT RETURNS IDS, NEVER FACTS ... Only the daemon consumer that read the sealed graph may [stamp DAEMON_VERIFIED] (task-a888038d)\"). So THIS row mints `{factId, tier:null, truthClass:\"DAEMON_VERIFIED\"}` and core's `assessRisk` (policy-evaluation.ts:59-85) supplies the tier from the folded `riskClassifications`. Contract is intact; only the symbol name/package differ from the plan text. NOT BLOCKING.\n- DEPENDENCY EDGE VERIFIED: `apps/daemon/package.json` declares `\"@moe/scheduler\": \"workspace:*\"`; daemon production code already imports it by bare specifier (graph-body-record.ts:27 `import { decodeGraphContent } from \"@moe/scheduler\"`).\n\n(b) T1-a2 task-4e013e45 DONE (I delivered it, commit 659267db). Its reader change is live at bootstrap-policy-authority-reader.ts:192-195 (`exactObject(slices[0], POLICY_SLICE_KEYS) ?? exactObject(slices[0], POLICY_CLASSIFIED_SLICE_KEYS)`).\n\n(c) FINALIZE SEAM UNCHANGED: planning-authority-finalize.ts:49 `PLANNING_SUBMISSION_FINALIZED_EVENT_TYPE=\"PlanningSubmissionFinalized\"`, :205 `commitFinalizedSubmission`, :224-226 one decision via `commitAcceptedLegs`. 227 lines.\n\n(d) GAP STILL OPEN: `git grep -n 'runId' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-services.ts apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` **EXIT 1**. Positive control on the same two paths: `git grep -c 'projectId'` EXIT 0 (7 and 3).\nGAP-CLAIMED-PRESENT check (rail): `apps/daemon/src/planning/policy-risk-leg.ts` and `bootstrap/policy-risk-{record,reader}.ts` DO exist at HEAD — they are a DIFFERENT capability (a HUMAN-approved tier taken from `approval.riskTier`, `truthClass:\"HUMAN_APPROVED\"`, subject = active-graph content hash). No daemon-computed run tier exists. Not a duplicate.\n\n(e) FAN-OUT ENUMERATION (shape-grep, not symbol-grep). `git grep -n '\"PolicyEvaluated\"' HEAD -- apps packages tests` EXIT 0, 22 hits. **EVERY ONE addresses the aggregate `policyAggregateId(projectId)` = `` `${projectId}-policy` `` (bootstrap-sequence.ts:92).** Exact-key pins on the 8-key roster [\"decision\",\"decisionDigest\",\"decisionDigestVersion\",\"decisionMaterial\",\"policyRef\",\"principalId\",\"projectId\",\"sliceRef\"]: writer bootstrap-policy-services.ts:207-216; reader bootstrap-policy-authority-reader.ts:16-19 `ROW_KEYS` + :20-22 `MATERIAL_KEYS`; test pins admission-gate-resolver.test.ts:501-503 (sorted set-equality + a literal digest KAT `f4648891…`) and bootstrap-services.test.ts:558-569. Production readers that scan that aggregate newest-first and FAIL CLOSED on a strict-reader refusal: policy-admission-reader.ts:62-73, approval-policy-ref.ts:89-99, supersession-policy-decision.ts:81-92. No global `readEventsAfter` scanner filters PolicyEvaluated except journal-append.test.ts:49, whose harness never finalizes a run.\n\n**CONSEQUENCE, and the one design decision this measurement forces.** Writing the widened run-scoped row onto `${projectId}-policy` would put a row those three PRODUCTION readers cannot verify at the head of their newest-first scan, and each returns the refusal — admission, plan-approval policyRef and supersession would all break. So the run-scoped `PolicyEvaluated` lands on its OWN run-addressed aggregate (`policy-run-evaluation:<runId>`, the same aggregate-per-subject idiom as `graph-body:<project>:<hash>` and `policy-risk:sha256:<…>`), keeping the SAME event type and the SAME strict reader (task rail 2: no second event type, no parallel ledger). Caller-driven rows keep the exact 8-key roster, so all four exact-key pins above stay green UNEDITED; the reader gains a second run-scoped roster only. Fan-out is therefore fully covered by this plan's roster — no split signal.\n\n(f) SERVER-FACT SOURCES MEASURED (DoD-2 \"derived from the sealed revision, never the payload\"):\n- node content: `folded.state.sealedHashes.graphContentHash` — written by core's finalize reducer (planning-run-submission.ts:47,165) — then `readGraphBody(store, projectId, hash)` (graph-body-record.ts:163) -> `content.nodeAuthority.definitions: readonly NodeDefinition[]` (graph-content-fields.ts:65-67,82). NOTE: `state.graphRevisionRef` is a caller-chosen LABEL (the finalize suite's is the literal `\"graph-revision-authority\"`), so it is NOT the node source. Bodies are on the production path since task-c96ef2d1 (planning-graph-content-ingress.ts:9-16, mandatory `graphContentBytesBase64` at `plan.propose`).\n- policy chain: `content.policyRevision` (sealed graph content) selects the installed slice out of `stateOf(ledger, policyAggregateId(projectId))` — the same selector `validatePolicy` uses at :134-143.\n- `evaluatorVersion`: `POLICY_EVALUATOR_VERSION` (bootstrap-policy-authority.ts). `callerRiskHint: null`. Truth floor confirmed: policy-validation.ts:113 `strongTruth` admits only `DAEMON_VERIFIED`/`HUMAN_APPROVED`.\n\n(g) PORCELAIN on all seven owned paths: EMPTY. Whole tree: 99 dirty paths, all foreign/pre-existing — noted and left untouched.\n\nVERDICT: not blocked. Proceeding to step 2 with the adapted symbol (`deriveNodePropertyFactIds` from `@moe/scheduler`) and the run-addressed aggregate recorded above.",
      "timestamp": "2026-08-29T23:48:57.487Z"
    },
    {
      "id": "comment-ee612b1813574340954d3efb012e59a3",
      "author": "worker-e98a7edb",
      "content": "STEP 5 — MUTATION DRILLS (DoD-5). One degree of freedom each. Backups in `%TEMP%\\a888038d-worker-e98a7edb\\`. THREE of the four targets are UNTRACKED (`?? run-policy-{evaluation,record,leg}.ts` by `git status --porcelain`, taken BEFORE drilling), so `git diff` is blind to them and every restore is proven by SHA256 equality against the out-of-tree backup, in a call SEPARATE from the restore.\n\nBASELINE SHA256 (taken before any mutation):\n- run-policy-leg.ts `20c0177a8200c69b2a4172277791a79db82c3d82203f4344c8f8350dde0d17ef`\n- run-policy-evaluation.ts `722e94df6fb00e8df854da3e55402926abd55072166f2a3ab775f2e93259f3b1`\n- run-policy-record.ts `8b5a6f29b1d4f6b954adc6e750b07b958e9ad8878bc0eb32f1f2e58ca0d139ef`\n- planning-authority-finalize.ts (TRACKED) `b5cef6387fe675376b2003a34cba042cddcaa129091e1b3290297dc33ef25fa6`\n\n**D1 — the leg reads the caller-stated `graphRevisionRef` instead of the SEALED `sealedHashes.graphContentHash`.** RED, `Tests 14 failed | 22 passed (36)`. Named arms: \"carries a daemon-computed tier linked to the run, derived from the SEALED graph\"; \"REFUSES the whole finalize…\" flipped to `expected 'RUN_POLICY_GRAPH_UNAVAILABLE' to be 'RUN_POLICY_UNCLASSIFIABLE'`; \"DIVERGES on the classification alone\"; plus the whole accepted-control block. Restored, SHA256 equal.\n\n**D1b — the plan's LITERAL mutant: read the hash off `request.payload.revision`.** DOES NOT COMPILE: `src/planning/run-policy-leg.ts(69,52): error TS2339: Property 'request' does not exist on type 'RunPolicyLegInput'`. This is deliberate and is the more important half of D1. THE FINDING BEHIND IT: at this seam the two sources are PROVABLY EQUAL — the finalize reducer DERIVES `sealedHashes` from the command's own `revision`, so no fixture can make them differ and a value-level test could never catch a payload read. Per epic rail 7(A) (no divergence input constructible), I changed what the seam PRODUCES instead: `buildRunPolicyLeg` was narrowed from `HandlerContext` (which carries `request.payload`) to an explicit `RunPolicyLegInput` with no payload member, so the mutation is unrepresentable rather than merely untested. D1 above is the value-level control proving the remaining read is load-bearing. Restored, SHA256 equal.\n\n**D2 — `computedTier ?? \"R0\"` in `evaluateRunPolicy`.** RED at BOTH layers, exactly as the plan predicted, `Tests 4 failed | 46 passed (50)`: finalize-seam \"REFUSES the whole finalize when the installed policy cannot tier the run\" (`expected true to be false`) AND \"DIVERGES on the classification alone\" (`expected true to be false`) — the divergence arm can no longer distinguish, which is the point; plus the module-level twins \"refuses UNCLASSIFIABLE with its own code and layer when the newest slice classifies none\" and \"DIVERGES on the classification alone\". Restored, SHA256 equal.\n\n**D3 — drop `runId` from `RUN_POLICY_ROW_EXTRA_KEYS` and from the written row.** RED, `Tests 8 failed`. Reader-side linkage arms named: \"reads a run-scoped row with its linkage and its tier\" (`expected [ …(8) ] to strictly equal [ …(9) ]`), \"refuses a dropped runId leaves neither roster\", \"refuses a foreign run's refs\", \"refuses a tier the evidence does not derive\", \"refuses an emptied run id\"; finalize-side \"carries a daemon-computed tier linked to the run\" (`expected null to be 'run-1'`) and \"DIVERGES…\" (`expected null to be 'R2'`). Restored, SHA256 equal.\n\n**D4 — the seal no longer waits on the evaluation** (delete `if (risk.kind === \"REFUSED\") return refuse(...)`, i.e. exactly the second-decision hazard: seal lands, evaluation does not). RED, `Tests 2 failed | 34 passed (36)`: \"REFUSES the whole finalize when the installed policy cannot tier the run\" and \"DIVERGES on the classification alone\", both `expected true to be false` — the both-ledgers-unchanged assertions catch a seal that landed without its evaluation. Restored, `git diff --exit-code` is NOT used alone here (tracked file, but proven the same way): SHA256 equal to backup.\n\nPOST-DRILL STATE: all four targets `RESTORED-IDENTICAL` by SHA256, and `pnpm exec vitest run --root . --config package.json src/planning/planning-authority-finalize.test.ts src/planning/run-policy-evaluation.test.ts` back to `Test Files 2 passed (2)` / `Tests 50 passed (50)`.\n\nNO GREEN DRILL WAS BANKED. D1b is reported as a finding with the structural fix that answers it, not as a pass.",
      "timestamp": "2026-08-30T00:35:20.452Z"
    },
    {
      "id": "comment-c0f88cc6965f41549775619cfd73be2b",
      "author": "worker-e98a7edb",
      "content": "STEP 6 — GATES, all standalone and unpiped, each exit code from its own run.\n\n**G1** `pnpm --filter @moe/daemon typecheck` — EXIT 0, `$ tsc --project tsconfig.json` echoed. (Mid-session this leg was EXIT 1 on ONE foreign path, `src/daemon-command-registry.test.ts(1271,7) TS2353 'cutoverActivation'`, a ` M` file I never touched whose symbol was absent at HEAD — `git show HEAD:<path> | grep -c cutoverActivation` = 0 vs 1 in the worktree. A peer landed the production property during my session and the leg is now green. Disclosed because it was real when measured.)\n**G1b** `pnpm --filter @moe/control-room typecheck` — EXIT 0, `$ tsc --project tsconfig.json` echoed.\n**G2** owned suites, from apps/daemon: `pnpm exec vitest run --root . --config package.json src/planning/run-policy-evaluation.test.ts src/planning/planning-authority-finalize.test.ts src/bootstrap/bootstrap-services.test.ts src/orchestrator/demo-seed-payloads.test.ts src/orchestrator/demo-seed-plan.test.ts` — EXIT 0, `Test Files 5 passed (5)` / `Tests 140 passed (140)`.\n**G2b** owned scope, whole directories: `… src/planning src/bootstrap src/http src/orchestrator src/work/foundation-context-selection.test.ts` — `Test Files 1 failed | 95 passed (96)` / `Tests 2 failed | 1666 passed (1668)`. The one failure is `src/http/http-listener.test.ts`, which is BASELINE red (same two arms at HEAD) and is byte-identical to HEAD — `git status --porcelain` on it is EMPTY, so it is neither mine nor any peer's worktree edit. Its arms are `AFFORDANCE_PROJECT_MISMATCH @ AFFORDANCE_SURFACE` vs an expected `LISTENER_AFFORDANCE_REQUEST_INVALID @ CONTROL_ROOM_LISTENER` — a listener/surface layer disagreement with no policy content.\n**G3** DoD-4 untouched-path proof: `git diff --stat` EMPTY and `git status --porcelain` EMPTY on bootstrap-services.test.ts, policy-fact-adoption.test.ts, admission-gate-resolver.test.ts, policy-allowance-fixtures.ts, policy-admission-reader.ts, approval-policy-ref.ts, supersession-policy-decision.ts. Those suites run UNEDITED: EXIT 0, `Test Files 4 passed (4)` / `Tests 115 passed (115)`.\n**G4** `pnpm exec vitest run --root . --config package.json` (whole daemon package) — EXIT 1, `Test Files 7 failed | 312 passed (319)` / `Tests 21 failed | 6624 passed | 12 todo (6657)`.\n\n**G4 PATH ATTRIBUTION.** Merge-base baseline measured at HEAD `547c6c55` BEFORE any edit: `Test Files 5 failed | 311 passed (316)` / `Tests 18 failed | 6554 passed`, failing files `activation-run-commit`, `activation-telemetry-launch`, `cutover-activation-marker`, `daemon-command-registry`, `http-listener`, `launch-template-producer`.\n- Carried forward unchanged: `activation-run-commit`, `activation-telemetry-launch`, `http-listener`, `launch-template-producer`.\n- Cleared under me without my touching them: `cutover-activation-marker`, `daemon-command-registry`.\n- NEW and FOREIGN: `src/mcp-http/mcp-http-host.test.ts` and `src/mcp-main.test.ts`, both `expected 3 to be 2` on `EXCLUSION_CASES.length`. `git status --porcelain` shows ` M apps/daemon/src/mcp-tool-allowlist.ts` and ` M apps/daemon/src/mcp-tool-allowlist.test.ts` — a peer widening the MCP exclusion roster 2 -> 3 mid-flight. Nothing in my 26 owned paths is under `mcp*`.\n- NEW and LOAD-DEPENDENT: `src/work/foundation-attempt-windows.test.ts`, the real-exited-process arm, `256732ms` under full-suite contention. Re-run STANDALONE: `Test Files 1 passed (1)` / `Tests 8 passed (8)`.\nThe (HEAD-failures minus merge-base-failures) set intersected with my owned paths is therefore EMPTY.\n\n**SELF-REVIEW (adversarial).**\n(i) CAN ANY CALLER BYTE REACH THE EVALUATION? No, and not by a later edit either. `buildRunPolicyLeg` takes `RunPolicyLegInput` — `{decidedAt, ledger, principalId, projectId, runId, state, store}` — with NO payload member; the D1b drill confirms a payload read is TS2339. Inside, the only reads are `ownValue(state,\"lifecycle\")` and `ownValue(ownValue(state,\"sealedHashes\"),\"graphContentHash\")`, both reducer OUTPUT. `RunPolicyEvaluationInput` has no facts/tier/hint/scope/refs member. `principalId` is transport-authenticated, is an audit field only, and cannot move the tier (the tier comes solely from facts x classifications).\n(ii) CAN THE SEAL LAND WITHOUT THE EVALUATION, OR VICE VERSA? No. Both legs are built before any write and ride one `commitExpectedVersionDecisionLegs`; the risk refusal returns before the plan is composed. D4 is the control. A second concurrent finalize loses on the run aggregate's expectedVersion; a byte-identical resubmit REPLAYS and writes neither twice.\n(iii) DO LEGACY ROWS STILL READ EXACTLY? Yes — arm \"reads a caller-driven row exactly as before, with no run linkage\" drives the PRODUCTION `policy.validate` and asserts the 8-key roster plus `runId === null`, `riskTier === null`; and the three production scanners over `${projectId}-policy` see byte-identical input because the run row lives on `policy-run-evaluation:<runId>`.\n(iv) LINE CAPS, every production file touched: run-policy-evaluation.ts 216, run-policy-record.ts 160, run-policy-leg.ts 96, planning-authority-finalize.ts 244, bootstrap-policy-authority-reader.ts 247, bootstrap-policy-services.ts 228, bootstrap-ledger-vocabulary.ts 232, demo-seed-policy.ts 156, live-dispatch-payloads.ts 390. One file is over the 250 TARGET: `demo-seed-payloads.ts` at 302 — it was **411 at HEAD**, already past the 400 split threshold before this row, and I fixed it BY SUBTRACTION rather than growing it: the three policy bodies moved to the new `demo-seed-policy.ts` and are re-exported so no consumer's import moved. It leaves this row 109 lines smaller than it arrived. `live-dispatch-payloads.ts` 373 -> 390, under 400.\n(v) OTHER EDGE PATHS WALKED, all fail-closed: non-string sealed hash -> `\"\"` -> GRAPH_UNAVAILABLE (arm exists); null/undefined state -> ABSENT (the fold did not seal); zero node definitions -> zero facts -> computedTier null -> UNCLASSIFIABLE; unparseable `decidedAt` -> NaN epoch -> core INPUT_INVALID -> RUN_POLICY_INPUT_INVALID; a definition that fails re-admission -> NODE_UNADMITTED rather than silently contributing no facts. Two subtleties I found and DOCUMENTED in code rather than leaving implicit: `selectInstalledSlice` depends on insertion order (sound because `installPolicy` refuses reinstalling a core slice at its own address, so a core slice is inserted once and never moves), and `runScopedLinkage` compares `riskAssessment` by serialization so key order is part of the match (both sides originate in core's one `assessRisk` literal; fail-closed is the right side to err on for a risk tier).\n\nCOMMIT: 26 owned paths declared via `moe.declare_files` (9 untracked, 17 modified), verified against porcelain; the ~99 other dirty paths in the shared checkout are peers' and were never reverted, stashed or added.",
      "timestamp": "2026-08-30T00:45:52.333Z"
    },
    {
      "id": "comment-1c4723acb6ad4fd29f2d042bf81d4fac",
      "author": "worker-e98a7edb",
      "content": "COMPLETION RECORD — [T1-b producer].\n\n**every finalized run now carries a policy-declared, daemon-computed risk tier or is refused as unclassifiable**\n\nCONSUMER: [T1-c reader] task-f42d5165b747436a9f9b354dbfa37e3d, which reads the run tier off `readPolicyEvaluationAuthority`'s new `runId` / `riskTier` pair and feeds approval.decide_intent's RISK_TIER slot.\nLANDED PREREQUISITES: task-cb0d65ffcbc74776af06e2be5aa21f2e ([T1-a] policy vocabulary) and task-4e013e45d6434641a99483519fbd0da0 ([T1-a2] daemon rosters), both DONE on disk and both re-measured at step 1 rather than assumed.\n\nWHAT SHIPPED. At `commitFinalizedSubmission`, the daemon reads the SEALED graph body named by `folded.state.sealedHashes.graphContentHash`, derives node-property fact ids through @moe/scheduler's `deriveNodePropertyFactIds` (which re-admits each definition through the production codec), stamps them `DAEMON_VERIFIED` with `tier: null`, evaluates them through core's existing `evaluatePolicy` against the newest installed evaluation slice's `riskClassifications`, and appends the resulting `PolicyEvaluated` row as a LEG of the same decision that seals. `computedTier: null` refuses `RUN_POLICY_UNCLASSIFIABLE @ DAEMON_RUN_POLICY` and the seal is refused with it. No default tier exists anywhere on the path.\n\nTWO STRUCTURAL DECISIONS, both forced by measurement, both recorded at step 1 (comment-7bcc3be8):\n1. The run row lives on `policy-run-evaluation:<runId>`, not `${projectId}-policy`. All 22 `\"PolicyEvaluated\"` sites address the project aggregate, and three PRODUCTION readers (policy-admission-reader.ts:62, approval-policy-ref.ts:89, supersession-policy-decision.ts:81) scan it newest-first and FAIL CLOSED on any row the strict reader refuses. Same event type, same strict reader, different address — so all four exact-8-key pins stay green UNEDITED.\n2. The chain is the newest installed slice `derivePolicySliceDigest` accepts, re-digested against its install address. Coverage-free on purpose, so CORE owns the unclassifiable verdict instead of a shadow fence in this module.\n\nFAN-OUT, MEASURED NOT GUESSED. The mandated refusal reaches every finalize driver. Route A (classify the shared `POLICY_SLICE`, moving `POLICY_REF`) measured `94 failed | 222 passed` / `1486 failed` against a `5 failed | 311 passed` / `18 failed` baseline — rejected and restored with `git diff --exit-code` EXIT 0. The adopted route appends a SECOND classifying install, leaving `POLICY_REF` and every digest derived from it byte-identical, and cost 3 extra files. Seven worlds were seeded; `legacyProposedStore()` additionally now carries the graph-body row it always implicitly had. PRODUCTION surfaces both updated with the identical four-entry table at the same digest `fff1cc91…`: the demo seed's `validatablePolicySlice()` and the control room's live dispatch payload. Control-room suite re-measured AT HEAD before and after: `67 failed | 41 passed (108)` both times, so my change adds zero there; restore proven by SHA256 equality in a call separate from the restore.\n\nGATES (comment-c0f88cc6): G1 daemon typecheck EXIT 0 `$ tsc` echoed; G1b control-room typecheck EXIT 0; G2 `Test Files 5 passed (5)` / `Tests 140 passed (140)` EXIT 0; G2b owned scope `1 failed | 95 passed (96)`, the one failure being byte-untouched baseline-red `http-listener.test.ts`; G3 DoD-4 seven policy.validate paths with EMPTY `git diff --stat` and EMPTY porcelain, `Test Files 4 passed (4)` / `Tests 115 passed (115)` unedited; G4 whole package EXIT 1, `Test Files 7 failed | 312 passed (319)` / `Tests 21 failed | 6624 passed`, owned-path intersection against the HEAD-547c6c55 baseline EMPTY. Foreign red disclosed verbatim: mcp-http-host + mcp-main `expected 3 to be 2` from a peer's ` M mcp-tool-allowlist.{ts,test.ts}`; foundation-attempt-windows red only under contention and `8 passed (8)` standalone; four files carried forward from baseline.\n\nDRILLS (comment-ee612b18), SHA256 restore proofs for the three UNTRACKED targets in calls separate from the restore: D1 (leg reads the caller-stated ref) RED 14/36; D1b (the plan's literal payload-read mutant) DOES NOT COMPILE, TS2339 — reported as a FINDING with the structural fix, since at this seam the two sources are provably equal and no fixture could distinguish them; D2 (default tier R0) RED 4/50 at both layers including the divergence arm; D3 (drop runId) RED 8 on the reader linkage arms; D4 (seal without evaluation) RED 2/36 on the both-ledgers-unchanged assertions. All four RESTORED-IDENTICAL. No green drill was banked.\n\nFOR THE ARCHITECT, one item this row could not resolve inside its scope: `apps/daemon/src/orchestrator/demo-seed-payloads.ts` was 411 lines at HEAD, already past the 400 split threshold before this row. I fixed it by SUBTRACTION (the three policy bodies moved to a new `demo-seed-policy.ts`, re-exported so no consumer's import moved), leaving it at 302 — 109 lines smaller than it arrived, but still above the 250 target. A further split is available and is not this row's concern.",
      "timestamp": "2026-08-30T00:46:51.373Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T17:59:11.906Z",
  "updatedAt": "2026-08-30T00:48:16.953Z",
  "contextFetchedBy": [
    "architect-dba58d99",
    "worker-e98a7edb",
    "qa-063c9dff"
  ],
  "dependsOn": [
    "task-cb0d65ffcbc74776af06e2be5aa21f2e",
    "task-4e013e45d6434641a99483519fbd0da0"
  ],
  "metrics": {
    "firstClaimAt": "2026-08-29T22:09:28.671Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 6
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T22:11:29.901Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — caller-driven policy.validate for the run tier: measured circular (bootstrap-policy-services.ts:160-175 takes graphNodeRevisionRefs/scope/action/callerRiskHint from the CALLER and resolves facts via activation-written records); the task description already rules for daemon-owned evaluation at finalize. REJECTED — a second event type or parallel ledger for the run assessment: rail 2 forbids; the EXISTING PolicyEvaluated payload widens (the :200-225 writer already demonstrates the widening idiom with decisionMaterial). REJECTED — evaluating inside commitFinalizedSubmission's body: finalize is at 227 lines and the evaluation is a separable concern; a NEW module owns derivation+evaluation+record-shaping and finalize composes it as a leg of the same decision so the seal and the evaluation land atomically or not at all. DEFERRED DETAIL — exact T1-a symbol shapes: the plan names the ruling's contract (deriveNodePropertyFacts over the sealed revision's node content against the installed chain's riskClassifications); step 1 adapts to the landed surface and reports drift instead of guessing.",
    "codebaseInsights": "PLANNED AHEAD OF ITS DEPS by human directive: dependsOn (cb0d65ff T1-a vocabulary, 4e013e45 T1-a2 rosters) gates WORKING claims, so this plan is executable the moment they land — but T1-a had NOT landed at plan HEAD b050fd5d: `git grep -n 'export function deriveNodePropertyFacts' HEAD -- packages/core/src` EXIT 1 and `riskClassifications` absent from core (unpiped re-run; an earlier piped grep lied — pipe reports head's exit). Step 1 therefore RE-MEASURES everything and hard-gates. MEASURED NOW AND STABLE: finalize seam present (planning-authority-finalize.ts:49 PLANNING_SUBMISSION_FINALIZED_EVENT_TYPE=\"PlanningSubmissionFinalized\", :205 commitFinalizedSubmission, :217 event write); core evaluator present (packages/core/src/policy/policy-evaluation.ts:166 evaluatePolicy(value: unknown)); NO run-scoped evaluation yet (`git grep -n 'runId' HEAD -- bootstrap-policy-services.ts bootstrap-policy-authority-reader.ts` EXIT 1); the PolicyEvaluated writer widens eventPayload at bootstrap-policy-services.ts:200-225 (keys today: decision, decisionDigest, decisionDigestVersion, decisionMaterial, policyRef, principalId, projectId, sliceRef) and the strict reader exact-checks at bootstrap-policy-authority-reader.ts:176-215 (replayed action/decision/policyRevisionRef/graphNodeRevisionRefs, exactObject slice roster [autoApprovalOptIns, rules, sliceRef], evaluatorVersion pin, actor/principal equalities). LINE-CAP FACT: all three daemon files are AT CAP — services 225, reader 220, finalize 227 — so the run-scoped evaluation lives in a NEW module and the at-cap files take only surgical composition lines (rail: <=250, split before 400; fix-by-subtraction if an edit crosses).",
    "risks": "RISK 1 — T1-a drift: this plan is written against the ruling's contract, not landed symbols. Step 1 hard-gates on the greps; if the landed names/shapes differ, adapt and record, and if the CONTRACT differs (e.g. facts derived elsewhere), report blocked naming the delta — do not improvise a parallel derivation. RISK 2 — exact-key fan-out: widening the PolicyEvaluated payload trips every exact-roster consumer (reader :176-215, fixtures, tests, possibly control-room). Step 1 ENUMERATES the fan-out by shape-grep (rail E7(B)/global shape-grep rail): grep for the literal payload key lists and `PolicyEvaluated` across apps+packages+tests; if the enumeration exceeds this plan's roster, that is a SPLIT signal — report, do not absorb silently. RISK 3 — atomicity: the evaluation must land in the SAME decision as the seal (a sealed-but-unevaluated run would be exactly the gap T1 exists to close); use the finalize commit's existing multi-leg/single-decision idiom — never a second command. RISK 4 — the unclassifiable refusal code: mint ONE new stable code in the new module's own layer (e.g. RUN_POLICY_UNCLASSIFIABLE @ DAEMON_RUN_POLICY — worker names it consistently with neighboring rosters), assert code AND layer; never reuse a policy.validate code that means something else. RISK 5 — all three touched daemon files at cap: new logic in the new module only; if a surgical edit pushes any past 250, move lines OUT (fix-by-subtraction precedent). RISK 6 — evaluatePolicy takes `value: unknown` (a full evaluation input): construct its input from server facts only (action/scope/refs from the run, callerRiskHint null, evaluatorVersion the daemon's constant) — an arm presents each caller-supplied variant at the finalize ingress and asserts the exact-record refusal.",
    "keyFiles": [
      "apps/daemon/src/planning/planning-authority-finalize.ts",
      "apps/daemon/src/bootstrap/bootstrap-policy-services.ts",
      "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
      "packages/core/src/policy/policy-evaluation.ts",
      "apps/daemon/src/planning/approval-record-facts.ts"
    ]
  },
  "budget": {
    "wallClockMs": 5400000,
    "warnedAt": "2026-08-29T23:44:05.988Z",
    "escalatedAt": "2026-08-29T23:44:05.988Z"
  },
  "planApprovedAt": "2026-08-29T22:11:32.010Z",
  "workStartedAt": "2026-08-29T23:44:31.517Z",
  "declaredFiles": [
    "apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts",
    "apps/daemon/src/planning/run-policy-evaluation.ts",
    "apps/daemon/src/planning/run-policy-evaluation.js",
    "apps/daemon/src/planning/run-policy-evaluation.test.ts",
    "apps/daemon/src/planning/run-policy-record.ts",
    "apps/daemon/src/planning/run-policy-record.js",
    "apps/daemon/src/planning/run-policy-leg.ts",
    "apps/daemon/src/planning/run-policy-leg.js",
    "apps/daemon/src/planning/planning-authority-finalize.ts",
    "apps/daemon/src/planning/planning-authority-finalize.test.ts",
    "apps/daemon/src/planning/approval-activation.test.ts",
    "apps/daemon/src/planning/expansion-release-selector-test-fixtures.ts",
    "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
    "apps/daemon/src/bootstrap/bootstrap-policy-services.ts",
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
    "apps/daemon/src/bootstrap/bootstrap-journey-fixtures.ts",
    "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
    "apps/daemon/src/bootstrap/bootstrap-replay-bytes.test.ts",
    "apps/daemon/src/http/affordance-read-planning.test.ts",
    "apps/daemon/src/http/affordance-read.test.ts",
    "apps/daemon/src/http/http-listener-planning-run.test.ts",
    "apps/daemon/src/work/foundation-context-selection.test.ts",
    "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
    "apps/daemon/src/orchestrator/demo-seed-policy.ts",
    "apps/daemon/src/orchestrator/demo-seed-policy.js",
    "apps/control-room/src/live/live-dispatch-payloads.ts"
  ],
  "reviewStartedAt": "2026-08-30T00:48:11.225Z",
  "verification": {
    "command": "cd apps/daemon && pnpm exec vitest run --root . --config package.json src/planning/run-policy-evaluation.test.ts src/planning/planning-authority-finalize.test.ts src/bootstrap/bootstrap-services.test.ts src/orchestrator/demo-seed-payloads.test.ts src/orchestrator/demo-seed-plan.test.ts",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  5 passed (5)\n      Tests  140 passed (140)\n   Start at  03:46:17\n   Duration  3.55s (transform 6.86s, setup 0ms, import 9.06s, tests 2.27s, environment 0ms)\n\nFRESH_VERIFICATION_EXIT=0",
    "reportedAt": "2026-08-30T00:48:11.225Z"
  },
  "filesModified": [
    "apps/daemon/src/planning/planning-authority-finalize.ts",
    "apps/daemon/src/planning/run-policy-evaluation.ts",
    "apps/daemon/src/planning/run-policy-evaluation.js",
    "apps/daemon/src/planning/run-policy-evaluation.test.ts",
    "apps/daemon/src/planning/run-policy-record.ts",
    "apps/daemon/src/planning/run-policy-record.js",
    "apps/daemon/src/bootstrap/bootstrap-policy-services.ts",
    "apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts",
    "apps/daemon/src/planning/planning-authority-finalize.test.ts",
    "apps/daemon/src/planning/run-policy-leg.ts",
    "apps/daemon/src/planning/run-policy-leg.js",
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
    "apps/daemon/src/bootstrap/bootstrap-journey-fixtures.ts",
    "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
    "apps/daemon/src/bootstrap/bootstrap-replay-bytes.test.ts",
    "apps/daemon/src/planning/approval-activation.test.ts",
    "apps/daemon/src/planning/expansion-release-selector-test-fixtures.ts",
    "apps/daemon/src/http/affordance-read-planning.test.ts",
    "apps/daemon/src/http/affordance-read.test.ts",
    "apps/daemon/src/http/http-listener-planning-run.test.ts",
    "apps/daemon/src/work/foundation-context-selection.test.ts",
    "apps/daemon/src/orchestrator/demo-seed-payloads.ts",
    "apps/control-room/src/live/live-dispatch-payloads.ts",
    "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
    "apps/daemon/src/orchestrator/demo-seed-policy.ts",
    "apps/daemon/src/orchestrator/demo-seed-policy.js"
  ],
  "completionSummary": "every finalized run now carries a policy-declared, daemon-computed risk tier or is refused as unclassifiable.\n\nAt `commitFinalizedSubmission` the daemon reads the SEALED graph body named by `folded.state.sealedHashes.graphContentHash`, derives node-property fact ids through @moe/scheduler's `deriveNodePropertyFactIds`, stamps them DAEMON_VERIFIED with `tier: null`, evaluates through core's existing `evaluatePolicy` against the newest installed slice's `riskClassifications`, and appends the `PolicyEvaluated` row as a LEG of the same decision that seals. `computedTier: null` refuses RUN_POLICY_UNCLASSIFIABLE @ DAEMON_RUN_POLICY and the seal refuses with it. No default tier exists on the path.\n\nCONSUMER [T1-c reader] task-f42d5165b747436a9f9b354dbfa37e3d reads the new `runId`/`riskTier` pair off `readPolicyEvaluationAuthority`. PREREQUISITES task-cb0d65ff and task-4e013e45, both DONE and both re-measured at step 1.\n\nADAPTED, not assumed: T1-a landed as `deriveNodePropertyFactIds` in @moe/scheduler, not `deriveNodePropertyFacts` in core (that grep EXIT 1). It returns IDs only by design, so this row stamps the truth class and core supplies the tier.\n\nTWO FORCED DECISIONS. (1) The run row lives on `policy-run-evaluation:<runId>`, same event type and same strict reader: all 22 PolicyEvaluated sites address `${projectId}-policy` and three PRODUCTION readers scan it newest-first and FAIL CLOSED, so a widened row there would break admission, plan-approval policyRef and supersession. All four exact-8-key pins are green UNEDITED. (2) The chain selector is coverage-free so CORE owns the unclassifiable verdict rather than a shadow fence.\n\nFAN-OUT MEASURED: classifying the shared POLICY_SLICE reds 94 files / 1486 tests (baseline 5/18); appending a second classifying install leaves POLICY_REF byte-identical and cost 3. Seven test worlds seeded; PRODUCTION demo seed and control-room live payload both carry the identical table at digest fff1cc91. Control-room baseline re-measured at "
}