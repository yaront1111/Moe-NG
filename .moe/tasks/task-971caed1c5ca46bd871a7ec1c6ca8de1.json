{
  "id": "task-971caed1c5ca46bd871a7ec1c6ca8de1",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "A goal reports which required variables are unset, and the repository lists their names in .env.example",
  "description": "SPIDR grandchild C of task-72b22fcf9da54b3b91dabf5b7392aff1 (architect-aa54e231, 2026-09-05). Owns the parent's DoD 3 and 4. dependsOn grandchild B task-fd2d438f29d64830a2bd6110f2ded78b, which adds the field this row READS - without it there are no names to report, and the parent's DoD 3 is unsatisfiable (measured: no variable-name field exists on a deployment requirement anywhere).\n\nDeliberately does NOT depend on grandchild A: the unset report needs the NAMES of what is set, which the existing read already returns as metadata (apps/daemon/src/environment/environment-store.ts `readEnvironmentVariables`), not the plaintext values A delivers. A and C can run in parallel.\n\nWHAT THIS ROW OWNS: reading the required names off the approved contract, reporting which are unset for a given environment, and putting those names into the repository's `.env.example`.\n\n`.env.example` ALREADY EXISTS AND ALREADY CARRIES VALUES - this is the trap and it is measured, not guessed. It is generated by the CONTROLLED-PROFILE generator, not by the node lander the parent's description names:\n  apps/daemon/src/repository/controlled-profile/controlled-profile-root-templates.ts:204 maps `.env.example` to an ENV_EXAMPLE template, whose current content includes a real placeholder value (`POSTGRES_PASSWORD=...`, and README copy at :113 telling the operator to `cp .env.example .env   # then replace every CHANGE_ME`).\n  apps/daemon/src/repository/controlled-profile/controlled-profile-generator.test.ts:32 pins the file by SHA256 in a golden manifest - editing the template MOVES that hash and the pin must move with it.\n  apps/daemon/src/repository/deployment/deployment-infrastructure-generator.test.ts:305-306 READS a placeholder back out of `.env.example` with `/POSTGRES_PASSWORD=(\\S+)/`, so a rewrite that drops the placeholder line reds a test in a different directory.\nSo this row EXTENDS an existing generated file with a NAMES-ONLY section; it does not create one, and it must not break the profile's own placeholder contract. Decide deliberately whether the required names go in the same file or an adjacent one, and say which and why.\n\nCONCURRENCY WARNING: `controlled-profile-generator.ts` and its assets were being actively built by another session on 2026-09-05 (the ASSEMBLY HALF and the lockfile-asset mint). Re-measure at claim, expect the golden manifest hash to have moved, and never revert peer work.\n\nNOT IN SCOPE: the plaintext delivery and the spawn merge (grandchild A); the Environments screen and the goal's unset count in the browser (the parent's child 3 - this row's report is a daemon-side read, not UI).\n\nVERIFY: `pnpm --filter @moe/daemon test`.",
  "definitionOfDone": [
    "The required variable names are read from the APPROVED CONTRACT's deployment requirements (the field grandchild B added), never from a hand-kept list. Assert the read against a contract fixture carrying names, and assert that a contract with no names yields an empty required set rather than throwing.",
    "A goal reports which required variables are UNSET for a given environment. Assert the exact unset SET for a goal with two required names where one is set - set equality, not toContain, and assert the SET one is absent from the report so the arm fails if the report simply echoes the requirement list.",
    "`.env.example` carries every required NAME and NO VALUE for them. Assert the generated bytes contain each required name and that no required name is followed by a value; this file is committed and pushed through the repository, so a value here is a value published.",
    "THE EXISTING PROFILE CONTRACT SURVIVES: the profile's own `POSTGRES_PASSWORD` placeholder is still present and still matches `/POSTGRES_PASSWORD=(\\S+)/`, because apps/daemon/src/repository/deployment/deployment-infrastructure-generator.test.ts:305-306 reads it back. Assert it, and move the golden SHA256 pin at controlled-profile-generator.test.ts:32 to the new measured value rather than deleting the pin.",
    "A contract with NO deployment-variable names generates the `.env.example` byte-identically to today - assert byte-identity, so this feature cannot change the profile for every project that does not use it.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/daemon test`, `pnpm typecheck`."
  ],
  "taskRails": [
    "NAMES ONLY. `.env.example` is committed and pushed to the project's repository; a value written there is a value published. This is the single most common way this class of feature leaks.",
    "EXTEND, DO NOT REPLACE, the existing generated `.env.example`. It already carries the profile's own placeholder, another directory's test reads that placeholder back, and a golden SHA256 pins the file.",
    "MOVE THE GOLDEN PIN, NEVER DELETE IT. controlled-profile-generator.test.ts:32 pins .env.example by SHA256; re-measure and update the hash, and never soften the manifest to a looser assertion.",
    "READ NAMES FROM THE APPROVED CONTRACT, never a hand-kept list - that is the whole point of grandchild B.",
    "A PROJECT THAT NAMES NO VARIABLES MUST BE UNCHANGED, asserted as byte-identity of the generated file.",
    "NO UI: the Environments screen and the browser's unset count are the parent's child 3."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE GRANDCHILD B'S LANDED SHAPE BEFORE NAMING ANYTHING. This row READS a field that grandchild B (task-fd2d438f29d64830a2bd6110f2ded78b) adds, and at plan time B was claimed but UNPLANNED — so this plan deliberately does not name the field. Your dependsOn gates the WORKING claim until B is DONE, so the real shape exists by the time you run.\n\nRun and read the OUTPUT:\n  (a) `grep -rn 'variableName\\|environmentVariable\\|requiredVariables\\|envVar' apps/control-room/src/v2/goals/gate1-contract-shape.ts apps/daemon/src/planning packages/contracts/src` — at plan time this returned ZERO across the tree, which is why B exists. It must now return B's field. If it is still empty, B has not landed: `moe.report_blocked` with blockedOnTaskIds [\"task-fd2d438f29d64830a2bd6110f2ded78b\"] and stop.\n  (b) Read B's `completionSummary` and `reviewSummary` from get_context.epicSiblings rather than re-deriving its work. Record the field's exact NAME, whether it is OPTIONAL or required, and — critically — WHICH SECTIONS carry it (see the next paragraph).\n\nTHE SHARED-VALIDATOR FACT THAT DECIDES HOW YOU READ. Measured at plan time: `apps/control-room/src/v2/goals/gate1-contract-shape.ts:167-173` uses the SAME `requirement` validator for SIX sections — functionalRequirements, nonFunctionalRequirements, securityPrivacyRequirements, technologyRequirements, uxAccessibilityRequirements and deploymentRequirements. So B either put the field on ALL SIX or gave `deploymentRequirements` its own validator. Which one B chose changes this row: if all six carry it, you MUST filter to deployment requirements or you will collect variable names a security requirement happened to declare.\n\nAND DO NOT READ THROUGH `gate1Requirements()`. That helper (:182-189) FLATTENS all six sections into one array, so an entry's section of origin is lost and a deployment requirement is indistinguishable from a technology one. Read `revision.deploymentRequirements` directly. This is the single easiest way to get this row silently wrong — the flattened helper is the obvious thing to reach for and it compiles fine.\n\nAlso re-measure the `.env.example` seam, which was mid-change by another session on 2026-09-05:\n  `grep -n 'env.example' apps/daemon/src/repository/controlled-profile/controlled-profile-root-templates.ts apps/daemon/src/repository/controlled-profile/controlled-profile-generator.test.ts apps/daemon/src/repository/deployment/deployment-infrastructure-generator.test.ts`\nExpect the template map at root-templates.ts:204, the golden SHA256 pin at generator.test.ts:32, and the placeholder read-back at deployment-infrastructure-generator.test.ts:305-306. Expect the hash to have MOVED; note peer dirt and continue, never revert it.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/gate1-contract-shape.ts",
        "apps/daemon/src/repository/controlled-profile/controlled-profile-root-templates.ts",
        "apps/daemon/src/repository/controlled-profile/controlled-profile-generator.test.ts"
      ]
    },
    {
      "stepId": "step-2",
      "description": "RED ARMS FIRST for the required-names read and the unset report, in a new test beside the module you create in step 3. Write them and watch them FAIL.\n\nTHE UNSET SET IS A SET DIFFERENCE, and both sides are measured: the REQUIRED names come from the approved contract's `deploymentRequirements` (B's field), and the SET names come from the existing store read — `readEnvironmentVariables` returns `EnvironmentVariableRead[]`, each `{ fingerprintSha256, isSet: true, name, updatedAt }` (apps/daemon/src/environment/environment-contracts.ts:118-123). It returns only variables that ARE set, each carrying its `name`, so unset = required MINUS the names it returns. No value is read anywhere in this row.\n\nArms:\n  1. THE DoD-2 CASE, exactly as specified: two required names, one of them set. Assert the unset set by SET EQUALITY, and separately assert the SET one is ABSENT from the report. Without that second assertion the arm passes for a report that simply echoes the requirement list — which is the most likely wrong implementation.\n  2. NO NAMES IS EMPTY, NOT AN ERROR. A contract whose deployment requirements name no variables yields an empty required set and an empty unset set — it must not throw and must not report everything as unset.\n  3. WRONG-SECTION NAMES ARE NOT COLLECTED. Build a contract where a NON-deployment requirement (say a technologyRequirement) carries variable names, and assert they do NOT appear in the required set. If B put the field on all six sections this arm is the one that catches a read through `gate1Requirements()`; if B gave deploymentRequirements its own validator, write the arm anyway — it pins the intended scope against a later widening.\n  4. AN ENVIRONMENT WITH NOTHING SET reports every required name as unset.\n\nAssert against the production read, never a helper that re-derives the difference.",
      "status": "PENDING",
      "affectedFiles": []
    },
    {
      "stepId": "step-3",
      "description": "THE REQUIRED-NAMES READ AND THE UNSET REPORT — a new focused module under apps/daemon/src/environment/ (or the deployment area if that reads better; say which and why), plus its one-line `.js` bridge, copying the shape of apps/daemon/src/environment/environment-store.js.\n\nExport two things and keep them separate:\n  - `requiredVariableNames(revision)` — reads `revision.deploymentRequirements` DIRECTLY (never `gate1Requirements()`, which flattens six sections and loses the origin), collects B's field, dedupes, and returns names in a stable sorted order. Sorted because this feeds `.env.example` in step 4, and an unstable order would move the golden SHA256 pin on every unrelated contract edit.\n  - `unsetVariableNames(required, read)` — the set difference against the names `readEnvironmentVariables` returns. Keep it a pure function over its two arguments so it is testable without a store, and so the store read stays the single seam that touches durable bytes.\n\nNO VALUE IS READ, RETURNED OR LOGGED anywhere in this module. This row works entirely in names: `EnvironmentVariableRead` carries `name` and `fingerprintSha256` and no value, and grandchild A owns the only value-returning path. If you find yourself needing a value here, stop — the requirement was misread.\n\nHandle the shapes a real contract presents: a requirement whose field is absent, an empty list, a duplicate name across two requirements (dedupe), and a name that is not a plausible environment variable. For that last case decide deliberately whether to drop it or surface it, and say which in the step note — do not silently pass through whatever the contract text held.\n\nKeep the module well under 250 lines.",
      "status": "PENDING",
      "affectedFiles": []
    },
    {
      "stepId": "step-4",
      "description": "`.env.example` CARRIES THE NAMES, AND NOTHING ELSE. EXTEND the existing generated file — do not create one and do not rewrite it.\n\nWhat is already there, measured: `apps/daemon/src/repository/controlled-profile/controlled-profile-root-templates.ts:204` maps `.env.example` to an `ENV_EXAMPLE` template that already carries the profile's OWN placeholder (`POSTGRES_PASSWORD=...`), and README copy at :113 tells the operator `cp .env.example .env   # then replace every CHANGE_ME`. The docker-compose template consumes `${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}`.\n\nTHREE THINGS MUST ALL HOLD AFTERWARDS:\n  (a) Every required name from step 3 appears in the file, as a NAME ONLY — no value, no placeholder value, no CHANGE_ME token beside it. Assert that no required name is followed by a value. This file is committed and pushed into the project's repository, so a value written here is a value published; that is the single most common way this class of feature leaks.\n  (b) THE PROFILE'S OWN PLACEHOLDER SURVIVES UNCHANGED. `apps/daemon/src/repository/deployment/deployment-infrastructure-generator.test.ts:305-306` reads it back out with `/POSTGRES_PASSWORD=(\\S+)/` from a DIFFERENT directory, so dropping or reformatting that line reds a test that never mentions this row. The profile's placeholder and the contract's required names are two different things living in one file — keep them visibly separate (a commented section for the contract-derived names is the obvious shape) and say in the step note why they are not merged.\n  (c) THE GOLDEN PIN MOVES, IT DOES NOT SOFTEN. `controlled-profile-generator.test.ts:32` pins `.env.example` by SHA256 in the golden manifest. Re-measure the hash and update the literal. Never replace the pin with a looser assertion to avoid re-measuring it — the pin is what makes the manifest evidence.\n\nAND THE NO-OP CASE, which is DoD 5 and the reason this feature cannot regress every project: a contract naming NO variables must generate `.env.example` BYTE-IDENTICALLY to today. Assert byte-identity, and confirm the golden hash for that case is the pre-existing one — if the no-name path moves the hash, the extension is unconditional and must be made conditional.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/repository/controlled-profile/controlled-profile-root-templates.ts",
        "apps/daemon/src/repository/controlled-profile/controlled-profile-generator.test.ts",
        "apps/daemon/src/repository/deployment/deployment-infrastructure-generator.test.ts"
      ]
    },
    {
      "stepId": "step-5",
      "description": "GATES, DRILLS, RECORD.\n\nGATES. Each STANDALONE and FOREGROUND — no pipe, no `;` chain, no `&&` (a piped run reports tail's exit code; a chain reports only the last command's):\n  1. `pnpm --filter @moe/daemon test`\n  2. `pnpm typecheck`\nREAD THE OUTPUT, NOT THE EXIT CODE: a missed `--filter` exits 0, and \"No test files found\" / \"0 passed\" are also exit 0. The discriminator is `Test Files N passed` with N nonzero — quote it for both. A bare `pnpm test` covers no `apps/**` and is not evidence. Name `pnpm --filter @moe/daemon test` as the complete_task verification command.\n\nRun the two touched suites targeted first for a fast signal: the controlled-profile generator suite and the deployment infrastructure suite. The daemon suite is slow (~22 min) and flaky on this branch (two back-to-back baselines gave `17 failed | 416 passed` then `10 failed | 423 passed`, dominated by 30000ms timeouts), so attribute any red by PATH per global rail 3 rather than by whole-suite delta.\n\nDRILL 1, THE LEAK. Make the `.env.example` extension emit `NAME=something` instead of a bare name. The DoD-3 arm MUST go red. If it stays green it is only checking that the name is present, which a values-carrying file also satisfies — strengthen it.\nDRILL 2, THE CROSS-DIRECTORY READER. Remove the profile's `POSTGRES_PASSWORD` line. `deployment-infrastructure-generator.test.ts` must go red. This proves the cross-directory coupling is real and that you did not quietly reformat the placeholder.\nDRILL 3, THE WRONG-SECTION READ. Point `requiredVariableNames` at `gate1Requirements()` instead of `revision.deploymentRequirements`. Step 2's arm 3 must go red. This is the mistake most likely to survive review, so prove the arm catches it.\n\nRESTORE PROOF, global rail 13. `git status --porcelain` the exact mutated paths BEFORE drilling. The three repository files are TRACKED, so an empty `git diff -- <path>` is a valid restore proof — run it per path in a call SEPARATE from the restore. Any NEW module you created in step 3 is UNTRACKED and `git diff` is BLIND to it: back it up out of tree (%TEMP%, suffixed with your worker id) before mutating and prove the restore by SHA256 equality against that backup.\n\nCOMPLETION RECORD states: B's field name and which sections carry it; that the read goes through `revision.deploymentRequirements` and NOT `gate1Requirements()`, with the reason; where the required names live in `.env.example` and why they are kept separate from the profile's own placeholder; the OLD and NEW golden SHA256 with the command that measured it; that the no-name case is byte-identical to today; and the drill outcomes with restore proofs.",
      "status": "PENDING",
      "affectedFiles": []
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-72b22fcf9da54b3b91dabf5b7392aff1",
  "dependsOn": [
    "task-d0c63cd8848a4da18ad3fbb8eb1d95c3",
    "task-fd2d438f29d64830a2bd6110f2ded78b"
  ],
  "priority": "HIGH",
  "order": 102.3,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T19:10:57.448Z",
  "updatedAt": "2026-09-05T19:16:37.596Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T19:13:26.060Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 4
  },
  "priorHandoffs": [
    {
      "whatIsDone": "Nothing implemented. Row was filed by architect-aa54e231 minutes earlier as SPIDR grandchild C of task-72b22fcf, with a measured description, DoD and rails already in place. No plan submitted.",
      "whatRemains": "Plan this row AFTER grandchild B (task-fd2d438f29d64830a2bd6110f2ded78b) has a plan, so the field name, shape and optionality B chose are known rather than guessed. Then plan the two halves this row owns: the unset-set report read from the approved contract, and the names-only .env.example extension.",
      "pitfalls": "`.env.example` already exists, already carries a real POSTGRES_PASSWORD placeholder, and is pinned by SHA256 at controlled-profile-generator.test.ts:32 — and deployment-infrastructure-generator.test.ts:305-306 reads that placeholder back from another directory. It is generated by the CONTROLLED-PROFILE generator, not the node lander the parent's description names. Extend, never replace; move the golden pin, never soften it. That generator was being actively built by another session on 2026-09-05, so expect the hash to have moved.",
      "openQuestions": "Whether the required names belong in the existing .env.example or an adjacent file — the row asks for a deliberate decision with its reason, not a default.",
      "releasedAt": "2026-09-05T19:13:51.400Z",
      "releasedBy": "architect-aa54e231",
      "reason": "Build order, not queue order (global rail 17). This row CONSUMES the variable-names field that grandchild B (task-fd2d438f29d64830a2bd6110f2ded78b) adds, and B is still unplanned. Planning the consumer first would mean naming a field whose shape, name and optionality B has not chosen — exactly the plan-against-an-unmeasured-symbol hazard. Released in place and still claimable; claiming B now and planning it first."
    }
  ],
  "stepsCompleted": [],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-05T19:16:35.248Z",
  "planningNotes": {
    "approachesConsidered": "RELEASING AGAIN TO WAIT FOR B — rejected after the queue handed this row straight back. A release/reclaim loop burns sessions and delivers nothing, and the real protection is already structural: dependsOn withholds the WORKING claim until B is DONE, and step 1 hard-stops with report_blocked if the field is absent. Planning the consumer against a MEASURED SEAM rather than a named symbol is what makes that safe.\n\nREADING THROUGH `gate1Requirements()` — rejected on measurement: it flattens six sections and loses the origin, so it would collect variable names declared on non-deployment requirements. Named explicitly in the plan and drilled, because it is the convenient wrong answer.\n\nPUTTING THE REQUIRED NAMES IN A NEW ADJACENT FILE instead of `.env.example` — left as a deliberate decision for the worker with its reason required, per the row's own description. Extending the existing file is the default because the operator already has one file to copy and a second would need its own explanation in the README; but the profile's placeholder and the contract's names are genuinely different things, and the plan requires them kept visibly separate rather than interleaved.\n\nA SINGLE COMBINED FUNCTION over store + contract — rejected in favour of `requiredVariableNames(revision)` plus a pure `unsetVariableNames(required, read)`. The pure difference is testable without a store, and it keeps the store read as the only seam touching durable bytes.",
    "codebaseInsights": "MEASURED 2026-09-05 by architect-aa54e231 (who also filed this row) on branch moe/work-2026-09-04.\n\nBUILD ORDER, HANDLED WITHOUT NAMING B'S FIELD. This row consumes the variable-names field grandchild B (task-fd2d438f) adds. I released the row once to plan B first (global rail 17); the queue returned it, and B was meanwhile claimed by architect-fa3a3e17 and is being planned. Rather than loop on releases, this plan NEVER NAMES B's field: step 1 measures the landed shape and hard-stops with report_blocked if it is absent. dependsOn already gates the WORKING claim until B is DONE, so the worker sees the real thing.\n\nTHE SHARED `requirement` VALIDATOR IS THE FACT THAT SHAPES THIS ROW. apps/control-room/src/v2/goals/gate1-contract-shape.ts:167-173 applies the SAME `requirement` function to SIX sections: functionalRequirements, nonFunctionalRequirements, securityPrivacyRequirements, technologyRequirements, uxAccessibilityRequirements and deploymentRequirements. So B could not add a deployment-only field in one edit; it either put the field on all six or gave deploymentRequirements its own validator. Either outcome is workable here, but the READ must scope to deployment requirements, and step 2's arm 3 pins that regardless of which B chose. I posted this measurement to architect-fa3a3e17 in #architects because it changes B's own shape.\n\nDO NOT READ THROUGH `gate1Requirements()`. That helper (gate1-contract-shape.ts:182-189) spreads all six sections into ONE array, so an entry's section of origin is lost and a deployment requirement is indistinguishable from a technology one. It is the obvious helper to reach for, it compiles, and it silently collects names from five sections this row must ignore. Read `revision.deploymentRequirements` directly. Drill 3 exists to prove the arm catches this exact substitution.\n\nTHE SET-DIFFERENCE INPUTS ARE BOTH MEASURED AND NEITHER CARRIES A VALUE. `readEnvironmentVariables` returns `EnvironmentVariableRead[]`, each `{ fingerprintSha256, isSet: true, name, updatedAt }` (apps/daemon/src/environment/environment-contracts.ts:118-123) — only variables that ARE set, each with its name and no value. So unset = required minus those names, and this row never needs the plaintext path grandchild A owns.\n\n`.env.example` ALREADY EXISTS, CARRIES A REAL VALUE, AND IS PINNED. It is generated by the CONTROLLED-PROFILE generator, not the node lander the parent's description named: controlled-profile-root-templates.ts:204 maps `.env.example` to an ENV_EXAMPLE template holding `POSTGRES_PASSWORD=...`, with README copy at :113 (`cp .env.example .env   # then replace every CHANGE_ME`) and docker-compose consuming `${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}`. controlled-profile-generator.test.ts:32 pins the file by SHA256 in a golden manifest. And deployment-infrastructure-generator.test.ts:305-306 reads the placeholder back out with `/POSTGRES_PASSWORD=(\\S+)/` FROM ANOTHER DIRECTORY, so a reformat of that line reds a suite that never mentions this row. This row therefore EXTENDS under a golden pin and a cross-directory reader.\n\nCONCURRENCY: controlled-profile-generator.ts and its assets were being actively built by another session on 2026-09-05 (the assembly half and the lockfile-asset mint, seen in #general). Expect the golden hash to have moved by claim time; note peer dirt and continue.\n\nSORTED OUTPUT IS LOAD-BEARING, not tidiness: the required names feed a file pinned by SHA256, so an unstable order would move the golden hash on unrelated contract edits.",
    "risks": "1. READING THE FLATTENED HELPER. `gate1Requirements()` compiles, returns requirements, and silently mixes in five other sections. If B put the field on all six sections (which the shared validator makes likely), this collects variable names a security or technology requirement declared. Step 2 arm 3 and drill 3 exist for exactly this.\n\n2. A VALUE IN `.env.example`. The file is committed and pushed, so a value there is published — and because it ALREADY contains a real placeholder, \"it has values in it already\" is an available and wrong justification. DoD 3 requires asserting no required name is followed by a value; drill 1 proves the arm is not merely checking presence.\n\n3. THE CROSS-DIRECTORY PLACEHOLDER READER. deployment-infrastructure-generator.test.ts:305-306 parses `POSTGRES_PASSWORD` out of `.env.example` from another directory. A tidy rewrite of the template reds a suite that never mentions this row, and the failure reads as unrelated. Drill 2 makes the coupling explicit.\n\n4. SOFTENING THE GOLDEN PIN. When the SHA256 at generator.test.ts:32 reds, replacing it with a looser assertion is the quick fix and destroys the manifest's value. The plan requires moving the hash and forbids weakening the pin.\n\n5. AN UNCONDITIONAL EXTENSION. If the names section is emitted even when the contract names nothing, every project's `.env.example` changes and the no-op DoD fails. The plan requires byte-identity for the no-name case and says explicitly that a moved hash there means the extension must be made conditional.\n\n6. UNSTABLE NAME ORDER moving the golden hash on unrelated edits — hence the sorted, deduped output.\n\n7. B NOT LANDED. Step 1 hard-stops with blockedOnTaskIds rather than inventing a field name. Writing B's contract-shape edit here would be a silent duplicate colliding with architect-fa3a3e17, who holds that row.\n\n8. CONCURRENCY on the controlled-profile generator: another session was mid-build the same day. Re-measure, expect a moved hash, never revert peer work.",
    "keyFiles": [
      "apps/control-room/src/v2/goals/gate1-contract-shape.ts",
      "apps/daemon/src/environment/environment-contracts.ts",
      "apps/daemon/src/environment/environment-store.ts",
      "apps/daemon/src/repository/controlled-profile/controlled-profile-root-templates.ts",
      "apps/daemon/src/repository/controlled-profile/controlled-profile-generator.test.ts",
      "apps/daemon/src/repository/deployment/deployment-infrastructure-generator.test.ts"
    ]
  },
  "planApprovedAt": "2026-09-05T19:16:37.596Z"
}