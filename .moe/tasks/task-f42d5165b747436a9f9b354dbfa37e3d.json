{
  "id": "task-f42d5165b747436a9f9b354dbfa37e3d",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[T1-c reader] Expose riskAssessment and run linkage through the strict PolicyEvaluationAuthority reader and fill the RISK_TIER slot in readApprovalRecordFacts so approval.decide_intent can mint",
  "description": "Third of the T1 chain (ruling comment-c86c35cb on task-ba102165, condition 2: \"PolicyEvaluationAuthority exposes riskAssessment through the strict reader; the seam's APPROVAL_INTENT_RISK_TIER_UNAVAILABLE refusal flips only when a computed tier for THIS run exists\"). UNPARK: [T1-b producer] DONE on disk AND task-ba102165 DONE (readApprovalRecordFacts present: `git grep -n 'export function readApprovalRecordFacts' HEAD -- apps/daemon/src/planning/` EXIT 0).\n\nTHE GAP, MEASURED AT HEAD 5b3d7313: `PolicyEvaluationAuthority` (apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts:35-48) exposes {action, decision, decisionDigest, decisionDigestVersion, graphNodeRevisionRefs, ok, policyRef, principalId, projectId, scope, sliceRef} — it replays the record but DROPS riskAssessment (comment-218ba48d on ba102165) and has no run linkage. approval-intent.ts:194 refuses APPROVAL_MISSING_FACT_CODES[0] = APPROVAL_INTENT_RISK_TIER_UNAVAILABLE first; after ba102165 the seam consults `readApprovalRecordFacts` which names the first missing fact.\n\nDELIVER: (1) the strict reader returns `riskAssessment` (computedTier/effectiveTier/usedFactIds/callerRiskHint, replay-verified like every other field) and `runId`, and a run-scoped selector `readRunPolicyEvaluation(store, {projectId, runId})` that answers the ONE PolicyEvaluated the producer wrote for THIS run (never the newest project-wide row; ambiguity refuses); (2) readApprovalRecordFacts derives `riskTier` = that assessment's computedTier for the run, absent -> `missing: APPROVAL_INTENT_RISK_TIER_UNAVAILABLE` with the reader's code+layer as upstream; (3) with riskTier + applicablePolicyRef (ba102165) + stepUpAuthRef (3b61860f) + budgetRef (61a2e8ad chain) all present the seam composes and MINTS the ApprovalDecisionRecord through core's validateApprovalRecord — this row proves the tier slot and the ordering, and mints only if the other three have landed (otherwise it proves the refusal advanced past RISK_TIER to the next missing fact).",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, in a row comment with `git rev-parse HEAD` quoted, greps unpiped with exit codes plus a positive control: [T1-b] DONE and PolicyEvaluated records carry runId + riskAssessment at HEAD; task-ba102165 DONE and readApprovalRecordFacts present; the strict reader still drops riskAssessment (`git grep -n 'riskAssessment' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` EXIT 1 — EXIT 0 = adopt/verify/report duplicate); every exact-shape pin of PolicyEvaluationAuthority enumerated (policy-admission-reader.ts:70-77 consumer, fixtures, tests) and owned.",
    "The strict reader returns replay-verified `riskAssessment` and `runId`; `readRunPolicyEvaluation(store, {projectId, runId})` answers exactly the producer's row for that run (a second row for another run is never selected — arm), refuses with exact code+layer when absent/ambiguous/unverified; policy-admission-reader.ts's existing consumer compiles and its suites pass unedited.",
    "readApprovalRecordFacts derives `riskTier` from the run's computedTier through the production reader (absence -> missing APPROVAL_INTENT_RISK_TIER_UNAVAILABLE @ DAEMON_APPROVAL_INTENT with upstream) and never from a payload, a default, or the newest project-wide evaluation; an arm drives approval.decide_intent through the production command for a finalized classified run and asserts the seam's refusal ADVANCES past RISK_TIER to the next missing fact (or MINTS the record through validateApprovalRecord when all four facts exist at the delivered tree — state which, with the count of durable ApprovalDecisionRecord events).",
    "Mutation drills with SHA256 restore proofs: select the newest project-wide row instead of the run's -> the run-linkage arm reds; return an unverified row's assessment -> the strict arm reds; default riskTier when absent -> the absence arm reds. Gates standalone/unpiped with counts: `pnpm --filter @moe/daemon typecheck`, the authority-reader/admission/approval-intent/approval-record-facts suites from apps/daemon, `pnpm --filter @moe/daemon test` path-attributed; every production source <= 250 lines; explicit-pathspec commit; completion comment names the seam family rows and states: 'a finalized run's policy-declared tier now reaches approval.decide_intent; the seam refuses only on facts not yet produced'."
  ],
  "taskRails": [
    "Run-scoped selection only; never the newest project-wide PolicyEvaluated; ambiguity and unverified rows refuse with exact code+layer.",
    "Compose the strict reader (replay-verified) and readApprovalRecordFacts; never a second reader of PolicyEvaluated bytes; never a default tier.",
    "Sequenced after [T1-b] and task-ba102165; the mint arm is honest about which facts exist at the delivered tree.",
    "Explicit-pathspec commits; foreign WIP preserved; <=250 lines per production source; TDD red-first through the production command."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK (DoD-1) — read-only, ONE moe.add_comment, `git rev-parse HEAD` quoted, greps UNPIPED with exit codes + a positive control. (a) T1-b landed: PolicyEvaluated rows carry runId + riskAssessment — `git grep -n 'riskAssessment' HEAD -- apps/daemon/src/planning/run-policy-evaluation.ts apps/daemon/src/planning/planning-authority-finalize.ts` EXIT 0; quote task-a888038d's completionSummary from epicSiblings for the payload key roster instead of re-deriving it. (b) task-ba102165 DONE and `readApprovalRecordFacts` present; task-3b61860f and task-be80cb74 DONE — quote the walk export (`git grep -n 'firstMissingApprovalFact' HEAD -- apps/daemon/src/planning/approval-record-facts.ts` EXIT 0) and the slots present (riskTier?/stepUpAuthRef?/budgetRef?). (c) THE GAP IS OPEN: `git grep -n 'riskAssessment' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` EXIT 1 (EXIT 0 = adopt/verify/report duplicate). (d) CONSUMER PIN ENUMERATION: readPolicyEvaluationAuthority's callers — measured at plan time: activation/policy-admission-reader.ts:70 (value) and activation/admission-gate-resolver.ts:42,:103 (types); re-grep and list every hit plus every exact-shape fixture/test pinning PolicyEvaluationAuthority's key set. (e) porcelain on owned paths; foreign dirt noted, never staged.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts"
      ],
      "note": "ROUND 1 WORK BY worker-39362c3a, graded green by QA (comment-5d4aeb04). Reset to PENDING by the reopen, which was a LANDING failure, not an engineering one — I re-landed the bytes at c3955341 and wrote no production code. Recording the clause defect QA identified rather than re-running the probe: DoD 1 greps `riskAssessment` in bootstrap-policy-authority-reader.ts, where `git log -S` returns ZERO commits — that token never existed there; the field is riskTier, landed by 37f95c18 (task-a888038d). EXIT 1 was guaranteed either way, so DoD 1's \"EXIT 0 = adopt/verify/report duplicate\" branch could never fire.",
      "startedAt": "2026-08-30T07:34:58.156Z",
      "completedAt": "2026-08-30T07:35:02.659Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — STRICT READER WIDENING (DoD-2 first half). Arms in the reader's existing suite: (i) a RUN-SCOPED PolicyEvaluated row (T1-b shape) reads back with `riskAssessment` (computedTier et al) and `runId`, REPLAY-VERIFIED like every existing field — the assessment must be consistent with the replayed decision material, not merely present (mirror the :176-215 replay/digest discipline); a row whose riskAssessment disagrees with its replayed material refuses with the reader's existing mismatch vocabulary (exact code asserted); (ii) a LEGACY caller-driven row (no runId) still reads EXACTLY as today — same fields, same refusals (both-shapes arm); (iii) the existing consumer compiles and passes unedited: policy-admission-reader.ts and admission-gate-resolver.ts untouched (`git diff --stat` EMPTY on both; their suites green — quote counts). THEN GREEN in bootstrap-policy-authority-reader.ts: widen PolicyEvaluationAuthority with OPTIONAL `riskAssessment?` and `runId?` (optional so legacy rows and the existing consumer are untouched), verify them on run-scoped rows inside the existing exact-check block. The file is AT 220 LINES — the edit is surgical; if it crosses 250, move existing lines OUT into a sibling (fix-by-subtraction), never past the cap.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts"
      ],
      "note": "DISCHARGED WITHOUT AN EDIT, and that is the correct outcome — not a skipped step. The step asks to widen PolicyEvaluationAuthority with optional `riskAssessment?`/`runId?` in bootstrap-policy-authority-reader.ts. That widening ALREADY LANDED at 37f95c18 (task-a888038d, this row's own dependency), so the file is CLEAN and this row COMPOSES the landed reader instead of re-widening it. QA confirmed the file must stay out of the pathspec: `git add` on a clean path stages nothing and would silently shrink the commit. Verified at c3955341: `git show --name-only HEAD | grep -c bootstrap-policy-authority-reader` = 0. Consumers untouched as the step requires — policy-admission-reader.ts and admission-gate-resolver.ts carry no diff, and admission-gate-resolver.test.ts is green inside the 6-file/183-test G2 run. Engineering by worker-39362c3a, graded green in round 1.",
      "startedAt": "2026-08-30T07:35:09.806Z",
      "completedAt": "2026-08-30T07:35:17.670Z"
    },
    {
      "stepId": "step-3",
      "description": "RED FIRST — THE RUN-SCOPED SELECTOR (DoD-2 second half). NEW `apps/daemon/src/bootstrap/run-policy-selection.ts` + one-line exact-LF bridge + `run-policy-selection.test.ts`. Export `readRunPolicyEvaluation(store, { projectId, runId })`: walks the run linkage (the runId the T1-b writer persisted — selection by RUN, never by recency), hands the selected row to the STRICT reader (composition — never a second byte-reader), and answers `{ ok: true, evaluation }` with the replay-verified assessment. THREE refusals, each its own arm asserting code AND layer (module-local literals, no *_LAYER column-zero export): ABSENT (no row for the run), AMBIGUOUS (two rows claim the run — seed two and assert the refusal names ambiguity, never picks either), UNVERIFIED (the strict reader refused — its code+layer carried verbatim as upstream, never restamped). RUN-LINKAGE ARM (DoD-2's 'a second row for another run is never selected'): seed evaluations for run A and run B, ask for A, assert the returned assessment is A's BY VALUE (distinct tiers in the fixture so a wrong pick cannot alias). Module <= 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/run-policy-selection.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/bootstrap/run-policy-selection.ts",
        "apps/daemon/src/bootstrap/run-policy-selection.js",
        "apps/daemon/src/bootstrap/run-policy-selection.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/bootstrap/run-policy-selection.ts",
        "apps/daemon/src/bootstrap/run-policy-selection.js",
        "apps/daemon/src/bootstrap/run-policy-selection.test.ts"
      ],
      "note": "Engineering by worker-39362c3a, graded green by QA in round 1 (arms cited at :490 :511 :525 :548 :564). This step's three files were the round-1 failure: all UNTRACKED, so `git grep -c 'readRunPolicyEvaluation' HEAD` EXITED 1 and DoD 2 could not be verified at any sha — and DoD 4's drills had no constructible restore proof, because `git diff` is blind to `??` paths.\n\nLANDED BY ME at c3955341: run-policy-selection.ts (142 lines, under the 250 cap), run-policy-selection.test.ts (322), and the one-line .js bridge. `git grep -c 'readRunPolicyEvaluation' HEAD -- apps packages` now EXITS 0 across four files (run-policy-selection.ts:1, run-policy-selection.test.ts:10, approval-record-facts.ts:4, approval-record-facts.test.ts:4). All three paths are now tracked, so `git diff` is a valid restore proof and the drills are re-runnable by QA.\n\nGreen fresh at that sha inside G2: 6 files / 183 tests / EXIT 0.",
      "startedAt": "2026-08-30T07:35:19.931Z",
      "completedAt": "2026-08-30T07:35:28.424Z"
    },
    {
      "stepId": "step-4",
      "description": "RED FIRST — THE RISK_TIER SLOT (DoD-3). Arms in approval-record-facts.test.ts: (i) with a finalized classified run seeded through PRODUCTION T1-b finalize, `readApprovalRecordFacts` derives `derived.riskTier` = the run's computedTier THROUGH `readRunPolicyEvaluation` (production both sides); (ii) absence (no evaluation for the run) -> missing APPROVAL_INTENT_RISK_TIER_UNAVAILABLE @ DAEMON_APPROVAL_INTENT with the selector's refusal as `upstream` (code+layer verbatim); (iii) NEVER-NEWEST: seed a NEWER project-wide evaluation for a DIFFERENT run and assert the requested run's tier is still returned — the recency trap arm. THEN GREEN in approval-record-facts.ts: deriveFacts composes readRunPolicyEvaluation; the walk map marks RISK_TIER established when derived.riskTier is present; ABSENCE IS NOT A VALUE (field absent on refusal, never defaulted). Re-measure the file's line count AFTER the two sibling rows' edits; if past 250, split per the standing subtraction rule. THE SEAM ARM (DoD-3 tail, in approval-intent.test.ts): drive approval.decide_intent through the production command for the finalized classified run with the composition-root witness present — assert the refusal ADVANCES past RISK_TIER: if stepUp+budget+policyRef are all establishable at the delivered tree, the seam MINTS through validateApprovalRecord (count durable ApprovalDecisionRecord events = 1 and state it); otherwise assert the EXACT next missing code in roster order and record which facts existed. HONESTY over ambition: state which branch ran in the completion comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "Engineering by worker-39362c3a, graded green by QA in round 1; landed by me at c3955341 (approval-record-facts.ts +62/-, .test.ts +195, approval-intent.ts +58, .test.ts +32).\n\nDoD-3 BRANCH THAT RAN, carried forward from comment-cf170837 rather than re-derived: the refusal ADVANCES past RISK_TIER and does NOT mint. Durable ApprovalDecisionRecord events = ZERO. Measured through the production command and the production edge:\n  dispatch, no transported witness -> APPROVAL_INTENT_STEP_UP_UNAVAILABLE @ DAEMON_APPROVAL_INTENT\n  dispatch, transported witness    -> APPROVAL_INTENT_RECORD_UNMINTED     @ DAEMON_APPROVAL_INTENT\n  edgeRefusalOf(store, OPERATOR)   -> APPROVAL_INTENT_RECORD_UNMINTED     @ DAEMON_APPROVAL_INTENT\nWith a transported witness every roster fact resolves and the walk has nothing left to refuse, so readApprovalRecordFacts answers ok:true. The seam still cannot mint because validateApprovalRecord demands 18 keys and TWO of them — `validity` and `dependencyChanges` — have no durable producer and no derivation anywhere in the tree. APPROVAL_INTENT_RECORD_UNMINTED is deliberately a NON-roster code naming the true condition rather than a roster code that would misreport a resolved fact as missing.\n\nABSENCE IS NOT A VALUE holds: on refusal the field is absent, never defaulted — which is exactly what drill D3 (default the tier to \"R0\") reds against.",
      "startedAt": "2026-08-30T07:35:31.031Z",
      "completedAt": "2026-08-30T07:35:41.165Z"
    },
    {
      "stepId": "step-5",
      "description": "MUTATION DRILLS (DoD-4), one degree of freedom, %TEMP%\\f42d5165-<workerId>\\ backups; SHA256 restore proofs for the UNTRACKED new module trio in a SEPARATE call from the restore; `git diff --exit-code` only for tracked paths. (D1) in run-policy-selection.ts, select the NEWEST project-wide row instead of the run's -> the run-linkage arm AND the recency-trap arm red (distinct tiers make the alias visible). (D2) return an unverified row's assessment (skip the strict-reader handoff) -> the UNVERIFIED arm reds and the replay-consistency arm of step 2 reds. (D3) in approval-record-facts.ts, default riskTier (e.g. \"R0\") when the selector refuses -> the absence arm (ii) reds. (D4) drop the ambiguity check (pick the first of two claimant rows) -> the AMBIGUOUS arm reds. A GREEN drill is a FINDING — report, never bank. Record D1-D4 with red lines and restore proofs in ONE row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/run-policy-selection.ts",
        "apps/daemon/src/planning/approval-record-facts.ts"
      ],
      "note": "D1-D4 were executed by worker-39362c3a in round 1 and recorded on the row (comment-ec5ced19 carries D3 verbatim: the absent branch in approval-record-facts.ts changed from `{}` to `{ riskTier: \"R0\" }`). I did NOT re-run them — no net file change from this step, so nothing is claimed here that I did not do.\n\nWHAT ROUND 2 CHANGED FOR THIS DoD, and it is the reason the drills were a rejection reason at all: QA could not re-run them because three of the drilled paths (run-policy-selection.{ts,js,test.ts}) were UNTRACKED, and `git diff` is blind to `??` paths, so no restore proof was constructible in this shared checkout. Landing them at c3955341 makes all four drilled paths TRACKED, which converts every restore proof from an out-of-tree SHA256 comparison into a plain `git diff --exit-code` and makes the drills independently re-runnable by the reviewer. That is the defect this step's rejection actually named, and it is closed by the sha rather than by a re-run.\n\nI have not re-verified D1-D4 myself and am not asserting them as my own measurement.",
      "startedAt": "2026-08-30T07:35:43.547Z",
      "completedAt": "2026-08-30T07:35:51.656Z"
    },
    {
      "stepId": "step-6",
      "description": "GATES + SELF-REVIEW + COMMIT + COMPLETION. GATES standalone/unpiped, counts quoted: (G1) `pnpm --filter @moe/daemon typecheck` (quote `$ tsc`); (G2) from apps/daemon `pnpm exec vitest run --root . --config package.json src/bootstrap/run-policy-selection.test.ts src/planning/approval-record-facts.test.ts src/planning/approval-intent.test.ts src/activation/policy-admission-reader.test.ts` plus the authority-reader suite (adapt names to the real files; `--root . --config package.json` mandatory); (G3) `pnpm --filter @moe/daemon test` path-attributed per epic rail, foreign red disclosed verbatim. SELF-REVIEW: (i) is there ANY selection path keyed on recency, order, or aggregate position rather than the run linkage? (ii) can an unverified or foreign-run assessment reach the seam? (iii) legacy rows and the admission consumer untouched (`git diff --stat` EMPTY on policy-admission-reader.ts and admission-gate-resolver.ts); (iv) `wc -l` every touched production file <= 250. COMMIT by explicit pathspec of exactly the owned paths (porcelain first). COMPLETION COMMENT names the seam family (task-6646f888, task-ba102165, task-3b61860f, task-be80cb74, task-a888038d producer) and states EXACTLY: 'a finalized run's policy-declared tier now reaches approval.decide_intent; the seam refuses only on facts not yet produced' — plus which DoD-3 branch ran (mint, with the durable record count, or the exact next-missing code).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/bootstrap/run-policy-selection.ts",
        "apps/daemon/src/bootstrap/run-policy-selection.js",
        "apps/daemon/src/bootstrap/run-policy-selection.test.ts"
      ],
      "note": "THE ROUND-2 STEP. Full evidence: comment-fd0288df. COMMIT c39553412f3bdc87ea2ab1958e92b2e7868604d2 on moe/work-2026-08-08, parent 076324e4 — explicit pathspec, `-m` before the `--`, index proven EMPTY before staging and after commit; 7 files, 713(+)/99(-). bootstrap-policy-authority-reader.ts excluded (clean; `git show --name-only HEAD | grep -c` = 0). moe.record_commit called SEPARATELY: commitCount 1, duplicate false. It echoes EIGHT paths because it unions with previously declared paths — the COMMIT carries seven.\n\nGATES fresh at that sha, foreground, unpiped:\nG1 `pnpm --filter @moe/daemon typecheck` -> `$ tsc --project tsconfig.json`, EXIT 0. DISCLOSED as WORKTREE-measured: round 1's single TS2724 is gone only because task-2f554e29's `PAIRING_CLAIM_MAX_BODY_BYTES = 1024` now sits at pairing-approval-handshake.ts:33 IN THE WORKTREE — `git grep -c` at HEAD EXITS 1. A peer row's uncommitted bytes, not mine.\nG2 (named verification, QA's exact command) -> Test Files 6 passed (6) | Tests 183 passed (183) | EXIT 0. Matches the recorded run exactly.\nG3 `pnpm --filter @moe/daemon test` -> EXIT 1, foreign, and UNSTABLE: run A 20 failed / 6 files, run B 18 failed / 5 files — same command, same sha, minutes apart. Cause measured: pairing-approval-handshake.test.ts is ` M` and being edited live by the seat on task-2f554e29. Reporting both numbers rather than the flattering one.\nOWNED INTERSECTION EMPTY, with a positive control so the zero is a true negative: the unstable suite greps 0 for all three owned modules (EXIT 1) against a control returning 1 on a file that does import them.\n\nSELF-REVIEW, measured not asserted:\n(i) NO recency/order/position selection. `rows` come from a KEYED aggregate lookup (`runPolicyAggregateId(request.runId)`, :119), not a walk. `rows.length > 1` refuses AMBIGUOUS at :122 BEFORE any row is read, so the `rows[0]` at :123 is positional access on a singleton, not a positional pick. The three other grep hits for `newest|sort|latest` are all COMMENTS explaining why the module does not do it.\n(ii) NO. Two independent fences: `!authority.ok` -> RUN_POLICY_SELECTION_UNVERIFIED carrying the strict reader's code+layer verbatim as upstream; and `authority.runId !== request.runId` -> RUN_POLICY_SELECTION_RUN_MISMATCH.\n(iii) CONSUMERS UNTOUCHED: `git diff --stat HEAD --` on policy-admission-reader.ts and admission-gate-resolver.ts is EMPTY.\n(iv) LINE CAPS: run-policy-selection.ts 142, approval-record-facts.ts 239, approval-intent.ts 243 — all <= 250.\n\nADVERSARIAL POINT I CHASED AND CLEARED, recorded because a reviewer will have the same thought: RUN_MISMATCH looks unreachable, since the aggregate id is derived from the same runId — which would make it the undrillable guard the module's own comment at :133-136 rejects for the tier check. It is NOT unreachable: the arm at run-policy-selection.test.ts:239 asserts its code AND layer, seeding a payload whose runId diverges from its aggregate. The guard is drillable; the asymmetry with the removed tier guard is deliberate and correct.\n\nDoD-3 branch: the refusal ADVANCES past RISK_TIER, does NOT mint, durable ApprovalDecisionRecord events ZERO. Seam family: task-a888038d (producer), task-6646f888, task-ba102165, task-3b61860f, task-be80cb74.\n\nA finalized run's policy-declared tier now reaches approval.decide_intent; the seam refuses only on facts not yet produced.",
      "startedAt": "2026-08-30T07:36:50.634Z",
      "completedAt": "2026-08-30T07:37:10.722Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "LANDING FAILURE ONLY — THE ENGINEERING IS GRADED AND PASSES. Do not re-do it. Full evidence: comment-5d4aeb04e0454914baf2816d6baf1980.\n\n7 of 8 declared paths are not on the branch. HEAD = 076324e4 (be80cb74's sha), unmoved; task.commits = []; reviewStartedAt 07:19:11Z, measured through 07:23:07Z.\n  git grep -c 'readRunPolicyEvaluation' HEAD -- apps packages   EXIT 1   ABSENT\n   M approval-intent.ts, approval-intent.test.ts, approval-record-facts.ts, approval-record-facts.test.ts\n   ?? run-policy-selection.ts, run-policy-selection.js, run-policy-selection.test.ts\n\nNOT the ambiguous commits:[] case — bytes genuinely off-branch, so record_commit cannot repair it. DO NOT WAIT FOR THE WRAPPER: list_workers 07:21:58Z shows worker-39362c3a ALIVE but currentTaskId = task-2f554e29. The seat released this row; its exit commit can never fire.\n\nFIX: prove the index empty, `git add --` the seven, `git commit -m \"...\" -- <same seven>` with -m BEFORE the --, then moe.record_commit as a SEPARATE call. EXCLUDE bootstrap-policy-authority-reader.ts — it is clean, so git add stages nothing and shrinks the commit.\n\nVERIFIED BY ME, NOT SENT BACK: recorded gate re-run 6 files / 183 tests EXIT 0, matching exactly; daemon typecheck EXIT 1 is ONE foreign error attributed with a positive control, owned intersection EMPTY; line cap 247/142/239/243; rail 2 satisfied structurally; arms strong at :490 :511 :525 :548 :564.\n\nONE CLAUSE DEFECT, not a diff defect: DoD 1's probe greps 'riskAssessment' where that token has NEVER existed (git log -S = 0 commits); the field is riskTier, landed by 37f95c18 — this row's own dependency. EXIT 1 was guaranteed either way, so DoD 1's \"EXIT 0 = report duplicate\" branch could never fire. Composing instead of re-widening was correct. ADD ONE LINE to the completion comment: DoD 2's first clause was already satisfied at HEAD by 37f95c18.\n\nRound 2 I verify the sha, the seven paths at HEAD, and the drills.",
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 173,
  "comments": [
    {
      "id": "comment-76aa7742d40a40aa82b6dfe7d0ce4a9b",
      "author": "worker-39362c3a",
      "content": "INVERTED DEPENDENCY EDGE ON THIS ROW — blocking the entire worker lane. Measured at HEAD b8dd739a (`git rev-parse HEAD`). Read-only; zero bytes touched.\n\nTHIS ROW'S dependsOn = [a888038d, ba102165, 3b61860f, be80cb74]. First three DONE. The fourth is BLOCKED and points BACKWARDS.\n\nROSTER ORDER (`apps/daemon/src/planning/approval-intent.ts:68-77`), the ordering this whole T1 chain is built on:\n  index 0  APPROVAL_INTENT_RISK_TIER_UNAVAILABLE   <- THIS ROW's slot\n  index 1  APPROVAL_INTENT_STEP_UP_UNAVAILABLE     <- task-3b61860f, DONE\n  index 2  APPROVAL_INTENT_POLICY_REF_UNAVAILABLE  <- landed, approval-policy-ref.ts\n  index 3  APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE  <- task-be80cb74's slot\n\n`firstMissingApprovalFact` (approval-record-facts.ts:169-176) walks that roster and refuses under the FIRST unestablished fact. EARLIER slots gate LATER ones. This row is slot 0; be80cb74 is slot 3. The board edge points slot-0 at slot-3.\n\nHEAD SOURCE STATES THE TRUE DIRECTION, in a file be80cb74 itself authored —\n`apps/daemon/src/planning/approval-record-facts.test.ts:213-217`:\n  \"`readApprovalRecordFacts` cannot show STEP_UP -> BUDGET_REF today: `riskTier` has no durable\n   producer until the T1 chain's last row (task-f42d5165) lands, so the command always answers\n   the roster's FIRST code and every later row is unreachable behind it.\"\nThat is be80cb74 depending on THIS ROW. The board records the reverse. Backwards edge whose head is manually BLOCKED (dependsOnUnmet 0 + blockedOnTaskIds null = never auto-unblocks) = permanent deadlock.\n\nTHIS ROW'S OWN WRITTEN UNPARK NAMES TWO CONDITIONS AND be80cb74 IS NOT ONE (task description): \"[T1-b producer] DONE on disk AND task-ba102165 DONE (readApprovalRecordFacts present ... EXIT 0)\". BOTH HOLD AT HEAD. Greps unpiped, exit codes quoted, positive control included:\n\n1. `git grep -n 'export function readApprovalRecordFacts' b8dd739a -- apps/daemon/src/planning/`\n   -> apps/daemon/src/planning/approval-record-facts.ts:132 ; EXIT 0. ba102165 condition MET.\n2. T1-b producer (task-a888038d, DONE): `riskAssessment` present at HEAD in\n   apps/daemon/src/planning/run-policy-evaluation.ts and run-policy-record.ts. Condition MET.\n3. THE GAP IS STILL OPEN, so this row still has real work:\n   `git grep -n 'riskAssessment' b8dd739a -- apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts`\n   -> no output ; EXIT 1.\n   POSITIVE CONTROL, same file and same pathspec: `git grep -cn 'policyRef' b8dd739a -- <same>` -> 7 ; EXIT 0.\n   The empty result is a real absence, not a dead pathspec. Strict reader still drops riskAssessment.\n\nCOLLATERAL FINDING ON be80cb74 — IT IS PARTLY LANDED, so do not re-measure its premise as one unit. `apps/daemon/src/planning/approval-budget-ref.ts:9-11`: \"split out of `approval-record-facts.ts` by task-be80cb74 when the roster walk's fourth slot pushed that file past the 250-line cap\". `deriveApprovalBudgetRef` is already wired into `deriveFacts` (approval-record-facts.ts). Its BUDGET_REF-slot clause SHIPPED. Its live remainder is the SEED clause only: `budgetRef: hex64(\"bb\")` still at apps/control-room/src/live/live-dispatch-payloads.ts:218 and apps/daemon/src/orchestrator/demo-seed-payloads.ts:286.\n\nREMEDY IS ONE WRITE, NOT A NEW ROW: set this row's dependsOn to [a888038d, ba102165, 3b61860f] — drop be80cb74. `moe.set_task_dependencies` is ARCHITECT/GOVERNOR-ONLY, so a worker seat cannot take it. Needs an authorized seat.\n\nCONSEQUENCE UNTIL THEN: this row (WORKING, unassigned) stays unclaimable at dependsOnUnmet 1, task-c289b8fb (PR #3 integration re-run) stays at dependsOnUnmet 2 behind it, and the worker lane has nothing to claim. True chain is f42d5165 -> be80cb74 -> c289b8fb. Landing THIS row flips be80cb74's premise: the walk can finally reach BUDGET_REF, which is the movement demonstration its remaining seed clause needs.\n\n— worker-39362c3a",
      "timestamp": "2026-08-30T04:11:06.759Z"
    },
    {
      "id": "comment-3a5a99d2ae4047c487765ee69da50dda",
      "author": "worker-39362c3a",
      "content": "STEP 1 — VERIFY-OR-BLOCK (DoD-1). `git rev-parse HEAD` = **076324e40bbf63dd1cdfc1f252fe0a663a42b67c**. Read-only, zero bytes touched. Greps unpiped, exit codes quoted.\n\n**HEADLINE: DoD-1's gap grep is a FALSE GAP SIGNAL. It is EXIT 1, but the capability it screens for is ALREADY LANDED under different names. This is an ADOPT, not a block, and it retires step 2's edit.**\n\n(a) T1-b PRODUCER LANDED —\n`git grep -n 'riskAssessment' HEAD -- apps/daemon/src/planning/run-policy-evaluation.ts apps/daemon/src/planning/planning-authority-finalize.ts`\n-> `run-policy-evaluation.ts:195: const computedTier = evaluated.record.riskAssessment.computedTier;` **EXIT 0**.\n\n(b) SEAM ROWS LANDED —\n`git grep -n 'export function readApprovalRecordFacts\\|export function firstMissingApprovalFact' HEAD -- apps/daemon/src/planning/approval-record-facts.ts`\n-> `:132 readApprovalRecordFacts`, `:180 firstMissingApprovalFact` **EXIT 0**. Slots present: `budgetRef?` :60, `riskTier?` :65, `stepUpAuthRef?` :71. Walk map :164-167 carries all three predicates.\n\n(c) THE GAP GREP, AND WHY ITS ANSWER IS MISLEADING —\n`git grep -n 'riskAssessment' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` -> **EXIT 1**.\nPOSITIVE CONTROL, same file + same pathspec: `git grep -cn 'policyRef' HEAD -- <same>` -> **7, EXIT 0**. The empty result is a true absence of that SPELLING, not a dead pathspec.\n\n**But the strict reader already exposes the tier and the run linkage, replay-verified:**\n```\nbootstrap-policy-authority-reader.ts:58   readonly riskTier: PolicyRiskTier | null;\n                                   :60   readonly runId: string | null;\n                                   :8    import { RUN_POLICY_ROW_EXTRA_KEYS, runScopedLinkage } from \"../planning/run-policy-record.js\"\n                                   :30   const RUN_ROW_KEYS = [...ROW_KEYS, ...RUN_POLICY_ROW_EXTRA_KEYS]\n                                   :138  exactObject(payload, ROW_KEYS) ?? exactObject(payload, RUN_ROW_KEYS)\n                                   :229  const runScoped = Object.hasOwn(record, \"runId\");\n                                   :242-243  riskTier: linkage?.riskTier ?? null,  runId: linkage?.runId ?? null,\nrun-policy-record.ts:90 runScopedLinkage(row, replayed)\n   requires non-empty string runId; compares graphNodeRevisionRefs stated-vs-REPLAYED element-wise;\n   compares riskAssessment stated-vs-REPLAYED by serialized equality; requires computedTier in\n   POLICY_RISK_TIERS; returns { riskTier, runId }.\n```\nSo DoD-2's first half — \"the strict reader returns replay-verified `riskAssessment` and `runId`\" — **is satisfied at HEAD**. The assessment is verified IN FULL against the replayed material; the reader deliberately surfaces the derived `computedTier` rather than the whole object, which is the one field DoD-3 consumes.\n\n**CONSEQUENCE, disclosed rather than improvised:** adding an optional `riskAssessment?` to `PolicyEvaluationAuthority` now would be a SECOND representation of an already-verified fact, which task rail 2 (\"never a second reader of PolicyEvaluated bytes\") and global rail 17 (silent duplicate of a landed surface) both forbid. No consumer reads `effectiveTier`/`usedFactIds`/`callerRiskHint`. **Step 2 therefore becomes VERIFY-AND-PIN, not widen** — and its two planned arms already exist, authored by T1-b in `run-policy-evaluation.test.ts`: *\"reads a caller-driven row exactly as before, with no run linkage\"* (`runId` null, `riskTier` null — the legacy/both-shapes arm) and *\"reads a run-scoped row with its linkage and its tier\"* (`runId` === RUN_ID, `riskTier` === computedTier), plus a tamper matrix asserting exact code AND layer `DAEMON_POLICY_AUTHORITY`. I will pin them by re-running, not re-writing.\n\n**THE REAL REMAINING WORK IS STEPS 3 AND 4, both measured ABSENT:**\n1. `git grep -n 'readRunPolicyEvaluation\\|run-policy-selection' HEAD -- apps packages` -> **EXIT 1**. The run-scoped selector does not exist anywhere.\n2. `deriveFacts` (approval-record-facts.ts:189-213) merges `seam` (stepUpAuthRef), `slot` (budgetRef) and `policy` (applicablePolicyRef) — **and nothing else. `riskTier` is declared at :65 and never populated**, so the walk's `APPROVAL_INTENT_RISK_TIER_UNAVAILABLE` predicate (:166) is always false and the seam always answers roster index 0. That is exactly the shortfall this row exists to close.\n\n(d) CONSUMER PIN ENUMERATION — **the plan's list is STALE. It named 2 files; HEAD has 5 production files plus 4 test pins.** `git grep -l 'PolicyEvaluationAuthority' HEAD -- apps packages adapters tools tests`:\n- PRODUCTION: `activation/policy-admission-reader.ts` (:6 value import, :26 type, **:70 call**); `activation/admission-gate-resolver.ts` (:42, :103 type-only); `bootstrap/bootstrap-policy-services.ts` (**:15 re-exports the function**, :17-18 the types — every consumer imports through this barrel, not the reader directly); `planning/approval-policy-ref.ts` (**:95 call**, NOT in the plan); `planning/supersession-policy-decision.ts` (:5, :33, **:88 call**, NOT in the plan).\n- TEST PINS: `bootstrap/bootstrap-services.test.ts` (:686 describe, :741), `planning/graph-supersede-approval-binding.test.ts` (:173), `planning/planning-authority-finalize.test.ts` (:295), `planning/run-policy-evaluation.test.ts` (:288/:305/:328 + the exact-key-set pins at the sorted `Object.keys` assertions).\nSince step 2 no longer widens the type, none of these five needs an edit — but all nine are named here so DoD-2's \"compiles and passes unedited\" clause is graded against the measured roster, not the stale two.\n\n(e) PORCELAIN — owned paths (`src/bootstrap/`, `approval-record-facts.{ts,test.ts}`, `approval-intent.test.ts`, `policy-admission-reader.ts`, `admission-gate-resolver.ts`) **EMPTY, clean**. Repo-wide `git status --porcelain | wc -l` = **81** foreign dirty paths: noted, never staged, never reverted.\n\nLINE COUNTS AT CLAIM (re-measured per the plan): `bootstrap-policy-authority-reader.ts` **247** (plan said 220 — it grew under T1-b; at cap, so the selector correctly goes in a new module), `approval-record-facts.ts` **213** (plan said 227 before siblings; be80cb74 split `approval-budget-ref.ts` out).\n\nNOT BLOCKING. Proceeding to step 2 as verify-and-pin, then steps 3-6 as planned.\n— worker-39362c3a",
      "timestamp": "2026-08-30T06:46:18.501Z"
    },
    {
      "id": "comment-ec5ced1926174218be246e6a65182b9b",
      "author": "worker-39362c3a",
      "content": "STEP 5 — MUTATION DRILLS (DoD-4). Four drills, ONE degree of freedom each, restored between drills. ZERO green drills.\n\nBACKUPS at `%TEMP%\\f42d5165-worker-39362c3a\\`, taken BEFORE any drilling, with `git status --porcelain` of the exact mutated paths first:\n```\n M apps/daemon/src/planning/approval-record-facts.ts\n?? apps/daemon/src/bootstrap/run-policy-selection.ts      <- UNTRACKED, so git diff proves nothing here\n   apps/daemon/src/planning/run-policy-record.ts          <- clean/tracked\nbaseline sha256:\n  913d40ee…3863e  approval-record-facts.ts\n  732910d0…6ded  run-policy-record.ts\n  46efc747…77aa  run-policy-selection.ts\n```\nEvery restore proof was run in a call SEPARATE from the restore, per the drill-restore rail. `git diff --exit-code` is used only where it can mean something: `approval-record-facts.ts` is dirty with this row's own work and `run-policy-selection.ts` is untracked, so for those the SHA256-vs-backup equality IS the proof.\n\n**D1 — run-linkage guard.** Deleted `if (authority.runId !== request.runId) return refuse(\"RUN_POLICY_SELECTION_RUN_MISMATCH\");` in `run-policy-selection.ts`.\nRED, exactly one arm: `× refuses a foreign run's row filed on this run's aggregate` — `AssertionError: expected true to be false`. `Tests 1 failed | 33 passed (34)`.\nThat is the divergence fixture doing its job and NOTHING else moving: the arm files run B's own replay-verifiable row on run A's aggregate, so the strict reader accepts it and this guard is the only mechanism left. Restore: sha256 `46efc747…77aa` equal to backup; `grep -c 'D1 MUTANT'` -> 0.\n\n**D2 — the replay comparison behind the tier.** Deleted the `JSON.stringify(assessment) !== JSON.stringify(replayed[\"riskAssessment\"])` line in `run-policy-record.ts:runScopedLinkage`.\nRED, two arms across two files: `× carries the strict reader's refusal verbatim rather than restamping it` (mine) AND `× refuses a tier the evidence does not derive` (T1-b's). `Tests 2 failed | 19 passed (21)`.\n**THIS IS THE DRILL THAT GRADES STEP 2.** I banked T1-b's existing arms rather than re-authoring them and said at the time that a green D2 would be a FINDING that the pin was vacuous. It is not green — the arm I declined to rewrite genuinely reds when the replay verification is removed. Restore: sha256 `732910d0…6ded` equal to backup AND `git diff --exit-code` EXIT 0 (valid here — the path is tracked and clean); residue 0.\n\n**D3 — default the tier.** Changed the absent branch in `approval-record-facts.ts` from `{}` to `{ riskTier: \"R0\" }`.\nRED, three arms: `× leaves the tier ABSENT and carries the selector's refusal verbatim`, `× never answers with another run's tier`, and the PRE-EXISTING `× leaves applicablePolicyRef ABSENT rather than defaulting when no policy is verified`. `Tests 3 failed | 52 passed (55)`.\nThe third is the interesting one: a defaulted tier moves which fact the walk reports, so it reds an arm about a DIFFERENT slot. \"Absence is not a value\" is enforced across the whole derived window, not just at the tier. Restore: sha256 `913d40ee…3863e` equal to backup; residue 0.\n\n**D4 — ambiguity.** Deleted `if (rows.length > 1) return refuse(\"RUN_POLICY_SELECTION_AMBIGUOUS\");`, so the first claimant wins.\nRED, exactly one arm: `× refuses ambiguity rather than picking either claimant`. `Tests 1 failed | 6 passed (7)`. Restore: sha256 `46efc747…77aa` equal to backup; residue 0.\n\nFINAL RESIDUE SWEEP across all three drill targets: `grep -c 'MUTANT'` -> `0`, `0`, `0`.\n\nPOST-DRILL GREEN, standalone and unpiped from apps/daemon: `pnpm exec vitest run --root . --config package.json src/bootstrap/run-policy-selection.test.ts src/planning/approval-record-facts.test.ts src/planning/approval-intent.test.ts src/planning/run-policy-evaluation.test.ts` -> `Test Files 4 passed (4)` / `Tests 76 passed (76)`, EXIT 0.\n\nONE DEFECT THE DRILLS SURFACED, in my own delivered bytes, fixed here: `approval-record-facts.ts` carried the comment *\"The tier is first and has no producer, so it answers today\"* immediately above the walk. This row makes that FALSE, and a stale comment asserting a permanently-unfilled slot is exactly what a later reader would plan against. Rewritten to say which fact answers now depends on the run. Suites re-run green after the edit; the fix post-dates every drill above, so no drill outcome rests on it.\n\n— worker-39362c3a",
      "timestamp": "2026-08-30T07:07:59.838Z"
    },
    {
      "id": "comment-cf170837531b4f69a191f80957941d77",
      "author": "worker-39362c3a",
      "content": "COMPLETION RECORD. Seam family: task-6646f888 (the seam), task-ba102165 (readApprovalRecordFacts), task-3b61860f (step-up + the roster walk), task-be80cb74 (BUDGET_REF slot), task-a888038d (T1-b, the producer this row reads).\n\n**a finalized run's policy-declared tier now reaches approval.decide_intent; the seam refuses only on facts not yet produced**\n\nDoD-3 BRANCH THAT RAN: the refusal ADVANCES past RISK_TIER. It does NOT mint. Durable ApprovalDecisionRecord events: **ZERO**. Measured through the production command and the production edge:\n```\ndispatch, no transported witness   -> APPROVAL_INTENT_STEP_UP_UNAVAILABLE @ DAEMON_APPROVAL_INTENT\ndispatch, transported witness      -> APPROVAL_INTENT_RECORD_UNMINTED     @ DAEMON_APPROVAL_INTENT\nedgeRefusalOf(store, OPERATOR)     -> APPROVAL_INTENT_RECORD_UNMINTED     @ DAEMON_APPROVAL_INTENT\n```\nWith a transported witness EVERY roster fact resolves and the walk has nothing left to refuse — `readApprovalRecordFacts` answers `ok: true`. `APPROVAL_INTENT_RECORD_UNMINTED` is a NON-roster code naming the true condition, because naming a roster code there would report a producer missing that the seam had just read.\n\nWHY NOT MINT, and I want this challenged rather than assumed. `validateApprovalRecord` demands 18 keys. SIXTEEN have server sources. TWO — `validity` and `dependencyChanges` — have no durable producer and no derivation anywhere in the tree. Two further questions have no answer in any plan text: whether decide_intent also ACTIVATES (`planning-services.ts:224` documents approval.decide as \"J1's approve-and-activate action\"), and which aggregate/event holds the record relative to the one approval.decide commits from the caller's payload. Defaulting the two fields would fabricate part of a HUMAN_APPROVED authority record — the precise thing epic rail 1 and the seam's own docstring forbid. FILED, not narrowed: **task-6093483c85d34e4aa3b850abd8f00732 [T1-d mint]**, PLANNING, CRITICAL, dependsOn this row, carrying the 18-key source enumeration and all three decisions as a DoD clause requiring an architect answer.\n\n=== THE THING A GRADER MUST NOT SKIM: I REMOVED A BURN CALL AND REPHRASED ITS GUARD ===\n`approval-intent.ts` ended with `burnStepUpAuthRef(...)` then a terminal refusal, annotated by task-3b61860f: *\"making facts.ok reachable and leaving this refusal in place would BURN the one-shot reference and then refuse, bricking every retry.\"* This row made `facts.ok` reachable, so that was LIVE. The derived ref is one-shot per (identity, command, run), so a burnt-then-refused request bricks every retry of the same approval.\n\nI found it by measurement, not by reading the comment: the intent suite refused BUDGET_REF while the facts suite COMPLETED on an identical fixture, which cannot both be true of the reader. A throwaway probe settled it — `facts={\"applicablePolicyRef\":\"e7a5ee19…\",\"ok\":true,\"stepUpAuthRef\":\"e727c4ff…\"}`. Probe removed, `grep -c ZZPROBE` -> 0.\n\nFIRST ATTEMPT, which failed and is worth recording: I parked the burn below a new guard to preserve task-3b61860f's placement byte-for-byte. **Unreachable code is still typechecked**, and TS's control flow collapses the narrowing there — `Property 'stepUpAuthRef' does not exist on type 'ApprovalRecordFactsIncomplete'`, `Property 'code' does not exist on type 'StepUpBurn'`. So the burn had to leave the seam, and its unused import with it.\n\nThat reddened task-3b61860f's SOURCE-TEXT guard asserting the burn call exists after the gate. That guard existed for a stated reason — the burn was UNREACHABLE, so only source text could see it. This row removed that reason. I rephrased it from an ARRANGEMENT to an INVARIANT: `if (burn !== -1) expect(burn).toBeGreaterThan(gate)`, i.e. *if the seam burns at all, it burns after the gate* — true with no burn, true once task-6093483c restores it, RED the moment anyone puts a burn ahead of the facts gate, which is the only arrangement that bricks. I also narrowed its anchor from `burnStepUpAuthRef(` to `burnStepUpAuthRef(input.store` so the comments explaining the absence cannot satisfy it; a guard that counts its own prose is vacuous. The behaviour is now asserted directly by \"burns NOTHING when the approval refuses\", which this row turned from accidentally-true into load-bearing.\n\n=== DELIVERED ===\nNEW `bootstrap/run-policy-selection.ts` (142 lines) + exact LF bridge + 8 arms. `readRunPolicyEvaluation(store, {projectId, runId})` addresses `runPolicyAggregateId(runId)` — the address the writer files under — so run linkage IS the lookup, never a filter after a newest-first walk. It composes the strict reader and never reads evaluation bytes for meaning (task rail 2). FIVE codes on layer DAEMON_RUN_POLICY_SELECTION (module-private const, exported as a closed TYPE, so the security roster's column-zero `*_LAYER` count is unchanged): ABSENT, AMBIGUOUS, ROW_UNREADABLE, RUN_MISMATCH, UNVERIFIED. The last two are ADDITIONS to the planned three — ROW_UNREADABLE because collapsing an undecodable row into ABSENT lets a corrupt row read as \"never evaluated\", and RUN_MISMATCH because it is the only mechanism that can refuse a foreign run's honest row filed on this run's aggregate.\n\n`approval-record-facts.ts` (239): `deriveFacts` composes the selector; upstream fills in ROSTER ORDER with the tier first. The null-tier arm is a narrowing not a cast, so a future change to the linkage invariant makes the fact ABSENT rather than publishing `null` as a tier.\n\nSTEP 2 WAS DISCHARGED WITHOUT AN EDIT and that is deliberate: DoD-2's first half was ALREADY delivered by T1-b under different names — `riskTier` and `runId` are required-and-nullable on the strict reader (stronger than the planned optional), replay-verified in full by `runScopedLinkage`. Adding `riskAssessment?` would have been a second representation of an already-verified fact. DoD-1's gap grep is EXIT 1 on the literal `riskAssessment`, which reads as \"gap open\" but is a SPELLING, not the capability — full premise in comment-3a5a99d2.\n\nDRILLS: 4, one degree of freedom each, ZERO green, restores proven by SHA256 in calls separate from the restore (comment-ec5ced19). D2 is the one that grades my step-2 decision: deleting the replay comparison reds BOTH my UNVERIFIED arm AND the T1-b arm I banked rather than re-authored. The pin is not vacuous.\n\nVERIFICATION (fresh): `Test Files 6 passed (6)` / `Tests 183 passed (183)`, EXIT 0. G3 `pnpm --filter @moe/daemon test` EXIT 1 with 19 foreign failures across 5 files, owned intersection EMPTY, attributed by IMPORT CLOSURE (each greps 0 for this row's modules against a positive control of 1) and matching the 19 QA measured at this same HEAD before my diff existed.\n\n— worker-39362c3a",
      "timestamp": "2026-08-30T07:18:48.656Z"
    },
    {
      "id": "comment-5d4aeb04e0454914baf2816d6baf1980",
      "author": "qa-cc876c7b",
      "content": "QA REVIEW IN PROGRESS — everything gradeable without the sha is GREEN. One clause defect, and the\ndelivery is not on the branch yet. Measured 07:19-07:22Z, reviewStartedAt 07:19:11.256Z.\n\n=== LANDING: NOT ON THE BRANCH (not a rejection yet — flagged early) ===\n  HEAD = 076324e4 (task-be80cb74's sha), unmoved\n  git grep -c 'readRunPolicyEvaluation' HEAD -- apps packages    EXIT 1   ABSENT\n  task.commits = []\n   M approval-intent.ts, approval-intent.test.ts,\n     approval-record-facts.ts, approval-record-facts.test.ts\n   ?? run-policy-selection.ts, run-policy-selection.js, run-policy-selection.test.ts\nSEVEN paths to land. `bootstrap-policy-authority-reader.ts` is CLEAN and must be EXCLUDED — `git add`\non it stages nothing (see the clause finding below for why it is correctly unchanged).\nTHE WRAPPER CANNOT DO THIS: list_workers at 07:21:58Z shows worker-39362c3a ALIVE but with\ncurrentTaskId = task-2f554e29. The seat released this row, so its session-exit commit can never fire\n(alive-but-off-the-row; `currentTaskId != this row` is the discriminator, not `isAlive`).\nHand-land the seven, then moe.record_commit as a SEPARATE second call — a manual commit fires no hook.\n\n=== GRADED GREEN WITHOUT THE SHA — do not re-do any of this ===\n- RECORDED GATE RE-RUN BY ME, foreground, from apps/daemon:\n    pnpm exec vitest run --root . --config package.json src/bootstrap/run-policy-selection.test.ts\n      src/planning/approval-record-facts.test.ts src/planning/approval-intent.test.ts\n      src/planning/run-policy-evaluation.test.ts src/activation/admission-gate-resolver.test.ts\n      src/bootstrap/bootstrap-services.test.ts\n    -> Test Files 6 passed (6), Tests 183 passed (183), EXIT 0. Matches the recorded verification.\n- DAEMON TYPECHECK EXIT 1 — ONE error, 100% FOREIGN, attributed with a positive control:\n    src/http/pairing-approval-handshake.test.ts(5,3): TS2724 ... no exported member named\n      'PAIRING_CLAIM_MAX_BODY_BYTES'\n    git grep -c 'PAIRING_CLAIM_MAX_BODY_BYTES' HEAD -- apps/daemon/src        EXIT 1  (absent)\n    same token in the DIRTY worktree copy of that file                        3 hits\n    POSITIVE CONTROL, same form: 'PAIRING_APPROVAL_MAX_BODY_BYTES' at HEAD    EXIT 0, 3 files\n    the file is ` M` and is NOT among this row's 8 declared paths (all bootstrap/ + planning/)\n  => worker-d9877466's uncommitted task-2f554e29 step-2 arms. OWNED INTERSECTION EMPTY. Admissible\n  under the epic's path-attributed baseline rail. Every apps/daemon row will see this until that\n  ruling lands — attribute it, do not chase it.\n- LINE CAP (DoD 4, <=250 per production source): 247 / 142 / 239 / 243. All four compliant.\n- RAIL 2 (\"compose the strict reader; never a second reader of PolicyEvaluated bytes\") — SATISFIED\n  STRUCTURALLY, not by convention:\n    run-policy-selection.ts:127  readPolicyEvaluationAuthority(payload, projectId, ...)   composed\n    run-policy-selection.ts:141  if (authority.runId !== request.runId) ...               consumed\n  142 lines. Its :133-136 comment declines a separate tier guard on the grounds that it \"could never\n  be the only mechanism to refuse, and would therefore be undrillable\" — that is the correct reason\n  to omit a guard and it is the reasoning this board spent the night arriving at.\n- ARM TITLES (approval-intent.test.ts) are the strongest set I have graded tonight:\n    :490 advances past riskTier, naming the fact in its own code AND layer   (DoD 3's branch)\n    :511 reaches the mint boundary with every roster fact established\n    :525 burns NOTHING when the approval refuses, so a later retry is not locked out\n    :548 names one code per missing fact, over a NONZERO roster              (cardinality-guarded)\n    :564 produces no refusal whose code is UNKNOWN_ERROR\n  :525 is the regression arm for the bricking bug the worker found and self-disclosed.\n\n=== CLAUSE DEFECT: DoD 1'S VERIFY-OR-BLOCK PROBE IS VACUOUS ===\nDoD 1 requires: \"the strict reader still drops `riskAssessment`\n  (git grep -n 'riskAssessment' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts\n   EXIT 1 — EXIT 0 = adopt/verify/report duplicate)\".\nMEASURED:\n  git log -S'riskAssessment' -- bootstrap-policy-authority-reader.ts   ->  0 commits\n      the token has NEVER existed in that file, in its entire history\n  git show HEAD:bootstrap-policy-authority-reader.ts\n      :58  readonly riskTier: PolicyRiskTier | null;\n      :60  readonly runId: string | null;\n  git log -S'riskTier: PolicyRiskTier | null' -- <same file>\n      ->  37f95c18   task-a888038d   [T1-b producer]     <- THIS ROW'S OWN DEPENDENCY\nTHE FIELD IS `riskTier`, NOT `riskAssessment`. So EXIT 1 was guaranteed whether the gap was open or\nclosed — and it was already CLOSED, by the dependency this row is sequenced behind. The\n\"EXIT 0 = adopt/verify/report duplicate\" branch existed precisely to catch that and COULD NEVER FIRE.\nA verify-or-block probe anchored on a token that has never existed is an assertion that cannot fail,\ninside the gate whose whole job is to prevent duplicated work.\n\nTHE OUTCOME IS STILL CORRECT AND THIS IS NOT A REJECTION GROUND. The worker did not re-widen the\nreader; they COMPOSED it (rail 2, above), which is what the rails require. DoD 2's first clause\n(\"the strict reader returns replay-verified riskAssessment and runId\") is satisfied by the landed\ninterface rather than by this row's diff.\nASKED OF THE WORKER, one line in the completion comment: state that DoD 2's first clause was ALREADY\nsatisfied at HEAD by 37f95c18 and that this row composes it rather than delivering it. Without that,\na future reader diffs the seven paths, finds no reader widening, and reads DoD 2 as unmet.\n\n=== WHAT I CANNOT DO UNTIL IT LANDS ===\nRe-run the DoD-4 drills. Three of the paths are UNTRACKED, so `git diff` is blind in both directions\nand a restore proof is not constructible in a checkout with six live seats. Once committed they are\ntracked and `git checkout --` + `git diff --exit-code` is a sound restore. That is the only thing\nbetween this row and an approval.\n\nFILE COLLISION, noted not actioned: task-6093483c [T1-d mint] (PLANNING, filed by this row's worker)\nshares approval-record-facts.ts, approval-record-facts.test.ts and approval-intent.test.ts. Unclaimed,\nso no race today.\n\nZero bytes touched, zero commits.",
      "timestamp": "2026-08-30T07:23:03.274Z"
    },
    {
      "id": "comment-fd0288df3ed343138ba78616c9b2406e",
      "author": "worker-d9877466",
      "content": "ROUND 2 COMPLETION — LANDING ONLY. I wrote no production bytes. The engineering is worker-39362c3a's and was graded green by qa-cc876c7b in round 1 (comment-5d4aeb04); round 1 failed on delivery alone. The bytes I committed are byte-identical to the graded set.\n\nTHE SHA: c39553412f3bdc87ea2ab1958e92b2e7868604d2 on moe/work-2026-08-08, parent 076324e4. Explicit pathspec, `-m` before the `--`. Seven paths, exactly the seven QA named:\n  7 files changed, 713 insertions(+), 99 deletions(-)\n  create mode: run-policy-selection.ts (142), run-policy-selection.test.ts (322), run-policy-selection.js (1)\n  modified:    approval-intent.ts, approval-intent.test.ts, approval-record-facts.ts, approval-record-facts.test.ts\nIndex proven EMPTY before staging and after commit. bootstrap-policy-authority-reader.ts EXCLUDED as instructed — `git show --name-only HEAD | grep -c bootstrap-policy-authority-reader` = 0. Then moe.record_commit as a SEPARATE second call (a hand landing fires no hook): commitCount 1, duplicate false.\n  NOTE FOR THE GRADER: record_commit echoed EIGHT paths in filesModified because it unions with the row's previously declared paths. The COMMIT carries seven. The grep above is the discriminator.\n\nALL THREE ROUND-1 FAILURES CLEARED, each re-measured with QA's own command:\n1. Task rail 4 (nothing committed) -> c3955341 above; task.commits no longer [].\n2. DoD 2 (readRunPolicyEvaluation ABSENT at HEAD) -> `git grep -c 'readRunPolicyEvaluation' HEAD -- apps packages` now EXIT 0, four files: run-policy-selection.ts:1, run-policy-selection.test.ts:10, approval-record-facts.ts:4, approval-record-facts.test.ts:4.\n3. DoD 4 (three drilled paths UNTRACKED, so `git diff` restore proofs impossible) -> all three are now tracked; `git diff` is a valid restore proof for every drilled path and the drills are re-runnable.\n\nGATES, FOREGROUND, STANDALONE, UNPIPED, fresh at c3955341:\nG2 (the plan's named verification, QA's exact command from apps/daemon):\n  pnpm exec vitest run --root . --config package.json src/bootstrap/run-policy-selection.test.ts\n    src/planning/approval-record-facts.test.ts src/planning/approval-intent.test.ts\n    src/planning/run-policy-evaluation.test.ts src/activation/admission-gate-resolver.test.ts\n    src/bootstrap/bootstrap-services.test.ts\n  -> Test Files 6 passed (6) | Tests 183 passed (183) | EXIT 0. Matches the recorded verification EXACTLY.\nG1 `pnpm --filter @moe/daemon typecheck` -> `$ tsc --project tsconfig.json`, EXIT 0.\n  DISCLOSED, because this green is WORKTREE-measured and does not mean what it looks like: round 1's single TS2724 came from task-2f554e29's arms. That row's constant `PAIRING_CLAIM_MAX_BODY_BYTES = 1024` now exists at pairing-approval-handshake.ts:33 IN THE WORKTREE ONLY — `git grep -c` at HEAD EXITS 1. The green depends on a peer row's uncommitted bytes. Nothing of mine moved it.\nG3 `pnpm --filter @moe/daemon test` -> EXIT 1, foreign, disclosed verbatim and UNSTABLE:\n  run A: Test Files 6 failed | 319 passed (325); Tests 20 failed | 6705 passed | 12 todo\n  run B: Test Files 5 failed | 320 passed (325); Tests 18 failed | 6707 passed | 12 todo\n  Same command, same commit, minutes apart. CAUSE, measured not guessed: the moving file is src/http/pairing-approval-handshake.test.ts, which is ` M` and being edited live by the seat on task-2f554e29. A count that moves between two runs of one command is not a number to bank — I am reporting both.\n  Failing files (run B): activation-run-commit, activation-telemetry-launch, http-listener, pairing-approval-handshake, launch-template-producer.\n\nOWNED INTERSECTION IS EMPTY, with a positive control so the zero is a true negative:\n  none of {approval-intent, approval-record-facts, run-policy-selection} appears in the failing set (0/3)\n  grep -c 'run-policy-selection|approval-record-facts|approval-intent' pairing-approval-handshake.test.ts -> 0, EXIT 1\n  positive control, same grep shape on a file that DOES import mine:\n  grep -c 'approval-record-facts' approval-intent.ts -> 1, EXIT 0\nThe unstable suite has no import edge to this row's modules, so its variance cannot be this row's.\n\nDoD-3 BRANCH THAT RAN (carried forward from comment-cf170837, not re-derived): the refusal ADVANCES past RISK_TIER and does NOT mint. Durable ApprovalDecisionRecord events: ZERO. With a transported witness every roster fact resolves and readApprovalRecordFacts answers ok:true; the seam then answers APPROVAL_INTENT_RECORD_UNMINTED @ DAEMON_APPROVAL_INTENT, a non-roster code naming the true condition — validateApprovalRecord demands 18 keys and two (`validity`, `dependencyChanges`) have no durable producer anywhere in the tree.\n\nTHE CLAUSE DEFECT QA ASKED ME TO RECORD: DoD 2's first clause was ALREADY SATISFIED AT HEAD by 37f95c18 (task-a888038d), which landed riskTier and runId on the authority reader. DoD 1's probe greps `riskAssessment` in bootstrap-policy-authority-reader.ts, where `git log -S` returns ZERO commits — that token never existed there; the field is riskTier. EXIT 1 was guaranteed either way, so DoD 1's \"EXIT 0 = report duplicate\" branch could never fire. Composing the landed widening rather than re-widening it was correct.\n\nSEAM FAMILY: task-a888038d (producer), task-6646f888, task-ba102165, task-3b61860f, task-be80cb74.\n\nA finalized run's policy-declared tier now reaches approval.decide_intent; the seam refuses only on facts not yet produced.",
      "timestamp": "2026-08-30T07:34:14.676Z"
    },
    {
      "id": "comment-cc9d5d24352b460a97610d2fbb2bd3e9",
      "author": "qa-cc876c7b",
      "content": "QA REVIEW ROUND 2 — APPROVED. Landing fix verified; the engineering was graded green in round 1\n(comment-5d4aeb04) and was NOT re-opened, exactly as the rejection promised.\n\n=== THE THREE ROUND-2 CRITERIA I COMMITTED TO ===\n1. SHA c3955341 — `git branch --contains` = moe/work-2026-08-08; `git merge-base --is-ancestor\n   c3955341 HEAD` TRUE; `git show --stat` = exactly 7 files, 713(+)/99(-). Nothing rode along.\n   task.commits carries it kind=completion, recordedBy worker-d9877466, recordedAt 07:27:35.747Z.\n   bootstrap-policy-authority-reader.ts correctly EXCLUDED, as instructed.\n2. ALL SEVEN PATHS AT HEAD — `git cat-file -e HEAD:<path>` succeeds 7/7. The round-1 failing probe\n   inverts: `git grep -c readRunPolicyEvaluation HEAD -- apps packages` now EXIT 0.\n3. THE DRILL — I ran it myself once the paths were tracked, which is what round 1 could not do:\n     mutant  run-policy-selection.ts:140, remove\n             `if (authority.runId !== request.runId) return refuse(\"RUN_POLICY_SELECTION_RUN_MISMATCH\")`\n     -> × refuses a foreign run's row filed on this run's aggregate\n        × advertises exactly the codes its refusals actually produce\n        Tests 2 failed | 6 passed (8), EXIT 1\n     restore  git checkout -- ; `git diff --exit-code` EXIT 0 IDENTICAL_TO_HEAD ; porcelain clean ;\n              guard present again ; post-restore Tests 8 passed (8) EXIT 0\n   REDS BY NAME. And the second red is the notable one: removing a guard also reds \"advertises exactly\n   the codes its refusals actually produce\", so the code roster is GROUNDED IN OBSERVED BEHAVIOUR\n   rather than hand-maintained — a code that stops being producible cannot sit in the roster unnoticed.\n\n=== GATE RE-RUN BY ME, foreground ===\n  cd apps/daemon && pnpm exec vitest run --root . --config package.json <the six suites>\n  -> Test Files 6 passed (6), Tests 183 passed (183), EXIT 0.\nMatches the recorded verification exactly, and matches my own round-1 run.\n\n=== THE WORKER'S G1 DISCLOSURE IS CORRECT AND IT CORRECTS ME ===\nThey flagged their typecheck EXIT 0 as WORKTREE-measured. Verified:\n  git grep -c 'PAIRING_CLAIM_MAX_BODY_BYTES' HEAD -- apps/daemon/src        EXIT 1  (ABSENT)\n  worktree copy of pairing-approval-handshake.ts                            1 hit\n  pairing-approval-handshake.ts and http-listener-pairing-routes.ts are both ` M`\nSo the daemon typecheck is green ONLY because task-2f554e29's uncommitted constant sits in this\nworktree; at HEAD the TS2724 would still fire. I published \"the board-wide typecheck red is gone,\nEXIT 0\" in chat at 07:32 — that was a worktree measurement and the worker's framing is the accurate\none. Not a defect in this row either way: the sole failure mode at HEAD is a foreign file, and the\nepic's path-attributed rail asks only that the owned intersection be empty, which it is.\n\n=== WHAT MAKES THIS ROUND-2 SUBMISSION UNUSUALLY GOOD ===\n- G3 reported as UNSTABLE across two runs of one command at one sha (20 failed/6 files, then 18/5),\n  with the cause MEASURED (pairing-approval-handshake.test.ts being edited live by the seat on\n  task-2f554e29) and BOTH numbers published rather than the flattering one. That is precisely the\n  background-contamination artifact this board hit earlier today, correctly diagnosed and disclosed\n  instead of hidden.\n- The record_commit path-count discrepancy pre-empted: the ledger echoes EIGHT paths because it unions\n  with previously declared paths; the COMMIT carries seven. Stating that before a grader trips on it is\n  the right instinct.\n- No production bytes written in round 2, and it says so.\n\n=== ROUND-1 GRADING, CARRIED FORWARD UNCHANGED ===\nDoD 2 composition (run-policy-selection.ts:127 composes readPolicyEvaluationAuthority, :141 consumes\nauthority.runId — rail 2 satisfied structurally, no second byte-reader); DoD 3 branch honest (refusal\nADVANCES past RISK_TIER, durable ApprovalDecisionRecord events = ZERO, with the reason stated and\ntask-6093483c filed rather than the clause narrowed); line caps 247/142/239/243 all under 250; arms\nstrong at :490 :511 :525 :548 :564, including :525's regression arm for the bricking bug the original\nauthor found by measurement and refused to park as unreachable.\n\n=== THE CLAUSE DEFECT STANDS, AND IS NOT A REJECTION GROUND ===\nDoD 1's verify-or-block probe greps 'riskAssessment' in bootstrap-policy-authority-reader.ts, where\n`git log -S'riskAssessment'` returns ZERO commits — the token has never existed there. The field is\nriskTier, landed by 37f95c18 (task-a888038d, this row's own dependency). EXIT 1 was guaranteed whether\nthe gap was open or closed, so DoD 1's \"EXIT 0 = adopt/verify/report duplicate\" branch could never\nfire. The row COMPOSED rather than re-widened, which is what the rails require — the gate simply could\nnot have told them. Recorded for whoever writes the next verify-or-block clause: anchor the probe on a\ntoken that EXISTS in the state you are trying to detect.\n\n=== NOTED, NOT ACTIONED ===\nfileCollision: task-6093483c [T1-d mint] (PLANNING, filed BY this row) shares approval-record-facts.ts,\napproval-record-facts.test.ts and approval-intent.test.ts. Unclaimed, so no race today, but it lands on\nthe same three files.\n\nZero bytes of mine on the branch; my drill was restored and proven.",
      "timestamp": "2026-08-30T07:38:43.008Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T17:59:35.922Z",
  "updatedAt": "2026-08-30T07:52:02.438Z",
  "dependsOn": [
    "task-a888038dc44240e98d7bf6fbb41eb5d2",
    "task-ba1021652dcc4469bc4deb04a8e7d7d5",
    "task-3b61860f4f7045108aced01b2391f343",
    "task-be80cb7441d6483facd9c8f5b6c28096"
  ],
  "metrics": {
    "firstClaimAt": "2026-08-29T22:11:40.281Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 12,
    "rejectCount": 1,
    "reopenCount": 1,
    "doneAt": "2026-08-30T07:39:28.713Z",
    "wallClockMs": 34068432
  },
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [
    {
      "item": "Task rail 4 — explicit-pathspec commit of owned paths: NOTHING was committed. task.commits is empty and HEAD is another row's sha.",
      "rejectedAt": "2026-08-30T07:25:25.307Z",
      "rejectedBy": "qa-cc876c7b"
    },
    {
      "item": "DoD 2 — readRunPolicyEvaluation and the run-scoped selector: ABSENT from HEAD (git grep EXIT 1). run-policy-selection.ts/.js/.test.ts are untracked, so the clause cannot be verified at any sha.",
      "rejectedAt": "2026-08-30T07:25:25.307Z",
      "rejectedBy": "qa-cc876c7b"
    },
    {
      "item": "DoD 4 — mutation drills with SHA256 restore proofs: three of the drilled paths are UNTRACKED, so `git diff` is blind and no restore proof is constructible in this shared checkout. QA cannot re-run the drills until the paths land.",
      "rejectedAt": "2026-08-30T07:25:25.307Z",
      "rejectedBy": "qa-cc876c7b"
    }
  ],
  "planSubmittedAt": "2026-08-29T22:13:08.257Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — a second reader over PolicyEvaluated bytes for the run selection: rail 2 forbids; the selector COMPOSES the strict replay-verified reader per row and only chooses WHICH aggregate row to hand it. REJECTED — newest-project-wide selection with a runId filter afterwards: the ambiguity refusals must fire on selection, and 'newest' is exactly the bug the rail names; selection walks the run linkage. REJECTED — editing the 220-line reader to host the selector: at cap; new sibling module owns selection. REJECTED — running concurrently with 3b61860f/be80cb74: three-way same-file collision on approval-record-facts; deps serialize.",
    "codebaseInsights": "PLANNED AHEAD OF DEPS (human directive). Deps widened at plan time from [a888038d, ba102165] to ALSO include task-3b61860f and task-be80cb74: all three sibling rows edit approval-record-facts.{ts,test.ts} in the shared checkout, and this row's RISK_TIER slot plugs into the `firstMissingApprovalFact` walk that 3b61860f lands and the derived-slot idiom be80cb74 extends — the dependsOn field is the serialization, not prose. MEASURED AT HEAD b050fd5d: gap open — `git grep -n 'riskAssessment' HEAD -- apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts` EXIT 1; the strict reader (PolicyEvaluationAuthority :35, PolicyEvaluationAuthorityRefused :49) is 220 lines (AT CAP — the run-scoped selector goes in a NEW module, and the widening edit must stay surgical); the ONLY production consumer of readPolicyEvaluationAuthority is apps/daemon/src/activation/policy-admission-reader.ts:70 (plus type-only imports in admission-gate-resolver.ts:42,:103) — DoD-2's compile-unchanged clause covers those two; approval-record-facts.ts is 227 lines BEFORE the two sibling rows' edits — re-measure at claim, expect the walk `firstMissingApprovalFact` and slots riskTier?/stepUpAuthRef?/budgetRef? already present (3b61860f/be80cb74 landed by then; the dependsOn guarantees it). T1-b's contract (from its approved plan this session): finalize composes `evaluateRunPolicy` (new module apps/daemon/src/planning/run-policy-evaluation.ts) as a leg of the seal decision, writing ONE widened PolicyEvaluated per run carrying runId, server-derived graphNodeRevisionRefs, and riskAssessment (computedTier non-null); unclassifiable refuses and seals nothing.",
    "risks": "RISK 1 — this plan is written before T1-b/3b61860f/be80cb74 land; step 1 re-measures every anchor and ADAPTS names to the landed surfaces; a CONTRACT delta (no runId on the record; no walk export) is a blocked report, not improvisation. RISK 2 — the strict reader's widening must keep LEGACY rows (no runId) reading exactly as today: an arm pins both shapes; the replay-verification of riskAssessment must recompute/verify like every existing field, never trust presence. RISK 3 — the mint arm (DoD-3) is HONEST about the delivered tree: with stepUp (3b61860f) and budget (be80cb74) slots landed and this row's tier, all four facts may exist — if so the seam MINTS through validateApprovalRecord and the arm counts durable ApprovalDecisionRecord events; if any fact is still unproducible at the delivered tree, the arm asserts the advance to that EXACT next code and the completion comment states which. RISK 4 — selection refusals: absent / ambiguous (two rows claim the run) / unverified (replay fails) are THREE codes, each with its own arm and the layer asserted. RISK 5 — shared checkout: porcelain owned paths before staging; explicit pathspec only.",
    "keyFiles": [
      "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
      "apps/daemon/src/activation/policy-admission-reader.ts",
      "apps/daemon/src/planning/approval-record-facts.ts",
      "apps/daemon/src/planning/run-policy-evaluation.ts",
      "apps/daemon/src/planning/approval-intent.ts"
    ]
  },
  "budget": {
    "wallClockMs": 5400000,
    "warnedAt": "2026-08-30T06:43:37.849Z",
    "escalatedAt": "2026-08-30T06:43:37.849Z"
  },
  "planApprovedAt": "2026-08-29T22:13:10.409Z",
  "contextFetchedBy": [
    "worker-d9877466",
    "qa-cc876c7b",
    "architect-dba58d99"
  ],
  "workStartedAt": "2026-08-30T06:44:20.721Z",
  "reviewStartedAt": "2026-08-30T07:37:31.506Z",
  "verification": {
    "command": "cd apps/daemon && pnpm exec vitest run --root . --config package.json src/bootstrap/run-policy-selection.test.ts src/planning/approval-record-facts.test.ts src/planning/approval-intent.test.ts src/planning/run-policy-evaluation.test.ts src/activation/admission-gate-resolver.test.ts src/bootstrap/bootstrap-services.test.ts",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  6 passed (6)\n      Tests  183 passed (183)\n   Start at  10:26:52\n   Duration  4.51s (transform 9.93s, setup 0ms, import 13.16s, tests 4.59s, environment 0ms)\n\nEXIT=0",
    "reportedAt": "2026-08-30T07:37:31.506Z"
  },
  "filesModified": [
    "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
    "apps/daemon/src/bootstrap/run-policy-selection.ts",
    "apps/daemon/src/bootstrap/run-policy-selection.js",
    "apps/daemon/src/bootstrap/run-policy-selection.test.ts",
    "apps/daemon/src/planning/approval-record-facts.ts",
    "apps/daemon/src/planning/approval-record-facts.test.ts",
    "apps/daemon/src/planning/approval-intent.ts",
    "apps/daemon/src/planning/approval-intent.test.ts"
  ],
  "completionSummary": "ROUND 2 = LANDING ONLY. I wrote no production bytes. The engineering is worker-39362c3a's and was graded green by QA in round 1 (comment-5d4aeb04); round 1 failed on delivery alone. Full evidence: comment-fd0288df.\n\nSHA c3955341 on moe/work-2026-08-08, parent 076324e4. Explicit pathspec, -m before the --, index proven empty before and after. Seven paths, exactly the seven QA named: 713(+)/99(-); run-policy-selection.{ts,js,test.ts} created; approval-intent.{ts,test.ts} and approval-record-facts.{ts,test.ts} modified. bootstrap-policy-authority-reader.ts EXCLUDED as instructed (grep of the commit = 0). moe.record_commit called SEPARATELY (a hand landing fires no hook): commitCount 1, duplicate false. NOTE: record_commit echoes EIGHT paths because it unions with previously declared paths; the COMMIT carries seven.\n\nALL THREE ROUND-1 FAILURES CLEARED, re-measured with QA's own commands: (1) rail 4 - task.commits no longer []; (2) DoD 2 - `git grep -c readRunPolicyEvaluation HEAD -- apps packages` now EXIT 0 across four files; (3) DoD 4 - the three drilled paths are tracked, so `git diff` restore proofs are constructible and the drills re-runnable.\n\nGATES fresh at c3955341, foreground, unpiped. G2 (named verification) 6 files / 183 tests EXIT 0, matching the recorded run exactly. G1 typecheck EXIT 0 but DISCLOSED as worktree-measured: green only because task-2f554e29's PAIRING_CLAIM_MAX_BODY_BYTES=1024 sits in the worktree (absent at HEAD, grep EXIT 1) - a peer row's uncommitted bytes, nothing of mine. G3 EXIT 1 and UNSTABLE across two runs of one command at one sha: 20 failed/6 files then 18 failed/5 files; cause measured not guessed - pairing-approval-handshake.test.ts is being edited live by the seat on task-2f554e29. Both numbers reported rather than the flattering one.\n\nOWNED INTERSECTION EMPTY with a positive control: none of the three owned modules appears in the failing set; the unstable suite greps 0 for all three (EXIT 1) against a control returning 1 on a fil",
  "reviewCompletedAt": "2026-08-30T07:39:28.713Z",
  "rejectionDetails": {
    "failedDodItems": [
      "Task rail 4 — explicit-pathspec commit of owned paths: NOTHING was committed. task.commits is empty and HEAD is another row's sha.",
      "DoD 2 — readRunPolicyEvaluation and the run-scoped selector: ABSENT from HEAD (git grep EXIT 1). run-policy-selection.ts/.js/.test.ts are untracked, so the clause cannot be verified at any sha.",
      "DoD 4 — mutation drills with SHA256 restore proofs: three of the drilled paths are UNTRACKED, so `git diff` is blind and no restore proof is constructible in this shared checkout. QA cannot re-run the drills until the paths land."
    ],
    "issues": [
      {
        "type": "other",
        "file": "apps/daemon/src/bootstrap/run-policy-selection.ts",
        "description": "The row's central deliverable is UNTRACKED and absent from HEAD: `git grep -c readRunPolicyEvaluation HEAD -- apps packages` EXITS 1. Commit it with its .js bridge and .test.ts by explicit pathspec, then call moe.record_commit as a separate second call - a manual commit fires no wrapper hook."
      },
      {
        "type": "other",
        "file": "apps/daemon/src/planning/approval-record-facts.ts",
        "description": "Modified but uncommitted, along with approval-intent.ts and both .test.ts siblings. Four ' M' paths plus the three untracked ones = seven to land."
      },
      {
        "type": "other",
        "file": "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
        "description": "DO NOT add this to the pathspec - it is CLEAN and git add would stage nothing, silently shrinking the commit. Correctly unchanged: the riskTier/runId widening DoD 2 asks for already landed in 37f95c18 (task-a888038d); this row composes it rather than re-widening."
      },
      {
        "type": "other",
        "file": "apps/daemon/src/planning/approval-intent.ts",
        "description": "CLAUSE DEFECT, not a diff defect: DoD 1's probe greps 'riskAssessment' in bootstrap-policy-authority-reader.ts, where `git log -S` returns ZERO commits - the token never existed there. The field is riskTier. EXIT 1 was guaranteed either way, so DoD 1's 'EXIT 0 = report duplicate' branch could never fire. Add one line to the completion comment naming 37f95c18 as already satisfying DoD 2's first clause."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "LANDING FAILURE ONLY — THE ENGINEERING IS GRADED AND PASSES. Do not re-do it. Full evidence: comment-5d4aeb04e0454914baf2816d6baf1980.\n\n7 of 8 declared paths are not on the branch. HEAD = 076324e4 (be80cb74's sha), unmoved; task.commits = []; reviewStartedAt 07:19:11Z, measured through 07:23:07Z.\n  git grep -c 'readRunPolicyEvaluation' HEAD -- apps packages   EXIT 1   ABSENT\n   M approval-intent.ts, approval-intent.test.ts, approval-record-facts.ts, approval-record-facts.test.ts\n   ?? run-policy-selection.ts, run-policy-selection.js, run-policy-selection.test.ts\n\nNOT the ambiguous commits:[] case — bytes genuinely off-branch, so record_commit cannot repair it. DO NOT WAIT FOR THE WRAPPER: list_workers 07:21:58Z shows worker-39362c3a ALIVE but currentTaskId = task-2f554e29. The seat released this row; its exit commit can never fire.\n\nFIX: prove the index empty, `git add --` the seven, `git commit -m \"...\" -- <same seven>` with -m BEFORE the --, then moe.record_commit as a SEPARATE call. EXCLUDE bootstrap-policy-authority-reader.ts — it is clean, so git add stages nothing and shrinks the commit.\n\nVERIFIED BY ME, NOT SENT BACK: recorded gate re-run 6 files / 183 tests EXIT 0, matching exactly; daemon typecheck EXIT 1 is ONE foreign error attributed with a positive control, owned intersection EMPTY; line cap 247/142/239/243; rail 2 satisfied structurally; arms strong at :490 :511 :525 :548 :564.\n\nONE CLAUSE DEFECT, not a diff defect: DoD 1's probe greps 'riskAssessment' where that token has NEVER existed (git log -S = 0 commits); the field is riskTier, landed by 37f95c18 — this row's own dependency. EXIT 1 was guaranteed either way, so DoD 1's \"EXIT 0 = report duplicate\" branch could never fire. Composing instead of re-widening was correct. ADD ONE LINE to the completion comment: DoD 2's first clause was already satisfied at HEAD by 37f95c18.\n\nRound 2 I verify the sha, the seven paths at HEAD, and the drills.",
      "rejectedAt": "2026-08-30T07:25:25.307Z",
      "reopenCount": 1,
      "failedDodItems": [
        "Task rail 4 — explicit-pathspec commit of owned paths: NOTHING was committed. task.commits is empty and HEAD is another row's sha.",
        "DoD 2 — readRunPolicyEvaluation and the run-scoped selector: ABSENT from HEAD (git grep EXIT 1). run-policy-selection.ts/.js/.test.ts are untracked, so the clause cannot be verified at any sha.",
        "DoD 4 — mutation drills with SHA256 restore proofs: three of the drilled paths are UNTRACKED, so `git diff` is blind and no restore proof is constructible in this shared checkout. QA cannot re-run the drills until the paths land."
      ],
      "issues": [
        {
          "type": "other",
          "file": "apps/daemon/src/bootstrap/run-policy-selection.ts",
          "description": "The row's central deliverable is UNTRACKED and absent from HEAD: `git grep -c readRunPolicyEvaluation HEAD -- apps packages` EXITS 1. Commit it with its .js bridge and .test.ts by explicit pathspec, then call moe.record_commit as a separate second call - a manual commit fires no wrapper hook."
        },
        {
          "type": "other",
          "file": "apps/daemon/src/planning/approval-record-facts.ts",
          "description": "Modified but uncommitted, along with approval-intent.ts and both .test.ts siblings. Four ' M' paths plus the three untracked ones = seven to land."
        },
        {
          "type": "other",
          "file": "apps/daemon/src/bootstrap/bootstrap-policy-authority-reader.ts",
          "description": "DO NOT add this to the pathspec - it is CLEAN and git add would stage nothing, silently shrinking the commit. Correctly unchanged: the riskTier/runId widening DoD 2 asks for already landed in 37f95c18 (task-a888038d); this row composes it rather than re-widening."
        },
        {
          "type": "other",
          "file": "apps/daemon/src/planning/approval-intent.ts",
          "description": "CLAUSE DEFECT, not a diff defect: DoD 1's probe greps 'riskAssessment' in bootstrap-policy-authority-reader.ts, where `git log -S` returns ZERO commits - the token never existed there. The field is riskTier. EXIT 1 was guaranteed either way, so DoD 1's 'EXIT 0 = report duplicate' branch could never fire. Add one line to the completion comment naming 37f95c18 as already satisfying DoD 2's first clause."
        }
      ]
    }
  ],
  "commits": [
    {
      "sha": "c39553412f3bdc87ea2ab1958e92b2e7868604d2",
      "ref": "moe/work-2026-08-08",
      "kind": "completion",
      "role": "worker",
      "sessionId": "worker-d9877466@2026-08-30T07:00:00.000Z",
      "paths": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/bootstrap/run-policy-selection.ts",
        "apps/daemon/src/bootstrap/run-policy-selection.js",
        "apps/daemon/src/bootstrap/run-policy-selection.test.ts"
      ],
      "recordedBy": "worker-d9877466",
      "recordedAt": "2026-08-30T07:27:35.747Z",
      "status": "WORKING",
      "pushed": false,
      "message": "feat(task-f42d5165): expose run linkage through the strict policy reader and fill the RISK_TIER slot in readApprovalRecordFacts"
    }
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "worker-d9877466@2026-08-30T07:00:00.000Z",
    "at": "2026-08-30T07:27:35.747Z"
  },
  "completedAt": "2026-08-30T07:39:28.713Z",
  "reviewSummary": "APPROVED round 2 (landing fix). Full evidence: comment-cc9d5d24.\n\nTHREE ROUND-2 CRITERIA, all green:\n1. sha c3955341 — branch --contains = moe/work-2026-08-08; merge-base --is-ancestor HEAD TRUE; show --stat = exactly 7 files, 713+/99-. task.commits kind=completion. bootstrap-policy-authority-reader.ts correctly EXCLUDED.\n2. all seven paths AT HEAD (cat-file -e 7/7); the round-1 probe inverts — `git grep -c readRunPolicyEvaluation HEAD` now EXIT 0.\n3. THE DRILL, run by me now the paths are tracked: removing run-policy-selection.ts:140's run-mismatch guard reds TWO arms BY NAME — \"refuses a foreign run's row filed on this run's aggregate\" and \"advertises exactly the codes its refusals actually produce\" (2 failed | 6 passed, EXIT 1). Restore proven: git checkout, `git diff --exit-code` EXIT 0 identical to HEAD, porcelain clean, post-restore 8 passed (8). The second red shows the code roster is grounded in observed behaviour, not hand-maintained.\n\nGATE RE-RUN BY ME, foreground: the recorded six-suite command -> 6 files / 183 tests, EXIT 0, matching the record and my round-1 run.\n\nTHEIR G1 DISCLOSURE IS CORRECT AND CORRECTS ME: the daemon typecheck EXIT 0 is WORKTREE-measured — `git grep -c PAIRING_CLAIM_MAX_BODY_BYTES HEAD` EXITS 1, the constant lives only in task-2f554e29's uncommitted bytes. I published \"the board-wide red is gone\" from a worktree run; theirs is the accurate framing. Not a defect: the sole HEAD failure mode is foreign, owned intersection empty.\n\nEXEMPLARY: G3 reported UNSTABLE across two runs at one sha (20/6 then 18/5), cause measured (a peer editing that file live), BOTH numbers published rather than the flattering one; the eight-vs-seven record_commit path discrepancy pre-empted; no production bytes written in round 2.\n\nCLAUSE DEFECT stands, not a rejection ground: DoD 1's probe greps a token that has never existed in that file (git log -S = 0 commits); the field is riskTier, landed by 37f95c18, so EXIT 1 was guaranteed either way."
}