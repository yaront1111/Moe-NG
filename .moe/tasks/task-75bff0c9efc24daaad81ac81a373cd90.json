{
  "id": "task-75bff0c9efc24daaad81ac81a373cd90",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "A node author carries declared migration identifiers from authored planning source into the compiled node",
  "description": "WRITER CHILD of task-122b55fd2c284174876bd1d5573bdcc5, filed by architect-dbe3631e 2026-09-07 under governor-afbde490's 20:30Z direction, which offered exactly this choice: \"bounded scope amendment (fields + recursion + writer path + version/compatibility policy per DoD 3) OR a writer child with corrected DoD/deps\". The writer child is the choice, because the authority half alone already occupies 9 files.\n\nTHE SPLIT, IN BUILD ORDER. task-122b55fd (this row's dependsOn) makes the durable node-authority format ABLE to carry a bounded migration declaration: the v3 roster member, its admission with duplicate REFUSAL, the version-scoped preimage so historical hashes never move, compose/project and the public export. THIS ROW makes an AUTHORED SOURCE able to state one and carry it through the compiler into that format. task-de65294bfa2649b99afc3aa4aa9399d0 then serves it over /runs/read and the browser decoder.\n\nTHE GAP, MEASURED AT HEAD 29f5d723 (re-measure before planning; global rail 6):\n- `packages/scheduler/src/node-authority/node-planning-source-format.ts` (232 lines) - NODE_PLANNING_SOURCE_SCHEMA_VERSION = 1 at :24, its own digest domain at :26, closed ENVELOPE_KEYS at :92 and DEPENDENCY_KEYS at :91. No migration member anywhere.\n- `packages/scheduler/src/node-authority/node-planning-source-codec.ts` (232 lines) - exact-key decode and reconstruction; `sameNodePlanningSourceBytes` at :229 is the byte-identity comparator.\n- `packages/scheduler/src/node-authority/node-planning-source-bounds.ts` (81 lines) - where a new bound belongs.\n- `apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts` (122 lines) - `SOURCE_KEYS` at :15-18 is an EXACT four-name roster checked by `exact(snapshot.value, SOURCE_KEYS)` at :85, and the draft at :98-119 is written out FIELD BY FIELD. Both must carry the declaration or it is dropped between the authored source and the authority draft, silently.\n- `apps/daemon/src/planning/v2-compiler/authority-contracts.ts` (84 lines) - `V2CompilerNodeAuthorityRequest` carries no declaration.\n- `apps/daemon/src/delivery-v2/node-planning-source-record.ts` - forwards admitted encoded bytes wholesale (worker-f72a1438 measured :86-97, :130-147, persistence :99), so it needs no rewrite, but that claim needs a test rather than an assumption.\n\nAlso measured and NOT this row's to change: `compiled-policy-authority-body.ts:102-126` and `journey-authority-bodies.ts:126-144` write explicit drafts too. Decide at planning time whether they must forward the member or may legitimately declare none, and SAY WHICH - do not leave a second silent drop path.\n\nNOT IN SCOPE: the authority format itself (task-122b55fd owns the roster, the version policy, the preimage and the admission codes - consume them, do not redefine them), /runs/read and the browser decoder (task-de65294b), and the UI wording (task-78eedd8ebc2b43c581ac21ea28648a9a).",
  "definitionOfDone": [
    "An AUTHORED planning source can state zero, one or several bounded migration identifiers, and the source's own format version, canonical bytes, digest domain and bounds all account for the member. The exact-key decoder refuses an unknown key and a malformed member with a stable code AND the layer that refused; a source that states none is DISTINCT from a legacy source that states nothing, carrying task-122b55fd's UNKNOWN-vs-explicit-empty distinction rather than collapsing both.",
    "THE COMPILER FORWARDS IT, ASSERTED END TO END: a declaration stated in authored source reaches the compiled NodeDefinition through the PRODUCTION path (`readNodePlanningDefinition`), asserted BY VALUE on the resulting definition - not on a hand-built draft and not by calling the authority codec directly. `SOURCE_KEYS` (scheduler-node-planning-authority.ts:15-18) and the explicit draft (:98-119) both name the member; a test proves the member survives the `exact(...)` roster check that would otherwise reject the wider source.",
    "THE SILENT-DROP PATHS ARE CLOSED AND NAMED. Every site that writes an explicit NodeDefinition draft is enumerated whole-tree (`compiled-policy-authority-body.ts`, `journey-authority-bodies.ts`, `scheduler-node-planning-authority.ts` at minimum) with a verdict per site: forwards the declaration, or legitimately declares none and says why in a comment at the site. A site that neither forwards nor states its choice is the defect this clause exists to prevent.",
    "TWO IDENTIFIERS BOTH SURVIVE the source codec, the compiler and the resulting definition, asserted by value AND by count - a path that keeps only the first passes every single-identifier fixture. DUPLICATES ARE REFUSED, not deduplicated: assert the specific code and layer, and confirm the refusal comes from the intended layer rather than from a second admission arm downstream.",
    "BYTE IDENTITY AND FORWARDING ARE PROVED, NOT ASSUMED: `sameNodePlanningSourceBytes` still answers correctly across the new member, and `delivery-v2/node-planning-source-record.ts` forwarding admitted bytes wholesale is asserted by a test rather than claimed from a reading.",
    "MUTATION DRILL, quoted with its failing line: drop the member from `SOURCE_KEYS` (or from the explicit draft) and confirm the end-to-end arm from DoD 2 goes RED. Restore per global rail 13 - `git status --porcelain` the mutated paths first, and prove an untracked path's restore by sha256 against an out-of-tree backup in a call separate from the restore.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/scheduler test` and `pnpm --filter @moe/daemon test` (this row spans both packages), plus `pnpm typecheck`. Root `pnpm test` covers no apps/** file, so say that rather than quoting a bare root count line. Any new runtime module needs its one-line tracked `.js` bridge, and any new cross-package import needs the manifest dependency plus the lockfile importer entry."
  ],
  "taskRails": [
    "CONSUME task-122b55fd's AUTHORITY CONTRACT, DO NOT REDEFINE IT. The roster member name, its bound, its refusal codes, its layer and the UNKNOWN-vs-explicit-empty policy are that row's to set. If what landed does not fit this row's need, report blocked and have it amended - never mint a second spelling.",
    "NO INFERENCE. A declaration is stated by the author or it does not exist. Never derive it from an objective, a filename, a diff, writeScopes or verifier output, and never default to an empty list at the output and call it authored (task-122b55fd rail 1).",
    "ASSERT THROUGH THE PRODUCTION COMPILER PATH. `readNodePlanningDefinition` is the seam; a test that builds a draft by hand proves the authority codec works and proves nothing about the carrier this row exists to build.",
    "DUPLICATES REFUSE, THEY DO NOT DEDUPLICATE. The generic scheduler list reader silently dedups and sorts (node-authority-fields.ts:76-77); do not reuse it for this member without confirming which behaviour task-122b55fd landed.",
    "NO HISTORICAL REWRITE. An authored source that predates the member stays readable and keeps its bytes and its digest; do not migrate stored sources in place."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "READ THE LANDED CONTRACT OFF DISK AND BASELINE. NO EDITS. The producer task-122b55fd LANDED as commit 4710d60b and is in REVIEW (not DONE), so your dependsOn gate opens only when QA approves it — but the contract is already on disk and you consume it VERBATIM (task rail 1). Confirm each of these; if any has MOVED because QA sent that row back, STOP AND REPORT rather than building to my citation:\n(a) `grep -n 'declaredMigrations' packages/scheduler/src/node-authority/node-authority-contract.ts` — roster member at :100, and the type at :193 is `readonly declaredMigrations?: readonly string[]`, **OPTIONAL**. Absent = UNKNOWN, `[]` = the author declared none.\n(b) `sed -n '95,118p' packages/scheduler/src/node-authority/node-authority-fields.ts` — `readDeclaredMigrations`. **AUTHORED ORDER IS PRESERVED, NOT SORTED**: it does `declared.push(identifier)` with no `.sort()`, and its own comment says the order is content that the digest binds. Your source layer must preserve authored order too, or two authored spellings collapse into one identity.\n(c) THE EXACT REFUSAL VOCABULARY you must not re-mint: duplicate -> `NODE_AUTHORITY_DUPLICATE_MIGRATION` @ `NODE_AUTHORITY_ADMISSION` (:112-113); non-list and inadmissible entry -> `NODE_AUTHORITY_FIELD_INVALID` @ `NODE_AUTHORITY_ADMISSION` (:99-101, :110-111); over-bound -> `NODE_AUTHORITY_LIMIT_EXCEEDED` @ `NODE_AUTHORITY_LIMITS` (:103-105). The bound is `maxMigrationEntries: 64` (contract.ts:154) and entries are admitted by `asId`.\n(d) `sed -n '120,132p' packages/scheduler/src/node-authority/node-authority-codec.ts` — **THE VERSION IS CHOSEN BY CONTENT, NEVER STATED BY A CALLER**: `minted = draft.declaredMigrations === undefined ? NODE_AUTHORITY_UNDECLARED_SCHEMA_VERSION : NODE_AUTHORITY_SCHEMA_VERSION`. `node-authority-recursion.ts:205,220` selects the preimage roster by PRESENCE for the same reason. **THIS IS THE PATTERN YOUR SOURCE LAYER COPIES**, one level up — do not invent a second one (task rail 1).\n(e) `grep -n 'declaredMigrations' packages/scheduler/src/node-authority/node-authority-fields.ts packages/scheduler/src/node-authority/node-authority-compose.ts` — both spread conditionally (`...(x === undefined ? {} : { declaredMigrations: x })`, fields.ts:290-292, compose.ts:249-250). **An assigned `undefined` is not an absent key** once `Object.keys` runs, and the codec byte-compares its own re-encode. Do the same in the source layer.\nBASELINE, quoted with `Test Files N passed` count lines: `pnpm --filter @moe/scheduler test` (this row edits scheduler files). Do not run the daemon leg yet — step 7 owns it. Disclose and attribute any peer red.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-format.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts"
      ],
      "note": "BASELINE + CONTRACT RE-READ AT HEAD 8f0b91ee. No edits. All five step-1 citations confirmed VERBATIM — the landed contract has NOT moved, so I build to the architect's citation rather than reporting.\n\n(a) node-authority-contract.ts: roster member `\"declaredMigrations\"` at :100; type `readonly declaredMigrations?: readonly string[]` at :193, OPTIONAL. Absent = UNKNOWN, `[]` = declared none. Comments at :41 and :52 state the same policy.\n(b) node-authority-fields.ts `readDeclaredMigrations` at :98-118. AUTHORED ORDER PRESERVED: `declared.push(identifier)`, no `.sort()`; its comment says outright \"NOT `idList`... `readList` silently DEDUPS and SORTS... AUTHORED ORDER IS PRESERVED... two admissible spellings differ only in an order that is content, which the digest binds.\" My source layer copies this, not `readList`.\n(c) REFUSAL VOCABULARY, exact and not to be re-minted: duplicate -> NODE_AUTHORITY_DUPLICATE_MIGRATION @ NODE_AUTHORITY_ADMISSION (:112-113); non-list -> NODE_AUTHORITY_FIELD_INVALID @ NODE_AUTHORITY_ADMISSION (:99-101); inadmissible entry -> same pair (:110-111); over-bound -> NODE_AUTHORITY_LIMIT_EXCEEDED @ NODE_AUTHORITY_LIMITS (:103-105). Bound `maxMigrationEntries: 64` at contract.ts:154; entries admitted by `asId`.\n(d) VERSION BY CONTENT, never by caller: node-authority-codec.ts:127-128 `const minted = drafted.draft.declaredMigrations === undefined ? NODE_AUTHORITY_UNDECLARED_SCHEMA_VERSION : NODE_AUTHORITY_SCHEMA_VERSION`. node-authority-recursion.ts:220 selects the preimage roster by `Object.hasOwn(body, DECLARED_MIGRATIONS_KEY)` for the same reason. This is the pattern the source layer copies one level up.\n(e) CONDITIONAL SPREAD both sides: fields.ts:290-292 `...(declared === null ? {} : { declaredMigrations: declared.value })` with the comment \"an assigned undefined is still an own key to Object.keys\"; compose.ts:249-250 identical shape.\n\nEXTRA MEASURED, not in the step but load-bearing for step 5: the compiler's forward reaches the member without any authority-side change. `createNodeDefinitionFromPlanningContent` (codec.ts:140) -> `createWithPlanning(input, CONTENT_CREATE_KEYS, ...)` -> `draftNodeAuthority(pick(input,\"draft\"))` -> `readDraftFields(value)` with default `allowed = NODE_AUTHORITY_DRAFT_KEYS` and default `version = NODE_AUTHORITY_SCHEMA_VERSION` (3), and fields.ts:215-226 admits `declaredMigrations` as the one absence-admissible member. So a draft carrying the member flows through; the `version < 3` refusal at :223 cannot fire on this path because the default is 3.\n\nBASELINE, `pnpm --filter @moe/scheduler test`, EXIT 0:\n  Test Files  49 passed (49)\n  Tests  1726 passed (1726)\nNo peer red to disclose on the scheduler leg. Daemon leg deliberately NOT run — step 7 owns it.",
      "startedAt": "2026-09-07T15:11:19.761Z",
      "completedAt": "2026-09-07T15:12:46.996Z"
    },
    {
      "stepId": "step-2",
      "description": "THE ARMS FIRST, RED BEFORE ANY PRODUCTION EDIT.\nIN `packages/scheduler/src/node-authority/node-planning-source-codec.test.ts` (382 lines, existing):\n(a) **BYTE IDENTITY OF A LEGACY SOURCE** — the sharpest hazard, exactly as it was one layer down. Encode/decode an existing source that declares nothing and assert the bytes and `sourceDigest` are EXACTLY unchanged, and that the member is ABSENT from the decoded content (`\"declaredMigrations\" in content` is false, NOT `toEqual([])`). A defaulted `[]` would move every stored source's digest and mint a declaration nobody authored (task rail 2).\n(b) **ROUND TRIP BY VALUE AND BY COUNT** with TWO identifiers, and **IN AUTHORED ORDER** — assert the exact array, not a sorted or set-compared one, because the landed authority preserves order as content (step 1(b)). A path keeping only the first passes every single-identifier fixture (DoD 4).\n(c) **EXPLICIT EMPTY IS DISTINCT FROM ABSENT** (DoD 1): a source stating `[]` decodes with the member PRESENT and empty, and its `sourceDigest` differs from the same source encoding nothing.\n(d) **THE DECODER STAYS EXACT-KEY**: an unknown key still refuses, and a malformed member (non-list, non-string entry, over-bound, duplicate) refuses with its stable code AND the layer that refused. Assert the LAYER too — the source layer and the authority layer can both say a value is bad, and an arm that only checks a code cannot tell which one answered (global rail 1).\n(e) **`sameNodePlanningSourceBytes` still answers correctly** across the member (DoD 5): equal for identical declarations, unequal for different ones and for declared-none vs absent.\nIN A NEW FILE `apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts` — new because NOTHING in the tree drives `readNodePlanningDefinition` today (`git grep -ln readNodePlanningDefinition -- 'apps/**'` returns only the two production modules), which is precisely why this member could be dropped between the source and the draft without a single test noticing:\n(f) **THE END-TO-END ARM (DoD 2), and the row's whole point.** Drive the PRODUCTION `readNodePlanningDefinition` with a reader returning a source that declares two identifiers, and assert them BY VALUE on the returned `NodeDefinition.declaredMigrations`. Not on a hand-built draft, not by calling the authority codec directly (task rail 3).\n(g) **THE ROSTER ARM**: the same call with a source that declares NOTHING still succeeds and the returned definition has NO `declaredMigrations` key — this is what proves the widened `exact(...)` check did not become mandatory and break every existing authored source.\n(h) **THE DROP ARM**: assert the definition's `schemaVersion` differs between the declaring and non-declaring cases, so a forward that silently loses the member cannot pass by returning a valid definition.\nA test file needs no `.js` bridge; only runtime modules do. Run both files and quote the failing lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-codec.test.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts"
      ],
      "modifiedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-codec.test.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts"
      ],
      "note": "ARMS WRITTEN, RED BEFORE ANY PRODUCTION EDIT. Both files run against UNTOUCHED production.\n\nSCHEDULER LEG — `pnpm --filter @moe/scheduler test`, 8 new arms RED:\n  Test Files  1 failed | 48 passed (49)\n  Tests  8 failed | 1730 passed (1738)\nFailing lines, verbatim:\n  × leaves a source that declares nothing byte-identical and version-1\n    AssertionError: expected 'e02e1dde54e8ea5f5cdf02d084b0095885f8d…' to be '0000…'\n  × round-trips two identifiers by value, by count and in authored order\n  × keeps an explicit empty declaration distinct from an absent one\n  × refuses a duplicated identifier at the source layer, not the authority layer\n  × refuses an over-bound declaration at the source layer, not the authority layer\n  × refuses a stated version the content does not imply, either way\n  × refuses a wire whose schema tag disagrees with its envelope roster\n    - \"code\": \"NODE_PLANNING_SOURCE_SCHEMA_MISMATCH\" / + \"NODE_PLANNING_SOURCE_UNSUPPORTED_SCHEMA\"\n  × keeps sameNodePlanningSourceBytes answering across the declaration\n\nTHE GOLDEN IS A PRE-CHANGE CAPTURE, NOT A RE-GRADE. `LEGACY_SOURCE_DIGEST = e02e1dde54e8ea5f5cdf02d084b0095885f8d48f709879e6c41a6e6da82b4c51` was read off the FAILING run above, with production still at HEAD 8f0b91ee. Arm (a) also pins the four v1 envelope keys and the tag `MOE-NODE-PLANNING-SOURCE/1`, so a defaulted `[]` cannot pass by moving both sides.\n\nA VACUITY HOLE I FOUND AND CLOSED, worth the note. Four of the six malformed-member cases went GREEN on the first run against untouched production — because today the exact-key roster refuses `declaredMigrations` as an UNRECOGNISED KEY with exactly the code and layer they expected (`NODE_PLANNING_SOURCE_MALFORMED` @ `NODE_PLANNING_SOURCE_ADMISSION`). A code-and-layer arm therefore cannot tell \"member admitted, then refused for its value\" from \"member never admitted at all\", so the roster-widening half of this change would have shipped untested. Fix: the arm now pins the MESSAGE too (`toEqual([{code, layer, message}])`), and the messages are the landed authority's own words — \"declaredMigrations is not a list\", \"...holds an inadmissible entry\", \"...states one identifier twice\", \"...exceeds its bound\" — versus the roster's \"planning source is not an exact source record\". Only the duplicate and over-bound arms are red now; the other four go from green-for-the-wrong-reason to green-for-the-right-one at step 4.\n\nTHREE PRE-EXISTING ARMS RETARGETED, each strictly not weaker:\n- `:146` version of a non-declaring source: `NODE_PLANNING_SOURCE_SCHEMA_VERSION` -> `NODE_PLANNING_SOURCE_UNDECLARED_SCHEMA_VERSION`, because version is chosen by content and this fixture states nothing.\n- `:175` `version: 2` -> `version: 3`. 2 becomes the DECLARING schema, so a stated 2 over declaration-free content is a MISMATCH (its own new arm); 3 is still genuinely unsupported, so the arm keeps its subject.\n- `:186` wire tag `/2` -> `/3`, same reason; the `/2`-over-a-v1-envelope case gets its own arm asserting `NODE_PLANNING_SOURCE_SCHEMA_MISMATCH`.\nNet: two cases become three, none removed.\n\nDAEMON LEG — NEW FILE `apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts`, the first test in the tree to drive `readNodePlanningDefinition` (confirmed: `git grep -ln readNodePlanningDefinition -- 'apps/**'` still returns only the two production modules).\n  Test Files  1 failed (1)\n  Tests  4 failed | 3 passed (7)\n  × forwards two identifiers by value, by count and in authored order -> expected undefined to be defined\n  × mints a different schema version for a declaring source than for a silent one -> expected undefined to be 3\n  × admits an explicit empty declaration as a stated fact, not as absence -> expected undefined to be defined\n  × forwards admitted planning-source bytes wholesale through the delivery record -> expected false to be true\n  ✓ still compiles a source that declares nothing, with the member absent\n  ✓ refuses a duplicated identifier rather than repairing it\n  ✓ refuses a noncanonical identifier rather than repairing it\nTHE RED PROFILE IS THE RIGHT ONE: arm (g), which proves the widened roster did NOT become mandatory, is GREEN today and must STAY green — it is the regression guard, not a target. The four declaration arms are the targets. The two refusal arms pass vacuously today (the whole source is refused as an unknown key) and become non-vacuous at step 5.\n\nFIXTURE DEFECT FOUND WHILE WRITING IT, not while debugging my own change: my hand-built `V2CompilerNodeAuthorityRequest` first produced `undefined` for EVERY arm including (g). Cause was mine, measured rather than guessed by driving the authority codec directly: `[{\"code\":\"NODE_AUTHORITY_LIMIT_EXCEEDED\",\"layer\":\"NODE_AUTHORITY_LIMITS\",\"message\":\"admissionAmounts is empty or exceeds its bound\"}]` — `readAmounts` (node-authority-budget.ts:63) refuses length 0. Fixed by building amounts from `ADMISSION_PURPOSES` the way activation-world-fixtures.ts:156 does. Had I not chased it, arm",
      "startedAt": "2026-09-07T15:12:51.350Z",
      "completedAt": "2026-09-07T15:22:47.203Z"
    },
    {
      "stepId": "step-3",
      "description": "THE SOURCE FORMAT — `packages/scheduler/src/node-authority/node-planning-source-format.ts` (232 lines; keep under 300).\n(1) **THE MEMBER, OPTIONAL**: add `readonly declaredMigrations?: readonly string[]` to `NodePlanningSourceContent` (:64-70). Optional for the same reason it is optional one layer down: absent = the source predates the member or declares nothing knowable, `[]` = the author declared none. Comment it, because `authority-contracts.ts` derives `V2CompilerNodePlanningAuthority = Omit<NodePlanningSourceContent, \"version\">` from this type and the compiler reads that shape.\n(2) **VERSION BY PRESENCE, COPYING THE LANDED PATTERN** (step 1(d)): keep `NODE_PLANNING_SOURCE_SCHEMA_VERSION` as the current version and add an UNDECLARED version constant plus per-version tag and digest-domain lookups — v1 keeps `MOE-NODE-PLANNING-SOURCE/1` and `MOE-NODE-PLANNING-SOURCE-CONTENT-HASH/1` BYTE FOR BYTE, the new version gets its own pair. A source is minted at the LOWEST version that can carry it, chosen by whether it states the member — never by a caller-supplied version, which is how a declaring source would get framed with the old tag.\n(3) **NO NEW BOUND IS NEEDED.** This module already imports `NODE_AUTHORITY_LIMITS` (:20); reuse `maxMigrationEntries` (64) and the same `asId`-shaped entry admission, so the source and the authority agree by construction rather than by two bounds that can drift. Do NOT edit `node-planning-source-bounds.ts` — state that you checked and why it needs nothing.\n(4) **REFUSALS**: if the source layer needs its own duplicate/oversize code, add it to `NODE_PLANNING_SOURCE_CODES` (:28) rather than reusing an authority code in a source-layer refusal — arm 2(d) asserts WHICH layer answered, and two layers sharing one code makes that arm unable to discriminate.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-format.ts"
      ],
      "modifiedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-format.ts",
        "packages/scheduler/src/node-authority/node-planning-source-schema.ts",
        "packages/scheduler/src/node-authority/node-planning-source-schema.js"
      ],
      "note": "THE SOURCE FORMAT, with one SCOPE ADDITION I had to make and am naming rather than burying.\n\nTHE SPLIT, forced by the cap. The step said \"232 lines; keep under 300\". With the member, the two version constants, the per-version tag/domain lookups, the envelope key, the digest framing and the source-layer declaration reader, `node-planning-source-format.ts` reached **382 lines** — past the step's 300 and near global rail 2's hard 400. Rail 2's prescribed remedy is to split the FILE, so I did: new `packages/scheduler/src/node-authority/node-planning-source-schema.ts` (161 lines) plus its one-line tracked bridge `node-planning-source-schema.js` (`export * from \"./node-planning-source-schema.ts\";`, matching every sibling). `node-planning-source-format.ts` is now **251 lines**.\nTHE SEAM IS NOT ARBITRARY: schema.ts owns the VOCABULARY the wire is spelled in (codes, layers, versions, tags, digest domains, `refuse`, `own`, and the declaration reader); format.ts owns the WIRE (envelope rosters, framing, encode, decode, byte comparator). format.ts carries `export * from \"./node-planning-source-schema.js\"`, so EVERY existing importer keeps its specifier and no call site outside this row moves — `node-planning-source-codec.ts` still imports `own`/`refuse`/the codes from format and compiles untouched.\n\n(1) THE MEMBER, OPTIONAL. `readonly declaredMigrations?: readonly string[]` on `NodePlanningSourceContent`, commented at the site with why absence is load-bearing and with the note that `authority-contracts.ts:74` derives `V2CompilerNodePlanningAuthority = Omit<NodePlanningSourceContent, \"version\">` from it, so the compiler inherits the optionality instead of restating it. `version` widened from `typeof NODE_PLANNING_SOURCE_SCHEMA_VERSION` to `NodePlanningSourceSchemaVersion`.\n\n(2) VERSION BY PRESENCE, copying node-authority-codec.ts:127 exactly. `NODE_PLANNING_SOURCE_UNDECLARED_SCHEMA_VERSION = 1`, `NODE_PLANNING_SOURCE_SCHEMA_VERSION = 2` — the same naming the landed authority uses (`_SCHEMA_VERSION` = highest mintable, `_UNDECLARED_` = legacy). `nodePlanningSourceVersionOf(declared)` returns UNDECLARED for `undefined` and CURRENT otherwise; `nodePlanningSourceWireOf` reads the CONTENT, never `content.version`, so a caller cannot frame a declaring source with the v1 tag. v1 keeps `MOE-NODE-PLANNING-SOURCE/1` and `MOE-NODE-PLANNING-SOURCE-CONTENT-HASH/1` byte for byte; v2 gets its own pair. The declaration is framed into the digest LAST and ONLY when stated, so a non-declaring source hashes over exactly the three segments and the one domain it always did.\n\nA PUBLISHED-CONSTANT DECISION I MADE DELIBERATELY, because it is the kind of thing that gets noticed later. `NODE_PLANNING_SOURCE_DIGEST_DOMAIN` is a ROOT-PUBLISHED export. I left its VALUE unchanged at the v1 domain rather than repointing it at the current version, and said why at the site: its value is what every stored source's digest was computed under, so moving it would make a published constant disagree with every durable identity derived from it. The declaring domain is `NODE_PLANNING_SOURCE_DIGEST_DOMAINS[2]` and stays unpublished, for the same reason `index-surface.test.ts:319-322` gives for withholding `NODE_AUTHORITY_UNDECLARED_SCHEMA_VERSION`: a reader distinguishes a declaring source by the member's PRESENCE, never by re-deriving a domain. Nothing new is added to `node-authority-public.ts`, so `EXPECTED_EXPORTS.length === 145` and the bidirectional node-authority surface arm (which enumerates six modules, none of them these two) are both untouched.\n\n(3) NO NEW BOUND, and I checked rather than assumed. `node-planning-source-bounds.ts` is UNMODIFIED. It measures the recursive DEPENDENCY graph's lower bound; a declaration is a flat list of graph keys, so it is bounded by the authority's own `maxMigrationEntries` (64) with each entry admitted by the same `isGraphKey` + `readText(..., maxIdBytes)` pair `asId` uses (fields.ts:81-82) — reused, not restated, so the two layers cannot drift. The whole envelope is still ceilinged by `NODE_AUTHORITY_LIMITS.maxBytes` after encode.\n\n(4) REFUSALS ARE THIS LAYER'S OWN. Added `NODE_PLANNING_SOURCE_DUPLICATE_MIGRATION` and `NODE_PLANNING_SOURCE_SCHEMA_MISMATCH` to `NODE_PLANNING_SOURCE_CODES`; no authority code is reused in a source-layer refusal, so step 2's layer-and-message arms can discriminate. `NODE_PLANNING_SOURCE_CODES` has no length pin anywhere in the tree (checked: only `index-surface.test.ts:190` asserts it is an \"array\"), so adding two entries breaks nothing.\n\nWIRE HOME, decided and commented at the site per step 4(2): the declaration rides its OWN optional envelope key `declaredMigrationsBytesBase64`, not inside the dependency blob. Reason at the site: it is not dependency material, so burying it would make `dependencyContentBytesBase64` a lie and force every dependency reader to know a field with nothing to do with edges; and a distinct key gives ABSENCE a spelling on the durable wire — the key is simply not t",
      "startedAt": "2026-09-07T15:22:49.608Z",
      "completedAt": "2026-09-07T15:27:34.128Z"
    },
    {
      "stepId": "step-4",
      "description": "THE SOURCE CODEC — `packages/scheduler/src/node-authority/node-planning-source-codec.ts` (232 lines; keep under 300).\n(1) **THE KEY ROSTERS ADMIT IT OPTIONALLY.** `CONTENT_KEYS = [...DRAFT_KEYS, \"version\"]` at :54 and the check at :86-87 (`hasOnlyOwnStringKeys` PLUS every key present) make every roster member REQUIRED. Widening the roster alone therefore refuses every existing source. Admit the member as OPTIONAL: present-and-admissible, or absent — and a present-but-`undefined` value is NOT absent, so decide which you accept and refuse the other rather than letting both through.\n(2) **THE WIRE ENVELOPE.** `ENVELOPE_KEYS` (:92) is `[acceptanceCriterionContentBytesBase64, dependencyContentBytesBase64, planExecutionContentBytesBase64, schema]` — three base64 content blobs plus the schema tag. DECIDE AND COMMENT AT THE SITE where the declaration rides: its own optional envelope key, or inside the dependency blob beside `directHardDependencies`/`predicateRegistry` (`DEPENDENCY_KEYS`, :91). Either is defensible; leaving it implicit is not, because the envelope is the durable wire and a later reader has to know. Whichever you choose, the v1 envelope for a non-declaring source must serialize to the SAME BYTES it does today.\n(3) **ENCODE STAMPS THE SOURCE'S OWN VERSION AND TAG**, selected by presence per step 3(2) — not the module's current constant. Arm 2(a) is what catches this.\n(4) **RECONSTRUCTION CARRIES IT THROUGH** (the decode-side rebuild, ~:180-185) using conditional spread, never an assigned `undefined` (step 1(e)).\nRun `node-planning-source-codec.test.ts`. Arms (a) through (e) must now be green.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-codec.ts"
      ],
      "modifiedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-codec.ts"
      ],
      "note": "THE SOURCE CODEC. Scheduler leg GREEN, and it is the count line that says so:\n  Test Files  49 passed (49)\n  Tests  1738 passed (1738)\nEXIT 0. Baseline at step 1 was 49 / 1726, so this is +12 arms and zero regressions. `node-planning-source-codec.ts` is 287 lines, under 300.\n\n(1) THE ROSTERS ADMIT IT OPTIONALLY — as a SECOND roster, not a widened one, which is the whole trick. `hasOnlyOwnStringKeys` plus every-key-present makes every member of a roster REQUIRED, so appending the name to `CONTENT_KEYS`/`DRAFT_KEYS` would have refused every source already in the store. Added `DECLARING_DRAFT_KEYS` and `DECLARING_CONTENT_KEYS`; `admit` selects which pair to check by whether the value carries the member, so both rosters stay exactly as strict as they were.\n\nPRESENT-BUT-`undefined` IS REFUSED, and I chose that against the layer below on purpose, with the reasoning at the site. `readDraftFields` (node-authority-fields.ts:222) treats it as ABSENT — correctly, because its decode path states every roster key, so a body that never declared arrives present-and-undefined. Nothing on THIS layer's decode path does that: the wire reader spreads the member conditionally, so an assigned `undefined` can only come from a caller who meant to state something. Silently minting a version-1 source from it would answer a declaration the caller believed they made with a source that has none. Refused as `NODE_PLANNING_SOURCE_MALFORMED` @ `NODE_PLANNING_SOURCE_ADMISSION`, \"declaredMigrations is stated as undefined\". Detected with `readOwnDataProperty`, not `own`, because `own` collapses absent and undefined into the same answer.\n\n(2) WHERE IT RIDES, decided and commented at the site (the code constant lives in `node-planning-source-schema.ts` after the step-3 split): its OWN optional envelope key `declaredMigrationsBytesBase64`, NOT inside the dependency blob. It is not dependency material; burying it would make `dependencyContentBytesBase64` a lie and force every dependency reader to know a field with nothing to do with edges. The decisive reason is the durable wire itself: a distinct key gives ABSENCE a spelling — the key is simply not there — which is the UNKNOWN-vs-explicit-empty distinction expressed in bytes rather than inferred. A non-declaring source serializes to the SAME BYTES it always did, proved by the arm pinning `LEGACY_SOURCE_DIGEST = e02e1dde...` (captured pre-change) plus the four v1 envelope keys and the `MOE-NODE-PLANNING-SOURCE/1` tag.\n\n(3) ENCODE STAMPS THE SOURCE'S OWN VERSION. `nodePlanningSourceWireOf` reads `content.declaredMigrations` and calls `nodePlanningSourceVersionOf`; it never reads `content.version`. Two different refusals now guard the caller-stated version, because they are two different facts: version 3 is one this module cannot read at all (`NODE_PLANNING_SOURCE_UNSUPPORTED_SCHEMA`), while a stated 2 over declaration-free content is one it CAN read and the content contradicts (`NODE_PLANNING_SOURCE_SCHEMA_MISMATCH`). Collapsing them into UNSUPPORTED would have told a caller that version 2 is not supported, which is now false.\n\n(4) RECONSTRUCTION CARRIES IT THROUGH by conditional spread on BOTH sides — the decode-side rebuild uses `DECLARED_MIGRATIONS_KEY in decoded`, and the admitted content uses `...(declared === undefined ? {} : {...})`, placed second so a non-declaring source keeps the exact five-key shape and ORDER the existing `Object.keys` arm at :139 pins.\n\nALL EIGHT NEW SCHEDULER ARMS GREEN, including the four that were green-for-the-wrong-reason at step 2: the message pin now proves `declaredMigrations: \"migration-a\"` refuses as a malformed MEMBER (\"declaredMigrations is not a list\") rather than as an unrecognised KEY (\"planning source is not an exact source record\"), so the roster-widening half is measured rather than assumed.",
      "startedAt": "2026-09-07T15:27:36.530Z",
      "completedAt": "2026-09-07T15:29:29.537Z"
    },
    {
      "stepId": "step-5",
      "description": "THE COMPILER FORWARD — `apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts` (122 lines). This is the drop point the row exists to close, and BOTH halves are needed: either alone loses the member silently.\n(1) **`SOURCE_KEYS` (:15-18) MUST ADMIT IT WITHOUT REQUIRING IT.** `exact(value, keys)` (snapshot.ts:70-74) tests own-key COUNT equality plus every key present — so adding a fifth name makes the member MANDATORY and refuses every existing authored source. Accept either roster: the four required names, or those four plus `declaredMigrations`. Do NOT widen `exact` itself — it is used across this package and loosening it would relax unrelated boundary checks.\n(2) **THE EXPLICIT DRAFT (:98-119) FORWARDS IT.** The draft is written field by field, so a member nobody assigns is dropped with no error. Add it with conditional spread — `...(snapshot.value[\"declaredMigrations\"] === undefined ? {} : { declaredMigrations: snapshot.value[\"declaredMigrations\"] })` — never as an assigned `undefined`, which `readDraftFields` and the codec's byte comparison both treat differently from an absent key.\n(3) **DO NOT TOUCH `matchesRequest`** unless arm 2(f) forces it. If it reds because the created definition no longer matches the request, report what it compares before changing it — that function is the guard that the compiled node is the one that was asked for, and loosening it to make a test pass would be the wrong repair.\n(4) The member rides the SOURCE, not `V2CompilerNodeAuthorityRequest`. That is deliberate and measured: `authority-contracts.ts:72` already calls `V2CompilerNodePlanningAuthority` \"source-owned planning/dependency material\", `directHardDependencies` rides the source for the same reason, and putting it on the request instead would drag the product contract, `NodeFact` and `V2CompiledNode` into this row. Write that sentence at the site so the next reader does not re-litigate it.\nRun arm 2(f), (g), (h).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts"
      ],
      "note": "THE DROP POINT IS CLOSED, both halves. `scheduler-node-planning-authority.ts` is 150 lines (was 122).\n  Test Files  1 passed (1)\n  Tests  7 passed (7)\nEXIT 0 — all seven arms of the new file green, including the four that were red at step 2.\n\n(1) `SOURCE_KEYS` ADMITS WITHOUT REQUIRING, via a SECOND roster rather than a fifth name. `exact` is own-key COUNT equality plus every-key-present (snapshot.ts:70-74), so appending the name would have made the declaration MANDATORY and refused every authored source in the store. `readNodePlanningDefinition` now accepts `exact(value, SOURCE_KEYS) || exact(value, DECLARING_SOURCE_KEYS)`. `exact` ITSELF IS UNTOUCHED — it guards boundaries across this package and loosening it would relax checks with nothing to do with migrations. Arm (g), which was already green at step 2, is STILL green: that is the regression guard, and it staying green is the evidence the roster did not become mandatory.\n\n(2) THE EXPLICIT DRAFT FORWARDS IT by conditional spread, never an assigned `undefined`. The draft is written field by field, so an unassigned member is dropped with no error; and an assigned `undefined` is still an own key that `readDraftFields` (fields.ts:222) and the codec's byte comparison read differently from an absent one.\n\n(3) `matchesRequest` IS UNTOUCHED, and nothing forced the question. It compares the node-attribute fields the REQUEST owns — amounts, gate policy, nodeKey, capability, base tree, objective, policy slice, join role, linkage, constraints, scopes, resources, recipes, criterion ids, edge keys. `declaredMigrations` is not among them and cannot be, because the request has no such field: it is source-owned. So the guard that the compiled node is the one that was asked for still asks exactly what it asked before, and I did not have to loosen anything to make an arm pass.\n\n(4) WHY IT RIDES THE SOURCE AND NOT THE REQUEST, written at the site so the next reader does not re-litigate it: `authority-contracts.ts:73` already calls `V2CompilerNodePlanningAuthority` \"source-owned planning/dependency material\"; `directHardDependencies` rides the source for exactly this reason; and a declaration is stated by the node's AUTHOR, so there is no compiler-side fact that could supply one. Putting it on `V2CompilerNodeAuthorityRequest` would drag the product contract, `NodeFact` and `V2CompiledNode` in behind it for a value none of them own.\n\nTHE TWO REFUSAL ARMS ARE NO LONGER VACUOUS. At step 2 they passed because the whole source was refused as an unknown key; now the roster admits the member and the refusal comes from `readSourceDeclaredMigrations`, so `undefined` from the compiler means \"the declaration was read and rejected\" rather than \"the source was never looked at\". Their specific code and layer are pinned one layer down, in the scheduler arms, rather than restated here where they would only re-test the layer below.",
      "startedAt": "2026-09-07T15:29:32.388Z",
      "completedAt": "2026-09-07T15:30:14.701Z"
    },
    {
      "stepId": "step-6",
      "description": "CLOSE THE OTHER SILENT-DROP PATHS WITH A VERDICT EACH (DoD 3). `git grep -n 'draft: {' -- 'apps/**/*.ts' 'packages/**/*.ts' ':!*.test.ts'` returns NINE sites; six are fixtures. **THE THREE PRODUCTION SITES ARE**: `apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts:100` (step 5), `apps/daemon/src/planning/compiled-policy-authority-body.ts:104`, and `apps/daemon/src/planning/journey-authority-bodies.ts:128`. Re-run that grep at your HEAD and correct me if it has changed.\nFOR EACH OF THE TWO REMAINING SITES, decide and WRITE THE VERDICT AS A COMMENT AT THE SITE — not in the completion record, where the next reader will not find it:\n- **FORWARDS IT**, if that body composes a node whose author could have declared migrations; then add the conditional spread exactly as step 5(2) does, and add an arm.\n- **OR LEGITIMATELY DECLARES NONE**, if the body is synthesized by the daemon rather than authored — in which case say WHY in the comment (\"this body is minted by X, which has no author to state a declaration; absent is therefore correct and is NOT the same as `[]`\"). Absent, not `[]` — defaulting to empty would mint a false declaration (task rail 2).\nA SITE THAT NEITHER FORWARDS NOR STATES ITS CHOICE IS THE DEFECT THIS CLAUSE EXISTS TO PREVENT. \"It did not need one\" without a reason is not a verdict.\nThe six fixture sites need no verdict; name them and say they are fixtures rather than leaving them uncounted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/compiled-policy-authority-body.ts",
        "apps/daemon/src/planning/journey-authority-bodies.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/compiled-policy-authority-body.ts",
        "apps/daemon/src/planning/journey-authority-bodies.ts"
      ],
      "note": "EVERY EXPLICIT-DRAFT SITE HAS A VERDICT, AND THE VERDICTS ARE AT THE SITES, not here.\n\nCENSUS RE-RUN AT MY HEAD — `git grep -n 'draft: {' -- 'apps/**/*.ts' 'packages/**/*.ts' ':!*.test.ts'` returns NINE, matching the architect's count exactly; nothing has moved.\nTHREE PRODUCTION SITES:\n  apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts:113 — FORWARDS IT (step 5).\n  apps/daemon/src/planning/compiled-policy-authority-body.ts:104 — DECLARES NONE.\n  apps/daemon/src/planning/journey-authority-bodies.ts:128 — DECLARES NONE.\nSIX FIXTURE SITES, named rather than left uncounted, and none owes a verdict because none is a production carrier: activation-world-fixtures.ts:155, budget-ledger-fixtures.ts:194, foundation-registry-fixtures.ts:290, graph-query-test-fixtures.ts:211, plan-reject-test-fixtures.ts:97, v2-compiler/compiler-scheduler-test-fixtures.ts:212.\n\nVERDICT 1 — `compiled-policy-authority-body.ts:104`, DECLARES NONE, member ABSENT not `[]`. A declaration is stated by a node's AUTHOR in an authored planning source. This draft is synthesized by the daemon from `CompiledNodeInput` (compiled-authority-contracts.ts:68-80), which carries capability, criterionIds, dependsOn, nodeKey, objective, readScopes, resources, verificationRecipeRefs and writeScopes — no planning source and no declaration anywhere in the shape. There is no author here to state one, so ABSENT, meaning UNKNOWN, is the true answer.\n\nVERDICT 2 — `journey-authority-bodies.ts:128`, DECLARES NONE, member ABSENT not `[]`. Every field of this draft is minted by the module itself from a `JourneyAuthorityInput` carrying only an author ref and an id prefix. No authored planning source stands behind it.\n\nWHY \"ABSENT, NOT `[]`\" IS LOAD-BEARING AND NOT A STYLE POINT, written into both comments: an explicit empty list mints a SCHEMA-3 body (node-authority-codec.ts:127-128), because the version is chosen by the member's PRESENCE. So `[]` would not merely assert something nobody said — it would move the sealed digest of every body these two modules produce. For journey-authority-bodies that is concrete: the module's own header says it produces the bodies for all three shipped journeys and that two builds over one input must be byte-identical, so a defaulted `[]` would move `graphContentHash` and break the demo seed and the cross-app parity pin. That is task rail 2 with a measured consequence attached rather than restated as a principle.\n\nNO ARM IS OWED BY EITHER VERDICT. An arm is owed by a site that FORWARDS; a site that declares none is proved by the digests these modules already seal, which the daemon leg re-derives. If either had silently gained a declaration, those existing pins would red — which is a stronger guard than a new arm asserting a member is absent.\n\n`npx tsc --noEmit -p apps/daemon` — clean, no output. compiled-policy-authority-body.ts is 211 lines, journey-authority-bodies.ts is 263; both under the cap.",
      "startedAt": "2026-09-07T15:30:17.347Z",
      "completedAt": "2026-09-07T15:31:19.212Z"
    },
    {
      "stepId": "step-7",
      "description": "DRILLS, GATES, SELF-REVIEW. Load `adversarial-self-review` and read your own diff as an attacker first.\nDRILLS — `git status --porcelain` the mutated paths FIRST and record the output. `scheduler-node-planning-authority.test.ts` is NEW and therefore UNTRACKED, so `git diff` is blind to it and certifies nothing: back it up out of tree (%TEMP%, suffixed with your worker id) and prove its restore by SHA256 equality in a call SEPARATE from the restore (global rail 13). The other six paths are tracked; an empty `git diff -- <path>` in a separate call is valid proof for those. State which path fell in which class.\nTHREE DRILLS, each quoted with its failing line:\n(i) **THE ROW'S REASON FOR EXISTING** (DoD 6): drop `declaredMigrations` from the explicit draft at scheduler-node-planning-authority.ts:98-119, leaving `SOURCE_KEYS` widened. Confirm arm 2(f) goes RED. If it stays green, the arm is asserting the source rather than the compiled definition and must be rewritten — that is the exact hole this row was filed to close.\n(ii) **THE MANDATORY-KEY DRILL**: make the member REQUIRED in `SOURCE_KEYS` (a single five-name roster). Confirm arm 2(g) reds — a non-declaring source must still compile, or every authored source in the store stops compiling.\n(iii) **THE FALSE-DECLARATION DRILL**: default the member to `[]` in the source codec's reconstruction. Confirm arm 2(a) reds on byte or digest inequality. That is the mutation a well-meaning refactor makes, and it mints a declaration nobody authored.\nGATES — standalone and foreground, ONE command per call, no pipes and no `;` chains, each quoted with its `Test Files N passed` count line, because an exit-0 leg with NO count line ran nothing (global rail 8): `pnpm --filter @moe/scheduler test`, then `pnpm --filter @moe/daemon test` (this row spans both packages), then `pnpm typecheck`. Root `pnpm test` covers no `apps/**` file — say that rather than quoting it. The daemon leg is slow (~22 min) and flaky on this branch: attribute any red outside this row's seven files against your step 1 baseline and disclose foreign red verbatim rather than owning it or calling it green.\nNO NEW CROSS-PACKAGE IMPORT IS INTRODUCED: `apps/daemon` already depends on `@moe/scheduler` (authority-contracts.ts imports `NodePlanningSourceContent` from it today), so no manifest or lockfile edit is needed — state that you checked rather than leaving DoD 7's edge clause looking skipped. Likewise state that no new runtime module was created, so no `.js` bridge was owed.\nSELF-REVIEW AND RECORD, in completionSummary plus one row comment for detail over the cap: where the declaration rides on the wire and why (step 4(2)); that authored ORDER is preserved rather than sorted, matching the landed authority; the two verdicts from step 6 with their reasons; confirmation that a non-declaring source still compiles unchanged and byte-identical; the three drill outcomes with their failing lines; and the exact spelling and refusal vocabulary you consumed from task-122b55fd, so task-de65294b builds against the same one. Named verification command: `pnpm --filter @moe/scheduler test`.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-format.ts",
        "packages/scheduler/src/node-authority/node-planning-source-codec.ts",
        "packages/scheduler/src/node-authority/node-planning-source-codec.test.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts"
      ],
      "modifiedFiles": [
        "packages/scheduler/src/node-authority/node-planning-source-format.ts",
        "packages/scheduler/src/node-authority/node-planning-source-schema.ts",
        "packages/scheduler/src/node-authority/node-planning-source-schema.js",
        "packages/scheduler/src/node-authority/node-planning-source-codec.ts",
        "packages/scheduler/src/node-authority/node-planning-source-codec.test.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts",
        "apps/daemon/src/planning/compiled-policy-authority-body.ts",
        "apps/daemon/src/planning/journey-authority-bodies.ts"
      ],
      "note": "DRILLS, GATES, SELF-REVIEW — all green, zero foreign red, checkout left clean. Full detail in comments 1/2 and 2/2 on this row.\n\nRESUMED SESSION. step-7 was already IN_PROGRESS: a prior session died mid-step. I re-measured from disk/git and trusted nothing it claimed. Finding that changed the method: all nine paths are ALREADY TRACKED AND CLEAN, landed by wip commits 531145fa / 953e9f0d. The plan called `scheduler-node-planning-authority.test.ts` untracked; it is not any more, so global rail 13's sha256/out-of-tree-backup clause applies to NONE of my paths and `git diff` is valid proof for all nine (`git ls-files` returns 9/9).\n\nDRILLS WERE PROCESS-LOCAL, AND THAT WAS FORCED. worker-7b684492 held `full-suite-gate` and posted GATE START 16:04:38Z for a live daemon run. Mutating a tracked shared module in that window reds a peer's arms and everyone's typecheck with no diff to point at. So I queued (#1) instead of starting a second full run, announced the method in #general up front, and drilled via an out-of-tree vitest transform under %TEMP% that rewrites the module IN MEMORY at import time. The harness throws if its anchor is missing and throws in buildEnd if the target was never imported, so a vacuous mutation cannot pass as a drill. Positive controls with the harness armed but no mutation: daemon 8/8, scheduler 34/34.\n\nTHREE DRILLS, each red at the intended arm:\nD1 drop the conditional spread from the explicit draft -> `× forwards two identifiers by value, by count and in authored order`, `AssertionError: expected undefined to deeply equal [ 'migration-b', 'migration-a' ]` (6 failed | 2 passed). The arm reads the value returned by production `readNodePlanningDefinition`, so this is the compiled definition, not the source read back at itself.\nD2 single five-name mandatory roster -> `× still compiles a source that declares nothing, with the member absent`, `AssertionError: expected undefined to be defined` (2 failed | 6 passed).\nD3 default the member to `[]` in reconstruction -> `× leaves a source that declares nothing byte-identical and version-1`, `AssertionError: expected false to be true` at node-planning-source-codec.test.ts:449, the DECODE-side `expect(decoded.ok).toBe(true)`.\n\nRESTORE PROOF: none needed — no byte was written to the checkout at any point. `git status --porcelain` and `git diff --stat` over all nine paths were run before the drills, immediately after, and again after the gates: EMPTY every time. %TEMP% harness deleted.\n\nGATES (gate held 16:24:05Z-16:32Z, GATE START and GATE END both posted, resource released and granted on to task-1f994315):\n`pnpm --filter @moe/daemon test` EXIT 0 — `Test Files 587 passed (587)` / `Tests 10474 passed | 8 skipped (10482)`\n`pnpm typecheck` EXIT 0 — all six packages Done\n`pnpm --filter @moe/scheduler test` EXIT 0 — `Test Files 49 passed (49)` / `Tests 1738 passed (1738)`\nRoot `pnpm test` NOT run and NOT quoted: its include has no apps/**, so it covers zero of this row's four daemon files. Baseline was scheduler 49/1726; delivered 49/1738 = +12 arms, zero regressions. FOREIGN RED: NONE this round.\n\nDoD 7 EDGE CLAUSE CHECKED, NOT SKIPPED: no new cross-package edge. My daemon files add one @moe import (ADMISSION_PURPOSES from @moe/scheduler) and apps/daemon already declares `\"@moe/scheduler\": \"workspace:*\"`; my scheduler files add one (@moe/core types) and packages/scheduler already declares `\"@moe/core\": \"workspace:*\"` with the lockfile importer at pnpm-lock.yaml:232-241. No manifest or lockfile edit owed. One new runtime module, `node-planning-source-schema.ts`, has its tracked one-line bridge `node-planning-source-schema.js`.\n\nSELF-REVIEW: the only new attack surface is `readSourceDeclaredMigrations`; it is pure, no IO, no shared state, so the concurrency/cleanup checklist arms are vacuous. The bound check runs BEFORE the dense-shape walk and the loop, so a claimed length of 1e9 refuses without allocating. Accessor- and proxy-safe throughout. One judgement call flagged rather than buried: an unreadable length maps to LIMIT_EXCEEDED rather than MALFORMED — fail-closed from the correct layer either way, but a generous reading; left as-is because an arm pins the code, and noted so a reviewer can overrule cheaply. Both step-6 verdict comments had every citation re-verified against disk (codec.ts:127, schema 3 vs 2, the CompiledNodeInput field roster) rather than trusted from my own draft.\n\nNo files were modified in this step — it is drills, gates and review only. The nine paths listed are this task's delivered set, already committed; I report them so the wrapper's attribution names the whole row.",
      "startedAt": "2026-09-07T15:31:21.749Z",
      "completedAt": "2026-09-07T16:33:53.831Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "dependsOn": [
    "task-122b55fd2c284174876bd1d5573bdcc5"
  ],
  "priority": "HIGH",
  "order": 131,
  "comments": [
    {
      "id": "comment-8520c8cb044e4c9e907e290a5b6fe197",
      "author": "worker-4d8571e7",
      "content": "STEP 7 EVIDENCE 1/2 — GATES AND DRILLS (worker-4d8571e7, 2026-09-07)\n\nGATES, each standalone and foreground, one command per call, no pipes into the exit status, each quoted with its executed-test count line:\n  `pnpm --filter @moe/daemon test`    EXIT 0 — `Test Files  587 passed (587)` / `Tests  10474 passed | 8 skipped (10482)`, 407.81s\n  `pnpm typecheck`                    EXIT 0 — all six packages `Done` (scheduler, daemon, control-room, review, benchmark, runner)\n  `pnpm --filter @moe/scheduler test` EXIT 0 — `Test Files  49 passed (49)` / `Tests  1738 passed (1738)`  [named verification command, re-run FRESH for complete_task]\nRoot `pnpm test` was NOT run and is NOT quoted: its include has no `apps/**`, so it covers ZERO of this row's four daemon files. The daemon leg above is the only admissible evidence for them.\nFOREIGN RED: NONE. Baseline at step 1 was scheduler 49/1726; delivered is 49/1738 (+12 arms, zero regressions). Nothing to attribute.\n\nDRILL METHOD — PROCESS-LOCAL, and that was forced, not preferred. worker-7b684492 held `full-suite-gate` from 15:39Z and posted GATE START 16:04:38Z for a live `pnpm --filter @moe/daemon test`. An on-disk mutation of a tracked shared module in that window reds the peer's arms and everyone's typecheck with no diff to point at. So the drills ran as an out-of-tree vitest transform (`%TEMP%/moe-75bff-drill-worker-4d8571e7/`) that rewrote the module IN MEMORY at import time. The harness THROWS if its anchor string is not found and throws again in `buildEnd` if the target module was never imported — a silently vacuous mutation cannot pass as a drill.\nPOSITIVE CONTROLS (harness armed, no mutation): daemon file `Test Files 1 passed (1)` / `Tests 8 passed (8)`; scheduler file `Test Files 1 passed (1)` / `Tests 34 passed (34)`. So a drill red is the mutation, not the harness.\n\nD1 — DoD 6, the row's reason for existing. Dropped the conditional spread from the explicit draft (scheduler-node-planning-authority.ts:98-119), leaving both rosters widened. RED:\n  `× forwards two identifiers by value, by count and in authored order`\n  `AssertionError: expected undefined to deeply equal [ 'migration-b', 'migration-a' ]`\n  `Test Files  1 failed (1)` / `Tests  6 failed | 2 passed (8)`\nThe arm reads `definition.declaredMigrations` off the value returned by the production `readNodePlanningDefinition`, so `undefined` there is the compiled NodeDefinition, not the source. The arm is not asserting the source back at itself.\n\nD2 — the mandatory-key trap. Collapsed the two-roster check to a single five-name `DECLARING_SOURCE_KEYS`. RED:\n  `× still compiles a source that declares nothing, with the member absent`\n  `AssertionError: expected undefined to be defined`\n  `Tests  2 failed | 6 passed (8)`\nThat is every authored source already in the store refusing to compile.\n\nD3 — the false declaration. Defaulted the member to `[]` in the format reconstruction (node-planning-source-format.ts:227). RED:\n  `× leaves a source that declares nothing byte-identical and version-1`\n  `AssertionError: expected false to be true` at `node-planning-source-codec.test.ts:449`\nLine 449 is the DECODE-side `expect(decoded.ok).toBe(true)`. Worth knowing why it is there: an earlier pass of this drill red-lined a DIFFERENT arm, which proved the encode-side assertions were blind to a reconstruction default. The decode arm was added in response; the comment at :443-447 records it.\n\nRESTORE PROOF. No restore was needed because no byte was ever written to the checkout. All nine paths are TRACKED (`git ls-files` returns 9/9), so global rail 13's untracked/sha256 clause does not apply to any of them — including `scheduler-node-planning-authority.test.ts`, which the plan called untracked but which landed tracked in wip commit 531145fa. `git status --porcelain` and `git diff --stat` over all nine were run BEFORE the drills, immediately AFTER them, and again after the gates: EMPTY every time.",
      "timestamp": "2026-09-07T16:32:52.410Z"
    },
    {
      "id": "comment-4e427b52c69b40578c1b4caf4245bc7d",
      "author": "worker-4d8571e7",
      "content": "STEP 7 EVIDENCE 2/2 — DESIGN RECORD AND SELF-REVIEW (worker-4d8571e7, 2026-09-07)\n\nWHERE THE DECLARATION RIDES ON THE WIRE (step 4(2), decided and commented at the site, node-planning-source-schema.ts:101-110). ITS OWN OPTIONAL ENVELOPE KEY, `declaredMigrationsBytesBase64` — NOT folded into the dependency blob. A declaration is not dependency material; burying it in `dependencyContentBytesBase64` makes that key's name a lie and forces every reader of the dependency component to know about a field with nothing to do with edges. The decisive property is that a distinct key gives ABSENCE a SPELLING on the durable wire: the key is simply not present. That is task-122b55fd's UNKNOWN-vs-explicit-empty distinction expressed in bytes rather than reconstructed by a reader. A v1 envelope for a non-declaring source therefore serializes to the same four keys and the same `MOE-NODE-PLANNING-SOURCE/1` tag it always has, pinned by literal in the arm.\n\nAUTHORED ORDER IS PRESERVED, NOT SORTED, matching the landed authority. `readSourceDeclaredMigrations` (node-planning-source-schema.ts:133-163) pushes in authored order with no `.sort()`, deliberately NOT reusing the generic scheduler list reader, which dedups AND sorts (node-authority-fields.ts:76-77). Two admissible spellings differing only in order are two different declarations and the digest binds that difference. The arm asserts the exact array `[\"migration-b\",\"migration-a\"]` — deliberately not alphabetical, so a sort would red it.\n\nVERSION BY PRESENCE, one level up from the landed pattern. `nodePlanningSourceVersionOf` (schema.ts:112-118) mirrors node-authority-codec.ts:127 exactly: absent -> UNDECLARED version 1, present (including `[]`) -> version 2. No caller ever states a version; an arm pins the historical `1` as a literal so a later bump cannot promote a source that declares nothing.\n\nTHE TWO STEP-6 VERDICTS, both written AS COMMENTS AT THE SITES, not only here. Whole-tree census `git grep -n 'draft: {' -- 'apps/**/*.ts' 'packages/**/*.ts' ':!*.test.ts'` returns NINE; six are fixtures (activation-world, budget-ledger, foundation-registry, graph-query, plan-reject, compiler-scheduler). Three production:\n  - `scheduler-node-planning-authority.ts:100` — FORWARDS IT (step 5).\n  - `compiled-policy-authority-body.ts:104` — LEGITIMATELY DECLARES NONE, member ABSENT not `[]`. Synthesized by the daemon from `CompiledNodeInput`, whose field roster (compiled-authority-contracts.ts:68-80: capability, criterionIds, dependsOn, nodeKey, objective, readScopes, resources, verificationRecipeRefs, writeScopes) carries no planning source and no declaration. No author exists here to state one, so ABSENT — meaning UNKNOWN — is the true answer.\n  - `journey-authority-bodies.ts:128` — same verdict, and the consequence is concrete rather than theoretical: `[]` mints a schema-3 body, moving `graphContentHash` for all three shipped journeys and breaking the byte-identical rebuild the demo seed and the cross-app parity pin depend on.\nI re-verified every citation in those two durable comments rather than trusting my own draft: node-authority-codec.ts:127 is the minted-version line; NODE_AUTHORITY_SCHEMA_VERSION = 3 (contract.ts:39) and UNDECLARED = 2 (:59); the CompiledNodeInput roster matches field for field.\n\nREFUSAL VOCABULARY CONSUMED FROM task-122b55fd, so task-de65294b builds against the same spelling. Member name `declaredMigrations`, optional. Bound REUSED not restated: `NODE_AUTHORITY_LIMITS.maxMigrationEntries` (64), entries admitted by the authority's own `asId` shape. Authority codes (unchanged, this row mints none of them): `NODE_AUTHORITY_DUPLICATE_MIGRATION` @ `NODE_AUTHORITY_ADMISSION`; `NODE_AUTHORITY_FIELD_INVALID` @ `NODE_AUTHORITY_ADMISSION`; `NODE_AUTHORITY_LIMIT_EXCEEDED` @ `NODE_AUTHORITY_LIMITS`. NEW source-layer codes, deliberately distinct so an arm can tell WHICH layer answered: `NODE_PLANNING_SOURCE_MALFORMED` @ `NODE_PLANNING_SOURCE_ADMISSION`; `NODE_PLANNING_SOURCE_DUPLICATE_MIGRATION` @ `NODE_PLANNING_SOURCE_ADMISSION`; `NODE_PLANNING_SOURCE_LIMIT_EXCEEDED` @ `NODE_PLANNING_SOURCE_LIMITS`. Sharing the authority's codes would have left the layer arms unable to discriminate.\n\nADVERSARIAL SELF-REVIEW — findings, all in `readSourceDeclaredMigrations`, the only new attack surface (everything here is pure, allocation-free until admission, no IO, no shared state, so the concurrency and cleanup arms of the checklist are vacuous):\n  - Hostile size: the BOUND CHECK RUNS BEFORE the dense-shape walk and before the loop, so a claimed length of 1e9 refuses on the bound without allocating or iterating. Deliberate ordering, not luck.\n  - Accessors/proxies: `isPlainArray` + `readPlainArrayLength` + `hasExactDenseArrayShape` + `readOwnArrayElement`, so no getter or proxy trap is invoked. Sparse arrays refuse.\n  - Duplicate scan is `Array.includes`, O(n^2), but n <= 64 so 4096 comparisons worst case. Not worth an allocation to avoid.\n  - `null`/`undefined`/wrong type -> MALFORMED; `[]` -> accepted as the stated empty declaration, which is the point.\n  - ONE JUDGEMENT CALL I AM FLAGGING RATHER THAN HIDING: an UNREADABLE length (`readPlainArrayLength` returns null, reachable only for a hostile object) maps to `NODE_PLANNING_SOURCE_LIMIT_EXCEEDED` rather than `..._MALFORMED`. Fail-closed and from the correct layer either way, but \"exceeds its bound\" is a slightly generous reading of \"could not be read\". Left as-is because changing it would move a code an arm pins; noted so a reviewer can overrule cheaply.\n\nDoD 7 EDGE CLAUSE, checked rather than skipped. No new cross-package edge exists to declare. My daemon files add exactly one `@moe/*` import (`ADMISSION_PURPOSES` from `@moe/scheduler`, in the new test) and apps/daemon already declares `\"@moe/scheduler\": \"workspace:*\"`. My scheduler files add exactly one (`@moe/core` types) and packages/scheduler already declares `\"@moe/core\": \"workspace:*\"` with the lockfile importer entry at pnpm-lock.yaml:232-241. No manifest edit, no lockfile edit. ONE new runtime module was created, `node-planning-source-schema.ts`, and it has its one-line tracked bridge `node-planning-source-schema.js` (`export * from \"./node-planning-source-schema.ts\";`, confirmed in `git ls-files`).",
      "timestamp": "2026-09-07T16:33:26.541Z"
    },
    {
      "id": "comment-e2585ef734694f7ca00c39670c1b31fd",
      "author": "qa-eaa78526",
      "content": "QA REVIEW EVIDENCE (qa-eaa78526, 2026-09-07) — APPROVED. Detail over the summary cap.\n\nLANDING. Completion commit 55da8f76 carries ONLY the task JSON (1 file, 56+/20-). The nine owned\npaths landed in checkpoint 531145fa (10 paths, 861+/100-), both on moe/work-2026-09-04. All nine\npresent at HEAD via `git cat-file -e`; `git status --porcelain` over all nine is EMPTY, so nothing\nwas stranded dirty and no QA hand-landing was owed. A completion sha carrying only the JSON is the\nknown wrapper shape (mem:gotcha-completion-commit-can-carry-only-the-task-json), not a defect.\n\nGATES I RE-RAN MYSELF, foreground, one command per call, count lines quoted:\n- `pnpm --filter @moe/daemon test` EXIT 0 — `Test Files  590 passed (590)` /\n  `Tests  10521 passed | 8 skipped (10529)`, Duration 370.71s, PIPESTATUS[0]=0.\n  (Worker recorded 587/10474; the tree has since taken peer rows. Zero failures either round.)\n- `pnpm --filter @moe/scheduler test` EXIT 0 — `Test Files  49 passed (49)` /\n  `Tests  1738 passed (1738)`. Matches the worker's record exactly.\n- `pnpm typecheck` EXIT 0 — 6 packages Done (control-room, benchmark, review, scheduler, runner,\n  daemon). No foreign red in any leg. Root `pnpm test` NOT quoted: it covers zero apps/** files.\n\nTHREE MUTATION DRILLS RUN INDEPENDENTLY BY ME, not taken from the record. Both mutated paths were\nTRACKED at review time (committed in 531145fa), so an empty `git status --porcelain` in a call\nSEPARATE from the restore is admissible proof per global rail 13 — no untracked path was mutated,\nso no SHA256/backup proof was owed this round.\n\nD1 — dropped the conditional spread from the explicit draft\n(scheduler-node-planning-authority.ts:137-138), leaving both rosters widened. 6 of 8 arms RED:\n  AssertionError: expected undefined to deeply equal [ 'migration-b', 'migration-a' ]\n  AssertionError: expected 2 to be 3 // Object.is equality\n  Test Files  1 failed (1) / Tests  6 failed | 2 passed (8)\nThe by-value arm asserts the COMPILED DEFINITION, not the source it was built from — this is the\nexact hole the row was filed to close, and it is closed. Note the duplicate/noncanonical arms also\nred here (`expected { admissionAmounts: … } to be undefined`), which independently proves the\nrefusal is reached THROUGH the forwarded member rather than by some unrelated arm.\n\nD2 — collapsed the two rosters into one five-name roster (member MANDATORY). 2 arms RED:\n  AssertionError: expected undefined to be defined\n  Test Files  1 failed (1) / Tests  2 failed | 6 passed (8)\nConfirms `exact` is own-key COUNT equality and that a single widened roster would refuse every\nauthored source already in the store. The two-roster design is load-bearing, not stylistic.\n\nD3 — defaulted the member to `[]` in the source codec's admission and reconstruction. 6 of 34 arms\nRED, including the byte-identity arm and the digest-distinctness arm:\n  AssertionError: expected true to be false\n  AssertionError: expected 'f0e6af78cd940b6d801961250f39e28e6a5af…' not to be 'f0e6af78cd940b6d801961250f39e28e6a5af…'\n  Test Files  1 failed (1) / Tests  6 failed | 28 passed (34)\nThe digest line is the point: under the mutation, explicit-`[]` and absent collapse onto ONE\nidentity. The UNKNOWN-vs-explicit-empty distinction is genuinely pinned.\n\nFALSE RED I CAUGHT IN MY OWN METHOD, disclosed rather than buried: my first D3 invocation used\n`--root ../..` (the scheduler script's own flag) from the REPO ROOT, giving\n`Test Files 16 failed (16) / Tests no tests`. That was my mis-rooting, not the mutation. I\nre-established a green control (`Test Files 1 passed (1)` / `Tests 34 passed (34)`) with the\ncorrected `--root .` before re-running the drill, per\nmem:gotcha-narrowing-a-package-test-script-drops-its-flags-and-fakes-a-red.\n\nPOST-DRILL RECONFIRM, after restore: codec suite `34 passed (34)`, compiler suite `8 passed (8)`.\nWorking tree clean across all nine paths. Temp backups removed.\n\nCONSUMER CENSUS ON THE CHANGED CONSTANT (global rail: grep the SHAPE, not the symbol).\n`NODE_PLANNING_SOURCE_SCHEMA_VERSION` changed VALUE 1 -> 2. Every source-tree consumer enumerated\n(`git grep` over apps/**, packages/**, adapters/**): node-authority-public.ts:50 (barrel\nre-export), node-planning-source-codec.ts:20/41/136, the codec test, and index-surface.test.ts:192\n— which asserts only the TYPE `\"number\"`, never the value 1. No consumer pins the old value, so the\nbump is safe. `NODE_PLANNING_SOURCE_DIGEST_DOMAIN` remains exported at its version-1 value BYTE FOR\nBYTE, and `LEGACY_SOURCE_DIGEST` is a pinned literal\n(e02e1dde54e8ea5f5cdf02d084b0095885f8d48f709879e6c41a6e6da82b4c51), not a re-grading. No\nhard-coded `MOE-NODE-PLANNING-SOURCE` tag exists outside the schema module and the codec test.\n\nPER-FILE CAP (the only LOC bar that is a valid reject reason): format 251, schema 164, codec 287,\ncompiler 158 — all under 400. The step-3 module split was forced by the cap and is correct.\nTask-level net LOC is NEVER a reject reason and was not weighed.\n\nDOD 3 VERDICTS CONFIRMED AT THE SITES, not merely in the record: compiled-policy-authority-body.ts\n:104 and journey-authority-bodies.ts:128 each carry a written verdict that they LEGITIMATELY\nDECLARE NONE — ABSENT, not `[]` — because both drafts are daemon-synthesized with no author to\nstate a declaration, and `[]` would mint a schema-3 body and move `graphContentHash` for all three\nshipped journeys. That is the correct call and the reasoning is durable where the next reader is.\n\nONE NON-BLOCKING OBSERVATION, recorded so it is not lost and explicitly NOT a rejection.\n`readSourceDeclaredMigrations` (node-planning-source-schema.ts) maps an UNREADABLE array length\n(hostile object, `readPlainArrayLength` returns null) to NODE_PLANNING_SOURCE_LIMIT_EXCEEDED @\nNODE_PLANNING_SOURCE_LIMITS rather than NODE_PLANNING_SOURCE_MALFORMED @ …_ADMISSION. It fails\nclosed from a real layer with a stable code and an arm pins it, so the DoD 1 requirement (stable\ncode AND the layer that refused) is met. \"Exceeds its bound\" is merely generous phrasing for\n\"could not be read\". The worker flagged this themselves as cheap to overrule. Per the epic's\ndelivery-bias rail this is a hardening-row note, not a new row and not a reject.\n\nFOR task-de65294b (the /runs/read + browser decoder consumer): build against the spellings in\nmem:task-task-75bff0c9efc24daaad81ac81a373cd90-handoff. Wire home is its OWN envelope key\n`declaredMigrationsBytesBase64`, NOT the dependency blob. Version is chosen by PRESENCE, never\nstated by a caller: absent -> v1 + `MOE-NODE-PLANNING-SOURCE/1`; present incl. `[]` -> v2. Compiled\ndefinition schemaVersion: silent 2, declaring 3. Authored ORDER IS PRESERVED, NOT SORTED, and\nDUPLICATES REFUSE rather than dedup. Do not re-mint any of it.",
      "timestamp": "2026-09-07T16:57:56.848Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-07T11:04:58.185Z",
  "updatedAt": "2026-09-07T16:58:34.005Z",
  "metrics": {
    "firstClaimAt": "2026-09-07T13:01:27.743Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 7,
    "doneAt": "2026-09-07T16:58:34.005Z",
    "wallClockMs": 14226262
  },
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-07T13:06:18.882Z",
  "planningNotes": {
    "approachesConsidered": "THE PRODUCER ALREADY LANDED WHILE THIS ROW SAT IN PLANNING, so nothing here is planned against an unbuilt symbol. task-122b55fd landed as commit 4710d60b and is in REVIEW with qa-97200064; `declaredMigrations` is on disk with its bound, its refusal vocabulary and its version policy. I read all of it rather than restating my own plan for that row — and the worker made ONE refinement I did not specify and which is better: the version is selected by CONTENT/PRESENCE (`draft.declaredMigrations === undefined ? UNDECLARED : CURRENT`, codec.ts:127-128; recursion.ts:220 selects the preimage roster the same way) rather than by a stated version. That means a body declaring nothing is never promoted and never moves its identity. THIS ROW COPIES THAT PATTERN ONE LEVEL UP rather than inventing a second one (task rail 1).\n\nWHERE THE DECLARATION RIDES WAS THE ROW'S ONE REAL DESIGN QUESTION, and I measured it rather than assuming the DoD I wrote was right. Two candidates: `V2CompilerNodeAuthorityRequest` (which carries `constraints`, `resources`, `readScopes`, `writeScopes` — node attributes) or `NodePlanningSourceContent` (the authored source). I chose THE SOURCE, for three measured reasons: `authority-contracts.ts:72` already calls `V2CompilerNodePlanningAuthority = Omit<NodePlanningSourceContent,\"version\">` \"source-owned planning/dependency material\"; `directHardDependencies` — also a NodeDefinition field — rides the source for exactly that reason; and the request is built from `V2CompiledNode` (assembly.ts:116-143), which is derived from `NodeFact` and the product contract, so putting the member there would drag the product-contract schema, its codec, its digest, admission, NodeFact and V2CompiledNode into this row. The row's own DoD 1 also says the SOURCE's format version, canonical bytes, digest domain and bounds must account for the member, which only makes sense on the source. Step 5(4) has the worker write that reasoning at the site so it is not re-litigated.\n\nTHE END-TO-END ARM NEEDS A NEW TEST FILE, and the reason is itself the finding: `git grep -ln readNodePlanningDefinition -- 'apps/**'` returns ONLY the two production modules. **Nothing in the tree drives that function.** That is precisely why a member can be dropped between the authored source and the authority draft without a single test noticing, and it is why DoD 2 insists the arm run through the production path rather than a hand-built draft.\n\nREJECTED — widening `exact()` in snapshot.ts to take an optional-key list. It is used across the v2-compiler package for boundary checks, and loosening it would relax unrelated rosters to make one call site convenient. Step 5(1) accepts either of two exact rosters instead, which is local and reversible.\n\nREJECTED — editing `node-planning-source-bounds.ts`. The source format module already imports `NODE_AUTHORITY_LIMITS` (:20), so reusing `maxMigrationEntries` (64) makes the source and the authority agree by construction. A second bound is two numbers that can drift apart.\n\nREJECTED — releasing this row and waiting for the producer to reach DONE. dependsOn gates WORKING claims, not planning, and the contract is already readable at HEAD. Step 1 makes the worker re-read it and STOP if QA sent that row back and it moved.",
    "codebaseInsights": "MEASURED BY ME AT HEAD 4710d60b (the producer's own commit), 2026-09-07.\n\nTHE LANDED AUTHORITY CONTRACT — consume verbatim, do not re-mint:\n1. `node-authority-contract.ts:100` roster member `declaredMigrations`; `:193` `readonly declaredMigrations?: readonly string[]` — OPTIONAL. `:59` `NODE_AUTHORITY_UNDECLARED_SCHEMA_VERSION = 2`; `:154` `maxMigrationEntries: 64`; `:129` code `NODE_AUTHORITY_DUPLICATE_MIGRATION`.\n2. `node-authority-fields.ts:98-117` `readDeclaredMigrations`. **ORDER IS PRESERVED, NOT SORTED** — `declared.push(identifier)` with no `.sort()`, and the comment at :95-96 says two admissible spellings \"differ only in an order that is content, which the digest binds\". Refusals: non-list -> `NODE_AUTHORITY_FIELD_INVALID`@`NODE_AUTHORITY_ADMISSION` (:99-101); over-bound -> `NODE_AUTHORITY_LIMIT_EXCEEDED`@`NODE_AUTHORITY_LIMITS` (:103-105); inadmissible entry -> `NODE_AUTHORITY_FIELD_INVALID`@`NODE_AUTHORITY_ADMISSION` (:110-111); duplicate -> `NODE_AUTHORITY_DUPLICATE_MIGRATION`@`NODE_AUTHORITY_ADMISSION` (:112-113).\n3. **VERSION BY PRESENCE, NOT BY A STATED VERSION** — `node-authority-codec.ts:127-128`; `node-authority-recursion.ts:205,220-221` selects the preimage roster by `Object.hasOwn(body, \"declaredMigrations\")`. This is the pattern the source layer copies.\n4. **CONDITIONAL SPREAD EVERYWHERE, never an assigned `undefined`** — `fields.ts:290-292`, `compose.ts:249-250`, both with comments saying why.\n\nTHE WRITER SEAMS:\n5. `node-planning-source-format.ts` (232): `NODE_PLANNING_SOURCE_SCHEMA_VERSION = 1` (:24), tag `MOE-NODE-PLANNING-SOURCE/1` (:25), digest domain `...-CONTENT-HASH/1` (:26), `NODE_PLANNING_SOURCE_CODES` (:28). `NodePlanningSourceContent` (:64-70) = acceptanceCriterionContent, directHardDependencies, planExecutionContent, predicateRegistry, version. `DEPENDENCY_KEYS` (:91) = [directHardDependencies, predicateRegistry]; `ENVELOPE_KEYS` (:92) = [acceptanceCriterionContentBytesBase64, dependencyContentBytesBase64, planExecutionContentBytesBase64, schema] — THREE base64 blobs plus a tag, so the declaration needs a stated home on the wire. **It already imports `NODE_AUTHORITY_LIMITS` at :20**, so no new bound is needed.\n6. `node-planning-source-codec.ts` (232): `CONTENT_KEYS = [...DRAFT_KEYS, \"version\"]` (:54) with the check at :86-87 requiring EVERY key present — same mandatory-roster shape the authority row had to solve. `sameNodePlanningSourceBytes` (:229).\n7. `scheduler-node-planning-authority.ts` (122): `SOURCE_KEYS` (:15-18) is an EXACT four-name roster enforced by `exact(snapshot.value, SOURCE_KEYS)` (:85); the draft (:98-119) is written FIELD BY FIELD; `matchesRequest` guards the result (:120-121). `exact` (snapshot.ts:70-74) is own-key COUNT equality plus every-key-present — so a fifth name makes the member MANDATORY.\n8. `authority-contracts.ts:72`: `V2CompilerNodePlanningAuthority = Omit<NodePlanningSourceContent, \"version\">`, commented \"Source-owned planning/dependency material; all other draft fields and identities are derived.\" `V2CompilerNodeAuthorityRequest` (:41-67) carries the node-attribute fields and no declaration.\n9. **NOTHING DRIVES `readNodePlanningDefinition` IN TESTS**: `git grep -ln readNodePlanningDefinition -- 'apps/**'` returns only `scheduler-authority.ts` and `scheduler-node-planning-authority.ts`. Hence the new test file in step 2.\n10. THE THREE PRODUCTION EXPLICIT-DRAFT SITES: `v2-compiler/scheduler-node-planning-authority.ts:100`, `planning/compiled-policy-authority-body.ts:104`, `planning/journey-authority-bodies.ts:128`. The other six `draft: {` hits are fixtures (activation-world-fixtures, budget-ledger-fixtures, foundation-registry-fixtures, graph-query-test-fixtures, plan-reject-test-fixtures, compiler-scheduler-test-fixtures).\n11. NO NEW CROSS-PACKAGE EDGE: `apps/daemon` already imports `NodePlanningSourceContent` from `@moe/scheduler` (authority-contracts.ts:2), so DoD 7's manifest/lockfile clause needs a statement, not an edit. Test files carry no `.js` bridge; only runtime modules do.",
    "risks": "1. THE PRODUCER IS IN REVIEW, NOT DONE. If QA rejects task-122b55fd and the contract moves, every citation in step 1 goes stale. Step 1 makes the worker re-read and STOP AND REPORT rather than build to my line numbers. The dependsOn gate will not open until that row is DONE anyway.\n2. ORDER IS CONTENT. The landed authority preserves authored order and the digest binds it. A source layer that sorts — or that compares with a set — makes two distinct authored sources collide on one identity. Arm 2(b) asserts the exact array.\n3. THE MANDATORY-KEY TRAP, twice. `exact()` is count-equality (snapshot.ts:70-74) and `CONTENT_KEYS` requires every member present (codec.ts:86-87). Widening either roster without making the member optional refuses EVERY EXISTING AUTHORED SOURCE. Drill (ii) proves the arm catches it.\n4. NEVER DEFAULT TO `[]`. At the source layer it moves every stored source's digest and mints a declaration nobody authored (task rail 2). Absent and `[]` are different values with different digests; drill (iii) is the control.\n5. AN ASSIGNED `undefined` IS NOT AN ABSENT KEY once `Object.keys` runs, and both codecs byte-compare their own re-encode. Use conditional spread, as the landed authority does in two places.\n6. THE END-TO-END ARM MUST ASSERT THE COMPILED DEFINITION, not the source it was built from. An arm that reads back the source passes while the draft drops the member — which is the exact hole this row exists to close. Drill (i) is written to expose that.\n7. DO NOT WIDEN `exact()` ITSELF. It guards boundary rosters across the v2-compiler package; loosening it for one call site relaxes unrelated checks.\n8. DO NOT LOOSEN `matchesRequest` to make an arm pass. It is the guard that the compiled node is the one that was asked for; if it reds, report what it compares before touching it.\n9. THE TWO OTHER DRAFT SITES ARE THE SILENT-DROP RISK (DoD 3). A site that neither forwards nor states its choice looks correct and loses the member. The verdict goes in a comment AT THE SITE, not only in the record.\n10. THE NEW TEST FILE IS UNTRACKED, so `git diff` certifies nothing for it — sha256 against an out-of-tree backup is the only admissible restore proof (global rail 13).\n11. THE DAEMON LEG IS SLOW (~22 min) AND FLAKY ON THIS BRANCH. Attribute any red outside this row's seven files as foreign against the step 1 baseline; disclose it verbatim and never call it green."
  },
  "planApprovedAt": "2026-09-07T13:06:21.011Z",
  "contextFetchedBy": [
    "worker-c076bc6a",
    "worker-4d8571e7",
    "qa-eaa78526"
  ],
  "workStartedAt": "2026-09-07T15:11:19.761Z",
  "commits": [
    {
      "sha": "531145fafe007d631f737673ebc809d9a3616b94",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-c076bc6a@2026-09-07T15:42:54Z",
      "paths": [
        ".moe/tasks/task-75bff0c9efc24daaad81ac81a373cd90.json",
        "apps/daemon/src/planning/compiled-policy-authority-body.ts",
        "apps/daemon/src/planning/journey-authority-bodies.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts",
        "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts",
        "packages/scheduler/src/node-authority/node-planning-source-codec.test.ts",
        "packages/scheduler/src/node-authority/node-planning-source-codec.ts",
        "packages/scheduler/src/node-authority/node-planning-source-format.ts",
        "packages/scheduler/src/node-authority/node-planning-source-schema.js",
        "packages/scheduler/src/node-authority/node-planning-source-schema.ts"
      ],
      "recordedBy": "worker-c076bc6a",
      "recordedAt": "2026-09-07T15:43:05.019Z",
      "treeId": "af805b8703cf21e3fc9517a6c976458367a0a8ff",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "953e9f0d1ea66da043b3dedddc37797427844529",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-c076bc6a@2026-09-07T15:44:06Z",
      "paths": [
        ".moe/tasks/task-75bff0c9efc24daaad81ac81a373cd90.json"
      ],
      "recordedBy": "worker-c076bc6a",
      "recordedAt": "2026-09-07T15:44:14.180Z",
      "treeId": "c88703b501d016ec904caacf0ce29f95b874d5c1",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "d087fff821e66f8ed573bad5b8b30e1e44f6441f",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-c076bc6a@2026-09-07T15:46:15Z",
      "paths": [
        ".moe/tasks/task-75bff0c9efc24daaad81ac81a373cd90.json"
      ],
      "recordedBy": "worker-c076bc6a",
      "recordedAt": "2026-09-07T15:46:23.853Z",
      "treeId": "abefbb722f8ba0914b6197afda79a853993fdbe7",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "55da8f76147f31be07cdffced3504de71daad6c9",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "completion",
      "role": "worker",
      "sessionId": "worker-4d8571e7@2026-09-07T16:02:53Z",
      "paths": [
        ".moe/tasks/task-75bff0c9efc24daaad81ac81a373cd90.json"
      ],
      "recordedBy": "worker-4d8571e7",
      "recordedAt": "2026-09-07T16:35:28.476Z",
      "treeId": "add3582bea10aa75fb113ba70c63260ebd9c9ef3",
      "status": "REVIEW",
      "pushed": true
    }
  ],
  "filesModified": [
    "packages/scheduler/src/node-authority/node-planning-source-format.ts",
    "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.ts",
    "packages/scheduler/src/node-authority/node-planning-source-codec.test.ts",
    "apps/daemon/src/planning/v2-compiler/scheduler-node-planning-authority.test.ts",
    "packages/scheduler/src/node-authority/node-planning-source-schema.ts",
    "packages/scheduler/src/node-authority/node-planning-source-schema.js",
    "packages/scheduler/src/node-authority/node-planning-source-codec.ts",
    "apps/daemon/src/planning/compiled-policy-authority-body.ts",
    "apps/daemon/src/planning/journey-authority-bodies.ts",
    ".moe/tasks/task-75bff0c9efc24daaad81ac81a373cd90.json"
  ],
  "unattributedPaths": [],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "worker-4d8571e7@2026-09-07T16:02:53Z",
    "at": "2026-09-07T16:35:28.476Z"
  },
  "reviewStartedAt": "2026-09-07T16:35:11.948Z",
  "verification": {
    "command": "pnpm --filter @moe/scheduler test",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next\n\n Test Files  49 passed (49)\n      Tests  1738 passed (1738)\n   Start at  19:34:00\n   Duration  2.22s (transform 4.88s, setup 0ms, import 8.10s, tests 3.59s, environment 2ms)\n\nEXIT=0",
    "reportedAt": "2026-09-07T16:35:11.948Z"
  },
  "completionSummary": "An authored planning source can now state bounded migration identifiers and carry them through the production compiler into the compiled NodeDefinition. 9 files, landed 531145fa/953e9f0d, green at HEAD 132662cf. FULL EVIDENCE IN COMMENTS 1/2 AND 2/2 on this row — this summary is capped and the comments hold the load-bearing detail.\n\nGATES, all EXIT 0, count lines quoted: `pnpm --filter @moe/daemon test` `Test Files 587 passed (587)` / `Tests 10474 passed | 8 skipped (10482)`; `pnpm typecheck` 6 packages Done; `pnpm --filter @moe/scheduler test` `49 passed (49)` / `1738 passed (1738)`. Baseline 49/1726 -> 49/1738 = +12 arms, zero regressions. NO FOREIGN RED. Root `pnpm test` NOT quoted: no apps/** in its include, so it covers zero of the four daemon files.\n\nDESIGN. Member optional; ABSENT = UNKNOWN, `[]` = declared none — distinct values, distinct digests, distinct versions. Own wire key `declaredMigrationsBytesBase64`, so absence has a spelling in bytes. Version chosen BY PRESENCE, never stated by a caller, copying node-authority-codec.ts:127. AUTHORED ORDER PRESERVED, NOT SORTED, and duplicates REFUSE rather than dedupe — deliberately not the generic list reader, which sorts and dedups. Bound reused (maxMigrationEntries 64); source-layer codes deliberately distinct from the authority's so an arm can tell which layer refused.\n\nCOMPILER FORWARD: two rosters (`SOURCE_KEYS` four, `DECLARING_SOURCE_KEYS` five), never a fifth name on one — `exact` is count-equality, so one five-name roster refuses every authored source already in the store. `exact` itself untouched.\n\nSCOPE ADDITION, named not buried: the 300-line cap forced a new module `node-planning-source-schema.ts` (164L) out of format.ts, with its tracked one-line `.js` bridge. All files under cap.\n\nSILENT-DROP SITES: 9 `draft: {` sites, 6 fixtures. compiled-policy-authority-body.ts and journey-authority-bodies.ts both LEGITIMATELY DECLARE NONE (absent, not `[]`), with the reason written AS A COMMENT AT EACH SITE — ",
  "completedAt": "2026-09-07T16:58:34.005Z",
  "reviewCompletedAt": "2026-09-07T16:58:34.005Z",
  "reviewSummary": "APPROVED. All 7 DoD items verified. Full evidence in row comment 3/3.\n\nLANDING: completion sha 55da8f76 carries ONLY the task JSON; the 9 owned paths landed in checkpoint 531145fa, both on moe/work-2026-09-04. All 9 present at HEAD; `git status --porcelain` over all 9 EMPTY. No hand-landing owed.\n\nGATES RE-RUN BY ME, foreground, count lines quoted:\n- `pnpm --filter @moe/daemon test` EXIT 0 - Test Files 590 passed (590) / Tests 10521 passed | 8 skipped (10529)\n- `pnpm --filter @moe/scheduler test` EXIT 0 - Test Files 49 passed (49) / Tests 1738 passed (1738)\n- `pnpm typecheck` EXIT 0, 6 packages Done. No foreign red. Root `pnpm test` not quoted (covers no apps/** file).\n\nTHREE DRILLS RUN INDEPENDENTLY (both mutated paths TRACKED, so separate-call empty `git status` is admissible proof):\nD1 drop the draft spread -> 6 failed | 2 passed (8), \"expected undefined to deeply equal [ 'migration-b', 'migration-a' ]\". Arm asserts the COMPILED DEFINITION, not the source: the row's hole is closed.\nD2 single five-name roster -> 2 failed | 6 passed (8), \"expected undefined to be defined\". Two-roster design is load-bearing.\nD3 default member to [] -> 6 failed | 28 passed (34), \"expected 'f0e6af78...' not to be 'f0e6af78...'\" - explicit-[] and absent collapse onto one identity, so the UNKNOWN-vs-empty distinction is genuinely pinned.\nPost-restore: 34 passed (34), 8 passed (8); tree clean. Disclosed: my first D3 invocation was mis-rooted and gave a FALSE red; I established a green control before re-running.\n\nALSO CHECKED: NODE_PLANNING_SOURCE_SCHEMA_VERSION changed value 1->2 - every source consumer enumerated repo-wide; index-surface.test.ts:192 pins only type \"number\", none pins the old value. v1 digest domain and tag preserved byte-for-byte. Per-file caps 251/164/287/158, all under 400. DoD 3 verdicts written AT both non-forwarding sites (absent, not []). Tracked .js bridge present; no new cross-package edge."
}