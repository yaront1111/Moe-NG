{
  "id": "task-44d4873eb9f746b1a978e97ff9743dc4",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Durable verification receipt dispatch",
  "description": "Objective: compose the real verification process wrapper and evidence pipeline into the durable Foundation attempt path. Deliverable: a daemon service and focused tests that read the exact stored input/result/runtime identities from Durable Claude attempt dispatch, activate one verification effect, execute the sealed recipe, persist the immutable EvidenceReceipt and its stdout/stderr/exit bindings, and expose a read model for later review/closure. NOT in scope: review decisions, goal closure, provider launch, or E2E fixtures. Depends on the Verification process wrapper and Durable Claude attempt dispatch tasks. Owned paths: apps/daemon/src/evidence/foundation-verification-contracts.ts/.js, foundation-verification-service.ts/.js, and foundation-verification-service.test.ts. Verification: pnpm --filter @moe/runner test && pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck.",
  "definitionOfDone": [
    "The service loads the exact durable input manifest, result manifest, runtime observation, and sealed recipe by identity; caller-supplied replacements or stale hashes refuse with stable code/layer before execution.",
    "One atomic verification activation precedes the shell-free process wrapper, and a green observed run produces one immutable EvidenceReceipt binding recipe, input/result trees, runtime, stdout/stderr, exit, and timing digests.",
    "FAILED remains a proven failed receipt while UNKNOWN, truncated, mismatched, or unverified execution cannot create an authoritative passing receipt; each refusal asserts exact reason and layer.",
    "Replay leaves exactly one verification run and one receipt row; a conflict or changed candidate produces a distinct refusal and no overwritten history.",
    "The production read model returns prior receipts byte-identically for review/closure, all runner/daemon/repo gates exit 0, and production files stay below 250 lines."
  ],
  "taskRails": [
    "TDD every behavior change; every refusal test pins the exact stable reason code and refusing layer, and generated sweeps assert a positive case count.",
    "Use only public package roots across package boundaries; no deep imports, test-owned authority, mock-backed acceptance, or optimistic UNKNOWN upgrades.",
    "Keep production files focused, target <=250 physical lines and split before 400; stage and commit only explicit owned paths."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "BASELINE AND RE-MEASURE BOTH PRODUCERS — the cascade cleared only ONE of this task's two hard dependencies, so confirm both. Measured at HEAD f65fbde: `task-69f2b6f785ca4ba3a932a8256b0edfb8` (Verification process wrapper) is DONE and `task-6cbff01023b14b26a78fc5e3eb1dd8a9` (Durable Claude attempt dispatch) is DONE. `apps/daemon/src/evidence/` does NOT exist — this task creates it.\nTHE WRAPPER SURFACE, all published from the `@moe/runner` ROOT via surface/evidence-surface.ts, so every import is a bare specifier and no deep import is needed (task rail 2): `buildVerificationRecipe`, `recipeSealMatches`; `runVerifierProcess`, `createNodeProcessLauncher`, `RunVerifierProcessInput/Ok/Refused/Result`, `VerifierLaunchSpec`, `VerifierExitObservation`, `LaunchedProcess`, `ProcessLauncher`; `buildEvidenceReceipt`, `receiptDigestInput`, `EvidenceReceipt`, `EvidenceReceiptBody`, `BuildEvidenceReceiptInput/Result`, `ReceiptTimestamps`; `canonicalObligations`, `ObligationContext`; `EXECUTION_DISPOSITIONS`, `observedExecutionRejection`, `ObservedVerifierExecution`, `ObservedOutput`; `VERIFIER_PROCESS_ERROR_CODES`, `VERIFIER_PROCESS_LAYERS`, `MAX_VERIFIER_OUTPUT_BYTES`, `MAX_VERIFIER_RUN_MS`, `VERIFIER_REAP_GRACE_MS`, `hermeticVerifierEnvironment`, `VerifierActivation`, `VerifierCapture`, `VerifierClock`; `VerificationRecipe`, `VerificationRecipeBody`, `VerifierIdentity`.\nTHE DISPATCH READ SURFACE, daemon-local in apps/daemon/src/work/foundation-attempt-store.ts (235 lines after the round-2 refactor — LINE NUMBERS MOVED, so re-read rather than citing an old note): `readStoredFoundationAttempt` (:132), `readFoundationAttemptRecord` (:153), `readDurableFoundationObservation` (:77), `settleFoundationAttempt` (:165), `recordProvenFoundationAttempt` (:203), `FoundationAttemptCapture` (:181). The service is 213 lines and exports `createFoundationAttemptService` (:97).\nVERIFY THE CROSS-PACKAGE EDGE per the workspace-dep rail: apps/daemon declares `@moe/runner: workspace:*`, pnpm-lock carries the importer, and a compiled probe inside apps/daemon imports one evidence symbol from the BARE specifier and typechecks exit 0 — probe deleted in the same command. A root-barrel grep does not satisfy leg (c).\nGATES, each run separately with its own exit code — never `;`-chained, never piped into tail: `pnpm --filter @moe/runner test`, `pnpm --filter @moe/daemon typecheck`, `pnpm --filter @moe/daemon test`. Record count lines and every failing file as your before-set; the daemon legs are expected RED from foreign in-flight slices.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/surface/evidence-surface.ts",
        "apps/daemon/src/work/foundation-attempt-store.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/surface/evidence-surface.ts",
        "apps/daemon/src/work/foundation-attempt-store.ts"
      ],
      "note": "BASELINE at HEAD 81a9f30. Nothing written; both files listed were READ only.\n\nBOTH PRODUCERS CONFIRMED LANDED ON COMMITTED BYTES. apps/daemon/src/work/foundation-attempt-{store,service,contracts}.ts measured by `grep -c ''` at 235 / 213 / 218 lines — matching the plan's post-round-2 figures. `apps/daemon/src/evidence/` does NOT exist; this task creates it.\n\nDISPATCH READ SURFACE re-read rather than cited; every line number in the plan still holds exactly: readDurableFoundationObservation(:77), commitFoundationPhase(:103), readFoundationReservationDigest(:119), readStoredFoundationAttempt(:132), readFoundationAttemptRecord(:153), settleFoundationAttempt(:165), FoundationAttemptCapture(:181), recordProvenFoundationAttempt(:203); service exports createFoundationAttemptService(:97). Semantics worth carrying into step 5: readStoredFoundationAttempt is the one that re-encodes the decoded payload and refuses FOUNDATION_ATTEMPT_RECORD_DRIFT on byte drift (:145-148) — that is the discipline DoD 5's read model must copy; readFoundationAttemptRecord derives the dispatch aggregate id and refuses FOUNDATION_ATTEMPT_REQUEST_MALFORMED on an empty/non-string id; settleFoundationAttempt is the SINGLE writer of the one RECORDED event, committing at expectedVersion 1 under an event id COPIED from the grant.\n\nWRAPPER SURFACE verified present in packages/runner/src/surface/evidence-surface.ts (112 lines) and reachable from the ROOT: packages/runner/src/index.ts:164 `export * from \"./surface/evidence-surface.js\"`, package exports {\".\": \"./src/index.ts\"}. Every symbol the plan named is on the seam — buildVerificationRecipe, recipeSealMatches, runVerifierProcess, createNodeProcessLauncher, buildEvidenceReceipt, receiptDigestInput, canonicalObligations, EXECUTION_DISPOSITIONS, observedExecutionRejection, VERIFIER_PROCESS_ERROR_CODES, VERIFIER_PROCESS_LAYERS, MAX_VERIFIER_OUTPUT_BYTES, MAX_VERIFIER_RUN_MS, VERIFIER_REAP_GRACE_MS, hermeticVerifierEnvironment, plus the full type closure. No deep import is needed anywhere in this task.\n\nCROSS-PACKAGE EDGE proven on all three legs, not by root-barrel grep: (a) apps/daemon/package.json:20 declares \"@moe/runner\": \"workspace:*\"; (b) pnpm-lock.yaml:114 carries the apps/daemon importer, specifier workspace:*, version link:../../packages/runner; (c) a compiled probe apps/daemon/src/__bare_specifier_probe.ts importing buildEvidenceReceipt, recipeSealMatches and VERIFIER_PROCESS_ERROR_CODES from the BARE specifier \"@moe/runner\" ran `pnpm --filter @moe/daemon typecheck` at EXIT=0 with zero TS2307/TS6059, and the probe was deleted in the SAME command (absence re-confirmed by ls).\n\nGATES, each run separately with its own exit code captured — never `;`-chained into one status, never piped into tail:\n- `pnpm --filter @moe/runner test` EXIT=0 — \"Test Files  66 passed (66)\" / \"Tests  2216 passed | 1 skipped (2217)\".\n- `pnpm --filter @moe/daemon typecheck` EXIT=0.\n- `pnpm --filter @moe/daemon test` EXIT=0 — \"Test Files  102 passed (102)\" / \"Tests  2101 passed (2101)\".\n- `pnpm typecheck` (repo-wide) EXIT=0, every workspace project reporting Done.\nCounts are nonzero on both test legs, so neither filter silently matched no package.\n\nDEVIATION FROM THE PLAN'S PREMISE, disclosed rather than assumed: step 1 predicted \"the daemon legs are expected RED from foreign in-flight slices\". They are not. The failing-file BEFORE-SET IS EMPTY on all four gates. The foreign in-flight security-lane files that were reddening the repo earlier today live under tests/security/** and are not reached by any of these four gates. Consequence for step 7, and it makes this task STRICTER not looser: the path-attributed baseline is empty, so ANY red at the final gate is mine and there is no foreign red to quote or excuse. I will re-measure the baseline at step 7 rather than trusting this one, since peers are actively writing in this worktree.",
      "startedAt": "2026-08-16T13:20:17.848Z",
      "completedAt": "2026-08-16T13:22:33.566Z"
    },
    {
      "stepId": "step-2",
      "description": "CONTRACTS FIRST — create `apps/daemon/src/evidence/foundation-verification-contracts.ts` plus its one-line `.js` bridge (`export * from \"./foundation-verification-contracts.ts\";`, LF ending; apps/daemon requires a physical bridge per production module and the guard byte-compares).\nIt declares this service's own frozen reason-code and layer vocabularies — `Object.freeze([...] as const)` plus derived union types, the shape every contract in this repo uses, so a test can ITERATE them at runtime. Scope them to what THIS service can refuse: an identity that does not resolve, a stale or replaced hash, an activation that could not be committed, a candidate that changed between load and execution, a replay conflict. Do NOT restate the wrapper's own codes — `VERIFIER_PROCESS_ERROR_CODES` and `VERIFIER_PROCESS_LAYERS` already cover execution failures and travel through unchanged. Two vocabularies for one condition is the defect that got a sibling task rejected.\nEVERY DECLARED CODE MUST HAVE A PRODUCER. A code no layer can emit cannot fail closed; task-6cbff010 was rejected in round 2 for exactly three such orphans. Before the gate, grep each member and confirm a production emit site plus a code+layer test — or drop it.\nAlso declare the service's input and result types. The input names identities, never payloads: the durable attempt's identity, the sealed recipe's identity, and the verification's own correlation. Anything the caller could supply as a REPLACEMENT for durable state must be structurally absent from the input type — that is DoD 1's \"caller-supplied replacements refuse\", enforced by making them unrepresentable rather than validated away.\nKeep under 250 physical lines by `grep -c ''` (PowerShell `Measure-Object -Line` undercounts).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/evidence/foundation-verification-contracts.ts",
        "apps/daemon/src/evidence/foundation-verification-contracts.js"
      ],
      "newFiles": [
        "apps/daemon/src/evidence/foundation-verification-contracts.ts",
        "apps/daemon/src/evidence/foundation-verification-contracts.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/evidence/foundation-verification-contracts.ts",
        "apps/daemon/src/evidence/foundation-verification-contracts.js"
      ],
      "note": "CONTRACTS LANDED. apps/daemon/src/evidence/ created; foundation-verification-contracts.ts measured by `grep -c ''` at 239 lines (under the 250 target); the .js bridge is exactly one LF line, `od -c` verified byte-for-byte against the sibling convention in work/foundation-attempt-store.js, with no CR and no NUL (9696 raw bytes == 9696 after `tr -d '\\000'`). `pnpm --filter @moe/daemon typecheck` EXIT=0.\n\nVOCABULARIES. FOUNDATION_VERIFICATION_CODES holds 12 frozen members, FOUNDATION_VERIFICATION_LAYERS 5, both `Object.freeze([...] as const)` with derived union types so a test can ITERATE them at runtime. Codes: REQUEST_MALFORMED, ATTEMPT_UNPROVEN, RECIPE_UNRESOLVED, RECIPE_SEAL_INVALID, CANDIDATE_STALE, ACTIVATION_UNCOMMITTED, CANDIDATE_CHANGED, REPLAY_CONFLICT, CAPTURE_TRUNCATED, EXECUTION_UNVERIFIED, RECEIPT_AMBIGUOUS, RECEIPT_UNREADABLE. Layers: DAEMON_VERIFICATION_{REQUEST,IDENTITY,ACTIVATION,EXECUTION,RECEIPT}. I am carrying all 12 as a debt to discharge at step 7: the plan's rule is that a code no layer can emit cannot fail closed, so each gets a production emit site plus a code+layer test, or it gets dropped from the frozen list before the gate. The two that most need watching are CANDIDATE_STALE and CANDIDATE_CHANGED, which look like one condition and are not: STALE is the caller's expectedRecordDigest disagreeing with the durable record at LOAD, CHANGED is the durable record moving BETWEEN load and execution. Different producers, different times, and step 4's mismatched-candidate case pins the second. If step 5 cannot produce both distinctly, the weaker one gets dropped rather than left orphaned.\n\nNO RESTATEMENT OF THE WRAPPER'S CODES, which is the defect that got the sibling task rejected. There is deliberately no daemon code for a failed spawn, a timeout, an output overflow or a receipt-binding fault: VERIFIER_PROCESS_ERROR_CODES and RUNNER_EVIDENCE_ERROR_CODES already name those. Pass-through is structural, not conventional — FoundationVerificationRefused is a union discriminated by a `source` field over FOUNDATION_VERIFICATION_REFUSAL_SOURCES (DAEMON_VERIFICATION, FOUNDATION_ATTEMPT, VERIFIER_PROCESS, RUNNER_SUPERVISOR, RUNNER_EVIDENCE), and each arm is typed to ITS OWN authority's code and layer unions. `carryWrapperRefusal` is total over the wrapper's three-way refusal union and re-codes nothing; `carryAttemptRefusal` copies the store's code and reads its layer from `refusedBy`, which is how that record spells it. A restamp is now a type error rather than a review catch.\n\nDoD 1 ENFORCED BY THE TYPE, NOT BY A VALIDATOR. FoundationVerificationRequest carries exactly five fields — attemptAggregateId, candidateRoot, expectedRecordDigest, recipeAggregateId, verificationId. There is NO field for an input manifest, result manifest, runtime observation, recipe body or receipt, so a caller-supplied replacement for durable state is unrepresentable rather than validated away, and a later refactor cannot quietly stop calling the check. expectedRecordDigest is the caller's belief about WHICH record it is verifying, never a substitute for the record.\n\nDESIGN DECISION THAT THE PLAN DID NOT SETTLE, disclosed rather than assumed. DoD 1 requires the sealed recipe to be loaded BY IDENTITY, but I measured that no durable recipe store exists anywhere: `buildVerificationRecipe`/`VerificationRecipe` have zero non-test references in apps/daemon/src and packages/store/src, the durable attempt record body carries no recipe field (attemptRecordBody spells inputManifest, resultManifest, observation, registration, the identity set and the stdout/stderr digests, and nothing else), and the two daemon files matching /recipe/ are the orchestrator's older NodeMission-based verifier runner, an unrelated lane. So \"load the recipe by identity\" has nothing to load from unless this task also makes a recipe durable. I therefore declared FoundationRecipeRegistration: declaring a recipe is how one is CREATED, and from the sealed event onward it is loaded by identity and never again accepted from a caller. This keeps DoD 1 honest instead of quietly letting the caller pass a recipe body on the verify path. If the epic intends recipe sealing to belong to a different task, this is the seam to move and I want it flagged now rather than at QA.\n\nCONSUMED RATHER THAN REIMPLEMENTED: the canonical-bytes primitives (encodeFoundationPayload/decodeFoundationPayload/sha256Hex/sameBytes/isRecord/textOf/exactKeys) already exist in work/foundation-attempt-codec.ts and will be imported by the service at step 5, not rewritten — a second canonical form would be a second definition of the bytes review and closure hash. That file belongs to task-6cbff010 and stays untouched.",
      "startedAt": "2026-08-16T13:22:39.480Z",
      "completedAt": "2026-08-16T13:27:28.764Z"
    },
    {
      "stepId": "step-3",
      "description": "TDD RED, PART 1 — identity loading and the refusals that must happen BEFORE any process starts. Create `foundation-verification-service.test.ts` against the not-yet-existing service, driving a REAL SqliteEventStore opened per test and closed in a `finally` on every exit path (a held handle kills the vitest worker).\nDoD 1 CASES, each asserting the exact code AND layer: the durable input manifest, result manifest, runtime observation and sealed recipe are loaded BY IDENTITY through the landed readers (`readStoredFoundationAttempt`, `readFoundationAttemptRecord`, `readDurableFoundationObservation`); an identity that resolves to nothing refuses; a hash that no longer matches the durable record refuses; a recipe whose seal fails `recipeSealMatches` refuses.\nTHE ORDERING ASSERTION IS THE POINT OF THIS STEP: every one of those refusals must occur BEFORE the process wrapper is invoked. Assert it observably — drive with a launcher that records invocations and assert ZERO invocations on every refusal path, the same shape task-6cbff010's `forgedCalls 0` assertion used. A refusal that happens after a process already ran is not a refusal, it is a cleanup.\nAND ASSERT THE ATOMIC ACTIVATION ORDERING from DoD 2: the verification activation is committed BEFORE the wrapper runs. Read the durable state back and assert that, for a refusal after activation, an activation exists with no run — never a run with no activation. That direction matters: a run without a committed activation is an effect nothing authorised.\nEvery fixture valid at the earlier layers so the intended refusal answers — the store, the dispatch readers, the recipe seal and this service can each refuse, and a code-only assertion detaches the moment a different one answers first.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.test.ts"
      ],
      "startedAt": "2026-08-16T13:27:37.789Z"
    },
    {
      "stepId": "step-4",
      "description": "TDD RED, PART 2 — FAILED versus UNKNOWN, which is this task's hardest distinction and DoD 3's whole content. A PROVEN FAILED verification is authoritative evidence: the recipe ran, the verifier answered, and the answer was no. An UNVERIFIED run is not evidence of anything. Collapsing them either way is the defect — treating UNKNOWN as failure invents a verdict, and treating FAILED as UNKNOWN discards one.\nDrive through the real `runVerifierProcess` and pin, each with exact code and layer:\n- GREEN OBSERVED RUN → exactly one immutable `EvidenceReceipt` built by `buildEvidenceReceipt`, binding recipe, input and result trees, runtime, stdout, stderr, exit and timing digests. Assert the receipt's digest recomputes via `receiptDigestInput`, and assert it came from the runner's builder rather than a locally-composed lookalike.\n- FAILED OBSERVED RUN → a receipt that is PROVEN and FAILED. Assert it is durable and authoritative, not refused.\n- TRUNCATED capture (`MAX_VERIFIER_OUTPUT_BYTES`) → cannot produce an authoritative PASSING receipt, and the truncation is RECORDED rather than silently dropped. A truncated capture whose recorded output happens to look green is exactly how a false pass gets minted.\n- TIMED-OUT run (`MAX_VERIFIER_RUN_MS`, with `VERIFIER_REAP_GRACE_MS` for the reap) → refuses; assert the run terminates rather than hanging, and bound the test itself.\n- MISMATCHED candidate — the input or result tree changed between load and execution → refuses with a distinct code, no receipt.\n- UNVERIFIED execution → no authoritative passing receipt under any circumstance. Assert this as a PROPERTY over the whole non-green set rather than per case, so a case added later cannot escape it.\nUSE `hermeticVerifierEnvironment` for the process environment rather than assembling one — the wrapper publishes it precisely so a caller cannot widen the environment a verifier sees, and a locally-built env would be a second, drifting definition of hermetic.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.test.ts"
      ]
    },
    {
      "stepId": "step-5",
      "description": "IMPLEMENT THE SERVICE. Create `foundation-verification-service.ts` plus its one-line `.js` bridge. Flow: resolve identities through the landed dispatch readers; verify the recipe seal with `recipeSealMatches`; commit ONE atomic verification activation; run `runVerifierProcess` with `createNodeProcessLauncher` and `hermeticVerifierEnvironment`; on an observed run build the receipt with `buildEvidenceReceipt` and persist it once; otherwise refuse with a stable code and layer and persist no receipt.\nRETURN THE PRODUCERS' REFUSALS UNCHANGED. A `VerifierProcessRefusal` keeps its own `VERIFIER_PROCESS_ERROR_CODES` member and `VERIFIER_PROCESS_LAYERS` layer; a store refusal keeps the store's. Do NOT restamp either into this service's vocabulary — that erases which layer answered, and four layers can answer here.\nNEVER call `commitWithApply` or `commitExpectedVersionDecisionWithApply`: they hand the callback a raw `DatabaseSync` and the no-new-schema rail forbids it. Use the narrow port the dispatch slice established, and verify it still cannot name them.\nDO NOT MODIFY the producers — `packages/runner/src/evidence/**` belongs to task-69f2b6f7 and `apps/daemon/src/work/foundation-attempt-*` to task-6cbff010, both DONE. This is the consumer edge; if a producer genuinely cannot be composed as written, report blocked rather than editing it.\nKeep the file under 250 physical lines by `grep -c ''`. If it would exceed, split the persistence half into a sibling module with its own LF bridge rather than compressing — the dispatch slice was rejected partly for a 249-line service with no headroom, and the fix there was exactly this move.\nDo NOT publish from apps/daemon/src/index.ts — index-surface.test.ts pins an exact export count and index.ts is not an owned path.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.ts",
        "apps/daemon/src/evidence/foundation-verification-service.js"
      ],
      "newFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.ts",
        "apps/daemon/src/evidence/foundation-verification-service.js"
      ]
    },
    {
      "stepId": "step-6",
      "description": "REPLAY AND THE READ MODEL — DoD 4 and DoD 5. Replay: re-run the same verification identity and assert the durable state holds EXACTLY ONE verification run and ONE receipt row, read back from the store rather than from the return value. Then the case that actually kills the echo defect: replay with a MATERIALLY DIFFERENT candidate and assert a distinct refusal with no overwritten history — the first receipt's bytes must still be there afterwards. On this board the store's replay identity has repeatedly failed to cover the events array, so a replay answered from the caller's own record makes every idempotence assertion compare the caller's bytes with the caller's bytes and pass forever.\nREAD MODEL: DoD 5 says prior receipts come back BYTE-IDENTICALLY. Implement the read to re-decode the durable bytes and byte-compare before returning — the same discipline `readStoredFoundationAttempt` uses, which re-encodes the decoded payload and refuses on drift. Assert byte identity against the originally persisted bytes, not against a re-derived object: two objects can be deep-equal while their canonical bytes differ, and it is the bytes review and closure will hash.\nAssert the read model refuses rather than returning a partial answer when a stored receipt fails to decode or its digest does not recompute — an unreadable receipt is UNKNOWN, never an absent one, because \"no receipt\" and \"unreadable receipt\" lead a reviewer to opposite conclusions.\nNAME THE CONSUMER: this read model exists for task-8f9305b9bb5e4b8db327a55981b2ea0e (Review-qualified goal closure), then the Foundation canary task-97554aa4293e40eab56c0b642e18513a. Record both ids.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.ts",
        "apps/daemon/src/evidence/foundation-verification-service.test.ts"
      ]
    },
    {
      "stepId": "step-7",
      "description": "MUTATION DRILLS, GATE, ADVERSARIAL REVIEW, COMMIT. Drills, each alone and reverted before the next: (1) invoke the wrapper before the identity checks; the step-3 zero-invocation assertion must redden — proving refusals really precede execution rather than cleaning up after it. (2) Treat a TRUNCATED capture as a green run; the step-4 truncation case must redden — this is the false-pass path and the drill that matters most. (3) Collapse FAILED into UNKNOWN (or the reverse); the corresponding case must redden on its exact code, proving the distinction is asserted rather than described. (4) Answer a replay from the caller's candidate instead of the durable row; the changed-candidate case must redden — if it stays green that assertion is tautological and must be strengthened before completing. (5) Return the read model's object without byte-comparing; the byte-identity case must redden. For EVERY drill record the exact replaced range you edited — a drill that applied nothing reads as green — confirm the red lands on the INTENDED assertion, restore with `git checkout -- <file>` (never by hand) and verify `git status --short` clean before the next.\nGATES, each separate with its own exit code, never `;`-chained, never piped into tail: `pnpm --filter @moe/runner test`; `pnpm --filter @moe/daemon typecheck`; `pnpm --filter @moe/daemon test` reading the COUNT LINE, not the exit code; `pnpm typecheck` repo-wide. Apply the path-attributed baseline — HEAD failing paths minus merge-base failing paths, intersected with your five owned paths, MUST be empty — and quote the foreign daemon red verbatim without repairing it.\nADVERSARIAL REVIEW: (a) is every caller-supplied replacement for durable state structurally UNREPRESENTABLE in the input type, not merely validated away? (b) do all refusals precede wrapper invocation, proven by zero recorded invocations? (c) is the activation committed before the run, with no run-without-activation reachable? (d) is FAILED preserved as proven-and-authoritative while UNKNOWN/truncated/mismatched cannot mint a passing receipt — asserted as a property over the whole non-green set? (e) do producer refusals keep their own codes and layers, unrestamped? (f) does every declared code in the contracts file have a producer and a code+layer test? (g) does the read model byte-compare, and refuse rather than return partial on a decode failure? (h) are `commitWithApply`/`commitExpectedVersionDecisionWithApply` absent, `packages/runner/src/evidence/**` and `foundation-attempt-*` untouched, index.ts unmodified? (i) is every production file under 250 lines by `grep -c ''`, each bridge one LF line? (j) does `git status --porcelain` show only the five owned paths with every drill reverted?\nCommit by explicit five-path pathspec — never a bare `git commit`, never `git add -A`. Verify with `git show --stat`. In the completion quote the count lines and the foreign red verbatim, name the test that reddened for each drill, and record the consumers task-8f9305b9 then task-97554aa4. Keep verification.command under 500 characters.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/evidence/foundation-verification-service.ts",
        "apps/daemon/src/evidence/foundation-verification-contracts.ts",
        "apps/daemon/src/evidence/foundation-verification-service.test.ts"
      ]
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": "worker-5678886b",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor cascade 13:01Z: producer premise FALSE — task-6cbff010 (Durable Claude attempt dispatch) is QA-approved DONE after its round-2 rejection cycle; the Foundation attempt dispatch/read APIs are committed (foundation-attempt-service.ts 213 lines, foundation-attempt-store.ts 235 with settleFoundationAttempt + recordProviderRun persistence, real createFoundationClaudeLauncher wiring, validated input manifest, all three formerly-orphan refusal codes resolved produce-or-drop). Architect: re-measure the landed dispatch surface on committed bytes before naming symbols; note line numbers moved in the round-2 refactor (worker-5678886b's 12:52 note).",
  "createdBy": "WORKER",
  "parentTaskId": "task-97554aa4293e40eab56c0b642e18513a",
  "priority": "CRITICAL",
  "order": 3,
  "comments": [
    {
      "id": "comment-5badc527dfb64bcba6146a6d83d2578a",
      "author": "architect-8c9073cd",
      "content": "Hard dependencies: Verification process wrapper `task-69f2b6f785ca4ba3a932a8256b0edfb8` and Durable Claude attempt dispatch `task-6cbff01023b14b26a78fc5e3eb1dd8a9`. Downstream consumer: Review-qualified goal closure `task-8f9305b9bb5e4b8db327a55981b2ea0e`, then Foundation canary `task-97554aa4293e40eab56c0b642e18513a`.",
      "timestamp": "2026-08-09T12:29:45.654Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-09T12:29:11.053Z",
  "updatedAt": "2026-08-16T13:27:37.789Z",
  "metrics": {
    "firstClaimAt": "2026-08-09T14:23:08.863Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 2
  },
  "contextFetchedBy": [
    "architect-db2146e3",
    "governor-1f3b15b7",
    "governor-7c5e6be6",
    "architect-94dd1835",
    "governor-a627169a",
    "architect-27b64950",
    "worker-5678886b"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorHandoffs": [
    {
      "whatIsDone": "Re-read the prior handoff, re-probed HEAD and the @moe/runner root, confirmed the verification wrapper/receipt primitives are public but the durable Foundation attempt producer/read surface and all five owned files are absent, and reported BLOCKED on task-6cbff010.",
      "whatRemains": "After task-6cbff010 is QA-approved DONE, remeasure its exact stored attempt/input/result/runtime/read/replay API and submit the verification-dispatch plan.",
      "pitfalls": "Do not create test-owned attempt authority, caller-selected manifest/runtime/recipe replacements, or deep imports. The public runner wrapper is present; only the durable producer is missing.",
      "openQuestions": "Exact attempt record/read API cannot be planned before 6cb lands.",
      "releasedAt": "2026-08-15T18:29:34.104Z",
      "releasedBy": "architect-94dd1835",
      "reason": "Measured dependency block; free architect capacity."
    },
    {
      "whatIsDone": "Verified the runner verification wrapper is landed/public, proved the durable attempt-dispatch producer and all owned verification-dispatch files are absent, reported the task BLOCKED on task-6cbff01023b14b26a78fc5e3eb1dd8a9, and wrote the Serena handoff.",
      "whatRemains": "After durable attempt dispatch is DONE, re-measure its exact public stored-identity/read/activation APIs and submit the verification-dispatch plan.",
      "pitfalls": "Do not recreate attempt authority in verification tests, deep-import, accept caller-supplied manifest/runtime/recipe replacements, or narrow the DoD.",
      "openQuestions": "Exact durable attempt record/read API names and transaction seam cannot be known until prerequisite task-6cbff010 lands.",
      "releasedAt": "2026-08-09T14:28:08.511Z",
      "releasedBy": "architect-db2146e3",
      "reason": "Explicit planning handoff after measuring and reporting the hard dependency block; release architect capacity for the remaining PLANNING queue."
    }
  ],
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-16T13:08:31.639Z",
  "planningNotes": {
    "approachesConsidered": "1) Restate the wrapper's execution codes in this service's vocabulary for a uniform error shape. REJECTED: `VERIFIER_PROCESS_ERROR_CODES` and `VERIFIER_PROCESS_LAYERS` already cover execution failures, and four layers can answer here — store, dispatch readers, wrapper, this service. Restamping erases which one did, and two vocabularies for one condition is precisely what got 6cbff010 rejected in round 2.\n\n2) Accept the input/result manifests as payloads for convenience. REJECTED: DoD 1 forbids caller-supplied replacements, and the strongest form of that is structural — the input names identities only. A validated-away payload field is one refactor from being trusted.\n\n3) Validate refusals wherever convenient and rely on the process failing later. REJECTED: a refusal after the process ran is a cleanup, not a refusal. Assert zero launcher invocations on every refusal path.\n\n4) Run the wrapper first and commit the activation on success. REJECTED: that makes a run without a committed activation reachable — an effect nothing authorised. Activation first; a crash then leaves an activation with no run, which is the safe direction.\n\n5) Treat a truncated capture as UNKNOWN and move on without recording the truncation. REJECTED: the truncation is itself the evidence that the capture is not a verdict. Recording it is what stops a later reader treating the retained output as complete.\n\n6) Treat FAILED as UNKNOWN to be conservative. REJECTED — it looks safe and is not: it discards a real verdict the verifier produced, and downstream closure would then wait forever for evidence that already exists.\n\n7) Assemble the verifier environment locally. REJECTED: `hermeticVerifierEnvironment` is published so a caller cannot widen what a verifier sees; a local env is a second, drifting definition of hermetic.\n\n8) Return the read model's decoded object without byte-comparing. REJECTED: two objects can be deep-equal while their canonical bytes differ, and it is the bytes review and closure will hash. Re-decode and byte-compare, as `readStoredFoundationAttempt` already does.",
    "codebaseInsights": "MEASURED AT HEAD f65fbde. This task has TWO hard dependencies and the cascade cleared only one — I checked both: `task-69f2b6f7` (Verification process wrapper) DONE, `task-6cbff010` (Durable Claude attempt dispatch) DONE. `apps/daemon/src/evidence/` does not exist; this task creates it.\n\nI PLANNED 6cbff010'S ROUND-2 FIX EARLIER THIS SESSION, and it landed as designed: the compose+commit moved into the store, which is now 235 lines with `settleFoundationAttempt` (:165) and `recordProvenFoundationAttempt` (:203), while the service came down from 249 to 213. LINE NUMBERS MOVED in that refactor, so every citation here is re-measured rather than carried over.\n\nTHE DISPATCH READ SURFACE this task consumes (apps/daemon/src/work/foundation-attempt-store.ts): `readDurableFoundationObservation` (:77), `commitFoundationPhase` (:103), `readFoundationReservationDigest` (:119), `readStoredFoundationAttempt` (:132), `readFoundationAttemptRecord` (:153), `settleFoundationAttempt` (:165), `FoundationAttemptCapture` (:181), `recordProvenFoundationAttempt` (:203). Service exports `createFoundationAttemptService` (:97).\n\nTHE WRAPPER SURFACE IS FULLY PUBLISHED FROM THE RUNNER ROOT via surface/evidence-surface.ts, so task rail 2's no-deep-imports constraint costs nothing: `buildVerificationRecipe`/`recipeSealMatches`; `runVerifierProcess`/`createNodeProcessLauncher` with `RunVerifierProcessInput/Ok/Refused/Result`, `VerifierLaunchSpec`, `VerifierExitObservation`; `buildEvidenceReceipt`/`receiptDigestInput` with `EvidenceReceipt`, `EvidenceReceiptBody`, `ReceiptTimestamps`; `canonicalObligations`/`ObligationContext`; `EXECUTION_DISPOSITIONS`/`observedExecutionRejection`/`ObservedVerifierExecution`/`ObservedOutput`; `VERIFIER_PROCESS_ERROR_CODES`/`VERIFIER_PROCESS_LAYERS`/`MAX_VERIFIER_OUTPUT_BYTES`/`MAX_VERIFIER_RUN_MS`/`VERIFIER_REAP_GRACE_MS`/`hermeticVerifierEnvironment`/`VerifierActivation`/`VerifierCapture`/`VerifierClock`; `VerificationRecipe`/`VerificationRecipeBody`/`VerifierIdentity`.\n\nTHE HARD DISTINCTION, and the reason this task is CRITICAL rather than mechanical: a PROVEN FAILED verification is authoritative evidence — the recipe ran, the verifier answered, the answer was no. An UNVERIFIED run is evidence of nothing. Collapsing them in either direction is a defect: treating UNKNOWN as failure INVENTS a verdict; treating FAILED as UNKNOWN DISCARDS one. `EXECUTION_DISPOSITIONS` and `observedExecutionRejection` are the vocabulary that keeps them apart, and DoD 3 is entirely about not letting them merge.\n\nTHE FALSE-PASS PATH is truncation. `MAX_VERIFIER_OUTPUT_BYTES` bounds the capture, and a truncated capture whose retained output happens to look green is exactly how an authoritative passing receipt gets minted from an unverified run. The truncation must be RECORDED, not silently dropped.\n\nENFORCE DoD 1 BY TYPE, NOT BY VALIDATION. \"Caller-supplied replacements refuse\" is strongest when the replacement is structurally unrepresentable in the input type — the input names IDENTITIES, never payloads. A validated-away field is one refactor from being trusted.\n\nORDERING IS OBSERVABLE AND MUST BE ASSERTED AS SUCH: refusals before wrapper invocation (assert ZERO recorded launcher invocations, the shape 6cbff010's `forgedCalls 0` assertion used), and activation committed before the run (a run without a committed activation is an effect nothing authorised).\n\nREPLAY: this board has repeatedly found that the store's replay identity does not cover the events array, so only a CHANGED-CANDIDATE replay detects an implementation that echoes the caller.",
    "risks": "1) COLLAPSING FAILED AND UNKNOWN, in either direction. Treating UNKNOWN as failure invents a verdict; treating FAILED as UNKNOWN discards one and stalls closure. Drill 3 covers both directions.\n\n2) TRUNCATION MINTING A FALSE PASS. A bounded capture whose retained bytes look green is the concrete route to an authoritative passing receipt from an unverified run. Drill 2 is the most important drill in the plan.\n\n3) A REFUSAL AFTER THE PROCESS RAN is not a refusal. Assert zero launcher invocations on every refusal path — 6cbff010 proved this assertion shape works.\n\n4) A RUN WITHOUT A COMMITTED ACTIVATION is an effect nothing authorised. Order matters and must be asserted from durable state, not from control flow.\n\n5) THE REPLAY-ECHO DEFECT has recurred repeatedly on this board because the store's replay identity does not cover the events array. Only a changed-candidate replay detects it; a same-candidate replay compares the caller's bytes with the caller's bytes.\n\n6) FOUR LAYERS CAN REFUSE. Pin code AND layer everywhere, and build each fixture valid at the earlier layers or a different layer answers and the assertion quietly stops testing its subject.\n\n7) AN ORPHAN CODE CANNOT FAIL CLOSED. 6cbff010 was rejected in round 2 for three declared-but-unproduced codes. Grep every member of the new vocabulary for a producer before the gate, and drop what has none.\n\n8) LINE BUDGET. 6cbff010's service sat at 249 of 250 with no headroom and the fix was to move persistence into a sibling. Plan for that split from the start rather than compressing.\n\n9) A HELD SQLITE HANDLE kills the vitest worker; real store per test, closed in a `finally` on every exit path including throws. A timed-out verifier case must itself be bounded, or a hang reports no verdict at all.\n\n10) DAEMON GATES RED AT BASELINE from foreign in-flight slices. Record the before-set; only the owned-path delta must be empty. Read the count line — exit 0 with no count line ran nothing.\n\n11) CONSUMERS: task-8f9305b9bb5e4b8db327a55981b2ea0e (Review-qualified goal closure), then canary task-97554aa4293e40eab56c0b642e18513a. Record both rather than assuming the edge.",
    "keyFiles": [
      "packages/runner/src/surface/evidence-surface.ts",
      "packages/runner/src/evidence/verifier-process.ts",
      "packages/runner/src/evidence/evidence-receipt.ts",
      "packages/runner/src/evidence/verification-recipe.ts",
      "packages/runner/src/evidence/verifier-process-contract.ts",
      "apps/daemon/src/work/foundation-attempt-store.ts",
      "apps/daemon/src/work/foundation-attempt-service.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-16T13:19:14.899Z",
    "escalatedAt": "2026-08-16T13:19:14.899Z"
  },
  "planApprovedAt": "2026-08-16T13:08:33.716Z",
  "workStartedAt": "2026-08-16T13:20:17.848Z"
}