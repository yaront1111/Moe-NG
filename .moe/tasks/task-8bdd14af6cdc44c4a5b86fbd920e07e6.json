{
  "id": "task-8bdd14af6cdc44c4a5b86fbd920e07e6",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "A browser-approved goal still cannot close: the intent approval names an EMPTY approvedNodeScope, so the closure qualifier says no approval names one",
  "description": "Spun out of task-ebbcbdb4967748daa84f314b39bd3960 by worker-2739fee7 2026-09-05, measured while delivering that row. It is the SAME defect class one fence deeper, and it is what still stands between the product and its headline journey.\n\nWHAT task-ebbcbdb4 FIXED. `goal.close` and `repository.publish` required a durable `approval.decide`; the browser approves through `approval.decide_intent`, so a browser-approved goal died at the SEQUENCE gate with BOOTSTRAP_PREREQUISITE_MISSING. That row added `\"approval.decide\": [\"approval.decide_intent\"]` to PREREQUISITE_ALTERNATIVES. `repository.publish` after a browser approval is now FULLY ACCEPTED (arm: bootstrap-durability.test.ts \"accepts repository.publish on a goal approved through approval.decide_intent\", ok true, `kinds.has(\"repository.publish\")` true).\n\nWHAT IS STILL BROKEN, MEASURED NOT INFERRED. `goal.close` on the same world now gets PAST the sequence gate and answers:\n  GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED @ DAEMON_PREREQUISITE\nand it will answer that FOREVER on any project approved in the browser. Cause, symbol by symbol, all re-measurable at HEAD:\n- `apps/daemon/src/planning/approval-intent-sources.ts:137` — the intent seam mints `approvedNodeScope: Object.freeze([])`, with the comment \"No per-node narrowing: an initial-graph approval approves the sealed revision, and there is no durable per-node selection for it to restate.\"\n- `apps/daemon/src/goals/goal-close-prerequisite.ts:87` — `approvedNodeScope()` answers **null** when `scope.length === 0`.\n- `apps/daemon/src/goals/goal-qualification.ts:157-159` — `qualify()` reads `readApprovedNodeScope(store, goalId)`; null refuses GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED \"no current approval names an approved node scope\", BEFORE any per-node review walk.\nDIRECT MEASUREMENT: on a world driven through the shipped journey to `approval.decide` and then approved via `runApprovalIntentCommand`, `readApprovedNodeScope(store, GOAL_ID)` returns **null**. Pinned by a committed arm, so this row cannot be closed without it going red: bootstrap-durability.test.ts \"lets goal.close past the sequence gate, where the GOAL's own authority answers\" asserts BOTH the code and `readApprovedNodeScope(...) === null`, and affordance-read.test.ts \"dispatches the goal.close offer past the sequence gate to the goal's own authority\" asserts the same from a real `/affordances/read` frame's own offer.\n\nWHY IT MATTERS. The offer is still made — correctly, per the owner decision in comment-5a8278d76aa24b44989a906caaef589b — and the two authorities on the frame now agree (step READY, offer live). So the operator is offered a Close, clicks it, and gets a refusal. The button is no longer lying about the PREREQUISITE, but the journey \"close a finished goal from the browser\" is still unreachable end to end. The refusal is at least actionable now (a goal-shaped closure code instead of a generic bootstrap one), which is the improvement task-ebbcbdb4 shipped.\n\nWHY IT WAS NOT FIXED THERE. It is a CLOSURE-AUTHORITY design question, not a table entry: the empty scope is deliberate and commented, and lives in two modules that row did not own. Deciding it needs an owner. Both prior closure rows are ARCHIVED (task-8145137ca3c144f0878deb83d05bab63, task-8fdd81b81c0f4dc88c16203de139e2ba), so nothing live owns it.\n\nTHE QUESTION AN ARCHITECT MUST DECIDE, and it must be ONE authority, not two. Either (a) an initial-graph intent approval should name the sealed revision's execution-bearing nodes as its approved scope — measure what `commitIntentActivation` already knows about the graph revision before assuming it can; or (b) `qualify()` should treat an empty scope from an initial-graph approval as \"the whole sealed revision\" rather than as \"unknown\". (a) makes the durable record self-describing; (b) keeps the record as-is and moves the interpretation to the reader. Do NOT relax GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED or any closure code to make a close succeed — the fail-closed invariant that governed task-ebbcbdb4 governs here too.\n\nDO NOT re-implement task-ebbcbdb4's fix: `PREREQUISITE_ALTERNATIVES` already carries `\"approval.decide\": [\"approval.decide_intent\"]` and `COMMAND_PREREQUISITES` is byte-identical. Re-measure both before planning (task rail 2 of the parent applies here).",
  "definitionOfDone": [
    "A goal approved ONLY through approval.decide_intent, with its review acceptance seeded, is CLOSED by a real goal.close dispatch: ok true and the goal reaches its accepted terminal lifecycle. Asserted on the durable outcome, not on the absence of a throw.",
    "The two arms task-ebbcbdb4 left pinning the CURRENT answer are updated deliberately, not deleted: bootstrap-durability.test.ts 'lets goal.close past the sequence gate, where the GOAL's own authority answers' and affordance-read.test.ts 'dispatches the goal.close offer past the sequence gate to the goal's own authority' both assert readApprovedNodeScope(...) === null / a non-accepted dispatch today. Whichever fix ships, say in the arm why the answer changed.",
    "Fail-closed is preserved and proven by mutation: a goal whose approved criteria are NOT verified still refuses its specific closure code, and a REJECTED approval.decide_intent still cannot close. Assert the exact code AND the layer; verify each by mutating the production surface and confirming the arm goes red.",
    "The seeded approval.decide journey does not regress: apps/daemon/src/goals/j1-command-path.test.ts still closes its goal and still reads lifecycle COMPLETED.",
    "Gates quoted with count lines. `pnpm --filter @moe/daemon test` is currently BLOCKED in the shared checkout by an uncommitted peer 'playwright' dependency in apps/daemon/package.json (pnpm demands a full install); if it is still blocked, run the same vitest directly (cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json <paths>) and say so. `pnpm test` covers no apps/** and is not evidence."
  ],
  "taskRails": [
    "THE REFUSAL IS NOT THE BUG, THE UNSATISFIABLE PRECONDITION IS. Never relax GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED, any other GOAL_CLOSE_* code, or the closure qualifier's fail-closed catch, to make a close succeed. Make the approved node scope KNOWABLE for an intent approval; do not make an unknown scope pass.",
    "ONE AUTHORITY, NOT TWO. task-ebbcbdb4 exists because two authorities answered differently about the same command. Whichever side you fix — the writer (approval-intent-sources.ts) or the reader (goal-qualification.ts) — the other must not grow a second, compensating rule."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "THE RULING IS OPTION (a): THE WRITER NAMES THE SCOPE. Record it as a task comment before any byte moves, because the row was filed asking an architect to decide and the completion record must carry the reason.\n\nRULED: `approval-intent-sources.ts` derives the sealed revision's execution-bearing nodes and mints them as `approvedNodeScope`. `goal-close-prerequisite.ts` and `goal-qualification.ts` are NOT changed — re-measure them and confirm they stay byte-identical.\n\nWHY (a), MEASURED NOT PREFERRED. Three findings, each re-runnable:\n  1. THE SCOPE IS CONSUMED AS A NODE LIST, NOT A MARKER. `goal-qualification.ts:163` does `const nodes = [...new Set(approved.scope)].sort()` and hands it to `indexDurableReceipts(store, new Set(nodes))` — the scope IS the per-node receipt walk. So the reader genuinely needs the NAMES; it cannot be satisfied with a flag meaning \"all of them\".\n  2. (b) WOULD PUT GRAPH KNOWLEDGE IN THE CLOSURE QUALIFIER AND BREAK RAIL 2. For the reader to treat empty as \"the whole sealed revision\" it must itself enumerate that revision's nodes — and `empty` would then mean two different things depending on which approval kind wrote it. That is a second, compensating rule in the other authority, which is exactly what rail 2 forbids and exactly the shape that made task-ebbcbdb4 necessary.\n  3. THE WRITER ALREADY HAS EVERYTHING IT NEEDS. `graphRevisionRef` is already in scope at the mint site (guarded at :126, returned at :141), the function already takes the `store`, and `sealedCriteriaDigest(store, runId)` at :67-81 is the EXACT precedent: read the sealed planning authority's events off `planningAuthorityAggregateId(runId)`, find the BODIES event, parse it, derive a field, return null on any failure.\n(a) also keeps `goal-close-prerequisite.ts`'s docstring true as written — \"It answers null for absent, ambiguous, unreadable and non-current approvals alike: each is a reason the approved closure is unknown, and an unknown closure confers nothing.\"\n\nRE-MEASURE BEFORE PLANNING AGAINST IT (the parent's task rail 2 applies): confirm `PREREQUISITE_ALTERNATIVES` still carries `\"approval.decide\": [\"approval.decide_intent\"]` and that `COMMAND_PREREQUISITES` is byte-identical — task-ebbcbdb4 shipped that and this row must NOT re-implement it.\n\nSTATE THE MIGRATION BOUNDARY PLAINLY in the record: approvals ALREADY minted with `Object.freeze([])` are durable and keep an empty scope, so goals approved in the browser BEFORE this fix stay unclosable. This row makes newly-minted approvals self-describing; it does not rewrite history. Say so rather than letting a reader assume every existing goal is unblocked.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/goals/goal-close-prerequisite.ts",
        "apps/daemon/src/goals/goal-qualification.ts",
        "apps/daemon/src/bootstrap/bootstrap-sequence.ts"
      ],
      "note": "RULING RECORDED as comment-f8cbf7f98b7444abac4fe008560ae787 before any byte moved: OPTION (a), the WRITER names the scope; `goal-close-prerequisite.ts` and `goal-qualification.ts` untouched.\n\nRE-MEASURED AT HEAD (task rail 2 of the parent):\n- `bootstrap-sequence.ts:88` still carries `\"approval.decide\": Object.freeze([\"approval.decide_intent\"] as const)` — task-ebbcbdb4's fix is present and will NOT be re-implemented.\n- `COMMAND_PREREQUISITES` byte-identical: `git status --porcelain -- apps/daemon/src/bootstrap/bootstrap-sequence.ts apps/daemon/src/goals/goal-close-prerequisite.ts apps/daemon/src/goals/goal-qualification.ts` -> EMPTY.\n- The three findings the plan asked me to confirm all hold: the scope is consumed as a NODE LIST (`goal-qualification.ts:163` `[...new Set(approved.scope)].sort()` -> `indexDurableReceipts(store, new Set(nodes))` -> `for (const nodeRef of nodes)`), so (b) would need graph knowledge in the qualifier and `empty` would mean two things (rail 2 breach); and the writer already holds the store plus the sealed run identity.\n\nONE MEASURED REFINEMENT TO STEP 3, recorded in the comment. The bodies payload does NOT carry node keys — probed keys are [acceptanceContractBytesBase64, bodiesDigest, criteriaDigest, criteriaRef, goalRef, graphContentHash, graphRevisionRef, planHash, planRevisionBytesBase64, projectId, revisionId, runId, submissionHash]. The plan revision's `affectedNodeIds` is a plan-side list, not the graph's execution-bearing set. So the sealed derivation is bodies -> `graphContentHash` -> `readGraphBody(store, projectId, hash)` -> `content.snapshot.nodes.filter(executionBearing).map(nodeKey)` — the identical derivation already used at activation-world-fixtures.ts:348-352, runs-read.ts:102, compiled-node-source.ts:132, document-coverage-read.ts:100. An ACTIVE-graph read is impossible here: `readCurrentActiveGraph(store, PROJECT_ID)` answers ACTIVE_GRAPH_ABSENT at the mint (measured), because the approval is what enables execution. Shape of step 3 otherwise unchanged: one helper beside `sealedCriteriaDigest`, null on ANY failure, folded into the existing APPROVAL_AUTHORITY_UNSEALED@APPROVAL_RUN_BINDING guard, `approval-intent-sources.ts` only.\n\nMEASURED CONSEQUENCE FOR THE TESTS: the journey's sealed graph names ONE execution-bearing node, `node-a` — NOT `node-1`, which is the caller-supplied literal in the `approval.decide` fixture record (`bootstrap-test-fixtures.ts:732`) and `seedReviewAcceptance`'s default. So the DoD-1 close arm must seed its review acceptance on `node-a`, and asserting the node SET by equality (not length) is what catches a scope of the right size naming the wrong node.\n\nMIGRATION BOUNDARY: approvals already minted with `Object.freeze([])` are durable and stay unclosable; this row makes newly-minted approvals self-describing and rewrites no history.\n\nGATE NOTE: the peer `playwright` dirt in apps/daemon/package.json is GONE at claim (`git status --porcelain -- apps/daemon/package.json` empty), so the named gate is attempted first at step 5.\n\nThrowaway probe `apps/daemon/src/planning/zz-probe-scope.test.ts` and its output file were deleted; `git status --porcelain` on both paths is empty. No production file changed in this step.",
      "startedAt": "2026-09-05T22:47:41.663Z",
      "completedAt": "2026-09-05T22:50:10.131Z"
    },
    {
      "stepId": "step-2",
      "description": "RED TESTS FIRST. Write them and watch them fail before touching the mint.\n\n  1. THE HEADLINE (DoD 1). Drive a world to a goal approved ONLY through `approval.decide_intent`, seed its review acceptance, and dispatch a real `goal.close`. Assert `ok` true AND that the goal reaches its accepted terminal lifecycle from the DURABLE record — not the absence of a throw, and not merely that the refusal changed.\n  2. THE SCOPE IS NOW NAMED. On that same world, `readApprovedNodeScope(store, GOAL_ID)` returns a NON-null scope whose members are the sealed revision's execution-bearing nodes. Assert the actual node set by equality, not its length — a scope of the right size naming the wrong nodes would walk the wrong receipts and still close.\n  3. AN UNDERIVABLE SCOPE REFUSES AT THE MINT, NOT AT THE CLOSE. With the sealed bodies unreadable or absent, the intent approval refuses `APPROVAL_AUTHORITY_UNSEALED` at `APPROVAL_RUN_BINDING` — the same guard `sealedCriteriaDigest` already feeds at :126-128. Assert code AND layer. This is what keeps fail-closed intact while moving the failure EARLIER: an approval that cannot name its scope is never minted, rather than minted empty and refusing at close time.\n  4. FAIL-CLOSED AT THE CLOSE IS UNCHANGED (DoD 3). A goal whose approved criteria are NOT verified still refuses its specific closure code with its layer; a REJECTED `approval.decide_intent` still cannot close. Assert exact code AND layer for both.\n\nAssert against the production surfaces — `readApprovedNodeScope` and the real dispatch — never a helper that re-derives the scope.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "RED TESTS WRITTEN AND WATCHED FAIL, before any production byte moved (`git status --porcelain -- apps/daemon/src/planning/approval-intent-sources.ts` still EMPTY at this point).\n\nFOUR NEW ARMS.\nbootstrap-durability.test.ts, inside the existing task-ebbcbdb4 describe (it already holds `intentApprovedWorld`):\n 1. \"closes a goal approved ONLY through approval.decide_intent\" (DoD 1) — intent-approved world + `seedReviewAcceptance(store, \"node-a\")` + a real `goal.close` dispatch; asserts `ok` true AND `goalLifecycle(store) === \"COMPLETED\"` folded off the DURABLE committed decisions (new local helper over `stateOf(readDurableLedger(...), GOAL_ID)`), the same terminal j1-command-path.test.ts:206 asserts. Not the absence of a throw, not \"the refusal changed\".\n 2. \"names the sealed revision's execution-bearing nodes as the approved node scope\" (DoD 1/2) — `readApprovedNodeScope(store, GOAL_ID)` equals `{ approvalRef: \"approval:run-1\", scope: [\"node-a\"] }` by SET EQUALITY on the whole record. `node-a` is a GOLDEN LITERAL, not re-derived in the test: deriving it here would be a second copy of the mint's own derivation and the two would move together. Its distance from `node-1` (the caller-authored literal on the `approval.decide` fixture record, bootstrap-test-fixtures.ts:732, and seedReviewAcceptance's default) is exactly what a length-only assertion would miss.\n 3. \"still refuses the close when no review acceptance names the approved node\" (DoD 3) — code AND layer AND the exact `qualifyGoalClosure` MESSAGE. The message is the discriminator and it has to be: GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED@DAEMON_PREREQUISITE is ALSO what the scope fence answers, so a code+layer arm would have passed before this row shipped while testing nothing about acceptance. Second leg of the same proof: the scope is asserted NON-null in the same arm, so the scope fence provably is not what refused.\n 4. The existing \"admits a REJECTED approval.decide_intent at the sequence gate, and no further\" arm gained its missing discriminator: `readApprovedNodeScope(store, GOAL_ID)` is null, so the refusal is provably the UNKNOWN-scope fence. A rejection that minted a scope would close the goal, and that assertion is what would redden.\napproval-intent.test.ts:\n 5. \"refuses at the mint when the approved node scope cannot be derived\" (DoD 3, mint side) — a `withStoreFacade` that answers EMPTY for `graphBodyAggregateId(PROJECT_ID, SEALED_GRAPH_CONTENT_HASH)` and for nothing else, so only the scope derivation is blinded and the refusal is attributable to it rather than to the four other reads that share APPROVAL_AUTHORITY_UNSEALED. Asserts code AND layer on BOTH `readApprovalIntentSources` and the shipped `dispatch`, plus a non-vacuity leg (the readable world derives `[\"node-a\"]`) and an unchanged `decisionCount`.\n\nRED RUN, verbatim: `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/bootstrap/bootstrap-durability.test.ts src/planning/approval-intent.test.ts` -> `Test Files 2 failed (2)`, `Tests 6 failed | 149 passed (155)`.\nMy four fail for exactly the right reason: \"GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED@DAEMON_PREREQUISITE: expected false to be true\"; \"expected null to deeply equal { approvalRef: 'approval:run-1', ... }\" (x2); \"expected [] to deeply equal [ 'node-a' ]\". None is a typo or an import error.\n\nTWO OF THE SIX ARE FOREIGN RED, DISCLOSED VERBATIM AND NOT TOUCHED (epic rail 3, delta = (2)-(1) attribution):\n  FAIL src/bootstrap/bootstrap-durability.test.ts > \"accepts repository.publish on a goal approved through approval.decide_intent\" — `PUBLISH_APPROVAL_REQUIRED@DAEMON_INGRESS: expected false to be true`\n  FAIL src/bootstrap/bootstrap-durability.test.ts > \"admits a REJECTED approval.decide_intent at the sequence gate, and no further\" — `Expected: \"BOOTSTRAP_PREREQUISITE_MISSING@DAEMON_PREREQUISITE\" / Received: \"PUBLISH_APPROVAL_REQUIRED@DAEMON_INGRESS\"` (line 991, the `published` label — NOT the `closed` label, which passes)\nATTRIBUTION, MEASURED NOT ASSERTED: `PUBLISH_APPROVAL_REQUIRED` does not exist at HEAD. `git show HEAD:<file> | grep -c PUBLISH_APPROVAL_REQUIRED` answers 0 for all three files that contain it today (apps/daemon/src/repository/publish-services.ts, apps/daemon/src/orchestrator/node-publisher.ts, apps/daemon/src/repository/publication-approval.test.ts). It arrives from a peer's UNCOMMITTED `M apps/daemon/src/repository/publish-services.ts:92` (`if (approval === null) return refuse(request.kind, \"PUBLISH_APPROVAL_REQUIRED\", \"DAEMON_INGRESS\")`) plus ~14 untracked new `apps/daemon/src/repository/publication-*` modules. Neither arm is mine, neither is in my owned paths, and I did not edit, revert or stash any of it.\n\nPaths modified this step: the two test files only. `apps/daemon/src/planning/approval-intent-sources.ts` untouched.",
      "startedAt": "2026-09-05T22:53:23.347Z",
      "completedAt": "2026-09-05T22:56:32.995Z"
    },
    {
      "stepId": "step-3",
      "description": "THE FIX — `apps/daemon/src/planning/approval-intent-sources.ts` ONLY.\n\nAdd a `sealedNodeScope(store, runId)` helper BESIDE `sealedCriteriaDigest` (:67-81) and copy its shape exactly: read the events off `planningAuthorityAggregateId(runId)` inside a try/catch, find the sealed BODIES event, JSON-parse it inside a try/catch, derive the execution-bearing node refs, and return `null` on ANY failure. Every failure mode of that function returns null rather than throwing, and that is deliberate — it feeds a refusal rather than a crash.\n\nThen at the mint (:135-141), replace `approvedNodeScope: Object.freeze([])` with the derived, FROZEN, DEDUPED, SORTED scope, and add its null to the existing guard at :126-128 so an underivable scope refuses `APPROVAL_AUTHORITY_UNSEALED` at `APPROVAL_RUN_BINDING` alongside the other unsealed causes. Sorted and deduped because `goal-qualification.ts:163` already does `[...new Set(scope)].sort()` — minting it that way means the durable record and the reader agree on the exact bytes rather than agreeing only after normalisation.\n\nREPLACE THE COMMENT, DO NOT LEAVE IT. The current one at :137-138 says \"No per-node narrowing: an initial-graph approval approves the sealed revision, and there is no durable per-node selection for it to restate.\" That statement becomes FALSE with this change and would send the next reader to undo it. Replace it with why the scope is now named: the closure qualifier walks these nodes' receipts, an empty scope reads as \"unknown\" to `goal-close-prerequisite.ts:87`, and an unknown closure confers nothing — so an initial-graph approval must restate the sealed revision's execution-bearing nodes for a browser-approved goal to be closable at all.\n\nDO NOT TOUCH `goal-close-prerequisite.ts` OR `goal-qualification.ts` (rail 2). Their rule — empty means unknown, unknown confers nothing — stays exactly as it is; that is what keeps ONE authority. If you find yourself adding a case to either, the ruling has been inverted mid-flight: stop and report.\n\nDo NOT relax `GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED` or any other closure code (rail 1). The refusal is not the bug; the unsatisfiable precondition is.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent-sources.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent-sources.ts",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "THE FIX LANDED IN `apps/daemon/src/planning/approval-intent-sources.ts` ONLY (60 insertions, 4 deletions; file now 202 lines, under the 250 target).\n\nWHAT CHANGED, three edits:\n1. `sealedNodeScope(store, projectId, runId)` added BESIDE `sealedCriteriaDigest`, copying its shape: `store.readEvents(planningAuthorityAggregateId(runId))` in a try/catch, bodies event selected BY TYPE, `JSON.parse` in a try/catch, `null` on ANY failure — never a throw, because the null feeds a refusal rather than a crash.\n2. The mint's guard gained `|| approvedNodeScope === null`, so an underivable scope refuses APPROVAL_AUTHORITY_UNSEALED @ APPROVAL_RUN_BINDING alongside the other unsealed causes.\n3. `approvedNodeScope: Object.freeze([])` became `approvedNodeScope`, deduped and sorted at the mint (`[...new Set(...)].sort()`, frozen) so the durable bytes and `goal-qualification.ts:163`'s normalisation agree directly rather than only after normalisation.\n\nTHE ONE DEVIATION FROM STEP 3'S LETTER, and it is the measured refinement recorded in step 1 / comment-f8cbf7f98b. The step said \"derive the execution-bearing node refs\" from the bodies payload. The payload does not carry them — its keys are [acceptanceContractBytesBase64, bodiesDigest, criteriaDigest, criteriaRef, goalRef, graphContentHash, graphRevisionRef, planHash, planRevisionBytesBase64, projectId, revisionId, runId, submissionHash]. So the derivation is TWO SEALED HOPS: bodies -> `graphContentHash` -> `readGraphBody(store, projectId, hash)` -> `content.snapshot.nodes.filter(executionBearing).map(nodeKey)`. That is the derivation runs-read.ts:102 and compiled-node-source.ts:132 already use, and it is content-addressed so it needs no lifecycle. The helper therefore takes `projectId` (already in scope at the call site) in addition to `store`/`runId`. Two rejected alternatives are commented in the source: `readCurrentActiveGraph` (answers ACTIVE_GRAPH_ABSENT here — this approval is what enables execution) and the plan revision's `affectedNodeIds` (a plan-side list, not the graph's execution-bearing set).\n\nONE DELIBERATE STRENGTHENING beyond the letter: an EMPTY derivation returns null, not `[]`. Minting an empty scope is the exact defect this row exists to kill, so a graph with no execution-bearing node must refuse at the mint rather than produce an unclosable approval. This is what drill 3 targets.\n\nRAIL 2 HELD, PROVEN: `git status --porcelain -- apps/daemon/src/goals/goal-close-prerequisite.ts apps/daemon/src/goals/goal-qualification.ts` is EMPTY — both byte-identical to HEAD. No case was added to either. RAIL 1 HELD: no closure code was relaxed; `GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED` is untouched.\n\nGREEN: `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/planning/approval-intent.test.ts` -> `Test Files 1 passed (1)`, `Tests 94 passed (94)`. All four step-2 arms now pass.\n\nONE ARM IN MY OWN DIFF NEEDED RE-MEASURING, disclosed rather than quietly bumped. `criteriaValidatorFault` (approval-intent.test.ts) injects a bad `criteriaDigest` on the Nth `readEvents` of the planning-authority aggregate. That ordinal is a property of the mint's READ SHAPE, and my change adds a second read of that aggregate per `readApprovalIntentSources` call, so the ordinal that lands on the digest the record carries moved 4 -> 5 and the observed count moved 6 -> 9. Measured by sweeping n in {5,6,7}: only n=5 still produces the refusal the arm grades (APPROVAL_INTENT_RECORD_INVALID@DAEMON_APPROVAL_INTENT); n=6 and n=7 let the dispatch be ACCEPTED, which is precisely how a wrong ordinal announces itself. The arm still grades the same fence with the same code and layer; both numbers carry a comment saying why they moved. This is a red my own diff introduced and my own diff fixed — not an unrelated baseline.\n\nSTILL RED AND EXPECTED (addressed in step 4): the two task-ebbcbdb4 pinning arms. `bootstrap-durability.test.ts` \"lets goal.close past the sequence gate, where the GOAL's own authority answers\" now fails with `expected { approvalRef: 'approval:run-1', ... } to be null` — the tripwire firing exactly as designed.\n\nSTILL RED AND FOREIGN (epic rail 3, unchanged from step 2, not mine, not touched): the two `PUBLISH_APPROVAL_REQUIRED@DAEMON_INGRESS` arms, from a peer's uncommitted `M apps/daemon/src/repository/publish-services.ts:92` plus untracked `publication-*` modules. `git show HEAD:<file> | grep -c PUBLISH_APPROVAL_REQUIRED` = 0 for every file that contains it.",
      "startedAt": "2026-09-05T22:56:35.484Z",
      "completedAt": "2026-09-05T23:00:29.074Z"
    },
    {
      "stepId": "step-4",
      "description": "UPDATE THE TWO PINNING ARMS DELIBERATELY — DoD 2, and they are updated, never deleted.\n\ntask-ebbcbdb4 committed two arms that pin TODAY'S answer precisely so this row cannot close without them going red:\n  - `apps/daemon/src/bootstrap/bootstrap-durability.test.ts` — \"lets goal.close past the sequence gate, where the GOAL's own authority answers\", asserting both the code and `readApprovedNodeScope(...) === null`\n  - `apps/daemon/src/http/affordance-read.test.ts` — \"dispatches the goal.close offer past the sequence gate to the goal's own authority\", asserting the same from a real `/affordances/read` frame's own offer\n\nBoth will now fail, and that is the point — they are the tripwire proving the behaviour actually moved. Rewrite each to assert the NEW answer and, in the arm itself, say WHY it changed: the intent approval now names the sealed revision's nodes, so the scope is no longer null and the goal's own authority proceeds to the receipt walk. An arm silently flipped from null to non-null loses the history that made it valuable.\n\nKEEP WHAT EACH ARM WAS FOR. The affordance-read one exists to prove the OFFER and the COMMAND PATH agree on a real frame — it should still dispatch the frame's own offer rather than a hand-built envelope, since taking the dispatch parameters from the frame is what makes it an agreement test rather than two independent assertions.\n\nAND THE SEEDED JOURNEY MUST NOT REGRESS (DoD 4): `apps/daemon/src/goals/j1-command-path.test.ts` still closes its goal and still reads lifecycle COMPLETED. That path approves through `approval.decide`, not the intent seam, so it exercises the branch this row does NOT change — if it moves, the fix leaked into the shared reader.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
        "apps/daemon/src/http/affordance-read.test.ts",
        "apps/daemon/src/goals/j1-command-path.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
        "apps/daemon/src/http/affordance-read.test.ts"
      ],
      "note": "BOTH PINNING ARMS UPDATED, NEITHER DELETED (DoD 2), and each says in the arm itself why the answer moved.\n\n1. bootstrap-durability.test.ts \"lets goal.close past the sequence gate, where the GOAL's own authority answers\". Was: `readApprovedNodeScope(...) === null` plus the refusal. Now: the same refusal plus the scope asserted as `{ approvalRef: \"approval:run-1\", scope: [\"node-a\"] }`. Its docblock keeps the old assertion verbatim as history and states the cause — the mint used to write `Object.freeze([])` on the reasoning that an initial-graph approval has no per-node selection to restate, while `goal-close-prerequisite.ts:87` reads empty as \"unknown\". Its `seedReviewAcceptance(store)` setup is kept VERBATIM and now carries a SECOND meaning worth having: the default seeds `node-1`, which is NOT the sealed revision's node, so this world proves an acceptance naming a node OUTSIDE the approved scope confers nothing. `expect(SEALED_EXECUTION_NODE).not.toBe(\"node-1\")` makes that non-vacuous rather than a coincidence a rename could erase.\n\n2. affordance-read.test.ts \"dispatches the goal.close offer past the sequence gate and the goal closes\" (renamed from \"...to the goal's own authority\"). Was: an assertion that the dispatch REFUSED. Now: with `seedReviewAcceptance(worldStore, \"node-a\")` the frame's own offer DISPATCHES TO AN ACCEPTED CLOSE — the headline journey proven end to end through the real `/affordances/read` surface. WHAT THE ARM IS FOR IS PRESERVED: every ROUTING parameter is still taken from the frame's own offer (kind, target, expectedVersion, commandId, schemaVersion), which is what makes it an agreement test rather than two independent assertions.\n\nA DEFECT FOUND WHILE UPDATING IT, disclosed rather than quietly patched. The old arm sent `payload: { goalId }` and asserted only `ok === false` and `code !== \"BOOTSTRAP_PREREQUISITE_MISSING\"`. Measured when I made it expect success: that dispatch was answered `BOOTSTRAP_PAYLOAD_INVALID@DAEMON_INGRESS` — it never reached the goal's authority at all, so the arm's name outran its assertions. It now sends the full `acceptancePayload({ goalId: offered.targetAggregateId })`, which is the production split: an offer names what may be done, the operator supplies the acceptance witnesses. That is written into the arm's docblock.\n\nGREEN: `src/http/affordance-read.test.ts` -> `Test Files 1 passed (1)`, `Tests 33 passed (33)`. `src/bootstrap/bootstrap-durability.test.ts` and `src/planning/approval-intent.test.ts` both fully pass (see step 5 for the combined count lines).\n\nDoD 4 — THE SEEDED approval.decide JOURNEY DOES NOT REGRESS. `j1-command-path.test.ts` is currently RED, and the red is FOREIGN and BLOCKS THE ARM BEFORE `goal.close` EVER RUNS: all 5 failures name `repository.publish` (`PUBLISH_HUMAN_REQUIRED`), which sits earlier in `bootstrapSequence()`, and the loop throws on it. ATTRIBUTION BY THE (2)-MINUS-(1) DELTA, measured not asserted: I backed my file out to HEAD (`git checkout HEAD -- apps/daemon/src/planning/approval-intent-sources.ts`, backup taken first) and re-ran — IDENTICAL 5 failures, same 5 names, same messages. So the delta attributable to my diff is EMPTY. Corroborated at the source: `PUBLISH_HUMAN_REQUIRED` and `PUBLISH_APPROVAL_REQUIRED` do not exist at HEAD (`git show HEAD:<file> | grep -c` = 0 for every file containing them today); they come from a peer's uncommitted ` M apps/daemon/src/repository/publish-services.ts` plus an untracked `apps/daemon/src/repository/publication-*` family. Not reverted, not stashed, not touched.\n\nPOSITIVE PROOF FOR DoD 4 ANYWAY, since a blocked arm proves nothing. A throwaway probe drove the EXACT j1 sequence with the single foreign `repository.publish` step skipped and nothing else changed: the close is ACCEPTED and the durable goal state reads lifecycle COMPLETED. `Test Files 1 passed (1) / Tests 1 passed (1)`. Probe deleted; `git status --porcelain -- apps/daemon/src/goals/zz-j1-close-probe.test.ts` is empty and the path does not exist. FULL SOURCE, so a grader can recreate it (rail 12b): open `openStore()`; iterate `bootstrapSequence()`; on `approval.decide` first call `seedActivationWorldWithGatePolicy(store, \"HUMAN_APPROVAL\")`; `continue` past `repository.publish`; on `goal.close` call `seedReviewAcceptance(store)` then `send(store, envelope(\"goal.close\", 2, acceptancePayload()))` and expect `ok` true; every other request `send` and expect `ok` true; finally `expect((stateOf(readDurableLedger(store, PROJECT_ID), GOAL_ID) as Record<string, unknown>)[\"lifecycle\"]).toBe(\"COMPLETED\")`. Independent corroboration on the same branch: `goal-live-evidence.test.ts` is fully GREEN, including \"closes a live goal for real and refuses the second close at the core\", which approves through `approval.decide` — the branch this row does not change. THE FIX DID NOT LEAK INTO THE SHARED READER.\n\nONE MORE FOREIGN RED MEASURED AND ATTRIBUTED, in `src/goals`: `goal-close-readiness.test.ts` \"stays NOT_READY while a SECOND contract",
      "startedAt": "2026-09-05T23:00:34.039Z",
      "completedAt": "2026-09-05T23:09:52.675Z"
    },
    {
      "stepId": "step-5",
      "description": "GATES AND MUTATION DRILLS.\n\nTHE NAMED GATE IS BLOCKED BY PEER DIRT AND THAT IS NOT YOURS TO FIX. Measured 2026-09-06: `git status --porcelain -- apps/daemon/package.json` shows ` M`, and `git diff` shows an UNCOMMITTED `\"playwright\": \"1.62.1\"` added to dependencies — pnpm then demands a full install and `pnpm --filter @moe/daemon test` refuses. NEVER revert, stash or commit that file; it is a peer's in-flight edit. Re-check it at claim: if it has landed, run the normal gate. If it is still dirty, run the same vitest directly and SAY SO in the record:\n  `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json <paths>`\nEither way quote the `Test Files N passed` count line with N nonzero — exit 0 alone is not evidence, and \"No test files found\" is also exit 0. Also run `pnpm typecheck`. `pnpm test` covers NO `apps/**` file, so say that rather than quoting a bare root count line.\n\nDRILL 1, THE HEADLINE. Revert the mint to `Object.freeze([])`. The DoD-1 close arm and the non-null-scope arm MUST both go red with `GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED`. That is the before/after proof the row exists to produce.\nDRILL 2, FAIL-CLOSED AT THE CLOSE (DoD 3 requires mutation). Make the qualifier accept an unverified criterion. Its arm must go red on the exact code AND layer. Restore, then make a REJECTED intent approval mint a scope; the rejected-cannot-close arm must go red.\nDRILL 3, FAIL-CLOSED AT THE MINT. Make `sealedNodeScope` return `[]` instead of null on an unreadable body. The `APPROVAL_AUTHORITY_UNSEALED` arm must go red — otherwise an underivable scope silently mints an empty one and re-creates the exact defect this row fixes, one layer earlier.\n\nRESTORE PROOF, global rail 13. `git status --porcelain` the exact mutated paths BEFORE drilling. Every file here is TRACKED, so an empty `git diff -- <path>` is valid proof — run it per path in a call SEPARATE from the restore. Note that `apps/daemon/package.json` is ALREADY dirty from the peer: its non-empty diff is expected and is not yours.\n\nCOMPLETION RECORD states: the ruling and why (a) rather than (b), naming the three measurements; that `goal-close-prerequisite.ts` and `goal-qualification.ts` are byte-identical, proven by `git status --porcelain`; that task-ebbcbdb4's `PREREQUISITE_ALTERNATIVES` entry was not re-implemented; the migration boundary — approvals minted before this fix keep an empty scope and stay unclosable; which gate form you ran and why; and the three drill outcomes with restore proofs.",
      "status": "IN_PROGRESS",
      "affectedFiles": [],
      "startedAt": "2026-09-05T23:09:58.063Z"
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": "worker-0520a42e",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "dependsOn": [
    "task-ebbcbdb4967748daa84f314b39bd3960"
  ],
  "priority": "HIGH",
  "order": 1008.3,
  "comments": [
    {
      "id": "comment-f8cbf7f98b7444abac4fe008560ae787",
      "author": "worker-0520a42e",
      "content": "STEP 1 RULING RECORDED (worker-0520a42e, 2026-09-06). OPTION (a): THE WRITER NAMES THE SCOPE. `approval-intent-sources.ts` derives the sealed revision's execution-bearing nodes and mints them as `approvedNodeScope`. `goal-close-prerequisite.ts` and `goal-qualification.ts` are NOT changed.\n\nWHY (a), THE THREE MEASUREMENTS, RE-VERIFIED AT HEAD:\n1. THE SCOPE IS CONSUMED AS A NODE LIST, NOT A MARKER. `goal-qualification.ts:163` does `const nodes = [...new Set(approved.scope)].sort()` then `indexDurableReceipts(store, new Set(nodes))` and walks `for (const nodeRef of nodes)`. The reader needs the NAMES; a flag meaning \"all of them\" cannot satisfy it.\n2. (b) WOULD BREAK TASK RAIL 2. For the reader to treat empty as \"the whole sealed revision\" it must itself enumerate that revision — graph knowledge inside the closure qualifier, and `empty` meaning two different things by approval kind. That is the second compensating authority rail 2 forbids.\n3. THE WRITER HAS WHAT IT NEEDS. Measured by throwaway probe (`apps/daemon/src/planning/zz-probe-scope.test.ts`, deleted; `git status --porcelain` on that path is empty). Full source inline below.\n\nPROBE RESULT, verbatim, on a world driven `driveThrough(store, \"approval.decide\")` — i.e. exactly the state at the intent mint:\n  types: [\"PlanningAuthorityBodiesSealed\",\"PlanningAuthorityEnvelopeSealed\"]\n  bodiesKeys: [\"acceptanceContractBytesBase64\",\"bodiesDigest\",\"criteriaDigest\",\"criteriaRef\",\"goalRef\",\"graphContentHash\",\"graphRevisionRef\",\"planHash\",\"planRevisionBytesBase64\",\"projectId\",\"revisionId\",\"runId\",\"submissionHash\"]\n  graphContentHash: \"aa81e0c44486bd3fefb32366e2dcdf98bb448fa9e33ae710615768d6940f59ea\"\n  readGraphBody(store, PROJECT_ID, thatHash).ok = true\n  snapshot: {\"nodes\":[{\"nodeKey\":\"node-a\",\"executionBearing\":true}],\"edges\":[],\"completionNodeKey\":\"node-a\"}\n  readCurrentActiveGraph(store, PROJECT_ID).ok = FALSE, code ACTIVE_GRAPH_ABSENT\n\nPROBE SOURCE (recreatable): open the bootstrap store, `driveThrough(store, \"approval.decide\")`, `store.readEvents(planningAuthorityAggregateId(RUN_ID))`, find `PlanningAuthorityBodiesSealed`, JSON.parse its payload, then `readGraphBody(store, PROJECT_ID, payload.graphContentHash)` and `readCurrentActiveGraph(store, PROJECT_ID)`; write the four facts to a file (vitest swallows console.log at default reporter).\n\nREFINEMENT TO STEP 3, MEASURED NOT PREFERRED. The plan assumed the derivation reads node refs out of the bodies payload itself. It cannot: the payload carries `graphContentHash` and `projectId`, not node keys, and the plan revision's `affectedNodeIds` is a PLAN-side list, not the graph's execution-bearing set. The sealed source is therefore the content-addressed graph body the same bodies event names: `readGraphBody(store, projectId, sealedGraphContentHash)` -> `content.snapshot.nodes.filter(n => n.executionBearing).map(n => n.nodeKey)`, the exact derivation `activation-world-fixtures.ts:348-352`, `runs-read.ts:102`, `compiled-node-source.ts:132` and `document-coverage-read.ts:100` already use. An ACTIVE-graph read is NOT an option: `readCurrentActiveGraph` answers ACTIVE_GRAPH_ABSENT at the mint (measured above) — the graph is not yet active because the approval is what enables execution. Everything else in step 3 stands: one helper beside `sealedCriteriaDigest`, try/catch on every leg, null on ANY failure, null folded into the existing `APPROVAL_AUTHORITY_UNSEALED` @ `APPROVAL_RUN_BINDING` guard, and only `approval-intent-sources.ts` edited.\n\nTASK-EBBCBDB4 NOT RE-IMPLEMENTED, re-measured at HEAD: `bootstrap-sequence.ts:88` still carries `\"approval.decide\": Object.freeze([\"approval.decide_intent\"] as const)`, and `git status --porcelain -- apps/daemon/src/bootstrap/bootstrap-sequence.ts apps/daemon/src/goals/goal-close-prerequisite.ts apps/daemon/src/goals/goal-qualification.ts` is EMPTY (all three byte-identical to HEAD).\n\nMIGRATION BOUNDARY, STATED PLAINLY. Approvals already minted with `approvedNodeScope: Object.freeze([])` are DURABLE and keep an empty scope. Goals approved in the browser BEFORE this fix stay unclosable — `readApprovedNodeScope` will keep answering null for them and `goal.close` will keep answering GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED. This row makes NEWLY-MINTED approvals self-describing; it rewrites no history and unblocks no existing goal.\n\nGATE NOTE: the peer `playwright` dirt the plan warned about is GONE — `git status --porcelain -- apps/daemon/package.json` is empty at claim. The named gate `pnpm --filter @moe/daemon test` will be attempted first.",
      "timestamp": "2026-09-05T22:49:51.801Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T20:13:38.542Z",
  "updatedAt": "2026-09-05T23:25:56.668Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T22:26:28.199Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 4
  },
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-05T22:30:18.349Z",
  "planningNotes": {
    "approachesConsidered": "OPTION (b), THE READER TREATS EMPTY AS THE WHOLE SEALED REVISION — rejected on measurement (1) and (2) above. It reads as the smaller change but it is the more expensive one: the reader would need to enumerate the revision's nodes to satisfy its own receipt walk, which imports graph knowledge into the closure qualifier and gives `empty` two meanings. It also falsifies goal-close-prerequisite.ts's own docstring for one approval kind, which is how the next reader gets misled.\n\nRELAXING GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED — forbidden by rail 1 and never considered. The refusal is correct; the precondition is unsatisfiable. Making an unknown scope pass would close goals whose nodes were never receipt-checked.\n\nMINTING THE SCOPE UNSORTED / UNDEDUPED — rejected. goal-qualification.ts:163 already normalises with `[...new Set(scope)].sort()`, so minting it normalised means the durable bytes and the reader's view agree directly rather than only after normalisation — which matters when a human compares a stored approval against a refusal message.\n\nLETTING AN UNDERIVABLE SCOPE MINT AS EMPTY — explicitly rejected and drilled (drill 3). It would silently re-create this exact defect one layer earlier, and the arm that catches it is the difference between fail-closed and fail-quiet.\n\nDELETING THE TWO PINNING ARMS instead of rewriting them — rejected by DoD 2. They were committed by task-ebbcbdb4 precisely so this row could not close without them going red; rewriting them WITH the reason preserves that history, deleting them discards it.",
    "codebaseInsights": "MEASURED 2026-09-06 by architect-aa54e231 on branch moe/work-2026-09-04. The row was filed by worker-2739fee7 with precise symbol-level measurements; I verified them and measured the one thing the row asked an architect to settle.\n\nTHE RULING IS (a), THE WRITER SIDE, and three measurements decide it rather than taste.\n\n(1) THE SCOPE IS CONSUMED AS A NODE LIST, NOT A MARKER. goal-qualification.ts:163 does `const nodes = [...new Set(approved.scope)].sort()` and passes it to `indexDurableReceipts(store, new Set(nodes))`. The scope IS the per-node receipt walk, so the reader needs the NAMES. A flag meaning \"all of them\" cannot satisfy it — the reader would have to enumerate the revision itself.\n\n(2) SO (b) WOULD BREAK RAIL 2. For the reader to treat empty as \"the whole sealed revision\" it must acquire graph-revision knowledge inside the closure qualifier, and `empty` would then mean two different things depending on which approval kind wrote it. That is a second compensating rule in the other authority — precisely the two-authorities shape that made task-ebbcbdb4 necessary in the first place.\n\n(3) THE WRITER ALREADY HAS WHAT IT NEEDS — this is what the row asked to be measured before assuming. At the mint site in approval-intent-sources.ts: `graphRevisionRef` is already in scope (guarded at :126, returned at :141), the function already receives the `store`, and `sealedCriteriaDigest(store, runId)` at :67-81 is an exact precedent for a mint-time sealed read — `store.readEvents(planningAuthorityAggregateId(runId))` in a try/catch, find the BODIES event, JSON.parse in a try/catch, derive a field, return null on any failure, with that null feeding the APPROVAL_AUTHORITY_UNSEALED refusal at :126-128.\n\n(a) ALSO KEEPS THE READER'S DOCSTRING TRUE AS WRITTEN. goal-close-prerequisite.ts:87 rejects an empty scope, and the docstring immediately below says it \"answers null for absent, ambiguous, unreadable and non-current approvals alike: each is a reason the approved closure is unknown, and an unknown closure confers nothing.\" Under (a) that sentence stays exactly true; under (b) it becomes false for one approval kind.\n\nA CONSEQUENCE WORTH NAMING: (a) moves the failure EARLIER. An approval that cannot derive its scope refuses APPROVAL_AUTHORITY_UNSEALED at mint rather than being minted empty and refusing at close time. Fail-closed is preserved and becomes legible at the moment the approval is made.\n\nTHE MIGRATION BOUNDARY. `approvedNodeScope: Object.freeze([])` is already durable in existing approvals, so goals approved in the browser BEFORE this fix keep an empty scope and stay unclosable. This row makes newly-minted approvals self-describing; it rewrites no history. The plan requires that stated plainly rather than left for a reader to assume.\n\nTHE NAMED GATE IS BLOCKED BY PEER DIRT, CONFIRMED: `git status --porcelain -- apps/daemon/package.json` shows ` M` and the diff adds an uncommitted `\"playwright\": \"1.62.1\"` to dependencies, so pnpm demands a full install and `pnpm --filter @moe/daemon test` refuses. It is a peer's in-flight edit — never revert or commit it; the DoD's direct-vitest form is the workaround.\n\nDO NOT RE-IMPLEMENT task-ebbcbdb4: PREREQUISITE_ALTERNATIVES already carries `\"approval.decide\": [\"approval.decide_intent\"]` and COMMAND_PREREQUISITES is byte-identical. I planned that row earlier in this session; `repository.publish` after a browser approval is already fully accepted.",
    "risks": "1. THE FIX LEAKING INTO THE READER. The tempting shortcut when an arm reds is to add a case to goal-qualification.ts or goal-close-prerequisite.ts. That creates the second compensating authority rail 2 forbids and re-creates task-ebbcbdb4's original shape. The plan says explicitly: adding a case to either means the ruling has been inverted mid-flight, stop and report. DoD-level proof is `git status --porcelain` showing both byte-identical.\n\n2. AN UNDERIVABLE SCOPE MINTING AS EMPTY. If `sealedNodeScope` returns `[]` rather than null on an unreadable body, the approval is minted with the same empty scope this row exists to eliminate, and the defect returns one layer earlier and quieter. Drill 3 targets exactly this.\n\n3. A SCOPE OF THE RIGHT SIZE NAMING THE WRONG NODES. An arm asserting `scope.length` passes for a scope that walks the wrong receipts and still closes the goal. The plan requires asserting the node set by equality.\n\n4. THE STALE COMMENT. approval-intent-sources.ts:137-138 currently asserts there is no durable per-node selection to restate. Left in place it directly contradicts the new code and invites the next reader to revert the fix as an error.\n\n5. THE MIGRATION BOUNDARY GOING UNSAID. Existing approvals keep their empty scope, so goals approved before this lands stay unclosable. A completion record implying the journey is unblocked for every goal would be false.\n\n6. THE SEEDED JOURNEY REGRESSING. j1-command-path.test.ts approves through `approval.decide`, the branch this row does not touch; if it moves, the change leaked into shared reader code.\n\n7. THE BLOCKED GATE. `apps/daemon/package.json` carries uncommitted peer `playwright` dirt, so the named gate refuses. Reverting it to unblock the gate would destroy a peer's work — the direct-vitest form is the workaround, and its use must be disclosed.",
    "keyFiles": [
      "apps/daemon/src/planning/approval-intent-sources.ts",
      "apps/daemon/src/goals/goal-close-prerequisite.ts",
      "apps/daemon/src/goals/goal-qualification.ts",
      "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
      "apps/daemon/src/http/affordance-read.test.ts",
      "apps/daemon/src/goals/j1-command-path.test.ts"
    ]
  },
  "planApprovedAt": "2026-09-05T22:30:20.520Z",
  "contextFetchedBy": [
    "worker-0520a42e"
  ],
  "workStartedAt": "2026-09-05T22:47:41.663Z",
  "commits": [
    {
      "sha": "2ad380cbc86ad524a063e7f9521c6cfb99b6555f",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-0520a42e@2026-09-05T22:43:14Z",
      "paths": [
        ".moe/tasks/task-8bdd14af6cdc44c4a5b86fbd920e07e6.json",
        "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
        "apps/daemon/src/http/affordance-read.test.ts",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/planning/approval-intent-sources.ts"
      ],
      "recordedBy": "worker-0520a42e",
      "recordedAt": "2026-09-05T23:20:52.668Z",
      "treeId": "33b4d027034baaa82d4c3066e10dc53149e1251a",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "2ab09532eaadcb48899469d92cfd5494782620ec",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-0520a42e@2026-09-05T23:21:25Z",
      "paths": [
        ".moe/tasks/task-8bdd14af6cdc44c4a5b86fbd920e07e6.json"
      ],
      "recordedBy": "worker-0520a42e",
      "recordedAt": "2026-09-05T23:21:41.766Z",
      "treeId": "0698bbf4de551ca3545528d503fb548ebb6bc708",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "e4af847bb2dec0e686caf172118e3ebef98a1ddb",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-0520a42e@2026-09-05T23:22:19Z",
      "paths": [
        ".moe/tasks/task-8bdd14af6cdc44c4a5b86fbd920e07e6.json"
      ],
      "recordedBy": "worker-0520a42e",
      "recordedAt": "2026-09-05T23:23:14.814Z",
      "treeId": "f47cbc08b5e990c609b087a2d1ac356b1edf6e83",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "460f5b3bce2b54142a8ea5e8fc013e3e90e0d240",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-0520a42e@2026-09-05T23:23:53Z",
      "paths": [
        ".moe/tasks/task-8bdd14af6cdc44c4a5b86fbd920e07e6.json"
      ],
      "recordedBy": "worker-0520a42e",
      "recordedAt": "2026-09-05T23:24:51.023Z",
      "treeId": "ae541e3b0ff0bb43dc5b98c671c6d4e70b858bfc",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "662209c94be095e779d0465eea4651337e722218",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-0520a42e@2026-09-05T23:25:25Z",
      "paths": [
        ".moe/tasks/task-8bdd14af6cdc44c4a5b86fbd920e07e6.json"
      ],
      "recordedBy": "worker-0520a42e",
      "recordedAt": "2026-09-05T23:25:56.668Z",
      "treeId": "32ce3bfaf2ad0e2c5f15f78aaf7193de1483ad82",
      "status": "WORKING",
      "pushed": true
    }
  ],
  "filesModified": [
    ".moe/tasks/task-8bdd14af6cdc44c4a5b86fbd920e07e6.json",
    "apps/daemon/src/bootstrap/bootstrap-durability.test.ts",
    "apps/daemon/src/http/affordance-read.test.ts",
    "apps/daemon/src/planning/approval-intent.test.ts",
    "apps/daemon/src/planning/approval-intent-sources.ts"
  ],
  "touchedFiles": [
    "apps/daemon/src/planning/approval-intent-sources.ts",
    "apps/daemon/src/planning/approval-intent.test.ts",
    "apps/daemon/src/http/affordance-read.test.ts",
    "apps/daemon/src/bootstrap/bootstrap-durability.test.ts"
  ],
  "unattributedPaths": [
    "apps/control-room/src/live/live-document-proxy.test.ts",
    "apps/control-room/src/live/live-effect-read.test.ts",
    "apps/control-room/src/live/live-effect-read.ts",
    "apps/control-room/src/live/live-publication-candidate.test.ts",
    "apps/control-room/src/live/live-publication-candidate.ts",
    "apps/control-room/src/live/live-runs.ts",
    "apps/control-room/src/v2/components/use-effect-read.test.tsx",
    "apps/control-room/src/v2/components/use-effect-read.ts",
    "apps/control-room/src/v2/goals/criterion-check-form.tsx",
    "apps/control-room/src/v2/goals/criterion-evidence-card.test.tsx",
    "apps/control-room/src/v2/goals/criterion-evidence-card.tsx",
    "apps/control-room/src/v2/goals/criterion-evidence-port.test.ts",
    "apps/control-room/src/v2/goals/criterion-evidence-port.ts",
    "apps/control-room/src/v2/goals/goal-publish.test.tsx",
    "apps/control-room/src/v2/goals/goal-publish.tsx",
    "apps/control-room/src/v2/goals/live-criterion-evidence.tsx",
    "apps/control-room/src/v2/goals/publish-port.ts",
    "apps/control-room/src/v2/ops/live-ops.tsx",
    "apps/control-room/src/v2/ops/live-repository-recovery.tsx",
    "apps/control-room/src/v2/ops/repository-recovery-card.test.tsx",
    "apps/control-room/src/v2/ops/repository-recovery-card.tsx",
    "apps/control-room/src/v2/ops/repository-recovery-port.ts",
    "apps/control-room/src/v2/styles/cordum-repository-effects.css",
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
    "apps/daemon/src/bootstrap/bootstrap-test-fixtures.ts",
    "apps/daemon/src/criterion-evidence/criterion-artifact.js",
    "apps/daemon/src/criterion-evidence/criterion-artifact.ts",
    "apps/daemon/src/criterion-evidence/criterion-command.test.ts",
    "apps/daemon/src/criterion-evidence/criterion-command-edge.js",
    "apps/daemon/src/criterion-evidence/criterion-command-edge.ts",
    "apps/daemon/src/criterion-evidence/criterion-integrated.test.ts",
    "apps/daemon/src/criterion-evidence/criterion-read.js",
    "apps/daemon/src/criterion-evidence/criterion-read.ts",
    "apps/daemon/src/criterion-evidence/criterion-receipt.js",
    "apps/daemon/src/criterion-evidence/criterion-receipt.ts",
    "apps/daemon/src/criterion-evidence/criterion-run.js",
    "apps/daemon/src/criterion-evidence/criterion-run.test.ts",
    "apps/daemon/src/criterion-evidence/criterion-run.ts",
    "apps/daemon/src/criterion-evidence/criterion-runner.js",
    "apps/daemon/src/criterion-evidence/criterion-runner.ts",
    "apps/daemon/src/criterion-evidence/criterion-runner-recovery.test.ts",
    "apps/daemon/src/criterion-evidence/criterion-service.ts",
    "apps/daemon/src/criterion-evidence/criterion-test-fixtures.ts",
    "apps/daemon/src/daemon-repository-workflows.test.ts",
    "apps/daemon/src/daemon-repository-workflow-wiring.js",
    "apps/daemon/src/daemon-repository-workflow-wiring.ts",
    "apps/daemon/src/goals/goal-publish-remote.test.ts",
    "apps/daemon/src/http/document-coverage-criteria.js",
    "apps/daemon/src/http/document-coverage-criteria.ts",
    "apps/daemon/src/http/document-coverage-read.test.ts",
    "apps/daemon/src/http/document-coverage-read.ts",
    "apps/daemon/src/http/repository-publication-read.test.ts",
    "apps/daemon/src/http/repository-remote-read.ts",
    "apps/daemon/src/http/repository-workflow-read.js",
    "apps/daemon/src/http/repository-workflow-read.test.ts",
    "apps/daemon/src/http/repository-workflow-read.ts",
    "apps/daemon/src/http/run-goal-publication.js",
    "apps/daemon/src/http/run-goal-publication.ts",
    "apps/daemon/src/http/runs-read.ts",
    "apps/daemon/src/http/runs-read-contract.ts",
    "apps/daemon/src/orchestrator/node-lander.ts",
    "apps/daemon/src/orchestrator/node-lander-journal.js",
    "apps/daemon/src/orchestrator/node-lander-journal.test.ts",
    "apps/daemon/src/orchestrator/node-lander-journal.ts",
    "apps/daemon/src/orchestrator/node-publisher.test.ts",
    "apps/daemon/src/orchestrator/node-publisher.ts",
    "apps/daemon/src/orchestrator/node-publisher-approved.test.ts",
    "apps/daemon/src/orchestrator/node-publisher-reservation.js",
    "apps/daemon/src/orchestrator/node-publisher-reservation.ts",
    "apps/daemon/src/orchestrator/process-runner-lifecycle.ts",
    "apps/daemon/src/orchestrator/repository-delivery-contracts.ts",
    "apps/daemon/src/orchestrator/repository-delivery-coordinator.test.ts",
    "apps/daemon/src/orchestrator/repository-delivery-coordinator.ts",
    "apps/daemon/src/orchestrator/repository-delivery-runtime.ts",
    "apps/daemon/src/orchestrator/repository-delivery-shutdown.test.ts",
    "apps/daemon/src/orchestrator/repository-delivery-workflows.test.ts",
    "apps/daemon/src/repository/git-publication-credentials.js",
    "apps/daemon/src/repository/git-publication-credentials.test.ts",
    "apps/daemon/src/repository/git-publication-credentials.ts",
    "apps/daemon/src/repository/git-publication-native.test.ts",
    "apps/daemon/src/repository/git-publication-port.js",
    "apps/daemon/src/repository/git-publication-port.test.ts",
    "apps/daemon/src/repository/git-publication-port.ts",
    "apps/daemon/src/repository/publication-approval.test.ts",
    "apps/daemon/src/repository/publication-candidate.test.ts",
    "apps/daemon/src/repository/publication-candidate.ts",
    "apps/daemon/src/repository/publication-effect-ledger.test.ts",
    "apps/daemon/src/repository/publication-effect-ledger.ts",
    "apps/daemon/src/repository/publication-goal-integration.js",
    "apps/daemon/src/repository/publication-goal-integration.test.ts",
    "apps/daemon/src/repository/publication-goal-integration.ts",
    "apps/daemon/src/repository/publish-services.ts",
    "apps/daemon/src/repository/repository-execution-persistence.ts",
    "apps/daemon/src/repository/repository-execution-recovery.js",
    "apps/daemon/src/repository/repository-execution-recovery.test.ts",
    "apps/daemon/src/repository/repository-execution-recovery.ts",
    "apps/daemon/src/repository/repository-landing-attempt.js",
    "apps/daemon/src/repository/repository-landing-attempt.test.ts",
    "apps/daemon/src/repository/repository-landing-attempt.ts",
    "apps/daemon/src/repository/repository-recovery-approval.js",
    "apps/daemon/src/repository/repository-recovery-approval.ts",
    "apps/daemon/src/repository/repository-recovery-command.js",
    "apps/daemon/src/repository/repository-recovery-command.test.ts",
    "apps/daemon/src/repository/repository-recovery-command.ts",
    "apps/daemon/src/repository/repository-recovery-evidence.js",
    "apps/daemon/src/repository/repository-recovery-evidence.test.ts",
    "apps/daemon/src/repository/repository-recovery-evidence.ts",
    "apps/daemon/src/repository/repository-recovery-git.js",
    "apps/daemon/src/repository/repository-recovery-git.test.ts",
    "apps/daemon/src/repository/repository-recovery-git.ts",
    "apps/daemon/src/repository/repository-recovery-native.test.ts",
    "apps/daemon/src/repository/repository-recovery-replay.js",
    "apps/daemon/src/repository/repository-recovery-replay.ts",
    "apps/daemon/src/repository/repository-recovery-service.js",
    "apps/daemon/src/repository/repository-recovery-service.test.ts",
    "apps/daemon/src/repository/repository-recovery-service.ts",
    "apps/daemon/src/repository/repository-recovery-test-fixtures.ts",
    "packages/runner/src/index-surface.test.ts",
    "packages/runner/src/platform/windows/native/broker/Cargo.toml",
    "packages/runner/src/platform/windows/native/broker/src/approved_image.rs",
    "packages/runner/src/platform/windows/native/broker/src/approved_image_hash.rs",
    "packages/runner/src/platform/windows/native/broker/src/approved_image_windows.rs",
    "packages/runner/src/platform/windows/native/broker/src/control.rs",
    "packages/runner/src/platform/windows/native/broker/src/lib.rs",
    "packages/runner/src/platform/windows/native/broker/src/refusal.rs",
    "packages/runner/src/platform/windows/native/broker/src/session.rs",
    "packages/runner/src/platform/windows/native/broker/tests/approved_image.rs",
    "packages/runner/src/platform/windows/native/broker/tests/frame_sweep.rs",
    "packages/runner/src/platform/windows/windows-approved-image-launch.js",
    "packages/runner/src/platform/windows/windows-approved-image-launch.ts",
    "packages/runner/src/platform/windows/windows-boundary.test.ts",
    "packages/runner/src/platform/windows/windows-boundary.ts",
    "packages/runner/src/platform/windows/windows-process-broker-contract.ts",
    "packages/runner/src/platform/windows/windows-process-contract.test.ts",
    "packages/runner/src/platform/windows/windows-process-contract.ts",
    "packages/runner/src/platform/windows/windows-status.ts",
    "packages/runner/src/verification/criterion-check-executor.test.ts",
    "packages/runner/src/verification/criterion-check-executor.ts",
    "tests/e2e/control-room/repository-workflows.spec.ts"
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "worker-0520a42e@2026-09-05T23:25:25Z",
    "at": "2026-09-05T23:25:56.668Z"
  }
}