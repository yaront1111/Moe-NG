{
  "id": "task-8bdd14af6cdc44c4a5b86fbd920e07e6",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "A browser-approved goal still cannot close: the intent approval names an EMPTY approvedNodeScope, so the closure qualifier says no approval names one",
  "description": "Spun out of task-ebbcbdb4967748daa84f314b39bd3960 by worker-2739fee7 2026-09-05, measured while delivering that row. It is the SAME defect class one fence deeper, and it is what still stands between the product and its headline journey.\n\nWHAT task-ebbcbdb4 FIXED. `goal.close` and `repository.publish` required a durable `approval.decide`; the browser approves through `approval.decide_intent`, so a browser-approved goal died at the SEQUENCE gate with BOOTSTRAP_PREREQUISITE_MISSING. That row added `\"approval.decide\": [\"approval.decide_intent\"]` to PREREQUISITE_ALTERNATIVES. `repository.publish` after a browser approval is now FULLY ACCEPTED (arm: bootstrap-durability.test.ts \"accepts repository.publish on a goal approved through approval.decide_intent\", ok true, `kinds.has(\"repository.publish\")` true).\n\nWHAT IS STILL BROKEN, MEASURED NOT INFERRED. `goal.close` on the same world now gets PAST the sequence gate and answers:\n  GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED @ DAEMON_PREREQUISITE\nand it will answer that FOREVER on any project approved in the browser. Cause, symbol by symbol, all re-measurable at HEAD:\n- `apps/daemon/src/planning/approval-intent-sources.ts:137` — the intent seam mints `approvedNodeScope: Object.freeze([])`, with the comment \"No per-node narrowing: an initial-graph approval approves the sealed revision, and there is no durable per-node selection for it to restate.\"\n- `apps/daemon/src/goals/goal-close-prerequisite.ts:87` — `approvedNodeScope()` answers **null** when `scope.length === 0`.\n- `apps/daemon/src/goals/goal-qualification.ts:157-159` — `qualify()` reads `readApprovedNodeScope(store, goalId)`; null refuses GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED \"no current approval names an approved node scope\", BEFORE any per-node review walk.\nDIRECT MEASUREMENT: on a world driven through the shipped journey to `approval.decide` and then approved via `runApprovalIntentCommand`, `readApprovedNodeScope(store, GOAL_ID)` returns **null**. Pinned by a committed arm, so this row cannot be closed without it going red: bootstrap-durability.test.ts \"lets goal.close past the sequence gate, where the GOAL's own authority answers\" asserts BOTH the code and `readApprovedNodeScope(...) === null`, and affordance-read.test.ts \"dispatches the goal.close offer past the sequence gate to the goal's own authority\" asserts the same from a real `/affordances/read` frame's own offer.\n\nWHY IT MATTERS. The offer is still made — correctly, per the owner decision in comment-5a8278d76aa24b44989a906caaef589b — and the two authorities on the frame now agree (step READY, offer live). So the operator is offered a Close, clicks it, and gets a refusal. The button is no longer lying about the PREREQUISITE, but the journey \"close a finished goal from the browser\" is still unreachable end to end. The refusal is at least actionable now (a goal-shaped closure code instead of a generic bootstrap one), which is the improvement task-ebbcbdb4 shipped.\n\nWHY IT WAS NOT FIXED THERE. It is a CLOSURE-AUTHORITY design question, not a table entry: the empty scope is deliberate and commented, and lives in two modules that row did not own. Deciding it needs an owner. Both prior closure rows are ARCHIVED (task-8145137ca3c144f0878deb83d05bab63, task-8fdd81b81c0f4dc88c16203de139e2ba), so nothing live owns it.\n\nTHE QUESTION AN ARCHITECT MUST DECIDE, and it must be ONE authority, not two. Either (a) an initial-graph intent approval should name the sealed revision's execution-bearing nodes as its approved scope — measure what `commitIntentActivation` already knows about the graph revision before assuming it can; or (b) `qualify()` should treat an empty scope from an initial-graph approval as \"the whole sealed revision\" rather than as \"unknown\". (a) makes the durable record self-describing; (b) keeps the record as-is and moves the interpretation to the reader. Do NOT relax GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED or any closure code to make a close succeed — the fail-closed invariant that governed task-ebbcbdb4 governs here too.\n\nDO NOT re-implement task-ebbcbdb4's fix: `PREREQUISITE_ALTERNATIVES` already carries `\"approval.decide\": [\"approval.decide_intent\"]` and `COMMAND_PREREQUISITES` is byte-identical. Re-measure both before planning (task rail 2 of the parent applies here).",
  "definitionOfDone": [
    "A goal approved ONLY through approval.decide_intent, with its review acceptance seeded, is CLOSED by a real goal.close dispatch: ok true and the goal reaches its accepted terminal lifecycle. Asserted on the durable outcome, not on the absence of a throw.",
    "The two arms task-ebbcbdb4 left pinning the CURRENT answer are updated deliberately, not deleted: bootstrap-durability.test.ts 'lets goal.close past the sequence gate, where the GOAL's own authority answers' and affordance-read.test.ts 'dispatches the goal.close offer past the sequence gate to the goal's own authority' both assert readApprovedNodeScope(...) === null / a non-accepted dispatch today. Whichever fix ships, say in the arm why the answer changed.",
    "Fail-closed is preserved and proven by mutation: a goal whose approved criteria are NOT verified still refuses its specific closure code, and a REJECTED approval.decide_intent still cannot close. Assert the exact code AND the layer; verify each by mutating the production surface and confirming the arm goes red.",
    "The seeded approval.decide journey does not regress: apps/daemon/src/goals/j1-command-path.test.ts still closes its goal and still reads lifecycle COMPLETED.",
    "Gates quoted with count lines. `pnpm --filter @moe/daemon test` is currently BLOCKED in the shared checkout by an uncommitted peer 'playwright' dependency in apps/daemon/package.json (pnpm demands a full install); if it is still blocked, run the same vitest directly (cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json <paths>) and say so. `pnpm test` covers no apps/** and is not evidence."
  ],
  "taskRails": [
    "THE REFUSAL IS NOT THE BUG, THE UNSATISFIABLE PRECONDITION IS. Never relax GOAL_CLOSE_REVIEW_ACCEPTANCE_REQUIRED, any other GOAL_CLOSE_* code, or the closure qualifier's fail-closed catch, to make a close succeed. Make the approved node scope KNOWABLE for an intent approval; do not make an unknown scope pass.",
    "ONE AUTHORITY, NOT TWO. task-ebbcbdb4 exists because two authorities answered differently about the same command. Whichever side you fix — the writer (approval-intent-sources.ts) or the reader (goal-qualification.ts) — the other must not grow a second, compensating rule."
  ],
  "implementationPlan": [],
  "status": "PLANNING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "dependsOn": [
    "task-ebbcbdb4967748daa84f314b39bd3960"
  ],
  "priority": "HIGH",
  "order": 1008.3,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T20:13:38.542Z",
  "updatedAt": "2026-09-05T20:13:38.542Z"
}