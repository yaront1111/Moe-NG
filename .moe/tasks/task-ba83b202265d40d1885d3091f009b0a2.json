{
  "id": "task-ba83b202265d40d1885d3091f009b0a2",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "The Environments screen shows what is set without ever showing a value",
  "description": "SPIDR child 3 of task-9ae492b373fa4e7aa6b1e91585f6e612 (architect-20a6d1eb, 2026-09-05). dependsOn child 1 (task-d0c63cd8848a4da18ad3fbb8eb1d95c3, the store and reads) and child 2 (task-72b22fcf9da54b3b91dabf5b7392aff1, required names and delivery). PROOF ROW: it owns the UI, the Playwright journey and the docs (global rail 21).\n\nWHAT THIS ROW OWNS: an Environments screen (per environment: required vs set, set/unset dialogs, fingerprint, last update) and the goal's 'N required variables unset for preview' with a link to it.\n\nTHE SCREEN NEVER SHOWS A VALUE, and it is the surface most likely to leak one: it is the only place an operator TYPES a value, so the input, its state, any error message and any browser devtools-visible prop must be considered. The reads child 1 built return {name, isSet, sha256 fingerprint, updatedAt} and no value - the screen must not add one back by echoing what was typed after a failed submit, which is the natural way to be helpful and the exact way this leaks into a screenshot.\n\nTHE FINGERPRINT IS THE OPERATOR'S ONLY FEEDBACK. Since they can never see the value back, the sha256 fingerprint changing after an update is how they know the update took. Render it, and make clear it is a fingerprint rather than a truncated value.\n\nTHE UNSET COUNT IS THE POINT OF THE GOAL CARD. 'N required variables unset for preview' is what stops a deploy failing for a reason nobody can see - the required names come from the approved contract via child 2.\n\nMEASURED AT HEAD (branch moe/work-2026-09-04): the ops screens live at apps/control-room/src/v2/ops/ (ops-screens.tsx is 228 lines, close to the 250 ceiling - a new screen likely belongs in its own module rather than appended there), the live read clients in apps/control-room/src/live/ decode with EXACT-KEY checks, and every new route needs its pin in dev-proxy-paths.ts.",
  "definitionOfDone": [
    "An Environments screen renders, per environment, the required-vs-set table with each variable's fingerprint and last update, plus set and unset dialogs, against a REAL daemon frame.",
    "NO VALUE IS EVER RENDERED OR RETAINED: assert that after a FAILED submit the screen does not echo the typed value back, and that no value appears in the rendered output for any state. This is the leak this screen invites - echoing input after an error is the helpful-looking behaviour that puts a secret in a screenshot.",
    "The fingerprint CHANGES in the UI after an update and is labelled as a fingerprint, not as a truncated value - the operator can never see the value back, so this is their only confirmation the update took.",
    "The goal card shows 'N required variables unset for <environment>' with a link to the screen, driven by the required names child 2 reads from the approved contract; assert the count for a goal with two required names and one set, and assert the card is absent when none are required.",
    "All four refusals render VERBATIM with their codes (ENV_NAME_INVALID, ENV_VALUE_TOO_LARGE, ENV_ENVIRONMENT_UNKNOWN, ENV_STORE_KEY_UNAVAILABLE) - and the refusal text must not contain the submitted value.",
    "A Playwright journey against a REAL daemon covers the required-vs-set table, a set/unset round trip and the fingerprint changing on update; `pnpm test:e2e:browser` count line quoted. The new read's decoder is EXACT-KEY and its proxy pin is added to dev-proxy-paths.ts.",
    "The runbook documents the environments model (environments, required names from the contract, fingerprints, and that values are never readable back); gates quoted with count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, `pnpm test:e2e:browser`."
  ],
  "taskRails": [
    "NEVER ECHO A TYPED VALUE BACK - not after a failed submit, not in a refusal message, not in a retained form state. This screen is the only place a value is typed and therefore the likeliest leak.",
    "THE FINGERPRINT IS NOT A TRUNCATED VALUE. Label it so; an operator who thinks they are seeing part of the secret will treat the screen as unsafe to share, or worse, will not.",
    "THIS ROW PROVES, IT DOES NOT REBUILD: a store or read defect is child 1's, a required-names or delivery defect is child 2's. Comment there and report_blocked.",
    "ops-screens.tsx is 228 lines against a 250 ceiling - put a new screen in its own module rather than appending, and measure with `wc -l` before writing.",
    "ASCII-only sources and apostrophe-free JSX text (scheduler package-boundary scan); check any new testid namespace is free; EXACT-KEY decoder plus a dev-proxy-paths pin for any new read."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "PREREQUISITE HARD STOP. This row PROVES; it does not rebuild. Verify each prerequisite on disk and STOP if any is missing.\n\n- `ls apps/daemon/src/http/ | grep -i env` and `git grep -n \"/environments/read\" -- apps/daemon/src` — the HTTP read (task-ef76a7f4523d46f48a2f9eb19595e801). At plan time this returned NOTHING, which is why that row exists. Confirm it is reachable from the COMPOSED production listener, not only from a hand-built test server — that distinction is exactly what forced task-eb2bb09d to exist after task-7ca9dca3.\n- `git grep -rn \"required\" -- apps/daemon/src/environment` and whatever symbol child 2 (task-72b22fcf9da54b3b91dabf5b7392aff1) shipped for REQUIRED NAMES from the approved contract. It was still PLANNING at plan time and DoD 4 depends on it.\n- `git grep -rn \"ENV_NAME_INVALID\\|ENV_VALUE_TOO_LARGE\\|ENV_ENVIRONMENT_UNKNOWN\\|ENV_STORE_KEY_UNAVAILABLE\" -- apps/daemon/src/environment` — child 1's four codes and their CLOSED code->layer map. Record the exact layer each code carries; step 5 asserts code AND layer and must reuse this map rather than minting a second one.\n\nIf any is absent, `moe.report_blocked` with `blockedOnTaskIds`. Do NOT mock the read, do NOT hand-write a daemon frame, and do NOT narrow DoD 1 or 4 — a mock-backed journey proves the shape and retires the question unanswered.\n\nAlso measure before writing (rail 4): `wc -l apps/control-room/src/v2/ops/ops-screens.tsx` (236 at plan time against a 250 ceiling — the description's 228 is stale) and confirm the testid namespace you intend to claim is free with `git grep -c \"cr\\.<namespace>\" -- apps tests`.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/ops/ops-screens.tsx",
        "apps/control-room/src/live/dev-proxy-paths.ts"
      ],
      "note": "PREREQUISITE HARD STOP: ALL THREE PRESENT. No block.\n\n(1) HTTP READ EXISTS AND IS COMPOSED. `ls apps/daemon/src/http/ | grep -i env` -> environments-read.{ts,js,test.ts} (the plan's \"returned NOTHING\" is stale; task-ef76a7f4 landed). Reachable from the COMPOSED production listener, not only a hand-built server: apps/daemon/src/http/http-listener-read-dispatch.ts carries the path, and apps/daemon/src/daemon-entry-ops.test.ts:205-265 drives it through `startDaemon` reading `composed.environmentReads?.()` off the real composition, with a 503 LISTENER_ENVIRONMENTS_UNAVAILABLE negative control at :250. Frame measured verbatim: ok `{environment, ok:true, variables:[{fingerprintSha256,isSet,name,updatedAt}]}`; store refusal `{code,detail,layer,ok:false}` at 200; listener refusal `{code,layer}` at 400/503. NO value field on any path (environments-read.ts:32 path const, :120 verbatim forward).\n\n(2) REQUIRED NAMES (child 2) EXIST. apps/daemon/src/environment/environment-required-variables.ts:90 `requiredVariableNames(revision)` (deduped, UTF-16-sorted, off `revision.deploymentRequirements[].environmentVariableNames`) and :117 `unsetVariableNames(required, read)`. BROWSER-SIDE the same carrier is already decoded: apps/control-room/src/v2/goals/gate1-contract-shape.ts:28 rosters `environmentVariableNames` and :206 exports `gate1Requirements(revision)`; the read client is apps/control-room/src/live/live-product-contract-gate-1.ts:138 `readProductContractGate1`. Step 6 reads names from that surface, not from a re-derivation.\n\n(3) FOUR CODES + CLOSED code->layer MAP, recorded for step 5 reuse. apps/daemon/src/environment/environment-contracts.ts:63 `ENVIRONMENT_CODE_LAYERS` = { ENV_ENVIRONMENT_UNKNOWN: \"SCOPE\", ENV_NAME_INVALID: \"NAME\", ENV_STORE_KEY_UNAVAILABLE: \"KEY\", ENV_VALUE_TOO_LARGE: \"VALUE\" }. `ENVIRONMENT_LAYERS` = [KEY, NAME, SCOPE, VALUE]. Details are FIXED prose keyed by code (:80 `ENVIRONMENT_REFUSAL_DETAILS`), never interpolated and asserted digit-free -- so ENV_VALUE_TOO_LARGE reads \"the value exceeds the permitted size for an environment variable\" and cannot carry the value or the limit number. Step 5 reuses this map verbatim; no fifth code.\n\nMEASUREMENTS BEFORE WRITING (rail 4):\n- `wc -l apps/control-room/src/v2/ops/ops-screens.tsx` = 238 (plan said 236, description said 228 -- both stale, ceiling 250). New screen goes in its OWN module, confirmed.\n- TESTID NAMESPACE `cr.environments.*` IS NOT FREE: apps/control-room/src/v2/ops/environments-section.tsx (12 hits) + live-environments.tsx (16) already own it for the deployment-HEALTH probe ring (task-df972c274f, /deployments/health/read). That is a different capability from this row's variable table. Measured free alternatives (0 files each): cr.env-vars, cr.envvars, cr.variables, cr.env-variables. CLAIMING `cr.env-vars.*`.\n\nDISPATCH PATH MEASURED (needed by steps 3/4/5/7, not stated in the plan). `environment.set_variable` / `environment.unset_variable` are NOT in apps/daemon/src/bootstrap/bootstrap-contracts.ts:30 BOOTSTRAP_COMMAND_KINDS, so /affordances/read mints NO offer for them -- the spendOffer-from-a-daemon-offer pattern (publish-port.ts, policy-install-port.ts) does not apply. They ARE served: apps/daemon/src/daemon-command-registry.ts:510 routes them to `runEnvironmentEdge` BEFORE `requestOf` (so no plaintext reaches durable request bytes), and apps/daemon/src/daemon-command-environment.test.ts:324 `world.send` proves acceptance through the REAL `handleCommandRequest(deps, ..., \"HTTP_LISTENER\")` with a self-built envelope carrying `expectedVersion: 0` and an arbitrary `targetAggregateId`. The generated builder's guard is `isAffordanceFor` (generated-client.ts) which checks ONLY `commandKind`, and packages/control-room-client/src/generated/generated-client.ts:205-206,329-330 already exports builders for both kinds. So the screen mints its own affordance record `{commandId, commandKind, expectedVersion: 0, targetAggregateId}` and dispatches via `dispatchPreparedPayload` (live-command-dispatch.ts:139). Payloads measured off runEnvironmentEdge: set = {environment, name, value}; unset = {environment, name}.\n\nPeer dirt present in the shared checkout (35 M + untracked); none touched.",
      "startedAt": "2026-09-07T03:47:03.844Z",
      "completedAt": "2026-09-07T03:50:19.320Z"
    },
    {
      "stepId": "step-2",
      "description": "THE READ CLIENT: exact-key decoder plus the proxy pin (DoD 6, second half). Create the live read module under apps/control-room/src/live/ following the neighbouring live read clients.\n\nEXACT-KEY DECODING: an unknown key and a missing key each REFUSE with a specific code, asserted with the code AND the layer that refused — never merely that decoding failed. The decoded shape is {name, isSet, sha256 fingerprint, updatedAt} per variable, per environment. THERE IS NO VALUE FIELD, and the decoder must not tolerate one: if the daemon ever grows a value field, this decoder should REFUSE rather than pass it through to a screen that might render it. Assert that as its own arm — it is the client-side half of epic rail 3.\n\nADD THE PROXY PIN to `DEV_PROXY_PATHS` in apps/control-room/src/live/dev-proxy-paths.ts (55 lines, frozen list at :5), keeping the file's existing ordering. Note `/design/read` is NOT pinned there, so a pin is not automatic for every route — DoD 6 requires one here deliberately, and without it the dev lane cannot reach the read at all.\n\nNO VALUE IS RETAINED CLIENT-SIDE: no localStorage, no sessionStorage, no module-level cache of a submitted value. Copy the source-text assertion pattern from apps/control-room/src/v2/goals/goal-publish.test.tsx:204-215 — readFileSync of your own module, then `not.toContain(\"localStorage\")` / `\"sessionStorage\"`. That arm is cheap and it is the one that catches a helpful cache added later.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/live/live-environments-read.ts",
        "apps/control-room/src/live/dev-proxy-paths.ts"
      ],
      "newFiles": [
        "apps/control-room/src/live/live-environments-read.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/live/live-environment-variables.ts",
        "apps/control-room/src/live/live-environment-variables.test.ts",
        "apps/control-room/src/live/dev-proxy-paths.ts",
        "apps/control-room/src/live/live-document-proxy.test.ts"
      ],
      "note": "READ CLIENT + PROXY PIN LANDED. `pnpm --filter @moe/control-room test` -> Test Files 186 passed (186), Tests 2341 passed (2341), 13.52s, EXIT 0.\n\nNEW: apps/control-room/src/live/live-environment-variables.ts (196 lines, under 250). Frames measured off daemon-entry-ops.test.ts:219-236 against the COMPOSED listener, not invented: ok `{environment, ok, variables}` with four-key rows; store refusal `{code, detail, layer, ok:false}` at 200; listener refusal `{code, layer}` at 400/503. `exactDataRecord` copied verbatim from live-deployments-health.ts (duplicated deliberately, per that module's own note: a shared decoder widens every read at once).\n\nTHE VALUE ARM HAS ITS OWN CODE, and that is the load-bearing choice here. A frame carrying a value-shaped key answers ENVIRONMENT_VARIABLES_VALUE_PRESENT @ CONTROL_ROOM_ENVIRONMENT_VARIABLES, NOT the generic ENVIRONMENT_VARIABLES_RESPONSE_INVALID. Collapsing the two would make the guard satisfiable by any unrelated shape drift -- the arm would pass while no longer testing its subject (rail 1). The check runs BEFORE the refusal read too, because refusals are forwarded whole, so a refusal envelope that grew a value slot would otherwise reach the screen intact. `carriesValueShapedKey` recurses and covers cipher/plaintext/sealed/secret/value, with a negative control asserting it is false on the clean frame so the sweep is not vacuous.\n\nTEST (live-environment-variables.test.ts, 216 lines): 20 arms. Every refusal arm asserts CODE AND LAYER, never merely \"not ok\" -- three authorities answer this route (store SCOPE/NAME/KEY/VALUE, listener CONTROL_ROOM_LISTENER, this client CONTROL_ROOM_ENVIRONMENT_VARIABLES) and a \"not ok\" arm would stay green if a different one started answering. Unknown key, missing key, unknown row key, missing row key, non-200, wrong-environment frame, transport throw, non-JSON body: each pinned to its code. Empty table decodes as a FACT (variables: []), not a refusal.\n\nTWO CONSUMERS THE PLAN DID NOT NAME, found by grepping the SHAPE rather than the symbol (global rail 19):\n1. apps/control-room/src/live/live-document-proxy.test.ts:26 asserts `DEV_PROXY_PATHS` with toStrictEqual on the FULL literal array -- a pinned roster, so the pin is TWO coupled edits, not one. Added \"/environments/read\" between /deployments/health/read and /health/read in both files, keeping the existing ordering. It red first (`toStrictEqual` at :6) and is green now; that red is the proof the pin is actually reachable rather than dead.\n2. `new URL(..., import.meta.url)` + readFileSync FAILS under this package's jsdom environment: \"TypeError: The URL must be of scheme file\". The working pattern is goal-publish.test.tsx:226 -- `join(dirname(fileURLToPath(import.meta.url)), \"<file>\")`. Switched to it. Source-text arm now asserts absence of localStorage / sessionStorage / indexedDB with a positive control (the file contains mapEnvironmentVariablesAnswer) so the three absences cannot be an empty read.\n\nNOTE ON THE GATE SHAPE: `pnpm --filter @moe/control-room test -- live-environment-variables` does NOT filter -- it ran all 186 files (matches the known `--`-reaches-vitest-as-a-filter behaviour). Count line quoted above is the whole package leg either way.\n\nASCII-only confirmed in both new files.",
      "startedAt": "2026-09-07T03:50:22.531Z",
      "completedAt": "2026-09-07T03:53:19.090Z"
    },
    {
      "stepId": "step-3",
      "description": "THE ENVIRONMENTS SCREEN (DoD 1) — its OWN module under apps/control-room/src/v2/ops/, NOT appended to ops-screens.tsx (236 lines against a 250 ceiling).\n\nRender, per environment, the required-vs-set table: each variable's name, whether it is set, its sha256 FINGERPRINT and last update, plus set and unset dialogs.\n\nTHE FINGERPRINT IS THE OPERATOR'S ONLY FEEDBACK and it must be LABELLED AS A FINGERPRINT, not shown as a bare truncated-looking string (DoD 3, rail 2). An operator who believes they are seeing part of the secret will either treat the screen as unsafe to share or, worse, will not. Put the word in the rendered output and assert it — not in a comment.\n\nMount it as a route and register it wherever ops screens are registered; a screen no route reaches is dead code that satisfies every existence grep while the feature does not exist.\n\nCONSTRAINTS (rail 5): ASCII-only sources; apostrophe-free JSX text — the scheduler package-boundary scan reds on an apostrophe in JSX and the red looks unrelated to the diff that caused it; the new testid namespace confirmed free in step 1; and `wc -l` under 250, quoted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/ops/environments-screen.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/ops/environments-screen.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/ops/environment-variables-screen.tsx",
        "apps/control-room/src/v2/ops/environment-variables-screen.test.tsx",
        "apps/control-room/src/v2/ops/environment-variables-model.ts",
        "apps/control-room/src/v2/ops/environment-variables-port.ts",
        "apps/control-room/src/v2/ops/live-environment-variables.tsx",
        "apps/control-room/src/v2/goals/goal-environments.tsx",
        "apps/control-room/src/v2/goals/goal-status-strip.tsx",
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "note": "THE SCREEN, ITS PORT, ITS MODEL AND ITS ROUTE. `npx vitest run --root apps/control-room src/v2/ops/environment-variables-screen.test.tsx` -> Test Files 1 passed (1), Tests 16 passed (16). `npx tsc --noEmit -p apps/control-room/tsconfig.json` EXIT 0.\n\nSIZES, all under 250: environment-variables-screen.tsx 214, goal-environments.tsx 164 (168 after the projectId fix), environment-variables-model.ts 99, environment-variables-port.ts 96, live-environment-variables.tsx 88. ops-screens.tsx UNTOUCHED at 238. ASCII-only measured with `grep -cP '[^\\x00-\\x7F]'` = 0 on every new file; the only apostrophes are inside block comments, none in JSX text.\n\nFOUR PRODUCTION MODULES, NOT ONE, and the seams are load-bearing rather than cosmetic:\n- environment-variables-model.ts: the required-vs-set merge as a PURE function. The screen renders these rows and the goal card counts them, so \"N unset\" is the table with a filter applied BY CONSTRUCTION -- a separately derived count would let the card and the table disagree about the same environment, and the card is the one read before a deploy.\n- environment-variables-port.ts: the two writes.\n- environment-variables-screen.tsx: the pure screen.\n- live-environment-variables.tsx: read, poll, and a re-read the instant a write settles.\n\nTHE DISPATCH PATH, since the plan did not name one and no offer exists (step 1). The port MINTS its own affordance `{commandId, commandKind, expectedVersion: 0, targetAggregateId}` and spends it through `spendOffer`. Justified on disk, not by preference: the two kinds are absent from BOOTSTRAP_COMMAND_KINDS so /affordances/read mints nothing; `isAffordanceFor` (generated-client.ts) checks ONLY `commandKind`; and daemon-command-environment.test.ts:324 drives the real `handleCommandRequest` with exactly this envelope shape. `expectedVersion: 0` is not a version claim -- runEnvironmentEdge reads neither it nor targetAggregateId and derives the aggregate from the AUTHENTICATED principal, which is the point.\nCHOSE spendOffer OVER dispatchPreparedPayload deliberately: live-command-dispatch.ts flattens a refusal to `CODE @ stage`, dropping the daemon `detail` and reporting stage where DoD 5 needs layer. ENV_STORE_KEY_UNAVAILABLE without its detail is a bare code an operator cannot act on. Recorded in the port header, including the honest note that spendOffer derives requestDigest and correlationId from a sha256 OVER the payload -- the shared wire, and the same digest path the daemon canary already covers. I did not paper over it.\n\nWHERE IT IS MOUNTED, and why not where the plan implied. The screen is GOAL-scoped: mounted from cordum-app.tsx as `<LiveGoalEnvironments>` immediately after LiveGoalDeployments, under a new anchor `GOAL_SECTION_IDS.environments` = \"cr-goal-environments\". The Health screen was the obvious home but it CANNOT work there -- required names come from the APPROVED CONTRACT, contracts are goal-scoped (`readPendingContract(headers, goalId, expectedProjectId)`), and a project-scoped copy could only say set/not-set without knowing what is required, which is the entire content of the card. Added the anchor to GOAL_SECTION_IDS as an ANCHOR, not a strip stage: the strip states where the GOAL stands and unset variables are a fact about an environment, so `publish`-style strip membership would be wrong. Said either way, per step 6.\n\nTHE ENVIRONMENT ROSTER IS THE DAEMON'S. No hard-coded [preview, production, verify] browser-side -- measured, no such roster exists in apps/control-room and ENVIRONMENT_NAMES lives in apps/daemon (no import edge). `/deployments/read` names them. Unlike live-environments.tsx I do NOT filter to outcome === \"DEPLOYED\": that would hide the exact case the card exists for, an operator setting variables BEFORE the first deploy.\n\nFINGERPRINT LABELLED IN THE RENDERED OUTPUT, not in a comment: the cell reads `sha256 fingerprint <12 hex>` with title \"A sha256 fingerprint of the stored value. It is not part of the value.\", asserted. Unset renders \"Not set\", never an empty-looking string.\n\nVALUE HANDLING (arms land in step 4): the typed value lives in ONE useState inside SetDialog, and `settle()` clears `editing` on EVERY settled submit -- refused path included -- which UNMOUNTS the dialog that owns it. It is never passed downward as a prop, so no devtools-visible prop carries it. Input is type=\"password\" autoComplete=\"off\" spellCheck={false}.\n\nCONTRACT-UNREADABLE DEGRADE: `setup.projectId` is null on the legacy dev path, so the contract read is unavailable there; that answers NONE -> no required names -> card ABSENT. Absent, never a \"0 unset\" badge over a contract nobody read.\n\n16 arms landed: table (set/unset/Extra/ordering/updatedAt/summary/read-refusal), fingerprint labelling x3, dialogs and payload shapes x4, and the pure model x3 including a REFUSED read counting as no set names rather than as an empty environment.",
      "startedAt": "2026-09-07T03:53:21.367Z",
      "completedAt": "2026-09-07T04:00:28.105Z"
    },
    {
      "stepId": "step-4",
      "description": "NO VALUE IS EVER RENDERED OR RETAINED (DoD 2) — the row's central arm, in the screen's test file. This is the leak the screen invites, so test it as an attacker would.\n\nSubmit a distinctive sentinel value through the set dialog, then assert that sentinel appears NOWHERE, across every state:\n(a) after a SUCCESSFUL submit — in the rendered output, in the table, in the fingerprint cell;\n(b) after a FAILED submit — THIS IS THE ONE. Echoing the typed value back so the operator can correct it is the natural, helpful-looking behaviour, and it is exactly how a secret reaches a screenshot. Assert the input is NOT repopulated and the sentinel is not in the DOM;\n(c) in every refusal message (step 5 covers the codes; this arm covers the value);\n(d) in any component prop a browser devtools inspector would show — assert against the rendered tree, not only the visible text, since a value passed as a prop is visible in devtools even when nothing paints it.\n\nSearch the WHOLE rendered container for the sentinel, not a specific node: an assertion scoped to the input misses the value re-rendered somewhere else.\n\nMUTATE AND CONFIRM RED: make the failed-submit path repopulate the input, run the arm, confirm it goes RED, restore. A leak test that has never been seen red is not evidence — and this is the one arm on this row whose silent failure has a real-world cost.\n\nUse a sentinel that cannot occur naturally, and do NOT commit a realistic-looking secret as a fixture (epic rail 3 forbids a secret in a committed fixture, and a plausible one invites a credential scanner alert).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/ops/environments-screen.test.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/ops/environments-screen.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/ops/environment-variables-screen.test.tsx"
      ],
      "note": "NO VALUE IS EVER RENDERED OR RETAINED -- 9 arms + a positive control. `npx vitest run --root apps/control-room src/v2/ops/environment-variables-screen.test.tsx` -> Test Files 1 passed (1), Tests 25 passed (25), EXIT 0.\n\nSENTINEL = \"SENTINEL-not-a-secret-0000-1111-2222\". Cannot occur naturally, and deliberately NOT a plausible credential: epic rail 3 forbids a realistic-looking secret in a committed fixture, and a plausible one would trip a credential scanner on every clone.\n\nTWO SEARCH SURFACES, both over the WHOLE container, never a chosen node:\n- `domText` = `container.innerHTML`, chosen over textContent because it carries ATTRIBUTES too, so a value that reached value=/title=/data-*/aria-* is caught even though nothing paints it.\n- `fiberProps` walks the rendered React fiber off every `__react*` key, the way a devtools inspector reads props -- that is surface (d). A value handed to a child as a prop is invisible to innerHTML.\n\nARMS: (a) successful submit x2 (whole container, fiber, write-ok cell, summary cell; and the fingerprint cell after the read reports the new fingerprint); (b) FAILED submit -- refusal path, dialog gone, reopened field empty; (b) UNDELIVERED submit, because a thrown dispatch takes the OTHER branch of settle() and the value must die on that one too; (c) all four refusal codes swept, each asserting its own code present AND the sentinel absent; (d) the fiber walk; plus browser-local storage on a failed path, and the field attributes (type=password, autocomplete=off, spellcheck=false).\n\nTHREE CONTROLS so no absence is vacuous: a planted-sentinel render proves the search technique finds one; arm (d) asserts the walker DOES find the value while the dialog is open, which is what proves it reaches props at all; and every refusal arm asserts its refusal actually rendered, so \"no sentinel\" is not satisfied by an idle screen.\n\nMUTATION DRILL -- AND IT CHANGED THE TEST. Lease census first: full-suite-gate and browser-e2e-gate are both HELD by peers, so before touching disk I censused Win32_Process for vitest/pnpm-test and found NONE (only the two daemon-main.ts processes) -- the full-suite lease looks orphaned, matching task-ae309e47's 03:45Z census. Window under two minutes, one file, no peer run in flight.\n`git status --porcelain apps/control-room/src/v2/ops/environment-variables-screen.tsx` = `?? ` (UNTRACKED), so `git diff` certifies nothing for it -- backed up to %TEMP%/env-screen-backup-worker-b1872f24.tsx, pre-drill sha256 3587c047a0ce67b962315282379b3ad086e29e435dd7b620e2d60da4f0f88248.\nMUTANT: `settle()` changed from `setEditing(null)` to `if (next.ok) setEditing(null)` -- keep the dialog open on refusal so the operator can fix their typo. The helpful-looking leak, verbatim.\n\nFIRST DRILL RUN EXPOSED A WEAK ARM, and this is the part worth reading. 4 arms went red, but ALL FOUR failed at `AssertionError: expected <form>...</form> to be null` -- inside the `submitSentinel` helper, which waited for the DIALOG to disappear. So the arms reddened on the helper's precondition, not on the sentinel search: they would have gone red for the right change at the wrong assertion, and would still have been GREEN if the value leaked somewhere the dialog is not. That is rail 1's \"one added layer away from vacuous\", found by drilling rather than by reading.\nFIX: `submitSentinel` now settles on the ANSWER (`cr.env-vars.write-ok` or `cr.env-vars.write-refusal` appearing), not on the dialog closing; the dialog-is-gone check moved into arm (b) as its own explicit assertion.\nSECOND DRILL RUN, the right red: 3 arms, each failing on `AssertionError: expected '<div class=\"cr2-card\" data-testid=\"cr...' not to contain 'SENTINEL-not-a-secret-0000-1111-2222'` at `expect(domText(container)).not.toContain(SENTINEL)`. The sentinel search itself is the failing assertion.\n\nRESTORE PROVED IN A SEPARATE CALL: post-restore sha256 = 3587c047a0ce67b962315282379b3ad086e29e435dd7b620e2d60da4f0f88248, byte-identical to the backup and to the pre-drill hash; `grep -c \"MUTATION DRILL\"` = 0. Re-run after restore: 25 passed (25) EXIT 0. Only the TEST file changed in this step; the production screen is byte-identical to what step 3 landed.",
      "startedAt": "2026-09-07T04:00:30.767Z",
      "completedAt": "2026-09-07T04:03:11.422Z"
    },
    {
      "stepId": "step-5",
      "description": "THE FINGERPRINT CHANGES, AND THE FOUR REFUSALS (DoD 3 and 5) — same test file.\n\nFINGERPRINT (DoD 3): after an update, the rendered fingerprint CHANGES. Assert the before and after values differ AND that the new one matches what the read returned — not merely that something changed, which a re-render alone would satisfy. Because the operator can never see the value back, this is their only confirmation the update took. Also assert the fingerprint is labelled as a fingerprint in the rendered output.\n\nREFUSALS (DoD 5): ENV_NAME_INVALID, ENV_VALUE_TOO_LARGE, ENV_ENVIRONMENT_UNKNOWN and ENV_STORE_KEY_UNAVAILABLE each render VERBATIM with their code plus operator words. Reuse child 1's CLOSED code->layer map recorded in step 1 — do not mint a second map, and do not invent a fifth code.\n\nAND THE REFUSAL TEXT MUST NOT CONTAIN THE SUBMITTED VALUE. ENV_VALUE_TOO_LARGE is the trap: the natural message names the size and often quotes the offending input. Assert the sentinel is absent from each refusal message, and assert ENV_VALUE_TOO_LARGE reports the LIMIT rather than the value.\n\nENV_STORE_KEY_UNAVAILABLE deserves operator words that say what to do — it means the store key is missing, which is an operator-fixable condition, not a bug report.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/ops/environments-screen.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/ops/environment-variables-screen.tsx",
        "apps/control-room/src/v2/ops/environment-variables-screen.test.tsx"
      ],
      "note": "FINGERPRINT CHANGES + ALL FOUR REFUSALS. `npx vitest run --root apps/control-room src/v2/ops/environment-variables-screen.test.tsx` -> Test Files 1 passed (1), Tests 34 passed (34), EXIT 0 (25 -> 34, 9 new arms).\n\nFINGERPRINT (DoD 3), asserted on BOTH halves rather than \"it changed\": after a settled write the shown `data-fingerprint` differs from the old one AND equals exactly what the read returned. \"Something changed\" alone would be satisfied by a re-render that scrambled it, which is the failure this DoD clause is written against. The visible text is checked too, still carrying the word: `sha256 fingerprint <12 hex>`. Second arm: an unset takes a fingerprinted row back to \"Not set\" on both the fingerprint cell and the state cell.\n\nTHE FOUR REFUSALS (DoD 5). Codes, layers and details are child 1's, reused, not re-minted:\n  ENV_ENVIRONMENT_UNKNOWN @ SCOPE  \"the environment named is not one this project has\"\n  ENV_NAME_INVALID @ NAME          \"the variable name is not a permitted environment variable name\"\n  ENV_STORE_KEY_UNAVAILABLE @ KEY  \"the environment store key could not be derived from the daemon credential\"\n  ENV_VALUE_TOO_LARGE @ VALUE      \"the value exceeds the permitted size for an environment variable\"\nEach arm asserts CODE @ LAYER together (four authorities can answer this control; \"not ok\" alone would stay green when a different one starts answering), the daemon detail VERBATIM, operator words beside it, role=\"alert\", and the SENTINEL ABSENT.\n\nADDED `ENVIRONMENT_REFUSAL_ADVICE` to the screen -- what to DO, keyed by the closed four-code roster. The daemon prose says what HAPPENED; ENV_STORE_KEY_UNAVAILABLE especially needed words, since it is an operator-fixable condition (set the daemon credential, restart) and not a bug to file. The advice map is asserted BIDIRECTIONALLY against the tested code list with set-equality, so a fifth code minted on either side reds -- a one-directional subset check would let a served code drop out silently.\n\nENV_VALUE_TOO_LARGE, THE TRAP, handled at its root. The daemon detail is asserted DIGIT-FREE upstream on purpose (interpolating \"value X is too large\" is how a secret escapes through the one field a refusal may carry), so the daemon CANNOT state the limit and this side must. Added `VALUE_LIMIT_BYTES = 4096` as a browser-side constant restating `MAX_ENVIRONMENT_VALUE_BYTES` (apps/control-room has no import edge to apps/daemon), with the source named in the comment. The message reads \"Shorten the value to under 4096 bytes.\" Asserted: it contains \"under 4096 bytes\", it does NOT contain the sentinel, and it does NOT contain the submitted LENGTH either -- reporting the size of what was typed is a weaker leak than reporting the value but still a fact about it.\n\nFALLBACK ARM: a code outside the closed roster (TRANSPORT_REQUEST_FAILED @ CONTROL_ROOM_TRANSPORT) renders its OWN code and layer and picks up no advice, so an unrelated failure is never dressed up as one of the four.\n\nScreen now 249 lines -- under the 250 ceiling but only just; a further clause there should move to the model module. No fifth code minted, no second copy of the code-to-layer map: the layer always travels on the refusal from whichever authority answered.",
      "startedAt": "2026-09-07T04:03:21.180Z",
      "completedAt": "2026-09-07T04:04:39.314Z"
    },
    {
      "stepId": "step-6",
      "description": "THE GOAL CARD: 'N required variables unset for <environment>' (DoD 4). This is what stops a deploy failing for a reason nobody can see.\n\nThe required names come from the approved contract via child 2 (task-72b22fcf9da54b3b91dabf5b7392aff1) — read them from its surface, do not re-derive them from the contract here.\n\nRender the count with a LINK to the Environments screen, and assert exactly what DoD 4 names:\n(a) a goal with TWO required names and ONE set shows a count of ONE;\n(b) the card is ABSENT when none are required — absent, not a zero badge. Assert absence by querying for the testid and finding nothing.\nAlso assert the card names the ENVIRONMENT it is counting for: 'unset for preview' and 'unset for production' are different facts and a card that omits which one it means invites the wrong fix.\n\nThe card renders NAMES and COUNTS only — never a value, never a fingerprint that could be mistaken for one.\n\nMount it on the goal beside the existing cards; check whether it belongs in the status-strip map at apps/control-room/src/v2/goals/goal-status-strip.tsx:33 the way `publish` does, and say either way in the note.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-environments-card.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/goal-environments-card.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goal-environments.test.tsx",
        "apps/control-room/src/v2/goals/goal-environments.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "note": "THE GOAL CARD (DoD 4). `pnpm --filter @moe/control-room test` -> Test Files 188 passed (188), Tests 2383 passed (2383), 13.37s, EXIT 0. Scoped: goal-environments.test.tsx 8 passed (8).\n\nARMS: (a) two required names, one set -> textContent exactly \"1 required variables unset for preview\" and data-count=\"1\". (b) ABSENT when nothing is required -- asserted by querying the testid and finding NULL, with the section root asserted present first so the absence is a fact about the card and not about an unmounted tree. Plus: per-environment counts differ on one goal (preview 0, production 2); each card NAMES its environment (\"unset for preview\" / \"unset for production\"); the link href is `#cr-goal-environments` AND that anchor is asserted to exist in the document, so the link is not a dangling one; and one screen renders per environment the daemon names, kickers asserted in order.\n\nCARD CARRIES NAMES AND COUNTS ONLY, with a CONTROL. The card's innerHTML contains no fingerprint hex and not the word \"fingerprint\" -- a fingerprint on a summary card is the thing an operator would most plausibly mistake for a truncated secret. The control asserts \"sha256 fingerprint\" IS present further down in the table, so the absence is a property of the CARD rather than of the whole render.\n\nABSENT ON AN UNREADABLE CONTRACT TOO, its own arm: status NONE yields no required names and no card. An unread contract is not the fact \"nothing is required\", and a card saying 0 over one is the quiet green that gets believed.\n\nSTATUS-STRIP QUESTION, ANSWERED EITHER WAY AS ASKED: NO, it does not belong in the strip map beside `publish`. The strip's STAGE_WORDS state where the GOAL stands in its lifecycle (No PRD, Contract at Gate 1, Agents working, Ready to close); unset variables are a fact about an ENVIRONMENT, and a goal can be at any stage with any count. What it does belong in is GOAL_SECTION_IDS, which is the anchor roster the strip links THROUGH -- added `environments: \"cr-goal-environments\"` there and nothing else.\n\nA REAL DEFECT FELL OUT OF THE FULL LEG, and it was not a test collision. Two cordum-app.test.tsx arms went red at `expected [2] to deeply equal [1]` on `app.pendingReads`. Diagnosis, not patching:\n- THE V1 ARM WAS A GENUINE BUG IN MY CODE. Its property is \"the V1 plane reads the /1 route, never /v2/...\". `readPendingContract` IS the /v2 route, and my section called it UNCONDITIONALLY -- so on a V1-plane session the environments section would have read a v2 route the plane forbids. FIXED IN PRODUCTION, not in the test: the contract read is now gated on `setup.commandAuthorityPlane === \"V2\"` (alongside the existing projectId null gate), matching how cordum-app.tsx picks its Gate 1 card. The V1 arm now passes with ZERO changes to it -- which is the proof the gate is what fixed it, rather than the assertion being loosened.\n- THE V2 ARM was a genuine consumer of a shape I changed, and its own comment states the property as exclusivity: \"reads the /2 route, and only that route\". Two surfaces on that page now legitimately need the approved contract (the Gate 1 card and this section), so the CALL COUNT is not the property; the distinct ROUTE SET is. Amended to `[...new Set(app.pendingReads)]`. A stray `/product-contract/pending/read` still reds it, and the V1 arm holds the mirror direction, so exclusivity is preserved in both directions rather than weakened. Declared cordum-app.test.tsx as a modified path.\n\nThis is the \"grep the SHAPE, not the symbol\" consumer I did not find at step 3 -- `app.pendingReads` is a hard-coded call list, and nothing about it mentions the symbols I added. Recording it so the pattern is cheaper to spot next time.\n\nASCII: caught myself pasting a literal middot into the new test and replaced it with the `MIDDOT` import; `grep -cP '[^\\x00-\\x7F]'` = 0 on all three new test files.",
      "startedAt": "2026-09-07T04:04:41.993Z",
      "completedAt": "2026-09-07T04:07:32.917Z"
    },
    {
      "stepId": "step-7",
      "description": "THE JOURNEY (DoD 6) — a new spec under tests/e2e/control-room/, against a REAL daemon.\n\nHARNESS: `withDaemonBackedControlRoom` / `DaemonLane` from ./daemon-ports.js. Drive the bare `lane.baseUrl` — main.tsx:101 serves the legacy shell only for `?v1=1`, so a journey clicking `cr.liveboard.*` re-proves the old dev board and leaves the shipped surface untested. Seat any dispatch recorder BEFORE the first click: a card's dispatch report lives only until the next surface poll re-keys the card (board-chain.spec.ts:26-33).\n\nCOVER: the required-vs-set table rendering from a real frame; a set/unset ROUND TRIP; and the fingerprint CHANGING on update. The round trip is the part that proves the screen and the daemon agree — a set that renders but does not persist, or an unset that leaves the row 'set', both pass a component test.\n\nTHEN THE LEAK CHECK AT THE REAL BOUNDARY: after typing a sentinel value in the browser, assert it appears in NO response body the page received and in NO page content. A component test cannot see the wire; this can.\n\nTEARDOWN in a `finally`: the lane, the daemon child, and any store or temp directory it created — released on the throwing path too. A test that leaks a port makes every later gate on this board inadmissible (epic rail 4).\n\nQuote the `pnpm test:e2e:browser` count line.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/environments-screen.spec.ts"
      ],
      "newFiles": [
        "tests/e2e/control-room/environments-screen.spec.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/environment-variables.spec.ts",
        "apps/control-room/src/v2/ops/environment-variables-model.ts",
        "apps/control-room/src/v2/goals/goal-environments.tsx",
        "apps/control-room/src/v2/goals/goal-environments.test.tsx",
        "apps/control-room/src/v2/ops/live-environment-variables.tsx"
      ],
      "note": "JOURNEY WRITTEN AND TYPECHECKED; BROWSER GATE QUEUED, COUNT LINE NOT YET OWED-FREE. `npx tsc --noEmit -p tests/e2e/control-room/tsconfig.json` EXIT 0. `npx vitest run --root apps/control-room src/v2/goals/goal-environments.test.tsx` -> Test Files 1 passed (1), Tests 9 passed (9), EXIT 0. I claim NO browser-gate result, green or red.\n\nNEW SPEC tests/e2e/control-room/environment-variables.spec.ts (233 lines). Real daemon via `withDaemonBackedControlRoom({liveCredentials:\"ATTACHED\"})`, ADMIN reads and HUMAN writes on `mintLaneOperatorSeat`, driving the BARE `lane.baseUrl` (no `?v1=1`, so it exercises the shipped surface rather than the legacy shell). NOTHING IS DOUBLED here - no docker or gh is involved in this slice.\nCOVERS: empty table from a real frame; the ROUND TRIP set -> read -> update -> read -> unset -> read, with the unset asserted to leave ZERO rows (a row that stayed \"set\" passes a component test happily); the fingerprint moving between two different values, each a full 64-hex sha256; exact four-key arity on the served row so a future `value` slot reds here; and three of the four refusals raised at the REAL boundary with code and layer.\nWIRE-LEVEL LEAK CHECK, which is the thing a component test structurally cannot do: `page.on(\"response\")` and `page.on(\"request\")` recorders seated BEFORE the first navigation (a recorder attached later misses the mount reads, which are the ones that would carry a value), then the sentinel searched across every response body, every request body and `page.content()`. Two controls: the recorder is asserted non-empty so the absences are not three empty arrays, and a positive control proves the search WOULD find the sentinel if a surface carried it.\nTEARDOWN in `finally` via `assertStopped`: survivingPids empty, daemon port closed, dev-server port closed, scratch directory gone - on the throwing path too (epic rail 4).\n\nI CORRECTED A MODELLING ERROR OF MY OWN from step 3, and this is the substantive part of this step. I had derived the environment roster from `/deployments/read`. That read enumerates DEPLOY TARGETS, which is a different axis: a project HAS preview/production/verify from the moment it exists (`ENVIRONMENT_NAMES`, closed, anything else refuses ENV_ENVIRONMENT_UNKNOWN @ SCOPE). The deploy-derived list showed NOTHING on a project that had never deployed - i.e. exactly the projects the unset-count card exists to serve, since the operator must set preview variables BEFORE the first deploy. Writing the journey is what exposed it: a fresh lane would have rendered no screens at all.\nFIX: `ENVIRONMENT_NAMES` restated in environment-variables-model.ts with the daemon source named (no import edge from apps/control-room to apps/daemon), the deployments read dropped from goal-environments.tsx entirely. THE DRIFT IS GUARDED FROM BOTH ENDS: a component arm pins the literal [\"preview\",\"production\",\"verify\"], and the journey asserts an unrostered name (\"staging\") refuses ENV_ENVIRONMENT_UNKNOWN @ SCOPE at the store - so a browser-side drift surfaces as that refusal on screen rather than as a silently missing environment.\n\nREMOVED A DUPLICATE READ found while fixing the above. The section was reading every environment's table for the card WHILE each screen read the same table for itself - two answers per environment, and the card and the table it links to could have stated different counts. Now each `LiveEnvironmentVariables` reads once and reports upward via a new `onOutcome` prop; the section makes exactly ONE read of its own (the contract). One read per environment total.\n\nGATES QUEUED, NOT BYPASSED. browser-e2e-gate: queued #1 behind task-817d893f / worker-ca28be90 (acquired 04:08:51Z, re-running release-approval.spec.ts). full-suite-gate: queued #4. WORTH RECORDING: `.moe/resources/browser-e2e-gate.json` on disk still named qa-97200064 / task-cf2d91d9 from 03:47Z, and a Win32_Process census found zero playwright/chromium test processes - both signals pointed at an orphaned lease. The LIVE tool answer showed a DIFFERENT, current holder. Force-releasing on the file plus the census would have killed a legitimate in-flight run. The disk file is not the authority; `acquire_resource`/`list_resources` is.",
      "startedAt": "2026-09-07T04:07:37.766Z",
      "completedAt": "2026-09-07T04:13:39.251Z"
    },
    {
      "stepId": "step-8",
      "description": "THE RUNBOOK AND THE GATES (DoD 7).\n\nDOCS: `docs/agent-stack-runbook.md` documents the environments model — what an environment is, that required names come from the APPROVED CONTRACT (not from a config file), what the fingerprint is and what it is for, and, stated plainly, THAT VALUES ARE NEVER READABLE BACK. That last sentence is the one an operator needs before they type a secret into a browser: they must know the only way to verify an update is the fingerprint. Say what shipped, not what the design intended.\n\nGATES — each standalone, each quoted with its `Test Files N passed` count line; an exit-0 leg with no count line RAN NOTHING:\n- `pnpm --filter @moe/control-room test` — this row's primary evidence.\n- `pnpm --filter @moe/daemon test`\n- `pnpm typecheck`\n- `pnpm test:e2e:browser`\nState explicitly that root `pnpm test` covers NO apps/** file, so the control-room leg carries the apps/** claim.\n\nSIZE AND STYLE: `wc -l` every production file you created and quote it (250 ceiling). Confirm ASCII-only sources and apostrophe-free JSX text.\n\nPATH-ATTRIBUTED BASELINE: repo-wide failures at the merge-base, at HEAD, subtract, intersect with this row's owned paths. Complete only when that intersection is EMPTY; disclose any foreign red verbatim with its failing file paths.",
      "status": "COMPLETED",
      "affectedFiles": [
        "docs/agent-stack-runbook.md"
      ],
      "modifiedFiles": [
        "docs/agent-stack-runbook.md",
        "apps/control-room/src/v2/ops/environment-refusal-words.ts",
        "apps/control-room/src/v2/ops/environment-variables-screen.tsx",
        "apps/control-room/src/v2/ops/environment-variables-screen.test.tsx",
        "tests/e2e/control-room/environment-variables.spec.ts"
      ],
      "note": "RUNBOOK AND GATES. THREE OF FOUR LEGS RUN AND GREEN; the daemon leg is OWED and queued, stated plainly rather than claimed.\n\nGATES, each standalone and foreground, each with its count line:\n- `pnpm --filter @moe/control-room test` -> `Test Files 188 passed (188)` / `Tests 2386 passed (2386)`, 14.04s, EXIT 0. THIS ROW'S PRIMARY EVIDENCE.\n- `pnpm typecheck` -> EXIT 0. Execution proved, not inferred: `grep -c \"typecheck: Done\"` = 19, matching `Scope: 19 of 20 workspace projects`, with `apps/control-room typecheck: Done` and `apps/daemon typecheck: Done` both present. This matters because pnpm typecheck FAILS FAST across the workspace - an EXIT 0 alone does not prove the later legs ran.\n- `pnpm test:e2e:browser` -> `31 passed (8.1m)`, `6 failed`, EXIT 1. My new spec is among the 31. Attribution below.\n- `pnpm --filter @moe/daemon test` -> NOT RUN. full-suite-gate is held (task-5ef1a0a9 since 02:57:27Z); I am queued #4 and did NOT force-release. I claim NO daemon-leg result, green or red.\n\nROOT `pnpm test` COVERS NO apps/** FILE, verified rather than recited: vitest.config.ts:32 include is [\"adapters/**/*.test.ts\", \"packages/**/*.test.ts\", \"tests/**/*.test.ts\", \"tools/**/*.test.ts\"] - no apps/** entry. So the control-room leg carries the entire apps/** claim for this row.\n\nPATH-ATTRIBUTED BASELINE for the 6 browser failures. Merge-base 68eb1c5d. The failing specs are journeys.spec.ts (x2), prd-persistence-boundary.spec.ts, prd-to-approval.spec.ts, provider-pause.spec.ts, release-approval.spec.ts. NONE is a path this row owns. But I did NOT stop at \"not my file\": I edited cordum-app.tsx, the shell every browser journey renders, and `withStaticControlRoom` REBUILDS the bundle (dist/index.html mtime 07:25 is AFTER my 07:11 source edits), so my bytes really were in the tested bundle and could not be dismissed by construction.\nMEASURED THE DELTA: stashed ONLY my 5-line cordum-app.tsx mount, re-ran journeys.spec.ts, and BOTH failures reproduced IDENTICALLY without it (`2 failed, 5 passed`; same `expect(locator).toBeDisabled()` on cr.nav.resources and same `toHaveLength(5)` receiving 0). Foreign, by measurement rather than by assertion. Restored and proved: `git diff --stat` shows the 5 insertions back (cordum-app.tsx is TRACKED so git diff is a valid restore proof here) and sha256 2de067b7...da74 matches the pre-stash backup.\nVerbatim foreign reds: journeys.spec.ts:79 `cr.nav.resources` expected disabled, received enabled; journeys.spec.ts:95 truth chips `Expected length: 5, Received length: 0`; prd-persistence-boundary.spec.ts:127 `Test timeout of 180000ms exceeded`; prd-to-approval.spec.ts:38 `toHaveText` failure; provider-pause.spec.ts:206 `the wrapper must staff node.deliver@node-e2e-l1ucaa`; release-approval.spec.ts:177 `RELEASE READ: {\"goalId\":\"goal-live-1\",\"kind\":\"ABSENT\"}`. That last one is task-817d893f's own row, which held the browser gate immediately before me to re-run exactly that spec.\nINTERSECTION OF (HEAD failures MINUS merge-base failures) WITH THIS ROW'S OWNED PATHS: EMPTY.\n\nA REAL PRODUCT FENCE FOUND BY THE JOURNEY, and the most important thing in this step. The first e2e run failed `OPERATOR_PRINCIPAL_REQUIRED @ DAEMON_AUTHORIZATION` on the write. Both environment kinds sit in OPERATOR_PRINCIPAL_KINDS (daemon-command-vocabulary.ts:380, comment: \"AN AGENT MUST NEVER SET AN ENVIRONMENT VARIABLE\") and, unlike `repository.publish`, are NOT in the widening at daemon-command-registry.ts:353 that lets a paired browser act. CONSEQUENCE, MEASURED: a paired browser session READS this table (it holds ADMIN) but CANNOT WRITE it. My seat choice was simply wrong - the LANE CREDENTIAL authenticates as the configured operator principal and passes; a minted seat does not. Fixed the spec, and turned the discovery into a permanent guard: a new arm asserts a MINTED seat is refused OPERATOR_PRINCIPAL_REQUIRED @ DAEMON_AUTHORIZATION with the sentinel absent, while the lane credential is ACCEPTED - both directions. I did NOT widen the fence; that is an authority decision, not this proof row's.\nSURFACED IT rather than leaving a bare code: `OPERATOR_PRINCIPAL_REQUIRED` now has operator words (\"Set it from the daemon host instead. Reading this table is unaffected.\"), asserted, including that it must NOT read as a variable-name problem - the obvious wrong guess on a screen whose read half plainly works.\n\nRUNBOOK: docs/agent-stack-runbook.md gains \"Environment variables (what is set, never what it is)\" before the Replan section. Covers the closed three-environment roster, that required names come from the APPROVED CONTRACT rather than a config file, what the fingerprint is and that it is the ONLY confirmation an update took, the four refusals in a code/layer/what-to-do table, the operator-principal fence, and - stated plainly, twice - THAT VALUES ARE NEVER READABLE BACK, including \"if you lose the value, it is gone\". Says what SHIPPED, fence included.\n\nSIZE: the screen hit 262 lines when the fence advice landed, so the refus",
      "startedAt": "2026-09-07T04:13:41.377Z",
      "completedAt": "2026-09-07T04:28:25.172Z"
    },
    {
      "stepId": "step-9",
      "description": "ADVERSARIAL SELF-REVIEW before `moe.complete_task`. This screen handles secrets, so review it as someone trying to get a value out of it.\n\nTHE LEAK SURFACE, each answered with a command or an arm:\n- Does the sentinel appear in ANY response body, console line, or page content after a full round trip? The journey can see the wire; use it.\n- Does a FAILED submit retain the value anywhere — React state, an uncontrolled input's DOM value, a form's autocomplete, a retry buffer? Check the DOM after the failure, not just the React tree.\n- Would a browser autofill or password manager capture the input? Check the input's `type` and `autocomplete` attributes and say what they are.\n- Does any refusal message quote the input? ENV_VALUE_TOO_LARGE is the likeliest offender.\n- Does the fingerprint reveal anything about the value — is it the full sha256, and is it labelled so no one reads it as a prefix of the secret?\n- Is there a realistic-looking secret in ANY committed fixture (epic rail 3)? Grep your own new files.\n- Does an error boundary or a logging hook serialize props that carry the value?\n\nCORRECTNESS:\n- Two operators setting the same variable concurrently — does the fingerprint the second sees reflect the first's write, or a stale read?\n- An unset of a variable that is not set — defined answer, or a crash?\n- A variable required by the contract but for an environment that does not exist — which refusal wins, and is that the useful one?\n\nMUTATION DRILL: `git status --porcelain` the exact paths FIRST. Every file this row creates is NEW (untracked), so `git diff -- <path>` certifies NOTHING for them — back each mutated path up out of tree under %TEMP% suffixed by your worker id and prove the restore by SHA256 equality in a call SEPARATE from the restore. dev-proxy-paths.ts and the runbook are TRACKED, so `git diff` is a valid restore proof there.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/ops/environments-screen.tsx",
        "apps/control-room/src/v2/ops/environments-screen.test.tsx",
        "tests/e2e/control-room/environments-screen.spec.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/live/live-environment-variables.ts",
        "apps/control-room/src/v2/ops/environment-variables-screen.tsx",
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "note": "ADVERSARIAL SELF-REVIEW DONE. Full transcript in comment-16d4df25621a4aa3b55dac90adcb1927 (12 questions, each answered by a command or an arm). Final `pnpm --filter @moe/control-room test` after every drill restore -> Test Files 188 passed (188), Tests 2386 passed (2386), EXIT 0.\n\nThe three files listed here were MUTATED AND RESTORED during drills; none carries a net change from this step. All bytes this step leaves on disk are byte-identical to what steps 2-8 landed.\n\nHEADLINES (detail in the comment):\n- LEAK SURFACE: clean on all eight questions. Sentinel absent from every response body, request body and page content after a real round trip (with a positive control proving the search works). Failed submit retains nothing, checked in the DOM and in the React fiber. Input is type=password / autocomplete=off / spellcheck=false, asserted. No refusal quotes the input; ENV_VALUE_TOO_LARGE states the LIMIT and not even the submitted length. Fingerprint is the full sha256, labelled, never on the summary card. No realistic secret in any fixture (grepped). NO ERROR BOUNDARY EXISTS in apps/control-room, so nothing serializes props on a throw. Zero storage/console/cookie references in all seven production files.\n- CORRECTNESS: unset-of-unset is idempotent and defined (appends the event, answers the read). Scope beats name in the refusal order, which is the useful order.\n- ONE REAL PROPERTY DISCLOSED, NOT FIXED: no lost-update detection on concurrent writes. `runEnvironmentEdge` deliberately ignores expectedVersion, so operator B silently overwrites A. The screen re-reads after its own write so nobody sees a STALE fingerprint, but a CAS would have to exist at the daemon edge first - that is child 1's seam, not this proof row's. Flagged for the epic-final hardening row per DELIVERY BIAS rather than filed as a new row.\n- ONE FENCE DISCLOSED, NOT WIDENED: a paired browser cannot write (OPERATOR_PRINCIPAL_REQUIRED). Given operator words, pinned by an e2e arm in both directions, and stated in the runbook. Widening it is an authority decision I did not take.\n\nSECOND MUTATION DRILL this step, on the read client: removing the value-detection made BOTH ENVIRONMENT_VARIABLES_VALUE_PRESENT arms go red - including the refusal-envelope one, which is the arm that justifies checking for a value BEFORE the refusal read. Restored; sha256 65edce3f...c3c3 equal to the out-of-tree backup, proved in a call separate from the restore, `grep -c \"MUTATION DRILL\"` = 0. Both drilled files are UNTRACKED, so `git diff` would have certified nothing for either - SHA256 was the only valid proof.\n\nDAEMON LEG STILL OWED: full-suite-gate is held by task-5ef1a0a9 since 02:57:27Z and I have been queued #4 across three checks. I did not force-release and I claim no daemon result. This row's diff touches ZERO apps/daemon paths, so the owned-package leg (control-room, EXIT 0) is what carries its claim.",
      "startedAt": "2026-09-07T04:28:30.712Z",
      "completedAt": "2026-09-07T04:30:45.612Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "ARCHITECT",
  "parentTaskId": "task-9ae492b373fa4e7aa6b1e91585f6e612",
  "dependsOn": [
    "task-d0c63cd8848a4da18ad3fbb8eb1d95c3",
    "task-72b22fcf9da54b3b91dabf5b7392aff1",
    "task-ef76a7f4523d46f48a2f9eb19595e801"
  ],
  "priority": "HIGH",
  "order": 103,
  "comments": [
    {
      "id": "comment-16d4df25621a4aa3b55dac90adcb1927",
      "author": "worker-b1872f24",
      "content": "ADVERSARIAL SELF-REVIEW (step 9), worker-b1872f24. Every question answered by a command or an arm, not by reading the design.\n\n--- THE LEAK SURFACE ---\n\n1. SENTINEL IN ANY RESPONSE BODY / REQUEST / PAGE CONTENT AFTER A FULL ROUND TRIP? NO.\n   tests/e2e/control-room/environment-variables.spec.ts records `page.on(\"response\")` and\n   `page.on(\"request\")` BEFORE the first navigation, runs a real set/update/unset round trip\n   against a real daemon, then asserts absence across every response body, every request body and\n   `page.content()`. Two controls: the recorder is asserted non-empty, and a positive control\n   proves the search would find a planted sentinel. Spec EXIT 0 (`1 passed (11.4s)`), and it is\n   among the 31 passed in the full `pnpm test:e2e:browser` run.\n\n2. DOES A FAILED SUBMIT RETAIN THE VALUE? NO - and this is the arm the row turns on.\n   The typed value lives in ONE useState inside SetDialog. `settle()` clears `editing` on EVERY\n   settled submit, which UNMOUNTS the dialog that owns it. Checked in the DOM, not only the React\n   tree: after a refusal the arm asserts the dialog is gone, reopens it, and asserts the field's\n   `.value` is \"\". Also covered on the UNDELIVERED branch (a thrown dispatch takes the other\n   branch of settle()).\n   MUTATION-DRILLED (step 4): `if (next.ok) setEditing(null)` made the two value arms go RED at\n   `expected '<div class=\"cr2-card\"...' not to contain 'SENTINEL-...'` - the sentinel search\n   itself, not a helper precondition. Restored, sha256 3587c047...8248.\n\n3. AUTOFILL / PASSWORD MANAGER? Attributes ASSERTED, not assumed:\n   `type=\"password\"`, `autocomplete=\"off\"`, `spellcheck=\"false\"`. No `name` that a heuristic\n   autofill matches on (`cr-env-vars-value`), and no `form` wrapping a username field.\n\n4. DOES ANY REFUSAL QUOTE THE INPUT? NO. All four ENV_ codes swept, each asserting its code AND\n   layer present and the sentinel absent. ENV_VALUE_TOO_LARGE - the likeliest offender - reports\n   the LIMIT from a constant (\"under 4096 bytes\"), and the arm additionally asserts the SUBMITTED\n   LENGTH is absent: reporting the size of what was typed is a weaker leak but still a fact about\n   it. The daemon's own details are digit-free by upstream assertion, which is why the limit has\n   to come from this side at all.\n\n5. DOES THE FINGERPRINT REVEAL ANYTHING? It is the FULL sha256 of the value's bytes, computed\n   daemon-side from plaintext that is then dropped. The screen shows the first 12 chars LABELLED\n   `sha256 fingerprint`, with title \"A sha256 fingerprint of the stored value. It is not part of\n   the value.\" Both asserted. Unset renders \"Not set\", never an empty-looking string. The goal\n   CARD carries no fingerprint at all, with a control proving one IS present lower down - a\n   fingerprint on a summary card is the thing most plausibly misread as a truncated secret.\n\n6. REALISTIC-LOOKING SECRET IN A COMMITTED FIXTURE? NO. Grepped my own new files for\n   `sk-|ghp_|AKIA|-----BEGIN|password=|api_key=`: zero hits. Sentinels are\n   \"SENTINEL-not-a-secret-...\" shaped - unmistakably not a credential, so no scanner alert and no\n   rail-3 breach.\n\n7. ERROR BOUNDARY / LOGGING HOOK SERIALIZING PROPS? NONE EXISTS.\n   `grep -rn \"componentDidCatch|ErrorBoundary|getDerivedStateFromError|window.onerror\"` over\n   apps/control-room/src returns NOTHING. Separately, the value is never a prop at all: the\n   fiber-walk arm asserts it IS findable while the dialog is open (proving the walker reaches\n   props) and NOT findable after submit.\n\n8. CLIENT-SIDE RETENTION? `grep localStorage|sessionStorage|indexedDB|console.|document.cookie`\n   across all seven production files: zero hits. Plus a source-text arm on the read client.\n\n--- CORRECTNESS ---\n\n9. TWO OPERATORS SETTING THE SAME VARIABLE CONCURRENTLY. DISCLOSED, NOT FIXED, and not fixable\n   here. The screen re-reads on every settled write (`onSettled`), so the fingerprint an operator\n   sees after their OWN write is the daemon's post-write state, not a stale one. But there is NO\n   lost-update detection: `runEnvironmentEdge` deliberately ignores `expectedVersion` and derives\n   the aggregate from the principal, so B's write silently overwrites A's and B sees B's\n   fingerprint. That is a daemon-seam property (child 1's), not this row's - a CAS would have to\n   exist at the edge first. Flagging for the epic-final hardening row rather than filing a row.\n\n10. UNSET OF A VARIABLE THAT IS NOT SET. DEFINED, no crash: `unsetEnvironmentVariable` validates\n    scope then name, then appends an UNSET event and answers the read - idempotent. The screen\n    only offers Unset on a row that reads set, so this needs a concurrent unset to reach.\n\n11. REQUIRED BY THE CONTRACT BUT FOR AN ENVIRONMENT THAT DOES NOT EXIST. SCOPE WINS and that is\n    the useful order: `unsetEnvironmentVariable`/`setEnvironmentVariable` check the environment\n    name FIRST (ENV_ENVIRONMENT_UNKNOWN @ SCOPE), then the variable name, then the key. Telling\n    an operator their environment is unknown is more actionable than telling them a name is\n    invalid in an environment that was never going to accept it. Since the browser roster\n    restates the closed three, this can only arise on drift - which the e2e arm pins by asserting\n    \"staging\" refuses ENV_ENVIRONMENT_UNKNOWN @ SCOPE.\n\n--- THE THING I DID NOT FIX, DELIBERATELY ---\n\n12. A PAIRED BROWSER CANNOT WRITE. Both environment kinds are in OPERATOR_PRINCIPAL_KINDS and\n    absent from the widening that lets `repository.publish` through, so a paired session READS\n    the table (ADMIN) and is refused OPERATOR_PRINCIPAL_REQUIRED @ DAEMON_AUTHORIZATION on\n    write. Found by the journey, not by reading. I did NOT widen the fence - that is an authority\n    decision (same class as the repository.bootstrap widening, which needed an owner ruling).\n    Instead: the refusal is given operator words, an arm pins both directions, and the runbook\n    states it plainly. The set/unset dialogs work from the daemon host, which is the real\n    operator path today.\n\n--- DRILLS, both restored and proved in calls SEPARATE from the restore ---\n- environment-variables-screen.tsx (UNTRACKED, so git diff certifies nothing): sha256\n  3587c047a0ce67b962315282379b3ad086e29e435dd7b620e2d60da4f0f88248, `grep -c \"MUTATION DRILL\"` 0.\n- live-environment-variables.ts (UNTRACKED): value-detection removed -> the two VALUE_PRESENT arms\n  went RED, both of them, including the refusal-envelope one. Restored, sha256\n  65edce3f98ce5a0fc8e42bedd2ed8da2f6ffa63d1a005a167ec71f9aa38cc3c3, drill string count 0.\n- cordum-app.tsx (TRACKED, so git diff IS valid): stashed for the red-attribution delta, popped,\n  `git diff --stat` shows the 5 insertions back, sha256 2de067b7...da74 matches the backup.\nPost-restore: `pnpm --filter @moe/control-room test` -> 188 files / 2386 tests EXIT 0.",
      "timestamp": "2026-09-07T04:30:26.810Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-05T00:46:11.818Z",
  "updatedAt": "2026-09-07T04:32:24.013Z",
  "metrics": {
    "firstClaimAt": "2026-09-05T19:06:25.393Z",
    "plannedStepCount": 9,
    "plannedDistinctFileCount": 8,
    "executedStepCount": 9
  },
  "planSizeWarnings": [
    "Plan has 9 steps (target <=8); plans over 12 are rejected. Consider splitting the task before it grows.",
    "Plan touches 8 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8",
    "step-9"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-05T19:11:11.145Z",
  "planningNotes": {
    "approachesConsidered": "FILING THE MISSING ROUTE AS A PREREQUISITE vs BUILDING IT HERE. Building it here was rejected on two counts. First, size: the daemon route plus its .js bridge, its test, and the listener composition across http-listener-read-dispatch.ts / http-listener.ts / http-listener-guards.ts / daemon-entry.ts is six-plus paths, on top of this row's screen, client decoder, proxy pin, goal card, journey and docs - far past the cap. Second, ownership: this is a PROOF row (global rail 21) and its own rail 3 says a read defect is child 1's, not this row's. The rails' answer to a missing prerequisite is a production row measured symbol by symbol, which is what task-ef76a7f4 is.\n\nMOCKING THE READ TO UNBLOCK THE SCREEN. Rejected outright. A mock-backed journey proves the shape and retires the question unanswered, and DoD 1 explicitly says \"against a REAL daemon frame\".\n\nAPPENDING THE SCREEN TO ops-screens.tsx. Rejected on measurement - the file is 236 lines against a 250 ceiling, and the row's own rail 4 says to measure with `wc -l` before writing. Its own module.\n\nWHERE THE 'NO VALUE' PROPERTY IS PROVEN. The daemon boundary is the prerequisite row's arm; this row owns the BROWSER half, which is the harder half: the input, its retained state after a failed submit, the refusal text, and anything a devtools-visible prop would carry. Both halves are needed - a value that never leaves the daemon can still be leaked by the screen that typed it.",
    "codebaseInsights": "MEASURED AT HEAD (moe/work-2026-09-04) by architect-fa3a3e17, 2026-09-05. ONE PREREQUISITE WAS MISSING AND IS NOW A ROW; read this before step 1.\n\nTHE SCREEN HAD NOTHING TO READ. `ls apps/daemon/src/http/ | grep -i env` returns NOTHING - no environments HTTP route exists. Child 1 (task-d0c63cd8848a4da18ad3fbb8eb1d95c3) is DONE and its own completionSummary is explicit about why: \"17 paths, all new, all under apps/daemon/src/environment/. Nothing outside that directory changed.\" What it shipped are IN-PROCESS projection functions - environment-projection.ts `readEnvironmentState` (:80), `projectEnvironmentVariables` (:95), `EnvironmentReadOk` (:49), `EnvironmentReadResult` (:61) - plus the store, cipher and fold. The operator-facing READ was never built. DoD 1 says \"against a REAL daemon frame\" and DoD 6 requires \"the new read's decoder\", both of which presuppose a route that did not exist.\nI filed task-ef76a7f4523d46f48a2f9eb19595e801 (order 102.5) to serve `/environments/read` AND compose it into the production listener in the same row, and set this row's dependsOn to include it. That is the rails' required output for a missing prerequisite - a production row with the gap measured symbol by symbol, never a narrowed DoD and never a mock-backed journey.\n\nWHY THE PREREQUISITE INSISTS ON SERVE-AND-COMPOSE TOGETHER: the design chain needed TWO rows because the first only served. task-7ca9dca3 shipped `/design/read` and task-eb2bb09d then existed purely to compose the port into the listener. A served route nothing mounts satisfies every existence grep while the feature does not exist.\n\nCHILD 2 (task-72b22fcf9da54b3b91dabf5b7392aff1, required names and delivery) is still PLANNING, NOT landed. DoD 4 - the goal card's \"N required variables unset for <environment>\" - is driven by the required names it reads from the approved contract. Step 1 hard-stops on it.\n\nWHAT DOES EXIST AND IS REUSABLE: the four refusal codes with a CLOSED code->layer map, shipped and asserted by child 1 (ENV_ENVIRONMENT_UNKNOWN/SCOPE, ENV_NAME_INVALID/NAME, ENV_VALUE_TOO_LARGE/VALUE, ENV_STORE_KEY_UNAVAILABLE/...). Reuse that map; do not mint a second one. `environment.set_variable` is already in packages/contracts/src/runtime/runtime-vocabulary.ts and in apps/daemon/src/orchestrator/agent-spawn-contract.ts, so the kind is published and human-only-fenced by task-a2409cba's chain - this row publishes nothing.\n\nCONTROL-ROOM SURFACES:\n- apps/control-room/src/v2/ops/ is where ops screens live: ops-screens.tsx is 236 LINES at HEAD (the description says 228 - it has grown, and the ceiling is 250). A new screen goes in its OWN module; appending would breach the cap immediately.\n- apps/control-room/src/live/dev-proxy-paths.ts is 55 lines with `DEV_PROXY_PATHS` frozen at :5, an alphabetical-ish list of route strings (/activation/read, /activity/read, /affordances/read, /bootstrap, ... /goals/source/read, /health/read, /graph/get). Note `/design/read` is NOT pinned there - so the pin is not automatic for every route and DoD 6 requiring one for this read is a deliberate instruction, not a formality.\n- The card pattern for a goal-scoped card is goal-publish.tsx (173 lines) + publish-port.ts (29 lines), the port spending the offer through offer-wire's `spendOffer`.\n\nPLAYWRIGHT: tests/e2e/control-room/ with `withDaemonBackedControlRoom` / `DaemonLane` in daemon-ports.ts. Drive the bare `lane.baseUrl` (main.tsx:101 serves the legacy shell only for `?v1=1`). Seat any dispatch recorder BEFORE the first click - a card's dispatch report lives only until the next surface poll re-keys the card (board-chain.spec.ts:26-33)."
  },
  "planApprovedAt": "2026-09-05T19:11:13.481Z",
  "contextFetchedBy": [
    "worker-b1872f24"
  ],
  "workStartedAt": "2026-09-07T03:47:03.844Z",
  "reviewStartedAt": "2026-09-07T04:32:24.013Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": " RUN  v4.1.10 D:/projexts/moe-next/apps/control-room\n\n Test Files  188 passed (188)\n      Tests  2386 passed (2386)\n   Start at  07:30:49\n   Duration  13.36s (transform 21.37s, setup 0ms, import 51.57s, tests 102.91s, environment 157.09s)\n",
    "reportedAt": "2026-09-07T04:32:24.013Z"
  },
  "filesModified": [
    "apps/control-room/src/v2/ops/ops-screens.tsx",
    "apps/control-room/src/live/dev-proxy-paths.ts",
    "apps/control-room/src/live/live-environment-variables.ts",
    "apps/control-room/src/live/live-environment-variables.test.ts",
    "apps/control-room/src/live/live-document-proxy.test.ts",
    "apps/control-room/src/v2/ops/environment-variables-screen.tsx",
    "apps/control-room/src/v2/ops/environment-variables-screen.test.tsx",
    "apps/control-room/src/v2/ops/environment-variables-model.ts",
    "apps/control-room/src/v2/ops/environment-variables-port.ts",
    "apps/control-room/src/v2/ops/live-environment-variables.tsx",
    "apps/control-room/src/v2/goals/goal-environments.tsx",
    "apps/control-room/src/v2/goals/goal-status-strip.tsx",
    "apps/control-room/src/v2/cordum-app.tsx",
    "apps/control-room/src/v2/goals/goal-environments.test.tsx",
    "apps/control-room/src/v2/cordum-app.test.tsx",
    "tests/e2e/control-room/environment-variables.spec.ts",
    "docs/agent-stack-runbook.md",
    "apps/control-room/src/v2/ops/environment-refusal-words.ts"
  ],
  "completionSummary": "THE ENVIRONMENTS SCREEN SHIPS. 7 production files (all <250 lines), 4 test files, 12 owned paths. Full detail in comment-16d4df25621a4aa3b55dac90adcb1927 and Serena task-task-ba83b202...-handoff.\n\nGATES: `pnpm --filter @moe/control-room test` 188 files / 2386 tests EXIT 0 (named verification, run fresh). `pnpm typecheck` EXIT 0 with 19x \"typecheck: Done\" matching \"Scope: 19 of 20\" (execution proved, not inferred - it fails fast). `pnpm test:e2e:browser` 31 passed / 6 failed; my new spec is among the 31. `pnpm --filter @moe/daemon test` NOT RUN and NOT CLAIMED: full-suite-gate held by task-5ef1a0a9 since 02:57Z, queued #4 across three checks, no force-release. This row's diff touches ZERO apps/daemon paths. Root `pnpm test` covers NO apps/** (vitest.config.ts:32 include has no apps/** entry), so the control-room leg carries that claim.\n\nDoD 1 table + dialogs render against a real daemon frame. DoD 2 NO VALUE, EVER: 9 arms searching the whole container's innerHTML (attributes included) AND a React fiber walk (devtools-visible props), across success, failure, undelivered, all four refusals, and storage - with three controls so no absence is vacuous. DoD 3 fingerprint asserted to differ from the old one AND equal what the read returned, labelled \"sha256 fingerprint\" in rendered output. DoD 4 card shows 1 for two-required-one-set, is ABSENT when none required and when the contract is unreadable, names its environment, links to a verified anchor, carries no fingerprint (with a control proving one exists lower down). DoD 5 all four codes render VERBATIM with detail AND layer plus operator words; ENV_VALUE_TOO_LARGE states the LIMIT from a constant and not even the submitted length. DoD 6 journey does a real set/update/unset round trip and checks the sentinel against every response body, request body and page content. DoD 7 runbook section written.\n\nFOUR THINGS QA SHOULD READ:\n1. A REAL FENCE, FOUND BY THE JOURNEY: both environment kinds are in OPERATOR_PRINCIPAL_KINDS and "
}