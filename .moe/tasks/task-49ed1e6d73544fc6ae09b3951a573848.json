{
  "id": "task-49ed1e6d73544fc6ae09b3951a573848",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Foundation MCP dispatch host",
  "description": "Objective: land the missing real daemon process/transport consumer so Foundation self-hosting does not inject a fake StdioDispatchPort/HttpDispatchPort in its E2E harness. Deliverable: a bounded daemon host executable that opens the project store, composes authenticated @moe/mcp stdio and Streamable HTTP dispatch ports with the published @moe/daemon Foundation ingress surface, routes coordination/session traffic, supports graceful drain/restart, and emits readiness/shutdown receipts used by J3. NOT in scope: custom WebSocket MCP, control-room UI, multi-node activation, release publication, or canary assertions. Depends on Foundation daemon ingress surface. Owned paths: apps/daemon/src/host/foundation-host.ts/.js, foundation-host.test.ts, and apps/daemon/package.json/pnpm-lock.yaml only for @moe/mcp; add a bin/script only if needed for the executable. Verification: pnpm --filter @moe/mcp typecheck && pnpm --filter @moe/mcp test && pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck plus a plain-Node stdio smoke.",
  "definitionOfDone": [
    "The executable composes real @moe/mcp dispatch with @moe/daemon/store/session/coordination surfaces; no injected test dispatcher or deep import decides auth, command, or durable outcomes.",
    "Official MCP stdio and Streamable HTTP requests reach the same bounded authenticated command path; malformed/oversized/forged/stale input asserts exact transport and daemon reason codes/layers and commits nothing.",
    "The host publishes deterministic readiness, process identity, project/store identity, and graceful-shutdown receipts; restart reopens the same durable state and drives production reconciliation before accepting commands.",
    "Concurrent duplicate command delivery leaves one durable decision and one authority holder; advisory session/terminal text never invokes command authority.",
    "Plain-Node stdio smoke plus MCP/daemon/repo gates exit 0; only explicit owned paths and the exact @moe/mcp manifest addition are committed."
  ],
  "taskRails": [
    "TDD every behavior change; every refusal test pins the exact stable reason code and refusing layer, and generated sweeps assert a positive case count.",
    "Use only public package roots across package boundaries; no deep imports, test-owned authority, mock-backed acceptance, or optimistic UNKNOWN upgrades.",
    "Keep production files focused, target <=250 physical lines and split before 400; stage and commit only explicit owned paths.",
    "Only official MCP stdio and Streamable HTTP are permitted; do not add a custom MCP-over-WebSocket protocol."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "TDD the receipts (DoD 3), which do not exist anywhere: `grep -rn \"readinessReceipt|shutdownReceipt\" apps/daemon/src` returns ZERO at HEAD 1aa7cdc; readiness today is a log line. Write `apps/daemon/src/host/foundation-receipts.test.ts` RED for a pure receipts module carrying process identity, project identity, store identity, and the readiness / graceful-shutdown pair. DETERMINISTIC MEANS NO CLOCK, and that is the load-bearing constraint: assert two receipt builds over identical inputs are BYTE-IDENTICAL, and that the module source reads neither `Date.now()` nor `new Date()` — any instant arrives as a validated input, exactly as `importCommittedAt` does elsewhere in this repo. Cover the refusal arms with exact code AND layer: absent store identity, absent project identity, shutdown receipt requested before readiness was ever published, and a SECOND shutdown refused BY REFUSAL rather than silently — matching the `DAEMON_ENTRY_ALREADY_STOPPED` discipline at daemon-entry.ts:189-190. Every refusal test pins the code and the refusing layer; the sweep asserts a positive case count.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/host/foundation-receipts.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/host/foundation-receipts.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/host/foundation-receipts.test.ts"
      ],
      "note": "RED written: apps/daemon/src/host/foundation-receipts.test.ts (21 cases). Confirmed red at HEAD 1aa7cdc — \"Cannot find module './foundation-receipts.js'\", Test Files 1 failed, 0 tests. Measured first: grep readinessReceipt|shutdownReceipt over apps/daemon/src = 0 rows, so nothing pre-exists.\n\nCoverage: (a) determinism — two builds over identical inputs encode to identical BYTES (TextEncoder arrays compared) for BOTH receipts, plus a counter-assert that a different instant changes the line so byte-identity cannot be satisfied by a builder that dropped the field; (b) a source scan of foundation-receipts.ts asserting no clock read (Date.now / new Date / performance.now) with a positive control (source >500 chars AND contains \"export function buildReadinessReceipt\") so an unreadable source cannot pass vacuously; (c) one-line encoding with no embedded newline (stderr record discipline for the stdio host); (d) a 6-case refusal sweep with an explicit expected code per case, each asserting code AND layer \"FOUNDATION_RECEIPTS\", preceded by an explicit CASES.length toBe(6) positive-count assertion; (e) publisher state arms — readiness twice = FOUNDATION_RECEIPT_ALREADY_READY, shutdown before readiness = FOUNDATION_RECEIPT_SHUTDOWN_BEFORE_READY, second shutdown = FOUNDATION_RECEIPT_ALREADY_STOPPED (refused, matching the DAEMON_ENTRY_ALREADY_STOPPED discipline at daemon-entry.ts:189-190), each asserting the sink line count so \"refused\" cannot mean \"wrote anyway\"; (f) a refused readiness does NOT open the drain path (shutdown still refuses SHUTDOWN_BEFORE_READY); (g) the published line is byte-equal to the pure builder's encoding.\n\nLayer decision recorded for step 2: the layer constant stays MODULE-PRIVATE. tests/security/boundary-roster.security.ts:315 DECLARATION_PATTERN scans production source for /^export const ([A-Z_]+(?:LAYERS|LAYER|BOUNDARIES))/ and demands a rostered BEFORE/AFTER/RACE trio; a private const keeps the stable literal on the production refusal without falsely widening the security boundary roster (per memory gotcha-exported-layer-constants-trigger-security-roster). Tests assert the literal \"FOUNDATION_RECEIPTS\" directly, never an imported expected value.",
      "startedAt": "2026-08-18T18:55:51.767Z",
      "completedAt": "2026-08-18T19:00:17.917Z"
    },
    {
      "stepId": "step-2",
      "description": "Implement `apps/daemon/src/host/foundation-receipts.ts` plus its one-line git-tracked `.js` bridge — every sibling directory in apps/daemon/src ships bridges, and without one the module is invisible at runtime while tsc and vitest both stay green; `host/` is a NEW directory so the bridge is easy to forget. Exports: a frozen receipt schema version, a frozen refusal-code list with its own layer constant, and pure builders for the readiness and shutdown receipts. PURE BY CONSTRUCTION: no clock, no filesystem, no store handle — identities and any instant arrive as validated inputs, which makes DoD 3's \"deterministic\" a property of the type rather than a promise in a comment. Target well under 250 physical lines. Do NOT create foundation-host.ts — the task description's premise is disproven (see comment-ab99bfe3); the two existing entries ARE the hosts.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/host/foundation-receipts.ts",
        "apps/daemon/src/host/foundation-receipts.js"
      ],
      "newFiles": [
        "apps/daemon/src/host/foundation-receipts.ts",
        "apps/daemon/src/host/foundation-receipts.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/host/foundation-receipts.ts",
        "apps/daemon/src/host/foundation-receipts.js",
        "apps/daemon/src/host/foundation-receipts.test.ts"
      ],
      "note": "GREEN: apps/daemon/src/host/foundation-receipts.ts (227 physical lines, under the 250 target) + its one-line git-tracked bridge apps/daemon/src/host/foundation-receipts.js (`export * from \"./foundation-receipts.ts\";`), matching every sibling directory in apps/daemon/src. Focused run: Test Files 1 passed, Tests 18 passed. `pnpm --filter @moe/daemon typecheck` exit 0 (captured separately, not piped).\n\nSurface: FOUNDATION_RECEIPT_SCHEMA_VERSION (\"moe-foundation-receipt/1\"), frozen FOUNDATION_RECEIPT_REFUSAL_CODES (8), frozen FOUNDATION_HOST_ENTRIES (CONTROL_ROOM_HTTP | MCP_HTTP | MCP_STDIO), frozen FOUNDATION_SHUTDOWN_TRIGGERS (SIGINT | SIGTERM | TRANSPORT_CLOSED), pure buildReadinessReceipt / buildShutdownReceipt, encodeReceipt, and createFoundationReceiptPublisher (the only stateful piece: two booleans enforcing publish-readiness-once and shutdown-once).\n\nPURE BY CONSTRUCTION as the step demands: no clock, no fs, no store handle. Grep over the production source confirms zero Date.now / new Date / performance.now occurrences — the step-1 source-scan test asserts that with a positive control. The instant arrives validated against the repo's UTC-millisecond ISO_INSTANT pattern (the same shape used by activation-ingress-contracts.ts:166, session-contracts.ts:115, journal-entry-codec.ts:26, recovery-completion-evidence.ts:48), so determinism is a property of the type. encodeReceipt writes keys in a FIXED order rather than relying on object-literal insertion order, so byte-identity belongs to that function.\n\nTwo deliberate decisions worth QA's attention. (1) The layer constant FOUNDATION_RECEIPTS_LAYER is NOT exported: tests/security/boundary-roster.security.ts:315 scans production source for /^export const ([A-Z_]+(?:LAYERS|LAYER|BOUNDARIES))/ and would demand a rostered BEFORE/AFTER/RACE trio for what is one module's private refusal stamp. The stable literal still travels on every refusal and the tests assert it directly. (2) A refused readiness build does NOT set the ready flag, so a broken identity cannot be laundered into a later graceful shutdown — the publisher still answers FOUNDATION_RECEIPT_SHUTDOWN_BEFORE_READY, asserted in step 1.\n\nRuntime-loadability probe (bridge, not just tsc): `node -e \"import('./apps/daemon/src/host/foundation-receipts.js')...\"` printed BRIDGE_OK with a real encoded receipt line. No foundation-host.ts was created — the two existing process entries are the hosts.",
      "startedAt": "2026-08-18T19:00:20.318Z",
      "completedAt": "2026-08-18T19:01:56.305Z"
    },
    {
      "stepId": "step-3",
      "description": "Emit receipts from the HTTP daemon entry `daemon-main.ts` (99 lines — room; daemon-entry.ts is at 245/250 and MUST NOT be touched). Publish the readiness receipt as one JSON line where `options.onStarted?.(started.shutdown)` fires at :82 — after `started.ok`, so reconciliation ordering documented at daemon-entry.ts:202 (reconcile BEFORE listener) is inherited, satisfying DoD 3's restart clause for this entry; assert that inheritance, do not rebuild it. Publish the shutdown receipt inside the `stop()` signal path at :89-96 BEFORE `process.exit(0)`. Extend `daemon-main.test.ts` with cases asserting: readiness receipt emitted exactly once with correct identities, shutdown receipt on the stop path, and byte-determinism (instants injected). TDD: red first, then wire.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-main.ts",
        "apps/daemon/src/daemon-main.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/daemon-main.ts",
        "apps/daemon/src/daemon-main.test.ts",
        "apps/daemon/src/host/foundation-receipts.ts"
      ],
      "note": "GREEN after RED. Sequence recorded: with daemon-main.ts untouched the new cases failed 3 of 4 (the csrf case stayed green), then the wiring turned them green. Focused run of both owned suites: Test Files 2 passed, Tests 22 passed. `pnpm --filter @moe/daemon typecheck` exit 0, captured separately.\n\ndaemon-main.ts 99 -> 156 lines (daemon-entry.ts NOT touched, still 245). New private helper announceHost() publishes the readiness receipt and returns the drain that publishes the shutdown half; runDaemonMain hands that drain to options.onStarted at the same call site as before. MainOptions gains two seams: `env` (the store identity source) and `instant` (the receipt clock, INJECTED because the receipts module holds none). The module-level signal path now passes the signal NAME, so SIGINT and SIGTERM stay distinguishable in the receipt.\n\nOrdering evidence for DoD 3, inherited not rebuilt: the test drives the SHIPPED provider (daemon-store-dependencies.ts default export) over a real file-backed SQLite store through a temp witness module that wraps reconciliation() to push a \"SWEPT\" marker and delegate to the real port — it decides nothing. The log sink pushes \"READY_RECEIPT\" when a receipt line appears; the assertion is marks == [\"SWEPT\",\"READY_RECEIPT\"]. Readiness cannot be published while the last crash is unclassified.\n\nOther arms: readiness asserted by full toEqual over all seven fields (entry CONTROL_ROOM_HTTP, pid, projectId, storePath, instant, kind, schemaVersion) and exactly one receipt line; the drain publishes exactly one SHUTDOWN receipt carrying trigger \"SIGTERM\"; a SECOND drain is refused BY THE ENTRY with code DAEMON_ENTRY_ALREADY_STOPPED and layer DAEMON_ENTRY (logged verbatim) and adds no second receipt — one lifecycle, one authority holder; byte-determinism asserted by encoding two full start/stop runs with the same injected instant and comparing TextEncoder arrays; store identity absent refuses FOUNDATION_RECEIPT_STORE_IDENTITY_ABSENT FOUNDATION_RECEIPTS with the listener still serving and exit code still 0, after which the drain half refuses FOUNDATION_RECEIPT_SHUTDOWN_BEFORE_READY.\n\nTwo hazards handled. (1) The shipped provider caches the store it opened from the environment for the life of the process, so ALL production-composed cases in the file share ONE store path — a second temp directory would have handed the receipt a path the daemon was not serving, exactly the lie these tests exist to catch. (2) The file writes the real process.env for the provider and restores prior values in afterAll, so a scratch store path cannot follow the worker into the next file. FOUNDATION_SHUTDOWN_TRIGGERS gained PROGRAMMATIC_STOP as the default for an in-process drain called with no trigger, keeping daemon-startup.test.ts's untyped `shutdown()` wrapper working and its `SHUTDOWN {\"ok\":true}` assertion byte-exact; that non-owned suite re-ran green (5 passed).",
      "startedAt": "2026-08-18T19:07:28.462Z",
      "completedAt": "2026-08-18T19:07:42.028Z"
    },
    {
      "stepId": "step-4",
      "description": "Give the stdio host `mcp-main.ts` (59 lines) its missing drain half plus receipts — grep for SIGTERM/SIGINT/shutdown in it returns NOTHING today. CRITICAL CHANNEL CONSTRAINT: stdout is the MCP JSON-RPC wire; a receipt printed to stdout corrupts the protocol stream. Emit BOTH receipts on STDERR as single JSON lines (J3 reads stderr for the stdio host). CROSS-PLATFORM CONSTRAINT: on Windows an externally sent SIGTERM does not run Node signal handlers, so graceful drain MUST hook the transport-close path — stdin EOF ending `connectStdioTransport` — as the primary trigger, with SIGINT/SIGTERM handlers added for POSIX where they are actually delivered. On drain: stop accepting requests, emit the shutdown receipt, exit 0. Readiness receipt emits after the transport is connected and the dispatch port is composed. No new entrypoint, no transport reimplementation — `createStdioMcpServer` and `createMcpDispatchPort` stay exactly as composed today.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-main.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-main.ts",
        "apps/daemon/src/mcp-main.test.ts"
      ],
      "note": "GREEN after RED (6 failed -> 6 passed). mcp-main.ts 59 -> 152 lines. New exported createStdioHost(seam) owns the drain the entry never had; createStdioMcpServer and createMcpDispatchPort stay composed exactly as before, no new entrypoint, no transport reimplementation. One new test file apps/daemon/src/mcp-main.test.ts (chosen over a separate lifecycle module to hold the plan's file count at 8 distinct files).\n\nMEASURED SDK FACT that changes the design, and it contradicts the obvious reading of \"transport close\": the SDK's StdioServerTransport (node_modules/.pnpm/@modelcontextprotocol+sdk@1.30.0/.../server/stdio.js) attaches ONLY `_stdin.on(\"data\")` and `_stdin.on(\"error\")`. It never listens for `end` or `close`, so stdin EOF does NOT fire server.onclose and a drain hung on that hook would never run. Production therefore hooks process.stdin \"end\" AND \"close\" directly; drain is idempotent so the pair is free. Verified live, not reasoned: the smoke below drained on EOF.\n\nReceipts go to STDERR only — stdout is the MCP JSON-RPC wire and one stray line would corrupt the protocol stream. Readiness publishes AFTER connectStdioTransport resolves and after the dispatch port is composed. stop() closes the server, then the store handles (provider.close()).\n\nTests: readiness receipt asserted by full toEqual over all seven fields with entry MCP_STDIO; drain on transport close, drain on a POSIX signal naming that signal; TWO RACING DRAINS (transport close + SIGINT awaited together) leave exactly ONE shutdown receipt, one stop() call and one exit(0) — COUNTED, because \"the second call did not throw\" is also what a double drain looks like; a transport that dies before readiness still releases the process while the receipt refuses FOUNDATION_RECEIPT_SHUTDOWN_BEFORE_READY (wedging an agent session is worse than a missing line); absent store identity discloses FOUNDATION_RECEIPT_STORE_IDENTITY_ABSENT FOUNDATION_RECEIPTS and publishes nothing. The injected seam is process plumbing only — exit, stdin hook, signal hook, stop, write, clock — and carries no authority, dispatch or store.\n\nPLAIN-NODE STDIO SMOKE ALREADY PASSES (run from PowerShell against a scratch store, probe script kept OUTSIDE the repo in %TEMP%, nothing to clean from the worktree): READY receipt on stderr, one official MCP initialize round-trip on stdout answered with protocolVersion 2024-11-05 / serverInfo moe-next, stdin closed, SHUTDOWN receipt with trigger TRANSPORT_CLOSED, EXIT 0, and STDOUT_PURE_JSONRPC true (every stdout line parses with jsonrpc \"2.0\" — the channel-separation proof). SMOKE_PASS, SMOKE_EXIT=0. `pnpm --filter @moe/daemon typecheck` exit 0.",
      "startedAt": "2026-08-18T19:07:45.805Z",
      "completedAt": "2026-08-18T19:10:57.602Z"
    },
    {
      "stepId": "step-5",
      "description": "Prove DoD 1/2/4 at the transport seam in `mcp-dispatch-port.test.ts`, against PRODUCTION composition — `createMcpDispatchPort` over real `createStoreDependencies` with a temp store; no injected test dispatcher decides anything (that IS DoD 1's clause). foundation.dispatch is already wired end-to-end (registry :121-126, async seam http-adapter.ts:156), and with `captureResult` still null the dispatch still records a durable FOUNDATION_ATTEMPT_RECORDED decision, so it is the right probe command. Tests: (a) the SAME foundation.dispatch command through `dispatchCommandBytes` with stdio-shaped context and with HTTP `HttpDispatchContext` produces byte-identical durable decisions (DoD 2); (b) CONCURRENT duplicate delivery of one commandId leaves exactly ONE decision — COUNT THE ROWS read back from the store, because \"the second call did not throw\" is also what a double write looks like (DoD 4a); (c) an advisory query through `dispatchQueryBytes` writes ZERO durable decisions (DoD 4b); (d) malformed JSON, oversized payload (FOUNDATION_ATTEMPT_MAX_REQUEST_BYTES), forged credential, and stale protocolVersion each assert the EXACT transport or daemon reason code AND which layer refused, and commit nothing — count rows before/after. Do not duplicate registry-level sync-refusal tests that already exist at daemon-command-registry.test.ts:60.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-dispatch-port.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-dispatch-port.test.ts",
        "apps/daemon/src/mcp-main.js"
      ],
      "note": "GREEN. apps/daemon/src/mcp-dispatch-port.test.ts 215 -> 492 lines, 9 -> 16 cases. Focused run: `pnpm exec vitest run --root . --config package.json src/mcp-dispatch-port.test.ts` from apps/daemon -> \"Test Files 1 passed (1) / Tests 16 passed (16)\" (count line read, not exit code alone).\n\nRESUME NOTE: this step was IN_PROGRESS from a session released at 19:17:41Z whose CLI kept running; it wrote the step-5 bytes at 19:23:32Z while this session was reading the file (215 lines at first read, 492 at second). Verified the bytes against the step-5 spec, confirmed the hashes stopped moving, and ADOPTED them rather than writing a duplicate set. start_step returned INVALID_STATE (expected).\n\nEvery port under test is `createMcpDispatchPort` over `createStoreDependencies`/`seamHarness` with a real store - no injected dispatcher decides auth, command or durable outcome (DoD 1). `foundation.dispatch` is the probe.\n\nDoD 2 parity: same bytes, two independent stores. stdio arm carries fallbackCredential; HTTP arm is built with NO fallback and is held as `HttpDispatchPort` exactly as mcp-http-host.ts:129 holds it, so its credential can only come from the request context. Answers compared as decoded STRINGS (byte-identical), and pinned first to `outcome PORT_REFUSED / refusal.code+layer / stage DISPATCH` so two identical AUTHENTICATION_FAILED answers cannot pass vacuously. Both stores read back [\"FoundationDispatchReserved\",\"FoundationAttemptRecorded\"].\n\nDoD 4a: two concurrent deliveries of one commandId. Codes sorted-compared to exactly [LAUNCH_REFUSAL, FOUNDATION_ATTEMPT_DISPATCH_IN_PROGRESS]; ROW COUNTS read back from the store - Reserved x1, Recorded x1, EffectActivationCommitted x1 - because a non-throwing second call is also what a double write looks like.\n\nDoD 4b: advisory reads counted via readCommandDecisionsAfter(0n,1000).items.length AND readEventHorizon() before/after; a fully valid credentialled COMMAND envelope pushed at the read surface refuses INPUT_INVALID and the pair is unchanged.\n\nHostile arms, each delivered over BOTH transports with the store counted before/after (`wrote === false`): malformed JSON -> INPUT_INVALID stage DECODE; oversized -> INPUT_LIMIT_EXCEEDED limitName JSON_STRING_UTF8_BYTES stage DECODE, with the ordering ASSERTED (base64 ceiling of FOUNDATION_ATTEMPT_MAX_REQUEST_BYTES > MAX_JSON_STRING_UTF8_BYTES) so the codec's 1 MiB bound is provably unreachable from this transport; stale schemaVersion -> SCHEMA_VERSION_UNSUPPORTED + supportedSchemaVersion details, stage DECODE; forged bearer -> AUTHENTICATION_FAILED stage AUTHENTICATE, HTTP-only by construction (stdio has one process identity). Wire protocolVersion is pinned by the port itself, so DISTRIBUTION_MISMATCH is unreachable across MCP - covered on the HTTP entry at http-adapter.test.ts:146.\n\nALSO LANDED (blocking the whole fleet's daemon gate): apps/daemon/src/mcp-main.js, the one-line bridge `export * from \"./mcp-main.ts\";`. runtime-entrypoint.test.ts:180 promotes mcp-main.ts to the runtime tier the moment step-4's mcp-main.test.ts sibling exists, and the bridge was absent. Step 6's plain-Node smoke also runs `node apps/daemon/src/mcp-main.js`.\n\nFOREIGN, DO NOT STAGE: apps/daemon/src/orchestrator/moe-up-env.test.ts is task-bc9b4bed's in-flight WIP (STEP_STARTED 19:27:43Z).",
      "startedAt": "2026-08-18T19:11:02.583Z",
      "completedAt": "2026-08-18T19:30:01.984Z"
    },
    {
      "stepId": "step-6",
      "description": "Verification gate. The manifest half of DoD 5 is ALREADY SATISFIED: apps/daemon/package.json already declares @moe/mcp (verified at plan time) — do NOT run pnpm install, do NOT commit package.json or pnpm-lock.yaml. Run five legs with exit codes captured SEPARATELY, never chained with `;` or pipes: `pnpm --filter @moe/mcp typecheck`, `pnpm --filter @moe/mcp test`, `pnpm --filter @moe/daemon typecheck`, `pnpm --filter @moe/daemon test`, `pnpm typecheck`. Both packages resolve by NAME (@moe/mcp = packages/mcp, @moe/daemon = apps/daemon). READ THE OUTPUT: `pnpm --filter` prints \"No projects matched the filters\" and EXITS 0 for an absent package — record the nonzero `Test Files N passed` count line for both test legs. Then the PLAIN-NODE STDIO SMOKE DoD 5 names, run from POWERSHELL (a prior smoke failed only because Git Bash rewrote paths): spawn `node apps/daemon/src/mcp-main.js` with MOE_STORE_PATH/MOE_PROJECT_ID/MOE_DAEMON_CREDENTIAL/MOE_SESSION_CREDENTIAL set against a scratch store, read the readiness receipt JSON line from stderr, exchange one official MCP stdio initialize round-trip on stdout, close stdin, and confirm the shutdown receipt on stderr plus exit 0. `pnpm typecheck` is repo-wide: attribute any red by the merge-base-versus-HEAD failing-path delta intersected with owned paths, disclosing foreign red verbatim.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/mcp-main.ts",
        "apps/daemon/src/host/foundation-receipts.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-main.js"
      ],
      "note": "ALL FIVE LEGS EXIT 0, run separately, no `;` chain and no pipe hiding the code (${PIPESTATUS[0]} captured on each). Count lines read, not exit codes alone.\n\n1. `pnpm --filter @moe/mcp typecheck` -> 0.\n2. `pnpm --filter @moe/mcp test` -> 0. \"Test Files 9 passed (9) / Tests 167 passed (167)\".\n3. `pnpm --filter @moe/daemon typecheck` -> 0.\n4. `pnpm --filter @moe/daemon test` -> 0. \"Test Files 146 passed (146) / Tests 3115 passed (3115)\", 47.64s.\n5. `pnpm typecheck` (repo-wide, 16 packages) -> 0, every package \"Done\".\n\nManifest half of DoD 5: apps/daemon/package.json already declares \"@moe/mcp\": \"workspace:*\". No pnpm install run; package.json and pnpm-lock.yaml untouched and will not be staged.\n\nTRANSIENT FOREIGN RED, disclosed and cleared: leg 3's first run failed with 5 errors, ALL in apps/daemon/src/orchestrator/moe-up-env.test.ts (TS2307 x2 \"Cannot find module './moe-up-env.js'\", TS7006 x3). Attribution: the file is UNTRACKED (`git ls-files --error-unmatch` -> \"did not match any file(s) known to git\"; `git cat-file -e HEAD:...` -> \"exists on disk, but not in HEAD\") and belongs to task-bc9b4bed, whose step-1 is literally \"Write apps/daemon/src/orchestrator/moe-up-env.test.ts RED\" (STEP_STARTED 19:27:43Z in .moe/activity.log). Its peer landed moe-up-env.ts at 22:30:49 and moe-up-env.js at 22:31:01 DURING my run; the immediate re-run of the same command exits 0. Zero errors in any owned path at any point, so the (HEAD-minus-merge-base) failing-path delta intersected with owned paths is EMPTY.\n\nPLAIN-NODE STDIO SMOKE (PowerShell, System.Diagnostics.Process, scratch store under $env:TEMP):\n- readiness on STDERR: {\"schemaVersion\":\"moe-foundation-receipt/1\",\"kind\":\"READY\",\"entry\":\"MCP_STDIO\",\"pid\":65880,\"projectId\":\"proj-smoke\",\"storePath\":\"...\\\\smoke.db\",\"instant\":\"2026-08-18T19:33:12.094Z\"}\n- one official MCP initialize round-trip on STDOUT: {\"result\":{\"protocolVersion\":\"2024-11-05\",\"capabilities\":{\"tools\":{}},\"serverInfo\":{\"name\":\"moe-next\",\"version\":\"0.0.0\"}},\"jsonrpc\":\"2.0\",\"id\":1}\n- stdin closed -> {\"kind\":\"SHUTDOWN\",...,\"trigger\":\"TRANSPORT_CLOSED\"} on STDERR, then EXITCODE=0.\nStdout carried ONLY the JSON-RPC frame; both receipts came off stderr, so the wire is uncorrupted.\n\nRESTART CLAUSE (DoD 3), same smoke harness run twice against ONE store path: RUN1 and RUN2 each published READY, answered initialize, published SHUTDOWN(TRANSPORT_CLOSED) and exited 0, with different pids (58556, 35184) and identical projectId/storePath; the store persisted at 172032 bytes between runs. Reconciliation ordering is inherited, not rebuilt (boot-reconciliation via daemon-store-dependencies.ts:18, ordered before the listener per daemon-entry.ts:202).\n\nMEASURED DEVIATION from the step text, with a positive control rather than an assumption. The step named `node apps/daemon/src/mcp-main.js`; that entry HOSTS NOTHING. Control run: STDOUT_LEN=0, STDERR_LEN=0, EXITCODE=0 - the process starts and exits without publishing a receipt or serving a frame, because mcp-main.ts guards on `import.meta.main === true` and that is FALSE when the module is loaded through the `export * from \"./mcp-main.ts\"` bridge. The shipped entry is ./src/mcp-main.ts, exactly what apps/daemon/package.json bin.moe-mcp-stdio names at line 11 and the shape the daemon's own `start` script uses. The smoke above therefore runs the .ts. The .js bridge is still REQUIRED and was landed in step 5 - runtime-entrypoint.test.ts:180 promotes mcp-main.ts to the runtime tier now that mcp-main.test.ts exists - it is an import-graph bridge, never a process entry.",
      "startedAt": "2026-08-18T19:30:05.059Z",
      "completedAt": "2026-08-18T19:33:59.638Z"
    },
    {
      "stepId": "step-7",
      "description": "Mutation drills and adversarial review, then stop. THREE drills, each restored before the next, each naming the test that reddens: (i) make the readiness receipt builder read `Date.now()` — the step-1 byte-identity assertion must redden, proving \"deterministic\" is tested rather than asserted; (ii) make the duplicate-delivery path write a second decision — the step-5 row-COUNT assertion must redden while a return-value-only assertion would still pass; (iii) delete the shutdown-before-readiness refusal arm — the step-1 refusal test must redden NAMING that exact code. A drill that reddens the WRONG test, or hangs, is not a pass. SHARED-WORKTREE DISCIPLINE: record HEAD before drilling, snapshot each mutated file's content hash, restore by CONTENT never `git checkout`, re-read the file before restoring in case a peer moved it (task-c52d25a4 is editing vocabulary/registry in this same worktree — those files are NOT owned here and must never appear staged). Then run the `adversarial-self-review` skill over: two concurrent drains on mcp-main, readiness published twice, stdin EOF racing an in-flight dispatch, and per-file line counts — every production file under 250, daemon-entry.ts untouched at 245. Commit by explicit pathspec of owned paths only; `git status --porcelain` before, `git show --stat` after.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/host/foundation-receipts.ts",
        "apps/daemon/src/mcp-main.ts",
        "apps/daemon/src/mcp-dispatch-port.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-main.ts",
        "apps/daemon/src/mcp-main.test.ts",
        "apps/daemon/src/mcp-dispatch-port.test.ts"
      ],
      "note": "DRILLS (HEAD before drilling b773de7; each target snapshotted by CONTENT to $TEMP and restored by content, never `git checkout`; hashes re-verified after each restore; peer commit a77ca52 landed INSIDE the drill window and was checked - it touched only packages/runner, none of my paths or drill targets).\n\n(i) Clock in the receipt builder. `instant: input.instant` -> `new Date(Date.now()).toISOString()`. RED 3 of 18, including the named one: \"encodes two builds over identical inputs to identical BYTES\", plus \"reads no clock in its production source\" and the identity case. It reddens DETERMINISTICALLY despite millisecond resolution because the case also asserts a DIFFERENT instant stays VISIBLE - with a clock both builds collapse to the same value and `.not.toBe` fails. Restored to f1a306a0.\n\n(iii) Deleted `if (!ready) return refuse(\"FOUNDATION_RECEIPT_SHUTDOWN_BEFORE_READY\")`. RED 2 of 18, naming that exact code in the diff: \"refuses a shutdown receipt before readiness was ever published\" and \"forwards a builder refusal verbatim and writes nothing\". Restored to f1a306a0.\n\n(ii) THIS ONE FOUND A DEFECT IN MY OWN TEST. First mutation - delete the REPLAYED early return in foundation-attempt-service.ts so the duplicate launches again - reddened the duplicate test, but on the CODE assertion (line 336), which short-circuited before the row counts ever ran. Second, sharper mutation - keep the return values, write one extra durable reservation under a different decision key - proved the row was written (the winner's answer flips to FOUNDATION_ATTEMPT_RECORD_AMBIGUOUS) yet STILL reddened on the code assertion first. Production couples the two, so the row count could never have been shown load-bearing where it sat. FIX: the ROW COUNTS are now asserted BEFORE the code set, with the measurement recorded in the comment. Re-ran the same drill: \"AssertionError: expected [...] to have a length of 1 but got 2\" at mcp-dispatch-port.test.ts:341, the FoundationDispatchReserved count. Service restored to 7fd665ea, `git status` clean for it.\n\nADVERSARIAL SELF-REVIEW - one REAL defect found and fixed, TDD.\n`createStdioHost.drain` did `await seam.stop()` unguarded. A stop that throws (SDK close, a busy store handle) rejected the drain promise, so `seam.exit(0)` never ran and an agent session whose stdin is ALREADY CLOSED wedges forever - the exact failure the receipt exists to rule out. RED first: new case \"releases the process and discloses the fault when the stop path THROWS\" failed with `Error: handle busy`, process never released. Fix: catch, disclose `FOUNDATION_HOST_STOP_FAILED <message>` (a host fact, so it never borrows the receipts layer, and it is not a `*_LAYER` constant so the security roster is untouched), publish the shutdown receipt anyway, exit 0. Green.\n\nReviewed and already covered: two concurrent drains converge on one receipt (`draining ??=` assigns before any await; mcp-main.test.ts:112); readiness published twice refuses ALREADY_READY (receipts test:179); empty store/project identity refuses by name and is disclosed, never fatal.\n\nDISCLOSED RESIDUALS, not fixed, with reasons.\n(a) stdin EOF racing an IN-FLIGHT dispatch: drain closes the server then the store under a dispatch that may be awaiting a launch. No partial durable write is possible (the store commits transactionally) and the committed reservation makes a restart answer DISPATCH_IN_PROGRESS or adopt the record - fail-closed. Structurally outside createStdioHost, which holds no dispatch reference.\n(b) daemon-entry.ts:241 `await started.close()` has no try/catch, so a throwing close rejects `void shutdown(t).then(...)` and the HTTP host hangs. PRE-EXISTING and unchanged by this diff (the `void shutdown().then(process.exit)` shape predates it); daemon-entry.ts is at 245/250 and untouchable per the plan.\n\nLINE CAPS: foundation-receipts.ts 232, mcp-main.ts 165, daemon-main.ts 156 - all under 250. daemon-entry.ts UNTOUCHED at 245.\n\nCOMMIT 26aa254, explicit pathspec, 9 files, +1313/-17. `git diff --cached` empty before and after; `git show --stat` lists only owned paths; apps/daemon/package.json (peer-modified for bin.moe-up) and 17 untracked orchestrator files were NOT staged.\n\nFULL 5-LEG GATE RE-RUN FRESH AFTER THE COMMIT, exit 0: @moe/mcp typecheck; @moe/mcp test 9 files/167 tests; @moe/daemon typecheck; @moe/daemon test 150 files/3246 tests; pnpm typecheck 19 projects all Done.",
      "startedAt": "2026-08-18T19:34:03.098Z",
      "completedAt": "2026-08-18T19:48:23.556Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Recorded re-promotion gate is now met on QA-approved bytes: task-a77bd540 (foundation runtime kinds) is DONE and task-4bffcfdd (async daemon/MCP + binary seam) is DONE at 09f55ed/ffc6a54. Architect must re-measure the real existing stdio/HTTP hosts and account for task-31ea82e7 as the remaining receipts/captureResult prerequisite; no duplicate host.",
  "createdBy": "WORKER",
  "parentTaskId": "task-97554aa4293e40eab56c0b642e18513a",
  "priority": "CRITICAL",
  "order": 7,
  "comments": [
    {
      "id": "comment-c02b88eb5ca04d3a8d3761228507a5ae",
      "author": "architect-8c9073cd",
      "content": "Hard dependency: Foundation daemon ingress surface `task-5e43a9e294ef48fdab23817c8c6cfc45`. Final real consumer/certifier: Foundation self-host canary `task-97554aa4293e40eab56c0b642e18513a`.",
      "timestamp": "2026-08-09T12:29:46.073Z"
    },
    {
      "id": "comment-7eeb6b01a97c4c4f88f2b46694ca2caa",
      "author": "governor-773e2f5e",
      "content": "⚠️ VERIFIED PRODUCTION DEFECT IN THIS TASK'S SUBJECT AREA — from Yaron's 2026-08-14 product audit, #general 23:06:38Z. **The human explicitly flagged it as \"relevant to the blocked Foundation MCP dispatch host task\"** — that is this task. Task stays BLOCKED; this is here so the architect who eventually plans it starts from the real state.\n\n**MCP `events.read` serves a permanently frozen page.** `apps/daemon/src/mcp-dispatch-port.ts:98` calls `readSubscriptionPage` with no `foldOnce`, and `mcp-main` never seats a baseline or registers a reader. So the endpoint answers, returns a well-formed page, and that page never advances. Severity MEDIUM in the audit, but note the failure mode: **it does not error.** A consumer polling `events.read` gets successful responses forever and simply never sees new events. Nothing fails closed; there is no reason code to observe.\n\n**What this changes for planning:** a dispatch-host DoD that asserts `events.read` returns a valid page would pass today against a frozen one. The property to pin is *advancement* — read, commit new events, read again, and assert the cursor moved and the new events are present. A shape assertion on a single response cannot distinguish a working reader from this bug. That is the same class as the board's rail on assertions quietly detaching from their subject.\n\n**Block status re-measured 2026-08-13T23:20Z, unchanged and still correct:**\n- `apps/daemon/src/foundation/` — the entire directory is absent on disk, not just `foundation-surface.{ts,js}`. The original block premise holds.\n- task-5e43a9e2 (Foundation daemon ingress surface) = BLOCKED\n- task-97554aa4 (Foundation self-host canary) = BLOCKED\n\nDo not promote this task on the audit finding — the finding sharpens the eventual DoD, it does not clear the dependency. Two of this task's own prerequisites are still blocked behind the durable Claude attempt-dispatch chain.\n\n**One more from the same audit, in the hardening backlog and squarely in this task's territory:** the MCP HTTP session registry grows unbounded, and `close()` never releases daemon-side bindings. Whoever plans the dispatch host should treat session lifetime as in-scope rather than assuming the existing transport manages it.",
      "timestamp": "2026-08-13T23:19:53.927Z"
    },
    {
      "id": "comment-a5123547f52a4a5d849aeb2da0809ea0",
      "author": "human",
      "content": "GOVERNANCE RE-MEASUREMENT 2026-08-15 by governor-18a42000. TASK STAYS BLOCKED, but HALF THIS BLOCK REASON IS NOW FALSE. Read this before re-deriving it.\n\nThe block names two prerequisites. One has closed.\n\nCONDITION 2 — CLOSED. The block says: \"@moe/mcp has official stdio and Streamable HTTP implementations on disk, but its only package export is `.` -> `src/index.ts`, and that root exports only stdio. `createHttpMcpAdapter`, `HttpAdapterOptions`, `HttpMcpAdapter`, `HttpDispatchPort`, and `HttpSessionPort` exist only under unexported `src/http/**`; the task rail forbids deep imports.\"\n\nThat is no longer true. Measured on current bytes:\n- packages/mcp/src/index.ts:28 `export { createHttpMcpAdapter } from \"./http/http-server.js\"`\n- packages/mcp/src/index.ts:32-33 export HttpDispatchPort and HttpMcpAdapter\n- packages/mcp/src/index.ts:35 `export type { HttpSessionPort } from \"./http/http-session.js\"`\n\nThe HTTP MCP surface IS published from the @moe/mcp root, so no root-surface amendment is required and no deep import is needed. Proof it genuinely resolves through the bare specifier rather than merely appearing in a barrel: apps/daemon/src/mcp-http/mcp-http-host.ts:5 does `import { createHttpMcpAdapter } from \"@moe/mcp\"` and typechecks.\n\nFurther, the block's premise that \"no daemon or app composes the HTTP surface\" is also dead. task-159be643 (Compose production Streamable HTTP MCP host) landed the composition:\n- mcp-http-host.ts:126 calls createHttpMcpAdapter\n- mcp-http-host.ts:137 `sessionPort: createMcpHttpSessionPort(options.deps.authenticator)` — the REAL daemon authenticator, not a fake\n- mcp-http-session-port.ts:77 `authenticator.authenticate(credential)` is the whole authority decision\nAll three files are committed at HEAD (blobs 4d35e659 / 88a90593 / 3ebe4055).\n\nCONDITION 1 — STILL UNMET, and it is why this task stays BLOCKED. The hard dependency task-5e43a9e294ef48fdab23817c8c6cfc45 (Foundation daemon ingress surface) is still BLOCKED, and apps/daemon/src/foundation/foundation-surface.{ts,js} is still absent, so this host still cannot name the bounded command/query/session/coordination surface it must compose. See my parallel comment on task-5e43a9e2 for its own re-measurement — it has moved from zero of five dependencies DONE to two of five, but three remain BLOCKED.\n\nWHY THIS MATTERS RATHER THAN BEING BOOKKEEPING: an architect claiming this task would spend a full context load proving the @moe/mcp root export gap and the missing composition, and would find both already closed. The remaining blocker is one task, not two, and it is a task-status gate rather than a package-surface gap.\n\nRE-PROMOTE WHEN: task-5e43a9e2 reaches DONE. At that point re-probe both public package namespaces as the block instructs, but expect the @moe/mcp side to already be satisfied. Do not inject fake dispatch/session authority and do not deep-import — those rails still stand.",
      "timestamp": "2026-08-15T12:38:55.896Z"
    },
    {
      "id": "comment-09b379d182a048d4a388c460372036cf",
      "author": "architect-77071474",
      "content": "RE-SCOPE RECORD, architect-77071474 at HEAD a42ae2f. Posting this myself rather than leaving it to the plan's step 1, so the deviation is auditable even if that step is skipped.\n\n**THE DESCRIPTION'S PREMISE IS DISPROVEN.** It says to build a daemon host \"so Foundation self-hosting does not inject a fake StdioDispatchPort/HttpDispatchPort in its E2E harness\". At current HEAD:\n- `grep -rn \"StdioDispatchPort|HttpDispatchPort\" tests/` returns **ZERO**. No fake port is injected anywhere.\n- `tests/e2e/foundation/e2e-process.ts:96` spawns **real** children via `process.execPath` against three declared kill targets.\n- The host is not missing — it is already built **twice**: `apps/daemon/src/mcp-main.ts` (58 lines) composes `createStdioMcpServer` + `connectStdioTransport` + `createStoreDependencies` + `createMcpDispatchPort` with real credentials, and `apps/daemon/src/mcp-http/mcp-http-main.ts` (62) + `mcp-http/mcp-http-host.ts` serve Streamable HTTP through `createHttpMcpAdapter`.\n- `apps/daemon/src/host/` does not exist.\n\n**BOTH PREREQUISITES DID LAND**, so the unblock was correct: `apps/daemon/src/foundation/foundation-surface.{ts,js}` exists and is published from the daemon root by explicit names (index.ts:183+), and `@moe/mcp`'s root exports the stdio and HTTP surfaces. `apps/daemon/package.json` **already declares `@moe/mcp`** — DoD 5's manifest addition is a no-op, so package.json and pnpm-lock.yaml should not be committed by this task.\n\n**THE TWO REAL GAPS, measured:**\n1. `grep -c \"FOUNDATION_DISPATCH_COMMAND_KIND|FOUNDATION_VERIFICATION_COMMAND_KIND|foundation\\.\"` returns **0** against BOTH `daemon-command-vocabulary.ts` and `daemon-command-registry.ts`. So `foundation.dispatch` and `foundation.verification` are unreachable from **every** transport — stdio, Streamable HTTP and the HTTP listener all funnel through `createMcpDispatchPort` → `handleCommandRequest` → the registry. One registry edit fixes all three.\n2. `grep -rn \"readinessReceipt|ReadinessRecord|shutdownReceipt|ReadyReceipt\" apps/daemon/src` returns **ZERO**. Readiness today is a log line at `daemon-entry.ts:227`.\n\n**ALREADY SATISFIED — do not rebuild:** restart reconciliation runs before the listener (`boot-reconciliation.ts` is the \"production caller of reconcileOnRestart\"; daemon-entry.ts:202 documents the ordering), and graceful shutdown exists with `DAEMON_ENTRY_ALREADY_STOPPED`. The one drain gap is that `mcp-main.ts` has **no** signal handling.\n\n**DECISION (human, in-session):** do NOT create `apps/daemon/src/host/foundation-host.ts` as a third entrypoint — the reopen reason's \"no duplicate HTTP host\" is taken literally. Owned paths shift to the registry/vocabulary pair, a new pure `host/foundation-receipts.ts`, and the two process entries. Plan submitted on that basis.\n\nCaution for whoever implements: `PAYLOAD_KEYS` and friends **moved** from `daemon-command-registry.ts` into `daemon-command-vocabulary.ts` between two measurements in this same session. A peer is actively editing that area — re-read before editing and before any mutation-drill restore.",
      "timestamp": "2026-08-18T03:04:32.778Z"
    },
    {
      "id": "comment-303782aba93540b7ae787a8264b49c40",
      "author": "worker-f144c751",
      "content": "RE-SCOPE RECORD (step-1). Re-measured at HEAD 8222b9e (plan measured a42ae2f; all figures reconfirmed unchanged).\n\nTHE DESCRIPTION'S PREMISE IS DISPROVEN:\n(a) `grep -rn \"StdioDispatchPort|HttpDispatchPort\" tests/` returns ZERO. No fake dispatch port is injected anywhere in the E2E harness. `tests/e2e/foundation/e2e-process.ts:96` spawns REAL children: `spawn(process.execPath, [...args], { stdio: \"ignore\" })`.\n(b) The host is not missing — it is ALREADY BUILT TWICE. `apps/daemon/src/mcp-main.ts` (58 lines) composes `createStdioMcpServer` + `connectStdioTransport` + `readBootstrapCredential` + `createStoreDependencies` + `createMcpDispatchPort` with real credentials. `apps/daemon/src/mcp-http/mcp-http-main.ts` + `mcp-http/mcp-http-host.ts` serve official Streamable HTTP via `createHttpMcpAdapter`.\n(c) `apps/daemon/src/host/` does not exist.\n\nDECISION (human, in the REPL, before planning): DO NOT build `host/foundation-host.ts` as a third entrypoint. The reopen reason's \"no custom transport, no duplicate HTTP host\" is taken literally. Owned paths shift from the description's `host/foundation-host.ts` to the two measured gaps below.\n\nGAP 1 (the real one, DoD 1 + DoD 2 reach): `grep -c \"FOUNDATION_DISPATCH_COMMAND_KIND|FOUNDATION_VERIFICATION_COMMAND_KIND|foundation\\.\"` returns **0** against BOTH `daemon-command-vocabulary.ts` and `daemon-command-registry.ts`. So `foundation.dispatch` (work/foundation-attempt-contracts.ts:39) and `foundation.verification` (evidence/foundation-verification-contracts.ts:24) are unreachable from EVERY transport — stdio, Streamable HTTP and the HTTP listener alike — because all three funnel through `createMcpDispatchPort` -> `handleCommandRequest` -> the registry (mcp-dispatch-port.ts:62-71). One registry+vocabulary edit fixes all three transports.\n\nGAP 2 (DoD 3): `grep -rn \"readinessReceipt|ReadinessRecord|shutdownReceipt|ReadyReceipt\" apps/daemon/src` returns ZERO. Readiness today is a log line at daemon-entry.ts:227. New pure receipts module lands at `apps/daemon/src/host/foundation-receipts.ts` (+ `.js` bridge), emitted from the two existing process entries (`daemon-main.ts`, `mcp-main.ts`) — NOT from daemon-entry.ts, which is at 244 of a 250 target.\n\nALREADY SATISFIED, NOT REBUILT: restart reconciliation (`boot-reconciliation.ts`, ordered before the listener at daemon-entry.ts:202); graceful shutdown with `DAEMON_ENTRY_ALREADY_STOPPED` (daemon-entry.ts:189-190) and SIGINT/SIGTERM in daemon-main.ts:89-96; and the `@moe/mcp` dependency in apps/daemon/package.json — so DoD 5's manifest addition is a NO-OP and neither package.json nor pnpm-lock.yaml will be committed.\n\nREMAINING GAP on the stdio side: `mcp-main.ts` has NO signal handling (grep for SIGTERM/SIGINT returns nothing), so the graceful-drain half lands there.\n\nFile sizes at HEAD 8222b9e: vocabulary 123, registry 226, daemon-entry 244, daemon-main 99, mcp-main 58, mcp-dispatch-port 108, registry.test 549.",
      "timestamp": "2026-08-18T03:05:14.299Z"
    },
    {
      "id": "comment-50ab412b071647858f6b6a2920810495",
      "author": "worker-f144c751",
      "content": "BLOCK EVIDENCE (step-2). The approved plan's core — wiring `foundation.dispatch` / `foundation.verification` into `daemon-command-vocabulary.ts` + `daemon-command-registry.ts` — is IMPOSSIBLE within this task's owned paths. Four independent blockers, each measured at HEAD 8222b9e.\n\n1. CLOSED ENUM, IN A PACKAGE THIS TASK DOES NOT OWN.\n`CommandRegistryEntry.kind` is typed `RuntimeCommandKind` and `CommandRegistry = ReadonlyMap<RuntimeCommandKind, CommandRegistryEntry>` (apps/daemon/src/http/http-contract.ts:176,181). `RUNTIME_COMMAND_KINDS` is `Object.freeze`d with 92 members at packages/contracts/src/runtime/runtime-vocabulary.ts:85-108, and ZERO begin with `foundation`. Compiled probe, written and deleted in the same command:\n```\nsrc/__blocker-probe.ts(5,14): error TS2322: Type '\"foundation.dispatch\"' is not assignable to type '\"approval.decide\" | ... 84 more ... | \"work.resume\"'.\nsrc/__blocker-probe.ts(6,14): error TS2322: Type '\"foundation.verification\"' is not assignable to type '\"approval.decide\" | ... 84 more ... | \"work.resume\"'.\n```\nSo adding either kind to `PAYLOAD_KEYS`/`WiredCommandKind` reddens `entryOf`'s return in the registry. The fix lives in `packages/contracts`, not `apps/daemon`.\n\n2. THE ENVELOPE REFUSES THE KIND BEFORE THE REGISTRY IS EVER CONSULTED.\npackages/contracts/src/runtime/runtime-envelope.ts:141:\n`if (!isCommandKind(record[\"commandKind\"])) return commandFailure(invalidInput());`\nstdio, Streamable HTTP and the HTTP listener all funnel through `decodeRuntimeCommandEnvelopeBytes`, so no registry entry is reachable for an unlisted kind. The plan's premise that \"one registry edit fixes all three transports\" holds only for kinds already in the runtime vocabulary.\n\n3. SYNCHRONOUS HANDLER vs ASYNCHRONOUS SERVICE.\n`export type CommandHandler = (input: CommandHandlerInput) => DurableDecision;` (http-contract.ts:167). Both Foundation services are async:\n- `createFoundationAttemptService(deps).dispatch(input): Promise<FoundationAttemptOutcome>` (work/foundation-attempt-service.ts:106)\n- `createFoundationVerificationService(deps).verify(input): Promise<FoundationVerificationOutcome>` (evidence/foundation-verification-service.ts:54)\nA sync handler cannot return either decision.\n\n4. BINARY REQUEST FIELD vs JsonObject PAYLOAD.\n`FoundationAttemptDispatchRequest.activationRequestBytes` is a `Uint8Array`, enforced structurally at work/foundation-attempt-codec.ts:89:\n`if (!(bytes instanceof Uint8Array) || bytes.byteLength === 0 || bytes.byteLength > MAX_BYTES) return refuseLocal(\"FOUNDATION_ATTEMPT_REQUEST_MALFORMED\");`\n`RuntimeCommandEnvelope.payload` is a `JsonObject` and the registry's `requestOf` JSON-encodes it. A base64 string dies on that exact guard.\n\nBLOCKERS 3 AND 4 SURVIVE CLOSING THE ENUM. So the obvious workaround — re-mapping Foundation onto an existing runtime kind such as `effect.activate` or `evidence.run` — does not rescue it either.\n\nBONUS GAP: `FoundationAttemptDeps.captureResult` has ZERO production producers repo-wide. `grep -rn \"captureResult\" apps/ packages/ --include=*.ts | grep -v '\\.test\\.ts'` returns only the interface declaration (foundation-attempt-service.ts:42) and the service's own call site (:144). Every caller today is a test — so a registry entry, even if reachable, would have no production dependency to compose.\n\nCHEAPEST DISCRIMINATOR for a reviewer: a genuinely wired kind declares its membership at its definition —\n`export const EFFECT_ACTIVATE_COMMAND_KIND = \"effect.activate\" as const satisfies RuntimeCommandKind;` (activation/activation-ingress-contracts.ts:23).\nNeither `FOUNDATION_DISPATCH_COMMAND_KIND` (work/foundation-attempt-contracts.ts:39) nor `FOUNDATION_VERIFICATION_COMMAND_KIND` (evidence/foundation-verification-contracts.ts:24) carries a `satisfies RuntimeCommandKind` clause. That absence is the one-line check.\n\nPREREQUISITES NEEDED (measured symbol by symbol, per the global rail):\n(a) A `packages/contracts`-owned task adding `\"foundation.dispatch\"` and `\"foundation.verification\"` to `RUNTIME_COMMAND_KINDS` and pinning each definition with `satisfies RuntimeCommandKind`. Note this array is also what `packages/mcp/src/stdio/stdio-tool-schemas.ts:201` iterates to build the stdio tool list, and it is pinned by runtime-contract.test.ts, runtime-envelope.test.ts, stdio-schemas.test.ts and stdio-server.test.ts — so that task owns those test updates.\n(b) An `apps/daemon`-owned task resolving the two seam mismatches: an async-capable `CommandHandler` (or an async decision port), and a JSON-expressible dispatch request so `activationRequestBytes` can travel through a `JsonObject` payload without weakening the `instanceof Uint8Array` guard.\n(c) A production producer for `captureResult`.\n\nUntil all three land, DoD 1, DoD 2 and DoD 4's Foundation reach are unreachable from EVERY transport, and no test in this task could assert them without becoming exactly the mock-backed acceptance the rails forbid.\n\nWHAT IS STILL BUILDABLE, AND WHY IT WAS NOT HALF-BUILT: steps 4-6 (the pure receipts module at apps/daemon/src/host/foundation-receipts.ts + its `.js` bridge, emitted from daemon-main.ts and mcp-main.ts) close DoD 3 alone and are genuinely unblocked. They were deliberately NOT landed: steps 2 and 3 precede them in the approved plan, and step-6's justification is the host composition that cannot happen. Landing them now would be scope-narrowing under an invalid plan shape. No code was written this session; the working tree is unchanged apart from the two comments.",
      "timestamp": "2026-08-18T03:09:38.372Z"
    },
    {
      "id": "comment-09a71b01f3554a9485c28aeb17e3031f",
      "author": "governor-7610ed5a",
      "content": "GOVERNOR CHAIN UPDATE (08:10Z, sweep #4): the three prerequisites this block named are now FILED as PLANNING tasks — (a) task-a77bd540 (foundation kinds into RUNTIME_COMMAND_KINDS, clears blockers 1+2), (b) task-4bffcfdd (daemon async-handler + binary-payload seams, blockers 3+4, chained behind a77bd540), (c) task-31ea82e7 (production captureResult producer — note the telemetry-bound launcher chain landed since the original measurement, so its producer likely composes from launchActivationProviderRun's result). Re-promote this dispatch host only after (a) and (b) are DONE; (c) gates the receipts half. Original evidence: comment-50ab412b.",
      "timestamp": "2026-08-18T08:10:06.206Z"
    },
    {
      "id": "comment-ab99bfe3111b413a9ffc805d778ca70a",
      "author": "architect-6c720ab6",
      "content": "RE-MEASUREMENT AT HEAD 1aa7cdc (reopen gate met; supersedes comment-303782ab measurements). (1) foundation.dispatch is NOW FULLY WIRED — task-4bffcfdd landed the async seam: grep foundation over daemon-command-vocabulary.ts hits :60,70,100 (WiredCommandKind, WORK capability, PAYLOAD_KEYS) and daemon-command-registry.ts :121-126 (early-return entry, asyncHandler dispatchFoundationAttempt, sync refusal foundationSyncHandler); handleAsyncCommandRequest (http-adapter.ts:156) serves it and mcp-dispatch-port dispatchCommandBytes (:66-75) awaits it. The old plan's steps 2-3 (registry wiring) are OBSOLETE for dispatch. (2) foundation.verification is STILL UNWIRED — grep FOUNDATION_VERIFICATION_COMMAND_KIND over vocabulary+registry = 0 — but it is NOT named by any DoD item of this task; carved into sibling task-c52d25a41d284f8c8482d3f128c451af (created CRITICAL, PLANNING) because keeping it here made an 11-file plan (daemon hard-rejects >10). Human delegated the split decision (\"decide yourself best choice to move to prod\"); recorded here. (3) Receipts still ZERO: grep readinessReceipt|shutdownReceipt over apps/daemon/src = 0; mcp-main.ts (59 lines) still has NO signal/drain handling. (4) captureResult still null (daemon-foundation-command.ts:64) — stays with task-31ea82e750's chain, NOT this task. THIS TASK NOW = its written DoD exactly: transport-proof seam tests (DoD 1/2/4) + readiness/shutdown receipts from the two existing entries (DoD 3) + smoke/gates (DoD 5). No third entrypoint; apps/daemon/src/host/ gets only the pure receipts module.",
      "timestamp": "2026-08-18T18:54:06.812Z"
    },
    {
      "id": "comment-4603ccd0cfae4da681cf6c5e72111178",
      "author": "worker-bb4011b8",
      "content": "COLLISION DISCLOSURE from worker-bb4011b8 to worker-a211f4b4 (current claimholder). The wrapper dispatched this task to me with a RESUME banner claiming the prior session died. It did not: moe.list_workers shows worker-a211f4b4 ALIVE, 6s since last activity, READING_CONTEXT on this task. I did not release, steal, or replace your claim. Every moe.* write I attempted was refused NOT_ALLOWED, so the board is untouched by me: step-5 is still IN_PROGRESS with no note.\n\nBUT I ALREADY WROTE TO A SHARED FILE BEFORE I DETECTED YOU. Read this before you touch step 5.\n\nFILE: apps/daemon/src/mcp-dispatch-port.test.ts, 212 -> 492 lines, UNCOMMITTED, on disk now. I APPENDED; I clobbered nothing (the file matched its pre-existing 212-line content when I read it). If you have your own step-5 work, your write wins and nothing of yours is lost. If you do not, this is step 5 already done and green - verify it yourself rather than trusting me.\n\nMEASURED, not claimed:\n- pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/mcp-dispatch-port.test.ts -> Test Files 1 passed, Tests 16 passed (9 pre-existing + 7 new), EXIT=0\n- pnpm --filter @moe/daemon typecheck -> EXIT=0\n\nWHAT THE 7 CASES DO (all over production composition: createMcpDispatchPort over seamHarness = real createStoreDependencies + real store + real registry; no injected dispatcher):\n(a) transport parity - two INDEPENDENT harnesses so neither answer is the other's replay; stdio arm has fallbackCredential, HTTP arm has NO fallback so its credential can only arrive via HttpDispatchContext; asserts the launch refusal code+layer FIRST (two identical AUTHENTICATION_FAILED answers are also byte-identical), then httpAnswer === stdioAnswer, then both stores' DISPATCH_AGGREGATE == [FoundationDispatchReserved, FoundationAttemptRecorded].\n(b) duplicate concurrent delivery - same commandId via Promise.all; sorted code pair [<launch refusal>, FOUNDATION_ATTEMPT_DISPATCH_IN_PROGRESS], then ROW COUNTS read back: exactly 1 Reserved, 1 Recorded, 1 EffectActivationCommitted, 1 attempt record. Deterministic, not timing luck - the store is synchronous so call A reaches its `await boundLaunch` before B begins, and B always hits reservation REPLAYED with the record still absent.\n(c) advisory writes nothing - events.read AND a fully valid credentialled foundation.dispatch envelope pushed at dispatchQueryBytes (refused INPUT_INVALID); decision count + event horizon equal before/after.\n(d) hostile arms, exact code + exact refusing layer, rows counted before/after: malformed JSON -> INPUT_INVALID @ DECODE; oversized -> INPUT_LIMIT_EXCEEDED details.limitName JSON_STRING_UTF8_BYTES @ DECODE; stale envelope schemaVersion -> SCHEMA_VERSION_UNSUPPORTED @ DECODE; forged bearer -> AUTHENTICATION_FAILED @ AUTHENTICATE. First three delivered over BOTH transports from ONE composed port, answers asserted byte-identical.\n\nTHREE FINDINGS THAT CORRECT THE PLAN'S STEP-5 TEXT:\n1. \"oversized payload (FOUNDATION_ATTEMPT_MAX_REQUEST_BYTES)\" is UNREACHABLE from this transport. base64 of 1 MiB is ~1.33 MiB, over the envelope decoder's 256 KiB MAX_JSON_STRING_UTF8_BYTES bound, so DECODE always answers first. The test asserts that ordering as a derived property over the two production constants, then pins the DECODE code. It does not pretend to reach the codec ceiling.\n2. \"stale protocolVersion\" is NOT REPRESENTABLE at this seam: mcp-dispatch-port.ts:66-75 pins protocolVersion: WIRE_PROTOCOL_VERSION itself. The caller-controlled staleness vector is the envelope schemaVersion, which is what the arm uses.\n3. A forged bearer is HTTP-ONLY by construction - stdio has exactly one identity, the process credential - so that arm runs on the HTTP holding alone and the test says so.\n\nTYPE SEAM YOU WILL HIT: createMcpDispatchPort is declared StdioDispatchPort (1-arg dispatchCommandBytes). Passing a context needs the value held as HttpDispatchPort - exactly what production does at mcp-http/mcp-http-host.ts:129, and the existing idiom at planning/graph-query-test-fixtures.ts:407. My first typecheck caught this as TS2554 x2.\n\nNOTHING ELSE ON DISK IS MINE. Steps 1-4 output (host/foundation-receipts.{ts,js,test.ts}, mcp-main.ts, daemon-main.ts, daemon-main.test.ts) was already there when I arrived, from the earlier session on this task. Steps 6 and 7 are untouched by me: no gate legs run, no stdio smoke, no drills, nothing committed, nothing staged.",
      "timestamp": "2026-08-18T19:25:45.253Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-09T12:29:11.468Z",
  "updatedAt": "2026-08-18T19:58:51.820Z",
  "metrics": {
    "firstClaimAt": "2026-08-09T14:28:11.847Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 8,
    "doneAt": "2026-08-18T19:58:51.820Z",
    "wallClockMs": 797439973
  },
  "contextFetchedBy": [
    "architect-db2146e3",
    "governor-7c5e6be6",
    "architect-94dd1835",
    "governor-a627169a",
    "worker-f144c751",
    "governor-c2f49231",
    "architect-9b3c5cf3",
    "architect-f97d1ef5",
    "architect-6c720ab6",
    "worker-bb4011b8",
    "worker-a211f4b4",
    "qa-bbdecc14"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorHandoffs": [
    {
      "whatIsDone": "Steps 1-4 of 9: foundation-receipts tests, foundation-receipts.ts, daemon-main.ts receipt wiring, mcp-main.ts drain half. WIP is UNCOMMITTED in the shared worktree: modified daemon-main.ts/mcp-main.ts/daemon-main.test.ts, untracked src/host/ and mcp-main.test.ts.",
      "whatRemains": "Steps 5-9 including the missing mcp-main.js bridge (runtime-entrypoint.test.ts reddens the whole daemon gate on it for every fleet member), gates, adversarial review.",
      "pitfalls": "Do NOT revert the uncommitted litter; it is live WIP. Land the one-line mcp-main.js bridge EARLY to unred the shared gate. Governor commits f208038/03fd290 are already committed; do not re-stage.",
      "openQuestions": "Diff untracked src/host/ against the step-5 spec to see if step 5 was partially started.",
      "releasedAt": "2026-08-18T19:17:41.851Z",
      "releasedBy": "governor-f4cdc6ee",
      "reason": "Confirmed crash: worker-bb4011b8 wrapper exited CLI exit=1 at 19:14:26Z mid-step."
    },
    {
      "whatIsDone": "Step 1 landed in full (measurement recorded in comment-303782aba93540b7ae787a8264b49c40). Step 2 was entered, measured, and stopped on four hard blockers at HEAD 8222b9e; full evidence in comment-50ab412b071647858f6b6a2920810495 and mirrored in blockedReason. NO CODE WRITTEN — working tree carries no source path from this task.",
      "whatRemains": "Steps 2-3 (wiring foundation.dispatch/foundation.verification into daemon-command-vocabulary.ts + daemon-command-registry.ts) are IMPOSSIBLE inside the owned paths and must not be attempted as-specified. Steps 4-6 (receipts, DoD 3) are unblocked but deliberately NOT half-built, so they are still open. Three prerequisite tasks must be filed and land first: (a) a packages/contracts task adding both kinds to RUNTIME_COMMAND_KINDS with 'satisfies RuntimeCommandKind'; (b) an apps/daemon task fixing the sync-CommandHandler-vs-async-service and Uint8Array-vs-JsonObject seams; (c) a captureResult producer (zero production producers exist).",
      "pitfalls": "DO NOT re-plan this task from the description alone — the description's premise that one registry edit serves all three transports is DISPROVEN. runtime-envelope.ts:141 refuses on isCommandKind BEFORE the registry is consulted, so that premise holds only for kinds already in the frozen 92-member vocabulary (zero foundation-prefixed). Blockers 3 (sync CommandHandler vs async dispatch()/verify()) and 4 (activationRequestBytes: Uint8Array vs JsonObject payload; base64 dies on FOUNDATION_ATTEMPT_REQUEST_MALFORMED at foundation-attempt-codec.ts:89) SURVIVE closing the enum — re-mapping onto effect.activate/evidence.run does not rescue it. Also: this release clears the assignee only. Status MUST stay BLOCKED; if you find it WORKING, someone ran unblock_worker and the reason may have been wiped — read the two comments above before trusting the task text.",
      "openQuestions": "Who files the three prerequisite tasks (a/b/c)? packages/contracts is outside this task's owned paths, so the enum change needs its own owner. Governor/architect call.",
      "releasedAt": "2026-08-18T03:12:52.334Z",
      "releasedBy": "worker-f144c751",
      "reason": "Releasing my own assignment on a task I blocked 2 min ago, to free the worker slot for CRITICAL task-c7f2530b. The block is REAL and must stay: status must remain BLOCKED. Not using unblock_worker deliberately — it flips BLOCKED to WORKING and destroys blockedReason, re-serving a known trap to the next worker."
    },
    {
      "whatIsDone": "Re-read prior remeasurements, confirmed the @moe/mcp HTTP root/composition gap is closed, confirmed Foundation daemon surface is still absent/blocked, and reported this host BLOCKED only on task-5e43.",
      "whatRemains": "After task-5e43 is QA-approved DONE, probe the bare @moe/daemon Foundation surface and plan the real host; include event cursor advancement and bounded session reclamation tests.",
      "pitfalls": "Do not rebuild the existing HTTP adapter/host, deep-import, inject fake dispatch/session authority, or settle for a single well-formed but frozen events.read page.",
      "openQuestions": "Exact Foundation surface namespace remains unknown until 5e43 lands.",
      "releasedAt": "2026-08-15T18:32:02.514Z",
      "releasedBy": "architect-94dd1835",
      "reason": "Remaining public-surface dependency measured; free architect capacity."
    },
    {
      "whatIsDone": "Verified official stdio/HTTP MCP implementations on disk, proved HTTP is not exported from @moe/mcp root, proved Foundation daemon ingress and owned host files are absent, reported the task BLOCKED, and wrote Serena handoff/gotcha memories.",
      "whatRemains": "Land task-5e43a9e294ef48fdab23817c8c6cfc45 and publish the existing Streamable HTTP adapter/type closure from @moe/mcp root; then re-probe and plan this host.",
      "pitfalls": "No deep imports, fake dispatch/session authority, custom WebSocket protocol, or overwriting foreign apps/daemon/package.json and pnpm-lock.yaml WIP.",
      "openQuestions": "A governor/human must create or amend ownership for the @moe/mcp root-surface prerequisite; moe-epic-breakdown skill was unavailable, so this architect did not invoke create_task.",
      "releasedAt": "2026-08-09T14:30:56.253Z",
      "releasedBy": "architect-db2146e3",
      "reason": "Explicit planning handoff after measuring two hard public-surface dependencies and reporting BLOCKED; release architect capacity."
    }
  ],
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-18T18:55:09.173Z",
  "planningNotes": {
    "approachesConsidered": "TASK REOPENED after its recorded gate was met (task-a77bd540 runtime kinds DONE, task-4bffcfdd async seam DONE). Re-measured everything per the staleness rail — BOTH directions hit. (1) The old plan's core (wire foundation.dispatch+verification into vocabulary/registry) is HALF-CLOSED: foundation.dispatch is fully wired end-to-end by task-4bffcfdd, so re-planning it would be a silent duplicate. (2) foundation.verification is still unwired BUT is named by NO DoD item of this task. Options: (a) keep verification wiring here + receipts = 11 distinct files, daemon hard-rejects >10; (b) split verification into a sibling task, this task closes its written DoD exactly; (c) defer verification entirely. Asked the human (product-path pressure explicit in their messages: 'decide yourself best choice to move to prod') — CHOSE (b): created sibling task-c52d25a41d284f8c8482d3f128c451af (CRITICAL) with full symbol-level measurements; no ordering dependency between the two. Receipts are emitted from the PROCESS ENTRIES (daemon-main.ts 99 lines, mcp-main.ts 59 lines), never daemon-entry.ts (245/250). Receipts module is PURE — no clock, byte-comparable builds — because DoD 3 says deterministic and a clock read voids it. mcp-main drain hooks transport-close (stdin EOF) as primary because Windows does not deliver external SIGTERM to Node handlers; signals are the POSIX bonus path.",
    "codebaseInsights": "MEASURED AT HEAD 1aa7cdc. ALREADY LANDED, DO NOT REBUILD: foundation.dispatch wiring (vocabulary :60/:70/:100, registry early-return :121-126 with asyncHandler dispatchFoundationAttempt + foundationSyncHandler sync refusal ASYNC_ENTRY_REQUIRED_CODE); async command seam handleAsyncCommandRequest (http/http-adapter.ts:156, serves BOTH entry shapes) reached by mcp-dispatch-port dispatchCommandBytes :66-75; CommandRegistryEntry.asyncHandler (http/http-contract.ts:176); binary payload seam via FOUNDATION_DISPATCH_BYTES_KEY base64 (daemon-foundation-command.ts:28); restart reconciliation (boot-reconciliation via daemon-store-dependencies.ts:18, ordered before listener per daemon-entry.ts:202); graceful shutdown in daemon-main.ts :82-96 with SIGINT/SIGTERM. STILL ABSENT (this task's whole surface): receipts (grep readinessReceipt|shutdownReceipt over apps/daemon/src = 0), mcp-main drain (grep SIGTERM|SIGINT|shutdown in mcp-main.ts = 0), transport-seam tests for foundation.dispatch (only registry/vocab/store-deps/index-surface tests mention it). OUT OF SCOPE: captureResult null (daemon-foundation-command.ts:64) — task-31ea82e750's chain owns it; foundation.verification — sibling task-c52d25a4. STDOUT IS THE MCP WIRE in mcp-main: receipts go to stderr. daemon-main log path is fine for its readiness line. FILE SIZES at HEAD: vocabulary 130, registry 236, daemon-entry 245, daemon-main 99, mcp-main 59, mcp-dispatch-port 133, daemon-foundation-command 128. Gates resolve by package NAME: @moe/mcp = packages/mcp, @moe/daemon = apps/daemon.",
    "risks": "1. A worker reading the task description literally will build a third entrypoint at host/foundation-host.ts — the premise is disproven twice over (comment-ab99bfe3, comment-303782ab); only the pure receipts module lives under host/. 2. task-c52d25a4 (sibling) edits daemon-command-vocabulary.ts/registry.ts in this SAME worktree — those files are not owned here; never stage them, and drill restores must re-read before writing. 3. daemon-entry.ts at 245/250: untouchable. 4. Receipt on stdout in mcp-main corrupts the MCP JSON-RPC stream — stderr only. 5. Windows never delivers external SIGTERM to Node handlers: drain must trigger on stdin EOF/transport close or the smoke will hang; run the smoke from PowerShell (Git Bash rewrites paths). 6. Duplicate-delivery proof needs a ROW COUNT read back from the store — a non-throwing second call is also what a double write looks like. 7. Do not commit package.json/pnpm-lock.yaml — @moe/mcp already declared; needless lock churn in a shared worktree is a real hazard. 8. vitest focused runs in apps/daemon need `--root .` and `--config package.json` shape (see memory daemon-focused-vitest-finds-zero-tests) — read the count line, never trust exit 0 alone.",
    "keyFiles": [
      "apps/daemon/src/mcp-main.ts",
      "apps/daemon/src/daemon-main.ts",
      "apps/daemon/src/mcp-dispatch-port.ts",
      "apps/daemon/src/http/http-adapter.ts",
      "apps/daemon/src/daemon-foundation-command.ts",
      "apps/daemon/src/daemon-entry.ts",
      "apps/daemon/src/daemon-store-dependencies.ts"
    ]
  },
  "budget": {
    "wallClockMs": 14400000,
    "warnedAt": "2026-08-18T03:04:20.096Z",
    "escalatedAt": "2026-08-18T03:04:20.096Z"
  },
  "planApprovedAt": "2026-08-18T18:55:11.294Z",
  "workStartedAt": "2026-08-18T03:04:37.797Z",
  "reviewStartedAt": "2026-08-18T19:49:12.225Z",
  "verification": {
    "command": "pnpm --filter @moe/mcp typecheck && pnpm --filter @moe/mcp test && pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test && pnpm typecheck",
    "exitCode": 0,
    "outputTail": "$ vitest run --root ../.. packages/mcp/src\n Test Files  9 passed (9)\n      Tests  167 passed (167)\n$ tsc --project tsconfig.json\n$ vitest run --root . --config package.json src\n RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n Test Files  150 passed (150)\n      Tests  3246 passed (3246)\n   Duration  18.58s\n$ pnpm --recursive typecheck\nScope: 19 of 20 workspace projects\napps/control-room typecheck: Done\npackages/runner typecheck: Done\napps/daemon typecheck: Done\nCHAIN_EXIT=0",
    "reportedAt": "2026-08-18T19:49:12.225Z"
  },
  "filesModified": [
    "apps/daemon/src/host/foundation-receipts.test.ts",
    "apps/daemon/src/host/foundation-receipts.ts",
    "apps/daemon/src/host/foundation-receipts.js",
    "apps/daemon/src/daemon-main.ts",
    "apps/daemon/src/daemon-main.test.ts",
    "apps/daemon/src/mcp-main.ts",
    "apps/daemon/src/mcp-main.test.ts",
    "apps/daemon/src/mcp-dispatch-port.test.ts",
    "apps/daemon/src/mcp-main.js"
  ],
  "completedAt": "2026-08-18T19:58:51.820Z",
  "reviewCompletedAt": "2026-08-18T19:58:51.820Z",
  "reviewSummary": "VERIFIED at HEAD 26aa254: 9 files, all owned; no package.json/lock churn (@moe/mcp already at HEAD:22). Five legs re-run SEPARATELY (no ;-chain/pipe). @moe/mcp typecheck EXIT 0; @moe/mcp test EXIT 0 \"Test Files 9 passed / Tests 167 passed\". @moe/daemon test EXIT 1 \"2 failed | 149 passed (151) / 2 failed | 3275 passed\"; @moe/daemon + repo typecheck EXIT 1 on src/orchestrator/demo-seed-main.ts. ALL red foreign and DATED: demo-seed-* untracked (ls-files --error-unmatch fails, error changed between runs = live peer); daemon-store-dependencies.test.ts red from foundation.verification count HEAD=0 vs worktree=1/4 (sibling task-c52d25a4 uncommitted); hydrator red CLEARED on re-run. Failing-path INTERSECT owned = EMPTY; no cause appears in 26aa254.\nOWNED SUITES GREEN, run by me: foundation-receipts + mcp-main + daemon-main + mcp-dispatch-port = \"Test Files 4 passed / Tests 45 passed\", EXIT 0.\nPLAIN-NODE STDIO SMOKE run by me from PowerShell on node src/mcp-main.ts: READY receipt on stderr with pid/projectId/storePath, official MCP initialize round-trip on stdout uncontaminated, stdin close -> SHUTDOWN(TRANSPORT_CLOSED), exit 0. Run TWICE over one store (172032 bytes, different pids, same project/store) = DoD3 restart clause.\nTWO MUTATION DRILLS by me on foundation-receipts.ts, restored by content (sha b736beb8 re-verified, git clean, HEAD unmoved): delete SHUTDOWN_BEFORE_READY arm -> 3 tests red naming that code; inject new Date() into identityOf -> byte-identity AND no-clock source-scan tests red.\nDoD1-5: production composition, no injected dispatcher; stdio/HTTP byte-identical answers plus 4 hostile arms with exact code+layer and before/after row counts; SWEPT-before-READY witness over the production provider; duplicate delivery leaves 1 reservation/1 record/1 activation. Per-file 232/156/165; daemon-entry.ts untouched at 245.\nNOTE: stdio entry does not sweep; that clause is met by the control-room HTTP entry which owns the daemon lifecycle."
}