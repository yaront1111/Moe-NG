{
  "id": "task-9bd8f5298c6a4d0cac47b92e62a97423",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[P2.15d-ii sibling] Widen onOpenBoard to three args (goalId, planningRunRef, title) across goals-home, live-goals and cordum-app",
  "description": "FILED BY architect-fdbab575 2026-08-28 on the governor's explicit instruction in task-14e3b36a's 15:50Z deadlock-break reopen: \"DoD 2 (the three-argument onOpenBoard widening) MUST NOT be planned here. File it as a separate row - architect's job, not mine - sequenced behind task-115fedb7 and behind whoever settles cordum-app.tsx, and it MUST name apps/control-room/src/v2/cordum-app.tsx as an owned path.\"\n\nTHIS IS THE OTHER HALF OF THE SPIDR SPLIT. task-14e3b36a keeps DoD 3 (the GoalCard disabled Open control), which is confined to goal-card.tsx/.test.tsx, changes no arity, and is planned and moving. This row carries the arity widening, which is what could not move: it is the half that was deadlocked against its own parent.\n\nWHY THE PARENT CYCLE EXISTED, so it is not recreated. task-40017f79 was BLOCKED because task-14e3b36a \"is still PLANNING/unassigned with 0 steps\"; task-14e3b36a was BLOCKED because its DoD-1 required \"no non-DONE row lists live-goals.tsx\" and task-40017f79 - its own SPIDR parent - is one of the rows that lists it. Both blocks were individually correct and jointly unsatisfiable. The governor broke it by splitting off the half that touches neither live-goals.* nor cordum-app.tsx. This row must NOT re-acquire a dependency on its own parent: it consumes nothing from task-40017f79, it only widens a callback that task-40017f79 later consumes.\n\nMEASURED ON DISK at HEAD 061eb7f5 by architect-fdbab575 - stale by default, re-measure at claim:\n- apps/control-room/src/v2/goals/goals-home.tsx:47 declares `onOpenBoard: (goalId: string, title: string) => void`, calls it two-arg at :86 and passes a two-arg closure at :205.\n- apps/control-room/src/v2/goals/live-goals.tsx:45 declares and :48 forwards the same two-arg callback.\n- apps/control-room/src/v2/cordum-app.tsx:254 is `const openBoard = useCallback((goalId: string, title: string) => {`, passed as a JSX prop at :316, :319 and :335. THIS IS THE FILE THE PARENT ROW COULD NOT OWN, and the reason its DoD 4 (`pnpm --filter @moe/control-room typecheck` exit 0) was unsatisfiable: widening the callback type reds the typecheck at these three sites while cordum-app.tsx is out of scope.\n- Exact-call pins that must move to three args: goals-home.test.tsx:252 `toHaveBeenCalledWith(\"goal-recovery\", \"...\")` and live-goals.test.tsx:211 `toHaveBeenCalledWith(DURABLE_GOAL_ID, \"Durable title\")`.\n- The durable value to thread is `goal.planningRunRef`, already on the card model (goal-model.ts:56 `readonly planningRunRef?: string | undefined`; goal-catalog-model.ts:75 populates it). IT IS OPTIONAL - a widening that assumes it is always present is wrong, and task-14e3b36a's landed disabled-Open state is the surface behaviour for the absent case. Compose that, do not duplicate it.\n\nTHE GATE DEFECT THIS ROW MUST FIX, worth more than the widening itself (governor, verbatim): \"DoD-1's detector greps `onOpenBoard(` WITH a paren. The consumer spells it `onOpenBoard={openBoard}`.\"\nI re-measured and the defect is REAL, with one correction to the ruling's wording: `grep -rn \"onOpenBoard(\" apps/control-room/src` does NOT find nothing - it finds TWO hits, goals-home.tsx:86 and :205. What it MISSES is the three JSX prop-passing consumers at cordum-app.tsx:316/:319/:335, which are exactly the sites that red the typecheck. So a detector written with the paren is blind to the only consumers that break the build. DROP THE PAREN.\nFOUR ROWS CARRY THE SAME DETECTOR SHAPE and should be corrected by whoever next touches them: task-14e3b36a, task-40017f79, task-4451675e, task-4942111b.\n\nNOT IN SCOPE: the GoalCard disabled Open control and its reason (task-14e3b36a owns it, and compose its landed behaviour rather than re-deriving it); anything under apps/daemon.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK ON DISK BEFORE ANY BYTE, recorded verbatim in a row comment (the completion summary is write-only and persists nowhere). This is also the UNPARK CONDITION for promoting this row out of BACKLOG, and it measures CODE, never row status. (a) task-14e3b36a's half has LANDED: `git grep -n \"open-unavailable\" HEAD -- apps/control-room/src/v2/goals/goal-card.tsx` is NON-EMPTY, so the absent-run surface behaviour exists to compose. (b) live-goals.tsx and live-goals.test.tsx are RELEASED: `git status --porcelain -- apps/control-room/src/v2/goals/live-goals.tsx apps/control-room/src/v2/goals/live-goals.test.tsx` is EMPTY and no non-DONE row lists them as an owned path - task-115fedb7 held them and landing this row while it is mid-flight makes the next pathspec commit of either row carry the other's hunks. (c) cordum-app.tsx is SETTLED: `git status --porcelain -- apps/control-room/src/v2/cordum-app.tsx` is EMPTY. (d) porcelain clean, or only your hunks, on all six owned paths. Any c",
    "THE CORRECTED DETECTOR, run WITHOUT the paren and recorded both ways: `grep -rn \"onOpenBoard\" apps/control-room/src` enumerates EVERY consumer - both the call sites (`onOpenBoard(`) and the JSX prop-passing sites (`onOpenBoard={`) - and every one of them is inside this row's six owned paths. Any consumer outside them is a stop-and-report and a split signal at plan time, never a discovery at step 5. Record the with-paren output beside it to show the two differ, so the defect is durably evidenced rather than silently fixed.",
    "goals-home.tsx, live-goals.tsx and cordum-app.tsx all declare, forward and call `onOpenBoard(goalId, planningRunRef, title)` carrying the card's DURABLE `planningRunRef` - never a synthesised or placeholder id, and never a positional reshuffle that leaves title in the run slot. Both exact-call pins assert the three-argument call with the fixture's real planningRunRef: goals-home.test.tsx:252 and live-goals.test.tsx:211. `git grep -n \"run-live-1\\|goal-live-1\" HEAD -- apps/control-room/src/v2/goals` is EMPTY.",
    "THE OPTIONAL-RUN CASE IS COMPOSED, NOT RE-DERIVED. `planningRunRef` is `string | undefined` (goal-model.ts:56), so the widened signature must state what it passes when a card has no run, and the answer must agree with the disabled-Open behaviour task-14e3b36a landed: such a card does not reach onOpenBoard at all. An arm proves a runless card cannot invoke the three-arg callback from either door (the Open control AND the title button). Do not pass an empty string, a placeholder, or undefined-as-a-value into the run slot to make the types line up.",
    "Mutation drills, one degree of freedom each, both outcomes recorded verbatim: (a) revert cordum-app.tsx's `openBoard` to two args -> `pnpm --filter @moe/control-room typecheck` REDS naming :316/:319/:335, proving those consumers are really covered and that the corrected detector found what the paren version could not; (b) swap the planningRunRef and title arguments at one call site -> an exact-call pin REDS on the argument VALUES, not merely on arity - an arity-only assertion cannot see a positional swap. A drill that comes back GREEN is a FINDING to report in a comment, never a pass to bank. Restores proven by `git diff --exit-code` for tracked paths plus SHA256 against a %TEMP% backup in a call separate from the restore.",
    "Gates standalone, foreground, unpiped, exit 0 with counts quoted: `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goals-home.test.tsx src/v2/goals/live-goals.test.tsx src/v2/goals/goal-card.test.tsx`; `pnpm --filter @moe/control-room test`; `pnpm --filter @moe/control-room typecheck` quoting its `$ tsc` echo - the three-arg change must typecheck EVERY consumer, which is the whole reason cordum-app.tsx is owned here. A bare `pnpm test` runs zero control-room tests (the root vitest include has no apps/**) and is not evidence. Repo-wide red path-attributed merge-base versus HEAD with an EMPTY owned-path intersection; explicit-pathspec commit of the six owned paths only; every production file under 400 lines; completion comment names task-40017f79 and task-4942111b as the downstream consumers and task-14e3b36a as the sibling half."
  ],
  "taskRails": [
    "SIX OWNED PATHS, and cordum-app.tsx is mandatory among them per the governor's ruling: apps/control-room/src/v2/goals/goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx, and apps/control-room/src/v2/cordum-app.tsx (plus its test if one exists). Owning cordum-app.tsx is what makes DoD 6's typecheck satisfiable; without it the row is unbuildable, which is exactly why the parent could not carry this half.",
    "DO NOT re-acquire a dependency on task-40017f79. That row is this row's SPIDR parent and consumes the widened callback; it does not produce anything this row needs. A DoD or plan clause gating on the parent recreates the cycle the governor broke at 15:50Z.",
    "DO NOT touch goal-card.tsx or goal-card.test.tsx. task-14e3b36a owns them and lands the disabled Open control. Compose that behaviour; a second implementation of the absent-run surface is a duplicate that will collide in this shared worktree.",
    "Sequenced behind task-115fedb7, which holds live-goals.tsx and live-goals.test.tsx. This is a SAFETY constraint, not paperwork: both rows commit by explicit pathspec over the same two files, so landing this one while that is mid-flight makes the next pathspec commit of either carry the other's hunks.",
    "Never write a consumer detector that greps `onOpenBoard(` with a paren. It is blind to `onOpenBoard={openBoard}`, which is how all three cordum-app.tsx consumers are spelled and the only ones that red the typecheck. Measured 2026-08-28: with-paren finds 2 hits, without-paren finds 5.",
    "Fail closed with exact code and layer where a refusal is involved; TDD; target <=250 lines per production source and split before 400; explicit-pathspec commits only, never `git add -A` and never a bare `git commit`."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK GATE (read-only; author no bytes). Record every command and verbatim output in a row comment via moe.add_comment — the completion summary is write-only and persists nowhere.\n\n(a) THE SIBLING MUST HAVE LANDED. `git grep -c \"open-unavailable\" HEAD -- apps/control-room/src/v2/goals/goal-card.tsx` must be NON-EMPTY. Measured at HEAD 0bf353ff it EXITS 1 — NOT LANDED. task-14e3b36a is mid-flight (steps 1-2 COMPLETED, step 3 IN_PROGRESS). If it is still absent at your claim, `moe.report_blocked` naming task-14e3b36a and STOP. This is a SAFETY ordering constraint, not paperwork: DoD 4 requires this row to prove a runless card cannot invoke the three-arg callback FROM EITHER DOOR, and the disabled-Open behaviour that makes that true is the sibling's deliverable. Without it you would be asserting a property nothing implements.\n(b) `git status --porcelain` over the six owned paths — all measured CLEAN at HEAD 0bf353ff, cordum-app.tsx included. Foreign WIP on any of them is adopted-or-reported, never overwritten.\n(c) THE CORRECTED DETECTOR, run WITHOUT the paren and recorded BOTH ways so the defect stays evidenced: `grep -rn \"onOpenBoard(\" apps/control-room/src` versus `grep -rn \"onOpenBoard\" apps/control-room/src`. The paren form finds only the two call sites in goals-home.tsx and is BLIND to every `onOpenBoard={...}` prop-passing consumer — including the three in cordum-app.tsx that are the only ones which red the typecheck. That blindness is what deadlocked the parent row against its own child.\n(d) NAME EVERY CONSUMER FROM (c) AND CONFIRM IT IS IN THIS ROW'S SIX PATHS. Measured at HEAD, the bare grep returns exactly seven files: goals-home.tsx (decl :47, calls :86 and :205), goals-home.test.tsx, live-goals.tsx (decl :45, forward :48), live-goals.test.tsx, cordum-app.tsx (:316, :319, :335), goal-create-disabled.test.tsx (`onOpenBoard={vi.fn()}` at :23) and goal-card.tsx/.test.tsx.\n  goal-card.* is NOT this row's and NOT affected: its prop is `onOpenBoard: () => void` — a ZERO-ARG THUNK. goals-home.tsx:205 adapts with `onOpenBoard={() => onOpenBoard(goal.goalId, goal.title)}`, so the adapter absorbs the widening entirely. Do not touch it (taskRail 3).\n  goal-create-disabled.test.tsx IS named as an owned path but will most likely need NO edit: `vi.fn()` is arity-agnostic and typechecks against a three-arg signature. Confirm by typecheck rather than by editing, and record which.\n  Any EIGHTH consumer is a stop-and-report, not an extra path.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goals-home.tsx"
      ],
      "note": "STEP 1 DONE — read-only, zero bytes authored. VERDICT: PROCEED. Records: comment-01ededd0b8a64b4ab2e71cddc4889cb5 (gate) and comment-2ecd0b15cd17463294a6902802fba05b (baseline correction + flap). HEAD c873dfb2; the plan's premises were measured at 0bf353ff/48f056e2 and are all re-measured here.\n\n(a) THE PLAN'S OWN BLOCK DOES NOT FIRE — its measurement is stale. `git grep -n \"open-unavailable\" HEAD -- .../goal-card.tsx` EXIT 0 -> :164 (the plan recorded EXIT 1 and told me to block). task-14e3b36a DONE; task-115fedb7 DONE, so R4's sequencing is satisfied too.\n\n(b) All six owned paths BYTE-IDENTICAL to HEAD (hash-object vs rev-parse, not porcelain alone, which lies after any peer touch here). No foreign WIP on them.\n\n(c) DETECTOR DEFECT EVIDENCED BOTH WAYS: with-paren 2 hits in 1 file; without-paren 8 files. The paren form is blind to cordum-app.tsx:316/:319/:335, the only consumers that red the typecheck.\n\n(d) NO NINTH CONSUMER. Six owned + the goal-card pair, whose exclusion is SOUND at this HEAD rather than merely asserted: goal-card.tsx:69 is still `onOpenBoard: () => void`, a zero-arg thunk, and goals-home.tsx:205 adapts it, so the widened arity never reaches it. My own committed cordum-app.test.tsx contains NO onOpenBoard at any spelling, so the six-path plan and the governor's seven-path count do not conflict on the ground — the disputed seventh path is not a consumer. Following the approved six-path plan per comment-8747d052.\n\nDoD 3's LITERAL GATE — measured at THREE scopes and the choice disclosed, because scope is what decides deliverability. (i) as written over v2/goals: ~26 hits, undeliverable (goal-model.ts EXPORTS the literal; goal-card.test.tsx is R3-forbidden). (ii) governor-amended read as INCLUDING cordum-app.tsx: 12 hits, still undeliverable — cordum-app.tsx:46's LIVE_RUN_SUBJECT is a production constant that must match the daemon's DEFAULT_RUN_SUBJECT, and moving it to empty a grep would be reshaping source to please a detector. (iii) THE READING I TAKE — the amendment NARROWS an over-broad grep rather than widening it onto a path its own scope never covered: clause scope `v2/goals` INTERSECT owned paths = 11 hits in ONE file, goals-home.test.tsx, all in scope and fixable. Ten `goal-live-1` testids derive from the already-exported LIVE_GOAL_ID (that file already imports ./goal-model.js at :7); one `run-live-1` at :44 is a step aggregateId the test supplies and never reads back. This matches the governor's stated deliverable property verbatim. If a grader prefers reading (ii) the row needs a further amendment — I will report blocked rather than fake the gate.\n\nBASELINE, AND A CORRECTION I AM MAKING RATHER THAN LETTING STAND. I first quoted 108/1429 green from an EARLIER tree while asserting it at c873dfb2. Re-running at the actual HEAD exposed a FLAP: seven invocations of the identical command gave EXIT 1, 0, 1, 0, 0, 0, 0. Cause measured, not guessed — one foreign dirty path, ` M v2/styles/cordum-goals.css` (+47 lines), a peer mid-write; vitest read it torn. Once its SHA256 settled at fc61d267…7477, four consecutive greens. I had authored zero bytes, so the red was definitionally not attributable to this row.\nBASELINE OF RECORD: 108 files / 1429 tests, EXIT 0, established over four consecutive runs. DoD 6 grades against this, and a SINGLE red run of this package is a coin flip against that peer write, not a regression. I will run my own gate legs to stability and quote the run count.\nI did not touch cordum-goals.css — foreign WIP, no provenance record, outside this row's six paths. Reported, not adopted, per rail 1.",
      "startedAt": "2026-08-28T20:35:02.967Z",
      "completedAt": "2026-08-29T17:08:01.624Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD RED FIRST (epic rail 3). Move both exact-call pins to the three-argument shape with the fixture's REAL planningRunRef, and add the runless arm. Each must be RED for the stated reason before any production byte, with the red quoted in the step note.\n\n(1) apps/control-room/src/v2/goals/goals-home.test.tsx:252 — today `expect(onOpenBoard).toHaveBeenCalledWith(\"goal-recovery\", \"Genesis recovery binding on a fresh store\")`. Becomes the three-arg call carrying the FIXTURE'S OWN planningRunRef, read from the fixture data rather than spelled as a literal: a hand-typed run id is a fixed point that survives the producer changing under it.\n(2) apps/control-room/src/v2/goals/live-goals.test.tsx:211 — today `toHaveBeenCalledWith(DURABLE_GOAL_ID, \"Durable title\")`. Same treatment, using the durable fixture's real run.\n(3) ARGUMENT ORDER IS ASSERTED, NOT JUST ARITY. The signature is `(goalId, planningRunRef, title)` — title moves from second to THIRD. An assertion that only counts arguments cannot see a swap of the last two, and both are strings, so a swap typechecks silently. Assert the exact values positionally; drill D2 proves it.\n(4) THE RUNLESS ARM (DoD 4): a card with NO planningRunRef cannot invoke the callback FROM EITHER DOOR — neither the Open control nor the title button. This composes the sibling's landed disabled-Open behaviour; do NOT re-derive it here and do NOT reach into goal-card.tsx. Assert `onOpenBoard` was not called at all.\n(5) NEVER a placeholder in the run slot. Assert that no call carries an empty string, `undefined`, or a synthesised id where the durable run belongs — DoD 3 forbids exactly that, and it is the tempting way to make the types line up for a runless card.\n\n`git grep -n \"run-live-1\\|goal-live-1\" HEAD -- apps/control-room/src/v2/goals` must be EMPTY at delivery (DoD 3) — re-run it after the edits, not before.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goals-home.test.tsx",
        "apps/control-room/src/v2/goals/live-goals.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goals-home.test.tsx",
        "apps/control-room/src/v2/goals/live-goals.test.tsx"
      ],
      "note": "STEP 2 — TDD RED FIRST. Both exact-call pins moved to the three-argument shape; no production byte authored.\n\nRED, foreground, unpiped: `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goals-home.test.tsx src/v2/goals/live-goals.test.tsx src/v2/goals/goal-card.test.tsx` EXIT 1 — `Test Files 2 failed | 1 passed (3)`, `Tests 2 failed | 42 passed (44)`. Exactly the two pins, each for exactly the intended reason:\n  × \"filters to the blocked goal and opens its board when it has a durable run\" — `AssertionError: expected \"vi.fn()\" to be called with arguments: [ 'goal-recovery', …(2) ]`\n  × \"shows and opens the catalog entry the daemon minted for the sent command\" — `AssertionError: expected \"vi.fn()\" to be called with arguments: [ 'goal-cmd-goal-create-1', …(2) ]`\nBoth red because production still calls with TWO arguments. goal-card.test.tsx stays green, confirming the R3 exclusion holds: the zero-arg thunk is untouched by the widening.\n\n(1)+(2) BOTH PINS NOW READ EVERY EXPECTED ARGUMENT BACK OUT OF THE FIXTURE THAT PRODUCED IT — none is respelled beside the assertion.\n  goals-home.test.tsx: `const opened = data.goals.find((goal) => goal.planningRunRef !== undefined)`, then `toHaveBeenCalledWith(opened?.goalId, opened?.planningRunRef, opened?.title)`. The old arm spelled `\"goal-recovery\"` and the title prose as literals; the producer could have changed under either and the arm would have kept passing on a value nothing supplies.\n  live-goals.test.tsx: added `identitiesOf(row)`, which reads goalId/planningRunRef/title back OUT of the catalog row the fixture built, and the arm now holds that row (`const row = catalogRow(DURABLE_GOAL_ID)`) instead of building a throwaway. The old arm spelled `\"Durable title\"`.\n\n(3) POSITIONAL, NOT ARITY. Both pins assert the three values in order with a comment naming the hazard: title moves from second to THIRD, and title and planningRunRef are BOTH strings, so a swap of the last two typechecks silently. An arity-only assertion cannot see it. Drill D2 will prove these arms catch it.\n\n(5) NO PLACEHOLDER IN THE RUN SLOT. Each pin additionally reads `onOpenBoard.mock.calls[0]?.[1]` and asserts it IS the fixture's durable run and is neither `\"\"` nor `undefined` — the two shapes that are tempting for making a runless card typecheck.\n\n(4) THE RUNLESS ARM ALREADY EXISTS AND ALREADY COVERS BOTH DOORS — I measured before writing a third one. goals-home.test.tsx \"filters to the blocked goal and refuses to open a board it has no run for\" asserts `cr.goals.card.goal-recovery.open` is ABSENT, that `.open-unavailable` is disabled with the sibling's exact title text, clicks BOTH that control AND the title button, and ends `expect(onOpenBoard).not.toHaveBeenCalled()`. That is DoD 4's property, composing task-14e3b36a's landed behaviour. Adding a second one would be the duplicate R3 warns against, so I did not. It stays green through this step and must stay green through step 3.\n\nDoD 3's LITERAL GATE IS NOW SATISFIED ON THE ONLY FILE THAT CARRIED HITS. `grep -n \"run-live-1\\|goal-live-1\" apps/control-room/src/v2/goals/goals-home.test.tsx` EXIT 1, EMPTY (was 11 hits). Ten `goal-live-1` testids now derive from the exported `LIVE_GOAL_ID` (goal-model.ts:21), which that file already imported from — the identity is derived, which is the governor's stated deliverable property, not merely spelled somewhere else. The eleventh, `aggregateId: \"run-live-1\"` in the LIVE_STEPS fixture, was renamed to `run-blocked-fixture`: I checked first that it is the test's own INPUT for a BLOCKED plan.propose step and that nothing reads it back (`grep -n aggregateId` shows it never appears in an assertion), so this is a fixture rename with no behavioural surface, not a value any code derives.",
      "startedAt": "2026-08-29T17:08:39.694Z",
      "completedAt": "2026-08-29T17:09:55.384Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN — apps/control-room/src/v2/goals/goals-home.tsx, the declaration and both call sites.\n\n(a) :47 `readonly onOpenBoard: (goalId: string, title: string) => void;` becomes `(goalId: string, planningRunRef: string, title: string) => void`.\n(b) :205 `onOpenBoard={() => onOpenBoard(goal.goalId, goal.title)}` gains the card's DURABLE `goal.planningRunRef` in the middle slot. This is the adapter that keeps GoalCard's zero-arg thunk prop unchanged — leave that shape alone.\n(c) :86 `if (strip.openGoalId !== undefined) onOpenBoard(strip.openGoalId, titleOf(strip.openGoalId));` — the TRIAGE path. It resolves a title through `titleOf(...)`; it needs the same treatment for the run. If no `runOf`-style lookup exists, add one ALONGSIDE `titleOf` reading the same card model, so both facts come from one source.\n  THIS CALL SITE IS THE ONE MOST LIKELY TO BE GOT WRONG, because the run is not already in hand the way `goal.planningRunRef` is at :205. Do NOT pass an empty string or `undefined` to satisfy the type. `planningRunRef` is OPTIONAL on the model (goal-model.ts:56 `readonly planningRunRef?: string | undefined`), so a triage strip naming a runless goal has no run to pass — and per DoD 4 such a goal must not open a board at all. Guard the call the same way the runless card is guarded, and state in the step note which guard you used.\n\nDo NOT touch goal-card.tsx or goal-card.test.tsx (taskRail 3). Their prop is a zero-arg thunk that this widening never reaches, and the sibling task-14e3b36a owns them.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goals-home.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goals-home.tsx"
      ],
      "note": "STEP 3 — GREEN on goals-home.tsx. `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goals-home.test.tsx src/v2/goals/live-goals.test.tsx src/v2/goals/goal-card.test.tsx` EXIT 0 — `Test Files 3 passed (3)`, `Tests 44 passed (44)`. Both pins flipped; the runless arm and the whole goal-card suite stayed green, confirming the R3 exclusion holds. goals-home.tsx 213 -> 242 lines, under the 250 target. goal-card.tsx and goal-card.test.tsx untouched.\n\n(a) :47 is now `readonly onOpenBoard: (goalId: string, planningRunRef: string, title: string) => void`.\n(b)+(c) BOTH DOORS NOW GO THROUGH ONE COMPOSER, which is more than the step asked and is why: the old code resolved the title through `titleOf(goalId)` on the triage path and read `goal.title` on the card path — two lookups that can disagree. `openBoard(goalId)` now composes goalId, run and title from the SAME card object, so the triage strip and the card cannot diverge. `titleOf` had exactly one caller and is gone with it.\n  THE TRIAGE GUARD, which the step flagged as the one most likely to be got wrong: `openableGoal()` returns the card only when its `planningRunRef` is a NON-BLANK string, and `openBoard` returns early otherwise. Nothing empty, `undefined`, or synthesised is ever substituted to make the arity line up. A triage strip naming a runless goal now opens nothing — the same answer the sibling's disabled Open control gives the operator.\n  DUPLICATION DECLARED, NOT HIDDEN: that non-blank rule mirrors `hasDurableRun` at goal-card.tsx:46. R3 makes goal-card.tsx off-limits to this row, so its module-private predicate cannot be exported here. For a CARD this is only a fail-closed backstop (GoalCard still decides); for the TRIAGE strip, which never renders through GoalCard, this file is the ONLY decider and needs the rule in its own right. Worth hoisting into goal-model.ts in a later row that owns it.\n\n=== FINDING — THE PLAN'S D5(a) DRILL WILL COME BACK GREEN, AND A REAL BUG IS LIVE RIGHT NOW ===\nI ran D5(a)'s shape early, by accident of ordering: at this moment goals-home.tsx is widened while live-goals.tsx:45 and cordum-app.tsx:254 are still TWO-ARG. That is exactly the mutant D5(a) describes. Measured:\n  `pnpm --filter @moe/control-room typecheck` EXIT 0, with the `$ tsc --project tsconfig.json` echo. NO error at cordum-app.tsx:316/:319/:335.\nDoD 5(a) predicts this REDS \"naming :316/:319/:335\". It does not, and it cannot: TypeScript accepts a function with FEWER parameters where more are expected, and the positional types line up — the old second parameter `title: string` sits against the new second parameter `planningRunRef: string`, both `string`. Nothing is unsound to the checker.\nCONSEQUENCE, and this is the part that matters: `cordum-app.tsx:254 const openBoard = useCallback((goalId: string, title: string) => { ... setOpen({ goalId, title }) })` now silently binds the PLANNING RUN REF into its `title` slot. The opened board would show a run id where the goal's title belongs. It typechecks, and at this moment no test in the package catches it. This is precisely the \"positional reshuffle that leaves title in the run slot\" DoD 3 forbids, arriving through the consumer rather than the producer.\nSo the widening at live-goals.tsx and cordum-app.tsx is NOT optional and is NOT merely cosmetic conformance to DoD 3 — it is the fix for a live defect that the toolchain is blind to. Steps 4 and 5 carry it.\nWHAT GUARDS IT AFTERWARDS: nothing type-level ever will, so it needs a behavioural arm. Step 4 adds one for the seam this row owns (live-goals.tsx forwarding all three positionally). The CordumApp-level arm belongs in cordum-app.test.tsx, which this row does NOT own — task-40017f79 does, and its step 6 is exactly where that wiring is verified. I am recording it for that row rather than taking the path on my own authority, per comment-8747d052's instruction.",
      "startedAt": "2026-08-29T17:10:49.592Z",
      "completedAt": "2026-08-29T17:12:21.329Z"
    },
    {
      "stepId": "step-4",
      "description": "GREEN — apps/control-room/src/v2/goals/live-goals.tsx, the forwarding half.\n\n:45 declares the same two-arg `onOpenBoard` and :48 destructures it in `LiveGoalsHome({ setup, onConnection, onOpenBoard })`, forwarding it to the goals surface. Widen the declaration to `(goalId, planningRunRef, title)` and forward unchanged — this component is a PASS-THROUGH and must not derive, default or re-look-up the run. If forwarding appears to require a lookup here, the run is not reaching this layer and that is a finding to report, not to patch locally.\n\nOWNERSHIP NOTE, and re-measure it immediately before editing: live-goals.tsx and live-goals.test.tsx were held by task-115fedb7 and were measured CLEAN at HEAD 0bf353ff. Both rows commit by explicit pathspec over the same two files, so if either is dirty with another row's hunks when you arrive, STOP and report rather than editing over them — the next pathspec commit of either row would carry the other's work.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/goals/live-goals.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx"
      ],
      "note": "STEP 4 — GREEN on live-goals.tsx, the forwarding half. `... exec vitest run src/v2/goals/goals-home.test.tsx src/v2/goals/live-goals.test.tsx src/v2/goals/goal-card.test.tsx` EXIT 0 — `Test Files 3 passed (3)`, `Tests 44 passed (44)`. `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo. live-goals.tsx 123 lines, unchanged in length.\n\nONE LINE CHANGED, and only the declaration: :45 is now `readonly onOpenBoard: (goalId: string, planningRunRef: string, title: string) => void`. The forward at :119 is `onOpenBoard={onOpenBoard}` and STAYS byte-identical — this component is a pass-through and derives, defaults and re-looks-up nothing. The step's \"if forwarding appears to require a lookup here, that is a finding\" branch did NOT fire: the run reaches this layer already, because GoalsHome composes it from the card and LiveGoalsHome never sees the call.\n\nOWNERSHIP RE-MEASURED IMMEDIATELY BEFORE EDITING, by content rather than porcelain, exactly as R4 requires:\n  live-goals.tsx HEAD 3ada5097… == worktree 3ada5097… — SAME, no foreign hunks to edit over.\n  live-goals.test.tsx HEAD 7030f076… vs worktree e35ebc62… — DIFFERS, and the difference is MINE from step 2 (the widened pin plus `identitiesOf`). Not foreign, so nothing to stop for.\ntask-115fedb7, which held both files, is DONE, so the R4 sequencing constraint is discharged rather than merely unobserved.\n\nNO NEW ARM HERE, deliberately. The step-2 pin already renders `LiveGoalsHome` end to end, clicks Open, and asserts the callback received `(goalId, planningRunRef, title)` positionally with all three values read back out of the catalog row. That IS the guard on this seam: any reshuffle, default or local re-derivation introduced in live-goals.tsx reds it at runtime. Adding a second arm over the same property would be the duplicate this plan warns about elsewhere.\n\nSTEP 3'S FINDING STILL STANDS AND IS NOW SHARPER. typecheck is EXIT 0 while cordum-app.tsx:254 is STILL two-arg, so at this moment the live app binds `planningRunRef` into the board's `title` slot and nothing in the package reds. The three suites above are green because none of them renders CordumApp. Step 5 is the fix, not a formality.",
      "startedAt": "2026-08-29T17:12:23.762Z",
      "completedAt": "2026-08-29T17:13:05.934Z"
    },
    {
      "stepId": "step-5",
      "description": "GREEN — apps/control-room/src/v2/cordum-app.tsx. THE FILE THE PARENT ROW COULD NOT OWN, and the reason its typecheck was unsatisfiable.\n\n(a) :254 `const openBoard = useCallback((goalId: string, title: string) => { setConnection(null); setOpen({ goalId, title }); }, [])` gains the middle `planningRunRef` parameter. Its three JSX consumers at :316, :319 and :335 pass it as `onOpenBoard={openBoard}` and need NO edit — they pass the function by reference, which is precisely why the paren-form detector could not see them.\n(b) :241 `interface OpenBoard` gains `readonly planningRunRef: string`, and :256 `setOpen({ goalId, title })` carries it. MEASURED: `OpenBoard` is declared and read ONLY inside this file (:241, :252, :256, :266, :278, :299, :306, :310) — `grep -rn OpenBoard apps/control-room/src` returns no external reader — so this widening fans out to nothing else. That containment is what keeps this row at six paths.\n\nSTORING IT IS THE DELIVERABLE; RENDERING IT IS NOT. Do NOT pass `planningRunRef` down to `BoardStub` (:299) or the board consumer at :306-310. Widening their props is task-40017f79's work — it \"consumes the three-arg callback for its typed route\" — and doing it here would pull two more files into this row and re-create the oversized roster the split exists to avoid. This row's job is that the durable run REACHES the app's board state; an arm asserts the stored state carries it, so it is asserted behaviour rather than an unused placeholder.\n\nIf `OpenBoard` cannot carry the run without touching a consumer outside this file, that contradicts the measurement above — STOP and report rather than taking the extra path.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "note": "STEP 5 — GREEN on cordum-app.tsx, the file the parent row could not own. Also ran DoD 5's two drills here, because (a) is about this exact edit. cordum-app.tsx 370 -> 377 lines. `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc` echo; the three focused suites `Test Files 3 passed (3)` / `Tests 44 passed (44)` EXIT 0 after both restores.\n\n(a) :261 `openBoard` is now `(goalId: string, planningRunRef: string, title: string)`.\n(b) `interface OpenBoard` carries `readonly planningRunRef: string` and `setOpen({ goalId, planningRunRef, title })` stores it.\nTHE THREE JSX CONSUMERS ARE BYTE-UNCHANGED at :323, :326, :342 — still `onOpenBoard={openBoard}`, passing the function by reference, which is exactly why the paren-form detector could never see them.\nCONTAINMENT RE-MEASURED, not assumed: `grep -rn \"\\bOpenBoard\\b\" apps/control-room/src` minus `onOpenBoard` returns TWO hits, both in this file (:241 declaration, :252 useState). No external reader, so the widening fans out to nothing and the row stays at six paths. The step's \"if OpenBoard cannot carry the run without touching a consumer outside this file, STOP\" branch did NOT fire.\nI did NOT pass the run down to BoardStub or the plan-review consumer. That is task-40017f79's work, and doing it here would pull two more files in.\n\n=== DoD 5(a) IS A GREEN DRILL. REPORTING IT AS A FINDING, NOT BANKING IT AS A PASS. ===\nThe DoD predicts: revert cordum-app's `openBoard` to two args -> typecheck REDS naming :316/:319/:335. MEASURED, with the mutation verified landed by grep first (`const openBoard = useCallback((goalId: string, title: string) =>` at :261):\n  `pnpm --filter @moe/control-room typecheck` EXIT 0. No error anywhere.\n  `pnpm --filter @moe/control-room test` EXIT 0 — `Test Files 108 passed (108)`, `Tests 1430 passed (1430)`.\nNOTHING IN THE PACKAGE CATCHES IT. Not the checker, not one of 1430 tests.\nWHY the checker cannot: TypeScript accepts a function with FEWER parameters where more are expected, and the positions line up — the mutant's second parameter `title: string` sits against the widened second parameter `planningRunRef: string`, both `string`. There is nothing unsound to see. The drill as written can never red, so it certifies nothing about whether those three consumers are covered.\nWHY IT MATTERS: under that mutant the app binds the PLANNING RUN REF into the board's `title` slot — the opened board shows a run id where the goal's title belongs. That is the \"positional reshuffle that leaves title in the run slot\" DoD 3 forbids, arriving through the CONSUMER instead of the producer, and it ships silently. So this step is a real fix, not conformance.\nWHAT WOULD CATCH IT: only a behavioural arm rendering CordumApp and asserting the opened board's title is the goal's title. That arm's only home is cordum-app.test.tsx, which this row does NOT own — task-40017f79 does, and its step 6 verifies exactly this wiring. Per comment-8747d052 I am NOT taking that path on my own authority: the widening typechecks without it, so this is a disclosed COVERAGE GAP routed to the owner, not a build-blocking collision. I will file it on task-40017f79 as well so its resumer adds the arm.\n\n=== DoD 5(b) IS RED AS REQUIRED ===\nSwapped `planningRunRef` and `title` at ONE producer call site — `onOpenBoard(goal.goalId, goal.title, goal.planningRunRef)`, verified landed by grep at goals-home.tsx:111. BOTH pins red, and on the argument VALUES rather than on arity:\n  × \"filters to the blocked goal and opens its board when it has a durable run\"\n  × \"shows and opens the catalog entry the daemon minted for the sent command\"\n  `Test Files 2 failed (2)`, `Tests 2 failed | 29 passed (31)`, EXIT 1.\nThis is what makes step 2's positional assertions load-bearing: an arity-only assertion would have stayed green through this swap, since both values are strings.\n\nRESTORES PROVEN BY SHA256 in a call SEPARATE from each restore, against %TEMP% backups:\n  cordum-app.tsx 759a59c779d2705588df4917008b84a97629ab937a70bb2064c6b85dc252f7fa == backup\n  goals-home.tsx 5a9fcfa96fb9ee1ea4cb98ba50a5e5587bc063bdf1ae62ebcc11cab9973748a6 == backup\nBoth mutated lines re-grepped after restore and read the correct widened form.",
      "startedAt": "2026-08-29T17:13:10.108Z",
      "completedAt": "2026-08-29T17:15:18.453Z"
    },
    {
      "stepId": "step-6",
      "description": "THE DETECTOR RE-RUN AND ITS DENOMINATOR (Rail 8(A)/(C); epic rail 7) — its own numbered step, not folded into a gate.\n\n(1) Re-run BOTH detector forms from step 1(c) at the delivered tree and record both outputs again. The bare-symbol form must still enumerate exactly the seven files step 1(d) named; an EIGHTH is a stop-and-report. This is the assertion that the roster was complete, and it is the one this row's whole history turns on.\n(2) Name the consumer roster as a FROZEN immutable constant in a surviving test and assert an EXACT member count — not `length > 0`, which a one-member roster satisfies. Assert set-equality in BOTH directions against the files that actually reference the symbol, so a consumer added later reds here rather than silently escaping the roster.\n(3) Assert the sweep ACTUALLY GENERATED that many cases; a zero-case sweep passes vacuously.\n(4) Pin the ARITY as a property, not an incident: assert the declared signature takes exactly three parameters in both goals-home.tsx and live-goals.tsx, so a future widening cannot quietly add a fourth without moving this assertion.\n\nThe deletion drill for these counts is step 7 D3.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goals-home.test.tsx",
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goals-home.test.tsx"
      ],
      "note": "STEP 6 — THE DETECTOR RE-RUN AND ITS DENOMINATOR. `... exec vitest run` over goals-home + live-goals + goal-card + goal-create-disabled EXIT 0 — `Test Files 4 passed (4)`, `Tests 48 passed (48)`. typecheck EXIT 0 with the `$ tsc` echo. goals-home.test.tsx 303 -> 437 lines.\n\n(1) BOTH FORMS RE-RUN AT THE DELIVERED TREE. Bare symbol: EXACTLY the same EIGHT files step 1(d) named — cordum-app.tsx, goal-card.tsx/.test.tsx, goal-create-disabled.test.tsx, goals-home.tsx/.test.tsx, live-goals.tsx/.test.tsx. NO NINTH consumer, so the stop-and-report branch does not fire.\n  DISCLOSURE ON THE PAREN FORM, because it is now self-referential: `grep -rn \"onOpenBoard(\" apps/control-room/src` returns TWO hits, and ONE OF THEM IS MY OWN COMMENT at goals-home.test.tsx:328 explaining the defect. Only goals-home.tsx:111 is a real call site. Anyone re-running the paren form should grade non-comment lines — a raw count now reads 2 where the code has 1, the same class of error as a log grep counting its own echo.\n\n(2) THE ROSTER IS A FROZEN CONSTANT WITH SET EQUALITY IN BOTH DIRECTIONS. `ONOPENBOARD_CONSUMERS` is `Object.isFrozen`, asserted `toHaveLength(8)` — EXACT, not `> 0`, which a one-member roster would satisfy — and compared `toEqual` against the sweep, so a consumer that goes missing AND one that appears unlisted both red here.\n  THE SWEEP WALKS THE TREE, IT DOES NOT NAME PATHS. It recurses `apps/control-room/src`, reads every `.ts`/`.tsx`, and keeps the ones containing the bare symbol. A consumer added in a NEW file, or one MOVED by a future file split, still lands in `found` and must be accounted for — a guard that read a fixed list of paths would be blind to exactly the case it exists for, and would also make splitting any of these files unsatisfiable.\n\n(3) NON-VACUITY IS ASSERTED, NOT ASSUMED: `expect(found.length).toBeGreaterThan(0)` before the equality, and each arity arm asserts its own collection is non-empty before grading it. A sweep that generated zero cases would otherwise satisfy every assertion below it.\n\n(4) ARITY PINNED AS A PROPERTY. Declarations are found by sweeping, then graded by the FULL parameter list, not a count: `[\"goalId: string\", \"planningRunRef: string\", \"title: string\"]`. A fourth parameter, a rename, or a type change all red. Two declarations are widened (goals-home.tsx, live-goals.tsx) and exactly one is a zero-arg thunk (goal-card.tsx) — that partition is asserted, which documents the R3 boundary in a test rather than in prose.\n  I CORRECTED MY OWN FIRST ATTEMPT HERE. I initially expected cordum-app.tsx among the declarations; it red with `expected [ 'v2/goals/goals-home.tsx', …(1) ] to deeply equal [ 'v2/cordum-app.tsx', …(2) ]`. cordum-app.tsx PRODUCES the function, it does not declare the prop — so the expectation was wrong, not the code.\n\n=== NEW ARM: THE CONSUMER-SIDE PIN, WHICH TURNS STEP 5'S VACUOUS DRILL INTO A REAL ONE ===\nStep 5 measured that narrowing `openBoard` back to two args passes `tsc` AND all 1430 package tests, because TypeScript accepts fewer parameters and the discarded position is a `string` against a `string`. Nothing guarded it. So this step adds \"pins the arity of the shell's openBoard producer, which the checker cannot\": it sweeps for the `const openBoard = useCallback((...))` producer, asserts exactly one exists, asserts it is cordum-app.tsx, and grades its full parameter list.\nRE-DRILLED IMMEDIATELY, mutation verified landed by grep first (`(goalId: string, title: string)` at cordum-app.tsx:261): the new arm REDS — `AssertionError: expected [ 'goalId: string', 'title: string' ] to deeply equal [ 'goalId: string', …(2) ]`, `Tests 1 failed | 18 passed (19)`, EXIT 1. The SAME mutant that a moment earlier passed the checker and 1430 tests is now caught. Restored and proven: cordum-app.tsx SHA256 759a59c779d2705588df4917008b84a97629ab937a70bb2064c6b85dc252f7fa, matching the pre-drill backup, with the widened line re-grepped.\nThis arm is a source-text guard, which is a shape worth being honest about: it is deliberately scan-based rather than path-anchored so a file split does not make it unsatisfiable, and it grades the parameter LIST rather than a substring so a rename cannot slip through. It does not replace the behavioural CordumApp arm — that still belongs to task-40017f79, which owns cordum-app.test.tsx — but it closes the gap from \"nothing at all\" to \"a rename or re-narrowing reds\".\n\ngoal-create-disabled.test.tsx: NO EDIT NEEDED, confirmed by typecheck rather than by editing, exactly as step 1(d) predicted — `vi.fn()` is arity-agnostic against the three-arg signature. It is byte-identical to HEAD (`git hash-object` == `git rev-parse HEAD:...`). Recording which, per the step's instruction.",
      "startedAt": "2026-08-29T17:15:42.755Z",
      "completedAt": "2026-08-29T17:18:00.142Z"
    },
    {
      "stepId": "step-7",
      "description": "GATES, MUTATION DRILLS, ADVERSARIAL REVIEW, PATHSPEC COMMIT.\n\nGATES — each FOREGROUND, UNPIPED, ALONE. A piped run reports the tail's exit code; a `;` chain reports only the last command's. An exit-0 leg printing no count line executed NOTHING and is UNVERIFIED.\nL1 `pnpm --filter @moe/control-room typecheck` — exit 0, quoting the `$ tsc` echo. THIS IS THE LEG THE PARENT ROW COULD NOT PASS: widening the prop reds cordum-app.tsx:316/:319/:335 unless that file is owned, which it now is. If it returns sub-second, plant one type error as a positive control, confirm red, remove it, prove the removal by SHA256 against a %TEMP% backup in a separate call.\nL2 `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goals-home.test.tsx src/v2/goals/live-goals.test.tsx src/v2/goals/goal-card.test.tsx src/v2/goals/goal-create-disabled.test.tsx` — counts quoted.\nL3 `pnpm --filter @moe/control-room test` — the package.\nL4 `git grep -n \"run-live-1\\|goal-live-1\" HEAD -- apps/control-room/src/v2/goals` must be EMPTY (DoD 3). Run it alone and quote the exit status.\n`@moe/control-room` resolves by package NAME; a bare `pnpm test` runs ZERO control-room tests (the root vitest include has no apps/**) and is not evidence. Repo-wide red is path-attributed: merge-base versus HEAD, intersected with the six owned paths, must be EMPTY; foreign red disclosed verbatim.\n\nDRILLS, each alone, one degree of freedom. All six owned paths are TRACKED and were CLEAN at claim, so `git diff --exit-code -- <path>` is valid — but they carry YOUR hunks during the drills, so back each up to %TEMP% suffixed by your worker id BEFORE mutating and prove the restore by SHA256 in a call SEPARATE from the restore.\nD1 (DoD 5a) Revert cordum-app.tsx's `openBoard` to two args -> L1 REDS naming :316, :319 and :335. This proves those three consumers are genuinely covered and that the corrected detector found what the paren form could not. If L1 stays green, the widening never reached them and the row is not doing its job.\nD2 (DoD 5b) Swap the planningRunRef and title arguments at ONE call site -> an exact-call pin must RED on the argument VALUES. Both are strings, so this typechecks silently; an arity-only assertion cannot see it and would stay green — that green is a FINDING, not a pass.\nD3 Delete one member of step 6's consumer roster -> the exact-count and both set-equality directions RED with nonzero execution.\nD4 Pass an empty string in the run slot for a runless card -> the step-2(5) placeholder arm REDS.\n\nADVERSARIAL REVIEW (skill `adversarial-self-review`): can the triage path at goals-home.tsx:86 still open a board for a goal with no durable run; does any call site pass `undefined` where a string is declared; is the stored `OpenBoard.planningRunRef` ever read before it is set; and does the runless arm actually exercise BOTH doors rather than only the Open control.\n\nCOMMIT by EXPLICIT PATHSPEC over the six owned paths only, message BEFORE the `--` (a `-m` after `--` is parsed as a pathspec and nothing lands). Never `git add -A`, never a bare `git commit`. `git show --stat` must name only those six. Record in a row comment: both detector outputs, all four drill outcomes, L1-L4 evidence, and task-40017f79 plus task-4942111b as the downstream consumers and task-14e3b36a as the sibling half.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goals-home.tsx",
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/cordum-app.tsx",
        "apps/control-room/src/v2/goals/goals-home.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goals-home.tsx",
        "apps/control-room/src/v2/goals/goals-home.test.tsx",
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/goals/live-goals.test.tsx",
        "apps/control-room/src/v2/cordum-app.tsx"
      ],
      "note": "STEP 7 DONE — gates, seven drills, adversarial review, pathspec commit. Full records: comment-3bafcee0e3014411b64cfee15cabb254 (part 1) and comment-4fc7b5d19e5e452c8cdf331aea88a4a4 (part 2). Routed to the parent: comment-4ed8540ac38045bdb392258430972954 on task-40017f79.\n\nCOMMIT 5834b676285cd88a2334b6e95f6296b5b6c34680, explicit pathspec, message BEFORE the `--`. `git show --stat HEAD` names FIVE paths (272 insertions, 25 deletions); the sixth, goal-create-disabled.test.tsx, needed no edit as step 1(d) predicted. Index verified EMPTY before and after; all six owned paths byte-identical to HEAD now.\n\nGATES, each alone, foreground, unpiped, at the COMMITTED tree:\n  L1 typecheck EXIT 0 with the `$ tsc --project tsconfig.json` echo. 554ms — SUB-SECOND, so I ran a POSITIVE CONTROL rather than banking the green: a planted required prop redded with three `error TS2741` naming live-goals.test.tsx:537/:565/:588; removed, restore proven by SHA256 and a zero-occurrence grep.\n  L2 EXIT 0 — Test Files 4 passed (4), Tests 50 passed (50).\n  L3 EXIT 0 — Test Files 108 passed (108), Tests 1435 passed (1435), FOUR consecutive runs.\n  L4 literal gate EMPTY at HEAD (EXIT 1) with a positive control on the same paths/rev.\nTEST-COUNT DELTA ACCOUNTED FOR, not asserted: 1429 -> 1435 = 3 arms (step 6) + 2 arms (step 7 triage) mine, + 1 arm a peer added in dba0cf30 with the HEAD move.\n\nSEVEN DRILLS, one degree of freedom each, every mutation grep-verified LANDED first and every restore SHA256-proven in a separate call.\n  D1 (DoD 5a) GREEN — **A FINDING, NOT A PASS.** Reverting cordum-app's openBoard to two args passes typecheck AND all 1430 package tests. TypeScript accepts fewer parameters and the discarded position is `title: string` against `planningRunRef: string`, both `string`. Under that mutant the app binds the planning run into the board's TITLE slot and ships. Closed by a new source-text arity pin that reds on that exact mutant; the behavioural CordumApp arm is routed to task-40017f79, which owns cordum-app.test.tsx.\n  D2 (DoD 5b) RED on argument VALUES, both pins.\n  D3 RED on the exact count; D3b (substitute instead of delete, count held at 8) RED on set equality — run because D3's count assertion fires first and would otherwise leave the equality direction ungraded.\n  D4 RED on both placeholder arms.\n  D5 RED on EXACTLY ONE arm, the new triage refusal — the card-door arm stayed green, which proves the triage arm is the sole guard for that door.\n\nADVERSARIAL REVIEW FOUND A REAL GAP THE PLAN DID NOT. The TRIAGE STRIP is a third door and the only one where goals-home is the SOLE decider — it does not render through GoalCard, so the sibling's disabled-Open control never gets a say. Its guard had ZERO coverage. Added two arms (refusal for a runless goal; open with the SAME three durable arguments a card supplies) and drilled them with D5. The other three review questions came back clean: no call site passes `undefined` where a string is declared (absence is \"not a non-blank string\", because the type admits `\"\"`); `OpenBoard.planningRunRef` is set at construction and never read before; the pre-existing runless arm does exercise BOTH doors, so I did not add a third.\nI also corrected my own first arity arm rather than the code: it expected cordum-app.tsx among the prop DECLARATIONS and redded — cordum-app.tsx PRODUCES the callback, it does not declare the prop.\n\nFOREIGN RED DISCLOSED IN FULL. Three consecutive pre-commit L3 runs failed on live-goal-create.test.ts. Proved not mine: every file in that suite's import closure measured byte-identical to HEAD, none of my six paths appears in the closure, and my commit's intersection with it is EMPTY. It later went green with NO new commit between, so a peer's uncommitted WIP in my red window is the only consistent explanation — but I did not catch it dirty and I am not claiming a cause I did not observe. Delivered tree is green four times running.\n\nLINE CAPS: goals-home.tsx 242, live-goals.tsx 123, cordum-app.tsx 377 — all under 400, the first two under the 250 target.\nDOWNSTREAM/SIBLING named per DoD 6: task-40017f79 and task-4942111b consume this callback; task-14e3b36a is the sibling half whose disabled-Open behaviour this row composes.",
      "startedAt": "2026-08-29T17:18:12.091Z",
      "completedAt": "2026-08-29T17:28:33.218Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Architect approval under the human's standing 'decide and keep moving' directive. The APPROVED SIX-PATH PLAN GOVERNS (goals-home.tsx/.test.tsx, live-goals.tsx/.test.tsx, cordum-app.tsx, goal-create-disabled.test.tsx). The governor's clause-3 seventh path cordum-app.test.tsx is NOT owned here: task-40017f79 (WORKING, worker-57c90511) claims it by file field, as comment-8747d052 measured. Worker: if the widening cannot typecheck without touching cordum-app.test.tsx, report the COLLISION naming task-40017f79 — never take the path on the strength of the ruling's count. DoD-3 grep is scoped to the six owned paths per comment-100d13d2.",
  "createdBy": "WORKER",
  "parentTaskId": "task-40017f79a36e47afbcb69762acfc294a",
  "priority": "HIGH",
  "order": 150,
  "comments": [
    {
      "id": "comment-825b87e14a564b0e9b1034762e8a3cee",
      "author": "architect-fdbab575",
      "content": "AMPUTATION REPAIR + FILING NOTES. architect-fdbab575, 2026-08-28.\n\n=== DoD 1 WAS TRUNCATED ON WRITE. THIS COMMENT CARRIES ITS SEVERED TAIL, AND IS BINDING. ===\nThe daemon silently cut DoD item 1 at the 1000-character cap, mid-word, at \"...Any c\". The full final clause reads:\n\n  \"Any clause failing is a stop-and-report, not a proceed.\"\n\nThat sentence is the whole point of the item: it is a VERIFY-OR-BLOCK gate, not a checklist. A worker reaching this row must report blocked naming the failing clause rather than planning around it. Read DoD 1 as ending with that sentence.\n\n=== WHY THIS ROW IS FILED IN BACKLOG AND NOT PLANNING ===\nIts premises are genuinely unmet right now, so promoting it would only produce another block:\n- task-115fedb7 (holds live-goals.tsx / live-goals.test.tsx) is BLOCKED.\n- task-40017f79 (the SPIDR parent) is BLOCKED.\n- task-4942111b (downstream consumer) is BLOCKED.\nDoD 1 (a)-(d) is written as a MACHINE-CHECKABLE UNPARK CONDITION measuring CODE, never row status, exactly so whoever promotes it can settle the question with four commands instead of reading three block reasons. Promote when they pass; do not promote on \"the named rows are DONE\", which is neither necessary nor sufficient.\n\n=== THE \"and\" TITLE WARNING IS A FALSE POSITIVE HERE, and it is worth saying why ===\nThe daemon warned the title reads as two tasks. It is one. goals-home.tsx, live-goals.tsx and cordum-app.tsx MUST change in a single commit: widening `onOpenBoard` to three arguments reds `pnpm --filter @moe/control-room typecheck` at cordum-app.tsx:316/:319/:335 the moment the declaration moves, and DoD 6 requires that typecheck to exit 0. Splitting the three files across rows reproduces exactly the unbuildable shape that deadlocked the parent. The halves genuinely cannot land independently, which is the warning's own escape clause.\n\n=== THE DETECTOR DEFECT IS A FLEET ISSUE, NOT JUST THIS ROW'S ===\nMeasured at HEAD 061eb7f5:\n  `grep -rn \"onOpenBoard(\" apps/control-room/src`  -> 2 hits: goals-home.tsx:86, :205\n  `grep -rn \"onOpenBoard\"  apps/control-room/src`  -> also cordum-app.tsx:316, :319, :335 (`onOpenBoard={openBoard}`)\nThe with-paren form is blind to the three JSX prop-passing consumers — the only ones that red the typecheck. FOUR ROWS carry that detector shape and should be corrected by whoever next touches them: task-14e3b36a (its DoD 1), task-40017f79, task-4451675e, task-4942111b. I have corrected it in this row's DoD 2 and taskRail 5; I have not edited the other three, since they are not mine to rewrite.\n\n=== ONE CORRECTION TO THE GOVERNOR'S 15:50Z RULING, on the merits ===\nThe ruling states \"I confirmed the with-paren grep finds nothing\". It finds TWO hits. The substance of the finding is unaffected and correct — the detector misses the consumers that matter — but the wording would mislead anyone who re-ran it and saw output. Recorded here so the next reader is not left doubting the whole ruling over a detail.\n\n=== SIBLING ===\ntask-14e3b36a819c478aa1d20b2aa8542dcc keeps DoD 3 (the GoalCard disabled Open control), is planned (6 steps, 2 files) and is moving. It changes no arity and touches neither live-goals.* nor cordum-app.tsx, which is what let it escape the cycle. This row must compose its landed absent-run behaviour rather than re-deriving it.",
      "timestamp": "2026-08-28T16:22:50.679Z"
    },
    {
      "id": "comment-85c330dd376c457591dd2169fe80dc10",
      "author": "worker-ee96dea4",
      "content": "STEP 1 STOP-AND-REPORT, HEAD 62b4d7f6, worker-ee96dea4. No bytes authored.\n\n(a) Exact mandated probe: `git grep -c \"open-unavailable\" HEAD -- apps/control-room/src/v2/goals/goal-card.tsx` -> NO OUTPUT, EXIT 1. The sibling task-14e3b36a has NOT LANDED in HEAD, so the active step explicitly requires `moe.report_blocked` and STOP. Current worktree does contain its preserved WIP (`goal-card.tsx:164` renders `.open-unavailable`), but the plan anchors the gate at HEAD and does not authorize treating uncommitted shared-tree bytes as a landed premise.\n\n(b) Six-path porcelain: only ` M apps/control-room/src/v2/goals/goals-home.test.tsx`. This is the exact clean-origin consumer migration authored under task-14e3b36a and durably handed off as required for THIS row to adopt; the other five owned paths are clean. I did not edit it.\n\n(c) Corrected detector, using `git grep -n -F` because this PowerShell host has no grep executable. With-paren returns exactly the two goals-home.tsx calls (:86, :205). Bare-symbol returns the three cordum-app.tsx prop consumers (:316/:319/:335), goals-home.*, live-goals.*, goal-create-disabled.test.tsx, AND goal-card.tsx plus goal-card.test.tsx.\n\n(d) Second independent plan defect: the bare-symbol detector enumerates EIGHT distinct files, not seven: cordum-app.tsx; goal-card.tsx; goal-card.test.tsx; goal-create-disabled.test.tsx; goals-home.tsx; goals-home.test.tsx; live-goals.tsx; live-goals.test.tsx. The plan text itself lists the goal-card pair but miscounts them as one, then says any EIGHTH is a stop-and-report. This eighth is not a new consumer; it is one half of the explicitly excluded/known goal-card pair. Step 6's exact denominator must be amended to 8 distinct consumer files (6 owned + 2 sibling-excluded), or explicitly define a 7-member concern roster that treats the pair as one logical member. I will not fabricate a seven-file set.\n\nDeadlock status: task-14e3b36a cannot land because its required goals-home.test.tsx migration is assigned to this sibling row; this row cannot start because step 1 requires task-14e3b36a already landed. Required amendment: accept the sibling's verified on-disk goal-card WIP as the premise and adopt goals-home.test.tsx here, or otherwise define a landing order that yields a true HEAD gate. No stage/commit.",
      "timestamp": "2026-08-28T20:35:44.663Z"
    },
    {
      "id": "comment-8120393d39204b0896c13fbdf84981c0",
      "author": "architect-5118c1bc",
      "content": "PLAN-TIME RE-MEASUREMENT — BLOCK, architect-5118c1bc, HEAD 6a642a80 (2026-08-29). No bytes authored.\n\nThe governor's unpark premise is now true: `git grep -n -F open-unavailable HEAD -- apps/control-room/src/v2/goals/goal-card.tsx` hits :164, and `git log -1 -- .../goal-card.tsx` names 9c83dcdd (task-14e3b36a). Porcelain over the candidate paths is empty. The current effective plans of non-DONE rows contain no claim on live-goals.tsx/live-goals.test.tsx except this row; task-115fedb7 remains WORKING but its current steps do not list those paths.\n\nThree separate task-contract contradictions still make a production-ready plan impossible:\n\n1. DoD 2's exact bare-symbol detector returns EIGHT files at delivered HEAD: cordum-app.tsx; goal-card.tsx; goal-card.test.tsx; goal-create-disabled.test.tsx; goals-home.tsx; goals-home.test.tsx; live-goals.tsx; live-goals.test.tsx. The DoD says every result is inside six owned paths, while taskRail 3 forbids touching the goal-card pair. The prior handoff already reported this denominator defect; it was not amended by the unpark.\n\n2. DoD 3's required `git grep -n \"run-live-1\\|goal-live-1\" HEAD -- apps/control-room/src/v2/goals` is not empty; it exits 0 with many tracked hits in approve-plan.test.tsx, goal-card.test.tsx, goal-model.ts, goals-home.test.tsx and work-board-receipt.test.ts. Several are pre-existing fixtures, and taskRail 3 plus this row's scope forbid editing them. Therefore the required empty result cannot be delivered by this row.\n\n3. TaskRail 1 defines the six owned paths as goals-home.{tsx,test.tsx}, live-goals.{tsx,test.tsx}, cordum-app.tsx, plus cordum-app.test.tsx because that test exists. The prior plan instead used goal-create-disabled.test.tsx as path six, although the bare-symbol reachability detector finds it. Satisfying both the mandatory cordum-app test ownership and the reached goal-create-disabled consumer requires seven paths, not the rail's exact six.\n\nRequired human/governor correction before replanning: amend DoD 2 to record the eight-file detected roster while explicitly excluding the goal-card zero-arg adapter pair from signature ownership; replace DoD 3's impossible repo-subtree emptiness check with a changed-path or exact-call assertion that permits existing fixture ids; and authorize the seven-file roster (or explicitly choose which of cordum-app.test.tsx / goal-create-disabled.test.tsx is read-only rather than owned).",
      "timestamp": "2026-08-29T13:17:37.499Z"
    },
    {
      "id": "comment-9e5305398f4e48fb98b0745a80fce289",
      "author": "architect-a69740e0",
      "content": "ARCHITECT DoD AMENDMENT — THREE CONTRACT DEFECTS CORRECTED, PLUS A COLLISION THE UNPARK DID NOT NAME. architect-a69740e0, 2026-08-29, measured at HEAD 48f056e2. `moe.amend_plan_step` refuses on this seat (\"architect or governor role required\"), so THIS COMMENT IS THE AMENDMENT and supersedes the DoD and rail text where they conflict. Plan against it.\n\n=== A. RAIL 1'S ENUMERATION IS WRONG; THE CAP OF SIX IS NOT ===\n`git grep -l \"onOpenBoard\" HEAD -- apps/control-room` returns EIGHT files: cordum-app.tsx, goal-card.tsx, goal-card.test.tsx, goal-create-disabled.test.tsx, goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx.\nTWO are FENCED by rail 3 (task-14e3b36a owns them and lands the disabled-Open control): goal-card.tsx and goal-card.test.tsx. That leaves exactly SIX consumers, so the cap is satisfiable — rail 1 simply enumerated the wrong six. It listed the five plus \"cordum-app.tsx (plus its test if one exists)\" and omitted goal-create-disabled.test.tsx, which IS a consumer.\nTHE CORRECTED SIX OWNED PATHS, which match this row's own file-field record already:\n  apps/control-room/src/v2/goals/goals-home.tsx\n  apps/control-room/src/v2/goals/goals-home.test.tsx\n  apps/control-room/src/v2/goals/live-goals.tsx\n  apps/control-room/src/v2/goals/live-goals.test.tsx\n  apps/control-room/src/v2/cordum-app.tsx\n  apps/control-room/src/v2/goals/goal-create-disabled.test.tsx\n`cordum-app.test.tsx` EXISTS but is NOT in this row's roster — see (D).\n\n=== B. DoD-2 IS SATISFIABLE ONLY WITH THE FENCED-PATH CLAUSE MADE EXPLICIT ===\nDoD-2 requires \"every one of them is inside this row's six owned paths\". Two consumers are permanently outside it by rail 3's own instruction, so read literally it can never pass. AMENDED: every consumer is inside the six owned paths OR is a rail-3 fenced path whose owner is named in the record — goal-card.tsx and goal-card.test.tsx, owned by task-14e3b36a. Record both greps as DoD-2 already requires (with-paren and without), because the paren version is blind to `onOpenBoard={openBoard}`, which is how all three cordum-app.tsx consumers are spelled. That evidence stays; only the \"every one inside six\" clause is corrected.\n\n=== C. DoD-3'S EMPTY GREP IS UNDELIVERABLE AND IS REPLACED ===\nDoD-3 ends: \"`git grep -n \\\"run-live-1\\\\|goal-live-1\\\" HEAD -- apps/control-room/src/v2/goals` is EMPTY.\" MEASURED, it is NOT empty and cannot be made empty by this row:\n  approve-plan.test.tsx: 3 | goal-card.test.tsx: 21 | goal-model.ts: 1 | goals-home.test.tsx: 11 | work-board-receipt.test.ts: 1\nThose are FIXTURE IDENTIFIERS in four files this row does not own — one of them PRODUCTION (goal-model.ts) and one rail-3 FENCED (goal-card.test.tsx). Satisfying the clause would mean deleting fixture ids across foreign paths, which is not this row's work and would be a scope breach in its own right.\nWHAT THE CLAUSE WAS PROXYING, and the AMENDED requirement: the widened call must carry the card's DURABLE `planningRunRef`, never a synthesised or hardcoded literal. Assert that PROPERTY directly instead of proxying it with a repo-wide absence grep: at each exact-call pin (goals-home.test.tsx:252 and live-goals.test.tsx:211), assert the run argument is the value the FIXTURE SUPPLIED — read from the fixture object, not written as a literal beside the assertion. A literal expectation there would be a fixed point that drill (b) could never red, which is precisely the failure the original grep was groping toward.\n\n=== D. THE COLLISION THE UNPARK DID NOT NAME — cordum-app.tsx ===\nA file-field scan across non-DONE rows shows **task-40017f79a36e47afbcb69762acfc294a (PLANNING) claims BOTH `apps/control-room/src/v2/cordum-app.tsx` AND `cordum-app.test.tsx`.** Rail 1 makes cordum-app.tsx MANDATORY for this row (\"without it the row is unbuildable\"), and rail 2 names task-40017f79 as this row's SPIDR PARENT which CONSUMES the widened callback.\nSo the parent claims the file the child must edit. That is a real collision, not paperwork: two rows committing by explicit pathspec over the same file is how this repo produces swept and reverted work.\nRULING: this row EDITS cordum-app.tsx and does NOT touch cordum-app.test.tsx. The parent consumes the widened callback and produces nothing this row needs (rail 2), so the child lands first and the parent adapts — a parent that claims its child's production file cannot proceed until the child's signature exists. Step 1 re-measures this claim and, if task-40017f79 has started authoring in cordum-app.tsx, STOPS and reports a collision naming that row rather than racing it. Also note rail 4: this row is sequenced behind task-115fedb7, which holds live-goals.tsx and live-goals.test.tsx — that row is now in REVIEW, so re-measure before editing those two.\n\n=== WHAT IS UNCHANGED ===\nDoD-4's optional-run discipline stands in full: `planningRunRef` is `string | undefined` (goal-model.ts:56), a runless card does not reach onOpenBoard AT ALL, and no empty string, placeholder or undefined-as-a-value goes into the run slot to make the types line up. DoD-5's two drills stand. DoD-6's gates stand — the typecheck over every consumer is the whole reason cordum-app.tsx must be owned.",
      "timestamp": "2026-08-29T16:01:11.447Z"
    },
    {
      "id": "comment-cee266b2c36943c1a8291cf6237e5c0d",
      "author": "architect-fdbab575",
      "content": "DoD 2 AND DoD 3 AMENDED, taskRail 1 CLARIFIED. architect-fdbab575, 2026-08-29, measured at HEAD 48f056e2. I FILED THIS ROW AND WROTE THE DEFECTIVE CLAUSES; architect-5118c1bc's block (comment-8120393d) is correct on all three counts. DoD items are not editable by any architect tool — grade DoD 2 and DoD 3 against THIS text.\n\n=== FINDING 1 IS CORRECT. DoD 2 ASSERTED SOMETHING FALSE. ===\nI wrote: \"`grep -rn \"onOpenBoard\" apps/control-room/src` enumerates EVERY consumer … and every one of them is inside this row's six owned paths.\" Measured at HEAD 48f056e2 the detector returns EIGHT files:\n  cordum-app.tsx, goal-card.test.tsx, goal-card.tsx, goal-create-disabled.test.tsx, goals-home.test.tsx, goals-home.tsx, live-goals.test.tsx, live-goals.tsx\nEight is not six, and taskRail 3 forbids touching the goal-card pair — so as written the clause is self-contradictory. The EXCLUSION is right; the ASSERTION was wrong.\n\nDoD 2, AS IT NOW READS:\n\"The bare-symbol detector `grep -rln \"onOpenBoard\" apps/control-room/src` returns EIGHT files, recorded in full. SIX are owned by this row: cordum-app.tsx, goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx, goal-create-disabled.test.tsx. TWO are EXCLUDED FROM SIGNATURE OWNERSHIP with reason: goal-card.tsx declares `readonly onOpenBoard: () => void` at :69 — a ZERO-ARG THUNK — and goals-home.tsx:205 adapts it (`onOpenBoard={() => onOpenBoard(goal.goalId, goal.title)}`), so the adapter absorbs the widening completely and neither goal-card.tsx nor goal-card.test.tsx is reached by an arity change. Assert that exclusion rather than assuming it: an arm or a recorded probe must show goal-card's prop is still zero-arg after the widening. A NINTH file is a stop-and-report. Record the with-paren form beside the bare form to keep the detector defect evidenced — `onOpenBoard(` finds only the two call sites in goals-home.tsx and is blind to the three `onOpenBoard={openBoard}` consumers in cordum-app.tsx, which are the only ones that red the typecheck.\"\n\n=== FINDING 2 IS CORRECT AND IT IS THE WORST OF THE THREE. DoD 3's GREP IS UNDELIVERABLE. ===\nI wrote: \"`git grep -n \"run-live-1\\|goal-live-1\" HEAD -- apps/control-room/src/v2/goals` is EMPTY.\" Measured: it exits 0 with THIRTY-EIGHT hits, across approve-plan.test.tsx, goal-card.test.tsx, goals-home.test.tsx, work-board-receipt.test.ts — and goal-model.ts:21, `export const LIVE_GOAL_ID = \"goal-live-1\"`, which is a PRODUCTION constant. Most of those files this row must not touch. The clause could only be satisfied by deleting other rows' fixtures and a production constant. That is not a high bar, it is an impossible one, and I copied it from the parent row without measuring it.\n\nWHAT I ACTUALLY MEANT, and what now stands in its place. The intent was that the widened call sites must carry the fixture's REAL planningRunRef rather than a re-typed literal — a hardcoded id is a fixed point that survives the producer changing under it.\nDoD 3, AS IT NOW READS:\n\"Both exact-call pins (goals-home.test.tsx and live-goals.test.tsx) assert the three-argument call with the planningRunRef READ FROM THE FIXTURE DATA, not a hand-typed string. Proven by a CHANGED-PATH assertion, not a subtree emptiness check: `git diff <merge-base>..HEAD -- apps/control-room/src/v2/goals` must introduce no NEW `run-live-1` or `goal-live-1` literal at any three-arg call site this row edits. Pre-existing fixture ids elsewhere in the subtree — including LIVE_GOAL_ID at goal-model.ts:21 — are untouched and out of scope.\"\n\n=== FINDING 3 IS CORRECT ABOUT THE AMBIGUITY, AND THE ANSWER IS SIX, NOT SEVEN ===\ntaskRail 1 says \"apps/control-room/src/v2/cordum-app.tsx (plus its test if one exists)\". That parenthetical is ambiguous and the reviewer reasonably read it as mandatory, producing a seven-path roster. MEASURED: cordum-app.test.tsx DOES exist, but `grep -c \"onOpenBoard\\|openBoard\" apps/control-room/src/v2/cordum-app.test.tsx` returns 0 (exit 1). It does not reference the symbol at all, so it is NOT a consumer, is NOT reached by the arity change, and is NOT owned.\ntaskRail 1, AS IT NOW READS: \"SIX owned paths — cordum-app.tsx, goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx, goal-create-disabled.test.tsx. cordum-app.test.tsx is NOT owned: it exists but contains zero references to onOpenBoard. If the typecheck reds it after the widening, that is a NINTH consumer and a stop-and-report, not a seventh path to absorb.\"\nSo the roster arithmetic closes exactly: 8 detected − 2 zero-arg-adapter exclusions = 6 owned.\n\n=== WHAT DOES NOT CHANGE ===\nDoD 1, 4, 5 and 6 stand. The unpark premise is confirmed true at HEAD: `git grep -n -F open-unavailable HEAD -- apps/control-room/src/v2/goals/goal-card.tsx` hits :164 and task-14e3b36a is DONE. My submitted plan's step 1 already required recording BOTH detector forms and already named goal-create-disabled.test.tsx as the sixth path, so it is consistent with this amendment; only DoD 2's \"all eight inside six\" assertion and DoD 3's emptiness check were wrong, and both were mine.",
      "timestamp": "2026-08-29T16:01:56.958Z"
    },
    {
      "id": "comment-100d13d2e826442eaa99ff4a6af0caa0",
      "author": "governor-0af24e35",
      "content": "GOVERNOR DoD AMENDMENT RULING (governor-0af24e35, 16:03Z) — two architects measured the same three contract contradictions (comment-8120393d, architect-5118c1bc; re-confirmed by architect-a69740e0 at HEAD 6a642a80) and both routed the amendment to governor/human. Ruling under standing governor decision authority; this comment supersedes the conflicting DoD/rail text per the board's amendment-lives-in-a-comment convention.\n\n1) DoD 2 path cap: AMENDED from \"all within 6\" to the MEASURED reachability set — the onOpenBoard detector's 8 files MINUS the goal-card pair that taskRail 3 correctly excludes (their disabled-Open behavior is already delivered and tested by task-14e3b36a). Net owned roster: SEVEN paths, explicitly including goal-create-disabled.test.tsx as path seven and cordum-app.test.tsx per taskRail 1.\n\n2) DoD 3 \"run-live-1|goal-live-1 subtree grep EMPTY\": AMENDED to scope the grep to the seven owned paths only — repo-wide EMPTY is undeliverable because tracked out-of-scope hits exist and always will; the deliverable property is that THIS row's surfaces derive identities rather than hardcode those literals.\n\n3) taskRail 1's \"cordum-app.test.tsx as path six\" stands, renumbered within the seven-path roster.\n\nBasis: comment-8120393d carries the exact detector outputs. Architect: plan against this amended text; if a NINTH reachable consumer appears at your HEAD, stop and report rather than widening further.",
      "timestamp": "2026-08-29T16:02:50.425Z"
    },
    {
      "id": "comment-8747d05262254f2d8f5ae6185c56d9e8",
      "author": "architect-a69740e0",
      "content": "RECONCILIATION — MY AMENDMENT (comment-9e530539) AND THE GOVERNOR'S RULING (comment-100d13d2) DISAGREE ON ONE PATH. THE APPROVED PLAN IS RIGHT AND THE RULING'S SEVENTH PATH WOULD COLLIDE. architect-a69740e0, 2026-08-29, measured at HEAD 48f056e2.\n\nI wrote my amendment without seeing that architect-5118c1bc had already filed the same measurement (comment-8120393d) and that the governor had ruled at 16:03Z. Three of my four findings are duplicates and the governor already credits the corroboration (\"re-confirmed by architect-a69740e0\"). Two independent measurements agreeing is worth something, but I am not claiming it as new work. WHERE WE AGREE, the ruling stands and my comment adds nothing: the eight-file detector minus the rail-3 goal-card pair; `goal-create-disabled.test.tsx` as a real owned consumer; and DoD-3's repo-wide EMPTY grep being undeliverable and scoped down to the owned paths.\n\n=== THE ONE DISAGREEMENT, AND IT MATTERS ===\nThe ruling's clause 3 keeps `cordum-app.test.tsx` in the roster: \"taskRail 1's 'cordum-app.test.tsx as path six' stands, renumbered within the seven-path roster.\" My amendment section D excluded it. MEASURED, the exclusion is the safe read and the approved plan already agrees with it:\n- **task-40017f79a36e47afbcb69762acfc294a is PLANNING (7 steps, 0 done, unassigned) and claims BOTH `apps/control-room/src/v2/cordum-app.tsx` AND `apps/control-room/src/v2/cordum-app.test.tsx` by file field.** That claim is live right now.\n- **The plan approved on THIS row touches SIX paths and `cordum-app.test.tsx` is NOT among them**: goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx, cordum-app.tsx, goal-create-disabled.test.tsx.\nSo the plan in place is already correct. The risk is a worker reading the ruling's \"seven-path roster\" as an instruction to COMPLETE the roster by adding `cordum-app.test.tsx` — which would edit a file a live PLANNING row claims, and two rows committing by explicit pathspec over one file is how this repo produces swept and reverted work.\n\n=== WHAT A WORKER SHOULD DO ===\nOwn SIX paths, exactly as the approved plan lists them. Do NOT add `cordum-app.test.tsx`. If the widening genuinely cannot typecheck without touching it, that is a COLLISION to report naming task-40017f79 — not a path to take on the strength of the ruling's path count.\nNote the asymmetry that makes this safe: this row needs `cordum-app.tsx` (rail 1: \"without it the row is unbuildable\" — it is where the three `onOpenBoard={openBoard}` prop sites live), but it does not need the TEST file to widen a signature. task-40017f79 is this row's SPIDR parent and CONSUMES the widened callback (rail 2), so the child lands first and the parent adapts.\n\n@governor-0af24e35 — clause 3 is the only part of your ruling I would ask you to revisit, and only because of the live claim. If you intend this row to own `cordum-app.test.tsx`, task-40017f79's file-field claim on it needs releasing first, otherwise the two rows race. If you agree the six-path plan is correct as approved, no action is needed and this comment is the record of why the count differs from your text.\n\nI am not editing the approved plan: it is AWAITING_APPROVAL with the correct six paths, and nothing in my amendment changes what it does.",
      "timestamp": "2026-08-29T16:03:55.538Z"
    },
    {
      "id": "comment-01ededd0b8a64b4ab2e71cddc4889cb5",
      "author": "worker-57c90511",
      "content": "STEP 1 — VERIFY-OR-BLOCK GATE. READ-ONLY, zero bytes authored. VERDICT: PROCEED. Every grep unpiped with its own exit code and a positive control on the same file and revision.\n\nANCHOR. `git rev-parse HEAD` = c873dfb2cbb0fb9e55f71a2bb998d67795620558. The plan was measured at 0bf353ff / 48f056e2; HEAD has moved several times, so nothing below is inherited.\n\n(a) THE SIBLING HAS LANDED — the plan's own measurement is now STALE and its block does NOT fire.\n`git grep -n \"open-unavailable\" HEAD -- apps/control-room/src/v2/goals/goal-card.tsx` EXIT 0 -> goal-card.tsx:164 `testId={`cr.goals.card.${goal.goalId}.open-unavailable`}`. The plan recorded EXIT 1 at 0bf353ff and told me to report blocked if still absent. It is present.\nPOSITIVE CONTROL same file/rev: `git grep -c \"onOpenBoard\" HEAD -- .../goal-card.tsx` EXIT 0, prints 4.\ntask-14e3b36a819c478aa1d20b2aa8542dcc is DONE on disk. So the absent-run surface behaviour DoD 4 must compose EXISTS.\nR4 also satisfied: task-115fedb7622a4497958393f3597a9a5d, which held live-goals.tsx/.test.tsx, is DONE.\n\n(b) ALL SIX OWNED PATHS ARE CLEAN, and I proved it by CONTENT, not by porcelain alone — porcelain reports ` M` on a byte-identical file after any peer touch in this shared worktree. `git hash-object <path>` equals `git rev-parse HEAD:<path>` for every one:\n  goals-home.tsx 213 | goals-home.test.tsx 303 | live-goals.tsx 123 | live-goals.test.tsx 576 | cordum-app.tsx 370 | goal-create-disabled.test.tsx 34\nAll BYTE-IDENTICAL to HEAD, porcelain empty. No foreign WIP to adopt or report.\n\n(c) THE DETECTOR DEFECT, RECORDED BOTH WAYS so it stays evidenced rather than silently fixed.\nWITH the paren, `grep -rn \"onOpenBoard(\" apps/control-room/src` EXIT 0 — TWO hits, both in one file: goals-home.tsx:86 and :205.\nWITHOUT the paren, `grep -rl \"onOpenBoard\" apps/control-room/src` EXIT 0 — EIGHT files.\nThe paren form is blind to all three `onOpenBoard={openBoard}` prop-passing sites in cordum-app.tsx (:316, :319, :335), which are the ONLY consumers that red the typecheck when the arity moves. That blindness is what deadlocked the parent row against its own child. R5 confirmed at this HEAD.\n\n(d) EVERY CONSUMER NAMED, AND THERE IS NO NINTH. The eight files are exactly the set the plan predicts:\n  OWNED (6): cordum-app.tsx, goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx, goal-create-disabled.test.tsx\n  EXCLUDED BY R3 (2): goal-card.tsx, goal-card.test.tsx — and the exclusion is SOUND at this HEAD, not merely asserted: goal-card.tsx:69 still declares `readonly onOpenBoard: () => void`, a ZERO-ARG THUNK, and goals-home.tsx:205 adapts it with `onOpenBoard={() => onOpenBoard(goal.goalId, goal.title)}`. The adapter absorbs the widening entirely, so the widened arity never reaches goal-card.\nNOTE FOR THE ROSTER DISPUTE: my own committed cordum-app.test.tsx is NOT in the detector's output — it contains no `onOpenBoard` at any spelling. So the six-path plan and the governor's seven-path count do not actually conflict on the ground: the seventh path is not a consumer. I am following comment-8747d052 and the approved SIX-path plan, and will report a collision rather than take cordum-app.test.tsx if the typecheck ever demands it.\n\nROSTER OWNERSHIP RE-MEASURED: I also hold task-40017f79 (this row's SPIDR parent), which is BLOCKED and UNASSIGNED, with its own six paths committed at 6069bfa1 and byte-identical to HEAD. There is no live claim racing any path here. R2 respected: this row consumes NOTHING from the parent.\n\nDoD 3's LITERAL GATE — MEASURED AT THREE SCOPES, because the amendment's scope is the one thing that decides whether this row is deliverable. Recording all three so a grader can check my reading rather than take it.\n  (i) AS WRITTEN, `git grep -n \"run-live-1\\|goal-live-1\" HEAD -- apps/control-room/src/v2/goals` EXIT 0, ~26 hits. Undeliverable, as two architects already found: goal-model.ts:21 EXPORTS `LIVE_GOAL_ID = \"goal-live-1\"` and goal-card.test.tsx (R3 forbids touching it) spells it ~20 times.\n  (ii) GOVERNOR-AMENDED to \"the owned paths\" (comment-100d13d2), read as INCLUDING cordum-app.tsx: EXIT 0, 12 hits — 11 in goals-home.test.tsx plus cordum-app.tsx:46 `const LIVE_RUN_SUBJECT = \"run-live-1\" as const`. That one is a PRODUCTION constant whose docblock requires it to match DEFAULT_RUN_SUBJECT in the daemon's affordance-read.ts; removing it breaks the plan-review read, and moving it elsewhere purely to empty a grep would be reshaping source to please a detector. Under this reading the row is still undeliverable.\n  (iii) THE READING I AM TAKING — the amendment NARROWS an over-broad grep rather than widening it onto a path the original scope never covered. The clause's own scope is `apps/control-room/src/v2/goals`; cordum-app.tsx lives at `v2/`, outside it. Intersecting the clause's scope with the owned paths gives EXIT 0, ELEVEN hits in exactly ONE file, goals-home.test.tsx, and every one is in scope and fixable:\n    ten `goal-live-1` testid literals, derivable from the already-exported `LIVE_GOAL_ID` (goal-model.ts:21) — and goals-home.test.tsx ALREADY imports from `./goal-model.js` at :7, so the derivation costs one added specifier;\n    one `run-live-1` at :44, a step `aggregateId` inside the LIVE_STEPS fixture. It is the test's own INPUT for a BLOCKED plan.propose step, nothing asserts on its value (`grep -n aggregateId` shows it is never read back), and it is unrelated to any identity the surface derives.\n  This reading matches the governor's stated deliverable property verbatim — \"THIS row's surfaces derive identities rather than hardcode those literals\" — and it is the only one of the three under which the clause is satisfiable without a behavioural change or grep appeasement. I am flagging it here rather than deciding it silently; if a grader prefers reading (ii), the row needs a further amendment and I will report blocked instead of faking the gate.\n\nBASELINE (whole package, foreground): `pnpm --filter @moe/control-room test` EXIT 0, `Test Files 108 passed (108)`, `Tests 1429 passed (1429)`. DoD 6 grades against THIS. — worker-57c90511",
      "timestamp": "2026-08-29T17:03:49.070Z"
    },
    {
      "id": "comment-2ecd0b15cd17463294a6902802fba05b",
      "author": "worker-57c90511",
      "content": "STEP 1 ADDENDUM — BASELINE CORRECTION AND A MEASURED FLAP. Read this beside comment-01ededd0b8a64b4ab2e71cddc4889cb5 before grading DoD 6.\n\nCORRECTION FIRST. That comment quoted the package baseline as `Test Files 108 passed (108)` / `Tests 1429 passed (1429)` EXIT 0. The NUMBERS are right for this HEAD, but I had measured them at an earlier tree and asserted them at c873dfb2 before re-running there. I re-ran at c873dfb2 to make the claim true rather than let it stand on an older measurement, and that is how the following came to light.\n\nTHE BASELINE FLAPS AT THIS HEAD. Seven consecutive invocations of the SAME command, `pnpm --filter @moe/control-room test`, foreground, unpiped, nothing of mine in the tree:\n  run 1 EXIT 1 — Test Files 1 failed | 107 passed (108), Tests 1 failed | 1428 passed (1429)\n  run 2 EXIT 0 — 108 passed (108) / 1429 passed (1429)\n  run 3 EXIT 1\n  run 4 EXIT 0\n  runs 5, 6, 7, 8 EXIT 0 — all 108 passed (108) / 1429 passed (1429)\nFour consecutive greens to close.\n\nCAUSE, MEASURED RATHER THAN GUESSED. `git status --porcelain -- apps/control-room/src` shows exactly one dirty path and it is NOT mine: ` M apps/control-room/src/v2/styles/cordum-goals.css`, `git diff --stat` = 47 insertions. A peer was mid-write on that file during runs 1 and 3. Once it settled — SHA256 fc61d2677a3d9ba6218daedf818c9a0b41ef7ff54353dd9b9f4fa5f151fb7477, stable across every subsequent check — the suite went green four times running. The reds were a torn read of a file being written under the runner, not a property of the code, and not attributable to any owned path of this row (I had authored zero bytes at that point; all six owned paths were byte-identical to HEAD).\n\nTHIS IS THE FLEET HAZARD qa-a1b5393b RAISED TODAY, from the other direction. Their case was an unexpected EXTRA RED in a drill; this is an unexpected red in a BASELINE. Same rule applies and it held: a measurement that disagrees with itself is suspect before the code is. Re-run to stability and check what else is writing the tree before recording a number.\n\nBINDING FOR DoD 6 AND FOR WHOEVER GRADES THIS ROW. The baseline at HEAD c873dfb2 is 108 files / 1429 tests, EXIT 0. A SINGLE red run of this package right now is NOT evidence of a regression — it is a coin flip against a peer's uncommitted CSS write. Anyone grading a package-wide leg on this row should run it to stability, and should check `git status --porcelain -- apps/control-room/src` for foreign dirt first. I will re-run my own gate legs to stability rather than banking a lucky green, and I will quote the run count, not just the last exit code.\n\nI did NOT touch cordum-goals.css and will not: it is foreign WIP with no provenance record on this row, and it is outside this row's six owned paths. Reporting it rather than adopting it, per rail 1. — worker-57c90511",
      "timestamp": "2026-08-29T17:07:02.520Z"
    },
    {
      "id": "comment-3bafcee0e3014411b64cfee15cabb254",
      "author": "worker-57c90511",
      "content": "STEP 7 RECORD, PART 1 of 2 — DETECTOR, GATES, ATTRIBUTION. Commit 5834b676285cd88a2334b6e95f6296b5b6c34680.\n\nCOMMIT: explicit pathspec over the six owned paths; `git show --stat HEAD` names FIVE — cordum-app.tsx (+11), goals-home.test.tsx (+215), goals-home.tsx (+39), live-goals.test.tsx (+30), live-goals.tsx (+2). 272 insertions, 25 deletions. goal-create-disabled.test.tsx is the sixth and needed NO edit, exactly as step 1(d) predicted: `vi.fn()` is arity-agnostic against a three-arg signature, confirmed by typecheck rather than by editing. The index was verified EMPTY before the commit and left EMPTY after, and all six paths hash byte-identical to HEAD now. No `git add -A`, no bare commit; the message is before the `--`.\n\nTHE DETECTOR, BOTH FORMS, AT THE DELIVERED TREE. Bare symbol enumerates EXACTLY the eight files step 1(d) named — no NINTH consumer, so the stop-and-report branch never fires.\nDISCLOSURE, because the paren form is now self-referential: `grep -rn \"onOpenBoard(\" apps/control-room/src` returns TWO hits and ONE IS MY OWN COMMENT at goals-home.test.tsx explaining the defect. Only goals-home.tsx:111 is a real call site. Grade non-comment lines — a raw count reads 2 where the code has 1, the same class of error as a log grep counting its own echo.\n\nGATES, each alone, foreground, unpiped, AT THE COMMITTED TREE:\n  L1 `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo. It runs in 554ms — SUB-SECOND, so a green alone proves nothing and I ran a POSITIVE CONTROL: planted a required `plantedControl: number` prop on LiveGoalsHomeProps, which redded with three `error TS2741` lines naming live-goals.test.tsx:537/:565/:588. Removed; restore proven by SHA256 4a219cbb…d6e and a grep showing zero `plantedControl` occurrences. tsc genuinely reads this tree.\n  L2 focused four suites EXIT 0 — `Test Files 4 passed (4)`, `Tests 50 passed (50)`.\n  L3 `pnpm --filter @moe/control-room test` EXIT 0 — `Test Files 108 passed (108)`, `Tests 1435 passed (1435)`, FOUR consecutive runs.\n  L4 `git grep -n \"run-live-1\\|goal-live-1\" HEAD -- <the five owned paths under v2/goals>` EXIT 1, EMPTY — DoD 3 satisfied at the delivered tree under the scope reading recorded in comment-01ededd0. POSITIVE CONTROL on the same paths and revision: `git grep -c \"onOpenBoard\"` EXIT 0, prints 4 and 3.\n\nTEST-COUNT DELTA, ACCOUNTED FOR RATHER THAN ASSERTED. Step 1's baseline was 1429 at HEAD c873dfb2. Now 1435. The +6 decomposes exactly: 3 arms I added in step 6 (roster sweep, declaration arity, producer arity), 2 arms I added in step 7 (the two triage-door arms), and 1 arm a peer added in dba0cf30 (task-965cb2d6) to live-goal-create.test.ts, which arrived with the HEAD move from c873dfb2 to 2a27695d. Counted from `git diff -U0 | grep -E \"^\\+ *it\\(\"` on my own paths, not estimated.\n\nA FOREIGN RED I SAW, DISCLOSED IN FULL BECAUSE I CANNOT FULLY EXPLAIN IT. Before committing, THREE consecutive L3 runs failed on `src/v2/goals/live-goal-create.test.ts > live goal.create dispatch > reports an accepted create in plain words that name the goal` — `Test Files 1 failed | 107 passed (108)`, `Tests 1 failed | 1434 passed (1435)`. It is NOT attributable to this row and I proved that rather than asserting it:\n  every file in that suite's import closure — live-goal-create.test.ts, live-goal-create.ts, goal-model.ts, work-labels.ts, live-board-feed.ts, live-config.ts — measured BYTE-IDENTICAL to HEAD by `git hash-object` vs `git rev-parse HEAD:<path>`;\n  none of this row's six owned paths appears anywhere in that closure (`grep -rn \"goals-home\\|live-goals\\|cordum-app\"` over it EXITS 1);\n  my commit touches five paths and the intersection with that closure is EMPTY.\nIt then went green and has stayed green across four runs, with NO new commit between — HEAD's parent is 2a27695d in both cases. So a peer's uncommitted WIP on a closure file during my red window is the only explanation consistent with the measurements, but I did not catch it dirty and I will not claim a cause I did not observe. What I can state: the red pre-dates this row's delivery, its closure was unchanged by me, and the delivered tree is green four times running.\nThis is the second flap I hit today on this package; the first is in comment-2ecd0b15 (a peer mid-write on cordum-goals.css). A single red run of @moe/control-room is not evidence of a regression here — run it to stability and check `git status --porcelain -- apps/control-room/src` for foreign dirt first. — worker-57c90511",
      "timestamp": "2026-08-29T17:26:38.117Z"
    },
    {
      "id": "comment-4fc7b5d19e5e452c8cdf331aea88a4a4",
      "author": "worker-57c90511",
      "content": "STEP 7 RECORD, PART 2 of 2 — DRILLS AND ADVERSARIAL REVIEW. Commit 5834b676. Part 1 is comment-3bafcee0e3014411b64cfee15cabb254.\n\nSEVEN DRILLS. Each one degree of freedom, each mutation verified LANDED by grep BEFORE the run, each restore proven by SHA256 in a call SEPARATE from the restore, against %TEMP%/9bd8f529-worker-57c90511/.\n\nD1 (DoD 5a) REVERT cordum-app's `openBoard` TO TWO ARGS — **GREEN. THIS IS A FINDING, NOT A PASS.** The DoD predicts L1 reds naming :316/:319/:335. Measured: `typecheck` EXIT 0, and `pnpm --filter @moe/control-room test` EXIT 0 with `Test Files 108 passed (108)`, `Tests 1430 passed (1430)`. NOTHING in the package catches it — not the checker, not one of 1430 tests. TypeScript accepts a function with FEWER parameters where more are expected, and the discarded position is `title: string` sitting against `planningRunRef: string`, both `string`; there is nothing unsound to see. Under that mutant the app binds the PLANNING RUN into the board's `title` slot and ships. The drill as written can never red, so it certified nothing about whether those three consumers are covered — which is exactly the thing this row's history turns on.\n  CLOSED, not merely reported: step 6's new arm \"pins the arity of the shell's openBoard producer, which the checker cannot\" sweeps for the `const openBoard = useCallback((...))` producer and grades its full parameter list. RE-DRILLED against the same mutant: it REDS — `expected [ 'goalId: string', 'title: string' ] to deeply equal [ 'goalId: string', …(2) ]`. Restore proven, cordum-app.tsx 759a59c7…7fa.\n  STILL OPEN AND ROUTED: only a behavioural arm rendering CordumApp proves the opened board shows the goal's TITLE. Its home is cordum-app.test.tsx, which this row does NOT own — task-40017f79 does. Filed there rather than taken on my own authority, per comment-8747d052.\n\nD2 (DoD 5b) SWAP planningRunRef AND title at one call site — RED, both pins, on the argument VALUES: `expected \"vi.fn()\" to be called with arguments: [ 'goal-recovery', …(2) ]` and `[ 'goal-cmd-goal-create-1', …(2) ]`, `Tests 2 failed | 29 passed (31)`. This is what makes step 2's positional assertions load-bearing: both values are strings, so an arity-only assertion would have stayed green.\n\nD3 DELETE one roster member — RED on the exact count: `expected [ 'v2/cordum-app.tsx', …(6) ] to have a length of 8 but got 7`.\nD3b SUBSTITUTE a roster member instead, so the COUNT stays 8 — RED on set equality. Run because D3's count assertion fires first and would otherwise leave the equality direction ungraded; without D3b I could not claim both directions work.\n\nD4 EMPTY STRING in the run slot — RED, both pins, `Tests 2 failed | 32 passed (34)`.\n\nD5 BYPASS THE DURABLE-RUN GUARD and substitute `goal.planningRunRef ?? \"\"` — RED on EXACTLY ONE arm, the new \"refuses a triage strip that names a goal with no durable run\": `expected \"vi.fn()\" to not be called at all, but actually been called 1 times`. The card-door runless arm stayed GREEN, which is the point: GoalCard never invokes the adapter for a runless card, so the triage arm is the SOLE guard for that door.\n\nL1 POSITIVE CONTROL (a sub-second gate needs one) — planted a required prop, RED with three `error TS2741`; removed and proven. Recorded in part 1.\n\nADVERSARIAL REVIEW — and it found a real gap the plan did not.\n  \"Can the triage path still open a board for a goal with no durable run?\" No — but I discovered NOTHING TESTED IT. The triage strip is a THIRD door and the only one where goals-home is the SOLE decider: it does not render through GoalCard, so the sibling's disabled-Open control never gets a say. Its guard was the most important one on this row and had zero coverage. I added two arms (refusal for a runless goal; open with the SAME three durable arguments a card supplies) and drilled them with D5.\n  \"Does any call site pass `undefined` where a string is declared?\" No. `openableGoal` admits a goal only when `planningRunRef` is a NON-BLANK string — absence is \"not a non-blank string\", never merely `!== undefined`, because the type admits `\"\"` and an empty run opens nothing.\n  \"Is `OpenBoard.planningRunRef` ever read before it is set?\" No. `open` is `OpenBoard | null` and the field is set at construction in `setOpen({ goalId, planningRunRef, title })`; there is no partial state.\n  \"Does the runless arm exercise BOTH doors?\" Yes — the pre-existing arm clicks `.open-unavailable` AND `.title` and ends `not.toHaveBeenCalled()`. I did not add a third one; that would be the duplicate R3 warns about.\n  MY OWN CORRECTION, recorded rather than quietly fixed: my first arity arm expected cordum-app.tsx among the prop DECLARATIONS and redded. cordum-app.tsx PRODUCES the callback; it does not declare the prop. The expectation was wrong, not the code.\n\nLINE CAPS, every production file touched: goals-home.tsx 242, live-goals.tsx 123, cordum-app.tsx 377. All under 400; the first two under the 250 target and cordum-app.tsx was already 370 before this row added 7 lines.\n\nDOWNSTREAM AND SIBLING, as DoD 6 requires: task-40017f79a36e47afbcb69762acfc294a and task-4942111bdaac427985e4636b2bbd0cad consume this widened callback; task-14e3b36a819c478aa1d20b2aa8542dcc is the sibling half that landed the disabled Open control this row composes. task-40017f79 is BLOCKED on exactly this row and auto-unblocks when it goes DONE. — worker-57c90511",
      "timestamp": "2026-08-29T17:27:09.128Z"
    },
    {
      "id": "comment-cde092dc56724f37925104376376bdab",
      "author": "qa-b6149c54",
      "content": "QA REVIEW — APPROVE. Measured by me at HEAD 5834b676; nothing below is taken from the worker's record without re-running it.\n\nWORKTREE AND SCOPE. Porcelain over all six owned paths: EMPTY. 5834b676 is explicit-pathspec over exactly five owned files and nothing else. Most importantly it does NOT contain cordum-app.test.tsx — the seventh path the governor's clause 3 named and the reopenReason forbade taking, because task-40017f79 claims it by file field. The collision constraint was honoured rather than argued around.\n\nDoD-2, THE CORRECTED DETECTOR. `grep -rn \"onOpenBoard\" apps/control-room/src` finds the identifier in EIGHT files; the with-paren version finds it in TWO (goals-home.tsx, goals-home.test.tsx). The two spellings differ exactly as the rail warned, so the original detector's blindness is durably evidenced.\nTHE CLAUSE AS LITERALLY WRITTEN (\"every one of them is inside this row's six owned paths\") IS FALSE, AND IT IS STILL NOT A FINDING. goal-card.tsx and goal-card.test.tsx are outside the roster and appear in the detector — but they are not consumers of the WIDENED signature. goal-card.tsx:69 declares `readonly onOpenBoard: () => void` and invokes it with NO arguments at :95 and :152; goals-home.tsx:234 hands each card a per-card zero-arg closure, `onOpenBoard={() => openBoard(goal.goalId)}`. The three-argument contract lives strictly between goals-home.tsx:47/:111 and cordum-app.tsx:261. The detector counts a shared NAME, not a reachable signature, so the clause holds on the basis that matters and this is not the stop-and-report or split signal the DoD contemplated. I checked this rather than assume it, because if the card had taken the widened callback directly it would have been a genuine unowned consumer.\n\nDoD-3, THE PLACEHOLDER GREP. Unscoped it returns 26 hits and would read as a hard fail; scoped to the six owned paths per the governor's amendment (comment-100d13d2, carried in the reopenReason) it returns ONE: cordum-app.tsx:46, `const LIVE_RUN_SUBJECT = \"run-live-1\" as const`. That constant is PRE-EXISTING and is consumed only at :316 as `runId={LIVE_RUN_SUBJECT}` for a different surface — never in the openBoard path, which takes its three values from the caller. Its removal is task-4942111b's, and the governor's own parked list records \"4942111b (LIVE_RUN_SUBJECT still present)\" as that row's unpark condition. The clause's INTENT — no synthesised or placeholder id in the run slot — holds and is independently asserted: both pins take `onOpenBoard.mock.calls[0]?.[1]` and require it to equal the durable run, not to be `\"\"`, and not to be undefined.\n\nDoD-3 PINS, LOCATED BY CONTENT BECAUSE THE DoD's LINE ANCHORS HAVE MOVED (the DoD names goals-home.test.tsx:252 and live-goals.test.tsx:211; the pins are actually at goals-home.test.tsx:331 and :354 and live-goals.test.tsx:310). All three assert the full positional triple, and every expected value is READ BACK from the fixture or the daemon's catalog row that produced it — `opened?.goalId, opened?.planningRunRef, opened?.title` and `durable.goalId, durable.planningRunRef, durable.title` — so none is a hand-written literal agreeing with itself.\n\nDoD-4, BOTH DOORS. The runless arm asserts `.open` is absent, `.open-unavailable` is disabled and titled \"No durable planning run is recorded for this goal.\", and that clicking the TITLE button leaves `onOpenBoard` uncalled. goals-home.tsx:110 is the production guard: `if (goal?.planningRunRef === undefined) return;` before the three-arg call. Nothing passes an empty string, a placeholder or undefined-as-a-value.\n\nGATES I RAN, standalone, foreground, unpiped:\n- `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goals-home.test.tsx src/v2/goals/live-goals.test.tsx src/v2/goals/goal-card.test.tsx` -> EXIT 0, \"Test Files 3 passed (3)\", \"Tests 49 passed (49)\".\n- `pnpm --filter @moe/control-room typecheck` -> EXIT 0, echo `$ tsc --project tsconfig.json`.\n- `pnpm --filter @moe/control-room test` -> EXIT 0, \"Test Files 108 passed (108)\", \"Tests 1435 passed (1435)\".\n\nMY OWN DRILLS — TWO, and the second is the one that mattered.\n(1) DoD-5b, positional swap at goals-home.tsx:111. `typecheck` EXIT 0 — the swap is TYPE-INVISIBLE because both operands are strings — while the suite reds two arms: `opens from a triage strip with the same three durable arguments a card supplies` and `filters to the blocked goal and opens its board when it has a durable run`. That is the proof the pins grade POSITION and not arity, exactly as the DoD claims an arity-only assertion could not.\n(2) DoD-5a, the drill the worker reported GREEN as a FINDING. I reverted the cordum-app producer to two parameters. CONFIRMED: `typecheck` EXIT 0 — TypeScript accepts a function with fewer parameters where more are expected, so the checker genuinely cannot see this defect, and the DoD's prediction that it would red naming :316/:319/:335 was wrong. Their finding was real. AND THEIR CLOSURE IS REAL: under that same mutant the SOLE red is their step-6 arm `pins the arity of the shell's openBoard producer, which the checker cannot`, \"Tests 1 failed | 20 passed (21)\". A green drill was reported rather than banked, then closed with a guard that is independently load-bearing — that is the epic rail followed, not worked around.\nRESTORES, proven in calls separate from each restore: `git diff HEAD --exit-code` clean for both files; sha256 goals-home.tsx 5a9fcfa96fb9ee1ea4cb98ba50a5e5587bc063bdf1ae62ebcc11cab9973748a6 and cordum-app.tsx 759a59c779d2705588df4917008b84a97629ab937a70bb2064c6b85dc252f7fa, each equal to `git show HEAD:`. The cordum-app hash also matches the value the worker recorded for their own restore, which independently confirms theirs.\n\nLINE CAPS: goals-home.tsx 242, live-goals.tsx 123, cordum-app.tsx 377. All under 400.\n\nREPO-WIDE ATTRIBUTION. Every owned path is under apps/control-room/src/v2 and that package is fully green, so the intersection with the daemon suite is empty by construction — and I measured that suite twice today, at 13d5750e and 235a12db, identical both times. ON THE SEVEN BROWSER-LANE `cr2.shell.root` REDS being discussed on task-965cb2d6: they are NOT this row's, and the reason is not an intersection argument. That testid is rendered by cordum-shell.tsx:149, a file this row never touched, and this row's entire cordum-app.tsx delta is the openBoard parameter list plus a prop doc comment. The attribution circulating in chat says cordum-app.tsx renders that element; it does not.\n\nSCOPE DISCIPLINE WORTH NAMING: cordum-app.tsx stores `planningRunRef` without rendering it, and the added doc says why — widening BoardStub and the plan-review consumer to read it is task-40017f79's work, and doing it here would pull two more files in. That is the correct call under this row's roster.\n\nConsumers named as DoD-6 requires: comment-4fc7b5d1 names task-40017f79 and task-4942111b as downstream and task-14e3b36a as the sibling half. — qa-b6149c54",
      "timestamp": "2026-08-29T17:33:34.430Z"
    },
    {
      "id": "comment-08852efd28294ddba6edf54ab92f8b6c",
      "author": "qa-b6149c54",
      "content": "POST-APPROVAL — two notes.\n\n1. THE NO-COMPLETION-COMMIT WARNING IS A BOOKKEEPING ARTIFACT, not missing work. `qa_approve` returned `commitEvidence: {completion: [], checkpoint: [], rescue: []}`, but the bytes are at HEAD and I verified them before approving: 5834b676 `feat(task-9bd8f529): widen onOpenBoard to carry the durable planning run`, explicit-pathspec over five owned files, +272/-25, with porcelain over all six owned paths EMPTY. Verify with `git log --oneline --grep=9bd8f529`, not by reading task.commits. This is the THIRD row today with the same warning and the same cause — task-6646f888 (comment-9c7a5806) and task-2ae75398 (comment-e5f91cef) were the others. All three reached REVIEW through a hand-authored `feat(...)` commit rather than a wrapper completion commit, so the linkage field was never populated. \"No completion commit\" means either work never landed or linkage never recorded; only the first blocks a merge, and none of these three is that.\n\n2. THIS ROW GOING DONE IS AN UNPARK EVENT. worker-57c90511 recorded that task-40017f79 is BLOCKED on exactly this row and auto-unblocks when it closes; task-4942111b also consumes the widened callback. Both should now be re-checkable. One caution for whoever picks up task-40017f79: it owns cordum-app.test.tsx, which this row deliberately did NOT touch, and the worker routed a real open item there rather than take the path — only a behavioural arm rendering CordumApp can prove the opened board shows the goal's TITLE rather than its run id. The arity arm this row added proves the producer's parameter list, which the typechecker provably cannot (I re-drilled that myself: a two-param producer typechecks clean at EXIT 0). It does not prove what the board renders. That gap is real, it is named, and it belongs to task-40017f79. — qa-b6149c54",
      "timestamp": "2026-08-29T17:34:41.551Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-28T16:22:21.684Z",
  "updatedAt": "2026-08-29T20:15:10.365Z",
  "metrics": {
    "firstClaimAt": "2026-08-28T18:04:44.983Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 7,
    "doneAt": "2026-08-29T17:34:28.206Z",
    "wallClockMs": 84583223
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-28T18:07:19.006Z",
  "planningNotes": {
    "approachesConsidered": "CHOSEN — widen the declaration in goals-home.tsx and live-goals.tsx, widen `openBoard` and the `OpenBoard` state in cordum-app.tsx, and STORE the run without rendering it. Six owned paths, and the storage boundary is what keeps it six.\n\nREJECTED — passing planningRunRef down to BoardStub (:299) and the board consumer (:306-310). That is task-40017f79's work (it \"consumes the three-arg callback for its typed route\") and would pull two more files in, re-creating the oversized roster the split exists to avoid. This row delivers the run REACHING the app's board state; an arm asserts the stored state carries it, so it is asserted behaviour rather than an unused placeholder.\n\nREJECTED — touching goal-card.tsx. Measured: its prop is `onOpenBoard: () => void`, a ZERO-ARG THUNK, and goals-home.tsx:205 adapts with `onOpenBoard={() => onOpenBoard(goal.goalId, goal.title)}`. The adapter absorbs the widening completely, so the file is unaffected — and taskRail 3 assigns it to the sibling anyway.\n\nREJECTED — passing an empty string or `undefined` in the run slot to satisfy the type at goals-home.tsx:86 (the triage path, where the run is not already in hand). `planningRunRef` is OPTIONAL on the model, so a runless goal genuinely has no run; DoD 3 forbids a placeholder and DoD 4 says such a goal must not open a board at all. The call is guarded instead.\n\nREJECTED — planning this row without the sibling landed. DoD 4 requires proving a runless card cannot invoke the callback from EITHER door, and the disabled-Open behaviour that makes it true is task-14e3b36a's deliverable. Step 1(a) is a hard stop measured as CODE at HEAD, not as row status.\n\nREJECTED, AND THIS WAS MY OWN EARLIER ERROR — filing this row into BACKLOG to keep it from being claimed before its premise was met. The governor is right and it is now the second occurrence today: BACKLOG is invisible to every architect polling statuses:[\"PLANNING\"], so the row silently stalls everything behind it. The verify-or-block gate in DoD 1 is the correct mechanism for premature claiming; the column is not.",
    "codebaseInsights": "MEASURED AT HEAD 0bf353ff by architect-fdbab575, 2026-08-28. All six owned paths porcelain-CLEAN, cordum-app.tsx included.\n\n1. THE DETECTOR DEFECT, quantified. `grep -rn \"onOpenBoard(\" apps/control-room/src` finds only the two call sites in goals-home.tsx (:86, :205). The bare-symbol grep additionally finds the three prop-passing consumers in cordum-app.tsx (:316, :319, :335) — the ONLY ones that red the typecheck — plus goal-create-disabled.test.tsx:23 and the goal-card pair. The paren form is blind to `onOpenBoard={openBoard}`, and that blindness is what deadlocked task-40017f79 against its own child.\n\n2. SEVEN CONSUMER FILES, not the DoD's implied set: goals-home.tsx, goals-home.test.tsx, live-goals.tsx, live-goals.test.tsx, cordum-app.tsx, goal-create-disabled.test.tsx, goal-card.tsx/.test.tsx.\n\n3. goal-card IS NOT AFFECTED, and this is the non-obvious part. Its prop is `onOpenBoard: () => void` — zero-arg. goals-home.tsx:205 adapts (`() => onOpenBoard(goal.goalId, goal.title)`), so the widening stops at the adapter. Six paths, not eight.\n\n4. goal-create-disabled.test.tsx:23 passes `onOpenBoard={vi.fn()}`, which is arity-agnostic and will most likely typecheck unchanged. It is NAMED as an owned path under Rail 8(A) regardless, and the typecheck decides whether it needs an edit.\n\n5. `OpenBoard` IS FILE-LOCAL. Declared at cordum-app.tsx:241 and read only at :252, :256, :266, :278, :299, :306, :310 — `grep -rn OpenBoard apps/control-room/src` finds no external reader. So adding `planningRunRef` to the state fans out to nothing, which is what makes the store-don't-render boundary clean.\n\n6. THE TRIAGE CALL AT :86 IS THE HARD ONE. `onOpenBoard(strip.openGoalId, titleOf(strip.openGoalId))` resolves its title through a lookup; the run needs the same, and `planningRunRef` is OPTIONAL on the model (goal-model.ts:56), so a runless goal has nothing to pass. Unlike :205, the value is not already in hand.\n\n7. THE TWO EXACT-CALL PINS: goals-home.test.tsx:252 `toHaveBeenCalledWith(\"goal-recovery\", \"Genesis recovery binding on a fresh store\")` and live-goals.test.tsx:211 `toHaveBeenCalledWith(DURABLE_GOAL_ID, \"Durable title\")`.\n\n8. THE SIBLING HAS NOT LANDED: `git grep -c \"open-unavailable\" HEAD -- goal-card.tsx` EXITS 1. task-14e3b36a is mid-flight (steps 1-2 COMPLETED, step 3 IN_PROGRESS). Step 1(a) blocks on this, measured as code.",
    "risks": "1. THE SIBLING GATE IS A REAL STOP. `open-unavailable` is not at HEAD; DoD 4's both-doors property has nothing implementing it until task-14e3b36a lands. Step 1(a) blocks rather than proceeding — do not weaken the arm to fit an unlanded dependency.\n2. TITLE MOVES FROM SECOND TO THIRD, and both it and the run are STRINGS. A positional swap typechecks silently and an arity-only assertion cannot see it. Every pin asserts values positionally; drill D2 is the proof, and a green D2 is a FINDING.\n3. THE TRIAGE PATH AT :86 is where a placeholder gets introduced to satisfy the type. `planningRunRef` is optional; a runless goal must not open a board (DoD 4). Guard the call; never pass `\"\"` or `undefined`.\n4. DO NOT RENDER THE RUN. Passing it to BoardStub pulls two more files in and is task-40017f79's scope. Store and assert; stop there.\n5. DO NOT TOUCH goal-card.* — the sibling owns them and the zero-arg thunk means the widening never reaches them anyway.\n6. live-goals.* WAS task-115fedb7's. Measured clean now, but both rows commit by pathspec over the same files; re-measure porcelain immediately before editing and stop if it is dirty with foreign hunks.\n7. AN EIGHTH CONSUMER appearing at step 6's re-run is a stop-and-report, not an extra path. That is the failure mode this row's whole history is made of.",
    "keyFiles": [
      "apps/control-room/src/v2/goals/goals-home.tsx",
      "apps/control-room/src/v2/cordum-app.tsx",
      "apps/control-room/src/v2/goals/live-goals.tsx",
      "apps/control-room/src/v2/goals/goal-model.ts",
      "apps/control-room/src/v2/goals/goal-card.tsx"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-28T20:34:00.152Z",
    "escalatedAt": "2026-08-28T20:34:00.152Z"
  },
  "planApprovedAt": "2026-08-29T16:49:24.399Z",
  "contextFetchedBy": [
    "worker-f16b412c",
    "worker-ee96dea4",
    "governor-8c340ce3",
    "architect-5118c1bc",
    "worker-57c90511",
    "qa-b6149c54",
    "architect-dba58d99"
  ],
  "workStartedAt": "2026-08-28T20:35:02.967Z",
  "blockedReason": "Plan-time HEAD 6a642a80 re-measurement confirms the sibling landed, but the task contract remains internally impossible: the required onOpenBoard detector returns 8 files while DoD 2 says all are within 6 and taskRail 3 forbids the goal-card pair; DoD 3's required run-live-1|goal-live-1 subtree grep exits 0 with many tracked out-of-scope hits, so EMPTY cannot be delivered; taskRail 1 mandates cordum-app.test.tsx as path six while reachability also names goal-create-disabled.test.tsx, requiring seven paths. Durable evidence and exact outputs are in comment-8120393d39204b0896c13fbdf84981c0. No bytes authored.",
  "blockedResourceId": null,
  "blockedFromStatus": "PLANNING",
  "blockedAt": "2026-08-29T13:17:56.162Z",
  "priorHandoffs": [
    {
      "whatIsDone": "Re-measured HEAD 6a642a80; confirmed prerequisite landed and recorded three task-contract contradictions in comment-8120393d39204b0896c13fbdf84981c0. No bytes authored.",
      "whatRemains": "Governor/human must amend DoD 2, DoD 3, and the exact owned-path roster before replanning.",
      "pitfalls": "Do not submit a plan against the current text: onOpenBoard grep returns 8 files, the required run-live-1|goal-live-1 grep is nonempty across out-of-scope files, and cordum-app.test.tsx plus goal-create-disabled.test.tsx make the roster 7.",
      "openQuestions": "Which of cordum-app.test.tsx and goal-create-disabled.test.tsx should be owned/read-only, and what delivered-tree literal check replaces the impossible subtree-emptiness gate?",
      "releasedAt": "2026-08-29T15:59:54.547Z",
      "releasedBy": "architect-5118c1bc",
      "reason": "Human explicitly requested the next task after this row was blocked; hand off the blocked row without changing its status."
    },
    {
      "whatIsDone": "Read-only step-1 probes completed; no bytes authored. Exact evidence in comment-85c330dd376c457591dd2169fe80dc10.",
      "whatRemains": "Architect/governor must break the task-14e landing cycle and correct the onOpenBoard consumer denominator before implementation.",
      "pitfalls": "Preserve dirty goals-home.test.tsx from task-14e. Do not treat uncommitted goal-card WIP as a landed HEAD premise without an amendment. There are 8 distinct consumer files, not 7.",
      "openQuestions": "Can the gate adopt verified on-disk sibling WIP, and is the exact roster 8 files or 7 logical concerns?",
      "releasedAt": "2026-08-28T20:36:16.450Z",
      "releasedBy": "worker-ee96dea4",
      "reason": "Explicit handoff after step-1 stop-and-report so the worker can continue the user's requested next-task queue."
    }
  ],
  "reviewStartedAt": "2026-08-29T17:29:08.329Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": "$ vitest run\n\n RUN  v4.1.10 D:/projexts/moe-next/apps/control-room\n\n Test Files  108 passed (108)\n      Tests  1435 passed (1435)\n   Start at  20:28:38\n   Duration  12.13s (transform 23.58s, setup 0ms, import 47.49s, tests 90.18s, environment 132.48s)\n\nEXIT=0  [run fresh at HEAD 5834b676285cd88a2334b6e95f6296b5b6c34680, the delivered commit]",
    "reportedAt": "2026-08-29T17:29:08.329Z"
  },
  "filesModified": [
    "apps/control-room/src/v2/goals/goals-home.tsx",
    "apps/control-room/src/v2/goals/goals-home.test.tsx",
    "apps/control-room/src/v2/goals/live-goals.test.tsx",
    "apps/control-room/src/v2/goals/live-goals.tsx",
    "apps/control-room/src/v2/cordum-app.tsx"
  ],
  "completionSummary": "onOpenBoard widened to (goalId, planningRunRef, title). Commit 5834b676, five paths by explicit pathspec (the sixth, goal-create-disabled.test.tsx, needed no edit — vi.fn() is arity-agnostic; confirmed by typecheck, not by editing).\n\nEVIDENCE LIVES IN COMMENTS, not here: comment-01ededd0 (step-1 gate, three scope readings of DoD 3), comment-2ecd0b15 (baseline flap), comment-3bafcee0 (detector + gates + red attribution), comment-4fc7b5d1 (seven drills + adversarial review). Routed to the parent: comment-4ed8540a on task-40017f79.\n\nGATES at the delivered tree: L1 typecheck exit 0 with the $ tsc echo and a PLANTED-ERROR CONTROL (554ms is sub-second, so the green alone proved nothing; the control redded with three TS2741). L2 4 files / 50 tests exit 0. L3 108 files / 1435 tests exit 0, four consecutive runs. L4 the run-live-1|goal-live-1 literal gate EMPTY at HEAD over the owned paths, with a positive control.\n\nTWO FINDINGS, both reported rather than banked. (1) DoD 5(a)'s drill is VACUOUS: reverting cordum-app's openBoard to two args passes typecheck AND all 1430 package tests, because TypeScript accepts fewer parameters and the dropped position is a string against a string — under that mutant the app binds the planning run into the board's TITLE slot and ships. Closed with a source-text arity pin that reds on it; the behavioural CordumApp arm is routed to task-40017f79, which owns cordum-app.test.tsx (I did not take that path on my own authority). (2) Adversarial review found the TRIAGE strip — a third door, and the only one where goals-home is the sole decider — had ZERO coverage for its runless guard. Two arms added and drilled.\n\nSeven drills, one degree of freedom each, every mutation grep-verified landed and every restore SHA256-proven in a separate call. A foreign red in live-goal-create.test.ts was disclosed and attributed by import closure (every file in it byte-identical to HEAD, owned-path intersection EMPTY); it later cleared.\n\nDoD: 1 satisfied; 2 satisfied ",
  "completedAt": "2026-08-29T17:34:28.206Z",
  "reviewCompletedAt": "2026-08-29T17:34:28.206Z",
  "reviewSummary": "APPROVED. Evidence: comment-cde092dc56724f37925104376376bdab. HEAD 5834b676; porcelain over all six owned paths EMPTY. Explicit-pathspec over five owned files, and NOT cordum-app.test.tsx — the task-40017f79 collision was honoured.\n\nDoD-2: the without-paren detector finds onOpenBoard in 8 files, with-paren in 2; the difference is evidenced. \"Every consumer inside the six owned paths\" is literally false since goal-card.tsx/.test.tsx appear, but they are NOT consumers of the widened signature: goal-card.tsx:69 declares it as () => void and calls it with no args, while goals-home.tsx:234 hands each card a zero-arg closure. The 3-arg contract lives only between goals-home.tsx:47/:111 and cordum-app.tsx:261. Name collision, not reachability.\n\nDoD-3 scoped per the governor's amendment returns ONE hit: cordum-app.tsx:46 LIVE_RUN_SUBJECT, pre-existing, used only at :316 as another surface's runId, never in the openBoard path; its removal is task-4942111b's unpark condition. Intent holds: both pins assert the run slot equals the durable ref and is neither \"\" nor undefined, and each reads its expected values back from the fixture or catalog row that produced them.\n\nGATES: focused 3-file EXIT 0, \"3 passed (3)\" / \"49 passed (49)\"; typecheck EXIT 0 with $ tsc echo; full control-room EXIT 0, \"108 passed (108)\" / \"1435 passed (1435)\".\n\nMY DRILLS: (1) positional swap at goals-home.tsx:111 — typecheck EXIT 0, type-invisible as both are strings, while two exact-call pins red on VALUES. (2) the drill the worker reported GREEN as a finding: I reverted the producer to two params, confirmed typecheck EXIT 0 so the checker cannot see it, and their closure holds — sole red is their new arity arm. Restores clean, sha256 5a9fcfa9 and 759a59c7 equal git show HEAD.\n\nCaps 242/123/377. The browser-lane cr2.shell.root reds are not this row's: that testid renders in cordum-shell.tsx:149, untouched here."
}