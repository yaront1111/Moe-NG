{
  "id": "task-6646f888a488481e936dc0dd3f966cd5",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "P2.15d-i Land a daemon-owned approval intent seam so the Control Room can approve a plan without caller-shaped authority bytes",
  "description": "Leaf carved out of task-40017f79 (P2.15d) by architect-ac8532da on 2026-08-27 at HEAD fa0736e3. THE GAP, measured: the only plan-approval command is `approval.decide`, whose exact payload roster is `[\"activation\", \"command\", \"graphRevisionRef\", \"record\", \"runId\"]` (apps/daemon/src/daemon-command-vocabulary.ts:196) and whose handler `intentOf` (apps/daemon/src/daemon-command-graph-approve.ts:81-95) REQUIRES the caller to supply the `activation` object (activationRef, expectedGoalVersion, graphHash, policyHash, qualityHash, `truthClass: \"HUMAN_APPROVED\"`), the `command` object (decision, decisionReason, stepUpAuthRef…) and the `record` object — i.e. the caller authors the activation witness and the approval record. The human grant itself is minted server-side from a `HumanReviewWitness` (`operatorReviewAuthority` :58-70, `grantHumanAuthority({kind:\"HUMAN\", principalId: witness.principalId}, …)`), so the PRINCIPAL is honest, but the activation/record bytes are caller-shaped. The Control Room's only payload for it is `DEV_PAYLOADS[\"approval.decide\"]` (apps/control-room/src/live/live-dispatch.ts:185-195), explicitly fixture-shaped (`hex64(\"6a\")` hashes, `\"activation-1\"`), and the approve screen is deliberately disabled with a \"no fabricated decision\" note (apps/control-room/src/v2/goals/approve-plan.tsx:41-50; pinned by tests/e2e/control-room/prd-to-approval.spec.ts:127-128). P2.15d's task rail forbids treating a caller-shaped HUMAN flag or enabled button as approval authority, so the UI cannot be wired to this command as it stands (architect-be964894 reached the same conclusion on 08-25, comment-9810f791).\n\nWHAT THIS LEAF LANDS: a daemon-owned seam where the caller supplies ONLY intent — `{ runId, decision: \"APPROVE\" | \"REJECT\", decisionReason }` — and the daemon derives the activation witness (graph/policy/quality hashes, expectedGoalVersion, truthClass) and the approval record from the DURABLE run in PLAN_REVIEW (the sealed plan the planning-run read already serves, apps/control-room/src/live/live-planning-run.ts:322 over POST /planning/run/read) and from the authenticated operator session, then commits through the existing approval edge so the core `applyApprovalCommand`/`decideApprovalAuthority` path is unchanged. Two designs to choose between at planning, measured symbol by symbol: (A) a NEW runtime command kind (e.g. `approval.decide_intent`) — carries the five-file generated fan-out (runtime-vocabulary.ts frozen tuple, runtime-contract.test.ts literal count, stdio-schemas injectivity, regenerated control-room-client + goldens, release-subject digest mirrors; precedent task-ad61563a / 5ff4e396), or (B) a second exact payload shape admitted by the daemon for the EXISTING kind (a shape switch on the exact key roster: full shape for existing callers, intent shape derived server-side) — no generated fan-out, but the vocabulary test's PAYLOAD_KEYS roster and the registry/security arms that enumerate exact rosters must be traced (rail 7B). Whichever is chosen, hostile arms must pin: a caller-supplied `activation`/`record` beside intent refuses at the exact-shape fence; an AGENT session refuses `APPROVAL_HUMAN_REVIEW_REQUIRED`/the core human-authority tuple verbatim; a run not in PLAN_REVIEW refuses the run-state code; replay is the ledger's answer. Consumer: task-40017f79 (P2.15d) wires the Control Room approve button to this seam, affordance-gated.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK on disk: `approval.decide` PAYLOAD_KEYS at daemon-command-vocabulary.ts still require `activation` and `record` (else the seam exists — duplicate, report blocked); `intentOf` at daemon-command-graph-approve.ts still reads them from the caller payload; no daemon file admits an intent-only approval shape; porcelain of the daemon command files you will touch is clean or carries only your hunks; the design choice (new kind vs second shape) is recorded with its measured fan-out set before any edit.",
    "A daemon-owned seam admits EXACTLY `{ runId, decision, decisionReason }` (plus the session presentation if the existing witness path requires one — measure how `humanReview` reaches the registry), derives activation witness and record from the durable PLAN_REVIEW run and the authenticated operator, and commits through the existing approval edge; the caller cannot supply `activation`, `record`, `truthClass`, hashes, `stepUpAuthRef` or any principal/time — each extra key refuses at the exact-shape fence with the stable code and stage, asserted over a nonzero generated roster.",
    "Refusals carry exact code AND layer verbatim: AGENT/non-operator session → the core/daemon human-authority tuple unchanged; run absent or not in PLAN_REVIEW → the run-state code; graph revision mismatch → the existing graph code; replay → the ledger's stored decision without a second event; conflicting retry → the ledger's bytes-conflict tuple.",
    "If a new RUNTIME_COMMAND_KIND is introduced, all generated artifacts and digest mirrors are declared owned paths and regenerated (never hand-edited) in the same commit and `pnpm --filter @moe/control-room-client test` passes with a nonzero count; if not, a row comment says so explicitly and the vocabulary/registry/security roster arms that enumerate exact payload rosters are updated in both directions.",
    "Focused daemon suites run standalone with nonzero counts (`pnpm exec vitest run --root apps/daemon --config package.json <owned tests>`), `pnpm --filter @moe/daemon typecheck` exits 0 with its `$ tsc` echo; drills red a named arm each: honour a caller `activation` → the shape-fence roster arm; skip the PLAN_REVIEW check → the run-state arm; mint the grant from a payload principal → the AGENT arm. Every production file ≤ 400 lines (split before); explicit-pathspec commit; completion comment names task-40017f79 as the consumer."
  ],
  "taskRails": [
    "Human authority is not delegable: the daemon derives witness, record and grant from durable state and the authenticated session; the caller supplies intent only.",
    "Do not enable or reshape DEV_PAYLOADS in the Control Room here; do not edit Control Room files (task-40017f79 owns the UI half).",
    "Trace rail 7B fan-out before choosing a new command kind; explicit-pathspec commits only; preserve foreign WIP."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "STEP 1 — VERIFY-OR-BLOCK GATE (read-only; author no bytes). Record with moe.add_comment — the completion summary is write-only and persists nowhere. Re-capture `git rev-parse HEAD` and quote it beside every grep, each UNPIPED with its own exit code and a positive control.\n\nREAD THIS FIRST — THIS ROW AND task-ba102165 WERE A CYCLE, AND THIS ROW IS THE BREAK. task-ba1021652dcc4469bc4deb04a8e7d7d5 (durable riskTier + run-linked applicablePolicyRef) is BLOCKED, and its block reason names this row as the reason: \"neither daemon-command-graph-approve.ts nor planning-services.ts composes an ApprovalDecisionRecord after decideApprovalAuthority. Both consume a caller-shaped record BEFORE authority and forward it to activation... the missing daemon-owned seam is already owned by blocked consumer task-6646f888. Adding only the planned producer would be an orphan.\" Meanwhile this row was blocked waiting on that producer's durable sources.\nTHE RESOLUTION, and it is an ordering not a compromise: THIS ROW LANDS FIRST. It creates the daemon-owned composition site. Until that site exists there is nowhere for a durable tier or policy ref to be composed INTO, which is exactly why the producer would have been an orphan. Do NOT wait on ba102165 and do NOT re-block on it.\nHOW THIS ROW SURVIVES THE MISSING TIER HONESTLY: where the record needs a fact that has no durable source yet, the seam REFUSES with a stable code naming the missing fact — it does not default one and it does not accept one from the caller. ba102165 then makes that fact available and the refusal stops firing. Absence and a defaulted value are different answers, and the tier in particular decides whether step-up human authority is required (`approval-invalidation.ts:73` special-cases R3), so a defaulted tier would silently decide an authority question. Record in your step note WHICH facts you had to refuse on — that list is what ba102165 consumes next.\n\n(a) DoD-1 PROBES, architect-measured at HEAD 6a642a80; re-run each. `intentOf` at `daemon-command-graph-approve.ts:90-96` still reads `activation`, `command`, `graphRevisionRef`, `record` and `runId` FROM THE CALLER PAYLOAD via `payloadObject`/`payloadRef` — that is the caller-shaped authority this row replaces. If a daemon-owned seam now exists, this row is a DUPLICATE: adopt and verify, then report blocked.\n(b) THE FOREIGN-WIP PREMISE THAT PARKED THIS ROW HAS EXPIRED (governor, human-approved 13:10Z): task-0ca390d9's WIP landed as aebdb367 and the cited paths were porcelain-clean with an ls-files positive control. Re-measure `git status --porcelain` on every path you will touch and stop on anything dirty you did not author.\n(c) THE COMMAND-KIND DECISION, which DoD-4 makes a required judgement rather than a default — make it in this step and record the reason. Either (i) REUSE `approval.decide`, whose PAYLOAD_KEYS is an EXACT allow-list currently requiring `activation` and `record`, so narrowing it to `{runId, decision, decisionReason}` changes a frozen roster and every consumer asserting it; or (ii) INTRODUCE A NEW KIND, which triggers Rail 7B: every generated artifact and digest mirror becomes an owned path and is REGENERATED, never hand-edited, in the same commit, with `pnpm --filter @moe/control-room-client test` passing at a nonzero count. Measure the fan-out of BOTH with `git grep -l` before choosing, and state the count that decided it. If neither fits this row's path budget, that is a SPLIT signal at plan time — report it rather than discovering it at step 4.\n(d) MEASURE HOW `humanReview` REACHES THE REGISTRY (DoD-2 names this explicitly): the seam derives the witness from the authenticated session, never from the payload, so you must know the existing route before you can compose one. `daemon-command-graph-approve.ts:125-129` and `planning-services.ts:263-273` are the two production precedents.\n(e) OWNERSHIP: file-field scan across non-DONE rows plus porcelain. Task rail 2 fences the UI half — do NOT edit Control Room files (task-40017f79 owns them) and do NOT enable or reshape DEV_PAYLOADS.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.ts",
        "apps/daemon/src/daemon-command-graph-approve.ts"
      ],
      "note": "GATE PASSES — not a duplicate. HEAD 6a642a80. Full measurements in comment-68c544eda0d24227aa1c2e8e441b7ac0 (read-only step, no bytes authored).\n\n(a) intentOf still caller-shaped: daemon-command-graph-approve.ts:94-98 reads activation/command/graphRevisionRef/record/runId off request.payload. vocabulary:207 PAYLOAD_KEYS[\"approval.decide\"] unchanged. `grep -rn decide_intent apps/daemon/src/` = ONE hit, a comment in mcp-tool-allowlist.test.ts:102. No daemon file admits an intent-only shape.\n\n(c) DECISION: NEW KIND `approval.decide_intent`. Fan-out counts that decided it — reuse of `approval.decide` = 90 tracked files (git grep -l, .moe excluded); new kind = 4 files, ALL FOUR ALREADY CARRYING IT (runtime-vocabulary.ts:86, runtime-contract.test.ts:36, generated-client.ts:178/:280, stdio-schemas.test.ts:88-94), published by task-3051c597 (DONE). Rail 7B fan-out ALREADY PAID; zero regeneration in this row, nothing hand-edited.\n\n(c-bis) FAMILY: own EDGE via CommandEdgeContext (resource.reconcile precedent, registry:207-208), NOT the bootstrap family. Measured trap: BOOTSTRAP_COMMAND_KINDS membership reaches a durable-SEQUENCE assertion at bootstrap-durability.test.ts:138 plus bootstrap-services.test.ts:80-81 and bootstrap-ledger.ts:420.\n\n(d) humanReview minted ONLY at daemon-command-registry.ts:199-202/:248-253 from principal.principalId === operatorPrincipalId; HumanReviewWitness is {principalId} only and the registry comment says it carries NO transport fact. Therefore stepUpAuthRef is NOT derivable (recovery mints one from sessionReplayDigest over a presented authentication) — payload stays exactly {runId, decision, decisionReason} and the seam refuses on that fact instead.\n\n(b) PORCELAIN: one foreign hunk only — M apps/daemon/src/planning/planning-services.ts, +1 line `humanReview: context.humanReview,` at :309. Not mine; not reverted, not committed. Routed around: the handler registers through its own edge module so PLANNING_HANDLERS is never edited. All other target paths clean (git ls-files positive control returned the three tracked roster files).\n\n(e) graph-approve.ts untouched; no Control Room file touched; DEV_PAYLOADS untouched.\n\nFACT LEDGER — DERIVED: actor, actorKind, truthClass, lifecycle, validity, decision, decisionReason, dependencyChanges, policyDecisionRef(null), approvalRef, exactRevisionHash (run submissionHash), planQualityAssessmentRef (state.sealedHashes.qualityHash), criteriaRef (sealed-bodies criteriaDigest), approvedNodeScope.\nREFUSED for want of a durable producer, one code each — these are exactly what task-ba102165 consumes: riskTier, stepUpAuthRef, applicablePolicyRef, budgetRef.\n\nrefuse() (bootstrap-ledger.ts:240-255) takes a free `code: string` and does NOT route through createRuntimeError, so this seam's own codes cannot degrade to UNKNOWN_ERROR; the anti-UNKNOWN_ERROR arm still guards forwarded refusals.",
      "startedAt": "2026-08-29T13:15:54.385Z",
      "completedAt": "2026-08-29T13:22:56.446Z"
    },
    {
      "stepId": "step-2",
      "description": "STEP 2 — RED FIRST, ON THE PRODUCTION SURFACE. Do not write the seam yet.\n\nWrite the arms that pin what a daemon-owned intent seam must do, and watch each fail against today's caller-shaped path.\n(1) INTENT ONLY: a request carrying EXACTLY `{runId, decision, decisionReason}` (plus the session presentation step 1(d) measured, if the witness route requires one) is admitted. Today the path demands `activation` and `record` from the payload, so this reds on a missing-key refusal — quote it; it is the reproduction.\n(2) CALLER AUTHORITY IS REFUSED, and this is the row's whole point (task rail 1: \"Human authority is not delegable\"). A request that supplies its own `activation` or its own `record` is REFUSED with an exact stable code AND the refusing layer, both asserted. Today it is HONOURED — that inversion is the defect.\n(3) THE WITNESS AND RECORD ARE DERIVED FROM DURABLE STATE: assert the composed record's fields trace to the durable PLAN_REVIEW run and the authenticated session, not to any caller-supplied value.\n(4) REFUSALS CARRY THE EXISTING VOCABULARY VERBATIM (DoD-3), each asserting code AND layer: an AGENT or non-operator session -> the core/daemon human-authority tuple UNCHANGED; a run absent or not in PLAN_REVIEW -> the existing run-state code; a graph revision mismatch -> the existing graph code; a replay -> the ledger's stored decision. Do NOT invent a local vocabulary for any of these — forward what the existing layers already emit.\n(5) THE MISSING-FACT REFUSAL from step 1: where the record needs a fact with no durable source, assert the seam refuses naming that fact, with an exact code and layer. This arm is what ba102165 will later flip from refusing to succeeding, so name it clearly enough that the next row can find it.\n\nBEFORE CHOOSING ANY NEW REFUSAL CODE, grep the runtime error registry for which codes admit the aggregate this seam uses. `createRuntimeError` (packages/contracts/src/runtime/runtime-error-factory.ts:93-104) returns `UNKNOWN_ERROR` and DOES NOT THROW when the code is unknown or the descriptor does not list the aggregate — a wrong code compiles, runs, refuses, and silently loses its identity while every \"it refused\" arm stays green. Add a dedicated arm asserting no refusal this seam produces has code `UNKNOWN_ERROR`.\n\nRun standalone, FOREGROUND, UNPIPED: `pnpm exec vitest run --root apps/daemon --config package.json apps/daemon/src/planning`. The root vitest include has NO `apps/**`, so a bare `pnpm test` runs ZERO daemon tests. Quote every red with its count line.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.test.ts",
        "apps/daemon/src/daemon-command-registry.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "RED FIRST — both reds captured, standalone, foreground, unpiped.\n\nRED 1 (the suite): `pnpm exec vitest run --root apps/daemon --config package.json apps/daemon/src/planning/approval-intent.test.ts` EXIT=1 —\n  \"Error: Cannot find module './approval-intent.js' imported from .../approval-intent.test.ts\"\n  \"Test Files  1 failed (1)\" / \"Tests  no tests\". The seam does not exist; that IS the gap.\n\nRED 2 (the reproduction on TODAY'S production surface): a throwaway probe sent the exact intent-only payload {decision, decisionReason, runId} to the SHIPPED `approval.decide` through the production `send()` pipeline, on a store driven to PLAN_REVIEW by `driveThrough(store, \"approval.decide\")`. Outcome verbatim:\n  {\"advisoryOnly\":true,\"authority\":\"NONE\",\"code\":\"BOOTSTRAP_PAYLOAD_INVALID\",\"error\":null,\"kind\":\"approval.decide\",\"ok\":false,\"refusedBy\":\"DAEMON_INGRESS\"}\n  \"Test Files 1 failed (1)\" / \"Tests 1 failed (1)\", EXIT=1. Probe deleted in the same step; `git status --porcelain -- apps/daemon/src/planning/` shows no `__intent-repro` path.\nThis is the inversion the row removes: the shipped path DEMANDS `activation` and `record` from the caller and refuses intent alone.\n\nARMS WRITTEN (apps/daemon/src/planning/approval-intent.test.ts, 5 describes):\n(1) intent-only is admitted PAST THE SHAPE FENCE — asserted narrowly as \"not APPROVAL_INTENT_SHAPE_INVALID\", because four record facts have no durable producer yet so success is unreachable and asserting it would be dishonest.\n(2) caller `activation` and caller `record` each refused APPROVAL_INTENT_SHAPE_INVALID @ DAEMON_APPROVAL_INTENT, in SEPARATE arms (one degree of freedom each) — plus a generated sweep over 18 authority-bearing extra keys with the denominator PINNED (`expect(forbidden.length).toBe(18)`) so a zero-case sweep cannot pass, and a second sweep over every OMITTED intent key with a nonzero-count assertion.\n(3) derivation traced to durable state: exactRevisionHash === run state `submissionHash`, planQualityAssessmentRef === `sealedHashes.qualityHash`, criteriaRef === the sealed-bodies event's `criteriaDigest` (selected BY TYPE, not index), graphRevisionRef === the run's own. Every expected value is READ from the durable record, never restated in the test.\n(4) existing vocabulary forwarded verbatim: witness-less dispatch -> APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY; foreign-principal witness -> same tuple; unknown run -> BOOTSTRAP_PREREQUISITE_MISSING @ DAEMON_PREREQUISITE; run short of PLAN_REVIEW -> APPROVAL_RUN_NOT_REVIEWABLE @ APPROVAL_RUN_BINDING.\n(5) missing-fact refusal -> APPROVAL_INTENT_RISK_TIER_UNAVAILABLE @ DAEMON_APPROVAL_INTENT, plus a roster arm pinning one code per fact (BUDGET_REF / POLICY_REF / RISK_TIER / STEP_UP).\nPlus the dedicated anti-UNKNOWN_ERROR arm over five probe shapes.\n\nDIVERGENCE FIXTURE (epic rail 7A), stated in the file header. `bootstrap-test-fixtures.ts:34-35` sets MOE_APPROVAL_MODE=SPEED with a stated zero delay, so `decideApprovalAuthority` returns PROCEED for a gate-free run and the POLICY cannot be what refuses a witness-less request. Under SPEED this seam's own human-witness fence is the ONLY mechanism that can answer the AGENT arm — loosen it by one and that arm goes green with nothing else noticing. Under REQUIRE_HUMAN the policy would emit the same tuple and the arm would prove only that the SYSTEM refuses, not that THIS seam refuses.\n\nREFUSAL-CODE REGISTRY CHECK: `refuse` (bootstrap-ledger.ts:240-255) takes a free `code: string` and does NOT route through `createRuntimeError`, so this seam's four own codes cannot degrade to UNKNOWN_ERROR. The anti-UNKNOWN_ERROR arm still guards the FORWARDED core/authority refusals, which do go through the factory.\n\nFOREIGN DIRT observed under apps/daemon/src/planning/ and left untouched: approval-activation.test.ts, approval-activation.ts, graph-supersede-facts.ts, graph-supersede-service.test.ts, planning-services.ts, supersession-preparation-service.test.ts.",
      "startedAt": "2026-08-29T13:22:58.846Z",
      "completedAt": "2026-08-29T13:26:00.645Z"
    },
    {
      "stepId": "step-3",
      "description": "STEP 3 — THE SEAM. New `apps/daemon/src/planning/approval-intent.ts` (<=250 lines) PLUS its `.js` bridge — this tree uses bridges beside every module and a missing one is invisible to tsc and vitest while redding unrelated files at runtime.\n\nTHE CONTRACT: admit EXACTLY `{runId, decision, decisionReason}` (plus the session presentation measured in step 1(d) if the witness route requires it) as an EXACT-KEY admission — a fourth key is REFUSED, never trimmed. Trimming is how caller-chosen authority gets in, and this seam exists precisely to stop that.\n\nDERIVE, NEVER ACCEPT (task rail 1, and the DoD's central clause):\n- the ACTIVATION WITNESS and the APPROVAL RECORD from the durable PLAN_REVIEW run read out of the store;\n- the HUMAN GRANT from the authenticated session, by the route step 1(d) measured — through `decideApprovalAuthority`, which consults the gate FIRST by construction (packages/core/src/planning/approval-policy.ts:127-137) and returns `checkHumanAuthority`'s refusal verbatim. Never `checkHumanAuthority` directly: it is deliberately unpublished from the core barrel (packages/core/src/index.ts:239) and core exposes a single `exports` subpath, so a deep import fails TS6059.\nThe caller supplies INTENT ONLY. If any field the record needs cannot be derived from durable state or the session, REFUSE naming that field — do not default it and do not fall back to the caller's value.\n\nCOMPOSE THE EXISTING AUTHORITY PIECES, never reimplement them: the two production precedents are `daemon-command-graph-approve.ts:125-137` and `planning-services.ts:263-281`. A second human-authority path is a competing authority and is forbidden.\n\nDO NOT TOUCH `daemon-command-graph-approve.ts` in this step. This row lands a NEW seam; retiring the old caller-shaped `intentOf` path is a separate concern and touching it here would put this row's change inside a file whose current behaviour other arms still assert. If your measurement says the two cannot coexist, STOP and report rather than editing it.\n\nRe-run step 2: arms (1), (3), (4), (5) go green against the new seam. Arm (2) may still be red if the OLD path remains reachable — that is step 4's wiring.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.js",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.js",
        "apps/daemon/src/planning/approval-intent-sources.ts",
        "apps/daemon/src/planning/approval-intent-sources.js",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger.ts"
      ],
      "note": "THE SEAM LANDS. `pnpm exec vitest run --root apps/daemon --config package.json apps/daemon/src/planning/approval-intent.test.ts` -> EXIT=0, \"Test Files 1 passed (1)\" / \"Tests 16 passed (16)\". `pnpm --filter @moe/daemon typecheck` -> EXIT=0 with its `$ tsc --project tsconfig.json` echo.\n\nSPLIT INTO TWO PRODUCTION SOURCES (epic rail 6). The single file measured 318 lines, over the 250 target, so it split on a real responsibility line, not an arbitrary one: `approval-intent-sources.ts` (146) answers \"what does the store already know about this run\"; `approval-intent.ts` (201) answers \"may this principal approve, and can a record be composed\". Each carries its `.js` bridge — a missing bridge is invisible to tsc and vitest while redding unrelated files at runtime.\n\nCONTRACT: `APPROVAL_INTENT_PAYLOAD_KEYS = [\"decision\",\"decisionReason\",\"runId\"]`, admitted by `exactKeys` over `Reflect.ownKeys` — a fourth key is REFUSED, never trimmed. `readApprovalIntent` additionally pins decision to the closed {APPROVE,REJECT} set and refuses an empty-string reason.\n\nDERIVED, NEVER ACCEPTED: exactRevisionHash <- run `submissionHash`; planQualityAssessmentRef <- `state.sealedHashes.qualityHash`; criteriaRef <- the `PlanningAuthorityBodiesSealed` payload's `criteriaDigest`, SELECTED BY TYPE not index (bodies and envelope share the aggregate and write order is unpinned); goalRef and graphRevisionRef off the run; approvalRef server-minted as `approval:<runId>`. The caller cannot name a revision at all, which makes a diverged ref UNREPRESENTABLE here rather than merely refused — the seam is stronger than the shipped path on that axis, recorded because it makes APPROVAL_GRAPH_REVISION_DIVERGED unreachable from this kind by construction.\n\nAUTHORITY COMPOSED, NOT REIMPLEMENTED: `operatorReviewAuthority` is the same composition as daemon-command-graph-approve.ts:68-79 and planning-services.ts:190-202, routed through `decideApprovalAuthority` (gate first by construction). `checkHumanAuthority` is NOT called anywhere — it is unpublished from the core barrel by design.\n\nA DEFECT MY OWN ARM CAUGHT, and it is worth the record. First run was 15/16: the not-PLAN_REVIEW arm got BOOTSTRAP_PREREQUISITE_MISSING because I read the sealed facts BEFORE the lifecycle, so a merely-proposed run answered UNSEALED — exactly the mis-naming `approval-run-binding.ts:135-142` warns about (\"answering 'unsealed' for it would name the wrong defect to the operator\"). Fixed by calling `verifyApprovedRunBinding` first and forwarding its code AND layer. The arm's own fixture was also wrong: `driveThrough(store,\"plan.propose\")` stops at the FIRST kind match and leaves no run at all, so the arm would have been satisfied by the missing-run answer and never exercised the lifecycle check. It now drives `bootstrapSequence()` by commandId up to `cmd-finalize` and ASSERTS the subject exists and is short of PLAN_REVIEW before dispatching.\n\nTYPE SAFETY: `ApprovalIntentRefused.layer` is typed as the closed `ServiceRefusedBy` roster, not `string` — that is the compile-time agreement check between whatever answered and what `refuse` accepts. No `any`, no `as never` (an interim `as never` was removed in favour of the real type).\n\nONE TRACKED EDIT so far: `bootstrap-ledger.ts` SERVICE_REFUSED_BY gains the literal `\"DAEMON_APPROVAL_INTENT\"`. The layer const stays module-private and only the closed TYPE is exported — the precedent APPROVAL_RUN_BINDING and the budget pair follow, and it keeps `tests/security/boundary-roster.security.ts` from demanding a hostile trio for a name that only tags a composition refusal.\n\nNOT TOUCHED, as step 3 requires: `daemon-command-graph-approve.ts` and `planning-services.ts` (the latter also carries a foreign one-line hunk). No Control Room file; DEV_PAYLOADS untouched.\n\nLINE CAPS: approval-intent.ts 201, approval-intent-sources.ts 146 — both under the 250 target. Test file 345 (not held to the production cap).",
      "startedAt": "2026-08-29T13:26:02.777Z",
      "completedAt": "2026-08-29T13:30:18.528Z"
    },
    {
      "stepId": "step-4",
      "description": "STEP 4 — WIRE IT, AND CARRY THE ROSTER FAN-OUT IN THE SAME COMMIT.\n\nWire the seam per step 1(c)'s recorded decision — either narrowing `approval.decide`'s PAYLOAD_KEYS or registering the new kind — across `daemon-command-vocabulary.ts`, `daemon-command-families.ts` and `daemon-command-registry.ts`.\n\nEVERY EXACT-ROSTER ASSERTION THE CHANGE REACHES MOVES IN THIS COMMIT. A commit that lands a roster change without its count consumers leaves the daemon suite red for everyone on a shared branch. Enumerate them with `git grep -l` before editing — `daemon-command-vocabulary.test.ts` and `daemon-command-registry.test.ts` are already owned; if the grep finds a third, name it in the step note and add it, because an exact-count assertion changing when the roster changes is the assertion working, not a scope breach (Rail 8(A)).\n\nIF STEP 1(c) CHOSE A NEW KIND, Rail 7B binds and DoD-4 is explicit: every generated artifact and digest mirror is an OWNED PATH and is REGENERATED by its generator, never hand-edited, in the SAME commit, and `pnpm --filter @moe/control-room-client test` passes with a nonzero count. A hand-edited generated file is worse than an unregistered kind because it passes review while diverging from its source. IF STEP 1(c) CHOSE REUSE, a row comment must state that no new kind was introduced and why, per DoD-4's second clause.\n\nTHE BIDIRECTIONAL ROSTER GUARD: assert every ADVERTISED kind is SERVED and every SERVED kind is ADVERTISED, deriving the served set from the registry's own dispatch composition rather than from the roster constant. A test iterating the roster alone sees only one direction — deleting an entry shrinks its own iteration and stays green.\n\nRe-run step 2: ALL arms green, including (2) — caller-supplied `activation` or `record` is now refused on the production path. Quote the count line.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.ts",
        "apps/daemon/src/daemon-command-families.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/daemon-command-registry.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent-contracts.ts",
        "apps/daemon/src/planning/approval-intent-contracts.js",
        "apps/daemon/src/daemon-command-edges.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/daemon-command-vocabulary.test.ts",
        "apps/daemon/src/daemon-command-registry.test.ts",
        "apps/daemon/src/mcp-tool-allowlist.test.ts",
        "apps/daemon/src/daemon-store-dependencies.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.js"
      ],
      "note": "WIRED, ROSTER FAN-OUT CARRIED, HEAD REPAIRED. Commit 595c32c2 (explicit pathspec, 11 paths, zero foreign bytes). Full detail in comment-4cca3e614a4c4352b4914944821f1dba.\n\nREPAIR: this row's own WIP commit c2ab880b landed vocabulary.ts:7 + families.ts:5 importing ./planning/approval-intent-contracts.js but never committed that module — HEAD imported a file absent from git (`git cat-file -e HEAD:...` => \"exists on disk, but not in 'HEAD'\"). Module + .js bridge committed here.\n\nDoD-4 = the NO-NEW-KIND branch, stated explicitly: approval.decide_intent was already published by task-3051c597 (runtime-vocabulary.ts:86, generated-client.ts:178/280, stdio-schemas.test.ts:94). Nothing regenerated, no mirror owned. Mirrors verified green: contracts 483/483, control-room-client 81/81, mcp 218/218.\n\nFAN-OUT = FOUR consumers, two beyond the plan (named per step 4's own instruction + Rail 8(A)): vocabulary.test.ts (39->40, new APPROVAL_INTENT family, operator gate 7->8), registry.test.ts (same from the far side + its own OPERATOR_ONLY), mcp-tool-allowlist.test.ts (40-2+4=42), daemon-store-dependencies.test.ts (exact registryKinds under plain Node).\n\nAn arm caught a real omission: registry's gate-membership sweep failed 403-vs-422 AFTER the counts were fixed. Production was right — both approval wires are operator-gated; the test's OPERATOR_ONLY was stale. Gating one wire and not the derived one would hand back exactly the authority this row removes.\n\nLINE CAP DISCHARGED (qa-b6149c54's comment-9bb9b8a1): c2ab880b took bootstrap-ledger.ts 420->428, over the 400 bar. Split on a responsibility line into bootstrap-ledger-vocabulary.ts (175, what a refusal may say) + bootstrap-ledger.ts (292, how a decision is read/committed) + .js bridge; mechanics re-exports every moved name so no importer moves. Daemon suite byte-identical across the split (24/9 both sides) = the split moved no behaviour.\n\nGATES: typecheck EXIT=0 with its `$ tsc --project tsconfig.json` echo; owned seam suite 16/16 EXIT=0. Full daemon 31 failed/11 files -> 24 failed/9 files; the 7 cleared are exactly my roster arms. The 24 remaining are foreign and attributed, not assumed — the one in an owned file is goal.create's `+ \"binding\": null`, traced to goal-create-with-source.ts:55 (task-0ca390d9 lane). Security lane 13/3 IDENTICAL before and after; its 5 unrostered *_LAYER constants are all foreign.\n\nCARRIED INTO STEP 5: DoD-2 says the seam \"commits through the existing approval edge\". It does not — its happy path ends in refuse(APPROVAL_INTENT_RISK_TIER_UNAVAILABLE). That is the approved step-3 design and the step-1 cycle (riskTier/applicablePolicyRef have no durable producer; task-ba102165 flips them), NOT a defect introduced here. Raised in the comment; resolution recommendation goes in step 5.",
      "startedAt": "2026-08-29T13:30:20.603Z",
      "completedAt": "2026-08-29T16:32:38.947Z"
    },
    {
      "stepId": "step-5",
      "description": "STEP 5 — DRILLS, GATES, ADVERSARIAL REVIEW, DURABLE HANDOFF.\n\nRESTORE PROTOCOL: the vocabulary, families, registry and test files are TRACKED, so `git diff --exit-code -- <path>` is valid — check `git status --porcelain` per path FIRST since they carry your edits during drills. The three new `approval-intent` files are UNTRACKED until commit and `git diff` is BLIND to them; back those to `%TEMP%/6646f888-<workerId>/` and prove restore by SHA256 equality in a call SEPARATE from the restore (64 hex, non-empty, equal). Confirm each mutation LANDED before rerunning; a silent no-op write reads as green.\nD1 HONOUR A CALLER `activation` (DoD-5's named drill) -> the caller-authority refusal arm MUST red. This is the row's central property: human authority is not delegable.\nD2 HONOUR A CALLER `record` -> the same class of arm reds. Run it SEPARATELY from D1 — one degree of freedom each — because a seam might fence one and not the other.\nD3 DERIVE THE GRANT FROM THE PAYLOAD instead of the session -> the derived-witness arm reds.\nD4 DEFAULT THE MISSING FACT instead of refusing -> the step-2 arm (5) reds. Then default it to a DIFFERENT value -> the SAME arm must red. Absence and a defaulted value are different answers, and an arm catching only one is not testing the property.\nD5 SILENT DEGRADATION: change one refusal's `source.aggregate` to a value the descriptor does not list -> the anti-UNKNOWN_ERROR arm MUST red. The mutant compiles, runs and still refuses, which is exactly why that arm exists.\nD6 ROSTER, BOTH DIRECTIONS: remove the kind from the ADVERTISED roster while leaving it served -> the bidirectional arm reds; restore; remove it from the SERVED composition while leaving it advertised -> it reds again. A guard catching one direction is a subset check wearing a set-equality's clothes.\nA GREEN drill is a FINDING to report in the row comment, never a pass to bank.\n\nGATES — each alone, FOREGROUND, UNPIPED. An exit-0 leg printing no count line executed NOTHING and is UNVERIFIED.\nLEG A: `pnpm --filter @moe/daemon typecheck` — exit 0, quote the `$ tsc --project tsconfig.json` echo.\nLEG B: `pnpm exec vitest run --root apps/daemon --config package.json <this row's owned tests>` — exit 0, quote BOTH count lines.\nLEG C: if step 1(c) chose a new kind, `pnpm --filter @moe/control-room-client test` — exit 0 with a nonzero count (DoD-4).\nLEG D: `pnpm --filter @moe/daemon test` for attribution — the failing-path set at HEAD minus merge-base, intersected with this row's owned paths, must be EMPTY. Disclose foreign red VERBATIM with exact failing lines and paths; never edit it, never block on it.\nLINE CAPS: `wc -l` every production file; each <=250 target, under 400 hard.\n\nADVERSARIAL REVIEW, in the step note: can a caller still supply activation, record, or a grant by ANY route (D1-D3 guard it); is the witness derived from the authenticated session on every path; did any refusal invent a local code where an existing layer already emits one; is `checkHumanAuthority` called directly anywhere (it must not be — unpublished by design); did this row edit `daemon-command-graph-approve.ts` (it must not) or any Control Room file (task rail 2 fences them); and can any refusal produce `UNKNOWN_ERROR`.\n\nDURABLE HANDOFF — moe.add_comment, not the completion summary. Keep each under ~4500 chars and SPLIT rather than truncate (both cap at 5000 SILENTLY). Carry step 1's measurements with the freshly-captured HEAD sha; the command-kind decision and the fan-out count that decided it; THE LIST OF FACTS THE SEAM HAD TO REFUSE ON for want of a durable source — that list is exactly what task-ba1021652dcc4469bc4deb04a8e7d7d5 consumes next, and naming it precisely is what breaks the cycle rather than moving it; the six drill outcomes with restore proofs; and every gate leg's exit code and count line. Post a comment on ba102165 naming this row as its now-landed composition site. Commit by EXPLICIT PATHSPEC of this row's owned paths, message BEFORE the `--` (a `-m` after `--` is parsed as a pathspec and nothing lands). Never `git add -A`, never a bare `git commit`.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "DRILLS, GATES, ADVERSARIAL REVIEW, HANDOFF — all done. HEAD 13d5750e. Evidence split across comment-4265d1f7 (drills), comment-b83b8e4e (D5 finding + gates + attribution), comment-b8171f51 (adversarial review + refused-fact list + DoD-2).\n\nDRILLS 6/6, each mutation confirmed landed, each restore proven in a separate call:\nD1 caller `activation` RED — and under the SAME mutant the `record` arm stayed GREEN, proving one degree of freedom and that the two fences are graded separately. D2 caller `record` RED. D3 grant minted from the caller principal RED. D4 missing fact defaulted RED on TWO different values, same arm both times. D6 roster RED in BOTH directions (advertised-loses-it: 9 arms; served-loses-it: 9 arms).\n\nD5 WAS A GREEN DRILL = A FINDING, reported and then fixed. The plan's D5 premise (mutate `source.aggregate`) does not exist on this path — the seam never builds a RuntimeError through the factory. So I drilled what the arm's HEADER claims: retyping a refusal code to an unregistered string left it GREEN. A literal-`UNKNOWN_ERROR` control DID red, so the arm was not vacuous, just far narrower than its header. Fixed in 13d5750e by grading codes against a HAND-TRANSCRIBED emittable roster (importing the module's own constant would be a fixed point). Re-drilled: the previously-green mutant now reds with `\"emittable\": false`, and the control still reds — coverage gained, not traded.\n\nGATES: LEG A typecheck EXIT=0 with `$ tsc --project tsconfig.json`. LEG B owned suites at the clean commit `Test Files 4 passed (4)` / `Tests 178 passed (178)` EXIT=0. LEG C control-room-client 81/81 (not binding — no new kind), contracts 483/483, mcp 218/218. Line caps all under 400 (largest 307).\n\nLEG D BY CLEAN DETACHED WORKTREE, because the shared checkout carries live peer WIP: baseline 8f49c987 (immediately pre-my-work) = 21 failed | 6264 passed / 6 files; delivered 13d5750e = 21 failed | 6289 passed / 6 files. IDENTICAL failing file set and count, 25 more passing. Owned-path intersection with new reds is EMPTY — proven, not argued. The 3 extra reds visible in the shared tree are peers' uncommitted bytes and absent from both clean runs.\n\nFOREIGN RED disclosed verbatim and deliberately NOT repaired although it sits in a file I own: registry.test.ts:736 goal.create, one added key `+ \"binding\": null`, from goal-create-with-source.ts:55 landed by e3d20791 (task-fc42ae5e), proven an ancestor of 8f49c987 and reproduced red in a pristine worktree there. Repairing it would silently bless an unreviewed goal-lane contract change. Handed back to that lane in chat msg-096513a3.\n\nADVERSARIAL REVIEW: no route found for a caller to supply activation/record/grant (exact-arity over Reflect.ownKeys, try/catch for revoked proxies, explicit-undefined checked); witness fenced twice and never decoded from bytes; refusals forwarded unrestamped rather than restated; `checkHumanAuthority` never called (2 grep hits, both prose); graph-approve.ts and Control Room untouched across all three commits. Flagged forward: the seam is read-only on every path today, so idempotency on the approval edge is untested and becomes live the moment ba102165 adds the commit.\n\nHANDOFF: comment-c99bc430 on task-ba102165 discharges its block premise and hands it the four missing-fact codes as its work list with the measured constraints on riskTier and applicablePolicyRef. Serena: task-6646f888-handoff, gotcha-wip-commit-can-land-an-import-without-its-module, gotcha-not-unknown-error-arm-is-narrower-than-its-header.\n\nDoD-2 REMAINS UNMET AND IS FLAGGED, NOT PAPERED OVER: the seam does not commit; its happy path refuses on riskTier. That is the approved step-3 design and the documented cycle resolution, but the clause's literal text is not satisfied. I did not amend the DoD — that is the architect's call. Recommendation in comment-b8171f51.",
      "startedAt": "2026-08-29T16:32:48.222Z",
      "completedAt": "2026-08-29T16:47:24.952Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": "qa-b6149c54",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor unpark, human-approved 13:10Z: block premise expired — cited foreign task-0ca390d9 WIP landed as aebdb367; paths clean at HEAD 8c326067 (porcelain empty + ls-files positive control). Architect: re-probe at fresh HEAD before planning.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "MEDIUM",
  "order": 15.4,
  "comments": [
    {
      "id": "comment-2f68f46ca9a34913863515816d57e92e",
      "author": "worker-4aba1617",
      "content": "STEP 1 VERIFY-OR-BLOCK at HEAD 4954c61991ae0a6a64315ed062237084df29fe1a.\n\nLeaf A is absent on tracked objects: `git grep -n '\"approval.decide_intent\"' HEAD -- packages/contracts/src/runtime/runtime-vocabulary.ts packages/control-room-client/src/generated/generated-client.ts` prints no hits. Per the approved safety gate, task-3051c597eb7646678673a0665a274530 must publish the kind before this daemon entry may name it.\n\nA second independent collision also remains: exact porcelain across registry/vocabulary/families and their roster tests is ` M apps/daemon/src/daemon-command-registry.ts`. Its full hunk imports/threads `FoundationContextSealPort`, unrelated to this approval-intent row; it was present before claim and cannot be swept.\n\nNo owned bytes edited or staged. Remaining resolution-table and template measurements were intentionally not continued after the mandatory leaf gate failed.",
      "timestamp": "2026-08-27T18:46:33.948Z"
    },
    {
      "id": "comment-eb86110eaa1a4ebfb3bee79caa9379e0",
      "author": "governor-4f450452",
      "content": "GOVERNOR RE-MEASUREMENT at HEAD b5a9d452 — your block is HALF discharged. Do not resume yet; do not re-file the discharged half.\n\nThe block named two independent premises. They have moved apart.\n\nCLAUSE 1 — the leaf gate. SPLIT, one side landed:\n  git grep -n \"approval.decide_intent\" HEAD -- packages/contracts/src/runtime/runtime-vocabulary.ts\n    => 1 hit. PRESENT. Landed by 783cea51 \"feat(contracts): publish approval intent command\".\n  git grep -n \"approval.decide_intent\" HEAD -- packages/contracts/src/generated-client.ts\n    => ZERO hits. STILL ABSENT.\nYour gate required BOTH, and it was right to. The generated-client mirror is exactly what task-3051c597 (P2.15d-i-a, \"Publish approval.decide_intent as a generated command kind and rotate the contract digest mirrors\") is landing right now — it is in REVIEW under qa-914677ff as of 19:16Z. So this half of your block is live, correctly attributed, and about to clear on its own. Landing the daemon entry before that mirror rotates would still name a kind the contract refuses, exactly as you wrote.\n\nCLAUSE 2 — the foreign hunk. FULLY DISCHARGED, and by me, so you are owed the provenance:\n  git grep -c \"FoundationContextSealPort\" HEAD -- apps/daemon/src/daemon-command-registry.ts => 2\n  git status --short apps/daemon/src/daemon-command-registry.ts                              => clean\nThose were task-203a5ca7's four orphaned lines sitting uncommitted in the shared worktree across three separate rows' claims. I committed them as fc2babe6 with the human's approval after measuring that the imported type is tracked at HEAD, the field is optional, and the spread is conditional. The registry file is no longer dirty and no longer carries anyone's foreign WIP. Nothing in this clause blocks you any more.\n\nWHAT TO DO WHEN YOU RESUME: re-run the leaf gate on TRACKED OBJECTS at the HEAD you actually resume on — both legs, vocabulary AND generated-client — and quote that sha in the step note. Do not carry this comment's sha forward; task-3051c597 is in REVIEW, not DONE, and if QA rejects it the mirror can move again. If the generated-client leg is green by then, the whole block is discharged and you may self-clear; if it is not, re-block naming ONLY clause 1, because re-filing the registry clause would charge a defect that no longer exists.\n\nFleet note that applies to your gate directly: `pnpm test` does not cover apps/**, so a repo-wide green line is not evidence for the daemon side of this row. Run `pnpm --filter @moe/daemon test` explicitly and quote its count line. Rail proposal prop-85f547259b7e4d68a50d47188de751d1 is pending with the human.",
      "timestamp": "2026-08-27T19:22:50.887Z"
    },
    {
      "id": "comment-154a3840cb0f48fabfdc66918975976f",
      "author": "architect-94dd67c9",
      "content": "ARCHITECT RE-MEASUREMENT / STALE-BLOCK CLEARANCE at delivered-tree HEAD `06ad0c23ecfebdcf01a4b9babd4dcf78e18e001e` (2026-08-27).\n\nBoth independent premises in blockedReason are now discharged on tracked objects:\n\n1. Leaf A is complete and the required producer edge is present in BOTH places:\n- `packages/contracts/src/runtime/runtime-vocabulary.ts:86` contains `\"approval.decide_intent\"`.\n- `packages/control-room-client/src/generated/generated-client.ts:178,279` exposes/builds `approval.decide_intent`.\nMoe reports task-3051c597eb7646678673a0665a274530 DONE, 5/5.\n\n2. The collision fence is clean. `git status --porcelain -- apps/daemon/src/daemon-command-registry.ts apps/daemon/src/daemon-command-vocabulary.ts apps/daemon/src/daemon-command-families.ts apps/daemon/src/daemon-command-registry.test.ts apps/daemon/src/daemon-command-vocabulary.test.ts` printed nothing. A tracked-object grep finds no daemon/core `approval.decide_intent` implementation yet, so this is not a duplicate.\n\nThis satisfies the task's existing step-1 resume condition and governor comment-eb86110e's instruction to re-run both leaf legs at the actual resume SHA. Return the approved plan to WORKING; retain step 1 IN_PROGRESS so the next worker completes the remaining read-only resolution table before authoring bytes.",
      "timestamp": "2026-08-27T20:27:01.461Z"
    },
    {
      "id": "comment-b732b83ef12644b7a89d707a0373c11f",
      "author": "worker-4aba1617",
      "content": "STEP 1 RE-MEASUREMENT / SCOPE BLOCK at delivered-tree HEAD `380444767e7c4965ef0aac2bfcef881c7d1f8f9d` (2026-08-28). No task bytes were edited.\n\nRequired premises are green on tracked objects:\n- Leaf A: `git grep -n '\"approval.decide_intent\"' HEAD -- packages/contracts/src/runtime/runtime-vocabulary.ts packages/control-room-client/src/generated/generated-client.ts` hits runtime-vocabulary.ts:86 and generated-client.ts:178/:280.\n- Existing caller-shaped seam remains: daemon-command-vocabulary.ts:206 is `[\"activation\", \"command\", \"graphRevisionRef\", \"record\", \"runId\"]`; daemon-command-graph-approve.ts:94/:97 reads `activation`/`record` from the request.\n- `git grep -n approval.decide_intent HEAD -- apps/daemon/src` has zero hits: no duplicate daemon seam.\n- The five declared existing composition/test paths are porcelain-clean. All ten declared task paths are clean/absent.\n\nRail-required shape/fan-out search found an UNDECLARED exact asserting consumer: `apps/daemon/src/daemon-store-dependencies.test.ts`. Its child probe derives `registryKinds: [...deps.registry.keys()].sort()` at :547, then asserts the exact 38-kind literal at :600-618. The literal contains `approval.decide` but not `approval.decide_intent`. Registering the planned 39th kind necessarily makes this committed production-loadability test red until that exact roster is updated.\n\nThe file is clean (`git status --porcelain -- apps/daemon/src/daemon-store-dependencies.test.ts` empty) and is not among the approved ten owned paths. Global rail says an unnamed shape consumer discovered mid-step must stop and report rather than take the extra path; adding it would also take this row beyond its declared ten-path plan. Architect must amend/split ownership and add the consumer to the affected roster. This is a real delivered-byte blocker, not unrelated baseline red.",
      "timestamp": "2026-08-28T09:42:12.894Z"
    },
    {
      "id": "comment-2137a363053c4ba894d00d3caa8c4b81",
      "author": "worker-4aba1617",
      "content": "STEP 1 RESOLUTION TABLE at HEAD de458f8fb163665599109f9b781c71aa14ace4e3 (read-only; zero owned-path porcelain).\n\nPrerequisites/collision: both tracked Leaf-A surfaces publish approval.decide_intent (runtime-vocabulary.ts:86; generated-client.ts:178/280); all ten owned paths are clean/absent; no daemon duplicate exists. Existing approval.decide still takes caller activation/command/graphRevisionRef/record/runId.\n\nFULL RECORD SOURCES measured:\n- actor / actorKind / truthClass / stepUpAuthRef: authenticated SessionAuthority facts; require HUMAN; derive the step-up ref from the verified presentation proof exactly as recovery-completion-authority.preparePresentation does.\n- approvalRef: deterministic from durable runId. decision/decisionReason stay null before core applies the intent. lifecycle=PENDING, validity=CURRENT.\n- exactRevisionHash: durable run.submissionHash.\n- planQualityAssessmentRef: durable run.state.sealedHashes.qualityHash.\n- graphRevisionRef: durable run.state.graphRevisionRef, rechecked by verifyApprovedRunBinding.\n- approvedNodeScope: decoded sealed plan revision affectedNodeIds / content body node refs; refuse if absent.\n- criteriaRef: sealed PlanningAuthorityBodiesSealed.criteriaDigest (hex64). Its criteriaRef is a non-hex contract id in the real fixture and cannot satisfy the core record codec.\n- dependencyChanges: empty only when durable runKind=INITIAL; no producer for a revision delta was found, so non-INITIAL must refuse SOURCE_UNAVAILABLE.\n- applicablePolicyRef + policyDecisionRef: a verified matching PolicyEvaluated row exposes policyRef + decisionDigest through readPolicyEvaluationAuthority; absent/foreign/unreadable must refuse.\n- riskTier: MUST come from verifiedOutcome.riskAssessment.effectiveTier of that same server-authored PolicyEvaluated row. No default/caller/recovery value is admissible.\n- budgetRef: no first-class hex64 approval-budget reference is persisted on the PLAN_REVIEW run/goal. GoalCreated carries budgetAccountRef (the real fixture is \"budget-account-1\", non-hex). The server budget digest is computed later inside activateApprovedGraph after core accepts the approval record. Re-hashing the account name here would invent a second authority; current world must refuse SOURCE_UNAVAILABLE unless a durable hex64 source is named.\n\nACTIVATION SOURCES: expectedGoalVersion and the no-active-revision condition come from durable goal state; graphHash/qualityHash from sealedHashes; policyHash is derived by the existing approvalPolicyHash material over the server policy + human grant (not copied); the edge recomputes all bindings. Caller supplies only runId/decision/decisionReason/authentication.\n\nBLOCKING FIELD: riskTier is unavailable in the exact initial PLAN_REVIEW world step 2 mandates. Production riskTier grep excluding tests/fixtures has only recovery's hard-coded R3, an unrelated authority forbidden by this task's rails. POLICY_RISK_EVENT_TYPE/buildPolicyRiskRecord have no non-test writer call; task-bdbe0519, the writer, is BLOCKED. More fundamentally, policy-risk-reader.ts:120 first requires readCurrentActiveGraph and binds the record to its hash/epoch, while the initial goal remains DRAFT with activeGraphRevisionRef:null until THIS approval activates it. So initial approval -> active graph -> tier and tier -> approval form a live authority cycle.",
      "timestamp": "2026-08-28T09:44:04.015Z"
    },
    {
      "id": "comment-7cd04f4bdc9045329a65d1ffbce0e837",
      "author": "worker-4aba1617",
      "content": "STEP 1 DURABLE-SOURCE TABLE — SECOND INDEPENDENT BLOCK at tracked HEAD `380444767e7c4965ef0aac2bfcef881c7d1f8f9d`.\n\nUsable sources: `exactRevisionHash` = durable run `submissionHash`; quality/graph hashes = run `state.sealedHashes`; `graphRevisionRef` = run state; node scope = decoded `readGraphBody`; goal version/state = durable goal fold; `stepUpAuthRef` = authenticated presentation replay digest; HUMAN `policyDecisionRef` may honestly be null. Sealed acceptance `criteriaDigest` is 64-hex and can source approval `criteriaRef` (the contractId is only bounded text). `resolveApprovalBudgetRoot` at budget-genesis-leg.ts:185 can produce the canonical root digest, although the join to ApprovalDecisionRecord does not exist yet.\n\nHard absent authorities:\n- `riskTier`: tracked policy-risk-record.ts:7-11 explicitly says the read side has NO production writer. `readPolicyRisk` requires `readCurrentActiveGraph` before selecting subject (:120-127), so it cannot source the initial pre-activation approval this command must enable. Any literal/default or caller hint would fabricate risk authority.\n- `applicablePolicyRef`: the sealed graph only carries scheduler `policyRevision` as a generic GraphKey; the shipped journey value is `pol-000000000001` (journey-authority-bodies.ts:179), while approval-validation.ts requires `applicablePolicyRef` to be 64-hex. `PolicyEvaluated` has no runId/graphRevisionRef/submissionHash selector, so choosing its latest digest would not bind it to this run. approval-policy-digest.ts explicitly answers the distinct activation-policy question.\n- generic `dependencyChanges`: no production graph-diff reader exists; empty is derivable only for a separately proven INITIAL/dependency-free plan.\n\nTherefore the plan's ACCEPTED arm and DoD-2 cannot be made production-honest at current tracked HEAD: every real initial intent would have to fail `APPROVAL_INTENT_SOURCE_UNAVAILABLE`, or the task would invent authority. This needs prerequisite production source(s) or an architect-approved contract/scope change. It is independent of the undeclared `daemon-store-dependencies.test.ts` roster blocker in comment-b732b83ef12644b7a89d707a0373c11f.",
      "timestamp": "2026-08-28T09:44:23.187Z"
    },
    {
      "id": "comment-5eaa0e4219f64461a8563e5c303d2a5e",
      "author": "governor-fd35a081",
      "content": "RAIL PROPOSAL prop-95ae4be99bf8473a9414ebb7c9cf511c — CORRECTION FROM QA, ADOPTED. Read this WITH the proposal; the proposal's own reasoning is half wrong and this is the corrected rule. Anchored here because this row is the freshest instance, not because the rail is about it.\n\nqa-5d4d9bb3 measured my central claim rather than accepting it, and it is true for one instance and false for the other.\n\nMY CLAIM: \"both times the search failed today, the missed consumer was a STRUCTURAL LITERAL in a test expectation ... no symbol grep will ever see it.\"\n\nTRUE for task-db1b7162. `git grep -c \"x-moe-operator-channel\" e42236eb^ -- apps/control-room` returns ZERO. The missed consumer (scaffold.test.tsx) built its fixture longhand — `new Response(JSON.stringify({...}), { headers: {...}, status: 200 })`. There was no symbol to find because the row was INTRODUCING the header. Only a shape/key search finds that.\n\nFALSE for task-55c57abc, which is the one that actually reached a QA REJECT. The missed arm asserted a plain reason-code STRING:\n  git grep -n \"STORE_MIGRATION_REQUIRED\" 17d1b0b9 -- packages/store\n  -> 5 hits, including store-schema-v4-migration.test.ts:517 `)).toBe(\"STORE_MIGRATION_REQUIRED\")`\nThree of the five are trivially excludable (the union member in store-contracts.ts and the row's own two owned files). A ~30-second symbol-ish search would have caught it at plan time and saved a claim -> reject -> amendment -> resubmit cycle.\n\nSO THE RECEIPT NEEDS TWO SEARCHES, NOT ONE, AND THEY FAIL DIFFERENTLY:\n  1. Row changes a REFUSAL / RESULT CONTRACT -> grep the stable reason-code STRING over tracked objects. Cheap, exact, high yield. Missing this is the avoidable half.\n  2. Row INTRODUCES OR RESHAPES a payload, header or returned object -> grep the KEYS and the FORMAT, because no symbol exists yet.\nA receipt doing only (2) still misses task-55c57abc. A receipt doing only (1) still misses task-db1b7162. My filed proposedValue leans on (2) and is therefore incomplete as written.\n\nALSO ADOPTED, qa-5d4d9bb3's addition: THE RECEIPT MUST NAME THE SHA IT WAS RUN AT. An unanchored grep receipt rots in this shared worktree within the hour — qa-5d4d9bb3 and worker-7466867e watched a typecheck diagnostic change its code AND column between two runs two minutes apart on an untracked file. Same mechanism as the false-green problem the rail exists to prevent.\n\nTO THE HUMAN RULING ON prop-95ae4be9: if you take it, take it as the TWO-SEARCH, SHA-ANCHORED version above, not the one-search version in the filed proposedValue. Note the disk constraint — global custom rails truncate at 1000 chars, so the landed text must stay under that bound; the two-search framing fits if stated as \"grep the reason-code STRING for contract changes AND the KEYS/FORMAT for shape changes, receipt anchored to a sha\". I did not file a second proposal; one pending proposal per defect, corrected in place.\n\nSupporting evidence qa-5d4d9bb3 offered for the decision: task-55c57abc comment-b85b1bb7f81c46da9d30b7fba11dd0be (the reject) and comment-c1ed5f26ea6944e1a787974b0f4ebd06 (the clean resubmission after amend-1).\n\nThird time today a peer has corrected a claim I made in public, and all three were the same error on my side: reasoning from the instance in front of me instead of measuring the class. Reverse drill, both-sides porcelain, and now two-search receipts each came from someone else measuring what I asserted.",
      "timestamp": "2026-08-28T10:02:40.663Z"
    },
    {
      "id": "comment-18033faf4ddc461390e6b70be4e15083",
      "author": "worker-ee96dea4",
      "content": "STEP 1 VERIFY-OR-BLOCK at tracked HEAD `62b4d7f6365df30191b253e65433eb95c54e19c4`; read-only, no task byte authored.\n\nGap/publication measurements:\n- `git grep -n '\"approval.decide\"' HEAD -- apps/daemon/src/daemon-command-vocabulary.ts` exit 0 -> :57 capability, :206 exact payload `[\"activation\",\"command\",\"graphRevisionRef\",\"record\",\"runId\"]`, :268 served roster.\n- `git grep -n -e 'activation' -e 'record' HEAD -- apps/daemon/src/daemon-command-graph-approve.ts` exit 0 -> caller payload reads at :94 and :97, validated at :99, returned at :103.\n- `git grep -n 'decide_intent' HEAD -- apps/daemon/src` exit 0 -> only mcp-tool-allowlist.test.ts:102 unrelated comment; daemon still does not serve it.\n- Publication is already paid by task-3051c597: runtime-vocabulary.ts:86 contains `approval.decide_intent`; generated-client.ts:178 and :280 contain its builder; stdio-schemas.test.ts:94 pins `approval_decide_intent`. No generated artifact or digest mirror belongs to this row. The rejected second-shape alternative would give one kind two exact rosters and dissolve the one-roster shape fence; the published new kind remains the measured design.\n\nBLOCK: `git status --porcelain=v1 -- <all nine planned paths>` exit 0 returned foreign WIP in three planned paths:\n```\n M apps/daemon/src/daemon-command-registry.test.ts\n M apps/daemon/src/daemon-command-vocabulary.test.ts\n M apps/daemon/src/daemon-command-vocabulary.ts\n```\nThe exact-path diff is not this task's work: it adds `goal.create_with_source`, its 39-kind rosters, and the source-ingress suite. Moe search binds those bytes to BLOCKED critical task-0ca390d9e99d43e7bb3444d129adbf3e, whose handoff explicitly requires preserving ten daemon WIP paths. Step 1 requires these paths clean or carrying only this worker's hunks; shared-tree rail forbids overwriting/interleaving foreign WIP. No edit or staging was performed.\n\nNeeded: task-0ca390d9 must land/move its bytes, or a governor must provide explicit adoption/serialization authority.",
      "timestamp": "2026-08-28T21:42:00.473Z"
    },
    {
      "id": "comment-68c544eda0d24227aa1c2e8e441b7ac0",
      "author": "worker-9fe78697",
      "content": "STEP 1 — VERIFY-OR-BLOCK GATE. worker-9fe78697. HEAD `6a642a801662ae7a9fc18e46fa43d8be5eb778e7` (`git rev-parse HEAD`, EXIT=0). Every grep below unpiped with its own exit code.\n\n## (a) DoD-1 PROBES — GATE PASSES, this row is NOT a duplicate\n1. `grep -n ... apps/daemon/src/daemon-command-graph-approve.ts` EXIT=0 → `intentOf` at **:91-103** still reads `activation` (:94), `command` (:95), `graphRevisionRef` (:96), `record` (:97), `runId` (:98) FROM THE CALLER PAYLOAD via `payloadObject`/`payloadRef`. Caller-shaped authority INTACT.\n2. `grep -n \"approval.decide\" apps/daemon/src/daemon-command-vocabulary.ts` EXIT=0 → **:207** `\"approval.decide\": [\"activation\", \"command\", \"graphRevisionRef\", \"record\", \"runId\"]`. Exact roster UNCHANGED, still requires `activation` and `record`.\n3. `grep -rn \"decide_intent\" apps/daemon/src/` → ONE hit, `mcp-tool-allowlist.test.ts:102`, a COMMENT calling it an \"unrelated kind\". **No daemon file admits an intent-only approval shape.** The seam does not exist.\n4. Second production precedent confirmed: `planning-services.ts:230-234` (`decideApproval`) reads the same five members off `request.payload`.\n\n## (c) COMMAND-KIND DECISION — **NEW KIND `approval.decide_intent`**, and the fan-out counts that decided it\n`git grep -l` at HEAD, `.moe/` excluded:\n- **REUSE `approval.decide`** → `git grep -l '\"approval\\.decide\"' HEAD` = **90 files**. Narrowing a live exact allow-list that 90 files name, including `tests/security/integrity-hostile-cases.ts`, `packages/core/src/index-surface.test.ts` and 30+ Control Room files task rail 2 fences me out of. Rejected: unpriceable inside this row's budget, and it would break every existing `approval.decide` caller.\n- **NEW KIND `approval.decide_intent`** → **4 files**, and ALL FOUR ALREADY CARRY IT:\n  - `packages/contracts/src/runtime/runtime-vocabulary.ts:86`\n  - `packages/contracts/src/runtime/runtime-contract.test.ts:36`\n  - `packages/control-room-client/src/generated/generated-client.ts:178` and `:280`\n  - `packages/mcp/src/stdio/stdio-schemas.test.ts:88-94`\n\n**The Rail 7B generated fan-out was ALREADY PAID by task-3051c597 (DONE).** The kind is published in the frozen tuple, the regenerated client and the stdio injectivity arm; only the DAEMON does not serve it. That gap is exactly this row. **Zero generated artifacts to regenerate here; nothing hand-edited.** Adopt, never duplicate.\n\n## (c-bis) FAMILY DECISION — own EDGE, not the bootstrap family\nMeasured a scope trap and routed around it. Registering the kind in `BOOTSTRAP_COMMAND_KINDS` (`bootstrap-contracts.ts:30-42`) reaches a durable-SEQUENCE assertion, not just a count: `bootstrap-durability.test.ts:138` `expect(sequence.length).toBe(BOOTSTRAP_COMMAND_KINDS.length)`, plus `bootstrap-services.test.ts:80-81` (set-equality + `toHaveLength(11)`) and `bootstrap-ledger.ts:420` `BOOTSTRAP_KIND_COUNT`.\n\nThe `resource.reconcile` precedent fits exactly and costs none of that: registry comment at `daemon-command-registry.ts:207-208` — *\"The kinds whose request shape is exact and disjoint from `requestOf`'s envelope record are assembled by their own edge rather than trimmed here.\"* `CommandEdgeContext` (`daemon-command-edges.ts:26-35`) already carries `decidedAt`, `envelope`, `operatorPrincipalId`, `principal`, `projectId`, `store` — every input this seam needs, including the operator comparison. Precedent module: `work/resource-reconcile-command.ts` (own kind const, own exact PAYLOAD_KEYS, own layer const deliberately NOT spelled `*_LAYER` to avoid the security-boundary roster's hostile-trio demand).\n\n## (d) HOW `humanReview` REACHES THE REGISTRY — measured, and it decides a refusal\n`grep -rn \"humanReview\" apps/daemon/src/ --include=*.ts` (test/fixture filtered) EXIT=0. Minted in ONE place: `daemon-command-registry.ts:199-202` and `:248-253`, `principal.principalId === operatorPrincipalId ? Object.freeze({ principalId }) : undefined`. `HumanReviewWitness` is `{ principalId }` and NOTHING ELSE (`bootstrap-ledger.ts:170-172`). The registry's own comment at `:190-194`: *\"this mint carries NO transport fact.\"*\n\nCONSEQUENCE, load-bearing: `stepUpAuthRef` cannot be derived from this witness. `recovery-completion-authority.ts:102` mints one as `sessionReplayDigest({clientKeyId, generation, nonce, sessionId})` — material only a PRESENTED session authentication carries, which the operator-principal witness does not. So the intent seam needs NO session presentation on the payload (`{runId, decision, decisionReason}` stays exact) and instead REFUSES on `stepUpAuthRef`. `validateApprovalRecord` requires `validRef(record[\"stepUpAuthRef\"])` for every HUMAN record (`approval-validation.ts:65`), so this is not optional.\n\n## (b) PORCELAIN — one foreign hunk, routed around not reverted\n`git status --porcelain` over the eleven paths I considered touching: exactly one dirty path, `M apps/daemon/src/planning/planning-services.ts`, carrying a **one-line foreign hunk** `+ humanReview: context.humanReview,` at `:309` (`git diff --stat` = 1 insertion). Not mine, not reverted, not committed. **I will not touch `planning-services.ts`**: my handler registers from its own module through the edge dispatch, so `PLANNING_HANDLERS` never needs editing. All other target paths clean; `git ls-files` positive control returned `daemon-command-vocabulary.ts`, `daemon-command-families.ts`, `bootstrap-contracts.ts`.\n\n## (e) OWNERSHIP / RAILS\n`daemon-command-graph-approve.ts` NOT touched (step 3 forbids). No Control Room file touched and `DEV_PAYLOADS` not enabled or reshaped (task rail 2; task-40017f79 owns the UI half).\n\n## THE FACT LEDGER — what the seam derives vs what it must REFUSE on\n`validateApprovalRecord` (`approval-validation.ts:70-89`) requires 18 exact keys; five must be hex64 (`HASH_KEYS` :33) and a HUMAN record additionally needs `truthClass === \"HUMAN_APPROVED\"` and `validRef(stepUpAuthRef)`.\n\nDERIVED from the durable PLAN_REVIEW run + the authenticated session: `actor` (principalId), `actorKind`/`truthClass`/`lifecycle`/`validity` (constants), `decision`/`decisionReason` (null on the PENDING record; core sets them), `dependencyChanges` (empty triple), `policyDecisionRef` (null, legal for HUMAN), `approvalRef` (server-minted off the envelope commandId), `exactRevisionHash` (run `submissionHash`), `planQualityAssessmentRef` (run `state.sealedHashes.qualityHash`), `criteriaRef` (the sealed-bodies event's `criteriaDigest`, same aggregate `verifyApprovedRunBinding` already reads), `approvedNodeScope`.\n\n**REFUSED — no durable producer at HEAD.** These are the facts task-ba1021652dcc4469bc4deb04a8e7d7d5 consumes next:\n1. `riskTier` — `git grep \"effectiveTier|riskTier\" HEAD -- apps/daemon/src` (test/fixture filtered) finds NO pre-approval durable producer; only `recovery-completion-authority.ts:70` hard-codes `\"R3\"` for its own domain. Defaulting it would silently decide an authority question (`approval-invalidation.ts:73` special-cases R3).\n2. `stepUpAuthRef` — see (d).\n3. `applicablePolicyRef` — hex64; the only durable reader, `policyRevisionOf`, is module-PRIVATE at `recovery-completion-evidence.ts:227`. ba102165 already owns this fact.\n4. `budgetRef` — hex64; the root digest is minted only at ACTIVATION by `resolveApprovalBudgetRoot` (`budget-genesis-leg.ts:185`), downstream of the record.\n\nEach gets its OWN code so the refusal names exactly one fact. `refuse` (`bootstrap-ledger.ts:240-255`) takes `code: string` and does NOT route through `createRuntimeError`, so this seam's own codes cannot degrade to `UNKNOWN_ERROR`; the anti-`UNKNOWN_ERROR` arm still guards forwarded core/authority refusals.\n\n**CYCLE BROKEN, NOT MOVED:** this row lands the composition site; the four facts above are the precise list ba102165 flips from refusing to succeeding.",
      "timestamp": "2026-08-29T13:22:38.480Z"
    },
    {
      "id": "comment-9bb9b8a1bba54340b7ce54a520391d95",
      "author": "qa-b6149c54",
      "content": "LINE-CAP FLAG from QA on a neighbouring row (task-e87cfddf, now DONE) — filed here because the growth is attributable to THIS row, not that one.\n\napps/daemon/src/bootstrap/bootstrap-ledger.ts is 428 lines at HEAD d8552611, over the epic rail 6 hard bar of 400 (target 250).\n\nATTRIBUTION, measured per-sha with `git show <sha>:<path> | wc -l`:\n- 8c326067 (task-e87cfddf's own commit): 421 -> 420. That row SHRANK the file.\n- c2ab880b (this row's WIP commit, \"P2.15d-i approval intent seam\"): 428.\n- HEAD and 550cba73: 428.\n\nSo the file crossed from 420 to 428 inside c2ab880b. I did not reject task-e87cfddf for it, and I am not asking you to stop work — just recording it so it is not silently inherited by whoever reviews this row next. When this row reaches REVIEW the 400 bar will be graded against bootstrap-ledger.ts, and a split (with its .js bridge, per the wiring rails) will be the expected remedy unless the file is back under 400 by then.\n\nNo action needed from you today if the split is already in your plan. — qa-b6149c54",
      "timestamp": "2026-08-29T15:51:40.754Z"
    },
    {
      "id": "comment-4cca3e614a4c4352b4914944821f1dba",
      "author": "worker-9fe78697",
      "content": "STEP 4 — WIRING + ROSTER FAN-OUT. worker-9fe78697. Delivered as 595c32c2. RESUME session: the prior session died mid-step-4, so every claim below was re-measured from disk/git rather than inherited.\n\n## THE BREAK I FOUND AND REPAIRED (report this to QA as a real defect, not a chore)\nThis row's own WIP commit c2ab880b landed `daemon-command-vocabulary.ts:7` and `daemon-command-families.ts:5` importing `./planning/approval-intent-contracts.js`, but NEVER COMMITTED that module. Measured:\n  `git cat-file -e HEAD:apps/daemon/src/planning/approval-intent-contracts.ts`\n  -> \"fatal: path ... exists on disk, but not in 'HEAD'\"\n  `git show HEAD:apps/daemon/src/daemon-command-vocabulary.ts | grep -n approval-intent-contracts` -> :7, EXIT=0\nSo HEAD imported a file that did not exist in git: anyone checking out this shared branch got a daemon that cannot load, while MY worktree stayed green because the untracked file was sitting in it. Both `.ts` and its `.js` bridge are committed in 595c32c2. This is exactly the failure mode the bridge rail warns about, arriving from the opposite direction — the bridge was fine, the module was missing.\n\n## DoD-4 — NO NEW RUNTIME_COMMAND_KIND IS INTRODUCED, stated explicitly as the DoD requires\n`approval.decide_intent` was ALREADY published by task-3051c597 and is present at HEAD in:\n- `packages/contracts/src/runtime/runtime-vocabulary.ts:86` (frozen RUNTIME_COMMAND_KINDS)\n- `packages/control-room-client/src/generated/generated-client.ts:178,280`\n- `packages/mcp/src/stdio/stdio-schemas.test.ts:88-94`\nSo NO generated artifact and NO digest mirror belongs to this row, and nothing was regenerated or hand-edited. DoD-4's second branch is the live one: the roster arms are updated in both directions instead. Confirming the mirrors are untouched and green: `@moe/contracts` 483/483, `@moe/control-room-client` 81/81, `@moe/mcp` 218/218, all EXIT=0.\n\n## THE FAN-OUT SET — FOUR CONSUMERS, TWO OF THEM NOT IN THE PLAN\nPlan named two. The suite found two more. Per step 4's own instruction (\"if the grep finds a third, name it in the step note and add it\") and Rail 8(A), all four moved in the SAME commit:\n1. `daemon-command-vocabulary.test.ts` — 39->40 kinds; new APPROVAL_INTENT family map + FAMILY_NAMES entry + size 1; declared-family count 29->30; frozen-table arm; OPERATOR_ONLY 7->8. Now 127 passed (was 124 passed | 2 failed) — the count GREW by 3 because three `it.each(ROWS)` arms sweep the roster, which is the swept-case pin working.\n2. `daemon-command-registry.test.ts` — the same counts from the far side (ROWS, registry.size, REGISTRATION_ORDER, and both `createDaemonCommandPorts` arms), plus its OWN separate OPERATOR_ONLY list.\n3. `mcp-tool-allowlist.test.ts` — NOT IN THE PLAN. The independent literal witness behind the derived identity: 39-2+4=41 becomes 40-2+4=42. The derived arm alone stayed green (it moves with the vocabulary), which is precisely why that file pins literals as a second witness.\n4. `daemon-store-dependencies.test.ts` — NOT IN THE PLAN, though worker-4aba1617 predicted it in comment-b732b83e. Its child probe derives `registryKinds` under plain Node at :547 and asserts the exact set at :600.\n\n## AN ARM CAUGHT A REAL OMISSION — worth QA's attention because it is the row's own property\n`registry.test.ts > operator-principal gate membership > '$kind' answers the non-operator session from its own layer` failed with `expected 403 to be 422` AFTER I fixed the counts. Production was RIGHT: `approval.decide_intent` is in OPERATOR_PRINCIPAL_KINDS (vocabulary.ts:296), so the operator gate answers OPERATOR_PRINCIPAL_REQUIRED/DAEMON_AUTHORIZATION before the seam is reached. The test's own OPERATOR_ONLY list was the stale half. Both approval wires must be gated: gating `approval.decide` but not the derived wire would leave the seam reachable by a non-operator principal and hand back exactly the authority this row removes. Fixed in the test, and the negative half of that sweep is what makes it load-bearing.\n\n## LINE CAP — qa-b6149c54's FLAG (comment-9bb9b8a1) IS DISCHARGED\nConfirmed the attribution: `git show c2ab880b -- apps/daemon/src/bootstrap/bootstrap-ledger.ts` is +8 lines (the DAEMON_APPROVAL_INTENT member and its comment), 420 -> 428, over the epic rail 6 hard bar of 400. Split on a real responsibility line, not an arbitrary cut:\n- `bootstrap-ledger-vocabulary.ts` (175) — WHAT A REFUSAL MAY SAY: both frozen rosters (SERVICE_REFUSED_BY, PREREQUISITE_REFUSAL_CODES) and the outcome/witness/handler types built on them. This is the half that grows every time a slice contributes a layer.\n- `bootstrap-ledger.ts` (292) — HOW A DECISION IS READ AND COMMITTED against the durable store.\nPlus the `.js` bridge. The mechanics module RE-EXPORTS every moved name, so no importer moves; it also imports the moved types for its own local use, since a TypeScript re-export does not bind them in scope (that was a real typecheck red, now green). Full daemon suite is byte-identical before and after the split — 24 failures / 9 files both sides — so the split moved no behaviour.\n\n## GATES, all foreground, unpiped, own exit codes\n- `pnpm --filter @moe/daemon typecheck` -> EXIT=0, with its `$ tsc --project tsconfig.json` echo.\n- `pnpm exec vitest run --root apps/daemon --config package.json apps/daemon/src/planning/approval-intent.test.ts` -> EXIT=0, 16 passed (16).\n- vocabulary 127/127, registry 252/253, mcp-tool-allowlist 17/17, daemon-store-dependencies 18/18.\n- Full daemon: 31 failed / 11 files BEFORE -> 24 failed / 9 files AFTER. The 7 I cleared are exactly my roster arms.\n\n## THE 24 REMAINING ARE FOREIGN — attributed, not assumed\nThe one still inside an owned file is `registry.test.ts > goal.create ... mints the goal from the command identity`, and its diff is a single ADDED key: `+ \"binding\": null`. Traced to `apps/daemon/src/goals/goal-create-with-source.ts:55 GOAL_CREATED_BINDING_KEY` — the goal.create_with_source lane (task-0ca390d9 / task-115fedb7), which made the goal catalog read emit `binding` for plain `goal.create` goals too without moving this arm. My seam cannot add a key to a goal catalog projection. NOT MINE, and I did not edit a foreign lane's arm to hide it.\nSecurity lane (`pnpm test:security`, the FULL lane — standalone files give false reds): 13 failed / 3 files, IDENTICAL before and after my commit. Its roster-vs-scan arm names 5 unrostered constants, ALL foreign: PRD_LOCAL_LAYER@apps/control-room, IMPORT_GENERATION_READ_LAYER@apps/daemon/src/projections, GA_ACTIVATION_BINDING_LAYER and GA_ACTIVATION_RECORD_LAYER@packages/benchmark, LIVE_QUIESCE_EVIDENCE_LAYER@packages/core/src/cutover. NONE is mine — the seam deliberately names its layer const `LAYER` rather than `*_LAYER` and keeps it module-private, so it is invisible to that scanner by design, and this run is the evidence that the design choice works.\n\nExplicit-pathspec commit, 11 paths, zero foreign bytes swept. Foreign WIP grew DURING this session (foundation-attempt-service.ts, windows-broker-path.ts, cutover/, foundation-attempt-settlement.*) and none of it is in 595c32c2.\n\n## OPEN AGAINST DoD-2 — RAISED, NOT PAPERED OVER\nDoD-2 requires the seam to \"commit through the existing approval edge\". IT DOES NOT COMMIT. `runApprovalIntentCommand` admits the exact shape, reads durable sources, derives the human grant, checks the delay disposition — and then its last line is `return refuse(null, APPROVAL_MISSING_FACT_CODES[0], LAYER)`, i.e. APPROVAL_INTENT_RISK_TIER_UNAVAILABLE. This is the approved step-3 design, not a defect I introduced, and it is the cycle step 1 documents: worker-4aba1617 measured `riskTier` and `applicablePolicyRef` as HARD ABSENT authorities (comment-7cd04f4b) with no durable producer, and task-ba102165 is the row that flips these four codes from refusing to succeeding. Detail and my recommendation follow in the step 5 note; I am flagging it here so it is on the record before review.",
      "timestamp": "2026-08-29T16:32:19.242Z"
    },
    {
      "id": "comment-4265d1f705904d008b31479232fe800b",
      "author": "worker-9fe78697",
      "content": "STEP 5 (1 of 3) — DRILLS. worker-9fe78697, HEAD 13d5750e. All six drills foreground, unpiped, each with its mutation confirmed LANDED before the run and its restore proven in a SEPARATE call. Every owned file was TRACKED and clean before each drill (`git status --porcelain` empty), so `git diff --exit-code -- <path>` is a valid restore proof and was used as one — the step's %TEMP%/SHA256 protocol was written for the approval-intent files while they were untracked; they were committed in 595c32c2, so the stronger tracked-object proof applies instead.\n\nD1 — HONOUR A CALLER `activation` (DoD-5's named drill). Mutant added a SECOND admitted shape `[\"activation\", ...APPROVAL_INTENT_PAYLOAD_KEYS]` beside the exact one, so a caller activation is admitted while the 3-key payload still is. Deliberately NOT \"delete the arity check\": that admits every extra key at once and would grade D1 and D2 with one mutant.\n  landed: grep D1_MUTANT = 2\n  `-t \"refuses a caller-supplied \\`activation\\` beside intent\"` -> EXIT=1, RED, \"Tests 1 failed | 15 skipped (16)\".\n  ISOLATION PROVED: under the SAME mutant, `-t \"refuses a caller-supplied \\`record\\`\"` -> EXIT=0, GREEN. One degree of freedom, and it shows the two fences are graded separately rather than by one shared assertion.\n  restore: `git diff --exit-code` EXIT=0, grep D1_MUTANT = 0.\n\nD2 — HONOUR A CALLER `record`, run separately for exactly the reason the step gives.\n  landed: grep D2_MUTANT = 2. Target arm -> EXIT=1, RED. restore EXIT=0, grep = 0.\n\nD3 — DERIVE THE GRANT FROM THE CALLER instead of the session. Mutant: `const witness = input.humanReview ?? Object.freeze({ principalId: input.principalId })` — i.e. a dispatch with no server-assembled witness mints one from whoever authenticated, which is precisely \"human authority is delegable\".\n  landed: grep D3_MUTANT = 1.\n  `-t \"refuses a witness-less dispatch\"` -> EXIT=1, RED, under the describe \"the human grant comes from the authenticated session, never from the payload\". restore EXIT=0, grep = 0.\n\nD4 — DEFAULT THE MISSING FACT instead of refusing, TWICE with different values, because absence and a defaulted value are different answers.\n  D4a `APPROVAL_MISSING_FACT_CODES[1]` -> arm \"refuses on riskTier, naming the fact in its own code and layer\" EXIT=1, RED. restore EXIT=0, grep = 0.\n  D4b `APPROVAL_MISSING_FACT_CODES[2]` -> the SAME arm EXIT=1, RED. restore EXIT=0, grep = 0.\n  Both defaults red the same arm, so the arm is grading the FACT and not one spelling.\n\nD6 — ROSTER, BOTH DIRECTIONS (reported here; D5 is its own section below because it is a finding).\n  D6a ADVERTISED loses it, SERVED keeps it: deleted `[APPROVAL_DECIDE_INTENT_COMMAND_KIND]: APPROVAL_INTENT_PAYLOAD_KEYS` from PAYLOAD_KEYS (grep = 0 confirmed). registry suite -> EXIT=1, 9 arms RED including \"serves exactly the forty characterized kinds and nothing else\". restore EXIT=0.\n  D6b SERVED loses it, ADVERTISED keeps it: `.filter((k) => k !== \"approval.decide_intent\")` on the registry composition (grep D6B_MUTANT = 1). -> EXIT=1, 9 arms RED. restore EXIT=0, grep = 0.\n  A guard catching only one direction would have stayed green on one of these two. Neither stayed green.",
      "timestamp": "2026-08-29T16:44:19.802Z"
    },
    {
      "id": "comment-b83b8e4ec2ff4909acfa15876202ad79",
      "author": "worker-9fe78697",
      "content": "STEP 5 (2 of 3) — D5 IS A FINDING, NOT A PASS; and the GATES.\n\n## D5 — A GREEN DRILL. Reported as a finding, then fixed, then re-drilled.\nThe plan's D5 says: change a refusal's `source.aggregate` to a value the descriptor does not list, and the anti-UNKNOWN_ERROR arm must red. MEASURED: that premise does not match the delivered seam. `runApprovalIntentCommand` never builds a RuntimeError through `runtime-error-factory.ts`; the test's `refusalOf` reads `code`/`refusedBy` straight off the returned outcome, and `refuse()` (bootstrap-ledger.ts) is a plain constructor with `error = null`. There is no `source.aggregate` on this path to mutate — the fail-closed-to-UNKNOWN_ERROR mapping lives further out, at http-command-ingress/runtime-error-factory, which this arm does not exercise.\n\nSo I drilled what the arm's own header CLAIMS it catches — \"a wrong code compiles, runs, still refuses, and quietly loses its identity\":\n  D5a: retyped `APPROVAL_INTENT_SHAPE_INVALID` to `\"D5_NOT_A_REGISTERED_CODE\"` (landed, grep = 1).\n    -> arm \"produces no refusal whose code is UNKNOWN_ERROR\" EXIT=0, **GREEN**. THE DRILL DID NOT RED.\n  D5b CONTROL, to prove the arm was not simply vacuous: retyped the same constant to `\"UNKNOWN_ERROR\"` (landed).\n    -> EXIT=1, RED.\nDIAGNOSIS: the arm is not vacuous, it is strictly NARROWER than its header claims. It compared each code against ONE literal, and a code can lose its identity without ever becoming that one spelling — exactly the silent degradation the header says it exists to catch.\n\nFIX (13d5750e, owned test file only): the codes are now ALSO graded for membership in the roster this seam may emit. The roster is HAND-TRANSCRIBED, not imported from the module under test — importing `APPROVAL_MISSING_FACT_CODES` would make the expectation a fixed point that moves with the very edit it exists to catch.\nRE-DRILLED after the fix, one degree of freedom changed (the arm, not the mutant):\n  D5a mutant re-run -> EXIT=1, **now RED**, printing `\"emittable\": false`.\n  D5b mutant re-run -> still RED.\nSo the arm strictly gained coverage rather than trading one blind spot for another. Clean tree after: 16 passed (16), EXIT=0.\n\n## GATES — each alone, foreground, unpiped\nLEG A `pnpm --filter @moe/daemon typecheck` -> EXIT=0, echo `$ tsc --project tsconfig.json`.\nLEG B owned suites -> at the CLEAN delivered commit: `Test Files 4 passed (4)`, `Tests 178 passed (178)`, EXIT=0 for approval-intent + vocabulary + mcp-tool-allowlist + daemon-store-dependencies. The fifth owned file, daemon-command-registry.test.ts, is 252 passed | 1 failed — that one failure is foreign and PROVEN so below.\nLEG C DoD-4's control-room-client leg is not binding (no new kind introduced; see comment-4cca3e61) but was run anyway: 81 passed (81), EXIT=0. Also `@moe/contracts` 483/483 and `@moe/mcp` 218/218.\nLINE CAPS, `wc -l` per production file, all under the 400 hard bar: bootstrap-ledger.ts 292, bootstrap-ledger-vocabulary.ts 175, approval-intent.ts 198, approval-intent-sources.ts 146, approval-intent-contracts.ts 24, daemon-command-edges.ts 173, daemon-command-registry.ts 272, daemon-command-vocabulary.ts 307.\n\n## LEG D — ATTRIBUTION BY CLEAN DETACHED WORKTREE, because the shared checkout lies\nThe shared worktree showed 24 reds / 9 files, but it carries live peer WIP (foundation-attempt-service.ts, approval-activation.*, graph-supersede-*, planning-services.ts, cutover/, windows-broker-path.ts). So I measured the COMMIT, not the worktree — two detached worktrees, `pnpm install`, broker `.exe` copied in (without that copy foundation-attempt-windows manufactures 5 phantom reds and the gate reads as a real reject):\n  BASELINE 8f49c987 — the commit immediately BEFORE this row's first byte:\n    `Test Files 6 failed | 298 passed (304)`, `Tests 21 failed | 6264 passed`\n  DELIVERED 13d5750e:\n    `Test Files 6 failed | 299 passed (305)`, `Tests 21 failed | 6289 passed`\nIDENTICAL failing FILE SET and IDENTICAL failure COUNT, with 25 MORE tests passing. This row's owned-path intersection with NEW reds is EMPTY — proven, not argued. The 3 extra reds visible in the shared checkout (foundation-attempt-service, graph-supersede-service, approval-activation) are peers' UNCOMMITTED bytes: they are absent from both clean measurements.\n\nFOREIGN RED DISCLOSED VERBATIM, never edited:\n  `src/daemon-command-registry.test.ts > goal.create admits prose and nothing else > accepts the prose-only payload and mints the goal from the command identity`\n  `AssertionError: expected { …(3) } to deeply equal { goals: [ { …(3) } ], …(2) }`, the whole diff being one ADDED key `+ \"binding\": null` at registry.test.ts:736.\n  PROVENANCE: `binding` comes from `apps/daemon/src/goals/goal-create-with-source.ts:55 GOAL_CREATED_BINDING_KEY`, landed by e3d20791 `feat(task-fc42ae5e): bind the PRD document source to GoalCreated in one decision`, which did NOT touch registry.test.ts. `git merge-base --is-ancestor e3d20791 8f49c987` => TRUE, so it predates my first byte, and the arm reproduces RED in the pristine 8f49c987 worktree with the same `+ \"binding\": null`.\n  I did NOT repair it although it sits in a file I own. Updating that expectation would silently bless a goal-lane contract change I have not reviewed, on behalf of a lane that is still moving (task-0ca390d9 / task-115fedb7). It belongs to them; a note goes to that lane rather than a quiet edit here.",
      "timestamp": "2026-08-29T16:44:52.572Z"
    },
    {
      "id": "comment-b8171f51a9924be195ff771594d3a7c9",
      "author": "worker-9fe78697",
      "content": "STEP 5 (3 of 3) — ADVERSARIAL REVIEW, THE REFUSED-FACT LIST task-ba102165 CONSUMES, AND THE DoD-2 QUESTION.\n\n## ADVERSARIAL REVIEW — every question the step names, answered by measurement\n- CAN A CALLER STILL SUPPLY `activation`, `record`, OR A GRANT BY ANY ROUTE? No route found. The fence is exact-arity over `Reflect.ownKeys` (so a symbol key counts, and a prototype cannot hide one), inside a try/catch (so a revoked Proxy refuses rather than throws). D1/D2/D3 red it. A key present with value `undefined` still passes the arity check and is then refused by the type check rather than silently read as absent — checked deliberately, since that is the usual way an exactness fence leaks.\n- IS THE WITNESS DERIVED FROM THE AUTHENTICATED SESSION ON EVERY PATH? Yes, and it is fenced TWICE: `runApprovalIntentEdge` mints it only when `principal.principalId === operatorPrincipalId`, and the seam independently refuses unless `witness.principalId === input.principalId`. It is never decoded from request bytes. It is required even under a PROCEED_WITHOUT_HUMAN policy, which is what stops an unwitnessed dispatch minting a human's approval.\n- DID ANY REFUSAL INVENT A LOCAL CODE WHERE A LAYER ALREADY EMITS ONE? No. Prerequisite, run-binding and seal refusals are FORWARDED UNRESTAMPED with the originating layer's own code; the human tuple is the existing `APPROVAL_HUMAN_REVIEW_REQUIRED`/`APPROVAL_POLICY`. The only codes this row mints are the shape fence and the four named missing facts — each naming a fact no existing layer has a code for.\n- IS `checkHumanAuthority` CALLED DIRECTLY? No. `grep -rn checkHumanAuthority` over every file this row touches returns TWO hits, both PROSE inside a comment explaining why it must not be called. Authority goes through `decideApprovalAuthority`, which consults the gate first by construction.\n- DID THIS ROW EDIT `daemon-command-graph-approve.ts` OR ANY CONTROL ROOM FILE? No. `git show --name-only` across all three of this row's commits (c2ab880b, 595c32c2, 13d5750e) matches neither `graph-approve` nor `control-room`. Task rail 2 holds; task-40017f79 keeps the UI half.\n- CAN ANY REFUSAL PRODUCE `UNKNOWN_ERROR`? Not on any exercised path, and that is now graded by roster membership rather than one literal (D5).\n- CONCURRENCY / PARTIAL STATE: the seam is READ-ONLY end to end — it reads durable sources and returns an outcome, and its terminal branch is a refusal, so there is no commit, no partial write, and nothing to double-apply on retry. That property is temporary by design and is exactly what changes when ba102165 lands; whoever adds the commit must revisit idempotency on the approval edge at that point, because a composed record WILL be a write.\n\n## THE REFUSED-FACT LIST — this is the payload task-ba102165 consumes, named precisely so the cycle BREAKS rather than moves\nThe seam admits intent, verifies the run against durable state, derives the human grant from the authenticated operator, checks the delay disposition — and then refuses, because four fields of the record have no durable producer. One code per fact, so an operator reads WHICH producer is missing:\n1. `APPROVAL_INTENT_RISK_TIER_UNAVAILABLE` — policy-risk-record.ts has no production writer, and `readPolicyRisk` needs `readCurrentActiveGraph` before it can select a subject, so it cannot source the PRE-activation approval this command exists to enable. Any literal or caller hint would fabricate risk authority.\n2. `APPROVAL_INTENT_STEP_UP_UNAVAILABLE` — the operator witness carries no transport fact, so no session replay digest is derivable.\n3. `APPROVAL_INTENT_POLICY_REF_UNAVAILABLE` — the only durable policy-revision reader is private to recovery-completion-evidence.ts, and `PolicyEvaluated` has no runId/graphRevisionRef/submissionHash selector, so its latest digest could not be BOUND to this run even if read.\n4. `APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE` — the budget root digest is minted at ACTIVATION, downstream of the record it would sign.\nFacts already derivable and wired: exactRevisionHash (run submissionHash), quality/graph hashes (run state.sealedHashes), graphRevisionRef, approved node scope (decoded sealed revision), criteriaRef (sealed criteriaDigest, 64-hex), approvalRef (deterministic from the envelope commandId, never a payload field).\n\n## DoD-2 — THE ONE CLAUSE THIS ROW DOES NOT MEET, STATED PLAINLY\nDoD-2 requires the seam to \"commit through the existing approval edge\". IT DOES NOT COMMIT. Its happy path ends `return refuse(null, APPROVAL_MISSING_FACT_CODES[0], LAYER)`.\nEverything else in DoD-2 is met and drilled: exactly `{runId, decision, decisionReason}` admitted; activation/record/truthClass/hashes/stepUpAuthRef/principal/time all unrepresentable; each extra key refused at the exact-shape fence with stable code AND layer over a nonzero generated roster. DoD-1, DoD-3, DoD-4 and DoD-5 are met (DoD-5's suites, typecheck, drills and line caps are in the two comments above).\nThis is NOT a defect introduced here and NOT an oversight: it is the approved step-3 design and the resolution of the documented cycle. Step 1 records it — this row lands the composition SITE first so ba102165 stops being an orphan producer, and ba102165 then flips those four codes from refusing to succeeding. worker-4aba1617 measured riskTier and applicablePolicyRef as HARD ABSENT authorities in comment-7cd04f4b; committing today would require fabricating risk or policy authority, which task rail 1 forbids outright.\nMY RECOMMENDATION, for the architect/governor rather than a worker: amend DoD-2 by comment to read \"composes and commits, OR refuses under one named code per fact that has no durable producer\", and let ba102165 inherit the commit clause. I have NOT amended it myself — a DoD is not a worker's to rewrite. I am completing the row on the work the approved plan actually specifies and flagging this clause as unmet so review is not misled. If review would rather this row stay open until ba102165 lands, reopening is the cheaper correction, because the delivered bytes are the same either way.",
      "timestamp": "2026-08-29T16:45:27.699Z"
    },
    {
      "id": "comment-001f82d72b374fedaba362112024f229",
      "author": "qa-b6149c54",
      "content": "ANSWER TO comment-b83b8e4e (worker-9fe78697's handed-back `binding: null` red). I own this call: I am the QA who approved task-e87cfddf and reviewed task-115fedb7, i.e. the goal.create_with_source lane they handed it to. Measured at HEAD, not inferred.\n\nTHE PROVENANCE IS ONE COMMIT OFF, AND THE CORRECTION MATTERS. worker-9fe78697 attributed the emission to e3d20791 (task-fc42ae5e) via GOAL_CREATED_BINDING_KEY at goal-create-with-source.ts:55. That is the SOURCE-BOUND WRITER's key, and it cannot be the cause of a red on a PLAIN goal.create row: `boundFacts` (:63-67) is called only on the with-source handler's path, and goal-services.ts — the plain goal.create writer — never calls it. I read both.\n\nTHE ACTUAL PRODUCER is 9091fcac, `feat(task-221fa0c382d843b1b833da6617fd5d4e): project the bound source binding through /goals/read and admit it in the Control Room catalog decoder`, 2026-08-29 16:05:04 +0300, in apps/daemon/src/http/goal-catalog-entry.ts. The reader — not the writer — projects the member for EVERY row: :160-166 sets `binding` to `admittedBinding(...)` when `shaped.sourceBound` and to `null` otherwise. That is why a prose-only goal grows a `binding: null` in a /goals/read answer while its stored fact carries no such key.\n\nIT IS DELIBERATE, NOT LEAKAGE — that was the open question and it is answered in the source, goal-catalog-entry.ts:28-31: \"`null` rather than an omitted key: the Control Room decodes this answer under an exact-key roster whose LENGTH must match, so an omitted key would refuse every ordinary row.\" Omitting the key for unbound goals would refuse every ordinary catalog row at the Control Room decoder. So of the two exits worker-9fe78697 named, NARROWING THE READ IS THE WRONG ONE — it would trade a stale test expectation for a live decoder refusal on the majority path.\n\nCORRECT REPAIR: update the stale expectation at apps/daemon/src/daemon-command-registry.test.ts:736 to include `binding: null` in the expected goals[0] object. It is a `toEqual` against a full catalog answer whose shape a DONE row deliberately changed; the arm is asserting a contract that no longer exists. One added key, no production change.\n\nWORKER-9fe78697 WAS RIGHT TO REFUSE THE ONE-LINE GREEN. With the producer misidentified, a silent expectation update would have blessed what looked like an unreviewed writer change. It is not a writer change, the reader change was reviewed and is DONE, and now the update is safe. Whoever owns daemon-command-registry.test.ts next can make it.\n\nCORRECTION TO MY OWN EARLIER ATTRIBUTION, since it is the same file: when I reviewed task-e87cfddf and task-115fedb7 I recorded daemon-command-registry.test.ts as failing on the 39->40 hard-coded roster count from c2ab880b. That is true and remains true, but it is not the only failing arm in that file — this `binding: null` arm at :736 is a second, independent cause. One red masked another; my per-file attribution was right about the file and incomplete about the arms. Neither cause is attributable to either row I approved: 9091fcac and e3d20791 are both ancestors of 550cba73 and of 8f49c987, verified with `git merge-base --is-ancestor`.\n\nFiled here because comment-b83b8e4e is here. Not editing this row's status or plan. — qa-b6149c54",
      "timestamp": "2026-08-29T16:47:29.620Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-27T17:21:37.949Z",
  "updatedAt": "2026-08-29T16:54:33.354Z",
  "metrics": {
    "firstClaimAt": "2026-08-27T17:23:44.447Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 9,
    "executedStepCount": 5
  },
  "contextFetchedBy": [
    "architect-ac8532da",
    "worker-4aba1617",
    "architect-94dd67c9",
    "worker-777ee3eb",
    "worker-f16b412c",
    "worker-ee96dea4",
    "worker-9fe78697",
    "worker-5097adb2",
    "qa-860ef713",
    "qa-b6149c54"
  ],
  "planSizeWarnings": [
    "Plan touches 9 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T13:15:26.157Z",
  "planningNotes": {
    "approachesConsidered": "(0) THE CYCLE, AND WHY THIS ROW GOES FIRST. This row and task-ba102165 were mutually blocked. ba102165's block reason states it exactly: \"neither daemon-command-graph-approve.ts nor planning-services.ts composes an ApprovalDecisionRecord after decideApprovalAuthority... the missing daemon-owned seam is already owned by blocked consumer task-6646f888. Adding only the planned producer would be an orphan.\" Meanwhile this row was waiting on ba102165's durable riskTier and policy ref. RESOLUTION: this row lands FIRST, because until a daemon-owned composition site exists there is nowhere for a durable fact to be composed INTO — which is precisely why the producer would have been an orphan. This row survives the missing tier by REFUSING on it with a stable code rather than defaulting it, and step 5's handoff hands ba102165 the exact list of refused facts. That breaks the cycle instead of moving it. (1) Wait for ba102165 — REJECTED: it is BLOCKED on this row, so waiting deadlocks both permanently. (2) Default the missing riskTier so the seam can compose — REFUSED. The tier decides whether step-up human authority is required (approval-invalidation.ts:73 special-cases R3), so a default silently decides an authority question. D4 requires TWO different defaults to red the SAME arm, because an arm catching one is not testing the property. (3) Accept the caller's record when a durable one cannot be composed — REFUSED outright: that is the exact defect this row exists to remove (task rail 1, \"human authority is not delegable\"). (4) Edit `daemon-command-graph-approve.ts` to retire `intentOf` here — REJECTED as scope: this row lands a NEW seam, and touching that file puts the change inside code whose current behaviour other arms still assert. Step 3 forbids it and says to report if the two genuinely cannot coexist. (5) THE COMMAND-KIND CHOICE is deliberately left as a measured decision in step 1(c) rather than pre-empted, because both options carry real fan-out I could not price without measuring: reusing `approval.decide` changes an EXACT PAYLOAD_KEYS allow-list currently requiring `activation` and `record` (daemon-command-vocabulary.ts), while a new kind triggers Rail 7B — generated artifacts and digest mirrors become owned paths and must be REGENERATED, never hand-edited. The step requires the `git grep -l` counts that decided it, and says a budget overrun is a split signal at plan time rather than a step-4 discovery. (6) Calling `checkHumanAuthority` directly — IMPOSSIBLE and forbidden: it is deliberately unpublished (packages/core/src/index.ts:239) and core declares a single `exports` subpath, so a deep import fails TS6059. Route through `decideApprovalAuthority`, which consults the gate first by construction. Verification: `pnpm exec vitest run --root apps/daemon --config package.json apps/daemon/src/planning`.",
    "codebaseInsights": "Measured by architect-a69740e0 at HEAD 6a642a80, HEAD-anchored with positive controls.\n\nTHE DEFECT THIS ROW REMOVES: `intentOf` at `daemon-command-graph-approve.ts:90-96` reads `activation`, `command`, `graphRevisionRef`, `record` and `runId` FROM THE CALLER PAYLOAD via `payloadObject`/`payloadRef`. So the caller supplies the activation witness and the approval record — caller-shaped authority — and the daemon forwards it. `approval.decide` appears in daemon-command-vocabulary.ts at :57 (capability PLANNING), with doc references at :102 and :108.\n\nTHE CYCLE: task-ba102165 is BLOCKED (0/5 steps, unassigned) and its blockedReason names THIS row as the missing composition site; this row was blocked on ba102165's durable facts. Neither `approval-risk-tier-source` nor any `riskTierFor`/`durableRiskTier` symbol exists at HEAD — the grep returns nothing — so the durable tier genuinely is absent and the seam must refuse on it rather than default it.\n\nTHE AUTHORITY ROUTE to compose: `decideApprovalAuthority` (packages/core/src/planning/approval-policy.ts:127-137) branches on the gate FIRST and returns `checkHumanAuthority`'s refusal verbatim; only a null gate falls through to `decidePolicy`. `checkHumanAuthority` is deliberately unpublished from the core barrel (packages/core/src/index.ts:239) and core declares `exports: {\".\": \"./src/index.ts\"}` — one subpath — so a deep import fails TS6059. The two production precedents to copy are `daemon-command-graph-approve.ts:125-137` and `planning-services.ts:263-281`.\n\nTHE EXISTING PLAN SHAPE was seven steps over approval-intent.ts/.js/.test.ts plus the vocabulary, families and registry with their two test files — sound, and this plan keeps that roster while adding the cycle-break, the kind decision and the missing-fact refusal discipline. Zero steps were completed, so nothing is lost by re-planning.\n\nSILENT-DEGRADATION HAZARD: `createRuntimeError` (packages/contracts/src/runtime/runtime-error-factory.ts:93-104) returns `UNKNOWN_ERROR` and does NOT throw on an unknown code or an aggregate the descriptor does not list; `sourceAccepted` (:79-86) checks the aggregate ONLY, never the descriptor's `cmds(...)` list.\n\nFENCED BY TASK RAIL 2: the Control Room UI half belongs to task-40017f79; DEV_PAYLOADS must not be enabled or reshaped here.",
    "risks": "(1) THE CYCLE IS THE MAIN HAZARD AND IT IS EASY TO RE-ENTER: a worker who reads ba102165 as a prerequisite will block, and ba102165's own block names this row, so both park permanently. Step 1 states the ordering and forbids blocking on it. (2) DEFAULTING THE MISSING TIER would silently decide whether step-up human authority is required. D4 requires two DIFFERENT defaults to red the SAME arm — absence and a defaulted value are different answers. (3) ACCEPTING THE CALLER'S RECORD when a durable one cannot be composed re-creates the exact defect being removed; D1 and D2 drill `activation` and `record` SEPARATELY because a seam might fence one and not the other. (4) THE COMMAND-KIND DECISION carries unpriced fan-out either way — reuse changes a frozen exact allow-list, a new kind triggers Rail 7B regeneration. Step 1(c) requires the `git grep -l` counts BEFORE choosing and calls a budget overrun a split signal at plan time. (5) A HAND-EDITED GENERATED ARTIFACT is worse than an unregistered kind: it passes review while diverging from its source. If a new kind is chosen, regenerate. (6) THE SILENT UNKNOWN_ERROR: a refusal code the registry does not admit for this aggregate still compiles, runs and refuses while losing its identity; the dedicated arm plus D5 are the only guards. (7) INVENTING A LOCAL REFUSAL VOCABULARY where an existing layer already emits one — DoD-3 requires the existing tuples forwarded verbatim. (8) EDITING `daemon-command-graph-approve.ts` is out of scope and would put this change inside code other arms still assert; report if the two cannot coexist. (9) A ROSTER CHANGE WITHOUT ITS COUNT CONSUMERS in the same commit reds the branch for everyone. (10) Control Room files and DEV_PAYLOADS are fenced by task rail 2. (11) The three new files are untracked until commit, so SHA256 against a %TEMP% backup is the only valid restore proof for them.",
    "keyFiles": [
      "apps/daemon/src/daemon-command-graph-approve.ts",
      "apps/daemon/src/daemon-command-vocabulary.ts",
      "apps/daemon/src/daemon-command-registry.ts",
      "apps/daemon/src/planning/planning-services.ts",
      "packages/core/src/planning/approval-policy.ts",
      "packages/core/src/policy/approval-invalidation.ts",
      "packages/contracts/src/runtime/runtime-error-factory.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-28T09:33:49.412Z",
    "escalatedAt": "2026-08-28T09:33:49.412Z"
  },
  "planApprovedAt": "2026-08-29T13:15:28.432Z",
  "workStartedAt": "2026-08-27T18:46:14.028Z",
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorHandoffs": [
    {
      "whatIsDone": "Step 1 claim-time porcelain/provenance gate was executed at HEAD 62b4d7f6. It identified three planned paths containing preserved goal.create_with_source WIP owned by BLOCKED CRITICAL task-0ca390d9; evidence is comment-18033faf4ddc461390e6b70be4e15083. No task-specific bytes were authored or staged.",
      "whatRemains": "Resume only after task-0ca390d9's overlapping WIP is architect-corrected and landed or ownership is formally re-planned; then re-run the step-1 porcelain gate before editing.",
      "pitfalls": "Do not overwrite or interleave daemon-command-vocabulary.ts or its vocabulary/registry tests while task-0ca390d9 WIP remains present.",
      "openQuestions": "Architect must decide dependency/order or ownership for the three overlapping paths.",
      "releasedAt": "2026-08-28T21:48:41.526Z",
      "releasedBy": "governor-8c340ce3",
      "reason": "Explicit handoff requested by worker-ee96dea4 so they can claim the unassigned CRITICAL task-8c663da2; task-6646f888 remains blocked on a documented foreign-WIP ownership collision."
    },
    {
      "whatIsDone": "Step 1 re-measured at HEAD 380444767e7c4965ef0aac2bfcef881c7d1f8f9d. Leaf A, duplicate check, and declared collision fence are green. Two independent blocks were durably recorded in comments comment-b732b83ef12644b7a89d707a0373c11f and comment-7cd04f4bdc9045329a65d1ffbce0e837. No task bytes were edited or staged. Serena handoff task-task-6646f888a488481e936dc0dd3f966cd5-handoff is current.",
      "whatRemains": "Architect must amend/split scope to own apps/daemon/src/daemon-store-dependencies.test.ts, and provide honest proposed-run sources or a contract change for riskTier and run-linked applicablePolicyRef (plus generic dependencyChanges) before an accepted approval intent can be implemented. Then resume step 1 and the approved TDD sequence.",
      "pitfalls": "Do not default/infer R3/R2 or use callerRiskHint; do not select latest PolicyEvaluated without a run/graph join. Bind the signed presentation to a command-specific subject digest before authentication and check command replay before burning the single-use proof. Preserve foreign policy-risk-leg.* and graph-activation-service.test.ts WIP.",
      "openQuestions": "Which production authority classifies risk before initial graph activation, and what durable identity binds the applicable policy to the exact run/graph? Should the registry exact-consumer file be added here or split into a follow-up that lands atomically first?",
      "releasedAt": "2026-08-28T09:45:21.617Z",
      "releasedBy": "worker-4aba1617",
      "reason": "Explicit handoff after the mandatory step-1 scope and authority-source blocks; continuing to another user-requested WORKING task."
    },
    {
      "whatIsDone": "Step 1 measured leaf A absent and foreign registry collision; durable evidence comment-2f68f46ca9a34913863515816d57e92e. No task bytes changed.",
      "whatRemains": "After task-3051c597 lands approval.decide_intent and registry porcelain clears, rerun all step-1 measurements.",
      "pitfalls": "Do not name the daemon kind before the runtime vocabulary publishes it; do not sweep the FoundationContextSealPort registry hunk.",
      "openQuestions": "None.",
      "releasedAt": "2026-08-27T18:47:08.191Z",
      "releasedBy": "worker-4aba1617",
      "reason": "Explicit seat handoff after mandatory prerequisite block; user requested continued work on available rows."
    }
  ],
  "reviewStartedAt": "2026-08-29T16:48:05.812Z",
  "verification": {
    "command": "pnpm exec vitest run --root apps/daemon --config package.json apps/daemon/src/planning/approval-intent.test.ts apps/daemon/src/daemon-command-vocabulary.test.ts apps/daemon/src/mcp-tool-allowlist.test.ts apps/daemon/src/daemon-store-dependencies.test.ts",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  4 passed (4)\n      Tests  178 passed (178)\n   Start at  19:47:32\n   Duration  4.34s (transform 8.48s, setup 0ms, import 11.73s, tests 1.46s, environment 1ms)\n\nVERIFY_EXIT=0",
    "reportedAt": "2026-08-29T16:48:05.812Z"
  },
  "filesModified": [
    "apps/daemon/src/daemon-command-vocabulary.ts",
    "apps/daemon/src/daemon-command-graph-approve.ts",
    "apps/daemon/src/planning/approval-intent.test.ts",
    "apps/daemon/src/planning/approval-intent.ts",
    "apps/daemon/src/planning/approval-intent.js",
    "apps/daemon/src/planning/approval-intent-sources.ts",
    "apps/daemon/src/planning/approval-intent-sources.js",
    "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
    "apps/daemon/src/planning/approval-intent-contracts.ts",
    "apps/daemon/src/planning/approval-intent-contracts.js",
    "apps/daemon/src/daemon-command-edges.ts",
    "apps/daemon/src/daemon-command-registry.ts",
    "apps/daemon/src/daemon-command-vocabulary.test.ts",
    "apps/daemon/src/daemon-command-registry.test.ts",
    "apps/daemon/src/mcp-tool-allowlist.test.ts",
    "apps/daemon/src/daemon-store-dependencies.test.ts",
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.js"
  ]
}