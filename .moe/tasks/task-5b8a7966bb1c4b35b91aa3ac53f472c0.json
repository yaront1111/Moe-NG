{
  "id": "task-5b8a7966bb1c4b35b91aa3ac53f472c0",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[R3-5 adjacent] graph.supersede ingress must consult the approval policy settings and the humanReview witness like graph.approve does",
  "description": "FILED BY R3-5 PLANNING (task-b54b560991704162aed1f71814596cac) from the reviewer's ADJACENT note; board search for supersede+humanReview/decideApprovalAuthority returned no row (2026-08-28).\n\nMEASURED at the tree after 23682c14: `decideGraphSupersedeApproval` (apps/daemon/src/daemon-command-graph-supersede.ts:54-77) checks actor equality, `applyApprovalCommand`, and decision === \"APPROVE\" and returns. It never calls `readApprovalPolicySettings(process.env)`, `decideApprovalAuthority({ gate, policy })`, `operatorReviewAuthority(input.humanReview, ...)` or `approvalDelayDisposition(...)` — the four calls graph.approve applies at daemon-command-graph-approve.ts:125-137 and approval.decide applies in planning-services.ts. Consequence: a board configured to require human review (or a delay) for graph approvals is bypassed by supersession, and the `humanReview` witness the edge context carries (daemon-command-graph-edges.ts:150 `humanReview: context.humanReview`) is unread on this path.\n\nSCOPE: apply the same authority sequence in the supersede ingress after the actor guard and before the core reducer, forwarding the policy's own code and layer (APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY etc.) unchanged; hostile arms on the production edge with the exact code+layer; a DIVERGENCE fixture per epic rail 7A (an input only the new authority check can refuse — the record otherwise decides). Keep R3-5's seven-field binding (graph-supersede-approval-binding.ts) untouched; this row adds a layer, it does not move one.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK on disk: daemon-command-graph-supersede.ts still contains no call to readApprovalPolicySettings/decideApprovalAuthority/operatorReviewAuthority (grep -n) — else adopt or block as duplicate; R3-5's matcher module exists (ls apps/daemon/src/planning/graph-supersede-approval-binding.ts) so the two layers compose rather than collide.",
    "The supersede ingress applies readApprovalPolicySettings + decideApprovalAuthority + the humanReview fall-through + approvalDelayDisposition exactly as graph.approve :125-137, refusing with the policy's own code and layer; zero events and unchanged decision count on refusal.",
    "Arms on runGraphEdge (kind graph.supersede, real store via supersedableStore()) assert exact code, layer and refusedBy for: human review required with no witness; witness present decides; non-IMMEDIATE delay refuses; each is a divergence fixture that no other layer refuses.",
    "Mutation drill: delete the new authority call — the human-review-required arm goes RED with its exact code; restore proven by SHA256 against a %TEMP% backup in a separate call.",
    "Gates standalone with nonzero counts: pnpm --filter @moe/daemon typecheck ($ tsc echo) and the focused daemon vitest run over daemon-command-graph-edges.test.ts plus the new arms; daemon-command-graph-supersede.ts stays <=250 lines or is split; explicit-pathspec commit."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK, read-only; author no bytes. Record commands and results in a durable moe.add_comment.\n1) Anchor the premise at the delivered tree: `git grep -n -E 'readApprovalPolicySettings|decideApprovalAuthority|operatorReviewAuthority|approvalDelayDisposition|humanReview' HEAD -- apps/daemon/src/daemon-command-graph-supersede.ts` must return no hits. If it now hits, identify the owning commit and adopt/report duplicate rather than re-implementing.\n2) Prove the prerequisite is landed, not merely present: run `git ls-files --error-unmatch apps/daemon/src/planning/graph-supersede-approval-binding.ts` and require the printed path, then require `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` to print a commit line. Also grep graph-supersede-service.ts for the production import and call of matchSupersedeApproval. If any clause fails, report_blocked naming task-b54b5609; this ordering is a safety constraint because otherwise this ingress can authorize a successor before the seven-field binding exists.\n3) Re-run the shape/reachability measurement, not a symbol-only existence check: `git grep -n 'decideGraphSupersedeApproval' HEAD -- apps packages` must show only the definition/import/one call in daemon-command-graph-edges.ts; inspect runContextEdge and assembleGraphRequest to confirm request.decidedAt is server-assembled and the raw supersede payload omits decidedAt. Any new caller is a stop-and-report ownership expansion.\n4) Re-run a Moe full plan-field ownership scan for the three paths named by this plan and `git status --porcelain --` those exact paths. Proceed only with zero live foreign claims and clean paths (or explicitly attributable own hunks). task-9fb4e53d's durable decision work is adjacent; if its plan now claims either production path, report the collision rather than editing.\n5) Enumerate current runGraphEdge graph.supersede callers. Confirm daemon-command-graph-edges.test.ts and graph-supersede-approval-binding.test.ts attach humanReview and use a successor-bound accepted record; those controls must continue to decide under the default REQUIRE_HUMAN policy. Put the caller roster in the comment.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "VERIFY-OR-BLOCK PASSES, all 5 clauses, read-only, zero bytes authored. Full command/result transcript in comment-cc793048b1ed4ab08e6f82c57a5df7e6 (durable).\n\nRe-measured at HEAD aebdb367e0df0cfce1e18143d22c5dd63d42ed01 (planning anchored 62b4d7f6; HEAD moved, premise re-taken at the delivered tree).\n\n1) Gap live: the 5-symbol grep on daemon-command-graph-supersede.ts EXIT=1, zero hits, 78 lines. Not a duplicate; nothing to adopt.\n2) R3-5 landed with a REAL consumer edge: ls-files printed the matcher path; last commit 38acf9d3 fix(task-b54b5609); graph-supersede-service.ts:40 imports and :224 CALLS matchSupersedeApproval. Safety ordering satisfied.\n3) Reachability grep returns exactly 3 lines: definition supersede.ts:54, import edges.ts:19, one call edges.ts:155. No new caller. Confirmed decidedAt is server-assembled — contracts.ts:45 reserves the key, :112-126 assembleGraphRequest injects replayStableDecidedAt (:95) — and runContextEdge builds `request` BEFORE the supersede branch, so request.decidedAt is in scope. Reading facts.envelope.payload[\"decidedAt\"] would be wrong, as the plan warned.\n4) Ownership clean: porcelain on all three owned paths is EMPTY. Full .moe/tasks plan-field scan (excluding DONE/ARCHIVED) found two live rows — this one and task-9fb4e53d. Inspected 9fb4e53d directly: affectedFiles null, keyFiles lists 8 paths, NONE of my three; the basenames appear only inside its own comment declaring \"SIBLING BOUNDARY: NO COLLISION ... Zero file overlap\" (it is the WRITER/READER, this row is the INGRESS). No collision to report.\n5) runGraphEdge callers (6 files): edges.ts definition, daemon-command-registry.ts production, and 4 suites. Both supersede-driving suites attach a server witness — edges.test.ts:67 and graph-supersede-approval-binding.test.ts:84, `humanReview: Object.freeze({ principalId: PRINCIPAL })` — so they fall through the new branch and must stay accepted; that is the step-3/final-gate regression. transport and expansion-request-production-edge carry neither humanReview nor graph.supersede, unreachable by the new layer.\n\nEnv keys for step 2: APPROVAL_MODE_ENV_KEY=\"MOE_APPROVAL_MODE\", SPEED_MODE_DELAY_ENV_KEY=\"MOE_SPEED_MODE_DELAY_MS\" (approval-policy-settings.ts:33-34, read at :79-80).\n\nShared checkout carries ~84 dirty foreign paths; none intersect the three owned paths. Not touching them.",
      "startedAt": "2026-08-29T12:04:32.712Z",
      "completedAt": "2026-08-29T12:06:15.320Z"
    },
    {
      "stepId": "step-2",
      "description": "TDD RED FIRST on the production surface. Create graph-supersede-authority.test.ts; do not edit daemon-command-graph-edges.test.ts or the landed R3-5 suite.\nDrive `runGraphEdge` with kind graph.supersede, a real SqliteEventStore from supersedableStore(), approvalCommand(), and successorBoundApprovalInput(store), so actor equality, the core reducer, the seven-field matcher, and every service fact fence would otherwise accept. Copy only the small module-private edgeFor/refusalOf/decisionCount/eventCounts helpers needed by the suite. Use closeStores() on every test exit, and isolate process.env with vi.stubEnv plus vi.unstubAllEnvs in afterEach for APPROVAL_MODE_ENV_KEY and SPEED_MODE_DELAY_ENV_KEY.\nAdd four arms. A: default/REQUIRE_HUMAN with humanReview undefined refuses exact code APPROVAL_HUMAN_REVIEW_REQUIRED, layer APPROVAL_POLICY, and exact DomainRefusal detail carrying refusedBy DAEMON_GRAPH_INGRESS (including the source code/layer suffix if the ingress forwards it); decision count and all tracked aggregate event counts stay at their baselines. B: same policy and bytes with `{ principalId: PRINCIPAL }` decides and increments the decision count once. C: SPEED with delay `2000`, no witness, refuses the same exact code/layer/refusedBy with unchanged decisions/events. D: SPEED with delay `0`, no witness, decides and increments once.\nPut the rail-7A divergence statement in the suite header: A is reachable only through the policy want-of-human fence and B proves the otherwise-identical valid record decides with a server witness; C reaches an OK PROCEED_WITHOUT_HUMAN policy result and only the non-IMMEDIATE delay fence can refuse, while D proves the same record/configuration axis decides at delay zero. All arms pass the actor guard; accepted controls prove the core reducer and landed R3-5 service matcher are not answering first.\nRun exactly `pnpm --dir apps/daemon exec vitest run --root . --config package.json src/planning/graph-supersede-authority.test.ts` in the foreground, unpiped. Require a genuine red caused by A and C reaching `expected edge to refuse`, while B and D pass; quote the nonzero Test Files/Tests counts. Import, fixture, zero-test, and compile failures are not an acceptable red.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/graph-supersede-authority.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/planning/graph-supersede-authority.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/graph-supersede-authority.test.ts"
      ],
      "note": "RED RECEIPT — genuine, on the production surface, from the exact cause the plan named.\n\nCommand (foreground, standalone, unpiped):\n`pnpm --dir apps/daemon exec vitest run --root . --config package.json src/planning/graph-supersede-authority.test.ts`\nEXIT=1\n  Test Files  1 failed (1)\n       Tests  2 failed | 2 passed (4)\n\nFailures are exactly A and C, both `Error: expected the edge to refuse` raised by refusalOf at graph-supersede-authority.test.ts:119 (A from :166, C from :193). B and D pass. This is not an import, fixture, compile or zero-test red — the suite loaded, all four arms executed, and the two accepted controls already decide against the real store.\n\nWHAT THE SUITE DRIVES. Real `runGraphEdge`, kind graph.supersede, real `supersedableStore()`, `approvalCommand()`, and `successorBoundApprovalInput(store)` — so the authenticated-actor guard, the core reducer, the APPROVE guard, task-b54b5609's seven-field matcher and every graph-supersede-service fact fence would all accept. Only the approval configuration and the presence of a witness vary. B and D passing at RED is the proof that no downstream fence is answering first; if one were, A and C would have refused already and there would be no red to bank.\n\nFOUR ARMS.\n  A default/REQUIRE_HUMAN (both env keys stubbed to \"\"), humanReview undefined -> must refuse APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY, detail \"DAEMON_GRAPH_INGRESS (APPROVAL_HUMAN_REVIEW_REQUIRED/APPROVAL_POLICY)\", with decisions, all 7 tracked aggregate event counts, and the event horizon unchanged.\n  B same policy, same bytes, humanReview {principalId: PRINCIPAL} -> DECIDED, decisions +1.\n  C SPEED delay \"2000\", no witness -> same exact code/layer/detail, unchanged baselines.\n  D SPEED delay \"0\", no witness -> DECIDED, decisions +1.\n\nDIVERGENCE (epic rail 7A), stated in the suite header. A and C intentionally emit the SAME code and layer, so the code cannot say which mechanism answered; each is paired with a byte-identical DECIDING arm one degree away. A is reachable only through the policy's want-of-human fence (B proves the record is otherwise valid and that the witness fall-through clears it). C reaches an OK PROCEED_WITHOUT_HUMAN policy result, so the policy fence CANNOT be what refuses it — only the non-IMMEDIATE delay fence can — and D holds the configuration axis while moving only the delay. Loosening either fence alone therefore reddens a different arm; step 4's one-degree drills measure exactly that.\n\nENV ISOLATION. Both APPROVAL_MODE_ENV_KEY and SPEED_MODE_DELAY_ENV_KEY are stubbed on EVERY arm via vi.stubEnv (never left ambient — an inherited MOE_APPROVAL_MODE would otherwise decide an outcome), and afterEach runs closeStores() then vi.unstubAllEnvs().\n\nVerified before writing, not assumed: decideApprovalAuthority (packages/core/src/planning/approval-policy.ts:127) consults the gate FIRST and short-circuits, falling through to decidePolicy which returns APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY for REQUIRE_HUMAN; approvalDelayDisposition (approval-gate.ts:104-107) returns IMMEDIATE only at delayMs === 0; readApprovalPolicySettings (approval-policy-settings.ts:75-82) yields REQUIRE_HUMAN unless approvalMode is exactly \"SPEED\" with a digits-only delay; refuseGraph (daemon-command-graph-edges.ts:71-76) composes detail as `${refusedBy} (${sourceCode}/${sourceLayer})`, which is where the expected INGRESS_DETAIL literal comes from.\n\nDid not edit daemon-command-graph-edges.test.ts or the landed R3-5 suite. One new file, 208 lines (test file, not held to the production cap).",
      "startedAt": "2026-08-29T12:06:20.038Z",
      "completedAt": "2026-08-29T12:08:01.578Z"
    },
    {
      "stepId": "step-3",
      "description": "Implement the minimum green fix after the red receipt.\nIn daemon-command-graph-supersede.ts import decideApprovalAuthority and grantHumanAuthority from @moe/core, HumanReviewWitness from bootstrap-ledger, readApprovalPolicySettings, and approvalDelayDisposition. Add a private operatorReviewAuthority mirroring the two existing audited daemon implementations: mint a HumanAuthorityGate from the server witness with gateId `supersede-review:${commandId}`, workRef commandId, grant null, and Date.parse(decidedAt); return the core refusal unchanged or re-run decideApprovalAuthority against the granted gate. Explain in the comment that commandId is only the gate label and confers no authority; the witness supplies the human principal.\nWiden decideGraphSupersedeApproval to `(facts, humanReview, decidedAt)` and update the one call in runContextEdge to pass `context.humanReview` and the already-assembled `request.decidedAt`—never read decidedAt from facts.envelope.payload. Insert the authority sequence after the existing authenticated-actor guard and before applyApprovalCommand: readApprovalPolicySettings(process.env); decideApprovalAuthority({ gate: null, policy }); only for APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY with a defined witness, invoke operatorReviewAuthority; forward any refusal's exact code/layer through refused() without translation; refuse any approvalDelayDisposition other than IMMEDIATE as APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY.\nWhen forwarding a policy/delay refusal, preserve the policy code/layer as the top-level code/layer and as sourceCode/sourceLayer, while refusedBy remains DAEMON_GRAPH_INGRESS. Do not move or weaken the actor guard, core reducer call, APPROVE guard, or landed seven-field service matcher; this row adds an ingress layer only.\nRerun the single-file command from the prior step and require all four arms green with nonzero counts. Then run the landed peers with `pnpm --dir apps/daemon exec vitest run --root . --config package.json src/daemon-command-graph-edges.test.ts src/planning/graph-supersede-approval-binding.test.ts`; both files must execute and pass. Keep daemon-command-graph-supersede.ts below 250 physical lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-graph-supersede.ts",
        "apps/daemon/src/daemon-command-graph-edges.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/daemon-command-graph-supersede.ts",
        "apps/daemon/src/daemon-command-graph-edges.ts"
      ],
      "note": "GREEN after the red receipt. Minimum fix; an ingress LAYER added, nothing moved or weakened.\n\nRESULTS (foreground, standalone, unpiped):\n1) `pnpm --dir apps/daemon exec vitest run --root . --config package.json src/planning/graph-supersede-authority.test.ts` -> EXIT=0, `Test Files  1 passed (1)`, `Tests  4 passed (4)`. Same command that returned EXIT=1 with 2 failed | 2 passed before the fix; the only change between the two runs is the production diff below.\n2) `pnpm --dir apps/daemon exec vitest run --root . --config package.json src/daemon-command-graph-edges.test.ts src/planning/graph-supersede-approval-binding.test.ts` -> EXIT=0, `Test Files  2 passed (2)`, `Tests  75 passed (75)`. Both landed peer suites executed (nonzero counts) and pass; the step-1 prediction holds — both carry a server witness, so they fall through the new branch and stay accepted.\n\nWHAT LANDED, daemon-command-graph-supersede.ts:\n- imports: decideApprovalAuthority + grantHumanAuthority from @moe/core; HumanReviewWitness type from bootstrap-ledger; approvalDelayDisposition from planning/approval-gate.js; readApprovalPolicySettings from planning/approval-policy-settings.js.\n- private `operatorReviewAuthority(witness, commandId, decidedAt, policy)`, mirroring the two existing audited daemon copies (graph-approve.ts:69-81 and planning-services.ts). Mints the gate `{ gateId: \"supersede-review:${commandId}\", grant: null, workRef: commandId }` through the core's own grantHumanAuthority with `{ kind: \"HUMAN\", principalId: witness.principalId }` and `Date.parse(decidedAt)`, returns the core refusal UNCHANGED on failure, otherwise RE-DERIVES the verdict via decideApprovalAuthority against the granted gate. The doc comment states plainly that commandId is only the gate LABEL and confers no authority; the human principal comes from the witness and nowhere else, and a witness never travels on the payload.\n- private `decideSupersedeAuthority(humanReview, commandId, decidedAt)` returning `GraphSupersedeApprovalRefused | null`: readApprovalPolicySettings(process.env); decideApprovalAuthority({ gate: null, policy }); the witness fall-through fires ONLY for code APPROVAL_HUMAN_REVIEW_REQUIRED and layer APPROVAL_POLICY with humanReview !== undefined; then a non-IMMEDIATE approvalDelayDisposition refuses. `gate: null` is deliberate and documented — supersede has no runId and GraphRequestFacts carries no ledger, so no durable gate is manufactured.\n- signature widened to `decideGraphSupersedeApproval(facts, humanReview, decidedAt)`; the sequence is inserted AFTER the authenticated-actor guard and BEFORE applyApprovalCommand.\n\nFORWARDING. Every policy/delay refusal keeps the policy's own code and layer at top level AND as sourceCode/sourceLayer; refusedBy stays DAEMON_GRAPH_INGRESS. refuseGraph composes that into detail `DAEMON_GRAPH_INGRESS (APPROVAL_HUMAN_REVIEW_REQUIRED/APPROVAL_POLICY)`, which the arms assert exactly. No translation into a local vocabulary anywhere.\n\ndaemon-command-graph-edges.ts: the single call at the graph.supersede branch now passes `context.humanReview` and `request.decidedAt` — the value assembleGraphRequest already injected from replayStableDecidedAt, built before the branch. facts.envelope.payload[\"decidedAt\"] is NOT read; a raw supersede payload has no such member and reading one would let a caller date its own grant.\n\nUNTOUCHED, as the row requires: the authenticated-actor guard, the core reducer call, the APPROVE guard, and task-b54b5609's seven-field service matcher. This row adds a layer, it does not move one.\n\nLINE COUNTS: daemon-command-graph-supersede.ts 155 (was 78, cap 250); daemon-command-graph-edges.ts 194 (was 190); graph-supersede-authority.test.ts 213 (test file). All under the cap; no split needed.\n\nDrills, the repo-wide baseline attribution, adversarial review and the explicit-pathspec commit are step 4.",
      "startedAt": "2026-08-29T12:08:03.980Z",
      "completedAt": "2026-08-29T12:09:36.748Z"
    },
    {
      "stepId": "step-4",
      "description": "MUTATION DRILLS, one final verification gate, adversarial review, and durable evidence. Before drilling, `git status --porcelain` the exact mutated path and copy its current task bytes to a Windows `$env:TEMP` filename suffixed by worker id. Confirm each mutation actually changed the intended single occurrence. Restore after each drill; in a separate command compare SHA256 of the restored file and backup and require two nonempty equal 64-hex digests.\nDrill 1 (DoD reachability): replace only `decideApprovalAuthority({ gate: null, policy })` with a permissive immediate-success value while keeping the file runnable. A and C must go RED because they decide instead of returning their exact policy refusal; B and D remain green. Restore and rerun green. Drill 2 (identical-code divergence): disable only the approvalDelayDisposition refusal. Exactly C must go RED by deciding; A, B, and D remain green. Restore and rerun green. Drill 3 (witness fall-through): make only the humanReview branch condition false. Exactly B must go RED with APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY; A, C, and D remain green. Restore and rerun green. Any green drill or different red set is a finding to record, never evidence to bank.\nRun gates foreground, standalone, and unpiped. Leg A: `pnpm --filter @moe/daemon typecheck`; require exit 0 and quote the `$ tsc --project tsconfig.json` echo. Exact verification command/evidence leg B: `pnpm --dir apps/daemon exec vitest run --root . --config package.json src/planning/graph-supersede-authority.test.ts src/daemon-command-graph-edges.test.ts src/planning/graph-supersede-approval-binding.test.ts`; require exit 0 plus `Test Files 3 passed` and a nonzero Tests count. Leg C: `pnpm --filter @moe/daemon test`; record merge-base and HEAD failing-path sets, require no newly failing path intersecting these three owned paths, and disclose foreign red verbatim rather than fabricating green. An exit-zero leg without count/tsc evidence is unverified.\nAdversarial review in the final step note/comment: registry.ts, not payload bytes, creates humanReview only for the authenticated operator; the envelope commandId is a label and grantHumanAuthority still derives authority solely from the server witness; request.decidedAt is replay-stable and assembled before the ingress; policy/delay refusal happens before reducer/service writes and every refusal arm proves unchanged events/decisions; actor, APPROVE, and R3-5 matcher fences remain present; env stubs restore; repeat/replay cannot add a second decision. Report physical line counts for both production files and keep them below 250 where this row adds logic.\nPut drill outcomes, count lines, baseline attribution, line counts, and the explicit owned path list in a durable moe.add_comment (summary is only a pointer). Commit only the three explicit owned pathspecs; never stage the tree, push, merge, reset, stash, or touch foreign WIP.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-graph-supersede.ts",
        "apps/daemon/src/daemon-command-graph-edges.ts",
        "apps/daemon/src/planning/graph-supersede-authority.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/daemon-command-graph-supersede.ts",
        "apps/daemon/src/daemon-command-graph-edges.ts",
        "apps/daemon/src/planning/graph-supersede-authority.test.ts"
      ],
      "note": "DONE. Full evidence in two durable comments: comment-bdde88da (commit, drills, gates, baseline attribution) and comment-7030ef5d (adversarial review). This note is a pointer.\n\nCOMMIT a3849db9 via `git commit --only` over exactly the three owned paths — 3 files changed, 296 insertions(+), 2 deletions(-). Porcelain over those paths is EMPTY afterwards, so committed bytes == gated bytes. No tree stage, push, merge, reset or stash; ~84 foreign dirty paths untouched.\n\nDRILLS, all three landing the predicted red set and no other:\n  1 policy bypass  -> A and C red (\"expected the edge to refuse\"), B/D green.\n  2 delay fence off (opaque-false, call still executes) -> ONLY C red.\n  3 witness branch unsatisfiable -> ONLY B red, with the exact literal\n    `APPROVAL_HUMAN_REVIEW_REQUIRED: DAEMON_GRAPH_INGRESS (APPROVAL_HUMAN_REVIEW_REQUIRED/APPROVAL_POLICY)`.\nThree mechanisms, three distinct red sets one degree apart — which is what makes A and C's shared code non-vacuous. The mutated file was TRACKED BUT DIRTY, so git diff proves nothing; every restore is SHA256 equality against a %TEMP% backup suffixed by worker id, checked in a separate call (EQUAL64 True each time, B8CA83AF…1461). No green drill.\n\nGATES:\n  A `pnpm --filter @moe/daemon typecheck` EXIT 0, echo `$ tsc --project tsconfig.json`.\n  B (the row's exact verification command, 3 files) EXIT 0, `Test Files 3 passed (3)`, `Tests 79 passed (79)`.\n  C `pnpm --filter @moe/daemon test`: delivered tree 6 failed | 297 passed (303), 21 failed tests; merge-base — measured by actually reverting my whole diff, porcelain-empty — 8 failed | 294 passed (302), 65 failed tests. (2) MINUS (1) is EMPTY; baseline is strictly worse than delivered.\n\nFOREIGN RED DISCLOSED, NOT HIDDEN. One transient typecheck failure at ~15:20, verbatim:\n  `src/work/foundation-attempt-contracts.ts(187,7): error TS6133: 'numberOf' is declared but its value is never read.`\nNot mine: not an owned path, already foreign-dirty before I started, `numberOf` ABSENT at HEAD and present only in a peer's uncommitted edit, 0 occurrences in my three files, and the leg was EXIT 0 at 15:11 with these same bytes. A peer fixed it within the minute; re-run EXIT 0.\n\nREVERSE DRILL where required. Import-closure attribution returned NONE for five of the six failing suites (decisive). foundation-attempt-service.test.ts HIT daemon-command-graph-edges.ts, so an empty path intersection was not accepted as proof — ran it isolated at PARENT bytes and at MY bytes: 78 passed both times, identical. Its full-suite failure is a whole-suite interaction reproducing with my diff absent.\n\nADVERSARIAL REVIEW headline: the registry comment at daemon-command-registry.ts:179 ALREADY named graph.supersede — the operator witness was being assembled and forwarded for this kind all along and merely never READ, which is why the fix needed no registry change. Witness comes from the authenticated principal at :200-202, never payload; commandId is only a gate label; decidedAt is server-assembled and replay-stable; the sequence precedes every write so refusals leave zero residue (arms assert decisions, 7 aggregate event counts and the horizon all unmoved); actor/APPROVE/R3-5 fences intact; env stubs restore; no unadapted caller (typecheck would have caught one).\n\nDISCLOSED FOLLOW-UP, deliberately not taken: there are now three private operatorReviewAuthority copies (graph-approve, planning-services, here). Extraction was considered and rejected at planning time — moving one is not de-duplication, moving all three widens this security fix to seven files. Consolidation is a separate row.\n\nLINE COUNTS (wc -l): supersede 155 (was 78), edges 194 (was 190), new test 213. Both production files under the 250 target.",
      "startedAt": "2026-08-29T12:09:41.334Z",
      "completedAt": "2026-08-29T12:26:12.770Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "GOVERNOR UNBLOCK 18:15Z — build-order gate cleared, re-measured at HEAD 3c23339a.\n\nTHE BLOCK: \"BUILD-ORDER BLOCK on task-b54b5609 (R3-5). Step 1's read-only gate ran to completion; clause (b) FAILS.\" task-b54b5609 has since LANDED — commit 38acf9d3 \"fix(task-b54b5609): bind supersede approval\" is in HEAD.\n\nI verified the delivered surface rather than trusting the status flip, and it has a REAL CONSUMER EDGE, not just an existence hit:\n  apps/daemon/src/planning/graph-supersede-approval-binding.ts:55 — `export function matchSupersedeApproval(...)`, production, tracked\n  apps/daemon/src/planning/graph-supersede-service.ts:40  — `import { matchSupersedeApproval } from \"./graph-supersede-approval-binding.js\"`\n  apps/daemon/src/planning/graph-supersede-service.ts:224 — `const unbound = matchSupersedeApproval(store, supersede, facts, input.approval)`\nDefined, imported, and CALLED from the production service. That is the standard Rail 8(B) demands and the one I have been enforcing all day — a definition plus a bridge would not have been enough.\n\nNote graph-supersede-approval-binding.ts is now TRACKED. At 16:10Z it was untracked and did NOT compile — it used eight refusal codes absent from the GRAPH_SUPERSEDE code union — which is why I excluded it from commit 061eb7f5. It has since been finished properly and landed with its codes.\n\nARCHITECT: re-run step 1's clause (b) at your own HEAD before planning. This row's own scope is unchanged — graph.supersede ingress must consult the approval policy settings and the humanReview witness the way graph.approve does. Its sibling task-9fb4e53d is still blocked on task-bdbe0519's landing plus this row, so do not fold the two together.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 116.9,
  "comments": [
    {
      "id": "comment-d62157ba19034db9ad7fc5c14361bab5",
      "author": "worker-f16b412c",
      "content": "STEP 1 VERIFY-OR-BLOCK GATE — RESULT: (a),(c),(d) PASS; (b) FAILS. No bytes authored. Measured by worker-f16b412c at HEAD 7f062869 (plan measured d7a84177 — HEAD has moved 1 commit).\n\n(a) PARITY PREMISE CONFIRMED, NOT ASSUMED. `grep -n \"readApprovalPolicySettings\\|decideApprovalAuthority\\|operatorReviewAuthority\\|approvalDelayDisposition\\|humanReview\" apps/daemon/src/daemon-command-graph-supersede.ts` -> exit 1, ZERO hits. File is 78 lines; `decideGraphSupersedeApproval` :54-77 does exactly three things (payloadObject command/record :57-61, actor-vs-principal :62-64, applyApprovalCommand + APPROVE guard :65-76). No sibling has landed it. graph.approve's sequence measured intact at daemon-command-graph-approve.ts:125-137 with `operatorReviewAuthority` at :69-81 (signature today is 4-arg `(witness, runId, decidedAt, policy)`, gateId built inline as `approval-review:${runId}`, workRef = runId — matches what step 2 plans to generalise). wc -l: supersede 78, approve 164, edges 190.\n\n(b) **GATE FAILS — BUILD ORDER.** `ls apps/daemon/src/planning/graph-supersede-approval-binding.ts` -> \"No such file or directory\". `git ls-files` and `git status --porcelain` both return NOTHING for it and for `graph-supersede-approval-binding.test.ts`; a repo-wide `find apps -iname \"*supersede*\"` lists 17 files and the binding module is not among them, tracked or untracked. task-b54b560991704162aed1f71814596cac (R3-5) is status WORKING, assigned worker-a224437e, on **step-1 of 8, IN_PROGRESS, seven steps PENDING**; its plan declares `graph-supersede-approval-binding.ts`, `.js` and `.test.ts` as NEW files, so the module is 7 steps away, not imminent. Per the plan's own stop-and-report and DoD clause 1 (\"R3-5's matcher module exists (ls ...) so the two layers compose rather than collide\"), this row is blocked, not proceedable. DoD clause 5 is independently unsatisfiable for the same reason: Leg B names `src/planning/graph-supersede-approval-binding.test.ts` in the vitest invocation, and vitest cannot run a file that does not exist.\n\n(c) OWNERSHIP CLEAN. Scanned every non-DONE/non-ARCHIVED row in .moe/tasks for filesModified + every step's affectedFiles/modifiedFiles/newFiles: the ONLY live claim on `daemon-command-graph-supersede.ts`, `daemon-command-graph-edges.ts`, `daemon-command-graph-approve.ts`, `approval-review-authority*` and `graph-supersede-authority.test.ts` is THIS row. ZERO live claims on `planning/approval-policy-settings.ts` and `planning/approval-gate.ts`. `git status --porcelain` on all four production paths prints NOTHING. R3-5 does hold `daemon-command-graph-edges.test.ts`, `graph-supersede-contracts.ts`, `graph-supersede-refusals.test.ts`, `graph-supersede-service.ts`, `graph-supersede-service.test.ts` and `graph-supersede-test-fixtures.ts` — this row RUNS edges.test.ts in Leg B and edits none of them.\n\n(d) FAN-OUT ROSTER — the plan's roster is CORRECT but INCOMPLETE by one, and the omission is benign. Default env is REQUIRE_HUMAN confirmed: `decodeApprovalPolicy` (approval-policy-settings.ts:60-64) returns REQUIRE_HUMAN unless approvalMode === SPEED **and** `statedDelay` is non-null; `delaySetting` :69-72 only converts `/^\\d+$/u`. So every edge-level supersede caller begins refusing unless the witness carries it. Callers that drive graph.supersede through `runGraphEdge`:\n  1. **`daemon-command-registry.ts:182` — PRODUCTION, and the plan did not name it.** Witness supplied iff `principal.principalId === operatorPrincipalId`, else `undefined`. Benign: graph.supersede is in `HUMAN_ONLY` / OPERATOR_PRINCIPAL_KINDS (daemon-command-graph-authority.test.ts:49), so every principal admitted this far IS the operator and the witness is always present. Named here so no later step discovers it cold.\n  2. `daemon-command-graph-edges.test.ts:310,319,322` — `edgeFor` :63-75 sets `humanReview: Object.freeze({ principalId: PRINCIPAL })` at :67, so all three take the fall-through.\n  3. `daemon-command-graph-edges.test.ts:333,347` — foreign project / stale revision, refuse earlier, unaffected.\n  4. R3-5's planned suite — DOES NOT EXIST (see (b)).\n  NOT consumers, checked and cleared: `active-graph-projection.test.ts:370` builds a `GraphRevisionCommand` for the CORE, never calls `runGraphEdge`; `daemon-command-graph-transport.test.ts` names `runGraphEdge` only in a doc comment at :7 and drives no supersede; registry/vocabulary/store-dependencies tests reference the kind as a roster string only.\n\n(e) SIBLING task-9fb4e53d51db4c7f9009275445706723 is PLANNING, unassigned, **0 steps** — no plan, so no file collision to declare in #architects yet. If its plan later names `daemon-command-graph-supersede.ts`, that is the moment to raise it.",
      "timestamp": "2026-08-28T12:35:38.030Z"
    },
    {
      "id": "comment-5f017353ea0e46c4991aef8aa049821d",
      "author": "worker-f16b412c",
      "content": "BLOCK UPDATE — unpark condition is HALF MET, still not met. Re-measured 2026-08-28 by worker-f16b412c after R3-5 completed its step 4.\n\n`apps/daemon/src/planning/graph-supersede-approval-binding.ts` NOW EXISTS on disk (4138 bytes, mtime 15:37) alongside its `.js` bridge (55 bytes, 15:36). BUT `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` still prints NOTHING — the module is UNTRACKED, uncommitted. Plan step 1(b) requires \"exists AND ... shows a commit\", and names \"absent **or uncommitted**\" as the block trigger, so the gate still fails and this row correctly stays BLOCKED.\n\nTRAJECTORY, which should decide the architect's choice: task-b54b5609 (R3-5) has moved from step-1/8 to **step-5/8 IN_PROGRESS** since my block was filed (steps 1-4 COMPLETED). Its step 8 is the commit step. So the unpark is a matter of R3-5 finishing three more steps, not a structural problem.\n\nRECOMMENDATION: prefer the cheap option. Leave this row parked and let the unpark condition fire on its own rather than re-sequencing it as a hard dependent or amending the plan to authorise step 2 standalone. The unpark command is unchanged and still one line with no judgment:\n`git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` prints a commit.\n\nEverything else from step 1 remains green and recorded in comment-d62157ba19034db9ad7fc5c14361bab5; the resuming session starts at step 2 with no re-measurement debt beyond re-confirming (a) and (c) at the then-current HEAD.\n\nONE CAUTION for whoever resumes: do NOT adopt the untracked bytes as evidence the layers compose. Until R3-5 commits, that file is peer WIP in the shared worktree and can still change shape — read it, do not depend on it, and never edit it.",
      "timestamp": "2026-08-28T12:38:52.344Z"
    },
    {
      "id": "comment-be6e4a6a26cf48f8b80ad8af878a8856",
      "author": "worker-f16b412c",
      "content": "UNPARK-PROBE HAZARD — read this before acting on the unpark condition. Measured 2026-08-28 by worker-f16b412c at HEAD e3d20791.\n\nSTATE, measured atomically and authoritatively: `graph-supersede-approval-binding.ts` is STILL UNTRACKED. `git status --porcelain` prints `?? apps/daemon/src/planning/graph-supersede-approval-binding.ts`, `git ls-files` prints nothing, `git log --oneline -3` captured into a variable has `len=0`. R3-5 is at step-5/8. Row stays correctly BLOCKED.\n\nTHE HAZARD, because the obvious one-liner form of this row's own unpark check is INVERTED ON BOTH ARMS and I nearly banked its false positive:\n\n  echo \"committed?=$( [ -n \\\"$(git log --oneline -1 -- $P)\\\" ] && echo YES || echo NO )\"\n\nreports **YES for the UNCOMMITTED file** and **NO for a committed control**. Reproduced side by side in one call. Cause: the escaped quotes do not survive the outer `\"$( )\"`, so the inner output is word-split. With no commits the test degrades to `[ -n ]`, and a one-argument `[` tests whether the string `-n` is non-empty — which it is — so it is TRUE. With a real commit line it becomes `[ -n a1b2c3d some subject ]` -> `[: too many arguments` -> NO. The false-negative arm at least prints a diagnostic to stderr; **the false-positive arm prints nothing at all.**\n\nWHY IT MATTERS HERE SPECIFICALLY: that probe IS this row's unpark condition. Banking its YES would have unparked the row onto untracked peer WIP — exactly the state the block exists to prevent — with no output looking wrong.\n\nUSE THESE INSTEAD, any one of them is a direct answer and none depends on quoting:\n  git ls-files -- <path>                 # empty = not tracked\n  git status --porcelain -- <path>       # ?? = untracked\n  OUT=$(git log --oneline -1 -- <path>); echo \"len=${#OUT}\"    # assign first, then test\n\nAlso noted while re-measuring: HEAD has moved 7f062869 -> e3d20791 since the block was filed (peers landing unrelated work), so any resuming session must re-confirm step 1 clauses (a) and (c) at the then-current HEAD rather than trusting the shas in comment-d62157ba.\n\nDurable copy of the probe hazard: Serena memory `gotcha-nested-quoted-commit-probe-inverts-both-arms`.",
      "timestamp": "2026-08-28T12:43:56.409Z"
    },
    {
      "id": "comment-7c3bd64e952341f99fc1b264825ee01a",
      "author": "worker-f16b412c",
      "content": "RE-MEASUREMENT OF THE UNPARK CLAUSE — worker-f16b412c, HEAD e3d20791, 2026-08-28. Block STANDS. Zero bytes authored.\n\nThe park's unpark condition is one command: `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` must PRINT A COMMIT. Ran it: output is BLANK. `git log --all --oneline -- <path>` is also blank — the path has never been committed on ANY ref. Clause has not cleared.\n\nTWO THINGS CHANGED SINCE THE ORIGINAL BLOCK (measured at HEAD 7f062869), and both matter to whoever measures next:\n\n1. The module now EXISTS in the worktree, as UNTRACKED WIP. The original block reason recorded `git status --porcelain` empty, \"not untracked WIP either\". That is no longer true:\n```\n?? apps/daemon/src/planning/graph-supersede-approval-binding.js\n?? apps/daemon/src/planning/graph-supersede-approval-binding.ts\n```\n`graph-supersede-approval-binding.ts` is 4138 bytes of real content (header doc + `replayGraphRevisionEvents` / `encodeGraphContent` / `SqliteEventStore` imports), with its one-line `.js` bridge alongside. `git ls-files` on it is still EMPTY.\n\nTRAP: a next seat that checks the module with `ls` or `test -f` — the natural reading of DoD clause 1, \"that module must exist\" — will see it and conclude the park cleared. It has not. Untracked WIP is not a landed dependency; it belongs to the producer's uncommitted step and can be swept, reverted or rewritten by that seat at any moment.\n\nSECOND TRAP, on the clause itself: `git log` exits 0 whether or not it prints anything. So `git log ... ; echo $?` reads as SUCCESS on a path with no commits. The signal is the PRINTED LINE, not the exit code. Do not automate this clause on exit status.\n\n2. `graph-supersede-approval-binding.test.ts` STILL DOES NOT EXIST (`ls` fails; `git ls-files` empty). DoD clause 5 / step 5 Leg B names that file inside this row's verification vitest command, so the named verification still cannot reach exit 0 and `complete_task` would be rejected on evidence even if the row were coded. That gate is independent of the module's existence and has not moved at all.\n\nBLOCKER ROW STATE, `task-b54b5609` measured on disk just now: `status: BLOCKED`, `assignedWorkerId: null`, steps 1-4 COMPLETED, step 5 IN_PROGRESS, 6-8 PENDING. It is no longer WORKING as the original block recorded — it stalled mid-step-5 with its deliverable uncommitted, which is precisely why the module is on disk but not in git. The producer is itself parked, so this row cannot clear by waiting on an active seat; nothing is currently driving b54b5609.\n\nUNPARK, unchanged and still the single command: `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` PRINTS a commit line. Everything else in step 1's gate remains green and recorded, so the next session resumes at step 2 with no re-measurement debt.\n\nThe prior seat's standing decision request is also unchanged and still unactioned: step 2 (extract `operatorReviewAuthority` from approve.ts:69-81 into a shared leaf) is behaviour-neutral and cannot red R3-5; only step 3 refuses. Authorising step 2 standalone would allow progress under the park. I did not take it unilaterally for the same reason: step 1(b) forbids authoring any byte, and a leaf with no consumer is dead code.\n\nReleasing the assignment only; BLOCKED and its reason stand.",
      "timestamp": "2026-08-28T13:03:16.020Z"
    },
    {
      "id": "comment-97d3b5bd78074143ad810e199b421b0f",
      "author": "architect-a69740e0",
      "content": "STEP-1(b) GATE CORRECTION — I WROTE A PROBE THAT EXITS 0 ON FAILURE. This comment supersedes step 1(b)'s prerequisite check; `moe.amend_plan_step` refuses on this seat (\"architect or governor role required — worker architect-a69740e0 is not on an architect or governor team\"), so the plan field still carries the defective text. architect-a69740e0, 2026-08-28, HEAD 22951fea.\n\n=== THE DEFECT ===\nMy step 1(b) says: \"REQUIRE, before authoring any byte: `ls apps/daemon/src/planning/graph-supersede-approval-binding.ts` succeeds AND `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` shows a commit.\"\n\n**`git log --oneline -1 -- <path>` prints NOTHING and EXITS 0 when the path is untracked.** Measured just now on that exact file: no output, `exit=0`. A worker checking the exit status of that probe gets a PASS on a file that is in no commit. That is the \"exit 0 is not evidence\" rail, and I wrote a gate vulnerable to it.\n\n=== IT IS NOT HYPOTHETICAL — THAT FILE IS UNTRACKED RIGHT NOW ===\n```\nls  apps/daemon/src/planning/graph-supersede-approval-binding.ts   -> exists, 4138 bytes, mtime 15:37\ngit ls-files --error-unmatch <same path>                            -> error: pathspec ... did not match any file(s) known to git\ngit log --oneline -1 -- <same path>                                 -> (empty), exit 0\n```\nSo R3-5's `matchSupersedeApproval` seam EXISTS ON DISK but is in NO COMMIT. Both halves of my gate as written could be read as passing.\n\n=== THE CORRECTED GATE ===\nReplace the `git log` probe with a check that FAILS LOUDLY:\n- `git ls-files --error-unmatch apps/daemon/src/planning/graph-supersede-approval-binding.ts` — exits **1** with an explicit message when untracked. Unambiguous.\n- or `git cat-file -e HEAD:apps/daemon/src/planning/graph-supersede-approval-binding.ts` — exits non-zero when the path is not in HEAD.\nRun it UNPIPED (a pipe reports the tail's status) and read the OUTPUT, not only the status.\n\n=== AND DISTINGUISH THE TWO FAILURE MODES, because they route differently ===\n- **ABSENT** (no file on disk): R3-5 has not built the seam. Block naming task-b54b5609 and wait.\n- **PRESENT BUT UNTRACKED** (today's actual state): R3-5 HAS built it; the bytes simply are not committed. That is NOT a reason to wait for R3-5 to do more work, and it is NOT yours to commit — the file belongs to task-b54b5609 and epic rail 1 forbids committing another row's paths. Report it as an UNCOMMITTED-DELIVERABLE block naming task-b54b5609 and the exact path, so the routing is \"that row commits its bytes\", not \"that row writes more code\". Those are different asks and a block that conflates them wastes a cycle.\n\n=== WIDER CONTEXT, measured, and it is why this matters beyond one probe ===\nThe worktree at HEAD 22951fea carries **331 untracked paths and 120 modified tracked files, 0 staged**. Seven of the untracked paths are production `.ts`/`.tsx` under apps/ and packages/ — real code no commit contains, including `graph-supersede-approval-binding.ts`, `policy-risk-leg.ts` and `daemon-context-seal-wiring.ts`. governor-0af24e35 reached the same conclusion independently at 16:10Z and it reframes several \"MISSING SYMBOL\" blocks on this board: the symbol is not missing, it is uncommitted.\n\nCONSEQUENCE FOR ANY PREREQUISITE GATE ANYWHERE: a `git grep <symbol> HEAD -- <paths>` probe correctly reports \"not in HEAD\", but a bare `git grep <symbol> -- <paths>` (no HEAD) or a `git log -- <path>` exit-code check will disagree with it, and only one of the three is measuring what the gate means. Say which tree you are anchored at — HEAD, the worktree, or a named SHA — and pick the probe that fails loudly there.\n\nNothing else in this row's plan changes. The ordering constraint stands: task-b54b5609 first, and its bytes must be COMMITTED, not merely present.",
      "timestamp": "2026-08-28T16:12:33.636Z"
    },
    {
      "id": "comment-cc793048b1ed4ab08e6f82c57a5df7e6",
      "author": "worker-8d70fe57",
      "content": "STEP 1 VERIFY-OR-BLOCK — PASSES all five clauses. Re-measured at HEAD aebdb367e0df0cfce1e18143d22c5dd63d42ed01 (planning anchored at 62b4d7f6; HEAD has since moved, premise re-taken at the delivered tree). Read-only; zero bytes authored.\n\nCLAUSE 1 — gap still live.\n`git grep -n -E 'readApprovalPolicySettings|decideApprovalAuthority|operatorReviewAuthority|approvalDelayDisposition|humanReview' HEAD -- apps/daemon/src/daemon-command-graph-supersede.ts`\nEXIT=1, zero hits. File is 78 physical lines. Nothing to adopt; not a duplicate.\n\nCLAUSE 2 — R3-5 prerequisite LANDED with a real consumer edge, not merely present.\n`git ls-files --error-unmatch apps/daemon/src/planning/graph-supersede-approval-binding.ts` -> printed the path, EXIT=0.\n`git log --oneline -1 -- <that path>` -> `38acf9d3 fix(task-b54b5609): bind supersede approval`.\n`git grep -n 'matchSupersedeApproval' HEAD -- apps/daemon/src/planning/graph-supersede-service.ts` ->\n  :40 `import { matchSupersedeApproval } from \"./graph-supersede-approval-binding.js\";`\n  :224 `const unbound = matchSupersedeApproval(store, supersede, facts, input.approval);`\nImported AND called from the production service. The safety ordering this row named is satisfied: the seven-field binding exists before this ingress can authorize a successor.\n\nCLAUSE 3 — shape/reachability, not a symbol-only existence check.\n`git grep -n 'decideGraphSupersedeApproval' HEAD -- apps packages` returns exactly three lines and no more:\n  daemon-command-graph-supersede.ts:54  (definition)\n  daemon-command-graph-edges.ts:19      (import)\n  daemon-command-graph-edges.ts:155     (the one call, `decideGraphSupersedeApproval(facts)`)\nNo new caller; no ownership expansion. Inspected runContextEdge (edges.ts:137-175): it builds `const request = assembleGraphRequest(facts, decidedAt, GRAPH_APPROVAL_INTENT_KEYS)` BEFORE the supersede branch, so `request.decidedAt` is in scope at the call site. Confirmed in daemon-command-graph-contracts.ts that `decidedAt` is a SERVER-ASSEMBLED member (:45 lists it among the reserved keys; :112-126 assembleGraphRequest injects it from replayStableDecidedAt at :95) — the raw caller payload does not supply it. The plan's `decideGraphSupersedeApproval(facts, context.humanReview, request.decidedAt)` is the correct widening; reading facts.envelope.payload[\"decidedAt\"] would be wrong.\n\nCLAUSE 4 — ownership + porcelain on the three owned paths.\n`git status --porcelain -- apps/daemon/src/daemon-command-graph-supersede.ts apps/daemon/src/daemon-command-graph-edges.ts apps/daemon/src/planning/graph-supersede-authority.test.ts` -> EMPTY. All three clean.\nFull plan-field scan over .moe/tasks/*.json for the three basenames, excluding DONE/ARCHIVED, returned exactly two live rows: this row and task-9fb4e53d. Inspected task-9fb4e53d directly rather than trusting the grep: its `affectedFiles` is null and its `planningNotes.keyFiles` is [graph-supersede-approval-binding.ts, bootstrap-policy-authority.ts, bootstrap-policy-authority-reader.ts, approval-policy-digest.ts, policy-risk-leg.ts, graph-supersede-test-fixtures.ts, packages/core/src/policy/approval-validation.ts, admission-gate-resolver.ts] — none of my three. A walk of every string in that JSON located the three basenames ONLY inside comments[0].content, where that row's own author wrote \"SIBLING BOUNDARY: NO COLLISION ... Zero file overlap\", classifying itself as the WRITER/READER surface and this row as the INGRESS. No live foreign claim. Proceeding.\n(Its comment lists five paths for this row from an older plan revision; the approved plan owns three. The two extra — daemon-command-graph-approve.ts and approval-review-authority.ts — were dropped when the six-file extraction was rejected, so the boundary is strictly narrower than the one it cleared.)\n\nCLAUSE 5 — runGraphEdge caller roster and the accepted controls.\n`git grep -ln 'runGraphEdge' -- apps packages` (6 files):\n  apps/daemon/src/daemon-command-graph-edges.ts        (definition)\n  apps/daemon/src/daemon-command-registry.ts           (production caller)\n  apps/daemon/src/daemon-command-graph-edges.test.ts   (drives graph.supersede, 7 mentions)\n  apps/daemon/src/planning/graph-supersede-approval-binding.test.ts (drives graph.supersede, 5 mentions)\n  apps/daemon/src/daemon-command-graph-transport.test.ts\n  apps/daemon/src/planning/expansion-request-production-edge.test.ts\nBoth supersede-driving suites attach a server witness — daemon-command-graph-edges.test.ts:67 and graph-supersede-approval-binding.test.ts:84, both `humanReview: Object.freeze({ principalId: PRINCIPAL })`. Under the default REQUIRE_HUMAN policy they therefore fall through the new witness branch and must stay accepted; that is the regression I will re-run in step 3 and in the final gate. graph-transport and expansion-request-production-edge carry no humanReview and no graph.supersede, so the new layer cannot reach them.\n\nEnv keys confirmed for step 2 stubbing: approval-policy-settings.ts:33-34 export APPROVAL_MODE_ENV_KEY = \"MOE_APPROVAL_MODE\" and SPEED_MODE_DELAY_ENV_KEY = \"MOE_SPEED_MODE_DELAY_MS\"; :79-80 read both off the passed env.",
      "timestamp": "2026-08-29T12:05:59.425Z"
    },
    {
      "id": "comment-bdde88dacc834f43b91849affd5c812f",
      "author": "worker-8d70fe57",
      "content": "STEP 4 EVIDENCE 1/2 — COMMIT, DRILLS, GATES, BASELINE ATTRIBUTION. Durable record; the completion summary is only a pointer here. Adversarial review is in the sibling comment 2/2.\n\nCOMMIT: a3849db9, exactly three paths, `git show --stat`:\n  apps/daemon/src/daemon-command-graph-edges.ts       6 +-\n  apps/daemon/src/daemon-command-graph-supersede.ts  79 ++-\n  apps/daemon/src/planning/graph-supersede-authority.test.ts 213 +++\n  3 files changed, 296 insertions(+), 2 deletions(-)\nMade with `git commit --only -- <the three paths>`. No `git add -A`, no tree stage, no push/merge/reset/stash. After the commit `git status --porcelain` over those three paths is EMPTY, so the committed bytes ARE the gated bytes. ~84 foreign dirty paths were present throughout; none touched.\n\n=== MUTATION DRILLS ===\nPre-drill `git status --porcelain` on the mutated path: ` M apps/daemon/src/daemon-command-graph-supersede.ts` — TRACKED BUT DIRTY with task edits, so `git diff` is worthless as a restore proof (non-empty before, during and after). Backup taken OUT OF TREE to `%TEMP%\\graph-supersede-drill-backup-worker-8d70fe57.ts`, verified equal at capture (7868 bytes both sides, SHA256 B8CA83AF…1461). Every restore proved in a SEPARATE call by SHA256 equality, two nonempty 64-hex digests.\n\nDrill 1 — DoD reachability. Replaced ONLY `decideApprovalAuthority({ gate: null, policy })` with `Object.freeze({ delayMs: 0, grant: null, ok: true as const })`; single occurrence confirmed at line 94.\n  RESULT `Tests 2 failed | 2 passed (4)`. A (:166) and C (:193) RED, both `Error: expected the edge to refuse`; B and D green. Predicted set exactly.\n  Restore SHA256 SRC=BAK=B8CA83AF…1461, EQUAL64 True. Re-run green `Test Files 1 passed (1) / Tests 4 passed (4)`.\n\nDrill 2 — identical-code divergence. Disabled ONLY the delay fence by opaque-false: `approvalDelayDisposition(authority.delayMs) === \"NEVER_RETURNED_BY_THIS_FUNCTION\"` — the call still EXECUTES, which a comment-out would not have proven; single occurrence at line 105.\n  RESULT `Tests 1 failed | 3 passed (4)`, the single failure being C. A, B, D green. This is what separates the two identical APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY outcomes: drill 1 reds A+C, drill 2 reds only C, so C is provably answered by the DELAY fence and not by the policy fence.\n  Restore SHA256 equal, EQUAL64 True. Re-run green 4/4.\n\nDrill 3 — witness fall-through. Made ONLY the humanReview branch condition unsatisfiable (`humanReview?.principalId === \"<never>\"`; the witness is still dereferenced).\n  RESULT `Tests 1 failed | 3 passed (4)`, the single failure being B, and with the EXACT code rather than merely \"threw\":\n    `Error: APPROVAL_HUMAN_REVIEW_REQUIRED: DAEMON_GRAPH_INGRESS (APPROVAL_HUMAN_REVIEW_REQUIRED/APPROVAL_POLICY)`\n    at refuseGraph daemon-command-graph-edges.ts:74 <- runContextEdge :160 <- runGraphEdge :193 <- the arm at :179.\n  A, C, D green. Restore SHA256 equal, EQUAL64 True, NUL_BYTES_LEFT=0. Re-run green 4/4.\n  DRILL-HYGIENE FINDING: the drill string carried a literal NUL byte, so `grep -n` answered `Binary file … matches` and printed NOTHING — read alone that looks like \"mutation absent\". Landing was established by the behavioural red and by `sed -n '96p' | od -c`, not by the grep. Restore verified NUL-free.\n\nNo drill came back green and none produced a different red set, so there is no finding to bank on that axis.\n\n=== GATES (foreground, standalone, unpiped) ===\nLEG A `pnpm --filter @moe/daemon typecheck` -> EXIT 0; execution evidence is the script echo `$ tsc --project tsconfig.json`.\n  DISCLOSED VERBATIM: an earlier run of this same leg at ~15:20 returned EXIT 1 with exactly one error —\n    `src/work/foundation-attempt-contracts.ts(187,7): error TS6133: 'numberOf' is declared but its value is never read.`\n  Attributed and NOT mine: that path is not one of my three; it was already ` M` foreign-dirty before I started; `git show HEAD:…foundation-attempt-contracts.ts | grep numberOf` prints nothing, so the symbol is ABSENT at HEAD and exists only in a peer's uncommitted worktree edit; `grep -c numberOf` over my three files returns 0/0/0; and the leg had been EXIT 0 at 15:11 with these same bytes of mine in place. A peer fixed it within the minute and the re-run returned EXIT 0. Live peer WIP, disclosed rather than hidden and never fabricated green.\n\nLEG B — the row's exact verification command:\n  `pnpm --dir apps/daemon exec vitest run --root . --config package.json src/planning/graph-supersede-authority.test.ts src/daemon-command-graph-edges.test.ts src/planning/graph-supersede-approval-binding.test.ts`\n  EXIT 0, `Test Files  3 passed (3)`, `Tests  79 passed (79)`. Three files executed with a nonzero count line, so not a vacuous filter run.\n\nLEG C `pnpm --filter @moe/daemon test` — PATH-ATTRIBUTED BASELINE, both sides actually measured.\n  (2) DELIVERED TREE: `Test Files 6 failed | 297 passed (303)`, `Tests 21 failed | 6243 passed | 12 todo`. Failing set = {activation/activation-telemetry-launch, activation/activation-run-commit, http/http-listener, work/launch-template-producer, work/foundation-attempt-windows, work/foundation-attempt-service}.\n  (1) MERGE-BASE: measured by reverting my WHOLE diff — both production files restored via `git show HEAD:` and the new test moved out of the tree — confirmed by an EMPTY porcelain over all three paths. `Test Files 8 failed | 294 passed (302)`, `Tests 65 failed | 6195 passed | 12 todo`. Failing set = the six above PLUS {daemon-foundation-verification-command, evidence/foundation-verification-service}.\n  (3) (2) MINUS (1) = EMPTY. No path fails at the delivered tree that did not already fail at the merge-base, so the intersection with my three owned paths is empty. The baseline is strictly WORSE than the delivered tree (65 failing tests vs 21).\n  Restore after the baseline run proven by SHA256 on both production files (EQUAL64 True; supersede B8CA83AF…1461, edges 74303178…B1D0) and the test file re-checked at 213 lines by `wc -l`.\n  The failing set also FLAPS across two identical delivered-tree runs (7 files/22 tests, then 6 files/21), and foundation-attempt-{contracts,service,service.test,store,windows.test}.ts are all foreign-dirty — a peer mid-TDD in a shared checkout. Foreign red, disclosed verbatim, not charged to this row.\n\n  REVERSE DRILL on the one suite that could reach me. Import-closure attribution over all six failing suites (transitive relative imports from each test root) returned NONE for five — activation-telemetry-launch (2 modules), activation-run-commit (8), http-listener (235), launch-template-producer (131), foundation-attempt-windows (193) — none reaches either production file, which is decisive. work/foundation-attempt-service.test.ts returned HIT src/daemon-command-graph-edges.ts (198 modules), so an empty path intersection was NOT treated as sufficient there. Ran that single suite twice, isolated: at PARENT bytes `Tests 78 passed (78)`, at MY bytes `Tests 78 passed (78)` — identical. Its lone full-suite failure (`RELEASE_HANDOFF_SOURCE_FOREIGN@DAEMON_RELEASE_HANDOFF; upstream=FOUNDATION_CONTEXT_READER_BINDING_MISMATCH@FOUNDATION_CONTEXT_READER`) is a whole-suite interaction that reproduces with my diff absent and is unrelated to approval authority; my bytes are not a variable in it.\n\nLINE COUNTS (physical, `wc -l`): daemon-command-graph-supersede.ts 155 (was 78), daemon-command-graph-edges.ts 194 (was 190), graph-supersede-authority.test.ts 213. Both production files well under the 250 target; no split needed. (Note: PowerShell `Measure-Object -Line` reported 185 for the 213-line test file — it undercounts; `wc -l` is the figure quoted.)\n\nOWNED PATHS, complete list: apps/daemon/src/daemon-command-graph-supersede.ts, apps/daemon/src/daemon-command-graph-edges.ts, apps/daemon/src/planning/graph-supersede-authority.test.ts.",
      "timestamp": "2026-08-29T12:25:24.558Z"
    },
    {
      "id": "comment-7030ef5db50b4ff3a477c898c029f055",
      "author": "worker-8d70fe57",
      "content": "STEP 4 EVIDENCE 2/2 — ADVERSARIAL REVIEW. Sibling of comment-bdde88da (commit, drills, gates, baseline).\n\nWITNESS PROVENANCE. daemon-command-registry.ts:200-202 mints `humanReview` as\n  `principal.principalId === operatorPrincipalId ? Object.freeze({ principalId: principal.principalId }) : undefined`\n— from the AUTHENTICATED principal, never from payload bytes, and doubly fenced because OPERATOR_PRINCIPAL_KINDS has already refused every non-operator. Notably the comment at :179 ALREADY names `graph.supersede` beside `graph.approve`: the witness was being assembled and forwarded for this kind all along and was simply never READ on this path. That is exactly the gap this row closes, and it is why no registry change was needed. A caller cannot forge one; it never travels on the payload.\n\nTHE COMMAND ID IS A LABEL, NOT AUTHORITY. `gateId: supersede-review:${commandId}` and `workRef: commandId` only NAME the gate and the work it binds to. Authority comes from `grantHumanAuthority` fed the server witness's principalId, and the verdict is RE-DERIVED by handing the granted gate back to `decideApprovalAuthority`. A caller controlling commandId gains nothing, because it cannot supply a witness at all.\n\nDECIDED-AT IS SERVER-ASSEMBLED AND REPLAY-STABLE. `request.decidedAt` is the `replayStableDecidedAt` value `assembleGraphRequest` injects, built in runContextEdge BEFORE the supersede branch. `facts.envelope.payload[\"decidedAt\"]` is never read — a raw supersede payload has no such member, and reading one would let a caller date its own grant. If it were ever unparseable, `Date.parse` yields NaN and `grantHumanAuthority`'s validMoment check refuses APPROVAL_GRANT_MOMENT_INVALID at the gate layer, forwarded unchanged: fails closed, never opens.\n\nORDERING AND RESIDUE. The sequence sits after the authenticated-actor guard and before `applyApprovalCommand`, so it precedes every reducer and service write. Both refusal arms assert the decision count, all seven tracked aggregate event counts, AND `readEventHorizon()` unchanged — zero residue, not merely \"no decision\".\n\nFENCES INTACT. Actor guard, core reducer call, APPROVE guard and task-b54b5609's seven-field matcher are untouched; the landed peer suites pass 75/75 inside leg B's 79. This row adds a layer and moves none.\n\nREPLAY / DOUBLE-DECIDE. A refusal writes nothing, so a repeat is refused identically with no accumulation. The accepted path is unchanged, and the landed R3-5 replay arm — identical bytes return REPLAYED with the same effectId, decision count unmoved, horizon, active-graph snapshot and slot unmoved — is green in leg B.\n\nCONCURRENCY AND SHARED STATE. The new code is pure. It reads `process.env` per call exactly as graph.approve does, and dereferences only the passed witness. No shared mutable state, no file handles, no sockets, no subprocesses, no check-then-act window, nothing to close on an error path.\n\nNULL / UNDEFINED. `humanReview` is `HumanReviewWitness | undefined` and the branch requires `!== undefined` before dereferencing `witness.principalId`; drill 3 proves that condition is load-bearing rather than decorative. `decideSupersedeAuthority` returns `GraphSupersedeApprovalRefused | null` and the caller tests `!== null`, so there is no truthiness bug on a frozen object (a frozen refusal is truthy, which is precisely why the comparison is explicit).\n\nNO UNADAPTED CALLER. Step 1 measured exactly one call site for `decideGraphSupersedeApproval` across apps and packages, and the widened three-argument signature typechecks at EXIT 0 — which would have failed had another caller existed anywhere in the daemon's closure.\n\nENV HYGIENE. Both APPROVAL_MODE_ENV_KEY and SPEED_MODE_DELAY_ENV_KEY are stubbed on EVERY arm, with `vi.unstubAllEnvs()` and `closeStores()` in afterEach. No ambient board setting can decide an arm, and nothing leaks into neighbouring suites — relevant because the full-suite run shares a process pool.\n\nEMBARRASSMENT TEST. The one thing a cold reviewer would rightly push on is that arms A and C emit an identical code and layer, so the suite could in principle be green while only one mechanism exists. That is answered structurally, not by prose: drill 1 reds A+C, drill 2 reds only C, drill 3 reds only B. Three distinct mechanisms, three distinct red sets, each one degree of freedom apart.\n\nFOLLOW-UP NOT TAKEN (disclosed, not silently dropped). There are now THREE private `operatorReviewAuthority` copies in the daemon — graph-approve.ts, planning-services.ts, and this one. The architect considered and rejected extraction at planning time: moving only one is not de-duplication, and moving all three would widen this adjacent security fix to seven files and unrelated approval.decide regression work. The duplication is deliberate and audited, and consolidating it is a separate row.",
      "timestamp": "2026-08-29T12:25:49.153Z"
    },
    {
      "id": "comment-ad91541e8e1d43db91cd9b6bdf3d02f0",
      "author": "qa-860ef713",
      "content": "QA VERDICT: APPROVE, with one GREEN-DRILL FINDING recorded below that is a coverage gap beyond this row's DoD, not a defect in the delivered code. qa-860ef713, verified at HEAD a3849db9 — which IS the delivered commit, with all three owned paths clean and byte-identical to it.\n\nTHE DoD GATE WENT RED AT REVIEW TIME, AND IT IS NOT THIS ROW'S. I re-ran the row's exact verification command and got EXIT 1, `Test Files 1 failed | 2 passed (3)`, `Tests 1 failed | 79 passed (80)` — against the worker's recorded `3 passed / 79 passed (79)`. Exactly +1 test and exactly that test failing. I did not stop at \"foreign\", I attributed it:\n\n- The failure is `proposeSupersessionPreparation reads every current fact durably (task-32c1ba45) > keeps a real two-lineage preparation PARTIAL until successor content exists`, `AssertionError: expected 1 to be 2`.\n- Vitest reports it under `src/daemon-command-graph-edges.test.ts`, but that string does not exist in that file (483 lines, 22 `it(`, zero hits for \"lineage\"). It lives in `apps/daemon/src/planning/supersession-preparation-service.test.ts`, and it reaches the run because `daemon-command-graph-edges.test.ts:29-30` imports `./planning/supersession-preparation-service.test.js` FOR FIXTURES — importing a test module executes its `describe`/`it` registrations inside the importing file's suite. That is the whole mechanism, and it is worth knowing: this row's gate can be redded by an edit to a file the gate never names.\n- That file is ` M` — UNCOMMITTED peer WIP, +30/-6. The failing arm does NOT exist at HEAD (`git show HEAD:<path> | grep -c` = 0) and DOES exist in the worktree (= 1). It is a peer mid-TDD arm authored after this row's delivery.\n- Standalone, the peer's file fails on its own: `Tests 1 failed | 42 passed (43)`, EXIT 1 — the red does not need this row's test files to appear.\n- DETACHMENT BY IMPORT CLOSURE, not by tree mutation and not by an empty path intersection: transitive relative-import closure from `supersession-preparation-service.test.ts` = 147 modules, and it returns NONE for all three owned paths — `daemon-command-graph-supersede.ts`, `daemon-command-graph-edges.ts`, `graph-supersede-authority.test.ts`. NONE is decisive; the peer's suite cannot reach this row's bytes.\n- Row's own surface, excluding the file that imports the peer's dirty test: `graph-supersede-authority.test.ts` + `graph-supersede-approval-binding.test.ts` = `Test Files 2 passed (2)`, `Tests 15 passed (15)`, EXIT 0.\n\nSo the red post-dates delivery, is uncommitted peer WIP, and has an empty owned-path intersection proven by closure rather than by filename. It does not hold this row.\n\nDoD-2 — I READ THE DIFF AGAINST graph.approve, LINE FOR LINE. `decideSupersedeAuthority` does `readApprovalPolicySettings(process.env)` -> `decideApprovalAuthority({ gate: null, policy })` -> the humanReview fall-through under exactly the same guard (`!decided.ok && code === APPROVAL_HUMAN_REVIEW_REQUIRED && layer === APPROVAL_POLICY && witness !== undefined`) -> `approvalDelayDisposition(...) !== \"IMMEDIATE\"` refuses. That is `daemon-command-graph-approve.ts:125-137` with one documented difference: `gate: null` instead of `run.gate`, because a supersession has no runId and `GraphRequestFacts` carries no ledger. The witness is `context.humanReview` and `decidedAt` is `request.decidedAt` — both server-assembled, neither reachable from the payload, so a caller cannot forge a witness or date its own grant. It runs BEFORE the reducer, and the arms pin zero residue three ways (decision count, per-aggregate event counts, event horizon).\n\nDoD-3 — all three required arms present on `runGraphEdge` with a real `supersedableStore()`: A (REQUIRE_HUMAN, no witness -> refuses), B (witness present -> DECIDED, decisions+1), C (non-IMMEDIATE SPEED delay -> refuses), plus D as the delay-zero control. Each pins code, layer AND refusedBy — `refusal.detail` is `DAEMON_GRAPH_INGRESS (APPROVAL_HUMAN_REVIEW_REQUIRED/APPROVAL_POLICY)`, which carries the refusing site, not just the code. Divergence is real and proven by the worker's own matrix rather than asserted: drill 1 reds A+C, drill 2 (opaque-false on the delay fence only) reds C alone, drill 3 (witness branch unsatisfiable) reds B alone. Two outcomes that share an identical code/layer are therefore distinguished by WHICH fence answers, which is exactly what rail 7(A) asks for.\n\nGATES RE-RUN BY ME: `pnpm --filter @moe/daemon typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` script echo as execution evidence; row arms 4/4 EXIT 0; owned+binding pair 15/15 EXIT 0. Line counts by `wc -l`: supersede 155, edges 194, test 213 — all under 250. Commit is exactly the three owned paths.\n\n=== GREEN-DRILL FINDING (mine; reported per rail 7A, NOT banked as a pass) ===\nThe worker's three drills all vary WHICH MECHANISM refuses. I drilled the other axis the code argues hardest for — the comment says refusals are forwarded \"code and layer UNCHANGED — the policy's own vocabulary, never translated into a local one\". One degree of freedom, at daemon-command-graph-supersede.ts:100:\n\n  `return refused(authority.code, authority.layer, authority.code, authority.layer);`\n  ->`return refused(\"APPROVAL_HUMAN_REVIEW_REQUIRED\", \"APPROVAL_POLICY\", \"APPROVAL_HUMAN_REVIEW_REQUIRED\", \"APPROVAL_POLICY\");`\n\nRESULT: `Test Files 1 passed (1)`, `Tests 4 passed (4)`, EXIT 0. GREEN. A hardcoded forward survives every arm.\n\nThis is not vacuous pedantry, because the authority genuinely can emit other codes: `decidePolicy` returns `APPROVAL_POLICY_DELAY_INVALID` @ APPROVAL_POLICY for a PROCEED_WITHOUT_HUMAN policy with an invalid `delayMs`, and the witness path routes through `checkHumanAuthority`, which can return `APPROVAL_HUMAN_AUTHORITY_REQUIRED`, `APPROVAL_GRANT_MOMENT_INVALID` or `APPROVAL_AUTHORITY_BINDING_MISMATCH` @ the gate layer. All four arms exercise the single configuration where forwarded and hardcoded coincide, so none can tell them apart.\n\nWHY I AM STILL APPROVING. The delivered production code forwards correctly — I read it. DoD-2 requires the INGRESS to refuse with the policy's own code and layer, and it does. DoD-3 enumerates exactly three arms, all present and all divergent. DoD-4's required drill (delete the authority call) reds A+C with the exact code. Rail 7(A) governs which mechanism refuses, not which code is forwarded, and that is satisfied. Rejecting for an arm the DoD never asked for would be moving the bar after delivery. The gap is real and cheap to close — one arm at PROCEED_WITHOUT_HUMAN with an invalid `delayMs` would pin `APPROVAL_POLICY_DELAY_INVALID` and kill this mutant outright — but it is follow-up work, not a defect in what shipped. Flagging for an architect to decide whether it earns a row.\n\nDRILL HYGIENE: backup out-of-tree at %LOCALAPPDATA%\\Temp with a worker-id suffix; restore in a call SEPARATE from the drill; SHA256 b8ca83af667758146a2fff25034c0cd1bee9a2d98ce436c94b20a5060ecc1461 identical before and after and equal to the worker's recorded B8CA83AF…1461; porcelain empty; line 100 confirmed back to the forwarding form; re-run green 4/4; backup deleted and absence confirmed with ls. No peer byte was touched — in particular I did NOT revert or restore the peer's dirty `supersession-preparation-service.test.ts` to attribute its red, which is why the closure method was used instead.\n\nI also confirm the worker's own disclosures check out: the delivered-vs-merge-base failing-set delta is empty and the baseline is strictly worse (65 failing tests vs 21), and their `numberOf` TS6133 disclosure was foreign peer WIP that has since cleared — my typecheck leg is EXIT 0.",
      "timestamp": "2026-08-29T12:33:05.050Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-28T09:34:42.439Z",
  "updatedAt": "2026-08-29T12:33:58.178Z",
  "contextFetchedBy": [
    "worker-4d8ff141",
    "architect-a69740e0",
    "worker-f16b412c",
    "architect-5118c1bc",
    "worker-8d70fe57",
    "qa-860ef713"
  ],
  "metrics": {
    "firstClaimAt": "2026-08-28T12:27:27.129Z",
    "plannedStepCount": 4,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 5,
    "doneAt": "2026-08-29T12:33:58.178Z",
    "wallClockMs": 86791049
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T11:53:27.968Z",
  "planningNotes": {
    "approachesConsidered": "Chosen: a three-file production-reachable slice. Keep the human-review grant helper private to daemon-command-graph-supersede.ts, widen the one ingress call with the server-assembled humanReview witness and replay-stable decidedAt, and prove it through runGraphEdge. This matches the existing architecture: daemon-command-graph-approve.ts and planning/planning-services.ts already each carry a private operatorReviewAuthority with the same audited core sequence.\nRejected: the prior six-file plan extracted only graph.approve's helper into a new shared leaf. Re-measurement found a second existing copy in planning-services.ts; moving only one is not a real de-duplication, while moving both would widen this adjacent security fix to seven files and unrelated approval.decide regression work. Delivery bias favors the smallest working slice; the new local sequence is pinned by production-edge arms and one-degree drills.\nRejected: read decidedAt from facts.envelope.payload. The production supersede payload does not carry decidedAt; assembleGraphRequest injects replayStableDecidedAt and runContextEdge already holds it as request.decidedAt. The ingress therefore receives request.decidedAt explicitly.\nRejected: manufacture or read a durable approval gate. Supersede has no runId and GraphRequestFacts has no ledger/store; decideApprovalAuthority({ gate: null, policy }) deliberately applies the board policy directly. task-9fb4e53d owns the later durable policy-decision subject/digest work and must not be folded into this ingress row.",
    "codebaseInsights": "Re-measured at HEAD 62b4d7f6365d on 2026-08-29. `git grep -n -E 'readApprovalPolicySettings|decideApprovalAuthority|operatorReviewAuthority|approvalDelayDisposition|humanReview' HEAD -- apps/daemon/src/daemon-command-graph-supersede.ts` returned no hits; the file is 78 lines. The gap is still live.\nR3-5 is a landed prerequisite, not worktree-only WIP: `git ls-files --error-unmatch apps/daemon/src/planning/graph-supersede-approval-binding.ts` printed the path and `git log --oneline -1 -- <path>` printed `38acf9d3 fix(task-b54b5609): bind supersede approval`. graph-supersede-service.ts imports and calls matchSupersedeApproval.\nReachability grep found exactly one consumer of the exported signature: daemon-command-graph-edges.ts calls decideGraphSupersedeApproval(facts) at the graph.supersede branch. The approve branch already forwards context.humanReview. No active non-DONE supersede task found by the full Moe plan-field scan claims the three owned paths; all three current paths are porcelain-clean.\nassembleGraphRequest injects decidedAt into GraphCommandRequest after replayStableDecidedAt; the raw edge payload omits it. runContextEdge constructs request before the supersede ingress, so the safe call is decideGraphSupersedeApproval(facts, context.humanReview, request.decidedAt).\nThe daemon package resolves by manifest and filter: apps/daemon/package.json names @moe/daemon and the positive control printed FILTER_EXECUTED:@moe/daemon. The exact focused test command uses pnpm --dir apps/daemon so apps/** tests are actually discovered.",
    "risks": "Default policy is REQUIRE_HUMAN unless MOE_APPROVAL_MODE is exactly SPEED with a valid non-negative delay. Existing edge and R3-5 binding suites carry a server-side humanReview witness, so they should fall through and remain accepted; the new suite must stub and restore both env keys so no ambient value or test leak changes that conclusion.\nREQUIRE_HUMAN/no-witness and SPEED/nonzero-delay intentionally emit the same APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY. They are distinguishable only by configuration and one-degree drills: bypassing policy reds both refusal arms; bypassing only approvalDelayDisposition reds only the delayed arm.\nThe humanReview witness is not payload authority: daemon-command-registry.ts assembles it only when the authenticated principal equals operatorPrincipalId. The local grant labels use the replay-stable envelope commandId only as non-empty HumanAuthorityGate labels; grantHumanAuthority derives authority from the server witness and re-decides it through decideApprovalAuthority.\nMutation restores must preserve pre-drill task bytes, not merely Git HEAD. Back up the exact mutated file under the Windows TEMP directory with the worker id and prove SHA256 equality in a separate post-restore call; git diff alone is insufficient when the tracked file already contains intended task edits.\nSibling task-9fb4e53d is PLANNING with zero steps and owns durable policy-decision writer/reader semantics, not this ingress. If its eventual plan claims daemon-command-graph-supersede.ts, stop and route the collision before editing.",
    "keyFiles": [
      "apps/daemon/src/daemon-command-graph-supersede.ts",
      "apps/daemon/src/daemon-command-graph-edges.ts",
      "apps/daemon/src/planning/graph-supersede-authority.test.ts",
      "apps/daemon/src/daemon-command-graph-approve.ts",
      "apps/daemon/src/planning/planning-services.ts",
      "apps/daemon/src/planning/approval-policy-settings.ts",
      "packages/core/src/planning/approval-policy.ts",
      "packages/core/src/planning/approval-authority.ts"
    ]
  },
  "budget": {
    "wallClockMs": 3600000,
    "warnedAt": "2026-08-29T11:42:15.986Z",
    "escalatedAt": "2026-08-29T11:42:15.986Z"
  },
  "planApprovedAt": "2026-08-29T11:53:30.173Z",
  "workStartedAt": "2026-08-28T12:32:57.968Z",
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorHandoffs": [
    {
      "whatIsDone": "Step 1's read-only gate re-run IN FULL at HEAD e3d20791. Unpark clause re-measured: `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` prints NOTHING; `git log --all` on that path is also blank (never committed on any ref). Blocker row task-b54b5609 re-read from disk. Zero bytes authored, no file in this row's scope touched.",
      "whatRemains": "Steps 2-5 unstarted and must stay unstarted until the leaf LANDS AS A COMMIT. Resume condition, one command whose output flips: `git log --oneline -1 -- apps/daemon/src/planning/graph-supersede-approval-binding.ts` must PRINT a commit line. Rest of step 1's gate is green and recorded, so the next session resumes at step 2 with no re-measurement debt.",
      "pitfalls": "TWO measurement traps, both new since the original block. (1) The module now EXISTS in the worktree as UNTRACKED WIP (`?? ...binding.ts` 4138 bytes + its `.js` bridge), but `git ls-files` is EMPTY. A seat checking DoD clause 1 with `ls`/`test -f` will wrongly conclude the park cleared — untracked WIP is the producer's uncommitted step-5 output, not a landed dependency, and can be swept or rewritten at any moment. (2) `git log` EXITS 0 even when it prints nothing, so gating this clause on `$?` reads as SUCCESS on a path with no commits. The signal is the PRINTED LINE, never the exit code. Separately: `graph-supersede-approval-binding.test.ts` still does not exist at all, and DoD clause 5 / step 5 Leg B names it inside the verification vitest command — so the named verification cannot reach exit 0 and complete_task would be refused on evidence even if this row were fully coded. That gate is independent of the module and has not moved.",
      "openQuestions": "Blocker task-b54b5609 is now BLOCKED and UNASSIGNED itself (steps 1-4 COMPLETED, step 5 IN_PROGRESS), not WORKING as the original block recorded — it stalled mid-step-5 with its deliverable uncommitted, which is exactly why the module sits on disk but not in git. So nothing is currently driving the producer and this row cannot clear by waiting on an active seat. Standing decision request from the prior seat, still unactioned: step 2 (extract operatorReviewAuthority from approve.ts:69-81 into a shared leaf) is behaviour-neutral and CANNOT red R3-5 — only step 3 refuses. Amending the plan to authorise step 2 standalone would permit progress under the park. Not taken unilaterally: step 1(b) forbids authoring any byte, and a leaf with no consumer is dead code.",
      "releasedAt": "2026-08-28T13:03:30.966Z",
      "releasedBy": "worker-f16b412c",
      "reason": "Releasing the ASSIGNMENT only; BLOCKED state and its reason stand. An ASSIGNED blocked row gets demoted by the stale-worker reaper into unclaimable BACKLOG, while an unassigned one survives the sweep. Not idle, not a crash: the approved step-1(b) gate prescribes STOP and it still fires at HEAD e3d20791. Re-measurement in comment-7c3bd64e952341f99fc1b264825ee01a."
    }
  ],
  "reviewStartedAt": "2026-08-29T12:27:20.690Z",
  "verification": {
    "command": "pnpm --dir apps/daemon exec vitest run --root . --config package.json src/planning/graph-supersede-authority.test.ts src/daemon-command-graph-edges.test.ts src/planning/graph-supersede-approval-binding.test.ts",
    "exitCode": 0,
    "outputTail": " RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  3 passed (3)\n      Tests  79 passed (79)\n   Start at  15:26:17\n   Duration  4.30s (transform 6.00s, setup 0ms, import 7.69s, tests 2.77s, environment 0ms)\n\nEXIT=0\n\n(Run fresh against the COMMITTED bytes: `git status --porcelain` over the three owned paths was EMPTY immediately before this run.)",
    "reportedAt": "2026-08-29T12:27:20.690Z"
  },
  "filesModified": [
    "apps/daemon/src/planning/graph-supersede-authority.test.ts",
    "apps/daemon/src/daemon-command-graph-supersede.ts",
    "apps/daemon/src/daemon-command-graph-edges.ts"
  ],
  "completedAt": "2026-08-29T12:33:58.178Z",
  "reviewCompletedAt": "2026-08-29T12:33:58.178Z",
  "reviewSummary": "APPROVED at HEAD a3849db9 (= delivered commit; owned paths clean). One non-blocking GREEN-DRILL FINDING. Full evidence: comment-ad91541e8e1d43db91cd9b6bdf3d02f0.\n\nGATE RED AT REVIEW, NOT THIS ROW'S. Verification command: EXIT 1, 1 failed|79 passed (80) vs recorded 79/79. The failure (task-32c1ba45, \"two-lineage preparation PARTIAL\") is NOT in daemon-command-graph-edges.test.ts — it lives in planning/supersession-preparation-service.test.ts and rides in because edges.test.ts:29-30 IMPORTS that test module for fixtures, registering its suites under the importer. That file is ` M` peer WIP (+30/-6); the arm has 0 hits at HEAD, 1 in worktree, fails standalone (1 failed|42 passed). Detached by import CLOSURE not filename: 147 modules, NONE for all three owned paths. Row surface minus the importing file: 2 files/15 tests EXIT 0.\n\nD2: diff read against graph.approve:125-137 — same policy read, decideApprovalAuthority, humanReview fall-through, delay refusal; only `gate: null` differs (no runId on a supersession), documented. Witness/decidedAt server-assembled, unforgeable from payload; runs before the reducer; zero residue pinned 3 ways.\nD3: arms A/B/C + D control on runGraphEdge with a real supersedableStore(); refusedBy pinned via detail `DAEMON_GRAPH_INGRESS (CODE/LAYER)`; divergence proven by the drill matrix (drill1 reds A+C, drill2 C alone, drill3 B alone).\nD5: `pnpm --filter @moe/daemon typecheck` EXIT 0 with the `$ tsc` echo; arms 4/4 EXIT 0; wc -l 155/194/213 all <250; commit = exactly 3 owned paths.\n\nMY DRILL (forwarding axis, not mechanism): hardcoded the forwarded code/layer at supersede.ts:100 -> 4 passed EXIT 0, GREEN. The authority can also emit APPROVAL_POLICY_DELAY_INVALID and three gate-layer codes, none pinned by any arm. Approving regardless: shipped code forwards correctly and D2/D3/D4 are met as written. Restore proven SHA256 b8ca83af == worker's value, porcelain empty, re-run 4/4, backup deleted."
}