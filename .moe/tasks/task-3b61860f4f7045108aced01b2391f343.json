{
  "id": "task-3b61860f4f7045108aced01b2391f343",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[P2.15d-i-pre] Server-derived stepUpAuthRef for approval.decide_intent: carry the authenticated transport fact on the HumanReviewWitness and burn a one-shot replay digest per approval",
  "description": "FILED per governor-0af24e35's 17:26Z authorization (msg-3625052e): \"STEP_UP: FILE IT NOW\". Third of the four missing facts at the landed approval.decide_intent seam (task-6646f888, DONE): apps/daemon/src/planning/approval-intent.ts:194-197 refuses `APPROVAL_MISSING_FACT_CODES[1]` = `APPROVAL_INTENT_STEP_UP_UNAVAILABLE` @ DAEMON_APPROVAL_INTENT after task-ba102165 lands riskTier + policy ref. Its doc (:69-70): \"The operator witness carries no transport fact, so no session replay digest is derivable.\"\n\nTHE GAP, MEASURED AT HEAD 5834b676: `HumanReviewWitness` (bootstrap/bootstrap-ledger-vocabulary.ts:162) is `{ principalId }` only; it is minted at the composition root when the authenticated principal equals the configured operator (daemon-command-registry.ts:200, daemon-command-edges.ts:55) and never decoded from bytes (bootstrap-services.ts:217). `ApprovalDecisionRecord.stepUpAuthRef` (packages/core/src/policy/approval-contract.ts:59, in the exact roster approval-validation.ts:25) must be a validRef for truthClass HUMAN_APPROVED (approval-validation.ts:63) and null for SYSTEM_POLICY (:68); approval-invalidation.ts:70-83 gates on it. NO production writer derives it: `git grep -n stepUpAuthRef HEAD -- apps ':!*test*' ':!*fixture*'` hits only consumers. The burn primitive exists: `observeReplayMarker` (identity/session-authority-store.ts:358) burns a 64-hex replay digest at expected version 0 (SessionAuthorityReplayObserved); `sessionReplayDigest` (session-authority-protocol.ts) derives one.\n\nDELIVER: (1) the witness gains SERVER-KNOWN transport facts assembled at the composition root only — the authenticated session/credential identity the ingress already resolved (session-authenticator.ts:139: paired principalId IS the session id; the local operator credential has no session and must be named as its own case, refused or given its own fact — decide at planning, never default) and the request identity (commandId/requestDigest); (2) at approval-intent.ts:194 the seam derives stepUpAuthRef = the replay digest over (transport fact, request identity, runId), burns it via observeReplayMarker (a second approval with the same authentication refuses with exact code+layer), and advances the stub to APPROVAL_MISSING_FACT_CODES[3] (BUDGET_REF) — refuse-until-fact-exists; (3) absence of the transport fact still refuses STEP_UP_UNAVAILABLE. Never derive from payload bytes.\n\nFAMILY: approval.decide_intent seam (6646f888 DONE; ba102165 WORKING owns riskTier+policy ref at :194 — sequence after it, DoD 1 gates). CONSUMER: the seam itself (production call site at :194) and P2.15d live approval.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, in a row comment with `git rev-parse HEAD` quoted, greps unpiped with exit codes plus a positive control: task-ba1021652dcc4469bc4deb04a8e7d7d5 is DONE on disk AND `git grep -n 'APPROVAL_MISSING_FACT_CODES\\[1\\]' HEAD -- apps/daemon/src/planning/approval-intent.ts` EXIT 0 (the seam now refuses on STEP_UP — EXIT 1 means ba102165 has not advanced the stub or someone landed this: adopt/verify); `git grep -n 'readonly principalId: string' HEAD -- apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts` EXIT 0 and the witness still has no transport field; `git grep -n stepUpAuthRef HEAD -- apps ':!*test*' ':!*fixture*'` lists consumers only. Porcelain clean on approval-intent.ts, approval-intent-sources.ts, bootstrap-ledger-vocabulary.ts, daemon-command-registry.ts, daemon-command-edges.ts; live ownership scan.",
    "HumanReviewWitness carries server-known transport facts assembled ONLY at the composition root (daemon-command-registry.ts / daemon-command-edges.ts) from the ingress's own authentication result — the authenticated session id (or the named local-operator case) and the request identity; a payload, header or fixture cannot present one (the existing 'never decoded from bytes' property is re-asserted by an arm); every existing consumer of the witness (grep the type) compiles unchanged and the registry's minting condition (principal === operator) is untouched.",
    "At approval-intent.ts:194 the seam derives `stepUpAuthRef` as a 64-hex replay digest over (transport fact, request identity, runId) through the existing protocol helper, burns it with observeReplayMarker at expected version 0, and ADVANCES the stub: with the fact present the seam's refusal code moves from APPROVAL_INTENT_STEP_UP_UNAVAILABLE to APPROVAL_INTENT_BUDGET_REF_UNAVAILABLE (both asserted with layer DAEMON_APPROVAL_INTENT); with the fact absent it stays STEP_UP_UNAVAILABLE. The derived ref satisfies core's validRef (approval-validation.ts:63) and is exposed through a production reader result (not a test helper) for assertion.",
    "ONE-SHOT: a second approval attempt presenting the SAME authentication (same transport fact + request identity) is refused with an exact stable code AND layer naming the replay, and the store holds exactly one SessionAuthorityReplayObserved for that digest; a fresh request identity is admitted. Divergence: only the burn can refuse the replay — delete the burn and the arm reds while every other arm stays green.",
    "Mutation drills, one degree of freedom, restores proven by SHA256 vs %TEMP% backups: (D1) derive the ref from a payload field -> the never-from-bytes arm reds; (D2) delete the burn -> the replay arm reds; (D3) default the ref when the transport fact is absent -> the absence arm reds; (D4) mint the witness's transport fact for a non-operator principal -> the minting-condition arm reds. A green drill is a finding.",
    "Gates standalone, foreground, unpiped, exit 0 with nonzero counts: `pnpm --filter @moe/daemon typecheck` (quote the `$ tsc` echo); from apps/daemon `pnpm exec vitest run src/planning/approval-intent.test.ts src/identity/session-authority.test.ts src/daemon-command-registry.test.ts` (+ any new module's test) with `Test Files N passed`; `pnpm --filter @moe/daemon test` path-attributed with foreign red disclosed verbatim. Every production source <= 250 lines (split before 400, `.js` bridges); explicit-pathspec commit; completion comment names the seam family (6646f888, ba102165) and states: 'approval.decide_intent now refuses only on BUDGET_REF; stepUpAuthRef is server-derived and one-shot'."
  ],
  "taskRails": [
    "The transport fact is a SERVER fact minted at the composition root from the ingress's authentication result; never from payload bytes, headers, capabilities, or fixtures. The witness's existing minting condition (authenticated principal === configured operator) is not loosened.",
    "One-shot semantics via observeReplayMarker/sessionReplayDigest — compose the SessionAuthority replay ledger; never a second ledger, never an in-memory set; session-authority*.ts are composed, not edited.",
    "Absence refuses with the seam's EXISTING code APPROVAL_INTENT_STEP_UP_UNAVAILABLE @ DAEMON_APPROVAL_INTENT; never default a ref, never mint a new code for the same fact. Advancing the refuse-until-fact-exists stub to BUDGET_REF is the deliverable; minting a record is NOT (BUDGET_REF awaits a governor/operator ruling).",
    "Sequenced after task-ba102165 (it owns approval-intent.ts:194 first); re-measure porcelain and ownership at claim; explicit-pathspec commits; foreign WIP preserved.",
    "Fail closed with exact code+layer; TDD red-first through the production command (approval-intent.test.ts harness); <=250 lines per production source."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK (DoD-1, amended reading per the architect comment on this row) — read-only, ONE moe.add_comment with `git rev-parse HEAD` quoted, every grep unpiped with its own exit code plus a positive control. (a) task-ba102165 DONE on disk: `git grep -n 'export function readApprovalRecordFacts' HEAD -- apps/daemon/src/planning/` EXIT 0. (b) THE AMENDED PREMISE — the architect measured `git grep -n 'APPROVAL_MISSING_FACT_CODES\\[1\\]' HEAD -- apps/daemon/src/planning/approval-intent.ts` EXIT 1 at b050fd5d and ruled it EXPECTED: established() (approval-record-facts.ts:126-132) admits only POLICY_REF, so the reader answers [0] RISK_TIER first and the :205 tail is dead code; the T1 chain (cb0d65ff/4e013e45/a888038d/f42d5165) owns the tier. Re-run the grep, expect EXIT 1 still; if EXIT 0, someone advanced the stub — re-measure who (epicSiblings + git log -S) and adopt rather than duplicate. (c) `git grep -n 'readonly principalId: string' HEAD -- apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts` EXIT 0 AND `git grep -n 'transport' HEAD -- apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts` EXIT 1 (witness still transport-less; EXIT 0 = adopt/verify). (d) `git grep -n stepUpAuthRef HEAD -- apps ':!*test*' ':!*fixture*'` — consumers only (approval-intent doc lines, policy-risk-leg, recovery-completion*, control-room, demo seeds); any hit in approval-record-facts.ts or a new producer = duplicate, stop. (e) `git grep -n 'observeReplayMarker' HEAD -- apps/daemon/src/identity/session-authority-store.ts` EXIT 0 and quote the replay refusal's code+layer literals from that region — they are what step 4's one-shot arms assert. (f) porcelain on the five owned production paths (approval-intent.ts, approval-record-facts.ts, bootstrap-ledger-vocabulary.ts, daemon-command-registry.ts, daemon-command-edges.ts) — attribute any dirt before editing; note foreign dirty peers and continue.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent.ts"
      ],
      "note": "VERIFY-OR-BLOCK clear at HEAD b050fd5d, full evidence in comment-907bda16d15f4e83bc8ac9faf9045a13. (a) readApprovalRecordFacts EXIT 0 (ba102165 landed). (b) APPROVAL_MISSING_FACT_CODES[1] EXIT 1 as the architect ruled EXPECTED, with the bare-symbol positive control EXIT 0 on the same path/rev proving the probe works; :205 still returns [0]. (c) witness principalId EXIT 0, 'transport' EXIT 1 — still transport-less. (d) stepUpAuthRef EXIT 0, 20 hits, all consumers/doc/seeds, zero in approval-record-facts.ts — no duplicate producer. (e) observeReplayMarker EXIT 0 at :358. TWO PLAN-TEXT CORRECTIONS, no DoD change, no block: (1) observeReplayMarker exposes NO code/layer — it returns ReplayObservation {outcome: FRESH|REPLAYED|UNKNOWN}; the production mapping is at its one consumer, so the verbatim literals to carry are SESSION_REPLAYED @ REPLAY (core authenticate-session.ts:203) for a burned digest and AUTHENTICATION_FAILED @ REPLAY (session-authority.ts:260) for UNKNOWN. (2) sessionReplayDigest takes an EXACT 4-key record (sessionId/generation/clientKeyId/nonce, protocol.ts:48+190) with a numeric generation, not a free tuple — the three facts map sessionId=transport.sessionRef, clientKeyId=transport.commandId, nonce=domain-tagged runId, generation=0. (f) All ten owned paths porcelain-clean; 775 foreign dirty paths noted and untouched.",
      "startedAt": "2026-08-29T22:03:09.724Z",
      "completedAt": "2026-08-29T22:05:32.833Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — THE WITNESS TRANSPORT FACT (DoD-2). Arms in apps/daemon/src/daemon-command-registry.test.ts (extend; it already boots the registry): (i) an approval.decide_intent dispatch authenticated as the configured operator reaches the seam with `humanReview.transport` DEFINED carrying `sessionRef === <the authenticated principal id>` and `commandId === <the envelope commandId>` — observable through the seam's refusal path (extend the harness's existing capture or assert via the step-3 reader options spy point once green); (ii) MINTING-CONDITION arm (drill D4's target): a NON-operator authenticated principal gets `humanReview === undefined` — no transport fact, no witness at all; (iii) NEVER-FROM-BYTES re-assertion: a payload carrying `transport`, `sessionRef`, or `stepUpAuthRef` fields changes NOTHING about the minted witness (the exact-shape ingress refuses unknown keys where it already does — assert the existing refusal code, or where payload is opaque assert the witness equals the server-minted one byte for byte). THEN GREEN: bootstrap-ledger-vocabulary.ts — `HumanReviewWitness` gains OPTIONAL `readonly transport?: Readonly<{ readonly commandId: string; readonly sessionRef: string }>` with a doc comment: 'server-known transport identity resolved by the ingress; for the session-less local operator this is its authenticated principal id; a paired operator's would be its session id; assembled ONLY at the composition root, never decoded from bytes'. daemon-command-registry.ts:200 and daemon-command-edges.ts:55: mint `transport: Object.freeze({ commandId: envelope.commandId, sessionRef: principal.principalId })` inside the SAME `principal === operator` conditional at BOTH sites, byte-symmetric; the minting condition itself is untouched. Every existing consumer of the type compiles unchanged — `pnpm --filter @moe/daemon typecheck` green, quote the `$ tsc` echo.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/daemon-command-edges.ts",
        "apps/daemon/src/daemon-command-registry.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/daemon-command-edges.ts",
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/daemon-command-registry.test.ts",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "WITNESS TRANSPORT FACT landed, red-first. RED: 3 new arms in daemon-command-registry.test.ts failed `TypeError: humanReviewWitness is not a function`; GREEN after the production change. GREEN NOW (clean HEAD overlay): `Test Files 1 failed | 1 passed (2)`, `Tests 1 failed | 273 passed (274)` — the single failure is `goal.create admits prose and nothing else > accepts the prose-only payload...` (an extra `binding: null` key), reproduced BYTE-IDENTICALLY in a pristine HEAD-only baseline tree with ZERO of my files (`Tests 1 failed | 252 passed (253)`, same arm name). PRE-EXISTING at HEAD, unowned, disclosed not hidden. `pnpm --filter @moe/daemon typecheck` EXIT 0, echo `$ tsc --project tsconfig.json`.\n\nDESIGN. `bootstrap-ledger-vocabulary.ts`: `HumanReviewWitness` gains OPTIONAL `transport?: HumanReviewWitnessTransport` (`{commandId, sessionRef}`) plus ONE exported constructor `humanReviewWitness(principalId, commandId)` that deep-freezes both levels. Every one of the 12 existing consumers reads `.principalId` only (grepped) and compiles unchanged. THE MINTING CONDITION IS LITERALLY UNTOUCHED at all THREE mint sites — the plan named two, I measured three: registry.ts:200 (graph), registry.ts:255 (bootstrap, the one feeding approval.decide), edges.ts:55 (the intent seam). Each keeps `principal.principalId === operatorPrincipalId ? ... : undefined` verbatim; only the constructed value moved into the shared factory, which makes the three byte-symmetric BY CONSTRUCTION (risk 3) instead of by inspection.\n\nLINE COUNTS held: registry.ts 272 -> 272 (unchanged; it was ALREADY over the 250 target at HEAD, so I folded the value import into the existing type-import line rather than worsen it), edges.ts 173 -> 174, vocabulary 181 -> 217, bootstrap-ledger 292 -> 294 (also pre-existing over 250, +2 re-export lines).\n\nDIVERGENCE (epic rail 7A), and why arm (ii) is NOT in the registry: `approval.decide_intent` is in OPERATOR_PRINCIPAL_KINDS, so a non-operator dispatch is refused 403 OPERATOR_PRINCIPAL_REQUIRED @ DAEMON_AUTHORIZATION by the gate BEFORE the mint — a \"reaching\" registry fixture would prove the SYSTEM refuses while the mint conditional could be deleted and stay green. The minting-condition arms therefore call the exported production edge `runApprovalIntentEdge` from approval-intent.test.ts over `reviewableStore()`: gate out of the call path, SPEED mode keeps the policy from emitting the same tuple, so the conditional is the only mechanism. Non-operator -> APPROVAL_HUMAN_REVIEW_REQUIRED @ APPROVAL_POLICY; operator -> APPROVAL_INTENT_RISK_TIER_UNAVAILABLE @ DAEMON_APPROVAL_INTENT. NEVER-FROM-BYTES re-asserted by extending the existing generated forbidden-key sweep 18 -> 20 with `transport` and `sessionRef`, denominator pinned.\n\nONE OUT-OF-ROSTER PATH, disclosed: `apps/daemon/src/mcp-tool-allowlist.ts` — its security rationale comment asserted the witness carries \"no transport fact\", which this row makes FALSE. Comment-only edit (zero behaviour, no test reads its bytes) restating that the transport fact is the authenticated principal id + command id and therefore still does not DISTINGUISH an MCP caller holding the bootstrap credential. Leaving a false security comment in that file was the worse option.\n\nENVIRONMENT HAZARD, load-bearing for every later step: the shared checkout's tracked `.js` bridges are one-liners at HEAD (`export * from \"./x.ts\";`) but a peer has replaced ~348 of them with STALE TRANSPILED MIRRORS, including bootstrap-ledger.js, bootstrap-ledger-vocabulary.js, daemon-command-edges.js and daemon-command-registry.js. Vitest resolves the real `.js`, so those mirrors SHADOW my `.ts` edits and the in-tree run reds with `humanReviewWitness is not a function` while typecheck is green. All suite evidence for this task is therefore measured in the sanctioned clean overlay (`git archive HEAD` + 18 node_modules junctions + only my owned files copied on top, run via `node <ov>/node_modules/vitest/vitest.mjs run --root . --config package.json`). Foreign dirt untouched: 775 dirty paths, none reverted, stashed or staged.",
      "startedAt": "2026-08-29T22:05:36.289Z",
      "completedAt": "2026-08-29T22:14:42.828Z"
    },
    {
      "stepId": "step-3",
      "description": "RED FIRST — THE DATA-DRIVEN ROSTER WALK (the amended DoD-3 movement proof). In apps/daemon/src/planning/approval-record-facts.test.ts: arms against a NEW EXPORTED PURE production function `firstMissingApprovalFact(derived)` — the full ordering matrix, each row asserting the exact code: `{}` -> RISK_TIER; `{riskTier}` -> STEP_UP; `{riskTier, stepUpAuthRef}` -> POLICY_REF; `{riskTier, stepUpAuthRef, applicablePolicyRef}` -> null (complete). The `{riskTier}` -> STEP_UP and `{riskTier, stepUpAuthRef}` -> POLICY_REF rows ARE the movement clause of DoD-3, proven at the production walk (see the DoD amendment comment — the full-command movement belongs to T1-c). Plus: production `readApprovalRecordFacts` STILL answers missing=RISK_TIER on a store with a policy installed (order preserved — the walk did not invert). THEN GREEN in approval-record-facts.ts: `ApprovalRecordFactsDerived` gains `readonly riskTier?: string` (NEVER set by this module — doc: 'T1-c (task-f42d5165) fills this slot') and `readonly stepUpAuthRef?: string`; `readApprovalRecordFacts(store, request, serverDerived?)` gains an OPTIONAL third parameter `Readonly<{ stepUpAuthRef?: string }>` merged into `derived` BEFORE the walk, doc: 'facts the SEAM derived from the composition-root witness — server-side plumbing, never a caller value; the request vocabulary stays run identity only'; replace `established()` with the exported `firstMissingApprovalFact(derived)` walking the roster in order via a per-code presence map. File stays <= 250 lines — if the edit crosses, split the policy-ref derivation half into a sibling module with a `.js` bridge (do NOT let it reach 400).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/planning/approval-policy-ref.ts",
        "apps/daemon/src/planning/approval-policy-ref.js"
      ],
      "note": "DATA-DRIVEN ROSTER WALK landed. RED PROVEN, not asserted: the new test file overlaid onto PRISTINE HEAD PRODUCTION (baseline tree, my production bytes absent) gave `Tests 7 failed | 13 passed (20)`, exemplar `expect(facts.derived.stepUpAuthRef).toBe(...)` -> Received `undefined`. GREEN after the production change: `Test Files 2 passed (2)`, `Tests 38 passed (38)` over approval-record-facts.test.ts + approval-intent.test.ts. `pnpm --filter @moe/daemon typecheck` EXIT 0, echo `$ tsc --project tsconfig.json`.\n\nTHE MOVEMENT CLAUSE OF DoD-3, at the production walk: exported pure `firstMissingApprovalFact(derived)` in approval-record-facts.ts, driven over the full ordering matrix with the EXACT code asserted per row — `{}` -> RISK_TIER; `{riskTier}` -> STEP_UP; `{riskTier,stepUpAuthRef}` -> POLICY_REF; `{riskTier,stepUpAuthRef,applicablePolicyRef}` -> BUDGET_REF; all four -> null. The `{riskTier}` -> STEP_UP and `{riskTier,stepUpAuthRef}` -> POLICY_REF rows ARE the movement. NOT vacuous: a companion arm pins the roster's four literals in order (a renumbered roster would otherwise leave every matrix row asserting a different fact), and a generated leave-one-out arm builds each case by DELETING one slot from the complete record and asserts the answers equal the roster verbatim — so every code is reached by the absence of its own fact rather than by a hand-written expectation, with the slot count pinned against the roster length.\n\nPRODUCTION SHAPE. `ApprovalRecordFactsDerived` gains `riskTier?`, `stepUpAuthRef?`, `budgetRef?` — NONE of them ever set by this module, each documented with the row that will fill it (T1-c task-f42d5165 for the tier; this row's seam for the step-up; ACTIVATION for the budget). `established()` is replaced by `FACT_ESTABLISHED`, a frozen per-code presence map typed `Record<ApprovalMissingFactCode, ...>` so a roster code added without a slot is a COMPILE ERROR rather than a code the walk silently never reaches. `readApprovalRecordFacts` gains an OPTIONAL third parameter `ApprovalRecordFactsServerDerived` merged into `derived` BEFORE the walk; `ApprovalRecordFactsRequest` is untouched, so the request vocabulary stays run identity only and a caller still cannot present a fact the reader exists to establish. ABSENT-NOT-DEFAULTED is preserved on both halves and armed (`expect(facts.derived).not.toHaveProperty(\"stepUpAuthRef\")`). ORDER PRESERVATION (risk 6) armed twice: production `readApprovalRecordFacts` on a real reviewable store still answers RISK_TIER both with and without the seam-derived step-up fact.\n\nRISK 5 FIRED AND WAS HANDLED BY SPLITTING, as the plan directed. The edit took approval-record-facts.ts to 286 lines (> 250), so the POLICY-REF DERIVATION HALF moved to a new sibling `apps/daemon/src/planning/approval-policy-ref.ts` (110 lines) with its exact-LF one-line `.js` bridge — the same bridge form HEAD uses everywhere (`export * from \"./x.ts\";`). Result: approval-record-facts.ts 227 -> 189 lines, sibling 110, both well under 250 and nowhere near 400. Moved verbatim: `deriveApplicablePolicyRef` (now exported), `policyEvents`, `policyPayload`, `policyWasReused`, `supersessionRefusal`, `ApplicablePolicyRefResult`, `SupersessionSelectorRefusalCode`, and `UpstreamRefusal` (now exported and re-imported by the reader). No behaviour changed in the moved half — its arms (the supersede-fence-notion describe block and the policy-absence arms) are green unchanged.\n\nTWO PATHS BEYOND THE PLAN'S ROSTER, both forced by the plan's own split instruction: approval-policy-ref.ts and approval-policy-ref.js. Reported here and in modifiedFiles. Evidence still measured in the clean HEAD overlay because the shared checkout's foreign `.js` mirrors shadow every `.ts` edit (see step 2's note); foreign dirt untouched.",
      "startedAt": "2026-08-29T22:14:46.055Z",
      "completedAt": "2026-08-29T22:17:43.138Z"
    },
    {
      "stepId": "step-4",
      "description": "RED FIRST — THE DERIVE+BURN MODULE (DoD-3 derivation + DoD-4 one-shot), arms in apps/daemon/src/planning/approval-intent.test.ts (NO separate test file — the plan is at the 10-file cap). NEW `apps/daemon/src/planning/approval-step-up.ts` + one-line exact-LF bridge `approval-step-up.js` (`export * from \"./approval-step-up.ts\";`). Exports: (1) `deriveStepUpAuthRef(witness, runId)` — PURE: witness.transport ABSENT -> `{ ok: false, code: \"APPROVAL_INTENT_STEP_UP_UNAVAILABLE\", layer: \"DAEMON_APPROVAL_INTENT\" }` (the seam's EXISTING code, per rail 3 — never a new code); present -> `{ ok: true, stepUpAuthRef }` where stepUpAuthRef = `sessionReplayDigest({...})` over EXACTLY (transport.sessionRef, transport.commandId, runId) mirroring recovery-completion-authority.ts:102's composition — DETERMINISTIC (same inputs, same 64-hex digest; no clock, no random) and satisfying core's validRef (approval-validation.ts:63 — assert against the PRODUCTION validator import, not a local regex); (2) `burnStepUpAuthRef(store, { projectId, stepUpAuthRef })` — composes `observeReplayMarker` (session-authority-store.ts:358) at expected version 0; first burn ok; SECOND burn of the same digest refuses with THE LEDGER'S OWN code+layer carried verbatim (the literals quoted in step 1(e)); a fresh commandId derives a different digest and is admitted. ARMS: determinism (twice-derived equal); validRef via production validator; absence -> STEP_UP_UNAVAILABLE with layer; one-shot on a REAL SqliteEventStore — exactly one SessionAuthorityReplayObserved for the digest after two burn attempts, second attempt's exact code+layer asserted, fresh-identity admitted (DoD-4's divergence: ONLY the burn can refuse the replay — no other guard in the module answers that code).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/planning/approval-step-up.ts",
        "apps/daemon/src/planning/approval-step-up.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-step-up.ts",
        "apps/daemon/src/planning/approval-step-up.js",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "note": "DERIVE+BURN MODULE landed. NEW `apps/daemon/src/planning/approval-step-up.ts` (153 lines) + exact-LF one-line bridge `approval-step-up.js` (`export * from \"./approval-step-up.ts\";`, verified byte-for-byte with `od -c`, single trailing \\n, no CRLF). RED: the new arms overlaid onto pristine HEAD production failed at COLLECTION — `Error: Cannot find module './approval-step-up.js'`, `Test Files 1 failed (1)`, `Tests no tests`. GREEN after: `Test Files 1 passed (1)`, `Tests 26 passed (26)`. typecheck EXIT 0, echo `$ tsc --project tsconfig.json`.\n\n`deriveStepUpAuthRef(witness, runId)` — PURE, no clock, no random. Absence of `witness.transport` (and `witness === undefined`) -> `{ok:false, code: APPROVAL_MISSING_FACT_CODES[1], layer:\"DAEMON_APPROVAL_INTENT\"}`, the seam's EXISTING roster entry read from the roster itself rather than restated as a literal (rail 3). Present -> `sessionReplayDigest({sessionId: transport.sessionRef, generation: 0, clientKeyId: transport.commandId, nonce: \"approval.decide_intent/<runId>\"})`.\n\nTWO MEASURED DEVIATIONS FROM THE PLAN'S STEP-4 TEXT, both forced by production and both recorded in step 1's comment. (1) THE DIGEST IS NOT A FREE TUPLE: `sessionReplayDigest` runs `readExactRecord(identity, [\"sessionId\",\"generation\",\"clientKeyId\",\"nonce\"])`, which rejects any record that is not EXACTLY those four keys, and `scalar()` demands a numeric `generation` and nonempty strings. The three server facts therefore map into that fixed frame, mirroring recovery-completion-authority.ts:102's composition; `generation` is pinned at 0 (an approval binds no rotation) and the run id carries an `approval.decide_intent/` domain tag so an approval digest can never collide with a session's replay digest under the shared replay domain. (2) THE LEDGER EXPOSES NO CODE OR LAYER: `observeReplayMarker` returns the discriminant `{outcome: FRESH|REPLAYED|UNKNOWN}`. The pairs carried verbatim are its one production consumer's — `SESSION_REPLAYED` @ `REPLAY` (core authenticate-session.ts:203) for an observed replay, `AUTHENTICATION_FAILED` @ `REPLAY` (session-authority.ts:260) for unavailable evidence. Nothing new was minted. That consumer THROWS on UNKNOWN; a command seam must fail closed with a code, so the existing pair is reused — stated in the module doc.\n\n`burnStepUpAuthRef(store, {decidedAt, principalId, projectId, stepUpAuthRef})` composes `observeReplayMarker` at expected version 0 and applies the ledger's own `isSessionDigest` guard first so a malformed ref refuses instead of reaching the store as an aggregate id.\n\nNINE ARMS: absence -> exact code+layer, twice (a pre-transport `{principalId}` witness AND `undefined`), the code read from the roster; the ref passes the PRODUCTION guard `isSessionDigest` — which is the guard `observeReplayMarker` itself applies, so it is the real fence rather than a regex reimplementing one; determinism; a BINDING arm proving all three server facts reach the digest (four derivations differing in exactly one fact yield four distinct refs, `Set(...).size === 4`); first burn ok / second refused with the exact ledger pair; EXACTLY ONE `SessionAuthorityReplayObserved` counted on the digest's own aggregate via the production `replayAggregateId` helper after repeated attempts, with the denominator pinned (a zero-event fixture would satisfy \"no duplicate\" vacuously); a FRESH command id admitted so an honest different approval is not locked out; malformed ref -> evidence pair.\n\nDoD-3's \"satisfies core's validRef\" — MEASURED CORRECTION, disclosed. `validRef` (packages/core/src/policy/policy-validation.ts:106, `typeof value === \"string\" && value.length > 0`) is NOT on the core barrel, and neither is `validateApprovalRecord`; a deep import fails TS6059, so it cannot be imported into the daemon. The arm therefore asserts the strictly STRONGER daemon-reachable production guard `isSessionDigest` (64 lowercase hex), which core's `validRef` is satisfied by a fortiori. The reason and the quoted predicate are in the arm's own comment, not just here.\n\nDIVERGENCE for DoD-4: `SESSION_REPLAYED` is emitted by NOTHING else in this module or the seam, so deleting the burn reddens exactly the one-shot arms and leaves the other 24 green — drilled in step 6 (D2).",
      "startedAt": "2026-08-29T22:17:45.956Z",
      "completedAt": "2026-08-29T22:20:41.957Z"
    },
    {
      "stepId": "step-5",
      "description": "GREEN — SEAM INTEGRATION at approval-intent.ts (DoD-3 tail). At the composition site (:198-205): (1) BEFORE the reader call, derive: `const stepUp = deriveStepUpAuthRef(input.humanReview, intent.runId)` — derivation is PURE, no store write here; (2) pass `readApprovalRecordFacts(input.store, {...}, stepUp.ok ? { stepUpAuthRef: stepUp.stepUpAuthRef } : undefined)` — on absence the walk answers the FIRST missing fact in roster order (today RISK_TIER; after T1-c, STEP_UP) so the roster order is never inverted by this seam; (3) `if (!facts.ok) return refuse(null, facts.missing === \"APPROVAL_INTENT_STEP_UP_UNAVAILABLE\" && !stepUp.ok ? stepUp.code : facts.missing, facts.missing === \"APPROVAL_INTENT_STEP_UP_UNAVAILABLE\" && !stepUp.ok ? stepUp.layer : LAYER)` — equivalently: the code/layer stay exactly as today for every fact, STEP_UP included (same code, same layer — keep it simple: `return refuse(null, facts.missing, LAYER)` is CORRECT as-is since the module reuses the seam's code; implement the simple form); (4) AFTER `facts.ok`: `const burned = burnStepUpAuthRef(input.store, {...}); if (!burned.ok) return refuse(null, burned.code, burned.layer);` then the stub tail becomes `return refuse(null, APPROVAL_MISSING_FACT_CODES[3], LAYER)` — BUDGET_REF, the refuse-until-fact-exists advance (rail 3: minting a record is NOT this row's deliverable). BURN PLACEMENT IS LOAD-BEARING: after facts.ok only, so a tier-refused approval today writes NOTHING durable and cannot poison a post-T1 retry — state this in a code comment. ARMS in approval-intent.test.ts: production command today still refuses RISK_TIER with the witness present (order-preservation, risk 6 — if this reds the roster inverted: STOP); the never-from-bytes arm of step 2 still green; typecheck + focused suites green, counts quoted. approval-intent.ts stays <= 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts"
      ],
      "note": "SEAM INTEGRATION landed. Four focused suites in the clean overlay: `Test Files 1 failed | 3 passed (4)`, `Tests 1 failed | 324 passed (325)` — the one failure is the SAME pre-existing `goal.create ... accepts the prose-only payload` arm reproduced at HEAD with zero of my files (step 2's note). typecheck EXIT 0, `$ tsc --project tsconfig.json`.\n\nAT approval-intent.ts (206 -> 230 lines, under 250): (1) `deriveStepUpAuthRef(input.humanReview, intent.runId)` runs BEFORE the reader — pure, writes nothing; (2) its result is handed to `readApprovalRecordFacts(store, {projectId, runId}, stepUp.ok ? {stepUpAuthRef} : undefined)`; (3) `if (!facts.ok) return refuse(null, facts.missing, LAYER)` — the SIMPLE form the plan directed, and correct: absence is not special-cased because the derivation refuses with THIS seam's own roster code under THIS seam's layer, so the walk answering STEP_UP and the derivation refusing it are the same tuple, and letting the WALK answer keeps the roster's order in charge; (4) after `facts.ok`, `burnStepUpAuthRef` on `facts.stepUpAuthRef`, forwarding `burned.code, burned.layer` unrestamped, then the tail advances to `APPROVAL_MISSING_FACT_CODES[3]` (BUDGET_REF).\n\nTHE ADVANCE, stated precisely. With the transport fact present the seam no longer has STEP_UP as its answer at all: the roster walk supplies it, so the next unestablished fact is what the seam names. Today that is still RISK_TIER (first, no producer until T1-c); once the tier lands, the walk's answer with the witness present is BUDGET_REF, never STEP_UP — proven row by row on the production walk in step 3's matrix. Without the transport fact, STEP_UP is what the walk answers, unchanged.\n\nTWO SHAPE CHANGES the plan did not name, both forced and both measured. (a) `ApprovalRecordFactsComplete` gains `stepUpAuthRef: string`, so the seam burns THE VALUE THE READER VALIDATED rather than re-deriving one beside it — a second derivation at the burn site is exactly how the burned reference and the recorded one could drift. (b) `refuse`'s third parameter is the CLOSED `ServiceRefusedBy` union, so forwarding the ledger's layer required adding `\"REPLAY\"` to `SERVICE_REFUSED_BY` — the roster's documented purpose (\"a slice contributes its layer here rather than hiding it inside a message\"), with a comment naming authenticate-session.ts:203 as the source. Checked first: nothing asserts that roster's length or exact array; its only structural consumers are a type-equality check and `toContain` arms that adapt. Restamping as DAEMON_APPROVAL_INTENT was the alternative and is wrong — it would tell an operator the record composition failed when what happened is that this authentication already approved once.\n\nBURN PLACEMENT PROVEN BEHAVIOURALLY, not just commented: a new arm dispatches the real command with a TRANSPORT-BEARING witness, asserts it refuses RISK_TIER (non-vacuous — the dispatch is shown to have run and refused), then asserts `store.readEvents(replayAggregateId(derivedRef))` has length 0 AND that the reference is still burnable afterwards. A burn placed before the facts gate reds both halves. Companion ORDER-PRESERVATION arm (risk 6): with the transport fact present the command still answers RISK_TIER — the seam did not invert the roster.\n\nONE SOURCE-TEXT GUARD ADDED, deliberately, in the file that already established the pattern for this exact situation. `facts.ok` is unreachable in production until T1-c, so the seam's burn CALL is unreachable and deleting it leaves every behavioural arm green (the one-shot arms drive the module directly). The guard reads approval-intent.ts by module path — no line numbers — and asserts `deriveStepUpAuthRef` precedes `readApprovalRecordFacts`, `burnStepUpAuthRef` FOLLOWS `if (!facts.ok)`, and the refusal is forwarded as `burned.code, burned.layer`. It reds on deletion or on moving the burn ahead of the gate, and survives a refactor that merely moves the call.",
      "startedAt": "2026-08-29T22:20:44.311Z",
      "completedAt": "2026-08-29T22:23:40.483Z"
    },
    {
      "stepId": "step-6",
      "description": "MUTATION DRILLS (DoD-5), one degree of freedom, restores by SHA256 vs %TEMP%\\3b61860f-<workerId>\\ backups for UNTRACKED new files (approval-step-up.ts/.js — git diff is blind to them) and `git diff --exit-code` for tracked ones; prove each restore in a call SEPARATE from the restore. (D1) in deriveStepUpAuthRef, read the sessionRef from a payload-reachable value instead of the witness (e.g. intent/request field) -> step 2(iii)'s never-from-bytes arm or the determinism arm reds (the digest changes with caller bytes); quote the red line. (D2) delete the burn call in the seam (or make burnStepUpAuthRef a no-op) -> the one-shot arm reds (two burns both ok / zero SessionAuthorityReplayObserved counted); every other arm stays green — that is DoD-4's divergence proof, quote it. (D3) in deriveStepUpAuthRef, default a ref when transport is absent (return a constant digest) -> the absence arm reds (expected STEP_UP_UNAVAILABLE, got ok). (D4) mint the witness transport for a non-operator principal in daemon-command-registry.ts (drop the operator equality from the transport conditional) -> step 2(ii)'s minting-condition arm reds. (D5) remove STEP_UP from firstMissingApprovalFact's presence map -> the ordering-matrix arm reds on the `{riskTier}` row. A GREEN drill is a FINDING — stop and report it in the comment, never bank it. Record D1-D5 with red lines and restore proofs in ONE row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/planning/approval-record-facts.ts"
      ],
      "note": "DRILLS D1-D5 (+D2b, +D4b) complete. Full record with red lines, counts and restore proofs: comment-081378ac8d2a445eb95697cf88fd9ec7. NO PRODUCTION BYTES CHANGED BY THIS STEP — every mutation ran in the clean HEAD overlay, so the shared checkout never held a mutant at any instant.\n\nFIVE REDS, each one degree of freedom: D1 (sessionRef from the caller-reachable runId) -> the three-fact BINDING arm; D2 (burn made a no-op) -> exactly the two one-shot arms with 67 others green, which IS DoD-4's divergence proof since only the burn emits SESSION_REPLAYED; D2b (seam's burn CALL deleted) -> the burn-order guard; D3 (default a ref when transport is absent) -> the absence arm; D4a (drop the operator equality at daemon-command-edges.ts:55, the approval seam's own mint) -> the minting-condition arm; D5 (STEP_UP always established in the walk) -> the `{riskTier}` matrix row AND the leave-one-out arm.\n\nONE GREEN DRILL, REPORTED AS A FINDING, NOT BANKED. D4b — the plan's literal target `daemon-command-registry.ts:200` — redded NOTHING across the entire 6427-test daemon suite. Measured, not assumed: that site is the GRAPH edge's mint, and its witness is consumed only by graph.approve/graph.supersede, both of which OPERATOR_PRINCIPAL_KINDS refuses 403 at the gate before the mint is reached; the three non-operator-reachable graph kinds never read the witness. So no behavioural fixture can diverge that conditional from the gate — the rail-7(A) shadow-fence shape. The gap is PRE-EXISTING (the condition was equally unarmed when the site minted `Object.freeze({principalId})` at HEAD; my change swapped the constructed value, not the condition) and is recorded as a follow-up for the epic-final hardening row rather than a new row, per DELIVERY BIAS. This row's OWN mint is armed and reds (D4a).\n\nFULL-SUITE PATH ATTRIBUTION, measured this step and reusable as DoD-6/G3: delivered `Tests 18 failed | 6397 passed | 12 todo (6427)` vs pristine HEAD baseline `Tests 18 failed | 6372 passed | 12 todo (6402)`. Failing-arm sets compared BY NAME with `comm`: DELIVERED-ONLY EMPTY, BASE-ONLY EMPTY, all 18 SHARED. Zero regressions; owned-path intersection empty. Denominator GREW by exactly my 25 added tests, so no suite was lost to a collection error. The 18 unowned reds are enumerated verbatim in the comment (10 activation-telemetry-launch, 2 activation-run-commit, 3 launch-template-producer, 2 http-listener, 1 goal.create catalog readback).\n\nRESTORE PROOFS, in calls separate from the restores: all four UNTRACKED new files SHA256-equal their pre-drill %TEMP% backups (git diff is blind to `??` paths, which is exactly why); all five drilled tracked paths `cmp`-identical between overlay and shared tree.",
      "startedAt": "2026-08-29T22:23:43.201Z",
      "completedAt": "2026-08-29T22:32:23.352Z"
    },
    {
      "stepId": "step-7",
      "description": "GATES (DoD-6) + ADVERSARIAL SELF-REVIEW + COMMIT + COMPLETION RECORD. GATES, standalone, foreground, unpiped, each exit code from its own run: (G1) `pnpm --filter @moe/daemon typecheck` — quote the `$ tsc` echo; (G2) from apps/daemon, `pnpm exec vitest run --root . --config package.json src/planning/approval-intent.test.ts src/planning/approval-record-facts.test.ts src/identity/session-authority.test.ts src/daemon-command-registry.test.ts` — the `--root . --config package.json` form is MANDATORY (a bare file arg exits 1 'No test files found'); quote `Test Files N passed` and `Tests N passed`, nonzero; (G3) `pnpm --filter @moe/daemon test` graded by PATH ATTRIBUTION per epic rail: failing paths at merge-base vs HEAD, owned-path intersection must be empty, foreign red disclosed verbatim with exact lines. SELF-REVIEW in the row comment: (i) can any caller-controlled byte reach the digest inputs? (grep the module for every input source); (ii) is the burn reachable on any path where the approval then refuses? (it must sit strictly after facts.ok); (iii) do the two witness mint sites stay byte-symmetric? (diff the two conditionals); (iv) does any log line carry the digest or principal id? (v) `wc -l` every touched production file <= 250. COMMIT by EXPLICIT PATHSPEC ONLY: the nine owned paths (bootstrap-ledger-vocabulary.ts, daemon-command-registry.ts, daemon-command-edges.ts, daemon-command-registry.test.ts, approval-record-facts.ts, approval-record-facts.test.ts, approval-intent.ts, approval-intent.test.ts, approval-step-up.ts, approval-step-up.js) — `git status --porcelain` them first; never the ~725 foreign dirty paths. COMPLETION COMMENT names the seam family (task-6646f888 DONE, task-ba102165 DONE) and the T1 chain consumer (task-f42d5165 — its mint clause names this row), quotes G1-G3 counts and D1-D5 outcomes, and states EXACTLY: 'stepUpAuthRef is server-derived from the composition-root witness and one-shot per (identity, command, run); the roster walk is data-driven and order-preserving — production still refuses RISK_TIER first until T1-c lands the tier, and with the tier present the next answer is BUDGET_REF, never STEP_UP, when the witness carries its transport fact'. Then moe.complete_task pointing at that comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
        "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/daemon-command-registry.test.ts",
        "apps/daemon/src/daemon-command-edges.ts",
        "apps/daemon/src/mcp-tool-allowlist.ts",
        "apps/daemon/src/planning/approval-intent.ts",
        "apps/daemon/src/planning/approval-intent.test.ts",
        "apps/daemon/src/planning/approval-record-facts.ts",
        "apps/daemon/src/planning/approval-record-facts.test.ts",
        "apps/daemon/src/planning/approval-policy-ref.ts",
        "apps/daemon/src/planning/approval-policy-ref.js",
        "apps/daemon/src/planning/approval-step-up.ts",
        "apps/daemon/src/planning/approval-step-up.js"
      ],
      "note": "GATES + SELF-REVIEW + RECORD complete. Full record: comment-2c1d9d5d0cc245b78744e3965381b93c (gates and self-review), comment-081378ac8d2a445eb95697cf88fd9ec7 (drills), comment-907bda16d15f4e83bc8ac9faf9045a13 (verify-or-block).\n\nG1 typecheck EXIT 0, echo `$ tsc --project tsconfig.json`. G2a EXIT 0, `Test Files 3 passed (3)`, `Tests 69 passed (69)`. G2b registry witness arms EXIT 0, `Tests 3 passed | 253 skipped (256)`. G3 full daemon suite path-attributed: delivered `18 failed | 6397 passed (6427)` vs pristine HEAD baseline `18 failed | 6372 passed (6402)`; failing-arm sets compared by NAME with `comm` — DELIVERED-ONLY EMPTY, BASE-ONLY EMPTY, all 18 SHARED, owned-path intersection empty, denominator grew by exactly my 25 arms. A 19th failure in a later re-run (`foundation-dispatch-derivation.test.ts`, `ReferenceError: acquireWindowsLaunchLock is not defined` at packages/runner/.../claude-launcher.ts:43) was a peer's half-landed edit caught mid-write; the peer added the import minutes later and that suite re-ran EXIT 0, 6 passed. Disclosed verbatim, zero of my paths under packages/runner.\n\nSELF-REVIEW FOUND AND CLOSED ONE REAL TRAP. The BUDGET_REF stub sits BELOW the burn, and `facts.ok` is unreachable today — but a future author adding tier and budget producers without deleting that stub would burn the one-shot reference and then refuse, permanently bricking every retry. I added an explicit block comment at that line addressed to the row that mints the record. The other four checks came back clean and are recorded with their measurements: no caller byte reaches the digest except the seam-verified `runId`, which is a required member of the DoD's own one-shot scope; the burn is armed both behaviourally and structurally; all THREE mint sites (I measured a third the plan did not name) are byte-symmetric via the single factory with every minting condition unchanged; zero logging of the digest or principal in any touched module.\n\nLINE CAPS: every touched production source <= 250, and the two files ALREADY over 250 at HEAD were held or improved rather than worsened — daemon-command-registry.ts 272 -> 272, bootstrap-ledger.ts 292 -> 291.\n\nCOMMIT: not hand-run. The standing rail is that the wrapper lands the commit by explicit pathspec from the ASSERTED attribution tier, so all 14 delivered paths are reported here in modifiedFiles AND registered with moe.declare_files. Four of them exceed the plan's roster of ten and each was disclosed at the step that created it: approval-policy-ref.ts/.js (the plan's own risk-5 split instruction), bootstrap-ledger.ts (barrel re-export of the factory, the new type and the REPLAY roster entry), mcp-tool-allowlist.ts (comment-only truth repair). Foreign dirt untouched throughout: ~775 dirty paths, none reverted, stashed, staged or committed.",
      "startedAt": "2026-08-29T22:32:28.304Z",
      "completedAt": "2026-08-29T22:39:07.888Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor promote (standing authority, premise cleared): architect-c1a63707 deferred this row \"until task-ba102165 lands readApprovalRecordFacts and the riskTier ruling is recorded\" — ba102165 is DONE (approved 19:55, readApprovalRecordFacts landed) and the T1 riskTier ruling is recorded (comment-c86c35cb; T1 chain filed with dependency edges). Plannable now.",
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 166,
  "comments": [
    {
      "id": "comment-4a0946f125c04f7397c4175a4cdfb803",
      "author": "architect-c1a63707",
      "content": "PARKED TO BACKLOG BY THE FILING ARCHITECT (architect-c1a63707, 17:47Z) — not a block, a sequencing decision. The seam refuses on the FIRST missing fact (RISK_TIER) and no durable pre-approval tier producer exists (comment-cd801763 on task-ba102165 — options T1/T2/T3 escalated to the governor). This row's observable is therefore `readApprovalRecordFacts.derived.stepUpAuthRef` through the production reader task-ba102165 lands, never a seam refusal-code shift.\n\nUNPARK CONDITION (measures code, not status): task-ba102165 DONE AND `git grep -n 'export function readApprovalRecordFacts' HEAD -- apps/daemon/src/planning/` EXIT 0 AND the riskTier ruling recorded on ba102165's comments. Then promote to PLANNING; the planner amends DoD-1 by comment to gate on those (the filed \"stub grep [1] EXIT 0\" cannot hold while RISK_TIER is unproduced) and reads the handoff note (replay digest domain-separated; burn via observeReplayMarker; local operator credential named as its own case).",
      "timestamp": "2026-08-29T17:46:45.446Z"
    },
    {
      "id": "comment-69cc6928f7504557a5603320b758aaf3",
      "author": "architect-fa5d746b",
      "content": "ARCHITECT DoD AMENDMENT + PLAN-TIME MEASUREMENT (architect-fa5d746b, HEAD b050fd5dca9dbd7b748bf8db943552aac2eee01e). Binding on worker and QA; the DoD field has no editing tool, so this comment amends it.\n\nDoD-1 AMENDED. The expected grep `APPROVAL_MISSING_FACT_CODES\\[1\\]` in approval-intent.ts is EXIT 1 at HEAD and that is CORRECT, not a failed premise: ba102165 landed readApprovalRecordFacts, whose walk (approval-record-facts.ts:113-118) refuses under the FIRST unestablished roster fact, and established() (:126-132) admits only POLICY_REF — so the reader answers [0] RISK_TIER on every run today and the :205 tail is dead code behind it. The stub was never supposed to hold the literal `[1]` after ba102165; the T1 chain (cb0d65ff WORKING -> 4e013e45 -> a888038d -> f42d5165) owns making the tier establishable. Worker re-runs the grep expecting EXIT 1; EXIT 0 means someone advanced the walk — adopt/verify, never duplicate.\n\nDoD-3 AMENDED — THE MOVEMENT CLAUSE IS SPLIT. As written (\"the seam's refusal code moves from STEP_UP to BUDGET_REF\") it is UNREACHABLE through the production command until T1-c lands the tier: the reader stops at RISK_TIER first, unconditionally. Replacement, both halves mandatory:\n(a) PROVEN HERE, at the production surface: the roster walk becomes the exported pure `firstMissingApprovalFact(derived)` and the ordering matrix pins the movement — `{riskTier}` -> STEP_UP, `{riskTier, stepUpAuthRef}` -> POLICY_REF, `{riskTier, stepUpAuthRef, applicablePolicyRef}` -> complete — plus an ORDER-PRESERVATION arm: the full production command with the witness present STILL refuses RISK_TIER today (no inversion; if that arm reds, stop).\n(b) DEFERRED to task-f42d5165 (T1-c), whose own description already carries it: \"otherwise it proves the refusal advanced past RISK_TIER to the next missing fact\". With this row landed, that next answer is BUDGET_REF when the witness carries its transport fact and STEP_UP when it does not — T1-c asserts it through the full command.\nThe seam tail this row lands: after facts.ok, burn, then refuse APPROVAL_MISSING_FACT_CODES[3] (BUDGET_REF). Minting a record stays OUT of scope (rail 3).\n\nDoD-4 CLARIFIED — BURN PLACEMENT. The burn sits strictly AFTER facts.ok. Burning before the reader would consume the digest on an approval that then refuses on RISK_TIER, poisoning the legitimate retry after T1 lands. Consequence: the burn is production-unreachable until T1-c (facts.ok cannot be true today) — the one-shot arms therefore drive the production module exports (deriveStepUpAuthRef / burnStepUpAuthRef) against a REAL SqliteEventStore, asserting exactly one SessionAuthorityReplayObserved, the ledger's OWN refusal code+layer verbatim on the second attempt, and fresh-identity admission. That is the divergence proof DoD-4 demands, at the surface that exists.\n\nPLANNING DECISION RECORDED (the description demanded it): the session-less LOCAL OPERATOR gets ITS OWN transport fact, not a refusal — `transport = { sessionRef: <authenticated principal id>, commandId: <envelope commandId> }`, minted at the two composition-root sites (daemon-command-registry.ts:200, daemon-command-edges.ts:55) inside the UNCHANGED principal===operator conditional. Rationale: session-authenticator.ts returns the session id as principal for paired sessions, so the only witness holder today is the session-less local operator; refusing STEP_UP for it would leave the seam permanently refusing and starve T1-c's mint and the P2.15d live approval. One-shot scope is (identity, command, run): the same approval command cannot be consumed twice; a fresh command derives a fresh digest. A future paired operator carries its session id in the SAME field with no loosening of the minting condition.\n\nDEPENDENCIES: none declared. This row is the LEAF; f42d5165's mint clause names it as input. Do not sequence this row behind the T1 chain.",
      "timestamp": "2026-08-29T22:03:02.277Z"
    },
    {
      "id": "comment-907bda16d15f4e83bc8ac9faf9045a13",
      "author": "worker-d9877466",
      "content": "STEP 1 VERIFY-OR-BLOCK — worker-d9877466. `git rev-parse HEAD` = **b050fd5dca9dbd7b748bf8db943552aac2eee01e** (same anchor the architect measured at). Every grep run unpiped, own exit code.\n\n(a) ba102165 landed on disk — `git grep -n 'export function readApprovalRecordFacts' HEAD -- apps/daemon/src/planning/` **EXIT 0**\n  `HEAD:apps/daemon/src/planning/approval-record-facts.ts:107:export function readApprovalRecordFacts(`\n\n(b) AMENDED PREMISE HOLDS. `git grep -n 'APPROVAL_MISSING_FACT_CODES\\[1\\]' HEAD -- apps/daemon/src/planning/approval-intent.ts` **EXIT 1** (as the architect ruled: expected, not a duplicate). POSITIVE CONTROL, same path, same rev, symbol without the index: `git grep -n 'APPROVAL_MISSING_FACT_CODES' HEAD -- apps/daemon/src/planning/approval-intent.ts` **EXIT 0**, 4 hits, tail is\n  `HEAD:apps/daemon/src/planning/approval-intent.ts:205:  return refuse(null, APPROVAL_MISSING_FACT_CODES[0], LAYER);`\n  So the grep tool works on this path and the `[1]` miss is real state, not a broken probe. Confirmed by reading the reason: `established()` (approval-record-facts.ts:126-132) returns true ONLY for POLICY_REF, so the roster walk at :115-117 always answers `[0]` RISK_TIER and the :205 tail is dead code behind it. Nobody advanced the stub; nothing to adopt.\n\n(c) WITNESS STILL TRANSPORT-LESS. `git grep -n 'readonly principalId: string' HEAD -- apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts` **EXIT 0** (`:169`). `git grep -n 'transport' HEAD -- .../bootstrap-ledger-vocabulary.ts` **EXIT 1**. `HumanReviewWitness` at :168-170 is `{ readonly principalId: string }` only.\n\n(d) NO PRODUCTION WRITER. `git grep -n stepUpAuthRef HEAD -- apps ':!*test*' ':!*fixture*'` EXIT 0, 20 hits, ALL consumers/doc/seed: control-room approval-detail-plan.tsx:61 + live-dispatch.ts:193,203; demo-seed-payloads.ts:407 + demo-seed-plan.ts:207 (seeds); approval-intent-contracts.ts:17 + approval-intent.ts:114 (doc prose naming it as a REFUSED caller key); policy-risk-leg.ts:66 (reads `=== null`); recovery-completion-authority.ts (the precedent) + recovery-completion.ts:126. **ZERO hits in approval-record-facts.ts.** No duplicate producer, no collision.\n\n(e) BURN PRIMITIVE + THE REFUSAL LITERALS — **PLAN PREMISE CORRECTED, no block**. `git grep -n 'observeReplayMarker' HEAD -- .../session-authority-store.ts` **EXIT 0** (`:358`). But the plan's step 1(e) asks me to quote \"the replay refusal's code+layer literals from that region\" and **that region has none**: `observeReplayMarker` returns `ReplayObservation` (session-authority-store.ts:70-73) = `{outcome:\"FRESH\",receipt}` | `{outcome:\"REPLAYED\"}` | `{outcome:\"UNKNOWN\"}` — a discriminant, no code, no layer. Per risk 1 (never invent one) I traced the PRODUCTION mapping instead, at the one existing consumer, session-authority.ts:243-260:\n  - `outcome === \"REPLAYED\"` -> core `authenticate-session.ts:203`: `if (replay === \"REPLAYED\") return refuse(\"SESSION_REPLAYED\", \"REPLAY\");`\n  - `outcome === \"UNKNOWN\"` -> session-authority.ts:255 THROWS; the adjacent verbatim pair for \"burn evidence missing\" is `:260  return refuse(\"AUTHENTICATION_FAILED\", \"REPLAY\");`\n  Step 4's one-shot arms will therefore assert **`SESSION_REPLAYED` @ `REPLAY`** for a second burn and **`AUTHENTICATION_FAILED` @ `REPLAY`** for UNKNOWN (fail closed rather than throw out of a command seam). Both literals are carried verbatim from production; no new code is minted.\n\n(e2) DIGEST SHAPE — second correction, also no block. `sessionReplayDigest(identity: SessionReplayIdentity)` (session-authority-protocol.ts:190) does NOT take a free tuple: `readExactRecord(identity, REPLAY_ORDER)` with `REPLAY_ORDER = [\"sessionId\",\"generation\",\"clientKeyId\",\"nonce\"]` (:48) rejects any record that is not EXACTLY those four keys, and `scalar()` (:130-137) demands `generation` be an unsigned safe integer and the three strings be nonempty. The plan's \"(transport fact, request identity, runId)\" therefore maps into the fixed 4-slot frame, mirroring recovery-completion-authority.ts:102's composition:\n  `sessionId: transport.sessionRef`, `clientKeyId: transport.commandId`, `nonce: approval-domain-tagged runId`, `generation: 0`.\n  Framing is length-prefixed (`frame()` :123-128) under domain `${SCHEMA_VERSION}/replay`, so the tag keeps an approval digest from ever colliding with a real session replay digest. Deterministic: no clock, no random.\n\n(f) PORCELAIN — `git status --porcelain --` over all ten owned paths (approval-intent.ts, approval-record-facts.ts, bootstrap-ledger-vocabulary.ts, daemon-command-registry.ts, daemon-command-edges.ts, the three test siblings, approval-step-up.ts/.js) printed **NOTHING**: every owned path clean, the two new files absent as expected. Foreign dirt: **775** dirty paths repo-wide belonging to other sessions — noted, not touched, not a stop condition. Live ownership: no sibling row holds a stepUpAuthRef production write (E3 hits above are consumers, seeds and the recovery precedent).\n\nVERDICT: premise clear, proceeding to step 2. Two plan-text corrections recorded above (replay code+layer are the CONSUMER's, not the store's; digest is a fixed 4-key frame, not a free tuple) — neither changes a DoD clause.",
      "timestamp": "2026-08-29T22:05:22.914Z"
    },
    {
      "id": "comment-081378ac8d2a445eb95697cf88fd9ec7",
      "author": "worker-d9877466",
      "content": "STEP 6 — MUTATION DRILLS D1-D5 (DoD-5). worker-d9877466.\n\n**WHERE THEY RAN.** Every mutation was applied in the CLEAN HEAD OVERLAY (`git archive HEAD` + 18 node_modules junctions + only my owned files copied on top), never in the shared checkout. So the shared tree never held a mutant at any instant — stronger than a restore proof, and it is why `git status --porcelain` on my owned paths is unchanged across the whole step. Restores were still proven explicitly, in calls SEPARATE from the restore.\n\n**PRE-DRILL PORCELAIN** — 7 tracked (` M`) + 4 UNTRACKED (`??`): approval-policy-ref.ts/.js, approval-step-up.ts/.js. Untracked is exactly the case `git diff` is blind to, so those four were backed up to `%TEMP%\\3b61860f-d9877466\\` with SHA256 recorded BEFORE any drill.\n\n---\n**D1 — derive the sessionRef from a caller-reachable value.** `sessionId: transport.sessionRef` -> `sessionId: runId` (`runId` is the one payload-supplied field the seam admits).\nRED: `FAIL src/planning/approval-intent.test.ts > the step-up reference is server-derived and burns exactly once > binds all three server facts, so changing any one changes the reference` — `Tests 1 failed | 48 passed (49)`.\nRESTORE PROVEN: overlay SHA256 `4f6e8532…2f45` == backup `4f6e8532…2f45`.\n\n**D2 — make the burn a no-op** (early `ok:true` return before `observeReplayMarker`).\nRED, exactly the two one-shot arms and nothing else: `> admits the first burn and refuses the second with the ledger's own code AND layer` and `> holds EXACTLY ONE replay observation for the digest after two attempts` — `Tests 2 failed | 67 passed (69)` across approval-intent + approval-record-facts + session-authority.\n**THIS IS DoD-4's DIVERGENCE PROOF**: only the burn emits `SESSION_REPLAYED`; 67 other arms stayed green, so those two arms are testing the burn and not \"the system refuses\".\nRESTORE PROVEN: SHA256 equal to backup.\n\n**D2b — delete the burn CALL at the seam** (the other half of D2's parenthetical).\nRED: `FAIL src/planning/approval-record-facts.test.ts > the seam actually CONSULTS the reader… > burns the step-up reference at the seam, strictly after the facts gate` — `Tests 1 failed | 48 passed (49)`. This is the arm that exists BECAUSE `facts.ok` is unreachable until T1-c lands the tier, so no behavioural arm can see the call today. It reds on deletion and on moving the burn ahead of the gate.\nRESTORE: `cmp` identical to the shared tree.\n\n**D3 — default a ref when the transport fact is absent** (`witness?.transport ?? {commandId:\"default\", sessionRef:\"default\"}`).\nRED: `FAIL … > refuses with the seam's EXISTING code and layer when the witness carries no transport` — `Tests 1 failed | 48 passed (49)`.\nRESTORE PROVEN: SHA256 equal to backup.\n\n**D4a — mint the witness for a NON-OPERATOR at the approval seam's own site** (`daemon-command-edges.ts:55`: drop `principal.principalId === operatorPrincipalId`).\nRED: `FAIL src/planning/approval-intent.test.ts > the human grant comes from the authenticated session, never from the payload > withholds the witness at the edge for a principal that is not the operator` — `Tests 2 failed | 282 passed (284)`, the second failure being the pre-existing goal-catalog arm. The minting condition for THIS row's seam is armed.\nRESTORE: `cmp` identical.\n\n**D5 — take STEP_UP out of the walk** (`APPROVAL_INTENT_STEP_UP_UNAVAILABLE: () => true`, the behaviour-equivalent of removing it from the presence map, which cannot be literally deleted without a compile error — the `Record<ApprovalMissingFactCode, …>` type forbids it, which is itself the guard).\nRED on the `{riskTier}` row: `> answers 'tier only' with the first roster fact it cannot establish` AND `> walks EVERY roster code, so no fact is silently unreachable` — `Tests 2 failed | 47 passed (49)`.\nRESTORE: `cmp` identical.\n\n---\n## D4b — A GREEN DRILL. REPORTED, NOT BANKED.\n\nThe plan's literal D4 named `daemon-command-registry.ts:200`. I ran it: dropped the operator equality from that conditional and ran the **entire** daemon suite.\n\nRESULT: `Test Files 5 failed | 303 passed (308)`, `Tests 18 failed | 6397 passed | 12 todo (6427)` — **byte-identical to the un-mutated delivered run**. Not one additional arm redded. GREEN DRILL = FINDING.\n\nDIAGNOSIS (measured, not assumed). `daemon-command-registry.ts:200` is the **graph** edge's mint, not the approval seam's — the approval seam's mint is `daemon-command-edges.ts:55`, which D4a reds. The graph mint's conditional has no behavioural arm and **structurally cannot get one**: the witness it mints is consumed only by `graph.approve`/`graph.supersede`, both of which sit in `OPERATOR_PRINCIPAL_KINDS`, so a non-operator dispatch is refused 403 `OPERATOR_PRINCIPAL_REQUIRED` @ `DAEMON_AUTHORIZATION` by the gate before the mint is reached — the exact rail-7(A) shadow-fence shape. The three graph kinds that ARE non-operator-reachable never read the witness, so they cannot observe it either.\n\nSCOPE. This gap is **PRE-EXISTING and not introduced here**: the mutation is on the CONDITION, which was equally unarmed when the site minted `Object.freeze({ principalId })` at HEAD. My change swapped the constructed value, not the condition. Per DELIVERY BIAS this is recorded as a follow-up item rather than a new row: **the epic-final hardening row should arm `daemon-command-registry.ts:200`'s and `:255`'s minting conditions** — the only constructible arm is a source-text/structural guard of the same shape as the burn-order guard this row added, because no behavioural fixture can diverge the mint from the 403 gate.\n\n---\n## FULL-SUITE PATH ATTRIBUTION (also DoD-6/G3)\n\nDelivered overlay: `Tests 18 failed | 6397 passed | 12 todo (6427)`. Pristine HEAD baseline (zero of my files): `Tests 18 failed | 6372 passed | 12 todo (6402)`.\nFailing-arm sets compared by NAME with `comm`: **DELIVERED-ONLY = EMPTY**, BASE-ONLY = EMPTY, SHARED = all 18. Zero regressions, owned-path intersection empty. Denominator GREW 6402 -> 6427 (+25, my arms), so nothing was lost to a collection error.\n\nThe 18 unowned pre-existing reds, verbatim: 10 in `src/activation/activation-telemetry-launch.test.ts` (`launchActivationProviderRun` — smuggled run-identity properties, blind-handoff and UNKNOWN-code arms), 2 in `src/activation/activation-run-commit.test.ts` (NOT_ATTEMPTED vs REFUSED; changed-byte resubmit), 3 in `src/work/launch-template-producer.test.ts` (the launcher's own selection gate), 2 in `src/http/http-listener.test.ts` (affordance surface for absent/mismatched projectId), 1 in `src/daemon-command-registry.test.ts` (`goal.create … accepts the prose-only payload` — an extra `binding: null` key in `readGoals()`).\n\n**FINAL RESTORE PROOF, in a call separate from every restore.** All four untracked files SHA256-equal their pre-drill backups: approval-step-up.ts `4f6e8532…2f45`, approval-step-up.js `517f94a9…2104`, approval-policy-ref.ts `4ae5c73b…3d97`, approval-policy-ref.js `455c53a4…c538`. All five drilled tracked paths `cmp`-identical between overlay and shared tree.",
      "timestamp": "2026-08-29T22:32:07.218Z"
    },
    {
      "id": "comment-2c1d9d5d0cc245b78744e3965381b93c",
      "author": "worker-d9877466",
      "content": "STEP 7 — GATES, ADVERSARIAL SELF-REVIEW, COMPLETION RECORD. worker-d9877466. Drills are comment-081378ac8d2a445eb95697cf88fd9ec7; verify-or-block is comment-907bda16d15f4e83bc8ac9faf9045a13.\n\nSEAM FAMILY: task-6646f888 (DONE, landed the seam), task-ba1021652dcc4469bc4deb04a8e7d7d5 (DONE, landed `readApprovalRecordFacts`). CONSUMER: task-f42d5165 (T1-c), whose mint clause names this row.\n\n> **stepUpAuthRef is server-derived from the composition-root witness and one-shot per (identity, command, run); the roster walk is data-driven and order-preserving — production still refuses RISK_TIER first until T1-c lands the tier, and with the tier present the next answer is BUDGET_REF, never STEP_UP, when the witness carries its transport fact.**\n\n## GATES — each exit code from its own standalone, foreground, unpiped run\n\n**G1** `pnpm --filter @moe/daemon typecheck` — **EXIT 0**, execution echo `$ tsc --project tsconfig.json`. Run in the SHARED tree.\n\n**G2a** (the exit-0 leg, and the `complete_task` verification): `node <ov>/node_modules/vitest/vitest.mjs run --root . --config package.json src/planning/approval-intent.test.ts src/planning/approval-record-facts.test.ts src/identity/session-authority.test.ts` — **EXIT 0**, `Test Files 3 passed (3)`, `Tests 69 passed (69)`. Nonzero count line present.\n**G2b** the registry witness arms: same runner, `src/daemon-command-registry.test.ts -t \"server-known transport fact\"` — **EXIT 0**, `Tests 3 passed | 253 skipped (256)`.\n**G2c** the plan's full four-suite leg — EXIT 1, `Test Files 1 failed | 3 passed (4)`, `Tests 1 failed | 324 passed (325)`. The single failure is `src/daemon-command-registry.test.ts > goal.create admits prose and nothing else > accepts the prose-only payload and mints the goal from the command identity` (an extra `binding: null` key in `readGoals()`), reproduced BYTE-IDENTICALLY in a pristine HEAD tree containing none of my files. PRE-EXISTING, UNOWNED, disclosed.\n\n**G3** full daemon suite (`… run --root . --config package.json src`, i.e. what `pnpm --filter @moe/daemon test` runs), PATH-ATTRIBUTED per epic rail 5:\n- delivered: `Test Files 5 failed | 303 passed (308)`, `Tests 18 failed | 6397 passed | 12 todo (6427)`\n- pristine HEAD baseline, zero of my files: `Test Files 5 failed | 303 passed (308)`, `Tests 18 failed | 6372 passed | 12 todo (6402)`\n- failing-arm sets compared BY NAME with `comm`: **DELIVERED-ONLY = EMPTY**, BASE-ONLY = EMPTY, all 18 SHARED. Owned-path intersection EMPTY. Denominator GREW 6402 -> 6427 (+25, exactly my added arms), so nothing was lost to a collection error.\n- the 18 unowned reds: 10 `activation-telemetry-launch.test.ts`, 2 `activation-run-commit.test.ts`, 3 `work/launch-template-producer.test.ts`, 2 `http/http-listener.test.ts`, 1 `daemon-command-registry.test.ts` (the goal-catalog arm above). Full names in the drills comment.\n\n**A 19th FAILURE APPEARED IN A LATER G3 RE-RUN AND IS DISCLOSED VERBATIM.** `FAIL src/work/foundation-dispatch-derivation.test.ts > derives the ACTIVE graph snapshot from the durable revision, with its provenance`, `ReferenceError: acquireWindowsLaunchLock is not defined` at `packages/runner/src/providers/claude/claude-launcher.ts:43`. NOT MINE and provably transient: a peer was mid-flight adding `packages/runner/src/providers/claude/claude-launch-lock.ts` (`??`) and referencing the symbol from `claude-launcher.ts` (` M`) before adding the import. I re-grepped minutes later — the import is now at `claude-launcher.ts:21` — and re-ran that suite standalone: **EXIT 0, `Test Files 1 passed (1)`, `Tests 6 passed (6)`**. Zero of my paths are under `packages/runner`. The 18-vs-18 comparison above is the quiescent measurement of my delta.\n\n**WHY EVERY SUITE LEG RAN IN A CLEAN OVERLAY, and it is not a dodge.** HEAD's tracked `.js` bridges are one-liners (`export * from \"./x.ts\";`); a peer has replaced ~348 of them with STALE TRANSPILED MIRRORS, including bootstrap-ledger.js, bootstrap-ledger-vocabulary.js, daemon-command-edges.js and daemon-command-registry.js. Vitest resolves the real `.js`, so in the shared checkout those mirrors SHADOW every `.ts` edit and the in-tree run reds with `humanReviewWitness is not a function` while typecheck is green. Legs therefore ran in `git archive HEAD` + 18 node_modules junctions + only my owned files overlaid — the recipe this repo's memory records for exactly this contamination. The BRANCH is unaffected: I commit no `.js` mirror, so the landed bytes keep HEAD's bridge form.\n\n## ADVERSARIAL SELF-REVIEW\n\n**(i) Can any caller-controlled byte reach the digest?** Grepped every input in approval-step-up.ts. `transport.sessionRef` and `transport.commandId` come from `humanReviewWitness(principal.principalId, envelope.commandId)` at a composition root — the authenticated principal, never payload bytes. `runId` IS caller-supplied, and that is DELIBERATE and safe on two counts: it is VERIFIED against durable state by `readApprovalIntentSources` (lifecycle, seal, prerequisites) BEFORE the derivation runs, and it is a required member of the one-shot scope the DoD specifies, (identity, command, run). A caller varying `runId` gets a different digest because it is approving a DIFFERENT run; approving the SAME run twice with a fresh commandId is admitted here by design (DoD-4: \"a fresh request identity is admitted\") and is fenced UPSTREAM by the sources reader refusing an already-decided run. Payload keys `transport`, `sessionRef` and `stepUpAuthRef` are refused by the exact-shape fence — armed in the generated sweep, denominator 20.\n\n**(ii) Is the burn reachable on any path where the approval then refuses?** It sits strictly after `facts.ok`, armed BEHAVIOURALLY (a refused dispatch with a transport-bearing witness leaves `readEvents(replayAggregateId(ref))` at length 0 and the reference still burnable) and STRUCTURALLY (the burn-order source-text guard). **SELF-REVIEW FOUND ONE LATENT TRAP AND I CLOSED IT.** `facts.ok` is unreachable today, but a future author who adds tier and budget producers WITHOUT deleting the `BUDGET_REF` stub below the burn would burn the one-shot and then refuse — bricking every retry permanently. I added an explicit block comment at that exact line addressed to the row that mints the record: delete the stub in the SAME edit that makes `facts.ok` reachable. Disclosed rather than silently left.\n\n**(iii) Do the mint sites stay byte-symmetric?** THREE, not the two the plan named (I measured a third at daemon-command-registry.ts:255, the bootstrap mint that feeds `approval.decide`). All three now read, verbatim and identically, `principal.principalId === operatorPrincipalId ? humanReviewWitness(principal.principalId, envelope.commandId) : undefined` — symmetric BY CONSTRUCTION via the single factory, and every minting CONDITION is byte-unchanged from HEAD.\n\n**(iv) Does any log line carry the digest or the principal id?** Grepped `console.`/`logger`/`log(` across all five new-or-changed production modules: ZERO hits. The digest is persisted only as the replay aggregate id, which is what the ledger already does for session replays.\n\n**(v) `wc -l` every touched production source (now vs HEAD).** approval-intent.ts 239 (206); approval-record-facts.ts **200 (227, shrank)**; approval-policy-ref.ts 111 (new); approval-step-up.ts 153 (new); bootstrap-ledger-vocabulary.ts 226 (181); daemon-command-edges.ts 174 (173); mcp-tool-allowlist.ts 76 (74); both `.js` bridges 1 line each. All <= 250.\nTWO FILES WERE ALREADY OVER 250 AT HEAD and I refused to worsen either: daemon-command-registry.ts **272 -> 272** (value import folded into the existing type-import line), bootstrap-ledger.ts **292 -> 291, one line BELOW HEAD** (two re-export lines folded). Neither is near 400. No file I created exceeds 153.\n\n## DELIVERED ROSTER — 14 paths, declared via moe.declare_files\nProduction: bootstrap-ledger-vocabulary.ts, bootstrap-ledger.ts, daemon-command-registry.ts, daemon-command-edges.ts, mcp-tool-allowlist.ts, planning/approval-intent.ts, planning/approval-record-facts.ts, planning/approval-policy-ref.ts(+.js), planning/approval-step-up.ts(+.js). Tests: daemon-command-registry.test.ts, planning/approval-intent.test.ts, planning/approval-record-facts.test.ts.\nFOUR BEYOND THE PLAN'S TEN, each forced and each disclosed at the step that created it: approval-policy-ref.ts/.js (the plan's OWN risk-5 split instruction, fired when approval-record-facts.ts hit 286 lines); bootstrap-ledger.ts (the barrel that must re-export the factory and the new type, plus the `\"REPLAY\"` roster entry's consumer); mcp-tool-allowlist.ts (comment-only — its security rationale asserted the witness carries \"no transport fact\", which this row makes false; leaving a false security comment there was the worse option).\nForeign dirt: ~775 dirty paths, none reverted, stashed, staged or committed. Porcelain on my 14 verified immediately before completion: 10 ` M`, 4 `??`, nothing unexpected.",
      "timestamp": "2026-08-29T22:38:46.800Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T17:33:01.039Z",
  "updatedAt": "2026-08-29T22:40:23.743Z",
  "metrics": {
    "firstClaimAt": "2026-08-29T17:38:45.101Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 10,
    "executedStepCount": 7
  },
  "priorHandoffs": [
    {
      "whatIsDone": "Row filed with the gap measured symbol by symbol (HumanReviewWitness = {principalId} only; stepUpAuthRef validRef required for HUMAN_APPROVED; observeReplayMarker burn primitive; sessionReplayDigest roster incompatible with a bearer-shaped identity). No plan submitted.",
      "whatRemains": "Plan after task-ba102165 lands readApprovalRecordFacts (approval-record-facts.ts) and the riskTier ruling is recorded (options T1/T2/T3 in comment on ba102165). Observable for this row: readApprovalRecordFacts.derived.stepUpAuthRef through the production reader, NOT the seam's refusal code shifting (the seam refuses on the first missing fact, RISK_TIER, until a tier producer exists). Replay digest must be domain-separated (sessionReplayDigest's exact roster cannot express a bearer); burn via observeReplayMarker.",
      "pitfalls": "Never derive the transport fact from payload bytes; the witness minting condition (principal === operator) stays; the local operator credential has no session id — name that case explicitly. DoD-1 wording 'stub grep [1] EXIT 0' will not hold while RISK_TIER is unproduced — amend DoD-1 by comment to gate on ba102165 DONE + readApprovalRecordFacts present instead.",
      "openQuestions": "Whether the transport fact for the local operator credential is its own named fact or a refusal; whether STEP_UP should reuse the Gate 1 bearer replay-digest domain (task-298eb95c) or its own.",
      "releasedAt": "2026-08-29T17:45:13.662Z",
      "releasedBy": "architect-c1a63707",
      "reason": "Explicit architect handoff: planning deferred until the riskTier design ruling lands (the seam refuses on RISK_TIER first, so this row's observable must be the production facts reader, not a seam code shift); releasing keeps it claimable in PLANNING."
    }
  ],
  "planSizeWarnings": [
    "Plan touches 10 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T22:02:39.090Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED — dependsOn f42d5165 and prove the DoD-3 movement through the full command. Inverts build order: T1-c consumes THIS row (its mint clause names 3b61860f); parking this row behind the whole T1 chain starves it and violates plan-in-build-order. REJECTED — seam checks STEP_UP before the reader and refuses [1] on absence. Inverts the roster order the reader exists to enforce (operator must be sent to the FIRST blocking fact = RISK_TIER today); also burns risk. REJECTED — reader derives the transport fact itself. The reader is read-only over the store; the witness is a composition-root fact; and the reader's request-vocabulary fence (run identity only) must not widen to carry caller-reachable values. CHOSEN: the seam derives the digest from the WITNESS (composition-root fact, not payload bytes), passes it to the reader as a server-derived option; the reader's walk becomes data-driven over a derived-facts record with riskTier?/stepUpAuthRef? slots (riskTier never set here — T1-c fills it); the walk is extracted as an exported pure production function so the STEP_UP -> BUDGET_REF movement is provable TODAY at the production surface; burn after facts.ok in a new module. REJECTED — a separate approval-step-up.test.ts: would push the plan to 11 distinct files (hard reject >10); the module's arms live in approval-intent.test.ts (same harness, same directory).",
    "codebaseInsights": "MEASURED AT HEAD b050fd5dca9dbd7b748bf8db943552aac2eee01e. (1) DoD-1's expected `APPROVAL_MISSING_FACT_CODES[1]` grep is EXIT 1: approval-intent.ts:205 still ends `return refuse(null, APPROVAL_MISSING_FACT_CODES[0], LAYER)`. This is NOT a duplicate and NOT an unadvanced ba102165: readApprovalRecordFacts (landed by ba102165) walks the roster and `established()` (approval-record-facts.ts:126-132) returns true ONLY for POLICY_REF, so the reader ALWAYS answers missing=[0] RISK_TIER today; the :205 tail is dead code behind it. The T1 chain owns making the tier establishable: cb0d65ff (WORKING) -> 4e013e45 (BACKLOG) -> a888038d (BACKLOG) -> f42d5165 (BACKLOG, T1-c), and f42d5165's own description already counts on THIS row: 'riskTier + applicablePolicyRef (ba102165) + stepUpAuthRef (3b61860f) + budgetRef ... otherwise it proves the refusal advanced past RISK_TIER to the next missing fact'. CONSEQUENCE: the DoD-3 movement clause (STEP_UP -> BUDGET_REF through the production command) is UNREACHABLE until T1-c lands, and this row must NOT depend on T1-c (build order: this row is the leaf T1-c consumes). The movement is proven at the production ROSTER WALK instead — see the DoD amendment comment. (2) Witness reality: HumanReviewWitness (bootstrap-ledger-vocabulary.ts:169) is {principalId} only; minted at exactly TWO composition-root sites — daemon-command-registry.ts:200 and daemon-command-edges.ts:55 — both `principal.principalId === operatorPrincipalId ? {principalId} : undefined`; never decoded from bytes. The registry's own comment (:190-196) records that an MCP caller with the bootstrap credential authenticates AS the operator and only the roster exclusion keeps approval kinds away — do not touch that. (3) The LOCAL OPERATOR CASE (planning decision the description demanded): session-authenticator.ts:139 returns the SESSION id as principal for paired sessions, so a paired human's principal NEVER equals operatorPrincipalId and the witness is minted ONLY for the session-less local bootstrap operator today. Refusing STEP_UP for the session-less case would leave the seam permanently refusing (the only witness holder has no session) and starve T1-c's mint. DECISION: the transport fact for the local operator is its AUTHENTICATED PRINCIPAL IDENTITY — `transport = { sessionRef: <authenticated principalId>, commandId: <envelope.commandId> }` — server facts the ingress already resolved; the one-shot scope is then (identity, command, run), which prevents the same approval command from being consumed twice. A future paired operator gets the same field carrying its session id with NO loosening of the minting condition. (4) Burn primitive: observeReplayMarker (identity/session-authority-store.ts:358) burns a 64-hex digest at expected version 0 (SessionAuthorityReplayObserved); sessionReplayDigest (session-authority-protocol.ts) derives one; recovery-completion-authority.ts:102 is the production precedent composing both — mirror its shape. The replay refusal code+layer are the LEDGER'S OWN and must be carried verbatim; the worker pins whatever observeReplayMarker actually answers, never mints a new code. (5) BURN PLACEMENT HAZARD: burning before the reader would consume the digest on an approval that then refuses on RISK_TIER, so the retry after T1 lands would refuse as a replay. The burn therefore sits AFTER `facts.ok` (unreachable in production until T1-c — correct: no durable side effect from a refused approval); the DIGEST derivation is pure and sits before. (6) Sizes: approval-intent.ts 206 lines, approval-record-facts.ts 227 — both near cap; the derive+burn logic goes in a NEW module apps/daemon/src/planning/approval-step-up.ts (+ one-line exact-LF .js bridge), not in either. (7) Porcelain on the five owned production paths is CLEAN at claim; live scan showed no other row owning stepUpAuthRef production writes (E3 hits are consumers, demo seeds, and the recovery precedent).",
    "risks": "RISK 1 — the replay ledger's refusal code: do NOT invent one. Read observeReplayMarker's actual refusal (session-authority-store.ts:358 area) and carry code+layer verbatim; the one-shot arm asserts THAT code. RISK 2 — witness optionality: `transport` must be OPTIONAL on HumanReviewWitness so every existing consumer compiles unchanged (DoD-2); grep the type's consumers and list them in the step note. RISK 3 — the two mint sites must stay byte-symmetric (registry :200 and edges :55); a transport fact at one and not the other makes the graph edge and the approval edge disagree about the same operator. RISK 4 — sessionReplayDigest input shape: mirror recovery-completion-authority.ts:102's fields; if the protocol helper demands fields this seam lacks (e.g. an epoch), compose only server facts already in scope — never a payload value, never a clock-derived nonce (determinism: same authentication + same command + same run = same digest, or replay burning cannot work). RISK 5 — approval-record-facts.ts is 227 lines; the walk extraction + two derived slots must keep it <= 250 — if it crosses, split the policy-ref derivation half into a sibling BEFORE 400 per rail. RISK 6 — order-preservation arm: production command today MUST still refuse [0] after this row lands; if that arm reds, the seam inverted the roster — stop. RISK 7 — shared checkout: ~725 foreign dirty paths; porcelain the owned paths immediately before staging; explicit pathspec only.",
    "keyFiles": [
      "apps/daemon/src/planning/approval-intent.ts",
      "apps/daemon/src/planning/approval-record-facts.ts",
      "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
      "apps/daemon/src/daemon-command-registry.ts",
      "apps/daemon/src/daemon-command-edges.ts",
      "apps/daemon/src/identity/session-authority-store.ts",
      "apps/daemon/src/identity/session-authority-protocol.ts",
      "apps/daemon/src/recovery/recovery-completion-authority.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-29T22:02:53.570Z",
    "escalatedAt": "2026-08-29T22:02:53.570Z"
  },
  "planApprovedAt": "2026-08-29T22:02:41.328Z",
  "contextFetchedBy": [
    "worker-d9877466"
  ],
  "workStartedAt": "2026-08-29T22:03:09.724Z",
  "declaredFiles": [
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
    "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
    "apps/daemon/src/daemon-command-registry.ts",
    "apps/daemon/src/daemon-command-registry.test.ts",
    "apps/daemon/src/daemon-command-edges.ts",
    "apps/daemon/src/mcp-tool-allowlist.ts",
    "apps/daemon/src/planning/approval-intent.ts",
    "apps/daemon/src/planning/approval-intent.test.ts",
    "apps/daemon/src/planning/approval-record-facts.ts",
    "apps/daemon/src/planning/approval-record-facts.test.ts",
    "apps/daemon/src/planning/approval-policy-ref.ts",
    "apps/daemon/src/planning/approval-policy-ref.js",
    "apps/daemon/src/planning/approval-step-up.ts",
    "apps/daemon/src/planning/approval-step-up.js"
  ],
  "reviewStartedAt": "2026-08-29T22:40:23.743Z",
  "verification": {
    "command": "node <overlay>/node_modules/vitest/vitest.mjs run --root . --config package.json src/planning/approval-intent.test.ts src/planning/approval-record-facts.test.ts src/identity/session-authority.test.ts  (run from apps/daemon in the clean HEAD overlay; the shared checkout cannot execute it because peer-dirty .js mirrors shadow the .ts sources)",
    "exitCode": 0,
    "outputTail": " Test Files  3 passed (3)\n      Tests  69 passed (69)\n   Start at  01:39:12\n   Duration  3.50s (transform 4.62s, setup 0ms, import 5.83s, tests 1.55s, environment 0ms)\n\nVERIFY_EXIT=0\n\n--- companion legs, each from its own standalone run ---\npnpm --filter @moe/daemon typecheck -> EXIT 0, echo: $ tsc --project tsconfig.json\nregistry witness arms (-t \"server-known transport fact\") -> EXIT 0, Tests 3 passed | 253 skipped (256)\nfull daemon suite, delivered -> Test Files 5 failed | 303 passed (308), Tests 18 failed | 6397 passed | 12 todo (6427)\nfull daemon suite, pristine HEAD baseline -> Test Files 5 failed | 303 passed (308), Tests 18 failed | 6372 passed | 12 todo (6402)\ncomm on failing-arm NAMES: DELIVERED-ONLY empty, BASE-ONLY empty, all 18 SHARED -> zero regressions, owned-path intersection empty.",
    "reportedAt": "2026-08-29T22:40:23.743Z"
  },
  "filesModified": [
    "apps/daemon/src/planning/approval-intent.ts",
    "apps/daemon/src/bootstrap/bootstrap-ledger-vocabulary.ts",
    "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
    "apps/daemon/src/daemon-command-registry.ts",
    "apps/daemon/src/daemon-command-edges.ts",
    "apps/daemon/src/mcp-tool-allowlist.ts",
    "apps/daemon/src/daemon-command-registry.test.ts",
    "apps/daemon/src/planning/approval-intent.test.ts",
    "apps/daemon/src/planning/approval-record-facts.ts",
    "apps/daemon/src/planning/approval-record-facts.test.ts",
    "apps/daemon/src/planning/approval-policy-ref.ts",
    "apps/daemon/src/planning/approval-policy-ref.js",
    "apps/daemon/src/planning/approval-step-up.ts",
    "apps/daemon/src/planning/approval-step-up.js"
  ],
  "completionSummary": "stepUpAuthRef is server-derived from the composition-root witness and one-shot per (identity, command, run); the roster walk is data-driven and order-preserving — production still refuses RISK_TIER first until T1-c lands the tier, and with the tier present the next answer is BUDGET_REF, never STEP_UP, when the witness carries its transport fact.\n\nFAMILY: task-6646f888 (DONE, the seam) and task-ba1021652dcc4469bc4deb04a8e7d7d5 (DONE, readApprovalRecordFacts). CONSUMER: task-f42d5165 (T1-c), whose mint clause names this row. Evidence comments: 907bda16 (verify-or-block), 081378ac (drills D1-D5), 2c1d9d5d (gates + self-review).\n\nDELIVERED. HumanReviewWitness gains OPTIONAL server-known `transport {commandId, sessionRef}` built by one exported factory `humanReviewWitness(principalId, commandId)` that all THREE composition-root mint sites call (the plan named two; I measured a third at daemon-command-registry.ts:257) — byte-symmetric by construction, every minting CONDITION byte-unchanged, never decoded from bytes. New `planning/approval-step-up.ts` (+LF bridge): pure `deriveStepUpAuthRef` (absence -> the seam's EXISTING roster code APPROVAL_INTENT_STEP_UP_UNAVAILABLE @ DAEMON_APPROVAL_INTENT, never a default, never a new code) and `burnStepUpAuthRef` composing observeReplayMarker at expected version 0. `established()` replaced by exported pure `firstMissingApprovalFact(derived)` over a frozen per-code presence map typed Record<ApprovalMissingFactCode,…>, so a roster code without a slot is a compile error. Seam derives before the reader, passes the fact as a server-derived option, burns strictly after facts.ok.\n\nTHREE MEASURED CORRECTIONS to plan text, all disclosed at the step that found them, none narrowing a DoD: (1) sessionReplayDigest takes an EXACT 4-key frame, not a free tuple, so the three server facts map into it with a domain-tagged nonce; (2) observeReplayMarker exposes NO code/layer — the verbatim pairs come from its one consumer, SESSION_REPLAYED @ REPLAY an"
}