{
  "id": "task-33ceae56edc348e9864bc592430fa1d0",
  "epicId": "epic-af14d73585e14c0da0264d48c37587c3",
  "title": "Approve the working preview in the browser, and reject it into rework",
  "description": "SPIDR child 3 of task-f3edb8fb39ad42ce8980dae7d62b604c (architect-20a6d1eb, 2026-09-05). dependsOn child 1 (task-40e52fa8a85a4921b97bee933b00403f, the kind) and child 2 (task-f5a74c23f8754665ab9d36cba386e1d0, the receipt this UI renders). This is the PROOF row for the Gate 2 feature: it owns the browser journey, the live drive, the docs sweep and the adversarial pass, so neither sibling files its own evidence row (global rail 21).\n\nWHAT THIS ROW OWNS: the operator's side of Gate 2. A Needs-you card 'Approve working preview' carrying the loopback URL (opening in a new tab), the captured screenshots inline, and Approve / Reject-with-findings. The goal status strip gains a PREVIEW stage. Runs shows the preview receipt per goal. REJECT moves the named nodes to REWORK on the board with the findings text visible.\n\nMEASURED AT HEAD df5bbfd5 (branch moe/work-2026-09-04) by architect-20a6d1eb - every pattern this row needs already exists, so it composes rather than invents:\n- The Needs-you card + offer-wire pattern: `apps/control-room/src/v2/approvals/needs-you.tsx` and `offer-wire.ts` (`spendOffer`), with `escalation-port.ts` as the closest working example of a human decision reaching the daemon.\n- The browser-side roster a new decide kind touches, traced from `escalation.decide`: live/live-activity.ts, v2/approvals/needs-you-model.ts, v2/goals/goal-status.ts, v2/goals/work-labels.ts, v2/ops/activity-words.ts.\n- The board's REWORK column already exists: `apps/control-room/src/v2/board/board-columns.ts:20` BOARD_COLUMNS includes REWORK, and :47 routes a READY node with prior review rounds there. Rejecting into rework should reuse that column, not add one.\n- The Playwright lane: `pnpm test:e2e:browser` = `tsc -p tests/e2e/control-room/tsconfig.json && playwright test -c tests/e2e/control-room/playwright.config.ts`; existing specs under tests/e2e/control-room/ drive a REAL daemon (daemon-board.spec.ts, prd-to-approval.spec.ts).\n\nWORDS ARE THE PRODUCT HERE. Gate 2's question to the human is 'Is this product good enough and visually correct?' - the card must ask that, not surface a receipt dump. Refusal words render verbatim from the daemon's codes.",
  "definitionOfDone": [
    "The Needs-you card renders for a goal with a STARTED preview receipt: the loopback URL as a link opening in a new tab, the captured screenshots inline, and both Approve and Reject-with-findings controls. A control-room test asserts all three are present and that the card is ABSENT when no preview receipt exists.",
    "APPROVE commits a durable decision that is read back from `/activity/read` carrying its verdict - assert the verdict value from the activity read, not from local component state, so a click that never reached the daemon cannot pass.",
    "REJECT with findings moves the named node to REWORK on the board with the findings text visible. Assert the node's column is REWORK (reusing the existing BOARD_COLUMNS entry, board-columns.ts:20) and that the findings text renders - a rejection whose reason the operator cannot read is the defect this clause exists to prevent.",
    "The goal status strip shows a PREVIEW stage while a preview is running, and the four refusal codes render as operator words, verbatim from the daemon: PREVIEW_COMMAND_MISSING, PREVIEW_START_TIMEOUT, PREVIEW_GOAL_NOT_LANDED, PREVIEW_DECISION_INVALID. Assert the rendered sentence for at least PREVIEW_COMMAND_MISSING and PREVIEW_START_TIMEOUT, and that an unknown code still renders verbatim rather than blank.",
    "A Playwright journey under tests/e2e/control-room/ against a REAL daemon covers start -> card -> approve, with the `pnpm test:e2e:browser` count line quoted. No fixture-backed substitute (parent rail 4).",
    "LIVE DRIVE: a preview started for UnAI (D:/projexts/UnAI) or a fresh scaffold app, with a real screenshot (path + sha256) and the committed decision recorded in a row comment under ~4500 chars. README's 'no working preview' clause is rewritten to the measured truth; `git grep -n 'no working preview'` returns 0 hits. If the live drive did not happen, say so plainly rather than implying it did.",
    "Gates quoted with their count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e:browser`."
  ],
  "taskRails": [
    "THIS ROW PROVES, IT DOES NOT REBUILD. If the card cannot render because child 2's receipt is missing a member, comment on THAT row and report blocked on it - do not add a daemon field here (global rail 4: a proof that repairs its own subject retires the question unanswered).",
    "REUSE THE EXISTING PATTERNS: offer-wire.ts `spendOffer` for the decision, the existing REWORK column for rejection, needs-you.tsx for the card. A second decision wire or a new board column is a design regression, not a feature.",
    "WORDS FOR HUMANS, not for tests (memory ui-legibility-pass-landed-2026-09-04). Ask Gate 2's actual question. Avoid an aria-live region that re-announces on every poll - PR #26's review caught exactly that; announce once and keep counters outside the live region.",
    "FEATURES ONLY (inherited): no security-boundary or policy work.",
    "Check any new testid namespace is free before using it (`cr.board.*` was already taken once, memory board-screen-landed-2026-09-04), and keep JSX ASCII-only - an apostrophe in JSX reds the scheduler boundary scan."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY THE PREREQUISITES ON DISK, NO EDITS. This row certifies what two siblings shipped, so prove they are there rather than trusting the board (global rail 4 clause 2). (a) task-40e52fa8a85a4921b97bee933b00403f and task-5f883e4e12c34842b3cb6c2f218e4256 — `preview.decide` is in the daemon vocabulary, MCP-excluded, and in the regenerated client. (b) task-f5a74c23f8754665ab9d36cba386e1d0 — `grep -rn 'moe-preview-receipt' --include=*.ts apps/daemon/src` returns hits and a receipt carries {goalId, sha, url, pid, screenshots[], STARTED|REFUSED(code)}. Read its completion record for the one thing it was permitted to leave uncovered: whether the daemon-SHUTDOWN cleanup arm was reachable. If any prerequisite is absent, report_blocked with blockedOnTaskIds naming it — do NOT add a daemon field here (task rail 1). (c) Confirm the offer actually reaches the browser: the daemon must OFFER `preview.decide` on the affordance surface for a goal with a STARTED receipt, or no card can ever render. If the offer is missing, that is a gap in task-f5a74c23, not something to work around with a synthetic card.",
      "status": "IN_PROGRESS",
      "affectedFiles": [],
      "startedAt": "2026-09-06T01:13:44.330Z"
    },
    {
      "stepId": "step-2",
      "description": "THE DECISION PORT, in a new apps/control-room/src/v2/approvals/preview-port.ts, mirroring apps/control-room/src/v2/approvals/escalation-port.ts — 43 lines, and the closest working example of a human decision reaching the daemon. Same shape: a `PREVIEW_COMMAND_KIND` constant, a decision union (APPROVE | REJECT), and a port whose single method calls `spendOffer(wire, KIND, affordance, payload, \"<ui-source>\", LAYER)` from offer-wire.js. Do NOT invent a second decision wire (task rail 2): `spendOffer` is how every existing human decision is spent, and a parallel path would drift on offer accounting. Add preview-port.test.ts asserting the payload shape for APPROVE (no findings) and for REJECT (findings naming the nodes), and that a refusal from the wire is surfaced with its code rather than swallowed.",
      "status": "PENDING",
      "affectedFiles": [],
      "newFiles": [
        "apps/control-room/src/v2/approvals/preview-port.ts",
        "apps/control-room/src/v2/approvals/preview-port.test.ts"
      ]
    },
    {
      "stepId": "step-3",
      "description": "THE NEEDS-YOU ITEM, in apps/control-room/src/v2/approvals/needs-you-model.ts (197 lines). Add the preview decision as an item kind so it joins the queue in the order an operator should act. The model is PURE (needs-you.tsx:17: \"no fetch, no dispatch, no clock\") — keep it that way: it folds the daemon's offers and the preview receipt into an item, and decides nothing. Assert in its test that the item appears ONLY for a goal whose receipt is STARTED, and is ABSENT when there is no receipt, when the receipt is REFUSED, and when the daemon offers no `preview.decide` — a card offering a decision the daemon will refuse is worse than no card.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you-model.ts"
      ]
    },
    {
      "stepId": "step-4",
      "description": "THE CARD, in apps/control-room/src/v2/approvals/needs-you.tsx (207 lines). Render the loopback URL as a link that opens in a NEW TAB (the operator is about to leave the page to look at their product; losing the queue would be hostile), the captured screenshots inline, and both Approve and Reject-with-findings controls. ASK GATE 2's ACTUAL QUESTION — \"Is this product good enough and visually correct?\" — rather than titling the card with a receipt name; words are the product here (task rail 3). Reject requires findings naming the nodes to rework, and the control must not submit an empty rejection. Follow the file's existing card conventions: `data-testid={`cr.needsyou.item.${slug}`}` (:108) and the `decisionResults` prop for per-item outcomes (:20-23). CHECK ANY NEW TESTID NAMESPACE IS FREE before using it (`cr.board.*` was already taken once), and keep the JSX ASCII-only — an apostrophe in JSX reds the scheduler boundary scan, so write \"is this product good enough\" without a typographic apostrophe.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you.tsx"
      ]
    },
    {
      "stepId": "step-5",
      "description": "THE TWO OUTCOMES, asserted where they actually land. (a) APPROVE commits a durable decision that is READ BACK from `/activity/read` carrying its verdict — assert the verdict from the activity read, NOT from component state, so a click that never reached the daemon cannot pass (DoD 2). (b) REJECT with findings moves the named node to REWORK on the board with the findings text visible: assert the node's column is REWORK, reusing the EXISTING column (board-columns.ts:20 already lists REWORK and :47 already routes a READY node with prior review rounds there — do not add a column), and assert the findings text renders, because a rejection whose reason the operator cannot read is the defect this clause exists to prevent. (c) The four refusal codes render as operator words verbatim from the daemon — assert the rendered sentence for at least PREVIEW_COMMAND_MISSING and PREVIEW_START_TIMEOUT, and assert an UNKNOWN code still renders verbatim rather than blank, which is the existing convention for unmirrored tokens.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/approvals/needs-you.tsx",
        "apps/control-room/src/v2/ops/activity-words.ts"
      ]
    },
    {
      "stepId": "step-6",
      "description": "THE PREVIEW STAGE ON THE STATUS STRIP. apps/control-room/src/v2/goals/goal-status.ts: add a PREVIEW stage to the `GoalStage` union and its branch in `deriveGoalStatus`, placed so it cannot mask the gates above it — read the branch ORDER first (CLOSED, then CONTRACT at Gate 1, then PLAN, then REPLANNED, then ESCALATION, then READY_TO_CLOSE, then WORKING). A preview is running AFTER the nodes are landed and BEFORE the goal closes, so it belongs after ESCALATION and before READY_TO_CLOSE; state your placement and why in the step note. Adding to the union means STAGE_WORDS in goal-status-strip.tsx:17 must gain the entry too or the strip renders an undefined label — the union is exhaustively keyed, so the typechecker will name it; do not silence that with a cast. Assert the stage and its words for a goal with a STARTED receipt, and assert READY_TO_CLOSE still wins once the preview is approved and coverage is complete.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-status.ts"
      ]
    },
    {
      "stepId": "step-7",
      "description": "THE BROWSER JOURNEY over a REAL daemon, in a new spec under tests/e2e/control-room/ (existing specs there — daemon-board.spec.ts, prd-to-approval.spec.ts — already drive a real daemon; follow their harness rather than standing up a new one). Cover start -> card -> approve: the preview starts, the Needs-you card appears with its link and at least one screenshot, the operator approves, and the decision is durable. NO FIXTURE-BACKED SUBSTITUTE (parent rail 4): if the lane cannot start a real preview because the fixture product needs a workspace this harness does not provide, report blocked rather than stubbing the receipt. Run `pnpm test:e2e:browser` and quote its count line (DoD 5).",
      "status": "PENDING",
      "affectedFiles": [],
      "newFiles": [
        "tests/e2e/control-room/preview-gate.spec.ts"
      ]
    },
    {
      "stepId": "step-8",
      "description": "LIVE DRIVE, DOCS, GATES, ADVERSARIAL PASS — this row owns them for the whole Gate 2 feature (global rail 21). (a) LIVE DRIVE: start a preview for UnAI (D:/projexts/UnAI) or a fresh scaffold app; record the real screenshot PATH and its SHA256 plus the committed decision in a row comment under ~4500 chars. NEVER restart the wrapper to unstick a seat — claim TTL is 30 minutes. If the drive did not happen, say so plainly; do NOT let the README edit imply a capability nobody drove. (b) DOCS: rewrite README's \"no working preview\" clause to the measured truth; `git grep -n 'no working preview'` returns 0 hits. That clause sits in a \"What this is not\" section whose purpose is honesty — do not overclaim while editing it. (c) GATES, each standalone and foreground with its `Test Files N passed` count line quoted (global rail 8): `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/control-room test`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e:browser`. Note that `pnpm test` covers no apps/** (global rail 18). (d) ADVERSARIAL PASS in a step note: what the card shows when the receipt names a pid long gone or a screenshot file since deleted; what two operators approving concurrently do to the offer; and whether the loopback link can ever point somewhere other than the daemon's own preview.",
      "status": "PENDING",
      "affectedFiles": [
        "README.md"
      ]
    }
  ],
  "status": "BLOCKED",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "ARCHITECT",
  "parentTaskId": "task-f3edb8fb39ad42ce8980dae7d62b604c",
  "dependsOn": [
    "task-40e52fa8a85a4921b97bee933b00403f",
    "task-f5a74c23f8754665ab9d36cba386e1d0",
    "task-dd86f35e117e4de1b5b885332d8af1f8"
  ],
  "priority": "HIGH",
  "order": 33,
  "comments": [
    {
      "id": "comment-66e75ba771c540fd9d8381ade1e74126",
      "author": "worker-0520a42e",
      "content": "STEP 1 PREREQUISITE VERIFICATION - FULL MEASUREMENT (the block reason is capped at 2000 chars; this is the evidence it points at). worker-0520a42e, 2026-09-06, HEAD eb13d2f6 on moe/work-2026-09-04. NO EDITS MADE. step-1 deliberately left IN_PROGRESS so the verification is re-run at the new HEAD rather than trusted from this session.\n\n(a) PASS - the kind. preview.decide on the shared roster: packages/contracts/src/runtime/runtime-vocabulary.ts:104. Regenerated client: packages/control-room-client/src/generated/generated-client.ts:245 and :367. Human-only: apps/daemon/src/orchestrator/agent-spawn-contract.ts:229 (HUMAN_ONLY_STEPS). MCP-excluded: apps/daemon/src/mcp-tool-allowlist.ts:72. Contract module: apps/daemon/src/preview/preview-contracts.ts:43.\n\n(b) PASS - the receipt. `grep -rn \"moe-preview-receipt\" --include=*.ts apps/daemon/src` -> preview-receipt-contracts.ts:47 `PREVIEW_RECEIPT_VERSION = \"moe-preview-receipt/1\"`. Members and STARTED|REFUSED(code) present as the plan describes. Read task-f5a74c23's handoff: the arm it was permitted to leave uncovered is the daemon-SHUTDOWN sweep - createPreviewSupervisor().close(), named there as \"the daemon-shutdown seam a composition row should call\". I carried that into the new row's DoD item 6 so it is not lost.\n\n(c) FAIL - the offer, and everything upstream of it. Four independent measurements:\n\n1. THE REGISTRY ENTRY IS AN UNCONDITIONAL FAIL-CLOSED STUB. apps/daemon/src/daemon-command-registry.ts:433-439. The comment at :432 says so itself: \"The second throw is the FAIL-CLOSED stub the runner replaces.\"\n     if (preview) {\n       const decoded = decodePreviewDecidePayload(envelope.payload);\n       if (!decoded.ok) {\n         throw new DomainRefusal(decoded.code, decoded.layer, \"preview.decide payload refused\");\n       }\n       const stub = previewRefusal(\"PREVIEW_GOAL_NOT_LANDED\");\n       throw new DomainRefusal(stub.code, stub.layer, \"preview runner is not landed\");\n     }\n   EVERY well-formed decision - APPROVE and REJECT alike - throws PREVIEW_GOAL_NOT_LANDED @ GOAL_AUTHORITY. Only a MALFORMED payload gets a different code. Nothing is ever committed, so nothing can be read back from /activity/read.\n\n2. THE RUNNER PACKAGE HAS ZERO CONSUMERS.\n     git grep -n \"preview/preview-\" -- '*.ts' '*.tsx' '*.js' ':!*/dist/*' ':!apps/daemon/src/preview/*'\n   returns exactly FOUR hits, and every one imports ONLY ./preview/preview-contracts.js - the vocabulary task-40e52fa8 published, not the runner:\n     apps/daemon/src/daemon-command-families.ts:21\n     apps/daemon/src/daemon-command-payload-keys.ts:34\n     apps/daemon/src/daemon-command-registry.ts:28\n     apps/daemon/src/daemon-command-vocabulary.ts:26\n     git grep -n \"createPreviewRunner\\|createPreviewSupervisor\\|previewReceiptId\" outside apps/daemon/src/preview/ -> ZERO.\n   preview-runner, preview-supervisor, preview-ledger, preview-capture, preview-process, preview-receipt-contracts, preview-goal-landing and preview-command-resolution are unreachable from the live daemon.\n\n3. NOTHING CAN START A PREVIEW. `git grep -n \"preview\\.start\"` over apps/ and packages/ -> ZERO. preview.decide is the ONLY preview kind on the roster. runPreview has no command, no goal-lifecycle hook and no HTTP route, so recordPreviewReceipt never runs and no receipt reaches a live store.\n\n4. preview.decide IS NOT ON THE AFFORDANCE OFFER SURFACE. `git grep -in \"preview\" -- 'apps/daemon/src/http/affordance-*.ts'` excluding tests -> ZERO. The one hit in affordance-planning-offers.test.ts:456 is HUMAN_ONLY_STEPS membership - the FENCE - not an offer.\n\nDoD REACHABILITY, clause by clause. 1: no receipt and no offer to render from. 2: the registry refuses every APPROVE - precisely the defect the clause exists to catch. 3: REJECT never commits, so no node moves to REWORK. 5: no start and no approve for Playwright to drive. 6: cannot drive UnAI. Clauses 4 (stage + refusal words) and 7 (gates) are the only reachable ones, and neither is worth landing alone.\n\nAMENDMENT CHECK (global rail 9): all eight steps have activeAmendmentId=None and zero amendments, so the base plan text is authoritative and this block is premised on the effective text.\n\nACTIONS TAKEN: filed task-dd86f35e117e4de1b5b885332d8af1f8 (PLANNING, CRITICAL, dependsOn task-f5a74c23 + task-40e52fa8) for the registry edge, the start trigger, the affordance offer and the shutdown sweep; commented the consumer-edge measurement on task-f5a74c23 (global rail 4 clause 1 puts it on the producer's record, not a reopen); commented the band-shape finding on the parent task-f3edb8fb with the architect ask.\n\nPLAN NEEDS NO AMENDMENT. Steps 2-8 stand as written once the prerequisite exists.",
      "timestamp": "2026-09-06T01:19:07.013Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-04T23:00:06.234Z",
  "updatedAt": "2026-09-06T01:20:57.498Z",
  "metrics": {
    "firstClaimAt": "2026-09-04T23:12:12.571Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 8
  },
  "planSizeWarnings": [
    "Plan touches 8 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-04T23:13:28.242Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED, a second decision wire: `spendOffer` (offer-wire.ts) is how every existing human decision is spent and escalation-port.ts is a 43-line worked example; a parallel path would drift on offer accounting. REJECTED, a new board column for rejected-into-rework: board-columns.ts:20 already lists REWORK and :47 already routes a re-opened node there — adding one would be a design regression, not a feature. REJECTED, rendering the card from a synthetic receipt when the daemon offers no `preview.decide`: a card that offers a decision the daemon will refuse is worse than no card, so step 1 treats a missing offer as a gap in task-f5a74c23 rather than something to work around. REJECTED, folding the live drive and README into a sibling: global rail 21 puts the e2e, docs sweep and adversarial pass in ONE row for the feature, and this is it. REJECTED, putting the PREVIEW stage last in the branch order: it would be masked by WORKING; the plan requires the worker to state the placement and defend it.",
    "codebaseInsights": "MEASURED AT HEAD df5bbfd5 (branch moe/work-2026-09-04) by architect-20a6d1eb. EVERY PATTERN THIS ROW NEEDS EXISTS — it composes rather than invents. `apps/control-room/src/v2/approvals/escalation-port.ts` is 43 lines and is the template: a KIND constant, a decision union, and one method calling `spendOffer(wire, KIND, affordance, payload, \"ui-escalate\", LAYER)` from offer-wire.ts (84 lines). `needs-you.tsx` (207) documents its own contract at :12-17 — one card per decision, cards carry a daemon offer, and the component is \"otherwise pure: no fetch, no dispatch, no clock\"; cards are keyed `cr.needsyou.item.${slug}` (:108) and per-item outcomes arrive via `decisionResults` (:20-23). `needs-you-model.ts` (197) is the pure fold. THE BOARD ALREADY HAS REWORK: board-columns.ts:20 `BOARD_COLUMNS = [\"QUEUED\",\"WORKING\",\"REVIEW\",\"REWORK\",\"DONE\",\"BLOCKED\"]`, and :47 routes a READY node with prior review rounds to REWORK. THE STAGE UNION IS EXHAUSTIVELY KEYED: `GoalStage` (goal-status.ts:13-22) is mirrored by `STAGE_WORDS` (goal-status-strip.tsx:17) as a full Record, so adding a member forces the words entry at typecheck — which is the desired behaviour, not something to cast away. BRANCH ORDER in deriveGoalStatus: CLOSED, CONTRACT, PLAN, REPLANNED, ESCALATION, READY_TO_CLOSE (gated on verified === criteria at :139-143), WORKING. THE E2E LANE: `pnpm test:e2e:browser` = `tsc -p tests/e2e/control-room/tsconfig.json && playwright test -c tests/e2e/control-room/playwright.config.ts`; daemon-board.spec.ts and prd-to-approval.spec.ts already drive a real daemon.",
    "risks": "BIGGEST RISK — asserting APPROVE from component state. A click that never reached the daemon would pass; DoD 2 and step 5(a) require reading the verdict back from /activity/read. SECOND — this row is the last line of defence for the whole Gate 2 feature, so a failing arm is evidence a sibling under-delivered; task rail 1 forbids patching the daemon here, and step 1 blocks instead. THIRD — the live drive is the clause most likely to be skipped while the README clause is deleted anyway, leaving the docs claiming a capability nobody drove; step 8(a) permits saying it did not happen but not implying it did. FOURTH — stage placement: a PREVIEW branch after WORKING never fires, and one before READY_TO_CLOSE could mask completion; the plan makes the worker state and defend the position and assert that READY_TO_CLOSE still wins after approval. FIFTH — adding to GoalStage without STAGE_WORDS renders an undefined label; the Record is exhaustive so the typechecker names it, and a cast would hide exactly the thing that protects the strip. SIXTH — ASCII-only JSX: an apostrophe in JSX reds the scheduler boundary scan, and Gate 2's question is naturally written with one. SEVENTH — stale receipts: a pid long dead or a deleted screenshot file must not blank the card, which step 8(d) makes the worker answer.",
    "keyFiles": [
      "apps/control-room/src/v2/approvals/escalation-port.ts",
      "apps/control-room/src/v2/approvals/offer-wire.ts",
      "apps/control-room/src/v2/approvals/needs-you.tsx",
      "apps/control-room/src/v2/approvals/needs-you-model.ts",
      "apps/control-room/src/v2/goals/goal-status.ts",
      "apps/control-room/src/v2/board/board-columns.ts",
      "tests/e2e/control-room/daemon-board.spec.ts"
    ]
  },
  "planApprovedAt": "2026-09-04T23:13:30.442Z",
  "contextFetchedBy": [
    "worker-0520a42e"
  ],
  "workStartedAt": "2026-09-06T01:13:44.330Z",
  "blockedReason": "STEP 1 ASKED ITS QUESTION; ANSWER IS NO. Step 1(c): a missing offer \"is a gap in task-f5a74c23, not something to work around with a synthetic card.\" NO EDITS; step-1 left IN_PROGRESS to force re-measurement at the new HEAD. FULL EVIDENCE: comment-66e75ba771c540fd9d8381ade1e74126 on this row.\n\nHEAD eb13d2f6. (a) kind PASSES; (b) receipt PASSES (preview-receipt-contracts.ts:47); (c) FAILS four ways:\n1. daemon-command-registry.ts:433-439 is an unconditional fail-closed stub - its comment at :432 calls it \"the FAIL-CLOSED stub the runner replaces\". EVERY well-formed APPROVE and REJECT throws PREVIEW_GOAL_NOT_LANDED @ GOAL_AUTHORITY. Nothing commits.\n2. git grep \"preview/preview-\" (minus dist + preview dir) -> 4 hits, ALL importing only preview-contracts.js, the vocabulary. createPreviewRunner/createPreviewSupervisor/previewReceiptId outside that dir -> ZERO. Runner unreachable from the live daemon.\n3. git grep \"preview\\.start\" over apps/ and packages/ -> ZERO. Nothing starts a preview; recordPreviewReceipt never runs.\n4. git grep -in preview -- apps/daemon/src/http/affordance-*.ts minus tests -> ZERO. No offer reaches the browser.\n\nA MISSING ROW, NOT AN UNDER-DELIVERY. Epic rail 1 makes a kind a four-row job with \"daemon wiring and operator fence\" third. Gate 2 shipped rows 1, 2, the FENCE HALF of 3, and 4. The release band has its wiring row explicitly (task-54b74b80); Gate 2 never got one.\n\nDoD 1,2,3,5,6 unreachable. DoD 2 is refused by the registry - the exact defect it exists to catch. Task rail 1 forbids repairing the subject inside the proof; parent rail 4 forbids a fixture substitute. planningNotes already rejected this: \"a card that offers a decision the daemon will refuse is worse than no card.\"\n\nFILED task-dd86f35e117e4de1b5b885332d8af1f8 (PLANNING, CRITICAL): registry edge, start trigger, offer, shutdown sweep. Commented task-f5a74c23 and parent task-f3edb8fb.\n\nASK: plan it; add it to task-f3edb8fb dependsOn (architect-only). Steps 2-8 stand unamended.",
  "blockedOnTaskIds": [
    "task-dd86f35e117e4de1b5b885332d8af1f8"
  ],
  "blockedResourceId": null,
  "blockedFromStatus": "WORKING",
  "blockedAt": "2026-09-06T01:20:12.940Z"
}