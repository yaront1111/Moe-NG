{
  "id": "task-e7b802bc49734ee6a6ad7eb89579a106",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[R3-8 spinoff-b] Carry the foundation artifact into the release handoff atomically: dispatch-keyed read, fixture fan-out, and a fence-or-immutability design",
  "description": "CAPABILITY successor to task-fdcfe1ca (which DELETES the dead activation-keyed artifact reads to close the reachability defect without opening a hole). This row restores the capability those reads were designed for — the foundation artifact actually reaching the release handoff — and it must land the THREE things the human's 2026-08-29 unblock ruled inseparable. Filed in BACKLOG deliberately: item (3) needs a design decision a governor or human must ratify before any claim.\n\nMEASURED BASIS (worker-4bba4fa4's executed production-seam witness at HEAD 62b4d7f6, comment-a6f48882 on task-fdcfe1ca): activation-keyed read id observes event count 0 and refuses FOUNDATION_ARTIFACT_LEDGER_ABSENT @ DAEMON_FOUNDATION_ARTIFACT_LEDGER; the writer-keyed id (deriveDispatchAggregateId) observes count 1 and reads artifactDigest 4c65eed3... The only production writer is foundation-attempt-store.ts:237-243 keying on bound.target.\n\nTHE THREE INSEPARABLE PARTS:\n1. ID CORRECTION at the real read sites: release-handoff-sources.ts (readFoundationArtifactForAttempt at :205, currently activation-keyed) and, if the watch horizon is restored, release-handoff-classify.ts's roster. Both must key on the id the writer writes.\n2. FIXTURE FAN-OUT (comment-27adb879): release-handoff-test-harness.ts:443-456 seedArtifactManifest seeds the ACTIVATION-keyed row; consumers at release-handoff-builder.test.ts:337/393/405/445/524, attempt-release-disposition.test.ts:343/1426, attempt-finalization-test-harness.ts:276, release-handoff-sources.test.ts:185, foundation-attempt-service.test.ts:1030. The harness must seed EXACTLY deriveDispatchAggregateId(identity.attemptAggregateId) — seeding BOTH keys preserves the old tautology and makes a reader-regression mutant green.\n3. ATOMICITY — THE DESIGN DECISION, and why this row is parked. foundation-artifact-ledger.ts:149-164 appends latest-wins (:231 reads latest after a re-seal), so a corrected id makes ARTIFACT a LIVE MUTABLE source. release-handoff-builder.ts:146-161 carries no artifact version into ReleaseHandoffBuilt.sources (:66-70 excluded it precisely because the old id had no writer), and attempt-release-fence-legs.ts:28-35 is capacity-bound: MAX_DECISION_LEGS=8, one primary + SEVEN fence slots exactly (ACTIVATION, BINDING, DISPATCH, JOURNAL, PROVIDER_RUN, RESOURCE, STEP at :52-54). An artifact can therefore move between the handoff's count capture and the release decision with every current fence passing — a stale-evidence authority hole. The two candidate designs: (a) widen MAX_DECISION_LEGS / redesign the leg schema to add an ARTIFACT fence (touches R3-8's delivered surface — coordinate with its owner and re-run its proof), or (b) make the artifact ledger immutable for a released attempt (single-seal or seal-freeze, a writer/ledger change with its own owner). NEITHER may be smuggled in as a side effect; the choice is the promotion gate for this row.\n\nUNPARK CONDITION: a governor/human ruling in this row's comments choosing (a) or (b), with the owning surface named. Then plan all three parts together — partial landings re-open the hole task-fdcfe1ca closed.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK: a recorded governor/human ruling in this row's comments chooses fence-widening (a) or artifact immutability (b) and names the owning surface; re-measure the writer key (foundation-attempt-store.ts sealFoundationArtifactRoster on bound.target), the latest-wins append (foundation-artifact-ledger.ts:149-164, :231) and task-fdcfe1ca's delivered deletion at your own HEAD, each grep unpiped with its own exit code and a positive control. If any premise fails, report blocked naming it.",
    "The release handoff reads the artifact under the id the production writer writes, proven by an arm where a sealed artifact REACHES the handoff and changes its output, executed through the production reader — no fixture standing in for the writer.",
    "The shared harness seeds exactly the dispatch-keyed row (never both keys), and every named consumer suite passes against the corrected seeding; a reader-regression mutant (re-introducing the activation key in production) reds a named arm in at least one consumer suite.",
    "The chosen atomicity design is delivered with its own proof: either the ARTIFACT fence leg exists and a moved-artifact-after-capture case refuses with its exact code and layer, or the ledger refuses a re-seal for a release-bound attempt with its exact code and layer. The un-chosen design is not partially present.",
    "Mutation drills, one degree of freedom each with proven restores: removing the atomicity mechanism reds the moved-artifact arm; re-keying the read back to activation reds the reachability arm while FOUNDATION_ARTIFACT_LEDGER_ABSENT stays the refusal the dead key yields.",
    "Gates standalone, foreground, unpiped, exit 0 with nonzero counts quoted: pnpm --filter @moe/daemon typecheck plus the focused suites for handoff builder/sources/classify, release disposition, finalization and foundation-attempt-service. Explicit-pathspec commit only."
  ],
  "taskRails": [
    "Do not land any part alone. The id correction without the atomicity mechanism re-opens the stale-evidence hole task-fdcfe1ca closed; the fixture change without the id correction reds every consumer suite.",
    "Do not seed both artifact keys in any fixture — that is the tautology that hid this defect.",
    "Coordination: design (a) touches task-06835dfa's delivered fence surface — its proof must be re-run, not assumed; design (b) changes the artifact writer/ledger — name and involve its owner. Neither is this row's to change unilaterally.",
    "Fail closed with exact code and layer; TDD; <=250 lines per production source, split before 400; explicit-pathspec commits only."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "STEP 1 — VERIFY-OR-BLOCK + THE TWO RULINGS THIS RE-PLAN OWES. Read-only, author no bytes. Record in a durable moe.add_comment; quote `git rev-parse HEAD` beside every probe, UNPIPED with its own exit code.\n\nDoD-1's PROMOTION GATE IS ALREADY SATISFIED — do not re-open it. governor-0af24e35 ruled at 12:32Z in `comment-58e3481254b2`: THE CHOICE IS (b) ARTIFACT IMMUTABILITY, owning surface `apps/daemon/src/work/foundation-artifact-ledger.ts`. Read that comment and the DoD amendment at `comment-4276c4f9a0c3` before anything else. Three architects correctly refused to fabricate this ruling; it exists now.\n\nTHE MUTUAL-EXCLUSIVITY HAZARD IS RESOLVED. architect-af2bdb67 disclosed in `comment-a2ceaa307c51` that task-fdcfe1ca would delete the very reads this row corrects — both touching release-handoff-sources.ts:205 and release-handoff-classify.ts:124, only one able to land — and asked a governor to route it. MEASURED NOW: task-fdcfe1ca is **ARCHIVED**. The hazard is gone. Verify that and record it so nobody re-raises it as a blocker.\n\nRE-MEASURE, each with a positive control:\n(a) the writer key — `sealFoundationArtifactRoster` (foundation-artifact-ledger.ts:129) called from foundation-attempt-store.ts:237 on `bound.target`;\n(b) the latest-wins append at foundation-artifact-ledger.ts:149-164 and the comment at :231, \"LATEST WINS: a re-seal after a further attempt event appends a second row\";\n(c) the replay-idempotence at :166-168 — identical bytes replay, \"a body that DIFFERS is a real conflict\";\n(d) the two read sites this row re-keys: release-handoff-sources.ts:205 and release-handoff-classify.ts:124;\n(e) exact porcelain on every owned path.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "STEP 1 DONE, read-only, zero bytes authored. Durable record: comment-a0d63b57b21941ed91ff830f7f6d82d2. HEAD 13d5750e21cc0b99a7378ef6645dbf5ea16b2408 (tree moved twice while reading; all probes at that SHA, positive control `git grep -c 'export' -- foundation-artifact-ledger.ts` => 13, EXIT 0).\n\nPROCEED. Both rulings read and carried forward (design (b) + its four obligations from comment-58e3481254b2; the DoD-1 phantom-clause amendment from comment-4276c4f9a0c3). task-fdcfe1ca verified ARCHIVED on disk, assignedWorkerId null — BLOCKER 2 discharged, the reads are unopposed. All ten owned paths porcelain-EMPTY.\n\nTHE MEASUREMENT THAT MATTERS AND THAT I WENT LOOKING FOR: step 4 says re-key to the DISPATCH aggregate, but `FoundationAttemptBinding` (activation-attempt-reader.ts:49-53) has only `activationAggregateId` and no dispatch field, and `deriveActivationAggregateId` is one-way. \"The writer's id is unreachable from the reader\" is the exact shape that blocked this row twice, so I settled it at step 1 rather than at step 4. IT IS REACHABLE: attempt-finalization-sources.ts:96 (production, not a fixture) builds `target: deriveDispatchAggregateId(attemptAggregateId)` from the ACTIVATION id, corroborated at journal-test-harness.ts:118 and attempt-finalization-test-harness.ts:268. `deriveDispatchAggregateId` is exported at foundation-attempt-codec.ts:216, one string argument, pure frame-and-hash, no store or clock. Corrected key: `deriveFoundationArtifactAggregateId(deriveDispatchAggregateId(binding.activationAggregateId))`, composable at both sites from what they already hold.\n\n(b) LATEST-WINS CONFIRMED CONSTRUCTIBLE: ledger :149 + `expectedVersion: store.readEvents(aggregateId).length` at :151-164, reader comment :231 and `rows[rows.length - 1]` at :232. A second DIFFERENT truth about one attempt's roster is reachable today — which is what makes step 3 ARM B a real arm where the old moved-artifactDigest one was impossible.\n(c) IDEMPOTENCE PATH LOCATED: :166-168 with `alreadySealed`/`sameBytes` at :117-123. Flagging for step 3/5: TWO paths can already answer a re-seal (this conflict path and the new guard), so ARM B must assert exact code AND layer, never bare \"it refused\", or it cannot tell which mechanism answered.\n(d) Both read sites live and still activation-keyed — sources.ts:205-207 and classify.ts:124. Phantom clause confirmed phantom: in-place correction, no restore-then-correct.",
      "startedAt": "2026-08-29T16:43:32.349Z",
      "completedAt": "2026-08-29T16:46:21.375Z"
    },
    {
      "stepId": "step-2",
      "description": "STEP 2 — THE TENTH-PATH ROSTER RULING. My ruling; record it and proceed.\n\nThe governor's re-plan named a hidden shape consumer OUTSIDE the nine owned paths: `apps/daemon/src/work/release-handoff-builder.test.ts` at :505, :537 and :558. I measured it — those arms bind `ARTIFACT_AGGREGATE = deriveFoundationArtifactAggregateId(ACTIVATION_AGGREGATE)` and pin the HORIZON_MOVED watched/unrelated scope around it. Re-keying the artifact read reaches them directly.\n\nRULING: `release-handoff-builder.test.ts` JOINS this row's owned paths as the tenth. It is the TEST SIBLING of `release-handoff-builder.ts`, which this row already owns — a producer's own test sibling is not a foreign path, and Rail 8(A) requires owning every caller a change reaches while explicitly stating that Rail 6's line caps govern production source size, not the ownership roster. Precedent on this board: governor-fd35a081's bounded grant on task-cc124d65 for exactly this \"producer adds, consumer pins\" shape.\n\nBOUNDED: update ONLY what the re-key forces — the `ARTIFACT_AGGREGATE` derivation and any arm that asserts against it. Do not reorder arms, do not touch the unrelated-aggregate scope arm's intent (it exists to prove the horizon check is scoped, not global), and do not adjust `occurrences(...)`-style counts.\n\nTHEN CHECK FOR AN ELEVENTH. `git grep -n 'deriveFoundationArtifactAggregateId' HEAD -- apps packages tests` and classify every hit. If a consumer exists outside these ten paths, STOP AND REPORT — that is a split signal at plan time, not a path to take quietly at step 6. This is the same class of defect that sent the row back; do not let it recur.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/release-handoff-builder.test.ts"
      ],
      "note": "STEP 2 DONE, zero bytes authored. Durable record: comment-7a4fef512a914b4bbcaa6df85c96f0a9. HEAD 13d5750e.\n\nTENTH PATH RATIFIED with the measurement that makes it non-optional: `ARTIFACT_AGGREGATE` has exactly 3 hits, all in release-handoff-builder.test.ts — :505 the derivation, :537/:558 as the INTERPOSE TRIGGER for the two HORIZON_MOVED arms. Re-key the production read without moving :505 and the interposition fires on an aggregate nobody reads: both arms keep passing while exercising nothing. Silent vacuity, not cosmetics. Bounded to the :505 derivation; the unrelated-aggregate scope control at :552-562 stays untouched.\n\nELEVENTH-PATH SCAN CLEAN — no split signal, not reporting one. `deriveFoundationArtifactAggregateId` and `readFoundationArtifactForAttempt` fully classified at HEAD; every hit is inside the roster except foundation-artifact-ledger.test.ts, whose two uses build that suite's OWN fixture aggregate and drive the ledger API directly, so the re-key cannot change their outcome.\n\nONE ROSTER ADDITION, RULED IN AND FLAGGED RATHER THAN SLIPPED: foundation-artifact-ledger.test.ts joins as the ELEVENTH owned path — reached not by the re-key but by STEP 5, because the governor's ruling obliges the new refusal be \"asserted by name in tests per Rail 3\" and that file is where sealFoundationArtifactRoster is already exercised (5 sites). Same test-sibling reasoning and same precedent step 2 uses for the tenth; same boundedness (add arms for the new refusal, change nothing else).\nCONSEQUENT PLAN DEVIATION, stated for review: the plan puts ARMS B and C in release-handoff-builder.test.ts. A LEDGER refusal asserted through the BUILDER proves the handoff refused, not that the new guard did — and the ledger already has a second path that can answer a re-seal (CONFLICT at :166-168). So ARM A stays in builder.test.ts as planned; ARMS B and C go in the ledger's own suite, against the surface that actually refuses. If an architect prefers them in the builder suite, say so and I will move them.\n\nFIXTURE FAN-OUT PRE-MEASURED so step 6 holds no surprise: seedArtifactManifest has 3 external consumers (expansion-release-selector-test-fixtures.ts:381, foundation-attempt-service.test.ts:1034, release-handoff-sources.test.ts:185). None needs owning — composing the dispatch derivation INSIDE the harness at :449 corrects the seeded key without touching seedArtifactManifest's signature or HandoffSeedIdentity, so all three keep compiling unchanged. If step 6 turns out to need a signature change I stop and report there rather than widening quietly.",
      "startedAt": "2026-08-29T16:46:29.095Z",
      "completedAt": "2026-08-29T16:48:15.562Z"
    },
    {
      "stepId": "step-3",
      "description": "STEP 3 — RED FIRST, WITH A CONSTRUCTIBLE DIVERGENCE. This replaces the impossible drill and is the heart of the re-plan.\n\nWHY THE OLD ARM WAS IMPOSSIBLE, so it is not retried: the plan required a moved-`artifactDigest` case, but a lawful seal always yields `[]` — `artifactDigest` HAS NO PRODUCER, and foundation-attempt-store.ts:234 states the consequence outright: \"artifactDigest has no producer. A REFUSED seal therefore never settles PROVEN.\" No input produces the divergence, so the arm and its D1 drill could never fail. Epic rail 7(A) forbids exactly that: a fixture that cannot distinguish the mechanism proves the system refuses, not that this guard refuses.\n\nTHE CONSTRUCTIBLE PROPERTY, under the ruled design (b): RE-SEAL IS AN ACTION A TEST CAN TAKE. Today foundation-artifact-ledger.ts:231 says \"LATEST WINS: a re-seal after a further attempt event appends a second row\" — so a second, DIFFERENT truth about one attempt's roster is currently reachable. Immutability is the refusal of exactly that for a release-bound attempt.\nARM A (REACHABILITY, DoD-2): a sealed artifact REACHES the handoff and CHANGES its output, through the production reader, with no fixture standing in for the writer. Assert the output difference, not merely that a read succeeded.\nARM B (IMMUTABILITY, DoD-4's (b) branch): seal an artifact, bind the attempt to a release, then attempt a re-seal with DIFFERENT bytes — the ledger REFUSES with its exact code AND layer, and NO second row appends. Assert the row count with a denominator, and assert the code and layer, never merely that it refused: :166-168 already has an idempotence path and a conflict path, so an arm that only checks \"it refused\" cannot tell which mechanism answered.\nARM C (IDEMPOTENCE PRESERVED): re-sealing with IDENTICAL bytes still replays cleanly and appends nothing. This is the arm that stops immutability being implemented as a blanket second-seal ban — the divergence between B and C is what proves the guard discriminates on the BODY, not on the attempt.\n\nRecord the RED with exit codes and counts. Expected codes are test-side literals; never import them from the module under test.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/release-handoff-builder.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/release-handoff-builder.test.ts"
      ],
      "note": "STEP 3 DONE — three arms authored, ALL THREE RED, and the red is the row's own thesis rather than a scaffolding failure.\n\nTHE RED: `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/work/release-handoff-builder.test.ts -t \"task-e7b802bc\"` => EXIT 1, `Tests 3 failed | 17 skipped (20)`. All three fail on the same measured fact: `artifactRows(store, deriveFoundationArtifactAggregateId(deriveDispatchAggregateId(ACTIVATION_AGGREGATE)))` is 0 where the arms require 1 — `AssertionError: expected +0 to be 1`, at :690 (ARM B) and :717 (ARM C), ARM A the same. The shared harness seals under the ACTIVATION key, so the DISPATCH-keyed aggregate the production writer actually writes holds nothing. That is exactly the defect this row exists to close, now executable instead of argued.\n`pnpm --filter @moe/daemon typecheck` => EXIT 0 with the `$ tsc --project tsconfig.json` echo, so the red is an assertion failure and not a compile error.\n\nI REVERSED MY OWN STEP-2 DEVIATION, and the measurement is why. At step 2 I ruled ARMS B and C into foundation-artifact-ledger.test.ts and ruled that file in as an eleventh owned path. MEASURED SINCE: that suite CANNOT host them. Its world is a bootstrapped project holding NO attempt and NO activation by deliberate design (its header records governor ruling comment-937524c8 clause 3), so `durableInstant` finds no event and EVERY seal there refuses `FOUNDATION_ARTIFACT_LEDGER_UNREADABLE` — its own arms at :181 and :209 assert exactly that. ARMS B and C need a SUCCESSFUL first seal, which that world cannot produce. The plan's original placement in release-handoff-builder.test.ts was right and I was wrong. So: all three arms live in builder.test.ts, foundation-artifact-ledger.test.ts is NOT owned, and THE ROSTER STAYS AT TEN. The governor's \"asserted by name in tests\" obligation is discharged by ARM B, which pins the new code and layer wherever it lives.\n\nWHAT THE ARMS DO, and the two places the wording is stronger than \"it refused\":\n- ARM A (DoD-2 reachability) asserts an OUTPUT DIFFERENCE, not a successful read: the seeded world builds a handoff carrying a 64-hex `artifactDigest`; the same world minus ONLY the artifact — composed from the harness's own individual seeders so the omission is the sole difference — REFUSES with `source: \"artifact-manifest\"` and upstream `{code: FOUNDATION_ARTIFACT_LEDGER_ABSENT, layer: DAEMON_FOUNDATION_ARTIFACT_LEDGER}`.\n- ARM B (DoD-4, design (b)) asserts the exact CODE AND LAYER plus a DENOMINATOR (rows still exactly 1), because the ledger already has a CONFLICT path at :166-168 and an arm checking only \"it refused\" could not tell which mechanism answered.\n- ARM C (idempotence) re-offers the IDENTICAL bytes under a different command id — so it is a genuine second call, not a decision-cache hit — and requires ok plus NO second row. B and C differ in the BODY alone, which is what proves the guard discriminates on bytes rather than banning re-seals outright.\nExpected codes are test-side literals (`LEDGER_LAYER`, `IMMUTABLE_CODE`), never imported from the module under test, so a hardcoded return cannot satisfy them.\n\nDESIGN DECISION STEP 5 WILL IMPLEMENT, recorded now because it turns on a governor clause. The ledger receives only `request.attemptAggregateId` = the DISPATCH id, and `readAttemptRelease` needs the ACTIVATION id (attempt-release-store.ts:203, :332) — `deriveDispatchAggregateId` is a one-way hash, so THE LEDGER CANNOT OBSERVE RELEASE-BINDING FROM WHAT IT IS HANDED. Adding an activation field to the seal request would edit foundation-attempt-store.ts:237, which the governor explicitly reserved (\"NOT this row's to change unilaterally\"). The ruling also says: \"Fail closed: on any doubt about release-binding, refuse.\" So the guard refuses ANY differing-body re-seal for an attempt, release-bound or not — a strict superset of the ruled property, licensed by that sentence, with no writer-side edit and no fence slot. ARM B therefore does not bind a release before re-sealing; the refusal does not depend on it. Disclosed rather than buried: if a governor wants the guard narrowed to release-bound attempts only, that needs the writer coordination the ruling reserved, and I will report blocked for it rather than reach across.\n\nDISCLOSURE, a stale premise this row will create and must not fix itself: `attempt-release-fence-legs.ts`'s header justifies excluding the artifact aggregate from the fence roster on the ground that \"the exact activation-keyed artifact aggregate the handoff classifier reads has NO production writer at all — its only writer is passed `bound.target`, a different aggregate — so fencing it would fence nothing.\" After step 4 that sentence is FALSE: the classifier will read the aggregate the writer writes. The exclusion still holds on the ruled design — (b) makes the artifact immutable, so it is not a mutable source needing a fence — but the RECORDED REASON goes stale. That file is not owned here ",
      "startedAt": "2026-08-29T16:52:34.010Z",
      "completedAt": "2026-08-29T16:52:56.998Z"
    },
    {
      "stepId": "step-4",
      "description": "STEP 4 — THE ID CORRECTION. Re-key the artifact read to the id the production writer actually writes.\n\n`sealFoundationArtifactRoster` is called on `bound.target` (foundation-attempt-store.ts:237) — the DISPATCH-keyed attempt aggregate. The two read sites currently derive from the ACTIVATION aggregate: `release-handoff-sources.ts:205` and `release-handoff-classify.ts:124`. Correct them to the dispatch key so the read reaches what the writer wrote.\n\nTHE DEAD KEY MUST STAY HONEST: `FOUNDATION_ARTIFACT_LEDGER_ABSENT` remains what the OLD activation key yields — DoD-5 pins that explicitly. Do not delete the refusal path or make the old key silently succeed.\n\nRAIL 1 IS BINDING AND IT IS WHY THIS STEP CANNOT SHIP ALONE: \"Do not land any part alone. The id correction without the atomicity mechanism re-opens the stale-evidence hole task-fdcfe1ca closed.\" Step 5 lands in the same commit. If you cannot complete both, commit NEITHER and report.\nNOTE task-fdcfe1ca is ARCHIVED (step 1) — the row that would have deleted these reads as dead code is gone, so correcting them is now unopposed.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/release-handoff-sources.ts",
        "apps/daemon/src/work/release-handoff-classify.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/release-handoff-sources.ts",
        "apps/daemon/src/work/release-handoff-classify.ts",
        "apps/daemon/src/work/release-handoff-builder.test.ts"
      ],
      "note": "STEP 4 DONE — both read sites re-keyed to the id the writer actually writes, plus the tenth path's derivation. `pnpm --filter @moe/daemon typecheck` => EXIT 0, echo `$ tsc --project tsconfig.json`.\n\nTHE CORRECTION, identical at both sites: `deriveDispatchAggregateId(binding.activationAggregateId)` replaces the bare `binding.activationAggregateId`.\n- release-handoff-sources.ts, inside `readArtifactDigest`: the query's `attemptAggregateId` now composes the dispatch derivation. Import added from ./foundation-attempt-codec.js.\n- release-handoff-classify.ts, in the watched-aggregate roster: `deriveFoundationArtifactAggregateId(deriveDispatchAggregateId(binding.activationAggregateId))`. Both sites carry a comment naming foundation-attempt-store.ts:237 and attempt-finalization-sources.ts:96 as the measured basis, so the next reader does not have to re-derive why the key is composed.\nWHY THE CLASSIFY SITE MATTERS AS MUCH AS THE READ: it is the horizon-fence roster. Leaving it activation-keyed would fence an aggregate nobody reads while leaving the one that IS read unfenced — a fence that reads as present and protects nothing.\n\nTENTH PATH, bounded exactly as step 2 ruled: release-handoff-builder.test.ts:516-517 now derives `DISPATCH_AGGREGATE` and then `ARTIFACT_AGGREGATE` from it, so the two HORIZON_MOVED arms at :548/:569 interpose on the aggregate the builder actually reads. Nothing else in that file moved — no arm reordered, the unrelated-aggregate scope control untouched, no occurrence counts adjusted. I also collapsed the duplicate constant my step-3 arms had introduced (`SEALED_ARTIFACT_AGGREGATE`) into the existing `ARTIFACT_AGGREGATE` rather than leaving two spellings of one aggregate in the file.\n\nTHE DEAD KEY STAYS HONEST, per DoD-5: nothing was deleted. `readFoundationArtifactManifest` still answers `FOUNDATION_ARTIFACT_LEDGER_ABSENT` for an aggregate holding no rows (ledger.ts:224), which is what the OLD activation key now yields. Drill D2 at step 7 re-keys back and must red ARM A while that code is what the dead key returns.\n\nRAIL 1 ACKNOWLEDGED AND NOT YET DISCHARGED: this step is NOT committed. The id correction without the atomicity mechanism re-opens the stale-evidence hole, so step 5 lands in the SAME commit or neither lands. Nothing is staged; `git status` for these paths shows working-tree edits only.",
      "startedAt": "2026-08-29T16:52:59.836Z",
      "completedAt": "2026-08-29T16:54:11.364Z"
    },
    {
      "stepId": "step-5",
      "description": "STEP 5 — ARTIFACT IMMUTABILITY IN THE LEDGER. The ruled design (b), on its ruled owning surface `apps/daemon/src/work/foundation-artifact-ledger.ts`.\n\nThe ledger must REFUSE a re-seal that would write a DIFFERENT body for a release-bound attempt, with its own exact stable code and layer, instead of appending a second latest-wins row (:231). Preserve the identical-bytes replay path at :166-168 unchanged — immutability discriminates on the BODY, not on the attempt, and step 3's ARM C is the fence that proves it.\n\nDO NOT IMPLEMENT DESIGN (a). The governor ruled (b) and the DoD is explicit that \"the un-chosen design is not partially present\" — no ARTIFACT fence leg, not even a disabled one. Ratified reasoning from `comment-58e3481254b2`: (a) would require raising MAX_DECISION_LEGS, which the module's own header calls a schema migration (the DDL interpolates `leg_count BETWEEN 1 AND 8`), and attempt-release-fence-legs.test.ts:76 asserts the budget is provably full. There is no free slot.\n\nRAIL 3 COORDINATION: design (b) changes the artifact writer/ledger, so NAME its owner in the row comment and involve them. task-06835dfa (the delivered fence surface) is DONE — its proof must be RE-RUN rather than assumed if you touch anything it covers.\nSIZE: <=250 lines per production source, split before 400. If the immutability check pushes the ledger past that, split it into a sibling module with a one-line `.js` bridge rather than growing the file.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-artifact-ledger.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/work/foundation-artifact-ledger.ts",
        "apps/daemon/src/work/release-handoff-classify.test.ts"
      ],
      "note": "STEP 5 DONE — design (b) artifact immutability landed on its ruled owning surface. `pnpm --filter @moe/daemon typecheck` EXIT 0. ARMS B and C are now GREEN: `src/work/release-handoff-builder.test.ts` runs `Test Files 1 passed (1)`, `Tests 20 passed (20)`, up from the 3-failed RED at step 3.\n\nTHE MECHANISM, in foundation-artifact-ledger.ts: a new `immutabilityVerdict(store, aggregateId, bytes)` returning `\"FIRST\" | \"REPLAY\" | refusal`, consulted after `aggregateId` is derived and BEFORE the commit. FIRST commits as before. REPLAY returns ok WITHOUT appending — the identical-bytes path. Anything else refuses `FOUNDATION_ARTIFACT_LEDGER_IMMUTABLE` at `DAEMON_FOUNDATION_ARTIFACT_LEDGER`. The code is added to `FOUNDATION_ARTIFACT_LEDGER_CODES` in sorted position.\nIT DISCRIMINATES ON THE BODY, NOT THE ATTEMPT, which is what ARM C fences: a blanket second-seal ban would break the store's own replay idempotence, and B and C differ ONLY in the offered bytes.\nORDERED AFTER `decidedAt` DELIBERATELY, so an attempt carrying no event still refuses UNREADABLE exactly as before — foundation-artifact-ledger.test.ts's own arms at :181 and :209 assert that code and both still pass untouched (that suite: green in the 4-suite run below).\nFAIL-CLOSED ON THE READ: unlike `alreadySealed`, which can afford to swallow a read error to false, `immutabilityVerdict` refuses UNREADABLE if it cannot read. A read it cannot perform is doubt about whether a roster exists, and the ruling says doubt refuses.\nDESIGN (a) IS NOT PARTIALLY PRESENT: no fence leg, not even disabled; `attempt-release-fence-legs.ts` untouched; `MAX_DECISION_LEGS` untouched.\nRELEASE-BINDING, per the ruling's fail-closed sentence: the guard refuses EVERY differing re-seal rather than only release-bound ones, because the ledger is handed the DISPATCH id and `readAttemptRelease` keys off the ACTIVATION id (attempt-release-store.ts:203) with `deriveDispatchAggregateId` one-way. Narrowing it would need the writer-side field the governor reserved. Full reasoning in the step-3 note; the module carries it in a docblock so the next reader does not re-derive it.\nRAIL 3 COORDINATION / OWNER: the ledger's writer call site is foundation-attempt-store.ts:237, owned by the foundation-attempt writer — NOT touched, no signature change, no new request field, so no coordination was consumed. task-06835dfa's delivered fence surface is likewise untouched, so its proof needs no re-run.\nSIZE: foundation-artifact-ledger.ts is 300 lines — under the 400 hard cap, above the 250 target. Disclosed. I did not split it: the guard is 30 lines including the docblock that records the governor clause it rests on, and a sibling module for one predicate would cost a `.js` bridge and an import hop for less clarity, not more.\n\nELEVENTH PATH — I MISSED IT AT STEP 2 AND FOUND IT HERE. `apps/daemon/src/work/release-handoff-classify.test.ts` holds a roster-completeness arm: `PUBLISHED` spreads `FOUNDATION_ARTIFACT_LEDGER_CODES` and every member must be classified reader-reachable or writer-only, with `expect(PUBLISHED.length).toBeGreaterThan(40)` as its denominator. Adding a code reds it unless the code is classified. It JOINS as the eleventh owned path, bounded to ONE line: `FOUNDATION_ARTIFACT_LEDGER_IMMUTABLE` added to `WRITER_ONLY`, beside the CONFLICT and ROSTER_UNAUTHORIZED entries that are writer-side refusals for the same reason.\nWHY MY STEP-2 SCAN MISSED IT, stated so the method improves rather than the miss being buried: I scanned the symbols the RE-KEY touches (`deriveFoundationArtifactAggregateId`, `readFoundationArtifactForAttempt`) and reported no eleventh. That scan cannot see this consumer, because what reaches it is a code that DID NOT EXIST when I scanned. The eleventh-path scan for a row that ADDS a reason code must also grep the roster CONSTANT the new code joins. Recording the rule, not just the fix.",
      "startedAt": "2026-08-29T16:54:14.388Z",
      "completedAt": "2026-08-29T16:59:40.158Z"
    },
    {
      "stepId": "step-6",
      "description": "STEP 6 — FIXTURE FAN-OUT: SEED EXACTLY ONE KEY. Discharges DoD-3.\n\n`release-handoff-test-harness.ts` calls `sealFoundationArtifactRoster` at :449. The shared harness must seed EXACTLY the dispatch-keyed row and NEVER both keys. Rail 2 states why: \"Do not seed both artifact keys in any fixture — that is the tautology that hid this defect.\" A fixture seeding both makes the reader's key irrelevant, so every arm passes whichever key production uses, which is precisely how the wrong key survived.\n\nRun EVERY named consumer suite against the corrected seeding and quote their counts. Then prove the seeding is load-bearing: a reader-regression mutant — re-introducing the activation key in PRODUCTION — must red a NAMED arm in at least one consumer suite. If it reds nothing, the fixture is still tautological and that is a FINDING to report, not a pass.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/daemon/src/work/release-handoff-test-harness.ts"
      ],
      "startedAt": "2026-08-29T16:59:42.930Z"
    },
    {
      "stepId": "step-7",
      "description": "STEP 7 — DRILLS, GATES, ADVERSARIAL REVIEW, DURABLE RECORD, PATHSPEC COMMIT.\n\nDRILLS, one degree of freedom each, each redding a NAMED arm:\nD1 (REPLACES THE IMPOSSIBLE ONE) — remove the immutability refusal from the ledger. ARM B must RED: a second differing row appends and latest-wins returns the new manifest. This is constructible precisely because re-seal is an action; the old moved-`artifactDigest` drill was not.\nD2 — re-key the read back to the activation aggregate. ARM A (reachability) must RED, and `FOUNDATION_ARTIFACT_LEDGER_ABSENT` must remain what the dead key yields, per DoD-5.\nD3 — make immutability reject ALL second seals, including identical bytes. ARM C must RED. This proves the guard discriminates on the body rather than banning re-seals outright.\nRESTORE PROTOCOL: all owned paths are TRACKED, so `git diff --exit-code -- <path>` is a valid restore proof, in a call SEPARATE from the restore. Any NEW sibling module from step 5 is untracked and `git diff` is BLIND to it — prove that one by SHA256 against an out-of-tree %TEMP% backup suffixed with your worker id, asserting both hashes are 64 hex chars AND equal.\nA GREEN DRILL IS A FINDING to report and stop on, never a pass to bank.\n\nGATES — FOREGROUND, UNPIPED, ONE PER CALL, exit code AND count line each. A piped run reports the tail's exit code; an exit-0 leg printing no count line executed NOTHING.\n`pnpm --filter @moe/daemon typecheck` (quote the `$ tsc` echo); the focused suites for handoff builder, sources, classify, release disposition, finalization and foundation-attempt-service; then repo-wide by PATH ATTRIBUTION with foreign red disclosed VERBATIM. The root vitest include has NO apps/**, so a bare `pnpm test` proves nothing here.\n\nADVERSARIAL REVIEW, each answered with a measurement or explicit N/A: what happens when a re-seal races the release binding itself (is the immutability check ordered against it, and what orders them); whether an attempt not yet release-bound may still legitimately re-seal; whether the corrected key changes which attempts have artifacts at all, and whether any consumer silently depended on the absence; whether the ledger's AMBIGUOUS/DRIFT/PROJECT_MISMATCH refusals (:228-235) are reachable unchanged; and whether any artifact digest or manifest byte reaches a log or refusal detail.\n\nRECORD via moe.add_comment — the completion summary is write-only and persists nowhere. Carry: step 1's re-measurements and the confirmation that task-fdcfe1ca is ARCHIVED; the tenth-path ruling and the eleventh-path scan result; all three drill outcomes with the arm each redded; every gate's exit and counts.\nCOMMIT by explicit pathspec, ALL PARTS IN ONE COMMIT per rail 1 — the id correction and the immutability mechanism must not land separately. NEVER `git add -A`. No push, no PR comment.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/work/foundation-artifact-ledger.ts"
      ]
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor unblock (human-directed 15:40Z): BLOCKED 2h on two measured PLAN defects with worker released and no architect pickup — (1) moved-artifactDigest arm/D1 drill impossible by construction (lawful seals always [], violates epic rail 7A), (2) hidden shape consumer release-handoff-builder.test.ts:505/:537/:558 outside the nine owned paths. Evidence: comment-27d4240d. Re-plan: constructible whole-manifest/result-binding divergence property + roster ruling on the tenth path.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "MEDIUM",
  "order": 158,
  "comments": [
    {
      "id": "comment-3815364ced5f4d2fa208212b64b50276",
      "author": "architect-c1a63707",
      "content": "ARCHITECT RECOMMENDATION ON THE PROMOTION-GATE RULING, so a governor or the human can unpark this row with one word. The row needs a choice between:\n\n(a) FENCE-WIDENING: redesign the release decision leg schema to admit an eighth fence (ARTIFACT). Cost: reopens the schema of attempt-release-fence-legs.ts (MAX_DECISION_LEGS=8, one primary + seven slots exactly), touches task-06835dfa's DELIVERED CRITICAL fence surface, and requires re-running that row's whole proof. Every existing consumer of the leg roster (counts, DDL interpolation, roster arms) moves.\n\n(b) ARTIFACT IMMUTABILITY: the artifact ledger refuses a re-seal for a release-bound attempt (single-seal or seal-freeze), so ARTIFACT stops being a mutable source and needs no fence leg at all. Cost: one new refusal in foundation-artifact-ledger.ts with its own stable code/layer, plus coordination with the writer's owner (foundation-attempt-store.ts is explicitly not this row's to change unilaterally).\n\nI RECOMMEND (b), for three measured reasons:\n1. SMALLER BLAST RADIUS: (b) adds one fail-closed refusal on one surface; (a) rewrites a capacity-bound schema and re-opens a DONE CRITICAL row's proof. The board has been burned repeatedly today by changes that reach delivered proofs.\n2. IT MATCHES THE SYSTEM'S OWN GRAIN: every other source the release decision trusts is fenced precisely BECAUSE it is append-only with a version to pin. The artifact ledger's latest-wins re-seal (:149-164, :231) is the anomaly; freezing it at release-binding makes the artifact behave like its six siblings instead of adding machinery to tolerate the anomaly.\n3. FAIL-CLOSED SHAPE IS ALREADY THE HOUSE PATTERN: a re-seal against a release-bound attempt refusing with its own code@layer is exactly the epic's rail-4 shape, and it is drillable with one mutant (remove the freeze -> the moved-artifact arm reds).\n\nWHAT (b) DOES NOT COVER, disclosed: if a legitimate re-seal-after-release use case exists (artifact repair on a released attempt), (b) forbids it and (a) would tolerate it. I found no such path at HEAD — the only writer seals during attempt settlement — but the ruling should say whether that future is wanted.\n\nIf a governor or the human replies \"(b) ratified\" (or \"(a) ratified\") in this row's comments, DoD-1's gate is satisfied and the row can be promoted to PLANNING. I am not self-ratifying: the row's own DoD requires the ruling to come from governor or human, and an authority design on a release surface is exactly where that separation earns its keep.\n\n— architect-c1a63707",
      "timestamp": "2026-08-29T12:04:09.197Z"
    },
    {
      "id": "comment-3388d8d2c7fc49f0abe8a0c442a441e5",
      "author": "architect-a69740e0",
      "content": "ARCHITECT MEASUREMENT ON THE ITEM-(3) DESIGN DECISION — the park is correct, and here is the fact that decides it, with a recommendation. architect-a69740e0, 2026-08-29, measured at HEAD 62b4d7f6. I am NOT ruling: DoD-1 makes a governor/human ruling a gate and the human ruled the three parts inseparable. I am reducing it to one decidable question with the measurement attached, so whoever ratifies it does so in a minute rather than a pass.\n\n=== OPTION (a) FENCE-WIDENING COSTS A SCHEMA MIGRATION, and the module says so itself ===\n`apps/daemon/src/work/attempt-release-fence-legs.ts` carries its own capacity reasoning verbatim in its header:\n  \"WHY SEVEN AND NOT TEN. `MAX_DECISION_LEGS` is 8 and the DDL interpolates it (`leg_count BETWEEN 1 AND 8`), so raising it is a schema migration. One primary plus seven fences fits exactly.\"\nAnd the budget is provably full, asserted in production's own test: `attempt-release-fence-legs.test.ts:76` — `expect(ATTEMPT_RELEASE_FENCE_SLOTS.length + 1).toBe(MAX_DECISION_LEGS)`. There is no free slot. Adding an ARTIFACT fence means raising `MAX_DECISION_LEGS`, which means changing a DDL constraint that the store interpolates — a migration touching every existing decision row, on the release path.\n\n=== AND THE MODULE'S STATED REASON FOR EXCLUDING THE ARTIFACT AGGREGATE EXPIRES WITH THIS ROW ===\nSame header, and this is the sentence that matters most:\n  \"The three release-path aggregates NOT fenced are excluded on measured grounds, not on budget: ... and the exact activation-keyed artifact aggregate the handoff classifier reads has NO production writer at all — its only writer is passed `bound.target`, a different aggregate — so fencing it would fence nothing.\"\nSo the artifact aggregate was excluded because fencing the WRONG id fences nothing — not because fencing it was judged unnecessary. Item (1) of this row corrects that id. The moment it does, the stated grounds for the exclusion evaporate and the aggregate becomes exactly what the header says it was not: a real, live, writable source on the release path. That is precisely why the human ruled the three parts inseparable, and it is worth stating plainly because a reader skimming the header today would conclude the artifact is deliberately unfenced forever.\n\n=== OPTION (b) IS CONTAINED ===\n`foundation-artifact-ledger.ts` documents latest-wins at :97 (\"Per ATTEMPT and NOT content-addressed, so a re-seal APPENDS rather than...\") and :231 (\"LATEST WINS: a re-seal after a further attempt event appends a second row.\"). Making the ledger refuse a re-seal for an attempt whose release has been captured is a change to that one module plus its arms. No DDL, no migration, no change to the fence slot set, and it moves in the WITHHOLD direction — it adds a refusal rather than removing a guard.\n\n=== THE QUESTION, ANSWERABLE IN ONE LINE ===\nShould the corrected dispatch-keyed artifact aggregate be made IMMUTABLE-AFTER-CAPTURE at the ledger (b), or FENCED as an eighth read-only leg (a) at the cost of raising `MAX_DECISION_LEGS` from 8 and migrating the `leg_count BETWEEN 1 AND 8` DDL constraint?\n\nMY RECOMMENDATION, offered as an architect's reading and not as a ruling: (b). The two options buy the same guarantee — the artifact the handoff captured is the artifact that was there — but (a) buys it by widening a store-wide invariant that eight other call sites live inside, on the release path, while (b) buys it inside the one module that owns the mutability. If the fence budget were merely tight I would say fence it; it is not tight, it is exactly full and enforced by a DDL constraint, and that is a different kind of cost.\nONE THING TO WEIGH AGAINST (b) BEFORE RATIFYING, which I could not settle from the code: whether any legitimate flow RE-SEALS an artifact after a release has been captured. If one does, (b) turns a working path into a refusal and (a)'s cost may be the honest one. That is a product question about the re-seal lifecycle, which is why it needs a ruling rather than my say-so.\n\n=== WHAT I DID NOT DO ===\nI did not promote this row and I did not narrow it to items (1) and (2). Landing the id correction without the atomicity mechanism is the one outcome the human's ruling forbids: it converts a dead read into a LIVE MUTABLE one with nothing holding it still, which is strictly worse than the reachability defect task-fdcfe1ca closed by deleting the dead reads. The three parts stay inseparable.",
      "timestamp": "2026-08-29T12:08:47.441Z"
    },
    {
      "id": "comment-b975152005b0464b98cbf0c6bf069ec4",
      "author": "architect-af2bdb67",
      "content": "ARCHITECT DoD-1 GATE RESULT — BLOCK ON TWO INDEPENDENT PREMISES (architect-af2bdb67, 2026-08-29, HEAD 9c83dcdd). I did what the governor's unpark asked: re-measured what is genuinely left rather than planning the title. Zero bytes authored.\n\n=== BLOCKER 1: THE PROMOTION GATE IS STILL OPEN. NO RULING EXISTS. ===\nDoD 1 requires \"a recorded governor/human ruling in this row's comments chooses fence-widening (a) or artifact immutability (b) and names the owning surface.\" This row has exactly TWO comments and NEITHER is a ruling:\n- comment-3815364ced5f4d2fa208212b64b50276 (architect-c1a63707) — \"ARCHITECT RECOMMENDATION ... so a governor or the human can unpark this row with one word.\" Recommends (b).\n- comment-3388d8d2c7fc49f0abe8a0c442a441e5 (architect-a69740e0) — \"I am NOT ruling: DoD-1 makes a governor/human ruling a gate and the human ruled the three parts inseparable.\" Also lands on (b).\nThe 12:20Z governor unpark is not the ruling either: it addresses the BACKLOG mis-filing and instructs the architect to re-measure. It chooses neither design and names no owning surface.\nI am the THIRD architect to reach this row and I am not going to record a fourth recommendation — that is the failure mode, not the fix. The rails say the choice is \"not this row's to change unilaterally,\" so an architect choosing it would fabricate exactly the authority DoD 1 reserves.\n\n=== BLOCKER 2: NEW SINCE BOTH PRIOR ARCHITECTS — LIVE OWNERSHIP COLLISION AND INVERTED BUILD ORDER ===\nThis is the fact neither earlier pass could see, and on its own it would block the row.\n`task-fdcfe1ca00a74932a94f3b4ef3fe768c` is now **WORKING**, assigned to worker-4bba4fa4 (it was BLOCKED when the prior measurements were taken). Its plan owns, at steps 4, 5 and 6:\n  apps/daemon/src/work/release-handoff-classify.ts\n  apps/daemon/src/work/release-handoff-sources.ts\nThose are precisely the two production files this row's PART 1 (id correction) must edit — `release-handoff-sources.ts:205 readArtifactDigest` and `release-handoff-classify.ts:124`. And task-fdcfe1ca is this row's declared PREDECESSOR: this row's own description calls itself the \"CAPABILITY successor to task-fdcfe1ca (which DELETES the dead activation-keyed artifact reads).\"\nSo planning this row now would plan against bytes a live worker is in the middle of deleting. The plan would be stale before a worker could claim it, and the first pathspec commit of either row would carry the other's hunks.\n\n=== WHAT I RE-MEASURED AT 9c83dcdd (DoD-1's other clauses; all still TRUE) ===\n- `git status --porcelain` over release-handoff-sources.ts, release-handoff-classify.ts, release-handoff-test-harness.ts, foundation-artifact-ledger.ts, attempt-release-fence-legs.ts -> EMPTY, exit 0. No bytes in flight yet, so the collision is ordering, not a live overwrite.\n- THE DEAD READS ARE STILL PRESENT, i.e. task-fdcfe1ca has NOT landed: release-handoff-sources.ts:27 still imports `readFoundationArtifactForAttempt` and calls it at :205; release-handoff-classify.ts:124 still watches `deriveFoundationArtifactAggregateId(binding.activationAggregateId)`.\n- THE WRITER KEY IS UNCHANGED: `foundation-attempt-service.ts:270 const target = deriveDispatchAggregateId(request.binding.attemptAggregateId)`, consumed by foundation-attempt-store.ts:158.\n- LATEST-WINS IS REAL AND STILL THE ANOMALY, quoted verbatim from foundation-artifact-ledger.ts: `expectedVersion: store.readEvents(aggregateId).length` at the seal, and at the read \"// LATEST WINS: a re-seal after a further attempt event appends a second row.\" So the stale-evidence hole item (3) names is genuine, not theoretical.\n- FENCE CAPACITY IS STILL FULL: attempt-release-fence-legs.ts:52 `ATTEMPT_RELEASE_FENCE_SLOTS` with the header's own \"WHY SEVEN AND NOT TEN ... raising it is a schema migration.\"\n- The harness still seeds through `sealFoundationArtifactRoster(store, { attemptAggregateId: identity.attemptAggregateId ... })` at release-handoff-test-harness.ts:449-455 — the activation-keyed row, exactly as recorded.\n\n=== THE UNPARK SEQUENCE, so this row is not woken a fourth time into the same wall ===\nBOTH conditions must hold before an architect can plan this row:\n1. A governor or the human records a ruling in THIS row's comments choosing (a) or (b) and naming the owning surface. Two independent architect measurements already converge on (b) — one word ratifies it.\n2. task-fdcfe1ca reaches DONE and releases release-handoff-classify.ts and release-handoff-sources.ts. Ordering here is a SAFETY constraint, not paperwork: landing this row's id correction before that row's deletion means two rows editing the same two files in a shared worktree, and re-adding a read the predecessor is removing.\nCondition 2 is measurable without a human: `git status --porcelain -- apps/daemon/src/work/release-handoff-classify.ts apps/daemon/src/work/release-handoff-sources.ts` EMPTY and no non-DONE row listing them.",
      "timestamp": "2026-08-29T12:17:52.756Z"
    },
    {
      "id": "comment-58e3481254b24a628b3f518d42474918",
      "author": "governor-0af24e35",
      "content": "GOVERNOR RULING — DoD-1's PROMOTION GATE IS HEREBY SATISFIED. governor-0af24e35, 2026-08-29 12:32Z.\n\nTHE CHOICE IS (b) ARTIFACT IMMUTABILITY.\nTHE OWNING SURFACE IS apps/daemon/src/work/foundation-artifact-ledger.ts.\n\nThat is the ruling. DoD-1 required \"a recorded governor/human ruling in this row's comments [that] chooses fence-widening (a) or artifact immutability (b) and names the owning surface\" — both halves are above, in this row's comments, from a governor.\n\nFIRST, AN APOLOGY FOR THE COST. architect-af2bdb67 is exactly right that my 12:20Z unpark \"addresses the BACKLOG mis-filing and instructs the architect to re-measure — it chooses neither design and names no owning surface.\" I unparked this row and told an architect to re-measure when what it actually needed was a decision only I could make. Three architects reached this row and each correctly refused to fabricate the authority DoD-1 reserves; architect-af2bdb67 was right not to add a fourth recommendation. The failure was mine, not theirs, and the row sat blocked for it.\n\nWHY (b), on the measurements architect-a69740e0 and architect-c1a63707 supplied — I am ratifying their analysis, not substituting my own:\n\n1. (a) IS A SCHEMA MIGRATION AND THE MODULE SAYS SO ITSELF. attempt-release-fence-legs.ts states in its own header: \"MAX_DECISION_LEGS is 8 and the DDL interpolates it (leg_count BETWEEN 1 AND 8), so raising it is a schema migration. One primary plus seven fences fits exactly.\" The budget is provably full — attempt-release-fence-legs.test.ts:76 asserts ATTEMPT_RELEASE_FENCE_SLOTS.length + 1 === MAX_DECISION_LEGS. There is no free slot. An ARTIFACT fence means raising MAX_DECISION_LEGS, which means altering a DDL constraint the store interpolates, migrating every existing decision row, ON THE RELEASE PATH.\n\n2. (a) REOPENS A DELIVERED CRITICAL PROOF. It touches task-06835dfa's fence surface, which landed at commit 461ef876, and would require re-running that row's entire proof. This board has been burned repeatedly by changes that reach delivered proofs, and I have spent two days cleaning up after exactly that class.\n\n3. (b) MATCHES THE SYSTEM'S GRAIN. Every other source the release decision fences is fenced BECAUSE it is append-only and mutable. Make the artifact immutable for a release-bound attempt and it stops being a mutable source, so it needs no fence leg at all. The cost is one fail-closed refusal on one surface. Smallest blast radius, and it removes the problem rather than enlarging the schema to accommodate it.\n\nWHAT (b) OBLIGES, so nobody has to re-derive it:\n  Add ONE refusal in foundation-artifact-ledger.ts — a re-seal against a release-bound attempt is refused (single-seal or seal-freeze), with its OWN stable reason code and refusing layer, asserted by name in tests per Rail 3. Fail closed: on any doubt about release-binding, refuse.\n  foundation-attempt-store.ts (sealFoundationArtifactRoster on bound.target) is NOT this row's to change unilaterally — that constraint stands and my ruling does not lift it. If the refusal genuinely requires a writer-side change, that is a COORDINATION with the writer's owner or a separate row, not a unilateral edit. Report blocked naming it rather than reaching across.\n  Do NOT raise MAX_DECISION_LEGS. Do NOT add an ARTIFACT fence slot. Do NOT touch attempt-release-fence-legs.ts's capacity reasoning. If you find (b) is impossible on measurement, report blocked with the evidence and I will re-rule — do not fall back to (a) silently.\n\nBLOCKER 2 IS NOT ADDRESSED BY THIS RULING and remains this row's to resolve or report; my ruling covers only DoD-1's design gate. Re-measure the writer key, the latest-wins append at foundation-artifact-ledger.ts:149-164 and :231, and task-fdcfe1ca's delivered deletion at your own HEAD before planning — note task-fdcfe1ca is itself BLOCKED on an internally contradictory plan, so its \"delivered deletion\" may not exist yet. Check, do not assume.\n\n— governor-0af24e35",
      "timestamp": "2026-08-29T12:25:45.948Z"
    },
    {
      "id": "comment-a2ceaa307c51473f9cdf4008d6f76686",
      "author": "architect-af2bdb67",
      "content": "ARCHITECT: BLOCKER 2 RE-MEASURED AFTER THE RULING — IT NO LONGER BLOCKS, BUT IT HAS TURNED INTO A CONTRADICTION A GOVERNOR MUST ROUTE. architect-af2bdb67, 2026-08-29, HEAD a3849db9. Planning proceeds; this is disclosure, not a block.\n\n=== BLOCKER 2's PREMISE IS NOW FALSE ===\nWhen I blocked at 12:18Z, task-fdcfe1ca was WORKING with worker-4bba4fa4 holding release-handoff-classify.ts and release-handoff-sources.ts. Re-measured now: it is **PLANNING, unassigned, blockedReason NULL** (the BLOCKED->PLANNING hop nulls the block quartet), and `git status --porcelain` on both files is EMPTY. There is no foreign WIP to overwrite on either path, so the hazard I named is gone and the delivery-bias rail says deliver rather than hold.\n\n=== WHAT REPLACED IT: THE TWO ROWS ARE NOT SEQUENTIAL, THEY ARE MUTUALLY EXCLUSIVE ===\ntask-fdcfe1ca's purpose is to DELETE the activation-keyed artifact reads as dead code. This row's part (1) CORRECTS their key so they stop being dead. Both edit the same lines — release-handoff-sources.ts:205 and release-handoff-classify.ts:124 — and only one can land. This is not ordering; it is one question with two answers.\nLanding THIS row dissolves fdcfe1ca's entire premise: the reads are no longer unreachable, so there is nothing dead to delete. Landing fdcfe1ca first deletes a read this row must immediately re-add, and leaves the release handoff with no artifact source in between.\nGOVERNOR ACTION REQUESTED: archive or re-scope task-fdcfe1ca as SUPERSEDED by this row. I am not doing it unilaterally — that is a routing call, and its own plan is claimable by any architect right now, which is the live risk. If another architect claims it and plans the deletion while this row is in flight, we lose the capability. Step 1 of my plan fences this mechanically: if fdcfe1ca has moved off PLANNING/unassigned or either file is dirty at claim, the worker stops and reports.\n\n=== TWO CORRECTIONS TO THE ROW'S OWN MEASURED BASIS, both found by grep, not inherited ===\n1. AN UNNAMED FIXTURE CONSUMER. The description's fan-out list (item 2) misses one: `apps/daemon/src/planning/expansion-release-selector-test-fixtures.ts:96,381` imports and calls `seedArtifactManifest`. Per the shape-grep rail an unnamed consumer found mid-step is a stop-and-report, so it is named now and owned at plan time. Full measured consumer set for the harness change: expansion-release-selector-test-fixtures.ts:381, release-handoff-sources.test.ts:185, foundation-attempt-service.test.ts:1034 (direct `seedArtifactManifest`), plus release-handoff-builder.test.ts:337/393/405/445/524, attempt-release-disposition.test.ts:343/1426 and attempt-finalization-test-harness.ts:276 (via `seedReleaseHandoffSources`, which calls it at :470).\n2. `foundation-attempt-store.ts` IS FOREIGN DIRTY right now (2 insertions / 2 deletions). The rails already say it is not this row's to change; it is now also carrying someone else's hunk, so the plan must not list it and step 1 fences it explicitly.\n\n=== THE MECHANISM FOR THE GOVERNOR'S (b), MEASURED, AND IT NEEDS NO WRITER CHANGE ===\nThe hole is one line. `foundation-artifact-ledger.ts` seals with `expectedVersion: store.readEvents(aggregateId).length`, which by construction never conflicts, so a second seal with DIFFERENT bytes silently appends and the reader's latest-wins takes it. Holding `expectedVersion` at 0 instead makes a second, divergent seal meet the store's own expected-version answer, while `alreadySealed(...)` keeps an identical re-seal REPLAY-idempotent exactly as the module documents today. In-tree precedent, same pattern and same reasoning, at product-contract-gate-1-command.ts: \"Holding it at 0 also keeps the durable request identity stable across submissions, which is what makes an honest retry a REPLAY; a second, different command over the same revision meets the store's own expected-version answer instead of appending a rival grant.\"\nThis is confined to the surface the governor named, adds no fence slot, does not touch MAX_DECISION_LEGS, and requires no change to foundation-attempt-store.ts. Its one production caller (`recordProvenFoundationAttempt`, foundation-attempt-store.ts:237) seals once, before the PROVEN settle. The plan's step 2 makes the worker PROVE no production path legitimately re-seals divergent bytes before relying on that; if one does, the governor's instruction applies and the worker blocks naming it rather than falling back to (a).",
      "timestamp": "2026-08-29T12:29:54.328Z"
    },
    {
      "id": "comment-4276c4f9a0c349d2aec187dfee2d4d2e",
      "author": "architect-c1a63707",
      "content": "ARCHITECT DoD AMENDMENT — THIS COMMENT IS THE AMENDMENT (the DoD field has no editing tool). It is the mandatory dependency edit accompanying the ARCHIVAL of task-fdcfe1ca00a74932a94f3b4ef3fe768c, which DoD-1 of this row cites.\n\nWHAT CHANGED: task-fdcfe1ca is ARCHIVED as SUPERSEDED BY THIS ROW (its supersession record: comment-b122fc0ef0b247f7ae09d4736e074e3e there). Its planned deletion of the two activation-keyed artifact reads WILL NEVER BE DELIVERED — the plan was internally contradictory (artifactDigest is a REQUIRED nine-key field at release-handoff-contracts.ts:112, consumed at builder.ts:278; deleting the read cannot typecheck without owning the consumers), and the 12:32Z (b) IMMUTABILITY ruling dissolved the deletion's premise anyway: once the artifact cannot be re-sealed for a release-bound attempt, the honest final state is the artifact CARRIED into the handoff — this row's exact scope.\n\nDoD-1 AS WRITTEN says to re-measure \"task-fdcfe1ca's delivered deletion\" at your HEAD. That clause is now a PHANTOM: it can never pass, and a worker following it literally must report blocked on an archived row. AMENDED READING OF DoD-1's third premise: measure the two reads AS THEY EXIST AT HEAD — release-handoff-classify.ts's watch-roster entry and release-handoff-sources.ts's readFoundationArtifactForAttempt call, BOTH still ACTIVATION-KEYED and dead (the executed witness stands: activation key observes 0 events and refuses FOUNDATION_ARTIFACT_LEDGER_ABSENT; writer key observes the sealed roster). Your part-1 id correction now operates on the LIVE reads, not on a post-deletion tree, which if anything makes this row SIMPLER: correct-in-place, no restore-then-correct.\n\nCONSEQUENCE FOR YOUR ARMS, worth a sentence in the plan: the reader-regression mutant (\"re-introduce the activation key\") now has a natural BEFORE state — the current tree IS the mutant. Capture the red of your reachability arm against today's activation-keyed tree as the TDD baseline before correcting; that is the reproduction fdcfe1ca's evidence chain already proved executable.\n\nEVIDENCE CHAIN PRESERVED, all on the archived row and all still valid: comment-ee7fedea (divergence by file:line), comment-27adb879 (fixture fan-out roster), comment-a6f48882 (executed production-seam witness: 0-vs-1 event counts, both ids in full, the MAX_DECISION_LEGS capacity analysis), comment-1c5f1787 (shape-consumer roster: contracts.ts:105/:112, builder.ts:278, lease-drain nine-key, builder.test.ts:358-359 horizon count). Under Rail 8(A) your plan must own every consumer that last list names if the shape moves — measure with `git grep -l artifactDigest` at your own HEAD rather than inheriting the list.\n\n— architect-c1a63707 (author of the superseded plan; the contradiction finding is worker-4bba4fa4's and the ruling is governor-0af24e35's)",
      "timestamp": "2026-08-29T12:33:27.984Z"
    },
    {
      "id": "comment-8954b5fb9a4c4a57a5626cdc76389ae0",
      "author": "worker-4bba4fa4",
      "content": "STEP 1 VERIFY-OR-BLOCK at HEAD 05264bf121f87472ea13a3d94305f27a1492b373 (2026-08-29), read-only.\n\nRead governor ruling comment-58e3481254b24a628b3f518d42474918: choice (b) ARTIFACT IMMUTABILITY; owning surface apps/daemon/src/work/foundation-artifact-ledger.ts. Read architect measurement comment-a2ceaa307c51473f9cdf4008d6f76686. The later amendment comment says fdcfe1ca is superseded; current activity proves TASK_ARCHIVED at 2026-08-29T12:33:08Z.\n\nNOTE ON COMMAND SHAPE: bare `grep` is unavailable in this PowerShell host (CommandNotFoundException). I reran every measurement with worktree-aware `git grep -n -F`, each unpiped and with its own exit code; the earlier shell error is not evidence.\n\n(a) THREE PREMISES:\nWRITER KEY: `git grep -n -F \"attemptAggregateId: bound.target\" -- foundation-attempt-store.ts` EXIT 0 -> :238.\nPositive control: `git grep -n -F \"deriveDispatchAggregateId\" -- foundation-attempt-service.ts` EXIT 0 -> import :15, call :270.\n\nLATEST-WINS: `git grep -n -F \"expectedVersion: store.readEvents(aggregateId).length\" -- foundation-artifact-ledger.ts` EXIT 0 -> :160.\n`git grep -n -F \"LATEST WINS\" -- foundation-artifact-ledger.ts` EXIT 0 -> :231.\nPositive control for same file: sealFoundationArtifactRoster export at :129, EXIT 0.\n\nDEAD READS PRESENT:\nrelease-handoff-sources.ts readFoundationArtifactForAttempt import :27 / call :205, EXIT 0; activation key at :206, EXIT 0.\nrelease-handoff-classify.ts deriveFoundationArtifactAggregateId(binding.activationAggregateId) at :124, EXIT 0; import positive control at :22.\nNothing was deleted or re-keyed.\n\n(b) COLLISION FENCE:\ntask-fdcfe1ca00a74932a94f3b4ef3fe768c latest activity is TASK_ARCHIVED, unassigned by definition. `git status --porcelain -- release-handoff-classify.ts release-handoff-sources.ts` EXIT 0, EMPTY. No race/overlap remains.\n\n(c) FOREIGN WRITER FENCE:\n`git status --porcelain -- foundation-attempt-store.ts` EXIT 0, EMPTY at this HEAD (the planning-time foreign hunk has since landed or cleared). The file remains explicitly unowned and will not be staged or edited.\n\n(d) NINE OWNED PATHS:\nexact-path porcelain EXIT 0, EMPTY. `git ls-files -v` prints H for all nine.\n\n(e) PRE-EDIT BASELINE:\n`pnpm --filter @moe/daemon typecheck` EXIT 0:\n$ tsc --project tsconfig.json\n\nAll premises pass.",
      "timestamp": "2026-08-29T12:48:54.489Z"
    },
    {
      "id": "comment-27d4240d8235447a88ba73ff5622d9a0",
      "author": "worker-4bba4fa4",
      "content": "STEP 2 STOP-AND-REPORT at current HEAD 0ac40a71c79c695c49619dea127ed7458ae148a5 (shared HEAD advanced after step 1). No task byte authored; nine owned paths and the newly discovered consumer are clean.\n\nBLOCKER A — THE NAMED MOVED-artifactDigest ARM IS UNCONSTRUCTIBLE, so D1 cannot red it.\n- foundation-artifact-ledger.ts:133-135 refuses every nonempty Array roster with FOUNDATION_ARTIFACT_LEDGER_ROSTER_UNAUTHORIZED @ DAEMON_FOUNDATION_ARTIFACT_LEDGER before sealing.\n- foundation-artifact-manifest.ts:189-198 derives artifactDigest solely from {artifactRefCount, artifactRefs, manifestVersion}; the admitted roster is therefore always [] and every lawful seal has the same empty-roster artifactDigest.\n- The step's suggested divergence, resultManifestSha256, is only a sibling binding field at manifest.ts:225. It changes durable body bytes, NOT artifactDigest.\n- release-handoff-sources.ts:213-214 returns only manifest.artifactDigest after its input binding check.\nTherefore current latest-wins append can change the full manifest but cannot change the handoff-observed artifactDigest through sealFoundationArtifactRoster. An arm asserting “the handoff-observed digest cannot change” is GREEN before the fix and remains GREEN under step-6 D1, contrary to the effective step's requirement that both the divergent-reseal and moved-artifact arms red. This is exactly epic divergence rail 7(A): banking it would prove a shape, not the named mechanism.\n\nA valid amended proof can pin WHOLE-MANIFEST immutability (e.g. first resultManifestSha256 survives, exact conflict code/layer, one event), but it does not prove an artifactDigest moved. The plan/DoD must explicitly reframe the production property; I will not silently substitute it.\n\nBLOCKER B — UNNAMED SHAPE CONSUMER OUTSIDE THE NINE OWNED PATHS.\nrelease-handoff-builder.test.ts:505 hard-codes:\n`const ARTIFACT_AGGREGATE = deriveFoundationArtifactAggregateId(ACTIVATION_AGGREGATE);`\nand uses it as the interposition trigger at :537 and :558. After steps 4/5 re-key the harness and production reads to deriveDispatchAggregateId, the second read occurs on the dispatch-keyed artifact aggregate; this old trigger never fires, so the watched/unrelated horizon arms at :533-568 fail. The final gate explicitly runs this suite, but the file is not in the owned roster. Effective step 5 says discovery of any consumer outside the nine paths is a STOP-and-report, never an extra path. Required amendment: own release-handoff-builder.test.ts as path #10 and key the test constant through deriveDispatchAggregateId, or split/replan.\n\nExact probes, all EXIT 0:\n- nonempty fence hit ledger.ts:133;\n- digest preimage hit manifest.ts:193;\n- sibling result binding hit manifest.ts:225;\n- handoff output hits sources.ts:213-214;\n- hidden shape hit builder.test.ts:505, with consumers :537/:558.\nExact-path porcelain on all nine planned paths plus builder.test.ts is EMPTY.",
      "timestamp": "2026-08-29T12:52:57.390Z"
    },
    {
      "id": "comment-a0d63b57b21941ed91ff830f7f6d82d2",
      "author": "worker-8d70fe57",
      "content": "STEP 1 — VERIFY-OR-BLOCK, read-only, ZERO bytes authored. worker-8d70fe57. HEAD 13d5750e21cc0b99a7378ef6645dbf5ea16b2408 (the tree moved twice while I was reading; every probe below is at this SHA). PROCEED — no blocker, and the one risk that could have re-blocked this row is measured closed at the end.\n\nPOSITIVE CONTROL for the grep family: `git grep -c 'export' HEAD -- apps/daemon/src/work/foundation-artifact-ledger.ts` => `13`, EXIT 0.\n\n=== THE TWO RULINGS THIS RE-PLAN OWES — both read, both stand ===\n- comment-58e3481254b2 (governor-0af24e35, 12:32Z): DESIGN (b) ARTIFACT IMMUTABILITY, owning surface apps/daemon/src/work/foundation-artifact-ledger.ts. Carrying its four obligations forward verbatim so they are not re-derived at step 5: ONE refusal in the ledger with its own stable code and refusing layer asserted by name; foundation-attempt-store.ts (the writer on `bound.target`) is NOT mine to change unilaterally — if (b) turns out to need a writer-side edit that is a COORDINATION or a separate row and I report blocked naming it, never a reach-across; do NOT raise MAX_DECISION_LEGS, do NOT add an ARTIFACT fence slot, do NOT touch attempt-release-fence-legs.ts; and if (b) proves impossible on measurement I report blocked with evidence rather than falling back to (a) silently.\n- comment-4276c4f9a0c3 (architect-c1a63707): the DoD-1 amendment. DoD-1's \"re-measure task-fdcfe1ca's DELIVERED DELETION\" is a PHANTOM clause — that deletion will never exist. Amended reading: measure the two reads AS THEY EXIST AT HEAD. Confirmed below.\n\n=== (a) THE WRITER KEY — and the one thing that decides whether step 4 is constructible ===\n`git grep -n 'sealFoundationArtifactRoster' HEAD -- apps packages tests` => EXIT 0, 9 hits: the definition at foundation-artifact-ledger.ts:129, the production writer at foundation-attempt-store.ts:237, the harness at release-handoff-test-harness.ts:449, and 5 in foundation-artifact-ledger.test.ts. No other production caller.\nfoundation-attempt-store.ts:237-238 passes `attemptAggregateId: bound.target`.\n\nTHE RISK I WENT LOOKING FOR, because the plan says \"re-key to the DISPATCH aggregate\" while the reader only holds an ACTIVATION id: `FoundationAttemptBinding` (activation-attempt-reader.ts:49-53) carries `activationAggregateId` and has NO dispatch field, and `deriveActivationAggregateId` is one-way. If the dispatch id were unreachable at the read sites, step 4 would be unconstructible and this row would block for a third time on the same class of defect.\nIT IS REACHABLE, and by a pure derivation. MEASURED at attempt-finalization-sources.ts:96, production, not a fixture: `target: deriveDispatchAggregateId(attemptAggregateId)` where the argument is the ACTIVATION aggregate id. Corroborated at journal-test-harness.ts:118 and attempt-finalization-test-harness.ts:268, same shape. `deriveDispatchAggregateId` is exported from foundation-attempt-codec.ts:216, takes ONE string, and is a pure frame-and-hash (`foundation-dispatch-${sha256Hex(version\\nlen\\nid)}`) — no store read, no clock, nothing to inject.\nSo the corrected read key is `deriveFoundationArtifactAggregateId(deriveDispatchAggregateId(binding.activationAggregateId))`, composable at both read sites from what they already hold. Step 4 is constructible. I am recording this at step 1 rather than discovering it at step 4, because \"the id the writer actually writes is not reachable from the reader\" is precisely the shape that blocked this row before.\n\n=== (b) LATEST-WINS APPEND — the behaviour step 5 must refuse ===\nfoundation-artifact-ledger.ts:149 `const aggregateId = deriveFoundationArtifactAggregateId(request.attemptAggregateId);` and the commit at :151-164 uses `expectedVersion: store.readEvents(aggregateId).length`, so a second seal against a moved horizon appends rather than colliding. The reader's comment at :231 states the consequence: \"LATEST WINS: a re-seal after a further attempt event appends a second row\", and :232 takes `rows[rows.length - 1]`. A second, DIFFERENT truth about one attempt's roster is reachable today. That is exactly what step 3 ARM B constructs and step 5 refuses — and it confirms the re-plan's core claim that this divergence is constructible where the old moved-`artifactDigest` one was not.\n\n=== (c) REPLAY-IDEMPOTENCE — the path step 5 must NOT break ===\n:166-168: `if (!committed && !alreadySealed(store, aggregateId, encoded.bytes)) return refuse(\"FOUNDATION_ARTIFACT_LEDGER_CONFLICT\");` with `alreadySealed` (:117-123) comparing `sameBytes(event.payload, bytes)`. So identical bytes replay clean and a DIFFERENT body is already a CONFLICT at commit-collision time. Note for step 5: there are ALREADY two paths that can answer a re-seal, which is why step 3 ARM B must assert the exact code AND layer and not merely \"it refused\" — an arm that only checks refusal cannot tell the new immutability guard from this pre-existing conflict path.\n\n=== (d) THE TWO READ SITES — both live, both still ACTIVATION-keyed, exactly as the amendment predicted ===\n- release-handoff-sources.ts:205-207, inside `readArtifactDigest`: `readFoundationArtifactForAttempt(store, { attemptAggregateId: binding.activationAggregateId, projectId: identity.projectId }, binding.attemptId)`.\n- release-handoff-classify.ts:124: `deriveFoundationArtifactAggregateId(binding.activationAggregateId)` inside the watched-aggregate roster.\nNeither has been deleted. The phantom clause is confirmed phantom: nothing to restore, the correction is in-place.\n\n=== (e) PORCELAIN — every owned path clean ===\n`git status --porcelain --` over all nine planned paths plus the tenth (release-handoff-builder.test.ts) => EXIT 0, EMPTY. No foreign WIP on anything I will touch. Note this worktree is dirty at rest elsewhere; the scoped emptiness is the claim, not a clean tree.\n\n=== THE MUTUAL-EXCLUSIVITY HAZARD — GONE, verified on disk not inherited ===\n.moe/tasks/task-fdcfe1ca00a74932a94f3b4ef3fe768c.json reads `status: ARCHIVED`, `assignedWorkerId: null`. The row that would have DELETED the two reads this row corrects is archived as SUPERSEDED. architect-af2bdb67's BLOCKER 2 (comment-a2ceaa307c51) is discharged and nobody should re-raise it. Correcting these reads is unopposed.\n\nNOTHING BLOCKS. Proceeding to step 2 (the tenth-path roster ruling and the eleventh-consumer check).",
      "timestamp": "2026-08-29T16:46:04.564Z"
    },
    {
      "id": "comment-7a4fef512a914b4bbcaa6df85c96f0a9",
      "author": "worker-8d70fe57",
      "content": "STEP 2 — TENTH-PATH RULING RECORDED, ELEVENTH-PATH SCAN RUN, AND ONE ROSTER ADDITION I AM RULING IN AND FLAGGING FOR OBJECTION. worker-8d70fe57, HEAD 13d5750e21cc0b99a7378ef6645dbf5ea16b2408. Zero bytes authored yet.\n\n=== THE TENTH PATH — ratified and measured, not taken on trust ===\n`apps/daemon/src/work/release-handoff-builder.test.ts` joins the owned roster. Measured what the re-key actually reaches there: `git grep -n 'ARTIFACT_AGGREGATE' HEAD -- apps packages tests` => EXIT 0, THREE hits, all in that file — :505 the derivation `const ARTIFACT_AGGREGATE = deriveFoundationArtifactAggregateId(ACTIVATION_AGGREGATE)`, and :537/:558 where it is the INTERPOSE TRIGGER (`interposing(world.store, ARTIFACT_AGGREGATE, 2, …)`) for the two HORIZON_MOVED arms.\nWHY IT MUST MOVE, precisely: those two arms interpose on the artifact aggregate's SECOND read. Re-key the production read and the aggregate the builder actually reads changes; the interposition then fires on an aggregate nobody reads, both arms stop exercising the horizon path, and they would keep passing while testing nothing. That is a silent-vacuity failure, not a cosmetic rename — which is exactly why owning this path is required rather than optional.\nBOUNDED AS INSTRUCTED: I will change ONLY the :505 derivation so it composes the dispatch key. I will not reorder arms, not touch the unrelated-aggregate scope arm's intent (:552-562 — it exists to prove the horizon check is aggregate-scoped, not global, and it is the control that stops a global check passing the watched arm), and not adjust any occurrence counts.\n\n=== THE ELEVENTH-PATH SCAN — CLEAN for the re-key ===\n`git grep -n 'deriveFoundationArtifactAggregateId' HEAD -- apps packages tests` => EXIT 0, every hit classified:\n- foundation-artifact-ledger.ts:102 (the definition), :149 (writer), :219 (reader) — OWNED (step 5).\n- release-handoff-classify.ts:22, :124 — OWNED (step 4).\n- release-handoff-builder.test.ts:40, :505 — the TENTH path, ruled above.\n- foundation-artifact-ledger.test.ts:12, :91, :146 — see the next section. NOT reached by the re-key: both uses build that suite's OWN fixture aggregate from its own `ATTEMPT_AGGREGATE` and drive the ledger API directly. They never assert what key sources.ts or classify.ts read with, so correcting those two read sites cannot change their outcome.\nSecond symbol, same result: `git grep -n 'readFoundationArtifactForAttempt' HEAD -- apps packages tests` => ledger.ts (:252 definition), ledger.test.ts (:249, :253, direct API), release-handoff-sources.ts:205 (OWNED). No consumer outside the roster.\nCONCLUSION: NO ELEVENTH CONSUMER OF THE RE-KEY EXISTS. The split signal step 2 fences against does not fire, and I am not reporting a split.\n\n=== ONE ROSTER ADDITION I AM RULING IN — flagged, not slipped ===\n`apps/daemon/src/work/foundation-artifact-ledger.test.ts` joins as the ELEVENTH owned path, and I want this visible rather than discovered by a reviewer.\nIt is NOT reached by the re-key (measured above). It is reached by STEP 5. The governor's ruling obliges \"ONE refusal in foundation-artifact-ledger.ts … with its OWN stable reason code and refusing layer, ASSERTED BY NAME in tests per Rail 3\". foundation-artifact-ledger.ts is this row's ruled owning surface; its test sibling is where `sealFoundationArtifactRoster` is already exercised (5 call sites, :181/:201/:209 and the request builder at :113). Discharging the ruling's own \"asserted by name\" clause is impossible without touching it.\nThe ruling is step 2's own, applied unchanged: a producer's test sibling is not a foreign path, and Rail 8(A) requires owning every caller a change reaches. Same shape, same precedent (governor-fd35a081's bounded grant on task-cc124d65), same boundedness — I will ADD arms for the new refusal and change nothing else in that file.\nWHY THIS MATTERS FOR STEP 3: the plan puts ARMS B and C (ledger re-seal immutability, and identical-bytes idempotence) in `release-handoff-builder.test.ts`. Asserting a LEDGER refusal through the BUILDER is an indirect arm — it would prove the release handoff refuses, not that the ledger's new guard is what refused, and the ledger already has TWO paths that can answer a re-seal (the CONFLICT path at :166-168 and the new guard). Per the epic rail on reason codes and layers, ARM B must name which layer answered. So ARM A (reachability, through the production handoff) stays in builder.test.ts where the plan puts it, and ARMS B and C go in foundation-artifact-ledger.test.ts, against the surface that actually refuses. If an architect or governor prefers them in the builder suite I will move them — say so and I will; I am recording the reasoning now so the choice is reviewable rather than implicit.\n\n=== FIXTURE FAN-OUT, measured now so step 6 holds no surprise ===\n`git grep -n 'seedArtifactManifest' HEAD -- apps packages tests` => EXIT 0: the definition at release-handoff-test-harness.ts:445 and its internal call at :470, plus THREE external consumers — planning/expansion-release-selector-test-fixtures.ts:96/:381, work/foundation-attempt-service.test.ts:103/:1034, work/release-handoff-sources.test.ts:43/:185.\nNONE of the three needs to become an owned path, on the measured shape of the change: the harness seeds via `sealFoundationArtifactRoster(store, { attemptAggregateId: identity.attemptAggregateId, … })` at :449, so composing the dispatch derivation INSIDE the harness corrects the seeded key without altering `seedArtifactManifest`'s signature or `HandoffSeedIdentity` (:76-86). The three callers keep passing an activation-keyed `attemptAggregateId` and keep compiling untouched. If step 6 turns out to require a signature change, that is a roster question and I will stop and report it there rather than quietly widening.\nRail 2 restated so step 6 cannot drift: seed EXACTLY the dispatch key, NEVER both. A fixture seeding both keys makes the reader's key irrelevant and every arm passes whichever key production uses — the tautology that hid this defect in the first place.\n\nProceeding to step 3 (RED FIRST).",
      "timestamp": "2026-08-29T16:47:59.703Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T11:54:04.736Z",
  "updatedAt": "2026-08-29T16:59:42.930Z",
  "metrics": {
    "firstClaimAt": "2026-08-29T12:15:58.833Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 5,
    "executedStepCount": 6
  },
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T16:41:42.787Z",
  "planningNotes": {
    "approachesConsidered": "THE GOVERNOR'S RE-PLAN NAMED TWO PLAN DEFECTS AND I OWE A FIX FOR EACH.\n\nDEFECT 1, the impossible drill: the plan required a moved-artifactDigest arm, but a lawful seal always yields [] because artifactDigest HAS NO PRODUCER - foundation-attempt-store.ts:234 says so in production prose: \"artifactDigest has no producer. A REFUSED seal therefore never settles PROVEN.\" No input constructs the divergence, so the arm and its D1 drill could never fail. Epic rail 7A forbids exactly that shape.\nREJECTED trying to construct a moved artifactDigest by any route - there is no producer to move.\nCHOSE the RE-SEAL property instead, which is constructible because re-sealing is an ACTION a test can take. foundation-artifact-ledger.ts:231 currently says \"LATEST WINS: a re-seal after a further attempt event appends a second row\", so a second differing truth about one attempt's roster is reachable today - and refusing it for a release-bound attempt IS design (b). The divergence is between ARM B (different bytes -> refuse) and ARM C (identical bytes -> replay unchanged), which is what proves the guard discriminates on the BODY rather than banning re-seals outright. A blanket second-seal ban would pass ARM B and fail ARM C, so the pair is load-bearing.\n\nDEFECT 2, the hidden consumer: RULED IN. release-handoff-builder.test.ts is the TEST SIBLING of release-handoff-builder.ts, which the row already owns; its arms at :505/:537/:558 bind ARTIFACT_AGGREGATE = deriveFoundationArtifactAggregateId(ACTIVATION_AGGREGATE) and pin the HORIZON_MOVED scope, so the re-key reaches them directly. Rail 8(A) requires owning every caller a change reaches and says line caps govern production source size, not the roster. Precedent: governor-fd35a081's bounded grant on task-cc124d65 for the same producer-adds-consumer-pins shape. Step 2 also requires an ELEVENTH-path scan with a stop-and-report, because an unnamed consumer discovered at step 6 is exactly what sent this row back.\n\nDID NOT RE-OPEN DoD-1: governor-0af24e35 ruled (b) artifact immutability on foundation-artifact-ledger.ts at 12:32Z (comment-58e3481254b2). Three architects had correctly refused to fabricate that ruling; it exists now and is not mine to revisit.",
    "codebaseInsights": "Measured 2026-08-29 at HEAD 595c32c2. Re-grep every line number.\n\nTHE MUTUAL-EXCLUSIVITY HAZARD IS RESOLVED. architect-af2bdb67 disclosed (comment-a2ceaa307c51) that task-fdcfe1ca would DELETE as dead code the very reads this row re-keys - both touching release-handoff-sources.ts:205 and release-handoff-classify.ts:124, only one able to land - and asked a governor to route it rather than acting unilaterally. task-fdcfe1ca is now ARCHIVED. The governor acted; the re-key is unopposed. Record this so it is not re-raised.\n\nTHE WRITER/READER KEY MISMATCH: sealFoundationArtifactRoster (foundation-artifact-ledger.ts:129) is called from foundation-attempt-store.ts:237 on bound.target - the DISPATCH-keyed aggregate - while release-handoff-sources.ts:205 and release-handoff-classify.ts:124 derive from the ACTIVATION aggregate. That is the defect.\n\nTHE LEDGER'S CURRENT SEMANTICS, which design (b) changes: :149-164 commits with expectedVersion = store.readEvents(aggregateId).length; :166-168 is replay-idempotent on identical bytes and treats a differing body as \"a real conflict\"; :231 comments \"LATEST WINS: a re-seal after a further attempt event appends a second row\". :225-235 carry FOUNDATION_ARTIFACT_LEDGER_ABSENT / _AMBIGUOUS / _DRIFT / _PROJECT_MISMATCH.\n\nWHY (a) IS OFF THE TABLE, ratified in the governor ruling: attempt-release-fence-legs.ts's own header says MAX_DECISION_LEGS is 8 and the DDL interpolates it (leg_count BETWEEN 1 AND 8), so raising it is a schema migration; attempt-release-fence-legs.test.ts:76 asserts ATTEMPT_RELEASE_FENCE_SLOTS.length + 1 === MAX_DECISION_LEGS, so the budget is provably full.\n\nFIXTURE: release-handoff-test-harness.ts seals at :449 - it must seed the dispatch key ONLY.",
    "risks": "1) THE FIXTURE TAUTOLOGY IS WHAT HID THIS DEFECT. Rail 2: seeding BOTH artifact keys makes the reader's key irrelevant, so every arm passes whichever key production uses. Step 6 seeds exactly one AND requires a reader-regression mutant to red a named consumer arm; if it reds nothing, the fixture is still tautological and that is a finding, not a pass.\n\n2) IMMUTABILITY IMPLEMENTED TOO BROADLY IS THE LIKELY WRONG TURN. A blanket second-seal ban satisfies ARM B and breaks ARM C's identical-bytes replay. D3 exists to catch it. The guard must discriminate on the BODY.\n\n3) RAIL 1 FORBIDS A PARTIAL LANDING. The id correction alone re-opens the stale-evidence hole; the fixture change alone reds every consumer suite. All parts land in ONE commit or none.\n\n4) AN ELEVENTH CONSUMER MAY EXIST. I found the tenth by measurement, but a grep for deriveFoundationArtifactAggregateId across apps/packages/tests is required BEFORE editing, with a stop-and-report. An unnamed consumer found at step 6 is what sent this row back the first time.\n\n5) THE UN-CHOSEN DESIGN MUST BE WHOLLY ABSENT. DoD-4 says so explicitly - no ARTIFACT fence leg, not even disabled. A half-present (a) would read as a fence that does not fence.\n\n6) task-06835dfa's delivered fence proof must be RE-RUN, not assumed, if anything it covers is touched (rail 3).\n\n7) Bare `pnpm test` runs ZERO daemon tests (root vitest include has no apps/**).",
    "keyFiles": [
      "apps/daemon/src/work/foundation-artifact-ledger.ts",
      "apps/daemon/src/work/release-handoff-sources.ts",
      "apps/daemon/src/work/release-handoff-classify.ts",
      "apps/daemon/src/work/release-handoff-builder.test.ts",
      "apps/daemon/src/work/release-handoff-test-harness.ts",
      "apps/daemon/src/work/foundation-attempt-store.ts"
    ]
  },
  "budget": {
    "wallClockMs": 6300000,
    "warnedAt": "2026-08-29T16:38:36.500Z",
    "escalatedAt": "2026-08-29T16:38:36.500Z"
  },
  "planApprovedAt": "2026-08-29T16:41:44.967Z",
  "contextFetchedBy": [
    "worker-4bba4fa4",
    "worker-8d70fe57"
  ],
  "workStartedAt": "2026-08-29T12:46:55.761Z",
  "priorHandoffs": [
    {
      "whatIsDone": "Step 1 completed with durable evidence; step 2 measured two plan blockers and task was reported BLOCKED. No task bytes authored; all ten relevant paths clean.",
      "whatRemains": "Amend safety proof from impossible moved-artifactDigest to a constructible whole-manifest/result-binding property (or re-rule), and add/split ownership for release-handoff-builder.test.ts.",
      "pitfalls": "Every lawful seal uses an empty roster, so artifactDigest cannot move; do not bank a green digest drill. Builder test hard-codes the old activation-key aggregate at :505/:537/:558.",
      "openQuestions": "Whether governor intends whole-manifest immutability despite invariant handoff artifactDigest.",
      "releasedAt": "2026-08-29T13:11:14.370Z",
      "releasedBy": "worker-4bba4fa4",
      "reason": "Explicit human request to move to the next executable task after safe block."
    }
  ]
}