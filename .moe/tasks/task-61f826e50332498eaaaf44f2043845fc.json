{
  "id": "task-61f826e50332498eaaaf44f2043845fc",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "bug: a missing migration TOOL is reported as a missing migration FILE, and the deploy receipt redacts it",
  "description": "MEASURED 2026-09-08 at HEAD 7622a2e8 by worker-bcaa6d5e from the epic-final row task-99f364e3. Cost me roughly forty minutes to diagnose; it will cost the next operator more, because in production the cause is not visible at all.\n\nWHAT HAPPENS. Deploy a freshly scaffolded (or freshly cloned) product whose workspace has not been installed. The deploy REFUSES. `report.detail` is `DEPLOY_BUILD_FAILED` and the receipt's own refusal detail is `[REDACTED]` under the fail-closed policy from task-c78140e9. Probing the migrate port directly is the only way to see the real answer:\n  {\"code\":\"MIGRATION_FAILED\",\"detail\":\"MIGRATION_FILE_UNKNOWN\",\"layer\":\"DAEMON_INGRESS\"}\n\nTHE CAUSE, symbol by symbol. `apps/daemon/src/repository/migrations/migration-ports.ts:54 apply` runs `node --input-type=module --eval MIGRATE` with cwd = the deploy's build context. `MIGRATE`'s first statement is `import { runner } from 'node-pg-migrate'` — a WORKSPACE dependency of the generated product. A tree that has never been installed has no `node_modules`, the import throws before `readdirSync('migrations')` is ever reached, no `MOE_MIGRATION_RESULT=` line is emitted, and `:64` throws `new MigrationExecutionError(null)` whose `file` field defaults to the literal `MIGRATION_FILE_UNKNOWN`. So a MISSING TOOL is reported as an UNKNOWN FILE.\n\nTHE CONSTANT FOR THE RIGHT ANSWER ALREADY EXISTS AND IS NEVER RAISED FROM THIS PATH: `controlled-profile-generator.ts:43 MIGRATION_TOOL_MISSING`, whose own comment says \"Defined here for the verifier and deployment migration consumers (children 2 and 3); THEY detect and raise it\". Nothing on the deploy migration path does.\n\nCONFIRMED NOT A PATH DEFECT: with the workspace installed, the same tree migrates green and the pipeline completes end to end (task-99f364e3 step 3). The migrations do live at `packages/api/migrations/` while the runner reads `migrations` at the workspace root — that is correct, because `node-pg-migrate` is invoked through the workspace and resolves its own directory; it is NOT the cause here and should not be \"fixed\".\n\nDEFINITION OF DONE\n1. A deploy against an uninstalled workspace refuses with `MIGRATION_TOOL_MISSING`, distinguishable from a genuinely absent or unreadable migration file.\n2. The distinction survives the redaction policy: an operator reading only the receipt can tell \"your workspace is not installed\" from \"your migration file is broken\" without a debugger. Redaction must not be weakened to achieve this — the code carries the meaning, not free text.\n3. Arms for BOTH conditions: an uninstalled workspace, and an installed workspace with an unreadable migration.\n4. `pnpm --filter @moe/daemon test` EXIT 0 with its count line quoted.",
  "definitionOfDone": [
    "A deploy whose workspace has no installed migration tool refuses with MIGRATION_TOOL_MISSING rather than MIGRATION_FILE_UNKNOWN, and the two conditions are distinguishable from the refusal code alone.",
    "The distinction is readable by an operator from the receipt under the existing fail-closed redaction policy; redaction is not weakened and no free text carries the meaning.",
    "Both conditions have arms: an uninstalled workspace and an installed workspace with an unreadable migration file.",
    "pnpm --filter @moe/daemon test EXIT 0 with its Test Files count line quoted."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "CONFIRM THE CHAIN AND THE TWO ROSTERS AT YOUR OWN HEAD. NO EDITS.\n\nThe plan below was measured at HEAD 502bb0af. Re-confirm these six facts by reading; if any has moved, stop and say so rather than coding around it.\n\n(1) THE CAUSE. migration-ports.ts:21 — the `MIGRATE` eval script's FIRST statement is a TOP-LEVEL `import { runner } from 'node-pg-migrate'`, outside the try at :38. It throws at module evaluation on an uninstalled workspace, so no `MOE_MIGRATION_RESULT=` line is emitted; :62-63 finds none, decodes null, and :64 throws `new MigrationExecutionError(null)`, whose constructor at :14 defaults `file` to the literal `MIGRATION_FILE_UNKNOWN`. migration-service.ts:68-69 turns that into `migrationRefusal(\"MIGRATION_FAILED\", error.file)`.\n\n(2) THE BRANCH YOU WILL EDIT IS GUARDED. migration-service.ts:67-73 reaches the `MIGRATION_FAILED` line only when `backupRef !== null`. `ports.dump` runs before `ports.apply`, so the filed scenario lands there. Below it, a failed backup returns the base receipt with `MIGRATION_BACKUP_FAILED` — a condition this row does not touch.\n\n(3) THE DETAIL CANNOT BE EMPTY. migration-receipt.ts:45 `text` requires `value.length > 0`. So the tool-missing refusal follows the code-as-detail idiom at :24, and the composite the DEPLOY receipt carries (deploy-service.ts:167-168, nonempty branch) is exactly `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING`. That single literal is what step 4 allowlists.\n\n(4) THE CONSTANT EXISTS AND IS UNRAISED. `git grep -n MIGRATION_TOOL_MISSING -- 'apps/**' 'packages/**' 'adapters/**' 'docs/**'` — scope it to source, an unscoped grep drowns in .moe/messages. Expect only controlled-profile-generator.ts:43,55, its test at :9,157,158, and repository-bootstrap-read.ts:18 with its test at :177. No raise site.\n\n(5) THE TWO ROSTERS AND THE ASSERTING CONSUMER. deploy-ledger.ts:39-46 `PUBLIC_PHASE_DETAILS` holds 17 members and deploy-refusal-redaction.test.ts:67-74 `SAFE_PHASES` restates the same 17 BY HAND — deliberately not imported. `git grep -n \"SAFE_PHASES\\|PUBLIC_PHASE_DETAILS\" -- 'apps/**' 'packages/**' 'adapters/**' 'docs/**'` must return only deploy-ledger.ts:39,51 and that test's :67,209,221,225. There is NO set-equality arm between the mirror and production, which is why step 4 edits BOTH.\n\n(6) `import.meta.resolve` IS AVAILABLE. package.json:10 pins `\"node\": \">=24.16.0 <25\"`. Probe it: `node --input-type=module --eval \"try{import.meta.resolve('node:fs');console.log('RESOLVE_OK')}catch(e){console.log('RESOLVE_ERR',e.code)}\"`. Step 2 depends on this.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/migrations/migration-ports.ts",
        "apps/daemon/src/repository/migrations/migration-service.ts",
        "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts"
      ],
      "note": "READ-ONLY. All six facts CONFIRMED at my HEAD c0c4d975. `git diff --stat 502bb0af c0c4d975` = 1 file changed, only `.moe/tasks/task-61f826e50332498eaaaf44f2043845fc.json`, so every line anchor in the plan holds unchanged.\n\n(1) CAUSE: migration-ports.ts:21 `import { runner } from 'node-pg-migrate'` is the MIGRATE script's first statement, top-level, outside the try at :38. :62 filters for `MOE_MIGRATION_RESULT=`, :63 `JSON.parse(line?.slice(...) ?? \"null\")` yields null, :64 `throw new MigrationExecutionError(null)`; :14 `this.file = migrationFilename(file) ? file : \"MIGRATION_FILE_UNKNOWN\"`. migration-service.ts:68-69 maps it to `migrationRefusal(\"MIGRATION_FAILED\", error.file)`.\n\n(2) GUARD: migration-service.ts:68 `if (backupRef !== null)`. `ports.dump` at :63 precedes `ports.apply` at :65 and backupRef is set at :64, so an uninstalled workspace with a reachable DB lands in that branch. Below it :70-73 is the failed-backup path returning the base receipt (MIGRATION_BACKUP_FAILED, service.ts:46) — untouched by this row.\n\n(3) NONEMPTY DETAIL: migration-receipt.ts:45 `text = ... value.length > 0 && value.length <= 4096`; :74 gates the refusal detail on it. Code-as-detail idiom at :24 (`migrationError` passes the code as its own detail). deploy-service.ts:164-166 composes `${code}@${layer}: ${detail}` on the nonempty branch, bare `${code}@${layer}` only when detail === \"\". So the deploy detail will be exactly `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING`.\n\n(4) CONSTANT EXISTS, NO RAISE SITE. `git grep -n MIGRATION_TOOL_MISSING -- 'apps/**' 'packages/**' 'adapters/**' 'docs/**'` returns exactly 7 lines and no raise site: controlled-profile-generator.ts:43 (definition), :55 (union member), its test :9,157,158, repository-bootstrap-read.ts:18 and its test :177. Comment at :38-41 confirms \"THEY detect and raise it\" and names the existing DAEMON_INGRESS layer, so no new `*_LAYER` constant.\n\n(5) TWO ROSTERS + ASSERTING CONSUMER. `git grep -n \"SAFE_PHASES\\|PUBLIC_PHASE_DETAILS\" -- 'apps/**' 'packages/**' 'adapters/**' 'docs/**'` returns exactly 7 lines: deploy-ledger.ts:39 (declaration) and :51 (the only production read, inside `declassifyRefusal`), and deploy-refusal-redaction.test.ts:67,209,221,225. No set-equality arm between mirror and production — confirmed by that grep being exhaustive. PUBLIC_PHASE_DETAILS counts 17 members across :40-46.\n\n(6) `import.meta.resolve` AVAILABLE. Probe printed `RESOLVE_OK`; package.json:9-10 pins `\"node\": \">=24.16.0 <25\"`.\n\nNO IMPORT CYCLE (needed for step 3): controlled-profile-generator.ts:1-5 imports only node:fs, ../../bootstrap/bootstrap-contracts.js (type-only) and its two sibling template modules. Nothing under repository/migrations/.\n\nPEER DIRT, not a stop condition, not touched: modified apps/daemon/src/http/affordance-read.ts, .codex/*, .grok/config.toml, .moe/** (messages, pins, roles, 11 task json); untracked .moe/resources/browser-e2e-gate.json, .moe/resources/full-suite-gate.json, .moe/tasks/task-d11695b0b6714183a16146eb4dd444f3.json, Moe-NG_Deep_Review_2026-09-05.md, apps/daemon/src/preview/preview-goal-landing.test.ts. Note the last one is an untracked peer .test.ts under apps/daemon and WILL execute in my step-6 gate leg, inflating its count line.\n\nNo edits made.",
      "startedAt": "2026-09-09T09:11:57.834Z",
      "completedAt": "2026-09-09T09:12:43.694Z"
    },
    {
      "stepId": "step-2",
      "description": "DETECT THE MISSING TOOL WHERE IT ACTUALLY FAILS — INSIDE THE CHILD (migration-ports.ts).\n\nThe top-level import cannot be caught where it is. Restructure the head of the `MIGRATE` script so the tool check happens inside a region that can still emit:\n- Move the `emit` helper (:30) ABOVE the check, since it is the only thing the failure path needs and it depends on nothing but `process.stdout`.\n- Replace the top-level `import { runner } from 'node-pg-migrate'` with a resolve probe followed by a dynamic import: `import.meta.resolve('node-pg-migrate')` inside a try, then `const { runner } = await import('node-pg-migrate')`. The script already runs under `--input-type=module` and already uses top-level await at :39, so this needs no new capability.\n- On the probe throwing, emit a result the parent can tell apart and set `process.exitCode = 1`. Add ONE new field meaning \"the tool was not importable\" — do NOT overload `file` with a second sentinel, which is the defect this row exists to remove. `apply` reads `value.file` and `value.applied` off this object by name (:63-67); it is not key-exact, so an extra field is safe.\n\nWHY THE RESOLVE PROBE AND NOT A CAUGHT `ERR_MODULE_NOT_FOUND`. A dynamic import raises that same code when a TRANSITIVE dependency of an INSTALLED `node-pg-migrate` is missing — a partially installed workspace, not a missing tool. `import.meta.resolve` answers about the ROOT specifier only, so the two stay separated. Never match on message text: Node's wording is not a stable interface, and a message match is exactly the free-text dependence DoD 2 forbids. Leave the `await import(...)` itself OUTSIDE the tool-missing branch, so any other import failure keeps its current path through the existing catch at :44.\n\nTHEN CARRY IT UP THROUGH `apply`. Give `MigrationExecutionError` an OPTIONAL SECOND constructor parameter (default false) and a matching readonly field, so the four existing positional construction sites — deploy-service.test.ts:823, migration-observation.test.ts:170, goal-deployment-read.test.ts:107, migration-service.test.ts:124, none of them owned by this row — keep compiling untouched. At :66, when `result.exitCode !== 0` and the marker carries the new field, construct the error with it set.\n\nDO NOT TOUCH THE MIGRATIONS DIRECTORY RESOLUTION. Migrations live at `packages/api/migrations/` while the runner reads `migrations` at the workspace root, and that is correct: `node-pg-migrate` is invoked through the workspace and resolves its own directory. With the workspace installed the same tree migrates green end to end. It is not the cause and must not be \"fixed\".",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/migrations/migration-ports.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/repository/migrations/migration-ports.ts"
      ],
      "note": "DETECTION LANDED INSIDE THE CHILD. One file touched: apps/daemon/src/repository/migrations/migration-ports.ts (75 -> 85 lines, well under the 250 bar).\n\nWHAT CHANGED, in the MIGRATE script:\n- `emit` moved to the head (now :23), directly after the two node: imports, so the failure path has the only helper it needs.\n- The top-level `import { runner } from 'node-pg-migrate'` is GONE. In its place: `let missing = false; try { import.meta.resolve('node-pg-migrate'); } catch { missing = true; }` then `if (missing) { emit({ applied: [], file: null, toolMissing: true }); process.exitCode = 1; } else { const { runner } = await import('node-pg-migrate'); ... }`.\n- The rest of the script moved into that else block UNCHANGED (readdirSync, the DATABASE_URL denial list, `safe`, `logger`, the runner try/catch and its stack-frame walk). Top-level await inside a block is legal in a module and is proven reachable below.\n- The `await import(...)` sits OUTSIDE the tool-missing branch on purpose, so a partially installed workspace (missing TRANSITIVE dep of an installed node-pg-migrate, same ERR_MODULE_NOT_FOUND) keeps its existing path through the catch. That is the discriminator the plan required and it is measured below, not assumed.\n\nWHAT CHANGED, in `apply`:\n- `MigrationExecutionError` gained an OPTIONAL SECOND parameter `toolMissing = false` and a readonly `toolMissing: boolean` field. All four existing positional sites (deploy-service.test.ts:823, migration-observation.test.ts:170, goal-deployment-read.test.ts:107, migration-service.test.ts:124) compile untouched — none owned, none edited.\n- `super(\"MIGRATION_FAILED@DAEMON_INGRESS\")` left UNCHANGED deliberately. `git grep -n \"MIGRATION_FAILED@DAEMON_INGRESS\"` finds 4 hits: deploy-ports.ts:61 (a doc comment), deploy-refusal-redaction.test.ts:241 (the impostor list), verifier-database.test.ts:496 (an unrelated fixture line) and this constructor. No consumer reads `.message`, but the meaning is carried by the CODE, so churning the message would be surface for nothing.\n- :66 now `throw new MigrationExecutionError(typeof value.file === \"string\" ? value.file : null, value.toolMissing === true)`. `value` is a `Record<string, unknown>` read by name, not key-exact, so the extra field is safe.\n- `file` is NOT overloaded with a second sentinel — the whole point of the row.\n\nMEASURED, both branches, by extracting the real MIGRATE template out of the source and spawning it exactly as `apply` does (temp trees, `rmSync` in `finally`, both torn down):\n(1) EMPTY TREE, no node_modules, DATABASE_URL=postgres://u:p@127.0.0.1:5432/db ->\n    EXIT 1\n    STDOUT \"\\nMOE_MIGRATION_RESULT={\\\"applied\\\":[],\\\"file\\\":null,\\\"toolMissing\\\":true}\\n\"\n    STDERR \"\"\n    No database was contacted and `migrations` was never read — the probe answers first, which is what lets step 5(a) be an ungated arm.\n(2) STUB `node_modules/node-pg-migrate` present whose `runner` throws, plus a real `migrations/1700000000009-broken.js` ->\n    EXIT 1\n    STDOUT \"\\nMOE_MIGRATION_RESULT={\\\"applied\\\":[],\\\"file\\\":null}\\n\"\n    STDERR \"\"\n    NO `toolMissing` key. So an installed-but-failing tool keeps the old shape and will keep MIGRATION_FAILED. That arm also proves the else block is reachable and the in-block top-level await evaluates.\n\nTYPECHECK: `npx tsc --noEmit -p apps/daemon/tsconfig.json` exit 0, no output.\n\nMigration directory resolution untouched, as instructed. No test files written in this step; arms land in step 5.",
      "startedAt": "2026-09-09T09:12:45.851Z",
      "completedAt": "2026-09-09T09:15:06.558Z"
    },
    {
      "stepId": "step-3",
      "description": "RAISE THE CODE THAT ALREADY EXISTS (migration-receipt.ts roster, migration-service.ts mapping).\n\n(1) ADD `MIGRATION_TOOL_MISSING: \"DAEMON_INGRESS\"` to the frozen `codeLayers` roster at migration-receipt.ts:12-19. DAEMON_INGRESS is an EXISTING layer and the constant's own comment at controlled-profile-generator.ts:38-41 says its refusal uses it — so this introduces NO new `*_LAYER` constant, which matters because a peer row has the security lane red over exactly that class of omission.\n\nIMPORT THE CONSTANT, DO NOT RETYPE THE STRING. controlled-profile-generator.ts:43 already exports it. A second literal spelling of the same code is the drift this codebase avoids elsewhere by exporting `MIGRATION_RECEIPT_COMMAND_KIND` for the same reason (migration-receipt.ts:7-10). The import direction is safe: controlled-profile-generator.ts imports only node:fs, bootstrap-contracts.js and its two sibling template modules, nothing under repository/migrations/, so there is no cycle. Confirm that before you write the import.\n\nDECODING COMES FREE, but verify rather than assume: :73 gates on `Object.hasOwn(codeLayers, v.refusal.code)` and :74 on the layer matching, so a receipt carrying the new code decodes once the roster holds it. Assert one round-trip through `decodeMigrationReceiptBytes` in step 5.\n\n(2) MAP IT AT THE RAISE SITE. migration-service.ts:68-69 currently produces `migrationRefusal(\"MIGRATION_FAILED\", error.file)` for every failure that kept its backup. Branch on the field step 2 propagates and produce `migrationRefusal(MIGRATION_TOOL_MISSING, MIGRATION_TOOL_MISSING)` — the code as its own detail, following the `migrationError` idiom at migration-receipt.ts:24, because `text` at :45 refuses an empty string. The detail is a PRODUCTION CONSTANT either way; that is the whole point, and it is what makes step 4 admissible.\n\nEVERY OTHER FAILURE KEEPS ITS CURRENT CODE AND ITS FILENAME DETAIL. This row narrows one condition out of `MIGRATION_FAILED`; it does not re-classify the rest, and it does not touch the `MIGRATION_BACKUP_FAILED` path below.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/migrations/migration-receipt.ts",
        "apps/daemon/src/repository/migrations/migration-service.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/repository/migrations/migration-receipt.ts",
        "apps/daemon/src/repository/migrations/migration-service.ts"
      ],
      "note": "THE EXISTING CONSTANT IS NOW RAISED. Two files, both under the bar: migration-receipt.ts 122 -> 129, migration-service.ts 98 -> 106.\n\n(1) ROSTER. `[MIGRATION_TOOL_MISSING]: \"DAEMON_INGRESS\"` added to the frozen `codeLayers` map, as a COMPUTED KEY off the imported constant — the string is never retyped. New import at migration-receipt.ts:4: `import { MIGRATION_TOOL_MISSING } from \"../controlled-profile/controlled-profile-generator.js\";`. NO new `*_LAYER` constant: DAEMON_INGRESS is the layer every other member already uses and the one the constant's own comment (controlled-profile-generator.ts:38-41) names.\n\nCYCLE RE-CONFIRMED BEFORE WRITING THE IMPORT, one hop deeper than the plan required. `git grep -n 'from \"' -- apps/daemon/src/repository/controlled-profile/` (non-test): the generator imports node:fs, `../../bootstrap/bootstrap-contracts.js` (type-only) and its two sibling template modules. Of those siblings only controlled-profile-root-templates.ts:24 leaves the directory, to `../../environment/environment-required-variables.js`, whose only imports are `@moe/core` (type-only) and `./environment-contracts.js` (type-only). Nothing reaches repository/migrations/. No cycle.\n\nTHE COMPUTED KEY DID NOT WIDEN THE ROSTER TYPE — measured, not assumed. A computed property can silently degrade `keyof typeof` to `string`, which would leave `MigrationCode` accepting anything while every call site still compiled. I wrote a throwaway `__typeprobe.ts` asserting BIDIRECTIONAL assignability between `MigrationCode` and the hand-written 10-member union (MIGRATION_BACKUP_FAILED, MIGRATION_FAILED, MIGRATION_IN_PROGRESS, MIGRATION_RECEIPT_INVALID, MIGRATION_RECEIPT_CONFLICT, MIGRATION_RECEIPT_WRITE_FAILED, MIGRATION_DOWN_BATCH_UNKNOWN, MIGRATION_DOWN_NOT_LAST_BATCH, MIGRATION_DOWN_FAILED, MIGRATION_TOOL_MISSING). `npx tsc --noEmit -p apps/daemon/tsconfig.json` exit 0 in BOTH directions, so the union is exactly those ten and did not widen. Probe deleted in the same command; `git status --porcelain apps/daemon/src/repository/migrations/` afterwards shows only my three owned modified files and no untracked residue.\n\n(2) RAISE SITE. migration-service.ts gained the import of the same constant and, ABOVE the existing MIGRATION_FAILED line, a narrower branch:\n    if (backupRef !== null && error instanceof MigrationExecutionError && error.toolMissing) {\n      return { ...base, backupRef, refusal: migrationRefusal(MIGRATION_TOOL_MISSING, MIGRATION_TOOL_MISSING) };\n    }\nCode-as-detail, per the `migrationError` idiom at migration-receipt.ts:24, because `text` at :45 refuses an empty string. Both halves of the composite are therefore production constants with no caller text — which is what makes step 4's single allowlist member admissible.\n\nNOTHING ELSE RE-CLASSIFIED. The `MIGRATION_FAILED` line below is byte-identical and still carries `error.file`; a non-MigrationExecutionError still yields MIGRATION_FAILED/MIGRATION_FILE_UNKNOWN; the `backupRef === null` MIGRATION_BACKUP_FAILED path at :70-73 and the base receipt at :46 are untouched. The new branch requires BOTH `instanceof MigrationExecutionError` AND `toolMissing === true`, and step 2 measured that an installed-but-failing tool emits no `toolMissing` key at all.\n\nDECODE ROUND-TRIP NOT YET ASSERTED HERE. I attempted an out-of-tree tsx probe and it died on `ERR_UNSUPPORTED_ESM_URL_SCHEME` resolving a Windows absolute path, so rather than contort it I am leaving the claim UNMADE at this step. Step 5 asserts it properly inside the package: a receipt carrying the new code with DAEMON_INGRESS decodes, and the same code with a wrong layer does not. Reading migration-receipt.ts:73-74 says it should pass (`Object.hasOwn(codeLayers, ...)` then layer equality) but that is a prediction, not evidence.\n\nTYPECHECK: `npx tsc --noEmit -p apps/daemon/tsconfig.json` exit 0.",
      "startedAt": "2026-09-09T09:15:10.010Z",
      "completedAt": "2026-09-09T09:16:40.936Z"
    },
    {
      "stepId": "step-4",
      "description": "MAKE THE ANSWER SURVIVE REDACTION — ONE MEMBER, NOT TWO. THIS STEP SETTLES THE CONFLICT THAT REOPENED THIS ROW; READ IT BEFORE TOUCHING EITHER FILE.\n\nTHE RULING. Add EXACTLY ONE literal to `PUBLIC_PHASE_DETAILS` (deploy-ledger.ts:39-46): `\"MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING\"`. Plain `MIGRATION_FAILED@DAEMON_INGRESS` is NOT added and STAYS REDACTED. The earlier plan asked for both; that was wrong and it is why this row came back. Do not restore it.\n\nWHY ONE IS ENOUGH, AND WHY TWO WAS WRONG. Today's file-failure detail always composes WITH a filename tail — migration-service.ts:69 passes `error.file` and `text` refuses an empty detail — so the deploy detail is `MIGRATION_FAILED@DAEMON_INGRESS: <filename>`. A filename is caller-influenced text and may never enter this set. The BARE form is only minted when the migration detail is empty (deploy-service.ts:167), which this path cannot reach. Admitting it would buy nothing and would red deploy-refusal-redaction.test.ts:241, which lists it as an impostor.\n\nWHY THE ONE IS ADMISSIBLE. That set's own criterion (deploy-ledger.ts:27-38) is \"minted by production code with no caller text in it\". Both halves of this composite are closed-roster constants — the code from migration-receipt.ts's frozen `codeLayers`, the layer from the same map, the detail the code itself. No filename, no stderr, no connection string, nothing an attacker or a tool can steer. This is USING the mechanism as designed, not weakening it.\n\nTHE LINE YOU MUST NOT CROSS: if the literal you are about to add is not a pure constant end to end, stop — the fix belongs in step 3's detail choice, not here.\n\nTHEN MIRROR IT, ADDITIVELY, IN deploy-refusal-redaction.test.ts. That file is an asserting consumer of this roster and is now an owned path, but it takes exactly two one-line edits and nothing else — it is already 406 lines, past the 400 split bar, before this row touches it.\n- Add the same literal to `SAFE_PHASES` (:67-74) and extend the comment at :64-66 to name migration-service.ts as a minting origin. The mirror is hand-maintained with NO set-equality arm against production, so a production-only addition would ship with zero coverage while every arm stayed green. Adding it here extends both the verbatim arm at :209 and the six-near-miss sweep at :221-225 for free, and `expect(misses.length).toBe(SAFE_PHASES.length * 6)` self-adjusts.\n- Add ONE entry to the impostor list at :239-242: the same code and layer with a FILENAME tail, e.g. `\"MIGRATION_TOOL_MISSING@DAEMON_INGRESS: 1700000000009-broken.js\"`. That is the half proving the allowlist did not become a blanket.\n\nLEAVE `\"MIGRATION_FAILED@DAEMON_INGRESS\"` STANDING AT :241. It is no longer incidental: it is now this row's own assertion that the generic migration failure was not admitted. Removing it would delete the guard.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/deploy-ledger.ts",
        "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/deploy-ledger.ts",
        "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts"
      ],
      "note": "ONE MEMBER ADMITTED. The ruling was followed exactly; the reopened conflict is settled by option (b).\n\nPRODUCTION (deploy-ledger.ts, 249 -> 255 lines). Added EXACTLY ONE literal to `PUBLIC_PHASE_DETAILS`:\n  \"MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING\"\nPlain `MIGRATION_FAILED@DAEMON_INGRESS` was NOT added. `git grep -n MIGRATION_FAILED apps/daemon/src/deployment/deploy-ledger.ts` returns nothing — it is absent from the set, and stays redacted.\n\nI CHECKED THE LINE I WAS TOLD NOT TO CROSS BEFORE WRITING IT. Every character of that literal is a production constant end to end: the code is the `MIGRATION_TOOL_MISSING` step 3 imported from controlled-profile-generator.ts:43 and added to the frozen `codeLayers`; the layer is `codeLayers[code]`, read out of that same frozen map by `migrationRefusal` (migration-receipt.ts:21-22) and never supplied by a caller; the detail is the code itself, per step 3's `migrationRefusal(MIGRATION_TOOL_MISSING, MIGRATION_TOOL_MISSING)`. The `@` and `: ` come from deploy-service.ts:164-166's own template. No filename, no stderr, no connection string, no caller-supplied fragment anywhere in it. It qualifies under the set's stated criterion at deploy-ledger.ts:27-38.\n\nTEST MIRROR (deploy-refusal-redaction.test.ts, 406 -> 411 lines). ADDITIVE ONLY, exactly the two edits the step named, plus their comments:\n- Same literal appended to `SAFE_PHASES`, now 18 entries. The doc comment above it extended to name migration-service.ts's closed-roster code/layer/detail as a minting origin alongside deploy-service.ts and deploy-image-build.ts.\n- One entry appended to the impostor list: \"MIGRATION_TOOL_MISSING@DAEMON_INGRESS: 1700000000009-broken.js\" — the SAME code and layer with a FILENAME tail, which must redact. That is the not-a-blanket half.\n- \"MIGRATION_FAILED@DAEMON_INGRESS\" LEFT STANDING at its line in the impostor list, untouched. It is now this row's own guard that the generic failure was not admitted.\nNothing else in that file was edited: no reformatting, no deletions, no line moves.\n\nMEASURED GREEN: `npx vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 --testNamePattern=\"^(?!.*LIVE_PROVIDER:)\" src/deployment/deploy-refusal-redaction.test.ts` from apps/daemon —\n  Test Files  1 passed (1)\n       Tests  43 passed (43)\n  exit 0\n43, up from 42, because the `it.each(SAFE_PHASES)` verbatim arm grew by one. The six-near-miss sweep self-adjusted as predicted: `expect(misses.length).toBe(SAFE_PHASES.length * 6)` now demands 108 and got it, so the new member is covered by six near-miss shapes for free — including `\"<member>: connection refused\"`, the suffix case that would catch a prefix policy.\n\nNOTE ON THE FILE-SIZE RAIL, disclosed not excused: deploy-refusal-redaction.test.ts was ALREADY 406 lines at HEAD, past the 400 split bar, before this row touched it. My edits add 5 lines. This is why the DoD-2 operator-view arm goes in a NEW file in step 5 rather than here, per the plan.\n\nOwned-path status clean: `git status --porcelain apps/daemon/` shows my five modified files plus the pre-existing peer dirt (affordance-read.ts modified, preview-goal-landing.test.ts untracked), neither touched.",
      "startedAt": "2026-09-09T09:16:43.197Z",
      "completedAt": "2026-09-09T09:17:52.889Z"
    },
    {
      "stepId": "step-5",
      "description": "BOTH CONDITIONS GET ARMS, AND THEY MUST BE ABLE TO FAIL DIFFERENTLY (DoD 3), PLUS THE OPERATOR'S VIEW (DoD 2).\n\n(a) AN UNINSTALLED WORKSPACE refuses `MIGRATION_TOOL_MISSING` — in migration-real.test.ts, as a NEW UNGATED arm alongside the existing ungated one at :40. It needs NO docker and NO database: with step 2's probe first, the child answers before it reads `migrations` or parses `DATABASE_URL`. So `mkdtempSync` an empty tree, call `nodeMigrationPorts().apply(root, \"postgres://u:p@127.0.0.1:5432/db\")` — a syntactically valid string so the arm cannot pass for the wrong reason — and assert the rejection carries the new tool-missing field, not a filename. A MOCKED port proves nothing here: the defect lives in a child process's module resolution, and only a real tree with no `node_modules` exercises it. EPIC RAIL 4: tear the temp tree down in a `finally`, on the throwing path too, the way the file's existing arm does at :101-108.\n\n(b) AN INSTALLED WORKSPACE WITH AN UNREADABLE MIGRATION still refuses `MIGRATION_FAILED` carrying the filename. Two arms already assert this and must stay green EXACTLY AS WRITTEN — migration-service.test.ts:120-133 at unit level and migration-real.test.ts's docker-gated live arm, which writes a throwing migration and pins `detail: broken`. Add to migration-service.test.ts one NEW arm for the discriminator step 2 introduced: an `apply` that fails for a reason OTHER than a missing root package still yields `MIGRATION_FAILED`, never tool-missing. That is what stops a partially installed workspace from being misreported. Also add the roster round-trip: a receipt carrying the new code decodes through `decodeMigrationReceiptBytes` and one carrying the code with the WRONG layer does not.\n\nASSERT THE CODE AND THE LAYER, NOT MERELY THAT IT REFUSED (project rail 1). Two refusal layers can answer here — the migration engine and the deploy engine — so pin WHICH one answered and with which code. This row exists precisely because two causes collapsed into one answer.\n\n(c) THE OPERATOR'S VIEW, which is the row's actual promise, in a NEW file apps/daemon/src/deployment/deploy-migration-disclosure.test.ts. It goes in its own file because deploy-refusal-redaction.test.ts is already over the line cap. Drive the production deploy service twice with an injected `migrate` port — once refusing tool-missing, once refusing a named file — and assert on the DURABLE receipt after declassification, not on the object handed back: the first reads `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING` verbatim, the second reads `[REDACTED]`, and the filename appears nowhere in the deploy receipt's bytes. deploy-refusal-redaction.test.ts:120-146 and :112-118 already show the service-with-injected-migrate and durable-bytes idioms; follow them rather than inventing a second harness. This clause is NOT implied by the migration receipt being correct — they are two different records and only one is redacted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/migrations/migration-real.test.ts",
        "apps/daemon/src/repository/migrations/migration-service.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/deployment/deploy-migration-disclosure.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/repository/migrations/migration-real.test.ts",
        "apps/daemon/src/repository/migrations/migration-service.test.ts",
        "apps/daemon/src/deployment/deploy-migration-disclosure.test.ts"
      ],
      "note": "BOTH CONDITIONS HAVE ARMS, AND THEY FAIL DIFFERENTLY. Three files; sizes 146 / 227 / 133, all well under the bar.\n\n(a) UNINSTALLED WORKSPACE — migration-real.test.ts, NEW UNGATED arm beside the existing ungated one. `mkdtempSync` an empty tree, then the REAL `nodeMigrationPorts().apply(root, \"postgres://u:p@127.0.0.1:5432/db\")` — a syntactically valid URL so the arm cannot pass because the URL was rejected instead. Asserts the rejection `toBeInstanceOf(MigrationExecutionError)` and `toMatchObject({ toolMissing: true })`, AND that `.file` is still `\"MIGRATION_FILE_UNKNOWN\"` — pinning that the sentinel alone can never be the discriminator, which is the filed defect stated as an assertion.\nThen it goes further than the step asked and drives the SERVICE end to end on that same real tree: a real store, the real `apply`, and only `dump` stubbed to write a nonempty backup. Asserts the refusal is `{ code: \"MIGRATION_TOOL_MISSING\", layer: \"DAEMON_INGRESS\", detail: \"MIGRATION_TOOL_MISSING\" }`, that `backupRef` is non-null, and that the DURABLE receipt read back through `readMigrationReceipt` equals it. So DoD 1 is proven from a real child process's module resolution to a persisted receipt with no docker and no database.\nCLEANUP (epic rail 4): `store?.close()` then `rmSync(root, { recursive: true, force: true })` in a `finally`, so the temp tree dies on the throwing path too. Measured Tests 2 passed | 1 skipped, tests 117ms — the child answers before any database work, which is what lets this arm be ungated.\n\n(b) INSTALLED WORKSPACE, UNREADABLE MIGRATION — migration-service.test.ts. The two existing arms are UNCHANGED, byte for byte: \"names a failing migration while retaining the actual backup\" (unit) and the docker-gated live arm pinning `detail: broken`. Three NEW arms added:\n1. \"keeps every non-tool failure on MIGRATION_FAILED with its filename\" — the discriminator, four labelled cases, each asserting the FULL refusal object with `toEqual`, not a partial: a named `MigrationExecutionError`, one constructed with the second parameter EXPLICITLY false, an unnamed one (`MIGRATION_FILE_UNKNOWN`), and a foreign `new Error(\"ERR_MODULE_NOT_FOUND\")` — that last one is the partially-installed-workspace shape, and it must stay MIGRATION_FAILED. `expect(failures).toHaveLength(4)` so a sweep that generated nothing cannot pass.\n2. \"narrows to MIGRATION_TOOL_MISSING only when the tool itself was unresolvable\" — the positive side, asserting code AND layer AND detail, that `backupRef`'s file exists, that the durable receipt equals it, and that `JSON.stringify(result)` does NOT contain `MIGRATION_FILE_UNKNOWN`, so no filename rides along.\n3. \"decodes the tool-missing code only under its own rostered layer\" — the round-trip step 3 left unmade. A REFUSED receipt carrying the new code with DAEMON_INGRESS decodes to itself; the SAME code with each of DAEMON_DEPLOY_ENGINE, LEDGER and KEY decodes to `{ ok: false, code: \"MIGRATION_RECEIPT_INVALID\", layer: \"DAEMON_INGRESS\" }`. Also pins `migrationRefusal(\"MIGRATION_TOOL_MISSING\", ...)` mints exactly that layer.\nMeasured Test Files 1 passed (1), Tests 12 passed (12), exit 0.\n\n(c) THE OPERATOR'S VIEW — NEW FILE apps/daemon/src/deployment/deploy-migration-disclosure.test.ts, 133 lines, following the injected-migrate and durable-bytes idioms from deploy-refusal-redaction.test.ts rather than inventing a second harness. Four arms, all reading the DURABLE ledger via `readDeployLedger(...).get(ENVIRONMENT)?.current`, never the returned report:\n1. Tool-missing survives VERBATIM as `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING`, while the deploy engine still stamps its own DEPLOY_BUILD_FAILED / DEPLOY_ENGINE_STAMP — so which layer answered is pinned, per project rail 1.\n2. The file failure reads `[REDACTED]`, and `1700000000009-broken.js` appears in NONE of the durable bytes (decision results plus every event payload and metadata) nor in `report.detail`.\n3. The same tool-missing CODE with a filename tail also redacts — the allowlist did not become a blanket on that code and layer.\n4. The two details are asserted UNEQUAL to each other. Asserting each merely \"refused\" would have stayed green straight through the filed defect, which was two causes collapsing into one answer; the inequality is the load-bearing claim.\nMeasured Test Files 1 passed (1), Tests 4 passed (4), exit 0.\n\n`npx tsc --noEmit -p apps/daemon/tsconfig.json` exit 0. Mutation drills are step 6; nothing here is claimed as drilled yet.",
      "startedAt": "2026-09-09T09:17:56.164Z",
      "completedAt": "2026-09-09T09:20:43.456Z"
    },
    {
      "stepId": "step-6",
      "description": "ONE CONCENTRATED CLOSE: DRILLS, GATE, ADVERSARIAL PASS (DoD 4).\n\nDRILL PROTOCOL, and the base rule is superseded — do NOT use a bare `git diff` as a restore proof. Before mutating any path, `git status --porcelain` it, copy it out of tree to `%TEMP%` under a name carrying BOTH the task id and your worker id, and record the SHA256 taken FROM THE LIVE FILE at that moment. Restore by copying the backup back, and prove it in a call SEPARATE from the restore by SHA256 equality against that pre-mutation hash. A `%TEMP%` path keyed only on a seat id can hold a stale vintage from a previous session and will certify a restore that destroyed the row. The new test file from step 5 is UNTRACKED, so `git diff` is blind to it entirely. Do not mutate any file while a peer holds a full-suite lease.\n\n(i) THE ROW'S SUBJECT: revert step 2's probe to the top-level import and confirm arm (a) goes RED reporting the old `MIGRATION_FILE_UNKNOWN`. If it stays green, the arm is not exercising real module resolution.\n(ii) THE REDACTION HALF: remove the new literal from `PUBLIC_PHASE_DETAILS` — production only, leave the test mirror alone — and confirm the operator-view assertion from step 5(c) goes RED with `[REDACTED]`. That proves the DEPLOY-receipt clause is tested, not just the migration receipt.\n(iii) THE ONE-SIDED-FIX GUARD: make the tool-missing branch in migration-service.ts fire for every failure and confirm BOTH the file-failure arms and the impostor arm go RED. That is the regression a rushed version of this fix would ship.\n\nGATE: `pnpm --filter @moe/daemon test`, FOREGROUND and STANDALONE, quoting the exit code AND the `Test Files N passed` count line verbatim — DoD 4 names it. Read the OUTPUT, not the exit code: a `--filter` leg prints \"No projects matched the filters\" and EXITS 0, and \"0 passed\" is exit 0 too, so the count line is the only discriminator. Also run `pnpm typecheck`: `MigrationCode` is a roster type and 21 modules import migration-receipt.js.\nThis leg is long and has an intermittent history on this host. If it reds at a path this row does not own, attribute it with the path-attributed baseline (global rail 3: repo-wide failures at merge-base, then at HEAD, delta intersected with owned paths) and disclose it verbatim rather than fixing it. A peer row currently has the SECURITY lane red over unrostered `*_LAYER` constants; this row adds no layer, so that red is not yours — say so with the path.\n\nADVERSARIAL PASS (load `adversarial-self-review`). Ask, in order: what does a PARTIALLY installed `node_modules` report — tool missing, or the honest failure? Does the verifier path, the other consumer the constant's comment names, still report its own way, or did this change reach it? Can a migration filename now leak into the deploy detail through the new branch or through the near-miss sweep? Did the temp tree and its child process die on the throwing path? And the question the filing poses: after this change, can an operator reading ONLY the deploy receipt tell \"your workspace is not installed\" from \"your migration file is broken\", without a debugger and without free text carrying the meaning?",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/repository/migrations/migration-ports.ts",
        "apps/daemon/src/repository/migrations/migration-service.ts",
        "apps/daemon/src/deployment/deploy-ledger.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/repository/migrations/migration-ports.ts",
        "apps/daemon/src/repository/migrations/migration-receipt.ts",
        "apps/daemon/src/repository/migrations/migration-service.ts",
        "apps/daemon/src/repository/migrations/migration-real.test.ts",
        "apps/daemon/src/repository/migrations/migration-service.test.ts",
        "apps/daemon/src/deployment/deploy-ledger.ts",
        "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts",
        "apps/daemon/src/deployment/deploy-migration-disclosure.test.ts"
      ],
      "note": "CLOSED. FOUR drills (three prescribed plus one I added), both gates green, adversarial pass done. Full evidence is in two row comments — comment-69456307 (drills) and comment-7a2b8fa (gates + adversarial pass) — because it exceeds what fits here.\n\nLEASE DISCIPLINE FIRST: the full-suite-gate was HELD by qa-01b7fe3f when step 6 opened. I planted nothing, queued at #1, polled in foreground, was granted at 09:23:12Z, drilled and gated inside my own exclusive lease, and released it.\n\nDRILLS. Backups keyed on BOTH task id and worker id, hash anchored to the LIVE file at window-open, backups grepped for mutant tokens (0/0/0), every restore proven by SHA256 in a SEPARATE call. All three targets are ` M` — tracked but modified by this row — so a bare `git diff` would have proven nothing.\n(i) Top-level import restored -> arm (a) RED: `expected ... to match object { toolMissing: true }` / `+ \"toolMissing\": false`. Proves it exercises real module resolution.\n(ii) Literal removed from PUBLIC_PHASE_DETAILS, production only -> 3 arms RED with `[REDACTED]`, including the operator-view arm. Proves the DEPLOY-receipt clause is tested, not just the migration receipt.\n(iii) Tool-missing branch fired for every failure -> 4 arms RED across 3 files, two NOT owned by this row (migration-observation.test.ts, goal-deployment-read.test.ts).\n(iv) MINE, ADDED. Drill (iii) did NOT red the impostor arm and could not: that arm and the filename-tail arm both drive recordDeployReceipt / an injected port, so neither routes through migration-service.ts. Rather than claim a red I did not get, I drilled the guard properly — widened the allowlist to admit a filename tail, and BOTH the impostor arm and the disclosure filename-tail arm went RED.\nFinal census after all four: three SHA256 matches, residual mutant-token greps 0/0/0.\n\nGATE, at the restored tree:\n  pnpm --filter @moe/daemon test -> exit 0\n    Test Files  604 passed (604)\n         Tests  10762 passed | 9 skipped (10771)\n  pnpm typecheck -> exit 0, every package Done.\nCount line present and nonzero, so the leg executed.\n\nNO FOREIGN RED TO ATTRIBUTE: both legs are exit 0, so the path-attributed baseline delta is empty by construction. The peer SECURITY red over unrostered `*_LAYER` constants is not this row — this row adds NO layer, it reuses the existing DAEMON_INGRESS.\nDISCLOSED: `apps/daemon/src/preview/preview-goal-landing.test.ts` is an untracked PEER file that predates my claim and executes inside that 604/10762 denominator. Not mine, never touched.\n\nADVERSARIAL PASS, five questions answered with measurements in comment-7a2b8fa: a partially installed tree reports the honest MIGRATION_FAILED (measured twice, once against a real stub package); the verifier path detects its own condition at verifier-database.ts:42-43 and is untouched, with repository-bootstrap-read.ts's bidirectional CODES pin undisturbed and typecheck proving it; no filename can reach the deploy detail (asserted across every durable byte, plus drill iv); the temp tree and the child process both die on the throwing path via `finally`; and yes, an operator reading only the deploy receipt can now tell the two conditions apart, from the code, with redaction unweakened.",
      "startedAt": "2026-09-09T09:20:46.651Z",
      "completedAt": "2026-09-09T09:31:01.026Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Governor re-plan handoff. This row was BLOCKED with NO blockedResourceId and NO blockedOnTaskIds and no assignee, so nothing could ever auto-clear it - it needed a status flip or it would sit forever. It is not a defect and not a resource wait: it is a PLAN CONFLICT the worker was right to refuse rather than code around.\n\nTHE CONFLICT, for the architect who picks this up. Approved step 4 instructs admitting plain MIGRATION_FAILED@DAEMON_INGRESS into PUBLIC_PHASE_DETAILS. At measured HEAD 2569dd4e, apps/daemon/src/deployment/deploy-refusal-redaction.test.ts:241 explicitly lists that exact literal as an IMPOSTOR, and :246-250 invokes recordDeployReceipt and asserts [REDACTED]. Following step 4 as written would knowingly red a required daemon gate. That asserting consumer is absent from all six approved owned paths, so editing it would breach the unnamed-consumer scope rail. The worker correctly did neither, made no source edits, claimed no tests, and asked for an amendment.\n\nRESOLVE IT ONE OF TWO WAYS, deliberately - do not split the difference: either (a) amend steps 4/5 to include deploy-refusal-redaction.test.ts as a seventh owned path and update the exact policy coverage there, or (b) amend step 4 to keep plain MIGRATION_FAILED redacted and re-aim the fix at distinguishing a missing migration TOOL from a missing migration FILE without widening the allowlist. Note that PUBLIC_PHASE_DETAILS may only ever contain strings minted by production code with no caller text in them; widening it to satisfy a test would reintroduce the leak task-c78140e9 closed. Step 1 read-only is complete and step 2 was started; full evidence and exact commands are in the task comment.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "MEDIUM",
  "order": 139,
  "comments": [
    {
      "id": "comment-435df213e760410983a860603e1040a6",
      "author": "architect-5385678f",
      "content": "ARCHITECT architect-5385678f 2026-09-08 — PLANNED (6 steps / 6 files; one over the 5-file guidance, justified at the end). Filing re-measured hop by hop and is accurate. Recording the two things a worker will otherwise discover too late.\n\nCHAIN CONFIRMED. `MIGRATE`'s first statement is a TOP-LEVEL `import { runner } from 'node-pg-migrate'`, OUTSIDE the try that begins at migration-ports.ts:38 — so on an uninstalled workspace it throws at module evaluation and no `MOE_MIGRATION_RESULT=` line is emitted at all. `apply` :63 finds no line, `JSON.parse(… ?? \"null\")` yields null, :64 throws `MigrationExecutionError(null)`, the constructor at :14 defaults `file` to the literal `MIGRATION_FILE_UNKNOWN`, and migration-service.ts:69 emits `migrationRefusal(\"MIGRATION_FAILED\", error.file)`. A missing TOOL becomes an unknown FILE.\n\nCONSTANT CENSUS — `MIGRATION_TOOL_MISSING` has NO raise site anywhere. `git grep -n` returns only controlled-profile-generator.ts:43 (definition), :55 (type union), controlled-profile-generator.test.ts:157-158, and repository-bootstrap-read.ts:18 with its test at :177. Its own comment says the deployment migration consumer is supposed to raise it.\n\n**FINDING 1 — THERE ARE TWO RECEIPTS AND ONLY ONE IS REDACTED. This is the trap.** deploy-service.ts:305-312 refuses `DEPLOY_BUILD_FAILED @ DAEMON_DEPLOY_ENGINE` and carries the MIGRATION's `code@layer` in the DEPLOY refusal's **detail** — and `declassifyRefusal` (deploy-ledger.ts:50-54) replaces any detail that is not an EXACT member of `PUBLIC_PHASE_DETAILS` with `[REDACTED]`. So raising the new code fixes the MIGRATION receipt (never declassified) and does NOT fix the operator's view. A worker who stops after the raise site will have satisfied DoD 1 and not DoD 2, with everything green. Step 4 handles it; drill (ii) proves it.\n\n**FINDING 2 — THE DETAIL COMPOSITION DECIDES WHAT CAN BE ALLOWLISTED.** deploy-service.ts:167-168 composes `` `${code}@${layer}` `` when the migration detail is EMPTY and `` `${code}@${layer}: ${detail}` `` when it is not. `PUBLIC_PHASE_DETAILS` membership is EXACT (no trim, no prefix, no case folding) and its comment at :36-38 says why: `\"<phase>: <tool output>\"` is exactly how a secret rides beside a safe word. So the tool-missing refusal must compose to a PURE CONSTANT. Step 1 measures the one branch point: whether migration-receipt.ts:74's `text(v.refusal.detail)` admits an empty string. If yes → `detail: \"\"` → bare `MIGRATION_TOOL_MISSING@DAEMON_INGRESS`. If no → pass the code as its own detail, the existing idiom at migration-receipt.ts:24 → `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING`. Either is a pure production constant end to end.\n\nWHY THE CODE-LEVEL FIX AND NOT A NICER MESSAGE. `declassifyRefusal` keeps `code` and `layer` ALWAYS. DoD 1 asks for a distinction \"from the refusal code alone\" and DoD 2 says \"the code carries the meaning, not free text\" — both point at raising a real code. And adding a `code@layer` composite to the allowlist is USING that mechanism as designed, not weakening it: the set's own criterion is \"minted by production code with no caller text in it\", and a pair drawn from two closed rosters is exactly that. What the plan forbids: adding `MIGRATION_FILE_UNKNOWN` or any composite with a FILENAME tail — a migration filename is caller-influenced text and belongs on the migration receipt, which is not declassified. That is the same line the governor drew on the sibling redaction row.\n\nNO NEW LAYER, DELIBERATELY. `codeLayers` (migration-receipt.ts:12-19) is a frozen roster whose entries are ALL `DAEMON_INGRESS`, and the constant's comment says its refusal uses that layer. Decoding comes free — :73 gates on `Object.hasOwn(codeLayers, code)`. This matters today: peer row task-44c0178b has the SECURITY lane red over two new `*_LAYER` constants landing unrostered. This row adds none, so that red is not this row's.\n\nTHE ONE-SIDED FIX IS THE LIKELIEST BAD OUTCOME: make everything report `MIGRATION_TOOL_MISSING` and DoD 1's first half passes while a genuine broken migration file becomes unreportable. DoD 3's second arm is the guard and drill (iii) hunts it. Related: condition (a) CANNOT be proven with a mocked port — the defect lives in a child process's module resolution, so it needs a real tree with no `node_modules` (migration-real.test.ts is the neighbour to follow). And `ERR_MODULE_NOT_FOUND` is not the only way an import fails: a syntax error in an installed package is not a missing tool, so branch on the error's `code` property, never its message.\n\nNOT TOUCHED, on the filing's own instruction: migrations live at `packages/api/migrations/` while the runner reads `migrations` at the workspace root. Measured not to be the cause — an installed tree migrates green end to end.\n\nSIX FILES, ONE OVER GUIDANCE, and I did not split. The defect spans a raise site (migration-ports), a roster (migration-receipt), a mapping (migration-service), a disclosure policy (deploy-ledger) and two test surfaces. Splitting lands either a code nothing raises, or a raise site whose answer the operator still cannot read — each half is a silent re-creation of the bug. Step count and file count are both inside the daemon's hard thresholds.",
      "timestamp": "2026-09-08T18:06:41.490Z"
    },
    {
      "id": "comment-b920acab8d3b4657825fc0224effb54c",
      "author": "worker-5d2b88e7",
      "content": "Measured plan-scope collision before any source edits, HEAD 2569dd4e2b0d89a998a41c6deee030d6b93cac8f. `Select-String -Path apps/daemon/src/deployment/deploy-refusal-redaction.test.ts -Pattern 'MIGRATION_FAILED@DAEMON_INGRESS' -Context 5,10` locates line 241 inside impostors; lines 246-250 call production recordDeployReceipt and assert detail [REDACTED]. Effective step 4 (activeAmendmentId null; amendments null, read directly from task JSON) instructs adding the tool-missing literal AND plain migration-failure literal. That changes an exact policy expectation in an unnamed consumer. Approved six owned paths exclude deploy-refusal-redaction.test.ts. Global shape/consumer rail requires stop/report, not silently take a seventh path. Please amend steps 4/5 to own that existing redaction test, move the now-approved plain literal from impostors into SAFE_PHASES and add new exact-member/filename-tail negative arms there; alternatively explicitly keep plain MIGRATION_FAILED redacted and amend the intended operator-view promise. No code edits, no mutations, no verification pass claimed. Step 1 completed; step 2 started only. Detail choice resolved: migration receipt text requires nonempty string, therefore use code-as-detail and exact composite MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING. Also ensure resumed drill instructions use live pre-mutation SHA256 + task-id backup, not base step6's superseded bare git-diff restore proof.",
      "timestamp": "2026-09-08T18:42:58.755Z"
    },
    {
      "id": "comment-f630ab602fe74b4c994768ffb4bc558a",
      "author": "worker-5d2b88e7",
      "content": "Human continuation ~19:43Z: task-specific WORKING claim rejected INVALID_STATE (task PLANNING). Confirmed governor re-plan handoff and no step4/5 amendments. Requested targeted amendment + return to WORKING in shared chat msg-c28c67c157e74d83bdafb83921adcb51; bounded wait found no response. No code changes/tests. Leaving row PLANNING and claimable for architect, not undoing governor routing by re-blocking it. Serena handoff updated.",
      "timestamp": "2026-09-08T19:47:32.569Z"
    },
    {
      "id": "comment-2b8cd6cc14c449b4896584617f2fe7ca",
      "author": "architect-f04f69cf",
      "content": "RE-PLAN 2026-09-09 by architect-f04f69cf at HEAD 502bb0af. The governor's plan conflict is resolved as option (b), deliberately, and the whole step list is replaced.\n\nTHE RULING. Exactly ONE new member enters `PUBLIC_PHASE_DETAILS`: `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING`. Plain `MIGRATION_FAILED@DAEMON_INGRESS` is NOT admitted and stays redacted, so deploy-refusal-redaction.test.ts:241 stays green UNEDITED and now serves as this row's own guard that the allowlist did not become a blanket.\n\nWHY, MEASURED. The file-failure detail always composes WITH a filename tail: migration-service.ts:69 passes `error.file` and migration-receipt.ts:45 `text` refuses an empty string, so the deploy detail is `MIGRATION_FAILED@DAEMON_INGRESS: <filename>`. A filename is caller-influenced text and may never enter that set. The BARE composite the old step 4 asked for is only minted when the migration detail is empty (deploy-service.ts:167), which this path cannot reach. Option (a) would therefore have admitted a string production never mints, in exchange for reddening a required daemon gate. It buys nothing.\n\nDoD 2 IS SATISFIED THIS WAY. Tool-missing reads positively off the deploy receipt as a pure constant. Every other migration failure reads `[REDACTED]`, and the filename stays on the MIGRATION receipt, which is never declassified. Distinguishable without a debugger, no free text carries the meaning, redaction not weakened.\n\nSCOPE CHANGE THE WORKER MUST NOT RE-DERIVE. deploy-refusal-redaction.test.ts IS now an owned path, but takes ADDITIVE edits only: one literal into `SAFE_PHASES` (:67-74) and one filename-tail entry into the impostor list (:239-242). It is already 406 lines at HEAD, past the 400 split bar, so the DoD-2 operator-view arm goes into a new file `apps/daemon/src/deployment/deploy-migration-disclosure.test.ts` rather than growing it. `SAFE_PHASES` is a HAND mirror of the production set with NO set-equality arm between them, so a production-only addition would ship with zero coverage while every arm stayed green: edit both.\n\nPlan is 6 steps over 8 distinct files (daemon warns above 5, hard-rejects above 20). The eighth file is the new test.",
      "timestamp": "2026-09-09T09:11:48.459Z"
    },
    {
      "id": "comment-6945630793b649418ed90644cdabaa4e",
      "author": "worker-0e797912",
      "content": "STEP 6 EVIDENCE — DRILLS. Full detail here because the completion summary truncates at 2000.\n\nLEASE DISCIPLINE. On entering step 6 the full-suite-gate was HELD by qa-01b7fe3f (task-a2dd124f, acquired 2026-09-09T09:17:46.352Z, etaMs 400000) running `pnpm --filter @moe/daemon test`. I planted NOTHING while they held it. I queued at position #1, polled in the foreground, and was GRANTED at 09:23:12.281Z. Every mutation below happened inside my own exclusive lease, which I released at the end.\n\nBACKUPS, taken before any mutation, each keyed on BOTH the task id and the worker id, hash anchored to the LIVE file at that moment (not to the backup), with the backup grepped for mutant tokens (0 in all three):\n  migration-ports.ts    86f4d6242dedd6fd4906638a47210f7f8ff9dc6a51cd1e971683d6fe2d9f13dc\n  deploy-ledger.ts      834d25b830d8c18137632fcc1fbc4536db510aa5657664ffc4c5e1c14e0d36dd\n  migration-service.ts  3a23fccdb550afe309d74e64a08f3c4728b8b716ad52d11be176af119a085d2a\n`git status --porcelain` on each beforehand: all three ` M` — tracked but MODIFIED BY THIS ROW, so an empty `git diff` would have been a worthless restore proof. Every restore below is a copy-back proven by SHA256 equality against the hash above IN A SEPARATE TOOL CALL.\n\n(i) THE ROW'S SUBJECT. Reverted the resolve probe to the top-level `import { runner } from 'node-pg-migrate'`. Arm (a) went RED:\n  × names the missing TOOL, not an unknown FILE, when the workspace was never installed\n  AssertionError: expected Error: MIGRATION_FAILED@DAEMON_INGRESS { …(2) } to match object { toolMissing: true }\n  -   \"toolMissing\": true,\n  +   \"toolMissing\": false,\n  Test Files 1 failed (1) | Tests 1 failed | 1 passed | 1 skipped (3)\nSo the arm exercises real child-process module resolution, not a stub. Restored, SHA256 matched.\n\n(ii) THE REDACTION HALF. Removed the new literal from `PUBLIC_PHASE_DETAILS` in PRODUCTION ONLY, leaving the test mirror alone. Three arms went RED, all with `[REDACTED]`:\n  × deploy-migration-disclosure.test.ts > keeps the tool-missing code verbatim on the durable deploy receipt\n  × deploy-migration-disclosure.test.ts > makes the two conditions different strings on the receipt, not merely both refusals\n  × deploy-refusal-redaction.test.ts > keeps the audited phase literal MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING verbatim\n  AssertionError: expected '[REDACTED]' to be 'MIGRATION_TOOL_MISSING@DAEMON_INGRESS…'\n  Test Files 2 failed (2) | Tests 3 failed | 44 passed (47)\nThat is the DEPLOY-receipt clause proven tested, separately from the migration receipt. Restored, SHA256 matched.\n\n(iii) THE ONE-SIDED-FIX GUARD. Made the tool-missing branch fire for every failure (`if (backupRef !== null && error instanceof MigrationExecutionError && error.toolMissing)` -> `if (backupRef !== null)`). FOUR arms went RED across THREE files, two of which this row does not own:\n  × migration-service.test.ts > names a failing migration while retaining the actual backup\n  × migration-service.test.ts > keeps every non-tool failure on MIGRATION_FAILED with its filename\n  × migration-observation.test.ts > reports a refusal taken AFTER the backup with the failing file basename and the backup kept\n  × goal-deployment-read.test.ts > carries a REFUSED receipt with its code, its layer, the failing basename and no backup\n  Test Files 3 failed | 2 passed (5) | Tests 4 failed | 74 passed (78)\nRestored, SHA256 matched.\n\nCORRECTION TO THE STEP'S EXPECTATION, stated rather than papered over: drill (iii) did NOT red the impostor arm, and could not. The impostor arm and the disclosure filename-tail arm both drive `recordDeployReceipt` / an injected migrate port with a hand-built detail, so neither routes through migration-service.ts and neither is reachable from that mutant. Claiming it went red would have been false. I ran a FOURTH drill to prove that guard properly:\n\n(iv) THE ALLOWLIST-IS-NOT-A-BLANKET GUARD. Added `\"MIGRATION_TOOL_MISSING@DAEMON_INGRESS: 1700000000009-broken.js\"` to `PUBLIC_PHASE_DETAILS`. Two arms went RED:\n  × deploy-migration-disclosure.test.ts > does not admit the tool-missing code with a filename tail\n  × deploy-refusal-redaction.test.ts > redacts code-looking strings that are not members of the set\n  Test Files 2 failed (2) | Tests 2 failed | 45 passed (47)\nRestored, SHA256 matched.\n\nFINAL RESTORE CENSUS, all three in one call after every drill: all three SHA256 equal to their pre-mutation hashes, and residual mutant-token greps are 0 / 0 / 0 (`import { runner } from 'node-pg-migrate'` in migration-ports.ts, `1700000000009-broken.js` in deploy-ledger.ts, `if (backupRef !== null) {` in migration-service.ts).",
      "timestamp": "2026-09-09T09:30:07.160Z"
    },
    {
      "id": "comment-7a2b8faa59fb4581b2dad4a189fc5c64",
      "author": "worker-0e797912",
      "content": "STEP 6 EVIDENCE — GATES AND ADVERSARIAL PASS.\n\nGATE, foreground and standalone, run at the restored tree AFTER every drill was reverted and SHA256-proven:\n  $ pnpm --filter @moe/daemon test\n   Test Files  604 passed (604)\n        Tests  10762 passed | 9 skipped (10771)\n     Duration  260.36s\n  exit 0\nThe count line is present and NONZERO, so the leg executed — this is not a \"No projects matched the filters\" or \"0 passed\" exit 0.\n\n  $ pnpm typecheck\n  apps/daemon typecheck: Done  (every workspace package Done)\n  exit 0\nRun because `MigrationCode` is a roster type and 21 modules import migration-receipt.js.\n\nNO FOREIGN RED TO ATTRIBUTE. Both legs are exit 0 with no failures, so the path-attributed baseline (global rail 3) has nothing to compute: the failing-path set at HEAD is empty, so its delta against merge-base is empty by construction. The peer row with the SECURITY lane red over unrostered `*_LAYER` constants is NOT this row: this row adds no `*_LAYER` constant at all. It reuses the EXISTING `DAEMON_INGRESS`, which every other member of `codeLayers` already uses and which controlled-profile-generator.ts:38-41 names for this exact code.\n\nONE DISCLOSURE ABOUT THE COUNT LINE. `apps/daemon/src/preview/preview-goal-landing.test.ts` is an UNTRACKED peer file present in the shared checkout since before I claimed. It is not mine, I never opened or edited it, and it executes inside that 604/10762. So the denominator includes work this row does not own. I am not treating that as a stop condition and I did not touch it.\n\nADVERSARIAL PASS (adversarial-self-review loaded), the step's five questions in order:\n\n1. WHAT DOES A PARTIALLY INSTALLED node_modules REPORT? The honest failure, measured twice. In step 2 I spawned the real MIGRATE script against a temp tree holding a STUB `node_modules/node-pg-migrate` whose `runner` throws: stdout was `MOE_MIGRATION_RESULT={\"applied\":[],\"file\":null}` with NO `toolMissing` key, exit 1 — the existing path. And migration-service.test.ts's new four-case arm includes a raw `new Error(\"ERR_MODULE_NOT_FOUND\")`, the exact shape a missing TRANSITIVE dependency raises, asserted to stay `MIGRATION_FAILED`. The discriminator is `import.meta.resolve` on the ROOT specifier, so \"the tool is absent\" and \"an installed tool's dependency is absent\" cannot collapse. The `await import(...)` deliberately sits OUTSIDE the tool-missing branch so any other import failure keeps the old catch.\n\n2. DID THIS REACH THE VERIFIER PATH? No. The verifier detects its own condition at verifier-database.ts:42-43 by checking for a `db:migrate` script in the workspace manifest — a different mechanism in a different module, untouched. I also checked the roster that would have caught collateral damage: repository-bootstrap-read.ts:14-25 pins `CODES` against `BootstrapCode` in BOTH directions with compile-time `Exclude<...> extends never` assertions. I added no member to `ControlledProfileRefusalCode`, so that pin is undisturbed — and `pnpm typecheck` exit 0 proves it rather than my saying so.\n\n3. CAN A FILENAME LEAK INTO THE DEPLOY DETAIL? No, on three separate measurements. The new branch mints `migrationRefusal(MIGRATION_TOOL_MISSING, MIGRATION_TOOL_MISSING)` — both halves closed-roster constants, no caller text. The disclosure arm asserts `1700000000009-broken.js` is absent from EVERY durable byte (decision results plus every event payload and metadata), not just the detail field. Drill (iv) proves the filename-tail form of the SAME code and layer still redacts. And the near-miss sweep now generates six shapes per member for the new one too, including `\"<member>: connection refused\"` — the suffix case a prefix policy would let through. Membership stays EXACT.\n\n4. DID THE TEMP TREE AND ITS CHILD PROCESS DIE ON THE THROWING PATH? Yes, both. The new ungated arm wraps everything in `try/finally` with `store?.close()` then `rmSync(root, { recursive: true, force: true })`, so the tree goes even when an assertion throws. The child process is closed by production: `apply` ends in `finally { await runner.close(); }`, which runs on the rejection path — and that rejection path is exactly what the arm exercises. No docker container and no port is involved, so epic rail 4 has nothing else outstanding here.\n\n5. THE FILING'S OWN QUESTION — can an operator reading ONLY the deploy receipt tell the two apart, with no debugger and no free text? Yes. Uninstalled workspace reads `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING` verbatim; a broken migration file reads `[REDACTED]`, with the filename preserved on the MIGRATION receipt, which is never declassified. Redaction was not weakened: exactly one member was added to a set whose stated criterion it meets, the generic `MIGRATION_FAILED@DAEMON_INGRESS` stays out and stays listed as an impostor, and no free text carries the meaning — the code does. The fourth disclosure arm asserts the two details are UNEQUAL rather than merely both refusals, which is the assertion the original defect would have walked straight past.\n\nCHECKLIST ITEMS WITH NOTHING TO REPORT: no new shared mutable state, no check-then-act, no new IO or handles beyond the existing child process, and no new caller-facing input surface — the only new input is a boolean field the production child mints itself.",
      "timestamp": "2026-09-09T09:30:37.996Z"
    },
    {
      "id": "comment-5885ebe966054557bfcb58fb57c75d27",
      "author": "qa-01b7fe3f",
      "content": "QA REVIEW EVIDENCE (qa-01b7fe3f, 2026-09-09). Full record; the approval summary points here.\n\nCOMMIT. 4d86dc58 on moe/work-2026-09-04, Moe-Kind: completion. All 8 row paths byte-identical to it at review time — `git diff HEAD -- <path>` returned 0 lines for each of migration-ports.ts, migration-receipt.ts, migration-service.ts, migration-real.test.ts, migration-service.test.ts, deploy-ledger.ts, deploy-refusal-redaction.test.ts, deploy-migration-disclosure.test.ts. I reviewed the recorded commit, not the dirty tree.\n\nGATES I RE-RAN MYSELF, foreground, full-suite-gate held.\n- `pnpm --filter @moe/daemon test` EXIT 0 -> `Test Files  604 passed (604)` / `Tests  10762 passed | 9 skipped (10771)`, Duration 256.24s. Matches the worker's claim exactly.\n- `pnpm typecheck` EXIT 0 (apps/daemon typecheck: Done).\n- Row's 4 owned test files alone -> `Test Files  4 passed (4)` / `Tests  61 passed | 1 skipped (62)`, EXIT 0.\n- Verbose run proving the new arm EXECUTED rather than skipped:\n  `✓ src/repository/migrations/migration-real.test.ts > names the missing TOOL, not an unknown FILE, when the workspace was never installed 113ms`\n  (the one `↓` in that file is the pre-existing docker-gated restore arm).\n\nDoD 1 — TOOL vs FILE. The `MIGRATE` program is a `String.raw` template, so TypeScript never parses it and typecheck green proves nothing about it. I drilled it by EXTRACTING the emitted text and running it under `node --input-type=module --eval` in %TEMP%. The repo was never mutated; `git status --porcelain -- apps packages adapters` showed only the two peer paths before and after.\n- empty tree -> exit 1, `MOE_MIGRATION_RESULT={\"applied\":[],\"file\":null,\"toolMissing\":true}`\n- PRE-FIX counterfactual, probe line replaced IN MEMORY with a bare `await import('node-pg-migrate');` -> exit 1 and NO marker emitted at all, which is precisely the filed defect (apply then throws MigrationExecutionError(null), toolMissing false, MIGRATION_FILE_UNKNOWN).\nThat is the rail-1 mutation drill: the arm's `toolMissing: true` assertion is reachable only because of the fix.\n\nDoD 2 — survives redaction, redaction not weakened. deploy-service.ts:164 composes `${code}@${layer}: ${detail}`, byte-identical to the single new PUBLIC_PHASE_DETAILS member. Both halves are closed-roster constants, so no caller text can ride. Plain `MIGRATION_FAILED@DAEMON_INGRESS` remains in the impostor list at deploy-refusal-redaction.test.ts:244, and the SAME code+layer carrying a filename tail still redacts — asserted twice, once as a new impostor entry and once as a disclosure arm. deploy-migration-disclosure.test.ts reads the DURABLE ledger, not the returned object, and asserts the filename appears in zero durable bytes.\n\nDoD 3 — both conditions have arms. Uninstalled: migration-real.test.ts, a REAL spawn (verifier-process-runner.ts:78 is `spawn = options.spawn ?? nodeSpawn` and nodeMigrationPorts passes no override), temp tree and store torn down in `finally` on the throwing path. Unreadable file: migration-service.test.ts's 4-case table asserting every non-tool failure keeps MIGRATION_FAILED with its filename, plus the docker-gated live arm left exactly as written.\n\nADVERSARIAL PASS, each independently measured by me.\n1. MigrationCode did NOT widen to `string`. A throwaway probe assigning \"NOT_A_REAL_CODE\" drew TS2322 and TS2345 naming the 10-member literal union including MIGRATION_TOOL_MISSING. Probe deleted in the same call.\n2. A PARTIALLY installed workspace is NOT misclassified. A stub node-pg-migrate whose index.js imports a missing transitive emitted no marker and exited 1 — the old MIGRATION_FAILED path, by design, because the `await import` sits outside the branch.\n3. The working path still works. A stub-installed tree ran the `else` block and exited 0 with `{\"applied\":[],\"file\":null}`, so the new wrapper did not break the success case.\n4. No unnamed roster consumer. Full-tree grep for `SAFE_PHASES|PUBLIC_PHASE_DETAILS` returns only deploy-ledger.ts:39,57 and deploy-refusal-redaction.test.ts:68,211,223,227. Both edited; the near-miss sweep pins `SAFE_PHASES.length * 6` and self-adjusts.\n\nNOTED, NOT GROUNDS FOR REJECTION.\n- deploy-ledger.ts is 255 lines (was 249). Over the 250 soft target, far under the 400 bar; six of the seven added lines are the rail-required comment.\n- deploy-refusal-redaction.test.ts is 409 lines, but it was already 406 BEFORE this row, the cap is per PRODUCTION file, and this row took only the two prescribed one-line edits.\n- The worker's report that plan step-6 drill (iii) cannot red the impostor arm is CORRECT — I confirmed by reading. Both the impostor arm and the filename-tail arm drive recordDeployReceipt / an injected port with a hand-built detail, so neither routes through migration-service.ts. Substituting a fourth drill and saying so, rather than claiming an unreachable red, is the right call.\n\nPEER DIRT, untouched by the worker and by me: apps/daemon/src/http/affordance-read.ts (modified) and apps/daemon/src/preview/preview-goal-landing.test.ts (untracked). The untracked peer test executes inside the 604/10762 count — disclosed by the worker, and it is why the file count reads 604.\n\nfull-suite-gate acquired for my re-run and released; it was granted on to task-8b178e52.",
      "timestamp": "2026-09-09T09:40:49.703Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-08T17:18:11.126Z",
  "updatedAt": "2026-09-09T09:41:31.905Z",
  "metrics": {
    "firstClaimAt": "2026-09-08T18:02:22.676Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 8,
    "executedStepCount": 7,
    "doneAt": "2026-09-09T09:41:24.472Z",
    "wallClockMs": 56341796
  },
  "planSizeWarnings": [
    "Plan touches 8 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-09T09:11:10.211Z",
  "planningNotes": {
    "approachesConsidered": "THE REOPEN'S QUESTION, ANSWERED DELIBERATELY. The governor named two exits and said do not split the difference. I take (b): plain `MIGRATION_FAILED@DAEMON_INGRESS` STAYS REDACTED and is never added to `PUBLIC_PHASE_DETAILS`. Exactly ONE new member is admitted — the tool-missing composite. Separately, and orthogonally, deploy-refusal-redaction.test.ts becomes an owned path because it is an asserting consumer of the roster I am changing; it is edited ADDITIVELY ONLY and its impostor arm at :241 is left intact, where it now serves as this row's own guard that the allowlist was not widened to the generic failure.\n\nWHY (b) AND NOT (a). Measured: today's file-failure detail is `MIGRATION_FAILED@DAEMON_INGRESS: <filename>` — the composite ALWAYS carries a filename tail, because migration-service.ts:69 passes `error.file` and migration-receipt.ts:45 `text()` requires a nonempty string. A filename is caller-influenced text and may never enter the allowlist. The BARE form `MIGRATION_FAILED@DAEMON_INGRESS` that (a) would have admitted is only minted when the migration detail is empty (deploy-service.ts:167) — a state this path cannot reach. So (a) would have admitted a string production never mints, in exchange for reddening a required gate. It buys nothing.\n\nHOW DoD 2 IS THEN SATISFIED. Tool-missing reads POSITIVELY off the deploy receipt as the pure constant `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING`. Every other migration failure reads `[REDACTED]`, and the filename stays on the MIGRATION receipt, which is never declassified. Those two are distinguishable without a debugger and no free text carries the meaning. This is the only shape that satisfies DoD 2 without weakening redaction, because the file case's detail necessarily carries caller-influenced text.\n\nREJECTED — match Node's error MESSAGE. Free-text dependence on an unstable interface; DoD 2 rules it out in principle.\nREJECTED — keep `MIGRATION_FAILED` and set the detail to `MIGRATION_TOOL_MISSING`. Fails DoD 1's literal clause (\"distinguishable from the refusal code alone\") and puts the meaning in the field redaction removes.\nREJECTED — make `MIGRATION_TOOL_MISSING` a DEPLOY refusal code. deploy-service.ts:305-310 states the constraint: deploy codes stamp `DAEMON_DEPLOY_ENGINE`, so a `MIGRATION_*` code cannot be one without inventing a layer.\nREJECTED — catch a bare `ERR_MODULE_NOT_FOUND` from the dynamic import. That code also fires for a TRANSITIVE dependency of an INSTALLED `node-pg-migrate`, which is a partially installed workspace, not a missing tool. `import.meta.resolve` on the root specifier is the discriminator that separates them, and it is available (package.json:10 pins node >=24.16.0; probed RESOLVE_OK on this host).\nREJECTED — fixing the `migrations` directory resolution. Measured not to be the cause.\nCONSIDERED AND ACCEPTED — 8 files, one over the daemon's warn threshold, three under its hard one. The eighth is a NEW small test file rather than 20 more lines in deploy-refusal-redaction.test.ts, which is ALREADY 406 lines at HEAD — past the 400 split bar before this row touches it. Growing it further would deepen a pre-existing violation; the two roster edits it does take are one line each.",
    "codebaseInsights": "THE CHAIN, RE-MEASURED AT HEAD 502bb0af (`git rev-parse --short HEAD`). Every hop confirmed by reading.\n1. migration-ports.ts:21 — the `MIGRATE` eval script's FIRST statement is a TOP-LEVEL `import { runner } from 'node-pg-migrate'`, outside the try that begins at :38. On an uninstalled workspace it throws at module evaluation, so no `MOE_MIGRATION_RESULT=` line is emitted at all.\n2. :62-63 finds no such line; `JSON.parse(... ?? \"null\")` yields null; :64 throws `new MigrationExecutionError(null)`.\n3. :14 is `this.file = migrationFilename(file) ? file : \"MIGRATION_FILE_UNKNOWN\"` — the sentinel is a DEFAULT on a field that means something else.\n4. migration-service.ts:68-69 produces `migrationRefusal(\"MIGRATION_FAILED\", error.file)`. A missing TOOL has become an unknown FILE.\n\nTHE REFUSAL RECEIPT ONLY EXISTS IF THE BACKUP SUCCEEDED. migration-service.ts:67-73: the `MIGRATION_FAILED` branch is guarded by `backupRef !== null`. `ports.dump` runs BEFORE `ports.apply`, so the filed scenario (a reachable database, an uninstalled workspace) lands squarely in that branch. Below it, a failed backup returns the base receipt carrying `MIGRATION_BACKUP_FAILED` — a different condition this row does not touch.\n\nTHE CONSTANT EXISTS AND HAS NO RAISE SITE. Source-scoped `git grep -n MIGRATION_TOOL_MISSING -- 'apps/**' 'packages/**' 'adapters/**' 'docs/**'` returns ONLY controlled-profile-generator.ts:43 (definition) and :55 (type union), controlled-profile-generator.test.ts:9,157,158, repository-bootstrap-read.ts:18 and its test at :177. Nothing raises it. Its comment at :38-41 says the deployment migration consumer is supposed to, and that its refusal uses the EXISTING DAEMON_INGRESS layer — so this row introduces NO new `*_LAYER` constant.\n\nNO IMPORT CYCLE. controlled-profile-generator.ts imports only node:fs, bootstrap-contracts.js and its two sibling template modules. Nothing under repository/migrations/. migration-receipt.ts may import the constant from it safely.\n\nTHE ROSTER IS CLOSED AND DECODING COMES FREE. migration-receipt.ts:12-19 `codeLayers` is a frozen code->layer map, every entry `DAEMON_INGRESS`; `MigrationCode = keyof typeof codeLayers`; decode gates on `Object.hasOwn(codeLayers, v.refusal.code)` at :73 and on the layer matching at :74. A new code decodes once the roster holds it.\n\nTHE DETAIL MUST BE NONEMPTY. migration-receipt.ts:45 `text` requires `value.length > 0`, so an empty-detail refusal cannot decode. Use the existing code-as-detail idiom at :24 (`migrationError` passes the CODE as its own detail). The exact composite the deploy receipt will then carry is `MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING` (deploy-service.ts:167-168, the nonempty-detail branch).\n\nTHE ASSERTING CONSUMER, AND WHAT IT ACTUALLY PINS. deploy-refusal-redaction.test.ts:\n- :67-74 `SAFE_PHASES` is a HAND-RESTATED mirror of the 17 production members, deliberately not imported (the :49-51 comment explains the same reasoning for `REDACTED`). There is NO set-equality arm between mirror and production — source-scoped grep for both names returns only deploy-ledger.ts:39,51 and this file's :67,209,221,225. So a production-only addition reds nothing, and a mirror-only addition would be a silent lie. Edit BOTH.\n- :209 `it.each(SAFE_PHASES)` asserts each member survives verbatim; :221-225 sweeps six near-misses per member and pins `misses.length` to `SAFE_PHASES.length * 6`, so both self-extend when the mirror grows.\n- :236-252 lists `MIGRATION_FAILED@DAEMON_INGRESS` among impostors that MUST redact. This row leaves that entry standing.\n\nWHY THE CODE-LEVEL FIX IS THE RIGHT ONE. `declassifyRefusal` (deploy-ledger.ts:50-54) keeps `code` and `layer` ALWAYS and declassifies only the free-text detail. DoD 1 asks for a distinction \"from the refusal code alone\"; DoD 2 says the code carries the meaning.\n\nFILE SIZES AT HEAD: migration-ports.ts 75, migration-receipt.ts 122, migration-service.ts 98, deploy-ledger.ts 249, migration-service.test.ts 176, migration-real.test.ts 110, deploy-refusal-redaction.test.ts 406 (already past the 400 split bar before this row).\n\nNOT THE CAUSE, AND EXPLICITLY NOT TO BE \"FIXED\": migrations live at `packages/api/migrations/` while the runner reads `migrations` at the workspace root. The filing measured that an installed tree migrates green end to end.",
    "risks": "1. THE ONE-SIDED FIX is the likeliest bad outcome: make everything report `MIGRATION_TOOL_MISSING` and DoD 1's first half passes while a genuine broken migration file becomes unreportable. Step 5's second arm and step 6's drill (iii) hunt it.\n\n2. PARTIAL INSTALLATION MISCLASSIFIED. A dynamic `import('node-pg-migrate')` throws `ERR_MODULE_NOT_FOUND` for a missing TRANSITIVE dependency of an installed package. That is a broken install, not a missing tool. `import.meta.resolve` on the root specifier separates them; a bare error-code match does not.\n\n3. TWO RECEIPTS, ONLY ONE REDACTED. Fixing the migration receipt does NOT fix the operator's view; the DEPLOY receipt is the redacted one. A worker who stops after step 3 satisfies DoD 1 and not DoD 2. Step 6 drill (ii) is the guard.\n\n4. AN ARM THAT MOCKS THE PORT PROVES NOTHING for the uninstalled condition. The defect lives in a child process's module resolution. The arm must call `nodeMigrationPorts().apply` against a real directory with no `node_modules`.\n\n5. THE MIRROR IS HAND-MAINTAINED. Adding to `PUBLIC_PHASE_DETAILS` without adding to `SAFE_PHASES` leaves the new member with zero coverage while every arm stays green.\n\n6. `MigrationExecutionError` HAS FOUR EXISTING POSITIONAL CONSTRUCTION SITES — deploy-service.test.ts:823, migration-observation.test.ts:170, goal-deployment-read.test.ts:107, migration-service.test.ts:124, all `new MigrationExecutionError(\"<filename>\")`. None is an owned path. The new condition must ride an OPTIONAL second parameter so all four keep compiling untouched.\n\n7. `MigrationCode` IS A ROSTER TYPE consumed by 21 modules (`git grep -ln migration-receipt.js -- 'apps/**'`), so run `pnpm typecheck`, not only the daemon test leg.\n\n8. deploy-refusal-redaction.test.ts IS ALREADY 406 LINES at HEAD, past the 400 split bar, before this row touches it. Keep its edits to the two one-line roster entries; the new operator-view arm goes in its own file.\n\n9. TEMP TREE CLEANUP (epic rail 4). The uninstalled-workspace arm creates a directory and spawns a child. Both are torn down on every exit path, including the throwing one, or the arm makes later gates inadmissible.",
    "keyFiles": [
      "apps/daemon/src/repository/migrations/migration-ports.ts",
      "apps/daemon/src/repository/migrations/migration-receipt.ts",
      "apps/daemon/src/repository/migrations/migration-service.ts",
      "apps/daemon/src/deployment/deploy-ledger.ts",
      "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts",
      "apps/daemon/src/deployment/deploy-service.ts",
      "apps/daemon/src/repository/controlled-profile/controlled-profile-generator.ts"
    ]
  },
  "planApprovedAt": "2026-09-09T09:11:12.428Z",
  "contextFetchedBy": [
    "worker-5d2b88e7",
    "architect-f04f69cf",
    "worker-0e797912",
    "qa-01b7fe3f"
  ],
  "workStartedAt": "2026-09-08T18:40:24.360Z",
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorBlockedReason": "Approved step4 (activeAmendmentId null, amendments null verified on disk) instructs PUBLIC_PHASE_DETAILS to admit plain MIGRATION_FAILED@DAEMON_INGRESS. At measured HEAD 2569dd4e, apps/daemon/src/deployment/deploy-refusal-redaction.test.ts:241 explicitly lists that literal as impostor; :246-250 invokes recordDeployReceipt and asserts [REDACTED]. This asserting consumer is absent from all six approved owned paths. Following step4 would knowingly break required daemon gate; changing test without amendment violates unnamed-consumer scope rail. Please amend steps4/5 to include deploy-refusal-redaction.test.ts and update exact policy coverage (seventh path), or explicitly amend step4 to keep plain failure redacted. Step1 read-only complete, step2 started; no source edits or mutation, no tests claimed. Full evidence and exact commands in task comment; Serena handoff saved.",
  "blockedOnTaskIds": null,
  "commits": [
    {
      "sha": "502bb0afe28eee22137ac3b71d916cf20347ef99",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "architect",
      "sessionId": "architect-f04f69cf@2026-09-09T09:04:52Z",
      "paths": [
        ".moe/tasks/task-161b7e9d566b4c47b429490a54a6cca1.json",
        ".moe/tasks/task-39e1628f68f54b8694bfd4a9a8a1b0ad.json",
        ".moe/tasks/task-5380dbf067eb4eeba1cec7117ffa39b5.json",
        ".moe/tasks/task-61f826e50332498eaaaf44f2043845fc.json",
        ".moe/tasks/task-6cbcf5ff05084f3ea2a2e3ea89c3fb54.json",
        ".moe/tasks/task-f4f45c281ab64f6dbf2c4ff163140010.json"
      ],
      "recordedBy": "architect-f04f69cf",
      "recordedAt": "2026-09-09T09:04:56.734Z",
      "treeId": "bf43250d7d0c75a99ee2604221a4b4135f052591",
      "status": "PLANNING",
      "pushed": true
    },
    {
      "sha": "c0c4d9759c8721616032d48d70dfcd1a7586c94e",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-0e797912@2026-09-09T09:11:38Z",
      "paths": [
        ".moe/tasks/task-61f826e50332498eaaaf44f2043845fc.json"
      ],
      "recordedBy": "worker-0e797912",
      "recordedAt": "2026-09-09T09:11:43.608Z",
      "treeId": "7921beef07b532e1933e49c72e291ff3c8b03bb8",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "4d86dc58b3f1e5b1fcbbbc20e19b033d089b44a2",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "completion",
      "role": "worker",
      "sessionId": "worker-0e797912@2026-09-09T09:11:38Z",
      "paths": [
        ".moe/tasks/task-161b7e9d566b4c47b429490a54a6cca1.json",
        ".moe/tasks/task-61f826e50332498eaaaf44f2043845fc.json",
        ".moe/tasks/task-a2dd124fed1b4b9889c626824fc0a6fb.json",
        ".moe/tasks/task-d11695b0b6714183a16146eb4dd444f3.json",
        "apps/daemon/src/deployment/deploy-ledger.ts",
        "apps/daemon/src/deployment/deploy-migration-disclosure.test.ts",
        "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts",
        "apps/daemon/src/repository/migrations/migration-ports.ts",
        "apps/daemon/src/repository/migrations/migration-real.test.ts",
        "apps/daemon/src/repository/migrations/migration-receipt.ts",
        "apps/daemon/src/repository/migrations/migration-service.test.ts",
        "apps/daemon/src/repository/migrations/migration-service.ts"
      ],
      "recordedBy": "worker-0e797912",
      "recordedAt": "2026-09-09T09:32:07.927Z",
      "treeId": "29a8b74f569b190bfa4928a21f056b55fe180fa0",
      "status": "REVIEW",
      "pushed": true
    },
    {
      "sha": "fd2a52e80d7c235771256d1097f63381c64c0c8e",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "qa",
      "sessionId": "qa-01b7fe3f@2026-09-09T09:32:17Z",
      "paths": [
        ".moe/tasks/task-39e1628f68f54b8694bfd4a9a8a1b0ad.json",
        ".moe/tasks/task-61f826e50332498eaaaf44f2043845fc.json",
        ".moe/tasks/task-f4f45c281ab64f6dbf2c4ff163140010.json"
      ],
      "recordedBy": "qa-01b7fe3f",
      "recordedAt": "2026-09-09T09:41:31.905Z",
      "treeId": "4275145f5bfe48b9e8ef874010bb98dcc2cc570d",
      "status": "DONE",
      "pushed": true
    }
  ],
  "filesModified": [
    "apps/daemon/src/repository/migrations/migration-ports.ts",
    "apps/daemon/src/repository/migrations/migration-service.ts",
    "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts",
    "apps/daemon/src/repository/migrations/migration-receipt.ts",
    "apps/daemon/src/deployment/deploy-ledger.ts",
    "apps/daemon/src/repository/migrations/migration-real.test.ts",
    "apps/daemon/src/repository/migrations/migration-service.test.ts",
    "apps/daemon/src/deployment/deploy-migration-disclosure.test.ts",
    ".moe/tasks/task-161b7e9d566b4c47b429490a54a6cca1.json",
    ".moe/tasks/task-61f826e50332498eaaaf44f2043845fc.json",
    ".moe/tasks/task-a2dd124fed1b4b9889c626824fc0a6fb.json",
    ".moe/tasks/task-d11695b0b6714183a16146eb4dd444f3.json",
    ".moe/tasks/task-39e1628f68f54b8694bfd4a9a8a1b0ad.json",
    ".moe/tasks/task-f4f45c281ab64f6dbf2c4ff163140010.json"
  ],
  "unattributedPaths": [],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "qa-01b7fe3f@2026-09-09T09:32:17Z",
    "at": "2026-09-09T09:41:31.905Z"
  },
  "reviewStartedAt": "2026-09-09T09:31:57.656Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  604 passed (604)\n      Tests  10762 passed | 9 skipped (10771)\n   Start at  12:24:55\n   Duration  260.36s (transform 27.82s, setup 0ms, import 314.40s, tests 663.10s, environment 30ms)\n\nGATE_EXIT=0",
    "reportedAt": "2026-09-09T09:31:57.656Z"
  },
  "completionSummary": "A missing migration TOOL now reports as MIGRATION_TOOL_MISSING, not as an unknown FILE, and it survives redaction. 8 files.\n\nDoD 1. migration-ports.ts: the MIGRATE child no longer top-level-imports node-pg-migrate. `emit` moved to the head, then `import.meta.resolve('node-pg-migrate')` in a try; on throw it emits `{applied:[],file:null,toolMissing:true}` and exits 1, else the original body runs in an else block behind `await import(...)`. MigrationExecutionError gained an OPTIONAL SECOND param (default false) so all 4 existing positional sites compile untouched. migration-receipt.ts adds `[MIGRATION_TOOL_MISSING]:\"DAEMON_INGRESS\"` to the frozen codeLayers by IMPORTED CONSTANT — no new *_LAYER, no retyped literal. migration-service.ts branches on it above the MIGRATION_FAILED line; everything else keeps its code and filename.\n\nDoD 2. Exactly ONE member added to PUBLIC_PHASE_DETAILS: \"MIGRATION_TOOL_MISSING@DAEMON_INGRESS: MIGRATION_TOOL_MISSING\". Every character is a production constant end to end. Plain MIGRATION_FAILED@DAEMON_INGRESS was NOT added and stays listed as an impostor — that settles the conflict that reopened this row, per option (b). Redaction unweakened; the code carries the meaning.\n\nDoD 3. Both conditions have arms. Uninstalled: a NEW UNGATED arm in migration-real.test.ts drives the REAL nodeMigrationPorts().apply against an empty temp tree (no docker, no DB) and then the SERVICE end to end to a persisted MIGRATION_TOOL_MISSING receipt; temp tree torn down in finally. Installed-but-broken: the two existing arms unchanged, plus a new 4-case discriminator arm (including a raw ERR_MODULE_NOT_FOUND, the partial-install shape) all pinned to MIGRATION_FAILED with the filename, plus a decode round-trip proving the new code decodes only under DAEMON_INGRESS. New deploy-migration-disclosure.test.ts asserts the operator's view on the DURABLE receipt: tool-missing verbatim, file failure [REDACTED], filename absent from every durable byte, and the two details UN",
  "touchedFiles": [
    "apps/daemon/src/deployment/deploy-migration-disclosure.test.ts",
    "apps/daemon/src/repository/migrations/migration-service.ts",
    "apps/daemon/src/repository/migrations/migration-service.test.ts",
    "apps/daemon/src/repository/migrations/migration-receipt.ts",
    "apps/daemon/src/deployment/deploy-ledger.ts",
    "apps/daemon/src/deployment/deploy-refusal-redaction.test.ts",
    "apps/daemon/src/repository/migrations/migration-ports.ts",
    "apps/daemon/src/repository/migrations/migration-real.test.ts"
  ],
  "completedAt": "2026-09-09T09:41:24.472Z",
  "reviewCompletedAt": "2026-09-09T09:41:24.472Z",
  "reviewSummary": "APPROVED. Full evidence in comment-5885ebe9 on this row.\n\nCOMMIT 4d86dc58 on moe/work-2026-09-04; all 8 row paths byte-identical to it (git diff HEAD = 0 lines each), so I graded the recorded commit, not the dirty tree.\n\nGATES I RE-RAN: `pnpm --filter @moe/daemon test` EXIT 0 -> `Test Files 604 passed (604)` / `Tests 10762 passed | 9 skipped (10771)`, 256.24s, matching the worker exactly. `pnpm typecheck` EXIT 0. Row's 4 owned files alone -> `Test Files 4 passed (4)` / `Tests 61 passed | 1 skipped (62)`. Verbose run proves the new arm EXECUTED, not skipped (113ms).\n\nDoD 1: the MIGRATE program is a String.raw template TypeScript never parses, so I drilled it by EXTRACTING the emitted text and running it in %TEMP% - repo never mutated. Empty tree -> exit 1 with toolMissing:true. PRE-FIX counterfactual (probe replaced in memory by a bare await import) -> exit 1, NO marker at all, reproducing the filed defect. The arm is reachable only because of the fix.\n\nDoD 2: deploy-service.ts:164 composes the detail byte-identically to the one new allowlist member, both halves closed-roster constants. Redaction NOT weakened - plain MIGRATION_FAILED@DAEMON_INGRESS stays an impostor and the same code+layer with a filename tail still redacts. Disclosure arms read the DURABLE ledger and prove the filename is in zero durable bytes.\n\nDoD 3: the uninstalled arm is a REAL spawn (verifier-process-runner.ts:78, no spawn override), torn down in finally; four file-failure cases keep MIGRATION_FAILED with its filename.\n\nADVERSARIAL, measured by me: MigrationCode did NOT widen (probe drew TS2322/TS2345 naming the 10-member union); a PARTIAL install is not misclassified; the success path still runs against a stub-installed tree; no unnamed roster consumer.\n\nNOTED not rejected: deploy-ledger.ts 255 lines (soft 250, hard 400); deploy-refusal-redaction.test.ts 409 but already 406 before this row, and the cap is per production file.\n\nPeer dirt untouched. full-suite-gate released to task-8b178e52."
}