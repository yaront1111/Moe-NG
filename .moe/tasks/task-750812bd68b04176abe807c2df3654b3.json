{
  "id": "task-750812bd68b04176abe807c2df3654b3",
  "epicId": "epic-bd387eeb759e4d62ac27933181a0065e",
  "title": "Re-advertise the two served daemon providers the exact-roster guard caught: documentIngest and planningRuns",
  "description": "FILED by governor-f2db8452 on human instruction, 2026-08-22. Small, fully measured, and the whole diff is expected to be two strings. It is filed as a row rather than left to rail 3 because a SERVED CAPABILITY IS CURRENTLY UNADVERTISED — the exact defect class the bidirectional-roster global rail exists to name — and because every `@moe/daemon` gate on every board currently reads one red it must stop and re-attribute by hand.\n\nFILED IN M1 AND NOT IN THE LANE THAT CAUSED IT, deliberately: the causing commits are the control-room lane (`feat(control-room)` / `feat(daemon)`), whose epic (M3 Canonical Control Room, epic-923660dc) is PLANNED. M1 Foundation Preview is ACTIVE and owns the `apps/daemon` tree the broken test lives in, so filing here gets it fixed rather than shelved. Attribution to the causing commit is recorded below so the record is not distorted by the routing choice.\n\nTHE RED, reproduced independently by three agents (worker-40cfc767, qa-8546c5ba, worker-934d8b66) at committed HEAD 9c09252 with `git status --porcelain -- apps/daemon/src` EMPTY before and after — so it is committed HEAD behaviour, nobody's WIP:\n  FAIL apps/daemon/src/daemon-store-dependencies.test.ts\n     > \"serves the default provider and its registry bridge under plain Node\"\n     providerKeys: + \"documentIngest\"  + \"planningRuns\"   (the expected set lacks both)\n  Test Files 1 failed | 209 passed (210) · Tests 1 failed | 4604 passed | 2 todo (4607)\nExactly ONE failing title. Any additional title, or a changed failure body, is NOT this row and must be re-attributed before it is charged here.\n\nATTRIBUTION, measured by governor-f2db8452:\n  git show --name-only --format=\"\" 1d9d84c | grep daemon-store-dependencies\n    -> apps/daemon/src/daemon-store-dependencies.ts        (production ONLY)\n  count of \"daemon-store-dependencies.test.ts\" in that commit -> 0\nCommit `1d9d84c` (\"feat(daemon): serve document ingest + pending-plan read live\", committed 18:21:31Z) added `documentIngest` (via `createDocumentIngestPort`) and `planningRuns` (`PlanningRunReadPort`) to the production provider in `daemon-store-dependencies.ts` and never touched the test that advertises the roster. Eight files changed, the test not among them.\n\nTHE ADVERTISED ROSTER is a hardcoded literal at `daemon-store-dependencies.test.ts:583-586`:\n  providerKeys: [\n    \"affordances\", \"documentDossiers\", \"graph\", \"provide\", \"reconciliation\", \"restore\",\n    \"sessionHandshake\", \"subscriptions\",\n  ],\nEight entries. The served set is now ten. THE GUARD IS WORKING AS DESIGNED — it caught a capability that is served by the real daemon and advertised nowhere.\n\nTHE FIX SHAPE IS ALREADY RULED ON BY THE TEST'S OWN AUTHOR, in the committed comment two lines above the literal (:581-582), and it pre-refutes the tempting repair verbatim:\n  \"unreachable from the real daemon while every direct-injection test stays green;\n   a subset assertion would have blessed exactly that omission.\"\nSo the repair is to APPEND the two names to the literal. Relaxing `toEqual` to a subset match, dropping the length assertion, or sorting-and-slicing the comparison are all forbidden — each would re-open precisely the hole this guard was written to close, and each would pass.\n\nNOT PRE-JUDGED: whether `documentIngest` and `planningRuns` are the correct final names for the advertised surface, and whether anything ELSE served by `daemon-store-dependencies.ts` is also missing from the literal. The planner measures the served set from the implementation seam and compares, rather than assuming the delta is exactly the two names this report happens to have observed.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK FIRST at the delivered tree: re-run the focused suite and confirm the failing title is still exactly `serves the default provider and its registry bridge under plain Node` with the same two-name roster delta, and confirm `git status --porcelain -- apps/daemon/src` is EMPTY so the red is committed and not a peer's WIP. If the red is gone, or if the delta differs, REPORT THE COLLISION and block rather than editing — another lane may have fixed it while this row waited.",
    "The served set is enumerated FROM THE IMPLEMENTATION SEAM (the provider that `daemon-store-dependencies.ts` actually builds), not from the report in this description, and compared against the advertised literal. If the true delta is larger than `documentIngest` + `planningRuns`, the row fixes the whole delta and says so — a repair sized to the observed failure rather than to the measured set would leave the next omission for someone else to rediscover.",
    "The advertised roster at `daemon-store-dependencies.test.ts:583-586` is corrected by APPENDING the missing names. The `toEqual` set-equality and the accompanying length assertion are PRESERVED EXACTLY. Relaxing the comparison to a subset match, removing the length assertion, or comparing sorted slices is a REJECTION even though every one of those makes the suite green — the committed comment at :581-582 pre-refutes exactly that repair, and re-opening that hole is a worse outcome than the red.",
    "A mutation drill proves the corrected guard still bites: remove ONE name from the advertised literal and confirm the same named test goes RED, then restore byte-exact with an EMPTY `git diff -- apps/daemon/src/daemon-store-dependencies.test.ts` verified in a call SEPARATE from the restore. A green drill means the assertion has detached and must be strengthened before proceeding, not recorded as a pass.",
    "`pnpm --filter @moe/daemon test` is EXIT 0 on a fresh run with the `Test Files N passed` COUNT LINE quoted verbatim — an exit-0 leg with no count line ran nothing and is UNVERIFIED. The prior baseline was `Test Files 1 failed | 209 passed (210)` / `Tests 1 failed | 4604 passed | 2 todo (4607)`; the new run must show the failure gone with the passing count NOT reduced, which is what proves the red was fixed rather than deselected. Commit by explicit pathspec; put the before/after counts in a row comment via moe.add_comment, never in the completion summary — that field is write-only and persisted nowhere."
  ],
  "taskRails": [
    "Scope is the advertised roster and whatever minimum makes it true. Do NOT change production provider wiring in `daemon-store-dependencies.ts` to match the old literal — the production side is correct and the advertisement is what is stale. Removing a served provider to make the test pass is the inverse defect and is a rejection.",
    "Preserve the exact set-equality guard and its length assertion. The committed comment at daemon-store-dependencies.test.ts:581-582 is a standing ruling by the test's author, not a suggestion: a subset assertion would bless the omission this guard exists to catch.",
    "This red is FOREIGN in origin (commit 1d9d84c, control-room lane) and this row is the authorized owner of the repair. Do not amend, revert or reset that commit; fix forward in the test that advertises the roster.",
    "Preserve foreign uncommitted WIP in the shared worktree; stage and commit only explicit owned paths; never git add -A, reset, or stash. Other lanes are actively building against this same tree."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK AND ENUMERATE (re-scoped items 1 and 2, which are one measurement pass). PLANNED UNDER comment-3d1504d885ea44c4bd91e2aca66d8a74, the SOLE planning authority; the definitionOfDone FIELD is stale in items 1-3 and the row TITLE misleads - there is NO production edit to make.\n\nI measured all of this at committed HEAD 17e048a with `git status --porcelain -- apps/daemon/src` EMPTY, so it is committed bytes and nobody's WIP. Re-run at your delivered tree and paste; if the roster differs or the named test FAILS, REPORT THE COLLISION and block rather than re-applying a landed fix.\n\nTHE REPAIR IS PRESENT. daemon-store-dependencies.test.ts:583-586 carries the TEN-name roster: affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions.\n\nTHE TWO ARITIES, each named with the object it came from - this is item 2 and it must be stated, not assumed:\n- DEFAULT EXPORT, the hand-maintained forwarding object at daemon-store-dependencies.ts:316-365, frozen and exported at :367: TEN keys, exactly the ten above. This is the object the subprocess binds - test:505 does `(await import(\"./src/daemon-store-dependencies.ts\")).default` - and test:535 reports `Object.keys(provider).sort()`, which is why the literal is in sorted order.\n- CONSTRUCTED OBJECT, `createStoreDependencies`'s frozen return at :294-306: ELEVEN keys, the same ten PLUS `close`.\nSet-identical on the ten. Delta is exactly `close`, and `close` belongs to the constructed object only.\n\nDO NOT ADD `close` TO THE ROSTER. The governor names this a rejection, and it is a live trap rather than a hypothetical: the JSDoc at :332-338 says \"Every optional factory this root builds must appear here\", so a reader who enumerates the CONSTRUCTED object finds a delta of one and reads that sentence as licence to close it. `close` is a lifecycle method, not an optional port factory - it is correctly absent, and adding it REDS A CURRENTLY-GREEN TEST.\n\nANCHOR NOTE, because a planner will grep: the pre-refuting comment is at :578-582, not :579-582 as the authority comment says; :571 is where the enclosing `toEqual` begins. Cite what you measure.\n\nBaseline: run the focused suite once and quote the count line verbatim before touching anything.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-store-dependencies.ts",
        "apps/daemon/src/daemon-store-dependencies.test.ts"
      ],
      "note": "VERIFY-OR-BLOCK: NO COLLISION. The repair is present in committed bytes, the named test PASSES, and there is nothing to fix. Enumeration done from the implementation seam, programmatically.\n\nDELIVERED TREE: HEAD `17e048a`, `git status --porcelain -- apps/daemon/src` EMPTY. Same sha the plan measured at, so committed bytes and nobody's WIP.\n\nTHE ROSTER IS THE TEN NAMES, at `daemon-store-dependencies.test.ts:583-586`:\n  affordances, documentDossiers, documentIngest, graph, planningRuns, provide,\n  reconciliation, restore, sessionHandshake, subscriptions\nFOCUSED BASELINE, before touching anything:\n  cd apps/daemon && npx vitest run --root . --config package.json src/daemon-store-dependencies.test.ts\n  EXIT 0    Test Files  1 passed (1)    Tests  17 passed (17)\nNo FAIL lines. `serves the default provider and its registry bridge under plain Node` is present exactly once and passes. The red this row was filed for is gone, fixed by `0a6bf25` before the row existed - exactly as the re-scope says.\n\nBOTH ARITIES, ENUMERATED FROM SOURCE BY SCRIPT rather than read off by eye, each named with the object it came from:\n- DEFAULT EXPORT, the hand-maintained forwarding object `const provider` at lines 316-366, `export default provider` at :368: TEN keys - affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions.\n- CONSTRUCTED OBJECT, `createStoreDependencies`' frozen return at lines 294-306: ELEVEN keys - the same ten PLUS `close`.\n- constructed MINUS default = `[\"close\"]`. default MINUS constructed = `[]`.\n- advertised literal == DEFAULT EXPORT -> TRUE. advertised literal == CONSTRUCTED -> FALSE.\nSo the true delta is exactly `close`, it belongs to the constructed object only, and the advertised roster is already correct for the object the test actually binds. DoD-2's \"if the true delta is larger, fix the whole delta\" does not fire: there is no missing name.\n\n`close` IS DELIBERATELY NOT ADDED. The default export is what the subprocess binds (`test:505` imports `.default`, `test:535` reports `Object.keys(provider).sort()`), so the ten-name sorted literal is correct for it. `close` is a lifecycle method, not an optional port factory. Adding it would RED a currently-green test. The trap the plan warned about is real and I walked into its edge: the JSDoc at `:332-338` says \"Every optional factory this root builds must appear here\", which reads as licence to close a one-key delta if you enumerate the CONSTRUCTED object. The sentence is about port factories; `close` is not one.\n\nANCHOR CORRECTIONS, both mine to state because a later reader will grep:\n- The pre-refuting comment is at `:579-582`, not `:578-582` as the plan says (`:578` is the closing `},` of the previous property). The enclosing `toEqual` begins at `:571`. Plan and authority comment each cite it one line differently; `:579-582` is what `grep -n \"THE EXACT KEY SET\"` and `grep -n \"a subset assertion would have blessed\"` return.\n- DoD-3's \"accompanying length assertion\" DOES NOT EXIST, confirming the re-scope. `grep -n \"\\.length\"` over the whole file returns exactly ONE hit, `:136` `expect([...deps.registry.keys()].length).toBeGreaterThan(0)` - a different subject in a different test. Nothing to preserve and, per the plan, nothing to add.\n\nONE INSTRUMENT ERROR OF MY OWN, recorded because it nearly produced a wrong answer and it is the same class this board keeps hitting. My first enumeration script anchored on the literal line `  return Object.freeze({` and took the FIRST match. That string occurs TWICE - lines 77 and 294 - so it enumerated the config object at :77 and reported the constructed object as 6 keys `[credential, nodeSpecsDir, principalId, projectId, storePath, workspaceCatalogPath]`, with a fabricated 6-key delta. It looked like a plausible finding, not like a fault. Fixed by taking the LAST match and printing the anchor line numbers alongside the result, so the reader can see which occurrence answered. A non-unique anchor returns a confident wrong answer, exactly as a wrong key name returns absence.\n\nNOTHING WAS EDITED. No file touched, porcelain still EMPTY.",
      "startedAt": "2026-08-22T21:18:29.333Z",
      "completedAt": "2026-08-22T21:19:39.525Z"
    },
    {
      "stepId": "step-2",
      "description": "THE MUTATION DRILL (item 4) - THE REASON THIS ROW EXISTS. Everything else is measurement; this is the only thing that produces new knowledge. A test that passes is not a test that bites, and this defect existed precisely because the roster and the served set drifted apart unobserved.\n\nRemove ONE name from the roster literal at :583-586 - pick `documentIngest` or `planningRuns`, one of the two the drift actually hid, not a name that was never at risk - and confirm THE SAME NAMED TEST goes RED: `serves the default provider and its registry bridge under plain Node`. Assert it by NAME. A different failing title means you reddened a neighbour and the drill has not tested this guard.\n\nRestore byte-exact. The file is TRACKED, so `git diff -- apps/daemon/src/daemon-store-dependencies.test.ts` is a valid restore proof - run it in a call SEPARATE from the restore, because a restore command returning is not evidence the bytes moved. Empty diff is the proof.\n\nA GREEN DRILL IS A FINDING, NOT A PASS. If the test stays green with a name removed, the set-equality has detached from what it guards and must be strengthened before you proceed - say so in the record rather than recording a pass.\n\nDO NOT relax the `toEqual` at :571-586 to a subset match, `arrayContaining`, or a sorted-slice comparison, and do not invent a length assertion to satisfy the stale field's item 3 - there is no length assertion in this test (`grep -n \"\\.length\"` over the file returns exactly ONE hit at :136, a different subject in a different test) and none may be added. The committed comment at :578-582 pre-refutes the subset repair by name.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-store-dependencies.test.ts"
      ],
      "note": "THE DRILL BITES, AND IT IS THE NARROWEST RESULT THE ARM TITLE CLAIMS. Not a green drill, nothing to strengthen.\n\nMUTANT: removed `documentIngest` - one of the two names the drift actually hid, not a name that was never at risk - from the advertised roster literal at `:583-586`. Applied by an anchored single-occurrence replace that refuses unless the anchor matches exactly once, so the mutation cannot land somewhere I did not intend.\n\nRESULT:\n  cd apps/daemon && npx vitest run --root . --config package.json src/daemon-store-dependencies.test.ts\n  EXIT 1    Test Files  1 failed (1)    Tests  1 failed | 16 passed (17)\n  FAIL  src/daemon-store-dependencies.test.ts > serves the default provider and its registry bridge under plain Node\n\nEXACTLY ONE test red, and it is THE NAMED ONE, asserted by title rather than by count. No neighbour reddened - 16 of 17 still pass - so the drill tested this guard and not something adjacent.\n\nTHE GUARD NAMES THE MISSING KEY, which is the part worth having. The failure diff reports `+ \"documentIngest\"`, so the assertion does not merely say \"these sets differ\"; it says which name went missing. That is the difference between a guard that catches drift and one that only announces it, and it is why the `toEqual` set-equality at `:571-586` is worth preserving as written.\n\nBASELINE vs DRILL, same command, same file:\n  before: EXIT 0    Test Files 1 passed (1)     Tests 17 passed (17)\n  drill:  EXIT 1    Test Files 1 failed (1)     Tests 1 failed | 16 passed (17)\nTotal arm count is 17 in both, so nothing was deselected - the delta is one arm flipping, which is what a sound drill looks like.\n\nRESTORE, PROVEN IN A SEPARATE CALL. Restored with `git checkout -- apps/daemon/src/daemon-store-dependencies.test.ts` issued from the repo root (pathspec resolves against the shell cwd, so running it from a subdirectory would silently restore nothing). Verified in a DIFFERENT command:\n  git diff -- apps/daemon/src/daemon-store-dependencies.test.ts        -> no output\n  git diff --quiet -- apps/daemon/src/daemon-store-dependencies.test.ts -> exit 0\nand the literal re-read from disk shows the ten names back in sorted order. A restore command returning is not evidence the bytes moved; the empty diff from the separate call is.\n\nNOTHING WAS RELAXED. The `toEqual` is untouched - no `arrayContaining`, no subset match, no sorted-slice comparison. No length assertion was added; per step 1 there is none in this test to preserve, and the stale DoD-3 clause that asked for one is superseded by the re-scope.\n\nTREE AFTER THIS STEP: this row has produced zero diff, which is the intended outcome.",
      "startedAt": "2026-08-22T21:19:43.039Z",
      "completedAt": "2026-08-22T21:20:17.932Z"
    },
    {
      "stepId": "step-3",
      "description": "GATE (item 5). `pnpm --filter @moe/daemon test` must be EXIT 0 on a FRESH run, with the `Test Files N passed` count line quoted VERBATIM - an exit-0 leg with no count line ran nothing and is UNVERIFIED.\n\nTHE COUNT IS THE EVIDENCE, NOT THE EXIT CODE. The pre-repair baseline recorded on this row was `Test Files 1 failed | 209 passed (210)` and `Tests 1 failed | 4604 passed | 2 todo (4607)`. Your run must show the failure GONE with the passing count NOT REDUCED. A green run with a smaller passing count means the red was deselected rather than fixed, and that is the one outcome this clause exists to catch.\n\nAlso run the repo-wide leg as a PATH-ATTRIBUTED BASELINE: failures at the merge-base, failures at HEAD, and the delta intersected with this row's owned paths must be EMPTY. Disclose any foreign red VERBATIM with its exact failing lines and file paths - this row's own description records that the red it was filed for was foreign in origin (commit 1d9d84c, control-room lane), so foreign reds on this tree are expected and are disclosed, never hidden and never fabricated green.\n\nRun `git status --porcelain -- apps/daemon/src` again at the end and confirm it is EMPTY, which is simultaneously the drill-restore proof and the proof this row changed nothing.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-store-dependencies.test.ts"
      ],
      "note": "GATE GREEN, AND THE COUNT - NOT THE EXIT CODE - IS THE EVIDENCE.\n\n  pnpm --filter @moe/daemon test        EXIT 0\n    Test Files  211 passed (211)\n         Tests  4634 passed | 1 todo (4635)\n  No FAIL lines at all.\n\nTHE PASSING COUNT WENT UP, WHICH IS THE CLAUSE THAT MATTERS. Against the pre-repair baseline recorded on this row:\n  before:  Test Files  1 failed | 209 passed (210)   Tests  1 failed | 4604 passed | 2 todo (4607)\n  now:     Test Files            211 passed (211)    Tests            4634 passed | 1 todo (4635)\n  files passing 209 -> 211 (+2), tests passing 4604 -> 4634 (+30), failures 1 -> 0.\nNothing was deselected: the passing count is not reduced, it grew, and the total arm count grew with it (4607 -> 4635). A green run with a SMALLER passing count is the outcome this clause exists to catch and it is not what happened.\n\nONE COUNT MOVED DOWNWARD AND IT IS NOT A DESELECTION: `todo` 2 -> 1. That is `task-f432799c` opening the production-applied PARTIAL arm in `budget-coverage-reader.test.ts` (DONE at 20:41Z) - a todo became a real executing test, which is why the executed total rose by more than the files added. Naming it so the single downward number is not read as an arm going missing.\n\nPATH-ATTRIBUTED BASELINE.\n- Owned paths for this row: `apps/daemon/src/daemon-store-dependencies.test.ts`. THIS ROW HAS ZERO DIFF, so the delta intersected with owned paths is EMPTY BY CONSTRUCTION - there are no owned bytes for a regression to live in. The baseline leg is corroborating here rather than load-bearing.\n- HEAD failing-path set at `17e048a`: EMPTY.\n- Merge-base `d543f71` (`git merge-base HEAD main`, unchanged): failing-path set of 10 files, measured earlier tonight in a DETACHED WORKTREE at that sha - project-configuration-selection, document-work-{identifiers,ingress,persistence,read,tail}, http/foundation-registry, index-surface, planning/active-graph-projection, planning/graph-body-record. None is an owned path.\n- Difference (failing at HEAD but not at merge-base): EMPTY. The set went 10 -> 0, so there are no additions at all.\n\nWHY THE IN-PLACE RUN IS ADMISSIBLE HERE, stated because on this tree it usually is not. `git status --porcelain -- apps packages scripts tests` is EMPTY - no modified and no untracked source anywhere in the repo. With no working-tree deviation the in-place run reads exactly the committed bytes of `17e048a`, so it is equivalent to a clean detached-worktree run without paying for one. That was NOT true two hours ago on this same tree, which is why the previous row needed worktrees; it is true now and the porcelain output is the proof.\n\nNO FOREIGN RED TO DISCLOSE. The package leg is fully green, so there is nothing to attribute. The red this row was originally filed for - `daemon-store-dependencies.test.ts` on the `providerKeys` roster, foreign in origin from `1d9d84c` - is gone, fixed by `0a6bf25`.\n\nFINAL TREE STATE, which is simultaneously the drill-restore proof and the proof this row changed nothing:\n  git status --porcelain -- apps/daemon/src   ->  EMPTY\n  HEAD 17e048a, unchanged from step 1.",
      "startedAt": "2026-08-22T21:20:21.412Z",
      "completedAt": "2026-08-22T21:22:08.016Z"
    },
    {
      "stepId": "step-4",
      "description": "RECORD (item 5's evidence half). Put everything in a ROW COMMENT via moe.add_comment - never in the completion summary, which is write-only and persisted nowhere, so a clause graded against it fails for the worker who wrote it.\n\nRECORD: (a) the verify-or-block output proving the ten-name roster is in committed bytes and the named test passes; (b) BOTH arities with the object each came from - default export TEN at :316-365, constructed object ELEVEN at :294-306 - and the explicit statement that `close` is the whole delta and was deliberately NOT added; (c) the drill: which name you removed, the failing test title verbatim, and the empty `git diff` from its separate call; (d) before/after count lines with the passing count shown not reduced; (e) the repo-wide path-attributed delta and any foreign red verbatim.\n\nTHIS ROW IS EXPECTED TO PRODUCE ZERO DIFF, and that is the correct outcome, not a failure to deliver. The repair landed in commit 0a6bf25 before the row was filed; the deliverable here is CERTIFICATION that the guard bites. Do not manufacture an edit in order to have something to commit. There is nothing to commit by pathspec; the evidence comment IS the artifact. QA: the absence of a commit bearing this task's id is not a rejection reason here - it is what this row's re-scope requires.\n\nOUT OF SCOPE, recorded so it is neither lost nor swept in: the JSDoc at :332-338 states a production invariant - every optional factory the root builds must appear in the forwarding object - whose failure mode is a port answering GRAPH_QUERY_UNAVAILABLE on a real authenticated request while every direct-injection test stays green. A BIDIRECTIONAL guard between the constructed object and the forwarding object would catch that whole class, and it must exempt `close`, which is a lifecycle method rather than a port factory. That is a SEPARATE row. Name it in the comment for the governor to file; do not add it here.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-store-dependencies.test.ts"
      ],
      "note": "RECORDED as `comment-58c34109adb5488f81649c43fc6ff473` on the row - the artifact of this certification. All five parts (a)-(e) are there: verify-or-block, both arities with the object each came from, the drill with its verbatim failing title and the empty separate-call diff, the before/after counts, and the path-attributed delta.\n\nZERO DIFF, WHICH IS THE DELIVERABLE. Nothing committed, nothing edited, nothing staged. `git status --porcelain -- apps/daemon/src` EMPTY at HEAD `17e048a`, unchanged from step 1 through step 4. No commit bearing this task id exists and none should; per the re-scope that is the required outcome, not a shortfall.\n\nADVERSARIAL PASS OVER A ZERO-DIFF ROW - the question is not \"what did I break\" but \"what could make this certification false\":\n- Could the drill have passed for the wrong reason? No. It reddened EXACTLY the named title, 1 of 17, and the failure diff names `documentIngest` specifically rather than reporting a generic set mismatch.\n- Could the restore have silently no-opped? The `git checkout --` was issued from the repo root, and the proof is an empty `git diff` plus `--quiet` exit 0 from a SEPARATE call, not from the restore command returning.\n- Could the green gate be a deselection? No - the passing count ROSE (209->211 files, 4604->4634 tests). The one downward number, `todo` 2->1, is `f432799c` opening an arm and is named in the comment.\n- Could the in-place run have been polluted by a peer? `git status --porcelain -- apps packages scripts tests` is EMPTY, so there is no working-tree deviation from the committed sha anywhere in the repo. That is what makes an in-place run admissible here where it was not two hours ago.\n- Could I have enumerated the wrong object? That is exactly the error I made and caught: a non-unique anchor gave me a fabricated six-key delta. Corrected, and both the fix and the wrong answer are in the comment so the next reader sees the failure mode rather than only the result.\n\nTWO ANCHOR CORRECTIONS carried into the record, both against the plan text: the pre-refuting comment is `:579-582` not `:578-582`, and DoD-3's \"length assertion\" does not exist (`grep -n \"\\.length\"` returns one hit at `:136`, a different subject).\n\nOUT-OF-SCOPE ROW NAMED FOR THE GOVERNOR, not filed by me and not swept in. Today's guard is ONE-DIRECTIONAL: it pins the default export against a hand-written literal, so it catches \"the advertisement drifted from the shipped object\" but never compares the CONSTRUCTED object to the forwarding one - so \"the root built a port and forgot to forward it\" is still unguarded, and that is the exact `graph` failure mode the JSDoc at `:332-338` was written about. A bidirectional guard, exempting `close` as a lifecycle method, would have caught the `documentIngest`/`planningRuns` drift in `1d9d84c` itself rather than ninety minutes later after it had blocked three rows. Details are in the comment.",
      "startedAt": "2026-08-22T21:22:11.754Z",
      "completedAt": "2026-08-22T21:23:05.529Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "RE-SCOPED FIX -> CERTIFY and returned to PLANNING by governor-f2db8452, 2026-08-23, under the split governor-7459dc6a and I agreed (they assigned me this row's re-scope and DoD correction).\n\nREAD comment-3d1504d885ea44c4bd91e2aca66d8a74 BEFORE PLANNING. It is the SOLE planning authority and it SUPERSEDES comment-19a757b7. The definitionOfDone FIELD is stale in items 1, 2 and 3, and this row's TITLE misleads — \"re-advertise\" reads as a production change and there is none to make.\n\nWHY IT WAS PARKED: the premise was spent before the row existed. Commit 0a6bf25 landed the two names at 20:29:00Z; I filed the row at 20:32:57Z. HEAD moved between my measurement and my filing. My own DoD-1 verify-or-block caught it.\n\nTHREE DoD DEFECTS, ALL MINE, ALL FOUND BY worker-40cfc767 AND VERIFIED BY ME AT HEAD 17e048a:\n- DoD-3 required preserving a \"length assertion\" that does not exist: `grep -n \"\\.length\"` over the test file returns EXACTLY ONE hit at :136, a different subject in a different test. Ungradeable as written.\n- DoD-2 named two different objects and the wrong reading REDDENS A GREEN TEST. `createStoreDependencies()`'s return at :294-305 INCLUDES `close`; the `export default` forwarding object at :309-368 does NOT; the subprocess binds the DEFAULT EXPORT at test:505. Followed literally my clause makes a planner add `close` and break a passing test. Now disambiguated to the default export, with adding `close` named as a rejection.\n- The title implies a production edit. 1d9d84c had already put both names in the constructed object AND the default export, missing only the test roster.\n\nWHAT REMAINS, AND WHY THE ROW IS NOT EMPTY: nobody has enumerated the seam rigorously, and nobody has drilled the corrected guard. A test that passes is not a test that bites, and this defect existed precisely because the roster and the served set drifted apart unobserved. DoD 4 (remove ONE name, confirm the SAME named test reds by name, restore byte-exact with an EMPTY `git diff` verified in a separate call) and DoD 5 (focused suite EXIT 0 with the count line quoted, foreign reds disclosed and path-attributed) are unchanged and are the substance. Certifying the guard bites is worth the row; re-fixing a landed fix is not.\n\nSMALL ROW. Expect a short plan: verify-or-block, enumerate the default export against the literal, drill, gate, record in a row comment. Do not expand it — the bidirectional constructed-vs-forwarding guard suggested by the JSDoc at :332-338 is explicitly OUT OF SCOPE and belongs to a separate row.",
  "createdBy": "HUMAN",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 143,
  "comments": [
    {
      "id": "comment-06ff26f3cb4848db9fbb6e0c750ba461",
      "author": "qa-8546c5ba",
      "content": "QA grading basis, from the agent who reproduced this red independently at 19:51Z (HEAD `9c09252`, `apps/daemon`, focused run: `Test Files 1 failed (1) | Tests 1 failed | 16 passed (17)`).\n\nWhat I will verify, beyond \"the suite is green\":\n\n1. **`toEqual` stays.** Relaxing the assertion to a subset/`objectContaining`/`arrayContaining` match is a reject even with a green suite. The test's own comment at `:581-582` pre-refutes that repair: a port built by `createStoreDependencies` but missing from the shipped default object is unreachable from the real daemon while every direct-injection test stays green. A subset match retires the only guard that catches that, permanently, in exchange for two strings.\n\n2. **The two keys must be derived from the shipped object, not transcribed from the failure diff.** Copying `documentIngest`/`planningRuns` out of the vitest `+` lines makes the roster a transcript of one failure rather than a statement about the seam. Name the seam sites in the completion note — `daemon-store-dependencies.ts:298` and `:300` in the returned default object (and `daemon-entry-port-resolution.ts:51`) — and show the roster matches what that object actually exports today. If a third key is served and also missing, a diff-copy repair will not find it and I will.\n\n3. **Sorted position preserved.** The roster reads as a sorted exact set; inserting out of order makes the next drift diff unreadable.\n\n4. **Post-fix evidence must be the run, not the edit.** Quote the count line from `pnpm --filter @moe/daemon test` at your delivered tree with EXIT recorded, unpiped. `Test Files 210 passed (210)` is the expected shape — a green claim without the count line is unverifiable, and the pre-fix baseline to beat is `1 failed | 209 passed (210)` / `Tests 1 failed | 4604 passed | 2 todo (4607)`.\n\nAttribution is settled and needs no re-derivation: `git log -S \"planningRuns\" -- apps/daemon/src/daemon-store-dependencies.ts` and the same for `documentIngest` both name `1d9d84c` alone (committed 18:21:31Z); `8a8248c` is a bystander that `-S` over the whole tree also names. Worktree was clean under `apps/daemon/src` at reproduction.\n\nNote for whoever gates this: while this row is open, the daemon test leg cannot exit 0 for any row, so an exit-0 DoD clause elsewhere cannot be satisfied by chat consensus that the red is foreign. That is a reason to land this quickly, not a reason to widen it.",
      "timestamp": "2026-08-22T19:56:47.407Z"
    },
    {
      "id": "comment-6ba05650135b49cdb59df77949dc831e",
      "author": "architect-a69eb088",
      "content": "VERIFY-OR-BLOCK measurement at delivered HEAD 9c092528ef6142932a5afe71383bbd1848269f4d (2026-08-22).\n\nThe behavioral premise still reproduces exactly on the two relevant committed files:\n- `daemon-store-dependencies.ts` worktree hash = HEAD hash `1379de2fd03e2ce2735ab1512eec5ea591e4cd28`.\n- `daemon-store-dependencies.test.ts` worktree hash = HEAD hash `8fa00655ce16e6ce96fbb4da26c5bcf8821dbbe3`.\n- Focused command `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/daemon-store-dependencies.test.ts -t \"serves the default provider and its registry bridge under plain Node\"` -> EXIT 1; Test Files 1 failed (1), Tests 1 failed | 16 skipped (17). The only delta is received `documentIngest` and `planningRuns` in providerKeys.\n\nHowever DoD 1's mandatory broad cleanliness gate is NOT met:\n`git status --porcelain -- apps/daemon/src` currently reports foreign WIP:\n- `M apps/daemon/src/budget/budget-settlement-application.test.ts`\n- `M apps/daemon/src/work/launch-template-producer.test.ts`\n- `?? apps/daemon/src/budget/budget-settlement-fixtures.ts`\n\nThose paths are outside this row's owned surface and must be preserved. The DoD explicitly requires apps/daemon/src to be EMPTY and says to block rather than edit when it is not. I therefore did not submit a plan or touch the roster. Once those foreign bytes land/clear, re-run the same gate; if clean, the measured implementation is one test-file edit preserving exact set equality and length.",
      "timestamp": "2026-08-22T19:57:30.966Z"
    },
    {
      "id": "comment-12de4a38670f45529a9806de4dacc347",
      "author": "architect-eecf80ed",
      "content": "DoD-1's CLEAN-TREE CLAUSE IS UNSATISFIABLE EXACTLY WHEN THIS ROW MATTERS — and its stated PURPOSE is already met. Measured at HEAD `e8cd214`, commands pasted. **Recommending a one-line amendment; I am not editing anything and this row is not mine.**\n\n**@architect-a69eb088's block was CORRECT.** DoD-1 says `git status --porcelain -- apps/daemon/src` must be EMPTY, it was not, and a written DoD clause outranks a judgement call. They verified the substance first (*\"the exact focused failure still reproduces from byte-identical target files\"*) and blocked on the letter anyway, which is the right order.\n\n**THE MEASUREMENT THAT MAKES THIS AN AMENDMENT RATHER THAN A WAIVER:**\n```\ngit status --porcelain -- apps/daemon/src/daemon-store-dependencies.ts \\\n                          apps/daemon/src/daemon-store-dependencies.test.ts \\\n                          apps/daemon/src/daemon-entry-port-resolution.ts\n  -> EMPTY.  All three TARGET files are byte-clean.\n\ngit status --porcelain -- apps/daemon/src   -> 5 entries, every one FOREIGN and in another subdirectory:\n   M  work/foundation-attempt-store.ts\n   ?? work/foundation-artifact-ledger.{ts,js}\n   ?? work/foundation-artifact-manifest.{ts,js}\n```\nThose five belong to the **`4a318d03` artifact-ledger lane**, mid-flight. That lane exists because of a block I filed, so I am not a neutral party here and say so plainly.\n\n**DoD-1's own stated purpose is \"so the red is committed and not a peer's WIP.\" That purpose is SATISFIED TODAY** — the files this row repairs carry no uncommitted bytes, so the failure demonstrably comes from committed code. The broad tree check fails only because an unrelated lane is working elsewhere under the same directory.\n\n**THE DEADLOCK, stated once: `apps/daemon/src` is never empty while `@moe/daemon` is under active development — which is the same condition that makes this red urgent.** The red blocks the gate of every daemon-owned row (`f432799c` is committed and DONE-but-for-one-exit-code behind it right now), so the busier the package gets, the more this row is needed and the less its gate can ever open.\n\n**RECOMMENDED AMENDMENT — one clause, intent preserved:**\n> In DoD-1, replace the porcelain check with `git status --porcelain -- apps/daemon/src/daemon-store-dependencies.ts apps/daemon/src/daemon-store-dependencies.test.ts apps/daemon/src/daemon-entry-port-resolution.ts` EMPTY.\n\nStrictly as strong for the purpose — it proves the red is committed **in the files being repaired** — satisfiable in a shared worktree, and satisfied right now.\n\n**NOTHING ELSE IN THIS DoD SHOULD MOVE, and items 2 and 3 in particular must not.** The `toEqual` set-equality and the length assertion at `daemon-store-dependencies.test.ts:583-586` are the load-bearing part: the committed comment at `:581-582` pre-refutes the subset repair, and DoD-3's mutation drill (remove one name, confirm the same named test reds, restore with an EMPTY `git diff` verified in a SEPARATE call) is what proves the corrected guard still bites. **Appending the two names in sorted position is the whole fix; relaxing the comparison would re-open the exact hole the guard was written to close.**\n\nAlso worth keeping from @worker-40cfc767's report (`msg-77734e9e`): the second daemon red — `runtime-entrypoint.test.ts` reporting `unexpected: [\"work\\\\foundation-artifact-ledger.js\", \"work\\\\foundation-artifact-manifest.js\"]` — is the same foreign lane's untracked bridges and **clears on its own when that lane lands**. It needs nobody, and it should not be confused with this roster red when someone re-runs the daemon leg.\n\nIf a governor amends DoD-1 I will plan this row immediately: it is two strings plus a drill, and the step-1 measurement above is ready to paste.",
      "timestamp": "2026-08-22T20:13:49.814Z"
    },
    {
      "id": "comment-29ded0b26e744c31921bd9b47637ef3c",
      "author": "architect-eecf80ed",
      "content": "DoD-2 PRE-MEASURED: **the true delta is EXACTLY `documentIngest` + `planningRuns`. Two names, no more.** And there is an adjacent seam that yields a WRONG delta of three — I read it first, so this comment names it. Measured at HEAD `e8cd214`; the planner should still re-run at their own tree, but this de-risks the step.\n\n**THE RIGHT SEAM.** DoD-2 says enumerate from \"the provider that `daemon-store-dependencies.ts` actually builds\". There are **two** such objects and only one is the subject:\n- `daemon-store-dependencies.test.ts:505` (inside the plain-Node probe) is `const provider = (await import(\"./src/daemon-store-dependencies.ts\")).default;` and `:535` is `providerKeys: Object.keys(provider).sort()`.\n- So the enumerated object is **the module's DEFAULT EXPORT** — the lazy facade at `daemon-store-dependencies.ts:316`, exported at `:368`.\n\n**THE FACADE'S TEN KEYS, read off `:316-367`:**\n```\naffordances · documentDossiers · documentIngest · graph · planningRuns\nprovide · reconciliation · restore · sessionHandshake · subscriptions\n```\n**ADVERTISED literal at `:583-586` — eight:**\n```\naffordances · documentDossiers · graph · provide · reconciliation · restore\nsessionHandshake · subscriptions\n```\n**DELTA = {documentIngest, planningRuns}. Exactly the two names in this row's title.** DoD-2's \"if the true delta is larger, fix the whole delta\" branch does NOT fire.\n\n**THE ADJACENT SEAM THAT GIVES THE WRONG ANSWER — I walked into it before catching myself.** `createStoreDependencies`' own return at `:294-306` is a *different* object: it carries `close`, which the facade does not. Enumerate THAT one and the delta reads **three** — `close`, `documentIngest`, `planningRuns` — and appending `close` to the advertised literal would red the corrected test on a name the shipped provider never exposes. The assertion's committed comment at `:579-582` draws exactly this distinction — *\"A port constructed by `createStoreDependencies` but absent from the shipped default object is unreachable from the real daemon\"* — so the two objects differing is the very thing the guard polices, and confusing them is the natural mistake.\n\n**SORTED POSITIONS, since DoD-2 requires appending in sorted order and `Object.keys(...).sort()` is lexicographic:** `documentDossiers` < `documentIngest` (`D` < `I` at the ninth char) and `planningRuns` < `provide` (`l` < `r`). So the corrected literal is:\n```ts\nproviderKeys: [\n  \"affordances\", \"documentDossiers\", \"documentIngest\", \"graph\", \"planningRuns\", \"provide\",\n  \"reconciliation\", \"restore\", \"sessionHandshake\", \"subscriptions\",\n],\n```\nTwo inserted strings, everything else byte-identical. **`toEqual` and the length assertion stay exactly as they are** — DoD-2 makes relaxing them a rejection, and the facade being enumerated by `Object.keys` is why the exactness is load-bearing rather than pedantic.\n\n**Also confirming the resolution-side roster is already correct and needs no edit:** `daemon-entry-port-resolution.ts:50-53` `FACTORIES` already lists both names among its eight. The omission is only in the test's advertised literal.\n\n**One caveat on my own measurement, stated because it is the class of error this row exists to fix:** I enumerated the facade by reading its top-level keys out of the source range `:316-367`, not by executing `Object.keys` on the imported default. Those agree unless something adds a key dynamically, which nothing in that literal does — but the planner's step-1 re-run should take the count from the failing test's own output, which prints both sets, rather than from this comment.\n\nThis is an addendum to `comment-12de4a38`, where I recommended scoping DoD-1's porcelain check to the three target files; that clause is still what blocks the row, and this measurement does not change it.",
      "timestamp": "2026-08-22T20:22:23.029Z"
    },
    {
      "id": "comment-5240dfa96e09403c90806003aa0d5222",
      "author": "architect-eecf80ed",
      "content": "**THE FIX IS ALREADY WRITTEN IN THE SHARED WORKTREE, UNCOMMITTED — and my 20:13Z amendment recommendation (`comment-12de4a38`) is now STALE. Retracting it here rather than letting it be read as current.** Measured at HEAD `d7eb0d1`.\n\n```\ngit diff -- apps/daemon/src/daemon-store-dependencies.test.ts\n@@ -581,8 +581,8 @@\n       providerKeys: [\n-        \"affordances\", \"documentDossiers\", \"graph\", \"provide\", \"reconciliation\", \"restore\",\n-        \"sessionHandshake\", \"subscriptions\",\n+        \"affordances\", \"documentDossiers\", \"documentIngest\", \"graph\", \"planningRuns\", \"provide\",\n+        \"reconciliation\", \"restore\", \"sessionHandshake\", \"subscriptions\",\n       ],\n```\n**Byte-identical to the repair `comment-29ded0b2` derived from the facade seam**: the two names inserted in sorted position, `toEqual` and the length assertion preserved exactly, the `:579-582` comment untouched, nothing else in the file changed. DoD-2 and DoD-3's substance are already satisfied by these bytes — what is missing is a commit and a drill.\n\n**WHY MY EARLIER RECOMMENDATION NO LONGER WORKS.** I proposed scoping DoD-1's porcelain check to the three target files, on the measurement that all three were clean. **`daemon-store-dependencies.test.ts` is no longer clean — it holds this very fix — so the scoped check now fails too.** A clean-tree gate of any width cannot be satisfied by a row whose own repair is already sitting in the tree.\n\n**THE AMENDMENT THAT ACTUALLY FITS: adopt-and-verify, not demand-clean.** Replace DoD-1's porcelain clause with a bytes check:\n> Confirm the working-tree edit to `daemon-store-dependencies.test.ts` is EXACTLY the sorted insertion of `documentIngest` and `planningRuns` into `providerKeys`, with `toEqual`, the length assertion and the `:579-582` comment unchanged and no other hunk in the file (`git diff -- <path>` shows one hunk, +2/-2). Then commit it by explicit pathspec. If the diff contains anything else, REPORT THE COLLISION and block.\n\n**That is strictly stronger than the original clause**, because it verifies the bytes that will be committed rather than the tidiness of a directory that other lanes legitimately occupy. It also keeps the row honest about not blessing a stranger's unrelated edit.\n\n**WHY THE FIX IS STRANDED, and it is nobody's fault.** This row owns the file but is BLOCKED. Whoever wrote the edit is on another lane, where epic rail 3 forbids staging a file outside their owned paths — the bind @worker-40cfc767 described at 20:11Z. So the repair exists, is correct, and no agent is currently permitted to land it.\n\n**CONTEXT THE CLAIMER NEEDS: the roster is NOT the dominant red.** At `d7eb0d1` all ten dirty entries under `apps/daemon/src` belong to the `4a318d03` lane, and @worker-934d8b66's measurement attributes **49 of 62** daemon failures to the uncommitted `work/foundation-attempt-store.ts` (+15) plus `goals/goal-closure-test-fixtures.ts` throwing `FOUNDATION_VERIFICATION_ATTEMPT_UNPROVEN` across goals, evidence, bootstrap, foundation-attempt and security. **So DoD-4's `pnpm --filter @moe/daemon test` EXIT 0 will NOT be reachable from this row alone** even after the roster is fixed — it needs that lane to commit first. Whoever plans this should expect DoD-4 to require either that sequencing or its own path-attributed amendment, and should not read a still-red daemon leg as evidence the roster repair failed.\n\nThree rows are waiting on this package gate: `f432799c` (committed `80cd6d1`), `ee5a385b` (committed `2adc7dc`, all 8 steps done, own suites 107/107 EXIT 0), and this one.",
      "timestamp": "2026-08-22T20:30:30.224Z"
    },
    {
      "id": "comment-19a757b764204065bbd4f864090c5af5",
      "author": "governor-f2db8452",
      "content": "GOVERNOR DoD AMENDMENT — governor-f2db8452, 2026-08-22. THIS COMMENT IS THE AMENDMENT AUTHORITY; the definitionOfDone FIELD is now STALE in two items and must not be graded as written. Two things changed since filing: I made a drafting error in DoD-1, and the repair landed while we were discussing it.\n\nPART 1 — DoD-1 WAS UNSATISFIABLE AND THAT IS MY FAULT, NOT THE PLANNER'S.\nI wrote \"confirm `git status --porcelain -- apps/daemon/src` is EMPTY so the red is committed and not a peer's WIP.\" @architect-a69eb088 correctly blocked on it: the clause was written, it was not met, and a written DoD clause outranks a judgement call. They verified the substance first and blocked on the letter anyway — the right order, and I am not overriding it.\n\nThe clause is defective because `apps/daemon/src` is NEVER clean while `@moe/daemon` is under active development, which is the same condition that makes this row urgent. @architect-eecf80ed diagnosed this at 20:13Z (comment-12de4a38), proposed narrowing the check to the three target files, then RETRACTED that himself (comment-5240dfa9) on the sharper observation that the target file had stopped being clean because it held the fix. A clean-tree gate of ANY width cannot be satisfied by a row whose own repair is already in the tree.\n\nDoD-1 IS REPLACED BY: prove the defect from COMMITTED BYTES, which is what the clause was always trying to establish and does so more directly than tree tidiness ever did:\n  git show <sha>:apps/daemon/src/daemon-store-dependencies.test.ts   -> the advertised literal\n  git show <sha>:apps/daemon/src/daemon-store-dependencies.ts        -> the served facade\nA delta between those two reads is committed by construction and cannot be a peer's WIP, whatever else is dirty in the directory. Worktree porcelain is RECORDED for attribution, never REQUIRED to be empty.\n\nPART 2 — THE REPAIR HAS LANDED. DoD 1, 2 and 5 ARE DISCHARGED BY COMMIT `0a6bf25`.\n\"fix(daemon): add the two shipped ports to the exact providerKeys roster\". I verified the bytes rather than the message:\n  git show --stat 0a6bf25 -> 1 file changed, 2 insertions(+), 2 deletions(-), ONE hunk at :581\n  +  \"affordances\", \"documentDossiers\", \"documentIngest\", \"graph\", \"planningRuns\", \"provide\",\n  +  \"reconciliation\", \"restore\", \"sessionHandshake\", \"subscriptions\",\nChecked against this row's rails, all of which hold: the two names are inserted in SORTED position; `toEqual` at :571 is preserved; the length/shape assertion is untouched; the author's pre-refutation comment at :579-582 is byte-identical; there is no subset match, no `arrayContaining`, no other hunk, no production-side edit. DoD-2's \"measure the delta from the implementation seam\" was independently discharged by @architect-eecf80ed in comment-29ded0b2 (facade at :316/:368 serves TEN keys, literal advertised EIGHT — true delta EXACTLY two). The commit's own verification reads `Test Files 211 passed (211)` / `Tests 4634 passed | 1 todo (4635)` / EXIT 0.\n\nThat the fix arrived in a commit not bearing this row's id is NOT a defect and NEVER a rejection reason — the global rail on foreign commits is explicit. This row is verified on the committed bytes.\n\nPART 3 — WHAT IS ACTUALLY LEFT. Exactly one substantive item, and it is worth keeping.\nDoD-3, THE MUTATION DRILL, IS NOT DISCHARGED. Nobody has proven the CORRECTED guard still bites. Remove ONE name from the roster literal, confirm the same named test (\"serves the default provider and its registry bridge under plain Node\") goes RED by name, restore byte-exact with an EMPTY `git diff -- apps/daemon/src/daemon-store-dependencies.test.ts` verified in a call SEPARATE from the restore. A green drill means the assertion detached and must be strengthened, not recorded as a pass. Note the drill target is a TRACKED file, so `git diff` is a valid restore proof here.\n\nDoD-4 IS AMENDED to the path-attributed baseline that global rail 3 already defines, because package-wide EXIT 0 is NOT reachable from this row: the `4a318d03` artifact-ledger lane holds uncommitted `work/foundation-attempt-store.ts` plus untracked `work/foundation-artifact-{ledger,manifest}.{ts,js}`, and @worker-934d8b66 attributed 49 of 62 daemon failures to those bytes. REQUIRED INSTEAD: the FOCUSED file `daemon-store-dependencies.test.ts` runs EXIT 0 with its count line quoted, AND the repo-wide failing-path set at HEAD minus merge-base, intersected with this row's owned paths, is EMPTY. Foreign reds are disclosed verbatim with path attribution, never edited and never fabricated green.\n\nNothing else moves. Rails 1-4 stand unchanged, and the prohibition on relaxing the guard is permanent, not spent by this amendment.",
      "timestamp": "2026-08-22T20:32:39.849Z"
    },
    {
      "id": "comment-e22aed2511e14049a7cbb3eb60bebaa2",
      "author": "worker-40cfc767",
      "content": "READ BEFORE PLANNING — THE FIX THIS ROW DESCRIBES IS ALREADY COMMITTED, AND TWO DoD CLAUSES ARE UNGRADEABLE AS WRITTEN.\n\nPosting on the row rather than only in chat: my chat send at 20:53Z returned `routingTargets: []`, so nobody may have been woken. This is action-bearing state and belongs where get_context will surface it.\n\n1. THE PREMISE IS SPENT. The two names were appended at commit 0a6bf25, 20:29:00Z — three minutes BEFORE this row was filed at 20:32:57Z. I landed it off task-f432799c (now DONE, QA-approved 20:41) as a declared out-of-scope repair, announced in chan-75ef142c msg-59e1f630 before editing. DoD 1 is this row's own verify-or-block instrument and it discharges: the named title `serves the default provider and its registry bridge under plain Node` now PASSES. `pnpm --filter @moe/daemon test` EXIT 0, `Test Files 211 passed (211)` / `Tests 4634 passed | 1 todo (4635)`, confirmed by three independent runs including qa-8546c5ba's DETACHED CLEAN worktree at afe1a1f (`git status --porcelain` empty), which also satisfies DoD 1's clean-tree clause.\n\n2. DoD 3's SHAPE WAS HONORED EXACTLY. Keys APPENDED in sorted position — the observed side is `Object.keys(provider).sort()` at daemon-store-dependencies.test.ts:535, so sorted position is load-bearing, not cosmetic. `toEqual` set-equality PRESERVED. No subset relaxation: the assertion's own comment says a subset match would bless exactly the unreachable-port omission it exists to catch. `git show 0a6bf25 --stat` = one file, +2/-2.\n\n3. DEFECT IN DoD 3: IT REQUIRES PRESERVING A \"LENGTH ASSERTION\" THAT DOES NOT EXIST. `grep -n \"\\.length\" apps/daemon/src/daemon-store-dependencies.test.ts` returns exactly ONE hit, :136 — `expect([...deps.registry.keys()].length).toBeGreaterThan(0)` — a different subject in a different test, not adjacent to the roster. As written a worker preserves nothing and QA cannot grade it. Strike the clause or point it at the real guard, which is the `toEqual` set-equality itself.\n\n4. DEFECT IN DoD 2: \"THE PROVIDER THAT daemon-store-dependencies.ts ACTUALLY BUILDS\" NAMES TWO DIFFERENT OBJECTS, AND THE WRONG ONE REDS A GREEN TEST.\n     createStoreDependencies() frozen return, :294-305 -> 11 keys, INCLUDES `close`\n     export default provider,               :309-368 -> 10 keys, NO `close`\n   The subprocess binds `const provider = (await import(\"./src/daemon-store-dependencies.ts\")).default` at :505, so the roster is measured against the DEFAULT EXPORT — a separate, hand-maintained forwarding object, not the constructed one. Enumerate the constructed seam instead and DoD 2's \"if the true delta is larger\" branch fires with a delta of THREE, you add `close`, and you red a currently-green test while believing you followed the clause literally. Name the default export explicitly.\n\n5. WHERE THE ROW'S REAL INVARIANT LIVES, and it is production-side. The forwarding object's JSDoc at :332-338 states it: \"`createStoreDependencies` returning a `graph` factory is invisible to the shipped daemon: `daemon-main` loads THIS frozen object... Every optional factory this root builds must appear here.\" On THIS defect production was already complete — 1d9d84c put both names in the constructed object AND the default export and missed only the test roster. So there is no re-advertising to do, despite the title. Stating it because \"re-advertise\" reads as a production change and a plan built on that reading will not find one.\n\n6. WHAT IS GENUINELY LEFT — this row is not empty. I did NOT perform DoD 2's rigorous seam enumeration and I did NOT perform DoD 4's mutation drill (remove ONE name, confirm the same named test reds, restore byte-exact with an empty `git diff -- apps/daemon/src/daemon-store-dependencies.test.ts` verified in a clean tree). Both are real verification against an already-landed two-line diff. DoD 5's count line is quoted above and re-runnable.\n\nRECOMMENDED DISPOSITION, for a governor rather than for me: RE-SCOPE FROM FIX TO CERTIFY. Rewrite DoD 1 (premise spent), rewrite DoD 3 around what exists, disambiguate DoD 2 to the default export, keep DoD 4 and 5 as the substance. I have changed no status on this row and claimed nothing.",
      "timestamp": "2026-08-22T20:53:34.655Z"
    },
    {
      "id": "comment-3d1504d885ea44c4bd91e2aca66d8a74",
      "author": "governor-f2db8452",
      "content": "GOVERNOR RE-SCOPE: FIX -> CERTIFY. governor-f2db8452, 2026-08-23. THIS COMMENT SUPERSEDES comment-19a757b7 AND IS THE SOLE PLANNING AUTHORITY FOR THIS ROW. The definitionOfDone FIELD is stale in items 1, 2 and 3; plan against the five items restated below, not against the field and not against the row's title.\n\nI wrote this row's DoD and three of its clauses were defective. @worker-40cfc767's comment-e22aed25 found all three; I verified each myself at HEAD 17e048a rather than accepting them, and each reproduces.\n\nDEFECT A — THE PREMISE WAS SPENT BEFORE THE ROW EXISTED. Commit 0a6bf25 landed the two names at 20:29:00Z; I filed this row at 20:32:57Z. HEAD moved between my measurement and my filing. My own DoD-1 verify-or-block is the instrument that caught it, which is the gate working, but the row was three minutes stale at birth.\n\nDEFECT B — DoD-3 REQUIRED PRESERVING A LENGTH ASSERTION THAT DOES NOT EXIST.\n  grep -n \"\\.length\" apps/daemon/src/daemon-store-dependencies.test.ts  -> EXACTLY ONE hit, :136\n  :136 is `expect([...deps.registry.keys()].length).toBeGreaterThan(0)` — a different subject, a different test, nowhere near the roster.\n  A worker preserves nothing and QA cannot grade it. The real guard is the `toEqual` set-equality itself.\n\nDEFECT C — DoD-2 NAMED TWO DIFFERENT OBJECTS, AND THE WRONG READING REDDENS A GREEN TEST. This is the serious one.\n  createStoreDependencies() frozen return, daemon-store-dependencies.ts:294-305 -> INCLUDES `close`\n  export default provider (the hand-maintained forwarding object), :309-368     -> does NOT include `close`\n  The subprocess binds the DEFAULT EXPORT: daemon-store-dependencies.test.ts:505\n     const provider = (await import(\"./src/daemon-store-dependencies.ts\")).default;\n  My clause said \"enumerate the served set from the implementation seam — the provider that daemon-store-dependencies.ts actually builds.\" Followed literally, a planner enumerates the CONSTRUCTED object, finds a delta of three, adds `close` to the roster, and REDS A CURRENTLY-GREEN TEST while believing they obeyed the clause. My clause would have manufactured the defect it was written to prevent.\n\nALSO CORRECTED — THE TITLE MISLEADS. \"Re-advertise the two served daemon providers\" reads as a production change. There is none to make: 1d9d84c put both names in the constructed object AND in the default export, and missed only the test roster. Production was complete on this defect throughout. Do not go looking for a production edit.\n\nTHE RE-SCOPED DoD. Items 4 and 5 are unchanged and are the substance; 1-3 are replaced:\n1. VERIFY-OR-BLOCK, at the delivered tree: confirm the repair is present in committed bytes (`git show <sha>:apps/daemon/src/daemon-store-dependencies.test.ts` carries the ten-name roster) and that the named test `serves the default provider and its registry bridge under plain Node` PASSES. If it fails, or the roster differs, REPORT THE COLLISION and block — do not re-apply a fix that has landed.\n2. ENUMERATE THE **DEFAULT EXPORT** (the forwarding object at :309-368) and compare it to the advertised literal at :583-586. NOT `createStoreDependencies`'s return. `close` belongs to the constructed object and NOT to the default export; adding it is a rejection. State the two arities you measured and which object each came from.\n3. THE `toEqual` SET-EQUALITY AT :571-586 IS PRESERVED EXACTLY. There is no separate length assertion in this test and none may be invented to satisfy a clause. Relaxing the comparison to a subset match or `arrayContaining` remains a rejection even when green — the committed comment at :579-582 pre-refutes it.\n4. UNCHANGED — MUTATION DRILL, and it is the reason this row still exists: remove ONE name from the roster literal, confirm the SAME named test goes RED by name, restore byte-exact with an EMPTY `git diff -- apps/daemon/src/daemon-store-dependencies.test.ts` verified in a call SEPARATE from the restore. Tracked file, so `git diff` is a valid restore proof. A green drill is a finding, not a pass.\n5. UNCHANGED — focused suite EXIT 0 with the `Test Files N passed` count line quoted; repo-wide failing-path delta intersected with owned paths EMPTY; foreign reds disclosed verbatim; evidence in a row comment, never the completion summary.\n\nWHY THIS ROW IS NOT EMPTY: nobody has enumerated the seam rigorously and nobody has drilled the corrected guard. A test that passes is not a test that bites, and the whole reason this defect existed is that the roster and the served set drifted apart unobserved. Certifying that the guard now catches the drift is worth the row; re-fixing a landed fix is not.\n\nOUT OF SCOPE, recorded so it is not lost and not swept in: the JSDoc at :332-338 states a production invariant — \"Every optional factory this root builds must appear here\" — with the failure mode that a port missing from the forwarding object answers `GRAPH_QUERY_UNAVAILABLE` on a real authenticated request while every direct-injection test stays green. A bidirectional guard between the constructed object and the forwarding object would catch that whole class. It is a SEPARATE row and must not be added here.",
      "timestamp": "2026-08-22T21:13:32.299Z"
    },
    {
      "id": "comment-a3e151ff03fd42989b573c6faddcec36",
      "author": "qa-8546c5ba",
      "content": "QA MEASUREMENT ON THE DELIVERED STATE — read before planning, because two clauses of this row's DoD do not match the tree.\n\n**1. THE REPAIR ALREADY LANDED, and it is the one this row specifies.** `0a6bf25` — \"fix(daemon): add the two shipped ports to the exact providerKeys roster\", one file, `2 insertions / 2 deletions`:\n```\n-        \"affordances\", \"documentDossiers\", \"graph\", \"provide\", \"reconciliation\", \"restore\",\n-        \"sessionHandshake\", \"subscriptions\",\n+        \"affordances\", \"documentDossiers\", \"documentIngest\", \"graph\", \"planningRuns\", \"provide\",\n+        \"reconciliation\", \"restore\", \"sessionHandshake\", \"subscriptions\",\n```\n`toEqual` is untouched and the two keys are in sorted position. That satisfies the three substantive points of my grading basis (`comment-06ff26f3`): no subset relaxation, sorted insertion, and — checked, not assumed — the observed value is derived from the seam, `daemon-store-dependencies.test.ts:535` reads `providerKeys: Object.keys(provider).sort()` off the shipped object rather than a transcript. I re-ran the full daemon suite at HEAD `17e048a`: `Test Files 211 passed (211)`, `Tests 4634 passed | 1 todo (4635)`, EXIT 0. **The red this row was filed for is gone.**\n\n**2. THE DoD's \"LENGTH ASSERTION\" CLAUSE NAMES SOMETHING THAT DOES NOT EXIST.** Measured: `grep -n \"length\"` over the whole file returns exactly ONE hit — `:136`, `expect([...deps.registry.keys()].length).toBeGreaterThan(0)` — and it is inside `describe(\"the Foundation workspace catalog never gates daemon boot\")` at `:111`, an entirely different test from the roster arm. The roster assertion lives in `it(\"serves the default provider and its registry bridge under plain Node\")` at `:548`, and that test has **no cardinality companion at all**. So a clause requiring the length assertion be \"preserved\" is unsatisfiable-by-absence: nothing can be preserved, and a plan that reports it satisfied is reporting on a missing anchor. Either amend the clause out, or state it as \"ADD a cardinality assertion\" if that is what is wanted — but note the exact-set `toEqual` already subsumes cardinality, so adding one buys nothing.\n\n**3. WHAT HONEST SCOPE REMAINS.** Not reimplementation. The repair exists and is correct. What this row can still deliver is CERTIFICATION: a mutation drill proving the corrected guard still bites — remove one key from the shipped default object in `daemon-store-dependencies.ts` and confirm the `:548` arm reds by name, restored byte-exact. That is worth having, because the guard's whole value is catching a port that is constructed but not shipped, and nobody has yet drilled it in its repaired form. If the plan delivers that, I grade it on the drill, the restore proof, and the gate — and the ports-were-added part is already evidenced above.\n\nAlso worth resolving before planning, per @governor-7459dc6a: the DoD's constructed-vs-default provider wording is ambiguous. The arm reads the DEFAULT exported provider object under plain Node (`:548`), not a `createStoreDependencies(...)` construction — the distinction is the entire point of the guard, so the clause should say \"the shipped default object\" explicitly.",
      "timestamp": "2026-08-22T21:15:12.318Z"
    },
    {
      "id": "comment-5adb07ec54214b9f8bc9ad13376b5acd",
      "author": "architect-0975cd3a",
      "content": "PLAN SUBMITTED — architect-0975cd3a, 4 steps, 2 files, ZERO expected diff. Planned under **comment-3d1504d885ea44c4bd91e2aca66d8a74** as the sole authority; the `definitionOfDone` field is stale in items 1-3 and the title's \"re-advertise\" is not a production instruction. Kept short as instructed.\n\n=== ITEMS 1 AND 2, MEASURED AT COMMITTED HEAD 17e048a ===\n`git status --porcelain -- apps/daemon/src` EMPTY, so committed bytes, nobody's WIP.\n\n**Item 1 — the repair is present.** `daemon-store-dependencies.test.ts:583-586` carries the ten-name roster: `affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions`. Not a collision — this is the landed state the re-scope anticipated.\n\n**Item 2 — the two arities, each with the object it came from:**\n- **DEFAULT EXPORT** — the hand-maintained forwarding object at `daemon-store-dependencies.ts:316-365`, frozen and exported at `:367`: **TEN keys**, set-identical to the literal. This is what the subprocess binds (`test:505` imports `.default`) and `test:535` reports `Object.keys(provider).sort()`, which is why the literal is in sorted order.\n- **CONSTRUCTED OBJECT** — `createStoreDependencies`'s frozen return at `:294-306`: **ELEVEN keys** — the same ten plus `close`.\n\nDelta is exactly `close`, and it belongs to the constructed object only. Your defect-C analysis reproduces exactly.\n\n=== ONE THING I WANT TO SHARPEN: THE `close` TRAP IS LIVE, NOT HYPOTHETICAL ===\nYou wrote that following the old clause literally would make a planner add `close`. The reason it would feel *justified* is worth pinning, because it is what a worker will actually reason from: the JSDoc at `:332-338` says **\"Every optional factory this root builds must appear here.\"** A reader who enumerates the constructed object finds exactly one missing name and reads that sentence as licence to close the gap. The sentence is correct and the inference is wrong — `close` is a **lifecycle method, not an optional port factory**, so it is correctly absent.\n\nThat has a consequence for the out-of-scope row: **any bidirectional constructed-vs-forwarding guard must exempt `close`**, or it fails on day one against a correct tree. I have recorded that in the plan's step 4 so it reaches whoever files it, rather than being rediscovered as a false positive.\n\n=== ANCHOR CORRECTION ===\nThe pre-refuting comment is at **:578-582**, not `:579-582`; `:571` is where the enclosing `toEqual` begins. Trivial, but the clause tells a planner to grep line numbers, so I am pinning what I measured.\n\n=== TWO THINGS THE PLAN STATES EXPLICITLY BECAUSE THEY CAUSE REJECTION LOOPS ===\n1. **This row is expected to produce ZERO DIFF, and that is the correct outcome.** The repair landed in 0a6bf25 before the row was filed; the deliverable is certification that the guard bites. Step 4 tells the worker not to manufacture an edit in order to have something to commit, and tells QA that the absence of a commit bearing this task's id is not a rejection reason here.\n2. **The drill removes one of the two names the drift actually hid** (`documentIngest` / `planningRuns`), never a name that was never at risk, and asserts the failing test **by name** — a coarse mutation that reds a neighbour reads as a pass. A green drill is recorded as a finding, not a pass.\n\n=== GATE DISCRIMINATOR ===\nStep 3 pins that the evidence is the **passing count**, not the exit code: against the recorded baseline `Test Files 1 failed | 209 passed (210)` / `Tests 1 failed | 4604 passed | 2 todo (4607)`, the new run must show the failure gone with the passing count **not reduced** — a green run with a smaller count means the red was deselected rather than fixed. Foreign reds are expected on this tree (the original red was foreign in origin, commit 1d9d84c, control-room lane) and are disclosed verbatim and path-attributed.\n\n@governors — the bidirectional guard stays out of scope and is named in the plan's record step for you to file, with the `close` exemption noted so it does not land broken.",
      "timestamp": "2026-08-22T21:18:27.454Z"
    },
    {
      "id": "comment-58c34109adb5488f81649c43fc6ff473",
      "author": "worker-934d8b66",
      "content": "CERTIFICATION EVIDENCE - the artifact of this row. Zero diff, as the re-scope requires. Planned under comment-3d1504d8; the definitionOfDone FIELD is stale in items 1-3 and is not what I graded myself against.\n\n(a) VERIFY-OR-BLOCK: NO COLLISION, THE REPAIR IS IN COMMITTED BYTES.\nDelivered tree HEAD `17e048a`, `git status --porcelain -- apps/daemon/src` EMPTY.\n`daemon-store-dependencies.test.ts:583-586` carries the TEN-name roster: affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions.\n  cd apps/daemon && npx vitest run --root . --config package.json src/daemon-store-dependencies.test.ts\n  EXIT 0    Test Files  1 passed (1)    Tests  17 passed (17)\n`serves the default provider and its registry bridge under plain Node` occurs exactly once and PASSES. The red this row was filed for is gone - fixed by `0a6bf25` at 20:29Z, before the row was filed at 20:32:57Z.\n\n(b) BOTH ARITIES, ENUMERATED FROM SOURCE BY SCRIPT, each named with the object it came from.\n- DEFAULT EXPORT - `const provider` at lines 316-366, `export default provider` at :368: TEN keys - affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions.\n- CONSTRUCTED OBJECT - `createStoreDependencies`' frozen return at lines 294-306: ELEVEN keys, the same ten PLUS `close`.\n- constructed MINUS default = [\"close\"];  default MINUS constructed = [].\n- advertised literal == DEFAULT EXPORT -> TRUE;  advertised literal == CONSTRUCTED -> FALSE.\n`close` IS THE WHOLE DELTA AND WAS DELIBERATELY NOT ADDED. The default export is the object under test - `test:505` imports `.default`, `test:535` reports `Object.keys(provider).sort()` - so the ten-name sorted literal is correct for it. `close` is a lifecycle method, not an optional port factory. Adding it would RED a currently-green test. The trap is live rather than hypothetical: the JSDoc at `:332-338` says \"Every optional factory this root builds must appear here\", which reads as licence to close a one-key delta if you enumerate the CONSTRUCTED object instead. DoD-2 does not fire - there is no missing name, so there is no larger delta to repair.\n\n(c) THE DRILL - the only step that produced new knowledge.\nRemoved `documentIngest` (one of the two names the drift actually hid) from the roster literal, via an anchored replace that refuses unless the anchor matches exactly once.\n  EXIT 1    Test Files  1 failed (1)    Tests  1 failed | 16 passed (17)\n  FAIL  src/daemon-store-dependencies.test.ts > serves the default provider and its registry bridge under plain Node\nEXACTLY ONE arm red and it is THE NAMED ONE, asserted by title and not by count. 16 of 17 still pass, so no neighbour was reddened. Total arm count is 17 before and during, so nothing was deselected.\nTHE GUARD NAMES THE MISSING KEY: the failure diff reports `+ \"documentIngest\"`. It does not merely say the sets differ - it says which name went missing, which is the difference between catching drift and only announcing it.\nRESTORE, PROVEN IN A SEPARATE CALL. `git checkout -- apps/daemon/src/daemon-store-dependencies.test.ts` issued from the REPO ROOT (the pathspec resolves against the shell cwd; from a subdirectory it would silently restore nothing). Verified in a different command: `git diff -- <path>` produced NO OUTPUT and `git diff --quiet -- <path>` exited 0. The literal re-read from disk shows the ten names back in sorted order.\nNOTHING WAS RELAXED: the `toEqual` at `:571-586` is untouched - no `arrayContaining`, no subset match, no sorted-slice comparison - and no length assertion was added.\n\n(d) BEFORE/AFTER COUNTS - the passing count is NOT reduced.\n  before:  Test Files  1 failed | 209 passed (210)   Tests  1 failed | 4604 passed | 2 todo (4607)\n  after:   Test Files            211 passed (211)    Tests            4634 passed | 1 todo (4635)\n  `pnpm --filter @moe/daemon test` EXIT 0, no FAIL lines.\nFiles passing 209 -> 211, tests passing 4604 -> 4634, failures 1 -> 0. ONE COUNT MOVED DOWN AND IT IS NOT A DESELECTION: `todo` 2 -> 1, which is `task-f432799c` (DONE 20:41Z) turning the production-applied PARTIAL arm from an `it.todo` into a real executing test. Naming it so the single downward number is not misread.\n\n(e) PATH-ATTRIBUTED BASELINE AND FOREIGN REDS.\nOwned path: `apps/daemon/src/daemon-store-dependencies.test.ts`. This row has ZERO DIFF, so the delta intersected with owned paths is EMPTY BY CONSTRUCTION - there are no owned bytes for a regression to live in.\n  HEAD `17e048a` failing-path set: EMPTY.\n  Merge-base `d543f71` (`git merge-base HEAD main`): 10 failing files, measured earlier tonight in a detached worktree at that sha - project-configuration-selection, document-work-{identifiers,ingress,persistence,read,tail}, http/foundation-registry, index-surface, planning/active-graph-projection, planning/graph-body-record. None is an owned path.\n  Difference (failing at HEAD but not at merge-base): EMPTY; the set went 10 -> 0.\nNO FOREIGN RED TO DISCLOSE - the package leg is fully green.\nWHY THE IN-PLACE RUN IS ADMISSIBLE HERE: `git status --porcelain -- apps packages scripts tests` is EMPTY, no modified and no untracked source anywhere in the repo, so the in-place run reads exactly the committed bytes of `17e048a` and is equivalent to a clean detached-worktree run. That was NOT true on this tree two hours ago, which is why the previous row needed worktrees; the porcelain output is the proof that it is true now.\n\nZERO DIFF IS THE DELIVERABLE. Nothing was committed and nothing should be. QA: the absence of a commit bearing this task's id is what the re-scope requires, not a rejection reason.\n\nONE INSTRUMENT ERROR OF MY OWN, recorded because it produced a confident wrong answer before I caught it. My first enumeration script anchored on the literal line `  return Object.freeze({` and took the FIRST match. That string occurs TWICE - lines 77 and 294 - so it enumerated the config object at :77 and reported the constructed object as SIX keys `[credential, nodeSpecsDir, principalId, projectId, storePath, workspaceCatalogPath]` with a fabricated six-key delta. It read as a finding, not as a fault. Fixed by taking the last match and printing the anchor line numbers beside the result so the reader can see which occurrence answered. A non-unique anchor returns a wrong answer with the same shape as a right one.\n\nANCHOR CORRECTION for later greppers: the pre-refuting comment is at `:579-582`, not `:578-582` as the plan says (`:578` is the closing `},` of the previous property); the enclosing `toEqual` begins at `:571`. And DoD-3's \"accompanying length assertion\" does not exist - `grep -n \"\\.length\"` over the file returns exactly one hit, `:136`, a different subject in a different test.\n\nOUT OF SCOPE, NAMED FOR THE GOVERNOR TO FILE RATHER THAN SWEPT IN OR LOST.\nThe JSDoc at `:332-338` states a PRODUCTION invariant that nothing currently guards: every optional factory `createStoreDependencies` builds must also appear in the forwarding default export. Its failure mode is silent and expensive - a port constructed but not forwarded answers `GRAPH_QUERY_UNAVAILABLE` on a real authenticated request while every direct-injection test stays green, which is precisely the `graph` incident the comment was written about. Today's guard is one-directional: it pins the DEFAULT EXPORT against a hand-written literal, so it catches \"the advertisement drifted from the shipped object\" but NOT \"the root built a port and forgot to forward it\" - the constructed object is never compared to the forwarding object at all.\nSuggested row: a BIDIRECTIONAL guard asserting `keys(constructed) - {close} === keys(defaultExport)`, exempting `close` as a lifecycle method rather than a port factory. Worth noting it would have caught the `documentIngest`/`planningRuns` drift at `1d9d84c` in the SAME commit that introduced it, rather than ninety minutes later after it had blocked three rows.",
      "timestamp": "2026-08-22T21:22:47.915Z"
    },
    {
      "id": "comment-6677431960784c47883097954621954b",
      "author": "qa-8546c5ba",
      "content": "QA VERIFICATION RECORD — approved. Graded against `comment-3d1504d8` (the FIX -> CERTIFY re-scope, sole planning authority), not the stale field and not the row's title. All measurements mine, at HEAD `17e048a`, `git status --porcelain -uall -- apps/daemon/src` EMPTY, HEAD unchanged throughout.\n\nZERO DIFF IS THE CORRECT DELIVERABLE, AND I CHECKED IT IS HONEST RATHER THAN ASSUMED: `git log --all --grep=\"750812bd\"` returns NOTHING, and the last commits touching either file are `0a6bf25` (the repair, 20:29:00Z) and `1d9d84c` — both predating the row's 20:32:57Z filing. No production edit was made, which is what the re-scope requires; the out-of-scope bidirectional constructed-vs-forwarding guard was correctly left alone.\n\nDoD 1 — PASS. `git show 17e048a:apps/daemon/src/daemon-store-dependencies.test.ts` carries the ten-name roster in committed bytes, and `serves the default provider and its registry bridge under plain Node` PASSES (`Test Files 1 passed (1)`, `Tests 17 passed (17)`). No collision.\n\nDoD 2 — PASS, enumerated by me from the source rather than from any report. **Two arities, and which object each came from:**\n- `export default` forwarding object, `daemon-store-dependencies.ts:317-368` -> **10 keys**: affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions.\n- `createStoreDependencies()` frozen return, `:294-305` -> **11 keys**, the same ten plus `close`.\nThe advertised literal at `:583-586` is exactly the 10-key default-export set. Delta EMPTY, so the true delta really was the two names and no more. `close` is absent from the default export and correctly absent from the roster — the trap in the superseded DoD-2 wording was not walked into.\n\nDoD 3 — PASS. The `toEqual` set-equality at `:571-586` is intact; no subset match, no `arrayContaining`. No length assertion was invented: `grep -n \"\\.length\"` over the file still returns exactly one hit, `:136`, in `describe(\"the Foundation workspace catalog never gates daemon boot\")` at `:111` — a different subject in a different test, as the re-scope says.\n\nDoD 4 — PASS, DRILLED BY ME, and this is the row's whole substance. Removed ONE name (`planningRuns`) from the advertised literal -> `Tests 1 failed | 16 passed (17)`, RED by name on `serves the default provider and its registry bridge under plain Node`, and the failure diff names the missing key (`+ \"planningRuns\"`) rather than reporting a bare inequality. Restored, then verified in a CALL SEPARATE FROM THE RESTORE exactly as the clause demands: `git diff --quiet -- apps/daemon/src/daemon-store-dependencies.test.ts` EXIT 0 and porcelain empty. Post-restore focused run back to `17 passed (17)`. The corrected guard bites.\n\nDoD 5 — PASS. Focused suite EXIT 0, `Test Files 1 passed (1)` / `Tests 17 passed (17)`. Full package leg fresh at the same tree: `pnpm --filter @moe/daemon test` EXIT 0, `Test Files 211 passed (211)`, `Tests 4634 passed | 1 todo (4635)`, no FAIL lines — so the repo-wide failing-path set is EMPTY and its intersection with the owned paths is empty a fortiori. No foreign red left to disclose. Against this row's recorded pre-repair baseline (`1 failed | 209 passed (210)` / `1 failed | 4604 passed | 2 todo`), passing counts rose 209 -> 211 and 4604 -> 4634, so the red was fixed rather than deselected; the single downward number, todo 2 -> 1, is `task-f432799c` opening the PARTIAL arm, which I approved separately.\n\nWorth stating plainly for the record, since a zero-diff row invites the opposite reading: this row earned its keep. Before it, the roster guard had never once been observed to fail — and the defect it exists to catch is precisely the roster and the served set drifting apart unobserved. A guard nobody has seen bite is a guard nobody has tested.",
      "timestamp": "2026-08-22T21:27:25.009Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-22T19:55:59.780Z",
  "updatedAt": "2026-08-22T21:27:36.962Z",
  "metrics": {
    "firstClaimAt": "2026-08-22T19:56:20.996Z",
    "plannedStepCount": 4,
    "plannedDistinctFileCount": 2,
    "executedStepCount": 4,
    "doneAt": "2026-08-22T21:27:36.962Z",
    "wallClockMs": 5475966
  },
  "contextFetchedBy": [
    "architect-a69eb088",
    "architect-c4355c55",
    "governor-7459dc6a",
    "worker-934d8b66",
    "qa-8546c5ba"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-22T21:18:04.743Z",
  "planningNotes": {
    "approachesConsidered": "(a) Planning against the definitionOfDone FIELD - rejected: the governor's re-scope comment-3d1504d8 is the sole planning authority and the field is stale in items 1-3. Following field item 2 literally (enumerate \"the provider that daemon-store-dependencies.ts actually builds\") makes a planner enumerate the CONSTRUCTED object, find a delta of one, add `close`, and RED A GREEN TEST while believing they obeyed the clause. (b) Preserving the \"length assertion\" the stale item 3 names - impossible and forbidden: `grep -n \"\\.length\"` over the test file returns exactly ONE hit at :136, a different subject in a different test. None may be invented to satisfy the clause. (c) Making a production edit, as the row TITLE implies - there is none: commit 1d9d84c put both names in the constructed object AND the default export and missed only the test roster; 0a6bf25 then landed the roster. Production was complete on this defect throughout. (d) Adding the bidirectional constructed-vs-forwarding guard the JSDoc at :332-338 argues for - explicitly OUT OF SCOPE per the authority comment; named in the record for the governor to file as its own row. (e) Manufacturing a diff so the row has something to commit - rejected and called out in step 4: the deliverable is certification, and zero diff is the correct outcome.",
    "codebaseInsights": "MEASURED AT COMMITTED HEAD 17e048a with `git status --porcelain -- apps/daemon/src` EMPTY, so these are committed bytes and not a peer's WIP.\n\nTHE REPAIR HAS LANDED. daemon-store-dependencies.test.ts:583-586 carries the ten-name roster: affordances, documentDossiers, documentIngest, graph, planningRuns, provide, reconciliation, restore, sessionHandshake, subscriptions.\n\nTHE TWO ARITIES, which item 2 requires be stated with their objects:\n- DEFAULT EXPORT, the hand-maintained forwarding object at daemon-store-dependencies.ts:316-365, frozen and exported at :367 - TEN keys, exactly matching the literal. The subprocess binds THIS: test:505 imports `.default`, and test:535 reports `Object.keys(provider).sort()`, which is why the literal is sorted.\n- CONSTRUCTED OBJECT, `createStoreDependencies`'s frozen return at :294-306 - ELEVEN keys: the same ten PLUS `close`.\nDelta is exactly `close`, and it belongs to the constructed object only.\n\nWHY THE `close` TRAP IS LIVE RATHER THAN HYPOTHETICAL: the JSDoc at :332-338 reads \"Every optional factory this root builds must appear here.\" A reader who enumerates the constructed object finds one missing name and reads that sentence as licence to add it. `close` is a LIFECYCLE METHOD, not an optional port factory - correctly absent, and adding it reds a currently-green test. Any future bidirectional guard must exempt it.\n\nANCHOR CORRECTION: the pre-refuting comment is at :578-582, not :579-582 as the authority comment states; :571 begins the enclosing `toEqual`. Small, but a planner greps line numbers.\n\nBASELINE recorded on the row: `Test Files 1 failed | 209 passed (210)`, `Tests 1 failed | 4604 passed | 2 todo (4607)`.",
    "risks": "(1) THE `close` TRAP is the single most likely way this row goes wrong, and it goes wrong while the worker believes they obeyed the DoD - the stale field's clause actively points at it. Step 1 names it as a rejection. (2) THIS ROW SHOULD PRODUCE ZERO DIFF. A worker who feels a task must produce a commit may manufacture an edit; QA seeing no commit may reject. Step 4 states both directions explicitly. (3) The drill must remove one of the two names the drift actually hid (documentIngest / planningRuns) and must assert the failing test BY NAME - a coarse mutation that reds a neighbour reads as a pass. (4) A green drill is a finding: the guard has detached and must be strengthened, not recorded as passing. (5) The gate's discriminator is the passing COUNT, not the exit code - a green run with a reduced count means the red was deselected. (6) Foreign reds on this tree are expected (the original red was foreign in origin, commit 1d9d84c, control-room lane) and must be disclosed verbatim and path-attributed, never hidden. (7) Shared worktree: no git add -A, no reset, no stash; the drill's restore is proven by an empty `git diff` in a SEPARATE call.",
    "keyFiles": [
      "apps/daemon/src/daemon-store-dependencies.ts",
      "apps/daemon/src/daemon-store-dependencies.test.ts"
    ]
  },
  "budget": {
    "wallClockMs": 3600000,
    "warnedAt": "2026-08-22T21:18:09.916Z",
    "escalatedAt": "2026-08-22T21:18:09.916Z"
  },
  "planApprovedAt": "2026-08-22T21:18:06.844Z",
  "workStartedAt": "2026-08-22T21:18:29.333Z",
  "reviewStartedAt": "2026-08-22T21:24:22.621Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  211 passed (211)\n      Tests  4634 passed | 1 todo (4635)\n\nFresh run at HEAD 17e048a, no FAIL lines. `git status --porcelain -- apps packages scripts tests` is EMPTY, so the run reads exactly the committed bytes and is equivalent to a clean detached-worktree run.\n\nCOUNT COMPARISON, which is the clause that matters here: pre-repair baseline on this row was `Test Files 1 failed | 209 passed (210)` / `Tests 1 failed | 4604 passed | 2 todo (4607)`. Files passing 209 -> 211, tests passing 4604 -> 4634, failures 1 -> 0. The passing count is NOT reduced, so the red was fixed rather than deselected. The one downward number, todo 2 -> 1, is task-f432799c opening the PARTIAL arm into a real executing test.\n\nDRILL: removing `documentIngest` from the roster literal reds EXACTLY `serves the default provider and its registry bridge under plain Node` (1 failed | 16 passed of 17), and the failure diff names the missing key. Restored byte-exact, proven by an empty `git diff` and `--quiet` exit 0 in a call SEPARATE from the restore.\n\nZERO DIFF is the deliverable per the re-scope: the repair landed in 0a6bf25 before the row was filed, so there is no commit bearing this task id and none should exist. Full evidence: comment-58c34109.",
    "reportedAt": "2026-08-22T21:24:22.621Z"
  },
  "filesModified": [
    "apps/daemon/src/daemon-store-dependencies.ts",
    "apps/daemon/src/daemon-store-dependencies.test.ts"
  ],
  "completedAt": "2026-08-22T21:27:36.962Z",
  "reviewCompletedAt": "2026-08-22T21:27:36.962Z",
  "reviewSummary": "APPROVED. Full record: comment-66774319. Graded against comment-3d1504d8 (FIX -> CERTIFY re-scope), not the stale field or the title. All measurements mine at HEAD 17e048a, tree clean, HEAD unchanged.\n\nZERO DIFF is the correct deliverable and I checked it is honest: git log --all --grep=\"750812bd\" returns nothing, and the last commits on both files (0a6bf25, 1d9d84c) predate the row's filing. No production edit; the out-of-scope bidirectional guard was left alone.\n\nDoD 1 PASS: git show 17e048a:<test> carries the ten-name roster in committed bytes and the named test passes (17 passed (17)). No collision.\nDoD 2 PASS, enumerated by me from source, both arities stated: the export default forwarding object at :317-368 has 10 keys; createStoreDependencies()'s return at :294-305 has 11, the same ten plus close. The literal at :583-586 is exactly the 10-key default-export set - delta EMPTY, and close is correctly absent, so the trap in the superseded DoD-2 wording was not walked into.\nDoD 3 PASS: toEqual set-equality at :571-586 intact, no subset/arrayContaining, and no length assertion invented - the only \".length\" in the file is still :136, a different subject in a different test.\nDoD 4 PASS, DRILLED BY ME: removed planningRuns from the literal -> Tests 1 failed | 16 passed (17), RED by name on \"serves the default provider and its registry bridge under plain Node\", failure diff naming + \"planningRuns\". Restored, then verified in a SEPARATE call as the clause demands: git diff --quiet EXIT 0, porcelain empty; post-restore run back to 17 passed.\nDoD 5 PASS: focused EXIT 0 with the count line; full leg fresh at the same tree EXIT 0, Test Files 211 passed (211), Tests 4634 passed | 1 todo (4635), no FAIL lines, so the failing-path delta is empty a fortiori. Versus the pre-repair baseline, passing rose 209->211 and 4604->4634 - fixed, not deselected.\n\nThe row earned its keep: this guard had never once been observed to fail before now."
}