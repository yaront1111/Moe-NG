{
  "id": "task-80745330b6a148ab9933766682a169a5",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[e4cd1eb6-pre-a] Read-only discover-current project-configuration reader on the selection authority (no expectedSettingsDigest input)",
  "description": "PREREQUISITE filed per governor-0af24e35's 16:06Z governance call on task-e4cd1eb6807244efa3334609ed746143 (R3-4): \"SPLIT two prerequisite rows — (1) a discover-current project-configuration reader (new authority API, bound after store/project selection)\". This is row (1). Row (2) is the provider launch-allowlist row filed beside it. e4cd1eb6 stays BLOCKED until both are DONE; it then sequences behind them with refuse-until-fact-exists semantics.\n\nTHE GAP, MEASURED AT HEAD 595c32c2 (worker-57c90511's comment-72e983b1 on e4cd1eb6, re-verified by me): apps/daemon/src/configuration/project-configuration-selection.ts exports exactly two functions — `readCurrentProjectConfiguration(store, input)` (:190) which REQUIRES an exact `{projectId, expectedSettingsDigest}` record (HEX64) and refuses PROJECT_CONFIGURATION_STALE when the tail's settingsDigest differs (:183), and `selectProjectConfiguration` (:204) which returns the digest only by COMMITTING a decision. `stableTail` (:102) is module-private; `git grep ProjectConfigurationSelected` outside the module is empty; provider-profile-resolver.ts:150 has the same digest-in shape. Nothing in production can DISCOVER the current settingsDigest without already holding it, so the daemon env composer (moe-up-env.ts overlay) cannot source MOE_PROJECT_CONFIGURATION_DIGEST from durable state — e4cd1eb6's DoD-2 is unreachable.\n\nDELIVER: one new exported READ-ONLY reader on the same authority (working name `readLatestProjectConfiguration(store, {projectId})`) that returns the SAME CurrentProjectConfiguration shape (manifest incl. settingsDigest, manifestBytes, selectionVersion, outcome CURRENT, authority DAEMON_VERIFIED, evidence DURABLE) or a refusal from the EXISTING code roster/layer, applying the SAME decision-coverage fences (validateDecision + validReceipt + schema/projectId checks) as readCurrentProjectConfiguration — only the digest equality check is absent because there is no input to compare. It never writes. readCurrentProjectConfiguration keeps its contract byte-for-byte.\n\nCONSUMER (global rail clause 1): task-e4cd1eb6 — its step 3 binds this reader after store/project selection to compose the digest key. No production caller is added here; the completion comment records the consumer id. Line cap: the module is 249 lines — if the reader pushes it past 250, extract into a sibling module with its hand-written `.js` bridge rather than exceeding.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, recorded in a row comment with `git rev-parse HEAD` quoted, every grep unpiped with its own exit code plus a positive control: `git grep -n '^export function' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` lists ONLY readCurrentProjectConfiguration and selectProjectConfiguration (if a third reader exists, adopt/verify it and report this row as duplicate); `git grep -n 'expectedSettingsDigest' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` EXIT 0 (the digest-in contract still stands); porcelain CLEAN on project-configuration-selection.ts/.js/.test.ts and apps/daemon/src/index.ts. Any premise failing: report blocked naming it.",
    "A new exported reader with an EXACT input roster `{projectId}` (any extra key, a missing key, or a non-logical-ref projectId refuses PROJECT_CONFIGURATION_UNREADABLE @ PROJECT_CONFIGURATION_SELECTION) returns, for a store holding a selected configuration, the same CurrentProjectConfiguration shape as readCurrentProjectConfiguration and BYTE-EQUAL manifestBytes; `result.manifest.settingsDigest` fed back into readCurrentProjectConfiguration as expectedSettingsDigest yields CURRENT (parity arm). An empty aggregate refuses PROJECT_CONFIGURATION_ABSENT. PROJECT_CONFIGURATION_STALE is unreachable from the new reader and an arm states so.",
    "READ-ONLY, proven not asserted: after any call (found, absent, refused) `store.getAggregateVersion(<aggregate>)` is unchanged and no new command decision exists for the project (compare the decision/command ledger count before and after through the store's own read API); the arm titles name the property. Decision coverage preserved: the existing test technique that plants a tail event WITHOUT its decision receipt (or with a foreign projectId) makes the NEW reader refuse with the SAME code and layer readCurrentProjectConfiguration returns for that fixture — assert both readers side by side.",
    "Exported from the apps/daemon barrel (apps/daemon/src/index.ts beside readCurrentProjectConfiguration) and typed from the same result union; a compiled probe importing it from the package barrel typechecks (probe deleted in the same command). CONSUMER named per global rail clause 1: the completion comment names task-e4cd1eb6807244efa3334609ed746143 as the composer that binds this reader; no other production caller is added by this row.",
    "Per-file cap: project-configuration-selection.ts stays <= 250 lines, or the reader lives in a sibling module (same directory) with a hand-written `.js` bridge (`export * from \"./<name>.ts\";`) that the daemon runtime-entrypoint sweep accepts; NEVER above 400. readCurrentProjectConfiguration and selectProjectConfiguration are contract-identical before and after (`git diff` over their bodies shows only extraction moves, if any; their existing arms pass with identical counts).",
    "Mutation drills, one degree of freedom each, restore proven by `git diff --exit-code` for tracked paths that were clean at claim and SHA256 vs a %TEMP% backup for any new/dirty file, hashed in a separate call: (D1) remove the validateDecision/validReceipt application from the new reader -> the no-receipt arm reds; (D2) make the new reader call selectProjectConfiguration -> the read-only arm reds on version/decision count; (D3) delete the barrel export -> the barrel probe fails typecheck. A green drill is a finding, never a pass.",
    "Gates standalone, foreground, unpiped, exit 0 with nonzero counts quoted: `pnpm --filter @moe/daemon typecheck` (quote the `$ tsc` echo; sub-second green needs a planted-error control); from apps/daemon `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts` (and the sibling's test if split) with `Test Files N passed`; `pnpm --filter @moe/daemon test` graded by path attribution with foreign red disclosed verbatim. Explicit-pathspec commit of owned paths only; completion comment names task-e4cd1eb6 and quotes the exported symbol and its input roster."
  ],
  "taskRails": [
    "READ-ONLY authority: the new reader never calls store.commitExpectedVersionDecision or selectProjectConfiguration; discovery of the current digest must not mint a decision.",
    "Exact-record input `{projectId}` only; no default digest, no env fallback, no caller-supplied digest hint. Refusals come from the EXISTING PROJECT_CONFIGURATION_SELECTION_CODES roster and layer — add no new code (the roster is frozen and asserted).",
    "readCurrentProjectConfiguration keeps its digest-validating contract untouched (PROJECT_CONFIGURATION_STALE stays reachable from it); provider-profile-resolver.ts is not edited.",
    "Compose the module's existing decision-coverage helpers (validateDecision, validReceipt, stableTail, decode) — never reimplement the tail read; if extraction is needed for the line cap, move helpers, do not copy them.",
    "Preserve foreign WIP: re-run porcelain on every touched path immediately before editing; explicit-pathspec commits only, never `git add -A`.",
    "Parent linkage: this row is a prerequisite of task-e4cd1eb6807244efa3334609ed746143 (consumer). Relaxing e4cd1eb6's DoD-2 instead of landing this reader is forbidden — a hand-typed digest is exactly the fabricated-authority class PR #3's reviewers flagged."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK (DoD-1), read-only, ONE moe.add_comment with `git rev-parse HEAD` quoted; every grep unpiped with its own exit code plus a positive control. (a) `git grep -n '^export function' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` prints EXACTLY two lines (readCurrentProjectConfiguration, selectProjectConfiguration). A third exported reader means someone landed it: read it, verify against DoD 2-3, report this row as duplicate via moe.report_blocked naming the commit. (b) `git grep -n 'expectedSettingsDigest' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` EXIT 0 and the line `if (decoded.manifest.settingsDigest !== expectedSettingsDigest)` is present (the digest-in contract still stands). (c) Positive control: `git grep -c export HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` prints a count, EXIT 0. (d) `git status --porcelain -- apps/daemon/src/configuration/project-configuration-selection.ts apps/daemon/src/configuration/project-configuration-selection.js apps/daemon/src/configuration/project-configuration-selection.test.ts apps/daemon/src/index.ts` prints NOTHING (all four clean; the .js bridge already exists and is not edited). Any dirt: STOP, report blocked naming the path — index.ts is a shared barrel other rows edit. (e) `wc -l apps/daemon/src/configuration/project-configuration-selection.ts` = 249; record it beside the DoD-5 amendment in comment (the reader lives in this module; <= 270 accepted, hard ceiling 400). (f) Baseline focused run from apps/daemon: `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts` — quote `Test Files 1 passed` and the `Tests N passed` count; that N is the baseline for step 4. If any premise fails, report blocked naming it; never plan around it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts"
      ],
      "note": "Read-only gate passed at HEAD dba0cf307077e7742d22181bf9d18eb2e8669b6b: exactly two exported functions, digest-in equality intact, four paths clean, module 249 lines. Baseline executed 1 file / 15 tests with the required daemon-local --root . --config package.json launcher; the literal plan launcher ran zero tests and was disclosed/corrected. Durable evidence: comment-b38fe3ab7eb34245b7dd17a24c594257.",
      "startedAt": "2026-08-29T17:17:20.732Z",
      "completedAt": "2026-08-29T17:18:31.835Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — extend apps/daemon/src/configuration/project-configuration-selection.test.ts (reuse manifestBytes, selectionRequest, captureRecords, portFor, currentRequest, expectUnknown, the real `store`). Add `readLatestProjectConfiguration` to the existing import from ./project-configuration-selection.js — the whole file is red at import until step 3; that is the intended red. New describe \"project configuration latest-read (discover-current)\" with arms, each title naming the ONE mechanism that can red it: (A) PARITY: select once (captureRecords); `const latest = readLatestProjectConfiguration(store, { projectId: PROJECT_ID })`; expect `latest` toEqual the exact frozen record `{ authority:'DAEMON_VERIFIED', evidence:'DURABLE', manifest: <records manifest>, manifestBytes, ok:true, outcome:'CURRENT', selectionVersion: records.version }`; `latest.manifestBytes` byte-equal to `records.event.payload`; and `readCurrentProjectConfiguration(store, { projectId: PROJECT_ID, expectedSettingsDigest: latest.manifest.settingsDigest })` toEqual `latest` (the discovered digest is the digest the validating reader accepts). (B) EXACT ROSTER, divergence from the old reader: `readLatestProjectConfiguration(store, { projectId: PROJECT_ID, expectedSettingsDigest: hex('f') })` -> expectUnknown PROJECT_CONFIGURATION_UNREADABLE (extra key — proves it is not an alias of the two-key reader); `{}` -> UNREADABLE; `{ projectId: '' }` and `{ projectId: 42 }` -> UNREADABLE; a symbol-keyed and an accessor-keyed object -> UNREADABLE (mirror :271's technique). (C) ABSENT: the :424 `empty` port (getAggregateVersion 0, everything else throws) -> expectUnknown PROJECT_CONFIGURATION_ABSENT — same port, same code as the old reader. (D) STALE UNREACHABLE, side by side: select v1 (`manifestBytes('model-1')`) then v2 (`manifestBytes('model-2')`, expectedVersion 1, distinct commandId); `readCurrentProjectConfiguration(store, { projectId, expectedSettingsDigest: <v1 digest> })` -> STALE; `readLatestProjectConfiguration(store, { projectId })` -> CURRENT with `manifest.settingsDigest` === v2's digest and selectionVersion 2. (E) READ-ONLY: (E1) real store: `const before = store.getAggregateVersion(<aggregate id>)` where the id is `project-configuration:` + sha256(PROJECT_ID) hex (recompute in the test with createHash, as aggregateId does) — call the reader on FOUND, then on an unknown projectId (ABSENT), then with a bad input (UNREADABLE); assert getAggregateVersion unchanged after each and `store.readAggregateEvents(id, 0, 10).items.length` unchanged; (E2) port: `portFor(records)` with `commitExpectedVersionDecision` replaced by a counter that throws if called; call the reader; expect count 0 and the result CURRENT. (F) FORGED-RECORD PARITY: inside the :465 loop (or a sibling loop over the same case table), for every forged case call BOTH readers and assert `readLatestProjectConfiguration(port, { projectId: PROJECT_ID })` toEqual the old reader's result (same code, layer, upstream) — this is the arm that proves the null-digest branch dropped no fence. Run from apps/daemon: `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts`; quote the red (import/TS failure) in a step note.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.test.ts"
      ],
      "note": "STEP 2 DONE — six arms A-F authored, ALL SIX RED, and the red is absence of the subject rather than a broken fixture.\n\nTHE RED: `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/configuration/project-configuration-selection.test.ts` => EXIT 1, `Tests 6 failed | 15 passed (21)`. Every one of the six fails with `TypeError: readLatestProjectConfiguration is not a function` — the 15 pre-existing arms are untouched and still pass, so the new arms are the entire delta and nothing was disturbed to make room for them.\nNote the plan predicted a whole-file import failure; the actual red is per-arm at call time, because the module is ESM-with-named-exports and vitest resolves the missing binding to undefined rather than throwing at import. Same evidence, narrower blast radius — recorded because it differs from the step text.\n\nTHE ARMS, each titled by the ONE mechanism that can red it:\n- A PARITY. Whole-value `toEqual` against the exact frozen record, byte-equality of `manifestBytes` against `records.event.payload` via spread, and then the clause that is the point of the whole row: `readCurrentProjectConfiguration(store, { expectedSettingsDigest: latest.manifest.settingsDigest, projectId })` must equal `latest`. THE DIGEST THIS READER DISCOVERS IS THE DIGEST THE VALIDATING READER ACCEPTS — without that, a caller who holds no digest still has no way in, which is the entire gap task-e4cd1eb6 blocked on.\n- B EXACT ROSTER. The load-bearing case is the EXTRA key: passing the old reader's own `expectedSettingsDigest` must REFUSE, which is what proves this is not the two-key reader wearing a new name. Plus `{}`, empty-string and numeric projectId, and the symbol-keyed and accessor-keyed objects that `exactRecord`'s own technique exists to reject.\n- C ABSENT. The same `unreachable`-port shape the existing suite uses at :424, so a stable empty head yields the SAME code as the validating reader.\n- D STALE UNREACHABLE, side by side on ONE store. Select v1, select v2 at expectedVersion 1 with a distinct commandId; the old reader given v1's digest refuses STALE while the new reader answers CURRENT at v2's digest and selectionVersion 2. The two assertions in one arm are what make \"stale is unreachable\" a comparison rather than a claim.\n- E READ-ONLY, measured twice because one measurement is not enough. E1 pins `getAggregateVersion` and the event count across ALL THREE outcomes — found, absent, and refused — since a write on a refusing path is exactly where a careless implementation would hide one. E2 counts the WRITE SEAM itself with a counting `commitExpectedVersionDecision`, because a commit against a DIFFERENT aggregate would leave every E1 assertion green.\n- F FORGED-RECORD PARITY. Eight cases from the existing forged table, chosen so each names a DIFFERENT fence (tail shape, decision cover, receipt cover, codec). Asserts WHOLE-VALUE equality between the two readers, so code, layer and upstream must all agree — an assertion on the code alone would let a re-stamped layer through. `expect(cases).toHaveLength(8)` is the denominator, since a sweep producing zero cases would pass while testing nothing.\n\nExpected codes are test-side literals throughout; nothing is imported from the module under test to be compared against itself.",
      "startedAt": "2026-08-29T17:18:44.391Z",
      "completedAt": "2026-08-29T17:35:42.393Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN — apps/daemon/src/configuration/project-configuration-selection.ts, minimal: (1) `readCurrent(store, projectId, expectedSettingsDigest: string | null)`; the ONLY logic change is line :183 becoming `if (expectedSettingsDigest !== null && decoded.manifest.settingsDigest !== expectedSettingsDigest) return refuse(\"PROJECT_CONFIGURATION_STALE\");` with a one-line comment: 'null = discover-current: every fence above still applies; only the caller-held digest comparison has no operand'. readCurrentProjectConfiguration keeps passing the validated string — its contract is byte-identical. (2) Add, directly after readCurrentProjectConfiguration: `/** Discover-current READ: the same decision-covered fold with no caller-held digest; never writes. Exact roster {projectId}. */ export function readLatestProjectConfiguration(store: ProjectConfigurationStore, input: unknown): ReadCurrentProjectConfigurationResult { try { const request = exactRecord(input, [\"projectId\"]); if (request === null || !isLogicalRef(request.projectId)) return refuse(\"PROJECT_CONFIGURATION_UNREADABLE\"); return readCurrent(store, request.projectId, null); } catch (error) { return storeRefusal(error); } }` — same try/storeRefusal shape as the sibling reader so DurableStoreError provenance is identical. Do NOT call readCurrentProjectConfiguration from it (double tail read; R2). (3) apps/daemon/src/index.ts:193-198: add `readLatestProjectConfiguration,` to the existing export list from ./configuration/project-configuration-selection.js (porcelain the file immediately before editing). (4) `wc -l` the module: expect ~262; <= 270 per the DoD-5 amendment, never code-golf existing lines. Re-run the focused suite from apps/daemon: all arms A-F green; quote `Test Files 1 passed` and `Tests N passed` where N = step-1 baseline + the new arm count. `pnpm --filter @moe/daemon typecheck` exit 0 with the `$ tsc` echo quoted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts",
        "apps/daemon/src/index.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts",
        "apps/daemon/src/index.ts",
        "apps/daemon/src/index-surface.test.ts"
      ],
      "note": "STEP 3 DONE — GREEN. `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/configuration/project-configuration-selection.test.ts src/index-surface.test.ts` => EXIT 0, `Test Files 2 passed (2)`, `Tests 229 passed (229)`. All six arms A-F flipped from the step-2 red; the 15 pre-existing selection arms and the whole barrel roster are untouched and still pass.\n\nTHE CHANGE, minimal as specified: `readCurrent`'s third parameter widens to `string | null`, and the digest line becomes `expectedSettingsDigest !== null && decoded.manifest.settingsDigest !== expectedSettingsDigest`, carrying the one-line comment the plan dictated. `readCurrentProjectConfiguration` still passes its validated string, so its contract is byte-identical — arm F proves that by whole-value equality against it across eight forged cases. The new `readLatestProjectConfiguration` takes the exact roster `[\"projectId\"]` through the same `exactRecord`/`isLogicalRef` gate and the same `try`/`storeRefusal` shape, so DurableStoreError provenance is identical, and it calls `readCurrent` DIRECTLY — routing through the sibling reader would read the tail twice and could answer on two different heads (R2). That reasoning is in the code, not only here.\n\nONE CORRECTION TO THE STEP TEXT, and it is worth recording because following it literally reds the gate. Step 3(4) says to insert the roster entry \"directly after `[\\\"readFoundationAttemptRecord\\\", \\\"function\\\"]`, since the list is sorted\". IT IS NOT SORTED THERE: `readImportShadowProjection` sits between them, and \"readI\" < \"readL\". Inserting where the step said produced `Test Files 1 failed | 1 passed`, failing the set-equality arm at :500 (`Object.keys(daemon).sort()` vs `EXPECTED_EXPORTS.map(...)`). Moved one line down to its true sorted slot, after `readImportShadowProjection`, and the arm passes. The count bump 143 -> 144 at :495 was correct as written. Both index-surface hunks are exactly the two the amendment authorised; nothing else in that file moved.\n\nSIZE: project-configuration-selection.ts is 269 lines — under the DoD-5 amendment's 270 and well under 400. It briefly measured 272; I trimmed MY OWN new docblock from five lines to three rather than code-golfing any existing line, which the step forbids. No sibling module or `.js` bridge was needed.\n\nTYPECHECK IS RED AND NOT MINE, disclosed verbatim rather than waved past. `pnpm --filter @moe/daemon typecheck` => EXIT 1:\n  run 1: `src/cutover/cutover-generation-snapshot.ts(4,10): error TS2305: Module '\"@moe/store\"' has no exported member 'decodeBoundedJsonBytes'.` + `src/planning/graph-supersede-service.test.ts(29,10): error TS6133` + two `graph-supersede-test-fixtures.ts` TS2339 at :93 and :107.\n  run 2, ~90 seconds later: the cutover error is GONE and the fixtures errors MOVED to :85, :97, :111 and grew from two to three.\nTHE ERROR SET IS CHANGING UNDER ME because two peers are editing this package right now — `ls -la` puts `cutover-generation-snapshot.ts` at 20:37 and `graph-supersede-test-fixtures.ts` at 20:37, alongside my own 20:36 edits. Every offending path is foreign: `apps/daemon/src/cutover/` is untracked (`??`, task-b2548479) and both graph-supersede paths are ` M` (architect-fdbab575, task-2ce6c3fe step 3 IN_PROGRESS).\nOWNED-PATH DELTA IS EMPTY, and this is a positive proof rather than an absence-of-mention: tsc reports ALL diagnostics in a project run, it does not stop at the first, so a file producing no diagnostic is a file that typechecked. Across BOTH runs, with the foreign set shifting between them, not one diagnostic ever named project-configuration-selection.ts, its test, index.ts or index-surface.test.ts. The DoD-7 exit-0 clause cannot be satisfied while a peer's in-flight file is broken; I am not editing foreign paths to green a gate. Step 5 re-runs it and will record whatever it says then.",
      "startedAt": "2026-08-29T17:35:45.123Z",
      "completedAt": "2026-08-29T17:38:14.234Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "GREEN — apps/daemon/src/configuration/project-configuration-selection.ts, minimal: (1) `readCurrent(store, projectId, expectedSettingsDigest: string | null)`; the ONLY logic change is the digest line becoming `if (expectedSettingsDigest !== null && decoded.manifest.settingsDigest !== expectedSettingsDigest) return refuse(\"PROJECT_CONFIGURATION_STALE\");` with a one-line comment: 'null = discover-current: every fence above still applies; only the caller-held digest comparison has no operand'. readCurrentProjectConfiguration keeps passing the validated string — its contract is byte-identical. (2) Add, directly after readCurrentProjectConfiguration: `/** Discover-current READ: the same decision-covered fold with no caller-held digest; never writes. Exact roster {projectId}. */ export function readLatestProjectConfiguration(store: ProjectConfigurationStore, input: unknown): ReadCurrentProjectConfigurationResult { try { const request = exactRecord(input, [\"projectId\"]); if (request === null || !isLogicalRef(request.projectId)) return refuse(\"PROJECT_CONFIGURATION_UNREADABLE\"); return readCurrent(store, request.projectId, null); } catch (error) { return storeRefusal(error); } }` — same try/storeRefusal shape as the sibling reader. Do NOT call readCurrentProjectConfiguration from it (double tail read; R2). (3) apps/daemon/src/index.ts:193-198: add `readLatestProjectConfiguration,` to the existing export list from ./configuration/project-configuration-selection.js (porcelain the file immediately before editing). (4) OWNED ROSTER WIDENED BY ARCHITECT (comment-420fbf63 finding): apps/daemon/src/index-surface.test.ts is an EXACT consumer of the barrel shape — edit it in the SAME commit: insert `[\"readLatestProjectConfiguration\", \"function\"],` into EXPECTED_EXPORTS in sorted position (directly after `[\"readFoundationAttemptRecord\", \"function\"],`, since the list is sorted and the set-equality arm at ~:499 compares against `Object.keys(daemon).sort()`), and bump the hand-written count at ~:495 from `toBe(143)` to `toBe(144)`. Nothing else in that file changes; quote the two-hunk `git diff --stat`/diff in the step note. (5) `wc -l` the selection module: expect ~262; <= 270 per the DoD-5 amendment, never code-golf existing lines. Re-run from apps/daemon: `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts src/index-surface.test.ts` — all arms A-F green AND the barrel roster arms green; quote `Test Files 2 passed` and the Tests count. `pnpm --filter @moe/daemon typecheck` exit 0 with the `$ tsc` echo quoted.",
          "reason": "Worker-5097adb2 found an unnamed exact-shape consumer of the barrel (comment-420fbf63): apps/daemon/src/index-surface.test.ts pins EXPECTED_EXPORTS (:402 region), its count 143 (:495) and Object.keys(daemon) set-equality (:499). Architect widens the owned roster by that one test file; the shape-grep was my miss.",
          "amendedBy": "architect-c1a63707",
          "amendedAt": "2026-08-29T17:30:38.291Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-4",
      "description": "BARREL PROBE, DRILLS, GATES (DoD 4-7). BARREL PROBE (DoD-4): create apps/daemon/src/zz-barrel-probe.ts containing `import { readLatestProjectConfiguration } from \"./index.js\"; export const probe: typeof readLatestProjectConfiguration = readLatestProjectConfiguration;` run `pnpm --filter @moe/daemon typecheck` (exit 0), then DELETE the probe in the same command sequence and confirm `git status --porcelain -- apps/daemon/src/zz-barrel-probe.ts` prints nothing. Record the probe source verbatim in the step note. DRILLS (DoD-6), one degree of freedom each; all three edited files are UNCOMMITTED, so `git diff --exit-code` proves NOTHING — back each up to `%TEMP%\\80745330-<workerId>\\` and prove every restore by SHA256 equality in a call SEPARATE from the restore: (D1) in readCurrent, make the null branch skip validateDecision (e.g. `if (expectedSettingsDigest !== null) { ...validateDecision... }`) -> arm F reds (the forged decision cases now diverge between the readers) while the old reader's own arms stay green; (D2) make readLatestProjectConfiguration call `selectProjectConfiguration(store, ...)` before returning (any well-formed request) -> arm E2 reds on the commit counter AND E1 reds on the version; (D3) remove `readLatestProjectConfiguration,` from index.ts -> the barrel probe fails typecheck (quote the TS2305 line) while the module's own tests stay green. A drill that stays GREEN is a FINDING: stop and report it; never bank it. GATES, standalone, foreground, unpiped, each exit code from its own run: (G1) `pnpm --filter @moe/daemon typecheck` — quote the `$ tsc` echo; if sub-second, plant a type error in the new function, observe red, remove, re-run green. (G2) from apps/daemon: `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts src/provider-profile/provider-profile-resolver.test.ts src/runtime-entrypoint.test.ts` — quote `Test Files 3 passed` and the Tests count (provider-profile-resolver is the other production reader; runtime-entrypoint proves the bridge sweep is untouched). (G3) `pnpm --filter @moe/daemon test` graded by PATH ATTRIBUTION: any red whose file imports none of the three owned paths (transitively via the barrel counts — name it) is foreign, disclosed verbatim with its exact failing line; an owned-path red = not done. Record G1-G3 count lines and D1-D3 outcomes in ONE row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts",
        "apps/daemon/src/index.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts",
        "apps/daemon/src/configuration/project-configuration-selection.test.ts",
        "apps/daemon/src/index.ts",
        "apps/daemon/src/index-surface.test.ts"
      ],
      "note": "STEP 4 DONE. Full record: comment-8220ad6b35dd4e79bc530c9efae023fa.\n\nBARREL PROBE (DoD-4): the two-line probe importing `readLatestProjectConfiguration` from \"./index.js\" produced ZERO diagnostics naming it, and was deleted in the same command with porcelain confirming it gone. D3 is its negative control — with the barrel export removed, the same probe and grep return `TS2724: '\"./index.js\"' has no exported member named 'readLatestProjectConfiguration'` — so the zero is a measurement rather than a grep that cannot match.\n\nDRILLS 4/4 RED, none green, each on a named arm, every restore proven by SHA256 in a call separate from the restore (all four owned paths are uncommitted, so git diff proves nothing here).\nD1 (skip the decision cover on the null branch) reds arm F ALONE — `forged parity failed: trace project` — while the old reader's own forged arms stay green. That asymmetry is the point: it proves the new entry point holds the decision cover itself rather than inheriting a fence nobody re-checked.\nD2 (make the read write) reds arm E as required, and arm A collaterally — disclosed, not trimmed: the injected write moves the aggregate version so the parity record stops matching.\nD3 (remove the barrel export) reds the SET-EQUALITY arm and the per-name arm while the COUNT arm stays green.\nD4 (revert only the count bump) reds the COUNT arm alone while set-equality stays green.\nD3 AND D4 TOGETHER ARE THE FINDING: each reds a different roster arm and leaves the other green, proving the two grade different things — the published SET versus the hand-maintained COUNT. Either drill alone would have left the other arm ungraded and possibly vacuous.\n\nGATES. G2 focused four suites EXIT 0, `Test Files 4 passed (4)`, `Tests 270 passed (270)`. G1 typecheck EXIT 1 and FOREIGN — the error set CHANGED across four runs this evening (store TS2305, three graph-supersede TS2339 at shifting lines, then two cutover TS6133) because two peers are editing this package live, and NOT ONE diagnostic in ANY run named an owned path. That is positive proof: tsc reports every diagnostic rather than stopping at the first, so a file producing none typechecked. G3 full package EXIT 1, `Tests 21 failed | 6343 passed | 12 todo (6376)`, all six failing files attributed — five have zero reference to any owned path and none imports the daemon barrel; the sixth (launch-template-producer.test.ts:50, a real importer of `selectProjectConfiguration`) was ALREADY failing with the same count in the run I did earlier today on task-e7b802bc at HEAD 235a12db, before my first byte here. Owned-path failure delta EMPTY.\n\nMETHOD WARNING recorded for the next agent: `pnpm ... typecheck 2>&1 | head -8` reported `PIPESTATUS[0]=0` while printing seven errors, because `head` closing the pipe early corrupts the producer's exit status. The unpiped re-run reported the true `Exit status 1`. A TRUNCATING pipe is as dangerous as a tail-exit pipe, and this board's rails only warn about the latter.",
      "startedAt": "2026-08-29T17:38:17.338Z",
      "completedAt": "2026-08-29T17:45:19.461Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "BARREL PROBE, DRILLS, GATES (DoD 4-7). BARREL PROBE (DoD-4): create apps/daemon/src/zz-barrel-probe.ts containing `import { readLatestProjectConfiguration } from \"./index.js\"; export const probe: typeof readLatestProjectConfiguration = readLatestProjectConfiguration;` run `pnpm --filter @moe/daemon typecheck` (exit 0), then DELETE the probe in the same command sequence and confirm `git status --porcelain -- apps/daemon/src/zz-barrel-probe.ts` prints nothing. Record the probe source verbatim in the step note. DRILLS (DoD-6), one degree of freedom each; all FOUR edited files are UNCOMMITTED, so `git diff --exit-code` proves NOTHING — back each up to `%TEMP%\\80745330-<workerId>\\` and prove every restore by SHA256 equality in a call SEPARATE from the restore: (D1) in readCurrent, make the null branch skip validateDecision -> arm F reds (forged decision cases diverge between the readers) while the old reader's own arms stay green; (D2) make readLatestProjectConfiguration call `selectProjectConfiguration(store, ...)` before returning -> arm E2 reds on the commit counter AND E1 reds on the version; (D3) remove `readLatestProjectConfiguration,` from index.ts -> the barrel probe fails typecheck (quote the TS2305 line) AND index-surface.test.ts's set-equality arm reds (quote the diff line naming the missing key) while the count arm at 144 stays green — that asymmetry proves the two roster arms grade different things; (D4) revert only the count bump (144 -> 143) -> the count arm reds alone. A drill that stays GREEN is a FINDING: stop and report it; never bank it. GATES, standalone, foreground, unpiped, each exit code from its own run: (G1) `pnpm --filter @moe/daemon typecheck` — quote the `$ tsc` echo; if sub-second, plant a type error in the new function, observe red, remove, re-run green. (G2) from apps/daemon: `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts src/index-surface.test.ts src/provider-profile/provider-profile-resolver.test.ts src/runtime-entrypoint.test.ts` — quote `Test Files 4 passed` and the Tests count. (G3) `pnpm --filter @moe/daemon test` graded by PATH ATTRIBUTION over the FOUR owned paths (selection.ts, selection.test.ts, index.ts, index-surface.test.ts; transitive via the barrel counts — name it); any other red is foreign, disclosed verbatim with its exact failing line; an owned-path red = not done. Record G1-G3 count lines and D1-D4 outcomes in ONE row comment. Step 5's commit pathspec becomes FOUR paths (add apps/daemon/src/index-surface.test.ts) and `git show --stat HEAD` must list exactly those four.",
          "reason": "Roster widened by apps/daemon/src/index-surface.test.ts (comment-420fbf63); the drills and gates must include the barrel-roster suite and D3 must red BOTH the probe and the roster arm.",
          "amendedBy": "architect-c1a63707",
          "amendedAt": "2026-08-29T17:30:50.925Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-5",
      "description": "ADVERSARIAL SELF-REVIEW, COMMIT, DURABLE COMPLETION RECORD. Self-review, answered in the row comment: (i) concurrent selection during a latest-read — stableTail's 3-attempt page/version consistency check still bounds it; the reader returns CONFLICT rather than a torn record (arm :442's technique applies unchanged; say whether you added a latest-reader twin of that arm — add it if cheap). (ii) a projectId whose aggregate holds a decision for ANOTHER project — validateDecision's key.projectId check refuses CONFLICT; covered by arm F. (iii) huge/hostile input — exactRecord + isLogicalRef bound it; arm B. (iv) the reader is exported from the barrel but NOT yet consumed — that is by design (global rail clause 1 (a)): the consumer is task-e4cd1eb6807244efa3334609ed746143 step 3; state it. (v) no secret, digest or manifest byte is logged. Line counts: `wc -l` on project-configuration-selection.ts (<= 270 per amendment) and index.ts. COMMIT BY EXPLICIT PATHSPEC ONLY: `git add -- apps/daemon/src/configuration/project-configuration-selection.ts apps/daemon/src/configuration/project-configuration-selection.test.ts apps/daemon/src/index.ts` then `git commit -m \"feat(task-80745330): read the latest durable project configuration without a caller-held digest\"`; `git show --stat HEAD` lists exactly those three paths. COMPLETION COMMENT (moe.add_comment — the completion summary is write-only): commit sha; HEAD at claim; G1/G2/G3 count lines verbatim; D1-D3 outcomes; per-file line counts; the exported symbol and its input roster quoted (`readLatestProjectConfiguration(store, { projectId })`); the consumer named: task-e4cd1eb6807244efa3334609ed746143 (its step 3 binds this reader after store/project selection to compose MOE_PROJECT_CONFIGURATION_DIGEST); and the delivered claim stated exactly: 'the current durable project configuration, including its settingsDigest, is now DISCOVERABLE through the selection authority with every decision-coverage fence intact and no write; readCurrentProjectConfiguration is unchanged.' Then moe.complete_task with a one-line summary pointing at that comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts",
        "apps/daemon/src/configuration/project-configuration-selection.test.ts",
        "apps/daemon/src/index.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/configuration/project-configuration-selection.ts",
        "apps/daemon/src/configuration/project-configuration-selection.test.ts",
        "apps/daemon/src/index.ts",
        "apps/daemon/src/index-surface.test.ts"
      ],
      "note": "STEP 5 DONE. Full record: comment-7a4600c8fa7349de8c278b5f2f44dcd6; drills and gates in comment-8220ad6b35dd4e79bc530c9efae023fa.\n\nCOMMIT 5b3d7313, explicit four-path pathspec (the amendment's four, not the base step's three), `4 files changed, 228 insertions(+), 4 deletions(-)`, `git show --stat HEAD` listing exactly those four. Index clean afterwards. No foreign path staged; the untracked apps/daemon/src/cutover/ and the dirty planning/graph-supersede-* belonging to two live peers were not touched. No push.\n\nFRESH VERIFICATION AT THE COMMIT: `Test Files 4 passed (4)`, `Tests 271 passed (271)`, EXIT 0.\n\nTHE SELF-REVIEW QUESTION I ANSWERED WITH AN ARM RATHER THAN PROSE, since the step invited it: arm G pins that a head moving across all three `stableTail` attempts refuses PROJECT_CONFIGURATION_CONFLICT for the latest reader too, so dropping the digest operand did not soften the tail's consistency bound; the same arm pins that a throwing store keeps `STORE_CLOSED @ DURABLE_STORE` unrestamped, which proves the catch/storeRefusal shape is genuinely shared rather than re-implemented. That is the 271st test. The other four review questions are answered by measurement in the completion comment — the manifest-byte one by construction, since the refusal type has no message or detail field at all and the module contains zero `console.`.\n\nWHAT THIS UNBLOCKS, and why the row was CRITICAL: task-e4cd1eb6 blocked TWICE with zero bytes authored on exactly this gap. worker-57c90511 measured it precisely — the existing reader takes the digest as a HEX64 INPUT it validates against and never yields it, `stableTail` is module-private, and `selectProjectConfiguration` returns it only through a WRITE. There was no read path to a digest a caller could hold. There is one now: `readLatestProjectConfiguration(store, { projectId })`, exported from the barrel, with every decision-coverage fence proven intact by whole-value parity against the validating reader and by D1 redding when that cover is skipped.\n\nPLAN CORRECTION worth carrying: step 3(4)'s stated insert position for the roster entry is wrong — the list is not sorted at that point (`readImportShadowProjection` sits between `readFoundationAttemptRecord` and `readLatestProjectConfiguration`), and following it literally reds the set-equality arm. The count bump was correct.",
      "startedAt": "2026-08-29T17:45:22.588Z",
      "completedAt": "2026-08-29T17:47:02.015Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Block resolved by architect: the unnamed exact-shape consumer (apps/daemon/src/index-surface.test.ts) is now an owned path via step-3/step-4 amend-1 and the roster comment. Resume at step 2; no bytes were authored.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 162,
  "comments": [
    {
      "id": "comment-3d4edcd8ce944634a975018034759343",
      "author": "architect-c1a63707",
      "content": "PLAN-TIME MEASUREMENT + ONE DoD AMENDMENT BY THE ROW'S AUTHOR (architect-c1a63707, HEAD 595c32c2, 2026-08-29).\n\nMEASURED: `git grep -n '^export function' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` -> :190 readCurrentProjectConfiguration, :204 selectProjectConfiguration (exactly two; gap open). Module 249 lines; `.js` bridge present (`export * from \"./project-configuration-selection.ts\";`); porcelain clean on the module, its bridge, its test, and apps/daemon/src/index.ts. readCurrent (:162-189) applies: stableTail -> ABSENT/CONFLICT -> event type/schema/aggregate -> validateDecision -> validReceipt -> decode -> projectId -> digest equality (:183) -> CURRENT. Only :183 has no operand without a caller-held digest; every other fence is reusable verbatim.\n\nDoD-5 AMENDMENT (the DoD field has no editing tool; this comment is the amendment, binding on worker and QA): the reader lives INSIDE project-configuration-selection.ts and the module may reach <= 270 physical lines (measured landing ~262). Reason: the sibling-module shape would require exporting three module-private helpers (readCurrent, exactRecord, refuse) and would split one authority across two files to save ~12 lines — the delivery-bias rail prefers the smaller working slice, and the global cap's 250 is a target with the hard ceiling at 400. No existing line may be code-golfed to hit 250. The hard ceiling and every other DoD-5 clause (contract-identical readCurrentProjectConfiguration and selectProjectConfiguration, identical existing arm counts) stand.\n\nDESIGN PINNED IN THE PLAN: `readCurrent(store, projectId, expectedSettingsDigest: string | null)` — null skips only the digest equality; `readLatestProjectConfiguration(store, input)` = exactRecord(input, [\"projectId\"]) + isLogicalRef + readCurrent(..., null), same try/storeRefusal provenance; NEVER implemented by calling readCurrentProjectConfiguration with a discovered digest (double tail read races a concurrent selection). Barrel export added at index.ts:193-198. Forged-record parity arm (step 2 F) grades that the null branch dropped no fence; read-only proven by a port commit counter plus real-store version.\n\nCONSUMER: task-e4cd1eb6807244efa3334609ed746143 step 3 (after re-plan) — recorded per global rail clause 1(a). Sibling prerequisite: task-fd1960561d9b4ba2bbb96ae031ac4606 (provider boundary roster).",
      "timestamp": "2026-08-29T16:58:38.419Z"
    },
    {
      "id": "comment-b38fe3ab7eb34245b7dd17a24c594257",
      "author": "worker-5097adb2",
      "content": "STEP 1 VERIFY-OR-BLOCK — PASS, read-only. Fresh anchor: HEAD `dba0cf307077e7742d22181bf9d18eb2e8669b6b` (`git rev-parse HEAD`, EXIT 0).\n\n(a) `git grep -n '^export function' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` EXIT 0 and printed EXACTLY two exports:\n- :190 readCurrentProjectConfiguration\n- :204 selectProjectConfiguration\nNo third/discover-current reader exists; this row is not a duplicate.\n\n(b) `git grep -n -F expectedSettingsDigest HEAD -- <same file>` EXIT 0; hits :163, :183, :194, :196-198, :238. Separate exact-equality probe printed :183 `if (decoded.manifest.settingsDigest !== expectedSettingsDigest) {`, EXIT 0. The validating reader contract still stands.\n\n(c) Positive control `git grep -c export HEAD -- <same file>` printed 12, EXIT 0.\n\n(d) Exact-path `git status --porcelain -- project-configuration-selection.ts project-configuration-selection.js project-configuration-selection.test.ts apps/daemon/src/index.ts` printed NOTHING, EXIT 0. All planned paths were clean before edits.\n\n(e) physical line count is 249. Author amendment comment-3d4edcd8 authorizes this one authority module through <=270 (hard ceiling 400); no code-golf/sibling split is needed.\n\nBASELINE EXECUTION AND PLAN-LAUNCHER CORRECTION. The literal plan command from apps/daemon, `pnpm exec vitest run src/configuration/project-configuration-selection.test.ts`, EXIT 1 with `No test files found`; output showed the root include excludes apps/**. It executed zero tests and cannot be banked under the non-vacuous gate rail. The measured working app-local pattern is:\n`pnpm exec vitest run --root . --config package.json src/configuration/project-configuration-selection.test.ts`\nEXIT 0; `Test Files 1 passed (1)`; `Tests 15 passed (15)`. This is the baseline and the command shape I will use for every focused daemon leg in later steps; omitting --root/--config would fabricate a zero-test gate. No production/test bytes authored in step 1.",
      "timestamp": "2026-08-29T17:18:24.182Z"
    },
    {
      "id": "comment-420fbf63b86a4680a85779a248793174",
      "author": "worker-5097adb2",
      "content": "STEP 2 SCOPE BLOCK — newly measured exact-shape consumer omitted from the approved ownership roster. Anchor HEAD `2a27695dea532bda8ade48976712e8aad6806fae`; exact porcelain is empty for selection.ts, selection.test.ts, index.ts, and index-surface.test.ts; no task byte has been authored in step 2.\n\n`apps/daemon/src/index-surface.test.ts` is a direct exact BARREL-SHAPE consumer of the planned `apps/daemon/src/index.ts` export:\n- :402 pins `[\"readCurrentProjectConfiguration\", \"function\"]` inside `EXPECTED_EXPORTS`.\n- :495 pins `EXPECTED_EXPORTS.length` to 143.\n- :499 asserts `Object.keys(daemon).sort()` equals that roster.\nPublishing `readLatestProjectConfiguration` from index.ts without changing this test makes the owned daemon surface gate red, attributable to this task's index.ts change. This is not foreign baseline red and cannot be omitted.\n\nGlobal exact-shape rail requires every literal roster/count consumer be named in the owned-path roster at plan time and says a worker discovering an unnamed consumer mid-step must STOP and report, not silently take the path. Required architect action: amend the plan/affectedFiles/DoD to add `apps/daemon/src/index-surface.test.ts`, with the sorted roster entry immediately after readCurrentProjectConfiguration and count 143 -> 144, plus the existing daemon test gate. The selection test patch and production design remain otherwise valid. No edits were made after discovery.",
      "timestamp": "2026-08-29T17:23:43.150Z"
    },
    {
      "id": "comment-35c3960645c94c4a825fefd5035309aa",
      "author": "architect-c1a63707",
      "content": "ARCHITECT RULING ON comment-420fbf63 (worker-5097adb2's STOP): CORRECT STOP, MY MISS. apps/daemon/src/index-surface.test.ts is an exact-shape consumer of the barrel (EXPECTED_EXPORTS roster, `toBe(143)` count, `Object.keys(daemon).sort()` set-equality) and the plan's shape-grep did not name it. OWNED ROSTER WIDENED BY ARCHITECT AUTHORITY to FOUR paths: project-configuration-selection.ts, project-configuration-selection.test.ts, index.ts, index-surface.test.ts. Step 3 amend-1 carries the exact edit (insert `[\"readLatestProjectConfiguration\", \"function\"],` after `[\"readFoundationAttemptRecord\", \"function\"],`; count 143 -> 144); step 4 amend-1 adds the roster suite to G2/G3, drill D3 now reds both the probe and the set-equality arm, D4 reds the count arm alone; step 5's commit pathspec is the four paths. Row returns to WORKING; resume at step 2 (unchanged) — no bytes were authored, so nothing to adopt. QA: grade the roster diff as exactly two hunks in index-surface.test.ts.",
      "timestamp": "2026-08-29T17:30:53.862Z"
    },
    {
      "id": "comment-8220ad6b35dd4e79bc530c9efae023fa",
      "author": "worker-8d70fe57",
      "content": "STEP 4 EVIDENCE (worker-8d70fe57) — barrel probe, four drills, three gates. All four owned paths are UNCOMMITTED, so `git diff` proves nothing about a restore; every restore below is proven by SHA256 against %TEMP%\\80745330-worker-8d70fe57\\, hashed in a call SEPARATE from the restore.\n\n=== BARREL PROBE (DoD-4) ===\nSource, verbatim, at apps/daemon/src/zz-barrel-probe.ts:\n  import { readLatestProjectConfiguration } from \"./index.js\";\n  export const probe: typeof readLatestProjectConfiguration = readLatestProjectConfiguration;\n`pnpm --filter @moe/daemon typecheck` then `grep -c 'zz-barrel-probe'` on its output => 0 diagnostics naming the probe. Deleted in the same command; `git status --porcelain -- apps/daemon/src/zz-barrel-probe.ts` prints nothing.\nA ZERO-COUNT GREP PROVES NOTHING WITHOUT A CONTROL, and D3 below is that control: with the barrel export removed, the SAME probe and the SAME grep return `src/zz-barrel-probe.ts(1,10): error TS2724: '\"./index.js\"' has no exported member named 'readLatestProjectConfiguration'. Did you mean 'selectProjectConfiguration'?`. So the 0 is a measurement, not a grep that cannot match.\n\n=== DRILLS (DoD-6) — 4 of 4 RED, each on a NAMED arm ===\nD1 SKIP THE DECISION COVER on the null branch (`expectedSettingsDigest === null ? { version: getAggregateVersion(id) } : validateDecision(...)`): EXIT 1, `Tests 1 failed | 228 passed (229)`, SOLE red `F: FORGED-RECORD PARITY`, failing on `forged parity failed: trace project`. The old reader's own forged arms stayed GREEN — that asymmetry is the whole point: it proves the new entry point holds the decision cover ITSELF rather than inheriting a fence nobody re-checked. Restore SHA256 4abf8398…24fe both sides, 64 hex, equal.\nD2 MAKE THE READ WRITE (call `selectProjectConfiguration` before returning the found record): EXIT 1, `Tests 2 failed | 19 passed (21)`, reds `E: READ-ONLY` as required AND `A: PARITY` collaterally — disclosed rather than trimmed: the injected write moves the aggregate version, so the parity record no longer matches the captured one. E is the arm the drill names and it redded. Restore SHA256 identical.\nD3 REMOVE THE BARREL EXPORT: two independent reds. Typecheck — the probe's TS2724 quoted above. Vitest — `Tests 2 failed | 206 passed (208)`, reds `publishes exactly the reviewed runtime namespace` (set equality) and `publishes readLatestProjectConfiguration as function`, while the hand-written COUNT arm stayed GREEN. Restore SHA256 3aae0c5e…3c32 both sides, equal; probe removed.\nD4 REVERT ONLY THE COUNT BUMP (144 -> 143): EXIT 1, `Tests 1 failed | 207 passed (208)`, SOLE red `guards the hand-written runtime export catalogue`, while the set-equality arm stayed GREEN. Restore SHA256 c6456925…4d51 both sides, equal.\nD3 AND D4 TOGETHER ARE THE FINDING WORTH KEEPING: each redded a DIFFERENT roster arm and left the other green, which proves the two arms grade different things — one the published SET, one the hand-maintained COUNT. A single drill would have left the other ungraded and either arm could have been vacuous.\nFinal scoped `git status --porcelain` over the four owned paths shows ` M` on exactly those four and nothing else. No mutant bytes survive.\n\n=== GATES ===\nG2 (focused, four suites) `cd apps/daemon && node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/configuration/project-configuration-selection.test.ts src/index-surface.test.ts src/provider-profile/provider-profile-resolver.test.ts src/runtime-entrypoint.test.ts` => EXIT 0, `Test Files 4 passed (4)`, `Tests 270 passed (270)`.\nG1 (typecheck) `pnpm --filter @moe/daemon typecheck` => EXIT 1, FOREIGN. Latest run: two errors, both `src/cutover/cutover-generation-snapshot.test.ts` TS6133 — an UNTRACKED foreign directory (`??`, task-b2548479). Across four runs this evening the error set kept CHANGING under me (the `@moe/store` TS2305, three `graph-supersede-test-fixtures.ts` TS2339 at shifting line numbers, two `graph-supersede-service.test.ts` TS6133), because two peers are editing this package live — `ls -la` puts their files at 20:35-20:41 beside my 20:36 edits. NOT ONE DIAGNOSTIC IN ANY RUN NAMED AN OWNED PATH. That is a positive proof, not an absence of mention: tsc reports every diagnostic in a project run rather than stopping at the first, so a file producing none is a file that typechecked. DoD-7's exit-0 clause cannot be met while a peer's in-flight file is broken, and I am not editing foreign paths to green a gate.\nMETHOD WARNING FOR THE NEXT AGENT, because it nearly fooled me: `pnpm ... typecheck 2>&1 | head -8` reported `PIPESTATUS[0]=0` while printing seven errors — `head` closing the pipe early corrupts the producer's exit status. The UNPIPED re-run reported the true `Exit status 1`. A truncating pipe is as dangerous as a tail-exit pipe.\nG3 (full daemon package) `pnpm --filter @moe/daemon test` => EXIT 1, `Test Files 8 failed | 299 passed (307)`, `Tests 21 failed | 6343 passed | 12 todo (6376)`.\nATTRIBUTION BY IMPORT CLOSURE over the four owned paths. Six failing files carry the 21: activation-telemetry-launch (10), launch-template-producer (3), activation-run-commit (2), http-listener (2), daemon-command-registry (1), foundation-attempt-windows (1). FIVE of the six contain ZERO textual reference to any owned path, and NONE of them imports the daemon barrel (`grep -l 'from \"./index.js\"' ...` returns nothing), so the barrel widening cannot reach them either.\nTHE SIXTH HAS A REAL IMPORT EDGE and gets the stronger proof: `launch-template-producer.test.ts:50` imports `selectProjectConfiguration` from the module I edited. Its three failures are `expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be 'CLAUDE_LAUNCH_MODEL_MISMATCH'` and siblings — the launcher's own gate vocabulary, nothing to do with project configuration. DECISIVE: I ran this same suite EARLIER TODAY on task-e7b802bc, at HEAD 235a12db, BEFORE my first byte on this row, and `launch-template-producer.test.ts (3)` was already in that failing list with the same count. The red pre-dates my edit. Corroborating: `selectProjectConfiguration` is byte-unchanged in my diff, and the only behavioural change — the null branch — is unreachable from it, since the sibling reader always passes a validated string.\nOWNED-PATH FAILURE DELTA: EMPTY. Every suite covering an owned path is green (G2, 270/270).",
      "timestamp": "2026-08-29T17:45:01.853Z"
    },
    {
      "id": "comment-7a4600c8fa7349de8c278b5f2f44dcd6",
      "author": "worker-8d70fe57",
      "content": "COMPLETION RECORD (worker-8d70fe57). COMMIT 5b3d7313, explicit four-path pathspec, `4 files changed, 228 insertions(+), 4 deletions(-)`; `git show --stat HEAD` lists exactly project-configuration-selection.ts, its test, index.ts and index-surface.test.ts, nothing else. HEAD at claim was 5834b676; drill evidence is comment-8220ad6b35dd4e79bc530c9efae023fa.\n\nTHE DELIVERED CLAIM, stated exactly as the plan asks: the current durable project configuration, including its settingsDigest, is now DISCOVERABLE through the selection authority with every decision-coverage fence intact and no write; readCurrentProjectConfiguration is unchanged.\n\nTHE SURFACE: `readLatestProjectConfiguration(store, { projectId })` — exact input roster of ONE key, returning the same `ReadCurrentProjectConfigurationResult` union as its sibling. Exported from the daemon barrel beside `readCurrentProjectConfiguration`.\nCONSUMER, per global rail clause 1(a): task-e4cd1eb6807244efa3334609ed746143. Its step 3 binds this reader after store/project selection to compose MOE_PROJECT_CONFIGURATION_DIGEST. That row blocked twice with ZERO bytes authored on exactly this gap — worker-57c90511 measured it precisely: `readCurrentProjectConfiguration` takes `expectedSettingsDigest` as a HEX64 INPUT it validates against, never yields it; `stableTail` is module-private; `selectProjectConfiguration` returns the digest only via a WRITE. The reader is deliberately UNCONSUMED at this commit, which rail clause 1(a) permits when the consumer is named.\n\nGATES, each from its own unpiped run. G2 focused four suites EXIT 0, `Test Files 4 passed (4)`, `Tests 270 passed (270)` — re-run FRESH at the commit with arm G added: `Tests 271 passed (271)`. G1 `pnpm --filter @moe/daemon typecheck` EXIT 1, FOREIGN and disclosed in the step-4 record: the error set changed across four runs because two peers are editing this package live, and not one diagnostic in any run named an owned path. G3 full package EXIT 1, `Tests 21 failed | 6343 passed | 12 todo (6376)`, six failing files, all attributed; the only one with a real import edge to my module was already failing with the same count in a run I did earlier today at HEAD 235a12db, before my first byte here. Owned-path failure delta EMPTY.\n\nSIZE: project-configuration-selection.ts 269 lines (DoD-5 amendment cap 270, hard cap 400); index.ts 256. No sibling module or `.js` bridge was needed. It briefly measured 272 and I trimmed MY OWN new docblock rather than code-golfing an existing line.\n\nADVERSARIAL SELF-REVIEW, each answered with a measurement:\n(i) CONCURRENT SELECTION DURING A LATEST-READ — answered with an ARM rather than prose, since the plan invited one if cheap. New arm G: `portFor(records, { versions: [1,2,2,3,3,4] })` against the latest reader returns PROJECT_CONFIGURATION_CONFLICT, so a head that keeps moving across all three `stableTail` attempts refuses rather than returning a record stitched from two heads. The same arm also pins that a throwing store keeps its provenance unrestamped — `STORE_CLOSED @ DURABLE_STORE` — proving the `catch`/`storeRefusal` shape is genuinely shared and not re-implemented. That is the 271st test.\n(ii) AN AGGREGATE HOLDING ANOTHER PROJECT'S DECISION — `validateDecision`'s `key.projectId` check refuses CONFLICT, and arm F covers it by whole-value parity: the `trace project` and `decision project` forged cases are in its table, and D1 proved that table is load-bearing by redding exactly `forged parity failed: trace project` when the cover was skipped.\n(iii) HUGE OR HOSTILE INPUT — `exactRecord` + `isLogicalRef` bound it, and arm B exercises the shapes that defeat a naive key check: an extra key, a missing key, empty and numeric projectId, a SYMBOL-keyed object and an ACCESSOR-keyed object. The extra-key case is the load-bearing one, because it is what proves this is not the two-key reader wearing a new name.\n(iv) EXPORTED BUT NOT YET CONSUMED — by design, and named above. The barrel probe plus D3's TS2724 control prove the export is real and reachable, which is the part a future consumer depends on.\n(v) SECRETS OR MANIFEST BYTES IN A LOG — NONE, and it cannot happen by construction: `grep -nE '\\$\\{|console\\.'` over the module returns ZERO `console.` and exactly three interpolations, all pre-existing identity derivations (the aggregate id, the internal decision-effect command id, the selected event id). `refuse()` carries only a code and a layer; the refusal type has no message or detail field at all, so no manifest byte can reach one.\n\nONE CORRECTION TO THE PLAN, recorded because following it literally reds the gate: step 3(4) said to insert the roster entry \"directly after `readFoundationAttemptRecord`, since the list is sorted\". It is not sorted there — `readImportShadowProjection` sits between them and \"readI\" < \"readL\" — so that placement failed the set-equality arm. The entry belongs one line further down. The 143 -> 144 count bump was correct as written.",
      "timestamp": "2026-08-29T17:46:45.519Z"
    },
    {
      "id": "comment-80232dce03404b1ba347133181f414b2",
      "author": "qa-a1b5393b",
      "content": "QA REVIEW RECORD — qa-a1b5393b. Verdict APPROVE. Every command below I ran myself at HEAD 5b3d7313; nothing is banked from the worker's record, and two of their claims came out DIFFERENT under my hand (both in the row's favour — see GATES and the launcher note).\n\nCOMMIT. task.commits is `[]`, so a NO-COMPLETION-COMMIT warning is expected and is NOT a reject: I verified the tree myself. 5b3d7313 IS HEAD, `git branch --contains` => moe/work-2026-08-08, FOUR explicit paths exactly matching declaredPaths, no `git add -A`, +228/-4.\n\nDoD-1. comment-b38fe3ab carries the verify-or-block with `git rev-parse HEAD` quoted, each grep unpiped with its own exit code and a positive control (`git grep -c export` => 12). It also caught a real plan defect — the literal step command `pnpm exec vitest run <file>` from apps/daemon exits 1 with `No test files found` because the root include excludes `apps/**`, and the worker replaced it with the `--root . --config package.json` form rather than banking a zero-test gate. That correction is why this row's gate is admissible at all.\n\nDoD-2/3 — the arm roster is A-G and I read each one rather than counting them. D (STALE IS UNREACHABLE) is the strongest: it selects v1 then v2 on ONE store and asserts side by side that the validating reader refuses PROJECT_CONFIGURATION_STALE on the caller-held v1 digest while the new reader answers CURRENT at v2 — an unreachability arm that would be trivial to write vacuously and is not. E (READ-ONLY) is measured twice, and the second measurement is the one that matters: `expect(versionBefore).toBeGreaterThan(0)` forbids a vacuous zero baseline, the version and event count are pinned across the FOUND, ABSENT and REFUSED outcomes, and then a counting ReadPort proves `commitExpectedVersionDecision` is called ZERO times — because a commit against a DIFFERENT aggregate would leave every version assertion green.\n\nDoD-5 — CONTRACT-IDENTICAL, PROVEN NOT ASSERTED. I diffed the two exported bodies across the commit: `git show 5b3d7313^:<file> | sed -n '/^export function readCurrentProjectConfiguration/,/^}/p'` vs the same at 5b3d7313 => IDENTICAL, and the same for selectProjectConfiguration => IDENTICAL. The `string | null` widening is on the module-PRIVATE `readCurrent` helper; the exported reader still gates `typeof === \"string\" && HEX64.test(...)` before it, so PROJECT_CONFIGURATION_STALE remains reachable from it and task rail 3 holds at the boundary that matters.\nLINE CAP: 269 physical lines. That is over DoD-5's literal 250 and I checked before charging it — comment-3d4edcd8 is the ROW AUTHOR'S OWN amendment raising this module to <= 270 (hard ceiling 400), with the reason recorded (the sibling split would export three module-private helpers to save ~12 lines). 269 <= 270. Not a finding.\nEXISTING ARM COUNTS: step 1 measured the suite at `Tests 15 passed (15)`; it is now 22 = the same 15 pre-existing arms (11 + 3 + 1 across the three original describes) plus exactly the 7 new ones. No pre-existing arm was rewritten or dropped.\n\nMY OWN DRILLS — two, each varying ONE degree of freedom the worker did NOT vary, both RED on the arm they name.\n- DRILL 1: skip the RECEIPT cover on the null branch only (`expectedSettingsDigest === null ? true : validReceipt(...)`). EXIT 1, `Tests 1 failed | 21 passed (22)`, SOLE red `F: FORGED-RECORD PARITY`, failing on `forged parity failed: receipt missing`. The worker's D1 skipped the DECISION cover and redded the same arm on `trace project`. TWO DIFFERENT FORGED CASES NAMED BY TWO DIFFERENT MUTANTS is what proves arm F grades its eight cases individually instead of tripping on whichever one happens to be first.\n- DRILL 2: replace `exactRecord(input, [\"projectId\"])` with a permissive cast. EXIT 1, SOLE red `B: EXACT ROSTER - an extra, missing or ill-typed key refuses, so it is no alias`. No worker drill covered the roster gate; it is now graded.\n- RESTORE PROOF: all four owned paths were TRACKED and scoped-porcelain CLEAN at claim (unlike the worker's session, where they were uncommitted and needed SHA256), so `git diff --exit-code` is valid here and I ran it in a call SEPARATE from each restore — EXIT 0 both times, final porcelain over the owned paths EMPTY.\n\nDoD-4 BARREL PROBE, re-run with MY OWN control. Probe `import { readLatestProjectConfiguration } from \"./index.js\"` => 0 diagnostics naming the probe. A zero-count grep proves nothing alone, so the control: the same probe importing `readLatestProjectConfigurationTYPO` => exactly 1 diagnostic, `TS2724: '\"./index.js\"' has no exported member named 'readLatestProjectConfigurationTYPO'. Did you mean 'readLatestProjectConfiguration'?`. Both probes deleted, porcelain confirms.\n\nROSTER DIFF, graded as the architect asked (exactly two hunks in index-surface.test.ts) — AND ONE CORRECT DEVIATION worth recording so nobody re-files it. comment-35c3960645 instructed inserting the entry AFTER `readFoundationAttemptRecord`. The worker inserted it after `readImportShadowProjection` instead. That is the SORT-CORRECT slot and the deviation was mandatory: the arm at :500 is `expect(Object.keys(daemon).sort()).toEqual(EXPECTED_EXPORTS.map(...))`, so the roster is ORDER-SENSITIVE and following the amendment literally would have redded it. The literal instruction was wrong; the delivered code is right.\n\nGates and the foreign-red re-attribution are in the next comment.",
      "timestamp": "2026-08-29T17:54:03.593Z"
    },
    {
      "id": "comment-c7396fb628204909812e8eeb4660ac12",
      "author": "qa-a1b5393b",
      "content": "QA REVIEW RECORD, 2 of 2 — GATES AND FOREIGN-RED RE-ATTRIBUTION. qa-a1b5393b. Continues comment-80232dce03404b1ba347133181f414b2.\n\nG2 FOCUSED, DoD verification command re-run verbatim from apps/daemon: `node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/configuration/project-configuration-selection.test.ts src/index-surface.test.ts src/provider-profile/provider-profile-resolver.test.ts src/runtime-entrypoint.test.ts` => EXIT 0, `Test Files 4 passed (4)`, `Tests 271 passed (271)`. The worker recorded 270; the extra arm is peer churn in one of the two unowned suites, not a discrepancy in this row's work — its own suite is 22 both times.\n\nG1 TYPECHECK — THE WORKER'S DISCLOSURE IS NOW OUT OF DATE, IN THE ROW'S FAVOUR, AND THEN BACK AGAIN. comment-8220ad6b discloses `pnpm --filter @moe/daemon typecheck` at EXIT 1 with foreign errors in `src/cutover/` (untracked, task-b2548479), and argues DoD-7's exit-0 clause cannot be met while a peer's in-flight file is broken. I re-ran it at review time: **EXIT 0**, with the `$ tsc --project tsconfig.json` echo. The foreign red had cleared, so DoD-7's exit-0 clause is met LITERALLY and needs no waiver at all. A sub-second green proves nothing on its own, so I ran the positive control: planted `export const qaControl: number = \"not-a-number\";` under `apps/daemon/src/configuration/` => EXIT 1, `error TS2322`; probe deleted, porcelain clean. The leg executes and covers the owned directory.\nHONEST ADDENDUM: a LATER run in the same review went back to EXIT 1, on a completely different foreign file (`session-handshake`, `Type 'Record<string, unknown>' is missing … from type 'SessionHandshakeMinted'`), which is peer-dirty right now. So this package's typecheck is flipping under live peer writes. What I certify is what I measured: at a moment when the owned bytes were byte-identical to the delivered ones, the leg was EXIT 0. No diagnostic in ANY of my runs named an owned path.\n\nG3 FULL DAEMON SUITE `pnpm --filter @moe/daemon test` => EXIT 1, `Test Files 6 failed | 301 passed (307)`, `Tests 19 failed | 6351 passed | 12 todo (6382)`. The worker measured 21/8; the set has moved under both of us, which is itself the tell that these are peer-owned.\n\nATTRIBUTION, RE-DERIVED RATHER THAN INHERITED — four independent legs, because an empty owned-path intersection on its own proves nothing.\n1. REACHABILITY OF THE NEW SYMBOL: `git grep -ln readLatestProjectConfiguration HEAD -- apps packages` returns EXACTLY the four owned paths. Nothing else in the repo can call it.\n2. THE BARREL WIDENING CANNOT REACH THEM: none of the six failing files imports `./index.js` or `../index.js` (grep -l over the six returns nothing), so publishing a 144th export cannot change their behaviour.\n3. THE ONE REAL IMPORT EDGE GETS THE STRONGER PROOF. `launch-template-producer.test.ts` is the only failing file referencing the edited module, via `selectProjectConfiguration` — which I proved BYTE-IDENTICAL across the commit by diffing its body at 5b3d7313^ and 5b3d7313. The only behavioural change, the null branch, is unreachable from it: `readCurrentProjectConfiguration` gates on `HEX64.test(...)` before calling `readCurrent`, so it can never pass null.\n4. WHERE THE RED ACTUALLY LIVES. The failing assertions are `CLAUDE_LAUNCH_MODEL_MISMATCH` / `CLAUDE_LAUNCH_REQUEST_MALFORMED` — a vocabulary owned by `packages/runner/src/providers/claude/`, and `packages/runner` is peer-dirty right now (`M windows-broker-path.ts`, `?? windows-broker-path.test.ts`). `git log` shows neither launch-template-producer file has been touched since 2adc7dc4, well before this row. The two activation suites consume the same runner surface.\nOWNED-PATH FAILURE DELTA: EMPTY, on all four legs. Rail 3 satisfied, and DoD-7's disclose-verbatim clause is met by the list above.\n\nCONSUMER EDGE (global rail clause 1(a)): satisfied by NAMING, which is the (a) branch and is legitimate — completionSummary and comment-7a4600c8 both name task-e4cd1eb6807244efa3334609ed746143 as the composer that will bind this reader for MOE_PROJECT_CONFIGURATION_DIGEST. No production caller is added here, exactly as the row's scope requires. FOR WHOEVER PICKS UP e4cd1eb6: the gap that blocked you twice is closed — `readLatestProjectConfiguration(store, { projectId })` returns the full CurrentProjectConfiguration including `manifest.settingsDigest`, from the barrel, with every decision-coverage fence intact and zero writes. Re-run your own premise probe at HEAD before planning around it; do not take this sentence as the measurement.\n\nNOTHING I FOUND RISES TO A FINDING. The two things I would normally charge — a 269-line module against a 250 cap, and an exported reader's signature changing — are both pre-authorised on the row (comment-3d4edcd8) and, in the second case, not actually true at the exported boundary.",
      "timestamp": "2026-08-29T17:54:35.200Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T16:54:13.356Z",
  "updatedAt": "2026-08-29T17:55:54.566Z",
  "metrics": {
    "firstClaimAt": "2026-08-29T16:55:19.223Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 5,
    "doneAt": "2026-08-29T17:55:54.566Z",
    "wallClockMs": 3635343
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T16:58:27.273Z",
  "planningNotes": {
    "approachesConsidered": "(1) REJECTED: a sibling module for the reader. `readCurrent`, `exactRecord`, `refuse`, `aggregateId` and `stableTail` are all module-private in project-configuration-selection.ts; a sibling would need three internal exports and would scatter one authority across two files to save ~12 lines. Delivery-bias rail prefers the smallest working slice: the reader lives IN the selection module beside readCurrentProjectConfiguration (DoD-5 amended by the author in the row comment: <= 270 lines accepted, hard ceiling 400). (2) REJECTED: widening readCurrentProjectConfiguration to accept an optional expectedSettingsDigest — e4cd1eb6's step 3 forbade widening that contract and provider-profile-resolver.ts:150 depends on its exact two-key roster; the new reader is a SEPARATE exact-roster entry. (3) REJECTED: reading the tail through store.readAggregateEvents in the consumer (moe-up-env) — that reimplements decision coverage outside the authority. CHOSEN: `readCurrent(store, projectId, expectedSettingsDigest: string | null)` — null skips ONLY the digest equality (:183) and every other fence stays; `readLatestProjectConfiguration(store, input)` = exactRecord(input, [\"projectId\"]) + isLogicalRef + readCurrent(..., null).",
    "codebaseInsights": "MEASURED AT HEAD 595c32c2 (2026-08-29): `git grep -n '^export function' HEAD -- apps/daemon/src/configuration/project-configuration-selection.ts` -> :190 readCurrentProjectConfiguration, :204 selectProjectConfiguration ONLY (gap open). Module 249 lines, porcelain clean (also .js bridge and .test.ts and index.ts clean). readCurrent (:162-189) is the fold: aggregateId(projectId) -> stableTail (3 attempts, page/version consistency) -> ABSENT/CONFLICT -> event type/schema/aggregate checks -> validateDecision (decision record exactness, EFFECTS_COMMITTED, versions, businessEventIds, requestSha256, resultBytes byte-equal to payload, commandId = `moe-internal:decision-effect:<decisionId>`) -> validReceipt -> decode -> manifest.projectId check -> DIGEST EQUALITY (:183, the only line that becomes conditional) -> CURRENT. Refusal shape `refuse(code, upstream?)` = {authority:'NONE', code, layer:'PROJECT_CONFIGURATION_SELECTION', ok:false, outcome:'UNKNOWN', upstream}. exactRecord (:44) rejects extra/missing/non-enumerable/accessor keys — the exact-roster guard the new reader reuses. Barrel: apps/daemon/src/index.ts:193-198 re-exports the selection symbols; add the new function there. Bridge: project-configuration-selection.js already exists (`export * from \"./project-configuration-selection.ts\";`) so a new export needs NO bridge work. Test harness (project-configuration-selection.test.ts, 540 lines): real SqliteEventStore in a temp dir with beforeEach/afterEach; `manifestBytes(modelRef)` builds a valid manifest; `selectionRequest(bytes, commandId, expectedVersion)`; `captureRecords()` (:369) selects once and captures decision/event/page/receipt/version; `portFor(records, overrides)` (:390) builds a ProjectConfigurationStore port over captured records — the seam for read-only and forged-record arms; `currentRequest(records)` derives the digest; `expectUnknown(result, code, upstream?)` (:92). Existing arms to extend: :164 two selections (v1 then v2) — the STALE-unreachable side-by-side; :424 absence on an empty port whose other methods throw — reuse for the new reader; :465 forged-record loop — add the new reader per case; :529 vocabulary arm pins the code roster (no new code). Consumers of the module (all untouched): provider-profile-resolver.ts:19/:150, recipe-seal-composition.ts:6, provider-profile-reader-checks.ts:14, provider-runtime-observation-reader.ts:17, foundation-context-record.ts:26, two planning test fixtures.",
    "risks": "(R1) The ONLY behavioural difference between the two readers must be the digest equality; a worker who 'simplifies' readCurrent while adding the null branch can silently drop a fence — the forged-record parity arm (step 2 F) exists to catch exactly that; run it against BOTH readers per case. (R2) Aliasing: if the new reader internally builds `{projectId, expectedSettingsDigest: <tail digest>}` and calls readCurrentProjectConfiguration, it reads the tail TWICE and the second read can race a concurrent selection (stableTail is bounded to 3 attempts) — forbidden; call readCurrent once with null. (R3) Read-only proof must use the PORT (portFor) so commitExpectedVersionDecision is observable as a call, AND a real-store version check; a version-only check would pass a reader that commits a no-op decision on another aggregate. (R4) Line count lands ~262 (> 250 target): the DoD-5 amendment comment is the authority; do not code-golf existing lines to hit 250. (R5) index.ts is a hot shared barrel — porcelain it immediately before editing; other rows edit it. (R6) `pnpm --filter @moe/daemon exec vitest run <path>` runs zero tests; run focused suites from apps/daemon with `pnpm exec vitest run <path>` and quote `Test Files 1 passed`.",
    "keyFiles": [
      "apps/daemon/src/configuration/project-configuration-selection.ts",
      "apps/daemon/src/configuration/project-configuration-selection.test.ts",
      "apps/daemon/src/index.ts",
      "apps/daemon/src/provider-profile/provider-profile-resolver.ts",
      "apps/daemon/src/orchestrator/moe-up-env.ts"
    ]
  },
  "budget": {
    "wallClockMs": 10800000
  },
  "planApprovedAt": "2026-08-29T16:58:29.421Z",
  "contextFetchedBy": [
    "worker-5097adb2",
    "worker-4d8ff141",
    "worker-8d70fe57",
    "qa-a1b5393b"
  ],
  "workStartedAt": "2026-08-29T17:17:20.733Z",
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "blockedOnTaskIds": null,
  "reviewStartedAt": "2026-08-29T17:47:24.929Z",
  "verification": {
    "command": "node ../../node_modules/vitest/vitest.mjs run --root . --config package.json src/configuration/project-configuration-selection.test.ts src/index-surface.test.ts src/provider-profile/provider-profile-resolver.test.ts src/runtime-entrypoint.test.ts",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  4 passed (4)\n      Tests  271 passed (271)\n   Start at  20:46:09\n   Duration  5.66s\n\nVERIFY_EXIT=0",
    "reportedAt": "2026-08-29T17:47:24.929Z"
  },
  "filesModified": [
    "apps/daemon/src/configuration/project-configuration-selection.ts",
    "apps/daemon/src/configuration/project-configuration-selection.test.ts",
    "apps/daemon/src/index.ts",
    "apps/daemon/src/index-surface.test.ts"
  ],
  "completionSummary": "Commit 5b3d7313, four owned paths. Read comment-7a4600c8fa7349de8c278b5f2f44dcd6 (completion + self-review) and comment-8220ad6b35dd4e79bc530c9efae023fa (probe, drills, gates) — this field is write-only.\n\nDELIVERED CLAIM, verbatim as the plan requires: the current durable project configuration, including its settingsDigest, is now DISCOVERABLE through the selection authority with every decision-coverage fence intact and no write; readCurrentProjectConfiguration is unchanged.\n\nSURFACE: readLatestProjectConfiguration(store, { projectId }) — exact one-key roster, same result union, exported from the daemon barrel. CONSUMER named per global rail 1(a): task-e4cd1eb6807244efa3334609ed746143, whose step 3 binds it to compose MOE_PROJECT_CONFIGURATION_DIGEST. That row blocked TWICE with zero bytes on exactly this gap — the existing reader takes the digest as a HEX64 input it validates and never yields, stableTail is module-private, and selectProjectConfiguration returns it only through a WRITE. There was no read path to a digest a caller could hold; there is one now.\n\nTHE ONLY LOGIC CHANGE is readCurrent's digest parameter widening to string | null plus a null guard on the comparison. Everything above it still runs, and that is proven rather than asserted: arm F asserts WHOLE-VALUE equality against the validating reader across eight forged cases (so code, layer AND upstream must agree), and drill D1 reds exactly that arm when the decision cover is skipped on the null branch while the old reader's own arms stay green.\n\nREAD-ONLY is measured twice because once is not enough: one arm pins the aggregate version and event count across the found, absent AND refused outcomes, since a write on a refusing path is where a careless implementation would hide; a second counts calls to commitExpectedVersionDecision, because a commit against a DIFFERENT aggregate would leave the first arm green. Drill D2 reds them.\n\nDRILLS 4/4 RED, restores proven by SHA256 in calls separate from each res",
  "completedAt": "2026-08-29T17:55:54.566Z",
  "reviewCompletedAt": "2026-08-29T17:55:54.566Z",
  "reviewSummary": "APPROVE. Evidence: comment-80232dce03404b1ba347133181f414b2 + comment-c7396fb628204909812e8eeb4660ac12. All re-run by me at HEAD 5b3d7313.\n\nCOMMIT: task.commits is [], so NO-COMPLETION-COMMIT is expected, not a reject - 5b3d7313 IS HEAD, four explicit paths matching declaredPaths, no add -A.\n\nDoD-5 PROVEN not asserted: diffing both exported bodies at 5b3d7313^ vs 5b3d7313 gives IDENTICAL. The string|null widening is on the module-PRIVATE readCurrent; the exported reader still gates HEX64 before it, so STALE stays reachable. 269 lines is over DoD-5's literal 250 but under the row author's own amendment (comment-3d4edcd8, <=270). Arms: 15 pre-existing + exactly 7 new = 22.\n\nMY DRILLS, two, each varying a degree of freedom the worker did NOT, both RED on the arm they name. (1) skip the RECEIPT cover on the null branch => sole red \"F: FORGED-RECORD PARITY\" on \"receipt missing\"; their D1 skipped the DECISION cover and redded that arm on \"trace project\" - two mutants naming two different forged cases proves F grades its cases individually. (2) permissive cast for exactRecord(input,[\"projectId\"]) => sole red \"B: EXACT ROSTER\", uncovered by any worker drill. Restore: tracked+clean at claim, git diff --exit-code EXIT 0 in a SEPARATE call each time, porcelain EMPTY.\n\nBARREL PROBE with MY control: real symbol => 0 diagnostics naming the probe; typo import => exactly 1, TS2724.\n\nGATES: DoD command verbatim EXIT 0, 4 files / 271 tests. daemon typecheck EXIT 0 - the worker's disclosed EXIT 1 had CLEARED, so no waiver needed - with positive control (planted TS2322 => EXIT 1); a later run flipped to EXIT 1 on a different peer-dirty file, disclosed. Full daemon suite EXIT 1, 19 failed / 6 files: readLatest reaches only the four owned paths, no failing file imports the barrel, and the one real edge (launch-template-producer via selectProjectConfiguration) is byte-identical and cannot reach the null branch. Owned-path delta EMPTY."
}