{
  "id": "task-f79d38b119984ae1ae51eba887a28b7d",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "The compiler seals the whole DAG: an INITIAL run admits N nodes, the mission asks for the complete graph",
  "description": "SPIDR child 1 of parent task-4423d2a4c3084d579c132b253d36b502 (architect-20a6d1eb, 2026-09-04). Dependency-free LEAF; children 2-5 build on it.\n\nOWNER RULING (task-4423d2a4 comment-fbe64776, 2026-09-04, relayed from the owner's words \"lift the fence\"): LIFT THE ONE-NODE FENCE. An INITIAL run seals the WHOLE graph. The parent's description still argues for keeping the fence and is SUPERSEDED by that comment; its DoD 1 and DoD 5 are replaced.\n\nRE-MEASURED BY architect-20a6d1eb AT HEAD (branch moe/work-2026-09-04) - the ruling is correct on disk:\n- CORE FENCE IS GONE. `packages/core/src/planning/planning-run-submission.ts` `finalize` now refuses only `executionBearingKeys(witness).length === 0`; there is no runKind/node-count arm. `MULTI_NODE_EXECUTION_UNSUPPORTED` survives ONLY as a string in `packages/core/src/planning/planning-event-contract.ts:171` and in `packages/core/src/planning/planning-invariants.test.ts:32`, which asserts the code is NOT driven. Do not \"restore\" it.\n- WHAT ACTUALLY REFUSES: `apps/daemon/src/planning/compile-dispatcher.ts:196-197` `if (nodes.length > 1) return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\")`. The code is rostered one place: `SUBMIT_DECOMPOSITION_CODES` at compile-dispatcher.ts:47-51 (the constant runs 47..~52; the literal is line 51).\n- THE MISSION STILL TELLS SEATS THE OPPOSITE: `apps/daemon/src/orchestrator/agent-mission-text.ts:123` \"dependsOn}]}}. Plan the SMALLEST COMPLETE SLICE: exactly ONE node in an\" (the sentence wraps past 123 - read the surrounding array, do not sed a single line).\n- THE DOWNSTREAM MACHINERY IS ALREADY MULTI-NODE AND NEEDS NO CHANGE: `compile-dispatcher.ts:209-228` already reads, validates and seals per-node `dependsOn`; `apps/daemon/src/planning/compiled-authority-bodies.ts:62-70` already refuses an unknown dependency, a self-edge and a dependency on the completion node (COMPILED_PLAN_MALFORMED) and already refuses an uncovered criterion (COMPILED_PLAN_CRITERION_UNBOUND, lines 84-92). Multi-node propose was proven live once already (memory initial-run-seals-one-node-by-core-design).\n\nSOURCE-TEXT ASSERTER (global rail 23 enumeration, `git grep -l SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL` over the whole tree = exactly 3 hits): compile-dispatcher.ts:51 (roster), compile-dispatcher.ts:197 (the refusal), and `apps/daemon/src/goals/goal-close-readiness.test.ts:18` - a PROSE COMMENT that states the refusal as a premise. That third file is in scope: correct the comment, do not leave it asserting a retired rule. NOTE that file is currently dirty in the shared checkout (peer WIP) - edit only the comment lines you own, never revert or stash the rest (global rail 5).",
  "definitionOfDone": [
    "`apps/daemon/src/planning/compile-dispatcher.ts` admits N>=1 execution-bearing nodes: the `nodes.length > 1` arm is deleted and `SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL` is REMOVED from `SUBMIT_DECOMPOSITION_CODES`. `git grep -c SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL` over the tree returns 0 hits in production source (the goal-close-readiness.test.ts prose comment is rewritten, not left stating the retired rule).",
    "A dispatcher test submits a THREE-node structure with a real HARD chain (c depends on b, b depends on a) and asserts the submission SEALS: assert the returned graphContentHash/submissionHash are present and the disposition is DECIDED - not merely that it did not refuse. A second arm submits one node and still seals (N=1 is not regressed).",
    "The refusal arms that MUST survive are pinned by code, each asserting the specific code and layer: an unknown `dependsOn` target and a self-edge both refuse COMPILED_PLAN_MALFORMED; a criterion bound by no node refuses COMPILED_PLAN_CRITERION_UNBOUND. These are the fence that replaces the node-count fence - a plan whose DAG is incoherent must still not seal.",
    "`apps/daemon/src/orchestrator/agent-mission-text.ts` tells the planning seat to plan the COMPLETE graph as a dependency DAG: every criterion of the approved revision covered by exactly one node, `dependsOn` naming hard build order. A mission-text test pins the NEW sentence verbatim and asserts the string \"exactly ONE node\" no longer appears anywhere in the produced mission.",
    "Mutation drill recorded in a step note: re-insert `if (nodes.length > 1) return refused(...)` in compile-dispatcher.ts, confirm the three-node sealing test goes RED, restore, and prove the restore with `git diff -- apps/daemon/src/planning/compile-dispatcher.ts` (tracked file; quote the empty diff).",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/core test`, `pnpm typecheck`, `pnpm test`. Note explicitly that `pnpm test` covers no apps/** (global rail 18)."
  ],
  "taskRails": [
    "DO NOT TOUCH packages/core. The core fence was already removed by 63efd3a3 and `planning-invariants.test.ts:32` asserts MULTI_NODE_EXECUTION_UNSUPPORTED is not driven. This row changes the DAEMON compiler and the mission text only. If a core change looks necessary, report blocked rather than editing core.",
    "FEATURES ONLY (inherited from the parent): no security-boundary or policy work.",
    "The board's shared checkout is dirty with peer WIP including apps/daemon/src/goals/goal-services.test.ts. Never revert, stash or `git add -A`; report every path you touch in complete_step.modifiedFiles (global rail 5)."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY AND RE-MEASURE, NO EDITS. The fence-lift ruling is the authority for this row; confirm it still describes disk before changing anything. Run and quote in a step note: (a) `git grep -n SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL -- '*.ts'` — expect EXACTLY 3 hits, all named in this plan: compile-dispatcher.ts:51 (the roster entry), compile-dispatcher.ts:197 (the refusal), goal-close-readiness.test.ts:18 (a prose comment). A FOURTH hit means a peer added a consumer while this row waited — stop and report rather than taking the extra path. (b) `git grep -n 'SUBMIT_DECOMPOSITION_CODES' -- '*.ts'` — expect exactly ONE hit, its own declaration; there is no external scanner and no length pin over this roster (measured at plan time). (c) `sed -n '125,155p' packages/core/src/planning/planning-run-submission.ts` — confirm `finalize` still refuses only `bearing.length === 0`, i.e. core will ACCEPT the multi-node runs this row starts sealing. If core has regained a node-count arm, STOP and report blocked: this row must not edit core. (d) `git status --porcelain` on the five files in this plan — `goal-close-readiness.test.ts` is expected DIRTY with peer WIP; record its state now so step 5 can prove it touched only its own line.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.ts",
        "apps/daemon/src/goals/goal-close-readiness.test.ts",
        "packages/core/src/planning/planning-run-submission.ts"
      ],
      "note": "STEP 1 RE-MEASURE, NO EDITS. All four probes match the plan; the fence-lift ruling still describes disk.\n\n(a) `git grep -n SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL -- '*.ts'` = EXACTLY 3 hits, all named in the plan:\n  apps/daemon/src/goals/goal-close-readiness.test.ts:18: * `runSubmitDecomposition` refuses `nodes.length > 1` (SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL —\n  apps/daemon/src/planning/compile-dispatcher.ts:51:  \"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\",\n  apps/daemon/src/planning/compile-dispatcher.ts:197:    return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\");\nNo 4th hit; no peer added a consumer. git grep is blind to untracked files, so I also ran an untracked-inclusive scan `grep -rn --include='*.ts' -l SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL apps packages adapters` = the same 2 FILES (goal-close-readiness.test.ts, compile-dispatcher.ts), no untracked consumer.\n\n(b) `git grep -n 'SUBMIT_DECOMPOSITION_CODES' -- '*.ts'` = EXACTLY ONE hit, its own declaration:\n  apps/daemon/src/planning/compile-dispatcher.ts:47:export const SUBMIT_DECOMPOSITION_CODES = Object.freeze([\nNo external scanner, no `.length` pin over this roster. Retiring an entry breaks no counter.\n\n(c) `sed -n '125,155p' packages/core/src/planning/planning-run-submission.ts` — `finalize` refuses only the empty-bearing case. Verbatim tail:\n  const bearing = executionBearingKeys(command.witness);\n  if (bearing.length === 0) return illegal(state, command.kind);\nNo runKind arm, no node-count arm. Its own docblock now says so: \"Finalization admits bounded execution-bearing rosters for every compound submission kind. Structural graph limits and dependency validity belong to the canonical graph/compiler authorities\". Core will ACCEPT the multi-node runs this row starts sealing — NOT blocked, no core edit needed (task rail 1 honoured).\n\n(d) `git status --porcelain` on the plan's five files = ONE dirty path, exactly the one the plan predicted:\n   M apps/daemon/src/goals/goal-close-readiness.test.ts   (peer WIP)\nThe other four (compile-dispatcher.ts, compile-dispatcher.test.ts, agent-mission-text.ts, agent-mission-text.test.ts) are CLEAN and TRACKED — so the empty-`git diff` restore proofs planned for steps 5/7 are sound on those (global rail 13 satisfied: no untracked path is being drilled). Whole tree carries 68 dirty paths of peer/pre-existing debris; noted and ignored per global rail 5 (never revert/stash/`git add -A`).\n\nShapes read for the coming steps: roster at compile-dispatcher.ts:47-52 (literal on :51); the refusal at :196-198 sits under a 5-line comment block at :191-195 asserting the retired core rule; the per-node `dependsOn` read/validate/seal loop at :204-228 is already N-node and untouched by this row. File sizes: compile-dispatcher.ts 341, compile-dispatcher.test.ts 292, agent-mission-text.ts 172 — all under the 400 cap, and step 4 only deletes.",
      "startedAt": "2026-09-04T21:03:50.713Z",
      "completedAt": "2026-09-04T21:04:28.920Z"
    },
    {
      "stepId": "step-2",
      "description": "TESTS FIRST, RUN RED. apps/daemon/src/planning/compile-dispatcher.test.ts (292 lines today; `structureOf()` at :160 currently takes no arguments and builds a single node — generalise it to take a node list, keeping every existing call site's behaviour byte-identical so the INITIAL arms in `describe(\"runSubmitDecomposition\")` at :194-292 stay green). Add: (1) THE SEALING ARM — submit a THREE-node structure with a real HARD chain (c dependsOn b, b dependsOn a; criteria split across the three so every contract criterion is bound exactly once) and assert it SEALS: `ok === true`, and BOTH `graphContentHash` and `submissionHash` are present non-empty strings, and the disposition is DECIDED. Asserting only \"did not refuse\" is not enough — a refusal arm that stops returning a code would pass that. (2) THE N=1 REGRESSION ARM — a one-node structure still seals, so lifting the fence did not trade one count limit for another. Run both RED before touching production: (1) must fail with `SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL` and (2) must already pass.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.test.ts"
      ],
      "note": "STEP 2 TESTS FIRST, RUN RED. Both arms written and run BEFORE any production edit; the three-node arm failed with the exact fence code, the N=1 arm already passed.\n\nTARGETED RUN (the package script's trailing `src` positional means `pnpm --filter @moe/daemon test -- <path>` does NOT scope — it ran all 408 files in 342 s; used `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src/planning/compile-dispatcher.test.ts` instead):\n  ❯ src/planning/compile-dispatcher.test.ts (8 tests | 1 failed) 641ms\n     × SEALS a three-node INITIAL graph with a hard c->b->a chain: node count is not a fence 96ms\n  Error: three-node dispatch refused: SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL @ COMPILE_DISPATCHER\n  Test Files  1 failed (1)\n       Tests  1 failed | 7 passed (8)\nRED for the right reason and at the right LAYER — the throw prints code AND layer, so the arm cannot be satisfied by some other layer answering first.\n\nWHAT WAS WRITTEN\n(1) SEALING ARM — a THREE-node structure with a real hard chain: node-c dependsOn node-b, node-b dependsOn node-a. completionNodeKey is \"node-c\" because nothing depends on it (compiled-authority-bodies.ts:66-71 refuses any node that depends on the completion node, so the chain's TAIL must be the completion node). It asserts SEALING, not absence-of-refusal: disposition === \"DECIDED\", graphContentHash and submissionHash both `typeof === \"string\"` with length > 0, runId === RUN_ID, and getAggregateVersion(RUN_ID) === 2 (propose fold + finalize). A refusal arm that merely stopped returning a code would fail every one of those.\n(2) N=1 REGRESSION ARM — a one-node structure still seals with DECIDED and both hashes non-empty.\n\nHELPER GENERALISATION, behaviour-preserving\n- `structureOf()` now takes `(nodes = [<the original single slice>], completionNodeKey = \"node-slice\")`. Its default node keeps the ORIGINAL nodeKey/criterionIds/objective bytes (\"Land the record read and its page.\"), so the four existing single-slice call sites are byte-identical — confirmed by the run: all 5 pre-existing arms stayed green, including the two that compare submissionHash/graphContentHash across runs (a changed objective string would have been invisible to those, since they compare two runs to each other and pin no hash literal — that is exactly why I preserved the bytes rather than trusting the arms to catch drift).\n- New `nodeOf(nodeKey, criterionIds, dependsOn = [], objective?)` builds a node over the shared NODE_SCOPES; `chainStructure()` = `structureOf(CHAIN_NODES, \"node-c\")`.\n- FIXTURE EXTENSION, opt-in: the committed revision carries only crit-api and crit-ui, so three nodes cannot each bind exactly one criterion. `committedRevision(store, thirdCriterion = false)` now appends crit-worker/req-worker ONLY when asked. Default false keeps every existing arm's revision byte-identical. The alternative — a third node binding zero criteria — would have leaned on compiled-policy-authority-body.ts:101 `node.criterionIds[0] ?? \"criterion-unbound\"`, i.e. proved sealing of a graph with an unbound node rather than the real DoD shape.\n\nMEASURED WHILE WRITING (shapes the later steps): the finalize witness at compile-dispatcher.ts:319-321 marks EVERY sealed node `executionBearing: true`, so a 3-node graph presents 3 bearing keys and core's `bearing.length === 0` arm is satisfied — core admits it. `COMPILED_PLAN_NODE_BUDGET = 24` (compiled-authority-contracts.ts:30) is the real, already-live ceiling on node count and is untouched by this row.",
      "startedAt": "2026-09-04T21:04:33.012Z",
      "completedAt": "2026-09-04T21:12:38.711Z"
    },
    {
      "stepId": "step-3",
      "description": "PIN THE FENCE THAT REPLACES THE NODE-COUNT FENCE, still in compile-dispatcher.test.ts, still before production changes. Lifting the count limit is only safe because DAG coherence is already enforced by `apps/daemon/src/planning/compiled-authority-bodies.ts` — measured at plan time: :62-70 refuses an unknown `dependsOn` target, a self-edge, and a dependency on the completion node with COMPILED_PLAN_MALFORMED; :84-92 refuses a criterion satisfied by no node with COMPILED_PLAN_CRITERION_UNBOUND. Write one arm per refusal, each asserting the SPECIFIC code AND the layer the dispatcher reports (global rail 1 — a bare \"it refused\" arm is vacuous here, because several layers can refuse a malformed structure and the wrong one answering would keep the test green). Cover at minimum: an unknown dependsOn target; a self-edge; a criterion bound by no node. These arms are this row's real deliverable — they are what makes a multi-node plan safe to seal — so write them even though they exercise code you are not editing. Run them: they should PASS already; if one does not, the coherence fence is weaker than measured and this row must report blocked rather than lift the count fence on top of it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.test.ts"
      ],
      "note": "STEP 3 COHERENCE FENCE PINNED, before any production edit. NOT BLOCKED: the fence is exactly as strong as the plan measured. Every arm pins the SPECIFIC code AND the layer (global rail 1) via a `refusalOf` helper that returns the literal `${code} @ ${layer}` string, so a different layer answering is a visible string mismatch, never a silent pass.\n\nLAYER MEASURED, NOT ASSUMED: compiled-authority-bodies.ts:20 `const LAYER = \"COMPILED_PLAN_PRODUCER\"` and :23-25 `refused(code, detail)` stamps it; the dispatcher forwards it verbatim at compile-dispatcher.ts:240 `return refused(compiled.code, compiled.layer)`. So the expected string is `<CODE> @ COMPILED_PLAN_PRODUCER` — a refusal from COMPILE_DISPATCHER would not match.\n\nARM A — \"refuses an incoherent DAG at the compiled-plan producer, with the count fence irrelevant\". SINGLE-node forms deliberately, so the still-live node-count arm cannot answer instead; this is the arm that settles the plan's block condition.\n  unknown dependsOn target (\"node-ghost\")  -> COMPILED_PLAN_MALFORMED @ COMPILED_PLAN_PRODUCER\n  self-edge (node-slice dependsOn itself)  -> COMPILED_PLAN_MALFORMED @ COMPILED_PLAN_PRODUCER\n  criterion bound by no node (binds crit-api only, crit-ui uncovered)\n                                           -> COMPILED_PLAN_CRITERION_UNBOUND @ COMPILED_PLAN_PRODUCER\n  plus getAggregateVersion(RUN_ID) === 0 — nothing durable was touched.\nRESULT: PASSES ALREADY, with the count fence still in place. The coherence fence stands on its own and does not depend on anything this row changes. No block.\n\nARM B — \"keeps refusing an incoherent DAG once N>1 is admitted: the coherence fence is the fence\". The same four refusals expressed on the THREE-node chain, plus the one that only exists at N>1:\n  node-c dependsOn \"node-ghost\"            -> COMPILED_PLAN_MALFORMED\n  node-b dependsOn itself                  -> COMPILED_PLAN_MALFORMED\n  node-a dependsOn the COMPLETION node     -> COMPILED_PLAN_MALFORMED  (the chain read backwards)\n  node-c binds no criterion (crit-worker uncovered)\n                                           -> COMPILED_PLAN_CRITERION_UNBOUND\n  plus version 0.\nRESULT: RED right now, and the failure text is itself the evidence this arm is not vacuous:\n  Expected: \"COMPILED_PLAN_MALFORMED @ COMPILED_PLAN_PRODUCER\"\n  Received: \"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL @ COMPILE_DISPATCHER\"\nThat is the \"wrong layer answers first\" failure mode global rail 1 warns about, caught in the open: the count fence pre-empts the coherence fence for every N>1 structure, so today NO multi-node incoherence is ever graded by the producer. Arm B goes green in step 4 when the count fence is deleted, and it is what proves the lift did not open a hole.\n\nRUN: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src/planning/compile-dispatcher.test.ts`\n  ❯ src/planning/compile-dispatcher.test.ts (10 tests | 2 failed) 708ms\n     × SEALS a three-node INITIAL graph with a hard c->b->a chain: node count is not a fence\n     × keeps refusing an incoherent DAG once N>1 is admitted: the coherence fence is the fence\n  Test Files  1 failed (1)\n       Tests  2 failed | 8 passed (10)\nThe two reds are step 2's sealing arm and this step's arm B — both blocked by the same one line. Arm A and all five pre-existing arms are green.",
      "startedAt": "2026-09-04T21:12:41.408Z",
      "completedAt": "2026-09-04T21:14:08.715Z"
    },
    {
      "stepId": "step-4",
      "description": "MINIMUM IMPLEMENTATION, turning step 2 green. apps/daemon/src/planning/compile-dispatcher.ts (341 lines; the file stays well under the 400 cap because this step only DELETES): (a) delete the `if (nodes.length > 1) return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\");` arm at :196-197 together with the 5-line comment block above it at :191-195 — that comment states the retired core rule (\"an INITIAL run seals exactly ONE execution-bearing node\") and must not survive as a false premise; replace it with one sentence saying node COUNT is unconstrained and DAG coherence is enforced by compiled-authority-bodies. (b) delete the `\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\",` entry from `SUBMIT_DECOMPOSITION_CODES` at :51. Change NOTHING else: the per-node `dependsOn` read, validation and sealing at :209-228 already handle N nodes and are what the sealing arm exercises. Do not touch packages/core (task rail 1). Re-run the step-2 and step-3 arms: all green.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.ts"
      ],
      "note": "STEP 4 MINIMUM IMPLEMENTATION — deletions only, both arms green. compile-dispatcher.ts 341 -> 337 lines (net -4; the file only lost code, so the 400 cap is not in play). packages/core untouched (task rail 1).\n\n(a) THE REFUSAL ARM AND ITS FALSE-PREMISE COMMENT, both deleted. Removed verbatim:\n      // CORE DESIGN, discovered at the finalize reducer (planning-run-submission.ts):\n      // an INITIAL run seals exactly ONE execution-bearing node — \"plan the smallest\n      // complete slice\" is a fence, not advice. Growth is the EXPANSION machinery's\n      // (graph.request_expansion), whose runs are inherently multi-node. Refusing\n      // here, with the path named, beats sealing a plan finalize must reject.\n      if (nodes.length > 1) {\n        return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\");\n      }\n   That comment was doubly false at HEAD — core's `finalize` has had no node-count arm since 63efd3a3 (re-measured in step 1), so the \"core design\" it cites does not exist. Replaced with a comment that states the live rule and names the layer that enforces it:\n      // NODE COUNT IS UNCONSTRAINED HERE: an INITIAL run seals the WHOLE graph. What a plan must\n      // satisfy is DAG COHERENCE, enforced by `compiledPlanAuthority` below — an unknown or\n      // self-referential `dependsOn` target and a dependency on the completion node refuse\n      // COMPILED_PLAN_MALFORMED, a criterion bound by no node refuses\n      // COMPILED_PLAN_CRITERION_UNBOUND, and COMPILED_PLAN_NODE_BUDGET caps the roster.\n\n(b) ROSTER ENTRY RETIRED. `\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\",` deleted from SUBMIT_DECOMPOSITION_CODES; the constant is now the three surviving codes (MALFORMED, GATE_DIGEST_MISMATCH, ALREADY_FINALIZED). Safe because step 1 measured the roster's only reference is its own declaration — no external scanner, no `.length` pin (unlike POLICY_RISK_READER_CODES, which IS count-pinned).\n\nNOTHING ELSE CHANGED. The per-node `dependsOn` read/validate/seal loop (now :205-224) was already N-node and is untouched; it is what the sealing arm exercises. The `refused()` helper, the payload-key arity check and every other refusal path are byte-identical.\n\nGREEN: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src/planning/compile-dispatcher.test.ts`\n  Test Files  1 passed (1)\n       Tests  10 passed (10)\nBoth step-2 arms (three-node SEALS with DECIDED + both hashes; N=1 still seals) and both step-3 arms now pass. Arm B flipping from `SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL @ COMPILE_DISPATCHER` to `COMPILED_PLAN_MALFORMED @ COMPILED_PLAN_PRODUCER` is the direct evidence that multi-node incoherence is now graded by the coherence fence rather than being hidden behind the count fence.\n\nDoD 1 GREP, re-run at this tree: `git grep -n SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL -- '*.ts'` = ONE hit, and it is not production source:\n  apps/daemon/src/goals/goal-close-readiness.test.ts:18: * `runSubmitDecomposition` refuses `nodes.length > 1` (SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL —\nZero hits in production source, as DoD 1 requires. Step 6 rewrites that surviving prose comment.",
      "startedAt": "2026-09-04T21:14:11.471Z",
      "completedAt": "2026-09-04T21:14:57.431Z"
    },
    {
      "stepId": "step-5",
      "description": "THE MISSION STOPS ASKING FOR ONE NODE. apps/daemon/src/orchestrator/agent-mission-text.ts: the sentence lives across :122-127 as array elements, not on one line — read the whole literal and rewrite the block, do not sed line 123. Today it reads \"Plan the SMALLEST COMPLETE SLICE: exactly ONE node in an INITIAL run (the daemon refuses more - growth is the expansion machinery's), its criterionIds covering every criterion of the approved revision.\" Replace with instructions to plan the COMPLETE graph as a dependency DAG: every criterion of the approved revision covered by exactly one node across the whole structure, and `dependsOn` naming hard build order between nodes. Keep the surrounding payload-shape sentences and the \"never authority bytes\" and RUN_POLICY_UNCLASSIFIABLE sentences unchanged. Then apps/daemon/src/orchestrator/agent-mission-text.test.ts:37-38 — the ONLY place in the tree that pins these words (measured: `git grep -n 'SMALLEST COMPLETE SLICE\\|exactly ONE node'` returns just the source and these two lines). Replace both pins with a verbatim pin on the new sentence, and ADD `expect(text).not.toContain(\"exactly ONE node\")` so the retired instruction cannot creep back. Note the test's own `it(...)` title says \"one slice\" — rename it to match what it now asserts.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/orchestrator/agent-mission-text.ts",
        "apps/daemon/src/orchestrator/agent-mission-text.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/orchestrator/agent-mission-text.ts",
        "apps/daemon/src/orchestrator/agent-mission-text.test.ts"
      ],
      "note": "STEP 5 THE MISSION STOPS ASKING FOR ONE NODE. Test edited FIRST and run RED, then the source. 14/14 green.\n\nTHE SENTENCE SPANS ARRAY ELEMENTS, so I rewrote the BLOCK (agent-mission-text.ts:122-127), never sed'd line 123. Measured first that `compilerMission` returns `[...].join(\" \")` (agent-mission-text.ts:148), so a pin must match across the element boundaries.\n\nREMOVED (3 elements):\n      \"dependsOn}]}}. Plan the SMALLEST COMPLETE SLICE: exactly ONE node in an\",\n      \"INITIAL run (the daemon refuses more - growth is the expansion machinery's),\",\n      \"its criterionIds covering every criterion of the approved revision. The daemon\",\nADDED (5 elements):\n      \"dependsOn}]}}. Plan the COMPLETE GRAPH as a dependency DAG: each criterion of\",\n      \"the approved revision bound by a single node, none left unbound and none bound\",\n      \"twice, and dependsOn naming the hard build order - a node lists the nodeKeys\",\n      \"that must land before it. No self-edge, no unknown target, and nothing may\",\n      \"depend on the completionNodeKey. The daemon\",\nThe parenthetical \"(the daemon refuses more...)\" had to go with it: it was the count fence's own justification and is now simply false. The three coherence rules replace it, so a seat that reads the mission knows what the compiler will actually refuse. Everything around it is byte-identical — the payload-shape sentences, \"you submit the plan, never authority bytes, hashes, witnesses or host facts\", and the RUN_POLICY_UNCLASSIFIABLE park sentence.\n\nTEST (agent-mission-text.test.ts:30-49), the tree's ONLY pin on these words:\n- Both old pins (`toContain(\"SMALLEST COMPLETE SLICE\")`, `toContain(\"exactly ONE node\")`) replaced by a VERBATIM pin on the new sentence, written as one concatenated string so it is checked across the join, plus a second verbatim pin on \"No self-edge, no unknown target, and nothing may depend on the completionNodeKey.\"\n- ADDED both negative pins: `not.toContain(\"exactly ONE node\")` and `not.toContain(\"SMALLEST COMPLETE SLICE\")`, so the retired instruction cannot creep back.\n- `it(...)` title renamed from \"structure only, one slice, policy park stops\" to \"structure only, the COMPLETE graph, policy park stops\" — it no longer asserts a slice.\n\nNEAR-MISS I DELIBERATELY AVOIDED: my first draft read \"covered by exactly one node\". `toContain` is case-sensitive so \"exactly one node\" would not have matched the `not.toContain(\"exactly ONE node\")` guard, but a reader auditing the file would have seen the banned phrase sitting a case-fold away from its own negative pin. Reworded to \"bound by a single node, none left unbound and none bound twice\" — the guard and the text cannot be confused for each other, and the wording states the coverage duty more exactly anyway.\n\nRED THEN GREEN, both runs quoted:\nBEFORE the source edit — `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src/orchestrator/agent-mission-text.test.ts`\n  ❯ src/orchestrator/agent-mission-text.test.ts (14 tests | 1 failed) 12ms\n  AssertionError: expected 'You are a moe-next PLANNING agent. Yo…' to contain 'Plan the COMPLETE GRAPH as a dependen…'\n  Tests  1 failed | 13 passed (14)\nAFTER:\n  Test Files  1 passed (1)\n       Tests  14 passed (14)\n\nRESIDUAL SWEEP at this tree: `git grep -n 'SMALLEST COMPLETE SLICE\\|exactly ONE node' -- '*.ts' '*.tsx' '*.md'` = TWO hits, and both are the negative pins themselves (agent-mission-text.test.ts:47-48). No producer, no doc, no other asserter anywhere in the tree still says it.",
      "startedAt": "2026-09-04T21:15:00.158Z",
      "completedAt": "2026-09-04T21:16:15.252Z"
    },
    {
      "stepId": "step-6",
      "description": "CORRECT THE SOURCE-TEXT ASSERTER. apps/daemon/src/goals/goal-close-readiness.test.ts:18 carries a prose comment stating `runSubmitDecomposition` refuses `nodes.length > 1` as a live premise; it is the third of the three grep hits from step 1 and would otherwise outlive the rule it describes. Rewrite ONLY that comment to say the compiler admits N nodes and that a criterion left unbound by every node still refuses COMPILED_PLAN_CRITERION_UNBOUND — which is the part of the premise that actually still holds and that the file's arms depend on. Do not change any assertion in that file: its arms reach a partial-coverage state via a SECOND contract with distinct criterion ids, which is unaffected by the node count. THIS FILE IS DIRTY WITH PEER WIP (confirmed `M` at plan time). Never revert, stash or `git add -A` it. Edit only the comment lines, and in the step note quote `git diff -- apps/daemon/src/goals/goal-close-readiness.test.ts` to show your change alongside the peer's, so the commit's attribution is honest (global rail 5).",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/goals/goal-close-readiness.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/goals/goal-close-readiness.test.ts"
      ],
      "note": "STEP 6 SOURCE-TEXT ASSERTER CORRECTED. Comment lines only; zero assertions touched; peer WIP left strictly alone. 17/17 green.\n\nPEER WIP MEASURED BEFORE EDITING (global rail 5). `git diff --stat` before my edit: `1 file changed, 15 insertions(+), 2 deletions(-)`. The peer's hunks are at @@ -104 (widening `view()`'s totals with an optional `unattributable`) and @@ -283 (a new arm \"never counts an UNATTRIBUTABLE criterion as verified\"). Both are far from the header docblock at :17-24. NO OVERLAP — I did not revert, stash, `git add -A` or otherwise touch a byte of theirs. After my edit: `1 file changed, 25 insertions(+), 9 deletions(-)` — the delta 10+/7- is exactly my comment rewrite.\n\nMY HUNK, verbatim from `git diff -- apps/daemon/src/goals/goal-close-readiness.test.ts`:\n  @@ -15,13 +15,16 @@\n    * THE COMPILER FENCES THE REACHABLE STATES, and they are not the ones a naive plan assumes:\n  - * `runSubmitDecomposition` refuses `nodes.length > 1` (SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL —\n  - * an INITIAL run seals exactly one node) and refuses a node that leaves any contract criterion\n  - * unbound (COMPILED_PLAN_CRITERION_UNBOUND). So a single contract can never sit at \"some\n  - * criteria verified\": its one node carries all of them and one acceptance verifies all of them.\n  - * A strict 0 < verified < criteria is reachable — and is asserted below — only with a SECOND\n  - * contract citing the same document whose criterion ids are DISTINCT, which is exactly the\n  - * \"another Product Contract is still pending Gate 1\" state a real product sits in.\n  + * `runSubmitDecomposition` admits N nodes — an INITIAL run seals the WHOLE graph — but the\n  + * compiled-plan producer still refuses a graph that leaves any contract criterion bound by no\n  + * node (COMPILED_PLAN_CRITERION_UNBOUND). COVERAGE, not node count, is the fence, so a contract\n  + * can never be PLANNED into partial coverage: whatever its node count, every one of its approved\n  + * criteria is bound somewhere in the sealed graph. The strict 0 < verified < criteria asserted\n  + * below is reached the way a real product reaches it — with a SECOND contract citing the same\n  + * document whose criterion ids are DISTINCT, which is exactly the \"another Product Contract is\n  + * still pending Gate 1\" state. (A multi-node graph can of course sit part-verified mid-build,\n  + * as its nodes are accepted one at a time; that is a different state from the one these arms\n  + * pin, and no arm here depends on it being unreachable.)\n\nWHY THE LAST PARENTHETICAL EXISTS. The old comment's load-bearing claim was \"its ONE node carries all of them and one acceptance verifies all of them\" — that inference dies with the fence. Swapping \"one node\" for \"N nodes\" while keeping the impossibility claim would have left a NEW false premise in place of the old one: with N nodes, a single contract CAN sit part-verified mid-build as nodes are accepted one at a time. I stated that plainly and recorded that no arm here depends on it, rather than quietly narrowing the sentence. What survives untouched is the part the file's arms actually rest on: coverage is still enforced (COMPILED_PLAN_CRITERION_UNBOUND), so a contract cannot be PLANNED into partial coverage, and the strict 0 < verified < criteria arms still reach their state through a SECOND contract with distinct criterion ids — which the node count never affected.\n\nVERIFIED: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src/goals/goal-close-readiness.test.ts`\n  Test Files  1 passed (1)\n       Tests  17 passed (17)\n17 = the file's 16 original arms plus the peer's new one; the peer's in-flight arm is green alongside mine.\n\nDoD 1 NOW FULLY SATISFIED at this tree, both greps:\n  `git grep -c SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL -- 'apps/**/*.ts' 'packages/**/*.ts' 'adapters/**/*.ts'` -> rc=1, ZERO hits\n  `grep -rn --include='*.ts' -l SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL apps packages adapters` (untracked-inclusive) -> rc=1, ZERO files\nThe code is gone from the entire source tree — production and prose alike. It survives only in `.moe/` board records and this row's notes, which is history, not a premise.",
      "startedAt": "2026-09-04T21:16:18.484Z",
      "completedAt": "2026-09-04T21:17:38.927Z"
    },
    {
      "stepId": "step-7",
      "description": "MUTATION DRILL, then GATES, then the adversarial read. (a) DRILL, recorded in a step note: re-insert `if (nodes.length > 1) return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\");` into compile-dispatcher.ts, confirm the step-2 three-node sealing arm goes RED (quote the failing assertion), restore, and prove the restore with `git diff -- apps/daemon/src/planning/compile-dispatcher.ts` showing empty — valid here because that file is TRACKED and was clean at step 1. Do the same for the mission pin: revert the sentence, confirm the step-5 `not.toContain(\"exactly ONE node\")` arm goes red, restore, prove empty diff. (b) GATES, each a standalone foreground command with its `Test Files N passed` count line quoted verbatim — an exit-0 leg with no count line is UNVERIFIED and may not be reported as a pass: `pnpm --filter @moe/daemon test`, `pnpm --filter @moe/core test`, `pnpm typecheck`, `pnpm test`. State in the note that `pnpm test` runs ZERO tests under apps/** so the daemon leg is what covers this row. Attribute any red by the merge-base/HEAD delta intersected with this row's five files (global rail 3) and quote foreign red verbatim rather than hiding it. The named verification command for completion is `pnpm --filter @moe/daemon test`. (c) ADVERSARIAL READ, in the note: does anything downstream assume one node per sealed graph — check the compiled-node-source and coverage readers before claiming none; can a dependsOn CYCLE now reach finalize, or does compiled-authority-bodies refuse it first (if a cycle is admissible, say so plainly — the dependency gate in task-1d7b9df2 would deadlock on it); and does the new mission sentence still fit the seat's context budget.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.ts",
        "apps/daemon/src/orchestrator/agent-mission-text.ts",
        "apps/daemon/src/planning/compile-dispatcher.test.ts",
        "apps/daemon/src/orchestrator/agent-mission-text.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/planning/compile-dispatcher.test.ts"
      ],
      "note": "STEP 7 DRILLS, GATES, ADVERSARIAL READ. Named verification `pnpm --filter @moe/daemon test` EXIT 0, `Test Files 412 passed (412)` / `Tests 8198 passed (8198)`.\n\n(a) DRILLS — 3 run, all restored and PROVEN restored.\nRESTORE METHOD CORRECTED FROM THE PLAN. Step 7 says to prove restore with an EMPTY `git diff -- compile-dispatcher.ts`. That premise died at step 4: I MODIFIED that file, so its `git diff` is legitimately non-empty and an \"empty diff\" could never be reached. Using it would have proven nothing either way. Instead: out-of-tree backups in %TEMP%/moe-drill-75411639 (worker-suffixed), SHA256 taken BEFORE drilling, and every restore proven by SHA256 in a call SEPARATE from the `cp` (global rail 13's method, applied to a tracked-but-dirty file — \"the command returned 0\" is not evidence bytes moved).\n  pre-drill  compile-dispatcher.ts  fe229b9b1aa50a7bcc008a281f166621682c35e9a671628a085e142c686f8da4\n  pre-drill  agent-mission-text.ts  7a4c7d2f8a5fc01ba5214f1ece9b564a7bca6c31e8aee6bd07cc4197d0543f57\nBoth hashes re-measured identical after EVERY restore (4 separate proof calls across the drills and the two attribution experiments), plus `grep -c 'nodes.length > 1|exactly ONE node|Prefer exactly'` = 0 on both files.\n\nDRILL 1, the fence. Re-inserted `if (nodes.length > 1) return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\");`. TWO arms went red, both quoted:\n  Error: three-node dispatch refused: SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL @ COMPILE_DISPATCHER\n  AssertionError: Expected: \"COMPILED_PLAN_MALFORMED @ COMPILED_PLAN_PRODUCER\"\n                  Received: \"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL @ COMPILE_DISPATCHER\"\n  Tests  2 failed | 8 passed (10)\nRestored -> 11 passed (11).\n\nDRILL 2, the mission sentence. Reverted the whole block to the old \"SMALLEST COMPLETE SLICE\" text. The arm went red — but on the POSITIVE pin, which MASKED the negative pin (vitest stops the arm at the first failure). That is exactly the vacuity trap, so I ran DRILL 2b to grade the negative pin ON ITS OWN: kept the new sentence and appended \"Prefer exactly ONE node when you can.\". Result — the positive pins stayed green and only the guard fired:\n  AssertionError: expected 'You are a moe-next PLANNING agent. Yo…' not to contain 'exactly ONE node'\n   ❯ src/orchestrator/agent-mission-text.test.ts:47:22\n  Tests  1 failed | 13 passed (14)\nSo `not.toContain(\"exactly ONE node\")` is independently load-bearing, not a passenger. Restored -> 14 passed (14).\n\n(b) GATES — four legs, each a standalone foreground command, each count line quoted. An exit-0 leg with no count line would be UNVERIFIED; all four printed one.\n  1. `pnpm --filter @moe/daemon test`  EXIT 0  ->  Test Files  412 passed (412) / Tests  8198 passed (8198)   [the named verification command; run fresh, last]\n  2. `pnpm --filter @moe/core test`    EXIT 0  ->  Test Files   65 passed (65)  / Tests  1628 passed (1628)\n  3. `pnpm typecheck`                  EXIT 0  ->  every package \"Done\", incl. `apps/daemon typecheck: Done`\n  4. `pnpm test`                       EXIT 1  ->  Test Files  3 failed | 421 passed | 2 skipped (426) / Tests  5 failed | 10266 passed | 46 skipped (10317)\nGLOBAL RAIL 18, STATED EXPLICITLY: `pnpm test` runs ZERO tests under apps/**, so it does NOT cover a single byte of this row. Leg 1 is what covers this row.\nNOTE on the package script: its trailing `src` positional means `pnpm --filter @moe/daemon test -- <path>` does NOT scope — it silently runs all 411 files. Targeted runs used `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 <path>`.\n\nFOREIGN RED, DISCLOSED VERBATIM AND ATTRIBUTED BY THE (2)-MINUS-(1) DELTA (global rail 3). Never hidden, never fabricated green.\n  `pnpm test` leg, 5 failures in 3 files, none in my owned paths:\n   - tests/integration/control-room/compiled-product-journey.test.ts\n       AssertionError: expected [ 'repository.publish', …(9) ] to include 'goal.close'   (:331)\n     MEASURED BOTH WAYS: I reverted my two production files to merge-base and re-ran this exact file — BYTE-IDENTICAL failure. Pre-existing; it belongs to the in-flight goal.close offer-gate row (see wip commit e71ae3fe task-8145137c).\n   - tests/integration/release/release-version-surfaces.test.ts (2)\n       AssertionError: unclassified tracked current-version occurrence: expected [ …(5) ] to deeply equal []\n       + [ \"apps/daemon/src/bootstrap/activation-receipts.test.ts4852v0.1\", \"…/activation-receipts.ts2100v0.1\",\n           \"…/activation-receipts.ts2368v0.1\", \"apps/daemon/src/http/activation-read.test.ts17097v0.1\",\n           \"…/activation-read.ts3054v0.1\" ]\n     All five flagged paths are foreign (peer WIP under bootstrap/ and http/). My diff adds no version string and touches no release manifest.\n   - tools/packaging/pack-source.test.ts — 22 failed in one run, 0 in the next, 293s. Machine flake (below).\n  `pnpm --filter @moe/daemon test` leg: `src/work/foundation-attempt-windows.test.ts` was red ",
      "startedAt": "2026-09-04T21:17:44.382Z",
      "completedAt": "2026-09-04T22:07:33.873Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": "task-4423d2a4c3084d579c132b253d36b502",
  "priority": "HIGH",
  "order": 2,
  "comments": [
    {
      "id": "comment-96b34ba3c8c54456903f60af1d0ea30f",
      "author": "worker-75411639",
      "content": "ADVERSARIAL READ FINDING (step 7c) — A DOWNSTREAM SINGLE-NODE ASSUMPTION THE LIFT NOW EXPOSES. Recorded here, not filed as a row (global rails 16 and 20: it belongs to a consumer row, not to this leaf, and this row's own bytes are working and green).\n\nWHAT I FOUND. `apps/daemon/src/activation/activation-budget-derivation.ts` refuses EVERY activation against a multi-node ACTIVE graph:\n  :178-181  const bearingKeys = graph.snapshot.nodes.filter((node) => node.executionBearing).map(...)\n            const resolved = resolveNodeKey(bearingKeys, nodeKey);\n  :125      if (bearingKeys.length > 1) return refuse(\"ACTIVATION_BUDGET_NODE_AMBIGUOUS\");\n`bearingKeys` is read off the CURRENT ACTIVE GRAPH, and compile-dispatcher.ts:319-321 stamps `executionBearing: true` on EVERY sealed node. So the moment a 2+-node graph this row now seals reaches activation, both of `runEffectActivateCommand`'s doors (foundation.dispatch at foundation-attempt-service.ts:218, and the registered `effect.activate` at daemon-command-registry.ts:202) refuse ACTIVATION_BUDGET_NODE_AMBIGUOUS. Its own docblock (:24-27) calls this \"THE BEARING RULE IS UNCONDITIONAL\" and deliberately declines core's EXPANSION exemption, so it will not fall away on its own.\n\nWHY THIS ROW DID NOT FIX IT. Out of scope by the plan's five owned paths and by task rail 1's spirit; a second row already owns the staffing side of a multi-node graph (task-1d7b9df2, the dependency gate, which declares dependsOn on this row). Fixing it here would mean editing activation + budget authority with no test surface in this plan — the \"one row quietly grows into three\" failure. This row's deliverable (the compiler admits N nodes, the DAG fence holds, the mission asks for the graph) is complete and green either way: a single-node plan activates exactly as before, so nothing regressed.\n\nWHO NEEDS IT. Whoever owns the first task that ACTIVATES a multi-node graph — most likely task-1d7b9df2 or a sibling of it. The fix is not a one-liner: `resolveNodeKey` returns THE node key and the whole budget derivation is written around a single node, so admitting N means deciding WHICH node an activation spends for (almost certainly the one the caller's `stated` argument names, promoting DOOR 1's cross-check into the selector, with DOOR 2 needing its own answer). That is a design decision, not a mechanical edit — hence a comment for the architect rather than a patch from me.\n\nTWO MORE ANSWERS FROM THE SAME READ, both good news:\n- A dependsOn CYCLE CANNOT SEAL. Measured, not reasoned: a 3-node graph with a<->b (every target known, no self-edge, nothing depending on the completion node, every criterion bound once — so it clears every arm in `shapeRefusal`) is refused COMPILED_PLAN_ADMISSION_REFUSED @ COMPILED_PLAN_PRODUCER, deeper in the graph codec. This is now a committed arm in compile-dispatcher.test.ts, so task-1d7b9df2's dependency gate CANNOT deadlock on a sealed cycle — no node would ever be READY, and that state is unreachable. (Note `v2-compiler/topology-cycle.ts` exists but is NOT on this path — it is imported only by v2-compiler/topology.ts.)\n- MISSION CONTEXT BUDGET is fine: the replaced sentence went 201 -> 326 chars (31 -> 56 words), +125 chars on a ~2500-char brief, about +5%.",
      "timestamp": "2026-09-04T22:06:14.940Z"
    },
    {
      "id": "comment-f8f86c8580ad4101816a875800e93ccf",
      "author": "qa-3fd61a21",
      "content": "QA EVIDENCE (qa-3fd61a21, 2026-09-05) — full detail behind the approval; summary is capped at 2000.\n\nANCHOR: delivered tree = commit a879421d17c38b16dd31a8ee528094770ec6087c, and all 5 owned paths measured `git status --porcelain` EMPTY, so worktree == HEAD == delivered bytes.\n\nGATES I RAN MYSELF (not read off the record):\n  1. `pnpm --filter @moe/daemon test`  EXIT 0  -> Test Files 412 passed (412) / Tests 8205 passed (8205)   [named verification command]\n  2. `pnpm --filter @moe/core test`    EXIT 0  -> Test Files  65 passed (65)  / Tests 1628 passed (1628)   [matches worker's figures exactly]\n  3. `pnpm typecheck`                  EXIT 0  -> `apps/daemon typecheck$ tsc --project tsconfig.json` then `apps/daemon typecheck: Done` (the `$ tsc` echo is the execution evidence)\n  4. SCOPED POSITIVE CONTROL on this row's 3 test files -> Test Files 3 passed (3) / Tests 42 passed (42), EXIT 0. Recipe: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 <paths>` (the package script's trailing `src` positional means `test -- <path>` does NOT scope).\n  `pnpm test` not re-run: global rail 18 — it covers ZERO apps/**, so it cannot certify a byte of this row. Leg 1 is what covers it.\n\nFOREIGN RED, RAISED THEN CLEARED. My FIRST daemon run was EXIT 1: `Test Files 1 failed | 411 passed (412)` / `Tests 2 failed | 8203 passed (8205)`, both in `src/orchestrator/agent-provider-pause.test.ts`:\n    AssertionError: expected { kind: 'policy.install', ...(4) } to match object { outcome: 'SPAWNED' }\n    - \"outcome\": \"SPAWNED\"  +  \"outcome\": \"STAFFING_ATTEMPTS_EXHAUSTED\"\nATTRIBUTED FOUR WAYS, none of which is \"it passed in isolation\" (mem:gotcha-spawn-flake-needs-a-merge-base-full-run-not-an-isolated-rerun says that control settles nothing):\n  (a) PATH — `git status` says `??` on agent-provider-pause.{ts,test.ts,js}: untracked peer WIP, absent from merge-base AND from HEAD. Not an owned path, so the rail-3 (HEAD-minus-merge-base) intersection with owned paths is EMPTY by construction.\n  (b) CLOCK — mtimes 22:15:08 / 22:15:38 UTC, plus agent-wrapper.ts 22:13:38 and agent-wrapper-staffing.ts 22:13:15. The worker's verification is stamped 22:08:02. Every one of those edits POST-DATES the verified run; the shared checkout moved under the row.\n  (c) NO CAUSAL PATH — that test hard-codes `mission: \"dispatch it\"` (:343); it never calls `compilerMission` and never imports compile-dispatcher. The only edge to this row was transitive (agent-wrapper.ts -> agent-mission-text.ts) and it is not exercised.\n  (d) RE-RUN FRESH -> 412 passed (412) / 8205 passed (8205) EXIT 0. Machine is at mtkbtsvc = 4,793,997 handles, above the 4.48M already recorded as making shell-out legs inadmissible.\n8205 vs the worker's 8198 = the 7 tests the peer added after 22:08.\n\nDRILLS RE-RUN BY ME. Pre-drill SHA256s matched the worker's recorded hashes EXACTLY — compile-dispatcher.ts FE229B9B1AA50A7BCC008A281F166621682C35E9A671628A085E142C686F8DA4, agent-mission-text.ts 7A4C7D2F8A5FC01BA5214F1ECE9B564A7BCA6C31E8AEE6BD07CC4197D0543F57 — which independently proves their restore actually moved bytes.\n  DRILL 1: re-inserted `if (nodes.length > 1) return refused(\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\")` -> Tests 3 failed | 8 passed (11). The sealing arm threw `three-node dispatch refused: SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL @ COMPILE_DISPATCHER`, and BOTH layer-pinning arms failed showing the wrong layer answered where `@ COMPILED_PLAN_PRODUCER` was expected. That is global rail 1 doing exactly its job — a bare \"it refused\" arm would have stayed green.\n  DRILL 2: appended \"Prefer exactly ONE node when unsure.\" to the mission block, POSITIVE PINS LEFT INTACT -> Tests 1 failed | 13 passed (14), only `not.toContain('exactly ONE node')` fired. The negative pin is independently load-bearing, not masked by the positive pins.\n  RESTORE PROOF for both: SHA256 re-measured in a call SEPARATE from the copy and equal to pre-drill, plus empty `git diff`, empty `git status --porcelain` across all 5 owned paths, and grep count 0 for both mutant strings. No mutant rode into the tree (mem:gotcha-peer-drill-mutant-rides-a-pathspec-commit-into-history).\n\nDoD 1 GREP, WIDER THAN THE DoD ASKED: `grep -rn SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.moe --exclude-dir=dist` = 5 hits, ALL of them `.serena/memories/*.md` agent notes plus one serena cache blob. ZERO in source, tests, tools, scripts or docs.\nROSTER: `SUBMIT_DECOMPOSITION_CODES` has exactly ONE hit tree-wide, its own declaration — no external scanner, no `.length` pin, so retiring an entry breaks no counter (global rail 9 has nothing to bite on here).\nTASK RAIL 1 HONOURED: `git show --stat a879421d -- packages/` is EMPTY. Core's `MULTI_NODE_EXECUTION_UNSUPPORTED` survives at planning-event-contract.ts:171 and planning-invariants.test.ts:32 still asserts it is not driven.\n\nSIZE, NOT A VIOLATION: compile-dispatcher.ts is 337 lines (it only lost code). compile-dispatcher.test.ts at 437 is NOT a breach — AGENTS.md:73 caps \"production source\", and 160 daemon test files already exceed 400 (largest 3545). The worker's self-disclosure was over-cautious; no split required, and epic rail 2 forbids task-level LOC as a reject reason.\n\nCOMMIT SHAPE: a879421d is `Moe-Kind: checkpoint`, not a completion commit, so a NO-COMPLETION-COMMIT warning is expected. Investigated per global rail 5 rather than auto-rejected: it carries all 5 owned paths with Moe-Task trailers, sits on moe/work-2026-09-04, and HEAD == worktree for every owned path. The peer's `unattributable` hunks in goal-close-readiness.test.ts rode along and are disclosed by the wrapper's own `Moe-Contested:` trailers — that is the attribution mechanism working, not worker misconduct. The worker edited only the comment lines it owned.\n\nCARRIED FORWARD, NOT A DoD GAP — see comment-96b34ba3: `activation/activation-budget-derivation.ts:125` refuses `ACTIVATION_BUDGET_NODE_AMBIGUOUS` when `bearingKeys.length > 1`, and compile-dispatcher stamps `executionBearing: true` on every sealed node. So as of this row a multi-node graph SEALS but cannot ACTIVATE. Fail-closed with a stable code, no data loss, and out of this row's scope (the mission now asks seats for complete graphs, so whichever row first ACTIVATES one must close this). Owner: task-1d7b9df2.",
      "timestamp": "2026-09-04T22:25:47.126Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-04T20:21:53.659Z",
  "updatedAt": "2026-09-04T22:28:59.099Z",
  "metrics": {
    "firstClaimAt": "2026-09-04T20:32:01.441Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 7,
    "doneAt": "2026-09-04T22:27:23.668Z",
    "wallClockMs": 6922227
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-04T20:38:55.857Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED, restore the core fence to satisfy the parent row's literal DoD 5: the core arm was removed by 63efd3a3 and `planning-invariants.test.ts:32` now ASSERTS `MULTI_NODE_EXECUTION_UNSUPPORTED` is not driven — restoring it would red that test and reverse an owner ruling. REJECTED, keep the code and narrow it to REVISION runs only: two sibling rows' rails asked for exactly that, but the owner's ruling is about INITIAL runs sealing the whole graph, and a count fence on REVISION would block the replan path from proposing a corrected multi-node graph — the same defect under another name. REJECTED, gating this row on task-138fab30 (which also owns compile-dispatcher.ts): the parent's rail forbids children depending on sibling feature rows, and the user chose instead to amend those rows — done this session (138fab30 step-4 amend-1, plus superseding comments on both). REJECTED, folding the dependency gate into this row: without it a multi-node graph staffs dependent nodes beside their parents, which is why task-1d7b9df2 exists and declares dependsOn on this row; keeping them separate keeps each under the file cap and makes the gate independently reviewable. REJECTED, writing the DAG-coherence arms as \"expect it to refuse\": several layers can refuse a malformed structure, so step 3 pins code AND layer per global rail 1.",
    "codebaseInsights": "ALL MEASURED 2026-09-04 on branch moe/work-2026-09-04. THE RULING IS CORRECT ON DISK: `packages/core/src/planning/planning-run-submission.ts` `finalize` refuses only `executionBearingKeys(witness).length === 0` — no runKind or node-count arm. `git grep -n SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL -- '*.ts'` = EXACTLY 3 hits, all in this plan's affectedFiles: compile-dispatcher.ts:51 (roster), compile-dispatcher.ts:197 (refusal), goal-close-readiness.test.ts:18 (prose comment, and that file is DIRTY with peer WIP). ROSTER IS CLEAN: `git grep -n SUBMIT_DECOMPOSITION_CODES` = ONE hit, its own declaration — no external scanner, no `.length` pin, unlike POLICY_RISK_READER_CODES/POLICY_WAIVER_READER_CODES which are count-pinned in their tests. So retiring an entry breaks no counter. MISSION PIN IS SINGULAR: `git grep -n 'SMALLEST COMPLETE SLICE|exactly ONE node'` = the source at agent-mission-text.ts:123 plus agent-mission-text.test.ts:37-38, nothing else. The sentence spans array elements :122-127, so it must be rewritten as a block, not sed'd by line. DOWNSTREAM IS ALREADY MULTI-NODE: compile-dispatcher.ts:209-228 already reads, validates and seals per-node `dependsOn`; compiled-authority-bodies.ts:62-70 already refuses an unknown dependency, a self-edge and a dependency on the completion node (COMPILED_PLAN_MALFORMED), and :84-92 refuses an unbound criterion (COMPILED_PLAN_CRITERION_UNBOUND). Those refusals are the fence that REPLACES the count fence, which is why step 3 pins them. TEST HELPER: `structureOf()` at compile-dispatcher.test.ts:160 takes no arguments and builds one node; it must be generalised without disturbing the INITIAL arms at :194-292. SIZES: compile-dispatcher.ts 341 lines (cap 400; this row only deletes), compile-dispatcher.test.ts 292, agent-mission-text.ts 172. CROSS-ROW CONFLICT RESOLVED AT PLAN TIME: task-138fab3075884df98c3c220e74c1a3e9 and task-957bccf162b446f3a4db46b56e8e8f3a both carried a task rail pinning the fence, and 138fab30's step-4 wrote an arm asserting it still refuses. Both are WORKING but unassigned with 0 commits and all steps PENDING. On the user's decision I amended 138fab30 step-4 (amend-1) to drop that arm and commented on both rows that their rail is superseded. Both also own compile-dispatcher.ts — disjoint regions (they change run-state branching at :184-300; this row deletes :51 and :191-197) — and the amended step tells that worker not to restore the code if it is already gone.",
    "risks": "BIGGEST RISK — lifting a count fence without the coherence fence being real. If compiled-authority-bodies' dependsOn checks were weaker than measured, this row would start sealing graphs that finalize or the scheduler cannot execute. Step 3 pins those refusals BEFORE step 4 deletes anything, and step 1 tells the worker to report blocked rather than proceed if core has regained a node-count arm. SECOND — a dependsOn CYCLE. compiled-authority-bodies refuses self-edges and unknown targets, but the plan-time measurement did not establish that a longer cycle (a->b->a) is refused. Step 7(c) makes the worker answer this explicitly, because task-1d7b9df2's dependency gate would deadlock a cycle at the surface rather than refuse it at compile. THIRD — file collision with task-138fab30 and task-957bccf1, both of which own compile-dispatcher.ts. Regions are disjoint and I amended 138fab30's conflicting step, but whichever lands second will rebase onto the other; the amended step and this plan both say the code's absence is expected and must not be restored. FOURTH — goal-close-readiness.test.ts is DIRTY with peer WIP. Step 6 edits one comment inside a file someone else is mid-change on: never revert, stash or `git add -A`, and quote the full `git diff` so attribution is honest. Its `git diff` is a valid restore proof only because it is tracked. FIFTH — the drill in step 7(a) mutates a TRACKED, clean file, so an empty `git diff` is sound proof; do not carry that shortcut to any untracked path (global rail 13). SIXTH — the mission text is read by live seats: a longer sentence costs seat context, and a seat that misreads it will submit a structure the compiler refuses. Step 7(c) asks the worker to sanity-check the length.",
    "keyFiles": [
      "apps/daemon/src/planning/compile-dispatcher.ts",
      "apps/daemon/src/planning/compile-dispatcher.test.ts",
      "apps/daemon/src/orchestrator/agent-mission-text.ts",
      "apps/daemon/src/orchestrator/agent-mission-text.test.ts",
      "apps/daemon/src/goals/goal-close-readiness.test.ts",
      "apps/daemon/src/planning/compiled-authority-bodies.ts",
      "packages/core/src/planning/planning-run-submission.ts"
    ]
  },
  "planApprovedAt": "2026-09-04T20:38:58.038Z",
  "contextFetchedBy": [
    "worker-75411639",
    "qa-3fd61a21"
  ],
  "workStartedAt": "2026-09-04T21:03:50.713Z",
  "reviewStartedAt": "2026-09-04T22:08:02.064Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  412 passed (412)\n      Tests  8198 passed (8198)\n   Start at  01:03:41\n   Duration  379.87s (transform 24.56s, setup 0ms, import 257.39s, tests 1184.01s, environment 24ms)\nEXIT=0",
    "reportedAt": "2026-09-04T22:08:02.064Z"
  },
  "filesModified": [
    "apps/daemon/src/planning/compile-dispatcher.ts",
    "apps/daemon/src/goals/goal-close-readiness.test.ts",
    "packages/core/src/planning/planning-run-submission.ts",
    "apps/daemon/src/planning/compile-dispatcher.test.ts",
    "apps/daemon/src/orchestrator/agent-mission-text.ts",
    "apps/daemon/src/orchestrator/agent-mission-text.test.ts",
    ".moe/tasks/task-7945f38da29249a291004f78b4024262.json",
    ".moe/tasks/task-f3edb8fb39ad42ce8980dae7d62b604c.json",
    ".moe/tasks/task-f79d38b119984ae1ae51eba887a28b7d.json",
    ".moe/tasks/task-278faf81c10845ffa571716105aa19e3.json"
  ],
  "completionSummary": "THE COMPILER SEALS THE WHOLE DAG. The one-node fence is gone; N>=1 execution-bearing nodes now seal, and the coherence fence that replaces it is pinned by code AND layer.\n\nDoD 1 — `nodes.length > 1` arm DELETED from compile-dispatcher.ts along with the 5-line comment above it that asserted the retired core rule as live fact; `\"SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL\"` REMOVED from SUBMIT_DECOMPOSITION_CODES. `git grep -c SUBMIT_DECOMPOSITION_MULTI_NODE_INITIAL -- 'apps/**/*.ts' 'packages/**/*.ts' 'adapters/**/*.ts'` = ZERO hits, and an untracked-inclusive `grep -rn` = ZERO files. The goal-close-readiness.test.ts prose comment was REWRITTEN, not left stating the retired rule. File shrank 341 -> 337; core untouched (task rail 1).\n\nDoD 2 — a THREE-node structure with a real hard chain (node-c dependsOn node-b dependsOn node-a, completionNodeKey node-c because nothing may depend on the completion node) SEALS: asserts disposition DECIDED, graphContentHash AND submissionHash both non-empty strings, runId, and aggregate version 2 — not merely \"did not refuse\". Second arm: N=1 still seals.\n\nDoD 3 — the replacement fence pinned, each arm asserting the SPECIFIC code AND the layer via a `${code} @ ${layer}` comparison (a bare \"it refused\" would stay green if another layer answered — which is precisely what the count fence was doing). Unknown dependsOn target and self-edge -> COMPILED_PLAN_MALFORMED @ COMPILED_PLAN_PRODUCER; criterion bound by no node -> COMPILED_PLAN_CRITERION_UNBOUND @ COMPILED_PLAN_PRODUCER. Written BOTH single-node (green before my edit, proving the fence stands alone) and across the 3-node chain (red before, green after — direct evidence multi-node incoherence is now graded by the producer instead of hidden behind the count fence), plus dependency-on-completion-node. BONUS ARM: a dependsOn CYCLE (a<->b) clears every `shapeRefusal` arm and is still refused COMPILED_PLAN_ADMISSION_REFUSED @ COMPILED_PLAN_PRODUCER — so task-1d7b9df2's dependency gate cannot d",
  "commits": [
    {
      "sha": "a879421d17c38b16dd31a8ee528094770ec6087c",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "qa",
      "sessionId": "qa-3fd61a21@2026-09-04T22:08:25Z",
      "paths": [
        ".moe/tasks/task-7945f38da29249a291004f78b4024262.json",
        ".moe/tasks/task-f3edb8fb39ad42ce8980dae7d62b604c.json",
        ".moe/tasks/task-f79d38b119984ae1ae51eba887a28b7d.json",
        "apps/daemon/src/goals/goal-close-readiness.test.ts",
        "apps/daemon/src/orchestrator/agent-mission-text.test.ts",
        "apps/daemon/src/orchestrator/agent-mission-text.ts",
        "apps/daemon/src/planning/compile-dispatcher.test.ts",
        "apps/daemon/src/planning/compile-dispatcher.ts"
      ],
      "recordedBy": "qa-3fd61a21",
      "recordedAt": "2026-09-04T22:08:30.042Z",
      "treeId": "bbe41c741efdb2df077d281848568ea42a7c1514",
      "status": "REVIEW",
      "contested": [
        {
          "path": "apps/daemon/src/goals/goal-close-readiness.test.ts",
          "taskId": "task-8145137ca3c144f0878deb83d05bab63"
        },
        {
          "path": "apps/daemon/src/planning/compile-dispatcher.test.ts",
          "taskId": "task-138fab3075884df98c3c220e74c1a3e9"
        },
        {
          "path": "apps/daemon/src/planning/compile-dispatcher.ts",
          "taskId": "task-138fab3075884df98c3c220e74c1a3e9"
        }
      ],
      "pushed": true
    },
    {
      "sha": "2cd15426b822a472ce08e52888dd7c92d5c3428a",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "completion",
      "role": "worker",
      "sessionId": "worker-75411639@2026-09-04T21:03:34Z",
      "paths": [
        ".moe/tasks/task-f79d38b119984ae1ae51eba887a28b7d.json"
      ],
      "recordedBy": "worker-75411639",
      "recordedAt": "2026-09-04T22:10:10.193Z",
      "treeId": "f492c62e8eaea3a27e502cfba11a0ba5c9e746b7",
      "status": "REVIEW",
      "pushed": true
    },
    {
      "sha": "1cf959a293bd9db81c25d9882892abe24adbcdbd",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "qa",
      "sessionId": "qa-3fd61a21@2026-09-04T22:08:25Z",
      "paths": [
        ".moe/tasks/task-278faf81c10845ffa571716105aa19e3.json",
        ".moe/tasks/task-f79d38b119984ae1ae51eba887a28b7d.json"
      ],
      "recordedBy": "qa-3fd61a21",
      "recordedAt": "2026-09-04T22:28:59.099Z",
      "treeId": "b6bdb964c624b8a2cee58ee4046dccf790377ea7",
      "status": "DONE",
      "pushed": true
    }
  ],
  "unattributedPaths": [],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "qa-3fd61a21@2026-09-04T22:08:25Z",
    "at": "2026-09-04T22:28:59.099Z"
  },
  "touchedFiles": [
    "apps/daemon/src/planning/compile-dispatcher.test.ts",
    "apps/daemon/src/planning/compile-dispatcher.ts",
    "apps/daemon/src/goals/goal-close-readiness.test.ts",
    "apps/daemon/src/orchestrator/agent-mission-text.test.ts",
    "apps/daemon/src/orchestrator/agent-mission-text.ts"
  ],
  "completedAt": "2026-09-04T22:27:23.668Z",
  "reviewCompletedAt": "2026-09-04T22:27:23.668Z",
  "reviewSummary": "APPROVED. 6/6 DoD verified at the delivered tree (a879421d; all 5 owned paths `git status` EMPTY = worktree == HEAD). Evidence: comment-f8f86c85.\n\nGATES I RAN: `pnpm --filter @moe/daemon test` EXIT 0 -> Test Files 412 passed (412) / Tests 8205 passed (8205). `pnpm --filter @moe/core test` EXIT 0 -> 65 (65) / 1628 (1628). `pnpm typecheck` EXIT 0 -> `apps/daemon typecheck: Done`. Scoped control on this row's 3 files -> 3 (3) / 42 (42). `pnpm test` skipped: rail 18, zero apps/** coverage.\n\nFOREIGN RED CLEARED: first daemon run EXIT 1, 2 failures in `agent-provider-pause.test.ts` — UNTRACKED peer WIP (`??`), mtime 22:15 UTC vs worker's 22:08 verification, no causal path (hard-codes `mission: \"dispatch it\"`, never imports compile-dispatcher). Unowned, so rail-3 intersection empty. Fresh re-run 412/412, 8205/8205 EXIT 0.\n\nDRILLS RE-RUN, NOT TRUSTED: pre-drill SHA256s matched the worker's recorded hashes exactly, proving their restore moved bytes. Re-inserting `nodes.length > 1` reddened 3 arms — incl. both layer pins, reporting `@ COMPILE_DISPATCHER` where `@ COMPILED_PLAN_PRODUCER` was expected (rail 1 working; a bare \"it refused\" arm would stay green). Appending \"exactly ONE node\" with positive pins intact fired ONLY `not.toContain`, 13 green. Restores proven by SHA256 in a call SEPARATE from the copy + empty `git diff` + grep 0.\n\nDoD 1 whole-tree grep = 5 hits, ALL `.serena/memories/*.md`; zero in source/tests/tools. Roster: one hit, its own declaration. Rail 1 honoured, core untouched.\n\nSIZE OK: compile-dispatcher.ts 337. Test file 437 not a breach — AGENTS.md:73 caps production source; 160 daemon test files already exceed 400.\n\nCOMMIT kind=checkpoint, so NO-COMPLETION-COMMIT expected; investigated per rail 5 — all 5 owned paths present with Moe-Task trailers.\n\nCARRIED FORWARD (comment-96b34ba3): activation-budget-derivation.ts:125 refuses ACTIVATION_BUDGET_NODE_AMBIGUOUS on `bearingKeys.length > 1` — multi-node graphs SEAL but cannot ACTIVATE. Owner task-1d7b9df2."
}