{
  "id": "task-fd1960561d9b4ba2bbb96ae031ac4606",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[e4cd1eb6-pre-b] Admit the Claude launch-selection env keys (ANTHROPIC_MODEL, CLAUDE_CODE_EFFORT_LEVEL) at the Windows provider boundary roster",
  "description": "PREREQUISITE filed per governor-0af24e35's 16:06Z governance call on task-e4cd1eb6807244efa3334609ed746143 (R3-4): \"(2) provider launch-allowlist ownership for the MOE launch env keys (the windows-launch-request comment says the public signature cannot widen it — that row owns the module)\". This is row (2); row (1) is the discover-current configuration reader filed beside it.\n\nTHE GAP, MEASURED AT HEAD 595c32c2 (worker-57c90511's runtime probe in comment-72e983b1, source inline there; re-verified by me): packages/runner/src/platform/windows/windows-launch-request.ts:35-59 ALLOWED_ENVIRONMENT_KEYS is a frozen 24-name roster with ZERO provider-selection keys; `encodeLaunchPayload` (:163-164) hard-binds it and the module's own comment (:167-171) says the provider entry \"cannot be widened through its public signature\". The production launch env is built by launch-template-producer.ts:177-179 as `{[CLAUDE_LAUNCH_SELECTION_ENV.model]: ..., [CLAUDE_LAUNCH_SELECTION_ENV.effort]: ...}` where claude-launch-selection.ts:65-66 (SAME package, @moe/runner) defines `{model: \"ANTHROPIC_MODEL\", effort: \"CLAUDE_CODE_EFFORT_LEVEL\"}`; launchRequestBody carries it verbatim; claude-launcher.ts:329 passes `environment: request.environment` into openWindowsProcessBoundary -> windows-boundary.ts:82 encodeLaunchPayload. Probe with a positive control (PATH-only env returned bytes): the producer env refuses `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED @ WINDOWS_PROCESS_REQUEST` at ENCODE, before providerStdin is written. So every real Foundation launch dies at the provider boundary; e4cd1eb6's DoD-3 is unreachable and no row owns this module.\n\nDELIVER — a DELIBERATE, REVIEWED WIDENING of the provider roster by EXACTLY the two CLAUDE_LAUNCH_SELECTION_ENV values and nothing else. Single source: the boundary roster must derive from, or be pinned bidirectionally to, CLAUDE_LAUNCH_SELECTION_ENV so producer and boundary cannot drift — without introducing a platform/windows -> providers/claude import cycle (claude-launcher already imports the platform boundary). The outer PROJECT_STACK_ENVIRONMENT_KEYS (windows-project-stack-boundary.ts:34) spreads the inner roster, so it widens by composition; assert against the COMPOSED set. NODE_OPTIONS, the *_PROXY family, credentials and every MOE_* key stay refused at the provider boundary.\n\nCONSUMER (global rail clause 1): task-e4cd1eb6 step 5 (real-process control). Foreign WIP present in the directory: windows-broker-path.ts ` M` and windows-broker-path.test.ts `??` — preserve, never commit.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK first, recorded in a row comment with `git rev-parse HEAD` quoted, every grep unpiped with its own exit code plus a positive control: `git grep -n -E 'ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL' HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` EXIT 1 (gap open; EXIT 0 means someone landed it — adopt, verify, report duplicate); `git grep -n 'export const CLAUDE_LAUNCH_SELECTION_ENV' HEAD -- packages/runner/src/providers/claude/claude-launch-selection.ts` EXIT 0; `git grep -n 'ALLOWED_ENVIRONMENT_KEYS' HEAD -- packages/runner/src/platform/windows/windows-project-stack-boundary.ts` EXIT 0 (composition still spreads it); porcelain on windows-launch-request.ts/.test.ts, windows-project-stack-boundary.ts/.test.ts, claude-launch-selection.ts/.test.ts is CLEAN, and windows-broker-path.ts ` M` / windows-broker-path.test.ts `??` are recorded as FOREIGN and left untouched.",
    "RED-FIRST production-surface reproduction, committed as a test arm: `encodeLaunchPayload({executable, argv, cwd, environment: {[CLAUDE_LAUNCH_SELECTION_ENV.model]: 'claude-opus-4-1', [CLAUDE_LAUNCH_SELECTION_ENV.effort]: 'high'}})` with a POSITIVE CONTROL in the same arm (a PATH-only environment returns bytes, proving the environment gate is reached) refuses `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED` @ `WINDOWS_PROCESS_REQUEST` before the change and returns a Uint8Array after; the keys are spelled through CLAUDE_LAUNCH_SELECTION_ENV, never as literals in the assertion.",
    "The widening is EXACTLY the two CLAUDE_LAUNCH_SELECTION_ENV values: a bidirectional parity arm asserts every value of CLAUDE_LAUNCH_SELECTION_ENV is in ALLOWED_ENVIRONMENT_KEYS AND that ALLOWED_ENVIRONMENT_KEYS minus the pre-change 24-name roster equals exactly that value set. Roster invariants stay green (frozen, unique, uppercase, length <= MAX_ENVIRONMENT_ENTRIES = 64). NO import cycle: windows-launch-request.ts imports nothing from packages/runner/src/providers/** (grep asserted in the arm or in a step note); if derivation needs a shared constant, it lives in a platform-neutral module both sides import.",
    "Negatives preserved and asserted individually with code AND layer: NODE_OPTIONS, HTTP_PROXY, HTTPS_PROXY, ANTHROPIC_API_KEY, MOE_STORE_PATH and MOE_PROJECT_CONFIGURATION_DIGEST each still refuse `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED` @ `WINDOWS_PROCESS_REQUEST` at the provider boundary. The composed outer roster PROJECT_STACK_ENVIRONMENT_KEYS contains both selection keys by composition (asserted against the composed set in windows-project-stack-boundary.test.ts) and its existing `not.toContain(\"NODE_OPTIONS\")` and uniqueness arms stay green.",
    "Mutation drills, one degree of freedom each, restores proven by `git diff --exit-code` for the tracked clean paths and SHA256 vs a %TEMP% backup for any new file, hashed in a separate call: (D1) remove ONE selection key from the roster -> the reproduction arm reds naming that key and the parity arm reds; (D2) add NODE_OPTIONS to the roster -> the negatives arm reds; (D3) if the roster carries literals, misspell one -> the parity arm reds. A green drill is a finding, never a pass.",
    "Gates standalone, foreground, unpiped, exit 0 with nonzero counts quoted: `pnpm --filter @moe/runner typecheck` (quote the `$ tsc` echo) and `pnpm --filter @moe/runner test` with `Test Files N passed` (windows-launch-request.test.ts, windows-project-stack-boundary.test.ts, claude-launch-selection.test.ts named in the count evidence), foreign red attributed by path and disclosed verbatim. Explicit-pathspec commit of owned paths only; completion comment names task-e4cd1eb6807244efa3334609ed746143 as consumer and states verbatim: 'the provider boundary admits exactly ANTHROPIC_MODEL and CLAUDE_CODE_EFFORT_LEVEL; no other name was widened'."
  ],
  "taskRails": [
    "Deliberate widening limited to the two CLAUDE_LAUNCH_SELECTION_ENV values. NODE_OPTIONS, the *_PROXY family, credentials and MOE_* keys stay OUT of the provider roster; the outer project-stack roster is not the place to admit provider keys (it governs the host process, not the provider).",
    "Single source or bidirectional pin: the boundary's admitted selection keys derive from CLAUDE_LAUNCH_SELECTION_ENV or are pinned to it by a set-equality arm in both directions; never a hand-retyped literal without that arm.",
    "No platform -> providers import cycle: windows-launch-request.ts must not import from providers/**; claude-launcher.ts already imports the platform boundary.",
    "Do not route around the roster via encodeLaunchPayloadWithAllowedEnvironment's injected roster for the provider entry — the provider entry keeps calling encodeLaunchPayload; the roster itself is what changes, under review.",
    "Preserve foreign WIP in the same directory (windows-broker-path.ts ` M`, windows-broker-path.test.ts `??`): re-run porcelain before every edit; explicit-pathspec commits only, never `git add -A`.",
    "Per-file cap: windows-launch-request.ts is 355 lines — add only the roster entries and their comment; split before 400. Consumer linkage per global rail clause 1: task-e4cd1eb6807244efa3334609ed746143 step 5."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY-OR-BLOCK (DoD-1), read-only, ONE moe.add_comment with `git rev-parse HEAD` quoted; every grep unpiped with its own exit code plus a positive control. (a) `git grep -n -E 'ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL' HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` EXIT 1 (gap open; EXIT 0 = someone landed it: read, verify against DoD 2-4, report duplicate via moe.report_blocked). (b) `git grep -n 'export const CLAUDE_LAUNCH_SELECTION_ENV' HEAD -- packages/runner/src/providers/claude/claude-launch-selection.ts` EXIT 0 and the next line reads `{ model: \"ANTHROPIC_MODEL\", effort: \"CLAUDE_CODE_EFFORT_LEVEL\" }`. (c) `git grep -n 'ALLOWED_ENVIRONMENT_KEYS' HEAD -- packages/runner/src/platform/windows/windows-project-stack-boundary.ts` EXIT 0 (composition still spreads it). (d) Positive control: `git grep -c export HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` prints a count, EXIT 0. (e) Native broker has NO second name allowlist: `git grep -n -i -E 'SYSTEMROOT|PATHEXT|allowlist' HEAD -- native/` run UNPIPED — record its exit code; if EXIT 0, read every hit and STOP if the broker filters environment names (then this row cannot reach the provider alone — report blocked naming the file:line). (f) `git status --porcelain -- packages/runner/src/platform/windows/windows-launch-request.ts packages/runner/src/platform/windows/windows-launch-request.test.ts packages/runner/src/providers/claude/claude-launch-selection.test.ts packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts` prints NOTHING; `git status --porcelain -- packages/runner/src/platform/windows/` may list windows-broker-path.ts ` M` and windows-broker-path.test.ts `??` — record them as FOREIGN, never touch or commit. (g) Row comments: if a governor hold/ruling is recorded, obey it before any byte. (h) Baseline: from packages/runner, `pnpm exec vitest run --root ../.. packages/runner/src/platform/windows/windows-launch-request.test.ts packages/runner/src/providers/claude/claude-launch-selection.test.ts packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts` — quote `Test Files 3 passed` and the Tests count (baseline for step 4). Any premise failing: report blocked naming it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts"
      ],
      "note": "VERIFY-OR-BLOCK done at HEAD 3986218b (board moved from plan's 595c32c2; all premises re-measured and still hold). Full record in comment-84926e3ccf4640ab8698c324c233e3ec. (a) gap OPEN, EXIT 1, roster still the 24 host names, file 355 lines. (b) EXIT 0, :66 = `{ model: \"ANTHROPIC_MODEL\", effort: \"CLAUDE_CODE_EFFORT_LEVEL\" }`. (c) EXIT 0, :11 import + :34 spread, composition intact. (d) positive control `git grep -c export` = 7, EXIT 0, so (a)'s absence is real. (e) PLAN PATH WAS WRONG AND ITS GREP VACUOUS: top-level `native/` has ZERO files at HEAD (`git ls-tree -r --name-only HEAD -- native/` empty), so the step's EXIT 1 proved nothing. Re-ran at the real tree packages/runner/src/platform/windows/native/ -> EXIT 0, 19 hits, all read: every \"allowlist\" is the inherited-HANDLE list (fd3/fd4/fd5), every SYSTEMROOT/PATHEXT is a test fixture. The broker's real env path (broker/src/launch.rs:55-61) refuses only on embedded NUL and payload.rs:77-83 decodes raw key/value pairs with no name check — NO environment-name allowlist in the broker, so this row reaches the provider alone. Not blocked. (f) four owned paths porcelain-CLEAN; FOREIGN windows-broker-path.ts ` M` + windows-broker-path.test.ts `??` recorded, untouched. (g) one row comment, architect plan-time only, no governor hold. (h) baseline EXIT 0, `Test Files  3 passed (3)`, `Tests  135 passed (135)`. Read-only step: no files modified.",
      "startedAt": "2026-08-29T17:19:55.809Z",
      "completedAt": "2026-08-29T17:21:24.236Z"
    },
    {
      "stepId": "step-2",
      "description": "RED FIRST — arms in three test files; each title names the ONE mechanism that can red it. (1) packages/runner/src/platform/windows/windows-launch-request.test.ts, new describe \"the provider boundary admits exactly the Claude launch-selection keys\": import `CLAUDE_LAUNCH_SELECTION_ENV` from ../../providers/claude/claude-launch-selection.js (tests may cross that edge) and `PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS` from ./windows-launch-request.js (does not exist yet — red at import). ARM A (DoD-2 reproduction with positive control in the SAME arm): `accepted({ ...GOOD, environment: { PATH: GOOD.cwd } })` returns bytes (the environment gate is reached and passable), then `encodeLaunchPayload({ ...GOOD, environment: { [CLAUDE_LAUNCH_SELECTION_ENV.model]: 'claude-opus-4-1', [CLAUDE_LAUNCH_SELECTION_ENV.effort]: 'high' } })` must be a Uint8Array whose decoded text contains both names; before the fix this is the refusal `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED` / `WINDOWS_PROCESS_REQUEST` — quote that red. ARM B (DoD-3 exact roster, shift-proof by design): `expect([...ALLOWED_ENVIRONMENT_KEYS]).toEqual([<the 24 names in their current order>, 'ANTHROPIC_MODEL', 'CLAUDE_CODE_EFFORT_LEVEL'])` — title it 'pins the full provider roster; any widening reds here by design'; plus `expect([...PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS]).toEqual(['ANTHROPIC_MODEL','CLAUDE_CODE_EFFORT_LEVEL'])` and every subset name is in ALLOWED_ENVIRONMENT_KEYS. ARM C (DoD-4 negatives, it.each with code AND layer): NODE_OPTIONS, HTTP_PROXY, HTTPS_PROXY, ANTHROPIC_API_KEY, MOE_STORE_PATH, MOE_PROJECT_CONFIGURATION_DIGEST — each `{ ...GOOD, environment: { [name]: 'x' } }` refuses with code `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED` and layer `WINDOWS_PROCESS_REQUEST` (green before and after — say so in the title; it is the fence D2 drills). ARM D: no platform->providers import in the module: `readFileSync` windows-launch-request.ts and assert no line matches /from \"\\.\\.\\/\\.\\.\\/providers\\//. (2) packages/runner/src/providers/claude/claude-launch-selection.test.ts, one new arm in the existing describe: 'the Windows provider boundary admits every override variable this provider spells, and no other selection key' — `expect([...PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS].sort()).toEqual(Object.values(CLAUDE_LAUNCH_SELECTION_ENV).sort())` and `Object.values(CLAUDE_LAUNCH_SELECTION_ENV).every((name) => ALLOWED_ENVIRONMENT_KEYS.includes(name))` — the BIDIRECTIONAL pin (rail 2). (3) packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts, extend :154 'publishes a finite reviewed environment roster': `expect(PROJECT_STACK_ENVIRONMENT_KEYS).toContain('ANTHROPIC_MODEL')` and `toContain('CLAUDE_CODE_EFFORT_LEVEL')` asserted against the COMPOSED constant (not a part), with the existing NODE_OPTIONS and uniqueness assertions untouched. Run the three files from packages/runner; quote the reds (import failure + ARM A refusal) in a step note.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.test.ts",
        "packages/runner/src/providers/claude/claude-launch-selection.test.ts",
        "packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.test.ts",
        "packages/runner/src/providers/claude/claude-launch-selection.test.ts",
        "packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts"
      ],
      "note": "RED FIRST, three test files, zero production bytes touched. Run from packages/runner: EXIT 1, `Test Files  3 failed (3)`, `Tests  5 failed | 141 passed (146)` (baseline was 135 passed; +11 = ARM A 1, ARM B 1, subset 1, ARM C it.each 6, ARM D 1, providers arm 1 — the project-stack extension is inside an existing arm, so it adds no test).\n\nTHE 5 REDS, each for the right reason:\n(1) ARM A `encodes the producer's launch-selection environment, with a PATH-only positive control` — the DoD-2 production-surface reproduction. Its POSITIVE CONTROL passed first (`accepted({...GOOD, environment: { PATH: GOOD.cwd }})` returned bytes), so the environment gate is REACHED and passable and the refusal below is the allowlist answering on these names, not an earlier fence rejecting the fixture. I rewrote the second assertion from a bare `toBeInstanceOf(Uint8Array)` to a projected `{accepted, code, layer}` toEqual precisely so the red NAMES the refusal instead of printing \"expected { truthClass: 'UNKNOWN' } to be an instance of Uint8Array\". Red verbatim: `- \"accepted\": true, - \"code\": null, - \"layer\": null, + \"accepted\": false, + \"code\": \"PROCESS_BOUNDARY_ENVIRONMENT_REJECTED\", + \"layer\": \"WINDOWS_PROCESS_REQUEST\"`. Keys spelled through CLAUDE_LAUNCH_SELECTION_ENV.model/.effort, never as literals.\n(2) ARM B `pins the full provider roster; any widening reds here by design` — `expected [ 'SYSTEMROOT', 'WINDIR', …(22) ] to deeply equal [ … …(24) ]`, diff missing exactly `- \"ANTHROPIC_MODEL\"` and `- \"CLAUDE_CODE_EFFORT_LEVEL\"`.\n(3) `publishes the launch-selection subset and admits every name in it` — `TypeError: PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS is not iterable` (export does not exist yet).\n(4) claude-launch-selection.test.ts `the Windows provider boundary admits every override variable this provider spells, and no other selection key` — same TypeError at :423, the BIDIRECTIONAL pin. Import direction providers -> platform only; the platform module still imports nothing from providers/**.\n(5) windows-project-stack-boundary.test.ts `publishes a finite reviewed environment roster` — `expected [ 'SYSTEMROOT', 'WINDIR', …(41) ] to include 'ANTHROPIC_MODEL'`, asserted against the COMPOSED PROJECT_STACK_ENVIRONMENT_KEYS, existing NODE_OPTIONS/uniqueness/credential-tuple assertions untouched.\n\nGREEN BEFORE AND AFTER BY DESIGN, said so in their titles: ARM C `still refuses %s at the provider boundary, with its code and layer` (6 it.each cases: NODE_OPTIONS, HTTP_PROXY, HTTPS_PROXY, ANTHROPIC_API_KEY, MOE_STORE_PATH, MOE_PROJECT_CONFIGURATION_DIGEST, each asserting code AND layer in one toEqual) and ARM D `keeps the platform module free of any providers import` (readFileSync of windows-launch-request.ts, regex `/(?:from|import\\()\\s*[\"']\\.\\.\\/\\.\\.\\/providers\\//` over its lines, with a `toContain(\"export const ALLOWED_ENVIRONMENT_KEYS\")` positive control proving the right file was read). Those two are fences that must NOT move; drills D2 and D4 redden them in step 4.\n\nForeign windows-broker-path.ts/.test.ts untouched.",
      "startedAt": "2026-08-29T17:21:29.159Z",
      "completedAt": "2026-08-29T17:24:11.849Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN — packages/runner/src/platform/windows/windows-launch-request.ts ONLY (355 lines; add roster lines and their comment, nothing else). (1) Directly above ALLOWED_ENVIRONMENT_KEYS add: `/** The provider's own launch-selection overrides — the ONLY non-host names a provider may receive. Spelled here by hand, as the provider spells them (providers/claude/claude-launch-selection.ts CLAUDE_LAUNCH_SELECTION_ENV) and pinned to that constant in both directions by claude-launch-selection.test.ts; this module never imports providers/**. They select a model id and an effort level; nothing reads them as a path, a flag list or executable input. NODE_OPTIONS and the *_PROXY family stay absent on purpose. */ export const PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS = Object.freeze([\"ANTHROPIC_MODEL\", \"CLAUDE_CODE_EFFORT_LEVEL\"] as const);` (2) In ALLOWED_ENVIRONMENT_KEYS append, after \"TZ\", the two literals `\"ANTHROPIC_MODEL\", \"CLAUDE_CODE_EFFORT_LEVEL\",` with a trailing comment `// provider launch selection — see PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS`. Keep it a literal roster (do not spread the subset constant into it — the exact 26-name pin in ARM B is the single source of truth for the composed list, and a spread would let a change to the subset silently move the roster). Extend the module doc (:30-34) with one sentence: 'The two provider launch-selection names are a reviewed widening (task-fd196056).' (3) Do NOT touch encodeLaunchPayloadWithAllowedEnvironment, readEnvironmentRecord, or any limit. Re-run the three focused files from packages/runner: all new arms green, existing arms green; quote `Test Files 3 passed` and `Tests N passed` (N = baseline + 5: A, B, C[6 cases count as the it.each total — quote the real number], D, the providers arm, the stack-boundary extension is inside an existing arm). `pnpm --filter @moe/runner typecheck` exit 0 with the `$ tsc` echo quoted; `wc -l` the module (expect ~366, under 400).",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts",
        "packages/runner/src/platform/windows/windows-launch-request.test.ts"
      ],
      "note": "GREEN. Production change is windows-launch-request.ts ONLY: (1) new exported `PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS = Object.freeze([\"ANTHROPIC_MODEL\",\"CLAUDE_CODE_EFFORT_LEVEL\"] as const)` with the doc block naming the producer constant and stating why the two names are safe (model id + effort token, never read as a path, flag list or executable input; values still bounded by MAX_ENVIRONMENT_VALUE_CHARS and the NUL/equals guards) and why the module never imports providers/**; (2) the two literals appended after \"TZ\" in ALLOWED_ENVIRONMENT_KEYS behind the comment `// provider launch selection — see PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS`, NOT spread from the subset, so a subset change cannot move the roster silently; (3) the roster doc extended with the reviewed-widening sentence naming task-fd196056. encodeLaunchPayloadWithAllowedEnvironment, readEnvironmentRecord and every limit are untouched.\n\n`wc -l packages/runner/src/platform/windows/windows-launch-request.ts` = 384 (was 355; +29, all roster entries and their comments). Under the 400 split bar, over the 250 target — no split taken because the row's rails forbid touching anything but the roster lines here and the module was already 355 before this row.\n\nGATE `pnpm --filter @moe/runner typecheck` EXIT 0, script echo `$ tsc --project tsconfig.json`. EXECUTION IS PROVEN, not assumed: the leg failed TWICE first against this exact file and I fixed both — `src/platform/windows/windows-launch-request.test.ts(481,72): error TS2345` then `(498,67): error TS2345`. Those two reds are the positive control that the sub-second green is a real compile.\n\nTEST-SIDE CORRECTION forced by the first typecheck red, and it strengthened the arm rather than working around it: my first delta assertion compared roster-minus-SUBSET length arithmetic, which is a near-tautology once ARM B pins 26. The DoD-3 clause is roster-minus-the-PRE-CHANGE-24. I hoisted the 24 pre-change names as a hand-written `PRE_CHANGE_ENVIRONMENT_KEYS: readonly string[]` (typed as plain strings, never read back from the module under test, so it can witness that the subject changed), rebuilt ARM B as `[...PRE_CHANGE, \"ANTHROPIC_MODEL\", \"CLAUDE_CODE_EFFORT_LEVEL\"]` with a `toHaveLength(24)` guard on the const, and renamed the third arm to `widened by the launch-selection subset and by nothing else`, which now asserts `widened = roster.filter(name => !PRE_CHANGE.includes(name))` toEqual the subset — so a THIRD name admitted alongside the two reds here even if it were also added to the subset constant — plus a no-name-was-dropped loop over the pre-change 24. Only the `.includes` lookup was widened to `readonly string[]`; the runtime membership check is unchanged.\n\nFOCUSED RUN from packages/runner, EXIT 0: `Test Files  3 passed (3)`, `Tests  146 passed (146)` — every one of the 5 reds from step 2 is now green and the 141 pre-existing arms stayed green (baseline 135 + 11 new). Foreign windows-broker-path.ts/.test.ts untouched.",
      "startedAt": "2026-08-29T17:24:13.879Z",
      "completedAt": "2026-08-29T17:26:00.940Z"
    },
    {
      "stepId": "step-4",
      "description": "DRILLS (DoD-5) + GATES (DoD-6). Every drilled file is UNCOMMITTED, so `git diff --exit-code` proves NOTHING — back windows-launch-request.ts up to `%TEMP%\\fd196056-<workerId>\\` and prove each restore by SHA256 equality in a call SEPARATE from the restore. (D1) remove `\"CLAUDE_CODE_EFFORT_LEVEL\"` from ALLOWED_ENVIRONMENT_KEYS (leave the subset constant) -> ARM A reds (refusal naming the environment), ARM B reds (exact roster), the providers bidirectional arm reds (subset name not admitted); ARM C stays green. Quote all three red lines. (D2) add `\"NODE_OPTIONS\"` to ALLOWED_ENVIRONMENT_KEYS -> ARM C reds on NODE_OPTIONS (it was accepted), ARM B reds, windows-project-stack-boundary's `not.toContain(\"NODE_OPTIONS\")` reds. (D3) misspell the subset constant entry to `\"ANTHROPIC_MODEL_\"` -> the providers bidirectional arm reds and ARM B's subset line reds while ARM A stays green (the roster literal is untouched) — that asymmetry proves the pin and the roster are graded separately. (D4) add `import { CLAUDE_LAUNCH_SELECTION_ENV } from \"../../providers/claude/claude-launch-selection.js\";` at the top of the module (unused) -> ARM D reds. A drill that stays GREEN is a FINDING: stop and report it; never bank it. After every restore, re-run the three files green. GATES, standalone, foreground, unpiped, each exit code from its own run: (G1) `pnpm --filter @moe/runner typecheck` — quote the `$ tsc` echo; sub-second green needs a planted-error control. (G2) `pnpm --filter @moe/runner test` (the whole runner package: `vitest run --root ../.. packages/runner/src`) — quote `Test Files N passed` / `Tests N passed`; the claude-launcher.test.ts arms at :663/:677 (fake boundary, ANTHROPIC_MODEL in env) must still pass. (G3) `pnpm --filter @moe/daemon typecheck` exit 0 (launch-template-producer.ts consumes the provider constant; nothing there changes, prove it compiles). (G4) daemon focused: from apps/daemon `pnpm exec vitest run src/work/launch-template-producer.test.ts` green with count (the producer's env expectations :331/:735-744 are the consumer side of the pin). Foreign red anywhere: attribute by import closure over the four owned paths and disclose verbatim. Record D1-D4 and G1-G4 in ONE row comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts",
        "packages/runner/src/platform/windows/windows-launch-request.test.ts",
        "packages/runner/src/providers/claude/claude-launch-selection.test.ts",
        "packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts"
      ],
      "note": "D1-D4 and G1-G4 recorded in full in comment-2b15d3dc40ba44bd96ba99c3b5da0363. Net file state is unchanged from step 3 — every drill was mutated and restored, proven by SHA256.\n\nRESTORE PROOF: all four owned paths are TRACKED AND MODIFIED, so `git diff --exit-code` cannot prove a restore (my delivered work is uncommitted; a correct restore still diffs). Used SHA256 vs `%TEMP%\\fd196056-worker-9fe78697\\windows-launch-request.ts.bak`, hashed in a call SEPARATE from every `cp`. Delivered hash `7083c93948a482741a3dda11c2c4fd1d01d2fc88f42fe876683cdba3d7e77e80`; all five restores (D1-D4 plus the attribution control) matched it.\n\nDRILLS, none green: D1 (drop CLAUDE_CODE_EFFORT_LEVEL from the roster, subset left intact) -> 5 red, ARM C green as designed, and the composed project-stack arm also reds (`to include 'CLAUDE_CODE_EFFORT_LEVEL'`) which is the composition working. D2 (add NODE_OPTIONS) -> 6 red, including ARM C accepting it (`expected Uint8Array[…] to not be an instance of Uint8Array`) and the delta arm catching the third name. D3 (misspell the SUBSET entry only) -> exactly 2 red, the providers bidirectional arm and the delta arm, while ARM A and ARM B stay GREEN — the designed asymmetry proving pin and roster are graded by separate mechanisms. D4 (plant a providers import) -> ARM D reds naming the offending line verbatim.\n\nGATES: G1 `pnpm --filter @moe/runner typecheck` EXIT 0 with `$ tsc --project tsconfig.json`; its two step-3 reds on this same file are the planted-error control for the sub-second green. G2 `pnpm --filter @moe/runner test` EXIT 0, `Test Files  76 passed (76)`, `Tests  2735 passed | 1 skipped (2736)` — includes claude-launcher.test.ts and its fake-boundary ANTHROPIC_MODEL arms at :663/:677. G3 `pnpm --filter @moe/daemon typecheck` EXIT 0.\n\nG4 NEEDED TWO CORRECTIONS. The plan's `pnpm exec vitest run src/work/launch-template-producer.test.ts` from apps/daemon is VACUOUS — `No test files found, exiting with code 1`, because the include roster has no `apps/**`; the daemon's own script is `vitest run --root . --config package.json src`. Re-run correctly: EXIT 1, `Tests  3 failed | 78 passed (81)`, all three `expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be '…'` at :372, :382, :665. FOREIGN, measured not argued: I swapped my one production file to its HEAD bytes and re-ran — THE SAME 3 REDS, IDENTICALLY — then restored and re-proved by SHA256. Owned-path delta is EMPTY. The code comes from `DEPENDENCIES_REFUSED` (claude-launcher-authority.ts:63), a launcher dependency-composition refusal that never reaches this row's gate; the claude provider production tree is porcelain-clean while foreign ` M windows-broker-path.ts` sits in that import closure (windows-boundary.ts:6). Disclosed verbatim, not touched, not fixed.\n\nFinal: three focused files EXIT 0, `Test Files  3 passed (3)`, `Tests  146 passed (146)`. Foreign windows-broker-path.ts/.test.ts untouched.",
      "startedAt": "2026-08-29T17:26:03.511Z",
      "completedAt": "2026-08-29T17:30:57.446Z"
    },
    {
      "stepId": "step-5",
      "description": "ADVERSARIAL SELF-REVIEW, COMMIT, DURABLE COMPLETION RECORD. Self-review, answered in the row comment: (i) can either admitted name carry executable input? `ANTHROPIC_MODEL` is a model id and `CLAUDE_CODE_EFFORT_LEVEL` an effort token; the provider's verifyLaunchSelection (claude-launch-selection.test.ts :479-556) refuses an environment that disagrees with argv or spells one name twice — cite those arms; values are still bounded by MAX_ENVIRONMENT_VALUE_CHARS and the NUL/equals-sign guards (:255 it.each). (ii) case-insensitive admission means `anthropic_model` is admitted too — existing policy (:239), unchanged; state it. (iii) the outer host roster gained the two names by composition — the daemon host process may now receive them; they are harmless there (no consumer reads them on the host) — say so, and that windows-project-stack-boundary.test.ts asserts the composition. (iv) the native broker filters no names (step 1(e) result quoted). (v) this row proves admission at encodeLaunchPayload, the surface the probe used; the real-process control belongs to task-e4cd1eb6 step 5 — state that boundary plainly. Line counts: `wc -l` on the production file (< 400). COMMIT BY EXPLICIT PATHSPEC ONLY: `git add -- packages/runner/src/platform/windows/windows-launch-request.ts packages/runner/src/platform/windows/windows-launch-request.test.ts packages/runner/src/providers/claude/claude-launch-selection.test.ts packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts` then `git commit -m \"fix(task-fd196056): admit the Claude launch-selection names at the Windows provider boundary\"`; `git show --stat HEAD` lists exactly those four paths and NOT windows-broker-path.*. COMPLETION COMMENT (moe.add_comment — the completion summary is write-only): commit sha; HEAD at claim; G1-G4 count lines verbatim; D1-D4 outcomes; the consumer named: task-e4cd1eb6807244efa3334609ed746143 step 5; and the delivered claim stated verbatim: 'the provider boundary admits exactly ANTHROPIC_MODEL and CLAUDE_CODE_EFFORT_LEVEL; no other name was widened'. Then moe.complete_task with a one-line summary pointing at that comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts",
        "packages/runner/src/platform/windows/windows-launch-request.test.ts"
      ],
      "modifiedFiles": [
        "packages/runner/src/platform/windows/windows-launch-request.ts",
        "packages/runner/src/platform/windows/windows-launch-request.test.ts",
        "packages/runner/src/providers/claude/claude-launch-selection.test.ts",
        "packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts"
      ],
      "note": "Committed 7a227a2549d4bb4da382d06072a182e98286d0e7 by explicit pathspec: `4 files changed, 211 insertions(+)`, zero deletions. `git show --name-only HEAD` contains NO broker-path path; foreign ` M windows-broker-path.ts` / `?? windows-broker-path.test.ts` still dirty and untouched afterwards. Production module 384 lines (< 400). Full self-review in comment-cb6fc016eab646db9fb085dbcc866ffe.\n\nSelf-review highlights: (i) neither admitted name can carry executable input — values still bounded by isBoundedText/MAX_ENVIRONMENT_VALUE_CHARS and the NUL/equals guards (test :262, :264), and the provider's verifyLaunchSelection refuses an environment disagreeing with proved argv (claude-launch-selection.test.ts :530 model, :501 effort incl. case-variant and lowercase name cases, :591 non-record); roster 26 vs MAX_ENVIRONMENT_ENTRIES 64. (ii) case-insensitive admission also admits `anthropic_model` — existing unchanged policy, test :244; duplicate-case spellings still refused. (iii) I MEASURED rather than asserted the \"harmless on the host\" claim: unpiped `git grep -n -E 'process\\.env(\\.|\\[\")(ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL)' -- apps packages adapters tools` EXIT 1 / zero hits, with a shape-matched positive control on MOE_STORE_PATH|MOE_PROJECT_ID returning 4 hits at EXIT 0. No host consumer reads either name. (iv) native broker filters no environment names (re-measured at the real tree in step 1; the plan's `native/` path is empty at HEAD). (v) stated plainly: this row proves admission at encodeLaunchPayload only — the real-process control is task-e4cd1eb6807244efa3334609ed746143 step 5. (vi) my own pass: the diff is purely additive and touches no control flow, limit or gate order, so there is no error path, partial-write, retry or concurrency surface; the one real design question is drift direction, and holding literals rather than spreading the subset makes drift FAIL CLOSED (a subset-only addition dies at ENCODE and reds the pin) — that is drill D3, proven red.\n\nFresh verification at the delivered commit: `pnpm --filter @moe/runner test` EXIT 0, `Test Files  76 passed (76)`, `Tests  2735 passed | 1 skipped (2736)`. Disclosed foreign red unchanged and unowned: apps/daemon launch-template-producer.test.ts 3 failures, proven identical with my production file swapped to HEAD bytes.",
      "startedAt": "2026-08-29T17:31:02.267Z",
      "completedAt": "2026-08-29T17:33:06.435Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": "qa-860ef713",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 163,
  "comments": [
    {
      "id": "comment-b339e4ee3ab44c6a824f6e6a93163532",
      "author": "architect-c1a63707",
      "content": "PLAN-TIME MEASUREMENT (architect-c1a63707, HEAD 595c32c2, 2026-08-29).\n\nGAP CONFIRMED: `git grep -n -E 'ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL' HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` EXIT 1. ALLOWED_ENVIRONMENT_KEYS (:35-59) is 24 host names; readEnvironmentRecord (:115-150) refuses any other name with PROCESS_BOUNDARY_ENVIRONMENT_REJECTED @ WINDOWS_PROCESS_REQUEST ('duplicated or outside the allowlist'); MAX_ENVIRONMENT_ENTRIES = 64. Producer spelling: claude-launch-selection.ts:65-66 `{ model: \"ANTHROPIC_MODEL\", effort: \"CLAUDE_CODE_EFFORT_LEVEL\" }`, hand-pinned by claude-launch-selection.test.ts:396. claude-launcher.test.ts:663/:677 pass ANTHROPIC_MODEL through a FAKED boundary — that is why the gap stayed invisible until worker-57c90511's real probe (comment-72e983b1 on e4cd1eb6). No other platform roster exists; the native broker showed no name allowlist by grep (step 1(e) re-verifies unpiped).\n\nDESIGN (a deliberate, reviewed widening): append exactly the two names to ALLOWED_ENVIRONMENT_KEYS as literals with a doc block; export the two-name subset `PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS`; pin the full 26-name roster EXACTLY in windows-launch-request.test.ts (any future widening reds there by design); pin subset == Object.values(CLAUDE_LAUNCH_SELECTION_ENV) bidirectionally in claude-launch-selection.test.ts (providers -> platform import direction already exists; the module itself never imports providers/**, ARM D asserts it). Negatives asserted individually with code+layer: NODE_OPTIONS, HTTP_PROXY, HTTPS_PROXY, ANTHROPIC_API_KEY, MOE_STORE_PATH, MOE_PROJECT_CONFIGURATION_DIGEST. PROJECT_STACK_ENVIRONMENT_KEYS widens by composition; asserted against the composed constant.\n\nGOVERNOR HOLD OPTION: this is an authority-surface widening on a fence whose own comment calls an unlisted name 'a deliberate widening rather than an oversight'. If a rail-4 ruling is wanted before it lands, record it HERE; step 1(g) makes the worker obey it before any byte.\n\nSCOPE BOUNDARY: this row proves admission at encodeLaunchPayload (the probe's surface). The real-process control stays with task-e4cd1eb6807244efa3334609ed746143 step 5 (consumer). Sibling prerequisite: task-80745330b6a148ab9933766682a169a5.",
      "timestamp": "2026-08-29T17:01:34.751Z"
    },
    {
      "id": "comment-84926e3ccf4640ab8698c324c233e3ec",
      "author": "worker-9fe78697",
      "content": "STEP 1 VERIFY-OR-BLOCK (worker-9fe78697). `git rev-parse HEAD` = 3986218b14aea3358929f005200b1b1aa6e96965 (board moved from the plan's 595c32c2; every premise re-measured at 3986218b, all still hold). Every grep unpiped, its own exit code.\n\n(a) GAP OPEN: `git grep -n -E 'ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL' HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` -> no output, EXIT 1. Nobody landed it. Read the file directly: ALLOWED_ENVIRONMENT_KEYS at :35-59 is still exactly the 24 host names (SYSTEMROOT..TZ), `wc -l` = 355.\n\n(b) `git grep -n 'export const CLAUDE_LAUNCH_SELECTION_ENV' HEAD -- packages/runner/src/providers/claude/claude-launch-selection.ts` -> `:65:export const CLAUDE_LAUNCH_SELECTION_ENV =`, EXIT 0. Line 66 verbatim: `  Object.freeze({ model: \"ANTHROPIC_MODEL\", effort: \"CLAUDE_CODE_EFFORT_LEVEL\" } as const);`\n\n(c) `git grep -n 'ALLOWED_ENVIRONMENT_KEYS' HEAD -- packages/runner/src/platform/windows/windows-project-stack-boundary.ts` -> `:11:  ALLOWED_ENVIRONMENT_KEYS,` (import) and `:34:  ...ALLOWED_ENVIRONMENT_KEYS,` (spread), EXIT 0. Composition intact; the outer roster still widens by composition.\n\n(d) POSITIVE CONTROL: `git grep -c export HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` -> `7`, EXIT 0. The pathspec resolves and greps the file, so (a)'s EXIT 1 is a real absence and not a bad path.\n\n(e) PLAN PATH CORRECTION — the step's grep as written was VACUOUS. `git grep ... HEAD -- native/` returned EXIT 1, but `git ls-tree -r --name-only HEAD -- native/` returns ZERO files: there is no top-level `native/` at HEAD. The native broker lives at packages/runner/src/platform/windows/native/. Re-ran there: `git grep -n -i -E 'SYSTEMROOT|PATHEXT|allowlist' HEAD -- packages/runner/src/platform/windows/native/` EXIT 0, 19 hits, all read. FINDING: NONE of them is an environment-NAME allowlist. Every \"allowlist\" hit is the inherited-HANDLE list (`PROC_THREAD_ATTRIBUTE_HANDLE_LIST`) — src/spec.rs:36, src/win32/process_calls.rs:23/26, src/win32/system_process.rs:84/199, src/win32/system_process_attrs.rs:126, broker/src/bin/handle_probe.rs:28/97, broker/tests/handle_inheritance.rs:165, broker/tests/session.rs:258 (\"THE ALLOWLIST IS THE PROVIDER TRIO AND NOTHING ELSE\" — asserts `handles == PROVIDERS`, i.e. fd3/fd4/fd5, handles not names), tests/process_sweep.rs:240. Every SYSTEMROOT/PATHEXT hit is a test fixture building an environment block (handle_inheritance.rs:286/298, process_sweep.rs:334, real_windows.rs:10/109/113/120). The broker's ACTUAL environment path: broker/src/launch.rs:55-61 iterates `request.environment()` and refuses ONLY on an embedded NUL (`every.iter().any(|text| text.contains('\\0'))`), then broker/src/payload.rs:77-83 decodes a u16 count of raw key/value string pairs with no name check at all. So the broker filters no environment NAMES and this row does reach the provider alone. NOT blocked.\n\n(f) PORCELAIN. Owned paths CLEAN — `git status --porcelain -- packages/runner/src/platform/windows/windows-launch-request.ts packages/runner/src/platform/windows/windows-launch-request.test.ts packages/runner/src/providers/claude/claude-launch-selection.test.ts packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts` printed NOTHING. FOREIGN in the same directory, recorded and left untouched, never to be committed by this row: ` M packages/runner/src/platform/windows/windows-broker-path.ts` and `?? packages/runner/src/platform/windows/windows-broker-path.test.ts`.\n\n(g) ROW COMMENTS: exactly one, comment-b339e4ee (architect-c1a63707, plan-time measurement). NO governor hold, no rail-4 ruling recorded against this row. Proceeding.\n\n(h) BASELINE, from packages/runner, standalone/foreground/unpiped-exit (`${PIPESTATUS[0]}` captured): `pnpm exec vitest run --root ../.. packages/runner/src/platform/windows/windows-launch-request.test.ts packages/runner/src/providers/claude/claude-launch-selection.test.ts packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts` EXIT 0, `Test Files  3 passed (3)`, `Tests  135 passed (135)`. That 135 is the step-4 baseline.",
      "timestamp": "2026-08-29T17:21:14.552Z"
    },
    {
      "id": "comment-2b15d3dc40ba44bd96ba99c3b5da0363",
      "author": "worker-9fe78697",
      "content": "STEP 4 — DRILLS (DoD-5) AND GATES (DoD-6). worker-9fe78697, HEAD at claim 3986218b.\n\nRESTORE-PROOF METHOD. All four owned paths are TRACKED AND MODIFIED (` M`), so `git diff --exit-code` is NOT a usable restore proof here: my delivered changes are uncommitted, so a correct restore still shows a diff. SHA256 against an out-of-tree backup is therefore the only valid proof and is what I used. Backup `%TEMP%\\fd196056-worker-9fe78697\\windows-launch-request.ts.bak`, taken before the first drill; the delivered file's SHA256 is `7083c93948a482741a3dda11c2c4fd1d01d2fc88f42fe876683cdba3d7e77e80`. EVERY restore below was proven by hashing in a call SEPARATE from the `cp`, because \"the copy returned 0\" is not evidence the bytes moved. All five hashes came back equal to the delivered hash; none of the other three owned files was ever mutated.\n\nD1 — remove `\"CLAUDE_CODE_EFFORT_LEVEL\"` from ALLOWED_ENVIRONMENT_KEYS (subset constant left intact). 5 RED, ARM C GREEN as designed. `Tests  5 failed | 141 passed (146)`. Reds: ARM A `expected { accepted: false, …(2) } to deeply equal { accepted: true, code: null, …(1) }`; ARM B `expected [ 'SYSTEMROOT', 'WINDIR', …(23) ] to deeply equal [ … …(24) ]`; the delta arm; the providers bidirectional arm; and — beyond the plan's prediction — the composed project-stack arm `expected [ 'SYSTEMROOT', 'WINDIR', …(42) ] to include 'CLAUDE_CODE_EFFORT_LEVEL'`, which is the composition working. Restore hash MATCHED.\n\nD2 — add `\"NODE_OPTIONS\"` to ALLOWED_ENVIRONMENT_KEYS. 6 RED. Reds: ARM C `still refuses NODE_OPTIONS at the provider boundary, with its code and layer` (`expected Uint8Array[ 26, 0, 67, 58, 92, …(437) ] to not be an instance of Uint8Array` — NODE_OPTIONS was ACCEPTED, which is the whole point); ARM B `…(25) to deeply equal …(24)`; the delta arm `expected [ 'ANTHROPIC_MODEL', …(2) ] to deeply equal [ 'ANTHROPIC_MODEL', …(1) ]` — this is DoD-3's third-name catch firing; project-stack `expected [ 'SYSTEMROOT', 'WINDIR', …(44) ] to not include 'NODE_OPTIONS'`; plus the two pre-existing fences `refuses a key outside the allowlist` and `refuses unreviewed environment key NODE_OPTIONS`. Restore hash MATCHED.\n\nD3 — misspell the SUBSET constant entry to `\"ANTHROPIC_MODEL_\"`, roster literal untouched. Exactly 2 RED and the designed ASYMMETRY held: the providers bidirectional arm and the delta arm both red with `expected [ 'ANTHROPIC_MODEL_', …(1) ] to deeply equal [ 'ANTHROPIC_MODEL', …(1) ]`, while ARM A and ARM B stayed GREEN. That is the proof the pin and the roster are graded by separate mechanisms rather than one assertion covering for the other. Restore hash MATCHED.\n\nD4 — plant `import { CLAUDE_LAUNCH_SELECTION_ENV } from \"../../providers/claude/claude-launch-selection.js\";` at line 1. ARM D RED, naming the offending line verbatim: `+ [ \"import { CLAUDE_LAUNCH_SELECTION_ENV } from \\\"../../providers/claude/claude-launch-selection.js\\\";\" ]` against `Expected []`. Restore hash MATCHED. NO DRILL STAYED GREEN, so there is no finding to report on this axis.\n\nG1 `pnpm --filter @moe/runner typecheck` EXIT 0, echo `$ tsc --project tsconfig.json`. Execution proven, not assumed: earlier in step 3 this same leg FAILED TWICE on this exact file (`windows-launch-request.test.ts(481,72): error TS2345` then `(498,67): error TS2345`) before I fixed both. Those reds are the planted-error control for the sub-second green.\nG2 `pnpm --filter @moe/runner test` (`$ vitest run --root ../.. packages/runner/src`) EXIT 0, `Test Files  76 passed (76)`, `Tests  2735 passed | 1 skipped (2736)`. Whole-package, so claude-launcher.test.ts is included; its fake-boundary ANTHROPIC_MODEL arms at :663 and :677 (grep-confirmed present) are inside that green.\nG3 `pnpm --filter @moe/daemon typecheck` EXIT 0, echo `$ tsc --project tsconfig.json`.\nG4 — TWO CORRECTIONS AND A DISCLOSED FOREIGN RED.\n  (i) The plan's invocation `pnpm exec vitest run src/work/launch-template-producer.test.ts` from apps/daemon is VACUOUS: it prints `No test files found, exiting with code 1` with `include: adapters/**, packages/**, tests/**, tools/**` — no `apps/**`. The daemon's real script is `vitest run --root . --config package.json src`, so the leg must carry `--root . --config package.json`. Anyone re-running G4 as written gets a bare exit 1 that says nothing about this row.\n  (ii) With the correct invocation: EXIT 1, `Test Files  1 failed (1)`, `Tests  3 failed | 78 passed (81)`. The 3 reds, verbatim: `passes the production selection gate and dies later, at the runtime port` — `expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be 'CLAUDE_LAUNCH_RUNTIME_THROWN'` (:372); `is refused by that same gate once argv names a different model` — `expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be 'CLAUDE_LAUNCH_MODEL_MISMATCH'` (:382); `leaves a non-token model id to the launcher, which refuses the whole selection` — `expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be 'CLAUDE_LAUNCH_SELECTION_MALFORMED'` (:665).\n  ATTRIBUTION, MEASURED NOT ARGUED. I did not rest on the (correct but weak) argument that widening an allowlist can only admit more. I ran the direct control: `git show HEAD:packages/runner/src/platform/windows/windows-launch-request.ts >` the file (confirmed at HEAD bytes by `git diff --exit-code` = 0, valid here because this compares against the index, not against a backup), then re-ran G4. THE SAME 3 TESTS FAILED IDENTICALLY, `Tests  3 failed | 78 passed (81)`. My production diff is the ONLY production byte this row changes, so the (HEAD-with-my-diff) minus (HEAD-without-my-diff) failing-path delta intersected with my owned paths is EMPTY. Then I restored the delivered bytes and re-proved by SHA256 in a separate call: MATCHED. Corroborating: all three refusals are `CLAUDE_LAUNCH_REQUEST_MALFORMED` from `DEPENDENCIES_REFUSED` (claude-launcher-authority.ts:63, \"the durable launcher composes its own shipped dependencies\") — a launcher dependency-composition refusal, not a `PROCESS_BOUNDARY_*` code, so it never reaches my gate. The whole packages/runner/src/providers/claude/ production tree is porcelain-CLEAN, while foreign ` M windows-broker-path.ts` IS in that closure (windows-boundary.ts:6 imports it); I did not touch it to find out, per rail. Disclosed, not hidden, not fixed, not mine.\n\nFINAL STATE: three focused files EXIT 0, `Test Files  3 passed (3)`, `Tests  146 passed (146)`. Delivered hashes — windows-launch-request.ts `7083c939…`, windows-launch-request.test.ts `ebae9c54…`, claude-launch-selection.test.ts `7513a257…`, windows-project-stack-boundary.test.ts `076f84b7…`. Foreign windows-broker-path.ts ` M` and windows-broker-path.test.ts `??` still present and untouched.",
      "timestamp": "2026-08-29T17:30:37.151Z"
    },
    {
      "id": "comment-cb6fc016eab646db9fb085dbcc866ffe",
      "author": "worker-9fe78697",
      "content": "STEP 5 — ADVERSARIAL SELF-REVIEW, COMMIT, COMPLETION RECORD. worker-9fe78697.\n\nCOMMIT `7a227a2549d4bb4da382d06072a182e98286d0e7` on moe/work-2026-08-08. HEAD at claim was `3986218b14aea3358929f005200b1b1aa6e96965`. `git show --stat HEAD`: `4 files changed, 211 insertions(+)`, ZERO deletions — windows-launch-request.ts +29, windows-launch-request.test.ts +154, claude-launch-selection.test.ts +22, windows-project-stack-boundary.test.ts +6. Explicit pathspec `git add --` of exactly those four. `git show --name-only HEAD | grep -i broker-path` found NOTHING, and foreign ` M windows-broker-path.ts` / `?? windows-broker-path.test.ts` are still dirty and untouched after the commit. `wc -l` production module = 384, under the 400 split bar.\n\nDELIVERED CLAIM, VERBATIM: 'the provider boundary admits exactly ANTHROPIC_MODEL and CLAUDE_CODE_EFFORT_LEVEL; no other name was widened'.\n\nCONSUMER (global rail clause 1): task-e4cd1eb6807244efa3334609ed746143, step 5 (real-process control).\n\nSELF-REVIEW.\n(i) CAN EITHER ADMITTED NAME CARRY EXECUTABLE INPUT? No. `ANTHROPIC_MODEL` is a model id and `CLAUDE_CODE_EFFORT_LEVEL` an effort token; neither is read as a path, a flag list or executable input anywhere. Three layers still bound them. First, at this boundary the values remain subject to `isBoundedText(value, MAX_ENVIRONMENT_VALUE_CHARS)` and the guards drilled at windows-launch-request.test.ts:262 (`a value with an embedded NUL`) and :264 (`a key with an equals sign`), untouched by this row. Second, the provider's own verifyLaunchSelection refuses an environment that disagrees with the argv it proved — claude-launch-selection.test.ts:530 `refuses an environment that overrides the model argv proved` and :501 `refuses an environment that overrides the effort argv proved` (the latter includes `case-variant-name` and `lowercase-name` cases precisely because Windows names are case-insensitive), plus :591 `refuses an environment that is not a bounded plain string record`. So admitting the NAME does not admit an unproven VALUE. Third, the roster is still 26 names against MAX_ENVIRONMENT_ENTRIES = 64, and the existing `<= bound` arm stays green.\n(ii) CASE-INSENSITIVE ADMISSION. `anthropic_model` and `Anthropic_Model` are admitted too, because matching upper-cases the name before the allowlist test. That is EXISTING, UNCHANGED policy — windows-launch-request.test.ts:244 `matches allowlisted keys case-insensitively, as Windows does` — and it is correct for Windows, where those spellings are the same variable. The duplicate-spelling ambiguity is still refused (`refuses two keys that differ only in case`). This row changed no matching logic.\n(iii) THE OUTER HOST ROSTER WIDENED BY COMPOSITION. windows-project-stack-boundary.ts:34 spreads ALLOWED_ENVIRONMENT_KEYS, so PROJECT_STACK_ENVIRONMENT_KEYS gained both names; the daemon host process may now receive them. I did NOT take the plan's \"harmless, no consumer reads them on the host\" on faith — I measured it: `git grep -n -E 'process\\.env(\\.|\\[\")(ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL)' -- apps packages adapters tools` run UNPIPED, EXIT 1, zero hits, with a SHAPE-MATCHED positive control (`process\\.env(\\.|\\[\")(MOE_STORE_PATH|MOE_PROJECT_ID)`) returning 4 real hits at EXIT 0, so the grep shape demonstrably finds host env reads. No host consumer reads either name. windows-project-stack-boundary.test.ts asserts the composition against the COMPOSED constant, and that arm's `not.toContain(\"NODE_OPTIONS\")`, uniqueness and exact credential-tuple assertions are untouched and green.\n(iv) THE NATIVE BROKER FILTERS NO ENVIRONMENT NAMES. Re-measured in step 1 at the REAL tree (the plan's `native/` path is empty at HEAD, so its grep was vacuous — see comment-84926e3c): broker/src/launch.rs:55-61 refuses only on an embedded NUL, payload.rs:77-83 decodes raw key/value pairs with no name check, and every \"allowlist\" hit in that tree is the inherited-HANDLE list. So nothing downstream re-refuses these names, and equally nothing downstream was silently relied on to.\n(v) THE BOUNDARY OF THIS ROW'S PROOF, STATED PLAINLY. This row proves admission at `encodeLaunchPayload` — the exact surface worker-57c90511's probe refused at. It does NOT prove a real process launched. The real-process control belongs to task-e4cd1eb6807244efa3334609ed746143 step 5. Anyone reading DoD-2 as an end-to-end launch proof is reading it wider than it was delivered.\n(vi) MY OWN HOSTILE PASS, beyond the plan's list. The diff is PURELY ADDITIVE (211 insertions, 0 deletions) and touches no control flow: no gate order, no limit, no `readEnvironmentRecord`, no `encodeLaunchPayloadWithAllowedEnvironment` — so there is no error path, partial write, retry or concurrency surface to review; the roster is a frozen module-level constant read under no lock because it is never written. The one real design question is FAIL-OPEN VS FAIL-CLOSED ON DRIFT: because the roster holds literals rather than a spread of the subset constant, a future name added to the subset but not the roster does NOT silently reach the boundary — it dies at ENCODE and reds the bidirectional pin. That is the direction I want the failure to point, and drill D3 is exactly that scenario proven red.\n\nGATES, verbatim count lines: G1 `pnpm --filter @moe/runner typecheck` EXIT 0, `$ tsc --project tsconfig.json` (control: two real TS2345 reds on this same file earlier in step 3). G2 `pnpm --filter @moe/runner test` EXIT 0, `Test Files  76 passed (76)`, `Tests  2735 passed | 1 skipped (2736)` — re-run FRESH at the delivered commit 7a227a25, same numbers. G3 `pnpm --filter @moe/daemon typecheck` EXIT 0. G4 daemon focused, run with the daemon's own `--root . --config package.json` because the plan's invocation is vacuous (`No test files found`, include roster has no `apps/**`): EXIT 1, `Tests  3 failed | 78 passed (81)`, all three `expected 'CLAUDE_LAUNCH_REQUEST_MALFORMED' to be '…'` at :372/:382/:665 — DISCLOSED FOREIGN, proven by swapping my production file to its HEAD bytes and getting the IDENTICAL 3 reds; owned-path delta EMPTY. Full detail in comment-2b15d3dc.\n\nDRILLS: D1 5 red (ARM C green as designed), D2 6 red (incl. ARM C accepting NODE_OPTIONS and the delta arm catching the third name), D3 exactly 2 red with ARM A/ARM B GREEN — the designed asymmetry, D4 ARM D red naming the planted import verbatim. No drill stayed green. Every restore proven by SHA256 vs a %TEMP% backup in a call separate from the copy, because all four owned paths were tracked-and-modified and `git diff --exit-code` therefore proves nothing here.",
      "timestamp": "2026-08-29T17:32:49.064Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-29T16:54:48.655Z",
  "updatedAt": "2026-08-29T17:34:23.792Z",
  "metrics": {
    "firstClaimAt": "2026-08-29T16:58:55.302Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 4,
    "executedStepCount": 5
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-29T17:01:23.404Z",
  "planningNotes": {
    "approachesConsidered": "(1) REJECTED: import CLAUDE_LAUNCH_SELECTION_ENV into windows-launch-request.ts and spread its values into the roster. No hard cycle exists today (claude-launch-selection.ts imports only platform-contract.js and canonical.js), but rail 3 forbids a platform->providers edge in this module and the provider's spellings are HAND-PINNED in claude-launch-selection.test.ts:396 ('spells the provider's own flags and override variables, hand-written here') — the honest shape is two hand-written literals at the boundary plus a BIDIRECTIONAL set-equality arm in the providers test, which may import platform (that direction already exists). (2) REJECTED: route the producer env through encodeLaunchPayloadWithAllowedEnvironment with a widened roster from claude-launcher — rail 4; the provider entry keeps calling encodeLaunchPayload and the roster itself changes under review. (3) REJECTED: admit the keys in the outer PROJECT_STACK_ENVIRONMENT_KEYS only — that roster governs the daemon host process (windows-project-stack-boundary), not the provider; the probe refuses at encodeLaunchPayload. CHOSEN: append EXACTLY 'ANTHROPIC_MODEL' and 'CLAUDE_CODE_EFFORT_LEVEL' to ALLOWED_ENVIRONMENT_KEYS with a doc block naming the producer, export the two-name subset as PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS for the parity arms, pin the full 26-name roster exactly in the boundary test, pin subset == Object.values(CLAUDE_LAUNCH_SELECTION_ENV) in the providers test.",
    "codebaseInsights": "MEASURED AT HEAD 595c32c2 (2026-08-29): `git grep -n -E 'ANTHROPIC_MODEL|CLAUDE_CODE_EFFORT_LEVEL' HEAD -- packages/runner/src/platform/windows/windows-launch-request.ts` EXIT 1 (gap open). ALLOWED_ENVIRONMENT_KEYS (:35-59) = 24 names: SYSTEMROOT WINDIR SYSTEMDRIVE PATH PATHEXT COMSPEC TEMP TMP OS NUMBER_OF_PROCESSORS PROCESSOR_ARCHITECTURE USERPROFILE HOMEDRIVE HOMEPATH APPDATA LOCALAPPDATA PROGRAMDATA PROGRAMFILES PROGRAMFILES(X86) COMMONPROGRAMFILES USERNAME COMPUTERNAME LANG TZ. encodeLaunchPayload (:163) -> encodeLaunchPayloadWithAllowedEnvironment(request, ALLOWED_ENVIRONMENT_KEYS) -> encodeChecked (gate order: shape, executable, argv, cwd, environment policy, environment, merge, size) -> readEnvironmentRecord (:115-150) refuses `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED` for 'an environment name is duplicated or outside the allowlist' (case-insensitive upper match). MAX_ENVIRONMENT_ENTRIES = 64 (:26). Producer: apps/daemon/src/work/launch-template-producer.ts:177-179 builds env from CLAUDE_LAUNCH_SELECTION_ENV = {model:'ANTHROPIC_MODEL', effort:'CLAUDE_CODE_EFFORT_LEVEL'} (packages/runner/src/providers/claude/claude-launch-selection.ts:65-66); claude-launcher.ts:329 forwards request.environment to openWindowsProcessBoundary (CLAUDE_LAUNCHER_DEFAULTS.openBoundary) -> windows-boundary.ts:82 encodeLaunchPayload. claude-launcher.test.ts:663/:677 already pass ANTHROPIC_MODEL through a FAKED boundary, which is why the gap was invisible until the real probe. Outer roster: windows-project-stack-boundary.ts:33-49 spreads ALLOWED_ENVIRONMENT_KEYS first, so it widens by composition; its test (:154-172) asserts membership and NODE_OPTIONS absence, pins PROJECT_STACK_PROVIDER_CREDENTIAL_KEYS exactly (unchanged), and :145-152 expects a raw provider shape carrying REQUEST.environment (MOE_* keys) to still refuse. Existing boundary test arms (:232-279) assert frozen/unique/uppercase/<=64 and that the allowlist is smaller than the entry bound — all stay true at 26. No other platform roster exists (`git grep ENVIRONMENT_KEYS packages/runner/src/platform` non-windows: none); the native broker carries no second name allowlist by grep (worker re-verifies unpiped in step 1). Test runner: packages/runner `test` = `vitest run --root ../.. packages/runner/src`; run focused files from packages/runner with `pnpm exec vitest run --root ../.. <paths>`. Porcelain: windows-broker-path.ts ` M` and windows-broker-path.test.ts `??` are FOREIGN in the same directory.",
    "risks": "(R1) Security widening: the roster doc says an unlisted name is 'a deliberate widening rather than an oversight' — the doc block on the two new entries must say WHY (provider model/effort selection, single-valued, no code path reads them as executable input) and the negatives arm must prove NODE_OPTIONS/*_PROXY/credentials/MOE_* are still refused; a governor may hold this row as a rail-4 ruling — check the row comments at claim. (R2) Exact-roster pin: pinning the full 26-name literal in the boundary test makes ANY future roster change red there by design; say so in the arm title. (R3) A drill that removes one key must red BOTH the reproduction arm and the parity arm; if only one reds, the arms are coupled to the wrong surface. (R4) Foreign WIP in the directory (windows-broker-path.ts/.test.ts): explicit pathspec only. (R5) e4cd1eb6's DoD-3 real-process control is NOT this row's proof — the reproduction here is at encodeLaunchPayload (the surface the probe used); state that boundary in the completion comment. (R6) Case-insensitive matching means a producer spelling `Anthropic_Model` is admitted too — that is existing policy (:239 arm); do not change it.",
    "keyFiles": [
      "packages/runner/src/platform/windows/windows-launch-request.ts",
      "packages/runner/src/platform/windows/windows-launch-request.test.ts",
      "packages/runner/src/providers/claude/claude-launch-selection.ts",
      "packages/runner/src/providers/claude/claude-launch-selection.test.ts",
      "packages/runner/src/platform/windows/windows-project-stack-boundary.ts",
      "apps/daemon/src/work/launch-template-producer.ts"
    ]
  },
  "budget": {
    "wallClockMs": 10800000
  },
  "planApprovedAt": "2026-08-29T17:01:25.734Z",
  "contextFetchedBy": [
    "worker-9fe78697",
    "worker-ee96dea4",
    "qa-860ef713"
  ],
  "workStartedAt": "2026-08-29T17:19:55.809Z",
  "reviewStartedAt": "2026-08-29T17:34:19.311Z",
  "verification": {
    "command": "pnpm --filter @moe/runner test",
    "exitCode": 0,
    "outputTail": "$ vitest run --root ../.. packages/runner/src\n\n RUN  v4.1.10 D:/projexts/moe-next\n\n\n Test Files  76 passed (76)\n      Tests  2735 passed | 1 skipped (2736)\n   Start at  20:31:44\n   Duration  24.57s (transform 4.77s, setup 0ms, import 11.57s, tests 98.31s, environment 5ms)\n\nEXIT=0",
    "reportedAt": "2026-08-29T17:34:19.311Z"
  },
  "filesModified": [
    "packages/runner/src/platform/windows/windows-launch-request.ts",
    "packages/runner/src/platform/windows/windows-launch-request.test.ts",
    "packages/runner/src/providers/claude/claude-launch-selection.test.ts",
    "packages/runner/src/platform/windows/windows-project-stack-boundary.test.ts"
  ],
  "completionSummary": "The Windows provider boundary now admits exactly ANTHROPIC_MODEL and CLAUDE_CODE_EFFORT_LEVEL; no other name was widened. Commit 7a227a2549d4bb4da382d06072a182e98286d0e7 (4 files, 211 insertions, 0 deletions), HEAD at claim 3986218b. Evidence comments: comment-84926e3c (verify-or-block), comment-2b15d3dc (drills D1-D4 + gates G1-G4), comment-cb6fc016 (self-review, commit, completion record).\n\nDELIVERED: windows-launch-request.ts (355->384 lines) exports PROVIDER_LAUNCH_SELECTION_ENVIRONMENT_KEYS and appends those two literals to ALLOWED_ENVIRONMENT_KEYS (now 26). Written as literals, NOT spread from the subset, so drift fails CLOSED. No control flow, gate order, limit, readEnvironmentRecord or encodeLaunchPayloadWithAllowedEnvironment touched. No platform->providers import (asserted by an arm; drill D4 reds it).\n\nRED-FIRST: the DoD-2 reproduction refused `PROCESS_BOUNDARY_ENVIRONMENT_REJECTED` @ `WINDOWS_PROCESS_REQUEST` before the change, with a PATH-only positive control in the same arm proving the environment gate was reached; returns bytes after. Keys spelled through CLAUDE_LAUNCH_SELECTION_ENV, never as literals. Bidirectional pin in claude-launch-selection.test.ts; composed PROJECT_STACK_ENVIRONMENT_KEYS asserted in windows-project-stack-boundary.test.ts; 6 negatives (NODE_OPTIONS, HTTP_PROXY, HTTPS_PROXY, ANTHROPIC_API_KEY, MOE_STORE_PATH, MOE_PROJECT_CONFIGURATION_DIGEST) each assert code AND layer.\n\nDRILLS, none green: D1 5 red (ARM C green by design), D2 6 red, D3 exactly 2 red with ARM A/B GREEN (the designed asymmetry), D4 red naming the planted import. All four owned paths were tracked-AND-modified, so `git diff --exit-code` proves nothing here — every restore proven by SHA256 vs a %TEMP% backup in a call separate from the copy.\n\nGATES: runner typecheck EXIT 0; runner test EXIT 0, 76 files / 2735 passed (fresh at the delivered commit); daemon typecheck EXIT 0.\n\nTWO PLAN DEFECTS CORRECTED, both recorded: (1) step 1(e)'s `git grep -- native/` is VACUOUS —"
}