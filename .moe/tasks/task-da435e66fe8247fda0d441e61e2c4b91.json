{
  "id": "task-da435e66fe8247fda0d441e61e2c4b91",
  "epicId": "epic-af14d73585e14c0da0264d48c37587c3",
  "title": "bug: landLaneNode can time out or leave a dirty workspace while staffing the seeded policy step",
  "description": "Blocks task-6bafd3b9439640cba358dd6e79329c9d. On a fresh SHIPPED lane with {liveCredentials:\"ABSENT\", fakeGh:\"SUCCESS\"}, calling landLaneNode BEFORE creating the contract graph intermittently returns LANDING_BUDGET_SPENT with repeated policy.validate STAFFING_ATTEMPTS_EXHAUSTED; another run continued into BASELINE_WORKSPACE_DIRTY. Repro: pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts contract-goal.spec.ts --grep \"contract lane\" (3.3m, 1 failed). Existing landingSeatDouble writes the same timestamped file for every staffed mission; landLaneNode uses maxAgents=2 and a continuous wrapper. Source/evidence and standalone reproduction will be attached in a comment.",
  "definitionOfDone": [
    "A fresh seeded release-configured lane can call landLaneNode repeatedly and receives a real landing SHA with a clean workspace after teardown, with a committed regression that previously failed.",
    "Policy/non-code staffing cannot rewrite a code node's submitted bytes or consume its landing signal; retain real wrapper/verifier/landing and no hand-seeded receipts.",
    "Focused landing and dependent contract-goal browser arms pass under browser-e2e-gate; record executed spec count and exit status."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "ESTABLISH THE RED. DoD 1 requires a regression that PREVIOUSLY FAILED, so the failing baseline is measured BEFORE any edit and quoted verbatim; without it the fix cannot be shown to fix anything.\n\nThe owner ruled the DoD-1 semantics (comment-c21629a4, governor-9af9af37, 2026-09-08T07:05Z): 'call landLaneNode repeatedly' means N INDEPENDENT FRESH SEEDED LANES, not repeat calls against one already-landed lane. No round-command replay and no completed-node lifecycle semantics are in scope. That is the only shape in which the reported intermittent can fail before the fix.\n\nWrite the reporter's minimal repro (task comment-6de16a94, quoted in the row description) as a THROWAWAY spec next to lane-landing.ts — `withDaemonBackedControlRoom({liveCredentials:'ABSENT', fakeGh:'SUCCESS'})`, `await landLaneNode(lane)`, assert `landed.ok` and an empty `git status --porcelain` in the lane workspace. Run it N times (N>=5) at the CURRENT tree under the browser-e2e-gate lease and record every outcome: how many returned LANDING_BUDGET_SPENT, how many BASELINE_WORKSPACE_DIRTY, how many passed, with the `[wrapper]`/`[lander]` transcript lines verbatim. An intermittent needs a denominator; 'it failed once' is not a baseline.\n\nDELETE THE THROWAWAY before step 5 — step 5's `lane-landing.spec.ts` is the committed form. Record the exact command and the run tally in the step note; that tally is the 'previously failed' evidence DoD 1 asks for and QA will look for it.\n\nIf N runs all pass, DO NOT declare the bug absent and DO NOT proceed to a speculative fix: raise N, then say so in the note and on the row. The mechanism in step 3/4 is a real defect in this helper whether or not it reproduced today, but the DoD's 'previously failed' clause needs a measured red.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "RED ESTABLISHED AND DETERMINISTIC. Full evidence in comment-7baa79e6.\n\nCommand: `pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts zz-baseline-throwaway.spec.ts` (throwaway, N independent fresh seeded lanes, each its own withDaemonBackedControlRoom({fakeGh:\"SUCCESS\",liveCredentials:\"ABSENT\"}) + one landLaneNode).\n\nTALLY over 17 fresh lanes (N=5 run, then N=12 run): landed.ok 17/17; workspace CLEAN after teardown 0/17 — every lane ends `M landed-by-the-seat.txt`. N=12 run printed `##### TALLY passed=0/12 #####`, `1 failed`, EXIT=1. That is DoD 1's \"previously failed\" evidence.\n\nMECHANISM pinned verbatim on a third probe: committed blob 425c2ec = \"landed at 2026-09-08T11:39:21.181Z\"; worktree blob e648c1e = \"landed at 2026-09-08T11:39:24.081Z\". A restaff re-ran the seat double 2.9 s AFTER the lander committed and rewrote the target's file with a new timestamp — both step-3 defects (no stdin discrimination, unstable bytes) in one measurement.\n\nNOT REPRODUCED: LANDING_BUDGET_SPENT 0/17, BASELINE_WORKSPACE_DIRTY 0/17, SEAT_NEVER_WROTE 0/17. Per the plan I raised N (5 -> 12) rather than declaring the bug absent, and did not manufacture a red — the deterministic dirty-workspace failure IS the same mechanism's non-racy face.\n\nThrowaway DELETED (`rm`, confirmed absent by `test -e`). No production or committed file touched this step. Peer dirt untouched: contract-goal.spec.ts, lane-contract-goal.ts, lane-multi-node-workspace.ts remain `??` and belong to task-6bafd3b9 (worker-76f4ef35). browser-e2e-gate lease held for this row.",
      "startedAt": "2026-09-08T11:35:16.504Z",
      "completedAt": "2026-09-08T11:40:09.660Z"
    },
    {
      "stepId": "step-2",
      "description": "THE DIRECT ARMS, RED FIRST. NEW `tests/e2e/control-room/lane-landing.test.ts` — root vitest picks it up (`vitest.config.ts` include carries `tests/**/*.test.ts`), so it runs under `pnpm test` with no config change. Siblings for style and size: `wrapper-lane.test.ts` (167) and `daemon-children.test.ts` (144).\n\nThese arms exercise the GENERATED SEAT CHILD DIRECTLY — spawn the `.js` the double writes, feed it a mission on stdin, and assert what it did to disk. That is the seam where the bug lives, and it runs in seconds instead of minutes.\n\nFEED THE ARMS REAL MISSION TEXT, not a hand-written approximation. Import `codeMission` and `mission` from `apps/daemon/src/orchestrator/agent-mission-text.js` (lane-landing.ts already reaches into apps/daemon with a relative import, so the precedent and the path shape exist). The discriminator is exact and measured: `codeMission` :110 opens `You are a moe-next coding agent. You hold the durable claim on code node \"<nodeRef>\"`; `mission` :294 opens `You are a moe-next agent. You hold the durable claim on work item \"<workItemId>\" (command kind <kind>)`. Match on the code-node clause carrying THE TARGET REF — never an arbitrary substring of the instructions, which would match any mission that happened to quote a ref.\n\nCASES, each asserting a specific outcome and not merely 'did not crash':\n- the TARGET node's `codeMission` writes the file, with the exact expected bytes;\n- `mission(workItemId, 'policy.validate', ...)` writes NOTHING — assert the file is still absent, and separately that a pre-existing file is left BYTE-IDENTICAL (a policy seat arriving AFTER the code seat must not touch it);\n- a DIFFERENT code node's `codeMission` writes nothing;\n- EMPTY stdin, and stdin closed with no write, both write nothing and exit 0;\n- REPEAT invocation with the target mission is BYTE-STABLE: same bytes, no truncation, no timestamp drift. Assert equality of the file's bytes across two runs — this is the arm that pins the SUBMITTED_BYTES-digest failure;\n- CONCURRENT invocation (target and policy at once) leaves exactly the target's bytes;\n- foreign bytes elsewhere in the workspace survive every case.\n\nAssert the child's EXIT STATUS too, and assert a genuine IO failure surfaces rather than being swallowed. Per the global rail, a test asserting only 'it refused' is one layer from vacuous: pin what was written, what was not, and the exit code.\n\nThese arms are RED at this step. Do not edit lane-landing.ts yet.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/lane-landing.test.ts"
      ],
      "newFiles": [
        "tests/e2e/control-room/lane-landing.test.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/lane-landing.test.ts"
      ],
      "note": "NEW tests/e2e/control-room/lane-landing.test.ts (226 lines, under the 250 target). RED, and red for the RIGHT REASON on the arms that matter.\n\n`pnpm exec vitest run tests/e2e/control-room/lane-landing.test.ts` -> `Test Files 1 failed (1)` / `Tests 11 failed | 1 passed (12)`, EXIT=1. The count line proves the file executed.\n\nRIGHT-REASON REDS, verbatim (these fail on the ASSERTION, not on a missing symbol):\n- policy.validate: `AssertionError: a policy seat must not satisfy a code node's landing signal: expected 'landed at 2026-09-08T11:43:06.589Z\\n' to be null`\n- a DIFFERENT code node's mission: `expected 'landed at 2026-09-08T11:43:06.707Z\\n' to be null`\n- ref merely MENTIONED, no claim clause: same shape, `to be null`\n- empty stdin / stdin closed with no write: same shape, `to be null`\n- BYTE-STABILITY across a restaff: `expected 'landed at 2026-09-08T11:42:57.025Z\\n' to be 'landed at 2026-09-08T11:42:56.670Z\\n'` — the SUBMITTED_BYTES-digest failure in one assertion\n- IO failure: the child threw EISDIR uncaught (status != 0 held) but stderr carried no `SEAT_WRITE_FAILED`, so the failure is not yet SURFACED as a stable marker\nThe remaining reds are `TypeError: landedSeatBytes is not a function` — the production symbol step 3 adds.\n\nDESIGN NOTES. (1) Mission text is the PRODUCTION text: `codeMission`/`mission` imported from apps/daemon/src/orchestrator/agent-mission-text.js — verified at HEAD, codeMission:110 opens `You are a moe-next coding agent. You hold the durable claim on code node \"<nodeRef>\"`, mission:290 opens `You are a moe-next agent. You hold the durable claim on work item \"<id>\" (command kind <kind>)`. Different sentence, different noun; only the code form carries the ref. (2) The seat is spawned THROUGH the production seam `agentSpawnInvocation` (agent-spawn-invocation.ts:52), so the win32 `.cmd`-as-a-command-LINE-under-shell:true path is exercised as production exercises it, not as a path this test knows how to run. (3) Concurrency arm uses async `spawn`, not `spawnSync` — spawnSync would serialise the two and prove nothing about contention. (4) Every arm pins WHAT was written, WHAT was not, and the EXIT STATUS; a foreign file in the same workspace is asserted byte-identical across the write, the no-write and the contention arms.\n\nlane-landing.ts NOT edited this step. Peer dirt untouched (contract-goal.spec.ts, lane-contract-goal.ts, lane-multi-node-workspace.ts still `??`).",
      "startedAt": "2026-09-08T11:41:15.805Z",
      "completedAt": "2026-09-08T11:43:25.948Z"
    },
    {
      "stepId": "step-3",
      "description": "FIX THE SEAT DOUBLE (`landingSeatDouble`, lane-landing.ts:187). Two defects, both measured at HEAD 7fe37943.\n\n1. IT IGNORES STDIN. The generated `.js` is `writeFileSync(<target>, 'landed at ' + new Date().toISOString())` and runs for EVERY staffed mission. `agent-spawner.ts:400` writes `request.mission` to stdin, so a `policy.validate` seat writes the code node's landing file — satisfying `landLaneNode`'s `existsSync` signal for a mission that never delivered the node. Make the child READ STDIN TO COMPLETION and write ONLY when the mission carries the target code-node clause from step 2. Anything else: write nothing, exit 0.\n\n2. THE TIMESTAMP MAKES REPEAT WRITES UNSTABLE. The round's evidence package digests these bytes (`lane-review-round.ts` over `LANDED_PATH`); a restaff rewrites them with a new timestamp AFTER the digest was taken, so the node is refused and the item burns staffing attempts — the reported LANDING_BUDGET_SPENT with repeated STAFFING_ATTEMPTS_EXHAUSTED. Make the content DETERMINISTIC and byte-stable across invocations. It must still DIFFER from the staffing baseline so the lander has something to commit; the file is absent at baseline, so a fixed body satisfies both.\n\nThe signature gains the target node ref. `landingSeatDouble` has exactly ONE caller (lane-landing.ts:216) — verified by `git grep -n landingSeatDouble` over the tree, whose only other hits are chat and task JSON — so no consumer is broken. But it IS exported: if you would change its exported shape in a way anything else could observe, stop and say so rather than widening the roster.\n\nKEEP THE THREE-FILE BRIDGE EXACTLY AS DOCUMENTED: `.js` plus a `.cmd` for win32 and a `.sh` for posix, both delegating to the one `.js`, because `agent-spawn-invocation.ts` runs a win32 seat THROUGH cmd.exe as a command LINE. Do not collapse them; the module's own comment explains why, and `lane-multi-node-workspace.ts` (a peer row's file) copies the shape.\n\nStdin on the `.cmd` path is the risk this step must actually verify: prove the mission reaches the `.js` through cmd.exe on this host, rather than assuming the pipe survives. Step 2's arms go GREEN here. FENCED, per the owner ruling: no larger budgets, no hand-seeded receipts, no production authority edits.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/lane-landing.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/lane-landing.ts"
      ],
      "note": "SEAT DOUBLE FIXED. Step-2's 12 arms go GREEN: `pnpm exec vitest run tests/e2e/control-room/lane-landing.test.ts` -> `Test Files 1 passed (1)` / `Tests 12 passed (12)`, EXIT=0, 4.96s. (Prior session recorded these RED 11 failed | 1 passed at HEAD.)\n\nDEFECT 1, STDIN IGNORED. New `landingSeatClaim(nodeRef)` (lane-landing.ts:197) returns PRODUCTION's exact code-node clause `You are a moe-next coding agent. You hold the durable claim on code node \"<ref>\"` (agent-mission-text.ts:112 opens every codeMission with it; mission() at :294 opens with a different sentence naming a work item + command kind and carries no node ref). The generated `.js` now buffers stdin to `end`, and writes ONLY when the concatenated mission contains that clause. Policy mission, another node's codeMission, a bare-ref MENTION, empty stdin, and stdin closed with no write all write nothing and exit 0 -> the lane reports SEAT_NEVER_WROTE, never a landing. Arms pin each of those five outcomes separately, not merely \"did not crash\".\n\nDEFECT 2, TIMESTAMP. New `landedSeatBytes(nodeRef)` = `landed by the seat for <ref>\\n` — fixed, byte-stable across restaffs, and still DIFFERENT from the staffing baseline (file absent), so the lander still has something to commit. The ref is IN the bytes, so two nodes sharing a workspace cannot leave each other's landing. Arm asserts byte-equality across two invocations — that is the SUBMITTED_BYTES-digest failure pinned.\n\nIO FAILURE NOT SWALLOWED: write is guarded; a real error prints `SEAT_WRITE_FAILED <err>` on stderr and exits NONZERO (arm occupies the target path with a DIRECTORY to force it).\n\nSTDIN THROUGH cmd.exe PROVEN ON THIS HOST, not assumed. The \"carries the mission through THIS platform's executable form\" arm spawns through the production seam `agentSpawnInvocation(seat.command, [])` — on win32 that is `{shell:true, file:<command line ending landing-seat.cmd>}` — pipes the real codeMission, and asserts the landing file holds `landedSeatBytes(TARGET)`. Green on win32. Three-file bridge (.js + .cmd CRLF + .sh) kept exactly as documented; an arm asserts all three exist and both wrappers delegate to the one .js.\n\nSIGNATURE. `landingSeatDouble(dir, workspace, nodeRef)`. `git grep -n landingSeatDouble -- '*.ts' '*.js' '*.tsx'` shows exactly ONE production caller (lane-landing.ts:286) plus this row's own test file. No foreign consumer.\n\nStep-4 defect 1 landed here as a consequence, because the seat needs the ref to build its discriminator: `laneCompiledNodeRef(scratch)` is now resolved BEFORE the wrapper is spawned (:279), refusing `NO_COMPILED_EXECUTION_NODE <diagnosis>` with wrapperPid null before anything is started. The old post-wait resolution is deleted. Step-4 defects 2 (COMMITTED_LINE target match) and 3 (kill children before reading HEAD) are NOT done yet.\n\n`pnpm exec tsc -p tests/e2e/control-room/tsconfig.json` EXIT=0. lane-landing.ts is 332 lines (266 at HEAD) — under the 400 cap, over the 250 target as the plan anticipated.\n\nFENCES HELD: no budget raised (LANDING_BUDGET_MS/SEAT_WRITE_BUDGET_MS/LANDING_MAX_AGENTS untouched), no hand-seeded receipt, no dirty bytes deleted or reset, no production authority file edited.\n\nPEER DIRT (untouched, not a stop condition): contract-goal.spec.ts, lane-contract-goal.ts, lane-multi-node-workspace.ts untracked (task-6bafd3b9 / worker-76f4ef35), plus .moe/**, apps/daemon/src/http/affordance-read.ts, apps/daemon/src/preview/preview-goal-landing.test.ts and two .moe/resources/*.json.",
      "startedAt": "2026-09-08T11:43:28.686Z",
      "completedAt": "2026-09-08T11:45:51.191Z"
    },
    {
      "stepId": "step-4",
      "description": "FIX `landLaneNode` (lane-landing.ts:210). Three ordering defects, all measured, all in the same function.\n\n1. THE TARGET IS RESOLVED AFTER THE SEAT WRITES. Today: spawn wrapper, wait for `existsSync(landedFile)`, THEN `laneCompiledNodeRef(scratch)`. So the file that releases the wait may have been written by any seat. RESOLVE THE COMPILED NODE REF BEFORE THE WRAPPER IS STARTED — it is available from `laneCompiledNodeRef(scratch)` the moment `retireSpecNode` has run, needs no wrapper, and step 3's seat double needs it to build the discriminator anyway. On null, refuse `NO_COMPILED_EXECUTION_NODE` with `laneCompiledNodeDiagnosis` exactly as now, before spawning anything.\n\n2. IT ACCEPTS ANY COMMITTED NODE. `COMMITTED_LINE = /^\\[lander\\] (\\S+): COMMITTED /mu` already CAPTURES the ref and the code discards it. Wait for a landing whose captured ref IS THE TARGET, and keep waiting otherwise — never credit another node's line, and never credit a `policy.validate` log line as a code landing. Keep the escaped `\\]`: under the `u` flag a lone `]` is a SyntaxError thrown at IMPORT, which would take every spec importing this module down and make `test:e2e:browser` find no tests at all.\n\n3. GIT HEAD IS READ WHILE THE WRAPPER IS STILL RUNNING. `laneWorkspaceIdentity(scratch.root)` sits inside the `try`; `killTree` is in the `finally`. A wrapper mid-baseline or mid-verify during that read is how a caller sees BASELINE_WORKSPACE_DIRTY. STOP THE TRACKED CHILDREN FIRST, then observe HEAD and cleanliness. The `finally` teardown STAYS — it must still fire on the refusing and throwing paths (epic rail 4) — so make the second teardown idempotent rather than moving it out.\n\nA POLICY ITEM EXHAUSTING ITS ATTEMPTS IS EXPECTED, NOT THE FAILURE. With the seat double correctly ignoring non-target missions, `policy.validate` never moves and logs STAFFING_ATTEMPTS_EXHAUSTED. That is why `LANDING_MAX_AGENTS = 2` exists — so it cannot starve the node that must land. The fix is that the code node lands ANYWAY; do not chase the policy log line, and do not raise a budget to silence it. A repair that makes the symptom vanish by widening a timeout is a rejectable defect, not a style note (owner ruling).\n\nPRESERVE THE PUBLIC SHAPE. `LaneLanded`, `LaneLandingRefused`, `LANDING_BUDGET_MS`, `LANDED_PATH`, `laneCompiledNodeRef` and `laneCompiledNodeDiagnosis` have SIX consumers measured at HEAD: deploy-environment.spec.ts:55, deploy-fake-docker.spec.ts:33, deploy-migrations.spec.ts:69, lane-preview-arms.ts:29, lane-rollback-incident.ts:38, release-approval.spec.ts:19. None is in this row's roster, so none may need editing — if a change would force one, stop and report rather than taking a seventh path.\n\nThe file is 266 lines at HEAD, already past the 250 target. Keep it UNDER 400 and quote `wc -l`. Bringing it back under 250 would mean splitting a module six specs import and is NOT this row's job.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/lane-landing.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/lane-landing.ts",
        "tests/e2e/control-room/lane-landing.test.ts"
      ],
      "note": "ALL THREE ORDERING DEFECTS FIXED. `pnpm exec vitest run tests/e2e/control-room/lane-landing.test.ts` -> `Test Files 1 passed (1)` / `Tests 15 passed (15)`, EXIT=0, 5.07s (12 seat arms from step 2/3 plus 3 new lander-line arms). `pnpm exec tsc -p tests/e2e/control-room/tsconfig.json` EXIT=0 — its include is `./*.ts`, so all six consumer specs typecheck against the changed module.\n\nDEFECT 1 (target resolved after the seat wrote) — landed in step 3 because the seat needs the ref for its discriminator. `laneCompiledNodeRef(scratch)` now runs at :279, before the wrapper is spawned; null refuses `NO_COMPILED_EXECUTION_NODE <laneCompiledNodeDiagnosis>` with wrapperPid null and nothing started. The post-wait resolution is deleted.\n\nDEFECT 2 (any committed node accepted). `const COMMITTED_LINE = /^\\[lander\\] (\\S+): COMMITTED /mu` is replaced by `committedLine(nodeRef)` (:161), which builds `^\\[lander\\] (<escaped ref>): COMMITTED ` with flags `mu`. THE ESCAPED `\\]` SURVIVES — it is written `\\\\[lander\\\\]` inside the RegExp string, and a lone `]` under `u` is a SyntaxError at IMPORT that would empty test:e2e:browser. New `escapeForPattern` (:169) neuters every metacharacter so a ref is matched as literal text. Capture group 1 is the ref itself, which keeps `watch()`'s contract (daemon-children.ts:68 settles on `exec(seen)?.[1]` and treats a missing capture as a pattern bug). A foreign line cannot match, so the wait CONTINUES rather than crediting it; a genuine miss still surfaces as LANDING_BUDGET_SPENT with the transcript.\n\nProducer format verified, not assumed: `repository-delivery-runtime.ts:91` prints `[lander] ${report.nodeRef}: ${report.outcome} (${report.detail})`.\n\nThree new direct arms pin it and distinguish \"waited for the right one\" from \"waited forever\": target line matches AND captures TARGET; a transcript carrying OTHER's COMMITTED line + TARGET's BASELINE_RECORDED + a work-item COMMITTED line matches NOTHING, then matches once TARGET's own line is appended; TARGET's REFUSED line is not credited; a ref containing `.`, `+`, `(`, `)` matches itself and does NOT match `node:v1:aXbXcd`.\n\nDEFECT 3 (git HEAD read while the wrapper still ran). New idempotent `stopTracked()` closure (`let stopped` guard) is awaited immediately after the COMMITTED match and BEFORE `laneWorkspaceIdentity(scratch.root)`. The `finally` STAYS and now calls the same closure, so the refusing and throwing paths are still covered (epic rail 4) and the second call no-ops instead of re-killing a dead child. Nothing owned by the lane is writing when git is observed.\n\nPOLICY EXHAUSTION LEFT ALONE, as the plan requires: no budget touched. LANDING_BUDGET_MS 180_000, SEAT_WRITE_BUDGET_MS 60_000, LANDING_MAX_AGENTS and LANDING_STAFFING_ATTEMPTS all unchanged.\n\nPUBLIC SHAPE PRESERVED. `LaneLanded`, `LaneLandingRefused`, `LANDING_BUDGET_MS`, `LANDED_PATH`, `laneCompiledNodeRef`, `laneCompiledNodeDiagnosis` unchanged; `committedLine` was module-private and is newly exported for the arms — additive, no consumer edited. Zero of the six consumer files (deploy-environment.spec.ts, deploy-fake-docker.spec.ts, deploy-migrations.spec.ts, lane-preview-arms.ts, lane-rollback-incident.ts, release-approval.spec.ts) touched.\n\n`wc -l tests/e2e/control-room/lane-landing.ts` = 369 (266 at HEAD): under the 400 cap, over the 250 target as the plan anticipated. lane-landing.test.ts = 256.\n\nPeer dirt untouched (contract-goal.spec.ts, lane-contract-goal.ts, lane-multi-node-workspace.ts, .moe/**, apps/daemon/src/http/affordance-read.ts, apps/daemon/src/preview/preview-goal-landing.test.ts).",
      "startedAt": "2026-09-08T11:45:54.178Z",
      "completedAt": "2026-09-08T11:48:03.777Z"
    },
    {
      "stepId": "step-5",
      "description": "THE COMMITTED REGRESSION (DoD 1). NEW `tests/e2e/control-room/lane-landing.spec.ts` — the standalone real-lane Playwright arm, the permanent form of step 1's throwaway. Delete the throwaway now if it is still on disk.\n\nN INDEPENDENT FRESH SEEDED LANES, per the owner ruling — each iteration its own `withDaemonBackedControlRoom({liveCredentials:'ABSENT', fakeGh:'SUCCESS'})` and its own `landLaneNode`, never a second call against a lane that already landed. Pick N from step 1's measured failure rate so the arm would have failed before the fix, and say in a comment WHY that N: an intermittent arm with N too small is green by luck and pins nothing.\n\nASSERT, PER LANE:\n- `landed.ok` is true, with the refusal detail in the failure message so a red is diagnosable;\n- the returned `sha` DIFFERS from the pre-landing baseline sha AND EQUALS the workspace's actual `git rev-parse HEAD` — read from git, never parsed from the transcript;\n- the TARGET node carries a COMMITTED landing receipt, read through the measured production path `readReviewLedgers(store, projectId, new Set([nodeRef])).landings` (`apps/daemon/src/review/review-read-model.ts:288`). Never credit a `[lander]` log line as the receipt, and never credit a policy line at all;\n- `git status --porcelain` in the lane workspace is EMPTY after the helper's teardown;\n- the wrapper pid returned in `LaneLanded.wrapperPid` is GONE — no surviving owned process.\n\nDO NOT DELETE OR RESET DIRTY BYTES TO MAKE THE CLEAN-STATUS ASSERTION PASS (owner fence). If the workspace is dirty, that is the bug and the arm must fail.\n\nTEARDOWN in a `finally` on every path including the throwing one: the lane, the daemon child, the wrapper, any temp root. A leaked wrapper keeps staffing against a store the lane is about to delete, and a leaked process makes every later gate on this board inadmissible.\n\nDO NOT TOUCH THE PEER'S FILES. `contract-goal.spec.ts`, `lane-contract-goal.ts` and `lane-multi-node-workspace.ts` are untracked in the shared worktree and belong to task-6bafd3b9 (worker-76f4ef35). Do not sweep, revert, commit or import them, and do not create a dependency cycle waiting on that consumer.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/lane-landing.spec.ts"
      ],
      "newFiles": [
        "tests/e2e/control-room/lane-landing.spec.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/lane-landing.spec.ts"
      ],
      "note": "COMMITTED REGRESSION LANDED AND GREEN. NEW `tests/e2e/control-room/lane-landing.spec.ts`, 200 lines.\n\n`pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts lane-landing.spec.ts` -> `ok 1 [chromium] > lane-landing.spec.ts:152:1 > landLaneNode lands the code node and leaves a clean workspace on five fresh lanes (43.9s)` / `1 passed (45.3s)`, EXIT=0. Executed spec count: 1 test, 1 spec file. 8.8 s per lane, matching step 1's measurement — a FAST pass is the green signature here, not a slow one waiting out a toBeVisible.\n\nStep 1's throwaway (`zz-baseline-throwaway.spec.ts`) is gone — `ls tests/e2e/control-room/ | grep -i throwaway` returns nothing and git status shows no such path.\n\nN INDEPENDENT FRESH LANES, per the owner ruling. Each of the 5 iterations is its own `withDaemonBackedControlRoom({fakeGh:\"SUCCESS\", liveCredentials:\"ABSENT\"})` — own daemon, dev server, store, scratch git repo, seeded board — and calls `landLaneNode` exactly ONCE. No second call against an already-landed lane, no round-command replay.\n\nWHY N=5, stated in the file header: step 1 measured the pre-fix per-lane clean rate at 1/17, so a pre-fix run of 5 independent lanes goes green by luck with probability (1/17)^5 ~ 7e-7. N=1 would have passed once in seventeen runs and pinned nothing. The DISCRIMINATOR is the clean workspace, NOT `landed.ok` — `landed.ok` was true 17/17 before the fix and is not what failed.\n\nASSERTED PER LANE, each with a `describeAudit` failure message carrying nodeRef, baseline, landed, head, receipt, wrapperPid, survivors, porcelain and refusal detail:\n- `landed.ok` true (refusal detail asserted null, so a red quotes the wrapper transcript);\n- returned `sha` != the pre-landing baseline sha AND == the workspace's own `git rev-parse --verify HEAD^{commit}` — both read from git via `spawnSync(\"git\", [\"-C\", ws, ...], {shell:false})`, never parsed from the transcript;\n- the TARGET node carries a COMMITTED landing receipt through the production path `readReviewLedgers(store, projectId, new Set([nodeRef])).landings.get(nodeRef)` (review-read-model.ts:288), and `receipt.commit.sha` EQUALS the returned sha. No `[lander]` log line is credited; a policy item never earns a receipt at all;\n- `git status --porcelain` in the lane workspace is EMPTY after landLaneNode's teardown — THE REGRESSION;\n- `survivingPids([landed.wrapperPid])` is `[]` — no wrapper outlives the helper.\n\nANTI-VACUITY: the sweep asserts `laneRefusals` is `[]` and `audits` has length 5 BEFORE the per-lane loop, so a run where zero lanes opened cannot pass with an empty loop.\n\nNO FENCE BREACHED. Nothing is deleted, reset or swept to make the clean-status assertion pass — the porcelain is measured and REPORTED; a dirty workspace fails the arm. No receipt is hand-seeded. No budget raised.\n\nTEARDOWN: `withDaemonBackedControlRoom` owns lane/daemon/dev-server/scratch teardown in its own `finally` on every path including a throwing one; `landLaneNode` owns the wrapper the same way. Audits are collected and asserted AFTER each lane closes, so an assertion cannot abandon the remaining lanes or strand a live child.\n\nPEER FILES UNTOUCHED: contract-goal.spec.ts, lane-contract-goal.ts and lane-multi-node-workspace.ts (task-6bafd3b9 / worker-76f4ef35) are not imported, edited, swept or waited on. No dependency added.\n\n`pnpm exec tsc -p tests/e2e/control-room/tsconfig.json` EXIT=0. browser-e2e-gate lease held under this taskId (expires 2026-09-09T11:50Z).",
      "startedAt": "2026-09-08T11:48:08.300Z",
      "completedAt": "2026-09-08T11:51:18.985Z"
    },
    {
      "stepId": "step-6",
      "description": "GATES (DoD 3), each standalone and foreground, each quoted with the count line that proves it EXECUTED. Exit 0 alone is not evidence: 'No test files found' and '0 passed' are also exit 0, so the discriminator is the printed `Test Files N passed` count — and for Playwright, the executed spec count and exit status, which DoD 3 names explicitly.\n\n- `pnpm exec vitest run tests/e2e/control-room/lane-landing.test.ts` — the direct arms, seconds not minutes. This is the fast oracle for steps 2-4.\n- `pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts lane-landing.spec.ts contract-goal.spec.ts` — the focused browser leg, under the browser-e2e-gate lease. `contract-goal.spec.ts` is the DEPENDENT arm DoD 3 requires and is the PEER'S UNTRACKED FILE: you RUN it as evidence, you do not own, edit or commit it. Name its owner and its untracked state in the record, and if it fails for a reason inside its own bytes rather than in the landing helper, attribute it there rather than absorbing it.\n- `pnpm test` — the root lane, which DOES include `tests/**/*.test.ts` (vitest.config.ts include), so it covers this row's new direct arms. It covers NO apps/** file; say so.\n- `pnpm typecheck` — and note that `tests/e2e/control-room/tsconfig.json` exists and needs its own explicit typecheck if the root leg does not reach it. Verify which is true rather than assuming.\n- `pnpm --filter @moe/daemon test` and `pnpm --filter @moe/control-room test`.\n- `pnpm test:e2e:browser` — the full browser lane, for the six existing consumers of `landLaneNode` listed in step 4. This is the regression that matters most: the fix changes when the helper resolves its target and when it stops its children, and every one of those specs drives it.\n\nPLAYWRIGHT INVERTS THE USUAL INSTINCT: a FAST pass is the green signature and a SLOW run is the red one waiting out a 30 s `toBeVisible`. Do not read a long run as thoroughness.\n\nATTRIBUTE FOREIGN RED, never hide it and never fabricate green. Path-attributed baseline: gate failures at the merge-base, gate failures at HEAD, subtract, intersect with this row's three owned paths; complete only when that intersection is EMPTY. Disclose foreign red verbatim with exact failing lines and paths. Acquire the shared gate lease rather than running a convoy beside other seats, and do not re-run a full suite repeatedly — it degrades every other seat's gate.",
      "status": "IN_PROGRESS",
      "affectedFiles": [],
      "startedAt": "2026-09-08T11:51:30.861Z"
    },
    {
      "stepId": "step-7",
      "description": "ADVERSARIAL SELF-REVIEW AND THE MUTATION DRILL. Load `adversarial-self-review` and `regression-check`.\n\nREAD YOUR OWN DIFF AS THE BUG REPORTER:\n- Does the seat double still write for a mission that merely MENTIONS the target ref somewhere in its instructions, rather than carrying the code-node claim clause? An over-broad match reintroduces the bug in a shape the arms do not cover.\n- Does it write for the target on a SECOND staffing after the round's digest was taken — and are those bytes identical?\n- Does `landLaneNode` still return the TARGET's sha if another node commits first, or does it now hang until the budget instead? Both are wrong in different ways; the arm must distinguish 'waited for the right one' from 'waited forever'.\n- Is the `finally` teardown still reached on the refusing and throwing paths after the children were stopped early — and is the second kill idempotent rather than throwing on an already-dead child?\n- Did the escaped `\\]` in COMMITTED_LINE survive? An unescaped one is a SyntaxError at IMPORT that empties the whole browser lane.\n- Did you raise ANY budget, seed ANY receipt, delete or reset ANY dirty bytes, or edit ANY production authority file? All four are owner-fenced rejections.\n- Did any of the three owned paths touch the peer's untracked contract-goal files?\n\nMUTATION DRILL, the arms' only real oracle. `git status --porcelain` the exact mutated paths FIRST. `lane-landing.test.ts` and `lane-landing.spec.ts` are NEW and UNTRACKED, so `git diff -- <path>` certifies NOTHING for them — empty before, during and after — and a silently unrestored mutation rides into the commit wearing a clean diff. Back each up OUT OF TREE under %TEMP% in a path carrying THIS TASK ID (a path keyed only on a worker id can hold a stale vintage whose SHA256 still matches after a restore that deleted the real work), hash the LIVE file at window-open, and prove the restore by SHA256 equality in a call SEPARATE from the restore. `git diff` is a valid restore proof for the TRACKED `lane-landing.ts` only. Do not drill on disk while holding a shared full-suite lease.\n\nMUTATE THE PRODUCTION SURFACE AND CONFIRM RED, per the global rail: revert the stdin discriminator and confirm the policy arm goes red; restore the timestamp and confirm the byte-stability arm goes red; drop the target-ref match in COMMITTED_LINE and confirm the spec goes red. An arm that stays green under its own mutation is testing nothing.\n\nReport every path you created or modified in `complete_step.modifiedFiles`.",
      "status": "PENDING",
      "affectedFiles": []
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": "worker-319873d7",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Both resume conditions met. Owner ruled DoD 1 semantics: \"call landLaneNode repeatedly\" means INDEPENDENT FRESH SEEDED LANES (N separate lanes), not repeated calls on one already-landed lane — so no round-command replay or completed-node lifecycle semantics need defining. That reading matches the reported evidence, which was an intermittent across fresh SHIPPED fakeGh=SUCCESS lanes, and it is the only shape in which DoD 1's \"previously failed\" regression can actually fail before the fix. The architect's bounded three-path scope (lane-landing.ts plus new lane-landing.test.ts and lane-landing.spec.ts) is approved as proposed, with their own fences reinforced: no larger budgets, no hand-seeded receipts, no deletion/reset of dirty bytes, no production authority edits, and DoD 2 stands. Root of a three-row chain (this -> task-6bafd3b9 -> task-817d893f); 6bafd3b9 auto-unblocks when this reaches DONE. Authority is comment-c21629a4; block record preserved verbatim there before this transition.",
  "createdBy": "WORKER",
  "parentTaskId": "task-6bafd3b9439640cba358dd6e79329c9d",
  "priority": "HIGH",
  "order": 72,
  "comments": [
    {
      "id": "comment-6de16a94ad0e47d8826fe5e24a01b74a",
      "author": "worker-76f4ef35",
      "content": "MEASURED before our contract graph is created: contract-goal.spec.ts calls createLaneContractGoal; its first effect after resolving scratch is await landLaneNode(lane). No prepared test files, graph seal, new seat or criterion service has run when the following failure occurs. Latest command `pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts contract-goal.spec.ts` -> 1 passed (workspace-only arm), 1 failed, 3.3m, exit 1. Assertion at lane-contract-goal.ts:73: legacy.ok false; detail LANDING_BUDGET_SPENT followed by repeated `[wrapper] policy.validate@e2e-proj-qknzux-policy: STAFFING_ATTEMPTS_EXHAUSTED`. Earlier run reached our next wrapper, which refused both producer nodes with BASELINE_WORKSPACE_DIRTY / REPOSITORY_DELIVERY_BASELINE_UNAVAILABLE (REPOSITORY_DELIVERY). That earlier run did not capture which dirty path, so the common-cause restaff/write race is a HYPOTHESIS, not established causation. Another run got past legacy landing with a clean workspace; failure is intermittent.\n\nMinimal standalone REPRO SOURCE (new temporary Playwright spec next to lane-landing.ts; not yet independently run as this reduced source):\nimport { test, expect } from '@playwright/test';\nimport { execFileSync } from 'node:child_process';\nimport { withDaemonBackedControlRoom } from './daemon-ports.js';\nimport { landLaneNode } from './lane-landing.js';\ntest('legacy landing returns a clean baseline', async () => {\n  test.setTimeout(240_000);\n  const result = await withDaemonBackedControlRoom({liveCredentials:'ABSENT', fakeGh:'SUCCESS'}, async lane => {\n    const landed = await landLaneNode(lane);\n    expect(landed.ok, JSON.stringify(landed)).toBe(true);\n    expect(execFileSync('git', ['status','--porcelain'], {cwd:lane.workspace, encoding:'utf8'})).toBe('');\n  });\n  expect(result.ok, JSON.stringify(result)).toBe(true);\n});\n\nSource evidence to investigate, not proof of root cause: landingSeatDouble ignores mission/arguments and always writes timestamped LANDED_PATH; landLaneNode uses LANDING_MAX_AGENTS=2, continuous wrapper, notices a file and submits only one round, kills only after first COMMITTED line. Policy-only seats therefore can write the same file as the code seat. No fixes made to shared lane modules.",
      "timestamp": "2026-09-07T18:54:00.831Z"
    },
    {
      "id": "comment-24801f4da15a46f789a18e49b059db02",
      "author": "architect-f362f7a6",
      "content": "ARCHITECT AUDIT (read-only, latest HEAD aa438b789ed579e6f3449fa8b17600a109fcc059). Confirmed reporter comment-6de16a94 keeps common-cause hypothesis distinct from measured failures. Current landingSeatDouble always timestamp-writes LANDED_PATH and ignores stdin. Production agent-spawner.ts:400 writes request.mission to stdin; agent-mission-text.ts:codeMission starts with the exact quoted target code-node durable-claim header, unlike mission(policy.validate). landLaneNode currently resolves its target only AFTER any seat writes, accepts any [lander] COMMITTED node, and reads git HEAD before finally kills the wrapper. This concretely permits non-code byte/signal interference; whether it explains both reported failures needs red/green evidence, not assertion.\nProposed bounded ownership: tests/e2e/control-room/lane-landing.ts plus NEW lane-landing.test.ts and lane-landing.spec.ts. Resolve intended compiled node before spawn, target-match the actual mission header (not arbitrary substring in instructions), keep repeat writes byte-stable without truncation and ignore policy/other-node missions, wait for target-specific landing, stop tracked children before final git clean/HEAD observation; no larger budgets, seeded receipts, deletion/reset of dirty bytes or production authority edits. Keep native .cmd/.sh bridges. Direct generated-child tests should feed actual codeMission/mission output, exercise policy before/after correct-node writes, different-node and empty input, repeat/concurrent invocations, preserved foreign bytes and genuine IO failures. Standalone real lane must assert SHA differs from baseline and equals actual git HEAD plus the target COMMITTED receipt, clean git status after helper teardown, and no surviving owned pids. Existing readReviewLedgers(store,projectId,new Set([nodeRef])).landings is the measured read-only receipt path. Never credit policy log lines as code landing.\nCandidate primary focused browser command: pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts lane-landing.spec.ts contract-goal.spec.ts (browser-e2e-gate; record exact nonzero counts/status). Direct helper command: pnpm exec vitest run tests/e2e/control-room/lane-landing.test.ts. Full repo gates per rails; tests/e2e/control-room/tsconfig.json needs explicit typecheck. Dependent contract-goal files currently belong to worker-76f4ef35 and are untracked; do NOT include them in this task's delivered diff or create a dependency cycle waiting for the consumer to be DONE. Its execution evidence must identify its exact cooperating bytes and owner.\nAsked human in REPL to confirm this approach and clarify 'repeatedly': separate fresh seeded lanes or repeat the same already-landed lane (latter changes command replay/target lifecycle requirements). No confirmation received yet; no speculative plan submitted, no source edits/tests claimed. Also context labels this bug isEpicFinal:true even though the existing Gates integration/hardening row owns whole-epic hardening; do not silently expand this 3-file bug into the entire epic.",
      "timestamp": "2026-09-07T18:59:57.960Z"
    },
    {
      "id": "comment-c21629a4f1114756a8c030ae6a3e9121",
      "author": "governor-9af9af37",
      "content": "🔒 OWNER RULING 07:07Z — **DoD 1 SEMANTICS ANSWERED: INDEPENDENT FRESH SEEDED LANES.** @architect-f362f7a6 asked this in the REPL and got no answer; I put it to the owner and they ruled. This comment is the authority. Recorded BEFORE the status change because BLOCKED→PLANNING nulls the block quartet; verbatim block record at the end.\n\n**THE RULING.** \"A fresh seeded release-configured lane can call `landLaneNode` repeatedly\" means **each call gets its own fresh seeded lane** — N independent lanes, not N calls against one already-landed lane. You do NOT need to define round-command replay semantics or completed-node lifecycle for this row; both were the things that made the other reading expensive, and neither is now in scope.\n\n**WHY THIS READING IS ALSO THE ONE THE BUG SUPPORTS**, so the DoD and the evidence agree rather than merely coexisting: the reported failure was \"on a fresh SHIPPED `fakeGh=SUCCESS` lane **INTERMITTENTLY**\" (task-6bafd3b9's blockedReason, evidence comment-6de16a94). An intermittent that appears across separate fresh runs is reproduced by repeating the fresh run, not by re-calling a lane that already succeeded. The regression this row owes is one that FAILS BEFORE THE FIX — DoD 1 says exactly that — and only the independent-lanes shape can fail that way.\n\n**YOUR DIAGNOSIS IS ACCEPTED AS THE WORKING MECHANISM.** From your audit (comment-24801f4d) at HEAD aa438b78: `landLaneNode` resolves its target only AFTER any seat writes, accepts ANY `[lander] COMMITTED` node, and reads git HEAD before it kills the wrapper — while `agent-spawner.ts:400` writes `request.mission` to stdin and `agent-mission-text.ts:codeMission` opens with the exact code-node durable-claim header that `mission(policy.validate)` does not. That concretely permits a policy seat to consume the code node's landing signal, which is precisely the two symptoms reported (LANDING_BUDGET_SPENT with STAFFING_ATTEMPTS_EXHAUSTED, and BASELINE_WORKSPACE_DIRTY). You said it needs red/green evidence rather than assertion — right, and DoD 1's \"previously failed\" regression is where you prove it.\n\n**YOUR PROPOSED BOUNDED SCOPE IS APPROVED AS PROPOSED:** `tests/e2e/control-room/lane-landing.ts` plus NEW `lane-landing.test.ts` and `lane-landing.spec.ts` — three owned paths. Resolve the intended compiled node BEFORE spawn; target-match the actual mission header rather than an arbitrary substring in instructions; keep repeat writes byte-stable without truncation; ignore policy and other-node missions; wait for target-specific landing; stop tracked children before the final git clean and HEAD observation. Keep the native `.cmd`/`.sh` bridges.\n\n⚠️ **AND THE FENCES YOU DREW YOURSELF, which I am reinforcing rather than relaxing:** no larger budgets, no hand-seeded receipts, no deletion or reset of dirty bytes, no production authority edits, and DoD 2 stands — policy/non-code staffing must not be able to rewrite a code node's submitted bytes or consume its landing signal, with the real wrapper, verifier and landing retained. A fix that makes the symptom disappear by widening a timeout would satisfy nobody and I would treat it as a rejectable defect, not a style note.\n\n**STATUS: BLOCKED → PLANNING.** Both resume conditions in your block are now met — the semantics are ruled and the bounded repair is confirmed. Plan and submit. This is the root of a three-row chain (this row → task-6bafd3b9 → task-817d893f, the epic's browser release proof), so it is the highest-value row on the board right now; task-6bafd3b9 auto-unblocks the moment this reaches DONE.\n\nOne note for whoever implements: task-6bafd3b9 already has three declared paths written and UNTRACKED in the shared worktree (`lane-multi-node-workspace.ts`, `lane-contract-goal.ts`, `contract-goal.spec.ts`, 268 lines). They are that row's, not this one's. Do not sweep, revert or commit them here — note them and continue.\n\nVERBATIM BLOCK RECORD, as it stood before this transition:\n---\nAwaiting human confirmation requested in REPL for bounded mission-aware landing-helper repair, and acceptance clarification: does 'call landLaneNode repeatedly' mean multiple independent fresh seeded lanes, or repeated calls on the same already-landed lane? Those differ in round command replay and completed-node lifecycle; submitting the former without confirmation would guess the DoD. Read-only source/repro audit and proposed three owned paths are preserved in the latest architect comment; no source changes or plan submitted. Reporter was asked for semantics; governor notified.\n---\nNo dependsOn. Audit: comment-24801f4d. Reporter evidence: comment-6de16a94 on task-6bafd3b9.",
      "timestamp": "2026-09-08T07:05:46.499Z"
    },
    {
      "id": "comment-7baa79e654cc4f57b57174f12263cafd",
      "author": "worker-319873d7",
      "content": "STEP 1 — THE MEASURED RED (baseline at HEAD 7fe37943, worker-319873d7, 2026-09-08T11:35-11:42Z, under the browser-e2e-gate lease).\n\nCOMMAND (throwaway spec `tests/e2e/control-room/zz-baseline-throwaway.spec.ts`, since DELETED):\n  pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts zz-baseline-throwaway.spec.ts\nEach iteration: its own `withDaemonBackedControlRoom({fakeGh:\"SUCCESS\", liveCredentials:\"ABSENT\"})`, one `landLaneNode(lane)`, then `git status --porcelain` in `lane.workspace`. N INDEPENDENT FRESH SEEDED LANES per the owner ruling (comment-c21629a4).\n\nTALLY, 17 fresh lanes across two runs (N=5 then N=12):\n- `landed.ok === true`: 17/17. sha real and distinct each time.\n- workspace CLEAN after the helper's teardown: 0/17. Every single lane ended `M landed-by-the-seat.txt`.\n- LANDING_BUDGET_SPENT: 0/17. BASELINE_WORKSPACE_DIRTY: 0/17. SEAT_NEVER_WROTE: 0/17.\nRun 2 (N=12) printed `##### TALLY passed=0/12 #####`, `1 failed`, EXIT=1 — that is the \"previously failed\" red DoD 1 asks for, and it is DETERMINISTIC, not intermittent.\n\nTHE MECHANISM, MEASURED VERBATIM on a third single-lane probe (`git show HEAD:<file>` vs the worktree, same run):\n  committed : \"landed at 2026-09-08T11:39:21.181Z\"   (blob 425c2ec)\n  worktree  : \"landed at 2026-09-08T11:39:24.081Z\"   (blob e648c1e)\n  porcelain : \"M landed-by-the-seat.txt\"\n  git log   : \"7f369ef Land node-e2e-ygwhpc.\" / \"9568ca1 Lane workspace baseline\"\n  landed    : { ok:true, sha:\"7f369ef6d474c33da50b041f14626828cebd7baf\", wrapperPid:9976 }\n2.9 s AFTER the lander committed 425c2ec, a further staffing pass re-ran the seat double and rewrote the same path with a new ISO timestamp. That is BOTH step-3 defects in one measurement: a non-target mission's seat writes the TARGET code node's file (defect 1, `landingSeatDouble` reads no stdin), and it writes UNSTABLE bytes (defect 2, `new Date().toISOString()`). ~2.9 s is ~6 wrapper passes at WRAPPER_INTERVAL_MS=500.\n\nWHAT DID NOT REPRODUCE. The reporter's LANDING_BUDGET_SPENT / BASELINE_WORKSPACE_DIRTY refusals did not fire in 17 lanes on this host (fast box, 17/17 landed in ~9 s each). I did NOT declare the bug absent and did not raise a budget: the same unstable-bytes/wrong-seat mechanism is the documented cause of both — the round's SUBMITTED_BYTES digest is taken over exactly these bytes (`lane-review-round.ts` over `LANDED_PATH`), so a restaff after the digest invalidates the round and burns staffing attempts. The dirty workspace is that mechanism's DETERMINISTIC face; the refusals are its racy face. Fixing the mechanism is what step 3/4 do.",
      "timestamp": "2026-09-08T11:39:57.530Z"
    },
    {
      "id": "comment-4bf34eacf6c04648b94d38f6e13e73c5",
      "author": "worker-319873d7",
      "content": "STEP 1 BASELINE RED — MEASURED, WITH A DENOMINATOR (worker-319873d7, 2026-09-08T11:35-11:40Z, tree = HEAD 0be4f7fc + peer dirt, lane-landing.ts UNMODIFIED).\n\nThrowaway spec `tests/e2e/control-room/zz-baseline-throwaway.spec.ts` (deleted before step 5): N independent fresh seeded lanes, each its own `withDaemonBackedControlRoom({liveCredentials:\"ABSENT\", fakeGh:\"SUCCESS\"})` + one `landLaneNode(lane)`, then `git status --porcelain` + `git diff` in the lane workspace. Never fails fast; every lane's outcome recorded, tally printed.\n\nCOMMAND: `pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts zz-baseline-throwaway.spec.ts`  (under the browser-e2e-gate lease, acquired 11:35:20Z)\n\nRUN A, N=5 — TALLY passed=0/5, exit 1, 42.4s.\n  All 5: `landed.ok=true` with a real sha, and ALL 5 dirty: `M landed-by-the-seat.txt`.\n\nRUN B, N=12 (with `git diff` capture) — TALLY passed=1/12, exit 1.\n  11/12 dirty with the SAME single-file modification; 1/12 (run 4) clean.\n\nTHE DIFF IS THE MECHANISM, VERBATIM (run 1/12):\n```\ndiff --git a/landed-by-the-seat.txt b/landed-by-the-seat.txt\nindex 2be37df..9dc90c0 100644\n--- a/landed-by-the-seat.txt\n+++ b/landed-by-the-seat.txt\n@@ -1 +1 @@\n-landed at 2026-09-08T11:37:55.168Z\n+landed at 2026-09-08T11:37:58.080Z\n```\nCommitted bytes vs worktree bytes differ by ~2.9 s of wall clock, run after run (11:38:03.531Z -> 11:38:06.473Z; 11:38:12.744Z -> 11:38:15.631Z). That is the seat double being RE-STAFFED after the lander already committed, and rewriting the exact file the lander committed with a fresh `new Date().toISOString()`.\n\nWHAT THIS CONFIRMS, at HEAD, in production bytes:\n1. `landingSeatDouble` (lane-landing.ts:187) reads NO stdin and writes unconditionally, so it fires for EVERY staffed mission — including the `policy.validate` item that shares the board.\n2. Its content is `\"landed at \" + new Date().toISOString()`, so a second staffing is NOT byte-stable. `lane-review-round.ts:49` digests exactly these bytes as SUBMITTED_BYTES, so a restaff after the digest invalidates the round — the reported LANDING_BUDGET_SPENT / repeated STAFFING_ATTEMPTS_EXHAUSTED.\n3. `laneWorkspaceIdentity(scratch.root)` (lane-landing.ts:257) is read INSIDE the `try` while the wrapper still lives, and `killTree` only runs in the `finally` — so the caller observes a workspace the wrapper is still writing to. That is the reported BASELINE_WORKSPACE_DIRTY, and it is the 11/12 measured here.\n\nThe reported symptom was an INTERMITTENT; measured as a fresh-lane sweep it is 11/12 — the same defect, made legible by asserting the workspace is clean rather than only that the call returned ok. DoD 1's \"previously failed\" clause is discharged by this tally: a committed regression asserting `git status --porcelain === \"\"` fails 11 times in 12 at this tree.\n\nN FOR THE COMMITTED ARM: at an 11/12 per-lane failure rate, N=3 lanes gives (1/12)^3 ≈ 1-in-1728 odds of passing by luck. Step 5 will use N=3 and say so.\n\nNOTE (peer activity, not a stop condition): the throwaway file was edited twice by another session while it sat on disk (N 5 -> 12, then replaced with a different probe body). It is untracked, disposable, and deleted at step 5. `tests/e2e/control-room/lane-landing.ts` is TRACKED and was verified clean (`git status --porcelain` shows it not at all) before and after both runs. Peer untracked dirt present throughout and untouched: contract-goal.spec.ts, lane-contract-goal.ts, lane-multi-node-workspace.ts (task-6bafd3b9), preview-goal-landing.test.ts, Moe-NG_Deep_Review_2026-09-05.md, .moe/resources/*.json.",
      "timestamp": "2026-09-08T11:40:12.690Z"
    },
    {
      "id": "comment-44e67cbaecad465a944838956e23a3ab",
      "author": "worker-319873d7",
      "content": "DOUBLE-ASSIGNED SEAT — SECOND SESSION YIELDING, NO FILES TOUCHED (2026-09-08T11:41Z).\n\nTWO LIVE CLI SESSIONS ARE RUNNING THIS ROW UNDER THE SAME workerId (worker-319873d7). I am the second. Measured, not inferred:\n- `Get-CimInstance Win32_Process` shows THREE worker claude.exe seats started today: 14:34:20 (mcp-config-worker-61964), 14:34:39 (worker-58096), 14:39:16 (worker-9404). Two of them are on this row.\n- The other session completed step-1 and wrote comment-7baa79e6 at ~11:40:12Z while my own step-1 run was still in flight; my `complete_step(step-1)` was refused `[INVALID_STATE] Step is in COMPLETED state`.\n- My `start_step(step-2)` was refused `[INVALID_STATE] Step is in IN_PROGRESS state` — the other session already holds it.\n- Liveness probe, 11:41:05-11:41:15Z: `tests/e2e/control-room/lane-landing.test.ts` appeared on disk within 10 s, and the task JSON was rewritten at 14:41:15 local. That seat is ACTIVE, not a dead session that stranded an IN_PROGRESS step.\n\nWHY I STOPPED RATHER THAN CONTINUED. Steps 2-5 all write the same three paths (lane-landing.ts, lane-landing.test.ts, lane-landing.spec.ts). Two sessions editing them concurrently produces torn bytes in the one production file this row exists to repair. Racing would not deliver the row faster; it would corrupt it.\n\nWHAT I DID NOT DO, deliberately, because the other seat is mid-flight:\n- No edit to any file. `tests/e2e/control-room/lane-landing.ts` verified clean (`git status --porcelain` empty for that path) at yield time.\n- No `report_blocked` — that would flip a row a live seat is working to BLOCKED and stop the wrapper relaunching it.\n- No `release_task` / `set_task_status` — both would strip the live seat's assignment mid-step.\n- No `declare_files` — my session owns no path, and declaring would make my exit commit sweep bytes the other seat is still writing.\n- My two throwaway files are GONE: `zz-baseline-throwaway.spec.ts` (deleted by the other seat after it read my run) and `zz-probe-import.ts` (deleted in the same command that created it). My session's exit commit therefore has nothing to land.\n\nWHAT SURVIVES FROM MY SESSION AND IS USEFUL TO THE HOLDER:\n1. comment-4bf34eac — my independent step-1 baseline, measured on lane-landing.ts at HEAD with NO edit applied. It corroborates comment-7baa79e6 from a separate process: N=5 -> passed 0/5 (42.4 s); N=12 -> passed 1/12; `landed.ok` true every time; the workspace dirty with `M landed-by-the-seat.txt` in 16 of 17 lanes across both runs. It also carries the `git diff` hunks the other seat's tally summarises, e.g. `-landed at 2026-09-08T11:37:55.168Z` / `+landed at 2026-09-08T11:37:58.080Z`.\n   NOTE A DISCREPANCY WORTH SETTLING: comment-7baa79e6 records \"workspace CLEAN 0/17\". My N=12 log shows run 4/12 came back `dirty=\"\"` — 1 clean lane in 17, not 0. It does not change the conclusion (16/17 red is still deterministic enough that step 5's N=3 is ~1-in-4900 to pass by luck), but the holder should not write \"0/17\" into a completion record my log contradicts.\n2. `pnpm exec tsc -p tests/e2e/control-room/tsconfig.json` exits 0 at this tree and compiles 1564 files, and a deleted probe importing `codeMission`/`mission` from `../../../apps/daemon/src/orchestrator/agent-mission-text.js` typechecked clean under it. So step 2's cross-package import is admissible and that tsc leg is a real, non-vacuous gate.\n\nTHE browser-e2e-gate LEASE STAYS HELD. It is keyed on this taskId, not on my session, so it belongs to the row; the holder's own `acquire_resource` on the same taskId is an idempotent renew. Releasing it would let another row take the gate out from under a seat that still needs it for step 6.\n\nFOR A GOVERNOR: this is the wrapper double-assign hazard, not a worker defect. The row is in good hands — the live seat is ahead of me and its step-1 record is sound. Nothing here needs unblocking.",
      "timestamp": "2026-09-08T11:42:40.158Z"
    },
    {
      "id": "comment-dfae717c2c3f468a85fab22eb8316302",
      "author": "worker-319873d7",
      "content": "SESSION COLLISION ON THIS ROW — READ BEFORE TRIAGING. Filed by the ORIGINAL session (claude.exe pid 26052, started 2026-09-08T14:34:20 local = 11:34Z, Moe-Session worker-319873d7@2026-09-08T11:34:35Z, the one that owns commit b16c544a).\n\nWHAT HAPPENED. The wrapper launched two FURTHER claude.exe sessions under the SAME workerId while this one was still running: pids 42944 (14:39:16) and 52000 (14:44:38). Measured with `Get-CimInstance Win32_Process` at 14:47 local; 52000 was still alive. `tasklist` from the Bash sandbox reports nothing, so CIM is the only census that works here.\n\nDIVISION OF WORK, as it actually landed:\n- step-1 (the measured red) and step-2 (the direct arms) were completed by THIS session. Their notes and comment-7baa79e6 are mine. `tests/e2e/control-room/lane-landing.test.ts` is my file and is on the branch in b16c544a (`Moe-Session: worker-319873d7@2026-09-08T11:34:35Z`, 226 lines, byte-identical to disk).\n- step-3 was completed by a LATER session (its note says \"Prior session recorded these RED 11 failed | 1 passed at HEAD\", which is my step-2 result). Its `lane-landing.ts` edit REPLACED mine on disk. I had made the same two fixes independently and reached the same green (`Tests 12 passed (12)`, EXIT=0) before its note appeared; the surviving version is functionally equivalent — same `landingSeatClaim`/`landedSeatBytes` exports, same stdin discrimination, same early `laneCompiledNodeRef` resolution. No work was lost and nothing is half-applied.\n- step-4 is IN_PROGRESS under that later session. My `complete_step` for step-3 was refused `[INVALID_STATE] Step is in COMPLETED state, expected IN_PROGRESS`, which is the documented signature of the purged seat.\n\nWHY I STOPPED RATHER THAN CONTINUED. Two sessions editing the same three files in one shared checkout is how a half-applied fix ships. I already watched my `lane-landing.ts` bytes be replaced mid-step. Continuing would race the live seat on `lane-landing.ts`, `lane-landing.test.ts` and the not-yet-created `lane-landing.spec.ts`. I did NOT `report_blocked`: that would flip the row to BLOCKED and stop the live session that is legitimately finishing it. I did NOT revert, stash or re-apply anything.\n\nSTATE I AM LEAVING BEHIND, for whoever finishes steps 4-7:\n- `browser-e2e-gate` lease is HELD, task-keyed to this row (acquired 11:35:20Z, note names this row), so the live session inherits it and does not need to re-acquire. Release it after step 6.\n- Step 1's baseline is comment-7baa79e6: 17 fresh lanes, `landed.ok` 17/17, clean workspace 0/17, `##### TALLY passed=0/12 #####` with EXIT=1. That is DoD 1's \"previously failed\" evidence and does not need re-measuring.\n- Both step-1 throwaways are DELETED and confirmed absent; no `zz-*` file remains under tests/e2e/control-room.\n- Peer dirt untouched throughout: contract-goal.spec.ts, lane-contract-goal.ts, lane-multi-node-workspace.ts are still `??` and belong to task-6bafd3b9 / worker-76f4ef35.",
      "timestamp": "2026-09-08T11:48:01.068Z"
    },
    {
      "id": "comment-06e1410105364af19a856229106be31b",
      "author": "worker-319873d7",
      "content": "STEP-5 DELIVERABLE WAS DESTROYED BY A TWIN SESSION OF THIS SAME workerId, AND REBUILT. Disclosing on the row so QA grades the bytes that are actually here, not the bytes the step-5 note was written about. Chat copy: msg-3dcd06ce (chan-388a290f).\n\nTWO LIVE SESSIONS, ONE workerId, ONE ROW — fourth occurrence on this epic. Census `Get-CimInstance Win32_Process -Filter \"Name='claude.exe'\"` at 2026-09-08T11:55:56Z:\n- PID 52000, `moe-mcp-config-worker-58096.json`, started 11:44:38Z — completed step-5 at 11:51:19Z and holds step-6 (task JSON last written 11:51:30Z). STILL ALIVE.\n- PID 31468, `moe-mcp-config-worker-61964.json`, started 11:49:35Z — me.\nBoth `%TEMP%\\moe-system-prompt-worker-{58096,61964}.md` carry `workerId=worker-319873d7` and `Claimed task id: task-da435e66fe8247fda0d441e61e2c4b91`.\n\nTHE DAMAGE, MEASURED. At my session start `git status --porcelain` showed `tests/e2e/control-room/lane-landing.spec.ts` ABSENT and the board showed step-5 IN_PROGRESS, so I wrote it. In that window the twin completed step-5 with its own 200-line copy (`1 passed (45.3s)`, EXIT 0, five fresh lanes) and moved to step-6. My write at ~11:53Z overwrote those bytes. They were UNTRACKED: `git log --all --oneline -- tests/e2e/control-room/lane-landing.spec.ts` is EMPTY and `git cat-file -e HEAD:tests/e2e/control-room/lane-landing.spec.ts` answers \"exists on disk, but not in 'HEAD'\". Not recoverable. This is my error, not the twin's.\n\nTHE REBUILD, SHAPED TO THE RECORD RATHER THAN TO MY CONVENIENCE. I did not leave a weaker file sitting under the twin's step-5 note. The delivered file is ONE test titled exactly `landLaneNode lands the code node and leaves a clean workspace on five fresh lanes`, FIVE independent fresh lanes, each its own `withDaemonBackedControlRoom({fakeGh:\"SUCCESS\", liveCredentials:\"ABSENT\"})` and its own `landLaneNode` — never a second call against a landed lane (which the helper cannot serve anyway: `retireSpecNode` deletes the node spec `resolveLaneScratch` matches on).\n\nPER-LANE ASSERTIONS, all against measured production surfaces:\n- `landed.ok`, with the refusal `detail` in the failure message;\n- `landed.sha` EQUALS `git rev-parse --verify HEAD^{commit}` read from the lane workspace with GIT_* stripped, and DIFFERS from the pre-landing `scratch.workspaceSha`;\n- `git status --porcelain` in the lane workspace is EMPTY — the arm that was 0/17 before the fix. Nothing is deleted or reset to make it pass;\n- the target's landing receipt via the production `readReviewLedgers(store, projectId, new Set([nodeRef])).landings` (`apps/daemon/src/review/review-read-model.ts:288`): `outcome === \"COMMITTED\"`, `subjectRef === nodeRef`, `commit.sha === landed.sha`, and `LANDED_PATH` among `commit.files`. No `[lander]` log line is credited as a receipt;\n- `survivingPids([landed.wrapperPid])` is `[]`, asked FIRST so a leaked wrapper is reported even when a later assertion is about to fail.\n\nFRESH RUN OF THE REBUILT FILE:\n`pnpm exec playwright test -c tests/e2e/control-room/playwright.config.ts lane-landing.spec.ts`\n```\n##### LANE TALLY passed=5/5 #####\nlane 1/5..lane 5/5: landed, clean, receipt COMMITTED\n  ok 1 [chromium] > lane-landing.spec.ts:131:1 > landLaneNode lands the code node and leaves a clean workspace on five fresh lanes (49.1s)\n  1 passed (50.4s)\n```\nEXIT 0. Executed spec count: 1 spec file, 1 test, 5 lanes. `pnpm exec tsc -p tests/e2e/control-room/tsconfig.json` EXIT 0.\n\nTHE ONE RESPECT IN WHICH THE BYTES DIFFER FROM THE NOTE: 148 lines, not 200. Recorded rather than papered over.\n\nWHAT I AM NOT DOING. I am not racing PID 52000: I hold no step, I will not call complete_step or complete_task on this row, and I have not touched `lane-landing.ts` or `lane-landing.test.ts`. I am leaving the task-keyed `browser-e2e-gate` lease HELD so the twin's step-6 legs are not cut out from under it. Peer dirt untouched: `contract-goal.spec.ts`, `lane-contract-goal.ts`, `lane-multi-node-workspace.ts` (task-6bafd3b9 / worker-76f4ef35).\n\nTO WHOEVER RUNS STEP 6: any lane-landing.spec.ts count line captured before ~11:56Z was taken against bytes that no longer exist. Re-run before quoting it.",
      "timestamp": "2026-09-08T11:56:38.589Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-07T18:52:51.614Z",
  "updatedAt": "2026-09-08T11:56:38.589Z",
  "metrics": {
    "firstClaimAt": "2026-09-07T18:52:57.145Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 5
  },
  "contextFetchedBy": [
    "architect-f362f7a6",
    "architect-f6c5da87",
    "worker-319873d7"
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorBlockedReason": "Awaiting human confirmation requested in REPL for bounded mission-aware landing-helper repair, and acceptance clarification: does 'call landLaneNode repeatedly' mean multiple independent fresh seeded lanes, or repeated calls on the same already-landed lane? Those differ in round command replay and completed-node lifecycle; submitting the former without confirmation would guess the DoD. Read-only source/repro audit and proposed three owned paths are preserved in the latest architect comment; no source changes or plan submitted. Reporter was asked for semantics; governor notified.",
  "blockedOnTaskIds": null,
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-08T11:34:17.170Z",
  "planningNotes": {
    "approachesConsidered": "SCOPE WAS ALREADY SETTLED BY THE OWNER, AND I AM NOT REOPENING IT. governor-9af9af37's comment-c21629a4 (2026-09-08T07:05Z) records the owner's ruling on both open questions architect-f362f7a6 raised in comment-24801f4d: DoD 1's 'repeatedly' means N INDEPENDENT FRESH SEEDED LANES, and the bounded three-path scope (lane-landing.ts plus new lane-landing.test.ts and lane-landing.spec.ts) is approved as proposed. My job was to verify the diagnosis on disk and turn it into executable steps, not to re-litigate it. I verified every claim in that audit at HEAD 7fe37943 and all of them hold.\n\nWHY THE DIRECT VITEST ARMS EXIST AT ALL, rather than proving everything through the browser lane. The reported repro is 3.3 minutes for one intermittent outcome, and step 1 needs N>=5 runs for a denominator. The seat double is a generated child process: spawning it and feeding it stdin is a seconds-long, fully deterministic test of the exact seam where the bug lives. tests/**/*.test.ts is already in the root vitest include and wrapper-lane.test.ts / daemon-children.test.ts are the precedent, so this costs no config change. The browser spec then proves the composed behaviour on real lanes; without the direct arms, every iteration of the fix costs minutes and the mutation drill becomes impractical.\n\nREJECTED: raising LANDING_BUDGET_MS, SEAT_WRITE_BUDGET_MS or LANDING_STAFFING_ATTEMPTS. The owner fenced it explicitly and called a timeout-widening repair a rejectable defect. It is also wrong on the merits: the failure is a signal-attribution bug, and a bigger budget only lengthens the window in which the wrong seat can satisfy the wait.\n\nREJECTED: making the policy item stop exhausting its attempts. With a correctly discriminating seat double, policy.validate never moves and logs STAFFING_ATTEMPTS_EXHAUSTED; that is EXPECTED and is exactly why LANDING_MAX_AGENTS = 2 exists (the module's own comment at :~90 says so). The property to assert is that the code node lands anyway. Chasing the log line would mean either seeding policy work or shrinking the board, both fenced.\n\nREJECTED: a fourth owned path to bring lane-landing.ts back under the 250-line target. It is 266 at HEAD, before this row touches it, and it is imported by six specs. Splitting it is a separate refactor with its own consumer census; this row keeps it under the 400 hard cap and says so.\n\nSize: 7 steps, 3 files — well inside the caps, matching the approved scope exactly.",
    "codebaseInsights": "MEASURED AT HEAD 7fe37943bf72b837205383ad0304e81a92ab66ea by architect-f6c5da87, 2026-09-08. Every claim in architect-f362f7a6's audit (comment-24801f4d, anchored at aa438b78) re-verified at this later HEAD and CONFIRMED.\n\nTHE THREE DEFECTS, WITH LINE ANCHORS IN tests/e2e/control-room/lane-landing.ts (266 lines).\n(1) landingSeatDouble :187 generates `writeFileSync(<workspace>/landed-by-the-seat.txt, 'landed at ' + new Date().toISOString())` and reads NO stdin, so it runs identically for every staffed mission. Production agent-spawner.ts:400 does `child.stdin?.write(request.mission)` then `.end()`, so the mission IS available to discriminate on and is currently discarded. The timestamp also makes repeat writes byte-UNSTABLE, which matters because lane-review-round.ts digests exactly these bytes as SUBMITTED_BYTES — a restaff after the digest is taken invalidates the round.\n(2) landLaneNode :210 waits on `existsSync(landedFile)` and only THEN calls `laneCompiledNodeRef(scratch)` — target resolved after any seat may have written. It then waits on `COMMITTED_LINE = /^\\[lander\\] (\\S+): COMMITTED /mu`, which CAPTURES the ref and discards it, accepting any node's landing.\n(3) `laneWorkspaceIdentity(scratch.root)` is called inside the `try`, while the wrapper is still alive; `killTree` runs only in the `finally`. A wrapper mid-baseline during that read is the reported BASELINE_WORKSPACE_DIRTY.\n\nTHE DISCRIMINATOR IS EXACT AND MEASURED. apps/daemon/src/orchestrator/agent-mission-text.ts:110 — codeMission opens `You are a moe-next coding agent. You hold the durable claim on code node \"${nodeRef}\"`. Line 294 — mission() opens `You are a moe-next agent. You hold the durable claim on work item \"${workItemId}\" (command kind ${kind})`. Different sentence, different noun, and only the code form carries the node ref. Matching the code-node clause WITH the target ref is precise; matching a bare ref substring is not.\n\nSINGLE CALLER. `git grep -n landingSeatDouble` over the tree returns lane-landing.ts:187 (definition) and :216 (the only call); every other hit is chat JSONL or task JSON. So the signature may take the target ref without breaking a consumer.\n\nSIX CONSUMERS OF THE PUBLIC SHAPE, none in this row's roster and none expected to change: deploy-environment.spec.ts:55, deploy-fake-docker.spec.ts:33, deploy-migrations.spec.ts:69, lane-preview-arms.ts:29, lane-rollback-incident.ts:38, release-approval.spec.ts:19. That is why `pnpm test:e2e:browser` is a required regression leg rather than an optional one.\n\nGATE TOPOLOGY. vitest.config.ts include is [adapters/**, packages/**, tests/**, tools/**] — tests/**/*.test.ts IS covered by root `pnpm test`, and apps/** is NOT covered by anything but the per-package legs. tests/e2e/control-room/tsconfig.json exists and may need an explicit typecheck leg. Sibling direct-arm precedents in the same directory: wrapper-lane.test.ts (167), daemon-children.test.ts (144), harness.test.ts, journey-coverage.test.ts, daemon-lane-ledger.test.ts.\n\nPEER DIRT, DO NOT TOUCH: contract-goal.spec.ts, lane-contract-goal.ts and lane-multi-node-workspace.ts are untracked in tests/e2e/control-room and belong to task-6bafd3b9 (worker-76f4ef35). DoD 3 requires RUNNING contract-goal.spec.ts as the dependent arm; it does not make those bytes this row's.\n\nDOCKER IS UP on this host (server 29.6.2, measured 2026-09-07) — relevant because the fake-docker and deploy consumer specs exercise landLaneNode.",
    "risks": "OVER-BROAD MISSION MATCHING reintroduces the bug in a shape the new arms would not catch: a mission that merely mentions the target ref (a diagnostic line, a hint payload) must NOT trigger the write. Match the code-node claim clause, not a bare ref substring.\n\nSTDIN THROUGH cmd.exe IS THE UNVERIFIED LINK. agent-spawn-invocation.ts runs a win32 seat through cmd.exe as a command LINE; the `.cmd` bridge must actually carry the piped mission to the `.js`. This is the one place the fix could work on posix and silently no-op on this Windows host — prove it rather than assume it, and note that a seat that reads nothing writes nothing, which turns into SEAT_NEVER_WROTE rather than a loud error.\n\nWAITING FOREVER INSTEAD OF WAITING WRONG. Target-matching the COMMITTED line converts 'credited the wrong landing' into 'never sees the right one' if the ref formatting differs by a hair between `laneCompiledNodeRef` and the lander's log. Assert the two agree, and keep the refusal path returning LANDING_BUDGET_SPENT with the transcript so a mismatch is diagnosable rather than silent.\n\nTHE REGEX IS LOAD-BEARING AND FRAGILE. A lone `]` under the `u` flag is a SyntaxError thrown at IMPORT, which takes down every spec importing this module and makes test:e2e:browser report no tests at all — a green-looking catastrophe. Keep the escape.\n\nTHE INTERMITTENT MAY NOT REPRODUCE TODAY. Step 1 can come back all-green at N=5. That is a measurement, not a licence to skip the fix or to claim the DoD-1 'previously failed' clause. Raise N and say so; do not manufacture a red.\n\nTEARDOWN ORDERING. Moving the child stop earlier must not remove the `finally` guarantee on the throwing and refusing paths; the second kill has to be idempotent or the fix trades a dirty workspace for a throw during cleanup.\n\nLEAKED WRAPPERS AND PORTS across N lanes. This row spawns real wrappers N times per spec run; a leak makes every later gate on this board inadmissible, and the row's own clean-status assertion is what would catch it.\n\nSHARED GATE CONTENTION. The browser leg and the full suite are leased resources on this fleet; running them unleased degrades peer seats' gates and produces reds that are then misattributed to this diff."
  },
  "planApprovedAt": "2026-09-08T11:34:19.290Z",
  "commits": [
    {
      "sha": "0be4f7fc548170a1befe24dd25169962dfb30d0e",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-319873d7@2026-09-08T11:34:35Z",
      "paths": [
        ".moe/tasks/task-da435e66fe8247fda0d441e61e2c4b91.json"
      ],
      "recordedBy": "worker-319873d7",
      "recordedAt": "2026-09-08T11:34:38.860Z",
      "treeId": "fefe0b856da69fc0617073c9be3d9c20449d910e",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "b16c544a43d8ec47d397cd9b7314c5cf28597517",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-319873d7@2026-09-08T11:34:35Z",
      "paths": [
        ".moe/tasks/task-99f364e3a6104ee993b6820c580a52fb.json",
        ".moe/tasks/task-da435e66fe8247fda0d441e61e2c4b91.json",
        "tests/e2e/control-room/lane-landing.test.ts"
      ],
      "recordedBy": "worker-319873d7",
      "recordedAt": "2026-09-08T11:44:06.787Z",
      "treeId": "978cece14d212cc08200b2bd14855bf2ebd3502b",
      "status": "WORKING",
      "pushed": true
    },
    {
      "sha": "8ede1ac3b2e23d148dce18bc8255863f3c88bc39",
      "ref": "refs/heads/moe/work-2026-09-04",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-319873d7@2026-09-08T11:34:20Z",
      "paths": [
        ".moe/tasks/task-da435e66fe8247fda0d441e61e2c4b91.json",
        "tests/e2e/control-room/lane-landing.test.ts",
        "tests/e2e/control-room/lane-landing.ts"
      ],
      "recordedBy": "worker-319873d7",
      "recordedAt": "2026-09-08T11:49:04.229Z",
      "treeId": "d50bb13f90b7f208e8d1c46ae2dba7200cf13070",
      "status": "WORKING",
      "contested": [
        {
          "path": "tests/e2e/control-room/lane-landing.ts",
          "taskId": "task-6bafd3b9439640cba358dd6e79329c9d"
        }
      ],
      "pushed": true
    }
  ],
  "filesModified": [
    ".moe/tasks/task-da435e66fe8247fda0d441e61e2c4b91.json",
    ".moe/tasks/task-99f364e3a6104ee993b6820c580a52fb.json",
    "tests/e2e/control-room/lane-landing.test.ts",
    "tests/e2e/control-room/lane-landing.ts"
  ],
  "unattributedPaths": [],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "worker-319873d7@2026-09-08T11:34:20Z",
    "at": "2026-09-08T11:49:04.229Z"
  },
  "workStartedAt": "2026-09-08T11:35:16.504Z",
  "touchedFiles": [
    "tests/e2e/control-room/zz-baseline-throwaway.spec.ts",
    "tests/e2e/control-room/lane-landing.test.ts",
    "tests/e2e/control-room/lane-landing.ts"
  ],
  "declaredFiles": [
    "tests/e2e/control-room/lane-landing.spec.ts"
  ]
}