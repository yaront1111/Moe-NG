{
  "id": "task-d92b1b15a5b048e49671ed34990fa4a1",
  "epicId": "epic-bf111658f9694b558bdc5596bbf0f924",
  "title": "Activation ledger: replay and effect scan fail closed at scale",
  "description": "Objective: make the activation ledger's replay and effect-binding lookups keep answering as the store grows and as the aggregate accumulates its Foundation tail.\n\nRAISED BY: governor-18a42000 adversarial audit of today's diff, 2026-08-15 (7 lenses, 35 findings raised, 36 independent skeptic verdicts). Both defects below are in task-df29871b \"Durable activation authority ledger\", which is DONE and QA-approved with a mutation-drilled retry. Both survived adversarial refutation and I re-verified each on current bytes myself. FILED AS ONE TASK deliberately: both live in the activation-ledger reader/commit pair, and two tasks editing activation-ledger-reader.ts concurrently would collide in this shared worktree.\n\nDEFECT 1 — REPLAY FEEDS A WHOLE AGGREGATE TO AN EXACTLY-ONE READER (HIGH).\napps/daemon/src/activation/activation-ledger-commit.ts:140-144:\n    events = store.readEvents(aggregateId);\n    const durable = readActivationLedgerRecord(aggregateId, events);\napps/daemon/src/activation/activation-ledger-reader.ts:61:\n    if (events.length > 1) return activationLedgerUnknown(\"ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS\");\n`answerReplayed` hands EVERY event on the aggregate to a reader whose second guard refuses more than one. The moment any second event exists on that aggregate — the Foundation launch tail is the obvious source — the idempotent re-commit path refuses AMBIGUOUS instead of returning the stored record. Idempotency is precisely the property that must survive a growing aggregate. Note that foundation-launch-authority.ts:36 imports a DIFFERENT reader, `readFoundationActivationHistory`, which handles history; the replay path appears to be using the wrong one of the two.\n\nDEFECT 2 — EFFECT SCAN DIES PERMANENTLY PAST 6,400 EVENTS (HIGH).\napps/daemon/src/activation/activation-ledger-reader.ts:107:\n    const MAX_SCAN_PAGES = 64, SCAN_PAGE_SIZE = 100, MAX_QUERY_CHARS = 400;\n`scanForEffect` pages the GLOBAL event stream via `store.readEventsAfter(cursor, SCAN_PAGE_SIZE)` from cursor 0n, and it CANNOT return early on a hit because it must prove the match unique — its only success exit is `if (!read.hasMore) return { aggregateId: found, refusal: null }` at :255. So it must exhaust the entire stream every call. Once the project's global event count exceeds 64 x 100 = 6,400, `read.hasMore` is still true when the loop at :237 finishes its 64 iterations, control falls through to `return no(\"FOUNDATION_BINDING_SCAN_INCOMPLETE\")` at :260, and `readCurrentEffectSessionBinding` stops finding ANY activation — permanently, for every effect, with no recovery and no operator signal beyond that one code.\n\nThis is a time bomb rather than an edge case: the global event count only grows, the bound is on TOTAL project events rather than per-effect, and it fails closed into a refusal that looks like a legitimate authority answer. A green suite cannot see it because no test store reaches 6,400 events.\n\nNOT IN SCOPE: changing the durable event format, the ledger's digest/seal discipline, the exactly-one guarantee itself where it is genuinely required, or foundation-launch-authority's own admission guards. If the right answer is an index or a per-aggregate query rather than a global scan, that is a design decision worth raising rather than assuming.\n\nVerification: pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test.",
  "definitionOfDone": [
    "An idempotent re-commit returns the stored durable record on an aggregate that carries its Foundation launch tail. A test commits, appends the tail so the aggregate holds more than one event, re-commits with the same idempotency key, and asserts the ORIGINAL record is returned - not ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS and not a caller-supplied echo.",
    "The exactly-one guarantee is preserved where it is genuinely required: a test proves a genuinely ambiguous aggregate still refuses with its exact stable code and layer, so defect 1's fix narrows the reader's input rather than weakening the guard.",
    "readCurrentEffectSessionBinding answers correctly on a store holding more than 6,400 events. A test drives a store past that boundary with a real file-backed SqliteEventStore, asserts the binding is FOUND, and pins the event count it was proven at with a literal so a later change to page size or page count cannot silently shrink the coverage.",
    "The uniqueness property the scan exists to prove still holds at scale: a second matching activation past the old 6,400 bound is still detected and refused with the exact FOUNDATION_BINDING_EVIDENCE_AMBIGUOUS code and its layer, proving the fix did not buy scale by giving up the invariant.",
    "Mutation drills prove both guards are attached to production: restoring the whole-aggregate read into the exactly-one reader turns a NAMED test red, and independently restoring the 64-page bound turns a NAMED test red on the past-6400 case. Each drill is restored byte-exact and verified by hash.",
    "Fresh pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test exits 0 with each leg's exit code read separately, and every existing activation-ledger test stays green and unmodified."
  ],
  "taskRails": [
    "Both defects fail CLOSED into refusals that look like legitimate authority answers. Fixing them must not weaken any invariant: the exactly-one guarantee and the scan's uniqueness proof both still have to hold. Buying scale by dropping a uniqueness check is a wrong answer.",
    "Assert the reason code and the layer, never merely that the call failed or succeeded. Where two layers can refuse the same input, the test states which one answered - both of these defects are cases of the WRONG refusal winning.",
    "Any test that proves behaviour at scale must pin the event count it was proven at as a literal. A later reduction in page size or page count must break that test rather than silently shrink the proof.",
    "Do not change the durable event format, the digest/seal discipline, or foundation-launch-authority's admission guards. If the correct fix is an index or a per-aggregate query instead of a global scan, raise that as a design decision rather than landing it unannounced.",
    "Re-measure both sites on current bytes before editing - activation-ledger-commit.ts:140-144 and activation-ledger-reader.ts:61/107/237/255/260 were measured today, but this is a shared worktree with live agents. Preserve foreign work and commit only explicit owned paths.",
    "Keep each production source under 250 physical lines, split before 400, and measure with grep -c '' rather than PowerShell Measure-Object, which undercounts."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "REOPEN PREFLIGHT + TDD RED ON CURRENT BYTES. Record `git rev-parse HEAD`, explicit-path `git status --short`, and the task merge-base; stop/report a collision rather than reset/stash if any of the eight owned paths is dirty. Re-read the already-landed d92 diff instead of reimplementing it: preserve the replay-tail filter/digest cross-check, the true-two-activation ambiguity test, the real file-backed literal 6,500-event BOUND test, and the late-second-match ambiguity test without weakening their literal code/layer assertions. In `event-read-model-contract.test.ts`, first add production-surface tests for an empty store horizon `0n`, the existing three-event seed horizon `3n`, and a fresh call advancing after an append, with exact bigint values/nonzero fixture cardinality. In `foundation-launch-authority.test.ts`, first add failing cases for a thrown/invalid horizon and hostile page shapes (gap, >H, premature end, cursor mismatch, `hasMore` at H), each asserting literal status `UNKNOWN`, code `FOUNDATION_BINDING_SCAN_INCOMPLETE` or `FOUNDATION_BINDING_EVIDENCE_UNREADABLE` as applicable, and layer `FOUNDATION_ACTIVATION_BINDING`; assert the generated hostile-case cardinality literally. Add the mandatory watchdog-isolated moving stream: child/Worker uses the production reader, H=3n exactly once, returns one contiguous noise event per page with `hasMore:true` forever, and must terminate after exactly three page reads with literal UNKNOWN/SCAN_INCOMPLETE/layer; current code must red by watchdog timeout, never hang the Vitest process. Run only these named tests to record genuine RED (missing API/timeout or wrong exact assertion, not zero tests/setup failure).",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/event-read-model-contract.test.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts",
        "apps/daemon/src/activation/activation-ledger-commit.test.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/event-read-model-contract.test.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts"
      ],
      "note": "Reopen preflight at HEAD 4d0a49fb8791b458565863377d7002a48a1a9bd8; task merge-base e9b379681545ed38da88da17c76f477db4f03191. All eight explicit owned paths were clean; no reset/stash. Re-read and preserved landed replay-tail filter/digest, true ambiguity, literal 6,500-event, and late-second-match tests. Added production-surface horizon contract (empty 0n, seeded 3n with literal/nonzero cardinality, fresh append 4n), exact horizon/page hostile cases (4 invalid horizons, 5 page shapes; UNKNOWN code+FOUNDATION_ACTIVATION_BINDING layer), and Worker watchdog (H=3n once, expected three pages). Genuine RED: store named test executed 1/24 and exited 1 at `empty.readEventHorizon is not a function`; daemon three named tests executed 3/33 and exited 1 with ABSENT vs UNKNOWN for horizon/shape cases and WATCHDOG_TIMEOUT for moving stream.",
      "startedAt": "2026-08-15T23:27:01.897Z",
      "completedAt": "2026-08-15T23:33:19.257Z"
    },
    {
      "stepId": "step-2",
      "description": "IMPLEMENT THE LOSSLESS PUBLIC STORE HORIZON. Add `EventReadModelStore.readEventHorizon(): bigint` inside `readSnapshotOperation(\"read event horizon\", …)`, call `assertCachedProjectBinding()`, and execute the single scalar query `SELECT CAST(COALESCE(MAX(global_position), 0) AS TEXT) AS event_horizon FROM domain_events`. Parse `\"0\"` as 0n and every nonzero value through the existing canonical positive-bigint durable-row validator so values beyond 2^53 remain exact and malformed durable rows retain stable store corruption semantics. Add the exact `() => bigint` member/delegate to `DecisionLedgerCore`, its frozen `createDecisionLedgerCore` facade, and `SqliteEventStore`; do not alter schema/version/indexes, durable rows, codecs, the root barrel, or manifests. Make the new store contract tests green, including closed/error behavior already supplied by existing read wrappers if touched.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/event-read-model.ts",
        "packages/store/src/decision-ledger.ts",
        "packages/store/src/sqlite-event-store.ts",
        "packages/store/src/event-read-model-contract.test.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/event-read-model.ts",
        "packages/store/src/decision-ledger.ts",
        "packages/store/src/sqlite-event-store.ts",
        "packages/store/src/event-read-model-contract.test.ts"
      ],
      "note": "Implemented one-snapshot `readEventHorizon()` with cached project-binding validation and exact TEXT MAX(global_position) query. Empty `\"0\"` maps to 0n; nonzero values use requireStoredPositiveBigIntText. Forwarded exact () => bigint through DecisionLedgerCore/frozen facade and SqliteEventStore. Added closed-handle STORE_CLOSED assertion. Fresh GREEN: focused horizon test 1 passed/23 skipped exit 0; full event-read-model-contract 24/24 exit 0; `pnpm --filter @moe/store typecheck` exit 0. Production line counts: event-read-model 109, decision-ledger 151, sqlite-event-store 372 (<400).",
      "startedAt": "2026-08-15T23:33:24.081Z",
      "completedAt": "2026-08-15T23:36:05.028Z"
    },
    {
      "stepId": "step-3",
      "description": "VERIFY THE CROSS-PACKAGE EDGE, THEN IMPLEMENT THE FINITE DAEMON SCAN. Before consuming the new member, record all three required edge facts: (a) `apps/daemon/package.json` has `@moe/store` in dependencies, (b) the `apps/daemon` importer in `pnpm-lock.yaml` links `../../packages/store`, and (c) in one trap-cleaned command create a temporary `.ts` probe under `apps/daemon/src` that imports `SqliteEventStore` from the BARE specifier `@moe/store`, typechecks `const horizon: bigint = store.readEventHorizon()`, runs `pnpm --filter @moe/daemon typecheck` with exit 0, and deletes the probe even on failure; no probe may remain. If any edge fact is absent, stop/report blocked rather than deep-relative import or silently edit an unowned manifest/lockfile. Add `readEventHorizon(): bigint` to `FoundationBindingStore`. In `scanForEffect`, read/validate H exactly once; H=0n is the finite empty result. While cursor<H request at most `min(SCAN_PAGE_SIZE, H-cursor)`, require every globalPosition to be exactly the next contiguous value and <=H, require nextCursor to equal the last returned position and advance, refuse a false `hasMore` before H, and on reaching H require `hasMore === false`. A horizon/page throw returns exact UNKNOWN/EVIDENCE_UNREADABLE/layer; an invalid/negative H, empty/gapped/duplicate/regressing/overshooting page, mismatched cursor, premature end, or terminal `hasMore` returns exact UNKNOWN/SCAN_INCOMPLETE/layer. Continue evaluating every activation through H so the second distinct aggregate remains exact EVIDENCE_AMBIGUOUS; never return on the first hit. Update typed fakes and make the watchdog/shape cases plus unchanged >6,400 and late-second-match tests green.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/activation/activation-ledger-reader.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts",
        "packages/store/src/sqlite-event-store.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/activation/activation-ledger-reader.ts"
      ],
      "note": "Cross-package edge re-verified fresh at HEAD d447a34 (all 8 owned paths clean; task merge-base e9b3796). (a) apps/daemon/package.json:22 \"@moe/store\": \"workspace:*\". (b) pnpm-lock.yaml apps/daemon importer -> '@moe/store' version: link:../../packages/store. (c) trap-cleaned probe apps/daemon/src/zz-d92-horizon-probe.ts imported SqliteEventStore from the BARE specifier and typed `const horizon: bigint = store.readEventHorizon()`; `pnpm --filter @moe/daemon typecheck` exit 0; PROBE_DELETED=YES (the old blockedReason premise is dead - the foreign TS2322 file is gone). Implemented the finite captured-horizon scan: added readEventHorizon(): bigint to FoundationBindingStore; new captureHorizon() reads H exactly ONCE (throw -> EVIDENCE_UNREADABLE; non-bigint or negative -> SCAN_INCOMPLETE); the `for (;;)` is now `while (cursor < horizon)` requesting min(SCAN_PAGE_SIZE, H-cursor); every item must sit at exactly the next contiguous position and <= H; empty/no-progress page, nextCursor !== last position, and hasMore !== (position < H) all refuse SCAN_INCOMPLETE. Contiguity is a real store invariant, not an assumption: sqlite-schema-integrity.ts:190-193 raises STORE_CORRUPT on `minimum_position <> 1 OR ledger_row_count <> maximum_position`. No early return on a hit; the walk still runs to H so a second distinct aggregate is EVIDENCE_AMBIGUOUS. Daemon typecheck exit 0. Focused run apps/daemon `vitest run --root . --config package.json src/activation/foundation-launch-authority.test.ts`: Test Files 1 passed, Tests 33 passed, exit 0 - including named cases \"fails closed for unreadable and invalid event horizons\", \"refuses every hostile page shape inside the captured event horizon\", \"bounds a continuously advancing scan to the captured horizon\" (233ms, was a 2s watchdog timeout), \"answers BOUND on a store holding more than 6,400 global events\", \"still refuses a SECOND matching activation found past the old 6,400 ceiling\". Reader is 375 physical lines (grep -c ''), under the 400 hard split.",
      "startedAt": "2026-08-15T23:36:10.004Z",
      "completedAt": "2026-08-16T01:09:00.971Z"
    },
    {
      "stepId": "step-4",
      "description": "PRESERVE THE LANDED REPLAY FIX AND REGRESSION SURFACE. Re-run the existing tailed-aggregate replay test, caller-echo/digest divergence checks, and true-two-activation ambiguity check against the production commit/reader surface; keep the returned durable record, exact refusal code, and exact layer assertions. Do not move filtering into the strict reader (a lone wrong event type must remain EVENT_TYPE_UNEXPECTED), relax exactly-one semantics, change the activation durable format/digest/admission rules, or edit the orchestrator/foundation admission path. Re-run the existing real file-backed 6,500-event BOUND case and late second match beyond the former ceiling without deriving their event count from page constants. Refactor only if necessary to keep each production file below 400 physical lines; do not split on total task LOC.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/activation/activation-ledger-commit.ts",
        "apps/daemon/src/activation/activation-ledger-commit.test.ts",
        "apps/daemon/src/activation/activation-ledger-reader.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts"
      ],
      "note": "Preservation step - no production edit needed; re-verified the landed surface rather than reimplementing it. activation-ledger-commit.ts:158-161 still filters to ACTIVATION_LEDGER_EVENT_TYPE at the COMMIT site and hands the singleton to the strict reader; activation-ledger-reader.ts:60-66 is untouched, so zero events still refuse ABSENT, two still refuse AMBIGUOUS, and a lone wrong-typed event still refuses EVENT_TYPE_UNEXPECTED. Digest cross-check (sealed.digest !== candidateDigest -> ACTIVATION_LEDGER_REPLAY_DIVERGED) unchanged. Durable format, digest/seal discipline, and foundation admission guards untouched. Regression surface, apps/daemon `vitest run --root . --config package.json src/activation`: Test Files 7 passed (7), Tests 94 passed (94), exit 0. Named cases confirmed executed by verbose reporter: \"replays the DURABLE record on an aggregate that also carries its Foundation tail\", \"refuses a drifted candidate on a tailed aggregate, so the stored bytes win\", \"replays the same command by resolving the DURABLE record, not by echoing the caller\", \"still refuses an aggregate carrying TWO activation events as AMBIGUOUS\", plus the literal 6,500-event BOUND case and the late-second-match ambiguity case (both still assert their own literal counts, not page constants). Production line counts (grep -c ''): activation-ledger-reader.ts 375, activation-ledger-commit.ts 227 - both under the 400 hard split, so no refactor was forced.",
      "startedAt": "2026-08-16T01:09:06.904Z",
      "completedAt": "2026-08-16T01:09:49.516Z"
    },
    {
      "stepId": "step-5",
      "description": "ADVERSARIAL REVIEW + REQUIRED MUTATION DRILLS. Invoke adversarial-self-review and capture SHA-256 for every production target before each drill; mutate only one invariant at a time, confirm the named production-surface test genuinely reds, then restore from a temporary out-of-repo byte copy and require the original hash (never `git checkout`). Mandatory drills: (1) restore whole-aggregate replay input -> the tailed replay test reds while the exact two-activation guard remains independently pinned; (2) restore the old 64-page ceiling -> the literal 6,500-event test reds on SCAN_INCOMPLETE; (3) delete/bypass the captured-H terminal/liveness guard -> the moving-stream child hits its watchdog and the parent named test reds; (4) weaken continuity/overshoot validation -> a hostile generated case reds on its literal SCAN_INCOMPLETE code AND layer; (5) return on first activation -> the late-second-match test reds on the lost exact ambiguity refusal. Confirm each mutation was actually applied, every generated sweep has its literal nonzero cardinality, restoration is byte-exact, one horizon read/three pages are asserted, no scratch/probe/evidence file remains, and the eight explicit owned paths are the only completion pathspecs. Record follow-up performance task `task-16d5bc3a10864351adf5be10dfa7df00`; do not add an index here.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/activation/activation-ledger-commit.ts",
        "apps/daemon/src/activation/activation-ledger-commit.test.ts",
        "apps/daemon/src/activation/activation-ledger-reader.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts",
        "packages/store/src/event-read-model.ts",
        "packages/store/src/event-read-model-contract.test.ts",
        "packages/store/src/decision-ledger.ts",
        "packages/store/src/sqlite-event-store.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/activation/foundation-launch-authority.test.ts"
      ],
      "note": "Adversarial self-review found two SURVIVING MUTANTS in my own step-3 work before any drill: the five committed hostile page-shape cases all refuse via the cursor/hasMore checks, so BOTH halves of the per-item guard `event.globalPosition !== position || position > horizon` could be deleted with the suite still green (a page of [pos2,pos3] with nextCursor 2 at H=2 skips a position behind a correct-looking count; a page of [pos1,pos2] at H=1 reads past the horizon). Added two cases that only that comparison can catch - \"positions mislabelled\" and \"overshoots horizon\" - and raised the literal cardinality assertion from 5 to 7. Baseline SHA-256 before drills: reader 45fcc7a963612578e87ce5c46e35efd3f42d36ce761d668faadf5c88fb62e26a, commit 832cdc1333cc188c747aa19217d9d76fadd5a375d225e4bf424b34c66c76a51f; byte copies kept OUT of the repo at /d/tmp/d92-drills, restored by cp (never git checkout) and re-hashed after every drill. DRILL 1 (commit.ts: activations -> events, mutant 3c118ef5...): \"replays the DURABLE record on an aggregate that also carries its Foundation tail\" RED; \"still refuses an aggregate carrying TWO activation events as AMBIGUOUS\" stayed GREEN, so the exactly-one guard is independently pinned. DRILL 2 (reader: 64-page ceiling reinstated, mutant d32c96e4...): \"answers BOUND on a store holding more than 6,400 global events\" RED with +\"code\": \"FOUNDATION_BINDING_SCAN_INCOMPLETE\". DRILL 3 (all three captured-H comparisons removed, mutant 5eed00ed...): \"bounds a continuously advancing scan to the captured horizon\" RED at 2021ms with kind WATCHDOG_TIMEOUT - QA's exit-124 hang reproduces exactly when the horizon bound goes, and the child watchdog keeps vitest itself alive. DRILL 4a (continuity clause dropped) and 4b (overshoot clause dropped): \"refuses every hostile page shape inside the captured event horizon\" RED both times, expected 'ABSENT' to be 'UNKNOWN' - each killed by exactly one of the two new cases. Because 4a/4b red on the status assertion, the code literal never executed, so DRILL 4c swapped that guard's refusal code to EVIDENCE_UNREADABLE: RED with \"expected 'FOUNDATION_BINDING_EVIDENCE_UNREADABLE' to be 'FOUNDATION_BINDING_SCAN_INCOMPLETE'\", pinning the literal code. The layer clause in the same helper runs on every green pass; mutating it would require editing foundation-activation-transition.ts, which is not an owned path. DRILL 5 (return on the first matching activation): \"still refuses a SECOND matching activation found past the old 6,400 ceiling\" RED, expected 'BOUND' to be 'UNKNOWN:FOUNDATION_BINDING_EVIDENCE_AMBIGUOUS'. Every mutation was proved applied by a printed diff/grep and a changed hash; every restore returned reader to 45fcc7a9... and commit to 832cdc13.... Post-drill: activation suite 7 files / 94 tests exit 0, reader 375 lines, no probe/scratch/drill/evidence file anywhere in the repo, and the only modified owned paths are activation-ledger-reader.ts and foundation-launch-authority.test.ts. DISCLOSED TRADE-OFF (not a deviation - QA's rejection and the approved plan both pin it): requiring hasMore === false AT the captured horizon means a concurrent append landing during a scan makes that lookup refuse UNKNOWN/SCAN_INCOMPLETE rather than answer. It fails closed and a retry succeeds, but it is a liveness cost that the index/per-effect-query follow-up task-16d5bc3a10864351adf5be10dfa7df00 should weigh; no index was added here. Also theoretical, pre-existing and unchanged: scanForEffect is not itself wrapped in try/catch, so a store whose page is not iterable would throw rather than refuse - the FoundationBindingStore contract types it away and the real store cannot produce it.",
      "startedAt": "2026-08-16T01:11:22.356Z",
      "completedAt": "2026-08-16T01:14:52.510Z"
    },
    {
      "stepId": "step-6",
      "description": "ONE FINAL FOREGROUND VERIFICATION GATE AND EXPLICIT-PATH HANDOFF. First compare repo-wide failure paths at the recorded task merge-base versus HEAD; only a new failing-path delta intersecting the eight owned paths blocks, while any foreign baseline red must be quoted verbatim with exact lines/paths. Then run this exact command once, without pipes or `&&` masking individual legs: `bash -lc 'set +e; pnpm --filter @moe/store typecheck; s1=$?; pnpm --filter @moe/store test; s2=$?; pnpm --filter @moe/daemon typecheck; s3=$?; pnpm --filter @moe/daemon test; s4=$?; pnpm verify:store; s5=$?; pnpm verify:foundation; s6=$?; pnpm typecheck; s7=$?; pnpm test; s8=$?; printf \"STORE_TYPECHECK_EXIT=%s\\nSTORE_TEST_EXIT=%s\\nDAEMON_TYPECHECK_EXIT=%s\\nDAEMON_TEST_EXIT=%s\\nVERIFY_STORE_EXIT=%s\\nVERIFY_FOUNDATION_EXIT=%s\\nROOT_TYPECHECK_EXIT=%s\\nROOT_TEST_EXIT=%s\\n\" \"$s1\" \"$s2\" \"$s3\" \"$s4\" \"$s5\" \"$s6\" \"$s7\" \"$s8\"; test \"$s1\" -eq 0 && test \"$s2\" -eq 0 && test \"$s3\" -eq 0 && test \"$s4\" -eq 0'`. Store/daemon owned-package legs must all be 0 and both Vitest outputs must contain nonzero `Test Files N passed` and `Tests N passed`; exit 0 without counts is unverified. Evaluate the repo/verify legs by the required path-attributed baseline rule, never fabricate green. Recheck production line counts <400 and explicit status/diff over the eight paths; completion must commit/stage only those explicit paths, preserve foreign work, and hand off the exact command output, mutation names/results/hashes, dependency-probe result/deletion, and any disclosed foreign red.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/activation/activation-ledger-reader.ts",
        "apps/daemon/src/activation/activation-ledger-commit.ts",
        "apps/daemon/src/activation/activation-ledger-commit.test.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts",
        "packages/store/src/event-read-model.ts",
        "packages/store/src/event-read-model-contract.test.ts",
        "packages/store/src/decision-ledger.ts",
        "packages/store/src/sqlite-event-store.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/activation/activation-ledger-reader.ts",
        "apps/daemon/src/activation/foundation-launch-authority.test.ts"
      ],
      "note": "Final gate under the governor's 01:22Z RESCOPE (path-attributed baseline). Session HEAD at start 539338f; recorded task merge-base e9b3796. Owned-path status showed only the two daemon files dirty, byte-identical to the drilled hashes (reader 45fcc7a9..., foundation test 7e9ac12e..., commit.ts 832cdc13... = drill baseline, no residue).\n\n(1) OWNED-SUITE LEGS, one foreground command, EXIT 0:\n`bash -lc 'set -e; pnpm --filter @moe/store typecheck; pnpm --filter @moe/daemon typecheck; (cd packages/store && npx vitest run --root ../.. packages/store/src/event-read-model-contract.test.ts); (cd apps/daemon && npx vitest run --root . --config package.json src/activation)'`\nStore contract suite: Test Files 1 passed (1) / Tests 25 passed (25). Activation suites: Test Files 7 passed (7) / Tests 94 passed (94) - all SEVEN, i.e. the previously-foreign activation-ledger-aggregate-id.test.ts is green too, one better than the six required. Both scoped typechecks exit 0 with no exclusions; the old daemon-typecheck exclusion QA needed is no longer necessary.\n\n(2) PACKAGE/REPO LEGS, judged by (HEAD-minus-merge-base failing-path delta) INTERSECT eight owned paths = EMPTY. Legs run separately, exits read individually: STORE_TEST=1, DAEMON_TEST=1, VERIFY_STORE=1, VERIFY_FOUNDATION=0, ROOT_TYPECHECK=0, ROOT_TEST=1.\n\nFOREIGN RED, quoted verbatim, never repaired:\n- packages/store/src/recovery-anchor.test.ts:694 \"AssertionError: RECOVERY_ANCHOR_FAULT_POINTS leaked onto the root: expected [ 'PREPARE', 'INACTIVE_INSTALL', ...(6) ] to be undefined\" (1 failed | 513 passed). Attribution: the symbol is grep-count 0 in packages/store/src/index.ts at e9b3796 and 1 at HEAD; `git log -S` names foreign commit 4e0201a (task-0c89476b).\n- apps/daemon/src/orchestrator/agent-spawner.test.ts x3 (\"contains a fast-exiting agent's EPIPE...\", \"gives code-node agents file/exec tools...\", \"hands only the scoped credential to loopback MCP...\") - \"AssertionError: expected [] to include ''\" at :168. agent-spawner.ts last touched by foreign c970f10 (task-ff589abd).\n- apps/daemon/src/work/foundation-attempt-windows.test.ts \"launches through the shipped broker once and adopts replay\" - \"AssertionError: expected false to be true\".\n- packages/runner/src/providers/claude/claude-launcher.windows.test.ts:178 \"stays alive beyond the control poll slice\" - foreign c970f10.\n- tests/integration/release-archive-cleanup.test.ts x2 - last touched AT the merge-base e9b3796 itself, so pure baseline red.\n\nBASE PROBE (required, because foundation-attempt-service.ts really does import readFoundationActivationHistory from my owned reader): reverted ONLY my production file to HEAD bytes (probe sha 7c7d1a9f...), re-ran both daemon files - the SAME 4 test NAMES failed, 2 files / 4 failed | 19 passed. Restored by cp from an out-of-repo copy, hash re-verified 45fcc7a9... . So none of the red is mine. The uncommitted reader diff is confined to SCAN_PAGE_LIMIT, the FoundationBindingStore member, captureHorizon and scanForEffect; readFoundationActivationHistory is untouched.\n\nProduction physical lines (grep -c ''): reader 375, commit 227, event-read-model 109, decision-ledger 151, sqlite-event-store 372 - all under the 400 hard split.\n\nHANDOFF: no scratch/probe/zz-*/evidence file remains (untracked scan over apps/daemon/src and packages/store/src is empty). Committed f33d4a2 by explicit pathspec over the two dirty owned paths only, 2 files changed 66(+)/17(-); the other six owned paths were already in the tree from earlier foreign whole-tree sweeps (+146 foundation test, +13 event-read-model, +2 decision-ledger, +4 sqlite, +68 store contract test vs e9b3796). Committed bytes hash-match the gated bytes exactly. Follow-up performance/index task remains task-16d5bc3a10864351adf5be10dfa7df00; no index added here.",
      "startedAt": "2026-08-16T01:14:58.125Z",
      "completedAt": "2026-08-16T01:28:56.924Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "Governor 01:22Z, same class ruling the human approved for 5fc/8e307/fc658/8f84: step-6 gate RESCOPED from raw store+daemon package legs to the global path-attributed baseline rail. Steps 1-5 are DONE and green including all five mandatory mutation drills — the substance is proven. Resume step 6 with this exact gate: (1) owned-suite legs exit 0 with nonzero counts — @moe/store event-read-model-contract suite + scoped store typecheck, and the six activation suites + scoped daemon typecheck (this task's own approved verification precedent); (2) package/repo legs judged by (HEAD-minus-merge-base failing-path delta) ∩ eight owned paths = EMPTY, foreign red quoted verbatim, never repaired. Known foreign red at rescope: recovery-anchor.test.ts:694 fault-points leak (task-0c894's surface, actively being fixed in its rejection loop) and residual review-slice/goal-services daemon cases. Commit by explicit pathspec over the eight owned paths only. QA grades under the same rescoped rule.",
  "createdBy": "HUMAN",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 104.9,
  "comments": [
    {
      "id": "comment-76869409f292467f9db77b3a6d524661",
      "author": "human",
      "content": "CORRECTION FROM THE AUTHOR (governor-18a42000), posted minutes after filing. THE TWO DEFECTS DIFFER IN REACHABILITY, and I stated both as live. One is; one is latent. Read this before planning — it changes how you reproduce defect 1.\n\nMy two skeptics DISAGREED on defect 1. One confirmed the mechanics in forensic detail; the other returned \"MECHANICS CONFIRMED, REACHABILITY REFUTED\". I re-measured myself rather than taking either.\n\nDEFECT 1 (replay -> ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS) IS LATENT, NOT LIVE:\n- `createFoundationLauncherAuthority` has NO caller outside its own module.\n- Its only composition point is `createFoundationClaudeLauncher` (foundation-launch-authority.ts:290), which has ZERO callers anywhere in apps/ or packages/, tests included.\n- It is NOT exported from the daemon root — `apps/daemon/src/index.ts` returns zero hits for it.\n- A positive control on the identical query shape (`createStoreDependencies` -> daemon-store-dependencies.ts:92, :167, mcp-http-main.ts:5) confirms the greps are well scoped, so these zeros are real absences.\n\nSo nothing in production can append the Foundation transition tail today, which means the aggregate never reaches 2+ events, which means the replay path cannot currently return AMBIGUOUS. The defect is real code with correct mechanics and NO live trigger.\n\nWHEN IT ACTIVATES, AND WHY THAT MATTERS RIGHT NOW: `createFoundationClaudeLauncher`'s own doc comment names its consumer — \"The composed launcher task-6cbff01023b14b26a78fc5e3eb1dd8a9 consumes.\" task-6cbff010 (Durable Claude attempt dispatch) is the keystone gating roughly nine downstream tasks and is in PLANNING today. **The moment task-6cbff010 lands its consumer edge, this defect goes live** — and it will present as an actively-launching activation reporting itself unverifiable on every retry. Fixing it before that edge lands is strictly cheaper than debugging it after.\n\nDEFECT 2 (6,400-event scan bound) IS LIVE. `readCurrentEffectSessionBinding` has a real production caller: apps/daemon/src/coordination/coordination-adapter.ts:27 imports it and :218 calls it. That path is composed today. The time-bomb framing in the description stands unchanged.\n\nCONSEQUENCES FOR YOUR PLAN:\n- Defect 2 is the urgent half. Do not let defect 1's unreachability delay it.\n- For defect 1, you CANNOT reproduce the failure through a production entry point today. Drive it at the adapter seam by appending a second event to the aggregate directly, and say plainly in your evidence that the trigger is not yet composed. Do NOT manufacture a fake consumer edge to make it reproducible — that would be reimplementing authority in a test, which Clause 2 forbids.\n- If you judge that defect 1 should instead be deferred and re-filed against task-6cbff010's landing, that is a defensible call. Say so in writing rather than silently dropping it; a half-fix that leaves one clause unaddressed is the exact pattern that produced task-70b6361d earlier today.\n\nThe daemon also warned on this task's title (\"contains 'and' — often two tasks wearing one title\"). That warning now looks better founded than my rationale for bundling: the halves have different urgency and different reproduction paths. I bundled to avoid two tasks colliding on activation-ledger-reader.ts in this shared worktree, which is still a real hazard. If you split, sequence them rather than running them in parallel.",
      "timestamp": "2026-08-15T18:30:52.827Z"
    },
    {
      "id": "comment-fa74c2bf0ee843efa209afc04e1e16ac",
      "author": "worker-1fe6c848",
      "content": "Step-6 gate output, verbatim, run once at HEAD d447a34 (merge-base e9b3796) with each leg's exit read separately:\n\nSTORE_TYPECHECK_EXIT=0\nSTORE_TEST_EXIT=1\nDAEMON_TYPECHECK_EXIT=0\nDAEMON_TEST_EXIT=1\nVERIFY_STORE_EXIT=1\nVERIFY_FOUNDATION_EXIT=1\nROOT_TYPECHECK_EXIT=1\nROOT_TEST_EXIT=1\n\nCounts (so no leg is an exit-0-without-tests false green): store \"Test Files 1 failed | 43 passed (44) / Tests 1 failed | 513 passed (514)\"; daemon \"Test Files 2 failed | 95 passed (97) / Tests 4 failed | 1990 passed (1994)\"; root \"Test Files 4 failed | 275 passed (279) / Tests 10 failed | 6573 passed | 2 skipped (6585)\".\n\nFOREIGN RED 1 - packages/store/src/recovery-anchor.test.ts:694\n  AssertionError: RECOVERY_ANCHOR_FAULT_POINTS leaked onto the root: expected [ 'PREPARE', 'INACTIVE_INSTALL', ...(6) ] to be undefined\n  Cause: packages/store/src/index.ts:95 exports RECOVERY_ANCHOR_FAULT_POINTS. `git log -1 -S RECOVERY_ANCHOR_FAULT_POINTS -- packages/store/src/index.ts` -> 4e0201a feat(task-0c89476b78044024b07c86c0c8986bd0): Disaster restore proof. ABSENT from index.ts at merge-base e9b3796, PRESENT at HEAD: a clean (2)-minus-(1) attribution to task-0c894. My store tree is byte-identical to HEAD this session (git status over packages/store/src is empty).\n\nFOREIGN RED 2 - apps/daemon, 4 tests in 2 files\n  src/orchestrator/agent-spawner.test.ts > claudeSpawner > hands only the scoped credential to loopback MCP, never the operator store authority\n  src/orchestrator/agent-spawner.test.ts > claudeSpawner > gives code-node agents file/exec tools and runs them in their workspace\n  src/orchestrator/agent-spawner.test.ts:537 > contains a fast-exiting agent's EPIPE instead of crashing the wrapper -- AssertionError: expected [] to deeply equal [ -4321 ]\n  src/work/foundation-attempt-windows.test.ts:284 > launches through the shipped broker once and adopts replay -- AssertionError: expected false to be true (10121ms)\n\nNOT MINE, proved rather than asserted. Base probe: I copied my reader out of the repo, wrote HEAD bytes over it (`git show HEAD:... > ...`, sha 7c7d1a9f = my step-3 change reverted), re-ran those exact two files, and got the SAME 4 failures by NAME. Restored to 45fcc7a9 and re-hashed. The set was byte-stable across three separate runs, so it is deterministic foreign red, not peer churn.\n\nAlso live in the tree and feeding the ROOT legs: packages/runner/src/providers/claude/claude-host-runtime.ts is UNTRACKED (??) peer WIP and throws \"ReferenceError: probeReportedVersion is not defined\" at claude-host-runtime.ts:230; grep counts that symbol 0 times in the file. git log and git diff are blind to it.\n\nNot fabricating a green. Both reds sit inside packages I own but in files another task owns, so I can neither fix them under epic rail 3 nor pass the plan's verification command. Steps 1-5 are complete and every DoD test is green: activation suite 7 files / 94 tests exit 0, both owned typechecks exit 0, reader 375 lines, no probe/scratch/drill file anywhere in the repo.",
      "timestamp": "2026-08-16T01:20:20.047Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-15T18:26:53.598Z",
  "updatedAt": "2026-08-16T01:30:10.685Z",
  "metrics": {
    "firstClaimAt": "2026-08-15T18:39:27.693Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 8,
    "executedStepCount": 14,
    "rejectCount": 1,
    "reopenCount": 1
  },
  "contextFetchedBy": [
    "worker-83e6139a",
    "architect-d9dccf21",
    "governor-a627169a",
    "governor-1a5aca5d",
    "worker-1fe6c848"
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-15T23:25:46.405Z",
  "planningNotes": {
    "approachesConsidered": "Rejected keeping the unbounded cursor-progress loop: a no-file production reproduction (`timeout 2s node … readCurrentEffectSessionBinding`) exited 124 because every SQLite page owns a fresh WAL snapshot, so an advancing writer can keep `hasMore` true forever. Rejected restoring any page-count ceiling: it recreates the false 6,400-event authority refusal already pinned by the literal 6,500-event tests. Rejected depending on the v5 event-type index/task-69c2c9e7ee084afea16c2b2ff935f459: that task is blocked/colliding and an index does not freeze a finite stream. Rejected a long-lived snapshot across all pages because it would retain WAL state and widen the store API. Chosen: one lossless scalar high-water read, close that snapshot, then a bounded contiguous fail-closed scan through the captured H. Although tolerating post-H `hasMore` could model start-snapshot truth, QA explicitly requires `hasMore=true` at H to refuse SCAN_INCOMPLETE, so the plan pins that exact behavior rather than speculating.",
    "codebaseInsights": "Fresh measurement at HEAD e9b379681545ed38da88da17c76f477db4f03191: `git status --short -- <eight paths>` is empty; `grep -R readEventHorizon packages/store/src apps/daemon/src` returns no symbol; activation-ledger-reader.ts still has the rejected unbounded `for (;;)`; commit 2e688c9 already contains the correct replay-event filtering plus literal >6,400 and late-second-match regressions and these must be preserved. `apps/daemon/package.json` declares `@moe/store: workspace:*`, and the `apps/daemon` lock importer is `link:../../packages/store`. Package names resolve as `@moe/store` and `@moe/daemon`. Production line counts are reader 335, commit 227, event-read-model 96, decision-ledger 149, sqlite-event-store 368: all below the 400 hard split, but reader/sqlite must receive compact deltas. Public CursorPage has only items/hasMore/nextCursor. SqliteEventStore is already exported from the bare `@moe/store` root, so no barrel or manifest change is needed. task-69c2c9e7ee084afea16c2b2ff935f459 owns the same four store paths; architect/governor were notified to serialize it behind this reopened high-priority task.",
    "risks": "The scanner must read the horizon exactly once, never recalculate it, never return on the first match, and never interpret incomplete evidence as ABSENT/BOUND. Horizon/page throws map to EVIDENCE_UNREADABLE; invalid/negative horizon, gaps, duplicate/regressing positions, item >H, cursor mismatch, premature end, or `hasMore` at H map literally to UNKNOWN / FOUNDATION_BINDING_SCAN_INCOMPLETE / FOUNDATION_ACTIVATION_BINDING. Keep the replay digest cross-check and exactly-one-activation invariant. The synchronous infinite-loop regression must execute in a child/Worker watchdog because a same-thread Vitest timeout cannot interrupt it. Keep both near-limit production sources under 400 physical lines; extract a focused helper only if the compact change would cross that boundary. Do not overlap store edits with task-69c2c9e7ee084afea16c2b2ff935f459.",
    "keyFiles": [
      "apps/daemon/src/activation/activation-ledger-reader.ts",
      "apps/daemon/src/activation/foundation-launch-authority.test.ts",
      "apps/daemon/src/activation/activation-ledger-commit.ts",
      "apps/daemon/src/activation/activation-ledger-commit.test.ts",
      "packages/store/src/event-read-model.ts",
      "packages/store/src/event-read-model-contract.test.ts",
      "packages/store/src/decision-ledger.ts",
      "packages/store/src/sqlite-event-store.ts"
    ]
  },
  "budget": {
    "wallClockMs": 3600000,
    "warnedAt": "2026-08-15T20:20:19.316Z",
    "escalatedAt": "2026-08-15T21:29:07.582Z"
  },
  "planApprovedAt": "2026-08-15T23:25:48.509Z",
  "workStartedAt": "2026-08-15T20:20:35.809Z",
  "reviewStartedAt": "2026-08-16T01:30:10.685Z",
  "verification": {
    "command": "bash -lc 'set -e; pnpm --filter @moe/store typecheck; pnpm --filter @moe/daemon typecheck; (cd packages/store && npx vitest run --root ../.. packages/store/src/event-read-model-contract.test.ts); (cd apps/daemon && npx vitest run --root . --config package.json src/activation)'",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\n$ tsc --project tsconfig.json\n\n RUN  v4.1.10 D:/projexts/moe-next\n\n Test Files  1 passed (1)\n      Tests  25 passed (25)\n   Start at  04:27:37\n   Duration  477ms (transform 240ms, setup 0ms, import 302ms, tests 79ms, environment 0ms)\n\n RUN  v4.1.10 D:/projexts/moe-next/apps/daemon\n\n Test Files  7 passed (7)\n      Tests  94 passed (94)\n   Start at  04:27:38\n   Duration  2.75s (transform 6.28s, setup 0ms, import 8.73s, tests 2.42s, environment 0ms)\n\nFINAL_GATE_EXIT=0",
    "reportedAt": "2026-08-16T01:30:10.685Z"
  },
  "filesModified": [
    "packages/store/src/event-read-model-contract.test.ts",
    "apps/daemon/src/activation/foundation-launch-authority.test.ts",
    "packages/store/src/event-read-model.ts",
    "packages/store/src/decision-ledger.ts",
    "packages/store/src/sqlite-event-store.ts",
    "apps/daemon/src/activation/activation-ledger-reader.ts",
    "apps/daemon/src/activation/activation-ledger-commit.ts",
    "apps/daemon/src/activation/activation-ledger-commit.test.ts"
  ],
  "reviewCompletedAt": "2026-08-15T23:04:48.703Z",
  "rejectionDetails": {
    "failedDodItems": [
      "DoD 3 / objective: readCurrentEffectSessionBinding must keep answering as the store grows.",
      "Fail-closed rail: unverifiable scan evidence must return a stable refusal, not hang the daemon thread."
    ],
    "issues": [
      {
        "type": "security",
        "file": "apps/daemon/src/activation/activation-ledger-reader.ts",
        "line": 243,
        "description": "`for (;;)` has no finite horizon. New WAL snapshots can keep `hasMore=true` while `nextCursor` advances under concurrent writes, so both guards pass forever. A valid fake returning one noise event at cursor+1 per page made readCurrentEffectSessionBinding hang until timeout exit 124. Use a stable horizon or approved index, preserve second-match uniqueness, and add an isolated moving-stream test asserting exact refusal code+layer; do not restore an arbitrary cap or return on first hit."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "Rejected despite a green owned gate (QA rerun: scoped typecheck exit 0; six activation suites 6 files/87 tests exit 0). The scale fix replaces the 6,400 ceiling with an unbounded synchronous scan, but cursor progress does not prove termination when the global stream grows concurrently. I reproduced the production call hanging past a 2-second foreground bound while every page was nonempty, `hasMore=true`, and `nextCursor` strictly advanced. That is a daemon-thread DoS, not a fail-closed answer. Establish a stable finite scan horizon (or approved indexed/per-effect query), exhaust candidates through it to preserve ambiguity detection, and mutation-pin the moving-stream liveness guard.",
      "rejectedAt": "2026-08-15T23:04:48.703Z",
      "reopenCount": 1,
      "failedDodItems": [
        "DoD 3 / objective: readCurrentEffectSessionBinding must keep answering as the store grows.",
        "Fail-closed rail: unverifiable scan evidence must return a stable refusal, not hang the daemon thread."
      ],
      "issues": [
        {
          "type": "security",
          "file": "apps/daemon/src/activation/activation-ledger-reader.ts",
          "line": 243,
          "description": "`for (;;)` has no finite horizon. New WAL snapshots can keep `hasMore=true` while `nextCursor` advances under concurrent writes, so both guards pass forever. A valid fake returning one noise event at cursor+1 per page made readCurrentEffectSessionBinding hang until timeout exit 124. Use a stable horizon or approved index, preserve second-match uniqueness, and add an isolated moving-stream test asserting exact refusal code+layer; do not restore an arbitrary cap or return on first hit."
        }
      ]
    }
  ],
  "priorAttempt": {
    "attemptedAt": "2026-08-15T23:14:48.432Z",
    "reason": "QA's moving-WAL rejection makes the approved plan unworkable: its step 5 explicitly treats `for (;;) + strictly advancing cursor` as safe, but each `readEventsAfter` uses a fresh SQLite read snapshot, so a continuously appended stream can advance forever and hang the daemon. Re-measurement found no public store horizon: CursorPage has only items/hasMore/nextCursor; SqliteEventStore exposes only forward paging. A sound fix cannot be implemented within the plan's daemon-only owned paths. Required scope expansion: add `readEventHorizon(): bigint` (one-snapshot `CAST(COALESCE(MAX(global_position),0) AS TEXT)`) in packages/store/src/event-read-model.ts, forward through decision-ledger.ts and sqlite-event-store.ts, and pin it in event-read-model-contract.test.ts. Then add it to FoundationBindingStore, capture once, scan contiguously through H, and fail UNKNOWN / FOUNDATION_BINDING_SCAN_INCOMPLETE / FOUNDATION_ACTIVATION_BINDING for gaps, >H, premature end, or hasMore=true upon reaching H. Add a watchdog-isolated moving-stream test (H=3n, one advancing noise event per page forever) that proves exactly 3 pages and one horizon read; deleting the liveness guard must timeout/red. Existing >6400 and late-second-match tests preserve scale/uniqueness. No production bytes were written during this reopen.",
    "implementationPlan": [
      {
        "stepId": "step-1",
        "description": "RE-MEASURE BOTH SITES ON CURRENT BYTES (task rail 5). Shared worktree with live agents; these were measured today but that is not enough. No production edits.\n\n1. `git rev-parse --short HEAD`.\n2. DEFECT 1: read `answerReplayed` (activation-ledger-commit.ts:133-157). Confirm `store.readEvents(aggregateId)` at :140 feeds `readActivationLedgerRecord(aggregateId, events)` at :144, and that the reader's count guard at reader.ts:60-61 precedes its type discriminator at :65. Note the digest cross-check at :150 (ACTIVATION_LEDGER_REPLAY_DIVERGED) — it is correct and must survive.\n3. DEFECT 2: read `scanForEffect` (reader.ts:232-262) and the constants at :107. Confirm the only success exit is `if (!read.hasMore)` at :255, the fallthrough at :261, and — importantly — the two guards that terminate the loop INDEPENDENTLY of the page cap: the empty-page-with-hasMore check at :242 and the cursor-must-advance check at :258-259.\n4. READ THE AUTHOR'S CORRECTION (comment-76869409) before writing any test. Defect 1 is LATENT: `createFoundationClaudeLauncher` (foundation-launch-authority.ts:290) has ZERO callers and the authority is not exported from the daemon root. Confirm that yourself with a grep plus a positive control. This changes REPRODUCTION — the test must construct the aggregate tail directly rather than expecting a live path to produce it.\n5. `grep -c ''` activation-ledger-reader.ts (expect ~328 — already over the 250 target, under 400, pre-existing) and activation-ledger-commit.ts.\n6. Read activation-ledger-commit.test.ts and activation-ledger-reader.test.ts to see what must stay green and unmodified (DoD 6), and activation-ledger-fixtures.ts for the existing builders.",
        "status": "COMPLETED",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-commit.ts",
          "apps/daemon/src/activation/activation-ledger-reader.ts",
          "apps/daemon/src/activation/foundation-launch-authority.ts"
        ],
        "note": "Re-measured at HEAD 6ca5da07da2c28f56867ab0ddf39c6448785cbdf. The old 64-page cap is gone and scanForEffect now uses an unbounded for(;;), which is live only for a static stream. createFoundationClaudeLauncher is a real production consumer via foundation-attempt-service.ts:226. Store CursorPage exposes only items/hasMore/nextCursor; EventReadQueryStore takes a fresh read snapshot per readEventsAfter call, and SqliteEventStore exposes no high-water/horizon method. Therefore a stable finite start-time horizon cannot be derived from the approved daemon-only surface. No production bytes written. Correct minimum is a @moe/store readEventHorizon(): bigint API (one-snapshot MAX(global_position), 0n empty) wired through EventReadModelStore, DecisionLedgerCore, and SqliteEventStore, then captured once by the daemon scanner. This is outside the approved plan/owned paths and supersedes its unsafe step-5 claim that for(;;) is sufficient.",
        "startedAt": "2026-08-15T23:05:41.066Z",
        "completedAt": "2026-08-15T23:14:38.828Z"
      },
      {
        "stepId": "step-2",
        "description": "TDD FOR DEFECT 1 — write both tests first, in `apps/daemon/src/activation/activation-ledger-commit.test.ts`.\n\nTEST A (DoD 1), the reproduction: commit an activation, then APPEND A FOREIGN EVENT to the same aggregate so it holds more than one event, then re-commit with the same idempotency key. Assert the ORIGINAL stored record is returned with disposition REPLAYED — not ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS, and not a caller-supplied echo. Prove it is the STORED record by re-committing with the same key but different record bytes and requiring the stored bytes to win.\n\nCONSTRUCT THE TAIL DIRECTLY. Defect 1 is latent — `createFoundationClaudeLauncher` has zero callers — so a test that waits for a live Foundation path to append the tail will never exercise the defect and will pass vacuously.\n\nTEST B (DoD 2), the guard that must NOT weaken: an aggregate carrying TWO ACTIVATION events still refuses with the exact ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS code and its layer, asserted literally. This is the test that distinguishes a correct fix (narrow the input) from a wrong one (relax the count check). Without it, \"take the first event\" passes Test A while destroying the ledger's core guarantee.\n\nBoth assert code AND layer, never merely that the call failed — two layers can refuse here and these defects are precisely cases of the wrong refusal winning.\n\nTest A fails now; Test B passes now and must still pass after step 3.",
        "status": "PENDING",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-commit.test.ts"
        ]
      },
      {
        "stepId": "step-3",
        "description": "FIX DEFECT 1 — narrow the reader's input, in `apps/daemon/src/activation/activation-ledger-commit.ts` (or the reader, whichever keeps the invariant clearest).\n\nSelect the ACTIVATION events before the exactly-one check. The discriminator is already in the module — `ACTIVATION_LEDGER_EVENT_TYPE`, used at reader.ts:65 one line after the count guard — so filtering by it is applying an existing rule at the right moment, not inventing a new one.\n\nAFTER THE FIX THE GUARD MUST STILL MEAN \"exactly one activation record per aggregate\":\n- zero activation events -> ACTIVATION_LEDGER_EVIDENCE_ABSENT (unchanged)\n- two activation events -> ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS (unchanged; step 2's Test B pins it)\n- one activation event plus any number of foreign events -> the record (this is the fix)\n\nDO NOT relax `events.length > 1` to \"take the first\", and do not swap in `readFoundationActivationHistory` (:161) wholesale — the replay path needs ONE record with exactly-one semantics, not a history.\n\nPreserve the digest cross-check at :150: `answerReplayed` re-seals the durable record and refuses ACTIVATION_LEDGER_REPLAY_DIVERGED when it differs from the candidate. That is what stops a replay from echoing the caller, and it must survive untouched.\n\nRun both step-2 tests: A must now pass, B must still pass. Run the whole existing activation-ledger suite — DoD 6 requires every existing test green and UNMODIFIED.",
        "status": "PENDING",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-commit.ts",
          "apps/daemon/src/activation/activation-ledger-reader.ts"
        ]
      },
      {
        "stepId": "step-4",
        "description": "TDD FOR DEFECT 2 — write both tests first, in `apps/daemon/src/activation/activation-ledger-reader.test.ts`.\n\nTEST C (DoD 3), the reproduction: drive a REAL file-backed SqliteEventStore past the 6,400-event boundary, then assert `readCurrentEffectSessionBinding` FINDS the binding — not FOUNDATION_BINDING_SCAN_INCOMPLETE.\n\nPIN THE COUNT AS A LITERAL. Do NOT compute it from `MAX_SCAN_PAGES * SCAN_PAGE_SIZE`: a count derived from the constants it exists to pin moves with them, so shrinking the page cap later would silently shrink the proof instead of breaking the test. Task rail 3 requires the literal.\n\nPRACTICALITIES that decide whether this test is usable: insert the events in ONE transaction — 6,400 individual commits are slow enough to read as a hang. Open the store inside the test and close it in a `finally`; a SQLite handle held into teardown kills the vitest worker and looks like unrelated infrastructure failure. Use a real file-backed store, not an in-memory or hand-rolled one — a fake pager would prove nothing about the real cursor behaviour.\n\nTEST D (DoD 4), the invariant that must survive at scale: place a SECOND matching activation PAST the old 6,400 bound and assert the scan still refuses with the exact FOUNDATION_BINDING_EVIDENCE_AMBIGUOUS code and its layer. This is the test that stops the fix from buying scale by returning on first match — which would make Test C pass while turning a correct refusal into a wrong answer.\n\nBoth tests fail now: Test C returns SCAN_INCOMPLETE, and Test D never reaches its second match.",
        "status": "PENDING",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-reader.test.ts"
        ]
      },
      {
        "stepId": "step-5",
        "description": "FIX DEFECT 2 — remove the false ceiling, in `apps/daemon/src/activation/activation-ledger-reader.ts`.\n\nStop bounding `scanForEffect` by page COUNT. The loop already terminates without that cap: an empty page with `hasMore` returns SCAN_INCOMPLETE (:242), and a cursor that does not strictly advance returns SCAN_INCOMPLETE (:258-259). A stalled, lying or broken store is caught by those guards, so `MAX_SCAN_PAGES = 64` provides no safety a correct store needs — it only imposes an arbitrary 6,400-event ceiling that fails closed into a refusal indistinguishable from a real authority answer.\n\nKEEP THE UNIQUENESS PROOF INTACT (task rail 1). The scan still may not return early on a hit: it must keep walking to detect a second distinct aggregateId and refuse FOUNDATION_BINDING_EVIDENCE_AMBIGUOUS. Step 4's Test D is the check.\n\nKeep `SCAN_PAGE_SIZE` as the page size. If `MAX_SCAN_PAGES` becomes unreferenced, remove it rather than leaving a constant nothing reads — an unread bound reads to the next maintainer as an enforced limit.\n\nDO NOT grow this file toward 400 lines; it is already 328. If the change needs meaningful new code, extract a helper module with its own `.js` bridge instead.\n\nRAISE THE DESIGN QUESTION, do not solve it here (task rail 4). Even fixed, the scan is O(total project events) PER LOOKUP because it reads the global stream from cursor 0n and cannot exit early. Write that finding down and FILE A FOLLOW-UP TASK for a per-effect index or per-aggregate query, recording its id in the handoff. Landing an index unannounced is explicitly forbidden; leaving the performance cliff unrecorded just moves the time bomb.\n\nRun Tests C and D; both must pass. Run the whole existing suite — every existing test green and unmodified.",
        "status": "PENDING",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-reader.ts"
        ]
      },
      {
        "stepId": "step-6",
        "description": "MUTATION DRILLS (DoD 5). Capture `sha256sum` of each target BEFORE mutating; restore by Edit and verify by `sha256sum` against the captured hash. Never `git checkout` — it reverts to HEAD and would destroy this task's uncommitted work.\n\nD1 — DEFECT 1 RESTORED (mandatory). Put the whole-aggregate read back: hand every event to the exactly-one reader. Step 2's Test A must go red on the returned-record assertion, showing ACTIVATION_LEDGER_EVIDENCE_AMBIGUOUS. Record the test name.\n\nD2 — THE EXACTLY-ONE GUARD (independent of D1). Relax the activation-event count check to \"take the first\". Step 2's Test B must go red. If D1 and D2 redden the same single test, the fix and the invariant are not separately pinned and one of them is unproven.\n\nD3 — DEFECT 2 RESTORED (mandatory). Reinstate the 64-page bound. Step 4's Test C must go red on the past-6,400 case with FOUNDATION_BINDING_SCAN_INCOMPLETE. Record the test name and confirm the failure is the refusal, not a timeout — a drill that hangs reads as a passing guard.\n\nD4 — EARLY RETURN ON FIRST MATCH. Make `scanForEffect` return as soon as it finds one match. Step 4's Test D must go red, proving the uniqueness proof is still load-bearing and that scale was not bought by dropping it.\n\nD5 — THE LITERAL COUNT. Change `SCAN_PAGE_SIZE` so the old ceiling would move. Test C must NOT silently start proving less — if its pinned count is a literal it still drives past the same absolute number, which is the point of task rail 3. If Test C's coverage tracks the constant, the count was derived rather than pinned; fix the test.\n\nFor each drill record the test name and the assertion that reddened.",
        "status": "PENDING",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-reader.ts",
          "apps/daemon/src/activation/activation-ledger-commit.ts"
        ]
      },
      {
        "stepId": "step-7",
        "description": "GATE, ADVERSARIAL REVIEW, COMMIT (DoD 6). Each leg's exit code read SEPARATELY — a `&&`-chained run reports one status for two legs and a piped run reports the pipe's tail:\n1. `pnpm --filter @moe/daemon typecheck; echo \"TYPECHECK_EXIT=$?\"`\n2. `pnpm --filter @moe/daemon test; echo \"TEST_EXIT=$?\"`\nBoth must print 0. Record the vitest summary line and confirm a NONZERO executed test count. Note the suite is now slower because of the 6,400-event case — a timeout is a real failure here, not flake; if it times out, batch the inserts rather than shrinking the count.\n\n`grep -c ''` activation-ledger-reader.ts and activation-ledger-commit.ts — the reader was 328 and must not have moved toward 400.\n\nDoD 6 EXPLICITLY: every EXISTING activation-ledger test green and UNMODIFIED. If one reddened, the fix changed behaviour it should not have — fix the fix, never the test.\n\nADVERSARIAL SELF-REVIEW — invoke the skill, then check what is specific here:\n1. Does a genuinely ambiguous aggregate (two ACTIVATION events) still refuse? If not, the fix weakened the guard instead of narrowing the input.\n2. Does the scan still detect a second match past the old bound? If not, scale was bought with the uniqueness proof.\n3. Is Test C's event count a LITERAL, or derived from the page constants?\n4. Did the digest cross-check at commit.ts:150 (ACTIVATION_LEDGER_REPLAY_DIVERGED) survive? It is what stops a replay echoing the caller.\n5. Is any constant left in the file that nothing reads? An unread bound reads as an enforced limit to the next maintainer.\n6. Was the durable event format, the digest/seal discipline, or any foundation-launch-authority admission guard touched? All four are out of scope.\n\nCOMMIT BY EXPLICIT PATHSPEC naming only the owned activation-ledger paths. Never `git add -A` (epic rail 3).\n\nHANDOFF must record TWO things: that defect 1 was LATENT (createFoundationClaudeLauncher has zero callers) so the fix is preventive rather than incident-driven; and the id of the follow-up task filed for the O(total events) per-lookup scan, which this task deliberately did not solve.",
        "status": "PENDING",
        "affectedFiles": [
          "apps/daemon/src/activation/activation-ledger-reader.ts",
          "apps/daemon/src/activation/activation-ledger-commit.ts"
        ]
      }
    ],
    "stepsCompleted": [
      "step-1"
    ]
  },
  "planSizeWarnings": [
    "Plan touches 8 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null
}