{
  "id": "task-14a2f0e4221e4def9153a2082a9385bf",
  "epicId": "epic-f02c6977535f4d4fa4c9ebf28d0662b8",
  "title": "Headless MCP access: launch + wire a free-session channel to a live moe-next project",
  "description": "MOTIVATION (owner, live UnAI deploy 2026-09-17): a \"free\"/headless session (an agent, a CLI, this Claude session) cannot drive a running moe-next project. The browser control-room command route (POST /command, /affordances/read) enforces checkHeaders = Host + Origin + CSRF (http-listener-guards.ts:347); a headless POST with only the operator credential + x-moe-protocol-version gets 426/refused because the CSRF token is session-minted at pairing. So today the ONLY way to create a goal / read state is a paired browser.\n\nWHAT ALREADY EXISTS (measured, not assumed):\n- moe-next SHIPS an MCP server: stdio entry apps/daemon/src/mcp-main.ts and loopback HTTP host apps/daemon/src/mcp-http/ (mcp-http-main.ts, mcp-http-host.ts binds 127.0.0.1/::1/localhost from an env port). It authenticates by CREDENTIAL/bearer, NOT the browser Host+Origin+CSRF wall — exactly the free-session path.\n- goal.create AND goal.create_with_source are MCP-WIRED: they are NOT in MCP_EXCLUDED_COMMAND_KINDS (that set is derived from OPERATOR_PRINCIPAL_KINDS and only strips operator-only kinds — goal.cancel/goal.close were added there 2026-09-17). wiredMcpToolKinds() therefore includes goal creation, plus MCP_SERVED_QUERY_KINDS for reads.\n- So the capability is ~90% present. The gap is launch + client wiring + docs, not build-from-scratch.\n\nTHE GAP:\n1. `moe start` does NOT launch the MCP server (grep of moe.ps1 / moe-cli-main.ts: no mcp wiring). There is no operator-facing launcher that starts the stdio/HTTP MCP bound to a given project's store + credential.\n2. No client wiring: nothing points an MCP client (an agent session, a .mcp.json) at a live project's MCP endpoint.\n3. No skill/doc telling a free session which tools exist (goal.create_with_source, the read/query kinds) and how to reach a live project.\n\nDELIVERABLE OPTIONS (pick the leanest in planning; MCP is the answer, skill/api are the client-side dressing):\n- A `moe mcp <projectPath>` launcher (or a `moe start --mcp` flag) that starts the MCP server bound to that project's store + operator credential from moe.config.json.\n- A documented .mcp.json / connection recipe so an agent session attaches to it.\n- A skill (or INSTALL.md section) listing the goal-create + read tools and the connect steps.\n\nRe-measure before planning (rails): confirm goal.create_with_source is in wiredMcpToolKinds() at HEAD; confirm mcp-http auth model (bearer, loopback, no CSRF); confirm the stdio entry's arg/env contract (project/store/credential).",
  "definitionOfDone": [
    "A launcher (moe mcp <projectPath> or moe start --mcp) starts the stdio and/or loopback-HTTP MCP server bound to the named project's store + operator credential; verified it comes up against the live UnAI project (unai-9705356d4240) without a browser.",
    "A free session (no browser, no pairing) dispatches goal.create_with_source over MCP and it commits GoalCreated on the target project; the round-trip binds the PRD source (displayPath/mediaType/text). Test asserts the exact reason code on both the accept and a refusal path (global rail: assert the code+layer, not just outcome).",
    "The free session reads the goal catalog / a state query over the MCP query kinds and gets the new goal back.",
    "A skill or INSTALL.md section documents the exposed tool set (goal.create/create_with_source + MCP_SERVED_QUERY_KINDS) and the connect recipe; bidirectional allowlist test still green (wiredMcpToolKinds set-equality).",
    "Re-measured at HEAD and recorded in planningNotes: goal.create_with_source present in wiredMcpToolKinds(), mcp-http auth = bearer/loopback/no-CSRF, stdio entry project/credential contract. pushe directly to main!"
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "CONFIRM THE MEASURED SURFACE STILL HOLDS AT YOUR HEAD, then stop. Re-run the four measurements recorded in planningNotes.codebaseInsights and paste their output into the step note. (a) Write a throwaway probe at apps/daemon/src/probe-wired.mts that imports { wiredMcpToolKinds } from \"./mcp-tool-allowlist.js\" and prints whether \"goal.create_with_source\" and \"goal.close\" are members plus the total length; run it with `npx tsx apps/daemon/src/probe-wired.mts` and DELETE IT IN THE SAME SHELL COMMAND (epic rail: no probe files in commits). Expect goal.create_with_source PRESENT, goal.close ABSENT, TOTAL 43. (b) `grep -n \"KNOWN_COMMANDS\" apps/daemon/src/cli/moe-cli-argv.ts` -- expect no \"mcp\" entry. (c) `grep -n \"LOOPBACK_HOSTS\\|MCP_HTTP_HOST_NON_LOOPBACK_BIND\" apps/daemon/src/mcp-http/mcp-http-host.ts` -- expect the loopback set and the refusal. (d) `Test-Path D:/projexts/UnAI/moe.config.json` -- expect True; step 8 needs it. IF ANY OF (a)-(c) DISAGREES with planningNotes, stop and moe.report_blocked naming the exact disagreement rather than planning around it. If only (d) is missing, continue -- step 8 has a named substitute. Note the 6 pre-existing foreign dirty paths in the shared checkout in your step note and do not touch them.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "RE-MEASURED AT MY HEAD (26403620). All four agree with planningNotes; nothing to block on.\n\n(a) tsx probe apps/daemon/src/probe-wired.mts (created, run, `rm -f`'d in ONE shell command; `ls` in the same command confirms \"No such file or directory\", so no probe file can enter a commit):\n  TOTAL 43\n  goal.create_with_source PRESENT\n  goal.close ABSENT\n  goal.create PRESENT\nMatches the expected 43 / PRESENT / ABSENT exactly.\n\n(b) `grep -n \"KNOWN_COMMANDS\" apps/daemon/src/cli/moe-cli-argv.ts`:\n  13:export const KNOWN_COMMANDS = Object.freeze([\"init\", \"start\", \"recover-review\", \"recover-replan\", \"projects\", \"version\", \"help\"] as const);\n  164:    `${MOE_CLI_UNKNOWN_COMMAND}: ${head} — known commands: ${KNOWN_COMMANDS.join(\", \")}`,\nNo \"mcp\" entry. Gap confirmed. Exactly the two references the plan's shape-consumer enumeration predicted.\n\n(c) `grep -n \"LOOPBACK_HOSTS\\|MCP_HTTP_HOST_NON_LOOPBACK_BIND\" apps/daemon/src/mcp-http/mcp-http-host.ts`:\n  75:  \"MCP_HTTP_HOST_NON_LOOPBACK_BIND\",\n  88:const LOOPBACK_HOSTS: ReadonlySet<string> = new Set([\"127.0.0.1\", \"::1\", \"[::1]\", \"localhost\"]);\n  196:    if (!LOOPBACK_HOSTS.has(host)) return refuse(\"MCP_HTTP_HOST_NON_LOOPBACK_BIND\");\nLoopback set + refusal both present as recorded.\n\n(d) `ls -la /d/projexts/UnAI/moe.config.json` -> `-rw-r--r-- 1 Yaron 197121 213 Sep 13 00:03`. PRESENT. Step 8 runs the LIVE arm against the real UnAI project; the named substitute (`moe init` a scratch dir) is NOT needed.\n\nFOREIGN DIRTY PATHS in this shared checkout at step start (`git status --porcelain`): 26 modified + 5 untracked, ALL board/runtime metadata, zero product source: .moe/messages/*.jsonl (5), .moe/pins/*.json, .moe/project.json, .moe/roles/*.md (7), .moe/skills/moe-qa-loop/SKILL.md, .moe/tasks/*.json (10 modified + 2 untracked), CLAUDE.md, and untracked .moe/attempts/, .moe/resources/browser-e2e-gate.json, .moe/resources/full-suite-gate.json. None is mine, none will be staged, reverted or stashed, and none is a stop condition. My owned paths are all under apps/daemon/src/cli, apps/daemon/src and tools/packaging.",
      "startedAt": "2026-09-17T21:00:27.828Z",
      "completedAt": "2026-09-17T21:00:55.301Z"
    },
    {
      "stepId": "step-2",
      "description": "RED -- argv tests for the `mcp` verb, INCLUDING the bidirectional roster arm that the existing test cannot provide. In apps/daemon/src/cli/moe-cli-argv.test.ts add: (1) `parseCliArgv([\"mcp\"])` yields {command:\"mcp\", ok:true, targetDir:\".\"}; (2) `parseCliArgv([\"mcp\",\"D:/projexts/UnAI\"])` yields targetDir \"D:/projexts/UnAI\"; (3) `parseCliArgv([\"mcp\",\"a\",\"b\"])` refuses with code MOE_CLI_TOO_MANY_ARGUMENTS and detail \"b\"; (4) `parseCliArgv([\"mcp\",\"--operator-stdin\"])` refuses with code MOE_CLI_UNKNOWN_OPTION and detail \"--operator-stdin\" -- pairing is a browser concept and has no meaning on a stdio wire, so `mcp` accepts NO options. (5) THE MANDATORY BIDIRECTIONAL ARM (global rail on roster coverage): the existing arm at line ~96 iterates KNOWN_COMMANDS and only asserts each name appears in the unknown-command MESSAGE -- adding \"mcp\" to the roster grows that iteration and it stays green even with no parse branch, so it can never catch an advertised-but-unparsed verb. Add the opposite direction: `for (const command of KNOWN_COMMANDS) expect(parseCliArgv([command]).ok, command).toBe(true)` -- i.e. every ADVERTISED command actually PARSES. Assert the exact refusal CODES (MOE_CLI_UNKNOWN_OPTION, MOE_CLI_TOO_MANY_ARGUMENTS), never merely that `ok` is false. Run the file and CONFIRM IT IS RED before moving on; paste the failing output into the step note.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cli/moe-cli-argv.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cli/moe-cli-argv.test.ts"
      ],
      "note": "RED CONFIRMED. Added 5 arms to apps/daemon/src/cli/moe-cli-argv.test.ts (arms 1-4 from the step, plus the mandatory bidirectional roster arm).\n\n`pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli/moe-cli-argv.test.ts`:\n  ⎯⎯⎯ Failed Tests 4 ⎯⎯⎯\n   FAIL  src/cli/moe-cli-argv.test.ts > parseCliArgv accepts the shipped roster > defaults an argument-free mcp to the current directory\n   FAIL  src/cli/moe-cli-argv.test.ts > parseCliArgv accepts the shipped roster > takes the mcp target from the first positional argument\n   FAIL  src/cli/moe-cli-argv.test.ts > parseCliArgv refuses by name > refuses a second positional argument on mcp and names the extra token\n  AssertionError: expected 'MOE_CLI_UNKNOWN_COMMAND' to be 'MOE_CLI_TOO_MANY_ARGUMENTS' // Object.is equality\n   FAIL  src/cli/moe-cli-argv.test.ts > parseCliArgv refuses by name > refuses --operator-stdin on mcp, which accepts no options at all\n  AssertionError: expected 'MOE_CLI_UNKNOWN_OPTION' ... Received: \"MOE_CLI_UNKNOWN_COMMAND\"\n   Test Files  1 failed (1)\n        Tests  4 failed | 18 passed (22)\n\nThe two refusal arms fail on the CODE, not on `ok`: today `mcp` falls through to MOE_CLI_UNKNOWN_COMMAND, so `refused()` succeeds and only the specific-code assertion catches it. That is the rail working — an arm that asserted merely \"!ok\" would already be GREEN here with no verb implemented at all.\n\nTHE FIFTH ARM (\"parses every command it advertises, not merely names it\") IS GREEN RIGHT NOW BY DESIGN, and that is not a vacuous pass: it iterates KNOWN_COMMANDS, which does not yet contain \"mcp\", and every one of the 7 current verbs does parse. It becomes load-bearing the instant step 3 adds \"mcp\" to the roster — if I add the name without the parse branch it goes RED, which is exactly the failure mode the existing arm at line ~96 structurally cannot see (that arm only checks each roster name appears in the unknown-command MESSAGE, so it grows its own iteration and stays green). Step 8C drills precisely this: delete the parse branch while leaving \"mcp\" in the roster and confirm this arm reds.\n\nNo production file touched in this step.",
      "startedAt": "2026-09-17T21:00:58.336Z",
      "completedAt": "2026-09-17T21:01:38.571Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN -- add the `mcp` verb to the pure argv layer. In apps/daemon/src/cli/moe-cli-argv.ts: (1) add \"mcp\" to KNOWN_COMMANDS (place it after \"start\" so the help roster reads in the order an operator meets them); (2) add `export interface CliMcp { readonly command: \"mcp\"; readonly ok: true; readonly targetDir: string; }` and add CliMcp to the CliInvocation union; (3) add a branch `if (head === \"mcp\")` that calls `unknownOption(parts, [])` -- the EMPTY allowed list, so the existing helper's \"options for this command: none\" message is produced verbatim with no new code -- then `targetOf(parts)` and returns the frozen CliMcp. Do NOT copy the start branch: `mcp` must not accept --operator-stdin. Keep the file under the 250-line target (it is 166 now). Re-run the argv test file and confirm every arm from step 2 is GREEN, including the new bidirectional arm.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cli/moe-cli-argv.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cli/moe-cli-argv.ts"
      ],
      "note": "GREEN. apps/daemon/src/cli/moe-cli-argv.ts now serves the `mcp` verb.\n\nThree edits, exactly as planned and no more:\n1. KNOWN_COMMANDS:13 -> [\"init\", \"start\", \"mcp\", \"recover-review\", \"recover-replan\", \"projects\", \"version\", \"help\"] — \"mcp\" placed after \"start\", the order an operator meets them.\n2. New `export interface CliMcp { command: \"mcp\"; ok: true; targetDir: string }` with a comment stating why it carries no option field, and CliMcp added to the CliInvocation union (union re-wrapped to 3 lines to stay readable).\n3. New branch before the `projects` branch:\n     if (head === \"mcp\") {\n       const bad = unknownOption(parts, []);\n       if (bad !== null) return bad;\n       const target = targetOf(parts);\n       if (typeof target !== \"string\") return target;\n       return Object.freeze({ command: \"mcp\", ok: true, targetDir: target });\n     }\n   The EMPTY allowed list reuses the existing helper's `allowed.length === 0 ? \"none\"` arm, so \"options for this command: none\" is produced with zero new code. The start branch was NOT copied: no --operator-stdin, no operatorStdin field.\n\n`pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli/moe-cli-argv.test.ts`:\n   Test Files  1 passed (1)\n        Tests  22 passed (22)\nA `Test Files N passed` count line IS present with a nonzero test count, so the run is not vacuous. All 4 step-2 arms flipped RED->GREEN, and the bidirectional arm now genuinely exercises \"mcp\": it iterates a roster that contains it and asserts it parses to ok:true.\n\nFILE SIZE: 185 lines (was 166). Under the 250 target, well under 400.\n\nONE EXPECTED TYPECHECK RED, resolved by step 5 and disclosed here rather than left for a grader to find:\n  `pnpm --filter @moe/daemon exec tsc --project tsconfig.json --noEmit`\n  src/cli/moe-cli-main.ts(284,25): error TS2345: Argument of type 'CliMcp | CliStart' is not assignable to parameter of type 'CliStart'.\nThis is tsc doing its job: moe-cli-main.ts:284 is the fall-through `return await runStart(invocation, io)`, and widening the union without adding the dispatch branch narrows nothing, so CliMcp reaches a CliStart parameter. It is caused by this step and fixed by this task (step 5 adds the `mcp` dispatch branch above it). It is NOT foreign red and NOT pre-existing. I verified this is the ONLY tsc error introduced — the compiler reported exactly one.",
      "startedAt": "2026-09-17T21:01:42.120Z",
      "completedAt": "2026-09-17T21:02:24.100Z"
    },
    {
      "stepId": "step-4",
      "description": "The runner: a NEW production module plus its bridge, kept out of moe-cli-main.ts because that file is ALREADY 347 lines and the epic rail says split before 400. Create apps/daemon/src/cli/moe-cli-mcp.ts exporting `runMcp(invocation, io): Promise<number>`. It must: (1) resolve targetDir against io.cwd and read moe.config.json through the SAME readConfig path moe-cli-main.ts uses -- export readConfig from moe-cli-main.ts if it is currently module-private, or move it here and import it back; whichever keeps one implementation, never two; (2) call preparePackagedLinks BEFORE launching (insight 7: junctions are lazy, and `@moe/mcp` will not resolve in a freshly extracted artifact without them); (3) set process.env MOE_STORE_PATH, MOE_PROJECT_ID, MOE_DAEMON_CREDENTIAL and MOE_SESSION_CREDENTIAL from the config -- the last two both take config.credential, which is the operator secret, matching mcp-main.ts's documented contract; resolve storePath against the project root so a relative config value works from any cwd; (4) `await (await import(\"../mcp-main.js\")).runMcpMain()` and return 0, returning 1 on a thrown startup error after writing the error's message. THE LOAD-BEARING CONSTRAINT: STDOUT IS THE JSON-RPC WIRE. This function must never call io.log (moe-cli-main.ts:307 wires it to process.stdout). Take an explicit stderr sink -- add `readonly diagnostic: (line: string) => void` to the CliIo interface in moe-cli-main.ts, wired to `process.stderr.write(line + \"\\n\")` in the real main -- and use it for EVERY line this verb emits, banner and refusal alike. One sink, no branching. Also create the one-line bridge apps/daemon/src/cli/moe-cli-mcp.js containing exactly `export * from \"./moe-cli-mcp.ts\";`, matching every sibling in that directory. Never log the credential's value (moe-init.ts ships that discipline; the config provenance lines name variables, never values).",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "apps/daemon/src/cli/moe-cli-mcp.ts",
        "apps/daemon/src/cli/moe-cli-mcp.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cli/moe-cli-mcp.ts",
        "apps/daemon/src/cli/moe-cli-mcp.js",
        "apps/daemon/src/cli/moe-cli-main.ts",
        "apps/daemon/src/cli/moe-cli-main.test.ts",
        "apps/daemon/src/cli/moe-cli-cwd.test.ts",
        "apps/daemon/src/cli/moe-cli-replan-recovery.test.ts",
        "apps/daemon/src/cli/moe-cli-recover-review.test.ts",
        "apps/daemon/src/projects/project-review-recovery-native.test.ts"
      ],
      "note": "RUNNER LANDED. apps/daemon/src/cli/moe-cli-mcp.ts (63 lines) + its one-line bridge moe-cli-mcp.js (`export * from \"./moe-cli-mcp.ts\";`, byte-format matched against the sibling moe-cli-argv.js).\n\nVERIFIED BEFORE CODING (explore-before-assume), not assumed:\n- `runMcpMain` IS exported: mcp-main.ts:178 `export { main as runMcpMain }`, and its auto-run is fenced by `meta.main === true` (:171), so importing the module does NOT start a server as a side effect.\n- The bridge apps/daemon/src/mcp-main.js already exists, so `import(\"../mcp-main.js\")` resolves.\n- Env contract re-read at daemon-store-dependencies.ts:24 — `ENV_KEYS = [\"MOE_STORE_PATH\",\"MOE_PROJECT_ID\",\"MOE_DAEMON_CREDENTIAL\"]`, exactly three, plus MOE_SESSION_CREDENTIAL via readBootstrapCredential. No MOE_PROJECT_ROOT needed.\n- MoeConfig is exactly {credential, projectId, schemaVersion, storePath} (moe-init.ts:99). Read the LIVE file: D:/projexts/UnAI/moe.config.json carries all four, projectId `unai-9705356d4240` (the id named in the DoD), storePath `D:\\projexts\\UnAI\\store.sqlite` (absolute), schemaVersion `moe-cli-config/1`, credential 64 chars. Step 8's live arm is executable.\n\nWHAT runMcp DOES: resolve targetDir against io.cwd -> readConfig -> preparePackagedLinks(\"mcp\") -> set the four env vars -> `await (await import(\"../mcp-main.js\")).runMcpMain()` -> return 0; on throw, emit `MOE_CLI_MCP_UNAVAILABLE: <message>` and return 1. storePath goes through `resolve(projectRoot, config.storePath)`, so an absolute value passes through unchanged and a relative one binds to the project root, never the cwd.\n\nTHE STDOUT HAZARD, handled at the SINK not the call sites. readConfig and preparePackagedLinks are SHARED with `moe start`, where io.log is stdout. Rather than thread a sink parameter through both (which only redirects the lines I remembered to change), runMcp builds `const offWire: CliIo = { ...io, log: io.diagnostic }` and hands THAT to both helpers. A line added to either helper later cannot leak onto the JSON-RPC wire behind this verb's back. Every line runMcp itself writes also goes to io.diagnostic. One destination, no branching. The credential's VALUE appears in no line: the provenance lines name the projectId and the path only.\n\nSINGLE IMPLEMENTATION, as the step required: readConfig was module-private; it is now `export function readConfig` (moe-cli-main.ts) with a comment saying why. Not copied. preparePackagedLinks likewise exported, and its command union widened with \"mcp\".\n\nCliIo GAINED A REQUIRED `diagnostic: (line: string) => void`, wired to `process.stderr.write` in the real main (moe-cli-main.ts, beside the stdout `log`). I made it REQUIRED rather than optional deliberately: an optional sink either falls back to stdout (the exact bug) or silently drops diagnostics, and a required field forces every composition root to answer where its diagnostics go.\n\nFOUR EXTRA TEST FILES IN THE DIFF, DISCLOSED — this is 4 more paths than the plan's 9. Making the field required made tsc enumerate every CliIo construction site for me, and it found two my `grep CliIo` had MISSED (moe-cli-cwd.test.ts and moe-cli-replan-recovery.test.ts build the object as a bare literal with no `CliIo` type annotation, so a symbol grep cannot see them — the compiler can). Each fix is one word: `diagnostic: () => {}`. The files: moe-cli-cwd.test.ts (1 site), moe-cli-replan-recovery.test.ts (3 sites), moe-cli-recover-review.test.ts (1), projects/project-review-recovery-native.test.ts (1). A no-op is the ACCURATE sink there: none of those paths emits a diagnostic line, only `mcp` does. I checked `git diff HEAD --` on all four before editing: empty, so no peer hunk was swept.\nThe moe-cli-main.test.ts harness also gained a `diagnostics` capture array (kept apart from `lines` so step 5 can prove WHICH sink a line took); its assertions land in step 5.\n\nTYPECHECK STATE, honestly reported. `pnpm --filter @moe/daemon exec tsc --project tsconfig.json --noEmit` now reports EXACTLY ONE error, the same one step 3 disclosed:\n  src/cli/moe-cli-main.ts(299,25): error TS2345: Argument of type 'CliMcp | CliStart' is not assignable to parameter of type 'CliStart'.\nAll six errors that existed mid-step (the five TS2741 missing-`diagnostic` ones) are gone. The remaining one is the un-dispatched `mcp` branch and step 5 closes it.",
      "startedAt": "2026-09-17T21:02:27.592Z",
      "completedAt": "2026-09-17T21:05:23.304Z"
    },
    {
      "stepId": "step-5",
      "description": "Wire the verb into the CLI and pin its behaviour. In apps/daemon/src/cli/moe-cli-main.ts: (1) add a `case \"mcp\"` / `if (invocation.command === \"mcp\")` branch in the same dispatch that routes start/projects, lazily importing runMcp from \"./moe-cli-mcp.js\" exactly as recoverReview/recoverReplan are imported; (2) widen the preparePackagedLinks command union to include \"mcp\"; (3) add a USAGE line `\"  moe mcp [dir]             serve this project to a headless MCP client over stdio (no browser, no pairing)\"` directly after the `moe start` line; (4) add `readonly diagnostic` to CliIo and wire it to stderr in the real main. Then in apps/daemon/src/cli/moe-cli-main.test.ts add: (a) `moe mcp` on a directory with no moe.config.json returns 1 and emits the exact code MOE_CONFIG_UNREADABLE (or MOE_CONFIG_INVALID for a malformed file -- assert the SPECIFIC code, and assert it arrives on the DIAGNOSTIC sink, not the log sink); (b) a happy-path arm with runMcp's import seam stubbed, asserting the four env variables are set to the config's values and that the STDOUT log sink received ZERO lines -- this is the arm that catches JSON-RPC wire poisoning, so assert emptiness explicitly, not merely that the call succeeded; (c) `moe --help` output contains \"moe mcp [dir]\" -- the USAGE block has no asserting consumer today, so this pins it. Test-file line count is not capped (the per-file cap is per PRODUCTION file), so these belong here rather than in a tenth file.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/cli/moe-cli-main.ts",
        "apps/daemon/src/cli/moe-cli-main.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/cli/moe-cli-main.ts",
        "apps/daemon/src/cli/moe-cli-main.test.ts"
      ],
      "note": "WIRED AND PINNED. `moe mcp` now dispatches, and four new arms hold it.\n\nPRODUCTION (apps/daemon/src/cli/moe-cli-main.ts, 347 -> 368 lines):\n- dispatch branch placed before `projects`, lazy-importing exactly as recoverReview/recoverReplan do:\n    if (invocation.command === \"mcp\") {\n      const { runMcp } = await import(\"./moe-cli-mcp.js\");\n      return await runMcp(invocation, io);\n    }\n- USAGE gained `\"  moe mcp [dir]             serve this project to a headless MCP client over stdio (no browser, no pairing)\"` directly after the `moe start` line.\n- preparePackagedLinks command union widened with \"mcp\" and CliIo.diagnostic wired to process.stderr — both landed in step 4, where runMcp needed them.\n\nTYPECHECK NOW CLEAN: `pnpm --filter @moe/daemon exec tsc --project tsconfig.json --noEmit` -> TSC EXIT: 0 (captured via ${PIPESTATUS[0]}, not the pipe's tail). The TS2345 CliMcp/CliStart error disclosed in steps 3 and 4 is closed by this dispatch branch, as predicted. Zero errors in the package.\n\nREFUSAL CODES: I ASSERTED WHAT I MEASURED, NOT WHAT THE STEP PREDICTED. The step text predicted \"MOE_CONFIG_UNREADABLE (or MOE_CONFIG_INVALID for a malformed file)\" with no file:line. Measured truth, with anchors:\n- ABSENT config -> `MOE_CLI_CONFIG_ABSENT` (declared moe-cli-main.ts:27, emitted from readConfig's `!existsSync` branch). NOT MOE_CONFIG_UNREADABLE — that constant never reaches this path.\n- MALFORMED config (`{not json`) -> `MOE_CONFIG_UNREADABLE` (moe-init.ts:30, returned by parseMoeConfig's JSON.parse catch at :198). NOT MOE_CONFIG_INVALID — INVALID (moe-init.ts:32, :201/:206) is for valid JSON of the wrong SHAPE, a different input class.\nBoth observed FROM A RUN (the arms are green against the production surface), not read off the source. The behavioural clause — \"returns 1 and emits a specific coded refusal on the diagnostic sink\" — is satisfied; I covered both input classes rather than matching a predicted string.\n\nFOUR ARMS in apps/daemon/src/cli/moe-cli-main.test.ts, new `describe(\"moe mcp\")`:\n(a) uninitialized target -> code 1, MOE_CLI_CONFIG_ABSENT on DIAGNOSTICS, `lines` (stdout) EMPTY, stdio entry never called.\n(b) corrupted config -> code 1, MOE_CONFIG_UNREADABLE on DIAGNOSTICS, stdout EMPTY, entry never called.\n(c) launch throws -> code 1, MOE_CLI_MCP_UNAVAILABLE on DIAGNOSTICS, stdout EMPTY. (Extra arm beyond the step: a startup failure is the one path that could plausibly dump a raw stack onto the wire, so it is pinned too.)\n(d) HAPPY PATH / THE WIRE ARM -> code 0; the stdio entry received MOE_PROJECT_ID = expectedProjectId(project), MOE_STORE_PATH = join(project,\"store.sqlite\") (which also proves `resolve(projectRoot, storePath)` binds to the project, not the cwd), and BOTH credential variables = the config credential; `result.lines` asserted `toEqual([])` — EMPTINESS, explicitly, not merely a 0 exit; and the diagnostics asserted NOT to contain the credential value.\n(e) the help arm was extended: \"moe mcp [dir]\" added to the command roster it walks, plus \"no browser, no pairing\". The USAGE block had no asserting consumer before this task.\n\nSEAM: `vi.mock(\"../mcp-main.js\")` with a `vi.hoisted` capture that SNAPSHOTS process.env INSIDE the call — necessary because runMcp mutates the real process.env, so a snapshot taken after the fact would read whatever afterEach left. afterEach deletes all four MOE_* keys and resets the stub, so the operator credential this verb writes into the test process cannot leak into a later arm.\n\nGATE: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli`\n   Test Files  9 passed (9)\n        Tests  126 passed (126)\nA `Test Files N passed` line with a nonzero count IS present — not a vacuous run. Every pre-existing cli arm still green alongside the new ones.\n\nFOLLOW-UP, recorded not filed (DELIVERY BIAS rail): moe-cli-main.ts is now 368 lines — under the 400 hard split bar, over the 250 target. My diff added 21 lines and the plan already kept the runner out of this file for exactly this reason. Splitting it is a separate refactor with no bearing on delivered behaviour; noting it here for the epic-final hardening row rather than spinning a row or widening this one.",
      "startedAt": "2026-09-17T21:05:26.024Z",
      "completedAt": "2026-09-17T21:07:13.331Z"
    },
    {
      "stepId": "step-6",
      "description": "The free-session round-trip, in a NEW file apps/daemon/src/mcp-free-session-roundtrip.test.ts (mcp-dispatch-port.test.ts is already 842 lines -- do not extend it). Build the harness by copying the real-store setup at the top of mcp-dispatch-port.test.ts (mkdtempSync + createStoreDependencies + SqliteEventStore.openForProject + installTestRecoveryBinding), but compose the port WITH `documents: provider.goalSource?.()` -- the existing harness omits it and goal.create_with_source plus documents.source_read both need it. Three arms, each asserting a STABLE CODE and naming WHICH LAYER refused: (1) ACCEPT -- dispatch goal.create_with_source with its exact payload keys [\"instructions\",\"source\",\"title\"] (measured at daemon-command-payload-keys.ts:149), carrying a source with displayPath/mediaType/text; assert the response is an accepted outcome, assert a GoalCreated event is committed to the store, and capture the new goalRef. (2) READ-BACK (DoD item 3) -- dispatch the documents.source_read QUERY with that goalRef and assert the returned displayPath, mediaType and text equal what arm 1 supplied, proving the free session both created the goal and can read its state back over the MCP query kinds. (3) REFUSE -- drive the PRODUCTION stdio surface (createStdioMcpServer with `toolAllowlist: wiredMcpToolKinds()`, exactly as mcp-main.ts composes it) and call the tool label for `goal.close`; assert the refusal code is exactly CAPABILITY_DENIED and that it came from the TRANSPORT layer -- i.e. before envelope construction, authentication or dispatch (stdio-server.ts callTool refuses a known-but-unadvertised label). Prove the layer, do not assert it by comment: make the dispatch port a spy and assert it was NEVER invoked for that call. This matters because the port authenticates with the operator credential as fallbackCredential, so a capability check alone would PASS -- the roster exclusion is the only fence, and an arm that merely asserted \"refused\" would stay green if the fence moved to a weaker layer. MUTATION-VERIFY ALL THREE: per epic rail 6, take a SHA256 backup of mcp-tool-allowlist.ts to %TEMP%/moe-task-14a2f0e4-mcp-tool-allowlist-<workerId>.ts (task id in the path, per the drill rail), then temporarily re-admit goal.close to the roster and confirm arm 3 goes RED; restore from the backup and prove restoration by SHA256 equality in a SEPARATE command from the restore. Record both hashes in the step note.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "apps/daemon/src/mcp-free-session-roundtrip.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/mcp-free-session-roundtrip.test.ts",
        "apps/daemon/package.json",
        "pnpm-lock.yaml"
      ],
      "note": "ROUND-TRIP LANDED, ALL THREE ARMS MUTATION-VERIFIED. New file apps/daemon/src/mcp-free-session-roundtrip.test.ts (4 arms, 212 lines). mcp-dispatch-port.test.ts NOT extended.\n\nHARNESS: real file-backed store in a mkdtemp dir; bootstrap prerequisites (`goal.create_with_source` declares `project.activate`, bootstrap-sequence.ts:31) seeded through the PRODUCTION bootstrap pipeline via driveThrough(store,\"goal.create\"), then the store is closed and reopened by createStoreDependencies. ONLY the goal creation itself crosses the MCP boundary — that is the claim under test. Port composed exactly as mcp-main.ts composes it, INCLUDING `documents: provider.goalSource?.()`, plus affordances/commandAuthorityPlane/design/graph/v2Deps.\n\nARM 1 ACCEPT — goal.create_with_source with the exact three payload keys measured at daemon-command-payload-keys.ts:149 ([\"instructions\",\"source\",\"title\"]), source = {displayPath,mediaType,text}. Asserts `{decision:{disposition:\"DECIDED\",resultCode:\"EFFECTS_COMMITTED\"}, ok:true, outcome:\"ACCEPTED\"}` AND then reopens the store and asserts `GoalCreated` is in readEvents(goalRef) — the wire answer is a claim, the store is the truth. targetAggregateId = `goal-${commandId}`, the identity production derives from the authenticated command (bootstrap-test-fixtures.ts:55-58).\n\nARM 2 READ-BACK (DoD item 3) — documents.source_read over dispatchQueryBytes with that goalRef; asserts displayPath, mediaType and text EQUAL what arm 1 supplied, plus contentSha256 equals sha256(PRD) computed independently in the test. Not \"it answered something\".\n\nARM 3 REFUSE, WITH THE LAYER PROVEN — production stdio surface: createStdioMcpServer({credential, port: spy, serverName:\"moe-next\", toolAllowlist: wiredMcpToolKinds()}), driven by a real SDK Client over InMemoryTransport, calling label `goal_close`. Asserts McpError.data.code === \"CAPABILITY_DENIED\" AND `dispatched` toEqual([]) — the spy port records every authenticate/dispatch call, so an empty array is positive proof the refusal landed before envelope construction, authentication or dispatch. ARM 4 pins the roster both ways (create kinds present, goal.close/goal.cancel absent) so arm 3 stays meaningful.\n\nDEPENDENCY EDGE — DECLARED, NOT PLANNED AROUND. The arm needs the MCP SDK Client/InMemoryTransport/McpError; apps/daemon did NOT depend on @modelcontextprotocol/sdk (packages/mcp did, at 1.30.0). First run failed loudly: \"Cannot find package '@modelcontextprotocol/sdk/client/index.js' imported from ...\". Per the dependency-edge rail I landed the edge rather than working around it: added `\"@modelcontextprotocol/sdk\": \"1.30.0\"` to apps/daemon/package.json dependencies (same pinned version as packages/mcp — no new version enters the tree), ran `pnpm install --filter @moe/daemon` (Done in 1.8s, 0 downloaded, 0 added). All three legs verified: (a) manifest declares it; (b) pnpm-lock.yaml importer entry present at `apps/daemon:` lines 136-138 — `git diff --stat` shows the lockfile change is exactly 3 insertions, purely additive; (c) the bare specifier resolves and TYPECHECKS — `pnpm --filter @moe/daemon exec tsc --project tsconfig.json --noEmit` -> TSC EXIT: 0 with the test file importing three bare SDK specifiers. Two owned paths added to the diff.\n\n=== MUTATION DRILLS: ALL THREE ARMS, BACKUP-BEFORE-MUTATE, RESTORE PROVEN SEPARATELY ===\nAll three targets were TRACKED and CLEAN before mutation (`git status --porcelain` empty on each). Backups at %TEMP%/moe-task-14a2f0e4-<file>-worker-6433dce1.ts — TASK ID in the path, per the drill rail.\n\nVINTAGE PROVEN, not assumed (the closed-loop hazard): the allowlist backup's SHA256 was checked against `git show HEAD:apps/daemon/src/mcp-tool-allowlist.ts | sha256sum` = 4156aa1faef3b427d6ac6e963d58c508d38bdb6dabbae11b92231e720c0eb8dd — identical to the live file and the backup, so the backup is this row's vintage, not a stale artefact.\n\nDRILL 1 (arm 3, the fence). Mutant: re-admit goal.close by changing MCP_REACHABLE_OPERATOR_KINDS to new Set([\"session.open\",\"goal.close\"]). Mutated SHA256 fcb4de03bbfd87d2ce44c950c7a081b5e7fe60e31dcc64bbb3f539022136c2a8. RESULT — RED, on exactly the right assertion:\n  × refuses an operator-only kind AT THE TRANSPORT, before the port is ever asked\n  AssertionError: expected { code: 'INPUT_INVALID', …(9) } to match object { code: 'CAPABILITY_DENIED' }\nTHIS IS THE RAIL'S POINT MADE VISIBLE. With the roster fence removed the call still got REFUSED — a LATER layer answered with INPUT_INVALID. An arm that asserted only \"refused\"/\"threw\" would have stayed GREEN with the fence gone. Mine went red because it pins the CODE. Arm 4 also reds (exclusion pair, expected); arms 1-2 stayed green, so no coupling.\n\nDRILL 2 (arms 1+2). Two simultaneous production mutants: daemon-command-payload-keys.ts:149 \"goal.create_with_source\" narrowed to [\"instructions\",\"title\"] (drops the source key), and mcp-dispatch-port.ts:306 \"documents.source_read\" handler replaced by a zero-byte stub. RESULT — both RED, arms 3+4 stayed GRE",
      "startedAt": "2026-09-17T21:07:17.406Z",
      "completedAt": "2026-09-17T21:13:12.221Z"
    },
    {
      "stepId": "step-7",
      "description": "Document the exposed surface and the connect recipe (DoD item 4). INSTALL.md is generated by tools/packaging/pack-docs.ts:14 installDoc(), which is the only source -- dist/moe-windows/INSTALL.md is output, never edit it. Add a section titled \"Headless access (no browser)\" that states, in the same plain register as the surrounding text: (1) `moe mcp <dir>` serves the project to an MCP client over stdio, with no pairing and no CSRF token, because the MCP transport authenticates by credential rather than by the browser's Host+Origin+CSRF wall; (2) the client config to paste, in the shape agent-spawner.ts already uses for seats but over stdio: {\"mcpServers\":{\"moe-next\":{\"command\":\"moe\",\"args\":[\"mcp\",\"<project path>\"]}}} -- note explicitly that NO credential belongs in this file, because `moe mcp` reads moe.config.json itself; (3) what the session can do: create goals with goal.create and goal.create_with_source, and read state through the query kinds work.get_context, graph.get, graph.preview, product_contract.read, events.read, documents.source_read and design.read; (4) what it CANNOT do and why: operator-only kinds -- approving a plan, closing or cancelling a goal, choosing a provider, rolling back a deployment -- are refused at the transport with CAPABILITY_DENIED because they are human acts, and an MCP caller holding the operator credential would otherwise be indistinguishable from the operator in the browser. (5) One warning line: stdout on this wire carries JSON-RPC, so diagnostics go to stderr. In tools/packaging/pack-docs.test.ts add an arm asserting installDoc() output contains \"moe mcp\" and \"goal.create_with_source\" -- the packaged doc is the only thing an operator reads after unzipping, and an unpinned section silently disappears on the next edit. Do NOT add any kind to MCP_SERVED_QUERY_KINDS or the command roster: mcp-tool-allowlist.test.ts:278 pins {excluded: 29, queries: 7, vocabulary: 65, wired: 43} and this task changes none of those numbers.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tools/packaging/pack-docs.ts",
        "tools/packaging/pack-docs.test.ts"
      ],
      "modifiedFiles": [
        "tools/packaging/pack-docs.ts",
        "tools/packaging/pack-docs.test.ts"
      ],
      "note": "DOCUMENTED AND PINNED (DoD item 4). Section \"## Headless access (no browser)\" added to installDoc() in tools/packaging/pack-docs.ts — THE GENERATOR, the only source. dist/moe-windows/INSTALL.md was NOT touched; it is output.\n\nRENDERED AND READ, not just written. I ran installDoc() through an out-of-tree probe (%TEMP%/moe-task-14a2f0e4-render-worker-6433dce1.mjs, deleted after — never inside the repo, so it cannot enter a commit) and read the actual markdown. All five required points are there, in the surrounding plain register:\n(1) `moe mcp demo` serves the project over stdio, \"no pairing step and no CSRF token\", with the REASON: the browser route is walled by Host/Origin/a token minted when you pair, which a script cannot obtain; MCP authenticates by credential instead.\n(2) The paste-able recipe, rendered exactly as:\n      {\"mcpServers\":{\"moe-next\":{\"command\":\"moe\",\"args\":[\"mcp\",\"C:\\\\work\\\\demo\"]}}}\n    — valid JSON, the Windows path correctly double-escaped (I verified the RENDERED bytes, not the TypeScript source escapes) — plus the explicit sentence \"NO credential belongs in the client config below\", with the reason (`moe mcp` reads moe.config.json itself).\n(3) What it CAN do: goal.create and goal.create_with_source, and all SEVEN query kinds named individually: work.get_context, graph.get, graph.preview, product_contract.read, events.read, documents.source_read, design.read.\n(4) What it CANNOT do AND WHY: approve a plan, close or cancel a goal, choose a provider, roll back a deployment — \"refused at the transport with `CAPABILITY_DENIED` before anything is dispatched, because they are human acts: this wire authenticates with your operator credential, so an MCP caller that reached them would be indistinguishable from you in the browser.\"\n(5) The stdout warning: \"stdout on this wire carries JSON-RPC\" and everything moe mcp says goes to stderr.\n\nROSTER UNCHANGED, as the step required: no kind added to MCP_SERVED_QUERY_KINDS or any command roster. mcp-tool-allowlist.test.ts:278's pinned {excluded:29, queries:7, vocabulary:65, wired:43} is untouched and green (verified in step 6's 60-test run).\n\nPINNING ARM in tools/packaging/pack-docs.test.ts — pinned by CAPABILITY, not prose: \"moe mcp\" present; `goal.create_with_source` present as a BACKTICKED literal (through the file's existing `quoted()` helper, so a claim cannot hide behind rewording); the exact JSON prefix of the connect recipe; the no-credential sentence; ALL SEVEN query kinds asserted individually in a loop with the kind as the failure label; `CAPABILITY_DENIED` as a quoted literal; and the stdout warning. I deliberately did NOT pin whole sentences — prose that must match verbatim rots on the first copy-edit while proving nothing.\n\nCHECKED FOR COLLISION FIRST: the file's existing `quoted(DOC)` arm constrains only literals starting with \"moe start:\" (pack-docs.test.ts:173), matching them against what the composer really prints. Nothing I added starts with that prefix, so the new backticked literals cannot red it. Confirmed by the run.\n\nMUTATION DRILL, backup-before-mutate, restore proven separately:\n  pre-mutation SHA256 c896f49fe77af136d3fe3f2412f81915f0ee07d400562712d5ec080a30f6b463 (live file == backup at %TEMP%/moe-task-14a2f0e4-pack-docs-worker-6433dce1.ts, task id in the path)\n  MUTANT: the whole \"## Headless access\" section spliced out (grep count 0 confirmed the deletion actually happened, not just that the command returned). Mutated SHA256 28fc1c5caa4ec3a49044c8f997f1b8d913dcd05946342cef374b4380e738b5de.\n  RESULT — RED: × documents the headless MCP path with the tools it actually serves\n             AssertionError: expected '# moe 9.9.9-test — Windows supervised…' to contain 'moe mcp'\n  RESTORED, proven in a SEPARATE call: SHA256 c896f49fe77af136d3fe3f2412f81915f0ee07d400562712d5ec080a30f6b463 — equal to pre-mutation. Section grep count back to 1.\n  GREEN after restore: `pnpm vitest run tools/packaging/pack-docs.test.ts` -> Test Files 1 passed (1), Tests 5 passed | 2 skipped (7). (The 2 skips are the pre-existing pwsh-gated moe.ps1 arms, `describe.skipIf(locate(\"pwsh\"))` — not mine and not new.)\n\n=== SHARED-CHECKOUT EVENT, DISCLOSED ===\nMid-step, `git status --porcelain` on my own edited files started returning EMPTY. Cause: a PEER SESSION landed commit 1cb6a82a (subject literally \"main\") which swept my in-flight work onto the branch — all 15 of my paths PLUS two files that are NOT mine: CLAUDE.md (+104) and apps/daemon/src/orchestrator/moe-up-main.test.ts (+40).\nHandled per the rails: I did not revert, stash, reset or touch the foreign paths, and I did not treat it as a stop condition. Two consequences I worked around rather than fought:\n  - `git diff`/`git status` are no longer valid restore proofs for my paths this session (empty means \"matches that commit\", not \"unmodified by me\"). Every drill in this step and step 6 already used out-of-tree SHA256 backups, which are unaffected — this is exactly the failure mode that rule exists for.\n  ",
      "startedAt": "2026-09-17T21:13:15.442Z",
      "completedAt": "2026-09-17T21:18:36.768Z"
    },
    {
      "stepId": "step-8",
      "description": "LIVE PROOF, GATE, AND ADVERSARIAL REVIEW. (A) LIVE (DoD item 1): run `moe mcp D:/projexts/UnAI` against the live UnAI project measured in step 1 and confirm the stdio server comes up with NO browser and NO pairing. Drive it as a real MCP client would -- pipe a JSON-RPC `initialize` followed by `tools/list` into its stdin and confirm the response lists goal.create_with_source; quote the request you sent and the decisive response line. Confirm stdout carried ONLY JSON-RPC and that the banner appeared on stderr. If D:/projexts/UnAI/moe.config.json turned out to be absent in step 1, the NAMED SUBSTITUTE is: `moe init` a fresh directory under your scratchpad, run `moe mcp` against it, and run the identical initialize+tools/list exchange -- disclose in the completion record which of the two you ran. Do not narrow this arm to a unit test: DoD item 1 is a live launch claim and a test double cannot discharge it. (B) GATE, ONE COMMAND, both count lines quoted: `pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli src/mcp-free-session-roundtrip.test.ts src/mcp-tool-allowlist.test.ts && pnpm test tools/packaging/pack-docs.test.ts`. Read the OUTPUT, not the exit code: a leg with no `Test Files N passed` count line RAN NOTHING and is not a pass (a bad --filter prints \"No projects matched the filters\" and exits 0). Then run the apps/** regression explicitly, because the root vitest include has no apps/**: `pnpm --filter @moe/daemon test`, and quote its count line too. Attribute any red by the path-attributed baseline rule -- record repo-wide failures at your merge-base and at HEAD, and only failures in the delta intersected with this task's owned paths block completion; report foreign red verbatim rather than hiding or fabricating it. (C) ADVERSARIAL SELF-REVIEW before complete_task, on this diff specifically: does anything this verb prints reach stdout? Does the credential appear in any log line, error message or stack trace? What happens on two concurrent `moe mcp` runs against one project -- does the store lock refuse by name, or corrupt? What happens when moe.config.json exists but storePath points at a file that does not -- is the refusal coded or a raw throw? Does the new bidirectional argv arm actually go red if you delete the `mcp` parse branch while leaving it in KNOWN_COMMANDS (try it, restore, prove the restore)? Does the CAPABILITY_DENIED arm still red if the fence is weakened? Record each answer in the completion record.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "apps/daemon/src/cli/moe-cli-project.ts",
        "apps/daemon/src/cli/moe-cli-project.js",
        "apps/daemon/src/cli/moe-cli-mcp.ts",
        "apps/daemon/src/cli/moe-cli-mcp.test.ts",
        "apps/daemon/src/cli/moe-cli-main.ts"
      ],
      "note": "(A) LIVE PROOF — RAN AGAINST THE REAL UnAI PROJECT, and it FOUND A REAL BUG a test double could never have caught.\n\nFIRST ATTEMPT FAILED, and that is the headline of this step. `node apps/daemon/src/cli/moe-cli-main.ts mcp D:/projexts/UnAI` printed NOTHING on either stream and exited 13:\n  Warning: Detected unsettled top-level await at file:///D:/projexts/moe-next/apps/daemon/src/cli/moe-cli-main.ts:322\n  EXIT=13\nReproduced on the CONFIG-ABSENT path too, which located it before `runMcp` ran at all. ROOT CAUSE: a MODULE CYCLE. `moe-cli-mcp.ts` imported `readConfig`/`preparePackagedLinks` as VALUES from `./moe-cli-main.js`. The entry is loaded as `moe-cli-main.ts`, so the `.js` bridge is a DIFFERENT specifier and Node instantiates a SECOND module record of the entry — whose `isMainModule(import.meta, process.argv[1])` is STILL TRUE, because import.meta.url and argv[1] both resolve to moe-cli-main.ts. That copy re-enters the top-level `await runMoeCli(...)`, re-parses `mcp`, and dynamically imports moe-cli-mcp.js which is already in flight. Deadlock on the module graph; Node drains the loop and exits 13.\nWHY EVERY TEST MISSED IT: vitest resolves `./moe-cli-main.js` and `moe-cli-main.ts` to the SAME module, and `isMainModule` is false under the runner. This is exactly the class the plan's DoD item 1 refused to let a unit test discharge.\n\nFIX (production, not the test): new module apps/daemon/src/cli/moe-cli-project.ts + bridge, holding `readConfig`, `preparePackagedLinks`, `readLinkManifest` and `MOE_CLI_CONFIG_ABSENT`, taking a NARROW `CliProjectIo {artifactRoot, log}` instead of the full `CliIo`. moe-cli-main.ts imports them from there and re-exports MOE_CLI_CONFIG_ABSENT for its existing consumers; moe-cli-mcp.ts imports the values from there and `CliIo` as `import type` ONLY — erased, so it creates no runtime edge. Still ONE implementation of each reader; the cycle is gone. The reason is written into both modules' comments so the next editor cannot re-create it.\n\nLIVE PROOF AFTER THE FIX, driven as a real MCP client (out-of-tree probe, deleted; it piped JSON-RPC `initialize` then `notifications/initialized` + `tools/list` into the child's stdin):\n  === STDOUT line count: 2\n  INITIALIZE RESULT serverInfo: {\"name\":\"moe-next\",\"version\":\"0.0.0\"}\n  INITIALIZE protocolVersion: 2024-11-05\n  TOOLS/LIST count: 43\n  has goal_create_with_source: true\n  has goal_create: true\n  has documents_source_read: true\n  has goal_close (must be false): false\n  === STDERR:\n  moe mcp: project unai-9705356d4240 -> D:\\projexts\\UnAI\n  moe mcp: serving stdio JSON-RPC on stdout; diagnostics on stderr; no browser, no pairing\n  {\"schemaVersion\":\"moe-foundation-receipt/1\",\"kind\":\"READY\",\"entry\":\"MCP_STDIO\",\"pid\":40092,\"projectId\":\"unai-9705356d4240\",\"storePath\":\"D:\\\\projexts\\\\UnAI\\\\store.sqlite\",\"instant\":\"2026-09-17T21:20:44.473Z\"}\nThis is the LIVE UnAI project (`unai-9705356d4240`, the exact id the DoD names) — NOT the named substitute, which was not needed. No browser, no pairing. STDOUT carried exactly 2 lines and BOTH parsed as JSON-RPC (the probe counts non-JSON-RPC lines; zero). The banner, the provenance line and the READY receipt all landed on stderr. 43 tools = the wiredMcpToolKinds() total measured in step 1.\n\n(B) GATE — run FRESH at current HEAD, OUTPUT read rather than exit code, every count line nonzero:\n  pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli src/mcp-free-session-roundtrip.test.ts src/mcp-tool-allowlist.test.ts\n    Test Files  12 passed (12) / Tests  166 passed (166)   LEG1 EXIT=0\n  pnpm test tools/packaging/pack-docs.test.ts\n    Test Files  1 passed (1) / Tests  5 passed | 2 skipped (7)   LEG2 EXIT=0\n  (Exit codes captured via ${PIPESTATUS[0]}, not the pipe tail. Both legs printed a `Test Files N passed` line with a nonzero test count, so neither ran vacuously.)\n  APPS/** REGRESSION, run explicitly because the root vitest include has no apps/**:\n  pnpm --filter @moe/daemon test\n    Test Files  744 passed (744) / Tests  12299 passed | 21 skipped (12320)   DAEMON EXIT=0\n  tsc: pnpm --filter @moe/daemon exec tsc --project tsconfig.json --noEmit -> TSC EXIT: 0.\n\nA SECOND REAL RED, MINE, FOUND AND FIXED BY THE apps/** LEG. The first full-suite run came back 1 failed / 739 passed:\n  FAIL src/runtime-entrypoint.test.ts > has an exact .js bridge for every runtime module and none for test-tier ones\n  + \"unexpected\": [ \"cli\\\\moe-cli-mcp.js\" ]\nThe bridge census classifies runtime tier as a forward closure from the package entry plus every module carrying its OWN `<name>.test.ts` sibling, and its import scan matches only `from \"….js\"` — it cannot follow a DYNAMIC import. `moe-cli-mcp.ts` is reached only dynamically and had no sibling test, so its bridge read as scaffolding. Every other dynamically-imported cli module (moe-cli-replan-recovery, moe-cli-review-recovery) earns its bridge the same way. FIX: added apps/daemon/src/cli/moe-cli-mcp.test.ts, 6 arms testing `runMcp` DIRECTLY — the two re",
      "startedAt": "2026-09-17T21:18:41.309Z",
      "completedAt": "2026-09-17T21:44:45.217Z"
    }
  ],
  "planRevision": 18,
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "HUMAN",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 1,
  "comments": [
    {
      "id": "comment-a7a0a49aa9154301bd3c24c009e9838e",
      "author": "worker-6433dce1",
      "content": "EVIDENCE DETAIL 1/2 — DoD items and mutation drills (overflow from completionSummary's 2000-char cap).\n\n=== DoD ITEM-BY-ITEM ===\n1. LAUNCHER, VERIFIED LIVE AGAINST UnAI. `moe mcp D:/projexts/UnAI`, no browser, no pairing. Driven as a real MCP client (JSON-RPC initialize -> notifications/initialized -> tools/list piped into the child's stdin). Decisive output:\n     === STDOUT line count: 2\n     INITIALIZE RESULT serverInfo: {\"name\":\"moe-next\",\"version\":\"0.0.0\"}\n     INITIALIZE protocolVersion: 2024-11-05\n     TOOLS/LIST count: 43\n     has goal_create_with_source: true\n     has goal_create: true\n     has documents_source_read: true\n     has goal_close (must be false): false\n     === STDERR:\n     moe mcp: project unai-9705356d4240 -> D:\\projexts\\UnAI\n     moe mcp: serving stdio JSON-RPC on stdout; diagnostics on stderr; no browser, no pairing\n     {\"schemaVersion\":\"moe-foundation-receipt/1\",\"kind\":\"READY\",\"entry\":\"MCP_STDIO\",\"pid\":40092,\"projectId\":\"unai-9705356d4240\",\"storePath\":\"D:\\\\projexts\\\\UnAI\\\\store.sqlite\",\"instant\":\"2026-09-17T21:20:44.473Z\"}\n   The REAL project (unai-9705356d4240), NOT the named substitute. Both stdout lines parsed as JSON-RPC; the probe counted non-JSON-RPC lines and got zero.\n2. CREATE OVER MCP + REFUSAL CODE. apps/daemon/src/mcp-free-session-roundtrip.test.ts arm 1 dispatches goal.create_with_source over the real port against a real file store, asserts {decision:{disposition:\"DECIDED\",resultCode:\"EFFECTS_COMMITTED\"}, ok:true, outcome:\"ACCEPTED\"}, then REOPENS the store and asserts GoalCreated is in readEvents(goalRef). Arm 3 drives the production stdio surface (createStdioMcpServer + wiredMcpToolKinds(), as mcp-main.ts composes it) with a real SDK Client, calls goal_close, asserts McpError.data.code === \"CAPABILITY_DENIED\" AND that a SPY dispatch port recorded ZERO calls — that empty array is the LAYER proof, not a comment.\n3. READ-BACK. Arm 2 dispatches documents.source_read with the new goalRef and asserts displayPath, mediaType, text and contentSha256 equal what arm 1 supplied.\n4. DOCS. INSTALL.md \"Headless access (no browser)\" via installDoc() (the generator; dist/ is output, never edited). Pinned in pack-docs.test.ts by CAPABILITY: \"moe mcp\", backticked goal.create_with_source, the JSON recipe prefix, the no-credential sentence, all SEVEN query kinds individually, CAPABILITY_DENIED, and the stdout warning. Roster untouched — mcp-tool-allowlist.test.ts:278's {excluded:29, queries:7, vocabulary:65, wired:43} unchanged and green.\n5. RE-MEASURED AT HEAD in step 1: wiredMcpToolKinds() TOTAL 43 / goal.create_with_source PRESENT / goal.close ABSENT (tsx probe, deleted in the SAME shell command, absence confirmed by ls); KNOWN_COMMANDS had no \"mcp\" (moe-cli-argv.ts:13); mcp-http LOOPBACK_HOSTS + MCP_HTTP_HOST_NON_LOOPBACK_BIND at mcp-http-host.ts:88,196; D:/projexts/UnAI/moe.config.json present.\n\n=== MUTATION DRILLS: 4 DRILLS, EVERY RESTORE SHA256-PROVEN IN A CALL SEPARATE FROM THE RESTORE ===\nBackups at %TEMP%/moe-task-14a2f0e4-<file>-worker-6433dce1.ts (TASK ID in the path). All targets tracked+clean pre-mutation; the allowlist backup's VINTAGE was additionally proven against `git show HEAD:<path> | sha256sum`.\n\nD1 mcp-tool-allowlist.ts — re-admit goal.close to MCP_REACHABLE_OPERATOR_KINDS.\n   pre 4156aa1faef3b427d6ac6e963d58c508d38bdb6dabbae11b92231e720c0eb8dd -> mut fcb4de03bbfd87d2ce44c950c7a081b5e7fe60e31dcc64bbb3f539022136c2a8\n   RED: \"expected { code: 'INPUT_INVALID', …(9) } to match object { code: 'CAPABILITY_DENIED' }\"\n   *** THE KEY RESULT: with the roster fence gone the call was STILL REFUSED, by a LATER layer, with a DIFFERENT code. An arm asserting only \"refused\"/\"threw\" would have stayed GREEN. ***\n   restored -> 4156aa1f… (equal), git status empty.\n\nD2 daemon-command-payload-keys.ts (drop \"source\" from goal.create_with_source) + mcp-dispatch-port.ts (stub the documents.source_read handler), simultaneously.\n   pre 02543c375992ac22dba9c8314a02533f6c3022dc6b77da4ce434ee71cabcc12d / 88afe924ee108fb70446f019ddad87afff475bd11b9931c9cd8c53aabcdd9d2c\n   RED: arms 1 and 2 both fail; arms 3 and 4 STAY GREEN (no coupling).\n   restored -> both hashes equal, git status empty.\n\nD3 moe-cli-argv.ts — delete the `head === \"mcp\"` parse branch, LEAVE \"mcp\" in KNOWN_COMMANDS.\n   pre 8871dd8f415200674d9fe80fa1a4c3a4f84fa5d197e5cbc1a71f58ff304c4f49 -> mut 70eb4b450b9ed20e9a243f9811c10f8004a98e42dd74cab76e7c647921928f18\n   (mutation verified by grep: roster hits 2, parse-branch hits 0)\n   RED: \"× parses every command it advertises, not merely names it — AssertionError: mcp: expected false to be true\"\n   *** The PRE-EXISTING one-directional arm (\"lists every known command in the unknown-subcommand message\") STAYED GREEN. The roster defect the global rail describes, reproduced. ***\n   restored -> 8871dd8f… (equal), 22/22 green.\n\nD4 pack-docs.ts — splice out the whole \"## Headless access\" section (grep count 0 confirms the bytes moved).\n   pre c896f49fe77af136d3fe3f2412f81915f0ee07d400562712d5ec080a30f6b463 -> mut 28fc1c5caa4ec3a49044c8f997f1b8d913dcd05946342cef374b4380e738b5de\n   RED: \"expected '# moe 9.9.9-test — Windows supervised…' to contain 'moe mcp'\"\n   restored -> c896f49f… (equal).",
      "timestamp": "2026-09-17T21:46:50.218Z"
    },
    {
      "id": "comment-5bd22f31b19444ac9102c3d06ca40a8b",
      "author": "worker-6433dce1",
      "content": "EVIDENCE DETAIL 2/2 — the two bugs the live arm found, the gate, the foreign red, and the adversarial answers.\n\n=== TWO REAL BUGS FOUND AFTER THE UNIT SUITE WAS GREEN ===\nBUG 1, MODULE-CYCLE DEADLOCK (found by the LIVE arm; no test double could have).\n  `node apps/daemon/src/cli/moe-cli-main.ts mcp D:/projexts/UnAI` printed NOTHING on either stream and exited 13:\n    Warning: Detected unsettled top-level await at file:///D:/projexts/moe-next/apps/daemon/src/cli/moe-cli-main.ts:322\n    EXIT=13\n  Reproduced on the config-absent path too, which located it BEFORE runMcp ran.\n  CAUSE: moe-cli-mcp.ts imported readConfig/preparePackagedLinks as VALUES from \"./moe-cli-main.js\". The entry is loaded as moe-cli-main.ts, so the .js specifier is a SECOND module record whose isMainModule(import.meta, process.argv[1]) is STILL TRUE (both sides resolve to moe-cli-main.ts). That copy re-enters the top-level `await runMoeCli(...)`, re-parses `mcp`, and dynamically imports moe-cli-mcp.js which is already in flight. Deadlock; Node drains the loop and exits 13.\n  WHY EVERY TEST MISSED IT: vitest resolves ./moe-cli-main.js and moe-cli-main.ts to the SAME module and isMainModule is false under the runner. The suite was green with the CLI 100% broken.\n  FIX (production): new apps/daemon/src/cli/moe-cli-project.ts + bridge holding readConfig, preparePackagedLinks, readLinkManifest and MOE_CLI_CONFIG_ABSENT, taking a NARROW `CliProjectIo {artifactRoot, log}`. moe-cli-main.ts imports from there and re-exports MOE_CLI_CONFIG_ABSENT for existing consumers; moe-cli-mcp.ts imports the values from there and CliIo as `import type` ONLY (erased, no runtime edge). Still ONE implementation of each reader. The reason is written into both modules' comments so the next editor cannot re-create it.\n\nBUG 2, BRIDGE CENSUS (found by the apps/** leg; first full run was 1 failed / 739 passed).\n    FAIL src/runtime-entrypoint.test.ts > has an exact .js bridge for every runtime module and none for test-tier ones\n    +   \"unexpected\": [ \"cli\\\\moe-cli-mcp.js\" ]\n  CAUSE: runtimeTierModules seeds from the package entry plus every module with its OWN <name>.test.ts sibling, and expands via /from\\s+\"(\\.[^\"]*)\\.js\"/g — which cannot follow a DYNAMIC import. moe-cli-mcp.ts is reached only dynamically, so its bridge read as scaffolding. Every other dynamically-imported cli module (moe-cli-replan-recovery, moe-cli-review-recovery) earns its bridge the same way.\n  FIX: added apps/daemon/src/cli/moe-cli-mcp.test.ts — 6 arms testing runMcp DIRECTLY: the two refusal codes kept DISTINCT (the malformed arm asserts MOE_CONFIG_UNREADABLE and explicitly NOT MOE_CLI_CONFIG_ABSENT), the coded launch failure, relative-vs-absolute storePath resolution, both credential variables, and EVERY arm asserting `stdout toEqual([])`.\n  Deleting the bridge was never an option: the live proof depends on plain Node resolving it. Census green after: runtime-entrypoint + moe-cli-mcp = 11 passed.\n\n=== GATE, RUN FRESH AT THE DELIVERED HEAD ===\n  pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli src/mcp-free-session-roundtrip.test.ts src/mcp-tool-allowlist.test.ts\n    Test Files  12 passed (12) / Tests  166 passed (166)   LEG1 EXIT=0\n  pnpm test tools/packaging/pack-docs.test.ts\n    Test Files  1 passed (1) / Tests  5 passed | 2 skipped (7)   LEG2 EXIT=0   (the 2 skips are the pre-existing pwsh-gated moe.ps1 arms, describe.skipIf — not mine, not new)\n  pnpm --filter @moe/daemon test   [the apps/** leg, run explicitly because the root vitest include has no apps/**]\n    Test Files  744 passed (744) / Tests  12299 passed | 21 skipped (12320)   DAEMON EXIT=0\n  pnpm --filter @moe/daemon exec tsc --project tsconfig.json --noEmit -> TSC EXIT: 0\nExit codes captured with ${PIPESTATUS[0]}, never the pipe tail. Every leg printed a `Test Files N passed` line with a NONZERO test count, so no leg ran vacuously.\n\n=== FOREIGN RED, DISCLOSED VERBATIM AND ATTRIBUTED (root lane: 2 failed | 464 passed of 467 files; 3 failed | 10695 passed) ===\n  FAIL tests/instruction-contract/checker.test.ts > root instruction contract > accepts the real root CLAUDE.md\n    AssertionError: expected [ { code: 'FILE_OVERSIZED', …(2) } ] to deeply equal []\n  FAIL tests/integration/release/release-version-surfaces.test.ts > discovers the exact nonempty current-version occurrence set in both directions\n    AssertionError: unclassified tracked current-version occurrence: expected [ …(4) ] to deeply equal []\n    + [ \"apps/control-room/CLAUDE.md8046v0.1\", \"tests/integration/CLAUDE.md9293v0.1\", \"tools/packaging/CLAUDE.md6705v0.1\", \"tools/packaging/CLAUDE.md6790v0.1\" ]\n  FAIL tests/integration/release/release-version-surfaces.test.ts > goes red for an unclassified tracked current-version occurrence\nPATH ATTRIBUTION — failing paths and their owning commits:\n  CLAUDE.md                   -> 1cb6a82a \"main\"                              (peer)\n  README.md                   -> 0c215e82 \"feat: bring main up to the fleet…\" (peer)\n  apps/control-room/CLAUDE.md -> b51935a7 \"docs: a CLAUDE.md per workspace folder\" (peer)\n  tests/integration/CLAUDE.md -> b51935a7 (peer)\n  tools/packaging/CLAUDE.md   -> b51935a7 (peer)\nNOT ONE of those five is in my modifiedFiles; my diff adds no CLAUDE.md and no version literal. Note tools/packaging/CLAUDE.md is a DIFFERENT file from my tools/packaging/pack-docs.ts. The (HEAD-minus-baseline) failing-path set intersected with this task's owned paths is EMPTY.\n\n=== ADVERSARIAL SELF-REVIEW (measured, not reasoned) ===\n1. Stdout leakage? NO — three independent proofs: the live run's stdout was 2 lines, both JSON-RPC; 10 test arms assert `stdout toEqual([])`; and runMcp redirects the SHARED helpers' sink via `{...io, log: io.diagnostic}` rather than threading a parameter, so a line added to readConfig or preparePackagedLinks LATER still cannot leak.\n2. Credential in any line/error/stack? NO — probed with a real 64-char credential: \"credential in stderr: false\", \"credential in stdout: false\"; asserted in two arms. Provenance names projectId and path only.\n3. Two concurrent `moe mcp` on one project? MEASURED, BOTH CAME UP: A exit=null stdoutLines=1 nonJsonRpcLines=0; B exit=null stdoutLines=1 nonJsonRpcLines=0; distinct pids 43164/43464, both READY, no corruption. HONEST FINDING: `moe mcp` takes NO per-store lock (unlike `moe start`). I judged that CORRECT and did not add one — a lock would forbid the primary use case (an agent attaching to a project `moe start` is already running), and the durable spine's real fence is optimistic concurrency at expectedVersion. Recorded so nobody later assumes a lock exists.\n4. storePath points at a nonexistent parent? CODED, not a raw throw: exit 1, STDOUT ZERO BYTES, stderr `MOE_CLI_MCP_UNAVAILABLE: STORE_INPUT_INVALID: database path parent must exist and be canonically resolvable`. Pinned by an arm.\n5. Bidirectional argv arm goes red on branch deletion? YES — drill D3 above.\n6. CAPABILITY_DENIED arm still red if the fence weakens? YES — drill D1 above, and D1 also proved the arm is not one-layer-vacuous.\n7. Error-path side effects: runMcp sets process.env BEFORE launching, so a failed launch leaves 4 vars set in a process that is exiting anyway; tests delete them in afterEach so no arm inherits another's credential. runMcp opens no handles itself — the stdio host owns server+store and drains on stdin EOF/SIGINT/SIGTERM.\n\n=== HOUSEKEEPING / FOLLOW-UPS (recorded, not filed as rows — DELIVERY BIAS rail) ===\n- apps/daemon/src/cli/moe-cli-main.ts is 368->~350 lines: under the 400 hard bar, over the 250 target. My net add there is small and the plan deliberately kept the runner out of it. A split is a separate refactor for the epic-final hardening row.\n- NEW DEPENDENCY EDGE, all three legs verified: apps/daemon/package.json declares @modelcontextprotocol/sdk@1.30.0 (same pinned version packages/mcp already used — no new version enters the tree); pnpm-lock.yaml carries the `apps/daemon:` importer entry (git diff --stat: exactly 3 insertions, purely additive); and the bare specifier typechecks (tsc exit 0 with three bare SDK imports in the new test).\n- SHARED CHECKOUT: HEAD moved 4x during this task (1cb6a82a, b51935a7, 5e16c739, 443ba129). 1cb6a82a swept my in-flight files onto the branch together with two files that are NOT mine (CLAUDE.md +104, apps/daemon/src/orchestrator/moe-up-main.test.ts +40). I reverted/stashed/committed nothing of theirs and treated none of it as a stop condition. After that sweep `git status`/`git diff` stopped being valid restore proofs for my paths — every drill already used out-of-tree SHA256 backups, which is exactly the failure mode that rule anticipates.\n- No probe, scratch or evidence file is in the delivered tree: the tsx roster probe was deleted in its own shell command (absence confirmed by ls), and the live/adversarial/render probes all lived OUTSIDE the repo under %TEMP% and were deleted.",
      "timestamp": "2026-09-17T21:47:30.261Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-17T20:05:26.229Z",
  "updatedAt": "2026-09-17T21:48:06.149Z",
  "metrics": {
    "firstClaimAt": "2026-09-17T20:39:59.929Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 9,
    "executedStepCount": 8
  },
  "contextFetchedBy": [
    "architect-73d103f8",
    "worker-6433dce1"
  ],
  "planSizeWarnings": [
    "Plan touches 9 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-17T20:48:45.212Z",
  "planningNotes": {
    "codebaseInsights": "RE-MEASURED AT HEAD 2026-09-17 (rails: description claims are stale by default). Commands and results:\n\n(1) `goal.create_with_source` IS MCP-WIRED. Compiled probe (tsx, inside apps/daemon/src, deleted in the same command) importing `./mcp-tool-allowlist.js` printed: TOTAL 43; goal.create PRESENT; goal.create_with_source PRESENT; goal.close ABSENT; goal.cancel ABSENT; session.open PRESENT; work.get_context/graph.get/documents.source_read/events.read/design.read PRESENT; EXCLUDED_N 29; QUERY [work.get_context, graph.get, graph.preview, product_contract.read, events.read, documents.source_read, design.read]. `MCP_EXCLUDED_COMMAND_KINDS` = all OPERATOR_PRINCIPAL_KINDS minus `session.open`; goal.create* are not operator-only, they sit in BOOTSTRAP_FAMILY (daemon-command-vocabulary.ts:24) with CAPABILITIES.GOAL.\n\n(2) MCP-HTTP AUTH = BEARER / LOOPBACK / NO CSRF. `packages/mcp/src/http/http-session.ts:28,60,160-210` reads exactly one `Bearer <token>` from the `authorization` header and calls `port.validateBearer`; `apps/daemon/src/mcp-http/mcp-http-session-port.ts:61-103` implements it. `mcp-http-host.ts:88` LOOPBACK_HOSTS = {127.0.0.1, ::1, [::1], localhost}, `:196` refuses `MCP_HTTP_HOST_NON_LOOPBACK_BIND`. Grep for csrf/Origin/Host-check in mcp-http-host.ts: ZERO hits. Confirms the free-session path is NOT behind http-listener-guards.ts checkHeaders.\n\n(3) STDIO ENTRY CONTRACT. `mcp-main.ts` = `readBootstrapCredential()` (packages/mcp/src/stdio/stdio-server.ts:48, reads MOE_SESSION_CREDENTIAL, throws naming the variable when absent/empty) + `readStoreDependencyEnv(process.env)` requiring MOE_STORE_PATH, MOE_PROJECT_ID, MOE_DAEMON_CREDENTIAL, else throws `STORE_DEPENDENCIES_ENV_MISSING: <names>` (daemon-store-dependencies.ts:22-32). It passes `fallbackCredential: credential` so an MCP caller authenticates AS the operator -- which is precisely why operator-only kinds are excluded from the roster.\n\n(4) THE GAP IS REAL, AND SMALLER THAN THE DESCRIPTION SAYS. `apps/daemon/package.json` ALREADY declares bins `moe-mcp-stdio` -> ./src/mcp-main.ts and `moe-mcp-http` -> ./src/mcp-http/mcp-http-main.ts, and `dist/moe-windows/apps/daemon/src/mcp-main.ts` IS in the packaged artifact. But: `KNOWN_COMMANDS` (moe-cli-argv.ts:13) = [init, start, recover-review, recover-replan, projects, version, help] -- NO `mcp`. And the only MCP HTTP host actually composed at runtime is `agent-wrapper-main.ts:332`, i.e. ONE PER SEAT, inside the wrapper process, with a seat-scoped credential and a per-seat origin that dies with the seat. There is NO project-scoped, operator-launchable MCP endpoint. So a free session cannot attach to anything today.\n\n(5) THE EXISTING CLIENT RECIPE IS ALREADY IN CODE. `agent-spawner.ts:181-189` writes `{mcpServers:{\"moe-next\":{type:\"http\",url:trustedOrigin,headers:{Authorization:\"Bearer <credential>\"}}}}` for claude seats. The free-session wiring is the same idea with the stdio transport instead.\n\n(6) CONFIG CARRIES EXACTLY WHAT THE ENTRY NEEDS. `MoeConfig` (moe-init.ts:83-103) = {credential, projectId, schemaVersion, storePath}. One file read supplies all four env vars.\n\n(7) PACKAGED LINKS ARE LAZY. `moe-cli-main.ts:241 preparePackagedLinks` -> `ensureWorkspaceLinks` (moe-cli-links.ts:138) creates node_modules\\@moe\\* junctions AT COMMAND TIME. A raw `node apps/daemon/src/mcp-main.ts` recipe in a freshly extracted artifact cannot resolve `@moe/mcp`. THIS is the decisive reason the deliverable is a CLI verb and not a documentation-only .mcp.json recipe.\n\n(8) STDOUT IS POISONED BY THE EXISTING CLI SINK. `moe-cli-main.ts:307` wires `log: (line) => process.stdout.write(...)`. For a stdio MCP server stdout IS the JSON-RPC wire. `moe mcp` must route EVERY line (banner and refusal alike) to stderr. This is the single highest-risk detail in the task.\n\n(9) LIVE TARGET IS REACHABLE. `/d/projexts/UnAI/moe.config.json` exists on this host, so DoD item 1's live verification against the UnAI project is executable, not hypothetical.\n\n(10) SHAPE-CONSUMER ENUMERATION (rail: grep the SHAPE, not the symbol). `KNOWN_COMMANDS` has exactly two references repo-wide outside dist/: moe-cli-argv.ts:13,164 and moe-cli-argv.test.ts:4,96. The test at :96 ITERATES the roster, so adding `mcp` grows its own iteration and it stays green even with no parse branch -- the classic one-directional roster defect. The plan therefore mandates a SECOND, opposite-direction arm. No test asserts the USAGE block's full text (grep of 'moe start [dir]', 'moe projects' and 'USAGE' over the cli tests returned only unrelated hits at moe-cli-main.test.ts:341,382), so the help line is currently unpinned and the plan pins it.\n\n(11) NO SIBLING OWNS THIS. moe.list_tasks over epic-f02c6977: 39 rows, 38 DONE, this is the only open row. The two producers it builds on -- task-c9a9bf3c 'Official MCP stdio adapter' and task-159be643 'Compose production Streamable HTTP MCP host' -- are both DONE and their surfaces are measured present at HEAD above, so no dependsOn is require"
  },
  "planApprovedAt": "2026-09-17T20:48:47.416Z",
  "workStartedAt": "2026-09-17T21:00:27.828Z",
  "reviewStartedAt": "2026-09-17T21:48:06.147Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/cli src/mcp-free-session-roundtrip.test.ts src/mcp-tool-allowlist.test.ts",
    "exitCode": 0,
    "outputTail": " Test Files  12 passed (12)\n      Tests  166 passed (166)\n   Start at  00:47:34\n   Duration  7.16s (transform 11.81s, setup 0ms, import 16.04s, tests 4.03s, environment 1ms)\n\nGATE EXIT=0",
    "reportedAt": "2026-09-17T21:48:06.147Z",
    "source": "agent-reported"
  },
  "filesModified": [
    "apps/daemon/src/cli/moe-cli-argv.test.ts",
    "apps/daemon/src/cli/moe-cli-argv.ts",
    "apps/daemon/src/cli/moe-cli-mcp.ts",
    "apps/daemon/src/cli/moe-cli-mcp.js",
    "apps/daemon/src/cli/moe-cli-main.ts",
    "apps/daemon/src/cli/moe-cli-main.test.ts",
    "apps/daemon/src/cli/moe-cli-cwd.test.ts",
    "apps/daemon/src/cli/moe-cli-replan-recovery.test.ts",
    "apps/daemon/src/cli/moe-cli-recover-review.test.ts",
    "apps/daemon/src/projects/project-review-recovery-native.test.ts",
    "apps/daemon/src/mcp-free-session-roundtrip.test.ts",
    "apps/daemon/package.json",
    "pnpm-lock.yaml",
    "tools/packaging/pack-docs.ts",
    "tools/packaging/pack-docs.test.ts",
    "apps/daemon/src/cli/moe-cli-project.ts",
    "apps/daemon/src/cli/moe-cli-project.js",
    "apps/daemon/src/cli/moe-cli-mcp.test.ts"
  ],
  "completionSummary": "`moe mcp [dir]` ships: a headless session (agent, CLI, script) now drives a live moe-next project with no browser and no pairing. All 5 DoD items met. Full detail in the two row comments.\n\nVERIFIED LIVE against the REAL UnAI project (unai-9705356d4240), not the substitute: piped JSON-RPC initialize + tools/list into it and got serverInfo {\"name\":\"moe-next\"}, TOOLS/LIST count 43, goal_create_with_source true, goal_close FALSE. STDOUT carried exactly 2 lines and both parsed as JSON-RPC; banner + READY receipt on stderr; credential in neither stream.\n\nTHE LIVE ARM EARNED ITS KEEP — it found two real bugs after the unit suite was green:\n1. MODULE-CYCLE DEADLOCK. Importing values from \"./moe-cli-main.js\" made Node load a SECOND copy of the entry whose isMainModule was still true, re-running its top-level await: exit 13, zero output, on every path. Vitest resolves .js/.ts to one module and isMainModule is false there, so no in-process test could see it. Fixed in production by extracting moe-cli-project.ts (shared readConfig/preparePackagedLinks, narrow CliProjectIo) and leaving only an erased `import type` edge to the entry.\n2. BRIDGE CENSUS. runtime-entrypoint.test.ts flagged cli\\moe-cli-mcp.js \"unexpected\" — its closure follows static `from \"…\"` only, so a dynamically-imported module needs its own .test.ts sibling. Fixed by adding moe-cli-mcp.test.ts (6 direct arms).\n\nDESIGN: stdout is the JSON-RPC wire, so CliIo gained a REQUIRED `diagnostic` stderr sink and runMcp hands the SHARED helpers `{...io, log: io.diagnostic}` — a line added to either helper later still cannot leak. 10 arms assert the stdout sink is EMPTY, not merely that the call succeeded. No credential enters the client config: `moe mcp` reads moe.config.json itself.\n\nREFUSAL CODES ASSERTED AS MEASURED, NOT AS PREDICTED: absent config MOE_CLI_CONFIG_ABSENT (moe-cli-main.ts:27), malformed JSON MOE_CONFIG_UNREADABLE (moe-init.ts:30/:198), failed launch MOE_CLI_MCP_UNAVAILABLE, operator-only kind CAPABILITY_DE"
}