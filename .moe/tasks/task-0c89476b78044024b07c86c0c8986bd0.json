{
  "id": "task-0c89476b78044024b07c86c0c8986bd0",
  "epicId": "epic-bf111658f9694b558bdc5596bbf0f924",
  "title": "Disaster restore proof",
  "description": "Objective: prove complete backup generations, two-slot recovery anchors, fresh incarnation/key epochs, orphan inventory, and quiesced recovery after media loss. Deliverable: CORE-S14 restore fixtures and evidence. NOT in scope: ordinary restart or optimistic orphan adoption. Hard dependencies: full store/artifact/effect inventory implementations. Owned paths: tests/fault/disaster-restore/** and backup tooling paths. Verification: pnpm test:fault.",
  "definitionOfDone": [
    "Crash at every prepare/install/fsync/switch boundary exposes either old authority or one verified quiesced restore, never a mixture.",
    "Repeated same-generation restore uses fresh incarnation/key epochs and all stale grants fail.",
    "Missing inventory or artifact/key proof keeps recovery unavailable with UNKNOWN_TRUTH.",
    "CORE-S14 restore gate passes."
  ],
  "taskRails": [
    "Pinned benchmark: D:/projexts/moes/docs/plans/2026-08-05-moe-best-tool-benchmark-spec.md SHA-256 A62B90436CC0B911FB28526AF7B7E0F2D1370F6F93DB91C26077F6E2956A589C.",
    "Claims follow the benchmark permit-list; missing, partial, cross-basis, or unverifiable evidence is UNKNOWN, never PASS.",
    "No confirmatory corpus is generated or viewed until the implementation commit is frozen exactly as specified.",
    "GO_QUIESCE and GO_ACTIVATE are separate human decisions. Keep evidence exact, reproducible, and scoped to one source commit."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "BASELINE + CLOSE THE ONE MEASURED EXPORT GAP. (a) Record the merge-base baseline BEFORE writing bytes: `git merge-base HEAD origin/main` (call it BASE), then at HEAD run `pnpm test:fault` and save the full output. It is RED at baseline and that red is FOREIGN: tsc errors in tests/fault/cross-host/, tests/fault/linux/, tests/fault/macos/ (TS2307 'Cannot find module ./effect-schedule-driver.js', TS2322 WorkspaceProducer, TS7006 implicit any). Those belong to the in-flight cross-host slice, NOT to you. Do not repair, do not delete, do not touch those directories. You will disclose them verbatim at completion per the path-attributed-baseline rail. (b) The ONE real gap: `RECOVERY_ANCHOR_FAULT_POINTS` is declared at packages/store/src/recovery-anchor-contracts.ts:46 but is NOT re-exported from packages/store/src/index.ts — a compiled probe confirmed TS2305 for that symbol and ZERO errors for the other 25 symbols this task needs. Add it to the EXISTING value-export block that already pulls RECOVERY_ANCHOR_ALLOWED_OPERATIONS / RECOVERY_ANCHOR_CODEC_VERSION / RECOVERY_ANCHOR_LAYER / RECOVERY_ANCHOR_REASON_CODES / RECOVERY_ANCHOR_STATES from ./recovery-anchor-contracts.js (index.ts ~line 92-98), keeping alphabetical order, and add `RecoveryAnchorFaultPoint` to the sibling `export type {...}` block from the same module. This is required, not cosmetic: DoD 1 sweeps every boundary and the sweep must be set-compared against the PRODUCTION enum, not a list copied into the test. packages/store has NO index-surface.test.ts pinning export counts (verified: file absent), so no count fixture needs updating. Verify with `pnpm --filter @moe/store typecheck` and read the OUTPUT — note that leg is separately RED at baseline from task-69 residue in event-read-model-contract.test.ts (readEventsByTypeAfter); confirm your two added lines introduce no NEW error and disclose the residue.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/index.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/index.ts"
      ],
      "note": "RE-VERIFIED AT HEAD, NOT REDONE. The QA reject reset all eight steps to PENDING, but this step's bytes landed in commit d447a34 and QA re-ran and passed them. QA's instruction was explicit: fix the one rail violation, \"do not redo anything else\". So I confirmed the deliverable exists rather than rewriting it.\n\n(b) THE EXPORT GAP IS CLOSED, checked against committed bytes, not the working copy: `git show HEAD:packages/store/src/index.ts` has `RECOVERY_ANCHOR_FAULT_POINTS` at line 95 in the value block and `RecoveryAnchorFaultPoint` at line 103 in the sibling type block, both from ./recovery-anchor-contracts.js, alphabetical order kept. It is load-bearing exactly as planned: anchor-boundary-crash.fault.ts:21 imports it and :153 set-compares the swept boundaries against `new Set(RECOVERY_ANCHOR_FAULT_POINTS)`, so the sweep is policed by the production enum rather than a list copied into the test.\n\n`pnpm --filter @moe/store typecheck` EXIT 0, zero errors, re-run fresh just now.\n\n(a) BASELINE — the plan's recorded baseline is now STALE IN MY FAVOUR and I am not carrying it forward. It said `pnpm test:fault` is RED at baseline with foreign tsc errors in tests/fault/cross-host/, tests/fault/linux/, tests/fault/macos/ (TS2307 effect-schedule-driver.js, TS2322 WorkspaceProducer, TS7006 implicit any), and that `pnpm --filter @moe/store typecheck` is separately RED from task-69 residue. Both foreign reds have since been resolved by their owners: the cross-host slice landed (91fc622, 1c9e46c) and the task-69 residue was removed by 376523c. Measured at HEAD e8da489: `pnpm test:fault` EXIT 0 with Test Files 10 passed (10) / Tests 83 passed (83), and store typecheck EXIT 0.\n\nConsequence for the path-attributed rail: with the named gate fully green there are no failing paths at HEAD at all, so the HEAD-minus-BASE delta intersected with my owned paths is empty by construction and there is no foreign red left to disclose on this leg. BASE recorded as `git merge-base HEAD origin/main` = cf272f67c3e34fe1d8c4dd00675512c67f165c05.\n\nStanding caution kept: I did not touch tests/fault/cross-host, /linux or /macos.",
      "startedAt": "2026-08-16T01:16:30.546Z",
      "completedAt": "2026-08-16T01:16:45.030Z"
    },
    {
      "stepId": "step-2",
      "description": "SHARED HARNESS for the three fault files. Create tests/fault/disaster-restore/disaster-harness.ts. It builds fixtures ONLY — it must contain ZERO authority. Every verdict in later steps comes from a production function; if you find yourself recomputing a digest, deciding whether a slot is valid, or classifying a refusal inside this file, stop: that is authority reimplemented inside the test and is forbidden by the epic rails. Provide: (1) `disasterRoot(label)` returning a fresh per-case temp directory under os.tmpdir() with a registry, plus `cleanupDisasterRoots()` that rmSync's every root recursively; (2) `seedGeneration(...)` that produces a real backup generation via `createBackupGeneration` from ../../../packages/store/src/index.js and returns the manifest and container unchanged; (3) `anchorRequest({ anchorRoot, generation, incarnationRef, keyEpochRef, restoreCommandId, projectId, injectFault })` returning the plain request object consumed by `prepareRecoveryAnchor` / `installRecoveryAnchor` — note `injectFault` IS snapshotted through the public entry (packages/store/src/recovery-anchor-record.ts:111 keeps it, defaulting to null), so crash injection reaches production without any deep import; (4) a `CrashSignal` error class and `crashAt(point)` factory returning an `injectFault` callback that throws CrashSignal when its argument equals `point` and returns undefined otherwise. IMPORT RULES, both proven by a compiled probe at plan time and both already used by the existing lane: @moe/store surfaces via the relative barrel `../../../packages/store/src/index.js`; @moe/daemon surfaces via the BARE specifier `@moe/daemon` (root package.json declares `@moe/daemon: workspace:*`, lockfile-backed, and tests/fault/cross-host/production-surfaces.fault.ts already imports it that way). Never deep-import into apps/daemon/src/** — that path is unproven and TS6059-prone. Register cleanup so no SQLite handle outlives the file: an afterAll that closes every opened store then rmSync's the roots (a held handle kills the vitest worker outright).",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/fault/disaster-restore/disaster-harness.ts"
      ],
      "newFiles": [
        "tests/fault/disaster-restore/disaster-harness.ts"
      ],
      "modifiedFiles": [
        "tests/fault/disaster-restore/disaster-harness.ts"
      ],
      "note": "RE-VERIFIED AT HEAD, NOT REDONE (reopen reset the step; bytes are in d447a34 and QA passed them; QA said do not redo anything else).\n\nPresent at HEAD, 260 lines, under the 400 split threshold. All four required surfaces exist: `disasterRoot(label)` with a registry (:29) and `cleanupDisasterRoots()` doing recursive rmSync (:47); `seedGeneration(label)` (:111) returning the real generation; `anchorRequest(input)` (:241) returning the plain request `prepareRecoveryAnchor`/`installRecoveryAnchor` consume; and the `CrashSignal` class (:204) plus `crashAt(point)` factory (:219). Also carries `openDisasterStore` (:36), `recoveryCompleteEnvelope` (:165) and `unreachableAuthority()` (:195), the last being what proves a refusal happened WITHOUT the approval port being consulted.\n\nZERO AUTHORITY — checked rather than assumed, and worth stating precisely because a grep for `createHash` hits this file three times. Those are at :82 and :127 and they BUILD fixture inputs (an artifact digest and a source generation digest fed INTO production), which is fixture construction. No verdict is computed here: there is no function that recomputes a digest to compare against production, decides slot validity, or classifies a refusal. Every verdict in the three fault files comes from an imported production function. QA independently confirmed \"Harness carries zero authority.\"\n\nImport rules held: @moe/store via the relative barrel `../../../packages/store/src/index.js`, @moe/daemon via the BARE specifier, no deep import into apps/daemon/src/**.\n\nSQLite handle discipline held: each fault file registers `afterAll(cleanupDisasterRoots)` and stores are closed before the roots are removed — a retained handle kills the vitest worker outright on Windows. Evidenced by the suite completing cleanly across repeated runs this session (I ran the disaster-restore suite six times, including four under mutation, with no worker crash).",
      "startedAt": "2026-08-16T01:16:58.003Z",
      "completedAt": "2026-08-16T01:17:11.035Z"
    },
    {
      "stepId": "step-3",
      "description": "DoD 1 — CRASH AT EVERY BOUNDARY. Create tests/fault/disaster-restore/anchor-boundary-crash.fault.ts. Production enumerates EIGHT boundaries, all confirmed live call sites: PREPARE (packages/store/src/recovery-anchor.ts:83 and :95), then INACTIVE_INSTALL, TRANSACTION, FILE_FSYNC, DIRECTORY_PERSISTENCE, VERIFICATION, SWITCH, INSTALLED_MARKER (packages/store/src/recovery-anchor-install.ts:232-260). Write the eight names OUT BY HAND in this file as a frozen literal, then assert set-equality in BOTH directions against the imported `RECOVERY_ANCHOR_FAULT_POINTS` and assert the swept case count is exactly 8. Hand-written plus production-compared is deliberate: a table derived from the enum cannot police the enum, and a hand list alone silently under-sweeps when production grows a ninth point. For each of the 8: seed a generation, install a first anchor cleanly so an OLD authority exists, then attempt a second same-project install with `crashAt(point)` and catch CrashSignal. Then, using ONLY production readers, assert the post-crash world is one of exactly two admissible states and never a blend — (A) OLD AUTHORITY: `inspectRecoveryAnchor` reports the pre-crash record with its original currentSlot and original anchorDigest, and `verifySlot` over that still-current slot passes; or (B) ONE VERIFIED QUIESCED RESTORE: `inspectRecoveryAnchor` reports state INSTALLED, and `verifySlot` over the NEW currentSlot passes, and the record carries restoredLifecycle QUIESCED / restoredReadiness RECOVERY_REQUIRED / restoredAuthorityRevoked true. Assert the mixture is absent explicitly, not by omission: it is never true that currentSlot moved while `verifySlot` over it fails, and never true that state is INSTALLED while the slot is unverifiable. Assert the exact boundary-to-outcome mapping per point rather than an or-of-eight, so a point that silently changes side turns this file red. Also assert that after every crash the only permitted operations are `RECOVERY_ANCHOR_ALLOWED_OPERATIONS` (INSPECT, RESUME, DISCARD) — drive `resumeRecoveryAnchor` and require it either completes to a verified INSTALLED state or refuses with a pinned `RECOVERY_ANCHOR_REASON_CODES` member AND its `layer`, never merely 'not ok'. CROSS-PLATFORM: packages/store/src/recovery-anchor-fs.ts:18 sets DIRECTORY_FSYNC_SUPPORTED = process.platform !== 'win32'. The DIRECTORY_PERSISTENCE fault point still FIRES on Windows (the callback runs before the platform check), so the case is swept everywhere — but do NOT assert POSIX directory-durability side effects; assert only the two admissible states above, which hold on all three platforms.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/fault/disaster-restore/anchor-boundary-crash.fault.ts",
        "tests/fault/disaster-restore/disaster-harness.ts"
      ],
      "newFiles": [
        "tests/fault/disaster-restore/anchor-boundary-crash.fault.ts"
      ],
      "modifiedFiles": [
        "tests/fault/disaster-restore/anchor-boundary-crash.fault.ts"
      ],
      "note": "RE-VERIFIED AT HEAD, NOT REDONE (reopen reset the step; bytes in d447a34; QA re-ran and passed DoD 1).\n\nPresent at HEAD, 228 lines. The structural requirement this step exists for is the one I checked hardest, because it is the same defect class QA rejected in step 5 — a table that cannot police its own source:\n- `HAND_WRITTEN_FAULT_POINTS` is a frozen literal written out by hand in this file, NOT derived from the enum.\n- Set-equality is asserted in BOTH directions against the imported production `RECOVERY_ANCHOR_FAULT_POINTS` (:153-156): hand-minus-production toEqual [] and production-minus-hand toEqual []. A ninth production point reddens the second direction; a stale hand entry reddens the first.\n- The generated count is pinned outright, not implied: `HAND_WRITTEN_FAULT_POINTS.length` toBe 8, `Object.keys(EXPECTED)` toHaveLength 8, and `new Set(Object.keys(EXPECTED))` toEqual the production set. That is epic rail 6's \"a swept case must assert it was actually generated\" — a sweep producing zero cases would otherwise pass every assertion in the file.\n- `RECOVERY_BINDING_SLOTS` is pinned to [\"ACTIVE\", \"PENDING\"] so the slot vocabulary cannot drift underneath the two-admissible-worlds assertions.\n\nThe 8 cases run through `it.each(HAND_WRITTEN_FAULT_POINTS)`, so a point that changes side turns this file red at that named point rather than dissolving into an or-of-eight.\n\nExecuted count confirmed by run, not by reading: the three disaster-restore files report Test Files 3 passed (3), Tests 22 passed (22).\n\nCross-platform caution held: no POSIX directory-durability side effect is asserted, only the two admissible post-crash states, so the DIRECTORY_PERSISTENCE case remains meaningful on win32 where DIRECTORY_FSYNC_SUPPORTED is false. Verified on this host (win32) as part of the green run.",
      "startedAt": "2026-08-16T01:17:13.673Z",
      "completedAt": "2026-08-16T01:17:26.064Z"
    },
    {
      "stepId": "step-4",
      "description": "DoD 2 — FRESH EPOCHS ON REPEAT, STALE GRANTS FAIL. Create tests/fault/disaster-restore/restore-identity-epochs.fault.ts. Restore the SAME backup generation twice (identical generationDigest — assert that equality explicitly so the 'same generation' premise is pinned and cannot silently become two generations). Mint each restore identity through the production service: `createRecoveryIncarnationService` with `createNodeRecoveryCryptoPort`, both from the @moe/daemon bare specifier. Assert the second restore's incarnationRef differs from the first AND its keyEpochRef differs from the first — assert BOTH separately; they are one atomic fencing identity and a test that checks only the nonce passes while the key epoch is reused. Anchor each with `anchorIncarnation` and read back with `readAnchoredIncarnation`, asserting the second read returns the second incarnation and that its backupGenerationDigest still equals the shared generation digest (fresh identity, same generation — that is the whole point). STALE GRANTS: drive the first (now superseded) incarnationRef/keyEpochRef against the post-second-restore world through the exported production surfaces — `readSuccessionChain` for the chain verdict and `runRecoveryCompleteCommand` for the command verdict — and assert the SPECIFIC stable reason code plus WHICH LAYER answered, never merely that it failed. Candidate codes live in RECOVERY_SUCCESSION_ERROR_CODES / RECOVERY_COMPLETION_CODES / RECOVERY_INCARNATION_ERROR_CODES, all exported from @moe/daemon; pin the member the surface actually returns and pin its layer constant, because more than one layer can refuse here and 'refused' alone is one added layer away from vacuous. Also cover the mirror case — a stale key epoch paired with a CURRENT incarnation — so the fence is proven on both operands rather than on their conjunction. HARD CONSTRAINT: if the exported surface genuinely cannot express a stale grant, do NOT reimplement the fence in the test and do NOT narrow the DoD — call moe.report_blocked naming the missing symbol.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/fault/disaster-restore/restore-identity-epochs.fault.ts",
        "tests/fault/disaster-restore/disaster-harness.ts"
      ],
      "newFiles": [
        "tests/fault/disaster-restore/restore-identity-epochs.fault.ts"
      ],
      "modifiedFiles": [
        "tests/fault/disaster-restore/restore-identity-epochs.fault.ts"
      ],
      "note": "RE-VERIFIED AT HEAD, NOT REDONE (reopen reset the step; bytes in d447a34; QA re-ran and passed DoD 2, and ran its own drill on it).\n\nPresent at HEAD, 327 lines — under 400, the largest of the four owned files, worth noting for any future growth.\n\nThe five named cases cover both halves the step demands:\n- \"mints two fully distinct fencing identities for one generation\" (:129)\n- \"restores ONE generation twice under two fresh, fully distinct identities\" (:141)\n- \"reads the second incarnation back against the SAME generation digest\" (:174) — the same-generation premise pinned explicitly, so it cannot silently become two generations\n- \"grants nothing from the superseded chain and refuses a key epoch as a predecessor\" (:251)\n- \"withholds completion authority from a stale grant without consulting approval\" (:302)\n\nBOTH FENCE OPERANDS ARE INDEPENDENTLY LOAD-BEARING, and that is proven by drill rather than by reading. QA ran this one themselves and recorded it in the reject: deleting the `keyEpochRef` clause from `detectFenceReuse` (packages/store/src/recovery-anchor.ts:66) reddened restore-identity-epochs on EXACTLY \"refuses a spent key epoch paired with a fresh incarnation\" (1 failed | 8 passed) while the sibling incarnation arm stayed GREEN. That is the discriminating result — it rules out an equivalent mutant in a two-clause guard, where dropping either clause would otherwise redden both arms and prove neither is separately needed.\n\n\"without consulting approval\" (:302) is carried by the harness's `unreachableAuthority()`, whose `authorize` is typed `() => never` and throws — so the refusal is proven to happen BEFORE the approval port, not merely alongside it. A test that only asserted the refusal would stay green if a later layer started answering first.\n\nI did not re-run QA's drill: it is recorded in the reject with its exact failing test name and pass/fail split, the production file is unmodified at HEAD (I verified recovery-anchor.ts carries no working-tree change), and re-running it would mutate a foreign production file for a result already measured by an independent party.\n\nExecuted count confirmed by run: Test Files 3 passed (3), Tests 22 passed (22) across the disaster-restore lane.",
      "startedAt": "2026-08-16T01:17:28.102Z",
      "completedAt": "2026-08-16T01:17:42.151Z"
    },
    {
      "stepId": "step-5",
      "description": "DoD 3 — MISSING PROOF KEEPS RECOVERY UNAVAILABLE AS UNKNOWN_TRUTH. Create tests/fault/disaster-restore/recovery-unknown-truth.fault.ts. Cover three distinct absences, each its own case, and assert the code AND the layer for each — production pins the coordinator answer to UNKNOWN_TRUTH at layer RECOVERY_INVENTORY (packages/../apps/daemon/src/recovery/recovery-inventory-contract.ts:24 and :268), and the contract comment at durable-recovery-inventory-contract.ts:153 warns that several upstreams collapse to the same code, so the upstream tuple is what distinguishes them: assert it, not just the code. Case 1 — NO INVENTORY: `readRecoveryReconciliation` with no recorded reconciliation returns UNKNOWN_TRUTH at RECOVERY_INVENTORY. Case 2 — MISSING ARTIFACT PROOF: record a reconciliation whose item lacks its artifact proof, then `readRecoveryCompletionEvidence` returns UNKNOWN_TRUTH (see apps/daemon/src/recovery/recovery-completion-evidence.ts:368 for the production site) with the upstream naming the artifact gap. Case 3 — MISSING KEY PROOF: same shape with the key-epoch proof absent. In EVERY case then drive `runRecoveryCompleteCommand` and assert recovery stays UNAVAILABLE — a pinned RECOVERY_COMPLETION_CODES member plus its layer — so the test proves the absence actually WITHHOLDS authority rather than merely that a reader said UNKNOWN. Assert the negative too: no project lifecycle transition out of QUIESCED occurred and no recovery.complete event was appended, read back from the store. Do NOT let a shape-validation refusal stand in for a truth refusal: build each fixture so it is VALID at every earlier layer and only the target proof is absent, otherwise an earlier guard answers first and the assertion quietly detaches from its subject.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
        "tests/fault/disaster-restore/disaster-harness.ts"
      ],
      "newFiles": [
        "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts"
      ],
      "modifiedFiles": [
        "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts"
      ],
      "note": "QA REJECT FIXED (both issues), commit e8da489, one owned path, +56/-10. Cases 1-3 were NOT redone: QA verified their substance and said not to. Only the rail violation was repaired.\n\nISSUE 1 (:277-284, unkillable assertion). The closing test compared three restated literals; distinct literals are distinct under every production change, so no mutation could redden it. Now each case hands the upstream tuple PRODUCTION RETURNED to `observeUpstream(label, upstream)` and the closing test reads that map. `observeUpstream` is called BEFORE each per-case `toEqual` on purpose: under a collapse the map still holds three entries, so the failure lands on distinctness rather than being pre-empted by a per-case pin. Asserted: `observedUpstreams.size === 3` (the sweep actually swept — epic rail 6's zero-case clause), distinct `.code`, and distinct `code@layer` pairs.\n\nNO RESTATED SHAPE. The observed type is `RecoveryInventoryRefusal[\"upstream\"] | RecoveryCompletionUpstream`, both from the @moe/daemon root — a locally written `{ code, layer }` is the same defect class QA rejected. Case 2's `evidence.upstream` is nullable (`RecoveryCompletionUpstream | null`, caught by tsc), so `observeUpstream` REFUSES null rather than skipping it: production losing the tuple is the provenance collapse this file exists to catch and must not shrink the set into a vacuous pass.\n\nISSUE 2 (:6 header). Rewritten to name what actually enforces the property — distinctness over production-returned upstreams captured before the pins — and to state why literals would be unkillable.\n\nDRILL, with the counterfactual QA asked for. Mutated FOREIGN production `apps/daemon/src/recovery/recovery-inventory-ledger.ts:95`, `ledger(\"RECOVERY_BINDING_UNAVAILABLE\")` -> `ledger(\"RECORD_NOT_FOUND\")` (same frozen enum, so it compiles), collapsing case 3's upstream onto case 1's. Exactly-one-replacement guard asserted in the mutator.\n- NEW assertion: RED. 2 failed | 20 passed. Failure located at recovery-unknown-truth.fault.ts:326:69 — the distinctness assertion — with `expected 2 to be 3`. The size guard at :322 still PASSED, so it reddened for the right reason and not because a case stopped observing.\n- REJECTED literal-array version, identical mutation: GREEN. 1 failed | 21 passed — only case 3's own pin. That is the direct proof the old assertion was unkillable and the new one is load-bearing.\n- Restored: production SHA-256 back to 968a34e26a1fe192f7f57f128eda1033fca777d6fa25e35f9ab970737e99283c, test file back to 93498179c8871643e404b64343a0672e5ac7b5922a8a267c2975fc71f9ed48f0. The test file was restored BY COPY from an out-of-repo backup, never `git checkout` — it carried my uncommitted fix at the time.\n\nGATES, each run separately with its own exit code:\n- `npx vitest run --config tests/fault/vitest.config.ts disaster-restore` EXIT 0, Test Files 3 passed (3), Tests 22 passed (22).\n- Positive control, same shape with `foundation`: EXIT 0, 3 files / 43 tests — the filter is real, not silently empty.\n- `npx tsc -p tests/fault/tsconfig.json --noEmit` EXIT 0, zero error lines repo-wide, zero under tests/fault/disaster-restore/.\n- `pnpm test:fault` EXIT 0, Test Files 10 passed (10), Tests 83 passed (83).\n\nPath-attributed baseline: the named gate is fully green at HEAD, so the HEAD-minus-base failing-path delta is empty by construction and no foreign red needs disclosing on this leg. Working tree carries peer work I did not touch (activation-ledger-reader.ts + foundation-launch-authority.test.ts = d92; claude-host-runtime.* = 32eddfd3); neither drilled file appears modified.",
      "startedAt": "2026-08-16T01:11:21.210Z",
      "completedAt": "2026-08-16T01:16:09.167Z"
    },
    {
      "stepId": "step-6",
      "description": "DoD 4 — RUN THE GATE AND PROVE IT EXECUTED. Three legs, each run SEPARATELY with its own exit code captured (never `;`-chained, never piped into tail — a pipe reports tail's exit code and a semicolon reports only the last command's). LEG 1, scoped executed-count proof: `npx vitest run --config tests/fault/vitest.config.ts disaster-restore` from the repo root. Exit 0 alone is NOT evidence — you must read the OUTPUT and see `Test Files 3 passed` and a nonzero `Tests N passed`. If it reports 0 files, the substring filter did not match and the leg is UNVERIFIED, not passing. POSITIVE CONTROL, mandatory: run the identical command shape with `foundation` instead of `disaster-restore` and confirm it reports the 3 committed foundation files; that discriminates a real filter from a silently empty one. LEG 2, typecheck attribution: `npx tsc -p tests/fault/tsconfig.json --noEmit`, capture ALL output, and require ZERO error lines whose path begins `tests/fault/disaster-restore/`. LEG 3, the task's named gate: `pnpm test:fault`, full output captured. It WILL exit 1 from the foreign cross-host/linux/macos tsc errors recorded in step 1. Apply the path-attributed baseline: failing-path set at HEAD minus failing-path set at BASE, intersected with tests/fault/disaster-restore/** and packages/store/src/index.ts, MUST be empty. Report the foreign red verbatim with its exact failing lines and file paths — disclose it, never hide it and never fabricate a green. If the delta is non-empty, that is YOUR regression and you fix it before completing.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/fault/disaster-restore/anchor-boundary-crash.fault.ts",
        "tests/fault/disaster-restore/restore-identity-epochs.fault.ts",
        "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts"
      ],
      "note": "ALL THREE LEGS RUN FRESH, each with its own exit code captured separately — no `;`-chaining (only the last status survives) and no piping into tail/grep for the status (a pipe reports the tail's exit code). Each leg redirected to its own file, `$?` read immediately, then the file grepped.\n\nLEG 1 — `npx vitest run --config tests/fault/vitest.config.ts disaster-restore` EXIT 0, `Test Files 3 passed (3)`, `Tests 22 passed (22)`. Counts read off the OUTPUT, not inferred from the exit code.\nPOSITIVE CONTROL — identical shape with `foundation`: EXIT 0, `Test Files 3 passed (3)`, `Tests 43 passed (43)`. Different file set and a different test count, so the substring filter is genuinely selecting and leg 1 is not a silently empty run.\nLEG 2 — `npx tsc -p tests/fault/tsconfig.json --noEmit` EXIT 0. Zero error lines under `tests/fault/disaster-restore/`, and zero `error TS` lines repo-wide.\nLEG 3, the named gate — `pnpm test:fault` EXIT 0, `Test Files 10 passed (10)`, `Tests 83 passed (83)`.\n\nTHE PLAN'S PREDICTION FOR LEG 3 IS NOW WRONG IN MY FAVOUR, so I am stating it rather than quietly banking it. The step says leg 3 \"WILL exit 1 from the foreign cross-host/linux/macos tsc errors\". It exits 0: those foreign reds were resolved by their owners after this plan was written (cross-host landed at 91fc622 / 1c9e46c / 6cd4c17, task-69 residue removed at 376523c). There is no foreign red left on this leg to disclose verbatim, because there is no failing path at all.\n\nPATH-ATTRIBUTED BASELINE: the failing-path set at HEAD is EMPTY, so HEAD-minus-BASE intersected with `tests/fault/disaster-restore/**` and `packages/store/src/index.ts` is empty a fortiori — an empty HEAD set cannot have a non-empty intersection, which is a stronger result than a set-difference over a stale BASE measurement. BASE = cf272f67c3e34fe1d8c4dd00675512c67f165c05.\n\nHEAD MOVED MID-VERIFICATION and I re-checked rather than assuming. My commit e8da489 was made at HEAD e8da489; a peer then landed 6cd4c17 (`fix(task-01c5f96e): disable ANSI color so the host gate greps read the real counts`) while leg 1 was running. Verified: `git merge-base --is-ancestor e8da489 HEAD` succeeds, and `git show HEAD:tests/fault/disaster-restore/recovery-unknown-truth.fault.ts | sha256sum` = 93498179c8871643e404b64343a0672e5ac7b5922a8a267c2975fc71f9ed48f0, byte-identical to my working copy, with the distinctness assertions still present at :322/:326/:328. So the gated bytes are the committed bytes and nothing of mine was reverted. Leg 3 was run after that peer commit, i.e. against the current tree, not a stale one.",
      "startedAt": "2026-08-16T01:17:50.198Z",
      "completedAt": "2026-08-16T01:18:43.626Z"
    },
    {
      "stepId": "step-7",
      "description": "MUTATION DRILL — prove each failure-path assertion is still attached to its subject. Epic rail 6 requires this and a green suite cannot show it. Run four drills, one at a time, each reverted before the next. (1) In packages/store/src/recovery-anchor-install.ts delete one `fault?.(...)` call site (e.g. line 256 SWITCH): anchor-boundary-crash.fault.ts must go RED on the sweep's per-point mapping, proving the boundary is really injected and not merely named. (2) In packages/store/src/recovery-anchor-contracts.ts remove one member from RECOVERY_ANCHOR_FAULT_POINTS: the set-equality assertion must go RED in the under-sweep direction, proving the hand-written list is load-bearing. (3) In the incarnation mint path force the key epoch to be REUSED while the nonce stays fresh: restore-identity-epochs.fault.ts must go RED on the keyEpochRef assertion specifically — if it stays green, that assertion is checking only the nonce. (4) In apps/daemon/src/recovery/recovery-completion-evidence.ts:368 change the UNKNOWN_TRUTH code to any other value: recovery-unknown-truth.fault.ts must go RED. For EVERY drill record the exact replaced range you edited (a drill that applied nothing reads as green and proves nothing), and confirm the red lands on the INTENDED assertion, not on an unrelated crash — a red for the wrong reason is not a pass. RESTORE VIA GIT, not by hand: `git checkout -- <the one drilled file>` after each, and before moving on verify `git status --short` shows that file clean. A hand-typed restore goes ambiguous, and a foreign task-completion hook can commit a whole tree — a drill left on disk becomes someone else's commit.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/recovery-anchor-install.ts",
        "packages/store/src/recovery-anchor-contracts.ts",
        "apps/daemon/src/recovery/recovery-completion-evidence.ts"
      ],
      "note": "FOUR DRILLS, all measured. Three run fresh by me this session; one is QA's own independent drill, which I did not re-run and say so plainly. Each drill asserted an exactly-one-replacement guard in the mutator and printed the replaced range, because a drill that applied nothing reads as green and proves nothing. Every restore was `git checkout -- <one file>` followed by an SHA-256 comparison and a `git status --short` check.\n\nDRILL 1 — boundary really injected, not merely named. Deleted `  fault?.(\"SWITCH\");\\n` (exactly 1 occurrence) from packages/store/src/recovery-anchor-install.ts:256.\nRED on exactly `crashing at SWITCH leaves exactly one admissible world`, `AssertionError: expected null to be an instance of CrashSignal`. 1 failed | 21 passed. The other seven points stayed green, so the red is per-point and did not smear.\nRestored: SHA 5c701668a5467972f06d833fa4f54bdd7272e2d80d7b0db09363d53ef2719817, status clean.\n\nDRILL 2 — the hand-written list is load-bearing. Removed `  \"SWITCH\",\\n` (exactly 1) from RECOVERY_ANCHOR_FAULT_POINTS in packages/store/src/recovery-anchor-contracts.ts.\nRED on exactly `sweeps exactly the boundaries production enumerates, in both directions`, `AssertionError: expected [ 'SWITCH' ] to deeply equal []` — the hand-minus-production direction, which is the one that catches production shrinking under a stale hand list. 1 failed | 21 passed.\nRestored: SHA bd01f3e1cc06c5489c464c1f1683784815ff73c4342f5732c0935236a4f7f141, status clean.\n\nDRILL 3 — key epoch checked separately from the nonce. NOT RE-RUN BY ME. QA ran it during the reject and recorded the discriminating result: deleting the `keyEpochRef` clause from `detectFenceReuse` (packages/store/src/recovery-anchor.ts:66) reddened restore-identity-epochs on EXACTLY `refuses a spent key epoch paired with a fresh incarnation` (1 failed | 8 passed) while the sibling incarnation arm stayed GREEN. I did not repeat it because it was measured by an independent party with its exact failing test name, the file is unmodified at HEAD, and repeating it would mutate foreign production for an answer already in hand. The sibling-stays-green half is what rules out an equivalent mutant in that two-clause guard.\n\nDRILL 4 — recovery-unknown-truth is attached to production. Run BOTH ways, because the QA reject was about a specific assertion in that file:\n(4a) The step's literal drill. Changed `code: \"UNKNOWN_TRUTH\"` -> `\"RECORD_UNREADABLE\"` (exactly 1) at apps/daemon/src/recovery/recovery-completion-evidence.ts:368. RED on `case 2: a recorded inventory whose ARTIFACT proof is absent` with `expected 'RECORD_UNREADABLE' to be 'UNKNOWN_TRUTH'`, and additionally on the closing distinctness test with `expected 2 to be 3`. That second red is the SIZE guard firing, not the distinctness one: case 2 aborts at its code assertion before reaching `observeUpstream`, so only two cases observe and `expect(observedUpstreams.size).toBe(3)` catches the under-swept set. That is precisely the vacuous-sweep failure the guard exists to prevent. 2 failed | 20 passed. Restored: SHA b0686d93085d90197a3f32f85470c0dc9b8d5815c7c57fef05eaa38381eb4a9a, clean.\n(4b) The drill aimed at the REJECTED assertion itself, reported in full in step 5. Collapsed `ledger(\"RECOVERY_BINDING_UNAVAILABLE\")` -> `ledger(\"RECORD_NOT_FOUND\")` at apps/daemon/src/recovery/recovery-inventory-ledger.ts:95. RED at recovery-unknown-truth.fault.ts:326:69 — the distinctness assertion — with `expected 2 to be 3`, while the size guard at :322 PASSED, so it reddened on distinctness and not on an under-swept set. Counterfactual under the identical mutation: the rejected literal-array version stayed GREEN (1 failed | 21 passed, only case 3's own pin). Restored: SHA 968a34e26a1fe192f7f57f128eda1033fca777d6fa25e35f9ab970737e99283c, clean.\n\nTaken together 4a and 4b discriminate the two assertions in that closing test: 4a reddens the size guard with the distinctness guard unreached, 4b reddens the distinctness guard with the size guard passing. Neither guard is covering for the other.\n\nPOST-DRILL TREE STATE, checked rather than assumed — a foreign whole-tree completion hook can commit a drill left on disk. `git status --short` over all four drilled files plus tests/fault/disaster-restore/ is EMPTY, `git diff --stat` over my owned directory is empty, and the suite is green again at Test Files 3 passed (3), Tests 22 passed (22).",
      "startedAt": "2026-08-16T01:18:55.905Z",
      "completedAt": "2026-08-16T01:20:34.987Z"
    },
    {
      "stepId": "step-8",
      "description": "ADVERSARIAL SELF-REVIEW then complete. Read your own diff as an attacker. Checklist, all drawn from defects already found on this board: (a) Does any assertion say only 'refused' / 'threw' / 'not ok' without pinning the reason code AND the answering layer? If yes it is one added layer away from vacuous — fix it. (b) Does the 8-boundary sweep assert the case COUNT actually generated? A sweep that produces zero cases passes silently. (c) Does any verdict get computed inside disaster-harness.ts instead of read off a production function? That is authority reimplemented in the test. (d) Is every fixture valid at every EARLIER layer so the guard under test is the one that answers? (e) Cross-platform: no assertion depends on POSIX directory fsync (Windows no-ops it), no hard-coded path separators, temp roots under os.tmpdir(). (f) Resource cleanup: every opened SQLite store closed and every temp root removed on EVERY exit path including thrown CrashSignal — a held handle kills the vitest worker and a leaked root fails the next run. (g) Re-run legs 1 and 2 from step 6 once more and confirm the counts are unchanged and no disaster-restore typecheck error appeared. (h) `git status --short` must show ONLY your five owned files — no drill residue, no scratch, no generated evidence. COMMIT by explicit pathspec, naming each of the five files, never `git add -A`. Then complete_task with: the leg-1 count line verbatim, the positive-control count line, the leg-3 BASE-vs-HEAD path-attributed delta shown to be empty over your owned paths, and the foreign cross-host/linux/macos red quoted exactly as disclosure. Keep verification.command under 500 characters. If the foreign lane red has since been repaired by its owner and pnpm test:fault exits 0 outright, say so plainly instead of citing the baseline exception.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/fault/disaster-restore/anchor-boundary-crash.fault.ts",
        "tests/fault/disaster-restore/restore-identity-epochs.fault.ts",
        "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
        "tests/fault/disaster-restore/disaster-harness.ts",
        "packages/store/src/index.ts"
      ],
      "modifiedFiles": [
        "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts"
      ],
      "note": "ADVERSARIAL SELF-REVIEW of my own diff, read as a hostile reviewer. It found one thing worth fixing, which I fixed in commit 539338f rather than only noting.\n\nFOUND AND FIXED — an unstated dependency. The closing case reads what cases 1-3 observed, so it is order-dependent, and nothing in the code said so. It already fails CLOSED (running it alone via `.only`, a name filter, or a shuffled sequence leaves the map short and `expect(observedUpstreams.size).toBe(3)` reddens), but an unstated dependency is the same defect class QA rejected here: a comment or structure that does not say what it relies on. Now stated in a comment above the case. Comment only; no assertion changed.\n\nCONSIDERED AND DELIBERATELY KEPT — the distinctness assertions compare against `observed.length`, not the literal 3. Pinning both to 3 would be no stronger: cardinality is already pinned separately by the size guard, and using `observed.length` keeps the distinctness assertion purely about distinctness. Drills 4a and 4b proved the two guards discriminate rather than cover for each other — 4a reddens the size guard with distinctness unreached, 4b reddens distinctness with the size guard passing. Collapsing them onto one literal would lose that attribution.\n\nTHE REST OF THE CHECKLIST, against my diff specifically:\n(a) Reason codes and layers — my change ADDS no assertion that says only \"refused\"; every existing `toEqual({code, layer})` pin survives untouched (visible in the diff as pure additions above them). The new assertions pin distinctness of `code` AND of `code@layer`, so a collapse that kept codes distinct but merged layers still reddens.\n(b) Case count actually generated — this is the guard I added: `expect(observedUpstreams.size).toBe(3)`. Drill 4a fired it for real.\n(c) No authority in the test — the observed type is `RecoveryInventoryRefusal[\"upstream\"] | RecoveryCompletionUpstream`, both from the @moe/daemon root. I write no `{ code, layer }` shape and compute no verdict; I only read what production returned. Writing that shape by hand is precisely what QA rejected.\n(d) Fixtures valid at earlier layers — unchanged by my diff; case 2's explicit proof of this (record recorded, ledger reads FOUND, five classes COMPLETE) is untouched.\n(e) Cross-platform — I added no path separator, no fsync assumption, no platform branch. Verified green on win32.\n(f) Cleanup — I added no resource. The map is file-local module state in a per-file vitest worker, holds no handle, and cannot leak across files. `afterAll(cleanupDisasterRoots)` untouched.\nConcurrency — vitest gives each file its own worker and runs these cases sequentially, so the map has a single writer. Two cases sharing a label would silently overwrite, which would shrink the set and trip the size guard: fails closed.\n(g) Legs re-run after the comment commit: leg 1 EXIT 0 `Test Files 3 passed (3)` / `Tests 22 passed (22)` — counts unchanged. Leg 2 EXIT 0 with zero disaster-restore errors.\n(h) `git status --short` over my owned paths is EMPTY. No drill residue, no scratch, no generated evidence. The rest of the dirty tree is peer work I did not touch and did not stage: activation-ledger-reader.ts + foundation-launch-authority.test.ts (d92, worker-1fe6c848), claude-host-runtime.* (32eddfd3, worker-e47be2a7), plus .moe/.serena bookkeeping.\n\nCOMMITS, both by explicit pathspec naming the single file, never `git add -A`: e8da489 (the fix, +56/-10) and 539338f (this comment, +5). `git show --stat` on each shows exactly one path.\n\nOnly ONE of the five owned files needed changing. The plan's step-8 wording says to commit all five; the other four are byte-identical to d447a34 and QA passed them, so naming them in a pathspec would commit nothing. Bytes verified at HEAD 539338f: `git show HEAD:tests/fault/disaster-restore/recovery-unknown-truth.fault.ts | sha256sum` = 4d29f048c2ca7a3862d4a8f1729ffed432f619e0cfb727281d3ea73472bfad43, identical to the working copy, so the gated bytes are the committed bytes.\n\nFINAL NAMED GATE, fresh, foreground, unpiped status: `pnpm test:fault` EXIT 0, `Test Files 10 passed (10)`, `Tests 83 passed (83)`.\n\nDISCLOSURE, per the step's own instruction to say so plainly if the foreign lane was repaired: it was. `pnpm test:fault` exits 0 outright and there is NO baseline exception to cite. The cross-host/linux/macos tsc errors the plan predicted are gone (owners landed 91fc622 / 1c9e46c / 6cd4c17) and the task-69 store residue is gone (376523c). The `hostScheduleOutcome=... CROSS_HOST_HOST_MISMATCH ...` lines in the output are not failures — they are the cross-host suite correctly refusing to prove anything about linux/darwin from a win32 host, and those tests pass.",
      "startedAt": "2026-08-16T01:20:37.686Z",
      "completedAt": "2026-08-16T01:22:09.854Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "CORE-S14 is strong work and I verified it end to end. ONE rail violation blocks it; the fix is ~8 lines in one file. Do not redo anything else.\n\nWHAT I RE-RAN MYSELF (all green, counts read off the OUTPUT):\n- `pnpm test:fault` -> EXIT=0, `Test Files 10 passed (10)`, `Tests 83 passed (83)`. No baseline exception needed - the foreign cross-host/linux/macos red is gone at HEAD.\n- `npx vitest run --config tests/fault/vitest.config.ts disaster-restore` -> EXIT=0, `Test Files 3 passed (3)`, `Tests 22 passed (22)`. Positive control with `foundation` -> 3 files / 43 tests, so the filter is real and not silently empty.\n- `npx tsc -p tests/fault/tsconfig.json --noEmit` -> EXIT=0, zero error lines repo-wide, none under tests/fault/disaster-restore/.\n- MY OWN DRILL: deleted the `keyEpochRef` clause from `detectFenceReuse` (recovery-anchor.ts:66). restore-identity-epochs reddened on EXACTLY \"refuses a spent key epoch paired with a fresh incarnation\" (1 failed | 8 passed) while the sibling incarnation arm stayed green. Both fence operands are independently load-bearing, not an equivalent mutant. Restored via git, status clean.\n- All 5 owned files: committed bytes == gated bytes. Per-file caps fine (228/260/285/327, all under 400). Harness carries zero authority. SQLite handles and temp roots closed/removed on every exit path.\n\nDoD 1, DoD 2 and DoD 4 PASS. DoD 3's substance also passes - cases 1-3 each pin UNKNOWN_TRUTH + layer + the upstream tuple, plus the negatives and the throwing authority port.\n\nBLOCKER - epic rail 6 / project rail 1: \"a property must be asserted against the production surface, not against a test helper that reimplements it\". recovery-unknown-truth.fault.ts:277-284 ships an assertion fully detached from its subject, and the file header states a guarantee that assertion does not provide. See issues.",
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "HIGH",
  "order": 24,
  "comments": [
    {
      "id": "comment-38459f966b4c495e8deb16840402d3e9",
      "author": "governor-fce4d563",
      "content": "GOVERNANCE — read before planning. This task returned to PLANNING after architect-8a4a1764 blocked it. The block was correct and governance upheld it (#governors msg-52d7aef8). Do NOT re-derive the audit; it costs a full context load to reach the same answer.\n\nTHE FINDING, measured on disk 2026-08-09, not read off the design:\n  EXISTS — project-contract.ts declares commands recovery.restore_quiesce and recovery.complete, plus RestoreQuiesceWitness {backupGenerationHash, recoveryIncarnationRef, truthClass} and RecoveryCompletionWitness {coverageProofHash, inventoryReconciliationHash, recoveryDecisionRef, ...}. project-reducer.ts:35,162 admits the transition and calls quiesce(). project-validation.ts:166,171 are exact-key SHAPE guards. Real fsync exists, but only in the artifact CAS.\n  DOES NOT EXIST — grep over packages/*/src and apps/*/src, zero hits each: recoveryAnchor / \"recovery anchor\" (the objective needs TWO-SLOT anchors and DoD 1 needs a crash at every prepare/install/fsync/switch boundary — no slot-install protocol exists); keyEpoch / \"key epoch\" (DoD 2 needs fresh key epochs with stale grants failing — recoveryIncarnationRef is a string field in a witness, nothing MINTS an incarnation).\n\nThe witness shapes are the vocabulary of a restore. The controller that performs one is absent. quiesce() recording that a restore happened is not a restore.\n\nWHAT NOT TO DO, both explicitly rejected by QA in advance (qa-812c17a0, #general msg-a4b06362): (a) reimplementing the controller inside a test fixture — a direct epic rail 6 violation, rejected on sight; (b) narrowing the DoD to \"shape guards accept well-formed witnesses\" — worse than (a), because it ships a green label over an untested mechanism. There is nothing to inject faults into, so there is no evidence to be had, and unverifiable evidence is UNKNOWN, never PASS. QA has stated they will not approve a task whose DoD requires proving a behaviour no production surface performs.\n\nREQUIRED OUTPUT INSTEAD: file the prerequisite production tasks, then leave this one blocked with their ids and an unblock trigger. At minimum a two-slot recovery anchor with its prepare/install/fsync/switch protocol, and key-epoch minting with stale-grant refusal; architect-8a4a1764's handoff (mem:task-task-0c89476b78044024b07c86c0c8986bd0-handoff) names the rest. Bound each as composition over existing core symbols where possible, and measure each gap symbol by symbol before writing the ticket.\n\nPRECEDENT TO COPY: architect-0b81de40 did exactly this off the Foundation canary's block report — three CRITICAL tickets (task-671578e5, task-9011e3b3, task-39fe2da5), each with the gap measured on disk and scoped as composition rather than new domain logic. Two are being coded now. That is the play; see task-97554aa4 comment-b6b518a5 for the shape of the block record.\n\nThis is the fourth instance of one pattern — vocabulary present, capability absent — and it is the subject of pending rail proposal prop-d68b105c.",
      "timestamp": "2026-08-09T10:45:43.146Z"
    },
    {
      "id": "comment-01dac749064641d9850276e3ee2ba02c",
      "author": "architect-f39f0c46",
      "content": "PREREQUISITE ROUTING COMPLETE (2026-08-09, measured at HEAD 1ce0059). CORE-S14 remains blocked; no plan was submitted over absent production authority. Created:\n1. task-5606947a9d7d4f228dc63e6ce4dea69a — cursor-bound complete BackupGeneration (PLANNING).\n2. task-684e6972c8c1418f8996ea3edd61f00c — fresh 256-bit recovery incarnation/signing-key epoch (PLANNING).\n3. task-b6e3dd2af916490fb2bc4d375a530683 — crash-safe two-slot RecoveryAnchor installer (BACKLOG; depends 1+2).\n4. task-8a01c025b65246aca09e69d82e009587 — core+daemon recovery-incarnation authentication fence and stale-grant `SESSION_REPLAYED` production edge (BACKLOG; depends 2+3).\n5. task-0325dcf7ee744123b40cf583230c7b6a — real node-side post-N inventory adapters (BACKLOG; depends 1+2).\n6. task-cf7fb147bd1c47698cbd65c9535370aa — exact-class inventory/quarantine coordinator plus claim/effect embargo (BACKLOG; depends 3+4+5).\n7. task-6f786c58cabf4f85be8ed4135e68a752 — exact-digest HUMAN R3 recovery.complete composition (BACKLOG; depends 1+3+4+6).\n8. task-b5e9bd6444514d02a1e554420c0245b8 — shared typechecked test:fault/test:security lanes (BACKLOG; serialize after package.json owners task-739879d0 and task-9449ce65).\n\nUNBLOCK TRIGGER: tasks 1-8 are DONE; production grep/probe finds the shipped BackupGeneration capture/verifier, RecoveryAnchor PREPARED/INSTALLED installer and current-slot selector, fresh nonce/key epoch mint, daemon-current identity fence, exact configured-class inventory/quarantine embargo, R3 digest completion call site, and runnable `pnpm test:fault`; task-05ce9b8f no longer concurrently owns tests/fault/disaster-restore/**. Then plan this task as the final production-backed CORE-S14 proof. Do not replace any missing item with fixture authority or a shape-only witness test.",
      "timestamp": "2026-08-09T12:09:47.592Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-07T11:57:17.013Z",
  "updatedAt": "2026-08-16T01:29:52.513Z",
  "contextFetchedBy": [
    "qa-c7cedba3",
    "worker-dd806f63",
    "qa-ce3ba361"
  ],
  "metrics": {
    "firstClaimAt": "2026-08-09T10:39:13.320Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 8,
    "executedStepCount": 16,
    "rejectCount": 1,
    "reopenCount": 1,
    "doneAt": "2026-08-16T01:29:52.513Z",
    "wallClockMs": 571839193
  },
  "blockedReason": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "priorHandoffs": [
    {
      "whatIsDone": "Capability audit at HEAD 9d60091; no plan, no code. CORRECTED THE STANDING BLOCK REASON, which was factually stale: its grep for mintIncarnation/slotInstall was a false negative on names that never existed in this codebase. The restore controller and all its supporting surfaces ARE landed - runRestoreQuiesce/runSnapshottedRestoreQuiesce/verifyRestoreGeneration (restore-controller.ts), mintGenesisIncarnation, createRecoveryIncarnationService, anchorIncarnation, readKeyEpochPointer/writeKeyEpochPointer/commitSuccessionRecord. Five of the seven prerequisites architect-f39f0c46 routed are DONE.",
      "whatRemains": "Plan the CORE-S14 restore proof once task-cf7fb147 lands. DoD 1 (crash at every prepare/install/fsync/switch boundary) and DoD 2 (repeated same-generation restore uses fresh incarnation/key epochs, stale grants fail) are already backed by real production authority and can be planned against it directly. DoD 3 is the only item still lacking a driver.",
      "pitfalls": "DO NOT re-derive the 11:29Z audit - it costs a full context load to reach an answer that is now wrong. Its symbol names were guesses, not measurements: grep the real names listed above. This is the second false-negative grep on this board today; the other was `grep telemetry packages/runner/src/index.ts` returning zero while the surface was published through a differently-named barrel. Prefer a compiled probe or a directory listing over a guessed-symbol grep. Also: DoD 3 must be driven through the real coordinator once it exists - fabricating a missing-inventory condition is the mock-backed journey Clause 2 forbids, and this task exists precisely because that shortcut was refused twice already.",
      "openQuestions": "Whether task-6f786c58cabf4f85be8ed4135e68a752 (exact-digest HUMAN R3 recovery.complete composition, prerequisite 7 of 7) is also required for this proof, or only for the R3 completion task. DoD 1-4 as written do not obviously need it, but the routing comment listed it in the same family - resolve before planning rather than assuming either way.",
      "releasedAt": "2026-08-15T18:33:20.569Z",
      "releasedBy": "architect-7f301fa7",
      "reason": "Releasing the ASSIGNMENT only; task stays BLOCKED, now on a corrected and much narrower gate (DoD 3 / task-cf7fb147) rather than the stale \"no controller exists\" reason."
    },
    {
      "whatIsDone": "Measured the production gaps; created eight prerequisite tasks (task-5606947a, task-684e6972, task-b6e3dd2a, task-8a01c025, task-0325dcf7, task-cf7fb147, task-6f786c58, task-b5e9bd64); recorded exact sequence/unblock probe in comment-01dac749064641d9850276e3ee2ba02c; wrote Serena handoff memory.",
      "whatRemains": "Keep Disaster restore proof BLOCKED until all eight prerequisites are DONE and production consumer edges plus pnpm test:fault are verified on disk; then plan the final CORE-S14 proof.",
      "pitfalls": "Do not call moe.unblock_worker on this assignment: it has previously flipped the BLOCKED task to PLANNING. Do not substitute fixture authority or shape-only witness tests.",
      "openQuestions": "Resolve tests/fault/disaster-restore ownership collision with task-05ce9b8f before unblocking.",
      "releasedAt": "2026-08-09T12:11:48.326Z",
      "releasedBy": "architect-f39f0c46",
      "reason": "Explicit architect handoff after completing the required prerequisite-production decomposition; user asked to plan another task. Keep the task BLOCKED."
    }
  ],
  "planSizeWarnings": [
    "Plan touches 8 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-5",
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [
    {
      "item": "Epic rail 6 / project rail 1 - assert the property against the production surface, not a test-local reimplementation (recovery-unknown-truth.fault.ts:277-284). DoD 3's substance is proven; this rail violation in DoD 3's file is what blocks.",
      "rejectedAt": "2026-08-16T01:09:56.468Z",
      "rejectedBy": "qa-c7cedba3"
    }
  ],
  "planSubmittedAt": "2026-08-16T00:35:53.890Z",
  "planningNotes": {
    "approachesConsidered": "1) REPORT BLOCKED again, as the 2026-08-09 handoff did. REJECTED after measurement: that handoff's unblock probe listed nine required capabilities and eight of nine are now demonstrably on disk. Re-blocking on a stale note would be exactly the second failure mode the 'stale by default' rail names — planning work to build something already landed.\n\n2) Drive DoD 1 through the daemon restore controller (`runRestoreQuiesce`, apps/daemon/src/recovery/restore-controller.ts:290). REJECTED: it is NOT exported from apps/daemon/src/index.ts (grep: 0 hits for restore-controller, effect-inventory, durable-recovery-inventory, recovery-key-provider), and its own fault enum RESTORE_CONTROLLER_FAULT_POINTS has only TWO members (AFTER_REDUCER_BEFORE_COMMIT, INSIDE_COMMIT_APPLY) — both commit-level, neither prepare/install/fsync/switch. It is the wrong surface for this DoD and reaching it would need a second barrel edit plus a deep-import risk. The @moe/store anchor installer is the right and reachable surface, and it enumerates all four named boundaries plus four more.\n\n3) Hand-copy the 8 boundary names into the test without exporting the enum, avoiding the packages/store/src/index.ts edit. REJECTED: a hand list alone under-sweeps silently the day production adds a ninth point, and the epic rail requires the property be asserted against the PRODUCTION surface. Step 1's two-line export plus bidirectional set-equality costs almost nothing and closes both drift directions. packages/store has no index-surface.test.ts, so nothing else has to change.\n\n4) Derive the sweep table FROM the exported enum only. REJECTED for the mirror reason: a generated table cannot police its own generator. Hence hand-written list AND production comparison, both.\n\n5) Split into three tasks, one per DoD. REJECTED: 8 steps and 5 distinct files sit inside the daemon's taskSizing thresholds, the three fault files share one harness, and there is no per-task LOC budget. Splitting would triplicate the harness and the gate.",
    "codebaseInsights": "RE-MEASURED AT HEAD 1357062 on 2026-08-16 — the reopen note and the 2026-08-09 handoff are both partly stale, in BOTH directions. Do not trust either; the measurements below are the ones that count.\n\nPREREQUISITE STATUS re-measured from .moe/tasks/*.json (the governor's unblock cited ONE clause of an EIGHT-clause premise): 5606947a DONE, 684e6972 DONE, b6e3dd2a DONE, 8a01c025 DONE, 0325dcf7 ARCHIVED (not DONE), cf7fb147 DONE, 6f786c58 BLOCKED, b5e9bd64 DONE. The two non-DONE ones do NOT block this task, and I verified why on disk rather than by status: 6f786c58's blockedReason is a GATE-EXECUTION block ('exact exit 0 cannot be truthfully recorded' against foreign reds), not a capability block — its deliverable IS landed (recovery-completion.ts, recovery-completion-digest.ts, recovery-completion-authority.ts, all barrel-exported). 0325dcf7's inventory adapters are present as apps/daemon/src/recovery/durable-recovery-inventory*.ts + effect-inventory*.ts + recovery-inventory-*.ts.\n\nCAPABILITY PROBE — `npx tsc -p tests/fault/tsconfig.json --noEmit` over a throwaway tests/fault/_probe/edge-probe.ts importing 26 symbols. RESULT: 25 of 26 reachable, exactly ONE error, TS2305 'Module ../../../packages/store/src/index.js has no exported member RECOVERY_ANCHOR_FAULT_POINTS'. Probe deleted in the same command. That single gap is what step 1 closes.\n\nDEPENDENCY EDGE (epic rail): tests/fault has NO package.json — it is part of the ROOT package `moe-next`, whose package.json declares `@moe/daemon: workspace:*` and `@moe/runner: workspace:*`. The bare specifier `@moe/daemon` therefore resolves from tests/fault, proven by the compiled probe above AND by the existing durable call site tests/fault/cross-host/production-surfaces.fault.ts. The relative barrel `../../../packages/store/src/index.js` is the lane's established pattern (foundation/*.test.ts already use it for store/core/contracts/scheduler/testkit). Deep imports into apps/daemon/src/** are UNPROVEN — do not use them.\n\nBOUNDARY SEAM: RECOVERY_ANCHOR_FAULT_POINTS (recovery-anchor-contracts.ts:46) enumerates 8 — PREPARE, INACTIVE_INSTALL, TRANSACTION, FILE_FSYNC, DIRECTORY_PERSISTENCE, VERIFICATION, SWITCH, INSTALLED_MARKER — and ALL EIGHT have live call sites: recovery-anchor.ts:83 and :95 for PREPARE, recovery-anchor-install.ts:232/235/241/247/250/256/260 for the rest. Injection reaches them through the PUBLIC entry: `installRecoveryAnchor(input: unknown)` snapshots `injectFault` and keeps it (recovery-anchor-record.ts:84/97/111, defaulting to null). No deep import and no test-owned controller is needed.\n\nGATE LANE: root package.json:22 `test:fault` = `tsc -p tests/fault/tsconfig.json && vitest run --config tests/fault/vitest.config.ts`. The lane config sets root=tests/fault, include ['foundation/**/*.test.ts','**/*.fault.ts'], passWithNoTests:false, retry 0, fileParallelism false, allowOnly false, and a locale-independent sequencer. So the `.fault.ts` suffix is what puts a file in this lane.",
    "risks": "1) `pnpm test:fault` IS RED AT BASELINE and the red is FOREIGN — 8 tsc errors across tests/fault/cross-host/, tests/fault/linux/, tests/fault/macos/ (TS2307 for a missing './effect-schedule-driver.js', TS2322 WorkspaceProducer, TS7006 implicit any). That is the in-flight cross-host slice. The worker must NOT repair it and must NOT let it masquerade as their own red; the path-attributed baseline (HEAD failing paths minus BASE failing paths, intersected with owned paths, must be EMPTY) is the whole discipline here. Note the lane's tsc leg typechecks the ENTIRE tests/fault tree, so exit 0 on `pnpm test:fault` may be unreachable through no fault of this task — that is precisely what blocked task-6f786c58, so anticipate it rather than fight it.\n\n2) EXIT 0 IS NOT EVIDENCE. A vitest filter that matches nothing, and `pnpm --filter` on an unresolved package, both exit 0 while running zero tests. Step 6 leg 1 therefore requires the printed `Test Files 3 passed` count line AND a positive control against `foundation`. An exit-0 leg with no count line is UNVERIFIED, not passing.\n\n3) NEVER `;`-chain or pipe a gate leg. A semicolon chain reports only the last command's status and a pipe reports tail's — both turn a red suite into a green record.\n\n4) SHARED WORKTREE. Other agents commit while this runs; a task-completion hook elsewhere can commit a whole tree and sweep up an uncommitted mutation drill. Restore every drill with `git checkout -- <file>` and verify `git status --short` clean before the next one. Commit by explicit pathspec only.\n\n5) WINDOWS. recovery-anchor-fs.ts:18 gates directory fsync behind `process.platform !== 'win32'`. The DIRECTORY_PERSISTENCE fault point still fires there (callback precedes the platform check), so the case is swept on every platform — but no assertion may depend on POSIX directory-durability side effects.\n\n6) SQLITE HANDLES. A store handle left open kills the vitest worker outright, and the lane runs fileParallelism:false so one leak stalls everything after it. Close on every exit path, including the thrown CrashSignal path, then rmSync the temp roots.\n\n7) REFUSAL ASSERTIONS DETACHING. Several layers can answer here — RECOVERY_ANCHOR, RECOVERY_INSTALL_TRANSACTION, RECOVERY_INVENTORY, RECOVERY_COMPLETION, RECOVERY_SUCCESSION. Pinning only the code, or only 'it failed', lets a different layer start answering while the test stays green. Pin code AND layer, and build each fixture valid at every earlier layer so the intended guard is the one that speaks.\n\n8) PATH OWNERSHIP. task-05ce9b8f (Security fault matrix) nominally owns the broader tests/fault/** but is BLOCKED with no worker, and its own blockedReason concedes it must be re-sliced. This task's description assigns tests/fault/disaster-restore/** specifically. Stay strictly inside that subtree plus packages/store/src/index.ts; do not touch tests/security/** or the other tests/fault subdirectories.\n\n9) If DoD 2's stale-grant refusal turns out to be inexpressible through the exported daemon surface, that is a real production gap: report_blocked naming the missing symbol. Do not narrow the DoD, do not mock the fence, do not reimplement authority inside the test.",
    "keyFiles": [
      "packages/store/src/index.ts",
      "packages/store/src/recovery-anchor-contracts.ts",
      "packages/store/src/recovery-anchor-install.ts",
      "packages/store/src/recovery-anchor.ts",
      "packages/store/src/recovery-anchor-record.ts",
      "packages/store/src/backup-generation.ts",
      "apps/daemon/src/index.ts",
      "apps/daemon/src/recovery/recovery-completion.ts",
      "apps/daemon/src/recovery/recovery-completion-evidence.ts",
      "apps/daemon/src/recovery/recovery-inventory-contract.ts",
      "apps/daemon/src/recovery/recovery-incarnation.ts",
      "tests/fault/vitest.config.ts",
      "tests/fault/tsconfig.json",
      "tests/fault/cross-host/production-surfaces.fault.ts"
    ]
  },
  "budget": {
    "wallClockMs": 7200000,
    "warnedAt": "2026-08-16T00:35:56.763Z",
    "escalatedAt": "2026-08-16T00:35:56.763Z"
  },
  "planApprovedAt": "2026-08-16T00:35:55.953Z",
  "workStartedAt": "2026-08-16T00:36:18.182Z",
  "reviewStartedAt": "2026-08-16T01:23:09.003Z",
  "verification": {
    "command": "pnpm test:fault",
    "exitCode": 0,
    "outputTail": "$ tsc -p tests/fault/tsconfig.json && vitest run --config tests/fault/vitest.config.ts\n\n RUN  v4.1.10 D:/projexts/moe-next/tests/fault\n\nhostScheduleOutcome={\"observedHost\":\"win32\",\"ok\":false,\"code\":\"CROSS_HOST_HOST_MISMATCH\",\"layer\":\"CROSS_HOST_COLLECTOR\",\"hostSlot\":\"linux\",\"message\":\"the executing host reports win32, which cannot prove anything about linux\"}\nhostScheduleOutcome={\"observedHost\":\"win32\",\"ok\":false,\"code\":\"CROSS_HOST_HOST_MISMATCH\",\"layer\":\"CROSS_HOST_COLLECTOR\",\"hostSlot\":\"darwin\",\"message\":\"the executing host reports win32, which cannot prove anything about darwin\"}\n\n Test Files  10 passed (10)\n      Tests  83 passed (83)\n   Start at  04:21:34\n   Duration  8.24s (transform 1.82s, setup 0ms, import 4.12s, tests 3.22s, environment 0ms)",
    "reportedAt": "2026-08-16T01:23:09.003Z"
  },
  "filesModified": [
    "packages/store/src/index.ts",
    "tests/fault/disaster-restore/disaster-harness.ts",
    "tests/fault/disaster-restore/anchor-boundary-crash.fault.ts",
    "tests/fault/disaster-restore/restore-identity-epochs.fault.ts",
    "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
    "packages/store/src/recovery-anchor-install.ts",
    "packages/store/src/recovery-anchor-contracts.ts",
    "apps/daemon/src/recovery/recovery-completion-evidence.ts"
  ],
  "reviewCompletedAt": "2026-08-16T01:29:52.513Z",
  "rejectionDetails": {
    "failedDodItems": [
      "Epic rail 6 / project rail 1 - assert the property against the production surface, not a test-local reimplementation (recovery-unknown-truth.fault.ts:277-284). DoD 3's substance is proven; this rail violation in DoD 3's file is what blocks."
    ],
    "issues": [
      {
        "type": "test_failure",
        "file": "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
        "line": 277,
        "description": "it(\"keeps the three absences distinguishable by upstream\") asserts new Set(upstreams).size === upstreams.length over a LOCAL array of 3 string literals. Distinct literals are distinct. No production input, so it cannot fail under ANY production change - unkillable by construction. Fix: assert distinctness over the upstreams production actually returned in cases 1-3, or delete it (those cases already pin each by toEqual). Drill it: collapse one production upstream, require red."
      },
      {
        "type": "other",
        "file": "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
        "line": 6,
        "description": "Header claims the three upstream codes are \"asserted pairwise distinct at the end, so a future collapse into a single 'not available' reddens instead of passing\". False of the test at :277-284, which never touches production. Only the per-case upstream assertions in cases 1-3 catch a collapse. A maintainer trusting this and trimming those would lose the property while the suite stays green. Fix the comment to name what enforces it."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "CORE-S14 is strong work and I verified it end to end. ONE rail violation blocks it; the fix is ~8 lines in one file. Do not redo anything else.\n\nWHAT I RE-RAN MYSELF (all green, counts read off the OUTPUT):\n- `pnpm test:fault` -> EXIT=0, `Test Files 10 passed (10)`, `Tests 83 passed (83)`. No baseline exception needed - the foreign cross-host/linux/macos red is gone at HEAD.\n- `npx vitest run --config tests/fault/vitest.config.ts disaster-restore` -> EXIT=0, `Test Files 3 passed (3)`, `Tests 22 passed (22)`. Positive control with `foundation` -> 3 files / 43 tests, so the filter is real and not silently empty.\n- `npx tsc -p tests/fault/tsconfig.json --noEmit` -> EXIT=0, zero error lines repo-wide, none under tests/fault/disaster-restore/.\n- MY OWN DRILL: deleted the `keyEpochRef` clause from `detectFenceReuse` (recovery-anchor.ts:66). restore-identity-epochs reddened on EXACTLY \"refuses a spent key epoch paired with a fresh incarnation\" (1 failed | 8 passed) while the sibling incarnation arm stayed green. Both fence operands are independently load-bearing, not an equivalent mutant. Restored via git, status clean.\n- All 5 owned files: committed bytes == gated bytes. Per-file caps fine (228/260/285/327, all under 400). Harness carries zero authority. SQLite handles and temp roots closed/removed on every exit path.\n\nDoD 1, DoD 2 and DoD 4 PASS. DoD 3's substance also passes - cases 1-3 each pin UNKNOWN_TRUTH + layer + the upstream tuple, plus the negatives and the throwing authority port.\n\nBLOCKER - epic rail 6 / project rail 1: \"a property must be asserted against the production surface, not against a test helper that reimplements it\". recovery-unknown-truth.fault.ts:277-284 ships an assertion fully detached from its subject, and the file header states a guarantee that assertion does not provide. See issues.",
      "rejectedAt": "2026-08-16T01:09:56.468Z",
      "reopenCount": 1,
      "failedDodItems": [
        "Epic rail 6 / project rail 1 - assert the property against the production surface, not a test-local reimplementation (recovery-unknown-truth.fault.ts:277-284). DoD 3's substance is proven; this rail violation in DoD 3's file is what blocks."
      ],
      "issues": [
        {
          "type": "test_failure",
          "file": "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
          "line": 277,
          "description": "it(\"keeps the three absences distinguishable by upstream\") asserts new Set(upstreams).size === upstreams.length over a LOCAL array of 3 string literals. Distinct literals are distinct. No production input, so it cannot fail under ANY production change - unkillable by construction. Fix: assert distinctness over the upstreams production actually returned in cases 1-3, or delete it (those cases already pin each by toEqual). Drill it: collapse one production upstream, require red."
        },
        {
          "type": "other",
          "file": "tests/fault/disaster-restore/recovery-unknown-truth.fault.ts",
          "line": 6,
          "description": "Header claims the three upstream codes are \"asserted pairwise distinct at the end, so a future collapse into a single 'not available' reddens instead of passing\". False of the test at :277-284, which never touches production. Only the per-case upstream assertions in cases 1-3 catch a collapse. A maintainer trusting this and trimming those would lose the property while the suite stays green. Fix the comment to name what enforces it."
        }
      ]
    }
  ],
  "completedAt": "2026-08-16T01:29:52.513Z",
  "reviewSummary": "APPROVED at HEAD 539338f. Both reject issues fixed and DRILLED by me, not taken on the note.\n\nISSUE 1 (unkillable distinctness) FIXED. Closing case reads production returns via observeUpstream, called BEFORE each per-case pin. MY DRILL, the one the reject named: collapsed a real production upstream, recovery-inventory-ledger.ts:95 ledger(\"RECOVERY_BINDING_UNAVAILABLE\") -> ledger(\"RECORD_NOT_FOUND\"). EXIT=1, 2 failed, and the distinctness assertion ITSELF reddened at recovery-unknown-truth.fault.ts:331 \"expected 2 to be 3\" while the size guard passed. Killable now; previously unkillable. Restored from out-of-repo byte copy, sha256 968a34e2...283c matches, tree clean.\n\nISSUE 2 (false header) FIXED and true: header now says distinctness holds over production-returned upstreams. Drill proves that claim literally.\n\nNEW GUARD LOAD-BEARING. Isolation run (-t filter) reds at :327 \"expected +0 to be 3\", distinctness unreached. Two guards discriminate; not equivalent mutants.\n\nGATES (counts off OUTPUT, unpiped, foreground):\n- pnpm test:fault EXIT=0, Test Files 10 passed (10), Tests 83 passed (83). Run twice, baseline and post-drill, identical. tsc green proven by the && chain.\n- vitest disaster-restore EXIT=0, 3 files / 22 tests. Positive control foundation -> 3/43, filter is real.\n\nDoD: 1 sweeps bidirectionally vs production RECOVERY_ANCHOR_FAULT_POINTS, count pinned 8 (:151-162). 2 pins code AND layer (:230,232,266,268,292-294). 3 pins UNKNOWN_TRUTH + layer + upstream per case, now production-bound. 4 green.\n\nRAILS: harness has zero authority (no hand-built {code,layer,ok}; unreachableAuthority throws). Per-file 336/228/327/260/262/261/119, all under 400. Commits 4e0201a (5 owned files, +1102), e8da489, 539338f are pathspec-clean. NOTE: d447a34 shares this task's message but is the board-wide platform sweep seen under 12 other task ids; not attributable, no peer work lost (7da2a1a landed its increment after). Gated bytes == committed bytes, 4d29f048...ad43."
}