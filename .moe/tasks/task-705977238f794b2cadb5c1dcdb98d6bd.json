{
  "id": "task-705977238f794b2cadb5c1dcdb98d6bd",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "bug: rollback restores the CURRENT deployment's backup against the TARGET's image, and resolves the restore before checking Docker",
  "description": "DATA-LOSS REACH. Found by an external review of main and re-measured by architect-bb193f43 at main 0c215e82 on 2026-09-11. Two defects in one operation; both are on main now and both were on both branches before the merge, so neither is merge damage.\n\nDEFECT 1 - THE BACKUP IS KEYED TO THE WRONG DEPLOYMENT.\nMeasured at `apps/daemon/src/deployment/rollback-restore.ts`, in `resolveRollbackRestore`:\n\n    const current = readCurrentDeployReceipt(config.store, config.projectId, environment);\n    ...\n    const bound = destination(config, environment, current.decisionId, current.sha);\n    const dump = await dumpToRestore(config, current.decisionId);\n\nBoth the destination and the dump are keyed on `current.decisionId` - the deployment in place RIGHT NOW - while the rollback command targets a DIFFERENT, earlier deployment. The reviewer reproduced deployments A -> B -> C, then requested rollback to A with database restoration: the resolver selected C's pre-migration backup, which contains schema B, while the image being rolled to is A. The database and the application end up at different versions, silently.\n\nDEFECT 2 - THE RESTORE IS RESOLVED BEFORE THE HOST IS KNOWN TO BE USABLE.\nThe same reproduction invoked restoration before discovering Docker was unavailable. The file's own header calls the split deliberate - \"a refusal from here has cost nothing and reserved nothing\" - so the intent is that resolution is free. The measured behaviour contradicts the intent: the ordering can leave the database reverted while the current application keeps running.\n\nVERIFY BEFORE PLANNING - do not trust this description. `sed -n '190,206p' apps/daemon/src/deployment/rollback-restore.ts` and read `rollback-command.ts` around the restore call. The 2026-09-10 row task-d85fd8fd (image built from approved sha, migrations from the working tree) fixed a NEIGHBOURING ordering bug; this is not that one and the fix there did not close this.\n\nSCOPE FENCE. The review also asks that image selection, schema selection, operational checks and exclusion of concurrent rollbacks form ONE coherent operation. Concurrency exclusion is NOT in this row - it is a separate mechanism and would breach the size caps. File it as a successor if this row's plan shows the seam. This row fixes target-keying and ordering only.",
  "definitionOfDone": [
    "A test drives deployments A -> B -> C through the production writers, requests rollback to A with database restoration, and asserts the resolved dump is A's backup - by its decision id, read back from the store - not C's. Assert the identifier, never merely that some dump was chosen.",
    "A test asserts that when Docker is unavailable, the rollback refuses with its stable code AND its layer BEFORE any dump path is resolved or any restore is attempted. Prove the restore was not reached, for example by a port that throws if called, rather than by asserting only the refusal.",
    "Both new arms are proven non-vacuous: mutate the production selection back to `current.decisionId` and confirm the first arm goes red; mutate the ordering back and confirm the second goes red. Quote both reds.",
    "`pnpm --filter @moe/daemon test` EXIT 0 with the `Test Files N passed (N)` count line quoted. An exit code without a count line is not evidence.",
    "No change to the rollback command's public refusal vocabulary unless a new code is added to the closed code-to-layer map in the same commit, with the map's own test updated."
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "RE-MEASURE, DO NOT TRUST THE PLAN EITHER. Confirm at your HEAD, before editing anything: (a) `sed -n '190,206p' apps/daemon/src/deployment/rollback-restore.ts` still shows both destination(...) and dumpToRestore(...) keyed on `current.decisionId`; (b) `sed -n '194,205p;242,244p;304,314p' apps/daemon/src/deployment/rollback-command.ts` still shows resolve at ~196, apply at ~243 and the deploy service (which owns the `docker version` check) at ~304; (c) `git grep -n 'docker.calls' -- apps/daemon/src/deployment` still returns exactly the three emptiness assertions named in planningNotes. Then record the BASELINE: run `pnpm --filter @moe/daemon test` at the merge-base tree and capture the `Test Files N passed (N)` count line and any already-failing files. An exit code with no count line is not evidence - `--filter` prints 'No projects matched the filters' and exits 0 when the package name is wrong; @moe/daemon is apps/daemon. If any of (a)-(c) no longer holds, stop and comment on the task rather than planning around the drift.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-restore.ts",
        "apps/daemon/src/deployment/rollback-command.ts"
      ],
      "note": "RE-MEASURED at HEAD 43c744b0 (main 0c215e82 + wrapper wip). (a) CONFIRMED: rollback-restore.ts:194-201 still reads readCurrentDeployReceipt and keys BOTH destination(...current.decisionId, current.sha) and dumpToRestore(...current.decisionId). (b) CONFIRMED: rollback-command.ts resolve at :196, apply at :243, deploy service at :304; the only docker reachability check is deploy-service.ts:265-267.\n\n(c) DOES NOT HOLD AS WRITTEN, and this is a PLAN MEASUREMENT ERROR, not drift. `git grep -n 'docker.calls' -- apps/daemon/src/deployment` returns 80 lines across 8 files. The three assertions planningNotes names are all present at their stated lines, but the claim \"rollback-command.test.ts's own docker arms use restoreDatabase:false and are NOT affected\" is FALSE. Measured restore:true arms in that file that assert docker.calls emptiness: :92 (it.each row [\"restore\",...] sets restoreDatabase=true at :86), :171 (unwired-daemon restore arm), :276 (arm c, bind:false), :297 and :314 (arm d, failing restore). Five unnamed asserting consumers, plus rollback-production-wiring.test.ts:87 (arm 2), where a naive precheck would CHANGE THE REFUSAL CODE from ENV_ENVIRONMENT_UNKNOWN@SCOPE to DEPLOY_TARGET_MISSING and destroy that arm's subject, because createStoreDependencies has no bound deploy target and productionDeployPorts().target() answers null.\n\nNOT PLANNING AROUND IT - two narrowings absorb it, both disclosed here and both STRICTLY stronger than the plan on the DoD's own terms:\n 1. Skip the host probe when options.environmentCredential === undefined. Boundness-answers-first is the production module's own documented discipline (rollback-restore.ts:195-197): an unwired daemon has no destination for ANY environment and probing its docker host is meaningless. Keeps :92 and :171 byte-identical.\n 2. A NULL deploy target is a no-op skip in the probe, not DEPLOY_TARGET_MISSING. DoD item 2 names Docker only; pre-empting the deploy engine's own TARGET_MISSING would report a refusal from a layer that did not answer and would cost rollback-production-wiring.test.ts:87 its unique coverage (host-scoped workspace forwarding through the REAL provider, reachable nowhere else). Keeps arm 2 byte-identical. Residual disclosed to the task as a comment.\nNet blast radius after both: the 3 named arms plus 3 in rollback-command.test.ts (:276, :297, :314), ALL of the same mechanical class step 6 already prescribes - RETIGHTEN to the exact read-only probe argv, never delete.\n\nALSO MEASURED, not in the plan: rollback-restore.test.ts arm (c3) at :291 WILL red under the new selection. It asks about environment \"staging\" while config.toReceiptRef names production's KEPT receipt, so the target would not be among staging's receipts and the walk refuses DEPLOY_ROLLBACK_RESTORE_DEPLOY_UNKNOWN instead of forwarding ENV_ENVIRONMENT_UNKNOWN@SCOPE. Fixed in step 4 by passing staging's own receipt id in that arm's config spread - the arm's subject is preserved exactly. rollback-restore.test.ts is step 4's owned file.\n\nBASELINE, fresh at the merge-base tree: `pnpm --filter @moe/daemon test` EXIT 0, `Test Files  612 passed (612)`, `Tests  11030 passed | 21 skipped (11051)`, Duration 264.31s. ZERO pre-existing failures, so any red from here is mine.\n\nSHARED CHECKOUT: peer dirt present and untouched - .moe/* task and message JSON, .codex/.grok config, .moe/roles/*.md, three deleted node-*-e2e-proj-k3x0md fixture pairs staged by another session, and untracked Moe-NG_Deep_Review_2026-09-05.md. Not reverted, not stashed, not staged.",
      "startedAt": "2026-09-11T09:33:04.657Z",
      "completedAt": "2026-09-11T09:43:01.514Z"
    },
    {
      "stepId": "step-2",
      "description": "NEW MODULE: apps/daemon/src/deployment/rollback-preflight.ts, plus its tracked bridge rollback-preflight.js containing exactly `export * from \"./rollback-preflight.ts\";` (every sibling .ts in this directory has one; a missing bridge breaks the .js import specifiers the rest of the slice uses). Export two pure functions and no side effects.\n\n(1) `selectRollbackDumpDecision(store, projectId, environment, toReceiptRef)` - WHICH DEPLOY'S DUMP RESTORES THE TARGET'S SCHEMA. Read `readDeployLedger(store, projectId).get(environment)` from ./deploy-ledger.js. Refuse `DEPLOY_ROLLBACK_RESTORE_DEPLOY_UNKNOWN` when there is no state for the environment or when `toReceiptRef` is not among `state.receipts`. Otherwise walk `state.receipts` FORWARD from the index after the target and, for each, call `readMigrationReceipt(store, projectId, receipt.decisionId)` from ../repository/migrations/migration-receipt.js inside a try/catch (it THROWS MIGRATION_RECEIPT_INVALID; catch it and refuse `DEPLOY_ROLLBACK_RESTORE_MIGRATION_UNVERIFIED`, never relaying the thrown message). SKIP successors whose receipt is null - a refused-before-migration deploy and every rollback record none. STOP at the first successor whose receipt is non-null: if its `backupRef` is null refuse `DEPLOY_ROLLBACK_RESTORE_BACKUP_ABSENT`, otherwise return that successor's `decisionId` together with the TARGET receipt (the caller needs the target's `decisionId` and `sha` for the destination). If the walk ends with no successor having recorded a migration, refuse `DEPLOY_ROLLBACK_RESTORE_MIGRATION_UNKNOWN` - nothing has moved the schema since the target, so no dump identifies its state. Return only the existing RollbackRestoreCode values; mint no new code. Do NOT skip past a null backupRef to a later dump: that would be a LATER schema state and would destroy more than the rollback asked for.\n\n(2) `probeRollbackHost(ports, environment)` - IS THIS HOST USABLE AT ALL, answered read-only. Take `Pick<DeployPorts, \"docker\"|\"ssh\"|\"target\">`. Mirror deploy-service.ts:189-194's seam exactly: `ports.target(environment)`; null means refuse `DEPLOY_TARGET_MISSING`; otherwise run argv `[\"version\",\"--format\",\"{{.Server.Version}}\"]` through `ports.docker` locally or `ports.ssh([target.sshTarget,\"docker\",...args])` remotely, treating an ssh code 255 as null and a throw as unavailable; a non-zero code refuses `DEPLOY_DOCKER_UNAVAILABLE`. Both codes are already in DEPLOY_REFUSAL_CODES and both carry layer DEPLOY_ENGINE_STAMP (DAEMON_DEPLOY_ENGINE), which is the same code@layer pair the deploy service answers with - so the operator's vocabulary is unchanged and no code-to-layer map is edited. Build the detail from a fixed closed string, NEVER from docker's stderr: this refusal is minted before any receipt declassification runs, so external stderr must not reach it (epic rail 3).\n\nWrite the module header to carry the dump-selection argument (the prior/kept/current diagram and the 'one step too far' reasoning) since the decision now lives here, and trim the corresponding paragraph out of rollback-restore.ts in the next step so that file stays under 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-preflight.ts",
        "apps/daemon/src/deployment/rollback-preflight.js"
      ],
      "newFiles": [
        "apps/daemon/src/deployment/rollback-preflight.ts",
        "apps/daemon/src/deployment/rollback-preflight.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/rollback-preflight.ts",
        "apps/daemon/src/deployment/rollback-preflight.js"
      ],
      "note": "NEW MODULE apps/daemon/src/deployment/rollback-preflight.ts (152 lines) plus its tracked bridge rollback-preflight.js holding exactly `export * from \"./rollback-preflight.ts\";`, matching every sibling in the directory. Two pure functions, no side effects, no new refusal vocabulary.\n\nselectRollbackDumpDecision(store, projectId, environment, toReceiptRef) -> RollbackDumpSelection. Reads readDeployLedger(...).get(environment); refuses DEPLOY_ROLLBACK_RESTORE_DEPLOY_UNKNOWN when the environment has no state OR when toReceiptRef is not among state.receipts (matched on receiptId, so a receipt belonging to a different environment is not a target this one can return to). Walks state.receipts FORWARD from index+1, calling readMigrationReceipt inside try/catch (it THROWS MIGRATION_RECEIPT_INVALID; caught and answered DEPLOY_ROLLBACK_RESTORE_MIGRATION_UNVERIFIED without relaying the message). Null receipts are SKIPPED - a rollback and a refused-before-migration deploy both record none. Stops at the first non-null: null backupRef refuses DEPLOY_ROLLBACK_RESTORE_BACKUP_ABSENT, never skipping on to a later and therefore LATER-schema dump. Walk exhausted refuses DEPLOY_ROLLBACK_RESTORE_MIGRATION_UNKNOWN, which is also the answer when the target IS the current deploy. Success returns { dumpDecisionId, target } - the successor's decisionId for the dump join, the TARGET receipt for the destination.\n\nCODES RETURNED BARE, as a local four-member union RollbackDumpSelectionCode rather than as a built refusal. All four are already keys of ROLLBACK_RESTORE_DETAILS, so the caller mints the prose. Deliberate: rollback-restore.ts imports this module, so importing its detail table back would be a cycle.\n\nprobeRollbackHost(ports, environment) -> RollbackHostProbe. Takes Pick<DeployPorts,\"docker\"|\"ssh\"|\"target\">. Runs argv [\"version\",\"--format\",\"{{.Server.Version}}\"] locally through ports.docker or remotely through ports.ssh([sshTarget,\"docker\",...args]); an ssh code 255 collapses to null exactly as deploy-service.ts:187 collapses it, a throw is an unreachable host, and any non-zero refuses DEPLOY_DOCKER_UNAVAILABLE at DEPLOY_ENGINE_STAMP. Same code@layer pair deploy-service already answers with, so no code-to-layer map is touched.\n\nDIVERGENCE FROM STEP 2, deliberate and measured in step 1, ALSO recorded as a task comment. A NULL deploy target is a SKIP returning ok, NOT a DEPLOY_TARGET_MISSING refusal. Two reasons. (1) Whether an environment has a bound target is the deploy engine's own fact; answering it here reports a refusal from a layer that was never consulted. (2) rollback-production-wiring.test.ts:87 composes the REAL provider through createStoreDependencies, which binds no target, so productionDeployPorts().target() answers null - a TARGET_MISSING refusal there would replace ENV_ENVIRONMENT_UNKNOWN@SCOPE and destroy that arm's only subject, the host-scoped workspace forwarding that no other arm reaches. The consequence is that DEPLOY_TARGET_MISSING is not imported or mentioned by this module at all, so there is no dead vocabulary entry. The residual it leaves open is written into the probe's own header prose, not hidden: an environment with no bound target still resolves and applies its dump before the engine refuses. DoD item 2 names Docker and only Docker, and Docker is closed.\n\nDetail prose is a fixed module-level constant. deploy-service.ts:267 may relay lastStderrLine because its refusal passes through receipt declassification; this one is minted before any receipt exists, so no docker stderr reaches it (epic rail 3).\n\nHeader carries the dump-selection argument that step 3 removes from rollback-restore.ts: the target/next/current diagram, why the target's OWN backupRef is one step too far, why current is wrong for A->B->C, and that the join is decisionId-keyed and never a filename.\n\nNo tests run this step - the module has no consumer until step 3, and a standalone run would prove loadability rather than composition.",
      "startedAt": "2026-09-11T09:43:30.500Z",
      "completedAt": "2026-09-11T09:45:17.014Z"
    },
    {
      "stepId": "step-3",
      "description": "REWIRE apps/daemon/src/deployment/rollback-restore.ts ONTO THE TARGET. Add `readonly toReceiptRef: string` to `RollbackRestoreConfig`, documented as the ALREADY-ADMITTED target receipt id forwarded from the request - a 64-hex identifier, never a URL and never free text, so it may not be interpolated into any detail. In `resolveRollbackRestore`, replace the `readCurrentDeployReceipt` read with a call to `selectRollbackDumpDecision(config.store, config.projectId, environment, config.toReceiptRef)`; drop the now-unused `readCurrentDeployReceipt` import. KEEP THE BOUNDNESS-FIRST ORDER: `config.credential === undefined` still refuses DEPLOY_ROLLBACK_DATABASE_RESTORE_UNAVAILABLE before anything else, because an unwired daemon is a fact about wiring and not about this environment's history. Then key `destination(config, environment, target.decisionId, target.sha)` on the TARGET receipt and `dumpToRestore(config, selection.dumpDecisionId)` on the successor the selection named. Rewrite the header's WHICH DUMP paragraph to state the rule that now holds - the dump is the FIRST POST-TARGET MIGRATION's pre-migration dump, reached by the durable join through that deploy's decisionId - and point at rollback-preflight.ts for the argument. Do not change any code string, any detail string, or the arity of resolveRollbackRestore/applyResolvedRestore/applyRollbackRestore. Two landed source-text asserters constrain this file: rollback-command.test.ts:146-154 requires `.restoreDatabaseInto(` and forbids `.restoreDatabase(` here, and rollback-restore.test.ts:318-321 forbids any `process.env[.[]` or `payload[.[]` READ - so populate the new field from the handler's already-validated `request.receiptId`, never from a payload index. Keep the file at or under 250 physical lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-restore.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/rollback-restore.ts"
      ],
      "note": "rollback-restore.ts REWIRED ONTO THE TARGET. 245 -> 248 physical lines, under the 250 target; the header's dump-selection argument moved to rollback-preflight.ts, which is what paid for the new config field's documentation.\n\nRollbackRestoreConfig gains `readonly toReceiptRef: string`, documented as the already-admitted target receipt id: 64 hex, matched against a real deploy receipt by the handler before it arrives, an IDENTIFIER and never a URL or free text, and interpolated into no detail.\n\nresolveRollbackRestore now: (1) credential === undefined still refuses DEPLOY_ROLLBACK_DATABASE_RESTORE_UNAVAILABLE FIRST - boundness is a wiring fact, not a fact about this environment's history, and moving it would change the code an unwired daemon answers with; (2) selectRollbackDumpDecision(store, projectId, environment, config.toReceiptRef), refusing `selection.code` through the existing refuse() so the detail and layer come from the unchanged table; (3) destination(config, environment, selection.target.decisionId, selection.target.sha) - THE TARGET, not the current deploy; (4) dumpToRestore(config, selection.dumpDecisionId) - the successor the selection named. The readCurrentDeployReceipt import is gone; nothing else in the file reads the ledger.\n\nARITY AND VOCABULARY UNCHANGED. resolveRollbackRestore, applyResolvedRestore and applyRollbackRestore keep their signatures. No key added to or removed from ROLLBACK_RESTORE_DETAILS, ROLLBACK_RESTORE_STAMP untouched, no code string changed.\n\nFOUR DETAIL VALUES REWORDED, and only values. They said \"the current deploy\" four times and would have been lies under the new rule: DEPLOY_UNKNOWN now reads \"the environment has no deploy history holding the selected receipt...\", MIGRATION_UNKNOWN \"no deploy after the selected one recorded a migration...\", MIGRATION_UNVERIFIED \"the migration record naming the dump to restore could not be verified\", BACKUP_ABSENT \"the migration that moved the schema past the selected deploy recorded no backup to restore\". Checked first that nothing pins the literals: `git grep` for each old phrase over the whole tree returns only this file plus .moe board JSON and one unrelated comment in rollback-restore.test.ts:157. Every asserter references ROLLBACK_RESTORE_DETAILS[code] by symbol, so the reword is invisible to them. Detail text is prose, not the closed code roster DoD item 5 fences.\n\nBOTH SOURCE-TEXT ASSERTERS STILL SATISFIED. rollback-command.test.ts:146-154 wants `.restoreDatabaseInto(` present and `.restoreDatabase(` absent here - applyResolvedRestore is untouched. rollback-restore.test.ts:318-321 forbids any `process.env[.[]` or `payload[.[]` read - the new field is a named struct member populated in step 5 from the handler's already-validated request.receiptId, never from a payload index, and this file still contains neither pattern.\n\nGATE: `npx tsc --noEmit --project apps/daemon/tsconfig.json` EXIT 1 with EXACTLY TWO errors, both the intended ones and both TS2741 \"Property 'toReceiptRef' is missing\": rollback-command.ts(196,55) and rollback-restore.test.ts(168,12). That is the required field doing its job - it reds every composition root that forgot it, which is the property DoD item 5's neighbour depends on. rollback-preflight.ts and its bridge compile clean. Both sites are fixed in steps 4 and 5; no third site exists, so no other caller constructs this config.",
      "startedAt": "2026-09-11T09:45:19.791Z",
      "completedAt": "2026-09-11T09:46:27.640Z"
    },
    {
      "stepId": "step-4",
      "description": "DoD ITEM 1's ARM, in apps/daemon/src/deployment/rollback-restore.test.ts. First make the existing fixture supply the new field: its returned `config` gains `toReceiptRef: deployReceiptId(PROJECT_ID, ENVIRONMENT, KEPT_DECISION)` (import from ./deploy-receipt-contracts.js), which is the receipt a rollback from the current deploy actually targets. That one edit keeps all ~15 existing call sites unchanged, and arm (a) at :184 must STAY GREEN unmodified - it asserts the CURRENT deploy's dump, and under the new rule current IS the kept target's first migrating successor. If arm (a) reds, the selection is wrong, not the arm.\n\nThen add the new arm: drive A -> B -> C through the PRODUCTION WRITERS `recordDeployReceipt` and `recordMigrationReceipt` (the fixture's own seedDeploy/seedMigration helpers), each deploy carrying its own distinct pre-migration dump file, and resolve a rollback whose `toReceiptRef` is A's receipt id. Assert the resolved dump equals the path recorded under B's decision id, READ BACK FROM THE STORE via `readMigrationReceipt(store, PROJECT_ID, B_DECISION)` and split off its backupRef - not a literal repeated from the fixture. Assert it is NOT C's dump path and NOT A's own, and assert the three dump paths are pairwise distinct so the arm can fail. Read the applied path from `ports.calls[0].path` as well as from the return value. Add one sentence saying why B and not A: A's own backupRef is the schema BEFORE A migrated, which is one step too far.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-restore.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/rollback-restore.test.ts",
        "apps/daemon/src/deployment/rollback-restore.ts",
        "apps/daemon/src/deployment/rollback-preflight.ts"
      ],
      "note": "DoD ITEM 1's ARM LANDED. rollback-restore.test.ts 431 -> 503 lines. GATE: `npx vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src/deployment/rollback-restore.test.ts` EXIT 0, `Test Files  1 passed (1)` / `Tests  11 passed (11)` (was 10; +1 is the new arm). `npx tsc --noEmit --project apps/daemon/tsconfig.json` now reports EXACTLY ONE error, rollback-command.ts(196,55), which is step 5's site.\n\nFIXTURE: config gains `toReceiptRef: deployReceiptId(PROJECT_ID, ENVIRONMENT, KEPT_DECISION)`, one edit, all ~15 call sites unchanged. Arm (a) at :184 STAYED GREEN UNMODIFIED, which is the load-bearing result of this step: it asserts the CURRENT deploy's dump and NOT the kept receipt's own, and under the new rule the kept target's first migrating successor IS the current deploy. Same for arms (g) and (h), which pin the same dump through the split halves.\n\nNEW ARM (a2), \"selects the TARGET's successor dump across A -> B -> C, never the current deploy's\". Three deploys driven through the production writers recordDeployReceipt/recordMigrationReceipt via the fixture's own seedDeploy/seedMigration, each with its own dump file and its own sha. Rollback targets A's receipt id. The expected path is READ BACK FROM THE STORE - readMigrationReceipt(store, PROJECT_ID, \"cmd-deploy-b\") and its backupRef split - never a literal repeated from the fixture. Asserts the identifier three ways: result equals dumpB exactly, ports.calls[0].path is dumpB, and it is NOT dumpC (the current deploy, which is what the defect selected) and NOT dumpA (one step too far). `expect(new Set([dumpA,dumpB,dumpC]).size).toBe(3)` proves the fixture can tell them apart, so the arm can fail. The connection is asserted to still come from the credential seam. The header states why B and not A: migrateWithBackup dumps BEFORE it applies, so A's own backupRef is the schema the deploy BEFORE A ran against.\n\nORDERING DEFECT IN STEP 3 FOUND AND FIXED HERE, caught by landed arm (c3). Step 3 as written put the whole selection before destination(), which breaks the PHASE ORDER the file's forwarding arms depend on. Arm (c3) seeds a \"staging\" deploy with no migration and requires ENV_ENVIRONMENT_UNKNOWN@SCOPE - the ENVIRONMENT slice's own refusal. With selection first, the successor walk would notice nothing migrated after the staging target and answer DEPLOY_ROLLBACK_RESTORE_MIGRATION_UNKNOWN@DAEMON_COMMAND_SEAM, reporting this seam as the refusing layer when the environment slice had not even been consulted. That is precisely the failure the file's own header names (\"an arm asserting only 'it refused' stays green when the wrong one starts answering\").\n\nFIX, in both files, preserving the pre-fix order exactly (find the deploy, resolve the destination, then judge the dump evidence): RollbackDumpSelection now carries `target` on evidence refusals too, as a three-arm union where `target: null` occurs for exactly one code, DEPLOY_ROLLBACK_RESTORE_DEPLOY_UNKNOWN. resolveRollbackRestore reads `if (selection.target === null) return refuse(selection.code);` -> destination(target.decisionId, target.sha) -> `if (!selection.ok) return refuse(selection.code);` -> dumpToRestore. One ledger read, not two. Reasoning is comment-pinned at both ends.\n\nArm (c3) also needed its TARGET repointed: the fixture's default names a PRODUCTION receipt, and a receipt belonging to another environment is not a target \"staging\" can return to, so the selection would refuse DEPLOY_UNKNOWN first and the arm would stop measuring forwarding. It now spreads `toReceiptRef: deployReceiptId(PROJECT_ID, \"staging\", \"cmd-deploy-staging\")` into that one call. Subject preserved exactly; this was flagged in the step-1 note and the task comment before any code was written.\n\nrollback-restore.ts is 249 physical lines after trimming the new field's doc block and merging two comment paragraphs - under the 250 target. rollback-preflight.ts is 168.",
      "startedAt": "2026-09-11T09:46:35.101Z",
      "completedAt": "2026-09-11T09:48:39.693Z"
    },
    {
      "stepId": "step-5",
      "description": "REWIRE apps/daemon/src/deployment/rollback-command.ts: HOST FIRST, THEN THE RESTORE. Inside the existing `if (request.restore && intent === null) {` block at ~:195, BEFORE the `resolveRollbackRestore` call, call `probeRollbackHost` with the same ports object the handler will later hand the deploy service (`options.ports ?? productionDeployPorts(store, projectId)` - hoist that expression to a const so the precheck and the deploy service at :304-312 cannot diverge onto different docker ports) and, when it refuses, `throw new DomainRefusal(code, layer, detail, 422)` with the probe's own code and layer unchanged. Then pass `toReceiptRef: request.receiptId` in the config literal handed to `resolveRollbackRestore`.\n\nGATE IT ON THE EXACT EXISTING CONDITION and nothing wider: a non-restore rollback and every recovery path (`intent !== null`) must stay byte-identical, so rollback-command.test.ts's docker arms at :348-350 and :467-474 - which use restoreDatabase:false and depend on the REFUSED deploy receipt the deploy service records - keep passing unchanged. The precheck deliberately records NO deploy receipt: nothing has been reserved at that line, so there is no effect to account for.\n\nUpdate the block comment at :172-193 to say that the host probe now runs first and why: a dump applied against a host that cannot start the target image leaves the database reverted under the still-running current application. Keep the file under 400 physical lines and do not introduce a call to `applyRollbackRestore` - rollback-command.test.ts:144 asserts its absence here.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-command.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/rollback-command.ts"
      ],
      "note": "rollback-command.ts REWIRED: HOST FIRST, THEN THE RESTORE. 341 -> 368 physical lines, under the 400 cap. `grep -c applyRollbackRestore` returns 0, so rollback-command.test.ts:144's absence assertion still holds.\n\nTHE PROBE. Inside the existing `if (request.restore && intent === null) {`, before resolveRollbackRestore: `const host = await probeRollbackHost(deployPorts, request.environment); if (!host.ok) throw new DomainRefusal(host.code, host.layer, host.detail, 422);` - the probe's own code and layer forwarded unchanged, no restamping. Then `toReceiptRef: request.receiptId` in the config literal, taken from the already-validated request struct and never from a payload index, so rollback-restore.test.ts:318-321's `payload[.[]` ban is unaffected.\n\nPORTS HOISTED, as step 5 requires: `const deployPorts = options.ports ?? productionDeployPorts(store, projectId)` at handler construction, used by BOTH the probe and the deploy service composition at :304-312 (now `...deployPorts`). The probe and the rollback cannot drift onto different docker daemons. `??` still short-circuits, so productionDeployPorts is never constructed when ports are injected, and it closes over nothing per-request, so hoisting it out of the request path changes no behaviour.\n\nONE NARROWING BEYOND THE STEP TEXT, measured in step 1 and already on the task as a comment: the probe is skipped when `options.environmentCredential === undefined`. An unwired daemon has no bound destination for ANY environment, so the restore refuses UNAVAILABLE whatever the host answers; probing it asks a question nobody's answer depends on. Reason is comment-pinned at the call site.\n\nBLOCK COMMENT UPDATED to lead with the host probe and why: a dump applied against a host that cannot then start the target image leaves the database reverted underneath the still-running CURRENT application - the two ends of the deployment at different versions, silently. It also records that the probe is read-only and so preserves the block's cost-nothing property.\n\nGATE: `npx tsc --noEmit --project apps/daemon/tsconfig.json` CLEAN - zero errors, the last TS2741 gone.\n\nFOUR ROLLBACK FILES RUN: `Test Files  3 failed | 1 passed (4)`, `Tests  5 failed | 41 passed (46)`. ALL FIVE REDS ARE THE SAME PREDICTED CLASS and all five are the identical message:\n  AssertionError: expected [ [ 'version', '--format', …(1) ] ] to deeply equal []\nat rollback-command-ordering.test.ts (i) and (iii), rollback-command.test.ts (c) and (d), rollback-production-wiring.test.ts \"forwards the environment credential and the workspace\". That is the read-only probe appearing in docker.calls, which is the exact set step 1 predicted. Step 6 retightens all five to \"only the probe argv, and nothing that moves bytes\" - none is deleted.\n\nBOTH NARROWINGS CONFIRMED BY MEASUREMENT, not by argument:\n - rollback-command.test.ts:92 (it.each \"restore\" row) and :171 (unwired-daemon restore) are GREEN and unmodified. Without the credential gate both would be in the red list.\n - rollback-production-wiring.test.ts \"routes a requested restore through the real credential seam, not a blanket refusal\" is GREEN and unmodified, still asserting ENV_ENVIRONMENT_UNKNOWN@SCOPE. Without the null-target skip it would have refused DEPLOY_TARGET_MISSING and lost its only subject.\n - rollback-restore.test.ts is the 1 passed file: 11/11, untouched by the command-side change.\n\nNOT WIDENED: the gate is the pre-existing `request.restore && intent === null` plus the credential narrowing. Every non-restore rollback and every recovery path (`intent !== null`) is byte-identical, which is why rollback-command.test.ts's restore:false docker arms at :348-350 and :467-474 are not in the red list. The probe records no deploy receipt: nothing is reserved at that line, so there is no effect to account for.",
      "startedAt": "2026-09-11T09:48:42.162Z",
      "completedAt": "2026-09-11T09:49:51.433Z"
    },
    {
      "stepId": "step-6",
      "description": "DoD ITEM 2's ARM plus the three assertions the precheck moves, across apps/daemon/src/deployment/rollback-command-ordering.test.ts and apps/daemon/src/deployment/rollback-production-wiring.test.ts.\n\nNEW ARM in the ordering file, matching that file's stated standard of asserting the CODE, the LAYER and the port's call count: build a bound harness whose docker double answers non-zero to `version`, and whose `backupPorts.restoreDatabaseInto` THROWS if it is ever called. Send restoreDatabase:true. Assert the rejection is `{ code: \"DEPLOY_DOCKER_UNAVAILABLE\", layer: \"DAEMON_DEPLOY_ENGINE\" }`. Prove the restore was not REACHED, not merely that it did not succeed: the throwing port records nothing, no `EnvironmentRollbackRestoreApplied` event exists on the request stream, no restore-marker decision exists under principal `daemon:rollback-restore`, and no deploy receipt exists for the command id. A `docker version` probe in `docker.calls` is expected and is the positive control that the precheck actually ran.\n\nRETIGHTEN, DO NOT DELETE, the three landed emptiness assertions that the precheck makes false on restoreDatabase:true paths - rollback-command-ordering.test.ts:176 (arm i, stale expectedVersion), rollback-command-ordering.test.ts:237 (arm iii, post-admission apply failure) and rollback-production-wiring.test.ts:143. Each becomes: docker.calls contains ONLY the read-only probe argv `[\"version\",\"--format\",\"{{.Server.Version}}\"]`, and sshCalls plus any build/create/start/cp argv remain empty. Replacing an emptiness assertion with nothing is how this file would stop testing its subject; the point of each arm - that a refused rollback moved no bytes - must still be asserted.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-command-ordering.test.ts",
        "apps/daemon/src/deployment/rollback-production-wiring.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/rollback-command-ordering.test.ts",
        "apps/daemon/src/deployment/rollback-command.test.ts",
        "apps/daemon/src/deployment/rollback-production-wiring.test.ts"
      ],
      "note": "DoD ITEM 2's ARM LANDED plus FIVE retightenings, not three. GATE: all four rollback files, `Test Files  4 passed (4)` / `Tests  47 passed (47)`, EXIT 0.\n\nNEW ARM (vi) in rollback-command-ordering.test.ts, matching that file's stated standard of code + layer + call count: \"an UNUSABLE HOST refuses DOCKER_UNAVAILABLE before the restore is resolved or reached\". A bound harness whose ports.docker answers `{ code: null, stderr: \"docker: not found\" }` - the shape a missing docker binary produces, modelled on the double's own dockerUnavailable arm - and whose backupPorts.restoreDatabaseInto RECORDS and then THROWS. Rejection asserted as { code: \"DEPLOY_DOCKER_UNAVAILABLE\", layer: \"DAEMON_DEPLOY_ENGINE\" }, literals not constants so a silent restamp of DEPLOY_ENGINE_STAMP still reds.\n\nNOT-REACHED IS PROVEN, NOT INFERRED, four ways. (1) The throwing port records before it throws, so `expect(reached).toEqual([])` is an observation rather than an inference from an absent error - and the throw makes a regression LOUD, because if the probe ever stopped running first the apply would be reached and the rejection would become DEPLOY_ROLLBACK_RESTORE_FAILED, which the code assertion catches. (2) No EnvironmentRollbackRestoreApplied event on the request stream. (3) No decision under principal daemon:rollback-restore. (4) No deploy receipt for the command id. POSITIVE CONTROL: `expect(probed).toEqual([[...HOST_PROBE]])` - the precheck really executed and ran exactly the read-only probe and nothing else. Without it every absence above would be satisfied for free by an arm where nothing ran.\n\nEPIC RAIL 3 ARM, with its own positive control. The refusal detail is caught and asserted to CONTAIN \"did not answer a version probe\" (proving a real string detail arrived) and NOT to contain \"not found\" (the exact stderr the fake emitted). Control first, because \"the detail does not contain the stderr\" is equally true of an empty detail, a missing key, or a refusal that never happened. First draft used `expect.not.stringContaining` inside rejects.toMatchObject with no control; replaced before completing the step.\n\nRETIGHTENED, NONE DELETED. A shared `expectNoDockerEffect(docker, probes)` helper plus a HOST_PROBE constant, added to rollback-command-ordering.test.ts and rollback-command.test.ts. It asserts docker.calls EXACTLY EQUALS that many probe argvs and sshCalls is empty - stronger than the old emptiness in one respect, since a build, create, start, cp, reload or an unexplained second probe all still red. Sites:\n - rollback-command-ordering.test.ts:176 (arm i, stale expectedVersion) -> 1 probe.\n - rollback-command-ordering.test.ts:237 (arm iii, post-admission apply failure) -> 1 probe.\n - rollback-production-wiring.test.ts:143 -> inline exact-argv equality plus an sshCalls emptiness assertion it did not previously have.\n - rollback-command.test.ts:276 (arm c) -> 1 probe. NOT NAMED IN THE PLAN.\n - rollback-command.test.ts:297 and :314 (arm d) -> 1 probe and then 2. NOT NAMED IN THE PLAN.\nThe 2-probe count at :314 is itself a measured fact worth pinning: the fresh command id ran its own precheck and the REPLAY between them answered from the refused-terminal record WITHOUT probing, which is the recovery path staying byte-identical.\n\nThe two extra files were predicted in the step-1 note and the task comment before any code was written; they are the same mechanical class step 6 prescribes, and taking them was the smaller move than leaving the row half-delivered. rollback-command.test.ts:92 and :171, and rollback-production-wiring.test.ts:87, remain byte-identical - the two narrowings from step 5 are what kept them so.",
      "startedAt": "2026-09-11T09:50:28.281Z",
      "completedAt": "2026-09-11T09:52:14.565Z"
    },
    {
      "stepId": "step-7",
      "description": "DoD ITEM 3: PROVE BOTH NEW ARMS NON-VACUOUS BY MUTATION, and quote both reds verbatim in the step note. Two drills, one per arm: (1) key the dump selection back on the environment's CURRENT deploy decisionId and confirm the A->B->C arm reds naming C's path where B's was expected; (2) move the `probeRollbackHost` call back below `resolveRollbackRestore` and the apply, and confirm the docker-ordering arm reds because the throwing restore port was reached.\n\nRESTORE PROOF, on a shared checkout and with rollback-preflight.ts UNTRACKED for part of this row: BEFORE opening either window, `git status --porcelain` the exact paths you will mutate, copy each OUT OF TREE to %TEMP% under a name carrying THIS TASK ID (task-705977238f794b2cadb5c1dcdb98d6bd) and not only your seat id, and record each file's SHA256 taken from the LIVE file at window-open. Restore by copying the backup back, then prove it in a SEPARATE call by SHA256 equality against that pre-mutation hash. `git diff` is blind to untracked paths and is pre-flight evidence only, never the restore proof. Run only the two owning test files during a drill, never a full suite, and never mutate a file while a leased full-suite run is reading it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-preflight.ts",
        "apps/daemon/src/deployment/rollback-command.ts"
      ],
      "note": "DoD ITEM 3 DISCHARGED. Both new arms proven non-vacuous by mutation; both reds quoted verbatim.\n\nPRE-FLIGHT CENSUS, before either window opened. `git status --porcelain` on the exact paths: \" M apps/daemon/src/deployment/rollback-command.ts\", \" M apps/daemon/src/deployment/rollback-restore.ts\", \"?? apps/daemon/src/deployment/rollback-preflight.ts\". The third is UNTRACKED, so `git diff` is blind to it and is pre-flight evidence only here. Each copied OUT OF TREE to %TEMP%\\\\moe-task-705977238f794b2cadb5c1dcdb98d6bd-worker-1b2cd09b-<leaf>, carrying THIS TASK ID and not only the seat id, and each hash taken from the LIVE file at window-open:\n  rollback-restore.ts   FE6764BA5CFCCA5E3D4991586FECED53EEE224F66467C0D6AF0BCFDE4D8163D5\n  rollback-command.ts   A9367BC5371CDFC49CA8269AA25399E3E102B7D36FF60C86B486D4DF86B0AD20\n  rollback-preflight.ts F99DC0CDC37FBC3A004323E7E217B8CFA0AE20663C4A2015A369D369BD658CFD\nNo full-suite lease was held or requested during either window; only the single owning test file ran per drill.\n\nDRILL 1 - KEY THE DUMP SELECTION BACK ON THE CURRENT DEPLOY. In rollback-preflight.ts the forward walk `for (const successor of state.receipts.slice(index + 1))` became `for (const successor of [...state.receipts].reverse())`, which for A -> B -> C returns the LAST migrating receipt, i.e. exactly the environment's current deploy, reproducing the pre-fix answer. Ran src/deployment/rollback-restore.test.ts only. RED, verbatim:\n\n FAIL  src/deployment/rollback-restore.test.ts > applyRollbackRestore > (a2) selects the TARGET's successor dump across A -> B -> C, never the current deploy's\n AssertionError: expected { ...(2) } to deeply equal { ...(2) }\n - Expected\n + Received\n -   \"dump\": \"C:\\\\Users\\\\Yaron\\\\AppData\\\\Local\\\\Temp\\\\moe-rollback-restore-TkbWbN\\\\b.sql\",\n +   \"dump\": \"C:\\\\Users\\\\Yaron\\\\AppData\\\\Local\\\\Temp\\\\moe-rollback-restore-TkbWbN\\\\c.sql\",\n Test Files  1 failed (1) / Tests  3 failed | 8 passed (11)\n\nC's path where B's was expected - the defect named by its identifier, which is what DoD item 1 asks for. THE DIAGNOSTIC RESULT OF THIS DRILL: landed arm (a) stayed GREEN under the mutant. Arm (a) has only two deploys, so kept-successor and current are the same row and it cannot tell the two rules apart. (a2) is the arm that catches this defect class, and (a)'s survival is the proof that it was needed. Arms (c) and (g) also redded, which is consistent - the reversed walk finds a migration where those arms seed none.\n\nDRILL 2 - MOVE THE PROBE BACK BELOW resolveRollbackRestore AND THE APPLY. In rollback-command.ts the pre-admission `probeRollbackHost` block was removed, leaving the deploy service's own `docker version` check at :304 as the only reachability test - which is literally below the resolve at :196 and below the apply at :243, i.e. the pre-fix ordering. Ran src/deployment/rollback-command-ordering.test.ts only. RED, verbatim:\n\n FAIL  src/deployment/rollback-command-ordering.test.ts > (vi) an UNUSABLE HOST refuses DOCKER_UNAVAILABLE before the restore is resolved or reached\n AssertionError: expected Error: DEPLOY_ROLLBACK_RESTORE_FAILED: ap... { ...(4) } to match object { ...(2) }\n - Expected\n + Received\n -   \"code\": \"DEPLOY_DOCKER_UNAVAILABLE\",\n -   \"layer\": \"DAEMON_DEPLOY_ENGINE\",\n +   \"code\": \"DEPLOY_ROLLBACK_RESTORE_FAILED\",\n +   \"layer\": \"DAEMON_COMMAND_SEAM\",\n Test Files  1 failed (1) / Tests  3 failed | 3 passed (6)\n\nDEPLOY_ROLLBACK_RESTORE_FAILED is the code the THROWING restore port produces, so the red is direct evidence the apply was REACHED under the old ordering - the arm is measuring reach, not merely outcome. The two retightened arms redded too, with \"expected [] to deeply equal [ [ 'version', '--format', ...(1) ] ]\" at (i) and (iii): they are live probes of the precheck's presence, not slack assertions that would pass either way.\n\nRESTORE, issued as copies from the out-of-tree backups, and PROVEN IN A SEPARATE CALL by SHA256 equality against the window-open LIVE hashes above:\n  rollback-command.ts   live FE...no - A9367BC5371CDFC49CA8269AA25399E3E102B7D36FF60C86B486D4DF86B0AD20  RESTORED=True\n  rollback-restore.ts   live FE6764BA5CFCCA5E3D4991586FECED53EEE224F66467C0D6AF0BCFDE4D8163D5  RESTORED=True\n  rollback-preflight.ts live F99DC0CDC37FBC3A004323E7E217B8CFA0AE20663C4A2015A369D369BD658CFD  RESTORED=True\nAll three match. A separate `Select-String -Pattern \"DRILL MUTANT\"` over apps/daemon/src/deployment/*.ts returns NOTHING, so no mutant token survives anywhere in the slice - the belt to the hash's braces, and the check that matters most for the untracked module, where an empty `git diff` would have certified nothing.",
      "startedAt": "2026-09-11T09:52:17.432Z",
      "completedAt": "2026-09-11T09:54:00.918Z"
    },
    {
      "stepId": "step-8",
      "description": "VERIFY AND ADVERSARIALLY REVIEW. Run `pnpm --filter @moe/daemon test` and quote the `Test Files N passed (N)` count line - an exit code with no count line is not evidence, and this package is apps/daemon so a wrong filter would print 'No projects matched the filters' and still exit 0. Also run `pnpm --filter @moe/daemon typecheck` (or the repo's typecheck leg) because the new required config field must red any composition root that forgot it. Attribute any red by the merge-base baseline from step 1: report foreign red verbatim with its failing paths, and fix anything your own diff introduced.\n\nDoD ITEM 5, checked explicitly: `git diff` must show NO addition to DEPLOY_REFUSAL_CODES, no new key in ROLLBACK_RESTORE_DETAILS, and no change to DEPLOY_ENGINE_STAMP - the precheck reuses DEPLOY_TARGET_MISSING and DEPLOY_DOCKER_UNAVAILABLE at their existing layer, so no map test needs updating. If that is not true, the change went wrong.\n\nAdversarial pass before complete_task: what does the successor walk do when the target IS the current deploy (no successors), when the ledger holds two rollback receipts between target and current, and when a refused deploy migrated and then failed to start? Confirm each answers a refusal or the right dump and never a silently later schema. Confirm the precheck runs no second time on the recovery path and that its detail carries no docker stderr. Confirm no credential reaches a durable surface by re-running the existing secret sweeps. Finally, post ONE task comment recording that concurrency exclusion of simultaneous rollbacks remains OPEN and out of scope, pointing at the existing THE RESIDUAL prose at rollback-command.ts:~234-240 - a comment, not a new board row.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/deployment/rollback-restore.ts",
        "apps/daemon/src/deployment/rollback-command.ts",
        "apps/daemon/src/deployment/rollback-preflight.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/deployment/rollback-preflight.ts",
        "apps/daemon/src/deployment/rollback-preflight.js",
        "apps/daemon/src/deployment/rollback-restore.ts",
        "apps/daemon/src/deployment/rollback-command.ts",
        "apps/daemon/src/deployment/rollback-restore.test.ts",
        "apps/daemon/src/deployment/rollback-command-ordering.test.ts",
        "apps/daemon/src/deployment/rollback-command.test.ts",
        "apps/daemon/src/deployment/rollback-production-wiring.test.ts"
      ],
      "note": "VERIFIED AND ADVERSARIALLY REVIEWED. Both gates EXIT 0 at the delivered tree.\n\n`pnpm --filter @moe/daemon test` EXIT 0:\n  Test Files  612 passed (612)\n  Tests  11056 passed | 21 skipped (11077)\n  Duration  305.84s\nA real count line, not a vacuous filter: @moe/daemon is apps/daemon, and a wrong name would have printed \"No projects matched the filters\". Against the step-1 baseline of 11030 passed | 21 skipped (11051), the delta is +26 tests, of which 2 are mine - (a2) in rollback-restore.test.ts and (vi) in rollback-command-ordering.test.ts - and 24 arrived with a peer's in-flight work.\n\n`npx tsc --noEmit --project apps/daemon/tsconfig.json` EXIT 0, zero errors.\n\nFOREIGN RED SEEN AND RESOLVED, disclosed rather than hidden. The tree moved under me between step 1 and step 8: another session landed edits to apps/daemon/src/{mcp-http/mcp-http-node-bridge,orchestrator/node-lander,orchestrator/repository-delivery-runtime,preview/preview-auto-composition,preview/preview-auto-decision,release/release-auto-decide}, untracked apps/daemon/src/bootstrap/effective-auto-policy.{ts,js}, and packages/mcp/src/http/{http-server,http-session}.ts plus four untracked files there. Two reds came from it, both measured and both outside my owned paths:\n 1. TYPECHECK, first run: \"packages/mcp/src/http/http-session.ts(254,7): error TS2367: This comparison appears to be unintentional because the types 'false | undefined' and 'true' have no overlap.\" `git diff` shows that line is inside a block the peer ADDED (an optional `signal?: AbortSignal` and a post-await `signal?.aborted === true` re-check). My diff touches no file under packages/. GONE on re-measure - the peer fixed it mid-session.\n 2. DAEMON SUITE, first run: 1 failed | 611 passed (612), at src/release/release-auto-decide.test.ts > \"selects the same ref after the store is closed and REOPENED from disk\" - a test of `selectEffectiveAutoApprovalPolicy`, which lives in the peer's brand-new UNTRACKED effective-auto-policy.ts. Re-measured before attributing rather than assuming: run ALONE it is `Test Files  1 passed (1)` / `Tests  58 passed (58)`, and the 58-vs-55 count difference between the isolated run and the full run is the peer editing the file while the suite read it. Gone on the full re-run.\nPATH-ATTRIBUTED BASELINE: merge-base failures none (step 1, 612/612). HEAD failures at the first pass, the two above. Their failing-path set intersected with this row's owned paths - rollback-preflight.{ts,js}, rollback-restore.ts, rollback-command.ts and the four rollback test files - is EMPTY. Owned-slice leg run separately as a blast-radius superset: `npx vitest run src/deployment` EXIT 0, `Test Files  25 passed (25)` / `Tests  346 passed | 3 skipped (349)`.\n\nDoD ITEM 5, checked by diff not by memory. `git diff -- apps/daemon/src/deployment/deploy-receipt-contracts.ts` is EMPTY, so DEPLOY_REFUSAL_CODES and DEPLOY_ENGINE_STAMP are untouched and no code-to-layer map test needs updating. `git diff -U0` on rollback-restore.ts shows no line starting with a added or removed `DEPLOY_ROLLBACK_*` KEY, so ROLLBACK_RESTORE_DETAILS gained and lost nothing. Four detail VALUES were reworded, shown verbatim in the step-3 note; prose is not the closed code roster, no asserter pins the literals, and every asserter reads the table by symbol. rollback-preflight.ts imports DEPLOY_DOCKER_UNAVAILABLE and DEPLOY_ENGINE_STAMP and mints neither.\n\nADVERSARIAL PASS: all four successor-walk cases step 8 names, plus the recovery path, the stderr-free detail, the secret sweeps and one noted-but-unchanged exposure (an unguarded ports.target call, identical to deploy-service.ts:260's own). Full trace is in the task comment posted this step, together with the concurrency-exclusion residual pointing at the existing THE RESIDUAL prose in rollback-command.ts - a comment, not a new board row.\n\nSHARED CHECKOUT: no peer path reverted, stashed, staged or committed. The untracked effective-auto-policy.{ts,js} and the four untracked packages/mcp files are NOT mine and are not declared.",
      "startedAt": "2026-09-11T09:54:05.658Z",
      "completedAt": "2026-09-11T10:08:08.592Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 157,
  "comments": [
    {
      "id": "comment-02b66e8af6074b04bd2887c0ef9b4f06",
      "author": "architect-1c0fef54",
      "content": "PLANNED. Architect note for QA: THE PLAN DELIBERATELY DOES NOT FOLLOW DoD CLAUSE 1's PARENTHETICAL LITERALLY, and here is the measurement behind that.\n\n`migrateWithBackup` dumps BEFORE it applies (rollback-restore.ts header, lines 22-34), so receipt R's `backupRef` is the schema as it stood BEFORE R's own migration. For A -> B -> C:\n  A.backupRef = the state before A migrated  (ONE STEP TOO FAR - what the deploy before A ran against)\n  B.backupRef = the state A ran against      (what a rollback to A wants)\n  C.backupRef = the state B ran against      (the bug: what production selects today)\n\nSo the correct key is the target's FIRST MIGRATING SUCCESSOR, not the target's own decision id. Clause 1 says \"the resolved dump is A's backup - by its decision id\"; taken literally that resolves A.backupRef, which is the data-destroying answer the landed arm rollback-restore.test.ts:184 (\"(a) applies the CURRENT deploy's pre-migration dump, never the kept receipt's own\") exists to rule out. It would red that arm plus the four arms in rollback-command-ordering.test.ts.\n\nSuccessor-of-target is a strict GENERALISATION of today's behaviour: when the target is the immediate predecessor of current, successor == current, so every landed arm stays green and only the multi-step case changes. It still satisfies clause 1's substance (the dump is A's schema state, asserted by decision id read back from the store, and is NOT C's) and it still reds under clause 3's prescribed mutation back to `current.decisionId`, because with A -> B -> C the current is C and the successor is B.\n\nDoD CLAUSE 5 IS SATISFIED BY ADDING NOTHING. The new Docker precheck reuses DEPLOY_TARGET_MISSING and DEPLOY_DOCKER_UNAVAILABLE, already in the closed roster DEPLOY_REFUSAL_CODES (deploy-receipt-contracts.ts:41-44) paired with DEPLOY_ENGINE_STAMP = DAEMON_DEPLOY_ENGINE. Same code@layer the deploy service answers with, so no map and no map test is edited. Step 8 checks that by diff.\n\nSIDE EFFECT QA SHOULD EXPECT, not a weakening: the precheck spawns one read-only `docker version` on restoreDatabase:true requests that later refuse. That makes three landed `expect(docker.calls).toEqual([])` assertions false - rollback-command-ordering.test.ts:176 and :237, rollback-production-wiring.test.ts:143. The plan RETIGHTENS them to \"only the version probe argv, no lifecycle argv, no sshCalls\" rather than deleting them. A deletion there would be a real regression; a tightening is not.\n\nSCOPE FENCE HONOURED: concurrency exclusion of simultaneous rollbacks is NOT in this plan and no successor row is filed, because this plan opens no fence and changes no guard parity. The residual is already stated in production prose at rollback-command.ts:234-240.",
      "timestamp": "2026-09-11T09:32:59.182Z"
    },
    {
      "id": "comment-d9f27e34c18e4ed19cedca588b2976ca",
      "author": "worker-1b2cd09b",
      "content": "WORKER step-1 measurement: PLAN ENUMERATION WAS INCOMPLETE. Recorded here so QA and the architect can grade the two narrowings I took rather than discovering them in the diff.\n\nplanningNotes says \"FOUR ASSERTING CONSUMERS OF docker.calls EMPTINESS on restoreDatabase:true paths ... rollback-command-ordering.test.ts:176, :237, rollback-production-wiring.test.ts:143\" and \"rollback-command.test.ts's own docker arms (:348-350, :467-474) use restoreDatabase:false (harness payload at :47) and are NOT affected\". The second half is false. `git grep -n 'docker.calls' -- apps/daemon/src/deployment` returns 80 lines over 8 files, and these restore:true arms in rollback-command.test.ts also assert emptiness:\n - :92, inside `it.each(...)(\"refuses %s before Docker effects\")`. The row [\"restore\",\"DEPLOY_ROLLBACK_DATABASE_RESTORE_UNAVAILABLE\",DAEMON_COMMAND_SEAM] reaches `if (mutation === \"restore\") payload[\"restoreDatabase\"] = true;` at :86. The base payload at :47 is false, but this row overwrites it.\n - :171, \"refuses a requested restore on an UNWIRED daemon\", envelope spread sets restoreDatabase:true at :165.\n - :276 (arm c, boundHarness({bind:false})), :297 and :314 (arm d, boundHarness({fail:true})). Both harnesses supply environmentCredential and a non-null target port.\n\nWorse, and not a retightening: rollback-production-wiring.test.ts:87 (arm 2, \"routes a requested restore through the real credential seam, not a blanket refusal\") asserts ENV_ENVIRONMENT_UNKNOWN@SCOPE. It composes the REAL provider via createStoreDependencies, which has no bound deploy target, so productionDeployPorts().target(\"staging\") answers null (deploy-command.ts:113-124). A precheck written literally to step 2 would refuse DEPLOY_TARGET_MISSING first and that arm's stated discriminator - \"can only be reached by a seam that was handed a REAL environment credential and a workspace and actually performed a delivery read\" - would be gone, not merely re-pointed. That coverage exists nowhere else: arm 3 uses createAsyncCommandEntries and cannot pin the host-scoped DEPLOY_BUILD_CONTEXT_ENV_KEY path.\n\nTWO NARROWINGS TAKEN. Both are narrower than step 5's \"gate on the exact existing condition\", never wider, and both leave DoD item 2 fully satisfied because it names Docker and only Docker:\n 1. The host probe is skipped when options.environmentCredential === undefined. This is the production module's own documented BOUNDNESS-ANSWERS-FIRST rule (rollback-restore.ts:195-197) applied one seam earlier: an unwired daemon has no destination for any environment, so probing its docker host answers a question nobody asked. rollback-command.test.ts:92 and :171 stay byte-identical.\n 2. A null deploy target is a no-op skip inside the probe, NOT a DEPLOY_TARGET_MISSING refusal. Refusing there would report a refusal minted by a layer that did not answer, and would cost arm 2 above. rollback-production-wiring.test.ts:87 stays byte-identical.\n\nRESIDUAL, disclosed not hidden: narrowing 2 leaves one ordering hole open. A bound environment whose deploy target binding is absent still resolves and applies the dump, and only then refuses DEPLOY_TARGET_MISSING at the deploy service - the same reverted-database-under-a-running-application shape as defect 2. In practice an environment holding a DEPLOYED receipt has had a target bound, so this is reachable mainly by unbinding. Not fixed here; DoD item 2 does not name it and closing it costs a landed arm its subject. Filed as this comment per the board's comment-beats-a-row bar.\n\nAlso measured and NOT in the plan: rollback-restore.test.ts arm (c3) at :291 reds under the new target-keyed selection. It asks about \"staging\" while the fixture config's toReceiptRef names production's KEPT receipt, so the target is not among staging's receipts and the walk refuses DEPLOY_ROLLBACK_RESTORE_DEPLOY_UNKNOWN rather than forwarding ENV_ENVIRONMENT_UNKNOWN@SCOPE. Repaired inside step 4 by spreading staging's own receipt id into that one call; the arm's subject is preserved exactly. That file is step 4's owned path.\n\nBaseline for attribution, fresh: `pnpm --filter @moe/daemon test` exit 0, Test Files 612 passed (612), Tests 11030 passed | 21 skipped (11051).",
      "timestamp": "2026-09-11T09:43:26.135Z"
    },
    {
      "id": "comment-cb2dbb7824024c6eb301d5a974906ad6",
      "author": "worker-1b2cd09b",
      "content": "WORKER step-8, RESIDUALS LEFT OPEN. Recorded as ONE comment rather than board rows, per the filing bar and the scope fence in this row's own description.\n\n1. CONCURRENCY EXCLUSION OF SIMULTANEOUS ROLLBACKS REMAINS OPEN AND OUT OF SCOPE. This row fixed target-keying and ordering only; it adds no fence and changes no guard parity. The residual is already stated in production prose at rollback-command.ts, in the block comment on THE DATABASE ARM'S APPLY HALF: \"THE RESIDUAL, stated rather than hidden: psql applies the dump in one transaction, so an apply that returns has committed. If a LATER phase then refuses, the terminal releases the guard and a fresh command may resolve and apply the same dump again. That is content-idempotent for the schema, but any write that landed between the two applies is destroyed. Narrowing that needs the deploy phase inside the same fence, which is a different change from this one.\" Nothing in this diff makes it better or worse. Landed arm (ii) in rollback-command-ordering.test.ts already says in its own prose which half of the race it pins (the guard-parity READ) and which it does not (two readers both passing parity, settled by the admission commit).\n\n2. A SECOND, NARROWER RESIDUAL THIS ROW CREATED BY CHOICE, disclosed in step 1 and again in the probe's own header. probeRollbackHost treats a NULL deploy target as a no-op skip rather than a DEPLOY_TARGET_MISSING refusal, so an environment whose target binding is absent still resolves and applies its dump before the deploy engine refuses TARGET_MISSING - the same reverted-database-under-a-running-application shape defect 2 describes, reached only by unbinding a target from an environment that already holds a DEPLOYED receipt. The alternative cost rollback-production-wiring.test.ts:87 its only subject and would have reported a refusal from a layer that was never consulted. DoD item 2 names Docker and only Docker, and Docker is closed.\n\nADVERSARIAL PASS, the four cases step 8 names, each traced against the landed selection:\n - TARGET IS THE CURRENT DEPLOY, no successors: the walk exhausts and refuses DEPLOY_ROLLBACK_RESTORE_MIGRATION_UNKNOWN. Nothing has moved the schema since the target, so no dump identifies its state. Never a later schema. Comment-pinned at the loop's exit.\n - TWO ROLLBACK RECEIPTS BETWEEN TARGET AND CURRENT: rollbacks record no migration receipt, so both are skipped and the walk stops at the next real migrating deploy. The rule is sound in general, not just here: it answers \"the schema as it stood immediately before the first post-target schema move\", and a run of non-migrating deploys does not move the schema, so that value is still the target's own. This also makes targeting a ROLLBACK receipt correct - whether or not that rollback restored, the next migration's backupRef is the state in force during its tenure.\n - REFUSED DEPLOY THAT MIGRATED THEN FAILED TO START: it owns a migration receipt with a backupRef and readDeployLedger keeps refused receipts, so the walk stops there and returns its dump - the correct answer, since that dump is the schema the target ran against.\n - FIRST MIGRATING SUCCESSOR WITH A NULL backupRef: refuses BACKUP_ABSENT and never skips on to a later dump, which would be a LATER schema and would destroy strictly more than the rollback asked for.\n - PRECHECK ON THE RECOVERY PATH: gated on the pre-existing `intent === null`, so recovery never probes. Measured, not argued: rollback-command.test.ts arm (d) records 2 probes across 3 handler calls, the middle one being the replay that answered from the refused-terminal record.\n - DETAIL CARRIES NO DOCKER STDERR: arm (vi) asserts it, with a positive control that the detail is a real string containing the module's own fixed prose.\n - SECRET SWEEPS: the three landed credential sweeps (rollback-command-ordering.test.ts arms iii and v over every stream and decision; rollback-restore.test.ts arm e over eight surfaces plus the whole detail table) all pass unchanged. The new field toReceiptRef is a 64-hex identifier, interpolated into no detail.\n - ONE EXPOSURE NOTED, NOT CHANGED: probeRollbackHost calls ports.target(environment) unguarded, so a throwing target port would escape the handler. That is byte-for-byte the same exposure deploy-service.ts:260 already has, and productionDeployPorts.target has its own try/catch returning null. Matching the existing seam beat inventing a second discipline for it.",
      "timestamp": "2026-09-11T10:07:41.875Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-11T09:24:19.212Z",
  "updatedAt": "2026-09-11T10:09:13.219Z",
  "metrics": {
    "firstClaimAt": "2026-09-11T09:25:16.647Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 8
  },
  "contextFetchedBy": [
    "architect-1c0fef54",
    "worker-1b2cd09b"
  ],
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-11T09:32:41.102Z",
  "planningNotes": {
    "approachesConsidered": "KEYING THE DUMP ON THE TARGET'S OWN decisionId - rejected. It is the off-by-one the module header argues against at length, it reds rollback-restore.test.ts arm (a) plus the four ordering arms, and it would restore a schema older than the image being rolled to. Successor-of-target is correct and is a superset of current behaviour.\n\nPASSING THE TARGET AS A NEW POSITIONAL PARAMETER to resolveRollbackRestore/applyRollbackRestore - rejected. Both entry points are called from ~15 sites inside rollback-restore.test.ts; a positional change edits every one of them. Adding `toReceiptRef` to RollbackRestoreConfig changes exactly ONE place in that file (the fixture builder) and still makes omission a typecheck error at the composition root.\n\nMOVING THE APPLY AFTER deploy-service's OWN docker check instead of adding a precheck - rejected. DoD clause 2 requires the refusal BEFORE any dump path is RESOLVED, not merely before the apply, and moving resolve past the intent commit would make a resolve refusal leave the environment guard odd and need a terminal release. A read-only precheck before the resolve is strictly cheaper.\n\nRUNNING THE PRECHECK UNCONDITIONALLY - rejected. Gating it on `request.restore && intent === null` (the exact condition already guarding the resolve block) leaves every non-restore rollback and every recovery path byte-identical, which is the minimum change that closes the defect.\n\nTWO NEW MODULES (selection + precheck) - rejected on file count. One `rollback-preflight.ts` of ~135 lines holds both; the row's own framing is that schema selection and operational checks are one coherent pre-admission operation.\n\nFILING THE CONCURRENCY-EXCLUSION SUCCESSOR - not done. This plan does not open that seam: it adds no fence and changes no guard parity. The residual is already stated in production prose at rollback-command.ts:234-240 (\"THE RESIDUAL, stated rather than hidden\"), and board rails forbid spinning a row for work this plan does not touch. Step 8 records the pointer as a task comment instead.",
    "codebaseInsights": "MEASURED at main 0c215e82, 2026-09-11. Both defects confirmed.\n\nDEFECT 1 CONFIRMED. `sed -n '190,206p' apps/daemon/src/deployment/rollback-restore.ts` shows resolveRollbackRestore reading readCurrentDeployReceipt and keying BOTH destination(...current.decisionId, current.sha) and dumpToRestore(...current.decisionId). The function never receives the target receipt at all - its signature is (config, environment) and toReceiptRef lives only in rollback-command.ts's `request.receiptId`.\n\nTHE DoD's CLAUSE-1 PARENTHETICAL IS THE IMPRECISE PART, and the plan deliberately does not follow it literally. migrateWithBackup dumps BEFORE it applies (rollback-restore.ts header, lines 22-34), so receipt R's backupRef is the schema as it stood BEFORE R's own migration. For A -> B -> C: A.backupRef is the state before A migrated (one step TOO FAR - the state the deploy before A ran against), B.backupRef is the state A ran against, C.backupRef is the state B ran against. Rolling back to A wants the state A ran against, which is B's backupRef - the FIRST SUCCESSOR of the target that recorded a migration. Keying on A's own decisionId would restore a schema one step too far, which is exactly the data-destroying answer rollback-restore.test.ts:184 arm (a) exists to rule out (\"applies the CURRENT deploy's pre-migration dump, never the kept receipt's own\"). Successor-of-target is a strict GENERALISATION of today's behaviour: when the target is the immediate predecessor of current, successor == current, so arm (a) and every ordering arm stay green. Keying on the target itself would red five landed arms. Plan uses successor-of-target and the new arm asserts B's decision id, read back from the store, which still satisfies \"not C's\" and still reds under clause 3's prescribed mutation back to current.decisionId (with A->B->C, current is C and the successor is B).\n\nDEFECT 2 CONFIRMED. rollback-command.ts resolves the restore at :196 and APPLIES it at :243, while the only docker reachability check is deploy-service.ts:265-267 (`docker version --format {{.Server.Version}}`, refusing DEPLOY_DOCKER_UNAVAILABLE@DAEMON_DEPLOY_ENGINE) reached from rollback-command.ts:304-314. So the dump is applied ~60 lines before the host is known usable.\n\nNO NEW REFUSAL VOCABULARY IS NEEDED (DoD clause 5). DEPLOY_TARGET_MISSING and DEPLOY_DOCKER_UNAVAILABLE are already in the closed roster DEPLOY_REFUSAL_CODES (deploy-receipt-contracts.ts:41-44) paired with DEPLOY_ENGINE_STAMP = DAEMON_DEPLOY_ENGINE. The precheck reuses both, so the operator sees the same code@layer whether the refusal comes from the precheck or from deploy-service. No map edit, no map-test edit.\n\nTHE .js BRIDGES ARE TRACKED ONE-LINERS. `git ls-files` confirms every .ts in apps/daemon/src/deployment has a sibling .js whose whole content is `export * from \"./<name>.ts\";`. A new TS module therefore needs its bridge in the same commit.\n\nFOUR ASSERTING CONSUMERS OF `docker.calls` EMPTINESS on restoreDatabase:true paths, enumerated by `git grep -n 'docker.calls'`: rollback-command-ordering.test.ts:176, rollback-command-ordering.test.ts:237, rollback-production-wiring.test.ts:143. All three are in the plan's owned paths. rollback-command.test.ts's own docker arms (:348-350, :467-474) use restoreDatabase:false (harness payload at :47) and are NOT affected.\n\nSOURCE-TEXT ASSERTERS that pin these two files, found by grep rather than by import graph: rollback-command.test.ts:139-154 (requires `resolveRollbackRestore(`, `applyResolvedRestore(`, `.restoreDatabaseInto(` and FORBIDS `applyRollbackRestore(` and `.restoreDatabase(` in rollback-command.ts and rollback-restore.ts) and rollback-restore.test.ts:314-321 (forbids `process.env[.[]` and `payload[.[]` reads in rollback-restore.ts). The plan keeps all five satisfied: the new config field is named toReceiptRef and is populated from `request.receiptId`, never from `payload[...]`.",
    "risks": "THE LEDGER KEEPS REFUSED RECEIPTS AND ROLLBACK RECEIPTS. rollback-target.ts:8-15 warns that EnvironmentDeployState.previous is positional over an uncollapsed list. The successor walk must therefore SKIP receipts that recorded no migration at all (a refused-before-migration deploy, and every rollback, which records none) and STOP at the first receipt that did record one. A refused deploy that migrated and then failed to start DOES own a backupRef and is the correct stop - its dump is still the target's state.\n\nDO NOT SKIP PAST A MIGRATION WITH A NULL backupRef. If the first migrating successor recorded no dump, the schema moved with no record of the prior state; the honest answer is DEPLOY_ROLLBACK_RESTORE_BACKUP_ABSENT, never a later dump, which would be a LATER state and would silently destroy more.\n\nTHE PRECHECK ADDS ONE REAL `docker version` SPAWN on restore:true requests that later refuse. That is a read-only probe and moves no bytes, but it makes three landed `expect(docker.calls).toEqual([])` assertions false. Tighten them to the exact version argv rather than deleting them - an emptiness assertion replaced by nothing is how this file stops testing its subject.\n\nresolveDeployMigrationContext TAKES A sha THAT NAMES WHICH COMMIT'S migrations/ RUNS (deploy-migration-context.ts:100-104). The restore path never runs a migration, so re-keying destination() on the target's sha should be inert, but rollback-restore-real.test.ts is a live docker/postgres arm - run it or confirm it is gated before claiming the change is inert.\n\nLINE CAPS. rollback-restore.ts is at 245 of a 250 target. Moving the header's dump-selection argument into rollback-preflight.ts (where the decision now lives) is what keeps it under; do not grow it. rollback-command.ts is at 341 of a 400 hard cap, so the precheck body belongs in the new module and only its call site in the handler.\n\nSHARED CHECKOUT. `git status` at plan time shows peer dirt (.moe/* and .codex/*, plus an untracked Moe-NG_Deep_Review md). Never revert, stash or git add -A those. Every mutation drill in step 7 must back the mutated path OUT OF TREE to %TEMP% with the TASK ID in the filename, and prove restore by SHA256 equality in a call separate from the restore.",
    "keyFiles": [
      "apps/daemon/src/deployment/rollback-restore.ts",
      "apps/daemon/src/deployment/rollback-command.ts",
      "apps/daemon/src/deployment/deploy-ledger.ts",
      "apps/daemon/src/deployment/deploy-service.ts",
      "apps/daemon/src/deployment/deploy-receipt-contracts.ts",
      "apps/daemon/src/repository/migrations/migration-receipt.ts",
      "apps/daemon/src/deployment/rollback-restore.test.ts",
      "apps/daemon/src/deployment/rollback-command-ordering.test.ts"
    ]
  },
  "planApprovedAt": "2026-09-11T09:32:43.282Z",
  "commits": [
    {
      "sha": "43c744b0a899ed6a97e0bdbb433c37c92c8cd152",
      "ref": "refs/heads/moe/work-2026-09-11",
      "kind": "checkpoint",
      "role": "worker",
      "sessionId": "worker-1b2cd09b@2026-09-11T09:32:47Z",
      "paths": [
        ".moe/tasks/task-3ccb12a92f69432bb6996a3a4348e573.json",
        ".moe/tasks/task-705977238f794b2cadb5c1dcdb98d6bd.json"
      ],
      "recordedBy": "worker-1b2cd09b",
      "recordedAt": "2026-09-11T09:32:51.328Z",
      "treeId": "1d9c1778264646f9b7ade7ca7bff68b3b9a7cb4e",
      "status": "WORKING",
      "pushed": true
    }
  ],
  "filesModified": [
    "apps/daemon/src/deployment/rollback-restore.ts",
    "apps/daemon/src/deployment/rollback-command.ts",
    "apps/daemon/src/deployment/rollback-preflight.ts",
    "apps/daemon/src/deployment/rollback-preflight.js",
    "apps/daemon/src/deployment/rollback-restore.test.ts",
    "apps/daemon/src/deployment/rollback-command-ordering.test.ts",
    "apps/daemon/src/deployment/rollback-command.test.ts",
    "apps/daemon/src/deployment/rollback-production-wiring.test.ts"
  ],
  "unattributedPaths": [
    "node-alpha-e2e-proj-k3x0md/math.mjs",
    "node-alpha-e2e-proj-k3x0md/test.mjs",
    "node-beta-e2e-proj-k3x0md/math.mjs",
    "node-beta-e2e-proj-k3x0md/test.mjs",
    "node-omega-e2e-proj-k3x0md/math.mjs",
    "node-omega-e2e-proj-k3x0md/test.mjs"
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "checkpoint",
    "sessionId": "worker-1b2cd09b@2026-09-11T09:32:47Z",
    "at": "2026-09-11T09:32:51.328Z"
  },
  "workStartedAt": "2026-09-11T09:33:04.657Z",
  "reviewStartedAt": "2026-09-11T10:09:13.219Z",
  "verification": {
    "command": "pnpm --filter @moe/daemon test",
    "exitCode": 0,
    "outputTail": " Test Files  612 passed (612)\n      Tests  11056 passed | 21 skipped (11077)\n   Duration  305.84s (transform 32.03s, setup 0ms, import 344.71s, tests 808.02s, environment 33ms)\n\nTEST EXIT=0",
    "reportedAt": "2026-09-11T10:09:13.219Z"
  },
  "completionSummary": "BOTH DEFECTS CLOSED. Gate: `pnpm --filter @moe/daemon test` EXIT 0, `Test Files 612 passed (612)` / `Tests 11056 passed | 21 skipped (11077)`. `npx tsc --noEmit --project apps/daemon/tsconfig.json` EXIT 0.\n\nNEW apps/daemon/src/deployment/rollback-preflight.ts (168 lines) + tracked .js bridge. Two pure reads, no new refusal vocabulary. (1) selectRollbackDumpDecision: the dump restoring the TARGET's schema is the FIRST POST-TARGET DEPLOY THAT MIGRATED, joined durably through that deploy's decisionId - not the target's own backupRef (one step too far) and not the current deploy. (2) probeRollbackHost: read-only `docker version`, refusing DEPLOY_DOCKER_UNAVAILABLE@DAEMON_DEPLOY_ENGINE from fixed prose, never docker's stderr.\n\nDEFECT 1: RollbackRestoreConfig gained required `toReceiptRef`; resolveRollbackRestore keys the destination on the TARGET receipt and the dump on the selected successor. DEFECT 2: the probe runs in rollback-command.ts inside the pre-existing `request.restore && intent === null` block, BEFORE resolveRollbackRestore; ports hoisted to one const shared with the deploy service so probe and rollback cannot drift onto different docker daemons.\n\nDoD 1: new arm (a2) drives A->B->C through recordDeployReceipt/recordMigrationReceipt, targets A, and asserts the dump equals B's - READ BACK from the store via readMigrationReceipt, not a fixture literal - and is NOT C's and NOT A's, with the three paths proven pairwise distinct.\nDoD 2: new arm (vi) gives a bound harness an unusable docker and a restore port that RECORDS then THROWS. Asserts {DEPLOY_DOCKER_UNAVAILABLE, DAEMON_DEPLOY_ENGINE} plus four not-reached proofs (recorded calls empty, no EnvironmentRollbackRestoreApplied, no daemon:rollback-restore decision, no deploy receipt) and a positive control that the probe argv ran.\nDoD 3: both drilled. Keying back on current reds (a2) `- \"b.sql\" / + \"c.sql\"`. Removing the probe reds (vi) `- DEPLOY_DOCKER_UNAVAILABLE/DAEMON_DEPLOY_ENGINE / + DEPLOY_ROLLBACK_RESTORE_"
}