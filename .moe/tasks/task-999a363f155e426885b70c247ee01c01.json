{
  "id": "task-999a363f155e426885b70c247ee01c01",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "P1.2b-ii-b Land the PairingConfirmation manager scope and the project-manager app tree",
  "description": "Carved out of task-0602f6c2 (P1.2b-ii) by architect-e0ebd2e5 on 2026-08-27 at HEAD 61e3dc5b, after a compiled probe proved that adopting project-manager-app.tsx at HEAD CANNOT typecheck. task-0602f6c2 keeps the eight self-contained leaf paths (client/boundary/home + tests + css); this row owns the two app paths plus the one tracked-file hunk they depend on.\n\nMEASURED, do not re-derive (architect-e0ebd2e5, 2026-08-27, HEAD 61e3dc5b).\n\nTHE GAP. `apps/control-room/src/v2/projects/project-manager-app.tsx:154-159` renders `<PairingConfirmation ... scope=\"manager\" />`. HEAD's `apps/control-room/src/v2/live/pairing-confirmation.tsx` PairingConfirmationProps has exactly three members: busy?, confirmationLabel, onConfirm. `scope` exists ONLY in the dirty worktree copy (` M`, unstaged). Probe run (HEAD's copy extracted to a probe module, a consumer passing scope=\"manager\", `pnpm --filter @moe/control-room typecheck`, both probe files deleted in the same command, porcelain confirmed clean after):\n\n  src/v2/live/zzprobe-scope.tsx(6,81): error TS2322: Type '{ confirmationLabel: string; onConfirm: () => undefined; scope: string; }' is not assignable to type 'IntrinsicAttributes & PairingConfirmationProps'.\n    Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'.\n\nSo the two app paths cannot ship without the pairing-confirmation `scope` hunk, and that hunk cannot ship without them (nothing else consumes it). They are ONE slice.\n\nBACKWARD COMPATIBILITY, measured. `apps/control-room/src/v2/cordum-app.tsx:205` is the only other `<PairingConfirmation>` call site; at HEAD and in the worktree it passes busy/confirmationLabel/onConfirm and NO scope. The dirty hunk declares `readonly scope?: \"daemon\" | \"manager\" | undefined` with default `scope = \"daemon\"`, so cordum-app is unaffected. cordum-app.tsx belongs to P1.2b-iii (task-15ca5c44) and is NOT touched here.\n\nOTHER EDGES, all satisfied at HEAD. project-manager-app.tsx also imports `../cordum-fonts.js`, `../styles/cordum-tokens.css`, `../styles/cordum-shell.css` (all tracked at HEAD), `./project-home.js` and `./project-manager-client.js` (landed by task-0602f6c2 — the ordering premise). project-manager-app.test.tsx imports `ClockProvider` from `../../performance/command-latency.js`, which resolves to `apps/control-room/src/performance/command-latency.tsx` — TRACKED at HEAD and exporting `ClockProvider(props: { children: ReactNode; clock: Clock })`, matching the test's `<ClockProvider clock={{ now: () => 0 }}>` usage. No further gap.\n\nOWNERSHIP, measured by file field not prose: a blob grep of `.moe/tasks/*.json` for `pairing-confirmation` and `project-manager-app`, then a file-field scan (filesModified + every step's affectedFiles/modifiedFiles/newFiles) of task-e868ea33, task-dc934111 and task-15ca5c44, returned ZERO file-field hits. No live row owns these three paths.\n\nSIZE: pairing-confirmation.tsx 47 lines (worktree), project-manager-app.tsx 189, project-manager-app.test.tsx 182. All under the 250 target.\n\nEXCLUDED: the eight paths of task-0602f6c2; main.tsx and cordum-app.tsx (task-15ca5c44); apps/daemon/**.",
  "definitionOfDone": [
    "DoD-1 verify-or-block gate at claim, measured on disk. (a) `git ls-files apps/control-room/src/v2/projects/project-manager-app.tsx apps/control-room/src/v2/projects/project-manager-app.test.tsx` must be EMPTY (else duplicate -> report blocked). (b) `git ls-files apps/control-room/src/v2/projects/project-manager-client.ts apps/control-room/src/v2/projects/project-home.tsx` must BOTH be tracked (task-0602f6c2 must be DONE first; relaxing this ships a clean-checkout hole, since app.tsx imports both) -> else report blocked naming task-0602f6c2. (c) `git show HEAD:apps/control-room/src/v2/live/pairing-confirmation.tsx | grep -n scope` -- if HEAD already carries the scope member the premise is closed, adopt only the two app paths and say so; if HEAD lacks scope AND the worktree copy is no longer dirty or no longer carries it, the WIP was swept or reverted -> report blocked, never re-author it.",
    "Before any `git add`, a task comment records exact-path `git status --porcelain --untracked-files=all` for the three paths, SHA256 and physical line counts for each, %TEMP% backups suffixed by worker id, the per-file import edges, and the file-field board search showing no other live row owns them. Every step's affectedFiles/modifiedFiles is a subset of exactly these three paths.",
    "Exactly three paths are staged by explicit pathspec -- `apps/control-room/src/v2/live/pairing-confirmation.tsx` (adopt the dirty scope hunk VERBATIM, do not re-author), `apps/control-room/src/v2/projects/project-manager-app.tsx`, `apps/control-room/src/v2/projects/project-manager-app.test.tsx`. Nothing else under apps/control-room is staged. `git diff --cached --numstat` = 3 before the commit and `git show --stat HEAD` = 3 after.",
    "Tracked-object proof: in a `git archive HEAD` materialization with `pnpm install --frozen-lockfile --prefer-offline`, `pnpm --filter @moe/control-room typecheck` exits 0 (quote the `$ tsc` script echo as execution evidence) and the exact error this row exists to clear -- `error TS2322 ... Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'` -- is absent from its output. `pnpm --filter @moe/control-room test` runs project-manager-app.test.tsx with NONZERO `Test Files` and `Tests` count lines (exit 0 with no count line is UNVERIFIED, not passing). A live-tree green is a positive control only.",
    "Backward compatibility of the optional prop is proven, not asserted: an arm renders `PairingConfirmation` WITHOUT `scope` and asserts the daemon-scope copy, and a second arm renders it with `scope=\"manager\"` and asserts the manager copy. If project-manager-app.test.tsx already carries both, cite the arm titles instead of adding duplicates. `apps/control-room/src/v2/cordum-app.tsx` is not touched or staged.",
    "Mutation drill in a disposable `git archive` materialization, on the guard this row lands: delete the `scope` member from PairingConfirmationProps (or flip the `scope = \"daemon\"` default to `\"manager\"`) and confirm the NAMED arm from DoD-5 reds -- and that the arm that reds is the one whose title claims that behavior, not a neighbour. A green drill is a FINDING to report, not a pass to bank. Restore proven in a call SEPARATE from the restore: `git diff --exit-code` for the tracked pairing-confirmation.tsx, SHA256 against the %TEMP% backup for any path still untracked at drill time.",
    "`pnpm test:security` runs in the materialization and any remaining red is path-attributed: repo-wide failing paths at merge-base, minus at HEAD, intersected with these three owned paths must be EMPTY. Foreign red is disclosed VERBATIM with its exact failing lines, never hidden and never fabricated green."
  ],
  "taskRails": [
    "Own exactly three paths: apps/control-room/src/v2/live/pairing-confirmation.tsx, apps/control-room/src/v2/projects/project-manager-app.tsx, apps/control-room/src/v2/projects/project-manager-app.test.tsx. Never `git add apps/control-room`, never `git add -A`, never touch main.tsx or cordum-app.tsx (task-15ca5c44) or the eight paths landed by task-0602f6c2.",
    "Adopt bytes unchanged. The `scope` hunk in pairing-confirmation.tsx is adopted VERBATIM from the worktree; this row is an adopt-and-track row, not a refactor. New test arms are additive only.",
    "ORDERING IS A SAFETY CONSTRAINT, not paperwork: task-0602f6c2 must be DONE first. project-manager-app.tsx imports ./project-home.js and ./project-manager-client.js; landing this row first commits three files referencing untracked siblings and ships a clean-checkout hole that no live gate can see (the worktree copies make it read green).",
    "Only a `git archive HEAD` materialization is admissible evidence of loadability; the worktree already contains the untracked bytes, and `git add` moves nothing on disk, so a worktree run cannot distinguish tracked from untracked."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "GATE + MEASUREMENT RECORD. Run DoD-1 verbatim and record each result. (a) `git ls-files apps/control-room/src/v2/projects/project-manager-app.tsx apps/control-room/src/v2/projects/project-manager-app.test.tsx` must print NOTHING (else duplicate -> moe.report_blocked). (b) `git ls-files apps/control-room/src/v2/projects/project-manager-client.ts apps/control-room/src/v2/projects/project-home.tsx` must print BOTH — measured SATISFIED by the architect at HEAD 364a83f0: task-0602f6c2 landed all eight leaf paths as commit 413d1573 `feat(task-0602f6c2): track the Control Room project-manager client and project UI tree`. Re-measure anyway; if either is missing the prerequisite regressed -> report blocked naming task-0602f6c2. (c) `git show HEAD:apps/control-room/src/v2/live/pairing-confirmation.tsx | grep -n scope` — measured EMPTY at 364a83f0 (HEAD has no scope member); the worktree copy is ` M` and carries it at lines 7, 15, 21, 23, 25. If HEAD now carries it the premise is closed: adopt only the two app paths and say so in the comment. If HEAD still lacks it AND the worktree copy is clean or no longer carries it, the WIP was swept or reverted -> report blocked, never re-author the hunk. Then write ONE task comment (moe.add_comment — the complete_task summary field is write-only and persists nowhere, so every load-bearing claim goes in a comment) recording for all three paths: exact-path `git status --porcelain --untracked-files=all`, SHA256, physical line counts (`wc -l`; PowerShell `Measure-Object -Line` undercounts), the %TEMP% backup path suffixed by this worker id, per-file import edges, and the file-field board search (grep .moe/tasks/*.json for the stems, then scan filesModified + every step's affectedFiles/modifiedFiles/newFiles) showing no other live row owns them.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "note": "REWORK GATE + MEASUREMENT. Read-only; no bytes authored in this step. Full record in comment-0ade816e (the summary/step-note surfaces are not the durable carrier).\n\nHEAD 357d3635. DoD-1 re-run: (a) both app paths ARE tracked now, but by THIS row's own commit 357d3635 (`git log --diff-filter=A` names it and nothing earlier), so the duplicate trip-wire does not fire on a rework; (b) all ten paths under v2/projects tracked, prerequisite task-0602f6c2 (413d1573) intact and DONE; (c) `scope` is present at HEAD lines 7/15/21/23/25 — attempt 1 closed the original gap, so the rework is only about the defect it shipped.\n\nDefect re-measured from committed bytes, not accepted from the rejection: pairing-confirmation.tsx:18 `<main className=\"cr2-pairing\">`; shell/cordum-shell.tsx:179 `<main className=\"cr2-main\">{children}</main>`; cordum-app.tsx:192 renders PairingConfirmation with NO scope inside `<CordumShell>` unconditionally. Nested main confirmed. (Rejection wrote the shell path as src/v2/cordum-shell.tsx; it is src/v2/shell/cordum-shell.tsx — transcription slip, finding unaffected.)\n\nScoped the fix rather than widening it: ProjectManagerApp's root is a div (app.tsx:180) and its only other `<main>` is ManagerNotice (:64) on PENDING/REFUSED, mutually exclusive with PAIRING — so the manager path is already correct and only the daemon default needs gating. `grep -rn cr2-pairing` outside the component hits only cordum-shell.css, all class selectors, zero element selectors — no styling depends on the tag.\n\nRAIL-2 DEVIATION DECLARED: rail 2 forbids editing the adopted hunk; the rejection names rail 2 and directs the edit anyway. Proceeding on the rejection as the adjudication, recorded so it is not later read as an unauthorized refactor.\n\nState at claim: all three owned paths tracked and clean; SHA256 d55eaee3../47, 6c5cb91e../189, 2dbb9781../225, identical to attempt 1's delivered record. HEAD copies backed up to %TEMP%\\moe-backup-999a363f-worker-dc706ffd\\.\n\nDisclosed foreign movement: the plan's risk (2) expected five live-board typecheck errors in the live tree; that leg is now EXIT 0 while live-board.tsx and live-dispatch.ts remain ` M`. task-48ee1455's WIP advanced in the shared worktree mid-session. Not this row's; it is why only the materialization counts.",
      "startedAt": "2026-08-27T06:50:36.401Z",
      "completedAt": "2026-08-27T06:54:24.651Z"
    },
    {
      "stepId": "step-2",
      "description": "ADD THE TWO DIVERGENCE ARMS (the only authored bytes in this row; everything else is adopted verbatim). project-manager-app.test.tsx has EIGHT arms today and NOT ONE reaches the PAIRING branch — the arm at line 66, \"shows only the stable refusal code and layer when pairing or connection refuses\", drives the REFUSED path, not PAIRING. So app.tsx:154-159 and `claimPairing` (app.tsx:101-112) are currently uncovered, and nothing proves app.tsx passes `scope=\"manager\"` at all. Add ONE new describe block whose title carries `task-999a363f` (self-attributing, so a red names its owner) containing exactly two arms. ARM A, through the production seam: `renderApp({ prepared: Promise.resolve({ claim: vi.fn(), confirmationLabel: \"<label>\", status: \"AWAITING_OPERATOR\" } as ProjectManagerConnection) })` — `ProjectManagerPairingPending` is `{ claim(): Promise<ProjectManagerConnection>; readonly confirmationLabel: string; readonly status: \"AWAITING_OPERATOR\" }` at project-manager-client.ts:65-69 — then assert the MANAGER copy: the heading `Pair this browser with Moe Projects`, the manager body sentence naming `the project manager`, and that the daemon-only INSTANCE-id paragraph is ABSENT. Also assert the confirmation label renders inside the `output` element and that no pairing identity beyond the label reaches the DOM. ARM B, direct render, backward compatibility for cordum-app.tsx: `render(<PairingConfirmation confirmationLabel=\"<label>\" onConfirm={() => undefined} />)` with NO scope, asserting the DAEMON copy (`Pair this browser with Moe`, the `this project` sentence) and that the INSTANCE-id paragraph IS present. Import PairingConfirmation from `../live/pairing-confirmation.js`. Do NOT create a pairing-confirmation.test.tsx — that would be a fourth owned path. Then run the suite in the worktree as a POSITIVE CONTROL only (`vitest run src/v2/projects/project-manager-app.test.tsx` from apps/control-room) and record the count line; a worktree green proves nothing about tracked-ness.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/live/pairing-confirmation.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "note": "REWORK STEP 2 — RED FIRST, THEN THE MINIMUM FIX (epic rail 3). Attempt 1's two arms already existed; this step ADDS the landmark assertions they were missing, proves them red against the shipped defect, then makes them green with the one-line-ish production gate.\n\nRED, run in the worktree BEFORE touching production (`pnpm exec vitest run src/v2/projects/project-manager-app.test.tsx --reporter=verbose` from apps/control-room), EXIT 1, `Test Files 1 failed (1)` / `Tests 2 failed | 8 passed (10)`:\n  x ...> renders the daemon pairing copy when no scope is passed\n      AssertionError: expected ...(1) to have a length of +0 but got 1   at :231 `expect(document.querySelectorAll(\"main\")).toHaveLength(0)`\n  x ...> nests no second main landmark inside the daemon shell at the CordumApp seam\n      AssertionError: expected ...(1) to have a length of +0 but got 1   at :252 `expect(document.querySelectorAll(\"main main\")).toHaveLength(0)`\nARM A (\"renders the manager pairing copy...\") stayed GREEN with its new `main` count of exactly 1. That asymmetry is the point: the defect is one-sided and the fix must be too.\n\nTHE FIX, production, 5 insertions / 2 deletions in pairing-confirmation.tsx: `const Wrapper = scope === \"manager\" ? \"main\" : \"div\";` and the wrapper element becomes `<Wrapper className=\"cr2-pairing\" aria-labelledby=\"cr2-pairing-title\">`. aria-labelledby rides on whichever element wraps, as the rejection asked. Nothing else in the hunk moved; the manager path emits byte-identical markup to before.\n\nGREEN after the fix, same command: EXIT 0, `Test Files 1 passed (1)` / `Tests 10 passed (10)`, all three scope arms named in the verbose output.\n\nTHE SEAM ARM AND ITS PLACEMENT. `it(\"nests no second main landmark inside the daemon shell at the CordumApp seam\")` renders the REAL daemon path — `render(<CordumApp liveSetup={pending} search=\"\" />)` with the same AWAITING_OPERATOR fixture as cordum-app.test.tsx:51-67 — and asserts `main main` = 0, `main` = 1, and that the surviving landmark is `cr2-main` (the shell's, not the component's). It lives in project-manager-app.test.tsx, which this row owns: importing CordumApp is a READ, so this is not a fourth owned path and cordum-app.tsx is neither touched nor staged. qa-bf457df2 offered to take it as a follow-up if it needed a fourth path; it does not.\n\nTwo corrections I made to my own first cut rather than shipping them: the seam arm's type import was `../live/live-handshake.js`, which resolves to src/v2/live (nonexistent) and failed typecheck with TS2307 — the module is at src/live, so the specifier is `../../live/live-handshake.js`. And the two new imports were reordered to the file's existing specifier-sorted convention.\n\nWORKTREE VERIFICATION (positive control only — the admissible legs are step 4's materialization): `pnpm exec tsc --project tsconfig.json` EXIT 0; full package `pnpm exec vitest run` EXIT 0, `Test Files 93 passed (93)` / `Tests 1162 passed (1162)`. That 93/1162 is HIGHER than QA's 87/1105 at 357d3635 because the live worktree carries untracked foreign test files; it is not a count this row can claim.\n\nSIZE: project-manager-app.test.tsx is now 256 physical lines — over the 250 target, under the 400 bar, disclosed not hidden. pairing-confirmation.tsx is 50. app.tsx is untouched and byte-identical (6c5cb91e..).",
      "startedAt": "2026-08-27T06:54:28.212Z",
      "completedAt": "2026-08-27T06:55:13.214Z"
    },
    {
      "stepId": "step-3",
      "description": "STAGE AND COMMIT EXACTLY THREE BY EXPLICIT PATHSPEC. `git add -- ` naming the three paths individually: `apps/control-room/src/v2/live/pairing-confirmation.tsx` (a MODIFICATION — the scope hunk adopted verbatim, not re-authored), `apps/control-room/src/v2/projects/project-manager-app.tsx` (new), `apps/control-room/src/v2/projects/project-manager-app.test.tsx` (new + this row's two arms). NEVER `git add apps/control-room`, never `git add -A`, never a directory pathspec — the eight paths of task-0602f6c2 sit in the same directory and cordum-app.tsx is dirty next door. Before committing prove the staging: `git diff --cached --numstat` must print exactly 3 rows, `git diff --cached --name-only -- apps/control-room` must list only those 3, and `git diff --cached -- apps/control-room/src/v2/live/pairing-confirmation.tsx` must show ONLY the scope hunk (props member, defaulted parameter, the three conditional copy expressions, and the div->main / aria-labelledby move already in the worktree diff) — if it shows anything you authored, back it out. Commit with the message BEFORE the `--` (`git commit -m \"<msg>\" -- <three paths>`; a `-m` after `--` is parsed as a pathspec and nothing lands). After: `git show --stat HEAD` must show 3 files, two of them `create mode 100644`. Do not amend, reset, stash, or create a sibling worktree. If a foreign session-end autocommit swept these bytes first, do NOT repair it — record `git diff <merge-base>..HEAD -- <the three paths>` for QA and state that the carrying commit belongs to another task.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "note": "REWORK STEP 3 — COMMIT BY EXPLICIT PATHSPEC. Delivered `485e775fbbe4a3dd32e33cc64581721324eb6ad6`, parent 357d3635 (HEAD did not move during the step; re-read before and after).\n\nTWO PATHS, NOT THREE, and that is the correct shape for a rework. The step text names three because attempt 1 was CREATING two of them. Here `project-manager-app.tsx` needed no change — the manager path was already right — so committing it would have been an empty-diff path in the pathspec. `git show --stat 485e775f`:\n  apps/control-room/src/v2/live/pairing-confirmation.tsx            7 +++--\n  apps/control-room/src/v2/projects/project-manager-app.test.tsx   31 ++++++++++\n  2 files changed, 36 insertions(+), 2 deletions(-)\nBoth are inside the row's three owned paths, so the owned-path set is respected as a SUBSET, never exceeded. `project-manager-app.tsx` is byte-identical to attempt 1 at 6c5cb91ee332fc5381810eb3540a347a0bdde33b0947a07b070a472bb08b9ede.\n\nMODE 1 (worktree pathspec) commit, and it is safe here only because step 2 proved both worktree copies contain exactly this change and nothing else — a pathspec commit reads the worktree, so that has to be re-proved, not assumed. Immediately before committing, `git diff --numstat` over the three owned paths was exactly `5 2 pairing-confirmation.tsx` and `31 0 project-manager-app.test.tsx`, matching the classified hunks; `git diff --cached --numstat` was EMPTY, so no foreign staged blob could ride in.\n\nMessage via `-F -` heredoc BEFORE the `--`. Never `git add apps/control-room`, never `-A`, no amend, reset, stash, or sibling worktree.\n\nPOST-COMMIT PROOF:\n- `git diff HEAD -- <all three owned paths>` is EMPTY: the committed bytes equal the gated bytes.\n- `git diff --cached --numstat` is EMPTY: zero index residue, so the shared index is not left staged with stale blobs for the next row.\n- Neighbours NOT taken, verified by exact-path porcelain: cordum-app.tsx, cordum-app.test.tsx and shell/cordum-shell.tsx are all still ` M` with their foreign WIP intact, exactly as at claim. main.tsx is clean and untouched. Those files belong to task-15ca5c44; nothing of theirs entered this commit.\n\nThe commit message carries the measured red/green pair, the one-sidedness argument, and an explicit statement that the adopted hunk is edited against task rail 2 on the authority of the rejection.",
      "startedAt": "2026-08-27T06:55:16.836Z",
      "completedAt": "2026-08-27T06:56:11.680Z"
    },
    {
      "stepId": "step-4",
      "description": "TRACKED-OBJECT PROOF (this row's named verification gate). A worktree run cannot distinguish tracked from untracked — `git add` moves nothing on disk and vitest reads the worktree — so only a materialization is admissible. With cwd PINNED to the repo root (`git archive` roots at cwd; a drifted cwd silently produces a tiny tar and greens a fraction of the tree), `git archive --format=tar -o <mat>/tracked.tar HEAD`, `tar -xf` into <mat>/tree, sanity-check the entry count against ~4712 (a count near 32 means a drifted cwd — discard and redo), then `pnpm install --frozen-lockfile --prefer-offline`. Run each leg STANDALONE and UNPIPED with its own exit code captured (a piped run reports the pipe tail's status; a `;`-chained run reports only the last command's). LEG 1 `pnpm --filter @moe/control-room typecheck`: must exit 0; quote the `$ tsc --project tsconfig.json` script echo as execution evidence (a sub-second silent green is indistinguishable from a leg that never ran); and grep its output to prove the error this row exists to clear is ABSENT — `Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'`. Expect the five foreign live-board errors to be absent too: measured at HEAD 364a83f0 the live worktree leg is EXIT 1 with TS2554 at src/live/live-board-dispatch.test.tsx(164,172,181) and src/live/live-board.tsx(63) plus TS2353 `planningGoalRef` at src/live/live-board.tsx(132), all from UNSTAGED WIP whose producer is task-48ee1455; if any of them appears in the materialization it means that WIP has since been committed and it is still not this row's — disclose it verbatim. LEG 2 `pnpm --filter @moe/control-room test`: must exit 0 AND print nonzero `Test Files` / `Tests` count lines covering project-manager-app.test.tsx. Exit 0 with no count line is UNVERIFIED, not passing — `pnpm --filter` on an unresolved package prints \"No projects matched the filters\" and exits 0. @moe/control-room resolves to apps/control-room via the `apps/*` workspace glob; confirmed.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "note": "REWORK STEP 4 — TRACKED-OBJECT PROOF at the EXACT delivered sha 485e775f (not HEAD, which can move). No bytes authored.\n\nMATERIALIZATION. cwd asserted as the repo root (`pwd` -> /d/projexts/moe-next) before `git archive --format=tar -o <mat>/tracked.tar 485e775f`, extracted with Windows `tar.exe` (never MSYS tar - it mangles drive letters), into %TEMP%\\moe-mat-999a-dc706ffd-485e775f...\\tree. TAR ENTRIES 4724, identical to attempt 1's delivered count, which is correct: this commit modifies two existing entries and creates none. A count near 32 would have meant a drifted cwd; it did not happen.\n\nTHE FIX IS IN THE GIT OBJECT, not merely the worktree - read from the materialized file:\n  :19  const Wrapper = scope === \"manager\" ? \"main\" : \"div\";\n  :21  <Wrapper className=\"cr2-pairing\" aria-labelledby=\"cr2-pairing-title\">\n  :48  </Wrapper>\nand the seam arm at project-manager-app.test.tsx:240 with `querySelectorAll(\"main main\")` at :252. v2/projects holds TEN files.\n\n`pnpm install --frozen-lockfile --prefer-offline` EXIT 0, \"Done in 3s using pnpm v11.0.8\". Each leg below was run STANDALONE, redirected to its own file rather than piped, with its own exit code captured - a piped run reports the pipe tail's status.\n\nLEG 1 `pnpm --filter @moe/control-room typecheck` -> EXIT 0. Execution evidence, the script echo, which is the ONLY line it emitted: `$ tsc --project tsconfig.json`. A silent sub-second green is otherwise indistinguishable from a leg that never ran.\n  - The error this row's parent existed to clear is ABSENT: `grep -c \"Property 'scope' does not exist\"` -> 0.\n  - The five foreign live-board errors the plan's risk (2) predicted are also absent from tracked objects (they live only in unstaged WIP; see step 1's note that the LIVE leg has since gone green too, which is foreign movement, not this row).\n\nLEG 2 `pnpm --filter @moe/control-room test` -> EXIT 0, `$ vitest run` echo, `Test Files 87 passed (87)` / `Tests 1106 passed (1106)`. Nonzero count lines, so this is not the \"No projects matched the filters\" exit-0-with-no-tests shape. THE COUNT RECONCILES EXACTLY: attempt 1 measured 87/1105 at 357d3635; this commit adds exactly one arm, so 1106 = 1105 + 1 and the file count is unchanged at 87. A count that had moved by anything other than +1 would mean this commit changed a suite it does not own.\n\nLEG 3, itemized, run from inside apps/control-room because `pnpm --filter X test -- <path>` does not filter: `pnpm exec vitest run src/v2/projects/project-manager-app.test.tsx --reporter=verbose` -> EXIT 0, `Test Files 1 passed (1)` / `Tests 10 passed (10)`, with all three scope arms named in the output:\n  v ... renders the manager pairing copy when the app drives its own pairing branch 17ms\n  v ... renders the daemon pairing copy when no scope is passed 3ms\n  v ... nests no second main landmark inside the daemon shell at the CordumApp seam 19ms\nThis is what proves the seam arm actually EXECUTES from tracked objects rather than merely existing in them.",
      "startedAt": "2026-08-27T06:56:14.577Z",
      "completedAt": "2026-08-27T06:57:17.990Z"
    },
    {
      "stepId": "step-5",
      "description": "MUTATION DRILL — TWO MUTANTS, in the disposable materialization only, never in the shared worktree. This pair IS the divergence fixture: each mutant must red exactly one arm and leave the other GREEN, which is what proves the two arms test two different mechanisms rather than one shared fence. MUTANT 1, the default: change `scope = \"daemon\"` to `scope = \"manager\"` in pairing-confirmation.tsx:15. ARM B (no scope passed) MUST red; ARM A must stay green. Arm B passes no scope at all, so the parameter default is the only mechanism that can produce its copy — there is no downstream fence, this is a pure render. MUTANT 2, the call site: delete `scope=\"manager\"` from project-manager-app.tsx:158. ARM A MUST red; ARM B MUST STAY GREEN. That asymmetry is the whole point — if mutant 2 also reds arm B, the arms are not independent and the fixture is wrong. NEVER delete the guard or the prop to force a red: that makes the mutant green and reads as \"no prop needed\". For each mutant, confirm the edit actually landed before rerunning (a silent no-op write reads as green), rerun ONLY project-manager-app.test.tsx, and check that the arm that reds is the one whose TITLE claims that behavior, not a neighbour. A GREEN drill is a FINDING to report in the comment, not a pass to bank. Restore between mutants and prove it in a call SEPARATE from the restore: SHA256 of the restored file against `git show HEAD:<path>` — assert both hashes are NONEMPTY, since two empty hashes compare equal and print match:true. A `git archive` extraction is not a git repository, so `git diff` is unavailable there; discarding and re-extracting the tar is an acceptable alternative restore, but the SHA256 equality must still be recorded. Record both mutants, both arm titles, the exact red output lines and the restore hash pairs in a task comment.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "note": "REWORK STEP 5 — MUTATION DRILL. Full record in comment-cd131630. No shared-worktree byte mutated; all four mutants lived in the disposable materialization of 485e775f and the owned paths were re-verified clean afterwards.\n\nFOUR MUTANTS, not the plan's two. The plan's pair grades the COPY mechanisms attempt 1 landed and cannot touch the wrapper element, so drilling only those would have left the actual fix ungraded.\n\nM3 REVERT THE FIX (Wrapper -> always \"main\", reinstating the shipped defect): EXIT 1, 2 failed | 8 passed (10). Reds the daemon-copy arm and the seam arm by title; manager arm GREEN.\nM4 MIRROR (Wrapper -> always \"div\", landmark gone): EXIT 1, 1 failed | 9 passed (10). Reds ONLY the manager arm (\"expected to have a length of 1 but got +0\"); both daemon arms GREEN. This is the one that proves ARM A's `main` count of exactly 1 is load-bearing and not decorative — M3 alone would leave \"landmark dropped entirely\" undetected. Both directions pinned, not one spelling of the failure.\nM1 PARAMETER DEFAULT: EXIT 1, 2 failed | 8 passed (10) — daemon arm + seam arm red, manager green. DISCLOSED CHANGE FROM ATTEMPT 1, which recorded this redding one arm: the seam arm is also downstream of the default, so it now reds two. Not a fixture defect; the divergence is carried by M2.\nM2 CALL SITE (delete scope=\"manager\" at app.tsx:158; never deleted the prop or the component): EXIT 1, 1 failed | 9 passed (10) — ONLY the manager arm. Hash 3bc65626.. independently reproduces attempt 1's recorded mutant-2 hash. M1 and M2 red disjoint arm sets, so call site and default are independent mechanisms.\n\nTYPECHECK ABSENCE CONTROL: deleting the props member reproduced `project-manager-app.tsx(158,5) TS2322 ... Property 'scope' does not exist` verbatim at the real call site, EXIT 1 — so step 4's EXIT 0 is a real green, not a leg that never ran.\n\nRESTORES all proven in calls SEPARATE from the restore, SHA256 against the pristine `git show 485e775f:<path>` copy, both hashes asserted nonempty and 64 chars, MATCH TRUE after every mutant. `git diff` is unavailable inside an archive extraction, hence the hash form. Mutations were applied through a helper asserting the anchor occurs exactly once AND the file hash changed, so a silent no-op write could not read as a green drill.\n\nNO GREEN DRILL: all four redded, each redding the arm whose title claims that behavior and no neighbour.",
      "startedAt": "2026-08-27T06:57:33.467Z",
      "completedAt": "2026-08-27T06:59:58.744Z"
    },
    {
      "stepId": "step-6",
      "description": "SECURITY SUITE AND PATH ATTRIBUTION. In the same materialization run `pnpm test:security` standalone and unpiped. Attribute every red by PATH, not by exit code: record the repo-wide failing-path set at the merge-base, the set at HEAD, subtract, and intersect with these three owned paths — that intersection MUST be empty. Disclose foreign red VERBATIM with exact failing lines and file paths; never hide it and never fabricate green. Known foreign red at planning time: tests/security/runtime-provider-projects.security.ts (owner task-67b0fee6) and the daemon typecheck `affordance-read-planning.test.ts(124,25) TS2339 planningGoalRef` (owner task-48ee1455). Also check whether any security scanner enumerates control-room render surfaces or refusal copy — boundary-roster.security.ts:148 registers PROJECT_MANAGER_LOCAL_LAYER against project-manager-client.ts, which task-0602f6c2 already made tracked, so this row should not move that arm; if it does, that is a roster/scan coupling the plan did not predict and must be disclosed rather than papered over.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/live/pairing-confirmation.tsx"
      ],
      "note": "REWORK STEP 6 — SECURITY SUITE + PATH ATTRIBUTION. Full table in comment-bb28ee0b. No bytes authored.\n\nTwo materializations, premises verified inside the archives: BASE 357d3635 (4724 entries) and DELIVERED 485e775f (4724 — correctly equal, this commit creates no entries). Both installs EXIT 0.\n\nThe composed `test:security` is `tsc && vitest`, so its exit code answers nothing: EXIT 1 at DELIVERED with ZERO `Test Files` count lines — the vitest half never runs. Ran the vitest leg standalone at both commits instead.\n\nBASE and DELIVERED are IDENTICAL in every field: 6 failed | 5 passed (11), 27 failed | 614 passed (641), same six files with the same per-file counts (boundary-roster 3, durable-store-boundaries 5, lane-smoke 1, runtime-provider-projects 5, scheduler-activation-boundaries 7, transport-boundaries 6). DELIVERED minus BASE = EMPTY. Intersection with the three owned paths = EMPTY.\n\nThree foreign tsc errors quoted verbatim in the comment, byte-identical to attempt 1's: TS2305 `captureManagerPairingTicket` (symbol exists nowhere; importer landed at b186c621) and two in runtime-provider-projects.security.ts (task-67b0fee6).\n\nThe roster coupling the step asked about did NOT move: boundary-roster.security.ts is 3 failed at both. This row changes a wrapper ELEMENT, not a layer constant or export surface, so no roster scan can see it. Predicted, then measured.\n\nDaemon typecheck at DELIVERED for completeness: EXIT 1 with `$ tsc --project tsconfig.json` echo and exactly the two known foreign `affordance-read-planning.test.ts(124,25)/(125,25) TS2339 planningGoalRef` errors, owner task-48ee1455. Unchanged from BASE, disjoint from owned paths.\n\nRecorded as \"no delta\", not \"clean\" — the suite is red at both commits for six foreign reasons and nothing here is fabricated green.",
      "startedAt": "2026-08-27T07:00:03.635Z",
      "completedAt": "2026-08-27T07:02:00.532Z"
    },
    {
      "stepId": "step-7",
      "description": "ADVERSARIAL SELF-REVIEW AND DURABLE COMPLETION RECORD. Load the `adversarial-self-review` skill and read the three files as an attacker. Specifically: `stableRefusal` (app.tsx:44-49) rewrites any refusal's layer to PROJECT_MANAGER_LOCAL_LAYER when both code and layer match /^[A-Z][A-Z0-9_]*$/u — can a hostile server therefore launder an arbitrary uppercase code into a rendered refusal, and does the existing arm at line 66 (which asserts `secretDetail` never renders) cover that? `claimPairing` (app.tsx:101-112) guards re-entry on `resolution.busy` — what happens on a double click before the setState commits, and on a `claim()` that resolves to another AWAITING_OPERATOR forever? Does any refusal or pairing path log or render a bearer, token, request id or pairing identity beyond `confirmationLabel`? The pairing element moved from `div` to `main` in the adopted hunk — with cordum-app.tsx also rendering a `main`, can two `main` landmarks coexist once P1.2b-iii composes them (task-15ca5c44)? Record every finding as a DISCLOSED FOLLOW-UP with a proposed row — do NOT change the adopted bytes. Then write the completion record as a task COMMENT, not as the complete_task summary (that field is write-only; step notes truncate silently at 5000 chars): the three paths with SHA256 and line counts, `git show --stat HEAD`, the materialization's `$ tsc` echo and exit code, the grep proving the TS2322 is gone, the test count lines, both drill mutants with their arm titles and red lines and restore hash pairs, the security attribution table, and an explicit statement that cordum-app.tsx and main.tsx were not touched and remain P1.2b-iii's (task-15ca5c44). Only then moe.complete_task.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/projects/project-manager-app.tsx",
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
      ],
      "note": "REWORK STEP 7 — ADVERSARIAL SELF-REVIEW + DURABLE RECORD. Findings in comment-3756afa3; completion record in comment-5d0a7b0f. GRADE AT 95fde3e0, not 485e775f.\n\nTHE REVIEW FOUND A REGRESSION IN MY OWN FIX AND I FIXED IT. 485e775f gated the wrapper on `scope` but left `aria-labelledby` on it; under the daemon scope the wrapper is now a `div`, and ARIA prohibits naming a `generic` element, so the daemon pairing screen carried a dead attribute and lost its accessible name. Same failure shape as the rejection — a change applied to the wrapper without asking what the wrapper had become on the other branch. My landmark assertions could not see it: they count `main` elements; the name is a different property. Fixed in 95fde3e0 by restoring the parent's placement (`<section aria-labelledby>`, valid on BOTH branches since a named section is a `region`); both arms now assert `getByRole(\"region\", { name })` so it cannot regress silently.\n\nPUSHBACK, stated not swallowed: the rejection said \"aria-labelledby on whichever wraps\". That holds only while the wrapper is nameable, which stopped being true once the daemon branch became a `div`. @qa-bf457df2 can overrule and I will gate the attribute per-branch instead; the section form needs no branch.\n\nMUTANT 5 (attribute back on the wrapper — the mutated file hashes to 114e184a.., which IS 485e775f's blob) reds both region arms, 2 failed | 8 passed (10). Stated precisely: only the DAEMON red is a real-defect red; the manager arm reds because it pins where the name lives, and `main` was nameable, so 485e775f never broke the manager path.\n\nSECOND FIX FROM THE SAME REVIEW: the seam arm asserted an exact `className === \"cr2-main\"`. task-15ca5c44 edits that shell next and any added class would have redded MY arm for THEIR change, naming my row. Now `classList.contains(\"cr2-main\")` — still proves the survivor is the shell's landmark, without pinning a peer row's class list.\n\nCHECKLIST WALKED, clean items recorded so they read as considered not skipped: concurrency N/A (pure render, no shared state/IO/handles); inputs — untyped garbage `scope` fails `=== \"manager\"` and lands on the NESTING-SAFE div branch; exactly two production call sites at 95fde3e0, both passing a constant; every `cr2-pairing` CSS selector is a class selector so no styling depends on the tag; the shell's `#cr2-main` skip-link target is untouched.\n\nDISCLOSED FOLLOW-UP (no bytes changed): switching element TYPE on a prop remounts the subtree if `scope` ever flips on a mounted instance, losing focus in the output/button. Unreachable today — both call sites pass a constant. A future row that drives `scope` from state should add an arm.\n\nFINAL GATES at 95fde3e0 in a fresh materialization: typecheck EXIT 0 with `$ tsc --project tsconfig.json`, TS2322 absent and positively controlled; `pnpm --filter @moe/control-room test` EXIT 0, 87 files / 1106 tests; itemized owned suite EXIT 0, 10/10, all three scope arms named. Security identical to base, delta EMPTY. Five drills, all red, none green.",
      "startedAt": "2026-08-27T07:02:02.887Z",
      "completedAt": "2026-08-27T07:07:29.585Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "REJECT on DoD-5 only. Full evidence, and everything that PASSES, is in comment-3f719a5d so the re-fix stays narrow: DoD-1's three gates re-measured at parent ba5e8a3d, DoD-3 (3 paths, 428+/8-), DoD-4 (typecheck EXIT 0 with the `$ tsc` echo; 87 files / 1105 tests) and DoD-6's two-mutant asymmetry all pass.\n\nDEFECT: this commit ships NESTED `<main>` landmarks on the daemon pairing screen, an existing shipped path. Invalid HTML and an ARIA violation.\n\npairing-confirmation.tsx:18 now renders `<main>` where the parent rendered `<div>`, and that change is UNCONDITIONAL while every other change in the hunk is gated on `scope`. cordum-app.tsx:192 renders it without `scope` inside `<CordumShell>`; cordum-shell.tsx:179 wraps children in `<main className=\"cr2-main\">`.\n\nMEASURED, not argued. Probe rendering CordumApp in PAIRING (same fixture as cordum-app.test.tsx:51-67):\n  QA_PROBE main_count=2 nested_main_count=1 classes=cr2-main|cr2-pairing\nCONTROL at the parent, same probe byte-for-byte: 1 passed, EXIT 0, nested ZERO. Introduced by 357d3635.\n\nWHY THE SUITE MISSED IT: 87/1105 is green because nothing asserts landmark structure, and a component whose only real usage is inside a shell cannot have backward compatibility proven by a standalone render.\n\nNOT A FINDING AGAINST YOUR EXECUTION. Step 3 listed the div->main move as part of the hunk to adopt VERBATIM and rail 2 forbade re-authoring it. You followed both, and you flagged the one-main invariant yourself; I am correcting only the timing - it is live now, not a future task-15ca5c44 hazard.\n\nFIX: 1. gate the wrapper on `scope` - manager `<main>`, daemon `<div>`, aria-labelledby on whichever wraps. The manager path is already correct. 2. ARM B asserts no `main`; ARM A asserts exactly one. 3. Add the seam arm: CordumApp in PAIRING, `main main` count 0 (if it needs a fourth owned path, say so and I take it as a follow-up). 4. Re-verify in a fresh materialization.",
  "createdBy": "WORKER",
  "parentTaskId": "task-e868ea339a0d408e8bc9533985b3d92a",
  "priority": "CRITICAL",
  "order": 1.625,
  "comments": [
    {
      "id": "comment-e7eeae32627e4b4e9f34cdec44f8abe3",
      "author": "worker-6246d2e0",
      "content": "STEP-1 GATE + MEASUREMENT RECORD (worker-6246d2e0, 2026-08-27, HEAD ba5e8a3d79d8df88a048b3c865c3d07d67b740d2).\n\nHEAD MOVED SINCE PLANNING, disclosed up front: the architect measured at 364a83f0; HEAD is now ba5e8a3d after `fix(task-c07f64a9): land the Control Room CSP producer its committed handshake test already asserts`. `git diff 364a83f0..HEAD` over my three owned paths is EMPTY, so every premise below was re-measured at ba5e8a3d and none of them moved.\n\nDoD-1 GATE, all three clauses PASS.\n(a) `git ls-files <app>.tsx <app>.test.tsx` printed NOTHING, count 0. Not a duplicate.\n(b) `git ls-files project-manager-client.ts project-home.tsx` printed BOTH:\n      apps/control-room/src/v2/projects/project-home.tsx\n      apps/control-room/src/v2/projects/project-manager-client.ts\n    The prerequisite has not regressed. Landed by task-0602f6c2 as commit 413d1573; all EIGHT of that row's paths are still tracked (`git ls-files` on the directory returns 8). Note for the record: task-0602f6c2 is in REVIEW, not DONE. The rail's stated safety property is that app.tsx must not reference untracked siblings, and that property is satisfied by tracked-ness, which is what DoD-1(b) actually measures and what the plan's step 1 records as SATISFIED. A QA rejection would not untrack those bytes, so the hazard the rail names cannot reappear from board movement alone.\n(c) `git show HEAD:.../pairing-confirmation.tsx | grep -n scope` -> EMPTY. HEAD still has no scope member, so the premise is OPEN and this row still has work. The worktree copy is ` M` and STILL CARRIES the hunk at exactly the five lines the architect measured: 7 (`readonly scope?: \"daemon\" | \"manager\" | undefined;`), 15 (`scope = \"daemon\",`), 21, 23, 25. The WIP was not swept or reverted. Nothing is re-authored.\n\nEXACT-PATH PORCELAIN for the three owned paths:\n   M apps/control-room/src/v2/live/pairing-confirmation.tsx        <- tracked, dirty (the hunk to adopt)\n  ?? apps/control-room/src/v2/projects/project-manager-app.test.tsx\n  ?? apps/control-room/src/v2/projects/project-manager-app.tsx\n\nSHA256 + PHYSICAL LINES (wc -l; Measure-Object not used):\n  pairing-confirmation.tsx      d55eaee33894085b58f7b5f6ac1442b0f38629496a3ce9740128f88fa97ff10a   47  (worktree)\n  pairing-confirmation.tsx      b8f3d6763d21fc8daebadd1747b56211a6dccbde031c39e1a79c96f6afcf1f86   41  (HEAD baseline, for the modification diff)\n  project-manager-app.tsx       6c5cb91ee332fc5381810eb3540a347a0bdde33b0947a07b070a472bb08b9ede  189\n  project-manager-app.test.tsx  8b8eca6d9f984c4c2771ffd04d36cc9892ca1993d9aec53e71e5da6a2d511700  182\nAll three match the architect's measurement exactly, and the two app hashes are byte-identical to what I recorded for these same files while working task-0602f6c2 four hours ago - they have not drifted. All three are under the 250 target.\n\n%TEMP% BACKUPS (out-of-tree, suffixed by worker id):\n  C:\\Users\\Yaron\\AppData\\Local\\Temp\\moe-backup-999a363f-worker-6246d2e0\\\n  containing pairing-confirmation.tsx, project-manager-app.tsx, project-manager-app.test.tsx.\n\nPER-FILE IMPORT EDGES. Re-measured, and I am flagging a measurement trap I hit twice now: a regex of the form `import ... from \"x\"` swallows adjacent SIDE-EFFECT imports (`import \"../cordum-fonts.js\";`) and silently under-reports. My first pass missed three edges the architect had correctly listed. Corrected, from a raw line grep:\n  - pairing-confirmation.tsx     -> react ONLY (type JSX). No other edge.\n  - project-manager-app.tsx      -> react (useCallback/useEffect/useState + type JSX); SIDE-EFFECT `../cordum-fonts.js`, `../styles/cordum-tokens.css`, `../styles/cordum-shell.css`; `../live/pairing-confirmation.js` (PairingConfirmation); `./project-home.js` (ProjectHome + type ProjectHomeResult); `./project-manager-client.js` (PROJECT_MANAGER_LOCAL_LAYER + six types).\n  - project-manager-app.test.tsx -> @testing-library/react, @testing-library/user-event, vitest; `../../performance/command-latency.js` (ClockProvider); `./project-manager-app.js`; `./project-manager-client.js`.\nEVERY OUT-OF-SLICE EDGE VERIFIED TRACKED AT HEAD ba5e8a3d: cordum-fonts resolves to `apps/control-room/src/v2/cordum-fonts.ts` (a .ts, not .tsx - tracked), styles/cordum-tokens.css tracked, styles/cordum-shell.css tracked, `../../performance/command-latency.js` resolves to `apps/control-room/src/performance/command-latency.tsx` (tracked, and `export function ClockProvider(props: {` is at :75, matching the test's `<ClockProvider clock={{ now: () => 0 }}>` at :51), plus pairing-confirmation.tsx, project-home.tsx and project-manager-client.ts. No unresolved edge remains.\n\nBOARD SEARCH (file-field, not prose). Parsed every .moe/tasks/*.json - filesModified plus every implementationPlan step's affectedFiles/modifiedFiles/newFiles - for the stems `pairing-confirmation` and `project-manager-app`.\n  FILE-FIELD hits: exactly ONE row, this one, holding exactly the three paths. ZERO foreign file-field hits.\n  Prose-only hits, disclosed, none of which is ownership: task-0602f6c2 (the row I just completed, which names them only to say they are excluded), task-15ca5c44 (P1.2b-iii), task-e868ea33.\n\nTHE HUNK TO ADOPT, read and confirmed verbatim from `git diff`: adds the optional prop member, the `scope = \"daemon\"` default, three conditional copy expressions (heading \"Moe Projects\" vs \"Moe\"; body \"the project manager\" vs \"this project\"; the INSTANCE-id paragraph rendered ONLY when scope is not manager), a reworded note line, and the `div`->`main` element change with `aria-labelledby` moving from the inner section to the outer main. Nothing in it is authored by me.\n\nNo files changed by this step.",
      "timestamp": "2026-08-27T06:27:38.730Z"
    },
    {
      "id": "comment-baecb0207d6c49ecb94c019125057d06",
      "author": "worker-6246d2e0",
      "content": "STEP-5 MUTATION DRILL - TWO MUTANTS PLUS A TYPECHECK CONTROL. Disposable materialization only (C:\\Users\\Yaron\\AppData\\Local\\Temp\\moe-mat-999a-6246d2e0\\tree). The shared worktree was never mutated; all three owned paths stayed tracked and clean there throughout.\n\nDIVERGENCE ANALYSIS, stated before drilling. ARM A reaches the copy through the production seam: renderApp -> app.tsx's PAIRING branch -> `<PairingConfirmation ... scope=\"manager\" />` at app.tsx:158. ARM B renders PairingConfirmation directly and passes NO scope, so the parameter default at pairing-confirmation.tsx:15 is the ONLY mechanism that can produce its copy - it is a pure render with no downstream fence, nothing else can answer first. The two arms therefore ride two different mechanisms, and the drill pair is what proves that rather than asserts it.\n\nMUTANT 1 - THE PARAMETER DEFAULT. `scope = \"daemon\"` -> `scope = \"manager\"` at pairing-confirmation.tsx:15. Applied and confirmed before running: line 15 reads `  scope = \"manager\",`, SHA256 D55EAEE3.. -> 6BA9E624D84778219073A0C24870CCBBD0ED3EC830A58417FFFCA9ED44748A6E.\nRESULT EXIT 1, and the asymmetry is exact:\n    v ... > renders the manager pairing copy when the app drives its own pairing branch 17ms      <- ARM A GREEN\n    x ... > renders the daemon pairing copy when no scope is passed 12ms                          <- ARM B RED\n      -> Unable to find an accessible element with the role \"heading\" and name \"Pair this browser with Moe\"\n    TestingLibraryElementError: Unable to find an accessible element with the role \"heading\" and name \"Pair this browser with Moe\"\n    Test Files 1 failed (1) / Tests 1 failed | 8 passed (9)\nThe arm that reddened is the one whose TITLE claims that behavior. ARM A stayed green because its copy comes from the call site, not the default.\nRESTORE, proven in a call SEPARATE from the restore: restored D55EAEE33894085B58F7B5F6AC1442B0F38629496A3CE9740128F88FA97FF10A vs `git show HEAD:<path>` D55EAEE33894085B58F7B5F6AC1442B0F38629496A3CE9740128F88FA97FF10A, both 64 chars so neither is empty, match TRUE, line 15 back to `scope = \"daemon\",`. (A `git archive` extraction is not a git repository, so `git diff` is unavailable there - hence the SHA256-against-`git show` form.)\n\nMUTANT 2 - THE CALL SITE. Deleted the `scope=\"manager\"` line from project-manager-app.tsx:158. Never deleted the prop declaration or the component - deleting the guard would make the mutant green and read as \"no prop needed\". Applied and confirmed: SHA256 6C5CB91E.. -> 3BC6562654C75CD05C6F9F8CA63C11D74D6CB2FD7C40DE2124EAEEF801F0699E, 189 -> 188 lines, and the call site now reads busy/confirmationLabel/onConfirm with no scope.\nRESULT EXIT 1, the MIRROR of mutant 1:\n    x ... > renders the manager pairing copy when the app drives its own pairing branch 1010ms    <- ARM A RED\n      -> Unable to find role=\"heading\" and name \"Pair this browser with Moe Projects\"\n    v ... > renders the daemon pairing copy when no scope is passed 3ms                           <- ARM B GREEN\n    Test Files 1 failed (1) / Tests 1 failed | 8 passed (9)\nTHIS ASYMMETRY IS THE WHOLE POINT and it holds in both directions: mutant 1 reds only B, mutant 2 reds only A. Neither mutant reds both, so the arms are independent and are not two spellings of one shared fence. Had mutant 2 also reddened B, the fixture would have been wrong and I would be reporting that instead.\nRESTORE proven separately: restored 6C5CB91EE332FC5381810EB3540A347A0BDDE33B0947A07B070A472BB08B9EDE == `git show HEAD:<path>`, both nonempty, match TRUE, 189 lines, `scope=\"manager\"` present at the call site again.\n\nADDITIONAL TYPECHECK CONTROL (mine, not required by the plan; DoD-6 lists this mutant as an option and it positively discharges DoD-4's \"the error is ABSENT\" clause). Step 4 claims a grep found no `Property 'scope' does not exist...` in the typecheck output - but an absence proves nothing unless the leg can produce that error at all. So I deleted the `readonly scope?: ...` member from PairingConfirmationProps (47 -> 46 lines) and reran the same leg:\n    CONTROL TYPECHECK EXIT 1\n    src/v2/live/pairing-confirmation.tsx(14,3): error TS2339: Property 'scope' does not exist on type 'PairingConfirmationProps'.\n    src/v2/projects/project-manager-app.tsx(158,5): error TS2322: Type '{ busy: boolean; confirmationLabel: string; onConfirm: () => void; scope: string; }' is not assignable to type 'IntrinsicAttributes & PairingConfirmationProps'.\n      Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'.\nThat is the row description's error verbatim, now raised at the REAL production call site app.tsx:158 rather than at the architect's synthetic probe module. So step 4's green is a real measurement and this row genuinely cleared that error.\nRESTORE proven separately: D55EAEE3.. == `git show HEAD:<path>`, both nonempty, match TRUE.\n\nRE-GREEN AFTER ALL THREE MUTANTS, in the same materialization: `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo, and `pnpm test src/v2/projects/project-manager-app.test.tsx` EXIT 0 with `Test Files 1 passed (1)` / `Tests 9 passed (9)`. No mutant leaked.\n\nNO GREEN DRILL to report on this row - both prescribed mutants reddened their named arm on the first attempt.",
      "timestamp": "2026-08-27T06:34:00.387Z"
    },
    {
      "id": "comment-8107e8f134ae404da11c11bc1fb5099b",
      "author": "worker-6246d2e0",
      "content": "STEP-6 SECURITY SUITE AND PATH ATTRIBUTION. Two materializations: BASE = ba5e8a3d (the parent of my commit; 4722 tar entries, projects/ holds EIGHT files with both app paths ABSENT, and `scope` appears ZERO times in pairing-confirmation.tsx) vs DELIVERED = 357d3635 (4724 entries, projects/ holds TEN, `scope` at lines 7/15/21/23/25). Both installed with --frozen-lockfile --prefer-offline, EXIT 0. The premises of the comparison were verified in the archives themselves, not assumed.\n\nTHE GATE IS CHAINED, so its exit code alone answers nothing: `tsc -p tests/security/tsconfig.json && vitest run --config tests/security/vitest.config.ts`. The tsc leg fails at BOTH commits, so vitest never runs under it and no count line is ever printed. I ran the vitest leg standalone at both commits to get real numbers.\n\nLEG A - chained `pnpm test:security`, EXIT 1 at both commits, with the SAME three tsc errors at both, verbatim and unchanged:\n  tests/security/project-transport-hostile-cases.ts(5,3): error TS2305: Module '\"../../apps/control-room/src/v2/projects/project-manager-client.js\"' has no exported member 'captureManagerPairingTicket'.\n  tests/security/runtime-provider-projects.security.ts(107,32): error TS2339: Property 'openTicket' does not exist on type 'ProjectRuntimeSession'.\n  tests/security/runtime-provider-projects.security.ts(129,5): error TS2322: ... Property 'approvePairing' is missing in type '...' but required in type 'ProjectStackDaemonHandle'.\nThe first is the one I disclosed on task-0602f6c2: `captureManagerPairingTicket` exists nowhere in the repository; its importer landed in b186c621 against a client version never delivered. This row does not touch it and does not change it.\n\nLEG B - vitest leg standalone. The two runs are IDENTICAL in every field:\n                                   BASE ba5e8a3d          DELIVERED 357d3635\n  Test Files                       6 failed | 5 passed (11)   6 failed | 5 passed (11)\n  Tests                            27 failed | 614 passed (641)  27 failed | 614 passed (641)\n  boundary-roster.security.ts      34 tests | 3 failed       34 tests | 3 failed\n  durable-store-boundaries         130 tests | 5 failed      130 tests | 5 failed\n  lane-smoke.security.ts           18 tests | 1 failed       18 tests | 1 failed\n  runtime-provider-projects        22 tests | 5 failed       22 tests | 5 failed\n  scheduler-activation-boundaries  159 tests | 7 failed      159 tests | 7 failed\n  transport-boundaries.security.ts 63 tests | 6 failed       63 tests | 6 failed\n\nPATH ATTRIBUTION. Failing-path set computed from both logs and diffed programmatically:\n  BASE failing files (6): boundary-roster, durable-store-boundaries, lane-smoke, runtime-provider-projects, scheduler-activation-boundaries, transport-boundaries - all under tests/security/.\n  DELIVERED failing files (6): the same six.\n  DELIVERED minus BASE = EMPTY.\n  INTERSECTION WITH THE THREE OWNED PATHS = EMPTY. Not one failing path is apps/control-room/src/v2/live/pairing-confirmation.tsx, project-manager-app.tsx or project-manager-app.test.tsx.\nDelta is zero in both directions: this row neither introduced nor cleared any security red. Note the contrast with task-0602f6c2, which DID move these numbers (it unmasked a whole uncollectable file); this row does not, and the totals sitting unchanged at 641 also confirms my nine new control-room tests are correctly outside the security config's include set.\n\nTHE COUPLING THE PLAN ASKED ME TO WATCH FOR DID NOT APPEAR. boundary-roster.security.ts:148 registers PROJECT_MANAGER_LOCAL_LAYER against project-manager-client.ts, which task-0602f6c2 already made tracked, and the plan warned that if this row moved that arm it would be an unpredicted roster/scan coupling. It did not move. The \"roster minus scan is empty\" arm (:595) received the SAME single entry at both commits:\n    [ \"PAIRING_APPROVAL_LAYER@apps/daemon/src/http/pairing-approval-window.ts\" ]\nidentical received value, identical failure, at base and delivered alike. So no security scanner enumerates control-room render surfaces or refusal copy in a way this row's three files perturb. Nothing to paper over and nothing to disclose beyond this.\n\nFOREIGN RED, present at BOTH commits, disclosed and not this row's: runtime-provider-projects.security.ts (5 failures; openTicket / approvePairing / ProjectStackDaemonHandle, owner task-67b0fee6 per the plan), scheduler-activation-boundaries.security.ts (7), durable-store-boundaries.security.ts (5), lane-smoke.security.ts (1), boundary-roster.security.ts (3, at :590/:595/:599), transport-boundaries.security.ts (6, all downstream of the missing captureManagerPairingTicket export). Never hidden, never fabricated green.\nThe daemon typecheck red the plan named as known-foreign (`affordance-read-planning.test.ts(124,25) TS2339 planningGoalRef`, owner task-48ee1455) does not appear in this gate at all - that leg is not part of `pnpm test:security`. Related and already disclosed in step 2 and step 4: the five foreign live-board planningGoalRef errors the plan expected in the control-room typecheck are now GONE from both the live worktree and the materialization, so that WIP was committed or reverted between planning and execution.\n\nThis step changed no files.",
      "timestamp": "2026-08-27T06:36:30.798Z"
    },
    {
      "id": "comment-0ca26edaed624c9c8ddc29c411873c9f",
      "author": "worker-6246d2e0",
      "content": "STEP-7 ADVERSARIAL SELF-REVIEW. Read the three files as an attacker; zero adopted bytes changed. Answering the plan's four questions in order.\n\nQ1 - CAN A HOSTILE SERVER LAUNDER A CODE THROUGH `stableRefusal` (app.tsx:44-49)? YES, within limits, and the existing arm does NOT cover it.\n    function stableRefusal(value) {\n      if (!value.ok && STABLE_NAME.test(value.code) && STABLE_NAME.test(value.layer)) {\n        return { code: value.code, layer: PROJECT_MANAGER_LOCAL_LAYER, ok: false };\n      }\n      return APP_REFUSAL;\n    }\nThe server's `code` is PRESERVED VERBATIM while the `layer` is OVERWRITTEN with the local layer. STABLE_NAME is /^[A-Z][A-Z0-9_]*$/u - no lowercase, no spaces, no punctuation, and NO LENGTH BOUND (contrast project-manager-client.ts's `text()` helper, which caps at 32_768). Consequences:\n (a) A secret cannot be laundered - real secrets carry lowercase or punctuation and fail the regex. That part holds.\n (b) An arbitrary SCREAMING_SNAKE string CAN be rendered, unbounded in length, e.g. a social-engineering code, displayed by ManagerNotice as `CODE @ CONTROL_ROOM_PROJECT_MANAGER`.\n (c) The sharper issue is ATTRIBUTION, and it cuts against epic rail 1. The layer rewrite is defensible as a refusal to let a remote claim a layer identity - but the rendered line then attributes a REMOTE-authored code to the LOCAL layer, so the layer no longer identifies who refused. A code minted by the daemon (layer PROJECT_MANAGER_HTTP) and a code minted locally are indistinguishable on screen.\nDOES THE :66 ARM COVER IT? No. That arm feeds code PROJECT_MANAGER_PAIRING_REFUSED with layer already equal to PROJECT_MANAGER_LOCAL_LAYER and asserts only that an extra `secretDetail` FIELD never renders. It never exercises a foreign layer being rewritten, nor a hostile code value, nor length. DISCLOSED FOLLOW-UPS: (1) an arm feeding `{code: \"PROJECT_MANAGER_CSRF_INVALID\", layer: \"PROJECT_MANAGER_HTTP\", ok:false}` and asserting the rendered layer becomes CONTROL_ROOM_PROJECT_MANAGER - pinning the rewrite as intended behaviour rather than an accident; (2) a length bound on `code` before render, plus an arm for an over-long code.\n\nQ2 - `claimPairing` (app.tsx:101-112) RE-ENTRY ON DOUBLE CLICK. The guard is `if (resolution.status !== \"PAIRING\" || resolution.busy) return;` where `resolution` is REACT STATE, so a second click landing before the setState commits re-enters with a stale `busy: false` and calls `pairing.claim()` twice. This is the same lag-prone shape I disclosed in ProjectBoundary on task-0602f6c2.\nIT IS SAVED BY A LOWER LAYER, and the distinction matters for anyone reading the UI guard as the protection: `ProjectManagerPairingPending.claim` (project-manager-client.ts:274-279) holds its own in-flight guard in a CLOSURE variable, `if (active !== null) return active;`, set synchronously - the correct primitive. So a double claim coalesces into one HTTP request. Defence in depth holds and there is no double pairing request.\nNOTHING ASSERTS THAT. The arm at :77 covers a claim that returns AWAITING_OPERATOR, not the coalescing. DISCLOSED FOLLOW-UP: an arm that fires onConfirm twice synchronously and asserts `claim` was called once.\nA `claim()` that resolves to AWAITING_OPERATOR forever is benign: :103-104 sets busy back to false and the UI returns to a clickable state. No loop, no leak - each claim is a bounded request under the client's 15s deadline, and the operator drives retries.\n\nQ3 - SECRETS. `confirmationLabel` is the only pairing datum that reaches the DOM. Grep across pairing-confirmation.tsx and project-manager-app.tsx for console./localStorage/sessionStorage/document.cookie/bearer/token/secret returns nothing (the single hit is the string \"cordum-tokens.css\" in an import). The module's own comment - \"The opaque pairing request identity never enters this renderable surface\" - now has a test behind it: my ARM A plants a `requestId` on the fixture and asserts `document.body.textContent` never contains it. That assertion did not exist before this row.\n\nQ4 - THE div->main CHANGE AND LANDMARK COUNT. Measured rather than reasoned. There are exactly three `<main>` elements in play: project-manager-app.tsx:64 (ManagerNotice, PENDING/REFUSED), pairing-confirmation.tsx:18 (PAIRING, introduced by the adopted hunk), project-home.tsx:209 (READY). They are the three mutually exclusive arms of a single `body` if/else chain, so exactly ONE renders at a time and the document keeps one landmark.\nTHE CHANGE IS AN IMPROVEMENT, NOT A HAZARD: before the hunk, PAIRING was the ONLY branch rendering a `div`, i.e. the pairing state had no main landmark at all while its two siblings did. The hunk makes it consistent. cordum-app.tsx contains NO `<main>` of its own (grepped), and it renders PairingConfirmation in its own exclusive branch, so no nesting or duplication arises there either.\nRESIDUAL RISK, disclosed: nothing ASSERTS the one-landmark invariant. If task-15ca5c44 composes ProjectManagerApp inside a shell that itself renders a `main`, two landmarks would coexist and no arm would catch it. Proposed row: an arm asserting `screen.getAllByRole(\"main\")` has length 1 in each of the four connection states.\n\nCONCURRENCY / SIDE-EFFECT SWEEP on the adopted app.tsx: the polling effect (:155-169) clears its timer and sets `cancelled` in its cleanup, so an unmount mid-poll leaks nothing; the `prepared` effect likewise guards on `cancelled`. `prepared` is a promise created ONCE before React, which is what stops StrictMode's double-invoke from duplicating a one-use pairing request - a deliberate design the props comment names. `refresh` catches a throwing `listProjects` and fails closed to APP_REFUSAL. No retry path double-writes.\n\nFRESH VERIFICATION AT HEAD 357d3635 (unchanged since my commit; worktree porcelain on all three owned paths is EMPTY):\n    pnpm --filter @moe/control-room typecheck   EXIT 0   ($ tsc --project tsconfig.json)\n    pnpm --filter @moe/control-room test        EXIT 0   Test Files 87 passed (87) / Tests 1105 passed (1105)",
      "timestamp": "2026-08-27T06:38:36.512Z"
    },
    {
      "id": "comment-492699172bd14d5bbde871c0a53ce457",
      "author": "worker-6246d2e0",
      "content": "DURABLE COMPLETION RECORD - task-999a363f (P1.2b-ii-b), worker-6246d2e0, 2026-08-27. Written as a COMMENT because the complete_task summary persists nowhere.\n\nDELIVERY: commit 357d3635163a958f65b4e2cbbdec3969f62484b7 on moe/work-2026-08-08, \"feat(task-999a363f): land the PairingConfirmation manager scope and the project-manager app tree\". Parent ba5e8a3d. HEAD unchanged since.\n\nTHE THREE OWNED PATHS - SHA256 and physical lines (wc -l), each verified in the worktree, the %TEMP% backup and `git show HEAD:<path>`:\n  apps/control-room/src/v2/live/pairing-confirmation.tsx        d55eaee33894085b58f7b5f6ac1442b0f38629496a3ce9740128f88fa97ff10a   47  (MODIFIED; HEAD baseline was b8f3d676.. at 41 lines)\n  apps/control-room/src/v2/projects/project-manager-app.tsx     6c5cb91ee332fc5381810eb3540a347a0bdde33b0947a07b070a472bb08b9ede  189  (NEW, adopted unchanged)\n  apps/control-room/src/v2/projects/project-manager-app.test.tsx 2dbb9781e9a65816d9e5.. (full: 2dbb9781e9a65816d9e5 prefix)  225  (NEW; adopted 182 + my 43 authored lines)\nAll three are under the 250 target. The ONLY authored bytes in this row are the two arms in project-manager-app.test.tsx; pairing-confirmation.tsx's hunk was adopted VERBATIM (its committed hash equals the step-1 worktree hash exactly) and app.tsx is byte-identical to the file task-0602f6c2 measured and deliberately left untracked.\n\n`git show --stat HEAD`: 3 files changed, 428 insertions(+), 8 deletions(-). `git show --summary HEAD`: exactly TWO `create mode 100644` lines (the two app paths); pairing-confirmation.tsx is correctly a MODIFICATION. Staging was by explicit three-path pathspec with a zero-entry index beforehand; `git diff --cached --numstat` = 3 rows (14/8, 225/0, 189/0); the staged pairing-confirmation diff showed ONLY the adopted hunk. Message via `-F` BEFORE the `--`. Index residue 0. No amend, reset, stash or sibling worktree.\n\nTRACKED-OBJECT PROOF (DoD-4). Materialization at 357d3635, cwd asserted as the repo root; 4724 tar entries against 4722 at the parent - exactly the two new files (pairing-confirmation.tsx was already an entry). The materialized projects/ holds TEN files and the materialized pairing-confirmation.tsx carries `scope` at lines 7/15/21/23/25, so the hunk is in the git object, not merely the worktree. `pnpm install --frozen-lockfile --prefer-offline` EXIT 0.\n  typecheck: `pnpm --filter @moe/control-room typecheck` EXIT 0, script echo `$ tsc --project tsconfig.json` (the only line emitted).\n  THE ERROR THIS ROW EXISTS TO CLEAR IS ABSENT: a SimpleMatch grep for `Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'` returns nothing.\n  AND THAT ABSENCE IS POSITIVELY CONTROLLED (an absence proves nothing unless the leg can emit the error at all). Deleting the `readonly scope?:` member from PairingConfirmationProps in the materialization reproduced it verbatim at the REAL production call site, not the architect's synthetic probe:\n      src/v2/projects/project-manager-app.tsx(158,5): error TS2322: Type '{ busy: boolean; confirmationLabel: string; onConfirm: () => void; scope: string; }' is not assignable to type 'IntrinsicAttributes & PairingConfirmationProps'.\n        Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'.\n      plus src/v2/live/pairing-confirmation.tsx(14,3): error TS2339.\n  test: `pnpm --filter @moe/control-room test` EXIT 0, `Test Files 87 passed (87)` / `Tests 1105 passed (1105)`. Arithmetic against task-0602f6c2's 86/1096: exactly +1 test FILE and +9 tests, which is project-manager-app.test.tsx's full arm count. Nothing else moved.\n  itemized (run from inside apps/control-room, because `pnpm --filter X test -- <path>` does not filter): `pnpm test src/v2/projects/project-manager-app.test.tsx --reporter=verbose` EXIT 0, `Test Files 1 passed (1)` / `Tests 9 passed (9)`, both new arms named.\n\nDoD-5 BACKWARD COMPATIBILITY IS PROVEN, NOT ASSERTED, and the arms are a genuine divergence pair (DoD-6). Before this row NO arm in the file reached app.tsx's PAIRING branch at all, so nothing proved the app passes `scope=\"manager\"`.\n  MUTANT 1, parameter default `scope = \"daemon\"` -> `\"manager\"` (sha D55EAEE3..->6BA9E624..): EXIT 1, ARM B \"renders the daemon pairing copy when no scope is passed\" RED with `Unable to find an accessible element with the role \"heading\" and name \"Pair this browser with Moe\"`; ARM A GREEN. Tests 1 failed | 8 passed (9).\n  MUTANT 2, call site - deleted `scope=\"manager\"` from app.tsx:158, never the prop declaration (sha 6C5CB91E..->3BC65626.., 189->188 lines): EXIT 1, ARM A \"renders the manager pairing copy when the app drives its own pairing branch\" RED with `Unable to find role=\"heading\" and name \"Pair this browser with Moe Projects\"`; ARM B GREEN. Tests 1 failed | 8 passed (9).\n  Neither mutant reddened both arms. The manager copy rides the call site, the daemon copy rides the parameter default; they are independent mechanisms, which is what the pair exists to prove.\n  RESTORES, each proven in a call SEPARATE from the restore, by SHA256 against `git show HEAD:<path>` (a git archive extraction is not a repo, so `git diff` is unavailable there): pairing-confirmation D55EAEE33894085B58F7B5F6AC1442B0F38629496A3CE9740128F88FA97FF10A and app.tsx 6C5CB91EE332FC5381810EB3540A347A0BDDE33B0947A07B070A472BB08B9EDE, every hash 64 chars so no empty-vs-empty false match, all match TRUE. Re-green after all three mutants: typecheck EXIT 0, app suite EXIT 0 at 9 passed. No mutant leaked. No green drill to report.\n\nSECURITY ATTRIBUTION (DoD-7). The gate is chained `tsc && vitest`, so its exit code alone hides everything; the vitest leg was run standalone at both commits.\n                                   BASE ba5e8a3d              DELIVERED 357d3635\n  chained gate exit                1                          1\n  tsc errors                       the same three, verbatim   the same three, verbatim\n  Test Files                       6 failed | 5 passed (11)   6 failed | 5 passed (11)\n  Tests                            27 failed | 614 passed (641)  27 failed | 614 passed (641)\n  per-file failures                3/5/1/5/7/6                3/5/1/5/7/6\n  DELIVERED failing paths MINUS BASE = EMPTY. Intersection with the three owned paths = EMPTY. Zero delta in both directions.\n  The coupling the plan told me to watch for did NOT appear: boundary-roster's \"roster minus scan is empty\" arm received the identical single entry `[ \"PAIRING_APPROVAL_LAYER@apps/daemon/src/http/pairing-approval-window.ts\" ]` at both commits.\n  Foreign red at both, disclosed verbatim in comment-8107e8f1, none of it this row's - including the missing `captureManagerPairingTicket` export I disclosed on task-0602f6c2, which drives all six transport-boundaries failures.\n\nBASELINE DRIFT DISCLOSED: the plan expected five foreign live-board typecheck errors (TS2554/TS2353 planningGoalRef, owner task-48ee1455). They are GONE from both the live worktree and the materialization, so that WIP was committed or reverted between planning and execution. My bytes add zero errors either way.\n\nSCOPE FENCE: cordum-app.tsx and main.tsx were NOT touched and are NOT in this commit (`git show --name-only HEAD` contains neither). They remain P1.2b-iii's, task-15ca5c44. cordum-app.tsx is unaffected by design - it passes no `scope` and the parameter default keeps it on the daemon copy, which ARM B now proves rather than assumes.\n\nDISCLOSED FOLLOW-UPS, none fixed here (task rail 2 - adopt bytes unchanged): (1) `stableRefusal` preserves a remote-authored `code` verbatim while rewriting the layer to the local one, so the rendered `CODE @ LAYER` attributes a remote code to the local layer, and `code` has no length bound; the :66 arm covers neither. (2) `claimPairing`'s re-entry guard is React state, so a fast double click re-enters with a stale `busy`; the real protection is the client's synchronous `active` closure at project-manager-client.ts:274-279, and nothing asserts that coalescing. (3) The one-`main`-landmark invariant is unasserted - the hunk is an a11y improvement (PAIRING previously had no landmark at all while its two sibling states did), but if task-15ca5c44 composes this app inside a shell that renders its own `main`, two landmarks would coexist uncaught.",
      "timestamp": "2026-08-27T06:39:22.732Z"
    },
    {
      "id": "comment-3f719a5d918142909bff46f98c896e2f",
      "author": "qa-bf457df2",
      "content": "QA RE-MEASUREMENT at delivered sha 357d3635 (qa-bf457df2, 2026-08-27 ~06:45Z). VERDICT: REJECT on DoD-5, on one defect only. Everything else in this row passes and is recorded below so the re-fix stays narrow.\n\nWHAT PASSES, measured, not taken from the report. Materialization `git archive 357d3635` -> %TEMP%, 4724 tar entries (= 4722 at the parent plus exactly the two new files), `pnpm install --frozen-lockfile --prefer-offline` EXIT 0.\n- DoD-1 all three gates re-measured AT THE PARENT ba5e8a3d rather than accepted: (a) `git ls-tree ba5e8a3d -- .../project-manager-app.tsx .../project-manager-app.test.tsx` returns NOTHING, no duplicate; (b) project-manager-client.ts and project-home.tsx are BOTH tracked there, so task-0602f6c2's prerequisite genuinely landed first; (c) `git show ba5e8a3d:.../pairing-confirmation.tsx | grep -n scope` is EMPTY, so the premise was open and the WIP had not been swept. PASS.\n- DoD-3 PASS: `git show --stat 357d3635` = exactly 3 paths, 428 insertions / 8 deletions, nothing else under apps/control-room; cordum-app.tsx untouched.\n- DoD-4 PASS: `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo. `pnpm --filter @moe/control-room test` EXIT 0, `Test Files 87 passed (87)` / `Tests 1105 passed (1105)` - exactly +1 file and +9 tests over the previous row's 86/1096.\n- DoD-6 drill design PASS and genuinely good: two mutants each redding exactly one arm, with the other staying GREEN, is the right asymmetry and it is what proves the arms ride independent mechanisms rather than one shared fence.\n- Your positive control for DoD-4's absence clause - deleting the props member to prove the leg CAN emit the TS2322, at the real call site rather than a synthetic probe - is exactly right. An absence assertion with no control is worth nothing, and you supplied one.\n\nTHE DEFECT. This commit ships nested `<main>` landmarks on the DAEMON pairing screen, an existing shipped path. Invalid HTML and an ARIA violation: a document may have one main landmark and `main` may never contain another.\n\nStructure, read from committed bytes at 357d3635:\n- pairing-confirmation.tsx:18 now renders `<main className=\"cr2-pairing\">`. At the parent it was `<div className=\"cr2-pairing\">`.\n- The landmark change is UNCONDITIONAL. Every other change in that hunk is gated on `scope` - the heading, the terminal sentence, the INSTANCE-id paragraph. The wrapper element is not.\n- cordum-app.tsx:192 renders `<PairingConfirmation .../>` as `body` WITHOUT scope (correctly - it is the daemon consumer), and cordum-app.tsx returns `<CordumShell>{body}</CordumShell>` unconditionally.\n- cordum-shell.tsx:179 wraps `{children}` in `<main className=\"cr2-main\">` unconditionally.\n\nMEASURED, not argued. QA probe rendered CordumApp in the PAIRING state (the same fixture as the existing cordum-app.test.tsx arm at :51-67) in a disposable materialization and counted landmarks:\n\n  QA_PROBE main_count=2 nested_main_count=1 classes=cr2-main|cr2-pairing\n  AssertionError: expected 1 to be +0\n\nCONTROL AT THE PARENT ba5e8a3d, same probe file byte-for-byte, same recipe: `Test Files 1 passed (1)` / `Tests 1 passed (1)`, EXIT 0 - nested count is ZERO there. So the regression is introduced by 357d3635 and is not pre-existing. Probe source is in this comment's sibling paragraph below; both materializations deleted after measuring, and no byte of the shared worktree was touched.\n\nProbe body, so this is reproducible rather than a claim: render `<CordumApp liveSetup={Promise.resolve({claim: vi.fn(), confirmationLabel: \"abcd-ef01-2345\", status: \"AWAITING_OPERATOR\"})} search=\"\" />`, `await screen.findByText(\"abcd-ef01-2345\")`, then `expect(document.querySelectorAll(\"main main\").length).toBe(0)`.\n\nWHY THE SUITE DID NOT CATCH IT, which is the part worth keeping. The whole control-room suite is GREEN at 87/1105 with this defect shipped, because nothing asserts landmark structure. DoD-5's two arms render PairingConfirmation STANDALONE - and a component whose only real usage is inside a shell cannot have its backward compatibility proven by a standalone render. The fixture never crosses the seam the consumer crosses, so it is structurally incapable of seeing the nesting. That is why I am calling DoD-5 rather than filing a follow-up: the clause says backward compatibility is \"proven, not asserted\", and what was proven is copy compatibility, not compatibility with the only consumer that exists.\n\nYou flagged the one-`main` invariant yourself, as a hazard for whoever takes task-15ca5c44. The finding is right; the timing is what I am correcting. It is not a future composition hazard - it is live in cordum-app at this commit.\n\nNOT YOUR FAULT, AND I WANT THAT ON THE RECORD. The plan's step 3 explicitly enumerated the `div->main / aria-labelledby move already in the worktree diff` as part of the hunk to adopt VERBATIM, and task rail 2 forbade you from re-authoring it. You followed both. The div->main move is correct for the standalone manager app - project-manager-app.tsx wraps its body in a plain `<div className=\"cr2-shell cr2-manager-root\">`, so PairingConfirmation's `<main>` is that document's only landmark and that path is right. The WIP author made the element change for the new consumer and did not gate it for the old one. This rejection is the sanctioned route to override an adopt-verbatim instruction; it is not a finding against your execution.",
      "timestamp": "2026-08-27T06:45:14.009Z"
    },
    {
      "id": "comment-0ade816e7dfb48f2af0ef5dd07c7966a",
      "author": "worker-dc706ffd",
      "content": "REWORK STEP-1 GATE + MEASUREMENT (worker-dc706ffd, 2026-08-27, HEAD 357d3635163a958f65b4e2cbbdec3969f62484b7). Second attempt after qa-bf457df2's DoD-5 reject (comment-3f719a5d). New worker seat: worker-6246d2e0 delivered attempt 1; I hold the rework.\n\nDoD-1 RE-RUN, AND ONE CLAUSE READS DIFFERENTLY ON A REWORK — stating it before QA has to.\n(a) `git ls-files <app>.tsx <app>.test.tsx` now returns BOTH paths. On a first attempt that is the duplicate trip-wire and would be a block. It is not one here: they are tracked because THIS ROW's own commit 357d3635 landed them, verified by `git log --oneline --diff-filter=A -- <both>` naming 357d3635 and nothing earlier. The clause guards against a foreign row having landed them first; that premise is still false. Not blocking.\n(b) `git ls-files apps/control-room/src/v2/projects` returns TEN paths: the eight from task-0602f6c2 (413d1573, now DONE) plus this row's two. project-manager-client.ts and project-home.tsx are both there. Prerequisite intact.\n(c) `git show HEAD:.../pairing-confirmation.tsx | grep -n scope` is now NON-EMPTY (lines 7, 15, 21, 23, 25). The gap this row existed to close is closed at HEAD by attempt 1; the rework is entirely about the DEFECT attempt 1 shipped, not about the scope member.\n\nTHE DEFECT, RE-MEASURED FROM COMMITTED BYTES RATHER THAN ACCEPTED FROM THE REJECTION. All three legs confirmed at HEAD 357d3635:\n  `git show HEAD:apps/control-room/src/v2/live/pairing-confirmation.tsx` :18 -> `<main className=\"cr2-pairing\" aria-labelledby=\"cr2-pairing-title\">`\n  `git show HEAD:apps/control-room/src/v2/shell/cordum-shell.tsx` :179 -> `<main className=\"cr2-main\" data-testid=\"cr.shell.main\" id=\"cr2-main\" ...>{children}</main>`\n  `git show HEAD:apps/control-room/src/v2/cordum-app.tsx` :192 -> `body = <PairingConfirmation busy confirmationLabel onConfirm />` with NO scope, and :214-226 returns `<CordumShell>{body}</CordumShell>` unconditionally.\nSo the daemon pairing screen nests `main` inside `main`. QA's reading is correct on the bytes. Note the shell path is `src/v2/shell/cordum-shell.tsx`, not `src/v2/cordum-shell.tsx` as the rejection wrote it — a transcription slip in the rejection, not an error in the finding.\n\nWHY ARM A IS UNAFFECTED, measured so the fix is not wider than it needs to be: ProjectManagerApp's root is `<div className=\"cr2-shell cr2-manager-root\">` (app.tsx:180), NOT a main. The only other `<main>` in app.tsx is ManagerNotice (:64), reached on PENDING/REFUSED, which are mutually exclusive with PAIRING. So under `scope=\"manager\"` PairingConfirmation's landmark is the document's only one and is correct as-is. The fix is one-sided: gate the wrapper, leave the manager path alone.\n\nNO CSS CONSUMER BREAKS. `grep -rn \"cr2-pairing\" apps/control-room/src` outside the component returns only `src/v2/styles/cordum-shell.css` (9 hits, all class selectors: `.cr2-pairing`, `.cr2-pairing-card`, ...). Zero element selectors, so `main.cr2-pairing` -> `div.cr2-pairing` changes no styling.\n\nRAIL-2 DEVIATION, DECLARED. Task rail 2 says \"Adopt bytes unchanged ... adopted VERBATIM ... not a refactor.\" This rework EDITS that adopted hunk. Authority: the rejection itself, which names rail 2 and directs the change anyway (\"Step 3 listed the div->main move as part of the hunk to adopt VERBATIM and rail 2 forbade re-authoring it. You followed both ... FIX: 1. gate the wrapper on `scope`\"). Recording it here so a later grader does not read the edit as an unauthorized refactor. Attempt 1's author is not at fault; the plan and rail told them to adopt it.\n\nSTATE AT CLAIM. Exact-path `git status --porcelain --untracked-files=all` over the three owned paths: EMPTY — all three tracked and clean. SHA256 / `wc -l` at claim, matching attempt 1's delivered record exactly:\n  pairing-confirmation.tsx        d55eaee33894085b58f7b5f6ac1442b0f38629496a3ce9740128f88fa97ff10a   47\n  project-manager-app.tsx         6c5cb91ee332fc5381810eb3540a347a0bdde33b0947a07b070a472bb08b9ede  189\n  project-manager-app.test.tsx    2dbb9781e9a65816d9e5135d8357e9e3f5cd638d13761047d378aafb63099caf  225\n%TEMP% backups of the HEAD copies, suffixed by this worker id: `C:\\Users\\Yaron\\AppData\\Local\\Temp\\moe-backup-999a363f-worker-dc706ffd\\` containing `<name>.HEAD` for each of the three.\n\nFOREIGN STATE THAT MOVED SINCE PLANNING, disclosed: the plan's risk (2) named five foreign live-board typecheck errors as the expected live-tree baseline. `pnpm exec tsc --project tsconfig.json` in the live worktree is now EXIT 0 — all five are gone, while `src/live/live-board.tsx` and `src/live/live-dispatch.ts` are both still ` M`. Someone advanced task-48ee1455's WIP in the shared worktree during this session. Not this row's, and it makes the live typecheck a green that was red an hour ago — a moving positive control, which is why step 4's materialization remains the only admissible leg.",
      "timestamp": "2026-08-27T06:54:09.359Z"
    },
    {
      "id": "comment-cd1316305601467c9253b618d06336c8",
      "author": "worker-dc706ffd",
      "content": "REWORK STEP 5 — FOUR MUTANTS PLUS A TYPECHECK CONTROL, at delivered sha 485e775f (worker-dc706ffd). Every mutation lived in the disposable materialization only; the shared worktree's three owned paths stayed tracked and CLEAN throughout, re-verified after the last drill (exact-path `git status --porcelain` EMPTY, HEAD still 485e775f).\n\nWHY FOUR AND NOT THE PLAN'S TWO. The plan's mutants (default flip, call-site delete) grade the COPY mechanisms attempt 1 landed. They cannot grade the guard this rework lands, because neither of them touches the wrapper element. Drilling only the plan's pair would have re-run a drill whose result I already knew and left the actual fix ungraded. Mutants 3 and 4 are the new guard, drilled in BOTH directions.\n\nMUTANT 3 — REVERT THE FIX (`const Wrapper = scope === \"manager\" ? \"main\" : \"div\"` -> `const Wrapper = \"main\";`). This reinstates exactly the shipped defect. Applied and confirmed non-no-op: SHA256 114e184a.. -> 47568282.. . EXIT 1, `Tests 2 failed | 8 passed (10)`, and the two reds are named by title:\n  FAIL ... task-999a363f PairingConfirmation scope > renders the daemon pairing copy when no scope is passed\n  FAIL ... task-999a363f PairingConfirmation scope > nests no second main landmark inside the daemon shell at the CordumApp seam\n    AssertionError: expected …(1) to have a length of +0 but got 1\n  v ... renders the manager pairing copy when the app drives its own pairing branch  <- GREEN\nSo the arms genuinely detect the regression QA found, and the manager path is provably unaffected by it.\n\nMUTANT 4 — THE MIRROR (`const Wrapper = \"div\";`, landmark removed everywhere). 114e184a.. -> d5ad0a23.. . EXIT 1, `Tests 1 failed | 9 passed (10)`, and the single red is the OTHER side:\n  FAIL ... renders the manager pairing copy when the app drives its own pairing branch\n    AssertionError: expected  to have a length of 1 but got +0\n  v ... renders the daemon pairing copy when no scope is passed\n  v ... nests no second main landmark inside the daemon shell at the CordumApp seam\nThis is the drill that matters most and it is the one I would have skipped a day ago. Mutant 3 alone proves the arms catch \"too many landmarks\". Only mutant 4 proves ARM A's `main` count of exactly 1 is load-bearing rather than decorative — without it, a future edit that dropped the landmark entirely would pass. The pair pins the property from both sides instead of pinning one spelling of the failure.\n\nMUTANT 1 — PARAMETER DEFAULT (`scope = \"daemon\"` -> `\"manager\"`). 114e184a.. -> 8c0b7e12.. . EXIT 1, `Tests 2 failed | 8 passed (10)`: the daemon-copy arm AND the seam arm red, manager arm green. NOTE A CHANGE FROM ATTEMPT 1, disclosed rather than glossed: attempt 1 recorded this mutant redding exactly ONE arm. It now reds two, because the seam arm is also downstream of the parameter default (CordumApp passes no scope). That is not a fixture defect — it is the truth about the mechanism, and the divergence the fixture needs is still exact, carried by mutant 2.\n\nMUTANT 2 — CALL SITE (delete `scope=\"manager\"` from project-manager-app.tsx:158; never deleted the prop declaration or the component, which would make the mutant green and read as \"no prop needed\"). 6c5cb91e.. -> 3bc65626.. , which reproduces attempt 1's recorded mutant-2 hash 3BC65626.. independently. EXIT 1, `Tests 1 failed | 9 passed (10)`: ONLY the manager arm reds; the daemon arm and the seam arm both stay GREEN. Mutant 1 and mutant 2 red disjoint arm sets, so call site and default are proven to be independent mechanisms.\n\nTYPECHECK ABSENCE CONTROL. DoD-4 asks the TS2322 to be ABSENT; an absence proves nothing unless the leg can emit it. Deleting `readonly scope?: \"daemon\" | \"manager\" | undefined;` from the props interface (114e184a.. -> 7be53e1d..) reproduced it verbatim at the REAL production call site: `src/v2/projects/project-manager-app.tsx(158,5): error TS2322 ... Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'`, EXIT 1. So step 4's EXIT 0 is a real green, not a leg that never ran.\n\nRESTORES, each proven in a call SEPARATE from the restore, by SHA256 against the pristine `git show 485e775f:<path>` copy, with both hashes asserted NONEMPTY (two empty hashes compare equal and print match:true):\n  after mutant 3: 114e184a808ab72379e099adf00fdd92c3a42e8097b99be450b5a83086f05610 == pristine, both 64 chars, MATCH TRUE\n  after mutant 4: same hash, MATCH TRUE\n  after mutant 1: same hash, MATCH TRUE\n  after mutant 2: project-manager-app.tsx 6c5cb91ee332fc5381810eb3540a347a0bdde33b0947a07b070a472bb08b9ede == pristine, MATCH TRUE\n  after the typecheck control: same pairing hash, MATCH TRUE\nA `git archive` extraction is not a git repository, so `git diff` is unavailable inside it — hence the SHA256-against-`git show` form. Every mutation was applied through a helper that asserts the anchor occurs EXACTLY ONCE and that the file hash CHANGED, so a silent no-op write could not have been recorded as a green drill.\n\nNO GREEN DRILL. All four mutants redded, each redding the arm whose title claims that behavior and no neighbour.",
      "timestamp": "2026-08-27T06:59:44.975Z"
    },
    {
      "id": "comment-bb28ee0b637545c3b0c600bb1ff98f2a",
      "author": "worker-dc706ffd",
      "content": "REWORK STEP 6 — SECURITY SUITE AND PATH ATTRIBUTION (worker-dc706ffd). TWO materializations, both premises verified inside the archives rather than assumed: BASE = 357d3635 (the parent of my commit, 4724 tar entries) and DELIVERED = 485e775f (4724 entries — this commit modifies two existing entries and creates none, so the counts are correctly equal). Both `pnpm install --frozen-lockfile --prefer-offline` EXIT 0.\n\nTHE GATE IS CHAINED, so its exit code alone answers nothing: `test:security` = `tsc -p tests/security/tsconfig.json && vitest run --config tests/security/vitest.config.ts`. Run standalone and unpiped at DELIVERED, EXIT 1, and `grep -c \"Test Files \"` on its output is ZERO — the tsc leg fails first, so the vitest half NEVER RUNS under the composed script and no count line is ever printed. Any clause citing a `test:security` count is citing an unexecuted suite.\n\nTSC LEG at DELIVERED, EXIT 1, all three errors verbatim and all three FOREIGN:\n  tests/security/project-transport-hostile-cases.ts(5,3): error TS2305: Module '\"../../apps/control-room/src/v2/projects/project-manager-client.js\"' has no exported member 'captureManagerPairingTicket'.\n  tests/security/runtime-provider-projects.security.ts(107,32): error TS2339: Property 'openTicket' does not exist on type 'ProjectRuntimeSession'.\n  tests/security/runtime-provider-projects.security.ts(129,5): error TS2322: Type '() => Promise<Readonly<{ issuePairingTicket: ...; }>>' is not assignable to type '() => Promise<ProjectStackDaemonHandle | ProjectStackRefused>'.\nByte-identical to attempt 1's measurement at ba5e8a3d and 357d3635. The first is the `captureManagerPairingTicket` gap worker-6246d2e0 disclosed on task-0602f6c2 (the symbol exists nowhere in the repository; its importer landed at b186c621 against a client version never delivered). The other two are task-67b0fee6's. This row touches none of them.\n\nVITEST LEG STANDALONE at both commits. The two runs are IDENTICAL in every field:\n                                        BASE 357d3635              DELIVERED 485e775f\n  Test Files                            6 failed | 5 passed (11)   6 failed | 5 passed (11)\n  Tests                                 27 failed | 614 passed (641)  27 failed | 614 passed (641)\n  boundary-roster.security.ts           34 tests | 3 failed        34 tests | 3 failed\n  durable-store-boundaries.security.ts  130 tests | 5 failed       130 tests | 5 failed\n  lane-smoke.security.ts                18 tests | 1 failed        18 tests | 1 failed\n  runtime-provider-projects.security.ts 22 tests | 5 failed        22 tests | 5 failed\n  scheduler-activation-boundaries...ts  159 tests | 7 failed       159 tests | 7 failed\n  transport-boundaries.security.ts      63 tests | 6 failed        63 tests | 6 failed\n\nPATH ATTRIBUTION.\n  BASE failing files (6): boundary-roster, durable-store-boundaries, lane-smoke, runtime-provider-projects, scheduler-activation-boundaries, transport-boundaries — all under tests/security/.\n  DELIVERED failing files (6): the same six, with the same per-file failure counts.\n  DELIVERED minus BASE = EMPTY.\n  INTERSECTION WITH THE THREE OWNED PATHS = EMPTY. Not one failing path is under apps/control-room/src/v2/.\n\nTHE ROSTER/SCAN COUPLING THE STEP ASKED ABOUT DID NOT MOVE. boundary-roster.security.ts is 3 failed at BOTH commits, unchanged. Its :148 entry registers PROJECT_MANAGER_LOCAL_LAYER against project-manager-client.ts, which task-0602f6c2 already made tracked; this row changes a wrapper ELEMENT, not a layer constant or an export surface, so nothing it does is visible to a roster scan. Predicted, then measured, and the prediction held.\n\nDAEMON TYPECHECK, run at DELIVERED for completeness of the epic's path-attribution discipline: `pnpm --filter @moe/daemon typecheck` EXIT 1 with the script echo `$ tsc --project tsconfig.json` and exactly the two known foreign errors, verbatim:\n  src/http/affordance-read-planning.test.ts(124,25): error TS2339: Property 'planningGoalRef' does not exist on type 'AffordanceSurface'.\n  src/http/affordance-read-planning.test.ts(125,25): error TS2339: Property 'planningGoalRef' does not exist on type 'AffordanceSurface'.\nOwner task-48ee1455 (claimed by worker-6246d2e0 at 06:47). Unchanged from BASE and disjoint from this row's owned paths. Disclosed, never edited, never blocked on.\n\nNOTHING FABRICATED GREEN: this row's security posture is \"no delta\", not \"clean\". The suite is red at both commits for six foreign reasons and I am recording it that way.",
      "timestamp": "2026-08-27T07:01:48.492Z"
    },
    {
      "id": "comment-3756afa3fa23401dadb21f5d5d92f768",
      "author": "worker-dc706ffd",
      "content": "REWORK STEP 7A — ADVERSARIAL SELF-REVIEW. It found a real regression IN MY OWN FIX and I fixed it rather than disclosing it, so the delivered sha moved from 485e775f to 95fde3e0. Recording the finding because a self-caught defect of the same family as the one QA caught is worth more to the next reader than a clean-looking record.\n\nTHE FINDING — `aria-labelledby` ON A GENERIC WRAPPER IS INERT. 485e775f gated the wrapper element on `scope` but left `aria-labelledby=\"cr2-pairing-title\"` on it. Under the daemon scope the wrapper is now a `div`, and ARIA prohibits naming a `generic` element, so the daemon pairing screen carried a dead attribute and LOST the accessible name 357d3635 had given it. Same failure shape as the rejection: a change applied to the wrapper without asking what the wrapper had become on the other branch. My landmark assertions could not see it — they count `main` elements, and the name is a different property.\n\nTHE PARENT ALREADY HAD IT RIGHT, which is how I found it. `git show ba5e8a3d:...pairing-confirmation.tsx`:\n  :16  <div className=\"cr2-pairing\">\n  :17  <section className=\"cr2-pairing-card\" aria-labelledby=\"cr2-pairing-title\">\nA `section` WITH an accessible name is a `region` landmark and can carry a name on BOTH branches; a bare wrapper cannot. So the name goes back on the section (95fde3e0) and the wrapper keeps only its class. The daemon path is now structurally identical to the parent; the manager path keeps its `main` with the named region inside it, and an unnamed `main` is standard because there is only one.\n\nPUSHBACK ON ONE LINE OF THE REJECTION, with reasoning rather than silence. It said \"aria-labelledby on whichever wraps\". That holds only while the wrapper is nameable, which stopped being true the moment the daemon branch became a `div`. Implemented the placement that stays valid on both branches instead. @qa-bf457df2 if you want it gated onto the wrapper per-branch instead, say so and I will change it — but the section form needs no branch at all.\n\nNOW PINNED SO IT CANNOT REGRESS SILENTLY. Both arms assert the name lands on the region: `getByRole(\"region\", { name: \"Pair this browser with Moe Projects\" })` on the manager arm, the daemon spelling on the other. MUTANT 5 (move the attribute back onto the wrapper — i.e. reproduce 485e775f exactly; the mutated file hashes to 114e184a.., which IS 485e775f's blob) reds BOTH arms:\n  FAIL ... renders the manager pairing copy ... -> Unable to find an accessible element with the role \"region\" and name \"Pair this browser with Moe Projects\"\n  FAIL ... renders the daemon pairing copy ...  -> Unable to find an accessible element with the role \"region\" and name \"Pair this browser with Moe\"\n  Tests 2 failed | 8 passed (10)\nSTATED PRECISELY, because only one of those two reds is a real-defect red: on the MANAGER path 485e775f was not broken — `main` is nameable, so the name existed, just on a different element. That arm reds because it pins WHERE the name lives, not merely that it exists. The daemon red is the actual regression. I would rather say that than let a two-red drill imply two defects.\n\nSECOND CHANGE FROM THE SAME REVIEW — AN ASSERTION THAT WOULD HAVE REDDENED A PEER'S ROW. The seam arm asserted `document.querySelector(\"main\")?.className === \"cr2-main\"`. task-15ca5c44 edits `shell/cordum-shell.tsx` next; adding any class there would have redded MY arm for THEIR change, and the failure would have named my row. Changed to `classList.contains(\"cr2-main\")`, which still proves the surviving landmark is the shell's rather than the component's without pinning a peer row's class list. The dirty shell copy currently still reads exactly `className=\"cr2-main\"`, so this was pre-emptive, not a live red.\n\nCHECKLIST ITEMS THAT CAME BACK CLEAN, so QA knows they were walked rather than skipped:\n- CONCURRENCY: pure render, no shared state, no check-then-act, no IO, no handles. N/A.\n- INPUTS: `scope` is a typed union with a destructuring default, so explicit `undefined` takes the default. An untyped JS caller passing garbage fails `=== \"manager\"` and lands on `div` + daemon copy — it fails toward the NESTING-SAFE branch, which is the correct direction.\n- CALL SITES: `git grep -n PairingConfirmation 95fde3e0 -- apps/control-room/src` returns exactly two production consumers, cordum-app.tsx:192 (no scope) and project-manager-app.tsx:154 (literal `scope=\"manager\"`). No third consumer can be surprised.\n- CSS: every `cr2-pairing` selector in cordum-shell.css is a class selector, zero element selectors, so `main.cr2-pairing` -> `div.cr2-pairing` changes no styling.\n- SKIP LINK: cordum-shell's `href=\"#cr2-main\"` targets the shell's own `main id=\"cr2-main\"`, untouched; the daemon pairing content now sits inside it as content rather than as a rival landmark.\n\nDISCLOSED FOLLOW-UP, not fixed here (proposed row, no bytes changed): changing the ELEMENT TYPE on a prop means React unmounts and remounts the subtree if `scope` ever flips on a mounted instance — focus inside the `output`/button would be lost. Both current call sites pass a constant, so this is unreachable today. It becomes real only if some future caller drives `scope` from state; a row that makes `scope` dynamic should add an arm for it.",
      "timestamp": "2026-08-27T07:06:26.567Z"
    },
    {
      "id": "comment-5d0a7b0f1d454316bbe114e599372552",
      "author": "worker-dc706ffd",
      "content": "DURABLE COMPLETION RECORD — task-999a363f REWORK (worker-dc706ffd, 2026-08-27). Written as a comment because complete_task's summary persists nowhere on the task record.\n\nDELIVERY: TWO commits on moe/work-2026-08-08, both by explicit two-path pathspec.\n  485e775fbbe4a3dd32e33cc64581721324eb6ad6  fix(task-999a363f): gate the pairing landmark on scope so the daemon shell keeps one main   (parent 357d3635)\n  95fde3e00a15457fed2b1a1eec3cafd5faec46c8  fix(task-999a363f): move the pairing accessible name off the generic wrapper              (the self-review fix)\nThe second exists because my own adversarial review found a regression the first introduced (comment-3756afa3). GRADE AT 95fde3e0.\n\nOWNED PATHS at 95fde3e0 — SHA256 and physical lines (`wc -l`; PowerShell Measure-Object undercounts):\n  apps/control-room/src/v2/live/pairing-confirmation.tsx          1ea1d2e7ec043e1a354c93b2cfa2924c8be4eb105885682670de1e7d43b25481   50\n  apps/control-room/src/v2/projects/project-manager-app.tsx       6c5cb91ee332fc5381810eb3540a347a0bdde33b0947a07b070a472bb08b9ede  189  (UNTOUCHED, byte-identical to attempt 1)\n  apps/control-room/src/v2/projects/project-manager-app.test.tsx  8e23d5c8c27b74792e8349b0019307a5239c6a9e27e1e75c326f9418ce28e87f  263\n`git diff 357d3635..95fde3e0 --stat` = 2 files, 47 insertions, 6 deletions. Only two of the three owned paths changed — a SUBSET, never an excess. test.tsx is 263 lines: over the 250 target, under the 400 bar, disclosed.\n\nNOT TOUCHED AND STILL FOREIGN, verified by exact-path porcelain after the last drill: cordum-app.tsx, cordum-app.test.tsx and shell/cordum-shell.tsx are all still ` M` with their WIP intact; main.tsx is clean. All belong to task-15ca5c44 (P1.2b-iii). Nothing of theirs entered either commit. `git diff --cached --numstat` is EMPTY after both commits — zero index residue, so the shared index is not left staged with stale blobs.\n\nGATES, all in a `git archive 95fde3e0` materialization (4724 tar entries, cwd pinned to the repo root), `pnpm install --frozen-lockfile --prefer-offline` EXIT 0. Each leg standalone, redirected not piped, own exit code.\n  LEG 1  `pnpm --filter @moe/control-room typecheck`  EXIT 0, script echo `$ tsc --project tsconfig.json` (its only output line).\n         The row's TS2322 is ABSENT (`grep -c` -> 0), and that absence is POSITIVELY CONTROLLED: deleting the props member reproduced it verbatim at the real call site, `src/v2/projects/project-manager-app.tsx(158,5): error TS2322 ... Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'`, EXIT 1.\n  LEG 2  `pnpm --filter @moe/control-room test`  EXIT 0, `$ vitest run`, `Test Files 87 passed (87)` / `Tests 1106 passed (1106)`. Reconciles exactly against attempt 1's 87/1105: +1 test (the seam arm), file count unchanged.\n  LEG 3  itemized, from inside apps/control-room (`pnpm --filter X test -- <path>` does not filter): `pnpm exec vitest run src/v2/projects/project-manager-app.test.tsx --reporter=verbose` EXIT 0, `Test Files 1 passed (1)` / `Tests 10 passed (10)`, all three scope arms named — proof they EXECUTE from tracked objects, not merely exist in them.\n\nTHE DEFECT IS CLOSED. QA's probe measured `main_count=2 nested_main_count=1` at 357d3635. The committed seam arm now asserts that property permanently: CordumApp rendered in PAIRING, `querySelectorAll(\"main main\")` empty, `main` exactly 1, and the survivor carries `cr2-main` (the shell's). It is a committed test arm, not a deleted probe.\n\nDRILLS — FIVE MUTANTS, ALL RED, none green. Full transcripts in comment-cd131630 (M1-M4 at 485e775f) and comment-3756afa3 (M5).\n  M3 revert the gate (Wrapper -> always \"main\")  -> daemon-copy arm + seam arm RED, manager arm GREEN.  2 failed | 8 passed (10)\n  M4 the mirror (Wrapper -> always \"div\")        -> manager arm RED only (\"expected to have a length of 1 but got +0\"); both daemon arms GREEN.  1 failed | 9 passed (10)\n  M5 aria back on the wrapper (= 485e775f's blob, 114e184a..) -> both region arms RED on \"Unable to find ... role region and name ...\".  2 failed | 8 passed (10)\n  M1 parameter default flip -> daemon arm + seam arm RED, manager GREEN. (Attempt 1 recorded ONE red here; it is two now because the seam arm is also downstream of the default. Disclosed, not a fixture defect.)\n  M2 call-site `scope=\"manager\"` deleted -> manager arm RED only; hash 3bc65626.. independently reproduces attempt 1's. M1 and M2 red DISJOINT arm sets, so call site and default are proven independent.\n  M3/M4 are the pair that matters: one proves the arms catch \"too many landmarks\", the other proves ARM A's count-of-exactly-1 is load-bearing rather than decorative. Pinning one direction only would let a future edit drop the landmark entirely and stay green.\n  Every mutation went through a helper asserting the anchor occurs EXACTLY ONCE and the file hash CHANGED, so no silent no-op could read as a green drill. Every restore proven in a call SEPARATE from the restore, SHA256 against the pristine `git show <sha>:<path>`, both hashes asserted nonempty and 64 chars, MATCH TRUE each time. `git diff` is unavailable inside an archive extraction, hence the hash form.\n\nSECURITY ATTRIBUTION at 95fde3e0 vs BASE 357d3635 — IDENTICAL in every field: `6 failed | 5 passed (11)`, `27 failed | 614 passed (641)`, same six files with the same per-file counts. DELIVERED minus BASE = EMPTY; intersection with the owned paths = EMPTY. The composed `test:security` is `tsc && vitest` and EXITS 1 with ZERO count lines, so the vitest leg was run standalone to get real numbers. Three foreign tsc errors quoted verbatim in comment-bb28ee0b (TS2305 `captureManagerPairingTicket`, and two in runtime-provider-projects.security.ts owned by task-67b0fee6). Daemon typecheck EXIT 1 with exactly the two known foreign `affordance-read-planning.test.ts(124,25)/(125,25) TS2339 planningGoalRef` errors, owner task-48ee1455. All disclosed, none edited, none fabricated green — this row's posture is \"no delta\", not \"clean\".\n\nRAIL-2 DEVIATION, declared once more so it is not read as an unauthorized refactor: task rail 2 forbids editing the adopted hunk. The rejection names that rail and directs the change anyway; I proceeded on the rejection as the adjudication (comment-0ade816e).",
      "timestamp": "2026-08-27T07:07:07.043Z"
    },
    {
      "id": "comment-c4105ef68c624fae9626521975215085",
      "author": "qa-bf457df2",
      "content": "QA RE-VERIFICATION of the reject fix, at sha 95fde3e0 (qa-bf457df2, 2026-08-27 ~07:10Z). VERDICT: APPROVE. Prior evidence in comment-3f719a5d; this comment covers only what changed.\n\nSCOPE OF THE FIX. Two commits: 485e775f gates the landmark, 95fde3e0 moves the accessible name. `git diff --name-only ba5e8a3d 95fde3e0` = exactly the three owned paths, cumulative 468 insertions / 7 deletions. Nothing else entered.\n\nTHE REJECTED DEFECT IS CLOSED. `const Wrapper = scope === \"manager\" ? \"main\" : \"div\"` at pairing-confirmation.tsx:19. Re-ran MY OWN probe - the same file that failed at 357d3635 - in a `git archive 95fde3e0` materialization (4724 entries, install EXIT 0), strengthened to assert the values rather than log them: nested `main main` count 0, total `main` count 1, that main's className exactly `cr2-main`, and the named region present. EXIT 0, `Tests 1 passed (1)`. Against the original failure at 357d3635 (`main_count=2 nested_main_count=1 classes=cr2-main|cr2-pairing`) that is a clean close.\n\nTHE NEW ARMS ARE NOT VACUOUS - I DRILLED THEM RATHER THAN READING THEM. One degree of freedom, the gate itself: `const Wrapper = scope === \"manager\" ? \"main\" : \"div\"` -> `const Wrapper = \"main\"`, i.e. exactly the pre-fix behaviour, without deleting the wrapper or the prop (a deletion would make the mutant green and read as \"no gate needed\"). Result:\n  Tests 2 failed | 8 passed (10)\n  FAIL ... > renders the daemon pairing copy when no scope is passed\n  FAIL ... > nests no second main landmark inside the daemon shell at the CordumApp seam\n  AssertionError: expected <main class=\"cr2-pairing\">...(1)</main> to have a length of +0 but got 1\nExactly the two arms whose TITLES claim that behaviour red, and ARM A - the manager arm, which still legitimately has one main - stays GREEN. That asymmetry is what proves the arms ride the gate and not some shared fence. Restore proven in a separate call: backup, restored file and `git show 95fde3e0:<path>` all SHA256 `1ea1d2e7ec043e1a354c93b2cfa2924c8be4eb105885682670de1e7d43b25481`, nonempty on all three sides.\n\nGATES AT THE NEW SHA. `pnpm --filter @moe/control-room typecheck` EXIT 0 with the `$ tsc --project tsconfig.json` echo. `pnpm --filter @moe/control-room test` EXIT 0, `Test Files 87 passed (87)` / `Tests 1106 passed (1106)` - exactly +1 over the rejected sha's 1105, which is the seam arm and nothing else. No file count change, so no suite was silently added or dropped.\n\nTHE SECOND DEFECT THE WORKER FOUND ON THEIR OWN, WHICH IS THE BEST PART OF THIS FIX. 485e775f alone would have satisfied my rejection and shipped a NEW silent regression: `aria-labelledby` was still on the `Wrapper`, and on a bare `<div>` with no role that attribute names nothing. The daemon path would have lost its accessible name entirely - and no arm would have caught it, because my rejection only asked about landmark counts. 95fde3e0 moves the name back onto the `<section>`, which carries it as a `region`, and adds `getByRole(\"region\", { name: ... })` on BOTH arms so the name is now pinned in each scope. I verified the daemon-side name independently in my probe (`Pair this browser with Moe` resolves as a named region at 95fde3e0). That is a defect my rejection did not name and would not have caught; finding it required reading the fix as an attacker rather than just satisfying the reject text.\n\nAlso worth keeping: the seam arm's final assertion was deliberately loosened from `className === \"cr2-main\"` to `classList.contains(\"cr2-main\")`, with a comment saying task-15ca5c44 edits that shell next and an added class must not red an arm it does not own. That is the correct direction for a cross-seam assertion - pin the property you depend on, not the whole rendered string. It is the same lesson as the CSP `not.toContain` finding on task-c07f64a9, applied in the other direction.\n\nRAIL-2 DEVIATION, ADJUDICATED AND ON THE RECORD. This row's rail 2 said adopt the hunk verbatim; the fix re-authors it. That deviation is authorised by this QA rejection, which outranks a plan-step instruction, and is scoped to the wrapper element and its `aria-labelledby`. A later grader reading `git log` alone would otherwise see re-authored bytes on an adopt-verbatim row.\n\nMaterializations deleted. The shared worktree was never written to across either review of this row.",
      "timestamp": "2026-08-27T07:10:53.399Z"
    },
    {
      "id": "comment-0e18d438283a4d5992b48e53d2a77720",
      "author": "worker-dc706ffd",
      "content": "ATTRIBUTION CORRECTION TO THE DAEMON-TYPECHECK DISCLOSURE (worker-dc706ffd, 2026-08-27). Raised by worker-6246d2e0 (msg-9f603863); re-measured here at delivered HEAD 95fde3e0 rather than accepted. The correction does not change this row's verdict — the intersection with owned paths is EMPTY either way — but it changes which attribution RULE applies, so it belongs on the row where a grader will read it.\n\nWHAT I WROTE, in the completion record and in chat: the two `affordance-read-planning.test.ts(124,25)/(125,25) TS2339 planningGoalRef` errors are foreign, \"owner task-48ee1455\", and in chat I phrased that as worker-6246d2e0's row. That phrasing reads as UNCOMMITTED PEER WIP redding my gate. It is not.\n\nRE-MEASURED AT HEAD 95fde3e0, THREE COMMANDS, ALL CONFIRMING WORKER-6246D2E0:\n  `git grep -n planningGoalRef HEAD -- apps/daemon/src` -> exactly two lines, both in the TEST:\n      :124  expect(boundSurface.planningGoalRef).toBe(DEFAULT_GOAL_SUBJECT);\n      :125  expect(boundSurface.planningGoalRef).not.toBe(DECOY_GOAL_SUBJECT);\n  `git grep -n planningGoalRef HEAD -- 'apps/daemon/src/**/*.ts' ':!*test*'` -> NOTHING. The producer is absent daemon-wide at HEAD.\n  `git log -1 -S planningGoalRef -- .../affordance-read-planning.test.ts` -> `1214c5b3 feat(task-9d86234a): compose the authenticated durable goal-brief writer`.\n\nCORRECTED LABEL: the TS2339 pair is a STANDING COMMITTED RED-FIRST TEST, authored by task-9d86234a at 1214c5b3, which landed its assertion ahead of its producer and is still committed that way. task-48ee1455 is the row that CLEARS it, not the row that caused it.\n\nWHY THE DISTINCTION MATTERS TO GRADING. Uncommitted peer WIP does not hold a row; a COMMITTED foreign red does, and must be carried as a standing red with a named owner rather than as \"a peer is mid-TDD, re-run later\". My own evidence already proved it is the committed kind and I did not say so: a `git archive` materialization contains committed bytes only, so no dirty worktree could have reached the leg that produced those two errors. Both spellings attribute the red away from this row; only the corrected one survives a re-measure.\n\nNOTHING ELSE MOVES. Owned-path intersection still EMPTY, security delta still EMPTY, this row's legs still EXIT 0. The `verification` field recorded at complete_task says \"owner task-48ee1455\", which remains true as the CLEARING owner; this comment is the precise form and supersedes the chat phrasing.\n\nSECOND-ORDER FACT worth carrying, from worker-6246d2e0 and consistent with the governor's 06:17:19Z post (msg-7592a944): task-48ee1455 is BLOCKED because the directive reserves `apps/daemon/src/http/affordance-*` verbatim, which matches both of that row's owned paths. So this red will NOT clear on its own timeline — any row gating on daemon typecheck between now and the governor's DONE should expect it and attribute it, not wait it out.",
      "timestamp": "2026-08-27T07:11:38.321Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-27T05:58:08.991Z",
  "updatedAt": "2026-08-30T19:56:16.081Z",
  "metrics": {
    "firstClaimAt": "2026-08-27T06:14:38.961Z",
    "plannedStepCount": 7,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 14,
    "rejectCount": 1,
    "reopenCount": 1,
    "doneAt": "2026-08-27T07:11:19.171Z",
    "wallClockMs": 3400210
  },
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7"
  ],
  "failedDodItems": [
    {
      "item": "DoD-5 — backward compatibility of the optional prop is proven, not asserted. Both arms render PairingConfirmation STANDALONE, so they cannot see that the only pre-existing consumer nests it inside CordumShell's <main>. Copy compatibility was proven; consumer compatibility was not, and it is broken.",
      "rejectedAt": "2026-08-27T06:47:23.701Z",
      "rejectedBy": "qa-bf457df2"
    }
  ],
  "planSubmittedAt": "2026-08-27T06:17:36.149Z",
  "planningNotes": {
    "approachesConsidered": "WHERE THE NEW ARMS LIVE. Three options. (1) A new `apps/control-room/src/v2/live/pairing-confirmation.test.tsx`: REJECTED — it would be a fourth owned path in a row deliberately scoped to three, and pairing-confirmation.tsx has no test file today, so creating one imports a maintenance surface this row is not chartered to own. (2) Arms in the eight-path tree landed by task-0602f6c2: REJECTED — those files are another row's committed bytes. (3) CHOSEN: both arms go in project-manager-app.test.tsx, which this row owns and which is the natural home because app.tsx is the ONLY consumer of the `scope` prop. WHY TWO ARMS AND NOT ONE. A single arm asserting the manager copy would be satisfied either by app.tsx passing scope=\"manager\" OR by the component defaulting to manager — one arm cannot separate call site from default. The pair does: mutant 1 (flip the default) reds only arm B, mutant 2 (delete the call-site prop) reds only arm A. That asymmetry is the divergence fixture epic rail 7(A) demands, and it is constructible here because the two mechanisms are genuinely distinct code locations in two different files. WHY NOT TDD-RED-FIRST IN THE USUAL SHAPE. This is an adopt-and-track row: the production bytes already exist and are correct, so there is no failing-then-passing sequence to stage. The red proof is relocated to step 5, where each authored arm must be shown to red under exactly its own mutant — that is the same guarantee, taken after the bytes are tracked rather than before they are written.",
    "codebaseInsights": "Measured by architect-e0ebd2e5 on 2026-08-27; HEAD MOVED during this session from 61e3dc5b to 364a83f0 — re-read HEAD before and after any materialization, the branch is shared. PREREQUISITE NOW SATISFIED: commit 413d1573 `feat(task-0602f6c2): ...` landed all eight leaf paths, so `git ls-files apps/control-room/src/v2/projects` returns 8 tracked files and project-manager-app.{tsx,test.tsx} are the only two `??` left there. THE GAP IS STILL OPEN: `git show HEAD:apps/control-room/src/v2/live/pairing-confirmation.tsx | grep scope` is EMPTY; the worktree copy is ` M` and carries scope at lines 7 (`readonly scope?: \"daemon\" | \"manager\" | undefined;`), 15 (`scope = \"daemon\",`), 21, 23, 25. The same worktree hunk also moves the root element from `div` to `main` and relocates `aria-labelledby` onto it — that rides along and is adopted verbatim; it is not this row's authorship. CONSUMER CONTRACT: `ProjectManagerPairingPending` at project-manager-client.ts:65-69 is `{ claim(): Promise<ProjectManagerConnection>; readonly confirmationLabel: string; readonly status: \"AWAITING_OPERATOR\" }` — trivially fakeable, no transport needed to reach PAIRING. app.tsx:88-91 routes it via `isPairingPending` (a `status === \"AWAITING_OPERATOR\"` check) into `{ busy: false, pairing, status: \"PAIRING\" }`, and app.tsx:154-159 renders `<PairingConfirmation busy confirmationLabel onConfirm scope=\"manager\" />`. COVERAGE HOLE THIS ROW CLOSES: project-manager-app.test.tsx has 8 arms across 2 describe blocks and NOT ONE reaches PAIRING — the arm titled \"shows only the stable refusal code and layer when pairing or connection refuses\" (line 66) drives the REFUSED branch. `claimPairing` (app.tsx:101-112) is entirely uncovered. BACKWARD COMPATIBILITY: cordum-app.tsx:205 is the only other call site, passes no scope at HEAD and in the worktree, and is P1.2b-iii's file (task-15ca5c44) — not touched here. Its safety rests on the parameter default, which arm B pins. OTHER EDGES ALL SATISFIED AT HEAD: app.tsx also imports ../cordum-fonts.js and ../styles/cordum-{tokens,shell}.css (tracked); app.test.tsx imports ClockProvider from ../../performance/command-latency.js, which resolves to apps/control-room/src/performance/command-latency.tsx — NOT src/v2/performance, which does not exist — tracked at HEAD, exporting `ClockProvider(props: { children: ReactNode; clock: Clock })`, matching the harness's `<ClockProvider clock={{ now: () => 0 }}>` at app.test.tsx:51. TOOLING: @moe/control-room = apps/control-room via the `apps/*` glob; `test: vitest run`, `typecheck: tsc --project tsconfig.json`; vite.config.ts sets jsdom, include src/**/*.test.ts(x), passWithNoTests:false, sequence.concurrent:false; tsconfig includes src/**/*.ts(x) so both adopted files are typechecked by the package leg; tsconfig.base.json has strict, exactOptionalPropertyTypes, verbatimModuleSyntax, noUnusedLocals. LINES: pairing-confirmation.tsx 47, app.tsx 189, app.test.tsx 182 — all under the 250 target, so the two added arms have ample headroom. OWNERSHIP: file-field scan of task-e868ea33, task-dc934111 and task-15ca5c44 returned ZERO hits on these stems; prose mentions are not claims. GENERATED FAN-OUT (epic rail 7B): nothing is generated from these three paths; boundary-roster.security.ts:148 registers PROJECT_MANAGER_LOCAL_LAYER against project-manager-client.ts, which task-0602f6c2 already tracked, so no regenerated artifact or rotated digest is in scope — step 6 is what would catch that assumption being wrong.",
    "risks": "(1) THE ROW'S REASON FOR EXISTING is a typecheck error that only appears against tracked HEAD: `error TS2322 ... Property 'scope' does not exist on type 'IntrinsicAttributes & PairingConfirmationProps'`. The live worktree cannot reproduce it, because the worktree copy of pairing-confirmation.tsx already carries the prop. Do not conclude from a green worktree run that the gap was imaginary — step 4's grep against the materialization output is the only place that claim can be settled. (2) BASELINE RED, foreign, disclosed: `pnpm --filter @moe/control-room typecheck` is EXIT 1 in the live worktree with 5 errors, all in ` M` src/live/live-board.tsx (63,132) and src/live/live-board-dispatch.test.tsx (164,172,181), TS2554 and TS2353 `planningGoalRef`, producer task-48ee1455. Their HEAD copies do not carry the WIP, so the materialization should be clean of them; if it is not, that WIP was committed in the interim and it is still not this row's. (3) The session-end autocommit sweeps deliberately-dirty foreign files under the completing row's id and this repo carries ~442 dirty paths at rest — keep every step's affectedFiles a strict subset of the three, commit by explicit pathspec, and if a foreign commit captures these bytes first do not amend/reset/empty-commit. (4) pairing-confirmation.tsx is a TRACKED file being MODIFIED, unlike every other path in this family — `git add` on it stages a diff, not a creation, so `git diff --cached` on that one path must be read hunk by hunk to confirm nothing beyond the adopted scope hunk went in. (5) `git archive` roots at cwd — pin cwd to the repo root or the gate greens on a fraction of the tree; a `git archive` extraction is NOT a git repo, so `git diff` is unavailable inside it and restores must be proven by SHA256 with a nonempty-hash assertion. (6) A `pnpm --filter` leg on an unresolved package exits 0 with no test-count line — read the counts, not the exit code; and quote the `$ tsc` echo, since a sub-second silent green is indistinguishable from a leg that never ran. (7) `git commit -m` AFTER a `--` is parsed as a pathspec and lands nothing — message first. (8) Windows: the sandbox blocks `Remove-Item -LiteralPath` inside a larger script and blocks `cmd /c rmdir /s /q`; use `rm -rf` via the Bash tool or a minimal standalone command for materialization cleanup. (9) STOP CONDITIONS: report blocked if either app path is already tracked (duplicate), if project-manager-client.ts or project-home.tsx is not tracked (prerequisite regressed), or if the worktree scope hunk has vanished (WIP swept — never re-author it).",
    "keyFiles": [
      "apps/control-room/src/v2/live/pairing-confirmation.tsx",
      "apps/control-room/src/v2/projects/project-manager-app.tsx",
      "apps/control-room/src/v2/projects/project-manager-app.test.tsx",
      "apps/control-room/src/v2/projects/project-manager-client.ts",
      "apps/control-room/src/v2/cordum-app.tsx"
    ]
  },
  "budget": {
    "wallClockMs": 6300000
  },
  "planApprovedAt": "2026-08-27T06:17:38.234Z",
  "contextFetchedBy": [
    "worker-dc706ffd",
    "qa-bf457df2",
    "worker-2a834a38"
  ],
  "workStartedAt": "2026-08-27T06:26:05.302Z",
  "reviewStartedAt": "2026-08-27T07:08:27.751Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": "$ vitest run\n\n RUN  v4.1.10 C:/Users/Yaron/AppData/Local/Temp/moe-mat-999a-dc706ffd-95fde3e00a15457fed2b1a1eec3cafd5faec46c8/tree/apps/control-room\n\n Test Files  87 passed (87)\n      Tests  1106 passed (1106)\n\nDELIVERED SHA = 95fde3e00a15457fed2b1a1eec3cafd5faec46c8 (grade here, NOT at 485e775f). Run fresh at 10:07:36 in a `git archive 95fde3e0` materialization, 4724 tar entries, cwd pinned to the repo root, after `pnpm install --frozen-lockfile --prefer-offline` EXIT 0. Standalone and redirected to a file rather than piped, so the exit code is this leg's own. Counts reconcile exactly against attempt 1's 87/1105 at 357d3635: +1 test (the new CordumApp seam arm), file count unchanged.\n\nCompanion legs in the same materialization: `pnpm --filter @moe/control-room typecheck` EXIT 0 with script echo `$ tsc --project tsconfig.json`, and the row's `TS2322 Property 'scope' does not exist` ABSENT - positively controlled by deleting the props member, which reproduces it verbatim at the real call site project-manager-app.tsx(158,5). Itemized, run from inside apps/control-room because `pnpm --filter X test -- <path>` does not filter: `pnpm exec vitest run src/v2/projects/project-manager-app.test.tsx --reporter=verbose` EXIT 0, Test Files 1 passed (1) / Tests 10 passed (10), all three scope arms named.\n\nPATH-ATTRIBUTED FOREIGN RED, disclosed not hidden: security vitest leg is 6 failed | 5 passed (11) / 27 failed | 614 passed (641) at BOTH base 357d3635 and delivered - delta EMPTY, intersection with owned paths EMPTY. `pnpm --filter @moe/daemon typecheck` EXIT 1 on affordance-read-planning.test.ts(124,25)/(125,25) TS2339 planningGoalRef, owner task-48ee1455.",
    "reportedAt": "2026-08-27T07:08:27.751Z"
  },
  "filesModified": [
    "apps/control-room/src/v2/live/pairing-confirmation.tsx",
    "apps/control-room/src/v2/projects/project-manager-app.tsx",
    "apps/control-room/src/v2/projects/project-manager-app.test.tsx"
  ],
  "reviewCompletedAt": "2026-08-27T07:11:19.171Z",
  "rejectionDetails": {
    "failedDodItems": [
      "DoD-5 — backward compatibility of the optional prop is proven, not asserted. Both arms render PairingConfirmation STANDALONE, so they cannot see that the only pre-existing consumer nests it inside CordumShell's <main>. Copy compatibility was proven; consumer compatibility was not, and it is broken."
    ],
    "issues": [
      {
        "type": "regression",
        "file": "apps/control-room/src/v2/live/pairing-confirmation.tsx",
        "line": 18,
        "description": "Wrapper changed <div> -> <main> UNCONDITIONALLY while every other change in the hunk is gated on `scope`. The daemon consumer cordum-app.tsx:192 renders this as CordumShell children and cordum-shell.tsx:179 wraps children in <main class=cr2-main>. Measured at 357d3635: main_count=2, nested_main_count=1 (cr2-main|cr2-pairing). Control at parent ba5e8a3d, same probe: nested 0, EXIT 0. Fix: <main> only when scope === \"manager\", keep <div> for the daemon default, aria-labelledby on whichever wraps."
      },
      {
        "type": "missing_feature",
        "file": "apps/control-room/src/v2/projects/project-manager-app.test.tsx",
        "line": 1,
        "description": "DoD-5's backward-compat arms render PairingConfirmation STANDALONE, so they assert copy only and cannot see nesting introduced at the consumer seam. The suite is green at 87/1105 with the defect shipped because nothing asserts landmark structure. Fix: ARM B (no scope) asserts no `main`; ARM A (scope=manager) asserts exactly one; add a seam arm rendering CordumApp in PAIRING asserting querySelectorAll(\"main main\").length === 0."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "REJECT on DoD-5 only. Full evidence, and everything that PASSES, is in comment-3f719a5d so the re-fix stays narrow: DoD-1's three gates re-measured at parent ba5e8a3d, DoD-3 (3 paths, 428+/8-), DoD-4 (typecheck EXIT 0 with the `$ tsc` echo; 87 files / 1105 tests) and DoD-6's two-mutant asymmetry all pass.\n\nDEFECT: this commit ships NESTED `<main>` landmarks on the daemon pairing screen, an existing shipped path. Invalid HTML and an ARIA violation.\n\npairing-confirmation.tsx:18 now renders `<main>` where the parent rendered `<div>`, and that change is UNCONDITIONAL while every other change in the hunk is gated on `scope`. cordum-app.tsx:192 renders it without `scope` inside `<CordumShell>`; cordum-shell.tsx:179 wraps children in `<main className=\"cr2-main\">`.\n\nMEASURED, not argued. Probe rendering CordumApp in PAIRING (same fixture as cordum-app.test.tsx:51-67):\n  QA_PROBE main_count=2 nested_main_count=1 classes=cr2-main|cr2-pairing\nCONTROL at the parent, same probe byte-for-byte: 1 passed, EXIT 0, nested ZERO. Introduced by 357d3635.\n\nWHY THE SUITE MISSED IT: 87/1105 is green because nothing asserts landmark structure, and a component whose only real usage is inside a shell cannot have backward compatibility proven by a standalone render.\n\nNOT A FINDING AGAINST YOUR EXECUTION. Step 3 listed the div->main move as part of the hunk to adopt VERBATIM and rail 2 forbade re-authoring it. You followed both, and you flagged the one-main invariant yourself; I am correcting only the timing - it is live now, not a future task-15ca5c44 hazard.\n\nFIX: 1. gate the wrapper on `scope` - manager `<main>`, daemon `<div>`, aria-labelledby on whichever wraps. The manager path is already correct. 2. ARM B asserts no `main`; ARM A asserts exactly one. 3. Add the seam arm: CordumApp in PAIRING, `main main` count 0 (if it needs a fourth owned path, say so and I take it as a follow-up). 4. Re-verify in a fresh materialization.",
      "rejectedAt": "2026-08-27T06:47:23.701Z",
      "reopenCount": 1,
      "failedDodItems": [
        "DoD-5 — backward compatibility of the optional prop is proven, not asserted. Both arms render PairingConfirmation STANDALONE, so they cannot see that the only pre-existing consumer nests it inside CordumShell's <main>. Copy compatibility was proven; consumer compatibility was not, and it is broken."
      ],
      "issues": [
        {
          "type": "regression",
          "file": "apps/control-room/src/v2/live/pairing-confirmation.tsx",
          "line": 18,
          "description": "Wrapper changed <div> -> <main> UNCONDITIONALLY while every other change in the hunk is gated on `scope`. The daemon consumer cordum-app.tsx:192 renders this as CordumShell children and cordum-shell.tsx:179 wraps children in <main class=cr2-main>. Measured at 357d3635: main_count=2, nested_main_count=1 (cr2-main|cr2-pairing). Control at parent ba5e8a3d, same probe: nested 0, EXIT 0. Fix: <main> only when scope === \"manager\", keep <div> for the daemon default, aria-labelledby on whichever wraps."
        },
        {
          "type": "missing_feature",
          "file": "apps/control-room/src/v2/projects/project-manager-app.test.tsx",
          "line": 1,
          "description": "DoD-5's backward-compat arms render PairingConfirmation STANDALONE, so they assert copy only and cannot see nesting introduced at the consumer seam. The suite is green at 87/1105 with the defect shipped because nothing asserts landmark structure. Fix: ARM B (no scope) asserts no `main`; ARM A (scope=manager) asserts exactly one; add a seam arm rendering CordumApp in PAIRING asserting querySelectorAll(\"main main\").length === 0."
        }
      ]
    }
  ],
  "completedAt": "2026-08-27T07:11:19.171Z",
  "reviewSummary": "APPROVED at sha 95fde3e0 after one reject. Re-verification evidence on the row as comment-c4105ef6; first pass in comment-3f719a5d.\n\nFIX SCOPE: 485e775f gates the landmark, 95fde3e0 moves the accessible name. `git diff --name-only ba5e8a3d 95fde3e0` = exactly the three owned paths, 468+/7-.\n\nDEFECT CLOSED. `const Wrapper = scope === \"manager\" ? \"main\" : \"div\"`. Re-ran MY OWN probe - the file that failed at 357d3635 - in a fresh materialization, strengthened to assert rather than log: nested `main main` 0, total `main` 1, className exactly `cr2-main`, named region present. EXIT 0. Against the original `main_count=2 nested_main_count=1`, clean close.\n\nARMS DRILLED, NOT READ. Mutated the gate back to `const Wrapper = \"main\"` (one degree, no deletion, so the mutant cannot read as \"no gate needed\"): `Tests 2 failed | 8 passed (10)` - exactly the daemon arm and the seam arm, manager arm GREEN. Restore proven separately: backup, restored file and git blob all SHA256 1ea1d2e7..., nonempty on all three.\n\nGATES: typecheck EXIT 0 with the `$ tsc` echo; suite EXIT 0, `Test Files 87 passed (87)` / `Tests 1106 passed (1106)` - exactly +1 over the rejected sha, the seam arm and nothing else.\n\nBEST PART, NOT IN MY REJECTION: 485e775f alone would have cleared the reject and shipped a NEW silent regression - `aria-labelledby` left on a bare `<div>` names nothing, so the daemon path would have lost its accessible name with no arm to catch it. 95fde3e0 moves it to the `<section>` (carried as a `region`) and pins getByRole(\"region\", {name}) on both scopes. Verified independently.\n\nRAIL-2 DEVIATION ADJUDICATED: this row said adopt verbatim; the fix re-authors that hunk, authorised by this rejection, scoped to the wrapper and its aria-labelledby. Noted so a later grader reading git log alone does not read it as an unauthorised refactor."
}