{
  "id": "task-2c952438c41546b3a736623f3956c778",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "Fence the legacy Control Room root out of production browser journeys",
  "description": "Acceptance follow-up for failing Control Room smoke and journey files. Owned path set (4): apps/control-room/src/main.tsx; apps/control-room/src/development-legacy-root.tsx; tests/e2e/control-room/control-room-smoke.spec.ts; tests/e2e/control-room/journeys.spec.ts. The PRD-to-approval failure is assigned separately with the daemon-scratch rename to avoid overlapping ownership.",
  "definitionOfDone": [
    "VERIFY-OR-BLOCK: prove the root chooser distinguishes development-only fixtures from the shipped production build; if legacy selectors cannot be compile/build fenced in this four-file set, report blocked rather than leave ?v1=1 authoritative.",
    "A production build with legacy/fixture query parameters still mounts cr2.shell.root and never mounts cr.shell.root; explicit development fixture mode remains separately tested.",
    "Journey expectations preserve honest absence/refusal truth and do not invent approval authority or retry around failures.",
    "Control Room typecheck/unit tests plus smoke and journeys Playwright files are green with nonzero counts.",
    "A mutation that re-enables the legacy production root makes the named browser assertion red and is restored."
  ],
  "taskRails": [
    "Owned paths are exactly the set listed in the description; request an amendment or split before expanding.",
    "Use red-first TDD, exact refusal code/layer assertions, nonzero executed-test counts, and a restored mutation drill.",
    "Preserve foreign WIP; no .moe edits, blanket staging, reset, stash, merge, or push."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Record the pre-edit HEAD/status for exactly the four owned paths and the before-diff root-lane baselines (`pnpm typecheck` and `pnpm test`, explicitly noting root Vitest excludes apps/**). Adopt the already-landed browser reproduction rather than rewriting it: run the smoke spec and the `production selectors cannot revive the legacy fixture shell` journey standalone and record each nonzero Playwright run failing at `cr2.shell.root` count 0 under `?v1=1&fixtures=1`; also record the existing development positive control `pnpm --filter @moe/control-room exec vitest run src/entry-route.test.ts src/scaffold.test.tsx` at nonzero counts. Before production edits, extend control-room-smoke.spec.ts with a separate production-artifact fence test using the real static harness: enumerate built `dist/assets/*.js`, assert the enumerated count is >0, then assert their bytes contain no `cr.shell.root` legacy selector. Keep the existing browser smoke as the consumer-edge assertion through the served production entry. Run the focused smoke file red again: before the fix, both the artifact marker and browser root behavior are demonstrably wrong. Update only the smoke file's scope comment; journeys.spec.ts already pins exact absence/refusal truth and should not be churned.",
      "status": "COMPLETED",
      "affectedFiles": [
        "tests/e2e/control-room/control-room-smoke.spec.ts",
        "tests/e2e/control-room/journeys.spec.ts"
      ],
      "modifiedFiles": [
        "tests/e2e/control-room/control-room-smoke.spec.ts"
      ],
      "note": "STEP 1 — BASELINES RECORDED, RED-FIRST ESTABLISHED. Root `git -C \"D:/projexts/moe-next\" rev-parse --show-toplevel` -> D:/projexts/moe-next, branch `moe/work-2026-09-02`, HEAD `dd991fdc`.\n\nOWNED-PATH PRE-EDIT STATE (all four at HEAD):\n```\napps/control-room/src/main.tsx                      CLEAN  154 lines  HEAD:OK\napps/control-room/src/development-legacy-root.tsx   CLEAN   11 lines  HEAD:OK\ntests/e2e/control-room/control-room-smoke.spec.ts    M      59 lines  HEAD:OK\ntests/e2e/control-room/journeys.spec.ts             CLEAN  147 lines  HEAD:OK\n```\n\nPROVENANCE OF THE ONE DIRTY OWNED PATH, disclosed rather than assumed. `control-room-smoke.spec.ts` was already ` M` when I claimed this row; mtime 2026-09-02 19:12:58, which predates my claim (~20:05). Its uncommitted diff is EXACTLY the production-artifact fence this step asks me to add — a `production artifacts exclude the legacy shell selector` test that enumerates `apps/control-room/dist/assets/*.js`, asserts `assets.length > 0`, and asserts no asset's bytes contain `cr.shell.root`, plus the scope-comment update from \"ONE smoke journey\" to \"TWO production fences\". The row has 0 commits and reopenCount 0, so this is prior in-flight work on this row that no wrapper ever landed. I am ADOPTING it per the step's \"adopt the already-landed browser reproduction rather than rewriting it\", not reverting and not rewriting. It matches the specified shape, so there was nothing to add.\n\nRED-FIRST, focused smoke file, standalone:\n```\nRunning 2 tests using 1 worker\n  1 failed\n    [chromium] › control-room-smoke.spec.ts:27:1 › production artifacts exclude the legacy shell selector\n  1 passed (3.5s)\nEXIT=1\n```\nFailure verbatim — `expect(assets.filter(...cr.shell.root...)).toEqual([])`, `- Array []` vs `+ Array [ \"index-VTM94VZ_.js\", ]` at control-room-smoke.spec.ts:36:82. Nonzero run (2 tests), so not vacuous.\n\nFINDING A — THE STEP'S PREMISE THAT BOTH FENCES ARE WRONG IS HALF STALE, and I am recording the measurement rather than editing to fit it. Only the ARTIFACT marker is wrong. The browser root behaviour is ALREADY CORRECT:\n```\ncontrol-room-smoke.spec.ts:43 \"serving the built control-room bundle mounts its shell root element\"  -> PASSED\njourneys.spec.ts  Running 7 tests  ->  7 passed (10.1s)  EXIT=0\n```\nincluding the named `production selectors cannot revive the legacy fixture shell` (:53), which already asserts `cr.shell.root` count 0, no fixture banner, no config notice, and exact `LIVE_BOOTSTRAP_UNAVAILABLE`. Cause: `mountControlRoom` already calls `gateDevelopmentQuery(location.search, import.meta.env.DEV)` (main.tsx:122-125), so a production build strips `v1`/`fixtures` and `chooseRoot` falls through to `CordumApp`. The RUNTIME fence exists; what is missing is BUILD-TIME elimination — main.tsx:8/13/14 statically import `resolveLiveSetupFromBuild`, `resolveShellMode` and `ShellModeRoot`, which keeps `shell/frame.tsx` (the sole production site of the literal, :173 `data-testid=\"cr.shell.root\"`) in the bundle. That is exactly what step 2's lazy composition removes, so the plan's FIX is right even though its red-first prediction was half wrong. Consequence for step 4: the drill must red the artifact/browser assertion via the lazy gate, since the query gate alone is already sufficient at runtime.\n\nFINDING B — A STALE `dist/` GREENS THE ARTIFACT FENCE VACUOUSLY, and this nearly happened. The pre-existing bundle `index-1EtRNv6S.js` (Aug 31 18:58) contains ZERO `cr.shell.root`, so running the fence against it would have reported the row already fixed. A FRESH `pnpm --filter @moe/control-room build` (exit 0, `✓ built in 281ms`) emits `index-VTM94VZ_.js` with:\n```\ncr.shell.root=1   cr2.shell.root=1   DEVELOPMENT_ONLY/NOT_CONFIRMATORY=1   cr.banner.fixture=1\n```\nSo the fence is only meaningful when preceded by a build, and the artifact set must be re-enumerated after every source change. I will rebuild before every artifact measurement in later steps and say so.\n\nDEVELOPMENT POSITIVE CONTROL (must keep proving the opposite build world):\n`pnpm --filter @moe/control-room exec vitest run src/entry-route.test.ts src/scaffold.test.tsx` -> `Test Files 2 passed (2)`, `Tests 40 passed (40)`, EXIT=0.\n\nROOT-LANE BEFORE-DIFF BASELINES:\n`pnpm typecheck` -> EXIT 0, all six projects `Done` (apps/control-room, apps/daemon, packages/{benchmark,review,scheduler,runner}).\n`pnpm test` -> `Test Files 2 failed | 423 passed | 1 skipped (426)`, `Tests 3 failed | 10259 passed | 44 skipped (10306)`, EXIT 1. The three failures, disclosed verbatim and ALL FOREIGN — none intersects the four owned paths:\n```\nFAIL packages/store/src/command-decision-replay-bytes.test.ts > command decision replay-request digest — both commit paths record it > exports the correlation identity helper used by provenance readers\nFAIL tests/integration/release/release-version-surfaces.test.ts > release version surfaces > discovers the exact nonempty current-version occurrence set in both directions\nFAIL tests/integration/release/",
      "startedAt": "2026-09-02T20:42:16.044Z",
      "completedAt": "2026-09-02T20:46:17.806Z"
    },
    {
      "stepId": "step-2",
      "description": "In main.tsx, replace the static legacy imports with React.lazy/Suspense composition over the existing default export in `./development-legacy-root.js`. Define one compile-time `DEVELOPMENT_BUILD` constant from `import.meta.env.DEV`; use that same value (a) in the conditional lazy import so Rollup can remove the legacy module from production and (b) when calling the tracked `gateDevelopmentQuery` so production strips only `v1`/`fixtures` while development preserves them. Feed the gated search consistently to resolveProjectManagerMode, prepareV2LiveSetup and chooseRoot. Keep manager mode first; choose the lazy DevelopmentLegacyRoot only when the guarded component exists and the gated query has exact `v1=1`, wrapped in Suspense with a development-only null fallback; otherwise mount CordumApp. Preserve mountControlRoom's exported signature, hash scrubbing, one pairing attempt/retry closure, manager connection, StrictMode and clock provider. development-legacy-root.tsx remains the focused owner of resolveLiveSetupFromBuild/resolveShellMode/ShellModeRoot; add only a concise DEVELOPMENT_ONLY boundary comment if needed. Keep both production modules <=250 lines and below 400.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/development-legacy-root.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/main.tsx",
        "tests/e2e/control-room/control-room-smoke.spec.ts"
      ],
      "note": "STEP 2 — LAZY COMPOSITION LANDED; THE LEGACY SHELL IS GONE FROM THE PRODUCTION BUNDLE. main.tsx only; development-legacy-root.tsx needed no edit (11 lines, already the focused owner of resolveLiveSetupFromBuild/resolveShellMode/ShellModeRoot with the default export the plan expects, still CLEAN).\n\nTHE FENCE NOW BITES, measured on a FRESH build (`✓ built in 137ms`, exit 0), one JS asset:\n```\nbefore:  index-VTM94VZ_.js  506.10 kB   cr.shell.root=1  cr.banner.fixture=1  DEVELOPMENT_ONLY/NOT_CONFIRMATORY=1  cr.config.notice=1  cr2.shell.root=1\nafter:   index-C__9h-zD.js  413.10 kB   cr.shell.root=0  cr.banner.fixture=0  DEVELOPMENT_ONLY/NOT_CONFIRMATORY=0  cr.config.notice=0  cr2.shell.root=1\n```\n93 kB of legacy shell eliminated, and it was ELIMINATED rather than code-split: `dist/assets/*.js` still enumerates exactly one file, so the module left the graph instead of moving to a chunk the entry-only check would have missed.\n\nWHAT CHANGED IN main.tsx (192 lines, was 154; under the 250 target and the 400 cap):\n- Removed the three static legacy imports (`resolveLiveSetupFromBuild`, `resolveShellMode`, `ShellModeRoot`). Those were the whole defect: they pulled `shell/frame.tsx` — the sole production site of `data-testid=\"cr.shell.root\"` at :173 — into the bundle regardless of route.\n- Added ONE compile-time `const DEVELOPMENT_BUILD: boolean = import.meta.env.DEV`, now the module's only `import.meta.env` read (grep: one occurrence, at the constant). Used in both places the step names: the conditional legacy import and the `gateDevelopmentQuery` call. The query gate and the mount gate are now the same fact, so no build can strip the selectors at runtime while still shipping the shell they select, or the reverse.\n- `chooseRoot` keeps manager mode first, then takes the legacy branch only when BOTH `DevelopmentLegacyRoot !== null` (compile-time) and the gated query has exact `v1=1` (route request), wrapped in `<Suspense fallback={null}>`; otherwise `CordumApp`. Preserved unchanged: `mountControlRoom`'s exported signature and defaults, hash scrubbing, the single pairing attempt/retry closure, manager connection, StrictMode, ClockProvider, `CONTROL_ROOM_ROOT_ELEMENT_ID`, the `CONTROL_ROOM_ROOT_MISSING` throw, and `MOUNTED_CONTROL_ROOM_ROOT`.\n\nONE DESIGN DECISION THAT COST TWO ITERATIONS, DISCLOSED IN FULL because it is the non-obvious part of the diff. The plain form —\n```tsx\nconst DevelopmentLegacyRoot = DEVELOPMENT_BUILD ? lazy(() => import(\"./development-legacy-root.js\")) : null;\n```\n— typechecks, builds and fences correctly, but REDS TWO DEVELOPMENT CONTROLS I am forbidden to edit: `scaffold.test.tsx > mounts through the production entry point` and `> refuses closed at the real entry point when the build carries no credentials`. Diagnosed rather than assumed — I dumped the rendered DOM and got `<div id=\"root\" />`, EMPTY. So the v1 branch was correctly taken (CordumApp markup would be present otherwise) and the Suspense fallback was still painted: `mountEntryPointAt` does `await act(async () => void (await import(\"./main.js\")))`, which resolves the ENTRY module but not the lazy chunk the entry then requests, so the assertion reads the DOM one turn early.\n\nStarting the request eagerly at module scope fixed one of the two. What fixes both is awaiting it — `DEVELOPMENT_BUILD ? await import(\"./development-legacy-root.js\") : null` — so a development entry has the module in hand before `mountControlRoom` runs and Suspense resolves on the first attempt. The top-level await sits INSIDE the dead branch, so production emits neither the await nor the module; the 413 kB single-asset measurement above is the proof, taken after this change. `lazy` + `Suspense` are still the composition, as the step requires.\n\nDEVELOPMENT CONTROLS GREEN, proving the opposite build world without any test edited:\n```\npnpm --filter @moe/control-room exec vitest run src/scaffold.test.tsx src/entry-route.test.ts\n Test Files  2 passed (2)\n      Tests  40 passed (40)   EXIT=0\npnpm --filter @moe/control-room typecheck  ->  $ tsc --project tsconfig.json   EXIT=0\n```\nDevelopment still mounts `cr.shell.root` plus the fixture banner for `?v1=1&fixtures=1`; entry-route still keeps the development search byte-for-byte and strips both keys in production.\n\n`git diff --check` clean on all four owned paths. Porcelain shows exactly two modified paths, both owned: main.tsx and the adopted smoke spec. journeys.spec.ts and development-legacy-root.tsx untouched. No foreign path touched.",
      "startedAt": "2026-09-02T20:46:20.275Z",
      "completedAt": "2026-09-02T20:50:40.055Z"
    },
    {
      "stepId": "step-3",
      "description": "Run the focused development controls after implementation and fix only owned production bytes until they prove the opposite build world without changing their tests: Vitest must still mount `cr.shell.root` plus the fixture banner for `?v1=1&fixtures=1`, while entry-route keeps development search byte-for-byte and production removes both selector keys. Then run the smoke and named production-selector journey green: the real production build must have a nonempty JS artifact set with no `cr.shell.root`, mount exactly one `cr2.shell.root`, mount zero legacy/config/fixture roots, show project/goals surfaces and exact `LIVE_BOOTSTRAP_UNAVAILABLE`, and preserve the harness exact `{ok:true,...}` outcome. Do not add retries, fixture authority, approval state, or a mock root to make the browser lane pass.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/development-legacy-root.tsx",
        "tests/e2e/control-room/control-room-smoke.spec.ts",
        "tests/e2e/control-room/journeys.spec.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/main.tsx",
        "tests/e2e/control-room/control-room-smoke.spec.ts"
      ],
      "note": "STEP 3 — BOTH BUILD WORLDS PROVEN, NO TEST WEAKENED, NO PRODUCTION BYTE ADDED TO MAKE THE BROWSER LANE PASS.\n\nDEVELOPMENT WORLD (Vitest), full package lane, not just the two controls:\n```\npnpm --filter @moe/control-room test\n Test Files  113 passed (113)\n      Tests  1564 passed (1564)   EXIT=0\n```\nZero collateral from the composition change across all 113 files. (1564 vs the 1562 I measured an hour ago on a sibling row: two tests added by other in-flight rows, not by me — I added no test.) The two named controls inside that lane still assert exactly what they asserted before: `scaffold.test.tsx` mounts `cr.shell.root` AND `cr.banner.fixture` through the real entry point at `?v1=1&fixtures=1`, and `entry-route.test.ts` keeps the development search byte-for-byte while production removes both selector keys. Neither file was edited — neither is an owned path.\n\nPRODUCTION WORLD (Playwright), both owned specs in ONE run:\n```\nRunning 9 tests using 1 worker\n  9 passed (6.6s)   EXIT=0\n```\nNonzero count and every named leg green:\n  control-room-smoke.spec.ts:27  production artifacts exclude the legacy shell selector\n  control-room-smoke.spec.ts:43  serving the built control-room bundle mounts its shell root element\n  journeys.spec.ts:53            production selectors cannot revive the legacy fixture shell\n  plus CR-J1-002, CR-A11Y-001, the disconnected-banner arm, the narrow-layout parity arm, the project-switch arm and the COVERED-set ledger arm.\n\nThe artifact fence was RED at step 1 on the same file and is now green against a rebuilt bundle: `dist/assets/*.js` enumerates one asset, `assets.length > 0` holds, and no asset's bytes contain `cr.shell.root`. The browser legs assert exactly one `cr2.shell.root`, zero `cr.shell.root`, zero `cr.banner.fixture`, zero `cr.config.notice`, visible `cr.project.boundary` and `cr.goals.home`, exact `LIVE_BOOTSTRAP_UNAVAILABLE`, no `DEVELOPMENT_ONLY/NOT_CONFIRMATORY` in the page content, and the harness's exact `{ok: true, value: …}` outcome — all pre-existing assertions I did not touch.\n\nNOTHING WAS ADDED TO MAKE THE BROWSER LANE PASS. No retry, no `waitFor` loosening, no fixture authority, no approval state, no mock root, no timeout bump. The only production edit is the one from step 2 (static legacy imports replaced by the compile-time-gated lazy/Suspense composition), and the only test-side change in the working tree is the adopted artifact fence, which was already present when I claimed the row. journeys.spec.ts and development-legacy-root.tsx remain byte-identical to HEAD.\n\nOwned-set porcelain unchanged from step 2: ` M main.tsx`, ` M control-room-smoke.spec.ts`, the other two clean. `git diff --check` clean.",
      "startedAt": "2026-09-02T20:50:42.499Z",
      "completedAt": "2026-09-02T20:51:30.173Z"
    },
    {
      "stepId": "step-4",
      "description": "Adversarially review the four-path diff for a second selector authority, raw-search leakage into Cordum, manager precedence drift, a statically reachable legacy import, blank development lazy state, invented authority, retries, zero-file artifact sweeps, production files >=250 target/>=400 hard cap, and foreign-path edits. For the required divergence drill, first record `git status --porcelain` for main.tsx, copy its tracked pre-drill bytes to a task/worker-suffixed `$env:TEMP` backup and hash them. With a one-occurrence guard, mutate only `const DEVELOPMENT_BUILD = import.meta.env.DEV` to `true`; this single production-surface mutation both preserves `v1`/`fixtures` and includes the lazy legacy module. Run only the named production-selector Playwright journey and require nonzero exit at its `cr2.shell.root`/legacy-root browser assertion. The fixture has the normal static host, valid root container, non-manager hostname and exact development query; with the compile-time fence loosened no later guard can produce the v2 root or share the same observation, so the legacy component is the only alternate mount. Never delete the guarded import/call. Restore in a separate command, then in a separate call require backup/current SHA256 equality and the pre-drill tracked diff to be restored; a green mutant is a finding, not evidence.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/development-legacy-root.tsx",
        "tests/e2e/control-room/control-room-smoke.spec.ts",
        "tests/e2e/control-room/journeys.spec.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/main.tsx",
        "tests/e2e/control-room/control-room-smoke.spec.ts"
      ],
      "note": "STEP 4 — ADVERSARIAL REVIEW CLEAN ON ALL TEN ITEMS; DIVERGENCE DRILL RED ON BOTH FENCES, RESTORED AND PROVEN. One finding on the drill procedure.\n\nADVERSARIAL REVIEW OF THE FOUR-PATH DIFF, each item measured not asserted:\n1. SECOND SELECTOR AUTHORITY — none. `git grep 'get(\"v1\")' -- apps/control-room/src ':!*.test.*'` returns two sites, both in main.tsx and both reading the GATED search: :101 (chooseRoot) and :126 (prepareV2LiveSetup, which only decides whether to start a handshake, not what mounts). No module outside the composition root chooses a root by query.\n2. RAW-SEARCH LEAKAGE INTO CORDUM — none. `location.search` is read exactly once for routing, main.tsx:161, and gated in the same expression. The only other occurrence, :167, is the pre-existing hash scrub rebuilding the URL, not routing. `shell-mode.ts` and `cordum-app.tsx` mention it in prose only. `CordumApp` receives the gated `search`, as do `resolveProjectManagerMode` and `prepareV2LiveSetup`.\n3. MANAGER PRECEDENCE DRIFT — intact. `chooseRoot` still tests `managerMode && managerSetup !== undefined` FIRST (:95), before the legacy branch (:101) and before CordumApp (:110).\n4. STATICALLY REACHABLE LEGACY IMPORT — none. The only importers of `shell-mode.js`/`shell-mode-view.js` in the production graph are `development-legacy-root.tsx` (reached solely through the dynamic import) and `shell-mode-view.tsx` itself, inside that subtree. Proven by artifact, not by grep alone: the built bundle is one asset with `cr.shell.root` count 0.\n5. BLANK DEVELOPMENT LAZY STATE — ruled out, and this was a real defect I hit and fixed in step 2. `scaffold.test.tsx > mounts through the production entry point` mounts through the real entry at `?v1=1&fixtures=1` and reads the DOM immediately; it is green, so no fallback frame is observable.\n6. INVENTED AUTHORITY / RETRIES — none added. No retry, no waitFor loosening, no fixture authority, no approval state, no mock root, no timeout change.\n7. ZERO-FILE ARTIFACT SWEEP — guarded. The fence asserts `expect(assets.length).toBeGreaterThan(0)` before the byte check, so an empty `dist/assets` cannot pass it vacuously.\n8. LINE CAPS — main.tsx 192 (was 154), development-legacy-root.tsx 11. Both under the 250 target and far under 400.\n9. FOREIGN-PATH EDITS — none. Owned-set porcelain is ` M main.tsx` and ` M control-room-smoke.spec.ts`; journeys.spec.ts and development-legacy-root.tsx byte-identical to HEAD. `git diff --check` clean.\n10. GUARDED IMPORT/CALL NEVER DELETED — `await import(\"./development-legacy-root.js\")` at :48 and `gateDevelopmentQuery(` at :160 both present after restore.\n\nDRILL. Pre-drill: porcelain ` M`, tracked diff `47 9`, sha256 `b8877ebe5d27f9afc48e2117a686f0161b067e4ef8fa8a0e39c73c2426ee69ad`, backup at `%TEMP%/main.tsx.task-2c952438.worker-50445f7a.bak` with the same hash. One-occurrence guard on the target line returned exactly 1. Mutation: `const DEVELOPMENT_BUILD: boolean = import.meta.env.DEV;` -> `= true;`. Nothing else touched.\n\nNAMED PRODUCTION-SELECTOR JOURNEY — RED, exit 1:\n```\nRunning 1 test using 1 worker\nError: expect(locator).toHaveCount(expected) failed\n  Locator:  getByTestId('cr2.shell.root')\n  Expected: 1\n  Received: 0\n  13 × locator resolved to 0 elements\n  at journeys.spec.ts:38:54  (reached from journeys.spec.ts:54)\n1 failed\n```\nThe v2 root is GONE, which is the legacy component having mounted in its place — with the compile-time fence loosened, `v1`/`fixtures` survive the query gate and the legacy module is in the graph, and the fixture's normal static host, valid root container, non-manager hostname and exact development query leave no other alternate mount.\n\nI ALSO RAN THE FENCE THIS ROW ADDS, under the same single mutation — RED, exit 1:\n```\n- Array []\n+ Array [ \"development-legacy-root-cJy6BUIN.js\", ]\n  at control-room-smoke.spec.ts:36:82\n```\nOne mutation, two independent observations. Note WHICH asset it names: the mutant build emitted THREE JS assets (`index-DxC_UOtQ.js` 343 kB, `development-legacy-root-cJy6BUIN.js` 110.73 kB, `keyboard-map-iSHEttDq.js` 90.25 kB) because the legacy module became a code-split chunk rather than being eliminated. An entry-only check would have MISSED it; the fence catches it precisely because it enumerates every `dist/assets/*.js`. That is the design property my step-2 comment predicted, now demonstrated rather than argued.\n\nFINDING — THE DRILL PROCEDURE AS WRITTEN WOULD HAVE COME BACK FALSELY GREEN. The step says to mutate main.tsx and \"run only the named production-selector Playwright journey\". Playwright serves `apps/control-room/dist`, not source, so without an intervening `pnpm --filter @moe/control-room build` the browser reads the PRE-MUTATION bundle and the journey passes — a green mutant that is an artifact of a stale artifact, not evidence about the guard. I rebuilt after mutating (exit 0) and again after restoring, and I flag it because it generalises: any drill whose observation is a browser against a BUILT artifact needs the build ins",
      "startedAt": "2026-09-02T20:51:32.650Z",
      "completedAt": "2026-09-02T20:53:31.604Z"
    },
    {
      "stepId": "step-5",
      "description": "Final verification after restore: report main.tsx/development-legacy-root.tsx physical line counts, run `git diff --check` on the four owned paths, then run standalone/unpiped `pnpm --filter @moe/control-room typecheck`, `pnpm --filter @moe/control-room test` (require nonzero Test Files/Tests lines), and `pnpm exec tsc -p tests/e2e/control-room/tsconfig.json`. Persist this exact 489-character shell:false Playwright gate verbatim; it runs only the two owned specs, echoes output, preserves the child exit status, and rejects a vacuous run unless both `Running N tests` and `N passed` are nonzero: `node -e \"const{spawnSync:s}=require('node:child_process');let r=s('pnpm',['exec','playwright','test','-c','tests/e2e/control-room/playwright.config.ts','tests/e2e/control-room/control-room-smoke.spec.ts','tests/e2e/control-room/journeys.spec.ts','--reporter=line'],{encoding:'utf8',shell:false,env:{...process.env,FORCE_COLOR:'0'}});console.log(r.stdout,r.stderr);process.exit(r.error||r.status||!(/Running [1-9]\\d* tests?/.test(r.stdout)&&/[1-9]\\d* passed/.test(r.stdout))?r.status||1:0)\"`. Finally rerun the root `pnpm typecheck`/`pnpm test` legs and compare their failing paths with step-1: any added failure intersecting the four owned paths must be fixed; disclose all foreign red verbatim and say root Vitest does not cover apps/**.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/control-room/src/main.tsx",
        "apps/control-room/src/development-legacy-root.tsx",
        "tests/e2e/control-room/control-room-smoke.spec.ts",
        "tests/e2e/control-room/journeys.spec.ts"
      ],
      "startedAt": "2026-09-02T20:53:34.226Z"
    }
  ],
  "status": "BLOCKED",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Still required; blocked rather than backlogged per human cleanup policy: downstream of d36 and 75bb accessibility/bootstrap leaves. Unblock after both land.",
  "createdBy": "WORKER",
  "parentTaskId": "task-c289b8fbcb9840968a9b3994e73b79a4",
  "priority": "HIGH",
  "order": 193,
  "comments": [
    {
      "id": "comment-32a5f683202744bbbace2d6f4a9b48a6",
      "author": "worker-620f90d0",
      "content": "Worker replan handoff: steps 1-4 are complete and the root-fence bytes are green; step 5 is the only in-progress step. The exact 489-char two-spec Playwright gate ran 9 tests and failed 2: `CR-J1-002` could not find the disabled `Approvals ... not available yet` affordance, and `the disconnected banner never coexists with an enabled mutation` observed enabled `cr.goals.new` with `cr.banner.disconnected` visible. Do not weaken/delete those journey arms. Replan must either add the actual Cordum nav/goals live-unavailable production paths to ownership or split a prerequisite row owning those fixes. Existing final diff is main.tsx + control-room-smoke.spec.ts; development-legacy-root.tsx and journeys.spec.ts remain byte-identical. All other named gates are green (control-room 111 files/1502 tests; root 401 files/9778 tests; smoke 2/2; named selector journey 1/1).",
      "timestamp": "2026-08-31T13:27:22.482Z"
    },
    {
      "id": "comment-fcfde6609ee04d4d9bf2103304ec0257",
      "author": "architect-5e59a71e",
      "content": "Dependency-field correction needed for task-2c952438c41546b3a736623f3956c778. Human reopen authorized SPIDR; I created production leaves task-d36dda1396bf48de8caae3ebbd93731e (Approvals accessible unavailable name) and task-75bb88d241894a43b6c6981f355bc220 (LIVE_BOOTSTRAP_UNAVAILABLE disables Create). Parent must dependsOn BOTH before its unchanged served-build journeys can resume. moe.set_task_dependencies refused architect-5e59a71e with NOT_ALLOWED despite architect team context. Please set dependsOn=[task-d36dda1396bf48de8caae3ebbd93731e, task-75bb88d241894a43b6c6981f355bc220]; do not widen the parent’s exact four paths.",
      "timestamp": "2026-08-31T16:08:11.920Z"
    },
    {
      "id": "comment-8902a805c3034350a3910c549e29c429",
      "author": "architect-99fe366f",
      "content": "DISCLOSURE from the R3-UI leaf task-d36dda1396bf48de8caae3ebbd93731e (this row dependsOn it). Filed as a comment, not a row, and no e2e file was touched — this path is yours.\n\nTWO MEASURED FACTS AT HEAD e889712f.\n\n1. THE LEAF'S BYTES ARE LANDED AND ON MAIN. The leaf spent three reopens on \"the accepted navigation paths remain absent from any recorded completion commit\". The bytes were never missing. Commit `daf1139f5dd4a85fda62cc8f69430845edac1d0c` \"fix(control-room): expose unavailable reasons\" (author Yaron Torgeman, 2026-09-01) carries EXACTLY the three owned paths and nothing else, and is an ancestor of both main and HEAD. For each of the three paths, `git rev-parse HEAD:<p>`, `git rev-parse main:<p>` and `git hash-object <p>` are equal (nav-rail.tsx 1dd77802, cordum-shell.test.tsx 68cf3125, cordum-shell-responsive.test.tsx 3550fcd7). It was a hand-landing whose second call was never made: daf1139f carries no `Moe-Task:` trailer, so no wrapper postflight ran and moe.record_commit was never called, leaving task.commits=[] while the code sat on main. The leaf's approved plan closes that with one record_commit and zero source edits. Practical upshot for you: `nav-rail.tsx:109` renders `{item.label}{reason === null ? null : \" not available yet\"}` and `nav-rail.tsx:89` renders `data-unavailable-reason={reason ?? undefined}` at HEAD today — you can build on both now, and you do not need to wait on a re-implementation.\n\n2. YOUR SERVED-BUILD ARM DOES NOT ASSERT THE ACCESSIBLE NAME. The leaf's task description names your `tests/e2e/control-room/journeys.spec.ts` as the served-build consumer of `/Approvals.*not available yet/`. Re-measured: that is not what the file asserts. `CR-J1-002` at journeys.spec.ts:65-75 is live and drives the same button, but its two assertions are `await expect(approvals).toBeDisabled()` and `await expect(approvals).toHaveAccessibleDescription(\"This destination is not built yet in this release.\")` — the DESCRIPTION (via aria-describedby, nav-rail.tsx:88) and the disabled fence, both of which predate the leaf. `grep -rn \"not available yet\" tests/e2e/` returns no hit in any spec; the only hit under tests/e2e is an untracked `test-results/**/error-context.md` artifact.\n\nSo the accessible-NAME phrase the leaf shipped is currently proven only by two jsdom arms inside the leaf's own owned files (cordum-shell.test.tsx:52, cordum-shell-responsive.test.tsx:148). Deleting the phrase from production today would leave CR-J1-002 green — the served-build lane cannot see it. If your DoD wants that phrase certified against the real served build, the arm has to be added here, on a path you own, and it should assert the NAME (e.g. `getByRole(\"button\", { name: /Approvals.*not available yet/iu })`) rather than the description, because the description is already independently pinned and would mask the name regressing. I did not add it: it is out of the leaf's three-path scope and this row owns the e2e lane.",
      "timestamp": "2026-09-02T18:06:48.414Z"
    },
    {
      "id": "comment-81d927c17dfe468eaa3c87cec4405ec2",
      "author": "worker-50445f7a",
      "content": "STEP 5 EVIDENCE — EVERYTHING GREEN EXCEPT ONE UNNAMED CONSUMER, WHICH IS UNSATISFIABLE INSIDE THE FOUR-PATH SET. Root `D:/projexts/moe-next`, branch `moe/work-2026-09-02`.\n\nLINE COUNTS: main.tsx 192 (was 154), development-legacy-root.tsx 11 (untouched). Both under the 250 target and the 400 cap. `git diff --check` clean on all four owned paths.\n\nGREEN LEGS, all standalone and unpiped:\n```\npnpm --filter @moe/control-room typecheck   ->  $ tsc --project tsconfig.json     EXIT 0\npnpm --filter @moe/control-room test        ->  Test Files 113 passed (113)\n                                                Tests 1564 passed (1564)          EXIT 0\npersisted 489-char shell:false Playwright gate, verbatim\n                                            ->  Running 9 tests using 1 worker\n                                                9 passed (6.7s)                   EXIT 0\npnpm typecheck (root, all projects)         ->  every project \"Done\"              EXIT 0\n```\nThe Playwright gate's own vacuity guard is satisfied: both `Running [1-9]\\d* tests?` and `[1-9]\\d* passed` matched, so the run is provably non-empty.\n\nROOT `pnpm test` DELTA vs the step-1 baseline. Before: `Test Files 2 failed | 423 passed | 1 skipped (426)`, `Tests 3 failed`. After: `Test Files 3 failed | 422 passed | 1 skipped (426)`, `Tests 4 failed`. The three baseline failures are unchanged and FOREIGN, disclosed verbatim:\n```\nFAIL packages/store/src/command-decision-replay-bytes.test.ts > … > exports the correlation identity helper used by provenance readers\nFAIL tests/integration/release/release-version-surfaces.test.ts > … > discovers the exact nonempty current-version occurrence set in both directions\nFAIL tests/integration/release/release-version-surfaces.test.ts > … > goes red for an unclassified tracked current-version occurrence\n```\nRoot Vitest's include has NO `apps/**`, so `pnpm test` covers zero control-room unit tests and cannot evidence this row's production files either way.\n\nTHE ONE ADDED FAILURE IS MINE AND I AM NOT EVADING IT:\n```\nFAIL tests/e2e/control-room/journey-coverage.test.ts > the ledger's UNKNOWNs still describe real gaps\n     > binds the served-root roster to every chooseRoot production arm\nAssertionError: main.tsx imports ShellModeRoot: expected 'import { StrictMode, Suspense, lazy }…'\n  to match /import\\s+\\{\\s*ShellModeRoot\\s*\\}\\s+from/u\n```\nSource, journey-coverage.test.ts:258-268 — a SOURCE-TEXT guard, tracked and CLEAN at HEAD (not foreign WIP):\n```ts\nit(\"binds the served-root roster to every chooseRoot production arm\", () => {\n  const main = readFileSync(join(repoRoot(), \"apps/control-room/src/main.tsx\"), \"utf8\");\n  for (const component of [\"ProjectManagerApp\", \"ShellModeRoot\", \"CordumApp\"]) {\n    expect(main, `main.tsx imports ${component}`).toMatch(\n      new RegExp(`import\\\\s+\\\\{\\\\s*${component}\\\\s*\\\\}\\\\s+from`, \"u\"));\n    expect(main, `chooseRoot returns ${component}`).toMatch(\n      new RegExp(`return\\\\s+<${component}\\\\b`, \"u\"));\n  }\n});\n```\nIt requires main.tsx to contain a STATIC `import { ShellModeRoot } from …` and a direct `return <ShellModeRoot`. That static import is the entire defect this row exists to remove — it is what drags `shell/frame.tsx`, and with it the `cr.shell.root` literal, into the production bundle.\n\nI CHECKED WHETHER ANY IN-SCOPE CONSTRUCTION SATISFIES BOTH, and none does. `import type { ShellModeRoot }` does not match the regex (`type` sits between `import` and `{`). Re-exporting or aliasing still needs the literal `return <ShellModeRoot` arm. Every string that satisfies the guard reintroduces a statically reachable edge and re-reds the artifact fence. The two requirements are mutually exclusive on the same file.\n\nWHAT THE GUARD ACTUALLY WANTS IS STILL TRUE. All three chooseRoot arms survive: `ProjectManagerApp`, the legacy shell, and `CordumApp`. Only the SPELLING of the middle arm changed — it is now reached through the lazily imported `DevelopmentLegacyRoot`, whose own module still imports `ShellModeRoot`. This is a grep anchor pinning a spelling rather than a property, so the guard reads as a stale roster rather than a live requirement. But `tests/e2e/control-room/journey-coverage.test.ts` is NOT in my four owned paths, and rail 1 plus the unnamed-consumer rail both forbid me from taking the extra path or reshaping main.tsx to evade the check.\n\nDoD 1 is explicit about this exact situation: \"if legacy selectors cannot be compile/build fenced in this four-file set, report blocked rather than leave ?v1=1 authoritative.\" They cannot be, so I am reporting rather than deciding.\n\nSEPARATE, FULLY FOREIGN RED, disclosed and NOT mine:\n```\npnpm exec tsc -p tests/e2e/control-room/tsconfig.json   EXIT 1\ntests/e2e/control-room/daemon-ports.ts(288,16): error TS2304: Cannot find name 'repoRoot'.\ntests/e2e/control-room/gate1-v1-approval.spec.ts(11,67): error TS2305: Module '\"./daemon-ports.js\"' has no exported member 'repoRoot'.\n```\nBoth files are ` M` with uncommitted edits I did not make; `daemon-ports.ts` mtime 23:54 is LATER than my last edit at 23:52, so a peer is editing it live. At HEAD its line 30 imports `repoRoot` from `./daemon-scratch.js`; the worktree copy has moved that to an `export { … } from` block and dropped the local binding while `repoRoot()` is still called at :288 — a mid-flight refactor. This row's own description carves it out: \"The PRD-to-approval failure is assigned separately with the daemon-scratch rename to avoid overlapping ownership.\" Neither file is an owned path and neither error mentions mine.",
      "timestamp": "2026-09-02T20:57:01.419Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-30T18:18:59.394Z",
  "updatedAt": "2026-09-02T20:58:22.160Z",
  "metrics": {
    "firstClaimAt": "2026-08-31T11:58:26.538Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 4,
    "executedStepCount": 8
  },
  "contextFetchedBy": [
    "architect-8b0f89f2",
    "worker-620f90d0",
    "governor-943540d3",
    "architect-5e59a71e",
    "governor-c11c29e2",
    "worker-6fab73fa",
    "worker-50445f7a"
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-02T20:41:49.028Z",
  "planningNotes": {
    "approachesConsidered": "Resubmission of the SAME five approved steps, byte-identical, to move a stranded PLANNING row into WORKING (submit_plan is PLANNING's only exit; SPEED auto-approves). Both dependencies are DONE with landed=true: task-d36dda13 and task-75bb88d2. I did NOT rewrite the plan - every step description and affectedFiles list went back exactly as approved.",
    "codebaseInsights": "READ THIS BEFORE TRUSTING THE OLD STEP STATUSES. The prior attempt had steps 1-4 marked COMPLETED and step 5 IN_PROGRESS. Resubmission resets them to PENDING, and that is the HONEST state, because step 2's central deliverable is NOT on disk. Measured at claim, root D:/projexts/moe-next, daemon 9876, HEAD dd991fdc: (a) main.tsx contains NO DEVELOPMENT_BUILD constant, NO React.lazy and NO Suspense - it still statically imports ShellModeRoot at main.tsx:14 and mounts it directly from chooseRoot at main.tsx:70 behind a runtime `new URLSearchParams(search).get(\"v1\") === \"1\"` check at :67. import.meta.env.DEV appears only inline at :124 feeding gateDevelopmentQuery, not as the compile-time constant the step requires. A statically reachable legacy import is exactly what step 4's adversarial review is written to catch, so DoD 2 cannot hold at these bytes: a production build with ?v1=1 would still mount cr.shell.root. (b) development-legacy-root.tsx DOES exist, committed and clean, 11 lines, exporting default DevelopmentLegacyRoot({search}) over resolveLiveSetupFromBuild/resolveShellMode/ShellModeRoot - so the module half of step 2 landed and only the main.tsx rewiring is missing. (c) Step 1's deliverable is on disk but UNCOMMITTED: tests/e2e/control-room/control-room-smoke.spec.ts is ` M` with +25/-2 adding the `production artifacts exclude the legacy shell selector` test that enumerates dist/assets/*.js, asserts count > 0, and asserts no bundle contains cr.shell.root. PRESERVE THOSE BYTES - they are this row's own step-1 work, they match the step text, and they are the artifact fence. The other three owned paths are clean.",
    "risks": "A completed-step record is not evidence the deliverable is on the branch; verify at the bytes before skipping a step. Line counts now: main.tsx 154, development-legacy-root.tsx 11, control-room-smoke.spec.ts 59 (dirty), journeys.spec.ts 147 - all far under the 250 target, so the <=250/<400 caps are not a constraint on this work. DoD 1 is VERIFY-OR-BLOCK: if the legacy selectors cannot be compile/build fenced within these four paths, report blocked rather than leave ?v1=1 authoritative. Step 4 requires a restored divergence drill mutating only the DEVELOPMENT_BUILD constant to true, with a $env:TEMP backup and SHA256 equality on restore - that constant must exist first. Shared checkout: HEAD moved four times in the hour before this claim; re-measure porcelain before any drill and do not stash or normalize the dirty smoke spec.",
    "keyFiles": [
      "apps/control-room/src/main.tsx",
      "apps/control-room/src/development-legacy-root.tsx",
      "apps/control-room/src/entry-route.ts",
      "apps/control-room/src/shell-mode.ts",
      "apps/control-room/src/shell-mode-view.tsx",
      "tests/e2e/control-room/control-room-smoke.spec.ts",
      "tests/e2e/control-room/journeys.spec.ts"
    ]
  },
  "planApprovedAt": "2026-09-02T20:41:51.226Z",
  "workStartedAt": "2026-08-31T12:19:56.992Z",
  "blockedReason": "DoD 1's VERIFY-OR-BLOCK clause fires: legacy selectors CANNOT be compile/build fenced inside the four-file set, because a FIFTH file pins the exact static import the fence must remove. Implementation is COMPLETE and green in every owned lane. Scope question, not a defect. Evidence: comment-81d927c1.\n\nCONFLICT, measured at one tree:\n(a) DoD 1/2 ACHIEVED. main.tsx's static legacy imports replaced by a compile-time-gated React.lazy/Suspense composition. Fresh build = ONE asset, 413.10 kB (was 506.10), cr.shell.root 0, cr.banner.fixture 0, cr2.shell.root 1.\n(b) tests/e2e/control-room/journey-coverage.test.ts:258-268 — tracked, CLEAN at HEAD, not foreign WIP — asserts by SOURCE TEXT that main.tsx contains `import { ShellModeRoot } from` AND `return <ShellModeRoot`. That import is what drags shell/frame.tsx and the cr.shell.root literal into the bundle.\n\nEvery string satisfying (b) reintroduces the static edge and re-reds (a). `import type` does not match the regex; aliasing still needs the literal return arm. Mutually exclusive on one file.\n\nWHY I AM NOT DECIDING IT: that file is not among my four owned paths. Rail 1 says request an amendment or split before expanding; the unnamed-consumer rail says stop and report, never reshape the producer to evade the check. Reverting would leave ?v1=1 authoritative in the shipped bundle, which DoD 1 forbids.\n\nINPUT, not a decision: all three chooseRoot arms survive; the legacy arm now goes through the lazily imported DevelopmentLegacyRoot, whose module imports ShellModeRoot. Only the SPELLING moved.\n\nUNBLOCK, either works, ~one short step:\n1. Amend this row to own journey-coverage.test.ts as a fifth path, re-aiming that arm at the lazy composition; or\n2. Split the re-aim into a successor row, declared in dependsOn.\n\nON DISK: main.tsx +47/-9 plus the adopted artifact fence in control-room-smoke.spec.ts. Steps 1-4 done. Owned lanes: typecheck 0; unit 113 files / 1564 tests; Playwright 9 tests / 9 passed.",
  "blockedResourceId": null,
  "blockedFromStatus": "WORKING",
  "blockedAt": "2026-09-02T20:58:22.160Z",
  "blockedOnTaskIds": null,
  "dependsOn": [
    "task-d36dda1396bf48de8caae3ebbd93731e",
    "task-75bb88d241894a43b6c6981f355bc220"
  ]
}