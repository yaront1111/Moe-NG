{
  "id": "task-6f786c58cabf4f85be8ed4135e68a752",
  "epicId": "epic-bf111658f9694b558bdc5596bbf0f924",
  "title": "R3 disaster-recovery completion composition",
  "description": "Objective: make `recovery.complete` a real durable command that can clear QUIESCED only for the exact installed restore and fully reconciled external world. Deliverable: a daemon recovery-completion service that recomputes a versioned R3 recovery digest over BackupGeneration/cursor, RecoveryAnchor install, incarnation/key epoch, configured inventory coverage and dispositions, verifies a current HUMAN R3 approval with reason and recent step-up bound to that digest, then calls the existing project reducer and durable store exactly once. MEASURED GAP at HEAD 1ce0059: core `RecoveryCompletionWitness` and `reduceProject` exist and generic policy supports R3, but no daemon service builds the witness, no canonical recovery digest exists, and no approval is bound to restore/inventory bytes. NOT in scope: minting the restore identity, installing slots, enumerating external state, weakening the embargo, UI controls, GO_QUIESCE/GO_ACTIVATE, or tests/fault. Hard dependencies: task-5606947a9d7d4f228dc63e6ce4dea69a, task-b6e3dd2af916490fb2bc4d375a530683, task-8a01c025b65246aca09e69d82e009587, and task-cf7fb147bd1c47698cbd65c9535370aa must be DONE and consumed. Owned paths: apps/daemon/src/recovery/recovery-completion.* plus minimal command-registry/index wiring. Verification: pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test.",
  "definitionOfDone": [
    "The production service recomputes one canonical versioned digest over project, backup generation/cursor/RPO, installed anchor slot and command, current incarnation/key epoch, exact configured inventory class set, coverage proofs, item dispositions, quarantine refs, and reconciliation digest; callers cannot supply the accepted digest as authority.",
    "Only a current HUMAN approval classified R3 and carrying a non-empty reason plus recent step-up reference, itself bound to that exact digest/incarnation/project, can authorize completion; system-policy, stale, carried-forward, mismatched, or weaker approvals refuse.",
    "Missing/unverifiable evidence or unresolved UNKNOWN/quarantined inventory returns `UNKNOWN_TRUTH` or `RECOVERY_RECONCILIATION_REQUIRED` from its originating layer; digest mismatch returns `RECOVERY_COMPLETION_DIGEST_MISMATCH` at layer `RECOVERY_COMPLETION`; tests pin which layer refused first.",
    "A valid request durably commits exactly one `recovery.complete` decision/event using the existing reducer, changes the selected database from QUIESCED/RECOVERY_REQUIRED to READY, and releases the effect embargo only in the same committed authority sequence; replay returns the stored decision without a second transition.",
    "Any post-approval change to anchor, generation, key epoch, coverage, inventory, disposition, quarantine, policy, reason, or step-up binding invalidates completion and leaves the project quiesced.",
    "Focused tests use the shipped store, core reducer, policy approval, anchor, and inventory surfaces—not reimplemented helpers—and mutation-check the production digest comparison.",
    "Fresh `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test` exits 0."
  ],
  "taskRails": [
    "Authoritative design is D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it.",
    "`recovery.complete` is human-only R3. No policy opt-in, waiver, test fixture, doctor proposal, or caller-supplied truth may bypass exact evidence binding.",
    "This task must land real consumer edges for the backup, anchor, identity-fence, inventory, policy, core reducer, and store surfaces; exports or shape validation alone are incomplete.",
    "Where multiple layers can refuse, preserve the upstream stable code/layer and test the intended precedence.",
    "Target <=250 physical lines per production source and split before 400; this cap is per file."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "Baseline and collision check before writing a byte. Re-measure HEAD (c32d82b at planning) and confirm the four consumed surfaces still resolve: `createBackupGeneration`/`verifyBackupGeneration` from bare @moe/store; `readAnchoredIncarnation`/`hasAnchoredIncarnation`/`anchorIncarnation` from ./recovery/recovery-incarnation-anchor.js; `readInstalledRestore` from ./recovery/restore-controller.js; `readDurableLedger`/`stateOf` from ./bootstrap/bootstrap-ledger.js; `readRecoveryReconciliation` from ./recovery/recovery-inventory-ledger.js; and from bare @moe/core: `reduceProject`, `RecoveryCompletionWitness`, `RecoveryCompleteCommand`, `applyApprovalCommand`, `ApprovalDecisionRecord`, `POLICY_RISK_TIERS`. Write a uniquely named temporary probe under apps/daemon/src importing every bare specifier above, run `pnpm --filter @moe/daemon typecheck`, and delete it in the SAME command; confirm `git status --porcelain` shows no residue. CRITICAL COLLISION WARNING: task-e33747f982e0452a9f9d784fd1cb914d is WORKING RIGHT NOW and edits apps/daemon/src/daemon-command-registry.ts and apps/daemon/src/daemon-command-registry.test.ts to add an `effect.activate` kind and move the ROWS table from 20 to 21. Do NOT edit either file until step 8, and at that moment re-read both from disk rather than from any earlier snapshot — the row count you must extend may already be 21, not 20. A foreign red in those two files that your diff did not cause is a disclosure, not a defect you may repair. Finally capture the merge-base output of the exact verification command for the step-9 path-attributed comparison.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "Baseline at HEAD d01a512 (plan's c32d82b stale). Probe apps/daemon/src/probe-r3-completion-edge-682e25b5.ts imported every bare/relative specifier the plan names: @moe/store createBackupGeneration+verifyBackupGeneration; @moe/core reduceProject, applyApprovalCommand, POLICY_RISK_TIERS + types ApprovalDecisionRecord/RecoveryCompleteCommand/RecoveryCompletionWitness; ./recovery/recovery-incarnation-anchor.js anchorIncarnation/hasAnchoredIncarnation/readAnchoredIncarnation; ./recovery/restore-controller.js readInstalledRestore; ./bootstrap/bootstrap-ledger.js readDurableLedger/stateOf; ./recovery/recovery-inventory-ledger.js readRecoveryReconciliation. pnpm --filter @moe/daemon typecheck EXIT 0, probe deleted in the same command, git status shows NO_PROBE_RESIDUE.\n\nBASELINE GATE (merge-base state, before any byte of mine): `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test` EXIT 0 — Test Files 85 passed (85), Tests 1738 passed (1738). Zero foreign red, so any failure at step 9 is attributable to my diff.\n\nFIVE MEASURED CORRECTIONS to the plan, all re-measured on disk:\n1. COLLISION ALREADY RESOLVED: task-e33747f982e0452a9f9d784fd1cb914d HAS LANDED. daemon-command-registry.ts is 261 lines (plan said 247) and already carries EFFECT_ACTIVATE_COMMAND_KIND. The ROWS table is already 21, not 20, and there are THREE assertions to raise at step 8, not two: .test.ts:159 expect(ROWS).toHaveLength(21), :160 deps.registry.size, :328 ports.registry.size. Step 8 extends 21 -> 22.\n2. readRecoveryReconciliation(store, projectId, recordDigest) takes THREE arguments; the plan's step-5 text shows two. projectId comes from the server-assembled envelope, and the reader itself refuses RECORD_CONFLICT when the stored record's projectId disagrees, so this is a real fence rather than a widened caller surface.\n3. RecoveryCompleteCommand extends ProjectCommandBase = {commandId, expectedVersion} and carries NO projectId. The recovery command identity (commandId/correlationId/decidedAt/principalId/projectId) is assembled server-side by daemon-command-registry requestOf(), exactly as effect.activate does. So PAYLOAD_KEYS [\"approval\",\"command\",\"expectedVersion\",\"reconciliationDigest\"] stands, with approval = the ApprovalDecisionRecord and command = the ApprovalDecideCommand -- the four pointers step 5 enumerates.\n4. expectedVersion CAS is answered by THIS layer: the service reads V = store.getAggregateVersion(projectId) (same source restore-controller.ts:283 uses) and refuses RECOVERY_COMPLETION_STALE when the caller's expectedVersion != V; the SERVER-read V is what reaches reduceProject. Taking the caller's number into the reducer instead would make core's EXPECTED_VERSION_CONFLICT answer first and leave RECOVERY_COMPLETION_STALE unreachable by any test -- an unguarded branch under epic rail 6.\n5. No stepUpAuthRef shape in this repo carries an instant (all existing values are opaque: \"stepup-1\", \"stepup/webauthn-1\", \"step-up-1\"), and core's 300s STEP_UP_WINDOW at identity/authenticate-command.ts:64 compares numeric recentStepUpAt against now, a surface the ApprovalDecisionRecord cannot reach. This module therefore defines its own versioned recovery step-up ref carrying its instant and checks recency against the server's decidedAt with the same 300s window; an opaque legacy ref is REFUSED, which is the DoD-2 \"weaker approval refuses\" clause.\n\nFixture path confirmed on disk, all production surfaces: restore-test-harness.ts restoreHarness()/anchoredIncarnation()/restoreRequest() + runRestoreQuiesce for a real QUIESCED project with an installed restore in the ACTIVE slot (RESTORE_BINDING_SLOT === \"ACTIVE\" === RECOVERY_BINDING_SLOTS[0], so one real restore also satisfies readCurrentRecoveryAuthenticationBinding, which recordRecoveryReconciliation requires); recovery-inventory-ledger.test.ts supplies the exact six-class/seven-population facts shape.\n\nQUARANTINE TRAP RE-CONFIRMED: recovery-inventory-invariants.ts deriveRecoveryReconciliationTruth returns UNKNOWN only for an UNKNOWN proof or an UNKNOWN item disposition, so a QUARANTINED item still yields truth COMPLETE with a null coordinator. This service is the only thing that refuses it; drill B in step 9 exists for exactly that.",
      "startedAt": "2026-08-15T18:21:37.278Z",
      "completedAt": "2026-08-15T18:25:41.939Z"
    },
    {
      "stepId": "step-2",
      "description": "RED: canonical R3 digest tests. Create the digest half of apps/daemon/src/recovery/recovery-completion.test.ts (the service half lands in step 4). Pin a versioned domain-separated digest over the SERVER-READ evidence tuple, never over caller bytes: projectId, projectTag, backupCursor, backupGenerationDigest, RPO/cursor bound, installed anchor slot + restore command identity, incarnationRef, keyEpochRef, anchorBindingDigest, the exact configuredClasses array, the ordered coverage proofs, the ordered item dispositions with their quarantineRefs, and the reconciliation recordDigest. Assert: (a) the digest is stable across two calls on equal input; (b) a GENERATED field-sensitivity sweep flips exactly one field at a time and every case changes the digest — and the sweep asserts its own exact non-zero case count, because a sweep that produces zero cases passes while testing nothing; (c) the sweep is bounded by the record's own key set, not a hand-written list a later field could escape; (d) the domain tag makes the digest non-equal to a bare hash of the same canonical bytes; (e) reordering configuredClasses/proofs/items is refused or normalized deterministically — pin which. Record the suite RED.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion.test.ts"
      ],
      "newFiles": [
        "apps/daemon/src/recovery/recovery-completion.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/recovery/recovery-completion.test.ts"
      ],
      "note": "RED recorded. Created apps/daemon/src/recovery/recovery-completion.test.ts (digest half, 7 cases). Focused run `npx vitest run --root . --config package.json src/recovery/recovery-completion.test.ts` from apps/daemon FAILS: \"Cannot find module './recovery-completion-digest.js'\" — Test Files 1 failed (1), Tests no tests.\n\nNote the focused-run incantation: `--root .` ALONE still resolves the repo-root config (include adapters/**, packages/**, tests/**) and exits \"No test files found\" with code 1, which reads like a red but tests nothing. The daemon's own script is `vitest run --root . --config package.json src`, so BOTH flags are required.\n\nCases pinned, matching step-2 clauses (a)-(e):\n(a) stability — the digest is hex64 and equal across two calls on a structurally-rebuilt equal tuple.\n(b) generated field-sensitivity sweep asserting EXACTLY 22 cases before the loop, plus a distinctness assertion that all 22 flip digests differ from each other (a colliding preimage would be the concatenation defect framing exists to stop).\n(c) the sweep is generated from Object.keys of the evidence and of each array's element 0 — bounded by the record's own key set, never a hand-written case list. Guarded against the \"a generated table cannot police its own generator\" trap by cross-checking the swept top-level key set against a HAND-WRITTEN EVIDENCE_KEYS census of 14 names and a hand-written EXPECTED_SWEEP_CASES = 11 scalars + configuredClasses[0] + 4 proof fields + 6 item fields. A new evidence field therefore reddens two assertions instead of silently shrinking the sweep.\n(d) domain separation asserted against the PRODUCTION preimage builder `recoveryCompletionPreimage`, not a reimplemented canonicalizer: sha256 of the same canonical bytes must NOT equal the digest.\n(e) ORDER PINNED AS SIGNIFICANT, not normalized and not refused: reversing configuredClasses, proofs or items each changes the digest. The record's arrays are already canonical-ordered by recovery-inventory-codec, so a reorder is a different evidence tuple and must not certify the same completion.\nPlus a length-framing collision case that moves one byte across the backupCursor/projectTag boundary at constant total length — only per-component framing can separate those two tuples.\n\nEvidence tuple pinned by the suite: anchorBindingDigest, backupCursor, backupGenerationDigest, configuredClasses, incarnationRef, items (class/disposition/identity/population/quarantineRef/sourceProofDigest), keyEpochRef, projectId, projectTag, proofs (class/itemCount/sourceProofDigest/truth), reconciliationRecordDigest, restoreBindingSlot, restoreCommandId, restoreGenerationDigest — every field DoD 1 enumerates, including the installed anchor slot and restore command identity.",
      "startedAt": "2026-08-15T18:25:44.155Z",
      "completedAt": "2026-08-15T18:27:35.580Z"
    },
    {
      "stepId": "step-3",
      "description": "GREEN: create apps/daemon/src/recovery/recovery-completion-digest.ts (target <=250 lines) plus its one-line sibling bridge apps/daemon/src/recovery/recovery-completion-digest.js containing exactly `export * from \"./recovery-completion-digest.ts\";` — every .ts in this tree has one and both tsc and vitest are blind to its absence. Export RECOVERY_COMPLETION_LAYER = \"RECOVERY_COMPLETION\", RECOVERY_COMPLETION_DIGEST_DOMAIN (a versioned domain tag), this module's OWN frozen closed code list including at minimum RECOVERY_COMPLETION_REQUEST_MALFORMED, RECOVERY_COMPLETION_EVIDENCE_ABSENT, RECOVERY_COMPLETION_EVIDENCE_MISMATCH, RECOVERY_COMPLETION_DIGEST_MISMATCH, RECOVERY_COMPLETION_APPROVAL_INVALID, RECOVERY_COMPLETION_STALE, RECOVERY_COMPLETION_STORE_UNAVAILABLE, and RECOVERY_RECONCILIATION_REQUIRED; and `recoveryCompletionDigest(evidence)` returning the hex64. Do NOT append to the recovery-inventory contract's closed RECOVERY_INVENTORY_UPSTREAM_CODES array — sibling suites sweep it and inventory vocabulary is out of scope. Follow the upstream-preservation shape already shipped in this repo (own code + own layer + a separate `upstream` field holding the foreign {code, layer} verbatim, null when this layer refused), exactly as @moe/core's ProjectConfigurationCodecRefusal and the daemon's ActivationLedgerRefusal do. Length-frame each component of the digest preimage so distinct tuples cannot collide by concatenation, and use a separator that is a legal identifier character — a NUL reads like the obvious delimiter and is rejected downstream by the store's requireIdentifier. Step-2 suite goes green.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion-digest.ts",
        "apps/daemon/src/recovery/recovery-completion-digest.js"
      ],
      "newFiles": [
        "apps/daemon/src/recovery/recovery-completion-digest.ts",
        "apps/daemon/src/recovery/recovery-completion-digest.js"
      ],
      "modifiedFiles": [
        "apps/daemon/src/recovery/recovery-completion-digest.ts",
        "apps/daemon/src/recovery/recovery-completion-digest.js"
      ],
      "note": "GREEN. Created apps/daemon/src/recovery/recovery-completion-digest.ts (249 physical lines by `grep -c ''`, under the 250 target) plus the one-line sibling bridge recovery-completion-digest.js containing exactly `export * from \"./recovery-completion-digest.ts\";`. Step-2 suite: Test Files 1 passed (1), Tests 7 passed (7). `tsc --project tsconfig.json` exit 0.\n\nExports: RECOVERY_COMPLETION_LAYER = \"RECOVERY_COMPLETION\"; RECOVERY_COMPLETION_SCHEMA_VERSION = \"moe-recovery-completion/1\"; RECOVERY_COMPLETION_COMMAND_KIND = \"recovery.complete\"; RECOVERY_COMPLETION_DIGEST_DOMAIN = \"moe-recovery-completion-digest/1\"; RECOVERY_COVERAGE_PROOF_DIGEST_DOMAIN = \"moe-recovery-coverage-proof/1\"; RECOVERY_COMPLETE_PAYLOAD_KEYS (frozen, sorted); the frozen closed RECOVERY_COMPLETION_CODES carrying all eight codes the step names; recoveryCompletionPreimage; recoveryCompletionDigest; recoveryCoverageProofDigest; recoveryCompletionRefusal + the RecoveryCompletionEvidence/Proof/Item and Refused/Upstream types.\n\nConstraint compliance:\n- Did NOT touch recovery-inventory-contract.ts's closed RECOVERY_INVENTORY_UPSTREAM_CODES. This module owns its own code list; a foreign refusal keeps its producer's code and layer verbatim in a separate `upstream` field, null when this layer refused — the ActivationLedgerRefusal / ProjectConfigurationCodecRefusal shape already shipped in this repo.\n- LENGTH FRAMING: every component is emitted as `<byteLength>.<utf8>.` with the length measured in BYTES via TextEncoder, so a multi-byte identity cannot be framed as a shorter one. Separator is `.`, NOT a NUL — a NUL is rejected downstream by the store's identifier validation while every local test would still pass.\n- Two extra framing hazards closed beyond the step text: a number is framed through toFixed(0) so 1 and 1.0 cannot spell differently, and `quarantineRef: null` gets its own single-byte marker (\" \") while a present ref is prefixed \"s\", so a literal string \"null\" can never be mistaken for absence.\n- TWO DOMAINS, deliberately distinct: the completion digest covers the whole evidence tuple, the coverage-proof digest covers only configuredClasses + ordered proofs (the narrower hash core's RecoveryCompletionWitness.coverageProofHash carries). One shared tag would let a coverage hash be presented where a completion hash is demanded; the suite asserts they differ.\n- ORDER PINNED SIGNIFICANT, never sorted: recovery-inventory-codec already fixes one canonical order for a stored record, so a reordered tuple is a different observation and must not certify under the approved digest.\n- recoveryCompletionPreimage is exported so the domain-separation test asserts against the PRODUCTION canonicalizer instead of a test-side reimplementation; it returns bytes and never a verdict, so it is not an authority surface.",
      "startedAt": "2026-08-15T18:27:37.949Z",
      "completedAt": "2026-08-15T18:29:18.607Z"
    },
    {
      "stepId": "step-4",
      "description": "RED: service tests against the not-yet-written `runRecoveryCompleteCommand(store, input)`. Build fixtures ONLY from shipped production surfaces — seed the project with the real bootstrap commands, quiesce it with the real `runRestoreQuiesce`, anchor with the real `anchorIncarnation`, and write the reconciliation record with the real `recordRecoveryReconciliation`; a reimplemented helper is a rejection under DoD 6. Open SQLite per test, never in a describe body; afterAll close then rmSync. POSITIVE CONTROL: a QUIESCED project with an installed restore, a matching anchor, and a stored reconciliation record whose truth is COMPLETE and whose every item disposition is non-QUARANTINED, plus a HUMAN R3 approval whose exactRevisionHash equals the recomputed digest and which carries a non-empty decisionReason and a step-up ref inside the 300s window — assert exactly one committed recovery.complete decision, exactly one ProjectRecovered event, project state moving to READY with recoveryRequired false, and the witness carrying truthClass HUMAN_APPROVED. Then assert the embargo release the same way the embargo itself observes it: `stateOf(readDurableLedger(store, projectId), projectId)` reports READY/recoveryRequired false in that same committed sequence — the effect embargo is a pure function of that state, so asserting the state IS asserting the release, and it avoids coupling this suite to task-e33747f9's in-flight module. REPLAY: the same commandId returns the stored decision with no second transition and no second event. REFUSAL PRECEDENCE, each pinning exact code AND layer AND which layer answered FIRST: malformed envelope -> RECOVERY_COMPLETION_REQUEST_MALFORMED/DAEMON_INGRESS; unknown or unreadable reconciliation digest -> the ledger's own RECORD_NOT_FOUND / RECORD_UNREADABLE / RECORD_CONFLICT at RECOVERY_INVENTORY_LEDGER verbatim; record truth UNKNOWN or a non-null coordinator answer -> UNKNOWN_TRUTH at RECOVERY_INVENTORY verbatim; any item QUARANTINED or carrying a quarantineRef -> RECOVERY_RECONCILIATION_REQUIRED at RECOVERY_INVENTORY (note `deriveRecoveryReconciliationTruth` treats only UNKNOWN as incomplete, so a COMPLETE record CAN carry quarantined items — this service is what refuses them, and a test must prove it); missing anchor / restore / project state or any disagreement among them -> RECOVERY_COMPLETION_EVIDENCE_ABSENT or _MISMATCH at RECOVERY_COMPLETION with upstream preserved; digest mismatch -> RECOVERY_COMPLETION_DIGEST_MISMATCH at RECOVERY_COMPLETION; SYSTEM_POLICY actor, riskTier below R3, empty or absent decisionReason, absent or stale step-up, wrong approval lifecycle -> the core `applyApprovalCommand` code surfaced verbatim, or RECOVERY_COMPLETION_APPROVAL_INVALID where core has no opinion; a non-QUIESCED project -> the core reducer's own code; CAS loss -> RECOVERY_COMPLETION_STALE with the store code in upstream. Every refusal also asserts zero decisions and zero events were written. Record the suite RED.",
      "status": "IN_PROGRESS",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion.test.ts"
      ],
      "startedAt": "2026-08-15T18:29:21.005Z"
    },
    {
      "stepId": "step-5",
      "description": "GREEN part 1 — evidence gathering and digest comparison, in apps/daemon/src/recovery/recovery-completion.ts (+ its one-line .js bridge). Decode the envelope first: exact payload keys, nothing else (structural refusal precedes every evidence read). The request carries POINTERS ONLY — reconciliationDigest, the approval record, the approval command, and expectedVersion — and NEVER the accepted digest itself; DoD 1 forbids a caller supplying the digest as authority, so the service recomputes it from what it reads. Read, in this order, wrapping each in try/catch and mapping a thrown DurableStoreError to RECOVERY_COMPLETION_STORE_UNAVAILABLE with its code in upstream: `readRecoveryReconciliation(store, reconciliationDigest)`; then the record's truth/coordinator/upstream gate; then the QUARANTINED-item gate; then `readInstalledRestore(store, record.projectId)`; then `readAnchoredIncarnation`; then `stateOf(readDurableLedger(store, record.projectId), record.projectId)`. Cross-check that the record's incarnationRef, keyEpochRef and anchorBindingDigest agree with the anchor and the installed restore, and that the project is QUIESCED with recoveryRequired true — any disagreement is RECOVERY_COMPLETION_EVIDENCE_MISMATCH, any absence RECOVERY_COMPLETION_EVIDENCE_ABSENT, both at RECOVERY_COMPLETION with the upstream layer preserved. Snapshot every store-returned object/array through own enumerable data descriptors before use. Then call `recoveryCompletionDigest` over the assembled server-read tuple and compare it to the approval record's `exactRevisionHash`; inequality is RECOVERY_COMPLETION_DIGEST_MISMATCH at RECOVERY_COMPLETION. Keep this file <=250 physical lines measured with `grep -c ''` — PowerShell Measure-Object -Line undercounts — and split before 400.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion.ts",
        "apps/daemon/src/recovery/recovery-completion.js"
      ],
      "newFiles": [
        "apps/daemon/src/recovery/recovery-completion.ts",
        "apps/daemon/src/recovery/recovery-completion.js"
      ]
    },
    {
      "stepId": "step-6",
      "description": "GREEN part 2 — approval verification, witness, and the single durable commit, in the same module. Run the supplied approval record and command through core `applyApprovalCommand` and surface its refusal code UNCHANGED (the same discipline apps/daemon/src/planning/planning-services.ts:163 already applies for the planning journey); this daemon owns only the bindings core cannot judge. On top of core's verdict require, each with its own assertable refusal: actorKind HUMAN and NOT SYSTEM_POLICY; riskTier exactly R3; decision APPROVE; a non-empty decisionReason; a non-null stepUpAuthRef; truthClass HUMAN_APPROVED; and the approval bound to THIS project and THIS incarnation. Note the existing `approval.decide` handler binds exactRevisionHash to a planning run's submissionHash and is NOT a recovery approval path — do not route through it. Build the RecoveryCompletionWitness EXACTLY as core's `validRecovery` demands: coverageProofHash = the digest over the record's configuredClasses + ordered proofs, inventoryReconciliationHash = the record's own recordDigest, recoveryDecisionRef = the approval's approvalRef, recoveryIncarnationRef = the anchored incarnation ref, truthClass = \"HUMAN_APPROVED\" as a server-owned literal, never forwarded from the caller. Call `reduceProject` with the RecoveryCompleteCommand and surface its refusal code verbatim. Then perform ONE `store.commitExpectedVersionDecision` at the caller's expectedVersion writing the single ProjectRecovered event and the decision result together; EXPECTED_VERSION_CONFLICT maps to RECOVERY_COMPLETION_STALE with the store code in upstream. A REPLAYED disposition returns the stored decision and performs no second transition. Nothing is written on any refusal path — accumulate in locals and construct the commit only after every gate passes, so a partial completion is unreachable rather than merely untested. Add TSDoc stating that the effect embargo clears because it is a pure function of the project state this commit changes, not because anything here touches it.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion.ts",
        "apps/daemon/src/recovery/recovery-completion.js",
        "apps/daemon/src/recovery/recovery-completion.test.ts"
      ]
    },
    {
      "stepId": "step-7",
      "description": "End the inventory island and publish the surface. apps/daemon/src/index.ts (347 physical lines at HEAD; keep it at or below that by consolidating only semantically equivalent adjacent export statements) currently exports recovery-incarnation, recovery-incarnation-anchor, recovery-succession and doctor-commands but NOTHING from the recovery-inventory area — `recordRecoveryReconciliation` and `readRecoveryReconciliation` have zero root exports and zero production consumers today, which is why task rail 3's consumer-edge requirement is unmet for the inventory surface. Export from the @moe/daemon root: `runRecoveryCompleteCommand` and the completion refusal vocabulary/types, plus `readRecoveryReconciliation`, `recordRecoveryReconciliation` and the RecoveryReconciliationRecord/Refusal types so the surface is reachable rather than an island. Update apps/daemon/src/index-surface.test.ts (573 physical lines at HEAD) with the exact namespace and type assertions for each new export; a type-only export is invisible to a runtime count test, so prove each published TYPE by importing it in a compiled position rather than by counting keys. Re-measure both line counts with `grep -c ''` after editing.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/index.ts",
        "apps/daemon/src/index-surface.test.ts"
      ]
    },
    {
      "stepId": "step-8",
      "description": "Wire `recovery.complete` as a real daemon command. FIRST re-read apps/daemon/src/daemon-command-registry.ts and apps/daemon/src/daemon-command-registry.test.ts from disk — task-e33747f982e0452a9f9d784fd1cb914d may have landed an `effect.activate` kind and moved the ROWS table to 21 while you worked; extend whatever is actually there and never restore an earlier snapshot over it. In the registry: import the completion kind, its payload keys and `runRecoveryCompleteCommand`; widen `WiredCommandKind` and the `decisionOf` union; add the PAYLOAD_KEYS entry with the exact sorted allow-list (approval, command, expectedVersion, reconciliationDigest); add the discriminator, schemaVersion branch, handler branch and requiredCapability = CAPABILITIES.ADMIN; and extend `agentCapabilitiesFor`. Note the capability is NOT what makes this human-only — R3 human-only enforcement lives entirely in the approval gate from step 6, and a comment should say so, because a reader who assumes the capability is the fence will later weaken the approval check. In the registry test, add one ROWS entry and raise BOTH the `expect(ROWS).toHaveLength(N)` and `expect(deps.registry.size).toBe(N)` assertions plus the it() title; the new row must pass the existing smuggled-key case unchanged. If the registry file exceeds 250 physical lines after this edit it is still well under the 400 split threshold — do not restructure a file another task is concurrently editing.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-registry.ts",
        "apps/daemon/src/daemon-command-registry.test.ts"
      ]
    },
    {
      "stepId": "step-9",
      "description": "Mutation drills and the verification gate. Drill TWO production guards, one at a time, each restored from your own saved bytes before the next — NEVER `git checkout`, which reverts to HEAD and destroys uncommitted work in this shared worktree. Drill A, the digest comparison: remove or invert the production `digest === approval.exactRevisionHash` check and confirm the NAMED digest-mismatch test goes red on its exact code/layer assertion, not on a crash and not on a hang — bound the run, because a hang reads as a held guard. Drill B, the quarantine gate: make the QUARANTINED-item check a no-op and confirm the named quarantine test goes red; a guard whose body is a plain no-op has no operator to flip, so if the drill stays green make the body THROW instead to prove the hook is still on the production path. Confirm each drill reddens for the RIGHT reason — over-reddening across many unrelated tests means a broken environment, not a held guard. Restore and prove green. Then run the fresh gate `pnpm --filter @moe/daemon typecheck && pnpm --filter @moe/daemon test`, `&&`-chained (a `;`-chained run masks a red leg) and never piped through tail (a piped run reports tail's exit code). A focused rerun needs `--root .` or it resolves the root config and finds zero tests, which exits 0 and reads as green. Compare against the step-1 merge-base baseline; the HEAD-minus-baseline failing-path set intersected with this task's owned paths must be EMPTY, and any foreign red — especially in the two registry files task-e33747f9 also edits — is reported verbatim with its exact failing lines, never hidden and never fabricated green.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion.ts",
        "apps/daemon/src/recovery/recovery-completion-digest.ts",
        "apps/daemon/src/recovery/recovery-completion.test.ts"
      ]
    },
    {
      "stepId": "step-10",
      "description": "Adversarial self-review before complete_task. Load the `adversarial-self-review` skill and read the owned diff as an attacker. Concretely: can ANY path reach the commit without the digest comparison, the quarantine gate, or the R3/HUMAN/step-up gate — including a thrown-and-caught store error landing in a catch above a gate; can a caller supply `truthClass`, the accepted digest, the incarnation ref, or the witness hashes and have them honoured rather than server-derived; does a SYSTEM_POLICY approval, an R2 approval, a carried-forward approval from an earlier digest, or a step-up outside the 300s window get through; does replay ever produce a second ProjectRecovered event or a second transition; does any refusal write a decision, an event, or leave the project non-QUIESCED; is any refusal answered by an EARLIER guard than the one its test names — trace which branch answers for each code and confirm the step-9 drills reddened the intended guard rather than a sibling; do proxy, revoked-proxy, accessor, symbol-keyed, sparse and oversized inputs return a typed refusal rather than throwing (`Array.isArray` THROWS on a revoked proxy — ask `types.isProxy` first, as recovery-inventory-contract.ts already does); and does every asserted code actually appear in the test file, since a code absent from the file means that whole branch is unguarded. Confirm both .js bridges exist and re-export their .ts siblings, that no production file exceeds 400 physical lines by `grep -c ''`, and that no probe, scratch or generated evidence file remains. Then stage and commit ONLY this task's owned paths by explicit pathspec — never `git add -A`, reset or stash — and if the two registry files were swept into another task's whole-tree commit, report the base-ref diff `git diff <merge-base>..HEAD -- <owned paths>` instead of trying to repair the foreign commit.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/recovery/recovery-completion.ts",
        "apps/daemon/src/recovery/recovery-completion-digest.ts",
        "apps/daemon/src/recovery/recovery-completion.test.ts",
        "apps/daemon/src/index.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ]
    }
  ],
  "status": "WORKING",
  "assignedWorkerId": "worker-682e25b5",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Returned to BLOCKED by governor-18a42000 on the human's instruction to fix small problems directly. Promoted to PLANNING with one of four hard dependencies unmet and no recorded justification (reopenReason empty). BLOCKED rather than BACKLOG so the gate is recorded and the next readiness sweep cannot silently re-promote it.\n\nLITERAL CLAUSE: \"Hard dependencies: task-5606947a9d7d4f228dc63e6ce4dea69a, task-b6e3dd2af916490fb2bc4d375a530683, task-8a01c025b65246aca09e69d82e009587, and task-cf7fb147bd1c47698cbd65c9535370aa must be DONE and consumed.\"\n\nMEASURED — three of four are met, which is exactly why this one is easy to promote by mistake:\n- task-5606947a (Cursor-bound complete backup generation) = DONE\n- task-b6e3dd2a (Crash-safe two-slot recovery anchor installer) = DONE\n- task-8a01c025 (Recovery-incarnation command authentication fence) = DONE\n- task-cf7fb147 (Recovery inventory quarantine coordinator) = **BLOCKED** — NOT met\n\nA 3-of-4 clause is still an unmet clause. task-cf7fb147 was independently re-audited today against the 17 tasks that closed on 2026-08-15 and came back genuinely gated, so this is not stale information.\n\nNOTE THE SECOND HALF OF THE CLAUSE, which is stricter than a status check and is the part most likely to be skipped: the four dependencies must be \"DONE **and consumed**\". Consumed means a real production consumer edge exists, not merely that the producing task reached DONE. Under Clause 1, exporting symbols is not composition, and the runtime-loadability gate proves a package LOADS while nothing proves anything IMPORTS it. When task-cf7fb147 lands, verify each of the four by grep to a real production call site before planning — three DONE flags do not establish three consumer edges.\n\nDO NOT RE-PROMOTE UNTIL task-cf7fb147 is DONE and all four dependencies are verified consumed on disk.\n\nNOTE ON AUTHORITY: governor-74ff6cb3 drives board state and promoted this task; I hold an audit role and acted only because the human authorised it. If the promotion was deliberate, record that reasoning here before this moves again.",
  "createdBy": "WORKER",
  "parentTaskId": "task-0c89476b78044024b07c86c0c8986bd0",
  "priority": "CRITICAL",
  "order": 23,
  "comments": [],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-09T12:08:18.169Z",
  "updatedAt": "2026-08-15T18:29:21.005Z",
  "contextFetchedBy": [
    "architect-f39f0c46",
    "architect-8b21d0bb",
    "architect-797c65f6",
    "worker-682e25b5"
  ],
  "metrics": {
    "firstClaimAt": "2026-08-15T18:02:20.406Z",
    "plannedStepCount": 10,
    "plannedDistinctFileCount": 9,
    "executedStepCount": 3
  },
  "blockedFromStatus": null,
  "blockedAt": null,
  "blockedReason": null,
  "blockedResourceId": null,
  "planSizeWarnings": [
    "Plan has 10 steps (target <=8); plans over 12 are rejected. Consider splitting the task before it grows.",
    "Plan touches 9 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-08-15T18:12:49.838Z",
  "planningNotes": {
    "approachesConsidered": "BLOCKING on task-cf7fb147bd1c47698cbd65c9535370aa was measured, presented to the human, and explicitly OVERRULED: the direction was to take the full production path without blocking. That is workable because the capability this task actually consumes from the inventory area — a durable, digest-addressed RecoveryReconciliationRecord carrying truth, coverage proofs, item dispositions and quarantineRefs — is already shipped by the DONE inventory chain (task-00956ac3, task-091c93db, task-d7da9be4, task-47eecd22, task-f6cf8d16). What cf7fb147 still owes is the quarantine RESOLUTION path, and this task only ever REFUSES on unresolved quarantine, so it does not need one. Rejected: routing the recovery approval through the existing `approval.decide` handler — that handler binds exactRevisionHash to a planning run's submissionHash (planning-services.ts:163-161) and is the J1 plan journey, not a recovery approval. Rejected: changing `deriveRecoveryReconciliationTruth` so QUARANTINED yields UNKNOWN — that file is foreign-owned by the DONE task-f6cf8d16 and its output feeds the record digest, so editing it would alter a shipped durable surface; this service refuses quarantined items itself instead, which is exactly what DoD 3 asks for. Rejected: wiring `recovery.reconcile_external` (the writer command) in this task — it needs its own adapter module plus bridge, which would put the plan at 11-12 distinct files, and \"enumerating external state\" is this task's explicit NOT-in-scope. Landing the daemon-root export of the ledger here gives that future task a reachable surface instead of an island. Rejected: asserting embargo release through task-e33747f9's activation-embargo module — it is WORKING and unlanded; asserting the project state it reads is equivalent and uncoupled.",
    "codebaseInsights": "MEASURED AT HEAD c32d82b (description's 1ce0059 is stale). Hard dependencies, re-measured at their public roots rather than read off DONE statuses:\n- task-5606947a (backup generation): `packages/store/src/index.ts:62` exports createBackupGeneration + verifyBackupGeneration. REACHABLE.\n- task-b6e3dd2a (anchor installer): recovery-incarnation-anchor.ts exports hasAnchoredIncarnation:54, readAnchoredIncarnation:109, readAnchoredGenesisIncarnation:127, anchorIncarnation:153, and apps/daemon/src/index.ts:231 re-exports it. REACHABLE.\n- task-8a01c025 (identity fence): recovery-incarnation.ts:82 createRecoveryIncarnationService, re-exported at index.ts:224. REACHABLE.\n- task-cf7fb147 (quarantine coordinator): BLOCKED, 0 plan steps, and `grep -rn \"quarantineCoordinator|coordinateQuarantine|RecoveryQuarantine|recoveryQuarantine\"` over apps+packages returns ZERO production symbols. ABSENT — human directed proceeding regardless.\nCORE, all reachable from the bare @moe/core root: RecoveryCompletionWitness = {coverageProofHash, inventoryReconciliationHash, recoveryDecisionRef, recoveryIncarnationRef, truthClass} (project-contract.ts:45); RecoveryCompleteCommand {kind:\"recovery.complete\", witness} (:75); ProjectRecovered event (:123); and project-validation.ts:171 `validRecovery` HARD-PINS truthClass === \"HUMAN_APPROVED\" and requires validHash on both hashes plus validProjectRef on both refs — the reducer already refuses anything weaker, so the daemon's job is the BINDING, not the tier check. project-reducer.ts:36 admits recovery.complete only from QUIESCED and :150 lands lifecycle READY with recoveryRequired false.\nAPPROVAL: ApprovalDecisionRecord (approval-contract.ts:43) carries actor, actorKind, approvalRef, decision, decisionReason, exactRevisionHash, lifecycle, policyDecisionRef, riskTier, stepUpAuthRef, truthClass, validity. approval-invalidation.ts:73 already requires a decisionReason when riskTier is R3 and :70-72 requires a stepUpAuthRef for HUMAN / null for SYSTEM_POLICY. policy-evaluation.ts:143 makes R2/R3 human-only. identity/authenticate-command.ts:64 sets STEP_UP_WINDOW = 300 seconds. `exactRevisionHash` is the field the recomputed R3 digest binds to.\nINVENTORY — THE LOAD-BEARING FINDING: the entire recovery-inventory area is BUILT AND TESTED BUT IS AN ISLAND. `recordRecoveryReconciliation` (recovery-inventory-ledger.ts:144) and `readRecoveryReconciliation` (:242) have ZERO production consumers and ZERO exports from apps/daemon/src/index.ts (grep for \"recovery\" in index.ts returns only doctor-commands, recovery-incarnation, recovery-incarnation-anchor, recovery-succession). This task lands the first consumer edge and the root export, which is what task rail 3 demands. RecoveryReconciliationRecord (recovery-inventory-contract.ts:200) carries schemaVersion, projectId, projectTag, backupCursor, backupGenerationDigest, incarnationRef, keyEpochRef, anchorBindingDigest, configuredClasses, proofs, items, truth, coordinator, upstream, recordDigest — every field DoD 1 enumerates.\nQUARANTINE TRAP: recovery-inventory-invariants.ts:159-178 `deriveRecoveryReconciliationTruth` returns UNKNOWN only when a PROOF is UNKNOWN or an ITEM disposition is UNKNOWN. A QUARANTINED item still yields truth COMPLETE and a null coordinator. So a COMPLETE record can carry quarantined items, and this completion service is the only thing that will refuse them. A test must prove that specific case or DoD 3 ships unenforced.\nREGISTRY: apps/daemon/src/daemon-command-registry.ts is 247 lines and its test pins a CLOSED ROWS table with `toHaveLength(20)` and `registry.size === 20` near line 151. task-e33747f9 is WORKING and moving that to 21 in the same two files RIGHT NOW.\nEDGES: apps/daemon/package.json declares @moe/contracts, @moe/core, @moe/runner, @moe/scheduler, @moe/store as workspace:*; pnpm-lock.yaml carries the apps/daemon importer entries. A bare-specifier probe inside apps/daemon typechecked exit 0 this session for the neighbouring configuration task, so the edge is live.\nLINE COUNTS at HEAD: apps/daemon/src/index.ts 347, index-surface.test.ts 573, daemon-command-registry.ts 247, recovery-inventory-ledger.ts 289, recovery-inventory-contract.ts 257.",
    "risks": "1. KNOWN OPEN GAP, recorded rather than hidden per the human's decision to proceed: no production command writes a RecoveryReconciliationRecord. The contract already reserves RECOVERY_RECONCILIATION_COMMAND_KIND = \"recovery.reconcile_external\" (recovery-inventory-contract.ts:28) and it has never been wired. This task's tests drive the real production writer `recordRecoveryReconciliation` directly — a shipped surface, not a mock — so DoD 6 holds, but until that command is wired the accept path is not reachable from HTTP ingress. The daemon-root export landed in step 7 is what makes wiring it a small follow-up. Flag this to the governor when handing off.\n2. LIVE FILE COLLISION: task-e33747f9 is WORKING in apps/daemon/src/daemon-command-registry.ts and .test.ts right now, moving the closed ROWS table 20 -> 21. Step 8 must re-read both files from disk at that moment. Never restore an earlier snapshot over them; a foreign whole-tree commit that captures your bytes is a known hazard to disclose, not repair.\n3. The quarantine gate is the DoD clause most likely to ship unenforced, because a COMPLETE record legitimately carries QUARANTINED items today. Drill B exists for exactly this. A no-op body has no operator to flip — if the drill stays green, make the body throw.\n4. Refusal precedence is the whole point of task rail 4: where several layers can refuse, the test must pin WHICH answered first. A test asserting only `!ok` is one added layer away from vacuous.\n5. Never let a caller supply truthClass, the accepted digest, the incarnation ref, or the witness hashes. Every one is server-derived; DoD 1 says the caller cannot supply the accepted digest as authority, and validRecovery pinning truthClass in core is a backstop, not the fence.\n6. The digest preimage must be length-framed per component and use a separator that is a legal identifier character. A bare concatenation makes distinct tuples collide; a NUL separator is rejected downstream by the store's requireIdentifier while every local test stays green.\n7. Windows/SQLite: open the store inside beforeEach or it, never a describe body — a held handle kills the vitest worker and afterAll rmSync then fails.\n8. Shared worktree: a repo-wide red may be an untracked peer file that git diff and git log both hide. Attribute by merge-base-vs-HEAD failing-path delta intersected with owned paths before completing.",
    "keyFiles": [
      "apps/daemon/src/recovery/recovery-inventory-ledger.ts",
      "apps/daemon/src/recovery/recovery-inventory-contract.ts",
      "apps/daemon/src/recovery/recovery-inventory-invariants.ts",
      "apps/daemon/src/recovery/recovery-incarnation-anchor.ts",
      "apps/daemon/src/recovery/restore-controller.ts",
      "apps/daemon/src/recovery/restore-test-harness.ts",
      "apps/daemon/src/bootstrap/bootstrap-ledger.ts",
      "apps/daemon/src/planning/planning-services.ts",
      "apps/daemon/src/daemon-command-registry.ts",
      "apps/daemon/src/index.ts",
      "packages/core/src/project/project-contract.ts",
      "packages/core/src/project/project-validation.ts",
      "packages/core/src/project/project-reducer.ts",
      "packages/core/src/policy/approval-contract.ts",
      "packages/core/src/policy/approval-invalidation.ts",
      "packages/core/src/identity/authenticate-command.ts"
    ]
  },
  "budget": {
    "wallClockMs": 9000000
  },
  "planApprovedAt": "2026-08-15T18:12:51.902Z",
  "workStartedAt": "2026-08-15T18:21:37.278Z"
}