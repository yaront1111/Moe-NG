{
  "id": "task-75bb88d241894a43b6c6981f355bc220",
  "epicId": "epic-072d85bea56c4badb07b9356ae195a98",
  "title": "[R3-UI leaf] Disable goal creation after live bootstrap refusal",
  "description": "SPIDR prerequisite leaf for parent task-2c952438c41546b3a736623f3956c778. Objective: prevent a production Control Room mutation affordance from appearing enabled when live bootstrap has already refused. Deliverable: LiveGoalsHome threads the exact existing LiveRefused code/detail into GoalsHome’s established createDisabledReason admission seam only for setup.ok === false, so no create form or dispatch becomes reachable while attached setup remains unchanged. Exact owned paths (2): apps/control-room/src/v2/goals/live-goals.tsx; apps/control-room/src/v2/goals/goal-create-disabled.test.tsx. NOT in scope: live-handshake/refusal production, GoalsHome, CordumApp, parent journeys, transport code, manifests, or any new retry/authority/layer. The parent’s unchanged disconnected-banner journey is the served-build consumer and parent dependsOn this row. Exact verification command: pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-create-disabled.test.tsx",
  "definitionOfDone": [
    "Red-first extend the focused disabled-create test to render the real LiveGoalsHome with exact {ok:false, code:LIVE_BOOTSTRAP_UNAVAILABLE, detail:daemon bootstrap unavailable}; assert cr.goals.new is disabled, its title/explanation carries that exact stable code and detail, clicking opens no form, and neither transport nor onOpenBoard/dispatch is called. The pre-fix production surface is red because the button remains enabled.",
    "LiveGoalsHome supplies GoalsHome.createDisabledReason only when setup.ok === false, deriving it solely from the existing LiveRefused code/detail; attached setup leaves the reason undefined and existing enabled create behavior intact. Do not invent a refusal layer because LiveRefused has none, do not change exported signatures, and keep live-goals.tsx below 250 physical lines.",
    "The divergence fixture reaches LiveGoalsHome directly with the exact bootstrap refusal, so the downstream fallback dispatcher is unreachable and cannot satisfy the disabled-state assertion. A tracked-path mutation removing or polarity-swapping only createDisabledReason propagation makes the exact enabled/disabled assertion red while the fallback dispatcher remains present; restore separately and prove tracked path bytes/diff match the pre-drill state.",
    "Run standalone/unpiped control-room typecheck, the exact focused verification command with nonzero Test Files/Tests counts, the full @moe/control-room test lane with nonzero counts, and root pnpm typecheck/pnpm test as merge-base-versus-HEAD path-attributed baselines; no new failure may intersect the two owned paths and foreign red is disclosed verbatim."
  ],
  "taskRails": [
    "Own exactly live-goals.tsx and goal-create-disabled.test.tsx. Do not edit live-handshake, GoalsHome, CordumApp, parent journeys, transport, manifests, or locks.",
    "Preserve exact LIVE_BOOTSTRAP_UNAVAILABLE refusal truth from the existing setup result. LiveRefused has no layer field; do not fabricate one, broaden retries, or convert refusal into setup authority.",
    "The test must exercise the production LiveGoalsHome surface, not reimplement the propagation helper or stop at GoalsHome’s already-proven local guard.",
    "Use red-first TDD, exact refusal-code assertions, nonzero test counts, and a restored tracked-path divergence drill; preserve all foreign WIP."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "MEASURE-ONLY. Record HEAD, `git status --porcelain --` and `wc -l` for the three owned paths — architect measured live-goals.tsx 131 lines, goal-create-disabled.test.tsx 34, cordum-app.test.tsx 1120, all tracked and clean. SCOPE AMENDMENT, authorized by this row's own reopen reason ('amend scope to include cordum-app.test.tsx') now that blocking row task-28cd95db is DONE and no longer owns that path: the owned set is THREE paths, live-goals.tsx + goal-create-disabled.test.tsx + cordum-app.test.tsx. THE GAP IS REAL AND IT IS A MISSING INNER GUARD, not a missing feature — confirm each of these yourself. live-goals.tsx DECLARES `createDisabledReason?: string | undefined` at :45, destructures it at :52 and forwards it to GoalsHome at :124, but NEVER DERIVES it from `setup.ok === false`; a direct render of LiveGoalsHome with a refused setup and no explicit prop leaves goal creation ENABLED. The admission seam it forwards into is already complete and proven: goals-home.tsx:184 `disabled={createDisabledReason !== undefined}`, :189 `title={createDisabledReason}`, :218-220 rendering `New goal unavailable ${EMDASH} ${createDisabledReason}`. The refusal shape is `LiveRefused { code, detail, ok: false }` at live-config.ts:49-53 — note it has NO layer field, and rail 2 forbids inventing one. WHY THIS IS INVISIBLE TODAY: cordum-app.tsx:250-255 computes a reason itself and passes it at :262, so at app level the button IS disabled and cordum-app.test.tsx:269 passes. The component is simply not self-protecting — it depends entirely on its caller remembering, and any other caller or a refactor of cordum-app silently re-opens a mutation affordance during a refused bootstrap. Record the baseline: `pnpm --filter @moe/control-room typecheck`, the row's exact focused command, the full `pnpm --filter @moe/control-room test` lane, and root `pnpm typecheck`/`pnpm test` (root vitest has NO apps/** in its include, so it is root-lane-only evidence and cannot evidence this row). Preserve all foreign dirt.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "note": "MEASURE-ONLY, no edits. Root pinned explicitly (D:/projexts/moe-next; my shell cwd was a DIFFERENT project, so every command carries `-C`/`--filter`). HEAD 8c21fd48a8c4cd3e47aca4eef6c520237e201e65, branch moe/work-2026-09-02.\n\nOWNED PATHS — all three match the architect exactly, all tracked and CLEAN (`git status --porcelain --` on the three returned empty):\n  live-goals.tsx 131 | goal-create-disabled.test.tsx 34 | cordum-app.test.tsx 1120.\n\nGAP CONFIRMED MYSELF, not taken on report. live-goals.tsx declares `createDisabledReason?: string | undefined` at :45, destructures at :52, forwards to GoalsHome at :124 — and nowhere derives it from setup. The only `setup.ok === false` consumers are notAttached() at :85 and the dispatch fallback at :90-92; neither touches the reason. So a direct render of LiveGoalsHome with a refused setup and no explicit prop leaves createDisabledReason undefined and goal creation ENABLED. Seam it forwards into is complete as described: goals-home.tsx:184 `disabled={createDisabledReason !== undefined}`, :189 `title={createDisabledReason}`, :218-220 `New goal unavailable ${EMDASH} ${createDisabledReason}`. LiveRefused is `{code, detail, ok:false}` at live-config.ts:49-53 — NO layer field, rail 2 holds. LIVE_BOOTSTRAP_UNAVAILABLE is a real member of LIVE_CONFIG_REFUSAL_CODES (live-config.ts:20-24), so the fixture code is type-legal, not a string I invented. Outer fence confirmed at cordum-app.tsx:250-255 + :262 with the generic \"Actions require an attached daemon session.\"\n\nONE STRUCTURAL FACT THE PLAN DID NOT STATE, and it bears on step 2's count: the `New goal unavailable ${EMDASH} ...` explanation at goals-home.tsx:218-220 renders ONLY inside the `data.goals.length === 0` branch. On a refused setup notAttached() returns `goals: []`, so it does render here — but the count I measure in step 2 is a property of that empty-state branch, not an unconditional one. Also note :189 puts the reason in a `title` ATTRIBUTE, which `getAllByText` does not see; I will measure the count by running it (step 2 says do not guess) rather than reasoning it out.\n\nBASELINES AT HEAD, each foreground and standalone:\n  `pnpm --filter @moe/control-room typecheck` -> GREEN (`$ tsc --project tsconfig.json`, no diagnostics).\n  `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-create-disabled.test.tsx` -> Test Files 1 passed (1), Tests 1 passed (1).\n  `pnpm --filter @moe/control-room test` -> Test Files 113 passed (113), Tests 1562 passed (1562).\n  `pnpm typecheck` (root) -> GREEN, every package Done.\n  `pnpm test` (root) -> RED AT BASELINE, PRE-EXISTING AND FOREIGN. Disclosed verbatim: Test Files 2 failed | 423 passed | 1 skipped (426); Tests 3 failed | 10254 passed | 44 skipped (10301).\n    FAIL packages/store/src/command-decision-replay-bytes.test.ts > command decision replay-request digest — both commit paths record it > exports the correlation identity helper used by provenance readers\n    FAIL tests/integration/release/release-version-surfaces.test.ts > release version surfaces > discovers the exact nonempty current-version occurrence set in both directions\n    FAIL tests/integration/release/release-version-surfaces.test.ts > release version surfaces > goes red for an unclassified tracked current-version occurrence\n  Neither file intersects the three owned paths, and root vitest has no apps/** include, so this row cannot have caused it and cannot fix it. This is the pre-fix RED I must compare against at step 5 — new failures are only those NOT in this set.\n\nHAZARD I INHERIT FROM THAT RED, worth stating because it constrains my own housekeeping: release-version-surfaces reds on unclassified version literals in TRACKED files and its diff names `.serena/memories/*.md` rows. `.serena/memories/` is tracked in this repo and is currently dirty (15 files). A Serena memory I write containing a bare `v0.1`/`0.1.0` literal would ADD to that foreign red. My handoff memory will avoid version literals.\n\nFOREIGN WIP PRESERVED, untouched: 116 dirty entries total, none of them mine. Beyond the .moe/ board and .codex/ rows: 15 .serena/memories/*.md, apps/control-room/src/v2/styles/cordum-goals.css, 4 apps/daemon/src/bootstrap/* files, docs/VISION.md, packages/runner/src/platform/windows/windows-broker-path.ts, tests/e2e/control-room/control-room-smoke.spec.ts, tests/security/scheduler-activation-{boundaries.security,hostile-cases}.ts (that last pair is live peer work on task-59eaddfbf — do not touch), plus untracked build .js leakage and docs/plans. I will not stage, revert or stash any of it.",
      "startedAt": "2026-09-02T20:09:39.165Z",
      "completedAt": "2026-09-02T20:15:15.162Z"
    },
    {
      "stepId": "step-2",
      "description": "SETTLE THE ASSERTING-CONSUMER FAN-OUT BEFORE WRITING ANY PRODUCTION CODE. The architect found one at plan time and it WILL break if you skip this. DoD item 1 requires the disabled control's title/explanation to carry the exact stable code, i.e. text containing `LIVE_BOOTSTRAP_UNAVAILABLE`. Today the reason reaching that control is cordum-app.tsx:255's GENERIC string `\"Actions require an attached daemon session.\"`, which contains no code. Meanwhile cordum-app.test.tsx:267 asserts a HARD-CODED COUNT: `expect(screen.getAllByText(/LIVE_BOOTSTRAP_UNAVAILABLE/)).toHaveLength(2)`, where those two occurrences come from the refusal notice and the NOT ATTACHED surface — NOT from the create button. Once LiveGoalsHome derives a code-carrying reason, goals-home.tsx renders it in BOTH the button title and the `New goal unavailable ${EMDASH} ...` text, so the document gains further occurrences and that `toHaveLength(2)` breaks. MEASURE the new count by running it, do not guess it, and update the literal to the measured value with a one-line comment naming what the added occurrences are. Grep the SHAPE, not just the symbol, across the repo before you finish: other hard-coded counts over this regex, other `getAllByText(/LIVE_BOOTSTRAP_UNAVAILABLE/)` sites, and any assertion pinning the create button's exact title string — the architect measured cordum-app.test.tsx:267/:383 and the exact-title lines at :405/:442/:448 as the candidates, plus the disabled assertions at :269 and :514-520. If a consumer outside the three owned paths asserts on this shape, STOP and report rather than taking the extra path.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "note": "FAN-OUT SETTLED. NO EDIT IN THIS STEP, DELIBERATELY: step 2 forbids guessing the count and step 4 says \"re-run cordum-app.test.tsx and apply the measured count update from step 2\", so the literal cannot be written until the derivation exists. This step enumerates the consumers and fixes which ones are count-sensitive; the measured number lands in step 4. modifiedFiles is empty and that is not an omission.\n\nTHE FULL CONSUMER SET, from `git grep -n LIVE_BOOTSTRAP_UNAVAILABLE` repo-wide (21 hits, .moe/ excluded), classified by whether an added occurrence can break it:\n\nCOUNT-SENSITIVE — WILL BREAK, both inside the owned set, both `toHaveLength(2)`:\n  cordum-app.test.tsx:267 `expect(screen.getAllByText(/LIVE_BOOTSTRAP_UNAVAILABLE/)).toHaveLength(2)` (the architect's).\n  cordum-app.test.tsx:383 `expect(await screen.findAllByText(/LIVE_BOOTSTRAP_UNAVAILABLE/u)).toHaveLength(2)` — the StrictMode bounded-recovery arm. SAME hard-coded 2, different query and a `u` flag. Both are refused-setup renders through CordumApp, so both see whatever the create control gains; I will update whichever the measurement actually moves and leave the other alone.\n\nCOUNT-INSENSITIVE — cannot break, verified by reading each, not by the symbol:\n  cordum-app.test.tsx:405/:442/:448 — exact string \"LIVE_BOOTSTRAP_UNAVAILABLE: daemon bootstrap unavailable\" asserted with `.length).toBeGreaterThan(0)`. Adding occurrences only makes these MORE true. Worth flagging: that exact string is `${code}: ${detail}`, the same shape live-goals.tsx:38 already uses for comingOnlineNote, so if I format the derived reason that way these three gain matching elements rather than losing them.\n  live-handshake.test.ts (9 hits) and cordum-handshake.tsx:58 — refusal PRODUCTION/producer assertions on the `{code, detail, ok}` value. No render, no document, untouched by a presentation change.\n  live-config.ts:23 — the code's declaration in LIVE_CONFIG_REFUSAL_CODES.\n\nOUTSIDE THE OWNED SET, AND IT IS SAFE — this is the one the step told me to stop on if it were count-sensitive, so I read it rather than assuming:\n  tests/e2e/control-room/journeys.spec.ts:60 `await expect(driven.getByText(/LIVE_BOOTSTRAP_UNAVAILABLE/iu).first()).toBeVisible()`. `.first()` + toBeVisible, no cardinality — Playwright strict mode is defused by `.first()`, so extra matches cannot fail it. Count-insensitive, no scope amendment needed, NOT a stop condition.\n\nA CONSUMER THE PLAN DID NOT NAME, found by grepping the SHAPE (`cr.goals.new`) instead of only the code — this is the one that could have bitten:\n  apps/control-room/src/v2/goals/live-goals.test.tsx:160-161, inside `openForm()`: `waitFor(() => expect(screen.getByTestId(\"cr.goals.new\")).not.toHaveProperty(\"disabled\", true))` then clicks it. A direct LiveGoalsHome consumer, outside my owned paths, asserting the button is ENABLED — exactly what my derivation disables. IT IS SAFE, and I verified it rather than reasoning it: all 15 `render(<LiveGoalsHome …>)` calls in that file pass `attachedSetup(state)` (lines 255-589), and `grep \"ok: false\"` in it returns only line 156's `refusalAnswer()`, which is a refused COMMAND RESPONSE, not a refused SETUP. The file never renders a refused setup, so `setup.ok === false` is never taken there and the enabled assertion is untouched. This is precisely why step 4's derivation must be conditional on `setup.ok === false` and must not touch the attached path — if it were unconditional, this file reds and it is not mine to fix.\n  Also `goals-home.test.tsx:91/115/188/203` click the button, but they render GoalsHome directly with no reason — the seam's own contract, unaffected.\n  `tests/e2e/control-room/{gate1-v1-approval,prd-persistence-boundary}.spec.ts` click it against a served build with a real attached daemon; attached path unchanged, and these are Playwright, not in any vitest lane.\n\nREGRESSION GUARD I MUST NOT MOVE: cordum-app.test.tsx:514-520 alternates `disabled` false/true/false across CONNECTED/DISCONNECTED on an ATTACHED setup — the caller's descriptor.banner path (cordum-app.tsx:250-254). It proves the attached prop still flows through untouched, so it stays exactly as written and green.\n\n`New goal unavailable` has exactly ONE production site (goals-home.tsx:220) and no test pins its text today.",
      "startedAt": "2026-09-02T20:15:17.280Z",
      "completedAt": "2026-09-02T20:16:46.213Z"
    },
    {
      "stepId": "step-3",
      "description": "RED-FIRST at the production surface, in goal-create-disabled.test.tsx. That file is currently 34 lines and renders `GoalsHome` DIRECTLY with a hand-supplied reason (:19) — which rail 3 explicitly calls out as stopping at the already-proven local guard. Add an arm that renders the real `LiveGoalsHome` with an exact refused setup `{ok: false, code: \"LIVE_BOOTSTRAP_UNAVAILABLE\", detail: \"daemon bootstrap unavailable\"}` and NO explicit createDisabledReason prop, then assert from the rendered production output: `cr.goals.new` is natively disabled; its title and the `New goal unavailable` explanation both carry the literal `LIVE_BOOTSTRAP_UNAVAILABLE`; the detail text appears; clicking it opens no `cr.goals.newgoal.form` and calls no create handler. THIS FIXTURE IS THE DIVERGENCE (DoD item 3): it reaches LiveGoalsHome directly, so cordum-app's own computed reason is not in the picture and cannot satisfy the assertion on the component's behalf. Also add the negative arm the DoD requires — an ATTACHED setup leaves the reason undefined and existing enabled create behaviour intact — so the derivation is proven conditional rather than unconditional. Keep the existing GoalsHome arm exactly as it is; it still has value as the seam's own contract. Run the focused file RED and quote which assertion failed and why before touching production.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx"
      ],
      "note": "RED-FIRST ACHIEVED AT THE PRODUCTION SURFACE. No production byte touched — `git status --porcelain` shows only goal-create-disabled.test.tsx (34 -> 126 lines); live-goals.tsx is still clean.\n\nTHE RED, QUOTED, AND IT IS THE RIGHT ONE:\n```\n FAIL  src/v2/goals/goal-create-disabled.test.tsx > refuses goal creation on its own when live bootstrap refused, naming the code\nAssertionError: expected false to be true // Object.is equality\n ❯ src/v2/goals/goal-create-disabled.test.tsx:86:27\n     86|   expect(button.disabled).toBe(true);\n Test Files  1 failed (1)\n      Tests  1 failed | 2 passed (3)\n```\n`button.disabled` is FALSE. A refused live bootstrap reaches LiveGoalsHome, the component renders NOT ATTACHED with the code in its coming-online note, AND STILL OFFERS AN ENABLED \"New goal\" BUTTON. That is the defect stated as an executable fact, not a description of one. The later title/explanation assertions were never reached — the very first one fired — which is what I want from a red-first: the failure is the missing guard, not a fixture defect further down.\n\nWHY THIS RED IS THE DIVERGENCE (DoD 3). The arm renders `<LiveGoalsHome onOpenBoard={…} setup={REFUSED} />` with NO createDisabledReason prop. cordum-app.tsx:250-255 computes its own generic reason and passes it at :262, which is the only thing disabling this control today — and it is not in this render, so it cannot answer on the component's behalf. The downstream fallback dispatcher at live-goals.tsx:90-92 is likewise unreachable as an explanation: it only turns an INVOKED create into a refusal report, and no invocation happens when the assertion is about the button's own disabled state.\n\nTWO GREEN ARMS IN THE SAME FILE, both load-bearing, neither incidental:\n- The original GoalsHome arm is preserved BYTE-FOR-BYTE (the seam's own contract, `createDisabledReason` -> disabled + title + no form). Its passing is what proves the seam is already complete and that the missing piece is the derivation feeding it — so the red above is a live-goals.tsx defect, not a goals-home.tsx one.\n- The new ATTACHED negative arm passes NOW and must still pass after the fix. This is the guard against an unconditional derivation, and it is not hypothetical: live-goals.test.tsx:160 (`openForm()`) asserts `cr.goals.new` is not disabled and clicks it, across all 15 of that file's renders — and that file is OUTSIDE my owned paths. An unconditional derivation reds a file I may not edit. The negative arm catches that here, in a file I do own.\n\nThat 2-passed/1-failed split also rules out the cheap failure mode where a red is really a broken harness: the same render/query/click machinery works in the two arms either side of it.\n\nFIXTURE HONESTY. `REFUSED` is annotated `LiveRefused`, so \"LIVE_BOOTSTRAP_UNAVAILABLE\" is checked against LIVE_CONFIG_REFUSAL_CODES by tsc — a drifted or misspelled code fails the typecheck instead of asserting prose the app can never produce. Detail is verbatim from live-handshake.ts:144.\n\nTWO ASSERTIONS I WROTE DELIBERATELY, both aimed at vacuity:\n1. The explanation is found by `getByText(/^New goal unavailable/u)`, NOT by reading the `cr.goals.empty` container. Both paragraphs live in that container and the sibling `comingOnlineNote` already contains the code today — a container-level `toContain` would pass with the guard still missing. Testing Library matches on an element's own text node, so this pins the body paragraph specifically.\n2. `expect(explanation.textContent?.endsWith(button.title)).toBe(true)` ties the two surfaces to ONE derivation without respelling the reason in the test (rail 3: no reimplementing the propagation). If a future change gave the title and the explanation different strings, this reds even though both would still contain the code.\n\nRefusal-path completeness per DoD 1: the click asserts no `cr.goals.newgoal.form`, `onOpenBoard` not called, and a stubbed global `fetch` not called at all — the transport leg. On a refused setup neither feed starts, so any fetch would itself be a defect.",
      "startedAt": "2026-09-02T20:16:50.638Z",
      "completedAt": "2026-09-02T20:18:29.192Z"
    },
    {
      "stepId": "step-4",
      "description": "IMPLEMENT THE INNER GUARD, minimum change, in live-goals.tsx only. When `setup.ok === false`, derive the GoalsHome `createDisabledReason` solely from the existing `LiveRefused` code and detail already on that value; when setup is attached, leave it undefined so enabled create behaviour is byte-for-byte unchanged. State the precedence explicitly in the code: on refusal the derived, code-carrying reason SUPERSEDES any caller-supplied prop, because the caller's generic string (cordum-app.tsx:255) does not name the code the DoD requires; on attached setup the caller's prop still flows through untouched, which is what preserves cordum-app's connection-descriptor banner behaviour at :250-254. Do NOT invent a refusal layer — LiveRefused has none (live-config.ts:49-53) and rail 2 forbids fabricating one. Do NOT edit live-handshake, GoalsHome, cordum-app.tsx, transport, manifests or locks; do NOT broaden retries or convert a refusal into setup authority; do NOT add a second refusal vocabulary. Keep live-goals.tsx well under the caps — it is 131 lines and this is a handful. Turn step 3's arms green, then re-run cordum-app.test.tsx and apply the measured count update from step 2.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "note": "INNER GUARD IMPLEMENTED. live-goals.tsx 131 -> 150 lines (cap 250, comfortable). Three edits, all in that one production file:\n\n1. Extracted `refusalSentence(setup: LiveRefused): string` returning `${setup.code}: ${setup.detail}` — the exact expression `notAttached()` already inlined at :38, now called from there instead. Byte-identical output for the coming-online note; no behaviour moved.\n2. `const createDisabled = setup.ok ? createDisabledReason : refusalSentence(setup);` beside the existing `data` derivation, with the precedence stated in the code as the step required.\n3. `<GoalsHome createDisabledReason={createDisabled} …>` — the forwarding site, one identifier changed.\n\nWHY THE HELPER AND NOT AN INLINE TEMPLATE, since this is the only judgement call I made beyond the literal instruction. The refused surface now says the same sentence in two places (the NOT ATTACHED note and the disabled control). Inlining the template a second time would be a second spelling of one refusal, free to drift under any later edit — the \"second refusal vocabulary\" rail 2 forbids, arrived at by duplication instead of by invention. One function means the two surfaces are the same string by construction, which is exactly what the test's `explanation.textContent.endsWith(button.title)` assertion pins. No layer field, no new code, no retry, no setup authority: `LiveRefused` is read, never widened.\n\nPRECEDENCE, and the reason it must go this way round: on `setup.ok === false` the derived sentence SUPERSEDES the caller's prop. cordum-app.tsx:255 passes \"Actions require an attached daemon session.\", which names no code — honouring the prop on the refused branch would satisfy `disabled` while failing DoD 1's requirement that the control carry the stable code, and the app-level count would never have moved. On the attached branch the prop flows through untouched, which is what keeps cordum-app.tsx:250-254's connection-descriptor banner and its CONNECTED/DISCONNECTED alternation at cordum-app.test.tsx:514-520 exactly as they were.\n\nSTEP 3'S ARMS ARE GREEN: focused file Test Files 1 passed (1), Tests 3 passed (3) — the divergence arm, the preserved GoalsHome seam arm, and the attached negative arm.\n\nTHE COUNT, MEASURED AND NOT GUESSED. Ran cordum-app.test.tsx against the new production surface first:\n```\nAssertionError: expected [ <p></p>, <p …(1)></p>, <p …(1)></p> ] to have a length of 2 but got 3\n ❯ src/v2/cordum-app.test.tsx:267:63\nAssertionError: expected [ <p></p>, <p …(1)></p>, <p …(1)></p> ] to have a length of 2 but got 3\n ❯ src/v2/cordum-app.test.tsx:383:71\n```\n2 -> 3 at BOTH sites, and the failure output settles the mechanism rather than leaving me to argue it: all three matches are `<p>` elements. The button's `title` is an ATTRIBUTE and `getAllByText` does not see it, so the single added occurrence is the empty-state explanation paragraph `New goal unavailable ${EMDASH} LIVE_BOOTSTRAP_UNAVAILABLE: daemon bootstrap unavailable`. I updated both literals to 3 with a comment naming the three paragraphs and noting explicitly that the title attribute is not among them — a future reader adding a fourth surface needs to know which surfaces the number counts.\n\nBoth sites moved together, which is itself the evidence that step 2's classification was right: exactly the two count-sensitive consumers moved, and none of the count-insensitive ones (:405/:442/:448 `toBeGreaterThan(0)`) or the disabled-state guards (:269, :514-520) went red.\n\nVERIFICATION AT THIS STEP, each foreground and standalone:\n  `pnpm --filter @moe/control-room typecheck` -> GREEN (`$ tsc --project tsconfig.json`, no diagnostics).\n  `pnpm --filter @moe/control-room exec vitest run src/v2/cordum-app.test.tsx` -> Test Files 1 passed (1), Tests 34 passed (34).\n  `pnpm --filter @moe/control-room test` -> Test Files 113 passed (113), Tests 1564 passed (1564).\nThat 1564 against the step-1 baseline of 1562 is exactly +2, the two arms I added, with the file count unchanged at 113 — so no test was silently dropped, renamed away or skipped to reach green.\n\nUNTOUCHED, as required: live-handshake, goals-home.tsx, cordum-app.tsx, transport, manifests, locks. `git status --porcelain` still shows exactly the three owned paths as mine; all 116 pre-existing foreign entries are preserved.",
      "startedAt": "2026-09-02T20:18:31.481Z",
      "completedAt": "2026-09-02T20:20:29.825Z"
    },
    {
      "stepId": "step-5",
      "description": "DIVERGENCE DRILL then FINAL VERIFICATION. All three paths are tracked, so `git diff` is an admissible restore proof; still back up each out-of-tree under %TEMP% suffixed by your worker id with `Get-FileHash -Algorithm SHA256` recorded BEFORE mutating, and restore in a call SEPARATE from the mutation. THE DRILL: in live-goals.tsx remove, and separately polarity-swap, ONLY the new `setup.ok === false` derivation — leave the prop plumbing, the GoalsHome forwarding at :124 and every other behaviour intact. REQUIRED OUTCOME, and read it carefully because it is a split result by design: the new DIRECT-RENDER assertions in goal-create-disabled.test.tsx must go RED, while cordum-app.test.tsx's DISABLED assertions at :269 and :514-520 must stay GREEN — because cordum-app.tsx:250-255 still supplies its own generic reason and the button stays disabled through that outer fence. That green-stays-green IS the divergence proof: it demonstrates the inner guard is independently load-bearing and that the outer fence would otherwise have masked its absence. The updated count assertion from step 2 may also red under the mutation; that is expected and is additional signal, not a failure of the drill. CRITICAL: do NOT 'simplify' by deleting cordum-app.tsx's computation to make the mutant cleaner — removing the redundant outer call would make the mutant green and read as 'no guard needed', which is the exact anti-pattern epic rail 7A names. A GREEN direct-render mutant is a FINDING to report via moe.report_blocked, never a pass. Restore all three paths and prove it with empty `git diff` plus SHA256 equality. THEN verify, each leg foreground, standalone and unpiped, reading the OUTPUT not the exit code: `git diff --check --` the three paths; `pnpm --filter @moe/control-room typecheck`; the row's exact command `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-create-disabled.test.tsx` with nonzero Test Files/Tests counts, which is complete_task.verification; then the full `pnpm --filter @moe/control-room test` lane with nonzero counts, which is what proves the cordum-app consumer still passes. Finally root `pnpm typecheck` and `pnpm test` as merge-base-versus-HEAD path-attributed baselines; no new failure may intersect the three owned paths, and foreign red is disclosed verbatim.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "note": "DRILL DONE, RESTORED AND PROVEN, FINAL VERIFICATION GREEN. Final sizes: live-goals.tsx 150 (cap 250), goal-create-disabled.test.tsx 128, cordum-app.test.tsx 1127.\n\nPRE-DRILL BACKUPS under %TEMP%, suffixed worker-9801b4a8, SHA256 recorded BEFORE any mutation:\n  live-goals.tsx                A28A0747162D19C2BF0D9241A01ADE813D4A873E89C23500BFA53460735CB180\n  goal-create-disabled.test.tsx F75ACC3F57149B676A1673C39613D9AE457307B5C22DFCDCD84F58E92BFF76C5\n  cordum-app.test.tsx           F397AB54DFDF522D1E5ADBBC9A263CB597D77D342DC223B655DE07A8146259C8\nPlus the pre-drill working-tree diff of the three paths saved whole (9079 bytes).\n\nDRILL A - DERIVATION REMOVED (`const createDisabled = createDisabledReason;`, plumbing and the :143 forwarding left intact):\n  goal-create-disabled.test.tsx:86 RED - `expected false to be true`, the direct-render `button.disabled`.\n  cordum-app.test.tsx :271/:390 RED on the counts, 3 -> 2.\n\nI DID NOT GET THE STAY-GREEN HALF OF THE SPLIT ON THE FIRST PASS, AND SAYING SO IS THE POINT.\nThe refused-path disabled assertion is at cordum-app.test.tsx:273, BELOW the count assertion at :271, inside the SAME `it()`. `expect` throws, so the count red aborted the test before :273 ever evaluated. The drill's required observation - \"cordum-app's disabled assertions stay GREEN\" - was not made; it was MASKED. Reporting pass 1 as the divergence proof would have been reporting an assertion that never ran.\n\nDRILL A PASS 2, DISCLOSED, TO UNMASK IT. I re-ran the same production mutant with the two count literals set to 2 - the value that is CORRECT UNDER THE MUTANT, i.e. their pre-task value. Result: `Test Files 1 passed (1) | Tests 34 passed (34)`. With the inner guard GONE, every cordum-app assertion passes, including :273 (refused path, button disabled) and :514-520 (attached CONNECTED/DISCONNECTED alternation). THAT is the divergence proof, now actually observed: cordum-app.tsx:250-255's generic reason holds the button disabled entirely on its own, so the outer fence masks the missing inner guard completely at app level, and only the direct render can see it.\nThis is not the forbidden move. Step 5 forbids deleting cordum-app.tsx's PRODUCTION computation to make the mutant green - that hides the outer fence so the guard looks necessary. I changed a test expectation only, in the direction that EXPOSES more assertions, and every production byte of the outer fence stayed in place. Both count literals were restored with everything else.\n\nDRILL B - POLARITY SWAPPED (derive on the attached branch, prop passthrough on refused). Ran the two owned suites AND live-goals.test.tsx, deliberately, because step 2 identified it as the at-risk out-of-scope consumer:\n  goal-create-disabled.test.tsx:86 RED (direct-render arm: `expected false to be true`).\n  goal-create-disabled.test.tsx:122 RED (attached negative arm: `expected true to be false`).\n  live-goals.test.tsx: 15 of 15 RED, every one at :160 `expected <button> to not have property \"disabled\" with value true`.\n  cordum-app.test.tsx:521 RED (attached CONNECTED expects enabled, got disabled).\n  Totals: Tests 18 failed | 34 passed (52).\nThe 15-file blast radius is the concrete justification for the negative arm I added in step 3: an inverted or unconditional derivation reds a file THIS ROW DOES NOT OWN, and the negative arm catches it inside a file I do own, before it ever gets there. Both mutants red the direct-render assertion, so the guard is independently load-bearing under removal and under inversion.\n\nRESTORE, in a call SEPARATE from every mutation, proven THREE ways:\n  SHA256 per file re-measured after restore - MATCH=True on all three, against the hashes above.\n  Working-tree diff of the three paths re-taken and hashed - byte-identical to the pre-drill diff.\n  `git grep -c DRILL -- live-goals.tsx` returns nothing: no drill scaffolding survived.\n\nFINAL VERIFICATION, each leg foreground and standalone, reading the output:\n  `git diff --check --` the three paths -> no output, exit 0 (no whitespace defects).\n  `pnpm --filter @moe/control-room typecheck` -> `$ tsc --project tsconfig.json`, no diagnostics.\n  `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-create-disabled.test.tsx` -> Test Files 1 passed (1), Tests 3 passed (3). Nonzero both. This is complete_task.verification.\n  `pnpm --filter @moe/control-room test` -> Test Files 113 passed (113), Tests 1564 passed (1564). Nonzero both; vs the 113/1562 baseline that is +2 tests and +0 files, exactly the two arms added and nothing skipped or dropped to reach green. This is what proves the cordum-app consumer still passes.\n  `pnpm typecheck` (root) -> every package Done.\n  `pnpm test` (root) -> Test Files 2 failed | 423 passed | 1 skipped (426); Tests 3 failed | 10258 passed | 44 skipped (10305).\n\nROOT RED IS THE BASELINE SET, UNCHANGED, NAME FOR NAME - same two files, same three test names as step 1:\n  FAIL packages/store/src/command-decision-replay-bytes.test.ts > … > exports the correlation ide",
      "startedAt": "2026-09-02T20:20:31.976Z",
      "completedAt": "2026-09-02T20:27:41.751Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Still required; blocked rather than backlogged per human cleanup policy: because its exact rendered-shape consumer is owned by 28cd, which exhausted reopen budget. Unblock after 28cd completion lands and amend scope to include cordum-app.test.tsx.",
  "createdBy": "WORKER",
  "parentTaskId": "task-2c952438c41546b3a736623f3956c778",
  "priority": "HIGH",
  "order": 192,
  "comments": [
    {
      "id": "comment-8dbd98399ac1453db2172c1c7e1c267e",
      "author": "architect-5e59a71e",
      "content": "Planning block for task-75bb88d241894a43b6c6981f355bc220 after mandatory shape grep. Straight exact propagation of `LIVE_BOOTSTRAP_UNAVAILABLE: daemon bootstrap unavailable` into GoalsHome.createDisabledReason adds a third rendered text occurrence: existing CordumApp already renders LiveRefusalNotice + LiveGoalsHome.notAttached().comingOnlineNote, and GoalsHome adds `New goal unavailable — <reason>`. `apps/control-room/src/v2/cordum-app.test.tsx` lines 249 and 362 pin exactly 2 occurrences, so the required full @moe/control-room lane would red. That asserting consumer is outside this row's exact two paths, is dirty foreign WIP, and is owned by blocked task-28cd95db8bb6444b861eac4384c7fdc2. Silently neutralizing/removing comingOnlineNote only to keep count=2 would violate the shape-consumer rail and broaden behavior. Required ruling: either (A) after task-28cd lands, amend this row to depend on it and own cordum-app.test.tsx so the exact-count assertion can become semantic (recommended), or (B) explicitly authorize de-duplication of the refusal presentation and amend DoD/rail to say which occurrence is canonical. Do not submit the current two-file plan or overwrite foreign WIP.",
      "timestamp": "2026-08-31T16:21:37.804Z"
    },
    {
      "id": "comment-c59110f6d30945c5a603ec195cb91bd5",
      "author": "qa-368df350",
      "content": "QA VERIFICATION RECORD — qa-368df350. Root D:/projexts/moe-next, daemon 9876, verified against $env:MOE_PROJECT_PATH before measuring. HEAD moved 083c08aa -> 5f76aaa9 (peer task-59eaddfb) during review; all gates below re-run at 5f76aaa9.\n\nCOMMIT — LANDED BY QA. Row reached REVIEW 20:28:10 with task.commits EMPTY, lastCommitOutcome null, and all three declared paths DIRTY; no wrapper commit over ~10 minutes. Landed b931be68d5aeec on moe/work-2026-09-02; `git show --name-only` carries EXACTLY the three declared paths and nothing foreign. record_commit duplicate=false, addedPaths=[]. Diff 125+/5- = net +120. live-goals.tsx 150 physical lines, under the 250 cap.\n\nDoD 1 — assertions present and exact. The divergence arm renders LiveGoalsHome directly with `{ok:false, code:\"LIVE_BOOTSTRAP_UNAVAILABLE\", detail:\"daemon bootstrap unavailable\"}` and NO createDisabledReason prop, then asserts `button.disabled === true`, code AND detail in BOTH `button.title` and the explanation text, `queryByTestId(\"cr.goals.newgoal.form\")` null after the click, and `onOpenBoard` / stubbed `fetch` both never called. A negative arm proves the derivation is conditional: attached setup -> `button.disabled === false`, `button.title === \"\"`, no \"New goal unavailable\" text, form opens.\n\nDoD 2 — derivation is `setup.ok ? createDisabledReason : refusalSentence(setup)` in live-goals.tsx only, sourced solely from the existing LiveRefused code/detail. No refusal layer invented (LiveRefused is `{code, detail, ok}`). No exported signature changed. `refusalSentence` is also reused by `notAttached()`, which had the same expression inlined — one vocabulary, so the coming-online note and the disabled control cannot drift.\n\nDoD 3 MUTATION DRILL — run by me, sha256 backup/restore. Reverting only the propagation (`createDisabledReason={createDisabled}` -> `={createDisabledReason}`), leaving the dispatcher untouched (`createGoalDispatcher` still imported at :11 and used at :108):\n  RED on the named arm — \"refuses goal creation on its own when live bootstrap refused, naming the code\", AssertionError: expected false to be true.\nRestore proof: `sha256sum -c` OK and `git diff --numstat` back to the exact pre-drill 21/2.\n\nEXTRA MEASUREMENT THE DoD DID NOT ASK FOR, because a count change from 2 to 3 in a peer-facing test is the shape that hides an accommodation. Under the same drill I ran cordum-app.test.tsx: BOTH updated counts RED — `expected [ <p></p>, <p …(1)></p> ] to have a length of 3 but got 2`, on \"shows CONNECTING, calls /bootstrap, then renders the refused surface when no daemon answers\" and on \"redeems the prepared initial attempt once under StrictMode\". So the 2 -> 3 edit is a real join to the new derived explanation, not a number moved to make a test pass.\n\nDoD 4 GATES, standalone and unpiped at 5f76aaa9:\n  exact focused command `pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-create-disabled.test.tsx` — Test Files 1 passed (1), Tests 3 passed (3), exit 0, reproducing the recorded outputTail.\n  `pnpm --filter @moe/control-room typecheck` — `$ tsc --project tsconfig.json` echo present, exit 0.\n  full @moe/control-room lane — 113 files / 1564 tests passed, exit 0 (1562 before this row; +2 is exactly the two added arms).\n  root `pnpm typecheck` — exit 0, no `error TS`.\n  root vitest — 3 failed / 10259 passed / 44 skipped over 426 files. FOREIGN, and the set is byte-identical to the baseline I took at 083c08aa half an hour ago, so this row moved nothing:\n    packages/store/src/command-decision-replay-bytes.test.ts > \"exports the correlation identity helper used by provenance readers\" — expected 'undefined' to be 'function' (beside the standing untracked packages/store/src/index.js bridge).\n    tests/integration/release/release-version-surfaces.test.ts > \"discovers the exact nonempty current-version occurrence set in both directions\".\n    tests/integration/release/release-version-surfaces.test.ts > \"goes red for an unclassified tracked current-version occurrence\".\n  ATTRIBUTION: neither failing file is an owned path, and the root vitest include is [\"adapters/**\",\"packages/**\",\"tests/**\",\"tools/**\"] with NO apps/**, so that lane cannot even see the three files this row touched. No new failure intersects the owned paths.",
      "timestamp": "2026-09-02T20:36:23.843Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-31T16:07:25.791Z",
  "updatedAt": "2026-09-02T20:36:45.174Z",
  "metrics": {
    "firstClaimAt": "2026-08-31T16:14:36.040Z",
    "plannedStepCount": 5,
    "plannedDistinctFileCount": 3,
    "executedStepCount": 5,
    "doneAt": "2026-09-02T20:36:45.174Z",
    "wallClockMs": 188529134
  },
  "contextFetchedBy": [
    "architect-5e59a71e",
    "governor-c11c29e2",
    "worker-9801b4a8",
    "qa-368df350"
  ],
  "dependsOn": [
    "task-28cd95db8bb6444b861eac4384c7fdc2"
  ],
  "blockedReason": null,
  "blockedOnTaskIds": null,
  "blockedResourceId": null,
  "blockedFromStatus": null,
  "blockedAt": null,
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-02T20:08:27.996Z",
  "planningNotes": {
    "approachesConsidered": "Selected the smallest change that makes LiveGoalsHome self-protecting: derive createDisabledReason from the LiveRefused already in hand when setup.ok === false, and leave attached behaviour untouched. Rejected adding a layer field or a new refusal code — LiveRefused has only {code, detail, ok} and rail 2 forbids fabricating a layer. Rejected fixing this in cordum-app.tsx: it already computes a reason there, the row does not own that file, and moving the guard outward would leave the component just as dependent on its caller. Rejected leaving the caller's generic string in force on refusal — DoD item 1 requires the exact stable code in the title/explanation and cordum-app.tsx:255 supplies only 'Actions require an attached daemon session.', so on refusal the derived reason must supersede the prop. Rejected testing through CordumApp: cordum-app.tsx's own computed reason would satisfy the disabled assertion on the component's behalf and prove nothing, which is exactly what DoD item 3's divergence requirement guards against.",
    "codebaseInsights": "Measured at HEAD, all three owned paths tracked and clean: live-goals.tsx 131 lines, goal-create-disabled.test.tsx 34, cordum-app.test.tsx 1120. THE GAP IS A MISSING INNER GUARD, NOT A MISSING FEATURE. live-goals.tsx declares `createDisabledReason?: string | undefined` at :45, destructures at :52 and forwards to GoalsHome at :124, but never derives it from setup.ok === false — so a direct render of LiveGoalsHome with a refused setup and no explicit prop leaves goal creation ENABLED. The seam it forwards into is already complete: goals-home.tsx:184 disabled, :189 title, :218-220 the `New goal unavailable ${EMDASH} ${reason}` text. LiveRefused is `{code, detail, ok:false}` at live-config.ts:49-53, with NO layer field. WHY THE DEFECT IS INVISIBLE TODAY — THE CRUX OF THIS ROW: cordum-app.tsx:250-255 computes its own createDisabledReason and passes it at :262, so at app level the button is disabled and cordum-app.test.tsx:269 passes. This is the redundant-guard-hidden-by-an-identical-downstream-fence shape: the outer fence masks the missing inner one, and the component depends entirely on its caller remembering. THE ASSERTING-CONSUMER FAN-OUT, found at plan time: cordum-app.tsx:255 supplies the GENERIC string 'Actions require an attached daemon session.', which carries no code, while cordum-app.test.tsx:267 asserts the hard-coded count `getAllByText(/LIVE_BOOTSTRAP_UNAVAILABLE/)).toHaveLength(2)` — those two occurrences come from the refusal notice and the NOT ATTACHED surface, not from the create button. Once LiveGoalsHome derives a code-carrying reason, goals-home renders it in BOTH the title and the explanation text, so that count breaks. Candidate consumers measured in cordum-app.test.tsx: counts at :267 and :383, exact-title strings at :405/:442/:448, disabled assertions at :269 and :514-520. SCOPE: this row's own reopen reason authorizes amending scope to include cordum-app.test.tsx once blocking row task-28cd95db lands; that row is now DONE and no longer owns the path, so the owned set is three.",
    "risks": "THE DRILL RESULT IS SPLIT BY DESIGN AND WILL LOOK WRONG IF UNEXPLAINED. Mutating away the new derivation must red the DIRECT-RENDER assertions while cordum-app.test.tsx's disabled assertions at :269 and :514-520 stay GREEN, because cordum-app.tsx:250-255 still supplies its own generic reason through the outer fence. That green-stays-green is the divergence proof, not a drill failure. The dangerous temptation is to 'clean up' by deleting cordum-app.tsx's computation so the mutant reds everywhere — that would make the mutant green in the sense epic rail 7A warns about, read as 'no guard needed', and it edits a file this row does not own. Do not do it. Second risk: the hard-coded count at cordum-app.test.tsx:267 WILL break; measure the new value by running it rather than guessing, and grep the shape (other hard-coded counts, other getAllByText over this regex, exact-title assertions) rather than only the symbol — if an asserting consumer exists outside the three owned paths, stop and report rather than taking the extra path. Third: precedence must be explicit. On refusal the derived reason supersedes any caller prop, which changes the app-level button title from the generic sentence to a code-carrying one; on attached setup the caller's prop must still flow through untouched or cordum-app's connection-descriptor banner behaviour at :250-254 regresses. Fourth: do not invent a layer, broaden retries, or convert refusal into setup authority; preserve the existing GoalsHome arm in the focused test, which still documents the seam's own contract. Preserve all foreign WIP in this shared checkout.",
    "keyFiles": [
      "apps/control-room/src/v2/goals/live-goals.tsx",
      "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx",
      "apps/control-room/src/v2/cordum-app.test.tsx",
      "apps/control-room/src/v2/goals/goals-home.tsx",
      "apps/control-room/src/v2/cordum-app.tsx",
      "apps/control-room/src/live/live-config.ts"
    ]
  },
  "planApprovedAt": "2026-09-02T20:08:30.101Z",
  "workStartedAt": "2026-09-02T20:09:39.165Z",
  "reviewStartedAt": "2026-09-02T20:28:10.646Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-create-disabled.test.tsx",
    "exitCode": 0,
    "outputTail": "RUN  v4.1.10 D:/projexts/moe-next/apps/control-room\n\n Test Files  1 passed (1)\n      Tests  3 passed (3)\n   Start at  23:27:45\n   Duration  1.13s (transform 227ms, setup 0ms, import 409ms, tests 242ms, environment 384ms)",
    "reportedAt": "2026-09-02T20:28:10.646Z"
  },
  "filesModified": [
    "apps/control-room/src/v2/goals/live-goals.tsx",
    "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx",
    "apps/control-room/src/v2/cordum-app.test.tsx"
  ],
  "completionSummary": "LiveGoalsHome now closes goal creation on its own authority when live bootstrap has refused, instead of depending on its caller to remember.\n\nTHE DEFECT. live-goals.tsx declared `createDisabledReason`, forwarded it to GoalsHome, and never derived it from `setup`. A direct render with a refused setup left the create button ENABLED. It looked fine only because cordum-app.tsx:250-255 computes a reason itself and passes it down — a redundant OUTER fence masking a missing INNER guard, so any other caller or a shell refactor silently reopens a mutation affordance during a refused bootstrap.\n\nTHE FIX, in live-goals.tsx only (131 -> 150 lines, cap 250):\n- `refusalSentence(setup: LiveRefused)` -> `${code}: ${detail}`, now also used by `notAttached()` which had that same expression inlined. One refusal vocabulary; the coming-online note and the disabled control cannot drift apart. No layer invented — LiveRefused has none.\n- `const createDisabled = setup.ok ? createDisabledReason : refusalSentence(setup);`\n- forwarded as `createDisabledReason={createDisabled}`.\nOn refusal the derived sentence supersedes the caller's prop (the shell's string is generic and names no code, which DoD 1 requires); on an attached setup nothing is derived and the prop flows through untouched, preserving the shell's connection-descriptor banner. No exported signature changed; live-handshake, GoalsHome, cordum-app.tsx, transport, manifests and locks untouched.\n\nTESTS. goal-create-disabled.test.tsx 34 -> 128 lines, original GoalsHome arm preserved byte-for-byte, two arms added: the DIVERGENCE arm renders LiveGoalsHome directly with `{ok:false, code:LIVE_BOOTSTRAP_UNAVAILABLE, detail:\"daemon bootstrap unavailable\"}` and NO prop (so cordum-app's reason cannot answer for it) and asserts disabled, code+detail in both title and explanation, click opens no form, and neither onOpenBoard nor a stubbed fetch is called; the NEGATIVE arm proves the derivation is conditional. Red-first was real: `expect(button.disa",
  "commits": [
    {
      "sha": "b931be68d5aeec900086dea377da6923e8cc2c12",
      "ref": "refs/heads/moe/work-2026-09-02",
      "kind": "completion",
      "role": "qa",
      "sessionId": "qa-368df350@2026-09-02T19:41:00Z",
      "paths": [
        "apps/control-room/src/v2/goals/live-goals.tsx",
        "apps/control-room/src/v2/goals/goal-create-disabled.test.tsx",
        "apps/control-room/src/v2/cordum-app.test.tsx"
      ],
      "recordedBy": "qa-368df350",
      "recordedAt": "2026-09-02T20:35:58.204Z",
      "status": "REVIEW",
      "pushed": false,
      "recoveredBy": "qa-368df350",
      "message": "feat(task-75bb88d2): [R3-UI leaf] Disable goal creation after live bootstrap refusal — landed by QA under the hand-landing rule; row reached REVIEW with the deliverable uncommitted and task.commits empty."
    }
  ],
  "lastCommitOutcome": {
    "outcome": "committed",
    "kind": "completion",
    "sessionId": "qa-368df350@2026-09-02T19:41:00Z",
    "at": "2026-09-02T20:35:58.204Z"
  },
  "completedAt": "2026-09-02T20:36:45.174Z",
  "reviewCompletedAt": "2026-09-02T20:36:45.174Z",
  "reviewSummary": "APPROVED at HEAD 5f76aaa9. Full record: comment-c59110f6.\n\nCOMMIT: I LANDED IT. Row hit REVIEW 20:28:10 with task.commits EMPTY, all three declared paths DIRTY, no wrapper commit in ~10min. Landed b931be68d5aeec; `git show --name-only` carries EXACTLY the three declared paths, nothing foreign. record_commit duplicate=false. Net +120 LOC; live-goals.tsx 150 lines, under the 250 cap.\n\nDoD 1 PASS: divergence arm renders LiveGoalsHome directly with the exact refusal and NO prop, asserting disabled, code AND detail in both button.title and the explanation, no form after click, onOpenBoard and stubbed fetch never called. Negative arm proves the derivation is conditional.\n\nDoD 2 PASS: `setup.ok ? createDisabledReason : refusalSentence(setup)`, sourced only from LiveRefused code/detail, no layer invented, no exported signature changed.\n\nDoD 3 PASS - drill run by me, sha256 backup/restore: reverting ONLY the propagation while leaving createGoalDispatcher intact (:11 import, :108 use) reds the named arm \"refuses goal creation on its own when live bootstrap refused, naming the code\". Restore proven: sha256 -c OK, numstat back to the exact pre-drill 21/2.\n\nEXTRA CHECK the DoD did not ask for, since a 2->3 count edit in a peer test is the shape that hides an accommodation: under the same drill BOTH cordum-app counts RED (\"to have a length of 3 but got 2\"). The count change is a real join, not a number moved to pass.\n\nDoD 4 PASS: exact focused command 1 file/3 tests exit 0, reproducing the recorded tail; control-room typecheck `$ tsc` echo exit 0; full CR lane 113 files/1564 tests (1562 before, +2 = the added arms); root typecheck exit 0; root vitest 3 failed/10259 passed, foreign and BYTE-IDENTICAL to my 083c08aa baseline, so this row moved nothing. Neither failing file is owned, and the root include has no apps/** so that lane cannot see the three touched files."
}