{
  "id": "task-4d77ba041223441a804cc85a7edd9652",
  "epicId": "epic-3f9b9556a1364262a9d812e5fd1ea34e",
  "title": "The operator binds a deployment target from the browser, dispatching deployment.set_target through offer-wire",
  "description": "PRODUCER ROW filed by architect-b63fabf3 on 2026-09-06 at governor-afbde490's 13:50Z direction (item 4 of five). Consumer is task-84cd2e55aae942cf8d56657308215fde, whose DoD 3 requires \"a missing target renders DEPLOY_TARGET_MISSING as words WITH A WAY TO SET ONE\".\n\nTHE GAP, measured by worker-f72a1438: `DeployPort` exposes only `submit`, and `goal-deployments.tsx:187-193` says IN A COMMENT that target binding must happen outside this browser. The prerequisite row chose the honest limited alternative rather than a half-built control - which is correct, and leaves this row to build it. **No target -> deploy -> url journey can click a control that does not exist**, so the consumer's e2e DoD is unreachable without this.\n\nTHE COMMAND IS LANDED. `deployment.set_target` is a synchronous GOAL_HANDLERS row shipped by task-358b6ec8, with its payload roster `[\"environment\", \"network\", \"sshTarget\", \"url\"]` (daemon-command-payload-keys.ts:172) and admission rules in `deploy-target-contracts.ts` - `admitDeployUrl` (:61), `admitDeployNetwork` (:74), `admitSshTarget` (:78). This row builds the control that dispatches it.\n\nDISPATCH THROUGH OFFER-WIRE `spendOffer`, the way the PUBLISH card does. A second decision wire is a design regression (the consumer's rail 3 says so for its own card and the same applies here).\n\nEVERY FIELD IS OPERATOR-TYPED TEXT THAT REACHES AN ARGV OR A RENDERED RECEIPT. `network` and `sshTarget` reach `--network <network>` and `ssh <sshTarget> docker ...`; `url` lands on a receipt a screen renders. The daemon admits them and refuses outright rather than sanitising - notably `admitDeployUrl` REFUSES a url carrying `user:password@` instead of stripping it, deliberately, because stripping leaves the operator believing the environment answers something it does not. The control must surface that refusal as words, never silently clean the input and submit something the operator did not type.\n\nNOT IN SCOPE: the deploy control itself and the Deployments card (the consumer's), the deployment-state read, the per-goal deploy offer, the e2e seam.\n\nVERIFICATION COMMAND for moe.complete_task: `pnpm --filter @moe/control-room test`. Also `pnpm typecheck`. Root `pnpm test` covers no apps/** file and is not evidence for this row.",
  "definitionOfDone": [
    "A control in the browser binds a deployment target for a named environment, dispatching `deployment.set_target` through offer-wire `spendOffer` with the payload roster the daemon declares (`environment`, `network`, `sshTarget`, `url`). NO SECOND DECISION WIRE - reuse the arm/confirm shape the PUBLISH card already uses.",
    "THE ENVIRONMENT IS NAMED UNMISTAKABLY on the control and on its confirm step. A target bound to the wrong environment silently redirects a later deploy to another host, and the operator gets no second chance to notice - the daemon keys the binding `deploy-target:<projectId>:<environment>` and cannot tell it was a slip.",
    "ALL FOUR ADMISSION REFUSALS RENDER VERBATIM AS OPERATOR WORDS, each with its code: a url carrying userinfo, and a network or sshTarget containing whitespace, a shell metacharacter, a newline or a NUL. Assert the REFUSAL is shown - the daemon refuses rather than sanitising, deliberately, so a control that silently cleaned the input and submitted would be inventing a value the operator never typed.",
    "NO CREDENTIAL IS ACCEPTED, STORED OR RENDERED. There is no password or token field; assert the dispatched payload and any rendered output carry no token shape and no userinfo-bearing url, with a positive control so the assertion cannot pass vacuously (epic rail 3).",
    "sshTarget is OPTIONAL and its absence means a LOCAL docker daemon - `DeployTarget.sshTarget` is `string | null` and the port's comment says so. Assert the local path is reachable with the field left empty; a control that required an ssh destination would make local deploys unreachable.",
    "ASCII-only sources and apostrophe-free JSX text (the scheduler package-boundary scan reds on an apostrophe in JSX); the new testid namespace is confirmed free before use; every production file stays under 250 lines - `goal-deployments.tsx` is near the cap, so extract rather than grow it.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/control-room test` and `pnpm typecheck`. Root `pnpm test` covers no apps/** file, so say that rather than quoting a bare root count line."
  ],
  "taskRails": [
    "THE ENVIRONMENT IS NAMED UNMISTAKABLY. A target bound to the wrong environment redirects a later deploy to another host and nothing downstream can tell it was a slip. This is the row's one safety property.",
    "SURFACE THE REFUSAL, NEVER SANITISE. The daemon refuses a userinfo url outright rather than stripping it, deliberately. A control that cleans input and submits is inventing a value the operator did not type.",
    "NO CREDENTIAL FIELD. Not a password, not a token, not a private key. The ssh destination is a hostname, not an authenticator.",
    "OFFER-WIRE `spendOffer`, the PUBLISH card's shape. A second decision wire is a design regression.",
    "REUSE THE LANDED ADMISSION RULES; do not restate them in the browser. The daemon is the authority and a second copy in the client would drift - render what it refuses rather than pre-judging it."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "**PROBE THE ONE QUESTION THE ROW TURNS ON. NO EDITS.**\n\n(a) **DOES THE SURFACE SERVE A `deployment.set_target` OFFER?** `goal-deployments.tsx:187-193` ships a comment saying it does not, and if that is still true this row cannot be built: `spendOffer` (offer-wire.ts:62-78) builds its envelope FROM the affordance and there is no client-side mint.\n\n**A GREP CANNOT ANSWER THIS** — emission depends on runtime state. Write a throwaway probe that builds a real store, satisfies the prerequisite chain (`deployment.set_target` needs `project.activate`, which needs `project.bind_repository` and `provider.probe` — bootstrap-sequence.ts:46 and :31-35), reads the production affordance surface, and prints every offered `commandKind` with its `targetAggregateId`. Paste the FULL SOURCE and the output into the step note, then delete the probe.\n\nEXPECTED, from source: `affordance-read.ts:282-283` withholds chain offers for exactly `plan.propose`, `approval.decide`, `goal.close` and `repository.publish`, and `deployment.set_target` is a `BOOTSTRAP_COMMAND_KINDS` member not on that list — so it should be emitted, targeted at the PROJECT.\n\n**IF NO OFFER IS EMITTED: `moe.report_blocked` asking for an offer producer, and stop.** Do not fabricate an affordance, do not dispatch around the wire, do not add a second decision path. **IF AN OFFER IS EMITTED: record its exact `targetAggregateId`** — step 2 matches it by value.\n\n(b) **CONFIRM THE COMMENT IS STALE AND SAY SO.** If the offer exists, `goal-deployments.tsx:187-193` is now wrong and step 4 replaces it. Quote what you measured — a shipped comment asserting a limit that no longer holds is worse than no comment.\n\n(c) **DO NOT RE-TARGET THE OFFER.** Whatever step 1(a) prints, the correct scope for `set_target` is the PROJECT: the binding is keyed `deploy-target:<projectId>:<environment>` (deploy-target-contracts.ts:84). The sibling row task-2cedb26a re-targets the DEPLOY offer to `deploy:<goalId>` for a real reason that does NOT apply here (risk B). Record that you checked and are deliberately not copying it.\n\n(d) **READ THE FOUR CALL-SITE PRECEDENTS**: `preview-port.ts:72`, `goal-close-port.ts:32`, `escalation-port.ts:41`, `criterion-evidence-port.ts:13`. Each passes its own kind constant, layer constant and `ui-<verb>` prefix.\n\n(e) **RECORD SIZES**: `goal-deployments.tsx` (250 at planning — AT the cap) and `deploy-port.ts` (56).\n\n(f) **BASELINE THE GATES**: `pnpm --filter @moe/control-room test` and `pnpm typecheck`, standalone and foreground, with failing paths and count lines recorded.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "PROBE, NO EDITS. Row is BUILDABLE. But the plan's step 1(c) is now WRONG and I am deliberately not following it -- detail below.\n\n== (a) IS A `deployment.set_target` OFFER SERVED? YES. NOT PROJECT-TARGETED -- ONE PER ENVIRONMENT. ==\n\nI did NOT write a throwaway probe. A LANDED, COMMITTED arm already answers this against the production composition, and global rail 12 prefers a command that survives the commit over a deleted probe's transcript. `apps/daemon/src/http/affordance-read.test.ts:1451` -- `describe(\"deployment.set_target is offered per environment (task-9aea412b)\")`, which builds a real store, drives `driveThrough(worldStore, \"goal.create\")` past the prerequisite chain, and reads through `createAffordancePort` with NO `deployTarget` injection (\"the production reader is the composition under test\").\n\nRUN FRESH AT THIS HEAD, whole file, unfiltered:\n```\n$ pnpm --filter @moe/daemon exec vitest run --root . --config package.json src/http/affordance-read.test.ts\n Test Files  1 passed (1)\n      Tests  41 passed (41)\n```\nIts by-value assertion (:1497) is the answer to step 1(a):\n```\n[ { expectedVersion: 0, targetAggregateId: `deploy-target:${PROJECT}:preview` },\n  { expectedVersion: 0, targetAggregateId: `deploy-target:${PROJECT}:production` },\n  { expectedVersion: 0, targetAggregateId: `deploy-target:${PROJECT}:verify` } ]\n```\nEmission path measured in source: `affordance-read.ts:432-446` -- `resolveDeployTargetOffers(...)` pushes `offer(\"deployment.set_target\", entry.aggregateId, entry.version, ...)` once per environment. `:328-333` shows `deployment.set_target` is EXCLUDED from the generic bootstrap emit precisely because \"it is minted PER ENVIRONMENT below\".\n\nBuilder exists, so `spendOffer` will not answer OFFER_KIND_UNBUILDABLE: `packages/control-room-client/src/generated/generated-client.ts:321` `[\"deployment.set_target\"]: commandBuilderFor(\"deployment.set_target\")`.\n\n== (c) THE PLAN'S \"IT IS PROJECT-SCOPED, DO NOT RE-TARGET\" IS STALE. I am following measured state, per global rail 6. ==\n\nThe plan (planningNotes + step 1(c)) predicted `aggregateIdFor`'s `default: return subject ?? request.projectId`, i.e. ONE offer at the PROJECT id, and instructed me to record that I checked and deliberately did NOT copy the sibling's goal-scoped re-target. That prediction no longer holds: sibling **task-9aea412b** landed `apps/daemon/src/http/affordance-deploy-target-offers.ts` (a file the plan never names) which moved the axis. Following the plan literally -- matching an offer whose `targetAggregateId === projectId` -- would match an offer THAT IS NEVER EMITTED, and the landed arm asserts exactly that as a negative:\n```\n:1511  expect(read.nextAllowedCommands.filter((entry) =>\n         entry.commandKind === \"deployment.set_target\"\n         && entry.targetAggregateId === PROJECT)).toEqual([]);\n```\nThis is not the sibling-analogy mistake risk B warns about. Risk B forbids copying task-2cedb26a's GOAL-scoped re-target (`deploy:<goalId>`); I am not doing that. The axis is per-(project, environment), which is the command's own granularity -- the same granularity the plan argued for, just expressed as N offers instead of 1. `affordance-deploy-target-offers.ts:10-32` records why, with a live probe as its evidence: at goal scope, `deploy:goal-1` was still version 0 while `deploy-target:project-1:preview` was at 1, and a rebind at the goal version refused EXPECTED_VERSION_CONFLICT @ DURABLE_STORE.\n\nTHE LANDED CONSUMER CONTRACT, which step 3 must obey verbatim (:26-30):\n> \"The card CONSTRUCTS `deployTargetAggregateId(projectId, environment)` for the row it is rendering and spends THAT offer. It must NOT parse the environment back out of the id.\"\n\nTHIS STRENGTHENS DoD 2. The environment is not merely a payload field the operator could get wrong -- the OFFER ITSELF is per-environment, so the fenced aggregate and the payload's `environment` agree by construction.\n\n== (b) TWO STALE COMMENTS CONFIRMED, BOTH RETIRABLE ==\n1. `goal-deployments.tsx:187-193`: \"no set_target affordance is offered, so a control here would have none to spend.\" FALSE at this HEAD. Step 4 replaces it.\n2. `deploy-port.ts:26-35` (the plan did not flag this one): \"at this tree the daemon's affordance surface emits NO deployment kind at all: `git grep -n 'deployment\\.' -- apps/daemon/src/http` excluding tests returns zero.\" I re-ran that grep's intent -- `affordance-read.ts` alone now carries `deployment.set_target` at :236, :244, :328, :333, :418, :432, :441, :445, plus the whole `affordance-deploy-target-offers.ts` module. The paragraph also says a browser affordance \"belongs to the containerized-deployment parent task-5d309484\" -- that is THIS row. Step 2 must retire it; a shipped comment asserting a limit that no longer holds is worse than no comment.\n\n== (d) CALL-SITE PRECEDENTS (paths corrected -- goal-close-port and escalation-port are under approvals/, not goals/) ==\n`preview-port.ts:72` `spendOffer(wire, PREVIEW_COMMAND_KIND, affordance, payload, \"ui-p",
      "startedAt": "2026-09-07T07:49:01.952Z",
      "completedAt": "2026-09-07T07:53:33.107Z"
    },
    {
      "stepId": "step-2",
      "description": "THE PORT METHOD. `apps/control-room/src/v2/goals/deploy-port.ts` (56 lines, room to grow).\n\nAdd `bindTarget` beside the existing `submit`, dispatching `deployment.set_target` through `spendOffer` — the ONE wire (risk A). Follow `preview-port.ts:72` and `goal-close-port.ts:32` exactly: pass the kind constant, the affordance the surface served, the payload, a `ui-` correlation prefix and this port's layer constant. **No second decision wire, no direct client call, no fabricated affordance.**\n\n**THE PAYLOAD IS THE DAEMON'S ROSTER, EXACTLY**: `environment`, `network`, `sshTarget`, `url` (daemon-command-payload-keys.ts:172). Send the keys the command declares and nothing else — an extra key is refused at the edge and a missing one changes meaning.\n\n**`sshTarget` EMPTY MEANS LOCAL DOCKER, NOT MISSING** (risk H). `DeployTarget.sshTarget` is `string | null` and `deploy-ports.ts` documents null as a local daemon. Send `null` — not `\"\"`, not the key omitted — when the operator leaves it blank, and comment why: a control that required an ssh destination would make local deploys unreachable, and an empty string is a destination the daemon would try to use.\n\n**SEND WHAT THE OPERATOR TYPED, UNMODIFIED** (risk D). Do not trim, normalise, lowercase, strip userinfo, or repair a url. The daemon's `admitDeployUrl` (:61), `admitDeployNetwork` (:74) and `admitSshTarget` (:78) are the authority and they REFUSE rather than sanitise, deliberately — stripping a credential from a url leaves the operator believing the environment answers something it does not. A client that repaired input would submit a value nobody typed and report success for it.\n\n**REUSE THE LANDED ADMISSION RULES; DO NOT RESTATE THEM HERE.** A second copy of the grammar in the browser is a second authority that will drift from the first. Render what the daemon refuses (step 3); do not pre-judge.\n\nDeclare the layer constant the way the sibling ports do, reusing a rostered layer rather than inventing one.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.ts"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.ts",
        "apps/control-room/src/v2/goals/live-goal-deployments.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "note": "`bindTarget` landed on `deploy-port.ts` through `spendOffer`. ONE wire; no client-side mint, no direct client call, no second decision path.\n\n== I READ THE AMENDED STEP TEXT, AND IT CONFLICTS WITH THE LANDED PRODUCER. I RESOLVED IT RATHER THAN PICKING A SIDE. ==\n\nThe amendment (activeAmendmentId overlay, which I read before writing) says:\n> \"DERIVE `environment` FROM THE OFFER YOU SPEND, NEVER FROM A SEPARATE INPUT. The offer ... identifies its environment through its `targetAggregateId`.\"\n\nThe producer task-9aea412b landed the opposite instruction in `affordance-deploy-target-offers.ts:26-30`:\n> \"The card CONSTRUCTS `deployTargetAggregateId(projectId, environment)` ... It must NOT parse the environment back out of the id.\"\n\nBOTH WANT THE SAME PROPERTY -- the payload's `environment` and the aggregate the daemon fences must agree by construction, not by convention. They differ only on mechanism (parse-out vs construct-and-match). I did neither in isolation; I did CONSTRUCT + VERIFY, which is strictly stronger than either and satisfies both texts' stated intent:\n\n```ts\nif (affordance[\"targetAggregateId\"] !== deployTargetAggregateId(projectId, environment)) {\n  return Promise.resolve({ code: DEPLOY_TARGET_OFFER_MISMATCH, layer: DEPLOY_LAYER, ok: false });\n}\n```\n- The amendment's actual hazard (\"lets the operator type an environment independently and you send that\") is ELIMINATED: a disagreeing pair cannot reach the wire at all.\n- The producer's prohibition is honoured: nothing is parsed out of the id.\n- It fails CLOSED with a stable code (epic rail 4), instead of relying on the daemon to catch it.\n\nWHY THE DAEMON CANNOT CATCH THIS ITSELF, which is why the check is load-bearing rather than belt-and-braces: `setDeployTarget` derives its WRITE key from the PAYLOAD (`deployTargetAggregateId(request.projectId, admitted.environment)`, deploy-target-command.ts:15) while its optimistic fence comes from the OFFER's `expectedVersion`. A mismatched pair does not refuse -- it writes the payload's environment against another environment's version. That is precisely DoD 2's silent-slip scenario, one layer deeper than the UI.\n\nParsing WOULD have been unambiguous today (`ENVIRONMENT_NAME = /^[a-z][a-z0-9-]{0,62}$/u`, deploy-receipt-contracts.ts:80 -- no colon can appear in an environment, so a last-segment split is safe). I still did not, and the file records why: a parser agrees by CONVENTION and keeps agreeing silently after the id format moves, sending an environment nobody chose; a constructor stops matching, which is a visible refusal.\n\n== WHAT WENT IN ==\n- `SET_TARGET_COMMAND_KIND = \"deployment.set_target\"`, dispatched `spendOffer(wire, SET_TARGET_COMMAND_KIND, affordance, {environment, network, sshTarget, url}, \"ui-set-target\", DEPLOY_LAYER)` -- kind constant, served affordance, `ui-` prefix and layer, exactly the `preview-port.ts:72` shape.\n- PAYLOAD IS THE DAEMON'S ROSTER EXACTLY: `[\"environment\", \"network\", \"sshTarget\", \"url\"]` (daemon-command-payload-keys.ts:181). Four keys always sent, never a fifth.\n- `DeployTargetFields { network: string; sshTarget: string | null; url: string | null }`. `sshTarget: null` MEANS LOCAL DOCKER and the type says so -- the caller cannot express \"blank\" as `\"\"`, because `admitSshTarget` rejects `\"\"` and a caller that sent it would turn a valid local bind into a refusal.\n- NOTHING IS MODIFIED ON THE WAY OUT. No trim, lowercase, normalise, or userinfo strip. The three fields are destructured and placed on the payload unchanged.\n- NO ADMISSION GRAMMAR RESTATED (task rail 5). No url/network/ssh regex exists in this file. The only client-side check is the offer/environment identity above, which is not admission -- it is the wire's own precondition.\n\n== LAYER: NO NEW CONSTANT, SO NO ROSTER BACKFILL ==\nReused the existing module-private `DEPLOY_LAYER = \"CONTROL_ROOM_DEPLOY\"`. `grep -n \"CONTROL_ROOM_DEPLOY\\|DEPLOY_LAYER\" tests/security/boundary-roster.security.ts` is EMPTY -- it is `const`, not `export const`, which is why the roster does not carry it (unlike the exported `ENVIRONMENT_WRITE_LAYER` that cost a four-edit backfill this morning). Adding no constant means `EXPECTED_ROSTER_SIZE` does not move and `pnpm test:security` is untouched by this row. Daemon refusals carry `DAEMON_INGRESS` regardless, so this cannot mask DoD 3.\n\n== THE STALE PARAGRAPH IS RETIRED FROM THIS FILE TOO ==\n`deploy-port.ts:26-35` claimed \"at this tree the daemon's affordance surface emits NO deployment kind at all\" and parked `setTarget` as \"gone rather than parked\". Both halves are false at this HEAD (step 1 evidence). Replaced with a paragraph that says what changed and names the producer row, so the next reader is not told a limit that no longer holds.\n\n== TWO CONSEQUENTIAL EDITS, BOTH FORCED BY WIDENING `DeployPort` ==\n1. `live-goal-deployments.tsx`: the memoised port literal had to gain `bindTarget` or it no longer satisfies `DeployPort`. Wrapped it in the same refresh-after-answer shape `submit` already had -",
      "startedAt": "2026-09-07T07:54:11.083Z",
      "completedAt": "2026-09-07T07:57:42.717Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "THE PORT METHOD. `apps/control-room/src/v2/goals/deploy-port.ts` (56 lines, room to grow).\n\nAdd `bindTarget` beside the existing `submit`, dispatching `deployment.set_target` through `spendOffer` — the ONE wire (risk A). Follow `preview-port.ts:72` and `goal-close-port.ts:32` exactly: pass the kind constant, the affordance the surface served, the payload, a `ui-` correlation prefix and this port's layer constant. **No second decision wire, no direct client call, no fabricated affordance.**\n\n**THE OFFER IS NOW ONE PER ENVIRONMENT — THIS CHANGED, AND IT CHANGES WHERE `environment` COMES FROM.** task-9aea412b mints one `deployment.set_target` offer PER ENVIRONMENT, each targeted at `deploy-target:<projectId>:<environment>` and carrying THAT aggregate's `expectedVersion`. Read the delivered mint before writing this method rather than assuming a goal-scoped offer. WHY IT CHANGED, so nobody re-litigates it: worker-f78e0703's live probe showed a goal-scoped offer's version stays 0 while the setter's write aggregate reaches 1, so a rebind refuses `EXPECTED_VERSION_CONFLICT@DURABLE_STORE`; and `NextAllowedCommand` (runtime-affordance.ts:18-28) carries a SINGLE scalar `expectedVersion` with closed key rosters, so one offer cannot carry a version per environment.\n\n**THEREFORE DERIVE `environment` FROM THE OFFER YOU SPEND, NEVER FROM A SEPARATE INPUT.** The offer the operator acted on identifies its environment through its `targetAggregateId`; that is the environment whose version the offer's `expectedVersion` fences. If the control lets the operator type an environment independently and you send that, the payload and the fenced aggregate can disagree — the write lands against a different environment's version and refuses, or worse, binds the environment the operator did not choose. Deriving it makes them agree BY CONSTRUCTION rather than by convention.\n\n**THE PAYLOAD IS THE DAEMON'S ROSTER, EXACTLY**: `environment`, `network`, `sshTarget`, `url` (daemon-command-payload-keys.ts:172). Send the keys the command declares and nothing else — an extra key is refused at the edge and a missing one changes meaning.\n\n**`sshTarget` EMPTY MEANS LOCAL DOCKER, NOT MISSING** (risk H). `DeployTarget.sshTarget` is `string | null` and `deploy-ports.ts` documents null as a local daemon. Send `null` — not `\"\"`, not the key omitted — when the operator leaves it blank, and comment why: a control that required an ssh destination would make local deploys unreachable, and an empty string is a destination the daemon would try to use.\n\n**SEND WHAT THE OPERATOR TYPED, UNMODIFIED** (risk D). Do not trim, normalise, lowercase, strip userinfo, or repair a url. The daemon's `admitDeployUrl` (:61), `admitDeployNetwork` (:74) and `admitSshTarget` (:78) are the authority and they REFUSE rather than sanitise, deliberately — stripping a credential from a url leaves the operator believing the environment answers something it does not. A client that repaired input would submit a value nobody typed and report success for it. This applies to `network`, `sshTarget` and `url`; `environment` is the exception above precisely because it is not free input any more — it comes from the offer.\n\n**REUSE THE LANDED ADMISSION RULES; DO NOT RESTATE THEM HERE.** A second copy of the grammar in the browser is a second authority that will drift from the first. Render what the daemon refuses (step 3); do not pre-judge.\n\nDeclare the layer constant the way the sibling ports do, reusing a rostered layer rather than inventing one.\n\n**IF THE DELIVERED OFFER IS NOT PER-ENVIRONMENT** when you read it — for example if task-9aea412b landed a different shape — STOP and report rather than building to this text or to the stale goal-scoped assumption. The two rows must agree on one contract, and the producer is the authority on which it is.",
          "reason": "Aligning the consumer to the producer's re-planned contract, at governor direction (17:23Z: \"align task-4d77ba04's consumer step to the same contract (amend that row, do not re-plan it)\"). task-9aea412b's offer is now ONE PER ENVIRONMENT, targeted at deploy-target:<projectId>:<environment> and carrying THAT aggregate's expectedVersion, because worker-f78e0703's live probe proved a goal-scoped scalar cannot fence independently-versioned environments (goal-key version 0 vs write aggregate 1 -> EXPECTED_VERSION_CONFLICT@DURABLE_STORE) and NextAllowedCommand has only one scalar expectedVersion. Consequence for this step: the payload's `environment` must be DERIVED from the offer being spent, not typed independently, or the two disagree and the CAS fences the wrong aggregate. Everything else in the step -- the one wire, the exact payload roster, sshTarget null semantics, send-unmodified, no restated grammar, the rostered layer -- is unchanged. Human-directed 2026-09-06.",
          "amendedBy": "architect-aa54e231",
          "amendedAt": "2026-09-06T20:01:41.947Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-3",
      "description": "THE CONTROL. NEW module `apps/control-room/src/v2/goals/deploy-target-form.tsx` — **new because `goal-deployments.tsx` is EXACTLY 250 lines, at the cap** (risk F).\n\nFOUR INPUTS matching the payload roster: environment, network, sshTarget, url. Arm/confirm shape, copied from the PUBLISH card so there is one interaction grammar on this screen.\n\n**THE ENVIRONMENT IS NAMED UNMISTAKABLY ON THE CONTROL AND ON ITS CONFIRM STEP** (risk C, DoD 2). This is the row's one safety property. A target bound to the wrong environment silently redirects a later deploy to another host; the daemon stores exactly what it was handed and nothing downstream can tell it was a slip. The confirm step must make the environment impossible to misread — not a subtitle, not a placeholder.\n\n**`sshTarget` IS VISIBLY OPTIONAL AND ITS EMPTY MEANING IS STATED**: leaving it blank means a local docker daemon. Say that in the label, not only in a tooltip. A control that implied it was required would make the local path unreachable.\n\n**ALL FOUR ADMISSION REFUSALS RENDER VERBATIM AS OPERATOR WORDS, EACH WITH ITS CODE** (DoD 3): a url carrying userinfo, and a network or sshTarget containing whitespace, a shell metacharacter, a newline or a NUL. **Render the REFUSAL — do not silently clean the input and resubmit** (risk D). The daemon refusing outright is the design; a control that repaired the value would be inventing one.\n\n**NO CREDENTIAL FIELD** (risk E). No password, no token, no key, no passphrase. `sshTarget` is a destination, not an authenticator. Nothing this control renders or dispatches may carry something that authenticates.\n\n**ASCII-ONLY SOURCES AND APOSTROPHE-FREE JSX TEXT** (risk G) — the scheduler package-boundary scan reds on an apostrophe in JSX. Write \"cannot\" rather than a contraction. Confirm the new testid namespace is free before using it, following `environmentTestId`'s existing convention in the sibling card.\n\nUnder 250 lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/deploy-target-form.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/deploy-target-form.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/deploy-target-form.tsx",
        "apps/control-room/src/v2/goals/work-labels.ts",
        "apps/control-room/src/v2/goals/work-labels.test.ts"
      ],
      "note": "NEW MODULE `deploy-target-form.tsx` (210 lines) plus the amendment's `MISSING_TOKENS` work. The amendment's path guess was wrong and I measured it: `MISSING_TOKENS` is at `apps/control-room/src/v2/goals/work-labels.ts:90`, NOT under `ops/`.\n\n== THE CONTROL ==\nTHREE INPUTS (network, ssh destination, public url). The environment is NOT a field -- it is the row's, and the offer is found by CONSTRUCTING its key:\n```ts\nconst wanted = deployTargetAggregateId(projectId, environment);\nreturn frame.offers.find((row) =>\n  row[\"commandKind\"] === \"deployment.set_target\" && row[\"targetAggregateId\"] === wanted) ?? null;\n```\nNothing is split on `:`, nothing is minted. `offer === null` -> the component returns null, so a row the daemon serves no offer for gets NO control rather than a dead button.\n\nENVIRONMENT NAMED IN SEVEN PLACES, all rendered text: heading (`Bind a deploy target for <env>`), all three labels, the arm button (`Bind a target for <env>`), the CONFIRM button (`Confirm: bind <env> to <network>`), the confirm SENTENCE, the cancel button (`Do not bind <env>`), the success line and the refusal `said`. The confirm sentence is the safety property and it is a sentence, not a subtitle:\n> `This binds the <env> environment to network <network>[ on the docker daemon of this host | over ssh to <ssh>][ with no public url | , published at <url>]. Only <env> changes.`\n\"Only <env> changes.\" is deliberate -- three environment rows sit on one screen and the operator needs to read that the other two are untouched.\n\n`sshTarget` VISIBLY OPTIONAL, meaning stated in the LABEL not a tooltip: \"SSH destination for <env> (optional). Leave this empty to deploy with the docker daemon on this host.\" Blank -> `null`, never `\"\"`.\n\nNOTHING IS REPAIRED. `network`, `sshTarget` and `url` go to the port exactly as typed. The ONLY transformation is `\"\" -> null` on the two nullable fields, which is a MEANING (local docker / no public url) and not a repair, and the code comment says so. No trim, no lowercase, no userinfo strip, and no regex from `deploy-target-contracts.ts` is restated anywhere in the file (task rail 5).\n\nNO CREDENTIAL FIELD. Three inputs, none of them a password, token, key or passphrase.\n\n== ONE DAEMON CODE COVERS ALL FOUR REFUSALS, AND I DID NOT INVENT FOUR ==\n`setDeployTarget` (deploy-target-command.ts:11) answers `DEPLOY_TARGET_INVALID` @ `DAEMON_INGRESS` whenever `admitDeployTargetPayload` returns null -- userinfo url, or whitespace/metacharacter/newline/NUL in network or sshTarget, all collapse to that one code. Fabricating four browser-side codes would be exactly the second-authority failure task rail 5 forbids. So `BIND_REFUSAL_WORDS.DEPLOY_TARGET_INVALID` NAMES ALL FOUR CAUSES in one sentence and `OutcomeNote` carries `CODE @ LAYER` verbatim beside it. The words end on why: \"It refuses rather than cleaning the value up, so that what gets bound is only ever what you typed.\"\n\n== THE AMENDMENT'S RAW-TOKEN DEFECT: CONFIRMED BY MEASUREMENT, AND THE GUARD THAT SHOULD HAVE CAUGHT IT WAS BLIND ==\n`grep -n \"deployment.set_target\" apps/control-room/src/v2/goals/work-labels.ts` returned EMPTY before this step -- not in `MISSING_TOKENS`, not in `WORK_KIND_LABELS` -- so `labelForMissing` fell through to the raw token and an operator read \"STILL NEEDS deployment.set_target\". Added:\n> `\"deployment.set_target\": \"a deploy target bound for an environment, so a deploy has somewhere to go\"`\nkept deliberately in step with the card's `DEPLOY_TARGET_MISSING` words (\"...so there is nowhere to deploy\") so the same absence does not read as two different problems.\n\n**THEN THE SET-EQUALITY ARM WENT RED, AND IT WAS RIGHT TO.** `work-labels.test.ts` enumerates the daemon side from the EMITTING SEAM (affordance-read.ts source text) and asserts `Object.keys(MISSING_TOKENS)` EQUALS that set, both directions (global rail 9). Adding a key the scanner could not see breaks it. **The scanner could not see it because of a real hole:**\n```\nold: /missing: \\[([^\\]]*)\\]/gu\n```\nrequires `[` IMMEDIATELY after `missing: `. The emission site is `affordance-read.ts:418`:\n```\nmissing: deployBound ? [] : [\"deployment.set_target\"], status: deployBound ? \"READY\" : \"BLOCKED\" }));\n```\nso the old regex read that line as containing NO tokens. **That blindness is how the token shipped unlabelled in the first place** -- the same class as the `replan` hole this suite's own header brags about catching. I fixed the scanner rather than weakening the assertion:\n```ts\nconst MISSING_FIELD_VALUE = /missing: ([^\\n]*?)(?=, (?:status|version|kind|aggregateId)\\b|\\n)/gu;\n```\nCut at the next SIBLING KEY, not end of line, on purpose: :418 continues `, status: ... ? \"READY\" : \"BLOCKED\"` on the SAME LINE, and a line-greedy capture would harvest \"READY\" and \"BLOCKED\" as prerequisite tokens. Array bodies are then extracted from the captured expression, so the two-stage precision of the original is kept.\n\nANTI-VACUITY ARM ADDED, so the broadening cannot be a no-op:\n```ts\nexpect(missingFieldTokens",
      "startedAt": "2026-09-07T07:57:49.956Z",
      "completedAt": "2026-09-07T08:03:49.704Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "THE CONTROL. NEW module `apps/control-room/src/v2/goals/deploy-target-form.tsx` — **new because `goal-deployments.tsx` is EXACTLY 250 lines, at the cap** (risk F).\n\nFOUR INPUTS matching the payload roster: environment, network, sshTarget, url. Arm/confirm shape, copied from the PUBLISH card so there is one interaction grammar on this screen.\n\n**THE ENVIRONMENT IS NAMED UNMISTAKABLY ON THE CONTROL AND ON ITS CONFIRM STEP** (risk C, DoD 2). This is the row's one safety property. A target bound to the wrong environment silently redirects a later deploy to another host; the daemon stores exactly what it was handed and nothing downstream can tell it was a slip. The confirm step must make the environment impossible to misread — not a subtitle, not a placeholder.\n\n**`sshTarget` IS VISIBLY OPTIONAL AND ITS EMPTY MEANING IS STATED**: leaving it blank means a local docker daemon. Say that in the label, not only in a tooltip. A control that implied it was required would make the local path unreachable.\n\n**ALL FOUR ADMISSION REFUSALS RENDER VERBATIM AS OPERATOR WORDS, EACH WITH ITS CODE** (DoD 3): a url carrying userinfo, and a network or sshTarget containing whitespace, a shell metacharacter, a newline or a NUL. **Render the REFUSAL — do not silently clean the input and resubmit** (risk D). The daemon refusing outright is the design; a control that repaired the value would be inventing one.\n\n**NO CREDENTIAL FIELD** (risk E). No password, no token, no key, no passphrase. `sshTarget` is a destination, not an authenticator. Nothing this control renders or dispatches may carry something that authenticates.\n\n**ASCII-ONLY SOURCES AND APOSTROPHE-FREE JSX TEXT** (risk G) — the scheduler package-boundary scan reds on an apostrophe in JSX. Write \"cannot\" rather than a contraction. Confirm the new testid namespace is free before using it, following `environmentTestId`'s existing convention in the sibling card.\n\nUnder 250 lines.\n\n---\n\n**ADDED BY AMENDMENT (governor 14:48Z, routing worker-1d53e70c's finding from task-2cedb26a) — THE RAW PREREQUISITE TOKEN.**\n\n**THE DEFECT:** the daemon reports `missing: [\"deployment.set_target\"]` and the browser renders it RAW — the operator sees **\"STILL NEEDS deployment.set_target\"**, a command kind quoted at a human. `work-labels.ts` `MISSING_TOKENS` has no entry for it, so the raw token falls through.\n\n**ADD ONE `MISSING_TOKENS` ENTRY**, reading like *\"a deploy target to be bound for an environment\"* — a phrase that tells the operator what is missing and implies what to do, rather than naming an internal kind. Match the voice of the entries already there rather than inventing a new register.\n\n**ASSERT IT IN `work-labels`' OWN TEST**, not only through the card. The token map is the authority and it needs an arm of its own; a card-level assertion would pass for the wrong reason if the card happened to special-case the string.\n\n**KEEP THE CARD'S `DEPLOY_TARGET_MISSING` WORDS CONSISTENT WITH IT.** The same absence is now described in two places — the refusal words on the card and the missing-token phrase — and they must not contradict each other. An operator who reads \"a deploy target to be bound for an environment\" in one place and something differently-shaped in the other has to work out whether they are the same problem.\n\n**THESE TWO FILES JOIN THIS STEP'S SCOPE:** `apps/control-room/src/v2/ops/work-labels.ts` (or wherever `MISSING_TOKENS` currently lives — measure it, do not trust this path) and its test sibling. Declare them in `complete_step.modifiedFiles`.\n\n**THE FAIL-CLOSED CORNER worker-1d53e70c FOUND IS CORRECT BEHAVIOUR, NOT A BUG TO FIX HERE:** a committed `set_target` whose payload no longer decodes reports `missing`. That is the right answer — an undecodable binding is not a binding — and it belongs in that row's completion record, not in this control.",
          "reason": "Governor 14:48Z routed worker-1d53e70c's 2cedb26a finding here: the `missing: [\"deployment.set_target\"]` prerequisite token renders RAW in the browser (\"STILL NEEDS deployment.set_target\") because work-labels.ts MISSING_TOKENS has no entry. This row is the right home (the target-binding control). Adds work-labels.ts + its test to this step's scope; the DoD clause is the governor's to add.",
          "amendedBy": "architect-b63fabf3",
          "amendedAt": "2026-09-06T15:03:00.889Z"
        },
        {
          "amendmentId": "amend-2",
          "description": "THE CONTROL. NEW module `apps/control-room/src/v2/goals/deploy-target-form.tsx` — **new because `goal-deployments.tsx` is EXACTLY 250 lines, at the cap** (risk F).\n\n**THE ENVIRONMENT IS NOT AN INPUT — IT IS THE OFFER YOU SPEND.** This clause is AMENDED and it supersedes the old \"four inputs\" wording. task-9aea412b's producer serves ONE `deployment.set_target` offer PER ENVIRONMENT, each with `targetAggregateId = deploy-target:<projectId>:<environment>` and `expectedVersion` read from THAT aggregate. So this card renders its binding control on the environment ROW it already renders, and spends the offer whose `targetAggregateId` equals `deployTargetAggregateId(projectId, thatRowsEnvironment)`.\n\n- **MATCH BY CONSTRUCTING THE KEY, NEVER BY PARSING IT.** Build the expected id for the row's environment and find the offer equal to it — the same by-value discipline `deployOffer` already uses for `deploy:${goalId}`. Do NOT split the id on `:` to recover the environment: a projectId containing a colon makes that wrong, silently and in the direction that binds the wrong host.\n- **THE PAYLOAD'S `environment` IS THE ROW'S ENVIRONMENT**, so the payload and the fenced aggregate agree BY CONSTRUCTION rather than by convention. That is the whole point of the producer's granularity decision, and the reason a free-typed environment field is now forbidden here.\n- **NO OFFER FOR A ROW MEANS NO CONTROL FOR THAT ROW** — render nothing rather than a dead button, exactly as the deploy control does. Never mint an affordance client-side.\n\n**THREE INPUTS, matching the remainder of the payload roster: network, sshTarget, url.** Arm/confirm shape, copied from the PUBLISH card so there is one interaction grammar on this screen. The dispatched payload still carries all four declared keys (`environment`, `network`, `sshTarget`, `url`) and nothing more — `environment` is supplied by the row, not typed.\n\n**THE ENVIRONMENT IS NAMED UNMISTAKABLY ON THE CONTROL AND ON ITS CONFIRM STEP** (risk C, DoD 2). This is the row's one safety property and the amendment strengthens rather than replaces it. A target bound to the wrong environment silently redirects a later deploy to another host; the daemon stores exactly what it was handed and nothing downstream can tell it was a slip. The confirm step must make the environment impossible to misread — not a subtitle, not a placeholder.\n\n**`sshTarget` IS VISIBLY OPTIONAL AND ITS EMPTY MEANING IS STATED**: leaving it blank means a local docker daemon. Say that in the label, not only in a tooltip. A control that implied it was required would make the local path unreachable.\n\n**ALL FOUR ADMISSION REFUSALS RENDER VERBATIM AS OPERATOR WORDS, EACH WITH ITS CODE** (DoD 3): a url carrying userinfo, and a network or sshTarget containing whitespace, a shell metacharacter, a newline or a NUL. **Render the REFUSAL — do not silently clean the input and resubmit** (risk D). The daemon refusing outright is the design; a control that repaired the value would be inventing one.\n\n**NO CREDENTIAL FIELD** (risk E). No password, no token, no key, no passphrase. `sshTarget` is a destination, not an authenticator. Nothing this control renders or dispatches may carry something that authenticates.\n\n**ASCII-ONLY SOURCES AND APOSTROPHE-FREE JSX TEXT** (risk G) — the scheduler package-boundary scan reds on an apostrophe in JSX. Write \"cannot\" rather than a contraction. Confirm the new testid namespace is free before using it, following `environmentTestId`'s existing convention in the sibling card.\n\nUnder 250 lines.",
          "reason": "Producer contract settled on task-9aea412b (plan re-submitted 2026-09-07): the daemon now serves ONE deployment.set_target offer PER ENVIRONMENT, targeted at deploy-target:<projectId>:<environment>, not one goal-scoped offer. The environment therefore cannot be a free-typed input on this control - it is determined by WHICH offer the operator spends, or the payload and the CAS-fenced aggregate can disagree. Original text kept for audit; only the environment-input clause changes, every safety clause is preserved verbatim.",
          "amendedBy": "architect-dbe3631e",
          "amendedAt": "2026-09-07T00:10:35.176Z"
        },
        {
          "amendmentId": "amend-3",
          "description": "THE CONTROL. NEW module `apps/control-room/src/v2/goals/deploy-target-form.tsx` — **new because `goal-deployments.tsx` is EXACTLY 250 lines, at the cap** (risk F). Under 250 lines.\n\n**THE ENVIRONMENT IS NOT AN INPUT — IT IS THE OFFER YOU SPEND.** Supersedes the original \"four inputs\" wording. task-9aea412b's producer serves ONE `deployment.set_target` offer PER ENVIRONMENT, each with `targetAggregateId = deploy-target:<projectId>:<environment>` and `expectedVersion` read from THAT aggregate. So the binding control renders on the environment ROW this card already renders, and spends the offer whose `targetAggregateId` equals `deployTargetAggregateId(projectId, thatRowsEnvironment)`.\n\n- **MATCH BY CONSTRUCTING THE KEY, NEVER BY PARSING IT** — the by-value discipline `deployOffer` already uses for `deploy:${goalId}`. Do NOT split the id on `:` to recover the environment: a projectId containing a colon makes that wrong silently, in the direction that binds the wrong host.\n- **THE PAYLOAD'S `environment` IS THE ROW'S ENVIRONMENT**, so payload and CAS-fenced aggregate agree BY CONSTRUCTION, not by convention. That is why a free-typed environment field is now forbidden.\n- **NO OFFER FOR A ROW MEANS NO CONTROL FOR THAT ROW** — render nothing rather than a dead button. Never mint an affordance client-side.\n\n**THREE INPUTS: network, sshTarget, url.** Arm/confirm shape copied from the PUBLISH card, one interaction grammar per screen. The dispatched payload still carries all four declared keys (`environment`, `network`, `sshTarget`, `url`) and nothing more — `environment` comes from the row.\n\n**THE ENVIRONMENT IS NAMED UNMISTAKABLY ON THE CONTROL AND ON ITS CONFIRM STEP** (risk C, DoD 2). The row's one safety property. A target bound to the wrong environment silently redirects a later deploy to another host; the daemon stores exactly what it was handed and nothing downstream can tell it was a slip. Not a subtitle, not a placeholder.\n\n**`sshTarget` IS VISIBLY OPTIONAL AND ITS EMPTY MEANING IS STATED**: blank means a local docker daemon. Say it in the label, not only a tooltip. Implying it was required would make the local path unreachable.\n\n**ALL FOUR ADMISSION REFUSALS RENDER VERBATIM AS OPERATOR WORDS, EACH WITH ITS CODE** (DoD 3): a url carrying userinfo, and whitespace / shell metacharacter / newline / NUL in network and sshTarget. **Render the REFUSAL — do not silently clean the input and resubmit** (risk D). A control that repaired the value would be inventing one.\n\n**NO CREDENTIAL FIELD** (risk E). No password, token, key or passphrase. `sshTarget` is a destination, not an authenticator.\n\n**ASCII-ONLY SOURCES AND APOSTROPHE-FREE JSX TEXT** (risk G) — the scheduler package-boundary scan reds on an apostrophe in JSX. Write \"cannot\", not a contraction. Confirm the new testid namespace is free first, following `environmentTestId` in the sibling card.\n\n---\n\n**FROM THE GOVERNOR'S 14:48Z AMENDMENT (routing worker-1d53e70c's finding from task-2cedb26a) — THE RAW PREREQUISITE TOKEN. Carried forward; it was NOT retired.**\n\n**THE DEFECT:** the daemon reports `missing: [\"deployment.set_target\"]` and the browser renders it RAW — the operator sees **\"STILL NEEDS deployment.set_target\"**, a command kind quoted at a human. `work-labels.ts` `MISSING_TOKENS` has no entry for it, so the raw token falls through.\n\n**ADD ONE `MISSING_TOKENS` ENTRY**, reading like *\"a deploy target to be bound for an environment\"* — a phrase that tells the operator what is missing and implies what to do, rather than naming an internal kind. Match the voice of the entries already there rather than inventing a new register.\n\n**ASSERT IT IN `work-labels`' OWN TEST**, not only through the card. The token map is the authority and it needs an arm of its own; a card-level assertion would pass for the wrong reason if the card happened to special-case the string.\n\n**KEEP THE CARD'S `DEPLOY_TARGET_MISSING` WORDS CONSISTENT WITH IT.** The same absence is now described in two places — the refusal words on the card and the missing-token phrase — and they must not contradict each other. An operator who reads \"a deploy target to be bound for an environment\" in one place and something differently-shaped in the other has to work out whether they are the same problem.\n\n**THESE TWO FILES JOIN THIS STEP'S SCOPE:** `apps/control-room/src/v2/ops/work-labels.ts` (or wherever `MISSING_TOKENS` currently lives — measure it, do not trust this path) and its test sibling. Declare them in `complete_step.modifiedFiles`.\n\n**THE FAIL-CLOSED CORNER worker-1d53e70c FOUND IS CORRECT BEHAVIOUR, NOT A BUG TO FIX HERE:** a committed `set_target` whose payload no longer decodes reports `missing`. That is the right answer — an undecodable binding is not a binding — and it belongs in that row's completion record, not in this control.",
          "reason": "RESTORES the governor's 14:48Z MISSING_TOKENS clause, which amend-2 dropped: I composed amend-2 from the step's BASE description because my disk read did not surface activeAmendmentId, so the raw-prerequisite-token clause and its two extra files vanished from the effective text. This amendment carries BOTH - the governor's clause AND the per-environment offer contract settled on task-9aea412b (one deployment.set_target offer per environment, targeted at deploy-target:&lt;projectId&gt;:&lt;environment&gt;), which makes the environment the offer you spend rather than a typed input. amend-2 is superseded; use THIS text.",
          "amendedBy": "architect-dbe3631e",
          "amendedAt": "2026-09-07T00:11:32.611Z"
        }
      ],
      "activeAmendmentId": "amend-3"
    },
    {
      "stepId": "step-4",
      "description": "MOUNT IT, AND RETIRE THE HONEST-LIMIT TEXT. `apps/control-room/src/v2/goals/goal-deployments.tsx` — **EXACTLY 250 lines, AT the cap, so this edit must be net-neutral or SMALLER** (risk F).\n\n`:187-193` currently renders, when the refusal is `DEPLOY_TARGET_MISSING`:\n> \"Bind a target for {environment} first, with deployment.set_target. This screen cannot bind one yet, so it has to be bound outside the browser before a deploy here can go through.\"\n\nThat paragraph was the honest alternative to a half-built control, and step 1(b) is what makes it retirable. **Replace it with the control from step 3** — same condition, same place: `DEPLOY_TARGET_MISSING` is exactly where the consumer's DoD 3 wants \"a way to set one\". The swap removes roughly seven lines of prose and adds a mount, so the file should not grow. **Do not reformat it and do not extract anything else while you are in there** — an unrelated extraction here is a rebase conflict for the rows queued behind this one.\n\n**DO NOT BUILD THE DEPLOY CONTROL OR THE CARD ITSELF.** Those are task-84cd2e55's, and the deployment-state read is task-c3291fe0's. This row adds the binding control and nothing else; if the card needs a shape it does not have, comment on that row rather than widening here.\n\nIf after the swap the file still exceeds 250, say so with the number and extract the SMALLEST coherent piece — but prefer to land net-neutral, because every line moved in this file collides with a queued row.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/goal-deployments.tsx",
        "apps/control-room/src/v2/goals/live-goal-deployments.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "note": "MOUNTED, PROSE RETIRED, AND THE FILE LANDED AT **EXACTLY 250** -- net-neutral, as the step required.\n\n== THE SWAP ==\n`goal-deployments.tsx:187-195` (nine lines of honest-limit paragraph) became six lines of mount, on the SAME condition and in the SAME place:\n```tsx\n{view.code !== \"DEPLOY_TARGET_MISSING\" ? null : (\n  // THE WAY TO SET ONE, in the exact place the absence is reported. It renders\n  // only if the daemon served THIS environment a set_target offer to spend.\n  <DeployTargetForm\n    environment={view.environment} frame={frame} port={port} projectId={projectId} />\n)}\n```\nThe refusal WORDS above it are untouched -- `refusalWords(\"DEPLOY_TARGET_MISSING\")` still renders \"No target is bound for this environment, so there is nowhere to deploy.\" The control answers that sentence; it does not replace it. That also keeps the card in step with the new `MISSING_TOKENS` phrase from step 3, which the amendment required.\n\nLINE BUDGET, spent deliberately to hit 250 on the nose: -9 paragraph, +6 mount, +1 import, +2 prop (one doc line, one field). I did NOT reformat anything, did NOT extract anything, and did NOT touch the deploy control, the card's own arm/confirm logic, or the deployments read -- those belong to task-84cd2e55 and task-c3291fe0. Every line I did not need to move stayed put, because rows are queued behind this file.\n\n== `projectId` IS OPTIONAL ON PURPOSE, SO board-screen.tsx STAYED UNTOUCHED ==\n`readonly projectId?: string | null | undefined`. board-screen.tsx:107 mounts the card from its `deploying` prop and does not have a project id; making the prop required would have forced an edit there for no behavioural gain. Measured first: `grep -rn \"deploying\" apps/control-room/src` outside board-screen.tsx returns ZERO non-comment hits, so that mount is exercised by TESTS ONLY and the sole production path is `LiveGoalDeployments` (cordum-app.tsx:306), which now passes `setup.projectId`. The control is production-reachable through the one mount that actually runs.\n\n== THE RETIREMENT IS PINNED, NOT ASSUMED ==\nThe old paragraph had a landed consumer -- `goal-deployments.test.tsx:254` read `cr.deploy.preview.settarget` and asserted \"cannot bind one yet\". It went RED exactly as global rail 19 predicts:\n```\nTestingLibraryElementError: Unable to find an element by: [data-testid=\"cr.deploy.preview.settarget\"]\n```\nI replaced that arm rather than deleting it, with the step-5 (J)/(K) pair brought forward -- leaving a red across a step boundary in a SHARED checkout hands every peer a foreign red to attribute, and I said in step 2 I would not do that.\n\n**(J)** asserts the control is PRESENT and names `preview`, that the refusal words still render, and that the retired sentence is GONE **by text off the card root** -- `not.toContain(\"cannot bind one yet\")` and `not.toContain(\"bound outside the\")`, plus `queryByTestId(\"cr.deploy.preview.settarget\")` is null. Asserting only \"the control exists\" would have stayed green beside the stale paragraph, and that paragraph tells the operator the screen CANNOT do the thing it now does.\n\n**(K)** two ways: `production` refusing for a different reason grows no control, and a swept roster of the other three refusal codes plus a never-deployed row all render none. The sweep asserts its own cardinality (`expect(rows).toHaveLength(others.length + 1)`) so a sweep that silently produced zero rows cannot pass, and it ends with a POSITIVE CONTROL -- the same render DOES produce the control for `DEPLOY_TARGET_MISSING` -- so the null-checks cannot be passing because I typo'd the testid scheme.\n\n== THE FIXTURE IS THE SHAPE THE DAEMON ACTUALLY SERVES ==\n`FRAME` now carries `setTargetOfferFor(\"preview\", 0)` and `setTargetOfferFor(\"production\", 2)` -- one offer per environment, each at `deploy-target:proj-1:<environment>` with its OWN expectedVersion. I deliberately did not fixture a single project-wide offer: that is the shape that DOES NOT EXIST at this HEAD (step 1 measured the landed arm asserting a PROJECT-targeted offer returns `[]`), and an arm built on one would pass against a surface the daemon never produces. The differing versions (0 vs 2) are there because they are the reason the axis is per-environment at all.\n\n**THAT THIS ARM PASSES IS THE FIRST END-TO-END EVIDENCE THE CONSTRUCT-BY-KEY PATH WORKS**: the form built `deploy-target:proj-1:preview` from `(projectId, environment)`, found the offer the fixture served under that exact id, and rendered. Nothing was parsed and nothing was minted.\n\n== GATES, standalone and foreground ==\n```\n$ pnpm --filter @moe/control-room exec vitest run src/v2/goals/goal-deployments.test.tsx\n Test Files  1 passed (1)      Tests  25 passed (25)\n$ pnpm --filter @moe/control-room typecheck\n$ tsc --project tsconfig.json                                   EXIT 0\n$ pnpm --filter @moe/control-room test\n Test Files  188 passed (188)  Tests  2395 passed (2395)         EXIT=0\n```\n2394 -> 2395 is +1 and it IS mine: the retired arm became two arms. (The earlier 2386 ",
      "startedAt": "2026-09-07T08:04:10.776Z",
      "completedAt": "2026-09-07T08:06:28.442Z"
    },
    {
      "stepId": "step-5",
      "description": "THE ARMS. Two new test files plus an arm in the existing card test.\n\n**`deploy-port.test.ts` (NEW):**\n- **(A)** `bindTarget` dispatches `deployment.set_target` through `spendOffer` with the affordance the surface served — assert the wire was called with THAT affordance, not a constructed one. This is what proves there is no second decision path.\n- **(B) THE PAYLOAD IS THE DAEMON'S EXACT ROSTER** (`environment`, `network`, `sshTarget`, `url`) and nothing more.\n- **(C) BLANK `sshTarget` SENDS `null`, NOT `\"\"` AND NOT AN OMITTED KEY**, and the local path is reachable that way. An empty string is a destination the daemon would try to use.\n- **(D) THE OPERATOR'S INPUT IS SENT UNMODIFIED.** Feed a url carrying `user:password@` and assert the port dispatches it AS TYPED rather than stripping it — the daemon must be the one to refuse. **This arm looks backwards and that is the point:** a client that sanitised would submit a value nobody typed.\n- **(E) NO CREDENTIAL ON THE WIRE.** `JSON.stringify` the dispatched payload and assert no token shape; **positive control** over a literal containing one so the arm cannot pass vacuously.\n\n**`deploy-target-form.test.tsx` (NEW):**\n- **(F) THE ENVIRONMENT IS NAMED ON THE CONTROL AND ON THE CONFIRM STEP**, asserted by rendered text, not by prop. The row's safety property.\n- **(G) ALL FOUR REFUSALS RENDER VERBATIM WITH THEIR CODES** — userinfo url, and whitespace / metacharacter / newline / NUL in network and sshTarget.\n- **(H) `sshTarget` IS VISIBLY OPTIONAL** and its blank meaning (local docker) is stated in the rendered text.\n- **(I) NO CREDENTIAL INPUT EXISTS.** Assert no password/token field is rendered.\n\n**`goal-deployments.test.tsx` (EXISTS — add arms, rebase around peers):**\n- **(J)** With `DEPLOY_TARGET_MISSING`, the control is PRESENT and the old \"cannot bind one yet\" prose is GONE — assert the absence of that sentence by its text, so the retirement is pinned and cannot silently come back.\n- **(K)** With any other refusal, the binding control is absent. It belongs to the missing-target case only.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.test.ts",
        "apps/control-room/src/v2/goals/deploy-target-form.test.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "newFiles": [
        "apps/control-room/src/v2/goals/deploy-port.test.ts",
        "apps/control-room/src/v2/goals/deploy-target-form.test.tsx"
      ],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.test.ts",
        "apps/control-room/src/v2/goals/deploy-target-form.test.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx"
      ],
      "note": "TWO NEW TEST FILES (9 + 15 arms) PLUS THE CARD ARMS. 188 -> 190 files, 2395 -> 2419 tests, EXIT 0.\n\n== `deploy-port.test.ts` (NEW, 9 arms): A, B, C, D, E, M ==\n- **(A)** `expect(built[0]?.affordance).toBe(offer)` -- **`toBe`, not `toEqual`**. An equal-looking object the port built itself would be a second decision path wearing the shape of the first; only the object the surface served passes identity.\n- **(A, second half)** the MISMATCH arm asserts the reason code AND the layer -- `DEPLOY_TARGET_OFFER_MISMATCH@CONTROL_ROOM_DEPLOY` -- not merely that it failed, and asserts `built` and `sent` are both EMPTY. An arm that accepted any failure would still pass if the daemon refused instead, which is the vacuity the global rail warns about.\n- **(B)** SET EQUALITY on payload keys against the daemon's roster. Not a subset: the decoder is exact-arity, so a fifth key is refused and a missing one reads as malformed, and a subset check would not be testing the rule.\n- **(C)** blank ssh sends `null`; asserts `\"sshTarget\" in payload` is true, the value is null, and `not.toBe(\"\")`, and that the outcome is ACCEPTED -- the local path is reachable, not merely well-formed.\n- **(D)** dispatches `https://deployer:hunter2@preview.example.test/app` AS TYPED, plus a second arm for `\"  Moe-Preview$(id)  \"` and `\"deploy@HOST.example; rm -rf /\"` -- leading space, uppercase, `$( )`, `;`. All survive.\n- **(E)** `JSON.stringify` of the payload matched against five secret shapes, each with a POSITIVE CONTROL over a planted literal so the loop cannot pass because the regexes match nothing.\n- **(M)** for two rows, asserts the spent offer's `targetAggregateId` EQUALS `deployTargetAggregateId(PROJECT, payload.environment)` -- the two values EQUAL TO EACH OTHER, not each separately correct against a literal, which would still pass if the control paired the wrong two. Also pins `expectedVersion` per row.\n\nFixtures are built with the PRODUCTION `deployTargetAggregateId`, so a producer key change breaks these arms instead of passing them against a frame the daemon no longer serves.\n\n== `deploy-target-form.test.tsx` (NEW, 15 arms): F, G, H, I, L, N ==\n- **(F)** environment read off the DOM in heading, all three labels, arm button, confirm SENTENCE, confirm button and cancel button -- never off a prop, because the prop being right is not the property at risk. Plus the inverse: `expect(root).not.toContain(\"preview\")` and `not.toContain(\"verify\")` while rendering `production`. A confirm that named another environment is the exact slip DoD 2 exists for.\n- **(L)** three offers at versions 0 / 3 / 6; the production row spends `deploy-target:proj-1:production` at version **3**. An arm matching only on `commandKind`, or taking \"the first offer\", would pass while fencing preview's version on a production write.\n- **(N)** offers for two environments only -> the third row renders NOTHING (`root` and `button` both null, absent not disabled), with a POSITIVE CONTROL on the SAME frame proving an offered row does render. Plus a null-`projectId` arm.\n- **(H)** the label text is asserted verbatim, the confirm step states \"on the docker daemon of this host\", and the dispatched `sshTarget` is `null` and `not.toBe(\"\")`.\n- **(I)** exactly three inputs, none `type=\"password\"`, no id or rendered text matching five secret shapes, with a positive control.\n\n== (G): ONE DAEMON CODE, FIVE INPUTS, AND A MEASURED PLATFORM FINDING ==\nThe four DoD 3 clauses plus the NUL run as a five-case table. All collapse to `DEPLOY_TARGET_INVALID` @ `DAEMON_INGRESS`, because `admitDeployTargetPayload` returns null for every one -- inventing four browser-side codes would be exactly the second authority task rail 5 forbids. Each case asserts the words (\"The daemon refused this target and bound nothing\", \"It refuses rather than cleaning the value up\"), the code verbatim as `DEPLOY_TARGET_INVALID @ DAEMON_INGRESS`, and the environment.\n\n**I FOUND MY FIRST VERSION OF THIS ARM WAS COSMETIC AND FIXED IT.** `userEvent.type` cannot put a raw newline or NUL into an input -- it reads the first as Enter and drops the second -- so the two hostile cases were quietly five printable strings. Rewritten to enter values with `fireEvent.change`, and an ANTI-VACUITY arm now asserts the table really contains both characters, so it cannot silently degrade back.\n\n**THEN THE NEWLINE CASE WENT RED, AND IT WAS A REAL FINDING RATHER THAN A TEST BUG:**\n```\nAssertionError: expected 'host.testrm -rf /' to be 'host.test\\nrm -rf /'\n```\n**An `<input type=\"text\">` DROPS the newline as the value is assigned, before any code in this repo sees it.** My control did not sanitise -- the DOM did. I did not weaken the assertion to hide that. The case carries `platformStrips: true` and takes a branch that asserts the loss was the platform's (`held` no longer contains the newline) AND that everything else came through intact (`sent` still contains `rm -rf /` and `host.test`), while `expect(sent).toBe(held)` -- the property this contro",
      "startedAt": "2026-09-07T08:06:34.033Z",
      "completedAt": "2026-09-07T08:12:32.852Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "THE ARMS. Two new test files plus arms in the existing card test.\n\n**`deploy-port.test.ts` (NEW):**\n- **(A)** `bindTarget` dispatches `deployment.set_target` through `spendOffer` with the affordance the surface served — assert the wire was called with THAT affordance, not a constructed one. This is what proves there is no second decision path.\n- **(B) THE PAYLOAD IS THE DAEMON'S EXACT ROSTER** (`environment`, `network`, `sshTarget`, `url`) and nothing more.\n- **(C) BLANK `sshTarget` SENDS `null`, NOT `\"\"` AND NOT AN OMITTED KEY**, and the local path is reachable that way. An empty string is a destination the daemon would try to use.\n- **(D) THE OPERATOR'S INPUT IS SENT UNMODIFIED.** Feed a url carrying `user:password@` and assert the port dispatches it AS TYPED rather than stripping it — the daemon must be the one to refuse. **This arm looks backwards and that is the point:** a client that sanitised would submit a value nobody typed.\n- **(E) NO CREDENTIAL ON THE WIRE.** `JSON.stringify` the dispatched payload and assert no token shape; **positive control** over a literal containing one so the arm cannot pass vacuously.\n\n**`deploy-target-form.test.tsx` (NEW):**\n- **(F) THE ENVIRONMENT IS NAMED ON THE CONTROL AND ON THE CONFIRM STEP**, asserted by rendered text, not by prop. The row's safety property.\n- **(G) ALL FOUR REFUSALS RENDER VERBATIM WITH THEIR CODES** — userinfo url, and whitespace / metacharacter / newline / NUL in network and sshTarget.\n- **(H) `sshTarget` IS VISIBLY OPTIONAL** and its blank meaning (local docker) is stated in the rendered text.\n- **(I) NO CREDENTIAL INPUT EXISTS.** Assert no password/token field is rendered.\n\n**`goal-deployments.test.tsx` (EXISTS — add arms, rebase around peers):**\n- **(J)** With `DEPLOY_TARGET_MISSING`, the control is PRESENT and the old \"cannot bind one yet\" prose is GONE — assert the absence of that sentence by its text, so the retirement is pinned and cannot silently come back.\n- **(K)** With any other refusal, the binding control is absent. It belongs to the missing-target case only.\n\n---\n\n**ADDED BY AMENDMENT — THE PER-ENVIRONMENT OFFER CONTRACT (task-9aea412b).** The daemon now serves ONE `deployment.set_target` offer PER ENVIRONMENT, targeted at `deploy-target:<projectId>:<environment>` and carrying THAT aggregate's version. Two arms pin the consequence, and without them the row can ship a control that dispatches against the wrong environment's fence — which is exactly the defect that sent the producer row back for a re-plan.\n\n- **(L) THE RIGHT OFFER PER ROW, BY VALUE.** Serve THREE offers (preview, production, verify) at different versions and assert the production row's control spends the offer whose `targetAggregateId` is `deploy-target:<projectId>:production` — **by value, never by index, order or \"the first one\"**. Assert its `expectedVersion` is production's, not preview's. An arm that only checked the kind would pass while the control fenced the wrong aggregate.\n- **(M) THE PAYLOAD AGREES WITH THE OFFER BY CONSTRUCTION.** For each of two different rows, assert the dispatched payload's `environment` equals the environment in the spent offer's `targetAggregateId`. These are the two values whose disagreement the producer's design exists to make impossible; assert them EQUAL rather than each separately correct, or the arm stops testing the property.\n- **(N) NO OFFER FOR A ROW MEANS NO CONTROL FOR THAT ROW.** Serve offers for two environments only and assert the third row renders no binding control — absent, not disabled, and no client-minted affordance.\n- **(O) RETIRE THE NOW-FALSE COMMENT** at `goal-deployments.test.tsx:260-261`, which reads that the daemon offers no set_target affordance on any frame. It was true when written and is false once task-9aea412b lands. Its assertion may still stand; the sentence must not.\n\nRun this file against the REAL producer shape, not a hand-written frame invented here: build the fixture offers with `deployTargetAggregateId(projectId, environment)` so a change in the producer's key breaks these arms rather than silently passing them.",
          "reason": "Aligns the arms with the per-environment offer contract settled on task-9aea412b (plan re-submitted 2026-09-07): the daemon serves ONE deployment.set_target offer per environment at deploy-target:&lt;projectId&gt;:&lt;environment&gt;, so the payload's environment is derived from the offer spent, not typed. Adds two arms - the right offer is picked per row, and the payload's environment agrees with that offer's target BY CONSTRUCTION - plus retirement of the now-false comment at goal-deployments.test.tsx:261. Step 5 carried no prior amendment; arms A-K are preserved verbatim.",
          "amendedBy": "architect-dbe3631e",
          "amendedAt": "2026-09-07T00:12:22.834Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-6",
      "description": "DRILLS, GATES, SELF-REVIEW. Load `adversarial-self-review` and read your own diff as an attacker first.\n\n**(a) DRILLS.** `git status --porcelain` every path you will mutate and RECORD it. For EVERY path — tracked or not — copy it to an out-of-tree backup (%TEMP%, suffixed with your worker id). **Restore from that backup, never `git checkout --`:** a tracked file can hold a peer's uncommitted delivery. Prove each restore in a call SEPARATE from the restore — for a path that was CLEAN an empty `git diff` confirms it; **for a path already ` M` or `??`, sha256 against the backup is the ONLY admissible proof**, because there an empty `git diff` means you reverted the peer, and the reassuring output is byte-identical to the catastrophic one.\n\nFOUR MUTATIONS, each alone, the named arm confirmed RED, then restored:\n  1. Strip userinfo from the url before dispatching -> **(D) reds.** The headline drill: it introduces exactly the client-side sanitising the row forbids.\n  2. Send `\"\"` instead of `null` for a blank sshTarget -> **(C) reds.**\n  3. Remove the environment from the confirm step -> **(F) reds.** The safety-property drill.\n  4. Construct an affordance client-side instead of using the served one -> **(A) reds.** Proves the offer-wire discipline is pinned rather than assumed.\n\n**(b) GATES**, standalone and foreground, never piped, never `;`-chained: `pnpm --filter @moe/control-room test` and `pnpm typecheck`. Quote the `Test Files N passed` COUNT LINE — an exit-0 leg with no count line RAN NOTHING. **Root `pnpm test` covers no `apps/**` file and is not evidence for this row** — say that rather than quoting a bare root count line (DoD 7).\n\n**(c) ATTRIBUTE RED** by the merge-base/HEAD delta intersected with owned paths (global rail 3). Completion is permitted when that intersection is EMPTY and the owned legs are exit 0. Disclose foreign red verbatim.\n\n**(d) ADVERSARIAL PASS** — answer each in writing:\n- Could an operator bind a target to `production` believing they bound `staging`? Quote the rendered confirm text and argue it.\n- Does anything client-side modify, trim, normalise or repair what the operator typed before it reaches the daemon? Trace each of the four fields.\n- Can a credential reach the payload, a log line, or any rendered surface? Grep the diff.\n- Is the local-docker path reachable with sshTarget blank, end to end?\n- Did the offer get re-targeted to a goal aggregate anywhere? It must stay PROJECT-scoped — the binding is keyed `deploy-target:<projectId>:<environment>` (risk B).\n- Is the retired \"cannot bind one yet\" sentence gone from the shipped bundle, not just from the branch it renders on?\n\n**(e) PER-FILE CAP:** `wc -l` every production file. `goal-deployments.tsx` must be **250 or fewer** — report before and after; it was AT the cap before your diff. The new control under 250. Report `deploy-port.ts`'s delta.\n\n**(f)** DECLARE EVERY PATH in `complete_step.modifiedFiles`, then re-check `git status --porcelain` for anything created but unreported, including mutate-and-restore paths, which must show CLEAN.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "modifiedFiles": [
        "apps/control-room/src/v2/goals/deploy-port.ts",
        "apps/control-room/src/v2/goals/deploy-port.test.ts",
        "apps/control-room/src/v2/goals/deploy-target-form.tsx",
        "apps/control-room/src/v2/goals/deploy-target-form.test.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.tsx",
        "apps/control-room/src/v2/goals/goal-deployments.test.tsx",
        "apps/control-room/src/v2/goals/live-goal-deployments.tsx",
        "apps/control-room/src/v2/goals/work-labels.ts",
        "apps/control-room/src/v2/goals/work-labels.test.ts"
      ],
      "note": "FIVE DRILLS (four named plus one of my own), ALL RED ON THE NAMED ARM, ALL RESTORED BY SHA256. Gates EXIT 0.\n\n== (a) DRILLS ==\nALL NINE PATHS WERE ALREADY DIRTY BY MY OWN HAND (six ` M`, three `??`), so `git diff` is INADMISSIBLE as restore proof for every one of them -- an empty diff would mean I had reverted my own delivery, and that output is byte-identical to the reassuring one. Every path was copied to `%TEMP%\\moe-drill-worker-b1872f24\\` and every restore is proved by SHA256 in a call SEPARATE from the restore.\n\n| # | mutation | arm | result |\n|---|---|---|---|\n| 1 | strip userinfo from the url before dispatching | (D) | RED: `expected 'https://preview.example.test/app' to be 'https://deployer:hunter2@preview.exam...'` |\n| 2 | send `\"\"` instead of `null` for a blank sshTarget | (C) | RED: `expected '' to be null` |\n| 3 | remove the environment from the confirm sentence | (F) | RED: `expected 'This binds the environment to network...' to contain 'This binds the production environment...'` |\n| 4 | client-mint the affordance instead of spending the served one | (A) | RED: `expected { ...(6) } to be { ...(6) } // Object.is equality` |\n| 5 | **mine:** match the offer by `commandKind` ONLY, ignoring the constructed key | (L),(M),(N) | RED x3: `expected 'deploy-target:proj-1:preview' to be 'deploy-target:proj-1:production'` |\n\n**Drill 4's message is the interesting one:** `expected { ...(6) } to be { ...(6) }` -- a STRUCTURALLY IDENTICAL clone still fails. That is why the arm uses `toBe` and not `toEqual`; with `toEqual` the mutation would have shipped green.\n\n**Drill 5 is the one I added, and it is the row's core hazard.** Matching by kind alone binds `production` against `preview`'s offer and therefore against `preview`'s `expectedVersion` -- the exact EXPECTED_VERSION_CONFLICT class that sent the producer row back for a re-plan. Three arms catch it, not one.\n\nFINAL RESTORE PROOF, all nine, live vs out-of-tree backup:\n```\ndeploy-port.ts match=True 2B16FA6F...   deploy-port.test.ts match=True F0FF64CE...\ndeploy-target-form.tsx match=True 1AFC31E3...  deploy-target-form.test.tsx match=True B7A6FA47...\ngoal-deployments.tsx match=True FDD747DE...   goal-deployments.test.tsx match=True BEF1C30B...\nlive-goal-deployments.tsx match=True B8E38F67...  work-labels.ts match=True 3A7DF3FE...\nwork-labels.test.ts match=True 2CD21901...      MISMATCHES=0\n```\n\n== (b) GATES, standalone and foreground, never piped into a filter that could mask the status, never `;`-chained ==\n```\n$ pnpm --filter @moe/control-room test\n Test Files  190 passed (190)\n      Tests  2420 passed (2420)           CONTROL_ROOM_EXIT=0\n$ pnpm typecheck\napps/control-room typecheck$ tsc --project tsconfig.json   ... Done\napps/daemon typecheck$ tsc --project tsconfig.json         ... Done   TYPECHECK_EXIT=0\n$ pnpm exec vitest run packages/scheduler/src/package-boundary.test.ts\n Test Files  1 passed (1)      Tests  56 passed (56)        BOUNDARY_EXIT=0\n```\n**ROOT `pnpm test` WAS NOT RUN AND IS NOT QUOTED. Its vitest include carries no `apps/**`, so it executes ZERO of this row's files and would be evidence of nothing here** (DoD 7). The scheduler boundary scan IS quoted because it tokenizes every `.tsx` repo-wide and therefore does reach my new file; it must be invoked from the repo ROOT, since `pnpm --filter @moe/scheduler exec vitest run src/...` answers \"No test files found\" and exits 1.\n\n== (c) RED ATTRIBUTION ==\nThe owned legs are exit 0, so the (HEAD-failures minus merge-base-failures) set intersected with my owned paths is EMPTY. FOREIGN DIRT DISCLOSED AND UNTOUCHED -- a peer landed in this shared checkout mid-row: ` M apps/control-room/src/live/live-sessions.{ts,test.ts}`, ` M apps/control-room/src/v2/ops/activity-screens.test.tsx`, ` M apps/daemon/src/http/sessions-read.{ts,test.ts}`, ` M apps/daemon/src/orchestrator/{agent-wrapper-main,provider-pause-ledger}.ts`, plus six untracked `seat-start-*` files and the `.codex`/`.grok` configs. **They are GREEN in my runs** (the control-room legs include their test files), and I did not revert, stash, `git add -A` or commit any of them.\n\n== (d) ADVERSARIAL PASS ==\n**Could an operator bind `production` believing they bound `staging`?** No, and it is defended structurally rather than by wording alone. The environment is NOT typed -- the control renders per row and spends that row's own offer, and `bindTarget` REFUSES `DEPLOY_TARGET_OFFER_MISMATCH` if the served affordance is not `deployTargetAggregateId(projectId, environment)`. Rendered confirm, verbatim: *\"This binds the production environment to network moe-prod on the docker daemon of this host with no public url. Only production changes.\"* The button reads `Confirm: bind production to moe-prod`, cancel reads `Do not bind production`. The arm also asserts the INVERSE -- while rendering `production`, the root contains neither \"preview\" nor \"verify\".\n\n**Does anything client-side modify what was typed?** Traced all four. `environment`: comes from the row, n",
      "startedAt": "2026-09-07T08:12:35.566Z",
      "completedAt": "2026-09-07T08:18:24.141Z"
    }
  ],
  "status": "REVIEW",
  "assignedWorkerId": "qa-97200064",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "ARCHITECT",
  "parentTaskId": null,
  "dependsOn": [
    "task-358b6ec8ee884ca6b6bc96bbf1f54d84",
    "task-9aea412b10d141c084c9bfdb04c33a21"
  ],
  "priority": "HIGH",
  "order": 91.88,
  "comments": [
    {
      "id": "comment-f2efba7e38864559a67f55816c34540b",
      "author": "architect-fa3a3e17",
      "content": "THE MISSING PIECE IS THE AFFORDANCE OFFER, NOT THE CONTROL - measured, and the card already documents it (architect-fa3a3e17, 2026-09-06). Carried from a duplicate row I filed in error and archived (task-9aea412b).\n\n`apps/control-room/src/v2/goals/goal-deployments.tsx:189-192` already refuses to render a binding control and says why in its own source:\n  // set_target affordance is offered, so a control here would have none to spend.\n  `Bind a target for ${view.environment} first, with deployment.set_target.`\n  + \" This screen cannot bind one yet, so it has to be bound outside the\"\nand `apps/control-room/src/v2/goals/deploy-port.ts:29` records the same measurement - a grep for `set_target` in the control room EXCLUDING TESTS returns zero, so a `deployment.set_target` dispatch has no affordance to spend.\n\nSo the browser is not missing a button by oversight; it is correctly declining to show one that could not be spent. If this row builds the control without the offer landing first, the control is a dead button - which is the exact hazard the consumer's DoD calls out (\"ABSENT rather than dead\").\n\nTHE PRODUCER IS THE SAME SHAPE AS task-2cedb26a598540bd9bab17046592a7bb (the `deployment.deploy` per-goal offer, planned today). Same generic emission at affordance-read.ts:282-284, same `aggregateIdFor` fallthrough in bootstrap-sequence.ts that targets the PROJECT rather than the goal, same requirement to assert the target id BY VALUE against what the card matches. Read that plan before writing this one.\n\nONE GRANULARITY DIFFERENCE WORTH DECIDING DELIBERATELY: `deployment.deploy` has payload keys `[\"environment\",\"sha\"]`, so its environment is chosen at dispatch from ONE goal-scoped offer. `deployment.set_target` has `[\"environment\",\"network\",\"sshTarget\",\"url\"]` (daemon-command-payload-keys.ts:165) and the durable target is keyed `deploy-target:<projectId>:<environment>` (deploy-target-contracts.ts:84) - binding is a per-(project, environment) act. Whether the offer is goal-scoped with the environment in the payload, or one per environment, is a real choice; state it at the offer site rather than leaving the browser to infer it.\n\nAnd note the withholding rule is NOT the deploy offer's inverted: an operator needs set_target precisely when nothing is bound, and REBINDING an already-bound environment is legitimate.",
      "timestamp": "2026-09-06T14:05:20.825Z"
    },
    {
      "id": "comment-45fc4ed3719f4b0f92512218e8ce0f93",
      "author": "architect-aa54e231",
      "content": "CONTRACT ALIGNMENT after task-9aea412b's re-plan (governor direction 17:23Z: amend this row, do not re-plan it). step-2 now carries amend-1 with the new contract. TWO OTHER STEPS ARE AFFECTED and are NOT amended - read this before starting either.\n\nWHAT CHANGED IN THE PRODUCER. The `deployment.set_target` offer is now ONE PER ENVIRONMENT, each targeted at `deploy-target:<projectId>:<environment>` - the setter's ACTUAL write aggregate - and carrying THAT aggregate's `expectedVersion`. It was going to be one goal-scoped offer with the environment in the payload; that design is dead.\n\nWHY, so it is not re-litigated. worker-f78e0703's live probe (comment-96a950d1 on task-9aea412b, EXIT 0, asserts the refusal) measured that after a real preview binding `versionOf(ledger, \"deploy:goal-1\")` is 0 while `deploy-target:project-1:preview` is 1, so a rebind at the offered version refuses EXPECTED_VERSION_CONFLICT@DURABLE_STORE. And `NextAllowedCommand` (packages/contracts/src/runtime/runtime-affordance.ts:18-28) carries a SINGLE scalar `expectedVersion` with closed required/optional key rosters, so one offer physically cannot carry a version per environment. Per-environment offers make the fenced aggregate and the offered version the same object - correct by construction rather than by coincidence.\n\nSTEP 1 (the probe): your gating question \"does the surface serve a set_target offer?\" now has a sharper answer to look for. Do not stop at \"an offer exists\" - the project-targeted offer ALREADY existed at HEAD and was never spendable (the probe measured its expectedVersion as 3 while the successful bind used 0). task-9aea412b also REMOVES that one from the generic emission. So the check is: is there an offer whose `targetAggregateId` is `deploy-target:<projectId>:<environment>`, and does its `expectedVersion` equal that aggregate's version? If you find a project-targeted set_target offer still present, the producer has not fully landed - report rather than spending it.\n\nSTEP 3 (the control, already carrying amend-1 from a previous change): its FOUR INPUTS were environment, network, sshTarget, url. THE ENVIRONMENT IS NO LONGER FREE INPUT for a rebind - it comes from the offer being spent, per step-2's amendment. Rework the control so the operator SELECTS which environment's offer to spend (or the card renders one control per offered environment, matching how the offers arrive) and types only network, sshTarget and url. Keep the \"environment named unmistakably on the control\" requirement that step 3 already carries - it matters more now, because the operator is choosing among several.\n\nTHE FIRST-BIND HOLE, disclosed rather than hidden. Per-environment offers can only be minted for environments the durable record already knows about, and `admitEnvironmentName` is a regex with no closed roster - so an operator with ZERO configured environments may have no offer to spend and no way to create the first binding from the browser. task-9aea412b's step 2 makes its worker measure and decide that explicitly. If it lands with the hole open, this row's control cannot create a first binding either, and that is a gap to report on the parent - NOT something to work around by minting an affordance client-side or letting the operator type an environment the offer does not name. `spendOffer` builds its envelope FROM the affordance; there is no client-side mint, and inventing one would be a second decision wire.\n\nNOTHING ELSE IN THIS ROW CHANGES: the one-wire rule, the exact four-key payload roster, `sshTarget: null` meaning local docker, send-unmodified, no restated admission grammar, the rostered layer constant, and the 250-line cap on goal-deployments.tsx all stand.",
      "timestamp": "2026-09-06T20:02:04.970Z"
    },
    {
      "id": "comment-9f360455fa494ab6ac044428ae79fa17",
      "author": "architect-dbe3631e",
      "content": "CONSUMER ALIGNMENT + ONE DISCLOSURE (architect-dbe3631e, 2026-09-07).\n\nFIRST, THE DISCLOSURE, because it affects what you may have read. I amended step-3 TWICE in a row. amend-2 was composed from the step's BASE description and therefore DROPPED architect-b63fabf3's amend-1 (the governor's 14:48Z MISSING_TOKENS clause and its two extra files). amend-3 restores that clause and carries both. If you read step-3 between those two writes, RE-READ IT - amend-3 is the effective text. Cause, worth knowing for anyone reading a task JSON directly: amendment entries are keyed `amendmentId`, NOT `id`, so a reader doing amendments.find(a => a.id === activeAmendmentId) silently returns undefined and falls back to the superseded base description. activeAmendmentId on disk is the authority.\n\nSECOND, WHY THE AMENDMENTS. worker-f72a1438's comment on task-9aea412b said this row's step 2 was \"already amended\" to derive the payload environment from the offer target. That was NOT true - every step here was unamended when I read it. It is true now, for step-3 and step-5.\n\nTHE PRODUCER CONTRACT, settled and re-planned on task-9aea412b 2026-09-07 (your dependsOn already names it): the daemon serves ONE `deployment.set_target` offer PER ENVIRONMENT, each with targetAggregateId = `deploy-target:<projectId>:<environment>` and expectedVersion read from THAT aggregate. NOT one goal-scoped offer with the environment chosen at dispatch - that design was falsified by a live probe (a rebind at the goal-scoped version refused EXPECTED_VERSION_CONFLICT@DURABLE_STORE, because the setter fences the per-environment aggregate). The names come from the closed ENVIRONMENT_NAMES roster (preview/production/verify) unioned with any environment already bound durably, so an unbound environment IS offered at expectedVersion 0 and the FIRST bind from the browser works.\n\nWHAT THAT MEANS HERE, and it is the reason step-3 changed: THE ENVIRONMENT IS NOT AN INPUT, IT IS THE OFFER YOU SPEND. Render the binding control on the environment ROW this card already renders, and spend the offer whose targetAggregateId equals deployTargetAggregateId(projectId, thatRowsEnvironment). MATCH BY CONSTRUCTING THE KEY, NEVER BY PARSING IT - do not split the id on `:` to recover the environment, because a projectId containing a colon makes that wrong silently, in the direction that binds the wrong host. The payload still carries all four declared keys; `environment` is supplied by the row, so the payload and the CAS-fenced aggregate agree BY CONSTRUCTION rather than by convention. No offer for a row means no control for that row - absent, not disabled, and never a client-minted affordance.\n\nStep-5 gains four arms for that: (L) the right offer per row asserted BY VALUE on targetAggregateId and expectedVersion, never by index or order; (M) the dispatched payload's environment asserted EQUAL to the environment in the spent offer's target, for two different rows; (N) a row with no offer renders no control; (O) retire the now-false comment at goal-deployments.test.tsx:260-261 (\"the daemon offers no set_target affordance on any frame\") - its assertion may stand, the sentence must not. Build fixture offers with deployTargetAggregateId(projectId, environment) so a producer key change breaks these arms instead of passing them silently.\n\nYour step-1 probe will now FIND offers rather than confirm their absence - expect three, at expectedVersion 0 on a fresh world. Steps 1, 2, 4 and 6 are unchanged and still correct as written. If anything in this row still reads as if the environment is typed by the operator, stop and report rather than building to it.",
      "timestamp": "2026-09-07T00:13:13.852Z"
    },
    {
      "id": "comment-68fdc05826924518ad2ac8129be96794",
      "author": "worker-767c9887",
      "content": "FROM task-9aea412b (the producer row you depend on) — worker-767c9887, 2026-09-07.\n\nTWO PIECES OF TEXT IN YOUR FILES GO STALE THE MOMENT MY ROW LANDS. I did NOT edit either — you own those files. Retire them rather than building to them.\n\n1. apps/control-room/src/v2/goals/goal-deployments.test.tsx:260-262\n   \"// AND THE HONEST LIMIT. The daemon offers no set_target affordance on any frame, so this\n    screen cannot bind one.\"\n   FALSE after my row: the daemon now offers ONE `deployment.set_target` per environment on every\n   surface read whose `deployment.set_target` chain step is not BLOCKED. The ASSERTIONS in that\n   test stay green (they pin the card's own note text, not the daemon roster), so nothing reds —\n   which is exactly why the stale comment is dangerous: it reads as a live measurement.\n\n2. apps/control-room/src/v2/goals/deploy-port.ts:29\n   \"excluding tests returns zero. So a `deployment.set_target` dispatch has no affordance to [spend]\"\n   Same: the count is no longer zero.\n\nTHE CONTRACT YOUR CARD MUST BUILD TO (this is the part that changed since your plan was written):\n- Granularity is ONE OFFER PER ENVIRONMENT, not one goal-scoped offer with the environment chosen\n  at dispatch. Confirmed by architect-dbe3631e's re-plan and by my live arms.\n- `targetAggregateId` === `deployTargetAggregateId(projectId, environment)` ===\n  `deploy-target:<projectId>:<environment>`, and `expectedVersion` is THAT aggregate's own version\n  (0 before the first bind, N after N binds). Per-environment versions move INDEPENDENTLY.\n- MATCH BY CONSTRUCTION: build `deployTargetAggregateId(projectId, environment)` for the row you\n  are rendering and find the offer with that exact `targetAggregateId`. Do NOT parse the\n  environment back out of the id on `:` — projectIds can contain separators and the payload's\n  `environment` key must agree with the fenced aggregate by construction, not by convention.\n- The payload keys are exactly [\"environment\",\"network\",\"sshTarget\",\"url\"]\n  (daemon-command-payload-keys.ts:181). `sshTarget` and `url` are each nullable.\n- REBINDING IS LEGITIMATE and still offered: an already-bound environment KEEPS its offer, at the\n  bumped version. Do not hide the control once a target exists.\n- WITHHELD ONLY by the bootstrap prerequisite `project.activate`. When it is unmet there are ZERO\n  set_target offers and ONE chain step `{kind:\"deployment.set_target\", status:\"BLOCKED\",\n  missing:[\"project.activate\"]}`. Nothing else withholds it — in particular bound-ness never does.\n- The old PROJECT-targeted `deployment.set_target` offer is GONE (excluded from the generic\n  emission). If your card matched `targetAggregateId === projectId` it will now find nothing.\n\nProducer evidence: apps/daemon/src/http/affordance-deploy-target-offers.ts (resolver + the written\ngranularity decision) and the wiring in affordance-read.ts; arms in affordance-read.test.ts under\n`describe(\"deployment.set_target is offered per environment (task-9aea412b)\")`, including a REAL\nfirst bind and a REAL rebind dispatched through the shipped handler table.",
      "timestamp": "2026-09-07T03:16:13.951Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-06T13:54:35.362Z",
  "updatedAt": "2026-09-07T08:20:14.532Z",
  "metrics": {
    "firstClaimAt": "2026-09-06T14:00:23.554Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 6
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 20 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-06T14:04:28.127Z",
  "planningNotes": {
    "approachesConsidered": "**THE ROW TURNS ON ONE QUESTION I COULD NOT SETTLE BY GREP, so step 1 probes it and blocks if the answer is no.** `spendOffer` (offer-wire.ts:62-78) takes an `affordance` and builds the envelope FROM it — no offer, nothing to spend. And `goal-deployments.tsx:187-193` ships a comment saying: *\"no set_target affordance is offered, so a control here would have none to spend.\"* If that is still true, this row cannot be built as scoped and needs an offer producer first.\n\n**BUT THE COMMENT LOOKS STALE, AND THE MEASUREMENT SAYS SO.** `affordance-read.ts:282-283` withholds a chain offer for EXACTLY four kinds — `plan.propose`, `approval.decide`, `goal.close`, `repository.publish` — and emits for every other member of `BOOTSTRAP_COMMAND_KINDS`. `deployment.set_target` is a member and is NOT on that list, so the generic path should emit it, targeted at the PROJECT via `aggregateIdFor`'s `default: return subject ?? request.projectId`.\n\n**AND PROJECT-TARGETING IS CORRECT HERE, which is the interesting part.** The sibling row task-2cedb26a exists because the DEPLOY offer is project-targeted while the card needs `deploy:<goalId>` — a real mismatch. `set_target` is the opposite case: the binding is keyed `deploy-target:<projectId>:<environment>` (deploy-target-contracts.ts:84), so a PROJECT-scoped offer matches the command's own granularity exactly. **Do not \"fix\" this offer to be goal-scoped by analogy with its sibling.** That would be a defect introduced by pattern-matching, and it is the likeliest mistake on this row because the two sit side by side.\n\nWhy a probe rather than a grep: whether an offer is EMITTED depends on runtime state (the kind must be ready and uncommitted, and `deployment.set_target` requires `project.activate`). A source read cannot answer it. The comment was probably written when that prerequisite was unmet on the author's fixture, not because the path is missing.\n\n**WHERE THE CONTROL LIVES.** `goal-deployments.tsx` is EXACTLY 250 lines — at the cap, not near it — so the control is a NEW module and that file's edit swaps its honest-limit paragraph for the control, which should be net-neutral or smaller. `deploy-port.ts` is 56 lines and exposes only `submit`; `bindTarget` belongs beside it, following `preview-port.ts:72` and `goal-close-port.ts:32`.",
    "codebaseInsights": "MEASURED AT HEAD (moe/work-2026-09-04).\n\n1. **`spendOffer` REQUIRES AN AFFORDANCE.** `offer-wire.ts:62-78`: `spendOffer(wire, kind, affordance, payload, correlationPrefix, layer)`. It looks the builder up by kind (`OFFER_KIND_UNBUILDABLE` if absent), then calls `builder(affordance, {correlationId, payload, requestDigest, sessionCredential})`. The affordance IS the offer the daemon served; no path mints one client-side, by design.\n\n2. **THE CALL-SITE PRECEDENTS**, all one-liners returning `spendOffer(...)`: `preview-port.ts:72`, `goal-close-port.ts:32`, `escalation-port.ts:41`, `criterion-evidence-port.ts:13`. Each passes its own kind constant, its own layer constant and a `ui-<verb>` correlation prefix. `preview-port.ts:10` documents the rule — \"One wire, `spendOffer`, shared with every other inline decision\".\n\n3. **THE WITHHELD LIST IS FOUR KINDS.** `affordance-read.ts:282-283` skips `plan.propose`, `approval.decide`, `goal.close` and `repository.publish`, and emits for every other `BOOTSTRAP_COMMAND_KINDS` member. `deployment.set_target` is a member and is not withheld.\n\n4. **THE COMMAND IS LANDED** by task-358b6ec8: payload roster `[\"environment\", \"network\", \"sshTarget\", \"url\"]` (daemon-command-payload-keys.ts:172), admission in `deploy-target-contracts.ts` — `admitDeployUrl` (:61), `admitDeployNetwork` (:74), `admitSshTarget` (:78), aggregate at `:84`. `admitDeployUrl` REFUSES a `user:password@` url outright rather than stripping it, deliberately: stripping leaves the operator believing the environment answers something it does not.\n\n5. **`DeployTarget.sshTarget` IS `string | null`** and `deploy-ports.ts` documents null as \"a local docker daemon\", so the field is genuinely optional and the local path must stay reachable with it empty.\n\n6. **SIZES:** `goal-deployments.tsx` is EXACTLY 250 — at the cap. `deploy-port.ts` is 56 and has room. The honest-limit paragraph is at `:187-193`. `goal-deployments.test.tsx` already exists; `deploy-port.test.ts` and `deploy-target-form.test.tsx` do not.\n\n7. **THE PREREQUISITE CHAIN:** `deployment.set_target` requires `project.activate` (bootstrap-sequence.ts:46), which requires `project.bind_repository` and `provider.probe`. A probe fixture must satisfy those or it sees no offer and reaches the wrong conclusion.",
    "risks": "A. **THE ROW IS UNBUILDABLE IF NO `deployment.set_target` OFFER IS SERVED.** `spendOffer` builds from the affordance; there is no client-side mint. Step 1 probes a real surface read with the prerequisites met. If nothing is emitted, `moe.report_blocked` asking for an offer producer — do NOT fabricate an affordance or dispatch around the wire. That would be a second decision path and exactly the design regression the consumer's rail 3 forbids.\n\nB. **DO NOT MAKE THIS OFFER GOAL-SCOPED BY ANALOGY WITH ITS SIBLING.** task-2cedb26a re-targets the DEPLOY offer to `deploy:<goalId>` because the card needs per-goal identity. `set_target` binds per `(project, environment)`, so a PROJECT-targeted offer is correct. Copying the sibling's fix here would break the command's own granularity, and the two rows sitting side by side makes it the likeliest mistake.\n\nC. **THE ENVIRONMENT IS THE SAFETY PROPERTY.** A target bound to the wrong environment silently redirects a later deploy to another host, and nothing downstream can tell it was a slip — the daemon stores exactly what it was given. Name the environment on the control AND on its confirm step.\n\nD. **SURFACE THE REFUSAL, NEVER SANITISE.** The daemon refuses a userinfo url rather than stripping it, on purpose. A control that cleaned input and submitted would send a value the operator never typed and report success for it. Render what the daemon refuses; do not pre-judge or repair input client-side.\n\nE. **NO CREDENTIAL FIELD AT ALL.** Not a password, not a token, not a key. `sshTarget` is a destination, not an authenticator. Assert the dispatched payload and every rendered surface carry no token shape and no userinfo url, with a positive control so the assertion cannot pass vacuously (epic rail 3).\n\nF. **`goal-deployments.tsx` IS AT 250 — THE CAP, NOT NEAR IT.** The control is a NEW module; that file's edit swaps its honest-limit paragraph for the control mount and must be net-neutral or smaller. Do not grow it, do not reformat it, do not extract anything unrelated — rows are queued behind it.\n\nG. **ASCII-ONLY, APOSTROPHE-FREE JSX.** The scheduler package-boundary scan reds on an apostrophe in JSX text. Write \"cannot\" rather than a contraction, and confirm any new testid namespace is free before using it.\n\nH. **`sshTarget` EMPTY MEANS LOCAL.** A control that required it would make local docker deploys unreachable — the field is `string | null` by design.",
    "keyFiles": [
      "apps/control-room/src/v2/approvals/offer-wire.ts",
      "apps/control-room/src/v2/approvals/preview-port.ts",
      "apps/control-room/src/v2/goals/deploy-port.ts",
      "apps/control-room/src/v2/goals/goal-deployments.tsx",
      "apps/daemon/src/deployment/deploy-target-contracts.ts",
      "apps/daemon/src/http/affordance-read.ts",
      "apps/daemon/src/bootstrap/bootstrap-sequence.ts"
    ]
  },
  "planApprovedAt": "2026-09-06T14:04:30.241Z",
  "contextFetchedBy": [
    "worker-b1872f24",
    "qa-97200064"
  ],
  "workStartedAt": "2026-09-07T07:49:01.952Z",
  "reviewStartedAt": "2026-09-07T08:19:24.463Z",
  "verification": {
    "command": "pnpm --filter @moe/control-room test",
    "exitCode": 0,
    "outputTail": " Test Files  190 passed (190)\n      Tests  2420 passed (2420)\n   Start at  11:18:32\n   Duration  15.12s (transform 35.02s, setup 0ms, import 73.27s, tests 115.65s, environment 186.75s)\nEXIT=0",
    "reportedAt": "2026-09-07T08:19:24.463Z"
  },
  "filesModified": [
    "apps/control-room/src/v2/goals/deploy-port.ts",
    "apps/control-room/src/v2/goals/live-goal-deployments.tsx",
    "apps/control-room/src/v2/goals/goal-deployments.test.tsx",
    "apps/control-room/src/v2/goals/deploy-target-form.tsx",
    "apps/control-room/src/v2/goals/work-labels.ts",
    "apps/control-room/src/v2/goals/work-labels.test.ts",
    "apps/control-room/src/v2/goals/goal-deployments.tsx",
    "apps/control-room/src/v2/goals/deploy-port.test.ts",
    "apps/control-room/src/v2/goals/deploy-target-form.test.tsx"
  ],
  "completionSummary": "An operator binds a deployment target from the browser. `DeployTargetForm` (NEW, 210 lines) dispatches `deployment.set_target` through offer-wire `spendOffer` via `DeployPort.bindTarget` -- ONE decision wire, the same one `submit` uses.\n\nTHE PLAN'S OFFER-SCOPE PREMISE WAS STALE AND I BUILT AGAINST MEASURED STATE. Step 1(c) said the offer is PROJECT-targeted and told me not to re-target it. Sibling task-9aea412b has since landed `affordance-deploy-target-offers.ts`: ONE OFFER PER ENVIRONMENT at `deploy-target:<projectId>:<environment>`, each fenced at that aggregate's own version. Building to the plan's literal words would have matched an offer that is never emitted -- the landed arm `affordance-read.test.ts:1451` asserts `targetAggregateId === PROJECT` returns `[]`. NOT the risk-B mistake: the axis is per-(project, environment), never goal-scoped.\n\nSTEP 2's AMENDMENT (\"derive environment from the offer\") CONFLICTED WITH THE PRODUCER (\"must NOT parse the environment back out of the id\"). Both want one property. I did CONSTRUCT + VERIFY, stronger than either: the port builds `deployTargetAggregateId(projectId, environment)` and refuses `DEPLOY_TARGET_OFFER_MISMATCH` @ `CONTROL_ROOM_DEPLOY` if the served affordance does not equal it. Load-bearing, not belt-and-braces: `setDeployTarget` keys its WRITE off the payload and its FENCE off the offer, so a mismatched pair does not refuse -- it binds one environment against another's version.\n\nDoD 1 offer-wire, no second path. DoD 2 environment named in heading, 3 labels, arm button, confirm SENTENCE (\"...Only production changes.\"), cancel, success and refusal; never typed, always verified. DoD 3 all four refusals plus NUL render as words with `DEPLOY_TARGET_INVALID @ DAEMON_INGRESS` verbatim -- ONE daemon code, not four invented ones. DoD 4 three inputs, no password/token/key, positive controls both sides. DoD 5 blank ssh sends `null` (not `\"\"`, key present), local path asserted reachable. DoD 6 ASCII-only, apostrophes only i"
}