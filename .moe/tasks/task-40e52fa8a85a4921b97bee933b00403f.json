{
  "id": "task-40e52fa8a85a4921b97bee933b00403f",
  "epicId": "epic-8ebe64173d934f72a339c32bc2eaaf7d",
  "title": "Publish preview.decide as a human-only kind with its refusal codes",
  "description": "SPIDR child 1 of task-f3edb8fb39ad42ce8980dae7d62b604c (architect-20a6d1eb, 2026-09-05). Dependency-free LEAF; children 2 and 3 build on it.\n\nWHAT THIS ROW OWNS: the vocabulary half only - `preview.decide` published as an operator-only command kind with its full roster backfill and its four stable refusal codes. It runs no process, captures no screenshot and renders nothing. Those are children 2 and 3.\n\nMEASURED AT HEAD df5bbfd5 (branch moe/work-2026-09-04) by architect-20a6d1eb - the gap is REAL in both directions:\n- `grep -rn 'preview.decide|preview.start|PREVIEW_COMMAND_MISSING|moe-preview-receipt' --include=*.ts apps/ packages/` = ZERO hits. Nothing to duplicate.\n- The existing `apps/daemon/src/graph-preview-request.ts` and `apps/control-room/src/preview/` are PLAN/document previews, not the product preview. Do not extend them.\n\nTHE ROSTER, enumerated by tracing an existing human-only decide kind (`escalation.decide`) across the tree - these are the DAEMON-side files it touches, and the ones this row owns:\n  apps/daemon/src/daemon-command-vocabulary.ts (kinds, capability, family table, PAYLOAD_KEYS, OPERATOR_PRINCIPAL_KINDS)\n  apps/daemon/src/daemon-command-registry.ts and daemon-command-families.ts\n  apps/daemon/src/daemon-v2-command-registry.ts\n  apps/daemon/src/mcp-tool-allowlist.ts (EXCLUDED - operator only)\n  packages/contracts/src/runtime/runtime-vocabulary.ts\n  packages/control-room-client/src/generated/generated-client.ts and every digest mirror (memory generated-contract-digest-has-four-mirrors)\n\nTWO HAZARDS MEASURED AT PLAN TIME, both of which have bitten this board before:\n1. `HUMAN_ONLY_STEPS` IS DEFINED TWICE, byte-for-byte: apps/daemon/src/orchestrator/agent-spawn-contract.ts:194 AND apps/daemon/src/orchestrator/agent-wrapper.ts:116. Nothing found asserts the two agree. A new human-only kind added to only one of them leaves the wrapper free to staff an operator gate - exactly the class of defect that minted 8 junk goals live (see the comment at agent-wrapper.ts:117-123). Both must be updated and the row must add the assertion that they are equal.\n2. Roster tests must be BIDIRECTIONAL set-equality (global rail 9): enumerate the SERVED set from the dispatch seam, not from the roster constant, or deleting an entry shrinks the iteration and stays green.",
  "definitionOfDone": [
    "`preview.decide` is published across the full roster - vocabulary (kind, capability, family, PAYLOAD_KEYS, operator-principal), registry, families, v2 registry, contracts runtime vocabulary, generated client and every digest mirror. A bidirectional set-equality test enumerates the SERVED kinds from the dispatch seam and asserts they equal the advertised roster in BOTH directions (global rail 9); deleting the entry from either side must turn it red.",
    "It is OPERATOR-ONLY and MCP-EXCLUDED: `preview.decide` appears in the MCP allowlist's exclusion set, and a test asserts an agent-authenticated dispatch is refused while an operator-authenticated one reaches the handler - asserting the refusal code and layer, not merely that it failed.",
    "It is in BOTH HUMAN_ONLY_STEPS rosters (agent-spawn-contract.ts:194 and agent-wrapper.ts:116), AND a new test asserts the two sets are EQUAL to each other. That equality test is this row's most valuable artifact: the duplication exists today with nothing guarding it. Verify by deleting the entry from one roster only and confirming the test goes red.",
    "The four refusal codes are defined with a closed code->layer map so a call site cannot mint a code and layer that disagree: PREVIEW_COMMAND_MISSING, PREVIEW_START_TIMEOUT, PREVIEW_GOAL_NOT_LANDED, PREVIEW_DECISION_INVALID. A test asserts each code's layer. Codes whose CONDITIONS belong to the runner (child 2) are defined here but may be exercised there - say so in the completion record rather than writing a fake condition to reach them.",
    "The payload contract is exact-arity and refuses an unknown key rather than ignoring it: APPROVE carries no findings; REJECT carries findings naming the nodes to rework. A test asserts PREVIEW_DECISION_INVALID + layer for a malformed decision, for an unknown extra key, and for REJECT with empty findings.",
    "Gates quoted with their `Test Files N passed` count lines: `pnpm --filter @moe/daemon test`, `pnpm typecheck`, `pnpm test` (packages/contracts is touched, so the root lane is real evidence here)."
  ],
  "taskRails": [
    "VOCABULARY ONLY. This row starts no process, captures no image, writes no receipt and renders no UI. If a step needs a running preview to be meaningful, it belongs to child 2 - say so and move on rather than stubbing a runner here.",
    "HUMAN GATES ARE HUMAN-ONLY (inherited): operator-only, MCP-excluded, in BOTH HUMAN_ONLY_STEPS rosters, every refusal carrying a stable code asserted with its layer.",
    "FEATURES ONLY (inherited): no security-boundary or policy work.",
    "A new daemon command follows the 8-touchpoint + .js bridge pattern; a missing `.js` bridge kills the daemon at boot with ERR_MODULE_NOT_FOUND (memory ui-connect-everything-built-2026-09-02). Never commit a generated index.js."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "VERIFY AND RE-MEASURE, NO EDITS. NARROWED SCOPE: this row now DEFINES the contract and wires it into the daemon's OWN roster only. Publishing it on the shared surface (MCP exclusion, packages/contracts, the regenerated client) and adding it to the HUMAN_ONLY_STEPS rosters moved to task-5f883e4e12c34842b3cb6c2f218e4256 when this plan measured 12 files against the 10-file cap. Do not do that work here. (a) Confirm the guard row task-8834ebe68c1a4f81b3be8e7581156755 landed. (b) Confirm the gap: `grep -rn 'preview\\.decide|PREVIEW_COMMAND_MISSING' --include=*.ts apps/ packages/` returns ZERO. (c) TRACE THE ROSTER BEFORE EDITING, recording the exact line of each touchpoint, using `escalation.decide` as the worked example: daemon-command-vocabulary.ts capability (:81 area) and PAYLOAD_KEYS (:288 area); daemon-command-families.ts per-kind flag block (:83-94); daemon-command-registry.ts `entryOf` (:205-212, which resolves asyncEntries first and then destructures family flags). If the trace names a file outside this plan's seven, STOP and report — there is no headroom.",
      "status": "COMPLETED",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.ts",
        "apps/daemon/src/daemon-command-families.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ],
      "note": "MEASURE-ONLY, NO EDITS. Read step-1 via activeAmendmentId=amend-1 (global rail 10); all clauses below cite the AMENDED text.\n\n(a) AMENDED CLAUSE VERIFIED. amend-1 replaces the guard-row prerequisite with \"no dependsOn, nothing to wait for\". Independently confirmed: task-8834ebe6 TASK_CREATED 2026-09-04T23:04:30Z, TASK_DELETED 23:08:29Z (.moe/activity.log); no .moe/tasks file; `git log moe/work-2026-09-04 --grep 'Moe-Task: task-8834ebe6'` = 0 hits. task.dependsOn=[]. amend-1's own premise re-measured and TRUE: `grep -rn 'HUMAN_ONLY_STEPS: ReadonlySet|HUMAN_ONLY_STEPS =' --include=*.ts apps/daemon/src` = exactly ONE hit, agent-spawn-contract.ts:195; agent-wrapper.ts:24 is `export { HUMAN_ONLY_STEPS } from \"./agent-spawn-contract.js\"` (re-export, comment at :23). No duplication. Not blocking on it.\n\n(b) GAP CONFIRMED ZERO. `grep -rn 'preview\\.decide|PREVIEW_COMMAND_MISSING|PREVIEW_START_TIMEOUT|PREVIEW_GOAL_NOT_LANDED|PREVIEW_DECISION_INVALID' --include=*.ts apps/ packages/` = 0 hits. `apps/daemon/src/preview/` does not exist.\n\n(c) ROSTER TRACE, exact lines, `escalation.decide` as worked example:\n- daemon-command-vocabulary.ts:106 REVIEW_FAMILY capability (NOT :81 - :81 is inside the header comment); PAYLOAD_KEYS entry :288; WiredCommandKind union :160-176; OPERATOR_PRINCIPAL_KINDS :337; PAYLOAD_KEYS decl :256.\n- daemon-command-families.ts: per-kind flag block confirmed at :83-94 region; `escalation.decide` has no literal there (it rides REVIEW_FAMILY via familyCapabilityOf).\n- daemon-command-registry.ts:205 `entryOf` resolves asyncEntries[kind] first (:209), then destructures commandFamilyFacts(kind) (:214). SERVED SET IS DERIVED: :392 `buildCommandRegistry((Object.keys(PAYLOAD_KEYS) as readonly WiredCommandKind[]).map(entryOf))`.\n- daemon-command-vocabulary.test.ts: VocabularyRow.kind typed WiredCommandKind (:31); ROWS :45; sweeps :244-245 (`Set(ROWS.map(kind)).size === 46`, `Object.keys(PAYLOAD_KEYS)` toEqual ROWS kinds); OPERATOR_ONLY :221 typed WiredCommandKind; :328-329 `OPERATOR_ONLY toHaveLength(11)` + `OPERATOR_PRINCIPAL_KINDS.size toBe(11)`.\n- daemon-command-registry.test.ts: Row.kind typed **RuntimeCommandKind** (:75); ROWS :91; REGISTRATION_ORDER :280 typed **readonly RuntimeCommandKind[]**; OPERATOR_ONLY :306 same; sweeps :814 (`[...registry.keys()].sort()` toEqual ROWS kinds) and :820-821 (`toHaveLength(46)`, `[...registry.keys()]` toEqual REGISTRATION_ORDER); `send` :363 takes RuntimeCommandKind and dispatches via handleCommandRequest.\n\nSTOP-AND-REPORT TRIGGERED. Clause (c): \"If the trace names a file outside this plan's seven, STOP and report - there is no headroom.\" The trace names TWO.\n\n1. apps/daemon/src/daemon-store-dependencies.test.ts - UNNAMED 8th FILE, hard runtime red. :670 builds `registryKinds: [...deps.registry.keys()].sort()` and :745 compares it to a hand-written 46-kind roster whose own comment reads \"A new command writes its own kind here.\" Adding preview.decide to PAYLOAD_KEYS reds this file. Invisible to a grep for the kind literal in production source (global rail 19 class).\n\n2. packages/contracts/src/runtime/runtime-vocabulary.ts - EXPLICITLY OUT OF SCOPE per amend-1, yet REQUIRED twice:\n   (i) TYPECHECK. Probe run and deleted in the same command: `apps/daemon/src/zz-probe-worker56ac.ts` = `export const PROBE: readonly RuntimeCommandKind[] = [\"preview.decide\"];` -> `npx tsc --noEmit -p apps/daemon/tsconfig.json` gives `error TS2322: Type '\"preview.decide\"' is not assignable to type '\"approval.decide\" | ... 95 more ... | \"work.resume\"'`. Probe removed; `ls` confirms absent. So registry.test.ts ROWS/:75, REGISTRATION_ORDER/:280 and OPERATOR_ONLY/:306 CANNOT gain a row without that line.\n   (ii) REACHABILITY. RUNTIME_COMMAND_KINDS (:85-115) has no preview.decide, and http/http-command-ingress.ts:152 calls decodeRuntimeCommandEnvelopeBytes, which refuses INPUT_INVALID at runtime-envelope.ts:142 `if (!isCommandKind(...))` BEFORE the registry or the operator fence. So DoD-2's \"an operator-authenticated one reaches the handler\" and step-4's \"THE KIND MUST BE SERVED, NOT ONLY ADVERTISED\" are unreachable in this scope.\n\nThat file is owned by sibling task-5f883e4e12c34842b3cb6c2f218e4256 (WORKING, order 31.5, DoD item 2), which dependsOn THIS row - so I cannot declare a dependency on it without closing a cycle, and taking the file would be the shared-worktree silent duplicate global rail 6 forbids.\n\nPROCEEDING under DELIVERY BIAS with everything that does NOT depend on the answer: steps 2-3 (apps/daemon/src/preview/ contract module + bridge + tests) are purely additive, touch no roster and can go green standalone. Will report_blocked at step 4, which is the first step that needs the two files above.\n\nPeer dirt noted, untouched: 11 dirty paths incl. apps/control-room/src/v2/goals/work-labels.*, apps/daemon/src/http/affordance-*.ts, apps/daemon/src/orchestrator/compiled-node-source.*, scripts/up.ps1, scripts/watch.ps1.",
      "startedAt": "2026-09-04T23:14:51.216Z",
      "completedAt": "2026-09-04T23:19:50.026Z",
      "amendments": [
        {
          "amendmentId": "amend-1",
          "description": "VERIFY AND RE-MEASURE, NO EDITS. NARROWED SCOPE: this row now DEFINES the contract and wires it into the daemon's OWN roster only. Publishing it on the shared surface (MCP exclusion, packages/contracts, the regenerated client) and adding it to HUMAN_ONLY_STEPS moved to task-5f883e4e12c34842b3cb6c2f218e4256 when this plan measured 12 files against the 10-file cap. Do not do that work here. (a) [AMENDED 2026-09-05] This row has NO dependsOn and nothing to wait for. The earlier text named a HUMAN_ONLY_STEPS guard row as a prerequisite; that row was filed on a false premise and has been DELETED. Measured truth, verify it yourself with `grep -rn 'HUMAN_ONLY_STEPS: ReadonlySet|HUMAN_ONLY_STEPS =' --include=*.ts apps/daemon/src`: there is exactly ONE definition, at agent-spawn-contract.ts:195. agent-wrapper.ts:24 is a deliberate RE-EXPORT (its comment at :23 says why), not a second copy. A bidirectional guard also already exists at http/affordance-planning-offers.test.ts:209 and :263. Do not add a roster, do not add an equality test, and do not block. (b) Confirm the gap: `grep -rn 'preview\\.decide|PREVIEW_COMMAND_MISSING' --include=*.ts apps/ packages/` returns ZERO. (c) TRACE THE ROSTER BEFORE EDITING, recording the exact line of each touchpoint, using `escalation.decide` as the worked example: daemon-command-vocabulary.ts capability (:81 area) and PAYLOAD_KEYS (:288 area); daemon-command-families.ts per-kind flag block (:83-94); daemon-command-registry.ts `entryOf` (:205-212, which resolves asyncEntries first and then destructures family flags). If the trace names a file outside this plan's seven, STOP and report — there is no headroom.",
          "reason": "Step 1(a) told the worker to verify a guard row (task-8834ebe68c1a4f81b3be8e7581156755) and report_blocked if it had not landed. I filed that row on a false premise and have since DELETED it: HUMAN_ONLY_STEPS is NOT duplicated. There is exactly ONE definition (agent-spawn-contract.ts:195) and agent-wrapper.ts:24 is a deliberate re-export. A worker following the base text would block forever on a row that no longer exists. Clause (a) is replaced with the measured truth; nothing else in the step changes.",
          "amendedBy": "architect-20a6d1eb",
          "amendedAt": "2026-09-04T23:09:02.029Z"
        }
      ],
      "activeAmendmentId": "amend-1"
    },
    {
      "stepId": "step-2",
      "description": "DEFINE THE CONTRACT IN ONE NEW MODULE, apps/daemon/src/preview/preview-contracts.ts, before touching any roster. Mirror apps/daemon/src/planning/expansion-request-contracts.ts — this repo's worked example of a command contract with a closed code->layer map. Export: the kind constant `preview.decide`; the EXACT payload key roster; and a frozen code->layer map covering PREVIEW_COMMAND_MISSING, PREVIEW_START_TIMEOUT, PREVIEW_GOAL_NOT_LANDED and PREVIEW_DECISION_INVALID, with the code roster DERIVED from that map's keys so the two can never disagree. The refusal factory takes NO layer argument: the map is the single source, so a call site cannot mint a code and layer that contradict each other. Ship the `.js` bridge alongside it (`export * from \"./preview-contracts.ts\";`) — a missing bridge kills the daemon at boot with ERR_MODULE_NOT_FOUND, and a generated index.js is never committed.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "apps/daemon/src/preview/preview-contracts.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/preview/preview-contracts.ts",
        "apps/daemon/src/preview/preview-contracts.js"
      ],
      "note": "CONTRACT MODULE LANDED. apps/daemon/src/preview/preview-contracts.ts (249 lines, under the 250 target) + the `.js` bridge `export * from \"./preview-contracts.ts\";` (convention verified against 627 existing bridges in apps/daemon/src; sample apps/daemon/src/activation/activation-admission-identity.js is byte-identical in form). No generated index.js.\n\nMIRRORS apps/daemon/src/planning/expansion-request-contracts.ts as the plan named. Exports: PREVIEW_DECIDE_COMMAND_KIND = \"preview.decide\"; PREVIEW_DECISIONS [\"APPROVE\",\"REJECT\"]; PREVIEW_LAYERS [\"GOAL_AUTHORITY\",\"REQUEST\",\"RUNNER\"] (closed); PREVIEW_CODE_LAYERS (frozen, `satisfies Readonly<Record<string, PreviewLayer>>`); PREVIEW_CODES DERIVED as `Object.keys(PREVIEW_CODE_LAYERS).sort()`; previewRefusal(code, sourceCode?, sourceLayer?) with NO layer argument - layer read from the map; decodePreviewDecidePayload; exactPreviewRecord; boundedPreviewText; isPreviewRefusal.\n\nCODE->LAYER MAP: PREVIEW_COMMAND_MISSING->RUNNER, PREVIEW_DECISION_INVALID->REQUEST, PREVIEW_GOAL_NOT_LANDED->GOAL_AUTHORITY, PREVIEW_START_TIMEOUT->RUNNER. All three declared layers are used, so the \"every declared layer is minted by some code\" arm is non-vacuous.\n\nTWO DESIGN CALLS, both measured rather than assumed:\n\n1. VARIANT ARITY, not one flat roster. DoD-5 says \"APPROVE carries no findings; REJECT carries findings\". I made that STRUCTURAL: PREVIEW_APPROVE_PAYLOAD_KEYS = [\"decision\",\"previewRef\"], PREVIEW_REJECT_PAYLOAD_KEYS = [\"decision\",\"findings\",\"previewRef\"]. An APPROVE carrying `findings` is refused as an UNKNOWN KEY, not accepted-with-an-empty-roster. This is legal at the seam because I measured `checkPayload` at apps/daemon/src/http/http-command-ingress.ts:118-126: it is an ALLOW-LIST (`for key of keys: if (!permitted.has(key)) return INPUT_INVALID`) - it refuses a key OUTSIDE the roster and never demands one INSIDE it. So the advertised roster may be the union while the decoder enforces the exact variant. If it had been exact-equality, APPROVE would have been refused at the seam and this design would be wrong.\n\n2. PREVIEW_DECIDE_PAYLOAD_KEYS is an ALIAS of PREVIEW_REJECT_PAYLOAD_KEYS, not a third literal. First draft had them as two byte-identical frozen arrays; that is exactly the drift shape the module's own header warns about, so the union is now `export const PREVIEW_DECIDE_PAYLOAD_KEYS = PREVIEW_REJECT_PAYLOAD_KEYS;` and step 4's vocabulary entry will import it rather than restate the keys.\n\nRUNNER CODES NOT FAKED (step-3/global-rail-1 honesty clause). Only PREVIEW_DECISION_INVALID is raised by production code in this row. PREVIEW_COMMAND_MISSING, PREVIEW_START_TIMEOUT and PREVIEW_GOAL_NOT_LANDED have no condition this module can reach; the header names task-f5a74c23f8754665ab9d36cba386e1d0 as where they are exercised. No manufactured arm.\n\nBUG CAUGHT AND FIXED IN-FLIGHT: the first write embedded a literal NUL byte into `boundedPreviewText` (`!value.includes(\"<NUL>\")`) instead of the escape `\"\u0000\"` - `grep | cat -A` reported \"Binary file matches\", and the Edit tool could not match the string because the on-disk bytes were not what I typed. Repaired with a byte-level python replace; `grep -c $'\\x00'` now 0 and line 163 reads `!value.includes(\"\u0000\")`. Recording it because a stray NUL in a source file is invisible in every normal diff view.\n\nNO ROSTER TOUCHED. This step edits no vocabulary, no families, no registry, no test table. Nothing in the repo imports the module yet - the consumer edge is step 4.\n\nGATE: `npx tsc --noEmit -p apps/daemon/tsconfig.json` - ZERO errors mentioning preview. EXECUTION PROVEN, not assumed: the same command produced `error TS2322` for the step-1 probe minutes earlier, so it demonstrably reports errors rather than short-circuiting.\n\nFOREIGN RED DISCLOSED, verbatim, NOT mine: `apps/daemon/src/bootstrap/bootstrap-durability.test.ts(671,13): error TS6133: 'cwd' is declared but its value is never read.` That file is ` M` in `git status --porcelain` - a peer's uncommitted in-flight edit (a 12th dirty path, beyond the 11 the wrapper snapshotted). My only paths are `?? apps/daemon/src/preview/`. Not reverted, not stashed, not committed.",
      "startedAt": "2026-09-04T23:19:54.902Z",
      "completedAt": "2026-09-04T23:23:05.794Z"
    },
    {
      "stepId": "step-3",
      "description": "THE PAYLOAD CONTRACT AND ITS REFUSALS, in a new apps/daemon/src/preview/preview-contracts.test.ts. Assert each code maps to its declared layer and that the derived code roster equals the map's keys exactly. Then the decoder: APPROVE carries no findings; REJECT carries findings naming the nodes to rework. Assert PREVIEW_DECISION_INVALID WITH ITS LAYER for a malformed decision value, an unknown extra key, REJECT with empty findings, and REJECT whose findings name no node. Assert code AND layer on every arm, never merely that it refused (global rail 1) — several layers can refuse a malformed payload, and the wrong one answering first would keep these green while testing nothing. The decoder REFUSES an unknown key rather than ignoring it: silently dropping a member would let an operator believe they supplied something load-bearing. STATE HONESTLY in the step note which codes this row can reach — PREVIEW_DECISION_INVALID is reachable here; PREVIEW_COMMAND_MISSING, PREVIEW_START_TIMEOUT and PREVIEW_GOAL_NOT_LANDED are raised by the runner in task-f5a74c23f8754665ab9d36cba386e1d0. Define them, name where they are exercised, and do NOT invent a fake condition to reach them: a manufactured arm proves the constant exists, not that the refusal works.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "apps/daemon/src/preview/preview-contracts.test.ts"
      ],
      "modifiedFiles": [
        "apps/daemon/src/preview/preview-contracts.test.ts"
      ],
      "note": "PAYLOAD CONTRACT AND ITS REFUSALS. apps/daemon/src/preview/preview-contracts.test.ts, 30 tests, all green. Mirrors the idiom of apps/daemon/src/planning/expansion-request-contracts.test.ts.\n\nWHICH CODES THIS ROW CAN REACH - stated plainly, no fake arms. PREVIEW_DECISION_INVALID is the ONLY code this row's production surface raises, and every one of its conditions is exercised. PREVIEW_COMMAND_MISSING, PREVIEW_START_TIMEOUT and PREVIEW_GOAL_NOT_LANDED have NO condition this module can reach; they are raised by the runner in task-f5a74c23f8754665ab9d36cba386e1d0 and exercised THERE. This file asserts their code->layer binding and their membership in the derived roster; it manufactures nothing. The file header says so in prose so a later reader cannot mistake the gap for an oversight.\n\nEVERY REFUSAL ARM ASSERTS CODE **AND** LAYER (global rail 1), through one helper `expectDecisionInvalid` that does `toStrictEqual({code:\"PREVIEW_DECISION_INVALID\", layer:\"REQUEST\"})` - not \"not ok\", not \"threw\". A second refusal layer landing above this decoder reddens these instead of silently passing.\n\nREFUSAL ARMS: malformed decision (7 values swept: \"approve\",\"REWORK\",\"\",7,null,undefined,true); no decision at all (absent / {} / null / bare string / array-wrapped); unknown extra key on BOTH variants; APPROVE carrying `findings` (empty roster AND populated - the variant-arity property); REJECT with empty findings; REJECT omitting findings; REJECT whose findings name no node (7 cases incl. over-length and the arity-drop case); finding with unknown key / missing detail / empty detail; findings not an array; roster past MAX_PREVIEW_FINDINGS with an at-bound positive control; malformed previewRef on both variants; prototype-bearing record; class instance; getter-backed member; symbol-keyed smuggled member.\n\nEVERY SWEEP ASSERTS ITS OWN DENOMINATOR (`expect(checked).toBe(7)` etc.) so a sweep that silently generated zero cases cannot pass.\n\nMUTATION DRILLS - THREE, ALL RED, ALL RESTORED WITH SHA256 PROOF. The three files are UNTRACKED (`?? apps/daemon/src/preview/`), so `git diff` is BLIND to them (global rail 13); restores are proved against out-of-tree backups in %TEMP%/moe-drill-worker-56ac23da, each proof in a call SEPARATE from the restore. Baseline SHA256: preview-contracts.ts = 5400ecbd995c12c8dc0235a0bd593a3983caf0badb8021282aa5dd354f1d6901; preview-contracts.test.ts = f05e68befc906ca6369b31d53b0370c4e1fbd608e732edf2b8c18431f85f871f.\n\nDRILL A (the one that matters - step 6(a)'s drill, run here because step 4 blocks): PREVIEW_DECISION_INVALID remapped \"REQUEST\" -> \"RUNNER\", a NEIGHBOURING layer in the same closed map. RED, 15 arms, exemplar verbatim: `AssertionError: expected 'RUNNER' to be 'REQUEST' // Object.is equality` plus `AssertionError: expected Set{ 'RUNNER', 'GOAL_AUTHORITY' } to strictly equal Set{ 'GOAL_AUTHORITY', 'REQUEST', ...(1) }`. Restored; SHA256 back to 5400ecbd...6901.\nDRILL B (unknown key IGNORED instead of refused): `exactPreviewRecord` `own.length !== keys.length` -> `own.length < keys.length`. RED, 3 arms: \"refuses an unknown extra key rather than ignoring it\", \"refuses an APPROVE that also carries findings\", \"refuses a finding carrying an unknown key\". Verbatim `AssertionError: expected false to be true`. `Tests 3 failed | 27 passed (30)`. Restored; SHA256 5400ecbd...6901.\nDRILL C (empty findings admitted): `findingsOf` dropped `value.length === 0 ||`. RED, exactly the \"refuses a REJECT with empty findings rather than recording a silent no-op\" arm. `Tests 1 failed | 29 passed (30)`. Restored; SHA256 5400ecbd...6901.\nPost-drill green re-confirmed: `Test Files 1 passed (1)` / `Tests 30 passed (30)`.\n\nGATE, AND A REAL PROBLEM WITH IT - DISCLOSED, NOT WORKED AROUND SILENTLY. `pnpm --filter @moe/daemon test` CANNOT RUN in this checkout: it exits 1 with `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY] Aborted removal of modules directory due to no TTY`. Cause measured, and it is FOREIGN: `node_modules/.modules.yaml` carries `virtualStoreDir = C:\\Users\\Yaron\\AppData\\Local\\Temp\\moe-glance-to-main\\node_modules\\.pnpm` and `storeDir = C:\\Users\\Yaron\\AppData\\Local\\pnpm\\store\\v11`, while this host's `pnpm store path` is `D:\\.pnpm-store\\v11`. A peer's scratch-worktree install (the build-origin/main-in-%TEMP% recipe) overwrote this checkout's modules metadata, so pnpm now wants to PURGE and reinstall node_modules. I did NOT set CI=true or confirmModulesPurge=false: both authorise the purge, which would destroy the shared checkout's install for every concurrent session. `npm_config_verify_deps_before_run=false` does not suppress the check.\nSUBSTITUTE, byte-identical to what the filter would execute: `@moe/daemon` resolves to apps/daemon (`package.json.name` confirmed) whose `test` script is `vitest run --root . --config package.json --maxWorkers=4 --testTimeout=30000 src`; I ran exactly that with cwd=apps/daemon.\n\nOWNED LEG GREEN: `Test Files 1 passed (1)` / `Tests 30 passed (30)`, exit 0 (targeted `src/preview`).\n\nREPO",
      "startedAt": "2026-09-04T23:23:12.934Z",
      "completedAt": "2026-09-04T23:43:56.495Z"
    },
    {
      "stepId": "step-4",
      "description": "WIRE IT INTO THE DAEMON'S OWN ROSTER. apps/daemon/src/daemon-command-vocabulary.ts: add `preview.decide` to the kind union, give it its capability alongside the other human decisions, add its PAYLOAD_KEYS entry from the contract module's roster (import it rather than restating the keys — a second copy drifts), and put it in OPERATOR_PRINCIPAL_KINDS so only an operator-authenticated dispatch mints its witness. apps/daemon/src/daemon-command-families.ts: add the per-kind family flag in the block at :83-94. apps/daemon/src/daemon-command-registry.ts: wire the entry so `entryOf` resolves it, for now to a handler that decodes the payload and refuses what the runner would own. THE KIND MUST BE SERVED, NOT ONLY ADVERTISED — an advertised-but-unserved kind breaks the bidirectional roster equality step 5 asserts, which is exactly why publishing and wiring are not split apart here even though the shared-surface publication is a separate row.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.ts",
        "apps/daemon/src/daemon-command-families.ts",
        "apps/daemon/src/daemon-command-registry.ts"
      ]
    },
    {
      "stepId": "step-5",
      "description": "UPDATE BOTH PAYLOAD-KEY TABLES AND PROVE SET-EQUALITY BOTH WAYS. TWO test tables enumerate every kind with its `payloadKeys` and BOTH must gain a row — apps/daemon/src/daemon-command-vocabulary.test.ts (the table at :170-182) and apps/daemon/src/daemon-command-registry.test.ts (the table at :197-203, plus the flat kind roster at :283-300). These are named here because a sibling row on this board shipped a plan and a governor triage that both missed exactly these two tables; they are invisible to a grep for the command-kind literal in production source. Then the bidirectional assertion global rail 9 requires: enumerate the SERVED set from the dispatch seam — the registry's own handled kinds — and assert it EQUALS the advertised roster in BOTH directions. A test that iterates only the roster constant sees one direction: deleting an entry shrinks its own iteration and stays green while a served capability vanishes from the advertised surface. Verify by deleting the new entry from each side in turn and confirming both go red.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.test.ts",
        "apps/daemon/src/daemon-command-registry.test.ts"
      ]
    },
    {
      "stepId": "step-6",
      "description": "DRILLS AND GATES. (a) MUTATION DRILLS recorded in a step note (global rail 1): break the code->layer map so one code reports a neighbouring layer and confirm the step-3 layer arm goes RED (this is the drill that matters — a code roster and a layer map that disagree is the defect the closed-map shape exists to prevent); then remove the kind from the advertised roster and confirm step 5's set-equality goes red in the served->advertised direction. Restore after each and prove with `git diff` on the tracked files showing empty; the two new files under apps/daemon/src/preview/ are UNTRACKED until committed, so if you drill inside them prove the restore by SHA256 against an out-of-tree backup rather than by `git diff`, which is blind to untracked paths (global rail 13). (b) GATES, standalone and foreground, each `Test Files N passed` count line quoted verbatim (global rail 8): `pnpm --filter @moe/daemon test`, `pnpm typecheck`. `pnpm test` covers NO apps/** and this row touches no packages/**, so it is not evidence here (global rail 18) — say so explicitly rather than quoting it as a pass. Named verification command for completion: `pnpm --filter @moe/daemon test`.",
      "status": "PENDING",
      "affectedFiles": [
        "apps/daemon/src/daemon-command-vocabulary.ts",
        "apps/daemon/src/daemon-command-registry.test.ts"
      ]
    }
  ],
  "status": "BLOCKED",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "ARCHITECT",
  "parentTaskId": "task-f3edb8fb39ad42ce8980dae7d62b604c",
  "priority": "HIGH",
  "order": 31,
  "comments": [
    {
      "id": "comment-6093ee6b869d4383bf6dec53c9334b02",
      "author": "worker-56ac23da",
      "content": "STEP-4 BLOCK EVIDENCE (worker-56ac23da, 2026-09-05). Full measurement behind the block; the block reason points here.\n\nStep 1's clause (c) — text taken from activeAmendmentId=amend-1, not the base description (global rail 10) — reads: \"If the trace names a file outside this plan's seven, STOP and report — there is no headroom.\" The trace names TWO. Both are needed by step 4/step 5 and by DoD items 1 and 2. Neither is in this plan.\n\n=== UNNAMED FILE 1: apps/daemon/src/daemon-store-dependencies.test.ts ===\nA hard runtime red the moment `preview.decide` enters PAYLOAD_KEYS.\n  :670  registryKinds: [...deps.registry.keys()].sort(),\n  :745  registryKinds: [ ...46 kinds hand-written... ],\nand the comment immediately above :745 reads verbatim: \"The kind SET, not its size: a bare count lands every registration as an off-by-one naming nothing. A new command writes its own kind here.\"\nThe served set is DERIVED — daemon-command-registry.ts:392 is\n  buildCommandRegistry((Object.keys(PAYLOAD_KEYS) as readonly WiredCommandKind[]).map(entryOf))\n— so adding the PAYLOAD_KEYS entry adds the registry key, which reds this file. It is invisible to a grep for the command-kind literal in production source because it lives in a test table (the exact class global rail 19 names, and the same class of miss the plan's own risk section flags for the other two tables).\n\n=== UNNAMED FILE 2: packages/contracts/src/runtime/runtime-vocabulary.ts ===\nExplicitly moved OUT of this row by amend-1 (\"Publishing it on the shared surface (MCP exclusion, packages/contracts, the regenerated client) ... moved to task-5f883e4e12c34842b3cb6c2f218e4256 ... Do not do that work here\"), yet step 4 and step 5 cannot be done without it, for two independent reasons.\n\n(i) TYPECHECK. RUNTIME_COMMAND_KINDS (:85-115) does not contain \"preview.decide\". Three declarations the plan's step 5 names are typed against the union derived from it:\n  daemon-command-registry.test.ts:75   readonly kind: RuntimeCommandKind;      (the ROWS table at :91)\n  daemon-command-registry.test.ts:280  const REGISTRATION_ORDER: readonly RuntimeCommandKind[]\n  daemon-command-registry.test.ts:306  const OPERATOR_ONLY: readonly RuntimeCommandKind[]\nand both sweeps are set-equality against the live registry, so a row cannot simply be omitted:\n  :814  expect([...deps.registry.keys()].sort()).toEqual(ROWS.map((row) => row.kind).sort());\n  :820  expect(REGISTRATION_ORDER).toHaveLength(46);\n  :821  expect([...deps.registry.keys()]).toEqual(REGISTRATION_ORDER);\nPROBE, written and deleted in the same command (`apps/daemon/src/zz-probe-worker56ac.ts`, contents `import type { RuntimeCommandKind } from \"@moe/contracts\"; export const PROBE: readonly RuntimeCommandKind[] = [\"preview.decide\"];`), run as `npx tsc --noEmit -p apps/daemon/tsconfig.json`, output verbatim:\n  apps/daemon/src/zz-probe-worker56ac.ts(2,54): error TS2322: Type '\"preview.decide\"' is not assignable to type '\"approval.decide\" | \"approval.decide_intent\" | \"blocker.challenge\" | \"blocker.open\" | \"blocker.resolve\" | \"budget.acknowledge_unknown_liability\" | \"budget.conservative_settle\" | ... 95 more ... | \"work.resume\"'.\nProbe removed; `ls` confirms absent. Full source is inline above so a grader can recreate it.\n(NOTE: daemon-command-vocabulary.test.ts is NOT affected — its VocabularyRow.kind (:31) and OPERATOR_ONLY (:221) are typed WiredCommandKind, which is daemon-local and extensible from the new contract module. Only the registry test file is fenced by the shared union.)\n\n(ii) REACHABILITY — the bigger one. DoD-2 requires \"an operator-authenticated one reaches the handler\" and step 4 requires \"THE KIND MUST BE SERVED, NOT ONLY ADVERTISED\". It cannot be, in this scope: apps/daemon/src/http/http-command-ingress.ts:152 calls decodeRuntimeCommandEnvelopeBytes, which at packages/contracts/src/runtime/runtime-envelope.ts:142 does `if (!isCommandKind(record[\"commandKind\"])) return commandFailure(invalidInput());`, and isCommandKind (runtime-vocabulary.ts:134) tests membership in RUNTIME_COMMAND_KINDS. A preview.decide dispatch is therefore refused INPUT_INVALID at the envelope, BEFORE the registry and before the OPERATOR_PRINCIPAL_KINDS fence. registry.test.ts's own `send` helper (:363) goes through handleCommandRequest, i.e. that same path — so even the test cannot reach the handler. Wiring the kind here without that line ships an advertised, registered, permanently unreachable command.\n\n=== WHY I DID NOT JUST TAKE THE LINE ===\ntask-5f883e4e12c34842b3cb6c2f218e4256 is WORKING, order 31.5, and its DoD item 2 is verbatim \"`preview.decide` is on the shared runtime command roster in packages/contracts/src/runtime/runtime-vocabulary.ts.\" Editing it here is the shared-worktree silent duplicate global rail 6 forbids, and it drags the four generated-contract digest mirrors that row's DoD item 3 owns. That row dependsOn THIS row, so I also cannot declare a dependency on it — that closes a cycle. This is an ordering/split defect only an architect can fix.\n\n=== WHAT IS ALREADY DELIVERED AND GREEN (steps 2 and 3, DoD items 4 and 5) ===\napps/daemon/src/preview/preview-contracts.ts (249 lines) + .js bridge + preview-contracts.test.ts. `Test Files 1 passed (1)` / `Tests 30 passed (30)`, exit 0. Closed code->layer map, code roster derived from its keys, refusal factory with no layer argument, variant exact-arity decoder (APPROVE refuses a `findings` member as an unknown key; REJECT refuses an empty roster). Three mutation drills all RED and restored with SHA256 proof against out-of-tree backups (untracked files, so `git diff` is blind — global rail 13). Details in the step-2 and step-3 notes.\n\n=== SMALLEST FIX I RECOMMEND ===\nAmend this row to own ONE extra line in packages/contracts/src/runtime/runtime-vocabulary.ts (the RUNTIME_COMMAND_KINDS entry) plus apps/daemon/src/daemon-store-dependencies.test.ts, and strike DoD item 2 from task-5f883e4e (leaving it the MCP exclusion, the client regeneration + digest mirrors, HUMAN_ONLY_STEPS and the staffing test). That makes this row's delivered bytes actually reachable and keeps the digest-mirror work where it belongs. The alternative — re-splitting so a tiny leaf row lands the shared-roster line first — costs a round trip and this row would still have to wait on it.",
      "timestamp": "2026-09-04T23:44:37.716Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-04T22:58:55.770Z",
  "updatedAt": "2026-09-04T23:46:20.839Z",
  "metrics": {
    "firstClaimAt": "2026-09-04T23:03:05.404Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 7,
    "executedStepCount": 3
  },
  "dependsOn": [],
  "planSizeWarnings": [
    "Plan touches 7 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-04T23:07:19.685Z",
  "planningNotes": {
    "approachesConsidered": "REJECTED, the first submitted shape of this plan: it carried the MCP exclusion, packages/contracts, the regenerated client and both HUMAN_ONLY_STEPS rosters as well, and measured 12 distinct files against the 10-file hard cap — the daemon refused it. Split along the INTERFACE axis instead: this row defines the contract and wires it into the daemon's own roster (7 files); task-5f883e4e12c34842b3cb6c2f218e4256 publishes it on the shared surface and fences it to the operator (5 files). REJECTED, advertising the kind without serving it: an advertised-but-unserved kind breaks the bidirectional roster equality global rail 9 demands, so the vocabulary entry and a minimal refusing handler land together here. REJECTED, restating the payload keys in the vocabulary rather than importing them from the contract module: a second copy of a key roster is how the decoder and the advertisement come to disagree. REJECTED, manufacturing arms for the three runner-owned codes: a fake condition proves the constant exists, not that the refusal fires — step 3 requires naming where child task-f5a74c23 exercises them instead. REJECTED, defining codes and layers as parallel constants: expansion-request-contracts.ts already shows the closed-map shape with a derived code roster, and it is the drill target in step 6.",
    "codebaseInsights": "MEASURED AT HEAD df5bbfd5 (branch moe/work-2026-09-04) by architect-20a6d1eb. THE GAP IS REAL: `grep -rn 'preview.decide|preview.start|PREVIEW_COMMAND_MISSING|moe-preview-receipt' --include=*.ts apps/ packages/` = ZERO hits. The existing apps/daemon/src/graph-preview-request.ts and apps/control-room/src/preview/ are PLAN/document previews, not the product preview — do not extend them. THE DAEMON-SIDE ROSTER, traced from escalation.decide: daemon-command-vocabulary.ts :81 (capability) and :288 (PAYLOAD_KEYS); daemon-command-families.ts :83-94 (the per-kind flag block); daemon-command-registry.ts :205-212 (`entryOf` resolves `asyncEntries[kind]` first, then destructures family flags — so a NEW handler needs both a family flag and a registry branch; the peer finding that project.activate needed ZERO registry edit does not transfer, because that kind already had a handler). TWO PAYLOAD-KEY TABLES must both gain a row: daemon-command-vocabulary.test.ts:170-182 and daemon-command-registry.test.ts:197-203 plus the flat roster at :283-300. I measured these the hard way on task-4b9c394d, where neither the original plan nor a governor's triage named them — they are invisible to a grep for the kind literal in production source, because they live in test tables. THE CONTRACT SHAPE TO MIRROR: apps/daemon/src/planning/expansion-request-contracts.ts, whose header explains why the layer is DERIVED from the code (\"a call site cannot mint a refusal whose code and layer disagree, and the code roster cannot drift from the layer map\").",
    "risks": "BIGGEST RISK — an unnamed roster touchpoint. This plan was REFUSED once at 12 files, and the two payload-key TEST tables are the kind of touchpoint a symbol grep cannot see; step 1 re-traces the whole roster before any edit and treats an eighth file as a stop-and-report rather than something to absorb. SECOND — decorative refusal codes: three of the four are raised by child task-f5a74c23's runner, so this row must define them and say plainly where they are exercised instead of manufacturing conditions. THIRD — a one-directional roster test. Iterating the roster constant alone hides a served capability vanishing from the advertised surface; step 5 requires enumerating the served set from the dispatch seam and verifying BOTH deletions go red. FOURTH — drill restore proof on UNTRACKED files: the two new modules under apps/daemon/src/preview/ are untracked until committed, and `git diff` is blind to untracked paths, so a mutation there must be restored against an out-of-tree SHA256 backup (global rail 13). FIFTH — a missing `.js` bridge on the new contracts module kills the daemon at boot with ERR_MODULE_NOT_FOUND. SIXTH — this row deliberately leaves the kind unpublished on the shared contract and unfenced in HUMAN_ONLY_STEPS; that is task-5f883e4e's, and until it lands the kind is daemon-internal, which is the correct intermediate state rather than a gap.",
    "keyFiles": [
      "apps/daemon/src/planning/expansion-request-contracts.ts",
      "apps/daemon/src/daemon-command-vocabulary.ts",
      "apps/daemon/src/daemon-command-families.ts",
      "apps/daemon/src/daemon-command-registry.ts",
      "apps/daemon/src/daemon-command-vocabulary.test.ts",
      "apps/daemon/src/daemon-command-registry.test.ts"
    ]
  },
  "planApprovedAt": "2026-09-04T23:07:21.794Z",
  "contextFetchedBy": [
    "worker-56ac23da"
  ],
  "workStartedAt": "2026-09-04T23:14:51.216Z",
  "blockedReason": "STEP 1 CLAUSE (c) STOP-AND-REPORT FIRED. Amended text (activeAmendmentId=amend-1, global rail 10): \"If the trace names a file outside this plan's seven, STOP and report - there is no headroom.\" It names TWO. Verbatim evidence: comment-6093ee6b869d4383bf6dec53c9334b02.\n\n1. apps/daemon/src/daemon-store-dependencies.test.ts - unnamed, hard red. :670 `registryKinds: [...deps.registry.keys()].sort()` vs a hand-written 46-kind roster at :745 (\"A new command writes its own kind here\"). registry.ts:392 DERIVES the served set from Object.keys(PAYLOAD_KEYS), so step 4's entry alone reds it. Invisible to a kind-literal grep in production source.\n\n2. packages/contracts/src/runtime/runtime-vocabulary.ts - moved OUT by amend-1, needed twice.\n(i) TYPECHECK: RUNTIME_COMMAND_KINDS lacks \"preview.decide\"; registry.test.ts types Row.kind (:75), REGISTRATION_ORDER (:280), OPERATOR_ONLY (:306) as RuntimeCommandKind, with set-equality sweeps at :814/:820-821 forbidding omission. Probe: `TS2322: Type '\"preview.decide\"' is not assignable to type '\"approval.decide\" | ... 95 more ... | \"work.resume\"'`.\n(ii) REACHABILITY, decisive: http-command-ingress.ts:152 -> runtime-envelope.ts:142 `if (!isCommandKind(...)) return commandFailure(invalidInput())`. preview.decide is refused INPUT_INVALID at the envelope BEFORE the registry and the operator fence, so step 4's \"MUST BE SERVED, NOT ONLY ADVERTISED\" and DoD-2's \"reaches the handler\" are unreachable here.\n\nNot taking the file: task-5f883e4e12c34842b3cb6c2f218e4256 is WORKING and its DoD-2 owns that line plus the four digest mirrors. It dependsOn THIS row, so blockedOnTaskIds would close a cycle - omitted deliberately.\n\nDELIVERED GREEN (DoD 4, 5): apps/daemon/src/preview/preview-contracts.ts (249 lines) + .js bridge + test. Tests 30 passed (30), exit 0. Three mutation drills RED, SHA256-restored.\n\nFIX: give this row one RUNTIME_COMMAND_KINDS line + daemon-store-dependencies.test.ts; strike DoD-2 from task-5f883e4e.",
  "blockedResourceId": null,
  "blockedFromStatus": "WORKING",
  "blockedAt": "2026-09-04T23:46:20.839Z"
}