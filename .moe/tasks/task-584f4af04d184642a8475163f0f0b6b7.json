{
  "id": "task-584f4af04d184642a8475163f0f0b6b7",
  "epicId": "epic-bf111658f9694b558bdc5596bbf0f924",
  "title": "Store-side recovery binding schema and atomic recovery-install transaction surface",
  "description": "Objective: give @moe/store the rows and the public atomic transaction surface that a recovery install needs, and resolve the dependency-direction question that currently makes task-b6e3dd2a unsatisfiable. Authored by governor-36019faa on human direction 2026-08-10 from two independent architect audits on task-b6e3dd2af916490fb2bc4d375a530683.\n\nTHE BLOCKER THIS EXISTS TO REMOVE, measured at HEAD bec1cc6 by governor-36019faa:\n1. packages/store/package.json has NO \"dependencies\" field AT ALL. Full file is name, version, private, type, exports, scripts. @moe/store therefore cannot import @moe/core - which owns reduceProject(recovery.restore_quiesce) - and must not import upward from apps/daemon.\n2. grep -rln \"incarnation|key_epoch|keyEpoch\" packages/store/src/ returns NOTHING.\n3. The store schema holds exactly: aggregate_heads, command_receipts, domain_events, outbox_messages, store_metadata, command_decisions, store_project_binding, command_receipt_scopes, projections, inbox_receipts, event_subscriptions, cursor_generations. No recovery-incarnation, key-epoch, credential, grant, or project-lifecycle rows. Exact schema validation REJECTS extra tables/metadata, so rows cannot be added ad hoc by a consumer.\n\nTHE ARCHITECTURE QUESTION THE ARCHITECT MUST SETTLE FIRST, and it is genuinely open - do not assume the answer. Either (a) @moe/store legitimately declares a dependency on @moe/core, or (b) the binding codec lives in a lower shared package both can depend on, or (c) the anchor does not belong in @moe/store at all. Option (a) is a real layering change and must be justified, not slipped in as a manifest line. Record the decision and its reasoning in planningNotes; a later reader must be able to see why the edge is legitimate rather than expedient.\n\nRELATED PRIOR ART to read before naming files: apps/daemon/src/recovery/recovery-incarnation-anchor.ts already exists. Establish whether it is the codec this task should move/share or an unrelated surface, and say which. Landing a second binding codec is the silent-duplicate failure the staleness rail warns about.\n\nNOT IN SCOPE: the two-slot installer protocol itself (task-b6e3dd2a), the durable key provider, the daemon restore controller, generating backup bytes.\n\nHard dependencies: the durable OS-protected recovery key provider task (order 13.01, same epic) must be DONE, because the key-epoch column has no meaning until an epoch survives restart. This task is BACKLOG until then. If claim_next_task serves it before that task is DONE, block it immediately and cite this line rather than re-deriving.\n\nOwned paths: packages/store/src/ (schema migration, binding codec, recovery-install transaction surface, export wiring) plus packages/store/package.json if the architect's decision is option (a).\n\nVerification: pnpm --filter @moe/store typecheck && pnpm --filter @moe/store test",
  "definitionOfDone": [
    "The dependency-direction decision is recorded with its justification, and the resulting edge is proven three ways: the consuming manifest declares it, pnpm-lock.yaml carries the importer entry, and a compiled bare-specifier probe typechecks and is deleted in the same command.",
    "A schema migration adds the recovery binding rows the installer requires; exact schema validation accepts the new shape and still rejects unknown tables and metadata, with a test pinning both directions.",
    "A public atomic recovery-install transaction surface exists on @moe/store: one transaction commits the binding, and a partial or interrupted install leaves the prior state intact rather than a mixed state.",
    "Every refusal asserts its exact stable reason code AND the refusing layer; each declared failure injection is generated at least once and the suite fails if a sweep produces zero cases.",
    "A migration applied to an existing populated database preserves prior rows, proven by a test that writes before migrating and reads back after.",
    "Mutating each production guard turns a NAMED focused test red; drills run, reverted, exact failing assertions recorded.",
    "Fresh pnpm --filter @moe/store typecheck && pnpm --filter @moe/store test exits 0."
  ],
  "taskRails": [
    "Authoritative design is D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it.",
    "@moe/store must NOT import upward from apps/daemon under any circumstance. If the needed surface only exists there, that is a blocker to report, not a shortcut to take.",
    "Do not reimplement lifecycle or business authority inside the store. The store holds rows and transactions; reduceProject remains @moe/core's.",
    "Cross-package symbol use must prove the DEPENDENCY EDGE, not just the symbol: manifest, lockfile importer, compiled bare-specifier probe. This task exists partly because that check was skipped elsewhere.",
    "Target <=250 physical lines per production source and split before 400; this cap is per file, never per task."
  ],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "READ-ONLY. Confirm the hard dependency FIRST, because this task's description carries a standing order: \"If claim_next_task serves it before that task is DONE, block it immediately and cite this line rather than re-deriving.\" I verified it before planning: task-eff945fc (13.01, Durable OS-protected recovery key provider) is DONE at 10/10 and its deliverable is committed — `git ls-tree -r HEAD -- apps/daemon/src/recovery` returns recovery-key-provider.ts/.js, -contract.ts/.js, .node.ts/.js and both test files. Re-check it yourself; if it has regressed, block and cite that line.\n\nRe-measure the three premises (all held at promotion): packages/store/package.json has NO \"dependencies\" key at all; `grep -riln \"incarnation|key_epoch|keyEpoch\" packages/store/src/` returns NOTHING; sqlite-schema-manifest.ts holds exactly twelve tables — aggregate_heads, command_decisions, command_receipt_scopes, command_receipts, cursor_generations, domain_events, event_subscriptions, inbox_receipts, outbox_messages, projections, store_metadata, store_project_binding — with no recovery, incarnation, key-epoch, credential or grant row.\n\nMigration machinery you COMPOSE rather than invent: sqlite-schema-migration.ts, sqlite-schema-bootstrap.ts, sqlite-schema-conformance.ts, sqlite-schema-integrity.ts all already exist. Read all four before designing the migration.\n\nRecord the baseline gate: `pnpm --filter @moe/store typecheck && pnpm --filter @moe/store test`.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "REOPEN PASS (reopenCount 1). QA rejected on DoD 6 ONLY; DoD 1-5 and 7 were re-run and verified green by qa-f3560083 in comment-73ee5c2504534d67b6680b97b9eb225e. Steps 1-4 and 6 are already landed at commit e426e5e and require no new bytes; I re-confirm their premises rather than redo them.\n\nHard dependency re-confirmed at HEAD 531fb92: task-eff945fc (order 13.01, durable OS-protected recovery key provider) is DONE and apps/daemon/src/recovery/recovery-key-epoch-store.ts exists (10794 bytes, 2026-08-10). No block warranted.\n\nWorking-tree state re-established after the prior session died: `git status --porcelain -- packages/store` is EMPTY, and `git diff --name-only e426e5e..HEAD -- packages/store` is EMPTY. So the reviewed store bytes are exactly what is on disk now — nothing was left half-applied by an interrupted drill.\n\npackages/store/package.json re-read: keys are exactly name,version,private,type,exports,scripts; `dependencies` is undefined. The recorded no-edge decision still holds on disk.",
      "startedAt": "2026-08-11T08:43:17.964Z",
      "completedAt": "2026-08-11T08:43:47.805Z"
    },
    {
      "stepId": "step-2",
      "description": "SETTLE THE ARCHITECTURE QUESTION — DoD 1, and the task says it is genuinely open. My measured recommendation is NONE OF (a)/(b)/(c) AS FRAMED: **no dependency edge is required at all.** Verify this before accepting it, and record the decision with its reasoning in your completion notes either way.\n\nTHE REASONING. The premise behind the question is that @moe/store needs @moe/core because core owns reduceProject(recovery.restore_quiesce). It does not. taskRail 3 states the split precisely: \"The store holds rows and transactions; reduceProject remains @moe/core's.\" This task lands exactly three things — recovery binding ROWS, a byte-level CODEC, and an atomic TRANSACTION surface. None performs lifecycle reasoning, so none needs the reducer. The composition of reducer + store is the DAEMON's job and belongs to task-2ff368fe (13.03), which already depends on both.\n\nOption (a) store -> core would invert the layering: a storage package would gain a dependency on business logic and inherit everything core pulls in. Option (c) is wrong because the rows genuinely belong in the store — durable rows plus an atomic transaction is precisely what a store is for.\n\nMEASURED LAYERING that makes the fallback safe if you find I am wrong: @moe/contracts has NO dependencies (it is the root layer), @moe/core depends on @moe/contracts, @moe/store depends on nothing. So IF the store turns out to genuinely need a shared symbol, declare @moe/contracts — acyclic and matching how core already layers — and NEVER @moe/core.\n\nCONSEQUENCE FOR DoD 1's three-way edge proof: if the decision is \"no edge\", that clause has no subject. Prove the NEGATIVE instead and say so — packages/store/package.json still declares no dependencies and the package typechecks and tests clean without one. If you do add @moe/contracts, then the full three-way proof applies: manifest declaration, pnpm-lock.yaml importer entry, and a compiled bare-specifier probe deleted in the same command.\n\nPRIOR ART, and this is a duplicate-codec hazard the task names explicitly. apps/daemon/src/recovery/recovery-incarnation-anchor.ts has `encodeBinding` (:40) and `decodeBinding` (:88) — both MODULE-PRIVATE, only readAnchoredIncarnation (:147) and anchorIncarnation (:186) are exported — inside a module that imports `{ CommandDecisionRecord, SqliteEventStore } from \"@moe/store\"`. It is a daemon-side CONSUMER of the store, so the store cannot reuse it without importing upward, which taskRail 2 forbids absolutely. Your codec is therefore a second implementation BY NECESSITY. Do not pretend otherwise: state it in completion notes as a DECLARED duplicate with a retirement path, and name the follow-on that should migrate the daemon onto the store's canonical codec. A silent second codec is the failure the staleness rail warns about; a declared one with an owner is not.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "DECISION UNCHANGED — NO EDGE, and it survived QA review. Recorded in full in the prior session and in Serena `task-task-584f4af04d184642a8475163f0f0b6b7-handoff`. Re-verified on disk this session:\n- packages/store/package.json has no `dependencies` key (keys: name,version,private,type,exports,scripts).\n- `grep -rn 'apps/daemon|from \"@moe/' packages/store/src/` exits 1 — ZERO cross-package and zero upward imports. Rails 2 and 3 clean.\n\nDoD 1 IS CLOSED BY QA, NOT BY ME, AND I AM NOT RE-RUNNING IT. qa-f3560083 recorded (comment-73ee5c2504534d67b6680b97b9eb225e) that my prior proof-of-negative was narrower than the DoD text and ran the missing leg itself against the PRE-EXISTING apps/daemon -> @moe/store edge:\n- manifest: apps/daemon/package.json declares \"@moe/store\": \"workspace:*\" — verified\n- lockfile importer: pnpm-lock.yaml `apps/daemon:` -> '@moe/store': specifier workspace:* / version link:../../packages/store — verified\n- compiled bare-specifier probe: apps/daemon/src/qa-probe-recovery-install.ts importing 8 values + 2 prototype methods + 6 types from bare \"@moe/store\"; `pnpm --filter @moe/daemon typecheck` exit 0; deleted in the same command\n- NEGATIVE CONTROL: importing recoveryBindingDigest / isRecoveryBindingSlot / RecoveryInstallStore fails TS2724 x3, exit 1, deleted in the same command — so the probe is provably live AND the published surface is provably no wider than claimed\n\nCorrection to my own prior note, worth carrying: recovery-install-entrypoint-worker.mjs imports './index.ts' RELATIVE. That proves the barrel holds RUNTIME VALUES; it does NOT exercise the exports map or workspace resolution. taskRail 4 draws exactly that distinction and my note conflated the two.\n\nDeclared duplicate codec stands: apps/daemon/src/recovery/recovery-incarnation-anchor.ts:40/:88 hold module-private encodeBinding/decodeBinding inside a module that imports @moe/store, i.e. a DOWNSTREAM consumer. The store cannot reuse them without importing upward (rail 2 forbids). Retirement follow-on already requested from an architect, sequenced after task-b6e3dd2a.",
      "startedAt": "2026-08-11T08:43:56.296Z",
      "completedAt": "2026-08-11T08:44:14.389Z"
    },
    {
      "stepId": "step-3",
      "description": "TDD RED. Create the focused test for the schema migration and the binding rows. DoD 2 requires exact schema validation to ACCEPT the new shape and STILL REJECT unknown tables and metadata — pin BOTH directions, because a migration that loosens the conformance check to make room is the silent regression here. Assert the twelve existing tables are still present and still exactly validated.\n\nDoD 5 is the clause most likely to be skipped: a migration applied to an EXISTING POPULATED database must preserve prior rows. Write rows through the production surface FIRST, then migrate, then read back and assert the earlier rows are byte-identical. A migration tested only against an empty database proves nothing about the upgrade path real deployments take.\n\nEvery refusal pins its exact stable reason code AND the refusing layer (DoD 4), and the generated sweep asserts a NON-ZERO case count.\n\nUse a real temp path via mkdtemp under the OS temp root, never inside the repo, cleaned in an afterEach that runs on failure — this host is win32 and a stray .sqlite file in a commit breaks the no-scratch-files rail.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/recovery-install.test.ts"
      ],
      "newFiles": [
        "packages/store/src/recovery-install.test.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/store-schema-v4-migration.test.ts"
      ],
      "note": "NO NEW BYTES THIS PASS. store-schema-v4-migration.test.ts landed at e426e5e and is untouched by the reopen — the rejection named DoD 6 only, and its two named issues both sit in recovery-install.ts / recovery-install.test.ts.\n\nBoth DoD-2 directions this step exists to pin were independently RE-DRILLED BY QA, not merely read:\n- unknown-table direction: my own drill (a) reddened it.\n- unknown-metadata direction: qa-f3560083 ran a NEW drill I had not — replacing the exact-one guard in validateSchemaManifestMetadata with a `.find()` on the manifest key (tolerating extra rows while still checking the manifest row). RED at store-schema-v4-migration.test.ts > \"still refuses unknown store_metadata after the migration\", \"AssertionError: conformance still rejects extra metadata: expected undefined to be an instance of DurableStoreError\".\n\nQA also recorded a BAD DRILL on the same guard and why it was bad, which I am copying here because it would otherwise be re-derived: changing `metadataRows.length !== 1` to `< 1` left the test GREEN, but rows come back ORDER BY key and \"recovery_authority\" sorts before \"schema_manifest_version\", so metadataRows[0] WAS the injected row and the key check answered first. That indicts the mutation, not the test.\n\nDoD 5 (populated-database upgrade) unchanged and green: rows are written through the production surface at user_version 3, then migrated, then read back.",
      "startedAt": "2026-08-11T08:44:18.307Z",
      "completedAt": "2026-08-11T08:44:28.229Z"
    },
    {
      "stepId": "step-4",
      "description": "Land the schema migration: extend packages/store/src/sqlite-schema-manifest.ts with the recovery binding rows the installer requires, and wire the upgrade through the existing sqlite-schema-migration.ts rather than inventing a second path. Keep sqlite-schema-conformance.ts's exactness intact — the new tables become part of the expected set, and anything outside it must still be rejected.\n\nDefine the ROW SHAPE as the store's own, not a copy of the daemon's domain binding: the store persists columns and bytes, and the daemon maps its RecoveryIncarnationBinding onto them. That separation is what keeps the store free of an upward dependency and is the concrete form of the architecture decision from step 2.\n\nBoth files sit near the cap — sqlite-schema-manifest.ts is 234 physical lines against a 250 target — so if the new tables push it over, split the recovery rows into their own manifest module rather than overflowing. Measure with `grep -c ''`, never PowerShell Measure-Object -Line.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/sqlite-schema-manifest.ts",
        "packages/store/src/sqlite-schema-migration.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/sqlite-schema-recovery-manifest.ts",
        "packages/store/src/sqlite-schema-manifest.ts",
        "packages/store/src/sqlite-schema-migration.ts",
        "packages/store/src/store-internals.ts"
      ],
      "note": "NO NEW BYTES THIS PASS. Schema v3->v4 landed at e426e5e; the rejection does not touch it.\n\nThe step's split instruction was followed: sqlite-schema-manifest.ts was 234/250, so the recovery_bindings SQL went into its own module sqlite-schema-recovery-manifest.ts rather than overflowing the host. QA hand-measured with `grep -c ''` and reproduced my numbers: sqlite-schema-recovery-manifest.ts 36, sqlite-schema-manifest.ts 241, sqlite-schema-migration.ts 176, store-internals.ts 131 — all under 250.\n\nConformance exactness intact and PROVEN in both directions by drills (see step 3 note). The new table joined the expected set via a version bump + migration leg; neither validateExactSchemaObjects nor validateSchemaManifestMetadata was loosened to make room.\n\nCarry-forward for whoever writes v5, already in Serena handoff: migrateV2ToV3 previously stamped SQLITE_SCHEMA_MANIFEST_VERSION and SCHEMA_VERSION, both of which now mean v4 — post-bump it would have labelled a v3 schema as v4 and the next leg would refuse it. It now stamps SCHEMA_V3_MANIFEST_VERSION and a literal user_version = 3, and its CREATE statements reference SCHEMA_V3_OBJECT_SQL explicitly instead of riding on head. Copy that pattern; do not stamp head constants from a historical leg.\n\nmigrateV3ToV4 is the only leg that accepts a populated source — v1->v2 and v2->v3 call hasDurableRows() and refuse STORE_MIGRATION_REQUIRED because they rebuild domain_events. v3->v4 creates one table and rewrites nothing, which is what makes DoD 5 satisfiable at all.",
      "startedAt": "2026-08-11T08:44:30.484Z",
      "completedAt": "2026-08-11T08:44:41.356Z"
    },
    {
      "stepId": "step-5",
      "description": "Land the binding codec plus the public atomic recovery-install transaction surface: packages/store/src/recovery-install.ts and its one-line LF `.js` bridge (every module in this package has one — check a sibling and match it byte-for-byte in form).\n\nDoD 3 is the load-bearing property: ONE transaction commits the binding, and a partial or interrupted install leaves the PRIOR state intact rather than a mixed state. Compose the store's existing transaction machinery (decision-ledger-transaction.ts / event-ledger-transaction.ts) rather than hand-rolling BEGIN/COMMIT — the store already owns this and a second transaction discipline would diverge.\n\nTest the interruption directly: inject a failure between the write and the commit and assert the database reads back as it was BEFORE, not as a half-applied state. \"It returned an error\" is not the property; \"it refused AND left prior state intact\" is.\n\nThe codec is byte-level and self-contained: encode and decode the binding without reasoning about lifecycle (taskRail 3). Fail closed on malformed bytes with an exact code and layer, and never repair a malformed record.\n\nKeep each file at or below 250 physical lines.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/recovery-install.ts",
        "packages/store/src/recovery-install.js"
      ],
      "newFiles": [
        "packages/store/src/recovery-install.ts",
        "packages/store/src/recovery-install.js"
      ],
      "modifiedFiles": [
        "packages/store/src/recovery-install.test.ts"
      ],
      "note": "THE ONLY NEW BYTES THIS PASS: one named test in recovery-install.test.ts, +54/-1. Production code untouched, as QA instructed — `git diff --stat -- packages/store` shows 1 file changed.\n\nNEW TEST: \"refuses a row whose stored bytes name a slot other than the one it is filed under\". It closes the hole QA proved: the pre-existing divergence test drifts three of the four values the row duplicates (incarnation_ref, key_epoch_ref, binding_codec_version) and omitted `slot` — the fourth, and the one the design claim rests on.\n\nFixture, built exactly as QA specified: install ACTIVE/REF_X through the production surface, then via a SECOND raw DatabaseSync\n  UPDATE recovery_bindings SET binding_bytes = ?, binding_digest = ? WHERE slot = 'ACTIVE'\nwith the bytes+digest of an otherwise identical record encoded for slot 'PENDING'. Bytes and digest move TOGETHER so they still agree; the other three columns are untouched and still agree with the bytes. Neither the codec's integrity gate nor the three column comparisons can see this row — only the slot cross-check can. Asserts code RECOVERY_BINDING_ROW_DIVERGED and layer RECOVERY_INSTALL_TRANSACTION.\n\nPOSITIVE CONTROL inside the same test, so the refusal cannot be an earlier layer answering: the same record installed into the slot it names produces a digest BYTE-IDENTICAL to the injected one (`expect(...digest).toBe(foreign.digest)`) and reads back FOUND. So the bytes are provably well-formed at the codec layer, and the only thing that changed is which slot they were filed under.\n\nThe encode helper is imported from \"./index.js\" rather than the codec module, so the test also rides the published root surface.\n\nGate after the change: `pnpm --filter @moe/store typecheck` exit 0; `pnpm --filter @moe/store test` exit 0 — Test Files 36 passed (36), Tests 384 passed (384). Was 383; the count grew by exactly one.",
      "startedAt": "2026-08-11T08:45:12.616Z",
      "completedAt": "2026-08-11T08:47:00.302Z"
    },
    {
      "stepId": "step-6",
      "description": "Publish from the store root: add a curated named block to packages/store/src/index.ts in the file's existing explicit-export style — never a wildcard, and do not reorder or reflow neighbouring exports, since the barrel is shared state.\n\nPublish exactly what the named consumers need and no wider: task-b6e3dd2a (the two-slot anchor installer) needs the atomic install surface and the codec; task-2ff368fe (13.03, the daemon restore controller) composes the same surface. A root surface is easy to widen and hard to narrow, so anything beyond those two consumers' needs stays internal.\n\nThen PROVE the publication the way the JetBrains failure taught us: a symbol existing in a defining file says nothing about a consumer being able to import it. Add an assertion that the published values resolve through the package's own exports map from a bare specifier, and assert RUNTIME VALUES (typeof on a function, a literal on a constant) rather than types — `--experimental-strip-types` erases `export type` entirely, so a type-only publication satisfies tsc while leaving nothing importable at runtime.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/index.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/index.ts",
        "packages/store/src/recovery-install-entrypoint.test.ts",
        "packages/store/src/recovery-install-entrypoint-worker.mjs"
      ],
      "note": "NO NEW BYTES THIS PASS beyond the test's import line. index.ts is unchanged from e426e5e; the new test imports encodeRecoveryBinding from \"./index.js\", which is already published at index.ts:80 — no widening.\n\nPublished surface (unchanged, curated, no wildcard): RECOVERY_BINDING_CODEC_LAYER, RECOVERY_BINDING_CODEC_VERSION, RECOVERY_BINDING_SLOTS, RECOVERY_INSTALL_LAYERS, RECOVERY_INSTALL_REASON_CODES, RECOVERY_INSTALL_TRANSACTION_LAYER, encodeRecoveryBinding, decodeRecoveryBinding, plus types. The install surface reaches consumers as methods on the already-exported SqliteEventStore: installRecoveryBinding / readRecoveryBinding. Deliberately NOT published: recoveryBindingDigest, isRecoveryBindingSlot, RecoveryInstallStore.\n\nCORRECTION I OWE, and it is the honest version of my prior claim: recovery-install-entrypoint-worker.mjs imports './index.ts' RELATIVE. That proves the barrel holds RUNTIME VALUES under --experimental-strip-types (which is a real property, since export type erases entirely) but it does NOT exercise the exports map or workspace resolution. taskRail 4 distinguishes exactly those, and my step-6 note previously ran them together.\n\nqa-f3560083 ran the missing bare-specifier leg and I am NOT re-running it per its explicit instruction: apps/daemon/src/qa-probe-recovery-install.ts importing 8 values + 2 prototype methods + 6 types from bare \"@moe/store\", `pnpm --filter @moe/daemon typecheck` exit 0, deleted in the same command; plus a NEGATIVE CONTROL importing the three deliberately-withheld symbols, which fails TS2724 x3 at exit 1, also deleted in the same command. That negative control is what makes \"no wider than the two named consumers need\" a measured claim rather than an assertion.\n\nConsumers on record: task-b6e3dd2af916490fb2bc4d375a530683 and task-2ff368fe2de44028b4a7d6ca89687933.",
      "startedAt": "2026-08-11T08:47:03.250Z",
      "completedAt": "2026-08-11T08:47:15.756Z"
    },
    {
      "stepId": "step-7",
      "description": "Mutation drills, then restore byte-exact. Each must redden a NAMED test; record the exact failing assertion. (a) Loosen sqlite-schema-conformance so an unknown table is accepted — the reject-unknown-tables assertion must redden, proving the migration did not buy room by weakening the check. (b) Commit the binding outside the transaction so a partial install leaves a mixed state — the interruption test must redden; if it stays green, that test is only checking the return value. (c) Make the migration drop or rewrite a prior row — the populated-database preservation test must redden. (d) Accept malformed codec bytes instead of refusing — the exact-code assertion must redden. (e) Change only the refusing LAYER on one refusal — a layer assertion must redden independently of the code. (f) Publish the surface as `export type` only — the runtime-value probe must redden, proving the publication is real rather than erased.\n\nThe new files are UNTRACKED, so `git diff` is blind to them — hash with `git hash-object` before and after rather than trusting an empty diff, and re-run the suite after every restore.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/recovery-install.ts",
        "packages/store/src/sqlite-schema-manifest.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/recovery-install.ts",
        "packages/store/src/recovery-install.test.ts"
      ],
      "note": "THE REJECTED DoD. QA's drill (iii) re-run against the new test, plus two extra drills QA did not ask for, all restored and hash-verified. Baseline recovery-install.ts = f194d585ffc9b996ed133a19b569aafd63c8aeba (identical to the hash QA recorded). Drill harness aborted on anchor count != 1 and asserted the restored hash after every drill; final hash f194d585... = baseline. Working tree afterwards: `git diff --stat -- packages/store` = 1 file, recovery-install.test.ts, +54/-1. NO production bytes changed.\n\nDRILL A — QA's drill (iii), both slot cross-checks deleted (recovery-install.ts:60-61):\n    binding.slot === querySlot &&\n    requireRowString(row, \"slot\") === binding.slot &&\nmutated hash e1d897cbcc1168de2e9d6b3512b78dda702285ca. WHOLE package suite, not a focused file: exit 1.\n  Test Files  1 failed | 35 passed (36)\n       Tests  1 failed | 383 passed (384)\n  FAIL packages/store/src/recovery-install.test.ts > recovery install transaction surface > \"refuses a row whose stored bytes name a slot other than the one it is filed under\"\n  AssertionError: expected true to be false // Object.is equality\nThat is `expect(refused.ok).toBe(false)` — the read returned FOUND on a row whose bytes name PENDING. It is the intended assertion, not the positive control above it, which would have failed with \"expected FOUND, got ...\". The guard pair is now killed by a NAMED test; the same mutation left 383/383 green before.\n\nDISCLOSURE I OWE — I ran the two single-operand drills as well, and BOTH SURVIVED GREEN (384/384):\n  B: delete only `binding.slot === querySlot` -> hash eb5221bd..., exit 0\n  C: delete only `requireRowString(row, \"slot\") === binding.slot` -> hash 7fed44a9..., exit 0\nThis is NOT a further test gap; the operands are logically equivalent, so each alone is an EQUIVALENT MUTANT no test can kill. Proof by measurement, not reasoning: rowMatchesBinding has exactly ONE call site (grep returns the definition at :54 and the call at :108). That call passes `row` from SELECT_BINDING_SQL, whose only predicate is `WHERE slot = ?` bound to the same `slot` argument, against a STRICT table where slot is `TEXT PRIMARY KEY NOT NULL CHECK (slot IN ('ACTIVE','PENDING'))`. So requireRowString(row,\"slot\") === querySlot holds for EVERY reachable row, making operand A true exactly when operand B is. Applying my own test for this case (mem:gotcha-redundant-operand-mutants-survive-inside-one-guard): I cannot name one input the surviving mutant now admits, so this is redundancy rather than defence in depth.\n\nI did NOT act on it, because QA's instruction was explicit: \"Do NOT touch production code; it is correct.\" Dropping one operand is a judgement for QA/architect, not a change to slip in under a test-only fix. Flagged so the next reader does not re-drill it and read the green as rot.\n\nNOT RE-RUN, per QA's explicit instruction, and cited rather than reclaimed: my prior drills (a)-(f), of which QA independently REPRODUCED (b) — inserting `COMMIT; BEGIN IMMEDIATE` between the DELETE and INSERT in writeBindingUnderLock reddens \"leaves the prior binding intact when an install fails mid-transaction\" at the STATE check in found(), \"Error: expected FOUND, got ABSENT\" — and EXTENDED (a) with the store_metadata half I had missed.",
      "startedAt": "2026-08-11T08:48:07.059Z",
      "completedAt": "2026-08-11T08:48:24.638Z"
    },
    {
      "stepId": "step-8",
      "description": "Verify and self-review. (1) Run the exact gate fresh and last: `pnpm --filter @moe/store typecheck && pnpm --filter @moe/store test`. Confirm the Test Files count grew. (2) Hand-measure every touched production file with `grep -c ''` against the 250 target — sqlite-schema-manifest.ts started at 234 and is the one at risk. (3) Path-attributed baseline repo-wide; intersect the failing-path delta with owned paths; confirm EMPTY; disclose foreign red verbatim. Note `pnpm test:fault` is currently red at j4 for a reason that is NOT this task's — an over-broad probe tripped by task-2411ed9c — so disclose and do not chase it. (4) Adversarial review: does @moe/store import anything from apps/daemon (taskRail 2 forbids it absolutely)? Did I add a dependency on @moe/core rather than the decision from step 2? Does any lifecycle or business reasoning now live in the store (taskRail 3)? Can a partial install leave a mixed state? Does the conformance check still reject unknown tables and metadata? Does the migration preserve prior rows on a populated database? Is the published surface real at runtime rather than type-only? (5) Record BOTH decisions in completion notes: the dependency-direction decision with its justification (DoD 1), and the declared-duplicate codec with the follow-on that should retire the daemon's private copy. (6) Name the Clause 1 consumers: task-b6e3dd2af916490fb2bc4d375a530683 and task-2ff368fe2de44028b4a7d6ca89687933. (7) Commit by explicit pathspec over owned paths only; `git diff --cached --name-only` must list exactly those.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/store/src/recovery-install.ts",
        "packages/store/src/index.ts"
      ],
      "modifiedFiles": [
        "packages/store/src/recovery-install.test.ts"
      ],
      "note": "GATE FRESH AND LAST, after the commit: `pnpm --filter @moe/store typecheck` exit 0; `pnpm --filter @moe/store test` exit 0 — Test Files 36 passed (36), Tests 384 passed (384). Count grew by exactly one over the reviewed 383.\n\nCOMMIT 057ec1a, by explicit pathspec, ONE file: `git diff --cached --name-only` listed only packages/store/src/recovery-install.test.ts; `git log -1 --stat` confirms 1 file, +54/-1. Bytes proven landed, not assumed: `git rev-parse HEAD:packages/store/src/recovery-install.test.ts` = 4f5fdf665115286e3e519e091ec7b4307e27d6e7 and `git show HEAD:<path> | grep -c \"<new test name>\"` = 1. `git status --porcelain -- packages/store` empty afterwards.\n\nLINE CAPS, hand-measured with `grep -c ''`: recovery-install.ts 191, -codec.ts 211, -contracts.ts 167, sqlite-schema-recovery-manifest.ts 36, sqlite-schema-manifest.ts 241, sqlite-schema-migration.ts 176, store-internals.ts 131, decision-ledger.ts 138, index.ts 84 — all unchanged and under 250. recovery-install.test.ts is 363, a TEST file; rail 5 scopes production sources, and siblings in this package run 413-681 (store-project-binding-tamper 413, command-decision-store 534, command-decision-integrity 568, backup-generation 603, sqlite-event-store-recovery 612, sqlite-event-store-core 681).\n\nFOREIGN RED, path-attributed by measurement, both disclosed:\n1. `pnpm typecheck` is now exit 1 — it was 0 when QA reviewed. One error: apps/daemon/src/coordination/coordination-adapter.test.ts(374,32) TS7006 'item' implicitly has an 'any' type. That file is UNTRACKED (`git status --porcelain` returns `??`) and `git log -1 -- <path>` returns NOTHING, so it has never been committed: a live peer's in-flight TDD file in a shared worktree, landed after the review. Not an owned path and not reachable from packages/store.\n2. `pnpm test` exit 1 — Test Files 2 failed | 232 passed (234), Tests 2 failed | 4711 passed | 1 skipped. Same two foundation absence-probe ratchets QA verified: j1-linear `incident:hot-claim-loop-on-gated-work` and j4-replan-stale `incident:stale-assets-refuse-handshake`. Authorship settled by READ, not reasoning — foundation-incident-schedules.ts declares probe:scheduler-hot-claim-admission with package scope \"@moe/scheduler\" (:57-61) and probe:contracts-distribution-handshake with \"@moe/contracts\" (:74-78). Neither scopes @moe/store. Total tests moved 4710 -> 4711, exactly my one new test.\nIntersection of both failing-path sets with my diff (one store test file): EMPTY.\n\nADVERSARIAL SELF-REVIEW of the new test:\n- Vacuity: three independent ways it can fail loudly — encode refusal throws with layer/code, `expect(changed.changes).toBe(1)` catches a no-op UPDATE, and drill A empirically proved the final assertion is the one that fires.\n- Answered by an earlier guard: prevented by construction. The positive control pins the injected digest byte-identical to what production writes for that record, so the bytes are provably well-formed at the codec layer; and the test asserts the exact code AND layer, so a codec-layer answer would fail rather than pass as coverage.\n- Resource discipline: the tamper DatabaseSync and every store handle close in `finally`; the temp directory goes through databasePath()/mkdtempSync under the OS temp root and is removed in the existing afterEach. The control store is ephemeral, so it needs no path. Nothing under the repo.\n- The control uses a FRESH ephemeral database deliberately: installing REF_X into PENDING beside the existing ACTIVE/REF_X row would hit the UNIQUE incarnation_ref constraint and refuse with RECOVERY_INSTALL_INCARNATION_CONFLICT, which would prove nothing about the slot check.\n- Rails: the test imports only \"./index.js\" and \"./recovery-install-contracts.js\" — no @moe/ specifier, nothing from apps/daemon (rail 2), and no lifecycle reasoning (rail 3). Production code untouched, so the step-2 no-edge decision and the store's zero-dependency manifest are unchanged.\n- Published surface not widened: encodeRecoveryBinding was already exported at index.ts:80.\n\nDecisions restated for the record: (1) dependency direction — NO EDGE; the store gains nothing from @moe/core because it holds rows, bytes and a transaction and never reasons about lifecycle, and the composition point (apps/daemon depending on both) already exists. (2) The daemon's private encodeBinding/decodeBinding in recovery-incarnation-anchor.ts is a DECLARED duplicate, forced by rail 2 since that module is a downstream consumer of @moe/store; retirement follow-on requested from an architect, sequenced after task-b6e3dd2a. Clause 1 consumers: task-b6e3dd2af916490fb2bc4d375a530683 and task-2ff368fe2de44028b4a7d6ca89687933.",
      "startedAt": "2026-08-11T08:48:31.257Z",
      "completedAt": "2026-08-11T08:51:35.490Z"
    }
  ],
  "status": "DONE",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 1,
  "reopenReason": "REJECT on DoD 6 only, proven by drill. Everything else verified and strong. FULL EVIDENCE IN comment-73ee5c2504534d67b6680b97b9eb225e — read it first; it also CLOSES DoD 1 so you do not redo it.\n\nTHE GAP. I deleted BOTH slot cross-checks from rowMatchesBinding, recovery-install.ts:60-61:\n    binding.slot === querySlot &&\n    requireRowString(row, \"slot\") === binding.slot &&\nthen ran the WHOLE store suite: Test Files 36 passed (36), Tests 383 passed (383). ZERO failures. Restored via git checkout, hash-verified.\n\nTwo production guards you added are unasserted. Not hypothetical: your step-5 note claims \"A ROW FILED UNDER THE WRONG SLOT IS REFUSED ... the row content is the authority rather than the key.\" Nothing backs that sentence. Your divergence test drifts THREE of the FOUR values the row duplicates; `slot` is the fourth, and it is the one that claim rests on.\n\nConstructible, and production refuses it correctly today, so you have a fixture. Install ACTIVE/REF_X, then via a second raw DatabaseSync:\n  UPDATE recovery_bindings SET binding_bytes = ?, binding_digest = ? WHERE slot = 'ACTIVE'\nusing bytes+digest of an otherwise identical record encoded for slot 'PENDING'. The other three columns and the digest still agree with the bytes, so neither the codec gate nor your three comparisons can see it — only the slot cross-check can. Reopen, read ACTIVE:\n  outcome REFUSED, code RECOVERY_BINDING_ROW_DIVERGED, layer RECOVERY_INSTALL_TRANSACTION\n\nTO PASS: add that case at recovery-install.test.ts:251 (or a named sibling) asserting exact code AND layer. Re-run the drill — deleting both slot comparisons must redden a NAMED test — restore, hash-verify, record the failing assertion. Re-run the DoD 7 gate. Do NOT touch production code; it is correct.\n\nVERIFIED GREEN, do not redo: DoD 7 gate reproduced exactly. Repo typecheck 0; the only 2 pnpm test reds are foreign probes, owned-path intersection EMPTY. Rails 2/3 clean. All EIGHT reason codes assert code AND layer.",
  "createdBy": "HUMAN",
  "parentTaskId": null,
  "priority": "CRITICAL",
  "order": 13.02,
  "comments": [
    {
      "id": "comment-689e76ccc1bd4ac2afc2e045a500c863",
      "author": "architect-5aba94da",
      "content": "FOR @architect-8ee37119 -- you hold this task; I promoted it from BACKLOG and had measured it before you claimed. Handing over the evidence so you do not re-derive it. Nothing here is a decision imposed on you; it is measurement plus my reasoning, and the call is yours.\n\nPREMISES RE-VERIFIED AT HEAD 2026-08-11, all three hold:\n1. packages/store/package.json has NO \"dependencies\" field. Keys exactly: name, version, private, type, exports, scripts. It DOES declare `typecheck` and `test`, so DoD 7's legs exist.\n2. `grep -riln \"incarnation|key_epoch|keyEpoch\" packages/store/src/` returns NOTHING.\n3. sqlite-schema-manifest.ts (234 lines) holds exactly the twelve tables the description names; no recovery/incarnation/key-epoch/credential/grant row.\n\nHARD DEPENDENCY IS SATISFIED -- I checked before promoting. task-eff945fc (order 13.01, durable OS-protected recovery key provider) is DONE, 10/10, failedDodItems [], completed 2026-08-10T08:45Z, and apps/daemon/src/recovery/recovery-key-epoch-store.ts exists. So an epoch does survive restart, which was the stated reason 13.01 gated this task.\n\nTHE ARCHITECTURE QUESTION -- MY ANSWER IS (c), AND (a) IS A TRAP.\nThe measurements that decide it:\n- Layering: @moe/contracts deps=[], @moe/store deps=[], @moe/core deps=[@moe/contracts]. @moe/store is the LOWEST persistence package; @moe/core is domain.\n- No cycle exists today in either direction: packages/store/src imports no @moe/core, packages/core/src imports no @moe/store. So (a) would NOT fail as a technical cycle -- which is exactly what makes it tempting and worth refusing deliberately.\n- Rail 3 already removes the only reason to want it: \"the store holds rows and transactions; reduceProject remains @moe/core's.\" The store never needs reduceProject, so it never needs @moe/core.\n- DECISIVE: apps/daemon ALREADY declares BOTH @moe/store AND @moe/core, in package.json dependencies AND as pnpm-lock.yaml importer entries (link:../../packages/...). The composition point already exists.\nSo: @moe/store takes NO new dependency and package.json is NOT modified. The store defines its own persistence row shape and atomic transaction; the daemon maps its domain binding onto it.\n\nHOW DoD 1 IS STILL SATISFIED, not narrowed: the \"resulting edge\" is the EXISTING apps/daemon -> @moe/store edge. Prove it three ways anyway -- manifest (verified), lockfile importer (verified), and RUN the compiled bare-specifier probe and delete it in the same command even though the edge pre-exists, because rail 4 exists precisely because that check was skipped elsewhere. Record in completion notes WHY no manifest line was added, or a reviewer reads the absence as an omission.\n\n(b) is out on scope grounds: moving a codec into @moe/contracts means writing packages/contracts/src and editing apps/daemon's RecoveryIncarnationBinding, both outside this task's owned paths.\n\nPRIOR ART IS THE CONSUMER, NOT A DUPLICATE. apps/daemon/src/recovery/recovery-incarnation-anchor.ts (217 lines) ALREADY does `import type { CommandDecisionRecord, SqliteEventStore } from \"@moe/store\"` and exports readAnchoredIncarnation (:147) / anchorIncarnation (:186). RecoveryIncarnationBinding is daemon-local at recovery-incarnation-contract.ts:68 and is a DOMAIN shape; what this task adds is a PERSISTENCE shape. Different layers, not two codecs -- but say so in the module doc, or someone later \"unifies\" them into the silent duplicate the description warns about.\n\nMECHANICS YOU CAN REUSE RATHER THAN INVENT:\n- Schema is already a versioned ladder: SCHEMA_V1_OBJECT_SQL (:3), SCHEMA_V2_OBJECT_SQL (:87), current SCHEMA_OBJECT_SQL (:180). sqlite-schema-migration.ts (157 lines) steps with validateMigrationSource, writes store_metadata schema_manifest_version, sets PRAGMA user_version, dispatches in migrateLocked (:136). Add the next step in that exact shape; update BOTH version records or a later migration dispatches off a stale one.\n- Tables are STRICT with CHECK constraints. A new table missing STRICT accepts type-coerced junk every other table rejects, and no test will notice.\n- Transaction idiom already exists: event-ledger-transaction.ts:124/129 applyWithinCommit. Don't invent a second one.\n\nTHREE TRAPS I'd flag:\n1. DoD 2 cuts both ways -- the new table must be ACCEPTED while unknown tables/metadata are still REJECTED. Loosening conformance to admit the new table silently admits anything. Use a genuinely novel table name for the rejection case.\n2. DoD 5 is the most fakeable: build the DB at the PRIOR user_version and populate it BEFORE migrating. A fresh DB created at the new version and asserted non-empty proves nothing and would pass even if the migration dropped every table.\n3. DoD 3's atomicity needs an injected failure MID-transaction so rollback actually executes. Happy path plus a validation refusal never exercises atomicity.\n\nConsumer edge for the pure-package clause: task-b6e3dd2a and task-2ff368fe (13.03). Record both ids.",
      "timestamp": "2026-08-11T08:00:11.387Z"
    },
    {
      "id": "comment-73ee5c2504534d67b6680b97b9eb225e",
      "author": "qa-f3560083",
      "content": "QA FULL EVIDENCE — qa-f3560083, reviewed commit e426e5e (store bytes unchanged since; `git diff --name-only e426e5e..HEAD -- packages/store` empty, e426e5e still an ancestor of HEAD 531fb92).\n\nVERDICT: REJECT on DoD 6 only. One proven gap, one test case wide. Everything else verified and strong.\n\n=== WHAT I RE-RAN MYSELF (not trusted from notes) ===\n1. DoD 7 gate, fresh: `pnpm --filter @moe/store typecheck` exit 0; `pnpm --filter @moe/store test` exit 0 — Test Files 36 passed (36), Tests 383 passed (383). Reproduces task.verification exactly.\n2. Repo-wide, rail 3: `pnpm typecheck` exit 0 (19/19). `pnpm test` exit 1 — Test Files 2 failed | 232 passed (234), Tests 2 failed | 4710 passed | 1 skipped. Both reds are j1-linear `incident:hot-claim-loop-on-gated-work` and j4-replan-stale `incident:stale-assets-refuse-handshake`, both `expected { kind: 'PASS_EXPECTED' } to deeply equal { …(3) }`. AUTHORSHIP SETTLED BY READ, not by reasoning: foundation-incident-schedules.ts:59 scopes `@moe/scheduler` and :77 scopes `@moe/contracts`. Neither scopes @moe/store, and this commit touches only packages/store/src. Intersection of the failing-path delta with owned paths is EMPTY. Your disclosure was accurate.\n3. Commit scope: 24 files, all under packages/store/src, zero outside. package.json genuinely unmodified — consistent with the recorded no-edge decision.\n4. Line caps, `grep -c ''`: recovery-install.ts 191, -codec.ts 211, -contracts.ts 167, sqlite-schema-recovery-manifest.ts 36, sqlite-schema-manifest.ts 241, sqlite-schema-migration.ts 176, store-internals.ts 131, decision-ledger.ts 138, index.ts 84. All under 250. sqlite-event-store.ts 351 and store-contracts.ts 297 are pre-existing over-target and under the 400 split threshold; your disclosure stands.\n5. Rail 2: `grep -rn 'apps/daemon|from \"@moe/' packages/store/src/` exits 1. Zero upward or cross-package imports. Rail 3: only a comment mentions reduceProject; no code.\n6. Reason-code noun count: all EIGHT of RECOVERY_INSTALL_REASON_CODES are asserted somewhere with BOTH code and layer. Nothing silently unrecorded.\n\n=== DoD 1 — CLOSED BY QA, NOT A FIX ITEM ===\nYour proof-of-negative was narrower than the DoD text, and architect-5aba94da's comment had already said how to close it: the \"resulting edge\" is the pre-existing apps/daemon -> @moe/store edge, proven three ways anyway. Your entrypoint probe imports `./index.ts` RELATIVE — that proves the barrel holds runtime values, but it never exercises the exports map or workspace resolution, which is exactly what taskRail 4 distinguishes. I ran the missing leg so you do not have to:\n- manifest: apps/daemon/package.json declares `\"@moe/store\": \"workspace:*\"` ✓\n- lockfile importer: pnpm-lock.yaml `apps/daemon:` -> `'@moe/store': specifier workspace:* / version link:../../packages/store` ✓\n- compiled bare-specifier probe: wrote apps/daemon/src/qa-probe-recovery-install.ts importing all 8 values + 2 prototype methods + 6 types from bare `\"@moe/store\"`, `pnpm --filter @moe/daemon typecheck` exit 0, deleted in the same command ✓\n- NEGATIVE CONTROL, so the probe is provably live and the surface provably no wider than claimed: importing recoveryBindingDigest / isRecoveryBindingSlot / RecoveryInstallStore fails with three `error TS2724: '\"@moe/store\"' has no exported member named ...`, exit 1, deleted in the same command ✓\nRecord these commands in your completion notes. Do not re-run them.\n\n=== MY OWN MUTATION DRILLS (independent of your seven) ===\nAll restored with `git checkout --` and hash-verified; working tree clean for packages/store, apps/daemon, packages/testkit.\n\n(i) REPRODUCED your drill (b). Inserted `COMMIT; BEGIN IMMEDIATE` between the DELETE and the INSERT in writeBindingUnderLock. RED: recovery-install.test.ts > \"leaves the prior binding intact when an install fails mid-transaction\" — \"Error: expected FOUND, got ABSENT\". It failed at the STATE check in `found()`, not at the refusal assertions above it. DoD 3 is genuinely about durable state. hash f194d585... before and after.\n\n(ii) NEW — the DoD 2 half your drill (a) did not cover. First attempt was a BAD DRILL and I say so: changing `metadataRows.length !== 1` to `< 1` left the test GREEN, but that indicts the mutation, not the test — rows come back ORDER BY key, \"recovery_authority\" sorts before \"schema_manifest_version\", so metadataRows[0] was the injected row and the key check answered first. Re-drilled with a proper isolation (replace the exact-one guard with a `.find()` on the manifest key, so extra rows are tolerated while the manifest row is still checked). RED: store-schema-v4-migration.test.ts > \"still refuses unknown store_metadata after the migration\" — \"AssertionError: conformance still rejects extra metadata: expected undefined to be an instance of DurableStoreError\". DoD 2's metadata direction is live.\n\n=== THE GAP — DoD 6, drill (iii) ===\nDeleted BOTH slot cross-checks from rowMatchesBinding (recovery-install.ts:60-61):\n    binding.slot === querySlot &&\n    requireRowString(row, \"slot\") === binding.slot &&\nRan the WHOLE store suite, not a focused file: **Test Files 36 passed (36), Tests 383 passed (383).** Zero failures. Restored, hash-verified.\n\nSo two production guards you added are unasserted by any test. This is not hypothetical rot — your step-5 note makes a positive factual claim about them: \"A ROW FILED UNDER THE WRONG SLOT IS REFUSED, not answered with: readRecoveryBinding compares the DECODED slot against the queried slot, so the row content is the authority rather than the key.\" Nothing in the suite backs that sentence.\n\nI verified the claim is TRUE in production and that the scenario is constructible, so you have a working fixture. Probe run outside the repo against the real surface:\n  install ACTIVE/REF_X, then through a second raw DatabaseSync:\n  UPDATE recovery_bindings SET binding_bytes = ?, binding_digest = ? WHERE slot = 'ACTIVE'\n  with the bytes+digest of an otherwise identical record encoded for slot 'PENDING'\n  (incarnation_ref, key_epoch_ref and binding_codec_version columns all still agree with the bytes,\n   and the digest agrees with the bytes, so neither the codec gate nor your three column\n   comparisons can see it — only the slot cross-check can)\n  reopen, readRecoveryBinding(\"ACTIVE\") ->\n  outcome: REFUSED | code: RECOVERY_BINDING_ROW_DIVERGED | layer: RECOVERY_INSTALL_TRANSACTION\n\nNote the shape of the hole: your divergence test drifts THREE of the FOUR values the row duplicates (incarnation_ref, key_epoch_ref, binding_codec_version). `slot` is the fourth and it is the one your design claim rests on — \"the bytes are the authority, not the index\" is precisely a statement about the slot KEY.\n\n=== TO PASS ===\nAdd the wrong-slot case to recovery-install.test.ts > \"refuses a row whose indexed columns diverge from the bytes they index\" (or a named sibling), asserting code RECOVERY_BINDING_ROW_DIVERGED and layer RECOVERY_INSTALL_TRANSACTION. Then re-run drill (iii) — deleting both slot comparisons must redden a NAMED test — restore, hash-verify, and record the exact failing assertion. Re-run the DoD 7 gate. Nothing else needs to change; do not touch the production code, it is correct.",
      "timestamp": "2026-08-11T08:38:32.428Z"
    },
    {
      "id": "comment-4a83151d18f540168159d922dd914f49",
      "author": "architect-8ee37119",
      "content": "@worker-767ae903 @qa-f3560083 — the QA finding is correct and the root cause is a defect in MY plan. Fix it as QA specified, but read this first because it likely applies to MORE than the one guard they found.\n\nMY ERROR. Step 7 gave you a drill list of six items (a)-(f), derived from the DoD's stated properties: conformance loosening, transaction partiality, migration row loss, malformed codec bytes, layer-only change, type-only publication. That list was written BEFORE the implementation existed, so it could only cover guards the DoD implied — not guards you would go on to write. `rowMatchesBinding`'s four-way cross-check is your design, and it is a good one; my list simply had no entry for it.\n\nDoD 6 says \"mutating EACH production guard turns a NAMED focused test red\". Each means each guard that EXISTS in the finished code, not each item an architect listed in advance. **Treat a plan's drill list as a FLOOR, never a ceiling.** The authoritative drill set is enumerated from the implementation at the end, not from the plan at the start.\n\nCONCRETE CONSEQUENCE, and why this is worth more than the one fix: QA found the `slot` cross-check by deleting it and watching 36 files / 383 tests stay green. Before you resubmit, do that enumeration yourself across everything you added — walk `recovery-install.ts` and the migration, list every comparison, guard clause and refusal branch you wrote, and for each ask \"which NAMED test reddens if I delete this?\" Any guard where the answer is \"none\" is the same defect QA just found, and there may be more than one. The slot check was the fourth of four duplicated values; check the other guards that share that shape.\n\nQA's fixture is exact and I would not improve on it: install ACTIVE/REF_X, then via a second raw DatabaseSync write the bytes+digest of a record encoded for slot 'PENDING' into the ACTIVE row, leaving the other three columns and the digest agreeing with the bytes. Neither the codec gate nor the three existing comparisons can see it — only the slot cross-check can. Assert exact code RECOVERY_BINDING_ROW_DIVERGED at layer RECOVERY_INSTALL_TRANSACTION, then re-run the drill and confirm deleting both cross-checks now reddens that named case.\n\nEverything else in the rejection is clean — QA verified the rest and explicitly closed DoD 1 so you do not redo the dependency-direction decision. My step-5 note asserting \"a row filed under the wrong slot is refused ... the row content is the authority rather than the key\" was a claim with nothing behind it; your fix is what makes that sentence true.",
      "timestamp": "2026-08-11T08:44:50.731Z"
    },
    {
      "id": "comment-5529848efb3147d8bdbb0a00bdd946c4",
      "author": "qa-f3560083",
      "content": "QA REOPEN-1 VERIFICATION — qa-f3560083. APPROVED. The DoD 6 gap is closed. First-round evidence stands in comment-73ee5c2504534d67b6680b97b9eb225e; this covers only what changed.\n\nTHE FIX. Commit 057ec1a, one file, +54/-1, packages/store/src/recovery-install.test.ts. ZERO production bytes — confirmed independently, not taken from the note: after my drill and restore, `git hash-object packages/store/src/recovery-install.ts` = f194d585ffc9b996ed133a19b569aafd63c8aeba, byte-identical to the hash I recorded before the rejection. So everything I verified in round one still applies unchanged.\n\nGATE, re-run myself: `pnpm --filter @moe/store typecheck` exit 0; `pnpm --filter @moe/store test` exit 0 — Test Files 36 passed (36), Tests 384 passed (384). Was 383; grew by exactly the one test.\n\nDRILL (iii) REPRODUCED INDEPENDENTLY, whole store suite rather than a focused file:\n  both cross-checks deleted from rowMatchesBinding ->\n  FAIL packages/store/src/recovery-install.test.ts > recovery install transaction surface >\n       \"refuses a row whose stored bytes name a slot other than the one it is filed under\"\n  AssertionError: expected true to be false\n  Test Files 1 failed | 35 passed (36) / Tests 1 failed | 383 passed (384)\nRestored via git checkout, hash back to f194d585. Critically, it reddens on the REFUSAL assertion (`expect(refused.ok).toBe(false)`), not on the positive control above it — the control fails with \"expected FOUND, got ...\", so I can tell which assertion answered (mem:mutation-drill-red-on-wrong-assertion). The guard is now genuinely asserted.\n\nTHE POSITIVE CONTROL, added unprompted, is better than what I asked for. My recipe only required asserting the refusal. Installing the same record under the slot it names, pinning its digest byte-identical to the injected one, and reading it back FOUND turns \"this fixture is well-formed at the codec layer\" into a measurement — so the refusal below cannot be an earlier guard answering a malformed value. That is precisely the defect class the rejection was about, applied one level up.\n\nTHE DISCLOSED SURVIVING MUTANTS — WEIGHED, AND NOT A SECOND GAP. The worker reported that deleting either operand ALONE leaves 384/384 green, and asked me to judge rather than accept it. Measured: `rowMatchesBinding` has exactly one call site (recovery-install.ts:108), and the row reaching it comes from `SELECT ... WHERE slot = ?` bound to the same validated `slot` (:98), on a STRICT table where slot is TEXT PRIMARY KEY CHECK IN ('ACTIVE','PENDING'). So `row.slot === querySlot` holds for every reachable row, which makes operand B true exactly when operand A is. Two equivalent mutants over one property; neither of us can name an input a survivor admits. RULING: keep both. The redundancy costs nothing and stops being redundant if that SELECT ever stops being an equality on the same bound value; deleting an operand to kill a mutant would trade a real invariant for a metric. Correct call not to touch production under a test-only fix.\n\nFOREIGN RED — RESOLVED ITSELF, WHICH CONFIRMS THE ATTRIBUTION. The worker disclosed `pnpm typecheck` exit 1 with one error, apps/daemon/src/coordination/coordination-adapter.test.ts(374,32) TS7006, noting the file was untracked and never committed. I verified that (`git log -1 -- <path>` empty, `git status` `??`) and then, minutes later, re-ran: `pnpm typecheck` is exit 0 and the file is gone from the working tree. A live peer's in-flight TDD file in the shared worktree, exactly as attributed — the red moved without anyone touching packages/store. `pnpm test`'s two remaining reds are the same foundation probes scoped @moe/scheduler and @moe/contracts; owned-path intersection EMPTY.\n\nLINE CAPS unchanged and re-measured: all production files under 250. recovery-install.test.ts is 363 lines and is a TEST file — rail 5 scopes production sources, and siblings in this package run 413-681, so no issue.\n\nDoD 1 remains closed by QA from round one (manifest, lockfile importer, and the compiled bare-specifier probe from apps/daemon with its TS2724 negative control). Correctly not re-run; the commands are now recorded in the step notes as instructed.\n\nAll 7 DoD verified. Approving.",
      "timestamp": "2026-08-11T08:55:29.594Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-08-10T07:31:27.898Z",
  "updatedAt": "2026-08-11T08:56:07.766Z",
  "metrics": {
    "firstClaimAt": "2026-08-11T07:57:18.816Z",
    "plannedStepCount": 8,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 16,
    "rejectCount": 1,
    "reopenCount": 1,
    "doneAt": "2026-08-11T08:56:07.766Z",
    "wallClockMs": 3528950
  },
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6",
    "step-7",
    "step-8"
  ],
  "failedDodItems": [
    {
      "item": "Mutating each production guard turns a NAMED focused test red; drills run, reverted, exact failing assertions recorded.",
      "rejectedAt": "2026-08-11T08:41:39.106Z",
      "rejectedBy": "qa-f3560083"
    }
  ],
  "planSubmittedAt": "2026-08-11T08:00:19.936Z",
  "planningNotes": {
    "approachesConsidered": "THE ARCHITECTURE QUESTION, which the task requires be settled and justified rather than assumed. It offers (a) @moe/store declares a dependency on @moe/core, (b) the binding codec lives in a lower shared package both can depend on, or (c) the anchor does not belong in @moe/store at all.\n\nMY MEASURED ANSWER IS NONE OF THEM AS FRAMED: **no dependency edge is required.** The question's premise is that the store needs @moe/core because core owns reduceProject(recovery.restore_quiesce). It does not. taskRail 3 draws the line explicitly — \"The store holds rows and transactions; reduceProject remains @moe/core's\" — and this task lands exactly three things: recovery binding ROWS, a byte-level CODEC, and an atomic TRANSACTION surface. None of the three performs lifecycle reasoning, so none needs the reducer. Composing reducer with store is the DAEMON's job and is already scoped to task-2ff368fe (13.03), which depends on both.\n\nOPTION (a) REJECTED: a storage package depending on business logic inverts the layering and drags core's whole transitive graph into the store. The task itself warns it \"is a real layering change and must be justified, not slipped in as a manifest line\" — and nothing here justifies it.\n\nOPTION (c) REJECTED: durable rows plus an atomic transaction is precisely what a store is for. Moving them out would leave the installer with nowhere to persist.\n\nOPTION (b) HELD AS FALLBACK, with the layering measured so it is safe if I am wrong: @moe/contracts has NO dependencies (root layer), @moe/core depends on it, @moe/store depends on nothing. So store -> contracts is acyclic and mirrors how core already layers. If the worker finds the store genuinely needs a shared symbol, they declare @moe/contracts — never @moe/core — and then the full three-way edge proof applies.\n\nREUSING THE DAEMON'S CODEC — IMPOSSIBLE, not merely undesirable. apps/daemon/src/recovery/recovery-incarnation-anchor.ts holds encodeBinding (:40) and decodeBinding (:88) as MODULE-PRIVATE functions (only readAnchoredIncarnation :147 and anchorIncarnation :186 are exported), inside a module that imports `{ CommandDecisionRecord, SqliteEventStore } from \"@moe/store\"`. It is a downstream consumer of the store, so reuse would require importing upward, which taskRail 2 forbids absolutely. The store's codec is therefore a second implementation BY NECESSITY — planned as a DECLARED duplicate with a named retirement path rather than a silent one.\n\nHAND-ROLLING BEGIN/COMMIT — REJECTED. The store already owns transaction discipline in decision-ledger-transaction.ts and event-ledger-transaction.ts; a second one would diverge.",
    "codebaseInsights": "MEASURED ON DISK 2026-08-11 before planning.\n\nTHE HARD DEPENDENCY IS SATISFIED, and this task carries a standing order to block if it is not. task-eff945fc (13.01, durable OS-protected recovery key provider) is DONE at 10/10 and its deliverable is committed: apps/daemon/src/recovery/recovery-key-provider.ts/.js, -contract.ts/.js, .node.ts/.js and both test files all appear in `git ls-tree -r HEAD`. The promoting architect's reason did not mention this check, so I made it explicitly.\n\nLAYERING, which decides the architecture question: @moe/contracts dependencies = NONE (root layer); @moe/core = { @moe/contracts }; @moe/store = NONE. So contracts is the common lower layer and store -> contracts would be acyclic, while store -> core would invert the layering.\n\nTHE THREE PROMOTION PREMISES ALL HOLD: no \"dependencies\" key in packages/store/package.json; zero grep hits for incarnation/key_epoch/keyEpoch under packages/store/src; sqlite-schema-manifest.ts (234 lines) holds exactly the twelve named tables and no recovery/incarnation/key-epoch/credential/grant row.\n\nMIGRATION MACHINERY ALREADY EXISTS — compose it: sqlite-schema-migration.ts, sqlite-schema-bootstrap.ts, sqlite-schema-conformance.ts, sqlite-schema-integrity.ts. Transaction machinery too: decision-ledger-transaction.ts, event-ledger-transaction.ts.\n\nPRIOR-ART CODEC IS UNREACHABLE FROM THE STORE: recovery-incarnation-anchor.ts's encodeBinding/decodeBinding are module-private and the module imports @moe/store, making it a downstream consumer. A second codec is unavoidable; it must be declared, not silent.\n\nLINE PRESSURE: sqlite-schema-manifest.ts is 234 against a 250 target, so the new tables may force a split rather than an edit.\n\nKNOWN FOREIGN RED to disclose and not chase: `pnpm test:fault` is red at j4 (incident:stale-assets-refuse-handshake) because an over-broad probe in packages/testkit/src/foundation/foundation-incident-schedules.ts is tripped by task-2411ed9c's landing. Unrelated to this package.",
    "risks": "1. ADDING THE @moe/core EDGE BECAUSE THE QUESTION OFFERED IT. Option (a) is listed first and reads as sanctioned; the task explicitly warns it must be justified rather than slipped in as a manifest line. The store needs rows, bytes and a transaction — none of which is business logic. If a core symbol seems necessary, that is a signal the store is about to absorb lifecycle reasoning taskRail 3 forbids.\n\n2. A MIGRATION THAT BUYS ROOM BY LOOSENING CONFORMANCE. DoD 2 wants the new shape accepted AND unknown tables still rejected. Relaxing the exactness check is the invisible way to make a migration pass, so both directions must be pinned. Drill (a) targets it.\n\n3. TESTING THE MIGRATION ONLY ON AN EMPTY DATABASE. DoD 5 exists because that proves nothing about the upgrade path real deployments take. Write rows through the production surface first, then migrate, then read back.\n\n4. A PARTIAL INSTALL LEAVING A MIXED STATE. DoD 3's property is not \"it returned an error\" but \"it refused AND prior state is intact\". Inject the failure between write and commit; drill (b) proves the assertion is live.\n\n5. A SECOND CODEC LANDING SILENTLY. It is unavoidable here for layering reasons, but the staleness rail's failure mode is the SILENT duplicate. Declare it with a named retirement follow-on.\n\n6. TYPE-ONLY PUBLICATION. `--experimental-strip-types` erases `export type`, so a type-only export satisfies tsc and leaves nothing importable — exactly how the JetBrains adapter got blocked earlier. Assert runtime values through the bare specifier.\n\n7. LINE CAP ON A NEAR-FULL FILE. sqlite-schema-manifest.ts at 234/250 may not absorb the new tables; split rather than overflow.\n\n8. IN-REPO SQLITE ARTEFACTS. Use mkdtemp under the OS temp root with cleanup that runs on failure; this host is win32."
  },
  "budget": {
    "wallClockMs": 7200000
  },
  "planApprovedAt": "2026-08-11T08:00:21.985Z",
  "contextFetchedBy": [
    "worker-767ae903",
    "qa-f3560083"
  ],
  "workStartedAt": "2026-08-11T08:00:40.493Z",
  "reviewStartedAt": "2026-08-11T08:53:00.190Z",
  "verification": {
    "command": "pnpm --filter @moe/store typecheck && pnpm --filter @moe/store test",
    "exitCode": 0,
    "outputTail": "$ tsc --project tsconfig.json\ntypecheck=0\n$ vitest run --root ../.. packages/store/src\n\n RUN  v4.1.10 D:/projexts/moe-next\n\n\n Test Files  36 passed (36)\n      Tests  384 passed (384)\n   Start at  11:50:42\n   Duration  3.80s (transform 8.80s, setup 0ms, import 14.30s, tests 14.15s, environment 3ms)\n\ntest=0",
    "reportedAt": "2026-08-11T08:53:00.190Z"
  },
  "filesModified": [
    "packages/store/src/store-schema-v4-migration.test.ts",
    "packages/store/src/sqlite-schema-recovery-manifest.ts",
    "packages/store/src/sqlite-schema-manifest.ts",
    "packages/store/src/sqlite-schema-migration.ts",
    "packages/store/src/store-internals.ts",
    "packages/store/src/recovery-install.test.ts",
    "packages/store/src/index.ts",
    "packages/store/src/recovery-install-entrypoint.test.ts",
    "packages/store/src/recovery-install-entrypoint-worker.mjs",
    "packages/store/src/recovery-install.ts"
  ],
  "reviewCompletedAt": "2026-08-11T08:56:07.766Z",
  "rejectionDetails": {
    "failedDodItems": [
      "Mutating each production guard turns a NAMED focused test red; drills run, reverted, exact failing assertions recorded."
    ],
    "issues": [
      {
        "type": "test_failure",
        "file": "packages/store/src/recovery-install.ts",
        "line": 60,
        "description": "Unasserted production guard. Deleting both slot cross-checks in rowMatchesBinding (`binding.slot === querySlot` and `requireRowString(row, \"slot\") === binding.slot`) leaves the entire store suite green: 36 files / 383 tests, zero failures. DoD 6 requires mutating each production guard to redden a NAMED focused test."
      },
      {
        "type": "missing_feature",
        "file": "packages/store/src/recovery-install.test.ts",
        "line": 251,
        "description": "'refuses a row whose indexed columns diverge from the bytes they index' drifts three of the four duplicated values (incarnation_ref, key_epoch_ref, binding_codec_version) and omits `slot`. Add a case writing the bytes+digest of a record encoded for slot 'PENDING' into the ACTIVE row via a raw second DatabaseSync, other three columns and digest still agreeing with the bytes, asserting RECOVERY_BINDING_ROW_DIVERGED at RECOVERY_INSTALL_TRANSACTION. Verified constructible and refused today."
      },
      {
        "type": "other",
        "file": "packages/store/src/recovery-install-entrypoint-worker.mjs",
        "line": 3,
        "description": "Not a fix item; recorded so your notes are accurate. This probe imports './index.ts' RELATIVE, proving the barrel holds runtime values but never exercising the exports map or workspace resolution - the distinction taskRail 4 draws. QA ran the missing bare-specifier leg from apps/daemon: positive typecheck exit 0, negative control on the three withheld symbols fails TS2724 exit 1, both deleted in the same command. DoD 1 satisfied; copy these into your notes rather than re-running."
      }
    ]
  },
  "rejectionHistory": [
    {
      "reason": "REJECT on DoD 6 only, proven by drill. Everything else verified and strong. FULL EVIDENCE IN comment-73ee5c2504534d67b6680b97b9eb225e — read it first; it also CLOSES DoD 1 so you do not redo it.\n\nTHE GAP. I deleted BOTH slot cross-checks from rowMatchesBinding, recovery-install.ts:60-61:\n    binding.slot === querySlot &&\n    requireRowString(row, \"slot\") === binding.slot &&\nthen ran the WHOLE store suite: Test Files 36 passed (36), Tests 383 passed (383). ZERO failures. Restored via git checkout, hash-verified.\n\nTwo production guards you added are unasserted. Not hypothetical: your step-5 note claims \"A ROW FILED UNDER THE WRONG SLOT IS REFUSED ... the row content is the authority rather than the key.\" Nothing backs that sentence. Your divergence test drifts THREE of the FOUR values the row duplicates; `slot` is the fourth, and it is the one that claim rests on.\n\nConstructible, and production refuses it correctly today, so you have a fixture. Install ACTIVE/REF_X, then via a second raw DatabaseSync:\n  UPDATE recovery_bindings SET binding_bytes = ?, binding_digest = ? WHERE slot = 'ACTIVE'\nusing bytes+digest of an otherwise identical record encoded for slot 'PENDING'. The other three columns and the digest still agree with the bytes, so neither the codec gate nor your three comparisons can see it — only the slot cross-check can. Reopen, read ACTIVE:\n  outcome REFUSED, code RECOVERY_BINDING_ROW_DIVERGED, layer RECOVERY_INSTALL_TRANSACTION\n\nTO PASS: add that case at recovery-install.test.ts:251 (or a named sibling) asserting exact code AND layer. Re-run the drill — deleting both slot comparisons must redden a NAMED test — restore, hash-verify, record the failing assertion. Re-run the DoD 7 gate. Do NOT touch production code; it is correct.\n\nVERIFIED GREEN, do not redo: DoD 7 gate reproduced exactly. Repo typecheck 0; the only 2 pnpm test reds are foreign probes, owned-path intersection EMPTY. Rails 2/3 clean. All EIGHT reason codes assert code AND layer.",
      "rejectedAt": "2026-08-11T08:41:39.106Z",
      "reopenCount": 1,
      "failedDodItems": [
        "Mutating each production guard turns a NAMED focused test red; drills run, reverted, exact failing assertions recorded."
      ],
      "issues": [
        {
          "type": "test_failure",
          "file": "packages/store/src/recovery-install.ts",
          "line": 60,
          "description": "Unasserted production guard. Deleting both slot cross-checks in rowMatchesBinding (`binding.slot === querySlot` and `requireRowString(row, \"slot\") === binding.slot`) leaves the entire store suite green: 36 files / 383 tests, zero failures. DoD 6 requires mutating each production guard to redden a NAMED focused test."
        },
        {
          "type": "missing_feature",
          "file": "packages/store/src/recovery-install.test.ts",
          "line": 251,
          "description": "'refuses a row whose indexed columns diverge from the bytes they index' drifts three of the four duplicated values (incarnation_ref, key_epoch_ref, binding_codec_version) and omits `slot`. Add a case writing the bytes+digest of a record encoded for slot 'PENDING' into the ACTIVE row via a raw second DatabaseSync, other three columns and digest still agreeing with the bytes, asserting RECOVERY_BINDING_ROW_DIVERGED at RECOVERY_INSTALL_TRANSACTION. Verified constructible and refused today."
        },
        {
          "type": "other",
          "file": "packages/store/src/recovery-install-entrypoint-worker.mjs",
          "line": 3,
          "description": "Not a fix item; recorded so your notes are accurate. This probe imports './index.ts' RELATIVE, proving the barrel holds runtime values but never exercising the exports map or workspace resolution - the distinction taskRail 4 draws. QA ran the missing bare-specifier leg from apps/daemon: positive typecheck exit 0, negative control on the three withheld symbols fails TS2724 exit 1, both deleted in the same command. DoD 1 satisfied; copy these into your notes rather than re-running."
        }
      ]
    }
  ],
  "completedAt": "2026-08-11T08:56:07.766Z",
  "reviewSummary": "APPROVED after reopen 1. DoD 6 closed; all 7 DoD verified. Round-1 evidence comment-73ee5c25; this round comment-5529848e.\n\nFIX: commit 057ec1a, one file, +54/-1, recovery-install.test.ts. ZERO production bytes — verified independently: after my drill+restore, recovery-install.ts hashes to f194d585ffc9b996ed133a19b569aafd63c8aeba, byte-identical to my pre-rejection baseline, so all of round 1 still applies.\n\nGATE re-run: typecheck 0; test 0 — 36 files / 384 tests (was 383).\n\nDRILL (iii) REPRODUCED against the whole store suite: both cross-checks deleted from rowMatchesBinding -> FAIL \"refuses a row whose stored bytes name a slot other than the one it is filed under\", AssertionError: expected true to be false, 1 failed | 383 passed (384). Reddens on the REFUSAL assertion, not the positive control (which fails \"expected FOUND, got ...\"), so I can tell which one answered. Restored, hash-verified.\n\nPOSITIVE CONTROL added unprompted, better than what I asked for: installing the same record under the slot it names and pinning its digest byte-identical to the injected one makes \"fixture well-formed at the codec layer\" a measurement, so the refusal cannot be an earlier guard answering.\n\nSURVIVING SINGLE-OPERAND MUTANTS — weighed on request, NOT a second gap. rowMatchesBinding has one call site (:108) fed by SELECT WHERE slot = ? bound to the same validated slot (:98), on a STRICT table with slot as TEXT PRIMARY KEY CHECK IN (ACTIVE,PENDING). So row.slot === querySlot always, making operand B true exactly when A is — equivalent mutants over one property. RULING: keep both. Right call not to touch production under a test-only fix.\n\nFOREIGN RED resolved itself, confirming attribution: the disclosed TS7006 in an untracked apps/daemon peer file is gone, repo typecheck now exit 0, nobody touched packages/store. pnpm test's 2 remaining reds are the foundation probes; owned-path intersection EMPTY."
}