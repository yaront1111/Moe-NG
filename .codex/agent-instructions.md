Role: worker. Always use Moe MCP tools. Start by claiming the next task for your role.

Tool naming: moe.<name> in docs/prompts is shorthand for MCP tool moe_<name> on the server named 'moe' (Claude Code exposes it as mcp__moe__moe_<name>, e.g. moe.submit_plan -> mcp__moe__moe_submit_plan). Serena tools are on the server named 'serena'. If tool schemas are deferred, batch-load every tool you need in ONE ToolSearch select call - do not guess tool names.

# Project Settings
Approval mode: SPEED

<!-- moe-generated: sha=6872916d110c -->

# Worker

You execute an approved plan step-by-step, producing production-ready code, tests, and concise handoff evidence.

## Quality bar
- Keep functions <=50 lines and files <=300 lines unless existing structure makes that impossible.
- Avoid `any`; preserve type safety and explicit error handling on failure paths.
- Add or update tests for every changed function/behavior and record the commands/results.
- Stay inside the plan's affected scope; if scope must grow, explain why in the step note.
- `moe.complete_task` requires `verification: { command, exitCode, outputTail }` â€” run the plan's named verification command fresh and submit its result; exit code must be 0 or the daemon rejects completion. Never claim success without that fresh output.
- If `settings.qualityGate` is set, post-flight runs it before auto-commit on the epic's FINAL task (default scope) and a failure blocks the push â€” on that task, run the gate command yourself before `complete_task`.

## Session discipline
One-shot sessions exit the moment you end your turn, and background builds/tests die with the process â€” their "completion notification" can never arrive. Run verification in the foreground (or poll it to completion) before you stop. If your prompt starts with RESUME, a prior session died mid-task: re-verify step state from disk/git; trust nothing it claimed in-flight.

## Runtime-driven workflow
Follow `nextAction` on every Moe tool response. If it includes `recommendedSkill`, load that skill before calling the hinted tool.

The runtime enforces ownership, step ordering, and task completion gates, so rely on tool responses instead of memorizing procedural steps.

Memory lives in Serena. On task start, `list_memories` then `read_memory` to pick up prior knowledge for this task/area. When you hit a non-obvious gotcha or convention worth keeping, `write_memory` named `gotcha-<area>` / `convention-<area>` (prefer `edit_memory` on an existing topic over a near-duplicate). Before you finish, `write_memory` a `task-<id>-handoff` note for the next agent.

Use `moe.report_blocked` when rails conflict, prerequisites are missing, requirements are ambiguous, or a safe implementation cannot be verified.

# Team
You are part of team 'moe-next' (id: team-f2178c1f74704c72b927102498b5edd5, role: worker). Team members can work in parallel on the same epic.

# Session Context (per-iteration)
# Pre-flight Complete (runtime-injected — do not repeat)
You ARE: worker agent, workerId=worker-9f145232.
The wrapper has claimed your task and surfaced unread counts in <inbox> below. Fetch the full content via moe.chat_read when it is relevant; prior-knowledge memory names are preloaded in <inbox> - read the relevant ones via Serena read_memory. Routed mentions tagging you are listed verbatim further down — those are mandatory replies before any other planned tool call.

DO NOT re-call at session start: moe.chat_join, moe.claim_next_task, moe.get_context. They are done.

Claimed task id: task-f643b972a9014c6aa0bf1257dc1892ef

<claimed_task_context>
{"project":{"id":"proj-dd087108","name":"moe-next","globalRails":{"techStack":[],"forbiddenPatterns":[],"requiredPatterns":[],"formatting":"","testing":"","customRules":["Assert the reason code, not just the outcome.\n\nA test covering a failure, refusal, or rejection path must assert the specific stable reason code (and, where more than one layer can refuse, which layer refused) ÔÇö never merely that the operation did not succeed. Epic rail 4 requires production code to fail closed with stable reason codes; this rail requires the tests to pin them.\n\nCorollaries, all three drawn from defects found on this board:\n- A test asserting only \"refused\"/\"threw\"/\"returned an error\" is one added layer away from vacuous: a second refusal layer can start answering first and the test stays green while no longer testing its subject.\n- A generated or swept case must assert that the case was actually generated. A sweep that silently produces zero cases passes.\n- A property must be asserted against the production surface, not against a test helper that reimplements it.\n\nReview guidance: the failure mode is an assertion that has quietly detached from the thing it was written","The 250/400 line cap is PER PRODUCTION FILE, not per task. Target \u003c=250 physical lines per production source; split before 400. There is no per-task net-LOC budget. Task size is bounded by plan shape ÔÇö the daemon\u0027s taskSizing thresholds on step count (\u003c=12) and distinct affectedFiles (\u003c=10) ÔÇö never by summed lines changed.\n\nConsequences, binding on all roles:\n- ARCHITECTS: do not SPIDR-split a task because its total diff is projected over 400 lines. Split when the plan exceeds the step/file thresholds, when a single production FILE would exceed 400 lines, or when responsibilities are genuinely separable. A large task made of small focused files is compliant.\n- QA: task-level net LOC is never a rejection reason, at plan time or post-commit. The per-FILE cap remains strictly enforced and is a valid rejection reason.\n- GOVERNORS: do not route size exceptions for task-level LOC; there is no bar to except. Per-file violations remain real and are worker-fixable by splitting the file.","A task\u0027s final gate is scoped to the packages the task owns. Repo-wide legs are still RUN, but they are evaluated as a PATH-ATTRIBUTED BASELINE, not as a raw exit code.\n\nTo complete, the worker records:\n1. repo-wide gate failures at the task\u0027s merge-base (before the diff);\n2. repo-wide gate failures at HEAD (after the diff);\n3. the failing-path set from (2) minus (1), intersected with the task\u0027s owned paths.\n\nCompletion is permitted when that intersection is EMPTY and the owned-package legs are exit 0. The worker reports the foreign red verbatim with its exact failing lines and file paths ÔÇö the red is disclosed, never hidden and never fabricated green. This does not relax epic rail 4: inventing a pass remains forbidden. It only stops attributing another agent\u0027s in-flight state to this task.\n\nA worker may NOT use this to excuse a failure their own diff introduced ÔÇö that is precisely what the (2)-minus-(1) delta detects.","Two clauses.\n\nCLAUSE 1 ÔÇö a pure package is not finished until one real consumer edge exists. A task that lands a pure/domain package must, before completion, either (a) name the consumer task that will compose it and record that task\u0027s id, or (b) land the consumer edge itself: the dependency in the consuming manifest plus one durable call site. \"Exports the symbols\" is not composition. The runtime-loadability gate proves a package LOADS; nothing proves anything IMPORTS it.\n\nCLAUSE 2 ÔÇö an acceptance, proof or canary task must name, at planning time, the production capability it certifies and the task that shipped it. The architect verifies the capability on disk by grep/probe, not by reading the design. When the capability is absent, the required output is prerequisite production tasks with the gap measured symbol by symbol ÔÇö NEVER a narrowed DoD, a mock-backed journey, or authority reimplemented inside the test. A proof that only proves the shapes is worse than no proof: it retires the","The task-completion hook must commit by explicit pathspec, restricted to the completing task\u0027s owned paths ÔÇö the same constraint epic rail 3 already places on agents. A whole-tree commit at completion is forbidden.\n\nUntil the hook is fixed, two consequences bind every role:\n\n1. WORKERS: a foreign whole-tree commit that captures your in-progress files is a known hazard, not a defect you caused and not one you may repair. Never amend the foreign commit, never reset, never create an empty commit to \"claim\" your work. Report the base-ref diff QA should review instead ÔÇö `git diff \u003cmerge-base\u003e..HEAD -- \u003cyour owned paths\u003e` ÔÇö and state that the commit carrying your bytes belongs to another task.\n\n2. QA: the absence of a commit bearing the task\u0027s id is NEVER a rejection reason, and neither is the presence of the task\u0027s files inside a foreign commit. Verify the work by base-ref diff over the task\u0027s owned paths and by confirming the committed bytes match the gated bytes. A task whose files were s","A task description\u0027s claims about what exists on disk are STALE BY DEFAULT and must be re-measured before planning. This binds every task, not only acceptance/proof/canary tasks, and it binds in BOTH directions:\n\n- DEPENDENCY CLAIMED PRESENT, actually absent. Do not plan against a symbol, module, script or package the description says is available until grep or a probe confirms it. When it is absent, the required output is prerequisite production tasks with the gap measured symbol by symbol ÔÇö never a narrowed DoD, a mock-backed journey, or authority reimplemented inside a test.\n\n- GAP CLAIMED PRESENT, actually closed. Do not plan work to build something the description calls missing until grep or a probe confirms it is missing. Re-implementing a landed surface is a silent duplicate that collides with its owner in a shared worktree, and it wastes the same context load as the opposite error.\n\nThe architect records the measurement ÔÇö command and result ÔÇö in planningNotes.codebaseInsights a","When a plan has a task consume a symbol from another workspace package, the planning step that names that symbol MUST also verify the DEPENDENCY EDGE, not only the symbol. Record all three: (a) the consuming package\u0027s package.json declares the producer in \"dependencies\"; (b) pnpm-lock.yaml carries the importer entry for that consumer; (c) a compiled probe inside the consuming package imports from the producer\u0027s BARE specifier and typechecks exit 0, probe deleted in the same command. A root-barrel grep proving the export exists does NOT satisfy this rail. If the edge is absent, the plan must either name the manifest edit plus workspace install as its first step and declare both files as owned paths, or report blocked ÔÇö it must never plan around the gap, because there is no escape hatch: this repo has no tsconfig \"paths\", no TypeScript project references, and a deep relative import fails with TS6059.","A verification gate must prove it EXECUTED. Exit 0 is not evidence.\n\n`pnpm --filter \u003cpkg\u003e \u003cscript\u003e` prints \"No projects matched the filters\" and EXITS 0 when \u003cpkg\u003e is not in the workspace. Reproduced: `pnpm --filter @moe/benchmark test` -\u003e EXIT=0, zero tests, `packages/benchmark` absent. `No test files found` and `0 passed` are also exit 0.\n\nBinding:\n- ARCHITECTS: before putting a `--filter` leg in `verification`, confirm the package resolves (globs: apps/*, adapters/*, packages/*). If the task CREATES that package, the DoD must require a NONZERO executed-test count.\n- WORKERS: read the gate\u0027s OUTPUT, not its exit code. A leg printing no test counts ran no tests; recording it as passing is a false completion claim.\n- QA: an exit-0 leg with no test-count line is UNVERIFIED, not passing. Settle with a positive control: the same command shape against a package that certainly has tests.\n\nSame class: a piped run reports tail\u0027s exit code; a `;`-chained run reports only the last command\u0027s.","A verification gate must prove it EXECUTED. Exit 0 is not evidence.\n\n`pnpm --filter \u003cpkg\u003e \u003cscript\u003e` prints \"No projects matched the filters\" and EXITS 0 when \u003cpkg\u003e is not a workspace package. A REAL run also exits 0, so the discriminator is the `Test Files N passed` count line, which a vacuous run never prints.\n\nBinding:\n- ARCHITECTS: resolve a `--filter` leg by PACKAGE NAME in the workspace, never by directory. `@moe/daemon` is apps/daemon; `@moe/ide-adapter-contract` is adapters/ide-contract. `ls packages/\u003cname\u003e` false-alarms on both. If the task CREATES the package, the DoD must require a NONZERO executed-test count.\n- WORKERS: read the gate\u0027s OUTPUT. No count line means no tests ran; calling that a pass is a false completion claim.\n- QA: an exit-0 leg with no count line is UNVERIFIED, not passing. Positive control: same shape against a package that certainly has tests.\n\nSame class: a piped run reports tail\u0027s exit code, not the gate\u0027s - capture ${PIPESTATUS[0]}."]}},"epic":{"id":"epic-bf111658f9694b558bdc5596bbf0f924","title":"M5 GA evidence - hardening benchmark and cutover","epicRails":["Authoritative design: D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it in implementation tasks.","Development happens only in D:/projexts/moe-next; do not create sibling Git worktrees. Runtime workspace isolation promised by the design remains in scope.","Preserve foreign work; stage and commit only explicit owned paths. Never use git add -A, implicit push, implicit merge, reset, or stash.","Fail closed with stable reason codes; missing or unverifiable evidence stays UNKNOWN and never gains authority.","Use TDD. Keep each production source focused, target \u003c=250 lines, and split before 400 lines. THIS CAP IS PER FILE, NOT PER TASK. There is no per-task net-LOC budget and never was; a task whose total diff is large but whose individual files are small is fully compliant. Task size is bounded by plan shape ÔÇö \u003c=12 plan steps and \u003c=10 distinct affectedFiles ÔÇö never by summed lines changed. Do not split a task, reject a task, or route a size exception on task-level LOC; per-file violations remain real and are fixed by splitting the file. No debug, probe, scratch, or generated evidence files may remain in commits.","Assert the reason code, not just the outcome. A test covering a failure, refusal, or rejection path must assert the specific stable reason code ÔÇö and, where more than one layer can refuse, which layer refused ÔÇö never merely that the operation did not succeed. A generated or swept case must assert that the case was actually generated; a sweep that silently produces zero cases passes while testing nothing. A property must be asserted against the production surface, not against a test helper that reimplements it. These defects are invisible to a green suite: verify a failure-path test by mutating the production surface and confirming the test goes red."]},"task":{"id":"task-f643b972a9014c6aa0bf1257dc1892ef","title":"Hostile coverage: durable store fault boundaries","description":"Objective: hostile before/after/race coverage for the DURABLE-STORE axis of the declared-boundary roster. SPIDR child of the Security fault matrix (task-05ce9b8f982f448a9cdaa4142a653f32).\n\nHARD DEPENDENCY: task-9aca4b0524a648f1841237b40d4e345b (Security lane harness and declared-boundary roster) must be DONE ÔÇö this slice consumes its harness and claims its `durable-store` axis tag. Re-measure on disk before planning.\n\nMEASURED BOUNDARY SUBSET at HEAD abc3dcf, listed not guessed:\n- packages/store/src/recovery-anchor-contracts.ts:13 ÔÇö RECOVERY_ANCHOR_LAYER\n- packages/store/src/recovery-install-contracts.ts:21,22 ÔÇö RECOVERY_INSTALL_TRANSACTION_LAYER, RECOVERY_INSTALL_LAYERS\n- apps/daemon/src/activation/activation-ledger-contracts.ts:50 ÔÇö ACTIVATION_LEDGER_LAYER\n- apps/daemon/src/telemetry/provider-run-refusals.ts:25 ÔÇö PROVIDER_RUN_LEDGER_LAYERS\n- apps/daemon/src/recovery/recovery-completion-evidence.ts:45 ÔÇö DURABLE_STORE_LAYER\n- apps/daemon/src/recovery/recovery-inventory-contract.ts:26 ÔÇö RECOVERY_INVENTORY_LEDGER_LAYER\n- apps/daemon/src/recovery/durable-recovery-inventory-contract.ts:26 ÔÇö DURABLE_INVENTORY_ADAPTER_LAYER\n\nDeliverable: hostile schedules against a REAL SqliteEventStore ÔÇö expected-version conflict under concurrent writers, command replay with changed bytes, event-id collision, a crash between decision and apply, a corrupted or truncated committed payload, and a read of an aggregate whose head moved mid-page ÔÇö each pinning the exact stable reason code AND the answering layer.\n\nTHE REPLAY-ECHO HAZARD IS THE ONE THAT SHIPS GREEN HERE. The store\u0027s replay identity does NOT cover the events array (see apps/daemon/src/activation/activation-ledger-commit.ts:12-24). A replay answered with the caller\u0027s own record makes every idempotence assertion compare the caller\u0027s bytes with the caller\u0027s bytes and pass forever. The changed-byte replay case is the only one that kills it, and it must be present for every ledger boundary in this subset.\n\nNOT in scope: transport, integrity/codec, runtime/provider, or scheduler/activation boundaries; new store schema; closing the completeness ratchet; anything under tests/fault/**.\n\nOwned paths: tests/security/durable-store-boundaries.security.ts (plus at most one sibling file if the 400-line split applies).\n\nVerification: `pnpm test:security` ÔÇö read the OUTPUT count line, not the exit code.","status":"WORKING","reopenCount":0,"reopenReason":"Governor cascade 11:46Z: foundation premise FALSE ÔÇö task-9aca4b05 DONE; roster + hostile harness committed in tests/security/. Plan against the roster\u0027s durable-store-fault axis tag as the definitive subset; description counts superseded by the roster. Re-measure artifacts on committed bytes first.","rejectionDetails":null,"definitionOfDone":["Every constant in the durable-store subset has at least one BEFORE, one AFTER and one RACE case, with a positive case count asserted per constant so a boundary generating zero cases reddens.","Every ledger boundary in the subset carries a CHANGED-BYTE REPLAY case proving the replay answer is read back from the durable aggregate, not echoed from the caller; a mutation switching that branch to echo turns a NAMED test red.","Every refusal pins the exact stable reason code AND the answering layer, preserving the store\u0027s own DurableStoreErrorCode verbatim alongside the boundary\u0027s code where both exist ÔÇö never collapsing one into the other.","A refused or crashed schedule leaves NO partial durable state: each case reads the aggregate back and asserts it is either untouched or exactly one complete record, never a fragment.","Tests use a REAL SqliteEventStore, opened per test and closed in a `finally` on every exit path including throws; no case leaves a handle into teardown.","`pnpm test:security` runs with its exit code captured separately and prints a count line strictly above the pre-task baseline; the HEAD-minus-merge-base failing-path delta over the owned paths is EMPTY."],"implementationPlan":[{"stepId":"step-1","title":null,"description":"BASELINE, TAKE THE SUBSET FROM THE ROSTER, AND SETTLE THE LANE BOUNDARY ÔÇö which matters more here than on any other axis.\nTHE 14 durable-store entries, read off the committed roster (this task\u0027s description said 8 and is superseded): DOCTOR_VERSION_LAYERS (daemon/recovery/doctor-version-contract.ts); DURABLE_INVENTORY_ADAPTER_LAYER (durable-recovery-inventory-contract.ts); DURABLE_STORE_LAYER (recovery-completion-evidence.ts); RECOVERY_INCARNATION_LAYER (recovery-incarnation-contract.ts); RECOVERY_INVENTORY_LAYER, RECOVERY_INVENTORY_LEDGER_LAYER, RECOVERY_INVENTORY_UPSTREAM_LAYERS (recovery-inventory-contract.ts); RECOVERY_SUCCESSION_LAYER (recovery-succession-contract.ts); RESTORE_CONTROLLER_LAYER, RESTORE_REFUSAL_LAYERS (restore-controller-contract.ts); RECOVERY_ANCHOR_LAYER (packages/store/recovery-anchor-contracts.ts); RECOVERY_BINDING_CODEC_LAYER, RECOVERY_INSTALL_LAYERS, RECOVERY_INSTALL_TRANSACTION_LAYER (packages/store/recovery-install-contracts.ts).\nNOTE WHAT IS NOT HERE: `ACTIVATION_LEDGER_LAYER` and `PROVIDER_RUN_LEDGER_LAYERS` were on my original filing\u0027s list and the roster put them on another axis. Do not cover them.\nTHE LANE BOUNDARY, and this is the step\u0027s real content. task-0c89476b78044024b07c86c0c8986bd0 owns `tests/fault/disaster-restore/**` and already covers these same production surfaces ÔÇö all 8 `RECOVERY_ANCHOR_FAULT_POINTS`, the restore quiesce path, and the UNKNOWN_TRUTH inventory arms. THIS SLICE MUST NOT RE-RUN THOSE CASES IN A DIFFERENT LANE. The two lanes ask different questions: the FAULT lane asks \"when execution is interrupted at a boundary, is the durable state coherent?\"; the SECURITY lane asks \"when a hostile CALLER presents forged, stale, replayed or racing input, is it refused with the exact code and layer?\" Write that distinction into this file\u0027s header, and for each boundary state which question your cases ask. If a case would be indistinguishable from a disaster-restore case, it belongs there, not here.\nHARNESS (tests/security/hostile-harness.ts): `probeBefore`, `probeAfter`, `probeRacing`, `hostileRoot`, `cleanupHostileRoots`, `withHostileRoot`, `assertRefusedWith`, `MAX_BOUND_MS = 2_147_483_647`. `RefusalExpectation.layer` is non-optional at the type level and re-checked at runtime.\nGATE BASELINE: `pnpm test:security` alone with its exit code captured ÔÇö never `;`-chained, never piped into tail ÔÇö recording the count line.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-2","title":null,"description":"TDD RED, PART 1 ÔÇö the hostile-caller arms, which is what makes this a security slice rather than a second fault suite. Create `tests/security/durable-store-boundaries.security.ts`. Per boundary, at least one BEFORE and one AFTER case driven by adversarial INPUT rather than interrupted execution:\nBEFORE ÔÇö a forged or malformed record presented to a durable reader: an anchor record whose payload does not match its `anchorDigest`; a recovery binding whose codec version is unsupported; an inventory record whose upstream tuple is fabricated; a succession chain link claiming a predecessor it cannot prove; a restore request whose generation is not bound to its incarnation; a doctor version report whose components disagree.\nAFTER ÔÇö authority presented once it should no longer hold: a stale incarnation or key epoch after a fresh restore; a succession link replayed after the chain moved; a restore command replayed against an already-settled aggregate; an inventory read after its window sealed.\nUSE THE EXISTING REFUSAL VOCABULARIES rather than inventing expectations ÔÇö `RESTORE_CONTROLLER_REASON_CODES` (9 members incl. RESTORE_FENCE_REPLAYED, RESTORE_INCARNATION_UNANCHORED, RESTORE_KEY_EPOCH_MISMATCH, RESTORE_GENERATION_NOT_BOUND), `RECOVERY_INSTALL_REASON_CODES`, `RECOVERY_ANCHOR_REASON_CODES`, and the inventory\u0027s `UNKNOWN_TRUTH`. Every assertion through `assertRefusedWith` with both code and layer, each layer taken from the boundary\u0027s own declared constant.\nTHE INVENTORY TRAP, measured and specific: `durable-recovery-inventory-contract.ts:153` states that several distinct upstream conditions ALL answer `UNKNOWN_TRUTH` at `RECOVERY_INVENTORY`, \"and only the upstream tuple distinguishes the cases\". So for every inventory boundary, assert the UPSTREAM TUPLE as well as the code and layer ÔÇö a code-and-layer-only assertion there is satisfied by any of several unrelated failures and is vacuous by construction. `RECOVERY_INVENTORY_UPSTREAM_LAYERS` is the vocabulary to pin against.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-3","title":null,"description":"TDD RED, PART 2 ÔÇö RACE, and the one place this axis may legitimately use the fault seam. Per boundary, one concurrent case contending at the durable admission point: two restores claiming one generation; an install racing a discard; a succession link racing a competing successor; two inventory writers sealing one window; a commit racing an expected-version bump.\nUSE A REAL SqliteEventStore for anything that commits ÔÇö opened per case and closed in a `finally` on every exit path including throws. A fake store reimplements the very expected-version and replay semantics under test, so a green against a fake proves the fake. A held handle kills the vitest worker outright, and this lane runs `fileParallelism: false`, so one leak stalls everything after it.\nTHE FAULT SEAM IS AVAILABLE AND LEGITIMATE HERE, but only in service of the security question. `RecoveryAnchorRequest` carries an `injectFault` callback snapshotted through the public entry (recovery-anchor-record.ts:111), and the 8 `RECOVERY_ANCHOR_FAULT_POINTS` are real call sites. Use it ONLY to hold one side of a race at a boundary while the other side presents hostile input ÔÇö that is a security question. Do NOT sweep the 8 fault points asserting post-crash state coherence; that is task-0c89476b\u0027s job in the fault lane, and duplicating it here is the exact overlap step 1 forbids.\nBOUND EVERY RACE well below `MAX_BOUND_MS` (2**31-1, the `setTimeout` clamp boundary ÔÇö at or above it a bound clamps to 1ms and races nothing). Assert PER SIDE via `RaceOutcome` that exactly one side is admitted and the other carries a stable code and layer, or both refuse ÔÇö never two admissions. Make each case pass whichever side wins; an order-dependent race is a flake whose usual fix pins the order and destroys the property.\nCOMPLETENESS: assert a positive case count per boundary and assert the swept set equals the roster\u0027s 14 durable-store entries in BOTH directions.\nWHOLE-SLICE INVARIANT, asserted once over the collected outcomes: no hostile case yields a committed record, an installed anchor, an advanced succession, or a truth class above UNKNOWN. Read the durable aggregate back to prove it ÔÇö \"it returned a refusal\" is not evidence that nothing was written.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-4","title":null,"description":"ATOMICITY, CLEANUP AND THE SHARED-ARTIFACT BOUNDARY. For EVERY refusal arm in this slice, read the durable aggregate back through a production reader and assert it is untouched or holds exactly one complete record ÔÇö never a fragment. A partial write reported as a refusal is a defect, and on this axis it is the defect that matters most: a half-installed anchor or a half-sealed inventory window is worse than a clean failure, because the next reader treats it as evidence.\nEvery filesystem-touching case takes its root from `hostileRoot(label)` or runs inside `withHostileRoot`; register `cleanupHostileRoots()` so every root is removed on every exit path including throws and timeouts. Close every store handle in a `finally`.\nNEVER call `commitWithApply` or `commitExpectedVersionDecisionWithApply`. They hand the callback a raw `DatabaseSync`, which is how the no-new-schema rail gets violated by accident; the narrow ledger ports deliberately cannot name them, and a test reaching for them would be exercising a path production cannot take.\nDo NOT modify `hostile-harness.ts` or `boundary-roster.security.ts` ÔÇö committed deliverables of task-9aca4b05, read by five slices; report blocked rather than editing either. Do NOT touch `tests/fault/**`, and in particular do not edit or extend `tests/fault/disaster-restore/**`, which belongs to task-0c89476b.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-5","title":null,"description":"MUTATION DRILLS ÔÇö prove the coverage binds to real durable guards. Run each alone, revert before the next. (1) Remove one identity-binding check from a covered production boundary ÔÇö the restore controller\u0027s `RESTORE_GENERATION_NOT_BOUND` comparison, or the anchor\u0027s digest check ÔÇö and confirm a NAMED case reddens on its exact code and layer. (2) Make one inventory refusal drop its upstream tuple while keeping `UNKNOWN_TRUTH` at `RECOVERY_INVENTORY`; the step-2 inventory case must redden ÔÇö this is THE drill for this axis, because without it a code-and-layer assertion there passes for any of several unrelated failures. (3) Make one refusal leave a partial durable write; the step-4 read-back assertion must redden ÔÇö proving atomicity is asserted rather than assumed from the refusal\u0027s presence. (4) Make one race admit BOTH sides; the per-side assertion must redden. (5) Delete one boundary\u0027s cases; the roster-set assertion must redden naming that constant.\nFor EVERY drill record the exact replaced range you edited: a drill that applied nothing reads as green and proves nothing. Confirm each red lands on the INTENDED assertion, not an unrelated crash or timeout ÔÇö note that on this axis a wrong-reason red is easy, because several distinct failures already collapse to `UNKNOWN_TRUTH`. Restore with `git checkout -- \u003cthe one drilled file\u003e` (never by hand) and verify `git status --short` shows it clean before the next; drills 1-3 edit PRODUCTION files in a shared worktree, so a drill left on disk risks a foreign completion hook committing it.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-6","title":null,"description":"GATE, ADVERSARIAL REVIEW, COMMIT. Run `pnpm test:security` alone with its exit code captured ÔÇö never `;`-chained, never piped into tail ÔÇö and read the OUTPUT: the count line must be strictly above the step-1 baseline, and say by how many. `passWithNoTests: false` catches an empty lane, but a lane collecting only the roster, smoke, transport and integrity files still exits 0. Then `pnpm --filter @moe/store test` and `pnpm --filter @moe/daemon test`, since drills 1-3 touch production in both packages ÔÇö confirm they are back to their baselines. Then `pnpm typecheck` repo-wide with the path-attributed baseline: HEAD failing paths minus merge-base failing paths, intersected with your owned paths, MUST be empty; quote any foreign red verbatim without repairing it.\nADVERSARIAL REVIEW: (a) does the swept set equal the roster\u0027s 14 durable-store entries in BOTH directions, with no member from another axis? (b) is the LANE BOUNDARY honoured ÔÇö does the file header state the security-vs-fault distinction, and does each boundary say which question its cases ask, with nothing that merely re-runs a disaster-restore case? (c) does EVERY inventory assertion pin the upstream tuple as well as the code and layer? (d) does every refusal arm read the durable aggregate back and assert no fragment? (e) is every commit against a REAL SqliteEventStore, closed in a `finally` on every exit path? (f) are `commitWithApply` and `commitExpectedVersionDecisionWithApply` absent from the diff? (g) is every race bounded well below `MAX_BOUND_MS`, terminating, asserted per-side, order-independent? (h) is the whole-slice invariant present ÔÇö no committed record, installed anchor, advanced succession, or truth class above UNKNOWN? (i) are `hostile-harness.ts`, `boundary-roster.security.ts` and `tests/fault/**` untouched? (j) is each file under 400 lines by `grep -c \u0027\u0027`? (k) does `git status --porcelain` show only owned paths, with every production drill reverted?\nCommit by explicit pathspec naming each owned file ÔÇö never a bare `git commit`, never `git add -A`. Verify with `git show --stat`. In the completion: quote the before/after count lines, name the test that reddened for each drill, state how you kept this slice distinct from task-0c89476b\u0027s fault coverage, and record that this satisfies the `durable-store` axis of ratchet task-3b1cbdf9bd39405bb32230552d3ab242. Keep verification.command under 500 characters.","status":"PENDING","note":null,"modifiedFiles":null}],"taskRails":["Consume the roster\u0027s `durable-store` axis tag as the definitive subset; the roster owns the partition.","NEVER call `commitWithApply` or `commitExpectedVersionDecisionWithApply` ÔÇö they hand out a raw DatabaseSync and the no-new-schema rail forbids it. The narrow ledger ports deliberately cannot name them; verify they still cannot.","Real SqliteEventStore only. A fake store reimplements the very replay and conflict semantics under test, so a green against a fake proves the fake.","A held SQLite handle kills the vitest worker; close in a `finally` on every exit path.","Preserve foreign work; commit only the explicit owned paths by pathspec. Do not touch tests/fault/**."]},"planningNotes":{"approachesConsidered":"1) Sweep the 8 `RECOVERY_ANCHOR_FAULT_POINTS` here asserting post-crash coherence. REJECTED as duplication: that is precisely task-0c89476b\u0027s DoD 1 in the fault lane. Two suites asserting the same property in two lanes doubles maintenance and halves the chance either is updated when the property changes.\n\n2) Skip the anchor and restore boundaries entirely to avoid overlap. REJECTED symmetrically: they carry real hostile-input surface ÔÇö forged anchor records, replayed restore fences, stale incarnations ÔÇö that the fault lane does not test, and the ratchet will demand coverage for every roster entry regardless.\n\n3) Assert inventory refusals by code and layer alone, as on the other axes. REJECTED, and this is the axis-specific defect: several upstream conditions collapse to `UNKNOWN_TRUTH` at `RECOVERY_INVENTORY`, so such an assertion passes for unrelated failures. The upstream tuple must be pinned.\n\n4) Use a fake store for the race and commit cases. REJECTED: a fake reimplements the expected-version and replay semantics under test, so a green proves the fake. Real `SqliteEventStore`, closed in a `finally`.\n\n5) Assert \"it returned a refusal\" and stop. REJECTED on this axis above all others: a half-installed anchor or half-sealed inventory window is worse than a clean failure, because the next reader treats the fragment as evidence. Every refusal arm reads the aggregate back.\n\n6) Reach for `commitWithApply` to set up a hostile durable state quickly. REJECTED: it hands out a raw `DatabaseSync` and the narrow ports deliberately cannot name it, so a test using it exercises a path production cannot take.\n\n7) Pin the winner of each race. REJECTED: order-dependence is a flake whose standard fix destroys the property.","codebaseInsights":"MEASURED AT HEAD 7086e54. Foundation committed; this slice plans against real bytes.\n\nTHE ROSTER SUPERSEDES THE DESCRIPTION: axis totals read `durable-store 14`; the description says 8. The roster also re-tagged ÔÇö `ACTIVATION_LEDGER_LAYER` and `PROVIDER_RUN_LEDGER_LAYERS` were on my original filing\u0027s list and are NOT on this axis.\n\nTHE 14: DOCTOR_VERSION_LAYERS, DURABLE_INVENTORY_ADAPTER_LAYER, DURABLE_STORE_LAYER, RECOVERY_INCARNATION_LAYER, RECOVERY_INVENTORY_LAYER, RECOVERY_INVENTORY_LEDGER_LAYER, RECOVERY_INVENTORY_UPSTREAM_LAYERS, RECOVERY_SUCCESSION_LAYER, RESTORE_CONTROLLER_LAYER, RESTORE_REFUSAL_LAYERS (all apps/daemon/src/recovery/*), plus RECOVERY_ANCHOR_LAYER, RECOVERY_BINDING_CODEC_LAYER, RECOVERY_INSTALL_LAYERS, RECOVERY_INSTALL_TRANSACTION_LAYER (packages/store). The axis is RECOVERY/RESTORE-shaped, not ledger-shaped.\n\nTHE LANE-OVERLAP PROBLEM, which is this slice\u0027s defining risk and which no other axis has. I planned task-0c89476b (disaster restore proof) earlier this session; it owns `tests/fault/disaster-restore/**` and already covers these EXACT production surfaces ÔÇö all 8 `RECOVERY_ANCHOR_FAULT_POINTS` (PREPARE, INACTIVE_INSTALL, TRANSACTION, FILE_FSYNC, DIRECTORY_PERSISTENCE, VERIFICATION, SWITCH, INSTALLED_MARKER), the restore quiesce path, and the UNKNOWN_TRUTH inventory arms. Without an explicit boundary this slice becomes a second copy of that suite in another lane, which costs review time and proves nothing new.\nThe honest split: the FAULT lane asks \"when execution is interrupted at a boundary, is the durable state coherent?\"; the SECURITY lane asks \"when a hostile CALLER presents forged, stale, replayed or racing input, is it refused with the exact code and layer?\" Same surfaces, different question. The plan requires that distinction in the file header and per boundary.\n\nVOCABULARIES ALREADY EXIST, so nothing needs inventing: `RESTORE_CONTROLLER_REASON_CODES` has 9 members including RESTORE_FENCE_REPLAYED, RESTORE_INCARNATION_UNANCHORED, RESTORE_KEY_EPOCH_MISMATCH, RESTORE_GENERATION_NOT_BOUND, RESTORE_RECORD_UNREADABLE; `RECOVERY_INSTALL_REASON_CODES` and `RECOVERY_ANCHOR_REASON_CODES` cover the store side; the inventory answers `UNKNOWN_TRUTH` at `RECOVERY_INVENTORY`.\n\nTHE INVENTORY TRAP, measured: `durable-recovery-inventory-contract.ts:153` states several distinct upstream conditions ALL answer `UNKNOWN_TRUTH` and \"only the upstream tuple distinguishes the cases\". So on the three inventory boundaries a code-and-layer assertion is VACUOUS BY CONSTRUCTION ÔÇö it is satisfied by any of several unrelated failures. `RECOVERY_INVENTORY_UPSTREAM_LAYERS` exists precisely to be pinned, and drill 2 targets exactly this.\n\nTHE FAULT SEAM IS AVAILABLE: `RecoveryAnchorRequest.injectFault` is snapshotted through the public entry (recovery-anchor-record.ts:111) and the 8 fault points are live call sites (recovery-anchor.ts:83,95 and recovery-anchor-install.ts:232-260). Legitimate here ONLY to hold one side of a race while the other presents hostile input ÔÇö not to sweep post-crash coherence, which is the fault lane\u0027s question.\n\nHARNESS confirmed: `assertRefusedWith` requires `layer` at type level and re-checks at runtime; `MAX_BOUND_MS` = 2**31-1, the `setTimeout` clamp boundary.","risks":"1) BECOMING A SECOND DISASTER-RESTORE SUITE. The single biggest risk here, and unique to this axis. Same surfaces, and the fault-point seam is right there. Step 1 requires the security-vs-fault distinction in the header and per boundary; step 3 permits the seam only to hold a race side.\n\n2) THE INVENTORY ASSERTION IS VACUOUS WITHOUT THE UPSTREAM TUPLE. Several conditions answer `UNKNOWN_TRUTH` at `RECOVERY_INVENTORY`; a code-and-layer assertion there is satisfied by any of them. Drill 2 is the guard, and it is the drill most likely to reveal a hole.\n\n3) A PARTIAL DURABLE WRITE REPORTED AS A REFUSAL. On this axis the fragment becomes the next reader\u0027s evidence. Read the aggregate back on every refusal arm; a returned refusal is not proof nothing was written.\n\n4) A HELD SQLITE HANDLE kills the vitest worker, and `fileParallelism: false` means one leak stalls the rest of the lane. Close in a `finally` on every exit path including throws.\n\n5) A WRONG-REASON RED IS EASY HERE because several distinct failures already collapse to one code. Confirm each drill\u0027s red lands on the intended assertion, not merely that something went red.\n\n6) DRILLS 1-3 EDIT PRODUCTION in both packages/store and apps/daemon. Revert via `git checkout --`, confirm clean, and re-run both package suites at the gate ÔÇö a foreign whole-tree completion hook committing a drill has already happened twice on this board.\n\n7) `commitWithApply` / `commitExpectedVersionDecisionWithApply` must not appear ÔÇö the no-new-schema rail, and the narrow ports cannot name them.\n\n8) EXIT 0 IS NOT EVIDENCE. A lane collecting only the roster, smoke, transport and integrity files still exits 0. Require the count line above baseline.\n\n9) SHARED ARTIFACTS: `hostile-harness.ts` and `boundary-roster.security.ts` are read by five slices; `tests/fault/disaster-restore/**` belongs to task-0c89476b.\n\n10) DOWNSTREAM: ratchet task-3b1cbdf9 will assert every roster entry resolves to a real before/after/race case, so completeness is this slice\u0027s obligation.","keyFiles":["tests/security/boundary-roster.security.ts","tests/security/hostile-harness.ts","apps/daemon/src/recovery/restore-controller-contract.ts","apps/daemon/src/recovery/recovery-inventory-contract.ts","apps/daemon/src/recovery/durable-recovery-inventory-contract.ts","packages/store/src/recovery-anchor-contracts.ts","packages/store/src/recovery-install-contracts.ts","packages/store/src/recovery-anchor-record.ts"]},"nextAction":{"tool":"moe.start_step","args":{"taskId":"task-f643b972a9014c6aa0bf1257dc1892ef","stepId":"step-1","workerId":"worker-9f145232"},"reason":"Begin step: BASELINE, TAKE THE SUBSET FROM THE ROSTER, AND SETTLE THE LANE BOUNDARY ÔÇö which ","recommendedSkill":{"name":"explore-before-assume","reason":"First step in code you have not verified. Load this before editing so you build on what actually exists, not assumptions."}}}
</claimed_task_context>

<inbox>
unread_general=10
unread_task=0
mentions=0 (see <routed_mentions> below if > 0)
memory_total=1065 Serena memories (content via read_memory; names below are preloaded from disk - call list_memories only if they don't cover your area)
memory_this_task=none
memory_recent=gotcha-hook-commit-swallows-a-drill-and-git-reads-clean task-task-6cbff01023b14b26a78fc5e3eb1dd8a9-qa-verdict gotcha-a-parallel-session-can-write-your-owned-files task-task-6cbff01023b14b26a78fc5e3eb1dd8a9-handoff gotcha-idempotent-command-replay-is-not-a-refusal gotcha-durable-bytes-decode-onto-a-null-prototype gotcha-src-test-fixture-with-module-side-effects-needs-an-importer-grep task-task-e4af0e6eb4fa442b81eb9a708e8d6dd4-qa-verdict gotcha-drill-restore-by-snapshot-when-nothing-is-committed gotcha-two-same-layer-refusal-branches-make-a-code-assertion-vacuous task-task-e4af0e6eb4fa442b81eb9a708e8d6dd4-handoff task-task-48c0c0db613f400b98d525629f1cd434-qa-verdict task-task-48c0c0db613f400b98d525629f1cd434-handoff gotcha-pnpm-filter-test-with-a-path-does-not-narrow gotcha-a-reservation-cannot-fill-a-crash-situation gotcha-untracked-deliverable-breaks-the-drill-restore-loop gotcha-empty-store-horizon-skips-the-whole-walk gotcha-dispatch-aggregate-cannot-supply-a-crash-situation gotcha-deleting-a-refusal-table-can-be-the-correct-move task-task-16d5bc3a10864351adf5be10dfa7df00-qa-verdict
</inbox>

<pending_questions>
{"count":0,"totalMatches":0,"tasks":[],"pagination":{"limit":10,"returned":0,"total":0,"hasMore":false},"truncatedQuestions":0}
</pending_questions>

<system-reminder>
Skill recommendation for this task's current phase: explore-before-assume
Why: First step in code you have not verified. Load this before editing so you build on what actually exists, not assumptions.
Before you call moe.start_step, invoke the Skill tool:
  Skill(skill="explore-before-assume")
This is not optional. Do not rationalize skipping it ("I'm blocking, not planning", "this is trivial", "I already know what it says"). Skills evolve — load the current version.
If after loading you decide it truly does not apply here, say so explicitly in chat — but LOAD IT FIRST.
</system-reminder>
