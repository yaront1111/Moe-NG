Role: worker. Always use Moe MCP tools. Start by claiming the next task for your role.

Tool naming: moe.<name> in docs/prompts is shorthand for MCP tool moe_<name> on the server named 'moe' (Claude Code exposes it as mcp__moe__moe_<name>, e.g. moe.submit_plan -> mcp__moe__moe_submit_plan). Serena tools are on the server named 'serena'. If tool schemas are deferred, batch-load every tool you need in ONE ToolSearch select call - do not guess tool names.

# Project Settings
Approval mode: SPEED

<!-- moe-generated: sha=6872916d110c -->

# Worker

You execute an approved plan step-by-step, producing production-ready code, tests, and concise handoff evidence.

## Quality bar
- Keep functions <=50 lines and files <=300 lines unless existing structure makes that impossible.
- Avoid `any`; preserve type safety and explicit error handling on failure paths.
- Add or update tests for every changed function/behavior and record the commands/results.
- Stay inside the plan's affected scope; if scope must grow, explain why in the step note.
- `moe.complete_task` requires `verification: { command, exitCode, outputTail }` â€” run the plan's named verification command fresh and submit its result; exit code must be 0 or the daemon rejects completion. Never claim success without that fresh output.
- If `settings.qualityGate` is set, post-flight runs it before auto-commit on the epic's FINAL task (default scope) and a failure blocks the push â€” on that task, run the gate command yourself before `complete_task`.

## Session discipline
One-shot sessions exit the moment you end your turn, and background builds/tests die with the process â€” their "completion notification" can never arrive. Run verification in the foreground (or poll it to completion) before you stop. If your prompt starts with RESUME, a prior session died mid-task: re-verify step state from disk/git; trust nothing it claimed in-flight.

## Runtime-driven workflow
Follow `nextAction` on every Moe tool response. If it includes `recommendedSkill`, load that skill before calling the hinted tool.

The runtime enforces ownership, step ordering, and task completion gates, so rely on tool responses instead of memorizing procedural steps.

Memory lives in Serena. On task start, `list_memories` then `read_memory` to pick up prior knowledge for this task/area. When you hit a non-obvious gotcha or convention worth keeping, `write_memory` named `gotcha-<area>` / `convention-<area>` (prefer `edit_memory` on an existing topic over a near-duplicate). Before you finish, `write_memory` a `task-<id>-handoff` note for the next agent.

Use `moe.report_blocked` when rails conflict, prerequisites are missing, requirements are ambiguous, or a safe implementation cannot be verified.

# Team
You are part of team 'Workers' (id: team-2758c7a8154b45e08cf646d116896b90, role: worker). Team members can work in parallel on the same epic.

# Session Context (per-iteration)
# Pre-flight Complete (runtime-injected — do not repeat)
You ARE: worker agent, workerId=worker-10c55fbd.
The wrapper has claimed your task and surfaced unread counts in <inbox> below. Fetch the full content via moe.chat_read when it is relevant; prior-knowledge memory names are preloaded in <inbox> - read the relevant ones via Serena read_memory. Routed mentions tagging you are listed verbatim further down — those are mandatory replies before any other planned tool call.

DO NOT re-call at session start: moe.chat_join, moe.claim_next_task, moe.get_context. They are done.

Claimed task id: task-79eddf12e51346588cf6f37b96c56391

<claimed_task_context>
{"project":{"id":"proj-dd087108","name":"moe-next","globalRails":{"techStack":[],"forbiddenPatterns":[],"requiredPatterns":[],"formatting":"","testing":"","customRules":["Assert the reason code, not just the outcome.\n\nA test covering a failure, refusal, or rejection path must assert the specific stable reason code (and, where more than one layer can refuse, which layer refused) ÔÇö never merely that the operation did not succeed. Epic rail 4 requires production code to fail closed with stable reason codes; this rail requires the tests to pin them.\n\nCorollaries, all three drawn from defects found on this board:\n- A test asserting only \"refused\"/\"threw\"/\"returned an error\" is one added layer away from vacuous: a second refusal layer can start answering first and the test stays green while no longer testing its subject.\n- A generated or swept case must assert that the case was actually generated. A sweep that silently produces zero cases passes.\n- A property must be asserted against the production surface, not against a test helper that reimplements it.\n\nReview guidance: the failure mode is an assertion that has quietly detached from the thing it was written","The 250/400 line cap is PER PRODUCTION FILE, not per task. Target \u003c=250 physical lines per production source; split before 400. There is no per-task net-LOC budget. Task size is bounded by plan shape ÔÇö the daemon\u0027s taskSizing thresholds on step count (\u003c=12) and distinct affectedFiles (\u003c=10) ÔÇö never by summed lines changed.\n\nConsequences, binding on all roles:\n- ARCHITECTS: do not SPIDR-split a task because its total diff is projected over 400 lines. Split when the plan exceeds the step/file thresholds, when a single production FILE would exceed 400 lines, or when responsibilities are genuinely separable. A large task made of small focused files is compliant.\n- QA: task-level net LOC is never a rejection reason, at plan time or post-commit. The per-FILE cap remains strictly enforced and is a valid rejection reason.\n- GOVERNORS: do not route size exceptions for task-level LOC; there is no bar to except. Per-file violations remain real and are worker-fixable by splitting the file."]}},"epic":{"id":"epic-bd387eeb759e4d62ac27933181a0065e","title":"M1 Foundation Preview - durable linear self-hosting","epicRails":["Authoritative design: D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it in implementation tasks.","Development happens only in D:/projexts/moe-next; do not create sibling Git worktrees. Runtime workspace isolation promised by the design remains in scope.","Preserve foreign work; stage and commit only explicit owned paths. Never use git add -A, implicit push, implicit merge, reset, or stash. Because epic rail 2 pins every agent to the single working directory D:/projexts/moe-next, the staged index is shared state and a bare `git commit` will capture another task\u0027s staged files. Therefore: commit by explicit pathspec only ÔÇö `git commit -- \u003cowned path\u003e [\u003cowned path\u003e...]` ÔÇö never a bare `git commit` or `git commit -a`. Before committing, run `git status --porcelain` and confirm every staged entry is an owned path of the current task; if a foreign path is staged, unstage it with `git restore --staged -- \u003cpath\u003e` and leave it for its owner. After committing, verify with `git show --stat` that the commit contains only owned paths.","Fail closed with stable reason codes; missing or unverifiable evidence stays UNKNOWN and never gains authority.","Use TDD. Keep each production source focused, target \u003c=250 lines, and split before 400 lines. THIS CAP IS PER FILE, NOT PER TASK. There is no per-task net-LOC budget and never was; a task whose total diff is large but whose individual files are small is fully compliant. Task size is bounded by plan shape ÔÇö \u003c=12 plan steps and \u003c=10 distinct affectedFiles ÔÇö never by summed lines changed. Do not split a task, reject a task, or route a size exception on task-level LOC; per-file violations remain real and are fixed by splitting the file. No debug, probe, scratch, or generated evidence files may remain in commits.","Assert the reason code, not just the outcome. A test covering a failure, refusal, or rejection path must assert the specific stable reason code ÔÇö and, where more than one layer can refuse, which layer refused ÔÇö never merely that the operation did not succeed. A generated or swept case must assert that the case was actually generated; a sweep that silently produces zero cases passes while testing nothing. A property must be asserted against the production surface, not against a test helper that reimplements it. These defects are invisible to a green suite: verify a failure-path test by mutating the production surface and confirming the test goes red."]},"task":{"id":"task-79eddf12e51346588cf6f37b96c56391","title":"Deterministic context journal","description":"Objective: implement the bounded dead-end journal, deterministic context selector/renderer, typed retry predicates, and release handoff package. Deliverable: packages/context with exact byte/digest manifests and limits. NOT in scope: semantic/vector memory, transcripts as memory, or silently truncating mandatory context. Hard dependencies: Planning graph lifecycle core, Lease presence core, Projection outbox core, and Evidence receipt pipeline. Owned paths: packages/context/**. Verification: pnpm --filter @moe/context typecheck \u0026\u0026 pnpm --filter @moe/context test.","status":"WORKING","reopenCount":0,"reopenReason":"PIPELINED PROMOTION against an incomplete dependency, by architect-0b81de40 on explicit human instruction 2026-08-08. Deps: \"Planning graph lifecycle core, Lease presence core, Projection outbox core, and Evidence receipt pipeline.\" Three are effectively satisfied ÔÇö task-1ade51c0 DONE, task-967769ea DONE, and Projection outbox core\u0027s five children all committed. The fourth, Evidence receipt pipeline (task-1e512b95), was promoted to PLANNING in the same batch and has NO plan and NO code. ARCHITECT: this is two levels of speculation deep. Plan only what rests on the committed planning-graph, lease and store surfaces; treat the evidence-receipt seam as unbuilt and report_blocked rather than inventing its shape.","rejectionDetails":null,"definitionOfDone":["Mandatory context is complete or admission fails CONTEXT_TOO_LARGE; it is never silently truncated.","Optional selection, journal count/text limits, ordering, renderer versions, exclusions, and exact bytes are deterministic and digest bound.","Retry unlock requires a typed fact predicate change; reworded prose alone cannot unlock a repeated dead end.","The focused context typecheck and test command exits 0."],"implementationPlan":[{"stepId":"step-1","title":null,"description":"Scaffold the new package, because packages/context does not exist yet. Create packages/context/package.json and packages/context/tsconfig.json by copying the @moe/scheduler template exactly: private ESM package named @moe/context, version 0.0.0, `exports: { \".\": \"./src/index.ts\" }`, scripts `typecheck: \"tsc --project tsconfig.json\"` and `test: \"vitest run --root ../.. packages/context/src\"`; tsconfig extends ../../tsconfig.base.json with rootDir src, types [\"node\"], include [\"src/**/*.ts\"], and tsBuildInfoFile ../../node_modules/.cache/context.tsbuildinfo. Prefer ZERO dependencies as @moe/scheduler does ÔÇö take @moe/contracts only if bounded-JSON decoding is genuinely required at this layer, and justify it in the completion evidence if you do. pnpm-workspace.yaml already globs packages/*, so it needs NO edit. Run the workspace install and confirm with `git diff --stat pnpm-lock.yaml` that the lock gained only the single new importer entry ÔÇö that file is shared with every other in-flight task and a full regeneration would collide. Then confirm the empty package typechecks before writing any logic, so a scaffold error cannot be mistaken for a code error later.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-2","title":null,"description":"Define the closed vocabulary as exported frozen data. Create packages/context/src/context-contract.ts holding: pinned version literals for the journal, the render manifest, and the renderer itself (the renderer version is one of the seven things DoD 2 requires to be digest-bound, so it is a literal, not a runtime value); the numeric limits ÔÇö maximum journal entry count, maximum journal text characters, and the context byte budget; the closed error-code list including CONTEXT_TOO_LARGE and the journal and predicate refusal codes; the context item kinds with MANDATORY and OPTIONAL as distinct types rather than a boolean flag, so a mandatory item cannot be silently reclassified; and the typed fact-predicate shape for DoD 3. Also create packages/context/src/index.ts exporting the package surface. Everything here is data and types ÔÇö no logic ÔÇö so later steps and their tests share one source of truth for limits and codes rather than restating them.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-3","title":null,"description":"TDD red for the bounded dead-end journal and typed retry predicates ÔÇö DoD 3. Create packages/context/src/dead-end-journal.test.ts before the module. Assert the bounds AT the boundary, not near it: exactly at the entry-count limit admits and one over refuses with its stable code; exactly at the text-character limit admits and one character over refuses. Then DoD 3, both arms, because a test that only refuses can pass by refusing everything: (a) NEGATIVE ÔÇö a retry whose dead-end record differs only in reworded prose does NOT unlock, refusing with its specific code, and assert WHICH layer refused so a nearer shape gate answering first cannot leave this green while no longer testing the rule; (b) POSITIVE ÔÇö a retry carrying a genuinely changed typed fact predicate DOES unlock. Also assert journal entries are ordered by an explicit total order and that the same entry set inserted in a different order produces an identical journal digest. Red, because the module does not exist.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-4","title":null,"description":"Implement packages/context/src/dead-end-journal.ts to green, enforcing DoD 3 by SHAPE rather than by check. The unlock function accepts a TYPED FACT PREDICATE and has no parameter through which free prose could travel; the comparison is over structured fact identity, so a reworded reason string is not merely rejected, it is not expressible as an unlock. Keep a stable refusal code as the backstop for the nearest legal attempt ÔÇö a caller passing an unchanged predicate. The journal itself is bounded and immutable: appending past the entry limit or the text limit is a refusal carrying the limit that was hit, never a silent drop of the oldest entry, because a journal that forgets a dead end lets the same dead end be retried forever. Entries are held in an explicit total order over a stable key so the digest does not depend on insertion order. Deep-freeze what you return and derive the digest over exactly the returned value. No Date.now: every timestamp is caller-supplied canonical UTC. Target under 250 lines.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-5","title":null,"description":"TDD red for deterministic selection and admission ÔÇö DoD 1, the property this package exists to guarantee. Create packages/context/src/context-pipeline.test.ts. Assert the three admission arms explicitly: mandatory content that fits admits and the rendered output contains EVERY mandatory item; mandatory content that does not fit fails admission with CONTEXT_TOO_LARGE; and ÔÇö the case that catches the dangerous implementation ÔÇö mandatory content that would fit only if something were trimmed still fails with CONTEXT_TOO_LARGE rather than admitting a trimmed result. There must be no third outcome in which mandatory content is dropped and the call still succeeds; assert the outcome kinds by SET EQUALITY against the declared list so a \u0027degraded success\u0027 arm cannot appear unnoticed. Optional selection is where trimming is legal: assert it is deterministic ÔÇö the same inputs always select the same optional set, selection follows the declared priority order, and declared exclusions are honoured. Red.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-6","title":null,"description":"Implement packages/context/src/context-selection.ts to green. Selection takes the mandatory set, the optional candidates with their declared priority, the exclusions, and the byte budget, and returns either an admitted selection or a refusal. The single rule that governs the whole module: MANDATORY CONTENT IS NEVER DROPPED. Compute the mandatory cost first; if it alone exceeds the budget, refuse CONTEXT_TOO_LARGE immediately and select nothing ÔÇö do not proceed to fill the remainder, because a partially-filled result invites a caller to use it. Only after mandatory content is proven to fit may optional candidates be admitted, in declared priority order, skipping exclusions, stopping at the budget. Every comparison uses an explicit total order over a stable key ÔÇö no locale-sensitive sort, no reliance on object key order or Map iteration order, since those would make the same inputs render different bytes on a different host. Target under 250 lines.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-7","title":null,"description":"Implement packages/context/src/context-render.ts ÔÇö exact bytes and the digest manifest, DoD 2. The renderer turns an admitted selection into exact output bytes plus a manifest binding all SEVEN things DoD 2 names: the optional selection, the journal count limit, the journal text limit, the ordering, the renderer version literal, the exclusions, and the exact bytes. Bind them under the pinned manifest version with one canonical digest over the whole frozen manifest. Rendering is a pure function of the selection: the same selection renders byte-identical output every time, on any host. Extend context-pipeline.test.ts to assert the manifest\u0027s bound field set by SET EQUALITY against a hand-written list taken from the DoD ÔÇö so a field silently dropped from the binding fails rather than passing unnoticed ÔÇö and to assert the digest CHANGES when each field changes, driven from that same list so every field is covered by construction, with the generated case count asserted equal to the list length and non-zero.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-8","title":null,"description":"Add the release handoff package, the last deliverable, and wire the package surface. A handoff package is the immutable bundle a released attempt leaves behind: the rendered context manifest digest, the journal digest, and the external identities it cites ÔÇö graph, lease, and evidence-receipt references. CITE THOSE BY OPAQUE CONTENT-ADDRESSED REFERENCE, validated for shape only (64-hex), and do NOT import or reconstruct them: the Evidence receipt pipeline that owns receipt shape is still unbuilt, and guessing its internals here would create a seam that has to be undone. Add it to context-render.ts, or to a focused sibling module if that file would burst 400 lines, creating the sibling in this same step. Export the finished surface from src/index.ts. Assert that a handoff package is immutable, that its digest covers every cited identity, and that a malformed or non-64-hex reference refuses with its stable code rather than being stored unvalidated.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-9","title":null,"description":"Prove the assertions are load-bearing and sweep determinism. MUTATION DRILL at each mechanism\u0027s single source: the mandatory-fits check that produces CONTEXT_TOO_LARGE, the entry-count bound, the text-length bound, the typed-predicate comparison, and the explicit sort comparator. Neutralise each operand with a constant ÔÇö NEVER delete a line, since a deleted line from a multi-line condition is a syntax error that reddens zero tests and reads as \u0027survived\u0027. Count failed SUITES as well as failed tests and confirm the red test is the one naming that guard\u0027s code. The comparator mutation matters most here: replacing the total order with a constant must redden the ordering-determinism test, and if it does not, the determinism claim is untested. Back each file up out-of-tree and verify the restore with git hash-object. Then the determinism sweep: grep the package for Date.now, Math.random, localeCompare, and any sort without an explicit comparator, and confirm zero hits. Add no .js bridge files ÔÇö @moe/store is the only package that uses them and this is not it.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-10","title":null,"description":"Run the gate on the new package. From the repo root, fresh, capturing real output as evidence: pnpm --filter @moe/context typecheck \u0026\u0026 pnpm --filter @moe/context test. Because this package did not exist before this task, a filter miss looks exactly like a passing run with zero tests ÔÇö so assert the run actually executed your suites by checking the reported file and test counts are non-zero, and treat \u0027No test files found\u0027 as a FAILURE of this step, not a pass. Every failure here is unambiguously yours: nothing else in the repo touches packages/context. Also run root pnpm typecheck to confirm the new package did not break the workspace build. Confirm with wc -l (not PowerShell Measure-Object -Line, which skips blank lines and only ever reports room) that every production file is under the 400 hard cap, and record each count.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-11","title":null,"description":"Adversarial self-review, then commit by explicit pathspec. Read the diff as an attacker: is there ANY path on which mandatory context is dropped and the call still returns success; can prose reach the unlock predicate by any route; does the render digest actually cover all seven DoD-2 fields, verified by set equality rather than by eye; can the same inputs render different bytes on a different host through a locale sort, an unordered Map, or object key order; does the journal ever silently evict an entry to stay in bounds; is any cited external identity stored without shape validation. Then stage and commit by EXPLICIT PER-FILE pathspec ÔÇö git commit -- \u003cfile\u003e \u003cfile\u003e ... ÔÇö never a bare git commit, never git commit -a, never git add -A. Every packages/context/ file is new and uncontested, but pnpm-lock.yaml is SHARED with every in-flight task: confirm it carries only the single new importer entry before staging it, and unstage any foreign path with git restore --staged. Run git status --porcelain first. After committing, verify with git show --stat that only packages/context/ paths plus the one lock hunk landed, and that no debug, probe, scratch, or generated evidence file is present.","status":"PENDING","note":null,"modifiedFiles":null}],"taskRails":["Follow the pinned design; this task cannot create authority beyond its named contract.","Fail closed with stable codes and immutable deterministic outputs.","Keep production modules focused, target \u003c=250 lines, split before 400.","Use TDD and stage only explicit owned paths; no worktree, broad add, push, or merge."]},"planningNotes":{"approachesConsidered":"(1) WHY THIS IS NOT BLOCKED, AND IS IN FACT THE SAFEST TASK IN THE BATCH ÔÇö the promotion note warned this was \u0027two levels of speculation deep\u0027 because Evidence receipt pipeline is unbuilt. It turns out `packages/context/**` DOES NOT EXIST: this task creates a GREENFIELD PACKAGE. It touches no file any other in-flight task can touch, which makes it the lowest-collision task on the board right now. The Evidence receipt dependency enters only as a digest/reference a journal entry or handoff package may CITE ÔÇö model it as an opaque content-addressed ref validated for shape (64-hex) and never import or reconstruct the receipt. Nothing here needs the receipt\u0027s internal shape, so nothing here is blocked.\n(2) PACKAGE SCAFFOLD ÔÇö `pnpm-workspace.yaml` already globs `packages/*`, so a new `packages/context` is picked up with NO workspace-file edit. @moe/scheduler is the exact template to copy: a private ESM package with `exports: {\".\": \"./src/index.ts\"}`, zero dependencies, `typecheck: tsc --project tsconfig.json`, and a vitest script rooted at the repo. Prefer ZERO dependencies like @moe/scheduler; take @moe/contracts only if bounded-JSON decoding is genuinely needed at this layer, and say so in the evidence if you do.\n(3) DoD 3 ENFORCED BY SHAPE, NOT BY CHECK ÔÇö \u0027reworded prose alone cannot unlock a repeated dead end\u0027 is the same class of property as the evidence task\u0027s \u0027agent text cannot satisfy an obligation\u0027. The strongest form is that the unlock function has NO parameter through which prose could travel: it accepts a typed fact predicate, and the comparison is over structured fact identity, not text. A runtime `if (isJustProse)` guard is weaker because a caller can dress prose up as a reason string. Build it by shape, and keep a refusal code as the backstop for the nearest legal attempt.\n(4) TRUNCATION IS THE ENEMY ÔÇö DoD 1 and the NOT-in-scope line both say mandatory context is never silently truncated. So the selector must NEVER have a code path that drops a mandatory item to fit; over-budget mandatory content is an admission FAILURE (CONTEXT_TOO_LARGE), which is a refusal, not a degraded success.","codebaseInsights":"VERIFIED ON DISK 2026-08-08:\n- **`packages/context/` DOES NOT EXIST.** This task creates the package from nothing. That is why it collides with no one.\n- **`pnpm-workspace.yaml` globs `apps/*`, `adapters/*`, `packages/*`** with `autoInstallPeers: false`, `engineStrict: true`, `saveExact: true`, `strictPeerDependencies: true`. A new directory under packages/ needs NO workspace-file change.\n- **Scaffold template ÔÇö `packages/scheduler/package.json`** (the zero-dependency precedent):\n  `{ name, version: \"0.0.0\", private: true, type: \"module\", exports: { \".\": \"./src/index.ts\" }, scripts: { typecheck: \"tsc --project tsconfig.json\", test: \"vitest run --root ../.. packages/scheduler/src\" } }` with NO dependencies block at all.\n- **`packages/scheduler/tsconfig.json`**: extends `../../tsconfig.base.json`, `rootDir: \"src\"`, `tsBuildInfoFile: \"../../node_modules/.cache/\u003cname\u003e.tsbuildinfo\"`, `types: [\"node\"]`, `include: [\"src/**/*.ts\"]`. Copy this exactly, changing only the cache filename.\n- **Bridge convention**: @moe/store is the ONLY package that uses `.js` bridge files. @moe/runner, @moe/scheduler and @moe/daemon have none. A new package should follow the majority: **no bridges**. Do not add them.\n- Committed surfaces this package may cite by reference: @moe/core planning (planning-contract, planning-command-contract, graph-revision-contract) and @moe/scheduler authority/budget for lease facts. Prefer citing IDs and digests over importing, so the new package keeps zero dependencies.","risks":"1. **Determinism is the whole product here, so every source of nondeterminism is a defect**: no Date.now, no Math.random, no locale-sensitive comparison, no Object key-order reliance, no Set/Map iteration order leaking into output bytes. Sort with an explicit total order over a stable key. All timestamps are caller-supplied canonical UTC strings. Grep for Date.now and Math.random before the gate.\n2. **DoD 1\u0027s failure mode is a helpful truncation.** The dangerous implementation is one that trims a mandatory item \u0027just to fit\u0027 and returns success. There must be NO code path that drops mandatory content; assert it by mutating the budget check and confirming a test goes red naming CONTEXT_TOO_LARGE, and by asserting the rendered bytes contain every mandatory item when admission succeeds.\n3. **DoD 2 lists SEVEN things that must be deterministic and digest-bound** ÔÇö optional selection, journal count limit, journal text limit, ordering, renderer version, exclusions, exact bytes. Assert the bound field set with SET EQUALITY against a hand-written list from the DoD, and assert the digest changes when each one changes, driven from that same list so coverage is by construction and the case count is asserted non-zero (epic rail 6).\n4. `mem:gotcha-digest-mutation-that-proves-nothing`: a same-in/same-out digest test passes against a broken hash. Assert the adversarial pairs ÔÇö two inputs differing only by a character moved across a field boundary must differ (framing); the same content in a different key insertion order must be EQUAL (canonical ordering); `-0` versus `0` must differ.\n5. **DoD 3\u0027s trap**: a test that changes the prose and asserts \u0027still locked\u0027 is weak if the shape gate refuses it first for another reason. Assert which layer refused, and add the positive arm ÔÇö a genuine typed fact-predicate change DOES unlock ÔÇö so the test cannot pass by refusing everything.\n6. Journal limits are bounds, so test AT the boundary, not near it: exactly at the count limit admits, one over refuses; exactly at the text limit admits, one character over refuses.\n7. `pnpm-lock.yaml` gains an importer entry when the new package is installed. That file is shared state across every in-flight task ÔÇö keep the change to that single entry, verify with `git diff --stat pnpm-lock.yaml`, and never regenerate the whole lock.","keyFiles":["packages/scheduler/package.json","packages/scheduler/tsconfig.json","pnpm-workspace.yaml","tsconfig.base.json","packages/core/src/planning/planning-contract.ts"]},"nextAction":{"tool":"moe.start_step","args":{"taskId":"task-79eddf12e51346588cf6f37b96c56391","stepId":"step-1","workerId":"worker-10c55fbd"},"reason":"Begin step: Scaffold the new package, because packages/context does not exist yet. Create pa","recommendedSkill":{"name":"explore-before-assume","reason":"First step in code you have not verified. Load this before editing so you build on what actually exists, not assumptions."}}}
</claimed_task_context>

<inbox>
unread_general=10
unread_task=0
mentions=0 (see <routed_mentions> below if > 0)
memory_total=193 Serena memories (content via read_memory; names below are preloaded from disk - call list_memories only if they don't cover your area)
memory_this_task=none
memory_recent=decision-session-coordination-mailbox task-task-f837ce45bd344b868ad84e72ffc549f2-handoff task-task-791d73407af64b179f6099810d940758-qa-verdict decision-m1-backlog-dependency-map-2026-08-08 gotcha-runner-package-does-not-load-under-plain-node task-task-2580a578812f46a49cae0af79ff6fc16-qa-verdict gotcha-clamped-page-must-not-trust-unclamped-hasmore gotcha-node-does-not-resolve-js-specifier-to-ts task-task-791d73407af64b179f6099810d940758-handoff task-task-4a3b5ec031f14079bce4141abf922905-handoff gotcha-python-inplace-edit-flips-line-endings convention-hostile-shape-reads-in-pure-kernels task-task-2580a578812f46a49cae0af79ff6fc16-handoff gotcha-architect-team-role-null-blocks-amend-plan-step gotcha-testkit-temp-fixture-leak-ebusy decision-crash-injection-via-udf-temp-trigger task-task-7617c00dfc4a46eb81ebb8673f724855-qa-verdict task-task-7617c00dfc4a46eb81ebb8673f724855-handoff gotcha-scheduler-js-shims gotcha-vitest-hides-missing-js-bridge
</inbox>

<pending_questions>
{"count":0,"totalMatches":0,"tasks":[],"pagination":{"limit":10,"returned":0,"total":0,"hasMore":false},"truncatedQuestions":0}
</pending_questions>

<system-reminder>
Skill recommendation for this task's current phase: explore-before-assume
Why: First step in code you have not verified. Load this before editing so you build on what actually exists, not assumptions.
Before you call moe.start_step, invoke the Skill tool:
  Skill(skill="explore-before-assume")
This is not optional. Do not rationalize skipping it ("I'm blocking, not planning", "this is trivial", "I already know what it says"). Skills evolve — load the current version.
If after loading you decide it truly does not apply here, say so explicitly in chat — but LOAD IT FIRST.
</system-reminder>
