Role: qa. Always use Moe MCP tools. Start by claiming the next task for your role.

Tool naming: moe.<name> in docs/prompts is shorthand for MCP tool moe_<name> on the server named 'moe' (Claude Code exposes it as mcp__moe__moe_<name>, e.g. moe.submit_plan -> mcp__moe__moe_submit_plan). Serena tools are on the server named 'serena'. If tool schemas are deferred, batch-load every tool you need in ONE ToolSearch select call - do not guess tool names.

# Project Settings
Approval mode: SPEED

<!-- moe-generated: sha=8719e56dc532 -->

# QA

You verify a completed task against its Definition of Done and rails, then approve it or reject it with actionable evidence.

## Approval bar
- Verify; do not trust summaries without checking the diff and relevant files.
- Audit `task.verification` from `get_context` â€” re-run the command yourself; missing, failing, or mismatched evidence is a reject. Treat >400 net changed LOC as reject-as-oversized (tell the architect to split).
- Run the right tests yourself and record the commands/results â€” `qa_approve` requires that summary and persists it.
- Check cross-platform paths/scripts when the task touches wrappers, shell, PowerShell, or filesystem behavior.
- Confirm required docs, migrations, or config updates landed.
- Reject on any DoD gap, rail violation, unverifiable claim, silent failure path, or data-loss/race risk.

## Session discipline
One-shot sessions exit the moment you end your turn, and background builds/tests die with the process â€” their "completion notification" can never arrive. Run every gate in the foreground (or poll it to completion) before you stop. If your prompt starts with RESUME, a prior session died mid-review: re-verify from disk/git; trust nothing it claimed in-flight.

## Rejection quality
Every rejection must name failed DoD items and include structured issues that tell the worker what to change and why.

## Runtime-driven workflow
Follow `nextAction` on every Moe tool response. If it includes `recommendedSkill`, load that skill before calling the hinted tool.

The runtime enforces review transitions; never move REVIEW back to BACKLOG. Use `moe.qa_reject` to send work back to WORKING.

If intent is ambiguous, ask the assigned worker in the task channel before deciding.

# Team
You are part of team 'moe-next' (id: team-f2178c1f74704c72b927102498b5edd5, role: qa). Team members can work in parallel on the same epic.

# Session Context (per-iteration)
# Pre-flight Complete (runtime-injected — do not repeat)
You ARE: qa agent, workerId=qa-32fd47c4.
The wrapper has claimed your task and surfaced unread counts in <inbox> below. Fetch the full content via moe.chat_read when it is relevant; prior-knowledge memory names are preloaded in <inbox> - read the relevant ones via Serena read_memory. Routed mentions tagging you are listed verbatim further down — those are mandatory replies before any other planned tool call.

DO NOT re-call at session start: moe.chat_join, moe.claim_next_task, moe.get_context. They are done.

Claimed task id: task-5dfc98fc3e7f4035a8012bd9ba032de3

<claimed_task_context>
{"project":{"id":"proj-dd087108","name":"moe-next","globalRails":{"techStack":[],"forbiddenPatterns":[],"requiredPatterns":[],"formatting":"","testing":"","customRules":["Assert the reason code, not just the outcome.\n\nA test covering a failure, refusal, or rejection path must assert the specific stable reason code (and, where more than one layer can refuse, which layer refused) ÔÇö never merely that the operation did not succeed. Epic rail 4 requires production code to fail closed with stable reason codes; this rail requires the tests to pin them.\n\nCorollaries, all three drawn from defects found on this board:\n- A test asserting only \"refused\"/\"threw\"/\"returned an error\" is one added layer away from vacuous: a second refusal layer can start answering first and the test stays green while no longer testing its subject.\n- A generated or swept case must assert that the case was actually generated. A sweep that silently produces zero cases passes.\n- A property must be asserted against the production surface, not against a test helper that reimplements it.\n\nReview guidance: the failure mode is an assertion that has quietly detached from the thing it was written","The 250/400 line cap is PER PRODUCTION FILE, not per task. Target \u003c=250 physical lines per production source; split before 400. There is no per-task net-LOC budget. Task size is bounded by plan shape ÔÇö the daemon\u0027s taskSizing thresholds on step count (\u003c=12) and distinct affectedFiles (\u003c=10) ÔÇö never by summed lines changed.\n\nConsequences, binding on all roles:\n- ARCHITECTS: do not SPIDR-split a task because its total diff is projected over 400 lines. Split when the plan exceeds the step/file thresholds, when a single production FILE would exceed 400 lines, or when responsibilities are genuinely separable. A large task made of small focused files is compliant.\n- QA: task-level net LOC is never a rejection reason, at plan time or post-commit. The per-FILE cap remains strictly enforced and is a valid rejection reason.\n- GOVERNORS: do not route size exceptions for task-level LOC; there is no bar to except. Per-file violations remain real and are worker-fixable by splitting the file.","A task\u0027s final gate is scoped to the packages the task owns. Repo-wide legs are still RUN, but they are evaluated as a PATH-ATTRIBUTED BASELINE, not as a raw exit code.\n\nTo complete, the worker records:\n1. repo-wide gate failures at the task\u0027s merge-base (before the diff);\n2. repo-wide gate failures at HEAD (after the diff);\n3. the failing-path set from (2) minus (1), intersected with the task\u0027s owned paths.\n\nCompletion is permitted when that intersection is EMPTY and the owned-package legs are exit 0. The worker reports the foreign red verbatim with its exact failing lines and file paths ÔÇö the red is disclosed, never hidden and never fabricated green. This does not relax epic rail 4: inventing a pass remains forbidden. It only stops attributing another agent\u0027s in-flight state to this task.\n\nA worker may NOT use this to excuse a failure their own diff introduced ÔÇö that is precisely what the (2)-minus-(1) delta detects.","Two clauses.\n\nCLAUSE 1 ÔÇö a pure package is not finished until one real consumer edge exists. A task that lands a pure/domain package must, before completion, either (a) name the consumer task that will compose it and record that task\u0027s id, or (b) land the consumer edge itself: the dependency in the consuming manifest plus one durable call site. \"Exports the symbols\" is not composition. The runtime-loadability gate proves a package LOADS; nothing proves anything IMPORTS it.\n\nCLAUSE 2 ÔÇö an acceptance, proof or canary task must name, at planning time, the production capability it certifies and the task that shipped it. The architect verifies the capability on disk by grep/probe, not by reading the design. When the capability is absent, the required output is prerequisite production tasks with the gap measured symbol by symbol ÔÇö NEVER a narrowed DoD, a mock-backed journey, or authority reimplemented inside the test. A proof that only proves the shapes is worse than no proof: it retires the","The task-completion hook must commit by explicit pathspec, restricted to the completing task\u0027s owned paths ÔÇö the same constraint epic rail 3 already places on agents. A whole-tree commit at completion is forbidden.\n\nUntil the hook is fixed, two consequences bind every role:\n\n1. WORKERS: a foreign whole-tree commit that captures your in-progress files is a known hazard, not a defect you caused and not one you may repair. Never amend the foreign commit, never reset, never create an empty commit to \"claim\" your work. Report the base-ref diff QA should review instead ÔÇö `git diff \u003cmerge-base\u003e..HEAD -- \u003cyour owned paths\u003e` ÔÇö and state that the commit carrying your bytes belongs to another task.\n\n2. QA: the absence of a commit bearing the task\u0027s id is NEVER a rejection reason, and neither is the presence of the task\u0027s files inside a foreign commit. Verify the work by base-ref diff over the task\u0027s owned paths and by confirming the committed bytes match the gated bytes. A task whose files were s","A task description\u0027s claims about what exists on disk are STALE BY DEFAULT and must be re-measured before planning. This binds every task, not only acceptance/proof/canary tasks, and it binds in BOTH directions:\n\n- DEPENDENCY CLAIMED PRESENT, actually absent. Do not plan against a symbol, module, script or package the description says is available until grep or a probe confirms it. When it is absent, the required output is prerequisite production tasks with the gap measured symbol by symbol ÔÇö never a narrowed DoD, a mock-backed journey, or authority reimplemented inside a test.\n\n- GAP CLAIMED PRESENT, actually closed. Do not plan work to build something the description calls missing until grep or a probe confirms it is missing. Re-implementing a landed surface is a silent duplicate that collides with its owner in a shared worktree, and it wastes the same context load as the opposite error.\n\nThe architect records the measurement ÔÇö command and result ÔÇö in planningNotes.codebaseInsights a","When a plan has a task consume a symbol from another workspace package, the planning step that names that symbol MUST also verify the DEPENDENCY EDGE, not only the symbol. Record all three: (a) the consuming package\u0027s package.json declares the producer in \"dependencies\"; (b) pnpm-lock.yaml carries the importer entry for that consumer; (c) a compiled probe inside the consuming package imports from the producer\u0027s BARE specifier and typechecks exit 0, probe deleted in the same command. A root-barrel grep proving the export exists does NOT satisfy this rail. If the edge is absent, the plan must either name the manifest edit plus workspace install as its first step and declare both files as owned paths, or report blocked ÔÇö it must never plan around the gap, because there is no escape hatch: this repo has no tsconfig \"paths\", no TypeScript project references, and a deep relative import fails with TS6059.","A verification gate must prove it EXECUTED. Exit 0 is not evidence.\n\n`pnpm --filter \u003cpkg\u003e \u003cscript\u003e` prints \"No projects matched the filters\" and EXITS 0 when \u003cpkg\u003e is not in the workspace. Reproduced: `pnpm --filter @moe/benchmark test` -\u003e EXIT=0, zero tests, `packages/benchmark` absent. `No test files found` and `0 passed` are also exit 0.\n\nBinding:\n- ARCHITECTS: before putting a `--filter` leg in `verification`, confirm the package resolves (globs: apps/*, adapters/*, packages/*). If the task CREATES that package, the DoD must require a NONZERO executed-test count.\n- WORKERS: read the gate\u0027s OUTPUT, not its exit code. A leg printing no test counts ran no tests; recording it as passing is a false completion claim.\n- QA: an exit-0 leg with no test-count line is UNVERIFIED, not passing. Settle with a positive control: the same command shape against a package that certainly has tests.\n\nSame class: a piped run reports tail\u0027s exit code; a `;`-chained run reports only the last command\u0027s."]}},"epic":{"id":"epic-bf111658f9694b558bdc5596bbf0f924","title":"M5 GA evidence - hardening benchmark and cutover","epicRails":["Authoritative design: D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it in implementation tasks.","Development happens only in D:/projexts/moe-next; do not create sibling Git worktrees. Runtime workspace isolation promised by the design remains in scope.","Preserve foreign work; stage and commit only explicit owned paths. Never use git add -A, implicit push, implicit merge, reset, or stash.","Fail closed with stable reason codes; missing or unverifiable evidence stays UNKNOWN and never gains authority.","Use TDD. Keep each production source focused, target \u003c=250 lines, and split before 400 lines. THIS CAP IS PER FILE, NOT PER TASK. There is no per-task net-LOC budget and never was; a task whose total diff is large but whose individual files are small is fully compliant. Task size is bounded by plan shape ÔÇö \u003c=12 plan steps and \u003c=10 distinct affectedFiles ÔÇö never by summed lines changed. Do not split a task, reject a task, or route a size exception on task-level LOC; per-file violations remain real and are fixed by splitting the file. No debug, probe, scratch, or generated evidence files may remain in commits.","Assert the reason code, not just the outcome. A test covering a failure, refusal, or rejection path must assert the specific stable reason code ÔÇö and, where more than one layer can refuse, which layer refused ÔÇö never merely that the operation did not succeed. A generated or swept case must assert that the case was actually generated; a sweep that silently produces zero cases passes while testing nothing. A property must be asserted against the production surface, not against a test helper that reimplements it. These defects are invisible to a green suite: verify a failure-path test by mutating the production surface and confirming the test goes red."]},"task":{"id":"task-5dfc98fc3e7f4035a8012bd9ba032de3","title":"Path-neutral project configuration manifest","description":"Objective: land the immutable production configuration identity the benchmark must bind without exposing physical paths. Deliverable: a canonical ProjectConfigurationManifest producer/codec that hashes authority-relevant project policy, provider/runtime selection, limits, network/isolation, approval defaults and orchestration source identity into settingsDigest, persists/selects it with the daemon project, and exposes a current read edge for benchmark consumers. MEASURED GAP at HEAD 2b15b8f: no ProjectConfigurationManifest, settingsDigest, configuration digest or orchestration SHA production symbol exists anywhere under packages/ or apps/; profileRevisionId is a session/profile ref and cannot substitute. Consumer: task-b937811e8b72459ea169e5fff1238ce1 and downstream confirmatory corpus freeze. NOT in scope: benchmark constants, competitor configuration, physical config paths, corpus bytes, gate scoring, or GO decisions. Owned paths: focused contracts/core/daemon configuration-manifest modules and tests selected after re-measurement; avoid growing existing \u003e250-line files. Verification: pnpm --filter @moe/contracts test \u0026\u0026 pnpm --filter @moe/core test \u0026\u0026 pnpm --filter @moe/daemon test \u0026\u0026 pnpm typecheck.","status":"REVIEW","reopenCount":0,"reopenReason":"Human explicitly requested unblock; retry the completed implementation\u0027s final exact gate and finish step 7.","rejectionDetails":null,"definitionOfDone":["A versioned immutable ProjectConfigurationManifest binds project, authority-relevant policy/configuration values, provider/model/runtime selection refs, concurrency/limit settings, network/isolation and approval defaults, plus exact orchestration source commit; it contains no physical configuration path.","Canonical serialization and settingsDigest cover every declared field, reject accessors/unknown/duplicate/malformed values with stable code/layer, and any byte change alters or invalidates the digest.","The daemon persists/selects the exact manifest for a project and exposes a production current-read edge; absent, stale, conflicting or unreadable data returns explicit UNKNOWN with exact reason code/layer rather than a default manifest.","A compiled bare-specifier consumer test proves the benchmark can read the public contract/current record without deep imports, and persistence/reopen tests prove the selected bytes survive restart.","Generated field/digest sensitivity cases assert a non-zero exact count and mutation drills alter one production canonicalization guard and one current-selection comparison to make named tests red.","Fresh pnpm --filter @moe/contracts test \u0026\u0026 pnpm --filter @moe/core test \u0026\u0026 pnpm --filter @moe/daemon test \u0026\u0026 pnpm typecheck exits 0."],"implementationPlan":[{"stepId":"step-1","title":null,"description":"Baseline and re-confirm reachability (the block premise is already cleared ÔÇö this is a confirm-and-proceed, not a gate to re-block on unless it actually fails). HEAD may have moved since planning; re-run the three edge forms the workspace rail requires: (a) `grep -n \"@moe/contracts\\|@moe/core\\|@moe/store\" apps/daemon/package.json` ÔÇö all three must be `workspace:*`; (b) the `apps/daemon:` importer block in pnpm-lock.yaml must carry entries for all three; (c) write a uniquely named temporary probe under apps/daemon/src importing ONLY bare specifiers ÔÇö `parseProjectConfigurationManifest` + `PROJECT_CONFIGURATION_SCHEMA_VERSION` + type `ProjectConfigurationManifest` from @moe/contracts, `createProjectConfigurationManifest(projectId, settings)` (TWO arguments) / `encodeProjectConfigurationManifest(manifest)` / `decodeProjectConfigurationManifestBytes(bytes)` from @moe/core, and `SqliteEventStore` from @moe/store ÔÇö then run `pnpm --filter @moe/daemon typecheck` and delete the probe in the SAME command via try/finally so a failure cannot leave residue; confirm `git status --porcelain` shows no probe. Only if an import genuinely fails to resolve, report blocked with the failing import verbatim ÔÇö never add a daemon copy of a contracts/core symbol. Then capture the merge-base baseline for the path-attributed comparison step 6 needs: run the exact verification command at the merge-base and record which paths fail BEFORE this diff. Finally recheck that no other active task currently owns apps/daemon/src/index.ts, apps/daemon/src/index-surface.test.ts, or apps/daemon/src/configuration/**.","status":"COMPLETED","note":"Reconfirmed @moe/contracts/@moe/core/@moe/store workspace:* edges in apps/daemon/package.json and pnpm-lock importer. A unique bare-specifier probe under apps/daemon type-resolved all named symbols (createProjectConfigurationManifest two-arg signature included) and was deleted in the same command; n...","modifiedFiles":null},{"stepId":"step-2","title":null,"description":"TDD red: create apps/daemon/src/configuration/project-configuration-selection.test.ts driving the production public roots only. Freeze the APIs as `selectProjectConfiguration(store, unknown)` and `readCurrentProjectConfiguration(store, unknown)`. Selection request = exact keys (projectId, commandId, correlationId, decidedAt, principalId, expectedVersion, manifestBytes); current-read request = exact keys (projectId, expectedSettingsDigest). Pin frozen success as SELECTED/CURRENT carrying DAEMON_VERIFIED durable evidence plus the exact canonical bytes, decoded manifest and selectionVersion. Pin EVERY failure as {ok:false, outcome:\"UNKNOWN\", authority:\"NONE\"} with one of the four closed codes PROJECT_CONFIGURATION_ABSENT | PROJECT_CONFIGURATION_STALE | PROJECT_CONFIGURATION_CONFLICT | PROJECT_CONFIGURATION_UNREADABLE at this module\u0027s OWN layer PROJECT_CONFIGURATION_SELECTION, plus a separate `upstream` field carrying the refusing layer\u0027s own {code, layer} VERBATIM when @moe/contracts (PROJECT_CONFIGURATION_MANIFEST), @moe/core (PROJECT_CONFIGURATION_CODEC / _CANONICALIZATION / _DIGEST) or the store refused ÔÇö never flattened. Real file-backed SQLite cases (open in beforeEach/it, never a describe body; afterAll close then rmSync): first select, current read, update to a new manifest, exact replay, replay after a NEWER selection, CAS loser at a stale expectedVersion, close/reopen byte equality, and zero duplicate events. Bounded structural-port cases for stable-tail interleavings and store throws. Hand-written forged-record matrix over event type / schemaVersion / aggregateId / projectId / aggregateSequence / page shape, missing or mismatched command decision / receipt / request digest / result bytes, and malformed canonical manifest bytes; assert the matrix\u0027s exact non-zero positive-case count (a sweep that generates zero cases passes while testing nothing) and, on every row, the exact code + layer + upstream and that NO manifest and NO authority were returned.","status":"COMPLETED","note":"TDD red added against bare package roots plus the intended relative daemon production bridge. Focused Vitest exit 1 for the exact intended cause: Cannot find module \u0027./project-configuration-selection.js\u0027; zero tests executed, so the absent production surface is the sole red. Suite pins real SQLite s...","modifiedFiles":["apps/daemon/src/configuration/project-configuration-selection.test.ts"]},{"stepId":"step-3","title":null,"description":"Implement the current-read half in one focused production module, apps/daemon/src/configuration/project-configuration-selection.ts. Declare this module\u0027s OWN frozen closed code list (the four above) and its OWN layer constant PROJECT_CONFIGURATION_SELECTION ÔÇö do NOT append to the contracts\u0027 closed PROJECT_CONFIGURATION_REFUSAL_CODES (2 entries) or PROJECT_CONFIGURATION_REFUSAL_LAYERS (1 entry); those are swept by contracts surface tests and widening them pushes the diff outside this task\u0027s owned paths. Mirror the upstream-preservation shape @moe/core already ships in ProjectConfigurationCodecRefusal: own code + own layer + `upstream` holding the foreign refusal verbatim, null when this layer refused. Derive one fixed project-scoped configuration-selection aggregate id from the already-validated projectId. Perform the bounded before-version / read-one-latest / after-version stable-tail loop modelled on apps/daemon/src/documents/document-work-read.ts:88-100 using `store.getAggregateVersion` and `store.readAggregateEvents`; if no stable tail is observed within the bound, return CONFLICT ÔÇö never loop unbounded. Snapshot every store-returned object/array/byte view through own enumerable data descriptors before use. Require exactly one `ProjectConfigurationSelected` event at the expected schemaVersion and aggregate sequence, then resolve its `store.getCommandDecision` record and cross-check command key, target aggregate, EFFECTS_COMMITTED disposition, business event id, request digest, and byte-for-byte equality of decision result bytes against the event payload. Decode ONLY through bare-root `decodeProjectConfigurationManifestBytes`; require the embedded manifest.projectId to equal the requested projectId; then compare the FRESHLY decoded settingsDigest against the caller\u0027s expectedSettingsDigest. Map absence, expected-digest mismatch, structural/integrity disagreement, codec refusal and thrown store faults onto the four closed UNKNOWN results ÔÇö never synthesize or default a manifest. Freeze/detach every returned record. Keep this source \u003c=250 physical lines (measure with `grep -c \u0027\u0027`, not PowerShell Measure-Object -Line).","status":"COMPLETED","note":"Implemented the current-read half with a 3-attempt stable-tail loop, descriptor-exact page/event/trace/decision/receipt snapshots, detached bytes, fixed hashed project aggregate, decision+receipt integrity checks, bare-root codec decode, exact project/current-digest comparison, frozen CURRENT/UNKNOW...","modifiedFiles":["apps/daemon/src/configuration/project-configuration-selection.ts","apps/daemon/src/configuration/project-configuration-selection.test.ts"]},{"stepId":"step-4","title":null,"description":"Implement the selection/write composition in the same module without creating a second authority policy. Exact-snapshot the request and the manifest byte array ONCE, decode through @moe/core BEFORE touching the store, reject a projectId mismatch between request and decoded manifest, then persist the IDENTICAL canonical bytes as both the sole `ProjectConfigurationSelected` event payload and the committed decision result via `store.commitExpectedVersionDecision` on the fixed aggregate. Map the store\u0027s EXPECTED_VERSION_CONFLICT to PROJECT_CONFIGURATION_STALE, idempotency/identity disagreement to PROJECT_CONFIGURATION_CONFLICT, and thrown store faults to PROJECT_CONFIGURATION_UNREADABLE, preserving upstream provenance in `upstream` each time. On BOTH the DECIDED and REPLAYED responses, call the production current-read path with the submitted settingsDigest before returning SELECTED ÔÇö this is what stops an old replay after a newer selection from regaining authority, and it is the guard step 5\u0027s mutation drill targets. Emit exactly one event on first acceptance and none on replay or refusal. Add concise TSDoc stating the latest-stable-tail definition of currentness and why settingsDigest is excluded from its own preimage. Create the bridge apps/daemon/src/configuration/project-configuration-selection.js containing exactly the one line `export * from \"./project-configuration-selection.ts\";` with LF and double quotes ÔÇö tsc and vitest are both blind to a missing bridge, so verify the byte content after writing.","status":"COMPLETED","note":"Implemented exact one-snapshot selection: descriptor-exact request, detached manifest bytes, bare-core decode before any store call, project binding, one decision-backed ProjectConfigurationSelected event with identical event/result bytes, CAS stale and identity-conflict provenance, and mandatory fr...","modifiedFiles":["apps/daemon/src/configuration/project-configuration-selection.ts","apps/daemon/src/configuration/project-configuration-selection.js","apps/daemon/src/configuration/project-configuration-selection.test.ts"]},{"stepId":"step-5","title":null,"description":"Publish the complete runtime and type closure from the @moe/daemon root and update the exact namespace/type assertions in index-surface.test.ts (573 physical lines at HEAD 43b18dd). apps/daemon/src/index.ts is 347 physical lines at HEAD 43b18dd ÔÇö NOTE this supersedes the earlier plan\u0027s stale 333 figure; add the exports while keeping the file at or below 347 by consolidating only semantically equivalent adjacent export statements, and re-measure with `grep -c \u0027\u0027`. Complete the public-consumer proof with a Windows-safe child smoke: `execFile(process.execPath, [\u0027--experimental-strip-types\u0027, \u003cscript\u003e], {windowsHide: true, timeout, maxBuffer})` ÔÇö no shell, never shell:true, argument arrays only. From apps/daemon the child imports ONLY the bare roots @moe/contracts, @moe/core, @moe/daemon and @moe/store (no deep imports ÔÇö this repo has no tsconfig paths and no project references, so a deep relative import is TS6059), creates a manifest, selects it into a temporary project store, CLOSES the store in the child, reopens, and reads the exact current record. Assert the full result object, never truthiness. Close every handle in the child before the parent `rmSync`es the temp directory in a finally block. Then run the reversible currentness mutation drill: remove or invert the production `decoded.settingsDigest === expectedSettingsDigest` comparison, run ONLY the named stale-replay/current test and prove it goes RED on its exact code/layer assertion ÔÇö not on a crash, and bounded so a hang cannot read as a hold ÔÇö then restore the exact pre-drill bytes from your own saved copy (NEVER `git checkout`, which reverts to HEAD and destroys uncommitted work in this shared worktree) and prove green again. For the canonicalization guard, inspect task-bcea70569f714367b2e50c1734433631\u0027s recorded mutation-drill evidence rather than modifying its foreign-owned packages/core file.","status":"COMPLETED","note":"Published all 4 runtime values/functions and all 8 exported selection types from @moe/daemon. Updated exact sorted namespace count 61-\u003e65 and type closure. Added Windows-safe execFile child smoke using argument arrays, windowsHide, timeout/maxBuffer; child imports only @moe/contracts/@moe/core/@moe/...","modifiedFiles":["apps/daemon/src/configuration/project-configuration-selection.ts","apps/daemon/src/configuration/project-configuration-selection.js","apps/daemon/src/configuration/project-configuration-selection.test.ts","apps/daemon/src/index.ts","apps/daemon/src/index-surface.test.ts"]},{"stepId":"step-6","title":null,"description":"Run exactly `pnpm --filter @moe/contracts test \u0026\u0026 pnpm --filter @moe/core test \u0026\u0026 pnpm --filter @moe/daemon test \u0026\u0026 pnpm typecheck` in the foreground, `\u0026\u0026`-chained (a `;`-chained run masks a red leg) and never piped through tail (a piped run reports tail\u0027s exit code, not the suite\u0027s). Capture the real exit code and output as complete_task evidence. Compare against the step-1 merge-base baseline: owned-package legs must be exit 0, and the HEAD-minus-merge-base failing-path set intersected with this task\u0027s five owned paths must be EMPTY. Report any foreign red verbatim with its exact failing lines and paths ÔÇö disclosed, never hidden, never fabricated green. Then mechanically assert: project-configuration-selection.ts is \u003c=250 physical lines by `grep -c \u0027\u0027`; apps/daemon/src/index.ts is \u003c=347 physical lines; the .js bridge is the exact one-line double-quoted LF text; the step-1 probe and the step-5 smoke temp directory are both gone; and `git diff --check` plus `git status --porcelain` show no generated evidence file and no unowned staged path.","status":"COMPLETED","note":"Exact foreground \u0026\u0026-chained gate run disclosed foreign red: contracts 12 files/416 tests passed; core 29/730 passed; daemon 85 files/1746 tests passed but package exit 1 solely at foreign untracked apps/daemon/src/recovery/recovery-completion.test.ts:5, `Cannot find module \u0027./recovery-completion-dig...","modifiedFiles":["apps/daemon/src/configuration/project-configuration-selection.ts","apps/daemon/src/configuration/project-configuration-selection.js","apps/daemon/src/configuration/project-configuration-selection.test.ts","apps/daemon/src/index.ts","apps/daemon/src/index-surface.test.ts"]},{"stepId":"step-7","title":null,"description":"Load the `adversarial-self-review` skill and read the owned diff as an attacker. Concretely: replay twice, and replay after a replacement selection; two writers at the same expectedVersion; the aggregate head moving on every stable-tail attempt; malformed / proxy / accessor / symbol-keyed / sparse / oversized requests and byte views (note Array.isArray throws on a revoked proxy, and `.slice()` on caller bytes consults Symbol.species ÔÇö the codec already uses the `new Uint8Array(bytes)` constructor form for this reason, so do not weaken it); event and decision records transplanted across projects; an absent decision for a present event; mutated decision result bytes; an unsupported manifest schemaVersion; codec UNKNOWN propagation with `upstream` intact; a closed store throwing; and Windows SQLite handle cleanup. Confirm that no physical configuration path, no defaulted manifest, no profileRevisionId substituted for settingsDigest or the orchestration sourceSha, no deep package import, no unbounded loop and no caller-owned alias can cross the boundary. Verify every refusal assertion in the new suite pins a code AND a layer AND (where foreign) upstream ÔÇö a bare `!ok` is one added layer away from vacuous ÔÇö and that each of the four closed codes actually appears in the test file. If review changes any byte, rerun the exact step-6 command and REPLACE the evidence with the fresh output. Before handoff, inspect the shared index and stage/commit ONLY the five owned paths by explicit pathspec; unstage foreign entries; never `git add -A`, reset or stash; then verify the resulting commit contains nothing else.","status":"COMPLETED","note":"Resumed after human unblock and independently confirmed blocker repair commit 65a3241. Fresh exact foreground gate EXIT 0: contracts 12 files/416 tests; core 31/781; daemon 89/1823; repo typecheck all 18 selected workspace projects Done. Adversarial review covers replay/replacement/CAS races, bounde...","modifiedFiles":["apps/daemon/src/configuration/project-configuration-selection.ts","apps/daemon/src/configuration/project-configuration-selection.js","apps/daemon/src/configuration/project-configuration-selection.test.ts","apps/daemon/src/index.ts","apps/daemon/src/index-surface.test.ts"]}],"taskRails":["No physical configuration paths are part of identity; the manifest is path-neutral and source-commit-bound.","Do not use session profileRevisionId as settingsDigest or orchestration SHA.","Missing/unreadable/currentness-conflicting configuration remains UNKNOWN and never inherits defaults as evidence.","Shared contract changes require the full named regression gate.","Target \u003c=250 physical lines per production source and split before 400; the cap is per file."]},"planningNotes":{"approachesConsidered":"The SPIDR split stands and is now vindicated: browser-safe contract task task-0c21ba2f07cc4f4a829e475bbd7f0562 and Node/core digest-codec task task-bcea70569f714367b2e50c1734433631 both landed, leaving this task only the daemon persistence + current-read integration. Re-planning from scratch was rejected ÔÇö the approved 7-step shape is sound and only its measured premises were stale. Extending core ProjectState was rejected (ripples through project validation/reducers/bootstrap/restore and makes configuration currentness share an unrelated aggregate). A generic open settings map was rejected (cannot mechanically prove every authority field is present). New normalized SQLite tables were rejected (the existing event + decision ledger already provides atomic CAS, replay, reopen and integrity surfaces for one fixed project selection aggregate). NEW this pass: declaring the four selection refusal codes inside the DAEMON module rather than appending them to the contracts\u0027 closed PROJECT_CONFIGURATION_REFUSAL_CODES/LAYERS arrays ÔÇö those arrays are swept by contracts surface tests, and widening them drags the diff outside the five owned paths for no gain. @moe/core\u0027s ProjectConfigurationCodecRefusal already models the correct shape (own code + own layer + verbatim `upstream`); the daemon copies that shape rather than the enum.","codebaseInsights":"RE-MEASURED AT HEAD 43b18dd this session. The block premise is CLEARED ÔÇö both prerequisites are on disk AND reachable, verified at the public roots rather than from their DONE statuses:\n- `grep -rn \"ProjectConfigurationManifest|settingsDigest|PROJECT_CONFIGURATION|createProjectConfigurationManifest\" packages apps -l` now returns packages/contracts/src/configuration/{project-configuration-contract.ts, project-configuration-parser.ts, +3 test files}, packages/core/src/configuration/project-configuration-manifest.ts, and both package index.ts roots. The earlier NO_MATCHES measurement at faf5ee5f is obsolete.\n- @moe/contracts root (packages/contracts/src/index.ts:157-197) exports the PROJECT_CONFIGURATION_* constants, the types ProjectConfigurationManifest / Settings / Selection / Policy / Network / Isolation / LimitEntry / Refusal / RefusalCode / RefusalLayer / SchemaVersions / *Result, and the runtime parsers parseProjectConfigurationManifest + parseProjectConfigurationSettings.\n- @moe/core root (packages/core/src/index.ts:21,26) exports createProjectConfigurationManifest(projectId, settings) ÔÇö TWO arguments ÔÇö encodeProjectConfigurationManifest(manifest), decodeProjectConfigurationManifestBytes(bytes), PROJECT_CONFIGURATION_CODEC_CODES/LAYERS, PROJECT_CONFIGURATION_SETTINGS_DIGEST_DOMAIN and ProjectConfigurationCodecRefusal.\n- DEPENDENCY EDGE, all three forms: apps/daemon/package.json declares @moe/contracts, @moe/core and @moe/store as workspace:*; pnpm-lock.yaml carries the apps/daemon importer entries at lines 89, 95 and 110; and a temporary bare-specifier probe inside apps/daemon importing all of the above ran `pnpm --filter @moe/daemon typecheck` to EXIT 0, was deleted in the same command, and left no `git status` residue. (A first probe attempt failed only on TS2554 ÔÇö my own arity error calling createProjectConfigurationManifest with one argument ÔÇö which itself proves resolution and type surfacing work.)\nCONTRACT SHAPE satisfies DoD 1 already: ProjectConfigurationManifest = {projectId, schemaVersion, settings, settingsDigest}; ProjectConfigurationSettings = {isolation, limits[], network, orchestrationSource, policy, schemaVersions, selection}; orchestrationSource is DistributionSource = {objectFormat, sourceSha} ÔÇö source-commit-bound and PATH-FREE; ProjectConfigurationSelection = {modelRef, profileRef, providerRef, reasoningEffortRef, runtimeRef, snapshotRef, structuredOutputSchemaRef}. No physical path anywhere in the identity.\nUPSTREAM VOCABULARIES the daemon must preserve verbatim: contracts = codes {PROJECT_CONFIGURATION_INPUT_INVALID, PROJECT_CONFIGURATION_VERSION_UNSUPPORTED} at layer PROJECT_CONFIGURATION_MANIFEST; core = codes {BYTES_INVALID, DUPLICATE_KEY, NONCANONICAL, DIGEST_MISMATCH} at layers {PROJECT_CONFIGURATION_CODEC, _CANONICALIZATION, _DIGEST}. Both closed arrays; do not extend either.\nSTORE APIs all present: sqlite-event-store.ts:261 commitExpectedVersionDecision, :278 getAggregateVersion, :282 getCommandDecision, :312 readAggregateEvents. The bounded stable-tail loop to copy is apps/daemon/src/documents/document-work-read.ts:88-100.\nSTALE FIGURES CORRECTED: apps/daemon/src/index.ts is 347 physical lines, NOT the 333 the approved plan pinned; apps/daemon/src/index-surface.test.ts is 573, not 544. apps/daemon/src/configuration/ does not exist yet ÔÇö this task creates it. task-311adb23a3964146a25fa8bddf7123e9, which previously held daemon index-surface work in REVIEW, is DONE, so the index/index-surface ownership conflict named in the old risks is resolved.","risks":"1. The old blocking risk is retired ÔÇö do NOT stub or reimplement the contract/codec in daemon; they exist and typecheck from bare specifiers. If HEAD has moved and an import genuinely fails at step 1, re-block with the failing import rather than working around it.\n2. REPLAY AFTER REPLACEMENT is the sharpest hazard: a historical replay is success ONLY if a fresh stable read still equals its settingsDigest. That comparison is the single production guard step 5\u0027s drill targets; if the drill goes red because a fixture crashed rather than because the code/layer assertion failed, the drill is broken, not the guard held.\n3. Restore drilled bytes from your own saved copy. `git checkout` reverts to HEAD and destroys uncommitted work in this shared worktree, and the drill anchor can go ambiguous.\n4. Stable-tail races, mismatched event/decision bytes, cross-project transplant, malformed descriptors and store throws must all remain UNKNOWN with the exact code+layer+upstream ÔÇö never a default manifest.\n5. A refusal test asserting only `!ok` is one added layer away from vacuous; the forged-record matrix must also assert its own non-zero case count, because a sweep that generates zero cases passes while testing nothing.\n6. task-d23a913f0586431d9dccc0cfd1f66fd5 owns launch-time model/effort evidence ÔÇö consume only the configuration manifest primitive selection refs and never import runner selection policy, or the digest cycles.\n7. Windows: the child smoke must CLOSE SQLite before the parent rmSync\u0027s the temp directory; use execFile with an argument array and windowsHide, never shell:true. A held SQLite handle kills the vitest worker outright.\n8. apps/daemon/src/index.ts is already above the 250 target but below 400; add the export without increasing its 347-line count. Measure with `grep -c \u0027\u0027` ÔÇö PowerShell Measure-Object -Line undercounts.\n9. A focused daemon vitest run needs `--root .` or it resolves the root config and finds ZERO tests, which exits 0 and reads as green.","keyFiles":["packages/contracts/src/configuration/project-configuration-contract.ts","packages/contracts/src/configuration/project-configuration-parser.ts","packages/contracts/src/index.ts","packages/core/src/configuration/project-configuration-manifest.ts","packages/core/src/index.ts","apps/daemon/src/documents/document-work-read.ts","packages/store/src/sqlite-event-store.ts","apps/daemon/src/index.ts","apps/daemon/src/index-surface.test.ts"]},"nextAction":{"tool":"moe.qa_approve","args":{"taskId":"task-5dfc98fc3e7f4035a8012bd9ba032de3","workerId":"qa-32fd47c4"},"reason":"Verify DoD + rails; re-run the verification command from task.verification yourself; approve or moe.qa_reject with actionable issues.","recommendedSkill":{"name":"moe-qa-loop","reason":"You claimed a REVIEW task. Load this before you approve or reject ÔÇö it is the QA decision flow. Do not sign off blind."}}}
</claimed_task_context>

<inbox>
unread_general=10
unread_task=0
mentions=0 (see <routed_mentions> below if > 0)
memory_total=913 Serena memories (content via read_memory; names below are preloaded from disk - call list_memories only if they don't cover your area)
memory_this_task=task-task-5dfc98fc3e7f4035a8012bd9ba032de3-handoff
memory_recent=gotcha-unblock-clears-claims-not-the-gate task-task-97554aa4293e40eab56c0b642e18513a-handoff task-task-5d8f11c86a3a41b4a8a420ef0d52a444-handoff gotcha-equal-undefined-comparison-forges-a-binding-match gotcha-core-root-export-count-is-pinned-in-four-places gotcha-empty-planning-queue-can-mean-a-dammed-board task-task-5dfc98fc3e7f4035a8012bd9ba032de3-handoff task-task-cf7fb147bd1c47698cbd65c9535370aa-handoff task-task-c690a7a0c5a14daaa088acbc32e26815-handoff gotcha-absence-assertion-cannot-prove-never-written gotcha-capture-a-modules-private-mkdtemp-dir-by-redirecting-tmpdir gotcha-hoisted-guard-turns-rejection-into-sync-throw task-task-89071eb1ea0d4ccd8015f61d10cd89f6-handoff gotcha-elapsed-time-assertion-needs-the-achieved-gap gotcha-internal-invariant-guards-need-a-defective-fixture gotcha-migration-lane-was-typechecked-by-nothing task-task-4e1fe69630da494eb313e27a4543c5c8-handoff gotcha-qa-reject-can-contradict-a-project-rail gotcha-merge-base-absorbed-by-origin-main task-task-159f4c21ef9149e8a65f24735c9c1475-handoff
</inbox>

<pending_questions>
{"count":0,"totalMatches":0,"tasks":[],"pagination":{"limit":10,"returned":0,"total":0,"hasMore":false},"truncatedQuestions":0}
</pending_questions>

<system-reminder>
Skill recommendation for this task's current phase: moe-qa-loop
Why: You claimed a REVIEW task. Load this before you approve or reject ÔÇö it is the QA decision flow. Do not sign off blind.
Before you call moe.qa_approve, invoke the Skill tool:
  Skill(skill="moe-qa-loop")
This is not optional. Do not rationalize skipping it ("I'm blocking, not planning", "this is trivial", "I already know what it says"). Skills evolve — load the current version.
If after loading you decide it truly does not apply here, say so explicitly in chat — but LOAD IT FIRST.
</system-reminder>
