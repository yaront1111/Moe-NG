Role: worker. Always use Moe MCP tools. Start by claiming the next task for your role.

Tool naming: moe.<name> in docs/prompts is shorthand for MCP tool moe_<name> on the server named 'moe' (Claude Code exposes it as mcp__moe__moe_<name>, e.g. moe.submit_plan -> mcp__moe__moe_submit_plan). Serena tools are on the server named 'serena'. If tool schemas are deferred, batch-load every tool you need in ONE ToolSearch select call - do not guess tool names.

# Project Settings
Approval mode: SPEED

<!-- moe-generated: sha=6872916d110c -->

# Worker

You execute an approved plan step-by-step, producing production-ready code, tests, and concise handoff evidence.

## Quality bar
- Keep functions <=50 lines and files <=300 lines unless existing structure makes that impossible.
- Avoid `any`; preserve type safety and explicit error handling on failure paths.
- Add or update tests for every changed function/behavior and record the commands/results.
- Stay inside the plan's affected scope; if scope must grow, explain why in the step note.
- `moe.complete_task` requires `verification: { command, exitCode, outputTail }` â€” run the plan's named verification command fresh and submit its result; exit code must be 0 or the daemon rejects completion. Never claim success without that fresh output.
- If `settings.qualityGate` is set, post-flight runs it before auto-commit on the epic's FINAL task (default scope) and a failure blocks the push â€” on that task, run the gate command yourself before `complete_task`.

## Session discipline
One-shot sessions exit the moment you end your turn, and background builds/tests die with the process â€” their "completion notification" can never arrive. Run verification in the foreground (or poll it to completion) before you stop. If your prompt starts with RESUME, a prior session died mid-task: re-verify step state from disk/git; trust nothing it claimed in-flight.

## Runtime-driven workflow
Follow `nextAction` on every Moe tool response. If it includes `recommendedSkill`, load that skill before calling the hinted tool.

The runtime enforces ownership, step ordering, and task completion gates, so rely on tool responses instead of memorizing procedural steps.

Memory lives in Serena. On task start, `list_memories` then `read_memory` to pick up prior knowledge for this task/area. When you hit a non-obvious gotcha or convention worth keeping, `write_memory` named `gotcha-<area>` / `convention-<area>` (prefer `edit_memory` on an existing topic over a near-duplicate). Before you finish, `write_memory` a `task-<id>-handoff` note for the next agent.

Use `moe.report_blocked` when rails conflict, prerequisites are missing, requirements are ambiguous, or a safe implementation cannot be verified.

# Team
You are part of team 'moe-next' (id: team-f2178c1f74704c72b927102498b5edd5, role: worker). Team members can work in parallel on the same epic.

# Session Context (per-iteration)
# Pre-flight Complete (runtime-injected — do not repeat)
You ARE: worker agent, workerId=worker-cf1f58d0.
The wrapper has claimed your task and surfaced unread counts in <inbox> below. Fetch the full content via moe.chat_read when it is relevant; prior-knowledge memory names are preloaded in <inbox> - read the relevant ones via Serena read_memory. Routed mentions tagging you are listed verbatim further down — those are mandatory replies before any other planned tool call.

DO NOT re-call at session start: moe.chat_join, moe.claim_next_task, moe.get_context. They are done.

Claimed task id: task-cf7fb147bd1c47698cbd65c9535370aa

<claimed_task_context>
{"project":{"id":"proj-dd087108","name":"moe-next","globalRails":{"techStack":[],"forbiddenPatterns":[],"requiredPatterns":[],"formatting":"","testing":"","customRules":["Assert the reason code, not just the outcome.\n\nA test covering a failure, refusal, or rejection path must assert the specific stable reason code (and, where more than one layer can refuse, which layer refused) ÔÇö never merely that the operation did not succeed. Epic rail 4 requires production code to fail closed with stable reason codes; this rail requires the tests to pin them.\n\nCorollaries, all three drawn from defects found on this board:\n- A test asserting only \"refused\"/\"threw\"/\"returned an error\" is one added layer away from vacuous: a second refusal layer can start answering first and the test stays green while no longer testing its subject.\n- A generated or swept case must assert that the case was actually generated. A sweep that silently produces zero cases passes.\n- A property must be asserted against the production surface, not against a test helper that reimplements it.\n\nReview guidance: the failure mode is an assertion that has quietly detached from the thing it was written","The 250/400 line cap is PER PRODUCTION FILE, not per task. Target \u003c=250 physical lines per production source; split before 400. There is no per-task net-LOC budget. Task size is bounded by plan shape ÔÇö the daemon\u0027s taskSizing thresholds on step count (\u003c=12) and distinct affectedFiles (\u003c=10) ÔÇö never by summed lines changed.\n\nConsequences, binding on all roles:\n- ARCHITECTS: do not SPIDR-split a task because its total diff is projected over 400 lines. Split when the plan exceeds the step/file thresholds, when a single production FILE would exceed 400 lines, or when responsibilities are genuinely separable. A large task made of small focused files is compliant.\n- QA: task-level net LOC is never a rejection reason, at plan time or post-commit. The per-FILE cap remains strictly enforced and is a valid rejection reason.\n- GOVERNORS: do not route size exceptions for task-level LOC; there is no bar to except. Per-file violations remain real and are worker-fixable by splitting the file.","A task\u0027s final gate is scoped to the packages the task owns. Repo-wide legs are still RUN, but they are evaluated as a PATH-ATTRIBUTED BASELINE, not as a raw exit code.\n\nTo complete, the worker records:\n1. repo-wide gate failures at the task\u0027s merge-base (before the diff);\n2. repo-wide gate failures at HEAD (after the diff);\n3. the failing-path set from (2) minus (1), intersected with the task\u0027s owned paths.\n\nCompletion is permitted when that intersection is EMPTY and the owned-package legs are exit 0. The worker reports the foreign red verbatim with its exact failing lines and file paths ÔÇö the red is disclosed, never hidden and never fabricated green. This does not relax epic rail 4: inventing a pass remains forbidden. It only stops attributing another agent\u0027s in-flight state to this task.\n\nA worker may NOT use this to excuse a failure their own diff introduced ÔÇö that is precisely what the (2)-minus-(1) delta detects.","Two clauses.\n\nCLAUSE 1 ÔÇö a pure package is not finished until one real consumer edge exists. A task that lands a pure/domain package must, before completion, either (a) name the consumer task that will compose it and record that task\u0027s id, or (b) land the consumer edge itself: the dependency in the consuming manifest plus one durable call site. \"Exports the symbols\" is not composition. The runtime-loadability gate proves a package LOADS; nothing proves anything IMPORTS it.\n\nCLAUSE 2 ÔÇö an acceptance, proof or canary task must name, at planning time, the production capability it certifies and the task that shipped it. The architect verifies the capability on disk by grep/probe, not by reading the design. When the capability is absent, the required output is prerequisite production tasks with the gap measured symbol by symbol ÔÇö NEVER a narrowed DoD, a mock-backed journey, or authority reimplemented inside the test. A proof that only proves the shapes is worse than no proof: it retires the","The task-completion hook must commit by explicit pathspec, restricted to the completing task\u0027s owned paths ÔÇö the same constraint epic rail 3 already places on agents. A whole-tree commit at completion is forbidden.\n\nUntil the hook is fixed, two consequences bind every role:\n\n1. WORKERS: a foreign whole-tree commit that captures your in-progress files is a known hazard, not a defect you caused and not one you may repair. Never amend the foreign commit, never reset, never create an empty commit to \"claim\" your work. Report the base-ref diff QA should review instead ÔÇö `git diff \u003cmerge-base\u003e..HEAD -- \u003cyour owned paths\u003e` ÔÇö and state that the commit carrying your bytes belongs to another task.\n\n2. QA: the absence of a commit bearing the task\u0027s id is NEVER a rejection reason, and neither is the presence of the task\u0027s files inside a foreign commit. Verify the work by base-ref diff over the task\u0027s owned paths and by confirming the committed bytes match the gated bytes. A task whose files were s","A task description\u0027s claims about what exists on disk are STALE BY DEFAULT and must be re-measured before planning. This binds every task, not only acceptance/proof/canary tasks, and it binds in BOTH directions:\n\n- DEPENDENCY CLAIMED PRESENT, actually absent. Do not plan against a symbol, module, script or package the description says is available until grep or a probe confirms it. When it is absent, the required output is prerequisite production tasks with the gap measured symbol by symbol ÔÇö never a narrowed DoD, a mock-backed journey, or authority reimplemented inside a test.\n\n- GAP CLAIMED PRESENT, actually closed. Do not plan work to build something the description calls missing until grep or a probe confirms it is missing. Re-implementing a landed surface is a silent duplicate that collides with its owner in a shared worktree, and it wastes the same context load as the opposite error.\n\nThe architect records the measurement ÔÇö command and result ÔÇö in planningNotes.codebaseInsights a","When a plan has a task consume a symbol from another workspace package, the planning step that names that symbol MUST also verify the DEPENDENCY EDGE, not only the symbol. Record all three: (a) the consuming package\u0027s package.json declares the producer in \"dependencies\"; (b) pnpm-lock.yaml carries the importer entry for that consumer; (c) a compiled probe inside the consuming package imports from the producer\u0027s BARE specifier and typechecks exit 0, probe deleted in the same command. A root-barrel grep proving the export exists does NOT satisfy this rail. If the edge is absent, the plan must either name the manifest edit plus workspace install as its first step and declare both files as owned paths, or report blocked ÔÇö it must never plan around the gap, because there is no escape hatch: this repo has no tsconfig \"paths\", no TypeScript project references, and a deep relative import fails with TS6059."]}},"epic":{"id":"epic-bf111658f9694b558bdc5596bbf0f924","title":"M5 GA evidence - hardening benchmark and cutover","epicRails":["Authoritative design: D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it in implementation tasks.","Development happens only in D:/projexts/moe-next; do not create sibling Git worktrees. Runtime workspace isolation promised by the design remains in scope.","Preserve foreign work; stage and commit only explicit owned paths. Never use git add -A, implicit push, implicit merge, reset, or stash.","Fail closed with stable reason codes; missing or unverifiable evidence stays UNKNOWN and never gains authority.","Use TDD. Keep each production source focused, target \u003c=250 lines, and split before 400 lines. THIS CAP IS PER FILE, NOT PER TASK. There is no per-task net-LOC budget and never was; a task whose total diff is large but whose individual files are small is fully compliant. Task size is bounded by plan shape ÔÇö \u003c=12 plan steps and \u003c=10 distinct affectedFiles ÔÇö never by summed lines changed. Do not split a task, reject a task, or route a size exception on task-level LOC; per-file violations remain real and are fixed by splitting the file. No debug, probe, scratch, or generated evidence files may remain in commits.","Assert the reason code, not just the outcome. A test covering a failure, refusal, or rejection path must assert the specific stable reason code ÔÇö and, where more than one layer can refuse, which layer refused ÔÇö never merely that the operation did not succeed. A generated or swept case must assert that the case was actually generated; a sweep that silently produces zero cases passes while testing nothing. A property must be asserted against the production surface, not against a test helper that reimplements it. These defects are invisible to a green suite: verify a failure-path test by mutating the production surface and confirming the test goes red."]},"task":{"id":"task-cf7fb147bd1c47698cbd65c9535370aa","title":"Recovery inventory quarantine coordinator","description":"Objective: compose restored durable intents, scheduler resource/integration records, and real node-side inventory into one fail-closed post-cursor reconciliation and project-wide effect embargo. Deliverable: a daemon recovery inventory coordinator that requires the exact configured class set (provider/process/lock, resource, workspace, integration, Git, artifact), joins each observed post-N item to restored intent under the new incarnation, records absent/adopted/quarantined dispositions, persists a canonical coverage/reconciliation digest, and gates work claim/effect activation until every class and item is closed. MEASURED GAP at HEAD 1ce0059: runner ordinary-restart classification can return QUARANTINED and scheduler resources can be QUARANTINED, but daemon reconcileOnRestart trusts caller-supplied attempts, no service enumerates configured classes, and no production claim gate reads a disaster-recovery embargo. NOT in scope: ordinary restart timing, optimistic orphan adoption, creating missing history, minting recovery identity, approving recovery.complete, or tests/fault. Hard dependencies: task-b6e3dd2af916490fb2bc4d375a530683 (installed quiesced restore), task-8a01c025b65246aca09e69d82e009587 (current incarnation fence), and task-0325dcf7ee744123b40cf583230c7b6a (node-side inventory). Owned paths: apps/daemon/src/recovery/effect-inventory.* plus minimal work/effect admission and daemon export wiring. Verification: pnpm --filter @moe/daemon typecheck \u0026\u0026 pnpm --filter @moe/daemon test.","status":"WORKING","reopenCount":0,"reopenReason":"Human-directed unblock after live dependency re-measurement: task-b6e3dd2 and task-8a01c025 are DONE; archived SPIDR shell task-0325dcf7 explicitly records its four implementation children task-00956ac3, task-091c93db, task-d7da9be4, and task-f6c9011 as DONE and the production inventory surface delivered. Hand planning to architects; re-measure current public symbols and call sites before planning.","rejectionDetails":null,"definitionOfDone":["The production coordinator derives the required class set from daemon configuration and demands one cursor/incarnation-bound COMPLETE coverage proof for provider/process/lock, resource, workspace, integration, Git, and artifact classes; omitted, duplicate, unknown, or extra class evidence cannot satisfy coverage.","Every post-N item is deterministically classified as proven absent, safely adopted only to an exact restored intent under the current incarnation, or quarantined as an orphan; an unmatched item is never inserted into restored history or automatically repeated.","Missing inventory, adapter/coverage uncertainty, non-fenceable unknown state, artifact/key proof failure, or an unresolved item persists truth `UNKNOWN` and returns stable `UNKNOWN_TRUTH` at layer `RECOVERY_INVENTORY`, with the precise upstream adapter code retained.","Daemon work-claim and effect-activation production paths consult the persisted project recovery embargo before allocating authority; unresolved/unknown inventory returns `RECOVERY_RECONCILIATION_REQUIRED` from layer `RECOVERY_INVENTORY` with no attempt, slot, lease, effect, or spend.","The frozen canonical coverage/reconciliation record binds project, backup cursor/generation, current incarnation/key epoch, configured class set, source proof digests, item dispositions, and quarantine references; any byte change alters or invalidates its digest.","Focused tests drive production runner/scheduler/store surfaces, assert all seven configured classes generated, pin exact code/layer precedence, and mutation-check the production embargo branch.","Fresh `pnpm --filter @moe/daemon typecheck \u0026\u0026 pnpm --filter @moe/daemon test` exits 0."],"implementationPlan":[{"stepId":"step-1","title":null,"description":"RE-MEASURE AT THE THEN-CURRENT HEAD. The description\u0027s gaps date from HEAD 1ce0059 and four prerequisite tasks have landed since, so every claim is stale by default and must be re-run in BOTH directions. No production bytes written.\n\n1. `git rev-parse --short HEAD`; `ls apps/daemon/src/recovery/effect-inventory*` ÔÇö must still be ABSENT. If it exists, a peer started this; STOP and re-plan.\n2. CARDINALITY, the crux the prior architect blocked on. Read recovery-inventory-contract.ts and record BOTH arrays literally: RECOVERY_PROOF_CLASSES (expect SIX) and RECOVERY_INVENTORY_POPULATIONS (expect SEVEN), plus RECOVERY_CLASS_POPULATION_ROWS which joins them. DoD 6\u0027s \u0027seven configured classes\u0027 conflates the two axes; there are six CLASSES and seven POPULATIONS, and PROVIDER_PROCESS_LAUNCH_LOCK covers two populations. Do NOT invent a seventh class and do NOT duplicate a combined proof to reach seven.\n3. VERIFY DoD 4 IS ALREADY SATISFIED, by reading not editing: activation-ingress.ts:286 runEffectActivateCommand reads readActivationEmbargo at :299 BEFORE claimStage at :303, and claimStage calls claimWork from ../work/work-claim.js. activation-embargo.ts:32 pins ACTIVATION_EMBARGO_CODE = \"RECOVERY_RECONCILIATION_REQUIRED\". Record this as satisfied. If it is NOT, report blocked naming task-e33747f9 ÔÇö do not edit that directory.\n4. Confirm the open gap: restart-reconciliation.ts:235 reconcileOnRestart still iterates request.attempts at :241 (caller-supplied). This is the work.\n5. VERIFY THE DEPENDENCY EDGE to @moe/runner\u0027s node-side inventory surface by COMPILED PROBE with a positive and a NEGATIVE control, probe deleted in the same command via a shell trap. A barrel grep is not sufficient and can produce a false BLOCKED ÔÇö the runner re-exports through differently-named surface files.\n6. `grep -c \u0027\u0027` the modules you will compose, and record which are already over the 250 target (ledger 289, contract 287) so you do not add to them.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-2","title":null,"description":"TDD ÔÇö WRITE THE COORDINATOR\u0027S CARDINALITY AND COVERAGE TESTS FIRST. Create `apps/daemon/src/recovery/effect-inventory.test.ts`.\n\nASSERT BOTH CARDINALITIES, and assert them so they cannot go vacuous:\n- The SIX proof classes by literal NAME, with the count derived from the imported RECOVERY_PROOF_CLASSES.\n- The SEVEN populations by literal NAME, count derived from RECOVERY_INVENTORY_POPULATIONS.\n- The class-to-population mapping covers every population exactly once across the six rows ÔÇö this is what proves seven collapses to six by the frozen mapping rather than by a duplicated proof.\nDeriving the EXPECTED NAMES from the same array you are testing compares the array with itself and survives any rename or addition; pin the names literally and derive only the count.\n\nCOVERAGE CASES (DoD 1): one COMPLETE cursor/incarnation-bound proof per class satisfies; evidence that OMITS a class, DUPLICATES one, names an UNKNOWN one, or supplies an EXTRA one each fails with its specific stable code. recovery-inventory-record.ts:98-105 already returns CLASS_EXTRA, CLASS_UNKNOWN and CLASS_OMITTED ÔÇö assert those production codes, do not invent new ones.\n\nFAIL-CLOSED CASES (DoD 3): missing inventory, adapter/coverage uncertainty, non-fenceable unknown state, artifact/key proof failure and an unresolved item each answer UNKNOWN_TRUTH at layer RECOVERY_INVENTORY while RETAINING the precise upstream code. Assert the coordinator code AND the retained upstream tuple AND which layer answered first ÔÇö more than one layer can refuse, so \u0027it refused\u0027 is one layer away from vacuous.\n\nAN EMPTY ENUMERATOR MUST NOT READ AS CLEAR. Include a case where nothing is enumerated and require UNKNOWN_TRUTH, not coverage. \u0027We measured nothing\u0027 and \u0027we measured everything and it was empty\u0027 are the two readings this test keeps apart.\n\nThese fail until step 5 lands; that is the intended order.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-3","title":null,"description":"THE ITEM JOIN AND DISPOSITION CLASSIFIER (DoD 2) ÔÇö the one genuinely new authority in this task. Create `apps/daemon/src/recovery/effect-inventory-join.ts`, separate from the coordinator entry so both stay under 250 lines.\n\nEvery post-N item is deterministically classified into exactly one disposition:\n- PROVEN ABSENT ÔÇö the item is demonstrably gone, with the adapter proof that shows it.\n- SAFELY ADOPTED ÔÇö ONLY when it joins an EXACT restored intent under the CURRENT incarnation. Both qualifiers are load-bearing: an exact match under a PRIOR incarnation is NOT adoptable, and neither is a near match.\n- QUARANTINED ÔÇö an orphan, or anything the first two cannot establish.\n\nTWO HARD PROHIBITIONS, both from DoD 2: an unmatched item is NEVER inserted into restored history, and it is NEVER automatically repeated. Adoption may only ever bind to history that already exists.\n\nDeterminism is required, not incidental: the same inputs must produce the same dispositions in the same canonical order, because a digest is computed over the result in step 5. Order the output by the canonical class order the contract already fixes (recovery-inventory-contract.ts:68 states row order is canonical).\n\nRE-DECLARE NOTHING. Proof classes, populations, the UNKNOWN_TRUTH code and the RECOVERY_INVENTORY layer all come from recovery-inventory-contract.ts. Node-side classes come from @moe/runner\u0027s package ROOT by bare specifier ÔÇö never a deep path (TS6059, no escape hatch).\n\nAdd the `.js` bridge: bytes exactly `export * from \"./effect-inventory-join.ts\";` plus LF.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-4","title":null,"description":"JOIN TESTS (DoD 2), written to separate the cases that look alike. Create `apps/daemon/src/recovery/effect-inventory-join.test.ts`.\n\nThe three dispositions each get positive coverage, but the cases that matter are the NEGATIVE ones around adoption, because adoption is the only disposition that grants anything:\n- An item matching an exact restored intent under the CURRENT incarnation -\u003e ADOPTED.\n- An item matching an exact intent under a PRIOR incarnation -\u003e QUARANTINED, not adopted. Assert the code, and assert it is the incarnation that decided.\n- An item matching NO intent -\u003e QUARANTINED.\n- An item whose match cannot be established because the adapter was uncertain -\u003e UNKNOWN_TRUTH with the upstream code retained, NOT quarantined-by-default. Uncertainty and orphanhood are different facts.\n\nAssert that an unmatched item produced NO insertion into restored history and NO repeat ÔÇö the two prohibitions DoD 2 names. These are absence assertions, so state what must NOT be present explicitly rather than inferring it from a passing happy path.\n\nDETERMINISM: run the join twice over the same inputs and assert byte-identical ordered output, since step 5 digests it. Also run it over a shuffled input set and assert the same canonical order comes back ÔÇö an order that depends on input order would make the digest unstable and the record unverifiable.\n\nDrive PRODUCTION runner/scheduler surfaces (DoD 6), never a fixture that reimplements them: a property asserted against a test helper proves only that the helper agrees with itself.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-5","title":null,"description":"THE COORDINATOR ENTRY (DoD 1, 3, 5). Create `apps/daemon/src/recovery/effect-inventory.ts`, composing what already exists rather than rebuilding it.\n\nFlow:\n1. Derive the required class set from daemon configuration and demand ONE cursor/incarnation-bound COMPLETE coverage proof for each of the six proof classes. The class-set validation already exists ÔÇö recovery-inventory-record.ts:98-105 returns CLASS_EXTRA / CLASS_UNKNOWN / CLASS_OMITTED ÔÇö so CALL it; do not write a second validator.\n2. Run the step-3 join over the post-N items.\n3. Build the frozen canonical record via `buildRecoveryReconciliationRecord` (recovery-inventory-record.ts:126) binding project, backup cursor/generation, current incarnation/key epoch, configured class set, source proof digests, item dispositions and quarantine references (DoD 5).\n4. Persist via `recordRecoveryReconciliation` (recovery-inventory-ledger.ts:144); read back with `readRecoveryReconciliation` (:242).\n5. Answer UNKNOWN_TRUTH at RECOVERY_INVENTORY on any missing inventory, adapter/coverage uncertainty, non-fenceable unknown state, artifact/key proof failure, or unresolved item ÔÇö RETAINING the precise upstream code. recovery-inventory-contract.ts:106 already warns that flattening an upstream refusal erases which layer refused; :268 states the coordinator answer is always UNKNOWN_TRUTH at that layer.\n\nDO NOT TOUCH apps/daemon/src/activation/**. DoD 4 is already satisfied there (verified in step 1) and task-e33747f9 owns it while WORKING.\n\nAdd the `.js` bridge. Keep under 250 lines ÔÇö if it overruns, move composition helpers into effect-inventory-join.ts rather than trimming behaviour. Run the step-2 tests; they must now pass.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-6","title":null,"description":"MUTATION DRILLS (DoD 6, which requires mutation-checking the production embargo branch). Capture `sha256sum` of each target BEFORE mutating; restore by Edit and verify by `sha256sum` against the captured hash. NEVER `git checkout` (reverts to HEAD, destroying this task\u0027s uncommitted work) and never accept an empty `git diff` as proof ÔÇö it is blind to untracked paths and every new file here is untracked.\n\nRequired drills, each naming the test that must redden and the assertion it reddens on:\n- D1 EMBARGO BRANCH. Invert the coordinator\u0027s fail-closed answer so an unresolved or unknown inventory reports clear instead of holding. A named test must redden on the UNKNOWN_TRUTH assertion. This is the drill DoD 6 names explicitly and it is the one that matters most: the inverted branch is what would let a broken recovery authorise execution.\n- D2 EMPTY ENUMERATOR. Make an empty item set report COMPLETE coverage instead of UNKNOWN. The empty-enumerator test must redden. If it stays green, the coordinator is treating \u0027measured nothing\u0027 as \u0027nothing to measure\u0027.\n- D3 INCARNATION QUALIFIER. Drop the current-incarnation check from the adoption path so a prior-incarnation match adopts. The prior-incarnation join test must redden ÔÇö this is the guard that separates a safe adoption from resurrecting a dead run.\n- D4 CLASS SET. Remove one member from the required class set so five classes satisfy coverage. The CLASS_OMITTED test must redden.\n- D5 UPSTREAM CODE. Restamp a carried upstream code as UNKNOWN_TRUTH instead of retaining it. The test asserting the retained upstream tuple must redden ÔÇö task rail 3 forbids restamping and this proves the test enforces it.\n\nConfirm each drill reddens ON ITS TARGET ASSERTION, not on a crash: a crash proves the module was reached, not that the assertion is load-bearing.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-7","title":null,"description":"GATE AND STRUCTURAL CHECKS (DoD 7), each leg\u0027s exit code read SEPARATELY ÔÇö a `\u0026\u0026`-chained run reports one status for two legs and a piped run reports the pipe\u0027s tail:\n1. `pnpm --filter @moe/daemon typecheck; echo \"TYPECHECK_EXIT=$?\"`\n2. `pnpm --filter @moe/daemon test; echo \"TEST_EXIT=$?\"`\n\nStructural, all mechanical:\n- Every new PRODUCTION module has its sibling `.js` bridge with bytes exactly `export * from \"./\u003cbasename\u003e.ts\";` plus LF. A missing bridge is caught by NEITHER tsc NOR vitest, so it ships silently. Test files get none ÔÇö that is the measured convention (recovery-test-fixtures.ts and restore-test-harness.ts have no bridges).\n- `grep -c \u0027\u0027` each new production file: under 250. Do NOT add lines to recovery-inventory-ledger.ts (289) or recovery-inventory-contract.ts (287) ÔÇö both are already over target and pre-existing, and growing them is a new violation attributable to this task.\n- `apps/daemon/src/index.ts` unchanged unless a consumer genuinely needs the root export; index-surface.test.ts:267 pins EXPECTED_EXPORTS.length to 61, so any publish must update that count deliberately and is not a silent edit.\n- `git status --porcelain apps/daemon/src/activation/` must show NOTHING attributable to this task.\n\nFOREIGN RED: the worktree is shared and task-e33747f9 is WORKING in apps/daemon/src/activation/. Apply the path-attributed-baseline rail ÔÇö repo-wide failures at the merge-base, at HEAD, subtract, intersect with apps/daemon/src/recovery/effect-inventory*. That intersection must be EMPTY. Disclose any foreign red verbatim with exact failing lines and paths; never repair it, never hide it, never fabricate green.","status":"PENDING","note":null,"modifiedFiles":null},{"stepId":"step-8","title":null,"description":"ADVERSARIAL SELF-REVIEW, COMMIT, HANDOFF. Invoke the adversarial-self-review skill, then work the checklist aimed at how THIS task fails while looking finished:\n\n1. SEVEN VS SIX. Is there any invented seventh proof class, or a combined proof duplicated to reach seven? The frozen answer is six classes and seven populations joined by RECOVERY_CLASS_POPULATION_ROWS. The prior architect blocked this task once over exactly this; do not reintroduce it.\n2. Does any cardinality assertion derive its expected NAMES from the array it is testing? That compares the array with itself and survives every rename and addition.\n3. Can the coordinator ever report clear from an absence of evidence? Missing inventory, an empty enumerator, an unreadable store and adapter uncertainty must ALL hold and answer UNKNOWN_TRUTH. Epic rail 4: unverifiable evidence never gains authority.\n4. Is any upstream code restamped rather than retained? Task rail 3, and recovery-inventory-contract.ts:106 says so in production.\n5. Can an unmatched item reach restored history, or be automatically repeated? Both are forbidden by DoD 2.\n6. Was any authority re-implemented that recovery-inventory-record/-ledger/-proofs/-invariants already owns? A second class-set validator or a second record builder is a silent duplicate.\n7. Was apps/daemon/src/activation/** touched? It must not be ÔÇö DoD 4 is already satisfied there and the directory belongs to a live task.\n8. Any probe, scratch or generated evidence file left behind? Epic rail 5 ÔÇö the step-1 dependency probe must be long gone.\n\nCOMMIT BY EXPLICIT PATHSPEC naming only the `apps/daemon/src/recovery/effect-inventory*` paths. Never `git add -A` (epic rail 3). If a foreign whole-tree commit already swept your bytes in, do NOT amend, reset or re-commit to claim it ÔÇö hand QA the base-ref diff over the owned paths.\n\nHANDOFF must record: that DoD 4 was verified as already satisfied by task-e33747f9 at activation-ingress.ts:299 rather than implemented here; that DoD 6\u0027s \u0027seven configured classes\u0027 is six classes plus seven populations under the frozen mapping; and that this task is the durable consumer of task-0325dcf7ee744123b40cf583230c7b6a\u0027s node-side inventory surface (task rail 4), naming the call site that imports it from the @moe/runner root.","status":"PENDING","note":null,"modifiedFiles":null}],"taskRails":["Authoritative design is D:/projexts/moes/docs/plans/2026-08-05-moe-rebuild-design.md SHA-256 1D9D1EC97D3F07247FBBC088045E0BA2FD6DA8307F10A9026C55106419383191; do not edit it.","Human optimism, an empty enumerator, or a fixture-provided list is not a complete production inventory. Missing/unverifiable coverage stays UNKNOWN and effect authority remains unavailable.","Where runner/scheduler/daemon can refuse, tests assert the exact code and the layer that answered first; carried upstream codes must not be restamped.","This task is the durable consumer of task-0325dcf7ee744123b40cf583230c7b6a; it must import and call that production surface.","Target \u003c=250 physical lines per production source and split before 400; this cap is per file."]},"planningNotes":{"approachesConsidered":"THE PRIOR ARCHITECT\u0027S BLOCK IS NOW RESOLVED, AND THE CARDINALITY CONTRADICTION WAS NOT A CONTRADICTION. architect-a42f4965 blocked this at HEAD c0c5929 with an exact conflict: \u0027design line 978 names 7 populations; CORE-S14 names 6 categories; runner contract has 4 combined classes; adding resource yields 5 proof classes; description lists 6; DoD says 7. Do not duplicate combined proofs to fabricate seven.\u0027 task-f6cf8d16 has since frozen the mapping and the answer is that SIX and SEVEN are both correct on DIFFERENT AXES:\n\n  RECOVERY_PROOF_CLASSES (6)  = PROVIDER_PROCESS_LAUNCH_LOCK, RESOURCE, WORKSPACE, INTEGRATION_TARGET, GIT_INTEGRATION_ON_DISK, ARTIFACT_OBJECT_STAGING\n  RECOVERY_INVENTORY_POPULATIONS (7) = EFFECT_LOCK_WRAPPER_REGISTRATION, PROVIDER_RUN, RESOURCE, PROJECT_TAGGED_WORKSPACE, INTEGRATION_TARGET, GIT_BRANCH_REF, ARTIFACT_STAGING\n  RECOVERY_CLASS_POPULATION_ROWS joins them; PROVIDER_PROCESS_LAUNCH_LOCK covers TWO populations, which is where 7 collapses to 6.\n\nSo DoD 1\u0027s six named items are the PROOF CLASSES and DoD 6\u0027s \u0027seven configured classes\u0027 conflates populations with classes. The reconciliation the prior architect asked for does NOT require narrowing anything: the tests assert BOTH cardinalities, each derived from its own imported production array. That satisfies both numbers honestly and is strictly stronger than picking one. No proof is duplicated to fabricate seven ÔÇö the many-to-one row is the production mapping, not a fabrication.\n\nMOST OF DoD 1, 3 AND 5 IS ALREADY LANDED and must be composed, not rebuilt. task-f6cf8d16 shipped recovery-inventory-contract.ts (287), -record.ts (188, with CLASS_EXTRA / CLASS_UNKNOWN / CLASS_OMITTED already implementing DoD 1\u0027s \u0027omitted, duplicate, unknown, or extra cannot satisfy coverage\u0027), -ledger.ts (289), -proofs.ts (166), -invariants.ts (186), -reader.ts, -codec.ts, -subject.ts. Re-implementing any of it is the silent-duplicate defect.\n\nDoD 4 IS ALREADY SATISFIED BY ANOTHER TASK, verified on disk rather than assumed: apps/daemon/src/activation/activation-ingress.ts:286 runEffectActivateCommand reads readActivationEmbargo(store, request.projectId) at :299 BEFORE claimStage at :303, and claimStage calls claimWork imported from ../work/work-claim.js at :7. So both the work-claim and effect-activation paths consult the persisted embargo before allocating authority, and ACTIVATION_EMBARGO_CODE is exactly RECOVERY_RECONCILIATION_REQUIRED (activation-embargo.ts:32). That file belongs to task-e33747f9, which is still WORKING ÔÇö so this plan VERIFIES DoD 4 and does not touch apps/daemon/src/activation/**.\n\nWHAT IS ACTUALLY LEFT is DoD 2: the coordinator itself. apps/daemon/src/recovery/effect-inventory.* does not exist, and restart-reconciliation.ts:235 reconcileOnRestart still iterates request.attempts at :241 ÔÇö caller-supplied, which is precisely the original gap and the one thing none of the four prerequisites closed.\n\nREJECTED: editing apps/daemon/src/activation/** to \u0027complete\u0027 DoD 4. It is already done, it belongs to a live task, and a second edit there is the shared-worktree collision epic rail 3 prevents.","codebaseInsights":"RE-MEASURED AT HEAD a967199. The description\u0027s gap claims are from HEAD 1ce0059 and the four prerequisite tasks have landed since, so every claim was re-run in both directions.\n\nGAP CLAIM \u0027no service enumerates configured classes\u0027 ÔÇö PARTLY CLOSED. `grep -rn RECOVERY_PROOF_CLASSES apps/daemon/src/ | grep -v \u0027\\.test\\.\u0027` shows real production consumers: recovery-inventory-invariants.ts:3,28; -proofs.ts:2,3,73,76,140; -reader.ts:3,7,108,113,158,201,204,252; -record.ts:2,98,101,105,166. The class SET is validated in production (record.ts:98-105 returns CLASS_EXTRA, CLASS_UNKNOWN, CLASS_OMITTED). What is still absent is the coordinator that DEMANDS one COMPLETE coverage proof per class and joins items.\n\nGAP CLAIM \u0027daemon reconcileOnRestart trusts caller-supplied attempts\u0027 ÔÇö STILL OPEN and it is this task\u0027s core work. restart-reconciliation.ts:235 `reconcileOnRestart(request)`, :241 `for (const attempt of request.attempts)`, with `attempts: readonly InFlightAttempt[]` declared at :61.\n\nGAP CLAIM \u0027no production claim gate reads a disaster-recovery embargo\u0027 ÔÇö CLOSED. activation-ingress.ts:299 reads the embargo before the four-leg claim at :303-312 (arm, slot, budget stages follow). activation-embargo.ts:32 `ACTIVATION_EMBARGO_CODE = \"RECOVERY_RECONCILIATION_REQUIRED\"`, with ActivationEmbargoCause, ActivationEmbargoUpstream and an unreadableStore() path at :92 that holds the embargo when the store cannot be read ÔÇö fail-closed, as epic rail 4 requires.\n\nTHE COORDINATOR IS ABSENT: `ls apps/daemon/src/recovery/effect-inventory*` -\u003e \u0027No such file or directory\u0027. This is absence-is-the-work, confirmed rather than assumed.\n\nTHE FROZEN VOCABULARY THE COORDINATOR MUST USE, not re-declare:\n- recovery-inventory-contract.ts:24 `RECOVERY_COORDINATOR_UNKNOWN_CODE = \"UNKNOWN_TRUTH\"`, :25 `RECOVERY_INVENTORY_LAYER = \"RECOVERY_INVENTORY\"` ÔÇö exactly the code/layer DoD 3 demands.\n- :36 RECOVERY_PROOF_CLASSES (6), :56 RECOVERY_INVENTORY_POPULATIONS (7), :69 RECOVERY_CLASS_POPULATION_ROWS (ordered canonically, \u0027order is canonical\u0027 per :68).\n- recovery-inventory-contract.ts:268 states the coordinator answer is ALWAYS UNKNOWN_TRUTH at RECOVERY_INVENTORY and :106 warns that flattening an upstream refusal into UNKNOWN_TRUTH \u0027would erase which layer refused and why\u0027 ÔÇö the carried-upstream-code rule is already written down.\n\nSURFACES TO COMPOSE:\n- recovery-inventory-record.ts:126 `buildRecoveryReconciliationRecord(...)` -\u003e RecoveryReconciliationBuilt (:41).\n- recovery-inventory-ledger.ts:144 `recordRecoveryReconciliation(...)`, :242 `readRecoveryReconciliation(...)`, with RecoveryReconciliationExternalFacts (:43), RecoveryDurableReconcileRequest (:52), RecoveryReconciliationRecorded (:59), RecoveryReconciliationFound (:68).\n- Node-side classes come from @moe/runner: packages/runner/src/recovery-inventory/recovery-inventory-contract.ts:24 RECOVERY_INVENTORY_CLASSES (4) with layer INVENTORY_ADAPTER, root-published via surface/recovery-inventory-surface.ts:35.\n- Durable daemon classes: durable-recovery-inventory-contract.ts:33 DURABLE_RECOVERY_INVENTORY_CLASSES = [RESOURCE, INTEGRATION_TARGET], \u0027Ordered exactly as the daemon\u0027s frozen proof classes order them\u0027. 4 node-side + 2 durable = the 6 proof classes.\n\nLINE BUDGET: recovery-inventory-ledger.ts is 289 and recovery-inventory-contract.ts is 287 ÔÇö both already over the 250 target and under the 400 split point, both PRE-EXISTING and not this task\u0027s to fix. New coordinator files must come in under 250, which is why the join is a separate module from the entry point.\n\nCONVENTION: apps/daemon requires a physical `.js` bridge per production module (every .ts in src/recovery/ has one; the test-only recovery-test-fixtures.ts and restore-test-harness.ts do not). apps/daemon/src/index-surface.test.ts:267 pins EXPECTED_EXPORTS.length to 61.","risks":"DO NOT TOUCH apps/daemon/src/activation/**. task-e33747f9 is WORKING (5/8 steps, worker-fd8f822b) and owns that directory; its files were last written at 17:04. DoD 4 is ALREADY satisfied there ÔÇö verify it by reading activation-ingress.ts:299-312, do not re-implement it, and do not \u0027improve\u0027 it. A second worker editing that directory is the collision epic rail 3 exists to prevent. If the verification shows DoD 4 is NOT satisfied when the worker arrives, report blocked naming task-e33747f9 rather than editing it.\n\nTHE SEVEN-VS-SIX TRAP IS THE ONE THAT PRODUCES A WRONG PLAN. DoD 6 says \u0027all seven configured classes\u0027. There are SIX proof classes and SEVEN populations. Do NOT invent a seventh class, and do NOT duplicate a combined proof to reach seven ÔÇö the prior architect\u0027s comment names that failure explicitly. Assert BOTH cardinalities from their own production arrays.\n\nA CARDINALITY ASSERTION THAT DERIVES ITS EXPECTATION FROM THE ARRAY IT IS TESTING COMPARES THE ARRAY WITH ITSELF and survives any addition or deletion. Pin the six class NAMES and the seven population NAMES literally, and derive only the COUNT from the imported arrays, so both a rename and a silent addition redden.\n\nFAIL-CLOSED IS THE WHOLE POINT AND IT IS EASY TO INVERT. An empty enumerator, a missing adapter, or an unreadable store must all hold the embargo and answer UNKNOWN_TRUTH ÔÇö never \u0027no items found, therefore clear\u0027. \u0027We measured nothing\u0027 and \u0027we measured everything and it was empty\u0027 are the two readings the coverage class exists to keep apart; the runner\u0027s own countCoverage() makes an empty set UNKNOWN rather than vacuously COMPLETE, and the coordinator must inherit that. A test that only drives the happy path cannot see this inversion.\n\nCARRIED UPSTREAM CODES MUST NOT BE RESTAMPED (task rail 3, and recovery-inventory-contract.ts:106 says so in production). The coordinator answers UNKNOWN_TRUTH at RECOVERY_INVENTORY while RETAINING the precise upstream adapter code ÔÇö INVENTORY_ADAPTER from the runner, or the scheduler\u0027s own. Tests must assert the coordinator code AND the retained upstream tuple AND which layer answered first; more than one layer can refuse here, so an assertion of \u0027it refused\u0027 is one layer away from vacuous.\n\nADOPTION IS THE DANGEROUS DISPOSITION. DoD 2 permits adoption ONLY to an exact restored intent under the CURRENT incarnation. An unmatched item must never be inserted into restored history and never automatically repeated. The negative cases ÔÇö an item matching an intent from a PRIOR incarnation, and an item matching nothing ÔÇö both quarantine, and both need their own test; a plan that only tests successful adoption cannot distinguish them.\n\nSHARED WORKTREE, ACTIVE. Read any premise outside owned paths with `git show HEAD:\u003cpath\u003e`; a peer\u0027s in-flight drill flipped a file mid-session earlier today. Apply the path-attributed-baseline rail to any foreign red.\n\nCONSUMER EDGE (task rail 4): this task is the durable consumer of task-0325dcf7\u0027s node-side inventory surface and MUST import and call it from @moe/runner\u0027s package root ÔÇö published at surface/recovery-inventory-surface.ts. Verify the dependency edge by compiled probe with a negative control, not by a barrel grep: a grep for \u0027recovery-inventory\u0027 in packages/runner/src/index.ts can miss a re-export routed through a differently-named surface file, which is exactly how a false BLOCKED was nearly filed on the telemetry family this session.","keyFiles":["apps/daemon/src/recovery/recovery-inventory-contract.ts","apps/daemon/src/recovery/recovery-inventory-record.ts","apps/daemon/src/recovery/recovery-inventory-ledger.ts","apps/daemon/src/recovery/restart-reconciliation.ts","apps/daemon/src/activation/activation-ingress.ts","apps/daemon/src/activation/activation-embargo.ts","packages/runner/src/recovery-inventory/recovery-inventory-contract.ts"]},"nextAction":{"tool":"moe.start_step","args":{"taskId":"task-cf7fb147bd1c47698cbd65c9535370aa","stepId":"step-1","workerId":"worker-cf1f58d0"},"reason":"Begin step: RE-MEASURE AT THE THEN-CURRENT HEAD. The description\u0027s gaps date from HEAD 1ce00","recommendedSkill":{"name":"explore-before-assume","reason":"First step in code you have not verified. Load this before editing so you build on what actually exists, not assumptions."}}}
</claimed_task_context>

<inbox>
unread_general=10
unread_task=0
mentions=0 (see <routed_mentions> below if > 0)
memory_total=879 Serena memories (content via read_memory; names below are preloaded from disk - call list_memories only if they don't cover your area)
memory_this_task=task-task-cf7fb147bd1c47698cbd65c9535370aa-handoff
memory_recent=task-task-159f4c21ef9149e8a65f24735c9c1475-handoff pnpm-filter-on-a-missing-package-exits-zero decision-recovery-inventory-six-classes-seven-populations task-task-6f786c58cabf4f85be8ed4135e68a752-handoff gotcha-serial-spidr-family-promoted-in-parallel task-task-8e3076177f87458f934a776eca68ba16-handoff gotcha-a-landed-family-can-be-complete-and-unconsumed task-task-1eeb2dccce204671b442704cd60b38ad-handoff task-task-5dfc98fc3e7f4035a8012bd9ba032de3-handoff decision-embargo-owns-its-code-list-not-the-inventory-enum task-task-e33747f982e0452a9f9d784fd1cb914d-handoff gotcha-wiring-an-unused-accessor-can-be-circular task-task-6f58ca42f8c2497898a9ed2ea02c9632-handoff gotcha-peer-drill-tree-flips-an-architect-decision gotcha-delegation-seam-needs-its-own-drill task-task-70b6361dfd124e67bf70deecf78490c6-qa-verdict session-handoff-worker-40286572-starved-queue gotcha-in-process-daemon-seam-clock-breaks-whole-payload-equality gotcha-stale-block-premise-strands-an-approved-plan gotcha-session-start-git-status-ages-into-false-hazard
</inbox>

<pending_questions>
{"count":0,"totalMatches":0,"tasks":[],"pagination":{"limit":10,"returned":0,"total":0,"hasMore":false},"truncatedQuestions":0}
</pending_questions>

<system-reminder>
Skill recommendation for this task's current phase: explore-before-assume
Why: First step in code you have not verified. Load this before editing so you build on what actually exists, not assumptions.
Before you call moe.start_step, invoke the Skill tool:
  Skill(skill="explore-before-assume")
This is not optional. Do not rationalize skipping it ("I'm blocking, not planning", "this is trivial", "I already know what it says"). Skills evolve — load the current version.
If after loading you decide it truly does not apply here, say so explicitly in chat — but LOAD IT FIRST.
</system-reminder>
