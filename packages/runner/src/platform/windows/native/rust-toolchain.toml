# Pinned to the toolchain this crate was built and verified against.
# Measured on the project host: rustc 1.96.0 (ac68faa20 2026-05-25),
# cargo 1.96.0 (30a34c682 2026-05-25) — so this pin resolves locally and does
# not trigger a toolchain download.
[toolchain]
channel = "1.96.0"
